// Generated macro for impl_7 (impl)
macro_rules! Depcrate_addressimpl_7 {
() => {
// Module: crate::address
// Provides: {"impl_7"}
// Dependencies: {}
# [allow (non_snake_case)] # [wasm_bindgen] impl Address { # [doc = " Create a new Address object"] # [doc = ""] # [doc = " * `value` - optional public key as a base58 encoded string, `Uint8Array`, `[number]`"] # [wasm_bindgen (constructor)] pub fn constructor (value : JsValue) -> Result < Self , JsValue > { if let Some (base58_str) = value . as_string () { base58_str . parse :: < solana_address :: Address > () . map (Into :: into) . map_err (display_to_jsvalue) } else if let Some (uint8_array) = value . dyn_ref :: < Uint8Array > () { if uint8_array . length () as usize != ADDRESS_BYTES { return Err (std :: format ! ("Invalid Uint8Array length: expected {}, got {}" , ADDRESS_BYTES , uint8_array . length ()) . into ()) ; } let mut bytes = [0u8 ; ADDRESS_BYTES] ; uint8_array . copy_to (& mut bytes) ; Ok (solana_address :: Address :: new_from_array (bytes) . into ()) } else if let Some (array) = value . dyn_ref :: < Array > () { if array . length () as usize != ADDRESS_BYTES { return Err (std :: format ! ("Invalid Array length: expected {}, got {}" , ADDRESS_BYTES , array . length ()) . into ()) ; } let mut bytes = [0u8 ; ADDRESS_BYTES] ; let iterator = js_sys :: try_iter (& array . values ()) ? . expect ("array to be iterable") ; for (i , x) in iterator . enumerate () { let x = x ? ; if let Some (n) = x . as_f64 () { if n >= 0. && n <= 255. { bytes [i] = n as u8 ; continue ; } } return Err (std :: format ! ("Invalid array argument: {:?}" , x) . into ()) ; } Ok (solana_address :: Address :: new_from_array (bytes) . into ()) } else if value . is_undefined () { Ok (solana_address :: Address :: default () . into ()) } else { Err ("Unsupported argument" . into ()) } } # [doc = " Return the base58 string representation of the public key"] pub fn toString (& self) -> std :: string :: String { std :: string :: ToString :: to_string (& self . inner) } # [doc = " Check if a `Address` is on the ed25519 curve."] pub fn isOnCurve (& self) -> bool { self . inner . is_on_curve () } # [doc = " Checks if two `Address`s are equal"] pub fn equals (& self , other : & Self) -> bool { self . inner == other . inner } # [doc = " Return the `Uint8Array` representation of the public key"] pub fn toBytes (& self) -> std :: boxed :: Box < [u8] > { self . inner . to_bytes () . into () } # [doc = " Derive an Address from anothern Address, string seed, and a program id"] pub fn createWithSeed (base : & Self , seed : & str , owner : & Self) -> Result < Self , JsValue > { solana_address :: Address :: create_with_seed (& base . inner , seed , & owner . inner) . map (Into :: into) . map_err (display_to_jsvalue) } # [doc = " Derive a program address from seeds and a program id"] pub fn createProgramAddress (seeds : std :: boxed :: Box < [JsValue] > , program_id : & Self ,) -> Result < Self , JsValue > { let seeds_vec = js_value_to_seeds_vec (& seeds) ? ; let seeds_slice = seeds_vec . iter () . map (| seed | seed . as_slice ()) . collect :: < Vec < _ > > () ; solana_address :: Address :: create_program_address (seeds_slice . as_slice () , & program_id . inner) . map (Into :: into) . map_err (display_to_jsvalue) } # [doc = " Find a valid program address"] # [doc = ""] # [doc = " Returns:"] # [doc = " * `[Address, number]` - the program address and bump seed"] pub fn findProgramAddress (seeds : std :: boxed :: Box < [JsValue] > , program_id : & Self ,) -> Result < JsValue , JsValue > { let seeds_vec = js_value_to_seeds_vec (& seeds) ? ; let seeds_slice = seeds_vec . iter () . map (| seed | seed . as_slice ()) . collect :: < Vec < _ > > () ; let (address , bump_seed) = solana_address :: Address :: find_program_address (seeds_slice . as_slice () , & program_id . inner ,) ; let result = Array :: new_with_length (2) ; result . set (0 , Address :: from (address) . into ()) ; result . set (1 , bump_seed . into ()) ; Ok (result . into ()) } }
};
}
