// Generated macro for Field (trait)
macro_rules! DepcrateField {
() => {
// Module: crate
// Provides: {"Field"}
// Dependencies: {}
# [doc = " This trait represents an element of a field."] pub trait Field : Sized + Eq + Copy + Clone + Default + Send + Sync + fmt :: Debug + 'static + ConditionallySelectable + ConstantTimeEq + Neg < Output = Self > + Add < Output = Self > + Sub < Output = Self > + Mul < Output = Self > + Sum + Product + for < 'a > Add < & 'a Self , Output = Self > + for < 'a > Sub < & 'a Self , Output = Self > + for < 'a > Mul < & 'a Self , Output = Self > + for < 'a > Sum < & 'a Self > + for < 'a > Product < & 'a Self > + AddAssign + SubAssign + MulAssign + for < 'a > AddAssign < & 'a Self > + for < 'a > SubAssign < & 'a Self > + for < 'a > MulAssign < & 'a Self > { # [doc = " The zero element of the field, the additive identity."] const ZERO : Self ; # [doc = " The one element of the field, the multiplicative identity."] const ONE : Self ; # [doc = " Returns an element chosen uniformly at random using a user-provided RNG."] fn random (rng : impl RngCore) -> Self ; # [doc = " Returns true iff this element is zero."] fn is_zero (& self) -> Choice { self . ct_eq (& Self :: ZERO) } # [doc = " Returns true iff this element is zero."] # [doc = ""] # [doc = " # Security"] # [doc = ""] # [doc = " This method provides **no** constant-time guarantees. Implementors of the"] # [doc = " `Field` trait **may** optimise this method using non-constant-time logic."] fn is_zero_vartime (& self) -> bool { self . is_zero () . into () } # [doc = " Squares this element."] # [must_use] fn square (& self) -> Self ; # [doc = " Cubes this element."] # [must_use] fn cube (& self) -> Self { self . square () * self } # [doc = " Doubles this element."] # [must_use] fn double (& self) -> Self ; # [doc = " Computes the multiplicative inverse of this element,"] # [doc = " failing if the element is zero."] fn invert (& self) -> CtOption < Self > ; # [doc = " Computes:"] # [doc = ""] # [doc = " - $(\\textsf{true}, \\sqrt{\\textsf{num}/\\textsf{div}})$, if $\\textsf{num}$ and"] # [doc = "   $\\textsf{div}$ are nonzero and $\\textsf{num}/\\textsf{div}$ is a square in the"] # [doc = "   field;"] # [doc = " - $(\\textsf{true}, 0)$, if $\\textsf{num}$ is zero;"] # [doc = " - $(\\textsf{false}, 0)$, if $\\textsf{num}$ is nonzero and $\\textsf{div}$ is zero;"] # [doc = " - $(\\textsf{false}, \\sqrt{G_S \\cdot \\textsf{num}/\\textsf{div}})$, if"] # [doc = "   $\\textsf{num}$ and $\\textsf{div}$ are nonzero and $\\textsf{num}/\\textsf{div}$ is"] # [doc = "   a nonsquare in the field;"] # [doc = ""] # [doc = " where $G_S$ is a non-square."] # [doc = ""] # [doc = " # Warnings"] # [doc = ""] # [doc = " - The choice of root from `sqrt` is unspecified."] # [doc = " - The value of $G_S$ is unspecified, and cannot be assumed to have any specific"] # [doc = "   value in a generic context."] fn sqrt_ratio (num : & Self , div : & Self) -> (Choice , Self) ; # [doc = " Equivalent to `Self::sqrt_ratio(self, one())`."] # [doc = ""] # [doc = " The provided method is implemented in terms of [`Self::sqrt_ratio`]."] fn sqrt_alt (& self) -> (Choice , Self) { Self :: sqrt_ratio (self , & Self :: ONE) } # [doc = " Returns the square root of the field element, if it is"] # [doc = " quadratic residue."] # [doc = ""] # [doc = " The provided method is implemented in terms of [`Self::sqrt_ratio`]."] fn sqrt (& self) -> CtOption < Self > { let (is_square , res) = Self :: sqrt_ratio (self , & Self :: ONE) ; CtOption :: new (res , is_square) } # [doc = " Exponentiates `self` by `exp`, where `exp` is a little-endian order integer"] # [doc = " exponent."] # [doc = ""] # [doc = " # Guarantees"] # [doc = ""] # [doc = " This operation is constant time with respect to `self`, for all exponents with the"] # [doc = " same number of digits (`exp.as_ref().len()`). It is variable time with respect to"] # [doc = " the number of digits in the exponent."] fn pow < S : AsRef < [u64] > > (& self , exp : S) -> Self { let mut res = Self :: ONE ; for e in exp . as_ref () . iter () . rev () { for i in (0 .. 64) . rev () { res = res . square () ; let mut tmp = res ; tmp *= self ; res . conditional_assign (& tmp , (((* e >> i) & 1) as u8) . into ()) ; } } res } # [doc = " Exponentiates `self` by `exp`, where `exp` is a little-endian order integer"] # [doc = " exponent."] # [doc = ""] # [doc = " # Guarantees"] # [doc = ""] # [doc = " **This operation is variable time with respect to `self`, for all exponent.** If"] # [doc = " the exponent is fixed, this operation is effectively constant time. However, for"] # [doc = " stronger constant-time guarantees, [`Field::pow`] should be used."] fn pow_vartime < S : AsRef < [u64] > > (& self , exp : S) -> Self { let mut res = Self :: ONE ; for e in exp . as_ref () . iter () . rev () { for i in (0 .. 64) . rev () { res = res . square () ; if ((* e >> i) & 1) == 1 { res . mul_assign (self) ; } } } res } }
};
}
