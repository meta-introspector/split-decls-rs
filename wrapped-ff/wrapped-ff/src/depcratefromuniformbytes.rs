// Generated macro for FromUniformBytes (trait)
macro_rules! DepcrateFromUniformBytes {
() => {
// Module: crate
// Provides: {"FromUniformBytes"}
// Dependencies: {}
# [doc = " Trait for constructing a [`PrimeField`] element from a fixed-length uniform byte"] # [doc = " array."] # [doc = ""] # [doc = " \"Uniform\" means that the byte array's contents must be indistinguishable from the"] # [doc = " [discrete uniform distribution]. Suitable byte arrays can be obtained:"] # [doc = " - from a cryptographically-secure randomness source (which makes this constructor"] # [doc = "   equivalent to [`Field::random`])."] # [doc = " - from a cryptographic hash function output, which enables a \"random\" field element to"] # [doc = "   be selected deterministically. This is the primary use case for `FromUniformBytes`."] # [doc = ""] # [doc = " The length `N` of the byte array is chosen by the trait implementer such that the loss"] # [doc = " of uniformity in the mapping from byte arrays to field elements is cryptographically"] # [doc = " negligible."] # [doc = ""] # [doc = " [discrete uniform distribution]: https://en.wikipedia.org/wiki/Discrete_uniform_distribution"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[cfg(feature = \"derive\")] {"] # [doc = " # // Fake this so we don't actually need a dev-dependency on bls12_381."] # [doc = " # mod bls12_381 {"] # [doc = " #     use ff::{Field, PrimeField};"] # [doc = " #"] # [doc = " #     #[derive(PrimeField)]"] # [doc = " #     #[PrimeFieldModulus = \"52435875175126190479447740508185965837690552500527637822603658699938581184513\"]"] # [doc = " #     #[PrimeFieldGenerator = \"7\"]"] # [doc = " #     #[PrimeFieldReprEndianness = \"little\"]"] # [doc = " #     pub struct Scalar([u64; 4]);"] # [doc = " #"] # [doc = " #     impl ff::FromUniformBytes<64> for Scalar {"] # [doc = " #         fn from_uniform_bytes(_bytes: &[u8; 64]) -> Self {"] # [doc = " #             // Fake impl for doctest"] # [doc = " #             Scalar::ONE"] # [doc = " #         }"] # [doc = " #     }"] # [doc = " # }"] # [doc = " #"] # [doc = " use blake2b_simd::blake2b;"] # [doc = " use bls12_381::Scalar;"] # [doc = " use ff::FromUniformBytes;"] # [doc = ""] # [doc = " // `bls12_381::Scalar` implements `FromUniformBytes<64>`, and BLAKE2b (by default)"] # [doc = " // produces a 64-byte hash."] # [doc = " let hash = blake2b(b\"Some message\");"] # [doc = " let val = Scalar::from_uniform_bytes(hash.as_array());"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " # Implementing `FromUniformBytes`"] # [doc = ""] # [doc = " [`Self::from_uniform_bytes`] should always be implemented by interpreting the provided"] # [doc = " byte array as the little endian unsigned encoding of an integer, and then reducing that"] # [doc = " integer modulo the field modulus."] # [doc = ""] # [doc = " For security, `N` must be chosen so that `N * 8 >= Self::NUM_BITS + 128`. A larger"] # [doc = " value of `N` may be chosen for convenience; for example, for a field with a 255-bit"] # [doc = " modulus, `N = 64` is convenient as it matches the output length of several common"] # [doc = " cryptographic hash functions (such as SHA-512 and BLAKE2b)."] # [doc = ""] # [doc = " ## Trait design"] # [doc = ""] # [doc = " This trait exists because `PrimeField::from_uniform_bytes([u8; N])` cannot currently"] # [doc = " exist (trait methods cannot use associated constants in the const positions of their"] # [doc = " type signature, and we do not want `PrimeField` to require a generic const parameter)."] # [doc = " However, this has the side-effect that `FromUniformBytes` can be implemented multiple"] # [doc = " times for different values of `N`. Most implementations of [`PrimeField`] should only"] # [doc = " need to implement `FromUniformBytes` trait for one value of `N` (chosen following the"] # [doc = " above considerations); if you find yourself needing to implement it multiple times,"] # [doc = " please [let us know about your use case](https://github.com/zkcrypto/ff/issues/new) so"] # [doc = " we can take it into consideration for future evolutions of the `ff` traits."] pub trait FromUniformBytes < const N : usize > : PrimeField { # [doc = " Returns a field element that is congruent to the provided little endian unsigned"] # [doc = " byte representation of an integer."] fn from_uniform_bytes (bytes : & [u8 ; N]) -> Self ; }
};
}
