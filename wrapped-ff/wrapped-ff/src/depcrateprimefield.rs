// Generated macro for PrimeField (trait)
macro_rules! DepcratePrimeField {
() => {
// Module: crate
// Provides: {"PrimeField"}
// Dependencies: {}
# [doc = " This represents an element of a non-binary prime field."] pub trait PrimeField : Field + From < u64 > { # [doc = " The prime field can be converted back and forth into this binary"] # [doc = " representation."] type Repr : Copy + Default + Send + Sync + 'static + AsRef < [u8] > + AsMut < [u8] > ; # [doc = " Interpret a string of numbers as a (congruent) prime field element."] # [doc = " Does not accept unnecessary leading zeroes or a blank string."] # [doc = ""] # [doc = " # Security"] # [doc = ""] # [doc = " This method provides **no** constant-time guarantees."] fn from_str_vartime (s : & str) -> Option < Self > { if s . is_empty () { return None ; } if s == "0" { return Some (Self :: ZERO) ; } let mut res = Self :: ZERO ; let ten = Self :: from (10) ; let mut first_digit = true ; for c in s . chars () { match c . to_digit (10) { Some (c) => { if first_digit { if c == 0 { return None ; } first_digit = false ; } res . mul_assign (& ten) ; res . add_assign (& Self :: from (u64 :: from (c))) ; } None => { return None ; } } } Some (res) } # [doc = " Obtains a field element congruent to the integer `v`."] # [doc = ""] # [doc = " For fields where `Self::CAPACITY >= 128`, this is injective and will produce a"] # [doc = " unique field element."] # [doc = ""] # [doc = " For fields where `Self::CAPACITY < 128`, this is surjective; some field elements"] # [doc = " will be produced by multiple values of `v`."] # [doc = ""] # [doc = " If you want to deterministically sample a field element representing a value, use"] # [doc = " [`FromUniformBytes`] instead."] fn from_u128 (v : u128) -> Self { let lower = v as u64 ; let upper = (v >> 64) as u64 ; let mut tmp = Self :: from (upper) ; for _ in 0 .. 64 { tmp = tmp . double () ; } tmp + Self :: from (lower) } # [doc = " Attempts to convert a byte representation of a field element into an element of"] # [doc = " this prime field, failing if the input is not canonical (is not smaller than the"] # [doc = " field's modulus)."] # [doc = ""] # [doc = " The byte representation is interpreted with the same endianness as elements"] # [doc = " returned by [`PrimeField::to_repr`]."] fn from_repr (repr : Self :: Repr) -> CtOption < Self > ; # [doc = " Attempts to convert a byte representation of a field element into an element of"] # [doc = " this prime field, failing if the input is not canonical (is not smaller than the"] # [doc = " field's modulus)."] # [doc = ""] # [doc = " The byte representation is interpreted with the same endianness as elements"] # [doc = " returned by [`PrimeField::to_repr`]."] # [doc = ""] # [doc = " # Security"] # [doc = ""] # [doc = " This method provides **no** constant-time guarantees. Implementors of the"] # [doc = " `PrimeField` trait **may** optimise this method using non-constant-time logic."] fn from_repr_vartime (repr : Self :: Repr) -> Option < Self > { Self :: from_repr (repr) . into () } # [doc = " Converts an element of the prime field into the standard byte representation for"] # [doc = " this field."] # [doc = ""] # [doc = " The endianness of the byte representation is implementation-specific. Generic"] # [doc = " encodings of field elements should be treated as opaque."] fn to_repr (& self) -> Self :: Repr ; # [doc = " Returns true iff this element is odd."] fn is_odd (& self) -> Choice ; # [doc = " Returns true iff this element is even."] # [inline (always)] fn is_even (& self) -> Choice { ! self . is_odd () } # [doc = " Modulus of the field written as a string for debugging purposes."] # [doc = ""] # [doc = " The encoding of the modulus is implementation-specific. Generic users of the"] # [doc = " `PrimeField` trait should treat this string as opaque."] const MODULUS : & 'static str ; # [doc = " How many bits are needed to represent an element of this field."] const NUM_BITS : u32 ; # [doc = " How many bits of information can be reliably stored in the field element."] # [doc = ""] # [doc = " This is usually `Self::NUM_BITS - 1`."] const CAPACITY : u32 ; # [doc = " Inverse of $2$ in the field."] const TWO_INV : Self ; # [doc = " A fixed multiplicative generator of `modulus - 1` order. This element must also be"] # [doc = " a quadratic nonresidue."] # [doc = ""] # [doc = " It can be calculated using [SageMath] as `GF(modulus).primitive_element()`."] # [doc = ""] # [doc = " Implementations of this trait MUST ensure that this is the generator used to"] # [doc = " derive `Self::ROOT_OF_UNITY`."] # [doc = ""] # [doc = " [SageMath]: https://www.sagemath.org/"] const MULTIPLICATIVE_GENERATOR : Self ; # [doc = " An integer `s` satisfying the equation `2^s * t = modulus - 1` with `t` odd."] # [doc = ""] # [doc = " This is the number of leading zero bits in the little-endian bit representation of"] # [doc = " `modulus - 1`."] const S : u32 ; # [doc = " The `2^s` root of unity."] # [doc = ""] # [doc = " It can be calculated by exponentiating `Self::MULTIPLICATIVE_GENERATOR` by `t`,"] # [doc = " where `t = (modulus - 1) >> Self::S`."] const ROOT_OF_UNITY : Self ; # [doc = " Inverse of [`Self::ROOT_OF_UNITY`]."] const ROOT_OF_UNITY_INV : Self ; # [doc = " Generator of the `t-order` multiplicative subgroup."] # [doc = ""] # [doc = " It can be calculated by exponentiating [`Self::MULTIPLICATIVE_GENERATOR`] by `2^s`,"] # [doc = " where `s` is [`Self::S`]."] const DELTA : Self ; }
};
}
