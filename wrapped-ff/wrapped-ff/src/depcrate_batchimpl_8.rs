// Generated macro for impl_8 (impl)
macro_rules! Depcrate_batchimpl_8 {
() => {
// Module: crate::batch
// Provides: {"impl_8"}
// Dependencies: {}
impl BatchInverter { # [doc = " Inverts each field element in `elements` (when nonzero). Zero-valued elements are"] # [doc = " left as zero."] # [doc = ""] # [doc = " - `scratch_space` is a slice of field elements that can be freely overwritten."] # [doc = ""] # [doc = " Returns the inverse of the product of all nonzero field elements."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `elements.len() != scratch_space.len()`."] pub fn invert_with_external_scratch < F > (elements : & mut [F] , scratch_space : & mut [F]) -> F where F : Field + ConstantTimeEq , { assert_eq ! (elements . len () , scratch_space . len ()) ; let mut acc = F :: ONE ; for (p , scratch) in elements . iter () . zip (scratch_space . iter_mut ()) { * scratch = acc ; acc = F :: conditional_select (& (acc * * p) , & acc , p . is_zero ()) ; } acc = acc . invert () . unwrap () ; let allinv = acc ; for (p , scratch) in elements . iter_mut () . zip (scratch_space . iter ()) . rev () { let tmp = * scratch * acc ; let skip = p . is_zero () ; acc = F :: conditional_select (& (acc * * p) , & acc , skip) ; * p = F :: conditional_select (& tmp , & p , skip) ; } allinv } # [doc = " Inverts each field element in `items` (when nonzero). Zero-valued elements are"] # [doc = " left as zero."] # [doc = ""] # [doc = " - `element` is a function that extracts the element to be inverted from `items`."] # [doc = " - `scratch_space` is a function that extracts the scratch space from `items`."] # [doc = ""] # [doc = " Returns the inverse of the product of all nonzero field elements."] pub fn invert_with_internal_scratch < F , T , TE , TS > (items : & mut [T] , element : TE , scratch_space : TS ,) -> F where F : Field + ConstantTimeEq , TE : Fn (& mut T) -> & mut F , TS : Fn (& mut T) -> & mut F , { let mut acc = F :: ONE ; for item in items . iter_mut () { * (scratch_space) (item) = acc ; let p = (element) (item) ; acc = F :: conditional_select (& (acc * * p) , & acc , p . is_zero ()) ; } acc = acc . invert () . unwrap () ; let allinv = acc ; for item in items . iter_mut () . rev () { let tmp = * (scratch_space) (item) * acc ; let p = (element) (item) ; let skip = p . is_zero () ; acc = F :: conditional_select (& (acc * * p) , & acc , skip) ; * p = F :: conditional_select (& tmp , & p , skip) ; } allinv } }
};
}
