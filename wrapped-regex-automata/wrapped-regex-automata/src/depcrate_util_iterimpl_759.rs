// Generated macro for impl_759 (impl)
macro_rules! Depcrate_util_iterimpl_759 {
() => {
// Module: crate::util::iter
// Provides: {"impl_759"}
// Dependencies: {}
impl < 'h > Searcher < 'h > { # [doc = " Create a new fallible non-overlapping matches iterator."] # [doc = ""] # [doc = " The given `input` provides the parameters (including the haystack),"] # [doc = " while the `finder` represents a closure that calls the underlying regex"] # [doc = " engine. The closure may borrow any additional state that is needed,"] # [doc = " such as a prefilter scanner."] pub fn new (input : Input < 'h >) -> Searcher < 'h > { Searcher { input , last_match_end : None } } # [doc = " Returns the current `Input` used by this searcher."] # [doc = ""] # [doc = " The `Input` returned is generally equivalent to the one given to"] # [doc = " [`Searcher::new`], but its start position may be different to reflect"] # [doc = " the start of the next search to be executed."] pub fn input < 's > (& 's self) -> & 's Input < 'h > { & self . input } # [doc = " Return the next half match for an infallible search if one exists, and"] # [doc = " advance to the next position."] # [doc = ""] # [doc = " This is like `try_advance_half`, except errors are converted into"] # [doc = " panics."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the given closure returns an error, then this panics. This is useful"] # [doc = " when you know your underlying regex engine has been configured to not"] # [doc = " return an error."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use a `Searcher` to iterate over all matches"] # [doc = " when using a DFA, which only provides \"half\" matches."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::dfa::DFA,"] # [doc = "     util::iter::Searcher,"] # [doc = "     HalfMatch, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let re = DFA::new(r\"[0-9]{4}-[0-9]{2}-[0-9]{2}\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let input = Input::new(\"2010-03-14 2016-10-08 2020-10-22\");"] # [doc = " let mut it = Searcher::new(input);"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 10));"] # [doc = " let got = it.advance_half(|input| re.try_search_fwd(&mut cache, input));"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 21));"] # [doc = " let got = it.advance_half(|input| re.try_search_fwd(&mut cache, input));"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 32));"] # [doc = " let got = it.advance_half(|input| re.try_search_fwd(&mut cache, input));"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " let expected = None;"] # [doc = " let got = it.advance_half(|input| re.try_search_fwd(&mut cache, input));"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " This correctly moves iteration forward even when an empty match occurs:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::dfa::DFA,"] # [doc = "     util::iter::Searcher,"] # [doc = "     HalfMatch, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let re = DFA::new(r\"a|\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let input = Input::new(\"abba\");"] # [doc = " let mut it = Searcher::new(input);"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 1));"] # [doc = " let got = it.advance_half(|input| re.try_search_fwd(&mut cache, input));"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 2));"] # [doc = " let got = it.advance_half(|input| re.try_search_fwd(&mut cache, input));"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 4));"] # [doc = " let got = it.advance_half(|input| re.try_search_fwd(&mut cache, input));"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " let expected = None;"] # [doc = " let got = it.advance_half(|input| re.try_search_fwd(&mut cache, input));"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn advance_half < F > (& mut self , finder : F) -> Option < HalfMatch > where F : FnMut (& Input < '_ >) -> Result < Option < HalfMatch > , MatchError > , { match self . try_advance_half (finder) { Ok (m) => m , Err (err) => panic ! ("unexpected regex half find error: {err}\n\
                 to handle find errors, use 'try' or 'search' methods" ,) , } } # [doc = " Return the next match for an infallible search if one exists, and"] # [doc = " advance to the next position."] # [doc = ""] # [doc = " The search is advanced even in the presence of empty matches by"] # [doc = " forbidding empty matches from overlapping with any other match."] # [doc = ""] # [doc = " This is like `try_advance`, except errors are converted into panics."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the given closure returns an error, then this panics. This is useful"] # [doc = " when you know your underlying regex engine has been configured to not"] # [doc = " return an error."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use a `Searcher` to iterate over all matches"] # [doc = " when using a regex based on lazy DFAs:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::regex::Regex,"] # [doc = "     util::iter::Searcher,"] # [doc = "     Match, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let re = Regex::new(r\"[0-9]{4}-[0-9]{2}-[0-9]{2}\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let input = Input::new(\"2010-03-14 2016-10-08 2020-10-22\");"] # [doc = " let mut it = Searcher::new(input);"] # [doc = ""] # [doc = " let expected = Some(Match::must(0, 0..10));"] # [doc = " let got = it.advance(|input| re.try_search(&mut cache, input));"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " let expected = Some(Match::must(0, 11..21));"] # [doc = " let got = it.advance(|input| re.try_search(&mut cache, input));"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " let expected = Some(Match::must(0, 22..32));"] # [doc = " let got = it.advance(|input| re.try_search(&mut cache, input));"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " let expected = None;"] # [doc = " let got = it.advance(|input| re.try_search(&mut cache, input));"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " This example shows the same as above, but with the PikeVM. This example"] # [doc = " is useful because it shows how to use this API even when the regex"] # [doc = " engine doesn't directly return a `Match`."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     util::iter::Searcher,"] # [doc = "     Match, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"[0-9]{4}-[0-9]{2}-[0-9]{2}\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " let input = Input::new(\"2010-03-14 2016-10-08 2020-10-22\");"] # [doc = " let mut it = Searcher::new(input);"] # [doc = ""] # [doc = " let expected = Some(Match::must(0, 0..10));"] # [doc = " let got = it.advance(|input| {"] # [doc = "     re.search(&mut cache, input, &mut caps);"] # [doc = "     Ok(caps.get_match())"] # [doc = " });"] # [doc = " // Note that if we wanted to extract capturing group spans, we could"] # [doc = " // do that here with 'caps'."] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " let expected = Some(Match::must(0, 11..21));"] # [doc = " let got = it.advance(|input| {"] # [doc = "     re.search(&mut cache, input, &mut caps);"] # [doc = "     Ok(caps.get_match())"] # [doc = " });"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " let expected = Some(Match::must(0, 22..32));"] # [doc = " let got = it.advance(|input| {"] # [doc = "     re.search(&mut cache, input, &mut caps);"] # [doc = "     Ok(caps.get_match())"] # [doc = " });"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " let expected = None;"] # [doc = " let got = it.advance(|input| {"] # [doc = "     re.search(&mut cache, input, &mut caps);"] # [doc = "     Ok(caps.get_match())"] # [doc = " });"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn advance < F > (& mut self , finder : F) -> Option < Match > where F : FnMut (& Input < '_ >) -> Result < Option < Match > , MatchError > , { match self . try_advance (finder) { Ok (m) => m , Err (err) => panic ! ("unexpected regex find error: {err}\n\
                 to handle find errors, use 'try' or 'search' methods" ,) , } } # [doc = " Return the next half match for a fallible search if one exists, and"] # [doc = " advance to the next position."] # [doc = ""] # [doc = " This is like `advance_half`, except it permits callers to handle errors"] # [doc = " during iteration."] # [inline] pub fn try_advance_half < F > (& mut self , mut finder : F ,) -> Result < Option < HalfMatch > , MatchError > where F : FnMut (& Input < '_ >) -> Result < Option < HalfMatch > , MatchError > , { let mut m = match finder (& self . input) ? { None => return Ok (None) , Some (m) => m , } ; if Some (m . offset ()) == self . last_match_end { m = match self . handle_overlapping_empty_half_match (m , finder) ? { None => return Ok (None) , Some (m) => m , } ; } self . input . set_start (m . offset ()) ; self . last_match_end = Some (m . offset ()) ; Ok (Some (m)) } # [doc = " Return the next match for a fallible search if one exists, and advance"] # [doc = " to the next position."] # [doc = ""] # [doc = " This is like `advance`, except it permits callers to handle errors"] # [doc = " during iteration."] # [inline] pub fn try_advance < F > (& mut self , mut finder : F ,) -> Result < Option < Match > , MatchError > where F : FnMut (& Input < '_ >) -> Result < Option < Match > , MatchError > , { let mut m = match finder (& self . input) ? { None => return Ok (None) , Some (m) => m , } ; if m . is_empty () && Some (m . end ()) == self . last_match_end { m = match self . handle_overlapping_empty_match (m , finder) ? { None => return Ok (None) , Some (m) => m , } ; } self . input . set_start (m . end ()) ; self . last_match_end = Some (m . end ()) ; Ok (Some (m)) } # [doc = " Given a closure that executes a single search, return an iterator over"] # [doc = " all successive non-overlapping half matches."] # [doc = ""] # [doc = " The iterator returned yields result values. If the underlying regex"] # [doc = " engine is configured to never return an error, consider calling"] # [doc = " [`TryHalfMatchesIter::infallible`] to convert errors into panics."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use a `Searcher` to create a proper"] # [doc = " iterator over half matches."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::dfa::DFA,"] # [doc = "     util::iter::Searcher,"] # [doc = "     HalfMatch, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let re = DFA::new(r\"[0-9]{4}-[0-9]{2}-[0-9]{2}\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let input = Input::new(\"2010-03-14 2016-10-08 2020-10-22\");"] # [doc = " let mut it = Searcher::new(input).into_half_matches_iter(|input| {"] # [doc = "     re.try_search_fwd(&mut cache, input)"] # [doc = " });"] # [doc = ""] # [doc = " let expected = Some(Ok(HalfMatch::must(0, 10)));"] # [doc = " assert_eq!(expected, it.next());"] # [doc = ""] # [doc = " let expected = Some(Ok(HalfMatch::must(0, 21)));"] # [doc = " assert_eq!(expected, it.next());"] # [doc = ""] # [doc = " let expected = Some(Ok(HalfMatch::must(0, 32)));"] # [doc = " assert_eq!(expected, it.next());"] # [doc = ""] # [doc = " let expected = None;"] # [doc = " assert_eq!(expected, it.next());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn into_half_matches_iter < F > (self , finder : F ,) -> TryHalfMatchesIter < 'h , F > where F : FnMut (& Input < '_ >) -> Result < Option < HalfMatch > , MatchError > , { TryHalfMatchesIter { it : self , finder } } # [doc = " Given a closure that executes a single search, return an iterator over"] # [doc = " all successive non-overlapping matches."] # [doc = ""] # [doc = " The iterator returned yields result values. If the underlying regex"] # [doc = " engine is configured to never return an error, consider calling"] # [doc = " [`TryMatchesIter::infallible`] to convert errors into panics."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use a `Searcher` to create a proper"] # [doc = " iterator over matches."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::regex::Regex,"] # [doc = "     util::iter::Searcher,"] # [doc = "     Match, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let re = Regex::new(r\"[0-9]{4}-[0-9]{2}-[0-9]{2}\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let input = Input::new(\"2010-03-14 2016-10-08 2020-10-22\");"] # [doc = " let mut it = Searcher::new(input).into_matches_iter(|input| {"] # [doc = "     re.try_search(&mut cache, input)"] # [doc = " });"] # [doc = ""] # [doc = " let expected = Some(Ok(Match::must(0, 0..10)));"] # [doc = " assert_eq!(expected, it.next());"] # [doc = ""] # [doc = " let expected = Some(Ok(Match::must(0, 11..21)));"] # [doc = " assert_eq!(expected, it.next());"] # [doc = ""] # [doc = " let expected = Some(Ok(Match::must(0, 22..32)));"] # [doc = " assert_eq!(expected, it.next());"] # [doc = ""] # [doc = " let expected = None;"] # [doc = " assert_eq!(expected, it.next());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn into_matches_iter < F > (self , finder : F) -> TryMatchesIter < 'h , F > where F : FnMut (& Input < '_ >) -> Result < Option < Match > , MatchError > , { TryMatchesIter { it : self , finder } } # [doc = " Given a closure that executes a single search, return an iterator over"] # [doc = " all successive non-overlapping `Captures` values."] # [doc = ""] # [doc = " The iterator returned yields result values. If the underlying regex"] # [doc = " engine is configured to never return an error, consider calling"] # [doc = " [`TryCapturesIter::infallible`] to convert errors into panics."] # [doc = ""] # [doc = " Unlike the other iterator constructors, this accepts an initial"] # [doc = " `Captures` value. This `Captures` value is reused for each search, and"] # [doc = " the iterator implementation clones it before returning it. The caller"] # [doc = " must provide this value because the iterator is purposely ignorant"] # [doc = " of the underlying regex engine and thus doesn't know how to create"] # [doc = " one itself. More to the point, a `Captures` value itself has a few"] # [doc = " different constructors, which change which kind of information is"] # [doc = " available to query in exchange for search performance."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use a `Searcher` to create a proper iterator"] # [doc = " over `Captures` values, which provides access to all capturing group"] # [doc = " spans for each match."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     util::iter::Searcher,"] # [doc = "     Input,"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::new("] # [doc = "     r\"(?P<y>[0-9]{4})-(?P<m>[0-9]{2})-(?P<d>[0-9]{2})\","] # [doc = " )?;"] # [doc = " let (mut cache, caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " let haystack = \"2010-03-14 2016-10-08 2020-10-22\";"] # [doc = " let input = Input::new(haystack);"] # [doc = " let mut it = Searcher::new(input)"] # [doc = "     .into_captures_iter(caps, |input, caps| {"] # [doc = "         re.search(&mut cache, input, caps);"] # [doc = "         Ok(())"] # [doc = "     });"] # [doc = ""] # [doc = " let got = it.next().expect(\"first date\")?;"] # [doc = " let year = got.get_group_by_name(\"y\").expect(\"must match\");"] # [doc = " assert_eq!(\"2010\", &haystack[year]);"] # [doc = ""] # [doc = " let got = it.next().expect(\"second date\")?;"] # [doc = " let month = got.get_group_by_name(\"m\").expect(\"must match\");"] # [doc = " assert_eq!(\"10\", &haystack[month]);"] # [doc = ""] # [doc = " let got = it.next().expect(\"third date\")?;"] # [doc = " let day = got.get_group_by_name(\"d\").expect(\"must match\");"] # [doc = " assert_eq!(\"22\", &haystack[day]);"] # [doc = ""] # [doc = " assert!(it.next().is_none());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "alloc")] # [inline] pub fn into_captures_iter < F > (self , caps : Captures , finder : F ,) -> TryCapturesIter < 'h , F > where F : FnMut (& Input < '_ > , & mut Captures) -> Result < () , MatchError > , { TryCapturesIter { it : self , caps , finder } } # [doc = " Handles the special case of a match that begins where the previous"] # [doc = " match ended. Without this special handling, it'd be possible to get"] # [doc = " stuck where an empty match never results in forward progress. This"] # [doc = " also makes it more consistent with how presiding general purpose regex"] # [doc = " engines work."] # [cold] # [inline (never)] fn handle_overlapping_empty_half_match < F > (& mut self , _ : HalfMatch , mut finder : F ,) -> Result < Option < HalfMatch > , MatchError > where F : FnMut (& Input < '_ >) -> Result < Option < HalfMatch > , MatchError > , { self . input . set_start (self . input . start () . checked_add (1) . unwrap ()) ; finder (& self . input) } # [doc = " Handles the special case of an empty match by ensuring that 1) the"] # [doc = " iterator always advances and 2) empty matches never overlap with other"] # [doc = " matches."] # [doc = ""] # [doc = " (1) is necessary because we principally make progress by setting the"] # [doc = " starting location of the next search to the ending location of the last"] # [doc = " match. But if a match is empty, then this results in a search that does"] # [doc = " not advance and thus does not terminate."] # [doc = ""] # [doc = " (2) is not strictly necessary, but makes intuitive sense and matches"] # [doc = " the presiding behavior of most general purpose regex engines. The"] # [doc = " \"intuitive sense\" here is that we want to report NON-overlapping"] # [doc = " matches. So for example, given the regex 'a|(?:)' against the haystack"] # [doc = " 'a', without the special handling, you'd get the matches [0, 1) and [1,"] # [doc = " 1), where the latter overlaps with the end bounds of the former."] # [doc = ""] # [doc = " Note that we mark this cold and forcefully prevent inlining because"] # [doc = " handling empty matches like this is extremely rare and does require"] # [doc = " quite a bit of code, comparatively. Keeping this code out of the main"] # [doc = " iterator function keeps it smaller and more amenable to inlining"] # [doc = " itself."] # [cold] # [inline (never)] fn handle_overlapping_empty_match < F > (& mut self , m : Match , mut finder : F ,) -> Result < Option < Match > , MatchError > where F : FnMut (& Input < '_ >) -> Result < Option < Match > , MatchError > , { assert ! (m . is_empty ()) ; self . input . set_start (self . input . start () . checked_add (1) . unwrap ()) ; finder (& self . input) } }
};
}
