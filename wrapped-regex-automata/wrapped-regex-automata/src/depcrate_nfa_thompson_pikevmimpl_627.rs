// Generated macro for impl_627 (impl)
macro_rules! Depcrate_nfa_thompson_pikevmimpl_627 {
() => {
// Module: crate::nfa::thompson::pikevm
// Provides: {"impl_627"}
// Dependencies: {}
impl Config { # [doc = " Return a new default PikeVM configuration."] pub fn new () -> Config { Config :: default () } # [doc = " Set the desired match semantics."] # [doc = ""] # [doc = " The default is [`MatchKind::LeftmostFirst`], which corresponds to the"] # [doc = " match semantics of Perl-like regex engines. That is, when multiple"] # [doc = " patterns would match at the same leftmost position, the pattern that"] # [doc = " appears first in the concrete syntax is chosen."] # [doc = ""] # [doc = " Currently, the only other kind of match semantics supported is"] # [doc = " [`MatchKind::All`]. This corresponds to \"classical DFA\" construction"] # [doc = " where all possible matches are visited in the NFA by the `PikeVM`."] # [doc = ""] # [doc = " Typically, `All` is used when one wants to execute an overlapping"] # [doc = " search and `LeftmostFirst` otherwise. In particular, it rarely makes"] # [doc = " sense to use `All` with the various \"leftmost\" find routines, since the"] # [doc = " leftmost routines depend on the `LeftmostFirst` automata construction"] # [doc = " strategy. Specifically, `LeftmostFirst` results in the `PikeVM`"] # [doc = " simulating dead states as a way to terminate the search and report a"] # [doc = " match. `LeftmostFirst` also supports non-greedy matches using this"] # [doc = " strategy where as `All` does not."] pub fn match_kind (mut self , kind : MatchKind) -> Config { self . match_kind = Some (kind) ; self } # [doc = " Set a prefilter to be used whenever a start state is entered."] # [doc = ""] # [doc = " A [`Prefilter`] in this context is meant to accelerate searches by"] # [doc = " looking for literal prefixes that every match for the corresponding"] # [doc = " pattern (or patterns) must start with. Once a prefilter produces a"] # [doc = " match, the underlying search routine continues on to try and confirm"] # [doc = " the match."] # [doc = ""] # [doc = " Be warned that setting a prefilter does not guarantee that the search"] # [doc = " will be faster. While it's usually a good bet, if the prefilter"] # [doc = " produces a lot of false positive candidates (i.e., positions matched"] # [doc = " by the prefilter but not by the regex), then the overall result can"] # [doc = " be slower than if you had just executed the regex engine without any"] # [doc = " prefilters."] # [doc = ""] # [doc = " By default no prefilter is set."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     util::prefilter::Prefilter,"] # [doc = "     Input, Match, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let pre = Prefilter::new(MatchKind::LeftmostFirst, &[\"foo\", \"bar\"]);"] # [doc = " let re = PikeVM::builder()"] # [doc = "     .configure(PikeVM::config().prefilter(pre))"] # [doc = "     .build(r\"(foo|bar)[a-z]+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let input = Input::new(\"foo1 barfox bar\");"] # [doc = " assert_eq!(Some(Match::must(0, 5..11)), re.find(&mut cache, input));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Be warned though that an incorrect prefilter can lead to incorrect"] # [doc = " results!"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     util::prefilter::Prefilter,"] # [doc = "     Input, HalfMatch, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let pre = Prefilter::new(MatchKind::LeftmostFirst, &[\"foo\", \"car\"]);"] # [doc = " let re = PikeVM::builder()"] # [doc = "     .configure(PikeVM::config().prefilter(pre))"] # [doc = "     .build(r\"(foo|bar)[a-z]+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let input = Input::new(\"foo1 barfox bar\");"] # [doc = " // No match reported even though there clearly is one!"] # [doc = " assert_eq!(None, re.find(&mut cache, input));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn prefilter (mut self , pre : Option < Prefilter >) -> Config { self . pre = Some (pre) ; self } # [doc = " Returns the match semantics set in this configuration."] pub fn get_match_kind (& self) -> MatchKind { self . match_kind . unwrap_or (MatchKind :: LeftmostFirst) } # [doc = " Returns the prefilter set in this configuration, if one at all."] pub fn get_prefilter (& self) -> Option < & Prefilter > { self . pre . as_ref () . unwrap_or (& None) . as_ref () } # [doc = " Overwrite the default configuration such that the options in `o` are"] # [doc = " always used. If an option in `o` is not set, then the corresponding"] # [doc = " option in `self` is used. If it's not set in `self` either, then it"] # [doc = " remains not set."] pub (crate) fn overwrite (& self , o : Config) -> Config { Config { match_kind : o . match_kind . or (self . match_kind) , pre : o . pre . or_else (| | self . pre . clone ()) , } } }
};
}
