// Generated macro for impl_415 (impl)
macro_rules! Depcrate_meta_regeximpl_415 {
() => {
// Module: crate::meta::regex
// Provides: {"impl_415"}
// Dependencies: {}
impl Cache { # [doc = " Creates a new `Cache` for use with this regex."] # [doc = ""] # [doc = " The cache returned should only be used for searches for the given"] # [doc = " `Regex`. If you want to reuse the cache for another `Regex`, then you"] # [doc = " must call [`Cache::reset`] with that `Regex`."] pub fn new (re : & Regex) -> Cache { re . create_cache () } # [doc = " Reset this cache such that it can be used for searching with the given"] # [doc = " `Regex` (and only that `Regex`)."] # [doc = ""] # [doc = " A cache reset permits potentially reusing memory already allocated in"] # [doc = " this cache with a different `Regex`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to re-purpose a cache for use with a different `Regex`."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{meta::Regex, Match, Input};"] # [doc = ""] # [doc = " let re1 = Regex::new(r\"\\w\")?;"] # [doc = " let re2 = Regex::new(r\"\\W\")?;"] # [doc = ""] # [doc = " let mut cache = re1.create_cache();"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 0..2)),"] # [doc = "     re1.search_with(&mut cache, &Input::new(\"Δ\")),"] # [doc = " );"] # [doc = ""] # [doc = " // Using 'cache' with re2 is not allowed. It may result in panics or"] # [doc = " // incorrect results. In order to re-purpose the cache, we must reset"] # [doc = " // it with the Regex we'd like to use it with."] # [doc = " //"] # [doc = " // Similarly, after this reset, using the cache with 're1' is also not"] # [doc = " // allowed."] # [doc = " cache.reset(&re2);"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 0..3)),"] # [doc = "     re2.search_with(&mut cache, &Input::new(\"☃\")),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn reset (& mut self , re : & Regex) { re . imp . strat . reset_cache (self) } # [doc = " Returns the heap memory usage, in bytes, of this cache."] # [doc = ""] # [doc = " This does **not** include the stack size used up by this cache. To"] # [doc = " compute that, use `std::mem::size_of::<Cache>()`."] pub fn memory_usage (& self) -> usize { let mut bytes = 0 ; bytes += self . pikevm . memory_usage () ; bytes += self . backtrack . memory_usage () ; bytes += self . onepass . memory_usage () ; bytes += self . hybrid . memory_usage () ; bytes += self . revhybrid . memory_usage () ; bytes } }
};
}
