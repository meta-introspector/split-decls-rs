// Generated macro for impl_549 (impl)
macro_rules! Depcrate_nfa_thompson_compilerimpl_549 {
() => {
// Module: crate::nfa::thompson::compiler
// Provides: {"impl_549"}
// Dependencies: {}
impl Compiler { # [doc = " Create a new NFA builder with its default configuration."] pub fn new () -> Compiler { Compiler { parser : ParserBuilder :: new () , config : Config :: default () , builder : RefCell :: new (Builder :: new ()) , utf8_state : RefCell :: new (Utf8State :: new ()) , trie_state : RefCell :: new (RangeTrie :: new ()) , utf8_suffix : RefCell :: new (Utf8SuffixMap :: new (1000)) , } } # [doc = " Compile the given regular expression pattern into an NFA."] # [doc = ""] # [doc = " If there was a problem parsing the regex, then that error is returned."] # [doc = ""] # [doc = " Otherwise, if there was a problem building the NFA, then an error is"] # [doc = " returned. The only error that can occur is if the compiled regex would"] # [doc = " exceed the size limits configured on this builder, or if any part of"] # [doc = " the NFA would exceed the integer representations used. (For example,"] # [doc = " too many states might plausibly occur on a 16-bit target.)"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::{NFA, pikevm::PikeVM}, Match};"] # [doc = ""] # [doc = " let config = NFA::config().nfa_size_limit(Some(1_000));"] # [doc = " let nfa = NFA::compiler().configure(config).build(r\"(?-u)\\w\")?;"] # [doc = ""] # [doc = " let re = PikeVM::new_from_nfa(nfa)?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = " let expected = Some(Match::must(0, 3..4));"] # [doc = " re.captures(&mut cache, \"!@#A#@!\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn build (& self , pattern : & str) -> Result < NFA , BuildError > { self . build_many (& [pattern]) } # [doc = " Compile the given regular expression patterns into a single NFA."] # [doc = ""] # [doc = " When matches are returned, the pattern ID corresponds to the index of"] # [doc = " the pattern in the slice given."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::{NFA, pikevm::PikeVM}, Match};"] # [doc = ""] # [doc = " let config = NFA::config().nfa_size_limit(Some(1_000));"] # [doc = " let nfa = NFA::compiler().configure(config).build_many(&["] # [doc = "     r\"(?-u)\\s\","] # [doc = "     r\"(?-u)\\w\","] # [doc = " ])?;"] # [doc = ""] # [doc = " let re = PikeVM::new_from_nfa(nfa)?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = " let expected = Some(Match::must(1, 1..2));"] # [doc = " re.captures(&mut cache, \"!A! !A!\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn build_many < P : AsRef < str > > (& self , patterns : & [P] ,) -> Result < NFA , BuildError > { let mut hirs = vec ! [] ; for p in patterns { hirs . push (self . parser . build () . parse (p . as_ref ()) . map_err (BuildError :: syntax) ? ,) ; debug ! ("parsed: {:?}" , p . as_ref ()) ; } self . build_many_from_hir (& hirs) } # [doc = " Compile the given high level intermediate representation of a regular"] # [doc = " expression into an NFA."] # [doc = ""] # [doc = " If there was a problem building the NFA, then an error is returned. The"] # [doc = " only error that can occur is if the compiled regex would exceed the"] # [doc = " size limits configured on this builder, or if any part of the NFA would"] # [doc = " exceed the integer representations used. (For example, too many states"] # [doc = " might plausibly occur on a 16-bit target.)"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::{NFA, pikevm::PikeVM}, Match};"] # [doc = " use regex_syntax::hir::{Hir, Class, ClassBytes, ClassBytesRange};"] # [doc = ""] # [doc = " let hir = Hir::class(Class::Bytes(ClassBytes::new(vec!["] # [doc = "     ClassBytesRange::new(b'0', b'9'),"] # [doc = "     ClassBytesRange::new(b'A', b'Z'),"] # [doc = "     ClassBytesRange::new(b'_', b'_'),"] # [doc = "     ClassBytesRange::new(b'a', b'z'),"] # [doc = " ])));"] # [doc = ""] # [doc = " let config = NFA::config().nfa_size_limit(Some(1_000));"] # [doc = " let nfa = NFA::compiler().configure(config).build_from_hir(&hir)?;"] # [doc = ""] # [doc = " let re = PikeVM::new_from_nfa(nfa)?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = " let expected = Some(Match::must(0, 3..4));"] # [doc = " re.captures(&mut cache, \"!@#A#@!\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn build_from_hir (& self , expr : & Hir) -> Result < NFA , BuildError > { self . build_many_from_hir (& [expr]) } # [doc = " Compile the given high level intermediate representations of regular"] # [doc = " expressions into a single NFA."] # [doc = ""] # [doc = " When matches are returned, the pattern ID corresponds to the index of"] # [doc = " the pattern in the slice given."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::{NFA, pikevm::PikeVM}, Match};"] # [doc = " use regex_syntax::hir::{Hir, Class, ClassBytes, ClassBytesRange};"] # [doc = ""] # [doc = " let hirs = &["] # [doc = "     Hir::class(Class::Bytes(ClassBytes::new(vec!["] # [doc = "         ClassBytesRange::new(b'\\t', b'\\r'),"] # [doc = "         ClassBytesRange::new(b' ', b' '),"] # [doc = "     ]))),"] # [doc = "     Hir::class(Class::Bytes(ClassBytes::new(vec!["] # [doc = "         ClassBytesRange::new(b'0', b'9'),"] # [doc = "         ClassBytesRange::new(b'A', b'Z'),"] # [doc = "         ClassBytesRange::new(b'_', b'_'),"] # [doc = "         ClassBytesRange::new(b'a', b'z'),"] # [doc = "     ]))),"] # [doc = " ];"] # [doc = ""] # [doc = " let config = NFA::config().nfa_size_limit(Some(1_000));"] # [doc = " let nfa = NFA::compiler().configure(config).build_many_from_hir(hirs)?;"] # [doc = ""] # [doc = " let re = PikeVM::new_from_nfa(nfa)?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = " let expected = Some(Match::must(1, 1..2));"] # [doc = " re.captures(&mut cache, \"!A! !A!\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn build_many_from_hir < H : Borrow < Hir > > (& self , exprs : & [H] ,) -> Result < NFA , BuildError > { self . compile (exprs) } # [doc = " Apply the given NFA configuration options to this builder."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::NFA;"] # [doc = ""] # [doc = " let config = NFA::config().nfa_size_limit(Some(1_000));"] # [doc = " let nfa = NFA::compiler().configure(config).build(r\"(?-u)\\w\")?;"] # [doc = " assert_eq!(nfa.pattern_len(), 1);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn configure (& mut self , config : Config) -> & mut Compiler { self . config = self . config . overwrite (config) ; self } # [doc = " Set the syntax configuration for this builder using"] # [doc = " [`syntax::Config`](crate::util::syntax::Config)."] # [doc = ""] # [doc = " This permits setting things like case insensitivity, Unicode and multi"] # [doc = " line mode."] # [doc = ""] # [doc = " This syntax configuration only applies when an NFA is built directly"] # [doc = " from a pattern string. If an NFA is built from an HIR, then all syntax"] # [doc = " settings are ignored."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, util::syntax};"] # [doc = ""] # [doc = " let syntax_config = syntax::Config::new().unicode(false);"] # [doc = " let nfa = NFA::compiler().syntax(syntax_config).build(r\"\\w\")?;"] # [doc = " // If Unicode were enabled, the number of states would be much bigger."] # [doc = " assert!(nfa.states().len() < 15);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn syntax (& mut self , config : crate :: util :: syntax :: Config ,) -> & mut Compiler { config . apply (& mut self . parser) ; self } }
};
}
