// Generated macro for impl_317 (impl)
macro_rules! Depcrate_hybrid_regeximpl_317 {
() => {
// Module: crate::hybrid::regex
// Provides: {"impl_317"}
// Dependencies: {}
# [doc = " Convenience routines for regex and cache construction."] impl Regex { # [doc = " Parse the given regular expression using the default configuration and"] # [doc = " return the corresponding regex."] # [doc = ""] # [doc = " If you want a non-default configuration, then use the [`Builder`] to"] # [doc = " set your own configuration."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::regex::Regex, Match};"] # [doc = ""] # [doc = " let re = Regex::new(\"foo[0-9]+bar\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 3..14)),"] # [doc = "     re.find(&mut cache, \"zzzfoo12345barzzz\"),"] # [doc = " );"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] pub fn new (pattern : & str) -> Result < Regex , BuildError > { Regex :: builder () . build (pattern) } # [doc = " Like `new`, but parses multiple patterns into a single \"multi regex.\""] # [doc = " This similarly uses the default regex configuration."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::regex::Regex, Match};"] # [doc = ""] # [doc = " let re = Regex::new_many(&[\"[a-z]+\", \"[0-9]+\"])?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let mut it = re.find_iter(&mut cache, \"abc 1 foo 4567 0 quux\");"] # [doc = " assert_eq!(Some(Match::must(0, 0..3)), it.next());"] # [doc = " assert_eq!(Some(Match::must(1, 4..5)), it.next());"] # [doc = " assert_eq!(Some(Match::must(0, 6..9)), it.next());"] # [doc = " assert_eq!(Some(Match::must(1, 10..14)), it.next());"] # [doc = " assert_eq!(Some(Match::must(1, 15..16)), it.next());"] # [doc = " assert_eq!(Some(Match::must(0, 17..21)), it.next());"] # [doc = " assert_eq!(None, it.next());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] pub fn new_many < P : AsRef < str > > (patterns : & [P] ,) -> Result < Regex , BuildError > { Regex :: builder () . build_many (patterns) } # [doc = " Return a builder for configuring the construction of a `Regex`."] # [doc = ""] # [doc = " This is a convenience routine to avoid needing to import the"] # [doc = " [`Builder`] type in common cases."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use the builder to disable UTF-8 mode"] # [doc = " everywhere."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::regex::Regex, nfa::thompson, util::syntax, Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .syntax(syntax::Config::new().utf8(false))"] # [doc = "     .thompson(thompson::Config::new().utf8(false))"] # [doc = "     .build(r\"foo(?-u:[^b])ar.*\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let haystack = b\"\\xFEfoo\\xFFarzz\\xE2\\x98\\xFF\\n\";"] # [doc = " let expected = Some(Match::must(0, 1..9));"] # [doc = " let got = re.find(&mut cache, haystack);"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn builder () -> Builder { Builder :: new () } # [doc = " Create a new cache for this `Regex`."] # [doc = ""] # [doc = " The cache returned should only be used for searches for this"] # [doc = " `Regex`. If you want to reuse the cache for another `Regex`, then"] # [doc = " you must call [`Cache::reset`] with that `Regex` (or, equivalently,"] # [doc = " [`Regex::reset_cache`])."] pub fn create_cache (& self) -> Cache { Cache :: new (self) } # [doc = " Reset the given cache such that it can be used for searching with the"] # [doc = " this `Regex` (and only this `Regex`)."] # [doc = ""] # [doc = " A cache reset permits reusing memory already allocated in this cache"] # [doc = " with a different `Regex`."] # [doc = ""] # [doc = " Resetting a cache sets its \"clear count\" to 0. This is relevant if the"] # [doc = " `Regex` has been configured to \"give up\" after it has cleared the cache"] # [doc = " a certain number of times."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to re-purpose a cache for use with a different `Regex`."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{hybrid::regex::Regex, Match};"] # [doc = ""] # [doc = " let re1 = Regex::new(r\"\\w\")?;"] # [doc = " let re2 = Regex::new(r\"\\W\")?;"] # [doc = ""] # [doc = " let mut cache = re1.create_cache();"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 0..2)),"] # [doc = "     re1.find(&mut cache, \"Δ\"),"] # [doc = " );"] # [doc = ""] # [doc = " // Using 'cache' with re2 is not allowed. It may result in panics or"] # [doc = " // incorrect results. In order to re-purpose the cache, we must reset"] # [doc = " // it with the Regex we'd like to use it with."] # [doc = " //"] # [doc = " // Similarly, after this reset, using the cache with 're1' is also not"] # [doc = " // allowed."] # [doc = " re2.reset_cache(&mut cache);"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 0..3)),"] # [doc = "     re2.find(&mut cache, \"☃\"),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn reset_cache (& self , cache : & mut Cache) { self . forward () . reset_cache (& mut cache . forward) ; self . reverse () . reset_cache (& mut cache . reverse) ; } }
};
}
