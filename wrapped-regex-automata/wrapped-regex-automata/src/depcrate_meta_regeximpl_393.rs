// Generated macro for impl_393 (impl)
macro_rules! Depcrate_meta_regeximpl_393 {
() => {
// Module: crate::meta::regex
// Provides: {"impl_393"}
// Dependencies: {}
# [doc = " Various non-search routines for querying properties of a `Regex` and"] # [doc = " convenience routines for creating [`Captures`] and [`Cache`] values."] impl Regex { # [doc = " Creates a new object for recording capture group offsets. This is used"] # [doc = " in search APIs like [`Regex::captures`] and [`Regex::search_captures`]."] # [doc = ""] # [doc = " This is a convenience routine for"] # [doc = " `Captures::all(re.group_info().clone())`. Callers may build other types"] # [doc = " of `Captures` values that record less information (and thus require"] # [doc = " less work from the regex engine) using [`Captures::matches`] and"] # [doc = " [`Captures::empty`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows some alternatives to [`Regex::create_captures`]:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     meta::Regex,"] # [doc = "     util::captures::Captures,"] # [doc = "     Match, PatternID, Span,"] # [doc = " };"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?<first>[A-Z][a-z]+) (?<last>[A-Z][a-z]+)\")?;"] # [doc = ""] # [doc = " // This is equivalent to Regex::create_captures. It stores matching"] # [doc = " // offsets for all groups in the regex."] # [doc = " let mut all = Captures::all(re.group_info().clone());"] # [doc = " re.captures(\"Bruce Springsteen\", &mut all);"] # [doc = " assert_eq!(Some(Match::must(0, 0..17)), all.get_match());"] # [doc = " assert_eq!(Some(Span::from(0..5)), all.get_group_by_name(\"first\"));"] # [doc = " assert_eq!(Some(Span::from(6..17)), all.get_group_by_name(\"last\"));"] # [doc = ""] # [doc = " // In this version, we only care about the implicit groups, which"] # [doc = " // means offsets for the explicit groups will be unavailable. It can"] # [doc = " // sometimes be faster to ask for fewer groups, since the underlying"] # [doc = " // regex engine needs to do less work to keep track of them."] # [doc = " let mut matches = Captures::matches(re.group_info().clone());"] # [doc = " re.captures(\"Bruce Springsteen\", &mut matches);"] # [doc = " // We still get the overall match info."] # [doc = " assert_eq!(Some(Match::must(0, 0..17)), matches.get_match());"] # [doc = " // But now the explicit groups are unavailable."] # [doc = " assert_eq!(None, matches.get_group_by_name(\"first\"));"] # [doc = " assert_eq!(None, matches.get_group_by_name(\"last\"));"] # [doc = ""] # [doc = " // Finally, in this version, we don't ask to keep track of offsets for"] # [doc = " // *any* groups. All we get back is whether a match occurred, and if"] # [doc = " // so, the ID of the pattern that matched."] # [doc = " let mut empty = Captures::empty(re.group_info().clone());"] # [doc = " re.captures(\"Bruce Springsteen\", &mut empty);"] # [doc = " // it's a match!"] # [doc = " assert!(empty.is_match());"] # [doc = " // for pattern ID 0"] # [doc = " assert_eq!(Some(PatternID::ZERO), empty.pattern());"] # [doc = " // Match offsets are unavailable."] # [doc = " assert_eq!(None, empty.get_match());"] # [doc = " // And of course, explicit groups are unavailable too."] # [doc = " assert_eq!(None, empty.get_group_by_name(\"first\"));"] # [doc = " assert_eq!(None, empty.get_group_by_name(\"last\"));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn create_captures (& self) -> Captures { Captures :: all (self . group_info () . clone ()) } # [doc = " Creates a new cache for use with lower level search APIs like"] # [doc = " [`Regex::search_with`]."] # [doc = ""] # [doc = " The cache returned should only be used for searches for this `Regex`."] # [doc = " If you want to reuse the cache for another `Regex`, then you must call"] # [doc = " [`Cache::reset`] with that `Regex`."] # [doc = ""] # [doc = " This is a convenience routine for [`Cache::new`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Input, Match};"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?-u)m\\w+\\s+m\\w+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let input = Input::new(\"crazy janey and her mission man\");"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 20..31)),"] # [doc = "     re.search_with(&mut cache, &input),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn create_cache (& self) -> Cache { self . imp . strat . create_cache () } # [doc = " Returns the total number of patterns in this regex."] # [doc = ""] # [doc = " The standard [`Regex::new`] constructor always results in a `Regex`"] # [doc = " with a single pattern, but [`Regex::new_many`] permits building a"] # [doc = " multi-pattern regex."] # [doc = ""] # [doc = " A `Regex` guarantees that the maximum possible `PatternID` returned in"] # [doc = " any match is `Regex::pattern_len() - 1`. In the case where the number"] # [doc = " of patterns is `0`, a match is impossible."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?m)^[a-z]$\")?;"] # [doc = " assert_eq!(1, re.pattern_len());"] # [doc = ""] # [doc = " let re = Regex::new_many::<&str>(&[])?;"] # [doc = " assert_eq!(0, re.pattern_len());"] # [doc = ""] # [doc = " let re = Regex::new_many(&[\"a\", \"b\", \"c\"])?;"] # [doc = " assert_eq!(3, re.pattern_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn pattern_len (& self) -> usize { self . imp . info . pattern_len () } # [doc = " Returns the total number of capturing groups."] # [doc = ""] # [doc = " This includes the implicit capturing group corresponding to the"] # [doc = " entire match. Therefore, the minimum value returned is `1`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows a few patterns and how many capture groups they have."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let len = |pattern| {"] # [doc = "     Regex::new(pattern).map(|re| re.captures_len())"] # [doc = " };"] # [doc = ""] # [doc = " assert_eq!(1, len(\"a\")?);"] # [doc = " assert_eq!(2, len(\"(a)\")?);"] # [doc = " assert_eq!(3, len(\"(a)|(b)\")?);"] # [doc = " assert_eq!(5, len(\"(a)(b)|(c)(d)\")?);"] # [doc = " assert_eq!(2, len(\"(a)|b\")?);"] # [doc = " assert_eq!(2, len(\"a|(b)\")?);"] # [doc = " assert_eq!(2, len(\"(b)*\")?);"] # [doc = " assert_eq!(2, len(\"(b)+\")?);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: multiple patterns"] # [doc = ""] # [doc = " This routine also works for multiple patterns. The total number is"] # [doc = " the sum of the capture groups of each pattern."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let len = |patterns| {"] # [doc = "     Regex::new_many(patterns).map(|re| re.captures_len())"] # [doc = " };"] # [doc = ""] # [doc = " assert_eq!(2, len(&[\"a\", \"b\"])?);"] # [doc = " assert_eq!(4, len(&[\"(a)\", \"(b)\"])?);"] # [doc = " assert_eq!(6, len(&[\"(a)|(b)\", \"(c)|(d)\"])?);"] # [doc = " assert_eq!(8, len(&[\"(a)(b)|(c)(d)\", \"(x)(y)\"])?);"] # [doc = " assert_eq!(3, len(&[\"(a)\", \"b\"])?);"] # [doc = " assert_eq!(3, len(&[\"a\", \"(b)\"])?);"] # [doc = " assert_eq!(4, len(&[\"(a)\", \"(b)*\"])?);"] # [doc = " assert_eq!(4, len(&[\"(a)+\", \"(b)+\"])?);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn captures_len (& self) -> usize { self . imp . info . props_union () . explicit_captures_len () . saturating_add (self . pattern_len ()) } # [doc = " Returns the total number of capturing groups that appear in every"] # [doc = " possible match."] # [doc = ""] # [doc = " If the number of capture groups can vary depending on the match, then"] # [doc = " this returns `None`. That is, a value is only returned when the number"] # [doc = " of matching groups is invariant or \"static.\""] # [doc = ""] # [doc = " Note that like [`Regex::captures_len`], this **does** include the"] # [doc = " implicit capturing group corresponding to the entire match. Therefore,"] # [doc = " when a non-None value is returned, it is guaranteed to be at least `1`."] # [doc = " Stated differently, a return value of `Some(0)` is impossible."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows a few cases where a static number of capture groups is"] # [doc = " available and a few cases where it is not."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let len = |pattern| {"] # [doc = "     Regex::new(pattern).map(|re| re.static_captures_len())"] # [doc = " };"] # [doc = ""] # [doc = " assert_eq!(Some(1), len(\"a\")?);"] # [doc = " assert_eq!(Some(2), len(\"(a)\")?);"] # [doc = " assert_eq!(Some(2), len(\"(a)|(b)\")?);"] # [doc = " assert_eq!(Some(3), len(\"(a)(b)|(c)(d)\")?);"] # [doc = " assert_eq!(None, len(\"(a)|b\")?);"] # [doc = " assert_eq!(None, len(\"a|(b)\")?);"] # [doc = " assert_eq!(None, len(\"(b)*\")?);"] # [doc = " assert_eq!(Some(2), len(\"(b)+\")?);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: multiple patterns"] # [doc = ""] # [doc = " This property extends to regexes with multiple patterns as well. In"] # [doc = " order for their to be a static number of capture groups in this case,"] # [doc = " every pattern must have the same static number."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let len = |patterns| {"] # [doc = "     Regex::new_many(patterns).map(|re| re.static_captures_len())"] # [doc = " };"] # [doc = ""] # [doc = " assert_eq!(Some(1), len(&[\"a\", \"b\"])?);"] # [doc = " assert_eq!(Some(2), len(&[\"(a)\", \"(b)\"])?);"] # [doc = " assert_eq!(Some(2), len(&[\"(a)|(b)\", \"(c)|(d)\"])?);"] # [doc = " assert_eq!(Some(3), len(&[\"(a)(b)|(c)(d)\", \"(x)(y)\"])?);"] # [doc = " assert_eq!(None, len(&[\"(a)\", \"b\"])?);"] # [doc = " assert_eq!(None, len(&[\"a\", \"(b)\"])?);"] # [doc = " assert_eq!(None, len(&[\"(a)\", \"(b)*\"])?);"] # [doc = " assert_eq!(Some(2), len(&[\"(a)+\", \"(b)+\"])?);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn static_captures_len (& self) -> Option < usize > { self . imp . info . props_union () . static_explicit_captures_len () . map (| len | len . saturating_add (1)) } # [doc = " Return information about the capture groups in this `Regex`."] # [doc = ""] # [doc = " A `GroupInfo` is an immutable object that can be cheaply cloned. It"] # [doc = " is responsible for maintaining a mapping between the capture groups"] # [doc = " in the concrete syntax of zero or more regex patterns and their"] # [doc = " internal representation used by some of the regex matchers. It is also"] # [doc = " responsible for maintaining a mapping between the name of each group"] # [doc = " (if one exists) and its corresponding group index."] # [doc = ""] # [doc = " A `GroupInfo` is ultimately what is used to build a [`Captures`] value,"] # [doc = " which is some mutable space where group offsets are stored as a result"] # [doc = " of a search."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows some alternatives to [`Regex::create_captures`]:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     meta::Regex,"] # [doc = "     util::captures::Captures,"] # [doc = "     Match, PatternID, Span,"] # [doc = " };"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?<first>[A-Z][a-z]+) (?<last>[A-Z][a-z]+)\")?;"] # [doc = ""] # [doc = " // This is equivalent to Regex::create_captures. It stores matching"] # [doc = " // offsets for all groups in the regex."] # [doc = " let mut all = Captures::all(re.group_info().clone());"] # [doc = " re.captures(\"Bruce Springsteen\", &mut all);"] # [doc = " assert_eq!(Some(Match::must(0, 0..17)), all.get_match());"] # [doc = " assert_eq!(Some(Span::from(0..5)), all.get_group_by_name(\"first\"));"] # [doc = " assert_eq!(Some(Span::from(6..17)), all.get_group_by_name(\"last\"));"] # [doc = ""] # [doc = " // In this version, we only care about the implicit groups, which"] # [doc = " // means offsets for the explicit groups will be unavailable. It can"] # [doc = " // sometimes be faster to ask for fewer groups, since the underlying"] # [doc = " // regex engine needs to do less work to keep track of them."] # [doc = " let mut matches = Captures::matches(re.group_info().clone());"] # [doc = " re.captures(\"Bruce Springsteen\", &mut matches);"] # [doc = " // We still get the overall match info."] # [doc = " assert_eq!(Some(Match::must(0, 0..17)), matches.get_match());"] # [doc = " // But now the explicit groups are unavailable."] # [doc = " assert_eq!(None, matches.get_group_by_name(\"first\"));"] # [doc = " assert_eq!(None, matches.get_group_by_name(\"last\"));"] # [doc = ""] # [doc = " // Finally, in this version, we don't ask to keep track of offsets for"] # [doc = " // *any* groups. All we get back is whether a match occurred, and if"] # [doc = " // so, the ID of the pattern that matched."] # [doc = " let mut empty = Captures::empty(re.group_info().clone());"] # [doc = " re.captures(\"Bruce Springsteen\", &mut empty);"] # [doc = " // it's a match!"] # [doc = " assert!(empty.is_match());"] # [doc = " // for pattern ID 0"] # [doc = " assert_eq!(Some(PatternID::ZERO), empty.pattern());"] # [doc = " // Match offsets are unavailable."] # [doc = " assert_eq!(None, empty.get_match());"] # [doc = " // And of course, explicit groups are unavailable too."] # [doc = " assert_eq!(None, empty.get_group_by_name(\"first\"));"] # [doc = " assert_eq!(None, empty.get_group_by_name(\"last\"));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn group_info (& self) -> & GroupInfo { self . imp . strat . group_info () } # [doc = " Returns the configuration object used to build this `Regex`."] # [doc = ""] # [doc = " If no configuration object was explicitly passed, then the"] # [doc = " configuration returned represents the default."] # [inline] pub fn get_config (& self) -> & Config { self . imp . info . config () } # [doc = " Returns true if this regex has a high chance of being \"accelerated.\""] # [doc = ""] # [doc = " The precise meaning of \"accelerated\" is specifically left unspecified,"] # [doc = " but the general meaning is that the search is a high likelihood of"] # [doc = " running faster than a character-at-a-time loop inside a standard"] # [doc = " regex engine."] # [doc = ""] # [doc = " When a regex is accelerated, it is only a *probabilistic* claim. That"] # [doc = " is, just because the regex is believed to be accelerated, that doesn't"] # [doc = " mean it will definitely execute searches very fast. Similarly, if a"] # [doc = " regex is *not* accelerated, that is also a probabilistic claim. That"] # [doc = " is, a regex for which `is_accelerated` returns `false` could still run"] # [doc = " searches more quickly than a regex for which `is_accelerated` returns"] # [doc = " `true`."] # [doc = ""] # [doc = " Whether a regex is marked as accelerated or not is dependent on"] # [doc = " implementations details that may change in a semver compatible release."] # [doc = " That is, a regex that is accelerated in a `x.y.1` release might not be"] # [doc = " accelerated in a `x.y.2` release."] # [doc = ""] # [doc = " Basically, the value of acceleration boils down to a hedge: a hodge"] # [doc = " podge of internal heuristics combine to make a probabilistic guess"] # [doc = " that this regex search may run \"fast.\" The value in knowing this from"] # [doc = " a caller's perspective is that it may act as a signal that no further"] # [doc = " work should be done to accelerate a search. For example, a grep-like"] # [doc = " tool might try to do some extra work extracting literals from a regex"] # [doc = " to create its own heuristic acceleration strategies. But it might"] # [doc = " choose to defer to this crate's acceleration strategy if one exists."] # [doc = " This routine permits querying whether such a strategy is active for a"] # [doc = " particular regex."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " // A simple literal is very likely to be accelerated."] # [doc = " let re = Regex::new(r\"foo\")?;"] # [doc = " assert!(re.is_accelerated());"] # [doc = ""] # [doc = " // A regex with no literals is likely to not be accelerated."] # [doc = " let re = Regex::new(r\"\\w\")?;"] # [doc = " assert!(!re.is_accelerated());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn is_accelerated (& self) -> bool { self . imp . strat . is_accelerated () } # [doc = " Return the total approximate heap memory, in bytes, used by this `Regex`."] # [doc = ""] # [doc = " Note that currently, there is no high level configuration for setting"] # [doc = " a limit on the specific value returned by this routine. Instead, the"] # [doc = " following routines can be used to control heap memory at a bit of a"] # [doc = " lower level:"] # [doc = ""] # [doc = " * [`Config::nfa_size_limit`] controls how big _any_ of the NFAs are"] # [doc = " allowed to be."] # [doc = " * [`Config::onepass_size_limit`] controls how big the one-pass DFA is"] # [doc = " allowed to be."] # [doc = " * [`Config::hybrid_cache_capacity`] controls how much memory the lazy"] # [doc = " DFA is permitted to allocate to store its transition table."] # [doc = " * [`Config::dfa_size_limit`] controls how big a fully compiled DFA is"] # [doc = " allowed to be."] # [doc = " * [`Config::dfa_state_limit`] controls the conditions under which the"] # [doc = " meta regex engine will even attempt to build a fully compiled DFA."] # [inline] pub fn memory_usage (& self) -> usize { self . imp . strat . memory_usage () } }
};
}
