// Generated macro for Compiler (struct)
macro_rules! Depcrate_nfa_thompson_compilerCompiler {
() => {
// Module: crate::nfa::thompson::compiler
// Provides: {"Compiler"}
// Dependencies: {}
# [doc = " A builder for compiling an NFA from a regex's high-level intermediate"] # [doc = " representation (HIR)."] # [doc = ""] # [doc = " This compiler provides a way to translate a parsed regex pattern into an"] # [doc = " NFA state graph. The NFA state graph can either be used directly to execute"] # [doc = " a search (e.g., with a Pike VM), or it can be further used to build a DFA."] # [doc = ""] # [doc = " This compiler provides APIs both for compiling regex patterns directly from"] # [doc = " their concrete syntax, or via a [`regex_syntax::hir::Hir`]."] # [doc = ""] # [doc = " This compiler has various options that may be configured via"] # [doc = " [`thompson::Config`](Config)."] # [doc = ""] # [doc = " Note that a compiler is not the same as a [`thompson::Builder`](Builder)."] # [doc = " A `Builder` provides a lower level API that is uncoupled from a regex"] # [doc = " pattern's concrete syntax or even its HIR. Instead, it permits stitching"] # [doc = " together an NFA by hand. See its docs for examples."] # [doc = ""] # [doc = " # Example: compilation from concrete syntax"] # [doc = ""] # [doc = " This shows how to compile an NFA from a pattern string while setting a size"] # [doc = " limit on how big the NFA is allowed to be (in terms of bytes of heap used)."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::{NFA, pikevm::PikeVM},"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let config = NFA::config().nfa_size_limit(Some(1_000));"] # [doc = " let nfa = NFA::compiler().configure(config).build(r\"(?-u)\\w\")?;"] # [doc = ""] # [doc = " let re = PikeVM::new_from_nfa(nfa)?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = " let expected = Some(Match::must(0, 3..4));"] # [doc = " re.captures(&mut cache, \"!@#A#@!\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: compilation from HIR"] # [doc = ""] # [doc = " This shows how to hand assemble a regular expression via its HIR, and then"] # [doc = " compile an NFA directly from it."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::{NFA, pikevm::PikeVM}, Match};"] # [doc = " use regex_syntax::hir::{Hir, Class, ClassBytes, ClassBytesRange};"] # [doc = ""] # [doc = " let hir = Hir::class(Class::Bytes(ClassBytes::new(vec!["] # [doc = "     ClassBytesRange::new(b'0', b'9'),"] # [doc = "     ClassBytesRange::new(b'A', b'Z'),"] # [doc = "     ClassBytesRange::new(b'_', b'_'),"] # [doc = "     ClassBytesRange::new(b'a', b'z'),"] # [doc = " ])));"] # [doc = ""] # [doc = " let config = NFA::config().nfa_size_limit(Some(1_000));"] # [doc = " let nfa = NFA::compiler().configure(config).build_from_hir(&hir)?;"] # [doc = ""] # [doc = " let re = PikeVM::new_from_nfa(nfa)?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = " let expected = Some(Match::must(0, 3..4));"] # [doc = " re.captures(&mut cache, \"!@#A#@!\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Debug)] pub struct Compiler { # [doc = " A regex parser, used when compiling an NFA directly from a pattern"] # [doc = " string."] parser : ParserBuilder , # [doc = " The compiler configuration."] config : Config , # [doc = " The builder for actually constructing an NFA. This provides a"] # [doc = " convenient abstraction for writing a compiler."] builder : RefCell < Builder > , # [doc = " State used for compiling character classes to UTF-8 byte automata."] # [doc = " State is not retained between character class compilations. This just"] # [doc = " serves to amortize allocation to the extent possible."] utf8_state : RefCell < Utf8State > , # [doc = " State used for arranging character classes in reverse into a trie."] trie_state : RefCell < RangeTrie > , # [doc = " State used for caching common suffixes when compiling reverse UTF-8"] # [doc = " automata (for Unicode character classes)."] utf8_suffix : RefCell < Utf8SuffixMap > , }
};
}
