// Generated macro for Builder (struct)
macro_rules! Depcrate_hybrid_dfaBuilder {
() => {
// Module: crate::hybrid::dfa
// Provides: {"Builder"}
// Dependencies: {}
# [doc = " A builder for constructing a lazy deterministic finite automaton from"] # [doc = " regular expressions."] # [doc = ""] # [doc = " As a convenience, [`DFA::builder`] is an alias for [`Builder::new`]. The"] # [doc = " advantage of the former is that it often lets you avoid importing the"] # [doc = " `Builder` type directly."] # [doc = ""] # [doc = " This builder provides two main things:"] # [doc = ""] # [doc = " 1. It provides a few different `build` routines for actually constructing"] # [doc = " a DFA from different kinds of inputs. The most convenient is"] # [doc = " [`Builder::build`], which builds a DFA directly from a pattern string. The"] # [doc = " most flexible is [`Builder::build_from_nfa`], which builds a DFA straight"] # [doc = " from an NFA."] # [doc = " 2. The builder permits configuring a number of things."] # [doc = " [`Builder::configure`] is used with [`Config`] to configure aspects of"] # [doc = " the DFA and the construction process itself. [`Builder::syntax`] and"] # [doc = " [`Builder::thompson`] permit configuring the regex parser and Thompson NFA"] # [doc = " construction, respectively. The syntax and thompson configurations only"] # [doc = " apply when building from a pattern string."] # [doc = ""] # [doc = " This builder always constructs a *single* lazy DFA. As such, this builder"] # [doc = " can only be used to construct regexes that either detect the presence"] # [doc = " of a match or find the end location of a match. A single DFA cannot"] # [doc = " produce both the start and end of a match. For that information, use a"] # [doc = " [`Regex`](crate::hybrid::regex::Regex), which can be similarly configured"] # [doc = " using [`regex::Builder`](crate::hybrid::regex::Builder). The main reason"] # [doc = " to use a DFA directly is if the end location of a match is enough for your"] # [doc = " use case. Namely, a `Regex` will construct two lazy DFAs instead of one,"] # [doc = " since a second reverse DFA is needed to find the start of a match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to build a lazy DFA that uses a tiny cache capacity"] # [doc = " and completely disables Unicode. That is:"] # [doc = ""] # [doc = " * Things such as `\\w`, `.` and `\\b` are no longer Unicode-aware. `\\w`"] # [doc = "   and `\\b` are ASCII-only while `.` matches any byte except for `\\n`"] # [doc = "   (instead of any UTF-8 encoding of a Unicode scalar value except for"] # [doc = "   `\\n`). Things that are Unicode only, such as `\\pL`, are not allowed."] # [doc = " * The pattern itself is permitted to match invalid UTF-8. For example,"] # [doc = "   things like `[^a]` that match any byte except for `a` are permitted."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::dfa::DFA,"] # [doc = "     nfa::thompson,"] # [doc = "     util::syntax,"] # [doc = "     HalfMatch, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .configure(DFA::config().cache_capacity(5_000))"] # [doc = "     .thompson(thompson::Config::new().utf8(false))"] # [doc = "     .syntax(syntax::Config::new().unicode(false).utf8(false))"] # [doc = "     .build(r\"foo[^b]ar.*\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = ""] # [doc = " let haystack = b\"\\xFEfoo\\xFFar\\xE2\\x98\\xFF\\n\";"] # [doc = " let expected = Some(HalfMatch::must(0, 10));"] # [doc = " let got = dfa.try_search_fwd(&mut cache, &Input::new(haystack))?;"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Debug)] pub struct Builder { config : Config , # [cfg (feature = "syntax")] thompson : thompson :: Compiler , }
};
}
