// Generated macro for State (enum)
macro_rules! Depcrate_nfa_thompson_builderState {
() => {
// Module: crate::nfa::thompson::builder
// Provides: {"State"}
// Dependencies: {}
# [doc = " An intermediate NFA state used during construction."] # [doc = ""] # [doc = " During construction of an NFA, it is often convenient to work with states"] # [doc = " that are amenable to mutation and other carry more information than we"] # [doc = " otherwise need once an NFA has been built. This type represents those"] # [doc = " needs."] # [doc = ""] # [doc = " Once construction is finished, the builder will convert these states to a"] # [doc = " [`nfa::thompson::State`](crate::nfa::thompson::State). This conversion not"] # [doc = " only results in a simpler representation, but in some cases, entire classes"] # [doc = " of states are completely removed (such as [`State::Empty`])."] # [derive (Clone , Debug , Eq , PartialEq)] enum State { # [doc = " An empty state whose only purpose is to forward the automaton to"] # [doc = " another state via an unconditional epsilon transition."] # [doc = ""] # [doc = " Unconditional epsilon transitions are quite useful during the"] # [doc = " construction of an NFA, as they permit the insertion of no-op"] # [doc = " placeholders that make it easier to compose NFA sub-graphs. When"] # [doc = " the Thompson NFA builder produces a final NFA, all unconditional"] # [doc = " epsilon transitions are removed, and state identifiers are remapped"] # [doc = " accordingly."] Empty { # [doc = " The next state that this state should transition to."] next : StateID , } , # [doc = " A state that only transitions to another state if the current input"] # [doc = " byte is in a particular range of bytes."] ByteRange { trans : Transition } , # [doc = " A state with possibly many transitions, represented in a sparse"] # [doc = " fashion. Transitions must be ordered lexicographically by input range"] # [doc = " and be non-overlapping. As such, this may only be used when every"] # [doc = " transition has equal priority. (In practice, this is only used for"] # [doc = " encoding large UTF-8 automata.) In contrast, a `Union` state has each"] # [doc = " alternate in order of priority. Priority is used to implement greedy"] # [doc = " matching and also alternations themselves, e.g., `abc|a` where `abc`"] # [doc = " has priority over `a`."] # [doc = ""] # [doc = " To clarify, it is possible to remove `Sparse` and represent all things"] # [doc = " that `Sparse` is used for via `Union`. But this creates a more bloated"] # [doc = " NFA with more epsilon transitions than is necessary in the special case"] # [doc = " of character classes."] Sparse { transitions : Vec < Transition > } , # [doc = " A conditional epsilon transition satisfied via some sort of"] # [doc = " look-around."] Look { look : Look , next : StateID } , # [doc = " An empty state that records the start of a capture location. This is an"] # [doc = " unconditional epsilon transition like `Empty`, except it can be used to"] # [doc = " record position information for a capture group when using the NFA for"] # [doc = " search."] CaptureStart { # [doc = " The ID of the pattern that this capture was defined."] pattern_id : PatternID , # [doc = " The capture group index that this capture state corresponds to."] # [doc = " The capture group index is always relative to its corresponding"] # [doc = " pattern. Therefore, in the presence of multiple patterns, both the"] # [doc = " pattern ID and the capture group index are required to uniquely"] # [doc = " identify a capturing group."] group_index : SmallIndex , # [doc = " The next state that this state should transition to."] next : StateID , } , # [doc = " An empty state that records the end of a capture location. This is an"] # [doc = " unconditional epsilon transition like `Empty`, except it can be used to"] # [doc = " record position information for a capture group when using the NFA for"] # [doc = " search."] CaptureEnd { # [doc = " The ID of the pattern that this capture was defined."] pattern_id : PatternID , # [doc = " The capture group index that this capture state corresponds to."] # [doc = " The capture group index is always relative to its corresponding"] # [doc = " pattern. Therefore, in the presence of multiple patterns, both the"] # [doc = " pattern ID and the capture group index are required to uniquely"] # [doc = " identify a capturing group."] group_index : SmallIndex , # [doc = " The next state that this state should transition to."] next : StateID , } , # [doc = " An alternation such that there exists an epsilon transition to all"] # [doc = " states in `alternates`, where matches found via earlier transitions"] # [doc = " are preferred over later transitions."] Union { alternates : Vec < StateID > } , # [doc = " An alternation such that there exists an epsilon transition to all"] # [doc = " states in `alternates`, where matches found via later transitions are"] # [doc = " preferred over earlier transitions."] # [doc = ""] # [doc = " This \"reverse\" state exists for convenience during compilation that"] # [doc = " permits easy construction of non-greedy combinations of NFA states. At"] # [doc = " the end of compilation, Union and UnionReverse states are merged into"] # [doc = " one Union type of state, where the latter has its epsilon transitions"] # [doc = " reversed to reflect the priority inversion."] # [doc = ""] # [doc = " The \"convenience\" here arises from the fact that as new states are"] # [doc = " added to the list of `alternates`, we would like that add operation"] # [doc = " to be amortized constant time. But if we used a `Union`, we'd need to"] # [doc = " prepend the state, which takes O(n) time. There are other approaches we"] # [doc = " could use to solve this, but this seems simple enough."] UnionReverse { alternates : Vec < StateID > } , # [doc = " A state that cannot be transitioned out of. This is useful for cases"] # [doc = " where you want to prevent matching from occurring. For example, if your"] # [doc = " regex parser permits empty character classes, then one could choose a"] # [doc = " `Fail` state to represent it."] Fail , # [doc = " A match state. There is at most one such occurrence of this state in"] # [doc = " an NFA for each pattern compiled into the NFA. At time of writing, a"] # [doc = " match state is always produced for every pattern given, but in theory,"] # [doc = " if a pattern can never lead to a match, then the match state could be"] # [doc = " omitted."] # [doc = ""] # [doc = " `pattern_id` refers to the ID of the pattern itself, which corresponds"] # [doc = " to the pattern's index (starting at 0)."] Match { pattern_id : PatternID } , }
};
}
