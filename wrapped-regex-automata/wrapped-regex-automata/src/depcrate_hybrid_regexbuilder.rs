// Generated macro for Builder (struct)
macro_rules! Depcrate_hybrid_regexBuilder {
() => {
// Module: crate::hybrid::regex
// Provides: {"Builder"}
// Dependencies: {}
# [doc = " A builder for a regex based on a hybrid NFA/DFA."] # [doc = ""] # [doc = " This builder permits configuring options for the syntax of a pattern, the"] # [doc = " NFA construction, the lazy DFA construction and finally the regex searching"] # [doc = " itself. This builder is different from a general purpose regex builder"] # [doc = " in that it permits fine grain configuration of the construction process."] # [doc = " The trade off for this is complexity, and the possibility of setting a"] # [doc = " configuration that might not make sense. For example, there are two"] # [doc = " different UTF-8 modes:"] # [doc = ""] # [doc = " * [`syntax::Config::utf8`](crate::util::syntax::Config::utf8) controls"] # [doc = " whether the pattern itself can contain sub-expressions that match invalid"] # [doc = " UTF-8."] # [doc = " * [`thompson::Config::utf8`] controls how the regex iterators themselves"] # [doc = " advance the starting position of the next search when a match with zero"] # [doc = " length is found."] # [doc = ""] # [doc = " Generally speaking, callers will want to either enable all of these or"] # [doc = " disable all of these."] # [doc = ""] # [doc = " Internally, building a regex requires building two hybrid NFA/DFAs,"] # [doc = " where one is responsible for finding the end of a match and the other is"] # [doc = " responsible for finding the start of a match. If you only need to detect"] # [doc = " whether something matched, or only the end of a match, then you should use"] # [doc = " a [`dfa::Builder`] to construct a single hybrid NFA/DFA, which is cheaper"] # [doc = " than building two of them."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to disable UTF-8 mode in the syntax and the regex"] # [doc = " itself. This is generally what you want for matching on arbitrary bytes."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::regex::Regex, nfa::thompson, util::syntax, Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .syntax(syntax::Config::new().utf8(false))"] # [doc = "     .thompson(thompson::Config::new().utf8(false))"] # [doc = "     .build(r\"foo(?-u:[^b])ar.*\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let haystack = b\"\\xFEfoo\\xFFarzz\\xE2\\x98\\xFF\\n\";"] # [doc = " let expected = Some(Match::must(0, 1..9));"] # [doc = " let got = re.find(&mut cache, haystack);"] # [doc = " assert_eq!(expected, got);"] # [doc = " // Notice that `(?-u:[^b])` matches invalid UTF-8,"] # [doc = " // but the subsequent `.*` does not! Disabling UTF-8"] # [doc = " // on the syntax permits this."] # [doc = " assert_eq!(b\"foo\\xFFarzz\", &haystack[got.unwrap().range()]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Debug)] pub struct Builder { dfa : dfa :: Builder , }
};
}
