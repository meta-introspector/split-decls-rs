// Generated macro for Cache (struct)
macro_rules! Depcrate_meta_regexCache {
() => {
// Module: crate::meta::regex
// Provides: {"Cache"}
// Dependencies: {}
# [doc = " Represents mutable scratch space used by regex engines during a search."] # [doc = ""] # [doc = " Most of the regex engines in this crate require some kind of"] # [doc = " mutable state in order to execute a search. This mutable state is"] # [doc = " explicitly separated from the core regex object (such as a"] # [doc = " [`thompson::NFA`](crate::nfa::thompson::NFA)) so that the read-only regex"] # [doc = " object can be shared across multiple threads simultaneously without any"] # [doc = " synchronization. Conversely, a `Cache` must either be duplicated if using"] # [doc = " the same `Regex` from multiple threads, or else there must be some kind of"] # [doc = " synchronization that guarantees exclusive access while it's in use by one"] # [doc = " thread."] # [doc = ""] # [doc = " A `Regex` attempts to do this synchronization for you by using a thread"] # [doc = " pool internally. Its size scales roughly with the number of simultaneous"] # [doc = " regex searches."] # [doc = ""] # [doc = " For cases where one does not want to rely on a `Regex`'s internal thread"] # [doc = " pool, lower level routines such as [`Regex::search_with`] are provided"] # [doc = " that permit callers to pass a `Cache` into the search routine explicitly."] # [doc = ""] # [doc = " General advice is that the thread pool is often more than good enough."] # [doc = " However, it may be possible to observe the effects of its latency,"] # [doc = " especially when searching many small haystacks from many threads"] # [doc = " simultaneously."] # [doc = ""] # [doc = " Caches can be created from their corresponding `Regex` via"] # [doc = " [`Regex::create_cache`]. A cache can only be used with either the `Regex`"] # [doc = " that created it, or the `Regex` that was most recently used to reset it"] # [doc = " with [`Cache::reset`]. Using a cache with any other `Regex` may result in"] # [doc = " panics or incorrect results."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Input, Match};"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?-u)m\\w+\\s+m\\w+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let input = Input::new(\"crazy janey and her mission man\");"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 20..31)),"] # [doc = "     re.search_with(&mut cache, &input),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Debug , Clone)] pub struct Cache { pub (crate) capmatches : Captures , pub (crate) pikevm : wrappers :: PikeVMCache , pub (crate) backtrack : wrappers :: BoundedBacktrackerCache , pub (crate) onepass : wrappers :: OnePassCache , pub (crate) hybrid : wrappers :: HybridCache , pub (crate) revhybrid : wrappers :: ReverseHybridCache , }
};
}
