// Generated macro for Transitions (struct)
macro_rules! Depcrate_dfa_sparseTransitions {
() => {
// Module: crate::dfa::sparse
// Provides: {"Transitions"}
// Dependencies: {}
# [doc = " The transition table portion of a sparse DFA."] # [doc = ""] # [doc = " The transition table is the core part of the DFA in that it describes how"] # [doc = " to move from one state to another based on the input sequence observed."] # [doc = ""] # [doc = " Unlike a typical dense table based DFA, states in a sparse transition"] # [doc = " table have variable size. That is, states with more transitions use more"] # [doc = " space than states with fewer transitions. This means that finding the next"] # [doc = " transition takes more work than with a dense DFA, but also typically uses"] # [doc = " much less space."] # [derive (Clone)] struct Transitions < T > { # [doc = " The raw encoding of each state in this DFA."] # [doc = ""] # [doc = " Each state has the following information:"] # [doc = ""] # [doc = " * A set of transitions to subsequent states. Transitions to the dead"] # [doc = "   state are omitted."] # [doc = " * If the state can be accelerated, then any additional accelerator"] # [doc = "   information."] # [doc = " * If the state is a match state, then the state contains all pattern"] # [doc = "   IDs that match when in that state."] # [doc = ""] # [doc = " To decode a state, use Transitions::state."] # [doc = ""] # [doc = " In practice, T is either Vec<u8> or &[u8]."] sparse : T , # [doc = " A set of equivalence classes, where a single equivalence class"] # [doc = " represents a set of bytes that never discriminate between a match"] # [doc = " and a non-match in the DFA. Each equivalence class corresponds to a"] # [doc = " single character in this DFA's alphabet, where the maximum number of"] # [doc = " characters is 257 (each possible value of a byte plus the special"] # [doc = " EOI transition). Consequently, the number of equivalence classes"] # [doc = " corresponds to the number of transitions for each DFA state. Note"] # [doc = " though that the *space* used by each DFA state in the transition table"] # [doc = " may be larger. The total space used by each DFA state is known as the"] # [doc = " stride and is documented above."] # [doc = ""] # [doc = " The only time the number of equivalence classes is fewer than 257 is"] # [doc = " if the DFA's kind uses byte classes which is the default. Equivalence"] # [doc = " classes should generally only be disabled when debugging, so that"] # [doc = " the transitions themselves aren't obscured. Disabling them has no"] # [doc = " other benefit, since the equivalence class map is always used while"] # [doc = " searching. In the vast majority of cases, the number of equivalence"] # [doc = " classes is substantially smaller than 257, particularly when large"] # [doc = " Unicode classes aren't used."] # [doc = ""] # [doc = " N.B. Equivalence classes aren't particularly useful in a sparse DFA"] # [doc = " in the current implementation, since equivalence classes generally tend"] # [doc = " to correspond to continuous ranges of bytes that map to the same"] # [doc = " transition. So in a sparse DFA, equivalence classes don't really lead"] # [doc = " to a space savings. In the future, it would be good to try and remove"] # [doc = " them from sparse DFAs entirely, but requires a bit of work since sparse"] # [doc = " DFAs are built from dense DFAs, which are in turn built on top of"] # [doc = " equivalence classes."] classes : ByteClasses , # [doc = " The total number of states in this DFA. Note that a DFA always has at"] # [doc = " least one state---the dead state---even the empty DFA. In particular,"] # [doc = " the dead state always has ID 0 and is correspondingly always the first"] # [doc = " state. The dead state is never a match state."] state_len : usize , # [doc = " The total number of unique patterns represented by these match states."] pattern_len : usize , }
};
}
