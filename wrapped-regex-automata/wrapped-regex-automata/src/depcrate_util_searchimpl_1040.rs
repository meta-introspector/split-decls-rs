// Generated macro for impl_1040 (impl)
macro_rules! Depcrate_util_searchimpl_1040 {
() => {
// Module: crate::util::search
// Provides: {"impl_1040"}
// Dependencies: {}
impl < 'h > Input < 'h > { # [doc = " Create a new search configuration for the given haystack."] # [inline] pub fn new < H : ? Sized + AsRef < [u8] > > (haystack : & 'h H) -> Input < 'h > { let haystack = haystack . as_ref () ; Input { haystack , span : Span { start : 0 , end : haystack . len () } , anchored : Anchored :: No , earliest : false , } } # [doc = " Set the span for this search."] # [doc = ""] # [doc = " This routine does not panic if the span given is not a valid range for"] # [doc = " this search's haystack. If this search is run with an invalid range,"] # [doc = " then the most likely outcome is that the actual search execution will"] # [doc = " panic."] # [doc = ""] # [doc = " This routine is generic over how a span is provided. While"] # [doc = " a [`Span`] may be given directly, one may also provide a"] # [doc = " `std::ops::Range<usize>`. To provide anything supported by range"] # [doc = " syntax, use the [`Input::range`] method."] # [doc = ""] # [doc = " The default span is the entire haystack."] # [doc = ""] # [doc = " Note that [`Input::range`] overrides this method and vice versa."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics if the given span does not correspond to valid bounds in"] # [doc = " the haystack or the termination of a search."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how the span of the search can impact whether a"] # [doc = " match is reported or not. This is particularly relevant for look-around"] # [doc = " operators, which might take things outside of the span into account"] # [doc = " when determining whether they match."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     Match, Input,"] # [doc = " };"] # [doc = ""] # [doc = " // Look for 'at', but as a distinct word."] # [doc = " let re = PikeVM::new(r\"\\bat\\b\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = ""] # [doc = " // Our haystack contains 'at', but not as a distinct word."] # [doc = " let haystack = \"batter\";"] # [doc = ""] # [doc = " // A standard search finds nothing, as expected."] # [doc = " let input = Input::new(haystack);"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(None, caps.get_match());"] # [doc = ""] # [doc = " // But if we wanted to search starting at position '1', we might"] # [doc = " // slice the haystack. If we do this, it's impossible for the \\b"] # [doc = " // anchors to take the surrounding context into account! And thus,"] # [doc = " // a match is produced."] # [doc = " let input = Input::new(&haystack[1..3]);"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(Some(Match::must(0, 0..2)), caps.get_match());"] # [doc = ""] # [doc = " // But if we specify the span of the search instead of slicing the"] # [doc = " // haystack, then the regex engine can \"see\" outside of the span"] # [doc = " // and resolve the anchors correctly."] # [doc = " let input = Input::new(haystack).span(1..3);"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(None, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " This may seem a little ham-fisted, but this scenario tends to come up"] # [doc = " if some other regex engine found the match span and now you need to"] # [doc = " re-process that span to look for capturing groups. (e.g., Run a faster"] # [doc = " DFA first, find a match, then run the PikeVM on just the match span to"] # [doc = " resolve capturing groups.) In order to implement that sort of logic"] # [doc = " correctly, you need to set the span on the search instead of slicing"] # [doc = " the haystack directly."] # [doc = ""] # [doc = " The other advantage of using this routine to specify the bounds of the"] # [doc = " search is that the match offsets are still reported in terms of the"] # [doc = " original haystack. For example, the second search in the example above"] # [doc = " reported a match at position `0`, even though `at` starts at offset"] # [doc = " `1` because we sliced the haystack."] # [inline] pub fn span < S : Into < Span > > (mut self , span : S) -> Input < 'h > { self . set_span (span) ; self } # [doc = " Like `Input::span`, but accepts any range instead."] # [doc = ""] # [doc = " This routine does not panic if the range given is not a valid range for"] # [doc = " this search's haystack. If this search is run with an invalid range,"] # [doc = " then the most likely outcome is that the actual search execution will"] # [doc = " panic."] # [doc = ""] # [doc = " The default range is the entire haystack."] # [doc = ""] # [doc = " Note that [`Input::span`] overrides this method and vice versa."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This routine will panic if the given range could not be converted"] # [doc = " to a valid [`Range`]. For example, this would panic when given"] # [doc = " `0..=usize::MAX` since it cannot be represented using a half-open"] # [doc = " interval in terms of `usize`."] # [doc = ""] # [doc = " This also panics if the given range does not correspond to valid bounds"] # [doc = " in the haystack or the termination of a search."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\");"] # [doc = " assert_eq!(0..6, input.get_range());"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\").range(2..=4);"] # [doc = " assert_eq!(2..5, input.get_range());"] # [doc = " ```"] # [inline] pub fn range < R : RangeBounds < usize > > (mut self , range : R) -> Input < 'h > { self . set_range (range) ; self } # [doc = " Sets the anchor mode of a search."] # [doc = ""] # [doc = " When a search is anchored (so that's [`Anchored::Yes`] or"] # [doc = " [`Anchored::Pattern`]), a match must begin at the start of a search."] # [doc = " When a search is not anchored (that's [`Anchored::No`]), regex engines"] # [doc = " will behave as if the pattern started with a `(?s-u:.)*?`. This prefix"] # [doc = " permits a match to appear anywhere."] # [doc = ""] # [doc = " By default, the anchored mode is [`Anchored::No`]."] # [doc = ""] # [doc = " **WARNING:** this is subtly different than using a `^` at the start of"] # [doc = " your regex. A `^` forces a regex to match exclusively at the start of"] # [doc = " a haystack, regardless of where you begin your search. In contrast,"] # [doc = " anchoring a search will allow your regex to match anywhere in your"] # [doc = " haystack, but the match must start at the beginning of a search."] # [doc = ""] # [doc = " For example, consider the haystack `aba` and the following searches:"] # [doc = ""] # [doc = " 1. The regex `^a` is compiled with `Anchored::No` and searches `aba`"] # [doc = "    starting at position `2`. Since `^` requires the match to start at"] # [doc = "    the beginning of the haystack and `2 > 0`, no match is found."] # [doc = " 2. The regex `a` is compiled with `Anchored::Yes` and searches `aba`"] # [doc = "    starting at position `2`. This reports a match at `[2, 3]` since"] # [doc = "    the match starts where the search started. Since there is no `^`,"] # [doc = "    there is no requirement for the match to start at the beginning of"] # [doc = "    the haystack."] # [doc = " 3. The regex `a` is compiled with `Anchored::Yes` and searches `aba`"] # [doc = "    starting at position `1`. Since `b` corresponds to position `1` and"] # [doc = "    since the search is anchored, it finds no match. While the regex"] # [doc = "    matches at other positions, configuring the search to be anchored"] # [doc = "    requires that it only report a match that begins at the same offset"] # [doc = "    as the beginning of the search."] # [doc = " 4. The regex `a` is compiled with `Anchored::No` and searches `aba`"] # [doc = "    starting at position `1`. Since the search is not anchored and"] # [doc = "    the regex does not start with `^`, the search executes as if there"] # [doc = "    is a `(?s:.)*?` prefix that permits it to match anywhere. Thus, it"] # [doc = "    reports a match at `[2, 3]`."] # [doc = ""] # [doc = " Note that the [`Anchored::Pattern`] mode is like `Anchored::Yes`,"] # [doc = " except it only reports matches for a particular pattern."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This demonstrates the differences between an anchored search and"] # [doc = " a pattern that begins with `^` (as described in the above warning"] # [doc = " message)."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     Anchored, Match, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let haystack = \"aba\";"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"^a\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = " let input = Input::new(haystack).span(2..3).anchored(Anchored::No);"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " // No match is found because 2 is not the beginning of the haystack,"] # [doc = " // which is what ^ requires."] # [doc = " assert_eq!(None, caps.get_match());"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"a\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = " let input = Input::new(haystack).span(2..3).anchored(Anchored::Yes);"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " // An anchored search can still match anywhere in the haystack, it just"] # [doc = " // must begin at the start of the search which is '2' in this case."] # [doc = " assert_eq!(Some(Match::must(0, 2..3)), caps.get_match());"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"a\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = " let input = Input::new(haystack).span(1..3).anchored(Anchored::Yes);"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " // No match is found since we start searching at offset 1 which"] # [doc = " // corresponds to 'b'. Since there is no '(?s:.)*?' prefix, no match"] # [doc = " // is found."] # [doc = " assert_eq!(None, caps.get_match());"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"a\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = " let input = Input::new(haystack).span(1..3).anchored(Anchored::No);"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " // Since anchored=no, an implicit '(?s:.)*?' prefix was added to the"] # [doc = " // pattern. Even though the search starts at 'b', the 'match anything'"] # [doc = " // prefix allows the search to match 'a'."] # [doc = " let expected = Some(Match::must(0, 2..3));"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn anchored (mut self , mode : Anchored) -> Input < 'h > { self . set_anchored (mode) ; self } # [doc = " Whether to execute an \"earliest\" search or not."] # [doc = ""] # [doc = " When running a non-overlapping search, an \"earliest\" search will return"] # [doc = " the match location as early as possible. For example, given a pattern"] # [doc = " of `foo[0-9]+` and a haystack of `foo12345`, a normal leftmost search"] # [doc = " will return `foo12345` as a match. But an \"earliest\" search for regex"] # [doc = " engines that support \"earliest\" semantics will return `foo1` as a"] # [doc = " match, since as soon as the first digit following `foo` is seen, it is"] # [doc = " known to have found a match."] # [doc = ""] # [doc = " Note that \"earliest\" semantics generally depend on the regex engine."] # [doc = " Different regex engines may determine there is a match at different"] # [doc = " points. So there is no guarantee that \"earliest\" matches will always"] # [doc = " return the same offsets for all regex engines. The \"earliest\" notion"] # [doc = " is really about when the particular regex engine determines there is"] # [doc = " a match rather than a consistent semantic unto itself. This is often"] # [doc = " useful for implementing \"did a match occur or not\" predicates, but"] # [doc = " sometimes the offset is useful as well."] # [doc = ""] # [doc = " This is disabled by default."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the difference between \"earliest\" searching and"] # [doc = " normal searching."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, Match, Input};"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"foo[0-9]+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = ""] # [doc = " // A normal search implements greediness like you expect."] # [doc = " let input = Input::new(\"foo12345\");"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(Some(Match::must(0, 0..8)), caps.get_match());"] # [doc = ""] # [doc = " // When 'earliest' is enabled and the regex engine supports"] # [doc = " // it, the search will bail once it knows a match has been"] # [doc = " // found."] # [doc = " let input = Input::new(\"foo12345\").earliest(true);"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(Some(Match::must(0, 0..4)), caps.get_match());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn earliest (mut self , yes : bool) -> Input < 'h > { self . set_earliest (yes) ; self } # [doc = " Set the span for this search configuration."] # [doc = ""] # [doc = " This is like the [`Input::span`] method, except this mutates the"] # [doc = " span in place."] # [doc = ""] # [doc = " This routine is generic over how a span is provided. While"] # [doc = " a [`Span`] may be given directly, one may also provide a"] # [doc = " `std::ops::Range<usize>`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics if the given span does not correspond to valid bounds in"] # [doc = " the haystack or the termination of a search."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let mut input = Input::new(\"foobar\");"] # [doc = " assert_eq!(0..6, input.get_range());"] # [doc = " input.set_span(2..4);"] # [doc = " assert_eq!(2..4, input.get_range());"] # [doc = " ```"] # [inline] pub fn set_span < S : Into < Span > > (& mut self , span : S) { let span = span . into () ; assert ! (span . end <= self . haystack . len () && span . start <= span . end . wrapping_add (1) , "invalid span {:?} for haystack of length {}" , span , self . haystack . len () ,) ; self . span = span ; } # [doc = " Set the span for this search configuration given any range."] # [doc = ""] # [doc = " This is like the [`Input::range`] method, except this mutates the"] # [doc = " span in place."] # [doc = ""] # [doc = " This routine does not panic if the range given is not a valid range for"] # [doc = " this search's haystack. If this search is run with an invalid range,"] # [doc = " then the most likely outcome is that the actual search execution will"] # [doc = " panic."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This routine will panic if the given range could not be converted"] # [doc = " to a valid [`Range`]. For example, this would panic when given"] # [doc = " `0..=usize::MAX` since it cannot be represented using a half-open"] # [doc = " interval in terms of `usize`."] # [doc = ""] # [doc = " This also panics if the given span does not correspond to valid bounds"] # [doc = " in the haystack or the termination of a search."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let mut input = Input::new(\"foobar\");"] # [doc = " assert_eq!(0..6, input.get_range());"] # [doc = " input.set_range(2..=4);"] # [doc = " assert_eq!(2..5, input.get_range());"] # [doc = " ```"] # [inline] pub fn set_range < R : RangeBounds < usize > > (& mut self , range : R) { use core :: ops :: Bound ; let start = match range . start_bound () { Bound :: Included (& i) => i , Bound :: Excluded (& i) => i . checked_add (1) . unwrap () , Bound :: Unbounded => 0 , } ; let end = match range . end_bound () { Bound :: Included (& i) => i . checked_add (1) . unwrap () , Bound :: Excluded (& i) => i , Bound :: Unbounded => self . haystack () . len () , } ; self . set_span (Span { start , end }) ; } # [doc = " Set the starting offset for the span for this search configuration."] # [doc = ""] # [doc = " This is a convenience routine for only mutating the start of a span"] # [doc = " without having to set the entire span."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics if the span resulting from the new start position does not"] # [doc = " correspond to valid bounds in the haystack or the termination of a"] # [doc = " search."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let mut input = Input::new(\"foobar\");"] # [doc = " assert_eq!(0..6, input.get_range());"] # [doc = " input.set_start(5);"] # [doc = " assert_eq!(5..6, input.get_range());"] # [doc = " ```"] # [inline] pub fn set_start (& mut self , start : usize) { self . set_span (Span { start , .. self . get_span () }) ; } # [doc = " Set the ending offset for the span for this search configuration."] # [doc = ""] # [doc = " This is a convenience routine for only mutating the end of a span"] # [doc = " without having to set the entire span."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics if the span resulting from the new end position does not"] # [doc = " correspond to valid bounds in the haystack or the termination of a"] # [doc = " search."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let mut input = Input::new(\"foobar\");"] # [doc = " assert_eq!(0..6, input.get_range());"] # [doc = " input.set_end(5);"] # [doc = " assert_eq!(0..5, input.get_range());"] # [doc = " ```"] # [inline] pub fn set_end (& mut self , end : usize) { self . set_span (Span { end , .. self . get_span () }) ; } # [doc = " Set the anchor mode of a search."] # [doc = ""] # [doc = " This is like [`Input::anchored`], except it mutates the search"] # [doc = " configuration in place."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{Anchored, Input, PatternID};"] # [doc = ""] # [doc = " let mut input = Input::new(\"foobar\");"] # [doc = " assert_eq!(Anchored::No, input.get_anchored());"] # [doc = ""] # [doc = " let pid = PatternID::must(5);"] # [doc = " input.set_anchored(Anchored::Pattern(pid));"] # [doc = " assert_eq!(Anchored::Pattern(pid), input.get_anchored());"] # [doc = " ```"] # [inline] pub fn set_anchored (& mut self , mode : Anchored) { self . anchored = mode ; } # [doc = " Set whether the search should execute in \"earliest\" mode or not."] # [doc = ""] # [doc = " This is like [`Input::earliest`], except it mutates the search"] # [doc = " configuration in place."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let mut input = Input::new(\"foobar\");"] # [doc = " assert!(!input.get_earliest());"] # [doc = " input.set_earliest(true);"] # [doc = " assert!(input.get_earliest());"] # [doc = " ```"] # [inline] pub fn set_earliest (& mut self , yes : bool) { self . earliest = yes ; } # [doc = " Return a borrow of the underlying haystack as a slice of bytes."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\");"] # [doc = " assert_eq!(b\"foobar\", input.haystack());"] # [doc = " ```"] # [inline] pub fn haystack (& self) -> & 'h [u8] { self . haystack } # [doc = " Return the start position of this search."] # [doc = ""] # [doc = " This is a convenience routine for `search.get_span().start()`."] # [doc = ""] # [doc = " When [`Input::is_done`] is `false`, this is guaranteed to return"] # [doc = " an offset that is less than or equal to [`Input::end`]. Otherwise,"] # [doc = " the offset is one greater than [`Input::end`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\");"] # [doc = " assert_eq!(0, input.start());"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\").span(2..4);"] # [doc = " assert_eq!(2, input.start());"] # [doc = " ```"] # [inline] pub fn start (& self) -> usize { self . get_span () . start } # [doc = " Return the end position of this search."] # [doc = ""] # [doc = " This is a convenience routine for `search.get_span().end()`."] # [doc = ""] # [doc = " This is guaranteed to return an offset that is a valid exclusive end"] # [doc = " bound for this input's haystack."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\");"] # [doc = " assert_eq!(6, input.end());"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\").span(2..4);"] # [doc = " assert_eq!(4, input.end());"] # [doc = " ```"] # [inline] pub fn end (& self) -> usize { self . get_span () . end } # [doc = " Return the span for this search configuration."] # [doc = ""] # [doc = " If one was not explicitly set, then the span corresponds to the entire"] # [doc = " range of the haystack."] # [doc = ""] # [doc = " When [`Input::is_done`] is `false`, the span returned is guaranteed"] # [doc = " to correspond to valid bounds for this input's haystack."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{Input, Span};"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\");"] # [doc = " assert_eq!(Span { start: 0, end: 6 }, input.get_span());"] # [doc = " ```"] # [inline] pub fn get_span (& self) -> Span { self . span } # [doc = " Return the span as a range for this search configuration."] # [doc = ""] # [doc = " If one was not explicitly set, then the span corresponds to the entire"] # [doc = " range of the haystack."] # [doc = ""] # [doc = " When [`Input::is_done`] is `false`, the range returned is guaranteed"] # [doc = " to correspond to valid bounds for this input's haystack."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\");"] # [doc = " assert_eq!(0..6, input.get_range());"] # [doc = " ```"] # [inline] pub fn get_range (& self) -> Range < usize > { self . get_span () . range () } # [doc = " Return the anchored mode for this search configuration."] # [doc = ""] # [doc = " If no anchored mode was set, then it defaults to [`Anchored::No`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{Anchored, Input, PatternID};"] # [doc = ""] # [doc = " let mut input = Input::new(\"foobar\");"] # [doc = " assert_eq!(Anchored::No, input.get_anchored());"] # [doc = ""] # [doc = " let pid = PatternID::must(5);"] # [doc = " input.set_anchored(Anchored::Pattern(pid));"] # [doc = " assert_eq!(Anchored::Pattern(pid), input.get_anchored());"] # [doc = " ```"] # [inline] pub fn get_anchored (& self) -> Anchored { self . anchored } # [doc = " Return whether this search should execute in \"earliest\" mode."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\");"] # [doc = " assert!(!input.get_earliest());"] # [doc = " ```"] # [inline] pub fn get_earliest (& self) -> bool { self . earliest } # [doc = " Return true if and only if this search can never return any other"] # [doc = " matches."] # [doc = ""] # [doc = " This occurs when the start position of this search is greater than the"] # [doc = " end position of the search."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let mut input = Input::new(\"foobar\");"] # [doc = " assert!(!input.is_done());"] # [doc = " input.set_start(6);"] # [doc = " assert!(!input.is_done());"] # [doc = " input.set_start(7);"] # [doc = " assert!(input.is_done());"] # [doc = " ```"] # [inline] pub fn is_done (& self) -> bool { self . get_span () . start > self . get_span () . end } # [doc = " Returns true if and only if the given offset in this search's haystack"] # [doc = " falls on a valid UTF-8 encoded codepoint boundary."] # [doc = ""] # [doc = " If the haystack is not valid UTF-8, then the behavior of this routine"] # [doc = " is unspecified."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows where codepoint boundaries do and don't exist in valid"] # [doc = " UTF-8."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::Input;"] # [doc = ""] # [doc = " let input = Input::new(\"☃\");"] # [doc = " assert!(input.is_char_boundary(0));"] # [doc = " assert!(!input.is_char_boundary(1));"] # [doc = " assert!(!input.is_char_boundary(2));"] # [doc = " assert!(input.is_char_boundary(3));"] # [doc = " assert!(!input.is_char_boundary(4));"] # [doc = " ```"] # [inline] pub fn is_char_boundary (& self , offset : usize) -> bool { utf8 :: is_boundary (self . haystack () , offset) } }
};
}
