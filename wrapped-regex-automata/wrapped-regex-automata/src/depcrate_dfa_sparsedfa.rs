// Generated macro for DFA (struct)
macro_rules! Depcrate_dfa_sparseDFA {
() => {
// Module: crate::dfa::sparse
// Provides: {"DFA"}
// Dependencies: {}
# [doc = " A sparse deterministic finite automaton (DFA) with variable sized states."] # [doc = ""] # [doc = " In contrast to a [dense::DFA], a sparse DFA uses a more space efficient"] # [doc = " representation for its transitions. Consequently, sparse DFAs may use much"] # [doc = " less memory than dense DFAs, but this comes at a price. In particular,"] # [doc = " reading the more space efficient transitions takes more work, and"] # [doc = " consequently, searching using a sparse DFA is typically slower than a dense"] # [doc = " DFA."] # [doc = ""] # [doc = " A sparse DFA can be built using the default configuration via the"] # [doc = " [`DFA::new`] constructor. Otherwise, one can configure various aspects of a"] # [doc = " dense DFA via [`dense::Builder`], and then convert a dense DFA to a sparse"] # [doc = " DFA using [`dense::DFA::to_sparse`]."] # [doc = ""] # [doc = " In general, a sparse DFA supports all the same search operations as a dense"] # [doc = " DFA."] # [doc = ""] # [doc = " Making the choice between a dense and sparse DFA depends on your specific"] # [doc = " work load. If you can sacrifice a bit of search time performance, then a"] # [doc = " sparse DFA might be the best choice. In particular, while sparse DFAs are"] # [doc = " probably always slower than dense DFAs, you may find that they are easily"] # [doc = " fast enough for your purposes!"] # [doc = ""] # [doc = " # Type parameters"] # [doc = ""] # [doc = " A `DFA` has one type parameter, `T`, which is used to represent the parts"] # [doc = " of a sparse DFA. `T` is typically a `Vec<u8>` or a `&[u8]`."] # [doc = ""] # [doc = " # The `Automaton` trait"] # [doc = ""] # [doc = " This type implements the [`Automaton`] trait, which means it can be used"] # [doc = " for searching. For example:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, sparse::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " let dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone)] pub struct DFA < T > { tt : Transitions < T > , st : StartTable < T > , special : Special , pre : Option < Prefilter > , quitset : ByteSet , flags : Flags , }
};
}
