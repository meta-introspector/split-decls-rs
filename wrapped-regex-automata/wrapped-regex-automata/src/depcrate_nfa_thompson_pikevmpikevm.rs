// Generated macro for PikeVM (struct)
macro_rules! Depcrate_nfa_thompson_pikevmPikeVM {
() => {
// Module: crate::nfa::thompson::pikevm
// Provides: {"PikeVM"}
// Dependencies: {}
# [doc = " A virtual machine for executing regex searches with capturing groups."] # [doc = ""] # [doc = " # Infallible APIs"] # [doc = ""] # [doc = " Unlike most other regex engines in this crate, a `PikeVM` never returns an"] # [doc = " error at search time. It supports all [`Anchored`] configurations, never"] # [doc = " quits and works on haystacks of arbitrary length."] # [doc = ""] # [doc = " There are two caveats to mention though:"] # [doc = ""] # [doc = " * If an invalid pattern ID is given to a search via [`Anchored::Pattern`],"] # [doc = " then the PikeVM will report \"no match.\" This is consistent with all other"] # [doc = " regex engines in this crate."] # [doc = " * When using [`PikeVM::which_overlapping_matches`] with a [`PatternSet`]"] # [doc = " that has insufficient capacity to store all valid pattern IDs, then if a"] # [doc = " match occurs for a `PatternID` that cannot be inserted, it is silently"] # [doc = " dropped as if it did not match."] # [doc = ""] # [doc = " # Advice"] # [doc = ""] # [doc = " The `PikeVM` is generally the most \"powerful\" regex engine in this crate."] # [doc = " \"Powerful\" in this context means that it can handle any regular expression"] # [doc = " that is parseable by `regex-syntax` and any size haystack. Regrettably,"] # [doc = " the `PikeVM` is also simultaneously often the _slowest_ regex engine in"] # [doc = " practice. This results in an annoying situation where one generally tries"] # [doc = " to pick any other regex engine (or perhaps none at all) before being"] # [doc = " forced to fall back to a `PikeVM`."] # [doc = ""] # [doc = " For example, a common strategy for dealing with capturing groups is to"] # [doc = " actually look for the overall match of the regex using a faster regex"] # [doc = " engine, like a [lazy DFA](crate::hybrid::regex::Regex). Once the overall"] # [doc = " match is found, one can then run the `PikeVM` on just the match span to"] # [doc = " find the spans of the capturing groups. In this way, the faster regex"] # [doc = " engine does the majority of the work, while the `PikeVM` only lends its"] # [doc = " power in a more limited role."] # [doc = ""] # [doc = " Unfortunately, this isn't always possible because the faster regex engines"] # [doc = " don't support all of the regex features in `regex-syntax`. This notably"] # [doc = " includes (and is currently limited to) Unicode word boundaries. So if"] # [doc = " your pattern has Unicode word boundaries, you typically can't use a"] # [doc = " DFA-based regex engine at all (unless you [enable heuristic support for"] # [doc = " it](crate::hybrid::dfa::Config::unicode_word_boundary)). (The [one-pass"] # [doc = " DFA](crate::dfa::onepass::DFA) can handle Unicode word boundaries for"] # [doc = " anchored searches only, but in a cruel sort of joke, many Unicode features"] # [doc = " tend to result in making the regex _not_ one-pass.)"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows that the `PikeVM` implements Unicode word boundaries"] # [doc = " correctly by default."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, Match};"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"\\b\\w+\\b\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let mut it = re.find_iter(&mut cache, \"Шерлок Холмс\");"] # [doc = " assert_eq!(Some(Match::must(0, 0..12)), it.next());"] # [doc = " assert_eq!(Some(Match::must(0, 13..23)), it.next());"] # [doc = " assert_eq!(None, it.next());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Debug)] pub struct PikeVM { config : Config , nfa : NFA , }
};
}
