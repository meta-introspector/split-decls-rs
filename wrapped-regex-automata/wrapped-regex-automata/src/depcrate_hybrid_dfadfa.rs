// Generated macro for DFA (struct)
macro_rules! Depcrate_hybrid_dfaDFA {
() => {
// Module: crate::hybrid::dfa
// Provides: {"DFA"}
// Dependencies: {}
# [doc = " A hybrid NFA/DFA (also called a \"lazy DFA\") for regex searching."] # [doc = ""] # [doc = " A lazy DFA is a DFA that builds itself at search time. It otherwise has"] # [doc = " very similar characteristics as a [`dense::DFA`](crate::dfa::dense::DFA)."] # [doc = " Indeed, both support precisely the same regex features with precisely the"] # [doc = " same semantics."] # [doc = ""] # [doc = " Where as a `dense::DFA` must be completely built to handle any input before"] # [doc = " it may be used for search, a lazy DFA starts off effectively empty. During"] # [doc = " a search, a lazy DFA will build itself depending on whether it has already"] # [doc = " computed the next transition or not. If it has, then it looks a lot like"] # [doc = " a `dense::DFA` internally: it does a very fast table based access to find"] # [doc = " the next transition. Otherwise, if the state hasn't been computed, then it"] # [doc = " does determinization _for that specific transition_ to compute the next DFA"] # [doc = " state."] # [doc = ""] # [doc = " The main selling point of a lazy DFA is that, in practice, it has"] # [doc = " the performance profile of a `dense::DFA` without the weakness of it"] # [doc = " taking worst case exponential time to build. Indeed, for each byte of"] # [doc = " input, the lazy DFA will construct as most one new DFA state. Thus, a"] # [doc = " lazy DFA achieves worst case `O(mn)` time for regex search (where `m ~"] # [doc = " pattern.len()` and `n ~ haystack.len()`)."] # [doc = ""] # [doc = " The main downsides of a lazy DFA are:"] # [doc = ""] # [doc = " 1. It requires mutable \"cache\" space during search. This is where the"] # [doc = " transition table, among other things, is stored."] # [doc = " 2. In pathological cases (e.g., if the cache is too small), it will run"] # [doc = " out of room and either require a bigger cache capacity or will repeatedly"] # [doc = " clear the cache and thus repeatedly regenerate DFA states. Overall, this"] # [doc = " will tend to be slower than a typical NFA simulation."] # [doc = ""] # [doc = " # Capabilities"] # [doc = ""] # [doc = " Like a `dense::DFA`, a single lazy DFA fundamentally supports the following"] # [doc = " operations:"] # [doc = ""] # [doc = " 1. Detection of a match."] # [doc = " 2. Location of the end of a match."] # [doc = " 3. In the case of a lazy DFA with multiple patterns, which pattern matched"] # [doc = " is reported as well."] # [doc = ""] # [doc = " A notable absence from the above list of capabilities is the location of"] # [doc = " the *start* of a match. In order to provide both the start and end of"] # [doc = " a match, *two* lazy DFAs are required. This functionality is provided by a"] # [doc = " [`Regex`](crate::hybrid::regex::Regex)."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to build a lazy DFA with the default configuration and"] # [doc = " execute a search. Notice how, in contrast to a `dense::DFA`, we must create"] # [doc = " a cache and pass it to our search routine."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::dfa::DFA, HalfMatch, Input};"] # [doc = ""] # [doc = " let dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd("] # [doc = "     &mut cache, &Input::new(\"foo12345\"))?,"] # [doc = " );"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Debug)] pub struct DFA { config : Config , nfa : thompson :: NFA , stride2 : usize , start_map : StartByteMap , classes : ByteClasses , quitset : ByteSet , cache_capacity : usize , }
};
}
