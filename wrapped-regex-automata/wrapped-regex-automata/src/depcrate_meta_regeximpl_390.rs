// Generated macro for impl_390 (impl)
macro_rules! Depcrate_meta_regeximpl_390 {
() => {
// Module: crate::meta::regex
// Provides: {"impl_390"}
// Dependencies: {}
# [doc = " High level convenience routines for using a regex to search a haystack."] impl Regex { # [doc = " Returns true if and only if this regex matches the given haystack."] # [doc = ""] # [doc = " This routine may short circuit if it knows that scanning future input"] # [doc = " will never lead to a different result. (Consider how this might make"] # [doc = " a difference given the regex `a+` on the haystack `aaaaaaaaaaaaaaa`."] # [doc = " This routine _may_ stop after it sees the first `a`, but routines like"] # [doc = " `find` need to continue searching because `+` is greedy by default.)"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(\"foo[0-9]+bar\")?;"] # [doc = ""] # [doc = " assert!(re.is_match(\"foo12345bar\"));"] # [doc = " assert!(!re.is_match(\"foobar\"));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: consistency with search APIs"] # [doc = ""] # [doc = " `is_match` is guaranteed to return `true` whenever `find` returns a"] # [doc = " match. This includes searches that are executed entirely within a"] # [doc = " codepoint:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Input};"] # [doc = ""] # [doc = " let re = Regex::new(\"a*\")?;"] # [doc = ""] # [doc = " // This doesn't match because the default configuration bans empty"] # [doc = " // matches from splitting a codepoint."] # [doc = " assert!(!re.is_match(Input::new(\"☃\").span(1..2)));"] # [doc = " assert_eq!(None, re.find(Input::new(\"☃\").span(1..2)));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Notice that when UTF-8 mode is disabled, then the above reports a"] # [doc = " match because the restriction against zero-width matches that split a"] # [doc = " codepoint has been lifted:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Input, Match};"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .configure(Regex::config().utf8_empty(false))"] # [doc = "     .build(\"a*\")?;"] # [doc = ""] # [doc = " assert!(re.is_match(Input::new(\"☃\").span(1..2)));"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 1..1)),"] # [doc = "     re.find(Input::new(\"☃\").span(1..2)),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " A similar idea applies when using line anchors with CRLF mode enabled,"] # [doc = " which prevents them from matching between a `\\r` and a `\\n`."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Input, Match};"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?Rm:$)\")?;"] # [doc = " assert!(!re.is_match(Input::new(\"\\r\\n\").span(1..1)));"] # [doc = " // A regular line anchor, which only considers \\n as a"] # [doc = " // line terminator, will match."] # [doc = " let re = Regex::new(r\"(?m:$)\")?;"] # [doc = " assert!(re.is_match(Input::new(\"\\r\\n\").span(1..1)));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn is_match < 'h , I : Into < Input < 'h > > > (& self , input : I) -> bool { let input = input . into () . earliest (true) ; if self . imp . info . is_impossible (& input) { return false ; } let mut guard = self . pool . get () ; let result = self . imp . strat . is_match (& mut guard , & input) ; PoolGuard :: put (guard) ; result } # [doc = " Executes a leftmost search and returns the first match that is found,"] # [doc = " if one exists."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Match};"] # [doc = ""] # [doc = " let re = Regex::new(\"foo[0-9]+\")?;"] # [doc = " assert_eq!(Some(Match::must(0, 0..8)), re.find(\"foo12345\"));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn find < 'h , I : Into < Input < 'h > > > (& self , input : I) -> Option < Match > { self . search (& input . into ()) } # [doc = " Executes a leftmost forward search and writes the spans of capturing"] # [doc = " groups that participated in a match into the provided [`Captures`]"] # [doc = " value. If no match was found, then [`Captures::is_match`] is guaranteed"] # [doc = " to return `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Span};"] # [doc = ""] # [doc = " let re = Regex::new(r\"^([0-9]{4})-([0-9]{2})-([0-9]{2})$\")?;"] # [doc = " let mut caps = re.create_captures();"] # [doc = ""] # [doc = " re.captures(\"2010-03-14\", &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " assert_eq!(Some(Span::from(0..4)), caps.get_group(1));"] # [doc = " assert_eq!(Some(Span::from(5..7)), caps.get_group(2));"] # [doc = " assert_eq!(Some(Span::from(8..10)), caps.get_group(3));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn captures < 'h , I : Into < Input < 'h > > > (& self , input : I , caps : & mut Captures ,) { self . search_captures (& input . into () , caps) } # [doc = " Returns an iterator over all non-overlapping leftmost matches in"] # [doc = " the given haystack. If no match exists, then the iterator yields no"] # [doc = " elements."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Match};"] # [doc = ""] # [doc = " let re = Regex::new(\"foo[0-9]+\")?;"] # [doc = " let haystack = \"foo1 foo12 foo123\";"] # [doc = " let matches: Vec<Match> = re.find_iter(haystack).collect();"] # [doc = " assert_eq!(matches, vec!["] # [doc = "     Match::must(0, 0..4),"] # [doc = "     Match::must(0, 5..10),"] # [doc = "     Match::must(0, 11..17),"] # [doc = " ]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn find_iter < 'r , 'h , I : Into < Input < 'h > > > (& 'r self , input : I ,) -> FindMatches < 'r , 'h > { let cache = self . pool . get () ; let it = iter :: Searcher :: new (input . into ()) ; FindMatches { re : self , cache , it } } # [doc = " Returns an iterator over all non-overlapping `Captures` values. If no"] # [doc = " match exists, then the iterator yields no elements."] # [doc = ""] # [doc = " This yields the same matches as [`Regex::find_iter`], but it includes"] # [doc = " the spans of all capturing groups that participate in each match."] # [doc = ""] # [doc = " **Tip:** See [`util::iter::Searcher`](crate::util::iter::Searcher) for"] # [doc = " how to correctly iterate over all matches in a haystack while avoiding"] # [doc = " the creation of a new `Captures` value for every match. (Which you are"] # [doc = " forced to do with an `Iterator`.)"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Span};"] # [doc = ""] # [doc = " let re = Regex::new(\"foo(?P<numbers>[0-9]+)\")?;"] # [doc = ""] # [doc = " let haystack = \"foo1 foo12 foo123\";"] # [doc = " let matches: Vec<Span> = re"] # [doc = "     .captures_iter(haystack)"] # [doc = "     // The unwrap is OK since 'numbers' matches if the pattern matches."] # [doc = "     .map(|caps| caps.get_group_by_name(\"numbers\").unwrap())"] # [doc = "     .collect();"] # [doc = " assert_eq!(matches, vec!["] # [doc = "     Span::from(3..4),"] # [doc = "     Span::from(8..10),"] # [doc = "     Span::from(14..17),"] # [doc = " ]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn captures_iter < 'r , 'h , I : Into < Input < 'h > > > (& 'r self , input : I ,) -> CapturesMatches < 'r , 'h > { let cache = self . pool . get () ; let caps = self . create_captures () ; let it = iter :: Searcher :: new (input . into ()) ; CapturesMatches { re : self , cache , caps , it } } # [doc = " Returns an iterator of spans of the haystack given, delimited by a"] # [doc = " match of the regex. Namely, each element of the iterator corresponds to"] # [doc = " a part of the haystack that *isn't* matched by the regular expression."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " To split a string delimited by arbitrary amounts of spaces or tabs:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"[ \\t]+\")?;"] # [doc = " let hay = \"a b \\t  c\\td    e\";"] # [doc = " let fields: Vec<&str> = re.split(hay).map(|span| &hay[span]).collect();"] # [doc = " assert_eq!(fields, vec![\"a\", \"b\", \"c\", \"d\", \"e\"]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: more cases"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\" \")?;"] # [doc = " let hay = \"Mary had a little lamb\";"] # [doc = " let got: Vec<&str> = re.split(hay).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"Mary\", \"had\", \"a\", \"little\", \"lamb\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\")?;"] # [doc = " let hay = \"\";"] # [doc = " let got: Vec<&str> = re.split(hay).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\")?;"] # [doc = " let hay = \"lionXXtigerXleopard\";"] # [doc = " let got: Vec<&str> = re.split(hay).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"lion\", \"\", \"tiger\", \"leopard\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"::\")?;"] # [doc = " let hay = \"lion::tiger::leopard\";"] # [doc = " let got: Vec<&str> = re.split(hay).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"lion\", \"tiger\", \"leopard\"]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " If a haystack contains multiple contiguous matches, you will end up"] # [doc = " with empty spans yielded by the iterator:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\")?;"] # [doc = " let hay = \"XXXXaXXbXc\";"] # [doc = " let got: Vec<&str> = re.split(hay).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"\", \"\", \"\", \"\", \"a\", \"\", \"b\", \"c\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"/\")?;"] # [doc = " let hay = \"(///)\";"] # [doc = " let got: Vec<&str> = re.split(hay).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"(\", \"\", \"\", \")\"]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Separators at the start or end of a haystack are neighbored by empty"] # [doc = " spans."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"0\")?;"] # [doc = " let hay = \"010\";"] # [doc = " let got: Vec<&str> = re.split(hay).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"\", \"1\", \"\"]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " When the empty string is used as a regex, it splits at every valid"] # [doc = " UTF-8 boundary by default (which includes the beginning and end of the"] # [doc = " haystack):"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\")?;"] # [doc = " let hay = \"rust\";"] # [doc = " let got: Vec<&str> = re.split(hay).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"\", \"r\", \"u\", \"s\", \"t\", \"\"]);"] # [doc = ""] # [doc = " // Splitting by an empty string is UTF-8 aware by default!"] # [doc = " let re = Regex::new(r\"\")?;"] # [doc = " let hay = \"☃\";"] # [doc = " let got: Vec<&str> = re.split(hay).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"\", \"☃\", \"\"]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " But note that UTF-8 mode for empty strings can be disabled, which will"] # [doc = " then result in a match at every byte offset in the haystack,"] # [doc = " including between every UTF-8 code unit."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .configure(Regex::config().utf8_empty(false))"] # [doc = "     .build(r\"\")?;"] # [doc = " let hay = \"☃\".as_bytes();"] # [doc = " let got: Vec<&[u8]> = re.split(hay).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec!["] # [doc = "     // Writing byte string slices is just brutal. The problem is that"] # [doc = "     // b\"foo\" has type &[u8; 3] instead of &[u8]."] # [doc = "     &[][..], &[b'\\xE2'][..], &[b'\\x98'][..], &[b'\\x83'][..], &[][..],"] # [doc = " ]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Contiguous separators (commonly shows up with whitespace), can lead to"] # [doc = " possibly surprising behavior. For example, this code is correct:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\" \")?;"] # [doc = " let hay = \"    a  b c\";"] # [doc = " let got: Vec<&str> = re.split(hay).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"\", \"\", \"\", \"\", \"a\", \"\", \"b\", \"c\"]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " It does *not* give you `[\"a\", \"b\", \"c\"]`. For that behavior, you'd want"] # [doc = " to match contiguous space characters:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\" +\")?;"] # [doc = " let hay = \"    a  b c\";"] # [doc = " let got: Vec<&str> = re.split(hay).map(|sp| &hay[sp]).collect();"] # [doc = " // N.B. This does still include a leading empty span because ' +'"] # [doc = " // matches at the beginning of the haystack."] # [doc = " assert_eq!(got, vec![\"\", \"a\", \"b\", \"c\"]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn split < 'r , 'h , I : Into < Input < 'h > > > (& 'r self , input : I ,) -> Split < 'r , 'h > { Split { finder : self . find_iter (input) , last : 0 } } # [doc = " Returns an iterator of at most `limit` spans of the haystack given,"] # [doc = " delimited by a match of the regex. (A `limit` of `0` will return no"] # [doc = " spans.) Namely, each element of the iterator corresponds to a part"] # [doc = " of the haystack that *isn't* matched by the regular expression. The"] # [doc = " remainder of the haystack that is not split will be the last element in"] # [doc = " the iterator."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Get the first two words in some haystack:"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\\W+\").unwrap();"] # [doc = " let hay = \"Hey! How are you?\";"] # [doc = " let fields: Vec<&str> ="] # [doc = "     re.splitn(hay, 3).map(|span| &hay[span]).collect();"] # [doc = " assert_eq!(fields, vec![\"Hey\", \"How\", \"are you?\"]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Examples: more cases"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\" \")?;"] # [doc = " let hay = \"Mary had a little lamb\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 3).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"Mary\", \"had\", \"a little lamb\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\")?;"] # [doc = " let hay = \"\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 3).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\")?;"] # [doc = " let hay = \"lionXXtigerXleopard\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 3).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"lion\", \"\", \"tigerXleopard\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"::\")?;"] # [doc = " let hay = \"lion::tiger::leopard\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 2).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"lion\", \"tiger::leopard\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\")?;"] # [doc = " let hay = \"abcXdef\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 1).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"abcXdef\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\")?;"] # [doc = " let hay = \"abcdef\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 2).map(|sp| &hay[sp]).collect();"] # [doc = " assert_eq!(got, vec![\"abcdef\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\")?;"] # [doc = " let hay = \"abcXdef\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 0).map(|sp| &hay[sp]).collect();"] # [doc = " assert!(got.is_empty());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn splitn < 'r , 'h , I : Into < Input < 'h > > > (& 'r self , input : I , limit : usize ,) -> SplitN < 'r , 'h > { SplitN { splits : self . split (input) , limit } } }
};
}
