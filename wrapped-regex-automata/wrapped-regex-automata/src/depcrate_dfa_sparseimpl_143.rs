// Generated macro for impl_143 (impl)
macro_rules! Depcrate_dfa_sparseimpl_143 {
() => {
// Module: crate::dfa::sparse
// Provides: {"impl_143"}
// Dependencies: {}
# [doc = " Routines for converting a sparse DFA to other representations, such as raw"] # [doc = " bytes suitable for persistent storage."] impl < T : AsRef < [u8] > > DFA < T > { # [doc = " Serialize this DFA as raw bytes to a `Vec<u8>` in little endian"] # [doc = " format."] # [doc = ""] # [doc = " The written bytes are guaranteed to be deserialized correctly and"] # [doc = " without errors in a semver compatible release of this crate by a"] # [doc = " `DFA`'s deserialization APIs (assuming all other criteria for the"] # [doc = " deserialization APIs has been satisfied):"] # [doc = ""] # [doc = " * [`DFA::from_bytes`]"] # [doc = " * [`DFA::from_bytes_unchecked`]"] # [doc = ""] # [doc = " Note that unlike a [`dense::DFA`]'s serialization methods, this does"] # [doc = " not add any initial padding to the returned bytes. Padding isn't"] # [doc = " required for sparse DFAs since they have no alignment requirements."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to serialize and deserialize a DFA:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, sparse::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " // N.B. We use native endianness here to make the example work, but"] # [doc = " // using to_bytes_little_endian would work on a little endian target."] # [doc = " let buf = original_dfa.to_bytes_native_endian();"] # [doc = " // Even if buf has initial padding, DFA::from_bytes will automatically"] # [doc = " // ignore it."] # [doc = " let dfa: DFA<&[u8]> = DFA::from_bytes(&buf)?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "dfa-build")] pub fn to_bytes_little_endian (& self) -> Vec < u8 > { self . to_bytes :: < wire :: LE > () } # [doc = " Serialize this DFA as raw bytes to a `Vec<u8>` in big endian"] # [doc = " format."] # [doc = ""] # [doc = " The written bytes are guaranteed to be deserialized correctly and"] # [doc = " without errors in a semver compatible release of this crate by a"] # [doc = " `DFA`'s deserialization APIs (assuming all other criteria for the"] # [doc = " deserialization APIs has been satisfied):"] # [doc = ""] # [doc = " * [`DFA::from_bytes`]"] # [doc = " * [`DFA::from_bytes_unchecked`]"] # [doc = ""] # [doc = " Note that unlike a [`dense::DFA`]'s serialization methods, this does"] # [doc = " not add any initial padding to the returned bytes. Padding isn't"] # [doc = " required for sparse DFAs since they have no alignment requirements."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to serialize and deserialize a DFA:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, sparse::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " // N.B. We use native endianness here to make the example work, but"] # [doc = " // using to_bytes_big_endian would work on a big endian target."] # [doc = " let buf = original_dfa.to_bytes_native_endian();"] # [doc = " // Even if buf has initial padding, DFA::from_bytes will automatically"] # [doc = " // ignore it."] # [doc = " let dfa: DFA<&[u8]> = DFA::from_bytes(&buf)?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "dfa-build")] pub fn to_bytes_big_endian (& self) -> Vec < u8 > { self . to_bytes :: < wire :: BE > () } # [doc = " Serialize this DFA as raw bytes to a `Vec<u8>` in native endian"] # [doc = " format."] # [doc = ""] # [doc = " The written bytes are guaranteed to be deserialized correctly and"] # [doc = " without errors in a semver compatible release of this crate by a"] # [doc = " `DFA`'s deserialization APIs (assuming all other criteria for the"] # [doc = " deserialization APIs has been satisfied):"] # [doc = ""] # [doc = " * [`DFA::from_bytes`]"] # [doc = " * [`DFA::from_bytes_unchecked`]"] # [doc = ""] # [doc = " Note that unlike a [`dense::DFA`]'s serialization methods, this does"] # [doc = " not add any initial padding to the returned bytes. Padding isn't"] # [doc = " required for sparse DFAs since they have no alignment requirements."] # [doc = ""] # [doc = " Generally speaking, native endian format should only be used when"] # [doc = " you know that the target you're compiling the DFA for matches the"] # [doc = " endianness of the target on which you're compiling DFA. For example,"] # [doc = " if serialization and deserialization happen in the same process or on"] # [doc = " the same machine. Otherwise, when serializing a DFA for use in a"] # [doc = " portable environment, you'll almost certainly want to serialize _both_"] # [doc = " a little endian and a big endian version and then load the correct one"] # [doc = " based on the target's configuration."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to serialize and deserialize a DFA:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, sparse::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " let buf = original_dfa.to_bytes_native_endian();"] # [doc = " // Even if buf has initial padding, DFA::from_bytes will automatically"] # [doc = " // ignore it."] # [doc = " let dfa: DFA<&[u8]> = DFA::from_bytes(&buf)?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "dfa-build")] pub fn to_bytes_native_endian (& self) -> Vec < u8 > { self . to_bytes :: < wire :: NE > () } # [doc = " The implementation of the public `to_bytes` serialization methods,"] # [doc = " which is generic over endianness."] # [cfg (feature = "dfa-build")] fn to_bytes < E : Endian > (& self) -> Vec < u8 > { let mut buf = vec ! [0 ; self . write_to_len ()] ; self . write_to :: < E > (& mut buf) . unwrap () ; buf } # [doc = " Serialize this DFA as raw bytes to the given slice, in little endian"] # [doc = " format. Upon success, the total number of bytes written to `dst` is"] # [doc = " returned."] # [doc = ""] # [doc = " The written bytes are guaranteed to be deserialized correctly and"] # [doc = " without errors in a semver compatible release of this crate by a"] # [doc = " `DFA`'s deserialization APIs (assuming all other criteria for the"] # [doc = " deserialization APIs has been satisfied):"] # [doc = ""] # [doc = " * [`DFA::from_bytes`]"] # [doc = " * [`DFA::from_bytes_unchecked`]"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This returns an error if the given destination slice is not big enough"] # [doc = " to contain the full serialized DFA. If an error occurs, then nothing"] # [doc = " is written to `dst`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to serialize and deserialize a DFA without"] # [doc = " dynamic memory allocation."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, sparse::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " // Create a 4KB buffer on the stack to store our serialized DFA."] # [doc = " let mut buf = [0u8; 4 * (1<<10)];"] # [doc = " // N.B. We use native endianness here to make the example work, but"] # [doc = " // using write_to_little_endian would work on a little endian target."] # [doc = " let written = original_dfa.write_to_native_endian(&mut buf)?;"] # [doc = " let dfa: DFA<&[u8]> = DFA::from_bytes(&buf[..written])?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn write_to_little_endian (& self , dst : & mut [u8] ,) -> Result < usize , SerializeError > { self . write_to :: < wire :: LE > (dst) } # [doc = " Serialize this DFA as raw bytes to the given slice, in big endian"] # [doc = " format. Upon success, the total number of bytes written to `dst` is"] # [doc = " returned."] # [doc = ""] # [doc = " The written bytes are guaranteed to be deserialized correctly and"] # [doc = " without errors in a semver compatible release of this crate by a"] # [doc = " `DFA`'s deserialization APIs (assuming all other criteria for the"] # [doc = " deserialization APIs has been satisfied):"] # [doc = ""] # [doc = " * [`DFA::from_bytes`]"] # [doc = " * [`DFA::from_bytes_unchecked`]"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This returns an error if the given destination slice is not big enough"] # [doc = " to contain the full serialized DFA. If an error occurs, then nothing"] # [doc = " is written to `dst`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to serialize and deserialize a DFA without"] # [doc = " dynamic memory allocation."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, sparse::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " // Create a 4KB buffer on the stack to store our serialized DFA."] # [doc = " let mut buf = [0u8; 4 * (1<<10)];"] # [doc = " // N.B. We use native endianness here to make the example work, but"] # [doc = " // using write_to_big_endian would work on a big endian target."] # [doc = " let written = original_dfa.write_to_native_endian(&mut buf)?;"] # [doc = " let dfa: DFA<&[u8]> = DFA::from_bytes(&buf[..written])?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn write_to_big_endian (& self , dst : & mut [u8] ,) -> Result < usize , SerializeError > { self . write_to :: < wire :: BE > (dst) } # [doc = " Serialize this DFA as raw bytes to the given slice, in native endian"] # [doc = " format. Upon success, the total number of bytes written to `dst` is"] # [doc = " returned."] # [doc = ""] # [doc = " The written bytes are guaranteed to be deserialized correctly and"] # [doc = " without errors in a semver compatible release of this crate by a"] # [doc = " `DFA`'s deserialization APIs (assuming all other criteria for the"] # [doc = " deserialization APIs has been satisfied):"] # [doc = ""] # [doc = " * [`DFA::from_bytes`]"] # [doc = " * [`DFA::from_bytes_unchecked`]"] # [doc = ""] # [doc = " Generally speaking, native endian format should only be used when"] # [doc = " you know that the target you're compiling the DFA for matches the"] # [doc = " endianness of the target on which you're compiling DFA. For example,"] # [doc = " if serialization and deserialization happen in the same process or on"] # [doc = " the same machine. Otherwise, when serializing a DFA for use in a"] # [doc = " portable environment, you'll almost certainly want to serialize _both_"] # [doc = " a little endian and a big endian version and then load the correct one"] # [doc = " based on the target's configuration."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This returns an error if the given destination slice is not big enough"] # [doc = " to contain the full serialized DFA. If an error occurs, then nothing"] # [doc = " is written to `dst`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to serialize and deserialize a DFA without"] # [doc = " dynamic memory allocation."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, sparse::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " // Create a 4KB buffer on the stack to store our serialized DFA."] # [doc = " let mut buf = [0u8; 4 * (1<<10)];"] # [doc = " let written = original_dfa.write_to_native_endian(&mut buf)?;"] # [doc = " let dfa: DFA<&[u8]> = DFA::from_bytes(&buf[..written])?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn write_to_native_endian (& self , dst : & mut [u8] ,) -> Result < usize , SerializeError > { self . write_to :: < wire :: NE > (dst) } # [doc = " The implementation of the public `write_to` serialization methods,"] # [doc = " which is generic over endianness."] fn write_to < E : Endian > (& self , dst : & mut [u8] ,) -> Result < usize , SerializeError > { let mut nw = 0 ; nw += wire :: write_label (LABEL , & mut dst [nw ..]) ? ; nw += wire :: write_endianness_check :: < E > (& mut dst [nw ..]) ? ; nw += wire :: write_version :: < E > (VERSION , & mut dst [nw ..]) ? ; nw += { E :: write_u32 (0 , & mut dst [nw ..]) ; size_of :: < u32 > () } ; nw += self . flags . write_to :: < E > (& mut dst [nw ..]) ? ; nw += self . tt . write_to :: < E > (& mut dst [nw ..]) ? ; nw += self . st . write_to :: < E > (& mut dst [nw ..]) ? ; nw += self . special . write_to :: < E > (& mut dst [nw ..]) ? ; nw += self . quitset . write_to :: < E > (& mut dst [nw ..]) ? ; Ok (nw) } # [doc = " Return the total number of bytes required to serialize this DFA."] # [doc = ""] # [doc = " This is useful for determining the size of the buffer required to pass"] # [doc = " to one of the serialization routines:"] # [doc = ""] # [doc = " * [`DFA::write_to_little_endian`]"] # [doc = " * [`DFA::write_to_big_endian`]"] # [doc = " * [`DFA::write_to_native_endian`]"] # [doc = ""] # [doc = " Passing a buffer smaller than the size returned by this method will"] # [doc = " result in a serialization error."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to dynamically allocate enough room to serialize"] # [doc = " a sparse DFA."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, sparse::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " let mut buf = vec![0; original_dfa.write_to_len()];"] # [doc = " let written = original_dfa.write_to_native_endian(&mut buf)?;"] # [doc = " let dfa: DFA<&[u8]> = DFA::from_bytes(&buf[..written])?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn write_to_len (& self) -> usize { wire :: write_label_len (LABEL) + wire :: write_endianness_check_len () + wire :: write_version_len () + size_of :: < u32 > () + self . flags . write_to_len () + self . tt . write_to_len () + self . st . write_to_len () + self . special . write_to_len () + self . quitset . write_to_len () } }
};
}
