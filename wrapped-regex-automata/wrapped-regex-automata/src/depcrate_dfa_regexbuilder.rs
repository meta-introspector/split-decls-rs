// Generated macro for Builder (struct)
macro_rules! Depcrate_dfa_regexBuilder {
() => {
// Module: crate::dfa::regex
// Provides: {"Builder"}
// Dependencies: {}
# [doc = " A builder for a regex based on deterministic finite automatons."] # [doc = ""] # [doc = " This builder permits configuring options for the syntax of a pattern, the"] # [doc = " NFA construction, the DFA construction and finally the regex searching"] # [doc = " itself. This builder is different from a general purpose regex builder in"] # [doc = " that it permits fine grain configuration of the construction process. The"] # [doc = " trade off for this is complexity, and the possibility of setting a"] # [doc = " configuration that might not make sense. For example, there are two"] # [doc = " different UTF-8 modes:"] # [doc = ""] # [doc = " * [`syntax::Config::utf8`](crate::util::syntax::Config::utf8) controls"] # [doc = " whether the pattern itself can contain sub-expressions that match invalid"] # [doc = " UTF-8."] # [doc = " * [`thompson::Config::utf8`](crate::nfa::thompson::Config::utf8) controls"] # [doc = " how the regex iterators themselves advance the starting position of the"] # [doc = " next search when a match with zero length is found."] # [doc = ""] # [doc = " Generally speaking, callers will want to either enable all of these or"] # [doc = " disable all of these."] # [doc = ""] # [doc = " Internally, building a regex requires building two DFAs, where one is"] # [doc = " responsible for finding the end of a match and the other is responsible"] # [doc = " for finding the start of a match. If you only need to detect whether"] # [doc = " something matched, or only the end of a match, then you should use a"] # [doc = " [`dense::Builder`] to construct a single DFA, which is cheaper than"] # [doc = " building two DFAs."] # [doc = ""] # [doc = " # Build methods"] # [doc = ""] # [doc = " This builder has a few \"build\" methods. In general, it's the result of"] # [doc = " combining the following parameters:"] # [doc = ""] # [doc = " * Building one or many regexes."] # [doc = " * Building a regex with dense or sparse DFAs."] # [doc = ""] # [doc = " The simplest \"build\" method is [`Builder::build`]. It accepts a single"] # [doc = " pattern and builds a dense DFA using `usize` for the state identifier"] # [doc = " representation."] # [doc = ""] # [doc = " The most general \"build\" method is [`Builder::build_many`], which permits"] # [doc = " building a regex that searches for multiple patterns simultaneously while"] # [doc = " using a specific state identifier representation."] # [doc = ""] # [doc = " The most flexible \"build\" method, but hardest to use, is"] # [doc = " [`Builder::build_from_dfas`]. This exposes the fact that a [`Regex`] is"] # [doc = " just a pair of DFAs, and this method allows you to specify those DFAs"] # [doc = " exactly."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to disable UTF-8 mode in the syntax and the regex"] # [doc = " itself. This is generally what you want for matching on arbitrary bytes."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     dfa::regex::Regex, nfa::thompson, util::syntax, Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .syntax(syntax::Config::new().utf8(false))"] # [doc = "     .thompson(thompson::Config::new().utf8(false))"] # [doc = "     .build(r\"foo(?-u:[^b])ar.*\")?;"] # [doc = " let haystack = b\"\\xFEfoo\\xFFarzz\\xE2\\x98\\xFF\\n\";"] # [doc = " let expected = Some(Match::must(0, 1..9));"] # [doc = " let got = re.find(haystack);"] # [doc = " assert_eq!(expected, got);"] # [doc = " // Notice that `(?-u:[^b])` matches invalid UTF-8,"] # [doc = " // but the subsequent `.*` does not! Disabling UTF-8"] # [doc = " // on the syntax permits this."] # [doc = " assert_eq!(b\"foo\\xFFarzz\", &haystack[got.unwrap().range()]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Debug)] pub struct Builder { # [cfg (feature = "dfa-build")] dfa : dense :: Builder , }
};
}
