// Generated macro for Captures (struct)
macro_rules! Depcrate_util_capturesCaptures {
() => {
// Module: crate::util::captures
// Provides: {"Captures"}
// Dependencies: {}
# [doc = " The span offsets of capturing groups after a match has been found."] # [doc = ""] # [doc = " This type represents the output of regex engines that can report the"] # [doc = " offsets at which capturing groups matches or \"submatches\" occur. For"] # [doc = " example, the [`PikeVM`](crate::nfa::thompson::pikevm::PikeVM). When a match"] # [doc = " occurs, it will at minimum contain the [`PatternID`] of the pattern that"] # [doc = " matched. Depending upon how it was constructed, it may also contain the"] # [doc = " start/end offsets of the entire match of the pattern and the start/end"] # [doc = " offsets of each capturing group that participated in the match."] # [doc = ""] # [doc = " Values of this type are always created for a specific [`GroupInfo`]. It is"] # [doc = " unspecified behavior to use a `Captures` value in a search with any regex"] # [doc = " engine that has a different `GroupInfo` than the one the `Captures` were"] # [doc = " created with."] # [doc = ""] # [doc = " # Constructors"] # [doc = ""] # [doc = " There are three constructors for this type that control what kind of"] # [doc = " information is available upon a match:"] # [doc = ""] # [doc = " * [`Captures::all`]: Will store overall pattern match offsets in addition"] # [doc = " to the offsets of capturing groups that participated in the match."] # [doc = " * [`Captures::matches`]: Will store only the overall pattern"] # [doc = " match offsets. The offsets of capturing groups (even ones that participated"] # [doc = " in the match) are not available."] # [doc = " * [`Captures::empty`]: Will only store the pattern ID that matched. No"] # [doc = " match offsets are available at all."] # [doc = ""] # [doc = " If you aren't sure which to choose, then pick the first one. The first one"] # [doc = " is what convenience routines like,"] # [doc = " [`PikeVM::create_captures`](crate::nfa::thompson::pikevm::PikeVM::create_captures),"] # [doc = " will use automatically."] # [doc = ""] # [doc = " The main difference between these choices is performance. Namely, if you"] # [doc = " ask for _less_ information, then the execution of regex search may be able"] # [doc = " to run more quickly."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " It is worth pointing out that this type is not coupled to any one specific"] # [doc = " regex engine. Instead, its coupling is with [`GroupInfo`], which is the"] # [doc = " thing that is responsible for mapping capturing groups to \"slot\" offsets."] # [doc = " Slot offsets are indices into a single sequence of memory at which matching"] # [doc = " haystack offsets for the corresponding group are written by regex engines."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to parse a simple date and extract the components of"] # [doc = " the date via capturing groups:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, Span};"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"^([0-9]{4})-([0-9]{2})-([0-9]{2})$\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"2010-03-14\", &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " assert_eq!(Some(Span::from(0..4)), caps.get_group(1));"] # [doc = " assert_eq!(Some(Span::from(5..7)), caps.get_group(2));"] # [doc = " assert_eq!(Some(Span::from(8..10)), caps.get_group(3));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: named capturing groups"] # [doc = ""] # [doc = " This example is like the one above, but leverages the ability to name"] # [doc = " capturing groups in order to make the code a bit clearer:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, Span};"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"^(?P<y>[0-9]{4})-(?P<m>[0-9]{2})-(?P<d>[0-9]{2})$\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"2010-03-14\", &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " assert_eq!(Some(Span::from(0..4)), caps.get_group_by_name(\"y\"));"] # [doc = " assert_eq!(Some(Span::from(5..7)), caps.get_group_by_name(\"m\"));"] # [doc = " assert_eq!(Some(Span::from(8..10)), caps.get_group_by_name(\"d\"));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone)] pub struct Captures { # [doc = " The group info that these capture groups are coupled to. This is what"] # [doc = " gives the \"convenience\" of the `Captures` API. Namely, it provides the"] # [doc = " slot mapping and the name|-->index mapping for capture lookups by name."] group_info : GroupInfo , # [doc = " The ID of the pattern that matched. Regex engines must set this to"] # [doc = " None when no match occurs."] pid : Option < PatternID > , # [doc = " The slot values, i.e., submatch offsets."] # [doc = ""] # [doc = " In theory, the smallest sequence of slots would be something like"] # [doc = " `max(groups(pattern) for pattern in regex) * 2`, but instead, we use"] # [doc = " `sum(groups(pattern) for pattern in regex) * 2`. Why?"] # [doc = ""] # [doc = " Well, the former could be used in theory, because we don't generally"] # [doc = " have any overlapping APIs that involve capturing groups. Therefore,"] # [doc = " there's technically never any need to have slots set for multiple"] # [doc = " patterns. However, this might change some day, in which case, we would"] # [doc = " need to have slots available."] # [doc = ""] # [doc = " The other reason is that during the execution of some regex engines,"] # [doc = " there exists a point in time where multiple slots for different"] # [doc = " patterns may be written to before knowing which pattern has matched."] # [doc = " Therefore, the regex engines themselves, in order to support multiple"] # [doc = " patterns correctly, must have all slots available. If `Captures`"] # [doc = " doesn't have all slots available, then regex engines can't write"] # [doc = " directly into the caller provided `Captures` and must instead write"] # [doc = " into some other storage and then copy the slots involved in the match"] # [doc = " at the end of the search."] # [doc = ""] # [doc = " So overall, at least as of the time of writing, it seems like the path"] # [doc = " of least resistance is to just require allocating all possible slots"] # [doc = " instead of the conceptual minimum. Another way to justify this is that"] # [doc = " the most common case is a single pattern, in which case, there is no"] # [doc = " inefficiency here since the 'max' and 'sum' calculations above are"] # [doc = " equivalent in that case."] # [doc = ""] # [doc = " N.B. The mapping from group index to slot is maintained by `GroupInfo`"] # [doc = " and is considered an API guarantee. See `GroupInfo` for more details on"] # [doc = " that mapping."] # [doc = ""] # [doc = " N.B. `Option<NonMaxUsize>` has the same size as a `usize`."] slots : Vec < Option < NonMaxUsize > > , }
};
}
