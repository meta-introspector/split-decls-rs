// Generated macro for impl_267 (impl)
macro_rules! Depcrate_hybrid_dfaimpl_267 {
() => {
// Module: crate::hybrid::dfa
// Provides: {"impl_267"}
// Dependencies: {}
impl DFA { # [doc = " Parse the given regular expression using a default configuration and"] # [doc = " return the corresponding lazy DFA."] # [doc = ""] # [doc = " If you want a non-default configuration, then use the [`Builder`] to"] # [doc = " set your own configuration."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::dfa::DFA, HalfMatch, Input};"] # [doc = ""] # [doc = " let dfa = DFA::new(\"foo[0-9]+bar\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = ""] # [doc = " let expected = HalfMatch::must(0, 11);"] # [doc = " assert_eq!("] # [doc = "     Some(expected),"] # [doc = "     dfa.try_search_fwd(&mut cache, &Input::new(\"foo12345bar\"))?,"] # [doc = " );"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] pub fn new (pattern : & str) -> Result < DFA , BuildError > { DFA :: builder () . build (pattern) } # [doc = " Parse the given regular expressions using a default configuration and"] # [doc = " return the corresponding lazy multi-DFA."] # [doc = ""] # [doc = " If you want a non-default configuration, then use the [`Builder`] to"] # [doc = " set your own configuration."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::dfa::DFA, HalfMatch, Input};"] # [doc = ""] # [doc = " let dfa = DFA::new_many(&[\"[0-9]+\", \"[a-z]+\"])?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = ""] # [doc = " let expected = HalfMatch::must(1, 3);"] # [doc = " assert_eq!("] # [doc = "     Some(expected),"] # [doc = "     dfa.try_search_fwd(&mut cache, &Input::new(\"foo12345bar\"))?,"] # [doc = " );"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] pub fn new_many < P : AsRef < str > > (patterns : & [P]) -> Result < DFA , BuildError > { DFA :: builder () . build_many (patterns) } # [doc = " Create a new lazy DFA that matches every input."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::dfa::DFA, HalfMatch, Input};"] # [doc = ""] # [doc = " let dfa = DFA::always_match()?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = ""] # [doc = " let expected = HalfMatch::must(0, 0);"] # [doc = " assert_eq!(Some(expected), dfa.try_search_fwd("] # [doc = "     &mut cache, &Input::new(\"\"))?,"] # [doc = " );"] # [doc = " assert_eq!(Some(expected), dfa.try_search_fwd("] # [doc = "     &mut cache, &Input::new(\"foo\"))?,"] # [doc = " );"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn always_match () -> Result < DFA , BuildError > { let nfa = thompson :: NFA :: always_match () ; Builder :: new () . build_from_nfa (nfa) } # [doc = " Create a new lazy DFA that never matches any input."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::dfa::DFA, Input};"] # [doc = ""] # [doc = " let dfa = DFA::never_match()?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = ""] # [doc = " assert_eq!(None, dfa.try_search_fwd(&mut cache, &Input::new(\"\"))?);"] # [doc = " assert_eq!(None, dfa.try_search_fwd(&mut cache, &Input::new(\"foo\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn never_match () -> Result < DFA , BuildError > { let nfa = thompson :: NFA :: never_match () ; Builder :: new () . build_from_nfa (nfa) } # [doc = " Return a default configuration for a `DFA`."] # [doc = ""] # [doc = " This is a convenience routine to avoid needing to import the [`Config`]"] # [doc = " type when customizing the construction of a lazy DFA."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to build a lazy DFA that heuristically supports"] # [doc = " Unicode word boundaries."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{hybrid::dfa::DFA, HalfMatch, MatchError, Input};"] # [doc = ""] # [doc = " let re = DFA::builder()"] # [doc = "     .configure(DFA::config().unicode_word_boundary(true))"] # [doc = "     .build(r\"\\b\\w+\\b\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " // Since our haystack is all ASCII, the DFA search sees then and knows"] # [doc = " // it is legal to interpret Unicode word boundaries as ASCII word"] # [doc = " // boundaries."] # [doc = " let input = Input::new(\"!!foo!!\");"] # [doc = " let expected = HalfMatch::must(0, 5);"] # [doc = " assert_eq!(Some(expected), re.try_search_fwd(&mut cache, &input)?);"] # [doc = ""] # [doc = " // But if our haystack contains non-ASCII, then the search will fail"] # [doc = " // with an error."] # [doc = " let input = Input::new(\"!!βββ!!\");"] # [doc = " let expected = MatchError::quit(b'\\xCE', 2);"] # [doc = " assert_eq!(Err(expected), re.try_search_fwd(&mut cache, &input));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn config () -> Config { Config :: new () } # [doc = " Return a builder for configuring the construction of a `Regex`."] # [doc = ""] # [doc = " This is a convenience routine to avoid needing to import the"] # [doc = " [`Builder`] type in common cases."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use the builder to disable UTF-8 mode"] # [doc = " everywhere for lazy DFAs."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{hybrid::dfa::DFA, util::syntax, HalfMatch, Input};"] # [doc = ""] # [doc = " let re = DFA::builder()"] # [doc = "     .syntax(syntax::Config::new().utf8(false))"] # [doc = "     .build(r\"foo(?-u:[^b])ar.*\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let input = Input::new(b\"\\xFEfoo\\xFFarzz\\xE2\\x98\\xFF\\n\");"] # [doc = " let expected = Some(HalfMatch::must(0, 9));"] # [doc = " let got = re.try_search_fwd(&mut cache, &input)?;"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn builder () -> Builder { Builder :: new () } # [doc = " Create a new cache for this lazy DFA."] # [doc = ""] # [doc = " The cache returned should only be used for searches for this"] # [doc = " lazy DFA. If you want to reuse the cache for another DFA, then"] # [doc = " you must call [`Cache::reset`] with that DFA (or, equivalently,"] # [doc = " [`DFA::reset_cache`])."] pub fn create_cache (& self) -> Cache { Cache :: new (self) } # [doc = " Reset the given cache such that it can be used for searching with the"] # [doc = " this lazy DFA (and only this DFA)."] # [doc = ""] # [doc = " A cache reset permits reusing memory already allocated in this cache"] # [doc = " with a different lazy DFA."] # [doc = ""] # [doc = " Resetting a cache sets its \"clear count\" to 0. This is relevant if the"] # [doc = " lazy DFA has been configured to \"give up\" after it has cleared the"] # [doc = " cache a certain number of times."] # [doc = ""] # [doc = " Any lazy state ID generated by the cache prior to resetting it is"] # [doc = " invalid after the reset."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to re-purpose a cache for use with a different DFA."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{hybrid::dfa::DFA, HalfMatch, Input};"] # [doc = ""] # [doc = " let dfa1 = DFA::new(r\"\\w\")?;"] # [doc = " let dfa2 = DFA::new(r\"\\W\")?;"] # [doc = ""] # [doc = " let mut cache = dfa1.create_cache();"] # [doc = " assert_eq!("] # [doc = "     Some(HalfMatch::must(0, 2)),"] # [doc = "     dfa1.try_search_fwd(&mut cache, &Input::new(\"Δ\"))?,"] # [doc = " );"] # [doc = ""] # [doc = " // Using 'cache' with dfa2 is not allowed. It may result in panics or"] # [doc = " // incorrect results. In order to re-purpose the cache, we must reset"] # [doc = " // it with the DFA we'd like to use it with."] # [doc = " //"] # [doc = " // Similarly, after this reset, using the cache with 'dfa1' is also not"] # [doc = " // allowed."] # [doc = " dfa2.reset_cache(&mut cache);"] # [doc = " assert_eq!("] # [doc = "     Some(HalfMatch::must(0, 3)),"] # [doc = "     dfa2.try_search_fwd(&mut cache, &Input::new(\"☃\"))?,"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn reset_cache (& self , cache : & mut Cache) { Lazy :: new (self , cache) . reset_cache () } # [doc = " Returns the total number of patterns compiled into this lazy DFA."] # [doc = ""] # [doc = " In the case of a DFA that contains no patterns, this returns `0`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the pattern length for a DFA that never matches:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::hybrid::dfa::DFA;"] # [doc = ""] # [doc = " let dfa = DFA::never_match()?;"] # [doc = " assert_eq!(dfa.pattern_len(), 0);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " And another example for a DFA that matches at every position:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::hybrid::dfa::DFA;"] # [doc = ""] # [doc = " let dfa = DFA::always_match()?;"] # [doc = " assert_eq!(dfa.pattern_len(), 1);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " And finally, a DFA that was constructed from multiple patterns:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::hybrid::dfa::DFA;"] # [doc = ""] # [doc = " let dfa = DFA::new_many(&[\"[0-9]+\", \"[a-z]+\", \"[A-Z]+\"])?;"] # [doc = " assert_eq!(dfa.pattern_len(), 3);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn pattern_len (& self) -> usize { self . nfa . pattern_len () } # [doc = " Returns the equivalence classes that make up the alphabet for this DFA."] # [doc = ""] # [doc = " Unless [`Config::byte_classes`] was disabled, it is possible that"] # [doc = " multiple distinct bytes are grouped into the same equivalence class"] # [doc = " if it is impossible for them to discriminate between a match and a"] # [doc = " non-match. This has the effect of reducing the overall alphabet size"] # [doc = " and in turn potentially substantially reducing the size of the DFA's"] # [doc = " transition table."] # [doc = ""] # [doc = " The downside of using equivalence classes like this is that every state"] # [doc = " transition will automatically use this map to convert an arbitrary"] # [doc = " byte to its corresponding equivalence class. In practice this has a"] # [doc = " negligible impact on performance."] pub fn byte_classes (& self) -> & ByteClasses { & self . classes } # [doc = " Returns this lazy DFA's configuration."] pub fn get_config (& self) -> & Config { & self . config } # [doc = " Returns a reference to the underlying NFA."] pub fn get_nfa (& self) -> & thompson :: NFA { & self . nfa } # [doc = " Returns the stride, as a base-2 exponent, required for these"] # [doc = " equivalence classes."] # [doc = ""] # [doc = " The stride is always the smallest power of 2 that is greater than or"] # [doc = " equal to the alphabet length. This is done so that converting between"] # [doc = " state IDs and indices can be done with shifts alone, which is much"] # [doc = " faster than integer division."] fn stride2 (& self) -> usize { self . stride2 } # [doc = " Returns the total stride for every state in this lazy DFA. This"] # [doc = " corresponds to the total number of transitions used by each state in"] # [doc = " this DFA's transition table."] fn stride (& self) -> usize { 1 << self . stride2 () } # [doc = " Returns the memory usage, in bytes, of this lazy DFA."] # [doc = ""] # [doc = " This does **not** include the stack size used up by this lazy DFA. To"] # [doc = " compute that, use `std::mem::size_of::<DFA>()`. This also does not"] # [doc = " include the size of the `Cache` used."] # [doc = ""] # [doc = " This also does not include any heap memory used by the NFA inside of"] # [doc = " this hybrid NFA/DFA. This is because the NFA's ownership is shared, and"] # [doc = " thus not owned by this hybrid NFA/DFA. More practically, several regex"] # [doc = " engines in this crate embed an NFA, and reporting the NFA's memory"] # [doc = " usage in all of them would likely result in reporting higher heap"] # [doc = " memory than is actually used."] pub fn memory_usage (& self) -> usize { 0 } }
};
}
