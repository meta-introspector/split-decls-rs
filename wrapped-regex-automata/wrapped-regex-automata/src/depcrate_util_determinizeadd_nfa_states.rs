// Generated macro for add_nfa_states (function)
macro_rules! Depcrate_util_determinizeadd_nfa_states {
() => {
// Module: crate::util::determinize
// Provides: {"add_nfa_states"}
// Dependencies: {}
# [doc = " Add the NFA state IDs in the given `set` to the given DFA builder state."] # [doc = " The order in which states are added corresponds to the order in which they"] # [doc = " were added to `set`."] # [doc = ""] # [doc = " The DFA builder state given should already have its complete set of match"] # [doc = " pattern IDs added (if any) and any look-behind assertions (StartLF, Start"] # [doc = " and whether this state is being generated for a transition over a word byte"] # [doc = " when applicable) that are true immediately prior to transitioning into this"] # [doc = " state (via `builder.look_have()`). The match pattern IDs should correspond"] # [doc = " to matches that occurred on the previous transition, since all matches are"] # [doc = " delayed by one byte. The things that should _not_ be set are look-ahead"] # [doc = " assertions (EndLF, End and whether the next byte is a word byte or not)."] # [doc = " The builder state should also not have anything in `look_need` set, as this"] # [doc = " routine will compute that for you."] # [doc = ""] # [doc = " The given NFA should be able to resolve all identifiers in `set` to a"] # [doc = " particular NFA state. Additionally, `set` must have capacity equivalent"] # [doc = " to `nfa.len()`."] pub (crate) fn add_nfa_states (nfa : & thompson :: NFA , set : & SparseSet , builder : & mut StateBuilderNFA ,) { for nfa_id in set . iter () { match * nfa . state (nfa_id) { thompson :: State :: ByteRange { .. } => { builder . add_nfa_state_id (nfa_id) ; } thompson :: State :: Sparse { .. } => { builder . add_nfa_state_id (nfa_id) ; } thompson :: State :: Dense { .. } => { builder . add_nfa_state_id (nfa_id) ; } thompson :: State :: Look { look , .. } => { builder . add_nfa_state_id (nfa_id) ; builder . set_look_need (| need | need . insert (look)) ; } thompson :: State :: Union { .. } | thompson :: State :: BinaryUnion { .. } => { builder . add_nfa_state_id (nfa_id) ; } thompson :: State :: Capture { .. } => { } thompson :: State :: Fail => { builder . add_nfa_state_id (nfa_id) ; } thompson :: State :: Match { .. } => { builder . add_nfa_state_id (nfa_id) ; } } } if builder . look_need () . is_empty () { builder . set_look_have (| _ | LookSet :: empty ()) ; } }
};
}
