// Generated macro for impl_68 (impl)
macro_rules! Depcrate_dfa_denseimpl_68 {
() => {
// Module: crate::dfa::dense
// Provides: {"impl_68"}
// Dependencies: {}
# [cfg (feature = "dfa-build")] impl BuildError { # [doc = " Returns true if and only if this error corresponds to an error with DFA"] # [doc = " construction that occurred because of exceeding a size limit."] # [doc = ""] # [doc = " While this can occur when size limits like [`Config::dfa_size_limit`]"] # [doc = " or [`Config::determinize_size_limit`] are exceeded, this can also occur"] # [doc = " when the number of states or patterns exceeds a hard-coded maximum."] # [doc = " (Where these maximums are derived based on the values representable by"] # [doc = " [`StateID`] and [`PatternID`].)"] # [doc = ""] # [doc = " This predicate is useful in contexts where you want to distinguish"] # [doc = " between errors related to something provided by an end user (for"] # [doc = " example, an invalid regex pattern) and errors related to configured"] # [doc = " heuristics. For example, building a DFA might be an optimization that"] # [doc = " you want to skip if construction fails because of an exceeded size"] # [doc = " limit, but where you want to bubble up an error if it fails for some"] # [doc = " other reason."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " # if !cfg!(target_pointer_width = \"64\") { return Ok(()); } // see #1039"] # [doc = " use regex_automata::{dfa::{dense, Automaton}, Input};"] # [doc = ""] # [doc = " let err = dense::Builder::new()"] # [doc = "     .configure(dense::Config::new()"] # [doc = "         .determinize_size_limit(Some(100_000))"] # [doc = "     )"] # [doc = "     .build(r\"\\w{20}\")"] # [doc = "     .unwrap_err();"] # [doc = " // This error occurs because a size limit was exceeded."] # [doc = " // But things are otherwise valid."] # [doc = " assert!(err.is_size_limit_exceeded());"] # [doc = ""] # [doc = " let err = dense::Builder::new()"] # [doc = "     .build(r\"\\bxyz\\b\")"] # [doc = "     .unwrap_err();"] # [doc = " // This error occurs because a Unicode word boundary"] # [doc = " // was used without enabling heuristic support for it."] # [doc = " // So... not related to size limits."] # [doc = " assert!(!err.is_size_limit_exceeded());"] # [doc = ""] # [doc = " let err = dense::Builder::new()"] # [doc = "     .build(r\"(xyz\")"] # [doc = "     .unwrap_err();"] # [doc = " // This error occurs because the pattern is invalid."] # [doc = " // So... not related to size limits."] # [doc = " assert!(!err.is_size_limit_exceeded());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn is_size_limit_exceeded (& self) -> bool { use self :: BuildErrorKind :: * ; match self . kind { NFA (_) | Unsupported (_) => false , TooManyStates | TooManyStartStates | TooManyMatchPatternIDs | DFAExceededSizeLimit { .. } | DeterminizeExceededSizeLimit { .. } => true , } } }
};
}
