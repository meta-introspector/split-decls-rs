// Generated macro for impl_516 (impl)
macro_rules! Depcrate_nfa_thompson_backtrackimpl_516 {
() => {
// Module: crate::nfa::thompson::backtrack
// Provides: {"impl_516"}
// Dependencies: {}
impl BoundedBacktracker { # [doc = " Returns true if and only if this regex matches the given haystack."] # [doc = ""] # [doc = " In the case of a backtracking regex engine, and unlike most other"] # [doc = " regex engines in this crate, short circuiting isn't practical. However,"] # [doc = " this routine may still be faster because it instructs backtracking to"] # [doc = " not keep track of any capturing groups."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine only errors if the search could not complete. For this"] # [doc = " backtracking regex engine, this only occurs when the haystack length"] # [doc = " exceeds [`BoundedBacktracker::max_haystack_len`]."] # [doc = ""] # [doc = " When a search cannot complete, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::backtrack::BoundedBacktracker;"] # [doc = ""] # [doc = " let re = BoundedBacktracker::new(\"foo[0-9]+bar\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " assert!(re.try_is_match(&mut cache, \"foo12345bar\")?);"] # [doc = " assert!(!re.try_is_match(&mut cache, \"foobar\")?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: consistency with search APIs"] # [doc = ""] # [doc = " `is_match` is guaranteed to return `true` whenever `find` returns a"] # [doc = " match. This includes searches that are executed entirely within a"] # [doc = " codepoint:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     Input,"] # [doc = " };"] # [doc = ""] # [doc = " let re = BoundedBacktracker::new(\"a*\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " assert!(!re.try_is_match(&mut cache, Input::new(\"☃\").span(1..2))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Notice that when UTF-8 mode is disabled, then the above reports a"] # [doc = " match because the restriction against zero-width matches that split a"] # [doc = " codepoint has been lifted:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::{backtrack::BoundedBacktracker, NFA},"] # [doc = "     Input,"] # [doc = " };"] # [doc = ""] # [doc = " let re = BoundedBacktracker::builder()"] # [doc = "     .thompson(NFA::config().utf8(false))"] # [doc = "     .build(\"a*\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " assert!(re.try_is_match(&mut cache, Input::new(\"☃\").span(1..2))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn try_is_match < 'h , I : Into < Input < 'h > > > (& self , cache : & mut Cache , input : I ,) -> Result < bool , MatchError > { let input = input . into () . earliest (true) ; self . try_search_slots (cache , & input , & mut []) . map (| pid | pid . is_some ()) } # [doc = " Executes a leftmost forward search and returns a `Match` if one exists."] # [doc = ""] # [doc = " This routine only includes the overall match span. To get"] # [doc = " access to the individual spans of each capturing group, use"] # [doc = " [`BoundedBacktracker::try_captures`]."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine only errors if the search could not complete. For this"] # [doc = " backtracking regex engine, this only occurs when the haystack length"] # [doc = " exceeds [`BoundedBacktracker::max_haystack_len`]."] # [doc = ""] # [doc = " When a search cannot complete, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = BoundedBacktracker::new(\"foo[0-9]+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let expected = Match::must(0, 0..8);"] # [doc = " assert_eq!(Some(expected), re.try_find(&mut cache, \"foo12345\")?);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn try_find < 'h , I : Into < Input < 'h > > > (& self , cache : & mut Cache , input : I ,) -> Result < Option < Match > , MatchError > { let input = input . into () ; if self . get_nfa () . pattern_len () == 1 { let mut slots = [None , None] ; let pid = match self . try_search_slots (cache , & input , & mut slots) ? { None => return Ok (None) , Some (pid) => pid , } ; let start = match slots [0] { None => return Ok (None) , Some (s) => s . get () , } ; let end = match slots [1] { None => return Ok (None) , Some (s) => s . get () , } ; return Ok (Some (Match :: new (pid , Span { start , end }))) ; } let ginfo = self . get_nfa () . group_info () ; let slots_len = ginfo . implicit_slot_len () ; let mut slots = vec ! [None ; slots_len] ; let pid = match self . try_search_slots (cache , & input , & mut slots) ? { None => return Ok (None) , Some (pid) => pid , } ; let start = match slots [pid . as_usize () * 2] { None => return Ok (None) , Some (s) => s . get () , } ; let end = match slots [pid . as_usize () * 2 + 1] { None => return Ok (None) , Some (s) => s . get () , } ; Ok (Some (Match :: new (pid , Span { start , end }))) } # [doc = " Executes a leftmost forward search and writes the spans of capturing"] # [doc = " groups that participated in a match into the provided [`Captures`]"] # [doc = " value. If no match was found, then [`Captures::is_match`] is guaranteed"] # [doc = " to return `false`."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine only errors if the search could not complete. For this"] # [doc = " backtracking regex engine, this only occurs when the haystack length"] # [doc = " exceeds [`BoundedBacktracker::max_haystack_len`]."] # [doc = ""] # [doc = " When a search cannot complete, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     Span,"] # [doc = " };"] # [doc = ""] # [doc = " let re = BoundedBacktracker::new("] # [doc = "     r\"^([0-9]{4})-([0-9]{2})-([0-9]{2})$\","] # [doc = " )?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.try_captures(&mut cache, \"2010-03-14\", &mut caps)?;"] # [doc = " assert!(caps.is_match());"] # [doc = " assert_eq!(Some(Span::from(0..4)), caps.get_group(1));"] # [doc = " assert_eq!(Some(Span::from(5..7)), caps.get_group(2));"] # [doc = " assert_eq!(Some(Span::from(8..10)), caps.get_group(3));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn try_captures < 'h , I : Into < Input < 'h > > > (& self , cache : & mut Cache , input : I , caps : & mut Captures ,) -> Result < () , MatchError > { self . try_search (cache , & input . into () , caps) } # [doc = " Returns an iterator over all non-overlapping leftmost matches in the"] # [doc = " given bytes. If no match exists, then the iterator yields no elements."] # [doc = ""] # [doc = " If the regex engine returns an error at any point, then the iterator"] # [doc = " will yield that error."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     Match, MatchError,"] # [doc = " };"] # [doc = ""] # [doc = " let re = BoundedBacktracker::new(\"foo[0-9]+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let text = \"foo1 foo12 foo123\";"] # [doc = " let result: Result<Vec<Match>, MatchError> = re"] # [doc = "     .try_find_iter(&mut cache, text)"] # [doc = "     .collect();"] # [doc = " let matches = result?;"] # [doc = " assert_eq!(matches, vec!["] # [doc = "     Match::must(0, 0..4),"] # [doc = "     Match::must(0, 5..10),"] # [doc = "     Match::must(0, 11..17),"] # [doc = " ]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn try_find_iter < 'r , 'c , 'h , I : Into < Input < 'h > > > (& 'r self , cache : & 'c mut Cache , input : I ,) -> TryFindMatches < 'r , 'c , 'h > { let caps = Captures :: matches (self . get_nfa () . group_info () . clone ()) ; let it = iter :: Searcher :: new (input . into ()) ; TryFindMatches { re : self , cache , caps , it } } # [doc = " Returns an iterator over all non-overlapping `Captures` values. If no"] # [doc = " match exists, then the iterator yields no elements."] # [doc = ""] # [doc = " This yields the same matches as [`BoundedBacktracker::try_find_iter`],"] # [doc = " but it includes the spans of all capturing groups that participate in"] # [doc = " each match."] # [doc = ""] # [doc = " If the regex engine returns an error at any point, then the iterator"] # [doc = " will yield that error."] # [doc = ""] # [doc = " **Tip:** See [`util::iter::Searcher`](crate::util::iter::Searcher) for"] # [doc = " how to correctly iterate over all matches in a haystack while avoiding"] # [doc = " the creation of a new `Captures` value for every match. (Which you are"] # [doc = " forced to do with an `Iterator`.)"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     Span,"] # [doc = " };"] # [doc = ""] # [doc = " let re = BoundedBacktracker::new(\"foo(?P<numbers>[0-9]+)\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let text = \"foo1 foo12 foo123\";"] # [doc = " let mut spans = vec![];"] # [doc = " for result in re.try_captures_iter(&mut cache, text) {"] # [doc = "     let caps = result?;"] # [doc = "     // The unwrap is OK since 'numbers' matches if the pattern matches."] # [doc = "     spans.push(caps.get_group_by_name(\"numbers\").unwrap());"] # [doc = " }"] # [doc = " assert_eq!(spans, vec!["] # [doc = "     Span::from(3..4),"] # [doc = "     Span::from(8..10),"] # [doc = "     Span::from(14..17),"] # [doc = " ]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn try_captures_iter < 'r , 'c , 'h , I : Into < Input < 'h > > > (& 'r self , cache : & 'c mut Cache , input : I ,) -> TryCapturesMatches < 'r , 'c , 'h > { let caps = self . create_captures () ; let it = iter :: Searcher :: new (input . into ()) ; TryCapturesMatches { re : self , cache , caps , it } } }
};
}
