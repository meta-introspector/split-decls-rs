// Generated macro for StartTable (struct)
macro_rules! Depcrate_dfa_denseStartTable {
() => {
// Module: crate::dfa::dense
// Provides: {"StartTable"}
// Dependencies: {}
# [doc = " The set of all possible starting states in a DFA."] # [doc = ""] # [doc = " The set of starting states corresponds to the possible choices one can make"] # [doc = " in terms of starting a DFA. That is, before following the first transition,"] # [doc = " you first need to select the state that you start in."] # [doc = ""] # [doc = " Normally, a DFA converted from an NFA that has a single starting state"] # [doc = " would itself just have one starting state. However, our support for look"] # [doc = " around generally requires more starting states. The correct starting state"] # [doc = " is chosen based on certain properties of the position at which we begin"] # [doc = " our search."] # [doc = ""] # [doc = " Before listing those properties, we first must define two terms:"] # [doc = ""] # [doc = " * `haystack` - The bytes to execute the search. The search always starts"] # [doc = "   at the beginning of `haystack` and ends before or at the end of"] # [doc = "   `haystack`."] # [doc = " * `context` - The (possibly empty) bytes surrounding `haystack`. `haystack`"] # [doc = "   must be contained within `context` such that `context` is at least as big"] # [doc = "   as `haystack`."] # [doc = ""] # [doc = " This split is crucial for dealing with look-around. For example, consider"] # [doc = " the context `foobarbaz`, the haystack `bar` and the regex `^bar$`. This"] # [doc = " regex should _not_ match the haystack since `bar` does not appear at the"] # [doc = " beginning of the input. Similarly, the regex `\\Bbar\\B` should match the"] # [doc = " haystack because `bar` is not surrounded by word boundaries. But a search"] # [doc = " that does not take context into account would not permit `\\B` to match"] # [doc = " since the beginning of any string matches a word boundary. Similarly, a"] # [doc = " search that does not take context into account when searching `^bar$` in"] # [doc = " the haystack `bar` would produce a match when it shouldn't."] # [doc = ""] # [doc = " Thus, it follows that the starting state is chosen based on the following"] # [doc = " criteria, derived from the position at which the search starts in the"] # [doc = " `context` (corresponding to the start of `haystack`):"] # [doc = ""] # [doc = " 1. If the search starts at the beginning of `context`, then the `Text`"] # [doc = "    start state is used. (Since `^` corresponds to"] # [doc = "    `hir::Anchor::Start`.)"] # [doc = " 2. If the search starts at a position immediately following a line"] # [doc = "    terminator, then the `Line` start state is used. (Since `(?m:^)`"] # [doc = "    corresponds to `hir::Anchor::StartLF`.)"] # [doc = " 3. If the search starts at a position immediately following a byte"] # [doc = "    classified as a \"word\" character (`[_0-9a-zA-Z]`), then the `WordByte`"] # [doc = "    start state is used. (Since `(?-u:\\b)` corresponds to a word boundary.)"] # [doc = " 4. Otherwise, if the search starts at a position immediately following"] # [doc = "    a byte that is not classified as a \"word\" character (`[^_0-9a-zA-Z]`),"] # [doc = "    then the `NonWordByte` start state is used. (Since `(?-u:\\B)`"] # [doc = "    corresponds to a not-word-boundary.)"] # [doc = ""] # [doc = " (N.B. Unicode word boundaries are not supported by the DFA because they"] # [doc = " require multi-byte look-around and this is difficult to support in a DFA.)"] # [doc = ""] # [doc = " To further complicate things, we also support constructing individual"] # [doc = " anchored start states for each pattern in the DFA. (Which is required to"] # [doc = " implement overlapping regexes correctly, but is also generally useful.)"] # [doc = " Thus, when individual start states for each pattern are enabled, then the"] # [doc = " total number of start states represented is `4 + (4 * #patterns)`, where"] # [doc = " the 4 comes from each of the 4 possibilities above. The first 4 represents"] # [doc = " the starting states for the entire DFA, which support searching for"] # [doc = " multiple patterns simultaneously (possibly unanchored)."] # [doc = ""] # [doc = " If individual start states are disabled, then this will only store 4"] # [doc = " start states. Typically, individual start states are only enabled when"] # [doc = " constructing the reverse DFA for regex matching. But they are also useful"] # [doc = " for building DFAs that can search for a specific pattern or even to support"] # [doc = " both anchored and unanchored searches with the same DFA."] # [doc = ""] # [doc = " Note though that while the start table always has either `4` or"] # [doc = " `4 + (4 * #patterns)` starting state *ids*, the total number of states"] # [doc = " might be considerably smaller. That is, many of the IDs may be duplicative."] # [doc = " (For example, if a regex doesn't have a `\\b` sub-pattern, then there's no"] # [doc = " reason to generate a unique starting state for handling word boundaries."] # [doc = " Similarly for start/end anchors.)"] # [derive (Clone)] pub (crate) struct StartTable < T > { # [doc = " The initial start state IDs."] # [doc = ""] # [doc = " In practice, T is either `Vec<u32>` or `&[u32]`."] # [doc = ""] # [doc = " The first `2 * stride` (currently always 8) entries always correspond"] # [doc = " to the starts states for the entire DFA, with the first 4 entries being"] # [doc = " for unanchored searches and the second 4 entries being for anchored"] # [doc = " searches. To keep things simple, we always use 8 entries even if the"] # [doc = " `StartKind` is not both."] # [doc = ""] # [doc = " After that, there are `stride * patterns` state IDs, where `patterns`"] # [doc = " may be zero in the case of a DFA with no patterns or in the case where"] # [doc = " the DFA was built without enabling starting states for each pattern."] table : T , # [doc = " The starting state configuration supported. When 'both', both"] # [doc = " unanchored and anchored searches work. When 'unanchored', anchored"] # [doc = " searches panic. When 'anchored', unanchored searches panic."] kind : StartKind , # [doc = " The start state configuration for every possible byte."] start_map : StartByteMap , # [doc = " The number of starting state IDs per pattern."] stride : usize , # [doc = " The total number of patterns for which starting states are encoded."] # [doc = " This is `None` for DFAs that were built without start states for each"] # [doc = " pattern. Thus, one cannot use this field to say how many patterns"] # [doc = " are in the DFA in all cases. It is specific to how many patterns are"] # [doc = " represented in this start table."] pattern_len : Option < usize > , # [doc = " The universal starting state for unanchored searches. This is only"] # [doc = " present when the DFA supports unanchored searches and when all starting"] # [doc = " state IDs for an unanchored search are equivalent."] universal_start_unanchored : Option < StateID > , # [doc = " The universal starting state for anchored searches. This is only"] # [doc = " present when the DFA supports anchored searches and when all starting"] # [doc = " state IDs for an anchored search are equivalent."] universal_start_anchored : Option < StateID > , }
};
}
