// Generated macro for impl_268 (impl)
macro_rules! Depcrate_hybrid_dfaimpl_268 {
() => {
// Module: crate::hybrid::dfa
// Provides: {"impl_268"}
// Dependencies: {}
impl DFA { # [doc = " Executes a forward search and returns the end position of the leftmost"] # [doc = " match that is found. If no match exists, then `None` is returned."] # [doc = ""] # [doc = " In particular, this method continues searching even after it enters"] # [doc = " a match state. The search only terminates once it has reached the"] # [doc = " end of the input or when it has entered a dead or quit state. Upon"] # [doc = " termination, the position of the last byte seen while still in a match"] # [doc = " state is returned."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine errors if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the lazy DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the lazy DFA quitting."] # [doc = " * The configuration of the lazy DFA may also permit it to \"give up\""] # [doc = " on a search if it makes ineffective use of its transition table"] # [doc = " cache. The default configuration does not enable this by default,"] # [doc = " although it is typically a good idea to."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search returns an error, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to run a basic search."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::dfa::DFA, HalfMatch, Input};"] # [doc = ""] # [doc = " let dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = " let expected = HalfMatch::must(0, 8);"] # [doc = " assert_eq!(Some(expected), dfa.try_search_fwd("] # [doc = "     &mut cache, &Input::new(\"foo12345\"))?,"] # [doc = " );"] # [doc = ""] # [doc = " // Even though a match is found after reading the first byte (`a`),"] # [doc = " // the leftmost first match semantics demand that we find the earliest"] # [doc = " // match that prefers earlier parts of the pattern over later parts."] # [doc = " let dfa = DFA::new(\"abc|a\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = " let expected = HalfMatch::must(0, 3);"] # [doc = " assert_eq!(Some(expected), dfa.try_search_fwd("] # [doc = "     &mut cache, &Input::new(\"abc\"))?,"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: specific pattern search"] # [doc = ""] # [doc = " This example shows how to build a lazy multi-DFA that permits searching"] # [doc = " for specific patterns."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::dfa::DFA,"] # [doc = "     Anchored, HalfMatch, PatternID, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .configure(DFA::config().starts_for_each_pattern(true))"] # [doc = "     .build_many(&[\"[a-z0-9]{6}\", \"[a-z][a-z0-9]{5}\"])?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = " let haystack = \"foo123\";"] # [doc = ""] # [doc = " // Since we are using the default leftmost-first match and both"] # [doc = " // patterns match at the same starting position, only the first pattern"] # [doc = " // will be returned in this case when doing a search for any of the"] # [doc = " // patterns."] # [doc = " let expected = Some(HalfMatch::must(0, 6));"] # [doc = " let got = dfa.try_search_fwd(&mut cache, &Input::new(haystack))?;"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " // But if we want to check whether some other pattern matches, then we"] # [doc = " // can provide its pattern ID."] # [doc = " let expected = Some(HalfMatch::must(1, 6));"] # [doc = " let input = Input::new(haystack)"] # [doc = "     .anchored(Anchored::Pattern(PatternID::must(1)));"] # [doc = " let got = dfa.try_search_fwd(&mut cache, &input)?;"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: specifying the bounds of a search"] # [doc = ""] # [doc = " This example shows how providing the bounds of a search can produce"] # [doc = " different results than simply sub-slicing the haystack."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::dfa::DFA, HalfMatch, Input};"] # [doc = ""] # [doc = " // N.B. We disable Unicode here so that we use a simple ASCII word"] # [doc = " // boundary. Alternatively, we could enable heuristic support for"] # [doc = " // Unicode word boundaries since our haystack is pure ASCII."] # [doc = " let dfa = DFA::new(r\"(?-u)\\b[0-9]{3}\\b\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = " let haystack = \"foo123bar\";"] # [doc = ""] # [doc = " // Since we sub-slice the haystack, the search doesn't know about the"] # [doc = " // larger context and assumes that `123` is surrounded by word"] # [doc = " // boundaries. And of course, the match position is reported relative"] # [doc = " // to the sub-slice as well, which means we get `3` instead of `6`."] # [doc = " let expected = Some(HalfMatch::must(0, 3));"] # [doc = " let got = dfa.try_search_fwd("] # [doc = "     &mut cache,"] # [doc = "     &Input::new(&haystack[3..6]),"] # [doc = " )?;"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " // But if we provide the bounds of the search within the context of the"] # [doc = " // entire haystack, then the search can take the surrounding context"] # [doc = " // into account. (And if we did find a match, it would be reported"] # [doc = " // as a valid offset into `haystack` instead of its sub-slice.)"] # [doc = " let expected = None;"] # [doc = " let got = dfa.try_search_fwd("] # [doc = "     &mut cache,"] # [doc = "     &Input::new(haystack).range(3..6),"] # [doc = " )?;"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn try_search_fwd (& self , cache : & mut Cache , input : & Input < '_ > ,) -> Result < Option < HalfMatch > , MatchError > { let utf8empty = self . get_nfa () . has_empty () && self . get_nfa () . is_utf8 () ; let hm = match search :: find_fwd (self , cache , input) ? { None => return Ok (None) , Some (hm) if ! utf8empty => return Ok (Some (hm)) , Some (hm) => hm , } ; empty :: skip_splits_fwd (input , hm , hm . offset () , | input | { let got = search :: find_fwd (self , cache , input) ? ; Ok (got . map (| hm | (hm , hm . offset ()))) }) } # [doc = " Executes a reverse search and returns the start of the position of the"] # [doc = " leftmost match that is found. If no match exists, then `None` is"] # [doc = " returned."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine errors if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the lazy DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the lazy DFA quitting."] # [doc = " * The configuration of the lazy DFA may also permit it to \"give up\""] # [doc = " on a search if it makes ineffective use of its transition table"] # [doc = " cache. The default configuration does not enable this by default,"] # [doc = " although it is typically a good idea to."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search returns an error, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This routine is principally useful when used in"] # [doc = " conjunction with the"] # [doc = " [`nfa::thompson::Config::reverse`](crate::nfa::thompson::Config::reverse)"] # [doc = " configuration. In general, it's unlikely to be correct to use both"] # [doc = " `try_search_fwd` and `try_search_rev` with the same DFA since any"] # [doc = " particular DFA will only support searching in one direction with"] # [doc = " respect to the pattern."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson,"] # [doc = "     hybrid::dfa::DFA,"] # [doc = "     HalfMatch, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .thompson(thompson::Config::new().reverse(true))"] # [doc = "     .build(\"foo[0-9]+\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = " let expected = HalfMatch::must(0, 0);"] # [doc = " assert_eq!("] # [doc = "     Some(expected),"] # [doc = "     dfa.try_search_rev(&mut cache, &Input::new(\"foo12345\"))?,"] # [doc = " );"] # [doc = ""] # [doc = " // Even though a match is found after reading the last byte (`c`),"] # [doc = " // the leftmost first match semantics demand that we find the earliest"] # [doc = " // match that prefers earlier parts of the pattern over latter parts."] # [doc = " let dfa = DFA::builder()"] # [doc = "     .thompson(thompson::Config::new().reverse(true))"] # [doc = "     .build(\"abc|c\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = " let expected = HalfMatch::must(0, 0);"] # [doc = " assert_eq!(Some(expected), dfa.try_search_rev("] # [doc = "     &mut cache, &Input::new(\"abc\"))?,"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: UTF-8 mode"] # [doc = ""] # [doc = " This examples demonstrates that UTF-8 mode applies to reverse"] # [doc = " DFAs. When UTF-8 mode is enabled in the underlying NFA, then all"] # [doc = " matches reported must correspond to valid UTF-8 spans. This includes"] # [doc = " prohibiting zero-width matches that split a codepoint."] # [doc = ""] # [doc = " UTF-8 mode is enabled by default. Notice below how the only zero-width"] # [doc = " matches reported are those at UTF-8 boundaries:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::dfa::DFA,"] # [doc = "     nfa::thompson,"] # [doc = "     HalfMatch, Input, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .thompson(thompson::Config::new().reverse(true))"] # [doc = "     .build(r\"\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = ""] # [doc = " // Run the reverse DFA to collect all matches."] # [doc = " let mut input = Input::new(\"☃\");"] # [doc = " let mut matches = vec![];"] # [doc = " loop {"] # [doc = "     match dfa.try_search_rev(&mut cache, &input)? {"] # [doc = "         None => break,"] # [doc = "         Some(hm) => {"] # [doc = "             matches.push(hm);"] # [doc = "             if hm.offset() == 0 || input.end() == 0 {"] # [doc = "                 break;"] # [doc = "             } else if hm.offset() < input.end() {"] # [doc = "                 input.set_end(hm.offset());"] # [doc = "             } else {"] # [doc = "                 // This is only necessary to handle zero-width"] # [doc = "                 // matches, which of course occur in this example."] # [doc = "                 // Without this, the search would never advance"] # [doc = "                 // backwards beyond the initial match."] # [doc = "                 input.set_end(input.end() - 1);"] # [doc = "             }"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // No matches split a codepoint."] # [doc = " let expected = vec!["] # [doc = "     HalfMatch::must(0, 3),"] # [doc = "     HalfMatch::must(0, 0),"] # [doc = " ];"] # [doc = " assert_eq!(expected, matches);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Now let's look at the same example, but with UTF-8 mode on the"] # [doc = " underlying NFA disabled:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::dfa::DFA,"] # [doc = "     nfa::thompson,"] # [doc = "     HalfMatch, Input, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .thompson(thompson::Config::new().reverse(true).utf8(false))"] # [doc = "     .build(r\"\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = ""] # [doc = " // Run the reverse DFA to collect all matches."] # [doc = " let mut input = Input::new(\"☃\");"] # [doc = " let mut matches = vec![];"] # [doc = " loop {"] # [doc = "     match dfa.try_search_rev(&mut cache, &input)? {"] # [doc = "         None => break,"] # [doc = "         Some(hm) => {"] # [doc = "             matches.push(hm);"] # [doc = "             if hm.offset() == 0 || input.end() == 0 {"] # [doc = "                 break;"] # [doc = "             } else if hm.offset() < input.end() {"] # [doc = "                 input.set_end(hm.offset());"] # [doc = "             } else {"] # [doc = "                 // This is only necessary to handle zero-width"] # [doc = "                 // matches, which of course occur in this example."] # [doc = "                 // Without this, the search would never advance"] # [doc = "                 // backwards beyond the initial match."] # [doc = "                 input.set_end(input.end() - 1);"] # [doc = "             }"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // No matches split a codepoint."] # [doc = " let expected = vec!["] # [doc = "     HalfMatch::must(0, 3),"] # [doc = "     HalfMatch::must(0, 2),"] # [doc = "     HalfMatch::must(0, 1),"] # [doc = "     HalfMatch::must(0, 0),"] # [doc = " ];"] # [doc = " assert_eq!(expected, matches);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn try_search_rev (& self , cache : & mut Cache , input : & Input < '_ > ,) -> Result < Option < HalfMatch > , MatchError > { let utf8empty = self . get_nfa () . has_empty () && self . get_nfa () . is_utf8 () ; let hm = match search :: find_rev (self , cache , input) ? { None => return Ok (None) , Some (hm) if ! utf8empty => return Ok (Some (hm)) , Some (hm) => hm , } ; empty :: skip_splits_rev (input , hm , hm . offset () , | input | { let got = search :: find_rev (self , cache , input) ? ; Ok (got . map (| hm | (hm , hm . offset ()))) }) } # [doc = " Executes an overlapping forward search and returns the end position of"] # [doc = " matches as they are found. If no match exists, then `None` is returned."] # [doc = ""] # [doc = " This routine is principally only useful when searching for multiple"] # [doc = " patterns on inputs where multiple patterns may match the same regions"] # [doc = " of text. In particular, callers must preserve the automaton's search"] # [doc = " state from prior calls so that the implementation knows where the last"] # [doc = " match occurred."] # [doc = ""] # [doc = " When using this routine to implement an iterator of overlapping"] # [doc = " matches, the `start` of the search should remain invariant throughout"] # [doc = " iteration. The `OverlappingState` given to the search will keep track"] # [doc = " of the current position of the search. (This is because multiple"] # [doc = " matches may be reported at the same position, so only the search"] # [doc = " implementation itself knows when to advance the position.)"] # [doc = ""] # [doc = " If for some reason you want the search to forget about its previous"] # [doc = " state and restart the search at a particular position, then setting the"] # [doc = " state to [`OverlappingState::start`] will accomplish that."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine errors if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the lazy DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the lazy DFA quitting."] # [doc = " * The configuration of the lazy DFA may also permit it to \"give up\""] # [doc = " on a search if it makes ineffective use of its transition table"] # [doc = " cache. The default configuration does not enable this by default,"] # [doc = " although it is typically a good idea to."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search returns an error, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to run a basic overlapping search. Notice"] # [doc = " that we build the automaton with a `MatchKind::All` configuration."] # [doc = " Overlapping searches are unlikely to work as one would expect when"] # [doc = " using the default `MatchKind::LeftmostFirst` match semantics, since"] # [doc = " leftmost-first matching is fundamentally incompatible with overlapping"] # [doc = " searches. Namely, overlapping searches need to report matches as they"] # [doc = " are seen, where as leftmost-first searches will continue searching even"] # [doc = " after a match has been observed in order to find the conventional end"] # [doc = " position of the match. More concretely, leftmost-first searches use"] # [doc = " dead states to terminate a search after a specific match can no longer"] # [doc = " be extended. Overlapping searches instead do the opposite by continuing"] # [doc = " the search to find totally new matches (potentially of other patterns)."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::dfa::{DFA, OverlappingState},"] # [doc = "     HalfMatch, Input, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .configure(DFA::config().match_kind(MatchKind::All))"] # [doc = "     .build_many(&[r\"\\w+$\", r\"\\S+$\"])?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = ""] # [doc = " let haystack = \"@foo\";"] # [doc = " let mut state = OverlappingState::start();"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(1, 4));"] # [doc = " dfa.try_search_overlapping_fwd("] # [doc = "     &mut cache, &Input::new(haystack), &mut state,"] # [doc = " )?;"] # [doc = " assert_eq!(expected, state.get_match());"] # [doc = ""] # [doc = " // The first pattern also matches at the same position, so re-running"] # [doc = " // the search will yield another match. Notice also that the first"] # [doc = " // pattern is returned after the second. This is because the second"] # [doc = " // pattern begins its match before the first, is therefore an earlier"] # [doc = " // match and is thus reported first."] # [doc = " let expected = Some(HalfMatch::must(0, 4));"] # [doc = " dfa.try_search_overlapping_fwd("] # [doc = "     &mut cache, &Input::new(haystack), &mut state,"] # [doc = " )?;"] # [doc = " assert_eq!(expected, state.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn try_search_overlapping_fwd (& self , cache : & mut Cache , input : & Input < '_ > , state : & mut OverlappingState ,) -> Result < () , MatchError > { let utf8empty = self . get_nfa () . has_empty () && self . get_nfa () . is_utf8 () ; search :: find_overlapping_fwd (self , cache , input , state) ? ; match state . get_match () { None => Ok (()) , Some (_) if ! utf8empty => Ok (()) , Some (_) => skip_empty_utf8_splits_overlapping (input , state , | input , state | { search :: find_overlapping_fwd (self , cache , input , state) } ,) , } } # [doc = " Executes a reverse overlapping search and returns the start of the"] # [doc = " position of the leftmost match that is found. If no match exists, then"] # [doc = " `None` is returned."] # [doc = ""] # [doc = " When using this routine to implement an iterator of overlapping"] # [doc = " matches, the `start` of the search should remain invariant throughout"] # [doc = " iteration. The `OverlappingState` given to the search will keep track"] # [doc = " of the current position of the search. (This is because multiple"] # [doc = " matches may be reported at the same position, so only the search"] # [doc = " implementation itself knows when to advance the position.)"] # [doc = ""] # [doc = " If for some reason you want the search to forget about its previous"] # [doc = " state and restart the search at a particular position, then setting the"] # [doc = " state to [`OverlappingState::start`] will accomplish that."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine errors if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the lazy DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the lazy DFA quitting."] # [doc = " * The configuration of the lazy DFA may also permit it to \"give up\""] # [doc = " on a search if it makes ineffective use of its transition table"] # [doc = " cache. The default configuration does not enable this by default,"] # [doc = " although it is typically a good idea to."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search returns an error, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Example: UTF-8 mode"] # [doc = ""] # [doc = " This examples demonstrates that UTF-8 mode applies to reverse"] # [doc = " DFAs. When UTF-8 mode is enabled in the underlying NFA, then all"] # [doc = " matches reported must correspond to valid UTF-8 spans. This includes"] # [doc = " prohibiting zero-width matches that split a codepoint."] # [doc = ""] # [doc = " UTF-8 mode is enabled by default. Notice below how the only zero-width"] # [doc = " matches reported are those at UTF-8 boundaries:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::dfa::{DFA, OverlappingState},"] # [doc = "     nfa::thompson,"] # [doc = "     HalfMatch, Input, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .configure(DFA::config().match_kind(MatchKind::All))"] # [doc = "     .thompson(thompson::Config::new().reverse(true))"] # [doc = "     .build_many(&[r\"\", r\"☃\"])?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = ""] # [doc = " // Run the reverse DFA to collect all matches."] # [doc = " let input = Input::new(\"☃\");"] # [doc = " let mut state = OverlappingState::start();"] # [doc = " let mut matches = vec![];"] # [doc = " loop {"] # [doc = "     dfa.try_search_overlapping_rev(&mut cache, &input, &mut state)?;"] # [doc = "     match state.get_match() {"] # [doc = "         None => break,"] # [doc = "         Some(hm) => matches.push(hm),"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // No matches split a codepoint."] # [doc = " let expected = vec!["] # [doc = "     HalfMatch::must(0, 3),"] # [doc = "     HalfMatch::must(1, 0),"] # [doc = "     HalfMatch::must(0, 0),"] # [doc = " ];"] # [doc = " assert_eq!(expected, matches);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Now let's look at the same example, but with UTF-8 mode on the"] # [doc = " underlying NFA disabled:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::dfa::{DFA, OverlappingState},"] # [doc = "     nfa::thompson,"] # [doc = "     HalfMatch, Input, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .configure(DFA::config().match_kind(MatchKind::All))"] # [doc = "     .thompson(thompson::Config::new().reverse(true).utf8(false))"] # [doc = "     .build_many(&[r\"\", r\"☃\"])?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = ""] # [doc = " // Run the reverse DFA to collect all matches."] # [doc = " let input = Input::new(\"☃\");"] # [doc = " let mut state = OverlappingState::start();"] # [doc = " let mut matches = vec![];"] # [doc = " loop {"] # [doc = "     dfa.try_search_overlapping_rev(&mut cache, &input, &mut state)?;"] # [doc = "     match state.get_match() {"] # [doc = "         None => break,"] # [doc = "         Some(hm) => matches.push(hm),"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // Now *all* positions match, even within a codepoint,"] # [doc = " // because we lifted the requirement that matches"] # [doc = " // correspond to valid UTF-8 spans."] # [doc = " let expected = vec!["] # [doc = "     HalfMatch::must(0, 3),"] # [doc = "     HalfMatch::must(0, 2),"] # [doc = "     HalfMatch::must(0, 1),"] # [doc = "     HalfMatch::must(1, 0),"] # [doc = "     HalfMatch::must(0, 0),"] # [doc = " ];"] # [doc = " assert_eq!(expected, matches);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn try_search_overlapping_rev (& self , cache : & mut Cache , input : & Input < '_ > , state : & mut OverlappingState ,) -> Result < () , MatchError > { let utf8empty = self . get_nfa () . has_empty () && self . get_nfa () . is_utf8 () ; search :: find_overlapping_rev (self , cache , input , state) ? ; match state . get_match () { None => Ok (()) , Some (_) if ! utf8empty => Ok (()) , Some (_) => skip_empty_utf8_splits_overlapping (input , state , | input , state | { search :: find_overlapping_rev (self , cache , input , state) } ,) , } } # [doc = " Writes the set of patterns that match anywhere in the given search"] # [doc = " configuration to `patset`. If multiple patterns match at the same"] # [doc = " position and the underlying DFA supports overlapping matches, then all"] # [doc = " matching patterns are written to the given set."] # [doc = ""] # [doc = " Unless all of the patterns in this DFA are anchored, then generally"] # [doc = " speaking, this will visit every byte in the haystack."] # [doc = ""] # [doc = " This search routine *does not* clear the pattern set. This gives some"] # [doc = " flexibility to the caller (e.g., running multiple searches with the"] # [doc = " same pattern set), but does make the API bug-prone if you're reusing"] # [doc = " the same pattern set for multiple searches but intended them to be"] # [doc = " independent."] # [doc = ""] # [doc = " If a pattern ID matched but the given `PatternSet` does not have"] # [doc = " sufficient capacity to store it, then it is not inserted and silently"] # [doc = " dropped."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine errors if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the lazy DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the lazy DFA quitting."] # [doc = " * The configuration of the lazy DFA may also permit it to \"give up\""] # [doc = " on a search if it makes ineffective use of its transition table"] # [doc = " cache. The default configuration does not enable this by default,"] # [doc = " although it is typically a good idea to."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search returns an error, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to find all matching patterns in a haystack,"] # [doc = " even when some patterns match at the same position as other patterns."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::dfa::DFA,"] # [doc = "     Input, MatchKind, PatternSet,"] # [doc = " };"] # [doc = ""] # [doc = " let patterns = &["] # [doc = "     r\"\\w+\", r\"\\d+\", r\"\\pL+\", r\"foo\", r\"bar\", r\"barfoo\", r\"foobar\","] # [doc = " ];"] # [doc = " let dfa = DFA::builder()"] # [doc = "     .configure(DFA::config().match_kind(MatchKind::All))"] # [doc = "     .build_many(patterns)?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\");"] # [doc = " let mut patset = PatternSet::new(dfa.pattern_len());"] # [doc = " dfa.try_which_overlapping_matches(&mut cache, &input, &mut patset)?;"] # [doc = " let expected = vec![0, 2, 3, 4, 6];"] # [doc = " let got: Vec<usize> = patset.iter().map(|p| p.as_usize()).collect();"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn try_which_overlapping_matches (& self , cache : & mut Cache , input : & Input < '_ > , patset : & mut PatternSet ,) -> Result < () , MatchError > { let mut state = OverlappingState :: start () ; while let Some (m) = { self . try_search_overlapping_fwd (cache , input , & mut state) ? ; state . get_match () } { let _ = patset . try_insert (m . pattern ()) ; if patset . is_full () || input . get_earliest () { break ; } } Ok (()) } }
};
}
