macro_rules! deps {
    () => {
        ByteClassIter!();
        DeserializeError!();
        Unit!();
        UnitKind!();
        U8!();
        ByteClassElementRanges!();
        DFA!();
        NFA!();
        ByteClassElements!();
        ByteClasses!();
        ByteClassRepresentatives!();
        SerializeError!();
    };
}

macro_rules! impl_584 {
    () => {
        deps!();
        impl ByteClasses { # [doc = " Creates a new set of equivalence classes where all bytes are mapped to"] # [doc = " the same class."] # [inline] pub fn empty () -> ByteClasses { ByteClasses ([0 ; 256]) } # [doc = " Creates a new set of equivalence classes where each byte belongs to"] # [doc = " its own equivalence class."] # [inline] pub fn singletons () -> ByteClasses { let mut classes = ByteClasses :: empty () ; for b in 0 ..= 255 { classes . set (b , b) ; } classes } # [doc = " Deserializes a byte class map from the given slice. If the slice is of"] # [doc = " insufficient length or otherwise contains an impossible mapping, then"] # [doc = " an error is returned. Upon success, the number of bytes read along with"] # [doc = " the map are returned. The number of bytes read is always a multiple of"] # [doc = " 8."] pub (crate) fn from_bytes (slice : & [u8] ,) -> Result < (ByteClasses , usize) , DeserializeError > { wire :: check_slice_len (slice , 256 , "byte class map") ? ; let mut classes = ByteClasses :: empty () ; for (b , & class) in slice [.. 256] . iter () . enumerate () { classes . set (u8 :: try_from (b) . unwrap () , class) ; } for & b in classes . 0 . iter () { if usize :: from (b) >= classes . alphabet_len () { return Err (DeserializeError :: generic ("found equivalence class greater than alphabet len" ,)) ; } } Ok ((classes , 256)) } # [doc = " Writes this byte class map to the given byte buffer. if the given"] # [doc = " buffer is too small, then an error is returned. Upon success, the total"] # [doc = " number of bytes written is returned. The number of bytes written is"] # [doc = " guaranteed to be a multiple of 8."] pub (crate) fn write_to (& self , mut dst : & mut [u8] ,) -> Result < usize , SerializeError > { let nwrite = self . write_to_len () ; if dst . len () < nwrite { return Err (SerializeError :: buffer_too_small ("byte class map")) ; } for b in 0 ..= 255 { dst [0] = self . get (b) ; dst = & mut dst [1 ..] ; } Ok (nwrite) } # [doc = " Returns the total number of bytes written by `write_to`."] pub (crate) fn write_to_len (& self) -> usize { 256 } # [doc = " Set the equivalence class for the given byte."] # [inline] pub fn set (& mut self , byte : u8 , class : u8) { self . 0 [usize :: from (byte)] = class ; } # [doc = " Get the equivalence class for the given byte."] # [inline] pub fn get (& self , byte : u8) -> u8 { self . 0 [usize :: from (byte)] } # [doc = " Get the equivalence class for the given haystack unit and return the"] # [doc = " class as a `usize`."] # [inline] pub fn get_by_unit (& self , unit : Unit) -> usize { match unit . 0 { UnitKind :: U8 (b) => usize :: from (self . get (b)) , UnitKind :: EOI (b) => usize :: from (b) , } } # [doc = " Create a unit that represents the \"end of input\" sentinel based on the"] # [doc = " number of equivalence classes."] # [inline] pub fn eoi (& self) -> Unit { Unit :: eoi (self . alphabet_len () . checked_sub (1) . unwrap ()) } # [doc = " Return the total number of elements in the alphabet represented by"] # [doc = " these equivalence classes. Equivalently, this returns the total number"] # [doc = " of equivalence classes."] # [inline] pub fn alphabet_len (& self) -> usize { usize :: from (self . 0 [255]) + 1 + 1 } # [doc = " Returns the stride, as a base-2 exponent, required for these"] # [doc = " equivalence classes."] # [doc = ""] # [doc = " The stride is always the smallest power of 2 that is greater than or"] # [doc = " equal to the alphabet length, and the `stride2` returned here is the"] # [doc = " exponent applied to `2` to get the smallest power. This is done so that"] # [doc = " converting between premultiplied state IDs and indices can be done with"] # [doc = " shifts alone, which is much faster than integer division."] # [inline] pub fn stride2 (& self) -> usize { let zeros = self . alphabet_len () . next_power_of_two () . trailing_zeros () ; usize :: try_from (zeros) . unwrap () } # [doc = " Returns true if and only if every byte in this class maps to its own"] # [doc = " equivalence class. Equivalently, there are 257 equivalence classes"] # [doc = " and each class contains either exactly one byte or corresponds to the"] # [doc = " singleton class containing the \"end of input\" sentinel."] # [inline] pub fn is_singleton (& self) -> bool { self . alphabet_len () == 257 } # [doc = " Returns an iterator over all equivalence classes in this set."] # [inline] pub fn iter (& self) -> ByteClassIter < '_ > { ByteClassIter { classes : self , i : 0 } } # [doc = " Returns an iterator over a sequence of representative bytes from each"] # [doc = " equivalence class within the range of bytes given."] # [doc = ""] # [doc = " When the given range is unbounded on both sides, the iterator yields"] # [doc = " exactly N items, where N is equivalent to the number of equivalence"] # [doc = " classes. Each item is an arbitrary byte drawn from each equivalence"] # [doc = " class."] # [doc = ""] # [doc = " This is useful when one is determinizing an NFA and the NFA's alphabet"] # [doc = " hasn't been converted to equivalence classes. Picking an arbitrary byte"] # [doc = " from each equivalence class then permits a full exploration of the NFA"] # [doc = " instead of using every possible byte value and thus potentially saves"] # [doc = " quite a lot of redundant work."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows an example of what a complete sequence of representatives"] # [doc = " might look like from a real example."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, util::alphabet::Unit};"] # [doc = ""] # [doc = " let nfa = NFA::new(\"[a-z]+\")?;"] # [doc = " let classes = nfa.byte_classes();"] # [doc = " let reps: Vec<Unit> = classes.representatives(..).collect();"] # [doc = " // Note that the specific byte values yielded are not guaranteed!"] # [doc = " let expected = vec!["] # [doc = "     Unit::u8(b'\\x00'),"] # [doc = "     Unit::u8(b'a'),"] # [doc = "     Unit::u8(b'{'),"] # [doc = "     Unit::eoi(3),"] # [doc = " ];"] # [doc = " assert_eq!(expected, reps);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Note though, that you can ask for an arbitrary range of bytes, and only"] # [doc = " representatives for that range will be returned:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, util::alphabet::Unit};"] # [doc = ""] # [doc = " let nfa = NFA::new(\"[a-z]+\")?;"] # [doc = " let classes = nfa.byte_classes();"] # [doc = " let reps: Vec<Unit> = classes.representatives(b'A'..=b'z').collect();"] # [doc = " // Note that the specific byte values yielded are not guaranteed!"] # [doc = " let expected = vec!["] # [doc = "     Unit::u8(b'A'),"] # [doc = "     Unit::u8(b'a'),"] # [doc = " ];"] # [doc = " assert_eq!(expected, reps);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn representatives < R : core :: ops :: RangeBounds < u8 > > (& self , range : R ,) -> ByteClassRepresentatives < '_ > { use core :: ops :: Bound ; let cur_byte = match range . start_bound () { Bound :: Included (& i) => usize :: from (i) , Bound :: Excluded (& i) => usize :: from (i) . checked_add (1) . unwrap () , Bound :: Unbounded => 0 , } ; let end_byte = match range . end_bound () { Bound :: Included (& i) => { Some (usize :: from (i) . checked_add (1) . unwrap ()) } Bound :: Excluded (& i) => Some (usize :: from (i)) , Bound :: Unbounded => None , } ; assert_ne ! (cur_byte , usize :: MAX , "start range must be less than usize::MAX" ,) ; ByteClassRepresentatives { classes : self , cur_byte , end_byte , last_class : None , } } # [doc = " Returns an iterator of the bytes in the given equivalence class."] # [doc = ""] # [doc = " This is useful when one needs to know the actual bytes that belong to"] # [doc = " an equivalence class. For example, conceptually speaking, accelerating"] # [doc = " a DFA state occurs when a state only has a few outgoing transitions."] # [doc = " But in reality, what is required is that there are only a small"] # [doc = " number of distinct bytes that can lead to an outgoing transition. The"] # [doc = " difference is that any one transition can correspond to an equivalence"] # [doc = " class which may contains many bytes. Therefore, DFA state acceleration"] # [doc = " considers the actual elements in each equivalence class of each"] # [doc = " outgoing transition."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows an example of how to get all of the elements in an"] # [doc = " equivalence class."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, util::alphabet::Unit};"] # [doc = ""] # [doc = " let nfa = NFA::new(\"[a-z]+\")?;"] # [doc = " let classes = nfa.byte_classes();"] # [doc = " let elements: Vec<Unit> = classes.elements(Unit::u8(1)).collect();"] # [doc = " let expected: Vec<Unit> = (b'a'..=b'z').map(Unit::u8).collect();"] # [doc = " assert_eq!(expected, elements);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn elements (& self , class : Unit) -> ByteClassElements < '_ > { ByteClassElements { classes : self , class , byte : 0 } } # [doc = " Returns an iterator of byte ranges in the given equivalence class."] # [doc = ""] # [doc = " That is, a sequence of contiguous ranges are returned. Typically, every"] # [doc = " class maps to a single contiguous range."] fn element_ranges (& self , class : Unit) -> ByteClassElementRanges < '_ > { ByteClassElementRanges { elements : self . elements (class) , range : None } } }
    };
}

impl_584!();