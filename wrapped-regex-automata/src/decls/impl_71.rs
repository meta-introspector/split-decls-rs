macro_rules! deps {
    () => {
        Cache!();
        Captures!();
        MatchKind!();
        BuildError!();
        NFA!();
        Match!();
        DFA!();
        Transition!();
        Builder!();
        Config!();
        StateID!();
    };
}

macro_rules! impl_71 {
    () => {
        deps!();
        impl DFA { # [doc = " Parse the given regular expression using the default configuration and"] # [doc = " return the corresponding one-pass DFA."] # [doc = ""] # [doc = " If you want a non-default configuration, then use the [`Builder`] to"] # [doc = " set your own configuration."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::onepass::DFA, Match};"] # [doc = ""] # [doc = " let re = DFA::new(\"foo[0-9]+bar\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"foo12345barzzz\", &mut caps);"] # [doc = " assert_eq!(Some(Match::must(0, 0..11)), caps.get_match());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] # [inline] pub fn new (pattern : & str) -> Result < DFA , BuildError > { DFA :: builder () . build (pattern) } # [doc = " Like `new`, but parses multiple patterns into a single \"multi regex.\""] # [doc = " This similarly uses the default regex configuration."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::onepass::DFA, Match};"] # [doc = ""] # [doc = " let re = DFA::new_many(&[\"[a-z]+\", \"[0-9]+\"])?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"abc123\", &mut caps);"] # [doc = " assert_eq!(Some(Match::must(0, 0..3)), caps.get_match());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"123abc\", &mut caps);"] # [doc = " assert_eq!(Some(Match::must(1, 0..3)), caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] # [inline] pub fn new_many < P : AsRef < str > > (patterns : & [P]) -> Result < DFA , BuildError > { DFA :: builder () . build_many (patterns) } # [doc = " Like `new`, but builds a one-pass DFA directly from an NFA. This is"] # [doc = " useful if you already have an NFA, or even if you hand-assembled the"] # [doc = " NFA."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to hand assemble a regular expression via its HIR,"] # [doc = " compile an NFA from it and build a one-pass DFA from the NFA."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     dfa::onepass::DFA,"] # [doc = "     nfa::thompson::NFA,"] # [doc = "     Match,"] # [doc = " };"] # [doc = " use regex_syntax::hir::{Hir, Class, ClassBytes, ClassBytesRange};"] # [doc = ""] # [doc = " let hir = Hir::class(Class::Bytes(ClassBytes::new(vec!["] # [doc = "     ClassBytesRange::new(b'0', b'9'),"] # [doc = "     ClassBytesRange::new(b'A', b'Z'),"] # [doc = "     ClassBytesRange::new(b'_', b'_'),"] # [doc = "     ClassBytesRange::new(b'a', b'z'),"] # [doc = " ])));"] # [doc = ""] # [doc = " let config = NFA::config().nfa_size_limit(Some(1_000));"] # [doc = " let nfa = NFA::compiler().configure(config).build_from_hir(&hir)?;"] # [doc = ""] # [doc = " let re = DFA::new_from_nfa(nfa)?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = " let expected = Some(Match::must(0, 0..1));"] # [doc = " re.captures(&mut cache, \"A\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn new_from_nfa (nfa : NFA) -> Result < DFA , BuildError > { DFA :: builder () . build_from_nfa (nfa) } # [doc = " Create a new one-pass DFA that matches every input."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::onepass::DFA, Match};"] # [doc = ""] # [doc = " let dfa = DFA::always_match()?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = " let mut caps = dfa.create_captures();"] # [doc = ""] # [doc = " let expected = Match::must(0, 0..0);"] # [doc = " dfa.captures(&mut cache, \"\", &mut caps);"] # [doc = " assert_eq!(Some(expected), caps.get_match());"] # [doc = " dfa.captures(&mut cache, \"foo\", &mut caps);"] # [doc = " assert_eq!(Some(expected), caps.get_match());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn always_match () -> Result < DFA , BuildError > { let nfa = thompson :: NFA :: always_match () ; Builder :: new () . build_from_nfa (nfa) } # [doc = " Create a new one-pass DFA that never matches any input."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::dfa::onepass::DFA;"] # [doc = ""] # [doc = " let dfa = DFA::never_match()?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = " let mut caps = dfa.create_captures();"] # [doc = ""] # [doc = " dfa.captures(&mut cache, \"\", &mut caps);"] # [doc = " assert_eq!(None, caps.get_match());"] # [doc = " dfa.captures(&mut cache, \"foo\", &mut caps);"] # [doc = " assert_eq!(None, caps.get_match());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn never_match () -> Result < DFA , BuildError > { let nfa = thompson :: NFA :: never_match () ; Builder :: new () . build_from_nfa (nfa) } # [doc = " Return a default configuration for a DFA."] # [doc = ""] # [doc = " This is a convenience routine to avoid needing to import the `Config`"] # [doc = " type when customizing the construction of a DFA."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to change the match semantics of this DFA from"] # [doc = " its default \"leftmost first\" to \"all.\" When using \"all,\" non-greediness"] # [doc = " doesn't apply and neither does preference order matching. Instead, the"] # [doc = " longest match possible is always returned. (Although, by construction,"] # [doc = " it's impossible for a one-pass DFA to have a different answer for"] # [doc = " \"preference order\" vs \"longest match.\")"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::onepass::DFA, Match, MatchKind};"] # [doc = ""] # [doc = " let re = DFA::builder()"] # [doc = "     .configure(DFA::config().match_kind(MatchKind::All))"] # [doc = "     .build(r\"(abc)+?\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = ""] # [doc = " re.captures(&mut cache, \"abcabc\", &mut caps);"] # [doc = " // Normally, the non-greedy repetition would give us a 0..3 match."] # [doc = " assert_eq!(Some(Match::must(0, 0..6)), caps.get_match());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn config () -> Config { Config :: new () } # [doc = " Return a builder for configuring the construction of a DFA."] # [doc = ""] # [doc = " This is a convenience routine to avoid needing to import the"] # [doc = " [`Builder`] type in common cases."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use the builder to disable UTF-8 mode."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     dfa::onepass::DFA,"] # [doc = "     nfa::thompson,"] # [doc = "     util::syntax,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = DFA::builder()"] # [doc = "     .syntax(syntax::Config::new().utf8(false))"] # [doc = "     .thompson(thompson::Config::new().utf8(false))"] # [doc = "     .build(r\"foo(?-u:[^b])ar.*\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " let haystack = b\"foo\\xFFarzz\\xE2\\x98\\xFF\\n\";"] # [doc = " let expected = Some(Match::must(0, 0..8));"] # [doc = " re.captures(&mut cache, haystack, &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn builder () -> Builder { Builder :: new () } # [doc = " Create a new empty set of capturing groups that is guaranteed to be"] # [doc = " valid for the search APIs on this DFA."] # [doc = ""] # [doc = " A `Captures` value created for a specific DFA cannot be used with any"] # [doc = " other DFA."] # [doc = ""] # [doc = " This is a convenience function for [`Captures::all`]. See the"] # [doc = " [`Captures`] documentation for an explanation of its alternative"] # [doc = " constructors that permit the DFA to do less work during a search, and"] # [doc = " thus might make it faster."] # [inline] pub fn create_captures (& self) -> Captures { Captures :: all (self . nfa . group_info () . clone ()) } # [doc = " Create a new cache for this DFA."] # [doc = ""] # [doc = " The cache returned should only be used for searches for this"] # [doc = " DFA. If you want to reuse the cache for another DFA, then you"] # [doc = " must call [`Cache::reset`] with that DFA (or, equivalently,"] # [doc = " [`DFA::reset_cache`])."] # [inline] pub fn create_cache (& self) -> Cache { Cache :: new (self) } # [doc = " Reset the given cache such that it can be used for searching with the"] # [doc = " this DFA (and only this DFA)."] # [doc = ""] # [doc = " A cache reset permits reusing memory already allocated in this cache"] # [doc = " with a different DFA."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to re-purpose a cache for use with a different DFA."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{dfa::onepass::DFA, Match};"] # [doc = ""] # [doc = " let re1 = DFA::new(r\"\\w\")?;"] # [doc = " let re2 = DFA::new(r\"\\W\")?;"] # [doc = " let mut caps1 = re1.create_captures();"] # [doc = " let mut caps2 = re2.create_captures();"] # [doc = ""] # [doc = " let mut cache = re1.create_cache();"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 0..2)),"] # [doc = "     { re1.captures(&mut cache, \"Δ\", &mut caps1); caps1.get_match() },"] # [doc = " );"] # [doc = ""] # [doc = " // Using 'cache' with re2 is not allowed. It may result in panics or"] # [doc = " // incorrect results. In order to re-purpose the cache, we must reset"] # [doc = " // it with the one-pass DFA we'd like to use it with."] # [doc = " //"] # [doc = " // Similarly, after this reset, using the cache with 're1' is also not"] # [doc = " // allowed."] # [doc = " re2.reset_cache(&mut cache);"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 0..3)),"] # [doc = "     { re2.captures(&mut cache, \"☃\", &mut caps2); caps2.get_match() },"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn reset_cache (& self , cache : & mut Cache) { cache . reset (self) ; } # [doc = " Return the config for this one-pass DFA."] # [inline] pub fn get_config (& self) -> & Config { & self . config } # [doc = " Returns a reference to the underlying NFA."] # [inline] pub fn get_nfa (& self) -> & NFA { & self . nfa } # [doc = " Returns the total number of patterns compiled into this DFA."] # [doc = ""] # [doc = " In the case of a DFA that contains no patterns, this returns `0`."] # [inline] pub fn pattern_len (& self) -> usize { self . get_nfa () . pattern_len () } # [doc = " Returns the total number of states in this one-pass DFA."] # [doc = ""] # [doc = " Note that unlike dense or sparse DFAs, a one-pass DFA does not expose"] # [doc = " a low level DFA API. Therefore, this routine has little use other than"] # [doc = " being informational."] # [inline] pub fn state_len (& self) -> usize { self . table . len () >> self . stride2 () } # [doc = " Returns the total number of elements in the alphabet for this DFA."] # [doc = ""] # [doc = " That is, this returns the total number of transitions that each"] # [doc = " state in this DFA must have. The maximum alphabet size is 256, which"] # [doc = " corresponds to each possible byte value."] # [doc = ""] # [doc = " The alphabet size may be less than 256 though, and unless"] # [doc = " [`Config::byte_classes`] is disabled, it is typically must less than"] # [doc = " 256. Namely, bytes are grouped into equivalence classes such that no"] # [doc = " two bytes in the same class can distinguish a match from a non-match."] # [doc = " For example, in the regex `^[a-z]+$`, the ASCII bytes `a-z` could"] # [doc = " all be in the same equivalence class. This leads to a massive space"] # [doc = " savings."] # [doc = ""] # [doc = " Note though that the alphabet length does _not_ necessarily equal the"] # [doc = " total stride space taken up by a single DFA state in the transition"] # [doc = " table. Namely, for performance reasons, the stride is always the"] # [doc = " smallest power of two that is greater than or equal to the alphabet"] # [doc = " length. For this reason, [`DFA::stride`] or [`DFA::stride2`] are"] # [doc = " often more useful. The alphabet length is typically useful only for"] # [doc = " informational purposes."] # [doc = ""] # [doc = " Note also that unlike dense or sparse DFAs, a one-pass DFA does"] # [doc = " not have a special end-of-input (EOI) transition. This is because"] # [doc = " a one-pass DFA handles look-around assertions explicitly (like the"] # [doc = " [`PikeVM`](crate::nfa::thompson::pikevm::PikeVM)) and does not build"] # [doc = " them into the transitions of the DFA."] # [inline] pub fn alphabet_len (& self) -> usize { self . alphabet_len } # [doc = " Returns the total stride for every state in this DFA, expressed as the"] # [doc = " exponent of a power of 2. The stride is the amount of space each state"] # [doc = " takes up in the transition table, expressed as a number of transitions."] # [doc = " (Unused transitions map to dead states.)"] # [doc = ""] # [doc = " The stride of a DFA is always equivalent to the smallest power of"] # [doc = " 2 that is greater than or equal to the DFA's alphabet length. This"] # [doc = " definition uses extra space, but possibly permits faster translation"] # [doc = " between state identifiers and their corresponding offsets in this DFA's"] # [doc = " transition table."] # [doc = ""] # [doc = " For example, if the DFA's stride is 16 transitions, then its `stride2`"] # [doc = " is `4` since `2^4 = 16`."] # [doc = ""] # [doc = " The minimum `stride2` value is `1` (corresponding to a stride of `2`)"] # [doc = " while the maximum `stride2` value is `9` (corresponding to a stride"] # [doc = " of `512`). The maximum in theory should be `8`, but because of some"] # [doc = " implementation quirks that may be relaxed in the future, it is one more"] # [doc = " than `8`. (Do note that a maximal stride is incredibly rare, as it"] # [doc = " would imply that there is almost no redundant in the regex pattern.)"] # [doc = ""] # [doc = " Note that unlike dense or sparse DFAs, a one-pass DFA does not expose"] # [doc = " a low level DFA API. Therefore, this routine has little use other than"] # [doc = " being informational."] # [inline] pub fn stride2 (& self) -> usize { self . stride2 } # [doc = " Returns the total stride for every state in this DFA. This corresponds"] # [doc = " to the total number of transitions used by each state in this DFA's"] # [doc = " transition table."] # [doc = ""] # [doc = " Please see [`DFA::stride2`] for more information. In particular, this"] # [doc = " returns the stride as the number of transitions, where as `stride2`"] # [doc = " returns it as the exponent of a power of 2."] # [doc = ""] # [doc = " Note that unlike dense or sparse DFAs, a one-pass DFA does not expose"] # [doc = " a low level DFA API. Therefore, this routine has little use other than"] # [doc = " being informational."] # [inline] pub fn stride (& self) -> usize { 1 << self . stride2 () } # [doc = " Returns the memory usage, in bytes, of this DFA."] # [doc = ""] # [doc = " The memory usage is computed based on the number of bytes used to"] # [doc = " represent this DFA."] # [doc = ""] # [doc = " This does **not** include the stack size used up by this DFA. To"] # [doc = " compute that, use `std::mem::size_of::<onepass::DFA>()`."] # [inline] pub fn memory_usage (& self) -> usize { use core :: mem :: size_of ; self . table . len () * size_of :: < Transition > () + self . starts . len () * size_of :: < StateID > () } }
    };
}

impl_71!()