macro_rules! deps {
    () => {
        DFA!();
        Input!();
        Searcher!();
        FindMatches!();
        Cache!();
        Match!();
        Regex!();
    };
}

macro_rules! impl_275 {
    () => {
        deps!();
        # [doc = " Standard infallible search routines for finding and iterating over matches."] impl Regex { # [doc = " Returns true if and only if this regex matches the given haystack."] # [doc = ""] # [doc = " This routine may short circuit if it knows that scanning future input"] # [doc = " will never lead to a different result. In particular, if the underlying"] # [doc = " DFA enters a match state or a dead state, then this routine will return"] # [doc = " `true` or `false`, respectively, without inspecting any future input."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This routine panics if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the lazy DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the lazy DFA quitting."] # [doc = " * The configuration of the lazy DFA may also permit it to \"give up\""] # [doc = " on a search if it makes ineffective use of its transition table"] # [doc = " cache. The default configuration does not enable this by default,"] # [doc = " although it is typically a good idea to."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search panics, callers cannot know whether a match exists or"] # [doc = " not."] # [doc = ""] # [doc = " Use [`Regex::try_search`] if you want to handle these error conditions."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::hybrid::regex::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(\"foo[0-9]+bar\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " assert!(re.is_match(&mut cache, \"foo12345bar\"));"] # [doc = " assert!(!re.is_match(&mut cache, \"foobar\"));"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn is_match < 'h , I : Into < Input < 'h > > > (& self , cache : & mut Cache , input : I ,) -> bool { self . forward () . try_search_fwd (& mut cache . forward , & input . into () . earliest (true)) . unwrap () . is_some () } # [doc = " Returns the start and end offset of the leftmost match. If no match"] # [doc = " exists, then `None` is returned."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This routine panics if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the lazy DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the lazy DFA quitting."] # [doc = " * The configuration of the lazy DFA may also permit it to \"give up\""] # [doc = " on a search if it makes ineffective use of its transition table"] # [doc = " cache. The default configuration does not enable this by default,"] # [doc = " although it is typically a good idea to."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search panics, callers cannot know whether a match exists or"] # [doc = " not."] # [doc = ""] # [doc = " Use [`Regex::try_search`] if you want to handle these error conditions."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{Match, hybrid::regex::Regex};"] # [doc = ""] # [doc = " let re = Regex::new(\"foo[0-9]+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 3..11)),"] # [doc = "     re.find(&mut cache, \"zzzfoo12345zzz\"),"] # [doc = " );"] # [doc = ""] # [doc = " // Even though a match is found after reading the first byte (`a`),"] # [doc = " // the default leftmost-first match semantics demand that we find the"] # [doc = " // earliest match that prefers earlier parts of the pattern over latter"] # [doc = " // parts."] # [doc = " let re = Regex::new(\"abc|a\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " assert_eq!(Some(Match::must(0, 0..3)), re.find(&mut cache, \"abc\"));"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn find < 'h , I : Into < Input < 'h > > > (& self , cache : & mut Cache , input : I ,) -> Option < Match > { self . try_search (cache , & input . into ()) . unwrap () } # [doc = " Returns an iterator over all non-overlapping leftmost matches in the"] # [doc = " given bytes. If no match exists, then the iterator yields no elements."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This routine panics if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the lazy DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the lazy DFA quitting."] # [doc = " * The configuration of the lazy DFA may also permit it to \"give up\""] # [doc = " on a search if it makes ineffective use of its transition table"] # [doc = " cache. The default configuration does not enable this by default,"] # [doc = " although it is typically a good idea to."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search panics, callers cannot know whether a match exists or"] # [doc = " not."] # [doc = ""] # [doc = " The above conditions also apply to the iterator returned as well. For"] # [doc = " example, if the lazy DFA gives up or quits during a search using this"] # [doc = " method, then a panic will occur during iteration."] # [doc = ""] # [doc = " Use [`Regex::try_search`] with [`util::iter::Searcher`](iter::Searcher)"] # [doc = " if you want to handle these error conditions."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::regex::Regex, Match};"] # [doc = ""] # [doc = " let re = Regex::new(\"foo[0-9]+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let text = \"foo1 foo12 foo123\";"] # [doc = " let matches: Vec<Match> = re.find_iter(&mut cache, text).collect();"] # [doc = " assert_eq!(matches, vec!["] # [doc = "     Match::must(0, 0..4),"] # [doc = "     Match::must(0, 5..10),"] # [doc = "     Match::must(0, 11..17),"] # [doc = " ]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn find_iter < 'r , 'c , 'h , I : Into < Input < 'h > > > (& 'r self , cache : & 'c mut Cache , input : I ,) -> FindMatches < 'r , 'c , 'h > { let it = iter :: Searcher :: new (input . into ()) ; FindMatches { re : self , cache , it } } }
    };
}

impl_275!()