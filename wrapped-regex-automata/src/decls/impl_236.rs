macro_rules! deps {
    () => {
        LazyStateID!();
        StartError!();
        DFA!();
        Unit!();
        StateSaver!();
        NFA!();
        Lazy!();
        LazyRef!();
        StateBuilderNFA!();
        Anchored!();
        StateBuilderEmpty!();
        Cache!();
        Start!();
        CacheError!();
        State!();
    };
}

macro_rules! impl_236 {
    () => {
        deps!();
        impl < 'i , 'c > Lazy < 'i , 'c > { # [doc = " Creates a new 'Lazy' wrapper for a DFA and its corresponding cache."] fn new (dfa : & 'i DFA , cache : & 'c mut Cache) -> Lazy < 'i , 'c > { Lazy { dfa , cache } } # [doc = " Return an immutable view by downgrading a writable cache to a read-only"] # [doc = " cache."] fn as_ref < 'a > (& 'a self) -> LazyRef < 'i , 'a > { LazyRef :: new (self . dfa , self . cache) } # [doc = " This is marked as 'inline(never)' to avoid bloating methods on 'DFA'"] # [doc = " like 'next_state' and 'next_eoi_state' that are called in critical"] # [doc = " areas. The idea is to let the optimizer focus on the other areas of"] # [doc = " those methods as the hot path."] # [doc = ""] # [doc = " Here's an example that justifies 'inline(never)'"] # [doc = ""] # [doc = " ```ignore"] # [doc = " regex-cli find match hybrid \\"] # [doc = "   --cache-capacity 100000000 \\"] # [doc = "   -p '\\pL{100}'"] # [doc = "   all-codepoints-utf8-100x"] # [doc = " ```"] # [doc = ""] # [doc = " Where 'all-codepoints-utf8-100x' is the UTF-8 encoding of every"] # [doc = " codepoint, in sequence, repeated 100 times."] # [doc = ""] # [doc = " With 'inline(never)' hyperfine reports 1.1s per run. With"] # [doc = " 'inline(always)', hyperfine reports 1.23s. So that's a 10% improvement."] # [cold] # [inline (never)] fn cache_next_state (& mut self , mut current : LazyStateID , unit : alphabet :: Unit ,) -> Result < LazyStateID , CacheError > { let stride2 = self . dfa . stride2 () ; let empty_builder = self . get_state_builder () ; let builder = determinize :: next (self . dfa . get_nfa () , self . dfa . get_config () . get_match_kind () , & mut self . cache . sparses , & mut self . cache . stack , & self . cache . states [current . as_usize_untagged () >> stride2] , unit , empty_builder ,) ; let save_state = ! self . as_ref () . state_builder_fits_in_cache (& builder) || self . cache . trans . len () >= LazyStateID :: MAX ; if save_state { self . save_state (current) ; } let next = self . add_builder_state (builder , | sid | sid) ? ; if save_state { current = self . saved_state_id () ; } self . set_transition (current , unit , next) ; Ok (next) } # [doc = " Compute and cache the starting state for the given pattern ID (if"] # [doc = " present) and the starting configuration."] # [doc = ""] # [doc = " This panics if a pattern ID is given and the DFA isn't configured to"] # [doc = " build anchored start states for each pattern."] # [doc = ""] # [doc = " This will never return an unknown lazy state ID."] # [doc = ""] # [doc = " If caching this state would otherwise result in a cache that has been"] # [doc = " cleared too many times, then an error is returned."] # [cold] # [inline (never)] fn cache_start_group (& mut self , anchored : Anchored , start : Start ,) -> Result < LazyStateID , StartError > { let nfa_start_id = match anchored { Anchored :: No => self . dfa . get_nfa () . start_unanchored () , Anchored :: Yes => self . dfa . get_nfa () . start_anchored () , Anchored :: Pattern (pid) => { if ! self . dfa . get_config () . get_starts_for_each_pattern () { return Err (StartError :: unsupported_anchored (anchored)) ; } match self . dfa . get_nfa () . start_pattern (pid) { None => return Ok (self . as_ref () . dead_id ()) , Some (sid) => sid , } } } ; let id = self . cache_start_one (nfa_start_id , start) . map_err (StartError :: cache) ? ; self . set_start_state (anchored , start , id) ; Ok (id) } # [doc = " Compute and cache the starting state for the given NFA state ID and the"] # [doc = " starting configuration. The NFA state ID might be one of the following:"] # [doc = ""] # [doc = " 1) An unanchored start state to match any pattern."] # [doc = " 2) An anchored start state to match any pattern."] # [doc = " 3) An anchored start state for a particular pattern."] # [doc = ""] # [doc = " This will never return an unknown lazy state ID."] # [doc = ""] # [doc = " If caching this state would otherwise result in a cache that has been"] # [doc = " cleared too many times, then an error is returned."] fn cache_start_one (& mut self , nfa_start_id : NFAStateID , start : Start ,) -> Result < LazyStateID , CacheError > { let mut builder_matches = self . get_state_builder () . into_matches () ; determinize :: set_lookbehind_from_start (self . dfa . get_nfa () , & start , & mut builder_matches ,) ; self . cache . sparses . set1 . clear () ; determinize :: epsilon_closure (self . dfa . get_nfa () , nfa_start_id , builder_matches . look_have () , & mut self . cache . stack , & mut self . cache . sparses . set1 ,) ; let mut builder = builder_matches . into_nfa () ; determinize :: add_nfa_states (& self . dfa . get_nfa () , & self . cache . sparses . set1 , & mut builder ,) ; let tag_starts = self . dfa . get_config () . get_specialize_start_states () ; self . add_builder_state (builder , | id | { if tag_starts { id . to_start () } else { id } }) } # [doc = " Either add the given builder state to this cache, or return an ID to an"] # [doc = " equivalent state already in this cache."] # [doc = ""] # [doc = " In the case where no equivalent state exists, the idmap function given"] # [doc = " may be used to transform the identifier allocated. This is useful if"] # [doc = " the caller needs to tag the ID with additional information."] # [doc = ""] # [doc = " This will never return an unknown lazy state ID."] # [doc = ""] # [doc = " If caching this state would otherwise result in a cache that has been"] # [doc = " cleared too many times, then an error is returned."] fn add_builder_state (& mut self , builder : StateBuilderNFA , idmap : impl Fn (LazyStateID) -> LazyStateID ,) -> Result < LazyStateID , CacheError > { if let Some (& cached_id) = self . cache . states_to_id . get (builder . as_bytes ()) { self . put_state_builder (builder) ; return Ok (cached_id) ; } let result = self . add_state (builder . to_state () , idmap) ; self . put_state_builder (builder) ; result } # [doc = " Allocate a new state ID and add the given state to this cache."] # [doc = ""] # [doc = " The idmap function given may be used to transform the identifier"] # [doc = " allocated. This is useful if the caller needs to tag the ID with"] # [doc = " additional information."] # [doc = ""] # [doc = " This will never return an unknown lazy state ID."] # [doc = ""] # [doc = " If caching this state would otherwise result in a cache that has been"] # [doc = " cleared too many times, then an error is returned."] fn add_state (& mut self , state : State , idmap : impl Fn (LazyStateID) -> LazyStateID ,) -> Result < LazyStateID , CacheError > { if ! self . as_ref () . state_fits_in_cache (& state) { self . try_clear_cache () ? ; } let mut id = idmap (self . next_state_id () ?) ; if state . is_match () { id = id . to_match () ; } self . cache . trans . extend (iter :: repeat (self . as_ref () . unknown_id ()) . take (self . dfa . stride ()) ,) ; if ! self . dfa . quitset . is_empty () && ! self . as_ref () . is_sentinel (id) { let quit_id = self . as_ref () . quit_id () ; for b in self . dfa . quitset . iter () { self . set_transition (id , alphabet :: Unit :: u8 (b) , quit_id) ; } } self . cache . memory_usage_state += state . memory_usage () ; self . cache . states . push (state . clone ()) ; self . cache . states_to_id . insert (state , id) ; Ok (id) } # [doc = " Allocate a new state ID."] # [doc = ""] # [doc = " This will never return an unknown lazy state ID."] # [doc = ""] # [doc = " If caching this state would otherwise result in a cache that has been"] # [doc = " cleared too many times, then an error is returned."] fn next_state_id (& mut self) -> Result < LazyStateID , CacheError > { let sid = match LazyStateID :: new (self . cache . trans . len ()) { Ok (sid) => sid , Err (_) => { self . try_clear_cache () ? ; LazyStateID :: new (self . cache . trans . len ()) . unwrap () } } ; Ok (sid) } # [doc = " Attempt to clear the cache used by this lazy DFA."] # [doc = ""] # [doc = " If clearing the cache exceeds the minimum number of required cache"] # [doc = " clearings, then this will return a cache error. In this case,"] # [doc = " callers should bubble this up as the cache can't be used until it is"] # [doc = " reset. Implementations of search should convert this error into a"] # [doc = " [`MatchError::gave_up`]."] # [doc = ""] # [doc = " If 'self.state_saver' is set to save a state, then this state is"] # [doc = " persisted through cache clearing. Otherwise, the cache is returned to"] # [doc = " its state after initialization with two exceptions: its clear count"] # [doc = " is incremented and some of its memory likely has additional capacity."] # [doc = " That is, clearing a cache does _not_ release memory."] # [doc = ""] # [doc = " Otherwise, any lazy state ID generated by the cache prior to resetting"] # [doc = " it is invalid after the reset."] fn try_clear_cache (& mut self) -> Result < () , CacheError > { let c = self . dfa . get_config () ; if let Some (min_count) = c . get_minimum_cache_clear_count () { if self . cache . clear_count >= min_count { if let Some (min_bytes_per) = c . get_minimum_bytes_per_state () { let len = self . cache . search_total_len () ; let min_bytes = min_bytes_per . saturating_mul (self . cache . states . len ()) ; if len == 0 { trace ! ("number of bytes searched is 0, but \
                             a minimum bytes per state searched ({}) is \
                             enabled, maybe Cache::search_update \
                             is not being used?" , min_bytes_per ,) ; } if len < min_bytes { trace ! ("lazy DFA cache has been cleared {} times, \
                             which exceeds the limit of {}, \
                             AND its bytes searched per state is less \
                             than the configured minimum of {}, \
                             therefore lazy DFA is giving up \
                             (bytes searched since cache clear = {}, \
                              number of states = {})" , self . cache . clear_count , min_count , min_bytes_per , len , self . cache . states . len () ,) ; return Err (CacheError :: bad_efficiency ()) ; } else { trace ! ("lazy DFA cache has been cleared {} times, \
                             which exceeds the limit of {}, \
                             AND its bytes searched per state is greater \
                             than the configured minimum of {}, \
                             therefore lazy DFA is continuing! \
                             (bytes searched since cache clear = {}, \
                              number of states = {})" , self . cache . clear_count , min_count , min_bytes_per , len , self . cache . states . len () ,) ; } } else { trace ! ("lazy DFA cache has been cleared {} times, \
                         which exceeds the limit of {}, \
                         since there is no configured bytes per state \
                         minimum, lazy DFA is giving up" , self . cache . clear_count , min_count ,) ; return Err (CacheError :: too_many_cache_clears ()) ; } } } self . clear_cache () ; Ok (()) } # [doc = " Clears _and_ resets the cache. Resetting the cache means that no"] # [doc = " states are persisted and the clear count is reset to 0. No heap memory"] # [doc = " is released."] # [doc = ""] # [doc = " Note that the caller may reset a cache with a different DFA than what"] # [doc = " it was created from. In which case, the cache can now be used with the"] # [doc = " new DFA (and not the old DFA)."] fn reset_cache (& mut self) { self . cache . state_saver = StateSaver :: none () ; self . clear_cache () ; self . cache . sparses . resize (self . dfa . get_nfa () . states () . len ()) ; self . cache . clear_count = 0 ; self . cache . progress = None ; } # [doc = " Clear the cache used by this lazy DFA."] # [doc = ""] # [doc = " If 'self.state_saver' is set to save a state, then this state is"] # [doc = " persisted through cache clearing. Otherwise, the cache is returned to"] # [doc = " its state after initialization with two exceptions: its clear count"] # [doc = " is incremented and some of its memory likely has additional capacity."] # [doc = " That is, clearing a cache does _not_ release memory."] # [doc = ""] # [doc = " Otherwise, any lazy state ID generated by the cache prior to resetting"] # [doc = " it is invalid after the reset."] fn clear_cache (& mut self) { self . cache . trans . clear () ; self . cache . starts . clear () ; self . cache . states . clear () ; self . cache . states_to_id . clear () ; self . cache . memory_usage_state = 0 ; self . cache . clear_count += 1 ; self . cache . bytes_searched = 0 ; if let Some (ref mut progress) = self . cache . progress { progress . start = progress . at ; } trace ! ("lazy DFA cache has been cleared (count: {})" , self . cache . clear_count) ; self . init_cache () ; if let Some ((old_id , state)) = self . cache . state_saver . take_to_save () { assert ! (! self . as_ref () . is_sentinel (old_id) , "cannot save sentinel state") ; let new_id = self . add_state (state , | id | { if old_id . is_start () { id . to_start () } else { id } }) . expect ("adding one state after cache clear must work") ; self . cache . state_saver = StateSaver :: Saved (new_id) ; } } # [doc = " Initialize this cache from emptiness to a place where it can be used"] # [doc = " for search."] # [doc = ""] # [doc = " This is called both at cache creation time and after the cache has been"] # [doc = " cleared."] # [doc = ""] # [doc = " Primarily, this adds the three sentinel states and allocates some"] # [doc = " initial memory."] fn init_cache (& mut self) { let mut starts_len = Start :: len () . checked_mul (2) . unwrap () ; if self . dfa . get_config () . get_starts_for_each_pattern () { starts_len += Start :: len () * self . dfa . pattern_len () ; } self . cache . starts . extend (iter :: repeat (self . as_ref () . unknown_id ()) . take (starts_len)) ; let dead = State :: dead () ; let unk_id = self . add_state (dead . clone () , | id | id . to_unknown ()) . unwrap () ; let dead_id = self . add_state (dead . clone () , | id | id . to_dead ()) . unwrap () ; let quit_id = self . add_state (dead . clone () , | id | id . to_quit ()) . unwrap () ; assert_eq ! (unk_id , self . as_ref () . unknown_id ()) ; assert_eq ! (dead_id , self . as_ref () . dead_id ()) ; assert_eq ! (quit_id , self . as_ref () . quit_id ()) ; self . set_all_transitions (unk_id , unk_id) ; self . set_all_transitions (dead_id , dead_id) ; self . set_all_transitions (quit_id , quit_id) ; self . cache . states_to_id . insert (dead , dead_id) ; } # [doc = " Save the state corresponding to the ID given such that the state"] # [doc = " persists through a cache clearing."] # [doc = ""] # [doc = " While the state may persist, the ID may not. In order to discover the"] # [doc = " new state ID, one must call 'saved_state_id' after a cache clearing."] fn save_state (& mut self , id : LazyStateID) { let state = self . as_ref () . get_cached_state (id) . clone () ; self . cache . state_saver = StateSaver :: ToSave { id , state } ; } # [doc = " Returns the updated lazy state ID for a state that was persisted"] # [doc = " through a cache clearing."] # [doc = ""] # [doc = " It is only correct to call this routine when both a state has been"] # [doc = " saved and the cache has just been cleared. Otherwise, this panics."] fn saved_state_id (& mut self) -> LazyStateID { self . cache . state_saver . take_saved () . expect ("state saver does not have saved state ID") } # [doc = " Set all transitions on the state 'from' to 'to'."] fn set_all_transitions (& mut self , from : LazyStateID , to : LazyStateID) { for unit in self . dfa . classes . representatives (..) { self . set_transition (from , unit , to) ; } } # [doc = " Set the transition on 'from' for 'unit' to 'to'."] # [doc = ""] # [doc = " This panics if either 'from' or 'to' is invalid."] # [doc = ""] # [doc = " All unit values are OK."] fn set_transition (& mut self , from : LazyStateID , unit : alphabet :: Unit , to : LazyStateID ,) { assert ! (self . as_ref () . is_valid (from) , "invalid 'from' id: {from:?}") ; assert ! (self . as_ref () . is_valid (to) , "invalid 'to' id: {to:?}") ; let offset = from . as_usize_untagged () + self . dfa . classes . get_by_unit (unit) ; self . cache . trans [offset] = to ; } # [doc = " Set the start ID for the given pattern ID (if given) and starting"] # [doc = " configuration to the ID given."] # [doc = ""] # [doc = " This panics if 'id' is not valid or if a pattern ID is given and"] # [doc = " 'starts_for_each_pattern' is not enabled."] fn set_start_state (& mut self , anchored : Anchored , start : Start , id : LazyStateID ,) { assert ! (self . as_ref () . is_valid (id)) ; let start_index = start . as_usize () ; let index = match anchored { Anchored :: No => start_index , Anchored :: Yes => Start :: len () + start_index , Anchored :: Pattern (pid) => { assert ! (self . dfa . get_config () . get_starts_for_each_pattern () , "attempted to search for a specific pattern \
                     without enabling starts_for_each_pattern" ,) ; let pid = pid . as_usize () ; (2 * Start :: len ()) + (Start :: len () * pid) + start_index } } ; self . cache . starts [index] = id ; } # [doc = " Returns a state builder from this DFA that might have existing"] # [doc = " capacity. This helps avoid allocs in cases where a state is built that"] # [doc = " turns out to already be cached."] # [doc = ""] # [doc = " Callers must put the state builder back with 'put_state_builder',"] # [doc = " otherwise the allocation reuse won't work."] fn get_state_builder (& mut self) -> StateBuilderEmpty { core :: mem :: replace (& mut self . cache . scratch_state_builder , StateBuilderEmpty :: new () ,) } # [doc = " Puts the given state builder back into this DFA for reuse."] # [doc = ""] # [doc = " Note that building a 'State' from a builder always creates a new alloc,"] # [doc = " so callers should always put the builder back."] fn put_state_builder (& mut self , builder : StateBuilderNFA) { let _ = core :: mem :: replace (& mut self . cache . scratch_state_builder , builder . clear () ,) ; } }
    };
}

impl_236!()