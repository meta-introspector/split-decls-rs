macro_rules! deps {
    () => {
        PatternID!();
        State!();
        Config!();
        LazyRef!();
        DFA!();
        MatchKind!();
        Unit!();
        Input!();
        StartError!();
        MatchError!();
        Anchored!();
        Start!();
        Cache!();
        CacheError!();
        Lazy!();
        Transitions!();
        LazyStateID!();
    };
}

macro_rules! impl_228 {
    () => {
        deps!();
        impl DFA { # [doc = " Transitions from the current state to the next state, given the next"] # [doc = " byte of input."] # [doc = ""] # [doc = " The given cache is used to either reuse pre-computed state"] # [doc = " transitions, or to store this newly computed transition for future"] # [doc = " reuse. Thus, this routine guarantees that it will never return a state"] # [doc = " ID that has an \"unknown\" tag."] # [doc = ""] # [doc = " # State identifier validity"] # [doc = ""] # [doc = " The only valid value for `current` is the lazy state ID returned"] # [doc = " by the most recent call to `next_state`, `next_state_untagged`,"] # [doc = " `next_state_untagged_unchecked`, `start_state_forward` or"] # [doc = " `state_state_reverse` for the given `cache`. Any state ID returned from"] # [doc = " prior calls to these routines (with the same `cache`) is considered"] # [doc = " invalid (even if it gives an appearance of working). State IDs returned"] # [doc = " from _any_ prior call for different `cache` values are also always"] # [doc = " invalid."] # [doc = ""] # [doc = " The returned ID is always a valid ID when `current` refers to a valid"] # [doc = " ID. Moreover, this routine is defined for all possible values of"] # [doc = " `input`."] # [doc = ""] # [doc = " These validity rules are not checked, even in debug mode. Callers are"] # [doc = " required to uphold these rules themselves."] # [doc = ""] # [doc = " Violating these state ID validity rules will not sacrifice memory"] # [doc = " safety, but _may_ produce an incorrect result or a panic."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the given ID does not refer to a valid state, then this routine"] # [doc = " may panic but it also may not panic and instead return an invalid or"] # [doc = " incorrect ID."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows a simplistic example for walking a lazy DFA for a given"] # [doc = " haystack by using the `next_state` method."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::dfa::DFA, Input};"] # [doc = ""] # [doc = " let dfa = DFA::new(r\"[a-z]+r\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = " let haystack = \"bar\".as_bytes();"] # [doc = ""] # [doc = " // The start state is determined by inspecting the position and the"] # [doc = " // initial bytes of the haystack."] # [doc = " let mut sid = dfa.start_state_forward("] # [doc = "     &mut cache, &Input::new(haystack),"] # [doc = " )?;"] # [doc = " // Walk all the bytes in the haystack."] # [doc = " for &b in haystack {"] # [doc = "     sid = dfa.next_state(&mut cache, sid, b)?;"] # [doc = " }"] # [doc = " // Matches are always delayed by 1 byte, so we must explicitly walk the"] # [doc = " // special \"EOI\" transition at the end of the search."] # [doc = " sid = dfa.next_eoi_state(&mut cache, sid)?;"] # [doc = " assert!(sid.is_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn next_state (& self , cache : & mut Cache , current : LazyStateID , input : u8 ,) -> Result < LazyStateID , CacheError > { let class = usize :: from (self . classes . get (input)) ; let offset = current . as_usize_untagged () + class ; let sid = cache . trans [offset] ; if ! sid . is_unknown () { return Ok (sid) ; } let unit = alphabet :: Unit :: u8 (input) ; Lazy :: new (self , cache) . cache_next_state (current , unit) } # [doc = " Transitions from the current state to the next state, given the next"] # [doc = " byte of input and a state ID that is not tagged."] # [doc = ""] # [doc = " The only reason to use this routine is performance. In particular, the"] # [doc = " `next_state` method needs to do some additional checks, among them is"] # [doc = " to account for identifiers to states that are not yet computed. In"] # [doc = " such a case, the transition is computed on the fly. However, if it is"] # [doc = " known that the `current` state ID is untagged, then these checks can be"] # [doc = " omitted."] # [doc = ""] # [doc = " Since this routine does not compute states on the fly, it does not"] # [doc = " modify the cache and thus cannot return an error. Consequently, `cache`"] # [doc = " does not need to be mutable and it is possible for this routine to"] # [doc = " return a state ID corresponding to the special \"unknown\" state. In"] # [doc = " this case, it is the caller's responsibility to use the prior state"] # [doc = " ID and `input` with `next_state` in order to force the computation of"] # [doc = " the unknown transition. Otherwise, trying to use the \"unknown\" state"] # [doc = " ID will just result in transitioning back to itself, and thus never"] # [doc = " terminating. (This is technically a special exemption to the state ID"] # [doc = " validity rules, but is permissible since this routine is guaranteed to"] # [doc = " never mutate the given `cache`, and thus the identifier is guaranteed"] # [doc = " to remain valid.)"] # [doc = ""] # [doc = " See [`LazyStateID`] for more details on what it means for a state ID"] # [doc = " to be tagged. Also, see"] # [doc = " [`next_state_untagged_unchecked`](DFA::next_state_untagged_unchecked)"] # [doc = " for this same idea, but with bounds checks forcefully elided."] # [doc = ""] # [doc = " # State identifier validity"] # [doc = ""] # [doc = " The only valid value for `current` is an **untagged** lazy"] # [doc = " state ID returned by the most recent call to `next_state`,"] # [doc = " `next_state_untagged`, `next_state_untagged_unchecked`,"] # [doc = " `start_state_forward` or `state_state_reverse` for the given `cache`."] # [doc = " Any state ID returned from prior calls to these routines (with the"] # [doc = " same `cache`) is considered invalid (even if it gives an appearance"] # [doc = " of working). State IDs returned from _any_ prior call for different"] # [doc = " `cache` values are also always invalid."] # [doc = ""] # [doc = " The returned ID is always a valid ID when `current` refers to a valid"] # [doc = " ID, although it may be tagged. Moreover, this routine is defined for"] # [doc = " all possible values of `input`."] # [doc = ""] # [doc = " Not all validity rules are checked, even in debug mode. Callers are"] # [doc = " required to uphold these rules themselves."] # [doc = ""] # [doc = " Violating these state ID validity rules will not sacrifice memory"] # [doc = " safety, but _may_ produce an incorrect result or a panic."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the given ID does not refer to a valid state, then this routine"] # [doc = " may panic but it also may not panic and instead return an invalid or"] # [doc = " incorrect ID."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows a simplistic example for walking a lazy DFA for a given"] # [doc = " haystack by using the `next_state_untagged` method where possible."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::dfa::DFA, Input};"] # [doc = ""] # [doc = " let dfa = DFA::new(r\"[a-z]+r\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = " let haystack = \"bar\".as_bytes();"] # [doc = ""] # [doc = " // The start state is determined by inspecting the position and the"] # [doc = " // initial bytes of the haystack."] # [doc = " let mut sid = dfa.start_state_forward("] # [doc = "     &mut cache, &Input::new(haystack),"] # [doc = " )?;"] # [doc = " // Walk all the bytes in the haystack."] # [doc = " let mut at = 0;"] # [doc = " while at < haystack.len() {"] # [doc = "     if sid.is_tagged() {"] # [doc = "         sid = dfa.next_state(&mut cache, sid, haystack[at])?;"] # [doc = "     } else {"] # [doc = "         let mut prev_sid = sid;"] # [doc = "         // We attempt to chew through as much as we can while moving"] # [doc = "         // through untagged state IDs. Thus, the transition function"] # [doc = "         // does less work on average per byte. (Unrolling this loop"] # [doc = "         // may help even more.)"] # [doc = "         while at < haystack.len() {"] # [doc = "             prev_sid = sid;"] # [doc = "             sid = dfa.next_state_untagged("] # [doc = "                 &mut cache, sid, haystack[at],"] # [doc = "             );"] # [doc = "             at += 1;"] # [doc = "             if sid.is_tagged() {"] # [doc = "                 break;"] # [doc = "             }"] # [doc = "         }"] # [doc = "         // We must ensure that we never proceed to the next iteration"] # [doc = "         // with an unknown state ID. If we don't account for this"] # [doc = "         // case, then search isn't guaranteed to terminate since all"] # [doc = "         // transitions on unknown states loop back to itself."] # [doc = "         if sid.is_unknown() {"] # [doc = "             sid = dfa.next_state("] # [doc = "                 &mut cache, prev_sid, haystack[at - 1],"] # [doc = "             )?;"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " // Matches are always delayed by 1 byte, so we must explicitly walk the"] # [doc = " // special \"EOI\" transition at the end of the search."] # [doc = " sid = dfa.next_eoi_state(&mut cache, sid)?;"] # [doc = " assert!(sid.is_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn next_state_untagged (& self , cache : & Cache , current : LazyStateID , input : u8 ,) -> LazyStateID { debug_assert ! (! current . is_tagged ()) ; let class = usize :: from (self . classes . get (input)) ; let offset = current . as_usize_unchecked () + class ; cache . trans [offset] } # [doc = " Transitions from the current state to the next state, eliding bounds"] # [doc = " checks, given the next byte of input and a state ID that is not tagged."] # [doc = ""] # [doc = " The only reason to use this routine is performance. In particular, the"] # [doc = " `next_state` method needs to do some additional checks, among them is"] # [doc = " to account for identifiers to states that are not yet computed. In"] # [doc = " such a case, the transition is computed on the fly. However, if it is"] # [doc = " known that the `current` state ID is untagged, then these checks can be"] # [doc = " omitted."] # [doc = ""] # [doc = " Since this routine does not compute states on the fly, it does not"] # [doc = " modify the cache and thus cannot return an error. Consequently, `cache`"] # [doc = " does not need to be mutable and it is possible for this routine to"] # [doc = " return a state ID corresponding to the special \"unknown\" state. In"] # [doc = " this case, it is the caller's responsibility to use the prior state"] # [doc = " ID and `input` with `next_state` in order to force the computation of"] # [doc = " the unknown transition. Otherwise, trying to use the \"unknown\" state"] # [doc = " ID will just result in transitioning back to itself, and thus never"] # [doc = " terminating. (This is technically a special exemption to the state ID"] # [doc = " validity rules, but is permissible since this routine is guaranteed to"] # [doc = " never mutate the given `cache`, and thus the identifier is guaranteed"] # [doc = " to remain valid.)"] # [doc = ""] # [doc = " See [`LazyStateID`] for more details on what it means for a state ID"] # [doc = " to be tagged. Also, see"] # [doc = " [`next_state_untagged`](DFA::next_state_untagged)"] # [doc = " for this same idea, but with memory safety guaranteed by retaining"] # [doc = " bounds checks."] # [doc = ""] # [doc = " # State identifier validity"] # [doc = ""] # [doc = " The only valid value for `current` is an **untagged** lazy"] # [doc = " state ID returned by the most recent call to `next_state`,"] # [doc = " `next_state_untagged`, `next_state_untagged_unchecked`,"] # [doc = " `start_state_forward` or `state_state_reverse` for the given `cache`."] # [doc = " Any state ID returned from prior calls to these routines (with the"] # [doc = " same `cache`) is considered invalid (even if it gives an appearance"] # [doc = " of working). State IDs returned from _any_ prior call for different"] # [doc = " `cache` values are also always invalid."] # [doc = ""] # [doc = " The returned ID is always a valid ID when `current` refers to a valid"] # [doc = " ID, although it may be tagged. Moreover, this routine is defined for"] # [doc = " all possible values of `input`."] # [doc = ""] # [doc = " Not all validity rules are checked, even in debug mode. Callers are"] # [doc = " required to uphold these rules themselves."] # [doc = ""] # [doc = " Violating these state ID validity rules will not sacrifice memory"] # [doc = " safety, but _may_ produce an incorrect result or a panic."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Callers of this method must guarantee that `current` refers to a valid"] # [doc = " state ID according to the rules described above. If `current` is not a"] # [doc = " valid state ID for this automaton, then calling this routine may result"] # [doc = " in undefined behavior."] # [doc = ""] # [doc = " If `current` is valid, then the ID returned is valid for all possible"] # [doc = " values of `input`."] # [inline] pub unsafe fn next_state_untagged_unchecked (& self , cache : & Cache , current : LazyStateID , input : u8 ,) -> LazyStateID { debug_assert ! (! current . is_tagged ()) ; let class = usize :: from (self . classes . get (input)) ; let offset = current . as_usize_unchecked () + class ; * cache . trans . get_unchecked (offset) } # [doc = " Transitions from the current state to the next state for the special"] # [doc = " EOI symbol."] # [doc = ""] # [doc = " The given cache is used to either reuse pre-computed state"] # [doc = " transitions, or to store this newly computed transition for future"] # [doc = " reuse. Thus, this routine guarantees that it will never return a state"] # [doc = " ID that has an \"unknown\" tag."] # [doc = ""] # [doc = " This routine must be called at the end of every search in a correct"] # [doc = " implementation of search. Namely, lazy DFAs in this crate delay matches"] # [doc = " by one byte in order to support look-around operators. Thus, after"] # [doc = " reaching the end of a haystack, a search implementation must follow one"] # [doc = " last EOI transition."] # [doc = ""] # [doc = " It is best to think of EOI as an additional symbol in the alphabet of a"] # [doc = " DFA that is distinct from every other symbol. That is, the alphabet of"] # [doc = " lazy DFAs in this crate has a logical size of 257 instead of 256, where"] # [doc = " 256 corresponds to every possible inhabitant of `u8`. (In practice, the"] # [doc = " physical alphabet size may be smaller because of alphabet compression"] # [doc = " via equivalence classes, but EOI is always represented somehow in the"] # [doc = " alphabet.)"] # [doc = ""] # [doc = " # State identifier validity"] # [doc = ""] # [doc = " The only valid value for `current` is the lazy state ID returned"] # [doc = " by the most recent call to `next_state`, `next_state_untagged`,"] # [doc = " `next_state_untagged_unchecked`, `start_state_forward` or"] # [doc = " `state_state_reverse` for the given `cache`. Any state ID returned from"] # [doc = " prior calls to these routines (with the same `cache`) is considered"] # [doc = " invalid (even if it gives an appearance of working). State IDs returned"] # [doc = " from _any_ prior call for different `cache` values are also always"] # [doc = " invalid."] # [doc = ""] # [doc = " The returned ID is always a valid ID when `current` refers to a valid"] # [doc = " ID."] # [doc = ""] # [doc = " These validity rules are not checked, even in debug mode. Callers are"] # [doc = " required to uphold these rules themselves."] # [doc = ""] # [doc = " Violating these state ID validity rules will not sacrifice memory"] # [doc = " safety, but _may_ produce an incorrect result or a panic."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the given ID does not refer to a valid state, then this routine"] # [doc = " may panic but it also may not panic and instead return an invalid or"] # [doc = " incorrect ID."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows a simplistic example for walking a DFA for a given haystack,"] # [doc = " and then finishing the search with the final EOI transition."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{hybrid::dfa::DFA, Input};"] # [doc = ""] # [doc = " let dfa = DFA::new(r\"[a-z]+r\")?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = " let haystack = \"bar\".as_bytes();"] # [doc = ""] # [doc = " // The start state is determined by inspecting the position and the"] # [doc = " // initial bytes of the haystack."] # [doc = " let mut sid = dfa.start_state_forward("] # [doc = "     &mut cache, &Input::new(haystack),"] # [doc = " )?;"] # [doc = " // Walk all the bytes in the haystack."] # [doc = " for &b in haystack {"] # [doc = "     sid = dfa.next_state(&mut cache, sid, b)?;"] # [doc = " }"] # [doc = " // Matches are always delayed by 1 byte, so we must explicitly walk"] # [doc = " // the special \"EOI\" transition at the end of the search. Without this"] # [doc = " // final transition, the assert below will fail since the DFA will not"] # [doc = " // have entered a match state yet!"] # [doc = " sid = dfa.next_eoi_state(&mut cache, sid)?;"] # [doc = " assert!(sid.is_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn next_eoi_state (& self , cache : & mut Cache , current : LazyStateID ,) -> Result < LazyStateID , CacheError > { let eoi = self . classes . eoi () . as_usize () ; let offset = current . as_usize_untagged () + eoi ; let sid = cache . trans [offset] ; if ! sid . is_unknown () { return Ok (sid) ; } let unit = self . classes . eoi () ; Lazy :: new (self , cache) . cache_next_state (current , unit) } # [doc = " Return the ID of the start state for this lazy DFA for the given"] # [doc = " starting configuration."] # [doc = ""] # [doc = " Unlike typical DFA implementations, the start state for DFAs in this"] # [doc = " crate is dependent on a few different factors:"] # [doc = ""] # [doc = " * The [`Anchored`] mode of the search. Unanchored, anchored and"] # [doc = " anchored searches for a specific [`PatternID`] all use different start"] # [doc = " states."] # [doc = " * Whether a \"look-behind\" byte exists. For example, the `^` anchor"] # [doc = " matches if and only if there is no look-behind byte."] # [doc = " * The specific value of that look-behind byte. For example, a `(?m:^)`"] # [doc = " assertion only matches when there is either no look-behind byte, or"] # [doc = " when the look-behind byte is a line terminator."] # [doc = ""] # [doc = " The [starting configuration](start::Config) provides the above"] # [doc = " information."] # [doc = ""] # [doc = " This routine can be used for either forward or reverse searches."] # [doc = " Although, as a convenience, if you have an [`Input`], then it"] # [doc = " may be more succinct to use [`DFA::start_state_forward`] or"] # [doc = " [`DFA::start_state_reverse`]. Note, for example, that the convenience"] # [doc = " routines return a [`MatchError`] on failure where as this routine"] # [doc = " returns a [`StartError`]."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This may return a [`StartError`] if the search needs to give up when"] # [doc = " determining the start state (for example, if it sees a \"quit\" byte"] # [doc = " or if the cache has become inefficient). This can also return an"] # [doc = " error if the given configuration contains an unsupported [`Anchored`]"] # [doc = " configuration."] # [cfg_attr (feature = "perf-inline" , inline (always))] pub fn start_state (& self , cache : & mut Cache , config : & start :: Config ,) -> Result < LazyStateID , StartError > { let lazy = LazyRef :: new (self , cache) ; let anchored = config . get_anchored () ; let start = match config . get_look_behind () { None => Start :: Text , Some (byte) => { if ! self . quitset . is_empty () && self . quitset . contains (byte) { return Err (StartError :: quit (byte)) ; } self . start_map . get (byte) } } ; let start_id = lazy . get_cached_start_id (anchored , start) ? ; if ! start_id . is_unknown () { return Ok (start_id) ; } Lazy :: new (self , cache) . cache_start_group (anchored , start) } # [doc = " Return the ID of the start state for this lazy DFA when executing a"] # [doc = " forward search."] # [doc = ""] # [doc = " This is a convenience routine for calling [`DFA::start_state`] that"] # [doc = " converts the given [`Input`] to a [start configuration](start::Config)."] # [doc = " Additionally, if an error occurs, it is converted from a [`StartError`]"] # [doc = " to a [`MatchError`] using the offset information in the given"] # [doc = " [`Input`]."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This may return a [`MatchError`] if the search needs to give up when"] # [doc = " determining the start state (for example, if it sees a \"quit\" byte or"] # [doc = " if the cache has become inefficient). This can also return an error if"] # [doc = " the given `Input` contains an unsupported [`Anchored`] configuration."] # [cfg_attr (feature = "perf-inline" , inline (always))] pub fn start_state_forward (& self , cache : & mut Cache , input : & Input < '_ > ,) -> Result < LazyStateID , MatchError > { let config = start :: Config :: from_input_forward (input) ; self . start_state (cache , & config) . map_err (| err | match err { StartError :: Cache { .. } => MatchError :: gave_up (input . start ()) , StartError :: Quit { byte } => { let offset = input . start () . checked_sub (1) . expect ("no quit in start without look-behind") ; MatchError :: quit (byte , offset) } StartError :: UnsupportedAnchored { mode } => { MatchError :: unsupported_anchored (mode) } }) } # [doc = " Return the ID of the start state for this lazy DFA when executing a"] # [doc = " reverse search."] # [doc = ""] # [doc = " This is a convenience routine for calling [`DFA::start_state`] that"] # [doc = " converts the given [`Input`] to a [start configuration](start::Config)."] # [doc = " Additionally, if an error occurs, it is converted from a [`StartError`]"] # [doc = " to a [`MatchError`] using the offset information in the given"] # [doc = " [`Input`]."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This may return a [`MatchError`] if the search needs to give up when"] # [doc = " determining the start state (for example, if it sees a \"quit\" byte or"] # [doc = " if the cache has become inefficient). This can also return an error if"] # [doc = " the given `Input` contains an unsupported [`Anchored`] configuration."] # [cfg_attr (feature = "perf-inline" , inline (always))] pub fn start_state_reverse (& self , cache : & mut Cache , input : & Input < '_ > ,) -> Result < LazyStateID , MatchError > { let config = start :: Config :: from_input_reverse (input) ; self . start_state (cache , & config) . map_err (| err | match err { StartError :: Cache { .. } => MatchError :: gave_up (input . end ()) , StartError :: Quit { byte } => { let offset = input . end () ; MatchError :: quit (byte , offset) } StartError :: UnsupportedAnchored { mode } => { MatchError :: unsupported_anchored (mode) } }) } # [doc = " Returns the total number of patterns that match in this state."] # [doc = ""] # [doc = " If the lazy DFA was compiled with one pattern, then this must"] # [doc = " necessarily always return `1` for all match states."] # [doc = ""] # [doc = " A lazy DFA guarantees that [`DFA::match_pattern`] can be called with"] # [doc = " indices up to (but not including) the length returned by this routine"] # [doc = " without panicking."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the given state is not a match state, then this may either panic"] # [doc = " or return an incorrect result."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows a simple instance of implementing overlapping"] # [doc = " matches. In particular, it shows not only how to determine how many"] # [doc = " patterns have matched in a particular state, but also how to access"] # [doc = " which specific patterns have matched."] # [doc = ""] # [doc = " Notice that we must use [`MatchKind::All`] when building the DFA. If we"] # [doc = " used [`MatchKind::LeftmostFirst`] instead, then the DFA would not be"] # [doc = " constructed in a way that supports overlapping matches. (It would only"] # [doc = " report a single pattern that matches at any particular point in time.)"] # [doc = ""] # [doc = " Another thing to take note of is the patterns used and the order in"] # [doc = " which the pattern IDs are reported. In the example below, pattern `3`"] # [doc = " is yielded first. Why? Because it corresponds to the match that"] # [doc = " appears first. Namely, the `@` symbol is part of `\\S+` but not part"] # [doc = " of any of the other patterns. Since the `\\S+` pattern has a match that"] # [doc = " starts to the left of any other pattern, its ID is returned before any"] # [doc = " other."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{hybrid::dfa::DFA, Input, MatchKind};"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .configure(DFA::config().match_kind(MatchKind::All))"] # [doc = "     .build_many(&["] # [doc = "         r\"\\w+\", r\"[a-z]+\", r\"[A-Z]+\", r\"\\S+\","] # [doc = "     ])?;"] # [doc = " let mut cache = dfa.create_cache();"] # [doc = " let haystack = \"@bar\".as_bytes();"] # [doc = ""] # [doc = " // The start state is determined by inspecting the position and the"] # [doc = " // initial bytes of the haystack."] # [doc = " let mut sid = dfa.start_state_forward("] # [doc = "     &mut cache, &Input::new(haystack),"] # [doc = " )?;"] # [doc = " // Walk all the bytes in the haystack."] # [doc = " for &b in haystack {"] # [doc = "     sid = dfa.next_state(&mut cache, sid, b)?;"] # [doc = " }"] # [doc = " sid = dfa.next_eoi_state(&mut cache, sid)?;"] # [doc = ""] # [doc = " assert!(sid.is_match());"] # [doc = " assert_eq!(dfa.match_len(&mut cache, sid), 3);"] # [doc = " // The following calls are guaranteed to not panic since `match_len`"] # [doc = " // returned `3` above."] # [doc = " assert_eq!(dfa.match_pattern(&mut cache, sid, 0).as_usize(), 3);"] # [doc = " assert_eq!(dfa.match_pattern(&mut cache, sid, 1).as_usize(), 0);"] # [doc = " assert_eq!(dfa.match_pattern(&mut cache, sid, 2).as_usize(), 1);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn match_len (& self , cache : & Cache , id : LazyStateID) -> usize { assert ! (id . is_match ()) ; LazyRef :: new (self , cache) . get_cached_state (id) . match_len () } # [doc = " Returns the pattern ID corresponding to the given match index in the"] # [doc = " given state."] # [doc = ""] # [doc = " See [`DFA::match_len`] for an example of how to use this method"] # [doc = " correctly. Note that if you know your lazy DFA is configured with a"] # [doc = " single pattern, then this routine is never necessary since it will"] # [doc = " always return a pattern ID of `0` for an index of `0` when `id`"] # [doc = " corresponds to a match state."] # [doc = ""] # [doc = " Typically, this routine is used when implementing an overlapping"] # [doc = " search, as the example for `DFA::match_len` does."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the state ID is not a match state or if the match index is out"] # [doc = " of bounds for the given state, then this routine may either panic"] # [doc = " or produce an incorrect result. If the state ID is correct and the"] # [doc = " match index is correct, then this routine always produces a valid"] # [doc = " `PatternID`."] # [inline] pub fn match_pattern (& self , cache : & Cache , id : LazyStateID , match_index : usize ,) -> PatternID { if self . pattern_len () == 1 { return PatternID :: ZERO ; } LazyRef :: new (self , cache) . get_cached_state (id) . match_pattern (match_index) } }
    };
}

impl_228!()