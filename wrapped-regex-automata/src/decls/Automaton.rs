macro_rules! deps {
    () => {
        MatchError!();
        PatternID!();
        HalfMatch!();
        OverlappingState!();
        StartError!();
        NFA!();
        Prefilter!();
        Transitions!();
        Input!();
        Anchored!();
        DFA!();
        StateID!();
        Config!();
        PatternSet!();
        MatchKind!();
    };
}

macro_rules! Automaton {
    () => {
        deps!();
        # [doc = " A trait describing the interface of a deterministic finite automaton (DFA)."] # [doc = ""] # [doc = " The complexity of this trait probably means that it's unlikely for others"] # [doc = " to implement it. The primary purpose of the trait is to provide for a way"] # [doc = " of abstracting over different types of DFAs. In this crate, that means"] # [doc = " dense DFAs and sparse DFAs. (Dense DFAs are fast but memory hungry, where"] # [doc = " as sparse DFAs are slower but come with a smaller memory footprint. But"] # [doc = " they otherwise provide exactly equivalent expressive power.) For example, a"] # [doc = " [`dfa::regex::Regex`](crate::dfa::regex::Regex) is generic over this trait."] # [doc = ""] # [doc = " Normally, a DFA's execution model is very simple. You might have a single"] # [doc = " start state, zero or more final or \"match\" states and a function that"] # [doc = " transitions from one state to the next given the next byte of input."] # [doc = " Unfortunately, the interface described by this trait is significantly"] # [doc = " more complicated than this. The complexity has a number of different"] # [doc = " reasons, mostly motivated by performance, functionality or space savings:"] # [doc = ""] # [doc = " * A DFA can search for multiple patterns simultaneously. This"] # [doc = " means extra information is returned when a match occurs. Namely,"] # [doc = " a match is not just an offset, but an offset plus a pattern ID."] # [doc = " [`Automaton::pattern_len`] returns the number of patterns compiled into"] # [doc = " the DFA, [`Automaton::match_len`] returns the total number of patterns"] # [doc = " that match in a particular state and [`Automaton::match_pattern`] permits"] # [doc = " iterating over the patterns that match in a particular state."] # [doc = " * A DFA can have multiple start states, and the choice of which start"] # [doc = " state to use depends on the content of the string being searched and"] # [doc = " position of the search, as well as whether the search is an anchored"] # [doc = " search for a specific pattern in the DFA. Moreover, computing the start"] # [doc = " state also depends on whether you're doing a forward or a reverse search."] # [doc = " [`Automaton::start_state_forward`] and [`Automaton::start_state_reverse`]"] # [doc = " are used to compute the start state for forward and reverse searches,"] # [doc = " respectively."] # [doc = " * All matches are delayed by one byte to support things like `$` and `\\b`"] # [doc = " at the end of a pattern. Therefore, every use of a DFA is required to use"] # [doc = " [`Automaton::next_eoi_state`]"] # [doc = " at the end of the search to compute the final transition."] # [doc = " * For optimization reasons, some states are treated specially. Every"] # [doc = " state is either special or not, which can be determined via the"] # [doc = " [`Automaton::is_special_state`] method. If it's special, then the state"] # [doc = " must be at least one of a few possible types of states. (Note that some"] # [doc = " types can overlap, for example, a match state can also be an accel state."] # [doc = " But some types can't. If a state is a dead state, then it can never be any"] # [doc = " other type of state.) Those types are:"] # [doc = "     * A dead state. A dead state means the DFA will never enter a match"] # [doc = "     state. This can be queried via the [`Automaton::is_dead_state`] method."] # [doc = "     * A quit state. A quit state occurs if the DFA had to stop the search"] # [doc = "     prematurely for some reason. This can be queried via the"] # [doc = "     [`Automaton::is_quit_state`] method."] # [doc = "     * A match state. A match state occurs when a match is found. When a DFA"] # [doc = "     enters a match state, the search may stop immediately (when looking"] # [doc = "     for the earliest match), or it may continue to find the leftmost-first"] # [doc = "     match. This can be queried via the [`Automaton::is_match_state`]"] # [doc = "     method."] # [doc = "     * A start state. A start state is where a search begins. For every"] # [doc = "     search, there is exactly one start state that is used, however, a"] # [doc = "     DFA may contain many start states. When the search is in a start"] # [doc = "     state, it may use a prefilter to quickly skip to candidate matches"] # [doc = "     without executing the DFA on every byte. This can be queried via the"] # [doc = "     [`Automaton::is_start_state`] method."] # [doc = "     * An accel state. An accel state is a state that is accelerated."] # [doc = "     That is, it is a state where _most_ of its transitions loop back to"] # [doc = "     itself and only a small number of transitions lead to other states."] # [doc = "     This kind of state is said to be accelerated because a search routine"] # [doc = "     can quickly look for the bytes leading out of the state instead of"] # [doc = "     continuing to execute the DFA on each byte. This can be queried via the"] # [doc = "     [`Automaton::is_accel_state`] method. And the bytes that lead out of"] # [doc = "     the state can be queried via the [`Automaton::accelerator`] method."] # [doc = ""] # [doc = " There are a number of provided methods on this trait that implement"] # [doc = " efficient searching (for forwards and backwards) with a DFA using"] # [doc = " all of the above features of this trait. In particular, given the"] # [doc = " complexity of all these features, implementing a search routine in"] # [doc = " this trait can be a little subtle. With that said, it is possible to"] # [doc = " somewhat simplify the search routine. For example, handling accelerated"] # [doc = " states is strictly optional, since it is always correct to assume that"] # [doc = " `Automaton::is_accel_state` returns false. However, one complex part of"] # [doc = " writing a search routine using this trait is handling the 1-byte delay of a"] # [doc = " match. That is not optional."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This trait is not safe to implement so that code may rely on the"] # [doc = " correctness of implementations of this trait to avoid undefined behavior."] # [doc = " The primary correctness guarantees are:"] # [doc = ""] # [doc = " * `Automaton::start_state` always returns a valid state ID or an error or"] # [doc = " panics."] # [doc = " * `Automaton::next_state`, when given a valid state ID, always returns"] # [doc = " a valid state ID for all values of `anchored` and `byte`, or otherwise"] # [doc = " panics."] # [doc = ""] # [doc = " In general, the rest of the methods on `Automaton` need to uphold their"] # [doc = " contracts as well. For example, `Automaton::is_dead` should only returns"] # [doc = " true if the given state ID is actually a dead state."] pub unsafe trait Automaton { # [doc = " Transitions from the current state to the next state, given the next"] # [doc = " byte of input."] # [doc = ""] # [doc = " Implementations must guarantee that the returned ID is always a valid"] # [doc = " ID when `current` refers to a valid ID. Moreover, the transition"] # [doc = " function must be defined for all possible values of `input`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the given ID does not refer to a valid state, then this routine"] # [doc = " may panic but it also may not panic and instead return an invalid ID."] # [doc = " However, if the caller provides an invalid ID then this must never"] # [doc = " sacrifice memory safety."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows a simplistic example for walking a DFA for a given haystack"] # [doc = " by using the `next_state` method."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, dense}, Input};"] # [doc = ""] # [doc = " let dfa = dense::DFA::new(r\"[a-z]+r\")?;"] # [doc = " let haystack = \"bar\".as_bytes();"] # [doc = ""] # [doc = " // The start state is determined by inspecting the position and the"] # [doc = " // initial bytes of the haystack."] # [doc = " let mut state = dfa.start_state_forward(&Input::new(haystack))?;"] # [doc = " // Walk all the bytes in the haystack."] # [doc = " for &b in haystack {"] # [doc = "     state = dfa.next_state(state, b);"] # [doc = " }"] # [doc = " // Matches are always delayed by 1 byte, so we must explicitly walk the"] # [doc = " // special \"EOI\" transition at the end of the search."] # [doc = " state = dfa.next_eoi_state(state);"] # [doc = " assert!(dfa.is_match_state(state));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] fn next_state (& self , current : StateID , input : u8) -> StateID ; # [doc = " Transitions from the current state to the next state, given the next"] # [doc = " byte of input."] # [doc = ""] # [doc = " Unlike [`Automaton::next_state`], implementations may implement this"] # [doc = " more efficiently by assuming that the `current` state ID is valid."] # [doc = " Typically, this manifests by eliding bounds checks."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Callers of this method must guarantee that `current` refers to a valid"] # [doc = " state ID. If `current` is not a valid state ID for this automaton, then"] # [doc = " calling this routine may result in undefined behavior."] # [doc = ""] # [doc = " If `current` is valid, then implementations must guarantee that the ID"] # [doc = " returned is valid for all possible values of `input`."] unsafe fn next_state_unchecked (& self , current : StateID , input : u8 ,) -> StateID ; # [doc = " Transitions from the current state to the next state for the special"] # [doc = " EOI symbol."] # [doc = ""] # [doc = " Implementations must guarantee that the returned ID is always a valid"] # [doc = " ID when `current` refers to a valid ID."] # [doc = ""] # [doc = " This routine must be called at the end of every search in a correct"] # [doc = " implementation of search. Namely, DFAs in this crate delay matches"] # [doc = " by one byte in order to support look-around operators. Thus, after"] # [doc = " reaching the end of a haystack, a search implementation must follow one"] # [doc = " last EOI transition."] # [doc = ""] # [doc = " It is best to think of EOI as an additional symbol in the alphabet of"] # [doc = " a DFA that is distinct from every other symbol. That is, the alphabet"] # [doc = " of DFAs in this crate has a logical size of 257 instead of 256, where"] # [doc = " 256 corresponds to every possible inhabitant of `u8`. (In practice, the"] # [doc = " physical alphabet size may be smaller because of alphabet compression"] # [doc = " via equivalence classes, but EOI is always represented somehow in the"] # [doc = " alphabet.)"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the given ID does not refer to a valid state, then this routine"] # [doc = " may panic but it also may not panic and instead return an invalid ID."] # [doc = " However, if the caller provides an invalid ID then this must never"] # [doc = " sacrifice memory safety."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows a simplistic example for walking a DFA for a given haystack,"] # [doc = " and then finishing the search with the final EOI transition."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, dense}, Input};"] # [doc = ""] # [doc = " let dfa = dense::DFA::new(r\"[a-z]+r\")?;"] # [doc = " let haystack = \"bar\".as_bytes();"] # [doc = ""] # [doc = " // The start state is determined by inspecting the position and the"] # [doc = " // initial bytes of the haystack."] # [doc = " //"] # [doc = " // The unwrap is OK because we aren't requesting a start state for a"] # [doc = " // specific pattern."] # [doc = " let mut state = dfa.start_state_forward(&Input::new(haystack))?;"] # [doc = " // Walk all the bytes in the haystack."] # [doc = " for &b in haystack {"] # [doc = "     state = dfa.next_state(state, b);"] # [doc = " }"] # [doc = " // Matches are always delayed by 1 byte, so we must explicitly walk"] # [doc = " // the special \"EOI\" transition at the end of the search. Without this"] # [doc = " // final transition, the assert below will fail since the DFA will not"] # [doc = " // have entered a match state yet!"] # [doc = " state = dfa.next_eoi_state(state);"] # [doc = " assert!(dfa.is_match_state(state));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] fn next_eoi_state (& self , current : StateID) -> StateID ; # [doc = " Return the ID of the start state for this DFA for the given starting"] # [doc = " configuration."] # [doc = ""] # [doc = " Unlike typical DFA implementations, the start state for DFAs in this"] # [doc = " crate is dependent on a few different factors:"] # [doc = ""] # [doc = " * The [`Anchored`] mode of the search. Unanchored, anchored and"] # [doc = " anchored searches for a specific [`PatternID`] all use different start"] # [doc = " states."] # [doc = " * Whether a \"look-behind\" byte exists. For example, the `^` anchor"] # [doc = " matches if and only if there is no look-behind byte."] # [doc = " * The specific value of that look-behind byte. For example, a `(?m:^)`"] # [doc = " assertion only matches when there is either no look-behind byte, or"] # [doc = " when the look-behind byte is a line terminator."] # [doc = ""] # [doc = " The [starting configuration](start::Config) provides the above"] # [doc = " information."] # [doc = ""] # [doc = " This routine can be used for either forward or reverse searches."] # [doc = " Although, as a convenience, if you have an [`Input`], then it may"] # [doc = " be more succinct to use [`Automaton::start_state_forward`] or"] # [doc = " [`Automaton::start_state_reverse`]. Note, for example, that the"] # [doc = " convenience routines return a [`MatchError`] on failure where as this"] # [doc = " routine returns a [`StartError`]."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This may return a [`StartError`] if the search needs to give up when"] # [doc = " determining the start state (for example, if it sees a \"quit\" byte)."] # [doc = " This can also return an error if the given configuration contains an"] # [doc = " unsupported [`Anchored`] configuration."] fn start_state (& self , config : & start :: Config ,) -> Result < StateID , StartError > ; # [doc = " Return the ID of the start state for this DFA when executing a forward"] # [doc = " search."] # [doc = ""] # [doc = " This is a convenience routine for calling [`Automaton::start_state`]"] # [doc = " that converts the given [`Input`] to a [start"] # [doc = " configuration](start::Config). Additionally, if an error occurs, it is"] # [doc = " converted from a [`StartError`] to a [`MatchError`] using the offset"] # [doc = " information in the given [`Input`]."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This may return a [`MatchError`] if the search needs to give up"] # [doc = " when determining the start state (for example, if it sees a \"quit\""] # [doc = " byte). This can also return an error if the given `Input` contains an"] # [doc = " unsupported [`Anchored`] configuration."] fn start_state_forward (& self , input : & Input < '_ > ,) -> Result < StateID , MatchError > { let config = start :: Config :: from_input_forward (input) ; self . start_state (& config) . map_err (| err | match err { StartError :: Quit { byte } => { let offset = input . start () . checked_sub (1) . expect ("no quit in start without look-behind") ; MatchError :: quit (byte , offset) } StartError :: UnsupportedAnchored { mode } => { MatchError :: unsupported_anchored (mode) } }) } # [doc = " Return the ID of the start state for this DFA when executing a reverse"] # [doc = " search."] # [doc = ""] # [doc = " This is a convenience routine for calling [`Automaton::start_state`]"] # [doc = " that converts the given [`Input`] to a [start"] # [doc = " configuration](start::Config). Additionally, if an error occurs, it is"] # [doc = " converted from a [`StartError`] to a [`MatchError`] using the offset"] # [doc = " information in the given [`Input`]."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This may return a [`MatchError`] if the search needs to give up"] # [doc = " when determining the start state (for example, if it sees a \"quit\""] # [doc = " byte). This can also return an error if the given `Input` contains an"] # [doc = " unsupported [`Anchored`] configuration."] fn start_state_reverse (& self , input : & Input < '_ > ,) -> Result < StateID , MatchError > { let config = start :: Config :: from_input_reverse (input) ; self . start_state (& config) . map_err (| err | match err { StartError :: Quit { byte } => { let offset = input . end () ; MatchError :: quit (byte , offset) } StartError :: UnsupportedAnchored { mode } => { MatchError :: unsupported_anchored (mode) } }) } # [doc = " If this DFA has a universal starting state for the given anchor mode"] # [doc = " and the DFA supports universal starting states, then this returns that"] # [doc = " state's identifier."] # [doc = ""] # [doc = " A DFA is said to have a universal starting state when the starting"] # [doc = " state is invariant with respect to the haystack. Usually, the starting"] # [doc = " state is chosen depending on the bytes immediately surrounding the"] # [doc = " starting position of a search. However, the starting state only differs"] # [doc = " when one or more of the patterns in the DFA have look-around assertions"] # [doc = " in its prefix."] # [doc = ""] # [doc = " Stated differently, if none of the patterns in a DFA have look-around"] # [doc = " assertions in their prefix, then the DFA has a universal starting state"] # [doc = " and _may_ be returned by this method."] # [doc = ""] # [doc = " It is always correct for implementations to return `None`, and indeed,"] # [doc = " this is what the default implementation does. When this returns `None`,"] # [doc = " callers must use either `start_state_forward` or `start_state_reverse`"] # [doc = " to get the starting state."] # [doc = ""] # [doc = " # Use case"] # [doc = ""] # [doc = " There are a few reasons why one might want to use this:"] # [doc = ""] # [doc = " * If you know your regex patterns have no look-around assertions in"] # [doc = " their prefix, then calling this routine is likely cheaper and perhaps"] # [doc = " more semantically meaningful."] # [doc = " * When implementing prefilter support in a DFA regex implementation,"] # [doc = " it is necessary to re-compute the start state after a candidate"] # [doc = " is returned from the prefilter. However, this is only needed when"] # [doc = " there isn't a universal start state. When one exists, one can avoid"] # [doc = " re-computing the start state."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{Automaton, dense::DFA},"] # [doc = "     Anchored,"] # [doc = " };"] # [doc = ""] # [doc = " // There are no look-around assertions in the prefixes of any of the"] # [doc = " // patterns, so we get a universal start state."] # [doc = " let dfa = DFA::new_many(&[\"[0-9]+\", \"[a-z]+$\", \"[A-Z]+\"])?;"] # [doc = " assert!(dfa.universal_start_state(Anchored::No).is_some());"] # [doc = " assert!(dfa.universal_start_state(Anchored::Yes).is_some());"] # [doc = ""] # [doc = " // One of the patterns has a look-around assertion in its prefix,"] # [doc = " // so this means there is no longer a universal start state."] # [doc = " let dfa = DFA::new_many(&[\"[0-9]+\", \"^[a-z]+$\", \"[A-Z]+\"])?;"] # [doc = " assert!(!dfa.universal_start_state(Anchored::No).is_some());"] # [doc = " assert!(!dfa.universal_start_state(Anchored::Yes).is_some());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] fn universal_start_state (& self , _mode : Anchored) -> Option < StateID > { None } # [doc = " Returns true if and only if the given identifier corresponds to a"] # [doc = " \"special\" state. A special state is one or more of the following:"] # [doc = " a dead state, a quit state, a match state, a start state or an"] # [doc = " accelerated state."] # [doc = ""] # [doc = " A correct implementation _may_ always return false for states that"] # [doc = " are either start states or accelerated states, since that information"] # [doc = " is only intended to be used for optimization purposes. Correct"] # [doc = " implementations must return true if the state is a dead, quit or match"] # [doc = " state. This is because search routines using this trait must be able"] # [doc = " to rely on `is_special_state` as an indicator that a state may need"] # [doc = " special treatment. (For example, when a search routine sees a dead"] # [doc = " state, it must terminate.)"] # [doc = ""] # [doc = " This routine permits search implementations to use a single branch to"] # [doc = " check whether a state needs special attention before executing the next"] # [doc = " transition. The example below shows how to do this."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how `is_special_state` can be used to implement a"] # [doc = " correct search routine with minimal branching. In particular, this"] # [doc = " search routine implements \"leftmost\" matching, which means that it"] # [doc = " doesn't immediately stop once a match is found. Instead, it continues"] # [doc = " until it reaches a dead state."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{Automaton, dense},"] # [doc = "     HalfMatch, MatchError, Input,"] # [doc = " };"] # [doc = ""] # [doc = " fn find<A: Automaton>("] # [doc = "     dfa: &A,"] # [doc = "     haystack: &[u8],"] # [doc = " ) -> Result<Option<HalfMatch>, MatchError> {"] # [doc = "     // The start state is determined by inspecting the position and the"] # [doc = "     // initial bytes of the haystack. Note that start states can never"] # [doc = "     // be match states (since DFAs in this crate delay matches by 1"] # [doc = "     // byte), so we don't need to check if the start state is a match."] # [doc = "     let mut state = dfa.start_state_forward(&Input::new(haystack))?;"] # [doc = "     let mut last_match = None;"] # [doc = "     // Walk all the bytes in the haystack. We can quit early if we see"] # [doc = "     // a dead or a quit state. The former means the automaton will"] # [doc = "     // never transition to any other state. The latter means that the"] # [doc = "     // automaton entered a condition in which its search failed."] # [doc = "     for (i, &b) in haystack.iter().enumerate() {"] # [doc = "         state = dfa.next_state(state, b);"] # [doc = "         if dfa.is_special_state(state) {"] # [doc = "             if dfa.is_match_state(state) {"] # [doc = "                 last_match = Some(HalfMatch::new("] # [doc = "                     dfa.match_pattern(state, 0),"] # [doc = "                     i,"] # [doc = "                 ));"] # [doc = "             } else if dfa.is_dead_state(state) {"] # [doc = "                 return Ok(last_match);"] # [doc = "             } else if dfa.is_quit_state(state) {"] # [doc = "                 // It is possible to enter into a quit state after"] # [doc = "                 // observing a match has occurred. In that case, we"] # [doc = "                 // should return the match instead of an error."] # [doc = "                 if last_match.is_some() {"] # [doc = "                     return Ok(last_match);"] # [doc = "                 }"] # [doc = "                 return Err(MatchError::quit(b, i));"] # [doc = "             }"] # [doc = "             // Implementors may also want to check for start or accel"] # [doc = "             // states and handle them differently for performance"] # [doc = "             // reasons. But it is not necessary for correctness."] # [doc = "         }"] # [doc = "     }"] # [doc = "     // Matches are always delayed by 1 byte, so we must explicitly walk"] # [doc = "     // the special \"EOI\" transition at the end of the search."] # [doc = "     state = dfa.next_eoi_state(state);"] # [doc = "     if dfa.is_match_state(state) {"] # [doc = "         last_match = Some(HalfMatch::new("] # [doc = "             dfa.match_pattern(state, 0),"] # [doc = "             haystack.len(),"] # [doc = "         ));"] # [doc = "     }"] # [doc = "     Ok(last_match)"] # [doc = " }"] # [doc = ""] # [doc = " // We use a greedy '+' operator to show how the search doesn't just"] # [doc = " // stop once a match is detected. It continues extending the match."] # [doc = " // Using '[a-z]+?' would also work as expected and stop the search"] # [doc = " // early. Greediness is built into the automaton."] # [doc = " let dfa = dense::DFA::new(r\"[a-z]+\")?;"] # [doc = " let haystack = \"123 foobar 4567\".as_bytes();"] # [doc = " let mat = find(&dfa, haystack)?.unwrap();"] # [doc = " assert_eq!(mat.pattern().as_usize(), 0);"] # [doc = " assert_eq!(mat.offset(), 10);"] # [doc = ""] # [doc = " // Here's another example that tests our handling of the special EOI"] # [doc = " // transition. This will fail to find a match if we don't call"] # [doc = " // 'next_eoi_state' at the end of the search since the match isn't"] # [doc = " // found until the final byte in the haystack."] # [doc = " let dfa = dense::DFA::new(r\"[0-9]{4}\")?;"] # [doc = " let haystack = \"123 foobar 4567\".as_bytes();"] # [doc = " let mat = find(&dfa, haystack)?.unwrap();"] # [doc = " assert_eq!(mat.pattern().as_usize(), 0);"] # [doc = " assert_eq!(mat.offset(), 15);"] # [doc = ""] # [doc = " // And note that our search implementation above automatically works"] # [doc = " // with multi-DFAs. Namely, `dfa.match_pattern(match_state, 0)` selects"] # [doc = " // the appropriate pattern ID for us."] # [doc = " let dfa = dense::DFA::new_many(&[r\"[a-z]+\", r\"[0-9]+\"])?;"] # [doc = " let haystack = \"123 foobar 4567\".as_bytes();"] # [doc = " let mat = find(&dfa, haystack)?.unwrap();"] # [doc = " assert_eq!(mat.pattern().as_usize(), 1);"] # [doc = " assert_eq!(mat.offset(), 3);"] # [doc = " let mat = find(&dfa, &haystack[3..])?.unwrap();"] # [doc = " assert_eq!(mat.pattern().as_usize(), 0);"] # [doc = " assert_eq!(mat.offset(), 7);"] # [doc = " let mat = find(&dfa, &haystack[10..])?.unwrap();"] # [doc = " assert_eq!(mat.pattern().as_usize(), 1);"] # [doc = " assert_eq!(mat.offset(), 5);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] fn is_special_state (& self , id : StateID) -> bool ; # [doc = " Returns true if and only if the given identifier corresponds to a dead"] # [doc = " state. When a DFA enters a dead state, it is impossible to leave. That"] # [doc = " is, every transition on a dead state by definition leads back to the"] # [doc = " same dead state."] # [doc = ""] # [doc = " In practice, the dead state always corresponds to the identifier `0`."] # [doc = " Moreover, in practice, there is only one dead state."] # [doc = ""] # [doc = " The existence of a dead state is not strictly required in the classical"] # [doc = " model of finite state machines, where one generally only cares about"] # [doc = " the question of whether an input sequence matches or not. Dead states"] # [doc = " are not needed to answer that question, since one can immediately quit"] # [doc = " as soon as one enters a final or \"match\" state. However, we don't just"] # [doc = " care about matches but also care about the location of matches, and"] # [doc = " more specifically, care about semantics like \"greedy\" matching."] # [doc = ""] # [doc = " For example, given the pattern `a+` and the input `aaaz`, the dead"] # [doc = " state won't be entered until the state machine reaches `z` in the"] # [doc = " input, at which point, the search routine can quit. But without the"] # [doc = " dead state, the search routine wouldn't know when to quit. In a"] # [doc = " classical representation, the search routine would stop after seeing"] # [doc = " the first `a` (which is when the search would enter a match state). But"] # [doc = " this wouldn't implement \"greedy\" matching where `a+` matches as many"] # [doc = " `a`'s as possible."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " See the example for [`Automaton::is_special_state`] for how to use this"] # [doc = " method correctly."] fn is_dead_state (& self , id : StateID) -> bool ; # [doc = " Returns true if and only if the given identifier corresponds to a quit"] # [doc = " state. A quit state is like a dead state (it has no transitions other"] # [doc = " than to itself), except it indicates that the DFA failed to complete"] # [doc = " the search. When this occurs, callers can neither accept or reject that"] # [doc = " a match occurred."] # [doc = ""] # [doc = " In practice, the quit state always corresponds to the state immediately"] # [doc = " following the dead state. (Which is not usually represented by `1`,"] # [doc = " since state identifiers are pre-multiplied by the state machine's"] # [doc = " alphabet stride, and the alphabet stride varies between DFAs.)"] # [doc = ""] # [doc = " The typical way in which a quit state can occur is when heuristic"] # [doc = " support for Unicode word boundaries is enabled via the"] # [doc = " [`dense::Config::unicode_word_boundary`](crate::dfa::dense::Config::unicode_word_boundary)"] # [doc = " option. But other options, like the lower level"] # [doc = " [`dense::Config::quit`](crate::dfa::dense::Config::quit)"] # [doc = " configuration, can also result in a quit state being entered. The"] # [doc = " purpose of the quit state is to provide a way to execute a fast DFA"] # [doc = " in common cases while delegating to slower routines when the DFA quits."] # [doc = ""] # [doc = " The default search implementations provided by this crate will return a"] # [doc = " [`MatchError::quit`] error when a quit state is entered."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " See the example for [`Automaton::is_special_state`] for how to use this"] # [doc = " method correctly."] fn is_quit_state (& self , id : StateID) -> bool ; # [doc = " Returns true if and only if the given identifier corresponds to a"] # [doc = " match state. A match state is also referred to as a \"final\" state and"] # [doc = " indicates that a match has been found."] # [doc = ""] # [doc = " If all you care about is whether a particular pattern matches in the"] # [doc = " input sequence, then a search routine can quit early as soon as the"] # [doc = " machine enters a match state. However, if you're looking for the"] # [doc = " standard \"leftmost-first\" match location, then search _must_ continue"] # [doc = " until either the end of the input or until the machine enters a dead"] # [doc = " state. (Since either condition implies that no other useful work can"] # [doc = " be done.) Namely, when looking for the location of a match, then"] # [doc = " search implementations should record the most recent location in"] # [doc = " which a match state was entered, but otherwise continue executing the"] # [doc = " search as normal. (The search may even leave the match state.) Once"] # [doc = " the termination condition is reached, the most recently recorded match"] # [doc = " location should be returned."] # [doc = ""] # [doc = " Finally, one additional power given to match states in this crate"] # [doc = " is that they are always associated with a specific pattern in order"] # [doc = " to support multi-DFAs. See [`Automaton::match_pattern`] for more"] # [doc = " details and an example for how to query the pattern associated with a"] # [doc = " particular match state."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " See the example for [`Automaton::is_special_state`] for how to use this"] # [doc = " method correctly."] fn is_match_state (& self , id : StateID) -> bool ; # [doc = " Returns true only if the given identifier corresponds to a start"] # [doc = " state"] # [doc = ""] # [doc = " A start state is a state in which a DFA begins a search."] # [doc = " All searches begin in a start state. Moreover, since all matches are"] # [doc = " delayed by one byte, a start state can never be a match state."] # [doc = ""] # [doc = " The main role of a start state is, as mentioned, to be a starting"] # [doc = " point for a DFA. This starting point is determined via one of"] # [doc = " [`Automaton::start_state_forward`] or"] # [doc = " [`Automaton::start_state_reverse`], depending on whether one is doing"] # [doc = " a forward or a reverse search, respectively."] # [doc = ""] # [doc = " A secondary use of start states is for prefix acceleration. Namely,"] # [doc = " while executing a search, if one detects that you're in a start state,"] # [doc = " then it may be faster to look for the next match of a prefix of the"] # [doc = " pattern, if one exists. If a prefix exists and since all matches must"] # [doc = " begin with that prefix, then skipping ahead to occurrences of that"] # [doc = " prefix may be much faster than executing the DFA."] # [doc = ""] # [doc = " As mentioned in the documentation for"] # [doc = " [`is_special_state`](Automaton::is_special_state) implementations"] # [doc = " _may_ always return false, even if the given identifier is a start"] # [doc = " state. This is because knowing whether a state is a start state or not"] # [doc = " is not necessary for correctness and is only treated as a potential"] # [doc = " performance optimization. (For example, the implementations of this"] # [doc = " trait in this crate will only return true when the given identifier"] # [doc = " corresponds to a start state and when [specialization of start"] # [doc = " states](crate::dfa::dense::Config::specialize_start_states) was enabled"] # [doc = " during DFA construction. If start state specialization is disabled"] # [doc = " (which is the default), then this method will always return false.)"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to implement your own search routine that does"] # [doc = " a prefix search whenever the search enters a start state."] # [doc = ""] # [doc = " Note that you do not need to implement your own search routine"] # [doc = " to make use of prefilters like this. The search routines"] # [doc = " provided by this crate already implement prefilter support via"] # [doc = " the [`Prefilter`](crate::util::prefilter::Prefilter) trait."] # [doc = " A prefilter can be added to your search configuration with"] # [doc = " [`dense::Config::prefilter`](crate::dfa::dense::Config::prefilter) for"] # [doc = " dense and sparse DFAs in this crate."] # [doc = ""] # [doc = " This example is meant to show how you might deal with prefilters in a"] # [doc = " simplified case if you are implementing your own search routine."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{Automaton, dense},"] # [doc = "     HalfMatch, MatchError, Input,"] # [doc = " };"] # [doc = ""] # [doc = " fn find_byte(slice: &[u8], at: usize, byte: u8) -> Option<usize> {"] # [doc = "     // Would be faster to use the memchr crate, but this is still"] # [doc = "     // faster than running through the DFA."] # [doc = "     slice[at..].iter().position(|&b| b == byte).map(|i| at + i)"] # [doc = " }"] # [doc = ""] # [doc = " fn find<A: Automaton>("] # [doc = "     dfa: &A,"] # [doc = "     haystack: &[u8],"] # [doc = "     prefix_byte: Option<u8>,"] # [doc = " ) -> Result<Option<HalfMatch>, MatchError> {"] # [doc = "     // See the Automaton::is_special_state example for similar code"] # [doc = "     // with more comments."] # [doc = ""] # [doc = "     let mut state = dfa.start_state_forward(&Input::new(haystack))?;"] # [doc = "     let mut last_match = None;"] # [doc = "     let mut pos = 0;"] # [doc = "     while pos < haystack.len() {"] # [doc = "         let b = haystack[pos];"] # [doc = "         state = dfa.next_state(state, b);"] # [doc = "         pos += 1;"] # [doc = "         if dfa.is_special_state(state) {"] # [doc = "             if dfa.is_match_state(state) {"] # [doc = "                 last_match = Some(HalfMatch::new("] # [doc = "                     dfa.match_pattern(state, 0),"] # [doc = "                     pos - 1,"] # [doc = "                 ));"] # [doc = "             } else if dfa.is_dead_state(state) {"] # [doc = "                 return Ok(last_match);"] # [doc = "             } else if dfa.is_quit_state(state) {"] # [doc = "                 // It is possible to enter into a quit state after"] # [doc = "                 // observing a match has occurred. In that case, we"] # [doc = "                 // should return the match instead of an error."] # [doc = "                 if last_match.is_some() {"] # [doc = "                     return Ok(last_match);"] # [doc = "                 }"] # [doc = "                 return Err(MatchError::quit(b, pos - 1));"] # [doc = "             } else if dfa.is_start_state(state) {"] # [doc = "                 // If we're in a start state and know all matches begin"] # [doc = "                 // with a particular byte, then we can quickly skip to"] # [doc = "                 // candidate matches without running the DFA through"] # [doc = "                 // every byte inbetween."] # [doc = "                 if let Some(prefix_byte) = prefix_byte {"] # [doc = "                     pos = match find_byte(haystack, pos, prefix_byte) {"] # [doc = "                         Some(pos) => pos,"] # [doc = "                         None => break,"] # [doc = "                     };"] # [doc = "                 }"] # [doc = "             }"] # [doc = "         }"] # [doc = "     }"] # [doc = "     // Matches are always delayed by 1 byte, so we must explicitly walk"] # [doc = "     // the special \"EOI\" transition at the end of the search."] # [doc = "     state = dfa.next_eoi_state(state);"] # [doc = "     if dfa.is_match_state(state) {"] # [doc = "         last_match = Some(HalfMatch::new("] # [doc = "             dfa.match_pattern(state, 0),"] # [doc = "             haystack.len(),"] # [doc = "         ));"] # [doc = "     }"] # [doc = "     Ok(last_match)"] # [doc = " }"] # [doc = ""] # [doc = " // In this example, it's obvious that all occurrences of our pattern"] # [doc = " // begin with 'Z', so we pass in 'Z'. Note also that we need to"] # [doc = " // enable start state specialization, or else it won't be possible to"] # [doc = " // detect start states during a search. ('is_start_state' would always"] # [doc = " // return false.)"] # [doc = " let dfa = dense::DFA::builder()"] # [doc = "     .configure(dense::DFA::config().specialize_start_states(true))"] # [doc = "     .build(r\"Z[a-z]+\")?;"] # [doc = " let haystack = \"123 foobar Zbaz quux\".as_bytes();"] # [doc = " let mat = find(&dfa, haystack, Some(b'Z'))?.unwrap();"] # [doc = " assert_eq!(mat.pattern().as_usize(), 0);"] # [doc = " assert_eq!(mat.offset(), 15);"] # [doc = ""] # [doc = " // But note that we don't need to pass in a prefix byte. If we don't,"] # [doc = " // then the search routine does no acceleration."] # [doc = " let mat = find(&dfa, haystack, None)?.unwrap();"] # [doc = " assert_eq!(mat.pattern().as_usize(), 0);"] # [doc = " assert_eq!(mat.offset(), 15);"] # [doc = ""] # [doc = " // However, if we pass an incorrect byte, then the prefix search will"] # [doc = " // result in incorrect results."] # [doc = " assert_eq!(find(&dfa, haystack, Some(b'X'))?, None);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] fn is_start_state (& self , id : StateID) -> bool ; # [doc = " Returns true if and only if the given identifier corresponds to an"] # [doc = " accelerated state."] # [doc = ""] # [doc = " An accelerated state is a special optimization"] # [doc = " trick implemented by this crate. Namely, if"] # [doc = " [`dense::Config::accelerate`](crate::dfa::dense::Config::accelerate) is"] # [doc = " enabled (and it is by default), then DFAs generated by this crate will"] # [doc = " tag states meeting certain characteristics as accelerated. States meet"] # [doc = " this criteria whenever most of their transitions are self-transitions."] # [doc = " That is, transitions that loop back to the same state. When a small"] # [doc = " number of transitions aren't self-transitions, then it follows that"] # [doc = " there are only a small number of bytes that can cause the DFA to leave"] # [doc = " that state. Thus, there is an opportunity to look for those bytes"] # [doc = " using more optimized routines rather than continuing to run through"] # [doc = " the DFA. This trick is similar to the prefilter idea described in"] # [doc = " the documentation of [`Automaton::is_start_state`] with two main"] # [doc = " differences:"] # [doc = ""] # [doc = " 1. It is more limited since acceleration only applies to single bytes."] # [doc = " This means states are rarely accelerated when Unicode mode is enabled"] # [doc = " (which is enabled by default)."] # [doc = " 2. It can occur anywhere in the DFA, which increases optimization"] # [doc = " opportunities."] # [doc = ""] # [doc = " Like the prefilter idea, the main downside (and a possible reason to"] # [doc = " disable it) is that it can lead to worse performance in some cases."] # [doc = " Namely, if a state is accelerated for very common bytes, then the"] # [doc = " overhead of checking for acceleration and using the more optimized"] # [doc = " routines to look for those bytes can cause overall performance to be"] # [doc = " worse than if acceleration wasn't enabled at all."] # [doc = ""] # [doc = " A simple example of a regex that has an accelerated state is"] # [doc = " `(?-u)[^a]+a`. Namely, the `[^a]+` sub-expression gets compiled down"] # [doc = " into a single state where all transitions except for `a` loop back to"] # [doc = " itself, and where `a` is the only transition (other than the special"] # [doc = " EOI transition) that goes to some other state. Thus, this state can"] # [doc = " be accelerated and implemented more efficiently by calling an"] # [doc = " optimized routine like `memchr` with `a` as the needle. Notice that"] # [doc = " the `(?-u)` to disable Unicode is necessary here, as without it,"] # [doc = " `[^a]` will match any UTF-8 encoding of any Unicode scalar value other"] # [doc = " than `a`. This more complicated expression compiles down to many DFA"] # [doc = " states and the simple acceleration optimization is no longer available."] # [doc = ""] # [doc = " Typically, this routine is used to guard calls to"] # [doc = " [`Automaton::accelerator`], which returns the accelerated bytes for"] # [doc = " the specified state."] fn is_accel_state (& self , id : StateID) -> bool ; # [doc = " Returns the total number of patterns compiled into this DFA."] # [doc = ""] # [doc = " In the case of a DFA that contains no patterns, this must return `0`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the pattern length for a DFA that never matches:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::dfa::{Automaton, dense::DFA};"] # [doc = ""] # [doc = " let dfa: DFA<Vec<u32>> = DFA::never_match()?;"] # [doc = " assert_eq!(dfa.pattern_len(), 0);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " And another example for a DFA that matches at every position:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::dfa::{Automaton, dense::DFA};"] # [doc = ""] # [doc = " let dfa: DFA<Vec<u32>> = DFA::always_match()?;"] # [doc = " assert_eq!(dfa.pattern_len(), 1);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " And finally, a DFA that was constructed from multiple patterns:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::dfa::{Automaton, dense::DFA};"] # [doc = ""] # [doc = " let dfa = DFA::new_many(&[\"[0-9]+\", \"[a-z]+\", \"[A-Z]+\"])?;"] # [doc = " assert_eq!(dfa.pattern_len(), 3);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] fn pattern_len (& self) -> usize ; # [doc = " Returns the total number of patterns that match in this state."] # [doc = ""] # [doc = " If the given state is not a match state, then implementations may"] # [doc = " panic."] # [doc = ""] # [doc = " If the DFA was compiled with one pattern, then this must necessarily"] # [doc = " always return `1` for all match states."] # [doc = ""] # [doc = " Implementations must guarantee that [`Automaton::match_pattern`] can be"] # [doc = " called with indices up to (but not including) the length returned by"] # [doc = " this routine without panicking."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Implementations are permitted to panic if the provided state ID does"] # [doc = " not correspond to a match state."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows a simple instance of implementing overlapping"] # [doc = " matches. In particular, it shows not only how to determine how many"] # [doc = " patterns have matched in a particular state, but also how to access"] # [doc = " which specific patterns have matched."] # [doc = ""] # [doc = " Notice that we must use"] # [doc = " [`MatchKind::All`](crate::MatchKind::All)"] # [doc = " when building the DFA. If we used"] # [doc = " [`MatchKind::LeftmostFirst`](crate::MatchKind::LeftmostFirst)"] # [doc = " instead, then the DFA would not be constructed in a way that"] # [doc = " supports overlapping matches. (It would only report a single pattern"] # [doc = " that matches at any particular point in time.)"] # [doc = ""] # [doc = " Another thing to take note of is the patterns used and the order in"] # [doc = " which the pattern IDs are reported. In the example below, pattern `3`"] # [doc = " is yielded first. Why? Because it corresponds to the match that"] # [doc = " appears first. Namely, the `@` symbol is part of `\\S+` but not part"] # [doc = " of any of the other patterns. Since the `\\S+` pattern has a match that"] # [doc = " starts to the left of any other pattern, its ID is returned before any"] # [doc = " other."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{dfa::{Automaton, dense}, Input, MatchKind};"] # [doc = ""] # [doc = " let dfa = dense::Builder::new()"] # [doc = "     .configure(dense::Config::new().match_kind(MatchKind::All))"] # [doc = "     .build_many(&["] # [doc = "         r\"[[:word:]]+\", r\"[a-z]+\", r\"[A-Z]+\", r\"[[:^space:]]+\","] # [doc = "     ])?;"] # [doc = " let haystack = \"@bar\".as_bytes();"] # [doc = ""] # [doc = " // The start state is determined by inspecting the position and the"] # [doc = " // initial bytes of the haystack."] # [doc = " let mut state = dfa.start_state_forward(&Input::new(haystack))?;"] # [doc = " // Walk all the bytes in the haystack."] # [doc = " for &b in haystack {"] # [doc = "     state = dfa.next_state(state, b);"] # [doc = " }"] # [doc = " state = dfa.next_eoi_state(state);"] # [doc = ""] # [doc = " assert!(dfa.is_match_state(state));"] # [doc = " assert_eq!(dfa.match_len(state), 3);"] # [doc = " // The following calls are guaranteed to not panic since `match_len`"] # [doc = " // returned `3` above."] # [doc = " assert_eq!(dfa.match_pattern(state, 0).as_usize(), 3);"] # [doc = " assert_eq!(dfa.match_pattern(state, 1).as_usize(), 0);"] # [doc = " assert_eq!(dfa.match_pattern(state, 2).as_usize(), 1);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] fn match_len (& self , id : StateID) -> usize ; # [doc = " Returns the pattern ID corresponding to the given match index in the"] # [doc = " given state."] # [doc = ""] # [doc = " See [`Automaton::match_len`] for an example of how to use this"] # [doc = " method correctly. Note that if you know your DFA is compiled with a"] # [doc = " single pattern, then this routine is never necessary since it will"] # [doc = " always return a pattern ID of `0` for an index of `0` when `id`"] # [doc = " corresponds to a match state."] # [doc = ""] # [doc = " Typically, this routine is used when implementing an overlapping"] # [doc = " search, as the example for `Automaton::match_len` does."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the state ID is not a match state or if the match index is out"] # [doc = " of bounds for the given state, then this routine may either panic"] # [doc = " or produce an incorrect result. If the state ID is correct and the"] # [doc = " match index is correct, then this routine must always produce a valid"] # [doc = " `PatternID`."] fn match_pattern (& self , id : StateID , index : usize) -> PatternID ; # [doc = " Returns true if and only if this automaton can match the empty string."] # [doc = " When it returns false, all possible matches are guaranteed to have a"] # [doc = " non-zero length."] # [doc = ""] # [doc = " This is useful as cheap way to know whether code needs to handle the"] # [doc = " case of a zero length match. This is particularly important when UTF-8"] # [doc = " modes are enabled, as when UTF-8 mode is enabled, empty matches that"] # [doc = " split a codepoint must never be reported. This extra handling can"] # [doc = " sometimes be costly, and since regexes matching an empty string are"] # [doc = " somewhat rare, it can be beneficial to treat such regexes specially."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows a few different DFAs and whether they match the"] # [doc = " empty string or not. Notice the empty string isn't merely a matter"] # [doc = " of a string of length literally `0`, but rather, whether a match can"] # [doc = " occur between specific pairs of bytes."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{dense::DFA, Automaton}, util::syntax};"] # [doc = ""] # [doc = " // The empty regex matches the empty string."] # [doc = " let dfa = DFA::new(\"\")?;"] # [doc = " assert!(dfa.has_empty(), \"empty matches empty\");"] # [doc = " // The '+' repetition operator requires at least one match, and so"] # [doc = " // does not match the empty string."] # [doc = " let dfa = DFA::new(\"a+\")?;"] # [doc = " assert!(!dfa.has_empty(), \"+ does not match empty\");"] # [doc = " // But the '*' repetition operator does."] # [doc = " let dfa = DFA::new(\"a*\")?;"] # [doc = " assert!(dfa.has_empty(), \"* does match empty\");"] # [doc = " // And wrapping '+' in an operator that can match an empty string also"] # [doc = " // causes it to match the empty string too."] # [doc = " let dfa = DFA::new(\"(a+)*\")?;"] # [doc = " assert!(dfa.has_empty(), \"+ inside of * matches empty\");"] # [doc = ""] # [doc = " // If a regex is just made of a look-around assertion, even if the"] # [doc = " // assertion requires some kind of non-empty string around it (such as"] # [doc = " // \\b), then it is still treated as if it matches the empty string."] # [doc = " // Namely, if a match occurs of just a look-around assertion, then the"] # [doc = " // match returned is empty."] # [doc = " let dfa = DFA::builder()"] # [doc = "     .configure(DFA::config().unicode_word_boundary(true))"] # [doc = "     .syntax(syntax::Config::new().utf8(false))"] # [doc = "     .build(r\"^$\\A\\z\\b\\B(?-u:\\b\\B)\")?;"] # [doc = " assert!(dfa.has_empty(), \"assertions match empty\");"] # [doc = " // Even when an assertion is wrapped in a '+', it still matches the"] # [doc = " // empty string."] # [doc = " let dfa = DFA::new(r\"^+\")?;"] # [doc = " assert!(dfa.has_empty(), \"+ of an assertion matches empty\");"] # [doc = ""] # [doc = " // An alternation with even one branch that can match the empty string"] # [doc = " // is also said to match the empty string overall."] # [doc = " let dfa = DFA::new(\"foo|(bar)?|quux\")?;"] # [doc = " assert!(dfa.has_empty(), \"alternations can match empty\");"] # [doc = ""] # [doc = " // An NFA that matches nothing does not match the empty string."] # [doc = " let dfa = DFA::new(\"[a&&b]\")?;"] # [doc = " assert!(!dfa.has_empty(), \"never matching means not matching empty\");"] # [doc = " // But if it's wrapped in something that doesn't require a match at"] # [doc = " // all, then it can match the empty string!"] # [doc = " let dfa = DFA::new(\"[a&&b]*\")?;"] # [doc = " assert!(dfa.has_empty(), \"* on never-match still matches empty\");"] # [doc = " // Since a '+' requires a match, using it on something that can never"] # [doc = " // match will itself produce a regex that can never match anything,"] # [doc = " // and thus does not match the empty string."] # [doc = " let dfa = DFA::new(\"[a&&b]+\")?;"] # [doc = " assert!(!dfa.has_empty(), \"+ on never-match still matches nothing\");"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] fn has_empty (& self) -> bool ; # [doc = " Whether UTF-8 mode is enabled for this DFA or not."] # [doc = ""] # [doc = " When UTF-8 mode is enabled, all matches reported by a DFA are"] # [doc = " guaranteed to correspond to spans of valid UTF-8. This includes"] # [doc = " zero-width matches. For example, the DFA must guarantee that the empty"] # [doc = " regex will not match at the positions between code units in the UTF-8"] # [doc = " encoding of a single codepoint."] # [doc = ""] # [doc = " See [`thompson::Config::utf8`](crate::nfa::thompson::Config::utf8) for"] # [doc = " more information."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how UTF-8 mode can impact the match spans that may"] # [doc = " be reported in certain cases."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{dense::DFA, Automaton},"] # [doc = "     nfa::thompson,"] # [doc = "     HalfMatch, Input,"] # [doc = " };"] # [doc = ""] # [doc = " // UTF-8 mode is enabled by default."] # [doc = " let re = DFA::new(\"\")?;"] # [doc = " assert!(re.is_utf8());"] # [doc = " let mut input = Input::new(\"☃\");"] # [doc = " let got = re.try_search_fwd(&input)?;"] # [doc = " assert_eq!(Some(HalfMatch::must(0, 0)), got);"] # [doc = ""] # [doc = " // Even though an empty regex matches at 1..1, our next match is"] # [doc = " // 3..3 because 1..1 and 2..2 split the snowman codepoint (which is"] # [doc = " // three bytes long)."] # [doc = " input.set_start(1);"] # [doc = " let got = re.try_search_fwd(&input)?;"] # [doc = " assert_eq!(Some(HalfMatch::must(0, 3)), got);"] # [doc = ""] # [doc = " // But if we disable UTF-8, then we'll get matches at 1..1 and 2..2:"] # [doc = " let re = DFA::builder()"] # [doc = "     .thompson(thompson::Config::new().utf8(false))"] # [doc = "     .build(\"\")?;"] # [doc = " assert!(!re.is_utf8());"] # [doc = " let got = re.try_search_fwd(&input)?;"] # [doc = " assert_eq!(Some(HalfMatch::must(0, 1)), got);"] # [doc = ""] # [doc = " input.set_start(2);"] # [doc = " let got = re.try_search_fwd(&input)?;"] # [doc = " assert_eq!(Some(HalfMatch::must(0, 2)), got);"] # [doc = ""] # [doc = " input.set_start(3);"] # [doc = " let got = re.try_search_fwd(&input)?;"] # [doc = " assert_eq!(Some(HalfMatch::must(0, 3)), got);"] # [doc = ""] # [doc = " input.set_start(4);"] # [doc = " let got = re.try_search_fwd(&input)?;"] # [doc = " assert_eq!(None, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] fn is_utf8 (& self) -> bool ; # [doc = " Returns true if and only if this DFA is limited to returning matches"] # [doc = " whose start position is `0`."] # [doc = ""] # [doc = " Note that if you're using DFAs provided by"] # [doc = " this crate, then this is _orthogonal_ to"] # [doc = " [`Config::start_kind`](crate::dfa::dense::Config::start_kind)."] # [doc = ""] # [doc = " This is useful in some cases because if a DFA is limited to producing"] # [doc = " matches that start at offset `0`, then a reverse search is never"] # [doc = " required for finding the start of a match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::dfa::{dense::DFA, Automaton};"] # [doc = ""] # [doc = " // The empty regex matches anywhere"] # [doc = " let dfa = DFA::new(\"\")?;"] # [doc = " assert!(!dfa.is_always_start_anchored(), \"empty matches anywhere\");"] # [doc = " // 'a' matches anywhere."] # [doc = " let dfa = DFA::new(\"a\")?;"] # [doc = " assert!(!dfa.is_always_start_anchored(), \"'a' matches anywhere\");"] # [doc = " // '^' only matches at offset 0!"] # [doc = " let dfa = DFA::new(\"^a\")?;"] # [doc = " assert!(dfa.is_always_start_anchored(), \"'^a' matches only at 0\");"] # [doc = " // But '(?m:^)' matches at 0 but at other offsets too."] # [doc = " let dfa = DFA::new(\"(?m:^)a\")?;"] # [doc = " assert!(!dfa.is_always_start_anchored(), \"'(?m:^)a' matches anywhere\");"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] fn is_always_start_anchored (& self) -> bool ; # [doc = " Return a slice of bytes to accelerate for the given state, if possible."] # [doc = ""] # [doc = " If the given state has no accelerator, then an empty slice must be"] # [doc = " returned. If `Automaton::is_accel_state` returns true for the given ID,"] # [doc = " then this routine _must_ return a non-empty slice. But note that it is"] # [doc = " not required for an implementation of this trait to ever return `true`"] # [doc = " for `is_accel_state`, even if the state _could_ be accelerated. That"] # [doc = " is, acceleration is an optional optimization. But the return values of"] # [doc = " `is_accel_state` and `accelerator` must be in sync."] # [doc = ""] # [doc = " If the given ID is not a valid state ID for this automaton, then"] # [doc = " implementations may panic or produce incorrect results."] # [doc = ""] # [doc = " See [`Automaton::is_accel_state`] for more details on state"] # [doc = " acceleration."] # [doc = ""] # [doc = " By default, this method will always return an empty slice."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows a contrived case in which we build a regex that we"] # [doc = " know is accelerated and extract the accelerator from a state."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{Automaton, dense},"] # [doc = "     util::{primitives::StateID, syntax},"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = dense::Builder::new()"] # [doc = "     // We disable Unicode everywhere and permit the regex to match"] # [doc = "     // invalid UTF-8. e.g., [^abc] matches \\xFF, which is not valid"] # [doc = "     // UTF-8. If we left Unicode enabled, [^abc] would match any UTF-8"] # [doc = "     // encoding of any Unicode scalar value except for 'a', 'b' or 'c'."] # [doc = "     // That translates to a much more complicated DFA, and also"] # [doc = "     // inhibits the 'accelerator' optimization that we are trying to"] # [doc = "     // demonstrate in this example."] # [doc = "     .syntax(syntax::Config::new().unicode(false).utf8(false))"] # [doc = "     .build(\"[^abc]+a\")?;"] # [doc = ""] # [doc = " // Here we just pluck out the state that we know is accelerated."] # [doc = " // While the stride calculations are something that can be relied"] # [doc = " // on by callers, the specific position of the accelerated state is"] # [doc = " // implementation defined."] # [doc = " //"] # [doc = " // N.B. We get '3' by inspecting the state machine using 'regex-cli'."] # [doc = " // e.g., try `regex-cli debug dense dfa -p '[^abc]+a' -BbUC`."] # [doc = " let id = StateID::new(3 * dfa.stride()).unwrap();"] # [doc = " let accelerator = dfa.accelerator(id);"] # [doc = " // The `[^abc]+` sub-expression permits [a, b, c] to be accelerated."] # [doc = " assert_eq!(accelerator, &[b'a', b'b', b'c']);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] fn accelerator (& self , _id : StateID) -> & [u8] { & [] } # [doc = " Returns the prefilter associated with a DFA, if one exists."] # [doc = ""] # [doc = " The default implementation of this trait always returns `None`. And"] # [doc = " indeed, it is always correct to return `None`."] # [doc = ""] # [doc = " For DFAs in this crate, a prefilter can be attached to a DFA via"] # [doc = " [`dense::Config::prefilter`](crate::dfa::dense::Config::prefilter)."] # [doc = ""] # [doc = " Do note that prefilters are not serialized by DFAs in this crate."] # [doc = " So if you deserialize a DFA that had a prefilter attached to it"] # [doc = " at serialization time, then it will not have a prefilter after"] # [doc = " deserialization."] # [inline] fn get_prefilter (& self) -> Option < & Prefilter > { None } # [doc = " Executes a forward search and returns the end position of the leftmost"] # [doc = " match that is found. If no match exists, then `None` is returned."] # [doc = ""] # [doc = " In particular, this method continues searching even after it enters"] # [doc = " a match state. The search only terminates once it has reached the"] # [doc = " end of the input or when it has entered a dead or quit state. Upon"] # [doc = " termination, the position of the last byte seen while still in a match"] # [doc = " state is returned."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine errors if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the DFA quitting."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search returns an error, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Notes for implementors"] # [doc = ""] # [doc = " Implementors of this trait are not required to implement any particular"] # [doc = " match semantics (such as leftmost-first), which are instead manifest in"] # [doc = " the DFA's transitions. But this search routine should behave as a"] # [doc = " general \"leftmost\" search."] # [doc = ""] # [doc = " In particular, this method must continue searching even after it enters"] # [doc = " a match state. The search should only terminate once it has reached"] # [doc = " the end of the input or when it has entered a dead or quit state. Upon"] # [doc = " termination, the position of the last byte seen while still in a match"] # [doc = " state is returned."] # [doc = ""] # [doc = " Since this trait provides an implementation for this method by default,"] # [doc = " it's unlikely that one will need to implement this."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use this method with a"] # [doc = " [`dense::DFA`](crate::dfa::dense::DFA)."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, dense}, HalfMatch, Input};"] # [doc = ""] # [doc = " let dfa = dense::DFA::new(\"foo[0-9]+\")?;"] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(b\"foo12345\"))?);"] # [doc = ""] # [doc = " // Even though a match is found after reading the first byte (`a`),"] # [doc = " // the leftmost first match semantics demand that we find the earliest"] # [doc = " // match that prefers earlier parts of the pattern over latter parts."] # [doc = " let dfa = dense::DFA::new(\"abc|a\")?;"] # [doc = " let expected = Some(HalfMatch::must(0, 3));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(b\"abc\"))?);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: specific pattern search"] # [doc = ""] # [doc = " This example shows how to build a multi-DFA that permits searching for"] # [doc = " specific patterns."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{Automaton, dense},"] # [doc = "     Anchored, HalfMatch, PatternID, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = dense::Builder::new()"] # [doc = "     .configure(dense::Config::new().starts_for_each_pattern(true))"] # [doc = "     .build_many(&[\"[a-z0-9]{6}\", \"[a-z][a-z0-9]{5}\"])?;"] # [doc = " let haystack = \"foo123\".as_bytes();"] # [doc = ""] # [doc = " // Since we are using the default leftmost-first match and both"] # [doc = " // patterns match at the same starting position, only the first pattern"] # [doc = " // will be returned in this case when doing a search for any of the"] # [doc = " // patterns."] # [doc = " let expected = Some(HalfMatch::must(0, 6));"] # [doc = " let got = dfa.try_search_fwd(&Input::new(haystack))?;"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " // But if we want to check whether some other pattern matches, then we"] # [doc = " // can provide its pattern ID."] # [doc = " let input = Input::new(haystack)"] # [doc = "     .anchored(Anchored::Pattern(PatternID::must(1)));"] # [doc = " let expected = Some(HalfMatch::must(1, 6));"] # [doc = " let got = dfa.try_search_fwd(&input)?;"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: specifying the bounds of a search"] # [doc = ""] # [doc = " This example shows how providing the bounds of a search can produce"] # [doc = " different results than simply sub-slicing the haystack."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, dense}, HalfMatch, Input};"] # [doc = ""] # [doc = " // N.B. We disable Unicode here so that we use a simple ASCII word"] # [doc = " // boundary. Alternatively, we could enable heuristic support for"] # [doc = " // Unicode word boundaries."] # [doc = " let dfa = dense::DFA::new(r\"(?-u)\\b[0-9]{3}\\b\")?;"] # [doc = " let haystack = \"foo123bar\".as_bytes();"] # [doc = ""] # [doc = " // Since we sub-slice the haystack, the search doesn't know about the"] # [doc = " // larger context and assumes that `123` is surrounded by word"] # [doc = " // boundaries. And of course, the match position is reported relative"] # [doc = " // to the sub-slice as well, which means we get `3` instead of `6`."] # [doc = " let input = Input::new(&haystack[3..6]);"] # [doc = " let expected = Some(HalfMatch::must(0, 3));"] # [doc = " let got = dfa.try_search_fwd(&input)?;"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " // But if we provide the bounds of the search within the context of the"] # [doc = " // entire haystack, then the search can take the surrounding context"] # [doc = " // into account. (And if we did find a match, it would be reported"] # [doc = " // as a valid offset into `haystack` instead of its sub-slice.)"] # [doc = " let input = Input::new(haystack).range(3..6);"] # [doc = " let expected = None;"] # [doc = " let got = dfa.try_search_fwd(&input)?;"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] fn try_search_fwd (& self , input : & Input < '_ > ,) -> Result < Option < HalfMatch > , MatchError > { let utf8empty = self . has_empty () && self . is_utf8 () ; let hm = match search :: find_fwd (& self , input) ? { None => return Ok (None) , Some (hm) if ! utf8empty => return Ok (Some (hm)) , Some (hm) => hm , } ; empty :: skip_splits_fwd (input , hm , hm . offset () , | input | { let got = search :: find_fwd (& self , input) ? ; Ok (got . map (| hm | (hm , hm . offset ()))) }) } # [doc = " Executes a reverse search and returns the start of the position of the"] # [doc = " leftmost match that is found. If no match exists, then `None` is"] # [doc = " returned."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine errors if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the DFA quitting."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search returns an error, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use this method with a"] # [doc = " [`dense::DFA`](crate::dfa::dense::DFA). In particular, this"] # [doc = " routine is principally useful when used in conjunction with the"] # [doc = " [`nfa::thompson::Config::reverse`](crate::nfa::thompson::Config::reverse)"] # [doc = " configuration. In general, it's unlikely to be correct to use"] # [doc = " both `try_search_fwd` and `try_search_rev` with the same DFA since"] # [doc = " any particular DFA will only support searching in one direction with"] # [doc = " respect to the pattern."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson,"] # [doc = "     dfa::{Automaton, dense},"] # [doc = "     HalfMatch, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = dense::Builder::new()"] # [doc = "     .thompson(thompson::Config::new().reverse(true))"] # [doc = "     .build(\"foo[0-9]+\")?;"] # [doc = " let expected = Some(HalfMatch::must(0, 0));"] # [doc = " assert_eq!(expected, dfa.try_search_rev(&Input::new(b\"foo12345\"))?);"] # [doc = ""] # [doc = " // Even though a match is found after reading the last byte (`c`),"] # [doc = " // the leftmost first match semantics demand that we find the earliest"] # [doc = " // match that prefers earlier parts of the pattern over latter parts."] # [doc = " let dfa = dense::Builder::new()"] # [doc = "     .thompson(thompson::Config::new().reverse(true))"] # [doc = "     .build(\"abc|c\")?;"] # [doc = " let expected = Some(HalfMatch::must(0, 0));"] # [doc = " assert_eq!(expected, dfa.try_search_rev(&Input::new(b\"abc\"))?);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: UTF-8 mode"] # [doc = ""] # [doc = " This examples demonstrates that UTF-8 mode applies to reverse"] # [doc = " DFAs. When UTF-8 mode is enabled in the underlying NFA, then all"] # [doc = " matches reported must correspond to valid UTF-8 spans. This includes"] # [doc = " prohibiting zero-width matches that split a codepoint."] # [doc = ""] # [doc = " UTF-8 mode is enabled by default. Notice below how the only zero-width"] # [doc = " matches reported are those at UTF-8 boundaries:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{dense::DFA, Automaton},"] # [doc = "     nfa::thompson,"] # [doc = "     HalfMatch, Input, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .thompson(thompson::Config::new().reverse(true))"] # [doc = "     .build(r\"\")?;"] # [doc = ""] # [doc = " // Run the reverse DFA to collect all matches."] # [doc = " let mut input = Input::new(\"☃\");"] # [doc = " let mut matches = vec![];"] # [doc = " loop {"] # [doc = "     match dfa.try_search_rev(&input)? {"] # [doc = "         None => break,"] # [doc = "         Some(hm) => {"] # [doc = "             matches.push(hm);"] # [doc = "             if hm.offset() == 0 || input.end() == 0 {"] # [doc = "                 break;"] # [doc = "             } else if hm.offset() < input.end() {"] # [doc = "                 input.set_end(hm.offset());"] # [doc = "             } else {"] # [doc = "                 // This is only necessary to handle zero-width"] # [doc = "                 // matches, which of course occur in this example."] # [doc = "                 // Without this, the search would never advance"] # [doc = "                 // backwards beyond the initial match."] # [doc = "                 input.set_end(input.end() - 1);"] # [doc = "             }"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // No matches split a codepoint."] # [doc = " let expected = vec!["] # [doc = "     HalfMatch::must(0, 3),"] # [doc = "     HalfMatch::must(0, 0),"] # [doc = " ];"] # [doc = " assert_eq!(expected, matches);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Now let's look at the same example, but with UTF-8 mode on the"] # [doc = " original NFA disabled (which results in disabling UTF-8 mode on the"] # [doc = " DFA):"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{dense::DFA, Automaton},"] # [doc = "     nfa::thompson,"] # [doc = "     HalfMatch, Input, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .thompson(thompson::Config::new().reverse(true).utf8(false))"] # [doc = "     .build(r\"\")?;"] # [doc = ""] # [doc = " // Run the reverse DFA to collect all matches."] # [doc = " let mut input = Input::new(\"☃\");"] # [doc = " let mut matches = vec![];"] # [doc = " loop {"] # [doc = "     match dfa.try_search_rev(&input)? {"] # [doc = "         None => break,"] # [doc = "         Some(hm) => {"] # [doc = "             matches.push(hm);"] # [doc = "             if hm.offset() == 0 || input.end() == 0 {"] # [doc = "                 break;"] # [doc = "             } else if hm.offset() < input.end() {"] # [doc = "                 input.set_end(hm.offset());"] # [doc = "             } else {"] # [doc = "                 // This is only necessary to handle zero-width"] # [doc = "                 // matches, which of course occur in this example."] # [doc = "                 // Without this, the search would never advance"] # [doc = "                 // backwards beyond the initial match."] # [doc = "                 input.set_end(input.end() - 1);"] # [doc = "             }"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // No matches split a codepoint."] # [doc = " let expected = vec!["] # [doc = "     HalfMatch::must(0, 3),"] # [doc = "     HalfMatch::must(0, 2),"] # [doc = "     HalfMatch::must(0, 1),"] # [doc = "     HalfMatch::must(0, 0),"] # [doc = " ];"] # [doc = " assert_eq!(expected, matches);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] fn try_search_rev (& self , input : & Input < '_ > ,) -> Result < Option < HalfMatch > , MatchError > { let utf8empty = self . has_empty () && self . is_utf8 () ; let hm = match search :: find_rev (self , input) ? { None => return Ok (None) , Some (hm) if ! utf8empty => return Ok (Some (hm)) , Some (hm) => hm , } ; empty :: skip_splits_rev (input , hm , hm . offset () , | input | { let got = search :: find_rev (self , input) ? ; Ok (got . map (| hm | (hm , hm . offset ()))) }) } # [doc = " Executes an overlapping forward search. Matches, if one exists, can be"] # [doc = " obtained via the [`OverlappingState::get_match`] method."] # [doc = ""] # [doc = " This routine is principally only useful when searching for multiple"] # [doc = " patterns on inputs where multiple patterns may match the same regions"] # [doc = " of text. In particular, callers must preserve the automaton's search"] # [doc = " state from prior calls so that the implementation knows where the last"] # [doc = " match occurred."] # [doc = ""] # [doc = " When using this routine to implement an iterator of overlapping"] # [doc = " matches, the `start` of the search should always be set to the end"] # [doc = " of the last match. If more patterns match at the previous location,"] # [doc = " then they will be immediately returned. (This is tracked by the given"] # [doc = " overlapping state.) Otherwise, the search continues at the starting"] # [doc = " position given."] # [doc = ""] # [doc = " If for some reason you want the search to forget about its previous"] # [doc = " state and restart the search at a particular position, then setting the"] # [doc = " state to [`OverlappingState::start`] will accomplish that."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine errors if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the DFA quitting."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search returns an error, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to run a basic overlapping search with a"] # [doc = " [`dense::DFA`](crate::dfa::dense::DFA). Notice that we build the"] # [doc = " automaton with a `MatchKind::All` configuration. Overlapping searches"] # [doc = " are unlikely to work as one would expect when using the default"] # [doc = " `MatchKind::LeftmostFirst` match semantics, since leftmost-first"] # [doc = " matching is fundamentally incompatible with overlapping searches."] # [doc = " Namely, overlapping searches need to report matches as they are seen,"] # [doc = " where as leftmost-first searches will continue searching even after a"] # [doc = " match has been observed in order to find the conventional end position"] # [doc = " of the match. More concretely, leftmost-first searches use dead states"] # [doc = " to terminate a search after a specific match can no longer be extended."] # [doc = " Overlapping searches instead do the opposite by continuing the search"] # [doc = " to find totally new matches (potentially of other patterns)."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{Automaton, OverlappingState, dense},"] # [doc = "     HalfMatch, Input, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = dense::Builder::new()"] # [doc = "     .configure(dense::Config::new().match_kind(MatchKind::All))"] # [doc = "     .build_many(&[r\"[[:word:]]+$\", r\"[[:^space:]]+$\"])?;"] # [doc = " let haystack = \"@foo\";"] # [doc = " let mut state = OverlappingState::start();"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(1, 4));"] # [doc = " dfa.try_search_overlapping_fwd(&Input::new(haystack), &mut state)?;"] # [doc = " assert_eq!(expected, state.get_match());"] # [doc = ""] # [doc = " // The first pattern also matches at the same position, so re-running"] # [doc = " // the search will yield another match. Notice also that the first"] # [doc = " // pattern is returned after the second. This is because the second"] # [doc = " // pattern begins its match before the first, is therefore an earlier"] # [doc = " // match and is thus reported first."] # [doc = " let expected = Some(HalfMatch::must(0, 4));"] # [doc = " dfa.try_search_overlapping_fwd(&Input::new(haystack), &mut state)?;"] # [doc = " assert_eq!(expected, state.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] fn try_search_overlapping_fwd (& self , input : & Input < '_ > , state : & mut OverlappingState ,) -> Result < () , MatchError > { let utf8empty = self . has_empty () && self . is_utf8 () ; search :: find_overlapping_fwd (self , input , state) ? ; match state . get_match () { None => Ok (()) , Some (_) if ! utf8empty => Ok (()) , Some (_) => skip_empty_utf8_splits_overlapping (input , state , | input , state | { search :: find_overlapping_fwd (self , input , state) } ,) , } } # [doc = " Executes a reverse overlapping forward search. Matches, if one exists,"] # [doc = " can be obtained via the [`OverlappingState::get_match`] method."] # [doc = ""] # [doc = " When using this routine to implement an iterator of overlapping"] # [doc = " matches, the `start` of the search should remain invariant throughout"] # [doc = " iteration. The `OverlappingState` given to the search will keep track"] # [doc = " of the current position of the search. (This is because multiple"] # [doc = " matches may be reported at the same position, so only the search"] # [doc = " implementation itself knows when to advance the position.)"] # [doc = ""] # [doc = " If for some reason you want the search to forget about its previous"] # [doc = " state and restart the search at a particular position, then setting the"] # [doc = " state to [`OverlappingState::start`] will accomplish that."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine errors if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the DFA quitting."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search returns an error, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Example: UTF-8 mode"] # [doc = ""] # [doc = " This examples demonstrates that UTF-8 mode applies to reverse"] # [doc = " DFAs. When UTF-8 mode is enabled in the underlying NFA, then all"] # [doc = " matches reported must correspond to valid UTF-8 spans. This includes"] # [doc = " prohibiting zero-width matches that split a codepoint."] # [doc = ""] # [doc = " UTF-8 mode is enabled by default. Notice below how the only zero-width"] # [doc = " matches reported are those at UTF-8 boundaries:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{dense::DFA, Automaton, OverlappingState},"] # [doc = "     nfa::thompson,"] # [doc = "     HalfMatch, Input, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .configure(DFA::config().match_kind(MatchKind::All))"] # [doc = "     .thompson(thompson::Config::new().reverse(true))"] # [doc = "     .build_many(&[r\"\", r\"☃\"])?;"] # [doc = ""] # [doc = " // Run the reverse DFA to collect all matches."] # [doc = " let input = Input::new(\"☃\");"] # [doc = " let mut state = OverlappingState::start();"] # [doc = " let mut matches = vec![];"] # [doc = " loop {"] # [doc = "     dfa.try_search_overlapping_rev(&input, &mut state)?;"] # [doc = "     match state.get_match() {"] # [doc = "         None => break,"] # [doc = "         Some(hm) => matches.push(hm),"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // No matches split a codepoint."] # [doc = " let expected = vec!["] # [doc = "     HalfMatch::must(0, 3),"] # [doc = "     HalfMatch::must(1, 0),"] # [doc = "     HalfMatch::must(0, 0),"] # [doc = " ];"] # [doc = " assert_eq!(expected, matches);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Now let's look at the same example, but with UTF-8 mode on the"] # [doc = " original NFA disabled (which results in disabling UTF-8 mode on the"] # [doc = " DFA):"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{dense::DFA, Automaton, OverlappingState},"] # [doc = "     nfa::thompson,"] # [doc = "     HalfMatch, Input, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = DFA::builder()"] # [doc = "     .configure(DFA::config().match_kind(MatchKind::All))"] # [doc = "     .thompson(thompson::Config::new().reverse(true).utf8(false))"] # [doc = "     .build_many(&[r\"\", r\"☃\"])?;"] # [doc = ""] # [doc = " // Run the reverse DFA to collect all matches."] # [doc = " let input = Input::new(\"☃\");"] # [doc = " let mut state = OverlappingState::start();"] # [doc = " let mut matches = vec![];"] # [doc = " loop {"] # [doc = "     dfa.try_search_overlapping_rev(&input, &mut state)?;"] # [doc = "     match state.get_match() {"] # [doc = "         None => break,"] # [doc = "         Some(hm) => matches.push(hm),"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // Now *all* positions match, even within a codepoint,"] # [doc = " // because we lifted the requirement that matches"] # [doc = " // correspond to valid UTF-8 spans."] # [doc = " let expected = vec!["] # [doc = "     HalfMatch::must(0, 3),"] # [doc = "     HalfMatch::must(0, 2),"] # [doc = "     HalfMatch::must(0, 1),"] # [doc = "     HalfMatch::must(1, 0),"] # [doc = "     HalfMatch::must(0, 0),"] # [doc = " ];"] # [doc = " assert_eq!(expected, matches);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] fn try_search_overlapping_rev (& self , input : & Input < '_ > , state : & mut OverlappingState ,) -> Result < () , MatchError > { let utf8empty = self . has_empty () && self . is_utf8 () ; search :: find_overlapping_rev (self , input , state) ? ; match state . get_match () { None => Ok (()) , Some (_) if ! utf8empty => Ok (()) , Some (_) => skip_empty_utf8_splits_overlapping (input , state , | input , state | { search :: find_overlapping_rev (self , input , state) } ,) , } } # [doc = " Writes the set of patterns that match anywhere in the given search"] # [doc = " configuration to `patset`. If multiple patterns match at the same"] # [doc = " position and the underlying DFA supports overlapping matches, then all"] # [doc = " matching patterns are written to the given set."] # [doc = ""] # [doc = " Unless all of the patterns in this DFA are anchored, then generally"] # [doc = " speaking, this will visit every byte in the haystack."] # [doc = ""] # [doc = " This search routine *does not* clear the pattern set. This gives some"] # [doc = " flexibility to the caller (e.g., running multiple searches with the"] # [doc = " same pattern set), but does make the API bug-prone if you're reusing"] # [doc = " the same pattern set for multiple searches but intended them to be"] # [doc = " independent."] # [doc = ""] # [doc = " If a pattern ID matched but the given `PatternSet` does not have"] # [doc = " sufficient capacity to store it, then it is not inserted and silently"] # [doc = " dropped."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine errors if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the DFA quitting."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search returns an error, callers cannot know whether a match"] # [doc = " exists or not."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to find all matching patterns in a haystack,"] # [doc = " even when some patterns match at the same position as other patterns."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{Automaton, dense::DFA},"] # [doc = "     Input, MatchKind, PatternSet,"] # [doc = " };"] # [doc = ""] # [doc = " let patterns = &["] # [doc = "     r\"[[:word:]]+\","] # [doc = "     r\"[0-9]+\","] # [doc = "     r\"[[:alpha:]]+\","] # [doc = "     r\"foo\","] # [doc = "     r\"bar\","] # [doc = "     r\"barfoo\","] # [doc = "     r\"foobar\","] # [doc = " ];"] # [doc = " let dfa = DFA::builder()"] # [doc = "     .configure(DFA::config().match_kind(MatchKind::All))"] # [doc = "     .build_many(patterns)?;"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\");"] # [doc = " let mut patset = PatternSet::new(dfa.pattern_len());"] # [doc = " dfa.try_which_overlapping_matches(&input, &mut patset)?;"] # [doc = " let expected = vec![0, 2, 3, 4, 6];"] # [doc = " let got: Vec<usize> = patset.iter().map(|p| p.as_usize()).collect();"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "alloc")] # [inline] fn try_which_overlapping_matches (& self , input : & Input < '_ > , patset : & mut PatternSet ,) -> Result < () , MatchError > { let mut state = OverlappingState :: start () ; while let Some (m) = { self . try_search_overlapping_fwd (input , & mut state) ? ; state . get_match () } { let _ = patset . insert (m . pattern ()) ; if patset . is_full () || input . get_earliest () { break ; } } Ok (()) } }
    };
}

Automaton!()