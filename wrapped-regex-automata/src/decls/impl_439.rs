macro_rules! deps {
    () => {
        Prefilter!();
        MatchKind!();
        HalfMatch!();
        Input!();
        NFA!();
        Match!();
        Config!();
    };
}

macro_rules! impl_439 {
    () => {
        deps!();
        impl Config { # [doc = " Return a new default regex configuration."] pub fn new () -> Config { Config :: default () } # [doc = " Set a prefilter to be used whenever a start state is entered."] # [doc = ""] # [doc = " A [`Prefilter`] in this context is meant to accelerate searches by"] # [doc = " looking for literal prefixes that every match for the corresponding"] # [doc = " pattern (or patterns) must start with. Once a prefilter produces a"] # [doc = " match, the underlying search routine continues on to try and confirm"] # [doc = " the match."] # [doc = ""] # [doc = " Be warned that setting a prefilter does not guarantee that the search"] # [doc = " will be faster. While it's usually a good bet, if the prefilter"] # [doc = " produces a lot of false positive candidates (i.e., positions matched"] # [doc = " by the prefilter but not by the regex), then the overall result can"] # [doc = " be slower than if you had just executed the regex engine without any"] # [doc = " prefilters."] # [doc = ""] # [doc = " By default no prefilter is set."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     util::prefilter::Prefilter,"] # [doc = "     Input, Match, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let pre = Prefilter::new(MatchKind::LeftmostFirst, &[\"foo\", \"bar\"]);"] # [doc = " let re = BoundedBacktracker::builder()"] # [doc = "     .configure(BoundedBacktracker::config().prefilter(pre))"] # [doc = "     .build(r\"(foo|bar)[a-z]+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let input = Input::new(\"foo1 barfox bar\");"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 5..11)),"] # [doc = "     re.try_find(&mut cache, input)?,"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Be warned though that an incorrect prefilter can lead to incorrect"] # [doc = " results!"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     util::prefilter::Prefilter,"] # [doc = "     Input, HalfMatch, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let pre = Prefilter::new(MatchKind::LeftmostFirst, &[\"foo\", \"car\"]);"] # [doc = " let re = BoundedBacktracker::builder()"] # [doc = "     .configure(BoundedBacktracker::config().prefilter(pre))"] # [doc = "     .build(r\"(foo|bar)[a-z]+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let input = Input::new(\"foo1 barfox bar\");"] # [doc = " // No match reported even though there clearly is one!"] # [doc = " assert_eq!(None, re.try_find(&mut cache, input)?);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn prefilter (mut self , pre : Option < Prefilter >) -> Config { self . pre = Some (pre) ; self } # [doc = " Set the visited capacity used to bound backtracking."] # [doc = ""] # [doc = " The visited capacity represents the amount of heap memory (in bytes) to"] # [doc = " allocate toward tracking which parts of the backtracking search have"] # [doc = " been done before. The heap memory needed for any particular search is"] # [doc = " proportional to `haystack.len() * nfa.states().len()`, which an be"] # [doc = " quite large. Therefore, the bounded backtracker is typically only able"] # [doc = " to run on shorter haystacks."] # [doc = ""] # [doc = " For a given regex, increasing the visited capacity means that the"] # [doc = " maximum haystack length that can be searched is increased. The"] # [doc = " [`BoundedBacktracker::max_haystack_len`] method returns that maximum."] # [doc = ""] # [doc = " The default capacity is a reasonable but empirically chosen size."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " As with other regex engines, Unicode is what tends to make the bounded"] # [doc = " backtracker less useful by making the maximum haystack length quite"] # [doc = " small. If necessary, increasing the visited capacity using this routine"] # [doc = " will increase the maximum haystack length at the cost of using more"] # [doc = " memory."] # [doc = ""] # [doc = " Note though that the specific maximum values here are not an API"] # [doc = " guarantee. The default visited capacity is subject to change and not"] # [doc = " covered by semver."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::nfa::thompson::backtrack::BoundedBacktracker;"] # [doc = ""] # [doc = " // Unicode inflates the size of the underlying NFA quite a bit, and"] # [doc = " // thus means that the backtracker can only handle smaller haystacks,"] # [doc = " // assuming that the visited capacity remains unchanged."] # [doc = " let re = BoundedBacktracker::new(r\"\\w+\")?;"] # [doc = " assert!(re.max_haystack_len() <= 7_000);"] # [doc = " // But we can increase the visited capacity to handle bigger haystacks!"] # [doc = " let re = BoundedBacktracker::builder()"] # [doc = "     .configure(BoundedBacktracker::config().visited_capacity(1<<20))"] # [doc = "     .build(r\"\\w+\")?;"] # [doc = " assert!(re.max_haystack_len() >= 25_000);"] # [doc = " assert!(re.max_haystack_len() <= 28_000);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn visited_capacity (mut self , capacity : usize) -> Config { self . visited_capacity = Some (capacity) ; self } # [doc = " Returns the prefilter set in this configuration, if one at all."] pub fn get_prefilter (& self) -> Option < & Prefilter > { self . pre . as_ref () . unwrap_or (& None) . as_ref () } # [doc = " Returns the configured visited capacity."] # [doc = ""] # [doc = " Note that the actual capacity used may be slightly bigger than the"] # [doc = " configured capacity."] pub fn get_visited_capacity (& self) -> usize { const DEFAULT : usize = 256 * (1 << 10) ; self . visited_capacity . unwrap_or (DEFAULT) } # [doc = " Overwrite the default configuration such that the options in `o` are"] # [doc = " always used. If an option in `o` is not set, then the corresponding"] # [doc = " option in `self` is used. If it's not set in `self` either, then it"] # [doc = " remains not set."] pub (crate) fn overwrite (& self , o : Config) -> Config { Config { pre : o . pre . or_else (| | self . pre . clone ()) , visited_capacity : o . visited_capacity . or (self . visited_capacity) , } } }
    };
}

impl_439!();