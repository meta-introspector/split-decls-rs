macro_rules! deps {
    () => {
        Captures!();
        BuildError!();
        MatchError!();
        Cache!();
        BoundedBacktracker!();
        Match!();
        Visited!();
        NFA!();
        Builder!();
        Config!();
    };
}

macro_rules! impl_443 {
    () => {
        deps!();
        impl BoundedBacktracker { # [doc = " Parse the given regular expression using the default configuration and"] # [doc = " return the corresponding `BoundedBacktracker`."] # [doc = ""] # [doc = " If you want a non-default configuration, then use the [`Builder`] to"] # [doc = " set your own configuration."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = BoundedBacktracker::new(\"foo[0-9]+bar\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " assert_eq!("] # [doc = "     Some(Ok(Match::must(0, 3..14))),"] # [doc = "     re.try_find_iter(&mut cache, \"zzzfoo12345barzzz\").next(),"] # [doc = " );"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] pub fn new (pattern : & str) -> Result < BoundedBacktracker , BuildError > { BoundedBacktracker :: builder () . build (pattern) } # [doc = " Like `new`, but parses multiple patterns into a single \"multi regex.\""] # [doc = " This similarly uses the default regex configuration."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = BoundedBacktracker::new_many(&[\"[a-z]+\", \"[0-9]+\"])?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let mut it = re.try_find_iter(&mut cache, \"abc 1 foo 4567 0 quux\");"] # [doc = " assert_eq!(Some(Ok(Match::must(0, 0..3))), it.next());"] # [doc = " assert_eq!(Some(Ok(Match::must(1, 4..5))), it.next());"] # [doc = " assert_eq!(Some(Ok(Match::must(0, 6..9))), it.next());"] # [doc = " assert_eq!(Some(Ok(Match::must(1, 10..14))), it.next());"] # [doc = " assert_eq!(Some(Ok(Match::must(1, 15..16))), it.next());"] # [doc = " assert_eq!(Some(Ok(Match::must(0, 17..21))), it.next());"] # [doc = " assert_eq!(None, it.next());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] pub fn new_many < P : AsRef < str > > (patterns : & [P] ,) -> Result < BoundedBacktracker , BuildError > { BoundedBacktracker :: builder () . build_many (patterns) } # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to hand assemble a regular expression via its HIR,"] # [doc = " compile an NFA from it and build a BoundedBacktracker from the NFA."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::{NFA, backtrack::BoundedBacktracker},"] # [doc = "     Match,"] # [doc = " };"] # [doc = " use regex_syntax::hir::{Hir, Class, ClassBytes, ClassBytesRange};"] # [doc = ""] # [doc = " let hir = Hir::class(Class::Bytes(ClassBytes::new(vec!["] # [doc = "     ClassBytesRange::new(b'0', b'9'),"] # [doc = "     ClassBytesRange::new(b'A', b'Z'),"] # [doc = "     ClassBytesRange::new(b'_', b'_'),"] # [doc = "     ClassBytesRange::new(b'a', b'z'),"] # [doc = " ])));"] # [doc = ""] # [doc = " let config = NFA::config().nfa_size_limit(Some(1_000));"] # [doc = " let nfa = NFA::compiler().configure(config).build_from_hir(&hir)?;"] # [doc = ""] # [doc = " let re = BoundedBacktracker::new_from_nfa(nfa)?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = " let expected = Some(Match::must(0, 3..4));"] # [doc = " re.try_captures(&mut cache, \"!@#A#@!\", &mut caps)?;"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn new_from_nfa (nfa : NFA) -> Result < BoundedBacktracker , BuildError > { BoundedBacktracker :: builder () . build_from_nfa (nfa) } # [doc = " Create a new `BoundedBacktracker` that matches every input."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = BoundedBacktracker::always_match()?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let expected = Some(Ok(Match::must(0, 0..0)));"] # [doc = " assert_eq!(expected, re.try_find_iter(&mut cache, \"\").next());"] # [doc = " assert_eq!(expected, re.try_find_iter(&mut cache, \"foo\").next());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn always_match () -> Result < BoundedBacktracker , BuildError > { let nfa = thompson :: NFA :: always_match () ; BoundedBacktracker :: new_from_nfa (nfa) } # [doc = " Create a new `BoundedBacktracker` that never matches any input."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::backtrack::BoundedBacktracker;"] # [doc = ""] # [doc = " let re = BoundedBacktracker::never_match()?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " assert_eq!(None, re.try_find_iter(&mut cache, \"\").next());"] # [doc = " assert_eq!(None, re.try_find_iter(&mut cache, \"foo\").next());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn never_match () -> Result < BoundedBacktracker , BuildError > { let nfa = thompson :: NFA :: never_match () ; BoundedBacktracker :: new_from_nfa (nfa) } # [doc = " Return a default configuration for a `BoundedBacktracker`."] # [doc = ""] # [doc = " This is a convenience routine to avoid needing to import the `Config`"] # [doc = " type when customizing the construction of a `BoundedBacktracker`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to disable UTF-8 mode. When UTF-8 mode is"] # [doc = " disabled, zero-width matches that split a codepoint are allowed."] # [doc = " Otherwise they are never reported."] # [doc = ""] # [doc = " In the code below, notice that `\"\"` is permitted to match positions"] # [doc = " that split the encoding of a codepoint."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::{self, backtrack::BoundedBacktracker},"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = BoundedBacktracker::builder()"] # [doc = "     .thompson(thompson::Config::new().utf8(false))"] # [doc = "     .build(r\"\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let haystack = \"a☃z\";"] # [doc = " let mut it = re.try_find_iter(&mut cache, haystack);"] # [doc = " assert_eq!(Some(Ok(Match::must(0, 0..0))), it.next());"] # [doc = " assert_eq!(Some(Ok(Match::must(0, 1..1))), it.next());"] # [doc = " assert_eq!(Some(Ok(Match::must(0, 2..2))), it.next());"] # [doc = " assert_eq!(Some(Ok(Match::must(0, 3..3))), it.next());"] # [doc = " assert_eq!(Some(Ok(Match::must(0, 4..4))), it.next());"] # [doc = " assert_eq!(Some(Ok(Match::must(0, 5..5))), it.next());"] # [doc = " assert_eq!(None, it.next());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn config () -> Config { Config :: new () } # [doc = " Return a builder for configuring the construction of a"] # [doc = " `BoundedBacktracker`."] # [doc = ""] # [doc = " This is a convenience routine to avoid needing to import the"] # [doc = " [`Builder`] type in common cases."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use the builder to disable UTF-8 mode"] # [doc = " everywhere."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::{self, backtrack::BoundedBacktracker},"] # [doc = "     util::syntax,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = BoundedBacktracker::builder()"] # [doc = "     .syntax(syntax::Config::new().utf8(false))"] # [doc = "     .thompson(thompson::Config::new().utf8(false))"] # [doc = "     .build(r\"foo(?-u:[^b])ar.*\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " let haystack = b\"\\xFEfoo\\xFFarzz\\xE2\\x98\\xFF\\n\";"] # [doc = " let expected = Some(Match::must(0, 1..9));"] # [doc = " re.try_captures(&mut cache, haystack, &mut caps)?;"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn builder () -> Builder { Builder :: new () } # [doc = " Create a new cache for this regex."] # [doc = ""] # [doc = " The cache returned should only be used for searches for this"] # [doc = " regex. If you want to reuse the cache for another regex, then you"] # [doc = " must call [`Cache::reset`] with that regex (or, equivalently,"] # [doc = " [`BoundedBacktracker::reset_cache`])."] pub fn create_cache (& self) -> Cache { Cache :: new (self) } # [doc = " Create a new empty set of capturing groups that is guaranteed to be"] # [doc = " valid for the search APIs on this `BoundedBacktracker`."] # [doc = ""] # [doc = " A `Captures` value created for a specific `BoundedBacktracker` cannot"] # [doc = " be used with any other `BoundedBacktracker`."] # [doc = ""] # [doc = " This is a convenience function for [`Captures::all`]. See the"] # [doc = " [`Captures`] documentation for an explanation of its alternative"] # [doc = " constructors that permit the `BoundedBacktracker` to do less work"] # [doc = " during a search, and thus might make it faster."] pub fn create_captures (& self) -> Captures { Captures :: all (self . get_nfa () . group_info () . clone ()) } # [doc = " Reset the given cache such that it can be used for searching with the"] # [doc = " this `BoundedBacktracker` (and only this `BoundedBacktracker`)."] # [doc = ""] # [doc = " A cache reset permits reusing memory already allocated in this cache"] # [doc = " with a different `BoundedBacktracker`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to re-purpose a cache for use with a different"] # [doc = " `BoundedBacktracker`."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re1 = BoundedBacktracker::new(r\"\\w\")?;"] # [doc = " let re2 = BoundedBacktracker::new(r\"\\W\")?;"] # [doc = ""] # [doc = " let mut cache = re1.create_cache();"] # [doc = " assert_eq!("] # [doc = "     Some(Ok(Match::must(0, 0..2))),"] # [doc = "     re1.try_find_iter(&mut cache, \"Δ\").next(),"] # [doc = " );"] # [doc = ""] # [doc = " // Using 'cache' with re2 is not allowed. It may result in panics or"] # [doc = " // incorrect results. In order to re-purpose the cache, we must reset"] # [doc = " // it with the BoundedBacktracker we'd like to use it with."] # [doc = " //"] # [doc = " // Similarly, after this reset, using the cache with 're1' is also not"] # [doc = " // allowed."] # [doc = " cache.reset(&re2);"] # [doc = " assert_eq!("] # [doc = "     Some(Ok(Match::must(0, 0..3))),"] # [doc = "     re2.try_find_iter(&mut cache, \"☃\").next(),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn reset_cache (& self , cache : & mut Cache) { cache . reset (self) ; } # [doc = " Returns the total number of patterns compiled into this"] # [doc = " `BoundedBacktracker`."] # [doc = ""] # [doc = " In the case of a `BoundedBacktracker` that contains no patterns, this"] # [doc = " returns `0`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the pattern length for a `BoundedBacktracker` that"] # [doc = " never matches:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::backtrack::BoundedBacktracker;"] # [doc = ""] # [doc = " let re = BoundedBacktracker::never_match()?;"] # [doc = " assert_eq!(re.pattern_len(), 0);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " And another example for a `BoundedBacktracker` that matches at every"] # [doc = " position:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::backtrack::BoundedBacktracker;"] # [doc = ""] # [doc = " let re = BoundedBacktracker::always_match()?;"] # [doc = " assert_eq!(re.pattern_len(), 1);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " And finally, a `BoundedBacktracker` that was constructed from multiple"] # [doc = " patterns:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::backtrack::BoundedBacktracker;"] # [doc = ""] # [doc = " let re = BoundedBacktracker::new_many(&[\"[0-9]+\", \"[a-z]+\", \"[A-Z]+\"])?;"] # [doc = " assert_eq!(re.pattern_len(), 3);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn pattern_len (& self) -> usize { self . nfa . pattern_len () } # [doc = " Return the config for this `BoundedBacktracker`."] # [inline] pub fn get_config (& self) -> & Config { & self . config } # [doc = " Returns a reference to the underlying NFA."] # [inline] pub fn get_nfa (& self) -> & NFA { & self . nfa } # [doc = " Returns the maximum haystack length supported by this backtracker."] # [doc = ""] # [doc = " This routine is a function of both [`Config::visited_capacity`] and the"] # [doc = " internal size of the backtracker's NFA."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how the maximum haystack length can vary depending"] # [doc = " on the size of the regex itself. Note though that the specific maximum"] # [doc = " values here are not an API guarantee. The default visited capacity is"] # [doc = " subject to change and not covered by semver."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     Match, MatchError,"] # [doc = " };"] # [doc = ""] # [doc = " // If you're only using ASCII, you get a big budget."] # [doc = " let re = BoundedBacktracker::new(r\"(?-u)\\w+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " assert_eq!(re.max_haystack_len(), 299_592);"] # [doc = " // Things work up to the max."] # [doc = " let mut haystack = \"a\".repeat(299_592);"] # [doc = " let expected = Some(Ok(Match::must(0, 0..299_592)));"] # [doc = " assert_eq!(expected, re.try_find_iter(&mut cache, &haystack).next());"] # [doc = " // But you'll get an error if you provide a haystack that's too big."] # [doc = " // Notice that we use the 'try_find_iter' routine instead, which"] # [doc = " // yields Result<Match, MatchError> instead of Match."] # [doc = " haystack.push('a');"] # [doc = " let expected = Some(Err(MatchError::haystack_too_long(299_593)));"] # [doc = " assert_eq!(expected, re.try_find_iter(&mut cache, &haystack).next());"] # [doc = ""] # [doc = " // Unicode inflates the size of the underlying NFA quite a bit, and"] # [doc = " // thus means that the backtracker can only handle smaller haystacks,"] # [doc = " // assuming that the visited capacity remains unchanged."] # [doc = " let re = BoundedBacktracker::new(r\"\\w+\")?;"] # [doc = " assert!(re.max_haystack_len() <= 7_000);"] # [doc = " // But we can increase the visited capacity to handle bigger haystacks!"] # [doc = " let re = BoundedBacktracker::builder()"] # [doc = "     .configure(BoundedBacktracker::config().visited_capacity(1<<20))"] # [doc = "     .build(r\"\\w+\")?;"] # [doc = " assert!(re.max_haystack_len() >= 25_000);"] # [doc = " assert!(re.max_haystack_len() <= 28_000);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn max_haystack_len (& self) -> usize { let capacity = 8 * self . get_config () . get_visited_capacity () ; let blocks = div_ceil (capacity , Visited :: BLOCK_SIZE) ; let real_capacity = blocks . saturating_mul (Visited :: BLOCK_SIZE) ; (real_capacity / self . nfa . states () . len ()) . saturating_sub (1) } }
    };
}

impl_443!();