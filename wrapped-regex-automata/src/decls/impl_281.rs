macro_rules! deps {
    () => {
        Cache!();
        DFA!();
        Regex!();
        Match!();
    };
}

macro_rules! impl_281 {
    () => {
        deps!();
        impl Cache { # [doc = " Create a new cache for the given `Regex`."] # [doc = ""] # [doc = " The cache returned should only be used for searches for the given"] # [doc = " `Regex`. If you want to reuse the cache for another `Regex`, then you"] # [doc = " must call [`Cache::reset`] with that `Regex`."] pub fn new (re : & Regex) -> Cache { let forward = dfa :: Cache :: new (re . forward ()) ; let reverse = dfa :: Cache :: new (re . reverse ()) ; Cache { forward , reverse } } # [doc = " Reset this cache such that it can be used for searching with the given"] # [doc = " `Regex` (and only that `Regex`)."] # [doc = ""] # [doc = " A cache reset permits reusing memory already allocated in this cache"] # [doc = " with a different `Regex`."] # [doc = ""] # [doc = " Resetting a cache sets its \"clear count\" to 0. This is relevant if the"] # [doc = " `Regex` has been configured to \"give up\" after it has cleared the cache"] # [doc = " a certain number of times."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to re-purpose a cache for use with a different `Regex`."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{hybrid::regex::Regex, Match};"] # [doc = ""] # [doc = " let re1 = Regex::new(r\"\\w\")?;"] # [doc = " let re2 = Regex::new(r\"\\W\")?;"] # [doc = ""] # [doc = " let mut cache = re1.create_cache();"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 0..2)),"] # [doc = "     re1.find(&mut cache, \"Δ\"),"] # [doc = " );"] # [doc = ""] # [doc = " // Using 'cache' with re2 is not allowed. It may result in panics or"] # [doc = " // incorrect results. In order to re-purpose the cache, we must reset"] # [doc = " // it with the Regex we'd like to use it with."] # [doc = " //"] # [doc = " // Similarly, after this reset, using the cache with 're1' is also not"] # [doc = " // allowed."] # [doc = " cache.reset(&re2);"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 0..3)),"] # [doc = "     re2.find(&mut cache, \"☃\"),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn reset (& mut self , re : & Regex) { self . forward . reset (re . forward ()) ; self . reverse . reset (re . reverse ()) ; } # [doc = " Return a reference to the forward cache."] pub fn forward (& mut self) -> & dfa :: Cache { & self . forward } # [doc = " Return a reference to the reverse cache."] pub fn reverse (& mut self) -> & dfa :: Cache { & self . reverse } # [doc = " Return a mutable reference to the forward cache."] # [doc = ""] # [doc = " If you need mutable references to both the forward and reverse caches,"] # [doc = " then use [`Cache::as_parts_mut`]."] pub fn forward_mut (& mut self) -> & mut dfa :: Cache { & mut self . forward } # [doc = " Return a mutable reference to the reverse cache."] # [doc = ""] # [doc = " If you need mutable references to both the forward and reverse caches,"] # [doc = " then use [`Cache::as_parts_mut`]."] pub fn reverse_mut (& mut self) -> & mut dfa :: Cache { & mut self . reverse } # [doc = " Return references to the forward and reverse caches, respectively."] pub fn as_parts (& self) -> (& dfa :: Cache , & dfa :: Cache) { (& self . forward , & self . reverse) } # [doc = " Return mutable references to the forward and reverse caches,"] # [doc = " respectively."] pub fn as_parts_mut (& mut self) -> (& mut dfa :: Cache , & mut dfa :: Cache) { (& mut self . forward , & mut self . reverse) } # [doc = " Returns the heap memory usage, in bytes, as a sum of the forward and"] # [doc = " reverse lazy DFA caches."] # [doc = ""] # [doc = " This does **not** include the stack size used up by this cache. To"] # [doc = " compute that, use `std::mem::size_of::<Cache>()`."] pub fn memory_usage (& self) -> usize { self . forward . memory_usage () + self . reverse . memory_usage () } }
    };
}

impl_281!();