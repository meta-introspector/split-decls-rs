macro_rules! deps {
    () => {
        WhichCaptures!();
        MatchKind!();
        Match!();
        Config!();
        NFA!();
        Span!();
        DFA!();
        Prefilter!();
        PikeVM!();
        Input!();
        Regex!();
    };
}

macro_rules! impl_362 {
    () => {
        deps!();
        impl Config { # [doc = " Create a new configuration object for a `Regex`."] pub fn new () -> Config { Config :: default () } # [doc = " Set the match semantics for a `Regex`."] # [doc = ""] # [doc = " The default value is [`MatchKind::LeftmostFirst`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Match, MatchKind};"] # [doc = ""] # [doc = " // By default, leftmost-first semantics are used, which"] # [doc = " // disambiguates matches at the same position by selecting"] # [doc = " // the one that corresponds earlier in the pattern."] # [doc = " let re = Regex::new(\"sam|samwise\")?;"] # [doc = " assert_eq!(Some(Match::must(0, 0..3)), re.find(\"samwise\"));"] # [doc = ""] # [doc = " // But with 'all' semantics, match priority is ignored"] # [doc = " // and all match states are included. When coupled with"] # [doc = " // a leftmost search, the search will report the last"] # [doc = " // possible match."] # [doc = " let re = Regex::builder()"] # [doc = "     .configure(Regex::config().match_kind(MatchKind::All))"] # [doc = "     .build(\"sam|samwise\")?;"] # [doc = " assert_eq!(Some(Match::must(0, 0..7)), re.find(\"samwise\"));"] # [doc = " // Beware that this can lead to skipping matches!"] # [doc = " // Usually 'all' is used for anchored reverse searches"] # [doc = " // only, or for overlapping searches."] # [doc = " assert_eq!(Some(Match::must(0, 4..11)), re.find(\"sam samwise\"));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn match_kind (self , kind : MatchKind) -> Config { Config { match_kind : Some (kind) , .. self } } # [doc = " Toggles whether empty matches are permitted to occur between the code"] # [doc = " units of a UTF-8 encoded codepoint."] # [doc = ""] # [doc = " This should generally be enabled when search a `&str` or anything that"] # [doc = " you otherwise know is valid UTF-8. It should be disabled in all other"] # [doc = " cases. Namely, if the haystack is not valid UTF-8 and this is enabled,"] # [doc = " then behavior is unspecified."] # [doc = ""] # [doc = " By default, this is enabled."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Match};"] # [doc = ""] # [doc = " let re = Regex::new(\"\")?;"] # [doc = " let got: Vec<Match> = re.find_iter(\"☃\").collect();"] # [doc = " // Matches only occur at the beginning and end of the snowman."] # [doc = " assert_eq!(got, vec!["] # [doc = "     Match::must(0, 0..0),"] # [doc = "     Match::must(0, 3..3),"] # [doc = " ]);"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .configure(Regex::config().utf8_empty(false))"] # [doc = "     .build(\"\")?;"] # [doc = " let got: Vec<Match> = re.find_iter(\"☃\").collect();"] # [doc = " // Matches now occur at every position!"] # [doc = " assert_eq!(got, vec!["] # [doc = "     Match::must(0, 0..0),"] # [doc = "     Match::must(0, 1..1),"] # [doc = "     Match::must(0, 2..2),"] # [doc = "     Match::must(0, 3..3),"] # [doc = " ]);"] # [doc = ""] # [doc = " Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn utf8_empty (self , yes : bool) -> Config { Config { utf8_empty : Some (yes) , .. self } } # [doc = " Toggles whether automatic prefilter support is enabled."] # [doc = ""] # [doc = " If this is disabled and [`Config::prefilter`] is not set, then the"] # [doc = " meta regex engine will not use any prefilters. This can sometimes"] # [doc = " be beneficial in cases where you know (or have measured) that the"] # [doc = " prefilter leads to overall worse search performance."] # [doc = ""] # [doc = " By default, this is enabled."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{meta::Regex, Match};"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .configure(Regex::config().auto_prefilter(false))"] # [doc = "     .build(r\"Bruce \\w+\")?;"] # [doc = " let hay = \"Hello Bruce Springsteen!\";"] # [doc = " assert_eq!(Some(Match::must(0, 6..23)), re.find(hay));"] # [doc = ""] # [doc = " Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn auto_prefilter (self , yes : bool) -> Config { Config { autopre : Some (yes) , .. self } } # [doc = " Overrides and sets the prefilter to use inside a `Regex`."] # [doc = ""] # [doc = " This permits one to forcefully set a prefilter in cases where the"] # [doc = " caller knows better than whatever the automatic prefilter logic is"] # [doc = " capable of."] # [doc = ""] # [doc = " By default, this is set to `None` and an automatic prefilter will be"] # [doc = " used if one could be built. (Assuming [`Config::auto_prefilter`] is"] # [doc = " enabled, which it is by default.)"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to set your own prefilter. In the case of a"] # [doc = " pattern like `Bruce \\w+`, the automatic prefilter is likely to be"] # [doc = " constructed in a way that it will look for occurrences of `Bruce `."] # [doc = " In most cases, this is the best choice. But in some cases, it may be"] # [doc = " the case that running `memchr` on `B` is the best choice. One can"] # [doc = " achieve that behavior by overriding the automatic prefilter logic"] # [doc = " and providing a prefilter that just matches `B`."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     meta::Regex,"] # [doc = "     util::prefilter::Prefilter,"] # [doc = "     Match, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let pre = Prefilter::new(MatchKind::LeftmostFirst, &[\"B\"])"] # [doc = "     .expect(\"a prefilter\");"] # [doc = " let re = Regex::builder()"] # [doc = "     .configure(Regex::config().prefilter(Some(pre)))"] # [doc = "     .build(r\"Bruce \\w+\")?;"] # [doc = " let hay = \"Hello Bruce Springsteen!\";"] # [doc = " assert_eq!(Some(Match::must(0, 6..23)), re.find(hay));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: incorrect prefilters can lead to incorrect results!"] # [doc = ""] # [doc = " Be warned that setting an incorrect prefilter can lead to missed"] # [doc = " matches. So if you use this option, ensure your prefilter can _never_"] # [doc = " report false negatives. (A false positive is, on the other hand, quite"] # [doc = " okay and generally unavoidable.)"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     meta::Regex,"] # [doc = "     util::prefilter::Prefilter,"] # [doc = "     Match, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let pre = Prefilter::new(MatchKind::LeftmostFirst, &[\"Z\"])"] # [doc = "     .expect(\"a prefilter\");"] # [doc = " let re = Regex::builder()"] # [doc = "     .configure(Regex::config().prefilter(Some(pre)))"] # [doc = "     .build(r\"Bruce \\w+\")?;"] # [doc = " let hay = \"Hello Bruce Springsteen!\";"] # [doc = " // Oops! No match found, but there should be one!"] # [doc = " assert_eq!(None, re.find(hay));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn prefilter (self , pre : Option < Prefilter >) -> Config { Config { pre : Some (pre) , .. self } } # [doc = " Configures what kinds of groups are compiled as \"capturing\" in the"] # [doc = " underlying regex engine."] # [doc = ""] # [doc = " This is set to [`WhichCaptures::All`] by default. Callers may wish to"] # [doc = " use [`WhichCaptures::Implicit`] in cases where one wants avoid the"] # [doc = " overhead of capture states for explicit groups."] # [doc = ""] # [doc = " Note that another approach to avoiding the overhead of capture groups"] # [doc = " is by using non-capturing groups in the regex pattern. That is,"] # [doc = " `(?:a)` instead of `(a)`. This option is useful when you can't control"] # [doc = " the concrete syntax but know that you don't need the underlying capture"] # [doc = " states. For example, using `WhichCaptures::Implicit` will behave as if"] # [doc = " all explicit capturing groups in the pattern were non-capturing."] # [doc = ""] # [doc = " Setting this to `WhichCaptures::None` is usually not the right thing to"] # [doc = " do. When no capture states are compiled, some regex engines (such as"] # [doc = " the `PikeVM`) won't be able to report match offsets. This will manifest"] # [doc = " as no match being found. Indeed, in order to enforce consistent"] # [doc = " behavior, the meta regex engine will always report `None` for routines"] # [doc = " that return match offsets even if one of its regex engines could"] # [doc = " service the request. This avoids \"match or not\" behavior from being"] # [doc = " influenced by user input (since user input can influence the selection"] # [doc = " of the regex engine)."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example demonstrates how the results of capture groups can change"] # [doc = " based on this option. First we show the default (all capture groups in"] # [doc = " the pattern are capturing):"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Match, Span};"] # [doc = ""] # [doc = " let re = Regex::new(r\"foo([0-9]+)bar\")?;"] # [doc = " let hay = \"foo123bar\";"] # [doc = ""] # [doc = " let mut caps = re.create_captures();"] # [doc = " re.captures(hay, &mut caps);"] # [doc = " assert_eq!(Some(Span::from(0..9)), caps.get_group(0));"] # [doc = " assert_eq!(Some(Span::from(3..6)), caps.get_group(1));"] # [doc = ""] # [doc = " Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " And now we show the behavior when we only include implicit capture"] # [doc = " groups. In this case, we can only find the overall match span, but the"] # [doc = " spans of any other explicit group don't exist because they are treated"] # [doc = " as non-capturing. (In effect, when `WhichCaptures::Implicit` is used,"] # [doc = " there is no real point in using [`Regex::captures`] since it will never"] # [doc = " be able to report more information than [`Regex::find`].)"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     meta::Regex,"] # [doc = "     nfa::thompson::WhichCaptures,"] # [doc = "     Match,"] # [doc = "     Span,"] # [doc = " };"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .configure(Regex::config().which_captures(WhichCaptures::Implicit))"] # [doc = "     .build(r\"foo([0-9]+)bar\")?;"] # [doc = " let hay = \"foo123bar\";"] # [doc = ""] # [doc = " let mut caps = re.create_captures();"] # [doc = " re.captures(hay, &mut caps);"] # [doc = " assert_eq!(Some(Span::from(0..9)), caps.get_group(0));"] # [doc = " assert_eq!(None, caps.get_group(1));"] # [doc = ""] # [doc = " Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: strange `Regex::find` behavior"] # [doc = ""] # [doc = " As noted above, when using [`WhichCaptures::None`], this means that"] # [doc = " `Regex::is_match` could return `true` while `Regex::find` returns"] # [doc = " `None`:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     meta::Regex,"] # [doc = "     nfa::thompson::WhichCaptures,"] # [doc = "     Input,"] # [doc = "     Match,"] # [doc = "     Span,"] # [doc = " };"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .configure(Regex::config().which_captures(WhichCaptures::None))"] # [doc = "     .build(r\"foo([0-9]+)bar\")?;"] # [doc = " let hay = \"foo123bar\";"] # [doc = ""] # [doc = " assert!(re.is_match(hay));"] # [doc = " assert_eq!(re.find(hay), None);"] # [doc = " assert_eq!(re.search_half(&Input::new(hay)), None);"] # [doc = ""] # [doc = " Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn which_captures (mut self , which_captures : WhichCaptures) -> Config { self . which_captures = Some (which_captures) ; self } # [doc = " Sets the size limit, in bytes, to enforce on the construction of every"] # [doc = " NFA build by the meta regex engine."] # [doc = ""] # [doc = " Setting it to `None` disables the limit. This is not recommended if"] # [doc = " you're compiling untrusted patterns."] # [doc = ""] # [doc = " Note that this limit is applied to _each_ NFA built, and if any of"] # [doc = " them exceed the limit, then construction will fail. This limit does"] # [doc = " _not_ correspond to the total memory used by all NFAs in the meta regex"] # [doc = " engine."] # [doc = ""] # [doc = " This defaults to some reasonable number that permits most reasonable"] # [doc = " patterns."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let result = Regex::builder()"] # [doc = "     .configure(Regex::config().nfa_size_limit(Some(20 * (1<<10))))"] # [doc = "     // Not even 20KB is enough to build a single large Unicode class!"] # [doc = "     .build(r\"\\pL\");"] # [doc = " assert!(result.is_err());"] # [doc = ""] # [doc = " // But notice that building such a regex with the exact same limit"] # [doc = " // can succeed depending on other aspects of the configuration. For"] # [doc = " // example, a single *forward* NFA will (at time of writing) fit into"] # [doc = " // the 20KB limit, but a *reverse* NFA of the same pattern will not."] # [doc = " // So if one configures a meta regex such that a reverse NFA is never"] # [doc = " // needed and thus never built, then the 20KB limit will be enough for"] # [doc = " // a pattern like \\pL!"] # [doc = " let result = Regex::builder()"] # [doc = "     .configure(Regex::config()"] # [doc = "         .nfa_size_limit(Some(20 * (1<<10)))"] # [doc = "         // The DFAs are the only thing that (currently) need a reverse"] # [doc = "         // NFA. So if both are disabled, the meta regex engine will"] # [doc = "         // skip building the reverse NFA. Note that this isn't an API"] # [doc = "         // guarantee. A future semver compatible version may introduce"] # [doc = "         // new use cases for a reverse NFA."] # [doc = "         .hybrid(false)"] # [doc = "         .dfa(false)"] # [doc = "     )"] # [doc = "     // Not even 20KB is enough to build a single large Unicode class!"] # [doc = "     .build(r\"\\pL\");"] # [doc = " assert!(result.is_ok());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn nfa_size_limit (self , limit : Option < usize >) -> Config { Config { nfa_size_limit : Some (limit) , .. self } } # [doc = " Sets the size limit, in bytes, for the one-pass DFA."] # [doc = ""] # [doc = " Setting it to `None` disables the limit. Disabling the limit is"] # [doc = " strongly discouraged when compiling untrusted patterns. Even if the"] # [doc = " patterns are trusted, it still may not be a good idea, since a one-pass"] # [doc = " DFA can use a lot of memory. With that said, as the size of a regex"] # [doc = " increases, the likelihood of it being one-pass likely decreases."] # [doc = ""] # [doc = " This defaults to some reasonable number that permits most reasonable"] # [doc = " one-pass patterns."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to set the one-pass DFA size limit. Note that since"] # [doc = " a one-pass DFA is an optional component of the meta regex engine,"] # [doc = " this size limit only impacts what is built internally and will never"] # [doc = " determine whether a `Regex` itself fails to build."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let result = Regex::builder()"] # [doc = "     .configure(Regex::config().onepass_size_limit(Some(2 * (1<<20))))"] # [doc = "     .build(r\"\\pL{5}\");"] # [doc = " assert!(result.is_ok());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn onepass_size_limit (self , limit : Option < usize >) -> Config { Config { onepass_size_limit : Some (limit) , .. self } } # [doc = " Set the cache capacity, in bytes, for the lazy DFA."] # [doc = ""] # [doc = " The cache capacity of the lazy DFA determines approximately how much"] # [doc = " heap memory it is allowed to use to store its state transitions. The"] # [doc = " state transitions are computed at search time, and if the cache fills"] # [doc = " up it, it is cleared. At this point, any previously generated state"] # [doc = " transitions are lost and are re-generated if they're needed again."] # [doc = ""] # [doc = " This sort of cache filling and clearing works quite well _so long as"] # [doc = " cache clearing happens infrequently_. If it happens too often, then the"] # [doc = " meta regex engine will stop using the lazy DFA and switch over to a"] # [doc = " different regex engine."] # [doc = ""] # [doc = " In cases where the cache is cleared too often, it may be possible to"] # [doc = " give the cache more space and reduce (or eliminate) how often it is"] # [doc = " cleared. Similarly, sometimes a regex is so big that the lazy DFA isn't"] # [doc = " used at all if its cache capacity isn't big enough."] # [doc = ""] # [doc = " The capacity set here is a _limit_ on how much memory is used. The"] # [doc = " actual memory used is only allocated as it's needed."] # [doc = ""] # [doc = " Determining the right value for this is a little tricky and will likely"] # [doc = " required some profiling. Enabling the `logging` feature and setting the"] # [doc = " log level to `trace` will also tell you how often the cache is being"] # [doc = " cleared."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let result = Regex::builder()"] # [doc = "     .configure(Regex::config().hybrid_cache_capacity(20 * (1<<20)))"] # [doc = "     .build(r\"\\pL{5}\");"] # [doc = " assert!(result.is_ok());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn hybrid_cache_capacity (self , limit : usize) -> Config { Config { hybrid_cache_capacity : Some (limit) , .. self } } # [doc = " Sets the size limit, in bytes, for heap memory used for a fully"] # [doc = " compiled DFA."] # [doc = ""] # [doc = " **NOTE:** If you increase this, you'll likely also need to increase"] # [doc = " [`Config::dfa_state_limit`]."] # [doc = ""] # [doc = " In contrast to the lazy DFA, building a full DFA requires computing"] # [doc = " all of its state transitions up front. This can be a very expensive"] # [doc = " process, and runs in worst case `2^n` time and space (where `n` is"] # [doc = " proportional to the size of the regex). However, a full DFA unlocks"] # [doc = " some additional optimization opportunities."] # [doc = ""] # [doc = " Because full DFAs can be so expensive, the default limits for them are"] # [doc = " incredibly small. Generally speaking, if your regex is moderately big"] # [doc = " or if you're using Unicode features (`\\w` is Unicode-aware by default"] # [doc = " for example), then you can expect that the meta regex engine won't even"] # [doc = " attempt to build a DFA for it."] # [doc = ""] # [doc = " If this and [`Config::dfa_state_limit`] are set to `None`, then the"] # [doc = " meta regex will not use any sort of limits when deciding whether to"] # [doc = " build a DFA. This in turn makes construction of a `Regex` take"] # [doc = " worst case exponential time and space. Even short patterns can result"] # [doc = " in huge space blow ups. So it is strongly recommended to keep some kind"] # [doc = " of limit set!"] # [doc = ""] # [doc = " The default is set to a small number that permits some simple regexes"] # [doc = " to get compiled into DFAs in reasonable time."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let result = Regex::builder()"] # [doc = "     // 100MB is much bigger than the default."] # [doc = "     .configure(Regex::config()"] # [doc = "         .dfa_size_limit(Some(100 * (1<<20)))"] # [doc = "         // We don't care about size too much here, so just"] # [doc = "         // remove the NFA state limit altogether."] # [doc = "         .dfa_state_limit(None))"] # [doc = "     .build(r\"\\pL{5}\");"] # [doc = " assert!(result.is_ok());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn dfa_size_limit (self , limit : Option < usize >) -> Config { Config { dfa_size_limit : Some (limit) , .. self } } # [doc = " Sets a limit on the total number of NFA states, beyond which, a full"] # [doc = " DFA is not attempted to be compiled."] # [doc = ""] # [doc = " This limit works in concert with [`Config::dfa_size_limit`]. Namely,"] # [doc = " where as `Config::dfa_size_limit` is applied by attempting to construct"] # [doc = " a DFA, this limit is used to avoid the attempt in the first place. This"] # [doc = " is useful to avoid hefty initialization costs associated with building"] # [doc = " a DFA for cases where it is obvious the DFA will ultimately be too big."] # [doc = ""] # [doc = " By default, this is set to a very small number."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let result = Regex::builder()"] # [doc = "     .configure(Regex::config()"] # [doc = "         // Sometimes the default state limit rejects DFAs even"] # [doc = "         // if they would fit in the size limit. Here, we disable"] # [doc = "         // the check on the number of NFA states and just rely on"] # [doc = "         // the size limit."] # [doc = "         .dfa_state_limit(None))"] # [doc = "     .build(r\"(?-u)\\w{30}\");"] # [doc = " assert!(result.is_ok());"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn dfa_state_limit (self , limit : Option < usize >) -> Config { Config { dfa_state_limit : Some (limit) , .. self } } # [doc = " Whether to attempt to shrink the size of the alphabet for the regex"] # [doc = " pattern or not. When enabled, the alphabet is shrunk into a set of"] # [doc = " equivalence classes, where every byte in the same equivalence class"] # [doc = " cannot discriminate between a match or non-match."] # [doc = ""] # [doc = " **WARNING:** This is only useful for debugging DFAs. Disabling this"] # [doc = " does not yield any speed advantages. Indeed, disabling it can result"] # [doc = " in much higher memory usage. Disabling byte classes is useful for"] # [doc = " debugging the actual generated transitions because it lets one see the"] # [doc = " transitions defined on actual bytes instead of the equivalence classes."] # [doc = ""] # [doc = " This option is enabled by default and should never be disabled unless"] # [doc = " one is debugging the meta regex engine's internals."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Match};"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .configure(Regex::config().byte_classes(false))"] # [doc = "     .build(r\"[a-z]+\")?;"] # [doc = " let hay = \"!!quux!!\";"] # [doc = " assert_eq!(Some(Match::must(0, 2..6)), re.find(hay));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn byte_classes (self , yes : bool) -> Config { Config { byte_classes : Some (yes) , .. self } } # [doc = " Set the line terminator to be used by the `^` and `$` anchors in"] # [doc = " multi-line mode."] # [doc = ""] # [doc = " This option has no effect when CRLF mode is enabled. That is,"] # [doc = " regardless of this setting, `(?Rm:^)` and `(?Rm:$)` will always treat"] # [doc = " `\\r` and `\\n` as line terminators (and will never match between a `\\r`"] # [doc = " and a `\\n`)."] # [doc = ""] # [doc = " By default, `\\n` is the line terminator."] # [doc = ""] # [doc = " **Warning**: This does not change the behavior of `.`. To do that,"] # [doc = " you'll need to configure the syntax option"] # [doc = " [`syntax::Config::line_terminator`](crate::util::syntax::Config::line_terminator)"] # [doc = " in addition to this. Otherwise, `.` will continue to match any"] # [doc = " character other than `\\n`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, util::syntax, Match};"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .syntax(syntax::Config::new().multi_line(true))"] # [doc = "     .configure(Regex::config().line_terminator(b'\\x00'))"] # [doc = "     .build(r\"^foo$\")?;"] # [doc = " let hay = \"\\x00foo\\x00\";"] # [doc = " assert_eq!(Some(Match::must(0, 1..4)), re.find(hay));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn line_terminator (self , byte : u8) -> Config { Config { line_terminator : Some (byte) , .. self } } # [doc = " Toggle whether the hybrid NFA/DFA (also known as the \"lazy DFA\") should"] # [doc = " be available for use by the meta regex engine."] # [doc = ""] # [doc = " Enabling this does not necessarily mean that the lazy DFA will"] # [doc = " definitely be used. It just means that it will be _available_ for use"] # [doc = " if the meta regex engine thinks it will be useful."] # [doc = ""] # [doc = " When the `hybrid` crate feature is enabled, then this is enabled by"] # [doc = " default. Otherwise, if the crate feature is disabled, then this is"] # [doc = " always disabled, regardless of its setting by the caller."] pub fn hybrid (self , yes : bool) -> Config { Config { hybrid : Some (yes) , .. self } } # [doc = " Toggle whether a fully compiled DFA should be available for use by the"] # [doc = " meta regex engine."] # [doc = ""] # [doc = " Enabling this does not necessarily mean that a DFA will definitely be"] # [doc = " used. It just means that it will be _available_ for use if the meta"] # [doc = " regex engine thinks it will be useful."] # [doc = ""] # [doc = " When the `dfa-build` crate feature is enabled, then this is enabled by"] # [doc = " default. Otherwise, if the crate feature is disabled, then this is"] # [doc = " always disabled, regardless of its setting by the caller."] pub fn dfa (self , yes : bool) -> Config { Config { dfa : Some (yes) , .. self } } # [doc = " Toggle whether a one-pass DFA should be available for use by the meta"] # [doc = " regex engine."] # [doc = ""] # [doc = " Enabling this does not necessarily mean that a one-pass DFA will"] # [doc = " definitely be used. It just means that it will be _available_ for"] # [doc = " use if the meta regex engine thinks it will be useful. (Indeed, a"] # [doc = " one-pass DFA can only be used when the regex is one-pass. See the"] # [doc = " [`dfa::onepass`](crate::dfa::onepass) module for more details.)"] # [doc = ""] # [doc = " When the `dfa-onepass` crate feature is enabled, then this is enabled"] # [doc = " by default. Otherwise, if the crate feature is disabled, then this is"] # [doc = " always disabled, regardless of its setting by the caller."] pub fn onepass (self , yes : bool) -> Config { Config { onepass : Some (yes) , .. self } } # [doc = " Toggle whether a bounded backtracking regex engine should be available"] # [doc = " for use by the meta regex engine."] # [doc = ""] # [doc = " Enabling this does not necessarily mean that a bounded backtracker will"] # [doc = " definitely be used. It just means that it will be _available_ for use"] # [doc = " if the meta regex engine thinks it will be useful."] # [doc = ""] # [doc = " When the `nfa-backtrack` crate feature is enabled, then this is enabled"] # [doc = " by default. Otherwise, if the crate feature is disabled, then this is"] # [doc = " always disabled, regardless of its setting by the caller."] pub fn backtrack (self , yes : bool) -> Config { Config { backtrack : Some (yes) , .. self } } # [doc = " Returns the match kind on this configuration, as set by"] # [doc = " [`Config::match_kind`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_match_kind (& self) -> MatchKind { self . match_kind . unwrap_or (MatchKind :: LeftmostFirst) } # [doc = " Returns whether empty matches must fall on valid UTF-8 boundaries, as"] # [doc = " set by [`Config::utf8_empty`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_utf8_empty (& self) -> bool { self . utf8_empty . unwrap_or (true) } # [doc = " Returns whether automatic prefilters are enabled, as set by"] # [doc = " [`Config::auto_prefilter`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_auto_prefilter (& self) -> bool { self . autopre . unwrap_or (true) } # [doc = " Returns a manually set prefilter, if one was set by"] # [doc = " [`Config::prefilter`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_prefilter (& self) -> Option < & Prefilter > { self . pre . as_ref () . unwrap_or (& None) . as_ref () } # [doc = " Returns the capture configuration, as set by"] # [doc = " [`Config::which_captures`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_which_captures (& self) -> WhichCaptures { self . which_captures . unwrap_or (WhichCaptures :: All) } # [doc = " Returns NFA size limit, as set by [`Config::nfa_size_limit`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_nfa_size_limit (& self) -> Option < usize > { self . nfa_size_limit . unwrap_or (Some (10 * (1 << 20))) } # [doc = " Returns one-pass DFA size limit, as set by"] # [doc = " [`Config::onepass_size_limit`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_onepass_size_limit (& self) -> Option < usize > { self . onepass_size_limit . unwrap_or (Some (1 * (1 << 20))) } # [doc = " Returns hybrid NFA/DFA cache capacity, as set by"] # [doc = " [`Config::hybrid_cache_capacity`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_hybrid_cache_capacity (& self) -> usize { self . hybrid_cache_capacity . unwrap_or (2 * (1 << 20)) } # [doc = " Returns DFA size limit, as set by [`Config::dfa_size_limit`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_dfa_size_limit (& self) -> Option < usize > { self . dfa_size_limit . unwrap_or (Some (40 * (1 << 10))) } # [doc = " Returns DFA size limit in terms of the number of states in the NFA, as"] # [doc = " set by [`Config::dfa_state_limit`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_dfa_state_limit (& self) -> Option < usize > { self . dfa_state_limit . unwrap_or (Some (30)) } # [doc = " Returns whether byte classes are enabled, as set by"] # [doc = " [`Config::byte_classes`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_byte_classes (& self) -> bool { self . byte_classes . unwrap_or (true) } # [doc = " Returns the line terminator for this configuration, as set by"] # [doc = " [`Config::line_terminator`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_line_terminator (& self) -> u8 { self . line_terminator . unwrap_or (b'\n') } # [doc = " Returns whether the hybrid NFA/DFA regex engine may be used, as set by"] # [doc = " [`Config::hybrid`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_hybrid (& self) -> bool { # [cfg (feature = "hybrid")] { self . hybrid . unwrap_or (true) } # [cfg (not (feature = "hybrid"))] { false } } # [doc = " Returns whether the DFA regex engine may be used, as set by"] # [doc = " [`Config::dfa`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_dfa (& self) -> bool { # [cfg (feature = "dfa-build")] { self . dfa . unwrap_or (true) } # [cfg (not (feature = "dfa-build"))] { false } } # [doc = " Returns whether the one-pass DFA regex engine may be used, as set by"] # [doc = " [`Config::onepass`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_onepass (& self) -> bool { # [cfg (feature = "dfa-onepass")] { self . onepass . unwrap_or (true) } # [cfg (not (feature = "dfa-onepass"))] { false } } # [doc = " Returns whether the bounded backtracking regex engine may be used, as"] # [doc = " set by [`Config::backtrack`]."] # [doc = ""] # [doc = " If it was not explicitly set, then a default value is returned."] pub fn get_backtrack (& self) -> bool { # [cfg (feature = "nfa-backtrack")] { self . backtrack . unwrap_or (true) } # [cfg (not (feature = "nfa-backtrack"))] { false } } # [doc = " Overwrite the default configuration such that the options in `o` are"] # [doc = " always used. If an option in `o` is not set, then the corresponding"] # [doc = " option in `self` is used. If it's not set in `self` either, then it"] # [doc = " remains not set."] pub (crate) fn overwrite (& self , o : Config) -> Config { Config { match_kind : o . match_kind . or (self . match_kind) , utf8_empty : o . utf8_empty . or (self . utf8_empty) , autopre : o . autopre . or (self . autopre) , pre : o . pre . or_else (| | self . pre . clone ()) , which_captures : o . which_captures . or (self . which_captures) , nfa_size_limit : o . nfa_size_limit . or (self . nfa_size_limit) , onepass_size_limit : o . onepass_size_limit . or (self . onepass_size_limit) , hybrid_cache_capacity : o . hybrid_cache_capacity . or (self . hybrid_cache_capacity) , hybrid : o . hybrid . or (self . hybrid) , dfa : o . dfa . or (self . dfa) , dfa_size_limit : o . dfa_size_limit . or (self . dfa_size_limit) , dfa_state_limit : o . dfa_state_limit . or (self . dfa_state_limit) , onepass : o . onepass . or (self . onepass) , backtrack : o . backtrack . or (self . backtrack) , byte_classes : o . byte_classes . or (self . byte_classes) , line_terminator : o . line_terminator . or (self . line_terminator) , } } }
    };
}

impl_362!()