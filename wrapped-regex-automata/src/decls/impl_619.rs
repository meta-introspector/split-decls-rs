macro_rules! deps {
    () => {
        GroupInfo!();
        GroupInfoAllNames!();
        GroupInfoError!();
        PatternID!();
        NFA!();
        CaptureNameMap!();
        GroupInfoPatternNames!();
        SmallIndex!();
        GroupInfoInner!();
        WhichCaptures!();
    };
}

macro_rules! impl_619 {
    () => {
        deps!();
        impl GroupInfo { # [doc = " Creates a new group info from a sequence of patterns, where each"] # [doc = " sequence of patterns yields a sequence of possible group names. The"] # [doc = " index of each pattern in the sequence corresponds to its `PatternID`,"] # [doc = " and the index of each group in each pattern's sequence corresponds to"] # [doc = " its corresponding group index."] # [doc = ""] # [doc = " While this constructor is very generic and therefore perhaps hard to"] # [doc = " chew on, an example of a valid concrete type that can be passed to"] # [doc = " this constructor is `Vec<Vec<Option<String>>>`. The outer `Vec`"] # [doc = " corresponds to the patterns, i.e., one `Vec<Option<String>>` per"] # [doc = " pattern. The inner `Vec` corresponds to the capturing groups for"] # [doc = " each pattern. The `Option<String>` corresponds to the name of the"] # [doc = " capturing group, if present."] # [doc = ""] # [doc = " It is legal to pass an empty iterator to this constructor. It will"] # [doc = " return an empty group info with zero slots. An empty group info is"] # [doc = " useful for cases where you have no patterns or for cases where slots"] # [doc = " aren't being used at all (e.g., for most DFAs in this crate)."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This constructor returns an error if the given capturing groups are"] # [doc = " invalid in some way. Those reasons include, but are not necessarily"] # [doc = " limited to:"] # [doc = ""] # [doc = " * Too many patterns (i.e., `PatternID` would overflow)."] # [doc = " * Too many capturing groups (e.g., `u32` would overflow)."] # [doc = " * A pattern is given that has no capturing groups. (All patterns must"] # [doc = " have at least an implicit capturing group at index `0`.)"] # [doc = " * The capturing group at index `0` has a name. It must be unnamed."] # [doc = " * There are duplicate capturing group names within the same pattern."] # [doc = " (Multiple capturing groups with the same name may exist, but they"] # [doc = " must be in different patterns.)"] # [doc = ""] # [doc = " An example below shows how to trigger some of the above error"] # [doc = " conditions."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to build a new `GroupInfo` and query it for"] # [doc = " information."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::util::captures::GroupInfo;"] # [doc = ""] # [doc = " let info = GroupInfo::new(vec!["] # [doc = "     vec![None, Some(\"foo\")],"] # [doc = "     vec![None],"] # [doc = "     vec![None, None, None, Some(\"bar\"), None],"] # [doc = "     vec![None, None, Some(\"foo\")],"] # [doc = " ])?;"] # [doc = " // The number of patterns being tracked."] # [doc = " assert_eq!(4, info.pattern_len());"] # [doc = " // 2 slots per group"] # [doc = " assert_eq!(22, info.slot_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: empty `GroupInfo`"] # [doc = ""] # [doc = " This example shows how to build a new `GroupInfo` and query it for"] # [doc = " information."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::util::captures::GroupInfo;"] # [doc = ""] # [doc = " let info = GroupInfo::empty();"] # [doc = " // Everything is zero."] # [doc = " assert_eq!(0, info.pattern_len());"] # [doc = " assert_eq!(0, info.slot_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: error conditions"] # [doc = ""] # [doc = " This example shows how to provoke some of the ways in which building"] # [doc = " a `GroupInfo` can fail."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::util::captures::GroupInfo;"] # [doc = ""] # [doc = " // Either the group info is empty, or all patterns must have at least"] # [doc = " // one capturing group."] # [doc = " assert!(GroupInfo::new(vec!["] # [doc = "     vec![None, Some(\"a\")], // ok"] # [doc = "     vec![None], // ok"] # [doc = "     vec![], // not ok"] # [doc = " ]).is_err());"] # [doc = " // Note that building an empty group info is OK."] # [doc = " assert!(GroupInfo::new(Vec::<Vec<Option<String>>>::new()).is_ok());"] # [doc = ""] # [doc = " // The first group in each pattern must correspond to an implicit"] # [doc = " // anonymous group. i.e., One that is not named. By convention, this"] # [doc = " // group corresponds to the overall match of a regex. Every other group"] # [doc = " // in a pattern is explicit and optional."] # [doc = " assert!(GroupInfo::new(vec![vec![Some(\"foo\")]]).is_err());"] # [doc = ""] # [doc = " // There must not be duplicate group names within the same pattern."] # [doc = " assert!(GroupInfo::new(vec!["] # [doc = "     vec![None, Some(\"foo\"), Some(\"foo\")],"] # [doc = " ]).is_err());"] # [doc = " // But duplicate names across distinct patterns is OK."] # [doc = " assert!(GroupInfo::new(vec!["] # [doc = "     vec![None, Some(\"foo\")],"] # [doc = "     vec![None, Some(\"foo\")],"] # [doc = " ]).is_ok());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " There are other ways for building a `GroupInfo` to fail but are"] # [doc = " difficult to show. For example, if the number of patterns given would"] # [doc = " overflow `PatternID`."] pub fn new < P , G , N > (pattern_groups : P) -> Result < GroupInfo , GroupInfoError > where P : IntoIterator < Item = G > , G : IntoIterator < Item = Option < N > > , N : AsRef < str > , { let mut group_info = GroupInfoInner { slot_ranges : vec ! [] , name_to_index : vec ! [] , index_to_name : vec ! [] , memory_extra : 0 , } ; for (pattern_index , groups) in pattern_groups . into_iter () . enumerate () { let pid = PatternID :: new (pattern_index) . map_err (GroupInfoError :: too_many_patterns) ? ; let mut groups_iter = groups . into_iter () . enumerate () ; match groups_iter . next () { None => return Err (GroupInfoError :: missing_groups (pid)) , Some ((_ , Some (_))) => { return Err (GroupInfoError :: first_must_be_unnamed (pid)) } Some ((_ , None)) => { } } group_info . add_first_group (pid) ; for (group_index , maybe_name) in groups_iter { let group = SmallIndex :: new (group_index) . map_err (| _ | { GroupInfoError :: too_many_groups (pid , group_index) }) ? ; group_info . add_explicit_group (pid , group , maybe_name) ? ; } } group_info . fixup_slot_ranges () ? ; group_info . slot_ranges . shrink_to_fit () ; group_info . name_to_index . shrink_to_fit () ; group_info . index_to_name . shrink_to_fit () ; Ok (GroupInfo (Arc :: new (group_info))) } # [doc = " This creates an empty `GroupInfo`."] # [doc = ""] # [doc = " This is a convenience routine for calling `GroupInfo::new` with an"] # [doc = " iterator that yields no elements."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to build a new empty `GroupInfo` and query it"] # [doc = " for information."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::util::captures::GroupInfo;"] # [doc = ""] # [doc = " let info = GroupInfo::empty();"] # [doc = " // Everything is zero."] # [doc = " assert_eq!(0, info.pattern_len());"] # [doc = " assert_eq!(0, info.all_group_len());"] # [doc = " assert_eq!(0, info.slot_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn empty () -> GroupInfo { GroupInfo :: new (core :: iter :: empty :: < [Option < & str > ; 0] > ()) . expect ("empty group info is always valid") } # [doc = " Return the capture group index corresponding to the given name in the"] # [doc = " given pattern. If no such capture group name exists in the given"] # [doc = " pattern, then this returns `None`."] # [doc = ""] # [doc = " If the given pattern ID is invalid, then this returns `None`."] # [doc = ""] # [doc = " This also returns `None` for all inputs if these captures are empty"] # [doc = " (e.g., built from an empty [`GroupInfo`]). To check whether captures"] # [doc = " are present for a specific pattern, use [`GroupInfo::group_len`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to find the capture index for the given pattern"] # [doc = " and group name."] # [doc = ""] # [doc = " Remember that capture indices are relative to the pattern, such that"] # [doc = " the same capture index value may refer to different capturing groups"] # [doc = " for distinct patterns."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{nfa::thompson::NFA, PatternID};"] # [doc = ""] # [doc = " let (pid0, pid1) = (PatternID::must(0), PatternID::must(1));"] # [doc = ""] # [doc = " let nfa = NFA::new_many(&["] # [doc = "     r\"a(?P<quux>\\w+)z(?P<foo>\\s+)\","] # [doc = "     r\"a(?P<foo>\\d+)z\","] # [doc = " ])?;"] # [doc = " let groups = nfa.group_info();"] # [doc = " assert_eq!(Some(2), groups.to_index(pid0, \"foo\"));"] # [doc = " // Recall that capture index 0 is always unnamed and refers to the"] # [doc = " // entire pattern. So the first capturing group present in the pattern"] # [doc = " // itself always starts at index 1."] # [doc = " assert_eq!(Some(1), groups.to_index(pid1, \"foo\"));"] # [doc = ""] # [doc = " // And if a name does not exist for a particular pattern, None is"] # [doc = " // returned."] # [doc = " assert!(groups.to_index(pid0, \"quux\").is_some());"] # [doc = " assert!(groups.to_index(pid1, \"quux\").is_none());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn to_index (& self , pid : PatternID , name : & str) -> Option < usize > { let indices = self . 0 . name_to_index . get (pid . as_usize ()) ? ; indices . get (name) . cloned () . map (| i | i . as_usize ()) } # [doc = " Return the capture name for the given index and given pattern. If the"] # [doc = " corresponding group does not have a name, then this returns `None`."] # [doc = ""] # [doc = " If the pattern ID is invalid, then this returns `None`."] # [doc = ""] # [doc = " If the group index is invalid for the given pattern, then this returns"] # [doc = " `None`. A group `index` is valid for a pattern `pid` in an `nfa` if and"] # [doc = " only if `index < nfa.pattern_capture_len(pid)`."] # [doc = ""] # [doc = " This also returns `None` for all inputs if these captures are empty"] # [doc = " (e.g., built from an empty [`GroupInfo`]). To check whether captures"] # [doc = " are present for a specific pattern, use [`GroupInfo::group_len`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to find the capture group name for the given"] # [doc = " pattern and group index."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{nfa::thompson::NFA, PatternID};"] # [doc = ""] # [doc = " let (pid0, pid1) = (PatternID::must(0), PatternID::must(1));"] # [doc = ""] # [doc = " let nfa = NFA::new_many(&["] # [doc = "     r\"a(?P<foo>\\w+)z(\\s+)x(\\d+)\","] # [doc = "     r\"a(\\d+)z(?P<foo>\\s+)\","] # [doc = " ])?;"] # [doc = " let groups = nfa.group_info();"] # [doc = " assert_eq!(None, groups.to_name(pid0, 0));"] # [doc = " assert_eq!(Some(\"foo\"), groups.to_name(pid0, 1));"] # [doc = " assert_eq!(None, groups.to_name(pid0, 2));"] # [doc = " assert_eq!(None, groups.to_name(pid0, 3));"] # [doc = ""] # [doc = " assert_eq!(None, groups.to_name(pid1, 0));"] # [doc = " assert_eq!(None, groups.to_name(pid1, 1));"] # [doc = " assert_eq!(Some(\"foo\"), groups.to_name(pid1, 2));"] # [doc = " // '3' is not a valid capture index for the second pattern."] # [doc = " assert_eq!(None, groups.to_name(pid1, 3));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn to_name (& self , pid : PatternID , group_index : usize) -> Option < & str > { let pattern_names = self . 0 . index_to_name . get (pid . as_usize ()) ? ; pattern_names . get (group_index) ? . as_deref () } # [doc = " Return an iterator of all capture groups and their names (if present)"] # [doc = " for a particular pattern."] # [doc = ""] # [doc = " If the given pattern ID is invalid or if this `GroupInfo` is empty,"] # [doc = " then the iterator yields no elements."] # [doc = ""] # [doc = " The number of elements yielded by this iterator is always equal to"] # [doc = " the result of calling [`GroupInfo::group_len`] with the same"] # [doc = " `PatternID`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to get a list of all capture group names for"] # [doc = " a particular pattern."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, PatternID};"] # [doc = ""] # [doc = " let nfa = NFA::new(r\"(a)(?P<foo>b)(c)(d)(?P<bar>e)\")?;"] # [doc = " // The first is the implicit group that is always unnamed. The next"] # [doc = " // 5 groups are the explicit groups found in the concrete syntax above."] # [doc = " let expected = vec![None, None, Some(\"foo\"), None, None, Some(\"bar\")];"] # [doc = " let got: Vec<Option<&str>> ="] # [doc = "     nfa.group_info().pattern_names(PatternID::ZERO).collect();"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " // Using an invalid pattern ID will result in nothing yielded."] # [doc = " let got = nfa.group_info().pattern_names(PatternID::must(999)).count();"] # [doc = " assert_eq!(0, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn pattern_names (& self , pid : PatternID) -> GroupInfoPatternNames < '_ > { GroupInfoPatternNames { it : self . 0 . index_to_name . get (pid . as_usize ()) . map (| indices | indices . iter ()) . unwrap_or ([] . iter ()) , } } # [doc = " Return an iterator of all capture groups for all patterns supported by"] # [doc = " this `GroupInfo`. Each item yielded is a triple of the group's pattern"] # [doc = " ID, index in the pattern and the group's name, if present."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to get a list of all capture groups found in"] # [doc = " one NFA, potentially spanning multiple patterns."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, PatternID};"] # [doc = ""] # [doc = " let nfa = NFA::new_many(&["] # [doc = "     r\"(?P<foo>a)\","] # [doc = "     r\"a\","] # [doc = "     r\"(a)\","] # [doc = " ])?;"] # [doc = " let expected = vec!["] # [doc = "     (PatternID::must(0), 0, None),"] # [doc = "     (PatternID::must(0), 1, Some(\"foo\")),"] # [doc = "     (PatternID::must(1), 0, None),"] # [doc = "     (PatternID::must(2), 0, None),"] # [doc = "     (PatternID::must(2), 1, None),"] # [doc = " ];"] # [doc = " let got: Vec<(PatternID, usize, Option<&str>)> ="] # [doc = "     nfa.group_info().all_names().collect();"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Unlike other capturing group related routines, this routine doesn't"] # [doc = " panic even if captures aren't enabled on this NFA:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::{NFA, WhichCaptures};"] # [doc = ""] # [doc = " let nfa = NFA::compiler()"] # [doc = "     .configure(NFA::config().which_captures(WhichCaptures::None))"] # [doc = "     .build_many(&["] # [doc = "         r\"(?P<foo>a)\","] # [doc = "         r\"a\","] # [doc = "         r\"(a)\","] # [doc = "     ])?;"] # [doc = " // When captures aren't enabled, there's nothing to return."] # [doc = " assert_eq!(0, nfa.group_info().all_names().count());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn all_names (& self) -> GroupInfoAllNames < '_ > { GroupInfoAllNames { group_info : self , pids : PatternID :: iter (self . pattern_len ()) , current_pid : None , names : None , } } # [doc = " Returns the starting and ending slot corresponding to the given"] # [doc = " capturing group for the given pattern. The ending slot is always one"] # [doc = " more than the starting slot returned."] # [doc = ""] # [doc = " Note that this is like [`GroupInfo::slot`], except that it also returns"] # [doc = " the ending slot value for convenience."] # [doc = ""] # [doc = " If either the pattern ID or the capture index is invalid, then this"] # [doc = " returns None."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows that the starting slots for the first capturing"] # [doc = " group of each pattern are distinct."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, PatternID};"] # [doc = ""] # [doc = " let nfa = NFA::new_many(&[\"a\", \"b\"])?;"] # [doc = " assert_ne!("] # [doc = "     nfa.group_info().slots(PatternID::must(0), 0),"] # [doc = "     nfa.group_info().slots(PatternID::must(1), 0),"] # [doc = " );"] # [doc = ""] # [doc = " // Also, the start and end slot values are never equivalent."] # [doc = " let (start, end) = nfa.group_info().slots(PatternID::ZERO, 0).unwrap();"] # [doc = " assert_ne!(start, end);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn slots (& self , pid : PatternID , group_index : usize ,) -> Option < (usize , usize) > { self . slot (pid , group_index) . map (| start | (start , start + 1)) } # [doc = " Returns the starting slot corresponding to the given capturing group"] # [doc = " for the given pattern. The ending slot is always one more than the"] # [doc = " value returned."] # [doc = ""] # [doc = " If either the pattern ID or the capture index is invalid, then this"] # [doc = " returns None."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows that the starting slots for the first capturing"] # [doc = " group of each pattern are distinct."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, PatternID};"] # [doc = ""] # [doc = " let nfa = NFA::new_many(&[\"a\", \"b\"])?;"] # [doc = " assert_ne!("] # [doc = "     nfa.group_info().slot(PatternID::must(0), 0),"] # [doc = "     nfa.group_info().slot(PatternID::must(1), 0),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn slot (& self , pid : PatternID , group_index : usize) -> Option < usize > { if group_index >= self . group_len (pid) { return None ; } if group_index == 0 { Some (pid . as_usize () * 2) } else { let (start , _) = self . 0 . slot_ranges [pid] ; Some (start . as_usize () + ((group_index - 1) * 2)) } } # [doc = " Returns the total number of patterns in this `GroupInfo`."] # [doc = ""] # [doc = " This may return zero if the `GroupInfo` was constructed with no"] # [doc = " patterns."] # [doc = ""] # [doc = " This is guaranteed to be no bigger than [`PatternID::LIMIT`] because"] # [doc = " `GroupInfo` construction will fail if too many patterns are added."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::NFA;"] # [doc = ""] # [doc = " let nfa = NFA::new_many(&[\"[0-9]+\", \"[a-z]+\", \"[A-Z]+\"])?;"] # [doc = " assert_eq!(3, nfa.group_info().pattern_len());"] # [doc = ""] # [doc = " let nfa = NFA::never_match();"] # [doc = " assert_eq!(0, nfa.group_info().pattern_len());"] # [doc = ""] # [doc = " let nfa = NFA::always_match();"] # [doc = " assert_eq!(1, nfa.group_info().pattern_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn pattern_len (& self) -> usize { self . 0 . pattern_len () } # [doc = " Return the number of capture groups in a pattern."] # [doc = ""] # [doc = " If the pattern ID is invalid, then this returns `0`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how the values returned by this routine may vary"] # [doc = " for different patterns and NFA configurations."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::{NFA, WhichCaptures}, PatternID};"] # [doc = ""] # [doc = " let nfa = NFA::new(r\"(a)(b)(c)\")?;"] # [doc = " // There are 3 explicit groups in the pattern's concrete syntax and"] # [doc = " // 1 unnamed and implicit group spanning the entire pattern."] # [doc = " assert_eq!(4, nfa.group_info().group_len(PatternID::ZERO));"] # [doc = ""] # [doc = " let nfa = NFA::new(r\"abc\")?;"] # [doc = " // There is just the unnamed implicit group."] # [doc = " assert_eq!(1, nfa.group_info().group_len(PatternID::ZERO));"] # [doc = ""] # [doc = " let nfa = NFA::compiler()"] # [doc = "     .configure(NFA::config().which_captures(WhichCaptures::None))"] # [doc = "     .build(r\"abc\")?;"] # [doc = " // We disabled capturing groups, so there are none."] # [doc = " assert_eq!(0, nfa.group_info().group_len(PatternID::ZERO));"] # [doc = ""] # [doc = " let nfa = NFA::compiler()"] # [doc = "     .configure(NFA::config().which_captures(WhichCaptures::None))"] # [doc = "     .build(r\"(a)(b)(c)\")?;"] # [doc = " // We disabled capturing groups, so there are none, even if there are"] # [doc = " // explicit groups in the concrete syntax."] # [doc = " assert_eq!(0, nfa.group_info().group_len(PatternID::ZERO));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn group_len (& self , pid : PatternID) -> usize { self . 0 . group_len (pid) } # [doc = " Return the total number of capture groups across all patterns."] # [doc = ""] # [doc = " This includes implicit groups that represent the entire match of a"] # [doc = " pattern."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how the values returned by this routine may vary"] # [doc = " for different patterns and NFA configurations."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::{NFA, WhichCaptures}, PatternID};"] # [doc = ""] # [doc = " let nfa = NFA::new(r\"(a)(b)(c)\")?;"] # [doc = " // There are 3 explicit groups in the pattern's concrete syntax and"] # [doc = " // 1 unnamed and implicit group spanning the entire pattern."] # [doc = " assert_eq!(4, nfa.group_info().all_group_len());"] # [doc = ""] # [doc = " let nfa = NFA::new(r\"abc\")?;"] # [doc = " // There is just the unnamed implicit group."] # [doc = " assert_eq!(1, nfa.group_info().all_group_len());"] # [doc = ""] # [doc = " let nfa = NFA::new_many(&[\"(a)\", \"b\", \"(c)\"])?;"] # [doc = " // Each pattern has one implicit groups, and two"] # [doc = " // patterns have one explicit group each."] # [doc = " assert_eq!(5, nfa.group_info().all_group_len());"] # [doc = ""] # [doc = " let nfa = NFA::compiler()"] # [doc = "     .configure(NFA::config().which_captures(WhichCaptures::None))"] # [doc = "     .build(r\"abc\")?;"] # [doc = " // We disabled capturing groups, so there are none."] # [doc = " assert_eq!(0, nfa.group_info().all_group_len());"] # [doc = ""] # [doc = " let nfa = NFA::compiler()"] # [doc = "     .configure(NFA::config().which_captures(WhichCaptures::None))"] # [doc = "     .build(r\"(a)(b)(c)\")?;"] # [doc = " // We disabled capturing groups, so there are none, even if there are"] # [doc = " // explicit groups in the concrete syntax."] # [doc = " assert_eq!(0, nfa.group_info().group_len(PatternID::ZERO));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn all_group_len (& self) -> usize { self . slot_len () / 2 } # [doc = " Returns the total number of slots in this `GroupInfo` across all"] # [doc = " patterns."] # [doc = ""] # [doc = " The total number of slots is always twice the total number of capturing"] # [doc = " groups, including both implicit and explicit groups."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the relationship between the number of capturing"] # [doc = " groups and slots."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::util::captures::GroupInfo;"] # [doc = ""] # [doc = " // There are 11 total groups here."] # [doc = " let info = GroupInfo::new(vec!["] # [doc = "     vec![None, Some(\"foo\")],"] # [doc = "     vec![None],"] # [doc = "     vec![None, None, None, Some(\"bar\"), None],"] # [doc = "     vec![None, None, Some(\"foo\")],"] # [doc = " ])?;"] # [doc = " // 2 slots per group gives us 11*2=22 slots."] # [doc = " assert_eq!(22, info.slot_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn slot_len (& self) -> usize { self . 0 . small_slot_len () . as_usize () } # [doc = " Returns the total number of slots for implicit capturing groups."] # [doc = ""] # [doc = " This is like [`GroupInfo::slot_len`], except it doesn't include the"] # [doc = " explicit slots for each pattern. Since there are always exactly 2"] # [doc = " implicit slots for each pattern, the number of implicit slots is always"] # [doc = " equal to twice the number of patterns."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the relationship between the number of capturing"] # [doc = " groups, implicit slots and explicit slots."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::util::captures::GroupInfo;"] # [doc = ""] # [doc = " // There are 11 total groups here."] # [doc = " let info = GroupInfo::new(vec![vec![None, Some(\"foo\"), Some(\"bar\")]])?;"] # [doc = " // 2 slots per group gives us 11*2=22 slots."] # [doc = " assert_eq!(6, info.slot_len());"] # [doc = " // 2 implicit slots per pattern gives us 2 implicit slots since there"] # [doc = " // is 1 pattern."] # [doc = " assert_eq!(2, info.implicit_slot_len());"] # [doc = " // 2 explicit capturing groups gives us 2*2=4 explicit slots."] # [doc = " assert_eq!(4, info.explicit_slot_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn implicit_slot_len (& self) -> usize { self . pattern_len () * 2 } # [doc = " Returns the total number of slots for explicit capturing groups."] # [doc = ""] # [doc = " This is like [`GroupInfo::slot_len`], except it doesn't include the"] # [doc = " implicit slots for each pattern. (There are always 2 implicit slots for"] # [doc = " each pattern.)"] # [doc = ""] # [doc = " For a non-empty `GroupInfo`, it is always the case that `slot_len` is"] # [doc = " strictly greater than `explicit_slot_len`. For an empty `GroupInfo`,"] # [doc = " both the total number of slots and the number of explicit slots is"] # [doc = " `0`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the relationship between the number of capturing"] # [doc = " groups, implicit slots and explicit slots."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::util::captures::GroupInfo;"] # [doc = ""] # [doc = " // There are 11 total groups here."] # [doc = " let info = GroupInfo::new(vec![vec![None, Some(\"foo\"), Some(\"bar\")]])?;"] # [doc = " // 2 slots per group gives us 11*2=22 slots."] # [doc = " assert_eq!(6, info.slot_len());"] # [doc = " // 2 implicit slots per pattern gives us 2 implicit slots since there"] # [doc = " // is 1 pattern."] # [doc = " assert_eq!(2, info.implicit_slot_len());"] # [doc = " // 2 explicit capturing groups gives us 2*2=4 explicit slots."] # [doc = " assert_eq!(4, info.explicit_slot_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn explicit_slot_len (& self) -> usize { self . slot_len () . saturating_sub (self . implicit_slot_len ()) } # [doc = " Returns the memory usage, in bytes, of this `GroupInfo`."] # [doc = ""] # [doc = " This does **not** include the stack size used up by this `GroupInfo`."] # [doc = " To compute that, use `std::mem::size_of::<GroupInfo>()`."] # [inline] pub fn memory_usage (& self) -> usize { use core :: mem :: size_of as s ; s :: < GroupInfoInner > () + self . 0 . slot_ranges . len () * s :: < (SmallIndex , SmallIndex) > () + self . 0 . name_to_index . len () * s :: < CaptureNameMap > () + self . 0 . index_to_name . len () * s :: < Vec < Option < Arc < str > > > > () + self . 0 . memory_extra } }
    };
}

impl_619!();