macro_rules! deps {
    () => {
        Start!();
        Split!();
        LookSet!();
        DebugByte!();
        UnicodeWordBoundaryError!();
        Look!();
        ByteClassSet!();
        LookMatcher!();
    };
}

macro_rules! impl_693 {
    () => {
        deps!();
        impl LookMatcher { # [doc = " Creates a new default matcher for look-around assertions."] pub fn new () -> LookMatcher { LookMatcher { lineterm : DebugByte (b'\n') } } # [doc = " Sets the line terminator for use with `(?m:^)` and `(?m:$)`."] # [doc = ""] # [doc = " Namely, instead of `^` matching after `\\n` and `$` matching immediately"] # [doc = " before a `\\n`, this will cause it to match after and before the byte"] # [doc = " given."] # [doc = ""] # [doc = " It can occasionally be useful to use this to configure the line"] # [doc = " terminator to the NUL byte when searching binary data."] # [doc = ""] # [doc = " Note that this does not apply to CRLF-aware line anchors such as"] # [doc = " `(?Rm:^)` and `(?Rm:$)`. CRLF-aware line anchors are hard-coded to"] # [doc = " use `\\r` and `\\n`."] pub fn set_line_terminator (& mut self , byte : u8) -> & mut LookMatcher { self . lineterm . 0 = byte ; self } # [doc = " Returns the line terminator that was configured for this matcher."] # [doc = ""] # [doc = " If no line terminator was configured, then this returns `\\n`."] # [doc = ""] # [doc = " Note that the line terminator should only be used for matching `(?m:^)`"] # [doc = " and `(?m:$)` assertions. It specifically should _not_ be used for"] # [doc = " matching the CRLF aware assertions `(?Rm:^)` and `(?Rm:$)`."] pub fn get_line_terminator (& self) -> u8 { self . lineterm . 0 } # [doc = " Returns true when the position `at` in `haystack` satisfies the given"] # [doc = " look-around assertion."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics when testing any Unicode word boundary assertion in this"] # [doc = " set and when the Unicode word data is not available. Specifically, this"] # [doc = " only occurs when the `unicode-word-boundary` feature is not enabled."] # [doc = ""] # [doc = " Since it's generally expected that this routine is called inside of"] # [doc = " a matching engine, callers should check the error condition when"] # [doc = " building the matching engine. If there is a Unicode word boundary"] # [doc = " in the matcher and the data isn't available, then the matcher should"] # [doc = " fail to build."] # [doc = ""] # [doc = " Callers can check the error condition with [`LookSet::available`]."] # [doc = ""] # [doc = " This also may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn matches (& self , look : Look , haystack : & [u8] , at : usize) -> bool { self . matches_inline (look , haystack , at) } # [doc = " Like `matches`, but forcefully inlined."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics when testing any Unicode word boundary assertion in this"] # [doc = " set and when the Unicode word data is not available. Specifically, this"] # [doc = " only occurs when the `unicode-word-boundary` feature is not enabled."] # [doc = ""] # [doc = " Since it's generally expected that this routine is called inside of"] # [doc = " a matching engine, callers should check the error condition when"] # [doc = " building the matching engine. If there is a Unicode word boundary"] # [doc = " in the matcher and the data isn't available, then the matcher should"] # [doc = " fail to build."] # [doc = ""] # [doc = " Callers can check the error condition with [`LookSet::available`]."] # [doc = ""] # [doc = " This also may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [cfg_attr (feature = "perf-inline" , inline (always))] pub (crate) fn matches_inline (& self , look : Look , haystack : & [u8] , at : usize ,) -> bool { match look { Look :: Start => self . is_start (haystack , at) , Look :: End => self . is_end (haystack , at) , Look :: StartLF => self . is_start_lf (haystack , at) , Look :: EndLF => self . is_end_lf (haystack , at) , Look :: StartCRLF => self . is_start_crlf (haystack , at) , Look :: EndCRLF => self . is_end_crlf (haystack , at) , Look :: WordAscii => self . is_word_ascii (haystack , at) , Look :: WordAsciiNegate => self . is_word_ascii_negate (haystack , at) , Look :: WordUnicode => self . is_word_unicode (haystack , at) . unwrap () , Look :: WordUnicodeNegate => { self . is_word_unicode_negate (haystack , at) . unwrap () } Look :: WordStartAscii => self . is_word_start_ascii (haystack , at) , Look :: WordEndAscii => self . is_word_end_ascii (haystack , at) , Look :: WordStartUnicode => { self . is_word_start_unicode (haystack , at) . unwrap () } Look :: WordEndUnicode => { self . is_word_end_unicode (haystack , at) . unwrap () } Look :: WordStartHalfAscii => { self . is_word_start_half_ascii (haystack , at) } Look :: WordEndHalfAscii => { self . is_word_end_half_ascii (haystack , at) } Look :: WordStartHalfUnicode => { self . is_word_start_half_unicode (haystack , at) . unwrap () } Look :: WordEndHalfUnicode => { self . is_word_end_half_unicode (haystack , at) . unwrap () } } } # [doc = " Returns true when _all_ of the assertions in the given set match at the"] # [doc = " given position in the haystack."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics when testing any Unicode word boundary assertion in this"] # [doc = " set and when the Unicode word data is not available. Specifically, this"] # [doc = " only occurs when the `unicode-word-boundary` feature is not enabled."] # [doc = ""] # [doc = " Since it's generally expected that this routine is called inside of"] # [doc = " a matching engine, callers should check the error condition when"] # [doc = " building the matching engine. If there is a Unicode word boundary"] # [doc = " in the matcher and the data isn't available, then the matcher should"] # [doc = " fail to build."] # [doc = ""] # [doc = " Callers can check the error condition with [`LookSet::available`]."] # [doc = ""] # [doc = " This also may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn matches_set (& self , set : LookSet , haystack : & [u8] , at : usize ,) -> bool { self . matches_set_inline (set , haystack , at) } # [doc = " Like `LookSet::matches`, but forcefully inlined for perf."] # [cfg_attr (feature = "perf-inline" , inline (always))] pub (crate) fn matches_set_inline (& self , set : LookSet , haystack : & [u8] , at : usize ,) -> bool { if set . contains (Look :: Start) { if ! self . is_start (haystack , at) { return false ; } } if set . contains (Look :: End) { if ! self . is_end (haystack , at) { return false ; } } if set . contains (Look :: StartLF) { if ! self . is_start_lf (haystack , at) { return false ; } } if set . contains (Look :: EndLF) { if ! self . is_end_lf (haystack , at) { return false ; } } if set . contains (Look :: StartCRLF) { if ! self . is_start_crlf (haystack , at) { return false ; } } if set . contains (Look :: EndCRLF) { if ! self . is_end_crlf (haystack , at) { return false ; } } if set . contains (Look :: WordAscii) { if ! self . is_word_ascii (haystack , at) { return false ; } } if set . contains (Look :: WordAsciiNegate) { if ! self . is_word_ascii_negate (haystack , at) { return false ; } } if set . contains (Look :: WordUnicode) { if ! self . is_word_unicode (haystack , at) . unwrap () { return false ; } } if set . contains (Look :: WordUnicodeNegate) { if ! self . is_word_unicode_negate (haystack , at) . unwrap () { return false ; } } if set . contains (Look :: WordStartAscii) { if ! self . is_word_start_ascii (haystack , at) { return false ; } } if set . contains (Look :: WordEndAscii) { if ! self . is_word_end_ascii (haystack , at) { return false ; } } if set . contains (Look :: WordStartUnicode) { if ! self . is_word_start_unicode (haystack , at) . unwrap () { return false ; } } if set . contains (Look :: WordEndUnicode) { if ! self . is_word_end_unicode (haystack , at) . unwrap () { return false ; } } if set . contains (Look :: WordStartHalfAscii) { if ! self . is_word_start_half_ascii (haystack , at) { return false ; } } if set . contains (Look :: WordEndHalfAscii) { if ! self . is_word_end_half_ascii (haystack , at) { return false ; } } if set . contains (Look :: WordStartHalfUnicode) { if ! self . is_word_start_half_unicode (haystack , at) . unwrap () { return false ; } } if set . contains (Look :: WordEndHalfUnicode) { if ! self . is_word_end_half_unicode (haystack , at) . unwrap () { return false ; } } true } # [doc = " Split up the given byte classes into equivalence classes in a way that"] # [doc = " is consistent with this look-around assertion."] # [cfg (feature = "alloc")] pub (crate) fn add_to_byteset (& self , look : Look , set : & mut crate :: util :: alphabet :: ByteClassSet ,) { match look { Look :: Start | Look :: End => { } Look :: StartLF | Look :: EndLF => { set . set_range (self . lineterm . 0 , self . lineterm . 0) ; } Look :: StartCRLF | Look :: EndCRLF => { set . set_range (b'\r' , b'\r') ; set . set_range (b'\n' , b'\n') ; } Look :: WordAscii | Look :: WordAsciiNegate | Look :: WordUnicode | Look :: WordUnicodeNegate | Look :: WordStartAscii | Look :: WordEndAscii | Look :: WordStartUnicode | Look :: WordEndUnicode | Look :: WordStartHalfAscii | Look :: WordEndHalfAscii | Look :: WordStartHalfUnicode | Look :: WordEndHalfUnicode => { let iswb = utf8 :: is_word_byte ; let asu8 = | b : u16 | u8 :: try_from (b) . unwrap () ; let mut b1 : u16 = 0 ; let mut b2 : u16 ; while b1 <= 255 { b2 = b1 + 1 ; while b2 <= 255 && iswb (asu8 (b1)) == iswb (asu8 (b2)) { b2 += 1 ; } assert ! (b2 <= 256) ; set . set_range (asu8 (b1) , asu8 (b2 . checked_sub (1) . unwrap ())) ; b1 = b2 ; } } } } # [doc = " Returns true when [`Look::Start`] is satisfied `at` the given position"] # [doc = " in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn is_start (& self , _haystack : & [u8] , at : usize) -> bool { at == 0 } # [doc = " Returns true when [`Look::End`] is satisfied `at` the given position in"] # [doc = " `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn is_end (& self , haystack : & [u8] , at : usize) -> bool { at == haystack . len () } # [doc = " Returns true when [`Look::StartLF`] is satisfied `at` the given"] # [doc = " position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn is_start_lf (& self , haystack : & [u8] , at : usize) -> bool { self . is_start (haystack , at) || haystack [at - 1] == self . lineterm . 0 } # [doc = " Returns true when [`Look::EndLF`] is satisfied `at` the given position"] # [doc = " in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn is_end_lf (& self , haystack : & [u8] , at : usize) -> bool { self . is_end (haystack , at) || haystack [at] == self . lineterm . 0 } # [doc = " Returns true when [`Look::StartCRLF`] is satisfied `at` the given"] # [doc = " position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn is_start_crlf (& self , haystack : & [u8] , at : usize) -> bool { self . is_start (haystack , at) || haystack [at - 1] == b'\n' || (haystack [at - 1] == b'\r' && (at >= haystack . len () || haystack [at] != b'\n')) } # [doc = " Returns true when [`Look::EndCRLF`] is satisfied `at` the given"] # [doc = " position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn is_end_crlf (& self , haystack : & [u8] , at : usize) -> bool { self . is_end (haystack , at) || haystack [at] == b'\r' || (haystack [at] == b'\n' && (at == 0 || haystack [at - 1] != b'\r')) } # [doc = " Returns true when [`Look::WordAscii`] is satisfied `at` the given"] # [doc = " position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn is_word_ascii (& self , haystack : & [u8] , at : usize) -> bool { let word_before = at > 0 && utf8 :: is_word_byte (haystack [at - 1]) ; let word_after = at < haystack . len () && utf8 :: is_word_byte (haystack [at]) ; word_before != word_after } # [doc = " Returns true when [`Look::WordAsciiNegate`] is satisfied `at` the given"] # [doc = " position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn is_word_ascii_negate (& self , haystack : & [u8] , at : usize) -> bool { ! self . is_word_ascii (haystack , at) } # [doc = " Returns true when [`Look::WordUnicode`] is satisfied `at` the given"] # [doc = " position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This returns an error when Unicode word boundary tables"] # [doc = " are not available. Specifically, this only occurs when the"] # [doc = " `unicode-word-boundary` feature is not enabled."] # [inline] pub fn is_word_unicode (& self , haystack : & [u8] , at : usize ,) -> Result < bool , UnicodeWordBoundaryError > { let word_before = is_word_char :: rev (haystack , at) ? ; let word_after = is_word_char :: fwd (haystack , at) ? ; Ok (word_before != word_after) } # [doc = " Returns true when [`Look::WordUnicodeNegate`] is satisfied `at` the"] # [doc = " given position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This returns an error when Unicode word boundary tables"] # [doc = " are not available. Specifically, this only occurs when the"] # [doc = " `unicode-word-boundary` feature is not enabled."] # [inline] pub fn is_word_unicode_negate (& self , haystack : & [u8] , at : usize ,) -> Result < bool , UnicodeWordBoundaryError > { let word_before = at > 0 && match utf8 :: decode_last (& haystack [.. at]) { None | Some (Err (_)) => return Ok (false) , Some (Ok (_)) => is_word_char :: rev (haystack , at) ? , } ; let word_after = at < haystack . len () && match utf8 :: decode (& haystack [at ..]) { None | Some (Err (_)) => return Ok (false) , Some (Ok (_)) => is_word_char :: fwd (haystack , at) ? , } ; Ok (word_before == word_after) } # [doc = " Returns true when [`Look::WordStartAscii`] is satisfied `at` the given"] # [doc = " position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn is_word_start_ascii (& self , haystack : & [u8] , at : usize) -> bool { let word_before = at > 0 && utf8 :: is_word_byte (haystack [at - 1]) ; let word_after = at < haystack . len () && utf8 :: is_word_byte (haystack [at]) ; ! word_before && word_after } # [doc = " Returns true when [`Look::WordEndAscii`] is satisfied `at` the given"] # [doc = " position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn is_word_end_ascii (& self , haystack : & [u8] , at : usize) -> bool { let word_before = at > 0 && utf8 :: is_word_byte (haystack [at - 1]) ; let word_after = at < haystack . len () && utf8 :: is_word_byte (haystack [at]) ; word_before && ! word_after } # [doc = " Returns true when [`Look::WordStartUnicode`] is satisfied `at` the"] # [doc = " given position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This returns an error when Unicode word boundary tables"] # [doc = " are not available. Specifically, this only occurs when the"] # [doc = " `unicode-word-boundary` feature is not enabled."] # [inline] pub fn is_word_start_unicode (& self , haystack : & [u8] , at : usize ,) -> Result < bool , UnicodeWordBoundaryError > { let word_before = is_word_char :: rev (haystack , at) ? ; let word_after = is_word_char :: fwd (haystack , at) ? ; Ok (! word_before && word_after) } # [doc = " Returns true when [`Look::WordEndUnicode`] is satisfied `at` the"] # [doc = " given position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This returns an error when Unicode word boundary tables"] # [doc = " are not available. Specifically, this only occurs when the"] # [doc = " `unicode-word-boundary` feature is not enabled."] # [inline] pub fn is_word_end_unicode (& self , haystack : & [u8] , at : usize ,) -> Result < bool , UnicodeWordBoundaryError > { let word_before = is_word_char :: rev (haystack , at) ? ; let word_after = is_word_char :: fwd (haystack , at) ? ; Ok (word_before && ! word_after) } # [doc = " Returns true when [`Look::WordStartHalfAscii`] is satisfied `at` the"] # [doc = " given position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn is_word_start_half_ascii (& self , haystack : & [u8] , at : usize ,) -> bool { let word_before = at > 0 && utf8 :: is_word_byte (haystack [at - 1]) ; ! word_before } # [doc = " Returns true when [`Look::WordEndHalfAscii`] is satisfied `at` the"] # [doc = " given position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [inline] pub fn is_word_end_half_ascii (& self , haystack : & [u8] , at : usize) -> bool { let word_after = at < haystack . len () && utf8 :: is_word_byte (haystack [at]) ; ! word_after } # [doc = " Returns true when [`Look::WordStartHalfUnicode`] is satisfied `at` the"] # [doc = " given position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This returns an error when Unicode word boundary tables"] # [doc = " are not available. Specifically, this only occurs when the"] # [doc = " `unicode-word-boundary` feature is not enabled."] # [inline] pub fn is_word_start_half_unicode (& self , haystack : & [u8] , at : usize ,) -> Result < bool , UnicodeWordBoundaryError > { let word_before = at > 0 && match utf8 :: decode_last (& haystack [.. at]) { None | Some (Err (_)) => return Ok (false) , Some (Ok (_)) => is_word_char :: rev (haystack , at) ? , } ; Ok (! word_before) } # [doc = " Returns true when [`Look::WordEndHalfUnicode`] is satisfied `at` the"] # [doc = " given position in `haystack`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This may panic when `at > haystack.len()`. Note that `at =="] # [doc = " haystack.len()` is legal and guaranteed not to panic."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This returns an error when Unicode word boundary tables"] # [doc = " are not available. Specifically, this only occurs when the"] # [doc = " `unicode-word-boundary` feature is not enabled."] # [inline] pub fn is_word_end_half_unicode (& self , haystack : & [u8] , at : usize ,) -> Result < bool , UnicodeWordBoundaryError > { let word_after = at < haystack . len () && match utf8 :: decode (& haystack [at ..]) { None | Some (Err (_)) => return Ok (false) , Some (Ok (_)) => is_word_char :: fwd (haystack , at) ? , } ; Ok (! word_after) } }
    };
}

impl_693!();