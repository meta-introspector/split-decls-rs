macro_rules! deps {
    () => {
        Config!();
        StateID!();
        PatternIter!();
        GroupInfo!();
        WhichCaptures!();
        LookSet!();
        Compiler!();
        NFA!();
        Inner!();
        ByteClassSet!();
        PatternID!();
        DFA!();
        Input!();
        LookMatcher!();
        Builder!();
        State!();
        ByteClasses!();
        Match!();
        BuildError!();
        PikeVM!();
    };
}

macro_rules! impl_513 {
    () => {
        deps!();
        impl NFA { # [doc = " Parse the given regular expression using a default configuration and"] # [doc = " build an NFA from it."] # [doc = ""] # [doc = " If you want a non-default configuration, then use the NFA"] # [doc = " [`Compiler`] with a [`Config`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, Match};"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"foo[0-9]+\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " let expected = Some(Match::must(0, 0..8));"] # [doc = " re.captures(&mut cache, b\"foo12345\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] pub fn new (pattern : & str) -> Result < NFA , BuildError > { NFA :: compiler () . build (pattern) } # [doc = " Parse the given regular expressions using a default configuration and"] # [doc = " build a multi-NFA from them."] # [doc = ""] # [doc = " If you want a non-default configuration, then use the NFA"] # [doc = " [`Compiler`] with a [`Config`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, Match};"] # [doc = ""] # [doc = " let re = PikeVM::new_many(&[\"[0-9]+\", \"[a-z]+\"])?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " let expected = Some(Match::must(1, 0..3));"] # [doc = " re.captures(&mut cache, b\"foo12345bar\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] pub fn new_many < P : AsRef < str > > (patterns : & [P]) -> Result < NFA , BuildError > { NFA :: compiler () . build_many (patterns) } # [doc = " Returns an NFA with a single regex pattern that always matches at every"] # [doc = " position."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::{NFA, pikevm::PikeVM}, Match};"] # [doc = ""] # [doc = " let re = PikeVM::new_from_nfa(NFA::always_match())?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " let expected = Some(Match::must(0, 0..0));"] # [doc = " re.captures(&mut cache, b\"\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = " re.captures(&mut cache, b\"foo\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn always_match () -> NFA { let mut builder = Builder :: new () ; let pid = builder . start_pattern () . unwrap () ; assert_eq ! (pid . as_usize () , 0) ; let start_id = builder . add_capture_start (StateID :: ZERO , 0 , None) . unwrap () ; let end_id = builder . add_capture_end (StateID :: ZERO , 0) . unwrap () ; let match_id = builder . add_match () . unwrap () ; builder . patch (start_id , end_id) . unwrap () ; builder . patch (end_id , match_id) . unwrap () ; let pid = builder . finish_pattern (start_id) . unwrap () ; assert_eq ! (pid . as_usize () , 0) ; builder . build (start_id , start_id) . unwrap () } # [doc = " Returns an NFA that never matches at any position."] # [doc = ""] # [doc = " This is a convenience routine for creating an NFA with zero patterns."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::{NFA, pikevm::PikeVM};"] # [doc = ""] # [doc = " let re = PikeVM::new_from_nfa(NFA::never_match())?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, b\"\", &mut caps);"] # [doc = " assert!(!caps.is_match());"] # [doc = " re.captures(&mut cache, b\"foo\", &mut caps);"] # [doc = " assert!(!caps.is_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn never_match () -> NFA { let mut builder = Builder :: new () ; let sid = builder . add_fail () . unwrap () ; builder . build (sid , sid) . unwrap () } # [doc = " Return a default configuration for an `NFA`."] # [doc = ""] # [doc = " This is a convenience routine to avoid needing to import the `Config`"] # [doc = " type when customizing the construction of an NFA."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to build an NFA with a small size limit that"] # [doc = " results in a compilation error for any regex that tries to use more"] # [doc = " heap memory than the configured limit."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::{NFA, pikevm::PikeVM};"] # [doc = ""] # [doc = " let result = PikeVM::builder()"] # [doc = "     .thompson(NFA::config().nfa_size_limit(Some(1_000)))"] # [doc = "     // Remember, \\w is Unicode-aware by default and thus huge."] # [doc = "     .build(r\"\\w+\");"] # [doc = " assert!(result.is_err());"] # [doc = " ```"] # [cfg (feature = "syntax")] pub fn config () -> Config { Config :: new () } # [doc = " Return a compiler for configuring the construction of an `NFA`."] # [doc = ""] # [doc = " This is a convenience routine to avoid needing to import the"] # [doc = " [`Compiler`] type in common cases."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to build an NFA that is permitted match invalid"] # [doc = " UTF-8. Without the additional syntax configuration here, compilation of"] # [doc = " `(?-u:.)` would fail because it is permitted to match invalid UTF-8."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     util::syntax,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::builder()"] # [doc = "     .syntax(syntax::Config::new().utf8(false))"] # [doc = "     .build(r\"[a-z]+(?-u:.)\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " let expected = Some(Match::must(0, 1..5));"] # [doc = " re.captures(&mut cache, b\"\\xFFabc\\xFF\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] pub fn compiler () -> Compiler { Compiler :: new () } # [doc = " Returns an iterator over all pattern identifiers in this NFA."] # [doc = ""] # [doc = " Pattern IDs are allocated in sequential order starting from zero,"] # [doc = " where the order corresponds to the order of patterns provided to the"] # [doc = " [`NFA::new_many`] constructor."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, PatternID};"] # [doc = ""] # [doc = " let nfa = NFA::new_many(&[\"[0-9]+\", \"[a-z]+\", \"[A-Z]+\"])?;"] # [doc = " let pids: Vec<PatternID> = nfa.patterns().collect();"] # [doc = " assert_eq!(pids, vec!["] # [doc = "     PatternID::must(0),"] # [doc = "     PatternID::must(1),"] # [doc = "     PatternID::must(2),"] # [doc = " ]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn patterns (& self) -> PatternIter < '_ > { PatternIter { it : PatternID :: iter (self . pattern_len ()) , _marker : core :: marker :: PhantomData , } } # [doc = " Returns the total number of regex patterns in this NFA."] # [doc = ""] # [doc = " This may return zero if the NFA was constructed with no patterns. In"] # [doc = " this case, the NFA can never produce a match for any input."] # [doc = ""] # [doc = " This is guaranteed to be no bigger than [`PatternID::LIMIT`] because"] # [doc = " NFA construction will fail if too many patterns are added."] # [doc = ""] # [doc = " It is always true that `nfa.patterns().count() == nfa.pattern_len()`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::NFA;"] # [doc = ""] # [doc = " let nfa = NFA::new_many(&[\"[0-9]+\", \"[a-z]+\", \"[A-Z]+\"])?;"] # [doc = " assert_eq!(3, nfa.pattern_len());"] # [doc = ""] # [doc = " let nfa = NFA::never_match();"] # [doc = " assert_eq!(0, nfa.pattern_len());"] # [doc = ""] # [doc = " let nfa = NFA::always_match();"] # [doc = " assert_eq!(1, nfa.pattern_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn pattern_len (& self) -> usize { self . 0 . start_pattern . len () } # [doc = " Return the state identifier of the initial anchored state of this NFA."] # [doc = ""] # [doc = " The returned identifier is guaranteed to be a valid index into the"] # [doc = " slice returned by [`NFA::states`], and is also a valid argument to"] # [doc = " [`NFA::state`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows a somewhat contrived example where we can easily"] # [doc = " predict the anchored starting state."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::{NFA, State, WhichCaptures};"] # [doc = ""] # [doc = " let nfa = NFA::compiler()"] # [doc = "     .configure(NFA::config().which_captures(WhichCaptures::None))"] # [doc = "     .build(\"a\")?;"] # [doc = " let state = nfa.state(nfa.start_anchored());"] # [doc = " match *state {"] # [doc = "     State::ByteRange { trans } => {"] # [doc = "         assert_eq!(b'a', trans.start);"] # [doc = "         assert_eq!(b'a', trans.end);"] # [doc = "     }"] # [doc = "     _ => unreachable!(\"unexpected state\"),"] # [doc = " }"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn start_anchored (& self) -> StateID { self . 0 . start_anchored } # [doc = " Return the state identifier of the initial unanchored state of this"] # [doc = " NFA."] # [doc = ""] # [doc = " This is equivalent to the identifier returned by"] # [doc = " [`NFA::start_anchored`] when the NFA has no unanchored starting state."] # [doc = ""] # [doc = " The returned identifier is guaranteed to be a valid index into the"] # [doc = " slice returned by [`NFA::states`], and is also a valid argument to"] # [doc = " [`NFA::state`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows that the anchored and unanchored starting states"] # [doc = " are equivalent when an anchored NFA is built."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::NFA;"] # [doc = ""] # [doc = " let nfa = NFA::new(\"^a\")?;"] # [doc = " assert_eq!(nfa.start_anchored(), nfa.start_unanchored());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn start_unanchored (& self) -> StateID { self . 0 . start_unanchored } # [doc = " Return the state identifier of the initial anchored state for the given"] # [doc = " pattern, or `None` if there is no pattern corresponding to the given"] # [doc = " identifier."] # [doc = ""] # [doc = " If one uses the starting state for a particular pattern, then the only"] # [doc = " match that can be returned is for the corresponding pattern."] # [doc = ""] # [doc = " The returned identifier is guaranteed to be a valid index into the"] # [doc = " slice returned by [`NFA::states`], and is also a valid argument to"] # [doc = " [`NFA::state`]."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the pattern doesn't exist in this NFA, then this returns an error."] # [doc = " This occurs when `pid.as_usize() >= nfa.pattern_len()`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows that the anchored and unanchored starting states"] # [doc = " are equivalent when an anchored NFA is built."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, PatternID};"] # [doc = ""] # [doc = " let nfa = NFA::new_many(&[\"^a\", \"^b\"])?;"] # [doc = " // The anchored and unanchored states for the entire NFA are the same,"] # [doc = " // since all of the patterns are anchored."] # [doc = " assert_eq!(nfa.start_anchored(), nfa.start_unanchored());"] # [doc = " // But the anchored starting states for each pattern are distinct,"] # [doc = " // because these starting states can only lead to matches for the"] # [doc = " // corresponding pattern."] # [doc = " let anchored = Some(nfa.start_anchored());"] # [doc = " assert_ne!(anchored, nfa.start_pattern(PatternID::must(0)));"] # [doc = " assert_ne!(anchored, nfa.start_pattern(PatternID::must(1)));"] # [doc = " // Requesting a pattern not in the NFA will result in None:"] # [doc = " assert_eq!(None, nfa.start_pattern(PatternID::must(2)));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn start_pattern (& self , pid : PatternID) -> Option < StateID > { self . 0 . start_pattern . get (pid . as_usize ()) . copied () } # [doc = " Get the byte class set for this NFA."] # [doc = ""] # [doc = " A byte class set is a partitioning of this NFA's alphabet into"] # [doc = " equivalence classes. Any two bytes in the same equivalence class are"] # [doc = " guaranteed to never discriminate between a match or a non-match. (The"] # [doc = " partitioning may not be minimal.)"] # [doc = ""] # [doc = " Byte classes are used internally by this crate when building DFAs."] # [doc = " Namely, among other optimizations, they enable a space optimization"] # [doc = " where the DFA's internal alphabet is defined over the equivalence"] # [doc = " classes of bytes instead of all possible byte values. The former is"] # [doc = " often quite a bit smaller than the latter, which permits the DFA to use"] # [doc = " less space for its transition table."] # [inline] pub (crate) fn byte_class_set (& self) -> & ByteClassSet { & self . 0 . byte_class_set } # [doc = " Get the byte classes for this NFA."] # [doc = ""] # [doc = " Byte classes represent a partitioning of this NFA's alphabet into"] # [doc = " equivalence classes. Any two bytes in the same equivalence class are"] # [doc = " guaranteed to never discriminate between a match or a non-match. (The"] # [doc = " partitioning may not be minimal.)"] # [doc = ""] # [doc = " Byte classes are used internally by this crate when building DFAs."] # [doc = " Namely, among other optimizations, they enable a space optimization"] # [doc = " where the DFA's internal alphabet is defined over the equivalence"] # [doc = " classes of bytes instead of all possible byte values. The former is"] # [doc = " often quite a bit smaller than the latter, which permits the DFA to use"] # [doc = " less space for its transition table."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to query the class of various bytes."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::NFA;"] # [doc = ""] # [doc = " let nfa = NFA::new(\"[a-z]+\")?;"] # [doc = " let classes = nfa.byte_classes();"] # [doc = " // 'a' and 'z' are in the same class for this regex."] # [doc = " assert_eq!(classes.get(b'a'), classes.get(b'z'));"] # [doc = " // But 'a' and 'A' are not."] # [doc = " assert_ne!(classes.get(b'a'), classes.get(b'A'));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn byte_classes (& self) -> & ByteClasses { & self . 0 . byte_classes } # [doc = " Return a reference to the NFA state corresponding to the given ID."] # [doc = ""] # [doc = " This is a convenience routine for `nfa.states()[id]`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics when the given identifier does not reference a valid state."] # [doc = " That is, when `id.as_usize() >= nfa.states().len()`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " The anchored state for a pattern will typically correspond to a"] # [doc = " capturing state for that pattern. (Although, this is not an API"] # [doc = " guarantee!)"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::{NFA, State}, PatternID};"] # [doc = ""] # [doc = " let nfa = NFA::new(\"a\")?;"] # [doc = " let state = nfa.state(nfa.start_pattern(PatternID::ZERO).unwrap());"] # [doc = " match *state {"] # [doc = "     State::Capture { slot, .. } => {"] # [doc = "         assert_eq!(0, slot.as_usize());"] # [doc = "     }"] # [doc = "     _ => unreachable!(\"unexpected state\"),"] # [doc = " }"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn state (& self , id : StateID) -> & State { & self . states () [id] } # [doc = " Returns a slice of all states in this NFA."] # [doc = ""] # [doc = " The slice returned is indexed by `StateID`. This provides a convenient"] # [doc = " way to access states while following transitions among those states."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This demonstrates that disabling UTF-8 mode can shrink the size of the"] # [doc = " NFA considerably in some cases, especially when using Unicode character"] # [doc = " classes."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::nfa::thompson::NFA;"] # [doc = ""] # [doc = " let nfa_unicode = NFA::new(r\"\\w\")?;"] # [doc = " let nfa_ascii = NFA::new(r\"(?-u)\\w\")?;"] # [doc = " // Yes, a factor of 45 difference. No lie."] # [doc = " assert!(40 * nfa_ascii.states().len() < nfa_unicode.states().len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn states (& self) -> & [State] { & self . 0 . states } # [doc = " Returns the capturing group info for this NFA."] # [doc = ""] # [doc = " The [`GroupInfo`] provides a way to map to and from capture index"] # [doc = " and capture name for each pattern. It also provides a mapping from"] # [doc = " each of the capturing groups in every pattern to their corresponding"] # [doc = " slot offsets encoded in [`State::Capture`] states."] # [doc = ""] # [doc = " Note that `GroupInfo` uses reference counting internally, such that"] # [doc = " cloning a `GroupInfo` is very cheap."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to get a list of all capture group names for"] # [doc = " a particular pattern."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, PatternID};"] # [doc = ""] # [doc = " let nfa = NFA::new(r\"(a)(?P<foo>b)(c)(d)(?P<bar>e)\")?;"] # [doc = " // The first is the implicit group that is always unnamed. The next"] # [doc = " // 5 groups are the explicit groups found in the concrete syntax above."] # [doc = " let expected = vec![None, None, Some(\"foo\"), None, None, Some(\"bar\")];"] # [doc = " let got: Vec<Option<&str>> ="] # [doc = "     nfa.group_info().pattern_names(PatternID::ZERO).collect();"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " // Using an invalid pattern ID will result in nothing yielded."] # [doc = " let got = nfa.group_info().pattern_names(PatternID::must(999)).count();"] # [doc = " assert_eq!(0, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn group_info (& self) -> & GroupInfo { & self . 0 . group_info () } # [doc = " Returns true if and only if this NFA has at least one"] # [doc = " [`Capture`](State::Capture) in its sequence of states."] # [doc = ""] # [doc = " This is useful as a way to perform a quick test before attempting"] # [doc = " something that does or does not require capture states. For example,"] # [doc = " some regex engines (like the PikeVM) require capture states in order to"] # [doc = " work at all."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows a few different NFAs and whether they have captures"] # [doc = " or not."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::{NFA, WhichCaptures};"] # [doc = ""] # [doc = " // Obviously has capture states."] # [doc = " let nfa = NFA::new(\"(a)\")?;"] # [doc = " assert!(nfa.has_capture());"] # [doc = ""] # [doc = " // Less obviously has capture states, because every pattern has at"] # [doc = " // least one anonymous capture group corresponding to the match for the"] # [doc = " // entire pattern."] # [doc = " let nfa = NFA::new(\"a\")?;"] # [doc = " assert!(nfa.has_capture());"] # [doc = ""] # [doc = " // Other than hand building your own NFA, this is the only way to build"] # [doc = " // an NFA without capturing groups. In general, you should only do this"] # [doc = " // if you don't intend to use any of the NFA-oriented regex engines."] # [doc = " // Overall, capturing groups don't have many downsides. Although they"] # [doc = " // can add a bit of noise to simple NFAs, so it can be nice to disable"] # [doc = " // them for debugging purposes."] # [doc = " //"] # [doc = " // Notice that 'has_capture' is false here even when we have an"] # [doc = " // explicit capture group in the pattern."] # [doc = " let nfa = NFA::compiler()"] # [doc = "     .configure(NFA::config().which_captures(WhichCaptures::None))"] # [doc = "     .build(\"(a)\")?;"] # [doc = " assert!(!nfa.has_capture());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn has_capture (& self) -> bool { self . 0 . has_capture } # [doc = " Returns true if and only if this NFA can match the empty string."] # [doc = " When it returns false, all possible matches are guaranteed to have a"] # [doc = " non-zero length."] # [doc = ""] # [doc = " This is useful as cheap way to know whether code needs to handle the"] # [doc = " case of a zero length match. This is particularly important when UTF-8"] # [doc = " modes are enabled, as when UTF-8 mode is enabled, empty matches that"] # [doc = " split a codepoint must never be reported. This extra handling can"] # [doc = " sometimes be costly, and since regexes matching an empty string are"] # [doc = " somewhat rare, it can be beneficial to treat such regexes specially."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows a few different NFAs and whether they match the"] # [doc = " empty string or not. Notice the empty string isn't merely a matter"] # [doc = " of a string of length literally `0`, but rather, whether a match can"] # [doc = " occur between specific pairs of bytes."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, util::syntax};"] # [doc = ""] # [doc = " // The empty regex matches the empty string."] # [doc = " let nfa = NFA::new(\"\")?;"] # [doc = " assert!(nfa.has_empty(), \"empty matches empty\");"] # [doc = " // The '+' repetition operator requires at least one match, and so"] # [doc = " // does not match the empty string."] # [doc = " let nfa = NFA::new(\"a+\")?;"] # [doc = " assert!(!nfa.has_empty(), \"+ does not match empty\");"] # [doc = " // But the '*' repetition operator does."] # [doc = " let nfa = NFA::new(\"a*\")?;"] # [doc = " assert!(nfa.has_empty(), \"* does match empty\");"] # [doc = " // And wrapping '+' in an operator that can match an empty string also"] # [doc = " // causes it to match the empty string too."] # [doc = " let nfa = NFA::new(\"(a+)*\")?;"] # [doc = " assert!(nfa.has_empty(), \"+ inside of * matches empty\");"] # [doc = ""] # [doc = " // If a regex is just made of a look-around assertion, even if the"] # [doc = " // assertion requires some kind of non-empty string around it (such as"] # [doc = " // \\b), then it is still treated as if it matches the empty string."] # [doc = " // Namely, if a match occurs of just a look-around assertion, then the"] # [doc = " // match returned is empty."] # [doc = " let nfa = NFA::compiler()"] # [doc = "     .syntax(syntax::Config::new().utf8(false))"] # [doc = "     .build(r\"^$\\A\\z\\b\\B(?-u:\\b\\B)\")?;"] # [doc = " assert!(nfa.has_empty(), \"assertions match empty\");"] # [doc = " // Even when an assertion is wrapped in a '+', it still matches the"] # [doc = " // empty string."] # [doc = " let nfa = NFA::new(r\"\\b+\")?;"] # [doc = " assert!(nfa.has_empty(), \"+ of an assertion matches empty\");"] # [doc = ""] # [doc = " // An alternation with even one branch that can match the empty string"] # [doc = " // is also said to match the empty string overall."] # [doc = " let nfa = NFA::new(\"foo|(bar)?|quux\")?;"] # [doc = " assert!(nfa.has_empty(), \"alternations can match empty\");"] # [doc = ""] # [doc = " // An NFA that matches nothing does not match the empty string."] # [doc = " let nfa = NFA::new(\"[a&&b]\")?;"] # [doc = " assert!(!nfa.has_empty(), \"never matching means not matching empty\");"] # [doc = " // But if it's wrapped in something that doesn't require a match at"] # [doc = " // all, then it can match the empty string!"] # [doc = " let nfa = NFA::new(\"[a&&b]*\")?;"] # [doc = " assert!(nfa.has_empty(), \"* on never-match still matches empty\");"] # [doc = " // Since a '+' requires a match, using it on something that can never"] # [doc = " // match will itself produce a regex that can never match anything,"] # [doc = " // and thus does not match the empty string."] # [doc = " let nfa = NFA::new(\"[a&&b]+\")?;"] # [doc = " assert!(!nfa.has_empty(), \"+ on never-match still matches nothing\");"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn has_empty (& self) -> bool { self . 0 . has_empty } # [doc = " Whether UTF-8 mode is enabled for this NFA or not."] # [doc = ""] # [doc = " When UTF-8 mode is enabled, all matches reported by a regex engine"] # [doc = " derived from this NFA are guaranteed to correspond to spans of valid"] # [doc = " UTF-8. This includes zero-width matches. For example, the regex engine"] # [doc = " must guarantee that the empty regex will not match at the positions"] # [doc = " between code units in the UTF-8 encoding of a single codepoint."] # [doc = ""] # [doc = " See [`Config::utf8`] for more information."] # [doc = ""] # [doc = " This is enabled by default."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how UTF-8 mode can impact the match spans that may"] # [doc = " be reported in certain cases."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::{self, pikevm::PikeVM},"] # [doc = "     Match, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::new(\"\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " // UTF-8 mode is enabled by default."] # [doc = " let mut input = Input::new(\"☃\");"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(Some(Match::must(0, 0..0)), caps.get_match());"] # [doc = ""] # [doc = " // Even though an empty regex matches at 1..1, our next match is"] # [doc = " // 3..3 because 1..1 and 2..2 split the snowman codepoint (which is"] # [doc = " // three bytes long)."] # [doc = " input.set_start(1);"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(Some(Match::must(0, 3..3)), caps.get_match());"] # [doc = ""] # [doc = " // But if we disable UTF-8, then we'll get matches at 1..1 and 2..2:"] # [doc = " let re = PikeVM::builder()"] # [doc = "     .thompson(thompson::Config::new().utf8(false))"] # [doc = "     .build(\"\")?;"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(Some(Match::must(0, 1..1)), caps.get_match());"] # [doc = ""] # [doc = " input.set_start(2);"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(Some(Match::must(0, 2..2)), caps.get_match());"] # [doc = ""] # [doc = " input.set_start(3);"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(Some(Match::must(0, 3..3)), caps.get_match());"] # [doc = ""] # [doc = " input.set_start(4);"] # [doc = " re.search(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(None, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn is_utf8 (& self) -> bool { self . 0 . utf8 } # [doc = " Returns true when this NFA is meant to be matched in reverse."] # [doc = ""] # [doc = " Generally speaking, when this is true, it means the NFA is supposed to"] # [doc = " be used in conjunction with moving backwards through the haystack. That"] # [doc = " is, from a higher memory address to a lower memory address."] # [doc = ""] # [doc = " It is often the case that lower level routines dealing with an NFA"] # [doc = " don't need to care about whether it is \"meant\" to be matched in reverse"] # [doc = " or not. However, there are some specific cases where it matters. For"] # [doc = " example, the implementation of CRLF-aware `^` and `$` line anchors"] # [doc = " needs to know whether the search is in the forward or reverse"] # [doc = " direction. In the forward direction, neither `^` nor `$` should match"] # [doc = " when a `\\r` has been seen previously and a `\\n` is next. However, in"] # [doc = " the reverse direction, neither `^` nor `$` should match when a `\\n`"] # [doc = " has been seen previously and a `\\r` is next. This fundamentally changes"] # [doc = " how the state machine is constructed, and thus needs to be altered"] # [doc = " based on the direction of the search."] # [doc = ""] # [doc = " This is automatically set when using a [`Compiler`] with a configuration"] # [doc = " where [`Config::reverse`] is enabled. If you're building your own NFA"] # [doc = " by hand via a [`Builder`]"] # [inline] pub fn is_reverse (& self) -> bool { self . 0 . reverse } # [doc = " Returns true if and only if all starting states for this NFA correspond"] # [doc = " to the beginning of an anchored search."] # [doc = ""] # [doc = " Typically, an NFA will have both an anchored and an unanchored starting"] # [doc = " state. Namely, because it tends to be useful to have both and the cost"] # [doc = " of having an unanchored starting state is almost zero (for an NFA)."] # [doc = " However, if all patterns in the NFA are themselves anchored, then even"] # [doc = " the unanchored starting state will correspond to an anchored search"] # [doc = " since the pattern doesn't permit anything else."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows a few different scenarios where this method's"] # [doc = " return value varies."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::NFA;"] # [doc = ""] # [doc = " // The unanchored starting state permits matching this pattern anywhere"] # [doc = " // in a haystack, instead of just at the beginning."] # [doc = " let nfa = NFA::new(\"a\")?;"] # [doc = " assert!(!nfa.is_always_start_anchored());"] # [doc = ""] # [doc = " // In this case, the pattern is itself anchored, so there is no way"] # [doc = " // to run an unanchored search."] # [doc = " let nfa = NFA::new(\"^a\")?;"] # [doc = " assert!(nfa.is_always_start_anchored());"] # [doc = ""] # [doc = " // When multiline mode is enabled, '^' can match at the start of a line"] # [doc = " // in addition to the start of a haystack, so an unanchored search is"] # [doc = " // actually possible."] # [doc = " let nfa = NFA::new(\"(?m)^a\")?;"] # [doc = " assert!(!nfa.is_always_start_anchored());"] # [doc = ""] # [doc = " // Weird cases also work. A pattern is only considered anchored if all"] # [doc = " // matches may only occur at the start of a haystack."] # [doc = " let nfa = NFA::new(\"(^a)|a\")?;"] # [doc = " assert!(!nfa.is_always_start_anchored());"] # [doc = ""] # [doc = " // When multiple patterns are present, if they are all anchored, then"] # [doc = " // the NFA is always anchored too."] # [doc = " let nfa = NFA::new_many(&[\"^a\", \"^b\", \"^c\"])?;"] # [doc = " assert!(nfa.is_always_start_anchored());"] # [doc = ""] # [doc = " // But if one pattern is unanchored, then the NFA must permit an"] # [doc = " // unanchored search."] # [doc = " let nfa = NFA::new_many(&[\"^a\", \"b\", \"^c\"])?;"] # [doc = " assert!(!nfa.is_always_start_anchored());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn is_always_start_anchored (& self) -> bool { self . start_anchored () == self . start_unanchored () } # [doc = " Returns the look-around matcher associated with this NFA."] # [doc = ""] # [doc = " A look-around matcher determines how to match look-around assertions."] # [doc = " In particular, some assertions are configurable. For example, the"] # [doc = " `(?m:^)` and `(?m:$)` assertions can have their line terminator changed"] # [doc = " from the default of `\\n` to any other byte."] # [doc = ""] # [doc = " If the NFA was built using a [`Compiler`], then this matcher"] # [doc = " can be set via the [`Config::look_matcher`] configuration"] # [doc = " knob. Otherwise, if you've built an NFA by hand, it is set via"] # [doc = " [`Builder::set_look_matcher`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to change the line terminator for multi-line assertions."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::{self, pikevm::PikeVM},"] # [doc = "     util::look::LookMatcher,"] # [doc = "     Match, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let mut lookm = LookMatcher::new();"] # [doc = " lookm.set_line_terminator(b'\\x00');"] # [doc = ""] # [doc = " let re = PikeVM::builder()"] # [doc = "     .thompson(thompson::Config::new().look_matcher(lookm))"] # [doc = "     .build(r\"(?m)^[a-z]+$\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " // Multi-line assertions now use NUL as a terminator."] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 1..4)),"] # [doc = "     re.find(&mut cache, b\"\\x00abc\\x00\"),"] # [doc = " );"] # [doc = " // ... and \\n is no longer recognized as a terminator."] # [doc = " assert_eq!("] # [doc = "     None,"] # [doc = "     re.find(&mut cache, b\"\\nabc\\n\"),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn look_matcher (& self) -> & LookMatcher { & self . 0 . look_matcher } # [doc = " Returns the union of all look-around assertions used throughout this"] # [doc = " NFA. When the returned set is empty, it implies that the NFA has no"] # [doc = " look-around assertions and thus zero conditional epsilon transitions."] # [doc = ""] # [doc = " This is useful in some cases enabling optimizations. It is not"] # [doc = " unusual, for example, for optimizations to be of the form, \"for any"] # [doc = " regex with zero conditional epsilon transitions, do ...\" where \"...\""] # [doc = " is some kind of optimization."] # [doc = ""] # [doc = " This isn't only helpful for optimizations either. Sometimes look-around"] # [doc = " assertions are difficult to support. For example, many of the DFAs in"] # [doc = " this crate don't support Unicode word boundaries or handle them using"] # [doc = " heuristics. Handling that correctly typically requires some kind of"] # [doc = " cheap check of whether the NFA has a Unicode word boundary in the first"] # [doc = " place."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how this routine varies based on the regex pattern:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, util::look::Look};"] # [doc = ""] # [doc = " // No look-around at all."] # [doc = " let nfa = NFA::new(\"a\")?;"] # [doc = " assert!(nfa.look_set_any().is_empty());"] # [doc = ""] # [doc = " // When multiple patterns are present, since this returns the union,"] # [doc = " // it will include look-around assertions that only appear in one"] # [doc = " // pattern."] # [doc = " let nfa = NFA::new_many(&[\"a\", \"b\", \"a^b\", \"c\"])?;"] # [doc = " assert!(nfa.look_set_any().contains(Look::Start));"] # [doc = ""] # [doc = " // Some groups of assertions have various shortcuts. For example:"] # [doc = " let nfa = NFA::new(r\"(?-u:\\b)\")?;"] # [doc = " assert!(nfa.look_set_any().contains_word());"] # [doc = " assert!(!nfa.look_set_any().contains_word_unicode());"] # [doc = " assert!(nfa.look_set_any().contains_word_ascii());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn look_set_any (& self) -> LookSet { self . 0 . look_set_any } # [doc = " Returns the union of all prefix look-around assertions for every"] # [doc = " pattern in this NFA. When the returned set is empty, it implies none of"] # [doc = " the patterns require moving through a conditional epsilon transition"] # [doc = " before inspecting the first byte in the haystack."] # [doc = ""] # [doc = " This can be useful for determining what kinds of assertions need to be"] # [doc = " satisfied at the beginning of a search. For example, typically DFAs"] # [doc = " in this crate will build a distinct starting state for each possible"] # [doc = " starting configuration that might result in look-around assertions"] # [doc = " being satisfied differently. However, if the set returned here is"] # [doc = " empty, then you know that the start state is invariant because there"] # [doc = " are no conditional epsilon transitions to consider."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how this routine varies based on the regex pattern:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::NFA, util::look::Look};"] # [doc = ""] # [doc = " // No look-around at all."] # [doc = " let nfa = NFA::new(\"a\")?;"] # [doc = " assert!(nfa.look_set_prefix_any().is_empty());"] # [doc = ""] # [doc = " // When multiple patterns are present, since this returns the union,"] # [doc = " // it will include look-around assertions that only appear in one"] # [doc = " // pattern. But it will only include assertions that are in the prefix"] # [doc = " // of a pattern. For example, this includes '^' but not '$' even though"] # [doc = " // '$' does appear."] # [doc = " let nfa = NFA::new_many(&[\"a\", \"b\", \"^ab$\", \"c\"])?;"] # [doc = " assert!(nfa.look_set_prefix_any().contains(Look::Start));"] # [doc = " assert!(!nfa.look_set_prefix_any().contains(Look::End));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn look_set_prefix_any (& self) -> LookSet { self . 0 . look_set_prefix_any } # [doc = " Returns the memory usage, in bytes, of this NFA."] # [doc = ""] # [doc = " This does **not** include the stack size used up by this NFA. To"] # [doc = " compute that, use `std::mem::size_of::<NFA>()`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows that large Unicode character classes can use quite"] # [doc = " a bit of memory."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::nfa::thompson::NFA;"] # [doc = ""] # [doc = " let nfa_unicode = NFA::new(r\"\\w\")?;"] # [doc = " let nfa_ascii = NFA::new(r\"(?-u:\\w)\")?;"] # [doc = ""] # [doc = " assert!(10 * nfa_ascii.memory_usage() < nfa_unicode.memory_usage());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn memory_usage (& self) -> usize { use core :: mem :: size_of ; size_of :: < Inner > () + self . 0 . states . len () * size_of :: < State > () + self . 0 . start_pattern . len () * size_of :: < StateID > () + self . 0 . group_info . memory_usage () + self . 0 . memory_extra } }
    };
}

impl_513!()