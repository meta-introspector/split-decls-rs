macro_rules! deps {
    () => {
        GroupInfo!();
        Span!();
        GroupInfoPatternNames!();
        Match!();
        PatternID!();
        Captures!();
        PikeVM!();
        CapturesPatternIter!();
    };
}

macro_rules! impl_609 {
    () => {
        deps!();
        impl Captures { # [doc = " Create new storage for the offsets of all matching capturing groups."] # [doc = ""] # [doc = " This routine provides the most information for matches---namely, the"] # [doc = " spans of matching capturing groups---but also requires the regex search"] # [doc = " routines to do the most work."] # [doc = ""] # [doc = " It is unspecified behavior to use the returned `Captures` value in a"] # [doc = " search with a `GroupInfo` other than the one that is provided to this"] # [doc = " constructor."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows that all capturing groups---but only ones that"] # [doc = " participated in a match---are available to query after a match has"] # [doc = " been found:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     util::captures::Captures,"] # [doc = "     Span, Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::new("] # [doc = "     r\"^(?:(?P<lower>[a-z]+)|(?P<upper>[A-Z]+))(?P<digits>[0-9]+)$\","] # [doc = " )?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = Captures::all(re.get_nfa().group_info().clone());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"ABC123\", &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " assert_eq!(Some(Match::must(0, 0..6)), caps.get_match());"] # [doc = " // The 'lower' group didn't match, so it won't have any offsets."] # [doc = " assert_eq!(None, caps.get_group_by_name(\"lower\"));"] # [doc = " assert_eq!(Some(Span::from(0..3)), caps.get_group_by_name(\"upper\"));"] # [doc = " assert_eq!(Some(Span::from(3..6)), caps.get_group_by_name(\"digits\"));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn all (group_info : GroupInfo) -> Captures { let slots = group_info . slot_len () ; Captures { group_info , pid : None , slots : vec ! [None ; slots] } } # [doc = " Create new storage for only the full match spans of a pattern. This"] # [doc = " does not include any capturing group offsets."] # [doc = ""] # [doc = " It is unspecified behavior to use the returned `Captures` value in a"] # [doc = " search with a `GroupInfo` other than the one that is provided to this"] # [doc = " constructor."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows that only overall match offsets are reported when"] # [doc = " this constructor is used. Accessing any capturing groups other than"] # [doc = " the 0th will always return `None`."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     util::captures::Captures,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::new("] # [doc = "     r\"^(?:(?P<lower>[a-z]+)|(?P<upper>[A-Z]+))(?P<digits>[0-9]+)$\","] # [doc = " )?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = Captures::matches(re.get_nfa().group_info().clone());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"ABC123\", &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " assert_eq!(Some(Match::must(0, 0..6)), caps.get_match());"] # [doc = " // We didn't ask for capturing group offsets, so they aren't available."] # [doc = " assert_eq!(None, caps.get_group_by_name(\"lower\"));"] # [doc = " assert_eq!(None, caps.get_group_by_name(\"upper\"));"] # [doc = " assert_eq!(None, caps.get_group_by_name(\"digits\"));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn matches (group_info : GroupInfo) -> Captures { let slots = group_info . pattern_len () . checked_mul (2) . unwrap () ; Captures { group_info , pid : None , slots : vec ! [None ; slots] } } # [doc = " Create new storage for only tracking which pattern matched. No offsets"] # [doc = " are stored at all."] # [doc = ""] # [doc = " It is unspecified behavior to use the returned `Captures` value in a"] # [doc = " search with a `GroupInfo` other than the one that is provided to this"] # [doc = " constructor."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows that only the pattern that matched can be accessed"] # [doc = " from a `Captures` value created via this constructor."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     util::captures::Captures,"] # [doc = "     PatternID,"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::new_many(&[r\"[a-z]+\", r\"[A-Z]+\"])?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = Captures::empty(re.get_nfa().group_info().clone());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"aABCz\", &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " assert_eq!(Some(PatternID::must(0)), caps.pattern());"] # [doc = " // We didn't ask for any offsets, so they aren't available."] # [doc = " assert_eq!(None, caps.get_match());"] # [doc = ""] # [doc = " re.captures(&mut cache, &\"aABCz\"[1..], &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " assert_eq!(Some(PatternID::must(1)), caps.pattern());"] # [doc = " // We didn't ask for any offsets, so they aren't available."] # [doc = " assert_eq!(None, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn empty (group_info : GroupInfo) -> Captures { Captures { group_info , pid : None , slots : vec ! [] } } # [doc = " Returns true if and only if this capturing group represents a match."] # [doc = ""] # [doc = " This is a convenience routine for `caps.pattern().is_some()`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " When using the PikeVM (for example), the lightest weight way of"] # [doc = " detecting whether a match exists is to create capturing groups that"] # [doc = " only track the ID of the pattern that match (if any):"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     util::captures::Captures,"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"[a-z]+\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = Captures::empty(re.get_nfa().group_info().clone());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"aABCz\", &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn is_match (& self) -> bool { self . pid . is_some () } # [doc = " Returns the identifier of the pattern that matched when this"] # [doc = " capturing group represents a match. If no match was found, then this"] # [doc = " always returns `None`."] # [doc = ""] # [doc = " This returns a pattern ID in precisely the cases in which `is_match`"] # [doc = " returns `true`. Similarly, the pattern ID returned is always the"] # [doc = " same pattern ID found in the `Match` returned by `get_match`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " When using the PikeVM (for example), the lightest weight way of"] # [doc = " detecting which pattern matched is to create capturing groups that only"] # [doc = " track the ID of the pattern that match (if any):"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     util::captures::Captures,"] # [doc = "     PatternID,"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::new_many(&[r\"[a-z]+\", r\"[A-Z]+\"])?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = Captures::empty(re.get_nfa().group_info().clone());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"ABC\", &mut caps);"] # [doc = " assert_eq!(Some(PatternID::must(1)), caps.pattern());"] # [doc = " // Recall that offsets are only available when using a non-empty"] # [doc = " // Captures value. So even though a match occurred, this returns None!"] # [doc = " assert_eq!(None, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn pattern (& self) -> Option < PatternID > { self . pid } # [doc = " Returns the pattern ID and the span of the match, if one occurred."] # [doc = ""] # [doc = " This always returns `None` when `Captures` was created with"] # [doc = " [`Captures::empty`], even if a match was found."] # [doc = ""] # [doc = " If this routine returns a non-`None` value, then `is_match` is"] # [doc = " guaranteed to return `true` and `pattern` is also guaranteed to return"] # [doc = " a non-`None` value."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to get the full match from a search:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, Match};"] # [doc = ""] # [doc = " let re = PikeVM::new_many(&[r\"[a-z]+\", r\"[A-Z]+\"])?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"ABC\", &mut caps);"] # [doc = " assert_eq!(Some(Match::must(1, 0..3)), caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn get_match (& self) -> Option < Match > { Some (Match :: new (self . pattern () ? , self . get_group (0) ?)) } # [doc = " Returns the span of a capturing group match corresponding to the group"] # [doc = " index given, only if both the overall pattern matched and the capturing"] # [doc = " group participated in that match."] # [doc = ""] # [doc = " This returns `None` if `index` is invalid. `index` is valid if and only"] # [doc = " if it's less than [`Captures::group_len`] for the matching pattern."] # [doc = ""] # [doc = " This always returns `None` when `Captures` was created with"] # [doc = " [`Captures::empty`], even if a match was found. This also always"] # [doc = " returns `None` for any `index > 0` when `Captures` was created with"] # [doc = " [`Captures::matches`]."] # [doc = ""] # [doc = " If this routine returns a non-`None` value, then `is_match` is"] # [doc = " guaranteed to return `true`, `pattern` is guaranteed to return a"] # [doc = " non-`None` value and `get_match` is guaranteed to return a non-`None`"] # [doc = " value."] # [doc = ""] # [doc = " By convention, the 0th capture group will always return the same"] # [doc = " span as the span returned by `get_match`. This is because the 0th"] # [doc = " capture group always corresponds to the entirety of the pattern's"] # [doc = " match. (It is similarly always unnamed because it is implicit.) This"] # [doc = " isn't necessarily true of all regex engines. For example, one can"] # [doc = " hand-compile a [`thompson::NFA`](crate::nfa::thompson::NFA) via a"] # [doc = " [`thompson::Builder`](crate::nfa::thompson::Builder), which isn't"] # [doc = " technically forced to make the 0th capturing group always correspond to"] # [doc = " the entire match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to get the capturing groups, by index, from a"] # [doc = " match:"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, Span, Match};"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"^(?P<first>\\pL+)\\s+(?P<last>\\pL+)$\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"Bruce Springsteen\", &mut caps);"] # [doc = " assert_eq!(Some(Match::must(0, 0..17)), caps.get_match());"] # [doc = " assert_eq!(Some(Span::from(0..5)), caps.get_group(1));"] # [doc = " assert_eq!(Some(Span::from(6..17)), caps.get_group(2));"] # [doc = " // Looking for a non-existent capturing group will return None:"] # [doc = " assert_eq!(None, caps.get_group(3));"] # [doc = " # // literals are too big for 32-bit usize: #1039"] # [doc = " # #[cfg(target_pointer_width = \"64\")]"] # [doc = " assert_eq!(None, caps.get_group(9944060567225171988));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn get_group (& self , index : usize) -> Option < Span > { let pid = self . pattern () ? ; let (slot_start , slot_end) = if self . group_info () . pattern_len () == 1 { (index . checked_mul (2) ? , index . checked_mul (2) ? . checked_add (1) ?) } else { self . group_info () . slots (pid , index) ? } ; let start = self . slots . get (slot_start) . copied () ? ? ; let end = self . slots . get (slot_end) . copied () ? ? ; Some (Span { start : start . get () , end : end . get () }) } # [doc = " Returns the span of a capturing group match corresponding to the group"] # [doc = " name given, only if both the overall pattern matched and the capturing"] # [doc = " group participated in that match."] # [doc = ""] # [doc = " This returns `None` if `name` does not correspond to a valid capturing"] # [doc = " group for the pattern that matched."] # [doc = ""] # [doc = " This always returns `None` when `Captures` was created with"] # [doc = " [`Captures::empty`], even if a match was found. This also always"] # [doc = " returns `None` for any `index > 0` when `Captures` was created with"] # [doc = " [`Captures::matches`]."] # [doc = ""] # [doc = " If this routine returns a non-`None` value, then `is_match` is"] # [doc = " guaranteed to return `true`, `pattern` is guaranteed to return a"] # [doc = " non-`None` value and `get_match` is guaranteed to return a non-`None`"] # [doc = " value."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to get the capturing groups, by name, from a"] # [doc = " match:"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, Span, Match};"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"^(?P<first>\\pL+)\\s+(?P<last>\\pL+)$\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"Bruce Springsteen\", &mut caps);"] # [doc = " assert_eq!(Some(Match::must(0, 0..17)), caps.get_match());"] # [doc = " assert_eq!(Some(Span::from(0..5)), caps.get_group_by_name(\"first\"));"] # [doc = " assert_eq!(Some(Span::from(6..17)), caps.get_group_by_name(\"last\"));"] # [doc = " // Looking for a non-existent capturing group will return None:"] # [doc = " assert_eq!(None, caps.get_group_by_name(\"middle\"));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn get_group_by_name (& self , name : & str) -> Option < Span > { let index = self . group_info () . to_index (self . pattern () ? , name) ? ; self . get_group (index) } # [doc = " Returns an iterator of possible spans for every capturing group in the"] # [doc = " matching pattern."] # [doc = ""] # [doc = " If this `Captures` value does not correspond to a match, then the"] # [doc = " iterator returned yields no elements."] # [doc = ""] # [doc = " Note that the iterator returned yields elements of type `Option<Span>`."] # [doc = " A span is present if and only if it corresponds to a capturing group"] # [doc = " that participated in a match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to collect all capturing groups:"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, Span};"] # [doc = ""] # [doc = " let re = PikeVM::new("] # [doc = "     // Matches first/last names, with an optional middle name."] # [doc = "     r\"^(?P<first>\\pL+)\\s+(?:(?P<middle>\\pL+)\\s+)?(?P<last>\\pL+)$\","] # [doc = " )?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"Harry James Potter\", &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " let groups: Vec<Option<Span>> = caps.iter().collect();"] # [doc = " assert_eq!(groups, vec!["] # [doc = "     Some(Span::from(0..18)),"] # [doc = "     Some(Span::from(0..5)),"] # [doc = "     Some(Span::from(6..11)),"] # [doc = "     Some(Span::from(12..18)),"] # [doc = " ]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " This example uses the same regex as the previous example, but with a"] # [doc = " haystack that omits the middle name. This results in a capturing group"] # [doc = " that is present in the elements yielded by the iterator but without a"] # [doc = " match:"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, Span};"] # [doc = ""] # [doc = " let re = PikeVM::new("] # [doc = "     // Matches first/last names, with an optional middle name."] # [doc = "     r\"^(?P<first>\\pL+)\\s+(?:(?P<middle>\\pL+)\\s+)?(?P<last>\\pL+)$\","] # [doc = " )?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"Harry Potter\", &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " let groups: Vec<Option<Span>> = caps.iter().collect();"] # [doc = " assert_eq!(groups, vec!["] # [doc = "     Some(Span::from(0..12)),"] # [doc = "     Some(Span::from(0..5)),"] # [doc = "     None,"] # [doc = "     Some(Span::from(6..12)),"] # [doc = " ]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn iter (& self) -> CapturesPatternIter < '_ > { let names = self . pattern () . map_or (GroupInfoPatternNames :: empty () . enumerate () , | pid | { self . group_info () . pattern_names (pid) . enumerate () }) ; CapturesPatternIter { caps : self , names } } # [doc = " Return the total number of capturing groups for the matching pattern."] # [doc = ""] # [doc = " If this `Captures` value does not correspond to a match, then this"] # [doc = " always returns `0`."] # [doc = ""] # [doc = " This always returns the same number of elements yielded by"] # [doc = " [`Captures::iter`]. That is, the number includes capturing groups even"] # [doc = " if they don't participate in the match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to count the total number of capturing groups"] # [doc = " associated with a pattern. Notice that it includes groups that did not"] # [doc = " participate in a match (just like `Captures::iter` does)."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::nfa::thompson::pikevm::PikeVM;"] # [doc = ""] # [doc = " let re = PikeVM::new("] # [doc = "     // Matches first/last names, with an optional middle name."] # [doc = "     r\"^(?P<first>\\pL+)\\s+(?:(?P<middle>\\pL+)\\s+)?(?P<last>\\pL+)$\","] # [doc = " )?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"Harry Potter\", &mut caps);"] # [doc = " assert_eq!(4, caps.group_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn group_len (& self) -> usize { let pid = match self . pattern () { None => return 0 , Some (pid) => pid , } ; self . group_info () . group_len (pid) } # [doc = " Returns a reference to the underlying group info on which these"] # [doc = " captures are based."] # [doc = ""] # [doc = " The difference between `GroupInfo` and `Captures` is that the former"] # [doc = " defines the structure of capturing groups where as the latter is what"] # [doc = " stores the actual match information. So where as `Captures` only gives"] # [doc = " you access to the current match, `GroupInfo` lets you query any"] # [doc = " information about all capturing groups, even ones for patterns that"] # [doc = " weren't involved in a match."] # [doc = ""] # [doc = " Note that a `GroupInfo` uses reference counting internally, so it may"] # [doc = " be cloned cheaply."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to get all capturing group names from the"] # [doc = " underlying `GroupInfo`. Notice that we don't even need to run a"] # [doc = " search."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, PatternID};"] # [doc = ""] # [doc = " let re = PikeVM::new_many(&["] # [doc = "     r\"(?P<foo>a)\","] # [doc = "     r\"(a)(b)\","] # [doc = "     r\"ab\","] # [doc = "     r\"(?P<bar>a)(?P<quux>a)\","] # [doc = "     r\"(?P<foo>z)\","] # [doc = " ])?;"] # [doc = " let caps = re.create_captures();"] # [doc = ""] # [doc = " let expected = vec!["] # [doc = "     (PatternID::must(0), 0, None),"] # [doc = "     (PatternID::must(0), 1, Some(\"foo\")),"] # [doc = "     (PatternID::must(1), 0, None),"] # [doc = "     (PatternID::must(1), 1, None),"] # [doc = "     (PatternID::must(1), 2, None),"] # [doc = "     (PatternID::must(2), 0, None),"] # [doc = "     (PatternID::must(3), 0, None),"] # [doc = "     (PatternID::must(3), 1, Some(\"bar\")),"] # [doc = "     (PatternID::must(3), 2, Some(\"quux\")),"] # [doc = "     (PatternID::must(4), 0, None),"] # [doc = "     (PatternID::must(4), 1, Some(\"foo\")),"] # [doc = " ];"] # [doc = " // We could also just use 're.get_nfa().group_info()'."] # [doc = " let got: Vec<(PatternID, usize, Option<&str>)> ="] # [doc = "     caps.group_info().all_names().collect();"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn group_info (& self) -> & GroupInfo { & self . group_info } # [doc = " Interpolates the capture references in `replacement` with the"] # [doc = " corresponding substrings in `haystack` matched by each reference. The"] # [doc = " interpolated string is returned."] # [doc = ""] # [doc = " See the [`interpolate` module](interpolate) for documentation on the"] # [doc = " format of the replacement string."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use interpolation, and also shows how it"] # [doc = " can work with multi-pattern regexes."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, PatternID};"] # [doc = ""] # [doc = " let re = PikeVM::new_many(&["] # [doc = "     r\"(?<day>[0-9]{2})-(?<month>[0-9]{2})-(?<year>[0-9]{4})\","] # [doc = "     r\"(?<year>[0-9]{4})-(?<month>[0-9]{2})-(?<day>[0-9]{2})\","] # [doc = " ])?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = ""] # [doc = " let replacement = \"year=$year, month=$month, day=$day\";"] # [doc = ""] # [doc = " // This matches the first pattern."] # [doc = " let hay = \"On 14-03-2010, I became a Tennessee lamb.\";"] # [doc = " re.captures(&mut cache, hay, &mut caps);"] # [doc = " let result = caps.interpolate_string(hay, replacement);"] # [doc = " assert_eq!(\"year=2010, month=03, day=14\", result);"] # [doc = ""] # [doc = " // And this matches the second pattern."] # [doc = " let hay = \"On 2010-03-14, I became a Tennessee lamb.\";"] # [doc = " re.captures(&mut cache, hay, &mut caps);"] # [doc = " let result = caps.interpolate_string(hay, replacement);"] # [doc = " assert_eq!(\"year=2010, month=03, day=14\", result);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn interpolate_string (& self , haystack : & str , replacement : & str ,) -> String { let mut dst = String :: new () ; self . interpolate_string_into (haystack , replacement , & mut dst) ; dst } # [doc = " Interpolates the capture references in `replacement` with the"] # [doc = " corresponding substrings in `haystack` matched by each reference. The"] # [doc = " interpolated string is written to `dst`."] # [doc = ""] # [doc = " See the [`interpolate` module](interpolate) for documentation on the"] # [doc = " format of the replacement string."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use interpolation, and also shows how it"] # [doc = " can work with multi-pattern regexes."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, PatternID};"] # [doc = ""] # [doc = " let re = PikeVM::new_many(&["] # [doc = "     r\"(?<day>[0-9]{2})-(?<month>[0-9]{2})-(?<year>[0-9]{4})\","] # [doc = "     r\"(?<year>[0-9]{4})-(?<month>[0-9]{2})-(?<day>[0-9]{2})\","] # [doc = " ])?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = ""] # [doc = " let replacement = \"year=$year, month=$month, day=$day\";"] # [doc = ""] # [doc = " // This matches the first pattern."] # [doc = " let hay = \"On 14-03-2010, I became a Tennessee lamb.\";"] # [doc = " re.captures(&mut cache, hay, &mut caps);"] # [doc = " let mut dst = String::new();"] # [doc = " caps.interpolate_string_into(hay, replacement, &mut dst);"] # [doc = " assert_eq!(\"year=2010, month=03, day=14\", dst);"] # [doc = ""] # [doc = " // And this matches the second pattern."] # [doc = " let hay = \"On 2010-03-14, I became a Tennessee lamb.\";"] # [doc = " re.captures(&mut cache, hay, &mut caps);"] # [doc = " let mut dst = String::new();"] # [doc = " caps.interpolate_string_into(hay, replacement, &mut dst);"] # [doc = " assert_eq!(\"year=2010, month=03, day=14\", dst);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn interpolate_string_into (& self , haystack : & str , replacement : & str , dst : & mut String ,) { interpolate :: string (replacement , | index , dst | { let span = match self . get_group (index) { None => return , Some (span) => span , } ; dst . push_str (& haystack [span]) ; } , | name | self . group_info () . to_index (self . pattern () ? , name) , dst ,) ; } # [doc = " Interpolates the capture references in `replacement` with the"] # [doc = " corresponding substrings in `haystack` matched by each reference. The"] # [doc = " interpolated byte string is returned."] # [doc = ""] # [doc = " See the [`interpolate` module](interpolate) for documentation on the"] # [doc = " format of the replacement string."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use interpolation, and also shows how it"] # [doc = " can work with multi-pattern regexes."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, PatternID};"] # [doc = ""] # [doc = " let re = PikeVM::new_many(&["] # [doc = "     r\"(?<day>[0-9]{2})-(?<month>[0-9]{2})-(?<year>[0-9]{4})\","] # [doc = "     r\"(?<year>[0-9]{4})-(?<month>[0-9]{2})-(?<day>[0-9]{2})\","] # [doc = " ])?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = ""] # [doc = " let replacement = b\"year=$year, month=$month, day=$day\";"] # [doc = ""] # [doc = " // This matches the first pattern."] # [doc = " let hay = b\"On 14-03-2010, I became a Tennessee lamb.\";"] # [doc = " re.captures(&mut cache, hay, &mut caps);"] # [doc = " let result = caps.interpolate_bytes(hay, replacement);"] # [doc = " assert_eq!(&b\"year=2010, month=03, day=14\"[..], result);"] # [doc = ""] # [doc = " // And this matches the second pattern."] # [doc = " let hay = b\"On 2010-03-14, I became a Tennessee lamb.\";"] # [doc = " re.captures(&mut cache, hay, &mut caps);"] # [doc = " let result = caps.interpolate_bytes(hay, replacement);"] # [doc = " assert_eq!(&b\"year=2010, month=03, day=14\"[..], result);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn interpolate_bytes (& self , haystack : & [u8] , replacement : & [u8] ,) -> Vec < u8 > { let mut dst = vec ! [] ; self . interpolate_bytes_into (haystack , replacement , & mut dst) ; dst } # [doc = " Interpolates the capture references in `replacement` with the"] # [doc = " corresponding substrings in `haystack` matched by each reference. The"] # [doc = " interpolated byte string is written to `dst`."] # [doc = ""] # [doc = " See the [`interpolate` module](interpolate) for documentation on the"] # [doc = " format of the replacement string."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use interpolation, and also shows how it"] # [doc = " can work with multi-pattern regexes."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, PatternID};"] # [doc = ""] # [doc = " let re = PikeVM::new_many(&["] # [doc = "     r\"(?<day>[0-9]{2})-(?<month>[0-9]{2})-(?<year>[0-9]{4})\","] # [doc = "     r\"(?<year>[0-9]{4})-(?<month>[0-9]{2})-(?<day>[0-9]{2})\","] # [doc = " ])?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = ""] # [doc = " let replacement = b\"year=$year, month=$month, day=$day\";"] # [doc = ""] # [doc = " // This matches the first pattern."] # [doc = " let hay = b\"On 14-03-2010, I became a Tennessee lamb.\";"] # [doc = " re.captures(&mut cache, hay, &mut caps);"] # [doc = " let mut dst = vec![];"] # [doc = " caps.interpolate_bytes_into(hay, replacement, &mut dst);"] # [doc = " assert_eq!(&b\"year=2010, month=03, day=14\"[..], dst);"] # [doc = ""] # [doc = " // And this matches the second pattern."] # [doc = " let hay = b\"On 2010-03-14, I became a Tennessee lamb.\";"] # [doc = " re.captures(&mut cache, hay, &mut caps);"] # [doc = " let mut dst = vec![];"] # [doc = " caps.interpolate_bytes_into(hay, replacement, &mut dst);"] # [doc = " assert_eq!(&b\"year=2010, month=03, day=14\"[..], dst);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn interpolate_bytes_into (& self , haystack : & [u8] , replacement : & [u8] , dst : & mut Vec < u8 > ,) { interpolate :: bytes (replacement , | index , dst | { let span = match self . get_group (index) { None => return , Some (span) => span , } ; dst . extend_from_slice (& haystack [span]) ; } , | name | self . group_info () . to_index (self . pattern () ? , name) , dst ,) ; } # [doc = " This is a convenience routine for extracting the substrings"] # [doc = " corresponding to matching capture groups in the given `haystack`. The"] # [doc = " `haystack` should be the same substring used to find the match spans in"] # [doc = " this `Captures` value."] # [doc = ""] # [doc = " This is identical to [`Captures::extract_bytes`], except it works with"] # [doc = " `&str` instead of `&[u8]`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics if the number of explicit matching groups in this"] # [doc = " `Captures` value is less than `N`. This also panics if this `Captures`"] # [doc = " value does not correspond to a match."] # [doc = ""] # [doc = " Note that this does *not* panic if the number of explicit matching"] # [doc = " groups is bigger than `N`. In that case, only the first `N` matching"] # [doc = " groups are extracted."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::pikevm::PikeVM;"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"([0-9]{4})-([0-9]{2})-([0-9]{2})\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = ""] # [doc = " let hay = \"On 2010-03-14, I became a Tennessee lamb.\";"] # [doc = " re.captures(&mut cache, hay, &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " let (full, [year, month, day]) = caps.extract(hay);"] # [doc = " assert_eq!(\"2010-03-14\", full);"] # [doc = " assert_eq!(\"2010\", year);"] # [doc = " assert_eq!(\"03\", month);"] # [doc = " assert_eq!(\"14\", day);"] # [doc = ""] # [doc = " // We can also ask for fewer than all capture groups."] # [doc = " let (full, [year]) = caps.extract(hay);"] # [doc = " assert_eq!(\"2010-03-14\", full);"] # [doc = " assert_eq!(\"2010\", year);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn extract < 'h , const N : usize > (& self , haystack : & 'h str ,) -> (& 'h str , [& 'h str ; N]) { let mut matched = self . iter () . flatten () ; let whole_match = & haystack [matched . next () . expect ("a match")] ; let group_matches = [0 ; N] . map (| _ | { let sp = matched . next () . expect ("too few matching groups") ; & haystack [sp] }) ; (whole_match , group_matches) } # [doc = " This is a convenience routine for extracting the substrings"] # [doc = " corresponding to matching capture groups in the given `haystack`. The"] # [doc = " `haystack` should be the same substring used to find the match spans in"] # [doc = " this `Captures` value."] # [doc = ""] # [doc = " This is identical to [`Captures::extract`], except it works with"] # [doc = " `&[u8]` instead of `&str`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics if the number of explicit matching groups in this"] # [doc = " `Captures` value is less than `N`. This also panics if this `Captures`"] # [doc = " value does not correspond to a match."] # [doc = ""] # [doc = " Note that this does *not* panic if the number of explicit matching"] # [doc = " groups is bigger than `N`. In that case, only the first `N` matching"] # [doc = " groups are extracted."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::nfa::thompson::pikevm::PikeVM;"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"([0-9]{4})-([0-9]{2})-([0-9]{2})\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = ""] # [doc = " let hay = b\"On 2010-03-14, I became a Tennessee lamb.\";"] # [doc = " re.captures(&mut cache, hay, &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " let (full, [year, month, day]) = caps.extract_bytes(hay);"] # [doc = " assert_eq!(b\"2010-03-14\", full);"] # [doc = " assert_eq!(b\"2010\", year);"] # [doc = " assert_eq!(b\"03\", month);"] # [doc = " assert_eq!(b\"14\", day);"] # [doc = ""] # [doc = " // We can also ask for fewer than all capture groups."] # [doc = " let (full, [year]) = caps.extract_bytes(hay);"] # [doc = " assert_eq!(b\"2010-03-14\", full);"] # [doc = " assert_eq!(b\"2010\", year);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn extract_bytes < 'h , const N : usize > (& self , haystack : & 'h [u8] ,) -> (& 'h [u8] , [& 'h [u8] ; N]) { let mut matched = self . iter () . flatten () ; let whole_match = & haystack [matched . next () . expect ("a match")] ; let group_matches = [0 ; N] . map (| _ | { let sp = matched . next () . expect ("too few matching groups") ; & haystack [sp] }) ; (whole_match , group_matches) } }
    };
}

impl_609!();