macro_rules! deps {
    () => {
        BE!();
        DFA!();
        StateID!();
        LE!();
        BuildError!();
        Endian!();
        NE!();
        HalfMatch!();
        SerializeError!();
        Input!();
    };
}

macro_rules! impl_22 {
    () => {
        deps!();
        # [doc = " Routines for converting a dense DFA to other representations, such as"] # [doc = " sparse DFAs or raw bytes suitable for persistent storage."] impl < T : AsRef < [u32] > > DFA < T > { # [doc = " Convert this dense DFA to a sparse DFA."] # [doc = ""] # [doc = " If a `StateID` is too small to represent all states in the sparse"] # [doc = " DFA, then this returns an error. In most cases, if a dense DFA is"] # [doc = " constructable with `StateID` then a sparse DFA will be as well."] # [doc = " However, it is not guaranteed."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, dense}, HalfMatch, Input};"] # [doc = ""] # [doc = " let dense = dense::DFA::new(\"foo[0-9]+\")?;"] # [doc = " let sparse = dense.to_sparse()?;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, sparse.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "dfa-build")] pub fn to_sparse (& self) -> Result < sparse :: DFA < Vec < u8 > > , BuildError > { sparse :: DFA :: from_dense (self) } # [doc = " Serialize this DFA as raw bytes to a `Vec<u8>` in little endian"] # [doc = " format. Upon success, the `Vec<u8>` and the initial padding length are"] # [doc = " returned."] # [doc = ""] # [doc = " The written bytes are guaranteed to be deserialized correctly and"] # [doc = " without errors in a semver compatible release of this crate by a"] # [doc = " `DFA`'s deserialization APIs (assuming all other criteria for the"] # [doc = " deserialization APIs has been satisfied):"] # [doc = ""] # [doc = " * [`DFA::from_bytes`]"] # [doc = " * [`DFA::from_bytes_unchecked`]"] # [doc = ""] # [doc = " The padding returned is non-zero if the returned `Vec<u8>` starts at"] # [doc = " an address that does not have the same alignment as `u32`. The padding"] # [doc = " corresponds to the number of leading bytes written to the returned"] # [doc = " `Vec<u8>`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to serialize and deserialize a DFA:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, dense::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " // N.B. We use native endianness here to make the example work, but"] # [doc = " // using to_bytes_little_endian would work on a little endian target."] # [doc = " let (buf, _) = original_dfa.to_bytes_native_endian();"] # [doc = " // Even if buf has initial padding, DFA::from_bytes will automatically"] # [doc = " // ignore it."] # [doc = " let dfa: DFA<&[u32]> = DFA::from_bytes(&buf)?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "dfa-build")] pub fn to_bytes_little_endian (& self) -> (Vec < u8 > , usize) { self . to_bytes :: < wire :: LE > () } # [doc = " Serialize this DFA as raw bytes to a `Vec<u8>` in big endian"] # [doc = " format. Upon success, the `Vec<u8>` and the initial padding length are"] # [doc = " returned."] # [doc = ""] # [doc = " The written bytes are guaranteed to be deserialized correctly and"] # [doc = " without errors in a semver compatible release of this crate by a"] # [doc = " `DFA`'s deserialization APIs (assuming all other criteria for the"] # [doc = " deserialization APIs has been satisfied):"] # [doc = ""] # [doc = " * [`DFA::from_bytes`]"] # [doc = " * [`DFA::from_bytes_unchecked`]"] # [doc = ""] # [doc = " The padding returned is non-zero if the returned `Vec<u8>` starts at"] # [doc = " an address that does not have the same alignment as `u32`. The padding"] # [doc = " corresponds to the number of leading bytes written to the returned"] # [doc = " `Vec<u8>`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to serialize and deserialize a DFA:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, dense::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " // N.B. We use native endianness here to make the example work, but"] # [doc = " // using to_bytes_big_endian would work on a big endian target."] # [doc = " let (buf, _) = original_dfa.to_bytes_native_endian();"] # [doc = " // Even if buf has initial padding, DFA::from_bytes will automatically"] # [doc = " // ignore it."] # [doc = " let dfa: DFA<&[u32]> = DFA::from_bytes(&buf)?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "dfa-build")] pub fn to_bytes_big_endian (& self) -> (Vec < u8 > , usize) { self . to_bytes :: < wire :: BE > () } # [doc = " Serialize this DFA as raw bytes to a `Vec<u8>` in native endian"] # [doc = " format. Upon success, the `Vec<u8>` and the initial padding length are"] # [doc = " returned."] # [doc = ""] # [doc = " The written bytes are guaranteed to be deserialized correctly and"] # [doc = " without errors in a semver compatible release of this crate by a"] # [doc = " `DFA`'s deserialization APIs (assuming all other criteria for the"] # [doc = " deserialization APIs has been satisfied):"] # [doc = ""] # [doc = " * [`DFA::from_bytes`]"] # [doc = " * [`DFA::from_bytes_unchecked`]"] # [doc = ""] # [doc = " The padding returned is non-zero if the returned `Vec<u8>` starts at"] # [doc = " an address that does not have the same alignment as `u32`. The padding"] # [doc = " corresponds to the number of leading bytes written to the returned"] # [doc = " `Vec<u8>`."] # [doc = ""] # [doc = " Generally speaking, native endian format should only be used when"] # [doc = " you know that the target you're compiling the DFA for matches the"] # [doc = " endianness of the target on which you're compiling DFA. For example,"] # [doc = " if serialization and deserialization happen in the same process or on"] # [doc = " the same machine. Otherwise, when serializing a DFA for use in a"] # [doc = " portable environment, you'll almost certainly want to serialize _both_"] # [doc = " a little endian and a big endian version and then load the correct one"] # [doc = " based on the target's configuration."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to serialize and deserialize a DFA:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, dense::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " let (buf, _) = original_dfa.to_bytes_native_endian();"] # [doc = " // Even if buf has initial padding, DFA::from_bytes will automatically"] # [doc = " // ignore it."] # [doc = " let dfa: DFA<&[u32]> = DFA::from_bytes(&buf)?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "dfa-build")] pub fn to_bytes_native_endian (& self) -> (Vec < u8 > , usize) { self . to_bytes :: < wire :: NE > () } # [doc = " The implementation of the public `to_bytes` serialization methods,"] # [doc = " which is generic over endianness."] # [cfg (feature = "dfa-build")] fn to_bytes < E : Endian > (& self) -> (Vec < u8 > , usize) { let len = self . write_to_len () ; let (mut buf , padding) = wire :: alloc_aligned_buffer :: < u32 > (len) ; self . as_ref () . write_to :: < E > (& mut buf [padding ..]) . unwrap () ; (buf , padding) } # [doc = " Serialize this DFA as raw bytes to the given slice, in little endian"] # [doc = " format. Upon success, the total number of bytes written to `dst` is"] # [doc = " returned."] # [doc = ""] # [doc = " The written bytes are guaranteed to be deserialized correctly and"] # [doc = " without errors in a semver compatible release of this crate by a"] # [doc = " `DFA`'s deserialization APIs (assuming all other criteria for the"] # [doc = " deserialization APIs has been satisfied):"] # [doc = ""] # [doc = " * [`DFA::from_bytes`]"] # [doc = " * [`DFA::from_bytes_unchecked`]"] # [doc = ""] # [doc = " Note that unlike the various `to_byte_*` routines, this does not write"] # [doc = " any padding. Callers are responsible for handling alignment correctly."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This returns an error if the given destination slice is not big enough"] # [doc = " to contain the full serialized DFA. If an error occurs, then nothing"] # [doc = " is written to `dst`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to serialize and deserialize a DFA without"] # [doc = " dynamic memory allocation."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, dense::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " // Create a 4KB buffer on the stack to store our serialized DFA. We"] # [doc = " // need to use a special type to force the alignment of our [u8; N]"] # [doc = " // array to be aligned to a 4 byte boundary. Otherwise, deserializing"] # [doc = " // the DFA may fail because of an alignment mismatch."] # [doc = " #[repr(C)]"] # [doc = " struct Aligned<B: ?Sized> {"] # [doc = "     _align: [u32; 0],"] # [doc = "     bytes: B,"] # [doc = " }"] # [doc = " let mut buf = Aligned { _align: [], bytes: [0u8; 4 * (1<<10)] };"] # [doc = " // N.B. We use native endianness here to make the example work, but"] # [doc = " // using write_to_little_endian would work on a little endian target."] # [doc = " let written = original_dfa.write_to_native_endian(&mut buf.bytes)?;"] # [doc = " let dfa: DFA<&[u32]> = DFA::from_bytes(&buf.bytes[..written])?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn write_to_little_endian (& self , dst : & mut [u8] ,) -> Result < usize , SerializeError > { self . as_ref () . write_to :: < wire :: LE > (dst) } # [doc = " Serialize this DFA as raw bytes to the given slice, in big endian"] # [doc = " format. Upon success, the total number of bytes written to `dst` is"] # [doc = " returned."] # [doc = ""] # [doc = " The written bytes are guaranteed to be deserialized correctly and"] # [doc = " without errors in a semver compatible release of this crate by a"] # [doc = " `DFA`'s deserialization APIs (assuming all other criteria for the"] # [doc = " deserialization APIs has been satisfied):"] # [doc = ""] # [doc = " * [`DFA::from_bytes`]"] # [doc = " * [`DFA::from_bytes_unchecked`]"] # [doc = ""] # [doc = " Note that unlike the various `to_byte_*` routines, this does not write"] # [doc = " any padding. Callers are responsible for handling alignment correctly."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This returns an error if the given destination slice is not big enough"] # [doc = " to contain the full serialized DFA. If an error occurs, then nothing"] # [doc = " is written to `dst`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to serialize and deserialize a DFA without"] # [doc = " dynamic memory allocation."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, dense::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " // Create a 4KB buffer on the stack to store our serialized DFA. We"] # [doc = " // need to use a special type to force the alignment of our [u8; N]"] # [doc = " // array to be aligned to a 4 byte boundary. Otherwise, deserializing"] # [doc = " // the DFA may fail because of an alignment mismatch."] # [doc = " #[repr(C)]"] # [doc = " struct Aligned<B: ?Sized> {"] # [doc = "     _align: [u32; 0],"] # [doc = "     bytes: B,"] # [doc = " }"] # [doc = " let mut buf = Aligned { _align: [], bytes: [0u8; 4 * (1<<10)] };"] # [doc = " // N.B. We use native endianness here to make the example work, but"] # [doc = " // using write_to_big_endian would work on a big endian target."] # [doc = " let written = original_dfa.write_to_native_endian(&mut buf.bytes)?;"] # [doc = " let dfa: DFA<&[u32]> = DFA::from_bytes(&buf.bytes[..written])?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn write_to_big_endian (& self , dst : & mut [u8] ,) -> Result < usize , SerializeError > { self . as_ref () . write_to :: < wire :: BE > (dst) } # [doc = " Serialize this DFA as raw bytes to the given slice, in native endian"] # [doc = " format. Upon success, the total number of bytes written to `dst` is"] # [doc = " returned."] # [doc = ""] # [doc = " The written bytes are guaranteed to be deserialized correctly and"] # [doc = " without errors in a semver compatible release of this crate by a"] # [doc = " `DFA`'s deserialization APIs (assuming all other criteria for the"] # [doc = " deserialization APIs has been satisfied):"] # [doc = ""] # [doc = " * [`DFA::from_bytes`]"] # [doc = " * [`DFA::from_bytes_unchecked`]"] # [doc = ""] # [doc = " Generally speaking, native endian format should only be used when"] # [doc = " you know that the target you're compiling the DFA for matches the"] # [doc = " endianness of the target on which you're compiling DFA. For example,"] # [doc = " if serialization and deserialization happen in the same process or on"] # [doc = " the same machine. Otherwise, when serializing a DFA for use in a"] # [doc = " portable environment, you'll almost certainly want to serialize _both_"] # [doc = " a little endian and a big endian version and then load the correct one"] # [doc = " based on the target's configuration."] # [doc = ""] # [doc = " Note that unlike the various `to_byte_*` routines, this does not write"] # [doc = " any padding. Callers are responsible for handling alignment correctly."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This returns an error if the given destination slice is not big enough"] # [doc = " to contain the full serialized DFA. If an error occurs, then nothing"] # [doc = " is written to `dst`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to serialize and deserialize a DFA without"] # [doc = " dynamic memory allocation."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, dense::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " // Compile our original DFA."] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " // Create a 4KB buffer on the stack to store our serialized DFA. We"] # [doc = " // need to use a special type to force the alignment of our [u8; N]"] # [doc = " // array to be aligned to a 4 byte boundary. Otherwise, deserializing"] # [doc = " // the DFA may fail because of an alignment mismatch."] # [doc = " #[repr(C)]"] # [doc = " struct Aligned<B: ?Sized> {"] # [doc = "     _align: [u32; 0],"] # [doc = "     bytes: B,"] # [doc = " }"] # [doc = " let mut buf = Aligned { _align: [], bytes: [0u8; 4 * (1<<10)] };"] # [doc = " let written = original_dfa.write_to_native_endian(&mut buf.bytes)?;"] # [doc = " let dfa: DFA<&[u32]> = DFA::from_bytes(&buf.bytes[..written])?.0;"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn write_to_native_endian (& self , dst : & mut [u8] ,) -> Result < usize , SerializeError > { self . as_ref () . write_to :: < wire :: NE > (dst) } # [doc = " Return the total number of bytes required to serialize this DFA."] # [doc = ""] # [doc = " This is useful for determining the size of the buffer required to pass"] # [doc = " to one of the serialization routines:"] # [doc = ""] # [doc = " * [`DFA::write_to_little_endian`]"] # [doc = " * [`DFA::write_to_big_endian`]"] # [doc = " * [`DFA::write_to_native_endian`]"] # [doc = ""] # [doc = " Passing a buffer smaller than the size returned by this method will"] # [doc = " result in a serialization error. Serialization routines are guaranteed"] # [doc = " to succeed when the buffer is big enough."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to dynamically allocate enough room to serialize"] # [doc = " a DFA."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{dfa::{Automaton, dense::DFA}, HalfMatch, Input};"] # [doc = ""] # [doc = " let original_dfa = DFA::new(\"foo[0-9]+\")?;"] # [doc = ""] # [doc = " let mut buf = vec![0; original_dfa.write_to_len()];"] # [doc = " // This is guaranteed to succeed, because the only serialization error"] # [doc = " // that can occur is when the provided buffer is too small. But"] # [doc = " // write_to_len guarantees a correct size."] # [doc = " let written = original_dfa.write_to_native_endian(&mut buf).unwrap();"] # [doc = " // But this is not guaranteed to succeed! In particular,"] # [doc = " // deserialization requires proper alignment for &[u32], but our buffer"] # [doc = " // was allocated as a &[u8] whose required alignment is smaller than"] # [doc = " // &[u32]. However, it's likely to work in practice because of how most"] # [doc = " // allocators work. So if you write code like this, make sure to either"] # [doc = " // handle the error correctly and/or run it under Miri since Miri will"] # [doc = " // likely provoke the error by returning Vec<u8> buffers with alignment"] # [doc = " // less than &[u32]."] # [doc = " let dfa: DFA<&[u32]> = match DFA::from_bytes(&buf[..written]) {"] # [doc = "     // As mentioned above, it is legal for an error to be returned"] # [doc = "     // here. It is quite difficult to get a Vec<u8> with a guaranteed"] # [doc = "     // alignment equivalent to Vec<u32>."] # [doc = "     Err(_) => return Ok(()),"] # [doc = "     Ok((dfa, _)) => dfa,"] # [doc = " };"] # [doc = ""] # [doc = " let expected = Some(HalfMatch::must(0, 8));"] # [doc = " assert_eq!(expected, dfa.try_search_fwd(&Input::new(\"foo12345\"))?);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Note that this example isn't actually guaranteed to work! In"] # [doc = " particular, if `buf` is not aligned to a 4-byte boundary, then the"] # [doc = " `DFA::from_bytes` call will fail. If you need this to work, then you"] # [doc = " either need to deal with adding some initial padding yourself, or use"] # [doc = " one of the `to_bytes` methods, which will do it for you."] pub fn write_to_len (& self) -> usize { wire :: write_label_len (LABEL) + wire :: write_endianness_check_len () + wire :: write_version_len () + size_of :: < u32 > () + self . flags . write_to_len () + self . tt . write_to_len () + self . st . write_to_len () + self . ms . write_to_len () + self . special . write_to_len () + self . accels . write_to_len () + self . quitset . write_to_len () } }
    };
}

impl_22!()