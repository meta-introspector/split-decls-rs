macro_rules! deps {
    () => {
        FollowEpsilon!();
        Captures!();
        Match!();
        Cache!();
        PikeVM!();
        ActiveStates!();
    };
}

macro_rules! impl_548 {
    () => {
        deps!();
        impl Cache { # [doc = " Create a new [`PikeVM`] cache."] # [doc = ""] # [doc = " A potentially more convenient routine to create a cache is"] # [doc = " [`PikeVM::create_cache`], as it does not require also importing the"] # [doc = " `Cache` type."] # [doc = ""] # [doc = " If you want to reuse the returned `Cache` with some other `PikeVM`,"] # [doc = " then you must call [`Cache::reset`] with the desired `PikeVM`."] pub fn new (re : & PikeVM) -> Cache { Cache { stack : vec ! [] , curr : ActiveStates :: new (re) , next : ActiveStates :: new (re) , } } # [doc = " Reset this cache such that it can be used for searching with a"] # [doc = " different [`PikeVM`]."] # [doc = ""] # [doc = " A cache reset permits reusing memory already allocated in this cache"] # [doc = " with a different `PikeVM`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to re-purpose a cache for use with a different `PikeVM`."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{nfa::thompson::pikevm::PikeVM, Match};"] # [doc = ""] # [doc = " let re1 = PikeVM::new(r\"\\w\")?;"] # [doc = " let re2 = PikeVM::new(r\"\\W\")?;"] # [doc = ""] # [doc = " let mut cache = re1.create_cache();"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 0..2)),"] # [doc = "     re1.find_iter(&mut cache, \"Δ\").next(),"] # [doc = " );"] # [doc = ""] # [doc = " // Using 'cache' with re2 is not allowed. It may result in panics or"] # [doc = " // incorrect results. In order to re-purpose the cache, we must reset"] # [doc = " // it with the PikeVM we'd like to use it with."] # [doc = " //"] # [doc = " // Similarly, after this reset, using the cache with 're1' is also not"] # [doc = " // allowed."] # [doc = " cache.reset(&re2);"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 0..3)),"] # [doc = "     re2.find_iter(&mut cache, \"☃\").next(),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn reset (& mut self , re : & PikeVM) { self . curr . reset (re) ; self . next . reset (re) ; } # [doc = " Returns the heap memory usage, in bytes, of this cache."] # [doc = ""] # [doc = " This does **not** include the stack size used up by this cache. To"] # [doc = " compute that, use `std::mem::size_of::<Cache>()`."] pub fn memory_usage (& self) -> usize { use core :: mem :: size_of ; (self . stack . len () * size_of :: < FollowEpsilon > ()) + self . curr . memory_usage () + self . next . memory_usage () } # [doc = " Clears this cache. This should be called at the start of every search"] # [doc = " to ensure we start with a clean slate."] # [doc = ""] # [doc = " This also sets the length of the capturing groups used in the current"] # [doc = " search. This permits an optimization where by 'SlotTable::for_state'"] # [doc = " only returns the number of slots equivalent to the number of slots"] # [doc = " given in the 'Captures' value. This may be less than the total number"] # [doc = " of possible slots, e.g., when one only wants to track overall match"] # [doc = " offsets. This in turn permits less copying of capturing group spans"] # [doc = " in the PikeVM."] fn setup_search (& mut self , captures_slot_len : usize) { self . stack . clear () ; self . curr . setup_search (captures_slot_len) ; self . next . setup_search (captures_slot_len) ; } }
    };
}

impl_548!()