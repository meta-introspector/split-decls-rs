// Generated macro for hard_link (function)
macro_rules! Depcrate_fshard_link {
() => {
// Module: crate::fs
// Provides: {"hard_link"}
// Dependencies: {}
# [doc = " Creates a new hard link on the filesystem."] # [doc = ""] # [doc = " The `link` path will be a link pointing to the `original` path. Note that"] # [doc = " systems often require these two paths to both be located on the same"] # [doc = " filesystem."] # [doc = ""] # [doc = " If `original` names a symbolic link, it is platform-specific whether the"] # [doc = " symbolic link is followed. On platforms where it's possible to not follow"] # [doc = " it, it is not followed, and the created hard link points to the symbolic"] # [doc = " link itself."] # [doc = ""] # [doc = " # Platform-specific behavior"] # [doc = ""] # [doc = " This function currently corresponds the `CreateHardLink` function on Windows."] # [doc = " On most Unix systems, it corresponds to the `linkat` function with no flags."] # [doc = " On Android, VxWorks, and Redox, it instead corresponds to the `link` function."] # [doc = " On MacOS, it uses the `linkat` function if it is available, but on very old"] # [doc = " systems where `linkat` is not available, `link` is selected at runtime instead."] # [doc = " Note that, this [may change in the future][changes]."] # [doc = ""] # [doc = " [changes]: io#platform-specific-behavior"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will return an error in the following situations, but is not"] # [doc = " limited to just these cases:"] # [doc = ""] # [doc = " * The `original` path is not a file or doesn't exist."] # [doc = " * The 'link' path already exists."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::fs;"] # [doc = ""] # [doc = " fn main() -> std::io::Result<()> {"] # [doc = "     fs::hard_link(\"a.txt\", \"b.txt\")?; // Hard link a.txt to b.txt"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc (alias = "CreateHardLink" , alias = "linkat")] # [stable (feature = "rust1" , since = "1.0.0")] pub fn hard_link < P : AsRef < Path > , Q : AsRef < Path > > (original : P , link : Q) -> io :: Result < () > { fs_imp :: hard_link (original . as_ref () , link . as_ref ()) }
};
}
