// Generated macro for OsString (struct)
macro_rules! Depcrate_ffi_os_strOsString {
() => {
// Module: crate::ffi::os_str
// Provides: {"OsString"}
// Dependencies: {}
# [doc = " A type that can represent owned, mutable platform-native strings, but is"] # [doc = " cheaply inter-convertible with Rust strings."] # [doc = ""] # [doc = " The need for this type arises from the fact that:"] # [doc = ""] # [doc = " * On Unix systems, strings are often arbitrary sequences of non-zero"] # [doc = "   bytes, in many cases interpreted as UTF-8."] # [doc = ""] # [doc = " * On Windows, strings are often arbitrary sequences of non-zero 16-bit"] # [doc = "   values, interpreted as UTF-16 when it is valid to do so."] # [doc = ""] # [doc = " * In Rust, strings are always valid UTF-8, which may contain zeros."] # [doc = ""] # [doc = " `OsString` and [`OsStr`] bridge this gap by simultaneously representing Rust"] # [doc = " and platform-native string values, and in particular allowing a Rust string"] # [doc = " to be converted into an \"OS\" string with no cost if possible. A consequence"] # [doc = " of this is that `OsString` instances are *not* `NUL` terminated; in order"] # [doc = " to pass to e.g., Unix system call, you should create a [`CStr`]."] # [doc = ""] # [doc = " `OsString` is to <code>&[OsStr]</code> as [`String`] is to <code>&[str]</code>: the former"] # [doc = " in each pair are owned strings; the latter are borrowed"] # [doc = " references."] # [doc = ""] # [doc = " Note, `OsString` and [`OsStr`] internally do not necessarily hold strings in"] # [doc = " the form native to the platform; While on Unix, strings are stored as a"] # [doc = " sequence of 8-bit values, on Windows, where strings are 16-bit value based"] # [doc = " as just discussed, strings are also actually stored as a sequence of 8-bit"] # [doc = " values, encoded in a less-strict variant of UTF-8. This is useful to"] # [doc = " understand when handling capacity and length values."] # [doc = ""] # [doc = " # Capacity of `OsString`"] # [doc = ""] # [doc = " Capacity uses units of UTF-8 bytes for OS strings which were created from valid unicode, and"] # [doc = " uses units of bytes in an unspecified encoding for other contents. On a given target, all"] # [doc = " `OsString` and `OsStr` values use the same units for capacity, so the following will work:"] # [doc = " ```"] # [doc = " use std::ffi::{OsStr, OsString};"] # [doc = ""] # [doc = " fn concat_os_strings(a: &OsStr, b: &OsStr) -> OsString {"] # [doc = "     let mut ret = OsString::with_capacity(a.len() + b.len()); // This will allocate"] # [doc = "     ret.push(a); // This will not allocate further"] # [doc = "     ret.push(b); // This will not allocate further"] # [doc = "     ret"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " # Creating an `OsString`"] # [doc = ""] # [doc = " **From a Rust string**: `OsString` implements"] # [doc = " <code>[From]<[String]></code>, so you can use <code>my_string.[into]\\()</code> to"] # [doc = " create an `OsString` from a normal Rust string."] # [doc = ""] # [doc = " **From slices:** Just like you can start with an empty Rust"] # [doc = " [`String`] and then [`String::push_str`] some <code>&[str]</code>"] # [doc = " sub-string slices into it, you can create an empty `OsString` with"] # [doc = " the [`OsString::new`] method and then push string slices into it with the"] # [doc = " [`OsString::push`] method."] # [doc = ""] # [doc = " # Extracting a borrowed reference to the whole OS string"] # [doc = ""] # [doc = " You can use the [`OsString::as_os_str`] method to get an <code>&[OsStr]</code> from"] # [doc = " an `OsString`; this is effectively a borrowed reference to the"] # [doc = " whole string."] # [doc = ""] # [doc = " # Conversions"] # [doc = ""] # [doc = " See the [module's toplevel documentation about conversions][conversions] for a discussion on"] # [doc = " the traits which `OsString` implements for [conversions] from/to native representations."] # [doc = ""] # [doc = " [`CStr`]: crate::ffi::CStr"] # [doc = " [conversions]: super#conversions"] # [doc = " [into]: Into::into"] # [cfg_attr (not (test) , rustc_diagnostic_item = "OsString")] # [stable (feature = "rust1" , since = "1.0.0")] pub struct OsString { inner : Buf , }
};
}
