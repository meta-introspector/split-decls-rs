// Generated macro for main_thread (module)
macro_rules! Depcrate_threadmain_thread {
() => {
// Module: crate::thread
// Provides: {"main_thread"}
// Dependencies: {}
# [doc = " Store the ID of the main thread."] # [doc = ""] # [doc = " The thread handle for the main thread is created lazily, and this might even"] # [doc = " happen pre-main. Since not every platform has a way to identify the main"] # [doc = " thread when that happens – macOS's `pthread_main_np` function being a notable"] # [doc = " exception – we cannot assign it the right name right then. Instead, in our"] # [doc = " runtime startup code, we remember the thread ID of the main thread (through"] # [doc = " this modules `set` function) and use it to identify the main thread from then"] # [doc = " on. This works reliably and has the additional advantage that we can report"] # [doc = " the right thread name on main even after the thread handle has been destroyed."] # [doc = " Note however that this also means that the name reported in pre-main functions"] # [doc = " will be incorrect, but that's just something we have to live with."] pub (crate) mod main_thread { cfg_select ! { target_has_atomic = "64" => { use super :: ThreadId ; use crate :: sync :: atomic :: { Atomic , AtomicU64 } ; use crate :: sync :: atomic :: Ordering :: Relaxed ; static MAIN : Atomic < u64 > = AtomicU64 :: new (0) ; pub (super) fn get () -> Option < ThreadId > { ThreadId :: from_u64 (MAIN . load (Relaxed)) } # [doc = " # Safety"] # [doc = " May only be called once."] pub (crate) unsafe fn set (id : ThreadId) { MAIN . store (id . as_u64 () . get () , Relaxed) } } _ => { use super :: ThreadId ; use crate :: mem :: MaybeUninit ; use crate :: sync :: atomic :: { Atomic , AtomicBool } ; use crate :: sync :: atomic :: Ordering :: { Acquire , Release } ; static INIT : Atomic < bool > = AtomicBool :: new (false) ; static mut MAIN : MaybeUninit < ThreadId > = MaybeUninit :: uninit () ; pub (super) fn get () -> Option < ThreadId > { if INIT . load (Acquire) { Some (unsafe { MAIN . assume_init () }) } else { None } } # [doc = " # Safety"] # [doc = " May only be called once."] pub (crate) unsafe fn set (id : ThreadId) { unsafe { MAIN = MaybeUninit :: new (id) } ; INIT . store (true , Release) ; } } } }
};
}
