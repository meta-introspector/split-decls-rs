// Generated macro for JoinHandle (struct)
macro_rules! Depcrate_threadJoinHandle {
() => {
// Module: crate::thread
// Provides: {"JoinHandle"}
// Dependencies: {}
# [doc = " An owned permission to join on a thread (block on its termination)."] # [doc = ""] # [doc = " A `JoinHandle` *detaches* the associated thread when it is dropped, which"] # [doc = " means that there is no longer any handle to the thread and no way to `join`"] # [doc = " on it."] # [doc = ""] # [doc = " Due to platform restrictions, it is not possible to [`Clone`] this"] # [doc = " handle: the ability to join a thread is a uniquely-owned permission."] # [doc = ""] # [doc = " This `struct` is created by the [`thread::spawn`] function and the"] # [doc = " [`thread::Builder::spawn`] method."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Creation from [`thread::spawn`]:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let join_handle: thread::JoinHandle<_> = thread::spawn(|| {"] # [doc = "     // some work here"] # [doc = " });"] # [doc = " ```"] # [doc = ""] # [doc = " Creation from [`thread::Builder::spawn`]:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let builder = thread::Builder::new();"] # [doc = ""] # [doc = " let join_handle: thread::JoinHandle<_> = builder.spawn(|| {"] # [doc = "     // some work here"] # [doc = " }).unwrap();"] # [doc = " ```"] # [doc = ""] # [doc = " A thread being detached and outliving the thread that spawned it:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " let original_thread = thread::spawn(|| {"] # [doc = "     let _detached_thread = thread::spawn(|| {"] # [doc = "         // Here we sleep to make sure that the first thread returns before."] # [doc = "         thread::sleep(Duration::from_millis(10));"] # [doc = "         // This will be called, even though the JoinHandle is dropped."] # [doc = "         println!(\"♫ Still alive ♫\");"] # [doc = "     });"] # [doc = " });"] # [doc = ""] # [doc = " original_thread.join().expect(\"The thread being joined has panicked\");"] # [doc = " println!(\"Original thread is joined.\");"] # [doc = ""] # [doc = " // We make sure that the new thread has time to run, before the main"] # [doc = " // thread returns."] # [doc = ""] # [doc = " thread::sleep(Duration::from_millis(1000));"] # [doc = " ```"] # [doc = ""] # [doc = " [`thread::Builder::spawn`]: Builder::spawn"] # [doc = " [`thread::spawn`]: spawn"] # [stable (feature = "rust1" , since = "1.0.0")] # [cfg_attr (target_os = "teeos" , must_use)] pub struct JoinHandle < T > (JoinInner < 'static , T >) ;
};
}
