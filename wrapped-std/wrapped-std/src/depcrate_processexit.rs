// Generated macro for exit (function)
macro_rules! Depcrate_processexit {
() => {
// Module: crate::process
// Provides: {"exit"}
// Dependencies: {}
# [doc = " Terminates the current process with the specified exit code."] # [doc = ""] # [doc = " This function will never return and will immediately terminate the current"] # [doc = " process. The exit code is passed through to the underlying OS and will be"] # [doc = " available for consumption by another process."] # [doc = ""] # [doc = " Note that because this function never returns, and that it terminates the"] # [doc = " process, no destructors on the current stack or any other thread's stack"] # [doc = " will be run. If a clean shutdown is needed it is recommended to only call"] # [doc = " this function at a known point where there are no more destructors left"] # [doc = " to run; or, preferably, simply return a type implementing [`Termination`]"] # [doc = " (such as [`ExitCode`] or `Result`) from the `main` function and avoid this"] # [doc = " function altogether:"] # [doc = ""] # [doc = " ```"] # [doc = " # use std::io::Error as MyError;"] # [doc = " fn main() -> Result<(), MyError> {"] # [doc = "     // ..."] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " In its current implementation, this function will execute exit handlers registered with `atexit`"] # [doc = " as well as other platform-specific exit handlers (e.g. `fini` sections of ELF shared objects)."] # [doc = " This means that Rust requires that all exit handlers are safe to execute at any time. In"] # [doc = " particular, if an exit handler cleans up some state that might be concurrently accessed by other"] # [doc = " threads, it is required that the exit handler performs suitable synchronization with those"] # [doc = " threads. (The alternative to this requirement would be to not run exit handlers at all, which is"] # [doc = " considered undesirable. Note that returning from `main` also calls `exit`, so making `exit` an"] # [doc = " unsafe operation is not an option.)"] # [doc = ""] # [doc = " ## Platform-specific behavior"] # [doc = ""] # [doc = " **Unix**: On Unix-like platforms, it is unlikely that all 32 bits of `exit`"] # [doc = " will be visible to a parent process inspecting the exit code. On most"] # [doc = " Unix-like platforms, only the eight least-significant bits are considered."] # [doc = ""] # [doc = " For example, the exit code for this example will be `0` on Linux, but `256`"] # [doc = " on Windows:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::process;"] # [doc = ""] # [doc = " process::exit(0x0100);"] # [doc = " ```"] # [doc = ""] # [doc = " ### Safe interop with C code"] # [doc = ""] # [doc = " On Unix, this function is currently implemented using the `exit` C function [`exit`][C-exit]. As"] # [doc = " of C23, the C standard does not permit multiple threads to call `exit` concurrently. Rust"] # [doc = " mitigates this with a lock, but if C code calls `exit`, that can still cause undefined behavior."] # [doc = " Note that returning from `main` is equivalent to calling `exit`."] # [doc = ""] # [doc = " Therefore, it is undefined behavior to have two concurrent threads perform the following"] # [doc = " without synchronization:"] # [doc = " - One thread calls Rust's `exit` function or returns from Rust's `main` function"] # [doc = " - Another thread calls the C function `exit` or `quick_exit`, or returns from C's `main` function"] # [doc = ""] # [doc = " Note that if a binary contains multiple copies of the Rust runtime (e.g., when combining"] # [doc = " multiple `cdylib` or `staticlib`), they each have their own separate lock, so from the"] # [doc = " perspective of code running in one of the Rust runtimes, the \"outside\" Rust code is basically C"] # [doc = " code, and concurrent `exit` again causes undefined behavior."] # [doc = ""] # [doc = " Individual C implementations might provide more guarantees than the standard and permit concurrent"] # [doc = " calls to `exit`; consult the documentation of your C implementation for details."] # [doc = ""] # [doc = " For some of the on-going discussion to make `exit` thread-safe in C, see:"] # [doc = " - [Rust issue #126600](https://github.com/rust-lang/rust/issues/126600)"] # [doc = " - [Austin Group Bugzilla (for POSIX)](https://austingroupbugs.net/view.php?id=1845)"] # [doc = " - [GNU C library Bugzilla](https://sourceware.org/bugzilla/show_bug.cgi?id=31997)"] # [doc = ""] # [doc = " [C-exit]: https://en.cppreference.com/w/c/program/exit"] # [stable (feature = "rust1" , since = "1.0.0")] # [cfg_attr (not (test) , rustc_diagnostic_item = "process_exit")] pub fn exit (code : i32) -> ! { crate :: rt :: cleanup () ; crate :: sys :: os :: exit (code) }
};
}
