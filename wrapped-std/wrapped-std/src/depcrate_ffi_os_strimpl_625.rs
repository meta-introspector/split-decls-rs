// Generated macro for impl_625 (impl)
macro_rules! Depcrate_ffi_os_strimpl_625 {
() => {
// Module: crate::ffi::os_str
// Provides: {"impl_625"}
// Dependencies: {}
impl OsString { # [doc = " Constructs a new empty `OsString`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsString;"] # [doc = ""] # [doc = " let os_string = OsString::new();"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [must_use] # [inline] # [rustc_const_stable (feature = "const_pathbuf_osstring_new" , since = "1.91.0")] pub const fn new () -> OsString { OsString { inner : Buf :: from_string (String :: new ()) } } # [doc = " Converts bytes to an `OsString` without checking that the bytes contains"] # [doc = " valid [`OsStr`]-encoded data."] # [doc = ""] # [doc = " The byte encoding is an unspecified, platform-specific, self-synchronizing superset of UTF-8."] # [doc = " By being a self-synchronizing superset of UTF-8, this encoding is also a superset of 7-bit"] # [doc = " ASCII."] # [doc = ""] # [doc = " See the [module's toplevel documentation about conversions][conversions] for safe,"] # [doc = " cross-platform [conversions] from/to native representations."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " As the encoding is unspecified, callers must pass in bytes that originated as a mixture of"] # [doc = " validated UTF-8 and bytes from [`OsStr::as_encoded_bytes`] from within the same Rust version"] # [doc = " built for the same target platform.  For example, reconstructing an `OsString` from bytes sent"] # [doc = " over the network or stored in a file will likely violate these safety rules."] # [doc = ""] # [doc = " Due to the encoding being self-synchronizing, the bytes from [`OsStr::as_encoded_bytes`] can be"] # [doc = " split either immediately before or immediately after any valid non-empty UTF-8 substring."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsStr;"] # [doc = ""] # [doc = " let os_str = OsStr::new(\"Mary had a little lamb\");"] # [doc = " let bytes = os_str.as_encoded_bytes();"] # [doc = " let words = bytes.split(|b| *b == b' ');"] # [doc = " let words: Vec<&OsStr> = words.map(|word| {"] # [doc = "     // SAFETY:"] # [doc = "     // - Each `word` only contains content that originated from `OsStr::as_encoded_bytes`"] # [doc = "     // - Only split with ASCII whitespace which is a non-empty UTF-8 substring"] # [doc = "     unsafe { OsStr::from_encoded_bytes_unchecked(word) }"] # [doc = " }).collect();"] # [doc = " ```"] # [doc = ""] # [doc = " [conversions]: super#conversions"] # [inline] # [stable (feature = "os_str_bytes" , since = "1.74.0")] pub unsafe fn from_encoded_bytes_unchecked (bytes : Vec < u8 >) -> Self { OsString { inner : unsafe { Buf :: from_encoded_bytes_unchecked (bytes) } } } # [doc = " Converts to an [`OsStr`] slice."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::{OsString, OsStr};"] # [doc = ""] # [doc = " let os_string = OsString::from(\"foo\");"] # [doc = " let os_str = OsStr::new(\"foo\");"] # [doc = " assert_eq!(os_string.as_os_str(), os_str);"] # [doc = " ```"] # [cfg_attr (not (test) , rustc_diagnostic_item = "os_string_as_os_str")] # [stable (feature = "rust1" , since = "1.0.0")] # [must_use] # [inline] pub fn as_os_str (& self) -> & OsStr { self } # [doc = " Converts the `OsString` into a byte vector.  To convert the byte vector back into an"] # [doc = " `OsString`, use the [`OsString::from_encoded_bytes_unchecked`] function."] # [doc = ""] # [doc = " The byte encoding is an unspecified, platform-specific, self-synchronizing superset of UTF-8."] # [doc = " By being a self-synchronizing superset of UTF-8, this encoding is also a superset of 7-bit"] # [doc = " ASCII."] # [doc = ""] # [doc = " Note: As the encoding is unspecified, any sub-slice of bytes that is not valid UTF-8 should"] # [doc = " be treated as opaque and only comparable within the same Rust version built for the same"] # [doc = " target platform.  For example, sending the bytes over the network or storing it in a file"] # [doc = " will likely result in incompatible data.  See [`OsString`] for more encoding details"] # [doc = " and [`std::ffi`] for platform-specific, specified conversions."] # [doc = ""] # [doc = " [`std::ffi`]: crate::ffi"] # [inline] # [stable (feature = "os_str_bytes" , since = "1.74.0")] pub fn into_encoded_bytes (self) -> Vec < u8 > { self . inner . into_encoded_bytes () } # [doc = " Converts the `OsString` into a [`String`] if it contains valid Unicode data."] # [doc = ""] # [doc = " On failure, ownership of the original `OsString` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsString;"] # [doc = ""] # [doc = " let os_string = OsString::from(\"foo\");"] # [doc = " let string = os_string.into_string();"] # [doc = " assert_eq!(string, Ok(String::from(\"foo\")));"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn into_string (self) -> Result < String , OsString > { self . inner . into_string () . map_err (| buf | OsString { inner : buf }) } # [doc = " Extends the string with the given <code>&[OsStr]</code> slice."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsString;"] # [doc = ""] # [doc = " let mut os_string = OsString::from(\"foo\");"] # [doc = " os_string.push(\"bar\");"] # [doc = " assert_eq!(&os_string, \"foobar\");"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] # [rustc_confusables ("append" , "put")] pub fn push < T : AsRef < OsStr > > (& mut self , s : T) { trait SpecPushTo { fn spec_push_to (& self , buf : & mut OsString) ; } impl < T : AsRef < OsStr > > SpecPushTo for T { # [inline] default fn spec_push_to (& self , buf : & mut OsString) { buf . inner . push_slice (& self . as_ref () . inner) ; } } macro spec_str ($ T : ty) { impl SpecPushTo for $ T { # [inline] fn spec_push_to (& self , buf : & mut OsString) { buf . inner . push_str (self) ; } } } spec_str ! (str) ; spec_str ! (String) ; s . spec_push_to (self) } # [doc = " Creates a new `OsString` with at least the given capacity."] # [doc = ""] # [doc = " The string will be able to hold at least `capacity` length units of other"] # [doc = " OS strings without reallocating. This method is allowed to allocate for"] # [doc = " more units than `capacity`. If `capacity` is 0, the string will not"] # [doc = " allocate."] # [doc = ""] # [doc = " See the main `OsString` documentation information about encoding and capacity units."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsString;"] # [doc = ""] # [doc = " let mut os_string = OsString::with_capacity(10);"] # [doc = " let capacity = os_string.capacity();"] # [doc = ""] # [doc = " // This push is done without reallocating"] # [doc = " os_string.push(\"foo\");"] # [doc = ""] # [doc = " assert_eq!(capacity, os_string.capacity());"] # [doc = " ```"] # [stable (feature = "osstring_simple_functions" , since = "1.9.0")] # [must_use] # [inline] pub fn with_capacity (capacity : usize) -> OsString { OsString { inner : Buf :: with_capacity (capacity) } } # [doc = " Truncates the `OsString` to zero length."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsString;"] # [doc = ""] # [doc = " let mut os_string = OsString::from(\"foo\");"] # [doc = " assert_eq!(&os_string, \"foo\");"] # [doc = ""] # [doc = " os_string.clear();"] # [doc = " assert_eq!(&os_string, \"\");"] # [doc = " ```"] # [stable (feature = "osstring_simple_functions" , since = "1.9.0")] # [inline] pub fn clear (& mut self) { self . inner . clear () } # [doc = " Returns the capacity this `OsString` can hold without reallocating."] # [doc = ""] # [doc = " See the main `OsString` documentation information about encoding and capacity units."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsString;"] # [doc = ""] # [doc = " let os_string = OsString::with_capacity(10);"] # [doc = " assert!(os_string.capacity() >= 10);"] # [doc = " ```"] # [stable (feature = "osstring_simple_functions" , since = "1.9.0")] # [must_use] # [inline] pub fn capacity (& self) -> usize { self . inner . capacity () } # [doc = " Reserves capacity for at least `additional` more capacity to be inserted"] # [doc = " in the given `OsString`. Does nothing if the capacity is"] # [doc = " already sufficient."] # [doc = ""] # [doc = " The collection may reserve more space to speculatively avoid frequent reallocations."] # [doc = ""] # [doc = " See the main `OsString` documentation information about encoding and capacity units."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsString;"] # [doc = ""] # [doc = " let mut s = OsString::new();"] # [doc = " s.reserve(10);"] # [doc = " assert!(s.capacity() >= 10);"] # [doc = " ```"] # [stable (feature = "osstring_simple_functions" , since = "1.9.0")] # [inline] pub fn reserve (& mut self , additional : usize) { self . inner . reserve (additional) } # [doc = " Tries to reserve capacity for at least `additional` more length units"] # [doc = " in the given `OsString`. The string may reserve more space to speculatively avoid"] # [doc = " frequent reallocations. After calling `try_reserve`, capacity will be"] # [doc = " greater than or equal to `self.len() + additional` if it returns `Ok(())`."] # [doc = " Does nothing if capacity is already sufficient. This method preserves"] # [doc = " the contents even if an error occurs."] # [doc = ""] # [doc = " See the main `OsString` documentation information about encoding and capacity units."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the capacity overflows, or the allocator reports a failure, then an error"] # [doc = " is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::{OsStr, OsString};"] # [doc = " use std::collections::TryReserveError;"] # [doc = ""] # [doc = " fn process_data(data: &str) -> Result<OsString, TryReserveError> {"] # [doc = "     let mut s = OsString::new();"] # [doc = ""] # [doc = "     // Pre-reserve the memory, exiting if we can't"] # [doc = "     s.try_reserve(OsStr::new(data).len())?;"] # [doc = ""] # [doc = "     // Now we know this can't OOM in the middle of our complex work"] # [doc = "     s.push(data);"] # [doc = ""] # [doc = "     Ok(s)"] # [doc = " }"] # [doc = " # process_data(\"123\").expect(\"why is the test harness OOMing on 3 bytes?\");"] # [doc = " ```"] # [stable (feature = "try_reserve_2" , since = "1.63.0")] # [inline] pub fn try_reserve (& mut self , additional : usize) -> Result < () , TryReserveError > { self . inner . try_reserve (additional) } # [doc = " Reserves the minimum capacity for at least `additional` more capacity to"] # [doc = " be inserted in the given `OsString`. Does nothing if the capacity is"] # [doc = " already sufficient."] # [doc = ""] # [doc = " Note that the allocator may give the collection more space than it"] # [doc = " requests. Therefore, capacity can not be relied upon to be precisely"] # [doc = " minimal. Prefer [`reserve`] if future insertions are expected."] # [doc = ""] # [doc = " [`reserve`]: OsString::reserve"] # [doc = ""] # [doc = " See the main `OsString` documentation information about encoding and capacity units."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsString;"] # [doc = ""] # [doc = " let mut s = OsString::new();"] # [doc = " s.reserve_exact(10);"] # [doc = " assert!(s.capacity() >= 10);"] # [doc = " ```"] # [stable (feature = "osstring_simple_functions" , since = "1.9.0")] # [inline] pub fn reserve_exact (& mut self , additional : usize) { self . inner . reserve_exact (additional) } # [doc = " Tries to reserve the minimum capacity for at least `additional`"] # [doc = " more length units in the given `OsString`. After calling"] # [doc = " `try_reserve_exact`, capacity will be greater than or equal to"] # [doc = " `self.len() + additional` if it returns `Ok(())`."] # [doc = " Does nothing if the capacity is already sufficient."] # [doc = ""] # [doc = " Note that the allocator may give the `OsString` more space than it"] # [doc = " requests. Therefore, capacity can not be relied upon to be precisely"] # [doc = " minimal. Prefer [`try_reserve`] if future insertions are expected."] # [doc = ""] # [doc = " [`try_reserve`]: OsString::try_reserve"] # [doc = ""] # [doc = " See the main `OsString` documentation information about encoding and capacity units."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the capacity overflows, or the allocator reports a failure, then an error"] # [doc = " is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::{OsStr, OsString};"] # [doc = " use std::collections::TryReserveError;"] # [doc = ""] # [doc = " fn process_data(data: &str) -> Result<OsString, TryReserveError> {"] # [doc = "     let mut s = OsString::new();"] # [doc = ""] # [doc = "     // Pre-reserve the memory, exiting if we can't"] # [doc = "     s.try_reserve_exact(OsStr::new(data).len())?;"] # [doc = ""] # [doc = "     // Now we know this can't OOM in the middle of our complex work"] # [doc = "     s.push(data);"] # [doc = ""] # [doc = "     Ok(s)"] # [doc = " }"] # [doc = " # process_data(\"123\").expect(\"why is the test harness OOMing on 3 bytes?\");"] # [doc = " ```"] # [stable (feature = "try_reserve_2" , since = "1.63.0")] # [inline] pub fn try_reserve_exact (& mut self , additional : usize) -> Result < () , TryReserveError > { self . inner . try_reserve_exact (additional) } # [doc = " Shrinks the capacity of the `OsString` to match its length."] # [doc = ""] # [doc = " See the main `OsString` documentation information about encoding and capacity units."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsString;"] # [doc = ""] # [doc = " let mut s = OsString::from(\"foo\");"] # [doc = ""] # [doc = " s.reserve(100);"] # [doc = " assert!(s.capacity() >= 100);"] # [doc = ""] # [doc = " s.shrink_to_fit();"] # [doc = " assert_eq!(3, s.capacity());"] # [doc = " ```"] # [stable (feature = "osstring_shrink_to_fit" , since = "1.19.0")] # [inline] pub fn shrink_to_fit (& mut self) { self . inner . shrink_to_fit () } # [doc = " Shrinks the capacity of the `OsString` with a lower bound."] # [doc = ""] # [doc = " The capacity will remain at least as large as both the length"] # [doc = " and the supplied value."] # [doc = ""] # [doc = " If the current capacity is less than the lower limit, this is a no-op."] # [doc = ""] # [doc = " See the main `OsString` documentation information about encoding and capacity units."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsString;"] # [doc = ""] # [doc = " let mut s = OsString::from(\"foo\");"] # [doc = ""] # [doc = " s.reserve(100);"] # [doc = " assert!(s.capacity() >= 100);"] # [doc = ""] # [doc = " s.shrink_to(10);"] # [doc = " assert!(s.capacity() >= 10);"] # [doc = " s.shrink_to(0);"] # [doc = " assert!(s.capacity() >= 3);"] # [doc = " ```"] # [inline] # [stable (feature = "shrink_to" , since = "1.56.0")] pub fn shrink_to (& mut self , min_capacity : usize) { self . inner . shrink_to (min_capacity) } # [doc = " Converts this `OsString` into a boxed [`OsStr`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::{OsString, OsStr};"] # [doc = ""] # [doc = " let s = OsString::from(\"hello\");"] # [doc = ""] # [doc = " let b: Box<OsStr> = s.into_boxed_os_str();"] # [doc = " ```"] # [must_use = "`self` will be dropped if the result is not used"] # [stable (feature = "into_boxed_os_str" , since = "1.20.0")] pub fn into_boxed_os_str (self) -> Box < OsStr > { let rw = Box :: into_raw (self . inner . into_box ()) as * mut OsStr ; unsafe { Box :: from_raw (rw) } } # [doc = " Consumes and leaks the `OsString`, returning a mutable reference to the contents,"] # [doc = " `&'a mut OsStr`."] # [doc = ""] # [doc = " The caller has free choice over the returned lifetime, including 'static."] # [doc = " Indeed, this function is ideally used for data that lives for the remainder of"] # [doc = " the program’s life, as dropping the returned reference will cause a memory leak."] # [doc = ""] # [doc = " It does not reallocate or shrink the `OsString`, so the leaked allocation may include"] # [doc = " unused capacity that is not part of the returned slice. If you want to discard excess"] # [doc = " capacity, call [`into_boxed_os_str`], and then [`Box::leak`] instead."] # [doc = " However, keep in mind that trimming the capacity may result in a reallocation and copy."] # [doc = ""] # [doc = " [`into_boxed_os_str`]: Self::into_boxed_os_str"] # [stable (feature = "os_string_pathbuf_leak" , since = "1.89.0")] # [inline] pub fn leak < 'a > (self) -> & 'a mut OsStr { OsStr :: from_inner_mut (self . inner . leak ()) } # [doc = " Truncate the `OsString` to the specified length."] # [doc = ""] # [doc = " # Panics"] # [doc = " Panics if `len` does not lie on a valid `OsStr` boundary"] # [doc = " (as described in [`OsStr::slice_encoded_bytes`])."] # [inline] # [unstable (feature = "os_string_truncate" , issue = "133262")] pub fn truncate (& mut self , len : usize) { self . as_os_str () . inner . check_public_boundary (len) ; unsafe { self . inner . truncate_unchecked (len) } ; } # [doc = " Provides plumbing to `Vec::extend_from_slice` without giving full"] # [doc = " mutable access to the `Vec`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The slice must be valid for the platform encoding (as described in"] # [doc = " [`OsStr::from_encoded_bytes_unchecked`])."] # [doc = ""] # [doc = " This bypasses the encoding-dependent surrogate joining, so either"] # [doc = " `self` must not end with a leading surrogate half, or `other` must not"] # [doc = " start with a trailing surrogate half."] # [inline] pub (crate) unsafe fn extend_from_slice_unchecked (& mut self , other : & [u8]) { unsafe { self . inner . extend_from_slice_unchecked (other) } ; } }
};
}
