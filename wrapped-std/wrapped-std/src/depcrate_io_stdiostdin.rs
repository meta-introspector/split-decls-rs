// Generated macro for Stdin (struct)
macro_rules! Depcrate_io_stdioStdin {
() => {
// Module: crate::io::stdio
// Provides: {"Stdin"}
// Dependencies: {}
# [doc = " A handle to the standard input stream of a process."] # [doc = ""] # [doc = " Each handle is a shared reference to a global buffer of input data to this"] # [doc = " process. A handle can be `lock`'d to gain full access to [`BufRead`] methods"] # [doc = " (e.g., `.lines()`). Reads to this handle are otherwise locked with respect"] # [doc = " to other reads."] # [doc = ""] # [doc = " This handle implements the `Read` trait, but beware that concurrent reads"] # [doc = " of `Stdin` must be executed with care."] # [doc = ""] # [doc = " Created by the [`io::stdin`] method."] # [doc = ""] # [doc = " [`io::stdin`]: stdin"] # [doc = ""] # [doc = " ### Note: Windows Portability Considerations"] # [doc = ""] # [doc = " When operating in a console, the Windows implementation of this stream does not support"] # [doc = " non-UTF-8 byte sequences. Attempting to read bytes that are not valid UTF-8 will return"] # [doc = " an error."] # [doc = ""] # [doc = " In a process with a detached console, such as one using"] # [doc = " `#![windows_subsystem = \"windows\"]`, or in a child process spawned from such a process,"] # [doc = " the contained handle will be null. In such cases, the standard library's `Read` and"] # [doc = " `Write` will do nothing and silently succeed. All other I/O operations, via the"] # [doc = " standard library or via raw Windows API calls, will fail."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io;"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut buffer = String::new();"] # [doc = "     let stdin = io::stdin(); // We get `Stdin` here."] # [doc = "     stdin.read_line(&mut buffer)?;"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [cfg_attr (not (test) , rustc_diagnostic_item = "Stdin")] pub struct Stdin { inner : & 'static Mutex < BufReader < StdinRaw > > , }
};
}
