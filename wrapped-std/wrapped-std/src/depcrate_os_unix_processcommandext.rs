// Generated macro for CommandExt (trait)
macro_rules! Depcrate_os_unix_processCommandExt {
() => {
// Module: crate::os::unix::process
// Provides: {"CommandExt"}
// Dependencies: {}
# [doc = " Unix-specific extensions to the [`process::Command`] builder."] # [doc = ""] # [doc = " This trait is sealed: it cannot be implemented outside the standard library."] # [doc = " This is so that future additional methods are not breaking changes."] # [stable (feature = "rust1" , since = "1.0.0")] pub trait CommandExt : Sealed { # [doc = " Sets the child process's user ID. This translates to a"] # [doc = " `setuid` call in the child process. Failure in the `setuid`"] # [doc = " call will cause the spawn to fail."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " This will also trigger a call to `setgroups(0, NULL)` in the child"] # [doc = " process if no groups have been specified."] # [doc = " This removes supplementary groups that might have given the child"] # [doc = " unwanted permissions."] # [stable (feature = "rust1" , since = "1.0.0")] fn uid (& mut self , id : UserId) -> & mut process :: Command ; # [doc = " Similar to `uid`, but sets the group ID of the child process. This has"] # [doc = " the same semantics as the `uid` field."] # [stable (feature = "rust1" , since = "1.0.0")] fn gid (& mut self , id : GroupId) -> & mut process :: Command ; # [doc = " Sets the supplementary group IDs for the calling process. Translates to"] # [doc = " a `setgroups` call in the child process."] # [unstable (feature = "setgroups" , issue = "90747")] fn groups (& mut self , groups : & [GroupId]) -> & mut process :: Command ; # [doc = " Schedules a closure to be run just before the `exec` function is"] # [doc = " invoked."] # [doc = ""] # [doc = " The closure is allowed to return an I/O error whose OS error code will"] # [doc = " be communicated back to the parent and returned as an error from when"] # [doc = " the spawn was requested."] # [doc = ""] # [doc = " Multiple closures can be registered and they will be called in order of"] # [doc = " their registration. If a closure returns `Err` then no further closures"] # [doc = " will be called and the spawn operation will immediately return with a"] # [doc = " failure."] # [doc = ""] # [doc = " # Notes and Safety"] # [doc = ""] # [doc = " This closure will be run in the context of the child process after a"] # [doc = " `fork`. This primarily means that any modifications made to memory on"] # [doc = " behalf of this closure will **not** be visible to the parent process."] # [doc = " This is often a very constrained environment where normal operations"] # [doc = " like `malloc`, accessing environment variables through [`std::env`]"] # [doc = " or acquiring a mutex are not guaranteed to work (due to"] # [doc = " other threads perhaps still running when the `fork` was run)."] # [doc = ""] # [doc = " For further details refer to the [POSIX fork() specification]"] # [doc = " and the equivalent documentation for any targeted"] # [doc = " platform, especially the requirements around *async-signal-safety*."] # [doc = ""] # [doc = " This also means that all resources such as file descriptors and"] # [doc = " memory-mapped regions got duplicated. It is your responsibility to make"] # [doc = " sure that the closure does not violate library invariants by making"] # [doc = " invalid use of these duplicates."] # [doc = ""] # [doc = " Panicking in the closure is safe only if all the format arguments for the"] # [doc = " panic message can be safely formatted; this is because although"] # [doc = " `Command` calls [`std::panic::always_abort`](crate::panic::always_abort)"] # [doc = " before calling the pre_exec hook, panic will still try to format the"] # [doc = " panic message."] # [doc = ""] # [doc = " When this closure is run, aspects such as the stdio file descriptors and"] # [doc = " working directory have successfully been changed, so output to these"] # [doc = " locations might not appear where intended."] # [doc = ""] # [doc = " [POSIX fork() specification]:"] # [doc = "     https://pubs.opengroup.org/onlinepubs/9699919799/functions/fork.html"] # [doc = " [`std::env`]: mod@crate::env"] # [stable (feature = "process_pre_exec" , since = "1.34.0")] unsafe fn pre_exec < F > (& mut self , f : F) -> & mut process :: Command where F : FnMut () -> io :: Result < () > + Send + Sync + 'static ; # [doc = " Schedules a closure to be run just before the `exec` function is"] # [doc = " invoked."] # [doc = ""] # [doc = " `before_exec` used to be a safe method, but it needs to be unsafe since the closure may only"] # [doc = " perform operations that are *async-signal-safe*. Hence it got deprecated in favor of the"] # [doc = " unsafe [`pre_exec`]. Meanwhile, Rust gained the ability to make an existing safe method"] # [doc = " fully unsafe in a new edition, which is how `before_exec` became `unsafe`. It still also"] # [doc = " remains deprecated; `pre_exec` should be used instead."] # [doc = ""] # [doc = " [`pre_exec`]: CommandExt::pre_exec"] # [stable (feature = "process_exec" , since = "1.15.0")] # [deprecated (since = "1.37.0" , note = "should be unsafe, use `pre_exec` instead")] # [rustc_deprecated_safe_2024 (audit_that = "the closure is async-signal-safe")] unsafe fn before_exec < F > (& mut self , f : F) -> & mut process :: Command where F : FnMut () -> io :: Result < () > + Send + Sync + 'static , { unsafe { self . pre_exec (f) } } # [doc = " Performs all the required setup by this `Command`, followed by calling"] # [doc = " the `execvp` syscall."] # [doc = ""] # [doc = " On success this function will not return, and otherwise it will return"] # [doc = " an error indicating why the exec (or another part of the setup of the"] # [doc = " `Command`) failed."] # [doc = ""] # [doc = " `exec` not returning has the same implications as calling"] # [doc = " [`process::exit`] – no destructors on the current stack or any other"] # [doc = " thread’s stack will be run. Therefore, it is recommended to only call"] # [doc = " `exec` at a point where it is fine to not run any destructors. Note,"] # [doc = " that the `execvp` syscall independently guarantees that all memory is"] # [doc = " freed and all file descriptors with the `CLOEXEC` option (set by default"] # [doc = " on all file descriptors opened by the standard library) are closed."] # [doc = ""] # [doc = " This function, unlike `spawn`, will **not** `fork` the process to create"] # [doc = " a new child. Like spawn, however, the default behavior for the stdio"] # [doc = " descriptors will be to inherit them from the current process."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " The process may be in a \"broken state\" if this function returns in"] # [doc = " error. For example the working directory, environment variables, signal"] # [doc = " handling settings, various user/group information, or aspects of stdio"] # [doc = " file descriptors may have changed. If a \"transactional spawn\" is"] # [doc = " required to gracefully handle errors it is recommended to use the"] # [doc = " cross-platform `spawn` instead."] # [stable (feature = "process_exec2" , since = "1.9.0")] # [must_use] fn exec (& mut self) -> io :: Error ; # [doc = " Set executable argument"] # [doc = ""] # [doc = " Set the first process argument, `argv[0]`, to something other than the"] # [doc = " default executable path."] # [stable (feature = "process_set_argv0" , since = "1.45.0")] fn arg0 < S > (& mut self , arg : S) -> & mut process :: Command where S : AsRef < OsStr > ; # [doc = " Sets the process group ID (PGID) of the child process. Equivalent to a"] # [doc = " `setpgid` call in the child process, but may be more efficient."] # [doc = ""] # [doc = " Process groups determine which processes receive signals."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Pressing Ctrl-C in a terminal will send SIGINT to all processes in"] # [doc = " the current foreground process group. By spawning the `sleep`"] # [doc = " subprocess in a new process group, it will not receive SIGINT from the"] # [doc = " terminal."] # [doc = ""] # [doc = " The parent process could install a signal handler and manage the"] # [doc = " subprocess on its own terms."] # [doc = ""] # [doc = " A process group ID of 0 will use the process ID as the PGID."] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::process::Command;"] # [doc = " use std::os::unix::process::CommandExt;"] # [doc = ""] # [doc = " Command::new(\"sleep\")"] # [doc = "     .arg(\"10\")"] # [doc = "     .process_group(0)"] # [doc = "     .spawn()?"] # [doc = "     .wait()?;"] # [doc = " #"] # [doc = " # Ok::<_, Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [stable (feature = "process_set_process_group" , since = "1.64.0")] fn process_group (& mut self , pgroup : i32) -> & mut process :: Command ; # [doc = " Set the root of the child process. This calls `chroot` in the child process before executing"] # [doc = " the command."] # [doc = ""] # [doc = " This happens before changing to the directory specified with"] # [doc = " [`process::Command::current_dir`], and that directory will be relative to the new root."] # [doc = ""] # [doc = " If no directory has been specified with [`process::Command::current_dir`], this will set the"] # [doc = " directory to `/`, to avoid leaving the current directory outside the chroot. (This is an"] # [doc = " intentional difference from the underlying `chroot` system call.)"] # [unstable (feature = "process_chroot" , issue = "141298")] fn chroot < P : AsRef < Path > > (& mut self , dir : P) -> & mut process :: Command ; # [unstable (feature = "process_setsid" , issue = "105376")] fn setsid (& mut self , setsid : bool) -> & mut process :: Command ; }
};
}
