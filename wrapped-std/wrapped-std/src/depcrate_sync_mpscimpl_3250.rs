// Generated macro for impl_3250 (impl)
macro_rules! Depcrate_sync_mpscimpl_3250 {
() => {
// Module: crate::sync::mpsc
// Provides: {"impl_3250"}
// Dependencies: {}
impl < T > Receiver < T > { # [doc = " Attempts to return a pending value on this receiver without blocking."] # [doc = ""] # [doc = " This method will never block the caller in order to wait for data to"] # [doc = " become available. Instead, this will always return immediately with a"] # [doc = " possible option of pending data on the channel."] # [doc = ""] # [doc = " This is useful for a flavor of \"optimistic check\" before deciding to"] # [doc = " block on a receiver."] # [doc = ""] # [doc = " Compared with [`recv`], this function has two failure cases instead of one"] # [doc = " (one for disconnection, one for an empty buffer)."] # [doc = ""] # [doc = " [`recv`]: Self::recv"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::sync::mpsc::{Receiver, channel};"] # [doc = ""] # [doc = " let (_, receiver): (_, Receiver<i32>) = channel();"] # [doc = ""] # [doc = " assert!(receiver.try_recv().is_err());"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn try_recv (& self) -> Result < T , TryRecvError > { self . inner . try_recv () } # [doc = " Attempts to wait for a value on this receiver, returning an error if the"] # [doc = " corresponding channel has hung up."] # [doc = ""] # [doc = " This function will always block the current thread if there is no data"] # [doc = " available and it's possible for more data to be sent (at least one sender"] # [doc = " still exists). Once a message is sent to the corresponding [`Sender`]"] # [doc = " (or [`SyncSender`]), this receiver will wake up and return that"] # [doc = " message."] # [doc = ""] # [doc = " If the corresponding [`Sender`] has disconnected, or it disconnects while"] # [doc = " this call is blocking, this call will wake up and return [`Err`] to"] # [doc = " indicate that no more messages can ever be received on this channel."] # [doc = " However, since channels are buffered, messages sent before the disconnect"] # [doc = " will still be properly received."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::sync::mpsc;"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let (send, recv) = mpsc::channel();"] # [doc = " let handle = thread::spawn(move || {"] # [doc = "     send.send(1u8).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " handle.join().unwrap();"] # [doc = ""] # [doc = " assert_eq!(Ok(1), recv.recv());"] # [doc = " ```"] # [doc = ""] # [doc = " Buffering behavior:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::sync::mpsc;"] # [doc = " use std::thread;"] # [doc = " use std::sync::mpsc::RecvError;"] # [doc = ""] # [doc = " let (send, recv) = mpsc::channel();"] # [doc = " let handle = thread::spawn(move || {"] # [doc = "     send.send(1u8).unwrap();"] # [doc = "     send.send(2).unwrap();"] # [doc = "     send.send(3).unwrap();"] # [doc = "     drop(send);"] # [doc = " });"] # [doc = ""] # [doc = " // wait for the thread to join so we ensure the sender is dropped"] # [doc = " handle.join().unwrap();"] # [doc = ""] # [doc = " assert_eq!(Ok(1), recv.recv());"] # [doc = " assert_eq!(Ok(2), recv.recv());"] # [doc = " assert_eq!(Ok(3), recv.recv());"] # [doc = " assert_eq!(Err(RecvError), recv.recv());"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn recv (& self) -> Result < T , RecvError > { self . inner . recv () } # [doc = " Attempts to wait for a value on this receiver, returning an error if the"] # [doc = " corresponding channel has hung up, or if it waits more than `timeout`."] # [doc = ""] # [doc = " This function will always block the current thread if there is no data"] # [doc = " available and it's possible for more data to be sent (at least one sender"] # [doc = " still exists). Once a message is sent to the corresponding [`Sender`]"] # [doc = " (or [`SyncSender`]), this receiver will wake up and return that"] # [doc = " message."] # [doc = ""] # [doc = " If the corresponding [`Sender`] has disconnected, or it disconnects while"] # [doc = " this call is blocking, this call will wake up and return [`Err`] to"] # [doc = " indicate that no more messages can ever be received on this channel."] # [doc = " However, since channels are buffered, messages sent before the disconnect"] # [doc = " will still be properly received."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Successfully receiving value before encountering timeout:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use std::sync::mpsc;"] # [doc = ""] # [doc = " let (send, recv) = mpsc::channel();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     send.send('a').unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     recv.recv_timeout(Duration::from_millis(400)),"] # [doc = "     Ok('a')"] # [doc = " );"] # [doc = " ```"] # [doc = ""] # [doc = " Receiving an error upon reaching timeout:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use std::sync::mpsc;"] # [doc = ""] # [doc = " let (send, recv) = mpsc::channel();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_millis(800));"] # [doc = "     send.send('a').unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     recv.recv_timeout(Duration::from_millis(400)),"] # [doc = "     Err(mpsc::RecvTimeoutError::Timeout)"] # [doc = " );"] # [doc = " ```"] # [stable (feature = "mpsc_recv_timeout" , since = "1.12.0")] pub fn recv_timeout (& self , timeout : Duration) -> Result < T , RecvTimeoutError > { self . inner . recv_timeout (timeout) } # [doc = " Attempts to wait for a value on this receiver, returning an error if the"] # [doc = " corresponding channel has hung up, or if `deadline` is reached."] # [doc = ""] # [doc = " This function will always block the current thread if there is no data"] # [doc = " available and it's possible for more data to be sent. Once a message is"] # [doc = " sent to the corresponding [`Sender`] (or [`SyncSender`]), then this"] # [doc = " receiver will wake up and return that message."] # [doc = ""] # [doc = " If the corresponding [`Sender`] has disconnected, or it disconnects while"] # [doc = " this call is blocking, this call will wake up and return [`Err`] to"] # [doc = " indicate that no more messages can ever be received on this channel."] # [doc = " However, since channels are buffered, messages sent before the disconnect"] # [doc = " will still be properly received."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Successfully receiving value before reaching deadline:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " #![feature(deadline_api)]"] # [doc = " use std::thread;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = " use std::sync::mpsc;"] # [doc = ""] # [doc = " let (send, recv) = mpsc::channel();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     send.send('a').unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     recv.recv_deadline(Instant::now() + Duration::from_millis(400)),"] # [doc = "     Ok('a')"] # [doc = " );"] # [doc = " ```"] # [doc = ""] # [doc = " Receiving an error upon reaching deadline:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " #![feature(deadline_api)]"] # [doc = " use std::thread;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = " use std::sync::mpsc;"] # [doc = ""] # [doc = " let (send, recv) = mpsc::channel();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_millis(800));"] # [doc = "     send.send('a').unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     recv.recv_deadline(Instant::now() + Duration::from_millis(400)),"] # [doc = "     Err(mpsc::RecvTimeoutError::Timeout)"] # [doc = " );"] # [doc = " ```"] # [unstable (feature = "deadline_api" , issue = "46316")] pub fn recv_deadline (& self , deadline : Instant) -> Result < T , RecvTimeoutError > { self . inner . recv_deadline (deadline) } # [doc = " Returns an iterator that will block waiting for messages, but never"] # [doc = " [`panic!`]. It will return [`None`] when the channel has hung up."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::sync::mpsc::channel;"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let (send, recv) = channel();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     send.send(1).unwrap();"] # [doc = "     send.send(2).unwrap();"] # [doc = "     send.send(3).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " let mut iter = recv.iter();"] # [doc = " assert_eq!(iter.next(), Some(1));"] # [doc = " assert_eq!(iter.next(), Some(2));"] # [doc = " assert_eq!(iter.next(), Some(3));"] # [doc = " assert_eq!(iter.next(), None);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn iter (& self) -> Iter < '_ , T > { Iter { rx : self } } # [doc = " Returns an iterator that will attempt to yield all pending values."] # [doc = " It will return `None` if there are no more pending values or if the"] # [doc = " channel has hung up. The iterator will never [`panic!`] or block the"] # [doc = " user by waiting for values."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::sync::mpsc::channel;"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " let (sender, receiver) = channel();"] # [doc = ""] # [doc = " // nothing is in the buffer yet"] # [doc = " assert!(receiver.try_iter().next().is_none());"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     sender.send(1).unwrap();"] # [doc = "     sender.send(2).unwrap();"] # [doc = "     sender.send(3).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " // nothing is in the buffer yet"] # [doc = " assert!(receiver.try_iter().next().is_none());"] # [doc = ""] # [doc = " // block for two seconds"] # [doc = " thread::sleep(Duration::from_secs(2));"] # [doc = ""] # [doc = " let mut iter = receiver.try_iter();"] # [doc = " assert_eq!(iter.next(), Some(1));"] # [doc = " assert_eq!(iter.next(), Some(2));"] # [doc = " assert_eq!(iter.next(), Some(3));"] # [doc = " assert_eq!(iter.next(), None);"] # [doc = " ```"] # [stable (feature = "receiver_try_iter" , since = "1.15.0")] pub fn try_iter (& self) -> TryIter < '_ , T > { TryIter { rx : self } } }
};
}
