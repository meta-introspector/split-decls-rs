// Generated macro for ExitStatusExt (trait)
macro_rules! Depcrate_os_unix_processExitStatusExt {
() => {
// Module: crate::os::unix::process
// Provides: {"ExitStatusExt"}
// Dependencies: {}
# [doc = " Unix-specific extensions to [`process::ExitStatus`] and"] # [doc = " [`ExitStatusError`](process::ExitStatusError)."] # [doc = ""] # [doc = " On Unix, `ExitStatus` **does not necessarily represent an exit status**, as"] # [doc = " passed to the `_exit` system call or returned by"] # [doc = " [`ExitStatus::code()`](crate::process::ExitStatus::code).  It represents **any wait status**"] # [doc = " as returned by one of the `wait` family of system"] # [doc = " calls."] # [doc = ""] # [doc = " A Unix wait status (a Rust `ExitStatus`) can represent a Unix exit status, but can also"] # [doc = " represent other kinds of process event."] # [doc = ""] # [doc = " This trait is sealed: it cannot be implemented outside the standard library."] # [doc = " This is so that future additional methods are not breaking changes."] # [stable (feature = "rust1" , since = "1.0.0")] pub trait ExitStatusExt : Sealed { # [doc = " Creates a new `ExitStatus` or `ExitStatusError` from the raw underlying integer status"] # [doc = " value from `wait`"] # [doc = ""] # [doc = " The value should be a **wait status, not an exit status**."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics on an attempt to make an `ExitStatusError` from a wait status of `0`."] # [doc = ""] # [doc = " Making an `ExitStatus` always succeeds and never panics."] # [stable (feature = "exit_status_from" , since = "1.12.0")] fn from_raw (raw : i32) -> Self ; # [doc = " If the process was terminated by a signal, returns that signal."] # [doc = ""] # [doc = " In other words, if `WIFSIGNALED`, this returns `WTERMSIG`."] # [stable (feature = "rust1" , since = "1.0.0")] fn signal (& self) -> Option < i32 > ; # [doc = " If the process was terminated by a signal, says whether it dumped core."] # [stable (feature = "unix_process_wait_more" , since = "1.58.0")] fn core_dumped (& self) -> bool ; # [doc = " If the process was stopped by a signal, returns that signal."] # [doc = ""] # [doc = " In other words, if `WIFSTOPPED`, this returns `WSTOPSIG`.  This is only possible if the status came from"] # [doc = " a `wait` system call which was passed `WUNTRACED`, and was then converted into an `ExitStatus`."] # [stable (feature = "unix_process_wait_more" , since = "1.58.0")] fn stopped_signal (& self) -> Option < i32 > ; # [doc = " Whether the process was continued from a stopped status."] # [doc = ""] # [doc = " Ie, `WIFCONTINUED`.  This is only possible if the status came from a `wait` system call"] # [doc = " which was passed `WCONTINUED`, and was then converted into an `ExitStatus`."] # [stable (feature = "unix_process_wait_more" , since = "1.58.0")] fn continued (& self) -> bool ; # [doc = " Returns the underlying raw `wait` status."] # [doc = ""] # [doc = " The returned integer is a **wait status, not an exit status**."] # [stable (feature = "unix_process_wait_more" , since = "1.58.0")] fn into_raw (self) -> i32 ; }
};
}
