// Generated macro for File (struct)
macro_rules! Depcrate_fsFile {
() => {
// Module: crate::fs
// Provides: {"File"}
// Dependencies: {}
# [doc = " An object providing access to an open file on the filesystem."] # [doc = ""] # [doc = " An instance of a `File` can be read and/or written depending on what options"] # [doc = " it was opened with. Files also implement [`Seek`] to alter the logical cursor"] # [doc = " that the file contains internally."] # [doc = ""] # [doc = " Files are automatically closed when they go out of scope.  Errors detected"] # [doc = " on closing are ignored by the implementation of `Drop`.  Use the method"] # [doc = " [`sync_all`] if these errors must be manually handled."] # [doc = ""] # [doc = " `File` does not buffer reads and writes. For efficiency, consider wrapping the"] # [doc = " file in a [`BufReader`] or [`BufWriter`] when performing many small [`read`]"] # [doc = " or [`write`] calls, unless unbuffered reads and writes are required."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Creates a new file and write bytes to it (you can also use [`write`]):"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::fs::File;"] # [doc = " use std::io::prelude::*;"] # [doc = ""] # [doc = " fn main() -> std::io::Result<()> {"] # [doc = "     let mut file = File::create(\"foo.txt\")?;"] # [doc = "     file.write_all(b\"Hello, world!\")?;"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Reads the contents of a file into a [`String`] (you can also use [`read`]):"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::fs::File;"] # [doc = " use std::io::prelude::*;"] # [doc = ""] # [doc = " fn main() -> std::io::Result<()> {"] # [doc = "     let mut file = File::open(\"foo.txt\")?;"] # [doc = "     let mut contents = String::new();"] # [doc = "     file.read_to_string(&mut contents)?;"] # [doc = "     assert_eq!(contents, \"Hello, world!\");"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Using a buffered [`Read`]er:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::fs::File;"] # [doc = " use std::io::BufReader;"] # [doc = " use std::io::prelude::*;"] # [doc = ""] # [doc = " fn main() -> std::io::Result<()> {"] # [doc = "     let file = File::open(\"foo.txt\")?;"] # [doc = "     let mut buf_reader = BufReader::new(file);"] # [doc = "     let mut contents = String::new();"] # [doc = "     buf_reader.read_to_string(&mut contents)?;"] # [doc = "     assert_eq!(contents, \"Hello, world!\");"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Note that, although read and write methods require a `&mut File`, because"] # [doc = " of the interfaces for [`Read`] and [`Write`], the holder of a `&File` can"] # [doc = " still modify the file, either through methods that take `&File` or by"] # [doc = " retrieving the underlying OS object and modifying the file that way."] # [doc = " Additionally, many operating systems allow concurrent modification of files"] # [doc = " by different processes. Avoid assuming that holding a `&File` means that the"] # [doc = " file will not change."] # [doc = ""] # [doc = " # Platform-specific behavior"] # [doc = ""] # [doc = " On Windows, the implementation of [`Read`] and [`Write`] traits for `File`"] # [doc = " perform synchronous I/O operations. Therefore the underlying file must not"] # [doc = " have been opened for asynchronous I/O (e.g. by using `FILE_FLAG_OVERLAPPED`)."] # [doc = ""] # [doc = " [`BufReader`]: io::BufReader"] # [doc = " [`BufWriter`]: io::BufWriter"] # [doc = " [`sync_all`]: File::sync_all"] # [doc = " [`write`]: File::write"] # [doc = " [`read`]: File::read"] # [stable (feature = "rust1" , since = "1.0.0")] # [cfg_attr (not (test) , rustc_diagnostic_item = "File")] pub struct File { inner : fs_imp :: File , }
};
}
