// Generated macro for impl_271 (impl)
macro_rules! Depcrate_threadimpl_271 {
() => {
// Module: crate::thread
// Provides: {"impl_271"}
// Dependencies: {}
impl < T > JoinHandle < T > { # [doc = " Extracts a handle to the underlying thread."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let builder = thread::Builder::new();"] # [doc = ""] # [doc = " let join_handle: thread::JoinHandle<_> = builder.spawn(|| {"] # [doc = "     // some work here"] # [doc = " }).unwrap();"] # [doc = ""] # [doc = " let thread = join_handle.thread();"] # [doc = " println!(\"thread id: {:?}\", thread.id());"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [must_use] pub fn thread (& self) -> & Thread { & self . 0 . thread } # [doc = " Waits for the associated thread to finish."] # [doc = ""] # [doc = " This function will return immediately if the associated thread has already finished."] # [doc = ""] # [doc = " In terms of [atomic memory orderings],  the completion of the associated"] # [doc = " thread synchronizes with this function returning. In other words, all"] # [doc = " operations performed by that thread [happen"] # [doc = " before](https://doc.rust-lang.org/nomicon/atomics.html#data-accesses) all"] # [doc = " operations that happen after `join` returns."] # [doc = ""] # [doc = " If the associated thread panics, [`Err`] is returned with the parameter given"] # [doc = " to [`panic!`] (though see the Notes below)."] # [doc = ""] # [doc = " [`Err`]: crate::result::Result::Err"] # [doc = " [atomic memory orderings]: crate::sync::atomic"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function may panic on some platforms if a thread attempts to join"] # [doc = " itself or otherwise may create a deadlock with joining threads."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let builder = thread::Builder::new();"] # [doc = ""] # [doc = " let join_handle: thread::JoinHandle<_> = builder.spawn(|| {"] # [doc = "     // some work here"] # [doc = " }).unwrap();"] # [doc = " join_handle.join().expect(\"Couldn't join on the associated thread\");"] # [doc = " ```"] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " If a \"foreign\" unwinding operation (e.g. an exception thrown from C++"] # [doc = " code, or a `panic!` in Rust code compiled or linked with a different"] # [doc = " runtime) unwinds all the way to the thread root, the process may be"] # [doc = " aborted; see the Notes on [`thread::spawn`]. If the process is not"] # [doc = " aborted, this function will return a `Result::Err` containing an opaque"] # [doc = " type."] # [doc = ""] # [doc = " [`catch_unwind`]: ../../std/panic/fn.catch_unwind.html"] # [doc = " [`thread::spawn`]: spawn"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn join (self) -> Result < T > { self . 0 . join () } # [doc = " Checks if the associated thread has finished running its main function."] # [doc = ""] # [doc = " `is_finished` supports implementing a non-blocking join operation, by checking"] # [doc = " `is_finished`, and calling `join` if it returns `true`. This function does not block. To"] # [doc = " block while waiting on the thread to finish, use [`join`][Self::join]."] # [doc = ""] # [doc = " This might return `true` for a brief moment after the thread's main"] # [doc = " function has returned, but before the thread itself has stopped running."] # [doc = " However, once this returns `true`, [`join`][Self::join] can be expected"] # [doc = " to return quickly, without blocking for any significant amount of time."] # [stable (feature = "thread_is_running" , since = "1.61.0")] pub fn is_finished (& self) -> bool { Arc :: strong_count (& self . 0 . packet) == 1 } }
};
}
