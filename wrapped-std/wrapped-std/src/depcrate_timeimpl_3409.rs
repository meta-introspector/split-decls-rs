// Generated macro for impl_3409 (impl)
macro_rules! Depcrate_timeimpl_3409 {
() => {
// Module: crate::time
// Provides: {"impl_3409"}
// Dependencies: {}
impl Instant { # [doc = " Returns an instant corresponding to \"now\"."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::time::Instant;"] # [doc = ""] # [doc = " let now = Instant::now();"] # [doc = " ```"] # [must_use] # [stable (feature = "time2" , since = "1.8.0")] # [cfg_attr (not (test) , rustc_diagnostic_item = "instant_now")] pub fn now () -> Instant { Instant (time :: Instant :: now ()) } # [doc = " Returns the amount of time elapsed from another instant to this one,"] # [doc = " or zero duration if that instant is later than this one."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Previous Rust versions panicked when `earlier` was later than `self`. Currently this"] # [doc = " method saturates. Future versions may reintroduce the panic in some circumstances."] # [doc = " See [Monotonicity]."] # [doc = ""] # [doc = " [Monotonicity]: Instant#monotonicity"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::time::{Duration, Instant};"] # [doc = " use std::thread::sleep;"] # [doc = ""] # [doc = " let now = Instant::now();"] # [doc = " sleep(Duration::new(1, 0));"] # [doc = " let new_now = Instant::now();"] # [doc = " println!(\"{:?}\", new_now.duration_since(now));"] # [doc = " println!(\"{:?}\", now.duration_since(new_now)); // 0ns"] # [doc = " ```"] # [must_use] # [stable (feature = "time2" , since = "1.8.0")] pub fn duration_since (& self , earlier : Instant) -> Duration { self . checked_duration_since (earlier) . unwrap_or_default () } # [doc = " Returns the amount of time elapsed from another instant to this one,"] # [doc = " or None if that instant is later than this one."] # [doc = ""] # [doc = " Due to [monotonicity bugs], even under correct logical ordering of the passed `Instant`s,"] # [doc = " this method can return `None`."] # [doc = ""] # [doc = " [monotonicity bugs]: Instant#monotonicity"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::time::{Duration, Instant};"] # [doc = " use std::thread::sleep;"] # [doc = ""] # [doc = " let now = Instant::now();"] # [doc = " sleep(Duration::new(1, 0));"] # [doc = " let new_now = Instant::now();"] # [doc = " println!(\"{:?}\", new_now.checked_duration_since(now));"] # [doc = " println!(\"{:?}\", now.checked_duration_since(new_now)); // None"] # [doc = " ```"] # [must_use] # [stable (feature = "checked_duration_since" , since = "1.39.0")] pub fn checked_duration_since (& self , earlier : Instant) -> Option < Duration > { self . 0 . checked_sub_instant (& earlier . 0) } # [doc = " Returns the amount of time elapsed from another instant to this one,"] # [doc = " or zero duration if that instant is later than this one."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::time::{Duration, Instant};"] # [doc = " use std::thread::sleep;"] # [doc = ""] # [doc = " let now = Instant::now();"] # [doc = " sleep(Duration::new(1, 0));"] # [doc = " let new_now = Instant::now();"] # [doc = " println!(\"{:?}\", new_now.saturating_duration_since(now));"] # [doc = " println!(\"{:?}\", now.saturating_duration_since(new_now)); // 0ns"] # [doc = " ```"] # [must_use] # [stable (feature = "checked_duration_since" , since = "1.39.0")] pub fn saturating_duration_since (& self , earlier : Instant) -> Duration { self . checked_duration_since (earlier) . unwrap_or_default () } # [doc = " Returns the amount of time elapsed since this instant."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Previous Rust versions panicked when the current time was earlier than self. Currently this"] # [doc = " method returns a Duration of zero in that case. Future versions may reintroduce the panic."] # [doc = " See [Monotonicity]."] # [doc = ""] # [doc = " [Monotonicity]: Instant#monotonicity"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::thread::sleep;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = ""] # [doc = " let instant = Instant::now();"] # [doc = " let three_secs = Duration::from_secs(3);"] # [doc = " sleep(three_secs);"] # [doc = " assert!(instant.elapsed() >= three_secs);"] # [doc = " ```"] # [must_use] # [stable (feature = "time2" , since = "1.8.0")] pub fn elapsed (& self) -> Duration { Instant :: now () - * self } # [doc = " Returns `Some(t)` where `t` is the time `self + duration` if `t` can be represented as"] # [doc = " `Instant` (which means it's inside the bounds of the underlying data structure), `None`"] # [doc = " otherwise."] # [stable (feature = "time_checked_add" , since = "1.34.0")] pub fn checked_add (& self , duration : Duration) -> Option < Instant > { self . 0 . checked_add_duration (& duration) . map (Instant) } # [doc = " Returns `Some(t)` where `t` is the time `self - duration` if `t` can be represented as"] # [doc = " `Instant` (which means it's inside the bounds of the underlying data structure), `None`"] # [doc = " otherwise."] # [stable (feature = "time_checked_add" , since = "1.34.0")] pub fn checked_sub (& self , duration : Duration) -> Option < Instant > { self . 0 . checked_sub_duration (& duration) . map (Instant) } # [cfg_attr (not (target_os = "linux") , allow (unused , reason = "not every platform has a specific `sleep_until`"))] pub (crate) fn into_inner (self) -> time :: Instant { self . 0 } }
};
}
