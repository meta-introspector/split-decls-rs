// Generated macro for FileExt (trait)
macro_rules! Depcrate_os_unix_fsFileExt {
() => {
// Module: crate::os::unix::fs
// Provides: {"FileExt"}
// Dependencies: {}
# [doc = " Unix-specific extensions to [`fs::File`]."] # [stable (feature = "file_offset" , since = "1.15.0")] pub trait FileExt { # [doc = " Reads a number of bytes starting from a given offset."] # [doc = ""] # [doc = " Returns the number of bytes read."] # [doc = ""] # [doc = " The offset is relative to the start of the file and thus independent"] # [doc = " from the current cursor."] # [doc = ""] # [doc = " The current file cursor is not affected by this function."] # [doc = ""] # [doc = " Note that similar to [`File::read`], it is not an error to return with a"] # [doc = " short read."] # [doc = ""] # [doc = " [`File::read`]: fs::File::read"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io;"] # [doc = " use std::fs::File;"] # [doc = " use std::os::unix::prelude::FileExt;"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut buf = [0u8; 8];"] # [doc = "     let file = File::open(\"foo.txt\")?;"] # [doc = ""] # [doc = "     // We now read 8 bytes from the offset 10."] # [doc = "     let num_bytes_read = file.read_at(&mut buf, 10)?;"] # [doc = "     println!(\"read {num_bytes_read} bytes: {buf:?}\");"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "file_offset" , since = "1.15.0")] fn read_at (& self , buf : & mut [u8] , offset : u64) -> io :: Result < usize > ; # [doc = " Like `read_at`, except that it reads into a slice of buffers."] # [doc = ""] # [doc = " Data is copied to fill each buffer in order, with the final buffer"] # [doc = " written to possibly being only partially filled. This method must behave"] # [doc = " equivalently to a single call to read with concatenated buffers."] # [unstable (feature = "unix_file_vectored_at" , issue = "89517")] fn read_vectored_at (& self , bufs : & mut [io :: IoSliceMut < '_ >] , offset : u64) -> io :: Result < usize > { io :: default_read_vectored (| b | self . read_at (b , offset) , bufs) } # [doc = " Reads the exact number of bytes required to fill `buf` from the given offset."] # [doc = ""] # [doc = " The offset is relative to the start of the file and thus independent"] # [doc = " from the current cursor."] # [doc = ""] # [doc = " The current file cursor is not affected by this function."] # [doc = ""] # [doc = " Similar to [`io::Read::read_exact`] but uses [`read_at`] instead of `read`."] # [doc = ""] # [doc = " [`read_at`]: FileExt::read_at"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If this function encounters an error of the kind"] # [doc = " [`io::ErrorKind::Interrupted`] then the error is ignored and the operation"] # [doc = " will continue."] # [doc = ""] # [doc = " If this function encounters an \"end of file\" before completely filling"] # [doc = " the buffer, it returns an error of the kind [`io::ErrorKind::UnexpectedEof`]."] # [doc = " The contents of `buf` are unspecified in this case."] # [doc = ""] # [doc = " If any other read error is encountered then this function immediately"] # [doc = " returns. The contents of `buf` are unspecified in this case."] # [doc = ""] # [doc = " If this function returns an error, it is unspecified how many bytes it"] # [doc = " has read, but it will never read more than would be necessary to"] # [doc = " completely fill the buffer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io;"] # [doc = " use std::fs::File;"] # [doc = " use std::os::unix::prelude::FileExt;"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut buf = [0u8; 8];"] # [doc = "     let file = File::open(\"foo.txt\")?;"] # [doc = ""] # [doc = "     // We now read exactly 8 bytes from the offset 10."] # [doc = "     file.read_exact_at(&mut buf, 10)?;"] # [doc = "     println!(\"read {} bytes: {:?}\", buf.len(), buf);"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "rw_exact_all_at" , since = "1.33.0")] fn read_exact_at (& self , mut buf : & mut [u8] , mut offset : u64) -> io :: Result < () > { while ! buf . is_empty () { match self . read_at (buf , offset) { Ok (0) => break , Ok (n) => { let tmp = buf ; buf = & mut tmp [n ..] ; offset += n as u64 ; } Err (ref e) if e . is_interrupted () => { } Err (e) => return Err (e) , } } if ! buf . is_empty () { Err (io :: Error :: READ_EXACT_EOF) } else { Ok (()) } } # [doc = " Reads some bytes starting from a given offset into the buffer."] # [doc = ""] # [doc = " This equivalent to the [`read_at`](FileExt::read_at) method, except that it is passed a"] # [doc = " [`BorrowedCursor`] rather than `&mut [u8]` to allow use with uninitialized buffers. The new"] # [doc = " data will be appended to any existing contents of `buf`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " #![feature(core_io_borrowed_buf)]"] # [doc = " #![feature(read_buf_at)]"] # [doc = ""] # [doc = " use std::io;"] # [doc = " use std::io::BorrowedBuf;"] # [doc = " use std::fs::File;"] # [doc = " use std::mem::MaybeUninit;"] # [doc = " use std::os::unix::prelude::*;"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut file = File::open(\"pi.txt\")?;"] # [doc = ""] # [doc = "     // Read some bytes starting from offset 2"] # [doc = "     let mut buf: [MaybeUninit<u8>; 10] = [MaybeUninit::uninit(); 10];"] # [doc = "     let mut buf = BorrowedBuf::from(buf.as_mut_slice());"] # [doc = "     file.read_buf_at(buf.unfilled(), 2)?;"] # [doc = ""] # [doc = "     assert!(buf.filled().starts_with(b\"1\"));"] # [doc = ""] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [unstable (feature = "read_buf_at" , issue = "140771")] fn read_buf_at (& self , buf : BorrowedCursor < '_ > , offset : u64) -> io :: Result < () > { io :: default_read_buf (| b | self . read_at (b , offset) , buf) } # [doc = " Reads the exact number of bytes required to fill the buffer from a given offset."] # [doc = ""] # [doc = " This is equivalent to the [`read_exact_at`](FileExt::read_exact_at) method, except that it"] # [doc = " is passed a [`BorrowedCursor`] rather than `&mut [u8]` to allow use with uninitialized"] # [doc = " buffers. The new data will be appended to any existing contents of `buf`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " #![feature(core_io_borrowed_buf)]"] # [doc = " #![feature(read_buf_at)]"] # [doc = ""] # [doc = " use std::io;"] # [doc = " use std::io::BorrowedBuf;"] # [doc = " use std::fs::File;"] # [doc = " use std::mem::MaybeUninit;"] # [doc = " use std::os::unix::prelude::*;"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut file = File::open(\"pi.txt\")?;"] # [doc = ""] # [doc = "     // Read exactly 10 bytes starting from offset 2"] # [doc = "     let mut buf: [MaybeUninit<u8>; 10] = [MaybeUninit::uninit(); 10];"] # [doc = "     let mut buf = BorrowedBuf::from(buf.as_mut_slice());"] # [doc = "     file.read_buf_exact_at(buf.unfilled(), 2)?;"] # [doc = ""] # [doc = "     assert_eq!(buf.filled(), b\"1415926535\");"] # [doc = ""] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [unstable (feature = "read_buf_at" , issue = "140771")] fn read_buf_exact_at (& self , mut buf : BorrowedCursor < '_ > , mut offset : u64) -> io :: Result < () > { while buf . capacity () > 0 { let prev_written = buf . written () ; match self . read_buf_at (buf . reborrow () , offset) { Ok (()) => { } Err (e) if e . is_interrupted () => { } Err (e) => return Err (e) , } let n = buf . written () - prev_written ; offset += n as u64 ; if n == 0 { return Err (io :: Error :: READ_EXACT_EOF) ; } } Ok (()) } # [doc = " Writes a number of bytes starting from a given offset."] # [doc = ""] # [doc = " Returns the number of bytes written."] # [doc = ""] # [doc = " The offset is relative to the start of the file and thus independent"] # [doc = " from the current cursor."] # [doc = ""] # [doc = " The current file cursor is not affected by this function."] # [doc = ""] # [doc = " When writing beyond the end of the file, the file is appropriately"] # [doc = " extended and the intermediate bytes are initialized with the value 0."] # [doc = ""] # [doc = " Note that similar to [`File::write`], it is not an error to return a"] # [doc = " short write."] # [doc = ""] # [doc = " # Bug"] # [doc = " On some systems, `write_at` utilises [`pwrite64`] to write to files."] # [doc = " However, this syscall has a [bug] where files opened with the `O_APPEND`"] # [doc = " flag fail to respect the offset parameter, always appending to the end"] # [doc = " of the file instead."] # [doc = ""] # [doc = " It is possible to inadvertently set this flag, like in the example below."] # [doc = " Therefore, it is important to be vigilant while changing options to mitigate"] # [doc = " unexpected behavior."] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::fs::File;"] # [doc = " use std::io;"] # [doc = " use std::os::unix::prelude::FileExt;"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     // Open a file with the append option (sets the `O_APPEND` flag)"] # [doc = "     let file = File::options().append(true).open(\"foo.txt\")?;"] # [doc = ""] # [doc = "     // We attempt to write at offset 10; instead appended to EOF"] # [doc = "     file.write_at(b\"sushi\", 10)?;"] # [doc = ""] # [doc = "     // foo.txt is 5 bytes long instead of 15"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [`File::write`]: fs::File::write"] # [doc = " [`pwrite64`]: https://man7.org/linux/man-pages/man2/pwrite.2.html"] # [doc = " [bug]: https://man7.org/linux/man-pages/man2/pwrite.2.html#BUGS"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::fs::File;"] # [doc = " use std::io;"] # [doc = " use std::os::unix::prelude::FileExt;"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let file = File::create(\"foo.txt\")?;"] # [doc = ""] # [doc = "     // We now write at the offset 10."] # [doc = "     file.write_at(b\"sushi\", 10)?;"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "file_offset" , since = "1.15.0")] fn write_at (& self , buf : & [u8] , offset : u64) -> io :: Result < usize > ; # [doc = " Like `write_at`, except that it writes from a slice of buffers."] # [doc = ""] # [doc = " Data is copied from each buffer in order, with the final buffer read"] # [doc = " from possibly being only partially consumed. This method must behave as"] # [doc = " a call to `write_at` with the buffers concatenated would."] # [unstable (feature = "unix_file_vectored_at" , issue = "89517")] fn write_vectored_at (& self , bufs : & [io :: IoSlice < '_ >] , offset : u64) -> io :: Result < usize > { io :: default_write_vectored (| b | self . write_at (b , offset) , bufs) } # [doc = " Attempts to write an entire buffer starting from a given offset."] # [doc = ""] # [doc = " The offset is relative to the start of the file and thus independent"] # [doc = " from the current cursor."] # [doc = ""] # [doc = " The current file cursor is not affected by this function."] # [doc = ""] # [doc = " This method will continuously call [`write_at`] until there is no more data"] # [doc = " to be written or an error of non-[`io::ErrorKind::Interrupted`] kind is"] # [doc = " returned. This method will not return until the entire buffer has been"] # [doc = " successfully written or such an error occurs. The first error that is"] # [doc = " not of [`io::ErrorKind::Interrupted`] kind generated from this method will be"] # [doc = " returned."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will return the first error of"] # [doc = " non-[`io::ErrorKind::Interrupted`] kind that [`write_at`] returns."] # [doc = ""] # [doc = " [`write_at`]: FileExt::write_at"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::fs::File;"] # [doc = " use std::io;"] # [doc = " use std::os::unix::prelude::FileExt;"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let file = File::open(\"foo.txt\")?;"] # [doc = ""] # [doc = "     // We now write at the offset 10."] # [doc = "     file.write_all_at(b\"sushi\", 10)?;"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "rw_exact_all_at" , since = "1.33.0")] fn write_all_at (& self , mut buf : & [u8] , mut offset : u64) -> io :: Result < () > { while ! buf . is_empty () { match self . write_at (buf , offset) { Ok (0) => { return Err (io :: Error :: WRITE_ALL_EOF) ; } Ok (n) => { buf = & buf [n ..] ; offset += n as u64 } Err (ref e) if e . is_interrupted () => { } Err (e) => return Err (e) , } } Ok (()) } }
};
}
