// Generated macro for BorrowedHandle (struct)
macro_rules! Depcrate_os_windows_io_handleBorrowedHandle {
() => {
// Module: crate::os::windows::io::handle
// Provides: {"BorrowedHandle"}
// Dependencies: {}
# [doc = " A borrowed handle."] # [doc = ""] # [doc = " This has a lifetime parameter to tie it to the lifetime of something that"] # [doc = " owns the handle."] # [doc = ""] # [doc = " This uses `repr(transparent)` and has the representation of a host handle,"] # [doc = " so it can be used in FFI in places where a handle is passed as an argument,"] # [doc = " it is not captured or consumed."] # [doc = ""] # [doc = " Note that it *may* have the value `-1`, which in `BorrowedHandle` always"] # [doc = " represents a valid handle value, such as [the current process handle], and"] # [doc = " not `INVALID_HANDLE_VALUE`, despite the two having the same value. See"] # [doc = " [here] for the full story."] # [doc = ""] # [doc = " And, it *may* have the value `NULL` (0), which can occur when consoles are"] # [doc = " detached from processes, or when `windows_subsystem` is used."] # [doc = ""] # [doc = " This type's `.to_owned()` implementation returns another `BorrowedHandle`"] # [doc = " rather than an `OwnedHandle`. It just makes a trivial copy of the raw"] # [doc = " handle, which is then borrowed under the same lifetime."] # [doc = ""] # [doc = " [here]: https://devblogs.microsoft.com/oldnewthing/20040302-00/?p=40443"] # [doc = " [the current process handle]: https://docs.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-getcurrentprocess#remarks"] # [derive (Copy , Clone)] # [repr (transparent)] # [stable (feature = "io_safety" , since = "1.63.0")] pub struct BorrowedHandle < 'handle > { handle : RawHandle , _phantom : PhantomData < & 'handle OwnedHandle > , }
};
}
