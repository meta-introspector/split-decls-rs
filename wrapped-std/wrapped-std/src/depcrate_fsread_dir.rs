// Generated macro for read_dir (function)
macro_rules! Depcrate_fsread_dir {
() => {
// Module: crate::fs
// Provides: {"read_dir"}
// Dependencies: {}
# [doc = " Returns an iterator over the entries within a directory."] # [doc = ""] # [doc = " The iterator will yield instances of <code>[io::Result]<[DirEntry]></code>."] # [doc = " New errors may be encountered after an iterator is initially constructed."] # [doc = " Entries for the current and parent directories (typically `.` and `..`) are"] # [doc = " skipped."] # [doc = ""] # [doc = " The order in which `read_dir` returns entries can change between calls. If reproducible"] # [doc = " ordering is required, the entries should be explicitly sorted."] # [doc = ""] # [doc = " # Platform-specific behavior"] # [doc = ""] # [doc = " This function currently corresponds to the `opendir` function on Unix"] # [doc = " and the `FindFirstFileEx` function on Windows. Advancing the iterator"] # [doc = " currently corresponds to `readdir` on Unix and `FindNextFile` on Windows."] # [doc = " Note that, this [may change in the future][changes]."] # [doc = ""] # [doc = " [changes]: io#platform-specific-behavior"] # [doc = ""] # [doc = " The order in which this iterator returns entries is platform and filesystem"] # [doc = " dependent."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will return an error in the following situations, but is not"] # [doc = " limited to just these cases:"] # [doc = ""] # [doc = " * The provided `path` doesn't exist."] # [doc = " * The process lacks permissions to view the contents."] # [doc = " * The `path` points at a non-directory file."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io;"] # [doc = " use std::fs::{self, DirEntry};"] # [doc = " use std::path::Path;"] # [doc = ""] # [doc = " // one possible implementation of walking a directory only visiting files"] # [doc = " fn visit_dirs(dir: &Path, cb: &dyn Fn(&DirEntry)) -> io::Result<()> {"] # [doc = "     if dir.is_dir() {"] # [doc = "         for entry in fs::read_dir(dir)? {"] # [doc = "             let entry = entry?;"] # [doc = "             let path = entry.path();"] # [doc = "             if path.is_dir() {"] # [doc = "                 visit_dirs(&path, cb)?;"] # [doc = "             } else {"] # [doc = "                 cb(&entry);"] # [doc = "             }"] # [doc = "         }"] # [doc = "     }"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::{fs, io};"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut entries = fs::read_dir(\".\")?"] # [doc = "         .map(|res| res.map(|e| e.path()))"] # [doc = "         .collect::<Result<Vec<_>, io::Error>>()?;"] # [doc = ""] # [doc = "     // The order in which `read_dir` returns entries is not guaranteed. If reproducible"] # [doc = "     // ordering is required the entries should be explicitly sorted."] # [doc = ""] # [doc = "     entries.sort();"] # [doc = ""] # [doc = "     // The entries have now been sorted by their path."] # [doc = ""] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc (alias = "ls" , alias = "opendir" , alias = "FindFirstFile" , alias = "FindNextFile")] # [stable (feature = "rust1" , since = "1.0.0")] pub fn read_dir < P : AsRef < Path > > (path : P) -> io :: Result < ReadDir > { fs_imp :: read_dir (path . as_ref ()) . map (ReadDir) }
};
}
