macro_rules! deps {
    () => {
        EitherOrBoth!();
    };
}

macro_rules! impl_148 {
    () => {
        deps!();
        impl < A , B > EitherOrBoth < A , B > { # [doc = " If `Left`, or `Both`, return true. Otherwise, return false."] pub fn has_left (& self) -> bool { self . as_ref () . left () . is_some () } # [doc = " If `Right`, or `Both`, return true, otherwise, return false."] pub fn has_right (& self) -> bool { self . as_ref () . right () . is_some () } # [doc = " If `Left`, return true. Otherwise, return false."] # [doc = " Exclusive version of [`has_left`](EitherOrBoth::has_left)."] pub fn is_left (& self) -> bool { matches ! (self , Left (_)) } # [doc = " If `Right`, return true. Otherwise, return false."] # [doc = " Exclusive version of [`has_right`](EitherOrBoth::has_right)."] pub fn is_right (& self) -> bool { matches ! (self , Right (_)) } # [doc = " If `Both`, return true. Otherwise, return false."] pub fn is_both (& self) -> bool { self . as_ref () . both () . is_some () } # [doc = " If `Left`, or `Both`, return `Some` with the left value. Otherwise, return `None`."] pub fn left (self) -> Option < A > { match self { Left (left) | Both (left , _) => Some (left) , _ => None , } } # [doc = " If `Right`, or `Both`, return `Some` with the right value. Otherwise, return `None`."] pub fn right (self) -> Option < B > { match self { Right (right) | Both (_ , right) => Some (right) , _ => None , } } # [doc = " Return tuple of options corresponding to the left and right value respectively"] # [doc = ""] # [doc = " If `Left` return `(Some(..), None)`, if `Right` return `(None,Some(..))`, else return"] # [doc = " `(Some(..),Some(..))`"] pub fn left_and_right (self) -> (Option < A > , Option < B >) { self . map_any (Some , Some) . or_default () } # [doc = " If `Left`, return `Some` with the left value. If `Right` or `Both`, return `None`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " // On the `Left` variant."] # [doc = " # use itertools::{EitherOrBoth, EitherOrBoth::{Left, Right, Both}};"] # [doc = " let x: EitherOrBoth<_, ()> = Left(\"bonjour\");"] # [doc = " assert_eq!(x.just_left(), Some(\"bonjour\"));"] # [doc = ""] # [doc = " // On the `Right` variant."] # [doc = " let x: EitherOrBoth<(), _> = Right(\"hola\");"] # [doc = " assert_eq!(x.just_left(), None);"] # [doc = ""] # [doc = " // On the `Both` variant."] # [doc = " let x = Both(\"bonjour\", \"hola\");"] # [doc = " assert_eq!(x.just_left(), None);"] # [doc = " ```"] pub fn just_left (self) -> Option < A > { match self { Left (left) => Some (left) , _ => None , } } # [doc = " If `Right`, return `Some` with the right value. If `Left` or `Both`, return `None`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " // On the `Left` variant."] # [doc = " # use itertools::{EitherOrBoth::{Left, Right, Both}, EitherOrBoth};"] # [doc = " let x: EitherOrBoth<_, ()> = Left(\"auf wiedersehen\");"] # [doc = " assert_eq!(x.just_left(), Some(\"auf wiedersehen\"));"] # [doc = ""] # [doc = " // On the `Right` variant."] # [doc = " let x: EitherOrBoth<(), _> = Right(\"adios\");"] # [doc = " assert_eq!(x.just_left(), None);"] # [doc = ""] # [doc = " // On the `Both` variant."] # [doc = " let x = Both(\"auf wiedersehen\", \"adios\");"] # [doc = " assert_eq!(x.just_left(), None);"] # [doc = " ```"] pub fn just_right (self) -> Option < B > { match self { Right (right) => Some (right) , _ => None , } } # [doc = " If `Both`, return `Some` containing the left and right values. Otherwise, return `None`."] pub fn both (self) -> Option < (A , B) > { match self { Both (a , b) => Some ((a , b)) , _ => None , } } # [doc = " If `Left` or `Both`, return the left value. Otherwise, convert the right value and return it."] pub fn into_left (self) -> A where B : Into < A > , { match self { Left (a) | Both (a , _) => a , Right (b) => b . into () , } } # [doc = " If `Right` or `Both`, return the right value. Otherwise, convert the left value and return it."] pub fn into_right (self) -> B where A : Into < B > , { match self { Right (b) | Both (_ , b) => b , Left (a) => a . into () , } } # [doc = " Converts from `&EitherOrBoth<A, B>` to `EitherOrBoth<&A, &B>`."] pub fn as_ref (& self) -> EitherOrBoth < & A , & B > { match * self { Left (ref left) => Left (left) , Right (ref right) => Right (right) , Both (ref left , ref right) => Both (left , right) , } } # [doc = " Converts from `&mut EitherOrBoth<A, B>` to `EitherOrBoth<&mut A, &mut B>`."] pub fn as_mut (& mut self) -> EitherOrBoth < & mut A , & mut B > { match * self { Left (ref mut left) => Left (left) , Right (ref mut right) => Right (right) , Both (ref mut left , ref mut right) => Both (left , right) , } } # [doc = " Converts from `&EitherOrBoth<A, B>` to `EitherOrBoth<&_, &_>` using the [`Deref`] trait."] pub fn as_deref (& self) -> EitherOrBoth < & A :: Target , & B :: Target > where A : Deref , B : Deref , { match * self { Left (ref left) => Left (left) , Right (ref right) => Right (right) , Both (ref left , ref right) => Both (left , right) , } } # [doc = " Converts from `&mut EitherOrBoth<A, B>` to `EitherOrBoth<&mut _, &mut _>` using the [`DerefMut`] trait."] pub fn as_deref_mut (& mut self) -> EitherOrBoth < & mut A :: Target , & mut B :: Target > where A : DerefMut , B : DerefMut , { match * self { Left (ref mut left) => Left (left) , Right (ref mut right) => Right (right) , Both (ref mut left , ref mut right) => Both (left , right) , } } # [doc = " Convert `EitherOrBoth<A, B>` to `EitherOrBoth<B, A>`."] pub fn flip (self) -> EitherOrBoth < B , A > { match self { Left (a) => Right (a) , Right (b) => Left (b) , Both (a , b) => Both (b , a) , } } # [doc = " Apply the function `f` on the value `a` in `Left(a)` or `Both(a, b)` variants. If it is"] # [doc = " present rewrapping the result in `self`'s original variant."] pub fn map_left < F , M > (self , f : F) -> EitherOrBoth < M , B > where F : FnOnce (A) -> M , { match self { Both (a , b) => Both (f (a) , b) , Left (a) => Left (f (a)) , Right (b) => Right (b) , } } # [doc = " Apply the function `f` on the value `b` in `Right(b)` or `Both(a, b)` variants."] # [doc = " If it is present rewrapping the result in `self`'s original variant."] pub fn map_right < F , M > (self , f : F) -> EitherOrBoth < A , M > where F : FnOnce (B) -> M , { match self { Left (a) => Left (a) , Right (b) => Right (f (b)) , Both (a , b) => Both (a , f (b)) , } } # [doc = " Apply the functions `f` and `g` on the value `a` and `b` respectively;"] # [doc = " found in `Left(a)`, `Right(b)`, or `Both(a, b)` variants."] # [doc = " The Result is rewrapped `self`'s original variant."] pub fn map_any < F , L , G , R > (self , f : F , g : G) -> EitherOrBoth < L , R > where F : FnOnce (A) -> L , G : FnOnce (B) -> R , { match self { Left (a) => Left (f (a)) , Right (b) => Right (g (b)) , Both (a , b) => Both (f (a) , g (b)) , } } # [doc = " Apply the function `f` on the value `a` in `Left(a)` or `Both(a, _)` variants if it is"] # [doc = " present."] pub fn left_and_then < F , L > (self , f : F) -> EitherOrBoth < L , B > where F : FnOnce (A) -> EitherOrBoth < L , B > , { match self { Left (a) | Both (a , _) => f (a) , Right (b) => Right (b) , } } # [doc = " Apply the function `f` on the value `b`"] # [doc = " in `Right(b)` or `Both(_, b)` variants if it is present."] pub fn right_and_then < F , R > (self , f : F) -> EitherOrBoth < A , R > where F : FnOnce (B) -> EitherOrBoth < A , R > , { match self { Left (a) => Left (a) , Right (b) | Both (_ , b) => f (b) , } } # [doc = " Returns a tuple consisting of the `l` and `r` in `Both(l, r)`, if present."] # [doc = " Otherwise, returns the wrapped value for the present element, and the supplied"] # [doc = " value for the other. The first (`l`) argument is used for a missing `Left`"] # [doc = " value. The second (`r`) argument is used for a missing `Right` value."] # [doc = ""] # [doc = " Arguments passed to `or` are eagerly evaluated; if you are passing"] # [doc = " the result of a function call, it is recommended to use [`or_else`],"] # [doc = " which is lazily evaluated."] # [doc = ""] # [doc = " [`or_else`]: EitherOrBoth::or_else"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use itertools::EitherOrBoth;"] # [doc = " assert_eq!(EitherOrBoth::Both(\"tree\", 1).or(\"stone\", 5), (\"tree\", 1));"] # [doc = " assert_eq!(EitherOrBoth::Left(\"tree\").or(\"stone\", 5), (\"tree\", 5));"] # [doc = " assert_eq!(EitherOrBoth::Right(1).or(\"stone\", 5), (\"stone\", 1));"] # [doc = " ```"] pub fn or (self , l : A , r : B) -> (A , B) { match self { Left (inner_l) => (inner_l , r) , Right (inner_r) => (l , inner_r) , Both (inner_l , inner_r) => (inner_l , inner_r) , } } # [doc = " Returns a tuple consisting of the `l` and `r` in `Both(l, r)`, if present."] # [doc = " Otherwise, returns the wrapped value for the present element, and the [`default`](Default::default)"] # [doc = " for the other."] pub fn or_default (self) -> (A , B) where A : Default , B : Default , { match self { Left (l) => (l , B :: default ()) , Right (r) => (A :: default () , r) , Both (l , r) => (l , r) , } } # [doc = " Returns a tuple consisting of the `l` and `r` in `Both(l, r)`, if present."] # [doc = " Otherwise, returns the wrapped value for the present element, and computes the"] # [doc = " missing value with the supplied closure. The first argument (`l`) is used for a"] # [doc = " missing `Left` value. The second argument (`r`) is used for a missing `Right` value."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use itertools::EitherOrBoth;"] # [doc = " let k = 10;"] # [doc = " assert_eq!(EitherOrBoth::Both(\"tree\", 1).or_else(|| \"stone\", || 2 * k), (\"tree\", 1));"] # [doc = " assert_eq!(EitherOrBoth::Left(\"tree\").or_else(|| \"stone\", || 2 * k), (\"tree\", 20));"] # [doc = " assert_eq!(EitherOrBoth::Right(1).or_else(|| \"stone\", || 2 * k), (\"stone\", 1));"] # [doc = " ```"] pub fn or_else < L : FnOnce () -> A , R : FnOnce () -> B > (self , l : L , r : R) -> (A , B) { match self { Left (inner_l) => (inner_l , r ()) , Right (inner_r) => (l () , inner_r) , Both (inner_l , inner_r) => (inner_l , inner_r) , } } # [doc = " Returns a mutable reference to the left value. If the left value is not present,"] # [doc = " it is replaced with `val`."] pub fn left_or_insert (& mut self , val : A) -> & mut A { self . left_or_insert_with (| | val) } # [doc = " Returns a mutable reference to the right value. If the right value is not present,"] # [doc = " it is replaced with `val`."] pub fn right_or_insert (& mut self , val : B) -> & mut B { self . right_or_insert_with (| | val) } # [doc = " If the left value is not present, replace it the value computed by the closure `f`."] # [doc = " Returns a mutable reference to the now-present left value."] pub fn left_or_insert_with < F > (& mut self , f : F) -> & mut A where F : FnOnce () -> A , { match self { Left (left) | Both (left , _) => left , Right (_) => self . insert_left (f ()) , } } # [doc = " If the right value is not present, replace it the value computed by the closure `f`."] # [doc = " Returns a mutable reference to the now-present right value."] pub fn right_or_insert_with < F > (& mut self , f : F) -> & mut B where F : FnOnce () -> B , { match self { Right (right) | Both (_ , right) => right , Left (_) => self . insert_right (f ()) , } } # [doc = " Sets the `left` value of this instance, and returns a mutable reference to it."] # [doc = " Does not affect the `right` value."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # use itertools::{EitherOrBoth, EitherOrBoth::{Left, Right, Both}};"] # [doc = ""] # [doc = " // Overwriting a pre-existing value."] # [doc = " let mut either: EitherOrBoth<_, ()> = Left(0_u32);"] # [doc = " assert_eq!(*either.insert_left(69), 69);"] # [doc = ""] # [doc = " // Inserting a second value."] # [doc = " let mut either = Right(\"no\");"] # [doc = " assert_eq!(*either.insert_left(\"yes\"), \"yes\");"] # [doc = " assert_eq!(either, Both(\"yes\", \"no\"));"] # [doc = " ```"] pub fn insert_left (& mut self , val : A) -> & mut A { match self { Left (left) | Both (left , _) => { * left = val ; left } Right (right) => { unsafe { let right = std :: ptr :: read (right as * mut _) ; std :: ptr :: write (self as * mut _ , Both (val , right)) ; } if let Both (left , _) = self { left } else { unsafe { std :: hint :: unreachable_unchecked () } } } } } # [doc = " Sets the `right` value of this instance, and returns a mutable reference to it."] # [doc = " Does not affect the `left` value."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # use itertools::{EitherOrBoth, EitherOrBoth::{Left, Both}};"] # [doc = " // Overwriting a pre-existing value."] # [doc = " let mut either: EitherOrBoth<_, ()> = Left(0_u32);"] # [doc = " assert_eq!(*either.insert_left(69), 69);"] # [doc = ""] # [doc = " // Inserting a second value."] # [doc = " let mut either = Left(\"what's\");"] # [doc = " assert_eq!(*either.insert_right(9 + 10), 21 - 2);"] # [doc = " assert_eq!(either, Both(\"what's\", 9 + 10));"] # [doc = " ```"] pub fn insert_right (& mut self , val : B) -> & mut B { match self { Right (right) | Both (_ , right) => { * right = val ; right } Left (left) => { unsafe { let left = std :: ptr :: read (left as * mut _) ; std :: ptr :: write (self as * mut _ , Both (left , val)) ; } if let Both (_ , right) = self { right } else { unsafe { std :: hint :: unreachable_unchecked () } } } } } # [doc = " Set `self` to `Both(..)`, containing the specified left and right values,"] # [doc = " and returns a mutable reference to those values."] pub fn insert_both (& mut self , left : A , right : B) -> (& mut A , & mut B) { * self = Both (left , right) ; if let Both (left , right) = self { (left , right) } else { unsafe { std :: hint :: unreachable_unchecked () } } } }
    };
}

impl_148!()