// Generated macro for impl_426 (impl)
macro_rules! Depcrate_weekimpl_426 {
() => {
// Module: crate::week
// Provides: {"impl_426"}
// Dependencies: {}
impl UnitInfo { # [doc = " Creates a UnitInfo for a given year or month."] fn new (first_day : Weekday , duration_days : u16) -> Result < UnitInfo , RangeError > { if duration_days < MIN_UNIT_DAYS { return Err (RangeError { field : "num_days_in_unit" , value : duration_days as i32 , min : MIN_UNIT_DAYS as i32 , max : i32 :: MAX , }) ; } Ok (UnitInfo { first_day , duration_days , }) } # [doc = " Returns the start of this unit's first week."] # [doc = ""] # [doc = " The returned value can be negative if this unit's first week started during the previous"] # [doc = " unit."] fn first_week_offset (self , calendar : WeekCalculator) -> i8 { let first_day_index = calendar . weekday_index (self . first_day) ; if 7 - first_day_index >= calendar . min_week_days as i8 { - first_day_index } else { 7 - first_day_index } } # [doc = " Returns the number of weeks in this unit according to `calendar`."] fn num_weeks (self , calendar : WeekCalculator) -> u8 { let first_week_offset = self . first_week_offset (calendar) ; let num_days_including_first_week = (self . duration_days as i32) - (first_week_offset as i32) ; debug_assert ! (num_days_including_first_week >= 0 , "Unit is shorter than a week.") ; ((num_days_including_first_week + 7 - (calendar . min_week_days as i32)) / 7) as u8 } # [doc = " Returns the week number for the given day in this unit."] fn relative_week (self , calendar : WeekCalculator , day : u16) -> RelativeWeek { let days_since_first_week = i32 :: from (day) - i32 :: from (self . first_week_offset (calendar)) - 1 ; if days_since_first_week < 0 { return RelativeWeek :: LastWeekOfPreviousUnit ; } let week_number = (1 + days_since_first_week / 7) as u8 ; if week_number > self . num_weeks (calendar) { return RelativeWeek :: FirstWeekOfNextUnit ; } RelativeWeek :: WeekOfCurrentUnit (week_number) } }
};
}
