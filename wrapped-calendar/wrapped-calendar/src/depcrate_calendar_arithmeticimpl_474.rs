// Generated macro for impl_474 (impl)
macro_rules! Depcrate_calendar_arithmeticimpl_474 {
() => {
// Module: crate::calendar_arithmetic
// Provides: {"impl_474"}
// Dependencies: {}
impl < C : DateFieldsResolver > ArithmeticDate < C > { pub (crate) fn year (self) -> C :: YearInfo { C :: YearInfo :: unpack_year (self . 0) } pub (crate) fn month (self) -> u8 { C :: YearInfo :: unpack_month (self . 0) } pub (crate) fn day (self) -> u8 { C :: YearInfo :: unpack_day (self . 0) } # [inline] pub (crate) fn new_unchecked (year : C :: YearInfo , month : u8 , day : u8) -> Self { ArithmeticDate (C :: YearInfo :: pack (year , month , day)) } pub (crate) fn cast < C2 : DateFieldsResolver < YearInfo = C :: YearInfo > > (self) -> ArithmeticDate < C2 > { ArithmeticDate :: new_unchecked (self . year () , self . month () , self . day ()) } pub (crate) fn from_codes (era : Option < & str > , year : i32 , month_code : types :: MonthCode , day : u8 , calendar : & C ,) -> Result < Self , DateError > { let year = range_check (year , "year" , VALID_YEAR_RANGE) ? ; let year = if let Some (era) = era { calendar . year_info_from_era (era . as_bytes () , year) ? } else { calendar . year_info_from_extended (year) } ; let validated = Month :: try_from_utf8 (month_code . 0 . as_bytes ()) . map_err (| e | match e { MonthCodeParseError :: InvalidSyntax => DateError :: UnknownMonthCode (month_code) , }) ? ; let month = calendar . ordinal_from_month (year , validated , Default :: default ()) . map_err (| e | match e { MonthCodeError :: NotInCalendar | MonthCodeError :: NotInYear => { DateError :: UnknownMonthCode (month_code) } }) ? ; let day = range_check (day , "day" , 1 ..= C :: days_in_provided_month (year , month)) ? ; Ok (ArithmeticDate :: new_unchecked (year , month , day)) } pub (crate) fn from_fields (fields : DateFields , options : DateFromFieldsOptions , calendar : & C ,) -> Result < Self , DateFromFieldsError > { let missing_fields_strategy = options . missing_fields_strategy . unwrap_or_default () ; let day = match fields . day { Some (day) => day , None => match missing_fields_strategy { MissingFieldsStrategy :: Reject => return Err (DateFromFieldsError :: NotEnoughFields) , MissingFieldsStrategy :: Ecma => { if fields . extended_year . is_some () || fields . era_year . is_some () { 1 } else { return Err (DateFromFieldsError :: NotEnoughFields) ; } } } , } ; if fields . month_code . is_none () && fields . ordinal_month . is_none () { return Err (DateFromFieldsError :: NotEnoughFields) ; } let mut valid_month_code = None ; let year = match (fields . era , fields . era_year) { (None , None) => match fields . extended_year { Some (extended_year) => calendar . year_info_from_extended (range_check (extended_year , "year" , VALID_YEAR_RANGE ,) ?) , None => match missing_fields_strategy { MissingFieldsStrategy :: Reject => { return Err (DateFromFieldsError :: NotEnoughFields) } MissingFieldsStrategy :: Ecma => { match (fields . month_code , fields . ordinal_month) { (Some (month_code) , None) => { let validated = Month :: try_from_utf8 (month_code) ? ; valid_month_code = Some (validated) ; calendar . reference_year_from_month_day (validated , day) ? } _ => return Err (DateFromFieldsError :: NotEnoughFields) , } } } , } , (Some (era) , Some (era_year)) => { let era_year_as_year_info = calendar . year_info_from_era (era , range_check (era_year , "year" , VALID_YEAR_RANGE) ?) ? ; if let Some (extended_year) = fields . extended_year { if era_year_as_year_info != calendar . year_info_from_extended (range_check (extended_year , "year" , VALID_YEAR_RANGE ,) ?) { return Err (DateFromFieldsError :: InconsistentYear) ; } } era_year_as_year_info } (Some (_) , None) | (None , Some (_)) => return Err (DateFromFieldsError :: NotEnoughFields) , } ; let month = match fields . month_code { Some (month_code) => { let validated = match valid_month_code { Some (validated) => validated , None => Month :: try_from_utf8 (month_code) ? , } ; let computed_month = calendar . ordinal_from_month (year , validated , options) ? ; if let Some (ordinal_month) = fields . ordinal_month { if computed_month != ordinal_month { return Err (DateFromFieldsError :: InconsistentMonth) ; } } computed_month } None => match fields . ordinal_month { Some (month) => month , None => { debug_assert ! (false , "Already checked above") ; return Err (DateFromFieldsError :: NotEnoughFields) ; } } , } ; let constrained_month = range_check_with_overflow (month , "month" , 1 ..= C :: months_in_provided_year (year) , options . overflow . unwrap_or_default () ,) ? ; let day = range_check_with_overflow (day , "day" , 1 ..= C :: days_in_provided_month (year , constrained_month) , options . overflow . unwrap_or_default () ,) ? ; Ok (Self :: new_unchecked (year , constrained_month , day)) } pub (crate) fn try_from_ymd (year : C :: YearInfo , month : u8 , day : u8) -> Result < Self , RangeError > { range_check (year . to_extended_year () , "year" , VALID_YEAR_RANGE) ? ; range_check (month , "month" , 1 ..= C :: months_in_provided_year (year)) ? ; range_check (day , "day" , 1 ..= C :: days_in_provided_month (year , month)) ? ; Ok (ArithmeticDate :: new_unchecked (year , month , day)) } # [doc = " Implements the Temporal abstract operation BalanceNonISODate."] # [doc = ""] # [doc = " This takes a year, month, and day, where the month and day might be out of range, then"] # [doc = " balances excess months into the year field and excess days into the month field."] pub (crate) fn new_balanced (year : C :: YearInfo , ordinal_month : i64 , day : i64 , cal : & C) -> Self { let mut resolved_year = year ; let mut resolved_month = ordinal_month ; let mut months_in_year = C :: months_in_provided_year (resolved_year) ; while resolved_month <= 0 { resolved_year = cal . year_info_from_extended (resolved_year . to_extended_year () - 1) ; months_in_year = C :: months_in_provided_year (resolved_year) ; resolved_month += i64 :: from (months_in_year) ; } while resolved_month > i64 :: from (months_in_year) { resolved_month -= i64 :: from (months_in_year) ; resolved_year = cal . year_info_from_extended (resolved_year . to_extended_year () + 1) ; months_in_year = C :: months_in_provided_year (resolved_year) ; } debug_assert ! (u8 :: try_from (resolved_month) . is_ok ()) ; let mut resolved_month = resolved_month as u8 ; let mut resolved_day = day ; let mut days_in_month = C :: days_in_provided_month (resolved_year , resolved_month) ; while resolved_day <= 0 { resolved_month -= 1 ; if resolved_month == 0 { resolved_year = cal . year_info_from_extended (resolved_year . to_extended_year () - 1) ; months_in_year = C :: months_in_provided_year (resolved_year) ; resolved_month = months_in_year ; } days_in_month = C :: days_in_provided_month (resolved_year , resolved_month) ; resolved_day += i64 :: from (days_in_month) ; } while resolved_day > i64 :: from (days_in_month) { resolved_day -= i64 :: from (days_in_month) ; resolved_month += 1 ; if resolved_month > months_in_year { resolved_year = cal . year_info_from_extended (resolved_year . to_extended_year () + 1) ; months_in_year = C :: months_in_provided_year (resolved_year) ; resolved_month = 1 ; } days_in_month = C :: days_in_provided_month (resolved_year , resolved_month) ; } debug_assert ! (u8 :: try_from (resolved_day) . is_ok ()) ; let resolved_day = resolved_day as u8 ; Self :: new_unchecked (resolved_year , resolved_month , resolved_day) } # [doc = " Implements the Temporal abstract operation NonISODateSurpasses."] # [doc = ""] # [doc = " This takes two dates (`self` and `other`), `duration`, and `sign` (either -1 or 1), then"] # [doc = " returns whether adding the duration to `self` results in a year/month/day that exceeds"] # [doc = " `other` in the direction indicated by `sign`, constraining the month but not the day."] pub (crate) fn surpasses (& self , other : & Self , duration : DateDuration , sign : i64 , cal : & C ,) -> bool { let y0 = cal . year_info_from_extended (duration . add_years_to (self . year () . to_extended_year ())) ; let base_month = cal . month_from_ordinal (self . year () , self . month ()) ; let constrain = DateFromFieldsOptions { overflow : Some (Overflow :: Constrain) , .. Default :: default () } ; let m0_result = cal . ordinal_from_month (y0 , base_month , constrain) ; let m0 = match m0_result { Ok (m0) => m0 , Err (_) => { debug_assert ! (false , "valid month code for calendar, and constrained to the year") ; 1 } } ; let end_of_month = Self :: new_balanced (y0 , duration . add_months_to (m0) + 1 , 0 , cal) ; let base_day = self . day () ; let y1 ; let m1 ; let d1 ; if duration . weeks != 0 || duration . days != 0 { let regulated_day = if base_day < end_of_month . day () { base_day } else { end_of_month . day () } ; let balanced_date = Self :: new_balanced (end_of_month . year () , i64 :: from (end_of_month . month ()) , duration . add_weeks_and_days_to (regulated_day) , cal ,) ; y1 = balanced_date . year () ; m1 = balanced_date . month () ; d1 = balanced_date . day () ; } else { y1 = end_of_month . year () ; m1 = end_of_month . month () ; d1 = base_day ; } # [allow (clippy :: collapsible_if)] if y1 != other . year () { if sign * (i64 :: from (y1 . to_extended_year ()) - i64 :: from (other . year () . to_extended_year ())) > 0 { return true ; } } else if m1 != other . month () { if sign * (i64 :: from (m1) - i64 :: from (other . month ())) > 0 { return true ; } } else if d1 != other . day () { if sign * (i64 :: from (d1) - i64 :: from (other . day ())) > 0 { return true ; } } false } # [doc = " Implements the Temporal abstract operation NonISODateAdd."] # [doc = ""] # [doc = " This takes a date (`self`) and `duration`, then returns a new date resulting from"] # [doc = " adding `duration` to `self`, constrained according to `options`."] pub (crate) fn added (& self , duration : DateDuration , cal : & C , options : DateAddOptions ,) -> Result < Self , DateError > { let y0 = cal . year_info_from_extended (duration . add_years_to (self . year () . to_extended_year ())) ; let base_month = cal . month_from_ordinal (self . year () , self . month ()) ; let m0 = cal . ordinal_from_month (y0 , base_month , DateFromFieldsOptions :: from_add_options (options) ,) . map_err (| e | { match e { MonthCodeError :: NotInCalendar => DateError :: UnknownMonthCode (base_month . code ()) , MonthCodeError :: NotInYear => DateError :: UnknownMonthCode (base_month . code ()) , } }) ? ; let end_of_month = Self :: new_balanced (y0 , duration . add_months_to (m0) + 1 , 0 , cal) ; let base_day = self . day () ; let regulated_day = if base_day < end_of_month . day () { base_day } else { if matches ! (options . overflow , Some (Overflow :: Reject)) { return Err (DateError :: Range { field : "day" , value : i32 :: from (base_day) , min : 1 , max : i32 :: from (end_of_month . day ()) , }) ; } end_of_month . day () } ; Ok (Self :: new_balanced (end_of_month . year () , i64 :: from (end_of_month . month ()) , duration . add_weeks_and_days_to (regulated_day) , cal ,)) } # [doc = " Implements the Temporal abstract operation NonISODateUntil."] # [doc = ""] # [doc = " This takes a duration (`self`) and a date (`other`), then returns a duration that, when"] # [doc = " added to `self`, results in `other`, with largest unit according to `options`."] pub (crate) fn until (& self , other : & Self , cal : & C , options : DateDifferenceOptions ,) -> DateDuration { let sign = match other . cmp (self) { Ordering :: Greater => 1i64 , Ordering :: Equal => return DateDuration :: default () , Ordering :: Less => - 1i64 , } ; let mut years = 0 ; if matches ! (options . largest_unit , Some (DateDurationUnit :: Years)) { let mut candidate_years = sign ; while ! self . surpasses (other , DateDuration :: from_signed_ymwd (candidate_years , 0 , 0 , 0) , sign , cal ,) { years = candidate_years ; candidate_years += sign ; } } let mut months = 0 ; if matches ! (options . largest_unit , Some (DateDurationUnit :: Years) | Some (DateDurationUnit :: Months)) { let mut candidate_months = sign ; while ! self . surpasses (other , DateDuration :: from_signed_ymwd (years , candidate_months , 0 , 0) , sign , cal ,) { months = candidate_months ; candidate_months += sign ; } } let mut weeks = 0 ; if matches ! (options . largest_unit , Some (DateDurationUnit :: Weeks)) { let mut candidate_weeks = sign ; while ! self . surpasses (other , DateDuration :: from_signed_ymwd (years , months , candidate_weeks , 0) , sign , cal ,) { weeks = candidate_weeks ; candidate_weeks += sign ; } } let mut days = 0 ; let mut candidate_days = sign ; while ! self . surpasses (other , DateDuration :: from_signed_ymwd (years , months , weeks , candidate_days) , sign , cal ,) { days = candidate_days ; candidate_days += sign ; } DateDuration :: from_signed_ymwd (years , months , weeks , days) } }
};
}
