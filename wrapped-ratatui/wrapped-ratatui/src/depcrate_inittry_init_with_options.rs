// Generated macro for try_init_with_options (function)
macro_rules! Depcrate_inittry_init_with_options {
() => {
// Module: crate::init
// Provides: {"try_init_with_options"}
// Dependencies: {}
# [doc = " Try to initialize a terminal with the given options and reasonable defaults."] # [doc = ""] # [doc = " This function allows the caller to specify a custom [`Viewport`] via the [`TerminalOptions`]. It"] # [doc = " will attempt to create a [`DefaultTerminal`] and initialize it with the given options and the"] # [doc = " following defaults:"] # [doc = ""] # [doc = " [`Viewport`]: crate::Viewport"] # [doc = ""] # [doc = " - Raw mode is enabled"] # [doc = " - A panic hook is installed that restores the terminal before panicking."] # [doc = ""] # [doc = " Unlike [`try_init`], this function does not enter the alternate screen buffer as this may not be"] # [doc = " desired in all cases. If you need the alternate screen buffer, you should enable it manually"] # [doc = " after calling this function."] # [doc = ""] # [doc = " If any of these steps fail, the error is returned."] # [doc = ""] # [doc = " Ensure that this method is called *after* your app installs any other panic hooks to ensure the"] # [doc = " terminal is restored before the other hooks are called."] # [doc = ""] # [doc = " Generally, you should use [`init_with_options`] instead of this function, as the panic hook"] # [doc = " installed by this function will ensure that any failures during initialization will restore the"] # [doc = " terminal before panicking. This function is provided for cases where you need to handle the"] # [doc = " error yourself."] # [doc = ""] # [doc = " See the [module-level documentation](mod@crate::init) for a comparison of all initialization"] # [doc = " functions and guidance on when to use each one."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use ratatui::{TerminalOptions, Viewport};"] # [doc = ""] # [doc = " let options = TerminalOptions {"] # [doc = "     viewport: Viewport::Inline(5),"] # [doc = " };"] # [doc = " let terminal = ratatui::try_init_with_options(options)?;"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] pub fn try_init_with_options (options : TerminalOptions) -> io :: Result < DefaultTerminal > { set_panic_hook () ; enable_raw_mode () ? ; let backend = CrosstermBackend :: new (stdout ()) ; Terminal :: with_options (backend , options) }
};
}
