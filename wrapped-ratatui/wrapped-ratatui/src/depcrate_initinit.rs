// Generated macro for init (function)
macro_rules! Depcrate_initinit {
() => {
// Module: crate::init
// Provides: {"init"}
// Dependencies: {}
# [doc = " Initialize a terminal with reasonable defaults for most applications."] # [doc = ""] # [doc = " This will create a new [`DefaultTerminal`] and initialize it with the following defaults:"] # [doc = ""] # [doc = " - Backend: [`CrosstermBackend`] writing to [`Stdout`]"] # [doc = " - Raw mode is enabled"] # [doc = " - Alternate screen buffer enabled"] # [doc = " - A panic hook is installed that restores the terminal before panicking. Ensure that this method"] # [doc = "   is called after any other panic hooks that may be installed to ensure that the terminal is"] # [doc = "   restored before those hooks are called."] # [doc = ""] # [doc = " For more control over the terminal initialization, use [`Terminal::new`] or"] # [doc = " [`Terminal::with_options`]."] # [doc = ""] # [doc = " Ensure that this method is called *after* your app installs any other panic hooks to ensure the"] # [doc = " terminal is restored before the other hooks are called."] # [doc = ""] # [doc = " Generally, use this function instead of [`try_init`] to ensure that the terminal is restored"] # [doc = " correctly if any of the initialization steps fail. If you need to handle the error yourself, use"] # [doc = " [`try_init`] instead."] # [doc = ""] # [doc = " See the [module-level documentation](mod@crate::init) for a comparison of all initialization"] # [doc = " functions and guidance on when to use each one."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if any of the following steps fail:"] # [doc = ""] # [doc = " - Enabling raw mode"] # [doc = " - Entering the alternate screen buffer"] # [doc = " - Creating the terminal fails due to being unable to calculate the terminal size"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " let terminal = ratatui::init();"] # [doc = " ```"] pub fn init () -> DefaultTerminal { try_init () . expect ("failed to initialize terminal") }
};
}
