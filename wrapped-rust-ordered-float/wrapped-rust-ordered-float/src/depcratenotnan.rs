// Generated macro for NotNan (struct)
macro_rules! DepcrateNotNan {
() => {
// Module: crate
// Provides: {"NotNan"}
// Dependencies: {}
# [doc = " A wrapper around floats providing an implementation of `Eq`, `Ord` and `Hash`."] # [doc = ""] # [doc = " A NaN value cannot be stored in this type."] # [doc = ""] # [doc = " ```"] # [doc = " use ordered_float::NotNan;"] # [doc = ""] # [doc = " let mut v = [NotNan::new(2.0).unwrap(), NotNan::new(1.0).unwrap()];"] # [doc = " v.sort();"] # [doc = " assert_eq!(v, [1.0, 2.0]);"] # [doc = " ```"] # [doc = ""] # [doc = " Because `NotNan` implements `Ord` and `Eq`, it can be used as a key in a `HashSet`,"] # [doc = " `HashMap`, `BTreeMap`, or `BTreeSet` (unlike the primitive `f32` or `f64` types):"] # [doc = ""] # [doc = " ```"] # [doc = " # use ordered_float::NotNan;"] # [doc = " # use std::collections::HashSet;"] # [doc = " let mut s: HashSet<NotNan<f32>> = HashSet::new();"] # [doc = " let key = NotNan::new(1.0).unwrap();"] # [doc = " s.insert(key);"] # [doc = " assert!(s.contains(&key));"] # [doc = " ```"] # [doc = ""] # [doc = " `-0.0` and `+0.0` are still considered equal. This different sign may show up in printing,"] # [doc = " or when dividing by zero (the sign of the zero becomes the sign of the resulting infinity)."] # [doc = " Therefore, `NotNan` may be unsuitable for use as a key in interning and memoization"] # [doc = " applications which require equal results from equal inputs, unless signed zeros make no"] # [doc = " difference or are canonicalized before insertion."] # [doc = ""] # [doc = " Arithmetic on NotNan values will panic if it produces a NaN value:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = " # use ordered_float::NotNan;"] # [doc = " let a = NotNan::new(std::f32::INFINITY).unwrap();"] # [doc = " let b = NotNan::new(std::f32::NEG_INFINITY).unwrap();"] # [doc = ""] # [doc = " // This will panic:"] # [doc = " let c = a + b;"] # [doc = " ```"] # [doc = ""] # [doc = " # Representation"] # [doc = ""] # [doc = " `NotNan` has `#[repr(transparent)]`, so it is sound to use"] # [doc = " [transmute](core::mem::transmute) or pointer casts to convert between any type `T` and"] # [doc = " `NotNan<T>`, as long as this does not create a NaN value."] # [doc = " However, consider using [`bytemuck`] as a safe alternative if possible."] # [cfg_attr (not (feature = "bytemuck") , doc = "[`bytemuck`]: https://docs.rs/bytemuck/1/")] # [derive (PartialOrd , PartialEq , Default , Clone , Copy)] # [repr (transparent)] pub struct NotNan < T > (T) ;
};
}
