// Generated macro for Logger (trait)
macro_rules! Depcrate_traitsLogger {
() => {
// Module: crate::traits
// Provides: {"Logger"}
// Dependencies: {}
# [doc = " Global logger acquire-release mechanism"] # [doc = ""] # [doc = " This trait's methods will be called by the defmt logging macros to transmit the"] # [doc = " encoded log data over the wire. The call order is:"] # [doc = " - One `acquire()` call to start the log frame."] # [doc = " - Multiple `write()` calls, with fragments of the log frame data each."] # [doc = " - One `release()` call."] # [doc = ""] # [doc = " The data passed to `write()` is *unencoded*. Implementations MUST encode it with `Encoder`"] # [doc = " prior to sending it over the wire. The simplest way is for `acquire()` to call `Encoder::start_frame()`,"] # [doc = " `write()` to call `Encoder::write()`, and `release()` to call `Encoder::end_frame()`."] # [doc = ""] # [doc = " The global logger can be acquired once for each \"execution context\". The definition"] # [doc = " of execution context is up to the implementation. For example, it can be:"] # [doc = ""] # [doc = " - the entire process."] # [doc = " - one thread in std environments."] # [doc = " - one interrupt priority level in embedded devices."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " - `acquire` logically acquires the global logger in the current execution context."] # [doc = "   The acquiring is tracked internally, no Rust object is returned representing ownership."] # [doc = " - `acquire` is a safe function, therefore it must be thread-safe and interrupt-safe"] # [doc = ""] # [doc = " And, not safety related, the methods should never be invoked from user code. The easiest way to"] # [doc = " ensure this is to implement `Logger` on a *private* `struct` and mark that `struct` as the"] # [doc = " `#[global_logger]`."] pub unsafe trait Logger { # [doc = " Acquire the global logger in the current execution context."] # [doc = ""] # [doc = " This will be called by the defmt logging macros before writing each log frame."] # [doc = ""] # [doc = " Panics if already acquired in the current execution context. Otherwise it must never fail."] fn acquire () ; # [doc = " Block until host has read all pending data."] # [doc = ""] # [doc = " The flush operation must not fail. This is a \"best effort\" operation, I/O errors should be discarded."] # [doc = ""] # [doc = " # Safety"] # [doc = " Must only be called when the global logger is acquired in the current execution context."] # [doc = " (i.e. between `acquire()` and `release()`)."] unsafe fn flush () ; # [doc = " Releases the global logger in the current execution context."] # [doc = ""] # [doc = " This will be called by the defmt logging macros after writing each log frame."] # [doc = ""] # [doc = " # Safety"] # [doc = " Must be called exactly once for each acquire(), in the same execution context."] unsafe fn release () ; # [doc = " Writes `bytes` to the destination."] # [doc = ""] # [doc = " This will be called by the defmt logging macros to transmit frame data. One log frame may cause multiple `write` calls."] # [doc = ""] # [doc = " The write operation must not fail. This is a \"best effort\" operation, I/O errors should be discarded."] # [doc = ""] # [doc = " The `bytes` are unencoded log frame data, they MUST be encoded with `Encoder` prior to"] # [doc = " sending over the wire."] # [doc = ""] # [doc = " Note that a call to `write` does *not* correspond to a defmt logging macro invocation. A"] # [doc = " single `defmt::info!` call can result in an arbitrary number of `write` calls."] # [doc = ""] # [doc = " # Safety"] # [doc = " Must only be called when the global logger is acquired in the current execution context."] # [doc = " (i.e. between `acquire()` and `release()`)."] unsafe fn write (bytes : & [u8]) ; }
};
}
