// Generated macro for impl_8 (impl)
macro_rules! Depcrate_encodingimpl_8 {
() => {
// Module: crate::encoding
// Provides: {"impl_8"}
// Dependencies: {}
impl Encoder { # [doc = " Create a new `Encoder`."] # [allow (clippy :: new_without_default)] pub const fn new () -> Self { Self { inner : inner :: Encoder :: new () , } } # [doc = " Start encoding a log frame."] # [doc = ""] # [doc = " `Logger` impls will typically call this from `acquire()`."] # [doc = ""] # [doc = " You may only call `start_frame` when no frame is currently being encoded."] # [doc = " Failure to do so may result in corrupted data on the wire."] # [doc = ""] # [doc = " The `write` closure will be called with the encoded data that must"] # [doc = " be sent on the wire. It may be called zero, one, or multiple times."] pub fn start_frame (& mut self , write : impl FnMut (& [u8])) { self . inner . start_frame (write) } # [doc = " Finish encoding a log frame."] # [doc = ""] # [doc = " `Logger` impls will typically call this from `release()`."] # [doc = ""] # [doc = " You may only call `end_frame` when a frame is currently being encoded."] # [doc = " Failure to do so may result in corrupted data on the wire."] # [doc = ""] # [doc = " The `write` closure will be called with the encoded data that must"] # [doc = " be sent on the wire. It may be called zero, one, or multiple times."] pub fn end_frame (& mut self , write : impl FnMut (& [u8])) { self . inner . end_frame (write) } # [doc = " Write part of data for a log frame."] # [doc = ""] # [doc = " `Logger` impls will typically call this from `write()`."] # [doc = ""] # [doc = " You may only call `write` when a frame is currently being encoded."] # [doc = " Failure to do so may result in corrupted data on the wire."] # [doc = ""] # [doc = " The `write` closure will be called with the encoded data that must"] # [doc = " be sent on the wire. It may be called zero, one, or multiple times."] pub fn write (& mut self , data : & [u8] , write : impl FnMut (& [u8])) { self . inner . write (data , write) } }
};
}
