// Generated macro for impl_395 (impl)
macro_rules! Depcrate_seq_coin_flipperimpl_395 {
() => {
// Module: crate::seq::coin_flipper
// Provides: {"impl_395"}
// Dependencies: {}
impl < R : RngCore > CoinFlipper < R > { pub fn new (rng : R) -> Self { Self { rng , chunk : 0 , chunk_remaining : 0 , } } # [inline] # [doc = " Returns true with a probability of 1 / d"] # [doc = " Uses an expected two bits of randomness"] # [doc = " Panics if d == 0"] pub fn random_ratio_one_over (& mut self , d : usize) -> bool { debug_assert_ne ! (d , 0) ; let c = (usize :: BITS - 1 - d . leading_zeros ()) . min (32) ; if self . flip_c_heads (c) { let numerator = 1 << c ; self . random_ratio (numerator , d) } else { false } } # [inline] # [doc = " Returns true with a probability of n / d"] # [doc = " Uses an expected two bits of randomness"] fn random_ratio (& mut self , mut n : usize , d : usize) -> bool { while n < d { let c = n . leading_zeros () . saturating_sub (d . leading_zeros () + 1) . clamp (1 , 32) ; if self . flip_c_heads (c) { n = n . saturating_mul (2_usize . pow (c)) ; } else { if c == 1 { let next_n = n . wrapping_add (n) . wrapping_sub (d) ; if next_n == 0 || next_n > n { return false ; } n = next_n ; } else { return false ; } } } true } # [doc = " If the next `c` bits of randomness all represent heads, consume them, return true"] # [doc = " Otherwise return false and consume the number of heads plus one."] # [doc = " Generates new bits of randomness when necessary (in 32 bit chunks)"] # [doc = " Has a 1 in 2 to the `c` chance of returning true"] # [doc = " `c` must be less than or equal to 32"] fn flip_c_heads (& mut self , mut c : u32) -> bool { debug_assert ! (c <= 32) ; loop { let zeros = self . chunk . leading_zeros () ; if zeros < c { self . chunk = self . chunk . wrapping_shl (zeros + 1) ; self . chunk_remaining = self . chunk_remaining . saturating_sub (zeros + 1) ; return false ; } else { if let Some (new_remaining) = self . chunk_remaining . checked_sub (c) { self . chunk_remaining = new_remaining ; self . chunk <<= c ; return true ; } else { c -= self . chunk_remaining ; self . chunk = self . rng . next_u32 () ; self . chunk_remaining = 32 ; } } } } }
};
}
