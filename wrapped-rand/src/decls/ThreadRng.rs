macro_rules! deps {
    () => {
        ReseedingRng!();
        OsRng!();
        StdRng!();
    };
}

macro_rules! ThreadRng {
    () => {
        deps!();
        # [doc = " A reference to the thread-local generator"] # [doc = ""] # [doc = " This type is a reference to a lazily-initialized thread-local generator."] # [doc = " An instance can be obtained via [`rand::rng()`][crate::rng()] or via"] # [doc = " [`ThreadRng::default()`]."] # [doc = " The handle cannot be passed between threads (is not `Send` or `Sync`)."] # [doc = ""] # [doc = " # Security"] # [doc = ""] # [doc = " Security must be considered relative to a threat model and validation"] # [doc = " requirements. The Rand project can provide no guarantee of fitness for"] # [doc = " purpose. The design criteria for `ThreadRng` are as follows:"] # [doc = ""] # [doc = " - Automatic seeding via [`OsRng`] and periodically thereafter (see"] # [doc = "   ([`ReseedingRng`] documentation). Limitation: there is no automatic"] # [doc = "   reseeding on process fork (see [below](#fork))."] # [doc = " - A rigorusly analyzed, unpredictable (cryptographic) pseudo-random generator"] # [doc = "   (see [the book on security](https://rust-random.github.io/book/guide-rngs.html#security))."] # [doc = "   The currently selected algorithm is ChaCha (12-rounds)."] # [doc = "   See also [`StdRng`] documentation."] # [doc = " - Not to leak internal state through [`Debug`] or serialization"] # [doc = "   implementations."] # [doc = " - No further protections exist to in-memory state. In particular, the"] # [doc = "   implementation is not required to zero memory on exit (of the process or"] # [doc = "   thread). (This may change in the future.)"] # [doc = " - Be fast enough for general-purpose usage. Note in particular that"] # [doc = "   `ThreadRng` is designed to be a \"fast, reasonably secure generator\""] # [doc = "   (where \"reasonably secure\" implies the above criteria)."] # [doc = ""] # [doc = " We leave it to the user to determine whether this generator meets their"] # [doc = " security requirements. For an alternative, see [`OsRng`]."] # [doc = ""] # [doc = " # Fork"] # [doc = ""] # [doc = " `ThreadRng` is not automatically reseeded on fork. It is recommended to"] # [doc = " explicitly call [`ThreadRng::reseed`] immediately after a fork, for example:"] # [doc = " ```ignore"] # [doc = " fn do_fork() {"] # [doc = "     let pid = unsafe { libc::fork() };"] # [doc = "     if pid == 0 {"] # [doc = "         // Reseed ThreadRng in child processes:"] # [doc = "         rand::rng().reseed();"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Methods on `ThreadRng` are not reentrant-safe and thus should not be called"] # [doc = " from an interrupt (e.g. a fork handler) unless it can be guaranteed that no"] # [doc = " other method on the same `ThreadRng` is currently executing."] # [doc = ""] # [doc = " [`ReseedingRng`]: crate::rngs::ReseedingRng"] # [doc = " [`StdRng`]: crate::rngs::StdRng"] # [derive (Clone)] pub struct ThreadRng { rng : Rc < UnsafeCell < ReseedingRng < Core , OsRng > > > , }
    };
}

ThreadRng!()