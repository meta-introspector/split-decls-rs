// Generated macro for IndexedRandom (trait)
macro_rules! Depcrate_seq_sliceIndexedRandom {
() => {
// Module: crate::seq::slice
// Provides: {"IndexedRandom"}
// Dependencies: {}
# [doc = " Extension trait on indexable lists, providing random sampling methods."] # [doc = ""] # [doc = " This trait is implemented on `[T]` slice types. Other types supporting"] # [doc = " [`std::ops::Index<usize>`] may implement this (only [`Self::len`] must be"] # [doc = " specified)."] pub trait IndexedRandom : Index < usize > { # [doc = " The length"] fn len (& self) -> usize ; # [doc = " True when the length is zero"] # [inline] fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Uniformly sample one element"] # [doc = ""] # [doc = " Returns a reference to one uniformly-sampled random element of"] # [doc = " the slice, or `None` if the slice is empty."] # [doc = ""] # [doc = " For slices, complexity is `O(1)`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::seq::IndexedRandom;"] # [doc = ""] # [doc = " let choices = [1, 2, 4, 8, 16, 32];"] # [doc = " let mut rng = rand::rng();"] # [doc = " println!(\"{:?}\", choices.choose(&mut rng));"] # [doc = " assert_eq!(choices[..0].choose(&mut rng), None);"] # [doc = " ```"] fn choose < R > (& self , rng : & mut R) -> Option < & Self :: Output > where R : Rng + ? Sized , { if self . is_empty () { None } else { Some (& self [rng . random_range (.. self . len ())]) } } # [doc = " Return an iterator which samples from `self` with replacement"] # [doc = ""] # [doc = " Returns `None` if and only if `self.is_empty()`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::seq::IndexedRandom;"] # [doc = ""] # [doc = " let choices = [1, 2, 4, 8, 16, 32];"] # [doc = " let mut rng = rand::rng();"] # [doc = " for choice in choices.choose_iter(&mut rng).unwrap().take(3) {"] # [doc = "     println!(\"{:?}\", choice);"] # [doc = " }"] # [doc = " ```"] fn choose_iter < R > (& self , rng : & mut R) -> Option < impl Iterator < Item = & Self :: Output > > where R : Rng + ? Sized , { let distr = crate :: distr :: Uniform :: new (0 , self . len ()) . ok () ? ; Some (rng . sample_iter (distr) . map (| i | & self [i])) } # [doc = " Uniformly sample `amount` distinct elements from self"] # [doc = ""] # [doc = " Chooses `amount` elements from the slice at random, without repetition,"] # [doc = " and in random order. The returned iterator is appropriate both for"] # [doc = " collection into a `Vec` and filling an existing buffer (see example)."] # [doc = ""] # [doc = " In case this API is not sufficiently flexible, use [`index::sample`]."] # [doc = ""] # [doc = " For slices, complexity is the same as [`index::sample`]."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " use rand::seq::IndexedRandom;"] # [doc = ""] # [doc = " let mut rng = &mut rand::rng();"] # [doc = " let sample = \"Hello, audience!\".as_bytes();"] # [doc = ""] # [doc = " // collect the results into a vector:"] # [doc = " let v: Vec<u8> = sample.sample(&mut rng, 3).cloned().collect();"] # [doc = ""] # [doc = " // store in a buffer:"] # [doc = " let mut buf = [0u8; 5];"] # [doc = " for (b, slot) in sample.sample(&mut rng, buf.len()).zip(buf.iter_mut()) {"] # [doc = "     *slot = *b;"] # [doc = " }"] # [doc = " ```"] # [cfg (feature = "alloc")] fn sample < R > (& self , rng : & mut R , amount : usize) -> IndexedSamples < '_ , Self , Self :: Output > where Self :: Output : Sized , R : Rng + ? Sized , { let amount = core :: cmp :: min (amount , self . len ()) ; IndexedSamples { slice : self , _phantom : Default :: default () , indices : index :: sample (rng , self . len () , amount) . into_iter () , } } # [doc = " Uniformly sample a fixed-size array of distinct elements from self"] # [doc = ""] # [doc = " Chooses `N` elements from the slice at random, without repetition,"] # [doc = " and in random order."] # [doc = ""] # [doc = " For slices, complexity is the same as [`index::sample_array`]."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " use rand::seq::IndexedRandom;"] # [doc = ""] # [doc = " let mut rng = &mut rand::rng();"] # [doc = " let sample = \"Hello, audience!\".as_bytes();"] # [doc = ""] # [doc = " let a: [u8; 3] = sample.sample_array(&mut rng).unwrap();"] # [doc = " ```"] fn sample_array < R , const N : usize > (& self , rng : & mut R) -> Option < [Self :: Output ; N] > where Self :: Output : Clone + Sized , R : Rng + ? Sized , { let indices = index :: sample_array (rng , self . len ()) ? ; Some (indices . map (| index | self [index] . clone ())) } # [doc = " Biased sampling for one element"] # [doc = ""] # [doc = " Returns a reference to one element of the slice, sampled according"] # [doc = " to the provided weights. Returns `None` if and only if `self.is_empty()`."] # [doc = ""] # [doc = " The specified function `weight` maps each item `x` to a relative"] # [doc = " likelihood `weight(x)`. The probability of each item being selected is"] # [doc = " therefore `weight(x) / s`, where `s` is the sum of all `weight(x)`."] # [doc = ""] # [doc = " For slices of length `n`, complexity is `O(n)`."] # [doc = " For more information about the underlying algorithm,"] # [doc = " see the [`WeightedIndex`] distribution."] # [doc = ""] # [doc = " See also [`choose_weighted_mut`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::prelude::*;"] # [doc = ""] # [doc = " let choices = [('a', 2), ('b', 1), ('c', 1), ('d', 0)];"] # [doc = " let mut rng = rand::rng();"] # [doc = " // 50% chance to print 'a', 25% chance to print 'b', 25% chance to print 'c',"] # [doc = " // and 'd' will never be printed"] # [doc = " println!(\"{:?}\", choices.choose_weighted(&mut rng, |item| item.1).unwrap().0);"] # [doc = " ```"] # [doc = " [`choose`]: IndexedRandom::choose"] # [doc = " [`choose_weighted_mut`]: IndexedMutRandom::choose_weighted_mut"] # [doc = " [`WeightedIndex`]: crate::distr::weighted::WeightedIndex"] # [cfg (feature = "alloc")] fn choose_weighted < R , F , B , X > (& self , rng : & mut R , weight : F ,) -> Result < & Self :: Output , WeightError > where R : Rng + ? Sized , F : Fn (& Self :: Output) -> B , B : SampleBorrow < X > , X : SampleUniform + Weight + PartialOrd < X > , { use crate :: distr :: weighted :: WeightedIndex ; let distr = WeightedIndex :: new ((0 .. self . len ()) . map (| idx | weight (& self [idx]))) ? ; Ok (& self [rng . sample (distr)]) } # [doc = " Biased sampling with replacement"] # [doc = ""] # [doc = " Returns an iterator which samples elements from `self` according to the"] # [doc = " given weights with replacement (i.e. elements may be repeated)."] # [doc = " Returns `None` if and only if `self.is_empty()`."] # [doc = ""] # [doc = " See also doc for [`Self::choose_weighted`]."] # [cfg (feature = "alloc")] fn choose_weighted_iter < R , F , B , X > (& self , rng : & mut R , weight : F ,) -> Result < impl Iterator < Item = & Self :: Output > , WeightError > where R : Rng + ? Sized , F : Fn (& Self :: Output) -> B , B : SampleBorrow < X > , X : SampleUniform + Weight + PartialOrd < X > , { use crate :: distr :: weighted :: WeightedIndex ; let distr = WeightedIndex :: new ((0 .. self . len ()) . map (| idx | weight (& self [idx]))) ? ; Ok (rng . sample_iter (distr) . map (| i | & self [i])) } # [doc = " Biased sampling of `amount` distinct elements"] # [doc = ""] # [doc = " Similar to [`sample`], but where the likelihood of each"] # [doc = " element's inclusion in the output may be specified. Zero-weighted"] # [doc = " elements are never returned; the result may therefore contain fewer"] # [doc = " elements than `amount` even when `self.len() >= amount`. The elements"] # [doc = " are returned in an arbitrary, unspecified order."] # [doc = ""] # [doc = " The specified function `weight` maps each item `x` to a relative"] # [doc = " likelihood `weight(x)`. The probability of each item being selected is"] # [doc = " therefore `weight(x) / s`, where `s` is the sum of all `weight(x)`."] # [doc = ""] # [doc = " This implementation uses `O(length + amount)` space and `O(length)` time."] # [doc = " See [`index::sample_weighted`] for details."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::prelude::*;"] # [doc = ""] # [doc = " let choices = [('a', 2), ('b', 1), ('c', 1)];"] # [doc = " let mut rng = rand::rng();"] # [doc = " // First Draw * Second Draw = total odds"] # [doc = " // -----------------------"] # [doc = " // (50% * 50%) + (25% * 67%) = 41.7% chance that the output is `['a', 'b']` in some order."] # [doc = " // (50% * 50%) + (25% * 67%) = 41.7% chance that the output is `['a', 'c']` in some order."] # [doc = " // (25% * 33%) + (25% * 33%) = 16.6% chance that the output is `['b', 'c']` in some order."] # [doc = " println!(\"{:?}\", choices.sample_weighted(&mut rng, 2, |item| item.1).unwrap().collect::<Vec<_>>());"] # [doc = " ```"] # [doc = " [`sample`]: IndexedRandom::sample"] # [cfg (feature = "std")] fn sample_weighted < R , F , X > (& self , rng : & mut R , amount : usize , weight : F ,) -> Result < IndexedSamples < '_ , Self , Self :: Output > , WeightError > where Self :: Output : Sized , R : Rng + ? Sized , F : Fn (& Self :: Output) -> X , X : Into < f64 > , { let amount = core :: cmp :: min (amount , self . len ()) ; Ok (IndexedSamples { slice : self , _phantom : Default :: default () , indices : index :: sample_weighted (rng , self . len () , | idx | weight (& self [idx]) . into () , amount ,) ? . into_iter () , }) } # [doc = " Deprecated: use [`Self::sample`] instead"] # [cfg (feature = "alloc")] # [deprecated (since = "0.9.2" , note = "Renamed to `sample`")] fn choose_multiple < R > (& self , rng : & mut R , amount : usize ,) -> IndexedSamples < '_ , Self , Self :: Output > where Self :: Output : Sized , R : Rng + ? Sized , { self . sample (rng , amount) } # [doc = " Deprecated: use [`Self::sample_array`] instead"] # [deprecated (since = "0.9.2" , note = "Renamed to `sample_array`")] fn choose_multiple_array < R , const N : usize > (& self , rng : & mut R) -> Option < [Self :: Output ; N] > where Self :: Output : Clone + Sized , R : Rng + ? Sized , { self . sample_array (rng) } # [doc = " Deprecated: use [`Self::sample_weighted`] instead"] # [cfg (feature = "std")] # [deprecated (since = "0.9.2" , note = "Renamed to `sample_weighted`")] fn choose_multiple_weighted < R , F , X > (& self , rng : & mut R , amount : usize , weight : F ,) -> Result < IndexedSamples < '_ , Self , Self :: Output > , WeightError > where Self :: Output : Sized , R : Rng + ? Sized , F : Fn (& Self :: Output) -> X , X : Into < f64 > , { self . sample_weighted (rng , amount , weight) } }
};
}
