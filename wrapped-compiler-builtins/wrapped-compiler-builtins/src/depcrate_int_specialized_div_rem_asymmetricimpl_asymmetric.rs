// Generated macro for impl_asymmetric (macro)
macro_rules! Depcrate_int_specialized_div_rem_asymmetricimpl_asymmetric {
() => {
// Module: crate::int::specialized_div_rem::asymmetric
// Provides: {"impl_asymmetric"}
// Dependencies: {}
# [doc = " Creates an unsigned division function optimized for dividing integers with the same"] # [doc = " bitwidth as the largest operand in an asymmetrically sized division. For example, x86-64 has an"] # [doc = " assembly instruction that can divide a 128 bit integer by a 64 bit integer if the quotient fits"] # [doc = " in 64 bits. The 128 bit version of this algorithm would use that fast hardware division to"] # [doc = " construct a full 128 bit by 128 bit division."] # [allow (unused_macros)] macro_rules ! impl_asymmetric { ($ fn : ident , $ zero_div_fn : ident , $ half_division : ident , $ asymmetric_division : ident , $ n_h : expr , $ uH : ident , $ uX : ident , $ uD : ident) => { # [doc = " Computes the quotient and remainder of `duo` divided by `div` and returns them as a"] # [doc = " tuple."] pub fn $ fn (duo : $ uD , div : $ uD) -> ($ uD , $ uD) { let n : u32 = $ n_h * 2 ; let duo_lo = duo as $ uX ; let duo_hi = (duo >> n) as $ uX ; let div_lo = div as $ uX ; let div_hi = (div >> n) as $ uX ; if div_hi == 0 { if div_lo == 0 { $ zero_div_fn () } if duo_hi < div_lo { let (quo , rem) = unsafe { $ asymmetric_division (duo , div_lo) } ; return (quo as $ uD , rem as $ uD) ; } else { let (quo_hi , rem_hi) = $ half_division (duo_hi , div_lo) ; let tmp = unsafe { $ asymmetric_division ((duo_lo as $ uD) | ((rem_hi as $ uD) << n) , div_lo) } ; return ((tmp . 0 as $ uD) | ((quo_hi as $ uD) << n) , tmp . 1 as $ uD) ; } } let div_lz = div_hi . leading_zeros () ; let div_extra = n - div_lz ; let div_sig_n = (div >> div_extra) as $ uX ; let tmp = unsafe { $ asymmetric_division (duo >> 1 , div_sig_n) } ; let mut quo = tmp . 0 >> ((n - 1) - div_lz) ; if quo != 0 { quo -= 1 ; } let mut rem = duo - (quo as $ uD) . wrapping_mul (div) ; if div <= rem { quo += 1 ; rem -= div ; } return (quo as $ uD , rem) ; } } ; }
};
}
