// Generated macro for Float (trait)
macro_rules! Depcrate_float_traitsFloat {
() => {
// Module: crate::float::traits
// Provides: {"Float"}
// Dependencies: {}
# [doc = " Trait for some basic operations on floats"] # [allow (dead_code)] pub trait Float : Copy + core :: fmt :: Debug + PartialEq + PartialOrd + ops :: AddAssign + ops :: MulAssign + ops :: Add < Output = Self > + ops :: Sub < Output = Self > + ops :: Div < Output = Self > + ops :: Rem < Output = Self > { # [doc = " A uint of the same width as the float"] type Int : Int < OtherSign = Self :: SignedInt , Unsigned = Self :: Int > ; # [doc = " A int of the same width as the float"] type SignedInt : Int + MinInt < OtherSign = Self :: Int , Unsigned = Self :: Int > ; # [doc = " An int capable of containing the exponent bits plus a sign bit. This is signed."] type ExpInt : Int ; const ZERO : Self ; const ONE : Self ; # [doc = " The bitwidth of the float type."] const BITS : u32 ; # [doc = " The bitwidth of the significand."] const SIG_BITS : u32 ; # [doc = " The bitwidth of the exponent."] const EXP_BITS : u32 = Self :: BITS - Self :: SIG_BITS - 1 ; # [doc = " The saturated (maximum bitpattern) value of the exponent, i.e. the infinite"] # [doc = " representation."] # [doc = ""] # [doc = " This is in the rightmost position, use `EXP_MASK` for the shifted value."] const EXP_SAT : u32 = (1 << Self :: EXP_BITS) - 1 ; # [doc = " The exponent bias value."] const EXP_BIAS : u32 = Self :: EXP_SAT >> 1 ; # [doc = " A mask for the sign bit."] const SIGN_MASK : Self :: Int ; # [doc = " A mask for the significand."] const SIG_MASK : Self :: Int ; # [doc = " The implicit bit of the float format."] const IMPLICIT_BIT : Self :: Int ; # [doc = " A mask for the exponent."] const EXP_MASK : Self :: Int ; # [doc = " Returns `self` transmuted to `Self::Int`"] fn to_bits (self) -> Self :: Int ; # [doc = " Returns `self` transmuted to `Self::SignedInt`"] fn to_bits_signed (self) -> Self :: SignedInt ; # [doc = " Checks if two floats have the same bit representation. *Except* for NaNs! NaN can be"] # [doc = " represented in multiple different ways. This method returns `true` if two NaNs are"] # [doc = " compared."] fn eq_repr (self , rhs : Self) -> bool ; # [doc = " Returns true if the sign is negative"] fn is_sign_negative (self) -> bool ; # [doc = " Returns the exponent, not adjusting for bias."] fn exp (self) -> Self :: ExpInt ; # [doc = " Returns the significand with no implicit bit (or the \"fractional\" part)"] fn frac (self) -> Self :: Int ; # [doc = " Returns the significand with implicit bit"] fn imp_frac (self) -> Self :: Int ; # [doc = " Returns a `Self::Int` transmuted back to `Self`"] fn from_bits (a : Self :: Int) -> Self ; # [doc = " Constructs a `Self` from its parts. Inputs are treated as bits and shifted into position."] fn from_parts (negative : bool , exponent : Self :: Int , significand : Self :: Int) -> Self ; fn abs (self) -> Self { let abs_mask = ! Self :: SIGN_MASK ; Self :: from_bits (self . to_bits () & abs_mask) } # [doc = " Returns (normalized exponent, normalized significand)"] fn normalize (significand : Self :: Int) -> (i32 , Self :: Int) ; # [doc = " Returns if `self` is subnormal"] fn is_subnormal (self) -> bool ; }
};
}
