// Generated macro for intrinsics (macro)
macro_rules! Depcrate_macrosintrinsics {
() => {
// Module: crate::macros
// Provides: {"intrinsics"}
// Dependencies: {}
# [doc = " The \"main macro\" used for defining intrinsics."] # [doc = ""] # [doc = " The compiler-builtins library is super platform-specific with tons of crazy"] # [doc = " little tweaks for various platforms. As a result it *could* involve a lot of"] # [doc = " #[cfg] and macro soup, but the intention is that this macro alleviates a lot"] # [doc = " of that complexity. Ideally this macro has all the weird ABI things"] # [doc = " platforms need and elsewhere in this library it just looks like normal Rust"] # [doc = " code."] # [doc = ""] # [doc = " All intrinsics functions are marked with #[linkage = \"weak\"] when"] # [doc = " `not(windows) and not(target_vendor = \"apple\")`."] # [doc = " `weak` linkage attribute is used so that these functions can be replaced"] # [doc = " by another implementation at link time. This is particularly useful for mixed"] # [doc = " Rust/C++ binaries that want to use the C++ intrinsics, otherwise linking against"] # [doc = " the Rust stdlib will replace those from the compiler-rt library."] # [doc = ""] # [doc = " This macro is structured to be invoked with a bunch of functions that looks"] # [doc = " like:"] # [doc = " ```ignore"] # [doc = "     intrinsics! {"] # [doc = "         pub extern \"C\" fn foo(a: i32) -> u32 {"] # [doc = "             // ..."] # [doc = "         }"] # [doc = ""] # [doc = "         #[nonstandard_attribute]"] # [doc = "         pub extern \"C\" fn bar(a: i32) -> u32 {"] # [doc = "             // ..."] # [doc = "         }"] # [doc = "     }"] # [doc = " ```"] # [doc = ""] # [doc = " Each function is defined in a manner that looks like a normal Rust function."] # [doc = " The macro then accepts a few nonstandard attributes that can decorate"] # [doc = " various functions. Each of the attributes is documented below with what it"] # [doc = " can do, and each of them slightly tweaks how further expansion happens."] # [doc = ""] # [doc = " A quick overview of attributes supported right now are:"] # [doc = ""] # [doc = " * `maybe_use_optimized_c_shim` - indicates that the Rust implementation is"] # [doc = "   ignored if an optimized C version was compiled."] # [doc = " * `aapcs_on_arm` - forces the ABI of the function to be `\"aapcs\"` on ARM and"] # [doc = "   the specified ABI everywhere else."] # [doc = " * `unadjusted_on_win64` - like `aapcs_on_arm` this switches to the"] # [doc = "   `\"unadjusted\"` abi on Win64 and the specified abi elsewhere."] # [doc = " * `arm_aeabi_alias` - handles the \"aliasing\" of various intrinsics on ARM"] # [doc = "   their otherwise typical names to other prefixed ones."] # [doc = " * `ppc_alias` - changes the name of the symbol on PowerPC platforms without"] # [doc = "   changing any other behavior. This is mostly for `f128`, which is `tf` on"] # [doc = "   most platforms but `kf` on PowerPC."] macro_rules ! intrinsics { () => () ; (# [cfg_attr ($ e : meta , $ ($ attr : tt) *)] $ (# [$ ($ attrs : tt) *]) * pub extern $ abi : tt fn $ name : ident ($ ($ argname : ident : $ ty : ty) ,*) $ (-> $ ret : ty) ? { $ ($ body : tt) * } $ ($ rest : tt) *) => (# [cfg ($ e)] intrinsics ! { # [$ ($ attr) *] $ (# [$ ($ attrs) *]) * pub extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } # [cfg (not ($ e))] intrinsics ! { $ (# [$ ($ attrs) *]) * pub extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } intrinsics ! ($ ($ rest) *) ;) ; (# [cfg_attr ($ e : meta , $ ($ attr : tt) *)] $ (# [$ ($ attrs : tt) *]) * pub unsafe extern $ abi : tt fn $ name : ident ($ ($ argname : ident : $ ty : ty) ,*) $ (-> $ ret : ty) ? { $ ($ body : tt) * } $ ($ rest : tt) *) => (# [cfg ($ e)] intrinsics ! { # [$ ($ attr) *] $ (# [$ ($ attrs) *]) * pub unsafe extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } # [cfg (not ($ e))] intrinsics ! { $ (# [$ ($ attrs) *]) * pub unsafe extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } intrinsics ! ($ ($ rest) *) ;) ; (# [maybe_use_optimized_c_shim] $ (# [$ ($ attr : tt) *]) * pub $ (unsafe $ (@ $ empty : tt) ?) ? extern $ abi : tt fn $ name : ident ($ ($ argname : ident : $ ty : ty) ,*) $ (-> $ ret : ty) ? { $ ($ body : tt) * } $ ($ rest : tt) *) => (# [cfg ($ name = "optimized-c")] pub $ (unsafe $ ($ empty) ?) ? extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { unsafe extern $ abi { fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ?; } unsafe { $ name ($ ($ argname) ,*) } } # [cfg (not ($ name = "optimized-c"))] intrinsics ! { $ (# [$ ($ attr) *]) * pub $ (unsafe $ ($ empty) ?) ? extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } intrinsics ! ($ ($ rest) *) ;) ; (# [aapcs_on_arm] $ (# [$ ($ attr : tt) *]) * pub extern $ abi : tt fn $ name : ident ($ ($ argname : ident : $ ty : ty) ,*) $ (-> $ ret : ty) ? { $ ($ body : tt) * } $ ($ rest : tt) *) => (# [cfg (target_arch = "arm")] intrinsics ! { $ (# [$ ($ attr) *]) * pub extern "aapcs" fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } # [cfg (not (target_arch = "arm"))] intrinsics ! { $ (# [$ ($ attr) *]) * pub extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } intrinsics ! ($ ($ rest) *) ;) ; (# [unadjusted_on_win64] $ (# [$ ($ attr : tt) *]) * pub extern $ abi : tt fn $ name : ident ($ ($ argname : ident : $ ty : ty) ,*) $ (-> $ ret : ty) ? { $ ($ body : tt) * } $ ($ rest : tt) *) => (# [cfg (all (any (windows , target_os = "cygwin" , all (target_os = "uefi" , target_arch = "x86_64")) , target_pointer_width = "64"))] intrinsics ! { $ (# [$ ($ attr) *]) * pub extern "unadjusted" fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } # [cfg (not (all (any (windows , target_os = "cygwin" , all (target_os = "uefi" , target_arch = "x86_64")) , target_pointer_width = "64")))] intrinsics ! { $ (# [$ ($ attr) *]) * pub extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } intrinsics ! ($ ($ rest) *) ;) ; (# [apple_f16_arg_abi] # [arm_aeabi_alias = $ alias : ident] $ ($ t : tt) *) => { intrinsics ! { # [apple_f16_arg_abi , arm_aeabi_alias = $ alias] $ ($ t) * } } ; (# [apple_f16_ret_abi] # [arm_aeabi_alias = $ alias : ident] $ ($ t : tt) *) => { intrinsics ! { # [apple_f16_ret_abi , arm_aeabi_alias = $ alias] $ ($ t) * } } ; (# [apple_f16_arg_abi $ (, arm_aeabi_alias = $ alias : ident) ?] $ (# [$ ($ attr : tt) *]) * pub extern $ abi : tt fn $ name : ident ($ ($ argname : ident : $ ty : ty) ,*) $ (-> $ ret : ty) ? { $ ($ body : tt) * } $ ($ rest : tt) *) => (# [cfg (all (target_vendor = "apple" , any (target_arch = "x86" , target_arch = "x86_64")))] $ (# [$ ($ attr) *]) * pub extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } # [cfg (all (target_vendor = "apple" , any (target_arch = "x86" , target_arch = "x86_64") , not (feature = "mangled-names")))] mod $ name { # [unsafe (no_mangle)] # [cfg_attr (not (any (all (windows , target_env = "gnu") , target_os = "cygwin")) , linkage = "weak")] $ (# [$ ($ attr) *]) * extern $ abi fn $ name ($ ($ argname : u16) ,*) $ (-> $ ret) ? { super ::$ name ($ (f16 :: from_bits ($ argname)) ,*) } } # [cfg (not (all (target_vendor = "apple" , any (target_arch = "x86" , target_arch = "x86_64"))))] intrinsics ! { $ (# [arm_aeabi_alias = $ alias]) ? $ (# [$ ($ attr) *]) * pub extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } intrinsics ! ($ ($ rest) *) ;) ; (# [apple_f16_ret_abi $ (, arm_aeabi_alias = $ alias : ident) ?] $ (# [$ ($ attr : tt) *]) * pub extern $ abi : tt fn $ name : ident ($ ($ argname : ident : $ ty : ty) ,*) $ (-> $ ret : ty) ? { $ ($ body : tt) * } $ ($ rest : tt) *) => (# [cfg (all (target_vendor = "apple" , any (target_arch = "x86" , target_arch = "x86_64")))] $ (# [$ ($ attr) *]) * pub extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } # [cfg (all (target_vendor = "apple" , any (target_arch = "x86" , target_arch = "x86_64") , not (feature = "mangled-names")))] mod $ name { # [unsafe (no_mangle)] # [cfg_attr (not (any (all (windows , target_env = "gnu") , target_os = "cygwin")) , linkage = "weak")] $ (# [$ ($ attr) *]) * extern $ abi fn $ name ($ ($ argname : $ ty) ,*) -> u16 { super ::$ name ($ ($ argname) ,*) . to_bits () } } # [cfg (not (all (target_vendor = "apple" , any (target_arch = "x86" , target_arch = "x86_64"))))] intrinsics ! { $ (# [arm_aeabi_alias = $ alias]) ? $ (# [$ ($ attr) *]) * pub extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } intrinsics ! ($ ($ rest) *) ;) ; (# [arm_aeabi_alias = $ alias : ident] $ (# [$ ($ attr : tt) *]) * pub extern $ abi : tt fn $ name : ident ($ ($ argname : ident : $ ty : ty) ,*) $ (-> $ ret : ty) ? { $ ($ body : tt) * } $ ($ rest : tt) *) => (# [cfg (target_arch = "arm")] $ (# [$ ($ attr) *]) * pub extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } # [cfg (all (target_arch = "arm" , not (feature = "mangled-names")))] mod $ name { # [unsafe (no_mangle)] # [cfg_attr (not (any (all (windows , target_env = "gnu") , target_os = "cygwin")) , linkage = "weak")] $ (# [$ ($ attr) *]) * extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { super ::$ name ($ ($ argname) ,*) } } # [cfg (all (target_arch = "arm" , not (feature = "mangled-names")))] mod $ alias { # [unsafe (no_mangle)] # [cfg_attr (not (any (all (windows , target_env = "gnu") , target_os = "cygwin")) , linkage = "weak")] $ (# [$ ($ attr) *]) * extern "aapcs" fn $ alias ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { super ::$ name ($ ($ argname) ,*) } } # [cfg (not (target_arch = "arm"))] intrinsics ! { $ (# [$ ($ attr) *]) * pub extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } intrinsics ! ($ ($ rest) *) ;) ; (# [ppc_alias = $ alias : ident] $ (# [$ ($ attr : tt) *]) * pub extern $ abi : tt fn $ name : ident ($ ($ argname : ident : $ ty : ty) ,*) $ (-> $ ret : ty) ? { $ ($ body : tt) * } $ ($ rest : tt) *) => (# [cfg (not (any (target_arch = "powerpc" , target_arch = "powerpc64")))] intrinsics ! { $ (# [$ ($ attr) *]) * pub extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } # [cfg (any (target_arch = "powerpc" , target_arch = "powerpc64"))] intrinsics ! { $ (# [$ ($ attr) *]) * pub extern $ abi fn $ alias ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } intrinsics ! ($ ($ rest) *) ;) ; (# [mem_builtin] $ (# [$ ($ attr : tt) *]) * pub unsafe extern $ abi : tt fn $ name : ident ($ ($ argname : ident : $ ty : ty) ,*) $ (-> $ ret : ty) ? { $ ($ body : tt) * } $ ($ rest : tt) *) => ($ (# [$ ($ attr) *]) * pub unsafe extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } # [cfg (all (feature = "mem" , not (feature = "mangled-names")))] mod $ name { $ (# [$ ($ attr) *]) * # [unsafe (no_mangle)] # [cfg_attr (not (any (all (windows , target_env = "gnu") , target_os = "cygwin")) , linkage = "weak")] unsafe extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { super ::$ name ($ ($ argname) ,*) } } intrinsics ! ($ ($ rest) *) ;) ; (# [unsafe (naked)] $ (# [$ ($ attr : tt) *]) * pub unsafe extern $ abi : tt fn $ name : ident ($ ($ argname : ident : $ ty : ty) ,*) $ (-> $ ret : ty) ? { $ ($ body : tt) * } $ ($ rest : tt) *) => (pub mod $ name { # [unsafe (naked)] $ (# [$ ($ attr) *]) * # [cfg_attr (not (feature = "mangled-names") , unsafe (no_mangle))] # [cfg_attr (not (any (all (windows , target_env = "gnu") , target_os = "cygwin")) , linkage = "weak")] pub unsafe extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } } intrinsics ! ($ ($ rest) *) ;) ; ($ (# [$ ($ attr : tt) *]) * pub $ (unsafe $ (@ $ empty : tt) ?) ? extern $ abi : tt fn $ name : ident ($ ($ argname : ident : $ ty : ty) ,*) $ (-> $ ret : ty) ? { $ ($ body : tt) * } $ ($ rest : tt) *) => ($ (# [$ ($ attr) *]) * pub $ (unsafe $ ($ empty) ?) ? extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ ($ body) * } # [cfg (not (feature = "mangled-names"))] mod $ name { $ (# [$ ($ attr) *]) * # [unsafe (no_mangle)] # [cfg_attr (not (any (all (windows , target_env = "gnu") , target_os = "cygwin")) , linkage = "weak")] $ (unsafe $ ($ empty) ?) ? extern $ abi fn $ name ($ ($ argname : $ ty) ,*) $ (-> $ ret) ? { $ (unsafe $ ($ empty) ?) ? { super ::$ name ($ ($ argname) ,*) } } } intrinsics ! ($ ($ rest) *) ;) ; }
};
}
