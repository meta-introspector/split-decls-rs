// Generated macro for mul (function)
macro_rules! Depcrate_float_mulmul {
() => {
// Module: crate::float::mul
// Provides: {"mul"}
// Dependencies: {}
fn mul < F : Float > (a : F , b : F) -> F where u32 : CastInto < F :: Int > , F :: Int : CastInto < u32 > , i32 : CastInto < F :: Int > , F :: Int : CastInto < i32 > , F :: Int : HInt , { let one = F :: Int :: ONE ; let zero = F :: Int :: ZERO ; let bits = F :: BITS ; let significand_bits = F :: SIG_BITS ; let max_exponent = F :: EXP_SAT ; let exponent_bias = F :: EXP_BIAS ; let implicit_bit = F :: IMPLICIT_BIT ; let significand_mask = F :: SIG_MASK ; let sign_bit = F :: SIGN_MASK ; let abs_mask = sign_bit - one ; let exponent_mask = F :: EXP_MASK ; let inf_rep = exponent_mask ; let quiet_bit = implicit_bit >> 1 ; let qnan_rep = exponent_mask | quiet_bit ; let exponent_bits = F :: EXP_BITS ; let a_rep = a . to_bits () ; let b_rep = b . to_bits () ; let a_exponent = (a_rep >> significand_bits) & max_exponent . cast () ; let b_exponent = (b_rep >> significand_bits) & max_exponent . cast () ; let product_sign = (a_rep ^ b_rep) & sign_bit ; let mut a_significand = a_rep & significand_mask ; let mut b_significand = b_rep & significand_mask ; let mut scale = 0 ; if a_exponent . wrapping_sub (one) >= (max_exponent - 1) . cast () || b_exponent . wrapping_sub (one) >= (max_exponent - 1) . cast () { let a_abs = a_rep & abs_mask ; let b_abs = b_rep & abs_mask ; if a_abs > inf_rep { return F :: from_bits (a_rep | quiet_bit) ; } if b_abs > inf_rep { return F :: from_bits (b_rep | quiet_bit) ; } if a_abs == inf_rep { if b_abs != zero { return F :: from_bits (a_abs | product_sign) ; } else { return F :: from_bits (qnan_rep) ; } } if b_abs == inf_rep { if a_abs != zero { return F :: from_bits (b_abs | product_sign) ; } else { return F :: from_bits (qnan_rep) ; } } if a_abs == zero { return F :: from_bits (product_sign) ; } if b_abs == zero { return F :: from_bits (product_sign) ; } if a_abs < implicit_bit { let (exponent , significand) = F :: normalize (a_significand) ; scale += exponent ; a_significand = significand ; } if b_abs < implicit_bit { let (exponent , significand) = F :: normalize (b_significand) ; scale += exponent ; b_significand = significand ; } } a_significand |= implicit_bit ; b_significand |= implicit_bit ; let (mut product_low , mut product_high) = a_significand . widen_mul (b_significand << exponent_bits) . lo_hi () ; let a_exponent_i32 : i32 = a_exponent . cast () ; let b_exponent_i32 : i32 = b_exponent . cast () ; let mut product_exponent : i32 = a_exponent_i32 . wrapping_add (b_exponent_i32) . wrapping_add (scale) . wrapping_sub (exponent_bias as i32) ; if (product_high & implicit_bit) != zero { product_exponent = product_exponent . wrapping_add (1) ; } else { product_high = (product_high << 1) | (product_low >> (bits - 1)) ; product_low <<= 1 ; } if product_exponent >= max_exponent as i32 { return F :: from_bits (inf_rep | product_sign) ; } if product_exponent <= 0 { let shift : u32 = one . wrapping_sub (product_exponent . cast_lossy ()) . cast () ; if shift >= bits { return F :: from_bits (product_sign) ; } let sticky = product_low << (bits - shift) != zero ; product_low = (product_high << (bits - shift)) | (product_low >> shift) | (sticky as u32) . cast () ; product_high >>= shift ; } else { product_high &= significand_mask ; product_high |= product_exponent . cast () << significand_bits ; } product_high |= product_sign ; if product_low > sign_bit { product_high += one ; } if product_low == sign_bit { product_high += product_high & one ; } F :: from_bits (product_high) }
};
}
