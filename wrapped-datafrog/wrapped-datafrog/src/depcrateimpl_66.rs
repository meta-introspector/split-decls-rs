// Generated macro for impl_66 (impl)
macro_rules! Depcrateimpl_66 {
() => {
// Module: crate
// Provides: {"impl_66"}
// Dependencies: {}
impl < Tuple : Ord > Variable < Tuple > { # [doc = " Adds tuples that result from joining `input1` and `input2` --"] # [doc = " each of the inputs must be a set of (Key, Value) tuples. Both"] # [doc = " `input1` and `input2` must have the same type of key (`K`) but"] # [doc = " they can have distinct value types (`V1` and `V2`"] # [doc = " respectively). The `logic` closure will be invoked for each"] # [doc = " key that appears in both inputs; it is also given the two"] # [doc = " values, and from those it should construct the resulting"] # [doc = " value."] # [doc = ""] # [doc = " Note that `input1` must be a variable, but `input2` can be a"] # [doc = " relation or a variable. Therefore, you cannot join two"] # [doc = " relations with this method. This is not because the result"] # [doc = " would be wrong, but because it would be inefficient: the"] # [doc = " result from such a join cannot vary across iterations (as"] # [doc = " relations are fixed), so you should prefer to invoke `insert`"] # [doc = " on a relation created by `Relation::from_join` instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " This example starts a collection with the pairs (x, x+1) and (x+1, x) for x in 0 .. 10."] # [doc = " It then adds pairs (y, z) for which (x, y) and (x, z) are present. Because the initial"] # [doc = " pairs are symmetric, this should result in all pairs (x, y) for x and y in 0 .. 11."] # [doc = ""] # [doc = " ```"] # [doc = " use datafrog::{Iteration, Relation};"] # [doc = ""] # [doc = " let mut iteration = Iteration::new();"] # [doc = " let variable = iteration.variable::<(usize, usize)>(\"source\");"] # [doc = " variable.extend((0 .. 10).map(|x| (x, x + 1)));"] # [doc = " variable.extend((0 .. 10).map(|x| (x + 1, x)));"] # [doc = ""] # [doc = " while iteration.changed() {"] # [doc = "     variable.from_join(&variable, &variable, |&key, &val1, &val2| (val1, val2));"] # [doc = " }"] # [doc = ""] # [doc = " let result = variable.complete();"] # [doc = " assert_eq!(result.len(), 121);"] # [doc = " ```"] pub fn from_join < 'me , K : Ord , V1 : Ord , V2 : Ord > (& self , input1 : & 'me Variable < (K , V1) > , input2 : impl JoinInput < 'me , (K , V2) > , logic : impl FnMut (& K , & V1 , & V2) -> Tuple ,) { join :: join_into (input1 , input2 , self , logic) } # [doc = " Adds tuples from `input1` whose key is not present in `input2`."] # [doc = ""] # [doc = " Note that `input1` must be a variable: if you have a relation"] # [doc = " instead, you can use `Relation::from_antijoin` and then"] # [doc = " `Variable::insert`.  Note that the result will not vary during"] # [doc = " the iteration."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " This example starts a collection with the pairs (x, x+1) for x in 0 .. 10. It then"] # [doc = " adds any pairs (x+1,x) for which x is not a multiple of three. That excludes four"] # [doc = " pairs (for 0, 3, 6, and 9) which should leave us with 16 total pairs."] # [doc = ""] # [doc = " ```"] # [doc = " use datafrog::{Iteration, Relation};"] # [doc = ""] # [doc = " let mut iteration = Iteration::new();"] # [doc = " let variable = iteration.variable::<(usize, usize)>(\"source\");"] # [doc = " variable.extend((0 .. 10).map(|x| (x, x + 1)));"] # [doc = ""] # [doc = " let relation: Relation<_> = (0 .. 10).filter(|x| x % 3 == 0).collect();"] # [doc = ""] # [doc = " while iteration.changed() {"] # [doc = "     variable.from_antijoin(&variable, &relation, |&key, &val| (val, key));"] # [doc = " }"] # [doc = ""] # [doc = " let result = variable.complete();"] # [doc = " assert_eq!(result.len(), 16);"] # [doc = " ```"] pub fn from_antijoin < K : Ord , V : Ord > (& self , input1 : & Variable < (K , V) > , input2 : & Relation < K > , logic : impl FnMut (& K , & V) -> Tuple ,) { self . insert (join :: antijoin (input1 , input2 , logic)) } # [doc = " Adds tuples that result from mapping `input`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " This example starts a collection with the pairs (x, x) for x in 0 .. 10. It then"] # [doc = " repeatedly adds any pairs (x, z) for (x, y) in the collection, where z is the Collatz"] # [doc = " step for y: it is y/2 if y is even, and 3*y + 1 if y is odd. This produces all of the"] # [doc = " pairs (x, y) where x visits y as part of its Collatz journey."] # [doc = ""] # [doc = " ```"] # [doc = " use datafrog::{Iteration, Relation};"] # [doc = ""] # [doc = " let mut iteration = Iteration::new();"] # [doc = " let variable = iteration.variable::<(usize, usize)>(\"source\");"] # [doc = " variable.extend((0 .. 10).map(|x| (x, x)));"] # [doc = ""] # [doc = " while iteration.changed() {"] # [doc = "     variable.from_map(&variable, |&(key, val)|"] # [doc = "         if val % 2 == 0 {"] # [doc = "             (key, val/2)"] # [doc = "         }"] # [doc = "         else {"] # [doc = "             (key, 3*val + 1)"] # [doc = "         });"] # [doc = " }"] # [doc = ""] # [doc = " let result = variable.complete();"] # [doc = " assert_eq!(result.len(), 74);"] # [doc = " ```"] pub fn from_map < T2 : Ord > (& self , input : & Variable < T2 > , logic : impl FnMut (& T2) -> Tuple) { map :: map_into (input , self , logic) } # [doc = " Adds tuples that result from combining `source` with the"] # [doc = " relations given in `leapers`. This operation is very flexible"] # [doc = " and can be used to do a combination of joins and anti-joins."] # [doc = " The main limitation is that the things being combined must"] # [doc = " consist of one dynamic variable (`source`) and then several"] # [doc = " fixed relations (`leapers`)."] # [doc = ""] # [doc = " The idea is as follows:"] # [doc = ""] # [doc = " - You will be inserting new tuples that result from joining (and anti-joining)"] # [doc = "   some dynamic variable `source` of source tuples (`SourceTuple`)"] # [doc = "   with some set of values (of type `Val`)."] # [doc = " - You provide these values by combining `source` with a set of leapers"] # [doc = "   `leapers`, each of which is derived from a fixed relation. The `leapers`"] # [doc = "   should be either a single leaper (of suitable type) or else a tuple of leapers."] # [doc = "   You can create a leaper in one of two ways:"] # [doc = "   - Extension: In this case, you have a relation of type `(K, Val)` for some"] # [doc = "     type `K`. You provide a closure that maps from `SourceTuple` to the key"] # [doc = "     `K`. If you use `relation.extend_with`, then any `Val` values the"] # [doc = "     relation provides will be added to the set of values; if you use"] # [doc = "     `extend_anti`, then the `Val` values will be removed."] # [doc = "   - Filtering: In this case, you have a relation of type `K` for some"] # [doc = "     type `K` and you provide a closure that maps from `SourceTuple` to"] # [doc = "     the key `K`. Filters don't provide values but they remove source"] # [doc = "     tuples."] # [doc = " - Finally, you get a callback `logic` that accepts each `(SourceTuple, Val)`"] # [doc = "   that was successfully joined (and not filtered) and which maps to the"] # [doc = "   type of this variable."] pub fn from_leapjoin < 'leap , SourceTuple : Ord , Val : Ord + 'leap > (& self , source : & Variable < SourceTuple > , leapers : impl Leapers < 'leap , SourceTuple , Val > , logic : impl FnMut (& SourceTuple , & Val) -> Tuple ,) { self . insert (treefrog :: leapjoin (& source . recent . borrow () , leapers , logic)) ; } }
};
}
