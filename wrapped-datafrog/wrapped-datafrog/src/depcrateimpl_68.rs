// Generated macro for impl_68 (impl)
macro_rules! Depcrateimpl_68 {
() => {
// Module: crate
// Provides: {"impl_68"}
// Dependencies: {}
impl < Tuple : Ord > Variable < Tuple > { fn new (name : & str) -> Self { Variable { distinct : true , name : name . to_string () , stable : Rc :: new (RefCell :: new (Vec :: new ())) , recent : Rc :: new (RefCell :: new (Vec :: new () . into ())) , to_add : Rc :: new (RefCell :: new (Vec :: new ())) , } } # [doc = " Inserts a relation into the variable."] # [doc = ""] # [doc = " This is most commonly used to load initial values into a variable."] # [doc = " it is not obvious that it should be commonly used otherwise, but"] # [doc = " it should not be harmful."] pub fn insert (& self , relation : Relation < Tuple >) { if ! relation . is_empty () { self . to_add . borrow_mut () . push (relation) ; } } # [doc = " Extend the variable with values from the iterator."] # [doc = ""] # [doc = " This is most commonly used to load initial values into a variable."] # [doc = " it is not obvious that it should be commonly used otherwise, but"] # [doc = " it should not be harmful."] pub fn extend < T > (& self , iterator : impl IntoIterator < Item = T >) where Relation < Tuple > : FromIterator < T > , { self . insert (iterator . into_iter () . collect ()) ; } # [doc = " Consumes the variable and returns a relation."] # [doc = ""] # [doc = " This method removes the ability for the variable to develop, and"] # [doc = " flattens all internal tuples down to one relation. The method"] # [doc = " asserts that iteration has completed, in that `self.recent` and"] # [doc = " `self.to_add` should both be empty."] pub fn complete (self) -> Relation < Tuple > { assert ! (self . recent . borrow () . is_empty ()) ; assert ! (self . to_add . borrow () . is_empty ()) ; let mut result : Relation < Tuple > = Vec :: new () . into () ; while let Some (batch) = self . stable . borrow_mut () . pop () { result = result . merge (batch) ; } result } }
};
}
