[package]
name = "bootstrap3"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
anyhow = "1.0"
syn = { version = "2.0", features = ["full", "visit"] }
quote = "1.0"
proc-macro2 = "1.0"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

# We'll include the declaration files directly instead of depending on the wrapped crate
