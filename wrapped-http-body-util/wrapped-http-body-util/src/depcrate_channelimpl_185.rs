// Generated macro for impl_185 (impl)
macro_rules! Depcrate_channelimpl_185 {
() => {
// Module: crate::channel
// Provides: {"impl_185"}
// Dependencies: {}
impl < D , E > Sender < D , E > { # [doc = " Send a frame on the channel."] pub async fn send (& mut self , frame : Frame < D >) -> Result < () , SendError > { self . tx_frame . send (frame) . await . map_err (| _ | SendError) } # [doc = " Send data on data channel."] pub async fn send_data (& mut self , buf : D) -> Result < () , SendError > { self . send (Frame :: data (buf)) . await } # [doc = " Send trailers on trailers channel."] pub async fn send_trailers (& mut self , trailers : HeaderMap) -> Result < () , SendError > { self . send (Frame :: trailers (trailers)) . await } # [doc = " Attempts to send a frame on this channel."] # [doc = ""] # [doc = " This function returns the unsent frame back as an `Err(_)` if the channel could not"] # [doc = " (currently) accept another frame."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " This is mostly useful for when trying to send a frame from outside of an asynchronous"] # [doc = " context. If in an async context, prefer [`Sender::send_data()`] instead."] pub fn try_send (& mut self , frame : Frame < D >) -> Result < () , Frame < D > > { let Self { tx_frame , tx_error : _ , } = self ; tx_frame . try_send (frame) . map_err (tokio :: sync :: mpsc :: error :: TrySendError :: into_inner) } # [doc = " Returns the current capacity of the channel."] # [doc = ""] # [doc = " The capacity goes down when [`Frame<T>`]s are sent. The capacity goes up when these frames"] # [doc = " are received by the corresponding [`Channel<D, E>`]. This is distinct from"] # [doc = " [`max_capacity()`][Self::max_capacity], which always returns the buffer capacity initially"] # [doc = " specified when [`Channel::new()`][Channel::new] was called."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use bytes::Bytes;"] # [doc = " use http_body_util::{BodyExt, channel::Channel};"] # [doc = " use std::convert::Infallible;"] # [doc = ""] # [doc = " #[tokio::main]"] # [doc = " async fn main() {"] # [doc = "    let (mut tx, mut body) = Channel::<Bytes, Infallible>::new(4);"] # [doc = "    assert_eq!(tx.capacity(), 4);"] # [doc = ""] # [doc = "    // Sending a value decreases the available capacity."] # [doc = "    tx.send_data(Bytes::from(\"Hel\")).await.unwrap();"] # [doc = "    assert_eq!(tx.capacity(), 3);"] # [doc = ""] # [doc = "    // Reading a value increases the available capacity."] # [doc = "    let _ = body.frame().await;"] # [doc = "    assert_eq!(tx.capacity(), 4);"] # [doc = " }"] # [doc = " ```"] pub fn capacity (& mut self) -> usize { self . tx_frame . capacity () } # [doc = " Returns the maximum capacity of the channel."] # [doc = ""] # [doc = " This function always returns the buffer capacity initially specified when"] # [doc = " [`Channel::new()`][Channel::new] was called. This is distinct from"] # [doc = " [`capacity()`][Self::capacity], which returns the currently available capacity."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use bytes::Bytes;"] # [doc = " use http_body_util::{BodyExt, channel::Channel};"] # [doc = " use std::convert::Infallible;"] # [doc = ""] # [doc = " #[tokio::main]"] # [doc = " async fn main() {"] # [doc = "    let (mut tx, mut body) = Channel::<Bytes, Infallible>::new(4);"] # [doc = "    assert_eq!(tx.max_capacity(), 4);"] # [doc = ""] # [doc = "    // Sending a value buffers it, but does not affect the maximum capacity reported."] # [doc = "    tx.send_data(Bytes::from(\"Hel\")).await.unwrap();"] # [doc = "    assert_eq!(tx.max_capacity(), 4);"] # [doc = " }"] # [doc = " ```"] pub fn max_capacity (& mut self) -> usize { self . tx_frame . max_capacity () } # [doc = " Aborts the body in an abnormal fashion."] pub fn abort (self , error : E) { self . tx_error . send (error) . ok () ; } }
};
}
