// Generated macro for File (struct)
macro_rules! Depcrate_fs_fileFile {
() => {
// Module: crate::fs::file
// Provides: {"File"}
// Dependencies: {}
# [doc = " A **File** is a wrapper around one of Rust’s `PathBuf` values, along with"] # [doc = " associated data about the file."] # [doc = ""] # [doc = " Each file is definitely going to have its filename displayed at least"] # [doc = " once, have its file extension extracted at least once, and have its metadata"] # [doc = " information queried at least once, so it makes sense to do all this at the"] # [doc = " start and hold on to all the information."] pub struct File < 'dir > { # [doc = " The filename portion of this file’s path, including the extension."] # [doc = ""] # [doc = " This is used to compare against certain filenames (such as checking if"] # [doc = " it’s “Makefile” or something) and to highlight only the filename in"] # [doc = " colour when displaying the path."] pub name : String , # [doc = " The file’s name’s extension, if present, extracted from the name."] # [doc = ""] # [doc = " This is queried many times over, so it’s worth caching it."] pub ext : Option < String > , # [doc = " The path that begat this file."] # [doc = ""] # [doc = " Even though the file’s name is extracted, the path needs to be kept"] # [doc = " around, as certain operations involve looking up the file’s absolute"] # [doc = " location (such as searching for compiled files) or using its original"] # [doc = " path (following a symlink)."] pub path : PathBuf , # [doc = " The cached filetype for this file"] pub filetype : OnceLock < Option < std :: fs :: FileType > > , # [doc = " A cached `metadata` (`stat`) call for this file."] # [doc = ""] # [doc = " This too is queried multiple times, and is *not* cached by the OS, as"] # [doc = " it could easily change between invocations — but exa is so short-lived"] # [doc = " it’s better to just cache it."] pub metadata : OnceLock < io :: Result < std :: fs :: Metadata > > , # [doc = " A reference to the directory that contains this file, if any."] # [doc = ""] # [doc = " Filenames that get passed in on the command-line directly will have no"] # [doc = " parent directory reference — although they technically have one on the"] # [doc = " filesystem, we’ll never need to look at it, so it’ll be `None`."] # [doc = " However, *directories* that get passed in will produce files that"] # [doc = " contain a reference to it, which is used in certain operations (such"] # [doc = " as looking up compiled files)."] pub parent_dir : Option < & 'dir Dir > , # [doc = " Whether this is one of the two `--all all` directories, `.` and `..`."] # [doc = ""] # [doc = " Unlike all other entries, these are not returned as part of the"] # [doc = " directory’s children, and are in fact added specifically by exa; this"] # [doc = " means that they should be skipped when recursing."] pub is_all_all : bool , # [doc = " Whether to dereference symbolic links when querying for information."] # [doc = ""] # [doc = " For instance, when querying the size of a symbolic link, if"] # [doc = " dereferencing is enabled, the size of the target will be displayed"] # [doc = " instead."] pub deref_links : bool , # [doc = " The recursive directory size when `total_size` is used."] recursive_size : RecursiveSize , # [doc = " The extended attributes of this file."] extended_attributes : OnceLock < Vec < Attribute > > , # [doc = " The absolute value of this path, used to look up mount points."] absolute_path : OnceLock < Option < PathBuf > > , }
};
}
