// Generated macro for DirAction (enum)
macro_rules! Depcrate_fs_dir_actionDirAction {
() => {
// Module: crate::fs::dir_action
// Provides: {"DirAction"}
// Dependencies: {}
# [doc = " The action to take when trying to list a file that turns out to be a"] # [doc = " directory."] # [doc = ""] # [doc = " By default, exa will display the information about files passed in as"] # [doc = " command-line arguments, with one file per entry. However, if a directory"] # [doc = " is passed in, exa assumes that the user wants to see its contents, rather"] # [doc = " than the directory itself."] # [doc = ""] # [doc = " This can get annoying sometimes: if a user does `exa ~/Downloads/img-*`"] # [doc = " to see the details of every file starting with `img-`, any directories"] # [doc = " that happen to start with the same will be listed after the files at"] # [doc = " the end in a separate block. By listing directories as files, their"] # [doc = " directory status will be ignored, and both will be listed side-by-side."] # [doc = ""] # [doc = " These two modes have recursive analogues in the “recurse” and “tree”"] # [doc = " modes. Here, instead of just listing the directories, exa will descend"] # [doc = " into them and print out their contents. The recurse mode does this by"] # [doc = " having extra output blocks at the end, while the tree mode will show"] # [doc = " directories inline, with their contents immediately underneath."] # [derive (PartialEq , Eq , Debug , Copy , Clone)] pub enum DirAction { # [doc = " This directory should be listed along with the regular files, instead"] # [doc = " of having its contents queried."] AsFile , # [doc = " This directory should not be listed, and should instead be opened and"] # [doc = " *its* files listed separately. This is the default behaviour."] List , # [doc = " This directory should be listed along with the regular files, and then"] # [doc = " its contents should be listed afterward. The recursive contents of"] # [doc = " *those* contents are dictated by the options argument."] Recurse (RecurseOptions) , }
};
}
