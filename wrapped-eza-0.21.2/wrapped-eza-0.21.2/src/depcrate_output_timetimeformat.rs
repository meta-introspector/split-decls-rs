// Generated macro for TimeFormat (enum)
macro_rules! Depcrate_output_timeTimeFormat {
() => {
// Module: crate::output::time
// Provides: {"TimeFormat"}
// Dependencies: {}
# [doc = " Every timestamp in exa needs to be rendered by a **time format**."] # [doc = " Formatting times is tricky, because how a timestamp is rendered can"] # [doc = " depend on one or more of the following:"] # [doc = ""] # [doc = " - The user’s locale, for printing the month name as “Feb”, or as “fév”,"] # [doc = "   or as “2月”;"] # [doc = " - The current year, because certain formats will be less precise when"] # [doc = "   dealing with dates far in the past;"] # [doc = " - The formatting style that the user asked for on the command-line."] # [doc = ""] # [doc = " Because not all formatting styles need the same data, they all have their"] # [doc = " own enum variants. It’s not worth looking the locale up if the formatter"] # [doc = " prints month names as numbers."] # [doc = ""] # [doc = " Also, eza supports *custom* styles, where the user enters a"] # [doc = " format string in an environment variable or something. Just these four."] # [derive (PartialEq , Eq , Debug , Clone)] pub enum TimeFormat { # [doc = " The **default format** uses the user’s locale to print month names,"] # [doc = " and specifies the timestamp down to the minute for recent times, and"] # [doc = " day for older times."] DefaultFormat , # [doc = " Use the **ISO format**, which specifies the timestamp down to the"] # [doc = " minute for recent times, and day for older times. It uses a number"] # [doc = " for the month so it doesn’t use the locale."] ISOFormat , # [doc = " Use the **long ISO format**, which specifies the timestamp down to the"] # [doc = " minute using only numbers, without needing the locale or year."] LongISO , # [doc = " Use the **full ISO format**, which specifies the timestamp down to the"] # [doc = " millisecond and includes its offset down to the minute. This too uses"] # [doc = " only numbers so doesn’t require any special consideration."] FullISO , # [doc = " Use a relative but fixed width representation."] Relative , # [doc = " Use custom formats, optionally a different custom format can be"] # [doc = " specified for recent times, otherwise the same custom format will be"] # [doc = " used for both recent and non-recent times."] Custom { non_recent : String , recent : Option < String > , } , }
};
}
