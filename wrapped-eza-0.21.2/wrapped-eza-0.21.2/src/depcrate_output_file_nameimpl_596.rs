// Generated macro for impl_596 (impl)
macro_rules! Depcrate_output_file_nameimpl_596 {
() => {
// Module: crate::output::file_name
// Provides: {"impl_596"}
// Dependencies: {}
impl < 'a , 'dir , C : Colours > FileName < 'a , 'dir , C > { # [doc = " Paints the name of the file using the colours, resulting in a vector"] # [doc = " of coloured cells that can be printed to the terminal."] # [doc = ""] # [doc = " This method returns some `TextCellContents`, rather than a `TextCell`,"] # [doc = " because for the last cell in a table, it doesn’t need to have its"] # [doc = " width calculated."] pub fn paint (& self) -> TextCellContents { let mut bits = Vec :: new () ; let (icon_override , filename_style_override) = match self . colours . style_override (self . file) { Some (FileNameStyle { icon , filename }) => (icon , filename) , None => (None , None) , } ; let spaces_count_opt = match self . options . show_icons { ShowIcons :: Always (spaces_count) => Some (spaces_count) , ShowIcons :: Automatic (spaces_count) if self . options . is_a_tty => Some (spaces_count) , _ => None , } ; let should_add_classify_char = match self . options . classify { Classify :: AddFileIndicators => true , Classify :: AutomaticAddFileIndicators if self . options . is_a_tty => true , _ => false , } ; if let Some (spaces_count) = spaces_count_opt { let (style , icon) = match icon_override { Some (icon_override) => (if let Some (style_override) = icon_override . style { style_override } else { iconify_style (self . style ()) } , icon_override . glyph . unwrap_or_else (| | icon_for_file (self . file)) . to_string () ,) , None => (iconify_style (self . style ()) , icon_for_file (self . file) . to_string () ,) , } ; bits . push (style . paint (icon)) ; bits . push (style . paint (" " . repeat (spaces_count as usize))) ; } if self . file . parent_dir . is_none () && self . options . absolute == Absolute :: Off { if let Some (parent) = self . file . path . parent () { self . add_parent_bits (& mut bits , parent) ; } } if ! self . file . name . is_empty () { for bit in self . escaped_file_name (filename_style_override) { bits . push (bit) ; } } if let (LinkStyle :: FullLinkPaths , Some (target)) = (self . link_style , self . target . as_ref ()) { match target { FileTarget :: Ok (target) => { bits . push (Style :: default () . paint (" ")) ; bits . push (self . colours . normal_arrow () . paint ("->")) ; bits . push (Style :: default () . paint (" ")) ; if let Some (parent) = target . path . parent () { self . add_parent_bits (& mut bits , parent) ; } if ! target . name . is_empty () { let target_options = Options { classify : Classify :: JustFilenames , quote_style : QuoteStyle :: QuoteSpaces , show_icons : ShowIcons :: Never , embed_hyperlinks : EmbedHyperlinks :: Off , is_a_tty : self . options . is_a_tty , absolute : Absolute :: Off , } ; let target_name = FileName { file : target , colours : self . colours , target : None , link_style : LinkStyle :: FullLinkPaths , options : target_options , mount_style : MountStyle :: JustDirectoryNames , } ; for bit in target_name . escaped_file_name (filename_style_override) { bits . push (bit) ; } if should_add_classify_char { if let Some (class) = self . classify_char (target) { bits . push (Style :: default () . paint (class)) ; } } } } FileTarget :: Broken (broken_path) => { bits . push (Style :: default () . paint (" ")) ; bits . push (self . colours . broken_symlink () . paint ("->")) ; bits . push (Style :: default () . paint (" ")) ; escape (broken_path . display () . to_string () , & mut bits , self . colours . broken_filename () , self . colours . broken_control_char () , self . options . quote_style ,) ; } FileTarget :: Err (_) => { } } } else if should_add_classify_char { if let Some (class) = self . classify_char (self . file) { bits . push (Style :: default () . paint (class)) ; } } if self . mount_style == MountStyle :: MountInfo { if let Some (mount_details) = self . file . mount_point_info () { bits . push (Style :: default () . paint (" [")) ; bits . push (Style :: default () . paint (mount_details . source . clone ())) ; bits . push (Style :: default () . paint (" (")) ; bits . push (Style :: default () . paint (mount_details . fstype . clone ())) ; bits . push (Style :: default () . paint (")]")) ; } } bits . into () } # [doc = " Adds the bits of the parent path to the given bits vector."] # [doc = " The path gets its characters escaped based on the colours."] fn add_parent_bits (& self , bits : & mut Vec < ANSIString < '_ > > , parent : & Path) { let coconut = parent . components () . count () ; if coconut == 1 && parent . has_root () { bits . push (self . colours . symlink_path () . paint (std :: path :: MAIN_SEPARATOR . to_string ()) ,) ; } else if coconut >= 1 { escape (parent . to_string_lossy () . to_string () , bits , self . colours . symlink_path () , self . colours . control_char () , self . options . quote_style ,) ; bits . push (self . colours . symlink_path () . paint (std :: path :: MAIN_SEPARATOR . to_string ()) ,) ; } } # [doc = " The character to be displayed after a file when classifying is on, if"] # [doc = " the file’s type has one associated with it."] # [cfg (unix)] pub (crate) fn classify_char (& self , file : & File < '_ >) -> Option < & 'static str > { if file . is_executable_file () { Some ("*") } else if file . is_directory () { Some ("/") } else if file . is_pipe () { Some ("|") } else if file . is_link () { Some ("@") } else if file . is_socket () { Some ("=") } else { None } } # [cfg (windows)] pub (crate) fn classify_char (& self , file : & File < '_ >) -> Option < & 'static str > { if file . is_directory () { Some ("/") } else if file . is_link () { Some ("@") } else { None } } # [doc = " Returns at least one ANSI-highlighted string representing this file’s"] # [doc = " name using the given set of colours."] # [doc = ""] # [doc = " If --hyperlink flag is provided, it will escape the filename accordingly."] # [doc = ""] # [doc = " Ordinarily, this will be just one string: the file’s complete name,"] # [doc = " coloured according to its file type. If the name contains control"] # [doc = " characters such as newlines or escapes, though, we can’t just print them"] # [doc = " to the screen directly, because then there’ll be newlines in weird places."] # [doc = ""] # [doc = " So in that situation, those characters will be escaped and highlighted in"] # [doc = " a different colour."] fn escaped_file_name < 'unused > (& self , style_override : Option < Style > ,) -> Vec < ANSIString < 'unused > > { use percent_encoding :: { utf8_percent_encode , CONTROLS } ; const HYPERLINK_START : & str = "\x1B]8;;" ; const HYPERLINK_END : & str = "\x1B\x5C" ; let file_style = style_override . unwrap_or (self . style ()) ; let mut bits = Vec :: new () ; let mut display_hyperlink = false ; if self . options . embed_hyperlinks == EmbedHyperlinks :: On { if let Some (abs_path) = self . file . absolute_path () . and_then (| p | p . as_os_str () . to_str ()) { let abs_path = utf8_percent_encode (abs_path , CONTROLS) . to_string () ; # [cfg (target_os = "windows")] let abs_path = abs_path . strip_prefix ("\\\\?\\") . unwrap_or (& abs_path) ; bits . push (ANSIString :: from (format ! ("{HYPERLINK_START}file://{abs_path}{HYPERLINK_END}"))) ; display_hyperlink = true ; } } escape (self . display_name () , & mut bits , file_style , self . colours . control_char () , self . options . quote_style ,) ; if display_hyperlink { bits . push (ANSIString :: from (format ! ("{HYPERLINK_START}{HYPERLINK_END}"))) ; } bits } # [doc = " Returns the string that should be displayed as the file's name."] fn display_name (& self) -> String { match self . options . absolute { Absolute :: On => std :: env :: current_dir () . ok () . and_then (| p | { path_clean :: clean (p . join (& self . file . path)) . to_str () . map (std :: borrow :: ToOwned :: to_owned) }) , Absolute :: Follow => self . file . absolute_path () . and_then (| p | p . to_str ()) . map (std :: borrow :: ToOwned :: to_owned) , Absolute :: Off => None , } . unwrap_or (self . file . name . clone ()) } # [doc = " Figures out which colour to paint the filename part of the output,"] # [doc = " depending on which “type” of file it appears to be — either from the"] # [doc = " class on the filesystem or from its name. (Or the broken link colour,"] # [doc = " if there’s nowhere else for that fact to be shown.)"] pub fn style (& self) -> Style { if let LinkStyle :: JustFilenames = self . link_style { if let Some (ref target) = self . target { if target . is_broken () { return self . colours . broken_symlink () ; } } } # [rustfmt :: skip] return match self . file { f if f . is_mount_point () => self . colours . mount_point () , f if f . is_directory () => self . colours . directory () , # [cfg (unix)] f if f . is_executable_file () => self . colours . executable_file () , f if f . is_link () => self . colours . symlink () , # [cfg (unix)] f if f . is_pipe () => self . colours . pipe () , # [cfg (unix)] f if f . is_block_device () => self . colours . block_device () , # [cfg (unix)] f if f . is_char_device () => self . colours . char_device () , # [cfg (unix)] f if f . is_socket () => self . colours . socket () , f if ! f . is_file () => self . colours . special () , _ => self . colours . colour_file (self . file) , } ; } # [doc = " For grid's use, to cover the case of hyperlink escape sequences"] pub fn bare_utf8_width (& self) -> usize { UnicodeWidthStr :: width (self . file . name . as_str ()) } }
};
}
