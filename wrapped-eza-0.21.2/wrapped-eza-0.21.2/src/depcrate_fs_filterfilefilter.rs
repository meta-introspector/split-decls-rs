// Generated macro for FileFilter (struct)
macro_rules! Depcrate_fs_filterFileFilter {
() => {
// Module: crate::fs::filter
// Provides: {"FileFilter"}
// Dependencies: {}
# [doc = " The **file filter** processes a list of files before displaying them to"] # [doc = " the user, by removing files they don’t want to see, and putting the list"] # [doc = " in the desired order."] # [doc = ""] # [doc = " Usually a user does not want to see *every* file in the list. The most"] # [doc = " common case is to remove files starting with `.`, which are designated"] # [doc = " as ‘hidden’ files."] # [doc = ""] # [doc = " The special files `.` and `..` files are not actually filtered out, but"] # [doc = " need to be inserted into the list, in a special case."] # [doc = ""] # [doc = " The filter also governs sorting the list. After being filtered, pairs of"] # [doc = " files are compared and sorted based on the result, with the sort field"] # [doc = " performing the comparison."] # [derive (PartialEq , Eq , Debug , Clone)] pub struct FileFilter { # [doc = " The metadata field to sort by."] pub sort_field : SortField , pub flags : Vec < FileFilterFlags > , # [doc = " Which invisible “dot” files to include when listing a directory."] # [doc = ""] # [doc = " Files starting with a single “.” are used to determine “system” or"] # [doc = " “configuration” files that should not be displayed in a regular"] # [doc = " directory listing, and the directory entries “.” and “..” are"] # [doc = " considered extra-special."] # [doc = ""] # [doc = " This came about more or less by a complete historical accident,"] # [doc = " when the original `ls` tried to hide `.` and `..`:"] # [doc = ""] # [doc = " [Linux History: How Dot Files Became Hidden Files](https://linux-audit.com/linux-history-how-dot-files-became-hidden-files/)"] pub dot_filter : DotFilter , # [doc = " Glob patterns to ignore. Any file name that matches *any* of these"] # [doc = " patterns won’t be displayed in the list."] pub ignore_patterns : IgnorePatterns , # [doc = " Whether to ignore Git-ignored patterns."] pub git_ignore : GitIgnore , # [doc = " Whether to ignore symlinks"] pub no_symlinks : bool , # [doc = " Whether to explicitly show symlinks"] pub show_symlinks : bool , }
};
}
