// Generated macro for impl_627 (impl)
macro_rules! Depcrate_output_grid_detailsimpl_627 {
() => {
// Module: crate::output::grid_details
// Provides: {"impl_627"}
// Dependencies: {}
impl < 'a > Render < 'a > { # [doc = " Create a temporary Details render that gets used for the columns of"] # [doc = " the grid-details render that’s being generated."] # [doc = ""] # [doc = " This includes an empty files vector because the files get added to"] # [doc = " the table in *this* file, not in details: we only want to insert every"] # [doc = " *n* files into each column’s table, not all of them."] fn details_for_column (& self) -> DetailsRender < 'a > { # [rustfmt :: skip] return DetailsRender { dir : self . dir , files : Vec :: new () , theme : self . theme , file_style : self . file_style , opts : self . details , recurse : None , filter : self . filter , git_ignoring : self . git_ignoring , git : self . git , git_repos : self . git_repos , } ; } pub fn render < W : Write > (mut self , w : & mut W) -> io :: Result < () > { let options = self . details . table . as_ref () . expect ("Details table options not given!") ; let drender = self . details_for_column () ; let color_scale_info = ColorScaleInformation :: from_color_scale (self . details . color_scale , & self . files , self . filter . dot_filter , self . git , self . git_ignoring , None ,) ; let mut table = self . make_table (options) ; let rows : Vec < _ > = self . files . iter () . map (| file | { let row = table . row_for_file (file , drender . show_xattr_hint (file) , color_scale_info) ; table . add_widths (& row) ; row }) . collect () ; let cells = rows . into_iter () . zip (& self . files) . map (| (row , file) | { let filename = self . file_style . for_file (file , self . theme) . paint () . strings () . to_string () ; let details = table . render (row) . strings () . to_string () ; let padding = " " . repeat (if self . details . header { 4usize . saturating_sub (ansi_width :: ansi_width (& filename)) } else { 0 }) ; format ! ("{details} {filename}{padding}") }) . collect () ; let grid = Grid :: new (cells , GridOptions { filling : Filling :: Spaces (4) , direction : Direction :: TopToBottom , width : self . console_width , } ,) ; if let RowThreshold :: MinimumRows (minimum_rows) = self . row_threshold { if grid . row_count () < minimum_rows { let Self { dir , files , theme , file_style , details : opts , filter , git_ignoring , git , git_repos , .. } = self ; let r = DetailsRender { dir , files , theme , file_style , opts , recurse : None , filter , git_ignoring , git , git_repos , } ; return r . render (w) ; } } if self . details . header { let row = table . header_row () ; let name = TextCell :: paint_str (self . theme . ui . header . unwrap_or_default () , "Name") . strings () . to_string () ; let s = table . render (row) . strings () . to_string () ; let combined_header = format ! ("{s} {name}") ; let header_width = ansi_width :: ansi_width (& combined_header) ; for column_width in grid . column_widths () { let padding = " " . repeat ((column_width + 4) . saturating_sub (header_width)) ; write ! (w , "{combined_header}{padding}") ? ; } writeln ! (w) ? ; } write ! (w , "{grid}") ? ; Ok (()) } fn make_table (& mut self , options : & 'a TableOptions) -> Table < 'a > { match (self . git , self . dir) { (Some (g) , Some (d)) => { if ! g . has_anything_for (& d . path) { self . git = None ; } } (Some (g) , None) => { if ! self . files . iter () . any (| f | g . has_anything_for (& f . path)) { self . git = None ; } } (None , _) => { } } let mut table = Table :: new (options , self . git , self . theme , self . git_repos) ; if self . details . header { let row = table . header_row () ; table . add_widths (& row) ; } table } }
};
}
