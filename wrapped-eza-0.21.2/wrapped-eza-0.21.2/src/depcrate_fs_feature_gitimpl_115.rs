// Generated macro for impl_115 (impl)
macro_rules! Depcrate_fs_feature_gitimpl_115 {
() => {
// Module: crate::fs::feature::git
// Provides: {"impl_115"}
// Dependencies: {}
impl Git { # [doc = " Get either the file or directory status for the given path."] # [doc = " “Prefix lookup” means that it should report an aggregate status of all"] # [doc = " paths starting with the given prefix (in other words, a directory)."] fn status (& self , index : & Path , prefix_lookup : bool) -> f :: Git { if prefix_lookup { self . dir_status (index) } else { self . file_status (index) } } # [doc = " Get the user-facing status of a file."] # [doc = " We check the statuses directly applying to a file, and for the ignored"] # [doc = " status we check if any of its parents directories is ignored by git."] fn file_status (& self , file : & Path) -> f :: Git { let path = reorient (file) ; let s = self . statuses . iter () . filter (| p | { if p . 1 == git2 :: Status :: IGNORED { path . starts_with (& p . 0) } else { p . 0 == path } }) . fold (git2 :: Status :: empty () , | a , b | a | b . 1) ; let staged = index_status (s) ; let unstaged = working_tree_status (s) ; f :: Git { staged , unstaged } } # [doc = " Get the combined, user-facing status of a directory."] # [doc = " Statuses are aggregating (for example, a directory is considered"] # [doc = " modified if any file under it has the status modified), except for"] # [doc = " ignored status which applies to files under (for example, a directory"] # [doc = " is considered ignored if one of its parent directories is ignored)."] fn dir_status (& self , dir : & Path) -> f :: Git { let path = reorient (dir) ; let s = self . statuses . iter () . filter (| p | { if p . 1 == git2 :: Status :: IGNORED { path . starts_with (& p . 0) } else { p . 0 . starts_with (& path) } }) . fold (git2 :: Status :: empty () , | a , b | a | b . 1) ; let staged = index_status (s) ; let unstaged = working_tree_status (s) ; f :: Git { staged , unstaged } } }
};
}
