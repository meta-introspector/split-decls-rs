// Generated macro for impl_379 (impl)
macro_rules! Depcrate_options_viewimpl_379 {
() => {
// Module: crate::options::view
// Provides: {"impl_379"}
// Dependencies: {}
impl TimeTypes { # [doc = " Determine which of a file’s time fields should be displayed for it"] # [doc = " based on the user’s options."] # [doc = ""] # [doc = " There are two separate ways to pick which fields to show: with a"] # [doc = " flag (such as `--modified`) or with a parameter (such as"] # [doc = " `--time=modified`). An error is signaled if both ways are used."] # [doc = ""] # [doc = " It’s valid to show more than one column by passing in more than one"] # [doc = " option, but passing *no* options means that the user just wants to"] # [doc = " see the default set."] fn deduce (matches : & MatchedFlags < '_ >) -> Result < Self , OptionsError > { let possible_word = matches . get (& flags :: TIME) ? ; let modified = matches . has (& flags :: MODIFIED) ? ; let changed = matches . has (& flags :: CHANGED) ? ; let accessed = matches . has (& flags :: ACCESSED) ? ; let created = matches . has (& flags :: CREATED) ? ; let no_time = matches . has (& flags :: NO_TIME) ? ; # [rustfmt :: skip] let time_types = if no_time { Self { modified : false , changed : false , accessed : false , created : false , } } else if let Some (word) = possible_word { if modified { return Err (OptionsError :: Useless (& flags :: MODIFIED , true , & flags :: TIME)) ; } else if changed { return Err (OptionsError :: Useless (& flags :: CHANGED , true , & flags :: TIME)) ; } else if accessed { return Err (OptionsError :: Useless (& flags :: ACCESSED , true , & flags :: TIME)) ; } else if created { return Err (OptionsError :: Useless (& flags :: CREATED , true , & flags :: TIME)) ; } else if word == "mod" || word == "modified" { Self { modified : true , changed : false , accessed : false , created : false } } else if word == "ch" || word == "changed" { Self { modified : false , changed : true , accessed : false , created : false } } else if word == "acc" || word == "accessed" { Self { modified : false , changed : false , accessed : true , created : false } } else if word == "cr" || word == "created" { Self { modified : false , changed : false , accessed : false , created : true } } else { return Err (OptionsError :: BadArgument (& flags :: TIME , word . into ())) ; } } else if modified || changed || accessed || created { Self { modified , changed , accessed , created , } } else { Self :: default () } ; Ok (time_types) } }
};
}
