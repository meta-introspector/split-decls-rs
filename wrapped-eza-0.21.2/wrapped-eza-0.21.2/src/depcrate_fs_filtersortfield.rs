// Generated macro for SortField (enum)
macro_rules! Depcrate_fs_filterSortField {
() => {
// Module: crate::fs::filter
// Provides: {"SortField"}
// Dependencies: {}
# [doc = " User-supplied field to sort by."] # [derive (PartialEq , Eq , Debug , Copy , Clone)] pub enum SortField { # [doc = " Don’t apply any sorting. This is usually used as an optimisation in"] # [doc = " scripts, where the order doesn’t matter."] Unsorted , # [doc = " The file name. This is the default sorting."] Name (SortCase) , # [doc = " The file’s extension, with extensionless files being listed first."] Extension (SortCase) , # [doc = " The file’s size, in bytes."] Size , # [doc = " The file’s inode, which usually corresponds to the order in which"] # [doc = " files were created on the filesystem, more or less."] # [cfg (unix)] FileInode , # [doc = " The time the file was modified (the “mtime”)."] # [doc = ""] # [doc = " As this is stored as a Unix timestamp, rather than a local time"] # [doc = " instance, the time zone does not matter and will only be used to"] # [doc = " display the timestamps, not compare them."] ModifiedDate , # [doc = " The time the file was accessed (the “atime”)."] # [doc = ""] # [doc = " Oddly enough, this field rarely holds the *actual* accessed time."] # [doc = " Recording a read time means writing to the file each time it’s read"] # [doc = " slows the whole operation down, so many systems will only update the"] # [doc = " timestamp in certain circumstances. This has become common enough that"] # [doc = " it’s now expected behaviour!"] # [doc = " <https://unix.stackexchange.com/a/8842>"] AccessedDate , # [doc = " The time the file was changed (the “ctime”)."] # [doc = ""] # [doc = " This field is used to mark the time when a file’s metadata"] # [doc = " changed — its permissions, owners, or link count."] # [doc = ""] # [doc = " In original Unix, this was, however, meant as creation time."] # [doc = " <https://www.bell-labs.com/usr/dmr/www/cacm.html>"] ChangedDate , # [doc = " The time the file was created (the “btime” or “birthtime”)."] CreatedDate , # [doc = " The type of the file: directories, links, pipes, regular, files, etc."] # [doc = ""] # [doc = " Files are ordered according to the `PartialOrd` implementation of"] # [doc = " `fs::fields::Type`, so changing that will change this."] FileType , # [doc = " The “age” of the file, which is the time it was modified sorted"] # [doc = " backwards. The reverse of the `ModifiedDate` ordering!"] # [doc = ""] # [doc = " It turns out that listing the most-recently-modified files first is a"] # [doc = " common-enough use case that it deserves its own variant. This would be"] # [doc = " implemented by just using the modified date and setting the reverse"] # [doc = " flag, but this would make reversing *that* output not work, which is"] # [doc = " bad, even though that’s kind of nonsensical. So it’s its own variant"] # [doc = " that can be reversed like usual."] ModifiedAge , # [doc = " The file's name, however if the name of the file begins with `.`"] # [doc = " ignore the leading `.` and then sort as Name"] NameMixHidden (SortCase) , }
};
}
