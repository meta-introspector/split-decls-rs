// Generated macro for impl_57 (impl)
macro_rules! Depcrate_pbes2impl_57 {
() => {
// Module: crate::pbes2
// Provides: {"impl_57"}
// Dependencies: {}
impl Parameters { # [doc = " Default length of an initialization vector."] # [cfg (feature = "rand_core")] const DEFAULT_IV_LEN : usize = AES_BLOCK_SIZE ; # [doc = " Default length of a salt for password hashing."] # [cfg (feature = "rand_core")] const DEFAULT_SALT_LEN : usize = 16 ; # [doc = " Generate PBES2 parameters using the recommended algorithm settings and"] # [doc = " a randomly generated salt and IV."] # [doc = ""] # [doc = " This is currently an alias for [`Parameters::scrypt`]. See that method"] # [doc = " for more information."] # [cfg (all (feature = "pbes2" , feature = "rand_core"))] pub fn recommended < R : CryptoRng > (rng : & mut R) -> Self { Self :: scrypt (rng) } # [doc = " Generate PBES2 parameters using PBKDF2 as the password hashing"] # [doc = " algorithm, using that algorithm's recommended algorithm settings"] # [doc = " (OWASP recommended default: 600,000 rounds) along with a randomly"] # [doc = " generated salt and IV."] # [doc = ""] # [doc = " This will use AES-256-CBC as the encryption algorithm and SHA-256 as"] # [doc = " the hash function for PBKDF2."] # [cfg (feature = "rand_core")] pub fn pbkdf2 < R : CryptoRng > (rng : & mut R) -> Self { let mut iv = [0u8 ; Self :: DEFAULT_IV_LEN] ; rng . fill_bytes (& mut iv) ; let mut salt = [0u8 ; Self :: DEFAULT_SALT_LEN] ; rng . fill_bytes (& mut salt) ; Self :: pbkdf2_sha256_aes256cbc (600_000 , & salt , iv) . expect ("invalid PBKDF2 parameters") } # [doc = " Initialize PBES2 parameters using PBKDF2-SHA256 as the password-based"] # [doc = " key derivation function and AES-128-CBC as the symmetric cipher."] pub fn pbkdf2_sha256_aes128cbc (pbkdf2_iterations : u32 , pbkdf2_salt : & [u8] , aes_iv : [u8 ; AES_BLOCK_SIZE] ,) -> Result < Self > { let kdf = Pbkdf2Params :: hmac_with_sha256 (pbkdf2_iterations , pbkdf2_salt) ? . into () ; let encryption = EncryptionScheme :: Aes128Cbc { iv : aes_iv } ; Ok (Self { kdf , encryption }) } # [doc = " Initialize PBES2 parameters using PBKDF2-SHA256 as the password-based"] # [doc = " key derivation function and AES-256-CBC as the symmetric cipher."] pub fn pbkdf2_sha256_aes256cbc (pbkdf2_iterations : u32 , pbkdf2_salt : & [u8] , aes_iv : [u8 ; AES_BLOCK_SIZE] ,) -> Result < Self > { let kdf = Pbkdf2Params :: hmac_with_sha256 (pbkdf2_iterations , pbkdf2_salt) ? . into () ; let encryption = EncryptionScheme :: Aes256Cbc { iv : aes_iv } ; Ok (Self { kdf , encryption }) } # [doc = " Generate PBES2 parameters using scrypt as the password hashing"] # [doc = " algorithm, using that algorithm's recommended algorithm settings"] # [doc = " along with a randomly generated salt and IV."] # [doc = ""] # [doc = " This will use AES-256-CBC as the encryption algorithm."] # [doc = ""] # [doc = " scrypt parameters are deliberately chosen to retain compatibility with"] # [doc = " OpenSSL v3. See [RustCrypto/formats#1205] for more information."] # [doc = " Parameter choices are as follows:"] # [doc = ""] # [doc = " - `log_n`: 14"] # [doc = " - `r`: 8"] # [doc = " - `p`: 1"] # [doc = " - salt length: 16"] # [doc = ""] # [doc = " [RustCrypto/formats#1205]: https://github.com/RustCrypto/formats/issues/1205"] # [cfg (all (feature = "pbes2" , feature = "rand_core"))] pub fn scrypt < R : CryptoRng > (rng : & mut R) -> Self { let mut iv = [0u8 ; Self :: DEFAULT_IV_LEN] ; rng . fill_bytes (& mut iv) ; let mut salt = [0u8 ; Self :: DEFAULT_SALT_LEN] ; rng . fill_bytes (& mut salt) ; scrypt :: Params :: new (14 , 8 , 1) . ok () . and_then (| params | Self :: scrypt_aes256cbc (params , & salt , iv) . ok ()) . expect ("invalid scrypt parameters") } # [doc = " Initialize PBES2 parameters using scrypt as the password-based"] # [doc = " key derivation function and AES-128-CBC as the symmetric cipher."] # [doc = ""] # [doc = " For more information on scrypt parameters, see documentation for the"] # [doc = " [`scrypt::Params`] struct."] # [cfg (feature = "pbes2")] pub fn scrypt_aes128cbc (params : scrypt :: Params , salt : & [u8] , aes_iv : [u8 ; AES_BLOCK_SIZE] ,) -> Result < Self > { let kdf = ScryptParams :: from_params_and_salt (params , salt) ? . into () ; let encryption = EncryptionScheme :: Aes128Cbc { iv : aes_iv } ; Ok (Self { kdf , encryption }) } # [doc = " Initialize PBES2 parameters using scrypt as the password-based"] # [doc = " key derivation function and AES-256-CBC as the symmetric cipher."] # [doc = ""] # [doc = " For more information on scrypt parameters, see documentation for the"] # [doc = " [`scrypt::Params`] struct."] # [doc = ""] # [doc = " When in doubt, use `Default::default()` as the [`scrypt::Params`]."] # [doc = " This also avoids the need to import the type from the `scrypt` crate."] # [cfg (feature = "pbes2")] pub fn scrypt_aes256cbc (params : scrypt :: Params , salt : & [u8] , aes_iv : [u8 ; AES_BLOCK_SIZE] ,) -> Result < Self > { let kdf = ScryptParams :: from_params_and_salt (params , salt) ? . into () ; let encryption = EncryptionScheme :: Aes256Cbc { iv : aes_iv } ; Ok (Self { kdf , encryption }) } # [doc = " Initialize PBES2 parameters using scrypt as the password-based"] # [doc = " key derivation function and AES-128-GCM as the symmetric cipher."] # [doc = ""] # [doc = " For more information on scrypt parameters, see documentation for the"] # [doc = " [`scrypt::Params`] struct."] # [cfg (feature = "pbes2")] pub fn scrypt_aes128gcm (params : scrypt :: Params , salt : & [u8] , gcm_nonce : [u8 ; GCM_NONCE_SIZE] ,) -> Result < Self > { let kdf = ScryptParams :: from_params_and_salt (params , salt) ? . into () ; let encryption = EncryptionScheme :: Aes128Gcm { nonce : gcm_nonce } ; Ok (Self { kdf , encryption }) } # [doc = " Initialize PBES2 parameters using scrypt as the password-based"] # [doc = " key derivation function and AES-256-GCM as the symmetric cipher."] # [doc = ""] # [doc = " For more information on scrypt parameters, see documentation for the"] # [doc = " [`scrypt::Params`] struct."] # [cfg (feature = "pbes2")] pub fn scrypt_aes256gcm (params : scrypt :: Params , salt : & [u8] , gcm_nonce : [u8 ; GCM_NONCE_SIZE] ,) -> Result < Self > { let kdf = ScryptParams :: from_params_and_salt (params , salt) ? . into () ; let encryption = EncryptionScheme :: Aes256Gcm { nonce : gcm_nonce } ; Ok (Self { kdf , encryption }) } # [doc = " Attempt to decrypt the given ciphertext, allocating and returning a"] # [doc = " byte vector containing the plaintext."] # [cfg (all (feature = "alloc" , feature = "pbes2"))] pub fn decrypt (& self , password : impl AsRef < [u8] > , ciphertext : & [u8]) -> Result < Vec < u8 > > { let mut buffer = ciphertext . to_vec () ; let pt_len = self . decrypt_in_place (password , & mut buffer) ? . len () ; buffer . truncate (pt_len) ; Ok (buffer) } # [doc = " Attempt to decrypt the given ciphertext in-place using a key derived"] # [doc = " from the provided password and this scheme's parameters."] # [doc = ""] # [doc = " Returns an error if the algorithm specified in this scheme's parameters"] # [doc = " is unsupported, or if the ciphertext is malformed (e.g. not a multiple"] # [doc = " of a block mode's padding)"] # [cfg (feature = "pbes2")] pub fn decrypt_in_place < 'a > (& self , password : impl AsRef < [u8] > , buffer : & 'a mut [u8] ,) -> Result < & 'a [u8] > { encryption :: decrypt_in_place (self , password , buffer) } # [doc = " Encrypt the given plaintext, allocating and returning a vector"] # [doc = " containing the ciphertext."] # [cfg (all (feature = "alloc" , feature = "pbes2"))] pub fn encrypt (& self , password : impl AsRef < [u8] > , plaintext : & [u8]) -> Result < Vec < u8 > > { let mut buffer = Vec :: with_capacity (plaintext . len () + AES_BLOCK_SIZE) ; buffer . extend_from_slice (plaintext) ; buffer . extend_from_slice (& [0u8 ; AES_BLOCK_SIZE]) ; let ct_len = self . encrypt_in_place (password , & mut buffer , plaintext . len ()) ? . len () ; buffer . truncate (ct_len) ; Ok (buffer) } # [doc = " Encrypt the given plaintext in-place using a key derived from the"] # [doc = " provided password and this scheme's parameters, writing the ciphertext"] # [doc = " into the same buffer."] # [cfg (feature = "pbes2")] pub fn encrypt_in_place < 'a > (& self , password : impl AsRef < [u8] > , buffer : & 'a mut [u8] , pos : usize ,) -> Result < & 'a [u8] > { encryption :: encrypt_in_place (self , password , buffer , pos) } }
};
}
