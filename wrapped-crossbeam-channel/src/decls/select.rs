macro_rules! deps {
    () => {
        Select!();
        Block!();
    };
}

macro_rules! select {
    () => {
        deps!();
        # [doc = " Selects from a set of channel operations."] # [doc = ""] # [doc = " This macro allows you to define a set of channel operations, wait until any one of them becomes"] # [doc = " ready, and finally execute it. If multiple operations are ready at the same time, a random one"] # [doc = " among them is selected (i.e. the unbiased selection). Use `select_biased!` for the biased"] # [doc = " selection."] # [doc = ""] # [doc = " It is also possible to define a `default` case that gets executed if none of the operations are"] # [doc = " ready, either right away or for a certain duration of time."] # [doc = ""] # [doc = " An operation is considered to be ready if it doesn't have to block. Note that it is ready even"] # [doc = " when it will simply return an error because the channel is disconnected."] # [doc = ""] # [doc = " The `select!` macro is a convenience wrapper around [`Select`]. However, it cannot select over a"] # [doc = " dynamically created list of channel operations."] # [doc = ""] # [doc = " [`Select`]: super::Select"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Block until a send or a receive operation is selected:"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::{select, unbounded};"] # [doc = ""] # [doc = " let (s1, r1) = unbounded();"] # [doc = " let (s2, r2) = unbounded();"] # [doc = " s1.send(10).unwrap();"] # [doc = ""] # [doc = " // Since both operations are initially ready, a random one will be executed."] # [doc = " select! {"] # [doc = "     recv(r1) -> msg => assert_eq!(msg, Ok(10)),"] # [doc = "     send(s2, 20) -> res => {"] # [doc = "         assert_eq!(res, Ok(()));"] # [doc = "         assert_eq!(r2.recv(), Ok(20));"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Select from a set of operations without blocking:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use crossbeam_channel::{select, unbounded};"] # [doc = ""] # [doc = " let (s1, r1) = unbounded();"] # [doc = " let (s2, r2) = unbounded();"] # [doc = ""] # [doc = " # let t1 ="] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s1.send(10).unwrap();"] # [doc = " });"] # [doc = " # let t2 ="] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_millis(500));"] # [doc = "     s2.send(20).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " // None of the operations are initially ready."] # [doc = " select! {"] # [doc = "     recv(r1) -> msg => panic!(),"] # [doc = "     recv(r2) -> msg => panic!(),"] # [doc = "     default => println!(\"not ready\"),"] # [doc = " }"] # [doc = " # t1.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " # t2.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] # [doc = ""] # [doc = " Select over a set of operations with a timeout:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use crossbeam_channel::{select, unbounded};"] # [doc = ""] # [doc = " let (s1, r1) = unbounded();"] # [doc = " let (s2, r2) = unbounded();"] # [doc = ""] # [doc = " # let t1 ="] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s1.send(10).unwrap();"] # [doc = " });"] # [doc = " # let t2 ="] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_millis(500));"] # [doc = "     s2.send(20).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " // None of the two operations will become ready within 100 milliseconds."] # [doc = " select! {"] # [doc = "     recv(r1) -> msg => panic!(),"] # [doc = "     recv(r2) -> msg => panic!(),"] # [doc = "     default(Duration::from_millis(100)) => println!(\"timed out\"),"] # [doc = " }"] # [doc = " # t1.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " # t2.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] # [doc = ""] # [doc = " Optionally add a receive operation to `select!` using [`never`]:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use crossbeam_channel::{select, never, unbounded};"] # [doc = ""] # [doc = " let (s1, r1) = unbounded();"] # [doc = " let (s2, r2) = unbounded();"] # [doc = ""] # [doc = " # let t1 ="] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s1.send(10).unwrap();"] # [doc = " });"] # [doc = " # let t2 ="] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_millis(500));"] # [doc = "     s2.send(20).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " // This receiver can be a `Some` or a `None`."] # [doc = " let r2 = Some(&r2);"] # [doc = ""] # [doc = " // None of the two operations will become ready within 100 milliseconds."] # [doc = " select! {"] # [doc = "     recv(r1) -> msg => panic!(),"] # [doc = "     recv(r2.unwrap_or(&never())) -> msg => assert_eq!(msg, Ok(20)),"] # [doc = " }"] # [doc = " # t1.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " # t2.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] # [doc = ""] # [doc = " To optionally add a timeout to `select!`, see the [example] for [`never`]."] # [doc = ""] # [doc = " [`never`]: super::never"] # [doc = " [example]: super::never#examples"] # [macro_export] macro_rules ! select { ($ ($ tokens : tt) *) => { { const _IS_BIASED : bool = false ; $ crate :: crossbeam_channel_internal ! ($ ($ tokens) *) } } ; }
    };
}

select!()