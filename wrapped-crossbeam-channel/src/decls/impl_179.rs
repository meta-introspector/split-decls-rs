macro_rules! deps {
    () => {
        Select!();
        TryReadyError!();
        TrySelectError!();
        Sender!();
        Timeout!();
        SelectTimeoutError!();
        Receiver!();
        SelectedOperation!();
        ReadyTimeoutError!();
    };
}

macro_rules! impl_179 {
    () => {
        deps!();
        impl < 'a > Select < 'a > { # [doc = " Creates an empty list of channel operations for selection."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::Select;"] # [doc = ""] # [doc = " let mut sel = Select::new();"] # [doc = ""] # [doc = " // The list of operations is empty, which means no operation can be selected."] # [doc = " assert!(sel.try_select().is_err());"] # [doc = " ```"] pub fn new () -> Self { Self { handles : Vec :: with_capacity (4) , next_index : 0 , biased : false , } } # [doc = " Creates an empty list of channel operations with biased selection."] # [doc = ""] # [doc = " When multiple handles are ready, this will select the operation with the lowest index."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::Select;"] # [doc = ""] # [doc = " let mut sel = Select::new_biased();"] # [doc = ""] # [doc = " // The list of operations is empty, which means no operation can be selected."] # [doc = " assert!(sel.try_select().is_err());"] # [doc = " ```"] pub fn new_biased () -> Self { Self { biased : true , .. Default :: default () } } # [doc = " Adds a send operation."] # [doc = ""] # [doc = " Returns the index of the added operation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::{unbounded, Select};"] # [doc = ""] # [doc = " let (s, r) = unbounded::<i32>();"] # [doc = ""] # [doc = " let mut sel = Select::new();"] # [doc = " let index = sel.send(&s);"] # [doc = " ```"] pub fn send < T > (& mut self , s : & 'a Sender < T >) -> usize { let i = self . next_index ; let ptr = s as * const Sender < _ > as * const u8 ; self . handles . push ((s , i , ptr)) ; self . next_index += 1 ; i } # [doc = " Adds a receive operation."] # [doc = ""] # [doc = " Returns the index of the added operation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::{unbounded, Select};"] # [doc = ""] # [doc = " let (s, r) = unbounded::<i32>();"] # [doc = ""] # [doc = " let mut sel = Select::new();"] # [doc = " let index = sel.recv(&r);"] # [doc = " ```"] pub fn recv < T > (& mut self , r : & 'a Receiver < T >) -> usize { let i = self . next_index ; let ptr = r as * const Receiver < _ > as * const u8 ; self . handles . push ((r , i , ptr)) ; self . next_index += 1 ; i } # [doc = " Removes a previously added operation."] # [doc = ""] # [doc = " This is useful when an operation is selected because the channel got disconnected and we"] # [doc = " want to try again to select a different operation instead."] # [doc = ""] # [doc = " If new operations are added after removing some, the indices of removed operations will not"] # [doc = " be reused."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " An attempt to remove a non-existing or already removed operation will panic."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::{unbounded, Select};"] # [doc = ""] # [doc = " let (s1, r1) = unbounded::<i32>();"] # [doc = " let (_, r2) = unbounded::<i32>();"] # [doc = ""] # [doc = " let mut sel = Select::new();"] # [doc = " let oper1 = sel.recv(&r1);"] # [doc = " let oper2 = sel.recv(&r2);"] # [doc = ""] # [doc = " // Both operations are initially ready, so a random one will be executed."] # [doc = " let oper = sel.select();"] # [doc = " assert_eq!(oper.index(), oper2);"] # [doc = " assert!(oper.recv(&r2).is_err());"] # [doc = " sel.remove(oper2);"] # [doc = ""] # [doc = " s1.send(10).unwrap();"] # [doc = ""] # [doc = " let oper = sel.select();"] # [doc = " assert_eq!(oper.index(), oper1);"] # [doc = " assert_eq!(oper.recv(&r1), Ok(10));"] # [doc = " ```"] pub fn remove (& mut self , index : usize) { assert ! (index < self . next_index , "index out of bounds; {} >= {}" , index , self . next_index ,) ; let i = self . handles . iter () . enumerate () . find (| (_ , (_ , i , _)) | * i == index) . expect ("no operation with this index") . 0 ; self . handles . swap_remove (i) ; } # [doc = " Attempts to select one of the operations without blocking."] # [doc = ""] # [doc = " If an operation is ready, it is selected and returned. If multiple operations are ready at"] # [doc = " the same time, a random one among them is selected. If none of the operations are ready, an"] # [doc = " error is returned."] # [doc = ""] # [doc = " An operation is considered to be ready if it doesn't have to block. Note that it is ready"] # [doc = " even when it will simply return an error because the channel is disconnected."] # [doc = ""] # [doc = " The selected operation must be completed with [`SelectedOperation::send`]"] # [doc = " or [`SelectedOperation::recv`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::{unbounded, Select};"] # [doc = ""] # [doc = " let (s1, r1) = unbounded();"] # [doc = " let (s2, r2) = unbounded();"] # [doc = ""] # [doc = " s1.send(10).unwrap();"] # [doc = " s2.send(20).unwrap();"] # [doc = ""] # [doc = " let mut sel = Select::new();"] # [doc = " let oper1 = sel.recv(&r1);"] # [doc = " let oper2 = sel.recv(&r2);"] # [doc = ""] # [doc = " // Both operations are initially ready, so a random one will be executed."] # [doc = " let oper = sel.try_select();"] # [doc = " match oper {"] # [doc = "     Err(_) => panic!(\"both operations should be ready\"),"] # [doc = "     Ok(oper) => match oper.index() {"] # [doc = "         i if i == oper1 => assert_eq!(oper.recv(&r1), Ok(10)),"] # [doc = "         i if i == oper2 => assert_eq!(oper.recv(&r2), Ok(20)),"] # [doc = "         _ => unreachable!(),"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn try_select (& mut self) -> Result < SelectedOperation < 'a > , TrySelectError > { try_select (& mut self . handles , self . biased) } # [doc = " Blocks until one of the operations becomes ready and selects it."] # [doc = ""] # [doc = " Once an operation becomes ready, it is selected and returned. If multiple operations are"] # [doc = " ready at the same time, a random one among them is selected."] # [doc = ""] # [doc = " An operation is considered to be ready if it doesn't have to block. Note that it is ready"] # [doc = " even when it will simply return an error because the channel is disconnected."] # [doc = ""] # [doc = " The selected operation must be completed with [`SelectedOperation::send`]"] # [doc = " or [`SelectedOperation::recv`]."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if no operations have been added to `Select`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use crossbeam_channel::{unbounded, Select};"] # [doc = ""] # [doc = " let (s1, r1) = unbounded();"] # [doc = " let (s2, r2) = unbounded();"] # [doc = ""] # [doc = " # let t1 ="] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s1.send(10).unwrap();"] # [doc = " });"] # [doc = " # let t2 ="] # [doc = " thread::spawn(move || s2.send(20).unwrap());"] # [doc = ""] # [doc = " let mut sel = Select::new();"] # [doc = " let oper1 = sel.recv(&r1);"] # [doc = " let oper2 = sel.recv(&r2);"] # [doc = ""] # [doc = " // The second operation will be selected because it becomes ready first."] # [doc = " let oper = sel.select();"] # [doc = " match oper.index() {"] # [doc = "     i if i == oper1 => assert_eq!(oper.recv(&r1), Ok(10)),"] # [doc = "     i if i == oper2 => assert_eq!(oper.recv(&r2), Ok(20)),"] # [doc = "     _ => unreachable!(),"] # [doc = " }"] # [doc = " # t1.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " # t2.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] pub fn select (& mut self) -> SelectedOperation < 'a > { select (& mut self . handles , self . biased) } # [doc = " Blocks for a limited time until one of the operations becomes ready and selects it."] # [doc = ""] # [doc = " If an operation becomes ready, it is selected and returned. If multiple operations are"] # [doc = " ready at the same time, a random one among them is selected. If none of the operations"] # [doc = " become ready for the specified duration, an error is returned."] # [doc = ""] # [doc = " An operation is considered to be ready if it doesn't have to block. Note that it is ready"] # [doc = " even when it will simply return an error because the channel is disconnected."] # [doc = ""] # [doc = " The selected operation must be completed with [`SelectedOperation::send`]"] # [doc = " or [`SelectedOperation::recv`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use crossbeam_channel::{unbounded, Select};"] # [doc = ""] # [doc = " let (s1, r1) = unbounded();"] # [doc = " let (s2, r2) = unbounded();"] # [doc = ""] # [doc = " # let t1 ="] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s1.send(10).unwrap();"] # [doc = " });"] # [doc = " # let t2 ="] # [doc = " thread::spawn(move || s2.send(20).unwrap());"] # [doc = ""] # [doc = " let mut sel = Select::new();"] # [doc = " let oper1 = sel.recv(&r1);"] # [doc = " let oper2 = sel.recv(&r2);"] # [doc = ""] # [doc = " // The second operation will be selected because it becomes ready first."] # [doc = " let oper = sel.select_timeout(Duration::from_millis(500));"] # [doc = " match oper {"] # [doc = "     Err(_) => panic!(\"should not have timed out\"),"] # [doc = "     Ok(oper) => match oper.index() {"] # [doc = "         i if i == oper1 => assert_eq!(oper.recv(&r1), Ok(10)),"] # [doc = "         i if i == oper2 => assert_eq!(oper.recv(&r2), Ok(20)),"] # [doc = "         _ => unreachable!(),"] # [doc = "     }"] # [doc = " }"] # [doc = " # t1.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " # t2.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] pub fn select_timeout (& mut self , timeout : Duration ,) -> Result < SelectedOperation < 'a > , SelectTimeoutError > { select_timeout (& mut self . handles , timeout , self . biased) } # [doc = " Blocks until a given deadline, or until one of the operations becomes ready and selects it."] # [doc = ""] # [doc = " If an operation becomes ready, it is selected and returned. If multiple operations are"] # [doc = " ready at the same time, a random one among them is selected. If none of the operations"] # [doc = " become ready before the given deadline, an error is returned."] # [doc = ""] # [doc = " An operation is considered to be ready if it doesn't have to block. Note that it is ready"] # [doc = " even when it will simply return an error because the channel is disconnected."] # [doc = ""] # [doc = " The selected operation must be completed with [`SelectedOperation::send`]"] # [doc = " or [`SelectedOperation::recv`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::{Instant, Duration};"] # [doc = " use crossbeam_channel::{unbounded, Select};"] # [doc = ""] # [doc = " let (s1, r1) = unbounded();"] # [doc = " let (s2, r2) = unbounded();"] # [doc = ""] # [doc = " # let t1 ="] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s1.send(10).unwrap();"] # [doc = " });"] # [doc = " # let t2 ="] # [doc = " thread::spawn(move || s2.send(20).unwrap());"] # [doc = ""] # [doc = " let mut sel = Select::new();"] # [doc = " let oper1 = sel.recv(&r1);"] # [doc = " let oper2 = sel.recv(&r2);"] # [doc = ""] # [doc = " let deadline = Instant::now() + Duration::from_millis(500);"] # [doc = ""] # [doc = " // The second operation will be selected because it becomes ready first."] # [doc = " let oper = sel.select_deadline(deadline);"] # [doc = " match oper {"] # [doc = "     Err(_) => panic!(\"should not have timed out\"),"] # [doc = "     Ok(oper) => match oper.index() {"] # [doc = "         i if i == oper1 => assert_eq!(oper.recv(&r1), Ok(10)),"] # [doc = "         i if i == oper2 => assert_eq!(oper.recv(&r2), Ok(20)),"] # [doc = "         _ => unreachable!(),"] # [doc = "     }"] # [doc = " }"] # [doc = " # t1.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " # t2.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] pub fn select_deadline (& mut self , deadline : Instant ,) -> Result < SelectedOperation < 'a > , SelectTimeoutError > { select_deadline (& mut self . handles , deadline , self . biased) } # [doc = " Attempts to find a ready operation without blocking."] # [doc = ""] # [doc = " If an operation is ready, its index is returned. If multiple operations are ready at the"] # [doc = " same time, a random one among them is chosen. If none of the operations are ready, an error"] # [doc = " is returned."] # [doc = ""] # [doc = " An operation is considered to be ready if it doesn't have to block. Note that it is ready"] # [doc = " even when it will simply return an error because the channel is disconnected."] # [doc = ""] # [doc = " Note that this method might return with success spuriously, so it's a good idea to always"] # [doc = " double check if the operation is really ready."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::{unbounded, Select};"] # [doc = ""] # [doc = " let (s1, r1) = unbounded();"] # [doc = " let (s2, r2) = unbounded();"] # [doc = ""] # [doc = " s1.send(10).unwrap();"] # [doc = " s2.send(20).unwrap();"] # [doc = ""] # [doc = " let mut sel = Select::new();"] # [doc = " let oper1 = sel.recv(&r1);"] # [doc = " let oper2 = sel.recv(&r2);"] # [doc = ""] # [doc = " // Both operations are initially ready, so a random one will be chosen."] # [doc = " match sel.try_ready() {"] # [doc = "     Err(_) => panic!(\"both operations should be ready\"),"] # [doc = "     Ok(i) if i == oper1 => assert_eq!(r1.try_recv(), Ok(10)),"] # [doc = "     Ok(i) if i == oper2 => assert_eq!(r2.try_recv(), Ok(20)),"] # [doc = "     Ok(_) => unreachable!(),"] # [doc = " }"] # [doc = " ```"] pub fn try_ready (& mut self) -> Result < usize , TryReadyError > { match run_ready (& mut self . handles , Timeout :: Now , self . biased) { None => Err (TryReadyError) , Some (index) => Ok (index) , } } # [doc = " Blocks until one of the operations becomes ready."] # [doc = ""] # [doc = " Once an operation becomes ready, its index is returned. If multiple operations are ready at"] # [doc = " the same time, a random one among them is chosen."] # [doc = ""] # [doc = " An operation is considered to be ready if it doesn't have to block. Note that it is ready"] # [doc = " even when it will simply return an error because the channel is disconnected."] # [doc = ""] # [doc = " Note that this method might return with success spuriously, so it's a good idea to always"] # [doc = " double check if the operation is really ready."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if no operations have been added to `Select`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use crossbeam_channel::{unbounded, Select};"] # [doc = ""] # [doc = " let (s1, r1) = unbounded();"] # [doc = " let (s2, r2) = unbounded();"] # [doc = ""] # [doc = " # let t1 ="] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s1.send(10).unwrap();"] # [doc = " });"] # [doc = " # let t2 ="] # [doc = " thread::spawn(move || s2.send(20).unwrap());"] # [doc = ""] # [doc = " let mut sel = Select::new();"] # [doc = " let oper1 = sel.recv(&r1);"] # [doc = " let oper2 = sel.recv(&r2);"] # [doc = ""] # [doc = " // The second operation will be selected because it becomes ready first."] # [doc = " match sel.ready() {"] # [doc = "     i if i == oper1 => assert_eq!(r1.try_recv(), Ok(10)),"] # [doc = "     i if i == oper2 => assert_eq!(r2.try_recv(), Ok(20)),"] # [doc = "     _ => unreachable!(),"] # [doc = " }"] # [doc = " # t1.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " # t2.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] pub fn ready (& mut self) -> usize { if self . handles . is_empty () { panic ! ("no operations have been added to `Select`") ; } run_ready (& mut self . handles , Timeout :: Never , self . biased) . unwrap () } # [doc = " Blocks for a limited time until one of the operations becomes ready."] # [doc = ""] # [doc = " If an operation becomes ready, its index is returned. If multiple operations are ready at"] # [doc = " the same time, a random one among them is chosen. If none of the operations become ready"] # [doc = " for the specified duration, an error is returned."] # [doc = ""] # [doc = " An operation is considered to be ready if it doesn't have to block. Note that it is ready"] # [doc = " even when it will simply return an error because the channel is disconnected."] # [doc = ""] # [doc = " Note that this method might return with success spuriously, so it's a good idea to double"] # [doc = " check if the operation is really ready."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use crossbeam_channel::{unbounded, Select};"] # [doc = ""] # [doc = " let (s1, r1) = unbounded();"] # [doc = " let (s2, r2) = unbounded();"] # [doc = ""] # [doc = " # let t1 ="] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s1.send(10).unwrap();"] # [doc = " });"] # [doc = " # let t2 ="] # [doc = " thread::spawn(move || s2.send(20).unwrap());"] # [doc = ""] # [doc = " let mut sel = Select::new();"] # [doc = " let oper1 = sel.recv(&r1);"] # [doc = " let oper2 = sel.recv(&r2);"] # [doc = ""] # [doc = " // The second operation will be selected because it becomes ready first."] # [doc = " match sel.ready_timeout(Duration::from_millis(500)) {"] # [doc = "     Err(_) => panic!(\"should not have timed out\"),"] # [doc = "     Ok(i) if i == oper1 => assert_eq!(r1.try_recv(), Ok(10)),"] # [doc = "     Ok(i) if i == oper2 => assert_eq!(r2.try_recv(), Ok(20)),"] # [doc = "     Ok(_) => unreachable!(),"] # [doc = " }"] # [doc = " # t1.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " # t2.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] pub fn ready_timeout (& mut self , timeout : Duration) -> Result < usize , ReadyTimeoutError > { match Instant :: now () . checked_add (timeout) { Some (deadline) => self . ready_deadline (deadline) , None => Ok (self . ready ()) , } } # [doc = " Blocks until a given deadline, or until one of the operations becomes ready."] # [doc = ""] # [doc = " If an operation becomes ready, its index is returned. If multiple operations are ready at"] # [doc = " the same time, a random one among them is chosen. If none of the operations become ready"] # [doc = " before the deadline, an error is returned."] # [doc = ""] # [doc = " An operation is considered to be ready if it doesn't have to block. Note that it is ready"] # [doc = " even when it will simply return an error because the channel is disconnected."] # [doc = ""] # [doc = " Note that this method might return with success spuriously, so it's a good idea to double"] # [doc = " check if the operation is really ready."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = " use crossbeam_channel::{unbounded, Select};"] # [doc = ""] # [doc = " let deadline = Instant::now() + Duration::from_millis(500);"] # [doc = ""] # [doc = " let (s1, r1) = unbounded();"] # [doc = " let (s2, r2) = unbounded();"] # [doc = ""] # [doc = " # let t1 ="] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s1.send(10).unwrap();"] # [doc = " });"] # [doc = " # let t2 ="] # [doc = " thread::spawn(move || s2.send(20).unwrap());"] # [doc = ""] # [doc = " let mut sel = Select::new();"] # [doc = " let oper1 = sel.recv(&r1);"] # [doc = " let oper2 = sel.recv(&r2);"] # [doc = ""] # [doc = " // The second operation will be selected because it becomes ready first."] # [doc = " match sel.ready_deadline(deadline) {"] # [doc = "     Err(_) => panic!(\"should not have timed out\"),"] # [doc = "     Ok(i) if i == oper1 => assert_eq!(r1.try_recv(), Ok(10)),"] # [doc = "     Ok(i) if i == oper2 => assert_eq!(r2.try_recv(), Ok(20)),"] # [doc = "     Ok(_) => unreachable!(),"] # [doc = " }"] # [doc = " # t1.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " # t2.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] pub fn ready_deadline (& mut self , deadline : Instant) -> Result < usize , ReadyTimeoutError > { match run_ready (& mut self . handles , Timeout :: At (deadline) , self . biased) { None => Err (ReadyTimeoutError) , Some (index) => Ok (index) , } } }
    };
}

impl_179!()