// Generated macro for impl_304 (impl)
macro_rules! Depcrate_wakerimpl_304 {
() => {
// Module: crate::waker
// Provides: {"impl_304"}
// Dependencies: {}
impl Waker { # [doc = " Creates a new `Waker`."] # [inline] pub (crate) fn new () -> Self { Self { selectors : Vec :: new () , observers : Vec :: new () , } } # [doc = " Registers a select operation."] # [inline] pub (crate) fn register (& mut self , oper : Operation , cx : & Context) { self . register_with_packet (oper , ptr :: null_mut () , cx) ; } # [doc = " Registers a select operation and a packet."] # [inline] pub (crate) fn register_with_packet (& mut self , oper : Operation , packet : * mut () , cx : & Context) { self . selectors . push (Entry { oper , packet , cx : cx . clone () , }) ; } # [doc = " Unregisters a select operation."] # [inline] pub (crate) fn unregister (& mut self , oper : Operation) -> Option < Entry > { if let Some ((i , _)) = self . selectors . iter () . enumerate () . find (| & (_ , entry) | entry . oper == oper) { let entry = self . selectors . remove (i) ; Some (entry) } else { None } } # [doc = " Attempts to find another thread's entry, select the operation, and wake it up."] # [inline] pub (crate) fn try_select (& mut self) -> Option < Entry > { if self . selectors . is_empty () { None } else { let thread_id = current_thread_id () ; self . selectors . iter () . position (| selector | { selector . cx . thread_id () != thread_id && selector . cx . try_select (Selected :: Operation (selector . oper)) . is_ok () && { selector . cx . store_packet (selector . packet) ; selector . cx . unpark () ; true } }) . map (| pos | self . selectors . remove (pos)) } } # [doc = " Returns `true` if there is an entry which can be selected by the current thread."] # [inline] pub (crate) fn can_select (& self) -> bool { if self . selectors . is_empty () { false } else { let thread_id = current_thread_id () ; self . selectors . iter () . any (| entry | { entry . cx . thread_id () != thread_id && entry . cx . selected () == Selected :: Waiting }) } } # [doc = " Registers an operation waiting to be ready."] # [inline] pub (crate) fn watch (& mut self , oper : Operation , cx : & Context) { self . observers . push (Entry { oper , packet : ptr :: null_mut () , cx : cx . clone () , }) ; } # [doc = " Unregisters an operation waiting to be ready."] # [inline] pub (crate) fn unwatch (& mut self , oper : Operation) { self . observers . retain (| e | e . oper != oper) ; } # [doc = " Notifies all operations waiting to be ready."] # [inline] pub (crate) fn notify (& mut self) { for entry in self . observers . drain (..) { if entry . cx . try_select (Selected :: Operation (entry . oper)) . is_ok () { entry . cx . unpark () ; } } } # [doc = " Notifies all registered operations that the channel is disconnected."] # [inline] pub (crate) fn disconnect (& mut self) { for entry in self . selectors . iter () { if entry . cx . try_select (Selected :: Disconnected) . is_ok () { entry . cx . unpark () ; } } self . notify () ; } }
};
}
