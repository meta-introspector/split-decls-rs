// Generated macro for impl_26 (impl)
macro_rules! Depcrate_channelimpl_26 {
() => {
// Module: crate::channel
// Provides: {"impl_26"}
// Dependencies: {}
impl < T > Sender < T > { # [doc = " Attempts to send a message into the channel without blocking."] # [doc = ""] # [doc = " This method will either send a message into the channel immediately or return an error if"] # [doc = " the channel is full or disconnected. The returned error contains the original message."] # [doc = ""] # [doc = " If called on a zero-capacity channel, this method will send the message only if there"] # [doc = " happens to be a receive operation on the other side of the channel at the same time."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::{bounded, TrySendError};"] # [doc = ""] # [doc = " let (s, r) = bounded(1);"] # [doc = ""] # [doc = " assert_eq!(s.try_send(1), Ok(()));"] # [doc = " assert_eq!(s.try_send(2), Err(TrySendError::Full(2)));"] # [doc = ""] # [doc = " drop(r);"] # [doc = " assert_eq!(s.try_send(3), Err(TrySendError::Disconnected(3)));"] # [doc = " ```"] pub fn try_send (& self , msg : T) -> Result < () , TrySendError < T > > { match & self . flavor { SenderFlavor :: Array (chan) => chan . try_send (msg) , SenderFlavor :: List (chan) => chan . try_send (msg) , SenderFlavor :: Zero (chan) => chan . try_send (msg) , } } # [doc = " Blocks the current thread until a message is sent or the channel is disconnected."] # [doc = ""] # [doc = " If the channel is full and not disconnected, this call will block until the send operation"] # [doc = " can proceed. If the channel becomes disconnected, this call will wake up and return an"] # [doc = " error. The returned error contains the original message."] # [doc = ""] # [doc = " If called on a zero-capacity channel, this method will wait for a receive operation to"] # [doc = " appear on the other side of the channel."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use crossbeam_channel::{bounded, SendError};"] # [doc = ""] # [doc = " let (s, r) = bounded(1);"] # [doc = " assert_eq!(s.send(1), Ok(()));"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     assert_eq!(r.recv(), Ok(1));"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     drop(r);"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!(s.send(2), Ok(()));"] # [doc = " assert_eq!(s.send(3), Err(SendError(3)));"] # [doc = " ```"] pub fn send (& self , msg : T) -> Result < () , SendError < T > > { match & self . flavor { SenderFlavor :: Array (chan) => chan . send (msg , None) , SenderFlavor :: List (chan) => chan . send (msg , None) , SenderFlavor :: Zero (chan) => chan . send (msg , None) , } . map_err (| err | match err { SendTimeoutError :: Disconnected (msg) => SendError (msg) , SendTimeoutError :: Timeout (_) => unreachable ! () , }) } # [doc = " Waits for a message to be sent into the channel, but only for a limited time."] # [doc = ""] # [doc = " If the channel is full and not disconnected, this call will block until the send operation"] # [doc = " can proceed or the operation times out. If the channel becomes disconnected, this call will"] # [doc = " wake up and return an error. The returned error contains the original message."] # [doc = ""] # [doc = " If called on a zero-capacity channel, this method will wait for a receive operation to"] # [doc = " appear on the other side of the channel."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use crossbeam_channel::{bounded, SendTimeoutError};"] # [doc = ""] # [doc = " let (s, r) = bounded(0);"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     assert_eq!(r.recv(), Ok(2));"] # [doc = "     drop(r);"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     s.send_timeout(1, Duration::from_millis(500)),"] # [doc = "     Err(SendTimeoutError::Timeout(1)),"] # [doc = " );"] # [doc = " assert_eq!("] # [doc = "     s.send_timeout(2, Duration::from_secs(1)),"] # [doc = "     Ok(()),"] # [doc = " );"] # [doc = " assert_eq!("] # [doc = "     s.send_timeout(3, Duration::from_millis(500)),"] # [doc = "     Err(SendTimeoutError::Disconnected(3)),"] # [doc = " );"] # [doc = " ```"] pub fn send_timeout (& self , msg : T , timeout : Duration) -> Result < () , SendTimeoutError < T > > { match Instant :: now () . checked_add (timeout) { Some (deadline) => self . send_deadline (msg , deadline) , None => self . send (msg) . map_err (SendTimeoutError :: from) , } } # [doc = " Waits for a message to be sent into the channel, but only until a given deadline."] # [doc = ""] # [doc = " If the channel is full and not disconnected, this call will block until the send operation"] # [doc = " can proceed or the operation times out. If the channel becomes disconnected, this call will"] # [doc = " wake up and return an error. The returned error contains the original message."] # [doc = ""] # [doc = " If called on a zero-capacity channel, this method will wait for a receive operation to"] # [doc = " appear on the other side of the channel."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = " use crossbeam_channel::{bounded, SendTimeoutError};"] # [doc = ""] # [doc = " let (s, r) = bounded(0);"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     assert_eq!(r.recv(), Ok(2));"] # [doc = "     drop(r);"] # [doc = " });"] # [doc = ""] # [doc = " let now = Instant::now();"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     s.send_deadline(1, now + Duration::from_millis(500)),"] # [doc = "     Err(SendTimeoutError::Timeout(1)),"] # [doc = " );"] # [doc = " assert_eq!("] # [doc = "     s.send_deadline(2, now + Duration::from_millis(1500)),"] # [doc = "     Ok(()),"] # [doc = " );"] # [doc = " assert_eq!("] # [doc = "     s.send_deadline(3, now + Duration::from_millis(2000)),"] # [doc = "     Err(SendTimeoutError::Disconnected(3)),"] # [doc = " );"] # [doc = " ```"] pub fn send_deadline (& self , msg : T , deadline : Instant) -> Result < () , SendTimeoutError < T > > { match & self . flavor { SenderFlavor :: Array (chan) => chan . send (msg , Some (deadline)) , SenderFlavor :: List (chan) => chan . send (msg , Some (deadline)) , SenderFlavor :: Zero (chan) => chan . send (msg , Some (deadline)) , } } # [doc = " Returns `true` if the channel is empty."] # [doc = ""] # [doc = " Note: Zero-capacity channels are always empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::unbounded;"] # [doc = ""] # [doc = " let (s, r) = unbounded();"] # [doc = " assert!(s.is_empty());"] # [doc = ""] # [doc = " s.send(0).unwrap();"] # [doc = " assert!(!s.is_empty());"] # [doc = " ```"] pub fn is_empty (& self) -> bool { match & self . flavor { SenderFlavor :: Array (chan) => chan . is_empty () , SenderFlavor :: List (chan) => chan . is_empty () , SenderFlavor :: Zero (chan) => chan . is_empty () , } } # [doc = " Returns `true` if the channel is full."] # [doc = ""] # [doc = " Note: Zero-capacity channels are always full."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::bounded;"] # [doc = ""] # [doc = " let (s, r) = bounded(1);"] # [doc = ""] # [doc = " assert!(!s.is_full());"] # [doc = " s.send(0).unwrap();"] # [doc = " assert!(s.is_full());"] # [doc = " ```"] pub fn is_full (& self) -> bool { match & self . flavor { SenderFlavor :: Array (chan) => chan . is_full () , SenderFlavor :: List (chan) => chan . is_full () , SenderFlavor :: Zero (chan) => chan . is_full () , } } # [doc = " Returns the number of messages in the channel."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::unbounded;"] # [doc = ""] # [doc = " let (s, r) = unbounded();"] # [doc = " assert_eq!(s.len(), 0);"] # [doc = ""] # [doc = " s.send(1).unwrap();"] # [doc = " s.send(2).unwrap();"] # [doc = " assert_eq!(s.len(), 2);"] # [doc = " ```"] pub fn len (& self) -> usize { match & self . flavor { SenderFlavor :: Array (chan) => chan . len () , SenderFlavor :: List (chan) => chan . len () , SenderFlavor :: Zero (chan) => chan . len () , } } # [doc = " If the channel is bounded, returns its capacity."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::{bounded, unbounded};"] # [doc = ""] # [doc = " let (s, _) = unbounded::<i32>();"] # [doc = " assert_eq!(s.capacity(), None);"] # [doc = ""] # [doc = " let (s, _) = bounded::<i32>(5);"] # [doc = " assert_eq!(s.capacity(), Some(5));"] # [doc = ""] # [doc = " let (s, _) = bounded::<i32>(0);"] # [doc = " assert_eq!(s.capacity(), Some(0));"] # [doc = " ```"] pub fn capacity (& self) -> Option < usize > { match & self . flavor { SenderFlavor :: Array (chan) => chan . capacity () , SenderFlavor :: List (chan) => chan . capacity () , SenderFlavor :: Zero (chan) => chan . capacity () , } } # [doc = " Returns `true` if senders belong to the same channel."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use crossbeam_channel::unbounded;"] # [doc = ""] # [doc = " let (s, _) = unbounded::<usize>();"] # [doc = ""] # [doc = " let s2 = s.clone();"] # [doc = " assert!(s.same_channel(&s2));"] # [doc = ""] # [doc = " let (s3, _) = unbounded();"] # [doc = " assert!(!s.same_channel(&s3));"] # [doc = " ```"] pub fn same_channel (& self , other : & Self) -> bool { match (& self . flavor , & other . flavor) { (SenderFlavor :: Array (ref a) , SenderFlavor :: Array (ref b)) => a == b , (SenderFlavor :: List (ref a) , SenderFlavor :: List (ref b)) => a == b , (SenderFlavor :: Zero (ref a) , SenderFlavor :: Zero (ref b)) => a == b , _ => false , } } }
};
}
