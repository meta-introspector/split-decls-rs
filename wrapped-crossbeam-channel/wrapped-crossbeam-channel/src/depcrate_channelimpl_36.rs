// Generated macro for impl_36 (impl)
macro_rules! Depcrate_channelimpl_36 {
() => {
// Module: crate::channel
// Provides: {"impl_36"}
// Dependencies: {}
impl < T > Receiver < T > { # [doc = " Attempts to receive a message from the channel without blocking."] # [doc = ""] # [doc = " This method will either receive a message from the channel immediately or return an error"] # [doc = " if the channel is empty."] # [doc = ""] # [doc = " If called on a zero-capacity channel, this method will receive a message only if there"] # [doc = " happens to be a send operation on the other side of the channel at the same time."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::{unbounded, TryRecvError};"] # [doc = ""] # [doc = " let (s, r) = unbounded();"] # [doc = " assert_eq!(r.try_recv(), Err(TryRecvError::Empty));"] # [doc = ""] # [doc = " s.send(5).unwrap();"] # [doc = " drop(s);"] # [doc = ""] # [doc = " assert_eq!(r.try_recv(), Ok(5));"] # [doc = " assert_eq!(r.try_recv(), Err(TryRecvError::Disconnected));"] # [doc = " ```"] pub fn try_recv (& self) -> Result < T , TryRecvError > { match & self . flavor { ReceiverFlavor :: Array (chan) => chan . try_recv () , ReceiverFlavor :: List (chan) => chan . try_recv () , ReceiverFlavor :: Zero (chan) => chan . try_recv () , ReceiverFlavor :: At (chan) => { let msg = chan . try_recv () ; unsafe { mem :: transmute_copy :: < Result < Instant , TryRecvError > , Result < T , TryRecvError > > (& msg ,) } } ReceiverFlavor :: Tick (chan) => { let msg = chan . try_recv () ; unsafe { mem :: transmute_copy :: < Result < Instant , TryRecvError > , Result < T , TryRecvError > > (& msg ,) } } ReceiverFlavor :: Never (chan) => chan . try_recv () , } } # [doc = " Blocks the current thread until a message is received or the channel is empty and"] # [doc = " disconnected."] # [doc = ""] # [doc = " If the channel is empty and not disconnected, this call will block until the receive"] # [doc = " operation can proceed. If the channel is empty and becomes disconnected, this call will"] # [doc = " wake up and return an error."] # [doc = ""] # [doc = " If called on a zero-capacity channel, this method will wait for a send operation to appear"] # [doc = " on the other side of the channel."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use crossbeam_channel::{unbounded, RecvError};"] # [doc = ""] # [doc = " let (s, r) = unbounded();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s.send(5).unwrap();"] # [doc = "     drop(s);"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!(r.recv(), Ok(5));"] # [doc = " assert_eq!(r.recv(), Err(RecvError));"] # [doc = " ```"] pub fn recv (& self) -> Result < T , RecvError > { match & self . flavor { ReceiverFlavor :: Array (chan) => chan . recv (None) , ReceiverFlavor :: List (chan) => chan . recv (None) , ReceiverFlavor :: Zero (chan) => chan . recv (None) , ReceiverFlavor :: At (chan) => { let msg = chan . recv (None) ; unsafe { mem :: transmute_copy :: < Result < Instant , RecvTimeoutError > , Result < T , RecvTimeoutError > , > (& msg) } } ReceiverFlavor :: Tick (chan) => { let msg = chan . recv (None) ; unsafe { mem :: transmute_copy :: < Result < Instant , RecvTimeoutError > , Result < T , RecvTimeoutError > , > (& msg) } } ReceiverFlavor :: Never (chan) => chan . recv (None) , } . map_err (| _ | RecvError) } # [doc = " Waits for a message to be received from the channel, but only for a limited time."] # [doc = ""] # [doc = " If the channel is empty and not disconnected, this call will block until the receive"] # [doc = " operation can proceed or the operation times out. If the channel is empty and becomes"] # [doc = " disconnected, this call will wake up and return an error."] # [doc = ""] # [doc = " If called on a zero-capacity channel, this method will wait for a send operation to appear"] # [doc = " on the other side of the channel."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use crossbeam_channel::{unbounded, RecvTimeoutError};"] # [doc = ""] # [doc = " let (s, r) = unbounded();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s.send(5).unwrap();"] # [doc = "     drop(s);"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     r.recv_timeout(Duration::from_millis(500)),"] # [doc = "     Err(RecvTimeoutError::Timeout),"] # [doc = " );"] # [doc = " assert_eq!("] # [doc = "     r.recv_timeout(Duration::from_secs(1)),"] # [doc = "     Ok(5),"] # [doc = " );"] # [doc = " assert_eq!("] # [doc = "     r.recv_timeout(Duration::from_secs(1)),"] # [doc = "     Err(RecvTimeoutError::Disconnected),"] # [doc = " );"] # [doc = " ```"] pub fn recv_timeout (& self , timeout : Duration) -> Result < T , RecvTimeoutError > { match Instant :: now () . checked_add (timeout) { Some (deadline) => self . recv_deadline (deadline) , None => self . recv () . map_err (RecvTimeoutError :: from) , } } # [doc = " Waits for a message to be received from the channel, but only before a given deadline."] # [doc = ""] # [doc = " If the channel is empty and not disconnected, this call will block until the receive"] # [doc = " operation can proceed or the operation times out. If the channel is empty and becomes"] # [doc = " disconnected, this call will wake up and return an error."] # [doc = ""] # [doc = " If called on a zero-capacity channel, this method will wait for a send operation to appear"] # [doc = " on the other side of the channel."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::{Instant, Duration};"] # [doc = " use crossbeam_channel::{unbounded, RecvTimeoutError};"] # [doc = ""] # [doc = " let (s, r) = unbounded();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s.send(5).unwrap();"] # [doc = "     drop(s);"] # [doc = " });"] # [doc = ""] # [doc = " let now = Instant::now();"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     r.recv_deadline(now + Duration::from_millis(500)),"] # [doc = "     Err(RecvTimeoutError::Timeout),"] # [doc = " );"] # [doc = " assert_eq!("] # [doc = "     r.recv_deadline(now + Duration::from_millis(1500)),"] # [doc = "     Ok(5),"] # [doc = " );"] # [doc = " assert_eq!("] # [doc = "     r.recv_deadline(now + Duration::from_secs(5)),"] # [doc = "     Err(RecvTimeoutError::Disconnected),"] # [doc = " );"] # [doc = " ```"] pub fn recv_deadline (& self , deadline : Instant) -> Result < T , RecvTimeoutError > { match & self . flavor { ReceiverFlavor :: Array (chan) => chan . recv (Some (deadline)) , ReceiverFlavor :: List (chan) => chan . recv (Some (deadline)) , ReceiverFlavor :: Zero (chan) => chan . recv (Some (deadline)) , ReceiverFlavor :: At (chan) => { let msg = chan . recv (Some (deadline)) ; unsafe { mem :: transmute_copy :: < Result < Instant , RecvTimeoutError > , Result < T , RecvTimeoutError > , > (& msg) } } ReceiverFlavor :: Tick (chan) => { let msg = chan . recv (Some (deadline)) ; unsafe { mem :: transmute_copy :: < Result < Instant , RecvTimeoutError > , Result < T , RecvTimeoutError > , > (& msg) } } ReceiverFlavor :: Never (chan) => chan . recv (Some (deadline)) , } } # [doc = " Returns `true` if the channel is empty."] # [doc = ""] # [doc = " Note: Zero-capacity channels are always empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::unbounded;"] # [doc = ""] # [doc = " let (s, r) = unbounded();"] # [doc = ""] # [doc = " assert!(r.is_empty());"] # [doc = " s.send(0).unwrap();"] # [doc = " assert!(!r.is_empty());"] # [doc = " ```"] pub fn is_empty (& self) -> bool { match & self . flavor { ReceiverFlavor :: Array (chan) => chan . is_empty () , ReceiverFlavor :: List (chan) => chan . is_empty () , ReceiverFlavor :: Zero (chan) => chan . is_empty () , ReceiverFlavor :: At (chan) => chan . is_empty () , ReceiverFlavor :: Tick (chan) => chan . is_empty () , ReceiverFlavor :: Never (chan) => chan . is_empty () , } } # [doc = " Returns `true` if the channel is full."] # [doc = ""] # [doc = " Note: Zero-capacity channels are always full."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::bounded;"] # [doc = ""] # [doc = " let (s, r) = bounded(1);"] # [doc = ""] # [doc = " assert!(!r.is_full());"] # [doc = " s.send(0).unwrap();"] # [doc = " assert!(r.is_full());"] # [doc = " ```"] pub fn is_full (& self) -> bool { match & self . flavor { ReceiverFlavor :: Array (chan) => chan . is_full () , ReceiverFlavor :: List (chan) => chan . is_full () , ReceiverFlavor :: Zero (chan) => chan . is_full () , ReceiverFlavor :: At (chan) => chan . is_full () , ReceiverFlavor :: Tick (chan) => chan . is_full () , ReceiverFlavor :: Never (chan) => chan . is_full () , } } # [doc = " Returns the number of messages in the channel."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::unbounded;"] # [doc = ""] # [doc = " let (s, r) = unbounded();"] # [doc = " assert_eq!(r.len(), 0);"] # [doc = ""] # [doc = " s.send(1).unwrap();"] # [doc = " s.send(2).unwrap();"] # [doc = " assert_eq!(r.len(), 2);"] # [doc = " ```"] pub fn len (& self) -> usize { match & self . flavor { ReceiverFlavor :: Array (chan) => chan . len () , ReceiverFlavor :: List (chan) => chan . len () , ReceiverFlavor :: Zero (chan) => chan . len () , ReceiverFlavor :: At (chan) => chan . len () , ReceiverFlavor :: Tick (chan) => chan . len () , ReceiverFlavor :: Never (chan) => chan . len () , } } # [doc = " If the channel is bounded, returns its capacity."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_channel::{bounded, unbounded};"] # [doc = ""] # [doc = " let (_, r) = unbounded::<i32>();"] # [doc = " assert_eq!(r.capacity(), None);"] # [doc = ""] # [doc = " let (_, r) = bounded::<i32>(5);"] # [doc = " assert_eq!(r.capacity(), Some(5));"] # [doc = ""] # [doc = " let (_, r) = bounded::<i32>(0);"] # [doc = " assert_eq!(r.capacity(), Some(0));"] # [doc = " ```"] pub fn capacity (& self) -> Option < usize > { match & self . flavor { ReceiverFlavor :: Array (chan) => chan . capacity () , ReceiverFlavor :: List (chan) => chan . capacity () , ReceiverFlavor :: Zero (chan) => chan . capacity () , ReceiverFlavor :: At (chan) => chan . capacity () , ReceiverFlavor :: Tick (chan) => chan . capacity () , ReceiverFlavor :: Never (chan) => chan . capacity () , } } # [doc = " A blocking iterator over messages in the channel."] # [doc = ""] # [doc = " Each call to [`next`] blocks waiting for the next message and then returns it. However, if"] # [doc = " the channel becomes empty and disconnected, it returns [`None`] without blocking."] # [doc = ""] # [doc = " [`next`]: Iterator::next"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use crossbeam_channel::unbounded;"] # [doc = ""] # [doc = " let (s, r) = unbounded();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     s.send(1).unwrap();"] # [doc = "     s.send(2).unwrap();"] # [doc = "     s.send(3).unwrap();"] # [doc = "     drop(s); // Disconnect the channel."] # [doc = " });"] # [doc = ""] # [doc = " // Collect all messages from the channel."] # [doc = " // Note that the call to `collect` blocks until the sender is dropped."] # [doc = " let v: Vec<_> = r.iter().collect();"] # [doc = ""] # [doc = " assert_eq!(v, [1, 2, 3]);"] # [doc = " ```"] pub fn iter (& self) -> Iter < '_ , T > { Iter { receiver : self } } # [doc = " A non-blocking iterator over messages in the channel."] # [doc = ""] # [doc = " Each call to [`next`] returns a message if there is one ready to be received. The iterator"] # [doc = " never blocks waiting for the next message."] # [doc = ""] # [doc = " [`next`]: Iterator::next"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use crossbeam_channel::unbounded;"] # [doc = ""] # [doc = " let (s, r) = unbounded::<i32>();"] # [doc = ""] # [doc = " # let t ="] # [doc = " thread::spawn(move || {"] # [doc = "     s.send(1).unwrap();"] # [doc = "     thread::sleep(Duration::from_secs(1));"] # [doc = "     s.send(2).unwrap();"] # [doc = "     thread::sleep(Duration::from_secs(2));"] # [doc = "     s.send(3).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " thread::sleep(Duration::from_secs(2));"] # [doc = ""] # [doc = " // Collect all messages from the channel without blocking."] # [doc = " // The third message hasn't been sent yet so we'll collect only the first two."] # [doc = " let v: Vec<_> = r.try_iter().collect();"] # [doc = ""] # [doc = " assert_eq!(v, [1, 2]);"] # [doc = " # t.join().unwrap(); // join thread to avoid https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] pub fn try_iter (& self) -> TryIter < '_ , T > { TryIter { receiver : self } } # [doc = " Returns `true` if receivers belong to the same channel."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use crossbeam_channel::unbounded;"] # [doc = ""] # [doc = " let (_, r) = unbounded::<usize>();"] # [doc = ""] # [doc = " let r2 = r.clone();"] # [doc = " assert!(r.same_channel(&r2));"] # [doc = ""] # [doc = " let (_, r3) = unbounded();"] # [doc = " assert!(!r.same_channel(&r3));"] # [doc = " ```"] pub fn same_channel (& self , other : & Self) -> bool { match (& self . flavor , & other . flavor) { (ReceiverFlavor :: Array (a) , ReceiverFlavor :: Array (b)) => a == b , (ReceiverFlavor :: List (a) , ReceiverFlavor :: List (b)) => a == b , (ReceiverFlavor :: Zero (a) , ReceiverFlavor :: Zero (b)) => a == b , (ReceiverFlavor :: At (a) , ReceiverFlavor :: At (b)) => Arc :: ptr_eq (a , b) , (ReceiverFlavor :: Tick (a) , ReceiverFlavor :: Tick (b)) => Arc :: ptr_eq (a , b) , (ReceiverFlavor :: Never (_) , ReceiverFlavor :: Never (_)) => true , _ => false , } } }
};
}
