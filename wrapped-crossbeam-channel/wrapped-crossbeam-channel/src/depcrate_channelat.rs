// Generated macro for at (function)
macro_rules! Depcrate_channelat {
() => {
// Module: crate::channel
// Provides: {"at"}
// Dependencies: {}
# [doc = " Creates a receiver that delivers a message at a certain instant in time."] # [doc = ""] # [doc = " The channel is bounded with capacity of 1 and never gets disconnected. Exactly one message will"] # [doc = " be sent into the channel at the moment in time `when`. The message is the instant at which it"] # [doc = " is sent, which is the same as `when`. If `when` is in the past, the message will be delivered"] # [doc = " instantly to the receiver."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Using an `at` channel for timeouts:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::time::{Instant, Duration};"] # [doc = " use crossbeam_channel::{at, select, unbounded};"] # [doc = ""] # [doc = " let (s, r) = unbounded::<i32>();"] # [doc = " let deadline = Instant::now() + Duration::from_millis(500);"] # [doc = ""] # [doc = " select! {"] # [doc = "     recv(r) -> msg => println!(\"received {:?}\", msg),"] # [doc = "     recv(at(deadline)) -> _ => println!(\"timed out\"),"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " When the message gets sent:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::time::{Duration, Instant};"] # [doc = " use crossbeam_channel::at;"] # [doc = ""] # [doc = " // Converts a number of milliseconds into a `Duration`."] # [doc = " let ms = |ms| Duration::from_millis(ms);"] # [doc = ""] # [doc = " let start = Instant::now();"] # [doc = " let end = start + ms(100);"] # [doc = ""] # [doc = " let r = at(end);"] # [doc = ""] # [doc = " // This message was sent 100 ms from the start"] # [doc = " assert_eq!(r.recv().unwrap(), end);"] # [doc = " assert!(Instant::now() > start + ms(100));"] # [doc = " ```"] pub fn at (when : Instant) -> Receiver < Instant > { Receiver { flavor : ReceiverFlavor :: At (Arc :: new (flavors :: at :: Channel :: new_deadline (when))) , } }
};
}
