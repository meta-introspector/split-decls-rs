// Generated macro for tick (function)
macro_rules! Depcrate_channeltick {
() => {
// Module: crate::channel
// Provides: {"tick"}
// Dependencies: {}
# [doc = " Creates a receiver that delivers messages periodically."] # [doc = ""] # [doc = " The channel is bounded with capacity of 1 and never gets disconnected. Messages will be"] # [doc = " sent into the channel in intervals of `duration`. Each message is the instant at which it is"] # [doc = " sent."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Using a `tick` channel to periodically print elapsed time:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::time::{Duration, Instant};"] # [doc = " use crossbeam_channel::tick;"] # [doc = ""] # [doc = " let start = Instant::now();"] # [doc = " let ticker = tick(Duration::from_millis(100));"] # [doc = ""] # [doc = " for _ in 0..5 {"] # [doc = "     ticker.recv().unwrap();"] # [doc = "     println!(\"elapsed: {:?}\", start.elapsed());"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " When messages get sent:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::thread;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = " use crossbeam_channel::tick;"] # [doc = ""] # [doc = " // Converts a number of milliseconds into a `Duration`."] # [doc = " let ms = |ms| Duration::from_millis(ms);"] # [doc = ""] # [doc = " // Returns `true` if `a` and `b` are very close `Instant`s."] # [doc = " let eq = |a, b| a + ms(65) > b && b + ms(65) > a;"] # [doc = ""] # [doc = " let start = Instant::now();"] # [doc = " let r = tick(ms(100));"] # [doc = ""] # [doc = " // This message was sent 100 ms from the start and received 100 ms from the start."] # [doc = " assert!(eq(r.recv().unwrap(), start + ms(100)));"] # [doc = " assert!(eq(Instant::now(), start + ms(100)));"] # [doc = ""] # [doc = " thread::sleep(ms(500));"] # [doc = ""] # [doc = " // This message was sent 200 ms from the start and received 600 ms from the start."] # [doc = " assert!(eq(r.recv().unwrap(), start + ms(200)));"] # [doc = " assert!(eq(Instant::now(), start + ms(600)));"] # [doc = ""] # [doc = " // This message was sent 700 ms from the start and received 700 ms from the start."] # [doc = " assert!(eq(r.recv().unwrap(), start + ms(700)));"] # [doc = " assert!(eq(Instant::now(), start + ms(700)));"] # [doc = " ```"] pub fn tick (duration : Duration) -> Receiver < Instant > { match Instant :: now () . checked_add (duration) { Some (delivery_time) => Receiver { flavor : ReceiverFlavor :: Tick (Arc :: new (flavors :: tick :: Channel :: new (delivery_time , duration ,))) , } , None => never () , } }
};
}
