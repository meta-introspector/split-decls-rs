// Generated macro for impl_14 (impl)
macro_rules! Depcrate_path_treeimpl_14 {
() => {
// Module: crate::path_tree
// Provides: {"impl_14"}
// Dependencies: {}
impl Node < LicenseId > { pub (crate) fn simplify (& mut self) { self . merge_directories () ; self . collapse_in_licensed_directories () ; self . merge_directory_licenses () ; self . merge_groups () ; self . remove_empty () ; } # [doc = " Initially, the build() function constructs a list of separate paths from the file"] # [doc = " system root down to each file, like so:"] # [doc = ""] # [doc = " ```text"] # [doc = "         ┌─► ./ ──► compiler/ ──► rustc/ ──► src/ ──► main.rs"] # [doc = "         │"] # [doc = " <root> ─┼─► ./ ──► compiler/ ──► rustc/ ──► Cargo.toml"] # [doc = "         │"] # [doc = "         └─► ./ ──► library/ ───► std/ ──► Cargo.toml"] # [doc = " ```"] # [doc = ""] # [doc = " This pass is responsible for turning that into a proper directory tree:"] # [doc = ""] # [doc = " ```text"] # [doc = "                 ┌─► compiler/ ──► rustc/ ──┬─► src/ ──► main.rs"] # [doc = "                 │                          │"] # [doc = " <root> ──► ./ ──┤                          └─► Cargo.toml"] # [doc = "                 │"] # [doc = "                 └─► library/ ───► std/ ──► Cargo.toml"] # [doc = " ```"] fn merge_directories (& mut self) { match self { Node :: Root { children } | Node :: Directory { children , license : None , .. } => { let mut directories = BTreeMap :: new () ; let mut files = Vec :: new () ; for child in children . drain (..) { match child { Node :: Directory { name , mut children , license : None } => { directories . entry (name) . or_insert_with (Vec :: new) . append (& mut children) ; } file @ Node :: File { .. } => files . push (file) , Node :: Empty => { } Node :: Root { .. } => { panic ! ("can't have a root inside another element") ; } Node :: Group { .. } => { panic ! ("Group should not be present at this stage") ; } Node :: Directory { license : Some (_) , .. } => { panic ! ("license should not be set at this stage") ; } } } children . extend (directories . into_iter () . map (| (name , children) | Node :: Directory { name , children , license : None , })) ; children . append (& mut files) ; for child in & mut * children { child . merge_directories () ; } } Node :: Empty => { } Node :: File { .. } => { } Node :: Group { .. } => { panic ! ("Group should not be present at this stage") ; } Node :: Directory { license : Some (_) , .. } => { panic ! ("license should not be set at this stage") ; } } } # [doc = " In our codebase, most files in a directory have the same license as the other files in that"] # [doc = " same directory, so it's redundant to store licensing metadata for all the files. Instead,"] # [doc = " we can add a license for a whole directory, and only record the exceptions to a directory"] # [doc = " licensing metadata."] # [doc = ""] # [doc = " We cannot instead record only the difference to Rust's standard licensing, as the majority"] # [doc = " of the files in our repository are *not* licensed under Rust's standard licensing due to"] # [doc = " our inclusion of LLVM."] fn collapse_in_licensed_directories (& mut self) { match self { Node :: Directory { children , license , .. } => { for child in & mut * children { child . collapse_in_licensed_directories () ; } let mut licenses_count = BTreeMap :: new () ; for child in & * children { let Some (license) = child . license () else { continue } ; * licenses_count . entry (license) . or_insert (0) += 1 ; } let most_popular_license = licenses_count . into_iter () . max_by_key (| (_ , count) | * count) . map (| (license , _) | license) ; if let Some (most_popular_license) = most_popular_license { children . retain (| child | child . license () != Some (most_popular_license)) ; * license = Some (most_popular_license) ; } } Node :: Root { children } => { for child in & mut * children { child . collapse_in_licensed_directories () ; } } Node :: File { .. } => { } Node :: Group { .. } => panic ! ("group should not be present at this stage") , Node :: Empty => { } } } # [doc = " Reduce the depth of the tree by merging subdirectories with the same license as their"] # [doc = " parent directory into their parent, and adjusting the paths of the children accordingly."] fn merge_directory_licenses (& mut self) { match self { Node :: Root { children } => { for child in & mut * children { child . merge_directory_licenses () ; } } Node :: Directory { children , license , .. } => { let mut to_add = Vec :: new () ; for child in & mut * children { child . merge_directory_licenses () ; let Node :: Directory { name : child_name , children : child_children , license : child_license , } = child else { continue ; } ; if child_license != license { continue ; } for mut child_child in child_children . drain (..) { match & mut child_child { Node :: Root { .. } => { panic ! ("can't have a root inside another element") ; } Node :: Group { .. } => { panic ! ("Group should not be present at this stage") ; } Node :: Directory { name : child_child_name , .. } => { * child_child_name = child_name . join (& child_child_name) ; } Node :: File { name : child_child_name , .. } => { * child_child_name = child_name . join (& child_child_name) ; } Node :: Empty => { } } to_add . push (child_child) ; } * child = Node :: Empty ; } children . append (& mut to_add) ; } Node :: Empty => { } Node :: File { .. } => { } Node :: Group { .. } => panic ! ("Group should not be present at this stage") , } } # [doc = " This pass groups multiple files in a directory with the same license into a single"] # [doc = " \"Group\", so that the license of all those files can be reported as a group."] # [doc = ""] # [doc = " This also merges directories *without exceptions*."] # [doc = ""] # [doc = " Crucially this pass runs after collapse_in_licensed_directories, so the most common license"] # [doc = " will already be marked as the directory's license and won't be turned into a group."] fn merge_groups (& mut self) { # [derive (Default)] struct Grouped { files : Vec < PathBuf > , directories : Vec < PathBuf > , } match self { Node :: Root { children } | Node :: Directory { children , .. } => { let mut grouped : BTreeMap < LicenseId , Grouped > = BTreeMap :: new () ; for child in & mut * children { child . merge_groups () ; match child { Node :: Directory { name , children , license : Some (license) } => { if children . is_empty () { grouped . entry (* license) . or_insert_with (Grouped :: default) . directories . push (name . clone ()) ; * child = Node :: Empty ; } } Node :: File { name , license } => { grouped . entry (* license) . or_insert_with (Grouped :: default) . files . push (name . clone ()) ; * child = Node :: Empty ; } _ => { } } } for (license , mut grouped) in grouped . into_iter () { if grouped . files . len () + grouped . directories . len () <= 1 { if let Some (name) = grouped . files . pop () { children . push (Node :: File { license , name }) ; } else if let Some (name) = grouped . directories . pop () { children . push (Node :: Directory { name , children : Vec :: new () , license : Some (license) , }) ; } } else { children . push (Node :: Group { license , files : grouped . files , directories : grouped . directories , }) ; } } } Node :: File { .. } => { } Node :: Group { .. } => panic ! ("FileGroup should not be present at this stage") , Node :: Empty => { } } } # [doc = " Some nodes were replaced with Node::Empty to mark them for deletion. As the last step, make"] # [doc = " sure to remove them from the tree."] fn remove_empty (& mut self) { match self { Node :: Root { children } | Node :: Directory { children , .. } => { for child in & mut * children { child . remove_empty () ; } children . retain (| child | ! matches ! (child , Node :: Empty)) ; } Node :: Group { .. } => { } Node :: File { .. } => { } Node :: Empty => { } } } fn license (& self) -> Option < LicenseId > { match self { Node :: Directory { children , license : Some (license) , .. } if children . is_empty () => { Some (* license) } Node :: File { license , .. } => Some (* license) , _ => None , } } }
};
}
