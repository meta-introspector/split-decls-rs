macro_rules! deps {
    () => {
        AtomicLoad!();
        AtomicStore!();
    };
}

macro_rules! AtomicCompareExchange {
    () => {
        deps!();
        # [doc = " Atomic compare and exchange."] # [doc = ""] # [doc = " This trait is sealed and cannot be implemented for types outside of `atomic-maybe-uninit`."] # [cfg_attr (not (atomic_maybe_uninit_no_diagnostic_namespace) , diagnostic :: on_unimplemented (message = "atomic compare and exchange of `{Self}` is not available on this target" , label = "this associated function is not available on this target" , note = "see <https://docs.rs/atomic-maybe-uninit/latest/atomic_maybe_uninit/#platform-support> for more."))] pub trait AtomicCompareExchange : AtomicLoad + AtomicStore { # [doc = " Stores a value into `dst` if the current value is the same as"] # [doc = " the `current` value. Here, \"the same\" is determined using byte-wise"] # [doc = " equality, not `PartialEq`."] # [doc = ""] # [doc = " The return value is a tuple of the previous value and the result indicating whether the new"] # [doc = " value was written and containing the previous value. On success, the returned value is"] # [doc = " guaranteed to be equal to the value at `current`."] # [doc = ""] # [doc = " `atomic_compare_exchange` takes two [`Ordering`] arguments to describe the memory"] # [doc = " ordering of this operation. `success` describes the required ordering for the"] # [doc = " read-modify-write operation that takes place if the comparison with `current` succeeds."] # [doc = " `failure` describes the required ordering for the load operation that takes place when"] # [doc = " the comparison fails. Using [`Acquire`] as success ordering makes the store part"] # [doc = " of this operation [`Relaxed`], and using [`Release`] makes the successful load"] # [doc = " [`Relaxed`]. The failure ordering can only be [`SeqCst`], [`Acquire`] or [`Relaxed`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Behavior is undefined if any of the following conditions are violated:"] # [doc = ""] # [doc = " - `dst` must be valid for both reads and writes."] # [doc = " - `dst` must be properly aligned **to the size of `Self`**."] # [doc = "   (For example, if `Self` is `u128`, `dst` must be aligned to 16-byte even if the alignment of `u128` is 8-byte.)"] # [doc = " - `success` must be [`SeqCst`], [`AcqRel`], [`Acquire`], [`Release`], or [`Relaxed`]."] # [doc = " - `failure` must be [`SeqCst`], [`Acquire`], or [`Relaxed`]."] # [doc = ""] # [doc = " The rules for the validity of the pointer follow [the rules applied to"] # [doc = " functions exposed by the standard library's `ptr` module][validity],"] # [doc = " except that concurrent atomic operations on `dst` are allowed if the"] # [doc = " pointer go through [`UnsafeCell::get`]."] # [doc = ""] # [doc = " [validity]: core::ptr#safety"] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " Comparison of two values containing uninitialized bytes may fail even if"] # [doc = " they are equivalent as Rust's type, because values can be byte-wise"] # [doc = " inequal even when they are equal as Rust values."] # [doc = ""] # [doc = " See [`AtomicMaybeUninit::compare_exchange`](crate::AtomicMaybeUninit::compare_exchange) for details."] unsafe fn atomic_compare_exchange (dst : * mut MaybeUninit < Self > , current : MaybeUninit < Self > , new : MaybeUninit < Self > , success : Ordering , failure : Ordering ,) -> (MaybeUninit < Self > , bool) ; # [doc = " Stores a value into `dst` if the current value is the same as"] # [doc = " the `current` value. Here, \"the same\" is determined using byte-wise"] # [doc = " equality, not `PartialEq`."] # [doc = ""] # [doc = " This function is allowed to spuriously fail even when the comparison succeeds, which can"] # [doc = " result in more efficient code on some platforms. The return value is a tuple of the previous"] # [doc = " value and the result indicating whether the new value was written and containing the"] # [doc = " previous value."] # [doc = ""] # [doc = " `atomic_compare_exchange_weak` takes two [`Ordering`] arguments to describe the memory"] # [doc = " ordering of this operation. `success` describes the required ordering for the"] # [doc = " read-modify-write operation that takes place if the comparison with `current` succeeds."] # [doc = " `failure` describes the required ordering for the load operation that takes place when"] # [doc = " the comparison fails. Using [`Acquire`] as success ordering makes the store part"] # [doc = " of this operation [`Relaxed`], and using [`Release`] makes the successful load"] # [doc = " [`Relaxed`]. The failure ordering can only be [`SeqCst`], [`Acquire`] or [`Relaxed`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Behavior is undefined if any of the following conditions are violated:"] # [doc = ""] # [doc = " - `dst` must be valid for both reads and writes."] # [doc = " - `dst` must be properly aligned **to the size of `Self`**."] # [doc = "   (For example, if `Self` is `u128`, `dst` must be aligned to 16-byte even if the alignment of `u128` is 8-byte.)"] # [doc = " - `success` must be [`SeqCst`], [`AcqRel`], [`Acquire`], [`Release`], or [`Relaxed`]."] # [doc = " - `failure` must be [`SeqCst`], [`Acquire`], or [`Relaxed`]."] # [doc = ""] # [doc = " The rules for the validity of the pointer follow [the rules applied to"] # [doc = " functions exposed by the standard library's `ptr` module][validity],"] # [doc = " except that concurrent atomic operations on `dst` are allowed if the"] # [doc = " pointer go through [`UnsafeCell::get`]."] # [doc = ""] # [doc = " [validity]: core::ptr#safety"] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " Comparison of two values containing uninitialized bytes may fail even if"] # [doc = " they are equivalent as Rust's type, because values can be byte-wise"] # [doc = " inequal even when they are equal as Rust values."] # [doc = ""] # [doc = " See [`AtomicMaybeUninit::compare_exchange`](crate::AtomicMaybeUninit::compare_exchange) for details."] # [inline] unsafe fn atomic_compare_exchange_weak (dst : * mut MaybeUninit < Self > , current : MaybeUninit < Self > , new : MaybeUninit < Self > , success : Ordering , failure : Ordering ,) -> (MaybeUninit < Self > , bool) { unsafe { Self :: atomic_compare_exchange (dst , current , new , success , failure) } } }
    };
}

AtomicCompareExchange!();