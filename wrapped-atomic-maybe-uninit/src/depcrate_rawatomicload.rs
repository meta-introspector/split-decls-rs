// Generated macro for AtomicLoad (trait)
macro_rules! Depcrate_rawAtomicLoad {
() => {
// Module: crate::raw
// Provides: {"AtomicLoad"}
// Dependencies: {}
# [doc = " Atomic load."] # [doc = ""] # [doc = " This trait is sealed and cannot be implemented for types outside of `atomic-maybe-uninit`."] # [cfg_attr (not (atomic_maybe_uninit_no_diagnostic_namespace) , diagnostic :: on_unimplemented (message = "atomic load of `{Self}` is not available on this target" , label = "this associated function is not available on this target" , note = "see <https://docs.rs/atomic-maybe-uninit/latest/atomic_maybe_uninit/#platform-support> for more."))] pub trait AtomicLoad : Primitive { # [doc = " Loads a value from `src`."] # [doc = ""] # [doc = " `atomic_load` takes an [`Ordering`] argument which describes the memory ordering of this operation."] # [doc = " Possible values are [`SeqCst`], [`Acquire`] and [`Relaxed`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Behavior is undefined if any of the following conditions are violated:"] # [doc = ""] # [doc = " - `src` must be valid for reads."] # [doc = " - `src` must be properly aligned **to the size of `Self`**."] # [doc = "   (For example, if `Self` is `u128`, `src` must be aligned to 16-byte even if the alignment of `u128` is 8-byte.)"] # [doc = " - `order` must be [`SeqCst`], [`Acquire`], or [`Relaxed`]."] # [doc = ""] # [doc = " The rules for the validity of the pointer follow [the rules applied to"] # [doc = " functions exposed by the standard library's `ptr` module][validity],"] # [doc = " except that concurrent atomic operations on `src` are allowed if the"] # [doc = " pointer go through [`UnsafeCell::get`]."] # [doc = ""] # [doc = " See the [\"Atomic accesses to read-only memory\" section in the `core::sync::atomic` docs][read-only-memory]"] # [doc = " for compatibility with read-only memory."] # [doc = ""] # [doc = " [read-only-memory]: core::sync::atomic#atomic-accesses-to-read-only-memory"] # [doc = " [validity]: core::ptr#safety"] unsafe fn atomic_load (src : * const MaybeUninit < Self > , order : Ordering) -> MaybeUninit < Self > ; }
};
}
