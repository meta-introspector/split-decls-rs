// Generated macro for tests (module)
macro_rules! Depcratetests {
() => {
// Module: crate
// Provides: {"tests"}
// Dependencies: {}
# [doc = " This test is compiled by the Github workflows with the"] # [doc = " filter regex set thusly: CHRONO_TZ_TIMEZONE_FILTER=\"(Europe/London|GMT)\""] # [doc = ""] # [doc = " We use it to check two things:"] # [doc = " 1) That the compiled chrono-tz contains the correct timezones (a compilation"] # [doc = "    failure will result if it doesn't)."] # [doc = " 2) That the compiled chrono-tz DOES NOT contain other, non-matched,"] # [doc = "    timezones. This is rather trickier to do without triggering a compilation"] # [doc = "    failure: we try our best by looking over the TZ_VARIANTS array to try and"] # [doc = "    ascertain if it contains anything obviously wrong."] # [cfg (test)] mod tests { use chrono :: offset :: TimeZone ; use chrono_tz :: { Europe , Europe :: London , Tz , TZ_VARIANTS } ; use std :: str :: FromStr ; # [test] fn london_compiles () { let _london_time = London . with_ymd_and_hms (2013 , 12 , 25 , 14 , 0 , 0) ; assert_eq ! ("Europe/London" , London . name ()) ; assert_eq ! (Tz :: from_str ("Europe/London") , Ok (London)) ; assert_eq ! (Tz :: from_str ("Europe/Isle_of_Man") , Ok (Europe :: Isle_of_Man)) ; assert_eq ! (Tz :: from_str ("Europe/Belfast") , Ok (Europe :: Belfast)) ; } # [test] fn excluded_things_are_missing () { assert ! (Tz :: from_str ("Australia/Melbourne") . is_err ()) ; assert ! (Tz :: from_str ("Indian/Maldives") . is_err ()) ; assert ! (Tz :: from_str ("Mexico/BajaSur") . is_err ()) ; assert ! (Tz :: from_str ("Pacific/Kwajalein") . is_err ()) ; assert ! (Tz :: from_str ("US/Central") . is_err ()) ; assert ! (Tz :: from_str ("Europe/Brussels") . is_err ()) ; assert ! (Tz :: from_str ("Europe/Dublin") . is_err ()) ; assert ! (Tz :: from_str ("Europe/Warsaw") . is_err ()) ; for tz in TZ_VARIANTS . iter () { assert ! (! tz . name () . starts_with ("Africa")) ; assert ! (! tz . name () . starts_with ("Asia")) ; assert ! (! tz . name () . starts_with ("Australia")) ; assert ! (! tz . name () . starts_with ("Canada")) ; assert ! (! tz . name () . starts_with ("Chile")) ; assert ! (! tz . name () . starts_with ("Indian")) ; assert ! (! tz . name () . starts_with ("Mexico")) ; assert ! (! tz . name () . starts_with ("Pacific")) ; assert ! (! tz . name () . starts_with ("US")) ; } } }
};
}
