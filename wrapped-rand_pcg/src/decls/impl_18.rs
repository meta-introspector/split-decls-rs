macro_rules! deps {
    () => {
        Lcg128CmDxsm64!();
    };
}

macro_rules! impl_18 {
    () => {
        deps!();
        impl Lcg128CmDxsm64 { # [doc = " Multi-step advance functions (jump-ahead, jump-back)"] # [doc = ""] # [doc = " The method used here is based on Brown, \"Random Number Generation"] # [doc = " with Arbitrary Stride,\", Transactions of the American Nuclear"] # [doc = " Society (Nov. 1994).  The algorithm is very similar to fast"] # [doc = " exponentiation."] # [doc = ""] # [doc = " Even though delta is an unsigned integer, we can pass a"] # [doc = " signed integer to go backwards, it just goes \"the long way round\"."] # [doc = ""] # [doc = " Using this function is equivalent to calling `next_64()` `delta`"] # [doc = " number of times."] # [inline] pub fn advance (& mut self , delta : u128) { let mut acc_mult : u128 = 1 ; let mut acc_plus : u128 = 0 ; let mut cur_mult = MULTIPLIER as u128 ; let mut cur_plus = self . increment ; let mut mdelta = delta ; while mdelta > 0 { if (mdelta & 1) != 0 { acc_mult = acc_mult . wrapping_mul (cur_mult) ; acc_plus = acc_plus . wrapping_mul (cur_mult) . wrapping_add (cur_plus) ; } cur_plus = cur_mult . wrapping_add (1) . wrapping_mul (cur_plus) ; cur_mult = cur_mult . wrapping_mul (cur_mult) ; mdelta /= 2 ; } self . state = acc_mult . wrapping_mul (self . state) . wrapping_add (acc_plus) ; } # [doc = " Construct an instance compatible with PCG seed and stream."] # [doc = ""] # [doc = " Note that the highest bit of the `stream` parameter is discarded"] # [doc = " to simplify upholding internal invariants."] # [doc = ""] # [doc = " Note that while two generators with different stream parameter may be"] # [doc = " closely correlated, this is [mitigated][upgrading-pcg64] by the DXSM output function."] # [doc = ""] # [doc = " PCG specifies the following default values for both parameters:"] # [doc = ""] # [doc = " - `state = 0xcafef00dd15ea5e5`"] # [doc = " - `stream = 0xa02bdbf7bb3c0a7ac28fa16a64abf96`"] # [doc = ""] # [doc = " [upgrading-pcg64]: https://numpy.org/doc/stable/reference/random/upgrading-pcg64.html"] pub fn new (state : u128 , stream : u128) -> Self { let increment = (stream << 1) | 1 ; Self :: from_state_incr (state , increment) } # [inline] fn from_state_incr (state : u128 , increment : u128) -> Self { let mut pcg = Self { state , increment } ; pcg . state = pcg . state . wrapping_add (pcg . increment) ; pcg . step () ; pcg } # [inline (always)] fn step (& mut self) { self . state = self . state . wrapping_mul (MULTIPLIER as u128) . wrapping_add (self . increment) ; } }
    };
}

impl_18!()