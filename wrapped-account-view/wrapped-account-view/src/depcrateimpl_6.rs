// Generated macro for impl_6 (impl)
macro_rules! Depcrateimpl_6 {
() => {
// Module: crate
// Provides: {"impl_6"}
// Dependencies: {}
impl AccountView { # [doc = " Creates a new [`AccountView`] for a given raw account pointer."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure that the `raw` pointer is valid and points"] # [doc = " to memory containing a `RuntimeAccount` struct, immediately followed by"] # [doc = " the account's data region."] # [inline (always)] pub unsafe fn new_unchecked (raw : * mut RuntimeAccount) -> Self { Self { raw } } # [doc = " Address of the account."] # [inline (always)] pub fn address (& self) -> & Address { unsafe { & (* self . raw) . address } } # [doc = " Return a reference to the address of the program that owns this account."] # [doc = ""] # [doc = " For ownership checks, use the safe `owned_by` method instead."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method is unsafe because it returns a reference to the owner field,"] # [doc = " which can be modified by `assign` and `close` methods. It is undefined"] # [doc = " behavior to use this reference after the account owner has been modified."] # [inline (always)] pub unsafe fn owner (& self) -> & Address { unsafe { & (* self . raw) . owner } } # [doc = " Indicate whether the transaction was signed by this account."] # [inline (always)] pub fn is_signer (& self) -> bool { unsafe { (* self . raw) . is_signer != 0 } } # [doc = " Indicate whether the account is writable or not."] # [inline (always)] pub fn is_writable (& self) -> bool { unsafe { (* self . raw) . is_writable != 0 } } # [doc = " Indicate whether this account represents an executable program"] # [doc = " or not."] # [inline (always)] pub fn executable (& self) -> bool { unsafe { (* self . raw) . executable != 0 } } # [doc = " Return the size of the account data."] # [inline (always)] pub fn data_len (& self) -> usize { unsafe { (* self . raw) . data_len as usize } } # [doc = " Return the delta between the original data length and the current"] # [doc = " data length."] # [doc = ""] # [doc = " This value will be different than zero if the account has been"] # [doc = " resized during the current instruction."] # [inline (always)] pub fn resize_delta (& self) -> i32 { unsafe { (* self . raw) . resize_delta } } # [doc = " Return the lamports in the account."] # [inline (always)] pub fn lamports (& self) -> u64 { unsafe { (* self . raw) . lamports } } # [doc = " Set the lamports in the account."] # [inline (always)] pub fn set_lamports (& self , lamports : u64) { unsafe { (* self . raw) . lamports = lamports ; } } # [doc = " Indicates whether the account data is empty or not."] # [doc = ""] # [doc = " An account is considered empty if the data length is zero."] # [inline (always)] pub fn is_data_empty (& self) -> bool { self . data_len () == 0 } # [doc = " Checks if the account is owned by the given program."] # [inline (always)] pub fn owned_by (& self , program : & Address) -> bool { unsafe { self . owner () == program } } # [doc = " Changes the owner of the account."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " It is undefined behavior to use this method while there is an active reference"] # [doc = " to the `owner` returned by [`Self::owner`]."] # [allow (clippy :: clone_on_copy)] # [inline (always)] pub unsafe fn assign (& self , new_owner : & Address) { write (& mut (* self . raw) . owner , new_owner . clone ()) ; } # [doc = " Return `true` if the account data is borrowed in any form."] # [inline (always)] pub fn is_borrowed (& self) -> bool { unsafe { (* self . raw) . borrow_state != NOT_BORROWED } } # [doc = " Return `true` if the account data is mutably borrowed."] # [inline (always)] pub fn is_borrowed_mut (& self) -> bool { unsafe { (* self . raw) . borrow_state == 0 } } # [doc = " Returns an immutable reference to the data in the account."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method is unsafe because it does not return a `Ref`, thus leaving the borrow"] # [doc = " flag untouched. Useful when an instruction has verified non-duplicate accounts."] # [inline (always)] pub unsafe fn borrow_unchecked (& self) -> & [u8] { from_raw_parts (self . data_ptr () , self . data_len ()) } # [doc = " Returns a mutable reference to the data in the account."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method is unsafe because it does not return a `RefMut`, thus leaving the borrow"] # [doc = " flag untouched. Useful when an instruction has verified non-duplicate accounts."] # [allow (clippy :: mut_from_ref)] # [inline (always)] pub unsafe fn borrow_unchecked_mut (& self) -> & mut [u8] { from_raw_parts_mut (self . data_ptr () , self . data_len ()) } # [doc = " Tries to get an immutable reference to the account data, failing if the account"] # [doc = " is already mutably borrowed."] pub fn try_borrow (& self) -> Result < Ref < '_ , [u8] > , ProgramError > { self . check_borrow () ? ; let borrow_state = self . raw as * mut u8 ; unsafe { * borrow_state -= 1 } ; Ok (Ref { value : unsafe { NonNull :: from (from_raw_parts (self . data_ptr () , self . data_len ())) } , state : unsafe { NonNull :: new_unchecked (borrow_state) } , marker : PhantomData , }) } # [doc = " Tries to get a mutable reference to the account data, failing if the account"] # [doc = " is already borrowed in any form."] pub fn try_borrow_mut (& self) -> Result < RefMut < '_ , [u8] > , ProgramError > { self . check_borrow_mut () ? ; let borrow_state = self . raw as * mut u8 ; unsafe { * borrow_state = 0 } ; Ok (RefMut { value : unsafe { NonNull :: from (from_raw_parts_mut (self . data_ptr () , self . data_len ())) } , state : unsafe { NonNull :: new_unchecked (borrow_state) } , marker : PhantomData , }) } # [doc = " Check if it is possible to get an immutable reference to the account data,"] # [doc = " failing if the account is already mutably borrowed or there are not enough"] # [doc = " immutable borrows available."] # [inline (always)] pub fn check_borrow (& self) -> Result < () , ProgramError > { if unsafe { (* self . raw) . borrow_state } < 2 { return Err (ProgramError :: AccountBorrowFailed) ; } Ok (()) } # [doc = " Checks if it is possible to get a mutable reference to the account data,"] # [doc = " failing if the account is already borrowed in any form."] # [inline (always)] pub fn check_borrow_mut (& self) -> Result < () , ProgramError > { if unsafe { (* self . raw) . borrow_state } != NOT_BORROWED { return Err (ProgramError :: AccountBorrowFailed) ; } Ok (()) } # [doc = " Resize (either truncating or zero extending) the account's data."] # [doc = ""] # [doc = " The account data can be increased by up to [`MAX_PERMITTED_DATA_INCREASE`] bytes"] # [doc = " within an instruction."] # [doc = ""] # [doc = " # Important"] # [doc = ""] # [doc = " This method makes assumptions about the layout and location of memory"] # [doc = " referenced by `RuntimeAccount` fields. It should only be called for"] # [doc = " instances of `AccountView` that were created by the runtime and received"] # [doc = " in the `process_instruction` entrypoint of a program."] # [inline] pub fn resize (& self , new_len : usize) -> Result < () , ProgramError > { self . check_borrow_mut () ? ; unsafe { self . resize_unchecked (new_len) } } # [doc = " Resize (either truncating or zero extending) the account's data."] # [doc = ""] # [doc = " The account data can be increased by up to [`MAX_PERMITTED_DATA_INCREASE`] bytes"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method is unsafe because it does not check if the account data is already"] # [doc = " borrowed. The caller must guarantee that there are no active borrows to the account"] # [doc = " data."] # [inline (always)] pub unsafe fn resize_unchecked (& self , new_len : usize) -> Result < () , ProgramError > { let current_len = self . data_len () as i32 ; let new_len = i32 :: try_from (new_len) . map_err (| _ | ProgramError :: InvalidRealloc) ? ; if new_len == current_len { return Ok (()) ; } let difference = new_len - current_len ; let accumulated_resize_delta = self . resize_delta () + difference ; if accumulated_resize_delta > MAX_PERMITTED_DATA_INCREASE as i32 { return Err (ProgramError :: InvalidRealloc) ; } unsafe { (* self . raw) . data_len = new_len as u64 ; (* self . raw) . resize_delta = accumulated_resize_delta ; } if difference > 0 { unsafe { write_bytes (self . data_ptr () . add (current_len as usize) , 0 , difference as usize ,) ; } } Ok (()) } # [doc = " Zero out the the account's data length, lamports and owner fields, effectively"] # [doc = " closing the account."] # [doc = ""] # [doc = " Note: This does not zero the account data. The account data will be zeroed by"] # [doc = " the runtime at the end of the instruction where the account was closed or at the"] # [doc = " next CPI call."] # [doc = ""] # [doc = " # Important"] # [doc = ""] # [doc = " The lamports must be moved from the account prior to closing it to prevent"] # [doc = " an unbalanced instruction error. Any existing reference to the account owner"] # [doc = " will be invalidated after calling this method."] # [inline] pub fn close (& self) -> ProgramResult { if self . is_borrowed () { return Err (ProgramError :: AccountBorrowFailed) ; } unsafe { (* self . raw) . resize_delta = self . resize_delta () - self . data_len () as i32 ; self . close_unchecked () ; } Ok (()) } # [doc = " Zero out the the account's data length, lamports and owner fields, effectively"] # [doc = " closing the account."] # [doc = ""] # [doc = " Note: This does not zero the account data. The account data will be zeroed by"] # [doc = " the runtime at the end of the instruction where the account was closed or at the"] # [doc = " next CPI call."] # [doc = ""] # [doc = " # Important"] # [doc = ""] # [doc = " The lamports must be moved from the account prior to closing it to prevent"] # [doc = " an unbalanced instruction error."] # [doc = ""] # [doc = " If [`Self::resize`] is called after closing the account, it might incorrectly"] # [doc = " return an error for going over the limit if the account previously had space"] # [doc = " allocated since this method does not update the [`Self::resize_delta`] value."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method is unsafe because it does not check if the account data is already"] # [doc = " borrowed. It should only be called when the account is not being used."] # [doc = ""] # [doc = " It also makes assumptions about the layout and location of memory"] # [doc = " referenced by `RuntimeAccount` fields. It should only be called for"] # [doc = " instances of `AccountView` that were created by the runtime and received"] # [doc = " in the `process_instruction` entrypoint of a program."] # [inline (always)] pub unsafe fn close_unchecked (& self) { write_bytes (self . data_ptr () . sub (48) , 0 , 48) ; } # [doc = " Returns the raw pointer to the `Account` struct."] pub const fn account_ptr (& self) -> * const RuntimeAccount { self . raw } # [doc = " Returns the memory address of the account data."] # [doc = ""] # [doc = " # Important"] # [doc = ""] # [doc = " Obtaining the raw pointer itself is safe, but de-referencing it requires"] # [doc = " the caller to uphold Rust's aliasing rules. It is undefined behavior to"] # [doc = " de-reference the pointer or write through it while any safe reference"] # [doc = " (e.g., from any of `borrow` or `borrow_mut` methods) to the same data"] # [doc = " is still alive."] # [inline (always)] pub fn data_ptr (& self) -> * mut u8 { unsafe { (self . raw as * mut u8) . add (size_of :: < RuntimeAccount > ()) } } }
};
}
