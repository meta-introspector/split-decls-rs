// Generated macro for verify_eq (macro)
macro_rules! Depcrate_assertionsverify_eq {
() => {
// Module: crate::assertions
// Provides: {"verify_eq"}
// Dependencies: {}
# [doc = " Checks whether the second argument is equal to the first argument."] # [doc = ""] # [doc = " Evaluates to `Result::Ok(())` if they are equal and"] # [doc = " `Result::Err(TestAssertionFailure)` if they are not. The caller must then"] # [doc = " decide how to handle the `Err` variant. It has a few options:"] # [doc = "  * Abort the current function with the `?` operator. This requires that the"] # [doc = "    function return a suitable `Result`."] # [doc = "  * Log the test failure and continue by calling the method"] # [doc = "    `and_log_failure`."] # [doc = ""] # [doc = " Of course, one can also use all other standard methods on `Result`."] # [doc = ""] # [doc = " **Invoking this macro by itself does not cause a test failure to be recorded"] # [doc = " or output.** The resulting `Result` must be handled as described above to"] # [doc = " cause the test to be recorded as a failure."] # [doc = ""] # [doc = " Example:"] # [doc = " ```ignore"] # [doc = " use googletest::prelude::*;"] # [doc = ""] # [doc = " #[test]"] # [doc = " fn should_fail() -> Result<()> {"] # [doc = "     verify_eq!(2, 1)"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " This macro has special support for matching against container. Namely:"] # [doc = "  * `verify_eq!(actual, [e1, e2, ...])` is equivalent to"] # [doc = "    `verify_that!(actual, elements_are![eq(e1), eq(e2), ...])`"] # [doc = "  * `verify_eq!(actual, {e1, e2, ...})` is equivalent to"] # [doc = "    `verify_that!(actual, unordered_elements_are![eq(e1), eq(e2), ...])`"] # [macro_export] macro_rules ! verify_eq { ($ actual : expr , [$ (($ ($ tuple_elt : expr) ,*)) ,+ $ (,) ?] $ (,) ?) => { $ crate :: verify_that ! (&$ actual , [$ (($ ($ crate :: matchers :: eq (&$ tuple_elt)) ,*)) ,*]) } ; ($ actual : expr , { $ (($ ($ tuple_elt : expr) ,*)) ,+ $ (,) ? } $ (,) ?) => { $ crate :: verify_that ! (&$ actual , { $ (($ ($ crate :: matchers :: eq (&$ tuple_elt)) ,*)) ,* }) } ; ($ actual : expr , [$ ($ expected : expr) ,+ $ (,) ?] $ (,) ?) => { $ crate :: verify_that ! (&$ actual , [$ ($ crate :: matchers :: eq (&$ expected)) ,*]) } ; ($ actual : expr , { $ ($ expected : expr) ,+ $ (,) ? } $ (,) ?) => { $ crate :: verify_that ! (&$ actual , { $ ($ crate :: matchers :: eq (&$ expected)) ,* }) } ; ($ actual : expr , $ expected : expr $ (,) ?) => { $ crate :: verify_that ! (&$ actual , $ crate :: matchers :: eq (&$ expected)) } ; }
};
}
