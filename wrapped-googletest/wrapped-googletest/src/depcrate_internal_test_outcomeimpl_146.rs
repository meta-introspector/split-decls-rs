// Generated macro for impl_146 (impl)
macro_rules! Depcrate_internal_test_outcomeimpl_146 {
() => {
// Module: crate::internal::test_outcome
// Provides: {"impl_146"}
// Dependencies: {}
impl TestOutcome { # [doc = " Resets the current test's [`TestOutcome`]."] # [doc = ""] # [doc = " This is intended only for use by the attribute macro `#[gtest]`."] # [doc = ""] # [doc = " **For internal use only. API stablility is not guaranteed!**"] # [doc (hidden)] pub fn init_current_test_outcome () { Self :: with_current_test_outcome (| mut current_test_outcome | { * current_test_outcome = Some (TestOutcome :: Success) ; }) } # [doc = " Evaluates the current test's [`TestOutcome`], producing a suitable"] # [doc = " `Result`."] # [doc = ""] # [doc = " The parameter `result` is the value returned by the test function"] # [doc = " itself. This returns `Result::Err` with a `Display`-formatted string of"] # [doc = " the error if `result` is `Result::Err`."] # [doc = ""] # [doc = " Otherwise, this returns `Result::Err` precisely when a test failure has"] # [doc = " been recorded with"] # [doc = " [`and_log_failure`](crate::GoogleTestSupport::and_log_failure)."] # [doc = ""] # [doc = " **For internal use only. API stablility is not guaranteed!**"] # [doc (hidden)] pub fn close_current_test_outcome < E : Display > (inner_result : Result < () , E > ,) -> Result < () , TestFailure > { TestOutcome :: with_current_test_outcome (| mut outcome | { let outer_result = match & * outcome { Some (TestOutcome :: Success) => match inner_result { Ok (()) => Ok (()) , Err (_) => Err (TestFailure) , } , Some (TestOutcome :: Failure) => Err (TestFailure) , None => { panic ! ("No test context found. This indicates a bug in GoogleTest.") } } ; if let Err (fatal_assertion_failure) = inner_result { println ! ("{fatal_assertion_failure}") ; } * outcome = None ; outer_result }) } # [doc = " Returns a `Result` corresponding to the outcome of the currently running"] # [doc = " test."] # [track_caller] pub (crate) fn get_current_test_outcome () -> Result < () , TestAssertionFailure > { TestOutcome :: with_current_test_outcome (| mut outcome | { let outcome = outcome . as_mut () . expect ("No test context found. This indicates a bug in GoogleTest.") ; match outcome { TestOutcome :: Success => Ok (()) , TestOutcome :: Failure => Err (TestAssertionFailure :: create ("Test failed" . into ())) , } }) } # [doc = " Records that the currently running test has failed."] fn fail_current_test () { TestOutcome :: with_current_test_outcome (| mut outcome | { let outcome = outcome . as_mut () . expect ("No test context found. This indicates a bug in GoogleTest.") ; * outcome = TestOutcome :: Failure ; }) } # [doc = " Runs `action` with the [`TestOutcome`] for the currently running test."] # [doc = ""] # [doc = " This is primarily intended for use by assertion macros like"] # [doc = " `expect_that!`."] fn with_current_test_outcome < T > (action : impl FnOnce (RefMut < Option < TestOutcome > >) -> T) -> T { CURRENT_TEST_OUTCOME . with (| current_test_outcome | action (current_test_outcome . borrow_mut ())) } # [doc = " Ensure that there is a test context present and panic if there is not."] pub (crate) fn ensure_test_context_present () { TestOutcome :: with_current_test_outcome (| outcome | { outcome . as_ref () . expect ("
No test context found.
 * Did you annotate the test with gtest?
 * Is the assertion running in the original test thread?
" ,) ; }) } }
};
}
