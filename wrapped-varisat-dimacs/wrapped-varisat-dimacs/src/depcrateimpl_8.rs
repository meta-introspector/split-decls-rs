// Generated macro for impl_8 (impl)
macro_rules! Depcrateimpl_8 {
() => {
// Module: crate
// Provides: {"impl_8"}
// Dependencies: {}
impl DimacsParser { # [doc = " Create a new DIMACS CNF parser."] pub fn new () -> DimacsParser { DimacsParser { formula : CnfFormula :: new () , partial_clause : vec ! [] , header : None , line_number : 1 , clause_count : 0 , partial_lit : 0 , negate_next_lit : false , in_lit : false , in_comment_or_header : false , in_header : false , start_of_line : true , error : false , header_line : vec ! [] , } } # [doc = " Parse the given input and check the header if present."] # [doc = ""] # [doc = " This parses the whole input into a single [`CnfFormula`](varisat_formula::CnfFormula)."] # [doc = " Incremental parsing is possible using [`parse_incremental`](DimacsParser::parse_incremental)"] # [doc = " or the [`parse_chunk`](DimacsParser::parse_chunk) method."] pub fn parse (input : impl io :: Read) -> Result < CnfFormula , Error > { Ok (Self :: parse_incremental (input , | _ | Ok (())) ? . take_formula ()) } # [doc = " Parse the given input incrementally and check the header if present."] # [doc = ""] # [doc = " The callback is invoked repeatedly with a reference to the parser. The callback can process"] # [doc = " the formula incrementally by calling [`take_formula`](DimacsParser::take_formula) on the"] # [doc = " passed argument."] pub fn parse_incremental (input : impl io :: Read , mut callback : impl FnMut (& mut DimacsParser) -> Result < () , Error > ,) -> Result < DimacsParser , Error > { use io :: BufRead ; let mut buffer = io :: BufReader :: new (input) ; let mut parser = Self :: new () ; loop { let data = buffer . fill_buf () ? ; if data . is_empty () { break ; } parser . parse_chunk (data) ? ; let len = data . len () ; buffer . consume (len) ; callback (& mut parser) ? ; } parser . eof () ? ; callback (& mut parser) ? ; parser . check_header () ? ; Ok (parser) } # [doc = " Parse a chunk of input."] # [doc = ""] # [doc = " After parsing the last chunk call the [`eof`](DimacsParser::eof) method."] # [doc = ""] # [doc = " If this method returns an error, the parser is in an invalid state and cannot parse further"] # [doc = " chunks."] pub fn parse_chunk (& mut self , chunk : & [u8]) -> Result < () , ParserError > { if self . error { return Err (ParserError :: PreviousError) ; } for & byte in chunk . iter () { if byte == b'\n' { self . line_number += 1 ; } match byte { b'\n' | b'\r' if self . in_comment_or_header => { if self . in_header { self . in_header = false ; self . parse_header_line () ? ; } self . in_comment_or_header = false ; self . start_of_line = true } _ if self . in_comment_or_header => { if self . in_header { self . header_line . push (byte) ; } } b'0' ..= b'9' => { self . in_lit = true ; let digit = (byte - b'0') as usize ; const CAN_OVERFLOW : usize = Var :: max_count () / 10 ; const OVERFLOW_DIGIT : usize = Var :: max_count () % 10 ; if CAN_OVERFLOW <= self . partial_lit { let carry = (digit <= OVERFLOW_DIGIT) as usize ; if CAN_OVERFLOW + carry <= self . partial_lit { self . error = true ; return Err (ParserError :: LiteralTooLarge { line : self . line_number , index : self . partial_lit , final_digit : digit , }) ; } } self . partial_lit = self . partial_lit * 10 + digit ; self . start_of_line = false } b'-' if ! self . negate_next_lit && ! self . in_lit => { self . negate_next_lit = true ; self . start_of_line = false } b' ' | b'\n' | b'\r' if ! self . negate_next_lit || self . in_lit => { self . finish_literal () ; self . negate_next_lit = false ; self . in_lit = false ; self . partial_lit = 0 ; self . start_of_line = byte != b' ' ; } b'c' if self . start_of_line => { self . in_comment_or_header = true ; } b'p' if self . start_of_line && self . header . is_none () => { self . in_comment_or_header = true ; self . in_header = true ; self . header_line . push (b'p') ; } _ => { self . error = true ; return Err (ParserError :: UnexpectedInput { line : self . line_number , unexpected : byte as char , }) ; } } } Ok (()) } # [doc = " Finish parsing the input."] # [doc = ""] # [doc = " This does not check whether the header information was correct, call"] # [doc = " [`check_header`](DimacsParser::check_header) for this."] pub fn eof (& mut self) -> Result < () , ParserError > { if self . in_header { self . parse_header_line () ? ; } self . finish_literal () ; if ! self . partial_clause . is_empty () { return Err (ParserError :: UnterminatedClause { line : self . line_number , }) ; } Ok (()) } # [doc = " Verifies the header information when present."] # [doc = ""] # [doc = " Does nothing when the input doesn't contain a header."] pub fn check_header (& self) -> Result < () , ParserError > { if let Some (header) = self . header { let var_count = self . formula . var_count () ; if var_count != header . var_count { return Err (ParserError :: VarCount { var_count , header_var_count : header . var_count , }) ; } if self . clause_count != header . clause_count { return Err (ParserError :: ClauseCount { clause_count : self . clause_count , header_clause_count : header . clause_count , }) ; } } Ok (()) } # [doc = " Returns the subformula of everything parsed since the last call to this method."] # [doc = ""] # [doc = " To parse the whole input into a single [`CnfFormula`](varisat_formula::CnfFormula), simply"] # [doc = " call this method once after calling [`eof`](DimacsParser::eof). For incremental parsing this"] # [doc = " method can be invoked after each call of [`parse_chunk`](DimacsParser::parse_chunk)."] # [doc = ""] # [doc = " The variable count of the returned formula will be the maximum of the variable count so far"] # [doc = " and the variable count of the header if present."] pub fn take_formula (& mut self) -> CnfFormula { let mut new_formula = CnfFormula :: new () ; new_formula . set_var_count (self . formula . var_count ()) ; replace (& mut self . formula , new_formula) } # [doc = " Return the DIMACS CNF header data if present."] pub fn header (& self) -> Option < DimacsHeader > { self . header } # [doc = " Number of clauses parsed."] pub fn clause_count (& self) -> usize { self . clause_count } # [doc = " Number of variables in the parsed formula."] pub fn var_count (& self) -> usize { self . formula . var_count () } fn finish_literal (& mut self) { if self . in_lit { if self . partial_lit == 0 { self . formula . add_clause (& self . partial_clause) ; self . partial_clause . clear () ; self . clause_count += 1 ; } else { self . partial_clause . push (Var :: from_dimacs (self . partial_lit as isize) . lit (! self . negate_next_lit)) ; } } } fn parse_header_line (& mut self) -> Result < () , ParserError > { let header_line = String :: from_utf8_lossy (& self . header_line) . into_owned () ; if ! header_line . starts_with ("p ") { return self . invalid_header (header_line) ; } let mut header_values = header_line [2 ..] . split_whitespace () ; if header_values . next () != Some ("cnf") { return self . invalid_header (header_line) ; } let var_count : usize = match header_values . next () . and_then (| value | str :: parse (value) . ok ()) { None => return self . invalid_header (header_line) , Some (value) => value , } ; if var_count > Var :: max_count () { self . error = true ; return Err (ParserError :: LiteralTooLarge { line : self . line_number , index : var_count / 10 , final_digit : var_count % 10 , }) ; } let clause_count : usize = match header_values . next () . and_then (| value | str :: parse (value) . ok ()) { None => return self . invalid_header (header_line) , Some (value) => value , } ; if header_values . next () . is_some () { return self . invalid_header (header_line) ; } self . header = Some (DimacsHeader { var_count , clause_count , }) ; self . formula . set_var_count (var_count) ; Ok (()) } fn invalid_header (& mut self , header_line : String) -> Result < () , ParserError > { self . error = true ; Err (ParserError :: InvalidHeader { line : self . line_number , header : header_line , }) } }
};
}
