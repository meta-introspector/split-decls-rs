macro_rules! macro_369 {
    () => {
        ast_struct ! { # [doc = " A glob import in a `use` item: `*`."] # [cfg_attr (docsrs , doc (cfg (feature = "full")))] pub struct UseGlob { pub star_token : Token ! [*] , } }
    };
}

macro_369!();