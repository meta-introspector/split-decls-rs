macro_rules! deps {
    () => {
        Builder!();
        LogTracer!();
        InterestCacheConfig!();
    };
}

macro_rules! impl_6 {
    () => {
        deps!();
        impl Builder { # [doc = " Returns a new `Builder` to construct a [`LogTracer`]."] # [doc = ""] pub fn new () -> Self { Self :: default () } # [doc = " Sets a global maximum level for `log` records."] # [doc = ""] # [doc = " Log records whose level is more verbose than the provided level will be"] # [doc = " disabled."] # [doc = ""] # [doc = " By default, all `log` records will be enabled."] pub fn with_max_level (self , filter : impl Into < log :: LevelFilter >) -> Self { let filter = filter . into () ; Self { filter , .. self } } # [doc = " Configures the `LogTracer` to ignore all log records whose target"] # [doc = " starts with the given string."] # [doc = ""] # [doc = " This should be used when a crate enables the `tracing/log` feature to"] # [doc = " emit log records for tracing events. Otherwise, those events will be"] # [doc = " recorded twice."] pub fn ignore_crate (mut self , name : impl Into < String >) -> Self { self . ignore_crates . push (name . into ()) ; self } # [doc = " Configures the `LogTracer` to ignore all log records whose target"] # [doc = " starts with any of the given the given strings."] # [doc = ""] # [doc = " This should be used when a crate enables the `tracing/log` feature to"] # [doc = " emit log records for tracing events. Otherwise, those events will be"] # [doc = " recorded twice."] pub fn ignore_all < I > (self , crates : impl IntoIterator < Item = I >) -> Self where I : Into < String > , { crates . into_iter () . fold (self , Self :: ignore_crate) } # [doc = " Configures the `LogTracer` to either disable or enable the interest cache."] # [doc = ""] # [doc = " When enabled, a per-thread LRU cache will be used to cache whenever the logger"] # [doc = " is interested in a given [level] + [target] pair for records generated through"] # [doc = " the `log` crate."] # [doc = ""] # [doc = " When no `trace!` logs are enabled the logger is able to cheaply filter"] # [doc = " them out just by comparing their log level to the globally specified"] # [doc = " maximum, and immediately reject them. When *any* other `trace!` log is"] # [doc = " enabled (even one which doesn't actually exist!) the logger has to run"] # [doc = " its full filtering machinery on each and every `trace!` log, which can"] # [doc = " potentially be very expensive."] # [doc = ""] # [doc = " Enabling this cache is useful in such situations to improve performance."] # [doc = ""] # [doc = " You most likely do not want to enabled this if you have registered any dynamic"] # [doc = " filters on your logger and you want them to be run every time."] # [doc = ""] # [doc = " This is disabled by default."] # [doc = ""] # [doc = " [level]: log::Metadata::level"] # [doc = " [target]: log::Metadata::target"] # [cfg (all (feature = "interest-cache" , feature = "std"))] # [cfg_attr (docsrs , doc (cfg (all (feature = "interest-cache" , feature = "std"))))] pub fn with_interest_cache (mut self , config : crate :: InterestCacheConfig) -> Self { self . interest_cache_config = Some (config) ; self } # [doc = " Constructs a new `LogTracer` with the provided configuration and sets it"] # [doc = " as the default logger."] # [doc = ""] # [doc = " Setting a global logger can only be done once."] # [cfg (feature = "std")] # [cfg_attr (docsrs , doc (cfg (feature = "std")))] # [allow (unused_mut)] pub fn init (mut self) -> Result < () , SetLoggerError > { # [cfg (all (feature = "interest-cache" , feature = "std"))] crate :: interest_cache :: configure (self . interest_cache_config . take ()) ; let ignore_crates = self . ignore_crates . into_boxed_slice () ; let logger = Box :: new (LogTracer { ignore_crates }) ; log :: set_boxed_logger (logger) ? ; log :: set_max_level (self . filter) ; Ok (()) } }
    };
}

impl_6!();