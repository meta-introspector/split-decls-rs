// Generated macro for impl_498 (impl)
macro_rules! Depcrate_instantimpl_498 {
() => {
// Module: crate::instant
// Provides: {"impl_498"}
// Dependencies: {}
impl Instant { # [doc = " Returns an `Instant` corresponding to \"now\"."] # [doc = ""] # [doc = " ```rust"] # [doc = " # #![expect(deprecated)]"] # [doc = " # use time::Instant;"] # [doc = " println!(\"{:?}\", Instant::now());"] # [doc = " ```"] # [inline] pub fn now () -> Self { Self (StdInstant :: now ()) } # [doc = " Returns the amount of time elapsed since this instant was created. The duration will always"] # [doc = " be nonnegative if the instant is not synthetically created."] # [doc = ""] # [doc = " ```rust"] # [doc = " # #![expect(deprecated)]"] # [doc = " # use time::{Instant, ext::{NumericalStdDuration, NumericalDuration}};"] # [doc = " # use std::thread;"] # [doc = " let instant = Instant::now();"] # [doc = " thread::sleep(1.std_milliseconds());"] # [doc = " assert!(instant.elapsed() >= 1.milliseconds());"] # [doc = " ```"] # [inline] pub fn elapsed (self) -> Duration { Self :: now () - self } # [doc = " Returns `Some(t)` where `t` is the time `self + duration` if `t` can be represented as"] # [doc = " `Instant` (which means it's inside the bounds of the underlying data structure), `None`"] # [doc = " otherwise."] # [doc = ""] # [doc = " ```rust"] # [doc = " # #![expect(deprecated)]"] # [doc = " # use time::{Instant, ext::NumericalDuration};"] # [doc = " let now = Instant::now();"] # [doc = " assert_eq!(now.checked_add(5.seconds()), Some(now + 5.seconds()));"] # [doc = " assert_eq!(now.checked_add((-5).seconds()), Some(now + (-5).seconds()));"] # [doc = " ```"] # [inline] pub fn checked_add (self , duration : Duration) -> Option < Self > { if duration . is_zero () { Some (self) } else if duration . is_positive () { self . 0 . checked_add (duration . unsigned_abs ()) . map (Self) } else { debug_assert ! (duration . is_negative ()) ; self . 0 . checked_sub (duration . unsigned_abs ()) . map (Self) } } # [doc = " Returns `Some(t)` where `t` is the time `self - duration` if `t` can be represented as"] # [doc = " `Instant` (which means it's inside the bounds of the underlying data structure), `None`"] # [doc = " otherwise."] # [doc = ""] # [doc = " ```rust"] # [doc = " # #![expect(deprecated)]"] # [doc = " # use time::{Instant, ext::NumericalDuration};"] # [doc = " let now = Instant::now();"] # [doc = " assert_eq!(now.checked_sub(5.seconds()), Some(now - 5.seconds()));"] # [doc = " assert_eq!(now.checked_sub((-5).seconds()), Some(now - (-5).seconds()));"] # [doc = " ```"] # [inline] pub fn checked_sub (self , duration : Duration) -> Option < Self > { if duration . is_zero () { Some (self) } else if duration . is_positive () { self . 0 . checked_sub (duration . unsigned_abs ()) . map (Self) } else { debug_assert ! (duration . is_negative ()) ; self . 0 . checked_add (duration . unsigned_abs ()) . map (Self) } } # [doc = " Obtain the inner [`std::time::Instant`]."] # [doc = ""] # [doc = " ```rust"] # [doc = " # #![expect(deprecated)]"] # [doc = " # use time::Instant;"] # [doc = " let now = Instant::now();"] # [doc = " assert_eq!(now.into_inner(), now.0);"] # [doc = " ```"] # [inline] pub const fn into_inner (self) -> StdInstant { self . 0 } }
};
}
