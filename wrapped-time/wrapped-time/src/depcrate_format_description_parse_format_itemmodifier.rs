// Generated macro for modifier (macro)
macro_rules! Depcrate_format_description_parse_format_itemmodifier {
() => {
// Module: crate::format_description::parse::format_item
// Provides: {"modifier"}
// Dependencies: {}
# [doc = " Declare the various modifiers."] # [doc = ""] # [doc = " For the general case, ordinary syntax can be used. Note that you _must_ declare a default"] # [doc = " variant. The only significant change is that the string representation of the variant must be"] # [doc = " provided after the variant name. For example, `Numerical = b\"numerical\"` declares a variant"] # [doc = " named `Numerical` with the string representation `b\"numerical\"`. This is the value that will be"] # [doc = " used when parsing the modifier. The value is not case sensitive."] # [doc = ""] # [doc = " If the type in the public API does not have the same name as the type in the internal"] # [doc = " representation, then the former must be specified in parenthesis after the internal name. For"] # [doc = " example, `HourBase(bool)` has an internal name \"HourBase\", but is represented as a boolean in"] # [doc = " the public API."] # [doc = ""] # [doc = " By default, the internal variant name is assumed to be the same as the public variant name. If"] # [doc = " this is not the case, the qualified path to the variant must be specified in parenthesis after"] # [doc = " the internal variant name. For example, `Twelve(true)` has an internal variant name \"Twelve\","] # [doc = " but is represented as `true` in the public API."] macro_rules ! modifier { ($ (enum $ name : ident $ (($ target_ty : ty)) ? { $ ($ (# [$ attr : meta]) ? $ variant : ident $ (($ target_value : expr)) ? = $ parse_variant : literal) ,* $ (,) ? }) +) => { $ (# [derive (Default)] enum $ name { $ ($ (# [$ attr]) ? $ variant) ,* } impl $ name { # [doc = " Parse the modifier from its string representation."] # [inline] fn from_modifier_value (value : & Spanned <& [u8] >) -> Result < Option < Self >, Error > { $ (if value . eq_ignore_ascii_case ($ parse_variant) { return Ok (Some (Self ::$ variant)) ; }) * Err (Error { _inner : unused (value . span . error ("invalid modifier value")) , public : crate :: error :: InvalidFormatDescription :: InvalidModifier { value : String :: from_utf8_lossy (value) . into_owned () , index : value . span . start . byte as usize , } , }) } } impl From <$ name > for target_ty ! ($ name $ ($ target_ty) ?) { # [inline] fn from (modifier : $ name) -> Self { match modifier { $ ($ name ::$ variant => target_value ! ($ name $ variant $ ($ target_value) ?)) ,* } } }) + } ; }
};
}
