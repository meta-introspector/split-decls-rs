// Generated macro for InstantExt (trait)
macro_rules! Depcrate_ext_instantInstantExt {
() => {
// Module: crate::ext::instant
// Provides: {"InstantExt"}
// Dependencies: {}
# [doc = " An extension trait for [`std::time::Instant`] that adds methods for"] # [doc = " [`time::Duration`](Duration)s."] pub trait InstantExt : sealed :: Sealed { # [doc = " # Panics"] # [doc = ""] # [doc = " This function may panic if the resulting point in time cannot be represented by the"] # [doc = " underlying data structure. See [`InstantExt::checked_add_signed`] for a non-panicking"] # [doc = " version."] # [inline] # [track_caller] fn add_signed (self , duration : Duration) -> Self { self . checked_add_signed (duration) . expect ("overflow when adding duration to instant") } # [doc = " # Panics"] # [doc = ""] # [doc = " This function may panic if the resulting point in time cannot be represented by the"] # [doc = " underlying data structure. See [`InstantExt::checked_sub_signed`] for a non-panicking"] # [doc = " version."] # [inline] # [track_caller] fn sub_signed (self , duration : Duration) -> Self { self . checked_sub_signed (duration) . expect ("overflow when subtracting duration from instant") } # [doc = " Returns `Some(t)` where `t` is the time `self.checked_add_signed(duration)` if `t` can be"] # [doc = " represented as `Instant` (which means it's inside the bounds of the underlying data"] # [doc = " structure), `None` otherwise."] fn checked_add_signed (& self , duration : Duration) -> Option < Self > ; # [doc = " Returns `Some(t)` where `t` is the time `self.checked_sub_signed(duration)` if `t` can be"] # [doc = " represented as `Instant` (which means it's inside the bounds of the underlying data"] # [doc = " structure), `None` otherwise."] fn checked_sub_signed (& self , duration : Duration) -> Option < Self > ; # [doc = " Returns the amount of time elapsed from another instant to this one. This will be negative"] # [doc = " if `earlier` is later than `self`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use std::thread::sleep;"] # [doc = " # use std::time::{Duration, Instant};"] # [doc = " # use time::ext::InstantExt;"] # [doc = " let now = Instant::now();"] # [doc = " sleep(Duration::new(1, 0));"] # [doc = " let new_now = Instant::now();"] # [doc = " println!(\"{:?}\", new_now.signed_duration_since(now)); // positive"] # [doc = " println!(\"{:?}\", now.signed_duration_since(new_now)); // negative"] # [doc = " ```"] fn signed_duration_since (& self , earlier : Self) -> Duration ; }
};
}
