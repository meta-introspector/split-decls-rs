// Generated macro for refresh_tz_unchecked (function)
macro_rules! Depcrate_utilrefresh_tz_unchecked {
() => {
// Module: crate::util
// Provides: {"refresh_tz_unchecked"}
// Dependencies: {}
# [doc = " Update time zone information from the system."] # [doc = ""] # [doc = " For a version of this function that is guaranteed to be sound, see [`refresh_tz`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This is a system call with specific requirements. The following is from POSIX's description of"] # [doc = " `tzset`:"] # [doc = ""] # [doc = " > If a thread accesses `tzname`, `daylight`, or `timezone` directly while another thread is in a"] # [doc = " > call to `tzset()`, or to any function that is required or allowed to set timezone information"] # [doc = " > as if by calling `tzset()`, the behavior is undefined."] # [doc = ""] # [doc = " Effectively, this translates to the requirement that at least one of the following must be true:"] # [doc = ""] # [doc = " - The operating system provides a thread-safe environment."] # [doc = " - The process is single-threaded."] # [doc = " - The process is multi-threaded **and** no other thread is mutating the environment in any way"] # [doc = "   at the same time a call to a method that obtains the local UTC offset. This includes adding,"] # [doc = "   removing, or modifying an environment variable."] # [doc = ""] # [doc = " ## Soundness is global"] # [doc = ""] # [doc = " You must not only verify this safety conditions for your code, but for **all** code that will be"] # [doc = " included in the final binary. Notably, it applies to both direct and transitive dependencies and"] # [doc = " to both Rust and non-Rust code. **For this reason it is not possible for a library to soundly"] # [doc = " call this method.**"] # [doc = ""] # [doc = " ## Forward compatibility"] # [doc = ""] # [doc = " This currently only does anything on Unix-like systems. On other systems, it is a no-op. This"] # [doc = " may change in the future if necessary, expanding the safety requirements. It is expected that,"] # [doc = " at a minimum, calling this method when the process is single-threaded will remain sound."] # [cfg (feature = "local-offset")] # [inline] pub unsafe fn refresh_tz_unchecked () { unsafe { crate :: sys :: refresh_tz_unchecked () } ; }
};
}
