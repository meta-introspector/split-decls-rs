// Generated macro for impl_126 (impl)
macro_rules! Depcrate_zone_ianaimpl_126 {
() => {
// Module: crate::zone::iana
// Provides: {"impl_126"}
// Dependencies: {}
impl < 'a > IanaParserExtendedBorrowed < 'a > { # [doc = " Gets the [`TimeZone`], the canonical IANA ID, and the case-normalized IANA ID from a case-insensitive IANA time zone ID."] # [doc = ""] # [doc = " Returns `TimeZone::UNKNOWN` / `\"Etc/Unknown\"` if the IANA ID is not found."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu_time::zone::iana::IanaParserExtended;"] # [doc = " use icu_time::TimeZone;"] # [doc = ""] # [doc = " let parser = IanaParserExtended::new();"] # [doc = ""] # [doc = " let r = parser.parse(\"Asia/CALCUTTA\");"] # [doc = ""] # [doc = " assert_eq!(r.time_zone.as_str(), \"inccu\");"] # [doc = " assert_eq!(r.canonical, \"Asia/Kolkata\");"] # [doc = " assert_eq!(r.normalized, \"Asia/Calcutta\");"] # [doc = ""] # [doc = " // Unknown IANA time zone ID:"] # [doc = " let r = parser.parse(\"America/San_Francisco\");"] # [doc = ""] # [doc = " assert_eq!(r.time_zone, TimeZone::UNKNOWN);"] # [doc = " assert_eq!(r.canonical, \"Etc/Unknown\");"] # [doc = " assert_eq!(r.normalized, \"Etc/Unknown\");"] # [doc = " ```"] pub fn parse (& self , iana_id : & str) -> TimeZoneAndCanonicalAndNormalized < 'a > { self . parse_from_utf8 (iana_id . as_bytes ()) } # [doc = " Same as [`Self::parse()`] but works with potentially ill-formed UTF-8."] pub fn parse_from_utf8 (& self , iana_id : & [u8]) -> TimeZoneAndCanonicalAndNormalized < 'a > { let Some (trie_value) = self . inner . trie_value (iana_id) else { return TimeZoneAndCanonicalAndNormalized :: UKNONWN ; } ; let Some (time_zone) = self . inner . data . bcp47_ids . get (trie_value . index ()) else { debug_assert ! (false , "index should be in range") ; return TimeZoneAndCanonicalAndNormalized :: UKNONWN ; } ; let Some (canonical) = self . data . normalized_iana_ids . get (trie_value . index ()) else { debug_assert ! (false , "index should be in range") ; return TimeZoneAndCanonicalAndNormalized :: UKNONWN ; } ; let normalized = if trie_value . is_canonical () { canonical } else { let Some (Ok (index)) = self . data . normalized_iana_ids . binary_search_in_range_by (| a | { a . as_bytes () . iter () . map (u8 :: to_ascii_lowercase) . cmp (iana_id . iter () . map (u8 :: to_ascii_lowercase)) } , self . inner . data . bcp47_ids . len () .. self . data . normalized_iana_ids . len () ,) else { debug_assert ! (false , "binary search should succeed if trie lookup succeeds") ; return TimeZoneAndCanonicalAndNormalized :: UKNONWN ; } ; let Some (normalized) = self . data . normalized_iana_ids . get (self . inner . data . bcp47_ids . len () + index) else { debug_assert ! (false , "binary search returns valid index") ; return TimeZoneAndCanonicalAndNormalized :: UKNONWN ; } ; normalized } ; TimeZoneAndCanonicalAndNormalized { time_zone , canonical , normalized , } } # [doc = " Returns an iterator over all time zones and their canonical IANA identifiers."] # [doc = ""] # [doc = " The iterator is sorted by the canonical IANA identifiers."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::subtags::subtag;"] # [doc = " use icu::time::zone::iana::IanaParserExtended;"] # [doc = " use icu::time::zone::TimeZone;"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let parser = IanaParserExtended::new();"] # [doc = ""] # [doc = " let ids = parser"] # [doc = "     .iter()"] # [doc = "     .map(|t| (t.time_zone, t.canonical))"] # [doc = "     .collect::<BTreeSet<_>>();"] # [doc = ""] # [doc = " assert!(ids.contains(&(TimeZone(subtag!(\"uaiev\")), \"Europe/Kyiv\")));"] # [doc = " assert!(parser.iter().count() >= 445);"] # [doc = " ```"] pub fn iter (& self) -> TimeZoneAndCanonicalIter < 'a > { TimeZoneAndCanonicalIter (self . inner . data . bcp47_ids . iter () . zip (self . data . normalized_iana_ids . iter ()) ,) } # [doc = " Returns an iterator equivalent to calling [`Self::parse`] on all IANA time zone identifiers."] # [doc = ""] # [doc = " The only guarantee w.r.t iteration order is that for a given time zone, the canonical IANA"] # [doc = " identifier will come first, and the following non-canonical IANA identifiers will be sorted."] # [doc = " However, the output is not grouped by time zone."] # [doc = ""] # [doc = " The current implementation returns all sorted canonical IANA identifiers first, followed by all"] # [doc = " sorted non-canonical identifiers, however this is subject to change."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::time::zone::iana::IanaParserExtended;"] # [doc = " use icu::time::zone::TimeZone;"] # [doc = " use std::collections::BTreeMap;"] # [doc = " use icu::locale::subtags::subtag;"] # [doc = ""] # [doc = " let parser = IanaParserExtended::new();"] # [doc = ""] # [doc = " let ids = parser.iter_all().enumerate().map(|(a, b)| ((b.time_zone, b.canonical, b.normalized), a)).collect::<std::collections::BTreeMap<_, _>>();"] # [doc = ""] # [doc = " let kyiv_idx = ids[&(TimeZone(subtag!(\"uaiev\")), \"Europe/Kyiv\", \"Europe/Kyiv\")];"] # [doc = " let kiev_idx = ids[&(TimeZone(subtag!(\"uaiev\")), \"Europe/Kyiv\", \"Europe/Kiev\")];"] # [doc = " let uzgh_idx = ids[&(TimeZone(subtag!(\"uaiev\")), \"Europe/Kyiv\", \"Europe/Uzhgorod\")];"] # [doc = " let zapo_idx = ids[&(TimeZone(subtag!(\"uaiev\")), \"Europe/Kyiv\", \"Europe/Zaporozhye\")];"] # [doc = ""] # [doc = " // The order for a particular time zone is guaranteed"] # [doc = " assert!(kyiv_idx < kiev_idx && kiev_idx < uzgh_idx && uzgh_idx < zapo_idx);"] # [doc = " // It is not guaranteed that the entries for a particular time zone are consecutive"] # [doc = " assert!(kyiv_idx + 1 != kiev_idx);"] # [doc = ""] # [doc = " assert!(parser.iter_all().count() >= 598);"] # [doc = " ```"] pub fn iter_all (& self) -> TimeZoneAndCanonicalAndNormalizedIter < 'a > { TimeZoneAndCanonicalAndNormalizedIter (0 , * self) } }
};
}
