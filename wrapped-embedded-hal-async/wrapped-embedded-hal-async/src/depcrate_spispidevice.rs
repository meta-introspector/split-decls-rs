// Generated macro for SpiDevice (trait)
macro_rules! Depcrate_spiSpiDevice {
() => {
// Module: crate::spi
// Provides: {"SpiDevice"}
// Dependencies: {}
# [doc = " SPI device trait."] # [doc = ""] # [doc = " `SpiDevice` represents ownership over a single SPI device on a (possibly shared) bus, selected"] # [doc = " with a CS (Chip Select) pin."] # [doc = ""] # [doc = " See [the docs on embedded-hal](embedded_hal::spi) for important information on SPI Bus vs Device traits."] pub trait SpiDevice < Word : Copy + 'static = u8 > : ErrorType { # [doc = " Perform a transaction against the device."] # [doc = ""] # [doc = " - Locks the bus"] # [doc = " - Asserts the CS (Chip Select) pin."] # [doc = " - Performs all the operations."] # [doc = " - [Flushes](SpiBus::flush) the bus."] # [doc = " - Deasserts the CS pin."] # [doc = " - Unlocks the bus."] # [doc = ""] # [doc = " The locking mechanism is implementation-defined. The only requirement is it must prevent two"] # [doc = " transactions from executing concurrently against the same bus. Examples of implementations are:"] # [doc = " critical sections, blocking mutexes, returning an error or panicking if the bus is already busy."] # [doc = ""] # [doc = " On bus errors the implementation should try to deassert CS."] # [doc = " If an error occurs while deasserting CS the bus error should take priority as the return value."] async fn transaction (& mut self , operations : & mut [Operation < '_ , Word >] ,) -> Result < () , Self :: Error > ; # [doc = " Do a read within a transaction."] # [doc = ""] # [doc = " This is a convenience method equivalent to `device.read_transaction(&mut [buf])`."] # [doc = ""] # [doc = " See also: [`SpiDevice::transaction`], [`SpiDevice::read`]"] # [inline] async fn read (& mut self , buf : & mut [Word]) -> Result < () , Self :: Error > { self . transaction (& mut [Operation :: Read (buf)]) . await } # [doc = " Do a write within a transaction."] # [doc = ""] # [doc = " This is a convenience method equivalent to `device.write_transaction(&mut [buf])`."] # [doc = ""] # [doc = " See also: [`SpiDevice::transaction`], [`SpiDevice::write`]"] # [inline] async fn write (& mut self , buf : & [Word]) -> Result < () , Self :: Error > { self . transaction (& mut [Operation :: Write (buf)]) . await } # [doc = " Do a transfer within a transaction."] # [doc = ""] # [doc = " This is a convenience method equivalent to `device.transaction(&mut [Operation::Transfer(read, write)])`."] # [doc = ""] # [doc = " See also: [`SpiDevice::transaction`], [`SpiBus::transfer`]"] # [inline] async fn transfer (& mut self , read : & mut [Word] , write : & [Word]) -> Result < () , Self :: Error > { self . transaction (& mut [Operation :: Transfer (read , write)]) . await } # [doc = " Do an in-place transfer within a transaction."] # [doc = ""] # [doc = " This is a convenience method equivalent to `device.transaction(&mut [Operation::TransferInPlace(buf)])`."] # [doc = ""] # [doc = " See also: [`SpiDevice::transaction`], [`SpiBus::transfer_in_place`]"] # [inline] async fn transfer_in_place (& mut self , buf : & mut [Word]) -> Result < () , Self :: Error > { self . transaction (& mut [Operation :: TransferInPlace (buf)]) . await } }
};
}
