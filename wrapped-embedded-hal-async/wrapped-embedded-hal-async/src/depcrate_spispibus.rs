// Generated macro for SpiBus (trait)
macro_rules! Depcrate_spiSpiBus {
() => {
// Module: crate::spi
// Provides: {"SpiBus"}
// Dependencies: {}
# [doc = " SPI bus."] # [doc = ""] # [doc = " `SpiBus` represents **exclusive ownership** over the whole SPI bus, with SCK, MOSI and MISO pins."] # [doc = ""] # [doc = " See [the docs on embedded-hal][embedded_hal::spi] for important information on SPI Bus vs Device traits."] pub trait SpiBus < Word : 'static + Copy = u8 > : ErrorType { # [doc = " Read `words` from the slave."] # [doc = ""] # [doc = " The word value sent on MOSI during reading is implementation-defined,"] # [doc = " typically `0x00`, `0xFF`, or configurable."] # [doc = ""] # [doc = " Implementations are allowed to return before the operation is"] # [doc = " complete. See [the docs on embedded-hal][embedded_hal::spi] for details on flushing."] async fn read (& mut self , words : & mut [Word]) -> Result < () , Self :: Error > ; # [doc = " Write `words` to the slave, ignoring all the incoming words."] # [doc = ""] # [doc = " Implementations are allowed to return before the operation is"] # [doc = " complete. See [the docs on embedded-hal][embedded_hal::spi] for details on flushing."] async fn write (& mut self , words : & [Word]) -> Result < () , Self :: Error > ; # [doc = " Write and read simultaneously. `write` is written to the slave on MOSI and"] # [doc = " words received on MISO are stored in `read`."] # [doc = ""] # [doc = " It is allowed for `read` and `write` to have different lengths, even zero length."] # [doc = " The transfer runs for `max(read.len(), write.len())` words. If `read` is shorter,"] # [doc = " incoming words after `read` has been filled will be discarded. If `write` is shorter,"] # [doc = " the value of words sent in MOSI after all `write` has been sent is implementation-defined,"] # [doc = " typically `0x00`, `0xFF`, or configurable."] # [doc = ""] # [doc = " Implementations are allowed to return before the operation is"] # [doc = " complete. See [the docs on embedded-hal][embedded_hal::spi] for details on flushing."] async fn transfer (& mut self , read : & mut [Word] , write : & [Word]) -> Result < () , Self :: Error > ; # [doc = " Write and read simultaneously. The contents of `words` are"] # [doc = " written to the slave, and the received words are stored into the same"] # [doc = " `words` buffer, overwriting it."] # [doc = ""] # [doc = " Implementations are allowed to return before the operation is"] # [doc = " complete. See [the docs on embedded-hal][embedded_hal::spi] for details on flushing."] async fn transfer_in_place (& mut self , words : & mut [Word]) -> Result < () , Self :: Error > ; # [doc = " Wait until all operations have completed and the bus is idle."] # [doc = ""] # [doc = " See [the docs on embedded-hal][embedded_hal::spi] for information on flushing."] async fn flush (& mut self) -> Result < () , Self :: Error > ; }
};
}
