// Generated macro for impl_80 (impl)
macro_rules! Depcrate_core_geometryimpl_80 {
() => {
// Module: crate::core::geometry
// Provides: {"impl_80"}
// Dependencies: {}
impl Position { pub fn new (middle : Point , size : Point , center : Point , halo : Point) -> Self { Self { middle , size , center , halo , } } pub fn distance_to_left (& self , with_halo : bool) -> f64 { self . center () . x - self . bbox (with_halo) . 0 . x } pub fn distance_to_right (& self , with_halo : bool) -> f64 { self . bbox (with_halo) . 1 . x - self . center () . x } pub fn left (& self , with_halo : bool) -> f64 { self . bbox (with_halo) . 0 . x } pub fn right (& self , with_halo : bool) -> f64 { self . bbox (with_halo) . 1 . x } pub fn top (& self , with_halo : bool) -> f64 { self . bbox (with_halo) . 0 . y } pub fn bottom (& self , with_halo : bool) -> f64 { self . bbox (with_halo) . 1 . y } pub fn bbox (& self , with_halo : bool) -> (Point , Point) { let size = self . size (with_halo) ; let top_left = self . middle . sub (size . scale (0.5)) ; let bottom_right = top_left . add (size) ; (top_left , bottom_right) } # [doc = " Returns the center of the shape in absolute coordinates."] pub fn center (& self) -> Point { self . middle . add (self . center) } # [doc = " Returns the middle of the shape. (not center!)"] pub fn middle (& self) -> Point { self . middle } pub fn size (& self , with_halo : bool) -> Point { if with_halo { self . size . add (self . halo) } else { self . size } } # [doc = " \\return True if the box fits within the x ranges of \\p range."] pub fn in_x_range (& self , range : (f64 , f64) , with_halo : bool) -> bool { self . left (with_halo) >= range . 0 && self . right (with_halo) <= range . 1 } pub fn set_size (& mut self , size : Point) { self . size = size ; } # [doc = " Update the center point for the shape. This is expressed as the delta"] # [doc = " from the center of mass (middle-point)."] pub fn set_new_center_point (& mut self , center : Point) { self . center = center ; assert ! (self . center . x . abs () < self . size . x) ; assert ! (self . center . y . abs () < self . size . y) ; } pub fn move_to (& mut self , p : Point) { let delta = p . sub (self . center ()) ; self . middle = self . middle . add (delta) ; } pub fn align_to_top (& mut self , y : f64) { self . middle . y = y + self . size . y / 2. + self . halo . y / 2. } pub fn align_to_left (& mut self , x : f64) { self . middle . x = x + self . size . x / 2. + self . halo . x / 2. } pub fn align_to_right (& mut self , x : f64) { self . middle . x = x - self . size . x / 2. - self . halo . x / 2. ; } pub fn translate (& mut self , d : Point) { self . middle = self . middle . add (d) ; } # [doc = " Align the shape to the line \\p x, to the right or the left, depending on"] # [doc = "  \\p to_left."] pub fn align_x (& mut self , x : f64 , to_left : bool) { let half_box = self . size . x / 2. + self . halo . x / 2. ; if to_left { self . middle . x = x + half_box ; } else { self . middle . x = x - half_box ; } } pub fn set_x (& mut self , x : f64) { self . middle . x = x - self . center . x ; } pub fn set_y (& mut self , y : f64) { self . middle . y = y - self . center . y ; } pub fn transpose (& mut self) { self . middle = self . middle . transpose () ; self . size = self . size . transpose () ; self . center = self . center . transpose () ; self . halo = self . halo . transpose () ; } }
};
}
