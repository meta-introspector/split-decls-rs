// Generated macro for impl_13 (impl)
macro_rules! Depcrate_adt_dagimpl_13 {
() => {
// Module: crate::adt::dag
// Provides: {"impl_13"}
// Dependencies: {}
impl DAG { pub fn new () -> Self { DAG { nodes : Vec :: new () , ranks : Vec :: new () , levels : Vec :: new () , validate : true , } } pub fn set_validate (& mut self , validate : bool) { self . validate = validate ; } pub fn clear (& mut self) { self . nodes . clear () ; self . ranks . clear () ; self . levels . clear () ; } pub fn iter (& self) -> NodeIterator { NodeIterator { curr : 0 , last : self . nodes . len () , } } pub fn add_edge (& mut self , from : NodeHandle , to : NodeHandle) { self . nodes [from . idx] . successors . push (to) ; self . nodes [to . idx] . predecessors . push (from) ; } # [doc = " Remove an edge from \\p from to \\p to."] # [doc = " \\returns True if an edge was removed."] pub fn remove_edge (& mut self , from : NodeHandle , to : NodeHandle) -> bool { let succ = & mut self . nodes [from . idx] . successors ; let mut removed_succ = false ; if let Some (pos) = succ . iter () . position (| x | * x == to) { succ . remove (pos) ; removed_succ = true ; } let pred = & mut self . nodes [to . idx] . predecessors ; let mut removed_pred = false ; if let Some (pos) = pred . iter () . position (| x | * x == from) { pred . remove (pos) ; removed_pred = true ; } assert_eq ! (removed_pred , removed_succ) ; removed_pred } # [doc = " Create a new node."] pub fn new_node (& mut self) -> NodeHandle { self . nodes . push (Node :: new ()) ; self . levels . push (0) ; let node = NodeHandle :: new (self . nodes . len () - 1) ; self . add_element_to_rank (node , 0 , false) ; node } # [doc = " Create \\p n new nodes."] pub fn new_nodes (& mut self , n : usize) { for _ in 0 .. n { self . nodes . push (Node :: new ()) ; self . levels . push (0) ; let node = NodeHandle :: new (self . nodes . len () - 1) ; self . add_element_to_rank (node , 0 , false) ; } self . verify () ; } pub fn successors (& self , from : NodeHandle) -> & Vec < NodeHandle > { & self . nodes [from . idx] . successors } pub fn predecessors (& self , from : NodeHandle) -> & Vec < NodeHandle > { & self . nodes [from . idx] . predecessors } pub fn single_pred (& self , from : NodeHandle) -> Option < NodeHandle > { if self . nodes [from . idx] . predecessors . len () == 1 { return Some (self . nodes [from . idx] . predecessors [0]) ; } None } pub fn single_succ (& self , from : NodeHandle) -> Option < NodeHandle > { if self . nodes [from . idx] . successors . len () == 1 { return Some (self . nodes [from . idx] . successors [0]) ; } None } pub fn verify (& self) { if self . validate { for node in & self . nodes { for edge in & node . successors { assert ! (edge . idx < self . nodes . len ()) ; } } for (i , node) in self . nodes . iter () . enumerate () { let from = NodeHandle :: from (i) ; for dest in node . successors . iter () { let reachable = self . is_reachable (* dest , from) && from != * dest ; assert ! (! reachable , "We found a cycle!") ; } } assert_eq ! (self . count_nodes_in_ranks () , self . len ()) ; } } pub fn len (& self) -> usize { self . nodes . len () } pub fn is_empty (& self) -> bool { self . nodes . is_empty () } # [doc = " \\returns True if the node \\to is reachable from the node \\p from."] # [doc = " This internal method is used for the verification of the graph."] fn is_reachable_inner (& self , from : NodeHandle , to : NodeHandle , visited : & mut Vec < bool > ,) -> bool { if from == to { return true ; } if visited [from . idx] { return false ; } visited [from . idx] = true ; let from_node = & self . nodes [from . idx] ; for edge in & from_node . successors { if self . is_reachable_inner (* edge , to , visited) { return true ; } } visited [from . idx] = false ; false } # [doc = " \\returns True if there is a path from \\p 'from' to \\p 'to'."] pub fn is_reachable (& self , from : NodeHandle , to : NodeHandle) -> bool { if from == to { return true ; } let mut visited = Vec :: new () ; visited . resize (self . nodes . len () , false) ; self . is_reachable_inner (from , to , & mut visited) } # [doc = " Return the topological sort order of the nodes in the dag."] # [doc = " This is implemented as the reverse post order scan."] fn topological_sort (& self) -> Vec < NodeHandle > { let mut order : Vec < NodeHandle > = Vec :: new () ; let mut visited = Vec :: new () ; visited . resize (self . nodes . len () , false) ; let mut worklist : Vec < (NodeHandle , bool) > = Vec :: new () ; for n in self . iter () { worklist . push ((n , false)) ; } while let Some ((current , cmd)) = worklist . pop () { if cmd { order . push (current) ; continue ; } if visited [current . idx] { continue ; } visited [current . idx] = true ; worklist . push ((current , true)) ; let node = & self . nodes [current . idx] ; for edge in & node . successors { worklist . push ((* edge , false)) ; } } order . reverse () ; order } # [doc = " \\returns the number of ranks in the dag."] pub fn num_levels (& self) -> usize { self . ranks . len () } # [doc = " \\return a mutable reference to a row at level \\p level."] pub fn row_mut (& mut self , level : usize) -> & mut Vec < NodeHandle > { assert ! (level < self . ranks . len () , "Invalid rank") ; & mut self . ranks [level] } # [doc = " \\return a reference to a row at level \\p level."] pub fn row (& self , level : usize) -> & Vec < NodeHandle > { assert ! (level < self . ranks . len () , "Invalid rank") ; & self . ranks [level] } # [doc = " \\return a reference to the whole rank data structure."] pub fn ranks (& self) -> & RankType { & self . ranks } # [doc = " \\return a mutable reference to the whole rank data structure."] pub fn ranks_mut (& mut self) -> & mut RankType { & mut self . ranks } # [doc = " \\returns True if \\p elem is the first node in the row \\p level."] pub fn is_first_in_row (& self , elem : NodeHandle , level : usize) -> bool { if level >= self . ranks . len () || self . ranks [level] . is_empty () { return false ; } self . ranks [level] [0] == elem } # [doc = " \\returns True if \\p elem is the last node in the row \\p level."] pub fn is_last_in_row (& self , elem : NodeHandle , level : usize) -> bool { if level >= self . ranks . len () || self . ranks [level] . is_empty () { return false ; } let last_idx = self . ranks [level] . len () - 1 ; self . ranks [level] [last_idx] == elem } # [doc = " Place the element \\p elem at the nth level \\p level. If the level does"] # [doc = " not exist then create it. If \\p prepend is set then the node is inserted"] # [doc = " at the beginning of the rank. The node must not be in the rank when this"] # [doc = " method is called."] fn add_element_to_rank (& mut self , elem : NodeHandle , level : usize , prepend : bool ,) { while self . ranks . len () < level + 1 { self . ranks . push (Vec :: new ()) ; } if prepend { self . ranks [level] . insert (0 , elem) ; } else { self . ranks [level] . push (elem) ; } self . levels [elem . get_index ()] = level ; } # [doc = " Places all of the nodes in ranks (levels)."] pub fn recompute_node_ranks (& mut self) { assert ! (! self . is_empty () , "Sorting an empty graph") ; let order = self . topological_sort () ; let levels = self . compute_levels (& order) ; self . ranks . clear () ; for (i , level) in levels . iter () . enumerate () { self . add_element_to_rank (NodeHandle :: from (i) , * level , false) ; } } # [doc = " \\returns the number of nodes that are in ranks."] # [doc = " This is used for verification of the dag."] fn count_nodes_in_ranks (& self) -> usize { let mut cnt = 0 ; for row in self . ranks . iter () { cnt += row . len () ; } cnt } # [doc = " Move the node \\p node to a new level \\p new_level."] # [doc = " Place the node before \\p node, or at the end."] pub fn update_node_rank_level (& mut self , node : NodeHandle , new_level : usize , insert_before : Option < NodeHandle > ,) { let curr_level = self . level (node) ; let level = & mut self . ranks [curr_level] ; let idx = level . iter () . position (| x | * x == node) . expect ("node not found") ; level . remove (idx) ; while self . ranks . len () < new_level + 1 { self . ranks . push (Vec :: new ()) ; } if let Option :: Some (marker) = insert_before { let row = & mut self . ranks [new_level] ; for i in 0 .. row . len () { if row [i] == marker { row . insert (i , node) ; self . levels [node . get_index ()] = new_level ; return ; } } panic ! ("Can't find the marker node in the array") ; } self . ranks [new_level] . push (node) ; self . levels [node . get_index ()] = new_level ; assert_eq ! (self . level (node) , new_level) ; } # [doc = " \\returns the level of the node \\p node in the rank."] pub fn level (& self , node : NodeHandle) -> usize { assert ! (node . get_index () < self . len () , "Node not in the dag") ; self . levels [node . get_index ()] } # [doc = " Computes and returns the level of each node in the graph based"] # [doc = " on the traversal order \\p order."] fn compute_levels (& self , order : & [NodeHandle]) -> Vec < usize > { let mut levels : Vec < usize > = Vec :: new () ; assert_eq ! (order . len () , self . nodes . len ()) ; levels . resize (self . nodes . len () , 0) ; for src in order { for dest in self . nodes [src . idx] . successors . iter () { if src . idx == dest . idx { continue ; } levels [dest . idx] = cmp :: max (levels [dest . idx] , levels [src . idx] + 1) ; } } for src in order { for dest in self . nodes [src . idx] . successors . iter () { assert ! (levels [dest . idx] >= levels [src . idx]) ; } } levels } }
};
}
