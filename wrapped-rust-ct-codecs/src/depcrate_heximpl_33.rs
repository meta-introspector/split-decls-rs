// Generated macro for impl_33 (impl)
macro_rules! Depcrate_heximpl_33 {
() => {
// Module: crate::hex
// Provides: {"impl_33"}
// Dependencies: {}
impl Encoder for Hex { # [doc = " Calculates the encoded length for a hexadecimal representation."] # [doc = ""] # [doc = " The encoded length is always twice the binary length, as each byte"] # [doc = " is represented by two hexadecimal characters."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `bin_len` - The length of the binary input in bytes"] # [doc = ""] # [doc = " # Returns"] # [doc = ""] # [doc = " * `Ok(usize)` - The required length for the encoded output"] # [doc = " * `Err(Error::Overflow)` - If the calculation would overflow"] # [inline] fn encoded_len (bin_len : usize) -> Result < usize , Error > { bin_len . checked_mul (2) . ok_or (Error :: Overflow) } # [doc = " Encodes binary data into a hexadecimal representation."] # [doc = ""] # [doc = " The encoding is performed in constant time relative to the input length."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `hex` - Mutable buffer to store the encoded output"] # [doc = " * `bin` - Binary input data to encode"] # [doc = ""] # [doc = " # Returns"] # [doc = ""] # [doc = " * `Ok(&[u8])` - A slice of the encoded buffer containing the hex data"] # [doc = " * `Err(Error::Overflow)` - If the output buffer is too small"] fn encode < IN : AsRef < [u8] > > (hex : & mut [u8] , bin : IN) -> Result < & [u8] , Error > { let bin = bin . as_ref () ; let bin_len = bin . len () ; let hex_maxlen = hex . len () ; if hex_maxlen < bin_len . checked_shl (1) . ok_or (Error :: Overflow) ? { return Err (Error :: Overflow) ; } for (i , v) in bin . iter () . enumerate () { let (b , c) = ((v >> 4) as u16 , (v & 0xf) as u16) ; let x = (((87 + c + (((c . wrapping_sub (10)) >> 8) & ! 38)) as u8) as u16) << 8 | ((87 + b + (((b . wrapping_sub (10)) >> 8) & ! 38)) as u8) as u16 ; hex [i * 2] = x as u8 ; hex [i * 2 + 1] = (x >> 8) as u8 ; } Ok (& hex [.. bin_len * 2]) } }
};
}
