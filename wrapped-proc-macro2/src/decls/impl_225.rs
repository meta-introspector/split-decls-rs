macro_rules! deps {
    () => {
        Span!();
        Ident!();
    };
}

macro_rules! impl_225 {
    () => {
        deps!();
        impl Ident { fn _new (inner : imp :: Ident) -> Self { Ident { inner , _marker : MARKER , } } fn _new_fallback (inner : fallback :: Ident) -> Self { Ident { inner : imp :: Ident :: from (inner) , _marker : MARKER , } } # [doc = " Creates a new `Ident` with the given `string` as well as the specified"] # [doc = " `span`."] # [doc = ""] # [doc = " The `string` argument must be a valid identifier permitted by the"] # [doc = " language, otherwise the function will panic."] # [doc = ""] # [doc = " Note that `span`, currently in rustc, configures the hygiene information"] # [doc = " for this identifier."] # [doc = ""] # [doc = " As of this time `Span::call_site()` explicitly opts-in to \"call-site\""] # [doc = " hygiene meaning that identifiers created with this span will be resolved"] # [doc = " as if they were written directly at the location of the macro call, and"] # [doc = " other code at the macro call site will be able to refer to them as well."] # [doc = ""] # [doc = " Later spans like `Span::def_site()` will allow to opt-in to"] # [doc = " \"definition-site\" hygiene meaning that identifiers created with this"] # [doc = " span will be resolved at the location of the macro definition and other"] # [doc = " code at the macro call site will not be able to refer to them."] # [doc = ""] # [doc = " Due to the current importance of hygiene this constructor, unlike other"] # [doc = " tokens, requires a `Span` to be specified at construction."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the input string is neither a keyword nor a legal variable"] # [doc = " name. If you are not sure whether the string contains an identifier and"] # [doc = " need to handle an error case, use"] # [doc = " <a href=\"https://docs.rs/syn/2.0/syn/fn.parse_str.html\"><code"] # [doc = "   style=\"padding-right:0;\">syn::parse_str</code></a><code"] # [doc = "   style=\"padding-left:0;\">::&lt;Ident&gt;</code>"] # [doc = " rather than `Ident::new`."] # [track_caller] pub fn new (string : & str , span : Span) -> Self { Ident :: _new (imp :: Ident :: new_checked (string , span . inner)) } # [doc = " Same as `Ident::new`, but creates a raw identifier (`r#ident`). The"] # [doc = " `string` argument must be a valid identifier permitted by the language"] # [doc = " (including keywords, e.g. `fn`). Keywords which are usable in path"] # [doc = " segments (e.g. `self`, `super`) are not supported, and will cause a"] # [doc = " panic."] # [track_caller] pub fn new_raw (string : & str , span : Span) -> Self { Ident :: _new (imp :: Ident :: new_raw_checked (string , span . inner)) } # [doc = " Returns the span of this `Ident`."] pub fn span (& self) -> Span { Span :: _new (self . inner . span ()) } # [doc = " Configures the span of this `Ident`, possibly changing its hygiene"] # [doc = " context."] pub fn set_span (& mut self , span : Span) { self . inner . set_span (span . inner) ; } }
    };
}

impl_225!();