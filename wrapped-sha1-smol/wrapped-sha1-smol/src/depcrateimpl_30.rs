// Generated macro for impl_30 (impl)
macro_rules! Depcrateimpl_30 {
() => {
// Module: crate
// Provides: {"impl_30"}
// Dependencies: {}
impl Sha1 { # [doc = " Creates an fresh sha1 hash object."] # [doc = ""] # [doc = " This is equivalent to creating a hash with `Default::default`."] pub fn new () -> Sha1 { Sha1 { state : DEFAULT_STATE , len : 0 , blocks : Blocks { len : 0 , block : [0 ; 64] , } , } } # [doc = " Shortcut to create a sha1 from some bytes."] # [doc = ""] # [doc = " This also lets you create a hash from a utf-8 string.  This is equivalent"] # [doc = " to making a new Sha1 object and calling `update` on it once."] pub fn from < D : AsRef < [u8] > > (data : D) -> Sha1 { let mut rv = Sha1 :: new () ; rv . update (data . as_ref ()) ; rv } # [doc = " Resets the hash object to it's initial state."] pub fn reset (& mut self) { self . state = DEFAULT_STATE ; self . len = 0 ; self . blocks . len = 0 ; } # [doc = " Update hash with input data."] pub fn update (& mut self , data : & [u8]) { let len = & mut self . len ; let state = & mut self . state ; self . blocks . input (data , | block | { * len += block . len () as u64 ; state . process (block) ; }) } # [doc = " Retrieve digest result."] pub fn digest (& self) -> Digest { let mut state = self . state ; let bits = (self . len + (self . blocks . len as u64)) * 8 ; let extra = [(bits >> 56) as u8 , (bits >> 48) as u8 , (bits >> 40) as u8 , (bits >> 32) as u8 , (bits >> 24) as u8 , (bits >> 16) as u8 , (bits >> 8) as u8 , (bits >> 0) as u8 ,] ; let mut last = [0 ; 128] ; let blocklen = self . blocks . len as usize ; last [.. blocklen] . clone_from_slice (& self . blocks . block [.. blocklen]) ; last [blocklen] = 0x80 ; if blocklen < 56 { last [56 .. 64] . clone_from_slice (& extra) ; state . process (as_block (& last [0 .. 64])) ; } else { last [120 .. 128] . clone_from_slice (& extra) ; state . process (as_block (& last [0 .. 64])) ; state . process (as_block (& last [64 .. 128])) ; } Digest { data : state } } # [doc = " Retrieve the digest result as hex string directly."] # [doc = ""] # [doc = " (The function is only available if the `alloc` feature is enabled)"] # [cfg (feature = "alloc")] pub fn hexdigest (& self) -> std :: string :: String { use std :: string :: ToString ; self . digest () . to_string () } }
};
}
