// Generated macro for impl_29 (impl)
macro_rules! Depcrate_montyimpl_29 {
() => {
// Module: crate::monty
// Provides: {"impl_29"}
// Dependencies: {}
impl < MOD , const LIMBS : usize > MontyFieldElement < MOD , LIMBS > where MOD : MontyFieldParams < LIMBS > , { # [doc = " Zero element (additive identity)."] pub const ZERO : Self = Self { inner : MontyForm :: ZERO , } ; # [doc = " Multiplicative identity."] pub const ONE : Self = Self { inner : MontyForm :: ONE , } ; # [doc = " Number of limbs used by the internal integer representation."] pub const LIMBS : usize = LIMBS ; # [doc = " Decode field element from a canonical bytestring representation."] # [inline] pub fn from_bytes (repr : & MontyFieldBytes < MOD , LIMBS >) -> CtOption < Self > where Uint < LIMBS > : ArrayEncoding , { let mut byte_array = ByteArray :: < Uint < LIMBS > > :: default () ; debug_assert ! (repr . len () <= byte_array . len ()) ; let offset = byte_array . len () . saturating_sub (repr . len ()) ; let uint = match MOD :: BYTE_ORDER { ByteOrder :: BigEndian => { byte_array [offset ..] . copy_from_slice (repr) ; Uint :: from_be_byte_array (byte_array) } ByteOrder :: LittleEndian => { byte_array [.. offset] . copy_from_slice (repr) ; Uint :: from_le_byte_array (byte_array) } } ; Self :: from_uint (& uint) } # [doc = " Decode field element from a canonical byte slice."] # [doc = ""] # [doc = " Slice is expected to be zero padded to the expected byte size."] # [inline] pub fn from_slice (slice : & [u8]) -> Option < Self > where Uint < LIMBS > : ArrayEncoding , { let array = Array :: try_from (slice) . ok () ? ; Self :: from_bytes (& array) . into () } # [doc = " Decode a field element from hex-encoded bytes."] # [doc = ""] # [doc = " This is primarily intended for defining constants using hex literals."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " - When hex is malformed"] # [doc = " - When input is the wrong length"] # [doc = " - If input overflows the modulus"] pub const fn from_hex_vartime (hex : & str) -> Self { let uint = match MOD :: BYTE_ORDER { ByteOrder :: BigEndian => Uint :: from_be_hex (hex) , ByteOrder :: LittleEndian => Uint :: from_le_hex (hex) , } ; assert ! (uint . cmp_vartime (MOD :: PARAMS . modulus () . as_ref ()) . is_lt () , "hex encoded field element overflows modulus") ; Self :: from_uint_reduced (& uint) } # [doc = " Convert [`Uint`] into [`MontyFieldElement`], first converting it into Montgomery form:"] # [doc = ""] # [doc = " ```text"] # [doc = " w * R^2 * R^-1 mod p = wR mod p"] # [doc = " ```"] # [doc = ""] # [doc = " Reduces the input modulo `p`."] # [inline] pub const fn from_uint_reduced (uint : & Uint < LIMBS >) -> Self { Self { inner : MontyForm :: new (uint) , } } # [doc = " Convert [`Uint`] into [`MontyFieldElement`], first converting it into Montgomery form:"] # [doc = ""] # [doc = " ```text"] # [doc = " w * R^2 * R^-1 mod p = wR mod p"] # [doc = " ```"] # [doc = ""] # [doc = " # Returns"] # [doc = ""] # [doc = " The `CtOption` equivalent of `None` if the input overflows the modulus."] # [inline] pub fn from_uint (uint : & Uint < LIMBS >) -> CtOption < Self > { let is_some = uint . ct_lt (MOD :: PARAMS . modulus ()) ; CtOption :: new (Self :: from_uint_reduced (uint) , is_some) } # [doc = " Convert a `u64` into a [`MontyFieldElement`]."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the modulus is 64-bits or smaller."] # [inline] pub const fn from_u64 (w : u64) -> Self { if MOD :: PARAMS . modulus () . as_ref () . bits () <= 64 { panic ! ("modulus is too small to ensure all u64s are in range") ; } Self :: from_uint_reduced (& Uint :: from_u64 (w)) } # [doc = " Create [`MontyFieldElement`] from a [`Uint`] which is already in Montgomery form."] # [doc = ""] # [doc = " # ⚠\u{fe0f} Warning"] # [doc = ""] # [doc = " This value is expected to be in Montgomery form and reduced. Failure to maintain these"] # [doc = " invariants will lead to miscomputation and potential security issues!"] # [inline] pub const fn from_montgomery (uint : Uint < LIMBS >) -> Self { Self { inner : MontyForm :: from_montgomery (uint) , } } # [doc = " Helper function to construct [`MontyFieldElement`] from words in Montgomery form."] # [inline] pub const fn from_montgomery_words (words : [Word ; LIMBS]) -> Self { Self :: from_montgomery (Uint :: from_words (words)) } # [doc = " Borrow the inner [`Uint`] type which is in Montgomery form."] # [doc = ""] # [doc = " # ⚠\u{fe0f} Warning"] # [doc = ""] # [doc = " Make sure you are actually expecting a value in Montgomery form! This is not the correct"] # [doc = " function for converting *out* of Montgomery form: that would be"] # [doc = " [`MontyFieldElement::to_canonical`]."] pub const fn as_montgomery (& self) -> & Uint < LIMBS > { self . inner . as_montgomery () } # [doc = " Retrieve the Montgomery form representation as an array of [`Word`]s."] pub const fn to_montgomery_words (& self) -> [Word ; LIMBS] { self . as_montgomery () . to_words () } # [doc = " Returns the bytestring encoding of this field element."] # [inline] pub fn to_bytes (self) -> MontyFieldBytes < MOD , LIMBS > where MOD : MontyFieldParams < LIMBS > , Uint < LIMBS > : ArrayEncoding , { let mut repr = MontyFieldBytes :: < MOD , LIMBS > :: default () ; debug_assert ! (repr . len () <= < Uint ::< LIMBS > as ArrayEncoding >:: ByteSize :: USIZE) ; let offset = < Uint < LIMBS > as ArrayEncoding > :: ByteSize :: USIZE . saturating_sub (repr . len ()) ; match MOD :: BYTE_ORDER { ByteOrder :: BigEndian => { let padded = self . inner . retrieve () . to_be_byte_array () ; repr . copy_from_slice (& padded [offset ..]) ; } ByteOrder :: LittleEndian => { let padded = self . inner . retrieve () . to_le_byte_array () ; repr . copy_from_slice (& padded [.. offset]) ; } } repr } # [doc = " Determine if this field element is odd: `self mod 2 == 1`."] # [doc = ""] # [doc = " # Returns"] # [doc = ""] # [doc = " If odd, return `Choice(1)`.  Otherwise, return `Choice(0)`."] # [inline] pub fn is_odd (& self) -> Choice { self . inner . retrieve () . is_odd () } # [doc = " Determine if this field element is even: `self mod 2 == 0`."] # [doc = ""] # [doc = " # Returns"] # [doc = ""] # [doc = " If even, return `Choice(1)`.  Otherwise, return `Choice(0)`."] # [inline] pub fn is_even (& self) -> Choice { ! self . is_odd () } # [doc = " Determine if this field element is zero."] # [doc = ""] # [doc = " # Returns"] # [doc = ""] # [doc = " If zero, return `Choice(1)`.  Otherwise, return `Choice(0)`."] # [inline] pub fn is_zero (& self) -> Choice { self . ct_eq (& Self :: ZERO) } # [doc = " Translate field element out of the Montgomery domain, returning a [`Uint`] in canonical form."] # [inline] pub const fn to_canonical (self) -> Uint < LIMBS > { self . inner . retrieve () } # [doc = " Add elements."] # [inline] pub const fn add (& self , rhs : & Self) -> Self { Self { inner : MontyForm :: add (& self . inner , & rhs . inner) , } } # [doc = " Double element (add it to itself)."] # [inline] # [must_use] pub const fn double (& self) -> Self { self . add (self) } # [doc = " Subtract elements."] # [inline] pub const fn sub (& self , rhs : & Self) -> Self { Self { inner : MontyForm :: sub (& self . inner , & rhs . inner) , } } # [doc = " Multiply elements."] # [inline] pub const fn multiply (& self , rhs : & Self) -> Self { Self { inner : MontyForm :: mul (& self . inner , & rhs . inner) , } } # [doc = " Negate element."] # [inline] pub const fn neg (& self) -> Self { Self { inner : MontyForm :: neg (& self . inner) , } } # [doc = " Compute modular square."] # [inline] # [must_use] pub const fn square (& self) -> Self { self . multiply (self) } # [doc = " Compute field inversion: `1 / self`."] # [inline] pub fn invert (& self) -> CtOption < Self > { CtOption :: from (self . inner . invert ()) . map (| inner | Self { inner }) } # [doc = " Compute field inversion as a `const fn`. Panics if `self` is zero."] # [doc = ""] # [doc = " This is mainly intended for inverting constants at compile time."] pub const fn const_invert (& self) -> Self { Self { inner : self . inner . invert () . expect ("input to invert should be non-zero") , } } # [doc = " Returns `self^exp`, where `exp` is a little-endian integer exponent."] # [doc = ""] # [doc = " **This operation is variable time with respect to the exponent `exp`.**"] # [doc = ""] # [doc = " If `exp` is fixed, this operation is constant time. Note that `exp` will still be branched"] # [doc = " upon and should NOT be a secret."] pub const fn pow_vartime < const RHS_LIMBS : usize > (& self , exp : & Uint < RHS_LIMBS >) -> Self { let mut i = RHS_LIMBS - 1 ; while i > 0 && exp . as_words () [i] == 0 { i -= 1 ; } let mut res = Self :: ONE ; loop { let mut j = Limb :: BITS ; while j > 0 { j -= 1 ; res = res . square () ; if ((exp . as_words () [i] >> j) & 1) == 1 { res = res . multiply (self) ; } } if i == 0 { return res ; } i -= 1 ; } } # [doc = " Returns `self^(2^n) mod p`."] # [doc = ""] # [doc = " **This operation is variable time with respect to the exponent `n`.**"] # [doc = ""] # [doc = " If the exponent is fixed, this operation is constant time."] pub const fn sqn_vartime (& self , n : usize) -> Self { let mut x = * self ; let mut i = 0 ; while i < n { x = x . square () ; i += 1 ; } x } }
};
}
