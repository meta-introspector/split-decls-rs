// Generated macro for macro_72 (macro)
macro_rules! Depcrate_opcodemacro_72 {
() => {
// Module: crate::opcode
// Provides: {"macro_72"}
// Dependencies: {}
opcode ! { # [doc = " Receive multiple messages on a socket, equivalent to `recvmsg(2)`."] # [doc = ""] # [doc = " Parameters:"] # [doc = "     msg:       For this multishot variant of ResvMsg, only the msg_namelen and msg_controllen"] # [doc = "                fields are relevant."] # [doc = "     buf_group: The id of the provided buffer pool to use for each received message."] # [doc = ""] # [doc = " See also the description of [`SendMsg`] and [`types::RecvMsgOut`]."] # [doc = ""] # [doc = " The multishot version allows the application to issue a single receive request, which"] # [doc = " repeatedly posts a CQE when data is available. It requires the MSG_WAITALL flag is not set."] # [doc = " Each CQE will take a buffer out of a provided buffer pool for receiving. The application"] # [doc = " should check the flags of each CQE, regardless of its result. If a posted CQE does not have"] # [doc = " the IORING_CQE_F_MORE flag set then the multishot receive will be done and the application"] # [doc = " should issue a new request."] # [doc = ""] # [doc = " Unlike [`RecvMsg`], this multishot recvmsg will prepend a struct which describes the layout"] # [doc = " of the rest of the buffer in combination with the initial msghdr structure submitted with"] # [doc = " the request. Use [`types::RecvMsgOut`] to parse the data received and access its"] # [doc = " components."] # [doc = ""] # [doc = " The recvmsg multishot variant is available since kernel 6.0."] # [derive (Debug)] pub struct RecvMsgMulti { fd : { impl sealed :: UseFixed } , msg : { * const libc :: msghdr } , buf_group : { u16 } , ;; ioprio : u16 = 0 , flags : u32 = 0 } pub const CODE = sys :: IORING_OP_RECVMSG ; pub fn build (self) -> Entry { let RecvMsgMulti { fd , msg , buf_group , ioprio , flags } = self ; let mut sqe = sqe_zeroed () ; sqe . opcode = Self :: CODE ; assign_fd ! (sqe . fd = fd) ; sqe . __bindgen_anon_2 . addr = msg as _ ; sqe . len = 1 ; sqe . __bindgen_anon_3 . msg_flags = flags ; sqe . __bindgen_anon_4 . buf_group = buf_group ; sqe . flags |= crate :: squeue :: Flags :: BUFFER_SELECT . bits () ; sqe . ioprio = ioprio | (sys :: IORING_RECV_MULTISHOT as u16) ; Entry (sqe) } }
};
}
