macro_rules! statx {
    () => {
        # [doc = " Opaque types, you should use [`statx`](struct@libc::statx) instead."] # [repr (C)] # [allow (non_camel_case_types)] pub struct statx { _priv : () , }
    };
}

statx!();