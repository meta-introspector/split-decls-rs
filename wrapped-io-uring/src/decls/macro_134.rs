macro_rules! macro_134 {
    () => {
        bitflags ! { # [doc = " Submission flags"] # [derive (Debug , Clone , Copy , PartialEq , Eq , PartialOrd , Ord)] pub struct Flags : u8 { # [doc = " When this flag is specified,"] # [doc = " `fd` is an index into the files array registered with the io_uring instance."] # [doc (hidden)] const FIXED_FILE = 1 << sys :: IOSQE_FIXED_FILE_BIT ; # [doc = " When this flag is specified,"] # [doc = " the SQE will not be started before previously submitted SQEs have completed,"] # [doc = " and new SQEs will not be started before this one completes."] const IO_DRAIN = 1 << sys :: IOSQE_IO_DRAIN_BIT ; # [doc = " When this flag is specified,"] # [doc = " it forms a link with the next SQE in the submission ring."] # [doc = " That next SQE will not be started before this one completes."] const IO_LINK = 1 << sys :: IOSQE_IO_LINK_BIT ; # [doc = " Like [`IO_LINK`](Self::IO_LINK), but it doesn’t sever regardless of the completion"] # [doc = " result."] const IO_HARDLINK = 1 << sys :: IOSQE_IO_HARDLINK_BIT ; # [doc = " Normal operation for io_uring is to try and issue an sqe as non-blocking first,"] # [doc = " and if that fails, execute it in an async manner."] # [doc = ""] # [doc = " To support more efficient overlapped operation of requests"] # [doc = " that the application knows/assumes will always (or most of the time) block,"] # [doc = " the application can ask for an sqe to be issued async from the start."] const ASYNC = 1 << sys :: IOSQE_ASYNC_BIT ; # [doc = " Conceptually the kernel holds a set of buffers organized into groups. When you issue a"] # [doc = " request with this flag and set `buf_group` to a valid buffer group ID (e.g."] # [doc = " [`buf_group` on `Read`](crate::opcode::Read::buf_group)) then once the file descriptor"] # [doc = " becomes ready the kernel will try to take a buffer from the group."] # [doc = ""] # [doc = " If there are no buffers in the group, your request will fail with `-ENOBUFS`. Otherwise,"] # [doc = " the corresponding [`cqueue::Entry::flags`](crate::cqueue::Entry::flags) will contain the"] # [doc = " chosen buffer ID, encoded with:"] # [doc = ""] # [doc = " ```text"] # [doc = " (buffer_id << IORING_CQE_BUFFER_SHIFT) | IORING_CQE_F_BUFFER"] # [doc = " ```"] # [doc = ""] # [doc = " You can use [`buffer_select`](crate::cqueue::buffer_select) to take the buffer ID."] # [doc = ""] # [doc = " The buffer will then be removed from the group and won't be usable by other requests"] # [doc = " anymore."] # [doc = ""] # [doc = " You can provide new buffers in a group with"] # [doc = " [`ProvideBuffers`](crate::opcode::ProvideBuffers)."] # [doc = ""] # [doc = " See also [the LWN thread on automatic buffer"] # [doc = " selection](https://lwn.net/Articles/815491/)."] const BUFFER_SELECT = 1 << sys :: IOSQE_BUFFER_SELECT_BIT ; # [doc = " Don't post CQE if request succeeded."] const SKIP_SUCCESS = 1 << sys :: IOSQE_CQE_SKIP_SUCCESS_BIT ; } }
    };
}

macro_134!()