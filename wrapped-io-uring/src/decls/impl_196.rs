macro_rules! deps {
    () => {
        RecvMsgOut!();
    };
}

macro_rules! impl_196 {
    () => {
        deps!();
        impl < 'buf > RecvMsgOut < 'buf > { const DATA_START : usize = std :: mem :: size_of :: < sys :: io_uring_recvmsg_out > () ; # [doc = " Parse the data buffered upon completion of a `RecvMsg` multishot operation."] # [doc = ""] # [doc = " `buffer` is the whole buffer previously provided to the ring, while `msghdr`"] # [doc = " is the same content provided as input to the corresponding SQE"] # [doc = " (only `msg_namelen` and `msg_controllen` fields are relevant)."] # [allow (clippy :: result_unit_err)] # [allow (clippy :: useless_conversion)] pub fn parse (buffer : & 'buf [u8] , msghdr : & libc :: msghdr) -> Result < Self , () > { let msghdr_name_len = usize :: try_from (msghdr . msg_namelen) . unwrap () ; let msghdr_control_len = usize :: try_from (msghdr . msg_controllen) . unwrap () ; if Self :: DATA_START . checked_add (msghdr_name_len) . and_then (| acc | acc . checked_add (msghdr_control_len)) . map (| header_len | buffer . len () < header_len) . unwrap_or (true) { return Err (()) ; } let header = unsafe { buffer . as_ptr () . cast :: < sys :: io_uring_recvmsg_out > () . read_unaligned () } ; let (name_data , control_start) = { let name_start = Self :: DATA_START ; let name_data_end = name_start + usize :: min (usize :: try_from (header . namelen) . unwrap () , msghdr_name_len) ; let name_field_end = name_start + msghdr_name_len ; (& buffer [name_start .. name_data_end] , name_field_end) } ; let (control_data , payload_start) = { let control_data_end = control_start + usize :: min (usize :: try_from (header . controllen) . unwrap () , msghdr_control_len ,) ; let control_field_end = control_start + msghdr_control_len ; (& buffer [control_start .. control_data_end] , control_field_end) } ; let payload_data = { let payload_data_end = payload_start + usize :: min (usize :: try_from (header . payloadlen) . unwrap () , buffer . len () - payload_start ,) ; & buffer [payload_start .. payload_data_end] } ; Ok (Self { header , msghdr_name_len , name_data , control_data , payload_data , }) } # [doc = " Return the length of the incoming `name` data."] # [doc = ""] # [doc = " This may be larger than the size of the content returned by"] # [doc = " `name_data()`, if the kernel could not fit all the incoming"] # [doc = " data in the provided buffer size. In that case, name data in"] # [doc = " the result buffer gets truncated."] pub fn incoming_name_len (& self) -> u32 { self . header . namelen } # [doc = " Return whether the incoming name data was larger than the provided limit/buffer."] # [doc = ""] # [doc = " When `true`, data returned by `name_data()` is truncated and"] # [doc = " incomplete."] pub fn is_name_data_truncated (& self) -> bool { self . header . namelen as usize > self . msghdr_name_len } # [doc = " Message control data, with the same semantics as `msghdr.msg_control`."] pub fn name_data (& self) -> & [u8] { self . name_data } # [doc = " Return the length of the incoming `control` data."] # [doc = ""] # [doc = " This may be larger than the size of the content returned by"] # [doc = " `control_data()`, if the kernel could not fit all the incoming"] # [doc = " data in the provided buffer size. In that case, control data in"] # [doc = " the result buffer gets truncated."] pub fn incoming_control_len (& self) -> u32 { self . header . controllen } # [doc = " Return whether the incoming control data was larger than the provided limit/buffer."] # [doc = ""] # [doc = " When `true`, data returned by `control_data()` is truncated and"] # [doc = " incomplete."] pub fn is_control_data_truncated (& self) -> bool { (self . header . flags & u32 :: try_from (libc :: MSG_CTRUNC) . unwrap ()) != 0 } # [doc = " Message control data, with the same semantics as `msghdr.msg_control`."] pub fn control_data (& self) -> & [u8] { self . control_data } # [doc = " Return whether the incoming payload was larger than the provided limit/buffer."] # [doc = ""] # [doc = " When `true`, data returned by `payload_data()` is truncated and"] # [doc = " incomplete."] pub fn is_payload_truncated (& self) -> bool { (self . header . flags & u32 :: try_from (libc :: MSG_TRUNC) . unwrap ()) != 0 } # [doc = " Message payload, as buffered by the kernel."] pub fn payload_data (& self) -> & [u8] { self . payload_data } # [doc = " Return the length of the incoming `payload` data."] # [doc = ""] # [doc = " This may be larger than the size of the content returned by"] # [doc = " `payload_data()`, if the kernel could not fit all the incoming"] # [doc = " data in the provided buffer size. In that case, payload data in"] # [doc = " the result buffer gets truncated."] pub fn incoming_payload_len (& self) -> u32 { self . header . payloadlen } # [doc = " Message flags, with the same semantics as `msghdr.msg_flags`."] pub fn flags (& self) -> u32 { self . header . flags } }
    };
}

impl_196!()