// Generated macro for Algorithm (struct)
macro_rules! DepcrateAlgorithm {
() => {
// Module: crate
// Provides: {"Algorithm"}
// Dependencies: {}
# [doc = " This struct describes a CRC algorithm using the fields specified by the [Catalogue of"] # [doc = " parametrised CRC algorithms](https://reveng.sourceforge.io/crc-catalogue/all.htm)."] # [derive (Clone , Copy , Debug , PartialEq , Eq)] pub struct Algorithm < W : Width > { # [doc = " The number of bit cells in the linear feedback shift register; the degree of the generator"] # [doc = " polynomial, minus one."] pub width : u8 , # [doc = " The generator polynomial that sets the feedback tap positions of the shift register. The"] # [doc = " least significant bit corresponds to the inward end of the shift register, and is always"] # [doc = " set. The highest-order term is omitted."] pub poly : W , # [doc = " The settings of the bit cells at the start of each calculation, before reading the first"] # [doc = " message bit. The least significant bit corresponds to the inward end of the shift register."] pub init : W , # [doc = " If equal to `false`, specifies that the characters of the message are read bit-by-bit, most"] # [doc = " significant bit (MSB) first; if equal to `true`, the characters are read bit-by-bit, least"] # [doc = " significant bit (LSB) first. Each sampled message bit is then XORed with the bit being"] # [doc = " simultaneously shifted out of the register at the most significant end, and the result is"] # [doc = " passed to the feedback taps."] pub refin : bool , # [doc = " If equal to `false`, specifies that the contents of the register after reading the last"] # [doc = " message bit are unreflected before presentation; if equal to `true`, it specifies that they"] # [doc = " are reflected, character-by-character, before presentation. For the purpose of this"] # [doc = " definition, the reflection is performed by swapping the content of each cell with that of"] # [doc = " the cell an equal distance from the opposite end of the register; the characters of the CRC"] # [doc = " are then true images of parts of the reflected register, the character containing the"] # [doc = " original MSB always appearing first."] pub refout : bool , # [doc = " The XOR value applied to the contents of the register after the last message bit has been"] # [doc = " read and after the optional reflection. It has the same endianness as the CRC such that its"] # [doc = " true image appears in the characters of the CRC."] pub xorout : W , # [doc = " The contents of the register after initialising, reading the UTF-8 string `\"123456789\"` (as"] # [doc = " 8-bit characters), optionally reflecting, and applying the final XOR."] pub check : W , # [doc = " The contents of the register after initialising, reading an error-free codeword and"] # [doc = " optionally reflecting the register (if [`refout`](Algorithm::refout)=`true`), but not"] # [doc = " applying the final XOR. This is mathematically equivalent to initialising the register with"] # [doc = " the xorout parameter, reflecting it as described (if [`refout`](Algorithm::refout)=`true`),"] # [doc = " reading as many zero bits as there are cells in the register, and reflecting the result (if"] # [doc = " [`refin`](Algorithm::refin)=`true`). The residue of a crossed-endian model is calculated"] # [doc = " assuming that the characters of the received CRC are specially reflected before submitting"] # [doc = " the codeword."] pub residue : W , }
};
}
