// Generated macro for impl_310 (impl)
macro_rules! Depcrate_generatedimpl_310 {
() => {
// Module: crate::generated
// Provides: {"impl_310"}
// Dependencies: {}
# [doc = " XCUIInterruptionMonitoring."] impl XCTestCase { extern_methods ! (# [cfg (all (feature = "block2" , feature = "objc2-xc-ui-automation"))] # [doc = " Adds a monitor to the test. Monitors are automatically removed at the end of the test or can be manually removed using -removeUIInterruptionMonitor:."] # [doc = " Monitors are invoked in the reverse order in which they are added until one of the monitors returns true, indicating that it has handled the interruption."] # [doc = ""] # [doc = ""] # [doc = " Parameter `handlerDescription`: Explanation of the behavior and purpose of this monitor, mainly used for debugging and analysis."] # [doc = ""] # [doc = ""] # [doc = " Parameter `handler`: Handler block for asynchronous, non-deterministic interrupting UI such as alerts and other dialogs. Handlers should return true if they handled the UI, false if they did not."] # [doc = " The handler is passed an XCUIElement representing the top level UI element for the alert."] # [doc = ""] # [doc = ""] # [doc = " Returns: Returns an opaque token that can be used to remove the monitor."] # [doc = ""] # [doc = ""] # [doc = " A \"UI interruption\" is any element which unexpectedly blocks access to an element with which a UI test is trying to interact. Interrupting elements are most commonly alerts,"] # [doc = " dialogs, or other windows, but can be of other types as well. Interruptions are unexpected or at least not deterministic: the appearance of an alert in direct response to"] # [doc = " a test action such as clicking a button is not an interruption and should not be handled using a UI interruption monitor. Instead, it's simply part of the UI and should be"] # [doc = " found using standard queries and interacted with using standard event methods. Note that some interruptions are part of the app's own UI, others are presented on"] # [doc = " behalf of system apps and services, so queries for these elements must be constructed with the right process at their root."] # [doc = ""] # [doc = " Use a UI interruption monitor for alerts that appear unexpectedly or with non-deterministic timing. Monitors are not invoked simply by the appearance of an alert or similar"] # [doc = " UI, they are called when the presence of such UI interferes with actions the test is attempting to take. For example, consider the following sequence:"] # [doc = ""] # [doc = " - test taps button"] # [doc = " - app displays confirmation dialog"] # [doc = ""] # [doc = " In this case, the dialog that is presented can be anticipated by the test, so a UI interruption monitor should not be used. Instead, the sequence should look like:"] # [doc = ""] # [doc = " - test taps button"] # [doc = " - test constructs an XCUIElement for the dialog and uses XCUIElement.waitForExistence(timeout:) to wait for it to appear"] # [doc = " - app displays confirmation dialog"] # [doc = " - test synthesizes a tap for the appropriate button in the dialog"] # [doc = " - test continues execution"] # [doc = ""] # [doc = " There was no UI interruption in this example because every step was deterministic and known in advance. Note the use of XCUIElement.waitForExistence(timeout:) to deal with"] # [doc = " asynchronous delays in the display of the dialog."] # [doc = ""] # [doc = " By contrast, consider the next sequence, where use of a UI interruption monitor is the correct solution:"] # [doc = ""] # [doc = " - test launches app"] # [doc = " - app initiates asynchronous network request"] # [doc = " - test interacts with app"] # [doc = " - network request completes, app decides to display a dialog to the user"] # [doc = " - dialog appears just as the test is about to tap on a button"] # [doc = " - the appearance of the dialog is not deterministic"] # [doc = " - the test can anticipate that the dialog might be displayed at some point, but not when"] # [doc = " - accordingly, the test has installed a UI interruption monitor that knows how to handle the network response dialog"] # [doc = " - when XCTest computes a hit point for the button, it discovers the dialog and treats it as \"interrupting UI\""] # [doc = " - the previously installed UI interruption monitor is invoked"] # [doc = " - it handles the dialog"] # [doc = " - XCTest computes the hit point for the button and synthesizes the requested tap event"] # [doc = " - test execution continues..."] # [doc = ""] # [doc = " Monitors can be designed to be general or specific in how they handle interruptions. The simplest general approach might simply attempt to cancel/dismiss/close"] # [doc = " any interrupting dialog/alert/window without consideration for its contents or purpose. A more specific monitor might make decisions based on the UI and contents"] # [doc = " of the interruption. Tests may install multiple monitors, which will be invoked in reverse order of installation. If a more specific monitor wishes to be skipped for a"] # [doc = " given interruption, its handler can simply return false - the next monitor will be invoked, and so on, until one of them returns true, signifying that it has handled"] # [doc = " the interruption. In some cases, a default monitor may handle interruptions."] # [unsafe (method (addUIInterruptionMonitorWithDescription : handler :))] # [unsafe (method_family = none)] pub fn addUIInterruptionMonitorWithDescription_handler (& self , handler_description : & NSString , handler : & block2 :: DynBlock < dyn Fn (NonNull < XCUIElement >) -> Bool >,) -> Retained < ProtocolObject < dyn NSObjectProtocol >>; # [doc = " Removes a monitor using the token provided when it was added."] # [doc = ""] # [doc = ""] # [doc = " Parameter `monitor`: The token representing the monitor returned from the call to addUIInterruptionMonitorWithDescription:handler: where it was registered."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `monitor` should be of the correct type."] # [unsafe (method (removeUIInterruptionMonitor :))] # [unsafe (method_family = none)] pub unsafe fn removeUIInterruptionMonitor (& self , monitor : & ProtocolObject < dyn NSObjectProtocol >,) ;) ; }
};
}
