// Generated macro for PermissionPriv (enum)
macro_rules! Depcrate_borrow_tracker_tree_borrows_permsPermissionPriv {
() => {
// Module: crate::borrow_tracker::tree_borrows::perms
// Provides: {"PermissionPriv"}
// Dependencies: {}
# [doc = " The activation states of a pointer."] # [derive (Debug , Clone , Copy , PartialEq , Eq , Hash)] enum PermissionPriv { # [doc = " represents: a shared reference to interior mutable data."] # [doc = " allows: all foreign and child accesses;"] # [doc = " rejects: nothing"] Cell , # [doc = " represents: a local mutable reference that has not yet been written to;"] # [doc = " allows: child reads, foreign reads;"] # [doc = " affected by: child writes (becomes Active),"] # [doc = " rejects: foreign writes (Disabled)."] # [doc = ""] # [doc = " `ReservedFrz` is mostly for types that are `Freeze` (no interior mutability)."] # [doc = " If the type has interior mutability, see `ReservedIM` instead."] # [doc = " (Note: since the discovery of `tests/fail/tree_borrows/reservedim_spurious_write.rs`,"] # [doc = " we also use `ReservedFreeze` for mutable references that were retagged with a protector"] # [doc = " independently of interior mutability)"] # [doc = ""] # [doc = " special case: behaves differently when protected, which is where `conflicted`"] # [doc = " is relevant"] # [doc = " - `conflicted` is set on foreign reads,"] # [doc = " - `conflicted` must not be set on child writes (there is UB otherwise)."] # [doc = ""] # [doc = " This is so that the behavior of `Reserved` adheres to the rules of `noalias`:"] # [doc = " - foreign-read then child-write is UB due to `conflicted`,"] # [doc = " - child-write then foreign-read is UB since child-write will activate and then"] # [doc = "   foreign-read disables a protected `Active`, which is UB."] ReservedFrz { conflicted : bool } , # [doc = " Alternative version of `ReservedFrz` made for types with interior mutability."] # [doc = " allows: child reads, foreign reads, foreign writes (extra);"] # [doc = " affected by: child writes (becomes Active);"] # [doc = " rejects: nothing."] ReservedIM , # [doc = " represents: a unique pointer;"] # [doc = " allows: child reads, child writes;"] # [doc = " rejects: foreign reads (Frozen), foreign writes (Disabled)."] Active , # [doc = " represents: a shared pointer;"] # [doc = " allows: all read accesses;"] # [doc = " rejects child writes (UB), foreign writes (Disabled)."] Frozen , # [doc = " represents: a dead pointer;"] # [doc = " allows: all foreign accesses;"] # [doc = " rejects: all child accesses (UB)."] Disabled , }
};
}
