// Generated macro for GlobalStateInner (struct)
macro_rules! Depcrate_alloc_addressesGlobalStateInner {
() => {
// Module: crate::alloc_addresses
// Provides: {"GlobalStateInner"}
// Dependencies: {}
# [derive (Debug)] pub struct GlobalStateInner { # [doc = " This is used as a map between the address of each allocation and its `AllocId`. It is always"] # [doc = " sorted by address. We cannot use a `HashMap` since we can be given an address that is offset"] # [doc = " from the base address, and we need to find the `AllocId` it belongs to. This is not the"] # [doc = " *full* inverse of `base_addr`; dead allocations have been removed."] int_to_ptr_map : Vec < (u64 , AllocId) > , # [doc = " The base address for each allocation.  We cannot put that into"] # [doc = " `AllocExtra` because function pointers also have a base address, and"] # [doc = " they do not have an `AllocExtra`."] # [doc = " This is the inverse of `int_to_ptr_map`."] base_addr : FxHashMap < AllocId , u64 > , # [doc = " Temporarily store prepared memory space for global allocations the first time their memory"] # [doc = " address is required. This is used to ensure that the memory is allocated before Miri assigns"] # [doc = " it an internal address, which is important for matching the internal address to the machine"] # [doc = " address so FFI can read from pointers."] prepared_alloc_bytes : FxHashMap < AllocId , MiriAllocBytes > , # [doc = " A pool of addresses we can reuse for future allocations."] reuse : ReusePool , # [doc = " Whether an allocation has been exposed or not. This cannot be put"] # [doc = " into `AllocExtra` for the same reason as `base_addr`."] exposed : FxHashSet < AllocId > , # [doc = " This is used as a memory address when a new pointer is casted to an integer. It"] # [doc = " is always larger than any address that was previously made part of a block."] next_base_addr : u64 , # [doc = " The provenance to use for int2ptr casts"] provenance_mode : ProvenanceMode , }
};
}
