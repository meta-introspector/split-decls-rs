// Generated macro for horizontal_bin_op (function)
macro_rules! Depcrate_shims_x86horizontal_bin_op {
() => {
// Module: crate::shims::x86
// Provides: {"horizontal_bin_op"}
// Dependencies: {}
# [doc = " Horizontally performs `which` operation on adjacent values of"] # [doc = " `left` and `right` SIMD vectors and stores the result in `dest`."] # [doc = " \"Horizontal\" means that the i-th output element is calculated"] # [doc = " from the elements 2*i and 2*i+1 of the concatenation of `left` and"] # [doc = " `right`."] # [doc = ""] # [doc = " Each 128-bit chunk is treated independently (i.e., the value for"] # [doc = " the is i-th 128-bit chunk of `dest` is calculated with the i-th"] # [doc = " 128-bit chunks of `left` and `right`)."] fn horizontal_bin_op < 'tcx > (ecx : & mut crate :: MiriInterpCx < 'tcx > , which : mir :: BinOp , saturating : bool , left : & OpTy < 'tcx > , right : & OpTy < 'tcx > , dest : & MPlaceTy < 'tcx > ,) -> InterpResult < 'tcx , () > { assert_eq ! (left . layout , dest . layout) ; assert_eq ! (right . layout , dest . layout) ; let (num_chunks , items_per_chunk , left) = split_simd_to_128bit_chunks (ecx , left) ? ; let (_ , _ , right) = split_simd_to_128bit_chunks (ecx , right) ? ; let (_ , _ , dest) = split_simd_to_128bit_chunks (ecx , dest) ? ; let middle = items_per_chunk / 2 ; for i in 0 .. num_chunks { let left = ecx . project_index (& left , i) ? ; let right = ecx . project_index (& right , i) ? ; let dest = ecx . project_index (& dest , i) ? ; for j in 0 .. items_per_chunk { let (k , src) = if j < middle { (j , & left) } else { (j . strict_sub (middle) , & right) } ; let base_i = k . strict_mul (2) ; let lhs = ecx . read_immediate (& ecx . project_index (src , base_i) ?) ? ; let rhs = ecx . read_immediate (& ecx . project_index (src , base_i . strict_add (1)) ?) ? ; let res = if saturating { Immediate :: from (ecx . saturating_arith (which , & lhs , & rhs) ?) } else { * ecx . binary_op (which , & lhs , & rhs) ? } ; ecx . write_immediate (res , & ecx . project_index (& dest , j) ?) ? ; } } interp_ok (()) }
};
}
