// Generated macro for EvalContextExt (trait)
macro_rules! Depcrate_shims_x86_aesniEvalContextExt {
() => {
// Module: crate::shims::x86::aesni
// Provides: {"EvalContextExt"}
// Dependencies: {}
pub (super) trait EvalContextExt < 'tcx > : crate :: MiriInterpCxExt < 'tcx > { fn emulate_x86_aesni_intrinsic (& mut self , link_name : Symbol , abi : & FnAbi < 'tcx , Ty < 'tcx > > , args : & [OpTy < 'tcx >] , dest : & MPlaceTy < 'tcx > ,) -> InterpResult < 'tcx , EmulateItemResult > { let this = self . eval_context_mut () ; this . expect_target_feature_for_intrinsic (link_name , "aes") ? ; let unprefixed_name = link_name . as_str () . strip_prefix ("llvm.x86.aesni.") . unwrap () ; match unprefixed_name { "aesdec" | "aesdec.256" | "aesdec.512" => { let [state , key] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; aes_round (this , state , key , dest , | state , key | { let key = aes :: Block :: from (key . to_le_bytes ()) ; let mut state = aes :: Block :: from (state . to_le_bytes ()) ; aes :: hazmat :: equiv_inv_cipher_round (& mut state , & key) ; u128 :: from_le_bytes (state . into ()) }) ? ; } "aesdeclast" | "aesdeclast.256" | "aesdeclast.512" => { let [state , key] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; aes_round (this , state , key , dest , | state , key | { let mut state = aes :: Block :: from (state . to_le_bytes ()) ; aes :: hazmat :: equiv_inv_cipher_round (& mut state , & aes :: Block :: from ([0 ; 16])) ; aes :: hazmat :: mix_columns (& mut state) ; u128 :: from_le_bytes (state . into ()) ^ key }) ? ; } "aesenc" | "aesenc.256" | "aesenc.512" => { let [state , key] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; aes_round (this , state , key , dest , | state , key | { let key = aes :: Block :: from (key . to_le_bytes ()) ; let mut state = aes :: Block :: from (state . to_le_bytes ()) ; aes :: hazmat :: cipher_round (& mut state , & key) ; u128 :: from_le_bytes (state . into ()) }) ? ; } "aesenclast" | "aesenclast.256" | "aesenclast.512" => { let [state , key] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; aes_round (this , state , key , dest , | state , key | { let mut state = aes :: Block :: from (state . to_le_bytes ()) ; aes :: hazmat :: cipher_round (& mut state , & aes :: Block :: from ([0 ; 16])) ; aes :: hazmat :: inv_mix_columns (& mut state) ; u128 :: from_le_bytes (state . into ()) ^ key }) ? ; } "aesimc" => { let [op] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; let op = op . transmute (this . machine . layouts . u128 , this) ? ; let dest = dest . transmute (this . machine . layouts . u128 , this) ? ; let state = this . read_scalar (& op) ? . to_u128 () ? ; let mut state = aes :: Block :: from (state . to_le_bytes ()) ; aes :: hazmat :: inv_mix_columns (& mut state) ; this . write_scalar (Scalar :: from_u128 (u128 :: from_le_bytes (state . into ())) , & dest) ? ; } _ => return interp_ok (EmulateItemResult :: NotSupported) , } interp_ok (EmulateItemResult :: NeedsReturn) } }
};
}
