// Generated macro for IdempotentForeignAccess (enum)
macro_rules! Depcrate_borrow_tracker_tree_borrows_foreign_access_skippingIdempotentForeignAccess {
() => {
// Module: crate::borrow_tracker::tree_borrows::foreign_access_skipping
// Provides: {"IdempotentForeignAccess"}
// Dependencies: {}
# [doc = " To speed up tree traversals, we want to skip traversing subtrees when we know the traversal will have no effect."] # [doc = " This is often the case for foreign accesses, since usually foreign accesses happen several times in a row, but also"] # [doc = " foreign accesses are idempotent. In particular, see tests `foreign_read_is_noop_after_foreign_write` and `all_transitions_idempotent`."] # [doc = " Thus, for each node we keep track of the \"strongest idempotent foreign access\" (SIFA), i.e. which foreign access can be skipped."] # [doc = " Note that for correctness, it is not required that this is the strongest access, just any access it is idempotent under. In particular, setting"] # [doc = " it to `None` is always correct, but the point of this optimization is to have it be as strong as possible so that more accesses can be skipped."] # [doc = " This enum represents the kinds of values we store:"] # [doc = " - `None` means that the node (and its subtrees) are not (guaranteed to be) idempotent under any foreign access."] # [doc = " - `Read` means that the node (and its subtrees) are idempotent under foreign reads, but not (yet / necessarily) under foreign writes."] # [doc = " - `Write` means that the node (and its subtrees) are idempotent under foreign writes. This also implies that it is idempotent under foreign"] # [doc = "   reads, since reads are stronger than writes (see test `foreign_read_is_noop_after_foreign_write`). In other words, this node can be skipped"] # [doc = "   for all foreign accesses."] # [doc = ""] # [doc = " Since a traversal does not just visit a node, but instead the entire subtree, the SIFA field for a given node indicates that the access to"] # [doc = " *the entire subtree* rooted at that node can be skipped. In order for this to work, we maintain the global invariant that at"] # [doc = " each location, the SIFA at each child must be stronger than that at the parent. For normal reads and writes, this is easily accomplished by"] # [doc = " tracking each foreign access as it occurs, so that then the next access can be skipped. This also obviously maintains the invariant, because"] # [doc = " if a node undergoes a foreign access, then all its children also see this as a foreign access. However, the invariant is broken during retags,"] # [doc = " because retags act across the entire allocation, but only emit a read event across a specific range. This means that for all nodes outside that"] # [doc = " range, the invariant is potentially broken, since a new child with a weaker SIFA is inserted. Thus, during retags, special care is taken to"] # [doc = " \"manually\" reset the parent's SIFA to be at least as strong as the new child's. This is accomplished with the `ensure_no_stronger_than` method."] # [doc = ""] # [doc = " Note that we derive Ord and PartialOrd, so the order in which variants are listed below matters:"] # [doc = " None < Read < Write. Do not change that order. See the `test_order` test."] # [derive (Clone , Copy , PartialEq , Eq , PartialOrd , Ord , Hash , Debug , Default)] pub enum IdempotentForeignAccess { # [default] None , Read , Write , }
};
}
