// Generated macro for impl_294 (impl)
macro_rules! Depcrate_borrow_tracker_tree_borrows_treeimpl_294 {
() => {
// Module: crate::borrow_tracker::tree_borrows::tree
// Provides: {"impl_294"}
// Dependencies: {}
impl < 'tree > TreeVisitor < 'tree > { # [doc = " Applies `f_propagate` to every vertex of the tree in a piecewise bottom-up way: First, visit"] # [doc = " all ancestors of `start_idx` (starting with `start_idx` itself), then children of `start_idx`, then the rest,"] # [doc = " going bottom-up in each of these two \"pieces\" / sections."] # [doc = " This ensures that errors are triggered in the following order"] # [doc = " - first invalid accesses with insufficient permissions, closest to the accessed node first,"] # [doc = " - then protector violations, bottom-up, starting with the children of the accessed node, and then"] # [doc = "   going upwards and outwards."] # [doc = ""] # [doc = " The following graphic visualizes it, with numbers indicating visitation order and `start_idx` being"] # [doc = " the node that is visited first (\"1\"):"] # [doc = ""] # [doc = " ```text"] # [doc = "         3"] # [doc = "        /|"] # [doc = "       / |"] # [doc = "      9  2"] # [doc = "      |  |\\"] # [doc = "      |  | \\"] # [doc = "      8  1  7"] # [doc = "        / \\"] # [doc = "       4   6"] # [doc = "           |"] # [doc = "           5"] # [doc = " ```"] # [doc = ""] # [doc = " `f_propagate` should follow the following format: for a given `Node` it updates its"] # [doc = " `Permission` depending on the position relative to `start_idx` (given by an"] # [doc = " `AccessRelatedness`)."] # [doc = " `f_continue` is called earlier on foreign nodes, and describes whether to even start"] # [doc = " visiting the subtree at that node. If it e.g. returns `SkipSelfAndChildren` on node 6"] # [doc = " above, then nodes 5 _and_ 6 would not be visited by `f_propagate`. It is not used for"] # [doc = " notes having a child access (nodes 1, 2, 3)."] # [doc = ""] # [doc = " Finally, remember that the iteration order is not relevant for UB, it only affects"] # [doc = " diagnostics. It also affects tree traversal optimizations built on top of this, so"] # [doc = " those need to be reviewed carefully as well whenever this changes."] fn traverse_this_parents_children_other < Err > (mut self , start_idx : UniIndex , f_continue : impl Fn (& NodeAppArgs < '_ >) -> ContinueTraversal , f_propagate : impl Fn (NodeAppArgs < '_ >) -> Result < () , Err > ,) -> Result < () , Err > { let mut stack = TreeVisitorStack :: new (f_continue , f_propagate) ; stack . go_upwards_from_accessed (& mut self , start_idx , ChildrenVisitMode :: VisitChildrenOfAccessed ,) ? ; stack . finish_foreign_accesses (& mut self) } # [doc = " Like `traverse_this_parents_children_other`, but skips the children of `start_idx`."] fn traverse_nonchildren < Err > (mut self , start_idx : UniIndex , f_continue : impl Fn (& NodeAppArgs < '_ >) -> ContinueTraversal , f_propagate : impl Fn (NodeAppArgs < '_ >) -> Result < () , Err > ,) -> Result < () , Err > { let mut stack = TreeVisitorStack :: new (f_continue , f_propagate) ; stack . go_upwards_from_accessed (& mut self , start_idx , ChildrenVisitMode :: SkipChildrenOfAccessed ,) ? ; stack . finish_foreign_accesses (& mut self) } }
};
}
