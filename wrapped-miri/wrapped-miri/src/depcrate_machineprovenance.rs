// Generated macro for Provenance (enum)
macro_rules! Depcrate_machineProvenance {
() => {
// Module: crate::machine
// Provides: {"Provenance"}
// Dependencies: {}
# [doc = " Pointer provenance."] # [derive (Clone , Copy , PartialEq , Eq , Hash)] pub enum Provenance { # [doc = " For pointers with concrete provenance. we exactly know which allocation they are attached to"] # [doc = " and what their borrow tag is."] Concrete { alloc_id : AllocId , # [doc = " Borrow Tracker tag."] tag : BorTag , } , # [doc = " Pointers with wildcard provenance are created on int-to-ptr casts. According to the"] # [doc = " specification, we should at that point angelically \"guess\" a provenance that will make all"] # [doc = " future uses of this pointer work, if at all possible. Of course such a semantics cannot be"] # [doc = " actually implemented in Miri. So instead, we approximate this, erroring on the side of"] # [doc = " accepting too much code rather than rejecting correct code: a pointer with wildcard"] # [doc = " provenance \"acts like\" any previously exposed pointer. Each time it is used, we check"] # [doc = " whether *some* exposed pointer could have done what we want to do, and if the answer is yes"] # [doc = " then we allow the access. This allows too much code in two ways:"] # [doc = " - The same wildcard pointer can \"take the role\" of multiple different exposed pointers on"] # [doc = "   subsequent memory accesses."] # [doc = " - In the aliasing model, we don't just have to know the borrow tag of the pointer used for"] # [doc = "   the access, we also have to update the aliasing state -- and that update can be very"] # [doc = "   different depending on which borrow tag we pick! Stacked Borrows has support for this by"] # [doc = "   switching to a stack that is only approximately known, i.e. we over-approximate the effect"] # [doc = "   of using *any* exposed pointer for this access, and only keep information about the borrow"] # [doc = "   stack that would be true with all possible choices."] Wildcard , }
};
}
