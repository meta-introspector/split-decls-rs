// Generated macro for EvalContextExt (trait)
macro_rules! Depcrate_shims_x86_sse41EvalContextExt {
() => {
// Module: crate::shims::x86::sse41
// Provides: {"EvalContextExt"}
// Dependencies: {}
pub (super) trait EvalContextExt < 'tcx > : crate :: MiriInterpCxExt < 'tcx > { fn emulate_x86_sse41_intrinsic (& mut self , link_name : Symbol , abi : & FnAbi < 'tcx , Ty < 'tcx > > , args : & [OpTy < 'tcx >] , dest : & MPlaceTy < 'tcx > ,) -> InterpResult < 'tcx , EmulateItemResult > { let this = self . eval_context_mut () ; this . expect_target_feature_for_intrinsic (link_name , "sse4.1") ? ; let unprefixed_name = link_name . as_str () . strip_prefix ("llvm.x86.sse41.") . unwrap () ; match unprefixed_name { "insertps" => { let [left , right , imm] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; let (left , left_len) = this . project_to_simd (left) ? ; let (right , right_len) = this . project_to_simd (right) ? ; let (dest , dest_len) = this . project_to_simd (dest) ? ; assert_eq ! (dest_len , left_len) ; assert_eq ! (dest_len , right_len) ; assert ! (dest_len <= 4) ; let imm = this . read_scalar (imm) ? . to_u8 () ? ; let src_index = u64 :: from ((imm >> 6) & 0b11) ; let dst_index = u64 :: from ((imm >> 4) & 0b11) ; let src_value = this . read_immediate (& this . project_index (& right , src_index) ?) ? ; for i in 0 .. dest_len { let dest = this . project_index (& dest , i) ? ; if imm & (1 << i) != 0 { this . write_scalar (Scalar :: from_u32 (0) , & dest) ? ; } else if i == dst_index { this . write_immediate (* src_value , & dest) ? ; } else { this . copy_op (& this . project_index (& left , i) ? , & dest) ? ; } } } "packusdw" => { let [left , right] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; packusdw (this , left , right , dest) ? ; } "dpps" | "dppd" => { let [left , right , imm] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; conditional_dot_product (this , left , right , imm , dest) ? ; } "round.ss" => { let [left , right , rounding] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; round_first :: < rustc_apfloat :: ieee :: Single > (this , left , right , rounding , dest) ? ; } "round.ps" => { let [op , rounding] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; round_all :: < rustc_apfloat :: ieee :: Single > (this , op , rounding , dest) ? ; } "round.sd" => { let [left , right , rounding] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; round_first :: < rustc_apfloat :: ieee :: Double > (this , left , right , rounding , dest) ? ; } "round.pd" => { let [op , rounding] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; round_all :: < rustc_apfloat :: ieee :: Double > (this , op , rounding , dest) ? ; } "phminposuw" => { let [op] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; let (op , op_len) = this . project_to_simd (op) ? ; let (dest , dest_len) = this . project_to_simd (dest) ? ; let mut min_value = u16 :: MAX ; let mut min_index = 0 ; for i in 0 .. op_len { let op = this . read_scalar (& this . project_index (& op , i) ?) ? . to_u16 () ? ; if op < min_value { min_value = op ; min_index = i ; } } this . write_scalar (Scalar :: from_u16 (min_value) , & this . project_index (& dest , 0) ?) ? ; this . write_scalar (Scalar :: from_u16 (min_index . try_into () . unwrap ()) , & this . project_index (& dest , 1) ? ,) ? ; for i in 2 .. dest_len { this . write_scalar (Scalar :: from_u16 (0) , & this . project_index (& dest , i) ?) ? ; } } "mpsadbw" => { let [left , right , imm] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; mpsadbw (this , left , right , imm , dest) ? ; } "ptestz" | "ptestc" | "ptestnzc" => { let [op , mask] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; let (all_zero , masked_set) = test_bits_masked (this , op , mask) ? ; let res = match unprefixed_name { "ptestz" => all_zero , "ptestc" => masked_set , "ptestnzc" => ! all_zero && ! masked_set , _ => unreachable ! () , } ; this . write_scalar (Scalar :: from_i32 (res . into ()) , dest) ? ; } _ => return interp_ok (EmulateItemResult :: NotSupported) , } interp_ok (EmulateItemResult :: NeedsReturn) } }
};
}
