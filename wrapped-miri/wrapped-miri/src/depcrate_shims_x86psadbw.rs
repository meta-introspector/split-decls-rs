// Generated macro for psadbw (function)
macro_rules! Depcrate_shims_x86psadbw {
() => {
// Module: crate::shims::x86
// Provides: {"psadbw"}
// Dependencies: {}
# [doc = " Compute the absolute differences of packed unsigned 8-bit integers"] # [doc = " in `left` and `right`, then horizontally sum each consecutive 8"] # [doc = " differences to produce unsigned 16-bit integers, and pack"] # [doc = " these unsigned 16-bit integers in the low 16 bits of 64-bit elements"] # [doc = " in `dest`."] # [doc = ""] # [doc = " <https://www.intel.com/content/www/us/en/docs/intrinsics-guide/index.html#text=_mm_sad_epu8>"] # [doc = " <https://www.intel.com/content/www/us/en/docs/intrinsics-guide/index.html#text=_mm256_sad_epu8>"] # [doc = " <https://www.intel.com/content/www/us/en/docs/intrinsics-guide/index.html#text=_mm512_sad_epu8>"] fn psadbw < 'tcx > (ecx : & mut crate :: MiriInterpCx < 'tcx > , left : & OpTy < 'tcx > , right : & OpTy < 'tcx > , dest : & MPlaceTy < 'tcx > ,) -> InterpResult < 'tcx , () > { let (left , left_len) = ecx . project_to_simd (left) ? ; let (right , right_len) = ecx . project_to_simd (right) ? ; let (dest , dest_len) = ecx . project_to_simd (dest) ? ; assert_eq ! (left_len , right_len) ; assert_eq ! (left_len , left . layout . layout . size () . bytes ()) ; assert_eq ! (dest_len , left_len . strict_div (8)) ; for i in 0 .. dest_len { let dest = ecx . project_index (& dest , i) ? ; let mut acc : u16 = 0 ; for j in 0 .. 8 { let src_index = i . strict_mul (8) . strict_add (j) ; let left = ecx . project_index (& left , src_index) ? ; let left = ecx . read_scalar (& left) ? . to_u8 () ? ; let right = ecx . project_index (& right , src_index) ? ; let right = ecx . read_scalar (& right) ? . to_u8 () ? ; acc = acc . strict_add (left . abs_diff (right) . into ()) ; } ecx . write_scalar (Scalar :: from_u64 (acc . into ()) , & dest) ? ; } interp_ok (()) }
};
}
