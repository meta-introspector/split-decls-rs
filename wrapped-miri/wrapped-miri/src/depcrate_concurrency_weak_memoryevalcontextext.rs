// Generated macro for EvalContextExt (trait)
macro_rules! Depcrate_concurrency_weak_memoryEvalContextExt {
() => {
// Module: crate::concurrency::weak_memory
// Provides: {"EvalContextExt"}
// Dependencies: {}
pub (super) trait EvalContextExt < 'tcx > : crate :: MiriInterpCxExt < 'tcx > { fn buffered_atomic_rmw (& mut self , new_val : Scalar , place : & MPlaceTy < 'tcx > , atomic : AtomicRwOrd , init : Scalar ,) -> InterpResult < 'tcx > { let this = self . eval_context_mut () ; let (alloc_id , base_offset , ..) = this . ptr_get_alloc_id (place . ptr () , 0) ? ; if let (crate :: AllocExtra { data_race : AllocDataRaceHandler :: Vclocks (_ , Some (alloc_buffers)) , .. } , crate :: MiriMachine { data_race : GlobalDataRaceHandler :: Vclocks (global) , threads , .. } ,) = this . get_alloc_extra_mut (alloc_id) ? { if atomic == AtomicRwOrd :: SeqCst { global . sc_read (threads) ; global . sc_write (threads) ; } let range = alloc_range (base_offset , place . layout . size) ; let buffer = alloc_buffers . get_or_create_store_buffer_mut (range , Some (init)) ? ; buffer . read_from_last_store (global , threads , atomic == AtomicRwOrd :: SeqCst) ; buffer . buffered_write (new_val , global , threads , atomic == AtomicRwOrd :: SeqCst) ? ; } interp_ok (()) } fn buffered_atomic_read (& self , place : & MPlaceTy < 'tcx > , atomic : AtomicReadOrd , latest_in_mo : Scalar , validate : impl FnOnce () -> InterpResult < 'tcx > ,) -> InterpResult < 'tcx , Option < Scalar > > { let this = self . eval_context_ref () ; 'fallback : { if let Some (global) = this . machine . data_race . as_vclocks_ref () { let (alloc_id , base_offset , ..) = this . ptr_get_alloc_id (place . ptr () , 0) ? ; if let Some (alloc_buffers) = this . get_alloc_extra (alloc_id) ? . data_race . as_weak_memory_ref () { if atomic == AtomicReadOrd :: SeqCst { global . sc_read (& this . machine . threads) ; } let mut rng = this . machine . rng . borrow_mut () ; let Some (buffer) = alloc_buffers . get_store_buffer (alloc_range (base_offset , place . layout . size)) ? else { break 'fallback ; } ; let (loaded , recency) = buffer . buffered_read (global , & this . machine . threads , atomic == AtomicReadOrd :: SeqCst , & mut * rng , validate ,) ? ; if global . track_outdated_loads && recency == LoadRecency :: Outdated { this . emit_diagnostic (NonHaltingDiagnostic :: WeakMemoryOutdatedLoad { ptr : place . ptr () , }) ; } return interp_ok (loaded) ; } } } validate () ? ; interp_ok (Some (latest_in_mo)) } # [doc = " Add the given write to the store buffer. (Does not change machine memory.)"] # [doc = ""] # [doc = " `init` says with which value to initialize the store buffer in case there wasn't a store"] # [doc = " buffer for this memory range before."] fn buffered_atomic_write (& mut self , val : Scalar , dest : & MPlaceTy < 'tcx > , atomic : AtomicWriteOrd , init : Option < Scalar > ,) -> InterpResult < 'tcx > { let this = self . eval_context_mut () ; let (alloc_id , base_offset , ..) = this . ptr_get_alloc_id (dest . ptr () , 0) ? ; if let (crate :: AllocExtra { data_race : AllocDataRaceHandler :: Vclocks (_ , Some (alloc_buffers)) , .. } , crate :: MiriMachine { data_race : GlobalDataRaceHandler :: Vclocks (global) , threads , .. } ,) = this . get_alloc_extra_mut (alloc_id) ? { if atomic == AtomicWriteOrd :: SeqCst { global . sc_write (threads) ; } let buffer = alloc_buffers . get_or_create_store_buffer_mut (alloc_range (base_offset , dest . layout . size) , init) ? ; buffer . buffered_write (val , global , threads , atomic == AtomicWriteOrd :: SeqCst) ? ; } interp_ok (()) } # [doc = " Caller should never need to consult the store buffer for the latest value."] # [doc = " This function is used exclusively for failed atomic_compare_exchange_scalar"] # [doc = " to perform load_impl on the latest store element"] fn perform_read_on_buffered_latest (& self , place : & MPlaceTy < 'tcx > , atomic : AtomicReadOrd ,) -> InterpResult < 'tcx > { let this = self . eval_context_ref () ; if let Some (global) = this . machine . data_race . as_vclocks_ref () { if atomic == AtomicReadOrd :: SeqCst { global . sc_read (& this . machine . threads) ; } let size = place . layout . size ; let (alloc_id , base_offset , ..) = this . ptr_get_alloc_id (place . ptr () , 0) ? ; if let Some (alloc_buffers) = this . get_alloc_extra (alloc_id) ? . data_race . as_weak_memory_ref () { let Some (buffer) = alloc_buffers . get_store_buffer (alloc_range (base_offset , size)) ? else { return interp_ok (()) ; } ; buffer . read_from_last_store (global , & this . machine . threads , atomic == AtomicReadOrd :: SeqCst ,) ; } } interp_ok (()) } }
};
}
