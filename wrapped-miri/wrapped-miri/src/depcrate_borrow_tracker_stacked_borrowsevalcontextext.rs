// Generated macro for EvalContextExt (trait)
macro_rules! Depcrate_borrow_tracker_stacked_borrowsEvalContextExt {
() => {
// Module: crate::borrow_tracker::stacked_borrows
// Provides: {"EvalContextExt"}
// Dependencies: {}
pub trait EvalContextExt < 'tcx > : crate :: MiriInterpCxExt < 'tcx > { fn sb_retag_ptr_value (& mut self , kind : RetagKind , val : & ImmTy < 'tcx > ,) -> InterpResult < 'tcx , ImmTy < 'tcx > > { let this = self . eval_context_mut () ; let new_perm = NewPermission :: from_ref_ty (val . layout . ty , kind , this) ; let cause = match kind { RetagKind :: TwoPhase => RetagCause :: TwoPhase , RetagKind :: FnEntry => unreachable ! () , RetagKind :: Raw | RetagKind :: Default => RetagCause :: Normal , } ; this . sb_retag_reference (val , new_perm , RetagInfo { cause , in_field : false }) } fn sb_retag_place_contents (& mut self , kind : RetagKind , place : & PlaceTy < 'tcx > ,) -> InterpResult < 'tcx > { let this = self . eval_context_mut () ; let retag_fields = this . machine . borrow_tracker . as_mut () . unwrap () . get_mut () . retag_fields ; let retag_cause = match kind { RetagKind :: TwoPhase => unreachable ! () , RetagKind :: FnEntry => RetagCause :: FnEntry , RetagKind :: Default | RetagKind :: Raw => RetagCause :: Normal , } ; let mut visitor = RetagVisitor { ecx : this , kind , retag_cause , retag_fields , in_field : false } ; return visitor . visit_value (place) ; struct RetagVisitor < 'ecx , 'tcx > { ecx : & 'ecx mut MiriInterpCx < 'tcx > , kind : RetagKind , retag_cause : RetagCause , retag_fields : RetagFields , in_field : bool , } impl < 'ecx , 'tcx > RetagVisitor < 'ecx , 'tcx > { # [inline (always)] fn retag_ptr_inplace (& mut self , place : & PlaceTy < 'tcx > , new_perm : NewPermission ,) -> InterpResult < 'tcx > { let val = self . ecx . read_immediate (& self . ecx . place_to_op (place) ?) ? ; let val = self . ecx . sb_retag_reference (& val , new_perm , RetagInfo { cause : self . retag_cause , in_field : self . in_field } ,) ? ; self . ecx . write_immediate (* val , place) ? ; interp_ok (()) } } impl < 'ecx , 'tcx > ValueVisitor < 'tcx , MiriMachine < 'tcx > > for RetagVisitor < 'ecx , 'tcx > { type V = PlaceTy < 'tcx > ; # [inline (always)] fn ecx (& self) -> & MiriInterpCx < 'tcx > { self . ecx } fn visit_box (& mut self , box_ty : Ty < 'tcx > , place : & PlaceTy < 'tcx >) -> InterpResult < 'tcx > { if box_ty . is_box_global (* self . ecx . tcx) { let new_perm = NewPermission :: from_box_ty (place . layout . ty , self . kind , self . ecx) ; self . retag_ptr_inplace (place , new_perm) ? ; } interp_ok (()) } fn visit_value (& mut self , place : & PlaceTy < 'tcx >) -> InterpResult < 'tcx > { if place . layout . is_sized () && place . layout . size < self . ecx . pointer_size () { return interp_ok (()) ; } match place . layout . ty . kind () { ty :: Ref (..) | ty :: RawPtr (..) => { if matches ! (place . layout . ty . kind () , ty :: Ref (..)) || self . kind == RetagKind :: Raw { let new_perm = NewPermission :: from_ref_ty (place . layout . ty , self . kind , self . ecx) ; self . retag_ptr_inplace (place , new_perm) ? ; } } ty :: Adt (adt , _) if adt . is_box () => { self . walk_value (place) ? ; } _ => { let recurse = match self . retag_fields { RetagFields :: No => false , RetagFields :: Yes => true , RetagFields :: OnlyScalar => { matches ! (place . layout . backend_repr , BackendRepr :: Scalar (..) | BackendRepr :: ScalarPair (..)) } } ; if recurse { let in_field = mem :: replace (& mut self . in_field , true) ; self . walk_value (place) ? ; self . in_field = in_field ; } } } interp_ok (()) } } } # [doc = " Protect a place so that it cannot be used any more for the duration of the current function"] # [doc = " call."] # [doc = ""] # [doc = " This is used to ensure soundness of in-place function argument/return passing."] fn sb_protect_place (& mut self , place : & MPlaceTy < 'tcx >) -> InterpResult < 'tcx , MPlaceTy < 'tcx > > { let this = self . eval_context_mut () ; let new_perm = NewPermission :: Uniform { perm : Permission :: Unique , access : Some (AccessKind :: Write) , protector : Some (ProtectorKind :: StrongProtector) , } ; this . sb_retag_place (place , new_perm , RetagInfo { cause : RetagCause :: InPlaceFnPassing , in_field : false } ,) } # [doc = " Mark the given tag as exposed. It was found on a pointer with the given AllocId."] fn sb_expose_tag (& self , alloc_id : AllocId , tag : BorTag) -> InterpResult < 'tcx > { let this = self . eval_context_ref () ; let kind = this . get_alloc_info (alloc_id) . kind ; match kind { AllocKind :: LiveData => { let alloc_extra = this . get_alloc_extra (alloc_id) ? ; trace ! ("Stacked Borrows tag {tag:?} exposed in {alloc_id:?}") ; alloc_extra . borrow_tracker_sb () . borrow_mut () . exposed_tags . insert (tag) ; } AllocKind :: Function | AllocKind :: VTable | AllocKind :: TypeId | AllocKind :: Dead => { } } interp_ok (()) } fn print_stacks (& mut self , alloc_id : AllocId) -> InterpResult < 'tcx > { let this = self . eval_context_mut () ; let alloc_extra = this . get_alloc_extra (alloc_id) ? ; let stacks = alloc_extra . borrow_tracker_sb () . borrow () ; for (range , stack) in stacks . stacks . iter_all () { print ! ("{range:?}: [") ; if let Some (bottom) = stack . unknown_bottom () { print ! (" unknown-bottom(..{bottom:?})") ; } for i in 0 .. stack . len () { let item = stack . get (i) . unwrap () ; print ! (" {:?}{:?}" , item . perm () , item . tag ()) ; } println ! (" ]") ; } interp_ok (()) } }
};
}
