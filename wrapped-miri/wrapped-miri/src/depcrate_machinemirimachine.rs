// Generated macro for MiriMachine (struct)
macro_rules! Depcrate_machineMiriMachine {
() => {
// Module: crate::machine
// Provides: {"MiriMachine"}
// Dependencies: {}
# [doc = " The machine itself."] # [doc = ""] # [doc = " If you add anything here that stores machine values, remember to update"] # [doc = " `visit_all_machine_values`!"] pub struct MiriMachine < 'tcx > { pub tcx : TyCtxt < 'tcx > , # [doc = " Global data for borrow tracking."] pub borrow_tracker : Option < borrow_tracker :: GlobalState > , # [doc = " Depending on settings, this will be `None`,"] # [doc = " global data for a data race detector,"] # [doc = " or the context required for running in GenMC mode."] # [doc = ""] # [doc = " Invariant: The enum variant must match the enum variant of `AllocDataRaceHandler` in the `data_race` field of all `AllocExtra`."] pub data_race : GlobalDataRaceHandler , # [doc = " Ptr-int-cast module global data."] pub alloc_addresses : alloc_addresses :: GlobalState , # [doc = " Environment variables."] pub (crate) env_vars : EnvVars < 'tcx > , # [doc = " Return place of the main function."] pub (crate) main_fn_ret_place : Option < MPlaceTy < 'tcx > > , # [doc = " Program arguments (`Option` because we can only initialize them after creating the ecx)."] # [doc = " These are *pointers* to argc/argv because macOS."] # [doc = " We also need the full command line as one string because of Windows."] pub (crate) argc : Option < Pointer > , pub (crate) argv : Option < Pointer > , pub (crate) cmd_line : Option < Pointer > , # [doc = " TLS state."] pub (crate) tls : TlsData < 'tcx > , # [doc = " What should Miri do when an op requires communicating with the host,"] # [doc = " such as accessing host env vars, random number generation, and"] # [doc = " file system access."] pub (crate) isolated_op : IsolatedOp , # [doc = " Whether to enforce the validity invariant."] pub (crate) validation : ValidationMode , # [doc = " The table of file descriptors."] pub (crate) fds : shims :: FdTable , # [doc = " The table of directory descriptors."] pub (crate) dirs : shims :: DirTable , # [doc = " The list of all EpollEventInterest."] pub (crate) epoll_interests : shims :: EpollInterestTable , # [doc = " This machine's monotone clock."] pub (crate) monotonic_clock : MonotonicClock , # [doc = " The set of threads."] pub (crate) threads : ThreadManager < 'tcx > , # [doc = " Stores which thread is eligible to run on which CPUs."] # [doc = " This has no effect at all, it is just tracked to produce the correct result"] # [doc = " in `sched_getaffinity`"] pub (crate) thread_cpu_affinity : FxHashMap < ThreadId , CpuAffinityMask > , # [doc = " Precomputed `TyLayout`s for primitive data types that are commonly used inside Miri."] pub (crate) layouts : PrimitiveLayouts < 'tcx > , # [doc = " Allocations that are considered roots of static memory (that may leak)."] pub (crate) static_roots : Vec < AllocId > , # [doc = " The `measureme` profiler used to record timing information about"] # [doc = " the emulated program."] profiler : Option < measureme :: Profiler > , # [doc = " Used with `profiler` to cache the `StringId`s for event names"] # [doc = " used with `measureme`."] string_cache : FxHashMap < String , measureme :: StringId > , # [doc = " Cache of `Instance` exported under the given `Symbol` name."] # [doc = " `None` means no `Instance` exported under the given name is found."] pub (crate) exported_symbols_cache : FxHashMap < Symbol , Option < Instance < 'tcx > > > , # [doc = " Equivalent setting as RUST_BACKTRACE on encountering an error."] pub (crate) backtrace_style : BacktraceStyle , # [doc = " Crates which are considered local for the purposes of error reporting."] pub (crate) local_crates : Vec < CrateNum > , # [doc = " Mapping extern static names to their pointer."] extern_statics : FxHashMap < Symbol , StrictPointer > , # [doc = " The random number generator used for resolving non-determinism."] # [doc = " Needs to be queried by ptr_to_int, hence needs interior mutability."] pub (crate) rng : RefCell < StdRng > , # [doc = " The allocator used for the machine's `AllocBytes` in native-libs mode."] pub (crate) allocator : Option < Rc < RefCell < crate :: alloc :: isolated_alloc :: IsolatedAlloc > > > , # [doc = " The allocation IDs to report when they are being allocated"] # [doc = " (helps for debugging memory leaks and use after free bugs)."] tracked_alloc_ids : FxHashSet < AllocId > , # [doc = " For the tracked alloc ids, also report read/write accesses."] track_alloc_accesses : bool , # [doc = " Controls whether alignment of memory accesses is being checked."] pub (crate) check_alignment : AlignmentCheck , # [doc = " Failure rate of compare_exchange_weak, between 0.0 and 1.0"] pub (crate) cmpxchg_weak_failure_rate : f64 , # [doc = " The probability of the active thread being preempted at the end of each basic block."] pub (crate) preemption_rate : f64 , # [doc = " If `Some`, we will report the current stack every N basic blocks."] pub (crate) report_progress : Option < u32 > , pub (crate) basic_block_count : u64 , # [doc = " Handle of the optional shared object file for native functions."] # [cfg (all (unix , feature = "native-lib"))] pub native_lib : Vec < (libloading :: Library , std :: path :: PathBuf) > , # [cfg (not (all (unix , feature = "native-lib")))] pub native_lib : Vec < ! > , # [doc = " Run a garbage collector for BorTags every N basic blocks."] pub (crate) gc_interval : u32 , # [doc = " The number of blocks that passed since the last BorTag GC pass."] pub (crate) since_gc : u32 , # [doc = " The number of CPUs to be reported by miri."] pub (crate) num_cpus : u32 , # [doc = " Determines Miri's page size and associated values"] pub (crate) page_size : u64 , pub (crate) stack_addr : u64 , pub (crate) stack_size : u64 , # [doc = " Whether to collect a backtrace when each allocation is created, just in case it leaks."] pub (crate) collect_leak_backtraces : bool , # [doc = " The spans we will use to report where an allocation was created and deallocated in"] # [doc = " diagnostics."] pub (crate) allocation_spans : RefCell < FxHashMap < AllocId , (Span , Option < Span >) > > , # [doc = " For each allocation, an offset inside that allocation that was deemed aligned even for"] # [doc = " symbolic alignment checks. This cannot be stored in `AllocExtra` since it needs to be"] # [doc = " tracked for vtables and function allocations as well as regular allocations."] # [doc = ""] # [doc = " Invariant: the promised alignment will never be less than the native alignment of the"] # [doc = " allocation."] pub (crate) symbolic_alignment : RefCell < FxHashMap < AllocId , (Size , Align) > > , # [doc = " A cache of \"data range\" computations for unions (i.e., the offsets of non-padding bytes)."] union_data_ranges : FxHashMap < Ty < 'tcx > , RangeSet > , # [doc = " Caches the sanity-checks for various pthread primitives."] pub (crate) pthread_mutex_sanity : Cell < bool > , pub (crate) pthread_rwlock_sanity : Cell < bool > , pub (crate) pthread_condvar_sanity : Cell < bool > , # [doc = " Remembers whether we already warned about an extern type with Stacked Borrows."] pub (crate) sb_extern_type_warned : Cell < bool > , # [doc = " Remember whether we already warned about sharing memory with a native call."] # [allow (unused)] pub (crate) native_call_mem_warned : Cell < bool > , # [doc = " Remembers which shims have already shown the warning about erroring in isolation."] pub (crate) reject_in_isolation_warned : RefCell < FxHashSet < String > > , # [doc = " Remembers which int2ptr casts we have already warned about."] pub (crate) int2ptr_warned : RefCell < FxHashSet < Span > > , # [doc = " Cache for `mangle_internal_symbol`."] pub (crate) mangle_internal_symbol_cache : FxHashMap < & 'static str , String > , # [doc = " Always prefer the intrinsic fallback body over the native Miri implementation."] pub force_intrinsic_fallback : bool , # [doc = " Whether floating-point operations can behave non-deterministically."] pub float_nondet : bool , # [doc = " Whether floating-point operations can have a non-deterministic rounding error."] pub float_rounding_error : FloatRoundingErrorMode , # [doc = " Whether Miri artifically introduces short reads/writes on file descriptors."] pub short_fd_operations : bool , }
};
}
