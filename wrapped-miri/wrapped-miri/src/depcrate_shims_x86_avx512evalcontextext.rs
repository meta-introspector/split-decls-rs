// Generated macro for EvalContextExt (trait)
macro_rules! Depcrate_shims_x86_avx512EvalContextExt {
() => {
// Module: crate::shims::x86::avx512
// Provides: {"EvalContextExt"}
// Dependencies: {}
pub (super) trait EvalContextExt < 'tcx > : crate :: MiriInterpCxExt < 'tcx > { fn emulate_x86_avx512_intrinsic (& mut self , link_name : Symbol , abi : & FnAbi < 'tcx , Ty < 'tcx > > , args : & [OpTy < 'tcx >] , dest : & MPlaceTy < 'tcx > ,) -> InterpResult < 'tcx , EmulateItemResult > { let this = self . eval_context_mut () ; let unprefixed_name = link_name . as_str () . strip_prefix ("llvm.x86.avx512.") . unwrap () ; match unprefixed_name { "pternlog.d.128" | "pternlog.d.256" | "pternlog.d.512" => { this . expect_target_feature_for_intrinsic (link_name , "avx512f") ? ; if matches ! (unprefixed_name , "pternlog.d.128" | "pternlog.d.256") { this . expect_target_feature_for_intrinsic (link_name , "avx512vl") ? ; } let [a , b , c , imm8] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; assert_eq ! (dest . layout , a . layout) ; assert_eq ! (dest . layout , b . layout) ; assert_eq ! (dest . layout , c . layout) ; let (a , _a_len) = this . project_to_simd (a) ? ; let (b , _b_len) = this . project_to_simd (b) ? ; let (c , _c_len) = this . project_to_simd (c) ? ; let (dest , dest_len) = this . project_to_simd (dest) ? ; let tern = | xa : u32 , xb : u32 , xc : u32 , imm : u32 | -> u32 { let mut out = 0u32 ; for bit in 0 .. 32 { let ia = (xa >> bit) & 1 ; let ib = (xb >> bit) & 1 ; let ic = (xc >> bit) & 1 ; let idx = (ia << 2) | (ib << 1) | ic ; let v = (imm >> idx) & 1 ; out |= v << bit ; } out } ; let imm8 = this . read_scalar (imm8) ? . to_u32 () ? & 0xFF ; for i in 0 .. dest_len { let a_lane = this . project_index (& a , i) ? ; let b_lane = this . project_index (& b , i) ? ; let c_lane = this . project_index (& c , i) ? ; let d_lane = this . project_index (& dest , i) ? ; let va = this . read_scalar (& a_lane) ? . to_u32 () ? ; let vb = this . read_scalar (& b_lane) ? . to_u32 () ? ; let vc = this . read_scalar (& c_lane) ? . to_u32 () ? ; let r = tern (va , vb , vc , imm8) ; this . write_scalar (Scalar :: from_u32 (r) , & d_lane) ? ; } } "psad.bw.512" => { this . expect_target_feature_for_intrinsic (link_name , "avx512bw") ? ; let [left , right] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; psadbw (this , left , right , dest) ? } "pmaddubs.w.512" => { let [left , right] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; pmaddbw (this , left , right , dest) ? ; } "permvar.si.512" => { let [left , right] = this . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; permute (this , left , right , dest) ? ; } _ => return interp_ok (EmulateItemResult :: NotSupported) , } interp_ok (EmulateItemResult :: NeedsReturn) } }
};
}
