// Generated macro for pclmulqdq (function)
macro_rules! Depcrate_shims_x86pclmulqdq {
() => {
// Module: crate::shims::x86
// Provides: {"pclmulqdq"}
// Dependencies: {}
# [doc = " Perform a carry-less multiplication of two 64-bit integers, selected from `left` and `right` according to `imm8`,"] # [doc = " and store the results in `dst`."] # [doc = ""] # [doc = " `left` and `right` are both vectors of type `len` x i64. Only bits 0 and 4 of `imm8` matter;"] # [doc = " they select the element of `left` and `right`, respectively."] # [doc = ""] # [doc = " `len` is the SIMD vector length (in counts of `i64` values). It is expected to be one of"] # [doc = " `2`, `4`, or `8`."] # [doc = ""] # [doc = " <https://www.intel.com/content/www/us/en/docs/intrinsics-guide/index.html#text=_mm_clmulepi64_si128>"] fn pclmulqdq < 'tcx > (ecx : & mut MiriInterpCx < 'tcx > , left : & OpTy < 'tcx > , right : & OpTy < 'tcx > , imm8 : & OpTy < 'tcx > , dest : & MPlaceTy < 'tcx > , len : u64 ,) -> InterpResult < 'tcx , () > { assert_eq ! (left . layout , right . layout) ; assert_eq ! (left . layout . size , dest . layout . size) ; assert ! ([2u64 , 4 , 8] . contains (& len)) ; let src_layout = ecx . layout_of (Ty :: new_array (ecx . tcx . tcx , ecx . tcx . types . u64 , len)) ? ; let dest_layout = ecx . layout_of (Ty :: new_array (ecx . tcx . tcx , ecx . tcx . types . u128 , len / 2)) ? ; let left = left . transmute (src_layout , ecx) ? ; let right = right . transmute (src_layout , ecx) ? ; let dest = dest . transmute (dest_layout , ecx) ? ; let imm8 = ecx . read_scalar (imm8) ? . to_u8 () ? ; for i in 0 .. (len / 2) { let lo = i . strict_mul (2) ; let hi = i . strict_mul (2) . strict_add (1) ; let index = if (imm8 & 0x01) == 0 { lo } else { hi } ; let left = ecx . read_scalar (& ecx . project_index (& left , index) ?) ? . to_u64 () ? ; let index = if (imm8 & 0x10) == 0 { lo } else { hi } ; let right = ecx . read_scalar (& ecx . project_index (& right , index) ?) ? . to_u64 () ? ; let mut result : u128 = 0 ; for i in 0 .. 64 { if (right & (1 << i)) != 0 { result ^= u128 :: from (left) << i ; } } let dest = ecx . project_index (& dest , i) ? ; ecx . write_scalar (Scalar :: from_u128 (result) , & dest) ? ; } interp_ok (()) }
};
}
