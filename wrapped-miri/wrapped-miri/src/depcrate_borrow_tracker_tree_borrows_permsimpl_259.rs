// Generated macro for impl_259 (impl)
macro_rules! Depcrate_borrow_tracker_tree_borrows_permsimpl_259 {
() => {
// Module: crate::borrow_tracker::tree_borrows::perms
// Provides: {"impl_259"}
// Dependencies: {}
impl Permission { # [doc = " Check if `self` can be the initial state of a pointer."] pub fn is_initial (& self) -> bool { self . inner . is_initial () } # [doc = " Check if `self` is the terminal state of a pointer (is `Disabled`)."] pub fn is_disabled (& self) -> bool { self . inner == Disabled } # [doc = " Check if `self` is the never-allow-writes-again state of a pointer (is `Frozen`)."] pub fn is_frozen (& self) -> bool { self . inner == Frozen } # [doc = " Check if `self` is the shared-reference-to-interior-mutable-data state of a pointer."] pub fn is_cell (& self) -> bool { self . inner == Cell } # [doc = " Default initial permission of the root of a new tree at inbounds positions."] # [doc = " Must *only* be used for the root, this is not in general an \"initial\" permission!"] pub fn new_unique () -> Self { Self { inner : Unique } } # [doc = " Default initial permission of a reborrowed mutable reference that is either"] # [doc = " protected or not interior mutable."] pub fn new_reserved_frz () -> Self { Self { inner : ReservedFrz { conflicted : false } } } # [doc = " Default initial permission of an unprotected interior mutable reference."] pub fn new_reserved_im () -> Self { Self { inner : ReservedIM } } # [doc = " Default initial permission of a reborrowed shared reference."] pub fn new_frozen () -> Self { Self { inner : Frozen } } # [doc = " Default initial permission of  the root of a new tree at out-of-bounds positions."] # [doc = " Must *only* be used for the root, this is not in general an \"initial\" permission!"] pub fn new_disabled () -> Self { Self { inner : Disabled } } # [doc = " Default initial permission of a shared reference to interior mutable data."] pub fn new_cell () -> Self { Self { inner : Cell } } # [doc = " Reject `ReservedIM` that cannot exist in the presence of a protector."] # [cfg (test)] pub fn compatible_with_protector (& self) -> bool { self . inner . compatible_with_protector () } # [doc = " What kind of access to perform before releasing the protector."] pub fn protector_end_access (& self) -> Option < AccessKind > { match self . inner { Cell => None , Unique => Some (AccessKind :: Write) , _ => Some (AccessKind :: Read) , } } # [doc = " Apply the transition to the inner PermissionPriv."] pub fn perform_access (kind : AccessKind , rel_pos : AccessRelatedness , old_perm : Self , protected : bool ,) -> Option < PermTransition > { let old_state = old_perm . inner ; transition :: perform_access (kind , rel_pos , old_state , protected) . map (| new_state | PermTransition { from : old_state , to : new_state }) } # [doc = " During a provenance GC, we want to compact the tree."] # [doc = " For this, we want to merge nodes upwards if they have a singleton parent."] # [doc = " But we need to be careful: If the parent is Frozen, and the child is Reserved,"] # [doc = " we can not do such a merge. In general, such a merge is possible if the parent"] # [doc = " allows similar accesses, and in particular if the parent never causes UB on its"] # [doc = " own. This is enforced by a test, namely `tree_compacting_is_sound`. See that"] # [doc = " test for more information."] # [doc = " This method is only sound if the parent is not protected. We never attempt to"] # [doc = " remove protected parents."] pub fn can_be_replaced_by_child (self , child : Self) -> bool { match (self . inner , child . inner) { (Cell , _) => true , (_ , Cell) => false , (ReservedIM , _) => true , (_ , ReservedIM) => false , (ReservedFrz { .. } , _) => true , (_ , ReservedFrz { .. }) => false , (Unique , Unique | Frozen | Disabled) => true , (_ , Unique) => false , (Frozen , Frozen | Disabled) => true , (_ , Frozen) => false , (Disabled , Disabled) => true , } } # [doc = " Returns the strongest foreign action this node survives (without change),"] # [doc = " where `prot` indicates if it is protected."] # [doc = " See `foreign_access_skipping`"] pub fn strongest_idempotent_foreign_access (& self , prot : bool) -> IdempotentForeignAccess { self . inner . strongest_idempotent_foreign_access (prot) } # [doc = " Returns the strongest access allowed from a child to this node without"] # [doc = " causing UB (only considers possible transitions to this permission)."] pub fn strongest_allowed_child_access (& self , protected : bool) -> WildcardAccessLevel { match self . inner { Disabled => WildcardAccessLevel :: None , Frozen => WildcardAccessLevel :: Read , ReservedFrz { conflicted : true } if protected => WildcardAccessLevel :: Read , _ => WildcardAccessLevel :: Write , } } }
};
}
