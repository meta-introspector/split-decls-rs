// Generated macro for GlobalState (struct)
macro_rules! Depcrate_concurrency_data_raceGlobalState {
() => {
// Module: crate::concurrency::data_race
// Provides: {"GlobalState"}
// Dependencies: {}
# [doc = " Global data-race detection state, contains the currently"] # [doc = " executing thread as well as the vector clocks associated"] # [doc = " with each of the threads."] # [derive (Debug , Clone)] pub struct GlobalState { # [doc = " Set to true once the first additional"] # [doc = " thread has launched, due to the dependency"] # [doc = " between before and after a thread launch."] # [doc = " Any data-races must be recorded after this"] # [doc = " so concurrent execution can ignore recording"] # [doc = " any data-races."] multi_threaded : Cell < bool > , # [doc = " A flag to mark we are currently performing"] # [doc = " a data race free action (such as atomic access)"] # [doc = " to suppress the race detector"] ongoing_action_data_race_free : Cell < bool > , # [doc = " Mapping of a vector index to a known set of thread"] # [doc = " clocks, this is not directly mapping from a thread id"] # [doc = " since it may refer to multiple threads."] vector_clocks : RefCell < IndexVec < VectorIdx , ThreadClockSet > > , # [doc = " Mapping of a given vector index to the current thread"] # [doc = " that the execution is representing, this may change"] # [doc = " if a vector index is re-assigned to a new thread."] vector_info : RefCell < IndexVec < VectorIdx , ThreadId > > , # [doc = " The mapping of a given thread to associated thread metadata."] thread_info : RefCell < IndexVec < ThreadId , ThreadExtraState > > , # [doc = " Potential vector indices that could be re-used on thread creation"] # [doc = " values are inserted here on after the thread has terminated and"] # [doc = " been joined with, and hence may potentially become free"] # [doc = " for use as the index for a new thread."] # [doc = " Elements in this set may still require the vector index to"] # [doc = " report data-races, and can only be re-used after all"] # [doc = " active vector clocks catch up with the threads timestamp."] reuse_candidates : RefCell < FxHashSet < VectorIdx > > , # [doc = " We make SC fences act like RMWs on a global location."] # [doc = " To implement that, they all release and acquire into this clock."] last_sc_fence : RefCell < VClock > , # [doc = " The timestamp of last SC write performed by each thread."] # [doc = " Threads only update their own index here!"] last_sc_write_per_thread : RefCell < VClock > , # [doc = " Track when an outdated (weak memory) load happens."] pub track_outdated_loads : bool , # [doc = " Whether weak memory emulation is enabled"] pub weak_memory : bool , }
};
}
