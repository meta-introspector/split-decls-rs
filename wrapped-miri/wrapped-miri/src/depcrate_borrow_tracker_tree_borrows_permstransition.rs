// Generated macro for transition (module)
macro_rules! Depcrate_borrow_tracker_tree_borrows_permstransition {
() => {
// Module: crate::borrow_tracker::tree_borrows::perms
// Provides: {"transition"}
// Dependencies: {}
# [doc = " This module controls how each permission individually reacts to an access."] # [doc = " Although these functions take `protected` as an argument, this is NOT because"] # [doc = " we check protector violations here, but because some permissions behave differently"] # [doc = " when protected."] mod transition { use super :: * ; # [doc = " A child node was read-accessed: UB on Disabled, noop on the rest."] fn child_read (state : PermissionPriv , _protected : bool) -> Option < PermissionPriv > { Some (match state { Disabled => return None , readable @ (Cell | ReservedFrz { .. } | ReservedIM | Active | Frozen) => readable , }) } # [doc = " A non-child node was read-accessed: keep `Reserved` but mark it as `conflicted` if it"] # [doc = " is protected; invalidate `Active`."] fn foreign_read (state : PermissionPriv , protected : bool) -> Option < PermissionPriv > { Some (match state { Cell => Cell , non_writeable @ (Frozen | Disabled) => non_writeable , ReservedFrz { .. } if protected => ReservedFrz { conflicted : true } , res @ (ReservedFrz { .. } | ReservedIM) => { assert ! (! protected) ; res } Active => if protected { Disabled } else { Frozen } , }) } # [doc = " A child node was write-accessed: `Reserved` must become `Active` to obtain"] # [doc = " write permissions, `Frozen` and `Disabled` cannot obtain such permissions and produce UB."] fn child_write (state : PermissionPriv , protected : bool) -> Option < PermissionPriv > { Some (match state { Cell => Cell , ReservedFrz { conflicted : true } if protected => return None , ReservedFrz { .. } | ReservedIM | Active => Active , Frozen | Disabled => return None , }) } # [doc = " A non-child node was write-accessed: this makes everything `Disabled` except for"] # [doc = " non-protected interior mutable `Reserved` which stay the same."] fn foreign_write (state : PermissionPriv , protected : bool) -> Option < PermissionPriv > { Some (match state { Cell => Cell , res @ ReservedIM => { assert ! (! protected) ; res } _ => Disabled , }) } # [doc = " Dispatch handler depending on the kind of access and its position."] pub (super) fn perform_access (kind : AccessKind , rel_pos : AccessRelatedness , child : PermissionPriv , protected : bool ,) -> Option < PermissionPriv > { match (kind , rel_pos . is_foreign ()) { (AccessKind :: Write , true) => foreign_write (child , protected) , (AccessKind :: Read , true) => foreign_read (child , protected) , (AccessKind :: Write , false) => child_write (child , protected) , (AccessKind :: Read , false) => child_read (child , protected) , } } }
};
}
