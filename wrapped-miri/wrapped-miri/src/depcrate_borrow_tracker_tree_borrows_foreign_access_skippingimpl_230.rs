// Generated macro for impl_230 (impl)
macro_rules! Depcrate_borrow_tracker_tree_borrows_foreign_access_skippingimpl_230 {
() => {
// Module: crate::borrow_tracker::tree_borrows::foreign_access_skipping
// Provides: {"impl_230"}
// Dependencies: {}
impl IdempotentForeignAccess { # [doc = " Returns true if a node where the strongest idempotent foreign access is `self`"] # [doc = " can skip the access `happening_next`. Note that if this returns"] # [doc = " `true`, then the entire subtree will be skipped."] pub fn can_skip_foreign_access (self , happening_next : IdempotentForeignAccess) -> bool { debug_assert ! (happening_next . is_foreign ()) ; happening_next <= self } # [doc = " Updates `self` to account for a foreign access."] pub fn record_new (& mut self , just_happened : IdempotentForeignAccess) { if just_happened . is_local () { * self = IdempotentForeignAccess :: None ; } else { * self = just_happened . max (* self) ; } } # [doc = " Returns true if this access is local."] pub fn is_local (self) -> bool { matches ! (self , IdempotentForeignAccess :: None) } # [doc = " Returns true if this access is foreign, i.e. not local."] pub fn is_foreign (self) -> bool { ! self . is_local () } # [doc = " Constructs a foreign access from an `AccessKind`"] pub fn from_foreign (acc : AccessKind) -> IdempotentForeignAccess { match acc { AccessKind :: Read => Self :: Read , AccessKind :: Write => Self :: Write , } } # [doc = " Usually, tree traversals have an `AccessKind` and an `AccessRelatedness`."] # [doc = " This methods converts these into the corresponding `IdempotentForeignAccess`, to be used"] # [doc = " to e.g. invoke `can_skip_foreign_access`."] pub fn from_acc_and_rel (acc : AccessKind , rel : AccessRelatedness) -> IdempotentForeignAccess { if rel . is_foreign () { Self :: from_foreign (acc) } else { Self :: None } } # [doc = " During retags, the SIFA needs to be weakened to account for children with weaker SIFAs being inserted."] # [doc = " Thus, this method is called from the bottom up on each parent, until it returns false, which means the"] # [doc = " \"children have stronger SIFAs\" invariant is restored."] pub fn ensure_no_stronger_than (& mut self , strongest_allowed : IdempotentForeignAccess) -> bool { if * self > strongest_allowed { * self = strongest_allowed ; true } else { false } } }
};
}
