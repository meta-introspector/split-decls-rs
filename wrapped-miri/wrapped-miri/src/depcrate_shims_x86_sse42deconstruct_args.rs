// Generated macro for deconstruct_args (function)
macro_rules! Depcrate_shims_x86_sse42deconstruct_args {
() => {
// Module: crate::shims::x86::sse42
// Provides: {"deconstruct_args"}
// Dependencies: {}
# [doc = " Obtain the arguments of the intrinsic based on its name."] # [doc = " The result is a tuple with the following values:"] # [doc = " * The first string argument."] # [doc = " * The second string argument."] # [doc = " * The string length values, if the intrinsic requires them."] # [doc = " * The immediate instruction byte."] # [doc = ""] # [doc = " The string arguments will be transmuted into arrays of bytes"] # [doc = " or two-byte words, depending on the value of the immediate byte."] # [doc = " Originally, they are [__m128i](https://doc.rust-lang.org/stable/core/arch/x86_64/struct.__m128i.html) values"] # [doc = " corresponding to the x86 128-bit integer SIMD type."] fn deconstruct_args < 'tcx > (unprefixed_name : & str , ecx : & mut MiriInterpCx < 'tcx > , link_name : Symbol , abi : & FnAbi < 'tcx , Ty < 'tcx > > , args : & [OpTy < 'tcx >] ,) -> InterpResult < 'tcx , (OpTy < 'tcx > , OpTy < 'tcx > , Option < (u64 , u64) > , u8) > { let array_layout_fn = | ecx : & mut MiriInterpCx < 'tcx > , imm : u8 | { if imm & USE_WORDS != 0 { ecx . layout_of (Ty :: new_array (ecx . tcx . tcx , ecx . tcx . types . u16 , 8)) } else { ecx . layout_of (Ty :: new_array (ecx . tcx . tcx , ecx . tcx . types . u8 , 16)) } } ; let is_explicit = match unprefixed_name . as_bytes () . get (4) { Some (& b'e') => true , Some (& b'i') => false , _ => unreachable ! () , } ; if is_explicit { let [str1 , len1 , str2 , len2 , imm] = ecx . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; let imm = ecx . read_scalar (imm) ? . to_u8 () ? ; let default_len = default_len :: < u32 > (imm) ; let len1 = u64 :: from (ecx . read_scalar (len1) ? . to_u32 () ? . min (default_len)) ; let len2 = u64 :: from (ecx . read_scalar (len2) ? . to_u32 () ? . min (default_len)) ; let array_layout = array_layout_fn (ecx , imm) ? ; let str1 = str1 . transmute (array_layout , ecx) ? ; let str2 = str2 . transmute (array_layout , ecx) ? ; interp_ok ((str1 , str2 , Some ((len1 , len2)) , imm)) } else { let [str1 , str2 , imm] = ecx . check_shim_sig_lenient (abi , CanonAbi :: C , link_name , args) ? ; let imm = ecx . read_scalar (imm) ? . to_u8 () ? ; let array_layout = array_layout_fn (ecx , imm) ? ; let str1 = str1 . transmute (array_layout , ecx) ? ; let str2 = str2 . transmute (array_layout , ecx) ? ; interp_ok ((str1 , str2 , None , imm)) } }
};
}
