// Generated macro for conditional_dot_product (function)
macro_rules! Depcrate_shims_x86conditional_dot_product {
() => {
// Module: crate::shims::x86
// Provides: {"conditional_dot_product"}
// Dependencies: {}
# [doc = " Conditionally multiplies the packed floating-point elements in"] # [doc = " `left` and `right` using the high 4 bits in `imm`, sums the calculated"] # [doc = " products (up to 4), and conditionally stores the sum in `dest` using"] # [doc = " the low 4 bits of `imm`."] # [doc = ""] # [doc = " Each 128-bit chunk is treated independently (i.e., the value for"] # [doc = " the is i-th 128-bit chunk of `dest` is calculated with the i-th"] # [doc = " 128-bit blocks of `left` and `right`)."] fn conditional_dot_product < 'tcx > (ecx : & mut crate :: MiriInterpCx < 'tcx > , left : & OpTy < 'tcx > , right : & OpTy < 'tcx > , imm : & OpTy < 'tcx > , dest : & MPlaceTy < 'tcx > ,) -> InterpResult < 'tcx , () > { assert_eq ! (left . layout , dest . layout) ; assert_eq ! (right . layout , dest . layout) ; let (num_chunks , items_per_chunk , left) = split_simd_to_128bit_chunks (ecx , left) ? ; let (_ , _ , right) = split_simd_to_128bit_chunks (ecx , right) ? ; let (_ , _ , dest) = split_simd_to_128bit_chunks (ecx , dest) ? ; let element_layout = left . layout . field (ecx , 0) . field (ecx , 0) ; assert ! (items_per_chunk <= 4) ; let imm = ecx . read_scalar (imm) ? . to_uint (imm . layout . size) ? ; for i in 0 .. num_chunks { let left = ecx . project_index (& left , i) ? ; let right = ecx . project_index (& right , i) ? ; let dest = ecx . project_index (& dest , i) ? ; let mut sum = ImmTy :: from_int (0u8 , element_layout) ; for j in 0 .. items_per_chunk { if imm & (1 << j . strict_add (4)) != 0 { let left = ecx . read_immediate (& ecx . project_index (& left , j) ?) ? ; let right = ecx . read_immediate (& ecx . project_index (& right , j) ?) ? ; let mul = ecx . binary_op (mir :: BinOp :: Mul , & left , & right) ? ; sum = ecx . binary_op (mir :: BinOp :: Add , & sum , & mul) ? ; } } for j in 0 .. items_per_chunk { let dest = ecx . project_index (& dest , j) ? ; if imm & (1 << j) != 0 { ecx . write_immediate (* sum , & dest) ? ; } else { ecx . write_scalar (Scalar :: from_int (0u8 , element_layout . size) , & dest) ? ; } } } interp_ok (()) }
};
}
