// Generated macro for futex (function)
macro_rules! Depcrate_shims_unix_linux_like_syncfutex {
() => {
// Module: crate::shims::unix::linux_like::sync
// Provides: {"futex"}
// Dependencies: {}
# [doc = " Implementation of the SYS_futex syscall."] # [doc = " `args` is the arguments *including* the syscall number."] pub fn futex < 'tcx > (ecx : & mut MiriInterpCx < 'tcx > , varargs : & [OpTy < 'tcx >] , dest : & MPlaceTy < 'tcx > ,) -> InterpResult < 'tcx > { let [addr , op , val] = check_min_vararg_count ("`syscall(SYS_futex, ...)`" , varargs) ? ; let addr = ecx . read_pointer (addr) ? ; let op = ecx . read_scalar (op) ? . to_i32 () ? ; let val = ecx . read_scalar (val) ? . to_u32 () ? ; let addr = ecx . ptr_to_mplace (addr , ecx . machine . layouts . i32) ; let futex_private = ecx . eval_libc_i32 ("FUTEX_PRIVATE_FLAG") ; let futex_wait = ecx . eval_libc_i32 ("FUTEX_WAIT") ; let futex_wait_bitset = ecx . eval_libc_i32 ("FUTEX_WAIT_BITSET") ; let futex_wake = ecx . eval_libc_i32 ("FUTEX_WAKE") ; let futex_wake_bitset = ecx . eval_libc_i32 ("FUTEX_WAKE_BITSET") ; let futex_realtime = ecx . eval_libc_i32 ("FUTEX_CLOCK_REALTIME") ; match op & ! futex_private { op if op & ! futex_realtime == futex_wait || op & ! futex_realtime == futex_wait_bitset => { let wait_bitset = op & ! futex_realtime == futex_wait_bitset ; let (timeout , bitset) = if wait_bitset { let [_ , _ , _ , timeout , uaddr2 , bitset] = check_min_vararg_count ("`syscall(SYS_futex, FUTEX_WAIT_BITSET, ...)`" , varargs ,) ? ; let _timeout = ecx . read_pointer (timeout) ? ; let _uaddr2 = ecx . read_pointer (uaddr2) ? ; (timeout , ecx . read_scalar (bitset) ? . to_u32 () ?) } else { let [_ , _ , _ , timeout] = check_min_vararg_count ("`syscall(SYS_futex, FUTEX_WAIT, ...)`" , varargs) ? ; (timeout , u32 :: MAX) } ; if bitset == 0 { return ecx . set_last_error_and_return (LibcError ("EINVAL") , dest) ; } let timeout = ecx . deref_pointer_as (timeout , ecx . libc_ty_layout ("timespec")) ? ; let timeout = if ecx . ptr_is_null (timeout . ptr ()) ? { None } else { let duration = match ecx . read_timespec (& timeout) ? { Some (duration) => duration , None => { return ecx . set_last_error_and_return (LibcError ("EINVAL") , dest) ; } } ; let timeout_clock = if op & futex_realtime == futex_realtime { ecx . check_no_isolation ("`futex` syscall with `op=FUTEX_WAIT` and non-null timeout with `FUTEX_CLOCK_REALTIME`" ,) ? ; TimeoutClock :: RealTime } else { TimeoutClock :: Monotonic } ; let timeout_anchor = if wait_bitset { TimeoutAnchor :: Absolute } else { TimeoutAnchor :: Relative } ; Some ((timeout_clock , timeout_anchor , duration)) } ; ecx . atomic_fence (AtomicFenceOrd :: SeqCst) ? ; let futex_val = ecx . read_scalar_atomic (& addr , AtomicReadOrd :: Acquire) ? . to_u32 () ? ; if val == futex_val { let futex_ref = ecx . get_sync_or_init (addr . ptr () , | _ | LinuxFutex { futex : Default :: default () }) . unwrap () . futex . clone () ; let dest = dest . clone () ; ecx . futex_wait (futex_ref , bitset , timeout , callback ! (@ capture <'tcx > { dest : MPlaceTy <'tcx >, } | ecx , unblock : UnblockKind | match unblock { UnblockKind :: Ready => { ecx . write_int (0 , & dest) } UnblockKind :: TimedOut => { ecx . set_last_error_and_return (LibcError ("ETIMEDOUT") , & dest) } }) ,) ; } else { return ecx . set_last_error_and_return (LibcError ("EAGAIN") , dest) ; } } op if op == futex_wake || op == futex_wake_bitset => { let Some (futex_ref) = ecx . get_sync_or_init (addr . ptr () , | _ | LinuxFutex { futex : Default :: default () }) else { return ecx . set_last_error_and_return (LibcError ("EFAULT") , dest) ; } ; let futex_ref = futex_ref . futex . clone () ; let bitset = if op == futex_wake_bitset { let [_ , _ , _ , timeout , uaddr2 , bitset] = check_min_vararg_count ("`syscall(SYS_futex, FUTEX_WAKE_BITSET, ...)`" , varargs ,) ? ; let _timeout = ecx . read_pointer (timeout) ? ; let _uaddr2 = ecx . read_pointer (uaddr2) ? ; ecx . read_scalar (bitset) ? . to_u32 () ? } else { u32 :: MAX } ; if bitset == 0 { return ecx . set_last_error_and_return (LibcError ("EINVAL") , dest) ; } ecx . atomic_fence (AtomicFenceOrd :: SeqCst) ? ; let woken = ecx . futex_wake (& futex_ref , bitset , val . try_into () . unwrap ()) ? ; ecx . write_scalar (Scalar :: from_target_isize (woken . try_into () . unwrap () , ecx) , dest) ? ; } op => throw_unsup_format ! ("Miri does not support `futex` syscall with op={}" , op) , } interp_ok (()) }
};
}
