// Generated macro for other_23 (other)
macro_rules! Depcrate_generatedother_23 {
() => {
// Module: crate::generated
// Provides: {"other_23"}
// Dependencies: {}
extern "C-unwind" { # [doc = " Hint to MPS how much memory your application expects to need for the command buffer"] # [doc = ""] # [doc = " This will cause MPS to prefetch a MTLHeap into its internal cache of"] # [doc = " the indicated size, which will be sub-allocated to back the temporary images,"] # [doc = " matrices, vectors and states in used over the course of the command buffer."] # [doc = " This can be helpful in certain pathological situations when allocation sizes"] # [doc = " needed to support temporary objects do not allow for reuse of previous allocations"] # [doc = " for new objects."] # [doc = ""] # [doc = " Example: if the temporary resources need progressively larger MTLHeaps"] # [doc = " over the course of the MTLCommandBuffer, such as 1 MB, 2MB, 4 MB and 8MB,"] # [doc = " the first allocation might create a 1 MB heap, this might be released,"] # [doc = " but since the second allocation needs a 2 MB heap and the 1 MB heap is too"] # [doc = " small to be used, a new heap would need to be made, and so forth.  Using"] # [doc = " MPSHintTemporaryMemoryHighWaterMark(), a single 8 MB heap might be made manifest,"] # [doc = " and all four allocations can use it if they don't overlap temporally. Otherwise,"] # [doc = " a total of 1+2+4+8=15 MB might be allocated."] # [doc = ""] # [doc = " The application should be careful not to pass the sum of all allocations over"] # [doc = " the course of the command buffer. As we expect that not all temporary resources"] # [doc = " need to coexist at the same time, and so can alias one another, that would waste"] # [doc = " memory. The application should instead track the high water mark of the most"] # [doc = " memory in use at any single point over the course of the command buffer."] # [doc = ""] # [doc = " This can be simply done by traversing your graph creating all the temporary images,"] # [doc = " states, matrices and vectors that you will need in advance. Since the allocation of"] # [doc = " the underlying MTLHeaps that they use is deferred until you actually attempt to write"] # [doc = " to these resources or get the underlying MTLTexture or MTLBuffer, you can create all"] # [doc = " the objects, then call MPSHintTemporaryMemoryUsage, then call the various -encode"] # [doc = " methods and the heap should be sized correctly before memory is distributed to the"] # [doc = " temporary objects. In this exercise, assume that memory is not distributed to the"] # [doc = " temporary object until it is used to hold data, and is reclaimed for reuse when readCount"] # [doc = " reaches zero. The expected size temporary memory used by each object can be queried"] # [doc = " using its -resourceSize method."] # [doc = ""] # [doc = " Notes: The MPSNNGraph does this automatically for its workload. It is not necessary to"] # [doc = " prefetch for that. If a MTLHeap large enough to satisfy the size is already cached,"] # [doc = " no new one will be created. If the prefetched heap turns out to be too small, additional"] # [doc = " small heaps will be created as needed dynamically. If the prefetched heap is too big,"] # [doc = " any additional memory is wasted."] # [doc = ""] # [doc = " When the graph is known in advance, this method is preferred over"] # [doc = " +[MPSTemporaryImage prefetchStorageWithCommandBuffer:imageDescriptorList:]"] # [doc = " as the latter can not estimate the time period over which each resource is used, and is"] # [doc = " likely to conservatively prefetch too small a heap."] # [doc = ""] # [doc = ""] # [doc = " Parameter `cmdBuf`: The scope of the MTLHeap"] # [doc = ""] # [doc = " Parameter `bytes`: The size, in bytes, of the prefetched heap. The actual size ussed may be rounded"] # [doc = " up according to device alignment requirements. This should be the maximum"] # [doc = " `                         amount of temporary memory used at any point in the command buffer."] pub fn MPSHintTemporaryMemoryHighWaterMark (cmd_buf : & ProtocolObject < dyn MTLCommandBuffer > , bytes : NSUInteger ,) ; }
};
}
