// Generated macro for impl_64 (impl)
macro_rules! Depcrateimpl_64 {
() => {
// Module: crate
// Provides: {"impl_64"}
// Dependencies: {}
# [doc = " Access"] impl Url { # [doc = " Return the username mentioned in the URL, if present."] # [doc = ""] # [doc = " # Security Warning"] # [doc = ""] # [doc = " URLs allow usernames to start with `-` which makes it possible to mask command-line arguments as username which then leads to"] # [doc = " the invocation of programs from an attacker controlled URL. See <https://secure.phabricator.com/T12961> for details."] # [doc = ""] # [doc = " If this value is ever going to be passed to a command-line application, call [Self::user_argument_safe()] instead."] pub fn user (& self) -> Option < & str > { self . user . as_deref () } # [doc = " Classify the username of this URL by whether it is safe to pass as a command-line argument."] # [doc = ""] # [doc = " Use this method instead of [Self::user()] if the host is going to be passed to a command-line application."] # [doc = " If the unsafe and absent cases need not be distinguished, [Self::user_argument_safe()] may also be used."] pub fn user_as_argument (& self) -> ArgumentSafety < '_ > { match self . user () { Some (user) if looks_like_command_line_option (user . as_bytes ()) => ArgumentSafety :: Dangerous (user) , Some (user) => ArgumentSafety :: Usable (user) , None => ArgumentSafety :: Absent , } } # [doc = " Return the username of this URL if present *and* if it can't be mistaken for a command-line argument."] # [doc = ""] # [doc = " Use this method or [Self::user_as_argument()] instead of [Self::user()] if the host is going to be"] # [doc = " passed to a command-line application. Prefer [Self::user_as_argument()] unless the unsafe and absent"] # [doc = " cases need not be distinguished from each other."] pub fn user_argument_safe (& self) -> Option < & str > { match self . user_as_argument () { ArgumentSafety :: Usable (user) => Some (user) , _ => None , } } # [doc = " Return the password mentioned in the url, if present."] pub fn password (& self) -> Option < & str > { self . password . as_deref () } # [doc = " Return the host mentioned in the URL, if present."] # [doc = ""] # [doc = " # Security Warning"] # [doc = ""] # [doc = " URLs allow hosts to start with `-` which makes it possible to mask command-line arguments as host which then leads to"] # [doc = " the invocation of programs from an attacker controlled URL. See <https://secure.phabricator.com/T12961> for details."] # [doc = ""] # [doc = " If this value is ever going to be passed to a command-line application, call [Self::host_as_argument()]"] # [doc = " or [Self::host_argument_safe()] instead."] pub fn host (& self) -> Option < & str > { self . host . as_deref () } # [doc = " Classify the host of this URL by whether it is safe to pass as a command-line argument."] # [doc = ""] # [doc = " Use this method instead of [Self::host()] if the host is going to be passed to a command-line application."] # [doc = " If the unsafe and absent cases need not be distinguished, [Self::host_argument_safe()] may also be used."] pub fn host_as_argument (& self) -> ArgumentSafety < '_ > { match self . host () { Some (host) if looks_like_command_line_option (host . as_bytes ()) => ArgumentSafety :: Dangerous (host) , Some (host) => ArgumentSafety :: Usable (host) , None => ArgumentSafety :: Absent , } } # [doc = " Return the host of this URL if present *and* if it can't be mistaken for a command-line argument."] # [doc = ""] # [doc = " Use this method or [Self::host_as_argument()] instead of [Self::host()] if the host is going to be"] # [doc = " passed to a command-line application. Prefer [Self::host_as_argument()] unless the unsafe and absent"] # [doc = " cases need not be distinguished from each other."] pub fn host_argument_safe (& self) -> Option < & str > { match self . host_as_argument () { ArgumentSafety :: Usable (host) => Some (host) , _ => None , } } # [doc = " Return the path of this URL *if* it can't be mistaken for a command-line argument."] # [doc = " Note that it always begins with a slash, which is ignored for this comparison."] # [doc = ""] # [doc = " Use this method instead of accessing [Self::path] directly if the path is going to be passed to a"] # [doc = " command-line application, unless it is certain that the leading `/` will always be included."] pub fn path_argument_safe (& self) -> Option < & BStr > { self . path . get (1 ..) . and_then (| truncated | (! looks_like_command_line_option (truncated)) . then_some (self . path . as_ref ())) } # [doc = " Return true if the path portion of the URL is `/`."] pub fn path_is_root (& self) -> bool { self . path == "/" } # [doc = " Return the actual or default port for use according to the URL scheme."] # [doc = " Note that there may be no default port either."] pub fn port_or_default (& self) -> Option < u16 > { self . port . or_else (| | { use Scheme :: * ; Some (match self . scheme { Http => 80 , Https => 443 , Ssh => 22 , Git => 9418 , File | Ext (_) => return None , }) }) } }
};
}
