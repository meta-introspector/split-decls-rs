// Generated macro for Error (struct)
macro_rules! Depcrate_errorError {
() => {
// Module: crate::error
// Provides: {"Error"}
// Dependencies: {}
# [doc = " An error object consists of both an error code and optional detailed error information for debugging."] # [doc = ""] # [doc = " # Extended error info and the `windows_slim_errors` configuration option"] # [doc = ""] # [doc = " `Error` contains an [`HRESULT`] value that describes the error, as well as an optional"] # [doc = " `IErrorInfo` COM object. The `IErrorInfo` object is a COM object that can provide detailed information"] # [doc = " about an error, such as a text string, a `ProgID` of the originator, etc. If the error object"] # [doc = " was originated in an WinRT component, then additional information such as a stack track may be"] # [doc = " captured."] # [doc = ""] # [doc = " However, many systems based on COM do not use `IErrorInfo`. For these systems, the optional error"] # [doc = " info within `Error` has no benefits, but has substantial costs because it increases the size of"] # [doc = " the `Error` object, which also increases the size of `Result<T>`."] # [doc = ""] # [doc = " This error information can be disabled at compile time by setting `RUSTFLAGS=--cfg=windows_slim_errors`."] # [doc = " This removes the `IErrorInfo` support within the [`Error`] type, which has these benefits:"] # [doc = ""] # [doc = " * It reduces the size of [`Error`] to 4 bytes (the size of [`HRESULT`])."] # [doc = ""] # [doc = " * It reduces the size of `Result<(), Error>` to 4 bytes, allowing it to be returned in a single"] # [doc = "   machine register."] # [doc = ""] # [doc = " * The `Error` (and `Result<T, Error>`) types no longer have a [`Drop`] impl. This removes the need"] # [doc = "   for lifetime checking and running drop code when [`Error`] and [`Result`] go out of scope. This"] # [doc = "   significantly reduces code size for codebase that make extensive use of [`Error`]."] # [doc = ""] # [doc = " Of course, these benefits come with a cost; you lose extended error information for those"] # [doc = " COM objects that support it."] # [doc = ""] # [doc = " This is controlled by a `--cfg` option rather than a Cargo feature because this compilation"] # [doc = " option sets a policy that applies to an entire graph of crates. Individual crates that take a"] # [doc = " dependency on the `windows-result` crate are not in a good position to decide whether they want"] # [doc = " slim errors or full errors.  Cargo features are meant to be additive, but specifying the size"] # [doc = " and contents of `Error` is not a feature so much as a whole-program policy decision."] # [doc = ""] # [doc = " # References"] # [doc = ""] # [doc = " * [`IErrorInfo`](https://learn.microsoft.com/en-us/windows/win32/api/oaidl/nn-oaidl-ierrorinfo)"] # [derive (Clone)] pub struct Error { # [doc = " The `HRESULT` error code, but represented using [`NonZeroI32`]. [`NonZeroI32`] provides"] # [doc = " a \"niche\" to the Rust compiler, which is a space-saving optimization. This allows the"] # [doc = " compiler to use more compact representation for enum variants (such as [`Result`]) that"] # [doc = " contain instances of [`Error`]."] code : NonZeroI32 , # [doc = " Contains details about the error, such as error text."] info : ErrorInfo , }
};
}
