// Generated macro for impl_8 (impl)
macro_rules! Depcrateimpl_8 {
() => {
// Module: crate
// Provides: {"impl_8"}
// Dependencies: {}
impl < T > TryLock < T > { # [doc = " Create a `TryLock` around the value."] # [inline] pub const fn new (val : T) -> TryLock < T > { TryLock { is_locked : AtomicBool :: new (false) , value : UnsafeCell :: new (val) , } } # [doc = " Try to acquire the lock of this value."] # [doc = ""] # [doc = " If the lock is already acquired by someone else, this returns"] # [doc = " `None`. You can try to acquire again whenever you want, perhaps"] # [doc = " by spinning a few times, or by using some other means of"] # [doc = " notification."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " The default memory ordering is to use `Acquire` to lock, and `Release`"] # [doc = " to unlock. If different ordering is required, use"] # [doc = " [`try_lock_explicit`](TryLock::try_lock_explicit) or"] # [doc = " [`try_lock_explicit_unchecked`](TryLock::try_lock_explicit_unchecked)."] # [inline] pub fn try_lock (& self) -> Option < Locked < '_ , T > > { unsafe { self . try_lock_explicit_unchecked (Ordering :: Acquire , Ordering :: Release) } } # [doc = " Try to acquire the lock of this value using the lock and unlock orderings."] # [doc = ""] # [doc = " If the lock is already acquired by someone else, this returns"] # [doc = " `None`. You can try to acquire again whenever you want, perhaps"] # [doc = " by spinning a few times, or by using some other means of"] # [doc = " notification."] # [inline] # [deprecated (since = "0.2.3" , note = "This method is actually unsafe because it unsafely allows \
        the use of weaker memory ordering. Please use try_lock_explicit instead")] pub fn try_lock_order (& self , lock_order : Ordering , unlock_order : Ordering) -> Option < Locked < '_ , T > > { unsafe { self . try_lock_explicit_unchecked (lock_order , unlock_order) } } # [doc = " Try to acquire the lock of this value using the specified lock and"] # [doc = " unlock orderings."] # [doc = ""] # [doc = " If the lock is already acquired by someone else, this returns"] # [doc = " `None`. You can try to acquire again whenever you want, perhaps"] # [doc = " by spinning a few times, or by using some other means of"] # [doc = " notification."] # [doc = ""] # [doc = " # Panic"] # [doc = ""] # [doc = " This method panics if `lock_order` is not any of `Acquire`, `AcqRel`,"] # [doc = " and `SeqCst`, or `unlock_order` is not any of `Release` and `SeqCst`."] # [inline] pub fn try_lock_explicit (& self , lock_order : Ordering , unlock_order : Ordering) -> Option < Locked < '_ , T > > { match lock_order { Ordering :: Acquire | Ordering :: AcqRel | Ordering :: SeqCst => { } _ => panic ! ("lock ordering must be `Acquire`, `AcqRel`, or `SeqCst`") , } match unlock_order { Ordering :: Release | Ordering :: SeqCst => { } _ => panic ! ("unlock ordering must be `Release` or `SeqCst`") , } unsafe { self . try_lock_explicit_unchecked (lock_order , unlock_order) } } # [doc = " Try to acquire the lock of this value using the specified lock and"] # [doc = " unlock orderings without checking that the specified orderings are"] # [doc = " strong enough to be safe."] # [doc = ""] # [doc = " If the lock is already acquired by someone else, this returns"] # [doc = " `None`. You can try to acquire again whenever you want, perhaps"] # [doc = " by spinning a few times, or by using some other means of"] # [doc = " notification."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Unlike [`try_lock_explicit`], this method is unsafe because it does not"] # [doc = " check that the given memory orderings are strong enough to prevent data"] # [doc = " race."] # [doc = ""] # [doc = " [`try_lock_explicit`]: Self::try_lock_explicit"] # [inline] pub unsafe fn try_lock_explicit_unchecked (& self , lock_order : Ordering , unlock_order : Ordering) -> Option < Locked < '_ , T > > { if ! self . is_locked . swap (true , lock_order) { Some (Locked { lock : self , order : unlock_order , _p : PhantomData , }) } else { None } } # [doc = " Take the value back out of the lock when this is the sole owner."] # [inline] pub fn into_inner (self) -> T { debug_assert ! (! self . is_locked . load (Ordering :: Relaxed) , "TryLock was mem::forgotten") ; self . value . into_inner () } }
};
}
