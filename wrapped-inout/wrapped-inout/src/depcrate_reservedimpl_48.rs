// Generated macro for impl_48 (impl)
macro_rules! Depcrate_reservedimpl_48 {
() => {
// Module: crate::reserved
// Provides: {"impl_48"}
// Dependencies: {}
impl < T > InOutBufReserved < '_ , '_ , T > { # [doc = " Create [`InOutBufReserved`] from raw input and output pointers."] # [doc = ""] # [doc = " # Safety"] # [doc = " Behavior is undefined if any of the following conditions are violated:"] # [doc = " - `in_ptr` must point to a properly initialized value of type `T` and"] # [doc = "   must be valid for reads for `in_len * mem::size_of::<T>()` many bytes."] # [doc = " - `out_ptr` must point to a properly initialized value of type `T` and"] # [doc = "   must be valid for both reads and writes for `out_len * mem::size_of::<T>()`"] # [doc = "   many bytes."] # [doc = " - `in_ptr` and `out_ptr` must be either equal or non-overlapping."] # [doc = " - If `in_ptr` and `out_ptr` are equal, then the memory referenced by"] # [doc = "   them must not be accessed through any other pointer (not derived from"] # [doc = "   the return value) for the duration of lifetime 'a. Both read and write"] # [doc = "   accesses are forbidden."] # [doc = " - If `in_ptr` and `out_ptr` are not equal, then the memory referenced by"] # [doc = "   `out_ptr` must not be accessed through any other pointer (not derived from"] # [doc = "   the return value) for the duration of lifetime 'a. Both read and write"] # [doc = "   accesses are forbidden. The memory referenced by `in_ptr` must not be"] # [doc = "   mutated for the duration of lifetime `'a`, except inside an `UnsafeCell`."] # [doc = " - The total size `in_len * mem::size_of::<T>()` and"] # [doc = "   `out_len * mem::size_of::<T>()`  must be no larger than `isize::MAX`."] # [inline (always)] pub unsafe fn from_raw (in_ptr : * const T , in_len : usize , out_ptr : * mut T , out_len : usize ,) -> Self { Self { in_ptr , out_ptr , in_len , out_len , _pd : PhantomData , } } # [doc = " Get raw input and output pointers."] # [inline (always)] pub fn into_raw (self) -> (* const T , * mut T) { (self . in_ptr , self . out_ptr) } # [doc = " Get input buffer length."] # [inline (always)] pub fn get_in_len (& self) -> usize { self . in_len } # [doc = " Get output buffer length."] # [inline (always)] pub fn get_out_len (& self) -> usize { self . out_len } # [doc = " Split buffer into `InOutBuf` with input length and mutable slice pointing to"] # [doc = " the remaining reserved suffix."] pub fn split_reserved (& mut self) -> (InOutBuf < '_ , '_ , T > , & mut [T]) { let in_len = self . get_in_len () ; let out_len = self . get_out_len () ; let in_ptr = self . get_in () . as_ptr () ; let out_ptr = self . get_out () . as_mut_ptr () ; let tail_len = out_len - in_len ; unsafe { let body = InOutBuf :: from_raw (in_ptr , out_ptr , in_len) ; let tail = slice :: from_raw_parts_mut (out_ptr . add (in_len) , tail_len) ; (body , tail) } } }
};
}
