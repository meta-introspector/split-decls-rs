// Generated macro for impl_21 (impl)
macro_rules! Depcrate_inoutimpl_21 {
() => {
// Module: crate::inout
// Provides: {"impl_21"}
// Dependencies: {}
impl < 'inp , 'out , T > InOut < 'inp , 'out , T > { # [doc = " Reborrow `self`."] # [inline (always)] pub fn reborrow (& mut self) -> InOut < '_ , '_ , T > { Self { in_ptr : self . in_ptr , out_ptr : self . out_ptr , _pd : PhantomData , } } # [doc = " Get immutable reference to the input value."] # [inline (always)] pub fn get_in (& self) -> & T { unsafe { & * self . in_ptr } } # [doc = " Get mutable reference to the output value."] # [inline (always)] pub fn get_out (& mut self) -> & mut T { unsafe { & mut * self . out_ptr } } # [doc = " Consume `self` and get mutable reference to the output value with lifetime `'out`"] # [doc = " and output value equal to the input value."] # [doc = ""] # [doc = " In the case if the input and output references are the same, simply returns"] # [doc = " the output reference. Otherwise, copies data from the former to the latter"] # [doc = " before returning the output reference."] pub fn into_out_with_copied_in (self) -> & 'out mut T where T : Copy , { if ! core :: ptr :: eq (self . in_ptr , self . out_ptr) { unsafe { ptr :: copy (self . in_ptr , self . out_ptr , 1) ; } } unsafe { & mut * self . out_ptr } } # [doc = " Consume `self` and get mutable reference to the output value with lifetime `'out`."] # [inline (always)] pub fn into_out (self) -> & 'out mut T { unsafe { & mut * self . out_ptr } } # [doc = " Convert `self` to a pair of raw input and output pointers."] # [inline (always)] pub fn into_raw (self) -> (* const T , * mut T) { (self . in_ptr , self . out_ptr) } # [doc = " Create `InOut` from raw input and output pointers."] # [doc = ""] # [doc = " # Safety"] # [doc = " Behavior is undefined if any of the following conditions are violated:"] # [doc = " - `in_ptr` must point to a properly initialized value of type `T` and"] # [doc = "   must be valid for reads."] # [doc = " - `out_ptr` must point to a properly initialized value of type `T` and"] # [doc = "   must be valid for both reads and writes."] # [doc = " - `in_ptr` and `out_ptr` must be either equal or non-overlapping."] # [doc = " - If `in_ptr` and `out_ptr` are equal, then the memory referenced by"] # [doc = "   them must not be accessed through any other pointer (not derived from"] # [doc = "   the return value) for the duration of lifetime 'a. Both read and write"] # [doc = "   accesses are forbidden."] # [doc = " - If `in_ptr` and `out_ptr` are not equal, then the memory referenced by"] # [doc = "   `out_ptr` must not be accessed through any other pointer (not derived from"] # [doc = "   the return value) for the duration of lifetime `'a`. Both read and write"] # [doc = "   accesses are forbidden. The memory referenced by `in_ptr` must not be"] # [doc = "   mutated for the duration of lifetime `'a`, except inside an `UnsafeCell`."] # [inline (always)] pub unsafe fn from_raw (in_ptr : * const T , out_ptr : * mut T) -> InOut < 'inp , 'out , T > { Self { in_ptr , out_ptr , _pd : PhantomData , } } }
};
}
