// Generated macro for impl_454 (impl)
macro_rules! Depcrate_wrappersimpl_454 {
() => {
// Module: crate::wrappers
// Provides: {"impl_454"}
// Dependencies: {}
impl < T > Unalign < T > { # [doc = " Constructs a new `Unalign`."] # [inline (always)] pub const fn new (val : T) -> Unalign < T > { Unalign (val) } # [doc = " Consumes `self`, returning the inner `T`."] # [inline (always)] pub const fn into_inner (self) -> T { unsafe { crate :: util :: transmute_unchecked (self) } } # [doc = " Attempts to return a reference to the wrapped `T`, failing if `self` is"] # [doc = " not properly aligned."] # [doc = ""] # [doc = " If `self` does not satisfy `align_of::<T>()`, then `try_deref` returns"] # [doc = " `Err`."] # [doc = ""] # [doc = " If `T: Unaligned`, then `Unalign<T>` implements [`Deref`], and callers"] # [doc = " may prefer [`Deref::deref`], which is infallible."] # [inline (always)] pub fn try_deref (& self) -> Result < & T , AlignmentError < & Self , T > > { let inner = Ptr :: from_ref (self) . transmute () ; match inner . try_into_aligned () { Ok (aligned) => Ok (aligned . as_ref ()) , Err (err) => Err (err . map_src (| src | src . into_unalign () . as_ref ())) , } } # [doc = " Attempts to return a mutable reference to the wrapped `T`, failing if"] # [doc = " `self` is not properly aligned."] # [doc = ""] # [doc = " If `self` does not satisfy `align_of::<T>()`, then `try_deref` returns"] # [doc = " `Err`."] # [doc = ""] # [doc = " If `T: Unaligned`, then `Unalign<T>` implements [`DerefMut`], and"] # [doc = " callers may prefer [`DerefMut::deref_mut`], which is infallible."] # [inline (always)] pub fn try_deref_mut (& mut self) -> Result < & mut T , AlignmentError < & mut Self , T > > { let inner = Ptr :: from_mut (self) . transmute :: < _ , _ , (_ , (_ , _)) > () ; match inner . try_into_aligned () { Ok (aligned) => Ok (aligned . as_mut ()) , Err (err) => Err (err . map_src (| src | src . into_unalign () . as_mut ())) , } } # [doc = " Returns a reference to the wrapped `T` without checking alignment."] # [doc = ""] # [doc = " If `T: Unaligned`, then `Unalign<T>` implements[ `Deref`], and callers"] # [doc = " may prefer [`Deref::deref`], which is safe."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must guarantee that `self` satisfies `align_of::<T>()`."] # [inline (always)] pub const unsafe fn deref_unchecked (& self) -> & T { unsafe { mem :: transmute (self) } } # [doc = " Returns a mutable reference to the wrapped `T` without checking"] # [doc = " alignment."] # [doc = ""] # [doc = " If `T: Unaligned`, then `Unalign<T>` implements[ `DerefMut`], and"] # [doc = " callers may prefer [`DerefMut::deref_mut`], which is safe."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must guarantee that `self` satisfies `align_of::<T>()`."] # [inline (always)] pub unsafe fn deref_mut_unchecked (& mut self) -> & mut T { unsafe { & mut * self . get_mut_ptr () } } # [doc = " Gets an unaligned raw pointer to the inner `T`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The returned raw pointer is not necessarily aligned to"] # [doc = " `align_of::<T>()`. Most functions which operate on raw pointers require"] # [doc = " those pointers to be aligned, so calling those functions with the result"] # [doc = " of `get_ptr` will result in undefined behavior if alignment is not"] # [doc = " guaranteed using some out-of-band mechanism. In general, the only"] # [doc = " functions which are safe to call with this pointer are those which are"] # [doc = " explicitly documented as being sound to use with an unaligned pointer,"] # [doc = " such as [`read_unaligned`]."] # [doc = ""] # [doc = " Even if the caller is permitted to mutate `self` (e.g. they have"] # [doc = " ownership or a mutable borrow), it is not guaranteed to be sound to"] # [doc = " write through the returned pointer. If writing is required, prefer"] # [doc = " [`get_mut_ptr`] instead."] # [doc = ""] # [doc = " [`read_unaligned`]: core::ptr::read_unaligned"] # [doc = " [`get_mut_ptr`]: Unalign::get_mut_ptr"] # [inline (always)] pub const fn get_ptr (& self) -> * const T { ptr :: addr_of ! (self . 0) } # [doc = " Gets an unaligned mutable raw pointer to the inner `T`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The returned raw pointer is not necessarily aligned to"] # [doc = " `align_of::<T>()`. Most functions which operate on raw pointers require"] # [doc = " those pointers to be aligned, so calling those functions with the result"] # [doc = " of `get_ptr` will result in undefined behavior if alignment is not"] # [doc = " guaranteed using some out-of-band mechanism. In general, the only"] # [doc = " functions which are safe to call with this pointer are those which are"] # [doc = " explicitly documented as being sound to use with an unaligned pointer,"] # [doc = " such as [`read_unaligned`]."] # [doc = ""] # [doc = " [`read_unaligned`]: core::ptr::read_unaligned"] # [inline (always)] pub fn get_mut_ptr (& mut self) -> * mut T { ptr :: addr_of_mut ! (self . 0) } # [doc = " Sets the inner `T`, dropping the previous value."] # [inline (always)] pub fn set (& mut self , t : T) { * self = Unalign :: new (t) ; } # [doc = " Updates the inner `T` by calling a function on it."] # [doc = ""] # [doc = " If [`T: Unaligned`], then `Unalign<T>` implements [`DerefMut`], and that"] # [doc = " impl should be preferred over this method when performing updates, as it"] # [doc = " will usually be faster and more ergonomic."] # [doc = ""] # [doc = " For large types, this method may be expensive, as it requires copying"] # [doc = " `2 * size_of::<T>()` bytes. \\[1\\]"] # [doc = ""] # [doc = " \\[1\\] Since the inner `T` may not be aligned, it would not be sound to"] # [doc = " invoke `f` on it directly. Instead, `update` moves it into a"] # [doc = " properly-aligned location in the local stack frame, calls `f` on it, and"] # [doc = " then moves it back to its original location in `self`."] # [doc = ""] # [doc = " [`T: Unaligned`]: Unaligned"] # [inline] pub fn update < O , F : FnOnce (& mut T) -> O > (& mut self , f : F) -> O { if mem :: align_of :: < T > () == 1 { let t = unsafe { self . deref_mut_unchecked () } ; return f (t) ; } struct WriteBackOnDrop < T > { copy : ManuallyDrop < T > , slf : * mut Unalign < T > , } impl < T > Drop for WriteBackOnDrop < T > { fn drop (& mut self) { let copy = unsafe { ManuallyDrop :: take (& mut self . copy) } ; unsafe { ptr :: write (self . slf , Unalign :: new (copy)) } ; } } let copy = unsafe { ptr :: read (self) } . into_inner () ; let mut write_back = WriteBackOnDrop { copy : ManuallyDrop :: new (copy) , slf : self } ; let ret = f (& mut write_back . copy) ; drop (write_back) ; ret } }
};
}
