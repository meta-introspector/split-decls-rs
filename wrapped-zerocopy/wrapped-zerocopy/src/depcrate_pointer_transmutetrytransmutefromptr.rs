// Generated macro for TryTransmuteFromPtr (trait)
macro_rules! Depcrate_pointer_transmuteTryTransmuteFromPtr {
() => {
// Module: crate::pointer::transmute
// Provides: {"TryTransmuteFromPtr"}
// Dependencies: {}
# [doc = " Transmutations which are sound to attempt, conditional on validating the bit"] # [doc = " validity of the destination type."] # [doc = ""] # [doc = " If a `Ptr` transmutation is `TryTransmuteFromPtr`, then it is sound to"] # [doc = " perform that transmutation so long as some additional mechanism is used to"] # [doc = " validate that the referent is bit-valid for the destination type. That"] # [doc = " validation mechanism could be a type bound (such as `TransmuteFrom`) or a"] # [doc = " runtime validity check."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " ## Post-conditions"] # [doc = ""] # [doc = " Given `Dst: TryTransmuteFromPtr<Src, A, SV, DV, _>`, callers may assume the"] # [doc = " following:"] # [doc = ""] # [doc = " Given `src: Ptr<'a, Src, (A, _, SV)>`, if the referent of `src` is"] # [doc = " `DV`-valid for `Dst`, then it is sound to transmute `src` into `dst: Ptr<'a,"] # [doc = " Dst, (A, Unaligned, DV)>` by preserving pointer address and metadata."] # [doc = ""] # [doc = " ## Pre-conditions"] # [doc = ""] # [doc = " Given `src: Ptr<Src, (A, _, SV)>` and `dst: Ptr<Dst, (A, Unaligned, DV)>`,"] # [doc = " `Dst: TryTransmuteFromPtr<Src, A, SV, DV, _>` is sound if all of the"] # [doc = " following hold:"] # [doc = " - Forwards transmutation: Either of the following hold:"] # [doc = "   - So long as `dst` is active, no mutation of `dst`'s referent is allowed"] # [doc = "     except via `dst` itself"] # [doc = "   - The set of `DV`-valid `Dst`s is a superset of the set of `SV`-valid"] # [doc = "     `Src`s"] # [doc = " - Reverse transmutation: Either of the following hold:"] # [doc = "   - `dst` does not permit mutation of its referent"] # [doc = "   - The set of `DV`-valid `Dst`s is a subset of the set of `SV`-valid `Src`s"] # [doc = " - No safe code, given access to `src` and `dst`, can cause undefined"] # [doc = "   behavior: Any of the following hold:"] # [doc = "   - `A` is `Exclusive`"] # [doc = "   - `Src: Immutable` and `Dst: Immutable`"] # [doc = "   - It is sound for shared code to operate on a `&Src` and `&Dst` which"] # [doc = "     reference the same byte range at the same time"] # [doc = ""] # [doc = " ## Proof"] # [doc = ""] # [doc = " Given:"] # [doc = " - `src: Ptr<'a, Src, (A, _, SV)>`"] # [doc = " - `src`'s referent is `DV`-valid for `Dst`"] # [doc = " - `Dst: SizeEq<Src>`"] # [doc = ""] # [doc = " We are trying to prove that it is sound to perform a pointer address- and"] # [doc = " metadata-preserving transmute from `src` to a `dst: Ptr<'a, Dst, (A,"] # [doc = " Unaligned, DV)>`. We need to prove that such a transmute does not violate"] # [doc = " any of `src`'s invariants, and that it satisfies all invariants of the"] # [doc = " destination `Ptr` type."] # [doc = ""] # [doc = " First, all of `src`'s `PtrInner` invariants are upheld. `src`'s address and"] # [doc = " metadata are unchanged, so:"] # [doc = " - If its referent is not zero sized, then it still has valid provenance for"] # [doc = "   its referent, which is still entirely contained in some Rust allocation,"] # [doc = "   `A`"] # [doc = " - If its referent is not zero sized, `A` is guaranteed to live for at least"] # [doc = "   `'a`"] # [doc = ""] # [doc = " Since `Dst: SizeEq<Src>`, and since `dst` has the same address and metadata"] # [doc = " as `src`, `dst` addresses the same byte range as `src`. `dst` also has the"] # [doc = " same lifetime as `src`. Therefore, all of the `PtrInner` invariants"] # [doc = " mentioned above also hold for `dst`."] # [doc = ""] # [doc = " Second, since `src`'s address is unchanged, it still satisfies its"] # [doc = " alignment. Since `dst`'s alignment is `Unaligned`, it trivially satisfies"] # [doc = " its alignment."] # [doc = ""] # [doc = " Third, aliasing is either `Exclusive` or `Shared`:"] # [doc = " - If it is `Exclusive`, then both `src` and `dst` satisfy `Exclusive`"] # [doc = "   aliasing trivially: since `src` and `dst` have the same lifetime, `src` is"] # [doc = "   inaccessible so long as `dst` is alive, and no other live `Ptr`s or"] # [doc = "   references may reference the same referent."] # [doc = " - If it is `Shared`, then either:"] # [doc = "   - `Src: Immutable` and `Dst: Immutable`, and so `UnsafeCell`s trivially"] # [doc = "     cover the same byte ranges in both types."] # [doc = "   - It is explicitly sound for safe code to operate on a `&Src` and a `&Dst`"] # [doc = "     pointing to the same byte range at the same time."] # [doc = ""] # [doc = " Fourth, `src`'s validity is satisfied. By invariant, `src`'s referent began"] # [doc = " as an `SV`-valid `Src`. It is guaranteed to remain so, as either of the"] # [doc = " following hold:"] # [doc = " - `dst` does not permit mutation of its referent."] # [doc = " - The set of `DV`-valid `Dst`s is a superset of the set of `SV`-valid"] # [doc = "   `Src`s. Thus, any value written via `dst` is guaranteed to be `SV`-valid"] # [doc = "   for `Src`."] # [doc = ""] # [doc = " Fifth, `dst`'s validity is satisfied. It is a given of this proof that the"] # [doc = " referent is `DV`-valid for `Dst`. It is guaranteed to remain so, as either"] # [doc = " of the following hold:"] # [doc = " - So long as `dst` is active, no mutation of the referent is allowed except"] # [doc = "   via `dst` itself."] # [doc = " - The set of `DV`-valid `Dst`s is a superset of the set of `SV`-valid"] # [doc = "   `Src`s. Thus, any value written via `src` is guaranteed to be a `DV`-valid"] # [doc = "   `Dst`."] pub unsafe trait TryTransmuteFromPtr < Src : ? Sized , A : Aliasing , SV : Validity , DV : Validity , R > : SizeEq < Src > { }
};
}
