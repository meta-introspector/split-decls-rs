// Generated macro for use_511 (pub_use)
macro_rules! Depcrateuse_511 {
() => {
// Module: crate
// Provides: {"use_511"}
// Dependencies: {}
# [doc = " Analyzes whether a type is [`Unaligned`]."] # [doc = ""] # [doc = " This derive analyzes, at compile time, whether the annotated type satisfies"] # [doc = " the [safety conditions] of `Unaligned` and implements `Unaligned` if it is"] # [doc = " sound to do so. This derive can be applied to structs, enums, and unions;"] # [doc = " e.g.:"] # [doc = ""] # [doc = " ```"] # [doc = " # use zerocopy_derive::Unaligned;"] # [doc = " #[derive(Unaligned)]"] # [doc = " #[repr(C)]"] # [doc = " struct MyStruct {"] # [doc = " # /*"] # [doc = "     ..."] # [doc = " # */"] # [doc = " }"] # [doc = ""] # [doc = " #[derive(Unaligned)]"] # [doc = " #[repr(u8)]"] # [doc = " enum MyEnum {"] # [doc = " #   Variant0,"] # [doc = " # /*"] # [doc = "     ..."] # [doc = " # */"] # [doc = " }"] # [doc = ""] # [doc = " #[derive(Unaligned)]"] # [doc = " #[repr(packed)]"] # [doc = " union MyUnion {"] # [doc = " #   variant: u8,"] # [doc = " # /*"] # [doc = "     ..."] # [doc = " # */"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " # Analysis"] # [doc = ""] # [doc = " *This section describes, roughly, the analysis performed by this derive to"] # [doc = " determine whether it is sound to implement `Unaligned` for a given type."] # [doc = " Unless you are modifying the implementation of this derive, or attempting to"] # [doc = " manually implement `Unaligned` for a type yourself, you don't need to read"] # [doc = " this section.*"] # [doc = ""] # [doc = " If a type has the following properties, then this derive can implement"] # [doc = " `Unaligned` for that type:"] # [doc = ""] # [doc = " - If the type is a struct or union:"] # [doc = "   - If `repr(align(N))` is provided, `N` must equal 1."] # [doc = "   - If the type is `repr(C)` or `repr(transparent)`, all fields must be"] # [doc = "     [`Unaligned`]."] # [doc = "   - If the type is not `repr(C)` or `repr(transparent)`, it must be"] # [doc = "     `repr(packed)` or `repr(packed(1))`."] # [doc = " - If the type is an enum:"] # [doc = "   - If `repr(align(N))` is provided, `N` must equal 1."] # [doc = "   - It must be a field-less enum (meaning that all variants have no fields)."] # [doc = "   - It must be `repr(i8)` or `repr(u8)`."] # [doc = ""] # [doc = " [safety conditions]: trait@Unaligned#safety"] # [cfg (any (feature = "derive" , test))] # [cfg_attr (doc_cfg , doc (cfg (feature = "derive")))] pub use zerocopy_derive :: Unaligned ;
};
}
