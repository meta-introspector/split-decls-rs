// Generated macro for _conversions (module)
macro_rules! Depcrate_pointer_ptr_conversions {
() => {
// Module: crate::pointer::ptr
// Provides: {"_conversions"}
// Dependencies: {}
# [doc = " Methods for converting to and from `Ptr` and Rust's safe reference types."] mod _conversions { use super :: * ; # [doc = " `&'a T` → `Ptr<'a, T>`"] impl < 'a , T > Ptr < 'a , T , (Shared , Aligned , Valid) > where T : 'a + ? Sized , { # [doc = " Constructs a `Ptr` from a shared reference."] # [doc (hidden)] # [inline] pub fn from_ref (ptr : & 'a T) -> Self { let inner = PtrInner :: from_ref (ptr) ; unsafe { Self :: from_inner (inner) } } } # [doc = " `&'a mut T` → `Ptr<'a, T>`"] impl < 'a , T > Ptr < 'a , T , (Exclusive , Aligned , Valid) > where T : 'a + ? Sized , { # [doc = " Constructs a `Ptr` from an exclusive reference."] # [inline] pub (crate) fn from_mut (ptr : & 'a mut T) -> Self { let inner = PtrInner :: from_mut (ptr) ; unsafe { Self :: from_inner (inner) } } } # [doc = " `Ptr<'a, T>` → `&'a T`"] impl < 'a , T , I > Ptr < 'a , T , I > where T : 'a + ? Sized , I : Invariants < Alignment = Aligned , Validity = Valid > , I :: Aliasing : Reference , { # [doc = " Converts `self` to a shared reference."] # [allow (clippy :: wrong_self_convention)] pub (crate) fn as_ref (self) -> & 'a T { let raw = self . as_inner () . as_non_null () ; unsafe { raw . as_ref () } } } impl < 'a , T , I > Ptr < 'a , T , I > where T : 'a + ? Sized , I : Invariants , I :: Aliasing : Reference , { # [doc = " Reborrows `self`, producing another `Ptr`."] # [doc = ""] # [doc = " Since `self` is borrowed immutably, this prevents any mutable"] # [doc = " methods from being called on `self` as long as the returned `Ptr`"] # [doc = " exists."] # [doc (hidden)] # [inline] # [allow (clippy :: needless_lifetimes)] pub fn reborrow < 'b > (& 'b mut self) -> Ptr < 'b , T , I > where 'a : 'b , { unsafe { Ptr :: from_inner (self . as_inner ()) } } } # [doc = " `Ptr<'a, T>` → `&'a mut T`"] impl < 'a , T > Ptr < 'a , T , (Exclusive , Aligned , Valid) > where T : 'a + ? Sized , { # [doc = " Converts `self` to a mutable reference."] # [allow (clippy :: wrong_self_convention)] pub (crate) fn as_mut (self) -> & 'a mut T { let mut raw = self . as_inner () . as_non_null () ; unsafe { raw . as_mut () } } } # [doc = " `Ptr<'a, T>` → `Ptr<'a, U>`"] impl < 'a , T : ? Sized , I > Ptr < 'a , T , I > where I : Invariants , { pub (crate) fn transmute < U , V , R > (self) -> Ptr < 'a , U , (I :: Aliasing , Unaligned , V) > where V : Validity , U : TransmuteFromPtr < T , I :: Aliasing , I :: Validity , V , R > + SizeEq < T > + ? Sized , { unsafe { self . transmute_unchecked (SizeEq :: cast_from_raw) } } # [doc (hidden)] # [inline (always)] # [must_use] pub fn recall_validity < V , R > (self) -> Ptr < 'a , T , (I :: Aliasing , I :: Alignment , V) > where V : Validity , T : TransmuteFromPtr < T , I :: Aliasing , I :: Validity , V , R > , { let ptr = unsafe { self . transmute_unchecked (SizeEq :: cast_from_raw) } ; unsafe { ptr . assume_alignment :: < I :: Alignment > () } } # [doc = " Casts to a different (unsized) target type without checking interior"] # [doc = " mutability."] # [doc = ""] # [doc = " Callers should prefer [`cast_unsized`] where possible."] # [doc = ""] # [doc = " [`cast_unsized`]: Ptr::cast_unsized"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises that `u = cast(p)` is a pointer cast with the"] # [doc = " following properties:"] # [doc = " - `u` addresses a subset of the bytes addressed by `p`"] # [doc = " - `u` has the same provenance as `p`"] # [doc = " - If `I::Aliasing` is [`Shared`], it must not be possible for safe"] # [doc = "   code, operating on a `&T` and `&U` with the same referent"] # [doc = "   simultaneously, to cause undefined behavior"] # [doc = " - It is sound to transmute a pointer of type `T` with aliasing"] # [doc = "   `I::Aliasing` and validity `I::Validity` to a pointer of type `U`"] # [doc = "   with aliasing `I::Aliasing` and validity `V`. This is a subtle"] # [doc = "   soundness requirement that is a function of `T`, `U`,"] # [doc = "   `I::Aliasing`, `I::Validity`, and `V`, and may depend upon the"] # [doc = "   presence, absence, or specific location of `UnsafeCell`s in `T`"] # [doc = "   and/or `U`. See [`Validity`] for more details."] # [doc (hidden)] # [inline] pub unsafe fn transmute_unchecked < U : ? Sized , V , F > (self , cast : F ,) -> Ptr < 'a , U , (I :: Aliasing , Unaligned , V) > where V : Validity , F : FnOnce (PtrInner < 'a , T >) -> PtrInner < 'a , U > , { let ptr = cast (self . as_inner ()) ; unsafe { Ptr :: from_inner (ptr) } } } # [doc = " `Ptr<'a, T, (_, _, _)>` → `Ptr<'a, Unalign<T>, (_, Aligned, _)>`"] impl < 'a , T , I > Ptr < 'a , T , I > where I : Invariants , { # [doc = " Converts a `Ptr` an unaligned `T` into a `Ptr` to an aligned"] # [doc = " `Unalign<T>`."] pub (crate) fn into_unalign (self ,) -> Ptr < 'a , crate :: Unalign < T > , (I :: Aliasing , Aligned , I :: Validity) > { let ptr = unsafe { self . transmute_unchecked (PtrInner :: cast_sized) } ; ptr . bikeshed_recall_aligned () } } impl < 'a , T , I > Ptr < 'a , T , I > where T : ? Sized , I : Invariants < Validity = Valid > , I :: Aliasing : Reference , { # [doc = " Reads the referent."] # [must_use] # [inline] pub fn read_unaligned < R > (self) -> T where T : Copy , T : Read < I :: Aliasing , R > , { (* self . into_unalign () . as_ref ()) . into_inner () } # [doc = " Views the value as an aligned reference."] # [doc = ""] # [doc = " This is only available if `T` is [`Unaligned`]."] # [must_use] # [inline] pub fn unaligned_as_ref (self) -> & 'a T where T : crate :: Unaligned , { self . bikeshed_recall_aligned () . as_ref () } } }
};
}
