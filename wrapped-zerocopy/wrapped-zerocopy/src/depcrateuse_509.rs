// Generated macro for use_509 (pub_use)
macro_rules! Depcrateuse_509 {
() => {
// Module: crate
// Provides: {"use_509"}
// Dependencies: {}
# [doc = " Analyzes whether a type is [`IntoBytes`]."] # [doc = ""] # [doc = " This derive analyzes, at compile time, whether the annotated type satisfies"] # [doc = " the [safety conditions] of `IntoBytes` and implements `IntoBytes` if it is"] # [doc = " sound to do so. This derive can be applied to structs and enums (see below"] # [doc = " for union support); e.g.:"] # [doc = ""] # [doc = " ```"] # [doc = " # use zerocopy_derive::{IntoBytes};"] # [doc = " #[derive(IntoBytes)]"] # [doc = " #[repr(C)]"] # [doc = " struct MyStruct {"] # [doc = " # /*"] # [doc = "     ..."] # [doc = " # */"] # [doc = " }"] # [doc = ""] # [doc = " #[derive(IntoBytes)]"] # [doc = " #[repr(u8)]"] # [doc = " enum MyEnum {"] # [doc = " #   Variant,"] # [doc = " # /*"] # [doc = "     ..."] # [doc = " # */"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [safety conditions]: trait@IntoBytes#safety"] # [doc = ""] # [doc = " # Error Messages"] # [doc = ""] # [doc = " On Rust toolchains prior to 1.78.0, due to the way that the custom derive"] # [doc = " for `IntoBytes` is implemented, you may get an error like this:"] # [doc = ""] # [doc = " ```text"] # [doc = " error[E0277]: the trait bound `(): PaddingFree<Foo, true>` is not satisfied"] # [doc = "   --> lib.rs:23:10"] # [doc = "    |"] # [doc = "  1 | #[derive(IntoBytes)]"] # [doc = "    |          ^^^^^^^^^ the trait `PaddingFree<Foo, true>` is not implemented for `()`"] # [doc = "    |"] # [doc = "    = help: the following implementations were found:"] # [doc = "                   <() as PaddingFree<T, false>>"] # [doc = " ```"] # [doc = ""] # [doc = " This error indicates that the type being annotated has padding bytes, which"] # [doc = " is illegal for `IntoBytes` types. Consider reducing the alignment of some"] # [doc = " fields by using types in the [`byteorder`] module, wrapping field types in"] # [doc = " [`Unalign`], adding explicit struct fields where those padding bytes would"] # [doc = " be, or using `#[repr(packed)]`. See the Rust Reference's page on [type"] # [doc = " layout] for more information about type layout and padding."] # [doc = ""] # [doc = " [type layout]: https://doc.rust-lang.org/reference/type-layout.html"] # [doc = ""] # [doc = " # Unions"] # [doc = ""] # [doc = " Currently, union bit validity is [up in the air][union-validity], and so"] # [doc = " zerocopy does not support `#[derive(IntoBytes)]` on unions by default."] # [doc = " However, implementing `IntoBytes` on a union type is likely sound on all"] # [doc = " existing Rust toolchains - it's just that it may become unsound in the"] # [doc = " future. You can opt-in to `#[derive(IntoBytes)]` support on unions by"] # [doc = " passing the unstable `zerocopy_derive_union_into_bytes` cfg:"] # [doc = ""] # [doc = " ```shell"] # [doc = " $ RUSTFLAGS='--cfg zerocopy_derive_union_into_bytes' cargo build"] # [doc = " ```"] # [doc = ""] # [doc = " However, it is your responsibility to ensure that this derive is sound on"] # [doc = " the specific versions of the Rust toolchain you are using! We make no"] # [doc = " stability or soundness guarantees regarding this cfg, and may remove it at"] # [doc = " any point."] # [doc = ""] # [doc = " We are actively working with Rust to stabilize the necessary language"] # [doc = " guarantees to support this in a forwards-compatible way, which will enable"] # [doc = " us to remove the cfg gate. As part of this effort, we need to know how much"] # [doc = " demand there is for this feature. If you would like to use `IntoBytes` on"] # [doc = " unions, [please let us know][discussion]."] # [doc = ""] # [doc = " [union-validity]: https://github.com/rust-lang/unsafe-code-guidelines/issues/438"] # [doc = " [discussion]: https://github.com/google/zerocopy/discussions/1802"] # [doc = ""] # [doc = " # Analysis"] # [doc = ""] # [doc = " *This section describes, roughly, the analysis performed by this derive to"] # [doc = " determine whether it is sound to implement `IntoBytes` for a given type."] # [doc = " Unless you are modifying the implementation of this derive, or attempting to"] # [doc = " manually implement `IntoBytes` for a type yourself, you don't need to read"] # [doc = " this section.*"] # [doc = ""] # [doc = " If a type has the following properties, then this derive can implement"] # [doc = " `IntoBytes` for that type:"] # [doc = ""] # [doc = " - If the type is a struct, its fields must be [`IntoBytes`]. Additionally:"] # [doc = "     - if the type is `repr(transparent)` or `repr(packed)`, it is"] # [doc = "       [`IntoBytes`] if its fields are [`IntoBytes`]; else,"] # [doc = "     - if the type is `repr(C)` with at most one field, it is [`IntoBytes`]"] # [doc = "       if its field is [`IntoBytes`]; else,"] # [doc = "     - if the type has no generic parameters, it is [`IntoBytes`] if the type"] # [doc = "       is sized and has no padding bytes; else,"] # [doc = "     - if the type is `repr(C)`, its fields must be [`Unaligned`]."] # [doc = " - If the type is an enum:"] # [doc = "   - It must have a defined representation (`repr`s `C`, `u8`, `u16`, `u32`,"] # [doc = "     `u64`, `usize`, `i8`, `i16`, `i32`, `i64`, or `isize`)."] # [doc = "   - It must have no padding bytes."] # [doc = "   - Its fields must be [`IntoBytes`]."] # [doc = ""] # [doc = " This analysis is subject to change. Unsafe code may *only* rely on the"] # [doc = " documented [safety conditions] of `FromBytes`, and must *not* rely on the"] # [doc = " implementation details of this derive."] # [doc = ""] # [doc = " [Rust Reference]: https://doc.rust-lang.org/reference/type-layout.html"] # [cfg (any (feature = "derive" , test))] # [cfg_attr (doc_cfg , doc (cfg (feature = "derive")))] pub use zerocopy_derive :: IntoBytes ;
};
}
