// Generated macro for _casts (module)
macro_rules! Depcrate_pointer_ptr_casts {
() => {
// Module: crate::pointer::ptr
// Provides: {"_casts"}
// Dependencies: {}
# [doc = " Casts of the referent type."] mod _casts { use super :: * ; impl < 'a , T , I > Ptr < 'a , T , I > where T : 'a + ? Sized , I : Invariants , { # [doc = " Casts to a different (unsized) target type without checking interior"] # [doc = " mutability."] # [doc = ""] # [doc = " Callers should prefer [`cast_unsized`] where possible."] # [doc = ""] # [doc = " [`cast_unsized`]: Ptr::cast_unsized"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises that `u = cast(p)` is a pointer cast with the"] # [doc = " following properties:"] # [doc = " - `u` addresses a subset of the bytes addressed by `p`"] # [doc = " - `u` has the same provenance as `p`"] # [doc = " - If `I::Aliasing` is [`Shared`], it must not be possible for safe"] # [doc = "   code, operating on a `&T` and `&U` with the same referent"] # [doc = "   simultaneously, to cause undefined behavior"] # [doc = ""] # [doc = " `cast_unsized_unchecked` guarantees that the pointer passed to"] # [doc = " `cast` will reference a byte sequence which is either contained"] # [doc = " inside a single allocated object or is zero sized. In either case,"] # [doc = " this means that its size will fit in an `isize` and it will not wrap"] # [doc = " around the address space."] # [doc (hidden)] # [inline] pub unsafe fn cast_unsized_unchecked < U , F : FnOnce (PtrInner < 'a , T >) -> PtrInner < 'a , U > > (self , cast : F ,) -> Ptr < 'a , U , (I :: Aliasing , Unaligned , I :: Validity) > where U : 'a + CastableFrom < T , I :: Validity , I :: Validity > + ? Sized , { unsafe { self . transmute_unchecked (cast) } } # [doc = " Casts to a different (unsized) target type."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises that `u = cast(p)` is a pointer cast with the"] # [doc = " following properties:"] # [doc = " - `u` addresses a subset of the bytes addressed by `p`"] # [doc = " - `u` has the same provenance as `p`"] # [doc (hidden)] # [inline] pub unsafe fn cast_unsized < U , F , R > (self , cast : F ,) -> Ptr < 'a , U , (I :: Aliasing , Unaligned , I :: Validity) > where T : MutationCompatible < U , I :: Aliasing , I :: Validity , I :: Validity , R > , U : 'a + ? Sized + CastableFrom < T , I :: Validity , I :: Validity > , F : FnOnce (PtrInner < 'a , T >) -> PtrInner < 'a , U > , { unsafe { self . cast_unsized_unchecked (cast) } } } impl < 'a , T , I > Ptr < 'a , T , I > where T : 'a + KnownLayout + ? Sized , I : Invariants < Validity = Initialized > , { # [doc = " Casts this pointer-to-initialized into a pointer-to-bytes."] # [allow (clippy :: wrong_self_convention)] # [must_use] # [inline] pub fn as_bytes < R > (self) -> Ptr < 'a , [u8] , (I :: Aliasing , Aligned , Valid) > where T : Read < I :: Aliasing , R > , I :: Aliasing : Reference , { let ptr = unsafe { self . cast_unsized (PtrInner :: as_bytes) } ; ptr . bikeshed_recall_aligned () . recall_validity :: < Valid , (_ , (_ , _)) > () } } impl < 'a , T , I , const N : usize > Ptr < 'a , [T ; N] , I > where T : 'a , I : Invariants , { # [doc = " Casts this pointer-to-array into a slice."] # [allow (clippy :: wrong_self_convention)] pub (crate) fn as_slice (self) -> Ptr < 'a , [T] , I > { let slice = self . as_inner () . as_slice () ; unsafe { Ptr :: from_inner (slice) } } } # [doc = " For caller convenience, these methods are generic over alignment"] # [doc = " invariant. In practice, the referent is always well-aligned, because the"] # [doc = " alignment of `[u8]` is 1."] impl < 'a , I > Ptr < 'a , [u8] , I > where I : Invariants < Validity = Valid > , { # [doc = " Attempts to cast `self` to a `U` using the given cast type."] # [doc = ""] # [doc = " If `U` is a slice DST and pointer metadata (`meta`) is provided,"] # [doc = " then the cast will only succeed if it would produce an object with"] # [doc = " the given metadata."] # [doc = ""] # [doc = " Returns `None` if the resulting `U` would be invalidly-aligned, if"] # [doc = " no `U` can fit in `self`, or if the provided pointer metadata"] # [doc = " describes an invalid instance of `U`. On success, returns a pointer"] # [doc = " to the largest-possible `U` which fits in `self`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller may assume that this implementation is correct, and may"] # [doc = " rely on that assumption for the soundness of their code. In"] # [doc = " particular, the caller may assume that, if `try_cast_into` returns"] # [doc = " `Some((ptr, remainder))`, then `ptr` and `remainder` refer to"] # [doc = " non-overlapping byte ranges within `self`, and that `ptr` and"] # [doc = " `remainder` entirely cover `self`. Finally:"] # [doc = " - If this is a prefix cast, `ptr` has the same address as `self`."] # [doc = " - If this is a suffix cast, `remainder` has the same address as"] # [doc = "   `self`."] # [inline (always)] pub (crate) fn try_cast_into < U , R > (self , cast_type : CastType , meta : Option < U :: PointerMetadata > ,) -> Result < (Ptr < 'a , U , (I :: Aliasing , Aligned , Initialized) > , Ptr < 'a , [u8] , I >) , CastError < Self , U > , > where I :: Aliasing : Reference , U : 'a + ? Sized + KnownLayout + Read < I :: Aliasing , R > , { let (inner , remainder) = self . as_inner () . try_cast_into (cast_type , meta) . map_err (| err | { err . map_src (| inner | unsafe { Ptr :: from_inner (inner) }) }) ? ; let res = unsafe { Ptr :: from_inner (inner) } ; let remainder = unsafe { Ptr :: from_inner (remainder) } ; Ok ((res , remainder)) } # [doc = " Attempts to cast `self` into a `U`, failing if all of the bytes of"] # [doc = " `self` cannot be treated as a `U`."] # [doc = ""] # [doc = " In particular, this method fails if `self` is not validly-aligned"] # [doc = " for `U` or if `self`'s size is not a valid size for `U`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " On success, the caller may assume that the returned pointer"] # [doc = " references the same byte range as `self`."] # [allow (unused)] # [inline (always)] pub (crate) fn try_cast_into_no_leftover < U , R > (self , meta : Option < U :: PointerMetadata > ,) -> Result < Ptr < 'a , U , (I :: Aliasing , Aligned , Initialized) > , CastError < Self , U > > where I :: Aliasing : Reference , U : 'a + ? Sized + KnownLayout + Read < I :: Aliasing , R > , { # [allow (unstable_name_collisions)] match self . try_cast_into (CastType :: Prefix , meta) { Ok ((slf , remainder)) => { if remainder . len () == 0 { Ok (slf) } else { let slf = slf . as_bytes () ; let slf = unsafe { slf . assume_alignment :: < I :: Alignment > () } ; let slf = slf . unify_invariants () ; Err (CastError :: Size (SizeError :: < _ , U > :: new (slf))) } } Err (err) => Err (err) , } } } impl < 'a , T , I > Ptr < 'a , core :: cell :: UnsafeCell < T > , I > where T : 'a + ? Sized , I : Invariants < Aliasing = Exclusive > , { # [doc = " Converts this `Ptr` into a pointer to the underlying data."] # [doc = ""] # [doc = " This call borrows the `UnsafeCell` mutably (at compile-time) which"] # [doc = " guarantees that we possess the only reference."] # [doc = ""] # [doc = " This is like [`UnsafeCell::get_mut`], but for `Ptr`."] # [doc = ""] # [doc = " [`UnsafeCell::get_mut`]: core::cell::UnsafeCell::get_mut"] # [must_use] # [inline (always)] pub fn get_mut (self) -> Ptr < 'a , T , I > { # [allow (clippy :: as_conversions)] let ptr = unsafe { self . transmute_unchecked (| ptr | cast ! (ptr)) } ; let ptr = unsafe { ptr . assume_alignment :: < I :: Alignment > () } ; ptr . unify_invariants () } } }
};
}
