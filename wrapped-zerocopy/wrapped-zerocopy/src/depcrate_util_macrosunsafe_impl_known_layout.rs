// Generated macro for unsafe_impl_known_layout (macro)
macro_rules! Depcrate_util_macrosunsafe_impl_known_layout {
() => {
// Module: crate::util::macros
// Provides: {"unsafe_impl_known_layout"}
// Dependencies: {}
# [doc = " Implements `KnownLayout` for a type in terms of the implementation of"] # [doc = " another type with the same representation."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " - `$ty` and `$repr` must have the same:"] # [doc = "   - Fixed prefix size"] # [doc = "   - Alignment"] # [doc = "   - (For DSTs) trailing slice element size"] # [doc = " - It must be valid to perform an `as` cast from `*mut $repr` to `*mut $ty`,"] # [doc = "   and this operation must preserve referent size (ie, `size_of_val_raw`)."] macro_rules ! unsafe_impl_known_layout { ($ ($ tyvar : ident : ? Sized + KnownLayout =>) ? # [repr ($ repr : ty)] $ ty : ty) => { { use core :: ptr :: NonNull ; crate :: util :: macros :: __unsafe () ; # [allow (non_local_definitions)] unsafe impl <$ ($ tyvar : ? Sized + KnownLayout) ?> KnownLayout for $ ty { # [allow (clippy :: missing_inline_in_public_items , dead_code)] # [cfg_attr (all (coverage_nightly , __ZEROCOPY_INTERNAL_USE_ONLY_NIGHTLY_FEATURES_IN_TESTS) , coverage (off))] fn only_derive_is_allowed_to_implement_this_trait () { } type PointerMetadata = <$ repr as KnownLayout >:: PointerMetadata ; type MaybeUninit = <$ repr as KnownLayout >:: MaybeUninit ; const LAYOUT : DstLayout = <$ repr as KnownLayout >:: LAYOUT ; # [inline (always)] fn raw_from_ptr_len (bytes : NonNull < u8 >, meta : <$ repr as KnownLayout >:: PointerMetadata) -> NonNull < Self > { # [allow (clippy :: as_conversions)] let ptr = <$ repr >:: raw_from_ptr_len (bytes , meta) . as_ptr () as * mut Self ; unsafe { NonNull :: new_unchecked (ptr) } } # [inline (always)] fn pointer_to_metadata (ptr : * mut Self) -> Self :: PointerMetadata { # [allow (clippy :: as_conversions)] let ptr = ptr as * mut $ repr ; <$ repr >:: pointer_to_metadata (ptr) } } } } ; }
};
}
