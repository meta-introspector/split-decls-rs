// Generated macro for SplitAt (trait)
macro_rules! Depcrate_split_atSplitAt {
() => {
// Module: crate::split_at
// Provides: {"SplitAt"}
// Dependencies: {}
# [doc = " Types that can be split in two."] # [doc = ""] # [doc = " This trait generalizes Rust's existing support for splitting slices to"] # [doc = " support slices and slice-based dynamically-sized types (\"slice DSTs\")."] # [doc = ""] # [doc = " # Implementation"] # [doc = ""] # [doc = " **Do not implement this trait yourself!** Instead, use"] # [doc = " [`#[derive(SplitAt)]`][derive]; e.g.:"] # [doc = ""] # [doc = " ```"] # [doc = " # use zerocopy_derive::{SplitAt, KnownLayout};"] # [doc = " #[derive(SplitAt, KnownLayout)]"] # [doc = " #[repr(C)]"] # [doc = " struct MyStruct<T: ?Sized> {"] # [doc = " # /*"] # [doc = "     ...,"] # [doc = " # */"] # [doc = "     // `SplitAt` types must have at least one field."] # [doc = "     field: T,"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " This derive performs a sophisticated, compile-time safety analysis to"] # [doc = " determine whether a type is `SplitAt`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This trait does not convey any safety guarantees to code outside this crate."] # [doc = ""] # [doc = " You must not rely on the `#[doc(hidden)]` internals of `SplitAt`. Future"] # [doc = " releases of zerocopy may make backwards-breaking changes to these items,"] # [doc = " including changes that only affect soundness, which may cause code which"] # [doc = " uses those items to silently become unsound."] # [doc = ""] # [cfg_attr (feature = "derive" , doc = "[derive]: zerocopy_derive::SplitAt")] # [cfg_attr (not (feature = "derive") , doc = concat ! ("[derive]: https://docs.rs/zerocopy/" , env ! ("CARGO_PKG_VERSION") , "/zerocopy/derive.SplitAt.html") ,)] # [cfg_attr (zerocopy_diagnostic_on_unimplemented_1_78_0 , diagnostic :: on_unimplemented (note = "Consider adding `#[derive(SplitAt)]` to `{Self}`"))] pub unsafe trait SplitAt : KnownLayout < PointerMetadata = usize > { # [doc = " The element type of the trailing slice."] type Elem ; # [doc (hidden)] fn only_derive_is_allowed_to_implement_this_trait () where Self : Sized ; # [doc = " Unsafely splits `self` in two."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises that `l_len` is not greater than the length of"] # [doc = " `self`'s trailing slice."] # [inline] # [must_use] unsafe fn split_at_unchecked (& self , l_len : usize) -> Split < & Self > { unsafe { Split :: < & Self > :: new (self , l_len) } } # [doc = " Attempts to split `self` in two."] # [doc = ""] # [doc = " Returns `None` if `l_len` is greater than the length of `self`'s"] # [doc = " trailing slice."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use zerocopy::{SplitAt, FromBytes};"] # [doc = " # use zerocopy_derive::*;"] # [doc = ""] # [doc = " #[derive(SplitAt, FromBytes, KnownLayout, Immutable)]"] # [doc = " #[repr(C)]"] # [doc = " struct Packet {"] # [doc = "     length: u8,"] # [doc = "     body: [u8],"] # [doc = " }"] # [doc = ""] # [doc = " // These bytes encode a `Packet`."] # [doc = " let bytes = &[4, 1, 2, 3, 4, 5, 6, 7, 8, 9][..];"] # [doc = ""] # [doc = " let packet = Packet::ref_from_bytes(bytes).unwrap();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4, 5, 6, 7, 8, 9]);"] # [doc = ""] # [doc = " // Attempt to split `packet` at `length`."] # [doc = " let split = packet.split_at(packet.length as usize).unwrap();"] # [doc = ""] # [doc = " // Use the `Immutable` bound on `Packet` to prove that it's okay to"] # [doc = " // return concurrent references to `packet` and `rest`."] # [doc = " let (packet, rest) = split.via_immutable();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4]);"] # [doc = " assert_eq!(rest, [5, 6, 7, 8, 9]);"] # [doc = " ```"] # [inline] # [must_use = "has no side effects"] fn split_at (& self , l_len : usize) -> Option < Split < & Self > > { MetadataOf :: new_in_bounds (self , l_len) . map (# [inline (always)] | l_len | { unsafe { Split :: new (self , l_len . get ()) } } ,) } # [doc = " Unsafely splits `self` in two."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises that `l_len` is not greater than the length of"] # [doc = " `self`'s trailing slice."] # [inline] # [must_use] unsafe fn split_at_mut_unchecked (& mut self , l_len : usize) -> Split < & mut Self > { unsafe { Split :: < & mut Self > :: new (self , l_len) } } # [doc = " Attempts to split `self` in two."] # [doc = ""] # [doc = " Returns `None` if `l_len` is greater than the length of `self`'s"] # [doc = " trailing slice, or if the given `l_len` would result in [the trailing"] # [doc = " padding](KnownLayout#slice-dst-layout) of the left portion overlapping"] # [doc = " the right portion."] # [doc = ""] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use zerocopy::{SplitAt, FromBytes};"] # [doc = " # use zerocopy_derive::*;"] # [doc = ""] # [doc = " #[derive(SplitAt, FromBytes, KnownLayout, IntoBytes)]"] # [doc = " #[repr(C)]"] # [doc = " struct Packet<B: ?Sized> {"] # [doc = "     length: u8,"] # [doc = "     body: B,"] # [doc = " }"] # [doc = ""] # [doc = " // These bytes encode a `Packet`."] # [doc = " let mut bytes = &mut [4, 1, 2, 3, 4, 5, 6, 7, 8, 9][..];"] # [doc = ""] # [doc = " let packet = Packet::<[u8]>::mut_from_bytes(bytes).unwrap();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4, 5, 6, 7, 8, 9]);"] # [doc = ""] # [doc = " {"] # [doc = "     // Attempt to split `packet` at `length`."] # [doc = "     let split = packet.split_at_mut(packet.length as usize).unwrap();"] # [doc = ""] # [doc = "     // Use the `IntoBytes` bound on `Packet` to prove that it's okay to"] # [doc = "     // return concurrent references to `packet` and `rest`."] # [doc = "     let (packet, rest) = split.via_into_bytes();"] # [doc = ""] # [doc = "     assert_eq!(packet.length, 4);"] # [doc = "     assert_eq!(packet.body, [1, 2, 3, 4]);"] # [doc = "     assert_eq!(rest, [5, 6, 7, 8, 9]);"] # [doc = ""] # [doc = "     rest.fill(0);"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4, 0, 0, 0, 0, 0]);"] # [doc = " ```"] # [inline] fn split_at_mut (& mut self , l_len : usize) -> Option < Split < & mut Self > > { MetadataOf :: new_in_bounds (self , l_len) . map (# [inline (always)] | l_len | { unsafe { Split :: new (self , l_len . get ()) } } ,) } }
};
}
