macro_rules! deps {
    () => {
        CastType!();
        SizeInfo!();
        Alignment!();
        DstLayout!();
        SliceDst!();
        MetadataCastError!();
        TrailingSliceLayout!();
    };
}

macro_rules! impl_290 {
    () => {
        deps!();
        impl DstLayout { # [doc = " The minimum possible alignment of a type."] const MIN_ALIGN : NonZeroUsize = match NonZeroUsize :: new (1) { Some (min_align) => min_align , None => const_unreachable ! () , } ; # [doc = " The maximum theoretic possible alignment of a type."] # [doc = ""] # [doc = " For compatibility with future Rust versions, this is defined as the"] # [doc = " maximum power-of-two that fits into a `usize`. See also"] # [doc = " [`DstLayout::CURRENT_MAX_ALIGN`]."] pub (crate) const THEORETICAL_MAX_ALIGN : NonZeroUsize = match NonZeroUsize :: new (1 << (POINTER_WIDTH_BITS - 1)) { Some (max_align) => max_align , None => const_unreachable ! () , } ; # [doc = " The current, documented max alignment of a type \\[1\\]."] # [doc = ""] # [doc = " \\[1\\] Per <https://doc.rust-lang.org/reference/type-layout.html#the-alignment-modifiers>:"] # [doc = ""] # [doc = "   The alignment value must be a power of two from 1 up to"] # [doc = "   2<sup>29</sup>."] # [cfg (not (kani))] # [cfg (not (target_pointer_width = "16"))] pub (crate) const CURRENT_MAX_ALIGN : NonZeroUsize = match NonZeroUsize :: new (1 << 28) { Some (max_align) => max_align , None => const_unreachable ! () , } ; # [cfg (not (kani))] # [cfg (target_pointer_width = "16")] pub (crate) const CURRENT_MAX_ALIGN : NonZeroUsize = match NonZeroUsize :: new (1 << 15) { Some (max_align) => max_align , None => const_unreachable ! () , } ; # [doc = " Assumes that this layout lacks static shallow padding."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This method does not panic."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If `self` describes the size and alignment of type that lacks static"] # [doc = " shallow padding, unsafe code may assume that the result of this method"] # [doc = " accurately reflects the size, alignment, and lack of static shallow"] # [doc = " padding of that type."] const fn assume_shallow_unpadded (self) -> Self { Self { statically_shallow_unpadded : true , .. self } } # [doc = " Constructs a `DstLayout` for a zero-sized type with `repr_align`"] # [doc = " alignment (or 1). If `repr_align` is provided, then it must be a power"] # [doc = " of two."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if the supplied `repr_align` is not a power of two."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Unsafe code may assume that the contract of this function is satisfied."] # [doc (hidden)] # [must_use] # [inline] pub const fn new_zst (repr_align : Option < NonZeroUsize >) -> DstLayout { let align = match repr_align { Some (align) => align , None => Self :: MIN_ALIGN , } ; const_assert ! (align . get () . is_power_of_two ()) ; DstLayout { align , size_info : SizeInfo :: Sized { size : 0 } , statically_shallow_unpadded : true , } } # [doc = " Constructs a `DstLayout` which describes `T` and assumes `T` may contain"] # [doc = " padding."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Unsafe code may assume that `DstLayout` is the correct layout for `T`."] # [doc (hidden)] # [must_use] # [inline] pub const fn for_type < T > () -> DstLayout { DstLayout { align : match NonZeroUsize :: new (mem :: align_of :: < T > ()) { Some (align) => align , None => const_unreachable ! () , } , size_info : SizeInfo :: Sized { size : mem :: size_of :: < T > () } , statically_shallow_unpadded : false , } } # [doc = " Constructs a `DstLayout` which describes a `T` that does not contain"] # [doc = " padding."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Unsafe code may assume that `DstLayout` is the correct layout for `T`."] # [doc (hidden)] # [must_use] # [inline] pub const fn for_unpadded_type < T > () -> DstLayout { Self :: for_type :: < T > () . assume_shallow_unpadded () } # [doc = " Constructs a `DstLayout` which describes `[T]`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Unsafe code may assume that `DstLayout` is the correct layout for `[T]`."] pub (crate) const fn for_slice < T > () -> DstLayout { DstLayout { align : match NonZeroUsize :: new (mem :: align_of :: < T > ()) { Some (align) => align , None => const_unreachable ! () , } , size_info : SizeInfo :: SliceDst (TrailingSliceLayout { offset : 0 , elem_size : mem :: size_of :: < T > () , }) , statically_shallow_unpadded : true , } } # [doc = " Constructs a complete `DstLayout` reflecting a `repr(C)` struct with the"] # [doc = " given alignment modifiers and fields."] # [doc = ""] # [doc = " This method cannot be used to match the layout of a record with the"] # [doc = " default representation, as that representation is mostly unspecified."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " For any definition of a `repr(C)` struct, if this method is invoked with"] # [doc = " alignment modifiers and fields corresponding to that definition, the"] # [doc = " resulting `DstLayout` will correctly encode the layout of that struct."] # [doc = ""] # [doc = " We make no guarantees to the behavior of this method when it is invoked"] # [doc = " with arguments that cannot correspond to a valid `repr(C)` struct."] # [must_use] # [inline] pub const fn for_repr_c_struct (repr_align : Option < NonZeroUsize > , repr_packed : Option < NonZeroUsize > , fields : & [DstLayout] ,) -> DstLayout { let mut layout = DstLayout :: new_zst (repr_align) ; let mut i = 0 ; # [allow (clippy :: arithmetic_side_effects)] while i < fields . len () { # [allow (clippy :: indexing_slicing)] let field = fields [i] ; layout = layout . extend (field , repr_packed) ; i += 1 ; } layout = layout . pad_to_align () ; layout } # [doc = " Like `Layout::extend`, this creates a layout that describes a record"] # [doc = " whose layout consists of `self` followed by `next` that includes the"] # [doc = " necessary inter-field padding, but not any trailing padding."] # [doc = ""] # [doc = " In order to match the layout of a `#[repr(C)]` struct, this method"] # [doc = " should be invoked for each field in declaration order. To add trailing"] # [doc = " padding, call `DstLayout::pad_to_align` after extending the layout for"] # [doc = " all fields. If `self` corresponds to a type marked with"] # [doc = " `repr(packed(N))`, then `repr_packed` should be set to `Some(N)`,"] # [doc = " otherwise `None`."] # [doc = ""] # [doc = " This method cannot be used to match the layout of a record with the"] # [doc = " default representation, as that representation is mostly unspecified."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If a (potentially hypothetical) valid `repr(C)` Rust type begins with"] # [doc = " fields whose layout are `self`, and those fields are immediately"] # [doc = " followed by a field whose layout is `field`, then unsafe code may rely"] # [doc = " on `self.extend(field, repr_packed)` producing a layout that correctly"] # [doc = " encompasses those two components."] # [doc = ""] # [doc = " We make no guarantees to the behavior of this method if these fragments"] # [doc = " cannot appear in a valid Rust type (e.g., the concatenation of the"] # [doc = " layouts would lead to a size larger than `isize::MAX`)."] # [doc (hidden)] # [must_use] # [inline] pub const fn extend (self , field : DstLayout , repr_packed : Option < NonZeroUsize >) -> Self { use util :: { max , min , padding_needed_for } ; let max_align = match repr_packed { Some (max_align) => max_align , None => Self :: THEORETICAL_MAX_ALIGN , } ; const_assert ! (max_align . get () . is_power_of_two ()) ; # [cfg (not (kani))] { const_debug_assert ! (self . align . get () <= DstLayout :: CURRENT_MAX_ALIGN . get ()) ; const_debug_assert ! (field . align . get () <= DstLayout :: CURRENT_MAX_ALIGN . get ()) ; if let Some (repr_packed) = repr_packed { const_debug_assert ! (repr_packed . get () <= DstLayout :: CURRENT_MAX_ALIGN . get ()) ; } } let field_align = min (field . align , max_align) ; let align = max (self . align , field_align) ; let (interfield_padding , size_info) = match self . size_info { SizeInfo :: SliceDst (..) => const_panic ! ("Cannot extend a DST with additional fields.") , SizeInfo :: Sized { size : preceding_size } => { let padding = padding_needed_for (preceding_size , field_align) ; let offset = match preceding_size . checked_add (padding) { Some (offset) => offset , None => const_panic ! ("Adding padding to `self`'s size overflows `usize`.") , } ; (padding , match field . size_info { SizeInfo :: Sized { size : field_size } => { let size = match offset . checked_add (field_size) { Some (size) => size , None => const_panic ! ("`field` cannot be appended without the total size overflowing `usize`") , } ; SizeInfo :: Sized { size } } SizeInfo :: SliceDst (TrailingSliceLayout { offset : trailing_offset , elem_size , }) => { let offset = match offset . checked_add (trailing_offset) { Some (offset) => offset , None => const_panic ! ("`field` cannot be appended without the total size overflowing `usize`") , } ; SizeInfo :: SliceDst (TrailingSliceLayout { offset , elem_size }) } } ,) } } ; let statically_shallow_unpadded = self . statically_shallow_unpadded && field . statically_shallow_unpadded && interfield_padding == 0 ; DstLayout { align , size_info , statically_shallow_unpadded } } # [doc = " Like `Layout::pad_to_align`, this routine rounds the size of this layout"] # [doc = " up to the nearest multiple of this type's alignment or `repr_packed`"] # [doc = " (whichever is less). This method leaves DST layouts unchanged, since the"] # [doc = " trailing padding of DSTs is computed at runtime."] # [doc = ""] # [doc = " The accompanying boolean is `true` if the resulting composition of"] # [doc = " fields necessitated static (as opposed to dynamic) padding; otherwise"] # [doc = " `false`."] # [doc = ""] # [doc = " In order to match the layout of a `#[repr(C)]` struct, this method"] # [doc = " should be invoked after the invocations of [`DstLayout::extend`]. If"] # [doc = " `self` corresponds to a type marked with `repr(packed(N))`, then"] # [doc = " `repr_packed` should be set to `Some(N)`, otherwise `None`."] # [doc = ""] # [doc = " This method cannot be used to match the layout of a record with the"] # [doc = " default representation, as that representation is mostly unspecified."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If a (potentially hypothetical) valid `repr(C)` type begins with fields"] # [doc = " whose layout are `self` followed only by zero or more bytes of trailing"] # [doc = " padding (not included in `self`), then unsafe code may rely on"] # [doc = " `self.pad_to_align(repr_packed)` producing a layout that correctly"] # [doc = " encapsulates the layout of that type."] # [doc = ""] # [doc = " We make no guarantees to the behavior of this method if `self` cannot"] # [doc = " appear in a valid Rust type (e.g., because the addition of trailing"] # [doc = " padding would lead to a size larger than `isize::MAX`)."] # [doc (hidden)] # [must_use] # [inline] pub const fn pad_to_align (self) -> Self { use util :: padding_needed_for ; let (static_padding , size_info) = match self . size_info { SizeInfo :: Sized { size : unpadded_size } => { let padding = padding_needed_for (unpadded_size , self . align) ; let size = match unpadded_size . checked_add (padding) { Some (size) => size , None => const_panic ! ("Adding padding caused size to overflow `usize`.") , } ; (padding , SizeInfo :: Sized { size }) } size_info @ SizeInfo :: SliceDst (_) => (0 , size_info) , } ; let statically_shallow_unpadded = self . statically_shallow_unpadded && static_padding == 0 ; DstLayout { align : self . align , size_info , statically_shallow_unpadded } } # [doc = " Produces `true` if `self` requires static padding; otherwise `false`."] # [must_use] # [inline (always)] pub const fn requires_static_padding (self) -> bool { ! self . statically_shallow_unpadded } # [doc = " Produces `true` if there exists any metadata for which a type of layout"] # [doc = " `self` would require dynamic trailing padding; otherwise `false`."] # [must_use] # [inline (always)] pub const fn requires_dynamic_padding (self) -> bool { # [allow (clippy :: arithmetic_side_effects)] match self . size_info { SizeInfo :: Sized { .. } => false , SizeInfo :: SliceDst (trailing_slice_layout) => { trailing_slice_layout . offset % self . align . get () != 0 || trailing_slice_layout . elem_size % self . align . get () != 0 } } } # [doc = " Validates that a cast is sound from a layout perspective."] # [doc = ""] # [doc = " Validates that the size and alignment requirements of a type with the"] # [doc = " layout described in `self` would not be violated by performing a"] # [doc = " `cast_type` cast from a pointer with address `addr` which refers to a"] # [doc = " memory region of size `bytes_len`."] # [doc = ""] # [doc = " If the cast is valid, `validate_cast_and_convert_metadata` returns"] # [doc = " `(elems, split_at)`. If `self` describes a dynamically-sized type, then"] # [doc = " `elems` is the maximum number of trailing slice elements for which a"] # [doc = " cast would be valid (for sized types, `elem` is meaningless and should"] # [doc = " be ignored). `split_at` is the index at which to split the memory region"] # [doc = " in order for the prefix (suffix) to contain the result of the cast, and"] # [doc = " in order for the remaining suffix (prefix) to contain the leftover"] # [doc = " bytes."] # [doc = ""] # [doc = " There are three conditions under which a cast can fail:"] # [doc = " - The smallest possible value for the type is larger than the provided"] # [doc = "   memory region"] # [doc = " - A prefix cast is requested, and `addr` does not satisfy `self`'s"] # [doc = "   alignment requirement"] # [doc = " - A suffix cast is requested, and `addr + bytes_len` does not satisfy"] # [doc = "   `self`'s alignment requirement (as a consequence, since all instances"] # [doc = "   of the type are a multiple of its alignment, no size for the type will"] # [doc = "   result in a starting address which is properly aligned)"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller may assume that this implementation is correct, and may rely"] # [doc = " on that assumption for the soundness of their code. In particular, the"] # [doc = " caller may assume that, if `validate_cast_and_convert_metadata` returns"] # [doc = " `Some((elems, split_at))`, then:"] # [doc = " - A pointer to the type (for dynamically sized types, this includes"] # [doc = "   `elems` as its pointer metadata) describes an object of size `size <="] # [doc = "   bytes_len`"] # [doc = " - If this is a prefix cast:"] # [doc = "   - `addr` satisfies `self`'s alignment"] # [doc = "   - `size == split_at`"] # [doc = " - If this is a suffix cast:"] # [doc = "   - `split_at == bytes_len - size`"] # [doc = "   - `addr + split_at` satisfies `self`'s alignment"] # [doc = ""] # [doc = " Note that this method does *not* ensure that a pointer constructed from"] # [doc = " its return values will be a valid pointer. In particular, this method"] # [doc = " does not reason about `isize` overflow, which is a requirement of many"] # [doc = " Rust pointer APIs, and may at some point be determined to be a validity"] # [doc = " invariant of pointer types themselves. This should never be a problem so"] # [doc = " long as the arguments to this method are derived from a known-valid"] # [doc = " pointer (e.g., one derived from a safe Rust reference), but it is"] # [doc = " nonetheless the caller's responsibility to justify that pointer"] # [doc = " arithmetic will not overflow based on a safety argument *other than* the"] # [doc = " mere fact that this method returned successfully."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " `validate_cast_and_convert_metadata` will panic if `self` describes a"] # [doc = " DST whose trailing slice element is zero-sized."] # [doc = ""] # [doc = " If `addr + bytes_len` overflows `usize`,"] # [doc = " `validate_cast_and_convert_metadata` may panic, or it may return"] # [doc = " incorrect results. No guarantees are made about when"] # [doc = " `validate_cast_and_convert_metadata` will panic. The caller should not"] # [doc = " rely on `validate_cast_and_convert_metadata` panicking in any particular"] # [doc = " condition, even if `debug_assertions` are enabled."] # [allow (unused)] # [inline (always)] pub (crate) const fn validate_cast_and_convert_metadata (& self , addr : usize , bytes_len : usize , cast_type : CastType ,) -> Result < (usize , usize) , MetadataCastError > { macro_rules ! __const_debug_assert { ($ e : expr $ (, $ msg : expr) ?) => { const_debug_assert ! ({ # [allow (clippy :: arithmetic_side_effects)] let e = $ e ; e } $ (, $ msg) ?) ; } ; } let size_info = match self . size_info . try_to_nonzero_elem_size () { Some (size_info) => size_info , None => const_panic ! ("attempted to cast to slice type with zero-sized element") , } ; __const_debug_assert ! (addr . checked_add (bytes_len) . is_some () , "`addr` + `bytes_len` > usize::MAX") ; { let offset = match cast_type { CastType :: Prefix => 0 , CastType :: Suffix => bytes_len , } ; # [allow (clippy :: arithmetic_side_effects)] if (addr + offset) % self . align . get () != 0 { return Err (MetadataCastError :: Alignment) ; } } let (elems , self_bytes) = match size_info { SizeInfo :: Sized { size } => { if size > bytes_len { return Err (MetadataCastError :: Size) ; } (0 , size) } SizeInfo :: SliceDst (TrailingSliceLayout { offset , elem_size }) => { let max_total_bytes = util :: round_down_to_next_multiple_of_alignment (bytes_len , self . align) ; let max_slice_and_padding_bytes = match max_total_bytes . checked_sub (offset) { Some (max) => max , None => return Err (MetadataCastError :: Size) , } ; # [allow (clippy :: arithmetic_side_effects)] let elems = max_slice_and_padding_bytes / elem_size . get () ; # [allow (clippy :: arithmetic_side_effects)] let without_padding = offset + elems * elem_size . get () ; # [allow (clippy :: arithmetic_side_effects)] let self_bytes = without_padding + util :: padding_needed_for (without_padding , self . align) ; (elems , self_bytes) } } ; __const_debug_assert ! (self_bytes <= bytes_len) ; let split_at = match cast_type { CastType :: Prefix => self_bytes , # [allow (clippy :: arithmetic_side_effects)] CastType :: Suffix => bytes_len - self_bytes , } ; Ok ((elems , split_at)) } }
    };
}

impl_290!()