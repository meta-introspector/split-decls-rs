macro_rules! deps {
    () => {
        Invariants!();
    };
}

macro_rules! def {
    () => {
        deps!();
        # [doc = " Module used to gate access to [`Ptr`]'s fields."] mod def { # [cfg (doc)] use super :: super :: invariant ; use super :: * ; # [doc = " A raw pointer with more restrictions."] # [doc = ""] # [doc = " `Ptr<T>` is similar to [`NonNull<T>`], but it is more restrictive in the"] # [doc = " following ways (note that these requirements only hold of non-zero-sized"] # [doc = " referents):"] # [doc = " - It must derive from a valid allocation."] # [doc = " - It must reference a byte range which is contained inside the"] # [doc = "   allocation from which it derives."] # [doc = "   - As a consequence, the byte range it references must have a size"] # [doc = "     which does not overflow `isize`."] # [doc = ""] # [doc = " Depending on how `Ptr` is parameterized, it may have additional"] # [doc = " invariants:"] # [doc = " - `ptr` conforms to the aliasing invariant of"] # [doc = "   [`I::Aliasing`](invariant::Aliasing)."] # [doc = " - `ptr` conforms to the alignment invariant of"] # [doc = "   [`I::Alignment`](invariant::Alignment)."] # [doc = " - `ptr` conforms to the validity invariant of"] # [doc = "   [`I::Validity`](invariant::Validity)."] # [doc = ""] # [doc = " `Ptr<'a, T>` is [covariant] in `'a` and invariant in `T`."] # [doc = ""] # [doc = " [`NonNull<T>`]: core::ptr::NonNull"] # [doc = " [covariant]: https://doc.rust-lang.org/reference/subtyping.html"] pub struct Ptr < 'a , T , I > where T : ? Sized , I : Invariants , { # [doc = " # Invariants"] # [doc = ""] # [doc = " 0. `ptr` conforms to the aliasing invariant of"] # [doc = "    [`I::Aliasing`](invariant::Aliasing)."] # [doc = " 1. `ptr` conforms to the alignment invariant of"] # [doc = "    [`I::Alignment`](invariant::Alignment)."] # [doc = " 2. `ptr` conforms to the validity invariant of"] # [doc = "    [`I::Validity`](invariant::Validity)."] ptr : PtrInner < 'a , T > , _invariants : PhantomData < I > , } impl < 'a , T , I > Ptr < 'a , T , I > where T : 'a + ? Sized , I : Invariants , { # [doc = " Constructs a new `Ptr` from a [`PtrInner`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises that:"] # [doc = ""] # [doc = " 0. `ptr` conforms to the aliasing invariant of"] # [doc = "    [`I::Aliasing`](invariant::Aliasing)."] # [doc = " 1. `ptr` conforms to the alignment invariant of"] # [doc = "    [`I::Alignment`](invariant::Alignment)."] # [doc = " 2. `ptr` conforms to the validity invariant of"] # [doc = "    [`I::Validity`](invariant::Validity)."] pub (crate) unsafe fn from_inner (ptr : PtrInner < 'a , T >) -> Ptr < 'a , T , I > { Self { ptr , _invariants : PhantomData } } # [doc = " Converts this `Ptr<T>` to a [`PtrInner<T>`]."] # [doc = ""] # [doc = " Note that this method does not consume `self`. The caller should"] # [doc = " watch out for `unsafe` code which uses the returned value in a way"] # [doc = " that violates the safety invariants of `self`."] pub (crate) fn as_inner (& self) -> PtrInner < 'a , T > { self . ptr } } }
    };
}

def!();