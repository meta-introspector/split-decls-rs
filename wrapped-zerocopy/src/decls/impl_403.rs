macro_rules! deps {
    () => {
        Aligned!();
        IntoBytes!();
        Unaligned!();
        Valid!();
        FromBytes!();
        Exclusive!();
        SplitAt!();
        KnownLayout!();
        Split!();
    };
}

macro_rules! impl_403 {
    () => {
        deps!();
        impl < 'a , T > Split < & 'a mut T > where T : ? Sized + SplitAt , { # [inline (always)] fn into_ptr (self) -> Split < Ptr < 'a , T , (Exclusive , Aligned , Valid) > > { let source = Ptr :: from_mut (self . source) ; unsafe { Split :: new (source , self . l_len) } } # [doc = " Produces the split parts of `self`, using [`IntoBytes`] to ensure that"] # [doc = " it is sound to have concurrent references to both parts."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use zerocopy::{SplitAt, FromBytes};"] # [doc = " # use zerocopy_derive::*;"] # [doc = ""] # [doc = " #[derive(SplitAt, FromBytes, KnownLayout, IntoBytes)]"] # [doc = " #[repr(C)]"] # [doc = " struct Packet<B: ?Sized> {"] # [doc = "     length: u8,"] # [doc = "     body: B,"] # [doc = " }"] # [doc = ""] # [doc = " // These bytes encode a `Packet`."] # [doc = " let mut bytes = &mut [4, 1, 2, 3, 4, 5, 6, 7, 8, 9][..];"] # [doc = ""] # [doc = " let packet = Packet::<[u8]>::mut_from_bytes(bytes).unwrap();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4, 5, 6, 7, 8, 9]);"] # [doc = ""] # [doc = " {"] # [doc = "     // Attempt to split `packet` at `length`."] # [doc = "     let split = packet.split_at_mut(packet.length as usize).unwrap();"] # [doc = ""] # [doc = "     // Use the `IntoBytes` bound on `Packet` to prove that it's okay to"] # [doc = "     // return concurrent references to `packet` and `rest`."] # [doc = "     let (packet, rest) = split.via_into_bytes();"] # [doc = ""] # [doc = "     assert_eq!(packet.length, 4);"] # [doc = "     assert_eq!(packet.body, [1, 2, 3, 4]);"] # [doc = "     assert_eq!(rest, [5, 6, 7, 8, 9]);"] # [doc = ""] # [doc = "     rest.fill(0);"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4, 0, 0, 0, 0, 0]);"] # [doc = " ```"] # [must_use = "has no side effects"] # [inline (always)] pub fn via_into_bytes (self) -> (& 'a mut T , & 'a mut [T :: Elem]) where T : IntoBytes , { let (l , r) = self . into_ptr () . via_into_bytes () ; (l . as_mut () , r . as_mut ()) } # [doc = " Produces the split parts of `self`, using [`Unaligned`] to ensure that"] # [doc = " it is sound to have concurrent references to both parts."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use zerocopy::{SplitAt, FromBytes};"] # [doc = " # use zerocopy_derive::*;"] # [doc = ""] # [doc = " #[derive(SplitAt, FromBytes, KnownLayout, IntoBytes, Unaligned)]"] # [doc = " #[repr(C)]"] # [doc = " struct Packet<B: ?Sized> {"] # [doc = "     length: u8,"] # [doc = "     body: B,"] # [doc = " }"] # [doc = ""] # [doc = " // These bytes encode a `Packet`."] # [doc = " let mut bytes = &mut [4, 1, 2, 3, 4, 5, 6, 7, 8, 9][..];"] # [doc = ""] # [doc = " let packet = Packet::<[u8]>::mut_from_bytes(bytes).unwrap();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4, 5, 6, 7, 8, 9]);"] # [doc = ""] # [doc = " {"] # [doc = "     // Attempt to split `packet` at `length`."] # [doc = "     let split = packet.split_at_mut(packet.length as usize).unwrap();"] # [doc = ""] # [doc = "     // Use the `Unaligned` bound on `Packet` to prove that it's okay to"] # [doc = "     // return concurrent references to `packet` and `rest`."] # [doc = "     let (packet, rest) = split.via_unaligned();"] # [doc = ""] # [doc = "     assert_eq!(packet.length, 4);"] # [doc = "     assert_eq!(packet.body, [1, 2, 3, 4]);"] # [doc = "     assert_eq!(rest, [5, 6, 7, 8, 9]);"] # [doc = ""] # [doc = "     rest.fill(0);"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4, 0, 0, 0, 0, 0]);"] # [doc = " ```"] # [must_use = "has no side effects"] # [inline (always)] pub fn via_unaligned (self) -> (& 'a mut T , & 'a mut [T :: Elem]) where T : Unaligned , { let (l , r) = self . into_ptr () . via_unaligned () ; (l . as_mut () , r . as_mut ()) } # [doc = " Produces the split parts of `self`, using a dynamic check to ensure that"] # [doc = " it is sound to have concurrent references to both parts. You should"] # [doc = " prefer using [`Self::via_into_bytes`] or [`Self::via_unaligned`], which"] # [doc = " have no runtime cost."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use zerocopy::{SplitAt, FromBytes};"] # [doc = " # use zerocopy_derive::*;"] # [doc = ""] # [doc = " #[derive(SplitAt, FromBytes, KnownLayout, IntoBytes, Debug)]"] # [doc = " #[repr(C)]"] # [doc = " struct Packet<B: ?Sized> {"] # [doc = "     length: u8,"] # [doc = "     body: B,"] # [doc = " }"] # [doc = ""] # [doc = " // These bytes encode a `Packet`."] # [doc = " let mut bytes = &mut [4, 1, 2, 3, 4, 5, 6, 7, 8, 9][..];"] # [doc = ""] # [doc = " let packet = Packet::<[u8]>::mut_from_bytes(bytes).unwrap();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4, 5, 6, 7, 8, 9]);"] # [doc = ""] # [doc = " {"] # [doc = "     // Attempt to split `packet` at `length`."] # [doc = "     let split = packet.split_at_mut(packet.length as usize).unwrap();"] # [doc = ""] # [doc = "     // Use a dynamic check to prove that it's okay to return concurrent"] # [doc = "     // references to `packet` and `rest`."] # [doc = "     let (packet, rest) = split.via_runtime_check().unwrap();"] # [doc = ""] # [doc = "     assert_eq!(packet.length, 4);"] # [doc = "     assert_eq!(packet.body, [1, 2, 3, 4]);"] # [doc = "     assert_eq!(rest, [5, 6, 7, 8, 9]);"] # [doc = ""] # [doc = "     rest.fill(0);"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4, 0, 0, 0, 0, 0]);"] # [doc = " ```"] # [must_use = "has no side effects"] # [inline (always)] pub fn via_runtime_check (self) -> Result < (& 'a mut T , & 'a mut [T :: Elem]) , Self > { match self . into_ptr () . via_runtime_check () { Ok ((l , r)) => Ok ((l . as_mut () , r . as_mut ())) , Err (s) => Err (s . into_mut ()) , } } # [doc = " Unsafely produces the split parts of `self`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The trailing padding bytes of the left portion must not overlap the"] # [doc = " right portion. For some dynamically sized types, the padding that"] # [doc = " appears after the trailing slice field [is a dynamic function of the"] # [doc = " trailing slice length](KnownLayout#slice-dst-layout). Thus, for some"] # [doc = " types, this condition is dependent on the length of the left portion."] # [must_use = "has no side effects"] # [inline (always)] pub unsafe fn via_unchecked (self) -> (& 'a mut T , & 'a mut [T :: Elem]) { let (l , r) = unsafe { self . into_ptr () . via_unchecked () } ; (l . as_mut () , r . as_mut ()) } }
    };
}

impl_403!()