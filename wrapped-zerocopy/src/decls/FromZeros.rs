macro_rules! deps {
    () => {
        TryFromBytes!();
        PointerMetadata!();
        Immutable!();
        KnownLayout!();
        FromBytes!();
        AllocError!();
    };
}

macro_rules! FromZeros {
    () => {
        deps!();
        # [doc = " Types for which a sequence of `0` bytes is a valid instance."] # [doc = ""] # [doc = " Any memory region of the appropriate length which is guaranteed to contain"] # [doc = " only zero bytes can be viewed as any `FromZeros` type with no runtime"] # [doc = " overhead. This is useful whenever memory is known to be in a zeroed state,"] # [doc = " such memory returned from some allocation routines."] # [doc = ""] # [doc = " # Warning: Padding bytes"] # [doc = ""] # [doc = " Note that, when a value is moved or copied, only the non-padding bytes of"] # [doc = " that value are guaranteed to be preserved. It is unsound to assume that"] # [doc = " values written to padding bytes are preserved after a move or copy. For more"] # [doc = " details, see the [`FromBytes` docs][frombytes-warning-padding-bytes]."] # [doc = ""] # [doc = " [frombytes-warning-padding-bytes]: FromBytes#warning-padding-bytes"] # [doc = ""] # [doc = " # Implementation"] # [doc = ""] # [doc = " **Do not implement this trait yourself!** Instead, use"] # [doc = " [`#[derive(FromZeros)]`][derive]; e.g.:"] # [doc = ""] # [doc = " ```"] # [doc = " # use zerocopy_derive::{FromZeros, Immutable};"] # [doc = " #[derive(FromZeros)]"] # [doc = " struct MyStruct {"] # [doc = " # /*"] # [doc = "     ..."] # [doc = " # */"] # [doc = " }"] # [doc = ""] # [doc = " #[derive(FromZeros)]"] # [doc = " #[repr(u8)]"] # [doc = " enum MyEnum {"] # [doc = " #   Variant0,"] # [doc = " # /*"] # [doc = "     ..."] # [doc = " # */"] # [doc = " }"] # [doc = ""] # [doc = " #[derive(FromZeros, Immutable)]"] # [doc = " union MyUnion {"] # [doc = " #   variant: u8,"] # [doc = " # /*"] # [doc = "     ..."] # [doc = " # */"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " This derive performs a sophisticated, compile-time safety analysis to"] # [doc = " determine whether a type is `FromZeros`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " *This section describes what is required in order for `T: FromZeros`, and"] # [doc = " what unsafe code may assume of such types. If you don't plan on implementing"] # [doc = " `FromZeros` manually, and you don't plan on writing unsafe code that"] # [doc = " operates on `FromZeros` types, then you don't need to read this section.*"] # [doc = ""] # [doc = " If `T: FromZeros`, then unsafe code may assume that it is sound to produce a"] # [doc = " `T` whose bytes are all initialized to zero. If a type is marked as"] # [doc = " `FromZeros` which violates this contract, it may cause undefined behavior."] # [doc = ""] # [doc = " `#[derive(FromZeros)]` only permits [types which satisfy these"] # [doc = " requirements][derive-analysis]."] # [doc = ""] # [cfg_attr (feature = "derive" , doc = "[derive]: zerocopy_derive::FromZeros" , doc = "[derive-analysis]: zerocopy_derive::FromZeros#analysis")] # [cfg_attr (not (feature = "derive") , doc = concat ! ("[derive]: https://docs.rs/zerocopy/" , env ! ("CARGO_PKG_VERSION") , "/zerocopy/derive.FromZeros.html") , doc = concat ! ("[derive-analysis]: https://docs.rs/zerocopy/" , env ! ("CARGO_PKG_VERSION") , "/zerocopy/derive.FromZeros.html#analysis") ,)] # [cfg_attr (zerocopy_diagnostic_on_unimplemented_1_78_0 , diagnostic :: on_unimplemented (note = "Consider adding `#[derive(FromZeros)]` to `{Self}`"))] pub unsafe trait FromZeros : TryFromBytes { # [doc (hidden)] fn only_derive_is_allowed_to_implement_this_trait () where Self : Sized ; # [doc = " Overwrites `self` with zeros."] # [doc = ""] # [doc = " Sets every byte in `self` to 0. While this is similar to doing `*self ="] # [doc = " Self::new_zeroed()`, it differs in that `zero` does not semantically"] # [doc = " drop the current value and replace it with a new one — it simply"] # [doc = " modifies the bytes of the existing value."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use zerocopy::FromZeros;"] # [doc = " # use zerocopy_derive::*;"] # [doc = " #"] # [doc = " #[derive(FromZeros)]"] # [doc = " #[repr(C)]"] # [doc = " struct PacketHeader {"] # [doc = "     src_port: [u8; 2],"] # [doc = "     dst_port: [u8; 2],"] # [doc = "     length: [u8; 2],"] # [doc = "     checksum: [u8; 2],"] # [doc = " }"] # [doc = ""] # [doc = " let mut header = PacketHeader {"] # [doc = "     src_port: 100u16.to_be_bytes(),"] # [doc = "     dst_port: 200u16.to_be_bytes(),"] # [doc = "     length: 300u16.to_be_bytes(),"] # [doc = "     checksum: 400u16.to_be_bytes(),"] # [doc = " };"] # [doc = ""] # [doc = " header.zero();"] # [doc = ""] # [doc = " assert_eq!(header.src_port, [0, 0]);"] # [doc = " assert_eq!(header.dst_port, [0, 0]);"] # [doc = " assert_eq!(header.length, [0, 0]);"] # [doc = " assert_eq!(header.checksum, [0, 0]);"] # [doc = " ```"] # [inline (always)] fn zero (& mut self) { let slf : * mut Self = self ; let len = mem :: size_of_val (self) ; unsafe { ptr :: write_bytes (slf . cast :: < u8 > () , 0 , len) } ; } # [doc = " Creates an instance of `Self` from zeroed bytes."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use zerocopy::FromZeros;"] # [doc = " # use zerocopy_derive::*;"] # [doc = " #"] # [doc = " #[derive(FromZeros)]"] # [doc = " #[repr(C)]"] # [doc = " struct PacketHeader {"] # [doc = "     src_port: [u8; 2],"] # [doc = "     dst_port: [u8; 2],"] # [doc = "     length: [u8; 2],"] # [doc = "     checksum: [u8; 2],"] # [doc = " }"] # [doc = ""] # [doc = " let header: PacketHeader = FromZeros::new_zeroed();"] # [doc = ""] # [doc = " assert_eq!(header.src_port, [0, 0]);"] # [doc = " assert_eq!(header.dst_port, [0, 0]);"] # [doc = " assert_eq!(header.length, [0, 0]);"] # [doc = " assert_eq!(header.checksum, [0, 0]);"] # [doc = " ```"] # [must_use = "has no side effects"] # [inline (always)] fn new_zeroed () -> Self where Self : Sized , { unsafe { mem :: zeroed () } } # [doc = " Creates a `Box<Self>` from zeroed bytes."] # [doc = ""] # [doc = " This function is useful for allocating large values on the heap and"] # [doc = " zero-initializing them, without ever creating a temporary instance of"] # [doc = " `Self` on the stack. For example, `<[u8; 1048576]>::new_box_zeroed()`"] # [doc = " will allocate `[u8; 1048576]` directly on the heap; it does not require"] # [doc = " storing `[u8; 1048576]` in a temporary variable on the stack."] # [doc = ""] # [doc = " On systems that use a heap implementation that supports allocating from"] # [doc = " pre-zeroed memory, using `new_box_zeroed` (or related functions) may"] # [doc = " have performance benefits."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns an error on allocation failure. Allocation failure is guaranteed"] # [doc = " never to cause a panic or an abort."] # [must_use = "has no side effects (other than allocation)"] # [cfg (any (feature = "alloc" , test))] # [cfg_attr (doc_cfg , doc (cfg (feature = "alloc")))] # [inline] fn new_box_zeroed () -> Result < Box < Self > , AllocError > where Self : Sized , { let layout = Layout :: new :: < Self > () ; if layout . size () == 0 { return Ok (unsafe { Box :: from_raw (NonNull :: dangling () . as_ptr ()) }) ; } # [allow (clippy :: undocumented_unsafe_blocks)] let ptr = unsafe { alloc :: alloc :: alloc_zeroed (layout) . cast :: < Self > () } ; if ptr . is_null () { return Err (AllocError) ; } # [allow (clippy :: undocumented_unsafe_blocks)] Ok (unsafe { Box :: from_raw (ptr) }) } # [doc = " Creates a `Box<[Self]>` (a boxed slice) from zeroed bytes."] # [doc = ""] # [doc = " This function is useful for allocating large values of `[Self]` on the"] # [doc = " heap and zero-initializing them, without ever creating a temporary"] # [doc = " instance of `[Self; _]` on the stack. For example,"] # [doc = " `u8::new_box_slice_zeroed(1048576)` will allocate the slice directly on"] # [doc = " the heap; it does not require storing the slice on the stack."] # [doc = ""] # [doc = " On systems that use a heap implementation that supports allocating from"] # [doc = " pre-zeroed memory, using `new_box_slice_zeroed` may have performance"] # [doc = " benefits."] # [doc = ""] # [doc = " If `Self` is a zero-sized type, then this function will return a"] # [doc = " `Box<[Self]>` that has the correct `len`. Such a box cannot contain any"] # [doc = " actual information, but its `len()` property will report the correct"] # [doc = " value."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns an error on allocation failure. Allocation failure is"] # [doc = " guaranteed never to cause a panic or an abort."] # [must_use = "has no side effects (other than allocation)"] # [cfg (feature = "alloc")] # [cfg_attr (doc_cfg , doc (cfg (feature = "alloc")))] # [inline] fn new_box_zeroed_with_elems (count : usize) -> Result < Box < Self > , AllocError > where Self : KnownLayout < PointerMetadata = usize > , { unsafe { crate :: util :: new_box (count , alloc :: alloc :: alloc_zeroed) } } # [deprecated (since = "0.8.0" , note = "renamed to `FromZeros::new_box_zeroed_with_elems`")] # [doc (hidden)] # [cfg (feature = "alloc")] # [cfg_attr (doc_cfg , doc (cfg (feature = "alloc")))] # [must_use = "has no side effects (other than allocation)"] # [inline (always)] fn new_box_slice_zeroed (len : usize) -> Result < Box < [Self] > , AllocError > where Self : Sized , { < [Self] > :: new_box_zeroed_with_elems (len) } # [doc = " Creates a `Vec<Self>` from zeroed bytes."] # [doc = ""] # [doc = " This function is useful for allocating large values of `Vec`s and"] # [doc = " zero-initializing them, without ever creating a temporary instance of"] # [doc = " `[Self; _]` (or many temporary instances of `Self`) on the stack. For"] # [doc = " example, `u8::new_vec_zeroed(1048576)` will allocate directly on the"] # [doc = " heap; it does not require storing intermediate values on the stack."] # [doc = ""] # [doc = " On systems that use a heap implementation that supports allocating from"] # [doc = " pre-zeroed memory, using `new_vec_zeroed` may have performance benefits."] # [doc = ""] # [doc = " If `Self` is a zero-sized type, then this function will return a"] # [doc = " `Vec<Self>` that has the correct `len`. Such a `Vec` cannot contain any"] # [doc = " actual information, but its `len()` property will report the correct"] # [doc = " value."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns an error on allocation failure. Allocation failure is"] # [doc = " guaranteed never to cause a panic or an abort."] # [must_use = "has no side effects (other than allocation)"] # [cfg (feature = "alloc")] # [cfg_attr (doc_cfg , doc (cfg (feature = "alloc")))] # [inline (always)] fn new_vec_zeroed (len : usize) -> Result < Vec < Self > , AllocError > where Self : Sized , { < [Self] > :: new_box_zeroed_with_elems (len) . map (Into :: into) } # [doc = " Extends a `Vec<Self>` by pushing `additional` new items onto the end of"] # [doc = " the vector. The new items are initialized with zeros."] # [cfg (zerocopy_panic_in_const_and_vec_try_reserve_1_57_0)] # [cfg (feature = "alloc")] # [cfg_attr (doc_cfg , doc (cfg (all (rust = "1.57.0" , feature = "alloc"))))] # [inline (always)] fn extend_vec_zeroed (v : & mut Vec < Self > , additional : usize) -> Result < () , AllocError > where Self : Sized , { < Self as FromZeros > :: insert_vec_zeroed (v , v . len () , additional) } # [doc = " Inserts `additional` new items into `Vec<Self>` at `position`. The new"] # [doc = " items are initialized with zeros."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `position > v.len()`."] # [cfg (zerocopy_panic_in_const_and_vec_try_reserve_1_57_0)] # [cfg (feature = "alloc")] # [cfg_attr (doc_cfg , doc (cfg (all (rust = "1.57.0" , feature = "alloc"))))] # [inline] fn insert_vec_zeroed (v : & mut Vec < Self > , position : usize , additional : usize ,) -> Result < () , AllocError > where Self : Sized , { assert ! (position <= v . len ()) ; v . try_reserve (additional) . map_err (| _ | AllocError) ? ; unsafe { let ptr = v . as_mut_ptr () ; # [allow (clippy :: arithmetic_side_effects)] ptr . add (position) . copy_to (ptr . add (position + additional) , v . len () - position) ; ptr . add (position) . write_bytes (0 , additional) ; # [allow (clippy :: arithmetic_side_effects)] v . set_len (v . len () + additional) ; } Ok (()) } }
    };
}

FromZeros!()