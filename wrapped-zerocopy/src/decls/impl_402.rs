macro_rules! deps {
    () => {
        Shared!();
        Aligned!();
        Split!();
        IntoBytes!();
        Valid!();
        KnownLayout!();
        FromBytes!();
        Immutable!();
        Unaligned!();
        SplitAt!();
    };
}

macro_rules! impl_402 {
    () => {
        deps!();
        impl < 'a , T > Split < & 'a T > where T : ? Sized + SplitAt , { # [inline (always)] fn into_ptr (self) -> Split < Ptr < 'a , T , (Shared , Aligned , Valid) > > { let source = Ptr :: from_ref (self . source) ; unsafe { Split :: new (source , self . l_len) } } # [doc = " Produces the split parts of `self`, using [`Immutable`] to ensure that"] # [doc = " it is sound to have concurrent references to both parts."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use zerocopy::{SplitAt, FromBytes};"] # [doc = " # use zerocopy_derive::*;"] # [doc = ""] # [doc = " #[derive(SplitAt, FromBytes, KnownLayout, Immutable)]"] # [doc = " #[repr(C)]"] # [doc = " struct Packet {"] # [doc = "     length: u8,"] # [doc = "     body: [u8],"] # [doc = " }"] # [doc = ""] # [doc = " // These bytes encode a `Packet`."] # [doc = " let bytes = &[4, 1, 2, 3, 4, 5, 6, 7, 8, 9][..];"] # [doc = ""] # [doc = " let packet = Packet::ref_from_bytes(bytes).unwrap();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4, 5, 6, 7, 8, 9]);"] # [doc = ""] # [doc = " // Attempt to split `packet` at `length`."] # [doc = " let split = packet.split_at(packet.length as usize).unwrap();"] # [doc = ""] # [doc = " // Use the `Immutable` bound on `Packet` to prove that it's okay to"] # [doc = " // return concurrent references to `packet` and `rest`."] # [doc = " let (packet, rest) = split.via_immutable();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4]);"] # [doc = " assert_eq!(rest, [5, 6, 7, 8, 9]);"] # [doc = " ```"] # [must_use = "has no side effects"] # [inline (always)] pub fn via_immutable (self) -> (& 'a T , & 'a [T :: Elem]) where T : Immutable , { let (l , r) = self . into_ptr () . via_immutable () ; (l . as_ref () , r . as_ref ()) } # [doc = " Produces the split parts of `self`, using [`IntoBytes`] to ensure that"] # [doc = " it is sound to have concurrent references to both parts."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use zerocopy::{SplitAt, FromBytes};"] # [doc = " # use zerocopy_derive::*;"] # [doc = ""] # [doc = " #[derive(SplitAt, FromBytes, KnownLayout, Immutable, IntoBytes)]"] # [doc = " #[repr(C)]"] # [doc = " struct Packet<B: ?Sized> {"] # [doc = "     length: u8,"] # [doc = "     body: B,"] # [doc = " }"] # [doc = ""] # [doc = " // These bytes encode a `Packet`."] # [doc = " let bytes = &[4, 1, 2, 3, 4, 5, 6, 7, 8, 9][..];"] # [doc = ""] # [doc = " let packet = Packet::<[u8]>::ref_from_bytes(bytes).unwrap();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4, 5, 6, 7, 8, 9]);"] # [doc = ""] # [doc = " // Attempt to split `packet` at `length`."] # [doc = " let split = packet.split_at(packet.length as usize).unwrap();"] # [doc = ""] # [doc = " // Use the `IntoBytes` bound on `Packet` to prove that it's okay to"] # [doc = " // return concurrent references to `packet` and `rest`."] # [doc = " let (packet, rest) = split.via_into_bytes();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4]);"] # [doc = " assert_eq!(rest, [5, 6, 7, 8, 9]);"] # [doc = " ```"] # [must_use = "has no side effects"] # [inline (always)] pub fn via_into_bytes (self) -> (& 'a T , & 'a [T :: Elem]) where T : IntoBytes , { let (l , r) = self . into_ptr () . via_into_bytes () ; (l . as_ref () , r . as_ref ()) } # [doc = " Produces the split parts of `self`, using [`Unaligned`] to ensure that"] # [doc = " it is sound to have concurrent references to both parts."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use zerocopy::{SplitAt, FromBytes};"] # [doc = " # use zerocopy_derive::*;"] # [doc = ""] # [doc = " #[derive(SplitAt, FromBytes, KnownLayout, Immutable, Unaligned)]"] # [doc = " #[repr(C)]"] # [doc = " struct Packet {"] # [doc = "     length: u8,"] # [doc = "     body: [u8],"] # [doc = " }"] # [doc = ""] # [doc = " // These bytes encode a `Packet`."] # [doc = " let bytes = &[4, 1, 2, 3, 4, 5, 6, 7, 8, 9][..];"] # [doc = ""] # [doc = " let packet = Packet::ref_from_bytes(bytes).unwrap();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4, 5, 6, 7, 8, 9]);"] # [doc = ""] # [doc = " // Attempt to split `packet` at `length`."] # [doc = " let split = packet.split_at(packet.length as usize).unwrap();"] # [doc = ""] # [doc = " // Use the `Unaligned` bound on `Packet` to prove that it's okay to"] # [doc = " // return concurrent references to `packet` and `rest`."] # [doc = " let (packet, rest) = split.via_unaligned();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [1, 2, 3, 4]);"] # [doc = " assert_eq!(rest, [5, 6, 7, 8, 9]);"] # [doc = " ```"] # [must_use = "has no side effects"] # [inline (always)] pub fn via_unaligned (self) -> (& 'a T , & 'a [T :: Elem]) where T : Unaligned , { let (l , r) = self . into_ptr () . via_unaligned () ; (l . as_ref () , r . as_ref ()) } # [doc = " Produces the split parts of `self`, using a dynamic check to ensure that"] # [doc = " it is sound to have concurrent references to both parts. You should"] # [doc = " prefer using [`Self::via_immutable`], [`Self::via_into_bytes`], or"] # [doc = " [`Self::via_unaligned`], which have no runtime cost."] # [doc = ""] # [doc = " Note that this check is overly conservative if `T` is [`Immutable`]; for"] # [doc = " some types, this check will reject some splits which"] # [doc = " [`Self::via_immutable`] will accept."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use zerocopy::{SplitAt, FromBytes, IntoBytes, network_endian::U16};"] # [doc = " # use zerocopy_derive::*;"] # [doc = ""] # [doc = " #[derive(SplitAt, FromBytes, KnownLayout, Immutable, Debug)]"] # [doc = " #[repr(C, align(2))]"] # [doc = " struct Packet {"] # [doc = "     length: U16,"] # [doc = "     body: [u8],"] # [doc = " }"] # [doc = ""] # [doc = " // These bytes encode a `Packet`."] # [doc = " let bytes = ["] # [doc = "     4u16.to_be(),"] # [doc = "     1u16.to_be(),"] # [doc = "     2u16.to_be(),"] # [doc = "     3u16.to_be(),"] # [doc = "     4u16.to_be()"] # [doc = " ];"] # [doc = ""] # [doc = " let packet = Packet::ref_from_bytes(bytes.as_bytes()).unwrap();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [0, 1, 0, 2, 0, 3, 0, 4]);"] # [doc = ""] # [doc = " // Attempt to split `packet` at `length`."] # [doc = " let split = packet.split_at(packet.length.into()).unwrap();"] # [doc = ""] # [doc = " // Use a dynamic check to prove that it's okay to return concurrent"] # [doc = " // references to `packet` and `rest`."] # [doc = " let (packet, rest) = split.via_runtime_check().unwrap();"] # [doc = ""] # [doc = " assert_eq!(packet.length, 4);"] # [doc = " assert_eq!(packet.body, [0, 1, 0, 2]);"] # [doc = " assert_eq!(rest, [0, 3, 0, 4]);"] # [doc = ""] # [doc = " // Attempt to split `packet` at `length - 1`."] # [doc = " let idx = packet.length.get() - 1;"] # [doc = " let split = packet.split_at(idx as usize).unwrap();"] # [doc = ""] # [doc = " // Attempt (and fail) to use a dynamic check to prove that it's okay"] # [doc = " // to return concurrent references to `packet` and `rest`. Note that"] # [doc = " // this is a case of `via_runtime_check` being overly conservative."] # [doc = " // Although the left and right parts indeed overlap, the `Immutable`"] # [doc = " // bound ensures that concurrently referencing these overlapping"] # [doc = " // parts is sound."] # [doc = " assert!(split.via_runtime_check().is_err());"] # [doc = " ```"] # [must_use = "has no side effects"] # [inline (always)] pub fn via_runtime_check (self) -> Result < (& 'a T , & 'a [T :: Elem]) , Self > { match self . into_ptr () . via_runtime_check () { Ok ((l , r)) => Ok ((l . as_ref () , r . as_ref ())) , Err (s) => Err (s . into_ref ()) , } } # [doc = " Unsafely produces the split parts of `self`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If `T` permits interior mutation, the trailing padding bytes of the left"] # [doc = " portion must not overlap the right portion. For some dynamically sized"] # [doc = " types, the padding that appears after the trailing slice field [is a"] # [doc = " dynamic function of the trailing slice"] # [doc = " length](KnownLayout#slice-dst-layout). Thus, for some types, this"] # [doc = " condition is dependent on the length of the left portion."] # [must_use = "has no side effects"] # [inline (always)] pub unsafe fn via_unchecked (self) -> (& 'a T , & 'a [T :: Elem]) { let (l , r) = unsafe { self . into_ptr () . via_unchecked () } ; (l . as_ref () , r . as_ref ()) } }
    };
}

impl_402!();