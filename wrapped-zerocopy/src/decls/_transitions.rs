macro_rules! deps {
    () => {
        TryFromBytes!();
        TryTransmuteFromPtr!();
        Read!();
        Invariants!();
        Exclusive!();
        Aliasing!();
        Valid!();
        FromBytes!();
        Alignment!();
        IntoBytes!();
        AlignmentError!();
        Initialized!();
        Unaligned!();
        Shared!();
        Reference!();
        Validity!();
        Aligned!();
        Immutable!();
        ValidityError!();
    };
}

macro_rules! _transitions {
    () => {
        deps!();
        # [doc = " State transitions between invariants."] mod _transitions { use super :: * ; use crate :: pointer :: transmute :: TryTransmuteFromPtr ; impl < 'a , T , I > Ptr < 'a , T , I > where T : 'a + ? Sized , I : Invariants , { # [doc = " Returns a `Ptr` with [`Exclusive`] aliasing if `self` already has"] # [doc = " `Exclusive` aliasing, or generates a compile-time assertion failure."] # [doc = ""] # [doc = " This allows code which is generic over aliasing to down-cast to a"] # [doc = " concrete aliasing."] # [doc = ""] # [doc = " [`Exclusive`]: crate::pointer::invariant::Exclusive"] # [inline] pub (crate) fn into_exclusive_or_pme (self ,) -> Ptr < 'a , T , (Exclusive , I :: Alignment , I :: Validity) > { trait AliasingExt : Aliasing { const IS_EXCL : bool ; } impl < A : Aliasing > AliasingExt for A { const IS_EXCL : bool = { const_assert ! (Self :: IS_EXCLUSIVE) ; true } ; } assert ! (I :: Aliasing :: IS_EXCL) ; unsafe { self . assume_exclusive () } } # [doc = " Assumes that `self` satisfies the invariants `H`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises that `self` satisfies the invariants `H`."] unsafe fn assume_invariants < H : Invariants > (self) -> Ptr < 'a , T , H > { unsafe { Ptr :: from_inner (self . as_inner ()) } } # [doc = " Helps the type system unify two distinct invariant types which are"] # [doc = " actually the same."] pub (crate) fn unify_invariants < H : Invariants < Aliasing = I :: Aliasing , Alignment = I :: Alignment , Validity = I :: Validity > , > (self ,) -> Ptr < 'a , T , H > { unsafe { self . assume_invariants :: < H > () } } # [doc = " Assumes that `self` satisfies the aliasing requirement of `A`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises that `self` satisfies the aliasing requirement"] # [doc = " of `A`."] # [inline] pub (crate) unsafe fn assume_aliasing < A : Aliasing > (self ,) -> Ptr < 'a , T , (A , I :: Alignment , I :: Validity) > { unsafe { self . assume_invariants () } } # [doc = " Assumes `self` satisfies the aliasing requirement of [`Exclusive`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises that `self` satisfies the aliasing requirement"] # [doc = " of `Exclusive`."] # [doc = ""] # [doc = " [`Exclusive`]: crate::pointer::invariant::Exclusive"] # [inline] pub (crate) unsafe fn assume_exclusive (self ,) -> Ptr < 'a , T , (Exclusive , I :: Alignment , I :: Validity) > { unsafe { self . assume_aliasing :: < Exclusive > () } } # [doc = " Assumes that `self`'s referent is validly-aligned for `T` if"] # [doc = " required by `A`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises that `self`'s referent conforms to the alignment"] # [doc = " invariant of `T` if required by `A`."] # [inline] pub (crate) unsafe fn assume_alignment < A : Alignment > (self ,) -> Ptr < 'a , T , (I :: Aliasing , A , I :: Validity) > { unsafe { self . assume_invariants () } } # [doc = " Checks the `self`'s alignment at runtime, returning an aligned `Ptr`"] # [doc = " on success."] pub (crate) fn try_into_aligned (self ,) -> Result < Ptr < 'a , T , (I :: Aliasing , Aligned , I :: Validity) > , AlignmentError < Self , T > > where T : Sized , { if let Err (err) = crate :: util :: validate_aligned_to :: < _ , T > (self . as_inner () . as_non_null ()) { return Err (err . with_src (self)) ; } Ok (unsafe { self . assume_alignment :: < Aligned > () }) } # [doc = " Recalls that `self`'s referent is validly-aligned for `T`."] # [inline] pub (crate) fn bikeshed_recall_aligned (self ,) -> Ptr < 'a , T , (I :: Aliasing , Aligned , I :: Validity) > where T : crate :: Unaligned , { unsafe { self . assume_alignment :: < Aligned > () } } # [doc = " Assumes that `self`'s referent conforms to the validity requirement"] # [doc = " of `V`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises that `self`'s referent conforms to the validity"] # [doc = " requirement of `V`."] # [doc (hidden)] # [must_use] # [inline] pub unsafe fn assume_validity < V : Validity > (self ,) -> Ptr < 'a , T , (I :: Aliasing , I :: Alignment , V) > { unsafe { self . assume_invariants () } } # [doc = " A shorthand for `self.assume_validity<invariant::Initialized>()`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises to uphold the safety preconditions of"] # [doc = " `self.assume_validity<invariant::Initialized>()`."] # [doc (hidden)] # [must_use] # [inline] pub unsafe fn assume_initialized (self ,) -> Ptr < 'a , T , (I :: Aliasing , I :: Alignment , Initialized) > { unsafe { self . assume_validity :: < Initialized > () } } # [doc = " A shorthand for `self.assume_validity<Valid>()`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller promises to uphold the safety preconditions of"] # [doc = " `self.assume_validity<Valid>()`."] # [doc (hidden)] # [must_use] # [inline] pub unsafe fn assume_valid (self) -> Ptr < 'a , T , (I :: Aliasing , I :: Alignment , Valid) > { unsafe { self . assume_validity :: < Valid > () } } # [doc = " Recalls that `self`'s referent is initialized."] # [doc (hidden)] # [must_use] # [inline] pub fn bikeshed_recall_initialized_from_bytes (self ,) -> Ptr < 'a , T , (I :: Aliasing , I :: Alignment , Initialized) > where T : crate :: IntoBytes + crate :: FromBytes , I : Invariants < Validity = Valid > , { unsafe { self . assume_initialized () } } # [doc = " Recalls that `self`'s referent is initialized."] # [doc (hidden)] # [must_use] # [inline] pub fn bikeshed_recall_initialized_immutable (self ,) -> Ptr < 'a , T , (Shared , I :: Alignment , Initialized) > where T : crate :: IntoBytes + crate :: Immutable , I : Invariants < Aliasing = Shared , Validity = Valid > , { unsafe { self . assume_initialized () } } # [doc = " Checks that `self`'s referent is validly initialized for `T`,"] # [doc = " returning a `Ptr` with `Valid` on success."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This method will panic if"] # [doc = " [`T::is_bit_valid`][TryFromBytes::is_bit_valid] panics."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " On error, unsafe code may rely on this method's returned"] # [doc = " `ValidityError` containing `self`."] # [inline] pub (crate) fn try_into_valid < R , S > (mut self ,) -> Result < Ptr < 'a , T , (I :: Aliasing , I :: Alignment , Valid) > , ValidityError < Self , T > > where T : TryFromBytes + Read < I :: Aliasing , R > + TryTransmuteFromPtr < T , I :: Aliasing , I :: Validity , Valid , S > , I :: Aliasing : Reference , I : Invariants < Validity = Initialized > , { if T :: is_bit_valid (self . reborrow () . forget_aligned ()) { Ok (unsafe { self . assume_valid () }) } else { Err (ValidityError :: new (self)) } } # [doc = " Forgets that `self`'s referent is validly-aligned for `T`."] # [doc (hidden)] # [must_use] # [inline] pub fn forget_aligned (self) -> Ptr < 'a , T , (I :: Aliasing , Unaligned , I :: Validity) > { unsafe { self . assume_invariants () } } } }
    };
}

_transitions!();