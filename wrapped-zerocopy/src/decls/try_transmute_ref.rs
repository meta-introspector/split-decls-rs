macro_rules! deps {
    () => {
        IntoBytes!();
        Alignment!();
        Immutable!();
        TryFromBytes!();
    };
}

macro_rules! try_transmute_ref {
    () => {
        deps!();
        # [doc = " Conditionally transmutes a mutable or immutable reference of one type to an"] # [doc = " immutable reference of another type of the same size and compatible"] # [doc = " alignment."] # [doc = ""] # [doc = " This macro behaves like an invocation of this function:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " fn try_transmute_ref<Src, Dst>(src: &Src) -> Result<&Dst, ValidityError<&Src, Dst>>"] # [doc = " where"] # [doc = "     Src: IntoBytes + Immutable,"] # [doc = "     Dst: TryFromBytes + Immutable,"] # [doc = "     size_of::<Src>() == size_of::<Dst>(),"] # [doc = "     align_of::<Src>() >= align_of::<Dst>(),"] # [doc = " {"] # [doc = " # /*"] # [doc = "     ..."] # [doc = " # */"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " However, unlike a function, this macro can only be invoked when the types of"] # [doc = " `Src` and `Dst` are completely concrete. The types `Src` and `Dst` are"] # [doc = " inferred from the calling context; they cannot be explicitly specified in"] # [doc = " the macro invocation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use zerocopy::*;"] # [doc = " // 0u8 → bool = false"] # [doc = " assert_eq!(try_transmute_ref!(&0u8), Ok(&false));"] # [doc = ""] # [doc = " // 1u8 → bool = true"] # [doc = "  assert_eq!(try_transmute_ref!(&1u8), Ok(&true));"] # [doc = ""] # [doc = " // 2u8 → bool = error"] # [doc = " assert!(matches!("] # [doc = "     try_transmute_ref!(&2u8),"] # [doc = "     Result::<&bool, _>::Err(ValidityError { .. })"] # [doc = " ));"] # [doc = " ```"] # [doc = ""] # [doc = " # Alignment increase error message"] # [doc = ""] # [doc = " Because of limitations on macros, the error message generated when"] # [doc = " `try_transmute_ref!` is used to transmute from a type of lower alignment to"] # [doc = " a type of higher alignment is somewhat confusing. For example, the following"] # [doc = " code:"] # [doc = ""] # [doc = " ```compile_fail"] # [doc = " let increase_alignment: Result<&u16, _> = zerocopy::try_transmute_ref!(&[0u8; 2]);"] # [doc = " ```"] # [doc = ""] # [doc = " ...generates the following error:"] # [doc = ""] # [doc = " ```text"] # [doc = " error[E0512]: cannot transmute between types of different sizes, or dependently-sized types"] # [doc = "  --> example.rs:1:47"] # [doc = "   |"] # [doc = " 1 |     let increase_alignment: Result<&u16, _> = zerocopy::try_transmute_ref!(&[0u8; 2]);"] # [doc = "   |                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^"] # [doc = "   |"] # [doc = "   = note: source type: `AlignOf<[u8; 2]>` (8 bits)"] # [doc = "   = note: target type: `MaxAlignsOf<[u8; 2], u16>` (16 bits)"] # [doc = "   = note: this error originates in the macro `$crate::assert_align_gt_eq` which comes from the expansion of the macro `zerocopy::try_transmute_ref` (in Nightly builds, run with -Z macro-backtrace for more info)/// ```"] # [doc = " ```"] # [doc = ""] # [doc = " This is saying that `max(align_of::<T>(), align_of::<U>()) !="] # [doc = " align_of::<T>()`, which is equivalent to `align_of::<T>() <"] # [doc = " align_of::<U>()`."] # [macro_export] macro_rules ! try_transmute_ref { ($ e : expr) => { { let e : & _ = $ e ; # [allow (unreachable_code , unused , clippy :: diverging_sub_expression)] if false { let mut t = loop { } ; e = & t ; let u ; $ crate :: assert_size_eq ! (t , u) ; $ crate :: assert_align_gt_eq ! (t , u) ; Ok (& u) } else { $ crate :: util :: macro_util :: try_transmute_ref ::< _ , _ > (e) } } } }
    };
}

try_transmute_ref!()