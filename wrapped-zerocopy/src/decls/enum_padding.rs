macro_rules! enum_padding {
    () => {
        # [doc = " How many padding bytes does the enum type `$t` have?"] # [doc = ""] # [doc = " `$disc` is the type of the enum tag, and `$ts` is a list of fields in each"] # [doc = " square-bracket-delimited variant. `$t` must be an enum, or else"] # [doc = " `enum_padding!`'s result may be meaningless. An enum has padding if any of"] # [doc = " its variant structs [1][2] contain padding, and so all of the variants of an"] # [doc = " enum must be \"full\" in order for the enum to not have padding."] # [doc = ""] # [doc = " The results of `enum_padding!` require that the enum is not `repr(Rust)`, as"] # [doc = " `repr(Rust)` enums may niche the enum's tag and reduce the total number of"] # [doc = " bytes required to represent the enum as a result. As long as the enum is"] # [doc = " `repr(C)`, `repr(int)`, or `repr(C, int)`, this will consistently return"] # [doc = " whether the enum contains any padding bytes."] # [doc = ""] # [doc = " [1]: https://doc.rust-lang.org/1.81.0/reference/type-layout.html#reprc-enums-with-fields"] # [doc = " [2]: https://doc.rust-lang.org/1.81.0/reference/type-layout.html#primitive-representation-of-enums-with-fields"] # [doc (hidden)] # [macro_export] macro_rules ! enum_padding { ($ t : ty , $ disc : ty , $ ([$ ($ ts : ty) ,*]) ,*) => { { let mut max = 0 ; $ ({ let padding = $ crate :: util :: macro_util :: size_of ::<$ t > () - ($ crate :: util :: macro_util :: size_of ::<$ disc > () $ (+ $ crate :: util :: macro_util :: size_of ::<$ ts > ()) *) ; if padding > max { max = padding ; } }) * max } } ; }
    };
}

enum_padding!();