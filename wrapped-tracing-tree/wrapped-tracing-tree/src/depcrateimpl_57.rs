// Generated macro for impl_57 (impl)
macro_rules! Depcrateimpl_57 {
() => {
// Module: crate
// Provides: {"impl_57"}
// Dependencies: {}
impl < W , FT > HierarchicalLayer < W , FT > where W : for < 'writer > MakeWriter < 'writer > + 'static , FT : FormatTime , { # [doc = " Enables terminal colors, boldness and italics."] pub fn with_ansi (self , ansi : bool) -> Self { Self { config : self . config . with_ansi (ansi) , .. self } } pub fn with_writer < W2 > (self , make_writer : W2) -> HierarchicalLayer < W2 , FT > where W2 : for < 'writer > MakeWriter < 'writer > , { HierarchicalLayer { make_writer , config : self . config , bufs : self . bufs , timer : self . timer , } } pub fn with_indent_amount (self , indent_amount : usize) -> Self { let config = Config { indent_amount , .. self . config } ; Self { config , .. self } } # [doc = " Renders an ascii art tree instead of just using whitespace indentation."] pub fn with_indent_lines (self , indent_lines : bool) -> Self { Self { config : self . config . with_indent_lines (indent_lines) , .. self } } # [doc = " Specifies how to measure and format time at which event has occurred."] pub fn with_timer < FT2 : FormatTime > (self , timer : FT2) -> HierarchicalLayer < W , FT2 > { HierarchicalLayer { make_writer : self . make_writer , config : self . config , bufs : self . bufs , timer , } } # [doc = " Whether to render the event and span targets. Usually targets are the module path to the"] # [doc = " event/span macro invocation."] pub fn with_targets (self , targets : bool) -> Self { Self { config : self . config . with_targets (targets) , .. self } } # [doc = " Whether to render the thread id in the beginning of every line. This is helpful to"] # [doc = " untangle the tracing statements emitted by each thread."] pub fn with_thread_ids (self , thread_ids : bool) -> Self { Self { config : self . config . with_thread_ids (thread_ids) , .. self } } # [doc = " Whether to render the thread name in the beginning of every line. Not all threads have"] # [doc = " names, but if they do, this may be more helpful than the generic thread ids."] pub fn with_thread_names (self , thread_names : bool) -> Self { Self { config : self . config . with_thread_names (thread_names) , .. self } } # [doc = " Resets the indentation to zero after `wraparound` indentation levels."] # [doc = " This is helpful if you expect very deeply nested spans as otherwise the indentation"] # [doc = " just runs out of your screen."] pub fn with_wraparound (self , wraparound : usize) -> Self { Self { config : self . config . with_wraparound (wraparound) , .. self } } # [doc = " Whether to print the currently active span's message again before entering a new span."] # [doc = " This helps if the entry to the current span was quite a while back (and with scrolling"] # [doc = " upwards in logs)."] pub fn with_verbose_entry (self , verbose_entry : bool) -> Self { Self { config : self . config . with_verbose_entry (verbose_entry) , .. self } } # [doc = " Whether to print the currently active span's message again before dropping it."] # [doc = " This helps if the entry to the current span was quite a while back (and with scrolling"] # [doc = " upwards in logs)."] pub fn with_verbose_exit (self , verbose_exit : bool) -> Self { Self { config : self . config . with_verbose_exit (verbose_exit) , .. self } } # [doc = " Whether to print the currently active span's message again if another span was entered in"] # [doc = " the meantime"] # [doc = " This helps during concurrent or multi-threaded events where threads are entered, but not"] # [doc = " necessarily *exited* before other *divergent* spans are entered and generating events."] pub fn with_span_retrace (self , enabled : bool) -> Self { Self { config : self . config . with_span_retrace (enabled) , .. self } } # [doc = " Defers printing span opening until an event is generated within the span."] # [doc = ""] # [doc = " Avoids printing empty spans with no generated events."] pub fn with_deferred_spans (self , enabled : bool) -> Self { Self { config : self . config . with_deferred_spans (enabled) , .. self } } # [doc = " Prefixes each branch with the event mode, such as `open`, or `close`"] pub fn with_span_modes (self , enabled : bool) -> Self { Self { config : self . config . with_span_modes (enabled) , .. self } } # [doc = " Whether to print `{}` around the fields when printing a span."] # [doc = " This can help visually distinguish fields from the rest of the message."] pub fn with_bracketed_fields (self , bracketed_fields : bool) -> Self { Self { config : self . config . with_bracketed_fields (bracketed_fields) , .. self } } fn styled (& self , style : Style , text : impl AsRef < str >) -> String { styled (self . config . ansi , style , text) } fn print_kvs < 'a , I , V > (& self , buf : & mut impl fmt :: Write , kvs : I) -> fmt :: Result where I : IntoIterator < Item = (& 'a str , V) > , V : fmt :: Display + 'a , { let mut kvs = kvs . into_iter () ; if let Some ((k , v)) = kvs . next () { if k == "message" { write ! (buf , "{}" , v) ? ; } else { write ! (buf , "{}={}" , k , v) ? ; } } for (k , v) in kvs { write ! (buf , ", {}={}" , k , v) ? ; } Ok (()) } # [doc = " Ensures that `new_span` and all its ancestors are properly printed before an event"] fn write_retrace_span < 'a , S > (& self , new_span : & SpanRef < 'a , S > , bufs : & mut Buffers , ctx : & 'a Context < S > , pre_open : bool ,) where S : Subscriber + for < 'new_span > LookupSpan < 'new_span > , { let old_span_id = bufs . current_span . replace ((new_span . id ()) . clone ()) ; let old_span_id = old_span_id . as_ref () ; let new_span_id = new_span . id () ; if Some (& new_span_id) != old_span_id { let old_span = old_span_id . as_ref () . and_then (| v | ctx . span (v)) ; let old_path = old_span . as_ref () . map (scope_path) . into_iter () . flatten () ; let new_path = scope_path (new_span) ; let new_path = DifferenceIter :: new (old_path , new_path , | v | v . id ()) ; for (i , span) in new_path . enumerate () { let was_written = if let Some (data) = span . extensions_mut () . get_mut :: < Data > () { mem :: replace (& mut data . written , true) } else { false } ; let mut verbose = false ; if i == 0 && pre_open { if let Some (span) = span . parent () { verbose = true ; self . write_span_info (& span , bufs , SpanMode :: PreOpen) ; } } self . write_span_info (& span , bufs , if was_written { SpanMode :: Retrace { verbose } } else { SpanMode :: Open { verbose } } ,) } } } fn write_span_info < S > (& self , span : & SpanRef < S > , bufs : & mut Buffers , style : SpanMode) where S : Subscriber + for < 'span > LookupSpan < 'span > , { let ext = span . extensions () ; let data = ext . get :: < Data > () . expect ("span does not have data") ; let mut current_buf = & mut bufs . current_buf ; if self . config . span_modes { write_span_mode (current_buf , style) } let indent = scope_path (span) . skip (1) . count () ; let should_write = match style { SpanMode :: Open { .. } | SpanMode :: Event => true , SpanMode :: PreOpen if self . config . verbose_entry => true , SpanMode :: Close { verbose } => verbose , SpanMode :: Retrace { .. } => true , SpanMode :: PostClose => true , _ => false , } ; if should_write { if self . config . targets { let target = span . metadata () . target () ; write ! (& mut current_buf , "{}::" , self . styled (Style :: new () . dimmed () , target ,) ,) . expect ("Unable to write to buffer") ; } write ! (current_buf , "{name}" , name = self . styled (Style :: new () . fg (Color :: Green) . bold () , span . metadata () . name ())) . unwrap () ; if self . config . bracketed_fields { write ! (current_buf , "{}" , self . styled (Style :: new () . fg (Color :: Green) . bold () , "{")) . unwrap () ; } else { write ! (current_buf , " ") . unwrap () ; } self . print_kvs (& mut current_buf , data . kvs . iter () . map (| (k , v) | (* k , v))) . unwrap () ; if self . config . bracketed_fields { write ! (current_buf , "{}" , self . styled (Style :: new () . fg (Color :: Green) . bold () , "}")) . unwrap () ; } } bufs . indent_current (indent , & self . config , style) ; let writer = self . make_writer . make_writer () ; bufs . flush_current_buf (writer) } fn write_timestamp < S > (& self , span : SpanRef < S > , buf : & mut String) where S : Subscriber + for < 'span > LookupSpan < 'span > , { let ext = span . extensions () ; let data = ext . get :: < Data > () . expect ("Data cannot be found in extensions") ; self . timer . style_timestamp (self . config . ansi , data . start . elapsed () , buf) . unwrap () } fn is_recursive () -> Option < RecursiveGuard > { thread_local ! { pub static IS_EMPTY : AtomicBool = const { AtomicBool :: new (true) } ; } IS_EMPTY . with (| is_empty | { is_empty . compare_exchange (true , false , Ordering :: Relaxed , Ordering :: Relaxed) . ok () . map (| _ | RecursiveGuard (& IS_EMPTY)) }) } }
};
}
