macro_rules! smart_display {
    () => {
        # [doc = " Macros for `SmartDisplay`."] mod smart_display { pub (crate) mod delegate ; pub (crate) mod private_metadata ; }
    };
}

smart_display!();