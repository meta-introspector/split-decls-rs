// Generated macro for SignatureVerificationAlgorithm (trait)
macro_rules! DepcrateSignatureVerificationAlgorithm {
() => {
// Module: crate
// Provides: {"SignatureVerificationAlgorithm"}
// Dependencies: {}
# [doc = " An abstract signature verification algorithm."] # [doc = ""] # [doc = " One of these is needed per supported pair of public key type (identified"] # [doc = " with `public_key_alg_id()`) and `signatureAlgorithm` (identified with"] # [doc = " `signature_alg_id()`).  Note that both of these `AlgorithmIdentifier`s include"] # [doc = " the parameters encoding, so separate `SignatureVerificationAlgorithm`s are needed"] # [doc = " for each possible public key or signature parameters."] # [doc = ""] # [doc = " Debug implementations should list the public key algorithm identifier and"] # [doc = " signature algorithm identifier in human friendly form (i.e. not encoded bytes),"] # [doc = " along with the name of the implementing library (to distinguish different"] # [doc = " implementations of the same algorithms)."] pub trait SignatureVerificationAlgorithm : Send + Sync + fmt :: Debug { # [doc = " Verify a signature."] # [doc = ""] # [doc = " `public_key` is the `subjectPublicKey` value from a `SubjectPublicKeyInfo` encoding"] # [doc = " and is untrusted.  The key's `subjectPublicKeyInfo` matches the [`AlgorithmIdentifier`]"] # [doc = " returned by `public_key_alg_id()`."] # [doc = ""] # [doc = " `message` is the data over which the signature was allegedly computed."] # [doc = " It is not hashed; implementations of this trait function must do hashing"] # [doc = " if that is required by the algorithm they implement."] # [doc = ""] # [doc = " `signature` is the signature allegedly over `message`."] # [doc = ""] # [doc = " Return `Ok(())` only if `signature` is a valid signature on `message`."] # [doc = ""] # [doc = " Return `Err(InvalidSignature)` if the signature is invalid, including if the `public_key`"] # [doc = " encoding is invalid.  There is no need or opportunity to produce errors"] # [doc = " that are more specific than this."] fn verify_signature (& self , public_key : & [u8] , message : & [u8] , signature : & [u8] ,) -> Result < () , InvalidSignature > ; # [doc = " Return the `AlgorithmIdentifier` that must equal a public key's"] # [doc = " `subjectPublicKeyInfo` value for this `SignatureVerificationAlgorithm`"] # [doc = " to be used for signature verification."] fn public_key_alg_id (& self) -> AlgorithmIdentifier ; # [doc = " Return the `AlgorithmIdentifier` that must equal the `signatureAlgorithm` value"] # [doc = " on the data to be verified for this `SignatureVerificationAlgorithm` to be used"] # [doc = " for signature verification."] fn signature_alg_id (& self) -> AlgorithmIdentifier ; # [doc = " Return `true` if this is backed by a FIPS-approved implementation."] fn fips (& self) -> bool { false } }
};
}
