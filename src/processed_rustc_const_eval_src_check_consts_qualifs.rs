/* FP:qualifs.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_USE_0001
/* FP:qualifs.rs-0002 */ use crate :: rustc_complete :: ErrorGuaranteed ;
/* FP:qualifs.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_USE_0002
/* FP:qualifs.rs-0004 */ use crate :: rustc_complete :: LangItem ;
/* FP:qualifs.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_USE_0003
/* FP:qualifs.rs-0006 */ use crate :: rustc_infer :: infer :: TyCtxtInferExt ;
/* FP:qualifs.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_USE_0004
/* FP:qualifs.rs-0008 */ use crate :: rustc_complete :: mir :: * ;
/* FP:qualifs.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_USE_0005
/* FP:qualifs.rs-0010 */ use crate :: rustc_complete :: ty :: { self , AdtDef , Ty } ;
/* FP:qualifs.rs-0011 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_USE_0006
/* FP:qualifs.rs-0012 */ use crate :: rustc_complete :: { bug , mir } ;
/* FP:qualifs.rs-0013 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_USE_0007
/* FP:qualifs.rs-0014 */ use crate :: rustc_trait_selection :: traits :: { Obligation , ObligationCause , ObligationCtxt } ;
/* FP:qualifs.rs-0015 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_USE_0008
/* FP:qualifs.rs-0016 */ use tracing :: instrument ;
/* FP:qualifs.rs-0017 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_USE_0009
/* FP:qualifs.rs-0018 */ use super :: ConstCx ;
/* FP:qualifs.rs-0019 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_FN_0010
/* FP:qualifs.rs-0020 */ pub fn in_any_value_of_ty < 'tcx > (cx : & ConstCx < '_ , 'tcx > , ty : Ty < 'tcx > , tainted_by_errors : Option < ErrorGuaranteed > ,) -> ConstQualifs { ConstQualifs { has_mut_interior : HasMutInterior :: in_any_value_of_ty (cx , ty) , needs_drop : NeedsDrop :: in_any_value_of_ty (cx , ty) , needs_non_const_drop : NeedsNonConstDrop :: in_any_value_of_ty (cx , ty) , tainted_by_errors , } }
/* FP:qualifs.rs-0021 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_TRAIT_0011
/* FP:qualifs.rs-0022 */ # [doc = " A \"qualif\"(-ication) is a way to look for something \"bad\" in the MIR that would disqualify some"] # [doc = " code for promotion or prevent it from evaluating at compile time."] # [doc = ""] # [doc = " Normally, we would determine what qualifications apply to each type and error when an illegal"] # [doc = " operation is performed on such a type. However, this was found to be too imprecise, especially"] # [doc = " in the presence of `enum`s. If only a single variant of an enum has a certain qualification, we"] # [doc = " needn't reject code unless it actually constructs and operates on the qualified variant."] # [doc = ""] # [doc = " To accomplish this, const-checking and promotion use a value-based analysis (as opposed to a"] # [doc = " type-based one). Qualifications propagate structurally across variables: If a local (or a"] # [doc = " projection of a local) is assigned a qualified value, that local itself becomes qualified."] pub trait Qualif { # [doc = " The name of the file used to debug the dataflow analysis that computes this qualif."] const ANALYSIS_NAME : & 'static str ; # [doc = " Whether this `Qualif` is cleared when a local is moved from."] const IS_CLEARED_ON_MOVE : bool = false ; # [doc = " Whether this `Qualif` might be evaluated after the promotion and can encounter a promoted."] const ALLOW_PROMOTED : bool = false ; # [doc = " Extracts the field of `ConstQualifs` that corresponds to this `Qualif`."] fn in_qualifs (qualifs : & ConstQualifs) -> bool ; # [doc = " Returns `true` if *any* value of the given type could possibly have this `Qualif`."] # [doc = ""] # [doc = " This function determines `Qualif`s when we cannot do a value-based analysis. Since qualif"] # [doc = " propagation is context-insensitive, this includes function arguments and values returned"] # [doc = " from a call to another function."] # [doc = ""] # [doc = " It also determines the `Qualif`s for primitive types."] fn in_any_value_of_ty < 'tcx > (cx : & ConstCx < '_ , 'tcx > , ty : Ty < 'tcx >) -> bool ; # [doc = " Returns `true` if the `Qualif` is structural in an ADT's fields, i.e. if we may"] # [doc = " recurse into an operand *value* to determine whether it has this `Qualif`."] # [doc = ""] # [doc = " If this returns false, `in_any_value_of_ty` will be invoked to determine the"] # [doc = " final qualif for this ADT."] fn is_structural_in_adt_value < 'tcx > (cx : & ConstCx < '_ , 'tcx > , adt : AdtDef < 'tcx >) -> bool ; }
/* FP:qualifs.rs-0023 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_STRUCT_0012
/* FP:qualifs.rs-0024 */ # [doc = " Constant containing interior mutability (`UnsafeCell<T>`)."] # [doc = " This must be ruled out to make sure that evaluating the constant at compile-time"] # [doc = " and at *any point* during the run-time would produce the same result. In particular,"] # [doc = " promotion of temporaries must not change program behavior; if the promoted could be"] # [doc = " written to, that would be a problem."] pub struct HasMutInterior ;
/* FP:qualifs.rs-0025 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_IMPL_0013
/* FP:qualifs.rs-0026 */ impl Qualif for HasMutInterior { const ANALYSIS_NAME : & 'static str = "flow_has_mut_interior" ; fn in_qualifs (qualifs : & ConstQualifs) -> bool { qualifs . has_mut_interior } fn in_any_value_of_ty < 'tcx > (cx : & ConstCx < '_ , 'tcx > , ty : Ty < 'tcx >) -> bool { if ty . is_trivially_freeze () { return false ; } if ty . ty_adt_def () . is_some_and (| adt | adt . is_unsafe_cell ()) { return true ; } let freeze_def_id = cx . tcx . require_lang_item (LangItem :: Freeze , cx . body . span) ; let typing_env = ty :: TypingEnv { typing_mode : ty :: TypingMode :: analysis_in_body (cx . tcx , cx . body . source . def_id () . expect_local () ,) , param_env : cx . typing_env . param_env , } ; let (infcx , param_env) = cx . tcx . infer_ctxt () . build_with_typing_env (typing_env) ; let ocx = ObligationCtxt :: new (& infcx) ; let obligation = Obligation :: new (cx . tcx , ObligationCause :: dummy_with_span (cx . body . span) , param_env , ty :: TraitRef :: new (cx . tcx , freeze_def_id , [ty :: GenericArg :: from (ty)]) ,) ; ocx . register_obligation (obligation) ; let errors = ocx . select_all_or_error () ; ! errors . is_empty () } fn is_structural_in_adt_value < 'tcx > (_cx : & ConstCx < '_ , 'tcx > , adt : AdtDef < 'tcx >) -> bool { ! adt . is_unsafe_cell () } }
/* FP:qualifs.rs-0027 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_STRUCT_0014
/* FP:qualifs.rs-0028 */ # [doc = " Constant containing an ADT that implements `Drop`."] # [doc = " This must be ruled out because implicit promotion would remove side-effects"] # [doc = " that occur as part of dropping that value. N.B., the implicit promotion has"] # [doc = " to reject const Drop implementations because even if side-effects are ruled"] # [doc = " out through other means, the execution of the drop could diverge."] pub struct NeedsDrop ;
/* FP:qualifs.rs-0029 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_IMPL_0015
/* FP:qualifs.rs-0030 */ impl Qualif for NeedsDrop { const ANALYSIS_NAME : & 'static str = "flow_needs_drop" ; const IS_CLEARED_ON_MOVE : bool = true ; const ALLOW_PROMOTED : bool = true ; fn in_qualifs (qualifs : & ConstQualifs) -> bool { qualifs . needs_drop } fn in_any_value_of_ty < 'tcx > (cx : & ConstCx < '_ , 'tcx > , ty : Ty < 'tcx >) -> bool { ty . needs_drop (cx . tcx , cx . typing_env) } fn is_structural_in_adt_value < 'tcx > (cx : & ConstCx < '_ , 'tcx > , adt : AdtDef < 'tcx >) -> bool { ! adt . has_dtor (cx . tcx) } }
/* FP:qualifs.rs-0031 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_STRUCT_0016
/* FP:qualifs.rs-0032 */ # [doc = " Constant containing an ADT that implements non-const `Drop`."] # [doc = " This must be ruled out because we cannot run `Drop` during compile-time."] pub struct NeedsNonConstDrop ;
/* FP:qualifs.rs-0033 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_IMPL_0017
/* FP:qualifs.rs-0034 */ impl Qualif for NeedsNonConstDrop { const ANALYSIS_NAME : & 'static str = "flow_needs_nonconst_drop" ; const IS_CLEARED_ON_MOVE : bool = true ; const ALLOW_PROMOTED : bool = true ; fn in_qualifs (qualifs : & ConstQualifs) -> bool { qualifs . needs_non_const_drop } # [instrument (level = "trace" , skip (cx) , ret)] fn in_any_value_of_ty < 'tcx > (cx : & ConstCx < '_ , 'tcx > , ty : Ty < 'tcx >) -> bool { if ! ty . needs_drop (cx . tcx , cx . typing_env) { return false ; } let destruct_def_id = cx . tcx . require_lang_item (LangItem :: Destruct , cx . body . span) ; let (infcx , param_env) = cx . tcx . infer_ctxt () . build_with_typing_env (cx . typing_env) ; let ocx = ObligationCtxt :: new (& infcx) ; ocx . register_obligation (Obligation :: new (cx . tcx , ObligationCause :: misc (cx . body . span , cx . def_id ()) , param_env , ty :: Binder :: dummy (ty :: TraitRef :: new (cx . tcx , destruct_def_id , [ty])) . to_host_effect_clause (cx . tcx , match cx . const_kind () { crate :: rustc_hir :: ConstContext :: ConstFn => ty :: BoundConstness :: Maybe , crate :: rustc_hir :: ConstContext :: Static (_) | crate :: rustc_hir :: ConstContext :: Const { .. } => ty :: BoundConstness :: Const , } ,) ,)) ; ! ocx . select_all_or_error () . is_empty () } fn is_structural_in_adt_value < 'tcx > (cx : & ConstCx < '_ , 'tcx > , adt : AdtDef < 'tcx >) -> bool { ! adt . has_dtor (cx . tcx) } }
/* FP:qualifs.rs-0035 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_FN_0018
/* FP:qualifs.rs-0036 */ # [doc = " Returns `true` if this `Rvalue` contains qualif `Q`."] pub fn in_rvalue < 'tcx , Q , F > (cx : & ConstCx < '_ , 'tcx > , in_local : & mut F , rvalue : & Rvalue < 'tcx > ,) -> bool where Q : Qualif , F : FnMut (Local) -> bool , { match rvalue { Rvalue :: ThreadLocalRef (_) | Rvalue :: NullaryOp (..) => { Q :: in_any_value_of_ty (cx , rvalue . ty (cx . body , cx . tcx)) } Rvalue :: Discriminant (place) | Rvalue :: Len (place) => { in_place :: < Q , _ > (cx , in_local , place . as_ref ()) } Rvalue :: CopyForDeref (place) => in_place :: < Q , _ > (cx , in_local , place . as_ref ()) , Rvalue :: Use (operand) | Rvalue :: Repeat (operand , _) | Rvalue :: UnaryOp (_ , operand) | Rvalue :: Cast (_ , operand , _) | Rvalue :: ShallowInitBox (operand , _) => in_operand :: < Q , _ > (cx , in_local , operand) , Rvalue :: BinaryOp (_ , box (lhs , rhs)) => { in_operand :: < Q , _ > (cx , in_local , lhs) || in_operand :: < Q , _ > (cx , in_local , rhs) } Rvalue :: Ref (_ , _ , place) | Rvalue :: RawPtr (_ , place) => { if let Some ((place_base , ProjectionElem :: Deref)) = place . as_ref () . last_projection () { let base_ty = place_base . ty (cx . body , cx . tcx) . ty ; if let ty :: Ref (..) = base_ty . kind () { return in_place :: < Q , _ > (cx , in_local , place_base) ; } } in_place :: < Q , _ > (cx , in_local , place . as_ref ()) } Rvalue :: WrapUnsafeBinder (op , _) => in_operand :: < Q , _ > (cx , in_local , op) , Rvalue :: Aggregate (kind , operands) => { if let AggregateKind :: Adt (adt_did , ..) = * * kind { let def = cx . tcx . adt_def (adt_did) ; if def . is_union () || ! Q :: is_structural_in_adt_value (cx , def) { return Q :: in_any_value_of_ty (cx , rvalue . ty (cx . body , cx . tcx)) ; } } operands . iter () . any (| o | in_operand :: < Q , _ > (cx , in_local , o)) } } }
/* FP:qualifs.rs-0037 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_FN_0019
/* FP:qualifs.rs-0038 */ # [doc = " Returns `true` if this `Place` contains qualif `Q`."] pub fn in_place < 'tcx , Q , F > (cx : & ConstCx < '_ , 'tcx > , in_local : & mut F , place : PlaceRef < 'tcx >) -> bool where Q : Qualif , F : FnMut (Local) -> bool , { let mut place = place ; while let Some ((place_base , elem)) = place . last_projection () { match elem { ProjectionElem :: Index (index) if in_local (index) => return true , ProjectionElem :: Deref | ProjectionElem :: Subtype (_) | ProjectionElem :: Field (_ , _) | ProjectionElem :: OpaqueCast (_) | ProjectionElem :: ConstantIndex { .. } | ProjectionElem :: Subslice { .. } | ProjectionElem :: Downcast (_ , _) | ProjectionElem :: Index (_) | ProjectionElem :: UnwrapUnsafeBinder (_) => { } } let base_ty = place_base . ty (cx . body , cx . tcx) ; let proj_ty = base_ty . projection_ty (cx . tcx , elem) . ty ; if ! Q :: in_any_value_of_ty (cx , proj_ty) { return false ; } if matches ! (elem , ProjectionElem :: Deref) { return true ; } place = place_base ; } assert ! (place . projection . is_empty ()) ; in_local (place . local) }
/* FP:qualifs.rs-0039 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_check_consts_qualifs_FN_0020
/* FP:qualifs.rs-0040 */ # [doc = " Returns `true` if this `Operand` contains qualif `Q`."] pub fn in_operand < 'tcx , Q , F > (cx : & ConstCx < '_ , 'tcx > , in_local : & mut F , operand : & Operand < 'tcx > ,) -> bool where Q : Qualif , F : FnMut (Local) -> bool , { let constant = match operand { Operand :: Copy (place) | Operand :: Move (place) => { return in_place :: < Q , _ > (cx , in_local , place . as_ref ()) ; } Operand :: Constant (c) => c , } ; let uneval = match constant . const_ { Const :: Ty (_ , ct) if matches ! (ct . kind () , ty :: ConstKind :: Param (_) | ty :: ConstKind :: Error (_) | ty :: ConstKind :: Value (_)) => { None } Const :: Ty (_ , c) => { bug ! ("expected ConstKind::Param or ConstKind::Value here, found {:?}" , c) } Const :: Unevaluated (uv , _) => Some (uv) , Const :: Val (..) => None , } ; if let Some (mir :: UnevaluatedConst { def , args : _ , promoted }) = uneval { assert ! (promoted . is_none () || Q :: ALLOW_PROMOTED) ; if promoted . is_none () && cx . tcx . trait_of_assoc (def) . is_none () { let qualifs = cx . tcx . at (constant . span) . mir_const_qualif (def) ; if ! Q :: in_qualifs (& qualifs) { return false ; } } } Q :: in_any_value_of_ty (cx , constant . const_ . ty ()) }