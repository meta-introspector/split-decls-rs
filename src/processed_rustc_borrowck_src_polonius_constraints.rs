/* FP:constraints.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_polonius_constraints_USE_0001
/* FP:constraints.rs-0002 */ use crate :: rustc_complete :: ty :: RegionVid ;
/* FP:constraints.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_polonius_constraints_USE_0002
/* FP:constraints.rs-0004 */ use crate :: rustc_mir_dataflow :: points :: PointIndex ;
/* FP:constraints.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_polonius_constraints_STRUCT_0003
/* FP:constraints.rs-0006 */ # [doc = " A localized outlives constraint reifies the CFG location where the outlives constraint holds,"] # [doc = " within the origins themselves as if they were different from point to point: from `a: b`"] # [doc = " outlives constraints to `a@p: b@p`, where `p` is the point in the CFG."] # [doc = ""] # [doc = " This models two sources of constraints:"] # [doc = " - constraints that traverse the subsets between regions at a given point, `a@p: b@p`. These"] # [doc = "   depend on typeck constraints generated via assignments, calls, etc."] # [doc = " - constraints that traverse the CFG via the same region, `a@p: a@q`, where `p` is a predecessor"] # [doc = "   of `q`. These depend on the liveness of the regions at these points, as well as their"] # [doc = "   variance."] # [doc = ""] # [doc = " The `source` origin at `from` flows into the `target` origin at `to`."] # [doc = ""] # [doc = " This dual of NLL's [crate::constraints::OutlivesConstraint] therefore encodes the"] # [doc = " position-dependent outlives constraints used by Polonius, to model the flow-sensitive loan"] # [doc = " propagation via reachability within a graph of localized constraints."] # [derive (Copy , Clone , PartialEq , Eq , Debug , Hash)] pub (crate) struct LocalizedOutlivesConstraint { pub source : RegionVid , pub from : PointIndex , pub target : RegionVid , pub to : PointIndex , }
/* FP:constraints.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_polonius_constraints_STRUCT_0004
/* FP:constraints.rs-0008 */ # [doc = " A container of [LocalizedOutlivesConstraint]s that can be turned into a traversable"] # [doc = " `rustc_data_structures` graph."] # [derive (Clone , Default , Debug)] pub (crate) struct LocalizedOutlivesConstraintSet { pub outlives : Vec < LocalizedOutlivesConstraint > , }
/* FP:constraints.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_polonius_constraints_IMPL_0005
/* FP:constraints.rs-0010 */ impl LocalizedOutlivesConstraintSet { pub (crate) fn push (& mut self , constraint : LocalizedOutlivesConstraint) { if constraint . source == constraint . target && constraint . from == constraint . to { return ; } self . outlives . push (constraint) ; } }