/* FP:compiler_interface.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0001
/* FP:compiler_interface.rs-0002 */ use std :: cell :: Cell ;
/* FP:compiler_interface.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0002
/* FP:compiler_interface.rs-0004 */ use crate :: rustc_complete :: def :: DefKind ;
/* FP:compiler_interface.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0003
/* FP:compiler_interface.rs-0006 */ use crate :: rustc_public_bridge :: context :: CompilerCtxt ;
/* FP:compiler_interface.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0004
/* FP:compiler_interface.rs-0008 */ use crate :: rustc_public_bridge :: { Bridge , Container } ;
/* FP:compiler_interface.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0005
/* FP:compiler_interface.rs-0010 */ use tracing :: debug ;
/* FP:compiler_interface.rs-0011 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0006
/* FP:compiler_interface.rs-0012 */ use crate :: abi :: { FnAbi , Layout , LayoutShape , ReprOptions } ;
/* FP:compiler_interface.rs-0013 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0007
/* FP:compiler_interface.rs-0014 */ use crate :: crate_def :: Attribute ;
/* FP:compiler_interface.rs-0015 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0008
/* FP:compiler_interface.rs-0016 */ use crate :: mir :: alloc :: { AllocId , GlobalAlloc } ;
/* FP:compiler_interface.rs-0017 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0009
/* FP:compiler_interface.rs-0018 */ use crate :: mir :: mono :: { Instance , InstanceDef , StaticDef } ;
/* FP:compiler_interface.rs-0019 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0010
/* FP:compiler_interface.rs-0020 */ use crate :: mir :: { BinOp , Body , Place , UnOp } ;
/* FP:compiler_interface.rs-0021 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0011
/* FP:compiler_interface.rs-0022 */ use crate :: target :: { MachineInfo , MachineSize } ;
/* FP:compiler_interface.rs-0023 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0012
/* FP:compiler_interface.rs-0024 */ use crate :: ty :: { AdtDef , AdtKind , Allocation , ClosureDef , ClosureKind , CoroutineDef , Discr , FieldDef , FnDef , ForeignDef , ForeignItemKind , ForeignModule , ForeignModuleDef , GenericArgs , GenericPredicates , Generics , ImplDef , ImplTrait , IntrinsicDef , LineInfo , MirConst , PolyFnSig , RigidTy , Span , TraitDecl , TraitDef , Ty , TyConst , TyConstId , TyKind , UintTy , VariantDef , VariantIdx , } ;
/* FP:compiler_interface.rs-0025 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0013
/* FP:compiler_interface.rs-0026 */ use crate :: unstable :: { RustcInternal , Stable , new_item_kind } ;
/* FP:compiler_interface.rs-0027 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_USE_0014
/* FP:compiler_interface.rs-0028 */ use crate :: { AssocItems , Crate , CrateDef , CrateItem , CrateItems , CrateNum , DefId , Error , Filename , ImplTraitDecls , ItemKind , Symbol , TraitDecls , alloc , mir , } ;
/* FP:compiler_interface.rs-0029 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_STRUCT_0015
/* FP:compiler_interface.rs-0030 */ pub struct BridgeTys ;
/* FP:compiler_interface.rs-0031 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_IMPL_0016
/* FP:compiler_interface.rs-0032 */ impl Bridge for BridgeTys { type DefId = crate :: DefId ; type AllocId = crate :: mir :: alloc :: AllocId ; type Span = crate :: ty :: Span ; type Ty = crate :: ty :: Ty ; type InstanceDef = crate :: mir :: mono :: InstanceDef ; type TyConstId = crate :: ty :: TyConstId ; type MirConstId = crate :: ty :: MirConstId ; type Layout = crate :: abi :: Layout ; type Error = crate :: Error ; type CrateItem = crate :: CrateItem ; type AdtDef = crate :: ty :: AdtDef ; type ForeignModuleDef = crate :: ty :: ForeignModuleDef ; type ForeignDef = crate :: ty :: ForeignDef ; type FnDef = crate :: ty :: FnDef ; type ClosureDef = crate :: ty :: ClosureDef ; type CoroutineDef = crate :: ty :: CoroutineDef ; type CoroutineClosureDef = crate :: ty :: CoroutineClosureDef ; type AliasDef = crate :: ty :: AliasDef ; type ParamDef = crate :: ty :: ParamDef ; type BrNamedDef = crate :: ty :: BrNamedDef ; type TraitDef = crate :: ty :: TraitDef ; type GenericDef = crate :: ty :: GenericDef ; type ConstDef = crate :: ty :: ConstDef ; type ImplDef = crate :: ty :: ImplDef ; type RegionDef = crate :: ty :: RegionDef ; type CoroutineWitnessDef = crate :: ty :: CoroutineWitnessDef ; type AssocDef = crate :: ty :: AssocDef ; type OpaqueDef = crate :: ty :: OpaqueDef ; type Prov = crate :: ty :: Prov ; type StaticDef = crate :: mir :: mono :: StaticDef ; type Allocation = crate :: ty :: Allocation ; }
/* FP:compiler_interface.rs-0033 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_TRAIT_0017
/* FP:compiler_interface.rs-0034 */ # [doc = " Public API for querying compiler information."] # [doc = ""] # [doc = " All queries are delegated to [`crate::rustc_public_bridge::context::CompilerCtxt`] that provides"] # [doc = " similar APIs but based on internal rustc constructs."] # [doc = ""] # [doc = " Do not use this directly. This is currently used in the macro expansion."] pub (crate) trait CompilerInterface { fn entry_fn (& self) -> Option < CrateItem > ; # [doc = " Retrieve all items of the local crate that have a MIR associated with them."] fn all_local_items (& self) -> CrateItems ; # [doc = " Retrieve the body of a function."] # [doc = " This function will panic if the body is not available."] fn mir_body (& self , item : DefId) -> mir :: Body ; # [doc = " Check whether the body of a function is available."] fn has_body (& self , item : DefId) -> bool ; fn foreign_modules (& self , crate_num : CrateNum) -> Vec < ForeignModuleDef > ; # [doc = " Retrieve all functions defined in this crate."] fn crate_functions (& self , crate_num : CrateNum) -> Vec < FnDef > ; # [doc = " Retrieve all static items defined in this crate."] fn crate_statics (& self , crate_num : CrateNum) -> Vec < StaticDef > ; fn foreign_module (& self , mod_def : ForeignModuleDef) -> ForeignModule ; fn foreign_items (& self , mod_def : ForeignModuleDef) -> Vec < ForeignDef > ; fn all_trait_decls (& self) -> TraitDecls ; fn trait_decls (& self , crate_num : CrateNum) -> TraitDecls ; fn trait_decl (& self , trait_def : & TraitDef) -> TraitDecl ; fn all_trait_impls (& self) -> ImplTraitDecls ; fn trait_impls (& self , crate_num : CrateNum) -> ImplTraitDecls ; fn trait_impl (& self , trait_impl : & ImplDef) -> ImplTrait ; fn generics_of (& self , def_id : DefId) -> Generics ; fn predicates_of (& self , def_id : DefId) -> GenericPredicates ; fn explicit_predicates_of (& self , def_id : DefId) -> GenericPredicates ; # [doc = " Get information about the local crate."] fn local_crate (& self) -> Crate ; # [doc = " Retrieve a list of all external crates."] fn external_crates (& self) -> Vec < Crate > ; # [doc = " Find a crate with the given name."] fn find_crates (& self , name : & str) -> Vec < Crate > ; # [doc = " Returns the name of given `DefId`"] fn def_name (& self , def_id : DefId , trimmed : bool) -> Symbol ; # [doc = " Return registered tool attributes with the given attribute name."] # [doc = ""] # [doc = " FIXME(jdonszelmann): may panic on non-tool attributes. After more attribute work, non-tool"] # [doc = " attributes will simply return an empty list."] # [doc = ""] # [doc = " Single segmented name like `#[clippy]` is specified as `&[\"clippy\".to_string()]`."] # [doc = " Multi-segmented name like `#[rustfmt::skip]` is specified as `&[\"rustfmt\".to_string(), \"skip\".to_string()]`."] fn tool_attrs (& self , def_id : DefId , attr : & [Symbol]) -> Vec < Attribute > ; # [doc = " Get all tool attributes of a definition."] fn all_tool_attrs (& self , def_id : DefId) -> Vec < Attribute > ; # [doc = " Returns printable, human readable form of `Span`"] fn span_to_string (& self , span : Span) -> String ; # [doc = " Return filename from given `Span`, for diagnostic purposes"] fn get_filename (& self , span : & Span) -> Filename ; # [doc = " Return lines corresponding to this `Span`"] fn get_lines (& self , span : & Span) -> LineInfo ; # [doc = " Returns the `kind` of given `DefId`"] fn item_kind (& self , item : CrateItem) -> ItemKind ; # [doc = " Returns whether this is a foreign item."] fn is_foreign_item (& self , item : DefId) -> bool ; # [doc = " Returns the kind of a given foreign item."] fn foreign_item_kind (& self , def : ForeignDef) -> ForeignItemKind ; # [doc = " Returns the kind of a given algebraic data type"] fn adt_kind (& self , def : AdtDef) -> AdtKind ; # [doc = " Returns if the ADT is a box."] fn adt_is_box (& self , def : AdtDef) -> bool ; # [doc = " Returns whether this ADT is simd."] fn adt_is_simd (& self , def : AdtDef) -> bool ; # [doc = " Returns whether this definition is a C string."] fn adt_is_cstr (& self , def : AdtDef) -> bool ; # [doc = " Returns the representation options for this ADT."] fn adt_repr (& self , def : AdtDef) -> ReprOptions ; # [doc = " Retrieve the function signature for the given generic arguments."] fn fn_sig (& self , def : FnDef , args : & GenericArgs) -> PolyFnSig ; # [doc = " Retrieve the intrinsic definition if the item corresponds one."] fn intrinsic (& self , item : DefId) -> Option < IntrinsicDef > ; # [doc = " Retrieve the plain function name of an intrinsic."] fn intrinsic_name (& self , def : IntrinsicDef) -> Symbol ; # [doc = " Retrieve the closure signature for the given generic arguments."] fn closure_sig (& self , args : & GenericArgs) -> PolyFnSig ; # [doc = " The number of variants in this ADT."] fn adt_variants_len (& self , def : AdtDef) -> usize ; # [doc = " Discriminant for a given variant index of AdtDef."] fn adt_discr_for_variant (& self , adt : AdtDef , variant : VariantIdx) -> Discr ; # [doc = " Discriminant for a given variand index and args of a coroutine."] fn coroutine_discr_for_variant (& self , coroutine : CoroutineDef , args : & GenericArgs , variant : VariantIdx ,) -> Discr ; # [doc = " The name of a variant."] fn variant_name (& self , def : VariantDef) -> Symbol ; fn variant_fields (& self , def : VariantDef) -> Vec < FieldDef > ; # [doc = " Evaluate constant as a target usize."] fn eval_target_usize (& self , cnst : & MirConst) -> Result < u64 , Error > ; fn eval_target_usize_ty (& self , cnst : & TyConst) -> Result < u64 , Error > ; # [doc = " Create a new zero-sized constant."] fn try_new_const_zst (& self , ty : Ty) -> Result < MirConst , Error > ; # [doc = " Create a new constant that represents the given string value."] fn new_const_str (& self , value : & str) -> MirConst ; # [doc = " Create a new constant that represents the given boolean value."] fn new_const_bool (& self , value : bool) -> MirConst ; # [doc = " Create a new constant that represents the given value."] fn try_new_const_uint (& self , value : u128 , uint_ty : UintTy) -> Result < MirConst , Error > ; fn try_new_ty_const_uint (& self , value : u128 , uint_ty : UintTy) -> Result < TyConst , Error > ; # [doc = " Create a new type from the given kind."] fn new_rigid_ty (& self , kind : RigidTy) -> Ty ; # [doc = " Create a new box type, `Box<T>`, for the given inner type `T`."] fn new_box_ty (& self , ty : Ty) -> Ty ; # [doc = " Returns the type of given crate item."] fn def_ty (& self , item : DefId) -> Ty ; # [doc = " Returns the type of given definition instantiated with the given arguments."] fn def_ty_with_args (& self , item : DefId , args : & GenericArgs) -> Ty ; # [doc = " Returns literal value of a const as a string."] fn mir_const_pretty (& self , cnst : & MirConst) -> String ; # [doc = " `Span` of an item"] fn span_of_an_item (& self , def_id : DefId) -> Span ; fn ty_const_pretty (& self , ct : TyConstId) -> String ; # [doc = " Obtain the representation of a type."] fn ty_pretty (& self , ty : Ty) -> String ; # [doc = " Obtain the kind of a type."] fn ty_kind (& self , ty : Ty) -> TyKind ; fn rigid_ty_discriminant_ty (& self , ty : & RigidTy) -> Ty ; # [doc = " Get the body of an Instance which is already monomorphized."] fn instance_body (& self , instance : InstanceDef) -> Option < Body > ; # [doc = " Get the instance type with generic instantiations applied and lifetimes erased."] fn instance_ty (& self , instance : InstanceDef) -> Ty ; # [doc = " Get the instantiation types."] fn instance_args (& self , def : InstanceDef) -> GenericArgs ; # [doc = " Get the instance."] fn instance_def_id (& self , instance : InstanceDef) -> DefId ; # [doc = " Get the instance mangled name."] fn instance_mangled_name (& self , instance : InstanceDef) -> Symbol ; # [doc = " Check if this is an empty DropGlue shim."] fn is_empty_drop_shim (& self , def : InstanceDef) -> bool ; # [doc = " Convert a non-generic crate item into an instance."] # [doc = " This function will panic if the item is generic."] fn mono_instance (& self , def_id : DefId) -> Instance ; # [doc = " Item requires monomorphization."] fn requires_monomorphization (& self , def_id : DefId) -> bool ; # [doc = " Resolve an instance from the given function definition and generic arguments."] fn resolve_instance (& self , def : FnDef , args : & GenericArgs) -> Option < Instance > ; # [doc = " Resolve an instance for drop_in_place for the given type."] fn resolve_drop_in_place (& self , ty : Ty) -> Instance ; # [doc = " Resolve instance for a function pointer."] fn resolve_for_fn_ptr (& self , def : FnDef , args : & GenericArgs) -> Option < Instance > ; # [doc = " Resolve instance for a closure with the requested type."] fn resolve_closure (& self , def : ClosureDef , args : & GenericArgs , kind : ClosureKind ,) -> Option < Instance > ; # [doc = " Evaluate a static's initializer."] fn eval_static_initializer (& self , def : StaticDef) -> Result < Allocation , Error > ; # [doc = " Try to evaluate an instance into a constant."] fn eval_instance (& self , def : InstanceDef , const_ty : Ty) -> Result < Allocation , Error > ; # [doc = " Retrieve global allocation for the given allocation ID."] fn global_alloc (& self , id : AllocId) -> GlobalAlloc ; # [doc = " Retrieve the id for the virtual table."] fn vtable_allocation (& self , global_alloc : & GlobalAlloc) -> Option < AllocId > ; fn krate (& self , def_id : DefId) -> Crate ; fn instance_name (& self , def : InstanceDef , trimmed : bool) -> Symbol ; # [doc = " Return information about the target machine."] fn target_info (& self) -> MachineInfo ; # [doc = " Get an instance ABI."] fn instance_abi (& self , def : InstanceDef) -> Result < FnAbi , Error > ; # [doc = " Get the ABI of a function pointer."] fn fn_ptr_abi (& self , fn_ptr : PolyFnSig) -> Result < FnAbi , Error > ; # [doc = " Get the layout of a type."] fn ty_layout (& self , ty : Ty) -> Result < Layout , Error > ; # [doc = " Get the layout shape."] fn layout_shape (& self , id : Layout) -> LayoutShape ; # [doc = " Get a debug string representation of a place."] fn place_pretty (& self , place : & Place) -> String ; # [doc = " Get the resulting type of binary operation."] fn binop_ty (& self , bin_op : BinOp , rhs : Ty , lhs : Ty) -> Ty ; # [doc = " Get the resulting type of unary operation."] fn unop_ty (& self , un_op : UnOp , arg : Ty) -> Ty ; # [doc = " Get all associated items of a definition."] fn associated_items (& self , def_id : DefId) -> AssocItems ; }
/* FP:compiler_interface.rs-0035 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_IMPL_0018
/* FP:compiler_interface.rs-0036 */ impl < 'tcx > CompilerInterface for Container < 'tcx , BridgeTys > { fn entry_fn (& self) -> Option < CrateItem > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = cx . entry_fn () ; Some (tables . crate_item (did ?)) } # [doc = " Retrieve all items of the local crate that have a MIR associated with them."] fn all_local_items (& self) -> CrateItems { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . all_local_items () . iter () . map (| did | tables . crate_item (* did)) . collect () } # [doc = " Retrieve the body of a function."] # [doc = " This function will panic if the body is not available."] fn mir_body (& self , item : DefId) -> mir :: Body { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [item] ; cx . mir_body (did) . stable (& mut * tables , cx) } # [doc = " Check whether the body of a function is available."] fn has_body (& self , item : DefId) -> bool { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let def = item . internal (& mut * tables , cx . tcx) ; cx . has_body (def) } fn foreign_modules (& self , crate_num : CrateNum) -> Vec < ForeignModuleDef > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . foreign_modules (crate_num . internal (& mut * tables , cx . tcx)) . iter () . map (| did | tables . foreign_module_def (* did)) . collect () } # [doc = " Retrieve all functions defined in this crate."] fn crate_functions (& self , crate_num : CrateNum) -> Vec < FnDef > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let krate = crate_num . internal (& mut * tables , cx . tcx) ; cx . crate_functions (krate) . iter () . map (| did | tables . fn_def (* did)) . collect () } # [doc = " Retrieve all static items defined in this crate."] fn crate_statics (& self , crate_num : CrateNum) -> Vec < StaticDef > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let krate = crate_num . internal (& mut * tables , cx . tcx) ; cx . crate_statics (krate) . iter () . map (| did | tables . static_def (* did)) . collect () } fn foreign_module (& self , mod_def : ForeignModuleDef) -> ForeignModule { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [mod_def . def_id ()] ; cx . foreign_module (did) . stable (& mut * tables , cx) } fn foreign_items (& self , mod_def : ForeignModuleDef) -> Vec < ForeignDef > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [mod_def . def_id ()] ; cx . foreign_items (did) . iter () . map (| did | tables . foreign_def (* did)) . collect () } fn all_trait_decls (& self) -> TraitDecls { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . all_trait_decls () . map (| did | tables . trait_def (did)) . collect () } fn trait_decls (& self , crate_num : CrateNum) -> TraitDecls { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let krate = crate_num . internal (& mut * tables , cx . tcx) ; cx . trait_decls (krate) . iter () . map (| did | tables . trait_def (* did)) . collect () } fn trait_decl (& self , trait_def : & TraitDef) -> TraitDecl { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [trait_def . 0] ; cx . trait_decl (did) . stable (& mut * tables , cx) } fn all_trait_impls (& self) -> ImplTraitDecls { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . all_trait_impls () . iter () . map (| did | tables . impl_def (* did)) . collect () } fn trait_impls (& self , crate_num : CrateNum) -> ImplTraitDecls { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let krate = crate_num . internal (& mut * tables , cx . tcx) ; cx . trait_impls (krate) . iter () . map (| did | tables . impl_def (* did)) . collect () } fn trait_impl (& self , trait_impl : & ImplDef) -> ImplTrait { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [trait_impl . 0] ; cx . trait_impl (did) . stable (& mut * tables , cx) } fn generics_of (& self , def_id : DefId) -> Generics { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [def_id] ; cx . generics_of (did) . stable (& mut * tables , cx) } fn predicates_of (& self , def_id : DefId) -> GenericPredicates { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [def_id] ; let (parent , kinds) = cx . predicates_of (did) ; crate :: ty :: GenericPredicates { parent : parent . map (| did | tables . trait_def (did)) , predicates : kinds . iter () . map (| (kind , span) | (kind . stable (& mut * tables , cx) , span . stable (& mut * tables , cx))) . collect () , } } fn explicit_predicates_of (& self , def_id : DefId) -> GenericPredicates { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [def_id] ; let (parent , kinds) = cx . explicit_predicates_of (did) ; crate :: ty :: GenericPredicates { parent : parent . map (| did | tables . trait_def (did)) , predicates : kinds . iter () . map (| (kind , span) | (kind . stable (& mut * tables , cx) , span . stable (& mut * tables , cx))) . collect () , } } # [doc = " Get information about the local crate."] fn local_crate (& self) -> Crate { let cx = & * self . cx . borrow () ; smir_crate (cx , cx . local_crate_num ()) } # [doc = " Retrieve a list of all external crates."] fn external_crates (& self) -> Vec < Crate > { let cx = & * self . cx . borrow () ; cx . external_crates () . iter () . map (| crate_num | smir_crate (cx , * crate_num)) . collect () } # [doc = " Find a crate with the given name."] fn find_crates (& self , name : & str) -> Vec < Crate > { let cx = & * self . cx . borrow () ; cx . find_crates (name) . iter () . map (| crate_num | smir_crate (cx , * crate_num)) . collect () } # [doc = " Returns the name of given `DefId`."] fn def_name (& self , def_id : DefId , trimmed : bool) -> Symbol { let tables = self . tables . borrow () ; let cx = & * self . cx . borrow () ; let did = tables [def_id] ; cx . def_name (did , trimmed) } # [doc = " Return registered tool attributes with the given attribute name."] # [doc = ""] # [doc = " FIXME(jdonszelmann): may panic on non-tool attributes. After more attribute work, non-tool"] # [doc = " attributes will simply return an empty list."] # [doc = ""] # [doc = " Single segmented name like `#[clippy]` is specified as `&[\"clippy\".to_string()]`."] # [doc = " Multi-segmented name like `#[rustfmt::skip]` is specified as `&[\"rustfmt\".to_string(), \"skip\".to_string()]`."] fn tool_attrs (& self , def_id : DefId , attr : & [Symbol]) -> Vec < Attribute > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [def_id] ; cx . tool_attrs (did , attr) . into_iter () . map (| (attr_str , span) | Attribute :: new (attr_str , span . stable (& mut * tables , cx))) . collect () } # [doc = " Get all tool attributes of a definition."] fn all_tool_attrs (& self , def_id : DefId) -> Vec < Attribute > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [def_id] ; cx . all_tool_attrs (did) . into_iter () . map (| (attr_str , span) | Attribute :: new (attr_str , span . stable (& mut * tables , cx))) . collect () } # [doc = " Returns printable, human readable form of `Span`."] fn span_to_string (& self , span : Span) -> String { let tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let sp = tables . spans [span] ; cx . span_to_string (sp) } # [doc = " Return filename from given `Span`, for diagnostic purposes."] fn get_filename (& self , span : & Span) -> Filename { let tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let sp = tables . spans [* span] ; cx . get_filename (sp) } # [doc = " Return lines corresponding to this `Span`."] fn get_lines (& self , span : & Span) -> LineInfo { let tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let sp = tables . spans [* span] ; let lines = cx . get_lines (sp) ; LineInfo :: from (lines) } # [doc = " Returns the `kind` of given `DefId`."] fn item_kind (& self , item : CrateItem) -> ItemKind { let tables = self . tables . borrow () ; let cx = & * self . cx . borrow () ; let did = tables [item . 0] ; new_item_kind (cx . def_kind (did)) } # [doc = " Returns whether this is a foreign item."] fn is_foreign_item (& self , item : DefId) -> bool { let tables = self . tables . borrow () ; let cx = & * self . cx . borrow () ; let did = tables [item] ; cx . is_foreign_item (did) } # [doc = " Returns the kind of a given foreign item."] fn foreign_item_kind (& self , def : ForeignDef) -> ForeignItemKind { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let def_id = tables [def . def_id ()] ; let def_kind = cx . foreign_item_kind (def_id) ; match def_kind { DefKind :: Fn => ForeignItemKind :: Fn (tables . fn_def (def_id)) , DefKind :: Static { .. } => ForeignItemKind :: Static (tables . static_def (def_id)) , DefKind :: ForeignTy => { use crate :: rustc_public_bridge :: context :: TyHelpers ; ForeignItemKind :: Type (tables . intern_ty (cx . new_foreign (def_id))) } def_kind => unreachable ! ("Unexpected kind for a foreign item: {:?}" , def_kind) , } } # [doc = " Returns the kind of a given algebraic data type."] fn adt_kind (& self , def : AdtDef) -> AdtKind { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . adt_kind (def . internal (& mut * tables , cx . tcx)) . stable (& mut * tables , cx) } # [doc = " Returns if the ADT is a box."] fn adt_is_box (& self , def : AdtDef) -> bool { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . adt_is_box (def . internal (& mut * tables , cx . tcx)) } # [doc = " Returns whether this ADT is simd."] fn adt_is_simd (& self , def : AdtDef) -> bool { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . adt_is_simd (def . internal (& mut * tables , cx . tcx)) } # [doc = " Returns whether this definition is a C string."] fn adt_is_cstr (& self , def : AdtDef) -> bool { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . adt_is_cstr (def . 0 . internal (& mut * tables , cx . tcx)) } # [doc = " Returns the representation options for this ADT"] fn adt_repr (& self , def : AdtDef) -> ReprOptions { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . adt_repr (def . internal (& mut * tables , cx . tcx)) . stable (& mut * tables , cx) } # [doc = " Retrieve the function signature for the given generic arguments."] fn fn_sig (& self , def : FnDef , args : & GenericArgs) -> PolyFnSig { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let def_id = def . 0 . internal (& mut * tables , cx . tcx) ; let args_ref = args . internal (& mut * tables , cx . tcx) ; cx . fn_sig (def_id , args_ref) . stable (& mut * tables , cx) } # [doc = " Retrieve the intrinsic definition if the item corresponds one."] fn intrinsic (& self , item : DefId) -> Option < IntrinsicDef > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let def_id = item . internal (& mut * tables , cx . tcx) ; cx . intrinsic (def_id) . map (| _ | IntrinsicDef (item)) } # [doc = " Retrieve the plain function name of an intrinsic."] fn intrinsic_name (& self , def : IntrinsicDef) -> Symbol { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let def_id = def . 0 . internal (& mut * tables , cx . tcx) ; cx . intrinsic_name (def_id) } # [doc = " Retrieve the closure signature for the given generic arguments."] fn closure_sig (& self , args : & GenericArgs) -> PolyFnSig { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let args_ref = args . internal (& mut * tables , cx . tcx) ; cx . closure_sig (args_ref) . stable (& mut * tables , cx) } # [doc = " The number of variants in this ADT."] fn adt_variants_len (& self , def : AdtDef) -> usize { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . adt_variants_len (def . internal (& mut * tables , cx . tcx)) } # [doc = " Discriminant for a given variant index of AdtDef."] fn adt_discr_for_variant (& self , adt : AdtDef , variant : VariantIdx) -> Discr { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . adt_discr_for_variant (adt . internal (& mut * tables , cx . tcx) , variant . internal (& mut * tables , cx . tcx) ,) . stable (& mut * tables , cx) } # [doc = " Discriminant for a given variand index and args of a coroutine."] fn coroutine_discr_for_variant (& self , coroutine : CoroutineDef , args : & GenericArgs , variant : VariantIdx ,) -> Discr { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let tcx = cx . tcx ; let def = coroutine . def_id () . internal (& mut * tables , tcx) ; let args_ref = args . internal (& mut * tables , tcx) ; cx . coroutine_discr_for_variant (def , args_ref , variant . internal (& mut * tables , tcx)) . stable (& mut * tables , cx) } # [doc = " The name of a variant."] fn variant_name (& self , def : VariantDef) -> Symbol { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . variant_name (def . internal (& mut * tables , cx . tcx)) } fn variant_fields (& self , def : VariantDef) -> Vec < FieldDef > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; def . internal (& mut * tables , cx . tcx) . fields . iter () . map (| f | f . stable (& mut * tables , cx)) . collect () } # [doc = " Evaluate constant as a target usize."] fn eval_target_usize (& self , mir_const : & MirConst) -> Result < u64 , Error > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let cnst = mir_const . internal (& mut * tables , cx . tcx) ; cx . eval_target_usize (cnst) } fn eval_target_usize_ty (& self , ty_const : & TyConst) -> Result < u64 , Error > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let cnst = ty_const . internal (& mut * tables , cx . tcx) ; cx . eval_target_usize_ty (cnst) } # [doc = " Create a new zero-sized constant."] fn try_new_const_zst (& self , ty : Ty) -> Result < MirConst , Error > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let ty_internal = ty . internal (& mut * tables , cx . tcx) ; cx . try_new_const_zst (ty_internal) . map (| cnst | cnst . stable (& mut * tables , cx)) } # [doc = " Create a new constant that represents the given string value."] fn new_const_str (& self , value : & str) -> MirConst { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . new_const_str (value) . stable (& mut * tables , cx) } # [doc = " Create a new constant that represents the given boolean value."] fn new_const_bool (& self , value : bool) -> MirConst { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . new_const_bool (value) . stable (& mut * tables , cx) } # [doc = " Create a new constant that represents the given value."] fn try_new_const_uint (& self , value : u128 , uint_ty : UintTy) -> Result < MirConst , Error > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let ty = cx . ty_new_uint (uint_ty . internal (& mut * tables , cx . tcx)) ; cx . try_new_const_uint (value , ty) . map (| cnst | cnst . stable (& mut * tables , cx)) } fn try_new_ty_const_uint (& self , value : u128 , uint_ty : UintTy) -> Result < TyConst , Error > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let ty = cx . ty_new_uint (uint_ty . internal (& mut * tables , cx . tcx)) ; cx . try_new_ty_const_uint (value , ty) . map (| cnst | cnst . stable (& mut * tables , cx)) } # [doc = " Create a new type from the given kind."] fn new_rigid_ty (& self , kind : RigidTy) -> Ty { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let internal_kind = kind . internal (& mut * tables , cx . tcx) ; cx . new_rigid_ty (internal_kind) . stable (& mut * tables , cx) } # [doc = " Create a new box type, `Box<T>`, for the given inner type `T`."] fn new_box_ty (& self , ty : Ty) -> Ty { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let inner = ty . internal (& mut * tables , cx . tcx) ; cx . new_box_ty (inner) . stable (& mut * tables , cx) } # [doc = " Returns the type of given crate item."] fn def_ty (& self , item : DefId) -> Ty { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let inner = item . internal (& mut * tables , cx . tcx) ; cx . def_ty (inner) . stable (& mut * tables , cx) } # [doc = " Returns the type of given definition instantiated with the given arguments."] fn def_ty_with_args (& self , item : DefId , args : & GenericArgs) -> Ty { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let inner = item . internal (& mut * tables , cx . tcx) ; let args_ref = args . internal (& mut * tables , cx . tcx) ; cx . def_ty_with_args (inner , args_ref) . stable (& mut * tables , cx) } # [doc = " Returns literal value of a const as a string."] fn mir_const_pretty (& self , cnst : & MirConst) -> String { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cnst . internal (& mut * tables , cx . tcx) . to_string () } # [doc = " `Span` of an item."] fn span_of_an_item (& self , def_id : DefId) -> Span { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [def_id] ; cx . span_of_an_item (did) . stable (& mut * tables , cx) } fn ty_const_pretty (& self , ct : TyConstId) -> String { let tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . ty_const_pretty (tables . ty_consts [ct]) } # [doc = " Obtain the representation of a type."] fn ty_pretty (& self , ty : Ty) -> String { let tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . ty_pretty (tables . types [ty]) } # [doc = " Obtain the kind of a type."] fn ty_kind (& self , ty : Ty) -> TyKind { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; cx . ty_kind (tables . types [ty]) . stable (& mut * tables , cx) } # [doc = " Get the discriminant Ty for this Ty if there's one."] fn rigid_ty_discriminant_ty (& self , ty : & RigidTy) -> Ty { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let internal_kind = ty . internal (& mut * tables , cx . tcx) ; cx . rigid_ty_discriminant_ty (internal_kind) . stable (& mut * tables , cx) } # [doc = " Get the body of an Instance which is already monomorphized."] fn instance_body (& self , instance : InstanceDef) -> Option < Body > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let instance = tables . instances [instance] ; cx . instance_body (instance) . map (| body | body . stable (& mut * tables , cx)) } # [doc = " Get the instance type with generic instantiations applied and lifetimes erased."] fn instance_ty (& self , instance : InstanceDef) -> Ty { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let instance = tables . instances [instance] ; cx . instance_ty (instance) . stable (& mut * tables , cx) } # [doc = " Get the instantiation types."] fn instance_args (& self , def : InstanceDef) -> GenericArgs { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let instance = tables . instances [def] ; cx . instance_args (instance) . stable (& mut * tables , cx) } # [doc = " Get the instance."] fn instance_def_id (& self , instance : InstanceDef) -> DefId { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let instance = tables . instances [instance] ; cx . instance_def_id (instance , & mut * tables) } # [doc = " Get the instance mangled name."] fn instance_mangled_name (& self , instance : InstanceDef) -> Symbol { let tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let instance = tables . instances [instance] ; cx . instance_mangled_name (instance) } # [doc = " Check if this is an empty DropGlue shim."] fn is_empty_drop_shim (& self , def : InstanceDef) -> bool { let tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let instance = tables . instances [def] ; cx . is_empty_drop_shim (instance) } # [doc = " Convert a non-generic crate item into an instance."] # [doc = " This function will panic if the item is generic."] fn mono_instance (& self , def_id : DefId) -> Instance { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [def_id] ; cx . mono_instance (did) . stable (& mut * tables , cx) } # [doc = " Item requires monomorphization."] fn requires_monomorphization (& self , def_id : DefId) -> bool { let tables = self . tables . borrow () ; let cx = & * self . cx . borrow () ; let did = tables [def_id] ; cx . requires_monomorphization (did) } # [doc = " Resolve an instance from the given function definition and generic arguments."] fn resolve_instance (& self , def : FnDef , args : & GenericArgs) -> Option < Instance > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let def_id = def . 0 . internal (& mut * tables , cx . tcx) ; let args_ref = args . internal (& mut * tables , cx . tcx) ; cx . resolve_instance (def_id , args_ref) . map (| inst | inst . stable (& mut * tables , cx)) } # [doc = " Resolve an instance for drop_in_place for the given type."] fn resolve_drop_in_place (& self , ty : Ty) -> Instance { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let internal_ty = ty . internal (& mut * tables , cx . tcx) ; cx . resolve_drop_in_place (internal_ty) . stable (& mut * tables , cx) } # [doc = " Resolve instance for a function pointer."] fn resolve_for_fn_ptr (& self , def : FnDef , args : & GenericArgs) -> Option < Instance > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let def_id = def . 0 . internal (& mut * tables , cx . tcx) ; let args_ref = args . internal (& mut * tables , cx . tcx) ; cx . resolve_for_fn_ptr (def_id , args_ref) . stable (& mut * tables , cx) } # [doc = " Resolve instance for a closure with the requested type."] fn resolve_closure (& self , def : ClosureDef , args : & GenericArgs , kind : ClosureKind ,) -> Option < Instance > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let def_id = def . 0 . internal (& mut * tables , cx . tcx) ; let args_ref = args . internal (& mut * tables , cx . tcx) ; let closure_kind = kind . internal (& mut * tables , cx . tcx) ; cx . resolve_closure (def_id , args_ref , closure_kind) . map (| inst | inst . stable (& mut * tables , cx)) } # [doc = " Evaluate a static's initializer."] fn eval_static_initializer (& self , def : StaticDef) -> Result < Allocation , Error > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let def_id = def . 0 . internal (& mut * tables , cx . tcx) ; cx . eval_static_initializer (def_id) . stable (& mut * tables , cx) } # [doc = " Try to evaluate an instance into a constant."] fn eval_instance (& self , def : InstanceDef , const_ty : Ty) -> Result < Allocation , Error > { let mut tables = self . tables . borrow_mut () ; let instance = tables . instances [def] ; let cx = & * self . cx . borrow () ; let const_ty = const_ty . internal (& mut * tables , cx . tcx) ; cx . eval_instance (instance) . map (| const_val | alloc :: try_new_allocation (const_ty , const_val , & mut * tables , cx)) . map_err (| e | e . stable (& mut * tables , cx)) ? } # [doc = " Retrieve global allocation for the given allocation ID."] fn global_alloc (& self , id : AllocId) -> GlobalAlloc { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let alloc_id = id . internal (& mut * tables , cx . tcx) ; cx . global_alloc (alloc_id) . stable (& mut * tables , cx) } # [doc = " Retrieve the id for the virtual table."] fn vtable_allocation (& self , global_alloc : & GlobalAlloc) -> Option < AllocId > { let mut tables = self . tables . borrow_mut () ; let GlobalAlloc :: VTable (ty , trait_ref) = global_alloc else { return None ; } ; let cx = & * self . cx . borrow () ; let ty = ty . internal (& mut * tables , cx . tcx) ; let trait_ref = trait_ref . internal (& mut * tables , cx . tcx) ; let alloc_id = cx . vtable_allocation (ty , trait_ref) ; Some (alloc_id . stable (& mut * tables , cx)) } fn krate (& self , def_id : DefId) -> Crate { let tables = self . tables . borrow () ; let cx = & * self . cx . borrow () ; smir_crate (cx , tables [def_id] . krate) } fn instance_name (& self , def : InstanceDef , trimmed : bool) -> Symbol { let tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let instance = tables . instances [def] ; cx . instance_name (instance , trimmed) } # [doc = " Return information about the target machine."] fn target_info (& self) -> MachineInfo { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; MachineInfo { endian : cx . target_endian () . stable (& mut * tables , cx) , pointer_width : MachineSize :: from_bits (cx . target_pointer_size ()) , } } # [doc = " Get an instance ABI."] fn instance_abi (& self , def : InstanceDef) -> Result < FnAbi , Error > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let instance = tables . instances [def] ; cx . instance_abi (instance) . map (| fn_abi | fn_abi . stable (& mut * tables , cx)) } # [doc = " Get the ABI of a function pointer."] fn fn_ptr_abi (& self , fn_ptr : PolyFnSig) -> Result < FnAbi , Error > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let sig = fn_ptr . internal (& mut * tables , cx . tcx) ; cx . fn_ptr_abi (sig) . map (| fn_abi | fn_abi . stable (& mut * tables , cx)) } # [doc = " Get the layout of a type."] fn ty_layout (& self , ty : Ty) -> Result < Layout , Error > { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let internal_ty = ty . internal (& mut * tables , cx . tcx) ; cx . ty_layout (internal_ty) . map (| layout | layout . stable (& mut * tables , cx)) } # [doc = " Get the layout shape."] fn layout_shape (& self , id : Layout) -> LayoutShape { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; id . internal (& mut * tables , cx . tcx) . 0 . stable (& mut * tables , cx) } # [doc = " Get a debug string representation of a place."] fn place_pretty (& self , place : & Place) -> String { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; format ! ("{:?}" , place . internal (& mut * tables , cx . tcx)) } # [doc = " Get the resulting type of binary operation."] fn binop_ty (& self , bin_op : BinOp , rhs : Ty , lhs : Ty) -> Ty { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let rhs_internal = rhs . internal (& mut * tables , cx . tcx) ; let lhs_internal = lhs . internal (& mut * tables , cx . tcx) ; let bin_op_internal = bin_op . internal (& mut * tables , cx . tcx) ; cx . binop_ty (bin_op_internal , rhs_internal , lhs_internal) . stable (& mut * tables , cx) } # [doc = " Get the resulting type of unary operation."] fn unop_ty (& self , un_op : UnOp , arg : Ty) -> Ty { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let un_op = un_op . internal (& mut * tables , cx . tcx) ; let arg = arg . internal (& mut * tables , cx . tcx) ; cx . unop_ty (un_op , arg) . stable (& mut * tables , cx) } # [doc = " Get all associated items of a definition."] fn associated_items (& self , def_id : DefId) -> AssocItems { let mut tables = self . tables . borrow_mut () ; let cx = & * self . cx . borrow () ; let did = tables [def_id] ; cx . associated_items (did) . iter () . map (| assoc | assoc . stable (& mut * tables , cx)) . collect () } }
/* FP:compiler_interface.rs-0037 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_MACRO_0019
/* FP:compiler_interface.rs-0038 */ scoped_tls :: scoped_thread_local ! (static TLV : Cell <* const () >) ;
/* FP:compiler_interface.rs-0039 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_FN_0020
/* FP:compiler_interface.rs-0040 */ pub (crate) fn run < F , T > (interface : & dyn CompilerInterface , f : F) -> Result < T , Error > where F : FnOnce () -> T , { if TLV . is_set () { Err (Error :: from ("rustc_public already running")) } else { let ptr : * const () = (& raw const interface) as _ ; TLV . set (& Cell :: new (ptr) , | | Ok (f ())) } }
/* FP:compiler_interface.rs-0041 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_FN_0021
/* FP:compiler_interface.rs-0042 */ # [doc = " Execute the given function with access the [`CompilerInterface`]."] # [doc = ""] # [doc = " I.e., This function will load the current interface and calls a function with it."] # [doc = " Do not nest these, as that will ICE."] pub (crate) fn with < R > (f : impl FnOnce (& dyn CompilerInterface) -> R) -> R { assert ! (TLV . is_set ()) ; TLV . with (| tlv | { let ptr = tlv . get () ; assert ! (! ptr . is_null ()) ; f (unsafe { * (ptr as * const & dyn CompilerInterface) }) }) }
/* FP:compiler_interface.rs-0043 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_public_src_compiler_interface_FN_0022
/* FP:compiler_interface.rs-0044 */ fn smir_crate < 'tcx > (cx : & CompilerCtxt < 'tcx , BridgeTys > , crate_num : crate :: rustc_span :: def_id :: CrateNum ,) -> Crate { let name = cx . crate_name (crate_num) ; let is_local = cx . crate_is_local (crate_num) ; let id = cx . crate_num_id (crate_num) ; debug ! (? name , ? crate_num , "smir_crate") ; Crate { id , name , is_local } }