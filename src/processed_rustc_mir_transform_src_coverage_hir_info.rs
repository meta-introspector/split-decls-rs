/* FP:hir_info.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_coverage_hir_info_USE_0001
/* FP:hir_info.rs-0002 */ use rustc_hir as hir ;
/* FP:hir_info.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_coverage_hir_info_USE_0002
/* FP:hir_info.rs-0004 */ use crate :: rustc_complete :: intravisit :: { Visitor , walk_expr } ;
/* FP:hir_info.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_coverage_hir_info_USE_0003
/* FP:hir_info.rs-0006 */ use crate :: rustc_complete :: hir :: nested_filter ;
/* FP:hir_info.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_coverage_hir_info_USE_0004
/* FP:hir_info.rs-0008 */ use crate :: rustc_complete :: ty :: TyCtxt ;
/* FP:hir_info.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_coverage_hir_info_USE_0005
/* FP:hir_info.rs-0010 */ use crate :: rustc_complete :: Span ;
/* FP:hir_info.rs-0011 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_coverage_hir_info_USE_0006
/* FP:hir_info.rs-0012 */ use crate :: rustc_complete :: def_id :: LocalDefId ;
/* FP:hir_info.rs-0013 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_coverage_hir_info_STRUCT_0007
/* FP:hir_info.rs-0014 */ # [doc = " Function information extracted from HIR by the coverage instrumentor."] # [derive (Debug)] pub (crate) struct ExtractedHirInfo { pub (crate) function_source_hash : u64 , pub (crate) is_async_fn : bool , # [doc = " The span of the function's signature, if available."] # [doc = " Must have the same context and filename as the body span."] pub (crate) fn_sig_span : Option < Span > , pub (crate) body_span : Span , # [doc = " \"Holes\" are regions within the function body (or its expansions) that"] # [doc = " should not be included in coverage spans for this function"] # [doc = " (e.g. closures and nested items)."] pub (crate) hole_spans : Vec < Span > , }
/* FP:hir_info.rs-0015 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_coverage_hir_info_FN_0008
/* FP:hir_info.rs-0016 */ pub (crate) fn extract_hir_info < 'tcx > (tcx : TyCtxt < 'tcx > , def_id : LocalDefId) -> ExtractedHirInfo { if tcx . is_synthetic_mir (def_id) { return extract_hir_info (tcx , tcx . local_parent (def_id)) ; } let hir_node = tcx . hir_node_by_def_id (def_id) ; let fn_body_id = hir_node . body_id () . expect ("HIR node is a function with body") ; let hir_body = tcx . hir_body (fn_body_id) ; let maybe_fn_sig = hir_node . fn_sig () ; let is_async_fn = maybe_fn_sig . is_some_and (| fn_sig | fn_sig . header . is_async ()) ; let mut body_span = hir_body . value . span ; use hir :: { Closure , Expr , ExprKind , Node } ; if let Node :: Expr (& Expr { kind : ExprKind :: Closure (& Closure { fn_decl_span , .. }) , .. }) = hir_node { body_span = body_span . find_ancestor_in_same_ctxt (fn_decl_span) . unwrap_or (body_span) ; } let fn_sig_span = maybe_fn_sig . map (| fn_sig | fn_sig . span) . filter (| & fn_sig_span | { let source_map = tcx . sess . source_map () ; let file_idx = | span : Span | source_map . lookup_source_file_idx (span . lo ()) ; fn_sig_span . eq_ctxt (body_span) && fn_sig_span . hi () <= body_span . lo () && file_idx (fn_sig_span) == file_idx (body_span) }) ; let function_source_hash = hash_mir_source (tcx , hir_body) ; let hole_spans = extract_hole_spans_from_hir (tcx , hir_body) ; ExtractedHirInfo { function_source_hash , is_async_fn , fn_sig_span , body_span , hole_spans } }
/* FP:hir_info.rs-0017 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_coverage_hir_info_FN_0009
/* FP:hir_info.rs-0018 */ fn hash_mir_source < 'tcx > (tcx : TyCtxt < 'tcx > , hir_body : & 'tcx hir :: Body < 'tcx >) -> u64 { let owner = hir_body . id () . hir_id . owner ; tcx . hir_owner_nodes (owner) . opt_hash_including_bodies . expect ("hash should be present when coverage instrumentation is enabled") . to_smaller_hash () . as_u64 () }
/* FP:hir_info.rs-0019 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_coverage_hir_info_FN_0010
/* FP:hir_info.rs-0020 */ fn extract_hole_spans_from_hir < 'tcx > (tcx : TyCtxt < 'tcx > , hir_body : & hir :: Body < 'tcx >) -> Vec < Span > { struct HolesVisitor < 'tcx > { tcx : TyCtxt < 'tcx > , hole_spans : Vec < Span > , } impl < 'tcx > Visitor < 'tcx > for HolesVisitor < 'tcx > { # [doc = " We have special handling for nested items, but we still want to"] # [doc = " traverse into nested bodies of things that are not considered items,"] # [doc = " such as \"anon consts\" (e.g. array lengths)."] type NestedFilter = nested_filter :: OnlyBodies ; fn maybe_tcx (& mut self) -> TyCtxt < 'tcx > { self . tcx } # [doc = " We override `visit_nested_item` instead of `visit_item` because we"] # [doc = " only need the item's span, not the item itself."] fn visit_nested_item (& mut self , id : hir :: ItemId) -> Self :: Result { let span = self . tcx . def_span (id . owner_id . def_id) ; self . visit_hole_span (span) ; } fn visit_expr (& mut self , expr : & 'tcx hir :: Expr < 'tcx >) { match expr . kind { hir :: ExprKind :: Closure (_) | hir :: ExprKind :: ConstBlock (_) => { self . visit_hole_span (expr . span) ; } _ => walk_expr (self , expr) , } } } impl HolesVisitor < '_ > { fn visit_hole_span (& mut self , hole_span : Span) { self . hole_spans . push (hole_span) ; } } let mut visitor = HolesVisitor { tcx , hole_spans : vec ! [] } ; visitor . visit_body (hir_body) ; visitor . hole_spans }