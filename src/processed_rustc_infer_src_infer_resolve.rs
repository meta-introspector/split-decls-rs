/* FP:resolve.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_USE_0001
/* FP:resolve.rs-0002 */ use crate :: rustc_complete :: bug ;
/* FP:resolve.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_USE_0002
/* FP:resolve.rs-0004 */ use crate :: rustc_complete :: ty :: { self , Const , DelayedMap , FallibleTypeFolder , InferConst , Ty , TyCtxt , TypeFoldable , TypeFolder , TypeSuperFoldable , TypeVisitableExt , } ;
/* FP:resolve.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_USE_0003
/* FP:resolve.rs-0006 */ use super :: { FixupError , FixupResult , InferCtxt } ;
/* FP:resolve.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_USE_0004
/* FP:resolve.rs-0008 */ use crate :: infer :: TyOrConstInferVar ;
/* FP:resolve.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_STRUCT_0005
/* FP:resolve.rs-0010 */ # [doc = " The opportunistic resolver can be used at any time. It simply replaces"] # [doc = " type/const variables that have been unified with the things they have"] # [doc = " been unified with (similar to `shallow_resolve`, but deep). This is"] # [doc = " useful for printing messages etc but also required at various"] # [doc = " points for correctness."] pub struct OpportunisticVarResolver < 'a , 'tcx > { infcx : & 'a InferCtxt < 'tcx > , # [doc = " We're able to use a cache here as the folder does"] # [doc = " not have any mutable state."] cache : DelayedMap < Ty < 'tcx > , Ty < 'tcx > > , }
/* FP:resolve.rs-0011 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_IMPL_0006
/* FP:resolve.rs-0012 */ impl < 'a , 'tcx > OpportunisticVarResolver < 'a , 'tcx > { # [inline] pub fn new (infcx : & 'a InferCtxt < 'tcx >) -> Self { OpportunisticVarResolver { infcx , cache : Default :: default () } } }
/* FP:resolve.rs-0013 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_IMPL_0007
/* FP:resolve.rs-0014 */ impl < 'a , 'tcx > TypeFolder < TyCtxt < 'tcx > > for OpportunisticVarResolver < 'a , 'tcx > { fn cx (& self) -> TyCtxt < 'tcx > { self . infcx . tcx } # [inline] fn fold_ty (& mut self , t : Ty < 'tcx >) -> Ty < 'tcx > { if ! t . has_non_region_infer () { t } else if let Some (& ty) = self . cache . get (& t) { ty } else { let shallow = self . infcx . shallow_resolve (t) ; let res = shallow . super_fold_with (self) ; assert ! (self . cache . insert (t , res)) ; res } } fn fold_const (& mut self , ct : Const < 'tcx >) -> Const < 'tcx > { if ! ct . has_non_region_infer () { ct } else { let ct = self . infcx . shallow_resolve_const (ct) ; ct . super_fold_with (self) } } fn fold_predicate (& mut self , p : ty :: Predicate < 'tcx >) -> ty :: Predicate < 'tcx > { if ! p . has_non_region_infer () { p } else { p . super_fold_with (self) } } fn fold_clauses (& mut self , c : ty :: Clauses < 'tcx >) -> ty :: Clauses < 'tcx > { if ! c . has_non_region_infer () { c } else { c . super_fold_with (self) } } }
/* FP:resolve.rs-0015 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_STRUCT_0008
/* FP:resolve.rs-0016 */ # [doc = " The opportunistic region resolver opportunistically resolves regions"] # [doc = " variables to the variable with the least variable id. It is used when"] # [doc = " normalizing projections to avoid hitting the recursion limit by creating"] # [doc = " many versions of a predicate for types that in the end have to unify."] # [doc = ""] # [doc = " If you want to resolve type and const variables as well, call"] # [doc = " [InferCtxt::resolve_vars_if_possible] first."] pub struct OpportunisticRegionResolver < 'a , 'tcx > { infcx : & 'a InferCtxt < 'tcx > , }
/* FP:resolve.rs-0017 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_IMPL_0009
/* FP:resolve.rs-0018 */ impl < 'a , 'tcx > OpportunisticRegionResolver < 'a , 'tcx > { pub fn new (infcx : & 'a InferCtxt < 'tcx >) -> Self { OpportunisticRegionResolver { infcx } } }
/* FP:resolve.rs-0019 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_IMPL_0010
/* FP:resolve.rs-0020 */ impl < 'a , 'tcx > TypeFolder < TyCtxt < 'tcx > > for OpportunisticRegionResolver < 'a , 'tcx > { fn cx (& self) -> TyCtxt < 'tcx > { self . infcx . tcx } fn fold_ty (& mut self , t : Ty < 'tcx >) -> Ty < 'tcx > { if ! t . has_infer_regions () { t } else { t . super_fold_with (self) } } fn fold_region (& mut self , r : ty :: Region < 'tcx >) -> ty :: Region < 'tcx > { match r . kind () { ty :: ReVar (vid) => self . infcx . inner . borrow_mut () . unwrap_region_constraints () . opportunistic_resolve_var (TypeFolder :: cx (self) , vid) , _ => r , } } fn fold_const (& mut self , ct : ty :: Const < 'tcx >) -> ty :: Const < 'tcx > { if ! ct . has_infer_regions () { ct } else { ct . super_fold_with (self) } } }
/* FP:resolve.rs-0021 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_FN_0011
/* FP:resolve.rs-0022 */ # [doc = " Full type resolution replaces all type and region variables with"] # [doc = " their concrete results. If any variable cannot be replaced (never unified, etc)"] # [doc = " then an `Err` result is returned."] pub fn fully_resolve < 'tcx , T > (infcx : & InferCtxt < 'tcx > , value : T) -> FixupResult < T > where T : TypeFoldable < TyCtxt < 'tcx > > , { value . try_fold_with (& mut FullTypeResolver { infcx }) }
/* FP:resolve.rs-0023 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_STRUCT_0012
/* FP:resolve.rs-0024 */ struct FullTypeResolver < 'a , 'tcx > { infcx : & 'a InferCtxt < 'tcx > , }
/* FP:resolve.rs-0025 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_infer_src_infer_resolve_IMPL_0013
/* FP:resolve.rs-0026 */ impl < 'a , 'tcx > FallibleTypeFolder < TyCtxt < 'tcx > > for FullTypeResolver < 'a , 'tcx > { type Error = FixupError ; fn cx (& self) -> TyCtxt < 'tcx > { self . infcx . tcx } fn try_fold_ty (& mut self , t : Ty < 'tcx >) -> Result < Ty < 'tcx > , Self :: Error > { if ! t . has_infer () { Ok (t) } else { let t = self . infcx . shallow_resolve (t) ; match * t . kind () { ty :: Infer (ty :: TyVar (vid)) => { Err (FixupError { unresolved : TyOrConstInferVar :: Ty (vid) }) } ty :: Infer (ty :: IntVar (vid)) => { Err (FixupError { unresolved : TyOrConstInferVar :: TyInt (vid) }) } ty :: Infer (ty :: FloatVar (vid)) => { Err (FixupError { unresolved : TyOrConstInferVar :: TyFloat (vid) }) } ty :: Infer (_) => { bug ! ("Unexpected type in full type resolver: {:?}" , t) ; } _ => t . try_super_fold_with (self) , } } } fn try_fold_region (& mut self , r : ty :: Region < 'tcx >) -> Result < ty :: Region < 'tcx > , Self :: Error > { match r . kind () { ty :: ReVar (_) => Ok (self . infcx . lexical_region_resolutions . borrow () . as_ref () . expect ("region resolution not performed") . resolve_region (self . infcx . tcx , r)) , _ => Ok (r) , } } fn try_fold_const (& mut self , c : ty :: Const < 'tcx >) -> Result < ty :: Const < 'tcx > , Self :: Error > { if ! c . has_infer () { Ok (c) } else { let c = self . infcx . shallow_resolve_const (c) ; match c . kind () { ty :: ConstKind :: Infer (InferConst :: Var (vid)) => { return Err (FixupError { unresolved : super :: TyOrConstInferVar :: Const (vid) }) ; } ty :: ConstKind :: Infer (InferConst :: Fresh (_)) => { bug ! ("Unexpected const in full const resolver: {:?}" , c) ; } _ => { } } c . try_super_fold_with (self) } } }