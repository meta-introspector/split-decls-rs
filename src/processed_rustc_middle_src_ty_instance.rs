/* FP:instance.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0001
/* FP:instance.rs-0002 */ use std :: assert_matches :: assert_matches ;
/* FP:instance.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0002
/* FP:instance.rs-0004 */ use std :: fmt ;
/* FP:instance.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0003
/* FP:instance.rs-0006 */ use crate :: rustc_data_structures :: fx :: FxHashMap ;
/* FP:instance.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0004
/* FP:instance.rs-0008 */ use crate :: rustc_complete :: ErrorGuaranteed ;
/* FP:instance.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0005
/* FP:instance.rs-0010 */ use rustc_hir as hir ;
/* FP:instance.rs-0011 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0006
/* FP:instance.rs-0012 */ use crate :: rustc_complete :: def :: { CtorKind , DefKind , Namespace } ;
/* FP:instance.rs-0013 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0007
/* FP:instance.rs-0014 */ use crate :: rustc_complete :: def_id :: { CrateNum , DefId } ;
/* FP:instance.rs-0015 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0008
/* FP:instance.rs-0016 */ use crate :: rustc_complete :: lang_items :: LangItem ;
/* FP:instance.rs-0017 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0009
/* FP:instance.rs-0018 */ use crate :: rustc_index :: bit_set :: FiniteBitSet ;
/* FP:instance.rs-0019 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0010
/* FP:instance.rs-0020 */ use rustc_macros :: { Decodable , Encodable , HashStable , Lift , TyDecodable , TyEncodable } ;
/* FP:instance.rs-0021 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0011
/* FP:instance.rs-0022 */ use crate :: rustc_complete :: def_id :: LOCAL_CRATE ;
/* FP:instance.rs-0023 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0012
/* FP:instance.rs-0024 */ use crate :: rustc_complete :: { DUMMY_SP , Span , Symbol } ;
/* FP:instance.rs-0025 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0013
/* FP:instance.rs-0026 */ use tracing :: { debug , instrument } ;
/* FP:instance.rs-0027 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0014
/* FP:instance.rs-0028 */ use crate :: error ;
/* FP:instance.rs-0029 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0015
/* FP:instance.rs-0030 */ use crate :: middle :: codegen_fn_attrs :: CodegenFnAttrFlags ;
/* FP:instance.rs-0031 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0016
/* FP:instance.rs-0032 */ use crate :: ty :: normalize_erasing_regions :: NormalizationError ;
/* FP:instance.rs-0033 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0017
/* FP:instance.rs-0034 */ use crate :: ty :: print :: { FmtPrinter , Print } ;
/* FP:instance.rs-0035 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_USE_0018
/* FP:instance.rs-0036 */ use crate :: ty :: { self , AssocContainer , EarlyBinder , GenericArgs , GenericArgsRef , Ty , TyCtxt , TypeFoldable , TypeSuperVisitable , TypeVisitable , TypeVisitableExt , TypeVisitor , } ;
/* FP:instance.rs-0037 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_STRUCT_0019
/* FP:instance.rs-0038 */ # [doc = " An `InstanceKind` along with the args that are needed to substitute the instance."] # [doc = ""] # [doc = " Monomorphization happens on-the-fly and no monomorphized MIR is ever created. Instead, this type"] # [doc = " simply couples a potentially generic `InstanceKind` with some args, and codegen and const eval"] # [doc = " will do all required instantiations as they run."] # [doc = ""] # [doc = " Note: the `Lift` impl is currently not used by rustc, but is used by"] # [doc = " rustc_codegen_cranelift when the `jit` feature is enabled."] # [derive (Copy , Clone , PartialEq , Eq , Hash , Debug , TyEncodable , TyDecodable)] # [derive (HashStable , Lift , TypeFoldable , TypeVisitable)] pub struct Instance < 'tcx > { pub def : InstanceKind < 'tcx > , pub args : GenericArgsRef < 'tcx > , }
/* FP:instance.rs-0039 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_ENUM_0020
/* FP:instance.rs-0040 */ # [doc = " Describes why a `ReifyShim` was created. This is needed to distinguish a ReifyShim created to"] # [doc = " adjust for things like `#[track_caller]` in a vtable from a `ReifyShim` created to produce a"] # [doc = " function pointer from a vtable entry."] # [doc = " Currently, this is only used when KCFI is enabled, as only KCFI needs to treat those two"] # [doc = " `ReifyShim`s differently."] # [derive (Copy , Clone , PartialEq , Eq , Hash , Debug)] # [derive (TyEncodable , TyDecodable , HashStable)] pub enum ReifyReason { # [doc = " The `ReifyShim` was created to produce a function pointer. This happens when:"] # [doc = " * A vtable entry is directly converted to a function call (e.g. creating a fn ptr from a"] # [doc = "   method on a `dyn` object)."] # [doc = " * A function with `#[track_caller]` is converted to a function pointer"] # [doc = " * If KCFI is enabled, creating a function pointer from a method on a dyn-compatible trait."] # [doc = " This includes the case of converting `::call`-like methods on closure-likes to function"] # [doc = " pointers."] FnPtr , # [doc = " This `ReifyShim` was created to populate a vtable. Currently, this happens when a"] # [doc = " `#[track_caller]` mismatch occurs between the implementation of a method and the method."] # [doc = " This includes the case of `::call`-like methods in closure-likes' vtables."] Vtable , }
/* FP:instance.rs-0041 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_ENUM_0021
/* FP:instance.rs-0042 */ # [derive (Copy , Clone , PartialEq , Eq , Hash , Debug)] # [derive (TyEncodable , TyDecodable , HashStable , TypeFoldable , TypeVisitable , Lift)] pub enum InstanceKind < 'tcx > { # [doc = " A user-defined callable item."] # [doc = ""] # [doc = " This includes:"] # [doc = " - `fn` items"] # [doc = " - closures"] # [doc = " - coroutines"] Item (DefId) , # [doc = " An intrinsic `fn` item (with`#[rustc_intrinsic]`)."] # [doc = ""] # [doc = " Alongside `Virtual`, this is the only `InstanceKind` that does not have its own callable MIR."] # [doc = " Instead, codegen and const eval \"magically\" evaluate calls to intrinsics purely in the"] # [doc = " caller."] Intrinsic (DefId) , # [doc = " `<T as Trait>::method` where `method` receives unsizeable `self: Self` (part of the"] # [doc = " `unsized_fn_params` feature)."] # [doc = ""] # [doc = " The generated shim will take `Self` via `*mut Self` - conceptually this is `&owned Self` -"] # [doc = " and dereference the argument to call the original function."] VTableShim (DefId) , # [doc = " `fn()` pointer where the function itself cannot be turned into a pointer."] # [doc = ""] # [doc = " One example is `<dyn Trait as Trait>::fn`, where the shim contains"] # [doc = " a virtual call, which codegen supports only via a direct call to the"] # [doc = " `<dyn Trait as Trait>::fn` instance (an `InstanceKind::Virtual`)."] # [doc = ""] # [doc = " Another example is functions annotated with `#[track_caller]`, which"] # [doc = " must have their implicit caller location argument populated for a call."] # [doc = " Because this is a required part of the function's ABI but can't be tracked"] # [doc = " as a property of the function pointer, we use a single \"caller location\""] # [doc = " (the definition of the function itself)."] # [doc = ""] # [doc = " The second field encodes *why* this shim was created. This allows distinguishing between"] # [doc = " a `ReifyShim` that appears in a vtable vs one that appears as a function pointer."] # [doc = ""] # [doc = " This field will only be populated if we are compiling in a mode that needs these shims"] # [doc = " to be separable, currently only when KCFI is enabled."] ReifyShim (DefId , Option < ReifyReason >) , # [doc = " `<fn() as FnTrait>::call_*` (generated `FnTrait` implementation for `fn()` pointers)."] # [doc = ""] # [doc = " `DefId` is `FnTrait::call_*`."] FnPtrShim (DefId , Ty < 'tcx >) , # [doc = " Dynamic dispatch to `<dyn Trait as Trait>::fn`."] # [doc = ""] # [doc = " This `InstanceKind` may have a callable MIR as the default implementation."] # [doc = " Calls to `Virtual` instances must be codegen'd as virtual calls through the vtable."] # [doc = " *This means we might not know exactly what is being called.*"] # [doc = ""] # [doc = " If this is reified to a `fn` pointer, a `ReifyShim` is used (see `ReifyShim` above for more"] # [doc = " details on that)."] Virtual (DefId , usize) , # [doc = " `<[FnMut/Fn closure] as FnOnce>::call_once`."] # [doc = ""] # [doc = " The `DefId` is the ID of the `call_once` method in `FnOnce`."] # [doc = ""] # [doc = " This generates a body that will just borrow the (owned) self type,"] # [doc = " and dispatch to the `FnMut::call_mut` instance for the closure."] ClosureOnceShim { call_once : DefId , track_caller : bool } , # [doc = " `<[FnMut/Fn coroutine-closure] as FnOnce>::call_once`"] # [doc = ""] # [doc = " The body generated here differs significantly from the `ClosureOnceShim`,"] # [doc = " since we need to generate a distinct coroutine type that will move the"] # [doc = " closure's upvars *out* of the closure."] ConstructCoroutineInClosureShim { coroutine_closure_def_id : DefId , receiver_by_ref : bool , } , # [doc = " Compiler-generated accessor for thread locals which returns a reference to the thread local"] # [doc = " the `DefId` defines. This is used to export thread locals from dylibs on platforms lacking"] # [doc = " native support."] ThreadLocalShim (DefId) , # [doc = " Proxy shim for async drop of future (def_id, proxy_cor_ty, impl_cor_ty)"] FutureDropPollShim (DefId , Ty < 'tcx > , Ty < 'tcx >) , # [doc = " `core::ptr::drop_in_place::<T>`."] # [doc = ""] # [doc = " The `DefId` is for `core::ptr::drop_in_place`."] # [doc = " The `Option<Ty<'tcx>>` is either `Some(T)`, or `None` for empty drop"] # [doc = " glue."] DropGlue (DefId , Option < Ty < 'tcx > >) , # [doc = " Compiler-generated `<T as Clone>::clone` implementation."] # [doc = ""] # [doc = " For all types that automatically implement `Copy`, a trivial `Clone` impl is provided too."] # [doc = " Additionally, arrays, tuples, and closures get a `Clone` shim even if they aren't `Copy`."] # [doc = ""] # [doc = " The `DefId` is for `Clone::clone`, the `Ty` is the type `T` with the builtin `Clone` impl."] CloneShim (DefId , Ty < 'tcx >) , # [doc = " Compiler-generated `<T as FnPtr>::addr` implementation."] # [doc = ""] # [doc = " Automatically generated for all potentially higher-ranked `fn(I) -> R` types."] # [doc = ""] # [doc = " The `DefId` is for `FnPtr::addr`, the `Ty` is the type `T`."] FnPtrAddrShim (DefId , Ty < 'tcx >) , # [doc = " `core::future::async_drop::async_drop_in_place::<'_, T>`."] # [doc = ""] # [doc = " The `DefId` is for `core::future::async_drop::async_drop_in_place`, the `Ty`"] # [doc = " is the type `T`."] AsyncDropGlueCtorShim (DefId , Ty < 'tcx >) , # [doc = " `core::future::async_drop::async_drop_in_place::<'_, T>::{closure}`."] # [doc = ""] # [doc = " async_drop_in_place poll function implementation (for generated coroutine)."] # [doc = " `Ty` here is `async_drop_in_place<T>::{closure}` coroutine type, not just `T`"] AsyncDropGlue (DefId , Ty < 'tcx >) , }
/* FP:instance.rs-0043 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_IMPL_0022
/* FP:instance.rs-0044 */ impl < 'tcx > Instance < 'tcx > { # [doc = " Returns the `Ty` corresponding to this `Instance`, with generic instantiations applied and"] # [doc = " lifetimes erased, allowing a `ParamEnv` to be specified for use during normalization."] pub fn ty (& self , tcx : TyCtxt < 'tcx > , typing_env : ty :: TypingEnv < 'tcx >) -> Ty < 'tcx > { let ty = tcx . type_of (self . def . def_id ()) ; tcx . instantiate_and_normalize_erasing_regions (self . args , typing_env , ty) } # [doc = " Finds a crate that contains a monomorphization of this instance that"] # [doc = " can be linked to from the local crate. A return value of `None` means"] # [doc = " no upstream crate provides such an exported monomorphization."] # [doc = ""] # [doc = " This method already takes into account the global `-Zshare-generics`"] # [doc = " setting, always returning `None` if `share-generics` is off."] pub fn upstream_monomorphization (& self , tcx : TyCtxt < 'tcx >) -> Option < CrateNum > { if self . def_id () . is_local () { return None ; } if ! tcx . sess . opts . share_generics () && tcx . codegen_fn_attrs (self . def_id ()) . inline != crate :: rustc_hir :: attrs :: InlineAttr :: Never { return None ; } self . args . non_erasable_generics () . next () ? ; if tcx . is_compiler_builtins (LOCAL_CRATE) { return None ; } match self . def { InstanceKind :: Item (def) => tcx . upstream_monomorphizations_for (def) . and_then (| monos | monos . get (& self . args) . cloned ()) , InstanceKind :: DropGlue (_ , Some (_)) => tcx . upstream_drop_glue_for (self . args) , InstanceKind :: AsyncDropGlue (_ , _) => None , InstanceKind :: FutureDropPollShim (_ , _ , _) => None , InstanceKind :: AsyncDropGlueCtorShim (_ , _) => { tcx . upstream_async_drop_glue_for (self . args) } _ => None , } } }
/* FP:instance.rs-0045 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_IMPL_0023
/* FP:instance.rs-0046 */ impl < 'tcx > InstanceKind < 'tcx > { # [inline] pub fn def_id (self) -> DefId { match self { InstanceKind :: Item (def_id) | InstanceKind :: VTableShim (def_id) | InstanceKind :: ReifyShim (def_id , _) | InstanceKind :: FnPtrShim (def_id , _) | InstanceKind :: Virtual (def_id , _) | InstanceKind :: Intrinsic (def_id) | InstanceKind :: ThreadLocalShim (def_id) | InstanceKind :: ClosureOnceShim { call_once : def_id , track_caller : _ } | ty :: InstanceKind :: ConstructCoroutineInClosureShim { coroutine_closure_def_id : def_id , receiver_by_ref : _ , } | InstanceKind :: DropGlue (def_id , _) | InstanceKind :: CloneShim (def_id , _) | InstanceKind :: FnPtrAddrShim (def_id , _) | InstanceKind :: FutureDropPollShim (def_id , _ , _) | InstanceKind :: AsyncDropGlue (def_id , _) | InstanceKind :: AsyncDropGlueCtorShim (def_id , _) => def_id , } } # [doc = " Returns the `DefId` of instances which might not require codegen locally."] pub fn def_id_if_not_guaranteed_local_codegen (self) -> Option < DefId > { match self { ty :: InstanceKind :: Item (def) => Some (def) , ty :: InstanceKind :: DropGlue (def_id , Some (_)) | InstanceKind :: AsyncDropGlueCtorShim (def_id , _) | InstanceKind :: AsyncDropGlue (def_id , _) | InstanceKind :: FutureDropPollShim (def_id , ..) | InstanceKind :: ThreadLocalShim (def_id) => Some (def_id) , InstanceKind :: VTableShim (..) | InstanceKind :: ReifyShim (..) | InstanceKind :: FnPtrShim (..) | InstanceKind :: Virtual (..) | InstanceKind :: Intrinsic (..) | InstanceKind :: ClosureOnceShim { .. } | ty :: InstanceKind :: ConstructCoroutineInClosureShim { .. } | InstanceKind :: DropGlue (..) | InstanceKind :: CloneShim (..) | InstanceKind :: FnPtrAddrShim (..) => None , } } # [inline] pub fn get_attrs (& self , tcx : TyCtxt < 'tcx > , attr : Symbol ,) -> impl Iterator < Item = & 'tcx hir :: Attribute > { tcx . get_attrs (self . def_id () , attr) } # [doc = " Returns `true` if the LLVM version of this instance is unconditionally"] # [doc = " marked with `inline`. This implies that a copy of this instance is"] # [doc = " generated in every codegen unit."] # [doc = " Note that this is only a hint. See the documentation for"] # [doc = " `generates_cgu_internal_copy` for more information."] pub fn requires_inline (& self , tcx : TyCtxt < 'tcx >) -> bool { use crate :: rustc_complete :: definitions :: DefPathData ; let def_id = match * self { ty :: InstanceKind :: Item (def) => def , ty :: InstanceKind :: DropGlue (_ , Some (_)) => return false , ty :: InstanceKind :: AsyncDropGlueCtorShim (_ , ty) => return ty . is_coroutine () , ty :: InstanceKind :: FutureDropPollShim (_ , _ , _) => return false , ty :: InstanceKind :: AsyncDropGlue (_ , _) => return false , ty :: InstanceKind :: ThreadLocalShim (_) => return false , _ => return true , } ; matches ! (tcx . def_key (def_id) . disambiguated_data . data , DefPathData :: Ctor | DefPathData :: Closure) } pub fn requires_caller_location (& self , tcx : TyCtxt < '_ >) -> bool { match * self { InstanceKind :: Item (def_id) | InstanceKind :: Virtual (def_id , _) => { tcx . body_codegen_attrs (def_id) . flags . contains (CodegenFnAttrFlags :: TRACK_CALLER) } InstanceKind :: ClosureOnceShim { call_once : _ , track_caller } => track_caller , _ => false , } } # [doc = " Returns `true` when the MIR body associated with this instance should be monomorphized"] # [doc = " by its users (e.g. codegen or miri) by instantiating the `args` from `Instance` (see"] # [doc = " `Instance::args_for_mir_body`)."] # [doc = ""] # [doc = " Otherwise, returns `false` only for some kinds of shims where the construction of the MIR"] # [doc = " body should perform necessary instantiations."] pub fn has_polymorphic_mir_body (& self) -> bool { match * self { InstanceKind :: CloneShim (..) | InstanceKind :: ThreadLocalShim (..) | InstanceKind :: FnPtrAddrShim (..) | InstanceKind :: FnPtrShim (..) | InstanceKind :: DropGlue (_ , Some (_)) | InstanceKind :: FutureDropPollShim (..) | InstanceKind :: AsyncDropGlue (_ , _) => false , InstanceKind :: AsyncDropGlueCtorShim (_ , _) => false , InstanceKind :: ClosureOnceShim { .. } | InstanceKind :: ConstructCoroutineInClosureShim { .. } | InstanceKind :: DropGlue (..) | InstanceKind :: Item (_) | InstanceKind :: Intrinsic (..) | InstanceKind :: ReifyShim (..) | InstanceKind :: Virtual (..) | InstanceKind :: VTableShim (..) => true , } } }
/* FP:instance.rs-0047 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_FN_0024
/* FP:instance.rs-0048 */ fn type_length < 'tcx > (item : impl TypeVisitable < TyCtxt < 'tcx > >) -> usize { struct Visitor < 'tcx > { type_length : usize , cache : FxHashMap < Ty < 'tcx > , usize > , } impl < 'tcx > TypeVisitor < TyCtxt < 'tcx > > for Visitor < 'tcx > { fn visit_ty (& mut self , t : Ty < 'tcx >) { if let Some (& value) = self . cache . get (& t) { self . type_length += value ; return ; } let prev = self . type_length ; self . type_length += 1 ; t . super_visit_with (self) ; if self . type_length > 16 { self . cache . insert (t , self . type_length - prev) ; } } fn visit_const (& mut self , ct : ty :: Const < 'tcx >) { self . type_length += 1 ; ct . super_visit_with (self) ; } } let mut visitor = Visitor { type_length : 0 , cache : Default :: default () } ; item . visit_with (& mut visitor) ; visitor . type_length }
/* FP:instance.rs-0049 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_IMPL_0025
/* FP:instance.rs-0050 */ impl < 'tcx > fmt :: Display for Instance < 'tcx > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { ty :: tls :: with (| tcx | { let mut p = FmtPrinter :: new (tcx , Namespace :: ValueNS) ; let instance = tcx . lift (* self) . expect ("could not lift for printing") ; instance . print (& mut p) ? ; let s = p . into_buffer () ; f . write_str (& s) }) } }
/* FP:instance.rs-0051 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_FN_0026
/* FP:instance.rs-0052 */ fn resolve_async_drop_poll < 'tcx > (mut cor_ty : Ty < 'tcx >) -> Instance < 'tcx > { let first_cor = cor_ty ; let ty :: Coroutine (poll_def_id , proxy_args) = first_cor . kind () else { bug ! () ; } ; let poll_def_id = * poll_def_id ; let mut child_ty = cor_ty ; loop { if let ty :: Coroutine (child_def , child_args) = child_ty . kind () { cor_ty = child_ty ; if * child_def == poll_def_id { child_ty = child_args . first () . unwrap () . expect_ty () ; continue ; } else { return Instance { def : ty :: InstanceKind :: FutureDropPollShim (poll_def_id , first_cor , cor_ty) , args : proxy_args , } ; } } else { let ty :: Coroutine (_ , child_args) = cor_ty . kind () else { bug ! () ; } ; if first_cor != cor_ty { return Instance { def : ty :: InstanceKind :: FutureDropPollShim (poll_def_id , first_cor , cor_ty) , args : proxy_args , } ; } else { return Instance { def : ty :: InstanceKind :: AsyncDropGlue (poll_def_id , cor_ty) , args : child_args , } ; } } } }
/* FP:instance.rs-0053 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_IMPL_0027
/* FP:instance.rs-0054 */ impl < 'tcx > Instance < 'tcx > { # [doc = " Creates a new [`InstanceKind::Item`] from the `def_id` and `args`."] # [doc = ""] # [doc = " Note that this item corresponds to the body of `def_id` directly, which"] # [doc = " likely does not make sense for trait items which need to be resolved to an"] # [doc = " implementation, and which may not even have a body themselves. Usages of"] # [doc = " this function should probably use [`Instance::expect_resolve`], or if run"] # [doc = " in a polymorphic environment or within a lint (that may encounter ambiguity)"] # [doc = " [`Instance::try_resolve`] instead."] pub fn new_raw (def_id : DefId , args : GenericArgsRef < 'tcx >) -> Instance < 'tcx > { assert ! (! args . has_escaping_bound_vars () , "args of instance {def_id:?} has escaping bound vars: {args:?}") ; Instance { def : InstanceKind :: Item (def_id) , args } } pub fn mono (tcx : TyCtxt < 'tcx > , def_id : DefId) -> Instance < 'tcx > { let args = GenericArgs :: for_item (tcx , def_id , | param , _ | match param . kind { ty :: GenericParamDefKind :: Lifetime => tcx . lifetimes . re_erased . into () , ty :: GenericParamDefKind :: Type { .. } => { bug ! ("Instance::mono: {:?} has type parameters" , def_id) } ty :: GenericParamDefKind :: Const { .. } => { bug ! ("Instance::mono: {:?} has const parameters" , def_id) } }) ; Instance :: new_raw (def_id , args) } # [inline] pub fn def_id (& self) -> DefId { self . def . def_id () } # [doc = " Resolves a `(def_id, args)` pair to an (optional) instance -- most commonly,"] # [doc = " this is used to find the precise code that will run for a trait method invocation,"] # [doc = " if known. This should only be used for functions and consts. If you want to"] # [doc = " resolve an associated type, use [`TyCtxt::try_normalize_erasing_regions`]."] # [doc = ""] # [doc = " Returns `Ok(None)` if we cannot resolve `Instance` to a specific instance."] # [doc = " For example, in a context like this,"] # [doc = ""] # [doc = " ```ignore (illustrative)"] # [doc = " fn foo<T: Debug>(t: T) { ... }"] # [doc = " ```"] # [doc = ""] # [doc = " trying to resolve `Debug::fmt` applied to `T` will yield `Ok(None)`, because we do not"] # [doc = " know what code ought to run. This setting is also affected by the current `TypingMode`"] # [doc = " of the environment."] # [doc = ""] # [doc = " Presuming that coherence and type-check have succeeded, if this method is invoked"] # [doc = " in a monomorphic context (i.e., like during codegen), then it is guaranteed to return"] # [doc = " `Ok(Some(instance))`, **except** for when the instance's inputs hit the type size limit,"] # [doc = " in which case it may bail out and return `Ok(None)`."] # [doc = ""] # [doc = " Returns `Err(ErrorGuaranteed)` when the `Instance` resolution process"] # [doc = " couldn't complete due to errors elsewhere - this is distinct"] # [doc = " from `Ok(None)` to avoid misleading diagnostics when an error"] # [doc = " has already been/will be emitted, for the original cause"] # [instrument (level = "debug" , skip (tcx) , ret)] pub fn try_resolve (tcx : TyCtxt < 'tcx > , typing_env : ty :: TypingEnv < 'tcx > , def_id : DefId , args : GenericArgsRef < 'tcx > ,) -> Result < Option < Instance < 'tcx > > , ErrorGuaranteed > { assert_matches ! (tcx . def_kind (def_id) , DefKind :: Fn | DefKind :: AssocFn | DefKind :: Const | DefKind :: AssocConst | DefKind :: AnonConst | DefKind :: InlineConst | DefKind :: Static { .. } | DefKind :: Ctor (_ , CtorKind :: Fn) | DefKind :: Closure | DefKind :: SyntheticCoroutineBody , "`Instance::try_resolve` should only be used to resolve instances of \
/* FP:instance.rs-0055 */             functions, statics, and consts; to resolve associated types, use \
/* FP:instance.rs-0056 */             `try_normalize_erasing_regions`.") ; if tcx . sess . opts . unstable_opts . enforce_type_length_limit && ! tcx . type_length_limit () . value_within_limit (type_length (args)) { return Ok (None) ; } tcx . resolve_instance_raw (tcx . erase_and_anonymize_regions (typing_env . as_query_input ((def_id , args))) ,) } pub fn expect_resolve (tcx : TyCtxt < 'tcx > , typing_env : ty :: TypingEnv < 'tcx > , def_id : DefId , args : GenericArgsRef < 'tcx > , span : Span ,) -> Instance < 'tcx > { let span_or_local_def_span = | | if span . is_dummy () && def_id . is_local () { tcx . def_span (def_id) } else { span } ; match ty :: Instance :: try_resolve (tcx , typing_env , def_id , args) { Ok (Some (instance)) => instance , Ok (None) => { let type_length = type_length (args) ; if ! tcx . type_length_limit () . value_within_limit (type_length) { tcx . dcx () . emit_fatal (error :: TypeLengthLimit { span : span_or_local_def_span () , instance : Instance :: new_raw (def_id , args) , type_length , }) ; } else { span_bug ! (span_or_local_def_span () , "failed to resolve instance for {}" , tcx . def_path_str_with_args (def_id , args)) } } instance => span_bug ! (span_or_local_def_span () , "failed to resolve instance for {}: {instance:#?}" , tcx . def_path_str_with_args (def_id , args)) , } } pub fn resolve_for_fn_ptr (tcx : TyCtxt < 'tcx > , typing_env : ty :: TypingEnv < 'tcx > , def_id : DefId , args : GenericArgsRef < 'tcx > ,) -> Option < Instance < 'tcx > > { debug ! ("resolve(def_id={:?}, args={:?})" , def_id , args) ; assert ! (! tcx . is_closure_like (def_id) , "Called `resolve_for_fn_ptr` on closure: {def_id:?}") ; let reason = tcx . sess . is_sanitizer_kcfi_enabled () . then_some (ReifyReason :: FnPtr) ; Instance :: try_resolve (tcx , typing_env , def_id , args) . ok () . flatten () . map (| mut resolved | { match resolved . def { InstanceKind :: Item (def) if resolved . def . requires_caller_location (tcx) => { debug ! (" => fn pointer created for function with #[track_caller]") ; resolved . def = InstanceKind :: ReifyShim (def , reason) ; } InstanceKind :: Virtual (def_id , _) => { debug ! (" => fn pointer created for virtual call") ; resolved . def = InstanceKind :: ReifyShim (def_id , reason) ; } _ if tcx . sess . is_sanitizer_kcfi_enabled () => { if tcx . is_closure_like (resolved . def_id ()) { resolved = Instance { def : InstanceKind :: ReifyShim (def_id , reason) , args } } else if let Some (assoc) = tcx . opt_associated_item (def_id) && let AssocContainer :: Trait | AssocContainer :: TraitImpl (Ok (_)) = assoc . container { resolved . def = InstanceKind :: ReifyShim (resolved . def_id () , reason) } } _ => { } } resolved }) } pub fn expect_resolve_for_vtable (tcx : TyCtxt < 'tcx > , typing_env : ty :: TypingEnv < 'tcx > , def_id : DefId , args : GenericArgsRef < 'tcx > , span : Span ,) -> Instance < 'tcx > { debug ! ("resolve_for_vtable(def_id={:?}, args={:?})" , def_id , args) ; let fn_sig = tcx . fn_sig (def_id) . instantiate_identity () ; let is_vtable_shim = ! fn_sig . inputs () . skip_binder () . is_empty () && fn_sig . input (0) . skip_binder () . is_param (0) && tcx . generics_of (def_id) . has_self ; if is_vtable_shim { debug ! (" => associated item with unsizeable self: Self") ; return Instance { def : InstanceKind :: VTableShim (def_id) , args } ; } let mut resolved = Instance :: expect_resolve (tcx , typing_env , def_id , args , span) ; let reason = tcx . sess . is_sanitizer_kcfi_enabled () . then_some (ReifyReason :: Vtable) ; match resolved . def { InstanceKind :: Item (def) => { let needs_track_caller_shim = resolved . def . requires_caller_location (tcx) && ! tcx . should_inherit_track_caller (def) && ! matches ! (tcx . opt_associated_item (def) , Some (ty :: AssocItem { container : ty :: AssocContainer :: Trait , .. })) ; if needs_track_caller_shim { if tcx . is_closure_like (def) { debug ! (" => vtable fn pointer created for closure with #[track_caller]: {:?} for method {:?} {:?}" , def , def_id , args) ; resolved = Instance { def : InstanceKind :: ReifyShim (def_id , reason) , args } ; } else { debug ! (" => vtable fn pointer created for function with #[track_caller]: {:?}" , def) ; resolved . def = InstanceKind :: ReifyShim (def , reason) ; } } } InstanceKind :: Virtual (def_id , _) => { debug ! (" => vtable fn pointer created for virtual call") ; resolved . def = InstanceKind :: ReifyShim (def_id , reason) } _ => { } } resolved } pub fn resolve_closure (tcx : TyCtxt < 'tcx > , def_id : DefId , args : ty :: GenericArgsRef < 'tcx > , requested_kind : ty :: ClosureKind ,) -> Instance < 'tcx > { let actual_kind = args . as_closure () . kind () ; match needs_fn_once_adapter_shim (actual_kind , requested_kind) { Ok (true) => Instance :: fn_once_adapter_instance (tcx , def_id , args) , _ => Instance :: new_raw (def_id , args) , } } pub fn resolve_drop_in_place (tcx : TyCtxt < 'tcx > , ty : Ty < 'tcx >) -> ty :: Instance < 'tcx > { let def_id = tcx . require_lang_item (LangItem :: DropInPlace , DUMMY_SP) ; let args = tcx . mk_args (& [ty . into ()]) ; Instance :: expect_resolve (tcx , ty :: TypingEnv :: fully_monomorphized () , def_id , args , ty . ty_adt_def () . and_then (| adt | tcx . hir_span_if_local (adt . did ())) . unwrap_or (DUMMY_SP) ,) } pub fn resolve_async_drop_in_place (tcx : TyCtxt < 'tcx > , ty : Ty < 'tcx >) -> ty :: Instance < 'tcx > { let def_id = tcx . require_lang_item (LangItem :: AsyncDropInPlace , DUMMY_SP) ; let args = tcx . mk_args (& [ty . into ()]) ; Instance :: expect_resolve (tcx , ty :: TypingEnv :: fully_monomorphized () , def_id , args , ty . ty_adt_def () . and_then (| adt | tcx . hir_span_if_local (adt . did ())) . unwrap_or (DUMMY_SP) ,) } pub fn resolve_async_drop_in_place_poll (tcx : TyCtxt < 'tcx > , def_id : DefId , ty : Ty < 'tcx > ,) -> ty :: Instance < 'tcx > { let args = tcx . mk_args (& [ty . into ()]) ; Instance :: expect_resolve (tcx , ty :: TypingEnv :: fully_monomorphized () , def_id , args , DUMMY_SP) } # [instrument (level = "debug" , skip (tcx) , ret)] pub fn fn_once_adapter_instance (tcx : TyCtxt < 'tcx > , closure_did : DefId , args : ty :: GenericArgsRef < 'tcx > ,) -> Instance < 'tcx > { let fn_once = tcx . require_lang_item (LangItem :: FnOnce , DUMMY_SP) ; let call_once = tcx . associated_items (fn_once) . in_definition_order () . find (| it | it . is_fn ()) . unwrap () . def_id ; let track_caller = tcx . codegen_fn_attrs (closure_did) . flags . contains (CodegenFnAttrFlags :: TRACK_CALLER) ; let def = ty :: InstanceKind :: ClosureOnceShim { call_once , track_caller } ; let self_ty = Ty :: new_closure (tcx , closure_did , args) ; let tupled_inputs_ty = args . as_closure () . sig () . map_bound (| sig | sig . inputs () [0]) ; let tupled_inputs_ty = tcx . instantiate_bound_regions_with_erased (tupled_inputs_ty) ; let args = tcx . mk_args_trait (self_ty , [tupled_inputs_ty . into ()]) ; debug ! (? self_ty , args =? tupled_inputs_ty . tuple_fields ()) ; Instance { def , args } } pub fn try_resolve_item_for_coroutine (tcx : TyCtxt < 'tcx > , trait_item_id : DefId , trait_id : DefId , rcvr_args : ty :: GenericArgsRef < 'tcx > ,) -> Option < Instance < 'tcx > > { let ty :: Coroutine (coroutine_def_id , args) = * rcvr_args . type_at (0) . kind () else { return None ; } ; let coroutine_kind = tcx . coroutine_kind (coroutine_def_id) . unwrap () ; let coroutine_callable_item = if tcx . is_lang_item (trait_id , LangItem :: Future) { assert_matches ! (coroutine_kind , hir :: CoroutineKind :: Desugared (hir :: CoroutineDesugaring :: Async , _)) ; hir :: LangItem :: FuturePoll } else if tcx . is_lang_item (trait_id , LangItem :: Iterator) { assert_matches ! (coroutine_kind , hir :: CoroutineKind :: Desugared (hir :: CoroutineDesugaring :: Gen , _)) ; hir :: LangItem :: IteratorNext } else if tcx . is_lang_item (trait_id , LangItem :: AsyncIterator) { assert_matches ! (coroutine_kind , hir :: CoroutineKind :: Desugared (hir :: CoroutineDesugaring :: AsyncGen , _)) ; hir :: LangItem :: AsyncIteratorPollNext } else if tcx . is_lang_item (trait_id , LangItem :: Coroutine) { assert_matches ! (coroutine_kind , hir :: CoroutineKind :: Coroutine (_)) ; hir :: LangItem :: CoroutineResume } else { return None ; } ; if tcx . is_lang_item (trait_item_id , coroutine_callable_item) { if tcx . is_async_drop_in_place_coroutine (coroutine_def_id) { return Some (resolve_async_drop_poll (rcvr_args . type_at (0))) ; } let ty :: Coroutine (_ , id_args) = * tcx . type_of (coroutine_def_id) . skip_binder () . kind () else { bug ! () } ; if args . as_coroutine () . kind_ty () == id_args . as_coroutine () . kind_ty () { Some (Instance { def : ty :: InstanceKind :: Item (coroutine_def_id) , args }) } else { Some (Instance { def : ty :: InstanceKind :: Item (tcx . coroutine_by_move_body_def_id (coroutine_def_id) ,) , args , }) } } else { debug_assert ! (tcx . defaultness (trait_item_id) . has_value ()) ; Some (Instance :: new_raw (trait_item_id , rcvr_args)) } } # [doc = " Depending on the kind of `InstanceKind`, the MIR body associated with an"] # [doc = " instance is expressed in terms of the generic parameters of `self.def_id()`, and in other"] # [doc = " cases the MIR body is expressed in terms of the types found in the generic parameter array."] # [doc = " In the former case, we want to instantiate those generic types and replace them with the"] # [doc = " values from the args when monomorphizing the function body. But in the latter case, we"] # [doc = " don't want to do that instantiation, since it has already been done effectively."] # [doc = ""] # [doc = " This function returns `Some(args)` in the former case and `None` otherwise -- i.e., if"] # [doc = " this function returns `None`, then the MIR body does not require instantiation during"] # [doc = " codegen."] fn args_for_mir_body (& self) -> Option < GenericArgsRef < 'tcx > > { self . def . has_polymorphic_mir_body () . then_some (self . args) } pub fn instantiate_mir < T > (& self , tcx : TyCtxt < 'tcx > , v : EarlyBinder < 'tcx , & T >) -> T where T : TypeFoldable < TyCtxt < 'tcx > > + Copy , { let v = v . map_bound (| v | * v) ; if let Some (args) = self . args_for_mir_body () { v . instantiate (tcx , args) } else { v . instantiate_identity () } } # [inline (always)] pub fn instantiate_mir_and_normalize_erasing_regions < T > (& self , tcx : TyCtxt < 'tcx > , typing_env : ty :: TypingEnv < 'tcx > , v : EarlyBinder < 'tcx , T > ,) -> T where T : TypeFoldable < TyCtxt < 'tcx > > , { if let Some (args) = self . args_for_mir_body () { tcx . instantiate_and_normalize_erasing_regions (args , typing_env , v) } else { tcx . normalize_erasing_regions (typing_env , v . instantiate_identity ()) } } # [inline (always)] pub fn try_instantiate_mir_and_normalize_erasing_regions < T > (& self , tcx : TyCtxt < 'tcx > , typing_env : ty :: TypingEnv < 'tcx > , v : EarlyBinder < 'tcx , T > ,) -> Result < T , NormalizationError < 'tcx > > where T : TypeFoldable < TyCtxt < 'tcx > > , { if let Some (args) = self . args_for_mir_body () { tcx . try_instantiate_and_normalize_erasing_regions (args , typing_env , v) } else { tcx . try_normalize_erasing_regions (typing_env , v . instantiate_identity ()) } } }
/* FP:instance.rs-0057 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_FN_0028
/* FP:instance.rs-0058 */ fn needs_fn_once_adapter_shim (actual_closure_kind : ty :: ClosureKind , trait_closure_kind : ty :: ClosureKind ,) -> Result < bool , () > { match (actual_closure_kind , trait_closure_kind) { (ty :: ClosureKind :: Fn , ty :: ClosureKind :: Fn) | (ty :: ClosureKind :: FnMut , ty :: ClosureKind :: FnMut) | (ty :: ClosureKind :: FnOnce , ty :: ClosureKind :: FnOnce) => { Ok (false) } (ty :: ClosureKind :: Fn , ty :: ClosureKind :: FnMut) => { Ok (false) } (ty :: ClosureKind :: Fn | ty :: ClosureKind :: FnMut , ty :: ClosureKind :: FnOnce) => { Ok (true) } (ty :: ClosureKind :: FnMut | ty :: ClosureKind :: FnOnce , _) => Err (()) , } }
/* FP:instance.rs-0059 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_STRUCT_0029
/* FP:instance.rs-0060 */ # [derive (Debug , Copy , Clone , Eq , PartialEq , Decodable , Encodable , HashStable)] pub struct UnusedGenericParams (FiniteBitSet < u32 >) ;
/* FP:instance.rs-0061 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_IMPL_0030
/* FP:instance.rs-0062 */ impl Default for UnusedGenericParams { fn default () -> Self { UnusedGenericParams :: new_all_used () } }
/* FP:instance.rs-0063 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_ty_instance_IMPL_0031
/* FP:instance.rs-0064 */ impl UnusedGenericParams { pub fn new_all_unused (amount : u32) -> Self { let mut bitset = FiniteBitSet :: new_empty () ; bitset . set_range (0 .. amount) ; Self (bitset) } pub fn new_all_used () -> Self { Self (FiniteBitSet :: new_empty ()) } pub fn mark_used (& mut self , idx : u32) { self . 0 . clear (idx) ; } pub fn is_unused (& self , idx : u32) -> bool { self . 0 . contains (idx) . unwrap_or (false) } pub fn is_used (& self , idx : u32) -> bool { ! self . is_unused (idx) } pub fn all_used (& self) -> bool { self . 0 . is_empty () } pub fn bits (& self) -> u32 { self . 0 . 0 } pub fn from_bits (bits : u32) -> UnusedGenericParams { UnusedGenericParams (FiniteBitSet (bits)) } }