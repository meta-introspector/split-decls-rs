/* FP:prefixes.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_prefixes_USE_0001
/* FP:prefixes.rs-0002 */ use crate :: rustc_complete :: mir :: { PlaceRef , ProjectionElem } ;
/* FP:prefixes.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_prefixes_USE_0002
/* FP:prefixes.rs-0004 */ use super :: MirBorrowckCtxt ;
/* FP:prefixes.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_prefixes_TRAIT_0003
/* FP:prefixes.rs-0006 */ pub (crate) trait IsPrefixOf < 'tcx > { fn is_prefix_of (& self , other : PlaceRef < 'tcx >) -> bool ; }
/* FP:prefixes.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_prefixes_IMPL_0004
/* FP:prefixes.rs-0008 */ impl < 'tcx > IsPrefixOf < 'tcx > for PlaceRef < 'tcx > { fn is_prefix_of (& self , other : PlaceRef < 'tcx >) -> bool { self . local == other . local && self . projection . len () <= other . projection . len () && self . projection == & other . projection [.. self . projection . len ()] } }
/* FP:prefixes.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_prefixes_STRUCT_0005
/* FP:prefixes.rs-0010 */ pub (super) struct Prefixes < 'tcx > { kind : PrefixSet , next : Option < PlaceRef < 'tcx > > , }
/* FP:prefixes.rs-0011 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_prefixes_ENUM_0006
/* FP:prefixes.rs-0012 */ # [derive (Copy , Clone , PartialEq , Eq , Debug)] pub (super) enum PrefixSet { # [doc = " Doesn't stop until it returns the base case (a Local or"] # [doc = " Static prefix)."] All , # [doc = " Stops at any dereference."] Shallow , }
/* FP:prefixes.rs-0013 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_prefixes_IMPL_0007
/* FP:prefixes.rs-0014 */ impl < 'tcx > MirBorrowckCtxt < '_ , '_ , 'tcx > { # [doc = " Returns an iterator over the prefixes of `place`"] # [doc = " (inclusive) from longest to smallest, potentially"] # [doc = " terminating the iteration early based on `kind`."] pub (super) fn prefixes (& self , place_ref : PlaceRef < 'tcx > , kind : PrefixSet) -> Prefixes < 'tcx > { Prefixes { next : Some (place_ref) , kind } } }
/* FP:prefixes.rs-0015 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_borrowck_src_prefixes_IMPL_0008
/* FP:prefixes.rs-0016 */ impl < 'tcx > Iterator for Prefixes < 'tcx > { type Item = PlaceRef < 'tcx > ; fn next (& mut self) -> Option < Self :: Item > { let mut cursor = self . next ? ; loop { match cursor . last_projection () { None => { self . next = None ; return Some (cursor) ; } Some ((cursor_base , elem)) => { match elem { ProjectionElem :: Field (_ , _) => { self . next = Some (cursor_base) ; return Some (cursor) ; } ProjectionElem :: UnwrapUnsafeBinder (_) => { self . next = Some (cursor_base) ; return Some (cursor) ; } ProjectionElem :: Downcast (..) | ProjectionElem :: Subslice { .. } | ProjectionElem :: OpaqueCast { .. } | ProjectionElem :: ConstantIndex { .. } | ProjectionElem :: Index (_) => { cursor = cursor_base ; } ProjectionElem :: Subtype (..) => { panic ! ("Subtype projection is not allowed before borrow check") } ProjectionElem :: Deref => { match self . kind { PrefixSet :: Shallow => { self . next = None ; return Some (cursor) ; } PrefixSet :: All => { self . next = Some (cursor_base) ; return Some (cursor) ; } } } } } } } } }