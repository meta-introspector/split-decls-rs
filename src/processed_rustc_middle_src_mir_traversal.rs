/* FP:traversal.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_USE_0001
/* FP:traversal.rs-0002 */ use super :: * ;
/* FP:traversal.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_STRUCT_0002
/* FP:traversal.rs-0004 */ # [doc = " Preorder traversal of a graph."] # [doc = ""] # [doc = " Preorder traversal is when each node is visited after at least one of its predecessors. If you"] # [doc = " are familiar with some basic graph theory, then this performs a depth first search and returns"] # [doc = " nodes in order of discovery time."] # [doc = ""] # [doc = " ```text"] # [doc = ""] # [doc = "         A"] # [doc = "        / \\"] # [doc = "       /   \\"] # [doc = "      B     C"] # [doc = "       \\   /"] # [doc = "        \\ /"] # [doc = "         D"] # [doc = " ```"] # [doc = ""] # [doc = " A preorder traversal of this graph is either `A B D C` or `A C D B`"] # [derive (Clone)] pub struct Preorder < 'a , 'tcx > { body : & 'a Body < 'tcx > , visited : DenseBitSet < BasicBlock > , worklist : Vec < BasicBlock > , }
/* FP:traversal.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_IMPL_0003
/* FP:traversal.rs-0006 */ impl < 'a , 'tcx > Preorder < 'a , 'tcx > { pub fn new (body : & 'a Body < 'tcx > , root : BasicBlock) -> Preorder < 'a , 'tcx > { let worklist = vec ! [root] ; Preorder { body , visited : DenseBitSet :: new_empty (body . basic_blocks . len ()) , worklist } } }
/* FP:traversal.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_FN_0004
/* FP:traversal.rs-0008 */ # [doc = " Preorder traversal of a graph."] # [doc = ""] # [doc = " This function creates an iterator over the `Body`'s basic blocks, that"] # [doc = " returns basic blocks in a preorder."] # [doc = ""] # [doc = " See [`Preorder`]'s docs to learn what is preorder traversal."] pub fn preorder < 'a , 'tcx > (body : & 'a Body < 'tcx >) -> Preorder < 'a , 'tcx > { Preorder :: new (body , START_BLOCK) }
/* FP:traversal.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_IMPL_0005
/* FP:traversal.rs-0010 */ impl < 'a , 'tcx > Iterator for Preorder < 'a , 'tcx > { type Item = (BasicBlock , & 'a BasicBlockData < 'tcx >) ; fn next (& mut self) -> Option < (BasicBlock , & 'a BasicBlockData < 'tcx >) > { while let Some (idx) = self . worklist . pop () { if ! self . visited . insert (idx) { continue ; } let data = & self . body [idx] ; if let Some (ref term) = data . terminator { self . worklist . extend (term . successors ()) ; } return Some ((idx , data)) ; } None } fn size_hint (& self) -> (usize , Option < usize >) { let lower = 0 ; let upper = self . body . basic_blocks . len () ; (lower , Some (upper)) } }
/* FP:traversal.rs-0011 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_STRUCT_0006
/* FP:traversal.rs-0012 */ # [doc = " Postorder traversal of a graph."] # [doc = ""] # [doc = " Postorder traversal is when each node is visited after all of its successors, except when the"] # [doc = " successor is only reachable by a back-edge. If you are familiar with some basic graph theory,"] # [doc = " then this performs a depth first search and returns nodes in order of completion time."] # [doc = ""] # [doc = ""] # [doc = " ```text"] # [doc = ""] # [doc = "         A"] # [doc = "        / \\"] # [doc = "       /   \\"] # [doc = "      B     C"] # [doc = "       \\   /"] # [doc = "        \\ /"] # [doc = "         D"] # [doc = " ```"] # [doc = ""] # [doc = " A Postorder traversal of this graph is `D B C A` or `D C B A`"] pub struct Postorder < 'a , 'tcx > { basic_blocks : & 'a IndexSlice < BasicBlock , BasicBlockData < 'tcx > > , visited : DenseBitSet < BasicBlock > , visit_stack : Vec < (BasicBlock , Successors < 'a >) > , # [doc = " A non-empty `extra` allows for a precise calculation of the successors."] extra : Option < (TyCtxt < 'tcx > , Instance < 'tcx >) > , }
/* FP:traversal.rs-0013 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_IMPL_0007
/* FP:traversal.rs-0014 */ impl < 'a , 'tcx > Postorder < 'a , 'tcx > { pub fn new (basic_blocks : & 'a IndexSlice < BasicBlock , BasicBlockData < 'tcx > > , root : BasicBlock , extra : Option < (TyCtxt < 'tcx > , Instance < 'tcx >) > ,) -> Postorder < 'a , 'tcx > { let mut po = Postorder { basic_blocks , visited : DenseBitSet :: new_empty (basic_blocks . len ()) , visit_stack : Vec :: new () , extra , } ; po . visit (root) ; po . traverse_successor () ; po } fn visit (& mut self , bb : BasicBlock) { if ! self . visited . insert (bb) { return ; } let data = & self . basic_blocks [bb] ; let successors = if let Some (extra) = self . extra { data . mono_successors (extra . 0 , extra . 1) } else { data . terminator () . successors () } ; self . visit_stack . push ((bb , successors)) ; } fn traverse_successor (& mut self) { while let Some (bb) = self . visit_stack . last_mut () . and_then (| (_ , iter) | iter . next_back ()) { self . visit (bb) ; } } }
/* FP:traversal.rs-0015 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_IMPL_0008
/* FP:traversal.rs-0016 */ impl < 'tcx > Iterator for Postorder < '_ , 'tcx > { type Item = BasicBlock ; fn next (& mut self) -> Option < BasicBlock > { let (bb , _) = self . visit_stack . pop () ? ; self . traverse_successor () ; Some (bb) } fn size_hint (& self) -> (usize , Option < usize >) { let lower = self . visit_stack . len () ; let upper = self . basic_blocks . len () ; (lower , Some (upper)) } }
/* FP:traversal.rs-0017 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_FN_0009
/* FP:traversal.rs-0018 */ # [doc = " Postorder traversal of a graph."] # [doc = ""] # [doc = " This function creates an iterator over the `Body`'s basic blocks, that:"] # [doc = " - returns basic blocks in a postorder,"] # [doc = " - traverses the `BasicBlocks` CFG cache's reverse postorder backwards, and does not cache the"] # [doc = "   postorder itself."] # [doc = ""] # [doc = " See [`Postorder`]'s docs to learn what is postorder traversal."] pub fn postorder < 'a , 'tcx > (body : & 'a Body < 'tcx > ,) -> impl Iterator < Item = (BasicBlock , & 'a BasicBlockData < 'tcx >) > + ExactSizeIterator + DoubleEndedIterator { reverse_postorder (body) . rev () }
/* FP:traversal.rs-0019 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_FN_0010
/* FP:traversal.rs-0020 */ pub fn mono_reachable_reverse_postorder < 'a , 'tcx > (body : & 'a Body < 'tcx > , tcx : TyCtxt < 'tcx > , instance : Instance < 'tcx > ,) -> Vec < BasicBlock > { let mut iter = Postorder :: new (& body . basic_blocks , START_BLOCK , Some ((tcx , instance))) ; let mut items = Vec :: with_capacity (body . basic_blocks . len ()) ; while let Some (block) = iter . next () { items . push (block) ; } items . reverse () ; items }
/* FP:traversal.rs-0021 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_FN_0011
/* FP:traversal.rs-0022 */ # [doc = " Returns an iterator over all basic blocks reachable from the `START_BLOCK` in no particular"] # [doc = " order."] # [doc = ""] # [doc = " This is clearer than writing `preorder` in cases where the order doesn't matter."] pub fn reachable < 'a , 'tcx > (body : & 'a Body < 'tcx > ,) -> impl 'a + Iterator < Item = (BasicBlock , & 'a BasicBlockData < 'tcx >) > { preorder (body) }
/* FP:traversal.rs-0023 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_FN_0012
/* FP:traversal.rs-0024 */ # [doc = " Returns a `DenseBitSet` containing all basic blocks reachable from the `START_BLOCK`."] pub fn reachable_as_bitset (body : & Body < '_ >) -> DenseBitSet < BasicBlock > { let mut iter = preorder (body) ; while let Some (_) = iter . next () { } iter . visited }
/* FP:traversal.rs-0025 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_FN_0013
/* FP:traversal.rs-0026 */ # [doc = " Reverse postorder traversal of a graph."] # [doc = ""] # [doc = " This function creates an iterator over the `Body`'s basic blocks, that:"] # [doc = " - returns basic blocks in a reverse postorder,"] # [doc = " - makes use of the `BasicBlocks` CFG cache's reverse postorder."] # [doc = ""] # [doc = " Reverse postorder is the reverse order of a postorder traversal."] # [doc = " This is different to a preorder traversal and represents a natural"] # [doc = " linearization of control-flow."] # [doc = ""] # [doc = " ```text"] # [doc = ""] # [doc = "         A"] # [doc = "        / \\"] # [doc = "       /   \\"] # [doc = "      B     C"] # [doc = "       \\   /"] # [doc = "        \\ /"] # [doc = "         D"] # [doc = " ```"] # [doc = ""] # [doc = " A reverse postorder traversal of this graph is either `A B C D` or `A C B D`"] # [doc = " Note that for a graph containing no loops (i.e., A DAG), this is equivalent to"] # [doc = " a topological sort."] pub fn reverse_postorder < 'a , 'tcx > (body : & 'a Body < 'tcx > ,) -> impl Iterator < Item = (BasicBlock , & 'a BasicBlockData < 'tcx >) > + ExactSizeIterator + DoubleEndedIterator { body . basic_blocks . reverse_postorder () . iter () . map (| & bb | (bb , & body . basic_blocks [bb])) }
/* FP:traversal.rs-0027 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_FN_0014
/* FP:traversal.rs-0028 */ # [doc = " Traversal of a [`Body`] that tries to avoid unreachable blocks in a monomorphized [`Instance`]."] # [doc = ""] # [doc = " This is allowed to have false positives; blocks may be visited even if they are not actually"] # [doc = " reachable."] # [doc = ""] # [doc = " Such a traversal is mostly useful because it lets us skip lowering the `false` side"] # [doc = " of `if <T as Trait>::CONST`, as well as [`NullOp::UbChecks`]."] # [doc = ""] # [doc = " [`NullOp::UbChecks`]: crate::rustc_middle::mir::NullOp::UbChecks"] pub fn mono_reachable < 'a , 'tcx > (body : & 'a Body < 'tcx > , tcx : TyCtxt < 'tcx > , instance : Instance < 'tcx > ,) -> MonoReachable < 'a , 'tcx > { MonoReachable :: new (body , tcx , instance) }
/* FP:traversal.rs-0029 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_FN_0015
/* FP:traversal.rs-0030 */ # [doc = " [`MonoReachable`] internally accumulates a [`DenseBitSet`] of visited blocks. This is just a"] # [doc = " convenience function to run that traversal then extract its set of reached blocks."] pub fn mono_reachable_as_bitset < 'a , 'tcx > (body : & 'a Body < 'tcx > , tcx : TyCtxt < 'tcx > , instance : Instance < 'tcx > ,) -> DenseBitSet < BasicBlock > { let mut iter = mono_reachable (body , tcx , instance) ; while let Some (_) = iter . next () { } iter . visited }
/* FP:traversal.rs-0031 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_STRUCT_0016
/* FP:traversal.rs-0032 */ pub struct MonoReachable < 'a , 'tcx > { body : & 'a Body < 'tcx > , tcx : TyCtxt < 'tcx > , instance : Instance < 'tcx > , visited : DenseBitSet < BasicBlock > , worklist : DenseBitSet < BasicBlock > , }
/* FP:traversal.rs-0033 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_IMPL_0017
/* FP:traversal.rs-0034 */ impl < 'a , 'tcx > MonoReachable < 'a , 'tcx > { pub fn new (body : & 'a Body < 'tcx > , tcx : TyCtxt < 'tcx > , instance : Instance < 'tcx > ,) -> MonoReachable < 'a , 'tcx > { let mut worklist = DenseBitSet :: new_empty (body . basic_blocks . len ()) ; worklist . insert (START_BLOCK) ; MonoReachable { body , tcx , instance , visited : DenseBitSet :: new_empty (body . basic_blocks . len ()) , worklist , } } fn add_work (& mut self , blocks : impl IntoIterator < Item = BasicBlock >) { for block in blocks . into_iter () { if ! self . visited . contains (block) { self . worklist . insert (block) ; } } } }
/* FP:traversal.rs-0035 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_middle_src_mir_traversal_IMPL_0018
/* FP:traversal.rs-0036 */ impl < 'a , 'tcx > Iterator for MonoReachable < 'a , 'tcx > { type Item = (BasicBlock , & 'a BasicBlockData < 'tcx >) ; fn next (& mut self) -> Option < (BasicBlock , & 'a BasicBlockData < 'tcx >) > { while let Some (idx) = self . worklist . iter () . next () { self . worklist . remove (idx) ; if ! self . visited . insert (idx) { continue ; } let data = & self . body [idx] ; let targets = data . mono_successors (self . tcx , self . instance) ; self . add_work (targets) ; return Some ((idx , data)) ; } None } }