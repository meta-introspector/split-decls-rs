/* FP:sharded.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_USE_0001
/* FP:sharded.rs-0002 */ use std :: borrow :: Borrow ;
/* FP:sharded.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_USE_0002
/* FP:sharded.rs-0004 */ use std :: hash :: { Hash , Hasher } ;
/* FP:sharded.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_USE_0003
/* FP:sharded.rs-0006 */ use std :: { iter , mem } ;
/* FP:sharded.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_USE_0004
/* FP:sharded.rs-0008 */ use either :: Either ;
/* FP:sharded.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_USE_0005
/* FP:sharded.rs-0010 */ use hashbrown :: hash_table :: { Entry , HashTable } ;
/* FP:sharded.rs-0011 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_USE_0006
/* FP:sharded.rs-0012 */ use crate :: fx :: FxHasher ;
/* FP:sharded.rs-0013 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_USE_0007
/* FP:sharded.rs-0014 */ use crate :: sync :: { CacheAligned , Lock , LockGuard , Mode , is_dyn_thread_safe } ;
/* FP:sharded.rs-0015 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_CONST_0008
/* FP:sharded.rs-0016 */ const SHARD_BITS : usize = 5 ;
/* FP:sharded.rs-0017 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_CONST_0009
/* FP:sharded.rs-0018 */ const SHARDS : usize = 1 << SHARD_BITS ;
/* FP:sharded.rs-0019 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_ENUM_0010
/* FP:sharded.rs-0020 */ # [doc = " An array of cache-line aligned inner locked structures with convenience methods."] # [doc = " A single field is used when the compiler uses only one thread."] pub enum Sharded < T > { Single (Lock < T >) , Shards (Box < [CacheAligned < Lock < T > > ; SHARDS] >) , }
/* FP:sharded.rs-0021 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_IMPL_0011
/* FP:sharded.rs-0022 */ impl < T : Default > Default for Sharded < T > { # [inline] fn default () -> Self { Self :: new (T :: default) } }
/* FP:sharded.rs-0023 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_IMPL_0012
/* FP:sharded.rs-0024 */ impl < T > Sharded < T > { # [inline] pub fn new (mut value : impl FnMut () -> T) -> Self { if is_dyn_thread_safe () { return Sharded :: Shards (Box :: new ([() ; SHARDS] . map (| () | CacheAligned (Lock :: new (value ()))) ,)) ; } Sharded :: Single (Lock :: new (value ())) } # [doc = " The shard is selected by hashing `val` with `FxHasher`."] # [inline] pub fn get_shard_by_value < K : Hash + ? Sized > (& self , val : & K) -> & Lock < T > { match self { Self :: Single (single) => single , Self :: Shards (..) => self . get_shard_by_hash (make_hash (val)) , } } # [inline] pub fn get_shard_by_hash (& self , hash : u64) -> & Lock < T > { self . get_shard_by_index (get_shard_hash (hash)) } # [inline] pub fn get_shard_by_index (& self , i : usize) -> & Lock < T > { match self { Self :: Single (single) => single , Self :: Shards (shards) => { unsafe { & shards . get_unchecked (i & (SHARDS - 1)) . 0 } } } } # [doc = " The shard is selected by hashing `val` with `FxHasher`."] # [inline] # [track_caller] pub fn lock_shard_by_value < K : Hash + ? Sized > (& self , val : & K) -> LockGuard < '_ , T > { match self { Self :: Single (single) => { unsafe { single . lock_assume (Mode :: NoSync) } } Self :: Shards (..) => self . lock_shard_by_hash (make_hash (val)) , } } # [inline] # [track_caller] pub fn lock_shard_by_hash (& self , hash : u64) -> LockGuard < '_ , T > { self . lock_shard_by_index (get_shard_hash (hash)) } # [inline] # [track_caller] pub fn lock_shard_by_index (& self , i : usize) -> LockGuard < '_ , T > { match self { Self :: Single (single) => { unsafe { single . lock_assume (Mode :: NoSync) } } Self :: Shards (shards) => { unsafe { shards . get_unchecked (i & (SHARDS - 1)) . 0 . lock_assume (Mode :: Sync) } } } } # [inline] pub fn lock_shards (& self) -> impl Iterator < Item = LockGuard < '_ , T > > { match self { Self :: Single (single) => Either :: Left (iter :: once (single . lock ())) , Self :: Shards (shards) => Either :: Right (shards . iter () . map (| shard | shard . 0 . lock ())) , } } # [inline] pub fn try_lock_shards (& self) -> impl Iterator < Item = Option < LockGuard < '_ , T > > > { match self { Self :: Single (single) => Either :: Left (iter :: once (single . try_lock ())) , Self :: Shards (shards) => Either :: Right (shards . iter () . map (| shard | shard . 0 . try_lock ())) , } } }
/* FP:sharded.rs-0025 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_FN_0013
/* FP:sharded.rs-0026 */ # [inline] pub fn shards () -> usize { if is_dyn_thread_safe () { return SHARDS ; } 1 }
/* FP:sharded.rs-0027 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_TYPE_0014
/* FP:sharded.rs-0028 */ pub type ShardedHashMap < K , V > = Sharded < HashTable < (K , V) > > ;
/* FP:sharded.rs-0029 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_IMPL_0015
/* FP:sharded.rs-0030 */ impl < K : Eq , V > ShardedHashMap < K , V > { pub fn with_capacity (cap : usize) -> Self { Self :: new (| | HashTable :: with_capacity (cap)) } pub fn len (& self) -> usize { self . lock_shards () . map (| shard | shard . len ()) . sum () } }
/* FP:sharded.rs-0031 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_IMPL_0016
/* FP:sharded.rs-0032 */ impl < K : Eq + Hash , V > ShardedHashMap < K , V > { # [inline] pub fn get < Q > (& self , key : & Q) -> Option < V > where K : Borrow < Q > , Q : Hash + Eq , V : Clone , { let hash = make_hash (key) ; let shard = self . lock_shard_by_hash (hash) ; let (_ , value) = shard . find (hash , | (k , _) | k . borrow () == key) ? ; Some (value . clone ()) } # [inline] pub fn get_or_insert_with (& self , key : K , default : impl FnOnce () -> V) -> V where V : Copy , { let hash = make_hash (& key) ; let mut shard = self . lock_shard_by_hash (hash) ; match table_entry (& mut shard , hash , & key) { Entry :: Occupied (e) => e . get () . 1 , Entry :: Vacant (e) => { let value = default () ; e . insert ((key , value)) ; value } } } # [inline] pub fn insert (& self , key : K , value : V) -> Option < V > { let hash = make_hash (& key) ; let mut shard = self . lock_shard_by_hash (hash) ; match table_entry (& mut shard , hash , & key) { Entry :: Occupied (e) => { let previous = mem :: replace (& mut e . into_mut () . 1 , value) ; Some (previous) } Entry :: Vacant (e) => { e . insert ((key , value)) ; None } } } }
/* FP:sharded.rs-0033 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_IMPL_0017
/* FP:sharded.rs-0034 */ impl < K : Eq + Hash + Copy > ShardedHashMap < K , () > { # [inline] pub fn intern_ref < Q : ? Sized > (& self , value : & Q , make : impl FnOnce () -> K) -> K where K : Borrow < Q > , Q : Hash + Eq , { let hash = make_hash (value) ; let mut shard = self . lock_shard_by_hash (hash) ; match table_entry (& mut shard , hash , value) { Entry :: Occupied (e) => e . get () . 0 , Entry :: Vacant (e) => { let v = make () ; e . insert ((v , ())) ; v } } } # [inline] pub fn intern < Q > (& self , value : Q , make : impl FnOnce (Q) -> K) -> K where K : Borrow < Q > , Q : Hash + Eq , { let hash = make_hash (& value) ; let mut shard = self . lock_shard_by_hash (hash) ; match table_entry (& mut shard , hash , & value) { Entry :: Occupied (e) => e . get () . 0 , Entry :: Vacant (e) => { let v = make (value) ; e . insert ((v , ())) ; v } } } }
/* FP:sharded.rs-0035 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_TRAIT_0018
/* FP:sharded.rs-0036 */ pub trait IntoPointer { # [doc = " Returns a pointer which outlives `self`."] fn into_pointer (& self) -> * const () ; }
/* FP:sharded.rs-0037 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_IMPL_0019
/* FP:sharded.rs-0038 */ impl < K : Eq + Hash + Copy + IntoPointer > ShardedHashMap < K , () > { pub fn contains_pointer_to < T : Hash + IntoPointer > (& self , value : & T) -> bool { let hash = make_hash (& value) ; let shard = self . lock_shard_by_hash (hash) ; let value = value . into_pointer () ; shard . find (hash , | (k , ()) | k . into_pointer () == value) . is_some () } }
/* FP:sharded.rs-0039 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_FN_0020
/* FP:sharded.rs-0040 */ # [inline] pub fn make_hash < K : Hash + ? Sized > (val : & K) -> u64 { let mut state = FxHasher :: default () ; val . hash (& mut state) ; state . finish () }
/* FP:sharded.rs-0041 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_FN_0021
/* FP:sharded.rs-0042 */ # [inline] fn table_entry < 'a , K , V , Q > (table : & 'a mut HashTable < (K , V) > , hash : u64 , key : & Q ,) -> Entry < 'a , (K , V) > where K : Hash + Borrow < Q > , Q : ? Sized + Eq , { table . entry (hash , move | (k , _) | k . borrow () == key , | (k , _) | make_hash (k)) }
/* FP:sharded.rs-0043 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_data_structures_src_sharded_FN_0022
/* FP:sharded.rs-0044 */ # [doc = " Get a shard with a pre-computed hash value. If `get_shard_by_value` is"] # [doc = " ever used in combination with `get_shard_by_hash` on a single `Sharded`"] # [doc = " instance, then `hash` must be computed with `FxHasher`. Otherwise,"] # [doc = " `hash` can be computed with any hasher, so long as that hasher is used"] # [doc = " consistently for each `Sharded` instance."] # [inline] fn get_shard_hash (hash : u64) -> usize { let hash_len = size_of :: < usize > () ; (hash >> (hash_len * 8 - 7 - SHARD_BITS)) as usize }