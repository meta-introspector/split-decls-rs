/* FP:pat.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0001
/* FP:pat.rs-0002 */ use std :: ops :: Bound ;
/* FP:pat.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0002
/* FP:pat.rs-0004 */ use crate :: rustc_complete :: mut_visit :: { self , MutVisitor } ;
/* FP:pat.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0003
/* FP:pat.rs-0006 */ use crate :: rustc_complete :: token :: NtPatKind :: * ;
/* FP:pat.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0004
/* FP:pat.rs-0008 */ use crate :: rustc_complete :: token :: { self , IdentIsRaw , MetaVarKind , Token } ;
/* FP:pat.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0005
/* FP:pat.rs-0010 */ use crate :: rustc_complete :: util :: parser :: ExprPrecedence ;
/* FP:pat.rs-0011 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0006
/* FP:pat.rs-0012 */ use crate :: rustc_complete :: visit :: { self , Visitor } ;
/* FP:pat.rs-0013 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0007
/* FP:pat.rs-0014 */ use crate :: rustc_complete :: { self as ast , Arm , AttrVec , BindingMode , ByRef , Expr , ExprKind , LocalKind , MacCall , Mutability , Pat , PatField , PatFieldsRest , PatKind , Path , QSelf , RangeEnd , RangeSyntax , Stmt , StmtKind , } ;
/* FP:pat.rs-0015 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0008
/* FP:pat.rs-0016 */ use rustc_ast_pretty :: pprust ;
/* FP:pat.rs-0017 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0009
/* FP:pat.rs-0018 */ use crate :: rustc_complete :: { Applicability , Diag , DiagArgValue , PResult , StashKey } ;
/* FP:pat.rs-0019 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0010
/* FP:pat.rs-0020 */ use crate :: rustc_complete :: errors :: ExprParenthesesNeeded ;
/* FP:pat.rs-0021 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0011
/* FP:pat.rs-0022 */ use crate :: rustc_complete :: source_map :: { Spanned , respan } ;
/* FP:pat.rs-0023 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0012
/* FP:pat.rs-0024 */ use crate :: rustc_complete :: { BytePos , ErrorGuaranteed , Ident , Span , kw , sym } ;
/* FP:pat.rs-0025 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0013
/* FP:pat.rs-0026 */ use thin_vec :: { ThinVec , thin_vec } ;
/* FP:pat.rs-0027 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0014
/* FP:pat.rs-0028 */ use super :: { ForceCollect , Parser , PathStyle , Restrictions , Trailing , UsePreAttrPos } ;
/* FP:pat.rs-0029 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0015
/* FP:pat.rs-0030 */ use crate :: errors :: { self , AmbiguousRangePattern , AtDotDotInStructPattern , AtInStructPattern , DotDotDotForRemainingFields , DotDotDotRangeToPatternNotAllowed , DotDotDotRestPattern , EnumPatternInsteadOfIdentifier , ExpectedBindingLeftOfAt , ExpectedCommaAfterPatternField , GenericArgsInPatRequireTurbofishSyntax , InclusiveRangeExtraEquals , InclusiveRangeMatchArrow , InclusiveRangeNoEnd , InvalidMutInPattern , ParenRangeSuggestion , PatternOnWrongSideOfAt , RemoveLet , RepeatedMutInPattern , SwitchRefBoxOrder , TopLevelOrPatternNotAllowed , TopLevelOrPatternNotAllowedSugg , TrailingVertNotAllowed , TrailingVertSuggestion , UnexpectedExpressionInPattern , UnexpectedExpressionInPatternSugg , UnexpectedLifetimeInPattern , UnexpectedParenInRangePat , UnexpectedParenInRangePatSugg , UnexpectedVertVertBeforeFunctionParam , UnexpectedVertVertInPattern , WrapInParens , } ;
/* FP:pat.rs-0031 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0016
/* FP:pat.rs-0032 */ use crate :: parser :: expr :: { DestructuredFloat , could_be_unclosed_char_literal } ;
/* FP:pat.rs-0033 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_USE_0017
/* FP:pat.rs-0034 */ use crate :: { exp , maybe_recover_from_interpolated_ty_qpath } ;
/* FP:pat.rs-0035 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_ENUM_0018
/* FP:pat.rs-0036 */ # [derive (PartialEq , Copy , Clone)] pub enum Expected { ParameterName , ArgumentName , Identifier , BindingPattern , }
/* FP:pat.rs-0037 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_IMPL_0019
/* FP:pat.rs-0038 */ impl Expected { fn to_string_or_fallback (expected : Option < Expected >) -> & 'static str { match expected { Some (Expected :: ParameterName) => "parameter name" , Some (Expected :: ArgumentName) => "argument name" , Some (Expected :: Identifier) => "identifier" , Some (Expected :: BindingPattern) => "binding pattern" , None => "pattern" , } } }
/* FP:pat.rs-0039 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_CONST_0020
/* FP:pat.rs-0040 */ const WHILE_PARSING_OR_MSG : & str = "while parsing this or-pattern starting here" ;
/* FP:pat.rs-0041 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_ENUM_0021
/* FP:pat.rs-0042 */ # [doc = " Whether or not to recover a `,` when parsing or-patterns."] # [derive (PartialEq , Copy , Clone)] pub enum RecoverComma { Yes , No , }
/* FP:pat.rs-0043 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_ENUM_0022
/* FP:pat.rs-0044 */ # [doc = " Whether or not to recover a `:` when parsing patterns that were meant to be paths."] # [derive (PartialEq , Copy , Clone)] pub enum RecoverColon { Yes , No , }
/* FP:pat.rs-0045 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_ENUM_0023
/* FP:pat.rs-0046 */ # [doc = " Whether or not to recover a `a, b` when parsing patterns as `(a, b)` or that *and* `a | b`."] # [derive (PartialEq , Copy , Clone)] pub enum CommaRecoveryMode { LikelyTuple , EitherTupleOrPipe , }
/* FP:pat.rs-0047 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_ENUM_0024
/* FP:pat.rs-0048 */ # [doc = " The result of `eat_or_separator`. We want to distinguish which case we are in to avoid"] # [doc = " emitting duplicate diagnostics."] # [derive (Debug , Clone , Copy)] enum EatOrResult { # [doc = " We recovered from a trailing vert."] TrailingVert , # [doc = " We ate an `|` (or `||` and recovered)."] AteOr , # [doc = " We did not eat anything (i.e. the current token is not `|` or `||`)."] None , }
/* FP:pat.rs-0049 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_ENUM_0025
/* FP:pat.rs-0050 */ # [doc = " The syntax location of a given pattern. Used for diagnostics."] # [derive (Clone , Copy)] pub enum PatternLocation { LetBinding , FunctionParameter , }
/* FP:pat.rs-0051 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_parse_src_parser_pat_IMPL_0026
/* FP:pat.rs-0052 */ impl < 'a > Parser < 'a > { # [doc = " Parses a pattern."] # [doc = ""] # [doc = " Corresponds to `Pattern` in RFC 3637 and admits guard patterns at the top level."] # [doc = " Used when parsing patterns in all cases where neither `PatternNoTopGuard` nor"] # [doc = " `PatternNoTopAlt` (see below) are used."] pub fn parse_pat_allow_top_guard (& mut self , expected : Option < Expected > , rc : RecoverComma , ra : RecoverColon , rt : CommaRecoveryMode ,) -> PResult < 'a , Box < Pat > > { let pat = self . parse_pat_no_top_guard (expected , rc , ra , rt) ? ; if self . eat_keyword (exp ! (If)) { let cond = self . parse_expr () ? ; self . psess . gated_spans . gate (sym :: guard_patterns , cond . span) ; let span = pat . span . to (cond . span) ; Ok (self . mk_pat (span , PatKind :: Guard (pat , cond))) } else { Ok (pat) } } # [doc = " Parses a pattern."] # [doc = ""] # [doc = " Corresponds to `PatternNoTopAlt` in RFC 3637 and does not admit or-patterns"] # [doc = " or guard patterns at the top level. Used when parsing the parameters of lambda"] # [doc = " expressions, functions, function pointers, and `pat_param` macro fragments."] pub fn parse_pat_no_top_alt (& mut self , expected : Option < Expected > , syntax_loc : Option < PatternLocation > ,) -> PResult < 'a , Box < Pat > > { self . parse_pat_with_range_pat (true , expected , syntax_loc) } # [doc = " Parses a pattern."] # [doc = ""] # [doc = " Corresponds to `PatternNoTopGuard` in RFC 3637 and allows or-patterns, but not"] # [doc = " guard patterns, at the top level. Used for parsing patterns in `pat` fragments (until"] # [doc = " the next edition) and `let`, `if let`, and `while let` expressions."] # [doc = ""] # [doc = " Note that after the FCP in <https://github.com/rust-lang/rust/issues/81415>,"] # [doc = " a leading vert is allowed in nested or-patterns, too. This allows us to"] # [doc = " simplify the grammar somewhat."] pub fn parse_pat_no_top_guard (& mut self , expected : Option < Expected > , rc : RecoverComma , ra : RecoverColon , rt : CommaRecoveryMode ,) -> PResult < 'a , Box < Pat > > { self . parse_pat_no_top_guard_inner (expected , rc , ra , rt , None) . map (| (pat , _) | pat) } # [doc = " Returns the pattern and a bool indicating whether we recovered from a trailing vert (true ="] # [doc = " recovered)."] fn parse_pat_no_top_guard_inner (& mut self , expected : Option < Expected > , rc : RecoverComma , ra : RecoverColon , rt : CommaRecoveryMode , syntax_loc : Option < PatternLocation > ,) -> PResult < 'a , (Box < Pat > , bool) > { let (leading_vert_span , mut trailing_vert) = match self . eat_or_separator (None) { EatOrResult :: AteOr => (Some (self . prev_token . span) , false) , EatOrResult :: TrailingVert => (None , true) , EatOrResult :: None => (None , false) , } ; let mut first_pat = match self . parse_pat_no_top_alt (expected , syntax_loc) { Ok (pat) => pat , Err (err) if self . token . is_reserved_ident () && ! self . token . is_keyword (kw :: In) && ! self . token . is_keyword (kw :: If) => { err . emit () ; self . bump () ; self . mk_pat (self . token . span , PatKind :: Wild) } Err (err) => return Err (err) , } ; if rc == RecoverComma :: Yes && ! first_pat . could_be_never_pattern () { self . maybe_recover_unexpected_comma (first_pat . span , rt) ? ; } if ! self . check (exp ! (Or)) && self . token != token :: OrOr { if ra == RecoverColon :: Yes { first_pat = self . maybe_recover_colon_colon_in_pat_typo (first_pat , expected) ; } if let Some (leading_vert_span) = leading_vert_span { let span = leading_vert_span . to (self . prev_token . span) ; return Ok ((self . mk_pat (span , PatKind :: Or (thin_vec ! [first_pat])) , trailing_vert)) ; } return Ok ((first_pat , trailing_vert)) ; } let lo = leading_vert_span . unwrap_or (first_pat . span) ; let mut pats = thin_vec ! [first_pat] ; loop { match self . eat_or_separator (Some (lo)) { EatOrResult :: AteOr => { } EatOrResult :: None => break , EatOrResult :: TrailingVert => { trailing_vert = true ; break ; } } let pat = self . parse_pat_no_top_alt (expected , syntax_loc) . map_err (| mut err | { err . span_label (lo , WHILE_PARSING_OR_MSG) ; err }) ? ; if rc == RecoverComma :: Yes && ! pat . could_be_never_pattern () { self . maybe_recover_unexpected_comma (pat . span , rt) ? ; } pats . push (pat) ; } let or_pattern_span = lo . to (self . prev_token . span) ; Ok ((self . mk_pat (or_pattern_span , PatKind :: Or (pats)) , trailing_vert)) } # [doc = " Parse a pattern and (maybe) a `Colon` in positions where a pattern may be followed by a"] # [doc = " type annotation (e.g. for `let` bindings or `fn` params)."] # [doc = ""] # [doc = " Generally, this corresponds to `pat_no_top_alt` followed by an optional `Colon`. It will"] # [doc = " eat the `Colon` token if one is present."] # [doc = ""] # [doc = " The return value represents the parsed pattern and `true` if a `Colon` was parsed (`false`"] # [doc = " otherwise)."] pub (super) fn parse_pat_before_ty (& mut self , expected : Option < Expected > , rc : RecoverComma , syntax_loc : PatternLocation ,) -> PResult < 'a , (Box < Pat > , bool) > { let (pat , trailing_vert) = self . parse_pat_no_top_guard_inner (expected , rc , RecoverColon :: No , CommaRecoveryMode :: LikelyTuple , Some (syntax_loc) ,) ? ; let colon = self . eat (exp ! (Colon)) ; if let PatKind :: Or (pats) = & pat . kind { let span = pat . span ; let sub = if let [_] = & pats [..] { let span = span . with_hi (span . lo () + BytePos (1)) ; Some (TopLevelOrPatternNotAllowedSugg :: RemoveLeadingVert { span }) } else { Some (TopLevelOrPatternNotAllowedSugg :: WrapInParens { span , suggestion : WrapInParens { lo : span . shrink_to_lo () , hi : span . shrink_to_hi () } , }) } ; let err = self . dcx () . create_err (match syntax_loc { PatternLocation :: LetBinding => { TopLevelOrPatternNotAllowed :: LetBinding { span , sub } } PatternLocation :: FunctionParameter => { TopLevelOrPatternNotAllowed :: FunctionParameter { span , sub } } }) ; if trailing_vert { err . delay_as_bug () ; } else { err . emit () ; } } Ok ((pat , colon)) } # [doc = " Parse the pattern for a function or function pointer parameter, followed by a colon."] # [doc = ""] # [doc = " The return value represents the parsed pattern and `true` if a `Colon` was parsed (`false`"] # [doc = " otherwise)."] pub (super) fn parse_fn_param_pat_colon (& mut self) -> PResult < 'a , (Box < Pat > , bool) > { if let token :: OrOr = self . token . kind { self . dcx () . emit_err (UnexpectedVertVertBeforeFunctionParam { span : self . token . span }) ; self . bump () ; } self . parse_pat_before_ty (Some (Expected :: ParameterName) , RecoverComma :: No , PatternLocation :: FunctionParameter ,) } # [doc = " Eat the or-pattern `|` separator."] # [doc = " If instead a `||` token is encountered, recover and pretend we parsed `|`."] fn eat_or_separator (& mut self , lo : Option < Span >) -> EatOrResult { if self . recover_trailing_vert (lo) { EatOrResult :: TrailingVert } else if self . token . kind == token :: OrOr { self . dcx () . emit_err (UnexpectedVertVertInPattern { span : self . token . span , start : lo }) ; self . bump () ; EatOrResult :: AteOr } else if self . eat (exp ! (Or)) { EatOrResult :: AteOr } else { EatOrResult :: None } } # [doc = " Recover if `|` or `||` is the current token and we have one of the"] # [doc = " tokens `=>`, `if`, `=`, `:`, `;`, `,`, `]`, `)`, or `}` ahead of us."] # [doc = ""] # [doc = " These tokens all indicate that we reached the end of the or-pattern"] # [doc = " list and can now reliably say that the `|` was an illegal trailing vert."] # [doc = " Note that there are more tokens such as `@` for which we know that the `|`"] # [doc = " is an illegal parse. However, the user's intent is less clear in that case."] fn recover_trailing_vert (& mut self , lo : Option < Span >) -> bool { let is_end_ahead = self . look_ahead (1 , | token | { matches ! (& token . uninterpolate () . kind , token :: FatArrow | token :: Ident (kw :: If , token :: IdentIsRaw :: No) | token :: Eq | token :: Semi | token :: Colon | token :: Comma | token :: CloseBracket | token :: CloseParen | token :: CloseBrace) }) ; match (is_end_ahead , & self . token . kind) { (true , token :: Or | token :: OrOr) => { self . dcx () . emit_err (TrailingVertNotAllowed { span : self . token . span , start : lo , suggestion : TrailingVertSuggestion { span : self . prev_token . span . shrink_to_hi () . with_hi (self . token . span . hi ()) , } , token : self . token , note_double_vert : self . token . kind == token :: OrOr , }) ; self . bump () ; true } _ => false , } } # [doc = " Ensures that the last parsed pattern (or pattern range bound) is not followed by an expression."] # [doc = ""] # [doc = " `is_end_bound` indicates whether the last parsed thing was the end bound of a range pattern (see [`parse_pat_range_end`](Self::parse_pat_range_end))"] # [doc = " in order to say \"expected a pattern range bound\" instead of \"expected a pattern\";"] # [doc = " ```text"] # [doc = " 0..=1 + 2"] # [doc = "     ^^^^^"] # [doc = " ```"] # [doc = " Only the end bound is spanned in this case, and this function has no idea if there was a `..=` before `pat_span`, hence the parameter."] # [doc = ""] # [doc = " This function returns `Some` if a trailing expression was recovered, and said expression's span."] # [must_use = "the pattern must be discarded as `PatKind::Err` if this function returns Some"] fn maybe_recover_trailing_expr (& mut self , pat_span : Span , is_end_bound : bool ,) -> Option < (ErrorGuaranteed , Span) > { if self . prev_token . is_keyword (kw :: Underscore) || ! self . may_recover () { return None ; } let is_one_tuple_index = | _ : & Self , token : & Token | -> bool { use token :: { Lit , LitKind } ; matches ! (token . kind , token :: Literal (Lit { kind : LitKind :: Integer , symbol : _ , suffix : None })) } ; let is_two_tuple_indexes = | this : & Self , token : & Token | -> bool { use token :: { Lit , LitKind } ; if let token :: Literal (Lit { kind : LitKind :: Float , symbol , suffix : None }) = token . kind && let DestructuredFloat :: MiddleDot (..) = this . break_up_float (symbol , token . span) { true } else { false } } ; let has_dot_expr = self . check_noexpect (& token :: Dot) && self . look_ahead (1 , | tok | { tok . is_ident () || is_one_tuple_index (& self , & tok) || is_two_tuple_indexes (& self , & tok) }) ; let has_trailing_operator = matches ! (self . token . kind , token :: Plus | token :: Minus | token :: Star | token :: Slash | token :: Percent | token :: Caret | token :: And | token :: Shl | token :: Shr) || self . token == token :: Question || (self . token == token :: OpenBracket && self . look_ahead (1 , | t | * t != token :: CloseBracket)) || self . token . is_keyword (kw :: As) ; if ! has_dot_expr && ! has_trailing_operator { return None ; } let mut snapshot = self . create_snapshot_for_diagnostic () ; snapshot . restrictions . insert (Restrictions :: IS_PAT) ; let Ok (expr) = snapshot . parse_expr_dot_or_call_with (AttrVec :: new () , self . mk_expr (pat_span , ExprKind :: Dummy) , pat_span ,) . map_err (| err | err . cancel ()) else { return None ; } ; let Ok ((expr , _)) = snapshot . parse_expr_assoc_rest_with (Bound :: Unbounded , false , expr) . map_err (| err | err . cancel ()) else { return None ; } ; self . restore_snapshot (snapshot) ; self . restrictions . remove (Restrictions :: IS_PAT) ; let is_bound = is_end_bound || self . token . is_range_separator () || self . token == token :: CloseParen && self . look_ahead (1 , Token :: is_range_separator) ; let span = expr . span ; Some ((self . dcx () . create_err (UnexpectedExpressionInPattern { span , is_bound , expr_precedence : expr . precedence () , }) . stash (span , StashKey :: ExprInPat) . unwrap () , span ,)) } # [doc = " Called by [`Parser::parse_stmt_without_recovery`], used to add statement-aware subdiagnostics to the errors stashed"] # [doc = " by [`Parser::maybe_recover_trailing_expr`]."] pub (super) fn maybe_augment_stashed_expr_in_pats_with_suggestions (& mut self , stmt : & Stmt) { if self . dcx () . has_errors () . is_none () { return ; } struct PatVisitor < 'a > { # [doc = " `self`"] parser : & 'a Parser < 'a > , # [doc = " The freshly-parsed statement."] stmt : & 'a Stmt , # [doc = " The current match arm (for arm guard suggestions)."] arm : Option < & 'a Arm > , # [doc = " The current struct field (for variable name suggestions)."] field : Option < & 'a PatField > , } impl < 'a > PatVisitor < 'a > { # [doc = " Looks for stashed [`StashKey::ExprInPat`] errors in `stash_span`, and emit them with suggestions."] # [doc = " `stash_span` is contained in `expr_span`, the latter being larger in borrow patterns;"] # [doc = " ```txt"] # [doc = " &mut x.y"] # [doc = " -----^^^ `stash_span`"] # [doc = " |"] # [doc = " `expr_span`"] # [doc = " ```"] # [doc = " `is_range_bound` is used to exclude arm guard suggestions in range pattern bounds."] fn maybe_add_suggestions_then_emit (& self , stash_span : Span , expr_span : Span , is_range_bound : bool ,) { self . parser . dcx () . try_steal_modify_and_emit_err (stash_span , StashKey :: ExprInPat , | err | { err . span . replace (stash_span , expr_span) ; let sm = self . parser . psess . source_map () ; let stmt = self . stmt ; let line_lo = sm . span_extend_to_line (stmt . span) . shrink_to_lo () ; let indentation = sm . indentation_before (stmt . span) . unwrap_or_default () ; let Ok (expr) = self . parser . span_to_snippet (expr_span) else { return ; } ; if let StmtKind :: Let (local) = & stmt . kind { match & local . kind { LocalKind :: Decl | LocalKind :: Init (_) => { return ; } LocalKind :: InitElse (_ , _) => { } } } if let Some (arm) = & self . arm && ! is_range_bound { let (ident , ident_span) = match self . field { Some (field) => { (field . ident . to_string () , field . ident . span . to (expr_span)) } None => ("val" . to_owned () , expr_span) , } ; let expr = match & err . args ["expr_precedence"] { DiagArgValue :: Number (expr_precedence) => { if * expr_precedence <= ExprPrecedence :: Compare as i32 { format ! ("({expr})") } else { format ! ("{expr}") } } _ => unreachable ! () , } ; match & arm . guard { None => { err . subdiagnostic (UnexpectedExpressionInPatternSugg :: CreateGuard { ident_span , pat_hi : arm . pat . span . shrink_to_hi () , ident , expr , } ,) ; } Some (guard) => { let wrap_guard = guard . precedence () <= ExprPrecedence :: LAnd ; err . subdiagnostic (UnexpectedExpressionInPatternSugg :: UpdateGuard { ident_span , guard_lo : if wrap_guard { Some (guard . span . shrink_to_lo ()) } else { None } , guard_hi : guard . span . shrink_to_hi () , guard_hi_paren : if wrap_guard { ")" } else { "" } , ident , expr , } ,) ; } } } let ident = match self . field { Some (field) => field . ident . as_str () . to_uppercase () , None => "VAL" . to_owned () , } ; err . subdiagnostic (UnexpectedExpressionInPatternSugg :: Const { stmt_lo : line_lo , ident_span : expr_span , expr , ident , indentation , }) ; } ,) ; } } impl < 'a > Visitor < 'a > for PatVisitor < 'a > { fn visit_arm (& mut self , a : & 'a Arm) -> Self :: Result { self . arm = Some (a) ; visit :: walk_arm (self , a) ; self . arm = None ; } fn visit_pat_field (& mut self , fp : & 'a PatField) -> Self :: Result { self . field = Some (fp) ; visit :: walk_pat_field (self , fp) ; self . field = None ; } fn visit_pat (& mut self , p : & 'a Pat) -> Self :: Result { match & p . kind { PatKind :: Err (_) | PatKind :: Expr (_) => { self . maybe_add_suggestions_then_emit (p . span , p . span , false) } PatKind :: Box (subpat) | PatKind :: Ref (subpat , _) if matches ! (subpat . kind , PatKind :: Err (_) | PatKind :: Expr (_)) => { self . maybe_add_suggestions_then_emit (subpat . span , p . span , false) } PatKind :: Range (start , end , _) => { if let Some (start) = start { self . maybe_add_suggestions_then_emit (start . span , start . span , true) ; } if let Some (end) = end { self . maybe_add_suggestions_then_emit (end . span , end . span , true) ; } } _ => visit :: walk_pat (self , p) , } } } PatVisitor { parser : self , stmt , arm : None , field : None } . visit_stmt (stmt) ; } fn eat_metavar_pat (& mut self) -> Option < Box < Pat > > { if let Some (pat) = self . eat_metavar_seq_with_matcher (| mv_kind | matches ! (mv_kind , MetaVarKind :: Pat (PatParam { .. })) , | this | this . parse_pat_no_top_alt (None , None) ,) { Some (pat) } else if let Some (pat) = self . eat_metavar_seq (MetaVarKind :: Pat (PatWithOr) , | this | { this . parse_pat_no_top_guard (None , RecoverComma :: No , RecoverColon :: No , CommaRecoveryMode :: EitherTupleOrPipe ,) }) { Some (pat) } else { None } } # [doc = " Parses a pattern, with a setting whether modern range patterns (e.g., `a..=b`, `a..b` are"] # [doc = " allowed)."] fn parse_pat_with_range_pat (& mut self , allow_range_pat : bool , expected : Option < Expected > , syntax_loc : Option < PatternLocation > ,) -> PResult < 'a , Box < Pat > > { maybe_recover_from_interpolated_ty_qpath ! (self , true) ; if let Some (pat) = self . eat_metavar_pat () { return Ok (pat) ; } let mut lo = self . token . span ; if self . token . is_keyword (kw :: Let) && self . look_ahead (1 , | tok | { tok . can_begin_pattern (token :: NtPatKind :: PatParam { inferred : false }) }) { self . bump () ; let span = lo . with_hi (self . token . span . lo ()) ; self . dcx () . emit_err (RemoveLet { span : lo , suggestion : span }) ; lo = self . token . span ; } let pat = if self . check (exp ! (And)) || self . token == token :: AndAnd { self . parse_pat_deref (expected) ? } else if self . check (exp ! (OpenParen)) { self . parse_pat_tuple_or_parens () ? } else if self . check (exp ! (OpenBracket)) { let (pats , _) = self . parse_delim_comma_seq (exp ! (OpenBracket) , exp ! (CloseBracket) , | p | { p . parse_pat_allow_top_guard (None , RecoverComma :: No , RecoverColon :: No , CommaRecoveryMode :: EitherTupleOrPipe ,) }) ? ; PatKind :: Slice (pats) } else if self . check (exp ! (DotDot)) && ! self . is_pat_range_end_start (1) { self . bump () ; PatKind :: Rest } else if self . check (exp ! (DotDotDot)) && ! self . is_pat_range_end_start (1) { self . recover_dotdotdot_rest_pat (lo) } else if let Some (form) = self . parse_range_end () { self . parse_pat_range_to (form) ? } else if self . eat (exp ! (Bang)) { self . psess . gated_spans . gate (sym :: never_patterns , self . prev_token . span) ; PatKind :: Never } else if self . eat_keyword (exp ! (Underscore)) { PatKind :: Wild } else if self . eat_keyword (exp ! (Mut)) { self . parse_pat_ident_mut () ? } else if self . eat_keyword (exp ! (Ref)) { if self . check_keyword (exp ! (Box)) { let span = self . prev_token . span . to (self . token . span) ; self . bump () ; self . dcx () . emit_err (SwitchRefBoxOrder { span }) ; } let mutbl = self . parse_mutability () ; self . parse_pat_ident (BindingMode (ByRef :: Yes (mutbl) , Mutability :: Not) , syntax_loc) ? } else if self . eat_keyword (exp ! (Box)) { self . parse_pat_box () ? } else if self . check_inline_const (0) { let const_expr = self . parse_const_block (lo . to (self . token . span) , true) ? ; if let Some (re) = self . parse_range_end () { self . parse_pat_range_begin_with (const_expr , re) ? } else { PatKind :: Expr (const_expr) } } else if self . is_builtin () { self . parse_pat_builtin () ? } else if self . can_be_ident_pat () || (self . is_lit_bad_ident () . is_some () && self . may_recover ()) { self . parse_pat_ident (BindingMode :: NONE , syntax_loc) ? } else if self . is_start_of_pat_with_path () { let (qself , path) = if self . eat_lt () { let (qself , path) = self . parse_qpath (PathStyle :: Pat) ? ; (Some (qself) , path) } else { (None , self . parse_path (PathStyle :: Pat) ?) } ; let span = lo . to (self . prev_token . span) ; if qself . is_none () && self . check (exp ! (Bang)) { self . parse_pat_mac_invoc (path) ? } else if let Some (form) = self . parse_range_end () { let begin = self . mk_expr (span , ExprKind :: Path (qself , path)) ; self . parse_pat_range_begin_with (begin , form) ? } else if self . check (exp ! (OpenBrace)) { self . parse_pat_struct (qself , path) ? } else if self . check (exp ! (OpenParen)) { self . parse_pat_tuple_struct (qself , path) ? } else { match self . maybe_recover_trailing_expr (span , false) { Some ((guar , _)) => PatKind :: Err (guar) , None => PatKind :: Path (qself , path) , } } } else if let Some ((lt , IdentIsRaw :: No)) = self . token . lifetime () && could_be_unclosed_char_literal (lt) && ! self . look_ahead (1 , | token | token . kind == token :: Colon) { let lt = self . expect_lifetime () ; let (lit , _) = self . recover_unclosed_char (lt . ident , Parser :: mk_token_lit_char , | self_ | { let expected = Expected :: to_string_or_fallback (expected) ; let msg = format ! ("expected {}, found {}" , expected , super :: token_descr (& self_ . token)) ; self_ . dcx () . struct_span_err (self_ . token . span , msg) . with_span_label (self_ . token . span , format ! ("expected {expected}")) }) ; PatKind :: Expr (self . mk_expr (lo , ExprKind :: Lit (lit))) } else { match self . parse_literal_maybe_minus () { Ok (begin) => { let begin = self . maybe_recover_trailing_expr (begin . span , false) . map (| (guar , sp) | self . mk_expr_err (sp , guar)) . unwrap_or (begin) ; match self . parse_range_end () { Some (form) => self . parse_pat_range_begin_with (begin , form) ? , None => PatKind :: Expr (begin) , } } Err (err) => return self . fatal_unexpected_non_pat (err , expected) , } } ; let pat = self . mk_pat (lo . to (self . prev_token . span) , pat) ; let pat = self . maybe_recover_from_bad_qpath (pat) ? ; let pat = self . recover_intersection_pat (pat) ? ; if ! allow_range_pat { self . ban_pat_range_if_ambiguous (& pat) } Ok (pat) } # [doc = " Recover from a typoed `...` pattern that was encountered"] # [doc = " Ref: Issue #70388"] fn recover_dotdotdot_rest_pat (& mut self , lo : Span) -> PatKind { self . bump () ; self . dcx () . emit_err (DotDotDotRestPattern { span : lo , suggestion : lo . with_lo (lo . hi () - BytePos (1)) , }) ; PatKind :: Rest } # [doc = " Try to recover the more general form `intersect ::= $pat_lhs @ $pat_rhs`."] # [doc = ""] # [doc = " Allowed binding patterns generated by `binding ::= ref? mut? $ident @ $pat_rhs`"] # [doc = " should already have been parsed by now at this point,"] # [doc = " if the next token is `@` then we can try to parse the more general form."] # [doc = ""] # [doc = " Consult `parse_pat_ident` for the `binding` grammar."] # [doc = ""] # [doc = " The notion of intersection patterns are found in"] # [doc = " e.g. [F#][and] where they are called AND-patterns."] # [doc = ""] # [doc = " [and]: https://docs.microsoft.com/en-us/dotnet/fsharp/language-reference/pattern-matching"] fn recover_intersection_pat (& mut self , lhs : Box < Pat >) -> PResult < 'a , Box < Pat > > { if self . token != token :: At { return Ok (lhs) ; } self . bump () ; let mut rhs = self . parse_pat_no_top_alt (None , None) ? ; let whole_span = lhs . span . to (rhs . span) ; if let PatKind :: Ident (_ , _ , sub @ None) = & mut rhs . kind { let lhs_span = lhs . span ; * sub = Some (lhs) ; self . dcx () . emit_err (PatternOnWrongSideOfAt { whole_span , whole_pat : pprust :: pat_to_string (& rhs) , pattern : lhs_span , binding : rhs . span , }) ; } else { rhs . kind = PatKind :: Wild ; self . dcx () . emit_err (ExpectedBindingLeftOfAt { whole_span , lhs : lhs . span , rhs : rhs . span , }) ; } rhs . span = whole_span ; Ok (rhs) } # [doc = " Ban a range pattern if it has an ambiguous interpretation."] fn ban_pat_range_if_ambiguous (& self , pat : & Pat) { match pat . kind { PatKind :: Range (.. , Spanned { node : RangeEnd :: Included (RangeSyntax :: DotDotDot) , .. } ,) => return , PatKind :: Range (..) => { } _ => return , } self . dcx () . emit_err (AmbiguousRangePattern { span : pat . span , suggestion : ParenRangeSuggestion { lo : pat . span . shrink_to_lo () , hi : pat . span . shrink_to_hi () , } , }) ; } # [doc = " Parse `&pat` / `&mut pat`."] fn parse_pat_deref (& mut self , expected : Option < Expected >) -> PResult < 'a , PatKind > { self . expect_and () ? ; if let Some ((lifetime , _)) = self . token . lifetime () { self . bump () ; self . dcx () . emit_err (UnexpectedLifetimeInPattern { span : self . prev_token . span , symbol : lifetime . name , suggestion : self . prev_token . span . until (self . token . span) , }) ; } let mutbl = self . parse_mutability () ; let subpat = self . parse_pat_with_range_pat (false , expected , None) ? ; Ok (PatKind :: Ref (subpat , mutbl)) } # [doc = " Parse a tuple or parenthesis pattern."] fn parse_pat_tuple_or_parens (& mut self) -> PResult < 'a , PatKind > { let open_paren = self . token . span ; let (fields , trailing_comma) = self . parse_paren_comma_seq (| p | { p . parse_pat_allow_top_guard (None , RecoverComma :: No , RecoverColon :: No , CommaRecoveryMode :: LikelyTuple ,) }) ? ; let paren_pattern = fields . len () == 1 && ! (matches ! (trailing_comma , Trailing :: Yes) || fields [0] . is_rest ()) ; let pat = if paren_pattern { let pat = fields . into_iter () . next () . unwrap () ; let close_paren = self . prev_token . span ; match & pat . kind { PatKind :: Expr (begin) if self . may_recover () && let Some (form) = self . parse_range_end () => { self . dcx () . emit_err (UnexpectedParenInRangePat { span : vec ! [open_paren , close_paren] , sugg : UnexpectedParenInRangePatSugg { start_span : open_paren , end_span : close_paren , } , }) ; self . parse_pat_range_begin_with (begin . clone () , form) ? } PatKind :: Err (guar) if self . may_recover () && let Some (form) = self . parse_range_end () => { self . dcx () . emit_err (UnexpectedParenInRangePat { span : vec ! [open_paren , close_paren] , sugg : UnexpectedParenInRangePatSugg { start_span : open_paren , end_span : close_paren , } , }) ; self . parse_pat_range_begin_with (self . mk_expr_err (pat . span , * guar) , form) ? } _ => PatKind :: Paren (pat) , } } else { PatKind :: Tuple (fields) } ; Ok (match self . maybe_recover_trailing_expr (open_paren . to (self . prev_token . span) , false) { None => pat , Some ((guar , _)) => PatKind :: Err (guar) , }) } # [doc = " Parse a mutable binding with the `mut` token already eaten."] fn parse_pat_ident_mut (& mut self) -> PResult < 'a , PatKind > { let mut_span = self . prev_token . span ; self . recover_additional_muts () ; let byref = self . parse_byref () ; self . recover_additional_muts () ; if let Some (MetaVarKind :: Pat (_)) = self . token . is_metavar_seq () { self . expected_ident_found_err () . emit () ; } let mut pat = self . parse_pat_no_top_alt (Some (Expected :: Identifier) , None) ? ; if let PatKind :: Ident (BindingMode (br @ ByRef :: No , m @ Mutability :: Not) , ..) = & mut pat . kind { * br = byref ; * m = Mutability :: Mut ; } else { let changed_any_binding = Self :: make_all_value_bindings_mutable (& mut pat) ; self . ban_mut_general_pat (mut_span , & pat , changed_any_binding) ; } if matches ! (pat . kind , PatKind :: Ident (BindingMode (ByRef :: Yes (_) , Mutability :: Mut) , ..)) { self . psess . gated_spans . gate (sym :: mut_ref , pat . span) ; } Ok (pat . kind) } # [doc = " Turn all by-value immutable bindings in a pattern into mutable bindings."] # [doc = " Returns `true` if any change was made."] fn make_all_value_bindings_mutable (pat : & mut Box < Pat >) -> bool { struct AddMut (bool) ; impl MutVisitor for AddMut { fn visit_pat (& mut self , pat : & mut Pat) { if let PatKind :: Ident (BindingMode (ByRef :: No , m @ Mutability :: Not) , ..) = & mut pat . kind { self . 0 = true ; * m = Mutability :: Mut ; } mut_visit :: walk_pat (self , pat) ; } } let mut add_mut = AddMut (false) ; add_mut . visit_pat (pat) ; add_mut . 0 } # [doc = " Error on `mut $pat` where `$pat` is not an ident."] fn ban_mut_general_pat (& self , lo : Span , pat : & Pat , changed_any_binding : bool) { self . dcx () . emit_err (if changed_any_binding { InvalidMutInPattern :: NestedIdent { span : lo . to (pat . span) , pat : pprust :: pat_to_string (pat) , } } else { InvalidMutInPattern :: NonIdent { span : lo . until (pat . span) } }) ; } # [doc = " Eat any extraneous `mut`s and error + recover if we ate any."] fn recover_additional_muts (& mut self) { let lo = self . token . span ; while self . eat_keyword (exp ! (Mut)) { } if lo == self . token . span { return ; } let span = lo . to (self . prev_token . span) ; let suggestion = span . with_hi (self . token . span . lo ()) ; self . dcx () . emit_err (RepeatedMutInPattern { span , suggestion }) ; } # [doc = " Parse macro invocation"] fn parse_pat_mac_invoc (& mut self , path : Path) -> PResult < 'a , PatKind > { self . bump () ; let args = self . parse_delim_args () ? ; let mac = Box :: new (MacCall { path , args }) ; Ok (PatKind :: MacCall (mac)) } fn fatal_unexpected_non_pat (& mut self , err : Diag < 'a > , expected : Option < Expected > ,) -> PResult < 'a , Box < Pat > > { err . cancel () ; let expected = Expected :: to_string_or_fallback (expected) ; let msg = format ! ("expected {}, found {}" , expected , super :: token_descr (& self . token)) ; let mut err = self . dcx () . struct_span_err (self . token . span , msg) ; err . span_label (self . token . span , format ! ("expected {expected}")) ; let sp = self . psess . source_map () . start_point (self . token . span) ; if let Some (sp) = self . psess . ambiguous_block_expr_parse . borrow () . get (& sp) { err . subdiagnostic (ExprParenthesesNeeded :: surrounding (* sp)) ; } Err (err) } # [doc = " Parses the range pattern end form `\"..\" | \"...\" | \"..=\" ;`."] fn parse_range_end (& mut self) -> Option < Spanned < RangeEnd > > { let re = if self . eat (exp ! (DotDotDot)) { RangeEnd :: Included (RangeSyntax :: DotDotDot) } else if self . eat (exp ! (DotDotEq)) { RangeEnd :: Included (RangeSyntax :: DotDotEq) } else if self . eat (exp ! (DotDot)) { RangeEnd :: Excluded } else { return None ; } ; Some (respan (self . prev_token . span , re)) } # [doc = " Parse a range pattern `$begin $form $end?` where `$form = \"..\" | \"...\" | \"..=\" ;`."] # [doc = " `$begin $form` has already been parsed."] fn parse_pat_range_begin_with (& mut self , begin : Box < Expr > , re : Spanned < RangeEnd > ,) -> PResult < 'a , PatKind > { let end = if self . is_pat_range_end_start (0) { Some (self . parse_pat_range_end () ?) } else { if let RangeEnd :: Included (_) = re . node { self . inclusive_range_with_incorrect_end () ; } None } ; Ok (PatKind :: Range (Some (begin) , end , re)) } pub (super) fn inclusive_range_with_incorrect_end (& mut self) -> ErrorGuaranteed { let tok = & self . token ; let span = self . prev_token . span ; let no_space = tok . span . lo () == span . hi () ; match tok . kind { token :: Eq if no_space => { let span_with_eq = span . to (tok . span) ; self . bump () ; if self . is_pat_range_end_start (0) { let _ = self . parse_pat_range_end () . map_err (| e | e . cancel ()) ; } self . dcx () . emit_err (InclusiveRangeExtraEquals { span : span_with_eq }) } token :: Gt if no_space => { let after_pat = span . with_hi (span . hi () - BytePos (1)) . shrink_to_hi () ; self . dcx () . emit_err (InclusiveRangeMatchArrow { span , arrow : tok . span , after_pat }) } _ => self . dcx () . emit_err (InclusiveRangeNoEnd { span , suggestion : span . with_lo (span . hi () - BytePos (1)) , }) , } } # [doc = " Parse a range-to pattern, `..X` or `..=X` where `X` remains to be parsed."] # [doc = ""] # [doc = " The form `...X` is prohibited to reduce confusion with the potential"] # [doc = " expression syntax `...expr` for splatting in expressions."] fn parse_pat_range_to (& mut self , mut re : Spanned < RangeEnd >) -> PResult < 'a , PatKind > { let end = self . parse_pat_range_end () ? ; if let RangeEnd :: Included (syn @ RangeSyntax :: DotDotDot) = & mut re . node { * syn = RangeSyntax :: DotDotEq ; self . dcx () . emit_err (DotDotDotRangeToPatternNotAllowed { span : re . span }) ; } Ok (PatKind :: Range (None , Some (end) , re)) } # [doc = " Is the token `dist` away from the current suitable as the start of a range patterns end?"] fn is_pat_range_end_start (& self , dist : usize) -> bool { self . check_inline_const (dist) || self . look_ahead (dist , | t | { t . is_path_start () || * t == token :: Dot || matches ! (t . kind , token :: Literal (..) | token :: Minus) || t . is_bool_lit () || t . is_metavar_expr () || t . is_lifetime () || (self . may_recover () && * t == token :: OpenParen && self . look_ahead (dist + 1 , | t | * t != token :: OpenParen) && self . is_pat_range_end_start (dist + 1)) }) } # [doc = " Parse a range pattern end bound"] fn parse_pat_range_end (& mut self) -> PResult < 'a , Box < Expr > > { let open_paren = (self . may_recover () && self . eat_noexpect (& token :: OpenParen)) . then_some (self . prev_token . span) ; let bound = if self . check_inline_const (0) { self . parse_const_block (self . token . span , true) } else if self . check_path () { let lo = self . token . span ; let (qself , path) = if self . eat_lt () { let (qself , path) = self . parse_qpath (PathStyle :: Pat) ? ; (Some (qself) , path) } else { (None , self . parse_path (PathStyle :: Pat) ?) } ; let hi = self . prev_token . span ; Ok (self . mk_expr (lo . to (hi) , ExprKind :: Path (qself , path))) } else { self . parse_literal_maybe_minus () } ? ; let recovered = self . maybe_recover_trailing_expr (bound . span , true) ; if let Some (open_paren) = open_paren { self . expect (exp ! (CloseParen)) ? ; self . dcx () . emit_err (UnexpectedParenInRangePat { span : vec ! [open_paren , self . prev_token . span] , sugg : UnexpectedParenInRangePatSugg { start_span : open_paren , end_span : self . prev_token . span , } , }) ; } Ok (match recovered { Some ((guar , sp)) => self . mk_expr_err (sp , guar) , None => bound , }) } # [doc = " Is this the start of a pattern beginning with a path?"] fn is_start_of_pat_with_path (& mut self) -> bool { self . check_path () || self . token . is_ident () && ! self . token . is_bool_lit () && ! self . token . is_keyword (kw :: In) } # [doc = " Would `parse_pat_ident` be appropriate here?"] fn can_be_ident_pat (& mut self) -> bool { self . check_ident () && ! self . token . is_bool_lit () && ! self . token . is_path_segment_keyword () && ! self . token . is_keyword (kw :: In) && self . look_ahead (1 , | t | ! matches ! (t . kind , token :: OpenParen | token :: OpenBrace | token :: DotDotDot | token :: DotDotEq | token :: DotDot | token :: PathSep | token :: Bang)) } # [doc = " Parses `ident` or `ident @ pat`."] # [doc = " Used by the copy foo and ref foo patterns to give a good"] # [doc = " error message when parsing mistakes like `ref foo(a, b)`."] fn parse_pat_ident (& mut self , binding_annotation : BindingMode , syntax_loc : Option < PatternLocation > ,) -> PResult < 'a , PatKind > { let ident = self . parse_ident_common (false) ? ; if self . may_recover () && ! matches ! (syntax_loc , Some (PatternLocation :: FunctionParameter)) && self . check_noexpect (& token :: Lt) && self . look_ahead (1 , | t | t . can_begin_type ()) { return Err (self . dcx () . create_err (GenericArgsInPatRequireTurbofishSyntax { span : self . token . span , suggest_turbofish : self . token . span . shrink_to_lo () , })) ; } let sub = if self . eat (exp ! (At)) { Some (self . parse_pat_no_top_alt (Some (Expected :: BindingPattern) , None) ?) } else { None } ; if self . token == token :: OpenParen { return Err (self . dcx () . create_err (EnumPatternInsteadOfIdentifier { span : self . prev_token . span })) ; } let pat = if sub . is_none () && let Some ((guar , _)) = self . maybe_recover_trailing_expr (ident . span , false) { PatKind :: Err (guar) } else { PatKind :: Ident (binding_annotation , ident , sub) } ; Ok (pat) } # [doc = " Parse a struct (\"record\") pattern (e.g. `Foo { ... }` or `Foo::Bar { ... }`)."] fn parse_pat_struct (& mut self , qself : Option < Box < QSelf > > , path : Path) -> PResult < 'a , PatKind > { if qself . is_some () { self . psess . gated_spans . gate (sym :: more_qualified_paths , path . span) ; } self . bump () ; let (fields , etc) = self . parse_pat_fields () . unwrap_or_else (| mut e | { e . span_label (path . span , "while parsing the fields for this pattern") ; let guar = e . emit () ; self . recover_stmt () ; (ThinVec :: new () , PatFieldsRest :: Recovered (guar)) }) ; self . bump () ; Ok (PatKind :: Struct (qself , path , fields , etc)) } # [doc = " Parse tuple struct or tuple variant pattern (e.g. `Foo(...)` or `Foo::Bar(...)`)."] fn parse_pat_tuple_struct (& mut self , qself : Option < Box < QSelf > > , path : Path ,) -> PResult < 'a , PatKind > { let (fields , _) = self . parse_paren_comma_seq (| p | { p . parse_pat_allow_top_guard (None , RecoverComma :: No , RecoverColon :: No , CommaRecoveryMode :: EitherTupleOrPipe ,) }) ? ; if qself . is_some () { self . psess . gated_spans . gate (sym :: more_qualified_paths , path . span) ; } Ok (PatKind :: TupleStruct (qself , path , fields)) } # [doc = " Are we sure this could not possibly be the start of a pattern?"] # [doc = ""] # [doc = " Currently, this only accounts for tokens that can follow identifiers"] # [doc = " in patterns, but this can be extended as necessary."] fn isnt_pattern_start (& self) -> bool { [token :: Eq , token :: Colon , token :: Comma , token :: Semi , token :: At , token :: OpenBrace , token :: CloseBrace , token :: CloseParen ,] . contains (& self . token . kind) } fn parse_pat_builtin (& mut self) -> PResult < 'a , PatKind > { self . parse_builtin (| self_ , _lo , ident | { Ok (match ident . name { sym :: deref => Some (ast :: PatKind :: Deref (self_ . parse_pat_allow_top_guard (None , RecoverComma :: Yes , RecoverColon :: Yes , CommaRecoveryMode :: LikelyTuple ,) ?)) , _ => None , }) }) } # [doc = " Parses `box pat`"] fn parse_pat_box (& mut self) -> PResult < 'a , PatKind > { let box_span = self . prev_token . span ; if self . isnt_pattern_start () { let descr = super :: token_descr (& self . token) ; self . dcx () . emit_err (errors :: BoxNotPat { span : self . token . span , kw : box_span , lo : box_span . shrink_to_lo () , descr , }) ; let sub = if self . eat (exp ! (At)) { Some (self . parse_pat_no_top_alt (Some (Expected :: BindingPattern) , None) ?) } else { None } ; Ok (PatKind :: Ident (BindingMode :: NONE , Ident :: new (kw :: Box , box_span) , sub)) } else { let pat = self . parse_pat_with_range_pat (false , None , None) ? ; self . psess . gated_spans . gate (sym :: box_patterns , box_span . to (self . prev_token . span)) ; Ok (PatKind :: Box (pat)) } } # [doc = " Parses the fields of a struct-like pattern."] fn parse_pat_fields (& mut self) -> PResult < 'a , (ThinVec < PatField > , PatFieldsRest) > { let mut fields : ThinVec < PatField > = ThinVec :: new () ; let mut etc = PatFieldsRest :: None ; let mut ate_comma = true ; let mut delayed_err : Option < Diag < 'a > > = None ; let mut first_etc_and_maybe_comma_span = None ; let mut last_non_comma_dotdot_span = None ; while self . token != token :: CloseBrace { if ! ate_comma { let err = if self . token == token :: At { let prev_field = fields . last () . expect ("Unreachable on first iteration, not empty otherwise") . ident ; self . report_misplaced_at_in_struct_pat (prev_field) } else { let mut err = self . dcx () . create_err (ExpectedCommaAfterPatternField { span : self . token . span }) ; self . recover_misplaced_pattern_modifiers (& fields , & mut err) ; err } ; if let Some (delayed) = delayed_err { delayed . emit () ; } return Err (err) ; } ate_comma = false ; if self . check (exp ! (DotDot)) || self . check_noexpect (& token :: DotDotDot) || self . check_keyword (exp ! (Underscore)) { etc = PatFieldsRest :: Rest (self . token . span) ; let mut etc_sp = self . token . span ; if first_etc_and_maybe_comma_span . is_none () { if let Some (comma_tok) = self . look_ahead (1 , | & t | if t == token :: Comma { Some (t) } else { None }) { let nw_span = self . psess . source_map () . span_extend_to_line (comma_tok . span) . trim_start (comma_tok . span . shrink_to_lo ()) . map (| s | self . psess . source_map () . span_until_non_whitespace (s)) ; first_etc_and_maybe_comma_span = nw_span . map (| s | etc_sp . to (s)) ; } else { first_etc_and_maybe_comma_span = Some (self . psess . source_map () . span_until_non_whitespace (etc_sp)) ; } } self . recover_bad_dot_dot () ; self . bump () ; if self . token == token :: CloseBrace { break ; } let token_str = super :: token_descr (& self . token) ; let msg = format ! ("expected `}}`, found {token_str}") ; let mut err = self . dcx () . struct_span_err (self . token . span , msg) ; err . span_label (self . token . span , "expected `}`") ; let mut comma_sp = None ; if self . token == token :: Comma { let nw_span = self . psess . source_map () . span_until_non_whitespace (self . token . span) ; etc_sp = etc_sp . to (nw_span) ; err . span_label (etc_sp , "`..` must be at the end and cannot have a trailing comma" ,) ; comma_sp = Some (self . token . span) ; self . bump () ; ate_comma = true ; } if self . token == token :: CloseBrace { if let Some (sp) = comma_sp { err . span_suggestion_short (sp , "remove this comma" , "" , Applicability :: MachineApplicable ,) ; } err . emit () ; break ; } else if self . token . is_ident () && ate_comma { if let Some (delayed_err) = delayed_err { delayed_err . emit () ; return Err (err) ; } else { delayed_err = Some (err) ; } } else { if let Some (err) = delayed_err { err . emit () ; } return Err (err) ; } } let attrs = match self . parse_outer_attributes () { Ok (attrs) => attrs , Err (err) => { if let Some (delayed) = delayed_err { delayed . emit () ; } return Err (err) ; } } ; let lo = self . token . span ; let field = self . collect_tokens (None , attrs , ForceCollect :: No , | this , attrs | { let field = match this . parse_pat_field (lo , attrs) { Ok (field) => Ok (field) , Err (err) => { if let Some (delayed_err) = delayed_err . take () { delayed_err . emit () ; } return Err (err) ; } } ? ; ate_comma = this . eat (exp ! (Comma)) ; last_non_comma_dotdot_span = Some (this . prev_token . span) ; Ok ((field , Trailing :: No , UsePreAttrPos :: No)) }) ? ; fields . push (field) } if let Some (mut err) = delayed_err { if let Some (first_etc_span) = first_etc_and_maybe_comma_span { if self . prev_token == token :: DotDot { err . multipart_suggestion ("remove the starting `..`" , vec ! [(first_etc_span , String :: new ())] , Applicability :: MachineApplicable ,) ; } else if let Some (last_non_comma_dotdot_span) = last_non_comma_dotdot_span { err . multipart_suggestion ("move the `..` to the end of the field list" , vec ! [(first_etc_span , String :: new ()) , (self . token . span . to (last_non_comma_dotdot_span . shrink_to_hi ()) , format ! ("{} .. }}" , if ate_comma { "" } else { "," }) ,) ,] , Applicability :: MachineApplicable ,) ; } } err . emit () ; } Ok ((fields , etc)) } # [deny (rustc :: untranslatable_diagnostic)] fn report_misplaced_at_in_struct_pat (& self , prev_field : Ident) -> Diag < 'a > { debug_assert_eq ! (self . token , token :: At) ; let span = prev_field . span . to (self . token . span) ; if let Some (dot_dot_span) = self . look_ahead (1 , | t | if t == & token :: DotDot { Some (t . span) } else { None }) { self . dcx () . create_err (AtDotDotInStructPattern { span : span . to (dot_dot_span) , remove : span . until (dot_dot_span) , ident : prev_field , }) } else { self . dcx () . create_err (AtInStructPattern { span }) } } # [doc = " If the user writes `S { ref field: name }` instead of `S { field: ref name }`, we suggest"] # [doc = " the correct code."] fn recover_misplaced_pattern_modifiers (& self , fields : & ThinVec < PatField > , err : & mut Diag < 'a >) { if let Some (last) = fields . iter () . last () && last . is_shorthand && let PatKind :: Ident (binding , ident , None) = last . pat . kind && binding != BindingMode :: NONE && self . token == token :: Colon && let Some (name_span) = self . look_ahead (1 , | t | t . is_ident () . then (| | t . span)) && self . look_ahead (2 , | t | { t == & token :: Comma || t == & token :: CloseBrace }) { let span = last . pat . span . with_hi (ident . span . lo ()) ; err . multipart_suggestion ("the pattern modifiers belong after the `:`" , vec ! [(span , String :: new ()) , (name_span . shrink_to_lo () , binding . prefix_str () . to_string ()) ,] , Applicability :: MachineApplicable ,) ; } } # [doc = " Recover on `...` or `_` as if it were `..` to avoid further errors."] # [doc = " See issue #46718."] fn recover_bad_dot_dot (& self) { if self . token == token :: DotDot { return ; } let token_str = pprust :: token_to_string (& self . token) ; self . dcx () . emit_err (DotDotDotForRemainingFields { span : self . token . span , token_str }) ; } fn parse_pat_field (& mut self , lo : Span , attrs : AttrVec) -> PResult < 'a , PatField > { let hi ; let (subpat , fieldname , is_shorthand) = if self . look_ahead (1 , | t | t == & token :: Colon) { let fieldname = self . parse_field_name () ? ; self . bump () ; let pat = self . parse_pat_allow_top_guard (None , RecoverComma :: No , RecoverColon :: No , CommaRecoveryMode :: EitherTupleOrPipe ,) ? ; hi = pat . span ; (pat , fieldname , false) } else { let is_box = self . eat_keyword (exp ! (Box)) ; let boxed_span = self . token . span ; let mutability = self . parse_mutability () ; let by_ref = self . parse_byref () ; let fieldname = self . parse_field_name () ? ; hi = self . prev_token . span ; let ann = BindingMode (by_ref , mutability) ; let fieldpat = self . mk_pat_ident (boxed_span . to (hi) , ann , fieldname) ; let subpat = if is_box { self . mk_pat (lo . to (hi) , PatKind :: Box (fieldpat)) } else { fieldpat } ; (subpat , fieldname , true) } ; Ok (PatField { ident : fieldname , pat : subpat , is_shorthand , attrs , id : ast :: DUMMY_NODE_ID , span : lo . to (hi) , is_placeholder : false , }) } pub (super) fn mk_pat_ident (& self , span : Span , ann : BindingMode , ident : Ident) -> Box < Pat > { self . mk_pat (span , PatKind :: Ident (ann , ident , None)) } pub (super) fn mk_pat (& self , span : Span , kind : PatKind) -> Box < Pat > { Box :: new (Pat { kind , span , id : ast :: DUMMY_NODE_ID , tokens : None }) } }