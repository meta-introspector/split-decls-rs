/* FP:invariant.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_ty_utils_src_layout_invariant_USE_0001
/* FP:invariant.rs-0002 */ use std :: assert_matches :: assert_matches ;
/* FP:invariant.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_ty_utils_src_layout_invariant_USE_0002
/* FP:invariant.rs-0004 */ use crate :: rustc_abi :: { BackendRepr , FieldsShape , Scalar , Size , TagEncoding , Variants } ;
/* FP:invariant.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_ty_utils_src_layout_invariant_USE_0003
/* FP:invariant.rs-0006 */ use crate :: rustc_complete :: bug ;
/* FP:invariant.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_ty_utils_src_layout_invariant_USE_0004
/* FP:invariant.rs-0008 */ use crate :: rustc_complete :: ty :: layout :: { HasTyCtxt , LayoutCx , TyAndLayout } ;
/* FP:invariant.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_ty_utils_src_layout_invariant_FN_0005
/* FP:invariant.rs-0010 */ # [doc = " Enforce some basic invariants on layouts."] pub (super) fn layout_sanity_check < 'tcx > (cx : & LayoutCx < 'tcx > , layout : & TyAndLayout < 'tcx >) { let tcx = cx . tcx () ; if ! layout . size . bytes () . is_multiple_of (layout . align . abi . bytes ()) { bug ! ("size is not a multiple of align, in the following layout:\n{layout:#?}") ; } if layout . size . bytes () >= tcx . data_layout . obj_size_bound () { bug ! ("size is too large, in the following layout:\n{layout:#?}") ; } if ! cfg ! (debug_assertions) { return ; } if layout . ty . is_privately_uninhabited (tcx , cx . typing_env) { assert ! (layout . is_uninhabited () , "{:?} is type-level uninhabited but not ABI-uninhabited?" , layout . ty) ; } # [doc = " Yields non-ZST fields of the type"] fn non_zst_fields < 'tcx , 'a > (cx : & 'a LayoutCx < 'tcx > , layout : & 'a TyAndLayout < 'tcx > ,) -> impl Iterator < Item = (Size , TyAndLayout < 'tcx >) > { (0 .. layout . layout . fields () . count ()) . filter_map (| i | { let field = layout . field (cx , i) ; let zst = field . is_zst () ; (! zst) . then (| | (layout . fields . offset (i) , field)) }) } fn skip_newtypes < 'tcx > (cx : & LayoutCx < 'tcx > , layout : & TyAndLayout < 'tcx >) -> TyAndLayout < 'tcx > { if matches ! (layout . layout . variants () , Variants :: Multiple { .. }) { return * layout ; } let mut fields = non_zst_fields (cx , layout) ; let Some (first) = fields . next () else { return * layout ; } ; if fields . next () . is_none () { let (offset , first) = first ; if offset == Size :: ZERO && first . layout . size () == layout . size { return skip_newtypes (cx , & first) ; } } * layout } fn check_layout_abi < 'tcx > (cx : & LayoutCx < 'tcx > , layout : & TyAndLayout < 'tcx >) { let align = layout . backend_repr . scalar_align (cx) ; let size = layout . backend_repr . scalar_size (cx) ; if let Some (align) = align { assert_eq ! (layout . layout . align () . abi , align , "alignment mismatch between ABI and layout in {layout:#?}") ; } if let Some (size) = size { assert_eq ! (layout . layout . size () , size , "size mismatch between ABI and layout in {layout:#?}") ; } match layout . layout . backend_repr () { BackendRepr :: Scalar (_) => { let align = align . unwrap () ; let size = size . unwrap () ; let inner = skip_newtypes (cx , layout) ; assert ! (matches ! (inner . layout . backend_repr () , BackendRepr :: Scalar (_)) , "`Scalar` type {} is newtype around non-`Scalar` type {}" , layout . ty , inner . ty) ; match inner . layout . fields () { FieldsShape :: Primitive => { } FieldsShape :: Union (..) => { return ; } FieldsShape :: Arbitrary { .. } => { assert ! (inner . ty . is_enum () , "`Scalar` layout for non-primitive non-enum type {}" , inner . ty) ; assert_eq ! (inner . layout . fields () . count () , 1 , "`Scalar` layout for multiple-field type in {inner:#?}" ,) ; let offset = inner . layout . fields () . offset (0) ; let field = inner . field (cx , 0) ; assert_eq ! (offset , Size :: ZERO , "`Scalar` field at non-0 offset in {inner:#?}" ,) ; assert_eq ! (field . size , size , "`Scalar` field with bad size in {inner:#?}" ,) ; assert_eq ! (field . align . abi , align , "`Scalar` field with bad align in {inner:#?}" ,) ; assert ! (matches ! (field . backend_repr , BackendRepr :: Scalar (_)) , "`Scalar` field with bad ABI in {inner:#?}" ,) ; } _ => { panic ! ("`Scalar` layout for non-primitive non-enum type {}" , inner . ty) ; } } } BackendRepr :: ScalarPair (scalar1 , scalar2) => { let inner = skip_newtypes (cx , layout) ; assert ! (matches ! (inner . layout . backend_repr () , BackendRepr :: ScalarPair (..)) , "`ScalarPair` type {} is newtype around non-`ScalarPair` type {}" , layout . ty , inner . ty) ; if matches ! (inner . layout . variants () , Variants :: Multiple { .. }) { return ; } match inner . layout . fields () { FieldsShape :: Arbitrary { .. } => { } FieldsShape :: Union (..) => { return ; } _ => { panic ! ("`ScalarPair` layout with unexpected field shape in {inner:#?}") ; } } let mut fields = non_zst_fields (cx , & inner) ; let (offset1 , field1) = fields . next () . unwrap_or_else (| | { panic ! ("`ScalarPair` layout for type with not even one non-ZST field: {inner:#?}") }) ; let (offset2 , field2) = fields . next () . unwrap_or_else (| | { panic ! ("`ScalarPair` layout for type with less than two non-ZST fields: {inner:#?}") }) ; assert_matches ! (fields . next () , None , "`ScalarPair` layout for type with at least three non-ZST fields: {inner:#?}") ; let (offset1 , field1 , offset2 , field2) = if offset1 <= offset2 { (offset1 , field1 , offset2 , field2) } else { (offset2 , field2 , offset1 , field1) } ; let size1 = scalar1 . size (cx) ; let align1 = scalar1 . align (cx) . abi ; let size2 = scalar2 . size (cx) ; let align2 = scalar2 . align (cx) . abi ; assert_eq ! (offset1 , Size :: ZERO , "`ScalarPair` first field at non-0 offset in {inner:#?}" ,) ; assert_eq ! (field1 . size , size1 , "`ScalarPair` first field with bad size in {inner:#?}" ,) ; assert_eq ! (field1 . align . abi , align1 , "`ScalarPair` first field with bad align in {inner:#?}" ,) ; assert_matches ! (field1 . backend_repr , BackendRepr :: Scalar (_) , "`ScalarPair` first field with bad ABI in {inner:#?}" ,) ; let field2_offset = size1 . align_to (align2) ; assert_eq ! (offset2 , field2_offset , "`ScalarPair` second field at bad offset in {inner:#?}" ,) ; assert_eq ! (field2 . size , size2 , "`ScalarPair` second field with bad size in {inner:#?}" ,) ; assert_eq ! (field2 . align . abi , align2 , "`ScalarPair` second field with bad align in {inner:#?}" ,) ; assert_matches ! (field2 . backend_repr , BackendRepr :: Scalar (_) , "`ScalarPair` second field with bad ABI in {inner:#?}" ,) ; } BackendRepr :: SimdVector { element , count } => { let align = layout . align . abi ; let size = layout . size ; let element_align = element . align (cx) . abi ; let element_size = element . size (cx) ; assert ! (align >= element_align) ; assert ! (size == (element_size * count) . align_to (align)) ; } BackendRepr :: Memory { .. } => { } } } check_layout_abi (cx , layout) ; match & layout . variants { Variants :: Empty => { assert ! (layout . is_uninhabited ()) ; } Variants :: Single { index } => { if let Some (variants) = layout . ty . variant_range (tcx) { assert ! (variants . contains (index)) ; } else { assert ! (index . as_u32 () == 0) ; } } Variants :: Multiple { variants , tag , tag_encoding , .. } => { if let TagEncoding :: Niche { niche_start , untagged_variant , niche_variants } = tag_encoding { let niche_size = tag . size (cx) ; assert ! (* niche_start <= niche_size . unsigned_int_max ()) ; for (idx , variant) in variants . iter_enumerated () { if ! variant . is_uninhabited () { assert ! (idx == * untagged_variant || niche_variants . contains (& idx)) ; } assert_eq ! (layout . ty . discriminant_for_variant (tcx , idx) . unwrap () . val , u128 :: from (idx . as_u32 ()) ,) ; } } for variant in variants . iter () { assert_matches ! (variant . variants , Variants :: Single { .. }) ; if variant . size > layout . size { bug ! ("Type with size {} bytes has variant with size {} bytes: {layout:#?}" , layout . size . bytes () , variant . size . bytes () ,) } if variant . align . abi > layout . align . abi { bug ! ("Type with alignment {} bytes has variant with alignment {} bytes: {layout:#?}" , layout . align . abi . bytes () , variant . align . abi . bytes () ,) } if variant . size == Size :: ZERO || variant . fields . count () == 0 || variant . is_uninhabited () { continue ; } let scalar_coherent = | s1 : Scalar , s2 : Scalar | { s1 . size (cx) == s2 . size (cx) && s1 . align (cx) == s2 . align (cx) } ; let abi_coherent = match (layout . backend_repr , variant . backend_repr) { (BackendRepr :: Scalar (s1) , BackendRepr :: Scalar (s2)) => scalar_coherent (s1 , s2) , (BackendRepr :: ScalarPair (a1 , b1) , BackendRepr :: ScalarPair (a2 , b2)) => { scalar_coherent (a1 , a2) && scalar_coherent (b1 , b2) } (BackendRepr :: Memory { .. } , _) => true , _ => false , } ; if ! abi_coherent { bug ! ("Variant ABI is incompatible with top-level ABI:\nvariant={:#?}\nTop-level: {layout:#?}" , variant) ; } } } } }