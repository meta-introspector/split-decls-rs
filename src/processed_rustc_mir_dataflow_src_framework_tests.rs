/* FP:tests.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_USE_0001
/* FP:tests.rs-0002 */ use std :: marker :: PhantomData ;
/* FP:tests.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_USE_0002
/* FP:tests.rs-0004 */ use crate :: rustc_index :: IndexVec ;
/* FP:tests.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_USE_0003
/* FP:tests.rs-0006 */ use crate :: rustc_complete :: ty ;
/* FP:tests.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_USE_0004
/* FP:tests.rs-0008 */ use crate :: rustc_complete :: DUMMY_SP ;
/* FP:tests.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_USE_0005
/* FP:tests.rs-0010 */ use super :: * ;
/* FP:tests.rs-0011 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_FN_0006
/* FP:tests.rs-0012 */ # [doc = " Creates a `mir::Body` with a few disconnected basic blocks."] # [doc = ""] # [doc = " This is the `Body` that will be used by the `MockAnalysis` below. The shape of its CFG is not"] # [doc = " important."] fn mock_body < 'tcx > () -> mir :: Body < 'tcx > { let source_info = mir :: SourceInfo :: outermost (DUMMY_SP) ; let mut blocks = IndexVec :: new () ; let mut block = | n , kind | { let nop = mir :: Statement :: new (source_info , mir :: StatementKind :: Nop) ; blocks . push (mir :: BasicBlockData :: new_stmts (std :: iter :: repeat (& nop) . cloned () . take (n) . collect () , Some (mir :: Terminator { source_info , kind }) , false ,)) } ; let dummy_place = mir :: Place { local : mir :: RETURN_PLACE , projection : ty :: List :: empty () } ; block (4 , mir :: TerminatorKind :: Return) ; block (1 , mir :: TerminatorKind :: Return) ; block (2 , mir :: TerminatorKind :: Call { func : mir :: Operand :: Copy (dummy_place . clone ()) , args : [] . into () , destination : dummy_place . clone () , target : Some (mir :: START_BLOCK) , unwind : mir :: UnwindAction :: Continue , call_source : mir :: CallSource :: Misc , fn_span : DUMMY_SP , } ,) ; block (3 , mir :: TerminatorKind :: Return) ; block (0 , mir :: TerminatorKind :: Return) ; block (4 , mir :: TerminatorKind :: Call { func : mir :: Operand :: Copy (dummy_place . clone ()) , args : [] . into () , destination : dummy_place . clone () , target : Some (mir :: START_BLOCK) , unwind : mir :: UnwindAction :: Continue , call_source : mir :: CallSource :: Misc , fn_span : DUMMY_SP , } ,) ; mir :: Body :: new_cfg_only (blocks) }
/* FP:tests.rs-0013 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_STRUCT_0007
/* FP:tests.rs-0014 */ # [doc = " A dataflow analysis whose state is unique at every possible `SeekTarget`."] # [doc = ""] # [doc = " Uniqueness is achieved by having a *locally* unique effect before and after each statement and"] # [doc = " terminator (see `effect_at_target`) while ensuring that the entry set for each block is"] # [doc = " *globally* unique (see `mock_entry_set`)."] # [doc = ""] # [doc = " For example, a `BasicBlock` with ID `2` and a `Call` terminator has the following state at each"] # [doc = " location (\"+x\" indicates that \"x\" is added to the state)."] # [doc = ""] # [doc = " | Location               | Before            | After  |"] # [doc = " |------------------------|-------------------|--------|"] # [doc = " | (on_entry)             | {102}                     ||"] # [doc = " | statement 0            | +0                | +1     |"] # [doc = " | statement 1            | +2                | +3     |"] # [doc = " | `Call` terminator      | +4                | +5     |"] # [doc = " | (on unwind)            | {102,0,1,2,3,4,5}         ||"] # [doc = ""] # [doc = " The `102` in the block's entry set is derived from the basic block index and ensures that the"] # [doc = " expected state is unique across all basic blocks. Remember, it is generated by"] # [doc = " `mock_results`, not from actually running `MockAnalysis` to fixpoint."] struct MockAnalysis < 'tcx , D > { body : & 'tcx mir :: Body < 'tcx > , dir : PhantomData < D > , }
/* FP:tests.rs-0015 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_IMPL_0008
/* FP:tests.rs-0016 */ impl < D : Direction > MockAnalysis < '_ , D > { const BASIC_BLOCK_OFFSET : usize = 100 ; # [doc = " The entry set for each `BasicBlock` is the ID of that block offset by a fixed amount to"] # [doc = " avoid colliding with the statement/terminator effects."] fn mock_entry_set (& self , bb : BasicBlock) -> DenseBitSet < usize > { let mut ret = self . bottom_value (self . body) ; ret . insert (Self :: BASIC_BLOCK_OFFSET + bb . index ()) ; ret } fn mock_results (& self) -> IndexVec < BasicBlock , DenseBitSet < usize > > { let empty = self . bottom_value (self . body) ; let mut ret = IndexVec :: from_elem (empty , & self . body . basic_blocks) ; for (bb , _) in self . body . basic_blocks . iter_enumerated () { ret [bb] = self . mock_entry_set (bb) ; } ret } # [doc = " Returns the index that should be added to the dataflow state at the given target."] fn effect (& self , loc : EffectIndex) -> usize { let idx = match loc . effect { Effect :: Early => loc . statement_index * 2 , Effect :: Primary => loc . statement_index * 2 + 1 , } ; assert ! (idx < Self :: BASIC_BLOCK_OFFSET , "Too many statements in basic block") ; idx } # [doc = " Returns the expected state at the given `SeekTarget`."] # [doc = ""] # [doc = " This is the union of index of the target basic block, the index assigned to the"] # [doc = " target statement or terminator, and the indices of all preceding statements in the target"] # [doc = " basic block."] # [doc = ""] # [doc = " For example, the expected state when calling"] # [doc = " `seek_before_primary_effect(Location { block: 2, statement_index: 2 })`"] # [doc = " would be `[102, 0, 1, 2, 3, 4]`."] fn expected_state_at_target (& self , target : SeekTarget) -> DenseBitSet < usize > { let block = target . block () ; let mut ret = self . bottom_value (self . body) ; ret . insert (Self :: BASIC_BLOCK_OFFSET + block . index ()) ; let target = match target { SeekTarget :: BlockEntry { .. } => return ret , SeekTarget :: Early (loc) => Effect :: Early . at_index (loc . statement_index) , SeekTarget :: After (loc) => Effect :: Primary . at_index (loc . statement_index) , } ; let mut pos = if D :: IS_FORWARD { Effect :: Early . at_index (0) } else { Effect :: Early . at_index (self . body [block] . statements . len ()) } ; loop { ret . insert (self . effect (pos)) ; if pos == target { return ret ; } if D :: IS_FORWARD { pos = pos . next_in_forward_order () ; } else { pos = pos . next_in_backward_order () ; } } } }
/* FP:tests.rs-0017 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_IMPL_0009
/* FP:tests.rs-0018 */ impl < 'tcx , D : Direction > Analysis < 'tcx > for MockAnalysis < 'tcx , D > { type Domain = DenseBitSet < usize > ; type Direction = D ; const NAME : & 'static str = "mock" ; fn bottom_value (& self , body : & mir :: Body < 'tcx >) -> Self :: Domain { DenseBitSet :: new_empty (Self :: BASIC_BLOCK_OFFSET + body . basic_blocks . len ()) } fn initialize_start_block (& self , _ : & mir :: Body < 'tcx > , _ : & mut Self :: Domain) { unimplemented ! ("This is never called since `MockAnalysis` is never iterated to fixpoint") ; } fn apply_early_statement_effect (& mut self , state : & mut Self :: Domain , _statement : & mir :: Statement < 'tcx > , location : Location ,) { let idx = self . effect (Effect :: Early . at_index (location . statement_index)) ; assert ! (state . insert (idx)) ; } fn apply_primary_statement_effect (& mut self , state : & mut Self :: Domain , _statement : & mir :: Statement < 'tcx > , location : Location ,) { let idx = self . effect (Effect :: Primary . at_index (location . statement_index)) ; assert ! (state . insert (idx)) ; } fn apply_early_terminator_effect (& mut self , state : & mut Self :: Domain , _terminator : & mir :: Terminator < 'tcx > , location : Location ,) { let idx = self . effect (Effect :: Early . at_index (location . statement_index)) ; assert ! (state . insert (idx)) ; } fn apply_primary_terminator_effect < 'mir > (& mut self , state : & mut Self :: Domain , terminator : & 'mir mir :: Terminator < 'tcx > , location : Location ,) -> TerminatorEdges < 'mir , 'tcx > { let idx = self . effect (Effect :: Primary . at_index (location . statement_index)) ; assert ! (state . insert (idx)) ; terminator . edges () } }
/* FP:tests.rs-0019 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_ENUM_0010
/* FP:tests.rs-0020 */ # [derive (Clone , Copy , Debug , PartialEq , Eq)] enum SeekTarget { BlockEntry (BasicBlock) , Early (Location) , After (Location) , }
/* FP:tests.rs-0021 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_IMPL_0011
/* FP:tests.rs-0022 */ impl SeekTarget { fn block (& self) -> BasicBlock { use SeekTarget :: * ; match * self { BlockEntry (block) => block , Early (loc) | After (loc) => loc . block , } } # [doc = " An iterator over all possible `SeekTarget`s in a given block in order, starting with"] # [doc = " `BlockEntry`."] fn iter_in_block (body : & mir :: Body < '_ > , block : BasicBlock) -> impl Iterator < Item = Self > { let statements_and_terminator = (0 ..= body [block] . statements . len ()) . flat_map (| i | (0 .. 2) . map (move | j | (i , j))) . map (move | (i , kind) | { let loc = Location { block , statement_index : i } ; match kind { 0 => SeekTarget :: Early (loc) , 1 => SeekTarget :: After (loc) , _ => unreachable ! () , } }) ; std :: iter :: once (SeekTarget :: BlockEntry (block)) . chain (statements_and_terminator) } }
/* FP:tests.rs-0023 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_FN_0012
/* FP:tests.rs-0024 */ fn test_cursor < D : Direction > (analysis : MockAnalysis < '_ , D >) { let body = analysis . body ; let mut cursor = AnalysisAndResults { results : analysis . mock_results () , analysis } . into_results_cursor (body) ; cursor . allow_unreachable () ; let every_target = | | { body . basic_blocks . iter_enumerated () . flat_map (| (bb , _) | SeekTarget :: iter_in_block (body , bb)) } ; let mut seek_to_target = | targ | { use SeekTarget :: * ; match targ { BlockEntry (block) => cursor . seek_to_block_entry (block) , Early (loc) => cursor . seek_before_primary_effect (loc) , After (loc) => cursor . seek_after_primary_effect (loc) , } assert_eq ! (cursor . get () , & cursor . analysis () . expected_state_at_target (targ)) ; } ; for from in every_target () { seek_to_target (from) ; for to in every_target () { dbg ! (from) ; dbg ! (to) ; seek_to_target (to) ; seek_to_target (from) ; } } }
/* FP:tests.rs-0025 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_FN_0013
/* FP:tests.rs-0026 */ # [test] fn backward_cursor () { let body = mock_body () ; let body = & body ; let analysis = MockAnalysis { body , dir : PhantomData :: < Backward > } ; test_cursor (analysis) }
/* FP:tests.rs-0027 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_dataflow_src_framework_tests_FN_0014
/* FP:tests.rs-0028 */ # [test] fn forward_cursor () { let body = mock_body () ; let body = & body ; let analysis = MockAnalysis { body , dir : PhantomData :: < Forward > } ; test_cursor (analysis) }