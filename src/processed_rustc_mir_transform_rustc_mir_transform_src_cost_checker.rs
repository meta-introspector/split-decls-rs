/* FP:cost_checker.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_USE_0001
/* FP:cost_checker.rs-0002 */ use crate :: rustc_complete :: bug ;
/* FP:cost_checker.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_USE_0002
/* FP:cost_checker.rs-0004 */ use crate :: rustc_complete :: mir :: visit :: * ;
/* FP:cost_checker.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_USE_0003
/* FP:cost_checker.rs-0006 */ use crate :: rustc_complete :: mir :: * ;
/* FP:cost_checker.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_USE_0004
/* FP:cost_checker.rs-0008 */ use crate :: rustc_complete :: ty :: { self , Ty , TyCtxt } ;
/* FP:cost_checker.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_CONST_0005
/* FP:cost_checker.rs-0010 */ const INSTR_COST : usize = 5 ;
/* FP:cost_checker.rs-0011 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_CONST_0006
/* FP:cost_checker.rs-0012 */ const CALL_PENALTY : usize = 25 ;
/* FP:cost_checker.rs-0013 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_CONST_0007
/* FP:cost_checker.rs-0014 */ const LANDINGPAD_PENALTY : usize = 50 ;
/* FP:cost_checker.rs-0015 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_CONST_0008
/* FP:cost_checker.rs-0016 */ const RESUME_PENALTY : usize = 45 ;
/* FP:cost_checker.rs-0017 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_CONST_0009
/* FP:cost_checker.rs-0018 */ const LARGE_SWITCH_PENALTY : usize = 20 ;
/* FP:cost_checker.rs-0019 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_CONST_0010
/* FP:cost_checker.rs-0020 */ const CONST_SWITCH_BONUS : usize = 10 ;
/* FP:cost_checker.rs-0021 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_STRUCT_0011
/* FP:cost_checker.rs-0022 */ # [doc = " Verify that the callee body is compatible with the caller."] # [derive (Clone)] pub (super) struct CostChecker < 'b , 'tcx > { tcx : TyCtxt < 'tcx > , typing_env : ty :: TypingEnv < 'tcx > , penalty : usize , bonus : usize , callee_body : & 'b Body < 'tcx > , instance : Option < ty :: Instance < 'tcx > > , }
/* FP:cost_checker.rs-0023 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_IMPL_0012
/* FP:cost_checker.rs-0024 */ impl < 'b , 'tcx > CostChecker < 'b , 'tcx > { pub (super) fn new (tcx : TyCtxt < 'tcx > , typing_env : ty :: TypingEnv < 'tcx > , instance : Option < ty :: Instance < 'tcx > > , callee_body : & 'b Body < 'tcx > ,) -> CostChecker < 'b , 'tcx > { CostChecker { tcx , typing_env , callee_body , instance , penalty : 0 , bonus : 0 } } # [doc = " Add function-level costs not well-represented by the block-level costs."] # [doc = ""] # [doc = " Needed because the `CostChecker` is used sometimes for just blocks,"] # [doc = " and even the full `Inline` doesn't call `visit_body`, so there's nowhere"] # [doc = " to put this logic in the visitor."] pub (super) fn add_function_level_costs (& mut self) { if self . callee_body . basic_blocks . iter () . filter (| bbd | is_call_like (bbd . terminator ())) . count () == 1 { self . bonus += CALL_PENALTY ; } } pub (super) fn cost (& self) -> usize { usize :: saturating_sub (self . penalty , self . bonus) } fn instantiate_ty (& self , v : Ty < 'tcx >) -> Ty < 'tcx > { if let Some (instance) = self . instance { instance . instantiate_mir (self . tcx , ty :: EarlyBinder :: bind (& v)) } else { v } } }
/* FP:cost_checker.rs-0025 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_IMPL_0013
/* FP:cost_checker.rs-0026 */ impl < 'tcx > Visitor < 'tcx > for CostChecker < '_ , 'tcx > { fn visit_statement (& mut self , statement : & Statement < 'tcx > , location : Location) { match statement . kind { StatementKind :: Intrinsic (ref ndi) => { self . penalty += match * * ndi { NonDivergingIntrinsic :: Assume (..) => INSTR_COST , NonDivergingIntrinsic :: CopyNonOverlapping (..) => CALL_PENALTY , } ; } _ => self . super_statement (statement , location) , } } fn visit_rvalue (& mut self , rvalue : & Rvalue < 'tcx > , _location : Location) { match rvalue { Rvalue :: NullaryOp (NullOp :: UbChecks , ..) if ! self . tcx . sess . opts . unstable_opts . inline_mir_preserve_debug . unwrap_or (self . tcx . sess . ub_checks ()) => { self . bonus += CALL_PENALTY ; } Rvalue :: NullaryOp (..) => { } _ => self . penalty += INSTR_COST , } } fn visit_terminator (& mut self , terminator : & Terminator < 'tcx > , _ : Location) { match & terminator . kind { TerminatorKind :: Drop { place , unwind , .. } => { let ty = self . instantiate_ty (place . ty (self . callee_body , self . tcx) . ty) ; if ty . needs_drop (self . tcx , self . typing_env) { self . penalty += CALL_PENALTY ; if let UnwindAction :: Cleanup (_) = unwind { self . penalty += LANDINGPAD_PENALTY ; } } } TerminatorKind :: Call { func , unwind , .. } => { self . penalty += if let Some ((def_id , ..)) = func . const_fn_def () && self . tcx . intrinsic (def_id) . is_some () { INSTR_COST } else { CALL_PENALTY } ; if let UnwindAction :: Cleanup (_) = unwind { self . penalty += LANDINGPAD_PENALTY ; } } TerminatorKind :: TailCall { .. } => { self . penalty += CALL_PENALTY ; } TerminatorKind :: SwitchInt { discr , targets } => { if discr . constant () . is_some () { self . bonus += CONST_SWITCH_BONUS ; } else if targets . all_targets () . len () > 3 { self . penalty += LARGE_SWITCH_PENALTY ; } else { self . penalty += INSTR_COST ; } } TerminatorKind :: Assert { unwind , msg , .. } => { self . penalty += if msg . is_optional_overflow_check () && ! self . tcx . sess . opts . unstable_opts . inline_mir_preserve_debug . unwrap_or (self . tcx . sess . overflow_checks ()) { INSTR_COST } else { CALL_PENALTY } ; if let UnwindAction :: Cleanup (_) = unwind { self . penalty += LANDINGPAD_PENALTY ; } } TerminatorKind :: UnwindResume => self . penalty += RESUME_PENALTY , TerminatorKind :: InlineAsm { unwind , .. } => { self . penalty += INSTR_COST ; if let UnwindAction :: Cleanup (_) = unwind { self . penalty += LANDINGPAD_PENALTY ; } } TerminatorKind :: Unreachable => { self . bonus += INSTR_COST ; } TerminatorKind :: Goto { .. } | TerminatorKind :: Return => { } TerminatorKind :: UnwindTerminate (..) => { } kind @ (TerminatorKind :: FalseUnwind { .. } | TerminatorKind :: FalseEdge { .. } | TerminatorKind :: Yield { .. } | TerminatorKind :: CoroutineDrop) => { bug ! ("{kind:?} should not be in runtime MIR") ; } } } }
/* FP:cost_checker.rs-0027 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_mir_transform_src_cost_checker_FN_0014
/* FP:cost_checker.rs-0028 */ # [doc = " A terminator that's more call-like (might do a bunch of work, might panic, etc)"] # [doc = " than it is goto-/return-like (no side effects, etc)."] # [doc = ""] # [doc = " Used to treat multi-call functions (which could inline exponentially)"] # [doc = " different from those that only do one or none of these \"complex\" things."] pub (super) fn is_call_like (terminator : & Terminator < '_ >) -> bool { use TerminatorKind :: * ; match terminator . kind { Call { .. } | TailCall { .. } | Drop { .. } | Assert { .. } | InlineAsm { .. } => true , Goto { .. } | SwitchInt { .. } | UnwindResume | UnwindTerminate (_) | Return | Unreachable => false , Yield { .. } | CoroutineDrop | FalseEdge { .. } | FalseUnwind { .. } => { unreachable ! () } } }