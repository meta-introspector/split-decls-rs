/* FP:projection.rs-0001 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_USE_0001
/* FP:projection.rs-0002 */ use std :: marker :: PhantomData ;
/* FP:projection.rs-0003 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_USE_0002
/* FP:projection.rs-0004 */ use std :: ops :: Range ;
/* FP:projection.rs-0005 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_USE_0003
/* FP:projection.rs-0006 */ use crate :: rustc_abi :: { self as abi , FieldIdx , Size , VariantIdx } ;
/* FP:projection.rs-0007 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_USE_0004
/* FP:projection.rs-0008 */ use crate :: rustc_complete :: ty :: Ty ;
/* FP:projection.rs-0009 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_USE_0005
/* FP:projection.rs-0010 */ use crate :: rustc_complete :: ty :: layout :: TyAndLayout ;
/* FP:projection.rs-0011 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_USE_0006
/* FP:projection.rs-0012 */ use crate :: rustc_complete :: { bug , mir , span_bug , ty } ;
/* FP:projection.rs-0013 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_USE_0007
/* FP:projection.rs-0014 */ use tracing :: { debug , instrument } ;
/* FP:projection.rs-0015 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_USE_0008
/* FP:projection.rs-0016 */ use super :: { InterpCx , InterpResult , MPlaceTy , Machine , MemPlaceMeta , OpTy , Provenance , Scalar , err_ub , interp_ok , throw_ub , throw_unsup , } ;
/* FP:projection.rs-0017 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_ENUM_0009
/* FP:projection.rs-0018 */ # [doc = " Describes the constraints placed on offset-projections."] # [derive (Copy , Clone , Debug)] pub enum OffsetMode { # [doc = " The offset has to be inbounds, like `ptr::offset`."] Inbounds , # [doc = " No constraints, just wrap around the edge of the address space."] Wrapping , }
/* FP:projection.rs-0019 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_TRAIT_0010
/* FP:projection.rs-0020 */ # [doc = " A thing that we can project into, and that has a layout."] pub trait Projectable < 'tcx , Prov : Provenance > : Sized + std :: fmt :: Debug { # [doc = " Get the layout."] fn layout (& self) -> TyAndLayout < 'tcx > ; # [doc = " Get the metadata of a wide value."] fn meta (& self) -> MemPlaceMeta < Prov > ; # [doc = " Get the length of a slice/string/array stored here."] fn len < M : Machine < 'tcx , Provenance = Prov > > (& self , ecx : & InterpCx < 'tcx , M > ,) -> InterpResult < 'tcx , u64 > { let layout = self . layout () ; if layout . is_unsized () { match layout . ty . kind () { ty :: Slice (..) | ty :: Str => self . meta () . unwrap_meta () . to_target_usize (ecx) , _ => bug ! ("len not supported on unsized type {:?}" , layout . ty) , } } else { match layout . fields { abi :: FieldsShape :: Array { count , .. } => interp_ok (count) , _ => bug ! ("len not supported on sized type {:?}" , layout . ty) , } } } # [doc = " Offset the value by the given amount, replacing the layout and metadata."] fn offset_with_meta < M : Machine < 'tcx , Provenance = Prov > > (& self , offset : Size , mode : OffsetMode , meta : MemPlaceMeta < Prov > , layout : TyAndLayout < 'tcx > , ecx : & InterpCx < 'tcx , M > ,) -> InterpResult < 'tcx , Self > ; fn offset < M : Machine < 'tcx , Provenance = Prov > > (& self , offset : Size , layout : TyAndLayout < 'tcx > , ecx : & InterpCx < 'tcx , M > ,) -> InterpResult < 'tcx , Self > { assert ! (layout . is_sized ()) ; self . offset_with_meta (offset , OffsetMode :: Inbounds , MemPlaceMeta :: None , layout , ecx) } # [doc = " This does an offset-by-zero, which is effectively a transmute. Note however that"] # [doc = " not all transmutes are supported by all projectables -- specifically, if this is an"] # [doc = " `OpTy` or `ImmTy`, the new layout must have almost the same ABI as the old one"] # [doc = " (only changing the `valid_range` is allowed and turning integers into pointers)."] fn transmute < M : Machine < 'tcx , Provenance = Prov > > (& self , layout : TyAndLayout < 'tcx > , ecx : & InterpCx < 'tcx , M > ,) -> InterpResult < 'tcx , Self > { assert ! (self . layout () . is_sized () && layout . is_sized ()) ; assert_eq ! (self . layout () . size , layout . size) ; self . offset_with_meta (Size :: ZERO , OffsetMode :: Wrapping , MemPlaceMeta :: None , layout , ecx) } # [doc = " Convert this to an `OpTy`. This might be an irreversible transformation, but is useful for"] # [doc = " reading from this thing."] fn to_op < M : Machine < 'tcx , Provenance = Prov > > (& self , ecx : & InterpCx < 'tcx , M > ,) -> InterpResult < 'tcx , OpTy < 'tcx , M :: Provenance > > ; }
/* FP:projection.rs-0021 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_STRUCT_0011
/* FP:projection.rs-0022 */ # [doc = " A type representing iteration over the elements of an array."] pub struct ArrayIterator < 'a , 'tcx , Prov : Provenance , P : Projectable < 'tcx , Prov > > { base : & 'a P , range : Range < u64 > , stride : Size , field_layout : TyAndLayout < 'tcx > , _phantom : PhantomData < Prov > , }
/* FP:projection.rs-0023 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_IMPL_0012
/* FP:projection.rs-0024 */ impl < 'a , 'tcx , Prov : Provenance , P : Projectable < 'tcx , Prov > > ArrayIterator < 'a , 'tcx , Prov , P > { # [doc = " Should be the same `ecx` on each call, and match the one used to create the iterator."] pub fn next < M : Machine < 'tcx , Provenance = Prov > > (& mut self , ecx : & InterpCx < 'tcx , M > ,) -> InterpResult < 'tcx , Option < (u64 , P) > > { let Some (idx) = self . range . next () else { return interp_ok (None) } ; interp_ok (Some ((idx , self . base . offset_with_meta (self . stride * idx , OffsetMode :: Wrapping , MemPlaceMeta :: None , self . field_layout , ecx ,) ? ,))) } }
/* FP:projection.rs-0025 */ #[warn(unused_variables)] // AST_.._rust_compiler_rustc_const_eval_src_interpret_projection_IMPL_0013
/* FP:projection.rs-0026 */ impl < 'tcx , Prov , M > InterpCx < 'tcx , M > where Prov : Provenance , M : Machine < 'tcx , Provenance = Prov > , { # [doc = " Offset a pointer to project to a field of a struct/union. Unlike `place_field`, this is"] # [doc = " always possible without allocating, so it can take `&self`. Also return the field's layout."] # [doc = " This supports both struct and array fields, but not slices!"] # [doc = ""] # [doc = " This also works for arrays, but then the `FieldIdx` index type is restricting."] # [doc = " For indexing into arrays, use [`Self::project_index`]."] pub fn project_field < P : Projectable < 'tcx , M :: Provenance > > (& self , base : & P , field : FieldIdx ,) -> InterpResult < 'tcx , P > { debug_assert ! (! matches ! (base . layout () . ty . kind () , ty :: Slice (..)) , "`field` projection called on a slice -- call `index` projection instead") ; let offset = base . layout () . fields . offset (field . as_usize ()) ; let field_layout = base . layout () . field (self , field . as_usize ()) ; let (meta , offset) = if field_layout . is_unsized () { assert ! (! base . layout () . is_sized ()) ; let base_meta = base . meta () ; match self . size_and_align_from_meta (& base_meta , & field_layout) ? { Some ((_ , align)) => { let align = if let ty :: Adt (def , _) = base . layout () . ty . kind () && let Some (packed) = def . repr () . pack { align . min (packed) } else { align } ; (base_meta , offset . align_to (align)) } None if offset == Size :: ZERO => { (base_meta , offset) } None => { throw_unsup ! (ExternTypeField) } } } else { (MemPlaceMeta :: None , offset) } ; base . offset_with_meta (offset , OffsetMode :: Inbounds , meta , field_layout , self) } # [doc = " Projects multiple fields at once. See [`Self::project_field`] for details."] pub fn project_fields < P : Projectable < 'tcx , M :: Provenance > , const N : usize > (& self , base : & P , fields : [FieldIdx ; N] ,) -> InterpResult < 'tcx , [P ; N] > { fields . try_map (| field | self . project_field (base , field)) } # [doc = " Downcasting to an enum variant."] pub fn project_downcast < P : Projectable < 'tcx , M :: Provenance > > (& self , base : & P , variant : VariantIdx ,) -> InterpResult < 'tcx , P > { assert ! (! base . meta () . has_meta ()) ; let layout = base . layout () . for_variant (self , variant) ; base . offset (Size :: ZERO , layout , self) } # [doc = " Compute the offset and field layout for accessing the given index."] pub fn project_index < P : Projectable < 'tcx , M :: Provenance > > (& self , base : & P , index : u64 ,) -> InterpResult < 'tcx , P > { let (offset , field_layout) = match base . layout () . fields { abi :: FieldsShape :: Array { stride , count : _ } => { let len = base . len (self) ? ; if index >= len { throw_ub ! (BoundsCheckFailed { len , index }) ; } let offset = self . compute_size_in_bytes (stride , index) . ok_or_else (| | err_ub ! (PointerArithOverflow)) ? ; let field_layout = base . layout () . field (self , 0) ; (offset , field_layout) } _ => span_bug ! (self . cur_span () , "`project_index` called on non-array type {:?}" , base . layout () . ty) , } ; base . offset (offset , field_layout , self) } # [doc = " Converts a repr(simd) value into an array of the right size, such that `project_index`"] # [doc = " accesses the SIMD elements. Also returns the number of elements."] pub fn project_to_simd < P : Projectable < 'tcx , M :: Provenance > > (& self , base : & P ,) -> InterpResult < 'tcx , (P , u64) > { assert ! (base . layout () . ty . ty_adt_def () . unwrap () . repr () . simd ()) ; let array = self . project_field (base , FieldIdx :: ZERO) ? ; let len = array . len (self) ? ; interp_ok ((array , len)) } fn project_constant_index < P : Projectable < 'tcx , M :: Provenance > > (& self , base : & P , offset : u64 , min_length : u64 , from_end : bool ,) -> InterpResult < 'tcx , P > { let n = base . len (self) ? ; if n < min_length { throw_ub ! (BoundsCheckFailed { len : min_length , index : n }) ; } let index = if from_end { assert ! (0 < offset && offset <= min_length) ; n . checked_sub (offset) . unwrap () } else { assert ! (offset < min_length) ; offset } ; self . project_index (base , index) } # [doc = " Iterates over all fields of an array. Much more efficient than doing the"] # [doc = " same by repeatedly calling `project_index`."] pub fn project_array_fields < 'a , P : Projectable < 'tcx , M :: Provenance > > (& self , base : & 'a P ,) -> InterpResult < 'tcx , ArrayIterator < 'a , 'tcx , M :: Provenance , P > > { let abi :: FieldsShape :: Array { stride , .. } = base . layout () . fields else { span_bug ! (self . cur_span () , "project_array_fields: expected an array layout, got {:#?}" , base . layout ()) ; } ; let len = base . len (self) ? ; let field_layout = base . layout () . field (self , 0) ; debug ! ("project_array_fields: {base:?} {len}") ; base . offset (len * stride , self . layout_of (self . tcx . types . unit) . unwrap () , self) ? ; interp_ok (ArrayIterator { base , range : 0 .. len , stride , field_layout , _phantom : PhantomData , }) } # [doc = " Subslicing"] fn project_subslice < P : Projectable < 'tcx , M :: Provenance > > (& self , base : & P , from : u64 , to : u64 , from_end : bool ,) -> InterpResult < 'tcx , P > { let len = base . len (self) ? ; let actual_to = if from_end { if from . checked_add (to) . is_none_or (| to | to > len) { throw_ub ! (BoundsCheckFailed { len , index : from . saturating_add (to) }) ; } len . checked_sub (to) . unwrap () } else { to } ; let from_offset = match base . layout () . fields { abi :: FieldsShape :: Array { stride , .. } => stride * from , _ => { span_bug ! (self . cur_span () , "unexpected layout of index access: {:#?}" , base . layout ()) } } ; let inner_len = actual_to . checked_sub (from) . unwrap () ; let (meta , ty) = match base . layout () . ty . kind () { ty :: Array (inner , _) => { (MemPlaceMeta :: None , Ty :: new_array (self . tcx . tcx , * inner , inner_len)) } ty :: Slice (..) => { let len = Scalar :: from_target_usize (inner_len , self) ; (MemPlaceMeta :: Meta (len) , base . layout () . ty) } _ => { span_bug ! (self . cur_span () , "cannot subslice non-array type: `{:?}`" , base . layout () . ty) } } ; let layout = self . layout_of (ty) ? ; base . offset_with_meta (from_offset , OffsetMode :: Inbounds , meta , layout , self) } # [doc = " Applying a general projection"] # [instrument (skip (self) , level = "trace")] pub fn project < P > (& self , base : & P , proj_elem : mir :: PlaceElem < 'tcx >) -> InterpResult < 'tcx , P > where P : Projectable < 'tcx , M :: Provenance > + From < MPlaceTy < 'tcx , M :: Provenance > > + std :: fmt :: Debug , { use crate :: rustc_complete :: mir :: ProjectionElem :: * ; interp_ok (match proj_elem { OpaqueCast (ty) => { span_bug ! (self . cur_span () , "OpaqueCast({ty}) encountered after borrowck") } UnwrapUnsafeBinder (target) => base . transmute (self . layout_of (target) ? , self) ? , Subtype (_) => base . transmute (base . layout () , self) ? , Field (field , _) => self . project_field (base , field) ? , Downcast (_ , variant) => self . project_downcast (base , variant) ? , Deref => self . deref_pointer (& base . to_op (self) ?) ? . into () , Index (local) => { let layout = self . layout_of (self . tcx . types . usize) ? ; let n = self . local_to_op (local , Some (layout)) ? ; let n = self . read_target_usize (& n) ? ; self . project_index (base , n) ? } ConstantIndex { offset , min_length , from_end } => { self . project_constant_index (base , offset , min_length , from_end) ? } Subslice { from , to , from_end } => self . project_subslice (base , from , to , from_end) ? , }) } }