// Generated macro for ContextCompat (trait)
macro_rules! DepcrateContextCompat {
() => {
// Module: crate
// Provides: {"ContextCompat"}
// Dependencies: {}
# [doc = " Provides the `context` and `with_context` methods for `Result` and `Option` to enhance"] # [doc = " compatibility when porting from anyhow."] # [doc = ""] # [doc = " This trait is sealed and cannot be implemented for types outside of"] # [doc = " `eyre`."] # [doc = ""] # [doc = " ## Why Doesn't `Eyre` impl `WrapErr` for `Option`?"] # [doc = ""] # [doc = " `eyre` doesn't impl `WrapErr` for `Option` because `wrap_err` implies that you're creating a"] # [doc = " new error that saves the previous error as its `source`. Calling `wrap_err` on an `Option` is"] # [doc = " meaningless because there is no source error. `anyhow` avoids this issue by using a different"] # [doc = " mental model where you're adding \"context\" to an error, though this not a mental model for"] # [doc = " error handling that `eyre` agrees with."] # [doc = ""] # [doc = " Instead, `eyre` encourages users to think of each error as distinct, where the previous error"] # [doc = " is the context being saved by the new error, which is backwards compared to anyhow's model. In"] # [doc = " this model you're encouraged to use combinators provided by `std` for `Option` to convert an"] # [doc = " option to a `Result`"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Instead of:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use eyre::ContextCompat;"] # [doc = ""] # [doc = " fn get_thing(mut things: impl Iterator<Item = u32>) -> eyre::Result<u32> {"] # [doc = "     things"] # [doc = "         .find(|&thing| thing == 42)"] # [doc = "         .context(\"the thing wasnt in the list\")"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " We encourage you to use this:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use eyre::eyre;"] # [doc = ""] # [doc = " fn get_thing(mut things: impl Iterator<Item = u32>) -> eyre::Result<u32> {"] # [doc = "     things"] # [doc = "         .find(|&thing| thing == 42)"] # [doc = "         .ok_or_else(|| eyre!(\"the thing wasnt in the list\"))"] # [doc = " }"] # [doc = " ```"] # [cfg (feature = "anyhow")] pub trait ContextCompat < T > : context :: private :: Sealed { # [doc = " Compatibility version of `wrap_err` for creating new errors with new source on `Option`"] # [doc = " when porting from `anyhow`"] # [cfg_attr (track_caller , track_caller)] fn context < D > (self , msg : D) -> Result < T , Report > where D : Display + Send + Sync + 'static ; # [doc = " Compatibility version of `wrap_err_with` for creating new errors with new source on `Option`"] # [doc = " when porting from `anyhow`"] # [cfg_attr (track_caller , track_caller)] fn with_context < D , F > (self , f : F) -> Result < T , Report > where D : Display + Send + Sync + 'static , F : FnOnce () -> D ; }
};
}
