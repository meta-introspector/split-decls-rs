// Generated macro for convert (module)
macro_rules! Depcrate_write_dwarfconvert {
() => {
// Module: crate::write::dwarf
// Provides: {"convert"}
// Dependencies: {}
# [cfg (feature = "read")] pub (crate) mod convert { use super :: * ; use crate :: common :: LineEncoding ; use crate :: read :: { self , Reader } ; use crate :: write :: { Address , ConvertLineProgram , ConvertResult , ConvertUnitSection , FilterUnitSection , } ; impl Dwarf { # [doc = " Create a `write::Dwarf` by converting a `read::Dwarf`."] # [doc = ""] # [doc = " `convert_address` is a function to convert read addresses into the `Address`"] # [doc = " type. For non-relocatable addresses, this function may simply return"] # [doc = " `Address::Constant(address)`. For relocatable addresses, it is the caller's"] # [doc = " responsibility to determine the symbol and addend corresponding to the address"] # [doc = " and return `Address::Symbol { symbol, addend }`."] # [doc = ""] # [doc = " `convert_address` should not be used for complex address transformations, as it"] # [doc = " will not be called for address offsets (such as in `DW_AT_high_pc`, line programs,"] # [doc = " location lists, or range lists). If you need complex transformations, then you"] # [doc = " need to use [`Dwarf::convert`] to enable you to transform the address offsets too."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " Convert DWARF sections using `Dwarf::from`."] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # fn example() -> Result<(), gimli::write::ConvertError> {"] # [doc = " # let loader = |name| -> Result<gimli::EndianSlice<gimli::RunTimeEndian>, gimli::Error> { unimplemented!() };"] # [doc = " let read_dwarf = gimli::read::Dwarf::load(loader)?;"] # [doc = " let write_dwarf = gimli::write::Dwarf::from("] # [doc = "     &read_dwarf,"] # [doc = "     &|address| Some(gimli::write::Address::Constant(address)),"] # [doc = " )?;"] # [doc = " # unreachable!()"] # [doc = " # }"] # [doc = " ```"] pub fn from < R : Reader < Offset = usize > > (from_dwarf : & read :: Dwarf < R > , convert_address : & dyn Fn (u64) -> Option < Address > ,) -> ConvertResult < Dwarf > { let mut dwarf = Dwarf :: default () ; let mut convert = dwarf . convert (from_dwarf) ? ; while let Some ((mut unit , root_entry)) = convert . read_unit () ? { if let Some (convert_program) = unit . read_line_program (None , None) ? { let (program , files) = convert_program . convert_all (convert_address) ? ; unit . set_line_program (program , files) ; } unit . convert_attributes (unit . unit . root () , & root_entry , convert_address) ? ; while let Some ((id , entry)) = unit . read_entry () ? { if id . is_none () { continue ; } let id = unit . add_entry (id , & entry) ; unit . convert_attributes (id , & entry , convert_address) ? ; } } Ok (dwarf) } # [doc = " Create a converter for all units in the `.debug_info` section of the given"] # [doc = " DWARF object."] # [doc = ""] # [doc = " `encoding` applies to the converted units, and may be different from the"] # [doc = " source unit. If `None`, the encoding from the source unit is used."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " Convert DWARF sections using `convert`."] # [doc = " See [`ConvertUnit`](crate::write::ConvertUnit) for an example of the unit"] # [doc = " conversion."] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # fn example() -> Result<(), gimli::write::ConvertError> {"] # [doc = " # let loader = |name| -> Result<gimli::EndianSlice<gimli::RunTimeEndian>, gimli::Error> { unimplemented!() };"] # [doc = " let read_dwarf = gimli::read::Dwarf::load(loader)?;"] # [doc = " let mut write_dwarf = gimli::write::Dwarf::new();"] # [doc = " let mut convert = write_dwarf.convert(&read_dwarf)?;"] # [doc = " while let Some((mut unit, root_entry)) = convert.read_unit()? {"] # [doc = "     // Now you can convert the root DIE attributes, and other DIEs."] # [doc = " }"] # [doc = " # unreachable!()"] # [doc = " # }"] # [doc = " ```"] pub fn convert < 'a , R : Reader < Offset = usize > > (& 'a mut self , dwarf : & 'a read :: Dwarf < R > ,) -> ConvertResult < ConvertUnitSection < 'a , R > > { ConvertUnitSection :: new (dwarf , self) } # [doc = " Create a converter for some of the DIEs in the `.debug_info` section of the"] # [doc = " given DWARF object."] # [doc = ""] # [doc = " `filter` determines which DIEs are converted. This can be created using"] # [doc = " [`FilterUnitSection::new`]."] # [doc = ""] # [doc = " `encoding` applies to the converted units, and may be different from the"] # [doc = " source unit. If `None`, the encoding from the source unit is used."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " Convert a DWARF section using `convert_with_filter`."] # [doc = " See [`ConvertUnit`](crate::write::ConvertUnit) for an example of the unit"] # [doc = " conversion."] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # fn example() -> Result<(), gimli::write::ConvertError> {"] # [doc = " # let loader = |name| -> Result<gimli::EndianSlice<gimli::RunTimeEndian>, gimli::Error> { unimplemented!() };"] # [doc = " # let need_entry = &|entry: &gimli::write::FilterUnitEntry<_>| -> Result<bool, gimli::write::ConvertError> { Ok(false) };"] # [doc = " let read_dwarf = gimli::read::Dwarf::load(loader)?;"] # [doc = " let mut filter = gimli::write::FilterUnitSection::new(&read_dwarf)?;"] # [doc = " while let Some(mut unit) = filter.read_unit()? {"] # [doc = "     while let Some(entry) = unit.read_entry()? {"] # [doc = "         if need_entry(&entry)? {"] # [doc = "             unit.require_entry(entry.offset);"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " let mut write_dwarf = gimli::write::Dwarf::new();"] # [doc = " let mut convert = write_dwarf.convert_with_filter(filter)?;"] # [doc = " while let Some((mut unit, root_entry)) = convert.read_unit()? {"] # [doc = "     // Now you can convert the root DIE attributes, and other DIEs."] # [doc = " }"] # [doc = " # unreachable!()"] # [doc = " # }"] # [doc = " ```"] pub fn convert_with_filter < 'a , R : Reader < Offset = usize > > (& 'a mut self , filter : FilterUnitSection < 'a , R > ,) -> ConvertResult < ConvertUnitSection < 'a , R > > { ConvertUnitSection :: new_with_filter (self , filter) } # [doc = " Start a new conversion of a line number program."] # [doc = ""] # [doc = " This is intended for line number programs that do not have an associated"] # [doc = " [`read::Unit`]. If the line number program has an associated [`read::Unit`]"] # [doc = " that you are converting, then you should use"] # [doc = " [`ConvertUnit::read_line_program`](crate::write::ConvertUnit::read_line_program)"] # [doc = " instead."] # [doc = ""] # [doc = " `encoding` and `line_encoding` apply to the converted program, and"] # [doc = " may be different from the source program. If `None`, the encoding from"] # [doc = " the source program is used."] # [doc = ""] # [doc = " See [`ConvertLineProgram`] for an example."] pub fn read_line_program < 'a , R : Reader < Offset = usize > > (& 'a mut self , dwarf : & 'a read :: Dwarf < R > , program : read :: IncompleteLineProgram < R > , encoding : Option < Encoding > , line_encoding : Option < LineEncoding > ,) -> ConvertResult < ConvertLineProgram < 'a , R > > { ConvertLineProgram :: new (dwarf , program , None , encoding , line_encoding , & mut self . line_strings , & mut self . strings ,) } } }
};
}
