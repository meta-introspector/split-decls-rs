// Generated macro for impl_583 (impl)
macro_rules! Depcrate_read_loclistsimpl_583 {
() => {
// Module: crate::read::loclists
// Provides: {"impl_583"}
// Dependencies: {}
impl < R : Reader > LocListIter < R > { # [doc = " Construct a `LocListIter`."] fn new (raw : RawLocListIter < R > , base_address : u64 , debug_addr : DebugAddr < R > , debug_addr_base : DebugAddrBase < R :: Offset > ,) -> LocListIter < R > { LocListIter { raw , base_address , debug_addr , debug_addr_base , } } # [inline] fn get_address (& self , index : DebugAddrIndex < R :: Offset >) -> Result < u64 > { self . debug_addr . get_address (self . raw . encoding . address_size , self . debug_addr_base , index) } # [doc = " Advance the iterator to the next location."] pub fn next (& mut self) -> Result < Option < LocationListEntry < R > > > { loop { let raw_loc = match self . raw . next () ? { Some (loc) => loc , None => return Ok (None) , } ; let loc = self . convert_raw (raw_loc) ? ; if loc . is_some () { return Ok (loc) ; } } } # [doc = " Return the next raw location."] # [doc = ""] # [doc = " The raw location should be passed to `convert_raw`."] # [doc (hidden)] pub fn next_raw (& mut self) -> Result < Option < RawLocListEntry < R > > > { self . raw . next () } # [doc = " Convert a raw location into a location, and update the state of the iterator."] # [doc = ""] # [doc = " The raw location should have been obtained from `next_raw`."] # [doc (hidden)] pub fn convert_raw (& mut self , raw_loc : RawLocListEntry < R > ,) -> Result < Option < LocationListEntry < R > > > { let address_size = self . raw . encoding . address_size ; let (range , data) = match raw_loc { RawLocListEntry :: BaseAddress { addr } => { self . base_address = addr ; return Ok (None) ; } RawLocListEntry :: BaseAddressx { addr } => { self . base_address = self . get_address (addr) ? ; return Ok (None) ; } RawLocListEntry :: StartxEndx { begin , end , data } => { let begin = self . get_address (begin) ? ; let end = self . get_address (end) ? ; (Range { begin , end } , data) } RawLocListEntry :: StartxLength { begin , length , data , } => { let begin = self . get_address (begin) ? ; let end = begin . wrapping_add_sized (length , address_size) ; (Range { begin , end } , data) } RawLocListEntry :: DefaultLocation { data } => (Range { begin : 0 , end : u64 :: MAX , } , data ,) , RawLocListEntry :: AddressOrOffsetPair { begin , end , data } | RawLocListEntry :: OffsetPair { begin , end , data } => { if self . base_address >= u64 :: min_tombstone (address_size) { return Ok (None) ; } let mut range = Range { begin , end } ; range . add_base_address (self . base_address , address_size) ; (range , data) } RawLocListEntry :: StartEnd { begin , end , data } => (Range { begin , end } , data) , RawLocListEntry :: StartLength { begin , length , data , } => { let end = begin . wrapping_add_sized (length , address_size) ; (Range { begin , end } , data) } } ; if range . begin >= u64 :: min_tombstone (address_size) || range . begin >= range . end { return Ok (None) ; } Ok (Some (LocationListEntry { range , data })) } }
};
}
