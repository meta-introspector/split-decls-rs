// Generated macro for impl_767 (impl)
macro_rules! Depcrate_read_unitimpl_767 {
() => {
// Module: crate::read::unit
// Provides: {"impl_767"}
// Dependencies: {}
impl < T : ReaderOffset > UnitOffset < T > { # [doc = " Return true if this offset is within the entries of the given unit."] # [doc = ""] # [doc = " This only checks that the offset is within the range of the data for unit entries,"] # [doc = " not that there is a valid DIE at this offset."] pub fn is_in_bounds < R > (& self , unit : & UnitHeader < R >) -> bool where R : Reader < Offset = T > , { unit . is_in_bounds (* self) } # [doc = " Convert an offset to be relative to the start of the section containing the unit,"] # [doc = " instead of relative to the start of the given unit."] # [doc = ""] # [doc = " Does not check that the offset is in bounds or valid. This is because often you"] # [doc = " will know that the offset came from this unit so it must be valid."] # [doc = " For offsets of uncertain origin, use [`UnitOffset::is_in_bounds`] first."] pub fn to_unit_section_offset < R > (& self , unit : & UnitHeader < R >) -> UnitSectionOffset < T > where R : Reader < Offset = T > , { UnitSectionOffset (unit . offset () . 0 + self . 0) } # [doc = " Convert an offset to be relative to the start of the `.debug_info` section,"] # [doc = " instead of relative to the start of the given unit."] # [doc = ""] # [doc = " Returns `None` if the unit is not within the `.debug_info` section."] pub fn to_debug_info_offset < R > (& self , unit : & UnitHeader < R >) -> Option < DebugInfoOffset < T > > where R : Reader < Offset = T > , { self . to_unit_section_offset (unit) . to_debug_info_offset (unit) } # [doc = " Convert an offset to be relative to the start of the `.debug_types` section,"] # [doc = " instead of relative to the start of the given unit."] # [doc = ""] # [doc = " Returns `None` if the unit is not within the `.debug_types` section."] pub fn to_debug_types_offset < R > (& self , unit : & UnitHeader < R >) -> Option < DebugTypesOffset < T > > where R : Reader < Offset = T > , { self . to_unit_section_offset (unit) . to_debug_types_offset (unit) } }
};
}
