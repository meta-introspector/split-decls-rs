// Generated macro for CallFrameInstruction (enum)
macro_rules! Depcrate_read_cfiCallFrameInstruction {
() => {
// Module: crate::read::cfi
// Provides: {"CallFrameInstruction"}
// Dependencies: {}
# [doc = " A parsed call frame instruction."] # [derive (Clone , Debug , PartialEq , Eq)] pub enum CallFrameInstruction < T : ReaderOffset > { # [doc = " > 1. DW_CFA_set_loc"] # [doc = " >"] # [doc = " > The DW_CFA_set_loc instruction takes a single operand that represents"] # [doc = " > a target address. The required action is to create a new table row"] # [doc = " > using the specified address as the location. All other values in the"] # [doc = " > new row are initially identical to the current row. The new location"] # [doc = " > value is always greater than the current one. If the segment_size"] # [doc = " > field of this FDE's CIE is non- zero, the initial location is preceded"] # [doc = " > by a segment selector of the given length."] SetLoc { # [doc = " The target address."] address : u64 , } , # [doc = " The `AdvanceLoc` instruction is used for all of `DW_CFA_advance_loc` and"] # [doc = " `DW_CFA_advance_loc{1,2,4}`."] # [doc = ""] # [doc = " > 2. DW_CFA_advance_loc"] # [doc = " >"] # [doc = " > The DW_CFA_advance instruction takes a single operand (encoded with"] # [doc = " > the opcode) that represents a constant delta. The required action is"] # [doc = " > to create a new table row with a location value that is computed by"] # [doc = " > taking the current entry’s location value and adding the value of"] # [doc = " > delta * code_alignment_factor. All other values in the new row are"] # [doc = " > initially identical to the current row."] AdvanceLoc { # [doc = " The delta to be added to the current address."] delta : u32 , } , # [doc = " > 1. DW_CFA_def_cfa"] # [doc = " >"] # [doc = " > The DW_CFA_def_cfa instruction takes two unsigned LEB128 operands"] # [doc = " > representing a register number and a (non-factored) offset. The"] # [doc = " > required action is to define the current CFA rule to use the provided"] # [doc = " > register and offset."] DefCfa { # [doc = " The target register's number."] register : Register , # [doc = " The non-factored offset."] offset : u64 , } , # [doc = " > 2. DW_CFA_def_cfa_sf"] # [doc = " >"] # [doc = " > The DW_CFA_def_cfa_sf instruction takes two operands: an unsigned"] # [doc = " > LEB128 value representing a register number and a signed LEB128"] # [doc = " > factored offset. This instruction is identical to DW_CFA_def_cfa"] # [doc = " > except that the second operand is signed and factored. The resulting"] # [doc = " > offset is factored_offset * data_alignment_factor."] DefCfaSf { # [doc = " The target register's number."] register : Register , # [doc = " The factored offset."] factored_offset : i64 , } , # [doc = " > 3. DW_CFA_def_cfa_register"] # [doc = " >"] # [doc = " > The DW_CFA_def_cfa_register instruction takes a single unsigned LEB128"] # [doc = " > operand representing a register number. The required action is to"] # [doc = " > define the current CFA rule to use the provided register (but to keep"] # [doc = " > the old offset). This operation is valid only if the current CFA rule"] # [doc = " > is defined to use a register and offset."] DefCfaRegister { # [doc = " The target register's number."] register : Register , } , # [doc = " > 4. DW_CFA_def_cfa_offset"] # [doc = " >"] # [doc = " > The DW_CFA_def_cfa_offset instruction takes a single unsigned LEB128"] # [doc = " > operand representing a (non-factored) offset. The required action is"] # [doc = " > to define the current CFA rule to use the provided offset (but to keep"] # [doc = " > the old register). This operation is valid only if the current CFA"] # [doc = " > rule is defined to use a register and offset."] DefCfaOffset { # [doc = " The non-factored offset."] offset : u64 , } , # [doc = " > 5. DW_CFA_def_cfa_offset_sf"] # [doc = " >"] # [doc = " > The DW_CFA_def_cfa_offset_sf instruction takes a signed LEB128 operand"] # [doc = " > representing a factored offset. This instruction is identical to"] # [doc = " > DW_CFA_def_cfa_offset except that the operand is signed and"] # [doc = " > factored. The resulting offset is factored_offset *"] # [doc = " > data_alignment_factor. This operation is valid only if the current CFA"] # [doc = " > rule is defined to use a register and offset."] DefCfaOffsetSf { # [doc = " The factored offset."] factored_offset : i64 , } , # [doc = " > 6. DW_CFA_def_cfa_expression"] # [doc = " >"] # [doc = " > The DW_CFA_def_cfa_expression instruction takes a single operand"] # [doc = " > encoded as a DW_FORM_exprloc value representing a DWARF"] # [doc = " > expression. The required action is to establish that expression as the"] # [doc = " > means by which the current CFA is computed."] DefCfaExpression { # [doc = " The location of the DWARF expression."] expression : UnwindExpression < T > , } , # [doc = " > 1. DW_CFA_undefined"] # [doc = " >"] # [doc = " > The DW_CFA_undefined instruction takes a single unsigned LEB128"] # [doc = " > operand that represents a register number. The required action is to"] # [doc = " > set the rule for the specified register to “undefined.”"] Undefined { # [doc = " The target register's number."] register : Register , } , # [doc = " > 2. DW_CFA_same_value"] # [doc = " >"] # [doc = " > The DW_CFA_same_value instruction takes a single unsigned LEB128"] # [doc = " > operand that represents a register number. The required action is to"] # [doc = " > set the rule for the specified register to “same value.”"] SameValue { # [doc = " The target register's number."] register : Register , } , # [doc = " The `Offset` instruction represents both `DW_CFA_offset` and"] # [doc = " `DW_CFA_offset_extended`."] # [doc = ""] # [doc = " > 3. DW_CFA_offset"] # [doc = " >"] # [doc = " > The DW_CFA_offset instruction takes two operands: a register number"] # [doc = " > (encoded with the opcode) and an unsigned LEB128 constant representing"] # [doc = " > a factored offset. The required action is to change the rule for the"] # [doc = " > register indicated by the register number to be an offset(N) rule"] # [doc = " > where the value of N is factored offset * data_alignment_factor."] Offset { # [doc = " The target register's number."] register : Register , # [doc = " The factored offset."] factored_offset : u64 , } , # [doc = " > 5. DW_CFA_offset_extended_sf"] # [doc = " >"] # [doc = " > The DW_CFA_offset_extended_sf instruction takes two operands: an"] # [doc = " > unsigned LEB128 value representing a register number and a signed"] # [doc = " > LEB128 factored offset. This instruction is identical to"] # [doc = " > DW_CFA_offset_extended except that the second operand is signed and"] # [doc = " > factored. The resulting offset is factored_offset *"] # [doc = " > data_alignment_factor."] OffsetExtendedSf { # [doc = " The target register's number."] register : Register , # [doc = " The factored offset."] factored_offset : i64 , } , # [doc = " > 6. DW_CFA_val_offset"] # [doc = " >"] # [doc = " > The DW_CFA_val_offset instruction takes two unsigned LEB128 operands"] # [doc = " > representing a register number and a factored offset. The required"] # [doc = " > action is to change the rule for the register indicated by the"] # [doc = " > register number to be a val_offset(N) rule where the value of N is"] # [doc = " > factored_offset * data_alignment_factor."] ValOffset { # [doc = " The target register's number."] register : Register , # [doc = " The factored offset."] factored_offset : u64 , } , # [doc = " > 7. DW_CFA_val_offset_sf"] # [doc = " >"] # [doc = " > The DW_CFA_val_offset_sf instruction takes two operands: an unsigned"] # [doc = " > LEB128 value representing a register number and a signed LEB128"] # [doc = " > factored offset. This instruction is identical to DW_CFA_val_offset"] # [doc = " > except that the second operand is signed and factored. The resulting"] # [doc = " > offset is factored_offset * data_alignment_factor."] ValOffsetSf { # [doc = " The target register's number."] register : Register , # [doc = " The factored offset."] factored_offset : i64 , } , # [doc = " > 8. DW_CFA_register"] # [doc = " >"] # [doc = " > The DW_CFA_register instruction takes two unsigned LEB128 operands"] # [doc = " > representing register numbers. The required action is to set the rule"] # [doc = " > for the first register to be register(R) where R is the second"] # [doc = " > register."] Register { # [doc = " The number of the register whose rule is being changed."] dest_register : Register , # [doc = " The number of the register where the other register's value can be"] # [doc = " found."] src_register : Register , } , # [doc = " > 9. DW_CFA_expression"] # [doc = " >"] # [doc = " > The DW_CFA_expression instruction takes two operands: an unsigned"] # [doc = " > LEB128 value representing a register number, and a DW_FORM_block value"] # [doc = " > representing a DWARF expression. The required action is to change the"] # [doc = " > rule for the register indicated by the register number to be an"] # [doc = " > expression(E) rule where E is the DWARF expression. That is, the DWARF"] # [doc = " > expression computes the address. The value of the CFA is pushed on the"] # [doc = " > DWARF evaluation stack prior to execution of the DWARF expression."] Expression { # [doc = " The target register's number."] register : Register , # [doc = " The location of the DWARF expression."] expression : UnwindExpression < T > , } , # [doc = " > 10. DW_CFA_val_expression"] # [doc = " >"] # [doc = " > The DW_CFA_val_expression instruction takes two operands: an unsigned"] # [doc = " > LEB128 value representing a register number, and a DW_FORM_block value"] # [doc = " > representing a DWARF expression. The required action is to change the"] # [doc = " > rule for the register indicated by the register number to be a"] # [doc = " > val_expression(E) rule where E is the DWARF expression. That is, the"] # [doc = " > DWARF expression computes the value of the given register. The value"] # [doc = " > of the CFA is pushed on the DWARF evaluation stack prior to execution"] # [doc = " > of the DWARF expression."] ValExpression { # [doc = " The target register's number."] register : Register , # [doc = " The location of the DWARF expression."] expression : UnwindExpression < T > , } , # [doc = " The `Restore` instruction represents both `DW_CFA_restore` and"] # [doc = " `DW_CFA_restore_extended`."] # [doc = ""] # [doc = " > 11. DW_CFA_restore"] # [doc = " >"] # [doc = " > The DW_CFA_restore instruction takes a single operand (encoded with"] # [doc = " > the opcode) that represents a register number. The required action is"] # [doc = " > to change the rule for the indicated register to the rule assigned it"] # [doc = " > by the initial_instructions in the CIE."] Restore { # [doc = " The register to be reset."] register : Register , } , # [doc = " > 1. DW_CFA_remember_state"] # [doc = " >"] # [doc = " > The DW_CFA_remember_state instruction takes no operands. The required"] # [doc = " > action is to push the set of rules for every register onto an implicit"] # [doc = " > stack."] RememberState , # [doc = " > 2. DW_CFA_restore_state"] # [doc = " >"] # [doc = " > The DW_CFA_restore_state instruction takes no operands. The required"] # [doc = " > action is to pop the set of rules off the implicit stack and place"] # [doc = " > them in the current row."] RestoreState , # [doc = " > DW_CFA_GNU_args_size"] # [doc = " >"] # [doc = " > GNU Extension"] # [doc = " >"] # [doc = " > The DW_CFA_GNU_args_size instruction takes an unsigned LEB128 operand"] # [doc = " > representing an argument size. This instruction specifies the total of"] # [doc = " > the size of the arguments which have been pushed onto the stack."] ArgsSize { # [doc = " The size of the arguments which have been pushed onto the stack"] size : u64 , } , # [doc = " > DW_CFA_AARCH64_negate_ra_state"] # [doc = " >"] # [doc = " > AArch64 Extension"] # [doc = " >"] # [doc = " > The DW_CFA_AARCH64_negate_ra_state operation negates bit 0 of the"] # [doc = " > RA_SIGN_STATE pseudo-register. It does not take any operands. The"] # [doc = " > DW_CFA_AARCH64_negate_ra_state must not be mixed with other DWARF Register"] # [doc = " > Rule Instructions on the RA_SIGN_STATE pseudo-register in one Common"] # [doc = " > Information Entry (CIE) and Frame Descriptor Entry (FDE) program sequence."] NegateRaState , # [doc = " > 1. DW_CFA_nop"] # [doc = " >"] # [doc = " > The DW_CFA_nop instruction has no operands and no required actions. It"] # [doc = " > is used as padding to make a CIE or FDE an appropriate size."] Nop , }
};
}
