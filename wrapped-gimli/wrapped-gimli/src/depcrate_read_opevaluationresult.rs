// Generated macro for EvaluationResult (enum)
macro_rules! Depcrate_read_opEvaluationResult {
() => {
// Module: crate::read::op
// Provides: {"EvaluationResult"}
// Dependencies: {}
# [doc = " The state of an `Evaluation` after evaluating a DWARF expression."] # [doc = " The evaluation is either `Complete`, or it requires more data"] # [doc = " to continue, as described by the variant."] # [derive (Debug , PartialEq)] pub enum EvaluationResult < R : Reader > { # [doc = " The `Evaluation` is complete, and `Evaluation::result()` can be called."] Complete , # [doc = " The `Evaluation` needs a value from memory to proceed further.  Once the"] # [doc = " caller determines what value to provide it should resume the `Evaluation`"] # [doc = " by calling `Evaluation::resume_with_memory`."] RequiresMemory { # [doc = " The address of the value required."] address : u64 , # [doc = " The size of the value required. This is guaranteed to be at most the"] # [doc = " word size of the target architecture."] size : u8 , # [doc = " If not `None`, a target-specific address space value."] space : Option < u64 > , # [doc = " The DIE of the base type or 0 to indicate the generic type"] base_type : UnitOffset < R :: Offset > , } , # [doc = " The `Evaluation` needs a value from a register to proceed further.  Once"] # [doc = " the caller determines what value to provide it should resume the"] # [doc = " `Evaluation` by calling `Evaluation::resume_with_register`."] RequiresRegister { # [doc = " The register number."] register : Register , # [doc = " The DIE of the base type or 0 to indicate the generic type"] base_type : UnitOffset < R :: Offset > , } , # [doc = " The `Evaluation` needs the frame base address to proceed further.  Once"] # [doc = " the caller determines what value to provide it should resume the"] # [doc = " `Evaluation` by calling `Evaluation::resume_with_frame_base`.  The frame"] # [doc = " base address is the address produced by the location description in the"] # [doc = " `DW_AT_frame_base` attribute of the current function."] RequiresFrameBase , # [doc = " The `Evaluation` needs a value from TLS to proceed further.  Once the"] # [doc = " caller determines what value to provide it should resume the"] # [doc = " `Evaluation` by calling `Evaluation::resume_with_tls`."] RequiresTls (u64) , # [doc = " The `Evaluation` needs the CFA to proceed further.  Once the caller"] # [doc = " determines what value to provide it should resume the `Evaluation` by"] # [doc = " calling `Evaluation::resume_with_call_frame_cfa`."] RequiresCallFrameCfa , # [doc = " The `Evaluation` needs the DWARF expression at the given location to"] # [doc = " proceed further.  Once the caller determines what value to provide it"] # [doc = " should resume the `Evaluation` by calling"] # [doc = " `Evaluation::resume_with_at_location`."] RequiresAtLocation (DieReference < R :: Offset >) , # [doc = " The `Evaluation` needs the value produced by evaluating a DWARF"] # [doc = " expression at the entry point of the current subprogram.  Once the"] # [doc = " caller determines what value to provide it should resume the"] # [doc = " `Evaluation` by calling `Evaluation::resume_with_entry_value`."] RequiresEntryValue (Expression < R >) , # [doc = " The `Evaluation` needs the value of the parameter at the given location"] # [doc = " in the current function's caller.  Once the caller determines what value"] # [doc = " to provide it should resume the `Evaluation` by calling"] # [doc = " `Evaluation::resume_with_parameter_ref`."] RequiresParameterRef (UnitOffset < R :: Offset >) , # [doc = " The `Evaluation` needs an address to be relocated to proceed further."] # [doc = " Once the caller determines what value to provide it should resume the"] # [doc = " `Evaluation` by calling `Evaluation::resume_with_relocated_address`."] RequiresRelocatedAddress (u64) , # [doc = " The `Evaluation` needs an address from the `.debug_addr` section."] # [doc = " This address may also need to be relocated."] # [doc = " Once the caller determines what value to provide it should resume the"] # [doc = " `Evaluation` by calling `Evaluation::resume_with_indexed_address`."] RequiresIndexedAddress { # [doc = " The index of the address in the `.debug_addr` section,"] # [doc = " relative to the `DW_AT_addr_base` of the compilation unit."] index : DebugAddrIndex < R :: Offset > , # [doc = " Whether the address also needs to be relocated."] relocate : bool , } , # [doc = " The `Evaluation` needs the `ValueType` for the base type DIE at"] # [doc = " the give unit offset.  Once the caller determines what value to provide it"] # [doc = " should resume the `Evaluation` by calling"] # [doc = " `Evaluation::resume_with_base_type`."] RequiresBaseType (UnitOffset < R :: Offset >) , }
};
}
