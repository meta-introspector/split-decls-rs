// Generated macro for LineInstruction (enum)
macro_rules! Depcrate_read_lineLineInstruction {
() => {
// Module: crate::read::line
// Provides: {"LineInstruction"}
// Dependencies: {}
# [doc = " A parsed line number program instruction."] # [derive (Clone , Copy , Debug , PartialEq , Eq)] pub enum LineInstruction < R , Offset = < R as Reader > :: Offset > where R : Reader < Offset = Offset > , Offset : ReaderOffset , { # [doc = " > ### 6.2.5.1 Special Opcodes"] # [doc = " >"] # [doc = " > Each ubyte special opcode has the following effect on the state machine:"] # [doc = " >"] # [doc = " >   1. Add a signed integer to the line register."] # [doc = " >"] # [doc = " >   2. Modify the operation pointer by incrementing the address and"] # [doc = " >      op_index registers as described below."] # [doc = " >"] # [doc = " >   3. Append a row to the matrix using the current values of the state"] # [doc = " >      machine registers."] # [doc = " >"] # [doc = " >   4. Set the basic_block register to “false.”"] # [doc = " >"] # [doc = " >   5. Set the prologue_end register to “false.”"] # [doc = " >"] # [doc = " >   6. Set the epilogue_begin register to “false.”"] # [doc = " >"] # [doc = " >   7. Set the discriminator register to 0."] # [doc = " >"] # [doc = " > All of the special opcodes do those same seven things; they differ from"] # [doc = " > one another only in what values they add to the line, address and"] # [doc = " > op_index registers."] Special (u8) , # [doc = " \"[`LineInstruction::Copy`] appends a row to the matrix using the current"] # [doc = " values of the state machine registers. Then it sets the discriminator"] # [doc = " register to 0, and sets the basic_block, prologue_end and epilogue_begin"] # [doc = " registers to “false.”\""] Copy , # [doc = " \"The DW_LNS_advance_pc opcode takes a single unsigned LEB128 operand as"] # [doc = " the operation advance and modifies the address and op_index registers"] # [doc = " [the same as `LineInstruction::Special`]\""] AdvancePc (u64) , # [doc = " \"The DW_LNS_advance_line opcode takes a single signed LEB128 operand and"] # [doc = " adds that value to the line register of the state machine.\""] AdvanceLine (i64) , # [doc = " \"The DW_LNS_set_file opcode takes a single unsigned LEB128 operand and"] # [doc = " stores it in the file register of the state machine.\""] SetFile (u64) , # [doc = " \"The DW_LNS_set_column opcode takes a single unsigned LEB128 operand and"] # [doc = " stores it in the column register of the state machine.\""] SetColumn (u64) , # [doc = " \"The DW_LNS_negate_stmt opcode takes no operands. It sets the is_stmt"] # [doc = " register of the state machine to the logical negation of its current"] # [doc = " value.\""] NegateStatement , # [doc = " \"The DW_LNS_set_basic_block opcode takes no operands. It sets the"] # [doc = " basic_block register of the state machine to “true.”\""] SetBasicBlock , # [doc = " > The DW_LNS_const_add_pc opcode takes no operands. It advances the"] # [doc = " > address and op_index registers by the increments corresponding to"] # [doc = " > special opcode 255."] # [doc = " >"] # [doc = " > When the line number program needs to advance the address by a small"] # [doc = " > amount, it can use a single special opcode, which occupies a single"] # [doc = " > byte. When it needs to advance the address by up to twice the range of"] # [doc = " > the last special opcode, it can use DW_LNS_const_add_pc followed by a"] # [doc = " > special opcode, for a total of two bytes. Only if it needs to advance"] # [doc = " > the address by more than twice that range will it need to use both"] # [doc = " > DW_LNS_advance_pc and a special opcode, requiring three or more bytes."] ConstAddPc , # [doc = " > The DW_LNS_fixed_advance_pc opcode takes a single uhalf (unencoded)"] # [doc = " > operand and adds it to the address register of the state machine and"] # [doc = " > sets the op_index register to 0. This is the only standard opcode whose"] # [doc = " > operand is not a variable length number. It also does not multiply the"] # [doc = " > operand by the minimum_instruction_length field of the header."] FixedAddPc (u16) , # [doc = " \"[`LineInstruction::SetPrologueEnd`] sets the prologue_end register to “true”.\""] SetPrologueEnd , # [doc = " \"[`LineInstruction::SetEpilogueBegin`] sets the epilogue_begin register to"] # [doc = " “true”.\""] SetEpilogueBegin , # [doc = " \"The DW_LNS_set_isa opcode takes a single unsigned LEB128 operand and"] # [doc = " stores that value in the isa register of the state machine.\""] SetIsa (u64) , # [doc = " An unknown standard opcode with zero operands."] UnknownStandard0 (constants :: DwLns) , # [doc = " An unknown standard opcode with one operand."] UnknownStandard1 (constants :: DwLns , u64) , # [doc = " An unknown standard opcode with multiple operands."] UnknownStandardN (constants :: DwLns , R) , # [doc = " > [`LineInstruction::EndSequence`] sets the end_sequence register of the state"] # [doc = " > machine to “true” and appends a row to the matrix using the current"] # [doc = " > values of the state-machine registers. Then it resets the registers to"] # [doc = " > the initial values specified above (see Section 6.2.2). Every line"] # [doc = " > number program sequence must end with a DW_LNE_end_sequence instruction"] # [doc = " > which creates a row whose address is that of the byte after the last"] # [doc = " > target machine instruction of the sequence."] EndSequence , # [doc = " > The DW_LNE_set_address opcode takes a single relocatable address as an"] # [doc = " > operand. The size of the operand is the size of an address on the target"] # [doc = " > machine. It sets the address register to the value given by the"] # [doc = " > relocatable address and sets the op_index register to 0."] # [doc = " >"] # [doc = " > All of the other line number program opcodes that affect the address"] # [doc = " > register add a delta to it. This instruction stores a relocatable value"] # [doc = " > into it instead."] SetAddress (u64) , # [doc = " Defines a new source file in the line number program and appends it to"] # [doc = " the line number program header's list of source files."] DefineFile (FileEntry < R , Offset >) , # [doc = " \"The DW_LNE_set_discriminator opcode takes a single parameter, an"] # [doc = " unsigned LEB128 integer. It sets the discriminator register to the new"] # [doc = " value.\""] SetDiscriminator (u64) , # [doc = " An unknown extended opcode and the slice of its unparsed operands."] UnknownExtended (constants :: DwLne , R) , }
};
}
