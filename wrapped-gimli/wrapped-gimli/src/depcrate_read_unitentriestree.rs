// Generated macro for EntriesTree (struct)
macro_rules! Depcrate_read_unitEntriesTree {
() => {
// Module: crate::read::unit
// Provides: {"EntriesTree"}
// Dependencies: {}
# [doc = " The state information for a tree view of the Debugging Information Entries."] # [doc = ""] # [doc = " The `EntriesTree` can be used to recursively iterate through the DIE"] # [doc = " tree, following the parent/child relationships. The `EntriesTree` contains"] # [doc = " shared state for all nodes in the tree, avoiding any duplicate parsing of"] # [doc = " entries during the traversal."] # [doc = ""] # [doc = " ## Example Usage"] # [doc = " ```rust,no_run"] # [doc = " # fn example() -> Result<(), gimli::Error> {"] # [doc = " # let debug_info = gimli::DebugInfo::new(&[], gimli::LittleEndian);"] # [doc = " # let get_some_unit = || debug_info.units().next().unwrap().unwrap();"] # [doc = " let unit = get_some_unit();"] # [doc = " # let debug_abbrev = gimli::DebugAbbrev::new(&[], gimli::LittleEndian);"] # [doc = " # let get_abbrevs_for_unit = |_| unit.abbreviations(&debug_abbrev).unwrap();"] # [doc = " let abbrevs = get_abbrevs_for_unit(&unit);"] # [doc = ""] # [doc = " let mut tree = unit.entries_tree(&abbrevs, None)?;"] # [doc = " let root = tree.root()?;"] # [doc = " process_tree(root)?;"] # [doc = " # unreachable!()"] # [doc = " # }"] # [doc = ""] # [doc = " fn process_tree<R>(mut node: gimli::EntriesTreeNode<R>) -> gimli::Result<()>"] # [doc = "     where R: gimli::Reader"] # [doc = " {"] # [doc = "     {"] # [doc = "         // Examine the entry attributes."] # [doc = "         let mut attrs = node.entry().attrs();"] # [doc = "         while let Some(attr) = attrs.next()? {"] # [doc = "         }"] # [doc = "     }"] # [doc = "     let mut children = node.children();"] # [doc = "     while let Some(child) = children.next()? {"] # [doc = "         // Recursively process a child."] # [doc = "         process_tree(child);"] # [doc = "     }"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [derive (Clone , Debug)] pub struct EntriesTree < 'abbrev , 'unit , R > where R : Reader , { root : R , unit : & 'unit UnitHeader < R > , abbreviations : & 'abbrev Abbreviations , input : R , entry : Option < DebuggingInformationEntry < 'abbrev , 'unit , R > > , depth : isize , }
};
}
