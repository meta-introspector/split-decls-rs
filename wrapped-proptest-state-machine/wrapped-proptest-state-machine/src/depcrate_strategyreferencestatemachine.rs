// Generated macro for ReferenceStateMachine (trait)
macro_rules! Depcrate_strategyReferenceStateMachine {
() => {
// Module: crate::strategy
// Provides: {"ReferenceStateMachine"}
// Dependencies: {}
# [doc = " This trait is used to model system under test as an abstract state machine."] # [doc = ""] # [doc = " The key to how this works is that the set of next valid transitions depends"] # [doc = " on its current state (it's not the same as generating a random sequence of"] # [doc = " transitions) and just like other prop strategies, the state machine strategy"] # [doc = " attempts to shrink the transitions to find the minimal reproducible example"] # [doc = " when it encounters a case that breaks any of the defined properties."] # [doc = ""] # [doc = " This is achieved with the [`ReferenceStateMachine::transitions`] that takes"] # [doc = " the current state as an argument and can be used to decide which transitions"] # [doc = " are valid from this state, together with the"] # [doc = " [`ReferenceStateMachine::preconditions`], which are checked during generation"] # [doc = " of transitions and during shrinking."] # [doc = ""] # [doc = " Hence, the `preconditions` only needs to contain checks for invariants that"] # [doc = " depend on the current state and may be broken by shrinking and it doesn't"] # [doc = " need to cover invariants that do not depend on the current state."] # [doc = ""] # [doc = " The reference state machine generation runs before the generated transitions"] # [doc = " are attempted to be executed against the SUT (the concrete state machine)"] # [doc = " as defined by [`crate::StateMachineTest`]."] pub trait ReferenceStateMachine : 'static { # [doc = " The reference state machine's state type. This should contain the minimum"] # [doc = " required information needed to implement the state machine. It is used"] # [doc = " to drive the generations of transitions to decide which transitions are"] # [doc = " valid for the current state."] type State : Clone + Debug ; # [doc = " The reference state machine's transition type. This is typically an enum"] # [doc = " with its variants containing the parameters required to apply the"] # [doc = " transition, if any."] type Transition : Clone + Debug ; # [doc = " The initial state may be generated by any strategy. For a constant"] # [doc = " initial state, use [`proptest::strategy::Just`]."] fn init_state () -> BoxedStrategy < Self :: State > ; # [doc = " Generate the initial transitions."] fn transitions (state : & Self :: State) -> BoxedStrategy < Self :: Transition > ; # [doc = " Apply a transition in the reference state."] fn apply (state : Self :: State , transition : & Self :: Transition) -> Self :: State ; # [doc = " Pre-conditions may be specified to control which transitions are valid"] # [doc = " from the current state. If not overridden, this allows any transition."] # [doc = " The pre-conditions are checked in the generated transitions and during"] # [doc = " shrinking."] # [doc = ""] # [doc = " The pre-conditions checking relies on proptest global rejection"] # [doc = " filtering, which comes with some [disadvantages](https://altsysrq.github.io/proptest-book/proptest/tutorial/filtering.html)."] # [doc = " This means that pre-conditions that are hard to satisfy might slow down"] # [doc = " the test or even fail by exceeding the maximum rejection count."] fn preconditions (state : & Self :: State , transition : & Self :: Transition ,) -> bool { let _ = (state , transition) ; true } # [doc = " A sequential strategy runs the state machine transitions generated from"] # [doc = " the reference model sequentially in a test over a concrete state, which"] # [doc = " can be implemented with the help of"] # [doc = " [`crate::StateMachineTest`] trait."] # [doc = ""] # [doc = " You typically never need to override this method."] fn sequential_strategy (size : impl Into < SizeRange > ,) -> Sequential < Self :: State , Self :: Transition , BoxedStrategy < Self :: State > , BoxedStrategy < Self :: Transition > , > { Sequential :: new (size . into () , Self :: init_state , Self :: preconditions , Self :: transitions , Self :: apply ,) } }
};
}
