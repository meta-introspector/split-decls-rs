// Generated macro for Sequential (struct)
macro_rules! Depcrate_strategySequential {
() => {
// Module: crate::strategy
// Provides: {"Sequential"}
// Dependencies: {}
# [doc = " In a sequential state machine strategy, we first generate an acceptable"] # [doc = " sequence of transitions. That is a sequence that satisfies the given"] # [doc = " pre-conditions. The acceptability of each transition in the sequence depends"] # [doc = " on the current state of the state machine, which is updated by the"] # [doc = " transitions with the `next` function."] # [doc = ""] # [doc = " The shrinking strategy is to iteratively apply `Shrink::InitialState`,"] # [doc = " `Shrink::DeleteTransition` and `Shrink::Transition`."] # [doc = ""] # [doc = " 1. We start by trying to delete transitions from the back of the list that"] # [doc = "    were never seen by the test, if any. Note that because proptest expects"] # [doc = "    deterministic results in for reproducible issues, unlike the following"] # [doc = "    steps this step will not be undone on `complicate`. If there were any"] # [doc = "    unseen transitions, then the next step will start at trying to delete"] # [doc = "    the transition before the last one seen as we know that the last"] # [doc = "    transition cannot be deleted as it's the one that has failed."] # [doc = " 2. Then, we keep trying to delete transitions from the back of the list, until"] # [doc = "    we can do so no further (reached the beginning of the list).."] # [doc = " 3. Then, we again iteratively attempt to shrink the individual transitions,"] # [doc = "    but this time starting from the front of the list - i.e. from the first"] # [doc = "    transition to be applied."] # [doc = " 4. Finally, we try to shrink the initial state until it's not possible to"] # [doc = "    shrink it any further."] # [doc = ""] # [doc = " For `complicate`, we attempt to undo the last shrink operation, if there was"] # [doc = " any."] pub struct Sequential < State , Transition , StateStrategy , TransitionStrategy > { size : SizeRange , init_state : Arc < dyn Fn () -> StateStrategy > , preconditions : Arc < dyn Fn (& State , & Transition) -> bool > , transitions : Arc < dyn Fn (& State) -> TransitionStrategy > , next : Arc < dyn Fn (State , & Transition) -> State > , }
};
}
