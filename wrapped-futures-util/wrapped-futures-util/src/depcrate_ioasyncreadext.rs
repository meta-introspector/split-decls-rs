// Generated macro for AsyncReadExt (trait)
macro_rules! Depcrate_ioAsyncReadExt {
() => {
// Module: crate::io
// Provides: {"AsyncReadExt"}
// Dependencies: {}
# [doc = " An extension trait which adds utility methods to `AsyncRead` types."] pub trait AsyncReadExt : AsyncRead { # [doc = " Creates an adaptor which will chain this stream with another."] # [doc = ""] # [doc = " The returned `AsyncRead` instance will first read all bytes from this object"] # [doc = " until EOF is encountered. Afterwards the output is equivalent to the"] # [doc = " output of `next`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{AsyncReadExt, Cursor};"] # [doc = ""] # [doc = " let reader1 = Cursor::new([1, 2, 3, 4]);"] # [doc = " let reader2 = Cursor::new([5, 6, 7, 8]);"] # [doc = ""] # [doc = " let mut reader = reader1.chain(reader2);"] # [doc = " let mut buffer = Vec::new();"] # [doc = ""] # [doc = " // read the value into a Vec."] # [doc = " reader.read_to_end(&mut buffer).await?;"] # [doc = " assert_eq!(buffer, [1, 2, 3, 4, 5, 6, 7, 8]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn chain < R > (self , next : R) -> Chain < Self , R > where Self : Sized , R : AsyncRead , { assert_read (Chain :: new (self , next)) } # [doc = " Tries to read some bytes directly into the given `buf` in asynchronous"] # [doc = " manner, returning a future type."] # [doc = ""] # [doc = " The returned future will resolve to the number of bytes read once the read"] # [doc = " operation is completed."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{AsyncReadExt, Cursor};"] # [doc = ""] # [doc = " let mut reader = Cursor::new([1, 2, 3, 4]);"] # [doc = " let mut output = [0u8; 5];"] # [doc = ""] # [doc = " let bytes = reader.read(&mut output[..]).await?;"] # [doc = ""] # [doc = " // This is only guaranteed to be 4 because `&[u8]` is a synchronous"] # [doc = " // reader. In a real system you could get anywhere from 1 to"] # [doc = " // `output.len()` bytes in a single read."] # [doc = " assert_eq!(bytes, 4);"] # [doc = " assert_eq!(output, [1, 2, 3, 4, 0]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn read < 'a > (& 'a mut self , buf : & 'a mut [u8]) -> Read < 'a , Self > where Self : Unpin , { assert_future :: < Result < usize > , _ > (Read :: new (self , buf)) } # [doc = " Creates a future which will read from the `AsyncRead` into `bufs` using vectored"] # [doc = " IO operations."] # [doc = ""] # [doc = " The returned future will resolve to the number of bytes read once the read"] # [doc = " operation is completed."] fn read_vectored < 'a , 'b > (& 'a mut self , bufs : & 'a mut [IoSliceMut < 'b >] ,) -> ReadVectored < 'a , 'b , Self > where Self : Unpin , { assert_future :: < Result < usize > , _ > (ReadVectored :: new (self , bufs)) } # [doc = " Creates a future which will read exactly enough bytes to fill `buf`,"] # [doc = " returning an error if end of file (EOF) is hit sooner."] # [doc = ""] # [doc = " The returned future will resolve once the read operation is completed."] # [doc = ""] # [doc = " In the case of an error the buffer and the object will be discarded, with"] # [doc = " the error yielded."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{AsyncReadExt, Cursor};"] # [doc = ""] # [doc = " let mut reader = Cursor::new([1, 2, 3, 4]);"] # [doc = " let mut output = [0u8; 4];"] # [doc = ""] # [doc = " reader.read_exact(&mut output).await?;"] # [doc = ""] # [doc = " assert_eq!(output, [1, 2, 3, 4]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] # [doc = ""] # [doc = " ## EOF is hit before `buf` is filled"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{self, AsyncReadExt, Cursor};"] # [doc = ""] # [doc = " let mut reader = Cursor::new([1, 2, 3, 4]);"] # [doc = " let mut output = [0u8; 5];"] # [doc = ""] # [doc = " let result = reader.read_exact(&mut output).await;"] # [doc = ""] # [doc = " assert_eq!(result.unwrap_err().kind(), io::ErrorKind::UnexpectedEof);"] # [doc = " # });"] # [doc = " ```"] fn read_exact < 'a > (& 'a mut self , buf : & 'a mut [u8]) -> ReadExact < 'a , Self > where Self : Unpin , { assert_future :: < Result < () > , _ > (ReadExact :: new (self , buf)) } # [doc = " Creates a future which will read all the bytes from this `AsyncRead`."] # [doc = ""] # [doc = " On success the total number of bytes read is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{AsyncReadExt, Cursor};"] # [doc = ""] # [doc = " let mut reader = Cursor::new([1, 2, 3, 4]);"] # [doc = " let mut output = Vec::with_capacity(4);"] # [doc = ""] # [doc = " let bytes = reader.read_to_end(&mut output).await?;"] # [doc = ""] # [doc = " assert_eq!(bytes, 4);"] # [doc = " assert_eq!(output, vec![1, 2, 3, 4]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn read_to_end < 'a > (& 'a mut self , buf : & 'a mut Vec < u8 >) -> ReadToEnd < 'a , Self > where Self : Unpin , { assert_future :: < Result < usize > , _ > (ReadToEnd :: new (self , buf)) } # [doc = " Creates a future which will read all the bytes from this `AsyncRead`."] # [doc = ""] # [doc = " On success the total number of bytes read is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{AsyncReadExt, Cursor};"] # [doc = ""] # [doc = " let mut reader = Cursor::new(&b\"1234\"[..]);"] # [doc = " let mut buffer = String::with_capacity(4);"] # [doc = ""] # [doc = " let bytes = reader.read_to_string(&mut buffer).await?;"] # [doc = ""] # [doc = " assert_eq!(bytes, 4);"] # [doc = " assert_eq!(buffer, String::from(\"1234\"));"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn read_to_string < 'a > (& 'a mut self , buf : & 'a mut String) -> ReadToString < 'a , Self > where Self : Unpin , { assert_future :: < Result < usize > , _ > (ReadToString :: new (self , buf)) } # [doc = " Helper method for splitting this read/write object into two halves."] # [doc = ""] # [doc = " The two halves returned implement the `AsyncRead` and `AsyncWrite`"] # [doc = " traits, respectively."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{self, AsyncReadExt, Cursor};"] # [doc = ""] # [doc = " // Note that for `Cursor` the read and write halves share a single"] # [doc = " // seek position. This may or may not be true for other types that"] # [doc = " // implement both `AsyncRead` and `AsyncWrite`."] # [doc = ""] # [doc = " let reader = Cursor::new([1, 2, 3, 4]);"] # [doc = " let mut buffer = Cursor::new(vec![0, 0, 0, 0, 5, 6, 7, 8]);"] # [doc = " let mut writer = Cursor::new(vec![0u8; 5]);"] # [doc = ""] # [doc = " {"] # [doc = "     let (buffer_reader, mut buffer_writer) = (&mut buffer).split();"] # [doc = "     io::copy(reader, &mut buffer_writer).await?;"] # [doc = "     io::copy(buffer_reader, &mut writer).await?;"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(buffer.into_inner(), [1, 2, 3, 4, 5, 6, 7, 8]);"] # [doc = " assert_eq!(writer.into_inner(), [5, 6, 7, 8, 0]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn split (self) -> (ReadHalf < Self > , WriteHalf < Self >) where Self : AsyncWrite + Sized , { let (r , w) = split :: split (self) ; (assert_read (r) , assert_write (w)) } # [doc = " Creates an AsyncRead adapter which will read at most `limit` bytes"] # [doc = " from the underlying reader."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{AsyncReadExt, Cursor};"] # [doc = ""] # [doc = " let reader = Cursor::new(&b\"12345678\"[..]);"] # [doc = " let mut buffer = [0; 5];"] # [doc = ""] # [doc = " let mut take = reader.take(4);"] # [doc = " let n = take.read(&mut buffer).await?;"] # [doc = ""] # [doc = " assert_eq!(n, 4);"] # [doc = " assert_eq!(&buffer, b\"1234\\0\");"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn take (self , limit : u64) -> Take < Self > where Self : Sized , { assert_read (Take :: new (self , limit)) } # [doc = " Wraps an [`AsyncRead`] in a compatibility wrapper that allows it to be"] # [doc = " used as a futures 0.1 / tokio-io 0.1 `AsyncRead`. If the wrapped type"] # [doc = " implements [`AsyncWrite`] as well, the result will also implement the"] # [doc = " futures 0.1 / tokio 0.1 `AsyncWrite` trait."] # [doc = ""] # [doc = " Requires the `io-compat` feature to enable."] # [cfg (feature = "io-compat")] # [cfg_attr (docsrs , doc (cfg (feature = "io-compat")))] fn compat (self) -> Compat < Self > where Self : Sized + Unpin , { Compat :: new (self) } }
};
}
