// Generated macro for SpawnExt (trait)
macro_rules! Depcrate_task_spawnSpawnExt {
() => {
// Module: crate::task::spawn
// Provides: {"SpawnExt"}
// Dependencies: {}
# [doc = " Extension trait for `Spawn`."] pub trait SpawnExt : Spawn { # [doc = " Spawns a task that polls the given future with output `()` to"] # [doc = " completion."] # [doc = ""] # [doc = " This method returns a [`Result`] that contains a [`SpawnError`] if"] # [doc = " spawning fails."] # [doc = ""] # [doc = " You can use [`spawn_with_handle`](SpawnExt::spawn_with_handle) if"] # [doc = " you want to spawn a future with output other than `()` or if you want"] # [doc = " to be able to await its completion."] # [doc = ""] # [doc = " Note this method will eventually be replaced with the upcoming"] # [doc = " `Spawn::spawn` method which will take a `dyn Future` as input."] # [doc = " Technical limitations prevent `Spawn::spawn` from being implemented"] # [doc = " today. Feel free to use this method in the meantime."] # [doc = ""] # [doc = " ```"] # [doc = " # {"] # [doc = " use futures::executor::ThreadPool;"] # [doc = " use futures::task::SpawnExt;"] # [doc = ""] # [doc = " let executor = ThreadPool::new().unwrap();"] # [doc = ""] # [doc = " let future = async { /* ... */ };"] # [doc = " executor.spawn(future).unwrap();"] # [doc = " # }"] # [doc = " # std::thread::sleep(std::time::Duration::from_millis(500)); // wait for background threads closed: https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] # [cfg (feature = "alloc")] fn spawn < Fut > (& self , future : Fut) -> Result < () , SpawnError > where Fut : Future < Output = () > + Send + 'static , { self . spawn_obj (FutureObj :: new (Box :: new (future))) } # [doc = " Spawns a task that polls the given future to completion and returns a"] # [doc = " future that resolves to the spawned future's output."] # [doc = ""] # [doc = " This method returns a [`Result`] that contains a [`RemoteHandle`](crate::future::RemoteHandle), or, if"] # [doc = " spawning fails, a [`SpawnError`]. [`RemoteHandle`](crate::future::RemoteHandle) is a future that"] # [doc = " resolves to the output of the spawned future."] # [doc = ""] # [doc = " ```"] # [doc = " # {"] # [doc = " use futures::executor::{block_on, ThreadPool};"] # [doc = " use futures::future;"] # [doc = " use futures::task::SpawnExt;"] # [doc = ""] # [doc = " let executor = ThreadPool::new().unwrap();"] # [doc = ""] # [doc = " let future = future::ready(1);"] # [doc = " let join_handle_fut = executor.spawn_with_handle(future).unwrap();"] # [doc = " assert_eq!(block_on(join_handle_fut), 1);"] # [doc = " # }"] # [doc = " # std::thread::sleep(std::time::Duration::from_millis(500)); // wait for background threads closed: https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] # [cfg (feature = "channel")] # [cfg_attr (docsrs , doc (cfg (feature = "channel")))] # [cfg (feature = "std")] fn spawn_with_handle < Fut > (& self , future : Fut) -> Result < RemoteHandle < Fut :: Output > , SpawnError > where Fut : Future + Send + 'static , Fut :: Output : Send , { let (future , handle) = future . remote_handle () ; self . spawn (future) ? ; Ok (handle) } # [doc = " Wraps a [`Spawn`] and makes it usable as a futures 0.1 `Executor`."] # [doc = " Requires the `compat` feature to enable."] # [cfg (feature = "compat")] # [cfg_attr (docsrs , doc (cfg (feature = "compat")))] fn compat (self) -> Compat < Self > where Self : Sized , { Compat :: new (self) } }
};
}
