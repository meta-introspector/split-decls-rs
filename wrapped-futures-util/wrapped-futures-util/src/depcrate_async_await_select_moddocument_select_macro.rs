// Generated macro for document_select_macro (macro)
macro_rules! Depcrate_async_await_select_moddocument_select_macro {
() => {
// Module: crate::async_await::select_mod
// Provides: {"document_select_macro"}
// Dependencies: {}
macro_rules ! document_select_macro { ($ select : item) => { # [allow (clippy :: too_long_first_doc_paragraph)] # [doc = " Polls multiple futures and streams simultaneously, executing the branch"] # [doc = " for the future that finishes first. If multiple futures are ready,"] # [doc = " one will be pseudo-randomly selected at runtime. Futures directly"] # [doc = " passed to `select!` must be `Unpin` and implement `FusedFuture`."] # [doc = ""] # [doc = " If an expression which yields a `Future` is passed to `select!`"] # [doc = " (e.g. an `async fn` call) instead of a `Future` by name the `Unpin`"] # [doc = " requirement is relaxed, since the macro will pin the resulting `Future`"] # [doc = " on the stack. However the `Future` returned by the expression must"] # [doc = " still implement `FusedFuture`."] # [doc = ""] # [doc = " Futures and streams which are not already fused can be fused using the"] # [doc = " `.fuse()` method. Note, though, that fusing a future or stream directly"] # [doc = " in the call to `select!` will not be enough to prevent it from being"] # [doc = " polled after completion if the `select!` call is in a loop, so when"] # [doc = " `select!`ing in a loop, users should take care to `fuse()` outside of"] # [doc = " the loop."] # [doc = ""] # [doc = " `select!` can be used as an expression and will return the return"] # [doc = " value of the selected branch. For this reason the return type of every"] # [doc = " branch in a `select!` must be the same."] # [doc = ""] # [doc = " This macro is only usable inside of async functions, closures, and blocks."] # [doc = " It is also gated behind the `async-await` feature of this library, which is"] # [doc = " activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::select;"] # [doc = " let mut a = future::ready(4);"] # [doc = " let mut b = future::pending::<()>();"] # [doc = ""] # [doc = " let res = select! {"] # [doc = "     a_res = a => a_res + 1,"] # [doc = "     _ = b => 0,"] # [doc = " };"] # [doc = " assert_eq!(res, 5);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = " use futures::select;"] # [doc = " let mut st = stream::iter(vec![2]).fuse();"] # [doc = " let mut fut = future::pending::<()>();"] # [doc = ""] # [doc = " select! {"] # [doc = "     x = st.next() => assert_eq!(Some(2), x),"] # [doc = "     _ = fut => panic!(),"] # [doc = " }"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " As described earlier, `select` can directly select on expressions"] # [doc = " which return `Future`s - even if those do not implement `Unpin`:"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::FutureExt;"] # [doc = " use futures::select;"] # [doc = ""] # [doc = " // Calling the following async fn returns a Future which does not"] # [doc = " // implement Unpin"] # [doc = " async fn async_identity_fn(arg: usize) -> usize {"] # [doc = "     arg"] # [doc = " }"] # [doc = ""] # [doc = " let res = select! {"] # [doc = "     a_res = async_identity_fn(62).fuse() => a_res + 1,"] # [doc = "     b_res = async_identity_fn(13).fuse() => b_res,"] # [doc = " };"] # [doc = " assert!(res == 63 || res == 13);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " If a similar async function is called outside of `select` to produce"] # [doc = " a `Future`, the `Future` must be pinned in order to be able to pass"] # [doc = " it to `select`. This can be achieved via `Box::pin` for pinning a"] # [doc = " `Future` on the heap or the `pin!` macro for pinning a `Future`"] # [doc = " on the stack."] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use core::pin::pin;"] # [doc = ""] # [doc = " use futures::future::FutureExt;"] # [doc = " use futures::select;"] # [doc = ""] # [doc = " // Calling the following async fn returns a Future which does not"] # [doc = " // implement Unpin"] # [doc = " async fn async_identity_fn(arg: usize) -> usize {"] # [doc = "     arg"] # [doc = " }"] # [doc = ""] # [doc = " let fut_1 = async_identity_fn(1).fuse();"] # [doc = " let fut_2 = async_identity_fn(2).fuse();"] # [doc = " let mut fut_1 = Box::pin(fut_1); // Pins the Future on the heap"] # [doc = " let mut fut_2 = pin!(fut_2); // Pins the Future on the stack"] # [doc = ""] # [doc = " let res = select! {"] # [doc = "     a_res = fut_1 => a_res,"] # [doc = "     b_res = fut_2 => b_res,"] # [doc = " };"] # [doc = " assert!(res == 1 || res == 2);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " `select` also accepts a `complete` branch and a `default` branch."] # [doc = " `complete` will run if all futures and streams have already been"] # [doc = " exhausted. `default` will run if no futures or streams are"] # [doc = " immediately ready. `complete` takes priority over `default` in"] # [doc = " the case where all futures have completed."] # [doc = " A motivating use-case for passing `Future`s by name as well as for"] # [doc = " `complete` blocks is to call `select!` in a loop, which is"] # [doc = " demonstrated in the following example:"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::select;"] # [doc = " let mut a_fut = future::ready(4);"] # [doc = " let mut b_fut = future::ready(6);"] # [doc = " let mut total = 0;"] # [doc = ""] # [doc = " loop {"] # [doc = "     select! {"] # [doc = "         a = a_fut => total += a,"] # [doc = "         b = b_fut => total += b,"] # [doc = "         complete => break,"] # [doc = "         default => panic!(), // never runs (futures run first, then complete)"] # [doc = "     }"] # [doc = " }"] # [doc = " assert_eq!(total, 10);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " Note that the futures that have been matched over can still be mutated"] # [doc = " from inside the `select!` block's branches. This can be used to implement"] # [doc = " more complex behavior such as timer resets or writing into the head of"] # [doc = " a stream."] $ select } ; ($ select : item $ select_biased : item) => { document_select_macro ! ($ select) ; # [allow (clippy :: too_long_first_doc_paragraph)] # [doc = " Polls multiple futures and streams simultaneously, executing the branch"] # [doc = " for the future that finishes first. Unlike [`select!`], if multiple futures are ready,"] # [doc = " one will be selected in order of declaration. Futures directly"] # [doc = " passed to `select_biased!` must be `Unpin` and implement `FusedFuture`."] # [doc = ""] # [doc = " If an expression which yields a `Future` is passed to `select_biased!`"] # [doc = " (e.g. an `async fn` call) instead of a `Future` by name the `Unpin`"] # [doc = " requirement is relaxed, since the macro will pin the resulting `Future`"] # [doc = " on the stack. However the `Future` returned by the expression must"] # [doc = " still implement `FusedFuture`."] # [doc = ""] # [doc = " Futures and streams which are not already fused can be fused using the"] # [doc = " `.fuse()` method. Note, though, that fusing a future or stream directly"] # [doc = " in the call to `select_biased!` will not be enough to prevent it from being"] # [doc = " polled after completion if the `select_biased!` call is in a loop, so when"] # [doc = " `select_biased!`ing in a loop, users should take care to `fuse()` outside of"] # [doc = " the loop."] # [doc = ""] # [doc = " `select_biased!` can be used as an expression and will return the return"] # [doc = " value of the selected branch. For this reason the return type of every"] # [doc = " branch in a `select_biased!` must be the same."] # [doc = ""] # [doc = " This macro is only usable inside of async functions, closures, and blocks."] # [doc = " It is also gated behind the `async-await` feature of this library, which is"] # [doc = " activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::select_biased;"] # [doc = " let mut a = future::ready(4);"] # [doc = " let mut b = future::pending::<()>();"] # [doc = ""] # [doc = " let res = select_biased! {"] # [doc = "     a_res = a => a_res + 1,"] # [doc = "     _ = b => 0,"] # [doc = " };"] # [doc = " assert_eq!(res, 5);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = " use futures::select_biased;"] # [doc = " let mut st = stream::iter(vec![2]).fuse();"] # [doc = " let mut fut = future::pending::<()>();"] # [doc = ""] # [doc = " select_biased! {"] # [doc = "     x = st.next() => assert_eq!(Some(2), x),"] # [doc = "     _ = fut => panic!(),"] # [doc = " }"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " As described earlier, `select_biased` can directly select on expressions"] # [doc = " which return `Future`s - even if those do not implement `Unpin`:"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::FutureExt;"] # [doc = " use futures::select_biased;"] # [doc = ""] # [doc = " // Calling the following async fn returns a Future which does not"] # [doc = " // implement Unpin"] # [doc = " async fn async_identity_fn(arg: usize) -> usize {"] # [doc = "     arg"] # [doc = " }"] # [doc = ""] # [doc = " let res = select_biased! {"] # [doc = "     a_res = async_identity_fn(62).fuse() => a_res + 1,"] # [doc = "     b_res = async_identity_fn(13).fuse() => b_res,"] # [doc = " };"] # [doc = " assert_eq!(res, 63);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " If a similar async function is called outside of `select_biased` to produce"] # [doc = " a `Future`, the `Future` must be pinned in order to be able to pass"] # [doc = " it to `select_biased`. This can be achieved via `Box::pin` for pinning a"] # [doc = " `Future` on the heap or the `pin!` macro for pinning a `Future`"] # [doc = " on the stack."] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use core::pin::pin;"] # [doc = ""] # [doc = " use futures::future::FutureExt;"] # [doc = " use futures::select_biased;"] # [doc = ""] # [doc = " // Calling the following async fn returns a Future which does not"] # [doc = " // implement Unpin"] # [doc = " async fn async_identity_fn(arg: usize) -> usize {"] # [doc = "     arg"] # [doc = " }"] # [doc = ""] # [doc = " let fut_1 = async_identity_fn(1).fuse();"] # [doc = " let fut_2 = async_identity_fn(2).fuse();"] # [doc = " let mut fut_1 = Box::pin(fut_1); // Pins the Future on the heap"] # [doc = " let mut fut_2 = pin!(fut_2); // Pins the Future on the stack"] # [doc = ""] # [doc = " let res = select_biased! {"] # [doc = "     a_res = fut_1 => a_res,"] # [doc = "     b_res = fut_2 => b_res,"] # [doc = " };"] # [doc = " assert!(res == 1 || res == 2);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " `select_biased` also accepts a `complete` branch and a `default` branch."] # [doc = " `complete` will run if all futures and streams have already been"] # [doc = " exhausted. `default` will run if no futures or streams are"] # [doc = " immediately ready. `complete` takes priority over `default` in"] # [doc = " the case where all futures have completed."] # [doc = " A motivating use-case for passing `Future`s by name as well as for"] # [doc = " `complete` blocks is to call `select_biased!` in a loop, which is"] # [doc = " demonstrated in the following example:"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::select_biased;"] # [doc = " let mut a_fut = future::ready(4);"] # [doc = " let mut b_fut = future::ready(6);"] # [doc = " let mut total = 0;"] # [doc = ""] # [doc = " loop {"] # [doc = "     select_biased! {"] # [doc = "         a = a_fut => total += a,"] # [doc = "         b = b_fut => total += b,"] # [doc = "         complete => break,"] # [doc = "         default => panic!(), // never runs (futures run first, then complete)"] # [doc = "     }"] # [doc = " }"] # [doc = " assert_eq!(total, 10);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " Note that the futures that have been matched over can still be mutated"] # [doc = " from inside the `select_biased!` block's branches. This can be used to implement"] # [doc = " more complex behavior such as timer resets or writing into the head of"] # [doc = " a stream."] # [doc = ""] # [doc = " [`select!`]: macro.select.html"] $ select_biased } ; }
};
}
