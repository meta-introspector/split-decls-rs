// Generated macro for try_join_all (function)
macro_rules! Depcrate_future_try_join_alltry_join_all {
() => {
// Module: crate::future::try_join_all
// Provides: {"try_join_all"}
// Dependencies: {}
# [doc = " Creates a future which represents either a collection of the results of the"] # [doc = " futures given or an error."] # [doc = ""] # [doc = " The returned future will drive execution for all of its underlying futures,"] # [doc = " collecting the results into a destination `Vec<T>` in the same order as they"] # [doc = " were provided."] # [doc = ""] # [doc = " If any future returns an error then all other futures will be canceled and"] # [doc = " an error will be returned immediately. If all futures complete successfully,"] # [doc = " however, then the returned future will succeed with a `Vec` of all the"] # [doc = " successful results."] # [doc = ""] # [doc = " This function is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # See Also"] # [doc = ""] # [doc = " `try_join_all` will switch to the more powerful [`FuturesOrdered`] for performance"] # [doc = " reasons if the number of futures is large. You may want to look into using it or"] # [doc = " it's counterpart [`FuturesUnordered`][crate::stream::FuturesUnordered] directly."] # [doc = ""] # [doc = " Some examples for additional functionality provided by these are:"] # [doc = ""] # [doc = "  * Adding new futures to the set even after it has been started."] # [doc = ""] # [doc = "  * Only polling the specific futures that have been woken. In cases where"] # [doc = "    you have a lot of futures this will result in much more efficient polling."] # [doc = ""] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::{self, try_join_all};"] # [doc = ""] # [doc = " let futures = vec!["] # [doc = "     future::ok::<u32, u32>(1),"] # [doc = "     future::ok::<u32, u32>(2),"] # [doc = "     future::ok::<u32, u32>(3),"] # [doc = " ];"] # [doc = ""] # [doc = " assert_eq!(try_join_all(futures).await, Ok(vec![1, 2, 3]));"] # [doc = ""] # [doc = " let futures = vec!["] # [doc = "     future::ok::<u32, u32>(1),"] # [doc = "     future::err::<u32, u32>(2),"] # [doc = "     future::ok::<u32, u32>(3),"] # [doc = " ];"] # [doc = ""] # [doc = " assert_eq!(try_join_all(futures).await, Err(2));"] # [doc = " # });"] # [doc = " ```"] pub fn try_join_all < I > (iter : I) -> TryJoinAll < I :: Item > where I : IntoIterator , I :: Item : TryFuture , { let iter = iter . into_iter () . map (TryFutureExt :: into_future) ; # [cfg (target_os = "none")] # [cfg_attr (target_os = "none" , cfg (not (target_has_atomic = "ptr")))] { let kind = TryJoinAllKind :: Small { elems : iter . map (TryMaybeDone :: Future) . collect :: < Box < [_] > > () . into () , } ; assert_future :: < Result < Vec < < I :: Item as TryFuture > :: Ok > , < I :: Item as TryFuture > :: Error > , _ > (TryJoinAll { kind } ,) } # [cfg_attr (target_os = "none" , cfg (target_has_atomic = "ptr"))] { let kind = match iter . size_hint () . 1 { Some (max) if max <= join_all :: SMALL => TryJoinAllKind :: Small { elems : iter . map (TryMaybeDone :: Future) . collect :: < Box < [_] > > () . into () , } , _ => TryJoinAllKind :: Big { fut : iter . collect :: < FuturesOrdered < _ > > () . try_collect () } , } ; assert_future :: < Result < Vec < < I :: Item as TryFuture > :: Ok > , < I :: Item as TryFuture > :: Error > , _ > (TryJoinAll { kind } ,) } }
};
}
