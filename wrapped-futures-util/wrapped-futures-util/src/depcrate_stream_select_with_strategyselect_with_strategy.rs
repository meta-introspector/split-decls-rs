// Generated macro for select_with_strategy (function)
macro_rules! Depcrate_stream_select_with_strategyselect_with_strategy {
() => {
// Module: crate::stream::select_with_strategy
// Provides: {"select_with_strategy"}
// Dependencies: {}
# [allow (clippy :: too_long_first_doc_paragraph)] # [doc = " This function will attempt to pull items from both streams. You provide a"] # [doc = " closure to tell [`SelectWithStrategy`] which stream to poll. The closure can"] # [doc = " store state on `SelectWithStrategy` to which it will receive a `&mut` on every"] # [doc = " invocation. This allows basing the strategy on prior choices."] # [doc = ""] # [doc = " After one of the two input streams completes, the remaining one will be"] # [doc = " polled exclusively. The returned stream completes when both input"] # [doc = " streams have completed."] # [doc = ""] # [doc = " Note that this function consumes both streams and returns a wrapped"] # [doc = " version of them."] # [doc = ""] # [doc = " ## Examples"] # [doc = ""] # [doc = " ### Priority"] # [doc = " This example shows how to always prioritize the left stream."] # [doc = ""] # [doc = " ```rust"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{ repeat, select_with_strategy, PollNext, StreamExt };"] # [doc = ""] # [doc = " let left = repeat(1);"] # [doc = " let right = repeat(2);"] # [doc = ""] # [doc = " // We don't need any state, so let's make it an empty tuple."] # [doc = " // We must provide some type here, as there is no way for the compiler"] # [doc = " // to infer it. As we don't need to capture variables, we can just"] # [doc = " // use a function pointer instead of a closure."] # [doc = " fn prio_left(_: &mut ()) -> PollNext { PollNext::Left }"] # [doc = ""] # [doc = " let mut out = select_with_strategy(left, right, prio_left);"] # [doc = ""] # [doc = " for _ in 0..100 {"] # [doc = "     // Whenever we poll out, we will always get `1`."] # [doc = "     assert_eq!(1, out.select_next_some().await);"] # [doc = " }"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " ### Round Robin"] # [doc = " This example shows how to select from both streams round robin."] # [doc = " Note: this special case is provided by [`futures-util::stream::select`]."] # [doc = ""] # [doc = " ```rust"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{ repeat, select_with_strategy, PollNext, StreamExt };"] # [doc = ""] # [doc = " let left = repeat(1);"] # [doc = " let right = repeat(2);"] # [doc = ""] # [doc = " let rrobin = |last: &mut PollNext| last.toggle();"] # [doc = ""] # [doc = " let mut out = select_with_strategy(left, right, rrobin);"] # [doc = ""] # [doc = " for _ in 0..100 {"] # [doc = "     // We should be alternating now."] # [doc = "     assert_eq!(1, out.select_next_some().await);"] # [doc = "     assert_eq!(2, out.select_next_some().await);"] # [doc = " }"] # [doc = " # });"] # [doc = " ```"] pub fn select_with_strategy < St1 , St2 , Clos , State > (stream1 : St1 , stream2 : St2 , which : Clos ,) -> SelectWithStrategy < St1 , St2 , Clos , State > where St1 : Stream , St2 : Stream < Item = St1 :: Item > , Clos : FnMut (& mut State) -> PollNext , State : Default , { assert_stream :: < St1 :: Item , _ > (SelectWithStrategy { stream1 , stream2 , state : Default :: default () , internal_state : InternalState :: Start , clos : which , }) }
};
}
