// Generated macro for try_unfold (function)
macro_rules! Depcrate_stream_try_stream_try_unfoldtry_unfold {
() => {
// Module: crate::stream::try_stream::try_unfold
// Provides: {"try_unfold"}
// Dependencies: {}
# [doc = " Creates a `TryStream` from a seed and a closure returning a `TryFuture`."] # [doc = ""] # [doc = " This function is the dual for the `TryStream::try_fold()` adapter: while"] # [doc = " `TryStream::try_fold()` reduces a `TryStream` to one single value,"] # [doc = " `try_unfold()` creates a `TryStream` from a seed value."] # [doc = ""] # [doc = " `try_unfold()` will call the provided closure with the provided seed, then"] # [doc = " wait for the returned `TryFuture` to complete with `(a, b)`. It will then"] # [doc = " yield the value `a`, and use `b` as the next internal state."] # [doc = ""] # [doc = " If the closure returns `None` instead of `Some(TryFuture)`, then the"] # [doc = " `try_unfold()` will stop producing items and return `Poll::Ready(None)` in"] # [doc = " future calls to `poll()`."] # [doc = ""] # [doc = " In case of error generated by the returned `TryFuture`, the error will be"] # [doc = " returned by the `TryStream`. The `TryStream` will then yield"] # [doc = " `Poll::Ready(None)` in future calls to `poll()`."] # [doc = ""] # [doc = " This function can typically be used when wanting to go from the \"world of"] # [doc = " futures\" to the \"world of streams\": the provided closure can build a"] # [doc = " `TryFuture` using other library functions working on futures, and"] # [doc = " `try_unfold()` will turn it into a `TryStream` by repeating the operation."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # #[derive(Debug, PartialEq)]"] # [doc = " # struct SomeError;"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, TryStreamExt};"] # [doc = ""] # [doc = " let stream = stream::try_unfold(0, |state| async move {"] # [doc = "     if state < 0 {"] # [doc = "         return Err(SomeError);"] # [doc = "     }"] # [doc = ""] # [doc = "     if state <= 2 {"] # [doc = "         let next_state = state + 1;"] # [doc = "         let yielded = state * 2;"] # [doc = "         Ok(Some((yielded, next_state)))"] # [doc = "     } else {"] # [doc = "         Ok(None)"] # [doc = "     }"] # [doc = " });"] # [doc = ""] # [doc = " let result: Result<Vec<i32>, _> = stream.try_collect().await;"] # [doc = " assert_eq!(result, Ok(vec![0, 2, 4]));"] # [doc = " # });"] # [doc = " ```"] pub fn try_unfold < T , F , Fut , Item > (init : T , f : F) -> TryUnfold < T , F , Fut > where F : FnMut (T) -> Fut , Fut : TryFuture < Ok = Option < (Item , T) > > , { assert_stream :: < Result < Item , Fut :: Error > , _ > (TryUnfold { f , state : Some (init) , fut : None }) }
};
}
