// Generated macro for impl_2681 (impl)
macro_rules! Depcrate_abortableimpl_2681 {
() => {
// Module: crate::abortable
// Provides: {"impl_2681"}
// Dependencies: {}
impl < T > Abortable < T > { # [doc = " Creates a new `Abortable` future/stream using an existing `AbortRegistration`."] # [doc = " `AbortRegistration`s can be acquired through `AbortHandle::new`."] # [doc = ""] # [doc = " When `abort` is called on the handle tied to `reg` or if `abort` has"] # [doc = " already been called, the future/stream will complete immediately without making"] # [doc = " any further progress."] # [doc = ""] # [doc = " # Examples:"] # [doc = ""] # [doc = " Usage with futures:"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::{Abortable, AbortHandle, Aborted};"] # [doc = ""] # [doc = " let (abort_handle, abort_registration) = AbortHandle::new_pair();"] # [doc = " let future = Abortable::new(async { 2 }, abort_registration);"] # [doc = " abort_handle.abort();"] # [doc = " assert_eq!(future.await, Err(Aborted));"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " Usage with streams:"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " # use futures::future::{Abortable, AbortHandle};"] # [doc = " # use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let (abort_handle, abort_registration) = AbortHandle::new_pair();"] # [doc = " let mut stream = Abortable::new(stream::iter(vec![1, 2, 3]), abort_registration);"] # [doc = " abort_handle.abort();"] # [doc = " assert_eq!(stream.next().await, None);"] # [doc = " # });"] # [doc = " ```"] pub fn new (task : T , reg : AbortRegistration) -> Self { Self { task , inner : reg . inner } } # [doc = " Checks whether the task has been aborted. Note that all this"] # [doc = " method indicates is whether [`AbortHandle::abort`] was *called*."] # [doc = " This means that it will return `true` even if:"] # [doc = " * `abort` was called after the task had completed."] # [doc = " * `abort` was called while the task was being polled - the task may still be running and"] # [doc = "   will not be stopped until `poll` returns."] pub fn is_aborted (& self) -> bool { self . inner . aborted . load (Ordering :: Relaxed) } }
};
}
