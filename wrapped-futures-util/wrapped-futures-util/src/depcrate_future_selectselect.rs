// Generated macro for select (function)
macro_rules! Depcrate_future_selectselect {
() => {
// Module: crate::future::select
// Provides: {"select"}
// Dependencies: {}
# [doc = " Waits for either one of two differently-typed futures to complete."] # [doc = ""] # [doc = " This function will return a new future which awaits for either one of both"] # [doc = " futures to complete. The returned future will finish with both the value"] # [doc = " resolved and a future representing the completion of the other work."] # [doc = ""] # [doc = " Note that this function consumes the receiving futures and returns a"] # [doc = " wrapped version of them."] # [doc = ""] # [doc = " Also note that if both this and the second future have the same"] # [doc = " output type you can use the `Either::factor_first` method to"] # [doc = " conveniently extract out the value at the end."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " A simple example"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use core::pin::pin;"] # [doc = ""] # [doc = " use futures::future;"] # [doc = " use futures::future::Either;"] # [doc = ""] # [doc = " // These two futures have different types even though their outputs have the same type."] # [doc = " let future1 = async {"] # [doc = "     future::pending::<()>().await; // will never finish"] # [doc = "     1"] # [doc = " };"] # [doc = " let future2 = async {"] # [doc = "     future::ready(2).await"] # [doc = " };"] # [doc = ""] # [doc = " // 'select' requires Future + Unpin bounds"] # [doc = " let future1 = pin!(future1);"] # [doc = " let future2 = pin!(future2);"] # [doc = ""] # [doc = " let value = match future::select(future1, future2).await {"] # [doc = "     Either::Left((value1, _)) => value1,  // `value1` is resolved from `future1`"] # [doc = "                                           // `_` represents `future2`"] # [doc = "     Either::Right((value2, _)) => value2, // `value2` is resolved from `future2`"] # [doc = "                                           // `_` represents `future1`"] # [doc = " };"] # [doc = ""] # [doc = " assert!(value == 2);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " A more complex example"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::future::{self, Either, Future, FutureExt};"] # [doc = ""] # [doc = " // A poor-man's join implemented on top of select"] # [doc = ""] # [doc = " fn join<A, B>(a: A, b: B) -> impl Future<Output=(A::Output, B::Output)>"] # [doc = "     where A: Future + Unpin,"] # [doc = "           B: Future + Unpin,"] # [doc = " {"] # [doc = "     future::select(a, b).then(|either| {"] # [doc = "         match either {"] # [doc = "             Either::Left((x, b)) => b.map(move |y| (x, y)).left_future(),"] # [doc = "             Either::Right((y, a)) => a.map(move |x| (x, y)).right_future(),"] # [doc = "         }"] # [doc = "     })"] # [doc = " }"] # [doc = " ```"] pub fn select < A , B > (future1 : A , future2 : B) -> Select < A , B > where A : Future + Unpin , B : Future + Unpin , { assert_future :: < Either < (A :: Output , B) , (B :: Output , A) > , _ > (Select { inner : Some ((future1 , future2)) , }) }
};
}
