// Generated macro for try_select (function)
macro_rules! Depcrate_future_try_selecttry_select {
() => {
// Module: crate::future::try_select
// Provides: {"try_select"}
// Dependencies: {}
# [doc = " Waits for either one of two differently-typed futures to complete."] # [doc = ""] # [doc = " This function will return a new future which awaits for either one of both"] # [doc = " futures to complete. The returned future will finish with both the value"] # [doc = " resolved and a future representing the completion of the other work."] # [doc = ""] # [doc = " Note that this function consumes the receiving futures and returns a"] # [doc = " wrapped version of them."] # [doc = ""] # [doc = " Also note that if both this and the second future have the same"] # [doc = " success/error type you can use the `Either::factor_first` method to"] # [doc = " conveniently extract out the value at the end."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::future::{self, Either, Future, FutureExt, TryFuture, TryFutureExt};"] # [doc = ""] # [doc = " // A poor-man's try_join implemented on top of select"] # [doc = ""] # [doc = " fn try_join<A, B, E>(a: A, b: B) -> impl TryFuture<Ok=(A::Ok, B::Ok), Error=E>"] # [doc = "      where A: TryFuture<Error = E> + Unpin + 'static,"] # [doc = "            B: TryFuture<Error = E> + Unpin + 'static,"] # [doc = "            E: 'static,"] # [doc = " {"] # [doc = "     future::try_select(a, b).then(|res| -> Box<dyn Future<Output = Result<_, _>> + Unpin> {"] # [doc = "         match res {"] # [doc = "             Ok(Either::Left((x, b))) => Box::new(b.map_ok(move |y| (x, y))),"] # [doc = "             Ok(Either::Right((y, a))) => Box::new(a.map_ok(move |x| (x, y))),"] # [doc = "             Err(Either::Left((e, _))) => Box::new(future::err(e)),"] # [doc = "             Err(Either::Right((e, _))) => Box::new(future::err(e)),"] # [doc = "         }"] # [doc = "     })"] # [doc = " }"] # [doc = " ```"] pub fn try_select < A , B > (future1 : A , future2 : B) -> TrySelect < A , B > where A : TryFuture + Unpin , B : TryFuture + Unpin , { super :: assert_future :: < Result < EitherOk < A , B > , EitherErr < A , B > > , _ > (TrySelect { inner : Some ((future1 , future2)) , }) }
};
}
