// Generated macro for join_all (function)
macro_rules! Depcrate_future_join_alljoin_all {
() => {
// Module: crate::future::join_all
// Provides: {"join_all"}
// Dependencies: {}
# [doc = " Creates a future which represents a collection of the outputs of the futures"] # [doc = " given."] # [doc = ""] # [doc = " The returned future will drive execution for all of its underlying futures,"] # [doc = " collecting the results into a destination `Vec<T>` in the same order as they"] # [doc = " were provided."] # [doc = ""] # [doc = " This function is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # See Also"] # [doc = ""] # [doc = " `join_all` will switch to the more powerful [`FuturesOrdered`] for performance"] # [doc = " reasons if the number of futures is large. You may want to look into using it or"] # [doc = " its counterpart [`FuturesUnordered`][crate::stream::FuturesUnordered] directly."] # [doc = ""] # [doc = " Some examples for additional functionality provided by these are:"] # [doc = ""] # [doc = "  * Adding new futures to the set even after it has been started."] # [doc = ""] # [doc = "  * Only polling the specific futures that have been woken. In cases where"] # [doc = "    you have a lot of futures this will result in much more efficient polling."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::join_all;"] # [doc = ""] # [doc = " async fn foo(i: u32) -> u32 { i }"] # [doc = ""] # [doc = " let futures = vec![foo(1), foo(2), foo(3)];"] # [doc = ""] # [doc = " assert_eq!(join_all(futures).await, [1, 2, 3]);"] # [doc = " # });"] # [doc = " ```"] pub fn join_all < I > (iter : I) -> JoinAll < I :: Item > where I : IntoIterator , I :: Item : Future , { let iter = iter . into_iter () ; # [cfg (target_os = "none")] # [cfg_attr (target_os = "none" , cfg (not (target_has_atomic = "ptr")))] { let kind = JoinAllKind :: Small { elems : iter . map (MaybeDone :: Future) . collect :: < Box < [_] > > () . into () } ; assert_future :: < Vec < < I :: Item as Future > :: Output > , _ > (JoinAll { kind }) } # [cfg_attr (target_os = "none" , cfg (target_has_atomic = "ptr"))] { let kind = match iter . size_hint () . 1 { Some (max) if max <= SMALL => JoinAllKind :: Small { elems : iter . map (MaybeDone :: Future) . collect :: < Box < [_] > > () . into () , } , _ => JoinAllKind :: Big { fut : iter . collect :: < FuturesOrdered < _ > > () . collect () } , } ; assert_future :: < Vec < < I :: Item as Future > :: Output > , _ > (JoinAll { kind }) } }
};
}
