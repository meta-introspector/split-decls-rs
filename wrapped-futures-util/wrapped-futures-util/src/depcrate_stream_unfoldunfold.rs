// Generated macro for unfold (function)
macro_rules! Depcrate_stream_unfoldunfold {
() => {
// Module: crate::stream::unfold
// Provides: {"unfold"}
// Dependencies: {}
# [doc = " Creates a `Stream` from a seed and a closure returning a `Future`."] # [doc = ""] # [doc = " This function is the dual for the `Stream::fold()` adapter: while"] # [doc = " `Stream::fold()` reduces a `Stream` to one single value, `unfold()` creates a"] # [doc = " `Stream` from a seed value."] # [doc = ""] # [doc = " `unfold()` will call the provided closure with the provided seed, then wait"] # [doc = " for the returned `Future` to complete with `(a, b)`. It will then yield the"] # [doc = " value `a`, and use `b` as the next internal state."] # [doc = ""] # [doc = " If the closure returns `None` instead of `Some(Future)`, then the `unfold()`"] # [doc = " will stop producing items and return `Poll::Ready(None)` in future"] # [doc = " calls to `poll()`."] # [doc = ""] # [doc = " This function can typically be used when wanting to go from the \"world of"] # [doc = " futures\" to the \"world of streams\": the provided closure can build a"] # [doc = " `Future` using other library functions working on futures, and `unfold()`"] # [doc = " will turn it into a `Stream` by repeating the operation."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::unfold(0, |state| async move {"] # [doc = "     if state <= 2 {"] # [doc = "         let next_state = state + 1;"] # [doc = "         let yielded = state * 2;"] # [doc = "         Some((yielded, next_state))"] # [doc = "     } else {"] # [doc = "         None"] # [doc = "     }"] # [doc = " });"] # [doc = ""] # [doc = " let result = stream.collect::<Vec<i32>>().await;"] # [doc = " assert_eq!(result, vec![0, 2, 4]);"] # [doc = " # });"] # [doc = " ```"] pub fn unfold < T , F , Fut , Item > (init : T , f : F) -> Unfold < T , F , Fut > where F : FnMut (T) -> Fut , Fut : Future < Output = Option < (Item , T) > > , { assert_stream :: < Item , _ > (Unfold { f , state : UnfoldState :: Value { value : init } }) }
};
}
