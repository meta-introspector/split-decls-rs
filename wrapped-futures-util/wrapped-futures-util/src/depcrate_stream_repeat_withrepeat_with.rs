// Generated macro for repeat_with (function)
macro_rules! Depcrate_stream_repeat_withrepeat_with {
() => {
// Module: crate::stream::repeat_with
// Provides: {"repeat_with"}
// Dependencies: {}
# [doc = " Creates a new stream that repeats elements of type `A` endlessly by"] # [doc = " applying the provided closure, the repeater, `F: FnMut() -> A`."] # [doc = ""] # [doc = " The `repeat_with()` function calls the repeater over and over again."] # [doc = ""] # [doc = " Infinite stream like `repeat_with()` are often used with adapters like"] # [doc = " [`stream.take()`], in order to make them finite."] # [doc = ""] # [doc = " If the element type of the stream you need implements [`Clone`], and"] # [doc = " it is OK to keep the source element in memory, you should instead use"] # [doc = " the [`stream.repeat()`] function."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " // let's assume we have some value of a type that is not `Clone`"] # [doc = " // or which don't want to have in memory just yet because it is expensive:"] # [doc = " #[derive(PartialEq, Debug)]"] # [doc = " struct Expensive;"] # [doc = ""] # [doc = " // a particular value forever:"] # [doc = " let mut things = stream::repeat_with(|| Expensive);"] # [doc = ""] # [doc = " assert_eq!(Some(Expensive), things.next().await);"] # [doc = " assert_eq!(Some(Expensive), things.next().await);"] # [doc = " assert_eq!(Some(Expensive), things.next().await);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " Using mutation and going finite:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " // From the zeroth to the third power of two:"] # [doc = " let mut curr = 1;"] # [doc = " let mut pow2 = stream::repeat_with(|| { let tmp = curr; curr *= 2; tmp })"] # [doc = "                     .take(4);"] # [doc = ""] # [doc = " assert_eq!(Some(1), pow2.next().await);"] # [doc = " assert_eq!(Some(2), pow2.next().await);"] # [doc = " assert_eq!(Some(4), pow2.next().await);"] # [doc = " assert_eq!(Some(8), pow2.next().await);"] # [doc = ""] # [doc = " // ... and now we're done"] # [doc = " assert_eq!(None, pow2.next().await);"] # [doc = " # });"] # [doc = " ```"] pub fn repeat_with < A , F : FnMut () -> A > (repeater : F) -> RepeatWith < F > { assert_stream :: < A , _ > (RepeatWith { repeater }) }
};
}
