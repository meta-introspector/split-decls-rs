// Generated macro for AsyncWriteExt (trait)
macro_rules! Depcrate_ioAsyncWriteExt {
() => {
// Module: crate::io
// Provides: {"AsyncWriteExt"}
// Dependencies: {}
# [doc = " An extension trait which adds utility methods to `AsyncWrite` types."] pub trait AsyncWriteExt : AsyncWrite { # [doc = " Creates a future which will entirely flush this `AsyncWrite`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{AllowStdIo, AsyncWriteExt};"] # [doc = " use std::io::{BufWriter, Cursor};"] # [doc = ""] # [doc = " let mut output = vec![0u8; 5];"] # [doc = ""] # [doc = " {"] # [doc = "     let writer = Cursor::new(&mut output);"] # [doc = "     let mut buffered = AllowStdIo::new(BufWriter::new(writer));"] # [doc = "     buffered.write_all(&[1, 2]).await?;"] # [doc = "     buffered.write_all(&[3, 4]).await?;"] # [doc = "     buffered.flush().await?;"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(output, [1, 2, 3, 4, 0]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn flush (& mut self) -> Flush < '_ , Self > where Self : Unpin , { assert_future :: < Result < () > , _ > (Flush :: new (self)) } # [doc = " Creates a future which will entirely close this `AsyncWrite`."] fn close (& mut self) -> Close < '_ , Self > where Self : Unpin , { assert_future :: < Result < () > , _ > (Close :: new (self)) } # [doc = " Creates a future which will write bytes from `buf` into the object."] # [doc = ""] # [doc = " The returned future will resolve to the number of bytes written once the write"] # [doc = " operation is completed."] fn write < 'a > (& 'a mut self , buf : & 'a [u8]) -> Write < 'a , Self > where Self : Unpin , { assert_future :: < Result < usize > , _ > (Write :: new (self , buf)) } # [doc = " Creates a future which will write bytes from `bufs` into the object using vectored"] # [doc = " IO operations."] # [doc = ""] # [doc = " The returned future will resolve to the number of bytes written once the write"] # [doc = " operation is completed."] fn write_vectored < 'a , 'b > (& 'a mut self , bufs : & 'a [IoSlice < 'b >]) -> WriteVectored < 'a , 'b , Self > where Self : Unpin , { assert_future :: < Result < usize > , _ > (WriteVectored :: new (self , bufs)) } # [doc = " Write data into this object."] # [doc = ""] # [doc = " Creates a future that will write the entire contents of the buffer `buf` into"] # [doc = " this `AsyncWrite`."] # [doc = ""] # [doc = " The returned future will not complete until all the data has been written."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{AsyncWriteExt, Cursor};"] # [doc = ""] # [doc = " let mut writer = Cursor::new(vec![0u8; 5]);"] # [doc = ""] # [doc = " writer.write_all(&[1, 2, 3, 4]).await?;"] # [doc = ""] # [doc = " assert_eq!(writer.into_inner(), [1, 2, 3, 4, 0]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn write_all < 'a > (& 'a mut self , buf : & 'a [u8]) -> WriteAll < 'a , Self > where Self : Unpin , { assert_future :: < Result < () > , _ > (WriteAll :: new (self , buf)) } # [doc = " Attempts to write multiple buffers into this writer."] # [doc = ""] # [doc = " Creates a future that will write the entire contents of `bufs` into this"] # [doc = " `AsyncWrite` using [vectored writes]."] # [doc = ""] # [doc = " The returned future will not complete until all the data has been"] # [doc = " written."] # [doc = ""] # [doc = " [vectored writes]: std::io::Write::write_vectored"] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " Unlike `io::Write::write_vectored`, this takes a *mutable* reference to"] # [doc = " a slice of `IoSlice`s, not an immutable one. That's because we need to"] # [doc = " modify the slice to keep track of the bytes already written."] # [doc = ""] # [doc = " Once this futures returns, the contents of `bufs` are unspecified, as"] # [doc = " this depends on how many calls to `write_vectored` were necessary. It is"] # [doc = " best to understand this function as taking ownership of `bufs` and to"] # [doc = " not use `bufs` afterwards. The underlying buffers, to which the"] # [doc = " `IoSlice`s point (but not the `IoSlice`s themselves), are unchanged and"] # [doc = " can be reused."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::AsyncWriteExt;"] # [doc = " use futures_util::io::Cursor;"] # [doc = " use std::io::IoSlice;"] # [doc = ""] # [doc = " let mut writer = Cursor::new(Vec::new());"] # [doc = " let bufs = &mut ["] # [doc = "     IoSlice::new(&[1]),"] # [doc = "     IoSlice::new(&[2, 3]),"] # [doc = "     IoSlice::new(&[4, 5, 6]),"] # [doc = " ];"] # [doc = ""] # [doc = " writer.write_all_vectored(bufs).await?;"] # [doc = " // Note: the contents of `bufs` is now unspecified, see the Notes section."] # [doc = ""] # [doc = " assert_eq!(writer.into_inner(), &[1, 2, 3, 4, 5, 6]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] # [cfg (feature = "write-all-vectored")] fn write_all_vectored < 'a , 'b > (& 'a mut self , bufs : & 'a mut [IoSlice < 'b >] ,) -> WriteAllVectored < 'a , 'b , Self > where Self : Unpin , { assert_future :: < Result < () > , _ > (WriteAllVectored :: new (self , bufs)) } # [doc = " Wraps an [`AsyncWrite`] in a compatibility wrapper that allows it to be"] # [doc = " used as a futures 0.1 / tokio-io 0.1 `AsyncWrite`."] # [doc = " Requires the `io-compat` feature to enable."] # [cfg (feature = "io-compat")] # [cfg_attr (docsrs , doc (cfg (feature = "io-compat")))] fn compat_write (self) -> Compat < Self > where Self : Sized + Unpin , { Compat :: new (self) } # [doc = " Allow using an [`AsyncWrite`] as a [`Sink`](futures_sink::Sink)`<Item: AsRef<[u8]>>`."] # [doc = ""] # [doc = " This adapter produces a sink that will write each value passed to it"] # [doc = " into the underlying writer."] # [doc = ""] # [doc = " Note that this function consumes the given writer, returning a wrapped"] # [doc = " version."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::AsyncWriteExt;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(vec![[1, 2, 3], [4, 5, 6]]);"] # [doc = ""] # [doc = " let mut writer = vec![];"] # [doc = ""] # [doc = " stream.forward((&mut writer).into_sink()).await?;"] # [doc = ""] # [doc = " assert_eq!(writer, vec![1, 2, 3, 4, 5, 6]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " # })?;"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "sink")] # [cfg_attr (docsrs , doc (cfg (feature = "sink")))] fn into_sink < Item : AsRef < [u8] > > (self) -> IntoSink < Self , Item > where Self : Sized , { crate :: sink :: assert_sink :: < Item , Error , _ > (IntoSink :: new (self)) } }
};
}
