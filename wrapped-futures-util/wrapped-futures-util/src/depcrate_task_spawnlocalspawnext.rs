// Generated macro for LocalSpawnExt (trait)
macro_rules! Depcrate_task_spawnLocalSpawnExt {
() => {
// Module: crate::task::spawn
// Provides: {"LocalSpawnExt"}
// Dependencies: {}
# [doc = " Extension trait for `LocalSpawn`."] pub trait LocalSpawnExt : LocalSpawn { # [doc = " Spawns a task that polls the given future with output `()` to"] # [doc = " completion."] # [doc = ""] # [doc = " This method returns a [`Result`] that contains a [`SpawnError`] if"] # [doc = " spawning fails."] # [doc = ""] # [doc = " You can use [`spawn_with_handle`](SpawnExt::spawn_with_handle) if"] # [doc = " you want to spawn a future with output other than `()` or if you want"] # [doc = " to be able to await its completion."] # [doc = ""] # [doc = " Note this method will eventually be replaced with the upcoming"] # [doc = " `Spawn::spawn` method which will take a `dyn Future` as input."] # [doc = " Technical limitations prevent `Spawn::spawn` from being implemented"] # [doc = " today. Feel free to use this method in the meantime."] # [doc = ""] # [doc = " ```"] # [doc = " use futures::executor::LocalPool;"] # [doc = " use futures::task::LocalSpawnExt;"] # [doc = ""] # [doc = " let executor = LocalPool::new();"] # [doc = " let spawner = executor.spawner();"] # [doc = ""] # [doc = " let future = async { /* ... */ };"] # [doc = " spawner.spawn_local(future).unwrap();"] # [doc = " ```"] # [cfg (feature = "alloc")] fn spawn_local < Fut > (& self , future : Fut) -> Result < () , SpawnError > where Fut : Future < Output = () > + 'static , { self . spawn_local_obj (LocalFutureObj :: new (Box :: new (future))) } # [doc = " Spawns a task that polls the given future to completion and returns a"] # [doc = " future that resolves to the spawned future's output."] # [doc = ""] # [doc = " This method returns a [`Result`] that contains a [`RemoteHandle`](crate::future::RemoteHandle), or, if"] # [doc = " spawning fails, a [`SpawnError`]. [`RemoteHandle`](crate::future::RemoteHandle) is a future that"] # [doc = " resolves to the output of the spawned future."] # [doc = ""] # [doc = " ```"] # [doc = " use futures::executor::LocalPool;"] # [doc = " use futures::task::LocalSpawnExt;"] # [doc = ""] # [doc = " let mut executor = LocalPool::new();"] # [doc = " let spawner = executor.spawner();"] # [doc = ""] # [doc = " let future = async { 1 };"] # [doc = " let join_handle_fut = spawner.spawn_local_with_handle(future).unwrap();"] # [doc = " assert_eq!(executor.run_until(join_handle_fut), 1);"] # [doc = " ```"] # [cfg (feature = "channel")] # [cfg_attr (docsrs , doc (cfg (feature = "channel")))] # [cfg (feature = "std")] fn spawn_local_with_handle < Fut > (& self , future : Fut ,) -> Result < RemoteHandle < Fut :: Output > , SpawnError > where Fut : Future + 'static , { let (future , handle) = future . remote_handle () ; self . spawn_local (future) ? ; Ok (handle) } }
};
}
