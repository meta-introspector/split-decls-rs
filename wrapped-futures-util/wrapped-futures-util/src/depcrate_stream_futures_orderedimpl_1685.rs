// Generated macro for impl_1685 (impl)
macro_rules! Depcrate_stream_futures_orderedimpl_1685 {
() => {
// Module: crate::stream::futures_ordered
// Provides: {"impl_1685"}
// Dependencies: {}
impl < Fut : Future > FuturesOrdered < Fut > { # [doc = " Constructs a new, empty `FuturesOrdered`"] # [doc = ""] # [doc = " The returned [`FuturesOrdered`] does not contain any futures and, in"] # [doc = " this state, [`FuturesOrdered::poll_next`] will return"] # [doc = " [`Poll::Ready(None)`](Poll::Ready)."] pub fn new () -> Self { Self { in_progress_queue : FuturesUnordered :: new () , queued_outputs : BinaryHeap :: new () , next_incoming_index : 0 , next_outgoing_index : 0 , } } # [doc = " Returns the number of futures contained in the queue."] # [doc = ""] # [doc = " This represents the total number of in-flight futures, both"] # [doc = " those currently processing and those that have completed but"] # [doc = " which are waiting for earlier futures to complete."] pub fn len (& self) -> usize { self . in_progress_queue . len () + self . queued_outputs . len () } # [doc = " Returns `true` if the queue contains no futures"] pub fn is_empty (& self) -> bool { self . in_progress_queue . is_empty () && self . queued_outputs . is_empty () } # [doc = " Push a future into the queue."] # [doc = ""] # [doc = " This function submits the given future to the internal set for managing."] # [doc = " This function will not call [`poll`](Future::poll) on the submitted"] # [doc = " future. The caller must ensure that [`FuturesOrdered::poll_next`] is"] # [doc = " called in order to receive task notifications."] # [deprecated (note = "use `push_back` instead")] pub fn push (& mut self , future : Fut) { self . push_back (future) ; } # [doc = " Pushes a future to the back of the queue."] # [doc = ""] # [doc = " This function submits the given future to the internal set for managing."] # [doc = " This function will not call [`poll`](Future::poll) on the submitted"] # [doc = " future. The caller must ensure that [`FuturesOrdered::poll_next`] is"] # [doc = " called in order to receive task notifications."] pub fn push_back (& mut self , future : Fut) { let wrapped = OrderWrapper { data : future , index : self . next_incoming_index } ; self . next_incoming_index += 1 ; self . in_progress_queue . push (wrapped) ; } # [doc = " Pushes a future to the front of the queue."] # [doc = ""] # [doc = " This function submits the given future to the internal set for managing."] # [doc = " This function will not call [`poll`](Future::poll) on the submitted"] # [doc = " future. The caller must ensure that [`FuturesOrdered::poll_next`] is"] # [doc = " called in order to receive task notifications. This future will be"] # [doc = " the next future to be returned complete."] pub fn push_front (& mut self , future : Fut) { let wrapped = OrderWrapper { data : future , index : self . next_outgoing_index - 1 } ; self . next_outgoing_index -= 1 ; self . in_progress_queue . push (wrapped) ; } # [doc = " Clear the whole `FuturesOrdered` queue."] # [doc = ""] # [doc = " This function clears the pending futures and the queued outputs"] # [doc = " to make it fully empty."] pub fn clear (& mut self) { self . in_progress_queue . clear () ; self . queued_outputs . clear () ; self . next_incoming_index = 0 ; self . next_outgoing_index = 0 ; } }
};
}
