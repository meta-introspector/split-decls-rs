macro_rules! deps {
    () => {
        NextIfEqFn!();
        Ready!();
    };
}

macro_rules! impl_401 {
    () => {
        deps!();
        impl < St : Stream > Peekable < St > { pub (super) fn new (stream : St) -> Self { Self { stream : stream . fuse () , peeked : None } } delegate_access_inner ! (stream , St , (.)) ; # [doc = " Produces a future which retrieves a reference to the next item"] # [doc = " in the stream, or `None` if the underlying stream terminates."] pub fn peek (self : Pin < & mut Self >) -> Peek < '_ , St > { Peek { inner : Some (self) } } # [doc = " Peek retrieves a reference to the next item in the stream."] # [doc = ""] # [doc = " This method polls the underlying stream and return either a reference"] # [doc = " to the next item if the stream is ready or passes through any errors."] pub fn poll_peek (self : Pin < & mut Self > , cx : & mut Context < '_ >) -> Poll < Option < & St :: Item > > { let mut this = self . project () ; Poll :: Ready (loop { if this . peeked . is_some () { break this . peeked . as_ref () ; } else if let Some (item) = ready ! (this . stream . as_mut () . poll_next (cx)) { * this . peeked = Some (item) ; } else { break None ; } }) } # [doc = " Produces a future which retrieves a mutable reference to the next item"] # [doc = " in the stream, or `None` if the underlying stream terminates."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use core::pin::pin;"] # [doc = ""] # [doc = " use futures::stream;"] # [doc = " use futures::stream::StreamExt;"] # [doc = ""] # [doc = " let stream = stream::iter(vec![1, 2, 3]).peekable();"] # [doc = " let mut stream = pin!(stream);"] # [doc = ""] # [doc = " assert_eq!(stream.as_mut().peek_mut().await, Some(&mut 1));"] # [doc = " assert_eq!(stream.as_mut().next().await, Some(1));"] # [doc = ""] # [doc = " // Peek into the stream and modify the value which will be returned next"] # [doc = " if let Some(p) = stream.as_mut().peek_mut().await {"] # [doc = "     if *p == 2 {"] # [doc = "         *p = 5;"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(stream.collect::<Vec<_>>().await, vec![5, 3]);"] # [doc = " # });"] # [doc = " ```"] pub fn peek_mut (self : Pin < & mut Self >) -> PeekMut < '_ , St > { PeekMut { inner : Some (self) } } # [doc = " Peek retrieves a mutable reference to the next item in the stream."] pub fn poll_peek_mut (self : Pin < & mut Self > , cx : & mut Context < '_ > ,) -> Poll < Option < & mut St :: Item > > { let mut this = self . project () ; Poll :: Ready (loop { if this . peeked . is_some () { break this . peeked . as_mut () ; } else if let Some (item) = ready ! (this . stream . as_mut () . poll_next (cx)) { * this . peeked = Some (item) ; } else { break None ; } }) } # [doc = " Creates a future which will consume and return the next value of this"] # [doc = " stream if a condition is true."] # [doc = ""] # [doc = " If `func` returns `true` for the next value of this stream, consume and"] # [doc = " return it. Otherwise, return `None`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Consume a number if it's equal to 0."] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use core::pin::pin;"] # [doc = ""] # [doc = " use futures::stream;"] # [doc = " use futures::stream::StreamExt;"] # [doc = ""] # [doc = " let stream = stream::iter(0..5).peekable();"] # [doc = " let mut stream = pin!(stream);"] # [doc = " // The first item of the stream is 0; consume it."] # [doc = " assert_eq!(stream.as_mut().next_if(|&x| x == 0).await, Some(0));"] # [doc = " // The next item returned is now 1, so `consume` will return `false`."] # [doc = " assert_eq!(stream.as_mut().next_if(|&x| x == 0).await, None);"] # [doc = " // `next_if` saves the value of the next item if it was not equal to `expected`."] # [doc = " assert_eq!(stream.next().await, Some(1));"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " Consume any number less than 10."] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use core::pin::pin;"] # [doc = ""] # [doc = " use futures::stream;"] # [doc = " use futures::stream::StreamExt;"] # [doc = ""] # [doc = " let stream = stream::iter(1..20).peekable();"] # [doc = " let mut stream = pin!(stream);"] # [doc = " // Consume all numbers less than 10"] # [doc = " while stream.as_mut().next_if(|&x| x < 10).await.is_some() {}"] # [doc = " // The next value returned will be 10"] # [doc = " assert_eq!(stream.next().await, Some(10));"] # [doc = " # });"] # [doc = " ```"] pub fn next_if < F > (self : Pin < & mut Self > , func : F) -> NextIf < '_ , St , F > where F : FnOnce (& St :: Item) -> bool , { NextIf { inner : Some ((self , func)) } } # [doc = " Creates a future which will consume and return the next item if it is"] # [doc = " equal to `expected`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Consume a number if it's equal to 0."] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use core::pin::pin;"] # [doc = ""] # [doc = " use futures::stream;"] # [doc = " use futures::stream::StreamExt;"] # [doc = ""] # [doc = " let stream = stream::iter(0..5).peekable();"] # [doc = " let mut stream = pin!(stream);"] # [doc = " // The first item of the stream is 0; consume it."] # [doc = " assert_eq!(stream.as_mut().next_if_eq(&0).await, Some(0));"] # [doc = " // The next item returned is now 1, so `consume` will return `false`."] # [doc = " assert_eq!(stream.as_mut().next_if_eq(&0).await, None);"] # [doc = " // `next_if_eq` saves the value of the next item if it was not equal to `expected`."] # [doc = " assert_eq!(stream.next().await, Some(1));"] # [doc = " # });"] # [doc = " ```"] pub fn next_if_eq < 'a , T > (self : Pin < & 'a mut Self > , expected : & 'a T) -> NextIfEq < 'a , St , T > where T : ? Sized , St :: Item : PartialEq < T > , { NextIfEq { inner : NextIf { inner : Some ((self , NextIfEqFn { expected , _next : PhantomData })) } , } } }
    };
}

impl_401!()