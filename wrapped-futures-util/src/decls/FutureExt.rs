macro_rules! deps {
    () => {
        Ready!();
        Either!();
        Send!();
        StreamExt!();
        Shared!();
        RemoteHandle!();
    };
}

macro_rules! FutureExt {
    () => {
        deps!();
        # [doc = " An extension trait for `Future`s that provides a variety of convenient"] # [doc = " adapters."] pub trait FutureExt : Future { # [doc = " Map this future's output to a different type, returning a new future of"] # [doc = " the resulting type."] # [doc = ""] # [doc = " This function is similar to the `Option::map` or `Iterator::map` where"] # [doc = " it will change the type of the underlying future. This is useful to"] # [doc = " chain along a computation once a future has been resolved."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it, similar to the existing `map` methods in the"] # [doc = " standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::FutureExt;"] # [doc = ""] # [doc = " let future = async { 1 };"] # [doc = " let new_future = future.map(|x| x + 3);"] # [doc = " assert_eq!(new_future.await, 4);"] # [doc = " # });"] # [doc = " ```"] fn map < U , F > (self , f : F) -> Map < Self , F > where F : FnOnce (Self :: Output) -> U , Self : Sized , { assert_future :: < U , _ > (Map :: new (self , f)) } # [doc = " Map this future's output to a different type, returning a new future of"] # [doc = " the resulting type."] # [doc = ""] # [doc = " This function is equivalent to calling `map(Into::into)` but allows naming"] # [doc = " the return type."] fn map_into < U > (self) -> MapInto < Self , U > where Self :: Output : Into < U > , Self : Sized , { assert_future :: < U , _ > (MapInto :: new (self)) } # [doc = " Chain on a computation for when a future finished, passing the result of"] # [doc = " the future to the provided closure `f`."] # [doc = ""] # [doc = " The returned value of the closure must implement the `Future` trait"] # [doc = " and can represent some more work to be done before the composed future"] # [doc = " is finished."] # [doc = ""] # [doc = " The closure `f` is only run *after* successful completion of the `self`"] # [doc = " future."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::FutureExt;"] # [doc = ""] # [doc = " let future_of_1 = async { 1 };"] # [doc = " let future_of_4 = future_of_1.then(|x| async move { x + 3 });"] # [doc = " assert_eq!(future_of_4.await, 4);"] # [doc = " # });"] # [doc = " ```"] fn then < Fut , F > (self , f : F) -> Then < Self , Fut , F > where F : FnOnce (Self :: Output) -> Fut , Fut : Future , Self : Sized , { assert_future :: < Fut :: Output , _ > (Then :: new (self , f)) } # [doc = " Wrap this future in an `Either` future, making it the left-hand variant"] # [doc = " of that `Either`."] # [doc = ""] # [doc = " This can be used in combination with the `right_future` method to write `if`"] # [doc = " statements that evaluate to different futures in different branches."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::FutureExt;"] # [doc = ""] # [doc = " let x = 6;"] # [doc = " let future = if x < 10 {"] # [doc = "     async { true }.left_future()"] # [doc = " } else {"] # [doc = "     async { false }.right_future()"] # [doc = " };"] # [doc = ""] # [doc = " assert_eq!(future.await, true);"] # [doc = " # });"] # [doc = " ```"] fn left_future < B > (self) -> Either < Self , B > where B : Future < Output = Self :: Output > , Self : Sized , { assert_future :: < Self :: Output , _ > (Either :: Left (self)) } # [doc = " Wrap this future in an `Either` future, making it the right-hand variant"] # [doc = " of that `Either`."] # [doc = ""] # [doc = " This can be used in combination with the `left_future` method to write `if`"] # [doc = " statements that evaluate to different futures in different branches."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::FutureExt;"] # [doc = ""] # [doc = " let x = 6;"] # [doc = " let future = if x > 10 {"] # [doc = "     async { true }.left_future()"] # [doc = " } else {"] # [doc = "     async { false }.right_future()"] # [doc = " };"] # [doc = ""] # [doc = " assert_eq!(future.await, false);"] # [doc = " # });"] # [doc = " ```"] fn right_future < A > (self) -> Either < A , Self > where A : Future < Output = Self :: Output > , Self : Sized , { assert_future :: < Self :: Output , _ > (Either :: Right (self)) } # [doc = " Convert this future into a single element stream."] # [doc = ""] # [doc = " The returned stream contains single success if this future resolves to"] # [doc = " success or single error if this future resolves into error."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::FutureExt;"] # [doc = " use futures::stream::StreamExt;"] # [doc = ""] # [doc = " let future = async { 17 };"] # [doc = " let stream = future.into_stream();"] # [doc = " let collected: Vec<_> = stream.collect().await;"] # [doc = " assert_eq!(collected, vec![17]);"] # [doc = " # });"] # [doc = " ```"] fn into_stream (self) -> IntoStream < Self > where Self : Sized , { assert_stream :: < Self :: Output , _ > (IntoStream :: new (self)) } # [doc = " Flatten the execution of this future when the output of this"] # [doc = " future is itself another future."] # [doc = ""] # [doc = " This can be useful when combining futures together to flatten the"] # [doc = " computation out the final result."] # [doc = ""] # [doc = " This method is roughly equivalent to `self.then(|x| x)`."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::FutureExt;"] # [doc = ""] # [doc = " let nested_future = async { async { 1 } };"] # [doc = " let future = nested_future.flatten();"] # [doc = " assert_eq!(future.await, 1);"] # [doc = " # });"] # [doc = " ```"] fn flatten (self) -> Flatten < Self > where Self :: Output : Future , Self : Sized , { let f = Flatten :: new (self) ; assert_future :: < < < Self as Future > :: Output as Future > :: Output , _ > (f) } # [doc = " Flatten the execution of this future when the successful result of this"] # [doc = " future is a stream."] # [doc = ""] # [doc = " This can be useful when stream initialization is deferred, and it is"] # [doc = " convenient to work with that stream as if stream was available at the"] # [doc = " call site."] # [doc = ""] # [doc = " Note that this function consumes this future and returns a wrapped"] # [doc = " version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::FutureExt;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream_items = vec![17, 18, 19];"] # [doc = " let future_of_a_stream = async { stream::iter(stream_items) };"] # [doc = ""] # [doc = " let stream = future_of_a_stream.flatten_stream();"] # [doc = " let list: Vec<_> = stream.collect().await;"] # [doc = " assert_eq!(list, vec![17, 18, 19]);"] # [doc = " # });"] # [doc = " ```"] fn flatten_stream (self) -> FlattenStream < Self > where Self :: Output : Stream , Self : Sized , { assert_stream :: < < Self :: Output as Stream > :: Item , _ > (FlattenStream :: new (self)) } # [doc = " Fuse a future such that `poll` will never again be called once it has"] # [doc = " completed. This method can be used to turn any `Future` into a"] # [doc = " `FusedFuture`."] # [doc = ""] # [doc = " Normally, once a future has returned `Poll::Ready` from `poll`,"] # [doc = " any further calls could exhibit bad behavior such as blocking"] # [doc = " forever, panicking, never returning, etc. If it is known that `poll`"] # [doc = " may be called too often then this method can be used to ensure that it"] # [doc = " has defined semantics."] # [doc = ""] # [doc = " If a `fuse`d future is `poll`ed after having returned `Poll::Ready`"] # [doc = " previously, it will return `Poll::Pending`, from `poll` again (and will"] # [doc = " continue to do so for all future calls to `poll`)."] # [doc = ""] # [doc = " This combinator will drop the underlying future as soon as it has been"] # [doc = " completed to ensure resources are reclaimed as soon as possible."] fn fuse (self) -> Fuse < Self > where Self : Sized , { let f = Fuse :: new (self) ; assert_future :: < Self :: Output , _ > (f) } # [doc = " Do something with the output of a future before passing it on."] # [doc = ""] # [doc = " When using futures, you'll often chain several of them together.  While"] # [doc = " working on such code, you might want to check out what's happening at"] # [doc = " various parts in the pipeline, without consuming the intermediate"] # [doc = " value. To do that, insert a call to `inspect`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::FutureExt;"] # [doc = ""] # [doc = " let future = async { 1 };"] # [doc = " let new_future = future.inspect(|&x| println!(\"about to resolve: {}\", x));"] # [doc = " assert_eq!(new_future.await, 1);"] # [doc = " # });"] # [doc = " ```"] fn inspect < F > (self , f : F) -> Inspect < Self , F > where F : FnOnce (& Self :: Output) , Self : Sized , { assert_future :: < Self :: Output , _ > (Inspect :: new (self , f)) } # [doc = " Catches unwinding panics while polling the future."] # [doc = ""] # [doc = " In general, panics within a future can propagate all the way out to the"] # [doc = " task level. This combinator makes it possible to halt unwinding within"] # [doc = " the future itself. It's most commonly used within task executors. It's"] # [doc = " not recommended to use this for error handling."] # [doc = ""] # [doc = " Note that this method requires the `UnwindSafe` bound from the standard"] # [doc = " library. This isn't always applied automatically, and the standard"] # [doc = " library provides an `AssertUnwindSafe` wrapper type to apply it"] # [doc = " after-the fact. To assist using this method, the `Future` trait is also"] # [doc = " implemented for `AssertUnwindSafe<F>` where `F` implements `Future`."] # [doc = ""] # [doc = " This method is only available when the `std` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::{self, FutureExt, Ready};"] # [doc = ""] # [doc = " let future = future::ready(2);"] # [doc = " assert!(future.catch_unwind().await.is_ok());"] # [doc = ""] # [doc = " let future = future::lazy(|_| -> Ready<i32> {"] # [doc = "     unimplemented!()"] # [doc = " });"] # [doc = " assert!(future.catch_unwind().await.is_err());"] # [doc = " # });"] # [doc = " ```"] # [cfg (feature = "std")] fn catch_unwind (self) -> CatchUnwind < Self > where Self : Sized + :: std :: panic :: UnwindSafe , { assert_future :: < Result < Self :: Output , Box < dyn std :: any :: Any + Send > > , _ > (CatchUnwind :: new (self ,)) } # [doc = " Create a cloneable handle to this future where all handles will resolve"] # [doc = " to the same result."] # [doc = ""] # [doc = " The `shared` combinator method provides a method to convert any future"] # [doc = " into a cloneable future. It enables a future to be polled by multiple"] # [doc = " threads."] # [doc = ""] # [doc = " This method is only available when the `std` or 'spin' feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::FutureExt;"] # [doc = ""] # [doc = " let future = async { 6 };"] # [doc = " let shared1 = future.shared();"] # [doc = " let shared2 = shared1.clone();"] # [doc = ""] # [doc = " assert_eq!(6, shared1.await);"] # [doc = " assert_eq!(6, shared2.await);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future::FutureExt;"] # [doc = " use futures::executor::block_on;"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let future = async { 6 };"] # [doc = " let shared1 = future.shared();"] # [doc = " let shared2 = shared1.clone();"] # [doc = " let join_handle = thread::spawn(move || {"] # [doc = "     assert_eq!(6, block_on(shared2));"] # [doc = " });"] # [doc = " assert_eq!(6, shared1.await);"] # [doc = " join_handle.join().unwrap();"] # [doc = " # });"] # [doc = " ```"] # [cfg (any (feature = "std" , all (feature = "alloc" , feature = "spin")))] fn shared (self) -> Shared < Self > where Self : Sized , Self :: Output : Clone , { assert_future :: < Self :: Output , _ > (Shared :: new (self)) } # [doc = " Turn this future into a future that yields `()` on completion and sends"] # [doc = " its output to another future on a separate task."] # [doc = ""] # [doc = " This can be used with spawning executors to easily retrieve the result"] # [doc = " of a future executing on a separate task or thread."] # [doc = ""] # [doc = " This method is only available when the `std` feature of this"] # [doc = " library is activated, and it is activated by default."] # [cfg (feature = "channel")] # [cfg_attr (docsrs , doc (cfg (feature = "channel")))] # [cfg (feature = "std")] fn remote_handle (self) -> (Remote < Self > , RemoteHandle < Self :: Output >) where Self : Sized , { let (wrapped , handle) = remote_handle :: remote_handle (self) ; (assert_future :: < () , _ > (wrapped) , handle) } # [doc = " Wrap the future in a Box, pinning it."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [cfg (feature = "alloc")] fn boxed < 'a > (self) -> BoxFuture < 'a , Self :: Output > where Self : Sized + Send + 'a , { assert_future :: < Self :: Output , _ > (Box :: pin (self)) } # [doc = " Wrap the future in a Box, pinning it."] # [doc = ""] # [doc = " Similar to `boxed`, but without the `Send` requirement."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [cfg (feature = "alloc")] fn boxed_local < 'a > (self) -> LocalBoxFuture < 'a , Self :: Output > where Self : Sized + 'a , { assert_future :: < Self :: Output , _ > (Box :: pin (self)) } # [doc = " Turns a [`Future<Output = T>`](Future) into a"] # [doc = " [`TryFuture<Ok = T, Error = ()`>](futures_core::future::TryFuture)."] fn unit_error (self) -> UnitError < Self > where Self : Sized , { assert_future :: < Result < Self :: Output , () > , _ > (UnitError :: new (self)) } # [doc = " Turns a [`Future<Output = T>`](Future) into a"] # [doc = " [`TryFuture<Ok = T, Error = Never`>](futures_core::future::TryFuture)."] fn never_error (self) -> NeverError < Self > where Self : Sized , { assert_future :: < Result < Self :: Output , Infallible > , _ > (NeverError :: new (self)) } # [doc = " A convenience for calling `Future::poll` on `Unpin` future types."] fn poll_unpin (& mut self , cx : & mut Context < '_ >) -> Poll < Self :: Output > where Self : Unpin , { Pin :: new (self) . poll (cx) } # [doc = " Evaluates and consumes the future, returning the resulting output if"] # [doc = " the future is ready after the first call to `Future::poll`."] # [doc = ""] # [doc = " If `poll` instead returns `Poll::Pending`, `None` is returned."] # [doc = ""] # [doc = " This method is useful in cases where immediacy is more important than"] # [doc = " waiting for a result. It is also convenient for quickly obtaining"] # [doc = " the value of a future that is known to always resolve immediately."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use futures::prelude::*;"] # [doc = " use futures::{future::ready, future::pending};"] # [doc = " let future_ready = ready(\"foobar\");"] # [doc = " let future_pending = pending::<&'static str>();"] # [doc = ""] # [doc = " assert_eq!(future_ready.now_or_never(), Some(\"foobar\"));"] # [doc = " assert_eq!(future_pending.now_or_never(), None);"] # [doc = " ```"] # [doc = ""] # [doc = " In cases where it is absolutely known that a future should always"] # [doc = " resolve immediately and never return `Poll::Pending`, this method can"] # [doc = " be combined with `expect()`:"] # [doc = ""] # [doc = " ```"] # [doc = " # use futures::{prelude::*, future::ready};"] # [doc = " let future_ready = ready(\"foobar\");"] # [doc = ""] # [doc = " assert_eq!(future_ready.now_or_never().expect(\"Future not ready\"), \"foobar\");"] # [doc = " ```"] fn now_or_never (self) -> Option < Self :: Output > where Self : Sized , { let noop_waker = crate :: task :: noop_waker () ; let mut cx = Context :: from_waker (& noop_waker) ; let this = pin ! (self) ; match this . poll (& mut cx) { Poll :: Ready (x) => Some (x) , _ => None , } } }
    };
}

FutureExt!();