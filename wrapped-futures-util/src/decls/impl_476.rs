macro_rules! impl_476 {
    () => {
        impl < St1 : Stream , St2 : Stream > Zip < St1 , St2 > { pub (super) fn new (stream1 : St1 , stream2 : St2) -> Self { Self { stream1 : stream1 . fuse () , stream2 : stream2 . fuse () , queued1 : None , queued2 : None } } # [doc = " Acquires a reference to the underlying streams that this combinator is"] # [doc = " pulling from."] pub fn get_ref (& self) -> (& St1 , & St2) { (self . stream1 . get_ref () , self . stream2 . get_ref ()) } # [doc = " Acquires a mutable reference to the underlying streams that this"] # [doc = " combinator is pulling from."] # [doc = ""] # [doc = " Note that care must be taken to avoid tampering with the state of the"] # [doc = " stream which may otherwise confuse this combinator."] pub fn get_mut (& mut self) -> (& mut St1 , & mut St2) { (self . stream1 . get_mut () , self . stream2 . get_mut ()) } # [doc = " Acquires a pinned mutable reference to the underlying streams that this"] # [doc = " combinator is pulling from."] # [doc = ""] # [doc = " Note that care must be taken to avoid tampering with the state of the"] # [doc = " stream which may otherwise confuse this combinator."] pub fn get_pin_mut (self : Pin < & mut Self >) -> (Pin < & mut St1 > , Pin < & mut St2 >) { let this = self . project () ; (this . stream1 . get_pin_mut () , this . stream2 . get_pin_mut ()) } # [doc = " Consumes this combinator, returning the underlying streams."] # [doc = ""] # [doc = " Note that this may discard intermediate state of this combinator, so"] # [doc = " care should be taken to avoid losing resources when this is called."] pub fn into_inner (self) -> (St1 , St2) { (self . stream1 . into_inner () , self . stream2 . into_inner ()) } }
    };
}

impl_476!();