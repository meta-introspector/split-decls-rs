macro_rules! deps {
    () => {
        ReadUntil!();
        Cursor!();
        FillBuf!();
        ReadLine!();
        TryStreamExt!();
    };
}

macro_rules! AsyncBufReadExt {
    () => {
        deps!();
        # [doc = " An extension trait which adds utility methods to `AsyncBufRead` types."] pub trait AsyncBufReadExt : AsyncBufRead { # [doc = " Creates a future which will wait for a non-empty buffer to be available from this I/O"] # [doc = " object or EOF to be reached."] # [doc = ""] # [doc = " This method is the async equivalent to [`BufRead::fill_buf`](std::io::BufRead::fill_buf)."] # [doc = ""] # [doc = " ```rust"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::{io::AsyncBufReadExt as _, stream::{iter, TryStreamExt as _}};"] # [doc = ""] # [doc = " let mut stream = iter(vec![Ok(vec![1, 2, 3]), Ok(vec![4, 5, 6])]).into_async_read();"] # [doc = ""] # [doc = " assert_eq!(stream.fill_buf().await?, vec![1, 2, 3]);"] # [doc = " stream.consume_unpin(2);"] # [doc = ""] # [doc = " assert_eq!(stream.fill_buf().await?, vec![3]);"] # [doc = " stream.consume_unpin(1);"] # [doc = ""] # [doc = " assert_eq!(stream.fill_buf().await?, vec![4, 5, 6]);"] # [doc = " stream.consume_unpin(3);"] # [doc = ""] # [doc = " assert_eq!(stream.fill_buf().await?, vec![]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn fill_buf (& mut self) -> FillBuf < '_ , Self > where Self : Unpin , { assert_future :: < Result < & [u8] > , _ > (FillBuf :: new (self)) } # [doc = " A convenience for calling [`AsyncBufRead::consume`] on [`Unpin`] IO types."] # [doc = ""] # [doc = " ```rust"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::{io::AsyncBufReadExt as _, stream::{iter, TryStreamExt as _}};"] # [doc = ""] # [doc = " let mut stream = iter(vec![Ok(vec![1, 2, 3])]).into_async_read();"] # [doc = ""] # [doc = " assert_eq!(stream.fill_buf().await?, vec![1, 2, 3]);"] # [doc = " stream.consume_unpin(2);"] # [doc = ""] # [doc = " assert_eq!(stream.fill_buf().await?, vec![3]);"] # [doc = " stream.consume_unpin(1);"] # [doc = ""] # [doc = " assert_eq!(stream.fill_buf().await?, vec![]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn consume_unpin (& mut self , amt : usize) where Self : Unpin , { Pin :: new (self) . consume (amt) } # [doc = " Creates a future which will read all the bytes associated with this I/O"] # [doc = " object into `buf` until the delimiter `byte` or EOF is reached."] # [doc = " This method is the async equivalent to [`BufRead::read_until`](std::io::BufRead::read_until)."] # [doc = ""] # [doc = " This function will read bytes from the underlying stream until the"] # [doc = " delimiter or EOF is found. Once found, all bytes up to, and including,"] # [doc = " the delimiter (if found) will be appended to `buf`."] # [doc = ""] # [doc = " The returned future will resolve to the number of bytes read once the read"] # [doc = " operation is completed."] # [doc = ""] # [doc = " In the case of an error the buffer and the object will be discarded, with"] # [doc = " the error yielded."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{AsyncBufReadExt, Cursor};"] # [doc = ""] # [doc = " let mut cursor = Cursor::new(b\"lorem-ipsum\");"] # [doc = " let mut buf = vec![];"] # [doc = ""] # [doc = " // cursor is at 'l'"] # [doc = " let num_bytes = cursor.read_until(b'-', &mut buf).await?;"] # [doc = " assert_eq!(num_bytes, 6);"] # [doc = " assert_eq!(buf, b\"lorem-\");"] # [doc = " buf.clear();"] # [doc = ""] # [doc = " // cursor is at 'i'"] # [doc = " let num_bytes = cursor.read_until(b'-', &mut buf).await?;"] # [doc = " assert_eq!(num_bytes, 5);"] # [doc = " assert_eq!(buf, b\"ipsum\");"] # [doc = " buf.clear();"] # [doc = ""] # [doc = " // cursor is at EOF"] # [doc = " let num_bytes = cursor.read_until(b'-', &mut buf).await?;"] # [doc = " assert_eq!(num_bytes, 0);"] # [doc = " assert_eq!(buf, b\"\");"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn read_until < 'a > (& 'a mut self , byte : u8 , buf : & 'a mut Vec < u8 >) -> ReadUntil < 'a , Self > where Self : Unpin , { assert_future :: < Result < usize > , _ > (ReadUntil :: new (self , byte , buf)) } # [doc = " Creates a future which will read all the bytes associated with this I/O"] # [doc = " object into `buf` until a newline (the 0xA byte) or EOF is reached,"] # [doc = " This method is the async equivalent to [`BufRead::read_line`](std::io::BufRead::read_line)."] # [doc = ""] # [doc = " This function will read bytes from the underlying stream until the"] # [doc = " newline delimiter (the 0xA byte) or EOF is found. Once found, all bytes"] # [doc = " up to, and including, the delimiter (if found) will be appended to"] # [doc = " `buf`."] # [doc = ""] # [doc = " The returned future will resolve to the number of bytes read once the read"] # [doc = " operation is completed."] # [doc = ""] # [doc = " In the case of an error the buffer and the object will be discarded, with"] # [doc = " the error yielded."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function has the same error semantics as [`read_until`] and will"] # [doc = " also return an error if the read bytes are not valid UTF-8. If an I/O"] # [doc = " error is encountered then `buf` may contain some bytes already read in"] # [doc = " the event that all data read so far was valid UTF-8."] # [doc = ""] # [doc = " [`read_until`]: AsyncBufReadExt::read_until"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{AsyncBufReadExt, Cursor};"] # [doc = ""] # [doc = " let mut cursor = Cursor::new(b\"foo\\nbar\");"] # [doc = " let mut buf = String::new();"] # [doc = ""] # [doc = " // cursor is at 'f'"] # [doc = " let num_bytes = cursor.read_line(&mut buf).await?;"] # [doc = " assert_eq!(num_bytes, 4);"] # [doc = " assert_eq!(buf, \"foo\\n\");"] # [doc = " buf.clear();"] # [doc = ""] # [doc = " // cursor is at 'b'"] # [doc = " let num_bytes = cursor.read_line(&mut buf).await?;"] # [doc = " assert_eq!(num_bytes, 3);"] # [doc = " assert_eq!(buf, \"bar\");"] # [doc = " buf.clear();"] # [doc = ""] # [doc = " // cursor is at EOF"] # [doc = " let num_bytes = cursor.read_line(&mut buf).await?;"] # [doc = " assert_eq!(num_bytes, 0);"] # [doc = " assert_eq!(buf, \"\");"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn read_line < 'a > (& 'a mut self , buf : & 'a mut String) -> ReadLine < 'a , Self > where Self : Unpin , { assert_future :: < Result < usize > , _ > (ReadLine :: new (self , buf)) } # [doc = " Returns a stream over the lines of this reader."] # [doc = " This method is the async equivalent to [`BufRead::lines`](std::io::BufRead::lines)."] # [doc = ""] # [doc = " The stream returned from this function will yield instances of"] # [doc = " [`io::Result`]`<`[`String`]`>`. Each string returned will *not* have a newline"] # [doc = " byte (the 0xA byte) or CRLF (0xD, 0xA bytes) at the end."] # [doc = ""] # [doc = " [`io::Result`]: std::io::Result"] # [doc = " [`String`]: String"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Each line of the stream has the same error semantics as [`AsyncBufReadExt::read_line`]."] # [doc = ""] # [doc = " [`AsyncBufReadExt::read_line`]: AsyncBufReadExt::read_line"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::io::{AsyncBufReadExt, Cursor};"] # [doc = " use futures::stream::StreamExt;"] # [doc = ""] # [doc = " let cursor = Cursor::new(b\"lorem\\nipsum\\xc2\\r\\ndolor\");"] # [doc = ""] # [doc = " let mut lines_stream = cursor.lines().map(|l| l.unwrap_or(String::from(\"invalid UTF_8\")));"] # [doc = " assert_eq!(lines_stream.next().await, Some(String::from(\"lorem\")));"] # [doc = " assert_eq!(lines_stream.next().await, Some(String::from(\"invalid UTF_8\")));"] # [doc = " assert_eq!(lines_stream.next().await, Some(String::from(\"dolor\")));"] # [doc = " assert_eq!(lines_stream.next().await, None);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] fn lines (self) -> Lines < Self > where Self : Sized , { assert_stream :: < Result < String > , _ > (Lines :: new (self)) } }
    };
}

AsyncBufReadExt!();