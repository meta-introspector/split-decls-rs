macro_rules! document_join_macro {
    () => {
        macro_rules ! document_join_macro { ($ join : item $ try_join : item) => { # [doc = " Polls multiple futures simultaneously, returning a tuple"] # [doc = " of all results once complete."] # [doc = ""] # [doc = " While `join!(a, b)` is similar to `(a.await, b.await)`,"] # [doc = " `join!` polls both futures concurrently and therefore is more efficient."] # [doc = ""] # [doc = " This macro is only usable inside of async functions, closures, and blocks."] # [doc = " It is also gated behind the `async-await` feature of this library, which is"] # [doc = " activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::join;"] # [doc = ""] # [doc = " let a = async { 1 };"] # [doc = " let b = async { 2 };"] # [doc = " assert_eq!(join!(a, b), (1, 2));"] # [doc = ""] # [doc = " // `join!` is variadic, so you can pass any number of futures"] # [doc = " let c = async { 3 };"] # [doc = " let d = async { 4 };"] # [doc = " let e = async { 5 };"] # [doc = " assert_eq!(join!(c, d, e), (3, 4, 5));"] # [doc = " # });"] # [doc = " ```"] $ join # [doc = " Polls multiple futures simultaneously, resolving to a [`Result`] containing"] # [doc = " either a tuple of the successful outputs or an error."] # [doc = ""] # [doc = " `try_join!` is similar to [`join!`], but completes immediately if any of"] # [doc = " the futures return an error."] # [doc = ""] # [doc = " This macro is only usable inside of async functions, closures, and blocks."] # [doc = " It is also gated behind the `async-await` feature of this library, which is"] # [doc = " activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " When used on multiple futures that return `Ok`, `try_join!` will return"] # [doc = " `Ok` of a tuple of the values:"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::try_join;"] # [doc = ""] # [doc = " let a = async { Ok::<i32, i32>(1) };"] # [doc = " let b = async { Ok::<i32, i32>(2) };"] # [doc = " assert_eq!(try_join!(a, b), Ok((1, 2)));"] # [doc = ""] # [doc = " // `try_join!` is variadic, so you can pass any number of futures"] # [doc = " let c = async { Ok::<i32, i32>(3) };"] # [doc = " let d = async { Ok::<i32, i32>(4) };"] # [doc = " let e = async { Ok::<i32, i32>(5) };"] # [doc = " assert_eq!(try_join!(c, d, e), Ok((3, 4, 5)));"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " If one of the futures resolves to an error, `try_join!` will return"] # [doc = " that error:"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::try_join;"] # [doc = ""] # [doc = " let a = async { Ok::<i32, i32>(1) };"] # [doc = " let b = async { Err::<u64, i32>(2) };"] # [doc = ""] # [doc = " assert_eq!(try_join!(a, b), Err(2));"] # [doc = " # });"] # [doc = " ```"] $ try_join } }
    };
}

document_join_macro!()