macro_rules! deps {
    () => {
        TryFutureExt!();
        TryChunksError!();
        StreamExt!();
        Compat!();
        Sink!();
        TryReadyChunksError!();
        TryNext!();
    };
}

macro_rules! TryStreamExt {
    () => {
        deps!();
        # [doc = " Adapters specific to `Result`-returning streams"] pub trait TryStreamExt : TryStream { # [doc = " Wraps the current stream in a new stream which converts the error type"] # [doc = " into the one provided."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, TryStreamExt};"] # [doc = ""] # [doc = " let mut stream ="] # [doc = "     stream::iter(vec![Ok(()), Err(5i32)])"] # [doc = "         .err_into::<i64>();"] # [doc = ""] # [doc = " assert_eq!(stream.try_next().await, Ok(Some(())));"] # [doc = " assert_eq!(stream.try_next().await, Err(5i64));"] # [doc = " # })"] # [doc = " ```"] fn err_into < E > (self) -> ErrInto < Self , E > where Self : Sized , Self :: Error : Into < E > , { assert_stream :: < Result < Self :: Ok , E > , _ > (ErrInto :: new (self)) } # [doc = " Wraps the current stream in a new stream which maps the success value"] # [doc = " using the provided closure."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, TryStreamExt};"] # [doc = ""] # [doc = " let mut stream ="] # [doc = "     stream::iter(vec![Ok(5), Err(0)])"] # [doc = "         .map_ok(|x| x + 2);"] # [doc = ""] # [doc = " assert_eq!(stream.try_next().await, Ok(Some(7)));"] # [doc = " assert_eq!(stream.try_next().await, Err(0));"] # [doc = " # })"] # [doc = " ```"] fn map_ok < T , F > (self , f : F) -> MapOk < Self , F > where Self : Sized , F : FnMut (Self :: Ok) -> T , { assert_stream :: < Result < T , Self :: Error > , _ > (MapOk :: new (self , f)) } # [doc = " Wraps the current stream in a new stream which maps the error value"] # [doc = " using the provided closure."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, TryStreamExt};"] # [doc = ""] # [doc = " let mut stream ="] # [doc = "     stream::iter(vec![Ok(5), Err(0)])"] # [doc = "         .map_err(|x| x + 2);"] # [doc = ""] # [doc = " assert_eq!(stream.try_next().await, Ok(Some(5)));"] # [doc = " assert_eq!(stream.try_next().await, Err(2));"] # [doc = " # })"] # [doc = " ```"] fn map_err < E , F > (self , f : F) -> MapErr < Self , F > where Self : Sized , F : FnMut (Self :: Error) -> E , { assert_stream :: < Result < Self :: Ok , E > , _ > (MapErr :: new (self , f)) } # [doc = " Chain on a computation for when a value is ready, passing the successful"] # [doc = " results to the provided closure `f`."] # [doc = ""] # [doc = " This function can be used to run a unit of work when the next successful"] # [doc = " value on a stream is ready. The closure provided will be yielded a value"] # [doc = " when ready, and the returned future will then be run to completion to"] # [doc = " produce the next value on this stream."] # [doc = ""] # [doc = " Any errors produced by this stream will not be passed to the closure,"] # [doc = " and will be passed through."] # [doc = ""] # [doc = " The returned value of the closure must implement the `TryFuture` trait"] # [doc = " and can represent some more work to be done before the composed stream"] # [doc = " is finished."] # [doc = ""] # [doc = " Note that this function consumes the receiving stream and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " To process the entire stream and return a single future representing"] # [doc = " success or error, use `try_for_each` instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::channel::mpsc;"] # [doc = " use futures::future;"] # [doc = " use futures::stream::TryStreamExt;"] # [doc = ""] # [doc = " let (_tx, rx) = mpsc::channel::<Result<i32, ()>>(1);"] # [doc = ""] # [doc = " let rx = rx.and_then(|result| {"] # [doc = "     future::ok(if result % 2 == 0 {"] # [doc = "         Some(result)"] # [doc = "     } else {"] # [doc = "         None"] # [doc = "     })"] # [doc = " });"] # [doc = " ```"] fn and_then < Fut , F > (self , f : F) -> AndThen < Self , Fut , F > where F : FnMut (Self :: Ok) -> Fut , Fut : TryFuture < Error = Self :: Error > , Self : Sized , { assert_stream :: < Result < Fut :: Ok , Fut :: Error > , _ > (AndThen :: new (self , f)) } # [doc = " Chain on a computation for when an error happens, passing the"] # [doc = " erroneous result to the provided closure `f`."] # [doc = ""] # [doc = " This function can be used to run a unit of work and attempt to recover from"] # [doc = " an error if one happens. The closure provided will be yielded an error"] # [doc = " when one appears, and the returned future will then be run to completion"] # [doc = " to produce the next value on this stream."] # [doc = ""] # [doc = " Any successful values produced by this stream will not be passed to the"] # [doc = " closure, and will be passed through."] # [doc = ""] # [doc = " The returned value of the closure must implement the [`TryFuture`](futures_core::future::TryFuture) trait"] # [doc = " and can represent some more work to be done before the composed stream"] # [doc = " is finished."] # [doc = ""] # [doc = " Note that this function consumes the receiving stream and returns a"] # [doc = " wrapped version of it."] fn or_else < Fut , F > (self , f : F) -> OrElse < Self , Fut , F > where F : FnMut (Self :: Error) -> Fut , Fut : TryFuture < Ok = Self :: Ok > , Self : Sized , { assert_stream :: < Result < Self :: Ok , Fut :: Error > , _ > (OrElse :: new (self , f)) } # [doc = " A future that completes after the given stream has been fully processed"] # [doc = " into the sink and the sink has been flushed and closed."] # [doc = ""] # [doc = " This future will drive the stream to keep producing items until it is"] # [doc = " exhausted, sending each item to the sink. It will complete once the"] # [doc = " stream is exhausted, the sink has received and flushed all items, and"] # [doc = " the sink is closed. Note that neither the original stream nor provided"] # [doc = " sink will be output by this future. Pass the sink by `Pin<&mut S>`"] # [doc = " (for example, via `try_forward(&mut sink)` inside an `async` fn/block) in"] # [doc = " order to preserve access to the `Sink`. If the stream produces an error,"] # [doc = " that error will be returned by this future without flushing/closing the sink."] # [cfg (feature = "sink")] # [cfg_attr (docsrs , doc (cfg (feature = "sink")))] fn try_forward < S > (self , sink : S) -> TryForward < Self , S > where S : Sink < Self :: Ok , Error = Self :: Error > , Self : Sized , { assert_future :: < Result < () , Self :: Error > , _ > (TryForward :: new (self , sink)) } # [doc = " Do something with the success value of this stream, afterwards passing"] # [doc = " it on."] # [doc = ""] # [doc = " This is similar to the `StreamExt::inspect` method where it allows"] # [doc = " easily inspecting the success value as it passes through the stream, for"] # [doc = " example to debug what's going on."] fn inspect_ok < F > (self , f : F) -> InspectOk < Self , F > where F : FnMut (& Self :: Ok) , Self : Sized , { assert_stream :: < Result < Self :: Ok , Self :: Error > , _ > (InspectOk :: new (self , f)) } # [doc = " Do something with the error value of this stream, afterwards passing it on."] # [doc = ""] # [doc = " This is similar to the `StreamExt::inspect` method where it allows"] # [doc = " easily inspecting the error value as it passes through the stream, for"] # [doc = " example to debug what's going on."] fn inspect_err < F > (self , f : F) -> InspectErr < Self , F > where F : FnMut (& Self :: Error) , Self : Sized , { assert_stream :: < Result < Self :: Ok , Self :: Error > , _ > (InspectErr :: new (self , f)) } # [doc = " Wraps a [`TryStream`] into a type that implements"] # [doc = " [`Stream`](futures_core::stream::Stream)"] # [doc = ""] # [doc = " [`TryStream`]s currently do not implement the"] # [doc = " [`Stream`](futures_core::stream::Stream) trait because of limitations"] # [doc = " of the compiler."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::stream::{Stream, TryStream, TryStreamExt};"] # [doc = ""] # [doc = " # type T = i32;"] # [doc = " # type E = ();"] # [doc = " fn make_try_stream() -> impl TryStream<Ok = T, Error = E> { // ... }"] # [doc = " # futures::stream::empty()"] # [doc = " # }"] # [doc = " fn take_stream(stream: impl Stream<Item = Result<T, E>>) { /* ... */ }"] # [doc = ""] # [doc = " take_stream(make_try_stream().into_stream());"] # [doc = " ```"] fn into_stream (self) -> IntoStream < Self > where Self : Sized , { assert_stream :: < Result < Self :: Ok , Self :: Error > , _ > (IntoStream :: new (self)) } # [doc = " Creates a future that attempts to resolve the next item in the stream."] # [doc = " If an error is encountered before the next item, the error is returned"] # [doc = " instead."] # [doc = ""] # [doc = " This is similar to the `Stream::next` combinator, but returns a"] # [doc = " `Result<Option<T>, E>` rather than an `Option<Result<T, E>>`, making"] # [doc = " for easy use with the `?` operator."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, TryStreamExt};"] # [doc = ""] # [doc = " let mut stream = stream::iter(vec![Ok(()), Err(())]);"] # [doc = ""] # [doc = " assert_eq!(stream.try_next().await, Ok(Some(())));"] # [doc = " assert_eq!(stream.try_next().await, Err(()));"] # [doc = " # })"] # [doc = " ```"] fn try_next (& mut self) -> TryNext < '_ , Self > where Self : Unpin , { assert_future :: < Result < Option < Self :: Ok > , Self :: Error > , _ > (TryNext :: new (self)) } # [doc = " Skip elements on this stream while the provided asynchronous predicate"] # [doc = " resolves to `true`."] # [doc = ""] # [doc = " This function is similar to"] # [doc = " [`StreamExt::skip_while`](crate::stream::StreamExt::skip_while) but exits"] # [doc = " early if an error occurs."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::stream::{self, TryStreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(vec![Ok::<i32, i32>(1), Ok(3), Ok(2)]);"] # [doc = " let stream = stream.try_skip_while(|x| future::ready(Ok(*x < 3)));"] # [doc = ""] # [doc = " let output: Result<Vec<i32>, i32> = stream.try_collect().await;"] # [doc = " assert_eq!(output, Ok(vec![3, 2]));"] # [doc = " # })"] # [doc = " ```"] fn try_skip_while < Fut , F > (self , f : F) -> TrySkipWhile < Self , Fut , F > where F : FnMut (& Self :: Ok) -> Fut , Fut : TryFuture < Ok = bool , Error = Self :: Error > , Self : Sized , { assert_stream :: < Result < Self :: Ok , Self :: Error > , _ > (TrySkipWhile :: new (self , f)) } # [doc = " Take elements on this stream while the provided asynchronous predicate"] # [doc = " resolves to `true`."] # [doc = ""] # [doc = " This function is similar to"] # [doc = " [`StreamExt::take_while`](crate::stream::StreamExt::take_while) but exits"] # [doc = " early if an error occurs."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::stream::{self, TryStreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(vec![Ok::<i32, i32>(1), Ok(2), Ok(3), Ok(2)]);"] # [doc = " let stream = stream.try_take_while(|x| future::ready(Ok(*x < 3)));"] # [doc = ""] # [doc = " let output: Result<Vec<i32>, i32> = stream.try_collect().await;"] # [doc = " assert_eq!(output, Ok(vec![1, 2]));"] # [doc = " # })"] # [doc = " ```"] fn try_take_while < Fut , F > (self , f : F) -> TryTakeWhile < Self , Fut , F > where F : FnMut (& Self :: Ok) -> Fut , Fut : TryFuture < Ok = bool , Error = Self :: Error > , Self : Sized , { assert_stream :: < Result < Self :: Ok , Self :: Error > , _ > (TryTakeWhile :: new (self , f)) } # [doc = " Attempt to transform a stream into a collection,"] # [doc = " returning a future representing the result of that computation."] # [doc = ""] # [doc = " This combinator will collect all successful results of this stream and"] # [doc = " collect them into the specified collection type. If an error happens then all"] # [doc = " collected elements will be dropped and the error will be returned."] # [doc = ""] # [doc = " The returned future will be resolved when the stream terminates."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::mpsc;"] # [doc = " use futures::stream::TryStreamExt;"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let (tx, rx) = mpsc::unbounded();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     for i in 1..=5 {"] # [doc = "         tx.unbounded_send(Ok(i)).unwrap();"] # [doc = "     }"] # [doc = "     tx.unbounded_send(Err(6)).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " let output: Result<Vec<i32>, i32> = rx.try_collect().await;"] # [doc = " assert_eq!(output, Err(6));"] # [doc = " # })"] # [doc = " ```"] fn try_collect < C : Default + Extend < Self :: Ok > > (self) -> TryCollect < Self , C > where Self : Sized , { assert_future :: < Result < C , Self :: Error > , _ > (TryCollect :: new (self)) } # [doc = " An adaptor for chunking up successful items of the stream inside a vector."] # [doc = ""] # [doc = " This combinator will attempt to pull successful items from this stream and buffer"] # [doc = " them into a local vector. At most `capacity` items will get buffered"] # [doc = " before they're yielded from the returned stream."] # [doc = ""] # [doc = " Note that the vectors returned from this iterator may not always have"] # [doc = " `capacity` elements. If the underlying stream ended and only a partial"] # [doc = " vector was created, it'll be returned. Additionally if an error happens"] # [doc = " from the underlying stream then the currently buffered items will be"] # [doc = " yielded."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " This function is similar to"] # [doc = " [`StreamExt::chunks`](crate::stream::StreamExt::chunks) but exits"] # [doc = " early if an error occurs."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, TryChunksError, TryStreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(vec![Ok::<i32, i32>(1), Ok(2), Ok(3), Err(4), Ok(5), Ok(6)]);"] # [doc = " let mut stream = stream.try_chunks(2);"] # [doc = ""] # [doc = " assert_eq!(stream.try_next().await, Ok(Some(vec![1, 2])));"] # [doc = " assert_eq!(stream.try_next().await, Err(TryChunksError(vec![3], 4)));"] # [doc = " assert_eq!(stream.try_next().await, Ok(Some(vec![5, 6])));"] # [doc = " # })"] # [doc = " ```"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This method will panic if `capacity` is zero."] # [cfg (feature = "alloc")] fn try_chunks (self , capacity : usize) -> TryChunks < Self > where Self : Sized , { assert_stream :: < Result < Vec < Self :: Ok > , TryChunksError < Self :: Ok , Self :: Error > > , _ > (TryChunks :: new (self , capacity) ,) } # [doc = " An adaptor for chunking up successful, ready items of the stream inside a vector."] # [doc = ""] # [doc = " This combinator will attempt to pull successful items from this stream and buffer"] # [doc = " them into a local vector. At most `capacity` items will get buffered"] # [doc = " before they're yielded from the returned stream. If the underlying stream"] # [doc = " returns `Poll::Pending`, and the collected chunk is not empty, it will"] # [doc = " be immediately returned."] # [doc = ""] # [doc = " Note that the vectors returned from this iterator may not always have"] # [doc = " `capacity` elements. If the underlying stream ended and only a partial"] # [doc = " vector was created, it'll be returned. Additionally if an error happens"] # [doc = " from the underlying stream then the currently buffered items will be"] # [doc = " yielded."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " This function is similar to"] # [doc = " [`StreamExt::ready_chunks`](crate::stream::StreamExt::ready_chunks) but exits"] # [doc = " early if an error occurs."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, TryReadyChunksError, TryStreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(vec![Ok::<i32, i32>(1), Ok(2), Ok(3), Err(4), Ok(5), Ok(6)]);"] # [doc = " let mut stream = stream.try_ready_chunks(2);"] # [doc = ""] # [doc = " assert_eq!(stream.try_next().await, Ok(Some(vec![1, 2])));"] # [doc = " assert_eq!(stream.try_next().await, Err(TryReadyChunksError(vec![3], 4)));"] # [doc = " assert_eq!(stream.try_next().await, Ok(Some(vec![5, 6])));"] # [doc = " # })"] # [doc = " ```"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This method will panic if `capacity` is zero."] # [cfg (feature = "alloc")] fn try_ready_chunks (self , capacity : usize) -> TryReadyChunks < Self > where Self : Sized , { assert_stream :: < Result < Vec < Self :: Ok > , TryReadyChunksError < Self :: Ok , Self :: Error > > , _ > (TryReadyChunks :: new (self , capacity) ,) } # [doc = " Attempt to filter the values produced by this stream according to the"] # [doc = " provided asynchronous closure."] # [doc = ""] # [doc = " As values of this stream are made available, the provided predicate `f`"] # [doc = " will be run on them. If the predicate returns a `Future` which resolves"] # [doc = " to `true`, then the stream will yield the value, but if the predicate"] # [doc = " return a `Future` which resolves to `false`, then the value will be"] # [doc = " discarded and the next value will be produced."] # [doc = ""] # [doc = " All errors are passed through without filtering in this combinator."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it, similar to the existing `filter` methods in"] # [doc = " the standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::stream::{self, StreamExt, TryStreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(vec![Ok(1i32), Ok(2i32), Ok(3i32), Err(\"error\")]);"] # [doc = " let mut evens = stream.try_filter(|x| {"] # [doc = "     future::ready(x % 2 == 0)"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!(evens.next().await, Some(Ok(2)));"] # [doc = " assert_eq!(evens.next().await, Some(Err(\"error\")));"] # [doc = " # })"] # [doc = " ```"] fn try_filter < Fut , F > (self , f : F) -> TryFilter < Self , Fut , F > where Fut : Future < Output = bool > , F : FnMut (& Self :: Ok) -> Fut , Self : Sized , { assert_stream :: < Result < Self :: Ok , Self :: Error > , _ > (TryFilter :: new (self , f)) } # [doc = " Attempt to filter the values produced by this stream while"] # [doc = " simultaneously mapping them to a different type according to the"] # [doc = " provided asynchronous closure."] # [doc = ""] # [doc = " As values of this stream are made available, the provided function will"] # [doc = " be run on them. If the future returned by the predicate `f` resolves to"] # [doc = " [`Some(item)`](Some) then the stream will yield the value `item`, but if"] # [doc = " it resolves to [`None`] then the next value will be produced."] # [doc = ""] # [doc = " All errors are passed through without filtering in this combinator."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it, similar to the existing `filter_map` methods in"] # [doc = " the standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use core::pin::pin;"] # [doc = ""] # [doc = " use futures::stream;"] # [doc = " use futures::stream::StreamExt;"] # [doc = " use futures::stream::TryStreamExt;"] # [doc = ""] # [doc = " let stream = stream::iter(vec![Ok(1i32), Ok(6i32), Err(\"error\")]);"] # [doc = " let halves = stream.try_filter_map(|x| async move {"] # [doc = "     let ret = if x % 2 == 0 { Some(x / 2) } else { None };"] # [doc = "     Ok(ret)"] # [doc = " });"] # [doc = ""] # [doc = " let mut halves = pin!(halves);"] # [doc = " assert_eq!(halves.next().await, Some(Ok(3)));"] # [doc = " assert_eq!(halves.next().await, Some(Err(\"error\")));"] # [doc = " # })"] # [doc = " ```"] fn try_filter_map < Fut , F , T > (self , f : F) -> TryFilterMap < Self , Fut , F > where Fut : TryFuture < Ok = Option < T > , Error = Self :: Error > , F : FnMut (Self :: Ok) -> Fut , Self : Sized , { assert_stream :: < Result < T , Self :: Error > , _ > (TryFilterMap :: new (self , f)) } # [doc = " Flattens a stream of streams into just one continuous stream. Produced streams"] # [doc = " will be polled concurrently and any errors will be passed through without looking at them."] # [doc = " If the underlying base stream returns an error, it will be **immediately** propagated."] # [doc = ""] # [doc = " The only argument is an optional limit on the number of concurrently"] # [doc = " polled streams. If this limit is not `None`, no more than `limit` streams"] # [doc = " will be polled at the same time. The `limit` argument is of type"] # [doc = " `Into<Option<usize>>`, and so can be provided as either `None`,"] # [doc = " `Some(10)`, or just `10`. Note: a limit of zero is interpreted as"] # [doc = " no limit at all, and will have the same result as passing in `None`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::mpsc;"] # [doc = " use futures::stream::{StreamExt, TryStreamExt};"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let (tx1, rx1) = mpsc::unbounded();"] # [doc = " let (tx2, rx2) = mpsc::unbounded();"] # [doc = " let (tx3, rx3) = mpsc::unbounded();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     tx1.unbounded_send(Ok(1)).unwrap();"] # [doc = " });"] # [doc = " thread::spawn(move || {"] # [doc = "     tx2.unbounded_send(Ok(2)).unwrap();"] # [doc = "     tx2.unbounded_send(Err(3)).unwrap();"] # [doc = "     tx2.unbounded_send(Ok(4)).unwrap();"] # [doc = " });"] # [doc = " thread::spawn(move || {"] # [doc = "     tx3.unbounded_send(Ok(rx1)).unwrap();"] # [doc = "     tx3.unbounded_send(Ok(rx2)).unwrap();"] # [doc = "     tx3.unbounded_send(Err(5)).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " let stream = rx3.try_flatten_unordered(None);"] # [doc = " let mut values: Vec<_> = stream.collect().await;"] # [doc = " values.sort();"] # [doc = ""] # [doc = " assert_eq!(values, vec![Ok(1), Ok(2), Ok(4), Err(3), Err(5)]);"] # [doc = " # });"] # [doc = " ```"] # [cfg_attr (target_os = "none" , cfg (target_has_atomic = "ptr"))] # [cfg (feature = "alloc")] fn try_flatten_unordered (self , limit : impl Into < Option < usize > >) -> TryFlattenUnordered < Self > where Self :: Ok : TryStream + Unpin , < Self :: Ok as TryStream > :: Error : From < Self :: Error > , Self : Sized , { assert_stream :: < Result < < Self :: Ok as TryStream > :: Ok , < Self :: Ok as TryStream > :: Error > , _ > (TryFlattenUnordered :: new (self , limit) ,) } # [doc = " Flattens a stream of streams into just one continuous stream."] # [doc = ""] # [doc = " If this stream's elements are themselves streams then this combinator"] # [doc = " will flatten out the entire stream to one long chain of elements. Any"] # [doc = " errors are passed through without looking at them, but otherwise each"] # [doc = " individual stream will get exhausted before moving on to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::mpsc;"] # [doc = " use futures::stream::{StreamExt, TryStreamExt};"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let (tx1, rx1) = mpsc::unbounded();"] # [doc = " let (tx2, rx2) = mpsc::unbounded();"] # [doc = " let (tx3, rx3) = mpsc::unbounded();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     tx1.unbounded_send(Ok(1)).unwrap();"] # [doc = " });"] # [doc = " thread::spawn(move || {"] # [doc = "     tx2.unbounded_send(Ok(2)).unwrap();"] # [doc = "     tx2.unbounded_send(Err(3)).unwrap();"] # [doc = "     tx2.unbounded_send(Ok(4)).unwrap();"] # [doc = " });"] # [doc = " thread::spawn(move || {"] # [doc = "     tx3.unbounded_send(Ok(rx1)).unwrap();"] # [doc = "     tx3.unbounded_send(Ok(rx2)).unwrap();"] # [doc = "     tx3.unbounded_send(Err(5)).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " let mut stream = rx3.try_flatten();"] # [doc = " assert_eq!(stream.next().await, Some(Ok(1)));"] # [doc = " assert_eq!(stream.next().await, Some(Ok(2)));"] # [doc = " assert_eq!(stream.next().await, Some(Err(3)));"] # [doc = " assert_eq!(stream.next().await, Some(Ok(4)));"] # [doc = " assert_eq!(stream.next().await, Some(Err(5)));"] # [doc = " assert_eq!(stream.next().await, None);"] # [doc = " # });"] # [doc = " ```"] fn try_flatten (self) -> TryFlatten < Self > where Self :: Ok : TryStream , < Self :: Ok as TryStream > :: Error : From < Self :: Error > , Self : Sized , { assert_stream :: < Result < < Self :: Ok as TryStream > :: Ok , < Self :: Ok as TryStream > :: Error > , _ > (TryFlatten :: new (self) ,) } # [doc = " Attempt to concatenate all items of a stream into a single"] # [doc = " extendable destination, returning a future representing the end result."] # [doc = ""] # [doc = " This combinator will extend the first item with the contents of all"] # [doc = " the subsequent successful results of the stream. If the stream is empty,"] # [doc = " the default value will be returned."] # [doc = ""] # [doc = " Works with all collections that implement the [`Extend`](std::iter::Extend) trait."] # [doc = ""] # [doc = " This method is similar to [`concat`](crate::stream::StreamExt::concat), but will"] # [doc = " exit early if an error is encountered in the stream."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::mpsc;"] # [doc = " use futures::stream::TryStreamExt;"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let (tx, rx) = mpsc::unbounded::<Result<Vec<i32>, ()>>();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     for i in (0..3).rev() {"] # [doc = "         let n = i * 3;"] # [doc = "         tx.unbounded_send(Ok(vec![n + 1, n + 2, n + 3])).unwrap();"] # [doc = "     }"] # [doc = " });"] # [doc = ""] # [doc = " let result = rx.try_concat().await;"] # [doc = ""] # [doc = " assert_eq!(result, Ok(vec![7, 8, 9, 4, 5, 6, 1, 2, 3]));"] # [doc = " # });"] # [doc = " ```"] fn try_concat (self) -> TryConcat < Self > where Self : Sized , Self :: Ok : Extend < < < Self as TryStream > :: Ok as IntoIterator > :: Item > + IntoIterator + Default , { assert_future :: < Result < Self :: Ok , Self :: Error > , _ > (TryConcat :: new (self)) } # [doc = " Attempt to execute several futures from a stream concurrently (unordered)."] # [doc = ""] # [doc = " This stream's `Ok` type must be a [`TryFuture`](futures_core::future::TryFuture) with an `Error` type"] # [doc = " that matches the stream's `Error` type."] # [doc = ""] # [doc = " This adaptor will buffer up to `n` futures and then return their"] # [doc = " outputs in the order in which they complete. If the underlying stream"] # [doc = " returns an error, it will be immediately propagated."] # [doc = ""] # [doc = " The limit argument is of type `Into<Option<usize>>`, and so can be"] # [doc = " provided as either `None`, `Some(10)`, or just `10`. Note: a limit of zero is"] # [doc = " interpreted as no limit at all, and will have the same result as passing in `None`."] # [doc = ""] # [doc = " The returned stream will be a stream of results, each containing either"] # [doc = " an error or a future's output. An error can be produced either by the"] # [doc = " underlying stream itself or by one of the futures it yielded."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Results are returned in the order of completion:"] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::oneshot;"] # [doc = " use futures::stream::{self, StreamExt, TryStreamExt};"] # [doc = ""] # [doc = " let (send_one, recv_one) = oneshot::channel();"] # [doc = " let (send_two, recv_two) = oneshot::channel();"] # [doc = ""] # [doc = " let stream_of_futures = stream::iter(vec![Ok(recv_one), Ok(recv_two)]);"] # [doc = ""] # [doc = " let mut buffered = stream_of_futures.try_buffer_unordered(10);"] # [doc = ""] # [doc = " send_two.send(2i32)?;"] # [doc = " assert_eq!(buffered.next().await, Some(Ok(2i32)));"] # [doc = ""] # [doc = " send_one.send(1i32)?;"] # [doc = " assert_eq!(buffered.next().await, Some(Ok(1i32)));"] # [doc = ""] # [doc = " assert_eq!(buffered.next().await, None);"] # [doc = " # Ok::<(), i32>(()) }).unwrap();"] # [doc = " ```"] # [doc = ""] # [doc = " Errors from the underlying stream itself are propagated:"] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::mpsc;"] # [doc = " use futures::stream::{StreamExt, TryStreamExt};"] # [doc = ""] # [doc = " let (sink, stream_of_futures) = mpsc::unbounded();"] # [doc = " let mut buffered = stream_of_futures.try_buffer_unordered(10);"] # [doc = ""] # [doc = " sink.unbounded_send(Ok(async { Ok(7i32) }))?;"] # [doc = " assert_eq!(buffered.next().await, Some(Ok(7i32)));"] # [doc = ""] # [doc = " sink.unbounded_send(Err(\"error in the stream\"))?;"] # [doc = " assert_eq!(buffered.next().await, Some(Err(\"error in the stream\")));"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] # [cfg_attr (target_os = "none" , cfg (target_has_atomic = "ptr"))] # [cfg (feature = "alloc")] fn try_buffer_unordered (self , n : impl Into < Option < usize > >) -> TryBufferUnordered < Self > where Self :: Ok : TryFuture < Error = Self :: Error > , Self : Sized , { assert_stream :: < Result < < Self :: Ok as TryFuture > :: Ok , Self :: Error > , _ > (TryBufferUnordered :: new (self , n . into ()) ,) } # [doc = " Attempt to execute several futures from a stream concurrently."] # [doc = ""] # [doc = " This stream's `Ok` type must be a [`TryFuture`](futures_core::future::TryFuture) with an `Error` type"] # [doc = " that matches the stream's `Error` type."] # [doc = ""] # [doc = " This adaptor will buffer up to `n` futures and then return their"] # [doc = " outputs in the same order as the underlying stream. If the underlying stream returns an error, it will"] # [doc = " be immediately propagated."] # [doc = ""] # [doc = " The limit argument is of type `Into<Option<usize>>`, and so can be"] # [doc = " provided as either `None`, `Some(10)`, or just `10`. Note: a limit of zero is"] # [doc = " interpreted as no limit at all, and will have the same result as passing in `None`."] # [doc = ""] # [doc = " The returned stream will be a stream of results, each containing either"] # [doc = " an error or a future's output. An error can be produced either by the"] # [doc = " underlying stream itself or by one of the futures it yielded."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Results are returned in the order of addition:"] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::oneshot;"] # [doc = " use futures::future::lazy;"] # [doc = " use futures::stream::{self, StreamExt, TryStreamExt};"] # [doc = ""] # [doc = " let (send_one, recv_one) = oneshot::channel();"] # [doc = " let (send_two, recv_two) = oneshot::channel();"] # [doc = ""] # [doc = " let mut buffered = lazy(move |cx| {"] # [doc = "     let stream_of_futures = stream::iter(vec![Ok(recv_one), Ok(recv_two)]);"] # [doc = ""] # [doc = "     let mut buffered = stream_of_futures.try_buffered(10);"] # [doc = ""] # [doc = "     assert!(buffered.try_poll_next_unpin(cx).is_pending());"] # [doc = ""] # [doc = "     send_two.send(2i32)?;"] # [doc = "     assert!(buffered.try_poll_next_unpin(cx).is_pending());"] # [doc = "     Ok::<_, i32>(buffered)"] # [doc = " }).await?;"] # [doc = ""] # [doc = " send_one.send(1i32)?;"] # [doc = " assert_eq!(buffered.next().await, Some(Ok(1i32)));"] # [doc = " assert_eq!(buffered.next().await, Some(Ok(2i32)));"] # [doc = ""] # [doc = " assert_eq!(buffered.next().await, None);"] # [doc = " # Ok::<(), i32>(()) }).unwrap();"] # [doc = " ```"] # [doc = ""] # [doc = " Errors from the underlying stream itself are propagated:"] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::mpsc;"] # [doc = " use futures::stream::{StreamExt, TryStreamExt};"] # [doc = ""] # [doc = " let (sink, stream_of_futures) = mpsc::unbounded();"] # [doc = " let mut buffered = stream_of_futures.try_buffered(10);"] # [doc = ""] # [doc = " sink.unbounded_send(Ok(async { Ok(7i32) }))?;"] # [doc = " assert_eq!(buffered.next().await, Some(Ok(7i32)));"] # [doc = ""] # [doc = " sink.unbounded_send(Err(\"error in the stream\"))?;"] # [doc = " assert_eq!(buffered.next().await, Some(Err(\"error in the stream\")));"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(()) }).unwrap();"] # [doc = " ```"] # [cfg_attr (target_os = "none" , cfg (target_has_atomic = "ptr"))] # [cfg (feature = "alloc")] fn try_buffered (self , n : impl Into < Option < usize > >) -> TryBuffered < Self > where Self :: Ok : TryFuture < Error = Self :: Error > , Self : Sized , { assert_stream :: < Result < < Self :: Ok as TryFuture > :: Ok , Self :: Error > , _ > (TryBuffered :: new (self , n . into () ,)) } # [doc = " A convenience method for calling [`TryStream::try_poll_next`] on [`Unpin`]"] # [doc = " stream types."] fn try_poll_next_unpin (& mut self , cx : & mut Context < '_ > ,) -> Poll < Option < Result < Self :: Ok , Self :: Error > > > where Self : Unpin , { Pin :: new (self) . try_poll_next (cx) } # [doc = " Wraps a [`TryStream`] into a stream compatible with libraries using"] # [doc = " futures 0.1 `Stream`. Requires the `compat` feature to be enabled."] # [doc = " ```"] # [doc = " # if cfg!(miri) { return; } // Miri does not support epoll"] # [doc = " use futures::future::{FutureExt, TryFutureExt};"] # [doc = " # let (tx, rx) = futures::channel::oneshot::channel();"] # [doc = ""] # [doc = " let future03 = async {"] # [doc = "     println!(\"Running on the pool\");"] # [doc = "     tx.send(42).unwrap();"] # [doc = " };"] # [doc = ""] # [doc = " let future01 = future03"] # [doc = "     .unit_error() // Make it a TryFuture"] # [doc = "     .boxed()  // Make it Unpin"] # [doc = "     .compat();"] # [doc = ""] # [doc = " tokio::run(future01);"] # [doc = " # assert_eq!(42, futures::executor::block_on(rx).unwrap());"] # [doc = " ```"] # [cfg (feature = "compat")] # [cfg_attr (docsrs , doc (cfg (feature = "compat")))] fn compat (self) -> Compat < Self > where Self : Sized + Unpin , { Compat :: new (self) } # [doc = " Adapter that converts this stream into an [`AsyncBufRead`](crate::io::AsyncBufRead)."] # [doc = ""] # [doc = " This method is only available when the `std` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, TryStreamExt};"] # [doc = " use futures::io::AsyncReadExt;"] # [doc = ""] # [doc = " let stream = stream::iter([Ok(vec![1, 2, 3]), Ok(vec![4, 5])]);"] # [doc = " let mut reader = stream.into_async_read();"] # [doc = ""] # [doc = " let mut buf = Vec::new();"] # [doc = " reader.read_to_end(&mut buf).await.unwrap();"] # [doc = " assert_eq!(buf, [1, 2, 3, 4, 5]);"] # [doc = " # })"] # [doc = " ```"] # [cfg (feature = "io")] # [cfg_attr (docsrs , doc (cfg (feature = "io")))] # [cfg (feature = "std")] fn into_async_read (self) -> IntoAsyncRead < Self > where Self : Sized + TryStreamExt < Error = std :: io :: Error > , Self :: Ok : AsRef < [u8] > , { crate :: io :: assert_read (IntoAsyncRead :: new (self)) } # [doc = " Attempt to execute a predicate over an asynchronous stream and evaluate if all items"] # [doc = " satisfy the predicate. Exits early if an `Err` is encountered or if an `Ok` item is found"] # [doc = " that does not satisfy the predicate."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt, TryStreamExt};"] # [doc = " use std::convert::Infallible;"] # [doc = ""] # [doc = " let number_stream = stream::iter(1..10).map(Ok::<_, Infallible>);"] # [doc = " let positive = number_stream.try_all(|i| async move { i > 0 });"] # [doc = " assert_eq!(positive.await, Ok(true));"] # [doc = ""] # [doc = " let stream_with_errors = stream::iter([Ok(1), Err(\"err\"), Ok(3)]);"] # [doc = " let positive = stream_with_errors.try_all(|i| async move { i > 0 });"] # [doc = " assert_eq!(positive.await, Err(\"err\"));"] # [doc = " # });"] # [doc = " ```"] fn try_all < Fut , F > (self , f : F) -> TryAll < Self , Fut , F > where Self : Sized , F : FnMut (Self :: Ok) -> Fut , Fut : Future < Output = bool > , { assert_future :: < Result < bool , Self :: Error > , _ > (TryAll :: new (self , f)) } # [doc = " Attempt to execute a predicate over an asynchronous stream and evaluate if any items"] # [doc = " satisfy the predicate. Exits early if an `Err` is encountered or if an `Ok` item is found"] # [doc = " that satisfies the predicate."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt, TryStreamExt};"] # [doc = " use std::convert::Infallible;"] # [doc = ""] # [doc = " let number_stream = stream::iter(0..10).map(Ok::<_, Infallible>);"] # [doc = " let contain_three = number_stream.try_any(|i| async move { i == 3 });"] # [doc = " assert_eq!(contain_three.await, Ok(true));"] # [doc = ""] # [doc = " let stream_with_errors = stream::iter([Ok(1), Err(\"err\"), Ok(3)]);"] # [doc = " let contain_three = stream_with_errors.try_any(|i| async move { i == 3 });"] # [doc = " assert_eq!(contain_three.await, Err(\"err\"));"] # [doc = " # });"] # [doc = " ```"] fn try_any < Fut , F > (self , f : F) -> TryAny < Self , Fut , F > where Self : Sized , F : FnMut (Self :: Ok) -> Fut , Fut : Future < Output = bool > , { assert_future :: < Result < bool , Self :: Error > , _ > (TryAny :: new (self , f)) } }
    };
}

TryStreamExt!()