macro_rules! deps {
    () => {
        Sink!();
        Next!();
        Either!();
        Send!();
        FuturesUnordered!();
        SplitSink!();
        SelectNextSome!();
        FlattenUnordered!();
        StreamFuture!();
        SplitStream!();
    };
}

macro_rules! StreamExt {
    () => {
        deps!();
        # [doc = " An extension trait for `Stream`s that provides a variety of convenient"] # [doc = " combinator functions."] pub trait StreamExt : Stream { # [doc = " Creates a future that resolves to the next item in the stream."] # [doc = ""] # [doc = " Note that because `next` doesn't take ownership over the stream,"] # [doc = " the [`Stream`] type must be [`Unpin`]. If you want to use `next` with a"] # [doc = " [`!Unpin`](Unpin) stream, you'll first have to pin the stream. This can"] # [doc = " be done by boxing the stream using [`Box::pin`] or"] # [doc = " pinning it to the stack using the `pin_mut!` macro from the `pin_utils`"] # [doc = " crate."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let mut stream = stream::iter(1..=3);"] # [doc = ""] # [doc = " assert_eq!(stream.next().await, Some(1));"] # [doc = " assert_eq!(stream.next().await, Some(2));"] # [doc = " assert_eq!(stream.next().await, Some(3));"] # [doc = " assert_eq!(stream.next().await, None);"] # [doc = " # });"] # [doc = " ```"] fn next (& mut self) -> Next < '_ , Self > where Self : Unpin , { assert_future :: < Option < Self :: Item > , _ > (Next :: new (self)) } # [doc = " Converts this stream into a future of `(next_item, tail_of_stream)`."] # [doc = " If the stream terminates, then the next item is [`None`]."] # [doc = ""] # [doc = " The returned future can be used to compose streams and futures together"] # [doc = " by placing everything into the \"world of futures\"."] # [doc = ""] # [doc = " Note that because `into_future` moves the stream, the [`Stream`] type"] # [doc = " must be [`Unpin`]. If you want to use `into_future` with a"] # [doc = " [`!Unpin`](Unpin) stream, you'll first have to pin the stream. This can"] # [doc = " be done by boxing the stream using [`Box::pin`] or"] # [doc = " pinning it to the stack using the `pin_mut!` macro from the `pin_utils`"] # [doc = " crate."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=3);"] # [doc = ""] # [doc = " let (item, stream) = stream.into_future().await;"] # [doc = " assert_eq!(Some(1), item);"] # [doc = ""] # [doc = " let (item, stream) = stream.into_future().await;"] # [doc = " assert_eq!(Some(2), item);"] # [doc = " # });"] # [doc = " ```"] fn into_future (self) -> StreamFuture < Self > where Self : Sized + Unpin , { assert_future :: < (Option < Self :: Item > , Self) , _ > (StreamFuture :: new (self)) } # [doc = " Maps this stream's items to a different type, returning a new stream of"] # [doc = " the resulting type."] # [doc = ""] # [doc = " The provided closure is executed over all elements of this stream as"] # [doc = " they are made available. It is executed inline with calls to"] # [doc = " [`poll_next`](Stream::poll_next)."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it, similar to the existing `map` methods in the"] # [doc = " standard library."] # [doc = ""] # [doc = " See [`StreamExt::then`](Self::then) if you want to use a closure that"] # [doc = " returns a future instead of a value."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=3);"] # [doc = " let stream = stream.map(|x| x + 3);"] # [doc = ""] # [doc = " assert_eq!(vec![4, 5, 6], stream.collect::<Vec<_>>().await);"] # [doc = " # });"] # [doc = " ```"] fn map < T , F > (self , f : F) -> Map < Self , F > where F : FnMut (Self :: Item) -> T , Self : Sized , { assert_stream :: < T , _ > (Map :: new (self , f)) } # [doc = " Creates a stream which gives the current iteration count as well as"] # [doc = " the next value."] # [doc = ""] # [doc = " The stream returned yields pairs `(i, val)`, where `i` is the"] # [doc = " current index of iteration and `val` is the value returned by the"] # [doc = " stream."] # [doc = ""] # [doc = " `enumerate()` keeps its count as a [`usize`]. If you want to count by a"] # [doc = " different sized integer, the [`zip`](StreamExt::zip) function provides similar"] # [doc = " functionality."] # [doc = ""] # [doc = " # Overflow Behavior"] # [doc = ""] # [doc = " The method does no guarding against overflows, so enumerating more than"] # [doc = " [`usize::MAX`] elements either produces the wrong result or panics. If"] # [doc = " debug assertions are enabled, a panic is guaranteed."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " The returned stream might panic if the to-be-returned index would"] # [doc = " overflow a [`usize`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(vec!['a', 'b', 'c']);"] # [doc = ""] # [doc = " let mut stream = stream.enumerate();"] # [doc = ""] # [doc = " assert_eq!(stream.next().await, Some((0, 'a')));"] # [doc = " assert_eq!(stream.next().await, Some((1, 'b')));"] # [doc = " assert_eq!(stream.next().await, Some((2, 'c')));"] # [doc = " assert_eq!(stream.next().await, None);"] # [doc = " # });"] # [doc = " ```"] fn enumerate (self) -> Enumerate < Self > where Self : Sized , { assert_stream :: < (usize , Self :: Item) , _ > (Enumerate :: new (self)) } # [doc = " Filters the values produced by this stream according to the provided"] # [doc = " asynchronous predicate."] # [doc = ""] # [doc = " As values of this stream are made available, the provided predicate `f`"] # [doc = " will be run against them. If the predicate returns a `Future` which"] # [doc = " resolves to `true`, then the stream will yield the value, but if the"] # [doc = " predicate returns a `Future` which resolves to `false`, then the value"] # [doc = " will be discarded and the next value will be produced."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it, similar to the existing `filter` methods in the"] # [doc = " standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=10);"] # [doc = " let events = stream.filter(|x| future::ready(x % 2 == 0));"] # [doc = ""] # [doc = " assert_eq!(vec![2, 4, 6, 8, 10], events.collect::<Vec<_>>().await);"] # [doc = " # });"] # [doc = " ```"] fn filter < Fut , F > (self , f : F) -> Filter < Self , Fut , F > where F : FnMut (& Self :: Item) -> Fut , Fut : Future < Output = bool > , Self : Sized , { assert_stream :: < Self :: Item , _ > (Filter :: new (self , f)) } # [doc = " Filters the values produced by this stream while simultaneously mapping"] # [doc = " them to a different type according to the provided asynchronous closure."] # [doc = ""] # [doc = " As values of this stream are made available, the provided function will"] # [doc = " be run on them. If the future returned by the predicate `f` resolves to"] # [doc = " [`Some(item)`](Some) then the stream will yield the value `item`, but if"] # [doc = " it resolves to [`None`] then the next value will be produced."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it, similar to the existing `filter_map` methods in"] # [doc = " the standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=10);"] # [doc = " let events = stream.filter_map(|x| async move {"] # [doc = "     if x % 2 == 0 { Some(x + 1) } else { None }"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!(vec![3, 5, 7, 9, 11], events.collect::<Vec<_>>().await);"] # [doc = " # });"] # [doc = " ```"] fn filter_map < Fut , T , F > (self , f : F) -> FilterMap < Self , Fut , F > where F : FnMut (Self :: Item) -> Fut , Fut : Future < Output = Option < T > > , Self : Sized , { assert_stream :: < T , _ > (FilterMap :: new (self , f)) } # [doc = " Computes from this stream's items new items of a different type using"] # [doc = " an asynchronous closure."] # [doc = ""] # [doc = " The provided closure `f` will be called with an `Item` once a value is"] # [doc = " ready, it returns a future which will then be run to completion"] # [doc = " to produce the next value on this stream."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " See [`StreamExt::map`](Self::map) if you want to use a closure that"] # [doc = " returns a value instead of a future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=3);"] # [doc = " let stream = stream.then(|x| async move { x + 3 });"] # [doc = ""] # [doc = " assert_eq!(vec![4, 5, 6], stream.collect::<Vec<_>>().await);"] # [doc = " # });"] # [doc = " ```"] fn then < Fut , F > (self , f : F) -> Then < Self , Fut , F > where F : FnMut (Self :: Item) -> Fut , Fut : Future , Self : Sized , { assert_stream :: < Fut :: Output , _ > (Then :: new (self , f)) } # [doc = " Transforms a stream into a collection, returning a"] # [doc = " future representing the result of that computation."] # [doc = ""] # [doc = " The returned future will be resolved when the stream terminates."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::mpsc;"] # [doc = " use futures::stream::StreamExt;"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let (tx, rx) = mpsc::unbounded();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     for i in 1..=5 {"] # [doc = "         tx.unbounded_send(i).unwrap();"] # [doc = "     }"] # [doc = " });"] # [doc = ""] # [doc = " let output = rx.collect::<Vec<i32>>().await;"] # [doc = " assert_eq!(output, vec![1, 2, 3, 4, 5]);"] # [doc = " # });"] # [doc = " ```"] fn collect < C : Default + Extend < Self :: Item > > (self) -> Collect < Self , C > where Self : Sized , { assert_future :: < C , _ > (Collect :: new (self)) } # [doc = " Converts a stream of pairs into a future, which"] # [doc = " resolves to pair of containers."] # [doc = ""] # [doc = " `unzip()` produces a future, which resolves to two"] # [doc = " collections: one from the left elements of the pairs,"] # [doc = " and one from the right elements."] # [doc = ""] # [doc = " The returned future will be resolved when the stream terminates."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::mpsc;"] # [doc = " use futures::stream::StreamExt;"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let (tx, rx) = mpsc::unbounded();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     tx.unbounded_send((1, 2)).unwrap();"] # [doc = "     tx.unbounded_send((3, 4)).unwrap();"] # [doc = "     tx.unbounded_send((5, 6)).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " let (o1, o2): (Vec<_>, Vec<_>) = rx.unzip().await;"] # [doc = " assert_eq!(o1, vec![1, 3, 5]);"] # [doc = " assert_eq!(o2, vec![2, 4, 6]);"] # [doc = " # });"] # [doc = " ```"] fn unzip < A , B , FromA , FromB > (self) -> Unzip < Self , FromA , FromB > where FromA : Default + Extend < A > , FromB : Default + Extend < B > , Self : Sized + Stream < Item = (A , B) > , { assert_future :: < (FromA , FromB) , _ > (Unzip :: new (self)) } # [doc = " Concatenate all items of a stream into a single extendable"] # [doc = " destination, returning a future representing the end result."] # [doc = ""] # [doc = " This combinator will extend the first item with the contents"] # [doc = " of all the subsequent results of the stream. If the stream is"] # [doc = " empty, the default value will be returned."] # [doc = ""] # [doc = " Works with all collections that implement the"] # [doc = " [`Extend`](std::iter::Extend) trait."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::mpsc;"] # [doc = " use futures::stream::StreamExt;"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let (tx, rx) = mpsc::unbounded();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     for i in (0..3).rev() {"] # [doc = "         let n = i * 3;"] # [doc = "         tx.unbounded_send(vec![n + 1, n + 2, n + 3]).unwrap();"] # [doc = "     }"] # [doc = " });"] # [doc = ""] # [doc = " let result = rx.concat().await;"] # [doc = ""] # [doc = " assert_eq!(result, vec![7, 8, 9, 4, 5, 6, 1, 2, 3]);"] # [doc = " # });"] # [doc = " ```"] fn concat (self) -> Concat < Self > where Self : Sized , Self :: Item : Extend < < < Self as Stream > :: Item as IntoIterator > :: Item > + IntoIterator + Default , { assert_future :: < Self :: Item , _ > (Concat :: new (self)) } # [doc = " Drives the stream to completion, counting the number of items."] # [doc = ""] # [doc = " # Overflow Behavior"] # [doc = ""] # [doc = " The method does no guarding against overflows, so counting elements of a"] # [doc = " stream with more than [`usize::MAX`] elements either produces the wrong"] # [doc = " result or panics. If debug assertions are enabled, a panic is guaranteed."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function might panic if the iterator has more than [`usize::MAX`]"] # [doc = " elements."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=10);"] # [doc = " let count = stream.count().await;"] # [doc = ""] # [doc = " assert_eq!(count, 10);"] # [doc = " # });"] # [doc = " ```"] fn count (self) -> Count < Self > where Self : Sized , { assert_future :: < usize , _ > (Count :: new (self)) } # [doc = " Repeats a stream endlessly."] # [doc = ""] # [doc = " The stream never terminates. Note that you likely want to avoid"] # [doc = " usage of `collect` or such on the returned stream as it will exhaust"] # [doc = " available memory as it tries to just fill up all RAM."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = " let a = [1, 2, 3];"] # [doc = " let mut s = stream::iter(a.iter()).cycle();"] # [doc = ""] # [doc = " assert_eq!(s.next().await, Some(&1));"] # [doc = " assert_eq!(s.next().await, Some(&2));"] # [doc = " assert_eq!(s.next().await, Some(&3));"] # [doc = " assert_eq!(s.next().await, Some(&1));"] # [doc = " assert_eq!(s.next().await, Some(&2));"] # [doc = " assert_eq!(s.next().await, Some(&3));"] # [doc = " assert_eq!(s.next().await, Some(&1));"] # [doc = " # });"] # [doc = " ```"] fn cycle (self) -> Cycle < Self > where Self : Sized + Clone , { assert_stream :: < Self :: Item , _ > (Cycle :: new (self)) } # [doc = " Execute an accumulating asynchronous computation over a stream,"] # [doc = " collecting all the values into one final result."] # [doc = ""] # [doc = " This combinator will accumulate all values returned by this stream"] # [doc = " according to the closure provided. The initial state is also provided to"] # [doc = " this method and then is returned again by each execution of the closure."] # [doc = " Once the entire stream has been exhausted the returned future will"] # [doc = " resolve to this value."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let number_stream = stream::iter(0..6);"] # [doc = " let sum = number_stream.fold(0, |acc, x| async move { acc + x });"] # [doc = " assert_eq!(sum.await, 15);"] # [doc = " # });"] # [doc = " ```"] fn fold < T , Fut , F > (self , init : T , f : F) -> Fold < Self , Fut , T , F > where F : FnMut (T , Self :: Item) -> Fut , Fut : Future < Output = T > , Self : Sized , { assert_future :: < T , _ > (Fold :: new (self , f , init)) } # [doc = " Execute predicate over asynchronous stream, and return `true` if any element in stream satisfied a predicate."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let number_stream = stream::iter(0..10);"] # [doc = " let contain_three = number_stream.any(|i| async move { i == 3 });"] # [doc = " assert_eq!(contain_three.await, true);"] # [doc = " # });"] # [doc = " ```"] fn any < Fut , F > (self , f : F) -> Any < Self , Fut , F > where F : FnMut (Self :: Item) -> Fut , Fut : Future < Output = bool > , Self : Sized , { assert_future :: < bool , _ > (Any :: new (self , f)) } # [doc = " Execute predicate over asynchronous stream, and return `true` if all element in stream satisfied a predicate."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let number_stream = stream::iter(0..10);"] # [doc = " let less_then_twenty = number_stream.all(|i| async move { i < 20 });"] # [doc = " assert_eq!(less_then_twenty.await, true);"] # [doc = " # });"] # [doc = " ```"] fn all < Fut , F > (self , f : F) -> All < Self , Fut , F > where F : FnMut (Self :: Item) -> Fut , Fut : Future < Output = bool > , Self : Sized , { assert_future :: < bool , _ > (All :: new (self , f)) } # [doc = " Flattens a stream of streams into just one continuous stream."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::mpsc;"] # [doc = " use futures::stream::StreamExt;"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let (tx1, rx1) = mpsc::unbounded();"] # [doc = " let (tx2, rx2) = mpsc::unbounded();"] # [doc = " let (tx3, rx3) = mpsc::unbounded();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     tx1.unbounded_send(1).unwrap();"] # [doc = "     tx1.unbounded_send(2).unwrap();"] # [doc = " });"] # [doc = " thread::spawn(move || {"] # [doc = "     tx2.unbounded_send(3).unwrap();"] # [doc = "     tx2.unbounded_send(4).unwrap();"] # [doc = " });"] # [doc = " thread::spawn(move || {"] # [doc = "     tx3.unbounded_send(rx1).unwrap();"] # [doc = "     tx3.unbounded_send(rx2).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " let output = rx3.flatten().collect::<Vec<i32>>().await;"] # [doc = " assert_eq!(output, vec![1, 2, 3, 4]);"] # [doc = " # });"] # [doc = " ```"] fn flatten (self) -> Flatten < Self > where Self :: Item : Stream , Self : Sized , { assert_stream :: < < Self :: Item as Stream > :: Item , _ > (Flatten :: new (self)) } # [doc = " Flattens a stream of streams into just one continuous stream. Polls"] # [doc = " inner streams produced by the base stream concurrently."] # [doc = ""] # [doc = " The only argument is an optional limit on the number of concurrently"] # [doc = " polled streams. If this limit is not `None`, no more than `limit` streams"] # [doc = " will be polled at the same time. The `limit` argument is of type"] # [doc = " `Into<Option<usize>>`, and so can be provided as either `None`,"] # [doc = " `Some(10)`, or just `10`. Note: a limit of zero is interpreted as"] # [doc = " no limit at all, and will have the same result as passing in `None`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::mpsc;"] # [doc = " use futures::stream::StreamExt;"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let (tx1, rx1) = mpsc::unbounded();"] # [doc = " let (tx2, rx2) = mpsc::unbounded();"] # [doc = " let (tx3, rx3) = mpsc::unbounded();"] # [doc = ""] # [doc = " thread::spawn(move || {"] # [doc = "     tx1.unbounded_send(1).unwrap();"] # [doc = "     tx1.unbounded_send(2).unwrap();"] # [doc = " });"] # [doc = " thread::spawn(move || {"] # [doc = "     tx2.unbounded_send(3).unwrap();"] # [doc = "     tx2.unbounded_send(4).unwrap();"] # [doc = " });"] # [doc = " thread::spawn(move || {"] # [doc = "     tx3.unbounded_send(rx1).unwrap();"] # [doc = "     tx3.unbounded_send(rx2).unwrap();"] # [doc = " });"] # [doc = ""] # [doc = " let mut output = rx3.flatten_unordered(None).collect::<Vec<i32>>().await;"] # [doc = " output.sort();"] # [doc = ""] # [doc = " assert_eq!(output, vec![1, 2, 3, 4]);"] # [doc = " # });"] # [doc = " ```"] # [cfg_attr (target_os = "none" , cfg (target_has_atomic = "ptr"))] # [cfg (feature = "alloc")] fn flatten_unordered (self , limit : impl Into < Option < usize > >) -> FlattenUnordered < Self > where Self :: Item : Stream + Unpin , Self : Sized , { assert_stream :: < < Self :: Item as Stream > :: Item , _ > (FlattenUnordered :: new (self , limit . into ())) } # [doc = " Maps a stream like [`StreamExt::map`] but flattens nested `Stream`s."] # [doc = ""] # [doc = " [`StreamExt::map`] is very useful, but if it produces a `Stream` instead,"] # [doc = " you would have to chain combinators like `.map(f).flatten()` while this"] # [doc = " combinator provides ability to write `.flat_map(f)` instead of chaining."] # [doc = ""] # [doc = " The provided closure which produces inner streams is executed over all elements"] # [doc = " of stream as last inner stream is terminated and next stream item is available."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it, similar to the existing `flat_map` methods in the"] # [doc = " standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=3);"] # [doc = " let stream = stream.flat_map(|x| stream::iter(vec![x + 3; x]));"] # [doc = ""] # [doc = " assert_eq!(vec![4, 5, 5, 6, 6, 6], stream.collect::<Vec<_>>().await);"] # [doc = " # });"] # [doc = " ```"] fn flat_map < U , F > (self , f : F) -> FlatMap < Self , U , F > where F : FnMut (Self :: Item) -> U , U : Stream , Self : Sized , { assert_stream :: < U :: Item , _ > (FlatMap :: new (self , f)) } # [doc = " Maps a stream like [`StreamExt::map`] but flattens nested `Stream`s"] # [doc = " and polls them concurrently, yielding items in any order, as they made"] # [doc = " available."] # [doc = ""] # [doc = " [`StreamExt::map`] is very useful, but if it produces `Stream`s"] # [doc = " instead, and you need to poll all of them concurrently, you would"] # [doc = " have to use something like `for_each_concurrent` and merge values"] # [doc = " by hand. This combinator provides ability to collect all values"] # [doc = " from concurrently polled streams into one stream."] # [doc = ""] # [doc = " The first argument is an optional limit on the number of concurrently"] # [doc = " polled streams. If this limit is not `None`, no more than `limit` streams"] # [doc = " will be polled at the same time. The `limit` argument is of type"] # [doc = " `Into<Option<usize>>`, and so can be provided as either `None`,"] # [doc = " `Some(10)`, or just `10`. Note: a limit of zero is interpreted as"] # [doc = " no limit at all, and will have the same result as passing in `None`."] # [doc = ""] # [doc = " The provided closure which produces inner streams is executed over"] # [doc = " all elements of stream as next stream item is available and limit"] # [doc = " of concurrently processed streams isn't exceeded."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and"] # [doc = " returns a wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..5);"] # [doc = " let stream = stream.flat_map_unordered(1, |x| stream::iter(vec![x; x]));"] # [doc = " let mut values = stream.collect::<Vec<_>>().await;"] # [doc = " values.sort();"] # [doc = ""] # [doc = " assert_eq!(vec![1usize, 2, 2, 3, 3, 3, 4, 4, 4, 4], values);"] # [doc = " # });"] # [doc = " ```"] # [cfg_attr (target_os = "none" , cfg (target_has_atomic = "ptr"))] # [cfg (feature = "alloc")] fn flat_map_unordered < U , F > (self , limit : impl Into < Option < usize > > , f : F ,) -> FlatMapUnordered < Self , U , F > where U : Stream + Unpin , F : FnMut (Self :: Item) -> U , Self : Sized , { assert_stream :: < U :: Item , _ > (FlatMapUnordered :: new (self , limit . into () , f)) } # [doc = " Combinator similar to [`StreamExt::fold`] that holds internal state"] # [doc = " and produces a new stream."] # [doc = ""] # [doc = " Accepts initial state and closure which will be applied to each element"] # [doc = " of the stream until provided closure returns `None`. Once `None` is"] # [doc = " returned, stream will be terminated."] # [doc = ""] # [doc = " Unlike [`Iterator::scan`], the closure takes the state by value instead of"] # [doc = " mutable reference to avoid [the limitation of the async"] # [doc = " block](https://github.com/rust-lang/futures-rs/issues/2171)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=10);"] # [doc = ""] # [doc = " let stream = stream.scan(0, |mut state, x| async move {"] # [doc = "     state += x;"] # [doc = "     if state < 10 {"] # [doc = "         Some((state, x))"] # [doc = "     } else {"] # [doc = "         None"] # [doc = "     }"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!(vec![1, 2, 3], stream.collect::<Vec<_>>().await);"] # [doc = " # });"] # [doc = " ```"] fn scan < S , B , Fut , F > (self , initial_state : S , f : F) -> Scan < Self , S , Fut , F > where F : FnMut (S , Self :: Item) -> Fut , Fut : Future < Output = Option < (S , B) > > , Self : Sized , { assert_stream :: < B , _ > (Scan :: new (self , initial_state , f)) } # [doc = " Skip elements on this stream while the provided asynchronous predicate"] # [doc = " resolves to `true`."] # [doc = ""] # [doc = " This function, like `Iterator::skip_while`, will skip elements on the"] # [doc = " stream until the predicate `f` resolves to `false`. Once one element"] # [doc = " returns `false`, all future elements will be returned from the underlying"] # [doc = " stream."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=10);"] # [doc = ""] # [doc = " let stream = stream.skip_while(|x| future::ready(*x <= 5));"] # [doc = ""] # [doc = " assert_eq!(vec![6, 7, 8, 9, 10], stream.collect::<Vec<_>>().await);"] # [doc = " # });"] # [doc = " ```"] fn skip_while < Fut , F > (self , f : F) -> SkipWhile < Self , Fut , F > where F : FnMut (& Self :: Item) -> Fut , Fut : Future < Output = bool > , Self : Sized , { assert_stream :: < Self :: Item , _ > (SkipWhile :: new (self , f)) } # [doc = " Take elements from this stream while the provided asynchronous predicate"] # [doc = " resolves to `true`."] # [doc = ""] # [doc = " This function, like `Iterator::take_while`, will take elements from the"] # [doc = " stream until the predicate `f` resolves to `false`. Once one element"] # [doc = " returns `false`, it will always return that the stream is done."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=10);"] # [doc = ""] # [doc = " let stream = stream.take_while(|x| future::ready(*x <= 5));"] # [doc = ""] # [doc = " assert_eq!(vec![1, 2, 3, 4, 5], stream.collect::<Vec<_>>().await);"] # [doc = " # });"] # [doc = " ```"] fn take_while < Fut , F > (self , f : F) -> TakeWhile < Self , Fut , F > where F : FnMut (& Self :: Item) -> Fut , Fut : Future < Output = bool > , Self : Sized , { assert_stream :: < Self :: Item , _ > (TakeWhile :: new (self , f)) } # [doc = " Take elements from this stream until the provided future resolves."] # [doc = ""] # [doc = " This function will take elements from the stream until the provided"] # [doc = " stopping future `fut` resolves. Once the `fut` future becomes ready,"] # [doc = " this stream combinator will always return that the stream is done."] # [doc = ""] # [doc = " The stopping future may return any type. Once the stream is stopped"] # [doc = " the result of the stopping future may be accessed with `TakeUntil::take_result()`."] # [doc = " The stream may also be resumed with `TakeUntil::take_future()`."] # [doc = " See the documentation of [`TakeUntil`] for more information."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = " use futures::task::Poll;"] # [doc = ""] # [doc = " let stream = stream::iter(1..=10);"] # [doc = ""] # [doc = " let mut i = 0;"] # [doc = " let stop_fut = future::poll_fn(|_cx| {"] # [doc = "     i += 1;"] # [doc = "     if i <= 5 {"] # [doc = "         Poll::Pending"] # [doc = "     } else {"] # [doc = "         Poll::Ready(())"] # [doc = "     }"] # [doc = " });"] # [doc = ""] # [doc = " let stream = stream.take_until(stop_fut);"] # [doc = ""] # [doc = " assert_eq!(vec![1, 2, 3, 4, 5], stream.collect::<Vec<_>>().await);"] # [doc = " # });"] # [doc = " ```"] fn take_until < Fut > (self , fut : Fut) -> TakeUntil < Self , Fut > where Fut : Future , Self : Sized , { assert_stream :: < Self :: Item , _ > (TakeUntil :: new (self , fut)) } # [doc = " Runs this stream to completion, executing the provided asynchronous"] # [doc = " closure for each element on the stream."] # [doc = ""] # [doc = " The closure provided will be called for each item this stream produces,"] # [doc = " yielding a future. That future will then be executed to completion"] # [doc = " before moving on to the next item."] # [doc = ""] # [doc = " The returned value is a `Future` where the `Output` type is `()`; it is"] # [doc = " executed entirely for its side effects."] # [doc = ""] # [doc = " To process each item in the stream and produce another stream instead"] # [doc = " of a single future, use `then` instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let mut x = 0;"] # [doc = ""] # [doc = " {"] # [doc = "     let fut = stream::repeat(1).take(3).for_each(|item| {"] # [doc = "         x += item;"] # [doc = "         future::ready(())"] # [doc = "     });"] # [doc = "     fut.await;"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(x, 3);"] # [doc = " # });"] # [doc = " ```"] fn for_each < Fut , F > (self , f : F) -> ForEach < Self , Fut , F > where F : FnMut (Self :: Item) -> Fut , Fut : Future < Output = () > , Self : Sized , { assert_future :: < () , _ > (ForEach :: new (self , f)) } # [doc = " Runs this stream to completion, executing the provided asynchronous"] # [doc = " closure for each element on the stream concurrently as elements become"] # [doc = " available."] # [doc = ""] # [doc = " This is similar to [`StreamExt::for_each`], but the futures"] # [doc = " produced by the closure are run concurrently (but not in parallel--"] # [doc = " this combinator does not introduce any threads)."] # [doc = ""] # [doc = " The closure provided will be called for each item this stream produces,"] # [doc = " yielding a future. That future will then be executed to completion"] # [doc = " concurrently with the other futures produced by the closure."] # [doc = ""] # [doc = " The first argument is an optional limit on the number of concurrent"] # [doc = " futures. If this limit is not `None`, no more than `limit` futures"] # [doc = " will be run concurrently. The `limit` argument is of type"] # [doc = " `Into<Option<usize>>`, and so can be provided as either `None`,"] # [doc = " `Some(10)`, or just `10`. Note: a limit of zero is interpreted as"] # [doc = " no limit at all, and will have the same result as passing in `None`."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::oneshot;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let (tx1, rx1) = oneshot::channel();"] # [doc = " let (tx2, rx2) = oneshot::channel();"] # [doc = " let (tx3, rx3) = oneshot::channel();"] # [doc = ""] # [doc = " let fut = stream::iter(vec![rx1, rx2, rx3]).for_each_concurrent("] # [doc = "     /* limit */ 2,"] # [doc = "     |rx| async move {"] # [doc = "         rx.await.unwrap();"] # [doc = "     }"] # [doc = " );"] # [doc = " tx1.send(()).unwrap();"] # [doc = " tx2.send(()).unwrap();"] # [doc = " tx3.send(()).unwrap();"] # [doc = " fut.await;"] # [doc = " # })"] # [doc = " ```"] # [cfg_attr (target_os = "none" , cfg (target_has_atomic = "ptr"))] # [cfg (feature = "alloc")] fn for_each_concurrent < Fut , F > (self , limit : impl Into < Option < usize > > , f : F ,) -> ForEachConcurrent < Self , Fut , F > where F : FnMut (Self :: Item) -> Fut , Fut : Future < Output = () > , Self : Sized , { assert_future :: < () , _ > (ForEachConcurrent :: new (self , limit . into () , f)) } # [doc = " Attempt to execute an accumulating asynchronous computation over a"] # [doc = " stream, collecting all the values into one final result."] # [doc = ""] # [doc = " This combinator will accumulate all values returned by this stream"] # [doc = " according to the closure provided. The initial state is also provided to"] # [doc = " this method and then is returned again by each execution of the closure."] # [doc = " Once the entire stream has been exhausted the returned future will"] # [doc = " resolve to this value."] # [doc = ""] # [doc = " This method is similar to [`fold`](crate::stream::StreamExt::fold), but"] # [doc = " will exit early if an error is encountered in the provided closure."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let number_stream = stream::iter(vec![1, 2]);"] # [doc = " let sum = number_stream.try_fold(0, |acc, x| async move { Ok::<i32, i32>(acc + x) });"] # [doc = " assert_eq!(sum.await, Ok(3));"] # [doc = ""] # [doc = " let number_stream_with_err = stream::iter(vec![Ok::<i32, i32>(1), Err(2), Ok(1)]);"] # [doc = " let sum = number_stream_with_err.try_fold(0, |acc, x| async move { Ok(acc + x?) });"] # [doc = " assert_eq!(sum.await, Err(2));"] # [doc = " # })"] # [doc = " ```"] fn try_fold < T , Fut , F > (self , init : T , f : F) -> TryFold < Self , Fut , T , F > where F : FnMut (T , Self :: Item) -> Fut , Fut : TryFuture < Ok = T > , Self : Sized , { assert_future :: < Result < T , Fut :: Error > , _ > (TryFold :: new (self , f , init)) } # [doc = " Attempts to run this stream to completion, executing the provided"] # [doc = " asynchronous closure for each element on the stream."] # [doc = ""] # [doc = " The provided closure will be called for each item this stream produces,"] # [doc = " yielding a future. That future will then be executed to completion"] # [doc = " before moving on to the next item."] # [doc = ""] # [doc = " The returned value is a [`Future`](futures_core::future::Future) where"] # [doc = " the [`Output`](futures_core::future::Future::Output) type is"] # [doc = " `Result<(), Fut::Error>`. If any of the intermediate futures returns"] # [doc = " an error, this future will return immediately with an error."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::future;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let mut x = 0i32;"] # [doc = ""] # [doc = " {"] # [doc = "     let fut = stream::repeat(1).try_for_each(|item| {"] # [doc = "         x += item;"] # [doc = "         future::ready(if x == 3 { Err(()) } else { Ok(()) })"] # [doc = "     });"] # [doc = "     assert_eq!(fut.await, Err(()));"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(x, 3);"] # [doc = " # })"] # [doc = " ```"] fn try_for_each < Fut , F > (self , f : F) -> TryForEach < Self , Fut , F > where F : FnMut (Self :: Item) -> Fut , Fut : TryFuture < Ok = () > , Self : Sized , { assert_future :: < Result < () , Fut :: Error > , _ > (TryForEach :: new (self , f)) } # [doc = " Attempts to run this stream to completion, executing the provided asynchronous"] # [doc = " closure for each element on the stream concurrently as elements become"] # [doc = " available, exiting as soon as an error occurs."] # [doc = ""] # [doc = " This is similar to"] # [doc = " [`StreamExt::for_each_concurrent`](crate::stream::StreamExt::for_each_concurrent),"] # [doc = " but will resolve to an error immediately if the provided closure returns"] # [doc = " an error."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::oneshot;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let (tx1, rx1) = oneshot::channel();"] # [doc = " let (tx2, rx2) = oneshot::channel();"] # [doc = " let (_tx3, rx3) = oneshot::channel();"] # [doc = ""] # [doc = " let stream = stream::iter(vec![rx1, rx2, rx3]);"] # [doc = " let fut = stream.try_for_each_concurrent("] # [doc = "     /* limit */ 2,"] # [doc = "     |rx| async move {"] # [doc = "         let res: Result<(), oneshot::Canceled> = rx.await;"] # [doc = "         res"] # [doc = "     }"] # [doc = " );"] # [doc = ""] # [doc = " tx1.send(()).unwrap();"] # [doc = " // Drop the second sender so that `rx2` resolves to `Canceled`."] # [doc = " drop(tx2);"] # [doc = ""] # [doc = " // The final result is an error because the second future"] # [doc = " // resulted in an error."] # [doc = " assert_eq!(Err(oneshot::Canceled), fut.await);"] # [doc = " # })"] # [doc = " ```"] # [cfg_attr (target_os = "none" , cfg (target_has_atomic = "ptr"))] # [cfg (feature = "alloc")] fn try_for_each_concurrent < Fut , F , E > (self , limit : impl Into < Option < usize > > , f : F ,) -> TryForEachConcurrent < Self , Fut , F > where F : FnMut (Self :: Item) -> Fut , Fut : Future < Output = Result < () , E > > , Self : Sized , { assert_future :: < Result < () , E > , _ > (TryForEachConcurrent :: new (self , limit . into () , f)) } # [doc = " Creates a new stream of at most `n` items of the underlying stream."] # [doc = ""] # [doc = " Once `n` items have been yielded from this stream then it will always"] # [doc = " return that the stream is done."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=10).take(3);"] # [doc = ""] # [doc = " assert_eq!(vec![1, 2, 3], stream.collect::<Vec<_>>().await);"] # [doc = " # });"] # [doc = " ```"] fn take (self , n : usize) -> Take < Self > where Self : Sized , { assert_stream :: < Self :: Item , _ > (Take :: new (self , n)) } # [doc = " Creates a new stream which skips `n` items of the underlying stream."] # [doc = ""] # [doc = " Once `n` items have been skipped from this stream then it will always"] # [doc = " return the remaining items on this stream."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=10).skip(5);"] # [doc = ""] # [doc = " assert_eq!(vec![6, 7, 8, 9, 10], stream.collect::<Vec<_>>().await);"] # [doc = " # });"] # [doc = " ```"] fn skip (self , n : usize) -> Skip < Self > where Self : Sized , { assert_stream :: < Self :: Item , _ > (Skip :: new (self , n)) } # [doc = " Fuse a stream such that [`poll_next`](Stream::poll_next) will never"] # [doc = " again be called once it has finished. This method can be used to turn"] # [doc = " any `Stream` into a `FusedStream`."] # [doc = ""] # [doc = " Normally, once a stream has returned [`None`] from"] # [doc = " [`poll_next`](Stream::poll_next) any further calls could exhibit bad"] # [doc = " behavior such as block forever, panic, never return, etc. If it is known"] # [doc = " that [`poll_next`](Stream::poll_next) may be called after stream"] # [doc = " has already finished, then this method can be used to ensure that it has"] # [doc = " defined semantics."] # [doc = ""] # [doc = " The [`poll_next`](Stream::poll_next) method of a `fuse`d stream"] # [doc = " is guaranteed to return [`None`] after the underlying stream has"] # [doc = " finished."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::executor::block_on_stream;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = " use futures::task::Poll;"] # [doc = ""] # [doc = " let mut x = 0;"] # [doc = " let stream = stream::poll_fn(|_| {"] # [doc = "     x += 1;"] # [doc = "     match x {"] # [doc = "         0..=2 => Poll::Ready(Some(x)),"] # [doc = "         3 => Poll::Ready(None),"] # [doc = "         _ => panic!(\"should not happen\")"] # [doc = "     }"] # [doc = " }).fuse();"] # [doc = ""] # [doc = " let mut iter = block_on_stream(stream);"] # [doc = " assert_eq!(Some(1), iter.next());"] # [doc = " assert_eq!(Some(2), iter.next());"] # [doc = " assert_eq!(None, iter.next());"] # [doc = " assert_eq!(None, iter.next());"] # [doc = " // ..."] # [doc = " ```"] fn fuse (self) -> Fuse < Self > where Self : Sized , { assert_stream :: < Self :: Item , _ > (Fuse :: new (self)) } # [doc = " Borrows a stream, rather than consuming it."] # [doc = ""] # [doc = " This is useful to allow applying stream adaptors while still retaining"] # [doc = " ownership of the original stream."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let mut stream = stream::iter(1..5);"] # [doc = ""] # [doc = " let sum = stream.by_ref()"] # [doc = "                 .take(2)"] # [doc = "                 .fold(0, |a, b| async move { a + b })"] # [doc = "                 .await;"] # [doc = " assert_eq!(sum, 3);"] # [doc = ""] # [doc = " // You can use the stream again"] # [doc = " let sum = stream.take(2)"] # [doc = "                 .fold(0, |a, b| async move { a + b })"] # [doc = "                 .await;"] # [doc = " assert_eq!(sum, 7);"] # [doc = " # });"] # [doc = " ```"] fn by_ref (& mut self) -> & mut Self { self } # [doc = " Catches unwinding panics while polling the stream."] # [doc = ""] # [doc = " Caught panic (if any) will be the last element of the resulting stream."] # [doc = ""] # [doc = " In general, panics within a stream can propagate all the way out to the"] # [doc = " task level. This combinator makes it possible to halt unwinding within"] # [doc = " the stream itself. It's most commonly used within task executors. This"] # [doc = " method should not be used for error handling."] # [doc = ""] # [doc = " Note that this method requires the `UnwindSafe` bound from the standard"] # [doc = " library. This isn't always applied automatically, and the standard"] # [doc = " library provides an `AssertUnwindSafe` wrapper type to apply it"] # [doc = " after-the fact. To assist using this method, the [`Stream`] trait is"] # [doc = " also implemented for `AssertUnwindSafe<St>` where `St` implements"] # [doc = " [`Stream`]."] # [doc = ""] # [doc = " This method is only available when the `std` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(vec![Some(10), None, Some(11)]);"] # [doc = " // Panic on second element"] # [doc = " let stream_panicking = stream.map(|o| o.unwrap());"] # [doc = " // Collect all the results"] # [doc = " let stream = stream_panicking.catch_unwind();"] # [doc = ""] # [doc = " let results: Vec<Result<i32, _>> = stream.collect().await;"] # [doc = " match results[0] {"] # [doc = "     Ok(10) => {}"] # [doc = "     _ => panic!(\"unexpected result!\"),"] # [doc = " }"] # [doc = " assert!(results[1].is_err());"] # [doc = " assert_eq!(results.len(), 2);"] # [doc = " # });"] # [doc = " ```"] # [cfg (feature = "std")] fn catch_unwind (self) -> CatchUnwind < Self > where Self : Sized + std :: panic :: UnwindSafe , { assert_stream (CatchUnwind :: new (self)) } # [doc = " Wrap the stream in a Box, pinning it."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [cfg (feature = "alloc")] fn boxed < 'a > (self) -> BoxStream < 'a , Self :: Item > where Self : Sized + Send + 'a , { assert_stream :: < Self :: Item , _ > (Box :: pin (self)) } # [doc = " Wrap the stream in a Box, pinning it."] # [doc = ""] # [doc = " Similar to `boxed`, but without the `Send` requirement."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [cfg (feature = "alloc")] fn boxed_local < 'a > (self) -> LocalBoxStream < 'a , Self :: Item > where Self : Sized + 'a , { assert_stream :: < Self :: Item , _ > (Box :: pin (self)) } # [doc = " An adaptor for creating a buffered list of pending futures."] # [doc = ""] # [doc = " If this stream's item can be converted into a future, then this adaptor"] # [doc = " will buffer up to at most `n` futures and then return the outputs in the"] # [doc = " same order as the underlying stream. No more than `n` futures will be"] # [doc = " buffered at any point in time, and less than `n` may also be buffered"] # [doc = " depending on the state of each future."] # [doc = ""] # [doc = " The limit argument is of type `Into<Option<usize>>`, and so can be"] # [doc = " provided as either `None`, `Some(10)`, or just `10`. Note: a limit of zero is"] # [doc = " interpreted as no limit at all, and will have the same result as passing in `None`."] # [doc = ""] # [doc = " The returned stream will be a stream of each future's output."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [cfg_attr (target_os = "none" , cfg (target_has_atomic = "ptr"))] # [cfg (feature = "alloc")] fn buffered (self , n : impl Into < Option < usize > >) -> Buffered < Self > where Self :: Item : Future , Self : Sized , { assert_stream :: < < Self :: Item as Future > :: Output , _ > (Buffered :: new (self , n . into ())) } # [doc = " An adaptor for creating a buffered list of pending futures (unordered)."] # [doc = ""] # [doc = " If this stream's item can be converted into a future, then this adaptor"] # [doc = " will buffer up to `n` futures and then return the outputs in the order"] # [doc = " in which they complete. No more than `n` futures will be buffered at"] # [doc = " any point in time, and less than `n` may also be buffered depending on"] # [doc = " the state of each future."] # [doc = ""] # [doc = " The limit argument is of type `Into<Option<usize>>`, and so can be"] # [doc = " provided as either `None`, `Some(10)`, or just `10`. Note: a limit of zero is"] # [doc = " interpreted as no limit at all, and will have the same result as passing in `None`."] # [doc = ""] # [doc = " The returned stream will be a stream of each future's output."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::oneshot;"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let (send_one, recv_one) = oneshot::channel();"] # [doc = " let (send_two, recv_two) = oneshot::channel();"] # [doc = ""] # [doc = " let stream_of_futures = stream::iter(vec![recv_one, recv_two]);"] # [doc = " let mut buffered = stream_of_futures.buffer_unordered(10);"] # [doc = ""] # [doc = " send_two.send(2i32)?;"] # [doc = " assert_eq!(buffered.next().await, Some(Ok(2i32)));"] # [doc = ""] # [doc = " send_one.send(1i32)?;"] # [doc = " assert_eq!(buffered.next().await, Some(Ok(1i32)));"] # [doc = ""] # [doc = " assert_eq!(buffered.next().await, None);"] # [doc = " # Ok::<(), i32>(()) }).unwrap();"] # [doc = " ```"] # [cfg_attr (target_os = "none" , cfg (target_has_atomic = "ptr"))] # [cfg (feature = "alloc")] fn buffer_unordered (self , n : impl Into < Option < usize > >) -> BufferUnordered < Self > where Self :: Item : Future , Self : Sized , { assert_stream :: < < Self :: Item as Future > :: Output , _ > (BufferUnordered :: new (self , n . into ())) } # [doc = " An adapter for zipping two streams together."] # [doc = ""] # [doc = " The zipped stream waits for both streams to produce an item, and then"] # [doc = " returns that pair. If either stream ends then the zipped stream will"] # [doc = " also end."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream1 = stream::iter(1..=3);"] # [doc = " let stream2 = stream::iter(5..=10);"] # [doc = ""] # [doc = " let vec = stream1.zip(stream2)"] # [doc = "                  .collect::<Vec<_>>()"] # [doc = "                  .await;"] # [doc = " assert_eq!(vec![(1, 5), (2, 6), (3, 7)], vec);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] fn zip < St > (self , other : St) -> Zip < Self , St > where St : Stream , Self : Sized , { assert_stream :: < (Self :: Item , St :: Item) , _ > (Zip :: new (self , other)) } # [doc = " Adapter for chaining two streams."] # [doc = ""] # [doc = " The resulting stream emits elements from the first stream, and when"] # [doc = " first stream reaches the end, emits the elements from the second stream."] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::stream::{self, StreamExt};"] # [doc = ""] # [doc = " let stream1 = stream::iter(vec![Ok(10), Err(false)]);"] # [doc = " let stream2 = stream::iter(vec![Err(true), Ok(20)]);"] # [doc = ""] # [doc = " let stream = stream1.chain(stream2);"] # [doc = ""] # [doc = " let result: Vec<_> = stream.collect().await;"] # [doc = " assert_eq!(result, vec!["] # [doc = "     Ok(10),"] # [doc = "     Err(false),"] # [doc = "     Err(true),"] # [doc = "     Ok(20),"] # [doc = " ]);"] # [doc = " # });"] # [doc = " ```"] fn chain < St > (self , other : St) -> Chain < Self , St > where St : Stream < Item = Self :: Item > , Self : Sized , { assert_stream :: < Self :: Item , _ > (Chain :: new (self , other)) } # [doc = " Creates a new stream which exposes a `peek` method."] # [doc = ""] # [doc = " Calling `peek` returns a reference to the next item in the stream."] fn peekable (self) -> Peekable < Self > where Self : Sized , { assert_stream :: < Self :: Item , _ > (Peekable :: new (self)) } # [doc = " An adaptor for chunking up items of the stream inside a vector."] # [doc = ""] # [doc = " This combinator will attempt to pull items from this stream and buffer"] # [doc = " them into a local vector. At most `capacity` items will get buffered"] # [doc = " before they're yielded from the returned stream."] # [doc = ""] # [doc = " Note that the vectors returned from this iterator may not always have"] # [doc = " `capacity` elements. If the underlying stream ended and only a partial"] # [doc = " vector was created, it'll be returned. Additionally if an error happens"] # [doc = " from the underlying stream then the currently buffered items will be"] # [doc = " yielded."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This method will panic if `capacity` is zero."] # [cfg (feature = "alloc")] fn chunks (self , capacity : usize) -> Chunks < Self > where Self : Sized , { assert_stream :: < Vec < Self :: Item > , _ > (Chunks :: new (self , capacity)) } # [doc = " An adaptor for chunking up ready items of the stream inside a vector."] # [doc = ""] # [doc = " This combinator will attempt to pull ready items from this stream and"] # [doc = " buffer them into a local vector. At most `capacity` items will get"] # [doc = " buffered before they're yielded from the returned stream. If underlying"] # [doc = " stream returns `Poll::Pending`, and collected chunk is not empty, it will"] # [doc = " be immediately returned."] # [doc = ""] # [doc = " If the underlying stream ended and only a partial vector was created,"] # [doc = " it will be returned."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This method will panic if `capacity` is zero."] # [cfg (feature = "alloc")] fn ready_chunks (self , capacity : usize) -> ReadyChunks < Self > where Self : Sized , { assert_stream :: < Vec < Self :: Item > , _ > (ReadyChunks :: new (self , capacity)) } # [doc = " A future that completes after the given stream has been fully processed"] # [doc = " into the sink and the sink has been flushed and closed."] # [doc = ""] # [doc = " This future will drive the stream to keep producing items until it is"] # [doc = " exhausted, sending each item to the sink. It will complete once the"] # [doc = " stream is exhausted, the sink has received and flushed all items, and"] # [doc = " the sink is closed. Note that neither the original stream nor provided"] # [doc = " sink will be output by this future. Pass the sink by `Pin<&mut S>`"] # [doc = " (for example, via `forward(&mut sink)` inside an `async` fn/block) in"] # [doc = " order to preserve access to the `Sink`."] # [cfg (feature = "sink")] # [cfg_attr (docsrs , doc (cfg (feature = "sink")))] fn forward < S > (self , sink : S) -> Forward < Self , S > where S : Sink < Self :: Item > , Self : Sized , { assert_future :: < Result < () , S :: Error > , _ > (Forward :: new (self , sink)) } # [doc = " Splits this `Stream + Sink` object into separate `Sink` and `Stream`"] # [doc = " objects."] # [doc = ""] # [doc = " This can be useful when you want to split ownership between tasks, or"] # [doc = " allow direct interaction between the two objects (e.g. via"] # [doc = " `Sink::send_all`)."] # [doc = ""] # [doc = " This method is only available when the `std` or `alloc` feature of this"] # [doc = " library is activated, and it is activated by default."] # [cfg (feature = "sink")] # [cfg_attr (docsrs , doc (cfg (feature = "sink")))] # [cfg_attr (target_os = "none" , cfg (target_has_atomic = "ptr"))] # [cfg (feature = "alloc")] fn split < Item > (self) -> (SplitSink < Self , Item > , SplitStream < Self >) where Self : Sink < Item > + Sized , { let (sink , stream) = split :: split (self) ; (crate :: sink :: assert_sink :: < Item , Self :: Error , _ > (sink) , assert_stream :: < Self :: Item , _ > (stream) ,) } # [doc = " Do something with each item of this stream, afterwards passing it on."] # [doc = ""] # [doc = " This is similar to the `Iterator::inspect` method in the standard"] # [doc = " library where it allows easily inspecting each value as it passes"] # [doc = " through the stream, for example to debug what's going on."] fn inspect < F > (self , f : F) -> Inspect < Self , F > where F : FnMut (& Self :: Item) , Self : Sized , { assert_stream :: < Self :: Item , _ > (Inspect :: new (self , f)) } # [doc = " Wrap this stream in an `Either` stream, making it the left-hand variant"] # [doc = " of that `Either`."] # [doc = ""] # [doc = " This can be used in combination with the `right_stream` method to write `if`"] # [doc = " statements that evaluate to different streams in different branches."] fn left_stream < B > (self) -> Either < Self , B > where B : Stream < Item = Self :: Item > , Self : Sized , { assert_stream :: < Self :: Item , _ > (Either :: Left (self)) } # [doc = " Wrap this stream in an `Either` stream, making it the right-hand variant"] # [doc = " of that `Either`."] # [doc = ""] # [doc = " This can be used in combination with the `left_stream` method to write `if`"] # [doc = " statements that evaluate to different streams in different branches."] fn right_stream < B > (self) -> Either < B , Self > where B : Stream < Item = Self :: Item > , Self : Sized , { assert_stream :: < Self :: Item , _ > (Either :: Right (self)) } # [doc = " A convenience method for calling [`Stream::poll_next`] on [`Unpin`]"] # [doc = " stream types."] fn poll_next_unpin (& mut self , cx : & mut Context < '_ >) -> Poll < Option < Self :: Item > > where Self : Unpin , { Pin :: new (self) . poll_next (cx) } # [doc = " Returns a [`Future`] that resolves when the next item in this stream is"] # [doc = " ready."] # [doc = ""] # [doc = " This is similar to the [`next`][StreamExt::next] method, but it won't"] # [doc = " resolve to [`None`] if used on an empty [`Stream`]. Instead, the"] # [doc = " returned future type will return `true` from"] # [doc = " [`FusedFuture::is_terminated`][] when the [`Stream`] is empty, allowing"] # [doc = " [`select_next_some`][StreamExt::select_next_some] to be easily used with"] # [doc = " the [`select!`] macro."] # [doc = ""] # [doc = " If the future is polled after this [`Stream`] is empty it will panic."] # [doc = " Using the future with a [`FusedFuture`][]-aware primitive like the"] # [doc = " [`select!`] macro will prevent this."] # [doc = ""] # [doc = " [`FusedFuture`]: futures_core::future::FusedFuture"] # [doc = " [`FusedFuture::is_terminated`]: futures_core::future::FusedFuture::is_terminated"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::{future, select};"] # [doc = " use futures::stream::{StreamExt, FuturesUnordered};"] # [doc = ""] # [doc = " let mut fut = future::ready(1);"] # [doc = " let mut async_tasks = FuturesUnordered::new();"] # [doc = " let mut total = 0;"] # [doc = " loop {"] # [doc = "     select! {"] # [doc = "         num = fut => {"] # [doc = "             // First, the `ready` future completes."] # [doc = "             total += num;"] # [doc = "             // Then we spawn a new task onto `async_tasks`,"] # [doc = "             async_tasks.push(async { 5 });"] # [doc = "         },"] # [doc = "         // On the next iteration of the loop, the task we spawned"] # [doc = "         // completes."] # [doc = "         num = async_tasks.select_next_some() => {"] # [doc = "             total += num;"] # [doc = "         }"] # [doc = "         // Finally, both the `ready` future and `async_tasks` have"] # [doc = "         // finished, so we enter the `complete` branch."] # [doc = "         complete => break,"] # [doc = "     }"] # [doc = " }"] # [doc = " assert_eq!(total, 6);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " [`select!`]: crate::select"] fn select_next_some (& mut self) -> SelectNextSome < '_ , Self > where Self : Unpin + FusedStream , { assert_future :: < Self :: Item , _ > (SelectNextSome :: new (self)) } }
    };
}

StreamExt!();