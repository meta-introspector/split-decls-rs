// Generated macro for Focus (enum)
macro_rules! Depcrate_vector_focusFocus {
() => {
// Module: crate::vector::focus
// Provides: {"Focus"}
// Dependencies: {}
# [doc = " Focused indexing over a [`Vector`][Vector]."] # [doc = ""] # [doc = " By remembering the last tree node accessed through an index lookup and the"] # [doc = " path we took to get there, we can speed up lookups for adjacent indices"] # [doc = " tremendously. Lookups on indices in the same node are instantaneous, and"] # [doc = " lookups on sibling nodes are also very fast."] # [doc = ""] # [doc = " A `Focus` can also be used as a restricted view into a vector, using the"] # [doc = " [`narrow`][narrow] and [`split_at`][split_at] methods."] # [doc = ""] # [doc = " # When should I use a `Focus` for better performance?"] # [doc = ""] # [doc = " `Focus` is useful when you need to perform a large number of index lookups"] # [doc = " that are more likely than not to be close to each other. It's usually worth"] # [doc = " using a `Focus` in any situation where you're batching a lot of index"] # [doc = " lookups together, even if they're not obviously adjacent - there's likely"] # [doc = " to be some performance gain for even completely random access."] # [doc = ""] # [doc = " If you're just iterating forwards or backwards over the [`Vector`][Vector]"] # [doc = " in order, you're better off with a regular iterator, which, in fact, is"] # [doc = " implemented using a `Focus`, but provides a simpler interface."] # [doc = ""] # [doc = " If you're just doing a very small number of index lookups, the setup cost"] # [doc = " for the `Focus` is probably not worth it."] # [doc = ""] # [doc = " A `Focus` is never faster than an index lookup on a small [`Vector`][Vector]"] # [doc = " with a length below the internal RRB tree's branching factor of 64."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " This example is contrived, as the better way to iterate forwards or"] # [doc = " backwards over a vector is with an actual iterator. Even so, the version"] # [doc = " using a `Focus` should run nearly an order of magnitude faster than the"] # [doc = " version using index lookups at a length of 1000. It should also be noted"] # [doc = " that [`vector::Iter`][Iter] is actually implemented using a `Focus` behind"] # [doc = " the scenes, so the performance of the two should be identical."] # [doc = ""] # [doc = " ```rust"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " # use im::vector::Vector;"] # [doc = " # use std::iter::FromIterator;"] # [doc = " let mut vec: Vector<i64> = Vector::from_iter(0..1000);"] # [doc = ""] # [doc = " // Summing a vector, the slow way:"] # [doc = " let mut sum = 0;"] # [doc = " for i in 0..1000 {"] # [doc = "     sum += *vec.get(i).unwrap();"] # [doc = " }"] # [doc = " assert_eq!(499500, sum);"] # [doc = ""] # [doc = " // Summing a vector faster using a Focus:"] # [doc = " let mut sum = 0;"] # [doc = " let mut focus = vec.focus();"] # [doc = " for i in 0..1000 {"] # [doc = "     sum += *focus.get(i).unwrap();"] # [doc = " }"] # [doc = " assert_eq!(499500, sum);"] # [doc = ""] # [doc = " // And the easy way, for completeness:"] # [doc = " let sum: i64 = vec.iter().sum();"] # [doc = " assert_eq!(499500, sum);"] # [doc = " ```"] # [doc = ""] # [doc = " [Vector]: enum.Vector.html"] # [doc = " [Iter]: struct.Iter.html"] # [doc = " [narrow]: #method.narrow"] # [doc = " [split_at]: #method.split_at"] pub enum Focus < 'a , A > { # [doc (hidden)] Single (& 'a [A]) , # [doc (hidden)] Full (TreeFocus < A >) , }
};
}
