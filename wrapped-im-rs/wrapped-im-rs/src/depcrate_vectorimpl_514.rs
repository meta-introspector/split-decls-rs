// Generated macro for impl_514 (impl)
macro_rules! Depcrate_vectorimpl_514 {
() => {
// Module: crate::vector
// Provides: {"impl_514"}
// Dependencies: {}
impl < A : Clone > Vector < A > { # [doc = " Get a reference to the memory pool this `Vector` is using."] # [doc = ""] # [doc = " Note that if you didn't specifically construct it with a pool, you'll"] # [doc = " get back a reference to a pool of size 0."] # [cfg_attr (not (feature = "pool") , doc (hidden))] pub fn pool (& self) -> & RRBPool < A > { match self . vector { Inline (ref pool , _) => pool , Single (ref pool , _) => pool , Full (ref pool , _) => pool , } } # [doc = " True if a vector is a full inline or single chunk, ie. must be promoted"] # [doc = " to grow further."] fn needs_promotion (& self) -> bool { match & self . vector { Inline (_ , chunk) if chunk . is_full () => true , Single (_ , chunk) if chunk . is_full () => true , _ => false , } } # [doc = " Promote an inline to a single."] fn promote_inline (& mut self) { if let Inline (pool , chunk) = & mut self . vector { self . vector = Single (pool . clone () , PoolRef :: new (& pool . value_pool , chunk . into ())) ; } } # [doc = " Promote a single to a full, with the single chunk becoming inner_f, or"] # [doc = " promote an inline to a single."] fn promote_front (& mut self) { self . vector = match & mut self . vector { Inline (pool , chunk) => { Single (pool . clone () , PoolRef :: new (& pool . value_pool , chunk . into ())) } Single (pool , chunk) => { let chunk = chunk . clone () ; Full (pool . clone () , Rrb { length : chunk . len () , middle_level : 0 , outer_f : PoolRef :: default (& pool . value_pool) , inner_f : chunk , middle : Ref :: new (Node :: new ()) , inner_b : PoolRef :: default (& pool . value_pool) , outer_b : PoolRef :: default (& pool . value_pool) , } ,) } Full (_ , _) => return , } } # [doc = " Promote a single to a full, with the single chunk becoming inner_b, or"] # [doc = " promote an inline to a single."] fn promote_back (& mut self) { self . vector = match & mut self . vector { Inline (pool , chunk) => { Single (pool . clone () , PoolRef :: new (& pool . value_pool , chunk . into ())) } Single (pool , chunk) => { let chunk = chunk . clone () ; Full (pool . clone () , Rrb { length : chunk . len () , middle_level : 0 , outer_f : PoolRef :: default (& pool . value_pool) , inner_f : PoolRef :: default (& pool . value_pool) , middle : Ref :: new (Node :: new ()) , inner_b : chunk , outer_b : PoolRef :: default (& pool . value_pool) , } ,) } Full (_ , _) => return , } } # [doc = " Construct an empty vector."] # [must_use] pub fn new () -> Self { Self { vector : Inline (RRBPool :: default () , InlineArray :: new ()) , } } # [doc = " Construct an empty vector using a specific memory pool."] # [cfg (feature = "pool")] # [must_use] pub fn with_pool (pool : & RRBPool < A >) -> Self { Self { vector : Inline (pool . clone () , InlineArray :: new ()) , } } # [doc = " Get the length of a vector."] # [doc = ""] # [doc = " Time: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " assert_eq!(5, vector![1, 2, 3, 4, 5].len());"] # [doc = " ```"] # [inline] # [must_use] pub fn len (& self) -> usize { match & self . vector { Inline (_ , chunk) => chunk . len () , Single (_ , chunk) => chunk . len () , Full (_ , tree) => tree . length , } } # [doc = " Test whether a vector is empty."] # [doc = ""] # [doc = " Time: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " # use im::Vector;"] # [doc = " let vec = vector![\"Joe\", \"Mike\", \"Robert\"];"] # [doc = " assert_eq!(false, vec.is_empty());"] # [doc = " assert_eq!(true, Vector::<i32>::new().is_empty());"] # [doc = " ```"] # [inline] # [must_use] pub fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Test whether a vector is currently inlined."] # [doc = ""] # [doc = " Vectors small enough that their contents could be stored entirely inside"] # [doc = " the space of `std::mem::size_of::<Vector<A>>()` bytes are stored inline on"] # [doc = " the stack instead of allocating any chunks. This method returns `true` if"] # [doc = " this vector is currently inlined, or `false` if it currently has chunks allocated"] # [doc = " on the heap."] # [doc = ""] # [doc = " This may be useful in conjunction with [`ptr_eq()`][ptr_eq], which checks if"] # [doc = " two vectors' heap allocations are the same, and thus will never return `true`"] # [doc = " for inlined vectors."] # [doc = ""] # [doc = " Time: O(1)"] # [doc = ""] # [doc = " [ptr_eq]: #method.ptr_eq"] # [inline] # [must_use] pub fn is_inline (& self) -> bool { matches ! (& self . vector , Inline (_ , _)) } # [doc = " Test whether two vectors refer to the same content in memory."] # [doc = ""] # [doc = " This uses the following rules to determine equality:"] # [doc = " * If the two sides are references to the same vector, return true."] # [doc = " * If the two sides are single chunk vectors pointing to the same chunk, return true."] # [doc = " * If the two sides are full trees pointing to the same chunks, return true."] # [doc = ""] # [doc = " This would return true if you're comparing a vector to itself, or"] # [doc = " if you're comparing a vector to a fresh clone of itself. The exception to this is"] # [doc = " if you've cloned an inline array (ie. an array with so few elements they can fit"] # [doc = " inside the space a `Vector` allocates for its pointers, so there are no heap allocations"] # [doc = " to compare)."] # [doc = ""] # [doc = " Time: O(1)"] # [must_use] pub fn ptr_eq (& self , other : & Self) -> bool { fn cmp_chunk < A > (left : & PoolRef < Chunk < A > > , right : & PoolRef < Chunk < A > >) -> bool { (left . is_empty () && right . is_empty ()) || PoolRef :: ptr_eq (left , right) } if std :: ptr :: eq (self , other) { return true ; } match (& self . vector , & other . vector) { (Single (_ , left) , Single (_ , right)) => cmp_chunk (left , right) , (Full (_ , left) , Full (_ , right)) => { cmp_chunk (& left . outer_f , & right . outer_f) && cmp_chunk (& left . inner_f , & right . inner_f) && cmp_chunk (& left . inner_b , & right . inner_b) && cmp_chunk (& left . outer_b , & right . outer_b) && ((left . middle . is_empty () && right . middle . is_empty ()) || Ref :: ptr_eq (& left . middle , & right . middle)) } _ => false , } } # [doc = " Get an iterator over a vector."] # [doc = ""] # [doc = " Time: O(1)"] # [inline] # [must_use] pub fn iter (& self) -> Iter < '_ , A > { Iter :: new (self) } # [doc = " Get a mutable iterator over a vector."] # [doc = ""] # [doc = " Time: O(1)"] # [inline] # [must_use] pub fn iter_mut (& mut self) -> IterMut < '_ , A > { IterMut :: new (self) } # [doc = " Get an iterator over the leaf nodes of a vector."] # [doc = ""] # [doc = " This returns an iterator over the [`Chunk`s][Chunk] at the leaves of the"] # [doc = " RRB tree. These are useful for efficient parallelisation of work on"] # [doc = " the vector, but should not be used for basic iteration."] # [doc = ""] # [doc = " Time: O(1)"] # [doc = ""] # [doc = " [Chunk]: ../chunk/struct.Chunk.html"] # [inline] # [must_use] pub fn leaves (& self) -> Chunks < '_ , A > { Chunks :: new (self) } # [doc = " Get a mutable iterator over the leaf nodes of a vector."] # [doc = " This returns an iterator over the [`Chunk`s][Chunk] at the leaves of the"] # [doc = " RRB tree. These are useful for efficient parallelisation of work on"] # [doc = " the vector, but should not be used for basic iteration."] # [doc = ""] # [doc = " Time: O(1)"] # [doc = ""] # [doc = " [Chunk]: ../chunk/struct.Chunk.html"] # [inline] # [must_use] pub fn leaves_mut (& mut self) -> ChunksMut < '_ , A > { ChunksMut :: new (self) } # [doc = " Construct a [`Focus`][Focus] for a vector."] # [doc = ""] # [doc = " Time: O(1)"] # [doc = ""] # [doc = " [Focus]: enum.Focus.html"] # [inline] # [must_use] pub fn focus (& self) -> Focus < '_ , A > { Focus :: new (self) } # [doc = " Construct a [`FocusMut`][FocusMut] for a vector."] # [doc = ""] # [doc = " Time: O(1)"] # [doc = ""] # [doc = " [FocusMut]: enum.FocusMut.html"] # [inline] # [must_use] pub fn focus_mut (& mut self) -> FocusMut < '_ , A > { FocusMut :: new (self) } # [doc = " Get a reference to the value at index `index` in a vector."] # [doc = ""] # [doc = " Returns `None` if the index is out of bounds."] # [doc = ""] # [doc = " Time: O(log n)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " # use im::Vector;"] # [doc = " let vec = vector![\"Joe\", \"Mike\", \"Robert\"];"] # [doc = " assert_eq!(Some(&\"Robert\"), vec.get(2));"] # [doc = " assert_eq!(None, vec.get(5));"] # [doc = " ```"] # [must_use] pub fn get (& self , index : usize) -> Option < & A > { if index >= self . len () { return None ; } match & self . vector { Inline (_ , chunk) => chunk . get (index) , Single (_ , chunk) => chunk . get (index) , Full (_ , tree) => { let mut local_index = index ; if local_index < tree . outer_f . len () { return Some (& tree . outer_f [local_index]) ; } local_index -= tree . outer_f . len () ; if local_index < tree . inner_f . len () { return Some (& tree . inner_f [local_index]) ; } local_index -= tree . inner_f . len () ; if local_index < tree . middle . len () { return Some (tree . middle . index (tree . middle_level , local_index)) ; } local_index -= tree . middle . len () ; if local_index < tree . inner_b . len () { return Some (& tree . inner_b [local_index]) ; } local_index -= tree . inner_b . len () ; Some (& tree . outer_b [local_index]) } } } # [doc = " Get a mutable reference to the value at index `index` in a"] # [doc = " vector."] # [doc = ""] # [doc = " Returns `None` if the index is out of bounds."] # [doc = ""] # [doc = " Time: O(log n)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " # use im::Vector;"] # [doc = " let mut vec = vector![\"Joe\", \"Mike\", \"Robert\"];"] # [doc = " {"] # [doc = "     let robert = vec.get_mut(2).unwrap();"] # [doc = "     assert_eq!(&mut \"Robert\", robert);"] # [doc = "     *robert = \"Bjarne\";"] # [doc = " }"] # [doc = " assert_eq!(vector![\"Joe\", \"Mike\", \"Bjarne\"], vec);"] # [doc = " ```"] # [must_use] pub fn get_mut (& mut self , index : usize) -> Option < & mut A > { if index >= self . len () { return None ; } match & mut self . vector { Inline (_ , chunk) => chunk . get_mut (index) , Single (pool , chunk) => PoolRef :: make_mut (& pool . value_pool , chunk) . get_mut (index) , Full (pool , tree) => { let mut local_index = index ; if local_index < tree . outer_f . len () { let outer_f = PoolRef :: make_mut (& pool . value_pool , & mut tree . outer_f) ; return Some (& mut outer_f [local_index]) ; } local_index -= tree . outer_f . len () ; if local_index < tree . inner_f . len () { let inner_f = PoolRef :: make_mut (& pool . value_pool , & mut tree . inner_f) ; return Some (& mut inner_f [local_index]) ; } local_index -= tree . inner_f . len () ; if local_index < tree . middle . len () { let middle = Ref :: make_mut (& mut tree . middle) ; return Some (middle . index_mut (pool , tree . middle_level , local_index)) ; } local_index -= tree . middle . len () ; if local_index < tree . inner_b . len () { let inner_b = PoolRef :: make_mut (& pool . value_pool , & mut tree . inner_b) ; return Some (& mut inner_b [local_index]) ; } local_index -= tree . inner_b . len () ; let outer_b = PoolRef :: make_mut (& pool . value_pool , & mut tree . outer_b) ; Some (& mut outer_b [local_index]) } } } # [doc = " Get the first element of a vector."] # [doc = ""] # [doc = " If the vector is empty, `None` is returned."] # [doc = ""] # [doc = " Time: O(log n)"] # [inline] # [must_use] pub fn front (& self) -> Option < & A > { self . get (0) } # [doc = " Get a mutable reference to the first element of a vector."] # [doc = ""] # [doc = " If the vector is empty, `None` is returned."] # [doc = ""] # [doc = " Time: O(log n)"] # [inline] # [must_use] pub fn front_mut (& mut self) -> Option < & mut A > { self . get_mut (0) } # [doc = " Get the first element of a vector."] # [doc = ""] # [doc = " If the vector is empty, `None` is returned."] # [doc = ""] # [doc = " This is an alias for the [`front`][front] method."] # [doc = ""] # [doc = " Time: O(log n)"] # [doc = ""] # [doc = " [front]: #method.front"] # [inline] # [must_use] pub fn head (& self) -> Option < & A > { self . get (0) } # [doc = " Get the last element of a vector."] # [doc = ""] # [doc = " If the vector is empty, `None` is returned."] # [doc = ""] # [doc = " Time: O(log n)"] # [must_use] pub fn back (& self) -> Option < & A > { if self . is_empty () { None } else { self . get (self . len () - 1) } } # [doc = " Get a mutable reference to the last element of a vector."] # [doc = ""] # [doc = " If the vector is empty, `None` is returned."] # [doc = ""] # [doc = " Time: O(log n)"] # [must_use] pub fn back_mut (& mut self) -> Option < & mut A > { if self . is_empty () { None } else { let len = self . len () ; self . get_mut (len - 1) } } # [doc = " Get the last element of a vector."] # [doc = ""] # [doc = " If the vector is empty, `None` is returned."] # [doc = ""] # [doc = " This is an alias for the [`back`][back] method."] # [doc = ""] # [doc = " Time: O(log n)"] # [doc = ""] # [doc = " [back]: #method.back"] # [inline] # [must_use] pub fn last (& self) -> Option < & A > { self . back () } # [doc = " Get the index of a given element in the vector."] # [doc = ""] # [doc = " Searches the vector for the first occurrence of a given value,"] # [doc = " and returns the index of the value if it's there. Otherwise,"] # [doc = " it returns `None`."] # [doc = ""] # [doc = " Time: O(n)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " # use im::Vector;"] # [doc = " let mut vec = vector![1, 2, 3, 4, 5];"] # [doc = " assert_eq!(Some(2), vec.index_of(&3));"] # [doc = " assert_eq!(None, vec.index_of(&31337));"] # [doc = " ```"] # [must_use] pub fn index_of (& self , value : & A) -> Option < usize > where A : PartialEq , { for (index , item) in self . iter () . enumerate () { if value == item { return Some (index) ; } } None } # [doc = " Test if a given element is in the vector."] # [doc = ""] # [doc = " Searches the vector for the first occurrence of a given value,"] # [doc = " and returns `true` if it's there. If it's nowhere to be found"] # [doc = " in the vector, it returns `false`."] # [doc = ""] # [doc = " Time: O(n)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " # use im::Vector;"] # [doc = " let mut vec = vector![1, 2, 3, 4, 5];"] # [doc = " assert_eq!(true, vec.contains(&3));"] # [doc = " assert_eq!(false, vec.contains(&31337));"] # [doc = " ```"] # [inline] # [must_use] pub fn contains (& self , value : & A) -> bool where A : PartialEq , { self . index_of (value) . is_some () } # [doc = " Discard all elements from the vector."] # [doc = ""] # [doc = " This leaves you with an empty vector, and all elements that"] # [doc = " were previously inside it are dropped."] # [doc = ""] # [doc = " Time: O(n)"] pub fn clear (& mut self) { if ! self . is_empty () { self . vector = Inline (self . pool () . clone () , InlineArray :: new ()) ; } } # [doc = " Binary search a sorted vector for a given element using a comparator"] # [doc = " function."] # [doc = ""] # [doc = " Assumes the vector has already been sorted using the same comparator"] # [doc = " function, eg. by using [`sort_by`][sort_by]."] # [doc = ""] # [doc = " If the value is found, it returns `Ok(index)` where `index` is the index"] # [doc = " of the element. If the value isn't found, it returns `Err(index)` where"] # [doc = " `index` is the index at which the element would need to be inserted to"] # [doc = " maintain sorted order."] # [doc = ""] # [doc = " Time: O(log n)"] # [doc = ""] # [doc = " [sort_by]: #method.sort_by"] pub fn binary_search_by < F > (& self , mut f : F) -> Result < usize , usize > where F : FnMut (& A) -> Ordering , { let mut size = self . len () ; if size == 0 { return Err (0) ; } let mut base = 0 ; while size > 1 { let half = size / 2 ; let mid = base + half ; base = match f (& self [mid]) { Ordering :: Greater => base , _ => mid , } ; size -= half ; } match f (& self [base]) { Ordering :: Equal => Ok (base) , Ordering :: Greater => Err (base) , Ordering :: Less => Err (base + 1) , } } # [doc = " Binary search a sorted vector for a given element."] # [doc = ""] # [doc = " If the value is found, it returns `Ok(index)` where `index` is the index"] # [doc = " of the element. If the value isn't found, it returns `Err(index)` where"] # [doc = " `index` is the index at which the element would need to be inserted to"] # [doc = " maintain sorted order."] # [doc = ""] # [doc = " Time: O(log n)"] pub fn binary_search (& self , value : & A) -> Result < usize , usize > where A : Ord , { self . binary_search_by (| e | e . cmp (value)) } # [doc = " Binary search a sorted vector for a given element with a key extract"] # [doc = " function."] # [doc = ""] # [doc = " Assumes the vector has already been sorted using the same key extract"] # [doc = " function, eg. by using [`sort_by_key`][sort_by_key]."] # [doc = ""] # [doc = " If the value is found, it returns `Ok(index)` where `index` is the index"] # [doc = " of the element. If the value isn't found, it returns `Err(index)` where"] # [doc = " `index` is the index at which the element would need to be inserted to"] # [doc = " maintain sorted order."] # [doc = ""] # [doc = " Time: O(log n)"] # [doc = ""] # [doc = " [sort_by_key]: #method.sort_by_key"] pub fn binary_search_by_key < B , F > (& self , b : & B , mut f : F) -> Result < usize , usize > where F : FnMut (& A) -> B , B : Ord , { self . binary_search_by (| k | f (k) . cmp (b)) } }
};
}
