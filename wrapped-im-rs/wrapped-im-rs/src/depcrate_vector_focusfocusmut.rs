// Generated macro for FocusMut (enum)
macro_rules! Depcrate_vector_focusFocusMut {
() => {
// Module: crate::vector::focus
// Provides: {"FocusMut"}
// Dependencies: {}
# [doc = " A mutable version of [`Focus`][Focus]."] # [doc = ""] # [doc = " See [`Focus`][Focus] for more details."] # [doc = ""] # [doc = " You can only build one `FocusMut` at a time for a vector, effectively"] # [doc = " keeping a lock on the vector until you're done with the focus, which relies"] # [doc = " on the structure of the vector not changing while it exists."] # [doc = ""] # [doc = " ```rust,compile_fail"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " # use im::vector::Vector;"] # [doc = " # use std::iter::FromIterator;"] # [doc = " let mut vec = Vector::from_iter(0..1000);"] # [doc = " let focus1 = vec.focus_mut();"] # [doc = " // Fails here in 2015 edition because you're creating"] # [doc = " // two mutable references to the same thing."] # [doc = " let focus2 = vec.focus_mut();"] # [doc = " // Fails here in 2018 edition because creating focus2"] # [doc = " // made focus1's lifetime go out of scope."] # [doc = " assert_eq!(Some(&0), focus1.get(0));"] # [doc = " ```"] # [doc = ""] # [doc = " On the other hand, you can split that one focus into multiple sub-focuses,"] # [doc = " which is safe because they can't overlap:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " # use im::vector::Vector;"] # [doc = " # use std::iter::FromIterator;"] # [doc = " let mut vec = Vector::from_iter(0..1000);"] # [doc = " let focus = vec.focus_mut();"] # [doc = " let (mut left, mut right) = focus.split_at(500);"] # [doc = " assert_eq!(Some(&0), left.get(0));"] # [doc = " assert_eq!(Some(&500), right.get(0));"] # [doc = " ```"] # [doc = ""] # [doc = " These sub-foci also work as a lock on the vector, even if the focus they"] # [doc = " were created from goes out of scope."] # [doc = ""] # [doc = " ```rust,compile_fail"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " # use im::vector::Vector;"] # [doc = " # use std::iter::FromIterator;"] # [doc = " let mut vec = Vector::from_iter(0..1000);"] # [doc = " let (left, right) = {"] # [doc = "     let focus = vec.focus_mut();"] # [doc = "     focus.split_at(500)"] # [doc = " };"] # [doc = " // `left` and `right` are still in scope even if `focus` isn't, so we can't"] # [doc = " // create another focus:"] # [doc = " let focus2 = vec.focus_mut();"] # [doc = " assert_eq!(Some(&0), left.get(0));"] # [doc = " ```"] # [doc = ""] # [doc = " [Focus]: enum.Focus.html"] pub enum FocusMut < 'a , A > { # [doc (hidden)] Single (RRBPool < A > , & 'a mut [A]) , # [doc (hidden)] Full (RRBPool < A > , TreeFocusMut < 'a , A >) , }
};
}
