// Generated macro for impl_313 (impl)
macro_rules! Depcrate_hash_mapimpl_313 {
() => {
// Module: crate::hash::map
// Provides: {"impl_313"}
// Dependencies: {}
impl < K , V , S > HashMap < K , V , S > { # [doc = " Test whether a hash map is empty."] # [doc = ""] # [doc = " Time: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " # use im::hashmap::HashMap;"] # [doc = " assert!("] # [doc = "   !hashmap!{1 => 2}.is_empty()"] # [doc = " );"] # [doc = " assert!("] # [doc = "   HashMap::<i32, i32>::new().is_empty()"] # [doc = " );"] # [doc = " ```"] # [inline] # [must_use] pub fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Get the size of a hash map."] # [doc = ""] # [doc = " Time: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " # use im::hashmap::HashMap;"] # [doc = " assert_eq!(3, hashmap!{"] # [doc = "   1 => 11,"] # [doc = "   2 => 22,"] # [doc = "   3 => 33"] # [doc = " }.len());"] # [doc = " ```"] # [inline] # [must_use] pub fn len (& self) -> usize { self . size } # [doc = " Test whether two maps refer to the same content in memory."] # [doc = ""] # [doc = " This is true if the two sides are references to the same map,"] # [doc = " or if the two maps refer to the same root node."] # [doc = ""] # [doc = " This would return true if you're comparing a map to itself, or"] # [doc = " if you're comparing a map to a fresh clone of itself."] # [doc = ""] # [doc = " Time: O(1)"] pub fn ptr_eq (& self , other : & Self) -> bool { std :: ptr :: eq (self , other) || PoolRef :: ptr_eq (& self . root , & other . root) } # [doc = " Get a reference to the memory pool used by this map."] # [doc = ""] # [doc = " Note that if you didn't specifically construct it with a pool, you'll"] # [doc = " get back a reference to a pool of size 0."] # [cfg (feature = "pool")] pub fn pool (& self) -> & HashMapPool < K , V > { & self . pool } # [doc = " Construct an empty hash map using the provided hasher."] # [inline] # [must_use] pub fn with_hasher < RS > (hasher : RS) -> Self where Ref < S > : From < RS > , { let pool = HashMapPool :: default () ; let root = PoolRef :: default (& pool . 0) ; HashMap { size : 0 , hasher : hasher . into () , pool , root , } } # [doc = " Construct an empty hash map using a specific memory pool and hasher."] # [cfg (feature = "pool")] # [must_use] pub fn with_pool_hasher < RS > (pool : & HashMapPool < K , V > , hasher : RS) -> Self where Ref < S > : From < RS > , { let root = PoolRef :: default (& pool . 0) ; Self { size : 0 , hasher : hasher . into () , pool : pool . clone () , root , } } # [doc = " Get a reference to the map's [`BuildHasher`][BuildHasher]."] # [doc = ""] # [doc = " [BuildHasher]: https://doc.rust-lang.org/std/hash/trait.BuildHasher.html"] # [must_use] pub fn hasher (& self) -> & Ref < S > { & self . hasher } # [doc = " Construct an empty hash map using the same hasher as the"] # [doc = " current hash map."] # [inline] # [must_use] pub fn new_from < K1 , V1 > (& self) -> HashMap < K1 , V1 , S > where K1 : Hash + Eq + Clone , V1 : Clone , { let pool = HashMapPool :: default () ; let root = PoolRef :: default (& pool . 0) ; HashMap { size : 0 , pool , root , hasher : self . hasher . clone () , } } # [doc = " Get an iterator over the key/value pairs of a hash map."] # [doc = ""] # [doc = " Please note that the order is consistent between maps using"] # [doc = " the same hasher, but no other ordering guarantee is offered."] # [doc = " Items will not come out in insertion order or sort order."] # [doc = " They will, however, come out in the same order every time for"] # [doc = " the same map."] # [inline] # [must_use] pub fn iter (& self) -> Iter < '_ , K , V > { Iter { it : NodeIter :: new (& self . root , self . size) , } } # [doc = " Get an iterator over a hash map's keys."] # [doc = ""] # [doc = " Please note that the order is consistent between maps using"] # [doc = " the same hasher, but no other ordering guarantee is offered."] # [doc = " Items will not come out in insertion order or sort order."] # [doc = " They will, however, come out in the same order every time for"] # [doc = " the same map."] # [inline] # [must_use] pub fn keys (& self) -> Keys < '_ , K , V > { Keys { it : NodeIter :: new (& self . root , self . size) , } } # [doc = " Get an iterator over a hash map's values."] # [doc = ""] # [doc = " Please note that the order is consistent between maps using"] # [doc = " the same hasher, but no other ordering guarantee is offered."] # [doc = " Items will not come out in insertion order or sort order."] # [doc = " They will, however, come out in the same order every time for"] # [doc = " the same map."] # [inline] # [must_use] pub fn values (& self) -> Values < '_ , K , V > { Values { it : NodeIter :: new (& self . root , self . size) , } } # [doc = " Discard all elements from the map."] # [doc = ""] # [doc = " This leaves you with an empty map, and all elements that"] # [doc = " were previously inside it are dropped."] # [doc = ""] # [doc = " Time: O(n)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[macro_use] extern crate im;"] # [doc = " # use im::HashMap;"] # [doc = " let mut map = hashmap![1=>1, 2=>2, 3=>3];"] # [doc = " map.clear();"] # [doc = " assert!(map.is_empty());"] # [doc = " ```"] pub fn clear (& mut self) { if ! self . is_empty () { self . root = PoolRef :: default (& self . pool . 0) ; self . size = 0 ; } } }
};
}
