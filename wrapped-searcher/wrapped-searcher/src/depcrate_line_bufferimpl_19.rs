// Generated macro for impl_19 (impl)
macro_rules! Depcrate_line_bufferimpl_19 {
() => {
// Module: crate::line_buffer
// Provides: {"impl_19"}
// Dependencies: {}
impl < 'b , R : io :: Read > LineBufferReader < 'b , R > { # [doc = " Create a new buffered reader that reads from `rdr` and uses the given"] # [doc = " `line_buffer` as an intermediate buffer."] # [doc = ""] # [doc = " This does not change the binary detection behavior of the given line"] # [doc = " buffer."] pub (crate) fn new (rdr : R , line_buffer : & 'b mut LineBuffer ,) -> LineBufferReader < 'b , R > { line_buffer . clear () ; LineBufferReader { rdr , line_buffer } } # [doc = " The absolute byte offset which corresponds to the starting offsets"] # [doc = " of the data returned by `buffer` relative to the beginning of the"] # [doc = " underlying reader's contents. As such, this offset does not generally"] # [doc = " correspond to an offset in memory. It is typically used for reporting"] # [doc = " purposes. It can also be used for counting the number of bytes that"] # [doc = " have been searched."] pub (crate) fn absolute_byte_offset (& self) -> u64 { self . line_buffer . absolute_byte_offset () } # [doc = " If binary data was detected, then this returns the absolute byte offset"] # [doc = " at which binary data was initially found."] pub (crate) fn binary_byte_offset (& self) -> Option < u64 > { self . line_buffer . binary_byte_offset () } # [doc = " Fill the contents of this buffer by discarding the part of the buffer"] # [doc = " that has been consumed. The free space created by discarding the"] # [doc = " consumed part of the buffer is then filled with new data from the"] # [doc = " reader."] # [doc = ""] # [doc = " If EOF is reached, then `false` is returned. Otherwise, `true` is"] # [doc = " returned. (Note that if this line buffer's binary detection is set to"] # [doc = " `Quit`, then the presence of binary data will cause this buffer to"] # [doc = " behave as if it had seen EOF at the first occurrence of binary data.)"] # [doc = ""] # [doc = " This forwards any errors returned by the underlying reader, and will"] # [doc = " also return an error if the buffer must be expanded past its allocation"] # [doc = " limit, as governed by the buffer allocation strategy."] pub (crate) fn fill (& mut self) -> Result < bool , io :: Error > { self . line_buffer . fill (& mut self . rdr) } # [doc = " Return the contents of this buffer."] pub (crate) fn buffer (& self) -> & [u8] { self . line_buffer . buffer () } # [doc = " Return the buffer as a BStr, used for convenient equality checking"] # [doc = " in tests only."] # [cfg (test)] fn bstr (& self) -> & bstr :: BStr { self . buffer () . as_bstr () } # [doc = " Consume the number of bytes provided. This must be less than or equal"] # [doc = " to the number of bytes returned by `buffer`."] pub (crate) fn consume (& mut self , amt : usize) { self . line_buffer . consume (amt) ; } # [doc = " Consumes the remainder of the buffer. Subsequent calls to `buffer` are"] # [doc = " guaranteed to return an empty slice until the buffer is refilled."] # [doc = ""] # [doc = " This is a convenience function for `consume(buffer.len())`."] # [cfg (test)] fn consume_all (& mut self) { self . line_buffer . consume_all () ; } }
};
}
