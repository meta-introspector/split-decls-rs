// Generated macro for Searcher (struct)
macro_rules! Depcrate_searcherSearcher {
() => {
// Module: crate::searcher
// Provides: {"Searcher"}
// Dependencies: {}
# [doc = " A searcher executes searches over a haystack and writes results to a caller"] # [doc = " provided sink."] # [doc = ""] # [doc = " Matches are detected via implementations of the `Matcher` trait, which must"] # [doc = " be provided by the caller when executing a search."] # [doc = ""] # [doc = " When possible, a searcher should be reused."] # [derive (Clone , Debug)] pub struct Searcher { # [doc = " The configuration for this searcher."] # [doc = ""] # [doc = " We make most of these settings available to users of `Searcher` via"] # [doc = " public API methods, which can be queried in implementations of `Sink`"] # [doc = " if necessary."] config : Config , # [doc = " A builder for constructing a streaming reader that transcodes source"] # [doc = " data according to either an explicitly specified encoding or via an"] # [doc = " automatically detected encoding via BOM sniffing."] # [doc = ""] # [doc = " When no transcoding is needed, then the transcoder built will pass"] # [doc = " through the underlying bytes with no additional overhead."] decode_builder : DecodeReaderBytesBuilder , # [doc = " A buffer that is used for transcoding scratch space."] decode_buffer : RefCell < Vec < u8 > > , # [doc = " A line buffer for use in line oriented searching."] # [doc = ""] # [doc = " We wrap it in a RefCell to permit lending out borrows of `Searcher`"] # [doc = " to sinks. We still require a mutable borrow to execute a search, so"] # [doc = " we statically prevent callers from causing RefCell to panic at runtime"] # [doc = " due to a borrowing violation."] line_buffer : RefCell < LineBuffer > , # [doc = " A buffer in which to store the contents of a reader when performing a"] # [doc = " multi line search. In particular, multi line searches cannot be"] # [doc = " performed incrementally, and need the entire haystack in memory at"] # [doc = " once."] multi_line_buffer : RefCell < Vec < u8 > > , }
};
}
