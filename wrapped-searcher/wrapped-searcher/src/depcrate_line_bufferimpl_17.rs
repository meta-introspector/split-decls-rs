// Generated macro for impl_17 (impl)
macro_rules! Depcrate_line_bufferimpl_17 {
() => {
// Module: crate::line_buffer
// Provides: {"impl_17"}
// Dependencies: {}
impl LineBufferBuilder { # [doc = " Create a new builder for a buffer."] pub (crate) fn new () -> LineBufferBuilder { LineBufferBuilder { config : Config :: default () } } # [doc = " Create a new line buffer from this builder's configuration."] pub (crate) fn build (& self) -> LineBuffer { LineBuffer { config : self . config , buf : vec ! [0 ; self . config . capacity] , pos : 0 , last_lineterm : 0 , end : 0 , absolute_byte_offset : 0 , binary_byte_offset : None , } } # [doc = " Set the default capacity to use for a buffer."] # [doc = ""] # [doc = " In general, the capacity of a buffer corresponds to the amount of data"] # [doc = " to hold in memory, and the size of the reads to make to the underlying"] # [doc = " reader."] # [doc = ""] # [doc = " This is set to a reasonable default and probably shouldn't be changed"] # [doc = " unless there's a specific reason to do so."] pub (crate) fn capacity (& mut self , capacity : usize ,) -> & mut LineBufferBuilder { self . config . capacity = capacity ; self } # [doc = " Set the line terminator for the buffer."] # [doc = ""] # [doc = " Every buffer has a line terminator, and this line terminator is used"] # [doc = " to determine how to roll the buffer forward. For example, when a read"] # [doc = " to the buffer's underlying reader occurs, the end of the data that is"] # [doc = " read is likely to correspond to an incomplete line. As a line buffer,"] # [doc = " callers should not access this data since it is incomplete. The line"] # [doc = " terminator is how the line buffer determines the part of the read that"] # [doc = " is incomplete."] # [doc = ""] # [doc = " By default, this is set to `b'\\n'`."] pub (crate) fn line_terminator (& mut self , lineterm : u8 ,) -> & mut LineBufferBuilder { self . config . lineterm = lineterm ; self } # [doc = " Set the maximum amount of additional memory to allocate for long lines."] # [doc = ""] # [doc = " In order to enable line oriented search, a fundamental requirement is"] # [doc = " that, at a minimum, each line must be able to fit into memory. This"] # [doc = " setting controls how big that line is allowed to be. By default, this"] # [doc = " is set to `BufferAllocation::Eager`, which means a line buffer will"] # [doc = " attempt to allocate as much memory as possible to fit a line, and will"] # [doc = " only be limited by available memory."] # [doc = ""] # [doc = " Note that this setting only applies to the amount of *additional*"] # [doc = " memory to allocate, beyond the capacity of the buffer. That means that"] # [doc = " a value of `0` is sensible, and in particular, will guarantee that a"] # [doc = " line buffer will never allocate additional memory beyond its initial"] # [doc = " capacity."] pub (crate) fn buffer_alloc (& mut self , behavior : BufferAllocation ,) -> & mut LineBufferBuilder { self . config . buffer_alloc = behavior ; self } # [doc = " Whether to enable binary detection or not. Depending on the setting,"] # [doc = " this can either cause the line buffer to report EOF early or it can"] # [doc = " cause the line buffer to clean the data."] # [doc = ""] # [doc = " By default, this is disabled. In general, binary detection should be"] # [doc = " viewed as an imperfect heuristic."] pub (crate) fn binary_detection (& mut self , detection : BinaryDetection ,) -> & mut LineBufferBuilder { self . config . binary = detection ; self } }
};
}
