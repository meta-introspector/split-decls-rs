// Generated macro for LineBuffer (struct)
macro_rules! Depcrate_line_bufferLineBuffer {
() => {
// Module: crate::line_buffer
// Provides: {"LineBuffer"}
// Dependencies: {}
# [doc = " A line buffer manages a (typically fixed) buffer for holding lines."] # [doc = ""] # [doc = " Callers should create line buffers sparingly and reuse them when possible."] # [doc = " Line buffers cannot be used directly, but instead must be used via the"] # [doc = " LineBufferReader."] # [derive (Clone , Debug)] pub (crate) struct LineBuffer { # [doc = " The configuration of this buffer."] config : Config , # [doc = " The primary buffer with which to hold data."] buf : Vec < u8 > , # [doc = " The current position of this buffer. This is always a valid sliceable"] # [doc = " index into `buf`, and its maximum value is the length of `buf`."] pos : usize , # [doc = " The end position of searchable content in this buffer. This is either"] # [doc = " set to just after the final line terminator in the buffer, or to just"] # [doc = " after the end of the last byte emitted by the reader when the reader"] # [doc = " has been exhausted."] last_lineterm : usize , # [doc = " The end position of the buffer. This is always greater than or equal to"] # [doc = " last_lineterm. The bytes between last_lineterm and end, if any, always"] # [doc = " correspond to a partial line."] end : usize , # [doc = " The absolute byte offset corresponding to `pos`. This is most typically"] # [doc = " not a valid index into addressable memory, but rather, an offset that"] # [doc = " is relative to all data that passes through a line buffer (since"] # [doc = " construction or since the last time `clear` was called)."] # [doc = ""] # [doc = " When the line buffer reaches EOF, this is set to the position just"] # [doc = " after the last byte read from the underlying reader. That is, it"] # [doc = " becomes the total count of bytes that have been read."] absolute_byte_offset : u64 , # [doc = " If binary data was found, this records the absolute byte offset at"] # [doc = " which it was first detected."] binary_byte_offset : Option < u64 > , }
};
}
