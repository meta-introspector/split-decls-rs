// Generated macro for sinks (module)
macro_rules! Depcrate_sinksinks {
() => {
// Module: crate::sink
// Provides: {"sinks"}
// Dependencies: {}
# [doc = " A collection of convenience implementations of `Sink`."] # [doc = ""] # [doc = " Each implementation in this module makes some kind of sacrifice in the name"] # [doc = " of making common cases easier to use. Most frequently, each type is a"] # [doc = " wrapper around a closure specified by the caller that provides limited"] # [doc = " access to the full suite of information available to implementors of"] # [doc = " `Sink`."] # [doc = ""] # [doc = " For example, the `UTF8` sink makes the following sacrifices:"] # [doc = ""] # [doc = " * All matches must be UTF-8. An arbitrary `Sink` does not have this"] # [doc = "   restriction and can deal with arbitrary data. If this sink sees invalid"] # [doc = "   UTF-8, then an error is returned and searching stops. (Use the `Lossy`"] # [doc = "   sink instead to suppress this error.)"] # [doc = " * The searcher must be configured to report line numbers. If it isn't,"] # [doc = "   an error is reported at the first match and searching stops."] # [doc = " * Context lines, context breaks and summary data reported at the end of"] # [doc = "   a search are all ignored."] # [doc = " * Implementors are forced to use `std::io::Error` as their error type."] # [doc = ""] # [doc = " If you need more flexibility, then you're advised to implement the `Sink`"] # [doc = " trait directly."] pub mod sinks { use std :: io ; use crate :: searcher :: Searcher ; use super :: { Sink , SinkError , SinkMatch } ; # [doc = " A sink that provides line numbers and matches as strings while ignoring"] # [doc = " everything else."] # [doc = ""] # [doc = " This implementation will return an error if a match contains invalid"] # [doc = " UTF-8 or if the searcher was not configured to count lines. Errors"] # [doc = " on invalid UTF-8 can be suppressed by using the `Lossy` sink instead"] # [doc = " of this one."] # [doc = ""] # [doc = " The closure accepts two parameters: a line number and a UTF-8 string"] # [doc = " containing the matched data. The closure returns a"] # [doc = " `Result<bool, std::io::Error>`. If the `bool` is `false`, then the"] # [doc = " search stops immediately. Otherwise, searching continues."] # [doc = ""] # [doc = " If multi line mode was enabled, the line number refers to the line"] # [doc = " number of the first line in the match."] # [derive (Clone , Debug)] pub struct UTF8 < F > (pub F) where F : FnMut (u64 , & str) -> Result < bool , io :: Error > ; impl < F > Sink for UTF8 < F > where F : FnMut (u64 , & str) -> Result < bool , io :: Error > , { type Error = io :: Error ; fn matched (& mut self , _searcher : & Searcher , mat : & SinkMatch < '_ > ,) -> Result < bool , io :: Error > { let matched = match std :: str :: from_utf8 (mat . bytes ()) { Ok (matched) => matched , Err (err) => return Err (io :: Error :: error_message (err)) , } ; let line_number = match mat . line_number () { Some (line_number) => line_number , None => { let msg = "line numbers not enabled" ; return Err (io :: Error :: error_message (msg)) ; } } ; (self . 0) (line_number , & matched) } } # [doc = " A sink that provides line numbers and matches as (lossily converted)"] # [doc = " strings while ignoring everything else."] # [doc = ""] # [doc = " This is like `UTF8`, except that if a match contains invalid UTF-8,"] # [doc = " then it will be lossily converted to valid UTF-8 by substituting"] # [doc = " invalid UTF-8 with Unicode replacement characters."] # [doc = ""] # [doc = " This implementation will return an error on the first match if the"] # [doc = " searcher was not configured to count lines."] # [doc = ""] # [doc = " The closure accepts two parameters: a line number and a UTF-8 string"] # [doc = " containing the matched data. The closure returns a"] # [doc = " `Result<bool, std::io::Error>`. If the `bool` is `false`, then the"] # [doc = " search stops immediately. Otherwise, searching continues."] # [doc = ""] # [doc = " If multi line mode was enabled, the line number refers to the line"] # [doc = " number of the first line in the match."] # [derive (Clone , Debug)] pub struct Lossy < F > (pub F) where F : FnMut (u64 , & str) -> Result < bool , io :: Error > ; impl < F > Sink for Lossy < F > where F : FnMut (u64 , & str) -> Result < bool , io :: Error > , { type Error = io :: Error ; fn matched (& mut self , _searcher : & Searcher , mat : & SinkMatch < '_ > ,) -> Result < bool , io :: Error > { use std :: borrow :: Cow ; let matched = match std :: str :: from_utf8 (mat . bytes ()) { Ok (matched) => Cow :: Borrowed (matched) , Err (_) => String :: from_utf8_lossy (mat . bytes ()) , } ; let line_number = match mat . line_number () { Some (line_number) => line_number , None => { let msg = "line numbers not enabled" ; return Err (io :: Error :: error_message (msg)) ; } } ; (self . 0) (line_number , & matched) } } # [doc = " A sink that provides line numbers and matches as raw bytes while"] # [doc = " ignoring everything else."] # [doc = ""] # [doc = " This implementation will return an error on the first match if the"] # [doc = " searcher was not configured to count lines."] # [doc = ""] # [doc = " The closure accepts two parameters: a line number and a raw byte string"] # [doc = " containing the matched data. The closure returns a"] # [doc = " `Result<bool, std::io::Error>`. If the `bool` is `false`, then the"] # [doc = " search stops immediately. Otherwise, searching continues."] # [doc = ""] # [doc = " If multi line mode was enabled, the line number refers to the line"] # [doc = " number of the first line in the match."] # [derive (Clone , Debug)] pub struct Bytes < F > (pub F) where F : FnMut (u64 , & [u8]) -> Result < bool , io :: Error > ; impl < F > Sink for Bytes < F > where F : FnMut (u64 , & [u8]) -> Result < bool , io :: Error > , { type Error = io :: Error ; fn matched (& mut self , _searcher : & Searcher , mat : & SinkMatch < '_ > ,) -> Result < bool , io :: Error > { let line_number = match mat . line_number () { Some (line_number) => line_number , None => { let msg = "line numbers not enabled" ; return Err (io :: Error :: error_message (msg)) ; } } ; (self . 0) (line_number , mat . bytes ()) } } }
};
}
