// Generated macro for impl_65 (impl)
macro_rules! Depcrate_searcher_mmapimpl_65 {
() => {
// Module: crate::searcher::mmap
// Provides: {"impl_65"}
// Dependencies: {}
impl MmapChoice { # [doc = " Use memory maps when they are believed to be advantageous."] # [doc = ""] # [doc = " The heuristics used to determine whether to use a memory map or not"] # [doc = " may depend on many things, including but not limited to, file size"] # [doc = " and platform."] # [doc = ""] # [doc = " If memory maps are unavailable or cannot be used for a specific input,"] # [doc = " then normal OS read calls are used instead."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This constructor is not safe because there is no obvious way to"] # [doc = " encapsulate the safety of file backed memory maps on all platforms"] # [doc = " without simultaneously negating some or all of their benefits."] # [doc = ""] # [doc = " The specific contract the caller is required to uphold isn't precise,"] # [doc = " but it basically amounts to something like, \"the caller guarantees that"] # [doc = " the underlying file won't be mutated.\" This, of course, isn't feasible"] # [doc = " in many environments. However, command line tools may still decide to"] # [doc = " take the risk of, say, a `SIGBUS` occurring while attempting to read a"] # [doc = " memory map."] pub unsafe fn auto () -> MmapChoice { MmapChoice (MmapChoiceImpl :: Auto) } # [doc = " Never use memory maps, no matter what. This is the default."] pub fn never () -> MmapChoice { MmapChoice (MmapChoiceImpl :: Never) } # [doc = " Return a memory map if memory maps are enabled and if creating a"] # [doc = " memory from the given file succeeded and if memory maps are believed"] # [doc = " to be advantageous for performance."] # [doc = ""] # [doc = " If this does attempt to open a memory map and it fails, then `None`"] # [doc = " is returned and the corresponding error (along with the file path, if"] # [doc = " present) is logged at the debug level."] pub (crate) fn open (& self , file : & File , path : Option < & Path > ,) -> Option < Mmap > { if ! self . is_enabled () { return None ; } if cfg ! (target_os = "macos") { return None ; } match unsafe { Mmap :: map (file) } { Ok (mmap) => Some (mmap) , Err (err) => { if let Some (path) = path { log :: debug ! ("{}: failed to open memory map: {}" , path . display () , err) ; } else { log :: debug ! ("failed to open memory map: {}" , err) ; } None } } } # [doc = " Whether this strategy may employ memory maps or not."] pub (crate) fn is_enabled (& self) -> bool { match self . 0 { MmapChoiceImpl :: Auto => true , MmapChoiceImpl :: Never => false , } } }
};
}
