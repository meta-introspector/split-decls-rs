// Generated macro for impl_79 (impl)
macro_rules! Depcrate_searcherimpl_79 {
() => {
// Module: crate::searcher
// Provides: {"impl_79"}
// Dependencies: {}
impl SearcherBuilder { # [doc = " Create a new searcher builder with a default configuration."] pub fn new () -> SearcherBuilder { SearcherBuilder { config : Config :: default () } } # [doc = " Build a searcher with the given matcher."] pub fn build (& self) -> Searcher { let mut config = self . config . clone () ; if config . passthru { config . before_context = 0 ; config . after_context = 0 ; } let mut decode_builder = DecodeReaderBytesBuilder :: new () ; decode_builder . encoding (self . config . encoding . as_ref () . map (| e | e . 0)) . utf8_passthru (true) . strip_bom (self . config . bom_sniffing) . bom_override (true) . bom_sniffing (self . config . bom_sniffing) ; Searcher { config , decode_builder , decode_buffer : RefCell :: new (vec ! [0 ; 8 * (1 << 10)]) , line_buffer : RefCell :: new (self . config . line_buffer ()) , multi_line_buffer : RefCell :: new (vec ! []) , } } # [doc = " Set the line terminator that is used by the searcher."] # [doc = ""] # [doc = " When using a searcher, if the matcher provided has a line terminator"] # [doc = " set, then it must be the same as this one. If they aren't, building"] # [doc = " a searcher will return an error."] # [doc = ""] # [doc = " By default, this is set to `b'\\n'`."] pub fn line_terminator (& mut self , line_term : LineTerminator ,) -> & mut SearcherBuilder { self . config . line_term = line_term ; self } # [doc = " Whether to invert matching, whereby lines that don't match are reported"] # [doc = " instead of reporting lines that do match."] # [doc = ""] # [doc = " By default, this is disabled."] pub fn invert_match (& mut self , yes : bool) -> & mut SearcherBuilder { self . config . invert_match = yes ; self } # [doc = " Whether to count and include line numbers with matching lines."] # [doc = ""] # [doc = " This is enabled by default. There is a small performance penalty"] # [doc = " associated with computing line numbers, so this can be disabled when"] # [doc = " this isn't desirable."] pub fn line_number (& mut self , yes : bool) -> & mut SearcherBuilder { self . config . line_number = yes ; self } # [doc = " Whether to enable multi line search or not."] # [doc = ""] # [doc = " When multi line search is enabled, matches *may* match across multiple"] # [doc = " lines. Conversely, when multi line search is disabled, it is impossible"] # [doc = " for any match to span more than one line."] # [doc = ""] # [doc = " **Warning:** multi line search requires having the entire contents to"] # [doc = " search mapped in memory at once. When searching files, memory maps"] # [doc = " will be used if possible and if they are enabled, which avoids using"] # [doc = " your program's heap. However, if memory maps cannot be used (e.g.,"] # [doc = " for searching streams like `stdin` or if transcoding is necessary),"] # [doc = " then the entire contents of the stream are read on to the heap before"] # [doc = " starting the search."] # [doc = ""] # [doc = " This is disabled by default."] pub fn multi_line (& mut self , yes : bool) -> & mut SearcherBuilder { self . config . multi_line = yes ; self } # [doc = " Whether to include a fixed number of lines after every match."] # [doc = ""] # [doc = " When this is set to a non-zero number, then the searcher will report"] # [doc = " `line_count` contextual lines after every match."] # [doc = ""] # [doc = " This is set to `0` by default."] pub fn after_context (& mut self , line_count : usize ,) -> & mut SearcherBuilder { self . config . after_context = line_count ; self } # [doc = " Whether to include a fixed number of lines before every match."] # [doc = ""] # [doc = " When this is set to a non-zero number, then the searcher will report"] # [doc = " `line_count` contextual lines before every match."] # [doc = ""] # [doc = " This is set to `0` by default."] pub fn before_context (& mut self , line_count : usize ,) -> & mut SearcherBuilder { self . config . before_context = line_count ; self } # [doc = " Whether to enable the \"passthru\" feature or not."] # [doc = ""] # [doc = " When passthru is enabled, it effectively treats all non-matching lines"] # [doc = " as contextual lines. In other words, enabling this is akin to"] # [doc = " requesting an unbounded number of before and after contextual lines."] # [doc = ""] # [doc = " When passthru mode is enabled, any `before_context` or `after_context`"] # [doc = " settings are ignored by setting them to `0`."] # [doc = ""] # [doc = " This is disabled by default."] pub fn passthru (& mut self , yes : bool) -> & mut SearcherBuilder { self . config . passthru = yes ; self } # [doc = " Set an approximate limit on the amount of heap space used by a"] # [doc = " searcher."] # [doc = ""] # [doc = " The heap limit is enforced in two scenarios:"] # [doc = ""] # [doc = " * When searching using a fixed size buffer, the heap limit controls"] # [doc = "   how big this buffer is allowed to be. Assuming contexts are disabled,"] # [doc = "   the minimum size of this buffer is the length (in bytes) of the"] # [doc = "   largest single line in the contents being searched. If any line"] # [doc = "   exceeds the heap limit, then an error will be returned."] # [doc = " * When performing a multi line search, a fixed size buffer cannot be"] # [doc = "   used. Thus, the only choices are to read the entire contents on to"] # [doc = "   the heap, or use memory maps. In the former case, the heap limit set"] # [doc = "   here is enforced."] # [doc = ""] # [doc = " If a heap limit is set to `0`, then no heap space is used. If there are"] # [doc = " no alternative strategies available for searching without heap space"] # [doc = " (e.g., memory maps are disabled), then the searcher wil return an error"] # [doc = " immediately."] # [doc = ""] # [doc = " By default, no limit is set."] pub fn heap_limit (& mut self , bytes : Option < usize > ,) -> & mut SearcherBuilder { self . config . heap_limit = bytes ; self } # [doc = " Set the strategy to employ use of memory maps."] # [doc = ""] # [doc = " Currently, there are only two strategies that can be employed:"] # [doc = ""] # [doc = " * **Automatic** - A searcher will use heuristics, including but not"] # [doc = "   limited to file size and platform, to determine whether to use memory"] # [doc = "   maps or not."] # [doc = " * **Never** - Memory maps will never be used. If multi line search is"] # [doc = "   enabled, then the entire contents will be read on to the heap before"] # [doc = "   searching begins."] # [doc = ""] # [doc = " The default behavior is **never**. Generally speaking, and perhaps"] # [doc = " against conventional wisdom, memory maps don't necessarily enable"] # [doc = " faster searching. For example, depending on the platform, using memory"] # [doc = " maps while searching a large directory can actually be quite a bit"] # [doc = " slower than using normal read calls because of the overhead of managing"] # [doc = " the memory maps."] # [doc = ""] # [doc = " Memory maps can be faster in some cases however. On some platforms,"] # [doc = " when searching a very large file that *is already in memory*, it can"] # [doc = " be slightly faster to search it as a memory map instead of using"] # [doc = " normal read calls."] # [doc = ""] # [doc = " Finally, memory maps have a somewhat complicated safety story in Rust."] # [doc = " If you aren't sure whether enabling memory maps is worth it, then just"] # [doc = " don't bother with it."] # [doc = ""] # [doc = " **WARNING**: If your process is searching a file backed memory map"] # [doc = " at the same time that file is truncated, then it's possible for the"] # [doc = " process to terminate with a bus error."] pub fn memory_map (& mut self , strategy : MmapChoice ,) -> & mut SearcherBuilder { self . config . mmap = strategy ; self } # [doc = " Set the binary detection strategy."] # [doc = ""] # [doc = " The binary detection strategy determines not only how the searcher"] # [doc = " detects binary data, but how it responds to the presence of binary"] # [doc = " data. See the [`BinaryDetection`] type for more information."] # [doc = ""] # [doc = " By default, binary detection is disabled."] pub fn binary_detection (& mut self , detection : BinaryDetection ,) -> & mut SearcherBuilder { self . config . binary = detection ; self } # [doc = " Set the encoding used to read the source data before searching."] # [doc = ""] # [doc = " When an encoding is provided, then the source data is _unconditionally_"] # [doc = " transcoded using the encoding, unless a BOM is present. If a BOM is"] # [doc = " present, then the encoding indicated by the BOM is used instead. If the"] # [doc = " transcoding process encounters an error, then bytes are replaced with"] # [doc = " the Unicode replacement codepoint."] # [doc = ""] # [doc = " When no encoding is specified (the default), then BOM sniffing is"] # [doc = " used (if it's enabled, which it is, by default) to determine whether"] # [doc = " the source data is UTF-8 or UTF-16, and transcoding will be performed"] # [doc = " automatically. If no BOM could be found, then the source data is"] # [doc = " searched _as if_ it were UTF-8. However, so long as the source data is"] # [doc = " at least ASCII compatible, then it is possible for a search to produce"] # [doc = " useful results."] pub fn encoding (& mut self , encoding : Option < Encoding > ,) -> & mut SearcherBuilder { self . config . encoding = encoding ; self } # [doc = " Enable automatic transcoding based on BOM sniffing."] # [doc = ""] # [doc = " When this is enabled and an explicit encoding is not set, then this"] # [doc = " searcher will try to detect the encoding of the bytes being searched"] # [doc = " by sniffing its byte-order mark (BOM). In particular, when this is"] # [doc = " enabled, UTF-16 encoded files will be searched seamlessly."] # [doc = ""] # [doc = " When this is disabled and if an explicit encoding is not set, then"] # [doc = " the bytes from the source stream will be passed through unchanged,"] # [doc = " including its BOM, if one is present."] # [doc = ""] # [doc = " This is enabled by default."] pub fn bom_sniffing (& mut self , yes : bool) -> & mut SearcherBuilder { self . config . bom_sniffing = yes ; self } # [doc = " Stop searching a file when a non-matching line is found after a"] # [doc = " matching line."] # [doc = ""] # [doc = " This is useful for searching sorted files where it is expected that all"] # [doc = " the matches will be on adjacent lines."] pub fn stop_on_nonmatch (& mut self , stop_on_nonmatch : bool ,) -> & mut SearcherBuilder { self . config . stop_on_nonmatch = stop_on_nonmatch ; self } # [doc = " Sets the maximum number of matches that should be emitted by this"] # [doc = " searcher."] # [doc = ""] # [doc = " If multi line search is enabled and a match spans multiple lines, then"] # [doc = " that match is counted exactly once for the purposes of enforcing this"] # [doc = " limit, regardless of how many lines it spans."] # [doc = ""] # [doc = " Note that `0` is a legal value. This will cause the searcher to"] # [doc = " immediately quick without searching anything."] # [doc = ""] # [doc = " By default, no limit is set."] # [inline] pub fn max_matches (& mut self , limit : Option < u64 >) -> & mut SearcherBuilder { self . config . max_matches = limit ; self } }
};
}
