// Generated macro for Sink (trait)
macro_rules! Depcrate_sinkSink {
() => {
// Module: crate::sink
// Provides: {"Sink"}
// Dependencies: {}
# [doc = " A trait that defines how results from searchers are handled."] # [doc = ""] # [doc = " In this crate, a searcher follows the \"push\" model. What that means is that"] # [doc = " the searcher drives execution, and pushes results back to the caller. This"] # [doc = " is in contrast to a \"pull\" model where the caller drives execution and"] # [doc = " takes results as they need them. These are also known as \"internal\" and"] # [doc = " \"external\" iteration strategies, respectively."] # [doc = ""] # [doc = " For a variety of reasons, including the complexity of the searcher"] # [doc = " implementation, this crate chooses the \"push\" or \"internal\" model of"] # [doc = " execution. Thus, in order to act on search results, callers must provide"] # [doc = " an implementation of this trait to a searcher, and the searcher is then"] # [doc = " responsible for calling the methods on this trait."] # [doc = ""] # [doc = " This trait defines several behaviors:"] # [doc = ""] # [doc = " * What to do when a match is found. Callers must provide this."] # [doc = " * What to do when an error occurs. Callers must provide this via the"] # [doc = "   [`SinkError`] trait. Generally, callers can just use `std::io::Error` for"] # [doc = "   this, which already implements `SinkError`."] # [doc = " * What to do when a contextual line is found. By default, these are"] # [doc = "   ignored."] # [doc = " * What to do when a gap between contextual lines has been found. By"] # [doc = "   default, this is ignored."] # [doc = " * What to do when a search has started. By default, this does nothing."] # [doc = " * What to do when a search has finished successfully. By default, this does"] # [doc = "   nothing."] # [doc = ""] # [doc = " Callers must, at minimum, specify the behavior when an error occurs and"] # [doc = " the behavior when a match occurs. The rest is optional. For each behavior,"] # [doc = " callers may report an error (say, if writing the result to another"] # [doc = " location failed) or simply return `false` if they want the search to stop"] # [doc = " (e.g., when implementing a cap on the number of search results to show)."] # [doc = ""] # [doc = " When errors are reported (whether in the searcher or in the implementation"] # [doc = " of `Sink`), then searchers quit immediately without calling `finish`."] # [doc = ""] # [doc = " For simpler uses of `Sink`, callers may elect to use one of"] # [doc = " the more convenient but less flexible implementations in the"] # [doc = " [`sinks`] module."] pub trait Sink { # [doc = " The type of an error that should be reported by a searcher."] # [doc = ""] # [doc = " Errors of this type are not only returned by the methods on this"] # [doc = " trait, but the constructors defined in `SinkError` are also used in"] # [doc = " the searcher implementation itself. e.g., When a I/O error occurs when"] # [doc = " reading data from a file."] type Error : SinkError ; # [doc = " This method is called whenever a match is found."] # [doc = ""] # [doc = " If multi line is enabled on the searcher, then the match reported here"] # [doc = " may span multiple lines and it may include multiple matches. When multi"] # [doc = " line is disabled, then the match is guaranteed to span exactly one"] # [doc = " non-empty line (where a single line is, at minimum, a line terminator)."] # [doc = ""] # [doc = " If this returns `true`, then searching continues. If this returns"] # [doc = " `false`, then searching is stopped immediately and `finish` is called."] # [doc = ""] # [doc = " If this returns an error, then searching is stopped immediately,"] # [doc = " `finish` is not called and the error is bubbled back up to the caller"] # [doc = " of the searcher."] fn matched (& mut self , _searcher : & Searcher , _mat : & SinkMatch < '_ > ,) -> Result < bool , Self :: Error > ; # [doc = " This method is called whenever a context line is found, and is optional"] # [doc = " to implement. By default, it does nothing and returns `true`."] # [doc = ""] # [doc = " In all cases, the context given is guaranteed to span exactly one"] # [doc = " non-empty line (where a single line is, at minimum, a line terminator)."] # [doc = ""] # [doc = " If this returns `true`, then searching continues. If this returns"] # [doc = " `false`, then searching is stopped immediately and `finish` is called."] # [doc = ""] # [doc = " If this returns an error, then searching is stopped immediately,"] # [doc = " `finish` is not called and the error is bubbled back up to the caller"] # [doc = " of the searcher."] # [inline] fn context (& mut self , _searcher : & Searcher , _context : & SinkContext < '_ > ,) -> Result < bool , Self :: Error > { Ok (true) } # [doc = " This method is called whenever a break in contextual lines is found,"] # [doc = " and is optional to implement. By default, it does nothing and returns"] # [doc = " `true`."] # [doc = ""] # [doc = " A break can only occur when context reporting is enabled (that is,"] # [doc = " either or both of `before_context` or `after_context` are greater than"] # [doc = " `0`). More precisely, a break occurs between non-contiguous groups of"] # [doc = " lines."] # [doc = ""] # [doc = " If this returns `true`, then searching continues. If this returns"] # [doc = " `false`, then searching is stopped immediately and `finish` is called."] # [doc = ""] # [doc = " If this returns an error, then searching is stopped immediately,"] # [doc = " `finish` is not called and the error is bubbled back up to the caller"] # [doc = " of the searcher."] # [inline] fn context_break (& mut self , _searcher : & Searcher ,) -> Result < bool , Self :: Error > { Ok (true) } # [doc = " This method is called whenever binary detection is enabled and binary"] # [doc = " data is found. If binary data is found, then this is called at least"] # [doc = " once for the first occurrence with the absolute byte offset at which"] # [doc = " the binary data begins."] # [doc = ""] # [doc = " If this returns `true`, then searching continues. If this returns"] # [doc = " `false`, then searching is stopped immediately and `finish` is called."] # [doc = ""] # [doc = " If this returns an error, then searching is stopped immediately,"] # [doc = " `finish` is not called and the error is bubbled back up to the caller"] # [doc = " of the searcher."] # [doc = ""] # [doc = " By default, it does nothing and returns `true`."] # [inline] fn binary_data (& mut self , _searcher : & Searcher , _binary_byte_offset : u64 ,) -> Result < bool , Self :: Error > { Ok (true) } # [doc = " This method is called when a search has begun, before any search is"] # [doc = " executed. By default, this does nothing."] # [doc = ""] # [doc = " If this returns `true`, then searching continues. If this returns"] # [doc = " `false`, then searching is stopped immediately and `finish` is called."] # [doc = ""] # [doc = " If this returns an error, then searching is stopped immediately,"] # [doc = " `finish` is not called and the error is bubbled back up to the caller"] # [doc = " of the searcher."] # [inline] fn begin (& mut self , _searcher : & Searcher) -> Result < bool , Self :: Error > { Ok (true) } # [doc = " This method is called when a search has completed. By default, this"] # [doc = " does nothing."] # [doc = ""] # [doc = " If this returns an error, the error is bubbled back up to the caller of"] # [doc = " the searcher."] # [inline] fn finish (& mut self , _searcher : & Searcher , _ : & SinkFinish ,) -> Result < () , Self :: Error > { Ok (()) } }
};
}
