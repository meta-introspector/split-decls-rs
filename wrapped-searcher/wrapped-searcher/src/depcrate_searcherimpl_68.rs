// Generated macro for impl_68 (impl)
macro_rules! Depcrate_searcherimpl_68 {
() => {
// Module: crate::searcher
// Provides: {"impl_68"}
// Dependencies: {}
impl BinaryDetection { # [doc = " No binary detection is performed. Data reported by the searcher may"] # [doc = " contain arbitrary bytes."] # [doc = ""] # [doc = " This is the default."] pub fn none () -> BinaryDetection { BinaryDetection (line_buffer :: BinaryDetection :: None) } # [doc = " Binary detection is performed by looking for the given byte."] # [doc = ""] # [doc = " When searching is performed using a fixed size buffer, then the"] # [doc = " contents of that buffer are always searched for the presence of this"] # [doc = " byte. If it is found, then the underlying data is considered binary"] # [doc = " and the search stops as if it reached EOF."] # [doc = ""] # [doc = " When searching is performed with the entire contents mapped into"] # [doc = " memory, then binary detection is more conservative. Namely, only a"] # [doc = " fixed sized region at the beginning of the contents are detected for"] # [doc = " binary data. As a compromise, any subsequent matching (or context)"] # [doc = " lines are also searched for binary data. If binary data is detected at"] # [doc = " any point, then the search stops as if it reached EOF."] pub fn quit (binary_byte : u8) -> BinaryDetection { BinaryDetection (line_buffer :: BinaryDetection :: Quit (binary_byte)) } # [doc = " Binary detection is performed by looking for the given byte, and"] # [doc = " replacing it with the line terminator configured on the searcher."] # [doc = " (If the searcher is configured to use `CRLF` as the line terminator,"] # [doc = " then this byte is replaced by just `LF`.)"] # [doc = ""] # [doc = " When searching is performed using a fixed size buffer, then the"] # [doc = " contents of that buffer are always searched for the presence of this"] # [doc = " byte and replaced with the line terminator. In effect, the caller is"] # [doc = " guaranteed to never observe this byte while searching."] # [doc = ""] # [doc = " When searching is performed with the entire contents mapped into"] # [doc = " memory, then this setting has no effect and is ignored."] pub fn convert (binary_byte : u8) -> BinaryDetection { BinaryDetection (line_buffer :: BinaryDetection :: Convert (binary_byte)) } # [doc = " If this binary detection uses the \"quit\" strategy, then this returns"] # [doc = " the byte that will cause a search to quit. In any other case, this"] # [doc = " returns `None`."] pub fn quit_byte (& self) -> Option < u8 > { match self . 0 { line_buffer :: BinaryDetection :: Quit (b) => Some (b) , _ => None , } } # [doc = " If this binary detection uses the \"convert\" strategy, then this returns"] # [doc = " the byte that will be replaced by the line terminator. In any other"] # [doc = " case, this returns `None`."] pub fn convert_byte (& self) -> Option < u8 > { match self . 0 { line_buffer :: BinaryDetection :: Convert (b) => Some (b) , _ => None , } } }
};
}
