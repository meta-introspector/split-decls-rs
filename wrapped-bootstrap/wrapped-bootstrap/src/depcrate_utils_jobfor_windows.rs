// Generated macro for for_windows (module)
macro_rules! Depcrate_utils_jobfor_windows {
() => {
// Module: crate::utils::job
// Provides: {"for_windows"}
// Dependencies: {}
# [doc = " Job management on Windows for bootstrapping"] # [doc = ""] # [doc = " Most of the time when you're running a build system (e.g., make) you expect"] # [doc = " Ctrl-C or abnormal termination to actually terminate the entire tree of"] # [doc = " processes in play. This currently works \"by"] # [doc = " default\" on Unix platforms because Ctrl-C actually sends a signal to the"] # [doc = " *process group* so everything will get torn"] # [doc = " down. On Windows, however, Ctrl-C is only sent to processes in the same console."] # [doc = " If a process is detached or attached to another console, it won't receive the"] # [doc = " signal."] # [doc = ""] # [doc = " To achieve the same semantics on Windows we use Job Objects to ensure that"] # [doc = " all processes die at the same time. Job objects have a mode of operation"] # [doc = " where when all handles to the object are closed it causes all child"] # [doc = " processes associated with the object to be terminated immediately."] # [doc = " Conveniently whenever a process in the job object spawns a new process the"] # [doc = " child will be associated with the job object as well. This means if we add"] # [doc = " ourselves to the job object we create then everything will get torn down!"] # [doc = ""] # [doc = " Unfortunately most of the time the build system is actually called from a"] # [doc = " python wrapper (which manages things like building the build system) so this"] # [doc = " all doesn't quite cut it so far. To go the last mile we duplicate the job"] # [doc = " object handle into our parent process (a python process probably) and then"] # [doc = " close our own handle. This means that the only handle to the job object"] # [doc = " resides in the parent python process, so when python dies the whole build"] # [doc = " system dies (as one would probably expect!)."] # [doc = ""] # [doc = " Note that this is a Windows specific module as none of this logic is required on Unix."] # [cfg (windows)] mod for_windows { use std :: ffi :: c_void ; use std :: io ; use windows :: Win32 :: Foundation :: CloseHandle ; use windows :: Win32 :: System :: Diagnostics :: Debug :: { SEM_NOGPFAULTERRORBOX , SetErrorMode , THREAD_ERROR_MODE , } ; use windows :: Win32 :: System :: JobObjects :: { AssignProcessToJobObject , CreateJobObjectW , JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE , JOB_OBJECT_LIMIT_PRIORITY_CLASS , JOBOBJECT_EXTENDED_LIMIT_INFORMATION , JobObjectExtendedLimitInformation , SetInformationJobObject , } ; use windows :: Win32 :: System :: Threading :: { BELOW_NORMAL_PRIORITY_CLASS , GetCurrentProcess } ; use windows :: core :: PCWSTR ; use crate :: Build ; pub unsafe fn setup (build : & mut Build) { unsafe { let mode = SetErrorMode (THREAD_ERROR_MODE :: default ()) ; SetErrorMode (mode & ! SEM_NOGPFAULTERRORBOX) ; let job = CreateJobObjectW (None , PCWSTR :: null ()) . unwrap () ; let mut info = JOBOBJECT_EXTENDED_LIMIT_INFORMATION :: default () ; info . BasicLimitInformation . LimitFlags = JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE ; if build . config . low_priority { info . BasicLimitInformation . LimitFlags |= JOB_OBJECT_LIMIT_PRIORITY_CLASS ; info . BasicLimitInformation . PriorityClass = BELOW_NORMAL_PRIORITY_CLASS . 0 ; } let r = SetInformationJobObject (job , JobObjectExtendedLimitInformation , & info as * const _ as * const c_void , size_of_val (& info) as u32 ,) ; assert ! (r . is_ok () , "{}" , io :: Error :: last_os_error ()) ; let r = AssignProcessToJobObject (job , GetCurrentProcess ()) ; if r . is_err () { CloseHandle (job) . ok () ; return ; } } } }
};
}
