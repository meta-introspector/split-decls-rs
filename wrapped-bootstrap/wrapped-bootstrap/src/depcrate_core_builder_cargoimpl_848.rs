// Generated macro for impl_848 (impl)
macro_rules! Depcrate_core_builder_cargoimpl_848 {
() => {
// Module: crate::core::builder::cargo
// Provides: {"impl_848"}
// Dependencies: {}
impl Cargo { # [doc = " Calls [`Builder::cargo`] and [`Cargo::configure_linker`] to prepare an invocation of `cargo`"] # [doc = " to be run."] # [track_caller] pub fn new (builder : & Builder < '_ > , compiler : Compiler , mode : Mode , source_type : SourceType , target : TargetSelection , cmd_kind : Kind ,) -> Cargo { let mut cargo = builder . cargo (compiler , mode , source_type , target , cmd_kind) ; match cmd_kind { Kind :: Clean | Kind :: Check | Kind :: Format | Kind :: Setup => { } _ => { cargo . configure_linker (builder) ; } } cargo } pub fn release_build (& mut self , release_build : bool) { self . release_build = release_build ; } pub fn compiler (& self) -> Compiler { self . compiler } pub fn into_cmd (self) -> BootstrapCommand { self . into () } # [doc = " Same as [`Cargo::new`] except this one doesn't configure the linker with"] # [doc = " [`Cargo::configure_linker`]."] # [track_caller] pub fn new_for_mir_opt_tests (builder : & Builder < '_ > , compiler : Compiler , mode : Mode , source_type : SourceType , target : TargetSelection , cmd_kind : Kind ,) -> Cargo { builder . cargo (compiler , mode , source_type , target , cmd_kind) } pub fn rustdocflag (& mut self , arg : & str) -> & mut Cargo { self . rustdocflags . arg (arg) ; self } pub fn rustflag (& mut self , arg : & str) -> & mut Cargo { self . rustflags . arg (arg) ; self } pub fn arg (& mut self , arg : impl AsRef < OsStr >) -> & mut Cargo { self . args . push (arg . as_ref () . into ()) ; self } pub fn args < I , S > (& mut self , args : I) -> & mut Cargo where I : IntoIterator < Item = S > , S : AsRef < OsStr > , { for arg in args { self . arg (arg . as_ref ()) ; } self } # [doc = " Add an env var to the cargo command instance. Note that `RUSTFLAGS`/`RUSTDOCFLAGS` must go"] # [doc = " through [`Cargo::rustdocflags`] and [`Cargo::rustflags`] because inconsistent `RUSTFLAGS`"] # [doc = " and `RUSTDOCFLAGS` usages will trigger spurious rebuilds."] pub fn env (& mut self , key : impl AsRef < OsStr > , value : impl AsRef < OsStr >) -> & mut Cargo { assert_ne ! (key . as_ref () , "RUSTFLAGS") ; assert_ne ! (key . as_ref () , "RUSTDOCFLAGS") ; self . command . env (key . as_ref () , value . as_ref ()) ; self } # [doc = " Append a value to an env var of the cargo command instance."] # [doc = " If the variable was unset previously, this is equivalent to [`Cargo::env`]."] # [doc = " If the variable was already set, this will append `delimiter` and then `value` to it."] # [doc = ""] # [doc = " Note that this only considers the existence of the env. var. configured on this `Cargo`"] # [doc = " instance. It does not look at the environment of this process."] pub fn append_to_env (& mut self , key : impl AsRef < OsStr > , value : impl AsRef < OsStr > , delimiter : impl AsRef < OsStr > ,) -> & mut Cargo { assert_ne ! (key . as_ref () , "RUSTFLAGS") ; assert_ne ! (key . as_ref () , "RUSTDOCFLAGS") ; let key = key . as_ref () ; if let Some ((_ , Some (previous_value))) = self . command . get_envs () . find (| (k , _) | * k == key) { let mut combined : OsString = previous_value . to_os_string () ; combined . push (delimiter . as_ref ()) ; combined . push (value . as_ref ()) ; self . env (key , combined) } else { self . env (key , value) } } pub fn add_rustc_lib_path (& mut self , builder : & Builder < '_ >) { builder . add_rustc_lib_path (self . compiler , & mut self . command) ; } pub fn current_dir (& mut self , dir : & Path) -> & mut Cargo { self . command . current_dir (dir) ; self } # [doc = " Adds nightly-only features that this invocation is allowed to use."] # [doc = ""] # [doc = " By default, all nightly features are allowed. Once this is called, it will be restricted to"] # [doc = " the given set."] pub fn allow_features (& mut self , features : & str) -> & mut Cargo { if ! self . allow_features . is_empty () { self . allow_features . push (',') ; } self . allow_features . push_str (features) ; self } fn configure_linker (& mut self , builder : & Builder < '_ >) -> & mut Cargo { let target = self . target ; let compiler = self . compiler ; if builder . config . rpath_enabled (target) && helpers :: use_host_linker (target) { let libdir = builder . sysroot_libdir_relative (compiler) . to_str () . unwrap () ; let rpath = if target . contains ("apple") { self . rustflags . arg ("-Zosx-rpath-install-name") ; Some (format ! ("-Wl,-rpath,@loader_path/../{libdir}")) } else if ! target . is_windows () && ! target . contains ("cygwin") && ! target . contains ("aix") && ! target . contains ("xous") { self . rustflags . arg ("-Clink-args=-Wl,-z,origin") ; Some (format ! ("-Wl,-rpath,$ORIGIN/../{libdir}")) } else { None } ; if let Some (rpath) = rpath { self . rustflags . arg (& format ! ("-Clink-args={rpath}")) ; } } for arg in linker_args (builder , compiler . host , LldThreads :: Yes) { self . hostflags . arg (& arg) ; } if let Some (target_linker) = builder . linker (target) { let target = crate :: envify (& target . triple) ; self . command . env (format ! ("CARGO_TARGET_{target}_LINKER") , target_linker) ; } for flag in linker_flags (builder , target , LldThreads :: Yes) { self . rustflags . arg (& flag) ; } for arg in linker_args (builder , target , LldThreads :: Yes) { self . rustdocflags . arg (& arg) ; } if ! builder . config . dry_run () && builder . cc [& target] . args () . iter () . any (| arg | arg == "-gz") { self . rustflags . arg ("-Clink-arg=-gz") ; } self . rustflags . arg ("-Alinker-messages") ; if target . is_msvc () { if let Some (ref cl) = builder . config . llvm_clang_cl { if ! target . starts_with ("aarch64") && ! target . starts_with ("arm64ec") { self . command . env ("CC" , cl) . env ("CXX" , cl) ; } } } else { let ccache = builder . config . ccache . as_ref () ; let ccacheify = | s : & Path | { let ccache = match ccache { Some (ref s) => s , None => return s . display () . to_string () , } ; match & ccache [..] { "ccache" | "sccache" => format ! ("{} {}" , ccache , s . display ()) , _ => s . display () . to_string () , } } ; let triple_underscored = target . triple . replace ('-' , "_") ; let cc = ccacheify (& builder . cc (target)) ; self . command . env (format ! ("CC_{triple_underscored}") , & cc) ; let env = format ! ("CFLAGS_{triple_underscored}") ; let mut cflags = builder . cc_unhandled_cflags (target , GitRepo :: Rustc , CLang :: C) . join (" ") ; if let Ok (var) = std :: env :: var (& env) { cflags . push (' ') ; cflags . push_str (& var) ; } self . command . env (env , & cflags) ; if let Some (ar) = builder . ar (target) { let ranlib = format ! ("{} s" , ar . display ()) ; self . command . env (format ! ("AR_{triple_underscored}") , ar) . env (format ! ("RANLIB_{triple_underscored}") , ranlib) ; } if let Ok (cxx) = builder . cxx (target) { let cxx = ccacheify (& cxx) ; self . command . env (format ! ("CXX_{triple_underscored}") , & cxx) ; let env = format ! ("CXXFLAGS_{triple_underscored}") ; let mut cxxflags = builder . cc_unhandled_cflags (target , GitRepo :: Rustc , CLang :: Cxx) . join (" ") ; if let Ok (var) = std :: env :: var (& env) { cxxflags . push (' ') ; cxxflags . push_str (& var) ; } self . command . env (& env , cxxflags) ; } } self } }
};
}
