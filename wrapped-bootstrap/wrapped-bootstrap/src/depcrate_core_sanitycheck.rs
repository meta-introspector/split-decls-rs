// Generated macro for check (function)
macro_rules! Depcrate_core_sanitycheck {
() => {
// Module: crate::core::sanity
// Provides: {"check"}
// Dependencies: {}
pub fn check (build : & mut Build) { let mut skip_target_sanity = env :: var_os ("BOOTSTRAP_SKIP_TARGET_SANITY") . is_some_and (| s | s == "1" || s == "true") ; skip_target_sanity |= build . config . cmd . kind () == Kind :: Check ; let skipped_paths = [OsStr :: new ("mir-opt") , OsStr :: new ("miri")] ; skip_target_sanity |= build . config . paths . iter () . any (| path | { path . components () . any (| component | skipped_paths . contains (& component . as_os_str ())) }) ; let path = env :: var_os ("PATH") . unwrap_or_default () ; if cfg ! (windows) && path . to_string_lossy () . contains ('\"') { panic ! ("PATH contains invalid character '\"'") ; } let mut cmd_finder = Finder :: new () ; if build . rust_info () . is_managed_git_subrepository () { cmd_finder . must_have ("git") ; } # [cfg (not (test))] if ! build . config . dry_run () && ! build . host_target . is_msvc () && build . config . llvm_from_ci { let builder = Builder :: new (build) ; let libcxx_version = builder . ensure (tool :: LibcxxVersionTool { target : build . host_target }) ; match libcxx_version { tool :: LibcxxVersion :: Gnu (version) => { if LIBSTDCXX_MIN_VERSION_THRESHOLD > version { eprintln ! ("\nYour system's libstdc++ version is too old for the `llvm.download-ci-llvm` option.") ; eprintln ! ("Current version detected: '{version}'") ; eprintln ! ("Minimum required version: '{LIBSTDCXX_MIN_VERSION_THRESHOLD}'") ; eprintln ! ("Consider upgrading libstdc++ or disabling the `llvm.download-ci-llvm` option.") ; eprintln ! ("If you choose to upgrade libstdc++, run `x clean` or delete `build/host/libcxx-version` manually after the upgrade.") ; } } tool :: LibcxxVersion :: Llvm (_) => { } } } let building_llvm = ! build . config . llvm_from_ci && build . hosts . iter () . any (| host | { build . config . llvm_enabled (* host) && build . config . target_config . get (host) . map (| config | config . llvm_config . is_none ()) . unwrap_or (true) }) ; let need_cmake = building_llvm || build . config . any_sanitizers_to_build () ; if need_cmake && cmd_finder . maybe_have ("cmake") . is_none () { eprintln ! ("
Couldn't find required command: cmake

You should install cmake, or set `download-ci-llvm = true` in the
`[llvm]` section of `bootstrap.toml` to download LLVM rather
than building it.
") ; crate :: exit ! (1) ; } build . config . python = build . config . python . take () . map (| p | cmd_finder . must_have (p)) . or_else (| | env :: var_os ("BOOTSTRAP_PYTHON") . map (PathBuf :: from)) . or_else (| | cmd_finder . maybe_have ("python")) . or_else (| | cmd_finder . maybe_have ("python3")) . or_else (| | cmd_finder . maybe_have ("python2")) ; build . config . nodejs = build . config . nodejs . take () . map (| p | cmd_finder . must_have (p)) . or_else (| | cmd_finder . maybe_have ("node")) . or_else (| | cmd_finder . maybe_have ("nodejs")) ; build . config . npm = build . config . npm . take () . map (| p | cmd_finder . must_have (p)) . or_else (| | cmd_finder . maybe_have ("npm")) ; build . config . gdb = build . config . gdb . take () . map (| p | cmd_finder . must_have (p)) . or_else (| | cmd_finder . maybe_have ("gdb")) ; build . config . reuse = build . config . reuse . take () . map (| p | cmd_finder . must_have (p)) . or_else (| | cmd_finder . maybe_have ("reuse")) ; let stage0_supported_target_list : HashSet < String > = command (& build . config . initial_rustc) . args (["--print" , "target-list"]) . run_in_dry_run () . run_capture_stdout (& build) . stdout () . lines () . map (| s | s . to_string ()) . collect () ; let skip_tools_checks = build . config . dry_run () || matches ! (build . config . cmd , Subcommand :: Clean { .. } | Subcommand :: Check { .. } | Subcommand :: Format { .. } | Subcommand :: Setup { .. }) ; for target in & build . targets { if target . contains ("emscripten") { continue ; } if target . contains ("wasm32") { continue ; } if skip_target_sanity && target != & build . host_target { continue ; } if cfg ! (not (test)) && ! skip_target_sanity && ! build . local_rebuild { let mut has_target = false ; let target_str = target . to_string () ; let missing_targets_hashset : HashSet < _ > = STAGE0_MISSING_TARGETS . iter () . map (| t | t . to_string ()) . collect () ; let duplicated_targets : Vec < _ > = stage0_supported_target_list . intersection (& missing_targets_hashset) . collect () ; if ! duplicated_targets . is_empty () { println ! ("Following targets supported from the stage0 compiler, please remove them from STAGE0_MISSING_TARGETS list.") ; for duplicated_target in duplicated_targets { println ! ("  {duplicated_target}") ; } std :: process :: exit (1) ; } has_target |= stage0_supported_target_list . contains (& target_str) ; has_target |= STAGE0_MISSING_TARGETS . contains (& target_str . as_str ()) ; if ! has_target { if target . filepath () . is_some_and (| p | p . exists ()) { has_target = true ; } else if let Some (custom_target_path) = env :: var_os ("RUST_TARGET_PATH") { let mut target_filename = OsString :: from (& target_str) ; target_filename . push (".json") ; let walker = walkdir :: WalkDir :: new (custom_target_path) . into_iter () ; for entry in walker . filter_map (| e | e . ok ()) { has_target |= entry . file_name () == target_filename ; } } } if ! has_target { panic ! ("No such target exists in the target list,\n\
                     make sure to correctly specify the location \
                     of the JSON specification file \
                     for custom targets!\n\
                     Use BOOTSTRAP_SKIP_TARGET_SANITY=1 to \
                     bypass this check.") ; } } if ! skip_tools_checks { cmd_finder . must_have (build . cc (* target)) ; if let Some (ar) = build . ar (* target) { cmd_finder . must_have (ar) ; } } } if ! skip_tools_checks { for host in & build . hosts { cmd_finder . must_have (build . cxx (* host) . unwrap ()) ; if build . config . llvm_enabled (* host) { let filecheck = build . llvm_filecheck (build . host_target) ; if ! filecheck . starts_with (& build . out) && ! filecheck . exists () && build . config . codegen_tests { panic ! ("FileCheck executable {filecheck:?} does not exist") ; } } } } for target in & build . targets { build . config . target_config . entry (* target) . or_insert_with (| | Target :: from_triple (& target . triple)) ; if target . contains ("wasm") && (* build . config . optimized_compiler_builtins (* target) != CompilerBuiltins :: BuildRustOnly || build . config . rust_std_features . contains ("compiler-builtins-c")) { let cc_tool = build . cc_tool (* target) ; if ! cc_tool . is_like_clang () && ! cc_tool . path () . ends_with ("emcc") { panic ! ("Clang is required to build C code for Wasm targets, got `{}` instead\n\
                    this is because compiler-builtins is configured to build C source. Either \
                    ensure Clang is used, or adjust this configuration." , cc_tool . path () . display ()) ; } } if (target . contains ("-none-") || target . contains ("nvptx")) && build . no_std (* target) == Some (false) { panic ! ("All the *-none-* and nvptx* targets are no-std targets") } if skip_target_sanity && target != & build . host_target { continue ; } if target . contains ("musl") && ! target . contains ("unikraft") { match build . musl_libdir (* target) { Some (libdir) => { if fs :: metadata (libdir . join ("libc.a")) . is_err () { panic ! ("couldn't find libc.a in musl libdir: {}" , libdir . display ()) ; } } None => panic ! ("when targeting MUSL either the rust.musl-root \
                            option or the target.$TARGET.musl-root option must \
                            be specified in bootstrap.toml") , } } if need_cmake && target . is_msvc () { let out = command ("cmake") . arg ("--help") . run_in_dry_run () . run_capture_stdout (& build) . stdout () ; if ! out . contains ("Visual Studio") { panic ! ("
cmake does not support Visual Studio generators.

This is likely due to it being an msys/cygwin build of cmake,
rather than the required windows version, built using MinGW
or Visual Studio.

If you are building under msys2 try installing the mingw-w64-x86_64-cmake
package instead of cmake:

$ pacman -R cmake && pacman -S mingw-w64-x86_64-cmake
") ; } } if target . contains ("wasip") && ! target . contains ("wasip1") && ! build . tool_enabled ("wasm-component-ld") { cmd_finder . must_have ("wasm-component-ld") ; } } if let Some (ref s) = build . config . ccache { cmd_finder . must_have (s) ; } }
};
}
