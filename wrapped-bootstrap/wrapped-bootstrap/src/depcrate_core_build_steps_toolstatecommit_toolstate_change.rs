// Generated macro for commit_toolstate_change (function)
macro_rules! Depcrate_core_build_steps_toolstatecommit_toolstate_change {
() => {
// Module: crate::core::build_steps::toolstate
// Provides: {"commit_toolstate_change"}
// Dependencies: {}
# [doc = " This function `commit_toolstate_change` provides functionality for pushing a change"] # [doc = " to the `rust-toolstate` repository."] # [doc = ""] # [doc = " The function relies on a GitHub bot user, which should have a Personal access"] # [doc = " token defined in the environment variable $TOOLSTATE_REPO_ACCESS_TOKEN. If for"] # [doc = " some reason you need to change the token, please update the Azure Pipelines"] # [doc = " variable group."] # [doc = ""] # [doc = "   1. Generate a new Personal access token:"] # [doc = ""] # [doc = "       * Login to the bot account, and go to Settings -> Developer settings ->"] # [doc = "         Personal access tokens"] # [doc = "       * Click \"Generate new token\""] # [doc = "       * Enable the \"public_repo\" permission, then click \"Generate token\""] # [doc = "       * Copy the generated token (should be a 40-digit hexadecimal number)."] # [doc = "         Save it somewhere secure, as the token would be gone once you leave"] # [doc = "         the page."] # [doc = ""] # [doc = "   2. Update the variable group in Azure Pipelines"] # [doc = ""] # [doc = "       * Ping a member of the infrastructure team to do this."] # [doc = ""] # [doc = "   4. Replace the email address below if the bot account identity is changed"] # [doc = ""] # [doc = "       * See <https://help.github.com/articles/about-commit-email-addresses/>"] # [doc = "         if a private email by GitHub is wanted."] fn commit_toolstate_change (builder : & Builder < '_ > , current_toolstate : & ToolstateData) { let message = format ! ("({} CI update)" , OS . expect ("linux/windows only")) ; let mut success = false ; for _ in 1 ..= 5 { publish_test_results (builder , current_toolstate) ; let status = helpers :: git (Some (Path :: new (TOOLSTATE_DIR))) . allow_failure () . arg ("commit") . arg ("-a") . arg ("-m") . arg (& message) . run (builder) ; if ! status { success = true ; break ; } let status = helpers :: git (Some (Path :: new (TOOLSTATE_DIR))) . allow_failure () . arg ("push") . arg ("origin") . arg ("master") . run (builder) ; if status { success = true ; break ; } eprintln ! ("Sleeping for 3 seconds before retrying push") ; std :: thread :: sleep (std :: time :: Duration :: from_secs (3)) ; helpers :: git (Some (Path :: new (TOOLSTATE_DIR))) . arg ("fetch") . arg ("origin") . arg ("master") . run (builder) ; helpers :: git (Some (Path :: new (TOOLSTATE_DIR))) . arg ("reset") . arg ("--hard") . arg ("origin/master") . run (builder) ; } if ! success { panic ! ("Failed to update toolstate repository with new data") ; } }
};
}
