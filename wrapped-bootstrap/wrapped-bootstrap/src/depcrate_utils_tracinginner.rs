// Generated macro for inner (module)
macro_rules! Depcrate_utils_tracinginner {
() => {
// Module: crate::utils::tracing
// Provides: {"inner"}
// Dependencies: {}
# [cfg (feature = "tracing")] mod inner { use std :: fmt :: Debug ; use std :: fs :: File ; use std :: io :: Write ; use std :: path :: { Path , PathBuf } ; use std :: sync :: atomic :: Ordering ; use chrono :: { DateTime , Utc } ; use tracing :: field :: { Field , Visit } ; use tracing :: { Event , Id , Level , Subscriber } ; use tracing_subscriber :: layer :: { Context , SubscriberExt } ; use tracing_subscriber :: registry :: { LookupSpan , SpanRef } ; use tracing_subscriber :: { EnvFilter , Layer } ; use super :: { COMMAND_SPAN_TARGET , IO_SPAN_TARGET } ; use crate :: STEP_SPAN_TARGET ; pub fn setup_tracing (env_name : & str) -> TracingGuard { let filter = EnvFilter :: from_env (env_name) ; let registry = tracing_subscriber :: registry () . with (filter) . with (TracingPrinter :: default ()) ; let tempdir = tempfile :: TempDir :: new () . expect ("Cannot create temporary directory") ; let chrome_tracing_path = tempdir . path () . join ("bootstrap-trace.json") ; let file = std :: io :: BufWriter :: new (File :: create (& chrome_tracing_path) . unwrap ()) ; let chrome_layer = tracing_chrome :: ChromeLayerBuilder :: new () . writer (file) . include_args (true) . name_fn (Box :: new (| event_or_span | match event_or_span { tracing_chrome :: EventOrSpan :: Event (e) => e . metadata () . name () . to_string () , tracing_chrome :: EventOrSpan :: Span (s) => { if s . metadata () . target () == STEP_SPAN_TARGET && let Some (extension) = s . extensions () . get :: < StepNameExtension > () { extension . 0 . clone () } else if s . metadata () . target () == COMMAND_SPAN_TARGET && let Some (extension) = s . extensions () . get :: < CommandNameExtension > () { extension . 0 . clone () } else { s . metadata () . name () . to_string () } } })) ; let (chrome_layer , guard) = chrome_layer . build () ; tracing :: subscriber :: set_global_default (registry . with (chrome_layer)) . unwrap () ; TracingGuard { guard , _tempdir : tempdir , chrome_tracing_path } } pub struct TracingGuard { guard : tracing_chrome :: FlushGuard , _tempdir : tempfile :: TempDir , chrome_tracing_path : std :: path :: PathBuf , } impl TracingGuard { pub fn copy_to_dir (self , dir : & std :: path :: Path) { drop (self . guard) ; crate :: utils :: helpers :: move_file (& self . chrome_tracing_path , dir . join ("chrome-trace.json") ,) . unwrap () ; } } # [doc = " Visitor that extracts both known and unknown field values from events and spans."] # [derive (Default)] struct FieldValues { # [doc = " Main event message"] message : Option < String > , # [doc = " Name of a recorded psna"] step_name : Option < String > , # [doc = " Short name of an executed command"] cmd_name : Option < String > , # [doc = " The rest of arbitrary event/span fields"] fields : Vec < (& 'static str , String) > , } impl Visit for FieldValues { # [doc = " Record fields if possible using `record_str`, to avoid rendering simple strings with"] # [doc = " their `Debug` representation, which adds extra quotes."] fn record_str (& mut self , field : & Field , value : & str) { match field . name () { "step_name" => { self . step_name = Some (value . to_string ()) ; } "cmd_name" => { self . cmd_name = Some (value . to_string ()) ; } name => { self . fields . push ((name , value . to_string ())) ; } } } fn record_debug (& mut self , field : & Field , value : & dyn Debug) { let formatted = format ! ("{value:?}") ; match field . name () { "message" => { self . message = Some (formatted) ; } name => { self . fields . push ((name , formatted)) ; } } } } # [derive (Copy , Clone)] enum SpanAction { Enter , } # [doc = " Holds the name of a step span, stored in `tracing_subscriber`'s extensions."] struct StepNameExtension (String) ; # [doc = " Holds the name of a command span, stored in `tracing_subscriber`'s extensions."] struct CommandNameExtension (String) ; # [derive (Default)] struct TracingPrinter { indent : std :: sync :: atomic :: AtomicU32 , span_values : std :: sync :: Mutex < std :: collections :: HashMap < tracing :: Id , FieldValues > > , } impl TracingPrinter { fn format_header < W : Write > (& self , writer : & mut W , time : DateTime < Utc > , level : & Level ,) -> std :: io :: Result < () > { let timestamp = time . format ("%H:%M:%S.%3f") ; write ! (writer , "{timestamp} ") ? ; write ! (writer , "{level:>5} ") ? ; write ! (writer , "{}" , " " . repeat (self . indent . load (Ordering :: Relaxed) as usize)) } fn write_event < W : Write > (& self , writer : & mut W , event : & Event < '_ >) -> std :: io :: Result < () > { let now = Utc :: now () ; self . format_header (writer , now , event . metadata () . level ()) ? ; let mut field_values = FieldValues :: default () ; event . record (& mut field_values) ; if let Some (msg) = & field_values . message { write ! (writer , "{msg}") ? ; } if ! field_values . fields . is_empty () { if field_values . message . is_some () { write ! (writer , " ") ? ; } write ! (writer , "[") ? ; for (index , (name , value)) in field_values . fields . iter () . enumerate () { write ! (writer , "{name} = {value}") ? ; if index < field_values . fields . len () - 1 { write ! (writer , ", ") ? ; } } write ! (writer , "]") ? ; } write_location (writer , event . metadata ()) ? ; writeln ! (writer) ? ; Ok (()) } fn write_span < W : Write , S > (& self , writer : & mut W , span : SpanRef < '_ , S > , field_values : Option < & FieldValues > , action : SpanAction ,) -> std :: io :: Result < () > where S : for < 'lookup > LookupSpan < 'lookup > , { let now = Utc :: now () ; self . format_header (writer , now , span . metadata () . level ()) ? ; match action { SpanAction :: Enter => { write ! (writer , "> ") ? ; } } fn write_fields < 'a , I : IntoIterator < Item = & 'a (& 'a str , String) > , W : Write > (writer : & mut W , iter : I ,) -> std :: io :: Result < () > { let items = iter . into_iter () . collect :: < Vec < _ > > () ; if ! items . is_empty () { write ! (writer , " [") ? ; for (index , (name , value)) in items . iter () . enumerate () { write ! (writer , "{name} = {value}") ? ; if index < items . len () - 1 { write ! (writer , ", ") ? ; } } write ! (writer , "]") ? ; } Ok (()) } let write_with_location = | writer : & mut W | -> std :: io :: Result < () > { if let Some (values) = field_values { write_fields (writer , values . fields . iter () . filter (| (name , _) | * name != "location") ,) ? ; let location = & values . fields . iter () . find (| (name , _) | * name == "location") . unwrap () . 1 ; let (filename , line) = location . rsplit_once (':') . unwrap () ; let filename = shorten_filename (filename) ; write ! (writer , " ({filename}:{line})" ,) ? ; } Ok (()) } ; match span . metadata () . target () { STEP_SPAN_TARGET => { let name = field_values . and_then (| v | v . step_name . as_deref ()) . unwrap_or (span . name ()) ; write ! (writer , "{name}") ? ; if let Some (values) = field_values { let field = & values . fields [0] ; write ! (writer , " {{{}}}" , field . 1) ? ; } write_location (writer , span . metadata ()) ? ; } COMMAND_SPAN_TARGET => { write ! (writer , "{}" , span . name ()) ? ; write_with_location (writer) ? ; } IO_SPAN_TARGET => { write ! (writer , "{}" , span . name ()) ? ; write_with_location (writer) ? ; } _ => { write ! (writer , "{}" , span . name ()) ? ; if let Some (values) = field_values { write_fields (writer , values . fields . iter ()) ? ; } write_location (writer , span . metadata ()) ? ; } } writeln ! (writer) ? ; Ok (()) } } fn write_location < W : Write > (writer : & mut W , metadata : & 'static tracing :: Metadata < 'static > ,) -> std :: io :: Result < () > { if let Some (filename) = metadata . file () { let filename = shorten_filename (filename) ; write ! (writer , " ({filename}") ? ; if let Some (line) = metadata . line () { write ! (writer , ":{line}") ? ; } write ! (writer , ")") ? ; } Ok (()) } # [doc = " Keep only the module name and file name to make it shorter"] fn shorten_filename (filename : & str) -> String { Path :: new (filename) . components () . rev () . take (2) . collect :: < Vec < _ > > () . into_iter () . rev () . collect :: < PathBuf > () . display () . to_string () } impl < S > Layer < S > for TracingPrinter where S : Subscriber , S : for < 'lookup > LookupSpan < 'lookup > , { fn on_new_span (& self , attrs : & tracing :: span :: Attributes < '_ > , id : & Id , ctx : Context < '_ , S >) { let mut field_values = FieldValues :: default () ; attrs . record (& mut field_values) ; if attrs . metadata () . target () == STEP_SPAN_TARGET && let Some (step_name) = field_values . step_name . clone () { ctx . span (id) . unwrap () . extensions_mut () . insert (StepNameExtension (step_name)) ; } else if attrs . metadata () . target () == COMMAND_SPAN_TARGET && let Some (cmd_name) = field_values . cmd_name . clone () { ctx . span (id) . unwrap () . extensions_mut () . insert (CommandNameExtension (cmd_name)) ; } self . span_values . lock () . unwrap () . insert (id . clone () , field_values) ; } fn on_event (& self , event : & Event < '_ > , _ctx : Context < '_ , S >) { let mut writer = std :: io :: stderr () . lock () ; self . write_event (& mut writer , event) . unwrap () ; } fn on_enter (& self , id : & Id , ctx : Context < '_ , S >) { if let Some (span) = ctx . span (id) { let mut writer = std :: io :: stderr () . lock () ; let values = self . span_values . lock () . unwrap () ; let values = values . get (id) ; self . write_span (& mut writer , span , values , SpanAction :: Enter) . unwrap () ; } self . indent . fetch_add (1 , Ordering :: Relaxed) ; } fn on_exit (& self , _id : & Id , _ctx : Context < '_ , S >) { self . indent . fetch_sub (1 , Ordering :: Relaxed) ; } } }
};
}
