// Generated macro for Mode (enum)
macro_rules! DepcrateMode {
() => {
// Module: crate
// Provides: {"Mode"}
// Dependencies: {}
# [doc = " The various \"modes\" of invoking Cargo."] # [doc = ""] # [doc = " These entries currently correspond to the various output directories of the"] # [doc = " build system, with each mod generating output in a different directory."] # [derive (Debug , Hash , Clone , Copy , PartialEq , Eq)] pub enum Mode { # [doc = " Build the standard library, placing output in the \"stageN-std\" directory."] Std , # [doc = " Build librustc, and compiler libraries, placing output in the \"stageN-rustc\" directory."] Rustc , # [doc = " Build a codegen backend for rustc, placing the output in the \"stageN-codegen\" directory."] Codegen , # [doc = " Build a tool, placing output in the \"bootstrap-tools\""] # [doc = " directory. This is for miscellaneous sets of tools that extend"] # [doc = " bootstrap."] # [doc = ""] # [doc = " These tools are intended to be only executed on the host system that"] # [doc = " invokes bootstrap, and they thus cannot be cross-compiled."] # [doc = ""] # [doc = " They are always built using the stage0 compiler, and they"] # [doc = " can be compiled with stable Rust."] # [doc = ""] # [doc = " These tools also essentially do not participate in staging."] ToolBootstrap , # [doc = " Build a cross-compilable helper tool. These tools do not depend on unstable features or"] # [doc = " compiler internals, but they might be cross-compilable (so we cannot build them using the"] # [doc = " stage0 compiler, unlike `ToolBootstrap`)."] # [doc = ""] # [doc = " Some of these tools are also shipped in our `dist` archives."] # [doc = " While we could compile them using the stage0 compiler when not cross-compiling, we instead"] # [doc = " use the in-tree compiler (and std) to build them, so that we can ship e.g. std security"] # [doc = " fixes and avoid depending fully on stage0 for the artifacts that we ship."] # [doc = ""] # [doc = " This mode is used e.g. for linkers and linker tools invoked by rustc on its host target."] ToolTarget , # [doc = " Build a tool which uses the locally built std, placing output in the"] # [doc = " \"stageN-tools\" directory. Its usage is quite rare, mainly used by"] # [doc = " compiletest which needs libtest."] ToolStd , # [doc = " Build a tool which uses the `rustc_private` mechanism, and thus"] # [doc = " the locally built rustc rlib artifacts,"] # [doc = " placing the output in the \"stageN-tools\" directory. This is used for"] # [doc = " everything that links to rustc as a library, such as rustdoc, clippy,"] # [doc = " rustfmt, miri, etc."] ToolRustcPrivate , }
};
}
