macro_rules! nghttp2_error_code {
    () => {
        # [doc = " @enum"] # [doc = " The status codes for the RST_STREAM and GOAWAY frames."] pub type nghttp2_error_code = u32 ;
    };
}

nghttp2_error_code!()