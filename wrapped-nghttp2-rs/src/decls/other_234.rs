macro_rules! other_234 {
    () => {
        extern "C" { # [doc = " @function"] # [doc = ""] # [doc = " Performs post-process of HTTP Upgrade request.  This function can"] # [doc = " be called from both client and server, but the behavior is very"] # [doc = " different in each other."] # [doc = ""] # [doc = " .. warning::"] # [doc = ""] # [doc = "   This function is deprecated in favor of"] # [doc = "   `nghttp2_session_upgrade2()`, because this function lacks the"] # [doc = "   parameter to tell the library the request method used in the"] # [doc = "   original HTTP request.  This information is required for client"] # [doc = "   to validate actual response body length against content-length"] # [doc = "   header field (see `nghttp2_option_set_no_http_messaging()`).  If"] # [doc = "   HEAD is used in request, the length of response body must be 0"] # [doc = "   regardless of value included in content-length header field."] # [doc = ""] # [doc = " If called from client side, the |settings_payload| must be the"] # [doc = " value sent in ``HTTP2-Settings`` header field and must be decoded"] # [doc = " by base64url decoder.  The |settings_payloadlen| is the length of"] # [doc = " |settings_payload|.  The |settings_payload| is unpacked and its"] # [doc = " setting values will be submitted using `nghttp2_submit_settings()`."] # [doc = " This means that the client application code does not need to submit"] # [doc = " SETTINGS by itself.  The stream with stream ID=1 is opened and the"] # [doc = " |stream_user_data| is used for its stream_user_data.  The opened"] # [doc = " stream becomes half-closed (local) state."] # [doc = ""] # [doc = " If called from server side, the |settings_payload| must be the"] # [doc = " value received in ``HTTP2-Settings`` header field and must be"] # [doc = " decoded by base64url decoder.  The |settings_payloadlen| is the"] # [doc = " length of |settings_payload|.  It is treated as if the SETTINGS"] # [doc = " frame with that payload is received.  Thus, callback functions for"] # [doc = " the reception of SETTINGS frame will be invoked.  The stream with"] # [doc = " stream ID=1 is opened.  The |stream_user_data| is ignored.  The"] # [doc = " opened stream becomes half-closed (remote)."] # [doc = ""] # [doc = " This function returns 0 if it succeeds, or one of the following"] # [doc = " negative error codes:"] # [doc = ""] # [doc = " :enum:`NGHTTP2_ERR_NOMEM`"] # [doc = "     Out of memory."] # [doc = " :enum:`NGHTTP2_ERR_INVALID_ARGUMENT`"] # [doc = "     The |settings_payload| is badly formed."] # [doc = " :enum:`NGHTTP2_ERR_PROTO`"] # [doc = "     The stream ID 1 is already used or closed; or is not available."] pub fn nghttp2_session_upgrade (session : * mut nghttp2_session , settings_payload : * const u8 , settings_payloadlen : usize , stream_user_data : * mut :: std :: os :: raw :: c_void ,) -> :: std :: os :: raw :: c_int ; }
    };
}

other_234!();