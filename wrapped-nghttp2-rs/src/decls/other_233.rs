macro_rules! other_233 {
    () => {
        extern "C" { # [doc = " @function"] # [doc = ""] # [doc = " Creates idle stream with the given |stream_id|, and priority"] # [doc = " |pri_spec|."] # [doc = ""] # [doc = " The stream creation is done without sending PRIORITY frame, which"] # [doc = " means that peer does not know about the existence of this idle"] # [doc = " stream in the local endpoint."] # [doc = ""] # [doc = " RFC 7540 does not disallow the use of creation of idle stream with"] # [doc = " odd or even stream ID regardless of client or server.  So this"] # [doc = " function can create odd or even stream ID regardless of client or"] # [doc = " server.  But probably it is a bit safer to use the stream ID the"] # [doc = " local endpoint can initiate (in other words, use odd stream ID for"] # [doc = " client, and even stream ID for server), to avoid potential"] # [doc = " collision from peer's instruction.  Also we can use"] # [doc = " `nghttp2_session_set_next_stream_id()` to avoid to open created"] # [doc = " idle streams accidentally if we follow this recommendation."] # [doc = ""] # [doc = " If |session| is initialized as server, and ``pri_spec->stream_id``"] # [doc = " points to the idle stream, the idle stream is created if it does"] # [doc = " not exist.  The created idle stream will depend on root stream"] # [doc = " (stream 0) with weight 16."] # [doc = ""] # [doc = " Otherwise, if stream denoted by ``pri_spec->stream_id`` is not"] # [doc = " found, we use default priority instead of given |pri_spec|.  That"] # [doc = " is make stream depend on root stream with weight 16."] # [doc = ""] # [doc = " This function returns 0 if it succeeds, or one of the following"] # [doc = " negative error codes:"] # [doc = ""] # [doc = " :enum:`NGHTTP2_ERR_NOMEM`"] # [doc = "     Out of memory."] # [doc = " :enum:`NGHTTP2_ERR_INVALID_ARGUMENT`"] # [doc = "     Attempted to depend on itself; or stream denoted by |stream_id|"] # [doc = "     already exists; or |stream_id| cannot be used to create idle"] # [doc = "     stream (in other words, local endpoint has already opened"] # [doc = "     stream ID greater than or equal to the given stream ID; or"] # [doc = "     |stream_id| is 0"] pub fn nghttp2_session_create_idle_stream (session : * mut nghttp2_session , stream_id : i32 , pri_spec : * const nghttp2_priority_spec ,) -> :: std :: os :: raw :: c_int ; }
    };
}

other_233!();