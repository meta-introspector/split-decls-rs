macro_rules! other_257 {
    () => {
        extern "C" { # [doc = " @function"] # [doc = ""] # [doc = " Set local window size (local endpoints's window size) to the given"] # [doc = " |window_size| for the given stream denoted by |stream_id|.  To"] # [doc = " change connection level window size, specify 0 to |stream_id|.  To"] # [doc = " increase window size, this function may submit WINDOW_UPDATE frame"] # [doc = " to transmission queue."] # [doc = ""] # [doc = " The |flags| is currently ignored and should be"] # [doc = " :enum:`NGHTTP2_FLAG_NONE`."] # [doc = ""] # [doc = " This sounds similar to `nghttp2_submit_window_update()`, but there"] # [doc = " are 2 differences.  The first difference is that this function"] # [doc = " takes the absolute value of window size to set, rather than the"] # [doc = " delta.  To change the window size, this may be easier to use since"] # [doc = " the application just declares the intended window size, rather than"] # [doc = " calculating delta.  The second difference is that"] # [doc = " `nghttp2_submit_window_update()` affects the received bytes count"] # [doc = " which has not acked yet.  By the specification of"] # [doc = " `nghttp2_submit_window_update()`, to strictly increase the local"] # [doc = " window size, we have to submit delta including all received bytes"] # [doc = " count, which might not be desirable in some cases.  On the other"] # [doc = " hand, this function does not affect the received bytes count.  It"] # [doc = " just sets the local window size to the given value."] # [doc = ""] # [doc = " This function returns 0 if it succeeds, or one of the following"] # [doc = " negative error codes:"] # [doc = ""] # [doc = " :enum:`NGHTTP2_ERR_INVALID_ARGUMENT`"] # [doc = "     The |stream_id| is negative."] # [doc = " :enum:`NGHTTP2_ERR_NOMEM`"] # [doc = "     Out of memory."] pub fn nghttp2_session_set_local_window_size (session : * mut nghttp2_session , flags : u8 , stream_id : i32 , window_size : i32 ,) -> :: std :: os :: raw :: c_int ; }
    };
}

other_257!()