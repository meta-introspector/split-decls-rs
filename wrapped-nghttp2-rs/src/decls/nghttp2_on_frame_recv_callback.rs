macro_rules! nghttp2_on_frame_recv_callback {
    () => {
        # [doc = " @functypedef"] # [doc = ""] # [doc = " Callback function invoked by `nghttp2_session_recv()` and"] # [doc = " `nghttp2_session_mem_recv()` when a frame is received.  The"] # [doc = " |user_data| pointer is the third argument passed in to the call to"] # [doc = " `nghttp2_session_client_new()` or `nghttp2_session_server_new()`."] # [doc = ""] # [doc = " If frame is HEADERS or PUSH_PROMISE, the ``nva`` and ``nvlen``"] # [doc = " member of their data structure are always ``NULL`` and 0"] # [doc = " respectively.  The header name/value pairs are emitted via"] # [doc = " :type:`nghttp2_on_header_callback`."] # [doc = ""] # [doc = " For HEADERS, PUSH_PROMISE and DATA frames, this callback may be"] # [doc = " called after stream is closed (see"] # [doc = " :type:`nghttp2_on_stream_close_callback`).  The application should"] # [doc = " check that stream is still alive using its own stream management or"] # [doc = " :func:`nghttp2_session_get_stream_user_data()`."] # [doc = ""] # [doc = " Only HEADERS and DATA frame can signal the end of incoming data."] # [doc = " If ``frame->hd.flags & NGHTTP2_FLAG_END_STREAM`` is nonzero, the"] # [doc = " |frame| is the last frame from the remote peer in this stream."] # [doc = ""] # [doc = " This callback won't be called for CONTINUATION frames."] # [doc = " HEADERS/PUSH_PROMISE + CONTINUATIONs are treated as single frame."] # [doc = ""] # [doc = " The implementation of this function must return 0 if it succeeds."] # [doc = " If nonzero value is returned, it is treated as fatal error and"] # [doc = " `nghttp2_session_recv()` and `nghttp2_session_mem_recv()` functions"] # [doc = " immediately return :enum:`NGHTTP2_ERR_CALLBACK_FAILURE`."] # [doc = ""] # [doc = " To set this callback to :type:`nghttp2_session_callbacks`, use"] # [doc = " `nghttp2_session_callbacks_set_on_frame_recv_callback()`."] pub type nghttp2_on_frame_recv_callback = :: std :: option :: Option < unsafe extern "C" fn (session : * mut nghttp2_session , frame : * const nghttp2_frame , user_data : * mut :: std :: os :: raw :: c_void ,) -> :: std :: os :: raw :: c_int , > ;
    };
}

nghttp2_on_frame_recv_callback!();