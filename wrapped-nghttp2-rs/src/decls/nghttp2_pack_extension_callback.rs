macro_rules! nghttp2_pack_extension_callback {
    () => {
        # [doc = " @functypedef"] # [doc = ""] # [doc = " Callback function invoked when library asks the application to pack"] # [doc = " extension payload in its wire format.  The frame header will be"] # [doc = " packed by library.  Application must pack payload only."] # [doc = " ``frame->ext.payload`` is the object passed to"] # [doc = " `nghttp2_submit_extension()` as payload parameter.  Application"] # [doc = " must pack extension payload to the |buf| of its capacity |len|"] # [doc = " bytes.  The |len| is at least 16KiB."] # [doc = ""] # [doc = " The implementation of this function should return the number of"] # [doc = " bytes written into |buf| when it succeeds."] # [doc = ""] # [doc = " To abort processing this extension frame, return"] # [doc = " :enum:`NGHTTP2_ERR_CANCEL`, and"] # [doc = " :type:`nghttp2_on_frame_not_send_callback` will be invoked."] # [doc = ""] # [doc = " If fatal error occurred, application should return"] # [doc = " :enum:`NGHTTP2_ERR_CALLBACK_FAILURE`.  In this case,"] # [doc = " `nghttp2_session_send()` and `nghttp2_session_mem_send()` functions"] # [doc = " immediately return :enum:`NGHTTP2_ERR_CALLBACK_FAILURE`.  If the"] # [doc = " other values are returned, currently they are treated as"] # [doc = " :enum:`NGHTTP2_ERR_CALLBACK_FAILURE`.  If the return value is"] # [doc = " strictly larger than |len|, it is treated as"] # [doc = " :enum:`NGHTTP2_ERR_CALLBACK_FAILURE`."] pub type nghttp2_pack_extension_callback = :: std :: option :: Option < unsafe extern "C" fn (session : * mut nghttp2_session , buf : * mut u8 , len : usize , frame : * const nghttp2_frame , user_data : * mut :: std :: os :: raw :: c_void ,) -> isize , > ;
    };
}

nghttp2_pack_extension_callback!();