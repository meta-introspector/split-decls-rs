macro_rules! nghttp2_on_invalid_header_callback2 {
    () => {
        # [doc = " @functypedef"] # [doc = ""] # [doc = " Callback function invoked when a invalid header name/value pair is"] # [doc = " received for the |frame|."] # [doc = ""] # [doc = " The parameter and behaviour are similar to"] # [doc = " :type:`nghttp2_on_header_callback2`.  The difference is that this"] # [doc = " callback is only invoked when a invalid header name/value pair is"] # [doc = " received which is silently ignored if this callback is not set."] # [doc = " Only invalid regular header field are passed to this callback.  In"] # [doc = " other words, invalid pseudo header field is not passed to this"] # [doc = " callback.  Also header fields which includes upper cased latter are"] # [doc = " also treated as error without passing them to this callback."] # [doc = ""] # [doc = " This callback is only considered if HTTP messaging validation is"] # [doc = " turned on (which is on by default, see"] # [doc = " `nghttp2_option_set_no_http_messaging()`)."] # [doc = ""] # [doc = " With this callback, application inspects the incoming invalid"] # [doc = " field, and it also can reset stream from this callback by returning"] # [doc = " :enum:`NGHTTP2_ERR_TEMPORAL_CALLBACK_FAILURE`.  By default, the"] # [doc = " error code is :enum:`NGHTTP2_INTERNAL_ERROR`.  To change the error"] # [doc = " code, call `nghttp2_submit_rst_stream()` with the error code of"] # [doc = " choice in addition to returning"] # [doc = " :enum:`NGHTTP2_ERR_TEMPORAL_CALLBACK_FAILURE`."] pub type nghttp2_on_invalid_header_callback2 = :: std :: option :: Option < unsafe extern "C" fn (session : * mut nghttp2_session , frame : * const nghttp2_frame , name : * mut nghttp2_rcbuf , value : * mut nghttp2_rcbuf , flags : u8 , user_data : * mut :: std :: os :: raw :: c_void ,) -> :: std :: os :: raw :: c_int , > ;
    };
}

nghttp2_on_invalid_header_callback2!();