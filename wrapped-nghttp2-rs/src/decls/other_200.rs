macro_rules! other_200 {
    () => {
        extern "C" { # [doc = " @function"] # [doc = ""] # [doc = " Returns the serialized data to send."] # [doc = ""] # [doc = " This function behaves like `nghttp2_session_send()` except that it"] # [doc = " does not use :type:`nghttp2_send_callback` to transmit data."] # [doc = " Instead, it assigns the pointer to the serialized data to the"] # [doc = " |*data_ptr| and returns its length.  The other callbacks are called"] # [doc = " in the same way as they are in `nghttp2_session_send()`."] # [doc = ""] # [doc = " If no data is available to send, this function returns 0."] # [doc = ""] # [doc = " This function may not return all serialized data in one invocation."] # [doc = " To get all data, call this function repeatedly until it returns 0"] # [doc = " or one of negative error codes."] # [doc = ""] # [doc = " The assigned |*data_ptr| is valid until the next call of"] # [doc = " `nghttp2_session_mem_send()` or `nghttp2_session_send()`."] # [doc = ""] # [doc = " The caller must send all data before sending the next chunk of"] # [doc = " data."] # [doc = ""] # [doc = " This function returns the length of the data pointed by the"] # [doc = " |*data_ptr| if it succeeds, or one of the following negative error"] # [doc = " codes:"] # [doc = ""] # [doc = " :enum:`NGHTTP2_ERR_NOMEM`"] # [doc = "     Out of memory."] # [doc = ""] # [doc = " .. note::"] # [doc = ""] # [doc = "   This function may produce very small byte string.  If that is the"] # [doc = "   case, and application disables Nagle algorithm (``TCP_NODELAY``),"] # [doc = "   then writing this small chunk leads to very small packet, and it"] # [doc = "   is very inefficient.  An application should be responsible to"] # [doc = "   buffer up small chunks of data as necessary to avoid this"] # [doc = "   situation."] pub fn nghttp2_session_mem_send (session : * mut nghttp2_session , data_ptr : * mut * const u8 ,) -> isize ; }
    };
}

other_200!();