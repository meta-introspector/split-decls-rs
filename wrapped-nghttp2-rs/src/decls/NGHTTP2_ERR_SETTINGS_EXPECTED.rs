macro_rules! NGHTTP2_ERR_SETTINGS_EXPECTED {
    () => {
        # [doc = " When a local endpoint expects to receive SETTINGS frame, it"] # [doc = " receives an other type of frame."] pub const NGHTTP2_ERR_SETTINGS_EXPECTED : nghttp2_error = - 536 ;
    };
}

NGHTTP2_ERR_SETTINGS_EXPECTED!()