macro_rules! nghttp2_on_data_chunk_recv_callback {
    () => {
        # [doc = " @functypedef"] # [doc = ""] # [doc = " Callback function invoked when a chunk of data in DATA frame is"] # [doc = " received.  The |stream_id| is the stream ID this DATA frame belongs"] # [doc = " to.  The |flags| is the flags of DATA frame which this data chunk"] # [doc = " is contained.  ``(flags & NGHTTP2_FLAG_END_STREAM) != 0`` does not"] # [doc = " necessarily mean this chunk of data is the last one in the stream."] # [doc = " You should use :type:`nghttp2_on_frame_recv_callback` to know all"] # [doc = " data frames are received.  The |user_data| pointer is the third"] # [doc = " argument passed in to the call to `nghttp2_session_client_new()` or"] # [doc = " `nghttp2_session_server_new()`."] # [doc = ""] # [doc = " If the application uses `nghttp2_session_mem_recv()`, it can return"] # [doc = " :enum:`NGHTTP2_ERR_PAUSE` to make `nghttp2_session_mem_recv()`"] # [doc = " return without processing further input bytes.  The memory by"] # [doc = " pointed by the |data| is retained until"] # [doc = " `nghttp2_session_mem_recv()` or `nghttp2_session_recv()` is called."] # [doc = " The application must retain the input bytes which was used to"] # [doc = " produce the |data| parameter, because it may refer to the memory"] # [doc = " region included in the input bytes."] # [doc = ""] # [doc = " The implementation of this function must return 0 if it succeeds."] # [doc = " If nonzero is returned, it is treated as fatal error, and"] # [doc = " `nghttp2_session_recv()` and `nghttp2_session_mem_recv()` functions"] # [doc = " immediately return :enum:`NGHTTP2_ERR_CALLBACK_FAILURE`."] # [doc = ""] # [doc = " To set this callback to :type:`nghttp2_session_callbacks`, use"] # [doc = " `nghttp2_session_callbacks_set_on_data_chunk_recv_callback()`."] pub type nghttp2_on_data_chunk_recv_callback = :: std :: option :: Option < unsafe extern "C" fn (session : * mut nghttp2_session , flags : u8 , stream_id : i32 , data : * const u8 , len : usize , user_data : * mut :: std :: os :: raw :: c_void ,) -> :: std :: os :: raw :: c_int , > ;
    };
}

nghttp2_on_data_chunk_recv_callback!()