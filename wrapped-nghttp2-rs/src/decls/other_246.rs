macro_rules! other_246 {
    () => {
        extern "C" { # [doc = " @function"] # [doc = ""] # [doc = " Submits one or more DATA frames to the stream |stream_id|.  The"] # [doc = " data to be sent are provided by |data_prd|.  If |flags| contains"] # [doc = " :enum:`NGHTTP2_FLAG_END_STREAM`, the last DATA frame has END_STREAM"] # [doc = " flag set."] # [doc = ""] # [doc = " This function does not take ownership of the |data_prd|.  The"] # [doc = " function copies the members of the |data_prd|."] # [doc = ""] # [doc = " This function returns 0 if it succeeds, or one of the following"] # [doc = " negative error codes:"] # [doc = ""] # [doc = " :enum:`NGHTTP2_ERR_NOMEM`"] # [doc = "     Out of memory."] # [doc = " :enum:`NGHTTP2_ERR_DATA_EXIST`"] # [doc = "     DATA or HEADERS has been already submitted and not fully"] # [doc = "     processed yet."] # [doc = " :enum:`NGHTTP2_ERR_INVALID_ARGUMENT`"] # [doc = "     The |stream_id| is 0."] # [doc = " :enum:`NGHTTP2_ERR_STREAM_CLOSED`"] # [doc = "     The stream was already closed; or the |stream_id| is invalid."] # [doc = ""] # [doc = " .. note::"] # [doc = ""] # [doc = "   Currently, only one DATA or HEADERS is allowed for a stream at a"] # [doc = "   time.  Submitting these frames more than once before first DATA"] # [doc = "   or HEADERS is finished results in :enum:`NGHTTP2_ERR_DATA_EXIST`"] # [doc = "   error code.  The earliest callback which tells that previous"] # [doc = "   frame is done is :type:`nghttp2_on_frame_send_callback`.  In side"] # [doc = "   that callback, new data can be submitted using"] # [doc = "   `nghttp2_submit_data()`.  Of course, all data except for last one"] # [doc = "   must not have :enum:`NGHTTP2_FLAG_END_STREAM` flag set in"] # [doc = "   |flags|.  This sounds a bit complicated, and we recommend to use"] # [doc = "   `nghttp2_submit_request()` and `nghttp2_submit_response()` to"] # [doc = "   avoid this cascading issue.  The experience shows that for HTTP"] # [doc = "   use, these two functions are enough to implement both client and"] # [doc = "   server."] pub fn nghttp2_submit_data (session : * mut nghttp2_session , flags : u8 , stream_id : i32 , data_prd : * const nghttp2_data_provider ,) -> :: std :: os :: raw :: c_int ; }
    };
}

other_246!()