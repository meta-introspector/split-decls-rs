macro_rules! other_256 {
    () => {
        extern "C" { # [doc = " @function"] # [doc = ""] # [doc = " Submits WINDOW_UPDATE frame."] # [doc = ""] # [doc = " The |flags| is currently ignored and should be"] # [doc = " :enum:`NGHTTP2_FLAG_NONE`."] # [doc = ""] # [doc = " The |stream_id| is the stream ID to send this WINDOW_UPDATE.  To"] # [doc = " send connection level WINDOW_UPDATE, specify 0 to |stream_id|."] # [doc = ""] # [doc = " If the |window_size_increment| is positive, the WINDOW_UPDATE with"] # [doc = " that value as window_size_increment is queued.  If the"] # [doc = " |window_size_increment| is larger than the received bytes from the"] # [doc = " remote endpoint, the local window size is increased by that"] # [doc = " difference.  If the sole purpose is to increase the local window"] # [doc = " size, consider to use `nghttp2_session_set_local_window_size()`."] # [doc = ""] # [doc = " If the |window_size_increment| is negative, the local window size"] # [doc = " is decreased by -|window_size_increment|.  If automatic"] # [doc = " WINDOW_UPDATE is enabled"] # [doc = " (`nghttp2_option_set_no_auto_window_update()`), and the library"] # [doc = " decided that the WINDOW_UPDATE should be submitted, then"] # [doc = " WINDOW_UPDATE is queued with the current received bytes count.  If"] # [doc = " the sole purpose is to decrease the local window size, consider to"] # [doc = " use `nghttp2_session_set_local_window_size()`."] # [doc = ""] # [doc = " If the |window_size_increment| is 0, the function does nothing and"] # [doc = " returns 0."] # [doc = ""] # [doc = " This function returns 0 if it succeeds, or one of the following"] # [doc = " negative error codes:"] # [doc = ""] # [doc = " :enum:`NGHTTP2_ERR_FLOW_CONTROL`"] # [doc = "     The local window size overflow or gets negative."] # [doc = " :enum:`NGHTTP2_ERR_NOMEM`"] # [doc = "     Out of memory."] pub fn nghttp2_submit_window_update (session : * mut nghttp2_session , flags : u8 , stream_id : i32 , window_size_increment : i32 ,) -> :: std :: os :: raw :: c_int ; }
    };
}

other_256!()