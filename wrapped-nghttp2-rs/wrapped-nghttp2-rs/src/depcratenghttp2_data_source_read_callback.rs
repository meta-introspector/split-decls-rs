// Generated macro for nghttp2_data_source_read_callback (type)
macro_rules! Depcratenghttp2_data_source_read_callback {
() => {
// Module: crate
// Provides: {"nghttp2_data_source_read_callback"}
// Dependencies: {}
# [doc = " @functypedef"] # [doc = ""] # [doc = " Callback function invoked when the library wants to read data from"] # [doc = " the |source|.  The read data is sent in the stream |stream_id|."] # [doc = " The implementation of this function must read at most |length|"] # [doc = " bytes of data from |source| (or possibly other places) and store"] # [doc = " them in |buf| and return number of data stored in |buf|.  If EOF is"] # [doc = " reached, set :enum:`NGHTTP2_DATA_FLAG_EOF` flag in |*data_flags|."] # [doc = ""] # [doc = " Sometime it is desirable to avoid copying data into |buf| and let"] # [doc = " application to send data directly.  To achieve this, set"] # [doc = " :enum:`NGHTTP2_DATA_FLAG_NO_COPY` to |*data_flags| (and possibly"] # [doc = " other flags, just like when we do copy), and return the number of"] # [doc = " bytes to send without copying data into |buf|.  The library, seeing"] # [doc = " :enum:`NGHTTP2_DATA_FLAG_NO_COPY`, will invoke"] # [doc = " :type:`nghttp2_send_data_callback`.  The application must send"] # [doc = " complete DATA frame in that callback."] # [doc = ""] # [doc = " If this callback is set by `nghttp2_submit_request()`,"] # [doc = " `nghttp2_submit_response()` or `nghttp2_submit_headers()` and"] # [doc = " `nghttp2_submit_data()` with flag parameter"] # [doc = " :enum:`NGHTTP2_FLAG_END_STREAM` set, and"] # [doc = " :enum:`NGHTTP2_DATA_FLAG_EOF` flag is set to |*data_flags|, DATA"] # [doc = " frame will have END_STREAM flag set.  Usually, this is expected"] # [doc = " behaviour and all are fine.  One exception is send trailer fields."] # [doc = " You cannot send trailer fields after sending frame with END_STREAM"] # [doc = " set.  To avoid this problem, one can set"] # [doc = " :enum:`NGHTTP2_DATA_FLAG_NO_END_STREAM` along with"] # [doc = " :enum:`NGHTTP2_DATA_FLAG_EOF` to signal the library not to set"] # [doc = " END_STREAM in DATA frame.  Then application can use"] # [doc = " `nghttp2_submit_trailer()` to send trailer fields."] # [doc = " `nghttp2_submit_trailer()` can be called inside this callback."] # [doc = ""] # [doc = " If the application wants to postpone DATA frames (e.g.,"] # [doc = " asynchronous I/O, or reading data blocks for long time), it is"] # [doc = " achieved by returning :enum:`NGHTTP2_ERR_DEFERRED` without reading"] # [doc = " any data in this invocation.  The library removes DATA frame from"] # [doc = " the outgoing queue temporarily.  To move back deferred DATA frame"] # [doc = " to outgoing queue, call `nghttp2_session_resume_data()`."] # [doc = ""] # [doc = " By default, |length| is limited to 16KiB at maximum.  If peer"] # [doc = " allows larger frames, application can enlarge transmission buffer"] # [doc = " size.  See :type:`nghttp2_data_source_read_length_callback` for"] # [doc = " more details."] # [doc = ""] # [doc = " If the application just wants to return from"] # [doc = " `nghttp2_session_send()` or `nghttp2_session_mem_send()` without"] # [doc = " sending anything, return :enum:`NGHTTP2_ERR_PAUSE`."] # [doc = ""] # [doc = " In case of error, there are 2 choices. Returning"] # [doc = " :enum:`NGHTTP2_ERR_TEMPORAL_CALLBACK_FAILURE` will close the stream"] # [doc = " by issuing RST_STREAM with :enum:`NGHTTP2_INTERNAL_ERROR`.  If a"] # [doc = " different error code is desirable, use"] # [doc = " `nghttp2_submit_rst_stream()` with a desired error code and then"] # [doc = " return :enum:`NGHTTP2_ERR_TEMPORAL_CALLBACK_FAILURE`.  Returning"] # [doc = " :enum:`NGHTTP2_ERR_CALLBACK_FAILURE` will signal the entire session"] # [doc = " failure."] pub type nghttp2_data_source_read_callback = :: std :: option :: Option < unsafe extern "C" fn (session : * mut nghttp2_session , stream_id : i32 , buf : * mut u8 , length : usize , data_flags : * mut u32 , source : * mut nghttp2_data_source , user_data : * mut :: std :: os :: raw :: c_void ,) -> isize , > ;
};
}
