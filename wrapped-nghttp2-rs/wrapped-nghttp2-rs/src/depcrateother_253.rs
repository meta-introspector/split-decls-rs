// Generated macro for other_253 (other)
macro_rules! Depcrateother_253 {
() => {
// Module: crate
// Provides: {"other_253"}
// Dependencies: {}
extern "C" { # [doc = " @function"] # [doc = ""] # [doc = " Submits GOAWAY frame with the last stream ID |last_stream_id| and"] # [doc = " the error code |error_code|."] # [doc = ""] # [doc = " The pre-defined error code is one of :enum:`nghttp2_error_code`."] # [doc = ""] # [doc = " The |flags| is currently ignored and should be"] # [doc = " :enum:`NGHTTP2_FLAG_NONE`."] # [doc = ""] # [doc = " The |last_stream_id| is peer's stream ID or 0.  So if |session| is"] # [doc = " initialized as client, |last_stream_id| must be even or 0.  If"] # [doc = " |session| is initialized as server, |last_stream_id| must be odd or"] # [doc = " 0."] # [doc = ""] # [doc = " The HTTP/2 specification says last_stream_id must not be increased"] # [doc = " from the value previously sent.  So the actual value sent as"] # [doc = " last_stream_id is the minimum value between the given"] # [doc = " |last_stream_id| and the last_stream_id previously sent to the"] # [doc = " peer."] # [doc = ""] # [doc = " If the |opaque_data| is not ``NULL`` and |opaque_data_len| is not"] # [doc = " zero, those data will be sent as additional debug data.  The"] # [doc = " library makes a copy of the memory region pointed by |opaque_data|"] # [doc = " with the length |opaque_data_len|, so the caller does not need to"] # [doc = " keep this memory after the return of this function.  If the"] # [doc = " |opaque_data_len| is 0, the |opaque_data| could be ``NULL``."] # [doc = ""] # [doc = " After successful transmission of GOAWAY, following things happen."] # [doc = " All incoming streams having strictly more than |last_stream_id| are"] # [doc = " closed.  All incoming HEADERS which starts new stream are simply"] # [doc = " ignored.  After all active streams are handled, both"] # [doc = " `nghttp2_session_want_read()` and `nghttp2_session_want_write()`"] # [doc = " return 0 and the application can close session."] # [doc = ""] # [doc = " This function returns 0 if it succeeds, or one of the following"] # [doc = " negative error codes:"] # [doc = ""] # [doc = " :enum:`NGHTTP2_ERR_NOMEM`"] # [doc = "     Out of memory."] # [doc = " :enum:`NGHTTP2_ERR_INVALID_ARGUMENT`"] # [doc = "     The |opaque_data_len| is too large; the |last_stream_id| is"] # [doc = "     invalid."] pub fn nghttp2_submit_goaway (session : * mut nghttp2_session , flags : u8 , last_stream_id : i32 , error_code : u32 , opaque_data : * const u8 , opaque_data_len : usize ,) -> :: std :: os :: raw :: c_int ; }
};
}
