// Generated macro for PhfBorrow (trait)
macro_rules! DepcratePhfBorrow {
() => {
// Module: crate
// Provides: {"PhfBorrow"}
// Dependencies: {}
# [doc = " Identical to `std::borrow::Borrow` except omitting blanket impls to facilitate other"] # [doc = " borrowing patterns."] # [doc = ""] # [doc = " The same semantic requirements apply:"] # [doc = ""] # [doc = " > In particular `Eq`, `Ord` and `Hash` must be equivalent for borrowed and owned values:"] # [doc = " > `x.borrow() == y.borrow()` should give the same result as `x == y`."] # [doc = ""] # [doc = " (This crate's API only requires `Eq` and `PhfHash`, however.)"] # [doc = ""] # [doc = " ### Motivation"] # [doc = " The conventional signature for lookup methods on collections looks something like this:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " impl<K, V> Map<K, V> where K: PhfHash + Eq {"] # [doc = "     fn get<T: ?Sized>(&self, key: &T) -> Option<&V> where T: PhfHash + Eq, K: Borrow<T> {"] # [doc = "         ..."] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " This allows the key type used for lookup to be different than the key stored in the map so for"] # [doc = " example you can use `&str` to look up a value in a `Map<String, _>`. However, this runs into"] # [doc = " a problem in the case where `T` and `K` are both a `Foo<_>` type constructor but"] # [doc = " the contained type is different (even being the same type with different lifetimes)."] # [doc = ""] # [doc = " The main issue for this crate's API is that, with this method signature, you cannot perform a"] # [doc = " lookup on a `Map<UniCase<&'static str>, _>` with a `UniCase<&'a str>` where `'a` is not"] # [doc = " `'static`; there is no impl of `Borrow` that resolves to"] # [doc = " `impl Borrow<UniCase<'a>> for UniCase<&'static str>` and one cannot be added either because of"] # [doc = " all the blanket impls."] # [doc = ""] # [doc = " Instead, this trait is implemented conservatively, without blanket impls, so that impls like"] # [doc = " this may be added. This is feasible since the set of types that implement `PhfHash` is"] # [doc = " intentionally small."] # [doc = ""] # [doc = " This likely won't be fixable with specialization alone but will require full support for lattice"] # [doc = " impls since we technically want to add overlapping blanket impls."] pub trait PhfBorrow < B : ? Sized > { # [doc = " Convert a reference to `self` to a reference to the borrowed type."] fn borrow (& self) -> & B ; }
};
}
