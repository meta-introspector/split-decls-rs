// Generated macro for impl_1247 (impl)
macro_rules! Depcrate_shapeimpl_1247 {
() => {
// Module: crate::shape
// Provides: {"impl_1247"}
// Dependencies: {}
impl Shape { # [doc = " `indent` is the indentation of the first line. The next lines"] # [doc = " should begin with at least `indent` spaces (except backwards"] # [doc = " indentation). The first line should not begin with indentation."] # [doc = " `width` is the maximum number of characters on the last line"] # [doc = " (excluding `indent`). The width of other lines is not limited by"] # [doc = " `width`."] # [doc = " Note that in reality, we sometimes use width for lines other than the"] # [doc = " last (i.e., we are conservative)."] pub (crate) fn legacy (width : usize , indent : Indent) -> Shape { Shape { width , indent , offset : indent . alignment , } } pub (crate) fn indented (indent : Indent , config : & Config) -> Shape { Shape { width : config . max_width () . saturating_sub (indent . width ()) , indent , offset : indent . alignment , } } pub (crate) fn with_max_width (& self , config : & Config) -> Shape { Shape { width : config . max_width () . saturating_sub (self . indent . width ()) , .. * self } } pub (crate) fn visual_indent (& self , extra_width : usize) -> Shape { let alignment = self . offset + extra_width ; Shape { width : self . width , indent : Indent :: new (self . indent . block_indent , alignment) , offset : alignment , } } pub (crate) fn block_indent (& self , extra_width : usize) -> Shape { if self . indent . alignment == 0 { Shape { width : self . width , indent : Indent :: new (self . indent . block_indent + extra_width , 0) , offset : 0 , } } else { Shape { width : self . width , indent : self . indent + extra_width , offset : self . indent . alignment + extra_width , } } } pub (crate) fn block_left (& self , width : usize) -> Option < Shape > { self . block_indent (width) . sub_width (width) } pub (crate) fn add_offset (& self , extra_width : usize) -> Shape { Shape { offset : self . offset + extra_width , .. * self } } pub (crate) fn block (& self) -> Shape { Shape { indent : self . indent . block_only () , .. * self } } pub (crate) fn saturating_sub_width (& self , width : usize) -> Shape { self . sub_width (width) . unwrap_or (Shape { width : 0 , .. * self }) } pub (crate) fn sub_width (& self , width : usize) -> Option < Shape > { Some (Shape { width : self . width . checked_sub (width) ? , .. * self }) } pub (crate) fn shrink_left (& self , width : usize) -> Option < Shape > { Some (Shape { width : self . width . checked_sub (width) ? , indent : self . indent + width , offset : self . offset + width , }) } pub (crate) fn offset_left (& self , width : usize) -> Option < Shape > { self . add_offset (width) . sub_width (width) } pub (crate) fn used_width (& self) -> usize { self . indent . block_indent + self . offset } pub (crate) fn rhs_overhead (& self , config : & Config) -> usize { config . max_width () . saturating_sub (self . used_width () + self . width) } pub (crate) fn comment (& self , config : & Config) -> Shape { let width = min (self . width , config . comment_width () . saturating_sub (self . indent . width ()) ,) ; Shape { width , .. * self } } pub (crate) fn to_string_with_newline (& self , config : & Config) -> Cow < 'static , str > { let mut offset_indent = self . indent ; offset_indent . alignment = self . offset ; offset_indent . to_string_inner (config , 0) } # [doc = " Creates a `Shape` with a virtually infinite width."] pub (crate) fn infinite_width (& self) -> Shape { Shape { width : INFINITE_SHAPE_WIDTH , .. * self } } }
};
}
