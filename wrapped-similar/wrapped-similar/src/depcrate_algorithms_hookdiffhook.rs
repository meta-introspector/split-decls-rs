// Generated macro for DiffHook (trait)
macro_rules! Depcrate_algorithms_hookDiffHook {
() => {
// Module: crate::algorithms::hook
// Provides: {"DiffHook"}
// Dependencies: {}
# [doc = " A trait for reacting to an edit script from the \"old\" version to"] # [doc = " the \"new\" version."] pub trait DiffHook : Sized { # [doc = " The error produced from the hook methods."] type Error ; # [doc = " Called when lines with indices `old_index` (in the old version) and"] # [doc = " `new_index` (in the new version) start an section equal in both"] # [doc = " versions, of length `len`."] fn equal (& mut self , old_index : usize , new_index : usize , len : usize) -> Result < () , Self :: Error > { let _ = old_index ; let _ = new_index ; let _ = len ; Ok (()) } # [doc = " Called when a section of length `old_len`, starting at `old_index`,"] # [doc = " needs to be deleted from the old version."] fn delete (& mut self , old_index : usize , old_len : usize , new_index : usize ,) -> Result < () , Self :: Error > { let _ = old_index ; let _ = old_len ; let _ = new_index ; Ok (()) } # [doc = " Called when a section of the new version, of length `new_len`"] # [doc = " and starting at `new_index`, needs to be inserted at position `old_index'."] fn insert (& mut self , old_index : usize , new_index : usize , new_len : usize ,) -> Result < () , Self :: Error > { let _ = old_index ; let _ = new_index ; let _ = new_len ; Ok (()) } # [doc = " Called when a section of the old version, starting at index"] # [doc = " `old_index` and of length `old_len`, needs to be replaced with a"] # [doc = " section of length `new_len`, starting at `new_index`, of the new"] # [doc = " version."] # [doc = ""] # [doc = " The default implementations invokes `delete` and `insert`."] # [doc = ""] # [doc = " You can use the [`Replace`](crate::algorithms::Replace) hook to"] # [doc = " automatically generate these."] # [inline (always)] fn replace (& mut self , old_index : usize , old_len : usize , new_index : usize , new_len : usize ,) -> Result < () , Self :: Error > { self . delete (old_index , old_len , new_index) ? ; self . insert (old_index , new_index , new_len) } # [doc = " Always called at the end of the algorithm."] # [inline (always)] fn finish (& mut self) -> Result < () , Self :: Error > { Ok (()) } }
};
}
