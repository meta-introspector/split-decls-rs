// Generated macro for impl_34 (impl)
macro_rules! Depcrate_graphql_transport_wsimpl_34 {
() => {
// Module: crate::graphql_transport_ws
// Provides: {"impl_34"}
// Dependencies: {}
impl < S , I > Connection < S , I > where S : Schema , I : Init < S :: ScalarValue , S :: Context > , { # [doc = " Creates a new connection, which is a sink for `TryInto<Input>` messages and a stream of"] # [doc = " `Output` messages."] # [doc = ""] # [doc = " The `schema` argument should typically be an `Arc<RootNode<...>>`."] # [doc = ""] # [doc = " The `init` argument is used to provide the context and additional configuration for"] # [doc = " connections. This can be a `ConnectionConfig` if the context and configuration are already"] # [doc = " known, or it can be a closure that gets executed asynchronously when the client sends the"] # [doc = " ConnectionInit message. Using a closure allows you to perform authentication based on the"] # [doc = " parameters provided by the client."] pub fn new (schema : S , init : I) -> Self { Self { reactions : SelectAll :: new () , stream_waker : None , stream_terminated : false , sink_state : ConnectionSinkState :: Ready { state : ConnectionState :: PreInit { init , schema } , } , } } # [doc = " Performs polling of the [`Sink`] part of this [`Connection`]."] # [doc = ""] # [doc = " Effectively represents an implementation of [`Sink::poll_ready()`] and"] # [doc = " [`Sink::poll_flush()`] methods."] fn poll_sink (mut self : Pin < & mut Self > , cx : & mut Context) -> Poll < Result < () , & 'static str > > { match & mut self . sink_state { ConnectionSinkState :: Ready { .. } => Poll :: Ready (Ok (())) , ConnectionSinkState :: HandlingMessage { result } => match Pin :: new (result) . poll (cx) { Poll :: Ready ((state , reactions)) => { self . reactions . push (reactions) ; self . sink_state = ConnectionSinkState :: Ready { state } ; if let Some (waker) = self . stream_waker . take () { waker . wake () ; } Poll :: Ready (Ok (())) } Poll :: Pending => Poll :: Pending , } , ConnectionSinkState :: Closed => Poll :: Ready (Err ("polled after close")) , } } }
};
}
