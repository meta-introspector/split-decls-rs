// Generated macro for extract_annotations (function)
macro_rules! Depcrateextract_annotations {
() => {
// Module: crate
// Provides: {"extract_annotations"}
// Dependencies: {}
# [doc = " Extracts `//^^^ some text` annotations."] # [doc = ""] # [doc = " A run of `^^^` can be arbitrary long and points to the corresponding range"] # [doc = " in the line above."] # [doc = ""] # [doc = " The `// ^file text` syntax can be used to attach `text` to the entirety of"] # [doc = " the file."] # [doc = ""] # [doc = " Multiline string values are supported:"] # [doc = ""] # [doc = " // ^^^ first line"] # [doc = " //   | second line"] # [doc = ""] # [doc = " Trailing whitespace is sometimes desired but usually stripped by the editor"] # [doc = " if at the end of a line, or incorrectly sized if followed by another"] # [doc = " annotation. In those cases the annotation can be explicitly ended with the"] # [doc = " `$` character."] # [doc = ""] # [doc = " // ^^^ trailing-ws-wanted  $"] # [doc = ""] # [doc = " Annotations point to the last line that actually was long enough for the"] # [doc = " range, not counting annotations themselves. So overlapping annotations are"] # [doc = " possible:"] # [doc = " ```text"] # [doc = " // stuff        other stuff"] # [doc = " // ^^ 'st'"] # [doc = " // ^^^^^ 'stuff'"] # [doc = " //              ^^^^^^^^^^^ 'other stuff'"] # [doc = " ```"] pub fn extract_annotations (text : & str) -> Vec < (TextRange , String) > { let mut res = Vec :: new () ; let mut line_start_map = BTreeMap :: new () ; let mut line_start : TextSize = 0 . into () ; let mut prev_line_annotations : Vec < (TextSize , usize) > = Vec :: new () ; for line in text . split_inclusive ('\n') { let mut this_line_annotations = Vec :: new () ; let line_length = if let Some ((prefix , suffix)) = line . split_once ("//") { let ss_len = TextSize :: of ("//") ; let annotation_offset = TextSize :: of (prefix) + ss_len ; for annotation in extract_line_annotations (suffix . trim_end_matches ('\n')) { match annotation { LineAnnotation :: Annotation { mut range , content , file } => { range += annotation_offset ; this_line_annotations . push ((range . end () , res . len ())) ; let range = if file { TextRange :: up_to (TextSize :: of (text)) } else { let line_start = line_start_map . range (range . end () ..) . next () . unwrap () ; range + line_start . 1 } ; res . push ((range , content)) ; } LineAnnotation :: Continuation { mut offset , content } => { offset += annotation_offset ; let & (_ , idx) = prev_line_annotations . iter () . find (| & & (off , _idx) | off == offset) . unwrap () ; res [idx] . 1 . push ('\n') ; res [idx] . 1 . push_str (& content) ; res [idx] . 1 . push ('\n') ; } } } annotation_offset } else { TextSize :: of (line) } ; line_start_map = line_start_map . split_off (& line_length) ; line_start_map . insert (line_length , line_start) ; line_start += TextSize :: of (line) ; prev_line_annotations = this_line_annotations ; } res }
};
}
