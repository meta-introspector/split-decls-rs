// Generated macro for impl_434 (impl)
macro_rules! Depcrate_uintimpl_434 {
() => {
// Module: crate::uint
// Provides: {"impl_434"}
// Dependencies: {}
impl < const LIMBS : usize > Uint < LIMBS > { # [doc = " The value `0`."] pub const ZERO : Self = Self :: from_u8 (0) ; # [doc = " The value `1`."] pub const ONE : Self = Self :: from_u8 (1) ; # [doc = " Maximum value this [`Uint`] can express."] pub const MAX : Self = Self { limbs : [Limb :: MAX ; LIMBS] , } ; # [doc = " Total size of the represented integer in bits."] pub const BITS : u32 = LIMBS as u32 * Limb :: BITS ; # [doc = " `floor(log2(Self::BITS))`."] pub (crate) const LOG2_BITS : u32 = u32 :: BITS - Self :: BITS . leading_zeros () - 1 ; # [doc = " Total size of the represented integer in bytes."] pub const BYTES : usize = LIMBS * Limb :: BYTES ; # [doc = " The number of limbs used on this platform."] pub const LIMBS : usize = LIMBS ; # [doc = " Const-friendly [`Uint`] constructor."] pub const fn new (limbs : [Limb ; LIMBS]) -> Self { Self { limbs } } # [doc = " Create a [`Uint`] from an array of [`Word`]s (i.e. word-sized unsigned"] # [doc = " integers)."] # [inline] pub const fn from_words (arr : [Word ; LIMBS]) -> Self { let mut limbs = [Limb :: ZERO ; LIMBS] ; let mut i = 0 ; while i < LIMBS { limbs [i] = Limb (arr [i]) ; i += 1 ; } Self { limbs } } # [doc = " Create an array of [`Word`]s (i.e. word-sized unsigned integers) from"] # [doc = " a [`Uint`]."] # [inline] pub const fn to_words (self) -> [Word ; LIMBS] { let mut arr = [0 ; LIMBS] ; let mut i = 0 ; while i < LIMBS { arr [i] = self . limbs [i] . 0 ; i += 1 ; } arr } # [doc = " Borrow the inner limbs as an array of [`Word`]s."] pub const fn as_words (& self) -> & [Word ; LIMBS] { # [allow (unsafe_code)] unsafe { & * self . limbs . as_ptr () . cast () } } # [doc = " Borrow the inner limbs as a mutable array of [`Word`]s."] pub const fn as_mut_words (& mut self) -> & mut [Word ; LIMBS] { # [allow (unsafe_code)] unsafe { & mut * self . limbs . as_mut_ptr () . cast () } } # [doc = " Borrow the inner limbs as a mutable slice of [`Word`]s."] # [deprecated (since = "0.7.0" , note = "please use `as_mut_words` instead")] pub const fn as_words_mut (& mut self) -> & mut [Word] { self . as_mut_words () } # [doc = " Borrow the limbs of this [`Uint`]."] pub const fn as_limbs (& self) -> & [Limb ; LIMBS] { & self . limbs } # [doc = " Borrow the limbs of this [`Uint`] mutably."] pub const fn as_mut_limbs (& mut self) -> & mut [Limb ; LIMBS] { & mut self . limbs } # [doc = " Borrow the limbs of this [`Uint`] mutably."] # [deprecated (since = "0.7.0" , note = "please use `as_mut_limbs` instead")] pub const fn as_limbs_mut (& mut self) -> & mut [Limb] { self . as_mut_limbs () } # [doc = " Convert this [`Uint`] into its inner limbs."] pub const fn to_limbs (self) -> [Limb ; LIMBS] { self . limbs } # [doc = " Borrow the limbs of this [`Uint`] as a [`UintRef`]."] # [inline (always)] pub (crate) const fn as_uint_ref (& self) -> & UintRef { UintRef :: new (& self . limbs) } # [doc = " Mutably borrow the limbs of this [`Uint`] as a [`UintRef`]."] # [inline (always)] pub (crate) const fn as_mut_uint_ref (& mut self) -> & mut UintRef { UintRef :: new_mut (& mut self . limbs) } # [doc = " Convert to a [`NonZero<Uint<LIMBS>>`]."] # [doc = ""] # [doc = " Returns some if the original value is non-zero, and false otherwise."] pub const fn to_nz (self) -> ConstCtOption < NonZero < Self > > { ConstCtOption :: new (NonZero (self) , self . is_nonzero ()) } # [doc = " Convert to a [`Odd<Uint<LIMBS>>`]."] # [doc = ""] # [doc = " Returns some if the original value is odd, and false otherwise."] pub const fn to_odd (self) -> ConstCtOption < Odd < Self > > { ConstCtOption :: new (Odd (self) , self . is_odd ()) } # [doc = " Interpret this object as an [`Int`] instead."] # [doc = ""] # [doc = " Note: this is a casting operation. See [`Self::try_into_int`] for the checked equivalent."] pub const fn as_int (& self) -> & Int < LIMBS > { # [allow (trivial_casts , unsafe_code)] unsafe { & * (self as * const Uint < LIMBS > as * const Int < LIMBS >) } } # [doc = " Convert this type into an [`Int`]; returns `None` if this value is greater than [`Int::MAX`]."] # [doc = ""] # [doc = " Note: this is the conversion operation. See [`Self::as_int`] for the unchecked equivalent."] pub const fn try_into_int (self) -> ConstCtOption < Int < LIMBS > > { Int :: new_from_abs_sign (self , ConstChoice :: FALSE) } }
};
}
