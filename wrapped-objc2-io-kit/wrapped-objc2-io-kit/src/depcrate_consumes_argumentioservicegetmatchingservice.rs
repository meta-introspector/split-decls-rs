// Generated macro for IOServiceGetMatchingService (function)
macro_rules! Depcrate_consumes_argumentIOServiceGetMatchingService {
() => {
// Module: crate::consumes_argument
// Provides: {"IOServiceGetMatchingService"}
// Dependencies: {}
# [doc = " Look up a registered IOService object that matches a matching dictionary."] # [doc = ""] # [doc = " This is the preferred method of finding IOService objects currently registered by IOKit (that is, objects that have had their registerService() methods invoked). To find IOService objects that aren't yet registered, use an iterator as created by IORegistryEntryCreateIterator(). IOServiceAddMatchingNotification can also supply this information and install a notification of new IOServices. The matching information used in the matching dictionary may vary depending on the class of service being looked up."] # [doc = ""] # [doc = " Parameter `mainPort`: The main port obtained from IOMainPort(). Pass kIOMainPortDefault to look up the default main port."] # [doc = ""] # [doc = " Parameter `matching`: A CF dictionary containing matching information, of which one reference is always consumed by this function (Note prior to the Tiger release there was a small chance that the dictionary might not be released if there was an error attempting to serialize the dictionary). IOKitLib can construct matching dictionaries for common criteria with helper functions such as IOServiceMatching, IOServiceNameMatching, IOBSDNameMatching."] # [doc = ""] # [doc = " Returns: The first service matched is returned on success. The service must be released by the caller."] pub unsafe extern "C-unwind" fn IOServiceGetMatchingService (main_port : libc :: mach_port_t , matching : Option < CFRetained < CFDictionary > > ,) -> io_service_t { extern "C-unwind" { fn IOServiceGetMatchingService (main_port : libc :: mach_port_t , matching : * mut CFDictionary ,) -> io_service_t ; } unsafe { IOServiceGetMatchingService (main_port , consume (matching)) } }
};
}
