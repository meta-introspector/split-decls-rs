// Generated macro for IOServiceAddMatchingNotification (function)
macro_rules! Depcrate_consumes_argumentIOServiceAddMatchingNotification {
() => {
// Module: crate::consumes_argument
// Provides: {"IOServiceAddMatchingNotification"}
// Dependencies: {}
# [doc = " Look up registered IOService objects that match a matching dictionary, and install a notification request of new IOServices that match."] # [doc = ""] # [doc = " This is the preferred method of finding IOService objects that may arrive at any time. The type of notification specifies the state change the caller is interested in, on IOService's that match the match dictionary. Notification types are identified by name, and are defined in IOKitKeys.h. The matching information used in the matching dictionary may vary depending on the class of service being looked up."] # [doc = ""] # [doc = " Parameter `notifyPort`: A IONotificationPortRef object that controls how messages will be sent when the armed notification is fired. When the notification is delivered, the io_iterator_t representing the notification should be iterated through to pick up all outstanding objects. When the iteration is finished the notification is rearmed. See IONotificationPortCreate."] # [doc = ""] # [doc = " Parameter `notificationType`: A notification type from IOKitKeys.h"] # [doc = " <br>"] # [doc = " kIOPublishNotification Delivered when an IOService is registered."] # [doc = " <br>"] # [doc = " kIOFirstPublishNotification Delivered when an IOService is registered, but only once per IOService instance. Some IOService's may be reregistered when their state is changed."] # [doc = " <br>"] # [doc = " kIOMatchedNotification Delivered when an IOService has had all matching drivers in the kernel probed and started."] # [doc = " <br>"] # [doc = " kIOFirstMatchNotification Delivered when an IOService has had all matching drivers in the kernel probed and started, but only once per IOService instance. Some IOService's may be reregistered when their state is changed."] # [doc = " <br>"] # [doc = " kIOTerminatedNotification Delivered after an IOService has been terminated."] # [doc = ""] # [doc = " Parameter `matching`: A CF dictionary containing matching information, of which one reference is always consumed by this function (Note prior to the Tiger release there was a small chance that the dictionary might not be released if there was an error attempting to serialize the dictionary). IOKitLib can construct matching dictionaries for common criteria with helper functions such as IOServiceMatching, IOServiceNameMatching, IOBSDNameMatching."] # [doc = ""] # [doc = " Parameter `callback`: A callback function called when the notification fires."] # [doc = ""] # [doc = " Parameter `refCon`: A reference constant for the callbacks use."] # [doc = ""] # [doc = " Parameter `notification`: An iterator handle is returned on success, and should be released by the caller when the notification is to be destroyed. The notification is armed when the iterator is emptied by calls to IOIteratorNext - when no more objects are returned, the notification is armed. Note the notification is not armed when first created."] # [doc = ""] # [doc = " Returns: A kern_return_t error code."] pub unsafe extern "C-unwind" fn IOServiceAddMatchingNotification (notify_port : IONotificationPortRef , notification_type : * mut io_name_t , matching : Option < CFRetained < CFDictionary > > , callback : IOServiceMatchingCallback , ref_con : * mut c_void , notification : * mut io_iterator_t ,) -> libc :: kern_return_t { extern "C-unwind" { fn IOServiceAddMatchingNotification (notify_port : IONotificationPortRef , notification_type : * mut io_name_t , matching : * mut CFDictionary , callback : IOServiceMatchingCallback , ref_con : * mut c_void , notification : * mut io_iterator_t ,) -> libc :: kern_return_t ; } unsafe { IOServiceAddMatchingNotification (notify_port , notification_type , consume (matching) , callback , ref_con , notification ,) } }
};
}
