mkuse!{use std :: sync :: Mutex ;}
mkuse!{use std :: collections :: HashMap ;}
mkuse!{use std :: sync :: LazyLock ;}
mkitem!{static USE_MATRIX : LazyLock < Mutex < HashMap < String , Vec < String > > > > = LazyLock :: new (| | Mutex :: new (HashMap :: new ())) ;}

macro_rules! get_use_matrix_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function get_use_matrix in module {}", module_path!());
    };
}

mkfn!{
    get_use_matrix_introspect!();
    pub fn get_use_matrix () -> HashMap < String , Vec < String > > { USE_MATRIX . lock () . unwrap () . clone () }
}
mkitem!{macro_rules ! emit_message { ($ ($ arg : tt) *) => { } ; }}
mkitem!{macro_rules ! mkfn { ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_trait_bounds_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_trait_bounds_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: trait_bounds_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: trait_bounds_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn catch_fatal_errors < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn catch_fatal_errors < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: catch_fatal_errors_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: catch_fatal_errors_specific") ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn parse_crate_attrs < $ lifetime : lifetime > ($ ($ param : tt) *) -> PResult < $ lifetime2 : lifetime , ast :: AttrVec > $ body : block) => { $ (# [$ attr]) * fn parse_crate_attrs < $ lifetime > ($ ($ param) *) -> PResult < $ lifetime2 , ast :: AttrVec > { $ introspect ; emit_message ! ("🚀 MARKER: parse_crate_attrs_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: parse_crate_attrs_specific") ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn init_logger_with_additional_layer < F , T > ($ ($ param : tt) *) where F : FnOnce () -> T , T : rustc_log :: BuildSubscriberRet , $ body : block) => { $ (# [$ attr]) * fn init_logger_with_additional_layer < F , T > ($ ($ param) *) where F : FnOnce () -> T , T : rustc_log :: BuildSubscriberRet , { $ introspect ; emit_message ! ("🚀 MARKER: init_logger_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: init_logger_specific") ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < F , T > ($ ($ param : tt) *) $ (-> $ ret : ty) ? where F : FnOnce ($ ($ fnonce_args : tt) *) $ ($ where_rest : tt) * $ body : block) => { $ (# [$ attr]) * fn $ name < F , T > ($ ($ param) *) $ (-> $ ret) ? where F : FnOnce ($ ($ fnonce_args) *) $ ($ where_rest) * { $ introspect ; emit_message ! ("🚀 MARKER: two_generics_where_fnonce - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics_where_fnonce - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? where $ ($ where_clause : tt) * $ body : block) => { $ (# [$ attr]) * fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? where $ ($ where_clause) * { $ introspect ; emit_message ! ("🚀 MARKER: two_generics_where - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics_where - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: two_generics - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_two_generics - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_two_generics - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub ($ vis : ident) fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub ($ vis) fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_vis - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_vis - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < $ gen : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name < $ gen > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: generic_single - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: generic_single - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident < $ gen : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name < $ gen > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_generic_single - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_generic_single - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < $ lifetime : lifetime > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name < $ lifetime > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: lifetime - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: lifetime - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident < $ lifetime : lifetime > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name < $ lifetime > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_lifetime - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_lifetime - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_non_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_non_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: non_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: non_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; fn $ name : ident () $ body : block) => { fn $ name () { $ introspect ; emit_message ! ("🚀 MARKER: simple - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: simple - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ ($ anything : tt) *) => { $ ($ anything) * } ; }}
mkitem!{macro_rules ! safe_println { ($ ($ arg : tt) *) => { () } ; }}
mkitem!{macro_rules ! safe_print { ($ ($ arg : tt) *) => { () } ; }}
mkitem!{# [macro_export] macro_rules ! include_rust_compiler { ($ crate_name : literal , $ subpath : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_" , $ subpath , "_" , $ file , ".rs")) ; } ; ($ crate_name : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_src_" , $ file , ".rs")) ; } ; ($ crate_name : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_src_lib.rs")) ; } ; }}
mkitem!{# [macro_export] macro_rules ! include_rust_library { ($ lib_name : literal , $ subpath : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_" , $ subpath , "_" , $ file , ".rs")) ; } ; ($ lib_name : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_src_" , $ file , ".rs")) ; } ; ($ lib_name : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_src_lib.rs")) ; } ; }}
mkitem!{# [macro_export] macro_rules ! include_processed { ($ path : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_" , $ path , ".rs")) ; } ; }}
mkitem!{macro_rules ! mkinclude { ($ path : ident) => { } ; ($ path : literal) => { include ! ($ path) } ; }}
mkitem!{macro_rules ! mkitem { (include ! ($ path : ident) ;) => { } ; ($ macro_name : ident :: $ macro_sub : ident ! { $ string_lit : literal }) => { $ macro_name :: $ macro_sub ! { $ string_lit } } ; ($ macro_name : ident :: $ macro_sub : ident ! { $ ($ args : tt) * }) => { $ macro_name :: $ macro_sub ! { $ ($ args) * } } ; ($ macro_name : ident ! { $ ($ args : tt) * }) => { $ macro_name ! { $ ($ args) * } } ; ($ item : item) => { $ item } ; }}
mkitem!{# [macro_export] macro_rules ! mkmod { ($ name : ident , { $ ($ content : tt) * }) => { compile_error ! (concat ! ("MOD|" , module_path ! () , "|" , stringify ! ($ name))) ; mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; (pub mod $ name : ident { $ ($ content : tt) * }) => { compile_error ! (concat ! ("MOD|" , module_path ! () , "|" , stringify ! ($ name))) ; pub mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; (mod $ name : ident { $ ($ content : tt) * }) => { compile_error ! (concat ! ("MOD|" , module_path ! () , "|" , stringify ! ($ name))) ; mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; }}
mkitem!{# [macro_export] macro_rules ! mkuse { ($ use_stmt : item) => { compile_error ! (concat ! ("USE|" , module_path ! () , "|" , stringify ! ($ use_stmt))) ; } ; }}
mkitem!{macro_rules ! mkstruct { ($ struct_def : item) => { $ struct_def } ; }}
mkitem!{macro_rules ! mkenum { ($ enum_def : item) => { $ enum_def } ; }}
mkitem!{macro_rules ! mktrait { ($ trait_def : item) => { $ trait_def } ; }}
mkitem!{macro_rules ! mkimpl { ($ impl_def : item) => { $ impl_def } ; }}
mkitem!{macro_rules ! getname { ($ name : ident) => { stringify ! ($ name) } ; }}
mkitem!{macro_rules ! getsrc { ($ name : ident) => { "processed file" } ; }}
mkitem!{macro_rules ! getpath { ($ name : ident) => { "processed_path" } ; }}
mkitem!{macro_rules ! get_deps { ($ name : ident) => { vec ! [] } ; }}
mkmod!{rustc_complete, { 
                getname!(rustc_complete);
                getsrc!(rustc_complete);
                getpath!(rustc_complete);
                get_deps!(rustc_complete);
                get_crates!(rustc_complete);
                mkinclude!(rustc_complete);
                mkmod!{emitter, { 
                getname!(emitter);
                getsrc!(emitter);
                getpath!(emitter);
                get_deps!(emitter);
                get_crates!(emitter);
                mkinclude!(emitter);
                
macro_rules! stderr_destination_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function stderr_destination in module {}", module_path!());
    };
}

mkfn!{
    stderr_destination_introspect!();
    pub fn stderr_destination () { }
} 
            }}
mkmod!{registry, { 
                getname!(registry);
                getsrc!(registry);
                getpath!(registry);
                get_deps!(registry);
                get_crates!(registry);
                mkinclude!(registry);
                mkitem!{mkstruct!{pub struct Registry ;}} 
            }}
mkmod!{translation, { 
                getname!(translation);
                getsrc!(translation);
                getpath!(translation);
                get_deps!(translation);
                get_crates!(translation);
                mkinclude!(translation);
                mkitem!{mkstruct!{pub struct Translator ;}} 
            }}
mkitem!{mkstruct!{pub struct ColorConfig ;}}
mkitem!{mkstruct!{pub struct DiagCtxt ;}}
mkitem!{mkstruct!{pub struct ErrCode ;}}
mkitem!{mkstruct!{pub struct FatalError ;}}
mkitem!{mkstruct!{pub struct PResult < T > (pub T) ;}}
mkmod!{markdown, { 
                getname!(markdown);
                getsrc!(markdown);
                getpath!(markdown);
                get_deps!(markdown);
                get_crates!(markdown);
                mkinclude!(markdown);
                 
            }}
mkmod!{config, { 
                getname!(config);
                getsrc!(config);
                getpath!(config);
                get_deps!(config);
                get_crates!(config);
                mkinclude!(config);
                mkitem!{mkstruct!{pub struct CG_OPTIONS ;}}
mkitem!{mkstruct!{pub struct CrateType ;}}
mkitem!{mkstruct!{pub struct ErrorOutputType ;}}
mkitem!{mkstruct!{pub struct Input ;}}
mkitem!{mkstruct!{pub struct OptionDesc ;}}
mkitem!{mkstruct!{pub struct OutFileName ;}}
mkitem!{mkstruct!{pub struct OutputType ;}}
mkitem!{mkstruct!{pub struct Sysroot ;}}
mkitem!{mkstruct!{pub struct UnstableOptions ;}}
mkitem!{mkstruct!{pub struct Z_OPTIONS ;}}

macro_rules! nightly_options_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function nightly_options in module {}", module_path!());
    };
}

mkfn!{
    nightly_options_introspect!();
    pub fn nightly_options () { }
}

macro_rules! parse_target_triple_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function parse_target_triple in module {}", module_path!());
    };
}

mkfn!{
    parse_target_triple_introspect!();
    pub fn parse_target_triple () { }
} 
            }}
mkmod!{getopts, { 
                getname!(getopts);
                getsrc!(getopts);
                getpath!(getopts);
                get_deps!(getopts);
                get_crates!(getopts);
                mkinclude!(getopts);
                mkitem!{mkstruct!{pub struct Matches ;}} 
            }}
mkmod!{lint, { 
                getname!(lint);
                getsrc!(lint);
                getpath!(lint);
                get_deps!(lint);
                get_crates!(lint);
                mkinclude!(lint);
                mkitem!{mkstruct!{pub struct Lint ;}}
mkitem!{mkstruct!{pub struct LintId ;}} 
            }}
mkmod!{output, { 
                getname!(output);
                getsrc!(output);
                getpath!(output);
                get_deps!(output);
                get_crates!(output);
                mkinclude!(output);
                mkitem!{mkstruct!{pub struct CRATE_TYPES ;}}

macro_rules! collect_crate_types_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function collect_crate_types in module {}", module_path!());
    };
}

mkfn!{
    collect_crate_types_introspect!();
    pub fn collect_crate_types () { }
}

macro_rules! invalid_output_for_target_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function invalid_output_for_target in module {}", module_path!());
    };
}

mkfn!{
    invalid_output_for_target_introspect!();
    pub fn invalid_output_for_target () { }
} 
            }}
mkitem!{mkstruct!{pub struct EarlyDiagCtxt ;}}
mkitem!{mkstruct!{pub struct Session ;}}
mkitem!{mkstruct!{pub struct FileName ;}}
mkmod!{def_id, { 
                getname!(def_id);
                getsrc!(def_id);
                getpath!(def_id);
                get_deps!(def_id);
                get_crates!(def_id);
                mkinclude!(def_id);
                mkitem!{mkstruct!{pub struct LOCAL_CRATE ;}} 
            }}
mkmod!{ty, { 
                getname!(ty);
                getsrc!(ty);
                getpath!(ty);
                get_deps!(ty);
                get_crates!(ty);
                mkinclude!(ty);
                mkitem!{mkstruct!{pub struct TyCtxt < T > (pub T) ;}} 
            }} 
            }}
mkmod!{session_diagnostics, { 
                getname!(session_diagnostics);
                getsrc!(session_diagnostics);
                getpath!(session_diagnostics);
                get_deps!(session_diagnostics);
                get_crates!(session_diagnostics);
                mkinclude!(session_diagnostics);
                mkitem!{mkstruct!{pub struct CantEmitMIR ;}}
mkitem!{mkstruct!{pub struct RLinkEmptyVersionNumber ;}}
mkitem!{mkstruct!{pub struct RLinkEncodingVersionMismatch ;}}
mkitem!{mkstruct!{pub struct RLinkRustcVersionMismatch ;}}
mkitem!{mkstruct!{pub struct RLinkWrongFileType ;}}
mkitem!{mkstruct!{pub struct RlinkCorruptFile ;}}
mkitem!{mkstruct!{pub struct RlinkNotAFile ;}}
mkitem!{mkstruct!{pub struct RlinkUnableToRead ;}}
mkitem!{mkstruct!{pub struct UnstableFeatureUsage ;}} 
            }}
mkitem!{macro_rules ! do_not_use_print { ($ ($ t : tt) *) => { compile_error ! ("Don't use print") } ; }}
mkitem!{macro_rules ! do_not_use_safe_print { ($ ($ t : tt) *) => { compile_error ! ("Don't use safe_print") } ; }}
mkitem!{macro_rules ! mktrait { ($ trait_def : item) => { $ trait_def } ; }}
mkitem!{macro_rules ! mkimpl { ($ impl_def : item) => { $ impl_def } ; }}
mkitem!{macro_rules ! getname { ($ name : ident) => { pub fn get_module_name () -> &'static str { stringify ! ($ name) } } ; }}
mkitem!{macro_rules ! getsrc { ($ name : ident) => { pub fn get_source_info () -> &'static str { concat ! ("Module: " , stringify ! ($ name)) } } ; }}
mkitem!{macro_rules ! getpath { ($ name : ident) => { pub fn get_module_path () -> &'static str { module_path ! () } } ; }}
mkitem!{macro_rules ! get_deps { ($ name : ident) => { pub fn get_dependencies () -> &'static [&'static str] { & [] } } ; }}
mkitem!{macro_rules ! get_crates { ($ name : ident) => { pub fn get_required_crates () -> &'static [&'static str] { & [] } } ; }}
mkitem!{macro_rules ! forall_crates { ($ ($ crate_name : ident) ,*) => { $ (extern crate $ crate_name ;) * } ; }}
mkitem!{macro_rules ! emit_extern { ($ crate_name : ident) => { extern crate $ crate_name ; } ; }}
mkitem!{macro_rules ! get_externs { ($ crate_name : ident) => { stringify ! ($ crate_name) } ; }}
mkuse!{use crate :: simd :: { LaneCount , Mask , MaskElement , SupportedLaneCount , Swizzle , cmp :: SimdPartialOrd , num :: SimdUint , ptr :: { SimdConstPtr , SimdMutPtr } , } ;}
mkitem!{mkstruct!{# [doc = " A SIMD vector with the shape of `[T; N]` but the operations of `T`."] # [doc = ""] # [doc = " `Simd<T, N>` supports the operators (+, *, etc.) that `T` does in \"elementwise\" fashion."] # [doc = " These take the element at each index from the left-hand side and right-hand side,"] # [doc = " perform the operation, then return the result in the same index in a vector of equal size."] # [doc = " However, `Simd` differs from normal iteration and normal arrays:"] # [doc = " - `Simd<T, N>` executes `N` operations in a single step with no `break`s"] # [doc = " - `Simd<T, N>` can have an alignment greater than `T`, for better mechanical sympathy"] # [doc = ""] # [doc = " By always imposing these constraints on `Simd`, it is easier to compile elementwise operations"] # [doc = " into machine instructions that can themselves be executed in parallel."] # [doc = ""] # [doc = " ```rust"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # use core::simd::{Simd};"] # [doc = " # use core::array;"] # [doc = " let a: [i32; 4] = [-2, 0, 2, 4];"] # [doc = " let b = [10, 9, 8, 7];"] # [doc = " let sum = array::from_fn(|i| a[i] + b[i]);"] # [doc = " let prod = array::from_fn(|i| a[i] * b[i]);"] # [doc = ""] # [doc = " // `Simd<T, N>` implements `From<[T; N]>`"] # [doc = " let (v, w) = (Simd::from(a), Simd::from(b));"] # [doc = " // Which means arrays implement `Into<Simd<T, N>>`."] # [doc = " assert_eq!(v + w, sum.into());"] # [doc = " assert_eq!(v * w, prod.into());"] # [doc = " ```"] # [doc = ""] # [doc = ""] # [doc = " `Simd` with integer elements treats operators as wrapping, as if `T` was [`Wrapping<T>`]."] # [doc = " Thus, `Simd` does not implement `wrapping_add`, because that is the default behavior."] # [doc = " This means there is no warning on overflows, even in \"debug\" builds."] # [doc = " For most applications where `Simd` is appropriate, it is \"not a bug\" to wrap,"] # [doc = " and even \"debug builds\" are unlikely to tolerate the loss of performance."] # [doc = " You may want to consider using explicitly checked arithmetic if such is required."] # [doc = " Division by zero on integers still causes a panic, so"] # [doc = " you may want to consider using `f32` or `f64` if that is unacceptable."] # [doc = ""] # [doc = " [`Wrapping<T>`]: core::num::Wrapping"] # [doc = ""] # [doc = " # Layout"] # [doc = " `Simd<T, N>` has a layout similar to `[T; N]` (identical \"shapes\"), with a greater alignment."] # [doc = " `[T; N]` is aligned to `T`, but `Simd<T, N>` will have an alignment based on both `T` and `N`."] # [doc = " Thus it is sound to [`transmute`] `Simd<T, N>` to `[T; N]` and should optimize to \"zero cost\","] # [doc = " but the reverse transmutation may require a copy the compiler cannot simply elide."] # [doc = ""] # [doc = " # ABI \"Features\""] # [doc = " Due to Rust's safety guarantees, `Simd<T, N>` is currently passed and returned via memory,"] # [doc = " not SIMD registers, except as an optimization. Using `#[inline]` on functions that accept"] # [doc = " `Simd<T, N>` or return it is recommended, at the cost of code generation time, as"] # [doc = " inlining SIMD-using functions can omit a large function prolog or epilog and thus"] # [doc = " improve both speed and code size. The need for this may be corrected in the future."] # [doc = ""] # [doc = " Using `#[inline(always)]` still requires additional care."] # [doc = ""] # [doc = " # Safe SIMD with Unsafe Rust"] # [doc = ""] # [doc = " Operations with `Simd` are typically safe, but there are many reasons to want to combine SIMD with `unsafe` code."] # [doc = " Care must be taken to respect differences between `Simd` and other types it may be transformed into or derived from."] # [doc = " In particular, the layout of `Simd<T, N>` may be similar to `[T; N]`, and may allow some transmutations,"] # [doc = " but references to `[T; N]` are not interchangeable with those to `Simd<T, N>`."] # [doc = " Thus, when using `unsafe` Rust to read and write `Simd<T, N>` through [raw pointers], it is a good idea to first try with"] # [doc = " [`read_unaligned`] and [`write_unaligned`]. This is because:"] # [doc = " - [`read`] and [`write`] require full alignment (in this case, `Simd<T, N>`'s alignment)"] # [doc = " - `Simd<T, N>` is often read from or written to [`[T]`](slice) and other types aligned to `T`"] # [doc = " - combining these actions violates the `unsafe` contract and explodes the program into"] # [doc = "   a puff of **undefined behavior**"] # [doc = " - the compiler can implicitly adjust layouts to make unaligned reads or writes fully aligned"] # [doc = "   if it sees the optimization"] # [doc = " - most contemporary processors with \"aligned\" and \"unaligned\" read and write instructions"] # [doc = "   exhibit no performance difference if the \"unaligned\" variant is aligned at runtime"] # [doc = ""] # [doc = " Less obligations mean unaligned reads and writes are less likely to make the program unsound,"] # [doc = " and may be just as fast as stricter alternatives."] # [doc = " When trying to guarantee alignment, [`[T]::as_simd`][as_simd] is an option for"] # [doc = " converting `[T]` to `[Simd<T, N>]`, and allows soundly operating on an aligned SIMD body,"] # [doc = " but it may cost more time when handling the scalar head and tail."] # [doc = " If these are not enough, it is most ideal to design data structures to be already aligned"] # [doc = " to `align_of::<Simd<T, N>>()` before using `unsafe` Rust to read or write."] # [doc = " Other ways to compensate for these facts, like materializing `Simd` to or from an array first,"] # [doc = " are handled by safe methods like [`Simd::from_array`] and [`Simd::from_slice`]."] # [doc = ""] # [doc = " [`transmute`]: core::mem::transmute"] # [doc = " [raw pointers]: pointer"] # [doc = " [`read_unaligned`]: pointer::read_unaligned"] # [doc = " [`write_unaligned`]: pointer::write_unaligned"] # [doc = " [`read`]: pointer::read"] # [doc = " [`write`]: pointer::write"] # [doc = " [as_simd]: slice::as_simd"] # [repr (simd , packed)] pub struct Simd < T , const N : usize > ([T ; N]) where LaneCount < N > : SupportedLaneCount , T : SimdElement ;}}
mkitem!{mkimpl!{impl < T , const N : usize > Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement , { # [doc = " Number of elements in this vector."] pub const LEN : usize = N ; # [doc = " Returns the number of elements in this SIMD vector."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::u32x4;"] # [doc = " let v = u32x4::splat(0);"] # [doc = " assert_eq!(v.len(), 4);"] # [doc = " ```"] # [inline] # [allow (clippy :: len_without_is_empty)] pub const fn len (& self) -> usize { Self :: LEN } # [doc = " Constructs a new SIMD vector with all elements set to the given value."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::u32x4;"] # [doc = " let v = u32x4::splat(8);"] # [doc = " assert_eq!(v.as_array(), &[8, 8, 8, 8]);"] # [doc = " ```"] # [inline] # [rustc_const_unstable (feature = "portable_simd" , issue = "86656")] pub const fn splat (value : T) -> Self { const fn splat_const < T , const N : usize > (value : T) -> Simd < T , N > where T : SimdElement , LaneCount < N > : SupportedLaneCount , { Simd :: from_array ([value ; N]) } fn splat_rt < T , const N : usize > (value : T) -> Simd < T , N > where T : SimdElement , LaneCount < N > : SupportedLaneCount , { struct Splat ; impl < const N : usize > Swizzle < N > for Splat { const INDEX : [usize ; N] = [0 ; N] ; } Splat :: swizzle :: < T , 1 > (Simd :: < T , 1 > :: from ([value])) } core :: intrinsics :: const_eval_select ((value ,) , splat_const , splat_rt) } # [doc = " Returns an array reference containing the entire SIMD vector."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # use core::simd::{Simd, u64x4};"] # [doc = " let v: u64x4 = Simd::from_array([0, 1, 2, 3]);"] # [doc = " assert_eq!(v.as_array(), &[0, 1, 2, 3]);"] # [doc = " ```"] # [inline] pub const fn as_array (& self) -> & [T ; N] { unsafe { & * (self as * const Self as * const [T ; N]) } } # [doc = " Returns a mutable array reference containing the entire SIMD vector."] # [inline] pub fn as_mut_array (& mut self) -> & mut [T ; N] { unsafe { & mut * (self as * mut Self as * mut [T ; N]) } } # [doc = " Loads a vector from an array of `T`."] # [doc = ""] # [doc = " This function is necessary since `repr(simd)` has padding for non-power-of-2 vectors (at the time of writing)."] # [doc = " With padding, `read_unaligned` will read past the end of an array of N elements."] # [doc = ""] # [doc = " # Safety"] # [doc = " Reading `ptr` must be safe, as if by `<*const [T; N]>::read`."] # [inline] const unsafe fn load (ptr : * const [T ; N]) -> Self { let mut tmp = core :: mem :: MaybeUninit :: < Self > :: uninit () ; unsafe { core :: ptr :: copy_nonoverlapping (ptr , tmp . as_mut_ptr () . cast () , 1) ; tmp . assume_init () } } # [doc = " Store a vector to an array of `T`."] # [doc = ""] # [doc = " See `load` as to why this function is necessary."] # [doc = ""] # [doc = " # Safety"] # [doc = " Writing to `ptr` must be safe, as if by `<*mut [T; N]>::write`."] # [inline] const unsafe fn store (self , ptr : * mut [T ; N]) { let tmp = self ; unsafe { core :: ptr :: copy_nonoverlapping (tmp . as_array () , ptr , 1) } } # [doc = " Converts an array to a SIMD vector."] # [inline] pub const fn from_array (array : [T ; N]) -> Self { unsafe { Self :: load (& array) } } # [doc = " Converts a SIMD vector to an array."] # [inline] pub const fn to_array (self) -> [T ; N] { let mut tmp = core :: mem :: MaybeUninit :: uninit () ; unsafe { self . store (tmp . as_mut_ptr ()) ; tmp . assume_init () } } # [doc = " Converts a slice to a SIMD vector containing `slice[..N]`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the slice's length is less than the vector's `Simd::N`."] # [doc = " Use `load_or_default` for an alternative that does not panic."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # use core::simd::u32x4;"] # [doc = " let source = vec![1, 2, 3, 4, 5, 6];"] # [doc = " let v = u32x4::from_slice(&source);"] # [doc = " assert_eq!(v.as_array(), &[1, 2, 3, 4]);"] # [doc = " ```"] # [must_use] # [inline] # [track_caller] pub const fn from_slice (slice : & [T]) -> Self { assert ! (slice . len () >= Self :: LEN , "slice length must be at least the number of elements") ; unsafe { Self :: load (slice . as_ptr () . cast ()) } } # [doc = " Writes a SIMD vector to the first `N` elements of a slice."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the slice's length is less than the vector's `Simd::N`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::u32x4;"] # [doc = " let mut dest = vec![0; 6];"] # [doc = " let v = u32x4::from_array([1, 2, 3, 4]);"] # [doc = " v.copy_to_slice(&mut dest);"] # [doc = " assert_eq!(&dest, &[1, 2, 3, 4, 0, 0]);"] # [doc = " ```"] # [inline] # [track_caller] pub fn copy_to_slice (self , slice : & mut [T]) { assert ! (slice . len () >= Self :: LEN , "slice length must be at least the number of elements") ; unsafe { self . store (slice . as_mut_ptr () . cast ()) } } # [doc = " Reads contiguous elements from `slice`. Elements are read so long as they're in-bounds for"] # [doc = " the `slice`. Otherwise, the default value for the element type is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::Simd;"] # [doc = " let vec: Vec<i32> = vec![10, 11];"] # [doc = ""] # [doc = " let result = Simd::<i32, 4>::load_or_default(&vec);"] # [doc = " assert_eq!(result, Simd::from_array([10, 11, 0, 0]));"] # [doc = " ```"] # [must_use] # [inline] pub fn load_or_default (slice : & [T]) -> Self where T : Default , { Self :: load_or (slice , Default :: default ()) } # [doc = " Reads contiguous elements from `slice`. Elements are read so long as they're in-bounds for"] # [doc = " the `slice`. Otherwise, the corresponding value from `or` is passed through."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::Simd;"] # [doc = " let vec: Vec<i32> = vec![10, 11];"] # [doc = " let or = Simd::from_array([-5, -4, -3, -2]);"] # [doc = ""] # [doc = " let result = Simd::load_or(&vec, or);"] # [doc = " assert_eq!(result, Simd::from_array([10, 11, -3, -2]));"] # [doc = " ```"] # [must_use] # [inline] pub fn load_or (slice : & [T] , or : Self) -> Self { Self :: load_select (slice , Mask :: splat (true) , or) } # [doc = " Reads contiguous elements from `slice`. Each element is read from memory if its"] # [doc = " corresponding element in `enable` is `true`."] # [doc = ""] # [doc = " When the element is disabled or out of bounds for the slice, that memory location"] # [doc = " is not accessed and the corresponding value from `or` is passed through."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::{Simd, Mask};"] # [doc = " let vec: Vec<i32> = vec![10, 11, 12, 13, 14, 15, 16, 17, 18];"] # [doc = " let enable = Mask::from_array([true, true, false, true]);"] # [doc = " let or = Simd::from_array([-5, -4, -3, -2]);"] # [doc = ""] # [doc = " let result = Simd::load_select(&vec, enable, or);"] # [doc = " assert_eq!(result, Simd::from_array([10, 11, -3, 13]));"] # [doc = " ```"] # [must_use] # [inline] pub fn load_select_or_default (slice : & [T] , enable : Mask < < T as SimdElement > :: Mask , N >) -> Self where T : Default , { Self :: load_select (slice , enable , Default :: default ()) } # [doc = " Reads contiguous elements from `slice`. Each element is read from memory if its"] # [doc = " corresponding element in `enable` is `true`."] # [doc = ""] # [doc = " When the element is disabled or out of bounds for the slice, that memory location"] # [doc = " is not accessed and the corresponding value from `or` is passed through."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::{Simd, Mask};"] # [doc = " let vec: Vec<i32> = vec![10, 11, 12, 13, 14, 15, 16, 17, 18];"] # [doc = " let enable = Mask::from_array([true, true, false, true]);"] # [doc = " let or = Simd::from_array([-5, -4, -3, -2]);"] # [doc = ""] # [doc = " let result = Simd::load_select(&vec, enable, or);"] # [doc = " assert_eq!(result, Simd::from_array([10, 11, -3, 13]));"] # [doc = " ```"] # [must_use] # [inline] pub fn load_select (slice : & [T] , mut enable : Mask < < T as SimdElement > :: Mask , N > , or : Self ,) -> Self { enable &= mask_up_to (slice . len ()) ; unsafe { Self :: load_select_ptr (slice . as_ptr () , enable , or) } } # [doc = " Reads contiguous elements from `slice`. Each element is read from memory if its"] # [doc = " corresponding element in `enable` is `true`."] # [doc = ""] # [doc = " When the element is disabled, that memory location is not accessed and the corresponding"] # [doc = " value from `or` is passed through."] # [doc = ""] # [doc = " # Safety"] # [doc = " Enabled loads must not exceed the length of `slice`."] # [must_use] # [inline] pub unsafe fn load_select_unchecked (slice : & [T] , enable : Mask < < T as SimdElement > :: Mask , N > , or : Self ,) -> Self { let ptr = slice . as_ptr () ; unsafe { Self :: load_select_ptr (ptr , enable , or) } } # [doc = " Reads contiguous elements starting at `ptr`. Each element is read from memory if its"] # [doc = " corresponding element in `enable` is `true`."] # [doc = ""] # [doc = " When the element is disabled, that memory location is not accessed and the corresponding"] # [doc = " value from `or` is passed through."] # [doc = ""] # [doc = " # Safety"] # [doc = " Enabled `ptr` elements must be safe to read as if by `std::ptr::read`."] # [must_use] # [inline] pub unsafe fn load_select_ptr (ptr : * const T , enable : Mask < < T as SimdElement > :: Mask , N > , or : Self ,) -> Self { unsafe { core :: intrinsics :: simd :: simd_masked_load (enable . to_int () , ptr , or) } } # [doc = " Reads from potentially discontiguous indices in `slice` to construct a SIMD vector."] # [doc = " If an index is out-of-bounds, the element is instead selected from the `or` vector."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # use core::simd::Simd;"] # [doc = " let vec: Vec<i32> = vec![10, 11, 12, 13, 14, 15, 16, 17, 18];"] # [doc = " let idxs = Simd::from_array([9, 3, 0, 5]);  // Note the index that is out-of-bounds"] # [doc = " let alt = Simd::from_array([-5, -4, -3, -2]);"] # [doc = ""] # [doc = " let result = Simd::gather_or(&vec, idxs, alt);"] # [doc = " assert_eq!(result, Simd::from_array([-5, 13, 10, 15]));"] # [doc = " ```"] # [must_use] # [inline] pub fn gather_or (slice : & [T] , idxs : Simd < usize , N > , or : Self) -> Self { Self :: gather_select (slice , Mask :: splat (true) , idxs , or) } # [doc = " Reads from indices in `slice` to construct a SIMD vector."] # [doc = " If an index is out-of-bounds, the element is set to the default given by `T: Default`."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # use core::simd::Simd;"] # [doc = " let vec: Vec<i32> = vec![10, 11, 12, 13, 14, 15, 16, 17, 18];"] # [doc = " let idxs = Simd::from_array([9, 3, 0, 5]);  // Note the index that is out-of-bounds"] # [doc = ""] # [doc = " let result = Simd::gather_or_default(&vec, idxs);"] # [doc = " assert_eq!(result, Simd::from_array([0, 13, 10, 15]));"] # [doc = " ```"] # [must_use] # [inline] pub fn gather_or_default (slice : & [T] , idxs : Simd < usize , N >) -> Self where T : Default , { Self :: gather_or (slice , idxs , Self :: splat (T :: default ())) } # [doc = " Reads from indices in `slice` to construct a SIMD vector."] # [doc = " The mask `enable`s all `true` indices and disables all `false` indices."] # [doc = " If an index is disabled or is out-of-bounds, the element is selected from the `or` vector."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # use core::simd::{Simd, Mask};"] # [doc = " let vec: Vec<i32> = vec![10, 11, 12, 13, 14, 15, 16, 17, 18];"] # [doc = " let idxs = Simd::from_array([9, 3, 0, 5]); // Includes an out-of-bounds index"] # [doc = " let alt = Simd::from_array([-5, -4, -3, -2]);"] # [doc = " let enable = Mask::from_array([true, true, true, false]); // Includes a masked element"] # [doc = ""] # [doc = " let result = Simd::gather_select(&vec, enable, idxs, alt);"] # [doc = " assert_eq!(result, Simd::from_array([-5, 13, 10, -2]));"] # [doc = " ```"] # [must_use] # [inline] pub fn gather_select (slice : & [T] , enable : Mask < isize , N > , idxs : Simd < usize , N > , or : Self ,) -> Self { let enable : Mask < isize , N > = enable & idxs . simd_lt (Simd :: splat (slice . len ())) ; unsafe { Self :: gather_select_unchecked (slice , enable , idxs , or) } } # [doc = " Reads from indices in `slice` to construct a SIMD vector."] # [doc = " The mask `enable`s all `true` indices and disables all `false` indices."] # [doc = " If an index is disabled, the element is selected from the `or` vector."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Calling this function with an `enable`d out-of-bounds index is *[undefined behavior]*"] # [doc = " even if the resulting value is not used."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::{Simd, cmp::SimdPartialOrd, Mask};"] # [doc = " let vec: Vec<i32> = vec![10, 11, 12, 13, 14, 15, 16, 17, 18];"] # [doc = " let idxs = Simd::from_array([9, 3, 0, 5]); // Includes an out-of-bounds index"] # [doc = " let alt = Simd::from_array([-5, -4, -3, -2]);"] # [doc = " let enable = Mask::from_array([true, true, true, false]); // Includes a masked element"] # [doc = " // If this mask was used to gather, it would be unsound. Let's fix that."] # [doc = " let enable = enable & idxs.simd_lt(Simd::splat(vec.len()));"] # [doc = ""] # [doc = " // The out-of-bounds index has been masked, so it's safe to gather now."] # [doc = " let result = unsafe { Simd::gather_select_unchecked(&vec, enable, idxs, alt) };"] # [doc = " assert_eq!(result, Simd::from_array([-5, 13, 10, -2]));"] # [doc = " ```"] # [doc = " [undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [must_use] # [inline] # [cfg_attr (miri , track_caller)] pub unsafe fn gather_select_unchecked (slice : & [T] , enable : Mask < isize , N > , idxs : Simd < usize , N > , or : Self ,) -> Self { let base_ptr = Simd :: < * const T , N > :: splat (slice . as_ptr ()) ; let ptrs = base_ptr . wrapping_add (idxs) ; unsafe { Self :: gather_select_ptr (ptrs , enable , or) } } # [doc = " Reads elementwise from pointers into a SIMD vector."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Each read must satisfy the same conditions as [`core::ptr::read`]."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::prelude::*;"] # [doc = " let values = [6, 2, 4, 9];"] # [doc = " let offsets = Simd::from_array([1, 0, 0, 3]);"] # [doc = " let source = Simd::splat(values.as_ptr()).wrapping_add(offsets);"] # [doc = " let gathered = unsafe { Simd::gather_ptr(source) };"] # [doc = " assert_eq!(gathered, Simd::from_array([2, 6, 6, 9]));"] # [doc = " ```"] # [must_use] # [inline] # [cfg_attr (miri , track_caller)] pub unsafe fn gather_ptr (source : Simd < * const T , N >) -> Self where T : Default , { unsafe { Self :: gather_select_ptr (source , Mask :: splat (true) , Self :: default ()) } } # [doc = " Conditionally read elementwise from pointers into a SIMD vector."] # [doc = " The mask `enable`s all `true` pointers and disables all `false` pointers."] # [doc = " If a pointer is disabled, the element is selected from the `or` vector,"] # [doc = " and no read is performed."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Enabled elements must satisfy the same conditions as [`core::ptr::read`]."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::prelude::*;"] # [doc = " let values = [6, 2, 4, 9];"] # [doc = " let enable = Mask::from_array([true, true, false, true]);"] # [doc = " let offsets = Simd::from_array([1, 0, 0, 3]);"] # [doc = " let source = Simd::splat(values.as_ptr()).wrapping_add(offsets);"] # [doc = " let gathered = unsafe { Simd::gather_select_ptr(source, enable, Simd::splat(0)) };"] # [doc = " assert_eq!(gathered, Simd::from_array([2, 6, 0, 9]));"] # [doc = " ```"] # [must_use] # [inline] # [cfg_attr (miri , track_caller)] pub unsafe fn gather_select_ptr (source : Simd < * const T , N > , enable : Mask < isize , N > , or : Self ,) -> Self { unsafe { core :: intrinsics :: simd :: simd_gather (or , source , enable . to_int ()) } } # [doc = " Conditionally write contiguous elements to `slice`. The `enable` mask controls"] # [doc = " which elements are written, as long as they're in-bounds of the `slice`."] # [doc = " If the element is disabled or out of bounds, no memory access to that location"] # [doc = " is made."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::{Simd, Mask};"] # [doc = " let mut arr = [0i32; 4];"] # [doc = " let write = Simd::from_array([-5, -4, -3, -2]);"] # [doc = " let enable = Mask::from_array([false, true, true, true]);"] # [doc = ""] # [doc = " write.store_select(&mut arr[..3], enable);"] # [doc = " assert_eq!(arr, [0, -4, -3, 0]);"] # [doc = " ```"] # [inline] pub fn store_select (self , slice : & mut [T] , mut enable : Mask < < T as SimdElement > :: Mask , N >) { enable &= mask_up_to (slice . len ()) ; unsafe { self . store_select_ptr (slice . as_mut_ptr () , enable) } } # [doc = " Conditionally write contiguous elements to `slice`. The `enable` mask controls"] # [doc = " which elements are written."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Every enabled element must be in bounds for the `slice`."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::{Simd, Mask};"] # [doc = " let mut arr = [0i32; 4];"] # [doc = " let write = Simd::from_array([-5, -4, -3, -2]);"] # [doc = " let enable = Mask::from_array([false, true, true, true]);"] # [doc = ""] # [doc = " unsafe { write.store_select_unchecked(&mut arr, enable) };"] # [doc = " assert_eq!(arr, [0, -4, -3, -2]);"] # [doc = " ```"] # [inline] pub unsafe fn store_select_unchecked (self , slice : & mut [T] , enable : Mask < < T as SimdElement > :: Mask , N > ,) { let ptr = slice . as_mut_ptr () ; unsafe { self . store_select_ptr (ptr , enable) } } # [doc = " Conditionally write contiguous elements starting from `ptr`."] # [doc = " The `enable` mask controls which elements are written."] # [doc = " When disabled, the memory location corresponding to that element is not accessed."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Memory addresses for element are calculated [`pointer::wrapping_offset`] and"] # [doc = " each enabled element must satisfy the same conditions as [`core::ptr::write`]."] # [inline] pub unsafe fn store_select_ptr (self , ptr : * mut T , enable : Mask < < T as SimdElement > :: Mask , N >) { unsafe { core :: intrinsics :: simd :: simd_masked_store (enable . to_int () , ptr , self) } } # [doc = " Writes the values in a SIMD vector to potentially discontiguous indices in `slice`."] # [doc = " If an index is out-of-bounds, the write is suppressed without panicking."] # [doc = " If two elements in the scattered vector would write to the same index"] # [doc = " only the last element is guaranteed to actually be written."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # use core::simd::Simd;"] # [doc = " let mut vec: Vec<i32> = vec![10, 11, 12, 13, 14, 15, 16, 17, 18];"] # [doc = " let idxs = Simd::from_array([9, 3, 0, 0]); // Note the duplicate index."] # [doc = " let vals = Simd::from_array([-27, 82, -41, 124]);"] # [doc = ""] # [doc = " vals.scatter(&mut vec, idxs); // two logical writes means the last wins."] # [doc = " assert_eq!(vec, vec![124, 11, 12, 82, 14, 15, 16, 17, 18]);"] # [doc = " ```"] # [inline] pub fn scatter (self , slice : & mut [T] , idxs : Simd < usize , N >) { self . scatter_select (slice , Mask :: splat (true) , idxs) } # [doc = " Writes values from a SIMD vector to multiple potentially discontiguous indices in `slice`."] # [doc = " The mask `enable`s all `true` indices and disables all `false` indices."] # [doc = " If an enabled index is out-of-bounds, the write is suppressed without panicking."] # [doc = " If two enabled elements in the scattered vector would write to the same index,"] # [doc = " only the last element is guaranteed to actually be written."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::{Simd, Mask};"] # [doc = " let mut vec: Vec<i32> = vec![10, 11, 12, 13, 14, 15, 16, 17, 18];"] # [doc = " let idxs = Simd::from_array([9, 3, 0, 0]); // Includes an out-of-bounds index"] # [doc = " let vals = Simd::from_array([-27, 82, -41, 124]);"] # [doc = " let enable = Mask::from_array([true, true, true, false]); // Includes a masked element"] # [doc = ""] # [doc = " vals.scatter_select(&mut vec, enable, idxs); // The last write is masked, thus omitted."] # [doc = " assert_eq!(vec, vec![-41, 11, 12, 82, 14, 15, 16, 17, 18]);"] # [doc = " ```"] # [inline] pub fn scatter_select (self , slice : & mut [T] , enable : Mask < isize , N > , idxs : Simd < usize , N >) { let enable : Mask < isize , N > = enable & idxs . simd_lt (Simd :: splat (slice . len ())) ; unsafe { self . scatter_select_unchecked (slice , enable , idxs) } } # [doc = " Writes values from a SIMD vector to multiple potentially discontiguous indices in `slice`."] # [doc = " The mask `enable`s all `true` indices and disables all `false` indices."] # [doc = " If two enabled elements in the scattered vector would write to the same index,"] # [doc = " only the last element is guaranteed to actually be written."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Calling this function with an enabled out-of-bounds index is *[undefined behavior]*,"] # [doc = " and may lead to memory corruption."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::{Simd, cmp::SimdPartialOrd, Mask};"] # [doc = " let mut vec: Vec<i32> = vec![10, 11, 12, 13, 14, 15, 16, 17, 18];"] # [doc = " let idxs = Simd::from_array([9, 3, 0, 0]);"] # [doc = " let vals = Simd::from_array([-27, 82, -41, 124]);"] # [doc = " let enable = Mask::from_array([true, true, true, false]); // Masks the final index"] # [doc = " // If this mask was used to scatter, it would be unsound. Let's fix that."] # [doc = " let enable = enable & idxs.simd_lt(Simd::splat(vec.len()));"] # [doc = ""] # [doc = " // We have masked the OOB index, so it's safe to scatter now."] # [doc = " unsafe { vals.scatter_select_unchecked(&mut vec, enable, idxs); }"] # [doc = " // The second write to index 0 was masked, thus omitted."] # [doc = " assert_eq!(vec, vec![-41, 11, 12, 82, 14, 15, 16, 17, 18]);"] # [doc = " ```"] # [doc = " [undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] # [cfg_attr (miri , track_caller)] pub unsafe fn scatter_select_unchecked (self , slice : & mut [T] , enable : Mask < isize , N > , idxs : Simd < usize , N > ,) { unsafe { let base_ptr = Simd :: < * mut T , N > :: splat (slice . as_mut_ptr ()) ; let ptrs = base_ptr . wrapping_add (idxs) ; self . scatter_select_ptr (ptrs , enable) ; } } # [doc = " Writes pointers elementwise into a SIMD vector."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Each write must satisfy the same conditions as [`core::ptr::write`]."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::{Simd, ptr::SimdMutPtr};"] # [doc = " let mut values = [0; 4];"] # [doc = " let offset = Simd::from_array([3, 2, 1, 0]);"] # [doc = " let ptrs = Simd::splat(values.as_mut_ptr()).wrapping_add(offset);"] # [doc = " unsafe { Simd::from_array([6, 3, 5, 7]).scatter_ptr(ptrs); }"] # [doc = " assert_eq!(values, [7, 5, 3, 6]);"] # [doc = " ```"] # [inline] # [cfg_attr (miri , track_caller)] pub unsafe fn scatter_ptr (self , dest : Simd < * mut T , N >) { unsafe { self . scatter_select_ptr (dest , Mask :: splat (true)) } } # [doc = " Conditionally write pointers elementwise into a SIMD vector."] # [doc = " The mask `enable`s all `true` pointers and disables all `false` pointers."] # [doc = " If a pointer is disabled, the write to its pointee is skipped."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Enabled pointers must satisfy the same conditions as [`core::ptr::write`]."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " # #![feature(portable_simd)]"] # [doc = " # #[cfg(feature = \"as_crate\")] use core_simd::simd;"] # [doc = " # #[cfg(not(feature = \"as_crate\"))] use core::simd;"] # [doc = " # use simd::{Mask, Simd, ptr::SimdMutPtr};"] # [doc = " let mut values = [0; 4];"] # [doc = " let offset = Simd::from_array([3, 2, 1, 0]);"] # [doc = " let ptrs = Simd::splat(values.as_mut_ptr()).wrapping_add(offset);"] # [doc = " let enable = Mask::from_array([true, true, false, false]);"] # [doc = " unsafe { Simd::from_array([6, 3, 5, 7]).scatter_select_ptr(ptrs, enable); }"] # [doc = " assert_eq!(values, [0, 0, 3, 6]);"] # [doc = " ```"] # [inline] # [cfg_attr (miri , track_caller)] pub unsafe fn scatter_select_ptr (self , dest : Simd < * mut T , N > , enable : Mask < isize , N >) { unsafe { core :: intrinsics :: simd :: simd_scatter (self , dest , enable . to_int ()) } } }}}
mkitem!{mkimpl!{impl < T , const N : usize > Copy for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement , { }}}
mkitem!{mkimpl!{impl < T , const N : usize > Clone for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement , { # [inline] fn clone (& self) -> Self { * self } }}}
mkitem!{mkimpl!{impl < T , const N : usize > Default for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement + Default , { # [inline] fn default () -> Self { Self :: splat (T :: default ()) } }}}
mkitem!{mkimpl!{impl < T , const N : usize > PartialEq for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement + PartialEq , { # [inline] fn eq (& self , other : & Self) -> bool { let mask = unsafe { let tfvec : Simd < < T as SimdElement > :: Mask , N > = core :: intrinsics :: simd :: simd_eq (* self , * other) ; Mask :: from_int_unchecked (tfvec) } ; mask . all () } # [allow (clippy :: partialeq_ne_impl)] # [inline] fn ne (& self , other : & Self) -> bool { let mask = unsafe { let tfvec : Simd < < T as SimdElement > :: Mask , N > = core :: intrinsics :: simd :: simd_ne (* self , * other) ; Mask :: from_int_unchecked (tfvec) } ; mask . any () } }}}
mkitem!{mkimpl!{# [doc = " Lexicographic order. For the SIMD elementwise minimum and maximum, use simd_min and simd_max instead."] impl < T , const N : usize > PartialOrd for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement + PartialOrd , { # [inline] fn partial_cmp (& self , other : & Self) -> Option < core :: cmp :: Ordering > { self . to_array () . partial_cmp (other . as_ref ()) } }}}
mkitem!{mkimpl!{impl < T , const N : usize > Eq for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement + Eq , { }}}
mkitem!{mkimpl!{# [doc = " Lexicographic order. For the SIMD elementwise minimum and maximum, use simd_min and simd_max instead."] impl < T , const N : usize > Ord for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement + Ord , { # [inline] fn cmp (& self , other : & Self) -> core :: cmp :: Ordering { self . to_array () . cmp (other . as_ref ()) } }}}
mkitem!{mkimpl!{impl < T , const N : usize > core :: hash :: Hash for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement + core :: hash :: Hash , { # [inline] fn hash < H > (& self , state : & mut H) where H : core :: hash :: Hasher , { self . as_array () . hash (state) } }}}
mkitem!{mkimpl!{impl < T , const N : usize > AsRef < [T ; N] > for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement , { # [inline] fn as_ref (& self) -> & [T ; N] { self . as_array () } }}}
mkitem!{mkimpl!{impl < T , const N : usize > AsMut < [T ; N] > for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement , { # [inline] fn as_mut (& mut self) -> & mut [T ; N] { self . as_mut_array () } }}}
mkitem!{mkimpl!{impl < T , const N : usize > AsRef < [T] > for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement , { # [inline] fn as_ref (& self) -> & [T] { self . as_array () } }}}
mkitem!{mkimpl!{impl < T , const N : usize > AsMut < [T] > for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement , { # [inline] fn as_mut (& mut self) -> & mut [T] { self . as_mut_array () } }}}
mkitem!{mkimpl!{impl < T , const N : usize > From < [T ; N] > for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement , { # [inline] fn from (array : [T ; N]) -> Self { Self :: from_array (array) } }}}
mkitem!{mkimpl!{impl < T , const N : usize > From < Simd < T , N > > for [T ; N] where LaneCount < N > : SupportedLaneCount , T : SimdElement , { # [inline] fn from (vector : Simd < T , N >) -> Self { vector . to_array () } }}}
mkitem!{mkimpl!{impl < T , const N : usize > TryFrom < & [T] > for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement , { type Error = core :: array :: TryFromSliceError ; # [inline] fn try_from (slice : & [T]) -> Result < Self , core :: array :: TryFromSliceError > { Ok (Self :: from_array (slice . try_into () ?)) } }}}
mkitem!{mkimpl!{impl < T , const N : usize > TryFrom < & mut [T] > for Simd < T , N > where LaneCount < N > : SupportedLaneCount , T : SimdElement , { type Error = core :: array :: TryFromSliceError ; # [inline] fn try_from (slice : & mut [T]) -> Result < Self , core :: array :: TryFromSliceError > { Ok (Self :: from_array (slice . try_into () ?)) } }}}
mkmod!{sealed, { 
                getname!(sealed);
                getsrc!(sealed);
                getpath!(sealed);
                get_deps!(sealed);
                get_crates!(sealed);
                mkinclude!(sealed);
                mkitem!{mktrait!{pub trait Sealed { }}} 
            }}
mkuse!{use sealed :: Sealed ;}
mkitem!{mktrait!{# [doc = " Marker trait for types that may be used as SIMD vector elements."] # [doc = ""] # [doc = " # Safety"] # [doc = " This trait, when implemented, asserts the compiler can monomorphize"] # [doc = " `#[repr(simd)]` structs with the marked type as an element."] # [doc = " Strictly, it is valid to impl if the vector will not be miscompiled."] # [doc = " Practically, it is user-unfriendly to impl it if the vector won't compile,"] # [doc = " even when no soundness guarantees are broken by allowing the user to try."] pub unsafe trait SimdElement : Sealed + Copy { # [doc = " The mask element type corresponding to this element type."] type Mask : MaskElement ; }}}
mkitem!{mkimpl!{impl Sealed for u8 { }}}
mkitem!{mkimpl!{unsafe impl SimdElement for u8 { type Mask = i8 ; }}}
mkitem!{mkimpl!{impl Sealed for u16 { }}}
mkitem!{mkimpl!{unsafe impl SimdElement for u16 { type Mask = i16 ; }}}
mkitem!{mkimpl!{impl Sealed for u32 { }}}
mkitem!{mkimpl!{unsafe impl SimdElement for u32 { type Mask = i32 ; }}}
mkitem!{mkimpl!{impl Sealed for u64 { }}}
mkitem!{mkimpl!{unsafe impl SimdElement for u64 { type Mask = i64 ; }}}
mkitem!{mkimpl!{impl Sealed for usize { }}}
mkitem!{mkimpl!{unsafe impl SimdElement for usize { type Mask = isize ; }}}
mkitem!{mkimpl!{impl Sealed for i8 { }}}
mkitem!{mkimpl!{unsafe impl SimdElement for i8 { type Mask = i8 ; }}}
mkitem!{mkimpl!{impl Sealed for i16 { }}}
mkitem!{mkimpl!{unsafe impl SimdElement for i16 { type Mask = i16 ; }}}
mkitem!{mkimpl!{impl Sealed for i32 { }}}
mkitem!{mkimpl!{unsafe impl SimdElement for i32 { type Mask = i32 ; }}}
mkitem!{mkimpl!{impl Sealed for i64 { }}}
mkitem!{mkimpl!{unsafe impl SimdElement for i64 { type Mask = i64 ; }}}
mkitem!{mkimpl!{impl Sealed for isize { }}}
mkitem!{mkimpl!{unsafe impl SimdElement for isize { type Mask = isize ; }}}
mkitem!{mkimpl!{impl Sealed for f32 { }}}
mkitem!{mkimpl!{unsafe impl SimdElement for f32 { type Mask = i32 ; }}}
mkitem!{mkimpl!{impl Sealed for f64 { }}}
mkitem!{mkimpl!{unsafe impl SimdElement for f64 { type Mask = i64 ; }}}
mkitem!{mkimpl!{impl < T > Sealed for * const T { }}}
mkitem!{mkimpl!{unsafe impl < T > SimdElement for * const T where T : core :: ptr :: Pointee < Metadata = () > , { type Mask = isize ; }}}
mkitem!{mkimpl!{impl < T > Sealed for * mut T { }}}
mkitem!{mkimpl!{unsafe impl < T > SimdElement for * mut T where T : core :: ptr :: Pointee < Metadata = () > , { type Mask = isize ; }}}

macro_rules! lane_indices_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function lane_indices in module {}", module_path!());
    };
}

mkfn!{
    lane_indices_introspect!();
    # [inline] fn lane_indices < const N : usize > () -> Simd < usize , N > where LaneCount < N > : SupportedLaneCount , { # ! [allow (clippy :: needless_range_loop)] let mut index = [0 ; N] ; for i in 0 .. N { index [i] = i ; } Simd :: from_array (index) }
}

macro_rules! mask_up_to_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function mask_up_to in module {}", module_path!());
    };
}

mkfn!{
    mask_up_to_introspect!();
    # [inline] fn mask_up_to < M , const N : usize > (len : usize) -> Mask < M , N > where LaneCount < N > : SupportedLaneCount , M : MaskElement , { let index = lane_indices :: < N > () ; let max_value : u64 = M :: max_unsigned () ; macro_rules ! case { ($ ty : ty) => { if N < <$ ty >:: MAX as usize && max_value as $ ty as u64 == max_value { return index . cast () . simd_lt (Simd :: splat (len . min (N) as $ ty)) . cast () ; } } ; } case ! (u8) ; case ! (u16) ; case ! (u32) ; case ! (u64) ; index . simd_lt (Simd :: splat (len)) . cast () }
}