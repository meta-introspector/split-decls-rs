mkuse!{# [stable (feature = "rust1" , since = "1.0.0")] # [allow (deprecated , deprecated_in_future)] pub use core :: f64 :: { DIGITS , EPSILON , INFINITY , MANTISSA_DIGITS , MAX , MAX_10_EXP , MAX_EXP , MIN , MIN_10_EXP , MIN_EXP , MIN_POSITIVE , NAN , NEG_INFINITY , RADIX , consts , } ;}
mkuse!{# [cfg (not (test))] use crate :: intrinsics ;}
mkuse!{# [cfg (not (test))] use crate :: sys :: cmath ;}
mkitem!{mkimpl!{# [cfg (not (test))] impl f64 { # [doc = " Returns the largest integer less than or equal to `self`."] # [doc = ""] # [doc = " This function always returns the precise result."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let f = 3.7_f64;"] # [doc = " let g = 3.0_f64;"] # [doc = " let h = -3.7_f64;"] # [doc = ""] # [doc = " assert_eq!(f.floor(), 3.0);"] # [doc = " assert_eq!(g.floor(), 3.0);"] # [doc = " assert_eq!(h.floor(), -4.0);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_float_round_methods" , since = "1.90.0")] # [inline] pub const fn floor (self) -> f64 { core :: f64 :: math :: floor (self) } # [doc = " Returns the smallest integer greater than or equal to `self`."] # [doc = ""] # [doc = " This function always returns the precise result."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let f = 3.01_f64;"] # [doc = " let g = 4.0_f64;"] # [doc = ""] # [doc = " assert_eq!(f.ceil(), 4.0);"] # [doc = " assert_eq!(g.ceil(), 4.0);"] # [doc = " ```"] # [doc (alias = "ceiling")] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_float_round_methods" , since = "1.90.0")] # [inline] pub const fn ceil (self) -> f64 { core :: f64 :: math :: ceil (self) } # [doc = " Returns the nearest integer to `self`. If a value is half-way between two"] # [doc = " integers, round away from `0.0`."] # [doc = ""] # [doc = " This function always returns the precise result."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let f = 3.3_f64;"] # [doc = " let g = -3.3_f64;"] # [doc = " let h = -3.7_f64;"] # [doc = " let i = 3.5_f64;"] # [doc = " let j = 4.5_f64;"] # [doc = ""] # [doc = " assert_eq!(f.round(), 3.0);"] # [doc = " assert_eq!(g.round(), -3.0);"] # [doc = " assert_eq!(h.round(), -4.0);"] # [doc = " assert_eq!(i.round(), 4.0);"] # [doc = " assert_eq!(j.round(), 5.0);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_float_round_methods" , since = "1.90.0")] # [inline] pub const fn round (self) -> f64 { core :: f64 :: math :: round (self) } # [doc = " Returns the nearest integer to a number. Rounds half-way cases to the number"] # [doc = " with an even least significant digit."] # [doc = ""] # [doc = " This function always returns the precise result."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let f = 3.3_f64;"] # [doc = " let g = -3.3_f64;"] # [doc = " let h = 3.5_f64;"] # [doc = " let i = 4.5_f64;"] # [doc = ""] # [doc = " assert_eq!(f.round_ties_even(), 3.0);"] # [doc = " assert_eq!(g.round_ties_even(), -3.0);"] # [doc = " assert_eq!(h.round_ties_even(), 4.0);"] # [doc = " assert_eq!(i.round_ties_even(), 4.0);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "round_ties_even" , since = "1.77.0")] # [rustc_const_stable (feature = "const_float_round_methods" , since = "1.90.0")] # [inline] pub const fn round_ties_even (self) -> f64 { core :: f64 :: math :: round_ties_even (self) } # [doc = " Returns the integer part of `self`."] # [doc = " This means that non-integer numbers are always truncated towards zero."] # [doc = ""] # [doc = " This function always returns the precise result."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let f = 3.7_f64;"] # [doc = " let g = 3.0_f64;"] # [doc = " let h = -3.7_f64;"] # [doc = ""] # [doc = " assert_eq!(f.trunc(), 3.0);"] # [doc = " assert_eq!(g.trunc(), 3.0);"] # [doc = " assert_eq!(h.trunc(), -3.0);"] # [doc = " ```"] # [doc (alias = "truncate")] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_float_round_methods" , since = "1.90.0")] # [inline] pub const fn trunc (self) -> f64 { core :: f64 :: math :: trunc (self) } # [doc = " Returns the fractional part of `self`."] # [doc = ""] # [doc = " This function always returns the precise result."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = 3.6_f64;"] # [doc = " let y = -3.6_f64;"] # [doc = " let abs_difference_x = (x.fract() - 0.6).abs();"] # [doc = " let abs_difference_y = (y.fract() - (-0.6)).abs();"] # [doc = ""] # [doc = " assert!(abs_difference_x < 1e-10);"] # [doc = " assert!(abs_difference_y < 1e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_float_round_methods" , since = "1.90.0")] # [inline] pub const fn fract (self) -> f64 { core :: f64 :: math :: fract (self) } # [doc = " Fused multiply-add. Computes `(self * a) + b` with only one rounding"] # [doc = " error, yielding a more accurate result than an unfused multiply-add."] # [doc = ""] # [doc = " Using `mul_add` *may* be more performant than an unfused multiply-add if"] # [doc = " the target architecture has a dedicated `fma` CPU instruction. However,"] # [doc = " this is not always true, and will be heavily dependant on designing"] # [doc = " algorithms with specific target hardware in mind."] # [doc = ""] # [doc = " # Precision"] # [doc = ""] # [doc = " The result of this operation is guaranteed to be the rounded"] # [doc = " infinite-precision result. It is specified by IEEE 754 as"] # [doc = " `fusedMultiplyAdd` and guaranteed not to change."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let m = 10.0_f64;"] # [doc = " let x = 4.0_f64;"] # [doc = " let b = 60.0_f64;"] # [doc = ""] # [doc = " assert_eq!(m.mul_add(x, b), 100.0);"] # [doc = " assert_eq!(m * x + b, 100.0);"] # [doc = ""] # [doc = " let one_plus_eps = 1.0_f64 + f64::EPSILON;"] # [doc = " let one_minus_eps = 1.0_f64 - f64::EPSILON;"] # [doc = " let minus_one = -1.0_f64;"] # [doc = ""] # [doc = " // The exact result (1 + eps) * (1 - eps) = 1 - eps * eps."] # [doc = " assert_eq!(one_plus_eps.mul_add(one_minus_eps, minus_one), -f64::EPSILON * f64::EPSILON);"] # [doc = " // Different rounding with the non-fused multiply and add."] # [doc = " assert_eq!(one_plus_eps * one_minus_eps + minus_one, 0.0);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [doc (alias = "fma" , alias = "fusedMultiplyAdd")] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn mul_add (self , a : f64 , b : f64) -> f64 { core :: f64 :: math :: mul_add (self , a , b) } # [doc = " Calculates Euclidean division, the matching method for `rem_euclid`."] # [doc = ""] # [doc = " This computes the integer `n` such that"] # [doc = " `self = n * rhs + self.rem_euclid(rhs)`."] # [doc = " In other words, the result is `self / rhs` rounded to the integer `n`"] # [doc = " such that `self >= n * rhs`."] # [doc = ""] # [doc = " # Precision"] # [doc = ""] # [doc = " The result of this operation is guaranteed to be the rounded"] # [doc = " infinite-precision result."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let a: f64 = 7.0;"] # [doc = " let b = 4.0;"] # [doc = " assert_eq!(a.div_euclid(b), 1.0); // 7.0 > 4.0 * 1.0"] # [doc = " assert_eq!((-a).div_euclid(b), -2.0); // -7.0 >= 4.0 * -2.0"] # [doc = " assert_eq!(a.div_euclid(-b), -1.0); // 7.0 >= -4.0 * -1.0"] # [doc = " assert_eq!((-a).div_euclid(-b), 2.0); // -7.0 >= -4.0 * 2.0"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [inline] # [stable (feature = "euclidean_division" , since = "1.38.0")] pub fn div_euclid (self , rhs : f64) -> f64 { core :: f64 :: math :: div_euclid (self , rhs) } # [doc = " Calculates the least nonnegative remainder of `self (mod rhs)`."] # [doc = ""] # [doc = " In particular, the return value `r` satisfies `0.0 <= r < rhs.abs()` in"] # [doc = " most cases. However, due to a floating point round-off error it can"] # [doc = " result in `r == rhs.abs()`, violating the mathematical definition, if"] # [doc = " `self` is much smaller than `rhs.abs()` in magnitude and `self < 0.0`."] # [doc = " This result is not an element of the function's codomain, but it is the"] # [doc = " closest floating point number in the real numbers and thus fulfills the"] # [doc = " property `self == self.div_euclid(rhs) * rhs + self.rem_euclid(rhs)`"] # [doc = " approximately."] # [doc = ""] # [doc = " # Precision"] # [doc = ""] # [doc = " The result of this operation is guaranteed to be the rounded"] # [doc = " infinite-precision result."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let a: f64 = 7.0;"] # [doc = " let b = 4.0;"] # [doc = " assert_eq!(a.rem_euclid(b), 3.0);"] # [doc = " assert_eq!((-a).rem_euclid(b), 1.0);"] # [doc = " assert_eq!(a.rem_euclid(-b), 3.0);"] # [doc = " assert_eq!((-a).rem_euclid(-b), 1.0);"] # [doc = " // limitation due to round-off error"] # [doc = " assert!((-f64::EPSILON).rem_euclid(3.0) != 0.0);"] # [doc = " ```"] # [doc (alias = "modulo" , alias = "mod")] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [inline] # [stable (feature = "euclidean_division" , since = "1.38.0")] pub fn rem_euclid (self , rhs : f64) -> f64 { core :: f64 :: math :: rem_euclid (self , rhs) } # [doc = " Raises a number to an integer power."] # [doc = ""] # [doc = " Using this function is generally faster than using `powf`."] # [doc = " It might have a different sequence of rounding operations than `powf`,"] # [doc = " so the results are not guaranteed to agree."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = 2.0_f64;"] # [doc = " let abs_difference = (x.powi(2) - (x * x)).abs();"] # [doc = " assert!(abs_difference <= 1e-14);"] # [doc = ""] # [doc = " assert_eq!(f64::powi(f64::NAN, 0), 1.0);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn powi (self , n : i32) -> f64 { core :: f64 :: math :: powi (self , n) } # [doc = " Raises a number to a floating point power."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = 2.0_f64;"] # [doc = " let abs_difference = (x.powf(2.0) - (x * x)).abs();"] # [doc = " assert!(abs_difference <= 1e-14);"] # [doc = ""] # [doc = " assert_eq!(f64::powf(1.0, f64::NAN), 1.0);"] # [doc = " assert_eq!(f64::powf(f64::NAN, 0.0), 1.0);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn powf (self , n : f64) -> f64 { unsafe { intrinsics :: powf64 (self , n) } } # [doc = " Returns the square root of a number."] # [doc = ""] # [doc = " Returns NaN if `self` is a negative number other than `-0.0`."] # [doc = ""] # [doc = " # Precision"] # [doc = ""] # [doc = " The result of this operation is guaranteed to be the rounded"] # [doc = " infinite-precision result. It is specified by IEEE 754 as `squareRoot`"] # [doc = " and guaranteed not to change."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let positive = 4.0_f64;"] # [doc = " let negative = -4.0_f64;"] # [doc = " let negative_zero = -0.0_f64;"] # [doc = ""] # [doc = " assert_eq!(positive.sqrt(), 2.0);"] # [doc = " assert!(negative.sqrt().is_nan());"] # [doc = " assert!(negative_zero.sqrt() == negative_zero);"] # [doc = " ```"] # [doc (alias = "squareRoot")] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn sqrt (self) -> f64 { core :: f64 :: math :: sqrt (self) } # [doc = " Returns `e^(self)`, (the exponential function)."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let one = 1.0_f64;"] # [doc = " // e^1"] # [doc = " let e = one.exp();"] # [doc = ""] # [doc = " // ln(e) - 1 == 0"] # [doc = " let abs_difference = (e.ln() - 1.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn exp (self) -> f64 { unsafe { intrinsics :: expf64 (self) } } # [doc = " Returns `2^(self)`."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let f = 2.0_f64;"] # [doc = ""] # [doc = " // 2^2 - 4 == 0"] # [doc = " let abs_difference = (f.exp2() - 4.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn exp2 (self) -> f64 { unsafe { intrinsics :: exp2f64 (self) } } # [doc = " Returns the natural logarithm of the number."] # [doc = ""] # [doc = " This returns NaN when the number is negative, and negative infinity when number is zero."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let one = 1.0_f64;"] # [doc = " // e^1"] # [doc = " let e = one.exp();"] # [doc = ""] # [doc = " // ln(e) - 1 == 0"] # [doc = " let abs_difference = (e.ln() - 1.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [doc = ""] # [doc = " Non-positive values:"] # [doc = " ```"] # [doc = " assert_eq!(0_f64.ln(), f64::NEG_INFINITY);"] # [doc = " assert!((-42_f64).ln().is_nan());"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn ln (self) -> f64 { unsafe { intrinsics :: logf64 (self) } } # [doc = " Returns the logarithm of the number with respect to an arbitrary base."] # [doc = ""] # [doc = " This returns NaN when the number is negative, and negative infinity when number is zero."] # [doc = ""] # [doc = " The result might not be correctly rounded owing to implementation details;"] # [doc = " `self.log2()` can produce more accurate results for base 2, and"] # [doc = " `self.log10()` can produce more accurate results for base 10."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let twenty_five = 25.0_f64;"] # [doc = ""] # [doc = " // log5(25) - 2 == 0"] # [doc = " let abs_difference = (twenty_five.log(5.0) - 2.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [doc = ""] # [doc = " Non-positive values:"] # [doc = " ```"] # [doc = " assert_eq!(0_f64.log(10.0), f64::NEG_INFINITY);"] # [doc = " assert!((-42_f64).log(10.0).is_nan());"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn log (self , base : f64) -> f64 { self . ln () / base . ln () } # [doc = " Returns the base 2 logarithm of the number."] # [doc = ""] # [doc = " This returns NaN when the number is negative, and negative infinity when number is zero."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let four = 4.0_f64;"] # [doc = ""] # [doc = " // log2(4) - 2 == 0"] # [doc = " let abs_difference = (four.log2() - 2.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [doc = ""] # [doc = " Non-positive values:"] # [doc = " ```"] # [doc = " assert_eq!(0_f64.log2(), f64::NEG_INFINITY);"] # [doc = " assert!((-42_f64).log2().is_nan());"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn log2 (self) -> f64 { unsafe { intrinsics :: log2f64 (self) } } # [doc = " Returns the base 10 logarithm of the number."] # [doc = ""] # [doc = " This returns NaN when the number is negative, and negative infinity when number is zero."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let hundred = 100.0_f64;"] # [doc = ""] # [doc = " // log10(100) - 2 == 0"] # [doc = " let abs_difference = (hundred.log10() - 2.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [doc = ""] # [doc = " Non-positive values:"] # [doc = " ```"] # [doc = " assert_eq!(0_f64.log10(), f64::NEG_INFINITY);"] # [doc = " assert!((-42_f64).log10().is_nan());"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn log10 (self) -> f64 { unsafe { intrinsics :: log10f64 (self) } } # [doc = " The positive difference of two numbers."] # [doc = ""] # [doc = " * If `self <= other`: `0.0`"] # [doc = " * Else: `self - other`"] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `fdim` from libc on Unix and"] # [doc = " Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = 3.0_f64;"] # [doc = " let y = -3.0_f64;"] # [doc = ""] # [doc = " let abs_difference_x = (x.abs_sub(1.0) - 2.0).abs();"] # [doc = " let abs_difference_y = (y.abs_sub(1.0) - 0.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference_x < 1e-10);"] # [doc = " assert!(abs_difference_y < 1e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] # [deprecated (since = "1.10.0" , note = "you probably meant `(self - other).abs()`: \
                this operation is `(self - other).max(0.0)` \
                except that `abs_sub` also propagates NaNs (also \
                known as `fdim` in C). If you truly need the positive \
                difference, consider using that expression or the C function \
                `fdim`, depending on how you wish to handle NaN (please consider \
                filing an issue describing your use-case too).")] pub fn abs_sub (self , other : f64) -> f64 { # [allow (deprecated)] core :: f64 :: math :: abs_sub (self , other) } # [doc = " Returns the cube root of a number."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `cbrt` from libc on Unix and"] # [doc = " Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = 8.0_f64;"] # [doc = ""] # [doc = " // x^(1/3) - 2 == 0"] # [doc = " let abs_difference = (x.cbrt() - 2.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn cbrt (self) -> f64 { core :: f64 :: math :: cbrt (self) } # [doc = " Compute the distance between the origin and a point (`x`, `y`) on the"] # [doc = " Euclidean plane. Equivalently, compute the length of the hypotenuse of a"] # [doc = " right-angle triangle with other sides having length `x.abs()` and"] # [doc = " `y.abs()`."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `hypot` from libc on Unix"] # [doc = " and Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = 2.0_f64;"] # [doc = " let y = 3.0_f64;"] # [doc = ""] # [doc = " // sqrt(x^2 + y^2)"] # [doc = " let abs_difference = (x.hypot(y) - (x.powi(2) + y.powi(2)).sqrt()).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn hypot (self , other : f64) -> f64 { cmath :: hypot (self , other) } # [doc = " Computes the sine of a number (in radians)."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = std::f64::consts::FRAC_PI_2;"] # [doc = ""] # [doc = " let abs_difference = (x.sin() - 1.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn sin (self) -> f64 { unsafe { intrinsics :: sinf64 (self) } } # [doc = " Computes the cosine of a number (in radians)."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = 2.0 * std::f64::consts::PI;"] # [doc = ""] # [doc = " let abs_difference = (x.cos() - 1.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn cos (self) -> f64 { unsafe { intrinsics :: cosf64 (self) } } # [doc = " Computes the tangent of a number (in radians)."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `tan` from libc on Unix and"] # [doc = " Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = std::f64::consts::FRAC_PI_4;"] # [doc = " let abs_difference = (x.tan() - 1.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-14);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn tan (self) -> f64 { cmath :: tan (self) } # [doc = " Computes the arcsine of a number. Return value is in radians in"] # [doc = " the range [-pi/2, pi/2] or NaN if the number is outside the range"] # [doc = " [-1, 1]."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `asin` from libc on Unix and"] # [doc = " Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let f = std::f64::consts::FRAC_PI_4;"] # [doc = ""] # [doc = " // asin(sin(pi/2))"] # [doc = " let abs_difference = (f.sin().asin() - f).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-14);"] # [doc = " ```"] # [doc (alias = "arcsin")] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn asin (self) -> f64 { cmath :: asin (self) } # [doc = " Computes the arccosine of a number. Return value is in radians in"] # [doc = " the range [0, pi] or NaN if the number is outside the range"] # [doc = " [-1, 1]."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `acos` from libc on Unix and"] # [doc = " Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let f = std::f64::consts::FRAC_PI_4;"] # [doc = ""] # [doc = " // acos(cos(pi/4))"] # [doc = " let abs_difference = (f.cos().acos() - std::f64::consts::FRAC_PI_4).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [doc (alias = "arccos")] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn acos (self) -> f64 { cmath :: acos (self) } # [doc = " Computes the arctangent of a number. Return value is in radians in the"] # [doc = " range [-pi/2, pi/2];"] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `atan` from libc on Unix and"] # [doc = " Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let f = 1.0_f64;"] # [doc = ""] # [doc = " // atan(tan(1))"] # [doc = " let abs_difference = (f.tan().atan() - 1.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [doc (alias = "arctan")] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn atan (self) -> f64 { cmath :: atan (self) } # [doc = " Computes the four quadrant arctangent of `self` (`y`) and `other` (`x`) in radians."] # [doc = ""] # [doc = " * `x = 0`, `y = 0`: `0`"] # [doc = " * `x >= 0`: `arctan(y/x)` -> `[-pi/2, pi/2]`"] # [doc = " * `y >= 0`: `arctan(y/x) + pi` -> `(pi/2, pi]`"] # [doc = " * `y < 0`: `arctan(y/x) - pi` -> `(-pi, -pi/2)`"] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `atan2` from libc on Unix"] # [doc = " and Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " // Positive angles measured counter-clockwise"] # [doc = " // from positive x axis"] # [doc = " // -pi/4 radians (45 deg clockwise)"] # [doc = " let x1 = 3.0_f64;"] # [doc = " let y1 = -3.0_f64;"] # [doc = ""] # [doc = " // 3pi/4 radians (135 deg counter-clockwise)"] # [doc = " let x2 = -3.0_f64;"] # [doc = " let y2 = 3.0_f64;"] # [doc = ""] # [doc = " let abs_difference_1 = (y1.atan2(x1) - (-std::f64::consts::FRAC_PI_4)).abs();"] # [doc = " let abs_difference_2 = (y2.atan2(x2) - (3.0 * std::f64::consts::FRAC_PI_4)).abs();"] # [doc = ""] # [doc = " assert!(abs_difference_1 < 1e-10);"] # [doc = " assert!(abs_difference_2 < 1e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn atan2 (self , other : f64) -> f64 { cmath :: atan2 (self , other) } # [doc = " Simultaneously computes the sine and cosine of the number, `x`. Returns"] # [doc = " `(sin(x), cos(x))`."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `(f64::sin(x),"] # [doc = " f64::cos(x))`. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = std::f64::consts::FRAC_PI_4;"] # [doc = " let f = x.sin_cos();"] # [doc = ""] # [doc = " let abs_difference_0 = (f.0 - x.sin()).abs();"] # [doc = " let abs_difference_1 = (f.1 - x.cos()).abs();"] # [doc = ""] # [doc = " assert!(abs_difference_0 < 1e-10);"] # [doc = " assert!(abs_difference_1 < 1e-10);"] # [doc = " ```"] # [doc (alias = "sincos")] # [rustc_allow_incoherent_impl] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn sin_cos (self) -> (f64 , f64) { (self . sin () , self . cos ()) } # [doc = " Returns `e^(self) - 1` in a way that is accurate even if the"] # [doc = " number is close to zero."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `expm1` from libc on Unix"] # [doc = " and Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = 1e-16_f64;"] # [doc = ""] # [doc = " // for very small x, e^x is approximately 1 + x + x^2 / 2"] # [doc = " let approx = x + x * x / 2.0;"] # [doc = " let abs_difference = (x.exp_m1() - approx).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-20);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn exp_m1 (self) -> f64 { cmath :: expm1 (self) } # [doc = " Returns `ln(1+n)` (natural logarithm) more accurately than if"] # [doc = " the operations were performed separately."] # [doc = ""] # [doc = " This returns NaN when `n < -1.0`, and negative infinity when `n == -1.0`."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `log1p` from libc on Unix"] # [doc = " and Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = 1e-16_f64;"] # [doc = ""] # [doc = " // for very small x, ln(1 + x) is approximately x - x^2 / 2"] # [doc = " let approx = x - x * x / 2.0;"] # [doc = " let abs_difference = (x.ln_1p() - approx).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-20);"] # [doc = " ```"] # [doc = ""] # [doc = " Out-of-range values:"] # [doc = " ```"] # [doc = " assert_eq!((-1.0_f64).ln_1p(), f64::NEG_INFINITY);"] # [doc = " assert!((-2.0_f64).ln_1p().is_nan());"] # [doc = " ```"] # [doc (alias = "log1p")] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn ln_1p (self) -> f64 { cmath :: log1p (self) } # [doc = " Hyperbolic sine function."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `sinh` from libc on Unix"] # [doc = " and Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let e = std::f64::consts::E;"] # [doc = " let x = 1.0_f64;"] # [doc = ""] # [doc = " let f = x.sinh();"] # [doc = " // Solving sinh() at 1 gives `(e^2-1)/(2e)`"] # [doc = " let g = ((e * e) - 1.0) / (2.0 * e);"] # [doc = " let abs_difference = (f - g).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn sinh (self) -> f64 { cmath :: sinh (self) } # [doc = " Hyperbolic cosine function."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `cosh` from libc on Unix"] # [doc = " and Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let e = std::f64::consts::E;"] # [doc = " let x = 1.0_f64;"] # [doc = " let f = x.cosh();"] # [doc = " // Solving cosh() at 1 gives this result"] # [doc = " let g = ((e * e) + 1.0) / (2.0 * e);"] # [doc = " let abs_difference = (f - g).abs();"] # [doc = ""] # [doc = " // Same result"] # [doc = " assert!(abs_difference < 1.0e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn cosh (self) -> f64 { cmath :: cosh (self) } # [doc = " Hyperbolic tangent function."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `tanh` from libc on Unix"] # [doc = " and Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let e = std::f64::consts::E;"] # [doc = " let x = 1.0_f64;"] # [doc = ""] # [doc = " let f = x.tanh();"] # [doc = " // Solving tanh() at 1 gives `(1 - e^(-2))/(1 + e^(-2))`"] # [doc = " let g = (1.0 - e.powi(-2)) / (1.0 + e.powi(-2));"] # [doc = " let abs_difference = (f - g).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1.0e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn tanh (self) -> f64 { cmath :: tanh (self) } # [doc = " Inverse hyperbolic sine function."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = 1.0_f64;"] # [doc = " let f = x.sinh().asinh();"] # [doc = ""] # [doc = " let abs_difference = (f - x).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1.0e-10);"] # [doc = " ```"] # [doc (alias = "arcsinh")] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn asinh (self) -> f64 { let ax = self . abs () ; let ix = 1.0 / ax ; (ax + (ax / (Self :: hypot (1.0 , ix) + ix))) . ln_1p () . copysign (self) } # [doc = " Inverse hyperbolic cosine function."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = 1.0_f64;"] # [doc = " let f = x.cosh().acosh();"] # [doc = ""] # [doc = " let abs_difference = (f - x).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1.0e-10);"] # [doc = " ```"] # [doc (alias = "arccosh")] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn acosh (self) -> f64 { if self < 1.0 { Self :: NAN } else { (self + ((self - 1.0) . sqrt () * (self + 1.0) . sqrt ())) . ln () } } # [doc = " Inverse hyperbolic tangent function."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let x = std::f64::consts::FRAC_PI_6;"] # [doc = " let f = x.tanh().atanh();"] # [doc = ""] # [doc = " let abs_difference = (f - x).abs();"] # [doc = ""] # [doc = " assert!(abs_difference < 1.0e-10);"] # [doc = " ```"] # [doc (alias = "arctanh")] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn atanh (self) -> f64 { 0.5 * ((2.0 * self) / (1.0 - self)) . ln_1p () } # [doc = " Gamma function."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `tgamma` from libc on Unix"] # [doc = " and Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(float_gamma)]"] # [doc = " let x = 5.0f64;"] # [doc = ""] # [doc = " let abs_difference = (x.gamma() - 24.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference <= 1e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [unstable (feature = "float_gamma" , issue = "99842")] # [inline] pub fn gamma (self) -> f64 { cmath :: tgamma (self) } # [doc = " Natural logarithm of the absolute value of the gamma function"] # [doc = ""] # [doc = " The integer part of the tuple indicates the sign of the gamma function."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform, Rust version, and"] # [doc = " can even differ within the same execution from one invocation to the next."] # [doc = " This function currently corresponds to the `lgamma_r` from libc on Unix"] # [doc = " and Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(float_gamma)]"] # [doc = " let x = 2.0f64;"] # [doc = ""] # [doc = " let abs_difference = (x.ln_gamma().0 - 0.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference <= f64::EPSILON);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [unstable (feature = "float_gamma" , issue = "99842")] # [inline] pub fn ln_gamma (self) -> (f64 , i32) { let mut signgamp : i32 = 0 ; let x = cmath :: lgamma_r (self , & mut signgamp) ; (x , signgamp) } # [doc = " Error function."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform,"] # [doc = " Rust version, and can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " This function currently corresponds to the `erf` from libc on Unix"] # [doc = " and Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(float_erf)]"] # [doc = " /// The error function relates what percent of a normal distribution lies"] # [doc = " /// within `x` standard deviations (scaled by `1/sqrt(2)`)."] # [doc = " fn within_standard_deviations(x: f64) -> f64 {"] # [doc = "     (x * std::f64::consts::FRAC_1_SQRT_2).erf() * 100.0"] # [doc = " }"] # [doc = ""] # [doc = " // 68% of a normal distribution is within one standard deviation"] # [doc = " assert!((within_standard_deviations(1.0) - 68.269).abs() < 0.01);"] # [doc = " // 95% of a normal distribution is within two standard deviations"] # [doc = " assert!((within_standard_deviations(2.0) - 95.450).abs() < 0.01);"] # [doc = " // 99.7% of a normal distribution is within three standard deviations"] # [doc = " assert!((within_standard_deviations(3.0) - 99.730).abs() < 0.01);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [unstable (feature = "float_erf" , issue = "136321")] # [inline] pub fn erf (self) -> f64 { cmath :: erf (self) } # [doc = " Complementary error function."] # [doc = ""] # [doc = " # Unspecified precision"] # [doc = ""] # [doc = " The precision of this function is non-deterministic. This means it varies by platform,"] # [doc = " Rust version, and can even differ within the same execution from one invocation to the next."] # [doc = ""] # [doc = " This function currently corresponds to the `erfc` from libc on Unix"] # [doc = " and Windows. Note that this might change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(float_erf)]"] # [doc = " let x: f64 = 0.123;"] # [doc = ""] # [doc = " let one = x.erf() + x.erfc();"] # [doc = " let abs_difference = (one - 1.0).abs();"] # [doc = ""] # [doc = " assert!(abs_difference <= 1e-10);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [must_use = "method returns a new number and does not mutate the original value"] # [unstable (feature = "float_erf" , issue = "136321")] # [inline] pub fn erfc (self) -> f64 { cmath :: erfc (self) } }}}