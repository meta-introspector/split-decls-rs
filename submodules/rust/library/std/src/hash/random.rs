mkuse!{# [allow (deprecated)] use super :: { BuildHasher , Hasher , SipHasher13 } ;}
mkuse!{use crate :: cell :: Cell ;}
mkuse!{use crate :: fmt ;}
mkuse!{use crate :: sys :: random :: hashmap_random_keys ;}
mkitem!{mkstruct!{# [doc = " `RandomState` is the default state for [`HashMap`] types."] # [doc = ""] # [doc = " A particular instance `RandomState` will create the same instances of"] # [doc = " [`Hasher`], but the hashers created by two different `RandomState`"] # [doc = " instances are unlikely to produce the same result for the same values."] # [doc = ""] # [doc = " [`HashMap`]: crate::collections::HashMap"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::HashMap;"] # [doc = " use std::hash::RandomState;"] # [doc = ""] # [doc = " let s = RandomState::new();"] # [doc = " let mut map = HashMap::with_hasher(s);"] # [doc = " map.insert(1, 2);"] # [doc = " ```"] # [stable (feature = "hashmap_build_hasher" , since = "1.7.0")] # [derive (Clone)] pub struct RandomState { k0 : u64 , k1 : u64 , }}}
mkitem!{mkimpl!{impl RandomState { # [doc = " Constructs a new `RandomState` that is initialized with random keys."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::hash::RandomState;"] # [doc = ""] # [doc = " let s = RandomState::new();"] # [doc = " ```"] # [inline] # [allow (deprecated)] # [must_use] # [stable (feature = "hashmap_build_hasher" , since = "1.7.0")] pub fn new () -> RandomState { thread_local ! (static KEYS : Cell < (u64 , u64) > = { Cell :: new (hashmap_random_keys ()) }) ; KEYS . with (| keys | { let (k0 , k1) = keys . get () ; keys . set ((k0 . wrapping_add (1) , k1)) ; RandomState { k0 , k1 } }) } }}}
mkitem!{mkimpl!{# [stable (feature = "hashmap_build_hasher" , since = "1.7.0")] impl BuildHasher for RandomState { type Hasher = DefaultHasher ; # [inline] # [allow (deprecated)] fn build_hasher (& self) -> DefaultHasher { DefaultHasher (SipHasher13 :: new_with_keys (self . k0 , self . k1)) } }}}
mkitem!{mkstruct!{# [doc = " The default [`Hasher`] used by [`RandomState`]."] # [doc = ""] # [doc = " The internal algorithm is not specified, and so it and its hashes should"] # [doc = " not be relied upon over releases."] # [allow (deprecated)] # [derive (Clone , Debug)] # [stable (feature = "hashmap_build_hasher" , since = "1.7.0")] pub struct DefaultHasher (SipHasher13) ;}}
mkitem!{mkimpl!{impl DefaultHasher { # [doc = " Creates a new `DefaultHasher`."] # [doc = ""] # [doc = " This hasher is not guaranteed to be the same as all other"] # [doc = " `DefaultHasher` instances, but is the same as all other `DefaultHasher`"] # [doc = " instances created through `new` or `default`."] # [stable (feature = "hashmap_default_hasher" , since = "1.13.0")] # [inline] # [allow (deprecated)] # [must_use] pub fn new () -> DefaultHasher { DefaultHasher (SipHasher13 :: new_with_keys (0 , 0)) } }}}
mkitem!{mkimpl!{# [stable (feature = "hashmap_default_hasher" , since = "1.13.0")] impl Default for DefaultHasher { # [doc = " Creates a new `DefaultHasher` using [`new`]."] # [doc = " See its documentation for more."] # [doc = ""] # [doc = " [`new`]: DefaultHasher::new"] # [inline] fn default () -> DefaultHasher { DefaultHasher :: new () } }}}
mkitem!{mkimpl!{# [stable (feature = "hashmap_default_hasher" , since = "1.13.0")] impl Hasher for DefaultHasher { # [inline] fn write (& mut self , msg : & [u8]) { self . 0 . write (msg) } # [inline] fn write_str (& mut self , s : & str) { self . 0 . write_str (s) ; } # [inline] fn finish (& self) -> u64 { self . 0 . finish () } }}}
mkitem!{mkimpl!{# [stable (feature = "hashmap_build_hasher" , since = "1.7.0")] impl Default for RandomState { # [doc = " Constructs a new `RandomState`."] # [inline] fn default () -> RandomState { RandomState :: new () } }}}
mkitem!{mkimpl!{# [stable (feature = "std_debug" , since = "1.16.0")] impl fmt :: Debug for RandomState { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("RandomState") . finish_non_exhaustive () } }}}