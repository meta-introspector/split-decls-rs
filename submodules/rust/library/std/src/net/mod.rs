# ! [doc = " Networking primitives for TCP/UDP communication."] # ! [doc = ""] # ! [doc = " This module provides networking functionality for the Transmission Control and User"] # ! [doc = " Datagram Protocols, as well as types for IP and socket addresses."] # ! [doc = ""] # ! [doc = " # Organization"] # ! [doc = ""] # ! [doc = " * [`TcpListener`] and [`TcpStream`] provide functionality for communication over TCP"] # ! [doc = " * [`UdpSocket`] provides functionality for communication over UDP"] # ! [doc = " * [`IpAddr`] represents IP addresses of either IPv4 or IPv6; [`Ipv4Addr`] and"] # ! [doc = "   [`Ipv6Addr`] are respectively IPv4 and IPv6 addresses"] # ! [doc = " * [`SocketAddr`] represents socket addresses of either IPv4 or IPv6; [`SocketAddrV4`]"] # ! [doc = "   and [`SocketAddrV6`] are respectively IPv4 and IPv6 socket addresses"] # ! [doc = " * [`ToSocketAddrs`] is a trait that is used for generic address resolution when interacting"] # ! [doc = "   with networking objects like [`TcpListener`], [`TcpStream`] or [`UdpSocket`]"] # ! [doc = " * Other types are return or parameter types for various methods in this module"] # ! [doc = ""] # ! [doc = " Rust disables inheritance of socket objects to child processes by default when possible.  For"] # ! [doc = " example, through the use of the `CLOEXEC` flag in UNIX systems or the `HANDLE_FLAG_INHERIT`"] # ! [doc = " flag on Windows."] # ! [stable (feature = "rust1" , since = "1.0.0")] use split_decls_genesis :: ourprelude :: * ; #[stable (feature = "rust1" , since = "1.0.0")] pub use core :: net :: AddrParseError ; #[stable (feature = "rust1" , since = "1.0.0")] pub use self :: ip_addr :: { IpAddr , Ipv4Addr , Ipv6Addr , Ipv6MulticastScope } ; #[stable (feature = "rust1" , since = "1.0.0")] pub use self :: socket_addr :: { SocketAddr , SocketAddrV4 , SocketAddrV6 , ToSocketAddrs } ; #[unstable (feature = "tcplistener_into_incoming" , issue = "88373")] pub use self :: tcp :: IntoIncoming ; #[stable (feature = "rust1" , since = "1.0.0")] pub use self :: tcp :: { Incoming , TcpListener , TcpStream } ; #[stable (feature = "rust1" , since = "1.0.0")] pub use self :: udp :: UdpSocket ; mod ip_addr ; mod socket_addr ; mod tcp ; #[cfg (test)] pub (crate) mod test ; mod udp ; #[doc = " Possible values which can be passed to the [`TcpStream::shutdown`] method."] #[derive (Copy , Clone , PartialEq , Eq , Debug)] #[stable (feature = "rust1" , since = "1.0.0")] pub enum Shutdown { #[doc = " The reading portion of the [`TcpStream`] should be shut down."] #[doc = ""] #[doc = " All currently blocked and future [reads] will return <code>[Ok]\\(0)</code>."] #[doc = ""] #[doc = " [reads]: crate::io::Read \"io::Read\""] #[stable (feature = "rust1" , since = "1.0.0")] Read , #[doc = " The writing portion of the [`TcpStream`] should be shut down."] #[doc = ""] #[doc = " All currently blocked and future [writes] will return an error."] #[doc = ""] #[doc = " [writes]: crate::io::Write \"io::Write\""] #[stable (feature = "rust1" , since = "1.0.0")] Write , #[doc = " Both the reading and the writing portions of the [`TcpStream`] should be shut down."] #[doc = ""] #[doc = " See [`Shutdown::Read`] and [`Shutdown::Write`] for more information."] #[stable (feature = "rust1" , since = "1.0.0")] Both , }