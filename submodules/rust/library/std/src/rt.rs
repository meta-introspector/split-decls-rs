# ! [doc = " Runtime services"] # ! [doc = ""] # ! [doc = " The `rt` module provides a narrow set of runtime services,"] # ! [doc = " including the global heap (exported in `heap`) and unwinding and"] # ! [doc = " backtrace support. The APIs in this module are highly unstable,"] # ! [doc = " and should be considered as private implementation details for the"] # ! [doc = " time being."] # ! [unstable (feature = "rt" , reason = "this public module should not exist and is highly likely \
              to disappear" , issue = "none")] # ! [doc (hidden)] # ! [deny (unsafe_op_in_unsafe_fn)] # ! [allow (unused_macros)] use split_decls_genesis :: ourprelude :: * ; #[rustfmt :: skip] pub use crate :: panicking :: { begin_panic , panic_count } ; pub use core :: panicking :: { panic_display , panic_fmt } ; #[rustfmt :: skip] use crate :: any :: Any ; use crate :: sync :: Once ; use crate :: thread :: { self , main_thread } ; use crate :: { mem , panic , sys } ; #[cfg (not (test))] #[rustc_std_internal_symbol] fn __rust_abort () { crate :: process :: abort () ; } macro_rules ! rtprintpanic { ($ ($ t : tt) *) => { #[cfg (not (feature = "panic_immediate_abort"))] if let Some (mut out) = crate :: sys :: stdio :: panic_output () { let _ = crate :: io :: Write :: write_fmt (& mut out , format_args ! ($ ($ t) *)) ; } #[cfg (feature = "panic_immediate_abort")] { let _ = format_args ! ($ ($ t) *) ; } } } macro_rules ! rtabort { ($ ($ t : tt) *) => { { rtprintpanic ! ("fatal runtime error: {}, aborting\n" , format_args ! ($ ($ t) *)) ; crate :: process :: abort () ; } } } macro_rules ! rtassert { ($ e : expr) => { if !$ e { rtabort ! (concat ! ("assertion failed: " , stringify ! ($ e))) ; } } ; } macro_rules ! rtunwrap { ($ ok : ident , $ e : expr) => { match $ e { $ ok (v) => v , ref err => { let err = err . as_ref () . map (drop) ; rtabort ! (concat ! ("unwrap failed: " , stringify ! ($ e) , " = {:?}") , err) } } } ; } fn handle_rt_panic < T > (e : Box < dyn Any + Send >) -> T { mem :: forget (e) ; rtabort ! ("initialization or cleanup bug") ; } #[cfg_attr (test , allow (dead_code))] unsafe fn init (argc : isize , argv : * const * const u8 , sigpipe : u8) { #[cfg_attr (target_os = "teeos" , allow (unused_unsafe))] unsafe { sys :: init (argc , argv , sigpipe) } ; unsafe { main_thread :: set (thread :: current_id ()) } ; } #[doc = " Clean up the thread-local runtime state. This *should* be run after all other"] #[doc = " code managed by the Rust runtime, but will not cause UB if that condition is"] #[doc = " not fulfilled. Also note that this function is not guaranteed to be run, but"] #[doc = " skipping it will cause leaks and therefore is to be avoided."] pub (crate) fn thread_cleanup () { panic :: catch_unwind (| | { crate :: thread :: drop_current () ; }) . unwrap_or_else (handle_rt_panic) ; } pub (crate) fn cleanup () { static CLEANUP : Once = Once :: new () ; CLEANUP . call_once (| | unsafe { crate :: io :: cleanup () ; sys :: cleanup () ; }) ; } #[cfg (not (test))] fn lang_start_internal (main : & (dyn Fn () -> i32 + Sync + crate :: panic :: RefUnwindSafe) , argc : isize , argv : * const * const u8 , sigpipe : u8 ,) -> isize { panic :: catch_unwind (move | | { unsafe { init (argc , argv , sigpipe) } ; let ret_code = panic :: catch_unwind (main) . unwrap_or_else (move | payload | { let payload = panic :: AssertUnwindSafe (payload) ; panic :: catch_unwind (move | | drop ({ payload } . 0)) . unwrap_or_else (move | e | { mem :: forget (e) ; rtabort ! ("drop of the panic payload panicked") ; }) ; 101 }) ; let ret_code = ret_code as isize ; cleanup () ; crate :: sys :: exit_guard :: unique_thread_exit () ; ret_code }) . unwrap_or_else (handle_rt_panic) } #[cfg (not (any (test , doctest)))] #[lang = "start"] fn lang_start < T : crate :: process :: Termination + 'static > (main : fn () -> T , argc : isize , argv : * const * const u8 , sigpipe : u8 ,) -> isize { lang_start_internal (& move | | crate :: sys :: backtrace :: __rust_begin_short_backtrace (main) . report () . to_i32 () , argc , argv , sigpipe ,) }