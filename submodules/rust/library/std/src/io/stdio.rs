mkmod!{tests, { 
                getname!(tests);
                getsrc!(tests);
                getpath!(tests);
                get_deps!(tests);
                get_crates!(tests);
                mkinclude!(tests);
                 
            }}
mkuse!{use crate :: cell :: { Cell , RefCell } ;}
mkuse!{use crate :: fmt ;}
mkuse!{use crate :: fs :: File ;}
mkuse!{use crate :: io :: prelude :: * ;}
mkuse!{use crate :: io :: { self , BorrowedCursor , BufReader , IoSlice , IoSliceMut , LineWriter , Lines , SpecReadByte , } ;}
mkuse!{use crate :: panic :: { RefUnwindSafe , UnwindSafe } ;}
mkuse!{use crate :: sync :: atomic :: { Atomic , AtomicBool , Ordering } ;}
mkuse!{use crate :: sync :: { Arc , Mutex , MutexGuard , OnceLock , ReentrantLock , ReentrantLockGuard } ;}
mkuse!{use crate :: sys :: stdio ;}
mkuse!{use crate :: thread :: AccessError ;}
mkitem!{type LocalStream = Arc < Mutex < Vec < u8 > > > ;}
mkitem!{thread_local ! { # [doc = " Used by the test crate to capture the output of the print macros and panics."] static OUTPUT_CAPTURE : Cell < Option < LocalStream >> = const { Cell :: new (None) } }}
mkitem!{# [doc = " Flag to indicate OUTPUT_CAPTURE is used."] # [doc = ""] # [doc = " If it is None and was never set on any thread, this flag is set to false,"] # [doc = " and OUTPUT_CAPTURE can be safely ignored on all threads, saving some time"] # [doc = " and memory registering an unused thread local."] # [doc = ""] # [doc = " Note about memory ordering: This contains information about whether a"] # [doc = " thread local variable might be in use. Although this is a global flag, the"] # [doc = " memory ordering between threads does not matter: we only want this flag to"] # [doc = " have a consistent order between set_output_capture and print_to *within"] # [doc = " the same thread*. Within the same thread, things always have a perfectly"] # [doc = " consistent order. So Ordering::Relaxed is fine."] static OUTPUT_CAPTURE_USED : Atomic < bool > = AtomicBool :: new (false) ;}
mkitem!{mkstruct!{# [doc = " A handle to a raw instance of the standard input stream of this process."] # [doc = ""] # [doc = " This handle is not synchronized or buffered in any fashion. Constructed via"] # [doc = " the `std::io::stdio::stdin_raw` function."] struct StdinRaw (stdio :: Stdin) ;}}
mkitem!{mkstruct!{# [doc = " A handle to a raw instance of the standard output stream of this process."] # [doc = ""] # [doc = " This handle is not synchronized or buffered in any fashion. Constructed via"] # [doc = " the `std::io::stdio::stdout_raw` function."] struct StdoutRaw (stdio :: Stdout) ;}}
mkitem!{mkstruct!{# [doc = " A handle to a raw instance of the standard output stream of this process."] # [doc = ""] # [doc = " This handle is not synchronized or buffered in any fashion. Constructed via"] # [doc = " the `std::io::stdio::stderr_raw` function."] struct StderrRaw (stdio :: Stderr) ;}}

macro_rules! stdin_raw_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function stdin_raw in module {}", module_path!());
    };
}

mkfn!{
    stdin_raw_introspect!();
    # [doc = " Constructs a new raw handle to the standard input of this process."] # [doc = ""] # [doc = " The returned handle does not interact with any other handles created nor"] # [doc = " handles returned by `std::io::stdin`. Data buffered by the `std::io::stdin`"] # [doc = " handles is **not** available to raw handles returned from this function."] # [doc = ""] # [doc = " The returned handle has no external synchronization or buffering."] # [unstable (feature = "libstd_sys_internals" , issue = "none")] const fn stdin_raw () -> StdinRaw { StdinRaw (stdio :: Stdin :: new ()) }
}

macro_rules! stdout_raw_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function stdout_raw in module {}", module_path!());
    };
}

mkfn!{
    stdout_raw_introspect!();
    # [doc = " Constructs a new raw handle to the standard output stream of this process."] # [doc = ""] # [doc = " The returned handle does not interact with any other handles created nor"] # [doc = " handles returned by `std::io::stdout`. Note that data is buffered by the"] # [doc = " `std::io::stdout` handles so writes which happen via this raw handle may"] # [doc = " appear before previous writes."] # [doc = ""] # [doc = " The returned handle has no external synchronization or buffering layered on"] # [doc = " top."] # [unstable (feature = "libstd_sys_internals" , issue = "none")] const fn stdout_raw () -> StdoutRaw { StdoutRaw (stdio :: Stdout :: new ()) }
}

macro_rules! stderr_raw_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function stderr_raw in module {}", module_path!());
    };
}

mkfn!{
    stderr_raw_introspect!();
    # [doc = " Constructs a new raw handle to the standard error stream of this process."] # [doc = ""] # [doc = " The returned handle does not interact with any other handles created nor"] # [doc = " handles returned by `std::io::stderr`."] # [doc = ""] # [doc = " The returned handle has no external synchronization or buffering layered on"] # [doc = " top."] # [unstable (feature = "libstd_sys_internals" , issue = "none")] const fn stderr_raw () -> StderrRaw { StderrRaw (stdio :: Stderr :: new ()) }
}
mkitem!{mkimpl!{impl Read for StdinRaw { fn read (& mut self , buf : & mut [u8]) -> io :: Result < usize > { handle_ebadf (self . 0 . read (buf) , | | Ok (0)) } fn read_buf (& mut self , buf : BorrowedCursor < '_ >) -> io :: Result < () > { handle_ebadf (self . 0 . read_buf (buf) , | | Ok (())) } fn read_vectored (& mut self , bufs : & mut [IoSliceMut < '_ >]) -> io :: Result < usize > { handle_ebadf (self . 0 . read_vectored (bufs) , | | Ok (0)) } # [inline] fn is_read_vectored (& self) -> bool { self . 0 . is_read_vectored () } fn read_exact (& mut self , buf : & mut [u8]) -> io :: Result < () > { if buf . is_empty () { return Ok (()) ; } handle_ebadf (self . 0 . read_exact (buf) , | | Err (io :: Error :: READ_EXACT_EOF)) } fn read_buf_exact (& mut self , buf : BorrowedCursor < '_ >) -> io :: Result < () > { if buf . capacity () == 0 { return Ok (()) ; } handle_ebadf (self . 0 . read_buf_exact (buf) , | | Err (io :: Error :: READ_EXACT_EOF)) } fn read_to_end (& mut self , buf : & mut Vec < u8 >) -> io :: Result < usize > { handle_ebadf (self . 0 . read_to_end (buf) , | | Ok (0)) } fn read_to_string (& mut self , buf : & mut String) -> io :: Result < usize > { handle_ebadf (self . 0 . read_to_string (buf) , | | Ok (0)) } }}}
mkitem!{mkimpl!{impl Write for StdoutRaw { fn write (& mut self , buf : & [u8]) -> io :: Result < usize > { handle_ebadf (self . 0 . write (buf) , | | Ok (buf . len ())) } fn write_vectored (& mut self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { let total = | | Ok (bufs . iter () . map (| b | b . len ()) . sum ()) ; handle_ebadf (self . 0 . write_vectored (bufs) , total) } # [inline] fn is_write_vectored (& self) -> bool { self . 0 . is_write_vectored () } fn flush (& mut self) -> io :: Result < () > { handle_ebadf (self . 0 . flush () , | | Ok (())) } fn write_all (& mut self , buf : & [u8]) -> io :: Result < () > { handle_ebadf (self . 0 . write_all (buf) , | | Ok (())) } fn write_all_vectored (& mut self , bufs : & mut [IoSlice < '_ >]) -> io :: Result < () > { handle_ebadf (self . 0 . write_all_vectored (bufs) , | | Ok (())) } fn write_fmt (& mut self , fmt : fmt :: Arguments < '_ >) -> io :: Result < () > { handle_ebadf (self . 0 . write_fmt (fmt) , | | Ok (())) } }}}
mkitem!{mkimpl!{impl Write for StderrRaw { fn write (& mut self , buf : & [u8]) -> io :: Result < usize > { handle_ebadf (self . 0 . write (buf) , | | Ok (buf . len ())) } fn write_vectored (& mut self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { let total = | | Ok (bufs . iter () . map (| b | b . len ()) . sum ()) ; handle_ebadf (self . 0 . write_vectored (bufs) , total) } # [inline] fn is_write_vectored (& self) -> bool { self . 0 . is_write_vectored () } fn flush (& mut self) -> io :: Result < () > { handle_ebadf (self . 0 . flush () , | | Ok (())) } fn write_all (& mut self , buf : & [u8]) -> io :: Result < () > { handle_ebadf (self . 0 . write_all (buf) , | | Ok (())) } fn write_all_vectored (& mut self , bufs : & mut [IoSlice < '_ >]) -> io :: Result < () > { handle_ebadf (self . 0 . write_all_vectored (bufs) , | | Ok (())) } fn write_fmt (& mut self , fmt : fmt :: Arguments < '_ >) -> io :: Result < () > { handle_ebadf (self . 0 . write_fmt (fmt) , | | Ok (())) } }}}

macro_rules! handle_ebadf_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function handle_ebadf in module {}", module_path!());
    };
}

mkfn!{
    handle_ebadf_introspect!();
    fn handle_ebadf < T > (r : io :: Result < T > , default : impl FnOnce () -> io :: Result < T >) -> io :: Result < T > { match r { Err (ref e) if stdio :: is_ebadf (e) => default () , r => r , } }
}
mkitem!{mkstruct!{# [doc = " A handle to the standard input stream of a process."] # [doc = ""] # [doc = " Each handle is a shared reference to a global buffer of input data to this"] # [doc = " process. A handle can be `lock`'d to gain full access to [`BufRead`] methods"] # [doc = " (e.g., `.lines()`). Reads to this handle are otherwise locked with respect"] # [doc = " to other reads."] # [doc = ""] # [doc = " This handle implements the `Read` trait, but beware that concurrent reads"] # [doc = " of `Stdin` must be executed with care."] # [doc = ""] # [doc = " Created by the [`io::stdin`] method."] # [doc = ""] # [doc = " [`io::stdin`]: stdin"] # [doc = ""] # [doc = " ### Note: Windows Portability Considerations"] # [doc = ""] # [doc = " When operating in a console, the Windows implementation of this stream does not support"] # [doc = " non-UTF-8 byte sequences. Attempting to read bytes that are not valid UTF-8 will return"] # [doc = " an error."] # [doc = ""] # [doc = " In a process with a detached console, such as one using"] # [doc = " `#![windows_subsystem = \"windows\"]`, or in a child process spawned from such a process,"] # [doc = " the contained handle will be null. In such cases, the standard library's `Read` and"] # [doc = " `Write` will do nothing and silently succeed. All other I/O operations, via the"] # [doc = " standard library or via raw Windows API calls, will fail."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io;"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut buffer = String::new();"] # [doc = "     let stdin = io::stdin(); // We get `Stdin` here."] # [doc = "     stdin.read_line(&mut buffer)?;"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [cfg_attr (not (test) , rustc_diagnostic_item = "Stdin")] pub struct Stdin { inner : & 'static Mutex < BufReader < StdinRaw > > , }}}
mkitem!{mkstruct!{# [doc = " A locked reference to the [`Stdin`] handle."] # [doc = ""] # [doc = " This handle implements both the [`Read`] and [`BufRead`] traits, and"] # [doc = " is constructed via the [`Stdin::lock`] method."] # [doc = ""] # [doc = " ### Note: Windows Portability Considerations"] # [doc = ""] # [doc = " When operating in a console, the Windows implementation of this stream does not support"] # [doc = " non-UTF-8 byte sequences. Attempting to read bytes that are not valid UTF-8 will return"] # [doc = " an error."] # [doc = ""] # [doc = " In a process with a detached console, such as one using"] # [doc = " `#![windows_subsystem = \"windows\"]`, or in a child process spawned from such a process,"] # [doc = " the contained handle will be null. In such cases, the standard library's `Read` and"] # [doc = " `Write` will do nothing and silently succeed. All other I/O operations, via the"] # [doc = " standard library or via raw Windows API calls, will fail."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::{self, BufRead};"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut buffer = String::new();"] # [doc = "     let stdin = io::stdin(); // We get `Stdin` here."] # [doc = "     {"] # [doc = "         let mut handle = stdin.lock(); // We get `StdinLock` here."] # [doc = "         handle.read_line(&mut buffer)?;"] # [doc = "     } // `StdinLock` is dropped here."] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [must_use = "if unused stdin will immediately unlock"] # [stable (feature = "rust1" , since = "1.0.0")] pub struct StdinLock < 'a > { inner : MutexGuard < 'a , BufReader < StdinRaw > > , }}}

macro_rules! stdin_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function stdin in module {}", module_path!());
    };
}

mkfn!{
    stdin_introspect!();
    # [doc = " Constructs a new handle to the standard input of the current process."] # [doc = ""] # [doc = " Each handle returned is a reference to a shared global buffer whose access"] # [doc = " is synchronized via a mutex. If you need more explicit control over"] # [doc = " locking, see the [`Stdin::lock`] method."] # [doc = ""] # [doc = " ### Note: Windows Portability Considerations"] # [doc = ""] # [doc = " When operating in a console, the Windows implementation of this stream does not support"] # [doc = " non-UTF-8 byte sequences. Attempting to read bytes that are not valid UTF-8 will return"] # [doc = " an error."] # [doc = ""] # [doc = " In a process with a detached console, such as one using"] # [doc = " `#![windows_subsystem = \"windows\"]`, or in a child process spawned from such a process,"] # [doc = " the contained handle will be null. In such cases, the standard library's `Read` and"] # [doc = " `Write` will do nothing and silently succeed. All other I/O operations, via the"] # [doc = " standard library or via raw Windows API calls, will fail."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Using implicit synchronization:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io;"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut buffer = String::new();"] # [doc = "     io::stdin().read_line(&mut buffer)?;"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Using explicit synchronization:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::{self, BufRead};"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut buffer = String::new();"] # [doc = "     let stdin = io::stdin();"] # [doc = "     let mut handle = stdin.lock();"] # [doc = ""] # [doc = "     handle.read_line(&mut buffer)?;"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [must_use] # [stable (feature = "rust1" , since = "1.0.0")] pub fn stdin () -> Stdin { static INSTANCE : OnceLock < Mutex < BufReader < StdinRaw > > > = OnceLock :: new () ; Stdin { inner : INSTANCE . get_or_init (| | { Mutex :: new (BufReader :: with_capacity (stdio :: STDIN_BUF_SIZE , stdin_raw ())) }) , } }
}
mkitem!{mkimpl!{impl Stdin { # [doc = " Locks this handle to the standard input stream, returning a readable"] # [doc = " guard."] # [doc = ""] # [doc = " The lock is released when the returned lock goes out of scope. The"] # [doc = " returned guard also implements the [`Read`] and [`BufRead`] traits for"] # [doc = " accessing the underlying data."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::{self, BufRead};"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut buffer = String::new();"] # [doc = "     let stdin = io::stdin();"] # [doc = "     let mut handle = stdin.lock();"] # [doc = ""] # [doc = "     handle.read_line(&mut buffer)?;"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn lock (& self) -> StdinLock < 'static > { StdinLock { inner : self . inner . lock () . unwrap_or_else (| e | e . into_inner ()) } } # [doc = " Locks this handle and reads a line of input, appending it to the specified buffer."] # [doc = ""] # [doc = " For detailed semantics of this method, see the documentation on"] # [doc = " [`BufRead::read_line`]. In particular:"] # [doc = " * Previous content of the buffer will be preserved. To avoid appending"] # [doc = "   to the buffer, you need to [`clear`] it first."] # [doc = " * The trailing newline character, if any, is included in the buffer."] # [doc = ""] # [doc = " [`clear`]: String::clear"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io;"] # [doc = ""] # [doc = " let mut input = String::new();"] # [doc = " match io::stdin().read_line(&mut input) {"] # [doc = "     Ok(n) => {"] # [doc = "         println!(\"{n} bytes read\");"] # [doc = "         println!(\"{input}\");"] # [doc = "     }"] # [doc = "     Err(error) => println!(\"error: {error}\"),"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " You can run the example one of two ways:"] # [doc = ""] # [doc = " - Pipe some text to it, e.g., `printf foo | path/to/executable`"] # [doc = " - Give it text interactively by running the executable directly,"] # [doc = "   in which case it will wait for the Enter key to be pressed before"] # [doc = "   continuing"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_confusables ("get_line")] pub fn read_line (& self , buf : & mut String) -> io :: Result < usize > { self . lock () . read_line (buf) } # [doc = " Consumes this handle and returns an iterator over input lines."] # [doc = ""] # [doc = " For detailed semantics of this method, see the documentation on"] # [doc = " [`BufRead::lines`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io;"] # [doc = ""] # [doc = " let lines = io::stdin().lines();"] # [doc = " for line in lines {"] # [doc = "     println!(\"got a line: {}\", line.unwrap());"] # [doc = " }"] # [doc = " ```"] # [must_use = "`self` will be dropped if the result is not used"] # [stable (feature = "stdin_forwarders" , since = "1.62.0")] pub fn lines (self) -> Lines < StdinLock < 'static > > { self . lock () . lines () } }}}
mkitem!{mkimpl!{# [stable (feature = "std_debug" , since = "1.16.0")] impl fmt :: Debug for Stdin { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("Stdin") . finish_non_exhaustive () } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl Read for Stdin { fn read (& mut self , buf : & mut [u8]) -> io :: Result < usize > { self . lock () . read (buf) } fn read_buf (& mut self , buf : BorrowedCursor < '_ >) -> io :: Result < () > { self . lock () . read_buf (buf) } fn read_vectored (& mut self , bufs : & mut [IoSliceMut < '_ >]) -> io :: Result < usize > { self . lock () . read_vectored (bufs) } # [inline] fn is_read_vectored (& self) -> bool { self . lock () . is_read_vectored () } fn read_to_end (& mut self , buf : & mut Vec < u8 >) -> io :: Result < usize > { self . lock () . read_to_end (buf) } fn read_to_string (& mut self , buf : & mut String) -> io :: Result < usize > { self . lock () . read_to_string (buf) } fn read_exact (& mut self , buf : & mut [u8]) -> io :: Result < () > { self . lock () . read_exact (buf) } fn read_buf_exact (& mut self , cursor : BorrowedCursor < '_ >) -> io :: Result < () > { self . lock () . read_buf_exact (cursor) } }}}
mkitem!{mkimpl!{# [stable (feature = "read_shared_stdin" , since = "1.78.0")] impl Read for & Stdin { fn read (& mut self , buf : & mut [u8]) -> io :: Result < usize > { self . lock () . read (buf) } fn read_buf (& mut self , buf : BorrowedCursor < '_ >) -> io :: Result < () > { self . lock () . read_buf (buf) } fn read_vectored (& mut self , bufs : & mut [IoSliceMut < '_ >]) -> io :: Result < usize > { self . lock () . read_vectored (bufs) } # [inline] fn is_read_vectored (& self) -> bool { self . lock () . is_read_vectored () } fn read_to_end (& mut self , buf : & mut Vec < u8 >) -> io :: Result < usize > { self . lock () . read_to_end (buf) } fn read_to_string (& mut self , buf : & mut String) -> io :: Result < usize > { self . lock () . read_to_string (buf) } fn read_exact (& mut self , buf : & mut [u8]) -> io :: Result < () > { self . lock () . read_exact (buf) } fn read_buf_exact (& mut self , cursor : BorrowedCursor < '_ >) -> io :: Result < () > { self . lock () . read_buf_exact (cursor) } }}}
mkitem!{mkimpl!{# [cfg (any (target_os = "linux" , target_os = "android"))] impl StdinLock < '_ > { pub (crate) fn as_mut_buf (& mut self) -> & mut BufReader < impl Read > { & mut self . inner } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl Read for StdinLock < '_ > { fn read (& mut self , buf : & mut [u8]) -> io :: Result < usize > { self . inner . read (buf) } fn read_buf (& mut self , buf : BorrowedCursor < '_ >) -> io :: Result < () > { self . inner . read_buf (buf) } fn read_vectored (& mut self , bufs : & mut [IoSliceMut < '_ >]) -> io :: Result < usize > { self . inner . read_vectored (bufs) } # [inline] fn is_read_vectored (& self) -> bool { self . inner . is_read_vectored () } fn read_to_end (& mut self , buf : & mut Vec < u8 >) -> io :: Result < usize > { self . inner . read_to_end (buf) } fn read_to_string (& mut self , buf : & mut String) -> io :: Result < usize > { self . inner . read_to_string (buf) } fn read_exact (& mut self , buf : & mut [u8]) -> io :: Result < () > { self . inner . read_exact (buf) } fn read_buf_exact (& mut self , cursor : BorrowedCursor < '_ >) -> io :: Result < () > { self . inner . read_buf_exact (cursor) } }}}
mkitem!{mkimpl!{impl SpecReadByte for StdinLock < '_ > { # [inline] fn spec_read_byte (& mut self) -> Option < io :: Result < u8 > > { BufReader :: spec_read_byte (& mut * self . inner) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl BufRead for StdinLock < '_ > { fn fill_buf (& mut self) -> io :: Result < & [u8] > { self . inner . fill_buf () } fn consume (& mut self , n : usize) { self . inner . consume (n) } fn read_until (& mut self , byte : u8 , buf : & mut Vec < u8 >) -> io :: Result < usize > { self . inner . read_until (byte , buf) } fn read_line (& mut self , buf : & mut String) -> io :: Result < usize > { self . inner . read_line (buf) } }}}
mkitem!{mkimpl!{# [stable (feature = "std_debug" , since = "1.16.0")] impl fmt :: Debug for StdinLock < '_ > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("StdinLock") . finish_non_exhaustive () } }}}
mkitem!{mkstruct!{# [doc = " A handle to the global standard output stream of the current process."] # [doc = ""] # [doc = " Each handle shares a global buffer of data to be written to the standard"] # [doc = " output stream. Access is also synchronized via a lock and explicit control"] # [doc = " over locking is available via the [`lock`] method."] # [doc = ""] # [doc = " By default, the handle is line-buffered when connected to a terminal, meaning"] # [doc = " it flushes automatically when a newline (`\\n`) is encountered. For immediate"] # [doc = " output, you can manually call the [`flush`] method. When the handle goes out"] # [doc = " of scope, the buffer is automatically flushed."] # [doc = ""] # [doc = " Created by the [`io::stdout`] method."] # [doc = ""] # [doc = " ### Note: Windows Portability Considerations"] # [doc = ""] # [doc = " When operating in a console, the Windows implementation of this stream does not support"] # [doc = " non-UTF-8 byte sequences. Attempting to write bytes that are not valid UTF-8 will return"] # [doc = " an error."] # [doc = ""] # [doc = " In a process with a detached console, such as one using"] # [doc = " `#![windows_subsystem = \"windows\"]`, or in a child process spawned from such a process,"] # [doc = " the contained handle will be null. In such cases, the standard library's `Read` and"] # [doc = " `Write` will do nothing and silently succeed. All other I/O operations, via the"] # [doc = " standard library or via raw Windows API calls, will fail."] # [doc = ""] # [doc = " [`lock`]: Stdout::lock"] # [doc = " [`flush`]: Write::flush"] # [doc = " [`io::stdout`]: stdout"] # [stable (feature = "rust1" , since = "1.0.0")] pub struct Stdout { inner : & 'static ReentrantLock < RefCell < LineWriter < StdoutRaw > > > , }}}
mkitem!{mkstruct!{# [doc = " A locked reference to the [`Stdout`] handle."] # [doc = ""] # [doc = " This handle implements the [`Write`] trait, and is constructed via"] # [doc = " the [`Stdout::lock`] method. See its documentation for more."] # [doc = ""] # [doc = " By default, the handle is line-buffered when connected to a terminal, meaning"] # [doc = " it flushes automatically when a newline (`\\n`) is encountered. For immediate"] # [doc = " output, you can manually call the [`flush`] method. When the handle goes out"] # [doc = " of scope, the buffer is automatically flushed."] # [doc = ""] # [doc = " ### Note: Windows Portability Considerations"] # [doc = ""] # [doc = " When operating in a console, the Windows implementation of this stream does not support"] # [doc = " non-UTF-8 byte sequences. Attempting to write bytes that are not valid UTF-8 will return"] # [doc = " an error."] # [doc = ""] # [doc = " In a process with a detached console, such as one using"] # [doc = " `#![windows_subsystem = \"windows\"]`, or in a child process spawned from such a process,"] # [doc = " the contained handle will be null. In such cases, the standard library's `Read` and"] # [doc = " `Write` will do nothing and silently succeed. All other I/O operations, via the"] # [doc = " standard library or via raw Windows API calls, will fail."] # [doc = ""] # [doc = " [`flush`]: Write::flush"] # [must_use = "if unused stdout will immediately unlock"] # [stable (feature = "rust1" , since = "1.0.0")] pub struct StdoutLock < 'a > { inner : ReentrantLockGuard < 'a , RefCell < LineWriter < StdoutRaw > > > , }}}
mkitem!{static STDOUT : OnceLock < ReentrantLock < RefCell < LineWriter < StdoutRaw > > > > = OnceLock :: new () ;}

macro_rules! stdout_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function stdout in module {}", module_path!());
    };
}

mkfn!{
    stdout_introspect!();
    # [doc = " Constructs a new handle to the standard output of the current process."] # [doc = ""] # [doc = " Each handle returned is a reference to a shared global buffer whose access"] # [doc = " is synchronized via a mutex. If you need more explicit control over"] # [doc = " locking, see the [`Stdout::lock`] method."] # [doc = ""] # [doc = " By default, the handle is line-buffered when connected to a terminal, meaning"] # [doc = " it flushes automatically when a newline (`\\n`) is encountered. For immediate"] # [doc = " output, you can manually call the [`flush`] method. When the handle goes out"] # [doc = " of scope, the buffer is automatically flushed."] # [doc = ""] # [doc = " ### Note: Windows Portability Considerations"] # [doc = ""] # [doc = " When operating in a console, the Windows implementation of this stream does not support"] # [doc = " non-UTF-8 byte sequences. Attempting to write bytes that are not valid UTF-8 will return"] # [doc = " an error."] # [doc = ""] # [doc = " In a process with a detached console, such as one using"] # [doc = " `#![windows_subsystem = \"windows\"]`, or in a child process spawned from such a process,"] # [doc = " the contained handle will be null. In such cases, the standard library's `Read` and"] # [doc = " `Write` will do nothing and silently succeed. All other I/O operations, via the"] # [doc = " standard library or via raw Windows API calls, will fail."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Using implicit synchronization:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::{self, Write};"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     io::stdout().write_all(b\"hello world\")?;"] # [doc = ""] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Using explicit synchronization:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::{self, Write};"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let stdout = io::stdout();"] # [doc = "     let mut handle = stdout.lock();"] # [doc = ""] # [doc = "     handle.write_all(b\"hello world\")?;"] # [doc = ""] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Ensuring output is flushed immediately:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::{self, Write};"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut stdout = io::stdout();"] # [doc = "     stdout.write_all(b\"hello, \")?;"] # [doc = "     stdout.flush()?;                // Manual flush"] # [doc = "     stdout.write_all(b\"world!\\n\")?; // Automatically flushed"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [`flush`]: Write::flush"] # [must_use] # [stable (feature = "rust1" , since = "1.0.0")] # [cfg_attr (not (test) , rustc_diagnostic_item = "io_stdout")] pub fn stdout () -> Stdout { Stdout { inner : STDOUT . get_or_init (| | ReentrantLock :: new (RefCell :: new (LineWriter :: new (stdout_raw ())))) , } }
}

macro_rules! cleanup_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function cleanup in module {}", module_path!());
    };
}

mkfn!{
    cleanup_introspect!();
    pub fn cleanup () { let mut initialized = false ; let stdout = STDOUT . get_or_init (| | { initialized = true ; ReentrantLock :: new (RefCell :: new (LineWriter :: with_capacity (0 , stdout_raw ()))) }) ; if ! initialized { if let Some (lock) = stdout . try_lock () { * lock . borrow_mut () = LineWriter :: with_capacity (0 , stdout_raw ()) ; } } }
}
mkitem!{mkimpl!{impl Stdout { # [doc = " Locks this handle to the standard output stream, returning a writable"] # [doc = " guard."] # [doc = ""] # [doc = " The lock is released when the returned lock goes out of scope. The"] # [doc = " returned guard also implements the `Write` trait for writing data."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::{self, Write};"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let mut stdout = io::stdout().lock();"] # [doc = ""] # [doc = "     stdout.write_all(b\"hello world\")?;"] # [doc = ""] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn lock (& self) -> StdoutLock < 'static > { StdoutLock { inner : self . inner . lock () } } }}}
mkitem!{mkimpl!{# [stable (feature = "catch_unwind" , since = "1.9.0")] impl UnwindSafe for Stdout { }}}
mkitem!{mkimpl!{# [stable (feature = "catch_unwind" , since = "1.9.0")] impl RefUnwindSafe for Stdout { }}}
mkitem!{mkimpl!{# [stable (feature = "std_debug" , since = "1.16.0")] impl fmt :: Debug for Stdout { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("Stdout") . finish_non_exhaustive () } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl Write for Stdout { fn write (& mut self , buf : & [u8]) -> io :: Result < usize > { (& * self) . write (buf) } fn write_vectored (& mut self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { (& * self) . write_vectored (bufs) } # [inline] fn is_write_vectored (& self) -> bool { io :: Write :: is_write_vectored (& & * self) } fn flush (& mut self) -> io :: Result < () > { (& * self) . flush () } fn write_all (& mut self , buf : & [u8]) -> io :: Result < () > { (& * self) . write_all (buf) } fn write_all_vectored (& mut self , bufs : & mut [IoSlice < '_ >]) -> io :: Result < () > { (& * self) . write_all_vectored (bufs) } fn write_fmt (& mut self , args : fmt :: Arguments < '_ >) -> io :: Result < () > { (& * self) . write_fmt (args) } }}}
mkitem!{mkimpl!{# [stable (feature = "write_mt" , since = "1.48.0")] impl Write for & Stdout { fn write (& mut self , buf : & [u8]) -> io :: Result < usize > { self . lock () . write (buf) } fn write_vectored (& mut self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { self . lock () . write_vectored (bufs) } # [inline] fn is_write_vectored (& self) -> bool { self . lock () . is_write_vectored () } fn flush (& mut self) -> io :: Result < () > { self . lock () . flush () } fn write_all (& mut self , buf : & [u8]) -> io :: Result < () > { self . lock () . write_all (buf) } fn write_all_vectored (& mut self , bufs : & mut [IoSlice < '_ >]) -> io :: Result < () > { self . lock () . write_all_vectored (bufs) } fn write_fmt (& mut self , args : fmt :: Arguments < '_ >) -> io :: Result < () > { self . lock () . write_fmt (args) } }}}
mkitem!{mkimpl!{# [stable (feature = "catch_unwind" , since = "1.9.0")] impl UnwindSafe for StdoutLock < '_ > { }}}
mkitem!{mkimpl!{# [stable (feature = "catch_unwind" , since = "1.9.0")] impl RefUnwindSafe for StdoutLock < '_ > { }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl Write for StdoutLock < '_ > { fn write (& mut self , buf : & [u8]) -> io :: Result < usize > { self . inner . borrow_mut () . write (buf) } fn write_vectored (& mut self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { self . inner . borrow_mut () . write_vectored (bufs) } # [inline] fn is_write_vectored (& self) -> bool { self . inner . borrow_mut () . is_write_vectored () } fn flush (& mut self) -> io :: Result < () > { self . inner . borrow_mut () . flush () } fn write_all (& mut self , buf : & [u8]) -> io :: Result < () > { self . inner . borrow_mut () . write_all (buf) } fn write_all_vectored (& mut self , bufs : & mut [IoSlice < '_ >]) -> io :: Result < () > { self . inner . borrow_mut () . write_all_vectored (bufs) } }}}
mkitem!{mkimpl!{# [stable (feature = "std_debug" , since = "1.16.0")] impl fmt :: Debug for StdoutLock < '_ > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("StdoutLock") . finish_non_exhaustive () } }}}
mkitem!{mkstruct!{# [doc = " A handle to the standard error stream of a process."] # [doc = ""] # [doc = " For more information, see the [`io::stderr`] method."] # [doc = ""] # [doc = " [`io::stderr`]: stderr"] # [doc = ""] # [doc = " ### Note: Windows Portability Considerations"] # [doc = ""] # [doc = " When operating in a console, the Windows implementation of this stream does not support"] # [doc = " non-UTF-8 byte sequences. Attempting to write bytes that are not valid UTF-8 will return"] # [doc = " an error."] # [doc = ""] # [doc = " In a process with a detached console, such as one using"] # [doc = " `#![windows_subsystem = \"windows\"]`, or in a child process spawned from such a process,"] # [doc = " the contained handle will be null. In such cases, the standard library's `Read` and"] # [doc = " `Write` will do nothing and silently succeed. All other I/O operations, via the"] # [doc = " standard library or via raw Windows API calls, will fail."] # [stable (feature = "rust1" , since = "1.0.0")] pub struct Stderr { inner : & 'static ReentrantLock < RefCell < StderrRaw > > , }}}
mkitem!{mkstruct!{# [doc = " A locked reference to the [`Stderr`] handle."] # [doc = ""] # [doc = " This handle implements the [`Write`] trait and is constructed via"] # [doc = " the [`Stderr::lock`] method. See its documentation for more."] # [doc = ""] # [doc = " ### Note: Windows Portability Considerations"] # [doc = ""] # [doc = " When operating in a console, the Windows implementation of this stream does not support"] # [doc = " non-UTF-8 byte sequences. Attempting to write bytes that are not valid UTF-8 will return"] # [doc = " an error."] # [doc = ""] # [doc = " In a process with a detached console, such as one using"] # [doc = " `#![windows_subsystem = \"windows\"]`, or in a child process spawned from such a process,"] # [doc = " the contained handle will be null. In such cases, the standard library's `Read` and"] # [doc = " `Write` will do nothing and silently succeed. All other I/O operations, via the"] # [doc = " standard library or via raw Windows API calls, will fail."] # [must_use = "if unused stderr will immediately unlock"] # [stable (feature = "rust1" , since = "1.0.0")] pub struct StderrLock < 'a > { inner : ReentrantLockGuard < 'a , RefCell < StderrRaw > > , }}}

macro_rules! stderr_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function stderr in module {}", module_path!());
    };
}

mkfn!{
    stderr_introspect!();
    # [doc = " Constructs a new handle to the standard error of the current process."] # [doc = ""] # [doc = " This handle is not buffered."] # [doc = ""] # [doc = " ### Note: Windows Portability Considerations"] # [doc = ""] # [doc = " When operating in a console, the Windows implementation of this stream does not support"] # [doc = " non-UTF-8 byte sequences. Attempting to write bytes that are not valid UTF-8 will return"] # [doc = " an error."] # [doc = ""] # [doc = " In a process with a detached console, such as one using"] # [doc = " `#![windows_subsystem = \"windows\"]`, or in a child process spawned from such a process,"] # [doc = " the contained handle will be null. In such cases, the standard library's `Read` and"] # [doc = " `Write` will do nothing and silently succeed. All other I/O operations, via the"] # [doc = " standard library or via raw Windows API calls, will fail."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Using implicit synchronization:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::{self, Write};"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     io::stderr().write_all(b\"hello world\")?;"] # [doc = ""] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Using explicit synchronization:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::{self, Write};"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let stderr = io::stderr();"] # [doc = "     let mut handle = stderr.lock();"] # [doc = ""] # [doc = "     handle.write_all(b\"hello world\")?;"] # [doc = ""] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [must_use] # [stable (feature = "rust1" , since = "1.0.0")] # [cfg_attr (not (test) , rustc_diagnostic_item = "io_stderr")] pub fn stderr () -> Stderr { static INSTANCE : ReentrantLock < RefCell < StderrRaw > > = ReentrantLock :: new (RefCell :: new (stderr_raw ())) ; Stderr { inner : & INSTANCE } }
}
mkitem!{mkimpl!{impl Stderr { # [doc = " Locks this handle to the standard error stream, returning a writable"] # [doc = " guard."] # [doc = ""] # [doc = " The lock is released when the returned lock goes out of scope. The"] # [doc = " returned guard also implements the [`Write`] trait for writing data."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io::{self, Write};"] # [doc = ""] # [doc = " fn foo() -> io::Result<()> {"] # [doc = "     let stderr = io::stderr();"] # [doc = "     let mut handle = stderr.lock();"] # [doc = ""] # [doc = "     handle.write_all(b\"hello world\")?;"] # [doc = ""] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn lock (& self) -> StderrLock < 'static > { StderrLock { inner : self . inner . lock () } } }}}
mkitem!{mkimpl!{# [stable (feature = "catch_unwind" , since = "1.9.0")] impl UnwindSafe for Stderr { }}}
mkitem!{mkimpl!{# [stable (feature = "catch_unwind" , since = "1.9.0")] impl RefUnwindSafe for Stderr { }}}
mkitem!{mkimpl!{# [stable (feature = "std_debug" , since = "1.16.0")] impl fmt :: Debug for Stderr { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("Stderr") . finish_non_exhaustive () } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl Write for Stderr { fn write (& mut self , buf : & [u8]) -> io :: Result < usize > { (& * self) . write (buf) } fn write_vectored (& mut self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { (& * self) . write_vectored (bufs) } # [inline] fn is_write_vectored (& self) -> bool { io :: Write :: is_write_vectored (& & * self) } fn flush (& mut self) -> io :: Result < () > { (& * self) . flush () } fn write_all (& mut self , buf : & [u8]) -> io :: Result < () > { (& * self) . write_all (buf) } fn write_all_vectored (& mut self , bufs : & mut [IoSlice < '_ >]) -> io :: Result < () > { (& * self) . write_all_vectored (bufs) } fn write_fmt (& mut self , args : fmt :: Arguments < '_ >) -> io :: Result < () > { (& * self) . write_fmt (args) } }}}
mkitem!{mkimpl!{# [stable (feature = "write_mt" , since = "1.48.0")] impl Write for & Stderr { fn write (& mut self , buf : & [u8]) -> io :: Result < usize > { self . lock () . write (buf) } fn write_vectored (& mut self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { self . lock () . write_vectored (bufs) } # [inline] fn is_write_vectored (& self) -> bool { self . lock () . is_write_vectored () } fn flush (& mut self) -> io :: Result < () > { self . lock () . flush () } fn write_all (& mut self , buf : & [u8]) -> io :: Result < () > { self . lock () . write_all (buf) } fn write_all_vectored (& mut self , bufs : & mut [IoSlice < '_ >]) -> io :: Result < () > { self . lock () . write_all_vectored (bufs) } fn write_fmt (& mut self , args : fmt :: Arguments < '_ >) -> io :: Result < () > { self . lock () . write_fmt (args) } }}}
mkitem!{mkimpl!{# [stable (feature = "catch_unwind" , since = "1.9.0")] impl UnwindSafe for StderrLock < '_ > { }}}
mkitem!{mkimpl!{# [stable (feature = "catch_unwind" , since = "1.9.0")] impl RefUnwindSafe for StderrLock < '_ > { }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl Write for StderrLock < '_ > { fn write (& mut self , buf : & [u8]) -> io :: Result < usize > { self . inner . borrow_mut () . write (buf) } fn write_vectored (& mut self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { self . inner . borrow_mut () . write_vectored (bufs) } # [inline] fn is_write_vectored (& self) -> bool { self . inner . borrow_mut () . is_write_vectored () } fn flush (& mut self) -> io :: Result < () > { self . inner . borrow_mut () . flush () } fn write_all (& mut self , buf : & [u8]) -> io :: Result < () > { self . inner . borrow_mut () . write_all (buf) } fn write_all_vectored (& mut self , bufs : & mut [IoSlice < '_ >]) -> io :: Result < () > { self . inner . borrow_mut () . write_all_vectored (bufs) } }}}
mkitem!{mkimpl!{# [stable (feature = "std_debug" , since = "1.16.0")] impl fmt :: Debug for StderrLock < '_ > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("StderrLock") . finish_non_exhaustive () } }}}

macro_rules! set_output_capture_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function set_output_capture in module {}", module_path!());
    };
}

mkfn!{
    set_output_capture_introspect!();
    # [doc = " Sets the thread-local output capture buffer and returns the old one."] # [unstable (feature = "internal_output_capture" , reason = "this function is meant for use in the test crate \
        and may disappear in the future" , issue = "none")] # [doc (hidden)] pub fn set_output_capture (sink : Option < LocalStream >) -> Option < LocalStream > { try_set_output_capture (sink) . expect ("cannot access a Thread Local Storage value \
         during or after destruction" ,) }
}

macro_rules! try_set_output_capture_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function try_set_output_capture in module {}", module_path!());
    };
}

mkfn!{
    try_set_output_capture_introspect!();
    # [doc = " Tries to set the thread-local output capture buffer and returns the old one."] # [doc = " This may fail once thread-local destructors are called. It's used in panic"] # [doc = " handling instead of `set_output_capture`."] # [unstable (feature = "internal_output_capture" , reason = "this function is meant for use in the test crate \
    and may disappear in the future" , issue = "none")] # [doc (hidden)] pub fn try_set_output_capture (sink : Option < LocalStream > ,) -> Result < Option < LocalStream > , AccessError > { if sink . is_none () && ! OUTPUT_CAPTURE_USED . load (Ordering :: Relaxed) { return Ok (None) ; } OUTPUT_CAPTURE_USED . store (true , Ordering :: Relaxed) ; OUTPUT_CAPTURE . try_with (move | slot | slot . replace (sink)) }
}

macro_rules! print_to_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function print_to in module {}", module_path!());
    };
}

mkfn!{
    print_to_introspect!();
    # [doc = " Writes `args` to the capture buffer if enabled and possible, or `global_s`"] # [doc = " otherwise. `label` identifies the stream in a panic message."] # [doc = ""] # [doc = " This function is used to print error messages, so it takes extra"] # [doc = " care to avoid causing a panic when `OUTPUT_CAPTURE` is unusable."] # [doc = " For instance, if the TLS key for output capturing is already destroyed, or"] # [doc = " if the local stream is in use by another thread, it will just fall back to"] # [doc = " the global stream."] # [doc = ""] # [doc = " However, if the actual I/O causes an error, this function does panic."] # [doc = ""] # [doc = " Writing to non-blocking stdout/stderr can cause an error, which will lead"] # [doc = " this function to panic."] fn print_to < T > (args : fmt :: Arguments < '_ > , global_s : fn () -> T , label : & str) where T : Write , { if print_to_buffer_if_capture_used (args) { return ; } if let Err (e) = global_s () . write_fmt (args) { panic ! ("failed printing to {label}: {e}") ; } }
}

macro_rules! print_to_buffer_if_capture_used_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function print_to_buffer_if_capture_used in module {}", module_path!());
    };
}

mkfn!{
    print_to_buffer_if_capture_used_introspect!();
    fn print_to_buffer_if_capture_used (args : fmt :: Arguments < '_ >) -> bool { OUTPUT_CAPTURE_USED . load (Ordering :: Relaxed) && OUTPUT_CAPTURE . try_with (| s | { s . take () . map (| w | { let _ = w . lock () . unwrap_or_else (| e | e . into_inner ()) . write_fmt (args) ; s . set (Some (w)) ; }) }) == Ok (Some (())) }
}

macro_rules! attempt_print_to_stderr_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function attempt_print_to_stderr in module {}", module_path!());
    };
}

mkfn!{
    attempt_print_to_stderr_introspect!();
    # [doc = " Used by impl Termination for Result to print error after `main` or a test"] # [doc = " has returned. Should avoid panicking, although we can't help it if one of"] # [doc = " the Display impls inside args decides to."] pub (crate) fn attempt_print_to_stderr (args : fmt :: Arguments < '_ >) { if print_to_buffer_if_capture_used (args) { return ; } let _ = stderr () . write_fmt (args) ; }
}
mkitem!{mktrait!{# [doc = " Trait to determine if a descriptor/handle refers to a terminal/tty."] # [stable (feature = "is_terminal" , since = "1.70.0")] pub trait IsTerminal : crate :: sealed :: Sealed { # [doc = " Returns `true` if the descriptor/handle refers to a terminal/tty."] # [doc = ""] # [doc = " On platforms where Rust does not know how to detect a terminal yet, this will return"] # [doc = " `false`. This will also return `false` if an unexpected error occurred, such as from"] # [doc = " passing an invalid file descriptor."] # [doc = ""] # [doc = " # Platform-specific behavior"] # [doc = ""] # [doc = " On Windows, in addition to detecting consoles, this currently uses some heuristics to"] # [doc = " detect older msys/cygwin/mingw pseudo-terminals based on device name: devices with names"] # [doc = " starting with `msys-` or `cygwin-` and ending in `-pty` will be considered terminals."] # [doc = " Note that this [may change in the future][changes]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " An example of a type for which `IsTerminal` is implemented is [`Stdin`]:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::{self, IsTerminal, Write};"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let stdin = io::stdin();"] # [doc = ""] # [doc = "     // Indicate that the user is prompted for input, if this is a terminal."] # [doc = "     if stdin.is_terminal() {"] # [doc = "         print!(\"> \");"] # [doc = "         io::stdout().flush()?;"] # [doc = "     }"] # [doc = ""] # [doc = "     let mut name = String::new();"] # [doc = "     let _ = stdin.read_line(&mut name)?;"] # [doc = ""] # [doc = "     println!(\"Hello {}\", name.trim_end());"] # [doc = ""] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " The example can be run in two ways:"] # [doc = ""] # [doc = " - If you run this example by piping some text to it, e.g. `echo \"foo\" | path/to/executable`"] # [doc = "   it will print: `Hello foo`."] # [doc = " - If you instead run the example interactively by running `path/to/executable` directly, it will"] # [doc = "   prompt for input."] # [doc = ""] # [doc = " [changes]: io#platform-specific-behavior"] # [doc = " [`Stdin`]: crate::io::Stdin"] # [doc (alias = "isatty")] # [stable (feature = "is_terminal" , since = "1.70.0")] fn is_terminal (& self) -> bool ; }}}
mkitem!{macro_rules ! impl_is_terminal { ($ ($ t : ty) ,*$ (,) ?) => { $ (# [unstable (feature = "sealed" , issue = "none")] impl crate :: sealed :: Sealed for $ t { } # [stable (feature = "is_terminal" , since = "1.70.0")] impl IsTerminal for $ t { # [inline] fn is_terminal (& self) -> bool { crate :: sys :: io :: is_terminal (self) } }) * } }}
mkitem!{impl_is_terminal ! (File , Stdin , StdinLock <'_ >, Stdout , StdoutLock <'_ >, Stderr , StderrLock <'_ >) ;}

macro_rules! _print_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function _print in module {}", module_path!());
    };
}

mkfn!{
    _print_introspect!();
    # [unstable (feature = "print_internals" , reason = "implementation detail which may disappear or be replaced at any time" , issue = "none")] # [doc (hidden)] # [cfg (not (test))] pub fn _print (args : fmt :: Arguments < '_ >) { print_to (args , stdout , "stdout") ; }
}

macro_rules! _eprint_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function _eprint in module {}", module_path!());
    };
}

mkfn!{
    _eprint_introspect!();
    # [unstable (feature = "print_internals" , reason = "implementation detail which may disappear or be replaced at any time" , issue = "none")] # [doc (hidden)] # [cfg (not (test))] pub fn _eprint (args : fmt :: Arguments < '_ >) { print_to (args , stderr , "stderr") ; }
}
mkuse!{# [cfg (test)] pub use realstd :: io :: { _eprint , _print } ;}