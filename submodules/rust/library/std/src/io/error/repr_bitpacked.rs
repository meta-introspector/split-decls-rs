# ! [doc = " This is a densely packed error representation which is used on targets with"] # ! [doc = " 64-bit pointers."] # ! [doc = ""] # ! [doc = " (Note that `bitpacked` vs `unpacked` here has no relationship to"] # ! [doc = " `#[repr(packed)]`, it just refers to attempting to use any available bits in"] # ! [doc = " a more clever manner than `rustc`'s default layout algorithm would)."] # ! [doc = ""] # ! [doc = " Conceptually, it stores the same data as the \"unpacked\" equivalent we use on"] # ! [doc = " other targets. Specifically, you can imagine it as an optimized version of"] # ! [doc = " the following enum (which is roughly equivalent to what's stored by"] # ! [doc = " `repr_unpacked::Repr`, e.g. `super::ErrorData<Box<Custom>>`):"] # ! [doc = ""] # ! [doc = " ```ignore (exposition-only)"] # ! [doc = " enum ErrorData {"] # ! [doc = "    Os(i32),"] # ! [doc = "    Simple(ErrorKind),"] # ! [doc = "    SimpleMessage(&'static SimpleMessage),"] # ! [doc = "    Custom(Box<Custom>),"] # ! [doc = " }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " However, it packs this data into a 64bit non-zero value."] # ! [doc = ""] # ! [doc = " This optimization not only allows `io::Error` to occupy a single pointer,"] # ! [doc = " but improves `io::Result` as well, especially for situations like"] # ! [doc = " `io::Result<()>` (which is now 64 bits) or `io::Result<u64>` (which is now"] # ! [doc = " 128 bits), which are quite common."] # ! [doc = ""] # ! [doc = " # Layout"] # ! [doc = " Tagged values are 64 bits, with the 2 least significant bits used for the"] # ! [doc = " tag. This means there are 4 \"variants\":"] # ! [doc = ""] # ! [doc = " - **Tag 0b00**: The first variant is equivalent to"] # ! [doc = "   `ErrorData::SimpleMessage`, and holds a `&'static SimpleMessage` directly."] # ! [doc = ""] # ! [doc = "   `SimpleMessage` has an alignment >= 4 (which is requested with"] # ! [doc = "   `#[repr(align)]` and checked statically at the bottom of this file), which"] # ! [doc = "   means every `&'static SimpleMessage` should have the both tag bits as 0,"] # ! [doc = "   meaning its tagged and untagged representation are equivalent."] # ! [doc = ""] # ! [doc = "   This means we can skip tagging it, which is necessary as this variant can"] # ! [doc = "   be constructed from a `const fn`, which probably cannot tag pointers (or"] # ! [doc = "   at least it would be difficult)."] # ! [doc = ""] # ! [doc = " - **Tag 0b01**: The other pointer variant holds the data for"] # ! [doc = "   `ErrorData::Custom` and the remaining 62 bits are used to store a"] # ! [doc = "   `Box<Custom>`. `Custom` also has alignment >= 4, so the bottom two bits"] # ! [doc = "   are free to use for the tag."] # ! [doc = ""] # ! [doc = "   The only important thing to note is that `ptr::wrapping_add` and"] # ! [doc = "   `ptr::wrapping_sub` are used to tag the pointer, rather than bitwise"] # ! [doc = "   operations. This should preserve the pointer's provenance, which would"] # ! [doc = "   otherwise be lost."] # ! [doc = ""] # ! [doc = " - **Tag 0b10**: Holds the data for `ErrorData::Os(i32)`. We store the `i32`"] # ! [doc = "   in the pointer's most significant 32 bits, and don't use the bits `2..32`"] # ! [doc = "   for anything. Using the top 32 bits is just to let us easily recover the"] # ! [doc = "   `i32` code with the correct sign."] # ! [doc = ""] # ! [doc = " - **Tag 0b11**: Holds the data for `ErrorData::Simple(ErrorKind)`. This"] # ! [doc = "   stores the `ErrorKind` in the top 32 bits as well, although it doesn't"] # ! [doc = "   occupy nearly that many. Most of the bits are unused here, but it's not"] # ! [doc = "   like we need them for anything else yet."] # ! [doc = ""] # ! [doc = " # Use of `NonNull<()>`"] # ! [doc = ""] # ! [doc = " Everything is stored in a `NonNull<()>`, which is odd, but actually serves a"] # ! [doc = " purpose."] # ! [doc = ""] # ! [doc = " Conceptually you might think of this more like:"] # ! [doc = ""] # ! [doc = " ```ignore (exposition-only)"] # ! [doc = " union Repr {"] # ! [doc = "     // holds integer (Simple/Os) variants, and"] # ! [doc = "     // provides access to the tag bits."] # ! [doc = "     bits: NonZero<u64>,"] # ! [doc = "     // Tag is 0, so this is stored untagged."] # ! [doc = "     msg: &'static SimpleMessage,"] # ! [doc = "     // Tagged (offset) `Box<Custom>` pointer."] # ! [doc = "     tagged_custom: NonNull<()>,"] # ! [doc = " }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " But there are a few problems with this:"] # ! [doc = ""] # ! [doc = " 1. Union access is equivalent to a transmute, so this representation would"] # ! [doc = "    require we transmute between integers and pointers in at least one"] # ! [doc = "    direction, which may be UB (and even if not, it is likely harder for a"] # ! [doc = "    compiler to reason about than explicit ptr->int operations)."] # ! [doc = ""] # ! [doc = " 2. Even if all fields of a union have a niche, the union itself doesn't,"] # ! [doc = "    although this may change in the future. This would make things like"] # ! [doc = "    `io::Result<()>` and `io::Result<usize>` larger, which defeats part of"] # ! [doc = "    the motivation of this bitpacking."] # ! [doc = ""] # ! [doc = " Storing everything in a `NonZero<usize>` (or some other integer) would be a"] # ! [doc = " bit more traditional for pointer tagging, but it would lose provenance"] # ! [doc = " information, couldn't be constructed from a `const fn`, and would probably"] # ! [doc = " run into other issues as well."] # ! [doc = ""] # ! [doc = " The `NonNull<()>` seems like the only alternative, even if it's fairly odd"] # ! [doc = " to use a pointer type to store something that may hold an integer, some of"] # ! [doc = " the time."] use split_decls_genesis :: ourprelude :: * ; use core :: marker :: PhantomData ; use core :: num :: NonZeroUsize ; use core :: ptr :: NonNull ; use super :: { Custom , ErrorData , ErrorKind , RawOsError , SimpleMessage } ; const TAG_MASK : usize = 0b11 ; const TAG_SIMPLE_MESSAGE : usize = 0b00 ; const TAG_CUSTOM : usize = 0b01 ; const TAG_OS : usize = 0b10 ; const TAG_SIMPLE : usize = 0b11 ; #[doc = " The internal representation."] #[doc = ""] #[doc = " See the module docs for more, this is just a way to hack in a check that we"] #[doc = " indeed are not unwind-safe."] #[doc = ""] #[doc = " ```compile_fail,E0277"] #[doc = " fn is_unwind_safe<T: core::panic::UnwindSafe>() {}"] #[doc = " is_unwind_safe::<std::io::Error>();"] #[doc = " ```"] #[repr (transparent)] #[rustc_insignificant_dtor] pub (super) struct Repr (NonNull < () > , PhantomData < ErrorData < Box < Custom > > >) ; unsafe impl Send for Repr { } unsafe impl Sync for Repr { } impl Repr { pub (super) fn new (dat : ErrorData < Box < Custom > >) -> Self { match dat { ErrorData :: Os (code) => Self :: new_os (code) , ErrorData :: Simple (kind) => Self :: new_simple (kind) , ErrorData :: SimpleMessage (simple_message) => Self :: new_simple_message (simple_message) , ErrorData :: Custom (b) => Self :: new_custom (b) , } } pub (super) fn new_custom (b : Box < Custom >) -> Self { let p = Box :: into_raw (b) . cast :: < u8 > () ; debug_assert_eq ! (p . addr () & TAG_MASK , 0) ; let tagged = p . wrapping_add (TAG_CUSTOM) . cast :: < () > () ; let res = Self (unsafe { NonNull :: new_unchecked (tagged) } , PhantomData) ; debug_assert ! (matches ! (res . data () , ErrorData :: Custom (_)) , "repr(custom) encoding failed") ; res } #[inline] pub (super) fn new_os (code : RawOsError) -> Self { let utagged = ((code as usize) << 32) | TAG_OS ; let res = Self (NonNull :: without_provenance (unsafe { NonZeroUsize :: new_unchecked (utagged) }) , PhantomData ,) ; debug_assert ! (matches ! (res . data () , ErrorData :: Os (c) if c == code) , "repr(os) encoding failed for {code}") ; res } #[inline] pub (super) fn new_simple (kind : ErrorKind) -> Self { let utagged = ((kind as usize) << 32) | TAG_SIMPLE ; let res = Self (NonNull :: without_provenance (unsafe { NonZeroUsize :: new_unchecked (utagged) }) , PhantomData ,) ; debug_assert ! (matches ! (res . data () , ErrorData :: Simple (k) if k == kind) , "repr(simple) encoding failed {:?}" , kind ,) ; res } #[inline] pub (super) const fn new_simple_message (m : & 'static SimpleMessage) -> Self { Self (unsafe { NonNull :: new_unchecked (m as * const _ as * mut ()) } , PhantomData) } #[inline] pub (super) fn data (& self) -> ErrorData < & Custom > { unsafe { decode_repr (self . 0 , | c | & * c) } } #[inline] pub (super) fn data_mut (& mut self) -> ErrorData < & mut Custom > { unsafe { decode_repr (self . 0 , | c | & mut * c) } } #[inline] pub (super) fn into_data (self) -> ErrorData < Box < Custom > > { let this = core :: mem :: ManuallyDrop :: new (self) ; unsafe { decode_repr (this . 0 , | p | Box :: from_raw (p)) } } } impl Drop for Repr { #[inline] fn drop (& mut self) { unsafe { let _ = decode_repr (self . 0 , | p | Box :: < Custom > :: from_raw (p)) ; } } } #[inline] unsafe fn decode_repr < C , F > (ptr : NonNull < () > , make_custom : F) -> ErrorData < C > where F : FnOnce (* mut Custom) -> C , { let bits = ptr . as_ptr () . addr () ; match bits & TAG_MASK { TAG_OS => { let code = ((bits as i64) >> 32) as RawOsError ; ErrorData :: Os (code) } TAG_SIMPLE => { let kind_bits = (bits >> 32) as u32 ; let kind = kind_from_prim (kind_bits) . unwrap_or_else (| | { debug_assert ! (false , "Invalid io::error::Repr bits: `Repr({:#018x})`" , bits) ; unsafe { core :: hint :: unreachable_unchecked () } ; }) ; ErrorData :: Simple (kind) } TAG_SIMPLE_MESSAGE => { unsafe { ErrorData :: SimpleMessage (& * ptr . cast :: < SimpleMessage > () . as_ptr ()) } } TAG_CUSTOM => { let custom = ptr . as_ptr () . wrapping_byte_sub (TAG_CUSTOM) . cast :: < Custom > () ; ErrorData :: Custom (make_custom (custom)) } _ => { unreachable ! () ; } } } #[inline] fn kind_from_prim (ek : u32) -> Option < ErrorKind > { macro_rules ! from_prim { ($ prim : expr => $ Enum : ident { $ ($ Variant : ident) ,* $ (,) ? }) => { { const _ : fn (e : $ Enum) = | e : $ Enum | match e { $ ($ Enum ::$ Variant => ()) ,* } ; match $ prim { $ (v if v == ($ Enum ::$ Variant as _) => Some ($ Enum ::$ Variant) ,) * _ => None , } } } } from_prim ! (ek => ErrorKind { NotFound , PermissionDenied , ConnectionRefused , ConnectionReset , HostUnreachable , NetworkUnreachable , ConnectionAborted , NotConnected , AddrInUse , AddrNotAvailable , NetworkDown , BrokenPipe , AlreadyExists , WouldBlock , NotADirectory , IsADirectory , DirectoryNotEmpty , ReadOnlyFilesystem , FilesystemLoop , StaleNetworkFileHandle , InvalidInput , InvalidData , TimedOut , WriteZero , StorageFull , NotSeekable , QuotaExceeded , FileTooLarge , ResourceBusy , ExecutableFileBusy , Deadlock , CrossesDevices , TooManyLinks , InvalidFilename , ArgumentListTooLong , Interrupted , Other , UnexpectedEof , Unsupported , OutOfMemory , InProgress , Uncategorized , }) } macro_rules ! static_assert { ($ condition : expr) => { const _ : () = assert ! ($ condition) ; } ; (@ usize_eq : $ lhs : expr , $ rhs : expr) => { const _ : [() ; $ lhs] = [() ; $ rhs] ; } ; } static_assert ! (@ usize_eq : size_of ::< NonNull < () >> () , 8) ; static_assert ! (@ usize_eq : size_of ::< NonNull < () >> () , size_of ::< usize > ()) ; static_assert ! (@ usize_eq : size_of ::<&'static SimpleMessage > () , 8) ; static_assert ! (@ usize_eq : size_of ::< Box < Custom >> () , 8) ; static_assert ! ((TAG_MASK + 1) . is_power_of_two ()) ; static_assert ! (align_of ::< SimpleMessage > () >= TAG_MASK + 1) ; static_assert ! (align_of ::< Custom > () >= TAG_MASK + 1) ; static_assert ! (@ usize_eq : TAG_MASK & TAG_SIMPLE_MESSAGE , TAG_SIMPLE_MESSAGE) ; static_assert ! (@ usize_eq : TAG_MASK & TAG_CUSTOM , TAG_CUSTOM) ; static_assert ! (@ usize_eq : TAG_MASK & TAG_OS , TAG_OS) ; static_assert ! (@ usize_eq : TAG_MASK & TAG_SIMPLE , TAG_SIMPLE) ; static_assert ! (size_of ::< Custom > () >= TAG_CUSTOM) ; static_assert ! (TAG_OS != 0) ; static_assert ! (TAG_SIMPLE != 0) ; static_assert ! (@ usize_eq : TAG_SIMPLE_MESSAGE , 0) ; static_assert ! (@ usize_eq : size_of ::< Repr > () , 8) ; static_assert ! (@ usize_eq : size_of ::< Option < Repr >> () , 8) ; static_assert ! (@ usize_eq : size_of ::< Result < () , Repr >> () , 8) ; static_assert ! (@ usize_eq : size_of ::< Result < usize , Repr >> () , 16) ;