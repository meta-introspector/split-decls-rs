mkmod!{tests, { 
                getname!(tests);
                getsrc!(tests);
                getpath!(tests);
                get_deps!(tests);
                get_crates!(tests);
                mkinclude!(tests);
                 
            }}
mkuse!{use crate :: fmt ;}
mkuse!{use crate :: io :: { self , BorrowedCursor , BufRead , IoSlice , IoSliceMut , Read , Seek , SeekFrom , SizeHint , Write , } ;}
mkitem!{mkstruct!{# [doc = " `Empty` ignores any data written via [`Write`], and will always be empty"] # [doc = " (returning zero bytes) when read via [`Read`]."] # [doc = ""] # [doc = " This struct is generally created by calling [`empty()`]. Please"] # [doc = " see the documentation of [`empty()`] for more details."] # [stable (feature = "rust1" , since = "1.0.0")] # [non_exhaustive] # [derive (Copy , Clone , Debug , Default)] pub struct Empty ;}}

macro_rules! empty_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function empty in module {}", module_path!());
    };
}

mkfn!{
    empty_introspect!();
    # [doc = " Creates a value that is always at EOF for reads, and ignores all data written."] # [doc = ""] # [doc = " All calls to [`write`] on the returned instance will return [`Ok(buf.len())`]"] # [doc = " and the contents of the buffer will not be inspected."] # [doc = ""] # [doc = " All calls to [`read`] from the returned reader will return [`Ok(0)`]."] # [doc = ""] # [doc = " [`Ok(buf.len())`]: Ok"] # [doc = " [`Ok(0)`]: Ok"] # [doc = ""] # [doc = " [`write`]: Write::write"] # [doc = " [`read`]: Read::read"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::io::{self, Write};"] # [doc = ""] # [doc = " let buffer = vec![1, 2, 3, 5, 8];"] # [doc = " let num_bytes = io::empty().write(&buffer).unwrap();"] # [doc = " assert_eq!(num_bytes, 5);"] # [doc = " ```"] # [doc = ""] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::io::{self, Read};"] # [doc = ""] # [doc = " let mut buffer = String::new();"] # [doc = " io::empty().read_to_string(&mut buffer).unwrap();"] # [doc = " assert!(buffer.is_empty());"] # [doc = " ```"] # [must_use] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_io_structs" , since = "1.79.0")] pub const fn empty () -> Empty { Empty }
}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl Read for Empty { # [inline] fn read (& mut self , _buf : & mut [u8]) -> io :: Result < usize > { Ok (0) } # [inline] fn read_buf (& mut self , _cursor : BorrowedCursor < '_ >) -> io :: Result < () > { Ok (()) } # [inline] fn read_vectored (& mut self , _bufs : & mut [IoSliceMut < '_ >]) -> io :: Result < usize > { Ok (0) } # [inline] fn is_read_vectored (& self) -> bool { false } # [inline] fn read_exact (& mut self , buf : & mut [u8]) -> io :: Result < () > { if ! buf . is_empty () { Err (io :: Error :: READ_EXACT_EOF) } else { Ok (()) } } # [inline] fn read_buf_exact (& mut self , cursor : BorrowedCursor < '_ >) -> io :: Result < () > { if cursor . capacity () != 0 { Err (io :: Error :: READ_EXACT_EOF) } else { Ok (()) } } # [inline] fn read_to_end (& mut self , _buf : & mut Vec < u8 >) -> io :: Result < usize > { Ok (0) } # [inline] fn read_to_string (& mut self , _buf : & mut String) -> io :: Result < usize > { Ok (0) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl BufRead for Empty { # [inline] fn fill_buf (& mut self) -> io :: Result < & [u8] > { Ok (& []) } # [inline] fn consume (& mut self , _n : usize) { } # [inline] fn has_data_left (& mut self) -> io :: Result < bool > { Ok (false) } # [inline] fn read_until (& mut self , _byte : u8 , _buf : & mut Vec < u8 >) -> io :: Result < usize > { Ok (0) } # [inline] fn skip_until (& mut self , _byte : u8) -> io :: Result < usize > { Ok (0) } # [inline] fn read_line (& mut self , _buf : & mut String) -> io :: Result < usize > { Ok (0) } }}}
mkitem!{mkimpl!{# [stable (feature = "empty_seek" , since = "1.51.0")] impl Seek for Empty { # [inline] fn seek (& mut self , _pos : SeekFrom) -> io :: Result < u64 > { Ok (0) } # [inline] fn stream_len (& mut self) -> io :: Result < u64 > { Ok (0) } # [inline] fn stream_position (& mut self) -> io :: Result < u64 > { Ok (0) } }}}
mkitem!{mkimpl!{impl SizeHint for Empty { # [inline] fn upper_bound (& self) -> Option < usize > { Some (0) } }}}
mkitem!{mkimpl!{# [stable (feature = "empty_write" , since = "1.73.0")] impl Write for Empty { # [inline] fn write (& mut self , buf : & [u8]) -> io :: Result < usize > { Ok (buf . len ()) } # [inline] fn write_vectored (& mut self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { let total_len = bufs . iter () . map (| b | b . len ()) . sum () ; Ok (total_len) } # [inline] fn is_write_vectored (& self) -> bool { true } # [inline] fn write_all (& mut self , _buf : & [u8]) -> io :: Result < () > { Ok (()) } # [inline] fn write_all_vectored (& mut self , _bufs : & mut [IoSlice < '_ >]) -> io :: Result < () > { Ok (()) } # [inline] fn write_fmt (& mut self , _args : fmt :: Arguments < '_ >) -> io :: Result < () > { Ok (()) } # [inline] fn flush (& mut self) -> io :: Result < () > { Ok (()) } }}}
mkitem!{mkimpl!{# [stable (feature = "empty_write" , since = "1.73.0")] impl Write for & Empty { # [inline] fn write (& mut self , buf : & [u8]) -> io :: Result < usize > { Ok (buf . len ()) } # [inline] fn write_vectored (& mut self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { let total_len = bufs . iter () . map (| b | b . len ()) . sum () ; Ok (total_len) } # [inline] fn is_write_vectored (& self) -> bool { true } # [inline] fn write_all (& mut self , _buf : & [u8]) -> io :: Result < () > { Ok (()) } # [inline] fn write_all_vectored (& mut self , _bufs : & mut [IoSlice < '_ >]) -> io :: Result < () > { Ok (()) } # [inline] fn write_fmt (& mut self , _args : fmt :: Arguments < '_ >) -> io :: Result < () > { Ok (()) } # [inline] fn flush (& mut self) -> io :: Result < () > { Ok (()) } }}}
mkitem!{mkstruct!{# [doc = " A reader which yields one byte over and over and over and over and over and..."] # [doc = ""] # [doc = " This struct is generally created by calling [`repeat()`]. Please"] # [doc = " see the documentation of [`repeat()`] for more details."] # [stable (feature = "rust1" , since = "1.0.0")] pub struct Repeat { byte : u8 , }}}

macro_rules! repeat_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function repeat in module {}", module_path!());
    };
}

mkfn!{
    repeat_introspect!();
    # [doc = " Creates an instance of a reader that infinitely repeats one byte."] # [doc = ""] # [doc = " All reads from this reader will succeed by filling the specified buffer with"] # [doc = " the given byte."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io::{self, Read};"] # [doc = ""] # [doc = " let mut buffer = [0; 3];"] # [doc = " io::repeat(0b101).read_exact(&mut buffer).unwrap();"] # [doc = " assert_eq!(buffer, [0b101, 0b101, 0b101]);"] # [doc = " ```"] # [must_use] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_io_structs" , since = "1.79.0")] pub const fn repeat (byte : u8) -> Repeat { Repeat { byte } }
}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl Read for Repeat { # [inline] fn read (& mut self , buf : & mut [u8]) -> io :: Result < usize > { buf . fill (self . byte) ; Ok (buf . len ()) } # [inline] fn read_exact (& mut self , buf : & mut [u8]) -> io :: Result < () > { buf . fill (self . byte) ; Ok (()) } # [inline] fn read_buf (& mut self , mut buf : BorrowedCursor < '_ >) -> io :: Result < () > { unsafe { buf . as_mut () } . write_filled (self . byte) ; unsafe { buf . advance_unchecked (buf . capacity ()) } ; Ok (()) } # [inline] fn read_buf_exact (& mut self , buf : BorrowedCursor < '_ >) -> io :: Result < () > { self . read_buf (buf) } # [doc = " This function is not supported by `io::Repeat`, because there's no end of its data"] fn read_to_end (& mut self , _ : & mut Vec < u8 >) -> io :: Result < usize > { Err (io :: Error :: from (io :: ErrorKind :: OutOfMemory)) } # [doc = " This function is not supported by `io::Repeat`, because there's no end of its data"] fn read_to_string (& mut self , _ : & mut String) -> io :: Result < usize > { Err (io :: Error :: from (io :: ErrorKind :: OutOfMemory)) } # [inline] fn read_vectored (& mut self , bufs : & mut [IoSliceMut < '_ >]) -> io :: Result < usize > { let mut nwritten = 0 ; for buf in bufs { nwritten += self . read (buf) ? ; } Ok (nwritten) } # [inline] fn is_read_vectored (& self) -> bool { true } }}}
mkitem!{mkimpl!{impl SizeHint for Repeat { # [inline] fn lower_bound (& self) -> usize { usize :: MAX } # [inline] fn upper_bound (& self) -> Option < usize > { None } }}}
mkitem!{mkimpl!{# [stable (feature = "std_debug" , since = "1.16.0")] impl fmt :: Debug for Repeat { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("Repeat") . finish_non_exhaustive () } }}}
mkitem!{mkstruct!{# [doc = " A writer which will move data into the void."] # [doc = ""] # [doc = " This struct is generally created by calling [`sink()`]. Please"] # [doc = " see the documentation of [`sink()`] for more details."] # [stable (feature = "rust1" , since = "1.0.0")] # [non_exhaustive] # [derive (Copy , Clone , Debug , Default)] pub struct Sink ;}}

macro_rules! sink_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function sink in module {}", module_path!());
    };
}

mkfn!{
    sink_introspect!();
    # [doc = " Creates an instance of a writer which will successfully consume all data."] # [doc = ""] # [doc = " All calls to [`write`] on the returned instance will return [`Ok(buf.len())`]"] # [doc = " and the contents of the buffer will not be inspected."] # [doc = ""] # [doc = " [`write`]: Write::write"] # [doc = " [`Ok(buf.len())`]: Ok"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::io::{self, Write};"] # [doc = ""] # [doc = " let buffer = vec![1, 2, 3, 5, 8];"] # [doc = " let num_bytes = io::sink().write(&buffer).unwrap();"] # [doc = " assert_eq!(num_bytes, 5);"] # [doc = " ```"] # [must_use] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_io_structs" , since = "1.79.0")] pub const fn sink () -> Sink { Sink }
}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl Write for Sink { # [inline] fn write (& mut self , buf : & [u8]) -> io :: Result < usize > { Ok (buf . len ()) } # [inline] fn write_vectored (& mut self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { let total_len = bufs . iter () . map (| b | b . len ()) . sum () ; Ok (total_len) } # [inline] fn is_write_vectored (& self) -> bool { true } # [inline] fn write_all (& mut self , _buf : & [u8]) -> io :: Result < () > { Ok (()) } # [inline] fn write_all_vectored (& mut self , _bufs : & mut [IoSlice < '_ >]) -> io :: Result < () > { Ok (()) } # [inline] fn write_fmt (& mut self , _args : fmt :: Arguments < '_ >) -> io :: Result < () > { Ok (()) } # [inline] fn flush (& mut self) -> io :: Result < () > { Ok (()) } }}}
mkitem!{mkimpl!{# [stable (feature = "write_mt" , since = "1.48.0")] impl Write for & Sink { # [inline] fn write (& mut self , buf : & [u8]) -> io :: Result < usize > { Ok (buf . len ()) } # [inline] fn write_vectored (& mut self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { let total_len = bufs . iter () . map (| b | b . len ()) . sum () ; Ok (total_len) } # [inline] fn is_write_vectored (& self) -> bool { true } # [inline] fn write_all (& mut self , _buf : & [u8]) -> io :: Result < () > { Ok (()) } # [inline] fn write_all_vectored (& mut self , _bufs : & mut [IoSlice < '_ >]) -> io :: Result < () > { Ok (()) } # [inline] fn write_fmt (& mut self , _args : fmt :: Arguments < '_ >) -> io :: Result < () > { Ok (()) } # [inline] fn flush (& mut self) -> io :: Result < () > { Ok (()) } }}}