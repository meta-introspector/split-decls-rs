mkuse!{use super :: raw :: { AsRawFd , FromRawFd , IntoRawFd , RawFd } ;}
mkuse!{# [cfg (not (target_os = "trusty"))] use crate :: fs ;}
mkuse!{use crate :: marker :: PhantomData ;}
mkuse!{use crate :: mem :: ManuallyDrop ;}
mkuse!{# [cfg (not (any (target_arch = "wasm32" , target_env = "sgx" , target_os = "hermit" , target_os = "trusty")))] use crate :: sys :: cvt ;}
mkuse!{# [cfg (not (target_os = "trusty"))] use crate :: sys_common :: { AsInner , FromInner , IntoInner } ;}
mkuse!{use crate :: { fmt , io } ;}
mkitem!{type ValidRawFd = core :: num :: niche_types :: NotAllOnes < RawFd > ;}
mkitem!{mkstruct!{# [doc = " A borrowed file descriptor."] # [doc = ""] # [doc = " This has a lifetime parameter to tie it to the lifetime of something that owns the file"] # [doc = " descriptor. For the duration of that lifetime, it is guaranteed that nobody will close the file"] # [doc = " descriptor."] # [doc = ""] # [doc = " This uses `repr(transparent)` and has the representation of a host file"] # [doc = " descriptor, so it can be used in FFI in places where a file descriptor is"] # [doc = " passed as an argument, it is not captured or consumed, and it never has the"] # [doc = " value `-1`."] # [doc = ""] # [doc = " This type does not have a [`ToOwned`][crate::borrow::ToOwned]"] # [doc = " implementation. Calling `.to_owned()` on a variable of this type will call"] # [doc = " it on `&BorrowedFd` and use `Clone::clone()` like `ToOwned` does for all"] # [doc = " types implementing `Clone`. The result will be descriptor borrowed under"] # [doc = " the same lifetime."] # [doc = ""] # [doc = " To obtain an [`OwnedFd`], you can use [`BorrowedFd::try_clone_to_owned`]"] # [doc = " instead, but this is not supported on all platforms."] # [derive (Copy , Clone)] # [repr (transparent)] # [rustc_nonnull_optimization_guaranteed] # [stable (feature = "io_safety" , since = "1.63.0")] pub struct BorrowedFd < 'fd > { fd : ValidRawFd , _phantom : PhantomData < & 'fd OwnedFd > , }}}
mkitem!{mkstruct!{# [doc = " An owned file descriptor."] # [doc = ""] # [doc = " This closes the file descriptor on drop. It is guaranteed that nobody else will close the file"] # [doc = " descriptor."] # [doc = ""] # [doc = " This uses `repr(transparent)` and has the representation of a host file"] # [doc = " descriptor, so it can be used in FFI in places where a file descriptor is"] # [doc = " passed as a consumed argument or returned as an owned value, and it never"] # [doc = " has the value `-1`."] # [doc = ""] # [doc = " You can use [`AsFd::as_fd`] to obtain a [`BorrowedFd`]."] # [repr (transparent)] # [rustc_nonnull_optimization_guaranteed] # [stable (feature = "io_safety" , since = "1.63.0")] pub struct OwnedFd { fd : ValidRawFd , }}}
mkitem!{mkimpl!{impl BorrowedFd < '_ > { # [doc = " Returns a `BorrowedFd` holding the given raw file descriptor."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The resource pointed to by `fd` must remain open for the duration of"] # [doc = " the returned `BorrowedFd`, and it must not have the value `-1`."] # [inline] # [track_caller] # [rustc_const_stable (feature = "io_safety" , since = "1.63.0")] # [stable (feature = "io_safety" , since = "1.63.0")] pub const unsafe fn borrow_raw (fd : RawFd) -> Self { Self { fd : ValidRawFd :: new (fd) . expect ("fd != -1") , _phantom : PhantomData } } }}}
mkitem!{mkimpl!{impl OwnedFd { # [doc = " Creates a new `OwnedFd` instance that shares the same underlying file"] # [doc = " description as the existing `OwnedFd` instance."] # [stable (feature = "io_safety" , since = "1.63.0")] pub fn try_clone (& self) -> crate :: io :: Result < Self > { self . as_fd () . try_clone_to_owned () } }}}
mkitem!{mkimpl!{impl BorrowedFd < '_ > { # [doc = " Creates a new `OwnedFd` instance that shares the same underlying file"] # [doc = " description as the existing `BorrowedFd` instance."] # [cfg (not (any (target_arch = "wasm32" , target_os = "hermit" , target_os = "trusty")))] # [stable (feature = "io_safety" , since = "1.63.0")] pub fn try_clone_to_owned (& self) -> crate :: io :: Result < OwnedFd > { # [cfg (not (any (target_os = "espidf" , target_os = "vita")))] let cmd = libc :: F_DUPFD_CLOEXEC ; # [cfg (any (target_os = "espidf" , target_os = "vita"))] let cmd = libc :: F_DUPFD ; let fd = cvt (unsafe { libc :: fcntl (self . as_raw_fd () , cmd , 3) }) ? ; Ok (unsafe { OwnedFd :: from_raw_fd (fd) }) } # [doc = " Creates a new `OwnedFd` instance that shares the same underlying file"] # [doc = " description as the existing `BorrowedFd` instance."] # [cfg (any (target_arch = "wasm32" , target_os = "hermit" , target_os = "trusty"))] # [stable (feature = "io_safety" , since = "1.63.0")] pub fn try_clone_to_owned (& self) -> crate :: io :: Result < OwnedFd > { Err (crate :: io :: Error :: UNSUPPORTED_PLATFORM) } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl AsRawFd for BorrowedFd < '_ > { # [inline] fn as_raw_fd (& self) -> RawFd { self . fd . as_inner () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl AsRawFd for OwnedFd { # [inline] fn as_raw_fd (& self) -> RawFd { self . fd . as_inner () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl IntoRawFd for OwnedFd { # [inline] fn into_raw_fd (self) -> RawFd { ManuallyDrop :: new (self) . fd . as_inner () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl FromRawFd for OwnedFd { # [doc = " Constructs a new instance of `Self` from the given raw file descriptor."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The resource pointed to by `fd` must be open and suitable for assuming"] # [doc = " [ownership][io-safety]. The resource must not require any cleanup other than `close`."] # [doc = ""] # [doc = " [io-safety]: io#io-safety"] # [inline] # [track_caller] unsafe fn from_raw_fd (fd : RawFd) -> Self { Self { fd : ValidRawFd :: new (fd) . expect ("fd != -1") } } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl Drop for OwnedFd { # [inline] fn drop (& mut self) { unsafe { # [cfg (not (target_os = "hermit"))] { # [cfg (unix)] crate :: sys :: fs :: debug_assert_fd_is_open (self . fd . as_inner ()) ; let _ = libc :: close (self . fd . as_inner ()) ; } # [cfg (target_os = "hermit")] let _ = hermit_abi :: close (self . fd . as_inner ()) ; } } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl fmt :: Debug for BorrowedFd < '_ > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("BorrowedFd") . field ("fd" , & self . fd) . finish () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl fmt :: Debug for OwnedFd { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("OwnedFd") . field ("fd" , & self . fd) . finish () } }}}
mkitem!{macro_rules ! impl_is_terminal { ($ ($ t : ty) ,*$ (,) ?) => { $ (# [unstable (feature = "sealed" , issue = "none")] impl crate :: sealed :: Sealed for $ t { } # [stable (feature = "is_terminal" , since = "1.70.0")] impl crate :: io :: IsTerminal for $ t { # [inline] fn is_terminal (& self) -> bool { crate :: sys :: io :: is_terminal (self) } }) * } }}
mkitem!{impl_is_terminal ! (BorrowedFd <'_ >, OwnedFd) ;}
mkitem!{mktrait!{# [doc = " A trait to borrow the file descriptor from an underlying object."] # [doc = ""] # [doc = " This is only available on unix platforms and must be imported in order to"] # [doc = " call the method. Windows platforms have a corresponding `AsHandle` and"] # [doc = " `AsSocket` set of traits."] # [stable (feature = "io_safety" , since = "1.63.0")] pub trait AsFd { # [doc = " Borrows the file descriptor."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::fs::File;"] # [doc = " # use std::io;"] # [doc = " # #[cfg(any(unix, target_os = \"wasi\"))]"] # [doc = " # use std::os::fd::{AsFd, BorrowedFd};"] # [doc = ""] # [doc = " let mut f = File::open(\"foo.txt\")?;"] # [doc = " # #[cfg(any(unix, target_os = \"wasi\"))]"] # [doc = " let borrowed_fd: BorrowedFd<'_> = f.as_fd();"] # [doc = " # Ok::<(), io::Error>(())"] # [doc = " ```"] # [stable (feature = "io_safety" , since = "1.63.0")] fn as_fd (& self) -> BorrowedFd < '_ > ; }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl < T : AsFd + ? Sized > AsFd for & T { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { T :: as_fd (self) } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl < T : AsFd + ? Sized > AsFd for & mut T { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { T :: as_fd (self) } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl AsFd for BorrowedFd < '_ > { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { * self } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl AsFd for OwnedFd { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { unsafe { BorrowedFd :: borrow_raw (self . as_raw_fd ()) } } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] # [cfg (not (target_os = "trusty"))] impl AsFd for fs :: File { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { self . as_inner () . as_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] # [cfg (not (target_os = "trusty"))] impl From < fs :: File > for OwnedFd { # [doc = " Takes ownership of a [`File`](fs::File)'s underlying file descriptor."] # [inline] fn from (file : fs :: File) -> OwnedFd { file . into_inner () . into_inner () . into_inner () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] # [cfg (not (target_os = "trusty"))] impl From < OwnedFd > for fs :: File { # [doc = " Returns a [`File`](fs::File) that takes ownership of the given"] # [doc = " file descriptor."] # [inline] fn from (owned_fd : OwnedFd) -> Self { Self :: from_inner (FromInner :: from_inner (FromInner :: from_inner (owned_fd))) } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] # [cfg (not (target_os = "trusty"))] impl AsFd for crate :: net :: TcpStream { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { self . as_inner () . socket () . as_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] # [cfg (not (target_os = "trusty"))] impl From < crate :: net :: TcpStream > for OwnedFd { # [doc = " Takes ownership of a [`TcpStream`](crate::net::TcpStream)'s socket file descriptor."] # [inline] fn from (tcp_stream : crate :: net :: TcpStream) -> OwnedFd { tcp_stream . into_inner () . into_socket () . into_inner () . into_inner () . into () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] # [cfg (not (target_os = "trusty"))] impl From < OwnedFd > for crate :: net :: TcpStream { # [inline] fn from (owned_fd : OwnedFd) -> Self { Self :: from_inner (FromInner :: from_inner (FromInner :: from_inner (FromInner :: from_inner (owned_fd ,)))) } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] # [cfg (not (target_os = "trusty"))] impl AsFd for crate :: net :: TcpListener { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { self . as_inner () . socket () . as_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] # [cfg (not (target_os = "trusty"))] impl From < crate :: net :: TcpListener > for OwnedFd { # [doc = " Takes ownership of a [`TcpListener`](crate::net::TcpListener)'s socket file descriptor."] # [inline] fn from (tcp_listener : crate :: net :: TcpListener) -> OwnedFd { tcp_listener . into_inner () . into_socket () . into_inner () . into_inner () . into () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] # [cfg (not (target_os = "trusty"))] impl From < OwnedFd > for crate :: net :: TcpListener { # [inline] fn from (owned_fd : OwnedFd) -> Self { Self :: from_inner (FromInner :: from_inner (FromInner :: from_inner (FromInner :: from_inner (owned_fd ,)))) } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] # [cfg (not (target_os = "trusty"))] impl AsFd for crate :: net :: UdpSocket { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { self . as_inner () . socket () . as_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] # [cfg (not (target_os = "trusty"))] impl From < crate :: net :: UdpSocket > for OwnedFd { # [doc = " Takes ownership of a [`UdpSocket`](crate::net::UdpSocket)'s file descriptor."] # [inline] fn from (udp_socket : crate :: net :: UdpSocket) -> OwnedFd { udp_socket . into_inner () . into_socket () . into_inner () . into_inner () . into () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] # [cfg (not (target_os = "trusty"))] impl From < OwnedFd > for crate :: net :: UdpSocket { # [inline] fn from (owned_fd : OwnedFd) -> Self { Self :: from_inner (FromInner :: from_inner (FromInner :: from_inner (FromInner :: from_inner (owned_fd ,)))) } }}}
mkitem!{mkimpl!{# [stable (feature = "asfd_ptrs" , since = "1.64.0")] # [doc = " This impl allows implementing traits that require `AsFd` on Arc."] # [doc = " ```"] # [doc = " # #[cfg(any(unix, target_os = \"wasi\"))] mod group_cfg {"] # [doc = " # #[cfg(target_os = \"wasi\")]"] # [doc = " # use std::os::wasi::io::AsFd;"] # [doc = " # #[cfg(unix)]"] # [doc = " # use std::os::unix::io::AsFd;"] # [doc = " use std::net::UdpSocket;"] # [doc = " use std::sync::Arc;"] # [doc = ""] # [doc = " trait MyTrait: AsFd {}"] # [doc = " impl MyTrait for Arc<UdpSocket> {}"] # [doc = " impl MyTrait for Box<UdpSocket> {}"] # [doc = " # }"] # [doc = " ```"] impl < T : AsFd + ? Sized > AsFd for crate :: sync :: Arc < T > { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { (* * self) . as_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "asfd_rc" , since = "1.69.0")] impl < T : AsFd + ? Sized > AsFd for crate :: rc :: Rc < T > { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { (* * self) . as_fd () } }}}
mkitem!{mkimpl!{# [unstable (feature = "unique_rc_arc" , issue = "112566")] impl < T : AsFd + ? Sized > AsFd for crate :: rc :: UniqueRc < T > { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { (* * self) . as_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "asfd_ptrs" , since = "1.64.0")] impl < T : AsFd + ? Sized > AsFd for Box < T > { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { (* * self) . as_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl AsFd for io :: Stdin { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { unsafe { BorrowedFd :: borrow_raw (0) } } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl < 'a > AsFd for io :: StdinLock < 'a > { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { unsafe { BorrowedFd :: borrow_raw (0) } } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl AsFd for io :: Stdout { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { unsafe { BorrowedFd :: borrow_raw (1) } } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl < 'a > AsFd for io :: StdoutLock < 'a > { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { unsafe { BorrowedFd :: borrow_raw (1) } } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl AsFd for io :: Stderr { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { unsafe { BorrowedFd :: borrow_raw (2) } } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl < 'a > AsFd for io :: StderrLock < 'a > { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { unsafe { BorrowedFd :: borrow_raw (2) } } }}}
mkitem!{mkimpl!{# [stable (feature = "anonymous_pipe" , since = "1.87.0")] # [cfg (not (target_os = "trusty"))] impl AsFd for io :: PipeReader { fn as_fd (& self) -> BorrowedFd < '_ > { self . 0 . as_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "anonymous_pipe" , since = "1.87.0")] # [cfg (not (target_os = "trusty"))] impl From < io :: PipeReader > for OwnedFd { fn from (pipe : io :: PipeReader) -> Self { pipe . 0 . into_inner () } }}}
mkitem!{mkimpl!{# [stable (feature = "anonymous_pipe" , since = "1.87.0")] # [cfg (not (target_os = "trusty"))] impl AsFd for io :: PipeWriter { fn as_fd (& self) -> BorrowedFd < '_ > { self . 0 . as_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "anonymous_pipe" , since = "1.87.0")] # [cfg (not (target_os = "trusty"))] impl From < io :: PipeWriter > for OwnedFd { fn from (pipe : io :: PipeWriter) -> Self { pipe . 0 . into_inner () } }}}
mkitem!{mkimpl!{# [stable (feature = "anonymous_pipe" , since = "1.87.0")] # [cfg (not (target_os = "trusty"))] impl From < OwnedFd > for io :: PipeReader { fn from (owned_fd : OwnedFd) -> Self { Self (FromInner :: from_inner (owned_fd)) } }}}
mkitem!{mkimpl!{# [stable (feature = "anonymous_pipe" , since = "1.87.0")] # [cfg (not (target_os = "trusty"))] impl From < OwnedFd > for io :: PipeWriter { fn from (owned_fd : OwnedFd) -> Self { Self (FromInner :: from_inner (owned_fd)) } }}}