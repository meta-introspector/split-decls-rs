# ! [doc = " Owned and borrowed Unix-like file descriptors."] # ! [doc = ""] # ! [doc = " This module is supported on Unix platforms and WASI, which both use a"] # ! [doc = " similar file descriptor system for referencing OS resources."] # ! [stable (feature = "os_fd" , since = "1.66.0")] # ! [deny (unsafe_op_in_unsafe_fn)] use split_decls_genesis :: ourprelude :: * ; mod raw ; mod owned ; #[cfg (not (target_os = "trusty"))] mod net ; #[cfg (test)] mod tests ; #[stable (feature = "os_fd" , since = "1.66.0")] pub use owned :: * ; #[stable (feature = "os_fd" , since = "1.66.0")] pub use raw :: * ;