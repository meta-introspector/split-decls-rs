mkuse!{use alloc :: wtf8 :: Wtf8Buf ;}
mkuse!{use crate :: ffi :: { OsStr , OsString } ;}
mkuse!{use crate :: fmt ;}
mkuse!{use crate :: iter :: FusedIterator ;}
mkuse!{use crate :: sealed :: Sealed ;}
mkuse!{use crate :: sys :: os_str :: Buf ;}
mkuse!{use crate :: sys_common :: { AsInner , FromInner } ;}
mkitem!{mktrait!{# [doc = " Windows-specific extensions to [`OsString`]."] # [doc = ""] # [doc = " This trait is sealed: it cannot be implemented outside the standard library."] # [doc = " This is so that future additional methods are not breaking changes."] # [stable (feature = "rust1" , since = "1.0.0")] pub trait OsStringExt : Sealed { # [doc = " Creates an `OsString` from a potentially ill-formed UTF-16 slice of"] # [doc = " 16-bit code units."] # [doc = ""] # [doc = " This is lossless: calling [`OsStrExt::encode_wide`] on the resulting string"] # [doc = " will always return the original code units."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsString;"] # [doc = " use std::os::windows::prelude::*;"] # [doc = ""] # [doc = " // UTF-16 encoding for \"Unicode\"."] # [doc = " let source = [0x0055, 0x006E, 0x0069, 0x0063, 0x006F, 0x0064, 0x0065];"] # [doc = ""] # [doc = " let string = OsString::from_wide(&source[..]);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] fn from_wide (wide : & [u16]) -> Self ; }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl OsStringExt for OsString { fn from_wide (wide : & [u16]) -> OsString { FromInner :: from_inner (Buf { inner : Wtf8Buf :: from_wide (wide) }) } }}}
mkitem!{mktrait!{# [doc = " Windows-specific extensions to [`OsStr`]."] # [doc = ""] # [doc = " This trait is sealed: it cannot be implemented outside the standard library."] # [doc = " This is so that future additional methods are not breaking changes."] # [stable (feature = "rust1" , since = "1.0.0")] pub trait OsStrExt : Sealed { # [doc = " Re-encodes an `OsStr` as a wide character sequence, i.e., potentially"] # [doc = " ill-formed UTF-16."] # [doc = ""] # [doc = " This is lossless: calling [`OsStringExt::from_wide`] and then"] # [doc = " `encode_wide` on the result will yield the original code units."] # [doc = " Note that the encoding does not add a final null terminator."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ffi::OsString;"] # [doc = " use std::os::windows::prelude::*;"] # [doc = ""] # [doc = " // UTF-16 encoding for \"Unicode\"."] # [doc = " let source = [0x0055, 0x006E, 0x0069, 0x0063, 0x006F, 0x0064, 0x0065];"] # [doc = ""] # [doc = " let string = OsString::from_wide(&source[..]);"] # [doc = ""] # [doc = " let result: Vec<u16> = string.encode_wide().collect();"] # [doc = " assert_eq!(&source[..], &result[..]);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] fn encode_wide (& self) -> EncodeWide < '_ > ; }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl OsStrExt for OsStr { # [inline] fn encode_wide (& self) -> EncodeWide < '_ > { EncodeWide { inner : self . as_inner () . inner . encode_wide () } } }}}
mkitem!{mkstruct!{# [doc = " Iterator returned by [`OsStrExt::encode_wide`]."] # [stable (feature = "rust1" , since = "1.0.0")] # [derive (Clone)] pub struct EncodeWide < 'a > { inner : alloc :: wtf8 :: EncodeWide < 'a > , }}}
mkitem!{mkimpl!{# [stable (feature = "encode_wide_debug" , since = "1.91.0")] impl fmt :: Debug for EncodeWide < '_ > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { fmt :: Debug :: fmt (& self . inner , f) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl Iterator for EncodeWide < '_ > { type Item = u16 ; # [inline] fn next (& mut self) -> Option < u16 > { self . inner . next () } # [inline] fn size_hint (& self) -> (usize , Option < usize >) { self . inner . size_hint () } }}}
mkitem!{mkimpl!{# [stable (feature = "encode_wide_fused_iterator" , since = "1.62.0")] impl FusedIterator for EncodeWide < '_ > { }}}