mkuse!{use crate :: ffi :: OsStr ;}
mkuse!{use crate :: os :: unix :: io :: { AsFd , AsRawFd , BorrowedFd , FromRawFd , IntoRawFd , OwnedFd , RawFd } ;}
mkuse!{use crate :: path :: Path ;}
mkuse!{use crate :: sealed :: Sealed ;}
mkuse!{use crate :: sys_common :: { AsInner , AsInnerMut , FromInner , IntoInner } ;}
mkuse!{use crate :: { io , process , sys } ;}
mkitem!{cfg_select ! { any (target_os = "vxworks" , target_os = "espidf" , target_os = "horizon" , target_os = "vita") => { type UserId = u16 ; type GroupId = u16 ; } target_os = "nto" => { type UserId = i32 ; type GroupId = i32 ; } _ => { type UserId = u32 ; type GroupId = u32 ; } }}
mkitem!{mktrait!{# [doc = " Unix-specific extensions to the [`process::Command`] builder."] # [doc = ""] # [doc = " This trait is sealed: it cannot be implemented outside the standard library."] # [doc = " This is so that future additional methods are not breaking changes."] # [stable (feature = "rust1" , since = "1.0.0")] pub trait CommandExt : Sealed { # [doc = " Sets the child process's user ID. This translates to a"] # [doc = " `setuid` call in the child process. Failure in the `setuid`"] # [doc = " call will cause the spawn to fail."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " This will also trigger a call to `setgroups(0, NULL)` in the child"] # [doc = " process if no groups have been specified."] # [doc = " This removes supplementary groups that might have given the child"] # [doc = " unwanted permissions."] # [stable (feature = "rust1" , since = "1.0.0")] fn uid (& mut self , id : UserId) -> & mut process :: Command ; # [doc = " Similar to `uid`, but sets the group ID of the child process. This has"] # [doc = " the same semantics as the `uid` field."] # [stable (feature = "rust1" , since = "1.0.0")] fn gid (& mut self , id : GroupId) -> & mut process :: Command ; # [doc = " Sets the supplementary group IDs for the calling process. Translates to"] # [doc = " a `setgroups` call in the child process."] # [unstable (feature = "setgroups" , issue = "90747")] fn groups (& mut self , groups : & [GroupId]) -> & mut process :: Command ; # [doc = " Schedules a closure to be run just before the `exec` function is"] # [doc = " invoked."] # [doc = ""] # [doc = " The closure is allowed to return an I/O error whose OS error code will"] # [doc = " be communicated back to the parent and returned as an error from when"] # [doc = " the spawn was requested."] # [doc = ""] # [doc = " Multiple closures can be registered and they will be called in order of"] # [doc = " their registration. If a closure returns `Err` then no further closures"] # [doc = " will be called and the spawn operation will immediately return with a"] # [doc = " failure."] # [doc = ""] # [doc = " # Notes and Safety"] # [doc = ""] # [doc = " This closure will be run in the context of the child process after a"] # [doc = " `fork`. This primarily means that any modifications made to memory on"] # [doc = " behalf of this closure will **not** be visible to the parent process."] # [doc = " This is often a very constrained environment where normal operations"] # [doc = " like `malloc`, accessing environment variables through [`std::env`]"] # [doc = " or acquiring a mutex are not guaranteed to work (due to"] # [doc = " other threads perhaps still running when the `fork` was run)."] # [doc = ""] # [doc = " For further details refer to the [POSIX fork() specification]"] # [doc = " and the equivalent documentation for any targeted"] # [doc = " platform, especially the requirements around *async-signal-safety*."] # [doc = ""] # [doc = " This also means that all resources such as file descriptors and"] # [doc = " memory-mapped regions got duplicated. It is your responsibility to make"] # [doc = " sure that the closure does not violate library invariants by making"] # [doc = " invalid use of these duplicates."] # [doc = ""] # [doc = " Panicking in the closure is safe only if all the format arguments for the"] # [doc = " panic message can be safely formatted; this is because although"] # [doc = " `Command` calls [`std::panic::always_abort`](crate::panic::always_abort)"] # [doc = " before calling the pre_exec hook, panic will still try to format the"] # [doc = " panic message."] # [doc = ""] # [doc = " When this closure is run, aspects such as the stdio file descriptors and"] # [doc = " working directory have successfully been changed, so output to these"] # [doc = " locations might not appear where intended."] # [doc = ""] # [doc = " [POSIX fork() specification]:"] # [doc = "     https://pubs.opengroup.org/onlinepubs/9699919799/functions/fork.html"] # [doc = " [`std::env`]: mod@crate::env"] # [stable (feature = "process_pre_exec" , since = "1.34.0")] unsafe fn pre_exec < F > (& mut self , f : F) -> & mut process :: Command where F : FnMut () -> io :: Result < () > + Send + Sync + 'static ; # [doc = " Schedules a closure to be run just before the `exec` function is"] # [doc = " invoked."] # [doc = ""] # [doc = " `before_exec` used to be a safe method, but it needs to be unsafe since the closure may only"] # [doc = " perform operations that are *async-signal-safe*. Hence it got deprecated in favor of the"] # [doc = " unsafe [`pre_exec`]. Meanwhile, Rust gained the ability to make an existing safe method"] # [doc = " fully unsafe in a new edition, which is how `before_exec` became `unsafe`. It still also"] # [doc = " remains deprecated; `pre_exec` should be used instead."] # [doc = ""] # [doc = " [`pre_exec`]: CommandExt::pre_exec"] # [stable (feature = "process_exec" , since = "1.15.0")] # [deprecated (since = "1.37.0" , note = "should be unsafe, use `pre_exec` instead")] # [rustc_deprecated_safe_2024 (audit_that = "the closure is async-signal-safe")] unsafe fn before_exec < F > (& mut self , f : F) -> & mut process :: Command where F : FnMut () -> io :: Result < () > + Send + Sync + 'static , { unsafe { self . pre_exec (f) } } # [doc = " Performs all the required setup by this `Command`, followed by calling"] # [doc = " the `execvp` syscall."] # [doc = ""] # [doc = " On success this function will not return, and otherwise it will return"] # [doc = " an error indicating why the exec (or another part of the setup of the"] # [doc = " `Command`) failed."] # [doc = ""] # [doc = " `exec` not returning has the same implications as calling"] # [doc = " [`process::exit`] – no destructors on the current stack or any other"] # [doc = " thread’s stack will be run. Therefore, it is recommended to only call"] # [doc = " `exec` at a point where it is fine to not run any destructors. Note,"] # [doc = " that the `execvp` syscall independently guarantees that all memory is"] # [doc = " freed and all file descriptors with the `CLOEXEC` option (set by default"] # [doc = " on all file descriptors opened by the standard library) are closed."] # [doc = ""] # [doc = " This function, unlike `spawn`, will **not** `fork` the process to create"] # [doc = " a new child. Like spawn, however, the default behavior for the stdio"] # [doc = " descriptors will be to inherit them from the current process."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " The process may be in a \"broken state\" if this function returns in"] # [doc = " error. For example the working directory, environment variables, signal"] # [doc = " handling settings, various user/group information, or aspects of stdio"] # [doc = " file descriptors may have changed. If a \"transactional spawn\" is"] # [doc = " required to gracefully handle errors it is recommended to use the"] # [doc = " cross-platform `spawn` instead."] # [stable (feature = "process_exec2" , since = "1.9.0")] # [must_use] fn exec (& mut self) -> io :: Error ; # [doc = " Set executable argument"] # [doc = ""] # [doc = " Set the first process argument, `argv[0]`, to something other than the"] # [doc = " default executable path."] # [stable (feature = "process_set_argv0" , since = "1.45.0")] fn arg0 < S > (& mut self , arg : S) -> & mut process :: Command where S : AsRef < OsStr > ; # [doc = " Sets the process group ID (PGID) of the child process. Equivalent to a"] # [doc = " `setpgid` call in the child process, but may be more efficient."] # [doc = ""] # [doc = " Process groups determine which processes receive signals."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Pressing Ctrl-C in a terminal will send SIGINT to all processes in"] # [doc = " the current foreground process group. By spawning the `sleep`"] # [doc = " subprocess in a new process group, it will not receive SIGINT from the"] # [doc = " terminal."] # [doc = ""] # [doc = " The parent process could install a signal handler and manage the"] # [doc = " subprocess on its own terms."] # [doc = ""] # [doc = " A process group ID of 0 will use the process ID as the PGID."] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::process::Command;"] # [doc = " use std::os::unix::process::CommandExt;"] # [doc = ""] # [doc = " Command::new(\"sleep\")"] # [doc = "     .arg(\"10\")"] # [doc = "     .process_group(0)"] # [doc = "     .spawn()?"] # [doc = "     .wait()?;"] # [doc = " #"] # [doc = " # Ok::<_, Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [stable (feature = "process_set_process_group" , since = "1.64.0")] fn process_group (& mut self , pgroup : i32) -> & mut process :: Command ; # [doc = " Set the root of the child process. This calls `chroot` in the child process before executing"] # [doc = " the command."] # [doc = ""] # [doc = " This happens before changing to the directory specified with"] # [doc = " [`process::Command::current_dir`], and that directory will be relative to the new root."] # [doc = ""] # [doc = " If no directory has been specified with [`process::Command::current_dir`], this will set the"] # [doc = " directory to `/`, to avoid leaving the current directory outside the chroot. (This is an"] # [doc = " intentional difference from the underlying `chroot` system call.)"] # [unstable (feature = "process_chroot" , issue = "141298")] fn chroot < P : AsRef < Path > > (& mut self , dir : P) -> & mut process :: Command ; # [unstable (feature = "process_setsid" , issue = "105376")] fn setsid (& mut self , setsid : bool) -> & mut process :: Command ; }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl CommandExt for process :: Command { fn uid (& mut self , id : UserId) -> & mut process :: Command { self . as_inner_mut () . uid (id) ; self } fn gid (& mut self , id : GroupId) -> & mut process :: Command { self . as_inner_mut () . gid (id) ; self } fn groups (& mut self , groups : & [GroupId]) -> & mut process :: Command { self . as_inner_mut () . groups (groups) ; self } unsafe fn pre_exec < F > (& mut self , f : F) -> & mut process :: Command where F : FnMut () -> io :: Result < () > + Send + Sync + 'static , { self . as_inner_mut () . pre_exec (Box :: new (f)) ; self } fn exec (& mut self) -> io :: Error { self . as_inner_mut () . exec (sys :: process :: Stdio :: Inherit) } fn arg0 < S > (& mut self , arg : S) -> & mut process :: Command where S : AsRef < OsStr > , { self . as_inner_mut () . set_arg_0 (arg . as_ref ()) ; self } fn process_group (& mut self , pgroup : i32) -> & mut process :: Command { self . as_inner_mut () . pgroup (pgroup) ; self } fn chroot < P : AsRef < Path > > (& mut self , dir : P) -> & mut process :: Command { self . as_inner_mut () . chroot (dir . as_ref ()) ; self } fn setsid (& mut self , setsid : bool) -> & mut process :: Command { self . as_inner_mut () . setsid (setsid) ; self } }}}
mkitem!{mktrait!{# [doc = " Unix-specific extensions to [`process::ExitStatus`] and"] # [doc = " [`ExitStatusError`](process::ExitStatusError)."] # [doc = ""] # [doc = " On Unix, `ExitStatus` **does not necessarily represent an exit status**, as"] # [doc = " passed to the `_exit` system call or returned by"] # [doc = " [`ExitStatus::code()`](crate::process::ExitStatus::code).  It represents **any wait status**"] # [doc = " as returned by one of the `wait` family of system"] # [doc = " calls."] # [doc = ""] # [doc = " A Unix wait status (a Rust `ExitStatus`) can represent a Unix exit status, but can also"] # [doc = " represent other kinds of process event."] # [doc = ""] # [doc = " This trait is sealed: it cannot be implemented outside the standard library."] # [doc = " This is so that future additional methods are not breaking changes."] # [stable (feature = "rust1" , since = "1.0.0")] pub trait ExitStatusExt : Sealed { # [doc = " Creates a new `ExitStatus` or `ExitStatusError` from the raw underlying integer status"] # [doc = " value from `wait`"] # [doc = ""] # [doc = " The value should be a **wait status, not an exit status**."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics on an attempt to make an `ExitStatusError` from a wait status of `0`."] # [doc = ""] # [doc = " Making an `ExitStatus` always succeeds and never panics."] # [stable (feature = "exit_status_from" , since = "1.12.0")] fn from_raw (raw : i32) -> Self ; # [doc = " If the process was terminated by a signal, returns that signal."] # [doc = ""] # [doc = " In other words, if `WIFSIGNALED`, this returns `WTERMSIG`."] # [stable (feature = "rust1" , since = "1.0.0")] fn signal (& self) -> Option < i32 > ; # [doc = " If the process was terminated by a signal, says whether it dumped core."] # [stable (feature = "unix_process_wait_more" , since = "1.58.0")] fn core_dumped (& self) -> bool ; # [doc = " If the process was stopped by a signal, returns that signal."] # [doc = ""] # [doc = " In other words, if `WIFSTOPPED`, this returns `WSTOPSIG`.  This is only possible if the status came from"] # [doc = " a `wait` system call which was passed `WUNTRACED`, and was then converted into an `ExitStatus`."] # [stable (feature = "unix_process_wait_more" , since = "1.58.0")] fn stopped_signal (& self) -> Option < i32 > ; # [doc = " Whether the process was continued from a stopped status."] # [doc = ""] # [doc = " Ie, `WIFCONTINUED`.  This is only possible if the status came from a `wait` system call"] # [doc = " which was passed `WCONTINUED`, and was then converted into an `ExitStatus`."] # [stable (feature = "unix_process_wait_more" , since = "1.58.0")] fn continued (& self) -> bool ; # [doc = " Returns the underlying raw `wait` status."] # [doc = ""] # [doc = " The returned integer is a **wait status, not an exit status**."] # [stable (feature = "unix_process_wait_more" , since = "1.58.0")] fn into_raw (self) -> i32 ; }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl ExitStatusExt for process :: ExitStatus { fn from_raw (raw : i32) -> Self { process :: ExitStatus :: from_inner (From :: from (raw)) } fn signal (& self) -> Option < i32 > { self . as_inner () . signal () } fn core_dumped (& self) -> bool { self . as_inner () . core_dumped () } fn stopped_signal (& self) -> Option < i32 > { self . as_inner () . stopped_signal () } fn continued (& self) -> bool { self . as_inner () . continued () } fn into_raw (self) -> i32 { self . as_inner () . into_raw () . into () } }}}
mkitem!{mkimpl!{# [unstable (feature = "exit_status_error" , issue = "84908")] impl ExitStatusExt for process :: ExitStatusError { fn from_raw (raw : i32) -> Self { process :: ExitStatus :: from_raw (raw) . exit_ok () . expect_err ("<ExitStatusError as ExitStatusExt>::from_raw(0) but zero is not an error") } fn signal (& self) -> Option < i32 > { self . into_status () . signal () } fn core_dumped (& self) -> bool { self . into_status () . core_dumped () } fn stopped_signal (& self) -> Option < i32 > { self . into_status () . stopped_signal () } fn continued (& self) -> bool { self . into_status () . continued () } fn into_raw (self) -> i32 { self . into_status () . into_raw () } }}}
mkitem!{mktrait!{# [unstable (feature = "unix_send_signal" , issue = "141975")] pub trait ChildExt : Sealed { # [doc = " Sends a signal to a child process."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will return an error if the signal is invalid. The integer values associated"] # [doc = " with signals are implementation-specific, so it's encouraged to use a crate that provides"] # [doc = " posix bindings."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " #![feature(unix_send_signal)]"] # [doc = ""] # [doc = " use std::{io, os::unix::process::ChildExt, process::{Command, Stdio}};"] # [doc = ""] # [doc = " use libc::SIGTERM;"] # [doc = ""] # [doc = " fn main() -> io::Result<()> {"] # [doc = "     let child = Command::new(\"cat\").stdin(Stdio::piped()).spawn()?;"] # [doc = "     child.send_signal(SIGTERM)?;"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] fn send_signal (& self , signal : i32) -> io :: Result < () > ; }}}
mkitem!{mkimpl!{# [unstable (feature = "unix_send_signal" , issue = "141975")] impl ChildExt for process :: Child { fn send_signal (& self , signal : i32) -> io :: Result < () > { self . handle . send_signal (signal) } }}}
mkitem!{mkimpl!{# [stable (feature = "process_extensions" , since = "1.2.0")] impl FromRawFd for process :: Stdio { # [inline] unsafe fn from_raw_fd (fd : RawFd) -> process :: Stdio { let fd = sys :: fd :: FileDesc :: from_raw_fd (fd) ; let io = sys :: process :: Stdio :: Fd (fd) ; process :: Stdio :: from_inner (io) } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl From < OwnedFd > for process :: Stdio { # [doc = " Takes ownership of a file descriptor and returns a [`Stdio`](process::Stdio)"] # [doc = " that can attach a stream to it."] # [inline] fn from (fd : OwnedFd) -> process :: Stdio { let fd = sys :: fd :: FileDesc :: from_inner (fd) ; let io = sys :: process :: Stdio :: Fd (fd) ; process :: Stdio :: from_inner (io) } }}}
mkitem!{mkimpl!{# [stable (feature = "process_extensions" , since = "1.2.0")] impl AsRawFd for process :: ChildStdin { # [inline] fn as_raw_fd (& self) -> RawFd { self . as_inner () . as_raw_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "process_extensions" , since = "1.2.0")] impl AsRawFd for process :: ChildStdout { # [inline] fn as_raw_fd (& self) -> RawFd { self . as_inner () . as_raw_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "process_extensions" , since = "1.2.0")] impl AsRawFd for process :: ChildStderr { # [inline] fn as_raw_fd (& self) -> RawFd { self . as_inner () . as_raw_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "into_raw_os" , since = "1.4.0")] impl IntoRawFd for process :: ChildStdin { # [inline] fn into_raw_fd (self) -> RawFd { self . into_inner () . into_inner () . into_raw_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "into_raw_os" , since = "1.4.0")] impl IntoRawFd for process :: ChildStdout { # [inline] fn into_raw_fd (self) -> RawFd { self . into_inner () . into_inner () . into_raw_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "into_raw_os" , since = "1.4.0")] impl IntoRawFd for process :: ChildStderr { # [inline] fn into_raw_fd (self) -> RawFd { self . into_inner () . into_inner () . into_raw_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl AsFd for crate :: process :: ChildStdin { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { self . as_inner () . as_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl From < crate :: process :: ChildStdin > for OwnedFd { # [doc = " Takes ownership of a [`ChildStdin`](crate::process::ChildStdin)'s file descriptor."] # [inline] fn from (child_stdin : crate :: process :: ChildStdin) -> OwnedFd { child_stdin . into_inner () . into_inner () . into_inner () } }}}
mkitem!{mkimpl!{# [doc = " Creates a `ChildStdin` from the provided `OwnedFd`."] # [doc = ""] # [doc = " The provided file descriptor must point to a pipe"] # [doc = " with the `CLOEXEC` flag set."] # [stable (feature = "child_stream_from_fd" , since = "1.74.0")] impl From < OwnedFd > for process :: ChildStdin { # [inline] fn from (fd : OwnedFd) -> process :: ChildStdin { let fd = sys :: fd :: FileDesc :: from_inner (fd) ; let pipe = sys :: pipe :: AnonPipe :: from_inner (fd) ; process :: ChildStdin :: from_inner (pipe) } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl AsFd for crate :: process :: ChildStdout { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { self . as_inner () . as_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl From < crate :: process :: ChildStdout > for OwnedFd { # [doc = " Takes ownership of a [`ChildStdout`](crate::process::ChildStdout)'s file descriptor."] # [inline] fn from (child_stdout : crate :: process :: ChildStdout) -> OwnedFd { child_stdout . into_inner () . into_inner () . into_inner () } }}}
mkitem!{mkimpl!{# [doc = " Creates a `ChildStdout` from the provided `OwnedFd`."] # [doc = ""] # [doc = " The provided file descriptor must point to a pipe"] # [doc = " with the `CLOEXEC` flag set."] # [stable (feature = "child_stream_from_fd" , since = "1.74.0")] impl From < OwnedFd > for process :: ChildStdout { # [inline] fn from (fd : OwnedFd) -> process :: ChildStdout { let fd = sys :: fd :: FileDesc :: from_inner (fd) ; let pipe = sys :: pipe :: AnonPipe :: from_inner (fd) ; process :: ChildStdout :: from_inner (pipe) } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl AsFd for crate :: process :: ChildStderr { # [inline] fn as_fd (& self) -> BorrowedFd < '_ > { self . as_inner () . as_fd () } }}}
mkitem!{mkimpl!{# [stable (feature = "io_safety" , since = "1.63.0")] impl From < crate :: process :: ChildStderr > for OwnedFd { # [doc = " Takes ownership of a [`ChildStderr`](crate::process::ChildStderr)'s file descriptor."] # [inline] fn from (child_stderr : crate :: process :: ChildStderr) -> OwnedFd { child_stderr . into_inner () . into_inner () . into_inner () } }}}
mkitem!{mkimpl!{# [doc = " Creates a `ChildStderr` from the provided `OwnedFd`."] # [doc = ""] # [doc = " The provided file descriptor must point to a pipe"] # [doc = " with the `CLOEXEC` flag set."] # [stable (feature = "child_stream_from_fd" , since = "1.74.0")] impl From < OwnedFd > for process :: ChildStderr { # [inline] fn from (fd : OwnedFd) -> process :: ChildStderr { let fd = sys :: fd :: FileDesc :: from_inner (fd) ; let pipe = sys :: pipe :: AnonPipe :: from_inner (fd) ; process :: ChildStderr :: from_inner (pipe) } }}}

macro_rules! parent_id_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function parent_id in module {}", module_path!());
    };
}

mkfn!{
    parent_id_introspect!();
    # [doc = " Returns the OS-assigned process identifier associated with this process's parent."] # [must_use] # [stable (feature = "unix_ppid" , since = "1.27.0")] pub fn parent_id () -> u32 { crate :: sys :: os :: getppid () }
}