mkuse!{# [allow (deprecated)] use crate :: os :: unix :: raw :: pthread_t ;}
mkuse!{use crate :: sys_common :: { AsInner , IntoInner } ;}
mkuse!{use crate :: thread :: JoinHandle ;}
mkitem!{# [stable (feature = "thread_extensions" , since = "1.9.0")] # [allow (deprecated)] pub type RawPthread = pthread_t ;}
mkitem!{mktrait!{# [doc = " Unix-specific extensions to [`JoinHandle`]."] # [stable (feature = "thread_extensions" , since = "1.9.0")] pub trait JoinHandleExt { # [doc = " Extracts the raw pthread_t without taking ownership"] # [stable (feature = "thread_extensions" , since = "1.9.0")] fn as_pthread_t (& self) -> RawPthread ; # [doc = " Consumes the thread, returning the raw pthread_t"] # [doc = ""] # [doc = " This function **transfers ownership** of the underlying pthread_t to"] # [doc = " the caller. Callers are then the unique owners of the pthread_t and"] # [doc = " must either detach or join the pthread_t once it's no longer needed."] # [stable (feature = "thread_extensions" , since = "1.9.0")] fn into_pthread_t (self) -> RawPthread ; }}}
mkitem!{mkimpl!{# [stable (feature = "thread_extensions" , since = "1.9.0")] impl < T > JoinHandleExt for JoinHandle < T > { fn as_pthread_t (& self) -> RawPthread { self . as_inner () . id () as RawPthread } fn into_pthread_t (self) -> RawPthread { self . into_inner () . into_id () as RawPthread } }}}