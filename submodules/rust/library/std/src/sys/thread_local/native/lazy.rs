use split_decls_genesis :: ourprelude :: * ; use crate :: cell :: { Cell , UnsafeCell } ; use crate :: mem :: MaybeUninit ; use crate :: ptr ; use crate :: sys :: thread_local :: { abort_on_dtor_unwind , destructors } ; pub unsafe trait DestroyedState : Sized + Copy { fn register_dtor < T > (s : & Storage < T , Self >) ; } unsafe impl DestroyedState for ! { fn register_dtor < T > (_ : & Storage < T , ! >) { } } unsafe impl DestroyedState for () { fn register_dtor < T > (s : & Storage < T , () >) { unsafe { destructors :: register (ptr :: from_ref (s) . cast_mut () . cast () , destroy :: < T >) ; } } } #[derive (Copy , Clone)] enum State < D > { Uninitialized , Alive , Destroyed (D) , } #[allow (missing_debug_implementations)] pub struct Storage < T , D > { state : Cell < State < D > > , value : UnsafeCell < MaybeUninit < T > > , } impl < T , D > Storage < T , D > where D : DestroyedState , { pub const fn new () -> Storage < T , D > { Storage { state : Cell :: new (State :: Uninitialized) , value : UnsafeCell :: new (MaybeUninit :: uninit ()) , } } #[doc = " Gets a pointer to the TLS value, potentially initializing it with the"] #[doc = " provided parameters. If the TLS variable has been destroyed, a null"] #[doc = " pointer is returned."] #[doc = ""] #[doc = " The resulting pointer may not be used after reentrant inialialization"] #[doc = " or thread destruction has occurred."] #[doc = ""] #[doc = " # Safety"] #[doc = " The `self` reference must remain valid until the TLS destructor is run."] #[inline] pub unsafe fn get_or_init (& self , i : Option < & mut Option < T > > , f : impl FnOnce () -> T) -> * const T { if let State :: Alive = self . state . get () { self . value . get () . cast () } else { unsafe { self . get_or_init_slow (i , f) } } } #[doc = " # Safety"] #[doc = " The `self` reference must remain valid until the TLS destructor is run."] #[cold] unsafe fn get_or_init_slow (& self , i : Option < & mut Option < T > > , f : impl FnOnce () -> T ,) -> * const T { match self . state . get () { State :: Uninitialized => { } State :: Alive => return self . value . get () . cast () , State :: Destroyed (_) => return ptr :: null () , } let v = i . and_then (Option :: take) . unwrap_or_else (f) ; let mut old_value = unsafe { self . value . get () . replace (MaybeUninit :: new (v)) } ; match self . state . replace (State :: Alive) { State :: Uninitialized => D :: register_dtor (self) , State :: Alive => unsafe { old_value . assume_init_drop () } , State :: Destroyed (_) => unreachable ! () , } self . value . get () . cast () } } #[doc = " Transition an `Alive` TLS variable into the `Destroyed` state, dropping its"] #[doc = " value."] #[doc = ""] #[doc = " # Safety"] #[doc = " * Must only be called at thread destruction."] #[doc = " * `ptr` must point to an instance of `Storage<T, ()>` and be valid for"] #[doc = "   accessing that instance."] unsafe extern "C" fn destroy < T > (ptr : * mut u8) { abort_on_dtor_unwind (| | { let storage = unsafe { & * (ptr as * const Storage < T , () >) } ; if let State :: Alive = storage . state . replace (State :: Destroyed (())) { unsafe { (* storage . value . get ()) . assume_init_drop () } } }) }