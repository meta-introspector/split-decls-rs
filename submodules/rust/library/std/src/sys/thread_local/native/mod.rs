# ! [doc = " Thread local support for platforms with native TLS."] # ! [doc = ""] # ! [doc = " To achieve the best performance, we choose from four different types for"] # ! [doc = " the TLS variable, depending on the method of initialization used (`const`"] # ! [doc = " or lazy) and the drop requirements of the stored type:"] # ! [doc = ""] # ! [doc = " |         | `Drop`               | `!Drop`             |"] # ! [doc = " |--------:|:--------------------:|:-------------------:|"] # ! [doc = " | `const` | `EagerStorage<T>`    | `T`                 |"] # ! [doc = " | lazy    | `LazyStorage<T, ()>` | `LazyStorage<T, !>` |"] # ! [doc = ""] # ! [doc = " For `const` initialization and `!Drop` types, we simply use `T` directly,"] # ! [doc = " but for other situations, we implement a state machine to handle"] # ! [doc = " initialization of the variable and its destructor and destruction."] # ! [doc = " Upon accessing the TLS variable, the current state is compared:"] # ! [doc = ""] # ! [doc = " 1. If the state is `Initial`, initialize the storage, transition the state"] # ! [doc = "    to `Alive` and (if applicable) register the destructor, and return a"] # ! [doc = "    reference to the value."] # ! [doc = " 2. If the state is `Alive`, initialization was previously completed, so"] # ! [doc = "    return a reference to the value."] # ! [doc = " 3. If the state is `Destroyed`, the destructor has been run already, so"] # ! [doc = "    return [`None`]."] # ! [doc = ""] # ! [doc = " The TLS destructor sets the state to `Destroyed` and drops the current value."] # ! [doc = ""] # ! [doc = " To simplify the code, we make `LazyStorage` generic over the destroyed state"] # ! [doc = " and use the `!` type (never type) as type parameter for `!Drop` types. This"] # ! [doc = " eliminates the `Destroyed` state for these values, which can allow more niche"] # ! [doc = " optimizations to occur for the `State` enum. For `Drop` types, `()` is used."] use split_decls_genesis :: ourprelude :: * ; use crate :: cell :: Cell ; use crate :: ptr ; mod eager ; mod lazy ; pub use eager :: Storage as EagerStorage ; pub use lazy :: Storage as LazyStorage ; #[doc (hidden)] #[allow_internal_unstable (thread_local_internals , cfg_target_thread_local , thread_local , never_type)] #[allow_internal_unsafe] #[unstable (feature = "thread_local_internals" , issue = "none")] #[rustc_macro_transparency = "semitransparent"] pub macro thread_local_inner { (@ key $ t : ty , const $ init : expr) => { { const __INIT : $ t = $ init ; unsafe { $ crate :: thread :: LocalKey :: new (const { if $ crate :: mem :: needs_drop ::<$ t > () { | _ | { #[thread_local] static VAL : $ crate :: thread :: local_impl :: EagerStorage <$ t > = $ crate :: thread :: local_impl :: EagerStorage :: new (__INIT) ; VAL . get () } } else { | _ | { #[thread_local] static VAL : $ t = __INIT ; & VAL } } }) } } } , (@ key $ t : ty , $ init : expr) => { { #[inline] fn __init () -> $ t { $ init } unsafe { $ crate :: thread :: LocalKey :: new (const { if $ crate :: mem :: needs_drop ::<$ t > () { | init | { #[thread_local] static VAL : $ crate :: thread :: local_impl :: LazyStorage <$ t , () > = $ crate :: thread :: local_impl :: LazyStorage :: new () ; VAL . get_or_init (init , __init) } } else { | init | { #[thread_local] static VAL : $ crate :: thread :: local_impl :: LazyStorage <$ t , !> = $ crate :: thread :: local_impl :: LazyStorage :: new () ; VAL . get_or_init (init , __init) } } }) } } } , ($ (#[$ attr : meta]) * $ vis : vis $ name : ident , $ t : ty , $ ($ init : tt) *) => { $ (#[$ attr]) * $ vis const $ name : $ crate :: thread :: LocalKey <$ t > = $ crate :: thread :: local_impl :: thread_local_inner ! (@ key $ t , $ ($ init) *) ; } , } #[rustc_macro_transparency = "semitransparent"] pub (crate) macro local_pointer { () => { } , ($ vis : vis static $ name : ident ; $ ($ rest : tt) *) => { #[thread_local] $ vis static $ name : $ crate :: sys :: thread_local :: LocalPointer = $ crate :: sys :: thread_local :: LocalPointer :: __new () ; $ crate :: sys :: thread_local :: local_pointer ! { $ ($ rest) * } } , } pub (crate) struct LocalPointer { p : Cell < * mut () > , } impl LocalPointer { pub const fn __new () -> LocalPointer { LocalPointer { p : Cell :: new (ptr :: null_mut ()) } } pub fn get (& self) -> * mut () { self . p . get () } pub fn set (& self , p : * mut ()) { self . p . set (p) } }