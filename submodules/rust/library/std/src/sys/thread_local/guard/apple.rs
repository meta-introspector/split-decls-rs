# ! [doc = " macOS allows registering destructors through _tlv_atexit. But since calling"] # ! [doc = " it while TLS destructors are running is UB, we still need to keep our own"] # ! [doc = " list of destructors."] use split_decls_genesis :: ourprelude :: * ; use crate :: cell :: Cell ; use crate :: ptr ; use crate :: sys :: thread_local :: destructors ; pub fn enable () { #[thread_local] static REGISTERED : Cell < bool > = Cell :: new (false) ; unsafe extern "C" { fn _tlv_atexit (dtor : unsafe extern "C" fn (* mut u8) , arg : * mut u8) ; } if ! REGISTERED . replace (true) { unsafe { _tlv_atexit (run_dtors , ptr :: null_mut ()) ; } } unsafe extern "C" fn run_dtors (_ : * mut u8) { unsafe { destructors :: run () ; crate :: rt :: thread_cleanup () ; } } }