mkuse!{use fortanix_sgx_abi :: * ;}
mkuse!{use super :: super :: mem :: { is_enclave_range , is_user_range } ;}
mkuse!{use crate :: arch :: asm ;}
mkuse!{use crate :: cell :: UnsafeCell ;}
mkuse!{use crate :: convert :: TryInto ;}
mkuse!{use crate :: mem :: { self , ManuallyDrop , MaybeUninit } ;}
mkuse!{use crate :: ops :: { CoerceUnsized , Deref , DerefMut , Index , IndexMut } ;}
mkuse!{use crate :: pin :: PinCoerceUnsized ;}
mkuse!{use crate :: ptr :: { self , NonNull } ;}
mkuse!{use crate :: slice :: SliceIndex ;}
mkuse!{use crate :: { cmp , intrinsics , slice } ;}
mkitem!{mktrait!{# [doc = " A type that can be safely read from or written to userspace."] # [doc = ""] # [doc = " Non-exhaustive list of specific requirements for reading and writing:"] # [doc = " * **Type is `Copy`** (and therefore also not `Drop`). Copies will be"] # [doc = "   created when copying from/to userspace. Destructors will not be called."] # [doc = " * **No references or Rust-style owned pointers** (`Vec`, `Arc`, etc.). When"] # [doc = "   reading from userspace, references into enclave memory must not be"] # [doc = "   created. Also, only enclave memory is considered managed by the Rust"] # [doc = "   compiler's static analysis. When reading from userspace, there can be no"] # [doc = "   guarantee that the value correctly adheres to the expectations of the"] # [doc = "   type. When writing to userspace, memory addresses of data in enclave"] # [doc = "   memory must not be leaked for confidentiality reasons. `User` and"] # [doc = "   `UserRef` are also not allowed for the same reasons."] # [doc = " * **No fat pointers.** When reading from userspace, the size or vtable"] # [doc = "   pointer could be automatically interpreted and used by the code. When"] # [doc = "   writing to userspace, memory addresses of data in enclave memory (such"] # [doc = "   as vtable pointers) must not be leaked for confidentiality reasons."] # [doc = ""] # [doc = " Non-exhaustive list of specific requirements for reading from userspace:"] # [doc = " * **Any bit pattern is valid** for this type (no `enum`s). There can be no"] # [doc = "   guarantee that the value correctly adheres to the expectations of the"] # [doc = "   type, so any value must be valid for this type."] # [doc = ""] # [doc = " Non-exhaustive list of specific requirements for writing to userspace:"] # [doc = " * **No pointers to enclave memory.** Memory addresses of data in enclave"] # [doc = "   memory must not be leaked for confidentiality reasons."] # [doc = " * **No internal padding.** Padding might contain previously-initialized"] # [doc = "   secret data stored at that memory location and must not be leaked for"] # [doc = "   confidentiality reasons."] # [unstable (feature = "sgx_platform" , issue = "56975")] pub unsafe trait UserSafeSized : Copy + Sized { }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl UserSafeSized for u8 { }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl < T > UserSafeSized for FifoDescriptor < T > { }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl UserSafeSized for ByteBuffer { }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl UserSafeSized for Usercall { }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl UserSafeSized for Return { }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl UserSafeSized for Cancel { }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl < T : UserSafeSized > UserSafeSized for [T ; 2] { }}}
mkitem!{mktrait!{# [doc = " A type that can be represented in memory as one or more `UserSafeSized`s."] # [unstable (feature = "sgx_platform" , issue = "56975")] pub unsafe trait UserSafe { # [doc = " Equivalent to `align_of::<Self>`."] fn align_of () -> usize ; # [doc = " Constructs a pointer to `Self` given a memory range in user space."] # [doc = ""] # [doc = " N.B., this takes a size, not a length!"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure the memory range is in user memory, is the"] # [doc = " correct size and is correctly aligned and points to the right type."] unsafe fn from_raw_sized_unchecked (ptr : * mut u8 , size : usize) -> * mut Self ; # [doc = " Constructs a pointer to `Self` given a memory range."] # [doc = ""] # [doc = " N.B., this takes a size, not a length!"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure the memory range points to the correct type."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if:"] # [doc = ""] # [doc = " * the pointer is not aligned."] # [doc = " * the pointer is null."] # [doc = " * the pointed-to range does not fit in the address space."] # [doc = " * the pointed-to range is not in user memory."] unsafe fn from_raw_sized (ptr : * mut u8 , size : usize) -> NonNull < Self > { assert ! (ptr . wrapping_add (size) >= ptr) ; let ret = unsafe { Self :: from_raw_sized_unchecked (ptr , size) } ; unsafe { Self :: check_ptr (ret) ; NonNull :: new_unchecked (ret as _) } } # [doc = " Checks if a pointer may point to `Self` in user memory."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure the memory range points to the correct type and"] # [doc = " length (if this is a slice)."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if:"] # [doc = ""] # [doc = " * the pointer is not aligned."] # [doc = " * the pointer is null."] # [doc = " * the pointed-to range is not in user memory."] unsafe fn check_ptr (ptr : * const Self) { let is_aligned = | p : * const u8 | -> bool { p . is_aligned_to (Self :: align_of ()) } ; assert ! (is_aligned (ptr as * const u8)) ; assert ! (is_user_range (ptr as _ , size_of_val (unsafe { &* ptr }))) ; assert ! (! ptr . is_null ()) ; } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl < T : UserSafeSized > UserSafe for T { fn align_of () -> usize { align_of :: < T > () } unsafe fn from_raw_sized_unchecked (ptr : * mut u8 , size : usize) -> * mut Self { assert_eq ! (size , size_of ::< T > ()) ; ptr as _ } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl < T : UserSafeSized > UserSafe for [T] { fn align_of () -> usize { align_of :: < T > () } # [doc = " # Safety"] # [doc = " Behavior is undefined if any of these conditions are violated:"] # [doc = " * `ptr` must be [valid] for writes of `size` many bytes, and it must be"] # [doc = "   properly aligned."] # [doc = ""] # [doc = " [valid]: core::ptr#safety"] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if:"] # [doc = ""] # [doc = " * the element size is not a factor of the size"] unsafe fn from_raw_sized_unchecked (ptr : * mut u8 , size : usize) -> * mut Self { let elem_size = size_of :: < T > () ; assert_eq ! (size % elem_size , 0) ; let len = size / elem_size ; unsafe { slice :: from_raw_parts_mut (ptr as _ , len) } } }}}
mkitem!{mkstruct!{# [doc = " A reference to some type in userspace memory. `&UserRef<T>` is equivalent"] # [doc = " to `&T` in enclave memory. Access to the memory is only allowed by copying"] # [doc = " to avoid TOCTTOU issues. After copying, code should make sure to completely"] # [doc = " check the value before use."] # [doc = ""] # [doc = " It is also possible to obtain a mutable reference `&mut UserRef<T>`. Unlike"] # [doc = " regular mutable references, these are not exclusive. Userspace may always"] # [doc = " write to the backing memory at any time, so it can't be assumed that there"] # [doc = " the pointed-to memory is uniquely borrowed. The two different reference types"] # [doc = " are used solely to indicate intent: a mutable reference is for writing to"] # [doc = " user memory, an immutable reference for reading from user memory."] # [unstable (feature = "sgx_platform" , issue = "56975")] # [repr (transparent)] pub struct UserRef < T : ? Sized > (UnsafeCell < T >) ;}}
mkitem!{mkstruct!{# [doc = " An owned type in userspace memory. `User<T>` is equivalent to `Box<T>` in"] # [doc = " enclave memory. Access to the memory is only allowed by copying to avoid"] # [doc = " TOCTTOU issues. The user memory will be freed when the value is dropped."] # [doc = " After copying, code should make sure to completely check the value before"] # [doc = " use."] # [unstable (feature = "sgx_platform" , issue = "56975")] pub struct User < T : UserSafe + ? Sized > (NonNull < UserRef < T > >) ;}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl < T : UserSafeSized > Send for User < T > { }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl < T : UserSafeSized > Send for User < [T] > { }}}
mkitem!{mktrait!{trait NewUserRef < T : ? Sized > { unsafe fn new_userref (v : T) -> Self ; }}}
mkitem!{mkimpl!{impl < T : ? Sized > NewUserRef < * mut T > for NonNull < UserRef < T > > { unsafe fn new_userref (v : * mut T) -> Self { unsafe { NonNull :: new_unchecked (v as _) } } }}}
mkitem!{mkimpl!{impl < T : ? Sized > NewUserRef < NonNull < T > > for NonNull < UserRef < T > > { unsafe fn new_userref (v : NonNull < T >) -> Self { unsafe { NonNull :: new_userref (v . as_ptr ()) } } }}}
mkitem!{mktrait!{# [doc = " A type which can a destination for safely copying from userspace."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Requires that `T` and `Self` have identical layouts."] # [unstable (feature = "sgx_platform" , issue = "56975")] pub unsafe trait UserSafeCopyDestination < T : ? Sized > { # [doc = " Returns a pointer for writing to the value."] fn as_mut_ptr (& mut self) -> * mut T ; }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl < T > UserSafeCopyDestination < T > for T { fn as_mut_ptr (& mut self) -> * mut T { self as _ } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl < T > UserSafeCopyDestination < [T] > for [T] { fn as_mut_ptr (& mut self) -> * mut [T] { self as _ } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl < T > UserSafeCopyDestination < T > for MaybeUninit < T > { fn as_mut_ptr (& mut self) -> * mut T { self as * mut Self as _ } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] unsafe impl < T > UserSafeCopyDestination < [T] > for [MaybeUninit < T >] { fn as_mut_ptr (& mut self) -> * mut [T] { self as * mut Self as _ } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < T : ? Sized > User < T > where T : UserSafe , { fn new_uninit_bytes (size : usize) -> Self { unsafe { let ptr = if size > 0 { let alignment = cmp :: max (T :: align_of () , 8) ; rtunwrap ! (Ok , super :: alloc (size , alignment)) as _ } else { T :: align_of () as _ } ; if let Ok (v) = crate :: panic :: catch_unwind (| | T :: from_raw_sized (ptr , size)) { User (NonNull :: new_userref (v)) } else { rtabort ! ("Got invalid pointer from alloc() usercall") } } } # [doc = " Copies `val` into freshly allocated space in user memory."] pub fn new_from_enclave (val : & T) -> Self { unsafe { let mut user = Self :: new_uninit_bytes (size_of_val (val)) ; user . copy_from_enclave (val) ; user } } # [doc = " Creates an owned `User<T>` from a raw pointer."] # [doc = ""] # [doc = " # Safety"] # [doc = " The caller must ensure `ptr` points to `T`, is freeable with the `free`"] # [doc = " usercall and the alignment of `T`, and is uniquely owned."] # [doc = ""] # [doc = " # Panics"] # [doc = " This function panics if:"] # [doc = ""] # [doc = " * The pointer is not aligned"] # [doc = " * The pointer is null"] # [doc = " * The pointed-to range is not in user memory"] pub unsafe fn from_raw (ptr : * mut T) -> Self { unsafe { T :: check_ptr (ptr) } ; User (unsafe { NonNull :: new_userref (ptr) }) } # [doc = " Converts this value into a raw pointer. The value will no longer be"] # [doc = " automatically freed."] pub fn into_raw (self) -> * mut T { ManuallyDrop :: new (self) . 0 . as_ptr () as _ } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < T > User < T > where T : UserSafe , { # [doc = " Allocates space for `T` in user memory."] pub fn uninitialized () -> Self { Self :: new_uninit_bytes (size_of :: < T > ()) } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < T > User < [T] > where [T] : UserSafe , { # [doc = " Allocates space for a `[T]` of `n` elements in user memory."] pub fn uninitialized (n : usize) -> Self { Self :: new_uninit_bytes (n * size_of :: < T > ()) } # [doc = " Creates an owned `User<[T]>` from a raw thin pointer and a slice length."] # [doc = ""] # [doc = " # Safety"] # [doc = " The caller must ensure `ptr` points to `len` elements of `T`, is"] # [doc = " freeable with the `free` usercall and the alignment of `T`, and is"] # [doc = " uniquely owned."] # [doc = ""] # [doc = " # Panics"] # [doc = " This function panics if:"] # [doc = ""] # [doc = " * The pointer is not aligned"] # [doc = " * The pointer is null"] # [doc = " * The pointed-to range does not fit in the address space"] # [doc = " * The pointed-to range is not in user memory"] pub unsafe fn from_raw_parts (ptr : * mut T , len : usize) -> Self { User (unsafe { NonNull :: new_userref (< [T] > :: from_raw_sized (ptr as _ , len * size_of :: < T > ())) }) } }}}

macro_rules! u64_align_to_guaranteed_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function u64_align_to_guaranteed in module {}", module_path!());
    };
}

mkfn!{
    u64_align_to_guaranteed_introspect!();
    # [doc = " Divide the slice `(ptr, len)` into three parts, where the middle part is"] # [doc = " aligned to `u64`."] # [doc = ""] # [doc = " The return values `(prefix_len, mid_len, suffix_len)` add back up to `len`."] # [doc = " The return values are such that the memory region `(ptr + prefix_len,"] # [doc = " mid_len)` is the largest possible region where `ptr + prefix_len` is aligned"] # [doc = " to `u64` and `mid_len` is a multiple of the byte size of `u64`. This means"] # [doc = " that `prefix_len` and `suffix_len` are guaranteed to be less than the byte"] # [doc = " size of `u64`, and that `(ptr, prefix_len)` and `(ptr + prefix_len +"] # [doc = " mid_len, suffix_len)` don't straddle an alignment boundary."] fn u64_align_to_guaranteed (ptr : * const u8 , mut len : usize) -> (usize , usize , usize) { const QWORD_SIZE : usize = size_of :: < u64 > () ; let offset = ptr as usize % QWORD_SIZE ; let prefix_len = if intrinsics :: unlikely (offset > 0) { QWORD_SIZE - offset } else { 0 } ; len = match len . checked_sub (prefix_len) { Some (remaining_len) => remaining_len , None => return (len , 0 , 0) , } ; let suffix_len = len % QWORD_SIZE ; len -= suffix_len ; (prefix_len , len , suffix_len) }
}

macro_rules! copy_quadwords_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function copy_quadwords in module {}", module_path!());
    };
}

mkfn!{
    copy_quadwords_introspect!();
    unsafe fn copy_quadwords (src : * const u8 , dst : * mut u8 , len : usize) { unsafe { asm ! ("rep movsq (%rsi), (%rdi)" , inout ("rcx") len / 8 => _ , inout ("rdi") dst => _ , inout ("rsi") src => _ , options (att_syntax , nostack , preserves_flags)) ; } }
}

macro_rules! copy_to_userspace_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function copy_to_userspace in module {}", module_path!());
    };
}

mkfn!{
    copy_to_userspace_introspect!();
    # [doc = " Copies `len` bytes of data from enclave pointer `src` to userspace `dst`"] # [doc = ""] # [doc = " This function mitigates stale data vulnerabilities by ensuring all writes to untrusted memory are either:"] # [doc = "  - preceded by the VERW instruction and followed by the MFENCE; LFENCE instruction sequence"] # [doc = "  - or are in multiples of 8 bytes, aligned to an 8-byte boundary"] # [doc = ""] # [doc = " # Panics"] # [doc = " This function panics if:"] # [doc = ""] # [doc = " * The `src` pointer is null"] # [doc = " * The `dst` pointer is null"] # [doc = " * The `src` memory range is not in enclave memory"] # [doc = " * The `dst` memory range is not in user memory"] # [doc = ""] # [doc = " # References"] # [doc = "  - https://www.intel.com/content/www/us/en/security-center/advisory/intel-sa-00615.html"] # [doc = "  - https://www.intel.com/content/www/us/en/developer/articles/technical/software-security-guidance/technical-documentation/processor-mmio-stale-data-vulnerabilities.html#inpage-nav-3-2-2"] pub (crate) unsafe fn copy_to_userspace (src : * const u8 , dst : * mut u8 , len : usize) { # [doc = " Like `ptr::copy(src, dst, len)`, except it uses the Intel-recommended"] # [doc = " instruction sequence for unaligned writes."] unsafe fn write_bytewise_to_userspace (src : * const u8 , dst : * mut u8 , len : usize) { if intrinsics :: likely (len == 0) { return ; } unsafe { let mut seg_sel : u16 = 0 ; for off in 0 .. len { asm ! ("
                    mov %ds, ({seg_sel})
                    verw ({seg_sel})
                    movb {val}, ({dst})
                    mfence
                    lfence
                    " , val = in (reg_byte) * src . add (off) , dst = in (reg) dst . add (off) , seg_sel = in (reg) & mut seg_sel , options (nostack , att_syntax)) ; } } } assert ! (! src . is_null ()) ; assert ! (! dst . is_null ()) ; assert ! (is_enclave_range (src , len)) ; assert ! (is_user_range (dst , len)) ; assert ! (len < isize :: MAX as usize) ; assert ! (! src . addr () . overflowing_add (len) . 1) ; assert ! (! dst . addr () . overflowing_add (len) . 1) ; unsafe { let (len1 , len2 , len3) = u64_align_to_guaranteed (dst , len) ; let (src1 , dst1) = (src , dst) ; let (src2 , dst2) = (src1 . add (len1) , dst1 . add (len1)) ; let (src3 , dst3) = (src2 . add (len2) , dst2 . add (len2)) ; write_bytewise_to_userspace (src1 , dst1 , len1) ; copy_quadwords (src2 , dst2 , len2) ; write_bytewise_to_userspace (src3 , dst3 , len3) ; } }
}

macro_rules! copy_from_userspace_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function copy_from_userspace in module {}", module_path!());
    };
}

mkfn!{
    copy_from_userspace_introspect!();
    # [doc = " Copies `len` bytes of data from userspace pointer `src` to enclave pointer `dst`"] # [doc = ""] # [doc = " This function mitigates AEPIC leak vulnerabilities by ensuring all reads from untrusted memory are 8-byte aligned"] # [doc = ""] # [doc = " # Panics"] # [doc = " This function panics if:"] # [doc = ""] # [doc = " * The `src` pointer is null"] # [doc = " * The `dst` pointer is null"] # [doc = " * The `src` memory range is not in user memory"] # [doc = " * The `dst` memory range is not in enclave memory"] # [doc = ""] # [doc = " # References"] # [doc = "  - https://www.intel.com/content/www/us/en/security-center/advisory/intel-sa-00657.html"] # [doc = "  - https://www.intel.com/content/www/us/en/developer/articles/technical/software-security-guidance/advisory-guidance/stale-data-read-from-xapic.html"] pub (crate) unsafe fn copy_from_userspace (src : * const u8 , dst : * mut u8 , len : usize) { # [doc = " Like `ptr::copy(src, dst, len)`, except it uses only u64-aligned reads."] # [doc = ""] # [doc = " # Safety"] # [doc = " The source memory region must not straddle an alignment boundary."] unsafe fn read_misaligned_from_userspace (src : * const u8 , dst : * mut u8 , len : usize) { if intrinsics :: likely (len == 0) { return ; } unsafe { let offset : usize ; let data : u64 ; asm ! ("
                movl {src:e}, {offset:e}
                andl $7, {offset:e}
                andq $-8, {src}
                movq ({src}), {dst}
                " , src = inout (reg) src => _ , offset = out (reg) offset , dst = out (reg) data , options (nostack , att_syntax , readonly , pure)) ; let data = data . to_le_bytes () ; ptr :: copy_nonoverlapping (data . as_ptr () . add (offset) , dst , len) ; } } assert ! (! src . is_null ()) ; assert ! (! dst . is_null ()) ; assert ! (is_user_range (src , len)) ; assert ! (is_enclave_range (dst , len)) ; assert ! (len < isize :: MAX as usize) ; assert ! (! (src as usize) . overflowing_add (len) . 1) ; assert ! (! (dst as usize) . overflowing_add (len) . 1) ; unsafe { let (len1 , len2 , len3) = u64_align_to_guaranteed (src , len) ; let (src1 , dst1) = (src , dst) ; let (src2 , dst2) = (src1 . add (len1) , dst1 . add (len1)) ; let (src3 , dst3) = (src2 . add (len2) , dst2 . add (len2)) ; read_misaligned_from_userspace (src1 , dst1 , len1) ; copy_quadwords (src2 , dst2 , len2) ; read_misaligned_from_userspace (src3 , dst3 , len3) ; } }
}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < T : ? Sized > UserRef < T > where T : UserSafe , { # [doc = " Creates a `&UserRef<[T]>` from a raw pointer."] # [doc = ""] # [doc = " # Safety"] # [doc = " The caller must ensure `ptr` points to `T`."] # [doc = ""] # [doc = " # Panics"] # [doc = " This function panics if:"] # [doc = ""] # [doc = " * The pointer is not aligned"] # [doc = " * The pointer is null"] # [doc = " * The pointed-to range is not in user memory"] pub unsafe fn from_ptr < 'a > (ptr : * const T) -> & 'a Self { unsafe { T :: check_ptr (ptr) } ; unsafe { & * (ptr as * const Self) } } # [doc = " Creates a `&mut UserRef<[T]>` from a raw pointer. See the struct"] # [doc = " documentation for the nuances regarding a `&mut UserRef<T>`."] # [doc = ""] # [doc = " # Safety"] # [doc = " The caller must ensure `ptr` points to `T`."] # [doc = ""] # [doc = " # Panics"] # [doc = " This function panics if:"] # [doc = ""] # [doc = " * The pointer is not aligned"] # [doc = " * The pointer is null"] # [doc = " * The pointed-to range is not in user memory"] pub unsafe fn from_mut_ptr < 'a > (ptr : * mut T) -> & 'a mut Self { unsafe { T :: check_ptr (ptr) } ; unsafe { & mut * (ptr as * mut Self) } } # [doc = " Copies `val` into user memory."] # [doc = ""] # [doc = " # Panics"] # [doc = " This function panics if the destination doesn't have the same size as"] # [doc = " the source. This can happen for dynamically-sized types such as slices."] pub fn copy_from_enclave (& mut self , val : & T) { unsafe { assert_eq ! (size_of_val (val) , size_of_val (&* self . 0 . get ())) ; copy_to_userspace (val as * const T as * const u8 , self . 0 . get () as * mut T as * mut u8 , size_of_val (val) ,) ; } } # [doc = " Copies the value from user memory and place it into `dest`."] # [doc = ""] # [doc = " # Panics"] # [doc = " This function panics if the destination doesn't have the same size as"] # [doc = " the source. This can happen for dynamically-sized types such as slices."] pub fn copy_to_enclave < U : ? Sized + UserSafeCopyDestination < T > > (& self , dest : & mut U) { unsafe { assert_eq ! (size_of_val (dest) , size_of_val (&* self . 0 . get ())) ; copy_from_userspace (self . 0 . get () as * const T as * const u8 , dest . as_mut_ptr () as * mut u8 , size_of_val (dest) ,) ; } } # [doc = " Obtain a raw pointer from this reference."] pub fn as_raw_ptr (& self) -> * const T { self as * const _ as _ } # [doc = " Obtain a raw pointer from this reference."] pub fn as_raw_mut_ptr (& mut self) -> * mut T { self as * mut _ as _ } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < T > UserRef < T > where T : UserSafe , { # [doc = " Copies the value from user memory into enclave memory."] pub fn to_enclave (& self) -> T { unsafe { let mut data = mem :: MaybeUninit :: uninit () ; copy_from_userspace (self . 0 . get () as _ , data . as_mut_ptr () as _ , size_of :: < T > ()) ; data . assume_init () } } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < T > UserRef < [T] > where [T] : UserSafe , { # [doc = " Creates a `&UserRef<[T]>` from a raw thin pointer and a slice length."] # [doc = ""] # [doc = " # Safety"] # [doc = " The caller must ensure `ptr` points to `n` elements of `T`."] # [doc = ""] # [doc = " # Panics"] # [doc = " This function panics if:"] # [doc = ""] # [doc = " * The pointer is not aligned"] # [doc = " * The pointer is null"] # [doc = " * The pointed-to range does not fit in the address space"] # [doc = " * The pointed-to range is not in user memory"] pub unsafe fn from_raw_parts < 'a > (ptr : * const T , len : usize) -> & 'a Self { unsafe { & * (< [T] > :: from_raw_sized (ptr as _ , len * size_of :: < T > ()) . as_ptr () as * const Self) } } # [doc = " Creates a `&mut UserRef<[T]>` from a raw thin pointer and a slice length."] # [doc = " See the struct documentation for the nuances regarding a"] # [doc = " `&mut UserRef<T>`."] # [doc = ""] # [doc = " # Safety"] # [doc = " The caller must ensure `ptr` points to `n` elements of `T`."] # [doc = ""] # [doc = " # Panics"] # [doc = " This function panics if:"] # [doc = ""] # [doc = " * The pointer is not aligned"] # [doc = " * The pointer is null"] # [doc = " * The pointed-to range does not fit in the address space"] # [doc = " * The pointed-to range is not in user memory"] pub unsafe fn from_raw_parts_mut < 'a > (ptr : * mut T , len : usize) -> & 'a mut Self { unsafe { & mut * (< [T] > :: from_raw_sized (ptr as _ , len * size_of :: < T > ()) . as_ptr () as * mut Self) } } # [doc = " Obtain a raw pointer to the first element of this user slice."] pub fn as_ptr (& self) -> * const T { self . 0 . get () as _ } # [doc = " Obtain a raw pointer to the first element of this user slice."] pub fn as_mut_ptr (& mut self) -> * mut T { self . 0 . get () as _ } # [doc = " Obtain the number of elements in this user slice."] pub fn len (& self) -> usize { unsafe { self . 0 . get () . len () } } # [doc = " Copies the value from user memory and appends it to `dest`."] pub fn append_to_enclave_vec (& self , dest : & mut Vec < T >) { dest . reserve (self . len ()) ; self . copy_to_enclave (& mut dest . spare_capacity_mut () [.. self . len ()]) ; unsafe { dest . set_len (dest . len () + self . len ()) } ; } # [doc = " Copies the value from user memory into a vector in enclave memory."] pub fn to_enclave (& self) -> Vec < T > { let mut ret = Vec :: with_capacity (self . len ()) ; self . append_to_enclave_vec (& mut ret) ; ret } # [doc = " Returns an iterator over the slice."] pub fn iter (& self) -> Iter < '_ , T > where T : UserSafe , { unsafe { Iter ((& * self . as_raw_ptr ()) . iter ()) } } # [doc = " Returns an iterator that allows modifying each value."] pub fn iter_mut (& mut self) -> IterMut < '_ , T > where T : UserSafe , { unsafe { IterMut ((& mut * self . as_raw_mut_ptr ()) . iter_mut ()) } } }}}
mkitem!{mkstruct!{# [doc = " Immutable user slice iterator"] # [doc = ""] # [doc = " This struct is created by the `iter` method on `UserRef<[T]>`."] # [unstable (feature = "sgx_platform" , issue = "56975")] pub struct Iter < 'a , T : 'a + UserSafe > (slice :: Iter < 'a , T >) ;}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < 'a , T : UserSafe > Iterator for Iter < 'a , T > { type Item = & 'a UserRef < T > ; # [inline] fn next (& mut self) -> Option < Self :: Item > { unsafe { self . 0 . next () . map (| e | UserRef :: from_ptr (e)) } } }}}
mkitem!{mkstruct!{# [doc = " Mutable user slice iterator"] # [doc = ""] # [doc = " This struct is created by the `iter_mut` method on `UserRef<[T]>`."] # [unstable (feature = "sgx_platform" , issue = "56975")] pub struct IterMut < 'a , T : 'a + UserSafe > (slice :: IterMut < 'a , T >) ;}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < 'a , T : UserSafe > Iterator for IterMut < 'a , T > { type Item = & 'a mut UserRef < T > ; # [inline] fn next (& mut self) -> Option < Self :: Item > { unsafe { self . 0 . next () . map (| e | UserRef :: from_mut_ptr (e)) } } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < T : ? Sized > Deref for User < T > where T : UserSafe , { type Target = UserRef < T > ; fn deref (& self) -> & Self :: Target { unsafe { & * self . 0 . as_ptr () } } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < T : ? Sized > DerefMut for User < T > where T : UserSafe , { fn deref_mut (& mut self) -> & mut Self :: Target { unsafe { & mut * self . 0 . as_ptr () } } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < T : ? Sized > Drop for User < T > where T : UserSafe , { fn drop (& mut self) { unsafe { let ptr = (* self . 0 . as_ptr ()) . 0 . get () ; super :: free (ptr as _ , size_of_val (& mut * ptr) , T :: align_of ()) ; } } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < T : CoerceUnsized < U > , U > CoerceUnsized < UserRef < U > > for UserRef < T > { }}}
mkitem!{mkimpl!{# [unstable (feature = "pin_coerce_unsized_trait" , issue = "123430")] unsafe impl < T : ? Sized > PinCoerceUnsized for UserRef < T > { }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < T , I > Index < I > for UserRef < [T] > where [T] : UserSafe , I : SliceIndex < [T] > , I :: Output : UserSafe , { type Output = UserRef < I :: Output > ; # [inline] fn index (& self , index : I) -> & UserRef < I :: Output > { unsafe { if let Some (slice) = index . get (& * self . as_raw_ptr ()) { UserRef :: from_ptr (slice) } else { rtabort ! ("index out of range for user slice") ; } } } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl < T , I > IndexMut < I > for UserRef < [T] > where [T] : UserSafe , I : SliceIndex < [T] > , I :: Output : UserSafe , { # [inline] fn index_mut (& mut self , index : I) -> & mut UserRef < I :: Output > { unsafe { if let Some (slice) = index . get_mut (& mut * self . as_raw_mut_ptr ()) { UserRef :: from_mut_ptr (slice) } else { rtabort ! ("index out of range for user slice") ; } } } }}}
mkitem!{mkimpl!{# [unstable (feature = "sgx_platform" , issue = "56975")] impl UserRef < super :: raw :: ByteBuffer > { # [doc = " Copies the user memory range pointed to by the user `ByteBuffer` to"] # [doc = " enclave memory."] # [doc = ""] # [doc = " # Panics"] # [doc = " This function panics if, in the user `ByteBuffer`:"] # [doc = ""] # [doc = " * The pointer is null"] # [doc = " * The pointed-to range does not fit in the address space"] # [doc = " * The pointed-to range is not in user memory"] pub fn copy_user_buffer (& self) -> Vec < u8 > { unsafe { let buf = self . to_enclave () ; if buf . len > 0 { User :: from_raw_parts (buf . data as _ , buf . len) . to_enclave () } else { Vec :: with_capacity (0) } } } }}}