use split_decls_genesis :: ourprelude :: * ; use super :: Mutex ; use crate :: cell :: UnsafeCell ; use crate :: pin :: Pin ; #[cfg (not (target_os = "nto"))] use crate :: sys :: pal :: time :: TIMESPEC_MAX ; #[cfg (target_os = "nto")] use crate :: sys :: pal :: time :: TIMESPEC_MAX_CAPPED ; use crate :: sys :: pal :: time :: Timespec ; use crate :: time :: Duration ; pub struct Condvar { inner : UnsafeCell < libc :: pthread_cond_t > , } impl Condvar { pub fn new () -> Condvar { Condvar { inner : UnsafeCell :: new (libc :: PTHREAD_COND_INITIALIZER) } } #[inline] fn raw (& self) -> * mut libc :: pthread_cond_t { self . inner . get () } #[doc = " # Safety"] #[doc = " `init` must have been called on this instance."] #[inline] pub unsafe fn notify_one (self : Pin < & Self >) { let r = unsafe { libc :: pthread_cond_signal (self . raw ()) } ; debug_assert_eq ! (r , 0) ; } #[doc = " # Safety"] #[doc = " `init` must have been called on this instance."] #[inline] pub unsafe fn notify_all (self : Pin < & Self >) { let r = unsafe { libc :: pthread_cond_broadcast (self . raw ()) } ; debug_assert_eq ! (r , 0) ; } #[doc = " # Safety"] #[doc = " * `init` must have been called on this instance."] #[doc = " * `mutex` must be locked by the current thread."] #[doc = " * This condition variable may only be used with the same mutex."] #[inline] pub unsafe fn wait (self : Pin < & Self > , mutex : Pin < & Mutex >) { let r = unsafe { libc :: pthread_cond_wait (self . raw () , mutex . raw ()) } ; debug_assert_eq ! (r , 0) ; } #[doc = " # Safety"] #[doc = " * `init` must have been called on this instance."] #[doc = " * `mutex` must be locked by the current thread."] #[doc = " * This condition variable may only be used with the same mutex."] pub unsafe fn wait_timeout (& self , mutex : Pin < & Mutex > , dur : Duration) -> bool { let mutex = mutex . raw () ; #[cfg (any (target_vendor = "apple" , target_os = "cygwin"))] let dur = Duration :: min (dur , Duration :: from_secs (1000 * 365 * 86400)) ; let timeout = Timespec :: now (Self :: CLOCK) . checked_add_duration (& dur) ; #[cfg (not (target_os = "nto"))] let timeout = timeout . and_then (| t | t . to_timespec ()) . unwrap_or (TIMESPEC_MAX) ; #[cfg (target_os = "nto")] let timeout = timeout . and_then (| t | t . to_timespec_capped ()) . unwrap_or (TIMESPEC_MAX_CAPPED) ; let r = unsafe { libc :: pthread_cond_timedwait (self . raw () , mutex , & timeout) } ; assert ! (r == libc :: ETIMEDOUT || r == 0) ; r == 0 } } #[cfg (not (any (target_os = "android" , target_vendor = "apple" , target_os = "espidf" , target_os = "horizon" , target_os = "l4re" , target_os = "redox" , target_os = "teeos" ,)))] impl Condvar { pub const PRECISE_TIMEOUT : bool = true ; const CLOCK : libc :: clockid_t = libc :: CLOCK_MONOTONIC ; #[doc = " # Safety"] #[doc = " May only be called once per instance of `Self`."] pub unsafe fn init (self : Pin < & mut Self >) { use crate :: mem :: MaybeUninit ; struct AttrGuard < 'a > (pub & 'a mut MaybeUninit < libc :: pthread_condattr_t >) ; impl Drop for AttrGuard < '_ > { fn drop (& mut self) { unsafe { let result = libc :: pthread_condattr_destroy (self . 0 . as_mut_ptr ()) ; assert_eq ! (result , 0) ; } } } unsafe { let mut attr = MaybeUninit :: < libc :: pthread_condattr_t > :: uninit () ; let r = libc :: pthread_condattr_init (attr . as_mut_ptr ()) ; assert_eq ! (r , 0) ; let attr = AttrGuard (& mut attr) ; let r = libc :: pthread_condattr_setclock (attr . 0 . as_mut_ptr () , Self :: CLOCK) ; assert_eq ! (r , 0) ; let r = libc :: pthread_cond_init (self . raw () , attr . 0 . as_ptr ()) ; assert_eq ! (r , 0) ; } } } #[cfg (any (target_os = "android" , target_vendor = "apple" , target_os = "espidf" , target_os = "horizon" , target_os = "l4re" , target_os = "redox" , target_os = "teeos" ,))] impl Condvar { pub const PRECISE_TIMEOUT : bool = false ; const CLOCK : libc :: clockid_t = libc :: CLOCK_REALTIME ; #[doc = " # Safety"] #[doc = " May only be called once per instance of `Self`."] pub unsafe fn init (self : Pin < & mut Self >) { if cfg ! (any (target_os = "espidf" , target_os = "horizon" , target_os = "teeos")) { let r = unsafe { libc :: pthread_cond_init (self . raw () , crate :: ptr :: null ()) } ; assert_eq ! (r , 0) ; } } } impl ! Unpin for Condvar { } unsafe impl Sync for Condvar { } unsafe impl Send for Condvar { } impl Drop for Condvar { #[inline] fn drop (& mut self) { let r = unsafe { libc :: pthread_cond_destroy (self . raw ()) } ; if cfg ! (target_os = "dragonfly") { debug_assert ! (r == 0 || r == libc :: EINVAL) ; } else { debug_assert_eq ! (r , 0) ; } } }