# ! [doc = " Memory allocation APIs."] # ! [doc = ""] # ! [doc = " In a given program, the standard library has one “global” memory allocator"] # ! [doc = " that is used for example by `Box<T>` and `Vec<T>`."] # ! [doc = ""] # ! [doc = " Currently the default global allocator is unspecified. Libraries, however,"] # ! [doc = " like `cdylib`s and `staticlib`s are guaranteed to use the [`System`] by"] # ! [doc = " default."] # ! [doc = ""] # ! [doc = " # The `#[global_allocator]` attribute"] # ! [doc = ""] # ! [doc = " This attribute allows configuring the choice of global allocator."] # ! [doc = " You can use this to implement a completely custom global allocator"] # ! [doc = " to route all default allocation requests to a custom object."] # ! [doc = ""] # ! [doc = " ```rust"] # ! [doc = " use std::alloc::{GlobalAlloc, System, Layout};"] # ! [doc = ""] # ! [doc = " struct MyAllocator;"] # ! [doc = ""] # ! [doc = " unsafe impl GlobalAlloc for MyAllocator {"] # ! [doc = "     unsafe fn alloc(&self, layout: Layout) -> *mut u8 {"] # ! [doc = "         unsafe { System.alloc(layout) }"] # ! [doc = "     }"] # ! [doc = ""] # ! [doc = "     unsafe fn dealloc(&self, ptr: *mut u8, layout: Layout) {"] # ! [doc = "         unsafe { System.dealloc(ptr, layout) }"] # ! [doc = "     }"] # ! [doc = " }"] # ! [doc = ""] # ! [doc = " #[global_allocator]"] # ! [doc = " static GLOBAL: MyAllocator = MyAllocator;"] # ! [doc = ""] # ! [doc = " fn main() {"] # ! [doc = "     // This `Vec` will allocate memory through `GLOBAL` above"] # ! [doc = "     let mut v = Vec::new();"] # ! [doc = "     v.push(1);"] # ! [doc = " }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " The attribute is used on a `static` item whose type implements the"] # ! [doc = " [`GlobalAlloc`] trait. This type can be provided by an external library:"] # ! [doc = ""] # ! [doc = " ```rust,ignore (demonstrates crates.io usage)"] # ! [doc = " use jemallocator::Jemalloc;"] # ! [doc = ""] # ! [doc = " #[global_allocator]"] # ! [doc = " static GLOBAL: Jemalloc = Jemalloc;"] # ! [doc = ""] # ! [doc = " fn main() {}"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " The `#[global_allocator]` can only be used once in a crate"] # ! [doc = " or its recursive dependencies."] # ! [deny (unsafe_op_in_unsafe_fn)] # ! [stable (feature = "alloc_module" , since = "1.28.0")] use split_decls_genesis :: ourprelude :: * ; use core :: ptr :: NonNull ; use core :: sync :: atomic :: { Atomic , AtomicPtr , Ordering } ; use core :: { hint , mem , ptr } ; #[stable (feature = "alloc_module" , since = "1.28.0")] #[doc (inline)] pub use alloc_crate :: alloc :: * ; #[doc = " The default memory allocator provided by the operating system."] #[doc = ""] #[doc = " This is based on `malloc` on Unix platforms and `HeapAlloc` on Windows,"] #[doc = " plus related functions. However, it is not valid to mix use of the backing"] #[doc = " system allocator with `System`, as this implementation may include extra"] #[doc = " work, such as to serve alignment requests greater than the alignment"] #[doc = " provided directly by the backing system allocator."] #[doc = ""] #[doc = " This type implements the [`GlobalAlloc`] trait. Currently the default"] #[doc = " global allocator is unspecified. Libraries, however, like `cdylib`s and"] #[doc = " `staticlib`s are guaranteed to use the [`System`] by default and as such"] #[doc = " work as if they had this definition:"] #[doc = ""] #[doc = " ```rust"] #[doc = " use std::alloc::System;"] #[doc = ""] #[doc = " #[global_allocator]"] #[doc = " static A: System = System;"] #[doc = ""] #[doc = " fn main() {"] #[doc = "     let a = Box::new(4); // Allocates from the system allocator."] #[doc = "     println!(\"{a}\");"] #[doc = " }"] #[doc = " ```"] #[doc = ""] #[doc = " You can also define your own wrapper around `System` if you'd like, such as"] #[doc = " keeping track of the number of all bytes allocated:"] #[doc = ""] #[doc = " ```rust"] #[doc = " use std::alloc::{System, GlobalAlloc, Layout};"] #[doc = " use std::sync::atomic::{AtomicUsize, Ordering::Relaxed};"] #[doc = ""] #[doc = " struct Counter;"] #[doc = ""] #[doc = " static ALLOCATED: AtomicUsize = AtomicUsize::new(0);"] #[doc = ""] #[doc = " unsafe impl GlobalAlloc for Counter {"] #[doc = "     unsafe fn alloc(&self, layout: Layout) -> *mut u8 {"] #[doc = "         let ret = unsafe { System.alloc(layout) };"] #[doc = "         if !ret.is_null() {"] #[doc = "             ALLOCATED.fetch_add(layout.size(), Relaxed);"] #[doc = "         }"] #[doc = "         ret"] #[doc = "     }"] #[doc = ""] #[doc = "     unsafe fn dealloc(&self, ptr: *mut u8, layout: Layout) {"] #[doc = "         unsafe { System.dealloc(ptr, layout); }"] #[doc = "         ALLOCATED.fetch_sub(layout.size(), Relaxed);"] #[doc = "     }"] #[doc = " }"] #[doc = ""] #[doc = " #[global_allocator]"] #[doc = " static A: Counter = Counter;"] #[doc = ""] #[doc = " fn main() {"] #[doc = "     println!(\"allocated bytes before main: {}\", ALLOCATED.load(Relaxed));"] #[doc = " }"] #[doc = " ```"] #[doc = ""] #[doc = " It can also be used directly to allocate memory independently of whatever"] #[doc = " global allocator has been selected for a Rust program. For example if a Rust"] #[doc = " program opts in to using jemalloc as the global allocator, `System` will"] #[doc = " still allocate memory using `malloc` and `HeapAlloc`."] #[stable (feature = "alloc_system_type" , since = "1.28.0")] #[derive (Debug , Default , Copy , Clone)] pub struct System ; impl System { #[inline] fn alloc_impl (& self , layout : Layout , zeroed : bool) -> Result < NonNull < [u8] > , AllocError > { match layout . size () { 0 => Ok (NonNull :: slice_from_raw_parts (layout . dangling () , 0)) , size => unsafe { let raw_ptr = if zeroed { GlobalAlloc :: alloc_zeroed (self , layout) } else { GlobalAlloc :: alloc (self , layout) } ; let ptr = NonNull :: new (raw_ptr) . ok_or (AllocError) ? ; Ok (NonNull :: slice_from_raw_parts (ptr , size)) } , } } #[inline] unsafe fn grow_impl (& self , ptr : NonNull < u8 > , old_layout : Layout , new_layout : Layout , zeroed : bool ,) -> Result < NonNull < [u8] > , AllocError > { debug_assert ! (new_layout . size () >= old_layout . size () , "`new_layout.size()` must be greater than or equal to `old_layout.size()`") ; match old_layout . size () { 0 => self . alloc_impl (new_layout , zeroed) , old_size if old_layout . align () == new_layout . align () => unsafe { let new_size = new_layout . size () ; hint :: assert_unchecked (new_size >= old_layout . size ()) ; let raw_ptr = GlobalAlloc :: realloc (self , ptr . as_ptr () , old_layout , new_size) ; let ptr = NonNull :: new (raw_ptr) . ok_or (AllocError) ? ; if zeroed { raw_ptr . add (old_size) . write_bytes (0 , new_size - old_size) ; } Ok (NonNull :: slice_from_raw_parts (ptr , new_size)) } , old_size => unsafe { let new_ptr = self . alloc_impl (new_layout , zeroed) ? ; ptr :: copy_nonoverlapping (ptr . as_ptr () , new_ptr . as_mut_ptr () , old_size) ; Allocator :: deallocate (self , ptr , old_layout) ; Ok (new_ptr) } , } } } #[unstable (feature = "allocator_api" , issue = "32838")] unsafe impl Allocator for System { #[inline] fn allocate (& self , layout : Layout) -> Result < NonNull < [u8] > , AllocError > { self . alloc_impl (layout , false) } #[inline] fn allocate_zeroed (& self , layout : Layout) -> Result < NonNull < [u8] > , AllocError > { self . alloc_impl (layout , true) } #[inline] unsafe fn deallocate (& self , ptr : NonNull < u8 > , layout : Layout) { if layout . size () != 0 { unsafe { GlobalAlloc :: dealloc (self , ptr . as_ptr () , layout) } } } #[inline] unsafe fn grow (& self , ptr : NonNull < u8 > , old_layout : Layout , new_layout : Layout ,) -> Result < NonNull < [u8] > , AllocError > { unsafe { self . grow_impl (ptr , old_layout , new_layout , false) } } #[inline] unsafe fn grow_zeroed (& self , ptr : NonNull < u8 > , old_layout : Layout , new_layout : Layout ,) -> Result < NonNull < [u8] > , AllocError > { unsafe { self . grow_impl (ptr , old_layout , new_layout , true) } } #[inline] unsafe fn shrink (& self , ptr : NonNull < u8 > , old_layout : Layout , new_layout : Layout ,) -> Result < NonNull < [u8] > , AllocError > { debug_assert ! (new_layout . size () <= old_layout . size () , "`new_layout.size()` must be smaller than or equal to `old_layout.size()`") ; match new_layout . size () { 0 => unsafe { Allocator :: deallocate (self , ptr , old_layout) ; Ok (NonNull :: slice_from_raw_parts (new_layout . dangling () , 0)) } , new_size if old_layout . align () == new_layout . align () => unsafe { hint :: assert_unchecked (new_size <= old_layout . size ()) ; let raw_ptr = GlobalAlloc :: realloc (self , ptr . as_ptr () , old_layout , new_size) ; let ptr = NonNull :: new (raw_ptr) . ok_or (AllocError) ? ; Ok (NonNull :: slice_from_raw_parts (ptr , new_size)) } , new_size => unsafe { let new_ptr = Allocator :: allocate (self , new_layout) ? ; ptr :: copy_nonoverlapping (ptr . as_ptr () , new_ptr . as_mut_ptr () , new_size) ; Allocator :: deallocate (self , ptr , old_layout) ; Ok (new_ptr) } , } } } static HOOK : Atomic < * mut () > = AtomicPtr :: new (ptr :: null_mut ()) ; #[doc = " Registers a custom allocation error hook, replacing any that was previously registered."] #[doc = ""] #[doc = " The allocation error hook is invoked when an infallible memory allocation fails — that is,"] #[doc = " as a consequence of calling [`handle_alloc_error`] — before the runtime aborts."] #[doc = ""] #[doc = " The allocation error hook is a global resource. [`take_alloc_error_hook`] may be used to"] #[doc = " retrieve a previously registered hook and wrap or discard it."] #[doc = ""] #[doc = " # What the provided `hook` function should expect"] #[doc = ""] #[doc = " The hook function is provided with a [`Layout`] struct which contains information"] #[doc = " about the allocation that failed."] #[doc = ""] #[doc = " The hook function may choose to panic or abort; in the event that it returns normally, this"] #[doc = " will cause an immediate abort."] #[doc = ""] #[doc = " Since [`take_alloc_error_hook`] is a safe function that allows retrieving the hook, the hook"] #[doc = " function must be _sound_ to call even if no memory allocations were attempted."] #[doc = ""] #[doc = " # The default hook"] #[doc = ""] #[doc = " The default hook, used if [`set_alloc_error_hook`] is never called, prints a message to"] #[doc = " standard error (and then returns, causing the runtime to abort the process)."] #[doc = " Compiler options may cause it to panic instead, and the default behavior may be changed"] #[doc = " to panicking in future versions of Rust."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(alloc_error_hook)]"] #[doc = ""] #[doc = " use std::alloc::{Layout, set_alloc_error_hook};"] #[doc = ""] #[doc = " fn custom_alloc_error_hook(layout: Layout) {"] #[doc = "    panic!(\"memory allocation of {} bytes failed\", layout.size());"] #[doc = " }"] #[doc = ""] #[doc = " set_alloc_error_hook(custom_alloc_error_hook);"] #[doc = " ```"] #[unstable (feature = "alloc_error_hook" , issue = "51245")] pub fn set_alloc_error_hook (hook : fn (Layout)) { HOOK . store (hook as * mut () , Ordering :: Release) ; } #[doc = " Unregisters the current allocation error hook, returning it."] #[doc = ""] #[doc = " *See also the function [`set_alloc_error_hook`].*"] #[doc = ""] #[doc = " If no custom hook is registered, the default hook will be returned."] #[unstable (feature = "alloc_error_hook" , issue = "51245")] pub fn take_alloc_error_hook () -> fn (Layout) { let hook = HOOK . swap (ptr :: null_mut () , Ordering :: Acquire) ; if hook . is_null () { default_alloc_error_hook } else { unsafe { mem :: transmute (hook) } } } fn default_alloc_error_hook (layout : Layout) { unsafe extern "Rust" { #[rustc_std_internal_symbol] fn __rust_alloc_error_handler_should_panic_v2 () -> u8 ; } if unsafe { __rust_alloc_error_handler_should_panic_v2 () != 0 } { panic ! ("memory allocation of {} bytes failed" , layout . size ()) ; } else { rtprintpanic ! ("memory allocation of {} bytes failed\n" , layout . size ()) ; } } #[cfg (not (test))] #[doc (hidden)] #[alloc_error_handler] #[unstable (feature = "alloc_internals" , issue = "none")] pub fn rust_oom (layout : Layout) -> ! { let hook = HOOK . load (Ordering :: Acquire) ; let hook : fn (Layout) = if hook . is_null () { default_alloc_error_hook } else { unsafe { mem :: transmute (hook) } } ; hook (layout) ; crate :: process :: abort () } #[cfg (not (test))] #[doc (hidden)] #[allow (unused_attributes)] #[unstable (feature = "alloc_internals" , issue = "none")] pub mod __default_lib_allocator { use super :: { GlobalAlloc , Layout , System } ; #[rustc_std_internal_symbol] pub unsafe extern "C" fn __rdl_alloc (size : usize , align : usize) -> * mut u8 { unsafe { let layout = Layout :: from_size_align_unchecked (size , align) ; System . alloc (layout) } } #[rustc_std_internal_symbol] pub unsafe extern "C" fn __rdl_dealloc (ptr : * mut u8 , size : usize , align : usize) { unsafe { System . dealloc (ptr , Layout :: from_size_align_unchecked (size , align)) } } #[rustc_std_internal_symbol] pub unsafe extern "C" fn __rdl_realloc (ptr : * mut u8 , old_size : usize , align : usize , new_size : usize ,) -> * mut u8 { unsafe { let old_layout = Layout :: from_size_align_unchecked (old_size , align) ; System . realloc (ptr , old_layout , new_size) } } #[rustc_std_internal_symbol] pub unsafe extern "C" fn __rdl_alloc_zeroed (size : usize , align : usize) -> * mut u8 { unsafe { let layout = Layout :: from_size_align_unchecked (size , align) ; System . alloc_zeroed (layout) } } }