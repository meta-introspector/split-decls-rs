# ! [doc = " Unwinding panics for Miri."] use split_decls_genesis :: ourprelude :: * ; use alloc :: boxed :: Box ; use core :: any :: Any ; type Payload = Box < Box < dyn Any + Send > > ; unsafe extern "Rust" { #[doc = " Miri-provided extern function to begin unwinding."] fn miri_start_unwind (payload : * mut u8) -> ! ; } pub (crate) unsafe fn panic (payload : Box < dyn Any + Send >) -> u32 { let payload_box : Payload = Box :: new (payload) ; unsafe { miri_start_unwind (Box :: into_raw (payload_box) as * mut u8) } } pub (crate) unsafe fn cleanup (payload_box : * mut u8) -> Box < dyn Any + Send > { let payload_box : Payload = unsafe { Box :: from_raw (payload_box as * mut _) } ; * payload_box }