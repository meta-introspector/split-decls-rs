# ! [doc = " Unwinding for *hermit* target."] # ! [doc = ""] # ! [doc = " Right now we don't support this, so this is just stubs."] use split_decls_genesis :: ourprelude :: * ; use alloc :: boxed :: Box ; use core :: any :: Any ; unsafe extern "Rust" { #[rustc_std_internal_symbol] safe fn __rust_abort () -> !; } pub (crate) unsafe fn cleanup (_ptr : * mut u8) -> Box < dyn Any + Send > { __rust_abort () } pub (crate) unsafe fn panic (_data : Box < dyn Any + Send >) -> u32 { __rust_abort () }