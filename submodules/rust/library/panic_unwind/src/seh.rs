# ! [doc = " Windows SEH"] # ! [doc = ""] # ! [doc = " On Windows (currently only on MSVC), the default exception handling"] # ! [doc = " mechanism is Structured Exception Handling (SEH). This is quite different"] # ! [doc = " than Dwarf-based exception handling (e.g., what other unix platforms use) in"] # ! [doc = " terms of compiler internals, so LLVM is required to have a good deal of"] # ! [doc = " extra support for SEH."] # ! [doc = ""] # ! [doc = " In a nutshell, what happens here is:"] # ! [doc = ""] # ! [doc = " 1. The `panic` function calls the standard Windows function"] # ! [doc = "    `_CxxThrowException` to throw a C++-like exception, triggering the"] # ! [doc = "    unwinding process."] # ! [doc = " 2. All landing pads generated by the compiler use the personality function"] # ! [doc = "    `__CxxFrameHandler3`, a function in the CRT, and the unwinding code in"] # ! [doc = "    Windows will use this personality function to execute all cleanup code on"] # ! [doc = "    the stack."] # ! [doc = " 3. All compiler-generated calls to `invoke` have a landing pad set as a"] # ! [doc = "    `cleanuppad` LLVM instruction, which indicates the start of the cleanup"] # ! [doc = "    routine. The personality (in step 2, defined in the CRT) is responsible"] # ! [doc = "    for running the cleanup routines."] # ! [doc = " 4. Eventually the \"catch\" code in the `try` intrinsic (generated by the"] # ! [doc = "    compiler) is executed and indicates that control should come back to"] # ! [doc = "    Rust. This is done via a `catchswitch` plus a `catchpad` instruction in"] # ! [doc = "    LLVM IR terms, finally returning normal control to the program with a"] # ! [doc = "    `catchret` instruction."] # ! [doc = ""] # ! [doc = " Some specific differences from the gcc-based exception handling are:"] # ! [doc = ""] # ! [doc = " * Rust has no custom personality function, it is instead *always*"] # ! [doc = "   `__CxxFrameHandler3`. Additionally, no extra filtering is performed, so we"] # ! [doc = "   end up catching any C++ exceptions that happen to look like the kind we're"] # ! [doc = "   throwing. Note that throwing an exception into Rust is undefined behavior"] # ! [doc = "   anyway, so this should be fine."] # ! [doc = " * We've got some data to transmit across the unwinding boundary,"] # ! [doc = "   specifically a `Box<dyn Any + Send>`. Like with Dwarf exceptions"] # ! [doc = "   these two pointers are stored as a payload in the exception itself. On"] # ! [doc = "   MSVC, however, there's no need for an extra heap allocation because the"] # ! [doc = "   call stack is preserved while filter functions are being executed. This"] # ! [doc = "   means that the pointers are passed directly to `_CxxThrowException` which"] # ! [doc = "   are then recovered in the filter function to be written to the stack frame"] # ! [doc = "   of the `try` intrinsic."] # ! [doc = ""] # ! [doc = " [win64]: https://docs.microsoft.com/en-us/cpp/build/exception-handling-x64"] # ! [doc = " [llvm]: https://llvm.org/docs/ExceptionHandling.html#background-on-windows-exceptions"] # ! [allow (nonstandard_style)] use split_decls_genesis :: ourprelude :: * ; use alloc :: boxed :: Box ; use core :: any :: Any ; use core :: ffi :: { c_int , c_uint , c_void } ; use core :: mem :: ManuallyDrop ; #[repr (C)] struct Exception { canary : * const _TypeDescriptor , data : Option < Box < dyn Any + Send > > , } #[cfg (target_arch = "x86")] mod imp { #[repr (transparent)] #[derive (Copy , Clone)] pub (super) struct ptr_t (* mut u8) ; impl ptr_t { pub (super) const fn null () -> Self { Self (core :: ptr :: null_mut ()) } pub (super) const fn new (ptr : * mut u8) -> Self { Self (ptr) } pub (super) const fn raw (self) -> * mut u8 { self . 0 } } } #[cfg (not (target_arch = "x86"))] mod imp { #[repr (transparent)] #[derive (Copy , Clone)] pub (super) struct ptr_t (u32) ; unsafe extern "C" { static __ImageBase : u8 ; } impl ptr_t { pub (super) const fn null () -> Self { Self (0) } pub (super) fn new (ptr : * mut u8) -> Self { let addr : usize = ptr . expose_provenance () ; let image_base = (& raw const __ImageBase) . addr () ; let offset : usize = addr - image_base ; Self (offset as u32) } pub (super) const fn raw (self) -> u32 { self . 0 } } } use imp :: ptr_t ; #[repr (C)] struct _ThrowInfo { pub attributes : c_uint , pub pmfnUnwind : ptr_t , pub pForwardCompat : ptr_t , pub pCatchableTypeArray : ptr_t , } #[repr (C)] struct _CatchableTypeArray { pub nCatchableTypes : c_int , pub arrayOfCatchableTypes : [ptr_t ; 1] , } #[repr (C)] struct _CatchableType { pub properties : c_uint , pub pType : ptr_t , pub thisDisplacement : _PMD , pub sizeOrOffset : c_int , pub copyFunction : ptr_t , } #[repr (C)] struct _PMD { pub mdisp : c_int , pub pdisp : c_int , pub vdisp : c_int , } #[repr (C)] struct _TypeDescriptor { pub pVFTable : * const u8 , pub spare : * mut u8 , pub name : [u8 ; 11] , } const TYPE_NAME : [u8 ; 11] = * b"rust_panic\0" ; static mut THROW_INFO : _ThrowInfo = _ThrowInfo { attributes : 0 , pmfnUnwind : ptr_t :: null () , pForwardCompat : ptr_t :: null () , pCatchableTypeArray : ptr_t :: null () , } ; static mut CATCHABLE_TYPE_ARRAY : _CatchableTypeArray = _CatchableTypeArray { nCatchableTypes : 1 , arrayOfCatchableTypes : [ptr_t :: null ()] } ; static mut CATCHABLE_TYPE : _CatchableType = _CatchableType { properties : 0 , pType : ptr_t :: null () , thisDisplacement : _PMD { mdisp : 0 , pdisp : - 1 , vdisp : 0 } , sizeOrOffset : size_of :: < Exception > () as c_int , copyFunction : ptr_t :: null () , } ; unsafe extern "C" { #[link_name = "\x01??_7type_info@@6B@"] static TYPE_INFO_VTABLE : * const u8 ; } static mut TYPE_DESCRIPTOR : _TypeDescriptor = _TypeDescriptor { pVFTable : (& raw const TYPE_INFO_VTABLE) as * const _ , spare : core :: ptr :: null_mut () , name : TYPE_NAME , } ; macro_rules ! define_cleanup { ($ abi : tt $ abi2 : tt) => { unsafe extern $ abi fn exception_cleanup (e : * mut Exception) { unsafe { if let Exception { data : Some (b) , .. } = e . read () { drop (b) ; super :: __rust_drop_panic () ; } } } unsafe extern $ abi2 fn exception_copy (_dest : * mut Exception , _src : * mut Exception) -> * mut Exception { unsafe { throw_exception (None) ; } } } } cfg_select ! { target_arch = "x86" => { define_cleanup ! ("thiscall" "thiscall-unwind") ; } _ => { define_cleanup ! ("C" "C-unwind") ; } } pub (crate) unsafe fn panic (data : Box < dyn Any + Send >) -> u32 { unsafe { throw_exception (Some (data)) } } unsafe fn throw_exception (data : Option < Box < dyn Any + Send > >) -> ! { use core :: intrinsics :: { AtomicOrdering , atomic_store } ; let mut exception = ManuallyDrop :: new (Exception { canary : (& raw const TYPE_DESCRIPTOR) , data }) ; let throw_ptr = (& raw mut exception) as * mut _ ; unsafe { atomic_store :: < _ , { AtomicOrdering :: SeqCst } > ((& raw mut THROW_INFO . pmfnUnwind) . cast () , ptr_t :: new (exception_cleanup as * mut u8) . raw () ,) ; atomic_store :: < _ , { AtomicOrdering :: SeqCst } > ((& raw mut THROW_INFO . pCatchableTypeArray) . cast () , ptr_t :: new ((& raw mut CATCHABLE_TYPE_ARRAY) . cast ()) . raw () ,) ; atomic_store :: < _ , { AtomicOrdering :: SeqCst } > ((& raw mut CATCHABLE_TYPE_ARRAY . arrayOfCatchableTypes [0]) . cast () , ptr_t :: new ((& raw mut CATCHABLE_TYPE) . cast ()) . raw () ,) ; atomic_store :: < _ , { AtomicOrdering :: SeqCst } > ((& raw mut CATCHABLE_TYPE . pType) . cast () , ptr_t :: new ((& raw mut TYPE_DESCRIPTOR) . cast ()) . raw () ,) ; atomic_store :: < _ , { AtomicOrdering :: SeqCst } > ((& raw mut CATCHABLE_TYPE . copyFunction) . cast () , ptr_t :: new (exception_copy as * mut u8) . raw () ,) ; } unsafe extern "system-unwind" { fn _CxxThrowException (pExceptionObject : * mut c_void , pThrowInfo : * mut u8) -> ! ; } unsafe { _CxxThrowException (throw_ptr , (& raw mut THROW_INFO) as * mut _) ; } } pub (crate) unsafe fn cleanup (payload : * mut u8) -> Box < dyn Any + Send > { unsafe { if payload . is_null () { super :: __rust_foreign_exception () ; } let exception = payload as * mut Exception ; let canary = (& raw const (* exception) . canary) . read () ; if ! core :: ptr :: eq (canary , & raw const TYPE_DESCRIPTOR) { super :: __rust_foreign_exception () ; } (* exception) . data . take () . unwrap () } }