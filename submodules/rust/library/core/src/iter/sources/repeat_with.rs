use split_decls_genesis :: ourprelude :: * ; use crate :: fmt ; use crate :: iter :: { FusedIterator , TrustedLen } ; use crate :: ops :: Try ; #[doc = " Creates a new iterator that repeats elements of type `A` endlessly by"] #[doc = " applying the provided closure, the repeater, `F: FnMut() -> A`."] #[doc = ""] #[doc = " The `repeat_with()` function calls the repeater over and over again."] #[doc = ""] #[doc = " Infinite iterators like `repeat_with()` are often used with adapters like"] #[doc = " [`Iterator::take()`], in order to make them finite."] #[doc = ""] #[doc = " If the element type of the iterator you need implements [`Clone`], and"] #[doc = " it is OK to keep the source element in memory, you should instead use"] #[doc = " the [`repeat()`] function."] #[doc = ""] #[doc = " An iterator produced by `repeat_with()` is not a [`DoubleEndedIterator`]."] #[doc = " If you need `repeat_with()` to return a [`DoubleEndedIterator`],"] #[doc = " please open a GitHub issue explaining your use case."] #[doc = ""] #[doc = " [`repeat()`]: crate::iter::repeat"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " use std::iter;"] #[doc = ""] #[doc = " // let's assume we have some value of a type that is not `Clone`"] #[doc = " // or which we don't want to have in memory just yet because it is expensive:"] #[doc = " #[derive(PartialEq, Debug)]"] #[doc = " struct Expensive;"] #[doc = ""] #[doc = " // a particular value forever:"] #[doc = " let mut things = iter::repeat_with(|| Expensive);"] #[doc = ""] #[doc = " assert_eq!(Some(Expensive), things.next());"] #[doc = " assert_eq!(Some(Expensive), things.next());"] #[doc = " assert_eq!(Some(Expensive), things.next());"] #[doc = " assert_eq!(Some(Expensive), things.next());"] #[doc = " assert_eq!(Some(Expensive), things.next());"] #[doc = " ```"] #[doc = ""] #[doc = " Using mutation and going finite:"] #[doc = ""] #[doc = " ```rust"] #[doc = " use std::iter;"] #[doc = ""] #[doc = " // From the zeroth to the third power of two:"] #[doc = " let mut curr = 1;"] #[doc = " let mut pow2 = iter::repeat_with(|| { let tmp = curr; curr *= 2; tmp })"] #[doc = "                     .take(4);"] #[doc = ""] #[doc = " assert_eq!(Some(1), pow2.next());"] #[doc = " assert_eq!(Some(2), pow2.next());"] #[doc = " assert_eq!(Some(4), pow2.next());"] #[doc = " assert_eq!(Some(8), pow2.next());"] #[doc = ""] #[doc = " // ... and now we're done"] #[doc = " assert_eq!(None, pow2.next());"] #[doc = " ```"] #[inline] #[stable (feature = "iterator_repeat_with" , since = "1.28.0")] pub fn repeat_with < A , F : FnMut () -> A > (repeater : F) -> RepeatWith < F > { RepeatWith { repeater } } #[doc = " An iterator that repeats elements of type `A` endlessly by"] #[doc = " applying the provided closure `F: FnMut() -> A`."] #[doc = ""] #[doc = " This `struct` is created by the [`repeat_with()`] function."] #[doc = " See its documentation for more."] #[derive (Copy , Clone)] #[stable (feature = "iterator_repeat_with" , since = "1.28.0")] pub struct RepeatWith < F > { repeater : F , } #[stable (feature = "iterator_repeat_with_debug" , since = "1.68.0")] impl < F > fmt :: Debug for RepeatWith < F > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("RepeatWith") . finish_non_exhaustive () } } #[stable (feature = "iterator_repeat_with" , since = "1.28.0")] impl < A , F : FnMut () -> A > Iterator for RepeatWith < F > { type Item = A ; #[inline] fn next (& mut self) -> Option < A > { Some ((self . repeater) ()) } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { (usize :: MAX , None) } #[inline] fn try_fold < Acc , Fold , R > (& mut self , mut init : Acc , mut fold : Fold) -> R where Fold : FnMut (Acc , Self :: Item) -> R , R : Try < Output = Acc > , { loop { let item = (self . repeater) () ; init = fold (init , item) ? ; } } } #[stable (feature = "iterator_repeat_with" , since = "1.28.0")] impl < A , F : FnMut () -> A > FusedIterator for RepeatWith < F > { } #[unstable (feature = "trusted_len" , issue = "37572")] unsafe impl < A , F : FnMut () -> A > TrustedLen for RepeatWith < F > { }