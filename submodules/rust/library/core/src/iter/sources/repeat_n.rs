mkuse!{use crate :: fmt ;}
mkuse!{use crate :: iter :: { FusedIterator , TrustedLen , UncheckedIterator } ;}
mkuse!{use crate :: num :: NonZero ;}
mkuse!{use crate :: ops :: Try ;}

macro_rules! repeat_n_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function repeat_n in module {}", module_path!());
    };
}

mkfn!{
    repeat_n_introspect!();
    # [doc = " Creates a new iterator that repeats a single element a given number of times."] # [doc = ""] # [doc = " The `repeat_n()` function repeats a single value exactly `n` times."] # [doc = ""] # [doc = " This is very similar to using [`repeat()`] with [`Iterator::take()`],"] # [doc = " but `repeat_n()` can return the original value, rather than always cloning."] # [doc = ""] # [doc = " [`repeat()`]: crate::iter::repeat"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::iter;"] # [doc = ""] # [doc = " // four of the number four:"] # [doc = " let mut four_fours = iter::repeat_n(4, 4);"] # [doc = ""] # [doc = " assert_eq!(Some(4), four_fours.next());"] # [doc = " assert_eq!(Some(4), four_fours.next());"] # [doc = " assert_eq!(Some(4), four_fours.next());"] # [doc = " assert_eq!(Some(4), four_fours.next());"] # [doc = ""] # [doc = " // no more fours"] # [doc = " assert_eq!(None, four_fours.next());"] # [doc = " ```"] # [doc = ""] # [doc = " For non-`Copy` types,"] # [doc = ""] # [doc = " ```"] # [doc = " use std::iter;"] # [doc = ""] # [doc = " let v: Vec<i32> = Vec::with_capacity(123);"] # [doc = " let mut it = iter::repeat_n(v, 5);"] # [doc = ""] # [doc = " for i in 0..4 {"] # [doc = "     // It starts by cloning things"] # [doc = "     let cloned = it.next().unwrap();"] # [doc = "     assert_eq!(cloned.len(), 0);"] # [doc = "     assert_eq!(cloned.capacity(), 0);"] # [doc = " }"] # [doc = ""] # [doc = " // ... but the last item is the original one"] # [doc = " let last = it.next().unwrap();"] # [doc = " assert_eq!(last.len(), 0);"] # [doc = " assert_eq!(last.capacity(), 123);"] # [doc = ""] # [doc = " // ... and now we're done"] # [doc = " assert_eq!(None, it.next());"] # [doc = " ```"] # [inline] # [stable (feature = "iter_repeat_n" , since = "1.82.0")] pub fn repeat_n < T : Clone > (element : T , count : usize) -> RepeatN < T > { RepeatN { inner : RepeatNInner :: new (element , count) } }
}
mkitem!{mkstruct!{# [derive (Clone , Copy)] struct RepeatNInner < T > { count : NonZero < usize > , element : T , }}}
mkitem!{mkimpl!{impl < T > RepeatNInner < T > { fn new (element : T , count : usize) -> Option < Self > { let count = NonZero :: < usize > :: new (count) ? ; Some (Self { element , count }) } }}}
mkitem!{mkstruct!{# [doc = " An iterator that repeats an element an exact number of times."] # [doc = ""] # [doc = " This `struct` is created by the [`repeat_n()`] function."] # [doc = " See its documentation for more."] # [stable (feature = "iter_repeat_n" , since = "1.82.0")] # [derive (Clone)] pub struct RepeatN < A > { inner : Option < RepeatNInner < A > > , }}}
mkitem!{mkimpl!{impl < A > RepeatN < A > { # [doc = " If we haven't already dropped the element, return it in an option."] # [inline] fn take_element (& mut self) -> Option < A > { self . inner . take () . map (| inner | inner . element) } }}}
mkitem!{mkimpl!{# [stable (feature = "iter_repeat_n" , since = "1.82.0")] impl < A : fmt :: Debug > fmt :: Debug for RepeatN < A > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { let (count , element) = match self . inner . as_ref () { Some (inner) => (inner . count . get () , Some (& inner . element)) , None => (0 , None) , } ; f . debug_struct ("RepeatN") . field ("count" , & count) . field ("element" , & element) . finish () } }}}
mkitem!{mkimpl!{# [stable (feature = "iter_repeat_n" , since = "1.82.0")] impl < A : Clone > Iterator for RepeatN < A > { type Item = A ; # [inline] fn next (& mut self) -> Option < A > { let inner = self . inner . as_mut () ? ; let count = inner . count . get () ; if let Some (decremented) = NonZero :: < usize > :: new (count - 1) { let tmp = inner . element . clone () ; inner . count = decremented ; return Some (tmp) ; } return self . take_element () ; } # [inline] fn size_hint (& self) -> (usize , Option < usize >) { let len = self . len () ; (len , Some (len)) } # [inline] fn advance_by (& mut self , skip : usize) -> Result < () , NonZero < usize > > { let Some (inner) = self . inner . as_mut () else { return NonZero :: < usize > :: new (skip) . map (Err) . unwrap_or (Ok (())) ; } ; let len = inner . count . get () ; if let Some (new_len) = len . checked_sub (skip) . and_then (NonZero :: < usize > :: new) { inner . count = new_len ; return Ok (()) ; } self . inner = None ; return NonZero :: < usize > :: new (skip - len) . map (Err) . unwrap_or (Ok (())) ; } # [inline] fn last (mut self) -> Option < A > { self . take_element () } # [inline] fn count (self) -> usize { self . len () } }}}
mkitem!{mkimpl!{# [stable (feature = "iter_repeat_n" , since = "1.82.0")] impl < A : Clone > ExactSizeIterator for RepeatN < A > { fn len (& self) -> usize { self . inner . as_ref () . map (| inner | inner . count . get ()) . unwrap_or (0) } }}}
mkitem!{mkimpl!{# [stable (feature = "iter_repeat_n" , since = "1.82.0")] impl < A : Clone > DoubleEndedIterator for RepeatN < A > { # [inline] fn next_back (& mut self) -> Option < A > { self . next () } # [inline] fn advance_back_by (& mut self , n : usize) -> Result < () , NonZero < usize > > { self . advance_by (n) } # [inline] fn nth_back (& mut self , n : usize) -> Option < A > { self . nth (n) } # [inline] fn try_rfold < B , F , R > (& mut self , init : B , f : F) -> R where F : FnMut (B , A) -> R , R : Try < Output = B > , { self . try_fold (init , f) } # [inline] fn rfold < B , F > (self , init : B , f : F) -> B where F : FnMut (B , A) -> B , { self . fold (init , f) } }}}
mkitem!{mkimpl!{# [stable (feature = "iter_repeat_n" , since = "1.82.0")] impl < A : Clone > FusedIterator for RepeatN < A > { }}}
mkitem!{mkimpl!{# [unstable (feature = "trusted_len" , issue = "37572")] unsafe impl < A : Clone > TrustedLen for RepeatN < A > { }}}
mkitem!{mkimpl!{# [stable (feature = "iter_repeat_n" , since = "1.82.0")] impl < A : Clone > UncheckedIterator for RepeatN < A > { }}}