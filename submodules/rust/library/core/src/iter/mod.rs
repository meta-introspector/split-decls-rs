# ! [doc = " Composable external iteration."] # ! [doc = ""] # ! [doc = " If you've found yourself with a collection of some kind, and needed to"] # ! [doc = " perform an operation on the elements of said collection, you'll quickly run"] # ! [doc = " into 'iterators'. Iterators are heavily used in idiomatic Rust code, so"] # ! [doc = " it's worth becoming familiar with them."] # ! [doc = ""] # ! [doc = " Before explaining more, let's talk about how this module is structured:"] # ! [doc = ""] # ! [doc = " # Organization"] # ! [doc = ""] # ! [doc = " This module is largely organized by type:"] # ! [doc = ""] # ! [doc = " * [Traits] are the core portion: these traits define what kind of iterators"] # ! [doc = "   exist and what you can do with them. The methods of these traits are worth"] # ! [doc = "   putting some extra study time into."] # ! [doc = " * [Functions] provide some helpful ways to create some basic iterators."] # ! [doc = " * [Structs] are often the return types of the various methods on this"] # ! [doc = "   module's traits. You'll usually want to look at the method that creates"] # ! [doc = "   the `struct`, rather than the `struct` itself. For more detail about why,"] # ! [doc = "   see '[Implementing Iterator](#implementing-iterator)'."] # ! [doc = ""] # ! [doc = " [Traits]: #traits"] # ! [doc = " [Functions]: #functions"] # ! [doc = " [Structs]: #structs"] # ! [doc = ""] # ! [doc = " That's it! Let's dig into iterators."] # ! [doc = ""] # ! [doc = " # Iterator"] # ! [doc = ""] # ! [doc = " The heart and soul of this module is the [`Iterator`] trait. The core of"] # ! [doc = " [`Iterator`] looks like this:"] # ! [doc = ""] # ! [doc = " ```"] # ! [doc = " trait Iterator {"] # ! [doc = "     type Item;"] # ! [doc = "     fn next(&mut self) -> Option<Self::Item>;"] # ! [doc = " }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " An iterator has a method, [`next`], which when called, returns an"] # ! [doc = " <code>[Option]\\<Item></code>. Calling [`next`] will return [`Some(Item)`] as long as there"] # ! [doc = " are elements, and once they've all been exhausted, will return `None` to"] # ! [doc = " indicate that iteration is finished. Individual iterators may choose to"] # ! [doc = " resume iteration, and so calling [`next`] again may or may not eventually"] # ! [doc = " start returning [`Some(Item)`] again at some point (for example, see [`TryIter`])."] # ! [doc = ""] # ! [doc = " [`Iterator`]'s full definition includes a number of other methods as well,"] # ! [doc = " but they are default methods, built on top of [`next`], and so you get"] # ! [doc = " them for free."] # ! [doc = ""] # ! [doc = " Iterators are also composable, and it's common to chain them together to do"] # ! [doc = " more complex forms of processing. See the [Adapters](#adapters) section"] # ! [doc = " below for more details."] # ! [doc = ""] # ! [doc = " [`Some(Item)`]: Some"] # ! [doc = " [`next`]: Iterator::next"] # ! [doc = " [`TryIter`]: ../../std/sync/mpsc/struct.TryIter.html"] # ! [doc = ""] # ! [doc = " # The three forms of iteration"] # ! [doc = ""] # ! [doc = " There are three common methods which can create iterators from a collection:"] # ! [doc = ""] # ! [doc = " * `iter()`, which iterates over `&T`."] # ! [doc = " * `iter_mut()`, which iterates over `&mut T`."] # ! [doc = " * `into_iter()`, which iterates over `T`."] # ! [doc = ""] # ! [doc = " Various things in the standard library may implement one or more of the"] # ! [doc = " three, where appropriate."] # ! [doc = ""] # ! [doc = " # Implementing Iterator"] # ! [doc = ""] # ! [doc = " Creating an iterator of your own involves two steps: creating a `struct` to"] # ! [doc = " hold the iterator's state, and then implementing [`Iterator`] for that `struct`."] # ! [doc = " This is why there are so many `struct`s in this module: there is one for"] # ! [doc = " each iterator and iterator adapter."] # ! [doc = ""] # ! [doc = " Let's make an iterator named `Counter` which counts from `1` to `5`:"] # ! [doc = ""] # ! [doc = " ```"] # ! [doc = " // First, the struct:"] # ! [doc = ""] # ! [doc = " /// An iterator which counts from one to five"] # ! [doc = " struct Counter {"] # ! [doc = "     count: usize,"] # ! [doc = " }"] # ! [doc = ""] # ! [doc = " // we want our count to start at one, so let's add a new() method to help."] # ! [doc = " // This isn't strictly necessary, but is convenient. Note that we start"] # ! [doc = " // `count` at zero, we'll see why in `next()`'s implementation below."] # ! [doc = " impl Counter {"] # ! [doc = "     fn new() -> Counter {"] # ! [doc = "         Counter { count: 0 }"] # ! [doc = "     }"] # ! [doc = " }"] # ! [doc = ""] # ! [doc = " // Then, we implement `Iterator` for our `Counter`:"] # ! [doc = ""] # ! [doc = " impl Iterator for Counter {"] # ! [doc = "     // we will be counting with usize"] # ! [doc = "     type Item = usize;"] # ! [doc = ""] # ! [doc = "     // next() is the only required method"] # ! [doc = "     fn next(&mut self) -> Option<Self::Item> {"] # ! [doc = "         // Increment our count. This is why we started at zero."] # ! [doc = "         self.count += 1;"] # ! [doc = ""] # ! [doc = "         // Check to see if we've finished counting or not."] # ! [doc = "         if self.count < 6 {"] # ! [doc = "             Some(self.count)"] # ! [doc = "         } else {"] # ! [doc = "             None"] # ! [doc = "         }"] # ! [doc = "     }"] # ! [doc = " }"] # ! [doc = ""] # ! [doc = " // And now we can use it!"] # ! [doc = ""] # ! [doc = " let mut counter = Counter::new();"] # ! [doc = ""] # ! [doc = " assert_eq!(counter.next(), Some(1));"] # ! [doc = " assert_eq!(counter.next(), Some(2));"] # ! [doc = " assert_eq!(counter.next(), Some(3));"] # ! [doc = " assert_eq!(counter.next(), Some(4));"] # ! [doc = " assert_eq!(counter.next(), Some(5));"] # ! [doc = " assert_eq!(counter.next(), None);"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " Calling [`next`] this way gets repetitive. Rust has a construct which can"] # ! [doc = " call [`next`] on your iterator, until it reaches `None`. Let's go over that"] # ! [doc = " next."] # ! [doc = ""] # ! [doc = " Also note that `Iterator` provides a default implementation of methods such as `nth` and `fold`"] # ! [doc = " which call `next` internally. However, it is also possible to write a custom implementation of"] # ! [doc = " methods like `nth` and `fold` if an iterator can compute them more efficiently without calling"] # ! [doc = " `next`."] # ! [doc = ""] # ! [doc = " # `for` loops and `IntoIterator`"] # ! [doc = ""] # ! [doc = " Rust's `for` loop syntax is actually sugar for iterators. Here's a basic"] # ! [doc = " example of `for`:"] # ! [doc = ""] # ! [doc = " ```"] # ! [doc = " let values = vec![1, 2, 3, 4, 5];"] # ! [doc = ""] # ! [doc = " for x in values {"] # ! [doc = "     println!(\"{x}\");"] # ! [doc = " }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " This will print the numbers one through five, each on their own line. But"] # ! [doc = " you'll notice something here: we never called anything on our vector to"] # ! [doc = " produce an iterator. What gives?"] # ! [doc = ""] # ! [doc = " There's a trait in the standard library for converting something into an"] # ! [doc = " iterator: [`IntoIterator`]. This trait has one method, [`into_iter`],"] # ! [doc = " which converts the thing implementing [`IntoIterator`] into an iterator."] # ! [doc = " Let's take a look at that `for` loop again, and what the compiler converts"] # ! [doc = " it into:"] # ! [doc = ""] # ! [doc = " [`into_iter`]: IntoIterator::into_iter"] # ! [doc = ""] # ! [doc = " ```"] # ! [doc = " let values = vec![1, 2, 3, 4, 5];"] # ! [doc = ""] # ! [doc = " for x in values {"] # ! [doc = "     println!(\"{x}\");"] # ! [doc = " }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " Rust de-sugars this into:"] # ! [doc = ""] # ! [doc = " ```"] # ! [doc = " let values = vec![1, 2, 3, 4, 5];"] # ! [doc = " {"] # ! [doc = "     let result = match IntoIterator::into_iter(values) {"] # ! [doc = "         mut iter => loop {"] # ! [doc = "             let next;"] # ! [doc = "             match iter.next() {"] # ! [doc = "                 Some(val) => next = val,"] # ! [doc = "                 None => break,"] # ! [doc = "             };"] # ! [doc = "             let x = next;"] # ! [doc = "             let () = { println!(\"{x}\"); };"] # ! [doc = "         },"] # ! [doc = "     };"] # ! [doc = "     result"] # ! [doc = " }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " First, we call `into_iter()` on the value. Then, we match on the iterator"] # ! [doc = " that returns, calling [`next`] over and over until we see a `None`. At"] # ! [doc = " that point, we `break` out of the loop, and we're done iterating."] # ! [doc = ""] # ! [doc = " There's one more subtle bit here: the standard library contains an"] # ! [doc = " interesting implementation of [`IntoIterator`]:"] # ! [doc = ""] # ! [doc = " ```ignore (only-for-syntax-highlight)"] # ! [doc = " impl<I: Iterator> IntoIterator for I"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " In other words, all [`Iterator`]s implement [`IntoIterator`], by just"] # ! [doc = " returning themselves. This means two things:"] # ! [doc = ""] # ! [doc = " 1. If you're writing an [`Iterator`], you can use it with a `for` loop."] # ! [doc = " 2. If you're creating a collection, implementing [`IntoIterator`] for it"] # ! [doc = "    will allow your collection to be used with the `for` loop."] # ! [doc = ""] # ! [doc = " # Iterating by reference"] # ! [doc = ""] # ! [doc = " Since [`into_iter()`] takes `self` by value, using a `for` loop to iterate"] # ! [doc = " over a collection consumes that collection. Often, you may want to iterate"] # ! [doc = " over a collection without consuming it. Many collections offer methods that"] # ! [doc = " provide iterators over references, conventionally called `iter()` and"] # ! [doc = " `iter_mut()` respectively:"] # ! [doc = ""] # ! [doc = " ```"] # ! [doc = " let mut values = vec![41];"] # ! [doc = " for x in values.iter_mut() {"] # ! [doc = "     *x += 1;"] # ! [doc = " }"] # ! [doc = " for x in values.iter() {"] # ! [doc = "     assert_eq!(*x, 42);"] # ! [doc = " }"] # ! [doc = " assert_eq!(values.len(), 1); // `values` is still owned by this function."] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " If a collection type `C` provides `iter()`, it usually also implements"] # ! [doc = " `IntoIterator` for `&C`, with an implementation that just calls `iter()`."] # ! [doc = " Likewise, a collection `C` that provides `iter_mut()` generally implements"] # ! [doc = " `IntoIterator` for `&mut C` by delegating to `iter_mut()`. This enables a"] # ! [doc = " convenient shorthand:"] # ! [doc = ""] # ! [doc = " ```"] # ! [doc = " let mut values = vec![41];"] # ! [doc = " for x in &mut values {"] # ! [doc = "     //   ^ same as `values.iter_mut()`"] # ! [doc = "     *x += 1;"] # ! [doc = " }"] # ! [doc = " for x in &values {"] # ! [doc = "     //   ^ same as `values.iter()`"] # ! [doc = "     assert_eq!(*x, 42);"] # ! [doc = " }"] # ! [doc = " assert_eq!(values.len(), 1);"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " While many collections offer `iter()`, not all offer `iter_mut()`. For"] # ! [doc = " example, mutating the keys of a [`HashSet<T>`] could put the collection"] # ! [doc = " into an inconsistent state if the key hashes change, so this collection"] # ! [doc = " only offers `iter()`."] # ! [doc = ""] # ! [doc = " [`into_iter()`]: IntoIterator::into_iter"] # ! [doc = " [`HashSet<T>`]: ../../std/collections/struct.HashSet.html"] # ! [doc = ""] # ! [doc = " # Adapters"] # ! [doc = ""] # ! [doc = " Functions which take an [`Iterator`] and return another [`Iterator`] are"] # ! [doc = " often called 'iterator adapters', as they're a form of the 'adapter"] # ! [doc = " pattern'."] # ! [doc = ""] # ! [doc = " Common iterator adapters include [`map`], [`take`], and [`filter`]."] # ! [doc = " For more, see their documentation."] # ! [doc = ""] # ! [doc = " If an iterator adapter panics, the iterator will be in an unspecified (but"] # ! [doc = " memory safe) state.  This state is also not guaranteed to stay the same"] # ! [doc = " across versions of Rust, so you should avoid relying on the exact values"] # ! [doc = " returned by an iterator which panicked."] # ! [doc = ""] # ! [doc = " [`map`]: Iterator::map"] # ! [doc = " [`take`]: Iterator::take"] # ! [doc = " [`filter`]: Iterator::filter"] # ! [doc = ""] # ! [doc = " # Laziness"] # ! [doc = ""] # ! [doc = " Iterators (and iterator [adapters](#adapters)) are *lazy*. This means that"] # ! [doc = " just creating an iterator doesn't _do_ a whole lot. Nothing really happens"] # ! [doc = " until you call [`next`]. This is sometimes a source of confusion when"] # ! [doc = " creating an iterator solely for its side effects. For example, the [`map`]"] # ! [doc = " method calls a closure on each element it iterates over:"] # ! [doc = ""] # ! [doc = " ```"] # ! [doc = " # #![allow(unused_must_use)]"] # ! [doc = " # #![allow(map_unit_fn)]"] # ! [doc = " let v = vec![1, 2, 3, 4, 5];"] # ! [doc = " v.iter().map(|x| println!(\"{x}\"));"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " This will not print any values, as we only created an iterator, rather than"] # ! [doc = " using it. The compiler will warn us about this kind of behavior:"] # ! [doc = ""] # ! [doc = " ```text"] # ! [doc = " warning: unused result that must be used: iterators are lazy and"] # ! [doc = " do nothing unless consumed"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " The idiomatic way to write a [`map`] for its side effects is to use a"] # ! [doc = " `for` loop or call the [`for_each`] method:"] # ! [doc = ""] # ! [doc = " ```"] # ! [doc = " let v = vec![1, 2, 3, 4, 5];"] # ! [doc = ""] # ! [doc = " v.iter().for_each(|x| println!(\"{x}\"));"] # ! [doc = " // or"] # ! [doc = " for x in &v {"] # ! [doc = "     println!(\"{x}\");"] # ! [doc = " }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " [`map`]: Iterator::map"] # ! [doc = " [`for_each`]: Iterator::for_each"] # ! [doc = ""] # ! [doc = " Another common way to evaluate an iterator is to use the [`collect`]"] # ! [doc = " method to produce a new collection."] # ! [doc = ""] # ! [doc = " [`collect`]: Iterator::collect"] # ! [doc = ""] # ! [doc = " # Infinity"] # ! [doc = ""] # ! [doc = " Iterators do not have to be finite. As an example, an open-ended range is"] # ! [doc = " an infinite iterator:"] # ! [doc = ""] # ! [doc = " ```"] # ! [doc = " let numbers = 0..;"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " It is common to use the [`take`] iterator adapter to turn an infinite"] # ! [doc = " iterator into a finite one:"] # ! [doc = ""] # ! [doc = " ```"] # ! [doc = " let numbers = 0..;"] # ! [doc = " let five_numbers = numbers.take(5);"] # ! [doc = ""] # ! [doc = " for number in five_numbers {"] # ! [doc = "     println!(\"{number}\");"] # ! [doc = " }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " This will print the numbers `0` through `4`, each on their own line."] # ! [doc = ""] # ! [doc = " Bear in mind that methods on infinite iterators, even those for which a"] # ! [doc = " result can be determined mathematically in finite time, might not terminate."] # ! [doc = " Specifically, methods such as [`min`], which in the general case require"] # ! [doc = " traversing every element in the iterator, are likely not to return"] # ! [doc = " successfully for any infinite iterators."] # ! [doc = ""] # ! [doc = " ```no_run"] # ! [doc = " let ones = std::iter::repeat(1);"] # ! [doc = " let least = ones.min().unwrap(); // Oh no! An infinite loop!"] # ! [doc = " // `ones.min()` causes an infinite loop, so we won't reach this point!"] # ! [doc = " println!(\"The smallest number one is {least}.\");"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " [`take`]: Iterator::take"] # ! [doc = " [`min`]: Iterator::min"] # ! [stable (feature = "rust1" , since = "1.0.0")] use split_decls_genesis :: ourprelude :: * ; macro_rules ! impl_fold_via_try_fold { (fold -> try_fold) => { impl_fold_via_try_fold ! { @ internal fold -> try_fold } } ; (rfold -> try_rfold) => { impl_fold_via_try_fold ! { @ internal rfold -> try_rfold } } ; (spec_fold -> spec_try_fold) => { impl_fold_via_try_fold ! { @ internal spec_fold -> spec_try_fold } } ; (spec_rfold -> spec_try_rfold) => { impl_fold_via_try_fold ! { @ internal spec_rfold -> spec_try_rfold } } ; (@ internal $ fold : ident -> $ try_fold : ident) => { #[inline] fn $ fold < AAA , FFF > (mut self , init : AAA , fold : FFF) -> AAA where FFF : FnMut (AAA , Self :: Item) -> AAA , { use crate :: ops :: NeverShortCircuit ; self .$ try_fold (init , NeverShortCircuit :: wrap_mut_2 (fold)) . 0 } } ; } #[unstable (feature = "iter_array_chunks" , reason = "recently added" , issue = "100450")] pub use self :: adapters :: ArrayChunks ; #[unstable (feature = "std_internals" , issue = "none")] pub use self :: adapters :: ByRefSized ; #[stable (feature = "iter_cloned" , since = "1.1.0")] pub use self :: adapters :: Cloned ; #[stable (feature = "iter_copied" , since = "1.36.0")] pub use self :: adapters :: Copied ; #[stable (feature = "iterator_flatten" , since = "1.29.0")] pub use self :: adapters :: Flatten ; #[stable (feature = "iter_map_while" , since = "1.57.0")] pub use self :: adapters :: MapWhile ; #[unstable (feature = "iter_map_windows" , reason = "recently added" , issue = "87155")] pub use self :: adapters :: MapWindows ; #[unstable (feature = "inplace_iteration" , issue = "none")] pub use self :: adapters :: SourceIter ; #[stable (feature = "iterator_step_by" , since = "1.28.0")] pub use self :: adapters :: StepBy ; #[unstable (feature = "trusted_random_access" , issue = "none")] pub use self :: adapters :: TrustedRandomAccess ; #[unstable (feature = "trusted_random_access" , issue = "none")] pub use self :: adapters :: TrustedRandomAccessNoCoerce ; #[stable (feature = "iter_chain" , since = "1.91.0")] pub use self :: adapters :: chain ; pub (crate) use self :: adapters :: try_process ; #[stable (feature = "iter_zip" , since = "1.59.0")] pub use self :: adapters :: zip ; #[stable (feature = "rust1" , since = "1.0.0")] pub use self :: adapters :: { Chain , Cycle , Enumerate , Filter , FilterMap , FlatMap , Fuse , Inspect , Map , Peekable , Rev , Scan , Skip , SkipWhile , Take , TakeWhile , Zip , } ; #[unstable (feature = "iter_intersperse" , reason = "recently added" , issue = "79524")] pub use self :: adapters :: { Intersperse , IntersperseWith } ; #[unstable (feature = "step_trait" , reason = "likely to be replaced by finer-grained traits" , issue = "42168")] pub use self :: range :: Step ; #[unstable (feature = "iter_macro" , issue = "142269" , reason = "generators are unstable")] pub use self :: sources :: iter ; #[stable (feature = "iter_empty" , since = "1.2.0")] pub use self :: sources :: { Empty , empty } ; #[unstable (feature = "iter_from_coroutine" , issue = "43122" , reason = "coroutines are unstable")] pub use self :: sources :: { FromCoroutine , from_coroutine } ; #[stable (feature = "iter_from_fn" , since = "1.34.0")] pub use self :: sources :: { FromFn , from_fn } ; #[stable (feature = "iter_once" , since = "1.2.0")] pub use self :: sources :: { Once , once } ; #[stable (feature = "iter_once_with" , since = "1.43.0")] pub use self :: sources :: { OnceWith , once_with } ; #[stable (feature = "rust1" , since = "1.0.0")] pub use self :: sources :: { Repeat , repeat } ; #[stable (feature = "iter_repeat_n" , since = "1.82.0")] pub use self :: sources :: { RepeatN , repeat_n } ; #[stable (feature = "iterator_repeat_with" , since = "1.28.0")] pub use self :: sources :: { RepeatWith , repeat_with } ; #[stable (feature = "iter_successors" , since = "1.34.0")] pub use self :: sources :: { Successors , successors } ; #[stable (feature = "fused" , since = "1.26.0")] pub use self :: traits :: FusedIterator ; #[unstable (issue = "none" , feature = "inplace_iteration")] pub use self :: traits :: InPlaceIterable ; #[stable (feature = "rust1" , since = "1.0.0")] pub use self :: traits :: Iterator ; #[unstable (issue = "none" , feature = "trusted_fused")] pub use self :: traits :: TrustedFused ; #[unstable (feature = "trusted_len" , issue = "37572")] pub use self :: traits :: TrustedLen ; #[unstable (feature = "trusted_step" , issue = "85731")] pub use self :: traits :: TrustedStep ; pub (crate) use self :: traits :: UncheckedIterator ; #[stable (feature = "rust1" , since = "1.0.0")] pub use self :: traits :: { DoubleEndedIterator , ExactSizeIterator , Extend , FromIterator , IntoIterator , Product , Sum , } ; mod adapters ; mod range ; mod sources ; mod traits ;