mkuse!{use std :: sync :: Mutex ;}
mkuse!{use std :: collections :: HashMap ;}
mkuse!{use std :: sync :: LazyLock ;}
mkitem!{static USE_MATRIX : LazyLock < Mutex < HashMap < String , Vec < String > > > > = LazyLock :: new (| | Mutex :: new (HashMap :: new ())) ;}

macro_rules! get_use_matrix_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function get_use_matrix in module {}", module_path!());
    };
}

mkfn!{
    get_use_matrix_introspect!();
    pub fn get_use_matrix () -> HashMap < String , Vec < String > > { USE_MATRIX . lock () . unwrap () . clone () }
}
mkitem!{macro_rules ! emit_message { ($ ($ arg : tt) *) => { { use std :: fs :: OpenOptions ; use std :: io :: Write ; let message = format ! ($ ($ arg) *) ; if let Ok (mut file) = OpenOptions :: new () . create (true) . append (true) . open ("macro_report.txt") { let _ = writeln ! (file , "{}" , message) ; } } } ; }}
mkitem!{macro_rules ! mkfn { ($ introspect : expr ; $ (#[$ attr : meta]) * pub fn $ name : ident < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * pub fn $ name < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_trait_bounds_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_trait_bounds_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * fn $ name < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: trait_bounds_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: trait_bounds_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn catch_fatal_errors < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * fn catch_fatal_errors < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: catch_fatal_errors_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: catch_fatal_errors_specific") ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn parse_crate_attrs < $ lifetime : lifetime > ($ ($ param : tt) *) -> PResult < $ lifetime2 : lifetime , ast :: AttrVec > $ body : block) => { $ (#[$ attr]) * fn parse_crate_attrs < $ lifetime > ($ ($ param) *) -> PResult < $ lifetime2 , ast :: AttrVec > { $ introspect ; emit_message ! ("🚀 MARKER: parse_crate_attrs_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: parse_crate_attrs_specific") ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn init_logger_with_additional_layer < F , T > ($ ($ param : tt) *) where F : FnOnce () -> T , T : rustc_log :: BuildSubscriberRet , $ body : block) => { $ (#[$ attr]) * fn init_logger_with_additional_layer < F , T > ($ ($ param) *) where F : FnOnce () -> T , T : rustc_log :: BuildSubscriberRet , { $ introspect ; emit_message ! ("🚀 MARKER: init_logger_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: init_logger_specific") ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident < F , T > ($ ($ param : tt) *) $ (-> $ ret : ty) ? where F : FnOnce ($ ($ fnonce_args : tt) *) $ ($ where_rest : tt) * $ body : block) => { $ (#[$ attr]) * fn $ name < F , T > ($ ($ param) *) $ (-> $ ret) ? where F : FnOnce ($ ($ fnonce_args) *) $ ($ where_rest) * { $ introspect ; emit_message ! ("🚀 MARKER: two_generics_where_fnonce - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics_where_fnonce - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? where $ ($ where_clause : tt) * $ body : block) => { $ (#[$ attr]) * fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? where $ ($ where_clause) * { $ introspect ; emit_message ! ("🚀 MARKER: two_generics_where - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics_where - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: two_generics - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * pub fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * pub fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_two_generics - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_two_generics - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * pub ($ vis : ident) fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * pub ($ vis) fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_vis - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_vis - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident < $ gen : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * fn $ name < $ gen > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: generic_single - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: generic_single - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * pub fn $ name : ident < $ gen : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * pub fn $ name < $ gen > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_generic_single - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_generic_single - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident < $ lifetime : lifetime > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * fn $ name < $ lifetime > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: lifetime - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: lifetime - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * pub fn $ name : ident < $ lifetime : lifetime > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * pub fn $ name < $ lifetime > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_lifetime - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_lifetime - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * pub fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * pub fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_non_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_non_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: non_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: non_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; fn $ name : ident () $ body : block) => { fn $ name () { $ introspect ; emit_message ! ("🚀 MARKER: simple - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: simple - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ ($ anything : tt) *) => { $ ($ anything) * } ; }}
mkitem!{macro_rules ! safe_println { ($ ($ arg : tt) *) => { () } ; }}
mkitem!{macro_rules ! safe_print { ($ ($ arg : tt) *) => { () } ; }}
mkitem!{#[macro_export] macro_rules ! include_rust_compiler { ($ crate_name : literal , $ subpath : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_" , $ subpath , "_" , $ file , ".rs")) ; } ; ($ crate_name : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_src_" , $ file , ".rs")) ; } ; ($ crate_name : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_src_lib.rs")) ; } ; }}
mkitem!{#[macro_export] macro_rules ! include_rust_library { ($ lib_name : literal , $ subpath : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_" , $ subpath , "_" , $ file , ".rs")) ; } ; ($ lib_name : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_src_" , $ file , ".rs")) ; } ; ($ lib_name : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_src_lib.rs")) ; } ; }}
mkitem!{#[macro_export] macro_rules ! include_processed { ($ path : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_" , $ path , ".rs")) ; } ; }}
mkitem!{macro_rules ! mkinclude { ($ path : ident) => { } ; ($ path : literal) => { include ! ($ path) } ; }}
mkitem!{macro_rules ! mkitem { (include ! ($ path : ident) ;) => { } ; ($ macro_name : ident :: $ macro_sub : ident ! { $ string_lit : literal }) => { $ macro_name :: $ macro_sub ! { $ string_lit } } ; ($ macro_name : ident :: $ macro_sub : ident ! { $ ($ args : tt) * }) => { $ macro_name :: $ macro_sub ! { $ ($ args) * } } ; ($ macro_name : ident ! { $ ($ args : tt) * }) => { $ macro_name ! { $ ($ args) * } } ; ($ item : item) => { $ item } ; }}
mkitem!{#[macro_export] macro_rules ! mkmod { ($ name : ident , { $ ($ content : tt) * }) => { emit_message ! ("MOD|{}|{}" , module_path ! () , stringify ! ($ name)) ; mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; (pub mod $ name : ident { $ ($ content : tt) * }) => { emit_message ! ("MOD|{}|{}" , module_path ! () , stringify ! ($ name)) ; pub mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; (mod $ name : ident { $ ($ content : tt) * }) => { emit_message ! ("MOD|{}|{}" , module_path ! () , stringify ! ($ name)) ; mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; }}
mkitem!{#[macro_export] macro_rules ! mkuse { ($ use_stmt : item) => { emit_message ! ("USE|{}|{}" , module_path ! () , stringify ! ($ use_stmt)) ; $ use_stmt } ; }}
mkitem!{macro_rules ! mkstruct { ($ struct_def : item) => { $ struct_def } ; }}
mkitem!{macro_rules ! mkenum { ($ enum_def : item) => { $ enum_def } ; }}
mkitem!{macro_rules ! mktrait { ($ trait_def : item) => { $ trait_def } ; }}
mkitem!{macro_rules ! mkimpl { ($ impl_def : item) => { $ impl_def } ; }}
mkitem!{macro_rules ! getname { ($ name : ident) => { stringify ! ($ name) } ; }}
mkitem!{macro_rules ! getsrc { ($ name : ident) => { "processed file" } ; }}
mkitem!{macro_rules ! getpath { ($ name : ident) => { "processed_path" } ; }}
mkitem!{macro_rules ! get_deps { ($ name : ident) => { vec ! [] } ; }}
mkmod!{rustc_complete, { 
                getname!(rustc_complete);
                getsrc!(rustc_complete);
                getpath!(rustc_complete);
                get_deps!(rustc_complete);
                get_crates!(rustc_complete);
                mkinclude!(rustc_complete);
                mkmod!{emitter, { 
                getname!(emitter);
                getsrc!(emitter);
                getpath!(emitter);
                get_deps!(emitter);
                get_crates!(emitter);
                mkinclude!(emitter);
                
macro_rules! stderr_destination_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function stderr_destination in module {}", module_path!());
    };
}

mkfn!{
    stderr_destination_introspect!();
    pub fn stderr_destination () { }
} 
            }}
mkmod!{registry, { 
                getname!(registry);
                getsrc!(registry);
                getpath!(registry);
                get_deps!(registry);
                get_crates!(registry);
                mkinclude!(registry);
                mkitem!{mkstruct!{pub struct Registry ;}} 
            }}
mkmod!{translation, { 
                getname!(translation);
                getsrc!(translation);
                getpath!(translation);
                get_deps!(translation);
                get_crates!(translation);
                mkinclude!(translation);
                mkitem!{mkstruct!{pub struct Translator ;}} 
            }}
mkitem!{mkstruct!{pub struct ColorConfig ;}}
mkitem!{mkstruct!{pub struct DiagCtxt ;}}
mkitem!{mkstruct!{pub struct ErrCode ;}}
mkitem!{mkstruct!{pub struct FatalError ;}}
mkitem!{mkstruct!{pub struct PResult < T > (pub T) ;}}
mkmod!{markdown, { 
                getname!(markdown);
                getsrc!(markdown);
                getpath!(markdown);
                get_deps!(markdown);
                get_crates!(markdown);
                mkinclude!(markdown);
                 
            }}
mkmod!{config, { 
                getname!(config);
                getsrc!(config);
                getpath!(config);
                get_deps!(config);
                get_crates!(config);
                mkinclude!(config);
                mkitem!{mkstruct!{pub struct CG_OPTIONS ;}}
mkitem!{mkstruct!{pub struct CrateType ;}}
mkitem!{mkstruct!{pub struct ErrorOutputType ;}}
mkitem!{mkstruct!{pub struct Input ;}}
mkitem!{mkstruct!{pub struct OptionDesc ;}}
mkitem!{mkstruct!{pub struct OutFileName ;}}
mkitem!{mkstruct!{pub struct OutputType ;}}
mkitem!{mkstruct!{pub struct Sysroot ;}}
mkitem!{mkstruct!{pub struct UnstableOptions ;}}
mkitem!{mkstruct!{pub struct Z_OPTIONS ;}}

macro_rules! nightly_options_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function nightly_options in module {}", module_path!());
    };
}

mkfn!{
    nightly_options_introspect!();
    pub fn nightly_options () { }
}

macro_rules! parse_target_triple_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function parse_target_triple in module {}", module_path!());
    };
}

mkfn!{
    parse_target_triple_introspect!();
    pub fn parse_target_triple () { }
} 
            }}
mkmod!{getopts, { 
                getname!(getopts);
                getsrc!(getopts);
                getpath!(getopts);
                get_deps!(getopts);
                get_crates!(getopts);
                mkinclude!(getopts);
                mkitem!{mkstruct!{pub struct Matches ;}} 
            }}
mkmod!{lint, { 
                getname!(lint);
                getsrc!(lint);
                getpath!(lint);
                get_deps!(lint);
                get_crates!(lint);
                mkinclude!(lint);
                mkitem!{mkstruct!{pub struct Lint ;}}
mkitem!{mkstruct!{pub struct LintId ;}} 
            }}
mkmod!{output, { 
                getname!(output);
                getsrc!(output);
                getpath!(output);
                get_deps!(output);
                get_crates!(output);
                mkinclude!(output);
                mkitem!{mkstruct!{pub struct CRATE_TYPES ;}}

macro_rules! collect_crate_types_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function collect_crate_types in module {}", module_path!());
    };
}

mkfn!{
    collect_crate_types_introspect!();
    pub fn collect_crate_types () { }
}

macro_rules! invalid_output_for_target_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function invalid_output_for_target in module {}", module_path!());
    };
}

mkfn!{
    invalid_output_for_target_introspect!();
    pub fn invalid_output_for_target () { }
} 
            }}
mkitem!{mkstruct!{pub struct EarlyDiagCtxt ;}}
mkitem!{mkstruct!{pub struct Session ;}}
mkitem!{mkstruct!{pub struct FileName ;}}
mkmod!{def_id, { 
                getname!(def_id);
                getsrc!(def_id);
                getpath!(def_id);
                get_deps!(def_id);
                get_crates!(def_id);
                mkinclude!(def_id);
                mkitem!{mkstruct!{pub struct LOCAL_CRATE ;}} 
            }}
mkmod!{ty, { 
                getname!(ty);
                getsrc!(ty);
                getpath!(ty);
                get_deps!(ty);
                get_crates!(ty);
                mkinclude!(ty);
                mkitem!{mkstruct!{pub struct TyCtxt < T > (pub T) ;}} 
            }} 
            }}
mkmod!{session_diagnostics, { 
                getname!(session_diagnostics);
                getsrc!(session_diagnostics);
                getpath!(session_diagnostics);
                get_deps!(session_diagnostics);
                get_crates!(session_diagnostics);
                mkinclude!(session_diagnostics);
                mkitem!{mkstruct!{pub struct CantEmitMIR ;}}
mkitem!{mkstruct!{pub struct RLinkEmptyVersionNumber ;}}
mkitem!{mkstruct!{pub struct RLinkEncodingVersionMismatch ;}}
mkitem!{mkstruct!{pub struct RLinkRustcVersionMismatch ;}}
mkitem!{mkstruct!{pub struct RLinkWrongFileType ;}}
mkitem!{mkstruct!{pub struct RlinkCorruptFile ;}}
mkitem!{mkstruct!{pub struct RlinkNotAFile ;}}
mkitem!{mkstruct!{pub struct RlinkUnableToRead ;}}
mkitem!{mkstruct!{pub struct UnstableFeatureUsage ;}} 
            }}
mkitem!{macro_rules ! do_not_use_print { ($ ($ t : tt) *) => { compile_error ! ("Don't use print") } ; }}
mkitem!{macro_rules ! do_not_use_safe_print { ($ ($ t : tt) *) => { compile_error ! ("Don't use safe_print") } ; }}
mkitem!{macro_rules ! mktrait { ($ trait_def : item) => { $ trait_def } ; }}
mkitem!{macro_rules ! mkimpl { ($ impl_def : item) => { $ impl_def } ; }}
mkitem!{macro_rules ! getname { ($ name : ident) => { pub fn get_module_name () -> &'static str { stringify ! ($ name) } } ; }}
mkitem!{macro_rules ! getsrc { ($ name : ident) => { pub fn get_source_info () -> &'static str { concat ! ("Module: " , stringify ! ($ name)) } } ; }}
mkitem!{macro_rules ! getpath { ($ name : ident) => { pub fn get_module_path () -> &'static str { module_path ! () } } ; }}
mkitem!{macro_rules ! get_deps { ($ name : ident) => { pub fn get_dependencies () -> &'static [&'static str] { & [] } } ; }}
mkitem!{macro_rules ! get_crates { ($ name : ident) => { pub fn get_required_crates () -> &'static [&'static str] { & [] } } ; }}
mkitem!{macro_rules ! forall_crates { ($ ($ crate_name : ident) ,*) => { $ (extern crate $ crate_name ;) * } ; }}
mkitem!{macro_rules ! emit_extern { ($ crate_name : ident) => { extern crate $ crate_name ; } ; }}
mkitem!{macro_rules ! get_externs { ($ crate_name : ident) => { stringify ! ($ crate_name) } ; }}
mkuse!{use super :: super :: { ArrayChunks , ByRefSized , Chain , Cloned , Copied , Cycle , Enumerate , Filter , FilterMap , FlatMap , Flatten , Fuse , Inspect , Intersperse , IntersperseWith , Map , MapWhile , MapWindows , Peekable , Product , Rev , Scan , Skip , SkipWhile , StepBy , Sum , Take , TakeWhile , TrustedRandomAccessNoCoerce , Zip , try_process , } ;}
mkuse!{use crate :: array ;}
mkuse!{use crate :: cmp :: { self , Ordering } ;}
mkuse!{use crate :: num :: NonZero ;}
mkuse!{use crate :: ops :: { ChangeOutputType , ControlFlow , FromResidual , Residual , Try } ;}

macro_rules! _assert_is_dyn_compatible_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function _assert_is_dyn_compatible in module {}", module_path!());
    };
}

mkfn!{
    _assert_is_dyn_compatible_introspect!();
    fn _assert_is_dyn_compatible (_ : & dyn Iterator < Item = () >) { }
}
mkitem!{mktrait!{#[doc = " A trait for dealing with iterators."] #[doc = ""] #[doc = " This is the main iterator trait. For more about the concept of iterators"] #[doc = " generally, please see the [module-level documentation]. In particular, you"] #[doc = " may want to know how to [implement `Iterator`][impl]."] #[doc = ""] #[doc = " [module-level documentation]: crate::iter"] #[doc = " [impl]: crate::iter#implementing-iterator"] #[stable (feature = "rust1" , since = "1.0.0")] #[rustc_on_unimplemented (on (Self = "core::ops::range::RangeTo<Idx>" , note = "you might have meant to use a bounded `Range`") , on (Self = "core::ops::range::RangeToInclusive<Idx>" , note = "you might have meant to use a bounded `RangeInclusive`") , label = "`{Self}` is not an iterator" , message = "`{Self}` is not an iterator")] #[doc (notable_trait)] #[lang = "iterator"] #[rustc_diagnostic_item = "Iterator"] #[must_use = "iterators are lazy and do nothing unless consumed"] pub trait Iterator { #[doc = " The type of the elements being iterated over."] #[rustc_diagnostic_item = "IteratorItem"] #[stable (feature = "rust1" , since = "1.0.0")] type Item ; #[doc = " Advances the iterator and returns the next value."] #[doc = ""] #[doc = " Returns [`None`] when iteration is finished. Individual iterator"] #[doc = " implementations may choose to resume iteration, and so calling `next()`"] #[doc = " again may or may not eventually start returning [`Some(Item)`] again at some"] #[doc = " point."] #[doc = ""] #[doc = " [`Some(Item)`]: Some"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let mut iter = a.into_iter();"] #[doc = ""] #[doc = " // A call to next() returns the next value..."] #[doc = " assert_eq!(Some(1), iter.next());"] #[doc = " assert_eq!(Some(2), iter.next());"] #[doc = " assert_eq!(Some(3), iter.next());"] #[doc = ""] #[doc = " // ... and then None once it's over."] #[doc = " assert_eq!(None, iter.next());"] #[doc = ""] #[doc = " // More calls may or may not return `None`. Here, they always will."] #[doc = " assert_eq!(None, iter.next());"] #[doc = " assert_eq!(None, iter.next());"] #[doc = " ```"] #[lang = "next"] #[stable (feature = "rust1" , since = "1.0.0")] fn next (& mut self) -> Option < Self :: Item > ; #[doc = " Advances the iterator and returns an array containing the next `N` values."] #[doc = ""] #[doc = " If there are not enough elements to fill the array then `Err` is returned"] #[doc = " containing an iterator over the remaining elements."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_next_chunk)]"] #[doc = ""] #[doc = " let mut iter = \"lorem\".chars();"] #[doc = ""] #[doc = " assert_eq!(iter.next_chunk().unwrap(), ['l', 'o']);              // N is inferred as 2"] #[doc = " assert_eq!(iter.next_chunk().unwrap(), ['r', 'e', 'm']);         // N is inferred as 3"] #[doc = " assert_eq!(iter.next_chunk::<4>().unwrap_err().as_slice(), &[]); // N is explicitly 4"] #[doc = " ```"] #[doc = ""] #[doc = " Split a string and get the first three items."] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_next_chunk)]"] #[doc = ""] #[doc = " let quote = \"not all those who wander are lost\";"] #[doc = " let [first, second, third] = quote.split_whitespace().next_chunk().unwrap();"] #[doc = " assert_eq!(first, \"not\");"] #[doc = " assert_eq!(second, \"all\");"] #[doc = " assert_eq!(third, \"those\");"] #[doc = " ```"] #[inline] #[unstable (feature = "iter_next_chunk" , reason = "recently added" , issue = "98326")] fn next_chunk < const N : usize > (& mut self ,) -> Result < [Self :: Item ; N] , array :: IntoIter < Self :: Item , N > > where Self : Sized , { array :: iter_next_chunk (self) } #[doc = " Returns the bounds on the remaining length of the iterator."] #[doc = ""] #[doc = " Specifically, `size_hint()` returns a tuple where the first element"] #[doc = " is the lower bound, and the second element is the upper bound."] #[doc = ""] #[doc = " The second half of the tuple that is returned is an <code>[Option]<[usize]></code>."] #[doc = " A [`None`] here means that either there is no known upper bound, or the"] #[doc = " upper bound is larger than [`usize`]."] #[doc = ""] #[doc = " # Implementation notes"] #[doc = ""] #[doc = " It is not enforced that an iterator implementation yields the declared"] #[doc = " number of elements. A buggy iterator may yield less than the lower bound"] #[doc = " or more than the upper bound of elements."] #[doc = ""] #[doc = " `size_hint()` is primarily intended to be used for optimizations such as"] #[doc = " reserving space for the elements of the iterator, but must not be"] #[doc = " trusted to e.g., omit bounds checks in unsafe code. An incorrect"] #[doc = " implementation of `size_hint()` should not lead to memory safety"] #[doc = " violations."] #[doc = ""] #[doc = " That said, the implementation should provide a correct estimation,"] #[doc = " because otherwise it would be a violation of the trait's protocol."] #[doc = ""] #[doc = " The default implementation returns <code>(0, [None])</code> which is correct for any"] #[doc = " iterator."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = " let mut iter = a.iter();"] #[doc = ""] #[doc = " assert_eq!((3, Some(3)), iter.size_hint());"] #[doc = " let _ = iter.next();"] #[doc = " assert_eq!((2, Some(2)), iter.size_hint());"] #[doc = " ```"] #[doc = ""] #[doc = " A more complex example:"] #[doc = ""] #[doc = " ```"] #[doc = " // The even numbers in the range of zero to nine."] #[doc = " let iter = (0..10).filter(|x| x % 2 == 0);"] #[doc = ""] #[doc = " // We might iterate from zero to ten times. Knowing that it's five"] #[doc = " // exactly wouldn't be possible without executing filter()."] #[doc = " assert_eq!((0, Some(10)), iter.size_hint());"] #[doc = ""] #[doc = " // Let's add five more numbers with chain()"] #[doc = " let iter = (0..10).filter(|x| x % 2 == 0).chain(15..20);"] #[doc = ""] #[doc = " // now both bounds are increased by five"] #[doc = " assert_eq!((5, Some(15)), iter.size_hint());"] #[doc = " ```"] #[doc = ""] #[doc = " Returning `None` for an upper bound:"] #[doc = ""] #[doc = " ```"] #[doc = " // an infinite iterator has no upper bound"] #[doc = " // and the maximum possible lower bound"] #[doc = " let iter = 0..;"] #[doc = ""] #[doc = " assert_eq!((usize::MAX, None), iter.size_hint());"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn size_hint (& self) -> (usize , Option < usize >) { (0 , None) } #[doc = " Consumes the iterator, counting the number of iterations and returning it."] #[doc = ""] #[doc = " This method will call [`next`] repeatedly until [`None`] is encountered,"] #[doc = " returning the number of times it saw [`Some`]. Note that [`next`] has to be"] #[doc = " called at least once even if the iterator does not have any elements."] #[doc = ""] #[doc = " [`next`]: Iterator::next"] #[doc = ""] #[doc = " # Overflow Behavior"] #[doc = ""] #[doc = " The method does no guarding against overflows, so counting elements of"] #[doc = " an iterator with more than [`usize::MAX`] elements either produces the"] #[doc = " wrong result or panics. If overflow checks are enabled, a panic is"] #[doc = " guaranteed."] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " This function might panic if the iterator has more than [`usize::MAX`]"] #[doc = " elements."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = " assert_eq!(a.iter().count(), 3);"] #[doc = ""] #[doc = " let a = [1, 2, 3, 4, 5];"] #[doc = " assert_eq!(a.iter().count(), 5);"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn count (self) -> usize where Self : Sized , { self . fold (0 , #[rustc_inherit_overflow_checks] | count , _ | count + 1 ,) } #[doc = " Consumes the iterator, returning the last element."] #[doc = ""] #[doc = " This method will evaluate the iterator until it returns [`None`]. While"] #[doc = " doing so, it keeps track of the current element. After [`None`] is"] #[doc = " returned, `last()` will then return the last element it saw."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = " assert_eq!(a.into_iter().last(), Some(3));"] #[doc = ""] #[doc = " let a = [1, 2, 3, 4, 5];"] #[doc = " assert_eq!(a.into_iter().last(), Some(5));"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn last (self) -> Option < Self :: Item > where Self : Sized , { #[inline] fn some < T > (_ : Option < T > , x : T) -> Option < T > { Some (x) } self . fold (None , some) } #[doc = " Advances the iterator by `n` elements."] #[doc = ""] #[doc = " This method will eagerly skip `n` elements by calling [`next`] up to `n`"] #[doc = " times until [`None`] is encountered."] #[doc = ""] #[doc = " `advance_by(n)` will return `Ok(())` if the iterator successfully advances by"] #[doc = " `n` elements, or a `Err(NonZero<usize>)` with value `k` if [`None`] is encountered,"] #[doc = " where `k` is remaining number of steps that could not be advanced because the iterator ran out."] #[doc = " If `self` is empty and `n` is non-zero, then this returns `Err(n)`."] #[doc = " Otherwise, `k` is always less than `n`."] #[doc = ""] #[doc = " Calling `advance_by(0)` can do meaningful work, for example [`Flatten`]"] #[doc = " can advance its outer iterator until it finds an inner iterator that is not empty, which"] #[doc = " then often allows it to return a more accurate `size_hint()` than in its initial state."] #[doc = ""] #[doc = " [`Flatten`]: crate::iter::Flatten"] #[doc = " [`next`]: Iterator::next"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_advance_by)]"] #[doc = ""] #[doc = " use std::num::NonZero;"] #[doc = ""] #[doc = " let a = [1, 2, 3, 4];"] #[doc = " let mut iter = a.into_iter();"] #[doc = ""] #[doc = " assert_eq!(iter.advance_by(2), Ok(()));"] #[doc = " assert_eq!(iter.next(), Some(3));"] #[doc = " assert_eq!(iter.advance_by(0), Ok(()));"] #[doc = " assert_eq!(iter.advance_by(100), Err(NonZero::new(99).unwrap())); // only `4` was skipped"] #[doc = " ```"] #[inline] #[unstable (feature = "iter_advance_by" , reason = "recently added" , issue = "77404")] fn advance_by (& mut self , n : usize) -> Result < () , NonZero < usize > > { #[doc = " Helper trait to specialize `advance_by` via `try_fold` for `Sized` iterators."] trait SpecAdvanceBy { fn spec_advance_by (& mut self , n : usize) -> Result < () , NonZero < usize > > ; } impl < I : Iterator + ? Sized > SpecAdvanceBy for I { default fn spec_advance_by (& mut self , n : usize) -> Result < () , NonZero < usize > > { for i in 0 .. n { if self . next () . is_none () { return Err (unsafe { NonZero :: new_unchecked (n - i) }) ; } } Ok (()) } } impl < I : Iterator > SpecAdvanceBy for I { fn spec_advance_by (& mut self , n : usize) -> Result < () , NonZero < usize > > { let Some (n) = NonZero :: new (n) else { return Ok (()) ; } ; let res = self . try_fold (n , | n , _ | NonZero :: new (n . get () - 1)) ; match res { None => Ok (()) , Some (n) => Err (n) , } } } self . spec_advance_by (n) } #[doc = " Returns the `n`th element of the iterator."] #[doc = ""] #[doc = " Like most indexing operations, the count starts from zero, so `nth(0)`"] #[doc = " returns the first value, `nth(1)` the second, and so on."] #[doc = ""] #[doc = " Note that all preceding elements, as well as the returned element, will be"] #[doc = " consumed from the iterator. That means that the preceding elements will be"] #[doc = " discarded, and also that calling `nth(0)` multiple times on the same iterator"] #[doc = " will return different elements."] #[doc = ""] #[doc = " `nth()` will return [`None`] if `n` is greater than or equal to the length of the"] #[doc = " iterator."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = " assert_eq!(a.into_iter().nth(1), Some(2));"] #[doc = " ```"] #[doc = ""] #[doc = " Calling `nth()` multiple times doesn't rewind the iterator:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let mut iter = a.into_iter();"] #[doc = ""] #[doc = " assert_eq!(iter.nth(1), Some(2));"] #[doc = " assert_eq!(iter.nth(1), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Returning `None` if there are less than `n + 1` elements:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = " assert_eq!(a.into_iter().nth(10), None);"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn nth (& mut self , n : usize) -> Option < Self :: Item > { self . advance_by (n) . ok () ? ; self . next () } #[doc = " Creates an iterator starting at the same point, but stepping by"] #[doc = " the given amount at each iteration."] #[doc = ""] #[doc = " Note 1: The first element of the iterator will always be returned,"] #[doc = " regardless of the step given."] #[doc = ""] #[doc = " Note 2: The time at which ignored elements are pulled is not fixed."] #[doc = " `StepBy` behaves like the sequence `self.next()`, `self.nth(step-1)`,"] #[doc = " `self.nth(step-1)`, …, but is also free to behave like the sequence"] #[doc = " `advance_n_and_return_first(&mut self, step)`,"] #[doc = " `advance_n_and_return_first(&mut self, step)`, …"] #[doc = " Which way is used may change for some iterators for performance reasons."] #[doc = " The second way will advance the iterator earlier and may consume more items."] #[doc = ""] #[doc = " `advance_n_and_return_first` is the equivalent of:"] #[doc = " ```"] #[doc = " fn advance_n_and_return_first<I>(iter: &mut I, n: usize) -> Option<I::Item>"] #[doc = " where"] #[doc = "     I: Iterator,"] #[doc = " {"] #[doc = "     let next = iter.next();"] #[doc = "     if n > 1 {"] #[doc = "         iter.nth(n - 2);"] #[doc = "     }"] #[doc = "     next"] #[doc = " }"] #[doc = " ```"] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " The method will panic if the given step is `0`."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [0, 1, 2, 3, 4, 5];"] #[doc = " let mut iter = a.into_iter().step_by(2);"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(0));"] #[doc = " assert_eq!(iter.next(), Some(2));"] #[doc = " assert_eq!(iter.next(), Some(4));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[inline] #[stable (feature = "iterator_step_by" , since = "1.28.0")] fn step_by (self , step : usize) -> StepBy < Self > where Self : Sized , { StepBy :: new (self , step) } #[doc = " Takes two iterators and creates a new iterator over both in sequence."] #[doc = ""] #[doc = " `chain()` will return a new iterator which will first iterate over"] #[doc = " values from the first iterator and then over values from the second"] #[doc = " iterator."] #[doc = ""] #[doc = " In other words, it links two iterators together, in a chain. 🔗"] #[doc = ""] #[doc = " [`once`] is commonly used to adapt a single value into a chain of"] #[doc = " other kinds of iteration."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let s1 = \"abc\".chars();"] #[doc = " let s2 = \"def\".chars();"] #[doc = ""] #[doc = " let mut iter = s1.chain(s2);"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some('a'));"] #[doc = " assert_eq!(iter.next(), Some('b'));"] #[doc = " assert_eq!(iter.next(), Some('c'));"] #[doc = " assert_eq!(iter.next(), Some('d'));"] #[doc = " assert_eq!(iter.next(), Some('e'));"] #[doc = " assert_eq!(iter.next(), Some('f'));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Since the argument to `chain()` uses [`IntoIterator`], we can pass"] #[doc = " anything that can be converted into an [`Iterator`], not just an"] #[doc = " [`Iterator`] itself. For example, arrays (`[T]`) implement"] #[doc = " [`IntoIterator`], and so can be passed to `chain()` directly:"] #[doc = ""] #[doc = " ```"] #[doc = " let a1 = [1, 2, 3];"] #[doc = " let a2 = [4, 5, 6];"] #[doc = ""] #[doc = " let mut iter = a1.into_iter().chain(a2);"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = " assert_eq!(iter.next(), Some(2));"] #[doc = " assert_eq!(iter.next(), Some(3));"] #[doc = " assert_eq!(iter.next(), Some(4));"] #[doc = " assert_eq!(iter.next(), Some(5));"] #[doc = " assert_eq!(iter.next(), Some(6));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " If you work with Windows API, you may wish to convert [`OsStr`] to `Vec<u16>`:"] #[doc = ""] #[doc = " ```"] #[doc = " #[cfg(windows)]"] #[doc = " fn os_str_to_utf16(s: &std::ffi::OsStr) -> Vec<u16> {"] #[doc = "     use std::os::windows::ffi::OsStrExt;"] #[doc = "     s.encode_wide().chain(std::iter::once(0)).collect()"] #[doc = " }"] #[doc = " ```"] #[doc = ""] #[doc = " [`once`]: crate::iter::once"] #[doc = " [`OsStr`]: ../../std/ffi/struct.OsStr.html"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn chain < U > (self , other : U) -> Chain < Self , U :: IntoIter > where Self : Sized , U : IntoIterator < Item = Self :: Item > , { Chain :: new (self , other . into_iter ()) } #[doc = " 'Zips up' two iterators into a single iterator of pairs."] #[doc = ""] #[doc = " `zip()` returns a new iterator that will iterate over two other"] #[doc = " iterators, returning a tuple where the first element comes from the"] #[doc = " first iterator, and the second element comes from the second iterator."] #[doc = ""] #[doc = " In other words, it zips two iterators together, into a single one."] #[doc = ""] #[doc = " If either iterator returns [`None`], [`next`] from the zipped iterator"] #[doc = " will return [`None`]."] #[doc = " If the zipped iterator has no more elements to return then each further attempt to advance"] #[doc = " it will first try to advance the first iterator at most one time and if it still yielded an item"] #[doc = " try to advance the second iterator at most one time."] #[doc = ""] #[doc = " To 'undo' the result of zipping up two iterators, see [`unzip`]."] #[doc = ""] #[doc = " [`unzip`]: Iterator::unzip"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let s1 = \"abc\".chars();"] #[doc = " let s2 = \"def\".chars();"] #[doc = ""] #[doc = " let mut iter = s1.zip(s2);"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(('a', 'd')));"] #[doc = " assert_eq!(iter.next(), Some(('b', 'e')));"] #[doc = " assert_eq!(iter.next(), Some(('c', 'f')));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Since the argument to `zip()` uses [`IntoIterator`], we can pass"] #[doc = " anything that can be converted into an [`Iterator`], not just an"] #[doc = " [`Iterator`] itself. For example, arrays (`[T]`) implement"] #[doc = " [`IntoIterator`], and so can be passed to `zip()` directly:"] #[doc = ""] #[doc = " ```"] #[doc = " let a1 = [1, 2, 3];"] #[doc = " let a2 = [4, 5, 6];"] #[doc = ""] #[doc = " let mut iter = a1.into_iter().zip(a2);"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some((1, 4)));"] #[doc = " assert_eq!(iter.next(), Some((2, 5)));"] #[doc = " assert_eq!(iter.next(), Some((3, 6)));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " `zip()` is often used to zip an infinite iterator to a finite one."] #[doc = " This works because the finite iterator will eventually return [`None`],"] #[doc = " ending the zipper. Zipping with `(0..)` can look a lot like [`enumerate`]:"] #[doc = ""] #[doc = " ```"] #[doc = " let enumerate: Vec<_> = \"foo\".chars().enumerate().collect();"] #[doc = ""] #[doc = " let zipper: Vec<_> = (0..).zip(\"foo\".chars()).collect();"] #[doc = ""] #[doc = " assert_eq!((0, 'f'), enumerate[0]);"] #[doc = " assert_eq!((0, 'f'), zipper[0]);"] #[doc = ""] #[doc = " assert_eq!((1, 'o'), enumerate[1]);"] #[doc = " assert_eq!((1, 'o'), zipper[1]);"] #[doc = ""] #[doc = " assert_eq!((2, 'o'), enumerate[2]);"] #[doc = " assert_eq!((2, 'o'), zipper[2]);"] #[doc = " ```"] #[doc = ""] #[doc = " If both iterators have roughly equivalent syntax, it may be more readable to use [`zip`]:"] #[doc = ""] #[doc = " ```"] #[doc = " use std::iter::zip;"] #[doc = ""] #[doc = " let a = [1, 2, 3];"] #[doc = " let b = [2, 3, 4];"] #[doc = ""] #[doc = " let mut zipped = zip("] #[doc = "     a.into_iter().map(|x| x * 2).skip(1),"] #[doc = "     b.into_iter().map(|x| x * 2).skip(1),"] #[doc = " );"] #[doc = ""] #[doc = " assert_eq!(zipped.next(), Some((4, 6)));"] #[doc = " assert_eq!(zipped.next(), Some((6, 8)));"] #[doc = " assert_eq!(zipped.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " compared to:"] #[doc = ""] #[doc = " ```"] #[doc = " # let a = [1, 2, 3];"] #[doc = " # let b = [2, 3, 4];"] #[doc = " #"] #[doc = " let mut zipped = a"] #[doc = "     .into_iter()"] #[doc = "     .map(|x| x * 2)"] #[doc = "     .skip(1)"] #[doc = "     .zip(b.into_iter().map(|x| x * 2).skip(1));"] #[doc = " #"] #[doc = " # assert_eq!(zipped.next(), Some((4, 6)));"] #[doc = " # assert_eq!(zipped.next(), Some((6, 8)));"] #[doc = " # assert_eq!(zipped.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " [`enumerate`]: Iterator::enumerate"] #[doc = " [`next`]: Iterator::next"] #[doc = " [`zip`]: crate::iter::zip"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn zip < U > (self , other : U) -> Zip < Self , U :: IntoIter > where Self : Sized , U : IntoIterator , { Zip :: new (self , other . into_iter ()) } #[doc = " Creates a new iterator which places a copy of `separator` between adjacent"] #[doc = " items of the original iterator."] #[doc = ""] #[doc = " In case `separator` does not implement [`Clone`] or needs to be"] #[doc = " computed every time, use [`intersperse_with`]."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_intersperse)]"] #[doc = ""] #[doc = " let mut a = [0, 1, 2].into_iter().intersperse(100);"] #[doc = " assert_eq!(a.next(), Some(0));   // The first element from `a`."] #[doc = " assert_eq!(a.next(), Some(100)); // The separator."] #[doc = " assert_eq!(a.next(), Some(1));   // The next element from `a`."] #[doc = " assert_eq!(a.next(), Some(100)); // The separator."] #[doc = " assert_eq!(a.next(), Some(2));   // The last element from `a`."] #[doc = " assert_eq!(a.next(), None);       // The iterator is finished."] #[doc = " ```"] #[doc = ""] #[doc = " `intersperse` can be very useful to join an iterator's items using a common element:"] #[doc = " ```"] #[doc = " #![feature(iter_intersperse)]"] #[doc = ""] #[doc = " let words = [\"Hello\", \"World\", \"!\"];"] #[doc = " let hello: String = words.into_iter().intersperse(\" \").collect();"] #[doc = " assert_eq!(hello, \"Hello World !\");"] #[doc = " ```"] #[doc = ""] #[doc = " [`Clone`]: crate::clone::Clone"] #[doc = " [`intersperse_with`]: Iterator::intersperse_with"] #[inline] #[unstable (feature = "iter_intersperse" , reason = "recently added" , issue = "79524")] fn intersperse (self , separator : Self :: Item) -> Intersperse < Self > where Self : Sized , Self :: Item : Clone , { Intersperse :: new (self , separator) } #[doc = " Creates a new iterator which places an item generated by `separator`"] #[doc = " between adjacent items of the original iterator."] #[doc = ""] #[doc = " The closure will be called exactly once each time an item is placed"] #[doc = " between two adjacent items from the underlying iterator; specifically,"] #[doc = " the closure is not called if the underlying iterator yields less than"] #[doc = " two items and after the last item is yielded."] #[doc = ""] #[doc = " If the iterator's item implements [`Clone`], it may be easier to use"] #[doc = " [`intersperse`]."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_intersperse)]"] #[doc = ""] #[doc = " #[derive(PartialEq, Debug)]"] #[doc = " struct NotClone(usize);"] #[doc = ""] #[doc = " let v = [NotClone(0), NotClone(1), NotClone(2)];"] #[doc = " let mut it = v.into_iter().intersperse_with(|| NotClone(99));"] #[doc = ""] #[doc = " assert_eq!(it.next(), Some(NotClone(0)));  // The first element from `v`."] #[doc = " assert_eq!(it.next(), Some(NotClone(99))); // The separator."] #[doc = " assert_eq!(it.next(), Some(NotClone(1)));  // The next element from `v`."] #[doc = " assert_eq!(it.next(), Some(NotClone(99))); // The separator."] #[doc = " assert_eq!(it.next(), Some(NotClone(2)));  // The last element from `v`."] #[doc = " assert_eq!(it.next(), None);               // The iterator is finished."] #[doc = " ```"] #[doc = ""] #[doc = " `intersperse_with` can be used in situations where the separator needs"] #[doc = " to be computed:"] #[doc = " ```"] #[doc = " #![feature(iter_intersperse)]"] #[doc = ""] #[doc = " let src = [\"Hello\", \"to\", \"all\", \"people\", \"!!\"].iter().copied();"] #[doc = ""] #[doc = " // The closure mutably borrows its context to generate an item."] #[doc = " let mut happy_emojis = [\" ❤\u{fe0f} \", \" 😀 \"].into_iter();"] #[doc = " let separator = || happy_emojis.next().unwrap_or(\" 🦀 \");"] #[doc = ""] #[doc = " let result = src.intersperse_with(separator).collect::<String>();"] #[doc = " assert_eq!(result, \"Hello ❤\u{fe0f} to 😀 all 🦀 people 🦀 !!\");"] #[doc = " ```"] #[doc = " [`Clone`]: crate::clone::Clone"] #[doc = " [`intersperse`]: Iterator::intersperse"] #[inline] #[unstable (feature = "iter_intersperse" , reason = "recently added" , issue = "79524")] fn intersperse_with < G > (self , separator : G) -> IntersperseWith < Self , G > where Self : Sized , G : FnMut () -> Self :: Item , { IntersperseWith :: new (self , separator) } #[doc = " Takes a closure and creates an iterator which calls that closure on each"] #[doc = " element."] #[doc = ""] #[doc = " `map()` transforms one iterator into another, by means of its argument:"] #[doc = " something that implements [`FnMut`]. It produces a new iterator which"] #[doc = " calls this closure on each element of the original iterator."] #[doc = ""] #[doc = " If you are good at thinking in types, you can think of `map()` like this:"] #[doc = " If you have an iterator that gives you elements of some type `A`, and"] #[doc = " you want an iterator of some other type `B`, you can use `map()`,"] #[doc = " passing a closure that takes an `A` and returns a `B`."] #[doc = ""] #[doc = " `map()` is conceptually similar to a [`for`] loop. However, as `map()` is"] #[doc = " lazy, it is best used when you're already working with other iterators."] #[doc = " If you're doing some sort of looping for a side effect, it's considered"] #[doc = " more idiomatic to use [`for`] than `map()`."] #[doc = ""] #[doc = " [`for`]: ../../book/ch03-05-control-flow.html#looping-through-a-collection-with-for"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let mut iter = a.iter().map(|x| 2 * x);"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(2));"] #[doc = " assert_eq!(iter.next(), Some(4));"] #[doc = " assert_eq!(iter.next(), Some(6));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " If you're doing some sort of side effect, prefer [`for`] to `map()`:"] #[doc = ""] #[doc = " ```"] #[doc = " # #![allow(unused_must_use)]"] #[doc = " // don't do this:"] #[doc = " (0..5).map(|x| println!(\"{x}\"));"] #[doc = ""] #[doc = " // it won't even execute, as it is lazy. Rust will warn you about this."] #[doc = ""] #[doc = " // Instead, use a for-loop:"] #[doc = " for x in 0..5 {"] #[doc = "     println!(\"{x}\");"] #[doc = " }"] #[doc = " ```"] #[rustc_diagnostic_item = "IteratorMap"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn map < B , F > (self , f : F) -> Map < Self , F > where Self : Sized , F : FnMut (Self :: Item) -> B , { Map :: new (self , f) } #[doc = " Calls a closure on each element of an iterator."] #[doc = ""] #[doc = " This is equivalent to using a [`for`] loop on the iterator, although"] #[doc = " `break` and `continue` are not possible from a closure. It's generally"] #[doc = " more idiomatic to use a `for` loop, but `for_each` may be more legible"] #[doc = " when processing items at the end of longer iterator chains. In some"] #[doc = " cases `for_each` may also be faster than a loop, because it will use"] #[doc = " internal iteration on adapters like `Chain`."] #[doc = ""] #[doc = " [`for`]: ../../book/ch03-05-control-flow.html#looping-through-a-collection-with-for"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " use std::sync::mpsc::channel;"] #[doc = ""] #[doc = " let (tx, rx) = channel();"] #[doc = " (0..5).map(|x| x * 2 + 1)"] #[doc = "       .for_each(move |x| tx.send(x).unwrap());"] #[doc = ""] #[doc = " let v: Vec<_> = rx.iter().collect();"] #[doc = " assert_eq!(v, vec![1, 3, 5, 7, 9]);"] #[doc = " ```"] #[doc = ""] #[doc = " For such a small example, a `for` loop may be cleaner, but `for_each`"] #[doc = " might be preferable to keep a functional style with longer iterators:"] #[doc = ""] #[doc = " ```"] #[doc = " (0..5).flat_map(|x| (x * 100)..(x * 110))"] #[doc = "       .enumerate()"] #[doc = "       .filter(|&(i, x)| (i + x) % 3 == 0)"] #[doc = "       .for_each(|(i, x)| println!(\"{i}:{x}\"));"] #[doc = " ```"] #[inline] #[stable (feature = "iterator_for_each" , since = "1.21.0")] fn for_each < F > (self , f : F) where Self : Sized , F : FnMut (Self :: Item) , { #[inline] fn call < T > (mut f : impl FnMut (T)) -> impl FnMut (() , T) { move | () , item | f (item) } self . fold (() , call (f)) ; } #[doc = " Creates an iterator which uses a closure to determine if an element"] #[doc = " should be yielded."] #[doc = ""] #[doc = " Given an element the closure must return `true` or `false`. The returned"] #[doc = " iterator will yield only the elements for which the closure returns"] #[doc = " `true`."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [0i32, 1, 2];"] #[doc = ""] #[doc = " let mut iter = a.into_iter().filter(|x| x.is_positive());"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = " assert_eq!(iter.next(), Some(2));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Because the closure passed to `filter()` takes a reference, and many"] #[doc = " iterators iterate over references, this leads to a possibly confusing"] #[doc = " situation, where the type of the closure is a double reference:"] #[doc = ""] #[doc = " ```"] #[doc = " let s = &[0, 1, 2];"] #[doc = ""] #[doc = " let mut iter = s.iter().filter(|x| **x > 1); // needs two *s!"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(&2));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " It's common to instead use destructuring on the argument to strip away one:"] #[doc = ""] #[doc = " ```"] #[doc = " let s = &[0, 1, 2];"] #[doc = ""] #[doc = " let mut iter = s.iter().filter(|&x| *x > 1); // both & and *"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(&2));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " or both:"] #[doc = ""] #[doc = " ```"] #[doc = " let s = &[0, 1, 2];"] #[doc = ""] #[doc = " let mut iter = s.iter().filter(|&&x| x > 1); // two &s"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(&2));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " of these layers."] #[doc = ""] #[doc = " Note that `iter.filter(f).next()` is equivalent to `iter.find(f)`."] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] #[rustc_diagnostic_item = "iter_filter"] fn filter < P > (self , predicate : P) -> Filter < Self , P > where Self : Sized , P : FnMut (& Self :: Item) -> bool , { Filter :: new (self , predicate) } #[doc = " Creates an iterator that both filters and maps."] #[doc = ""] #[doc = " The returned iterator yields only the `value`s for which the supplied"] #[doc = " closure returns `Some(value)`."] #[doc = ""] #[doc = " `filter_map` can be used to make chains of [`filter`] and [`map`] more"] #[doc = " concise. The example below shows how a `map().filter().map()` can be"] #[doc = " shortened to a single call to `filter_map`."] #[doc = ""] #[doc = " [`filter`]: Iterator::filter"] #[doc = " [`map`]: Iterator::map"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [\"1\", \"two\", \"NaN\", \"four\", \"5\"];"] #[doc = ""] #[doc = " let mut iter = a.iter().filter_map(|s| s.parse().ok());"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = " assert_eq!(iter.next(), Some(5));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Here's the same example, but with [`filter`] and [`map`]:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [\"1\", \"two\", \"NaN\", \"four\", \"5\"];"] #[doc = " let mut iter = a.iter().map(|s| s.parse()).filter(|s| s.is_ok()).map(|s| s.unwrap());"] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = " assert_eq!(iter.next(), Some(5));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn filter_map < B , F > (self , f : F) -> FilterMap < Self , F > where Self : Sized , F : FnMut (Self :: Item) -> Option < B > , { FilterMap :: new (self , f) } #[doc = " Creates an iterator which gives the current iteration count as well as"] #[doc = " the next value."] #[doc = ""] #[doc = " The iterator returned yields pairs `(i, val)`, where `i` is the"] #[doc = " current index of iteration and `val` is the value returned by the"] #[doc = " iterator."] #[doc = ""] #[doc = " `enumerate()` keeps its count as a [`usize`]. If you want to count by a"] #[doc = " different sized integer, the [`zip`] function provides similar"] #[doc = " functionality."] #[doc = ""] #[doc = " # Overflow Behavior"] #[doc = ""] #[doc = " The method does no guarding against overflows, so enumerating more than"] #[doc = " [`usize::MAX`] elements either produces the wrong result or panics. If"] #[doc = " overflow checks are enabled, a panic is guaranteed."] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " The returned iterator might panic if the to-be-returned index would"] #[doc = " overflow a [`usize`]."] #[doc = ""] #[doc = " [`zip`]: Iterator::zip"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = ['a', 'b', 'c'];"] #[doc = ""] #[doc = " let mut iter = a.into_iter().enumerate();"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some((0, 'a')));"] #[doc = " assert_eq!(iter.next(), Some((1, 'b')));"] #[doc = " assert_eq!(iter.next(), Some((2, 'c')));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] #[rustc_diagnostic_item = "enumerate_method"] fn enumerate (self) -> Enumerate < Self > where Self : Sized , { Enumerate :: new (self) } #[doc = " Creates an iterator which can use the [`peek`] and [`peek_mut`] methods"] #[doc = " to look at the next element of the iterator without consuming it. See"] #[doc = " their documentation for more information."] #[doc = ""] #[doc = " Note that the underlying iterator is still advanced when [`peek`] or"] #[doc = " [`peek_mut`] are called for the first time: In order to retrieve the"] #[doc = " next element, [`next`] is called on the underlying iterator, hence any"] #[doc = " side effects (i.e. anything other than fetching the next value) of"] #[doc = " the [`next`] method will occur."] #[doc = ""] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let xs = [1, 2, 3];"] #[doc = ""] #[doc = " let mut iter = xs.into_iter().peekable();"] #[doc = ""] #[doc = " // peek() lets us see into the future"] #[doc = " assert_eq!(iter.peek(), Some(&1));"] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(2));"] #[doc = ""] #[doc = " // we can peek() multiple times, the iterator won't advance"] #[doc = " assert_eq!(iter.peek(), Some(&3));"] #[doc = " assert_eq!(iter.peek(), Some(&3));"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(3));"] #[doc = ""] #[doc = " // after the iterator is finished, so is peek()"] #[doc = " assert_eq!(iter.peek(), None);"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Using [`peek_mut`] to mutate the next item without advancing the"] #[doc = " iterator:"] #[doc = ""] #[doc = " ```"] #[doc = " let xs = [1, 2, 3];"] #[doc = ""] #[doc = " let mut iter = xs.into_iter().peekable();"] #[doc = ""] #[doc = " // `peek_mut()` lets us see into the future"] #[doc = " assert_eq!(iter.peek_mut(), Some(&mut 1));"] #[doc = " assert_eq!(iter.peek_mut(), Some(&mut 1));"] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = ""] #[doc = " if let Some(p) = iter.peek_mut() {"] #[doc = "     assert_eq!(*p, 2);"] #[doc = "     // put a value into the iterator"] #[doc = "     *p = 1000;"] #[doc = " }"] #[doc = ""] #[doc = " // The value reappears as the iterator continues"] #[doc = " assert_eq!(iter.collect::<Vec<_>>(), vec![1000, 3]);"] #[doc = " ```"] #[doc = " [`peek`]: Peekable::peek"] #[doc = " [`peek_mut`]: Peekable::peek_mut"] #[doc = " [`next`]: Iterator::next"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn peekable (self) -> Peekable < Self > where Self : Sized , { Peekable :: new (self) } #[doc = " Creates an iterator that [`skip`]s elements based on a predicate."] #[doc = ""] #[doc = " [`skip`]: Iterator::skip"] #[doc = ""] #[doc = " `skip_while()` takes a closure as an argument. It will call this"] #[doc = " closure on each element of the iterator, and ignore elements"] #[doc = " until it returns `false`."] #[doc = ""] #[doc = " After `false` is returned, `skip_while()`'s job is over, and the"] #[doc = " rest of the elements are yielded."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [-1i32, 0, 1];"] #[doc = ""] #[doc = " let mut iter = a.into_iter().skip_while(|x| x.is_negative());"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(0));"] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Because the closure passed to `skip_while()` takes a reference, and many"] #[doc = " iterators iterate over references, this leads to a possibly confusing"] #[doc = " situation, where the type of the closure argument is a double reference:"] #[doc = ""] #[doc = " ```"] #[doc = " let s = &[-1, 0, 1];"] #[doc = ""] #[doc = " let mut iter = s.iter().skip_while(|x| **x < 0); // need two *s!"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(&0));"] #[doc = " assert_eq!(iter.next(), Some(&1));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Stopping after an initial `false`:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [-1, 0, 1, -2];"] #[doc = ""] #[doc = " let mut iter = a.into_iter().skip_while(|&x| x < 0);"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(0));"] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = ""] #[doc = " // while this would have been false, since we already got a false,"] #[doc = " // skip_while() isn't used any more"] #[doc = " assert_eq!(iter.next(), Some(-2));"] #[doc = ""] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[inline] #[doc (alias = "drop_while")] #[stable (feature = "rust1" , since = "1.0.0")] fn skip_while < P > (self , predicate : P) -> SkipWhile < Self , P > where Self : Sized , P : FnMut (& Self :: Item) -> bool , { SkipWhile :: new (self , predicate) } #[doc = " Creates an iterator that yields elements based on a predicate."] #[doc = ""] #[doc = " `take_while()` takes a closure as an argument. It will call this"] #[doc = " closure on each element of the iterator, and yield elements"] #[doc = " while it returns `true`."] #[doc = ""] #[doc = " After `false` is returned, `take_while()`'s job is over, and the"] #[doc = " rest of the elements are ignored."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [-1i32, 0, 1];"] #[doc = ""] #[doc = " let mut iter = a.into_iter().take_while(|x| x.is_negative());"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(-1));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Because the closure passed to `take_while()` takes a reference, and many"] #[doc = " iterators iterate over references, this leads to a possibly confusing"] #[doc = " situation, where the type of the closure is a double reference:"] #[doc = ""] #[doc = " ```"] #[doc = " let s = &[-1, 0, 1];"] #[doc = ""] #[doc = " let mut iter = s.iter().take_while(|x| **x < 0); // need two *s!"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(&-1));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Stopping after an initial `false`:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [-1, 0, 1, -2];"] #[doc = ""] #[doc = " let mut iter = a.into_iter().take_while(|&x| x < 0);"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(-1));"] #[doc = ""] #[doc = " // We have more elements that are less than zero, but since we already"] #[doc = " // got a false, take_while() ignores the remaining elements."] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Because `take_while()` needs to look at the value in order to see if it"] #[doc = " should be included or not, consuming iterators will see that it is"] #[doc = " removed:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3, 4];"] #[doc = " let mut iter = a.into_iter();"] #[doc = ""] #[doc = " let result: Vec<i32> = iter.by_ref().take_while(|&n| n != 3).collect();"] #[doc = ""] #[doc = " assert_eq!(result, [1, 2]);"] #[doc = ""] #[doc = " let result: Vec<i32> = iter.collect();"] #[doc = ""] #[doc = " assert_eq!(result, [4]);"] #[doc = " ```"] #[doc = ""] #[doc = " The `3` is no longer there, because it was consumed in order to see if"] #[doc = " the iteration should stop, but wasn't placed back into the iterator."] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn take_while < P > (self , predicate : P) -> TakeWhile < Self , P > where Self : Sized , P : FnMut (& Self :: Item) -> bool , { TakeWhile :: new (self , predicate) } #[doc = " Creates an iterator that both yields elements based on a predicate and maps."] #[doc = ""] #[doc = " `map_while()` takes a closure as an argument. It will call this"] #[doc = " closure on each element of the iterator, and yield elements"] #[doc = " while it returns [`Some(_)`][`Some`]."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [-1i32, 4, 0, 1];"] #[doc = ""] #[doc = " let mut iter = a.into_iter().map_while(|x| 16i32.checked_div(x));"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(-16));"] #[doc = " assert_eq!(iter.next(), Some(4));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Here's the same example, but with [`take_while`] and [`map`]:"] #[doc = ""] #[doc = " [`take_while`]: Iterator::take_while"] #[doc = " [`map`]: Iterator::map"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [-1i32, 4, 0, 1];"] #[doc = ""] #[doc = " let mut iter = a.into_iter()"] #[doc = "                 .map(|x| 16i32.checked_div(x))"] #[doc = "                 .take_while(|x| x.is_some())"] #[doc = "                 .map(|x| x.unwrap());"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(-16));"] #[doc = " assert_eq!(iter.next(), Some(4));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Stopping after an initial [`None`]:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [0, 1, 2, -3, 4, 5, -6];"] #[doc = ""] #[doc = " let iter = a.into_iter().map_while(|x| u32::try_from(x).ok());"] #[doc = " let vec: Vec<_> = iter.collect();"] #[doc = ""] #[doc = " // We have more elements that could fit in u32 (such as 4, 5), but `map_while` returned `None` for `-3`"] #[doc = " // (as the `predicate` returned `None`) and `collect` stops at the first `None` encountered."] #[doc = " assert_eq!(vec, [0, 1, 2]);"] #[doc = " ```"] #[doc = ""] #[doc = " Because `map_while()` needs to look at the value in order to see if it"] #[doc = " should be included or not, consuming iterators will see that it is"] #[doc = " removed:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, -3, 4];"] #[doc = " let mut iter = a.into_iter();"] #[doc = ""] #[doc = " let result: Vec<u32> = iter.by_ref()"] #[doc = "                            .map_while(|n| u32::try_from(n).ok())"] #[doc = "                            .collect();"] #[doc = ""] #[doc = " assert_eq!(result, [1, 2]);"] #[doc = ""] #[doc = " let result: Vec<i32> = iter.collect();"] #[doc = ""] #[doc = " assert_eq!(result, [4]);"] #[doc = " ```"] #[doc = ""] #[doc = " The `-3` is no longer there, because it was consumed in order to see if"] #[doc = " the iteration should stop, but wasn't placed back into the iterator."] #[doc = ""] #[doc = " Note that unlike [`take_while`] this iterator is **not** fused."] #[doc = " It is also not specified what this iterator returns after the first [`None`] is returned."] #[doc = " If you need a fused iterator, use [`fuse`]."] #[doc = ""] #[doc = " [`fuse`]: Iterator::fuse"] #[inline] #[stable (feature = "iter_map_while" , since = "1.57.0")] fn map_while < B , P > (self , predicate : P) -> MapWhile < Self , P > where Self : Sized , P : FnMut (Self :: Item) -> Option < B > , { MapWhile :: new (self , predicate) } #[doc = " Creates an iterator that skips the first `n` elements."] #[doc = ""] #[doc = " `skip(n)` skips elements until `n` elements are skipped or the end of the"] #[doc = " iterator is reached (whichever happens first). After that, all the remaining"] #[doc = " elements are yielded. In particular, if the original iterator is too short,"] #[doc = " then the returned iterator is empty."] #[doc = ""] #[doc = " Rather than overriding this method directly, instead override the `nth` method."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let mut iter = a.into_iter().skip(2);"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(3));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn skip (self , n : usize) -> Skip < Self > where Self : Sized , { Skip :: new (self , n) } #[doc = " Creates an iterator that yields the first `n` elements, or fewer"] #[doc = " if the underlying iterator ends sooner."] #[doc = ""] #[doc = " `take(n)` yields elements until `n` elements are yielded or the end of"] #[doc = " the iterator is reached (whichever happens first)."] #[doc = " The returned iterator is a prefix of length `n` if the original iterator"] #[doc = " contains at least `n` elements, otherwise it contains all of the"] #[doc = " (fewer than `n`) elements of the original iterator."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let mut iter = a.into_iter().take(2);"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = " assert_eq!(iter.next(), Some(2));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " `take()` is often used with an infinite iterator, to make it finite:"] #[doc = ""] #[doc = " ```"] #[doc = " let mut iter = (0..).take(3);"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(0));"] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = " assert_eq!(iter.next(), Some(2));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " If less than `n` elements are available,"] #[doc = " `take` will limit itself to the size of the underlying iterator:"] #[doc = ""] #[doc = " ```"] #[doc = " let v = [1, 2];"] #[doc = " let mut iter = v.into_iter().take(5);"] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = " assert_eq!(iter.next(), Some(2));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Use [`by_ref`] to take from the iterator without consuming it, and then"] #[doc = " continue using the original iterator:"] #[doc = ""] #[doc = " ```"] #[doc = " let mut words = [\"hello\", \"world\", \"of\", \"Rust\"].into_iter();"] #[doc = ""] #[doc = " // Take the first two words."] #[doc = " let hello_world: Vec<_> = words.by_ref().take(2).collect();"] #[doc = " assert_eq!(hello_world, vec![\"hello\", \"world\"]);"] #[doc = ""] #[doc = " // Collect the rest of the words."] #[doc = " // We can only do this because we used `by_ref` earlier."] #[doc = " let of_rust: Vec<_> = words.collect();"] #[doc = " assert_eq!(of_rust, vec![\"of\", \"Rust\"]);"] #[doc = " ```"] #[doc = ""] #[doc = " [`by_ref`]: Iterator::by_ref"] #[doc (alias = "limit")] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn take (self , n : usize) -> Take < Self > where Self : Sized , { Take :: new (self , n) } #[doc = " An iterator adapter which, like [`fold`], holds internal state, but"] #[doc = " unlike [`fold`], produces a new iterator."] #[doc = ""] #[doc = " [`fold`]: Iterator::fold"] #[doc = ""] #[doc = " `scan()` takes two arguments: an initial value which seeds the internal"] #[doc = " state, and a closure with two arguments, the first being a mutable"] #[doc = " reference to the internal state and the second an iterator element."] #[doc = " The closure can assign to the internal state to share state between"] #[doc = " iterations."] #[doc = ""] #[doc = " On iteration, the closure will be applied to each element of the"] #[doc = " iterator and the return value from the closure, an [`Option`], is"] #[doc = " returned by the `next` method. Thus the closure can return"] #[doc = " `Some(value)` to yield `value`, or `None` to end the iteration."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3, 4];"] #[doc = ""] #[doc = " let mut iter = a.into_iter().scan(1, |state, x| {"] #[doc = "     // each iteration, we'll multiply the state by the element ..."] #[doc = "     *state = *state * x;"] #[doc = ""] #[doc = "     // ... and terminate if the state exceeds 6"] #[doc = "     if *state > 6 {"] #[doc = "         return None;"] #[doc = "     }"] #[doc = "     // ... else yield the negation of the state"] #[doc = "     Some(-*state)"] #[doc = " });"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(-1));"] #[doc = " assert_eq!(iter.next(), Some(-2));"] #[doc = " assert_eq!(iter.next(), Some(-6));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn scan < St , B , F > (self , initial_state : St , f : F) -> Scan < Self , St , F > where Self : Sized , F : FnMut (& mut St , Self :: Item) -> Option < B > , { Scan :: new (self , initial_state , f) } #[doc = " Creates an iterator that works like map, but flattens nested structure."] #[doc = ""] #[doc = " The [`map`] adapter is very useful, but only when the closure"] #[doc = " argument produces values. If it produces an iterator instead, there's"] #[doc = " an extra layer of indirection. `flat_map()` will remove this extra layer"] #[doc = " on its own."] #[doc = ""] #[doc = " You can think of `flat_map(f)` as the semantic equivalent"] #[doc = " of [`map`]ping, and then [`flatten`]ing as in `map(f).flatten()`."] #[doc = ""] #[doc = " Another way of thinking about `flat_map()`: [`map`]'s closure returns"] #[doc = " one item for each element, and `flat_map()`'s closure returns an"] #[doc = " iterator for each element."] #[doc = ""] #[doc = " [`map`]: Iterator::map"] #[doc = " [`flatten`]: Iterator::flatten"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let words = [\"alpha\", \"beta\", \"gamma\"];"] #[doc = ""] #[doc = " // chars() returns an iterator"] #[doc = " let merged: String = words.iter()"] #[doc = "                           .flat_map(|s| s.chars())"] #[doc = "                           .collect();"] #[doc = " assert_eq!(merged, \"alphabetagamma\");"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn flat_map < U , F > (self , f : F) -> FlatMap < Self , U , F > where Self : Sized , U : IntoIterator , F : FnMut (Self :: Item) -> U , { FlatMap :: new (self , f) } #[doc = " Creates an iterator that flattens nested structure."] #[doc = ""] #[doc = " This is useful when you have an iterator of iterators or an iterator of"] #[doc = " things that can be turned into iterators and you want to remove one"] #[doc = " level of indirection."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let data = vec![vec![1, 2, 3, 4], vec![5, 6]];"] #[doc = " let flattened: Vec<_> = data.into_iter().flatten().collect();"] #[doc = " assert_eq!(flattened, [1, 2, 3, 4, 5, 6]);"] #[doc = " ```"] #[doc = ""] #[doc = " Mapping and then flattening:"] #[doc = ""] #[doc = " ```"] #[doc = " let words = [\"alpha\", \"beta\", \"gamma\"];"] #[doc = ""] #[doc = " // chars() returns an iterator"] #[doc = " let merged: String = words.iter()"] #[doc = "                           .map(|s| s.chars())"] #[doc = "                           .flatten()"] #[doc = "                           .collect();"] #[doc = " assert_eq!(merged, \"alphabetagamma\");"] #[doc = " ```"] #[doc = ""] #[doc = " You can also rewrite this in terms of [`flat_map()`], which is preferable"] #[doc = " in this case since it conveys intent more clearly:"] #[doc = ""] #[doc = " ```"] #[doc = " let words = [\"alpha\", \"beta\", \"gamma\"];"] #[doc = ""] #[doc = " // chars() returns an iterator"] #[doc = " let merged: String = words.iter()"] #[doc = "                           .flat_map(|s| s.chars())"] #[doc = "                           .collect();"] #[doc = " assert_eq!(merged, \"alphabetagamma\");"] #[doc = " ```"] #[doc = ""] #[doc = " Flattening works on any `IntoIterator` type, including `Option` and `Result`:"] #[doc = ""] #[doc = " ```"] #[doc = " let options = vec![Some(123), Some(321), None, Some(231)];"] #[doc = " let flattened_options: Vec<_> = options.into_iter().flatten().collect();"] #[doc = " assert_eq!(flattened_options, [123, 321, 231]);"] #[doc = ""] #[doc = " let results = vec![Ok(123), Ok(321), Err(456), Ok(231)];"] #[doc = " let flattened_results: Vec<_> = results.into_iter().flatten().collect();"] #[doc = " assert_eq!(flattened_results, [123, 321, 231]);"] #[doc = " ```"] #[doc = ""] #[doc = " Flattening only removes one level of nesting at a time:"] #[doc = ""] #[doc = " ```"] #[doc = " let d3 = [[[1, 2], [3, 4]], [[5, 6], [7, 8]]];"] #[doc = ""] #[doc = " let d2: Vec<_> = d3.into_iter().flatten().collect();"] #[doc = " assert_eq!(d2, [[1, 2], [3, 4], [5, 6], [7, 8]]);"] #[doc = ""] #[doc = " let d1: Vec<_> = d3.into_iter().flatten().flatten().collect();"] #[doc = " assert_eq!(d1, [1, 2, 3, 4, 5, 6, 7, 8]);"] #[doc = " ```"] #[doc = ""] #[doc = " Here we see that `flatten()` does not perform a \"deep\" flatten."] #[doc = " Instead, only one level of nesting is removed. That is, if you"] #[doc = " `flatten()` a three-dimensional array, the result will be"] #[doc = " two-dimensional and not one-dimensional. To get a one-dimensional"] #[doc = " structure, you have to `flatten()` again."] #[doc = ""] #[doc = " [`flat_map()`]: Iterator::flat_map"] #[inline] #[stable (feature = "iterator_flatten" , since = "1.29.0")] fn flatten (self) -> Flatten < Self > where Self : Sized , Self :: Item : IntoIterator , { Flatten :: new (self) } #[doc = " Calls the given function `f` for each contiguous window of size `N` over"] #[doc = " `self` and returns an iterator over the outputs of `f`. Like [`slice::windows()`],"] #[doc = " the windows during mapping overlap as well."] #[doc = ""] #[doc = " In the following example, the closure is called three times with the"] #[doc = " arguments `&['a', 'b']`, `&['b', 'c']` and `&['c', 'd']` respectively."] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_map_windows)]"] #[doc = ""] #[doc = " let strings = \"abcd\".chars()"] #[doc = "     .map_windows(|[x, y]| format!(\"{}+{}\", x, y))"] #[doc = "     .collect::<Vec<String>>();"] #[doc = ""] #[doc = " assert_eq!(strings, vec![\"a+b\", \"b+c\", \"c+d\"]);"] #[doc = " ```"] #[doc = ""] #[doc = " Note that the const parameter `N` is usually inferred by the"] #[doc = " destructured argument in the closure."] #[doc = ""] #[doc = " The returned iterator yields 𝑘 − `N` + 1 items (where 𝑘 is the number of"] #[doc = " items yielded by `self`). If 𝑘 is less than `N`, this method yields an"] #[doc = " empty iterator."] #[doc = ""] #[doc = " The returned iterator implements [`FusedIterator`], because once `self`"] #[doc = " returns `None`, even if it returns a `Some(T)` again in the next iterations,"] #[doc = " we cannot put it into a contiguous array buffer, and thus the returned iterator"] #[doc = " should be fused."] #[doc = ""] #[doc = " [`slice::windows()`]: slice::windows"] #[doc = " [`FusedIterator`]: crate::iter::FusedIterator"] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " Panics if `N` is zero. This check will most probably get changed to a"] #[doc = " compile time error before this method gets stabilized."] #[doc = ""] #[doc = " ```should_panic"] #[doc = " #![feature(iter_map_windows)]"] #[doc = ""] #[doc = " let iter = std::iter::repeat(0).map_windows(|&[]| ());"] #[doc = " ```"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Building the sums of neighboring numbers."] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_map_windows)]"] #[doc = ""] #[doc = " let mut it = [1, 3, 8, 1].iter().map_windows(|&[a, b]| a + b);"] #[doc = " assert_eq!(it.next(), Some(4));  // 1 + 3"] #[doc = " assert_eq!(it.next(), Some(11)); // 3 + 8"] #[doc = " assert_eq!(it.next(), Some(9));  // 8 + 1"] #[doc = " assert_eq!(it.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Since the elements in the following example implement `Copy`, we can"] #[doc = " just copy the array and get an iterator over the windows."] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_map_windows)]"] #[doc = ""] #[doc = " let mut it = \"ferris\".chars().map_windows(|w: &[_; 3]| *w);"] #[doc = " assert_eq!(it.next(), Some(['f', 'e', 'r']));"] #[doc = " assert_eq!(it.next(), Some(['e', 'r', 'r']));"] #[doc = " assert_eq!(it.next(), Some(['r', 'r', 'i']));"] #[doc = " assert_eq!(it.next(), Some(['r', 'i', 's']));"] #[doc = " assert_eq!(it.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " You can also use this function to check the sortedness of an iterator."] #[doc = " For the simple case, rather use [`Iterator::is_sorted`]."] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_map_windows)]"] #[doc = ""] #[doc = " let mut it = [0.5, 1.0, 3.5, 3.0, 8.5, 8.5, f32::NAN].iter()"] #[doc = "     .map_windows(|[a, b]| a <= b);"] #[doc = ""] #[doc = " assert_eq!(it.next(), Some(true));  // 0.5 <= 1.0"] #[doc = " assert_eq!(it.next(), Some(true));  // 1.0 <= 3.5"] #[doc = " assert_eq!(it.next(), Some(false)); // 3.5 <= 3.0"] #[doc = " assert_eq!(it.next(), Some(true));  // 3.0 <= 8.5"] #[doc = " assert_eq!(it.next(), Some(true));  // 8.5 <= 8.5"] #[doc = " assert_eq!(it.next(), Some(false)); // 8.5 <= NAN"] #[doc = " assert_eq!(it.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " For non-fused iterators, they are fused after `map_windows`."] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_map_windows)]"] #[doc = ""] #[doc = " #[derive(Default)]"] #[doc = " struct NonFusedIterator {"] #[doc = "     state: i32,"] #[doc = " }"] #[doc = ""] #[doc = " impl Iterator for NonFusedIterator {"] #[doc = "     type Item = i32;"] #[doc = ""] #[doc = "     fn next(&mut self) -> Option<i32> {"] #[doc = "         let val = self.state;"] #[doc = "         self.state = self.state + 1;"] #[doc = ""] #[doc = "         // yields `0..5` first, then only even numbers since `6..`."] #[doc = "         if val < 5 || val % 2 == 0 {"] #[doc = "             Some(val)"] #[doc = "         } else {"] #[doc = "             None"] #[doc = "         }"] #[doc = "     }"] #[doc = " }"] #[doc = ""] #[doc = ""] #[doc = " let mut iter = NonFusedIterator::default();"] #[doc = ""] #[doc = " // yields 0..5 first."] #[doc = " assert_eq!(iter.next(), Some(0));"] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = " assert_eq!(iter.next(), Some(2));"] #[doc = " assert_eq!(iter.next(), Some(3));"] #[doc = " assert_eq!(iter.next(), Some(4));"] #[doc = " // then we can see our iterator going back and forth"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " assert_eq!(iter.next(), Some(6));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " assert_eq!(iter.next(), Some(8));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = ""] #[doc = " // however, with `.map_windows()`, it is fused."] #[doc = " let mut iter = NonFusedIterator::default()"] #[doc = "     .map_windows(|arr: &[_; 2]| *arr);"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some([0, 1]));"] #[doc = " assert_eq!(iter.next(), Some([1, 2]));"] #[doc = " assert_eq!(iter.next(), Some([2, 3]));"] #[doc = " assert_eq!(iter.next(), Some([3, 4]));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = ""] #[doc = " // it will always return `None` after the first time."] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[inline] #[unstable (feature = "iter_map_windows" , reason = "recently added" , issue = "87155")] fn map_windows < F , R , const N : usize > (self , f : F) -> MapWindows < Self , F , N > where Self : Sized , F : FnMut (& [Self :: Item ; N]) -> R , { MapWindows :: new (self , f) } #[doc = " Creates an iterator which ends after the first [`None`]."] #[doc = ""] #[doc = " After an iterator returns [`None`], future calls may or may not yield"] #[doc = " [`Some(T)`] again. `fuse()` adapts an iterator, ensuring that after a"] #[doc = " [`None`] is given, it will always return [`None`] forever."] #[doc = ""] #[doc = " Note that the [`Fuse`] wrapper is a no-op on iterators that implement"] #[doc = " the [`FusedIterator`] trait. `fuse()` may therefore behave incorrectly"] #[doc = " if the [`FusedIterator`] trait is improperly implemented."] #[doc = ""] #[doc = " [`Some(T)`]: Some"] #[doc = " [`FusedIterator`]: crate::iter::FusedIterator"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " // an iterator which alternates between Some and None"] #[doc = " struct Alternate {"] #[doc = "     state: i32,"] #[doc = " }"] #[doc = ""] #[doc = " impl Iterator for Alternate {"] #[doc = "     type Item = i32;"] #[doc = ""] #[doc = "     fn next(&mut self) -> Option<i32> {"] #[doc = "         let val = self.state;"] #[doc = "         self.state = self.state + 1;"] #[doc = ""] #[doc = "         // if it's even, Some(i32), else None"] #[doc = "         (val % 2 == 0).then_some(val)"] #[doc = "     }"] #[doc = " }"] #[doc = ""] #[doc = " let mut iter = Alternate { state: 0 };"] #[doc = ""] #[doc = " // we can see our iterator going back and forth"] #[doc = " assert_eq!(iter.next(), Some(0));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " assert_eq!(iter.next(), Some(2));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = ""] #[doc = " // however, once we fuse it..."] #[doc = " let mut iter = iter.fuse();"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(4));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = ""] #[doc = " // it will always return `None` after the first time."] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn fuse (self) -> Fuse < Self > where Self : Sized , { Fuse :: new (self) } #[doc = " Does something with each element of an iterator, passing the value on."] #[doc = ""] #[doc = " When using iterators, you'll often chain several of them together."] #[doc = " While working on such code, you might want to check out what's"] #[doc = " happening at various parts in the pipeline. To do that, insert"] #[doc = " a call to `inspect()`."] #[doc = ""] #[doc = " It's more common for `inspect()` to be used as a debugging tool than to"] #[doc = " exist in your final code, but applications may find it useful in certain"] #[doc = " situations when errors need to be logged before being discarded."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 4, 2, 3];"] #[doc = ""] #[doc = " // this iterator sequence is complex."] #[doc = " let sum = a.iter()"] #[doc = "     .cloned()"] #[doc = "     .filter(|x| x % 2 == 0)"] #[doc = "     .fold(0, |sum, i| sum + i);"] #[doc = ""] #[doc = " println!(\"{sum}\");"] #[doc = ""] #[doc = " // let's add some inspect() calls to investigate what's happening"] #[doc = " let sum = a.iter()"] #[doc = "     .cloned()"] #[doc = "     .inspect(|x| println!(\"about to filter: {x}\"))"] #[doc = "     .filter(|x| x % 2 == 0)"] #[doc = "     .inspect(|x| println!(\"made it through filter: {x}\"))"] #[doc = "     .fold(0, |sum, i| sum + i);"] #[doc = ""] #[doc = " println!(\"{sum}\");"] #[doc = " ```"] #[doc = ""] #[doc = " This will print:"] #[doc = ""] #[doc = " ```text"] #[doc = " 6"] #[doc = " about to filter: 1"] #[doc = " about to filter: 4"] #[doc = " made it through filter: 4"] #[doc = " about to filter: 2"] #[doc = " made it through filter: 2"] #[doc = " about to filter: 3"] #[doc = " 6"] #[doc = " ```"] #[doc = ""] #[doc = " Logging errors before discarding them:"] #[doc = ""] #[doc = " ```"] #[doc = " let lines = [\"1\", \"2\", \"a\"];"] #[doc = ""] #[doc = " let sum: i32 = lines"] #[doc = "     .iter()"] #[doc = "     .map(|line| line.parse::<i32>())"] #[doc = "     .inspect(|num| {"] #[doc = "         if let Err(ref e) = *num {"] #[doc = "             println!(\"Parsing error: {e}\");"] #[doc = "         }"] #[doc = "     })"] #[doc = "     .filter_map(Result::ok)"] #[doc = "     .sum();"] #[doc = ""] #[doc = " println!(\"Sum: {sum}\");"] #[doc = " ```"] #[doc = ""] #[doc = " This will print:"] #[doc = ""] #[doc = " ```text"] #[doc = " Parsing error: invalid digit found in string"] #[doc = " Sum: 3"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn inspect < F > (self , f : F) -> Inspect < Self , F > where Self : Sized , F : FnMut (& Self :: Item) , { Inspect :: new (self , f) } #[doc = " Creates a \"by reference\" adapter for this instance of `Iterator`."] #[doc = ""] #[doc = " Consuming method calls (direct or indirect calls to `next`)"] #[doc = " on the \"by reference\" adapter will consume the original iterator,"] #[doc = " but ownership-taking methods (those with a `self` parameter)"] #[doc = " only take ownership of the \"by reference\" iterator."] #[doc = ""] #[doc = " This is useful for applying ownership-taking methods"] #[doc = " (such as `take` in the example below)"] #[doc = " without giving up ownership of the original iterator,"] #[doc = " so you can use the original iterator afterwards."] #[doc = ""] #[doc = " Uses [`impl<I: Iterator + ?Sized> Iterator for &mut I { type Item = I::Item; ...}`](https://doc.rust-lang.org/nightly/std/iter/trait.Iterator.html#impl-Iterator-for-%26mut+I)."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let mut words = [\"hello\", \"world\", \"of\", \"Rust\"].into_iter();"] #[doc = ""] #[doc = " // Take the first two words."] #[doc = " let hello_world: Vec<_> = words.by_ref().take(2).collect();"] #[doc = " assert_eq!(hello_world, vec![\"hello\", \"world\"]);"] #[doc = ""] #[doc = " // Collect the rest of the words."] #[doc = " // We can only do this because we used `by_ref` earlier."] #[doc = " let of_rust: Vec<_> = words.collect();"] #[doc = " assert_eq!(of_rust, vec![\"of\", \"Rust\"]);"] #[doc = " ```"] #[stable (feature = "rust1" , since = "1.0.0")] fn by_ref (& mut self) -> & mut Self where Self : Sized , { self } #[doc = " Transforms an iterator into a collection."] #[doc = ""] #[doc = " `collect()` can take anything iterable, and turn it into a relevant"] #[doc = " collection. This is one of the more powerful methods in the standard"] #[doc = " library, used in a variety of contexts."] #[doc = ""] #[doc = " The most basic pattern in which `collect()` is used is to turn one"] #[doc = " collection into another. You take a collection, call [`iter`] on it,"] #[doc = " do a bunch of transformations, and then `collect()` at the end."] #[doc = ""] #[doc = " `collect()` can also create instances of types that are not typical"] #[doc = " collections. For example, a [`String`] can be built from [`char`]s,"] #[doc = " and an iterator of [`Result<T, E>`][`Result`] items can be collected"] #[doc = " into `Result<Collection<T>, E>`. See the examples below for more."] #[doc = ""] #[doc = " Because `collect()` is so general, it can cause problems with type"] #[doc = " inference. As such, `collect()` is one of the few times you'll see"] #[doc = " the syntax affectionately known as the 'turbofish': `::<>`. This"] #[doc = " helps the inference algorithm understand specifically which collection"] #[doc = " you're trying to collect into."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let doubled: Vec<i32> = a.iter()"] #[doc = "                          .map(|x| x * 2)"] #[doc = "                          .collect();"] #[doc = ""] #[doc = " assert_eq!(vec![2, 4, 6], doubled);"] #[doc = " ```"] #[doc = ""] #[doc = " Note that we needed the `: Vec<i32>` on the left-hand side. This is because"] #[doc = " we could collect into, for example, a [`VecDeque<T>`] instead:"] #[doc = ""] #[doc = " [`VecDeque<T>`]: ../../std/collections/struct.VecDeque.html"] #[doc = ""] #[doc = " ```"] #[doc = " use std::collections::VecDeque;"] #[doc = ""] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let doubled: VecDeque<i32> = a.iter().map(|x| x * 2).collect();"] #[doc = ""] #[doc = " assert_eq!(2, doubled[0]);"] #[doc = " assert_eq!(4, doubled[1]);"] #[doc = " assert_eq!(6, doubled[2]);"] #[doc = " ```"] #[doc = ""] #[doc = " Using the 'turbofish' instead of annotating `doubled`:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let doubled = a.iter().map(|x| x * 2).collect::<Vec<i32>>();"] #[doc = ""] #[doc = " assert_eq!(vec![2, 4, 6], doubled);"] #[doc = " ```"] #[doc = ""] #[doc = " Because `collect()` only cares about what you're collecting into, you can"] #[doc = " still use a partial type hint, `_`, with the turbofish:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let doubled = a.iter().map(|x| x * 2).collect::<Vec<_>>();"] #[doc = ""] #[doc = " assert_eq!(vec![2, 4, 6], doubled);"] #[doc = " ```"] #[doc = ""] #[doc = " Using `collect()` to make a [`String`]:"] #[doc = ""] #[doc = " ```"] #[doc = " let chars = ['g', 'd', 'k', 'k', 'n'];"] #[doc = ""] #[doc = " let hello: String = chars.into_iter()"] #[doc = "     .map(|x| x as u8)"] #[doc = "     .map(|x| (x + 1) as char)"] #[doc = "     .collect();"] #[doc = ""] #[doc = " assert_eq!(\"hello\", hello);"] #[doc = " ```"] #[doc = ""] #[doc = " If you have a list of [`Result<T, E>`][`Result`]s, you can use `collect()` to"] #[doc = " see if any of them failed:"] #[doc = ""] #[doc = " ```"] #[doc = " let results = [Ok(1), Err(\"nope\"), Ok(3), Err(\"bad\")];"] #[doc = ""] #[doc = " let result: Result<Vec<_>, &str> = results.into_iter().collect();"] #[doc = ""] #[doc = " // gives us the first error"] #[doc = " assert_eq!(Err(\"nope\"), result);"] #[doc = ""] #[doc = " let results = [Ok(1), Ok(3)];"] #[doc = ""] #[doc = " let result: Result<Vec<_>, &str> = results.into_iter().collect();"] #[doc = ""] #[doc = " // gives us the list of answers"] #[doc = " assert_eq!(Ok(vec![1, 3]), result);"] #[doc = " ```"] #[doc = ""] #[doc = " [`iter`]: Iterator::next"] #[doc = " [`String`]: ../../std/string/struct.String.html"] #[doc = " [`char`]: type@char"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "if you really need to exhaust the iterator, consider `.for_each(drop)` instead"] #[rustc_diagnostic_item = "iterator_collect_fn"] fn collect < B : FromIterator < Self :: Item > > (self) -> B where Self : Sized , { if cfg ! (debug_assertions) { let hint = self . size_hint () ; assert ! (hint . 1 . is_none_or (| high | high >= hint . 0) , "Malformed size_hint {hint:?}") ; } FromIterator :: from_iter (self) } #[doc = " Fallibly transforms an iterator into a collection, short circuiting if"] #[doc = " a failure is encountered."] #[doc = ""] #[doc = " `try_collect()` is a variation of [`collect()`][`collect`] that allows fallible"] #[doc = " conversions during collection. Its main use case is simplifying conversions from"] #[doc = " iterators yielding [`Option<T>`][`Option`] into `Option<Collection<T>>`, or similarly for other [`Try`]"] #[doc = " types (e.g. [`Result`])."] #[doc = ""] #[doc = " Importantly, `try_collect()` doesn't require that the outer [`Try`] type also implements [`FromIterator`];"] #[doc = " only the inner type produced on `Try::Output` must implement it. Concretely,"] #[doc = " this means that collecting into `ControlFlow<_, Vec<i32>>` is valid because `Vec<i32>` implements"] #[doc = " [`FromIterator`], even though [`ControlFlow`] doesn't."] #[doc = ""] #[doc = " Also, if a failure is encountered during `try_collect()`, the iterator is still valid and"] #[doc = " may continue to be used, in which case it will continue iterating starting after the element that"] #[doc = " triggered the failure. See the last example below for an example of how this works."] #[doc = ""] #[doc = " # Examples"] #[doc = " Successfully collecting an iterator of `Option<i32>` into `Option<Vec<i32>>`:"] #[doc = " ```"] #[doc = " #![feature(iterator_try_collect)]"] #[doc = ""] #[doc = " let u = vec![Some(1), Some(2), Some(3)];"] #[doc = " let v = u.into_iter().try_collect::<Vec<i32>>();"] #[doc = " assert_eq!(v, Some(vec![1, 2, 3]));"] #[doc = " ```"] #[doc = ""] #[doc = " Failing to collect in the same way:"] #[doc = " ```"] #[doc = " #![feature(iterator_try_collect)]"] #[doc = ""] #[doc = " let u = vec![Some(1), Some(2), None, Some(3)];"] #[doc = " let v = u.into_iter().try_collect::<Vec<i32>>();"] #[doc = " assert_eq!(v, None);"] #[doc = " ```"] #[doc = ""] #[doc = " A similar example, but with `Result`:"] #[doc = " ```"] #[doc = " #![feature(iterator_try_collect)]"] #[doc = ""] #[doc = " let u: Vec<Result<i32, ()>> = vec![Ok(1), Ok(2), Ok(3)];"] #[doc = " let v = u.into_iter().try_collect::<Vec<i32>>();"] #[doc = " assert_eq!(v, Ok(vec![1, 2, 3]));"] #[doc = ""] #[doc = " let u = vec![Ok(1), Ok(2), Err(()), Ok(3)];"] #[doc = " let v = u.into_iter().try_collect::<Vec<i32>>();"] #[doc = " assert_eq!(v, Err(()));"] #[doc = " ```"] #[doc = ""] #[doc = " Finally, even [`ControlFlow`] works, despite the fact that it"] #[doc = " doesn't implement [`FromIterator`]. Note also that the iterator can"] #[doc = " continue to be used, even if a failure is encountered:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iterator_try_collect)]"] #[doc = ""] #[doc = " use core::ops::ControlFlow::{Break, Continue};"] #[doc = ""] #[doc = " let u = [Continue(1), Continue(2), Break(3), Continue(4), Continue(5)];"] #[doc = " let mut it = u.into_iter();"] #[doc = ""] #[doc = " let v = it.try_collect::<Vec<_>>();"] #[doc = " assert_eq!(v, Break(3));"] #[doc = ""] #[doc = " let v = it.try_collect::<Vec<_>>();"] #[doc = " assert_eq!(v, Continue(vec![4, 5]));"] #[doc = " ```"] #[doc = ""] #[doc = " [`collect`]: Iterator::collect"] #[inline] #[unstable (feature = "iterator_try_collect" , issue = "94047")] fn try_collect < B > (& mut self) -> ChangeOutputType < Self :: Item , B > where Self : Sized , Self :: Item : Try < Residual : Residual < B > > , B : FromIterator < < Self :: Item as Try > :: Output > , { try_process (ByRefSized (self) , | i | i . collect ()) } #[doc = " Collects all the items from an iterator into a collection."] #[doc = ""] #[doc = " This method consumes the iterator and adds all its items to the"] #[doc = " passed collection. The collection is then returned, so the call chain"] #[doc = " can be continued."] #[doc = ""] #[doc = " This is useful when you already have a collection and want to add"] #[doc = " the iterator items to it."] #[doc = ""] #[doc = " This method is a convenience method to call [Extend::extend](trait.Extend.html),"] #[doc = " but instead of being called on a collection, it's called on an iterator."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_collect_into)]"] #[doc = ""] #[doc = " let a = [1, 2, 3];"] #[doc = " let mut vec: Vec::<i32> = vec![0, 1];"] #[doc = ""] #[doc = " a.iter().map(|x| x * 2).collect_into(&mut vec);"] #[doc = " a.iter().map(|x| x * 10).collect_into(&mut vec);"] #[doc = ""] #[doc = " assert_eq!(vec, vec![0, 1, 2, 4, 6, 10, 20, 30]);"] #[doc = " ```"] #[doc = ""] #[doc = " `Vec` can have a manual set capacity to avoid reallocating it:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_collect_into)]"] #[doc = ""] #[doc = " let a = [1, 2, 3];"] #[doc = " let mut vec: Vec::<i32> = Vec::with_capacity(6);"] #[doc = ""] #[doc = " a.iter().map(|x| x * 2).collect_into(&mut vec);"] #[doc = " a.iter().map(|x| x * 10).collect_into(&mut vec);"] #[doc = ""] #[doc = " assert_eq!(6, vec.capacity());"] #[doc = " assert_eq!(vec, vec![2, 4, 6, 10, 20, 30]);"] #[doc = " ```"] #[doc = ""] #[doc = " The returned mutable reference can be used to continue the call chain:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_collect_into)]"] #[doc = ""] #[doc = " let a = [1, 2, 3];"] #[doc = " let mut vec: Vec::<i32> = Vec::with_capacity(6);"] #[doc = ""] #[doc = " let count = a.iter().collect_into(&mut vec).iter().count();"] #[doc = ""] #[doc = " assert_eq!(count, vec.len());"] #[doc = " assert_eq!(vec, vec![1, 2, 3]);"] #[doc = ""] #[doc = " let count = a.iter().collect_into(&mut vec).iter().count();"] #[doc = ""] #[doc = " assert_eq!(count, vec.len());"] #[doc = " assert_eq!(vec, vec![1, 2, 3, 1, 2, 3]);"] #[doc = " ```"] #[inline] #[unstable (feature = "iter_collect_into" , reason = "new API" , issue = "94780")] fn collect_into < E : Extend < Self :: Item > > (self , collection : & mut E) -> & mut E where Self : Sized , { collection . extend (self) ; collection } #[doc = " Consumes an iterator, creating two collections from it."] #[doc = ""] #[doc = " The predicate passed to `partition()` can return `true`, or `false`."] #[doc = " `partition()` returns a pair, all of the elements for which it returned"] #[doc = " `true`, and all of the elements for which it returned `false`."] #[doc = ""] #[doc = " See also [`is_partitioned()`] and [`partition_in_place()`]."] #[doc = ""] #[doc = " [`is_partitioned()`]: Iterator::is_partitioned"] #[doc = " [`partition_in_place()`]: Iterator::partition_in_place"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let (even, odd): (Vec<_>, Vec<_>) = a"] #[doc = "     .into_iter()"] #[doc = "     .partition(|n| n % 2 == 0);"] #[doc = ""] #[doc = " assert_eq!(even, [2]);"] #[doc = " assert_eq!(odd, [1, 3]);"] #[doc = " ```"] #[stable (feature = "rust1" , since = "1.0.0")] fn partition < B , F > (self , f : F) -> (B , B) where Self : Sized , B : Default + Extend < Self :: Item > , F : FnMut (& Self :: Item) -> bool , { #[inline] fn extend < 'a , T , B : Extend < T > > (mut f : impl FnMut (& T) -> bool + 'a , left : & 'a mut B , right : & 'a mut B ,) -> impl FnMut (() , T) + 'a { move | () , x | { if f (& x) { left . extend_one (x) ; } else { right . extend_one (x) ; } } } let mut left : B = Default :: default () ; let mut right : B = Default :: default () ; self . fold (() , extend (f , & mut left , & mut right)) ; (left , right) } #[doc = " Reorders the elements of this iterator *in-place* according to the given predicate,"] #[doc = " such that all those that return `true` precede all those that return `false`."] #[doc = " Returns the number of `true` elements found."] #[doc = ""] #[doc = " The relative order of partitioned items is not maintained."] #[doc = ""] #[doc = " # Current implementation"] #[doc = ""] #[doc = " The current algorithm tries to find the first element for which the predicate evaluates"] #[doc = " to false and the last element for which it evaluates to true, and repeatedly swaps them."] #[doc = ""] #[doc = " Time complexity: *O*(*n*)"] #[doc = ""] #[doc = " See also [`is_partitioned()`] and [`partition()`]."] #[doc = ""] #[doc = " [`is_partitioned()`]: Iterator::is_partitioned"] #[doc = " [`partition()`]: Iterator::partition"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_partition_in_place)]"] #[doc = ""] #[doc = " let mut a = [1, 2, 3, 4, 5, 6, 7];"] #[doc = ""] #[doc = " // Partition in-place between evens and odds"] #[doc = " let i = a.iter_mut().partition_in_place(|n| n % 2 == 0);"] #[doc = ""] #[doc = " assert_eq!(i, 3);"] #[doc = " assert!(a[..i].iter().all(|n| n % 2 == 0)); // evens"] #[doc = " assert!(a[i..].iter().all(|n| n % 2 == 1)); // odds"] #[doc = " ```"] #[unstable (feature = "iter_partition_in_place" , reason = "new API" , issue = "62543")] fn partition_in_place < 'a , T : 'a , P > (mut self , ref mut predicate : P) -> usize where Self : Sized + DoubleEndedIterator < Item = & 'a mut T > , P : FnMut (& T) -> bool , { #[inline] fn is_false < 'a , T > (predicate : & 'a mut impl FnMut (& T) -> bool , true_count : & 'a mut usize ,) -> impl FnMut (& & mut T) -> bool + 'a { move | x | { let p = predicate (& * * x) ; * true_count += p as usize ; ! p } } #[inline] fn is_true < T > (predicate : & mut impl FnMut (& T) -> bool) -> impl FnMut (& & mut T) -> bool + '_ { move | x | predicate (& * * x) } let mut true_count = 0 ; while let Some (head) = self . find (is_false (predicate , & mut true_count)) { if let Some (tail) = self . rfind (is_true (predicate)) { crate :: mem :: swap (head , tail) ; true_count += 1 ; } else { break ; } } true_count } #[doc = " Checks if the elements of this iterator are partitioned according to the given predicate,"] #[doc = " such that all those that return `true` precede all those that return `false`."] #[doc = ""] #[doc = " See also [`partition()`] and [`partition_in_place()`]."] #[doc = ""] #[doc = " [`partition()`]: Iterator::partition"] #[doc = " [`partition_in_place()`]: Iterator::partition_in_place"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_is_partitioned)]"] #[doc = ""] #[doc = " assert!(\"Iterator\".chars().is_partitioned(char::is_uppercase));"] #[doc = " assert!(!\"IntoIterator\".chars().is_partitioned(char::is_uppercase));"] #[doc = " ```"] #[unstable (feature = "iter_is_partitioned" , reason = "new API" , issue = "62544")] fn is_partitioned < P > (mut self , mut predicate : P) -> bool where Self : Sized , P : FnMut (Self :: Item) -> bool , { self . all (& mut predicate) || ! self . any (predicate) } #[doc = " An iterator method that applies a function as long as it returns"] #[doc = " successfully, producing a single, final value."] #[doc = ""] #[doc = " `try_fold()` takes two arguments: an initial value, and a closure with"] #[doc = " two arguments: an 'accumulator', and an element. The closure either"] #[doc = " returns successfully, with the value that the accumulator should have"] #[doc = " for the next iteration, or it returns failure, with an error value that"] #[doc = " is propagated back to the caller immediately (short-circuiting)."] #[doc = ""] #[doc = " The initial value is the value the accumulator will have on the first"] #[doc = " call. If applying the closure succeeded against every element of the"] #[doc = " iterator, `try_fold()` returns the final accumulator as success."] #[doc = ""] #[doc = " Folding is useful whenever you have a collection of something, and want"] #[doc = " to produce a single value from it."] #[doc = ""] #[doc = " # Note to Implementors"] #[doc = ""] #[doc = " Several of the other (forward) methods have default implementations in"] #[doc = " terms of this one, so try to implement this explicitly if it can"] #[doc = " do something better than the default `for` loop implementation."] #[doc = ""] #[doc = " In particular, try to have this call `try_fold()` on the internal parts"] #[doc = " from which this iterator is composed. If multiple calls are needed,"] #[doc = " the `?` operator may be convenient for chaining the accumulator value"] #[doc = " along, but beware any invariants that need to be upheld before those"] #[doc = " early returns. This is a `&mut self` method, so iteration needs to be"] #[doc = " resumable after hitting an error here."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " // the checked sum of all of the elements of the array"] #[doc = " let sum = a.into_iter().try_fold(0i8, |acc, x| acc.checked_add(x));"] #[doc = ""] #[doc = " assert_eq!(sum, Some(6));"] #[doc = " ```"] #[doc = ""] #[doc = " Short-circuiting:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [10, 20, 30, 100, 40, 50];"] #[doc = " let mut iter = a.into_iter();"] #[doc = ""] #[doc = " // This sum overflows when adding the 100 element"] #[doc = " let sum = iter.try_fold(0i8, |acc, x| acc.checked_add(x));"] #[doc = " assert_eq!(sum, None);"] #[doc = ""] #[doc = " // Because it short-circuited, the remaining elements are still"] #[doc = " // available through the iterator."] #[doc = " assert_eq!(iter.len(), 2);"] #[doc = " assert_eq!(iter.next(), Some(40));"] #[doc = " ```"] #[doc = ""] #[doc = " While you cannot `break` from a closure, the [`ControlFlow`] type allows"] #[doc = " a similar idea:"] #[doc = ""] #[doc = " ```"] #[doc = " use std::ops::ControlFlow;"] #[doc = ""] #[doc = " let triangular = (1..30).try_fold(0_i8, |prev, x| {"] #[doc = "     if let Some(next) = prev.checked_add(x) {"] #[doc = "         ControlFlow::Continue(next)"] #[doc = "     } else {"] #[doc = "         ControlFlow::Break(prev)"] #[doc = "     }"] #[doc = " });"] #[doc = " assert_eq!(triangular, ControlFlow::Break(120));"] #[doc = ""] #[doc = " let triangular = (1..30).try_fold(0_u64, |prev, x| {"] #[doc = "     if let Some(next) = prev.checked_add(x) {"] #[doc = "         ControlFlow::Continue(next)"] #[doc = "     } else {"] #[doc = "         ControlFlow::Break(prev)"] #[doc = "     }"] #[doc = " });"] #[doc = " assert_eq!(triangular, ControlFlow::Continue(435));"] #[doc = " ```"] #[inline] #[stable (feature = "iterator_try_fold" , since = "1.27.0")] fn try_fold < B , F , R > (& mut self , init : B , mut f : F) -> R where Self : Sized , F : FnMut (B , Self :: Item) -> R , R : Try < Output = B > , { let mut accum = init ; while let Some (x) = self . next () { accum = f (accum , x) ? ; } try { accum } } #[doc = " An iterator method that applies a fallible function to each item in the"] #[doc = " iterator, stopping at the first error and returning that error."] #[doc = ""] #[doc = " This can also be thought of as the fallible form of [`for_each()`]"] #[doc = " or as the stateless version of [`try_fold()`]."] #[doc = ""] #[doc = " [`for_each()`]: Iterator::for_each"] #[doc = " [`try_fold()`]: Iterator::try_fold"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " use std::fs::rename;"] #[doc = " use std::io::{stdout, Write};"] #[doc = " use std::path::Path;"] #[doc = ""] #[doc = " let data = [\"no_tea.txt\", \"stale_bread.json\", \"torrential_rain.png\"];"] #[doc = ""] #[doc = " let res = data.iter().try_for_each(|x| writeln!(stdout(), \"{x}\"));"] #[doc = " assert!(res.is_ok());"] #[doc = ""] #[doc = " let mut it = data.iter().cloned();"] #[doc = " let res = it.try_for_each(|x| rename(x, Path::new(x).with_extension(\"old\")));"] #[doc = " assert!(res.is_err());"] #[doc = " // It short-circuited, so the remaining items are still in the iterator:"] #[doc = " assert_eq!(it.next(), Some(\"stale_bread.json\"));"] #[doc = " ```"] #[doc = ""] #[doc = " The [`ControlFlow`] type can be used with this method for the situations"] #[doc = " in which you'd use `break` and `continue` in a normal loop:"] #[doc = ""] #[doc = " ```"] #[doc = " use std::ops::ControlFlow;"] #[doc = ""] #[doc = " let r = (2..100).try_for_each(|x| {"] #[doc = "     if 323 % x == 0 {"] #[doc = "         return ControlFlow::Break(x)"] #[doc = "     }"] #[doc = ""] #[doc = "     ControlFlow::Continue(())"] #[doc = " });"] #[doc = " assert_eq!(r, ControlFlow::Break(17));"] #[doc = " ```"] #[inline] #[stable (feature = "iterator_try_fold" , since = "1.27.0")] fn try_for_each < F , R > (& mut self , f : F) -> R where Self : Sized , F : FnMut (Self :: Item) -> R , R : Try < Output = () > , { #[inline] fn call < T , R > (mut f : impl FnMut (T) -> R) -> impl FnMut (() , T) -> R { move | () , x | f (x) } self . try_fold (() , call (f)) } #[doc = " Folds every element into an accumulator by applying an operation,"] #[doc = " returning the final result."] #[doc = ""] #[doc = " `fold()` takes two arguments: an initial value, and a closure with two"] #[doc = " arguments: an 'accumulator', and an element. The closure returns the value that"] #[doc = " the accumulator should have for the next iteration."] #[doc = ""] #[doc = " The initial value is the value the accumulator will have on the first"] #[doc = " call."] #[doc = ""] #[doc = " After applying this closure to every element of the iterator, `fold()`"] #[doc = " returns the accumulator."] #[doc = ""] #[doc = " This operation is sometimes called 'reduce' or 'inject'."] #[doc = ""] #[doc = " Folding is useful whenever you have a collection of something, and want"] #[doc = " to produce a single value from it."] #[doc = ""] #[doc = " Note: `fold()`, and similar methods that traverse the entire iterator,"] #[doc = " might not terminate for infinite iterators, even on traits for which a"] #[doc = " result is determinable in finite time."] #[doc = ""] #[doc = " Note: [`reduce()`] can be used to use the first element as the initial"] #[doc = " value, if the accumulator type and item type is the same."] #[doc = ""] #[doc = " Note: `fold()` combines elements in a *left-associative* fashion. For associative"] #[doc = " operators like `+`, the order the elements are combined in is not important, but for non-associative"] #[doc = " operators like `-` the order will affect the final result."] #[doc = " For a *right-associative* version of `fold()`, see [`DoubleEndedIterator::rfold()`]."] #[doc = ""] #[doc = " # Note to Implementors"] #[doc = ""] #[doc = " Several of the other (forward) methods have default implementations in"] #[doc = " terms of this one, so try to implement this explicitly if it can"] #[doc = " do something better than the default `for` loop implementation."] #[doc = ""] #[doc = " In particular, try to have this call `fold()` on the internal parts"] #[doc = " from which this iterator is composed."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " // the sum of all of the elements of the array"] #[doc = " let sum = a.iter().fold(0, |acc, x| acc + x);"] #[doc = ""] #[doc = " assert_eq!(sum, 6);"] #[doc = " ```"] #[doc = ""] #[doc = " Let's walk through each step of the iteration here:"] #[doc = ""] #[doc = " | element | acc | x | result |"] #[doc = " |---------|-----|---|--------|"] #[doc = " |         | 0   |   |        |"] #[doc = " | 1       | 0   | 1 | 1      |"] #[doc = " | 2       | 1   | 2 | 3      |"] #[doc = " | 3       | 3   | 3 | 6      |"] #[doc = ""] #[doc = " And so, our final result, `6`."] #[doc = ""] #[doc = " This example demonstrates the left-associative nature of `fold()`:"] #[doc = " it builds a string, starting with an initial value"] #[doc = " and continuing with each element from the front until the back:"] #[doc = ""] #[doc = " ```"] #[doc = " let numbers = [1, 2, 3, 4, 5];"] #[doc = ""] #[doc = " let zero = \"0\".to_string();"] #[doc = ""] #[doc = " let result = numbers.iter().fold(zero, |acc, &x| {"] #[doc = "     format!(\"({acc} + {x})\")"] #[doc = " });"] #[doc = ""] #[doc = " assert_eq!(result, \"(((((0 + 1) + 2) + 3) + 4) + 5)\");"] #[doc = " ```"] #[doc = " It's common for people who haven't used iterators a lot to"] #[doc = " use a `for` loop with a list of things to build up a result. Those"] #[doc = " can be turned into `fold()`s:"] #[doc = ""] #[doc = " [`for`]: ../../book/ch03-05-control-flow.html#looping-through-a-collection-with-for"] #[doc = ""] #[doc = " ```"] #[doc = " let numbers = [1, 2, 3, 4, 5];"] #[doc = ""] #[doc = " let mut result = 0;"] #[doc = ""] #[doc = " // for loop:"] #[doc = " for i in &numbers {"] #[doc = "     result = result + i;"] #[doc = " }"] #[doc = ""] #[doc = " // fold:"] #[doc = " let result2 = numbers.iter().fold(0, |acc, &x| acc + x);"] #[doc = ""] #[doc = " // they're the same"] #[doc = " assert_eq!(result, result2);"] #[doc = " ```"] #[doc = ""] #[doc = " [`reduce()`]: Iterator::reduce"] #[doc (alias = "inject" , alias = "foldl")] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn fold < B , F > (mut self , init : B , mut f : F) -> B where Self : Sized , F : FnMut (B , Self :: Item) -> B , { let mut accum = init ; while let Some (x) = self . next () { accum = f (accum , x) ; } accum } #[doc = " Reduces the elements to a single one, by repeatedly applying a reducing"] #[doc = " operation."] #[doc = ""] #[doc = " If the iterator is empty, returns [`None`]; otherwise, returns the"] #[doc = " result of the reduction."] #[doc = ""] #[doc = " The reducing function is a closure with two arguments: an 'accumulator', and an element."] #[doc = " For iterators with at least one element, this is the same as [`fold()`]"] #[doc = " with the first element of the iterator as the initial accumulator value, folding"] #[doc = " every subsequent element into it."] #[doc = ""] #[doc = " [`fold()`]: Iterator::fold"] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let reduced: i32 = (1..10).reduce(|acc, e| acc + e).unwrap_or(0);"] #[doc = " assert_eq!(reduced, 45);"] #[doc = ""] #[doc = " // Which is equivalent to doing it with `fold`:"] #[doc = " let folded: i32 = (1..10).fold(0, |acc, e| acc + e);"] #[doc = " assert_eq!(reduced, folded);"] #[doc = " ```"] #[inline] #[stable (feature = "iterator_fold_self" , since = "1.51.0")] fn reduce < F > (mut self , f : F) -> Option < Self :: Item > where Self : Sized , F : FnMut (Self :: Item , Self :: Item) -> Self :: Item , { let first = self . next () ? ; Some (self . fold (first , f)) } #[doc = " Reduces the elements to a single one by repeatedly applying a reducing operation. If the"] #[doc = " closure returns a failure, the failure is propagated back to the caller immediately."] #[doc = ""] #[doc = " The return type of this method depends on the return type of the closure. If the closure"] #[doc = " returns `Result<Self::Item, E>`, then this function will return `Result<Option<Self::Item>,"] #[doc = " E>`. If the closure returns `Option<Self::Item>`, then this function will return"] #[doc = " `Option<Option<Self::Item>>`."] #[doc = ""] #[doc = " When called on an empty iterator, this function will return either `Some(None)` or"] #[doc = " `Ok(None)` depending on the type of the provided closure."] #[doc = ""] #[doc = " For iterators with at least one element, this is essentially the same as calling"] #[doc = " [`try_fold()`] with the first element of the iterator as the initial accumulator value."] #[doc = ""] #[doc = " [`try_fold()`]: Iterator::try_fold"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Safely calculate the sum of a series of numbers:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iterator_try_reduce)]"] #[doc = ""] #[doc = " let numbers: Vec<usize> = vec![10, 20, 5, 23, 0];"] #[doc = " let sum = numbers.into_iter().try_reduce(|x, y| x.checked_add(y));"] #[doc = " assert_eq!(sum, Some(Some(58)));"] #[doc = " ```"] #[doc = ""] #[doc = " Determine when a reduction short circuited:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iterator_try_reduce)]"] #[doc = ""] #[doc = " let numbers = vec![1, 2, 3, usize::MAX, 4, 5];"] #[doc = " let sum = numbers.into_iter().try_reduce(|x, y| x.checked_add(y));"] #[doc = " assert_eq!(sum, None);"] #[doc = " ```"] #[doc = ""] #[doc = " Determine when a reduction was not performed because there are no elements:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iterator_try_reduce)]"] #[doc = ""] #[doc = " let numbers: Vec<usize> = Vec::new();"] #[doc = " let sum = numbers.into_iter().try_reduce(|x, y| x.checked_add(y));"] #[doc = " assert_eq!(sum, Some(None));"] #[doc = " ```"] #[doc = ""] #[doc = " Use a [`Result`] instead of an [`Option`]:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iterator_try_reduce)]"] #[doc = ""] #[doc = " let numbers = vec![\"1\", \"2\", \"3\", \"4\", \"5\"];"] #[doc = " let max: Result<Option<_>, <usize as std::str::FromStr>::Err> ="] #[doc = "     numbers.into_iter().try_reduce(|x, y| {"] #[doc = "         if x.parse::<usize>()? > y.parse::<usize>()? { Ok(x) } else { Ok(y) }"] #[doc = "     });"] #[doc = " assert_eq!(max, Ok(Some(\"5\")));"] #[doc = " ```"] #[inline] #[unstable (feature = "iterator_try_reduce" , reason = "new API" , issue = "87053")] fn try_reduce < R > (& mut self , f : impl FnMut (Self :: Item , Self :: Item) -> R ,) -> ChangeOutputType < R , Option < R :: Output > > where Self : Sized , R : Try < Output = Self :: Item , Residual : Residual < Option < Self :: Item > > > , { let first = match self . next () { Some (i) => i , None => return Try :: from_output (None) , } ; match self . try_fold (first , f) . branch () { ControlFlow :: Break (r) => FromResidual :: from_residual (r) , ControlFlow :: Continue (i) => Try :: from_output (Some (i)) , } } #[doc = " Tests if every element of the iterator matches a predicate."] #[doc = ""] #[doc = " `all()` takes a closure that returns `true` or `false`. It applies"] #[doc = " this closure to each element of the iterator, and if they all return"] #[doc = " `true`, then so does `all()`. If any of them return `false`, it"] #[doc = " returns `false`."] #[doc = ""] #[doc = " `all()` is short-circuiting; in other words, it will stop processing"] #[doc = " as soon as it finds a `false`, given that no matter what else happens,"] #[doc = " the result will also be `false`."] #[doc = ""] #[doc = " An empty iterator returns `true`."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " assert!(a.into_iter().all(|x| x > 0));"] #[doc = ""] #[doc = " assert!(!a.into_iter().all(|x| x > 2));"] #[doc = " ```"] #[doc = ""] #[doc = " Stopping at the first `false`:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let mut iter = a.into_iter();"] #[doc = ""] #[doc = " assert!(!iter.all(|x| x != 2));"] #[doc = ""] #[doc = " // we can still use `iter`, as there are more elements."] #[doc = " assert_eq!(iter.next(), Some(3));"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn all < F > (& mut self , f : F) -> bool where Self : Sized , F : FnMut (Self :: Item) -> bool , { #[inline] fn check < T > (mut f : impl FnMut (T) -> bool) -> impl FnMut (() , T) -> ControlFlow < () > { move | () , x | { if f (x) { ControlFlow :: Continue (()) } else { ControlFlow :: Break (()) } } } self . try_fold (() , check (f)) == ControlFlow :: Continue (()) } #[doc = " Tests if any element of the iterator matches a predicate."] #[doc = ""] #[doc = " `any()` takes a closure that returns `true` or `false`. It applies"] #[doc = " this closure to each element of the iterator, and if any of them return"] #[doc = " `true`, then so does `any()`. If they all return `false`, it"] #[doc = " returns `false`."] #[doc = ""] #[doc = " `any()` is short-circuiting; in other words, it will stop processing"] #[doc = " as soon as it finds a `true`, given that no matter what else happens,"] #[doc = " the result will also be `true`."] #[doc = ""] #[doc = " An empty iterator returns `false`."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " assert!(a.into_iter().any(|x| x > 0));"] #[doc = ""] #[doc = " assert!(!a.into_iter().any(|x| x > 5));"] #[doc = " ```"] #[doc = ""] #[doc = " Stopping at the first `true`:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let mut iter = a.into_iter();"] #[doc = ""] #[doc = " assert!(iter.any(|x| x != 2));"] #[doc = ""] #[doc = " // we can still use `iter`, as there are more elements."] #[doc = " assert_eq!(iter.next(), Some(2));"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn any < F > (& mut self , f : F) -> bool where Self : Sized , F : FnMut (Self :: Item) -> bool , { #[inline] fn check < T > (mut f : impl FnMut (T) -> bool) -> impl FnMut (() , T) -> ControlFlow < () > { move | () , x | { if f (x) { ControlFlow :: Break (()) } else { ControlFlow :: Continue (()) } } } self . try_fold (() , check (f)) == ControlFlow :: Break (()) } #[doc = " Searches for an element of an iterator that satisfies a predicate."] #[doc = ""] #[doc = " `find()` takes a closure that returns `true` or `false`. It applies"] #[doc = " this closure to each element of the iterator, and if any of them return"] #[doc = " `true`, then `find()` returns [`Some(element)`]. If they all return"] #[doc = " `false`, it returns [`None`]."] #[doc = ""] #[doc = " `find()` is short-circuiting; in other words, it will stop processing"] #[doc = " as soon as the closure returns `true`."] #[doc = ""] #[doc = " Because `find()` takes a reference, and many iterators iterate over"] #[doc = " references, this leads to a possibly confusing situation where the"] #[doc = " argument is a double reference. You can see this effect in the"] #[doc = " examples below, with `&&x`."] #[doc = ""] #[doc = " If you need the index of the element, see [`position()`]."] #[doc = ""] #[doc = " [`Some(element)`]: Some"] #[doc = " [`position()`]: Iterator::position"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " assert_eq!(a.into_iter().find(|&x| x == 2), Some(2));"] #[doc = " assert_eq!(a.into_iter().find(|&x| x == 5), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Stopping at the first `true`:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let mut iter = a.into_iter();"] #[doc = ""] #[doc = " assert_eq!(iter.find(|&x| x == 2), Some(2));"] #[doc = ""] #[doc = " // we can still use `iter`, as there are more elements."] #[doc = " assert_eq!(iter.next(), Some(3));"] #[doc = " ```"] #[doc = ""] #[doc = " Note that `iter.find(f)` is equivalent to `iter.filter(f).next()`."] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn find < P > (& mut self , predicate : P) -> Option < Self :: Item > where Self : Sized , P : FnMut (& Self :: Item) -> bool , { #[inline] fn check < T > (mut predicate : impl FnMut (& T) -> bool) -> impl FnMut (() , T) -> ControlFlow < T > { move | () , x | { if predicate (& x) { ControlFlow :: Break (x) } else { ControlFlow :: Continue (()) } } } self . try_fold (() , check (predicate)) . break_value () } #[doc = " Applies function to the elements of iterator and returns"] #[doc = " the first non-none result."] #[doc = ""] #[doc = " `iter.find_map(f)` is equivalent to `iter.filter_map(f).next()`."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [\"lol\", \"NaN\", \"2\", \"5\"];"] #[doc = ""] #[doc = " let first_number = a.iter().find_map(|s| s.parse().ok());"] #[doc = ""] #[doc = " assert_eq!(first_number, Some(2));"] #[doc = " ```"] #[inline] #[stable (feature = "iterator_find_map" , since = "1.30.0")] fn find_map < B , F > (& mut self , f : F) -> Option < B > where Self : Sized , F : FnMut (Self :: Item) -> Option < B > , { #[inline] fn check < T , B > (mut f : impl FnMut (T) -> Option < B >) -> impl FnMut (() , T) -> ControlFlow < B > { move | () , x | match f (x) { Some (x) => ControlFlow :: Break (x) , None => ControlFlow :: Continue (()) , } } self . try_fold (() , check (f)) . break_value () } #[doc = " Applies function to the elements of iterator and returns"] #[doc = " the first true result or the first error."] #[doc = ""] #[doc = " The return type of this method depends on the return type of the closure."] #[doc = " If you return `Result<bool, E>` from the closure, you'll get a `Result<Option<Self::Item>, E>`."] #[doc = " If you return `Option<bool>` from the closure, you'll get an `Option<Option<Self::Item>>`."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(try_find)]"] #[doc = ""] #[doc = " let a = [\"1\", \"2\", \"lol\", \"NaN\", \"5\"];"] #[doc = ""] #[doc = " let is_my_num = |s: &str, search: i32| -> Result<bool, std::num::ParseIntError> {"] #[doc = "     Ok(s.parse::<i32>()? == search)"] #[doc = " };"] #[doc = ""] #[doc = " let result = a.into_iter().try_find(|&s| is_my_num(s, 2));"] #[doc = " assert_eq!(result, Ok(Some(\"2\")));"] #[doc = ""] #[doc = " let result = a.into_iter().try_find(|&s| is_my_num(s, 5));"] #[doc = " assert!(result.is_err());"] #[doc = " ```"] #[doc = ""] #[doc = " This also supports other types which implement [`Try`], not just [`Result`]."] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(try_find)]"] #[doc = ""] #[doc = " use std::num::NonZero;"] #[doc = ""] #[doc = " let a = [3, 5, 7, 4, 9, 0, 11u32];"] #[doc = " let result = a.into_iter().try_find(|&x| NonZero::new(x).map(|y| y.is_power_of_two()));"] #[doc = " assert_eq!(result, Some(Some(4)));"] #[doc = " let result = a.into_iter().take(3).try_find(|&x| NonZero::new(x).map(|y| y.is_power_of_two()));"] #[doc = " assert_eq!(result, Some(None));"] #[doc = " let result = a.into_iter().rev().try_find(|&x| NonZero::new(x).map(|y| y.is_power_of_two()));"] #[doc = " assert_eq!(result, None);"] #[doc = " ```"] #[inline] #[unstable (feature = "try_find" , reason = "new API" , issue = "63178")] fn try_find < R > (& mut self , f : impl FnMut (& Self :: Item) -> R ,) -> ChangeOutputType < R , Option < Self :: Item > > where Self : Sized , R : Try < Output = bool , Residual : Residual < Option < Self :: Item > > > , { #[inline] fn check < I , V , R > (mut f : impl FnMut (& I) -> V ,) -> impl FnMut (() , I) -> ControlFlow < R :: TryType > where V : Try < Output = bool , Residual = R > , R : Residual < Option < I > > , { move | () , x | match f (& x) . branch () { ControlFlow :: Continue (false) => ControlFlow :: Continue (()) , ControlFlow :: Continue (true) => ControlFlow :: Break (Try :: from_output (Some (x))) , ControlFlow :: Break (r) => ControlFlow :: Break (FromResidual :: from_residual (r)) , } } match self . try_fold (() , check (f)) { ControlFlow :: Break (x) => x , ControlFlow :: Continue (()) => Try :: from_output (None) , } } #[doc = " Searches for an element in an iterator, returning its index."] #[doc = ""] #[doc = " `position()` takes a closure that returns `true` or `false`. It applies"] #[doc = " this closure to each element of the iterator, and if one of them"] #[doc = " returns `true`, then `position()` returns [`Some(index)`]. If all of"] #[doc = " them return `false`, it returns [`None`]."] #[doc = ""] #[doc = " `position()` is short-circuiting; in other words, it will stop"] #[doc = " processing as soon as it finds a `true`."] #[doc = ""] #[doc = " # Overflow Behavior"] #[doc = ""] #[doc = " The method does no guarding against overflows, so if there are more"] #[doc = " than [`usize::MAX`] non-matching elements, it either produces the wrong"] #[doc = " result or panics. If overflow checks are enabled, a panic is"] #[doc = " guaranteed."] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " This function might panic if the iterator has more than `usize::MAX`"] #[doc = " non-matching elements."] #[doc = ""] #[doc = " [`Some(index)`]: Some"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " assert_eq!(a.into_iter().position(|x| x == 2), Some(1));"] #[doc = ""] #[doc = " assert_eq!(a.into_iter().position(|x| x == 5), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Stopping at the first `true`:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3, 4];"] #[doc = ""] #[doc = " let mut iter = a.into_iter();"] #[doc = ""] #[doc = " assert_eq!(iter.position(|x| x >= 2), Some(1));"] #[doc = ""] #[doc = " // we can still use `iter`, as there are more elements."] #[doc = " assert_eq!(iter.next(), Some(3));"] #[doc = ""] #[doc = " // The returned index depends on iterator state"] #[doc = " assert_eq!(iter.position(|x| x == 4), Some(0));"] #[doc = ""] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn position < P > (& mut self , predicate : P) -> Option < usize > where Self : Sized , P : FnMut (Self :: Item) -> bool , { #[inline] fn check < 'a , T > (mut predicate : impl FnMut (T) -> bool + 'a , acc : & 'a mut usize ,) -> impl FnMut (() , T) -> ControlFlow < usize , () > + 'a { #[rustc_inherit_overflow_checks] move | _ , x | { if predicate (x) { ControlFlow :: Break (* acc) } else { * acc += 1 ; ControlFlow :: Continue (()) } } } let mut acc = 0 ; self . try_fold (() , check (predicate , & mut acc)) . break_value () } #[doc = " Searches for an element in an iterator from the right, returning its"] #[doc = " index."] #[doc = ""] #[doc = " `rposition()` takes a closure that returns `true` or `false`. It applies"] #[doc = " this closure to each element of the iterator, starting from the end,"] #[doc = " and if one of them returns `true`, then `rposition()` returns"] #[doc = " [`Some(index)`]. If all of them return `false`, it returns [`None`]."] #[doc = ""] #[doc = " `rposition()` is short-circuiting; in other words, it will stop"] #[doc = " processing as soon as it finds a `true`."] #[doc = ""] #[doc = " [`Some(index)`]: Some"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " assert_eq!(a.into_iter().rposition(|x| x == 3), Some(2));"] #[doc = ""] #[doc = " assert_eq!(a.into_iter().rposition(|x| x == 5), None);"] #[doc = " ```"] #[doc = ""] #[doc = " Stopping at the first `true`:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [-1, 2, 3, 4];"] #[doc = ""] #[doc = " let mut iter = a.into_iter();"] #[doc = ""] #[doc = " assert_eq!(iter.rposition(|x| x >= 2), Some(3));"] #[doc = ""] #[doc = " // we can still use `iter`, as there are more elements."] #[doc = " assert_eq!(iter.next(), Some(-1));"] #[doc = " assert_eq!(iter.next_back(), Some(3));"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn rposition < P > (& mut self , predicate : P) -> Option < usize > where P : FnMut (Self :: Item) -> bool , Self : Sized + ExactSizeIterator + DoubleEndedIterator , { #[inline] fn check < T > (mut predicate : impl FnMut (T) -> bool ,) -> impl FnMut (usize , T) -> ControlFlow < usize , usize > { move | i , x | { let i = i - 1 ; if predicate (x) { ControlFlow :: Break (i) } else { ControlFlow :: Continue (i) } } } let n = self . len () ; self . try_rfold (n , check (predicate)) . break_value () } #[doc = " Returns the maximum element of an iterator."] #[doc = ""] #[doc = " If several elements are equally maximum, the last element is"] #[doc = " returned. If the iterator is empty, [`None`] is returned."] #[doc = ""] #[doc = " Note that [`f32`]/[`f64`] doesn't implement [`Ord`] due to NaN being"] #[doc = " incomparable. You can work around this by using [`Iterator::reduce`]:"] #[doc = " ```"] #[doc = " assert_eq!("] #[doc = "     [2.4, f32::NAN, 1.3]"] #[doc = "         .into_iter()"] #[doc = "         .reduce(f32::max)"] #[doc = "         .unwrap_or(0.),"] #[doc = "     2.4"] #[doc = " );"] #[doc = " ```"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = " let b: [u32; 0] = [];"] #[doc = ""] #[doc = " assert_eq!(a.into_iter().max(), Some(3));"] #[doc = " assert_eq!(b.into_iter().max(), None);"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn max (self) -> Option < Self :: Item > where Self : Sized , Self :: Item : Ord , { self . max_by (Ord :: cmp) } #[doc = " Returns the minimum element of an iterator."] #[doc = ""] #[doc = " If several elements are equally minimum, the first element is returned."] #[doc = " If the iterator is empty, [`None`] is returned."] #[doc = ""] #[doc = " Note that [`f32`]/[`f64`] doesn't implement [`Ord`] due to NaN being"] #[doc = " incomparable. You can work around this by using [`Iterator::reduce`]:"] #[doc = " ```"] #[doc = " assert_eq!("] #[doc = "     [2.4, f32::NAN, 1.3]"] #[doc = "         .into_iter()"] #[doc = "         .reduce(f32::min)"] #[doc = "         .unwrap_or(0.),"] #[doc = "     1.3"] #[doc = " );"] #[doc = " ```"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = " let b: [u32; 0] = [];"] #[doc = ""] #[doc = " assert_eq!(a.into_iter().min(), Some(1));"] #[doc = " assert_eq!(b.into_iter().min(), None);"] #[doc = " ```"] #[inline] #[stable (feature = "rust1" , since = "1.0.0")] fn min (self) -> Option < Self :: Item > where Self : Sized , Self :: Item : Ord , { self . min_by (Ord :: cmp) } #[doc = " Returns the element that gives the maximum value from the"] #[doc = " specified function."] #[doc = ""] #[doc = " If several elements are equally maximum, the last element is"] #[doc = " returned. If the iterator is empty, [`None`] is returned."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [-3_i32, 0, 1, 5, -10];"] #[doc = " assert_eq!(a.into_iter().max_by_key(|x| x.abs()).unwrap(), -10);"] #[doc = " ```"] #[inline] #[stable (feature = "iter_cmp_by_key" , since = "1.6.0")] fn max_by_key < B : Ord , F > (self , f : F) -> Option < Self :: Item > where Self : Sized , F : FnMut (& Self :: Item) -> B , { #[inline] fn key < T , B > (mut f : impl FnMut (& T) -> B) -> impl FnMut (T) -> (B , T) { move | x | (f (& x) , x) } #[inline] fn compare < T , B : Ord > ((x_p , _) : & (B , T) , (y_p , _) : & (B , T)) -> Ordering { x_p . cmp (y_p) } let (_ , x) = self . map (key (f)) . max_by (compare) ? ; Some (x) } #[doc = " Returns the element that gives the maximum value with respect to the"] #[doc = " specified comparison function."] #[doc = ""] #[doc = " If several elements are equally maximum, the last element is"] #[doc = " returned. If the iterator is empty, [`None`] is returned."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [-3_i32, 0, 1, 5, -10];"] #[doc = " assert_eq!(a.into_iter().max_by(|x, y| x.cmp(y)).unwrap(), 5);"] #[doc = " ```"] #[inline] #[stable (feature = "iter_max_by" , since = "1.15.0")] fn max_by < F > (self , compare : F) -> Option < Self :: Item > where Self : Sized , F : FnMut (& Self :: Item , & Self :: Item) -> Ordering , { #[inline] fn fold < T > (mut compare : impl FnMut (& T , & T) -> Ordering) -> impl FnMut (T , T) -> T { move | x , y | cmp :: max_by (x , y , & mut compare) } self . reduce (fold (compare)) } #[doc = " Returns the element that gives the minimum value from the"] #[doc = " specified function."] #[doc = ""] #[doc = " If several elements are equally minimum, the first element is"] #[doc = " returned. If the iterator is empty, [`None`] is returned."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [-3_i32, 0, 1, 5, -10];"] #[doc = " assert_eq!(a.into_iter().min_by_key(|x| x.abs()).unwrap(), 0);"] #[doc = " ```"] #[inline] #[stable (feature = "iter_cmp_by_key" , since = "1.6.0")] fn min_by_key < B : Ord , F > (self , f : F) -> Option < Self :: Item > where Self : Sized , F : FnMut (& Self :: Item) -> B , { #[inline] fn key < T , B > (mut f : impl FnMut (& T) -> B) -> impl FnMut (T) -> (B , T) { move | x | (f (& x) , x) } #[inline] fn compare < T , B : Ord > ((x_p , _) : & (B , T) , (y_p , _) : & (B , T)) -> Ordering { x_p . cmp (y_p) } let (_ , x) = self . map (key (f)) . min_by (compare) ? ; Some (x) } #[doc = " Returns the element that gives the minimum value with respect to the"] #[doc = " specified comparison function."] #[doc = ""] #[doc = " If several elements are equally minimum, the first element is"] #[doc = " returned. If the iterator is empty, [`None`] is returned."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [-3_i32, 0, 1, 5, -10];"] #[doc = " assert_eq!(a.into_iter().min_by(|x, y| x.cmp(y)).unwrap(), -10);"] #[doc = " ```"] #[inline] #[stable (feature = "iter_min_by" , since = "1.15.0")] fn min_by < F > (self , compare : F) -> Option < Self :: Item > where Self : Sized , F : FnMut (& Self :: Item , & Self :: Item) -> Ordering , { #[inline] fn fold < T > (mut compare : impl FnMut (& T , & T) -> Ordering) -> impl FnMut (T , T) -> T { move | x , y | cmp :: min_by (x , y , & mut compare) } self . reduce (fold (compare)) } #[doc = " Reverses an iterator's direction."] #[doc = ""] #[doc = " Usually, iterators iterate from left to right. After using `rev()`,"] #[doc = " an iterator will instead iterate from right to left."] #[doc = ""] #[doc = " This is only possible if the iterator has an end, so `rev()` only"] #[doc = " works on [`DoubleEndedIterator`]s."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let mut iter = a.into_iter().rev();"] #[doc = ""] #[doc = " assert_eq!(iter.next(), Some(3));"] #[doc = " assert_eq!(iter.next(), Some(2));"] #[doc = " assert_eq!(iter.next(), Some(1));"] #[doc = ""] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[inline] #[doc (alias = "reverse")] #[stable (feature = "rust1" , since = "1.0.0")] fn rev (self) -> Rev < Self > where Self : Sized + DoubleEndedIterator , { Rev :: new (self) } #[doc = " Converts an iterator of pairs into a pair of containers."] #[doc = ""] #[doc = " `unzip()` consumes an entire iterator of pairs, producing two"] #[doc = " collections: one from the left elements of the pairs, and one"] #[doc = " from the right elements."] #[doc = ""] #[doc = " This function is, in some sense, the opposite of [`zip`]."] #[doc = ""] #[doc = " [`zip`]: Iterator::zip"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [(1, 2), (3, 4), (5, 6)];"] #[doc = ""] #[doc = " let (left, right): (Vec<_>, Vec<_>) = a.into_iter().unzip();"] #[doc = ""] #[doc = " assert_eq!(left, [1, 3, 5]);"] #[doc = " assert_eq!(right, [2, 4, 6]);"] #[doc = ""] #[doc = " // you can also unzip multiple nested tuples at once"] #[doc = " let a = [(1, (2, 3)), (4, (5, 6))];"] #[doc = ""] #[doc = " let (x, (y, z)): (Vec<_>, (Vec<_>, Vec<_>)) = a.into_iter().unzip();"] #[doc = " assert_eq!(x, [1, 4]);"] #[doc = " assert_eq!(y, [2, 5]);"] #[doc = " assert_eq!(z, [3, 6]);"] #[doc = " ```"] #[stable (feature = "rust1" , since = "1.0.0")] fn unzip < A , B , FromA , FromB > (self) -> (FromA , FromB) where FromA : Default + Extend < A > , FromB : Default + Extend < B > , Self : Sized + Iterator < Item = (A , B) > , { let mut unzipped : (FromA , FromB) = Default :: default () ; unzipped . extend (self) ; unzipped } #[doc = " Creates an iterator which copies all of its elements."] #[doc = ""] #[doc = " This is useful when you have an iterator over `&T`, but you need an"] #[doc = " iterator over `T`."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let v_copied: Vec<_> = a.iter().copied().collect();"] #[doc = ""] #[doc = " // copied is the same as .map(|&x| x)"] #[doc = " let v_map: Vec<_> = a.iter().map(|&x| x).collect();"] #[doc = ""] #[doc = " assert_eq!(v_copied, [1, 2, 3]);"] #[doc = " assert_eq!(v_map, [1, 2, 3]);"] #[doc = " ```"] #[stable (feature = "iter_copied" , since = "1.36.0")] #[rustc_diagnostic_item = "iter_copied"] fn copied < 'a , T > (self) -> Copied < Self > where T : Copy + 'a , Self : Sized + Iterator < Item = & 'a T > , { Copied :: new (self) } #[doc = " Creates an iterator which [`clone`]s all of its elements."] #[doc = ""] #[doc = " This is useful when you have an iterator over `&T`, but you need an"] #[doc = " iterator over `T`."] #[doc = ""] #[doc = " There is no guarantee whatsoever about the `clone` method actually"] #[doc = " being called *or* optimized away. So code should not depend on"] #[doc = " either."] #[doc = ""] #[doc = " [`clone`]: Clone::clone"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let v_cloned: Vec<_> = a.iter().cloned().collect();"] #[doc = ""] #[doc = " // cloned is the same as .map(|&x| x), for integers"] #[doc = " let v_map: Vec<_> = a.iter().map(|&x| x).collect();"] #[doc = ""] #[doc = " assert_eq!(v_cloned, [1, 2, 3]);"] #[doc = " assert_eq!(v_map, [1, 2, 3]);"] #[doc = " ```"] #[doc = ""] #[doc = " To get the best performance, try to clone late:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [vec![0_u8, 1, 2], vec![3, 4], vec![23]];"] #[doc = " // don't do this:"] #[doc = " let slower: Vec<_> = a.iter().cloned().filter(|s| s.len() == 1).collect();"] #[doc = " assert_eq!(&[vec![23]], &slower[..]);"] #[doc = " // instead call `cloned` late"] #[doc = " let faster: Vec<_> = a.iter().filter(|s| s.len() == 1).cloned().collect();"] #[doc = " assert_eq!(&[vec![23]], &faster[..]);"] #[doc = " ```"] #[stable (feature = "rust1" , since = "1.0.0")] #[rustc_diagnostic_item = "iter_cloned"] fn cloned < 'a , T > (self) -> Cloned < Self > where T : Clone + 'a , Self : Sized + Iterator < Item = & 'a T > , { Cloned :: new (self) } #[doc = " Repeats an iterator endlessly."] #[doc = ""] #[doc = " Instead of stopping at [`None`], the iterator will instead start again,"] #[doc = " from the beginning. After iterating again, it will start at the"] #[doc = " beginning again. And again. And again. Forever. Note that in case the"] #[doc = " original iterator is empty, the resulting iterator will also be empty."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = ""] #[doc = " let mut iter = a.into_iter().cycle();"] #[doc = ""] #[doc = " loop {"] #[doc = "     assert_eq!(iter.next(), Some(1));"] #[doc = "     assert_eq!(iter.next(), Some(2));"] #[doc = "     assert_eq!(iter.next(), Some(3));"] #[doc = " #   break;"] #[doc = " }"] #[doc = " ```"] #[stable (feature = "rust1" , since = "1.0.0")] #[inline] fn cycle (self) -> Cycle < Self > where Self : Sized + Clone , { Cycle :: new (self) } #[doc = " Returns an iterator over `N` elements of the iterator at a time."] #[doc = ""] #[doc = " The chunks do not overlap. If `N` does not divide the length of the"] #[doc = " iterator, then the last up to `N-1` elements will be omitted and can be"] #[doc = " retrieved from the [`.into_remainder()`][ArrayChunks::into_remainder]"] #[doc = " function of the iterator."] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " Panics if `N` is zero."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_array_chunks)]"] #[doc = ""] #[doc = " let mut iter = \"lorem\".chars().array_chunks();"] #[doc = " assert_eq!(iter.next(), Some(['l', 'o']));"] #[doc = " assert_eq!(iter.next(), Some(['r', 'e']));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " assert_eq!(iter.into_remainder().unwrap().as_slice(), &['m']);"] #[doc = " ```"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_array_chunks)]"] #[doc = ""] #[doc = " let data = [1, 1, 2, -2, 6, 0, 3, 1];"] #[doc = " //          ^-----^  ^------^"] #[doc = " for [x, y, z] in data.iter().array_chunks() {"] #[doc = "     assert_eq!(x + y + z, 4);"] #[doc = " }"] #[doc = " ```"] #[track_caller] #[unstable (feature = "iter_array_chunks" , reason = "recently added" , issue = "100450")] fn array_chunks < const N : usize > (self) -> ArrayChunks < Self , N > where Self : Sized , { ArrayChunks :: new (self) } #[doc = " Sums the elements of an iterator."] #[doc = ""] #[doc = " Takes each element, adds them together, and returns the result."] #[doc = ""] #[doc = " An empty iterator returns the *additive identity* (\"zero\") of the type,"] #[doc = " which is `0` for integers and `-0.0` for floats."] #[doc = ""] #[doc = " `sum()` can be used to sum any type implementing [`Sum`][`core::iter::Sum`],"] #[doc = " including [`Option`][`Option::sum`] and [`Result`][`Result::sum`]."] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " When calling `sum()` and a primitive integer type is being returned, this"] #[doc = " method will panic if the computation overflows and overflow checks are"] #[doc = " enabled."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [1, 2, 3];"] #[doc = " let sum: i32 = a.iter().sum();"] #[doc = ""] #[doc = " assert_eq!(sum, 6);"] #[doc = ""] #[doc = " let b: Vec<f32> = vec![];"] #[doc = " let sum: f32 = b.iter().sum();"] #[doc = " assert_eq!(sum, -0.0_f32);"] #[doc = " ```"] #[stable (feature = "iter_arith" , since = "1.11.0")] fn sum < S > (self) -> S where Self : Sized , S : Sum < Self :: Item > , { Sum :: sum (self) } #[doc = " Iterates over the entire iterator, multiplying all the elements"] #[doc = ""] #[doc = " An empty iterator returns the one value of the type."] #[doc = ""] #[doc = " `product()` can be used to multiply any type implementing [`Product`][`core::iter::Product`],"] #[doc = " including [`Option`][`Option::product`] and [`Result`][`Result::product`]."] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " When calling `product()` and a primitive integer type is being returned,"] #[doc = " method will panic if the computation overflows and overflow checks are"] #[doc = " enabled."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " fn factorial(n: u32) -> u32 {"] #[doc = "     (1..=n).product()"] #[doc = " }"] #[doc = " assert_eq!(factorial(0), 1);"] #[doc = " assert_eq!(factorial(1), 1);"] #[doc = " assert_eq!(factorial(5), 120);"] #[doc = " ```"] #[stable (feature = "iter_arith" , since = "1.11.0")] fn product < P > (self) -> P where Self : Sized , P : Product < Self :: Item > , { Product :: product (self) } #[doc = " [Lexicographically](Ord#lexicographical-comparison) compares the elements of this [`Iterator`] with those"] #[doc = " of another."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " use std::cmp::Ordering;"] #[doc = ""] #[doc = " assert_eq!([1].iter().cmp([1].iter()), Ordering::Equal);"] #[doc = " assert_eq!([1].iter().cmp([1, 2].iter()), Ordering::Less);"] #[doc = " assert_eq!([1, 2].iter().cmp([1].iter()), Ordering::Greater);"] #[doc = " ```"] #[stable (feature = "iter_order" , since = "1.5.0")] fn cmp < I > (self , other : I) -> Ordering where I : IntoIterator < Item = Self :: Item > , Self :: Item : Ord , Self : Sized , { self . cmp_by (other , | x , y | x . cmp (& y)) } #[doc = " [Lexicographically](Ord#lexicographical-comparison) compares the elements of this [`Iterator`] with those"] #[doc = " of another with respect to the specified comparison function."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_order_by)]"] #[doc = ""] #[doc = " use std::cmp::Ordering;"] #[doc = ""] #[doc = " let xs = [1, 2, 3, 4];"] #[doc = " let ys = [1, 4, 9, 16];"] #[doc = ""] #[doc = " assert_eq!(xs.into_iter().cmp_by(ys, |x, y| x.cmp(&y)), Ordering::Less);"] #[doc = " assert_eq!(xs.into_iter().cmp_by(ys, |x, y| (x * x).cmp(&y)), Ordering::Equal);"] #[doc = " assert_eq!(xs.into_iter().cmp_by(ys, |x, y| (2 * x).cmp(&y)), Ordering::Greater);"] #[doc = " ```"] #[unstable (feature = "iter_order_by" , issue = "64295")] fn cmp_by < I , F > (self , other : I , cmp : F) -> Ordering where Self : Sized , I : IntoIterator , F : FnMut (Self :: Item , I :: Item) -> Ordering , { #[inline] fn compare < X , Y , F > (mut cmp : F) -> impl FnMut (X , Y) -> ControlFlow < Ordering > where F : FnMut (X , Y) -> Ordering , { move | x , y | match cmp (x , y) { Ordering :: Equal => ControlFlow :: Continue (()) , non_eq => ControlFlow :: Break (non_eq) , } } match iter_compare (self , other . into_iter () , compare (cmp)) { ControlFlow :: Continue (ord) => ord , ControlFlow :: Break (ord) => ord , } } #[doc = " [Lexicographically](Ord#lexicographical-comparison) compares the [`PartialOrd`] elements of"] #[doc = " this [`Iterator`] with those of another. The comparison works like short-circuit"] #[doc = " evaluation, returning a result without comparing the remaining elements."] #[doc = " As soon as an order can be determined, the evaluation stops and a result is returned."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " use std::cmp::Ordering;"] #[doc = ""] #[doc = " assert_eq!([1.].iter().partial_cmp([1.].iter()), Some(Ordering::Equal));"] #[doc = " assert_eq!([1.].iter().partial_cmp([1., 2.].iter()), Some(Ordering::Less));"] #[doc = " assert_eq!([1., 2.].iter().partial_cmp([1.].iter()), Some(Ordering::Greater));"] #[doc = " ```"] #[doc = ""] #[doc = " For floating-point numbers, NaN does not have a total order and will result"] #[doc = " in `None` when compared:"] #[doc = ""] #[doc = " ```"] #[doc = " assert_eq!([f64::NAN].iter().partial_cmp([1.].iter()), None);"] #[doc = " ```"] #[doc = ""] #[doc = " The results are determined by the order of evaluation."] #[doc = ""] #[doc = " ```"] #[doc = " use std::cmp::Ordering;"] #[doc = ""] #[doc = " assert_eq!([1.0, f64::NAN].iter().partial_cmp([2.0, f64::NAN].iter()), Some(Ordering::Less));"] #[doc = " assert_eq!([2.0, f64::NAN].iter().partial_cmp([1.0, f64::NAN].iter()), Some(Ordering::Greater));"] #[doc = " assert_eq!([f64::NAN, 1.0].iter().partial_cmp([f64::NAN, 2.0].iter()), None);"] #[doc = " ```"] #[doc = ""] #[stable (feature = "iter_order" , since = "1.5.0")] fn partial_cmp < I > (self , other : I) -> Option < Ordering > where I : IntoIterator , Self :: Item : PartialOrd < I :: Item > , Self : Sized , { self . partial_cmp_by (other , | x , y | x . partial_cmp (& y)) } #[doc = " [Lexicographically](Ord#lexicographical-comparison) compares the elements of this [`Iterator`] with those"] #[doc = " of another with respect to the specified comparison function."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_order_by)]"] #[doc = ""] #[doc = " use std::cmp::Ordering;"] #[doc = ""] #[doc = " let xs = [1.0, 2.0, 3.0, 4.0];"] #[doc = " let ys = [1.0, 4.0, 9.0, 16.0];"] #[doc = ""] #[doc = " assert_eq!("] #[doc = "     xs.iter().partial_cmp_by(ys, |x, y| x.partial_cmp(&y)),"] #[doc = "     Some(Ordering::Less)"] #[doc = " );"] #[doc = " assert_eq!("] #[doc = "     xs.iter().partial_cmp_by(ys, |x, y| (x * x).partial_cmp(&y)),"] #[doc = "     Some(Ordering::Equal)"] #[doc = " );"] #[doc = " assert_eq!("] #[doc = "     xs.iter().partial_cmp_by(ys, |x, y| (2.0 * x).partial_cmp(&y)),"] #[doc = "     Some(Ordering::Greater)"] #[doc = " );"] #[doc = " ```"] #[unstable (feature = "iter_order_by" , issue = "64295")] fn partial_cmp_by < I , F > (self , other : I , partial_cmp : F) -> Option < Ordering > where Self : Sized , I : IntoIterator , F : FnMut (Self :: Item , I :: Item) -> Option < Ordering > , { #[inline] fn compare < X , Y , F > (mut partial_cmp : F) -> impl FnMut (X , Y) -> ControlFlow < Option < Ordering > > where F : FnMut (X , Y) -> Option < Ordering > , { move | x , y | match partial_cmp (x , y) { Some (Ordering :: Equal) => ControlFlow :: Continue (()) , non_eq => ControlFlow :: Break (non_eq) , } } match iter_compare (self , other . into_iter () , compare (partial_cmp)) { ControlFlow :: Continue (ord) => Some (ord) , ControlFlow :: Break (ord) => ord , } } #[doc = " Determines if the elements of this [`Iterator`] are equal to those of"] #[doc = " another."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " assert_eq!([1].iter().eq([1].iter()), true);"] #[doc = " assert_eq!([1].iter().eq([1, 2].iter()), false);"] #[doc = " ```"] #[stable (feature = "iter_order" , since = "1.5.0")] fn eq < I > (self , other : I) -> bool where I : IntoIterator , Self :: Item : PartialEq < I :: Item > , Self : Sized , { self . eq_by (other , | x , y | x == y) } #[doc = " Determines if the elements of this [`Iterator`] are equal to those of"] #[doc = " another with respect to the specified equality function."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(iter_order_by)]"] #[doc = ""] #[doc = " let xs = [1, 2, 3, 4];"] #[doc = " let ys = [1, 4, 9, 16];"] #[doc = ""] #[doc = " assert!(xs.iter().eq_by(ys, |x, y| x * x == y));"] #[doc = " ```"] #[unstable (feature = "iter_order_by" , issue = "64295")] fn eq_by < I , F > (self , other : I , eq : F) -> bool where Self : Sized , I : IntoIterator , F : FnMut (Self :: Item , I :: Item) -> bool , { #[inline] fn compare < X , Y , F > (mut eq : F) -> impl FnMut (X , Y) -> ControlFlow < () > where F : FnMut (X , Y) -> bool , { move | x , y | { if eq (x , y) { ControlFlow :: Continue (()) } else { ControlFlow :: Break (()) } } } match iter_compare (self , other . into_iter () , compare (eq)) { ControlFlow :: Continue (ord) => ord == Ordering :: Equal , ControlFlow :: Break (()) => false , } } #[doc = " Determines if the elements of this [`Iterator`] are not equal to those of"] #[doc = " another."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " assert_eq!([1].iter().ne([1].iter()), false);"] #[doc = " assert_eq!([1].iter().ne([1, 2].iter()), true);"] #[doc = " ```"] #[stable (feature = "iter_order" , since = "1.5.0")] fn ne < I > (self , other : I) -> bool where I : IntoIterator , Self :: Item : PartialEq < I :: Item > , Self : Sized , { ! self . eq (other) } #[doc = " Determines if the elements of this [`Iterator`] are [lexicographically](Ord#lexicographical-comparison)"] #[doc = " less than those of another."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " assert_eq!([1].iter().lt([1].iter()), false);"] #[doc = " assert_eq!([1].iter().lt([1, 2].iter()), true);"] #[doc = " assert_eq!([1, 2].iter().lt([1].iter()), false);"] #[doc = " assert_eq!([1, 2].iter().lt([1, 2].iter()), false);"] #[doc = " ```"] #[stable (feature = "iter_order" , since = "1.5.0")] fn lt < I > (self , other : I) -> bool where I : IntoIterator , Self :: Item : PartialOrd < I :: Item > , Self : Sized , { self . partial_cmp (other) == Some (Ordering :: Less) } #[doc = " Determines if the elements of this [`Iterator`] are [lexicographically](Ord#lexicographical-comparison)"] #[doc = " less or equal to those of another."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " assert_eq!([1].iter().le([1].iter()), true);"] #[doc = " assert_eq!([1].iter().le([1, 2].iter()), true);"] #[doc = " assert_eq!([1, 2].iter().le([1].iter()), false);"] #[doc = " assert_eq!([1, 2].iter().le([1, 2].iter()), true);"] #[doc = " ```"] #[stable (feature = "iter_order" , since = "1.5.0")] fn le < I > (self , other : I) -> bool where I : IntoIterator , Self :: Item : PartialOrd < I :: Item > , Self : Sized , { matches ! (self . partial_cmp (other) , Some (Ordering :: Less | Ordering :: Equal)) } #[doc = " Determines if the elements of this [`Iterator`] are [lexicographically](Ord#lexicographical-comparison)"] #[doc = " greater than those of another."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " assert_eq!([1].iter().gt([1].iter()), false);"] #[doc = " assert_eq!([1].iter().gt([1, 2].iter()), false);"] #[doc = " assert_eq!([1, 2].iter().gt([1].iter()), true);"] #[doc = " assert_eq!([1, 2].iter().gt([1, 2].iter()), false);"] #[doc = " ```"] #[stable (feature = "iter_order" , since = "1.5.0")] fn gt < I > (self , other : I) -> bool where I : IntoIterator , Self :: Item : PartialOrd < I :: Item > , Self : Sized , { self . partial_cmp (other) == Some (Ordering :: Greater) } #[doc = " Determines if the elements of this [`Iterator`] are [lexicographically](Ord#lexicographical-comparison)"] #[doc = " greater than or equal to those of another."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " assert_eq!([1].iter().ge([1].iter()), true);"] #[doc = " assert_eq!([1].iter().ge([1, 2].iter()), false);"] #[doc = " assert_eq!([1, 2].iter().ge([1].iter()), true);"] #[doc = " assert_eq!([1, 2].iter().ge([1, 2].iter()), true);"] #[doc = " ```"] #[stable (feature = "iter_order" , since = "1.5.0")] fn ge < I > (self , other : I) -> bool where I : IntoIterator , Self :: Item : PartialOrd < I :: Item > , Self : Sized , { matches ! (self . partial_cmp (other) , Some (Ordering :: Greater | Ordering :: Equal)) } #[doc = " Checks if the elements of this iterator are sorted."] #[doc = ""] #[doc = " That is, for each element `a` and its following element `b`, `a <= b` must hold. If the"] #[doc = " iterator yields exactly zero or one element, `true` is returned."] #[doc = ""] #[doc = " Note that if `Self::Item` is only `PartialOrd`, but not `Ord`, the above definition"] #[doc = " implies that this function returns `false` if any two consecutive items are not"] #[doc = " comparable."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " assert!([1, 2, 2, 9].iter().is_sorted());"] #[doc = " assert!(![1, 3, 2, 4].iter().is_sorted());"] #[doc = " assert!([0].iter().is_sorted());"] #[doc = " assert!(std::iter::empty::<i32>().is_sorted());"] #[doc = " assert!(![0.0, 1.0, f32::NAN].iter().is_sorted());"] #[doc = " ```"] #[inline] #[stable (feature = "is_sorted" , since = "1.82.0")] fn is_sorted (self) -> bool where Self : Sized , Self :: Item : PartialOrd , { self . is_sorted_by (| a , b | a <= b) } #[doc = " Checks if the elements of this iterator are sorted using the given comparator function."] #[doc = ""] #[doc = " Instead of using `PartialOrd::partial_cmp`, this function uses the given `compare`"] #[doc = " function to determine whether two elements are to be considered in sorted order."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " assert!([1, 2, 2, 9].iter().is_sorted_by(|a, b| a <= b));"] #[doc = " assert!(![1, 2, 2, 9].iter().is_sorted_by(|a, b| a < b));"] #[doc = ""] #[doc = " assert!([0].iter().is_sorted_by(|a, b| true));"] #[doc = " assert!([0].iter().is_sorted_by(|a, b| false));"] #[doc = ""] #[doc = " assert!(std::iter::empty::<i32>().is_sorted_by(|a, b| false));"] #[doc = " assert!(std::iter::empty::<i32>().is_sorted_by(|a, b| true));"] #[doc = " ```"] #[stable (feature = "is_sorted" , since = "1.82.0")] fn is_sorted_by < F > (mut self , compare : F) -> bool where Self : Sized , F : FnMut (& Self :: Item , & Self :: Item) -> bool , { #[inline] fn check < 'a , T > (last : & 'a mut T , mut compare : impl FnMut (& T , & T) -> bool + 'a ,) -> impl FnMut (T) -> bool + 'a { move | curr | { if ! compare (& last , & curr) { return false ; } * last = curr ; true } } let mut last = match self . next () { Some (e) => e , None => return true , } ; self . all (check (& mut last , compare)) } #[doc = " Checks if the elements of this iterator are sorted using the given key extraction"] #[doc = " function."] #[doc = ""] #[doc = " Instead of comparing the iterator's elements directly, this function compares the keys of"] #[doc = " the elements, as determined by `f`. Apart from that, it's equivalent to [`is_sorted`]; see"] #[doc = " its documentation for more information."] #[doc = ""] #[doc = " [`is_sorted`]: Iterator::is_sorted"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " assert!([\"c\", \"bb\", \"aaa\"].iter().is_sorted_by_key(|s| s.len()));"] #[doc = " assert!(![-2i32, -1, 0, 3].iter().is_sorted_by_key(|n| n.abs()));"] #[doc = " ```"] #[inline] #[stable (feature = "is_sorted" , since = "1.82.0")] fn is_sorted_by_key < F , K > (self , f : F) -> bool where Self : Sized , F : FnMut (Self :: Item) -> K , K : PartialOrd , { self . map (f) . is_sorted () } #[doc = " See [TrustedRandomAccess][super::super::TrustedRandomAccess]"] #[inline] #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe fn __iterator_get_unchecked (& mut self , _idx : usize) -> Self :: Item where Self : TrustedRandomAccessNoCoerce , { unreachable ! ("Always specialized") ; } }}}

macro_rules! iter_compare_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function iter_compare in module {}", module_path!());
    };
}

mkfn!{
    iter_compare_introspect!();
    #[doc = " Compares two iterators element-wise using the given function."] #[doc = ""] #[doc = " If `ControlFlow::Continue(())` is returned from the function, the comparison moves on to the next"] #[doc = " elements of both iterators. Returning `ControlFlow::Break(x)` short-circuits the iteration and"] #[doc = " returns `ControlFlow::Break(x)`. If one of the iterators runs out of elements,"] #[doc = " `ControlFlow::Continue(ord)` is returned where `ord` is the result of comparing the lengths of"] #[doc = " the iterators."] #[doc = ""] #[doc = " Isolates the logic shared by ['cmp_by'](Iterator::cmp_by),"] #[doc = " ['partial_cmp_by'](Iterator::partial_cmp_by), and ['eq_by'](Iterator::eq_by)."] #[inline] fn iter_compare < A , B , F , T > (mut a : A , mut b : B , f : F) -> ControlFlow < T , Ordering > where A : Iterator , B : Iterator , F : FnMut (A :: Item , B :: Item) -> ControlFlow < T > , { #[inline] fn compare < 'a , B , X , T > (b : & 'a mut B , mut f : impl FnMut (X , B :: Item) -> ControlFlow < T > + 'a ,) -> impl FnMut (X) -> ControlFlow < ControlFlow < T , Ordering > > + 'a where B : Iterator , { move | x | match b . next () { None => ControlFlow :: Break (ControlFlow :: Continue (Ordering :: Greater)) , Some (y) => f (x , y) . map_break (ControlFlow :: Break) , } } match a . try_for_each (compare (& mut b , f)) { ControlFlow :: Continue (()) => ControlFlow :: Continue (match b . next () { None => Ordering :: Equal , Some (_) => Ordering :: Less , }) , ControlFlow :: Break (x) => x , } }
}
mkitem!{mkimpl!{#[doc = " Implements `Iterator` for mutable references to iterators, such as those produced by [`Iterator::by_ref`]."] #[doc = ""] #[doc = " This implementation passes all method calls on to the original iterator."] #[stable (feature = "rust1" , since = "1.0.0")] impl < I : Iterator + ? Sized > Iterator for & mut I { type Item = I :: Item ; #[inline] fn next (& mut self) -> Option < I :: Item > { (* * self) . next () } fn size_hint (& self) -> (usize , Option < usize >) { (* * self) . size_hint () } fn advance_by (& mut self , n : usize) -> Result < () , NonZero < usize > > { (* * self) . advance_by (n) } fn nth (& mut self , n : usize) -> Option < Self :: Item > { (* * self) . nth (n) } fn fold < B , F > (self , init : B , f : F) -> B where F : FnMut (B , Self :: Item) -> B , { self . spec_fold (init , f) } fn try_fold < B , F , R > (& mut self , init : B , f : F) -> R where F : FnMut (B , Self :: Item) -> R , R : Try < Output = B > , { self . spec_try_fold (init , f) } }}}
mkitem!{mktrait!{#[doc = " Helper trait to specialize `fold` and `try_fold` for `&mut I where I: Sized`"] trait IteratorRefSpec : Iterator { fn spec_fold < B , F > (self , init : B , f : F) -> B where F : FnMut (B , Self :: Item) -> B ; fn spec_try_fold < B , F , R > (& mut self , init : B , f : F) -> R where F : FnMut (B , Self :: Item) -> R , R : Try < Output = B > ; }}}
mkitem!{mkimpl!{impl < I : Iterator + ? Sized > IteratorRefSpec for & mut I { default fn spec_fold < B , F > (self , init : B , mut f : F) -> B where F : FnMut (B , Self :: Item) -> B , { let mut accum = init ; while let Some (x) = self . next () { accum = f (accum , x) ; } accum } default fn spec_try_fold < B , F , R > (& mut self , init : B , mut f : F) -> R where F : FnMut (B , Self :: Item) -> R , R : Try < Output = B > , { let mut accum = init ; while let Some (x) = self . next () { accum = f (accum , x) ? ; } try { accum } } }}}
mkitem!{mkimpl!{impl < I : Iterator > IteratorRefSpec for & mut I { impl_fold_via_try_fold ! { spec_fold -> spec_try_fold } fn spec_try_fold < B , F , R > (& mut self , init : B , f : F) -> R where F : FnMut (B , Self :: Item) -> R , R : Try < Output = B > , { (* * self) . try_fold (init , f) } }}}