mkuse!{use std :: sync :: Mutex ;}
mkuse!{use std :: collections :: HashMap ;}
mkuse!{use std :: sync :: LazyLock ;}
mkitem!{static USE_MATRIX : LazyLock < Mutex < HashMap < String , Vec < String > > > > = LazyLock :: new (| | Mutex :: new (HashMap :: new ())) ;}

macro_rules! get_use_matrix_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function get_use_matrix in module {}", module_path!());
    };
}

mkfn!{
    get_use_matrix_introspect!();
    pub fn get_use_matrix () -> HashMap < String , Vec < String > > { USE_MATRIX . lock () . unwrap () . clone () }
}
mkitem!{macro_rules ! emit_message { ($ ($ arg : tt) *) => { } ; }}
mkitem!{macro_rules ! mkfn { ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_trait_bounds_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_trait_bounds_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: trait_bounds_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: trait_bounds_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn catch_fatal_errors < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn catch_fatal_errors < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: catch_fatal_errors_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: catch_fatal_errors_specific") ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn parse_crate_attrs < $ lifetime : lifetime > ($ ($ param : tt) *) -> PResult < $ lifetime2 : lifetime , ast :: AttrVec > $ body : block) => { $ (# [$ attr]) * fn parse_crate_attrs < $ lifetime > ($ ($ param) *) -> PResult < $ lifetime2 , ast :: AttrVec > { $ introspect ; emit_message ! ("🚀 MARKER: parse_crate_attrs_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: parse_crate_attrs_specific") ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn init_logger_with_additional_layer < F , T > ($ ($ param : tt) *) where F : FnOnce () -> T , T : rustc_log :: BuildSubscriberRet , $ body : block) => { $ (# [$ attr]) * fn init_logger_with_additional_layer < F , T > ($ ($ param) *) where F : FnOnce () -> T , T : rustc_log :: BuildSubscriberRet , { $ introspect ; emit_message ! ("🚀 MARKER: init_logger_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: init_logger_specific") ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < F , T > ($ ($ param : tt) *) $ (-> $ ret : ty) ? where F : FnOnce ($ ($ fnonce_args : tt) *) $ ($ where_rest : tt) * $ body : block) => { $ (# [$ attr]) * fn $ name < F , T > ($ ($ param) *) $ (-> $ ret) ? where F : FnOnce ($ ($ fnonce_args) *) $ ($ where_rest) * { $ introspect ; emit_message ! ("🚀 MARKER: two_generics_where_fnonce - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics_where_fnonce - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? where $ ($ where_clause : tt) * $ body : block) => { $ (# [$ attr]) * fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? where $ ($ where_clause) * { $ introspect ; emit_message ! ("🚀 MARKER: two_generics_where - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics_where - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: two_generics - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_two_generics - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_two_generics - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub ($ vis : ident) fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub ($ vis) fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_vis - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_vis - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < $ gen : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name < $ gen > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: generic_single - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: generic_single - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident < $ gen : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name < $ gen > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_generic_single - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_generic_single - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < $ lifetime : lifetime > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name < $ lifetime > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: lifetime - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: lifetime - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident < $ lifetime : lifetime > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name < $ lifetime > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_lifetime - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_lifetime - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_non_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_non_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: non_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: non_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; fn $ name : ident () $ body : block) => { fn $ name () { $ introspect ; emit_message ! ("🚀 MARKER: simple - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: simple - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ ($ anything : tt) *) => { $ ($ anything) * } ; }}
mkitem!{macro_rules ! safe_println { ($ ($ arg : tt) *) => { () } ; }}
mkitem!{macro_rules ! safe_print { ($ ($ arg : tt) *) => { () } ; }}
mkitem!{# [macro_export] macro_rules ! include_rust_compiler { ($ crate_name : literal , $ subpath : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_" , $ subpath , "_" , $ file , ".rs")) ; } ; ($ crate_name : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_src_" , $ file , ".rs")) ; } ; ($ crate_name : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_src_lib.rs")) ; } ; }}
mkitem!{# [macro_export] macro_rules ! include_rust_library { ($ lib_name : literal , $ subpath : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_" , $ subpath , "_" , $ file , ".rs")) ; } ; ($ lib_name : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_src_" , $ file , ".rs")) ; } ; ($ lib_name : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_src_lib.rs")) ; } ; }}
mkitem!{# [macro_export] macro_rules ! include_processed { ($ path : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_" , $ path , ".rs")) ; } ; }}
mkitem!{macro_rules ! mkinclude { ($ path : ident) => { } ; ($ path : literal) => { include ! ($ path) } ; }}
mkitem!{macro_rules ! mkitem { (include ! ($ path : ident) ;) => { } ; ($ macro_name : ident :: $ macro_sub : ident ! { $ string_lit : literal }) => { $ macro_name :: $ macro_sub ! { $ string_lit } } ; ($ macro_name : ident :: $ macro_sub : ident ! { $ ($ args : tt) * }) => { $ macro_name :: $ macro_sub ! { $ ($ args) * } } ; ($ macro_name : ident ! { $ ($ args : tt) * }) => { $ macro_name ! { $ ($ args) * } } ; ($ item : item) => { $ item } ; }}
mkitem!{# [macro_export] macro_rules ! mkmod { ($ name : ident , { $ ($ content : tt) * }) => { compile_error ! (concat ! ("MOD|" , module_path ! () , "|" , stringify ! ($ name))) ; mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; (pub mod $ name : ident { $ ($ content : tt) * }) => { compile_error ! (concat ! ("MOD|" , module_path ! () , "|" , stringify ! ($ name))) ; pub mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; (mod $ name : ident { $ ($ content : tt) * }) => { compile_error ! (concat ! ("MOD|" , module_path ! () , "|" , stringify ! ($ name))) ; mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; }}
mkitem!{# [macro_export] macro_rules ! mkuse { ($ use_stmt : item) => { compile_error ! (concat ! ("USE|" , module_path ! () , "|" , stringify ! ($ use_stmt))) ; } ; }}
mkitem!{macro_rules ! mkstruct { ($ struct_def : item) => { $ struct_def } ; }}
mkitem!{macro_rules ! mkenum { ($ enum_def : item) => { $ enum_def } ; }}
mkitem!{macro_rules ! mktrait { ($ trait_def : item) => { $ trait_def } ; }}
mkitem!{macro_rules ! mkimpl { ($ impl_def : item) => { $ impl_def } ; }}
mkitem!{macro_rules ! getname { ($ name : ident) => { stringify ! ($ name) } ; }}
mkitem!{macro_rules ! getsrc { ($ name : ident) => { "processed file" } ; }}
mkitem!{macro_rules ! getpath { ($ name : ident) => { "processed_path" } ; }}
mkitem!{macro_rules ! get_deps { ($ name : ident) => { vec ! [] } ; }}
mkmod!{rustc_complete, { 
                getname!(rustc_complete);
                getsrc!(rustc_complete);
                getpath!(rustc_complete);
                get_deps!(rustc_complete);
                get_crates!(rustc_complete);
                mkinclude!(rustc_complete);
                mkmod!{emitter, { 
                getname!(emitter);
                getsrc!(emitter);
                getpath!(emitter);
                get_deps!(emitter);
                get_crates!(emitter);
                mkinclude!(emitter);
                
macro_rules! stderr_destination_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function stderr_destination in module {}", module_path!());
    };
}

mkfn!{
    stderr_destination_introspect!();
    pub fn stderr_destination () { }
} 
            }}
mkmod!{registry, { 
                getname!(registry);
                getsrc!(registry);
                getpath!(registry);
                get_deps!(registry);
                get_crates!(registry);
                mkinclude!(registry);
                mkitem!{mkstruct!{pub struct Registry ;}} 
            }}
mkmod!{translation, { 
                getname!(translation);
                getsrc!(translation);
                getpath!(translation);
                get_deps!(translation);
                get_crates!(translation);
                mkinclude!(translation);
                mkitem!{mkstruct!{pub struct Translator ;}} 
            }}
mkitem!{mkstruct!{pub struct ColorConfig ;}}
mkitem!{mkstruct!{pub struct DiagCtxt ;}}
mkitem!{mkstruct!{pub struct ErrCode ;}}
mkitem!{mkstruct!{pub struct FatalError ;}}
mkitem!{mkstruct!{pub struct PResult < T > (pub T) ;}}
mkmod!{markdown, { 
                getname!(markdown);
                getsrc!(markdown);
                getpath!(markdown);
                get_deps!(markdown);
                get_crates!(markdown);
                mkinclude!(markdown);
                 
            }}
mkmod!{config, { 
                getname!(config);
                getsrc!(config);
                getpath!(config);
                get_deps!(config);
                get_crates!(config);
                mkinclude!(config);
                mkitem!{mkstruct!{pub struct CG_OPTIONS ;}}
mkitem!{mkstruct!{pub struct CrateType ;}}
mkitem!{mkstruct!{pub struct ErrorOutputType ;}}
mkitem!{mkstruct!{pub struct Input ;}}
mkitem!{mkstruct!{pub struct OptionDesc ;}}
mkitem!{mkstruct!{pub struct OutFileName ;}}
mkitem!{mkstruct!{pub struct OutputType ;}}
mkitem!{mkstruct!{pub struct Sysroot ;}}
mkitem!{mkstruct!{pub struct UnstableOptions ;}}
mkitem!{mkstruct!{pub struct Z_OPTIONS ;}}

macro_rules! nightly_options_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function nightly_options in module {}", module_path!());
    };
}

mkfn!{
    nightly_options_introspect!();
    pub fn nightly_options () { }
}

macro_rules! parse_target_triple_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function parse_target_triple in module {}", module_path!());
    };
}

mkfn!{
    parse_target_triple_introspect!();
    pub fn parse_target_triple () { }
} 
            }}
mkmod!{getopts, { 
                getname!(getopts);
                getsrc!(getopts);
                getpath!(getopts);
                get_deps!(getopts);
                get_crates!(getopts);
                mkinclude!(getopts);
                mkitem!{mkstruct!{pub struct Matches ;}} 
            }}
mkmod!{lint, { 
                getname!(lint);
                getsrc!(lint);
                getpath!(lint);
                get_deps!(lint);
                get_crates!(lint);
                mkinclude!(lint);
                mkitem!{mkstruct!{pub struct Lint ;}}
mkitem!{mkstruct!{pub struct LintId ;}} 
            }}
mkmod!{output, { 
                getname!(output);
                getsrc!(output);
                getpath!(output);
                get_deps!(output);
                get_crates!(output);
                mkinclude!(output);
                mkitem!{mkstruct!{pub struct CRATE_TYPES ;}}

macro_rules! collect_crate_types_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function collect_crate_types in module {}", module_path!());
    };
}

mkfn!{
    collect_crate_types_introspect!();
    pub fn collect_crate_types () { }
}

macro_rules! invalid_output_for_target_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function invalid_output_for_target in module {}", module_path!());
    };
}

mkfn!{
    invalid_output_for_target_introspect!();
    pub fn invalid_output_for_target () { }
} 
            }}
mkitem!{mkstruct!{pub struct EarlyDiagCtxt ;}}
mkitem!{mkstruct!{pub struct Session ;}}
mkitem!{mkstruct!{pub struct FileName ;}}
mkmod!{def_id, { 
                getname!(def_id);
                getsrc!(def_id);
                getpath!(def_id);
                get_deps!(def_id);
                get_crates!(def_id);
                mkinclude!(def_id);
                mkitem!{mkstruct!{pub struct LOCAL_CRATE ;}} 
            }}
mkmod!{ty, { 
                getname!(ty);
                getsrc!(ty);
                getpath!(ty);
                get_deps!(ty);
                get_crates!(ty);
                mkinclude!(ty);
                mkitem!{mkstruct!{pub struct TyCtxt < T > (pub T) ;}} 
            }} 
            }}
mkmod!{session_diagnostics, { 
                getname!(session_diagnostics);
                getsrc!(session_diagnostics);
                getpath!(session_diagnostics);
                get_deps!(session_diagnostics);
                get_crates!(session_diagnostics);
                mkinclude!(session_diagnostics);
                mkitem!{mkstruct!{pub struct CantEmitMIR ;}}
mkitem!{mkstruct!{pub struct RLinkEmptyVersionNumber ;}}
mkitem!{mkstruct!{pub struct RLinkEncodingVersionMismatch ;}}
mkitem!{mkstruct!{pub struct RLinkRustcVersionMismatch ;}}
mkitem!{mkstruct!{pub struct RLinkWrongFileType ;}}
mkitem!{mkstruct!{pub struct RlinkCorruptFile ;}}
mkitem!{mkstruct!{pub struct RlinkNotAFile ;}}
mkitem!{mkstruct!{pub struct RlinkUnableToRead ;}}
mkitem!{mkstruct!{pub struct UnstableFeatureUsage ;}} 
            }}
mkitem!{macro_rules ! do_not_use_print { ($ ($ t : tt) *) => { compile_error ! ("Don't use print") } ; }}
mkitem!{macro_rules ! do_not_use_safe_print { ($ ($ t : tt) *) => { compile_error ! ("Don't use safe_print") } ; }}
mkitem!{macro_rules ! mktrait { ($ trait_def : item) => { $ trait_def } ; }}
mkitem!{macro_rules ! mkimpl { ($ impl_def : item) => { $ impl_def } ; }}
mkitem!{macro_rules ! getname { ($ name : ident) => { pub fn get_module_name () -> &'static str { stringify ! ($ name) } } ; }}
mkitem!{macro_rules ! getsrc { ($ name : ident) => { pub fn get_source_info () -> &'static str { concat ! ("Module: " , stringify ! ($ name)) } } ; }}
mkitem!{macro_rules ! getpath { ($ name : ident) => { pub fn get_module_path () -> &'static str { module_path ! () } } ; }}
mkitem!{macro_rules ! get_deps { ($ name : ident) => { pub fn get_dependencies () -> &'static [&'static str] { & [] } } ; }}
mkitem!{macro_rules ! get_crates { ($ name : ident) => { pub fn get_required_crates () -> &'static [&'static str] { & [] } } ; }}
mkitem!{macro_rules ! forall_crates { ($ ($ crate_name : ident) ,*) => { $ (extern crate $ crate_name ;) * } ; }}
mkitem!{macro_rules ! emit_extern { ($ crate_name : ident) => { extern crate $ crate_name ; } ; }}
mkitem!{macro_rules ! get_externs { ($ crate_name : ident) => { stringify ! ($ crate_name) } ; }}
mkuse!{use crate :: any :: type_name ;}
mkuse!{use crate :: mem :: ManuallyDrop ;}
mkuse!{use crate :: { fmt , intrinsics , ptr , slice } ;}
mkitem!{# [doc = " A wrapper type to construct uninitialized instances of `T`."] # [doc = ""] # [doc = " # Initialization invariant"] # [doc = ""] # [doc = " The compiler, in general, assumes that a variable is properly initialized"] # [doc = " according to the requirements of the variable's type. For example, a variable of"] # [doc = " reference type must be aligned and non-null. This is an invariant that must"] # [doc = " *always* be upheld, even in unsafe code. As a consequence, zero-initializing a"] # [doc = " variable of reference type causes instantaneous [undefined behavior][ub],"] # [doc = " no matter whether that reference ever gets used to access memory:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # #![allow(invalid_value)]"] # [doc = " use std::mem::{self, MaybeUninit};"] # [doc = ""] # [doc = " let x: &i32 = unsafe { mem::zeroed() }; // undefined behavior! ⚠\u{fe0f}"] # [doc = " // The equivalent code with `MaybeUninit<&i32>`:"] # [doc = " let x: &i32 = unsafe { MaybeUninit::zeroed().assume_init() }; // undefined behavior! ⚠\u{fe0f}"] # [doc = " ```"] # [doc = ""] # [doc = " This is exploited by the compiler for various optimizations, such as eliding"] # [doc = " run-time checks and optimizing `enum` layout."] # [doc = ""] # [doc = " Similarly, entirely uninitialized memory may have any content, while a `bool` must"] # [doc = " always be `true` or `false`. Hence, creating an uninitialized `bool` is undefined behavior:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # #![allow(invalid_value)]"] # [doc = " use std::mem::{self, MaybeUninit};"] # [doc = ""] # [doc = " let b: bool = unsafe { mem::uninitialized() }; // undefined behavior! ⚠\u{fe0f}"] # [doc = " // The equivalent code with `MaybeUninit<bool>`:"] # [doc = " let b: bool = unsafe { MaybeUninit::uninit().assume_init() }; // undefined behavior! ⚠\u{fe0f}"] # [doc = " ```"] # [doc = ""] # [doc = " Moreover, uninitialized memory is special in that it does not have a fixed value (\"fixed\""] # [doc = " meaning \"it won't change without being written to\"). Reading the same uninitialized byte"] # [doc = " multiple times can give different results. This makes it undefined behavior to have"] # [doc = " uninitialized data in a variable even if that variable has an integer type, which otherwise can"] # [doc = " hold any *fixed* bit pattern:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # #![allow(invalid_value)]"] # [doc = " use std::mem::{self, MaybeUninit};"] # [doc = ""] # [doc = " let x: i32 = unsafe { mem::uninitialized() }; // undefined behavior! ⚠\u{fe0f}"] # [doc = " // The equivalent code with `MaybeUninit<i32>`:"] # [doc = " let x: i32 = unsafe { MaybeUninit::uninit().assume_init() }; // undefined behavior! ⚠\u{fe0f}"] # [doc = " ```"] # [doc = " On top of that, remember that most types have additional invariants beyond merely"] # [doc = " being considered initialized at the type level. For example, a `1`-initialized [`Vec<T>`]"] # [doc = " is considered initialized (under the current implementation; this does not constitute"] # [doc = " a stable guarantee) because the only requirement the compiler knows about it"] # [doc = " is that the data pointer must be non-null. Creating such a `Vec<T>` does not cause"] # [doc = " *immediate* undefined behavior, but will cause undefined behavior with most"] # [doc = " safe operations (including dropping it)."] # [doc = ""] # [doc = " [`Vec<T>`]: ../../std/vec/struct.Vec.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " `MaybeUninit<T>` serves to enable unsafe code to deal with uninitialized data."] # [doc = " It is a signal to the compiler indicating that the data here might *not*"] # [doc = " be initialized:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " // Create an explicitly uninitialized reference. The compiler knows that data inside"] # [doc = " // a `MaybeUninit<T>` may be invalid, and hence this is not UB:"] # [doc = " let mut x = MaybeUninit::<&i32>::uninit();"] # [doc = " // Set it to a valid value."] # [doc = " x.write(&0);"] # [doc = " // Extract the initialized data -- this is only allowed *after* properly"] # [doc = " // initializing `x`!"] # [doc = " let x = unsafe { x.assume_init() };"] # [doc = " ```"] # [doc = ""] # [doc = " The compiler then knows to not make any incorrect assumptions or optimizations on this code."] # [doc = ""] # [doc = " You can think of `MaybeUninit<T>` as being a bit like `Option<T>` but without"] # [doc = " any of the run-time tracking and without any of the safety checks."] # [doc = ""] # [doc = " ## out-pointers"] # [doc = ""] # [doc = " You can use `MaybeUninit<T>` to implement \"out-pointers\": instead of returning data"] # [doc = " from a function, pass it a pointer to some (uninitialized) memory to put the"] # [doc = " result into. This can be useful when it is important for the caller to control"] # [doc = " how the memory the result is stored in gets allocated, and you want to avoid"] # [doc = " unnecessary moves."] # [doc = ""] # [doc = " ```"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " unsafe fn make_vec(out: *mut Vec<i32>) {"] # [doc = "     // `write` does not drop the old contents, which is important."] # [doc = "     unsafe { out.write(vec![1, 2, 3]); }"] # [doc = " }"] # [doc = ""] # [doc = " let mut v = MaybeUninit::uninit();"] # [doc = " unsafe { make_vec(v.as_mut_ptr()); }"] # [doc = " // Now we know `v` is initialized! This also makes sure the vector gets"] # [doc = " // properly dropped."] # [doc = " let v = unsafe { v.assume_init() };"] # [doc = " assert_eq!(&v, &[1, 2, 3]);"] # [doc = " ```"] # [doc = ""] # [doc = " ## Initializing an array element-by-element"] # [doc = ""] # [doc = " `MaybeUninit<T>` can be used to initialize a large array element-by-element:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::mem::{self, MaybeUninit};"] # [doc = ""] # [doc = " let data = {"] # [doc = "     // Create an uninitialized array of `MaybeUninit`."] # [doc = "     let mut data: [MaybeUninit<Vec<u32>>; 1000] = [const { MaybeUninit::uninit() }; 1000];"] # [doc = ""] # [doc = "     // Dropping a `MaybeUninit` does nothing, so if there is a panic during this loop,"] # [doc = "     // we have a memory leak, but there is no memory safety issue."] # [doc = "     for elem in &mut data[..] {"] # [doc = "         elem.write(vec![42]);"] # [doc = "     }"] # [doc = ""] # [doc = "     // Everything is initialized. Transmute the array to the"] # [doc = "     // initialized type."] # [doc = "     unsafe { mem::transmute::<_, [Vec<u32>; 1000]>(data) }"] # [doc = " };"] # [doc = ""] # [doc = " assert_eq!(&data[0], &[42]);"] # [doc = " ```"] # [doc = ""] # [doc = " You can also work with partially initialized arrays, which could"] # [doc = " be found in low-level datastructures."] # [doc = ""] # [doc = " ```"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " // Create an uninitialized array of `MaybeUninit`."] # [doc = " let mut data: [MaybeUninit<String>; 1000] = [const { MaybeUninit::uninit() }; 1000];"] # [doc = " // Count the number of elements we have assigned."] # [doc = " let mut data_len: usize = 0;"] # [doc = ""] # [doc = " for elem in &mut data[0..500] {"] # [doc = "     elem.write(String::from(\"hello\"));"] # [doc = "     data_len += 1;"] # [doc = " }"] # [doc = ""] # [doc = " // For each item in the array, drop if we allocated it."] # [doc = " for elem in &mut data[0..data_len] {"] # [doc = "     unsafe { elem.assume_init_drop(); }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " ## Initializing a struct field-by-field"] # [doc = ""] # [doc = " You can use `MaybeUninit<T>`, and the [`std::ptr::addr_of_mut`] macro, to initialize structs field by field:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::mem::MaybeUninit;"] # [doc = " use std::ptr::addr_of_mut;"] # [doc = ""] # [doc = " #[derive(Debug, PartialEq)]"] # [doc = " pub struct Foo {"] # [doc = "     name: String,"] # [doc = "     list: Vec<u8>,"] # [doc = " }"] # [doc = ""] # [doc = " let foo = {"] # [doc = "     let mut uninit: MaybeUninit<Foo> = MaybeUninit::uninit();"] # [doc = "     let ptr = uninit.as_mut_ptr();"] # [doc = ""] # [doc = "     // Initializing the `name` field"] # [doc = "     // Using `write` instead of assignment via `=` to not call `drop` on the"] # [doc = "     // old, uninitialized value."] # [doc = "     unsafe { addr_of_mut!((*ptr).name).write(\"Bob\".to_string()); }"] # [doc = ""] # [doc = "     // Initializing the `list` field"] # [doc = "     // If there is a panic here, then the `String` in the `name` field leaks."] # [doc = "     unsafe { addr_of_mut!((*ptr).list).write(vec![0, 1, 2]); }"] # [doc = ""] # [doc = "     // All the fields are initialized, so we call `assume_init` to get an initialized Foo."] # [doc = "     unsafe { uninit.assume_init() }"] # [doc = " };"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     foo,"] # [doc = "     Foo {"] # [doc = "         name: \"Bob\".to_string(),"] # [doc = "         list: vec![0, 1, 2]"] # [doc = "     }"] # [doc = " );"] # [doc = " ```"] # [doc = " [`std::ptr::addr_of_mut`]: crate::ptr::addr_of_mut"] # [doc = " [ub]: ../../reference/behavior-considered-undefined.html"] # [doc = ""] # [doc = " # Layout"] # [doc = ""] # [doc = " `MaybeUninit<T>` is guaranteed to have the same size, alignment, and ABI as `T`:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::mem::MaybeUninit;"] # [doc = " assert_eq!(size_of::<MaybeUninit<u64>>(), size_of::<u64>());"] # [doc = " assert_eq!(align_of::<MaybeUninit<u64>>(), align_of::<u64>());"] # [doc = " ```"] # [doc = ""] # [doc = " However remember that a type *containing* a `MaybeUninit<T>` is not necessarily the same"] # [doc = " layout; Rust does not in general guarantee that the fields of a `Foo<T>` have the same order as"] # [doc = " a `Foo<U>` even if `T` and `U` have the same size and alignment. Furthermore because any bit"] # [doc = " value is valid for a `MaybeUninit<T>` the compiler can't apply non-zero/niche-filling"] # [doc = " optimizations, potentially resulting in a larger size:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use std::mem::MaybeUninit;"] # [doc = " assert_eq!(size_of::<Option<bool>>(), 1);"] # [doc = " assert_eq!(size_of::<Option<MaybeUninit<bool>>>(), 2);"] # [doc = " ```"] # [doc = ""] # [doc = " If `T` is FFI-safe, then so is `MaybeUninit<T>`."] # [doc = ""] # [doc = " While `MaybeUninit` is `#[repr(transparent)]` (indicating it guarantees the same size,"] # [doc = " alignment, and ABI as `T`), this does *not* change any of the previous caveats. `Option<T>` and"] # [doc = " `Option<MaybeUninit<T>>` may still have different sizes, and types containing a field of type"] # [doc = " `T` may be laid out (and sized) differently than if that field were `MaybeUninit<T>`."] # [doc = " `MaybeUninit` is a union type, and `#[repr(transparent)]` on unions is unstable (see [the"] # [doc = " tracking issue](https://github.com/rust-lang/rust/issues/60405)). Over time, the exact"] # [doc = " guarantees of `#[repr(transparent)]` on unions may evolve, and `MaybeUninit` may or may not"] # [doc = " remain `#[repr(transparent)]`. That said, `MaybeUninit<T>` will *always* guarantee that it has"] # [doc = " the same size, alignment, and ABI as `T`; it's just that the way `MaybeUninit` implements that"] # [doc = " guarantee may evolve."] # [doc = ""] # [doc = " Note that even though `T` and `MaybeUninit<T>` are ABI compatible it is still unsound to"] # [doc = " transmute `&mut T` to `&mut MaybeUninit<T>` and expose that to safe code because it would allow"] # [doc = " safe code to access uninitialized memory:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use core::mem::MaybeUninit;"] # [doc = ""] # [doc = " fn unsound_transmute<T>(val: &mut T) -> &mut MaybeUninit<T> {"] # [doc = "     unsafe { core::mem::transmute(val) }"] # [doc = " }"] # [doc = ""] # [doc = " fn main() {"] # [doc = "     let mut code = 0;"] # [doc = "     let code = &mut code;"] # [doc = "     let code2 = unsound_transmute(code);"] # [doc = "     *code2 = MaybeUninit::uninit();"] # [doc = "     std::process::exit(*code); // UB! Accessing uninitialized memory."] # [doc = " }"] # [doc = " ```"] # [stable (feature = "maybe_uninit" , since = "1.36.0")] # [lang = "maybe_uninit"] # [derive (Copy)] # [repr (transparent)] # [rustc_pub_transparent] pub union MaybeUninit < T > { uninit : () , value : ManuallyDrop < T > , }}
mkitem!{mkimpl!{# [stable (feature = "maybe_uninit" , since = "1.36.0")] impl < T : Copy > Clone for MaybeUninit < T > { # [inline (always)] fn clone (& self) -> Self { * self } }}}
mkitem!{mkimpl!{# [stable (feature = "maybe_uninit_debug" , since = "1.41.0")] impl < T > fmt :: Debug for MaybeUninit < T > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { let full_name = type_name :: < Self > () ; let prefix_len = full_name . find ("MaybeUninit") . unwrap () ; f . pad (& full_name [prefix_len ..]) } }}}
mkitem!{mkimpl!{impl < T > MaybeUninit < T > { # [doc = " Creates a new `MaybeUninit<T>` initialized with the given value."] # [doc = " It is safe to call [`assume_init`] on the return value of this function."] # [doc = ""] # [doc = " Note that dropping a `MaybeUninit<T>` will never call `T`'s drop code."] # [doc = " It is your responsibility to make sure `T` gets dropped if it got initialized."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let v: MaybeUninit<Vec<u8>> = MaybeUninit::new(vec![42]);"] # [doc = " # // Prevent leaks for Miri"] # [doc = " # unsafe { let _ = MaybeUninit::assume_init(v); }"] # [doc = " ```"] # [doc = ""] # [doc = " [`assume_init`]: MaybeUninit::assume_init"] # [stable (feature = "maybe_uninit" , since = "1.36.0")] # [rustc_const_stable (feature = "const_maybe_uninit" , since = "1.36.0")] # [must_use = "use `forget` to avoid running Drop code"] # [inline (always)] pub const fn new (val : T) -> MaybeUninit < T > { MaybeUninit { value : ManuallyDrop :: new (val) } } # [doc = " Creates a new `MaybeUninit<T>` in an uninitialized state."] # [doc = ""] # [doc = " Note that dropping a `MaybeUninit<T>` will never call `T`'s drop code."] # [doc = " It is your responsibility to make sure `T` gets dropped if it got initialized."] # [doc = ""] # [doc = " See the [type-level documentation][MaybeUninit] for some examples."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let v: MaybeUninit<String> = MaybeUninit::uninit();"] # [doc = " ```"] # [stable (feature = "maybe_uninit" , since = "1.36.0")] # [rustc_const_stable (feature = "const_maybe_uninit" , since = "1.36.0")] # [must_use] # [inline (always)] # [rustc_diagnostic_item = "maybe_uninit_uninit"] pub const fn uninit () -> MaybeUninit < T > { MaybeUninit { uninit : () } } # [doc = " Creates a new `MaybeUninit<T>` in an uninitialized state, with the memory being"] # [doc = " filled with `0` bytes. It depends on `T` whether that already makes for"] # [doc = " proper initialization. For example, `MaybeUninit<usize>::zeroed()` is initialized,"] # [doc = " but `MaybeUninit<&'static i32>::zeroed()` is not because references must not"] # [doc = " be null."] # [doc = ""] # [doc = " Note that if `T` has padding bytes, those bytes are *not* preserved when the"] # [doc = " `MaybeUninit<T>` value is returned from this function, so those bytes will *not* be zeroed."] # [doc = ""] # [doc = " Note that dropping a `MaybeUninit<T>` will never call `T`'s drop code."] # [doc = " It is your responsibility to make sure `T` gets dropped if it got initialized."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Correct usage of this function: initializing a struct with zero, where all"] # [doc = " fields of the struct can hold the bit-pattern 0 as a valid value."] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let x = MaybeUninit::<(u8, bool)>::zeroed();"] # [doc = " let x = unsafe { x.assume_init() };"] # [doc = " assert_eq!(x, (0, false));"] # [doc = " ```"] # [doc = ""] # [doc = " This can be used in const contexts, such as to indicate the end of static arrays for"] # [doc = " plugin registration."] # [doc = ""] # [doc = " *Incorrect* usage of this function: calling `x.zeroed().assume_init()`"] # [doc = " when `0` is not a valid bit-pattern for the type:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " enum NotZero { One = 1, Two = 2 }"] # [doc = ""] # [doc = " let x = MaybeUninit::<(u8, NotZero)>::zeroed();"] # [doc = " let x = unsafe { x.assume_init() };"] # [doc = " // Inside a pair, we create a `NotZero` that does not have a valid discriminant."] # [doc = " // This is undefined behavior. ⚠\u{fe0f}"] # [doc = " ```"] # [inline] # [must_use] # [rustc_diagnostic_item = "maybe_uninit_zeroed"] # [stable (feature = "maybe_uninit" , since = "1.36.0")] # [rustc_const_stable (feature = "const_maybe_uninit_zeroed" , since = "1.75.0")] pub const fn zeroed () -> MaybeUninit < T > { let mut u = MaybeUninit :: < T > :: uninit () ; unsafe { u . as_mut_ptr () . write_bytes (0u8 , 1) } ; u } # [doc = " Sets the value of the `MaybeUninit<T>`."] # [doc = ""] # [doc = " This overwrites any previous value without dropping it, so be careful"] # [doc = " not to use this twice unless you want to skip running the destructor."] # [doc = " For your convenience, this also returns a mutable reference to the"] # [doc = " (now safely initialized) contents of `self`."] # [doc = ""] # [doc = " As the content is stored inside a `ManuallyDrop`, the destructor is not"] # [doc = " run for the inner data if the MaybeUninit leaves scope without a call to"] # [doc = " [`assume_init`], [`assume_init_drop`], or similar. Code that receives"] # [doc = " the mutable reference returned by this function needs to keep this in"] # [doc = " mind. The safety model of Rust regards leaks as safe, but they are"] # [doc = " usually still undesirable. This being said, the mutable reference"] # [doc = " behaves like any other mutable reference would, so assigning a new value"] # [doc = " to it will drop the old content."] # [doc = ""] # [doc = " [`assume_init`]: Self::assume_init"] # [doc = " [`assume_init_drop`]: Self::assume_init_drop"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Correct usage of this method:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut x = MaybeUninit::<Vec<u8>>::uninit();"] # [doc = ""] # [doc = " {"] # [doc = "     let hello = x.write((&b\"Hello, world!\").to_vec());"] # [doc = "     // Setting hello does not leak prior allocations, but drops them"] # [doc = "     *hello = (&b\"Hello\").to_vec();"] # [doc = "     hello[0] = 'h' as u8;"] # [doc = " }"] # [doc = " // x is initialized now:"] # [doc = " let s = unsafe { x.assume_init() };"] # [doc = " assert_eq!(b\"hello\", s.as_slice());"] # [doc = " ```"] # [doc = ""] # [doc = " This usage of the method causes a leak:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut x = MaybeUninit::<String>::uninit();"] # [doc = ""] # [doc = " x.write(\"Hello\".to_string());"] # [doc = " # // FIXME(https://github.com/rust-lang/miri/issues/3670):"] # [doc = " # // use -Zmiri-disable-leak-check instead of unleaking in tests meant to leak."] # [doc = " # unsafe { MaybeUninit::assume_init_drop(&mut x); }"] # [doc = " // This leaks the contained string:"] # [doc = " x.write(\"hello\".to_string());"] # [doc = " // x is initialized now:"] # [doc = " let s = unsafe { x.assume_init() };"] # [doc = " ```"] # [doc = ""] # [doc = " This method can be used to avoid unsafe in some cases. The example below"] # [doc = " shows a part of an implementation of a fixed sized arena that lends out"] # [doc = " pinned references."] # [doc = " With `write`, we can avoid the need to write through a raw pointer:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use core::pin::Pin;"] # [doc = " use core::mem::MaybeUninit;"] # [doc = ""] # [doc = " struct PinArena<T> {"] # [doc = "     memory: Box<[MaybeUninit<T>]>,"] # [doc = "     len: usize,"] # [doc = " }"] # [doc = ""] # [doc = " impl <T> PinArena<T> {"] # [doc = "     pub fn capacity(&self) -> usize {"] # [doc = "         self.memory.len()"] # [doc = "     }"] # [doc = "     pub fn push(&mut self, val: T) -> Pin<&mut T> {"] # [doc = "         if self.len >= self.capacity() {"] # [doc = "             panic!(\"Attempted to push to a full pin arena!\");"] # [doc = "         }"] # [doc = "         let ref_ = self.memory[self.len].write(val);"] # [doc = "         self.len += 1;"] # [doc = "         unsafe { Pin::new_unchecked(ref_) }"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [inline (always)] # [stable (feature = "maybe_uninit_write" , since = "1.55.0")] # [rustc_const_stable (feature = "const_maybe_uninit_write" , since = "1.85.0")] pub const fn write (& mut self , val : T) -> & mut T { * self = MaybeUninit :: new (val) ; unsafe { self . assume_init_mut () } } # [doc = " Gets a pointer to the contained value. Reading from this pointer or turning it"] # [doc = " into a reference is undefined behavior unless the `MaybeUninit<T>` is initialized."] # [doc = " Writing to memory that this pointer (non-transitively) points to is undefined behavior"] # [doc = " (except inside an `UnsafeCell<T>`)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Correct usage of this method:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut x = MaybeUninit::<Vec<u32>>::uninit();"] # [doc = " x.write(vec![0, 1, 2]);"] # [doc = " // Create a reference into the `MaybeUninit<T>`. This is okay because we initialized it."] # [doc = " let x_vec = unsafe { &*x.as_ptr() };"] # [doc = " assert_eq!(x_vec.len(), 3);"] # [doc = " # // Prevent leaks for Miri"] # [doc = " # unsafe { MaybeUninit::assume_init_drop(&mut x); }"] # [doc = " ```"] # [doc = ""] # [doc = " *Incorrect* usage of this method:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let x = MaybeUninit::<Vec<u32>>::uninit();"] # [doc = " let x_vec = unsafe { &*x.as_ptr() };"] # [doc = " // We have created a reference to an uninitialized vector! This is undefined behavior. ⚠\u{fe0f}"] # [doc = " ```"] # [doc = ""] # [doc = " (Notice that the rules around references to uninitialized data are not finalized yet, but"] # [doc = " until they are, it is advisable to avoid them.)"] # [stable (feature = "maybe_uninit" , since = "1.36.0")] # [rustc_const_stable (feature = "const_maybe_uninit_as_ptr" , since = "1.59.0")] # [rustc_as_ptr] # [inline (always)] pub const fn as_ptr (& self) -> * const T { self as * const _ as * const T } # [doc = " Gets a mutable pointer to the contained value. Reading from this pointer or turning it"] # [doc = " into a reference is undefined behavior unless the `MaybeUninit<T>` is initialized."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Correct usage of this method:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut x = MaybeUninit::<Vec<u32>>::uninit();"] # [doc = " x.write(vec![0, 1, 2]);"] # [doc = " // Create a reference into the `MaybeUninit<Vec<u32>>`."] # [doc = " // This is okay because we initialized it."] # [doc = " let x_vec = unsafe { &mut *x.as_mut_ptr() };"] # [doc = " x_vec.push(3);"] # [doc = " assert_eq!(x_vec.len(), 4);"] # [doc = " # // Prevent leaks for Miri"] # [doc = " # unsafe { MaybeUninit::assume_init_drop(&mut x); }"] # [doc = " ```"] # [doc = ""] # [doc = " *Incorrect* usage of this method:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut x = MaybeUninit::<Vec<u32>>::uninit();"] # [doc = " let x_vec = unsafe { &mut *x.as_mut_ptr() };"] # [doc = " // We have created a reference to an uninitialized vector! This is undefined behavior. ⚠\u{fe0f}"] # [doc = " ```"] # [doc = ""] # [doc = " (Notice that the rules around references to uninitialized data are not finalized yet, but"] # [doc = " until they are, it is advisable to avoid them.)"] # [stable (feature = "maybe_uninit" , since = "1.36.0")] # [rustc_const_stable (feature = "const_maybe_uninit_as_mut_ptr" , since = "1.83.0")] # [rustc_as_ptr] # [inline (always)] pub const fn as_mut_ptr (& mut self) -> * mut T { self as * mut _ as * mut T } # [doc = " Extracts the value from the `MaybeUninit<T>` container. This is a great way"] # [doc = " to ensure that the data will get dropped, because the resulting `T` is"] # [doc = " subject to the usual drop handling."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " It is up to the caller to guarantee that the `MaybeUninit<T>` really is in an initialized"] # [doc = " state. Calling this when the content is not yet fully initialized causes immediate undefined"] # [doc = " behavior. The [type-level documentation][inv] contains more information about"] # [doc = " this initialization invariant."] # [doc = ""] # [doc = " [inv]: #initialization-invariant"] # [doc = ""] # [doc = " On top of that, remember that most types have additional invariants beyond merely"] # [doc = " being considered initialized at the type level. For example, a `1`-initialized [`Vec<T>`]"] # [doc = " is considered initialized (under the current implementation; this does not constitute"] # [doc = " a stable guarantee) because the only requirement the compiler knows about it"] # [doc = " is that the data pointer must be non-null. Creating such a `Vec<T>` does not cause"] # [doc = " *immediate* undefined behavior, but will cause undefined behavior with most"] # [doc = " safe operations (including dropping it)."] # [doc = ""] # [doc = " [`Vec<T>`]: ../../std/vec/struct.Vec.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Correct usage of this method:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut x = MaybeUninit::<bool>::uninit();"] # [doc = " x.write(true);"] # [doc = " let x_init = unsafe { x.assume_init() };"] # [doc = " assert_eq!(x_init, true);"] # [doc = " ```"] # [doc = ""] # [doc = " *Incorrect* usage of this method:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let x = MaybeUninit::<Vec<u32>>::uninit();"] # [doc = " let x_init = unsafe { x.assume_init() };"] # [doc = " // `x` had not been initialized yet, so this last line caused undefined behavior. ⚠\u{fe0f}"] # [doc = " ```"] # [stable (feature = "maybe_uninit" , since = "1.36.0")] # [rustc_const_stable (feature = "const_maybe_uninit_assume_init_by_value" , since = "1.59.0")] # [inline (always)] # [rustc_diagnostic_item = "assume_init"] # [track_caller] pub const unsafe fn assume_init (self) -> T { unsafe { intrinsics :: assert_inhabited :: < T > () ; (& raw const self . value) . cast :: < T > () . read () } } # [doc = " Reads the value from the `MaybeUninit<T>` container. The resulting `T` is subject"] # [doc = " to the usual drop handling."] # [doc = ""] # [doc = " Whenever possible, it is preferable to use [`assume_init`] instead, which"] # [doc = " prevents duplicating the content of the `MaybeUninit<T>`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " It is up to the caller to guarantee that the `MaybeUninit<T>` really is in an initialized"] # [doc = " state. Calling this when the content is not yet fully initialized causes undefined"] # [doc = " behavior. The [type-level documentation][inv] contains more information about"] # [doc = " this initialization invariant."] # [doc = ""] # [doc = " Moreover, similar to the [`ptr::read`] function, this function creates a"] # [doc = " bitwise copy of the contents, regardless whether the contained type"] # [doc = " implements the [`Copy`] trait or not. When using multiple copies of the"] # [doc = " data (by calling `assume_init_read` multiple times, or first calling"] # [doc = " `assume_init_read` and then [`assume_init`]), it is your responsibility"] # [doc = " to ensure that data may indeed be duplicated."] # [doc = ""] # [doc = " [inv]: #initialization-invariant"] # [doc = " [`assume_init`]: MaybeUninit::assume_init"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Correct usage of this method:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut x = MaybeUninit::<u32>::uninit();"] # [doc = " x.write(13);"] # [doc = " let x1 = unsafe { x.assume_init_read() };"] # [doc = " // `u32` is `Copy`, so we may read multiple times."] # [doc = " let x2 = unsafe { x.assume_init_read() };"] # [doc = " assert_eq!(x1, x2);"] # [doc = ""] # [doc = " let mut x = MaybeUninit::<Option<Vec<u32>>>::uninit();"] # [doc = " x.write(None);"] # [doc = " let x1 = unsafe { x.assume_init_read() };"] # [doc = " // Duplicating a `None` value is okay, so we may read multiple times."] # [doc = " let x2 = unsafe { x.assume_init_read() };"] # [doc = " assert_eq!(x1, x2);"] # [doc = " ```"] # [doc = ""] # [doc = " *Incorrect* usage of this method:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut x = MaybeUninit::<Option<Vec<u32>>>::uninit();"] # [doc = " x.write(Some(vec![0, 1, 2]));"] # [doc = " let x1 = unsafe { x.assume_init_read() };"] # [doc = " let x2 = unsafe { x.assume_init_read() };"] # [doc = " // We now created two copies of the same vector, leading to a double-free ⚠\u{fe0f} when"] # [doc = " // they both get dropped!"] # [doc = " ```"] # [stable (feature = "maybe_uninit_extra" , since = "1.60.0")] # [rustc_const_stable (feature = "const_maybe_uninit_assume_init_read" , since = "1.75.0")] # [inline (always)] # [track_caller] pub const unsafe fn assume_init_read (& self) -> T { unsafe { intrinsics :: assert_inhabited :: < T > () ; self . as_ptr () . read () } } # [doc = " Drops the contained value in place."] # [doc = ""] # [doc = " If you have ownership of the `MaybeUninit`, you can also use"] # [doc = " [`assume_init`] as an alternative."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " It is up to the caller to guarantee that the `MaybeUninit<T>` really is"] # [doc = " in an initialized state. Calling this when the content is not yet fully"] # [doc = " initialized causes undefined behavior."] # [doc = ""] # [doc = " On top of that, all additional invariants of the type `T` must be"] # [doc = " satisfied, as the `Drop` implementation of `T` (or its members) may"] # [doc = " rely on this. For example, setting a `Vec<T>` to an invalid but"] # [doc = " non-null address makes it initialized (under the current implementation;"] # [doc = " this does not constitute a stable guarantee), because the only"] # [doc = " requirement the compiler knows about it is that the data pointer must be"] # [doc = " non-null. Dropping such a `Vec<T>` however will cause undefined"] # [doc = " behavior."] # [doc = ""] # [doc = " [`assume_init`]: MaybeUninit::assume_init"] # [stable (feature = "maybe_uninit_extra" , since = "1.60.0")] pub unsafe fn assume_init_drop (& mut self) { unsafe { ptr :: drop_in_place (self . as_mut_ptr ()) } } # [doc = " Gets a shared reference to the contained value."] # [doc = ""] # [doc = " This can be useful when we want to access a `MaybeUninit` that has been"] # [doc = " initialized but don't have ownership of the `MaybeUninit` (preventing the use"] # [doc = " of `.assume_init()`)."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Calling this when the content is not yet fully initialized causes undefined"] # [doc = " behavior: it is up to the caller to guarantee that the `MaybeUninit<T>` really"] # [doc = " is in an initialized state."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ### Correct usage of this method:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut x = MaybeUninit::<Vec<u32>>::uninit();"] # [doc = " # let mut x_mu = x;"] # [doc = " # let mut x = &mut x_mu;"] # [doc = " // Initialize `x`:"] # [doc = " x.write(vec![1, 2, 3]);"] # [doc = " // Now that our `MaybeUninit<_>` is known to be initialized, it is okay to"] # [doc = " // create a shared reference to it:"] # [doc = " let x: &Vec<u32> = unsafe {"] # [doc = "     // SAFETY: `x` has been initialized."] # [doc = "     x.assume_init_ref()"] # [doc = " };"] # [doc = " assert_eq!(x, &vec![1, 2, 3]);"] # [doc = " # // Prevent leaks for Miri"] # [doc = " # unsafe { MaybeUninit::assume_init_drop(&mut x_mu); }"] # [doc = " ```"] # [doc = ""] # [doc = " ### *Incorrect* usages of this method:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let x = MaybeUninit::<Vec<u32>>::uninit();"] # [doc = " let x_vec: &Vec<u32> = unsafe { x.assume_init_ref() };"] # [doc = " // We have created a reference to an uninitialized vector! This is undefined behavior. ⚠\u{fe0f}"] # [doc = " ```"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::{cell::Cell, mem::MaybeUninit};"] # [doc = ""] # [doc = " let b = MaybeUninit::<Cell<bool>>::uninit();"] # [doc = " // Initialize the `MaybeUninit` using `Cell::set`:"] # [doc = " unsafe {"] # [doc = "     b.assume_init_ref().set(true);"] # [doc = "     //^^^^^^^^^^^^^^^ Reference to an uninitialized `Cell<bool>`: UB!"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "maybe_uninit_ref" , since = "1.55.0")] # [rustc_const_stable (feature = "const_maybe_uninit_assume_init_ref" , since = "1.59.0")] # [inline (always)] pub const unsafe fn assume_init_ref (& self) -> & T { unsafe { intrinsics :: assert_inhabited :: < T > () ; & * self . as_ptr () } } # [doc = " Gets a mutable (unique) reference to the contained value."] # [doc = ""] # [doc = " This can be useful when we want to access a `MaybeUninit` that has been"] # [doc = " initialized but don't have ownership of the `MaybeUninit` (preventing the use"] # [doc = " of `.assume_init()`)."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Calling this when the content is not yet fully initialized causes undefined"] # [doc = " behavior: it is up to the caller to guarantee that the `MaybeUninit<T>` really"] # [doc = " is in an initialized state. For instance, `.assume_init_mut()` cannot be used to"] # [doc = " initialize a `MaybeUninit`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ### Correct usage of this method:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # #![allow(unexpected_cfgs)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " # unsafe extern \"C\" fn initialize_buffer(buf: *mut [u8; 1024]) { unsafe { *buf = [0; 1024] } }"] # [doc = " # #[cfg(FALSE)]"] # [doc = " extern \"C\" {"] # [doc = "     /// Initializes *all* the bytes of the input buffer."] # [doc = "     fn initialize_buffer(buf: *mut [u8; 1024]);"] # [doc = " }"] # [doc = ""] # [doc = " let mut buf = MaybeUninit::<[u8; 1024]>::uninit();"] # [doc = ""] # [doc = " // Initialize `buf`:"] # [doc = " unsafe { initialize_buffer(buf.as_mut_ptr()); }"] # [doc = " // Now we know that `buf` has been initialized, so we could `.assume_init()` it."] # [doc = " // However, using `.assume_init()` may trigger a `memcpy` of the 1024 bytes."] # [doc = " // To assert our buffer has been initialized without copying it, we upgrade"] # [doc = " // the `&mut MaybeUninit<[u8; 1024]>` to a `&mut [u8; 1024]`:"] # [doc = " let buf: &mut [u8; 1024] = unsafe {"] # [doc = "     // SAFETY: `buf` has been initialized."] # [doc = "     buf.assume_init_mut()"] # [doc = " };"] # [doc = ""] # [doc = " // Now we can use `buf` as a normal slice:"] # [doc = " buf.sort_unstable();"] # [doc = " assert!("] # [doc = "     buf.windows(2).all(|pair| pair[0] <= pair[1]),"] # [doc = "     \"buffer is sorted\","] # [doc = " );"] # [doc = " ```"] # [doc = ""] # [doc = " ### *Incorrect* usages of this method:"] # [doc = ""] # [doc = " You cannot use `.assume_init_mut()` to initialize a value:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut b = MaybeUninit::<bool>::uninit();"] # [doc = " unsafe {"] # [doc = "     *b.assume_init_mut() = true;"] # [doc = "     // We have created a (mutable) reference to an uninitialized `bool`!"] # [doc = "     // This is undefined behavior. ⚠\u{fe0f}"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " For instance, you cannot [`Read`] into an uninitialized buffer:"] # [doc = ""] # [doc = " [`Read`]: ../../std/io/trait.Read.html"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::{io, mem::MaybeUninit};"] # [doc = ""] # [doc = " fn read_chunk (reader: &'_ mut dyn io::Read) -> io::Result<[u8; 64]>"] # [doc = " {"] # [doc = "     let mut buffer = MaybeUninit::<[u8; 64]>::uninit();"] # [doc = "     reader.read_exact(unsafe { buffer.assume_init_mut() })?;"] # [doc = "     //                         ^^^^^^^^^^^^^^^^^^^^^^^^"] # [doc = "     // (mutable) reference to uninitialized memory!"] # [doc = "     // This is undefined behavior."] # [doc = "     Ok(unsafe { buffer.assume_init() })"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Nor can you use direct field access to do field-by-field gradual initialization:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::{mem::MaybeUninit, ptr};"] # [doc = ""] # [doc = " struct Foo {"] # [doc = "     a: u32,"] # [doc = "     b: u8,"] # [doc = " }"] # [doc = ""] # [doc = " let foo: Foo = unsafe {"] # [doc = "     let mut foo = MaybeUninit::<Foo>::uninit();"] # [doc = "     ptr::write(&mut foo.assume_init_mut().a as *mut u32, 1337);"] # [doc = "     //              ^^^^^^^^^^^^^^^^^^^^^"] # [doc = "     // (mutable) reference to uninitialized memory!"] # [doc = "     // This is undefined behavior."] # [doc = "     ptr::write(&mut foo.assume_init_mut().b as *mut u8, 42);"] # [doc = "     //              ^^^^^^^^^^^^^^^^^^^^^"] # [doc = "     // (mutable) reference to uninitialized memory!"] # [doc = "     // This is undefined behavior."] # [doc = "     foo.assume_init()"] # [doc = " };"] # [doc = " ```"] # [stable (feature = "maybe_uninit_ref" , since = "1.55.0")] # [rustc_const_stable (feature = "const_maybe_uninit_assume_init" , since = "1.84.0")] # [inline (always)] pub const unsafe fn assume_init_mut (& mut self) -> & mut T { unsafe { intrinsics :: assert_inhabited :: < T > () ; & mut * self . as_mut_ptr () } } # [doc = " Extracts the values from an array of `MaybeUninit` containers."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " It is up to the caller to guarantee that all elements of the array are"] # [doc = " in an initialized state."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_array_assume_init)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut array: [MaybeUninit<i32>; 3] = [MaybeUninit::uninit(); 3];"] # [doc = " array[0].write(0);"] # [doc = " array[1].write(1);"] # [doc = " array[2].write(2);"] # [doc = ""] # [doc = " // SAFETY: Now safe as we initialised all elements"] # [doc = " let array = unsafe {"] # [doc = "     MaybeUninit::array_assume_init(array)"] # [doc = " };"] # [doc = ""] # [doc = " assert_eq!(array, [0, 1, 2]);"] # [doc = " ```"] # [unstable (feature = "maybe_uninit_array_assume_init" , issue = "96097")] # [inline (always)] # [track_caller] pub const unsafe fn array_assume_init < const N : usize > (array : [Self ; N]) -> [T ; N] { unsafe { intrinsics :: assert_inhabited :: < [T ; N] > () ; intrinsics :: transmute_unchecked (array) } } # [doc = " Returns the contents of this `MaybeUninit` as a slice of potentially uninitialized bytes."] # [doc = ""] # [doc = " Note that even if the contents of a `MaybeUninit` have been initialized, the value may still"] # [doc = " contain padding bytes which are left uninitialized."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_as_bytes, maybe_uninit_slice)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let val = 0x12345678_i32;"] # [doc = " let uninit = MaybeUninit::new(val);"] # [doc = " let uninit_bytes = uninit.as_bytes();"] # [doc = " let bytes = unsafe { uninit_bytes.assume_init_ref() };"] # [doc = " assert_eq!(bytes, val.to_ne_bytes());"] # [doc = " ```"] # [unstable (feature = "maybe_uninit_as_bytes" , issue = "93092")] pub const fn as_bytes (& self) -> & [MaybeUninit < u8 >] { unsafe { slice :: from_raw_parts (self . as_ptr () . cast :: < MaybeUninit < u8 > > () , super :: size_of :: < T > ()) } } # [doc = " Returns the contents of this `MaybeUninit` as a mutable slice of potentially uninitialized"] # [doc = " bytes."] # [doc = ""] # [doc = " Note that even if the contents of a `MaybeUninit` have been initialized, the value may still"] # [doc = " contain padding bytes which are left uninitialized."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_as_bytes)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let val = 0x12345678_i32;"] # [doc = " let mut uninit = MaybeUninit::new(val);"] # [doc = " let uninit_bytes = uninit.as_bytes_mut();"] # [doc = " if cfg!(target_endian = \"little\") {"] # [doc = "     uninit_bytes[0].write(0xcd);"] # [doc = " } else {"] # [doc = "     uninit_bytes[3].write(0xcd);"] # [doc = " }"] # [doc = " let val2 = unsafe { uninit.assume_init() };"] # [doc = " assert_eq!(val2, 0x123456cd);"] # [doc = " ```"] # [unstable (feature = "maybe_uninit_as_bytes" , issue = "93092")] pub const fn as_bytes_mut (& mut self) -> & mut [MaybeUninit < u8 >] { unsafe { slice :: from_raw_parts_mut (self . as_mut_ptr () . cast :: < MaybeUninit < u8 > > () , super :: size_of :: < T > () ,) } } # [doc = " Gets a pointer to the first element of the array."] # [unstable (feature = "maybe_uninit_slice" , issue = "63569")] # [inline (always)] pub const fn slice_as_ptr (this : & [MaybeUninit < T >]) -> * const T { this . as_ptr () as * const T } # [doc = " Gets a mutable pointer to the first element of the array."] # [unstable (feature = "maybe_uninit_slice" , issue = "63569")] # [inline (always)] pub const fn slice_as_mut_ptr (this : & mut [MaybeUninit < T >]) -> * mut T { this . as_mut_ptr () as * mut T } }}}
mkitem!{mkimpl!{impl < T > [MaybeUninit < T >] { # [doc = " Copies the elements from `src` to `self`,"] # [doc = " returning a mutable reference to the now initialized contents of `self`."] # [doc = ""] # [doc = " If `T` does not implement `Copy`, use [`write_clone_of_slice`] instead."] # [doc = ""] # [doc = " This is similar to [`slice::copy_from_slice`]."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if the two slices have different lengths."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_write_slice)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut dst = [MaybeUninit::uninit(); 32];"] # [doc = " let src = [0; 32];"] # [doc = ""] # [doc = " let init = dst.write_copy_of_slice(&src);"] # [doc = ""] # [doc = " assert_eq!(init, src);"] # [doc = " ```"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_write_slice)]"] # [doc = ""] # [doc = " let mut vec = Vec::with_capacity(32);"] # [doc = " let src = [0; 16];"] # [doc = ""] # [doc = " vec.spare_capacity_mut()[..src.len()].write_copy_of_slice(&src);"] # [doc = ""] # [doc = " // SAFETY: we have just copied all the elements of len into the spare capacity"] # [doc = " // the first src.len() elements of the vec are valid now."] # [doc = " unsafe {"] # [doc = "     vec.set_len(src.len());"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(vec, src);"] # [doc = " ```"] # [doc = ""] # [doc = " [`write_clone_of_slice`]: slice::write_clone_of_slice"] # [unstable (feature = "maybe_uninit_write_slice" , issue = "79995")] pub const fn write_copy_of_slice (& mut self , src : & [T]) -> & mut [T] where T : Copy , { let uninit_src : & [MaybeUninit < T >] = unsafe { super :: transmute (src) } ; self . copy_from_slice (uninit_src) ; unsafe { self . assume_init_mut () } } # [doc = " Clones the elements from `src` to `self`,"] # [doc = " returning a mutable reference to the now initialized contents of `self`."] # [doc = " Any already initialized elements will not be dropped."] # [doc = ""] # [doc = " If `T` implements `Copy`, use [`write_copy_of_slice`] instead."] # [doc = ""] # [doc = " This is similar to [`slice::clone_from_slice`] but does not drop existing elements."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if the two slices have different lengths, or if the implementation of `Clone` panics."] # [doc = ""] # [doc = " If there is a panic, the already cloned elements will be dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_write_slice)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut dst = [const { MaybeUninit::uninit() }; 5];"] # [doc = " let src = [\"wibbly\", \"wobbly\", \"timey\", \"wimey\", \"stuff\"].map(|s| s.to_string());"] # [doc = ""] # [doc = " let init = dst.write_clone_of_slice(&src);"] # [doc = ""] # [doc = " assert_eq!(init, src);"] # [doc = ""] # [doc = " # // Prevent leaks for Miri"] # [doc = " # unsafe { std::ptr::drop_in_place(init); }"] # [doc = " ```"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_write_slice)]"] # [doc = ""] # [doc = " let mut vec = Vec::with_capacity(32);"] # [doc = " let src = [\"rust\", \"is\", \"a\", \"pretty\", \"cool\", \"language\"].map(|s| s.to_string());"] # [doc = ""] # [doc = " vec.spare_capacity_mut()[..src.len()].write_clone_of_slice(&src);"] # [doc = ""] # [doc = " // SAFETY: we have just cloned all the elements of len into the spare capacity"] # [doc = " // the first src.len() elements of the vec are valid now."] # [doc = " unsafe {"] # [doc = "     vec.set_len(src.len());"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(vec, src);"] # [doc = " ```"] # [doc = ""] # [doc = " [`write_copy_of_slice`]: slice::write_copy_of_slice"] # [unstable (feature = "maybe_uninit_write_slice" , issue = "79995")] pub fn write_clone_of_slice (& mut self , src : & [T]) -> & mut [T] where T : Clone , { assert_eq ! (self . len () , src . len () , "destination and source slices have different lengths") ; let len = self . len () ; let src = & src [.. len] ; let mut guard = Guard { slice : self , initialized : 0 } ; for i in 0 .. len { guard . slice [i] . write (src [i] . clone ()) ; guard . initialized += 1 ; } super :: forget (guard) ; unsafe { self . assume_init_mut () } } # [doc = " Fills a slice with elements by cloning `value`, returning a mutable reference to the now"] # [doc = " initialized contents of the slice."] # [doc = " Any previously initialized elements will not be dropped."] # [doc = ""] # [doc = " This is similar to [`slice::fill`]."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if any call to `Clone` panics."] # [doc = ""] # [doc = " If such a panic occurs, any elements previously initialized during this operation will be"] # [doc = " dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_fill)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut buf = [const { MaybeUninit::uninit() }; 10];"] # [doc = " let initialized = buf.write_filled(1);"] # [doc = " assert_eq!(initialized, &mut [1; 10]);"] # [doc = " ```"] # [doc (alias = "memset")] # [unstable (feature = "maybe_uninit_fill" , issue = "117428")] pub fn write_filled (& mut self , value : T) -> & mut [T] where T : Clone , { SpecFill :: spec_fill (self , value) ; unsafe { self . assume_init_mut () } } # [doc = " Fills a slice with elements returned by calling a closure for each index."] # [doc = ""] # [doc = " This method uses a closure to create new values. If you'd rather `Clone` a given value, use"] # [doc = " [slice::write_filled]. If you want to use the `Default` trait to generate values, you can"] # [doc = " pass [`|_| Default::default()`][Default::default] as the argument."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if any call to the provided closure panics."] # [doc = ""] # [doc = " If such a panic occurs, any elements previously initialized during this operation will be"] # [doc = " dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_fill)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut buf = [const { MaybeUninit::<usize>::uninit() }; 5];"] # [doc = " let initialized = buf.write_with(|idx| idx + 1);"] # [doc = " assert_eq!(initialized, &mut [1, 2, 3, 4, 5]);"] # [doc = " ```"] # [unstable (feature = "maybe_uninit_fill" , issue = "117428")] pub fn write_with < F > (& mut self , mut f : F) -> & mut [T] where F : FnMut (usize) -> T , { let mut guard = Guard { slice : self , initialized : 0 } ; for (idx , element) in guard . slice . iter_mut () . enumerate () { element . write (f (idx)) ; guard . initialized += 1 ; } super :: forget (guard) ; unsafe { self . assume_init_mut () } } # [doc = " Fills a slice with elements yielded by an iterator until either all elements have been"] # [doc = " initialized or the iterator is empty."] # [doc = ""] # [doc = " Returns two slices. The first slice contains the initialized portion of the original slice."] # [doc = " The second slice is the still-uninitialized remainder of the original slice."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if the iterator's `next` function panics."] # [doc = ""] # [doc = " If such a panic occurs, any elements previously initialized during this operation will be"] # [doc = " dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Completely filling the slice:"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_fill)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut buf = [const { MaybeUninit::uninit() }; 5];"] # [doc = ""] # [doc = " let iter = [1, 2, 3].into_iter().cycle();"] # [doc = " let (initialized, remainder) = buf.write_iter(iter);"] # [doc = ""] # [doc = " assert_eq!(initialized, &mut [1, 2, 3, 1, 2]);"] # [doc = " assert_eq!(remainder.len(), 0);"] # [doc = " ```"] # [doc = ""] # [doc = " Partially filling the slice:"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_fill)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut buf = [const { MaybeUninit::uninit() }; 5];"] # [doc = " let iter = [1, 2];"] # [doc = " let (initialized, remainder) = buf.write_iter(iter);"] # [doc = ""] # [doc = " assert_eq!(initialized, &mut [1, 2]);"] # [doc = " assert_eq!(remainder.len(), 3);"] # [doc = " ```"] # [doc = ""] # [doc = " Checking an iterator after filling a slice:"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_fill)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut buf = [const { MaybeUninit::uninit() }; 3];"] # [doc = " let mut iter = [1, 2, 3, 4, 5].into_iter();"] # [doc = " let (initialized, remainder) = buf.write_iter(iter.by_ref());"] # [doc = ""] # [doc = " assert_eq!(initialized, &mut [1, 2, 3]);"] # [doc = " assert_eq!(remainder.len(), 0);"] # [doc = " assert_eq!(iter.as_slice(), &[4, 5]);"] # [doc = " ```"] # [unstable (feature = "maybe_uninit_fill" , issue = "117428")] pub fn write_iter < I > (& mut self , it : I) -> (& mut [T] , & mut [MaybeUninit < T >]) where I : IntoIterator < Item = T > , { let iter = it . into_iter () ; let mut guard = Guard { slice : self , initialized : 0 } ; for (element , val) in guard . slice . iter_mut () . zip (iter) { element . write (val) ; guard . initialized += 1 ; } let initialized_len = guard . initialized ; super :: forget (guard) ; let (initted , remainder) = unsafe { self . split_at_mut_unchecked (initialized_len) } ; (unsafe { initted . assume_init_mut () } , remainder) } # [doc = " Returns the contents of this `MaybeUninit` as a slice of potentially uninitialized bytes."] # [doc = ""] # [doc = " Note that even if the contents of a `MaybeUninit` have been initialized, the value may still"] # [doc = " contain padding bytes which are left uninitialized."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_as_bytes, maybe_uninit_write_slice, maybe_uninit_slice)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let uninit = [MaybeUninit::new(0x1234u16), MaybeUninit::new(0x5678u16)];"] # [doc = " let uninit_bytes = uninit.as_bytes();"] # [doc = " let bytes = unsafe { uninit_bytes.assume_init_ref() };"] # [doc = " let val1 = u16::from_ne_bytes(bytes[0..2].try_into().unwrap());"] # [doc = " let val2 = u16::from_ne_bytes(bytes[2..4].try_into().unwrap());"] # [doc = " assert_eq!(&[val1, val2], &[0x1234u16, 0x5678u16]);"] # [doc = " ```"] # [unstable (feature = "maybe_uninit_as_bytes" , issue = "93092")] pub const fn as_bytes (& self) -> & [MaybeUninit < u8 >] { unsafe { slice :: from_raw_parts (self . as_ptr () . cast :: < MaybeUninit < u8 > > () , super :: size_of_val (self)) } } # [doc = " Returns the contents of this `MaybeUninit` slice as a mutable slice of potentially"] # [doc = " uninitialized bytes."] # [doc = ""] # [doc = " Note that even if the contents of a `MaybeUninit` have been initialized, the value may still"] # [doc = " contain padding bytes which are left uninitialized."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_as_bytes, maybe_uninit_write_slice, maybe_uninit_slice)]"] # [doc = " use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let mut uninit = [MaybeUninit::<u16>::uninit(), MaybeUninit::<u16>::uninit()];"] # [doc = " let uninit_bytes = uninit.as_bytes_mut();"] # [doc = " uninit_bytes.write_copy_of_slice(&[0x12, 0x34, 0x56, 0x78]);"] # [doc = " let vals = unsafe { uninit.assume_init_ref() };"] # [doc = " if cfg!(target_endian = \"little\") {"] # [doc = "     assert_eq!(vals, &[0x3412u16, 0x7856u16]);"] # [doc = " } else {"] # [doc = "     assert_eq!(vals, &[0x1234u16, 0x5678u16]);"] # [doc = " }"] # [doc = " ```"] # [unstable (feature = "maybe_uninit_as_bytes" , issue = "93092")] pub const fn as_bytes_mut (& mut self) -> & mut [MaybeUninit < u8 >] { unsafe { slice :: from_raw_parts_mut (self . as_mut_ptr () as * mut MaybeUninit < u8 > , super :: size_of_val (self) ,) } } # [doc = " Drops the contained values in place."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " It is up to the caller to guarantee that every `MaybeUninit<T>` in the slice"] # [doc = " really is in an initialized state. Calling this when the content is not yet"] # [doc = " fully initialized causes undefined behavior."] # [doc = ""] # [doc = " On top of that, all additional invariants of the type `T` must be"] # [doc = " satisfied, as the `Drop` implementation of `T` (or its members) may"] # [doc = " rely on this. For example, setting a `Vec<T>` to an invalid but"] # [doc = " non-null address makes it initialized (under the current implementation;"] # [doc = " this does not constitute a stable guarantee), because the only"] # [doc = " requirement the compiler knows about it is that the data pointer must be"] # [doc = " non-null. Dropping such a `Vec<T>` however will cause undefined"] # [doc = " behaviour."] # [unstable (feature = "maybe_uninit_slice" , issue = "63569")] # [inline (always)] pub unsafe fn assume_init_drop (& mut self) { if ! self . is_empty () { unsafe { ptr :: drop_in_place (self as * mut [MaybeUninit < T >] as * mut [T]) } } } # [doc = " Gets a shared reference to the contained value."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Calling this when the content is not yet fully initialized causes undefined"] # [doc = " behavior: it is up to the caller to guarantee that every `MaybeUninit<T>` in"] # [doc = " the slice really is in an initialized state."] # [unstable (feature = "maybe_uninit_slice" , issue = "63569")] # [inline (always)] pub const unsafe fn assume_init_ref (& self) -> & [T] { unsafe { & * (self as * const Self as * const [T]) } } # [doc = " Gets a mutable (unique) reference to the contained value."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Calling this when the content is not yet fully initialized causes undefined"] # [doc = " behavior: it is up to the caller to guarantee that every `MaybeUninit<T>` in the"] # [doc = " slice really is in an initialized state. For instance, `.assume_init_mut()` cannot"] # [doc = " be used to initialize a `MaybeUninit` slice."] # [unstable (feature = "maybe_uninit_slice" , issue = "63569")] # [inline (always)] pub const unsafe fn assume_init_mut (& mut self) -> & mut [T] { unsafe { & mut * (self as * mut Self as * mut [T]) } } }}}
mkitem!{mkimpl!{impl < T , const N : usize > MaybeUninit < [T ; N] > { # [doc = " Transposes a `MaybeUninit<[T; N]>` into a `[MaybeUninit<T>; N]`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_uninit_array_transpose)]"] # [doc = " # use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let data: [MaybeUninit<u8>; 1000] = MaybeUninit::uninit().transpose();"] # [doc = " ```"] # [unstable (feature = "maybe_uninit_uninit_array_transpose" , issue = "96097")] # [inline] pub const fn transpose (self) -> [MaybeUninit < T > ; N] { unsafe { intrinsics :: transmute_unchecked (self) } } }}}
mkitem!{mkimpl!{impl < T , const N : usize > [MaybeUninit < T > ; N] { # [doc = " Transposes a `[MaybeUninit<T>; N]` into a `MaybeUninit<[T; N]>`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(maybe_uninit_uninit_array_transpose)]"] # [doc = " # use std::mem::MaybeUninit;"] # [doc = ""] # [doc = " let data = [MaybeUninit::<u8>::uninit(); 1000];"] # [doc = " let data: MaybeUninit<[u8; 1000]> = data.transpose();"] # [doc = " ```"] # [unstable (feature = "maybe_uninit_uninit_array_transpose" , issue = "96097")] # [inline] pub const fn transpose (self) -> MaybeUninit < [T ; N] > { unsafe { intrinsics :: transmute_unchecked (self) } } }}}
mkitem!{mkstruct!{struct Guard < 'a , T > { slice : & 'a mut [MaybeUninit < T >] , initialized : usize , }}}
mkitem!{mkimpl!{impl < 'a , T > Drop for Guard < 'a , T > { fn drop (& mut self) { let initialized_part = & mut self . slice [.. self . initialized] ; unsafe { initialized_part . assume_init_drop () ; } } }}}
mkitem!{mktrait!{trait SpecFill < T > { fn spec_fill (& mut self , value : T) ; }}}
mkitem!{mkimpl!{impl < T : Clone > SpecFill < T > for [MaybeUninit < T >] { default fn spec_fill (& mut self , value : T) { let mut guard = Guard { slice : self , initialized : 0 } ; if let Some ((last , elems)) = guard . slice . split_last_mut () { for el in elems { el . write (value . clone ()) ; guard . initialized += 1 ; } last . write (value) ; } super :: forget (guard) ; } }}}
mkitem!{mkimpl!{impl < T : Copy > SpecFill < T > for [MaybeUninit < T >] { fn spec_fill (& mut self , value : T) { self . fill (MaybeUninit :: new (value)) ; } }}}