mkuse!{use crate :: mem :: { self , MaybeUninit } ;}
mkuse!{use crate :: ptr ;}
mkitem!{mktrait!{# [doc = " Private specialization trait used by CloneToUninit, as per"] # [doc = " [the dev guide](https://std-dev-guide.rust-lang.org/policy/specialization.html)."] pub (super) unsafe trait CopySpec : Clone { unsafe fn clone_one (src : & Self , dst : * mut Self) ; unsafe fn clone_slice (src : & [Self] , dst : * mut [Self]) ; }}}
mkitem!{mkimpl!{unsafe impl < T : Clone > CopySpec for T { # [inline] default unsafe fn clone_one (src : & Self , dst : * mut Self) { unsafe { ptr :: write (dst , src . clone ()) ; } } # [inline] # [cfg_attr (debug_assertions , track_caller)] default unsafe fn clone_slice (src : & [Self] , dst : * mut [Self]) { let len = src . len () ; debug_assert_eq ! (len , dst . len () , "clone_to_uninit() source and destination must have equal lengths" ,) ; let uninit_ref = unsafe { & mut * (dst as * mut [MaybeUninit < T >]) } ; let mut initializing = InitializingSlice :: from_fully_uninit (uninit_ref) ; for element_ref in src { initializing . push (element_ref . clone ()) ; } mem :: forget (initializing) ; } }}}
mkitem!{mkimpl!{unsafe impl < T : Copy > CopySpec for T { # [inline] unsafe fn clone_one (src : & Self , dst : * mut Self) { unsafe { ptr :: copy_nonoverlapping (src , dst , 1) ; } } # [inline] # [cfg_attr (debug_assertions , track_caller)] unsafe fn clone_slice (src : & [Self] , dst : * mut [Self]) { let len = src . len () ; debug_assert_eq ! (len , dst . len () , "clone_to_uninit() source and destination must have equal lengths" ,) ; unsafe { ptr :: copy_nonoverlapping (src . as_ptr () , dst . as_mut_ptr () , len) ; } } }}}
mkitem!{mkstruct!{# [doc = " Ownership of a collection of values stored in a non-owned `[MaybeUninit<T>]`, some of which"] # [doc = " are not yet initialized. This is sort of like a `Vec` that doesn't own its allocation."] # [doc = " Its responsibility is to provide cleanup on unwind by dropping the values that *are*"] # [doc = " initialized, unless disarmed by forgetting."] # [doc = ""] # [doc = " This is a helper for `impl<T: Clone> CloneToUninit for [T]`."] struct InitializingSlice < 'a , T > { data : & 'a mut [MaybeUninit < T >] , # [doc = " Number of elements of `*self.data` that are initialized."] initialized_len : usize , }}}
mkitem!{mkimpl!{impl < 'a , T > InitializingSlice < 'a , T > { # [inline] fn from_fully_uninit (data : & 'a mut [MaybeUninit < T >]) -> Self { Self { data , initialized_len : 0 } } # [doc = " Push a value onto the end of the initialized part of the slice."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the slice is already fully initialized."] # [inline] fn push (& mut self , value : T) { MaybeUninit :: write (& mut self . data [self . initialized_len] , value) ; self . initialized_len += 1 ; } }}}
mkitem!{mkimpl!{impl < 'a , T > Drop for InitializingSlice < 'a , T > { # [cold] fn drop (& mut self) { let initialized_slice = ptr :: slice_from_raw_parts_mut (MaybeUninit :: slice_as_mut_ptr (self . data) , self . initialized_len ,) ; unsafe { ptr :: drop_in_place :: < [T] > (initialized_slice) ; } } }}}