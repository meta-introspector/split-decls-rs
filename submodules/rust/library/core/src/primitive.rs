# ! [doc = " This module reexports the primitive types to allow usage that is not"] # ! [doc = " possibly shadowed by other declared types."] # ! [doc = ""] # ! [doc = " This is normally only useful in macro generated code."] # ! [doc = ""] # ! [doc = " An example of this is when generating a new struct and an impl for it:"] # ! [doc = ""] # ! [doc = " ```rust,compile_fail"] # ! [doc = " pub struct bool;"] # ! [doc = ""] # ! [doc = " impl QueryId for bool {"] # ! [doc = "     const SOME_PROPERTY: bool = true;"] # ! [doc = " }"] # ! [doc = ""] # ! [doc = " # trait QueryId { const SOME_PROPERTY: ::core::primitive::bool; }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " Note that the `SOME_PROPERTY` associated constant would not compile, as its"] # ! [doc = " type `bool` refers to the struct, rather than to the primitive bool type."] # ! [doc = ""] # ! [doc = " A correct implementation could look like:"] # ! [doc = ""] # ! [doc = " ```rust"] # ! [doc = " # #[allow(non_camel_case_types)]"] # ! [doc = " pub struct bool;"] # ! [doc = ""] # ! [doc = " impl QueryId for bool {"] # ! [doc = "     const SOME_PROPERTY: ::core::primitive::bool = true;"] # ! [doc = " }"] # ! [doc = ""] # ! [doc = " # trait QueryId { const SOME_PROPERTY: ::core::primitive::bool; }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " We also used `::core` instead of `core`, because `core` can be"] # ! [doc = " shadowed, too. Paths, starting with `::`, are searched in"] # ! [doc = " the [extern prelude] since Edition 2018."] # ! [doc = ""] # ! [doc = " [extern prelude]: https://doc.rust-lang.org/nightly/reference/names/preludes.html#extern-prelude"] use split_decls_genesis :: ourprelude :: * ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use bool ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use char ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use f32 ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use f64 ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use i8 ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use i16 ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use i32 ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use i64 ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use i128 ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use isize ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use str ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use u8 ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use u16 ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use u32 ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use u64 ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use u128 ; #[stable (feature = "core_primitive" , since = "1.43.0")] pub use usize ;