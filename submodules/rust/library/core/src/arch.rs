# ! [doc = include_str ! ("../../stdarch/crates/core_arch/src/core_arch_docs.md")] use split_decls_genesis :: ourprelude :: * ; #[allow (unused_imports , unreachable_pub)] #[stable (feature = "simd_arch" , since = "1.27.0")] pub use crate :: core_arch :: arch :: * ; #[doc = " Inline assembly."] #[doc = ""] #[doc = " Refer to [Rust By Example] for a usage guide and the [reference] for"] #[doc = " detailed information about the syntax and available options."] #[doc = ""] #[doc = " [Rust By Example]: https://doc.rust-lang.org/nightly/rust-by-example/unsafe/asm.html"] #[doc = " [reference]: https://doc.rust-lang.org/nightly/reference/inline-assembly.html"] #[stable (feature = "asm" , since = "1.59.0")] #[rustc_builtin_macro] pub macro asm ("assembly template" , $ (operands ,) * $ (options ($ (option) ,*)) ?) { } #[doc = " Inline assembly used in combination with `#[naked]` functions."] #[doc = ""] #[doc = " Refer to [Rust By Example] for a usage guide and the [reference] for"] #[doc = " detailed information about the syntax and available options."] #[doc = ""] #[doc = " [Rust By Example]: https://doc.rust-lang.org/nightly/rust-by-example/unsafe/asm.html"] #[doc = " [reference]: https://doc.rust-lang.org/nightly/reference/inline-assembly.html"] #[stable (feature = "naked_functions" , since = "1.88.0")] #[rustc_builtin_macro] pub macro naked_asm ("assembly template" , $ (operands ,) * $ (options ($ (option) ,*)) ?) { } #[doc = " Module-level inline assembly."] #[doc = ""] #[doc = " Refer to [Rust By Example] for a usage guide and the [reference] for"] #[doc = " detailed information about the syntax and available options."] #[doc = ""] #[doc = " [Rust By Example]: https://doc.rust-lang.org/nightly/rust-by-example/unsafe/asm.html"] #[doc = " [reference]: https://doc.rust-lang.org/nightly/reference/inline-assembly.html"] #[stable (feature = "global_asm" , since = "1.59.0")] #[rustc_builtin_macro] pub macro global_asm ("assembly template" , $ (operands ,) * $ (options ($ (option) ,*)) ?) { } #[doc = " Compiles to a target-specific software breakpoint instruction or equivalent."] #[doc = ""] #[doc = " This will typically abort the program. It may result in a core dump, and/or the system logging"] #[doc = " debug information. Additional target-specific capabilities may be possible depending on"] #[doc = " debuggers or other tooling; in particular, a debugger may be able to resume execution."] #[doc = ""] #[doc = " If possible, this will produce an instruction sequence that allows a debugger to resume *after*"] #[doc = " the breakpoint, rather than resuming *at* the breakpoint; however, the exact behavior is"] #[doc = " target-specific and debugger-specific, and not guaranteed."] #[doc = ""] #[doc = " If the target platform does not have any kind of debug breakpoint instruction, this may compile"] #[doc = " to a trapping instruction (e.g. an undefined instruction) instead, or to some other form of"] #[doc = " target-specific abort that may or may not support convenient resumption."] #[doc = ""] #[doc = " The precise behavior and the precise instruction generated are not guaranteed, except that in"] #[doc = " normal execution with no debug tooling involved this will not continue executing."] #[doc = ""] #[doc = " - On x86 targets, this produces an `int3` instruction."] #[doc = " - On aarch64 targets, this produces a `brk #0xf000` instruction."] #[unstable (feature = "breakpoint" , issue = "133724")] #[inline (always)] pub fn breakpoint () { core :: intrinsics :: breakpoint () ; }