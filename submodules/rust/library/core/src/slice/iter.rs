# ! [doc = " Definitions of a bunch of iterators for `[T]`."] use split_decls_genesis :: ourprelude :: * ; #[macro_use] mod macros ; use super :: { from_raw_parts , from_raw_parts_mut } ; use crate :: hint :: assert_unchecked ; use crate :: iter :: { FusedIterator , TrustedLen , TrustedRandomAccess , TrustedRandomAccessNoCoerce , UncheckedIterator , } ; use crate :: marker :: PhantomData ; use crate :: mem :: { self , SizedTypeProperties } ; use crate :: num :: NonZero ; use crate :: ptr :: { NonNull , without_provenance , without_provenance_mut } ; use crate :: { cmp , fmt } ; #[stable (feature = "boxed_slice_into_iter" , since = "1.80.0")] impl < T > ! Iterator for [T] { } #[stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T > IntoIterator for & 'a [T] { type Item = & 'a T ; type IntoIter = Iter < 'a , T > ; fn into_iter (self) -> Iter < 'a , T > { self . iter () } } #[stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T > IntoIterator for & 'a mut [T] { type Item = & 'a mut T ; type IntoIter = IterMut < 'a , T > ; fn into_iter (self) -> IterMut < 'a , T > { self . iter_mut () } } #[doc = " Immutable slice iterator"] #[doc = ""] #[doc = " This struct is created by the [`iter`] method on [slices]."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " // First, we need a slice to call the `iter` method on:"] #[doc = " let slice = &[1, 2, 3];"] #[doc = ""] #[doc = " // Then we call `iter` on the slice to get the `Iter` iterator,"] #[doc = " // and iterate over it:"] #[doc = " for element in slice.iter() {"] #[doc = "     println!(\"{element}\");"] #[doc = " }"] #[doc = ""] #[doc = " // This for loop actually already works without calling `iter`:"] #[doc = " for element in slice {"] #[doc = "     println!(\"{element}\");"] #[doc = " }"] #[doc = " ```"] #[doc = ""] #[doc = " [`iter`]: slice::iter"] #[doc = " [slices]: slice"] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] #[rustc_diagnostic_item = "SliceIter"] pub struct Iter < 'a , T : 'a > { #[doc = " The pointer to the next element to return, or the past-the-end location"] #[doc = " if the iterator is empty."] #[doc = ""] #[doc = " This address will be used for all ZST elements, never changed."] ptr : NonNull < T > , #[doc = " For non-ZSTs, the non-null pointer to the past-the-end element."] #[doc = ""] #[doc = " For ZSTs, this is `ptr::without_provenance_mut(len)`."] end_or_len : * const T , _marker : PhantomData < & 'a T > , } #[stable (feature = "core_impl_debug" , since = "1.9.0")] impl < T : fmt :: Debug > fmt :: Debug for Iter < '_ , T > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_tuple ("Iter") . field (& self . as_slice ()) . finish () } } #[stable (feature = "rust1" , since = "1.0.0")] unsafe impl < T : Sync > Sync for Iter < '_ , T > { } #[stable (feature = "rust1" , since = "1.0.0")] unsafe impl < T : Sync > Send for Iter < '_ , T > { } impl < 'a , T > Iter < 'a , T > { #[inline] pub (super) const fn new (slice : & 'a [T]) -> Self { let len = slice . len () ; let ptr : NonNull < T > = NonNull :: from_ref (slice) . cast () ; unsafe { let end_or_len = if T :: IS_ZST { without_provenance (len) } else { ptr . as_ptr () . add (len) } ; Self { ptr , end_or_len , _marker : PhantomData } } } #[doc = " Views the underlying data as a subslice of the original data."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " // First, we need a slice to call the `iter` method on:"] #[doc = " let slice = &[1, 2, 3];"] #[doc = ""] #[doc = " // Then we call `iter` on the slice to get the `Iter` iterator:"] #[doc = " let mut iter = slice.iter();"] #[doc = " // Here `as_slice` still returns the whole slice, so this prints \"[1, 2, 3]\":"] #[doc = " println!(\"{:?}\", iter.as_slice());"] #[doc = ""] #[doc = " // Now, we call the `next` method to remove the first element from the iterator:"] #[doc = " iter.next();"] #[doc = " // Here the iterator does not contain the first element of the slice any more,"] #[doc = " // so `as_slice` only returns the last two elements of the slice,"] #[doc = " // and so this prints \"[2, 3]\":"] #[doc = " println!(\"{:?}\", iter.as_slice());"] #[doc = ""] #[doc = " // The underlying slice has not been modified and still contains three elements,"] #[doc = " // so this prints \"[1, 2, 3]\":"] #[doc = " println!(\"{:?}\", slice);"] #[doc = " ```"] #[must_use] #[stable (feature = "iter_to_slice" , since = "1.4.0")] #[inline] pub fn as_slice (& self) -> & 'a [T] { self . make_slice () } } iterator ! { struct Iter -> * const T , &'a T , const , { } , as_ref , { fn is_sorted_by < F > (self , mut compare : F) -> bool where Self : Sized , F : FnMut (& Self :: Item , & Self :: Item) -> bool , { self . as_slice () . is_sorted_by (| a , b | compare (& a , & b)) } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < T > Clone for Iter < '_ , T > { #[inline] fn clone (& self) -> Self { Iter { ptr : self . ptr , end_or_len : self . end_or_len , _marker : self . _marker } } } #[stable (feature = "slice_iter_as_ref" , since = "1.13.0")] impl < T > AsRef < [T] > for Iter < '_ , T > { #[inline] fn as_ref (& self) -> & [T] { self . as_slice () } } #[doc = " Mutable slice iterator."] #[doc = ""] #[doc = " This struct is created by the [`iter_mut`] method on [slices]."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " // First, we need a slice to call the `iter_mut` method on:"] #[doc = " let slice = &mut [1, 2, 3];"] #[doc = ""] #[doc = " // Then we call `iter_mut` on the slice to get the `IterMut` iterator,"] #[doc = " // iterate over it and increment each element value:"] #[doc = " for element in slice.iter_mut() {"] #[doc = "     *element += 1;"] #[doc = " }"] #[doc = ""] #[doc = " // We now have \"[2, 3, 4]\":"] #[doc = " println!(\"{slice:?}\");"] #[doc = " ```"] #[doc = ""] #[doc = " [`iter_mut`]: slice::iter_mut"] #[doc = " [slices]: slice"] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct IterMut < 'a , T : 'a > { #[doc = " The pointer to the next element to return, or the past-the-end location"] #[doc = " if the iterator is empty."] #[doc = ""] #[doc = " This address will be used for all ZST elements, never changed."] ptr : NonNull < T > , #[doc = " For non-ZSTs, the non-null pointer to the past-the-end element."] #[doc = ""] #[doc = " For ZSTs, this is `ptr::without_provenance_mut(len)`."] end_or_len : * mut T , _marker : PhantomData < & 'a mut T > , } #[stable (feature = "core_impl_debug" , since = "1.9.0")] impl < T : fmt :: Debug > fmt :: Debug for IterMut < '_ , T > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_tuple ("IterMut") . field (& self . make_slice ()) . finish () } } #[stable (feature = "rust1" , since = "1.0.0")] unsafe impl < T : Sync > Sync for IterMut < '_ , T > { } #[stable (feature = "rust1" , since = "1.0.0")] unsafe impl < T : Send > Send for IterMut < '_ , T > { } impl < 'a , T > IterMut < 'a , T > { #[inline] pub (super) const fn new (slice : & 'a mut [T]) -> Self { let len = slice . len () ; let ptr : NonNull < T > = NonNull :: from_mut (slice) . cast () ; unsafe { let end_or_len = if T :: IS_ZST { without_provenance_mut (len) } else { ptr . as_ptr () . add (len) } ; Self { ptr , end_or_len , _marker : PhantomData } } } #[doc = " Views the underlying data as a subslice of the original data."] #[doc = ""] #[doc = " To avoid creating `&mut` references that alias, this is forced"] #[doc = " to consume the iterator."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " // First, we need a slice to call the `iter_mut` method on:"] #[doc = " let mut slice = &mut [1, 2, 3];"] #[doc = ""] #[doc = " // Then we call `iter_mut` on the slice to get the `IterMut` struct:"] #[doc = " let mut iter = slice.iter_mut();"] #[doc = " // Now, we call the `next` method to remove the first element of the iterator,"] #[doc = " // unwrap and dereference what we get from `next` and increase its value by 1:"] #[doc = " *iter.next().unwrap() += 1;"] #[doc = " // Here the iterator does not contain the first element of the slice any more,"] #[doc = " // so `into_slice` only returns the last two elements of the slice,"] #[doc = " // and so this prints \"[2, 3]\":"] #[doc = " println!(\"{:?}\", iter.into_slice());"] #[doc = " // The underlying slice still contains three elements, but its first element"] #[doc = " // was increased by 1, so this prints \"[2, 2, 3]\":"] #[doc = " println!(\"{:?}\", slice);"] #[doc = " ```"] #[must_use = "`self` will be dropped if the result is not used"] #[stable (feature = "iter_to_slice" , since = "1.4.0")] pub fn into_slice (self) -> & 'a mut [T] { unsafe { from_raw_parts_mut (self . ptr . as_ptr () , len ! (self)) } } #[doc = " Views the underlying data as a subslice of the original data."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " // First, we need a slice to call the `iter_mut` method on:"] #[doc = " let slice = &mut [1, 2, 3];"] #[doc = ""] #[doc = " // Then we call `iter_mut` on the slice to get the `IterMut` iterator:"] #[doc = " let mut iter = slice.iter_mut();"] #[doc = " // Here `as_slice` still returns the whole slice, so this prints \"[1, 2, 3]\":"] #[doc = " println!(\"{:?}\", iter.as_slice());"] #[doc = ""] #[doc = " // Now, we call the `next` method to remove the first element from the iterator"] #[doc = " // and increment its value:"] #[doc = " *iter.next().unwrap() += 1;"] #[doc = " // Here the iterator does not contain the first element of the slice any more,"] #[doc = " // so `as_slice` only returns the last two elements of the slice,"] #[doc = " // and so this prints \"[2, 3]\":"] #[doc = " println!(\"{:?}\", iter.as_slice());"] #[doc = ""] #[doc = " // The underlying slice still contains three elements, but its first element"] #[doc = " // was increased by 1, so this prints \"[2, 2, 3]\":"] #[doc = " println!(\"{:?}\", slice);"] #[doc = " ```"] #[must_use] #[stable (feature = "slice_iter_mut_as_slice" , since = "1.53.0")] #[inline] pub fn as_slice (& self) -> & [T] { self . make_slice () } #[doc = " Views the underlying data as a mutable subslice of the original data."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(slice_iter_mut_as_mut_slice)]"] #[doc = ""] #[doc = " let mut slice: &mut [usize] = &mut [1, 2, 3];"] #[doc = ""] #[doc = " // First, we get the iterator:"] #[doc = " let mut iter = slice.iter_mut();"] #[doc = " // Then, we get a mutable slice from it:"] #[doc = " let mut_slice = iter.as_mut_slice();"] #[doc = " // So if we check what the `as_mut_slice` method returned, we have \"[1, 2, 3]\":"] #[doc = " assert_eq!(mut_slice, &mut [1, 2, 3]);"] #[doc = ""] #[doc = " // We can use it to mutate the slice:"] #[doc = " mut_slice[0] = 4;"] #[doc = " mut_slice[2] = 5;"] #[doc = ""] #[doc = " // Next, we can move to the second element of the slice, checking that"] #[doc = " // it yields the value we just wrote:"] #[doc = " assert_eq!(iter.next(), Some(&mut 4));"] #[doc = " // Now `as_mut_slice` returns \"[2, 5]\":"] #[doc = " assert_eq!(iter.as_mut_slice(), &mut [2, 5]);"] #[doc = " ```"] #[must_use] #[unstable (feature = "slice_iter_mut_as_mut_slice" , issue = "93079")] pub fn as_mut_slice (& mut self) -> & mut [T] { unsafe { from_raw_parts_mut (self . ptr . as_ptr () , len ! (self)) } } } #[stable (feature = "slice_iter_mut_as_slice" , since = "1.53.0")] impl < T > AsRef < [T] > for IterMut < '_ , T > { #[inline] fn as_ref (& self) -> & [T] { self . as_slice () } } iterator ! { struct IterMut -> * mut T , &'a mut T , mut , { mut } , as_mut , { } } #[doc = " An internal abstraction over the splitting iterators, so that"] #[doc = " splitn, splitn_mut etc can be implemented once."] #[doc (hidden)] pub (super) trait SplitIter : DoubleEndedIterator { #[doc = " Marks the underlying iterator as complete, extracting the remaining"] #[doc = " portion of the slice."] fn finish (& mut self) -> Option < Self :: Item > ; } #[doc = " An iterator over subslices separated by elements that match a predicate"] #[doc = " function."] #[doc = ""] #[doc = " This struct is created by the [`split`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let slice = [10, 40, 33, 20];"] #[doc = " let mut iter = slice.split(|num| num % 3 == 0);"] #[doc = " assert_eq!(iter.next(), Some(&[10, 40][..]));"] #[doc = " assert_eq!(iter.next(), Some(&[20][..]));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " [`split`]: slice::split"] #[doc = " [slices]: slice"] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct Split < 'a , T : 'a , P > where P : FnMut (& T) -> bool , { pub (crate) v : & 'a [T] , pred : P , pub (crate) finished : bool , } impl < 'a , T : 'a , P : FnMut (& T) -> bool > Split < 'a , T , P > { #[inline] pub (super) fn new (slice : & 'a [T] , pred : P) -> Self { Self { v : slice , pred , finished : false } } #[doc = " Returns a slice which contains items not yet handled by split."] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(split_as_slice)]"] #[doc = " let slice = [1,2,3,4,5];"] #[doc = " let mut split = slice.split(|v| v % 2 == 0);"] #[doc = " assert!(split.next().is_some());"] #[doc = " assert_eq!(split.as_slice(), &[3,4,5]);"] #[doc = " ```"] #[unstable (feature = "split_as_slice" , issue = "96137")] pub fn as_slice (& self) -> & 'a [T] { if self . finished { & [] } else { & self . v } } } #[stable (feature = "core_impl_debug" , since = "1.9.0")] impl < T : fmt :: Debug , P > fmt :: Debug for Split < '_ , T , P > where P : FnMut (& T) -> bool , { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("Split") . field ("v" , & self . v) . field ("finished" , & self . finished) . finish () } } #[stable (feature = "rust1" , since = "1.0.0")] impl < T , P > Clone for Split < '_ , T , P > where P : Clone + FnMut (& T) -> bool , { fn clone (& self) -> Self { Split { v : self . v , pred : self . pred . clone () , finished : self . finished } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T , P > Iterator for Split < 'a , T , P > where P : FnMut (& T) -> bool , { type Item = & 'a [T] ; #[inline] fn next (& mut self) -> Option < & 'a [T] > { if self . finished { return None ; } match self . v . iter () . position (| x | (self . pred) (x)) { None => self . finish () , Some (idx) => { let (left , right) = unsafe { (self . v . get_unchecked (.. idx) , self . v . get_unchecked (idx + 1 ..)) } ; let ret = Some (left) ; self . v = right ; ret } } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { if self . finished { (0 , Some (0)) } else { (1 , Some (self . v . len () + 1)) } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T , P > DoubleEndedIterator for Split < 'a , T , P > where P : FnMut (& T) -> bool , { #[inline] fn next_back (& mut self) -> Option < & 'a [T] > { if self . finished { return None ; } match self . v . iter () . rposition (| x | (self . pred) (x)) { None => self . finish () , Some (idx) => { let (left , right) = unsafe { (self . v . get_unchecked (.. idx) , self . v . get_unchecked (idx + 1 ..)) } ; let ret = Some (right) ; self . v = left ; ret } } } } impl < 'a , T , P > SplitIter for Split < 'a , T , P > where P : FnMut (& T) -> bool , { #[inline] fn finish (& mut self) -> Option < & 'a [T] > { if self . finished { None } else { self . finished = true ; Some (self . v) } } } #[stable (feature = "fused" , since = "1.26.0")] impl < T , P > FusedIterator for Split < '_ , T , P > where P : FnMut (& T) -> bool { } #[doc = " An iterator over subslices separated by elements that match a predicate"] #[doc = " function. Unlike `Split`, it contains the matched part as a terminator"] #[doc = " of the subslice."] #[doc = ""] #[doc = " This struct is created by the [`split_inclusive`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let slice = [10, 40, 33, 20];"] #[doc = " let mut iter = slice.split_inclusive(|num| num % 3 == 0);"] #[doc = " assert_eq!(iter.next(), Some(&[10, 40, 33][..]));"] #[doc = " assert_eq!(iter.next(), Some(&[20][..]));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " [`split_inclusive`]: slice::split_inclusive"] #[doc = " [slices]: slice"] #[stable (feature = "split_inclusive" , since = "1.51.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct SplitInclusive < 'a , T : 'a , P > where P : FnMut (& T) -> bool , { v : & 'a [T] , pred : P , finished : bool , } impl < 'a , T : 'a , P : FnMut (& T) -> bool > SplitInclusive < 'a , T , P > { #[inline] pub (super) fn new (slice : & 'a [T] , pred : P) -> Self { let finished = slice . is_empty () ; Self { v : slice , pred , finished } } } #[stable (feature = "split_inclusive" , since = "1.51.0")] impl < T : fmt :: Debug , P > fmt :: Debug for SplitInclusive < '_ , T , P > where P : FnMut (& T) -> bool , { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("SplitInclusive") . field ("v" , & self . v) . field ("finished" , & self . finished) . finish () } } #[stable (feature = "split_inclusive" , since = "1.51.0")] impl < T , P > Clone for SplitInclusive < '_ , T , P > where P : Clone + FnMut (& T) -> bool , { fn clone (& self) -> Self { SplitInclusive { v : self . v , pred : self . pred . clone () , finished : self . finished } } } #[stable (feature = "split_inclusive" , since = "1.51.0")] impl < 'a , T , P > Iterator for SplitInclusive < 'a , T , P > where P : FnMut (& T) -> bool , { type Item = & 'a [T] ; #[inline] fn next (& mut self) -> Option < & 'a [T] > { if self . finished { return None ; } let idx = self . v . iter () . position (| x | (self . pred) (x)) . map (| idx | idx + 1) . unwrap_or (self . v . len ()) ; if idx == self . v . len () { self . finished = true ; } let ret = Some (& self . v [.. idx]) ; self . v = & self . v [idx ..] ; ret } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { if self . finished { (0 , Some (0)) } else { (1 , Some (cmp :: max (1 , self . v . len ()))) } } } #[stable (feature = "split_inclusive" , since = "1.51.0")] impl < 'a , T , P > DoubleEndedIterator for SplitInclusive < 'a , T , P > where P : FnMut (& T) -> bool , { #[inline] fn next_back (& mut self) -> Option < & 'a [T] > { if self . finished { return None ; } let remainder = if self . v . is_empty () { & [] } else { & self . v [.. (self . v . len () - 1)] } ; let idx = remainder . iter () . rposition (| x | (self . pred) (x)) . map (| idx | idx + 1) . unwrap_or (0) ; if idx == 0 { self . finished = true ; } let ret = Some (& self . v [idx ..]) ; self . v = & self . v [.. idx] ; ret } } #[stable (feature = "split_inclusive" , since = "1.51.0")] impl < T , P > FusedIterator for SplitInclusive < '_ , T , P > where P : FnMut (& T) -> bool { } #[doc = " An iterator over the mutable subslices of the vector which are separated"] #[doc = " by elements that match `pred`."] #[doc = ""] #[doc = " This struct is created by the [`split_mut`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let mut v = [10, 40, 30, 20, 60, 50];"] #[doc = " let iter = v.split_mut(|num| *num % 3 == 0);"] #[doc = " ```"] #[doc = ""] #[doc = " [`split_mut`]: slice::split_mut"] #[doc = " [slices]: slice"] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct SplitMut < 'a , T : 'a , P > where P : FnMut (& T) -> bool , { v : & 'a mut [T] , pred : P , finished : bool , } impl < 'a , T : 'a , P : FnMut (& T) -> bool > SplitMut < 'a , T , P > { #[inline] pub (super) fn new (slice : & 'a mut [T] , pred : P) -> Self { Self { v : slice , pred , finished : false } } } #[stable (feature = "core_impl_debug" , since = "1.9.0")] impl < T : fmt :: Debug , P > fmt :: Debug for SplitMut < '_ , T , P > where P : FnMut (& T) -> bool , { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("SplitMut") . field ("v" , & self . v) . field ("finished" , & self . finished) . finish () } } impl < 'a , T , P > SplitIter for SplitMut < 'a , T , P > where P : FnMut (& T) -> bool , { #[inline] fn finish (& mut self) -> Option < & 'a mut [T] > { if self . finished { None } else { self . finished = true ; Some (mem :: take (& mut self . v)) } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T , P > Iterator for SplitMut < 'a , T , P > where P : FnMut (& T) -> bool , { type Item = & 'a mut [T] ; #[inline] fn next (& mut self) -> Option < & 'a mut [T] > { if self . finished { return None ; } match self . v . iter () . position (| x | (self . pred) (x)) { None => self . finish () , Some (idx) => { let tmp = mem :: take (& mut self . v) ; let (head , tail) = tmp . split_at_mut (idx + 1) ; self . v = tail ; Some (& mut head [.. idx]) } } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { if self . finished { (0 , Some (0)) } else { (1 , Some (self . v . len () + 1)) } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T , P > DoubleEndedIterator for SplitMut < 'a , T , P > where P : FnMut (& T) -> bool , { #[inline] fn next_back (& mut self) -> Option < & 'a mut [T] > { if self . finished { return None ; } let idx_opt = { let pred = & mut self . pred ; self . v . iter () . rposition (| x | (* pred) (x)) } ; match idx_opt { None => self . finish () , Some (idx) => { let tmp = mem :: take (& mut self . v) ; let (head , tail) = tmp . split_at_mut (idx) ; self . v = head ; Some (& mut tail [1 ..]) } } } } #[stable (feature = "fused" , since = "1.26.0")] impl < T , P > FusedIterator for SplitMut < '_ , T , P > where P : FnMut (& T) -> bool { } #[doc = " An iterator over the mutable subslices of the vector which are separated"] #[doc = " by elements that match `pred`. Unlike `SplitMut`, it contains the matched"] #[doc = " parts in the ends of the subslices."] #[doc = ""] #[doc = " This struct is created by the [`split_inclusive_mut`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let mut v = [10, 40, 30, 20, 60, 50];"] #[doc = " let iter = v.split_inclusive_mut(|num| *num % 3 == 0);"] #[doc = " ```"] #[doc = ""] #[doc = " [`split_inclusive_mut`]: slice::split_inclusive_mut"] #[doc = " [slices]: slice"] #[stable (feature = "split_inclusive" , since = "1.51.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct SplitInclusiveMut < 'a , T : 'a , P > where P : FnMut (& T) -> bool , { v : & 'a mut [T] , pred : P , finished : bool , } impl < 'a , T : 'a , P : FnMut (& T) -> bool > SplitInclusiveMut < 'a , T , P > { #[inline] pub (super) fn new (slice : & 'a mut [T] , pred : P) -> Self { let finished = slice . is_empty () ; Self { v : slice , pred , finished } } } #[stable (feature = "split_inclusive" , since = "1.51.0")] impl < T : fmt :: Debug , P > fmt :: Debug for SplitInclusiveMut < '_ , T , P > where P : FnMut (& T) -> bool , { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("SplitInclusiveMut") . field ("v" , & self . v) . field ("finished" , & self . finished) . finish () } } #[stable (feature = "split_inclusive" , since = "1.51.0")] impl < 'a , T , P > Iterator for SplitInclusiveMut < 'a , T , P > where P : FnMut (& T) -> bool , { type Item = & 'a mut [T] ; #[inline] fn next (& mut self) -> Option < & 'a mut [T] > { if self . finished { return None ; } let idx_opt = { let pred = & mut self . pred ; self . v . iter () . position (| x | (* pred) (x)) } ; let idx = idx_opt . map (| idx | idx + 1) . unwrap_or (self . v . len ()) ; if idx == self . v . len () { self . finished = true ; } let tmp = mem :: take (& mut self . v) ; let (head , tail) = tmp . split_at_mut (idx) ; self . v = tail ; Some (head) } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { if self . finished { (0 , Some (0)) } else { (1 , Some (cmp :: max (1 , self . v . len ()))) } } } #[stable (feature = "split_inclusive" , since = "1.51.0")] impl < 'a , T , P > DoubleEndedIterator for SplitInclusiveMut < 'a , T , P > where P : FnMut (& T) -> bool , { #[inline] fn next_back (& mut self) -> Option < & 'a mut [T] > { if self . finished { return None ; } let idx_opt = if self . v . is_empty () { None } else { let pred = & mut self . pred ; let remainder = & self . v [.. (self . v . len () - 1)] ; remainder . iter () . rposition (| x | (* pred) (x)) } ; let idx = idx_opt . map (| idx | idx + 1) . unwrap_or (0) ; if idx == 0 { self . finished = true ; } let tmp = mem :: take (& mut self . v) ; let (head , tail) = tmp . split_at_mut (idx) ; self . v = head ; Some (tail) } } #[stable (feature = "split_inclusive" , since = "1.51.0")] impl < T , P > FusedIterator for SplitInclusiveMut < '_ , T , P > where P : FnMut (& T) -> bool { } #[doc = " An iterator over subslices separated by elements that match a predicate"] #[doc = " function, starting from the end of the slice."] #[doc = ""] #[doc = " This struct is created by the [`rsplit`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let slice = [11, 22, 33, 0, 44, 55];"] #[doc = " let mut iter = slice.rsplit(|num| *num == 0);"] #[doc = " assert_eq!(iter.next(), Some(&[44, 55][..]));"] #[doc = " assert_eq!(iter.next(), Some(&[11, 22, 33][..]));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " [`rsplit`]: slice::rsplit"] #[doc = " [slices]: slice"] #[stable (feature = "slice_rsplit" , since = "1.27.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct RSplit < 'a , T : 'a , P > where P : FnMut (& T) -> bool , { inner : Split < 'a , T , P > , } impl < 'a , T : 'a , P : FnMut (& T) -> bool > RSplit < 'a , T , P > { #[inline] pub (super) fn new (slice : & 'a [T] , pred : P) -> Self { Self { inner : Split :: new (slice , pred) } } } #[stable (feature = "slice_rsplit" , since = "1.27.0")] impl < T : fmt :: Debug , P > fmt :: Debug for RSplit < '_ , T , P > where P : FnMut (& T) -> bool , { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("RSplit") . field ("v" , & self . inner . v) . field ("finished" , & self . inner . finished) . finish () } } #[stable (feature = "slice_rsplit" , since = "1.27.0")] impl < T , P > Clone for RSplit < '_ , T , P > where P : Clone + FnMut (& T) -> bool , { fn clone (& self) -> Self { RSplit { inner : self . inner . clone () } } } #[stable (feature = "slice_rsplit" , since = "1.27.0")] impl < 'a , T , P > Iterator for RSplit < 'a , T , P > where P : FnMut (& T) -> bool , { type Item = & 'a [T] ; #[inline] fn next (& mut self) -> Option < & 'a [T] > { self . inner . next_back () } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { self . inner . size_hint () } } #[stable (feature = "slice_rsplit" , since = "1.27.0")] impl < 'a , T , P > DoubleEndedIterator for RSplit < 'a , T , P > where P : FnMut (& T) -> bool , { #[inline] fn next_back (& mut self) -> Option < & 'a [T] > { self . inner . next () } } #[stable (feature = "slice_rsplit" , since = "1.27.0")] impl < 'a , T , P > SplitIter for RSplit < 'a , T , P > where P : FnMut (& T) -> bool , { #[inline] fn finish (& mut self) -> Option < & 'a [T] > { self . inner . finish () } } #[stable (feature = "slice_rsplit" , since = "1.27.0")] impl < T , P > FusedIterator for RSplit < '_ , T , P > where P : FnMut (& T) -> bool { } #[doc = " An iterator over the subslices of the vector which are separated"] #[doc = " by elements that match `pred`, starting from the end of the slice."] #[doc = ""] #[doc = " This struct is created by the [`rsplit_mut`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let mut slice = [11, 22, 33, 0, 44, 55];"] #[doc = " let iter = slice.rsplit_mut(|num| *num == 0);"] #[doc = " ```"] #[doc = ""] #[doc = " [`rsplit_mut`]: slice::rsplit_mut"] #[doc = " [slices]: slice"] #[stable (feature = "slice_rsplit" , since = "1.27.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct RSplitMut < 'a , T : 'a , P > where P : FnMut (& T) -> bool , { inner : SplitMut < 'a , T , P > , } impl < 'a , T : 'a , P : FnMut (& T) -> bool > RSplitMut < 'a , T , P > { #[inline] pub (super) fn new (slice : & 'a mut [T] , pred : P) -> Self { Self { inner : SplitMut :: new (slice , pred) } } } #[stable (feature = "slice_rsplit" , since = "1.27.0")] impl < T : fmt :: Debug , P > fmt :: Debug for RSplitMut < '_ , T , P > where P : FnMut (& T) -> bool , { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("RSplitMut") . field ("v" , & self . inner . v) . field ("finished" , & self . inner . finished) . finish () } } #[stable (feature = "slice_rsplit" , since = "1.27.0")] impl < 'a , T , P > SplitIter for RSplitMut < 'a , T , P > where P : FnMut (& T) -> bool , { #[inline] fn finish (& mut self) -> Option < & 'a mut [T] > { self . inner . finish () } } #[stable (feature = "slice_rsplit" , since = "1.27.0")] impl < 'a , T , P > Iterator for RSplitMut < 'a , T , P > where P : FnMut (& T) -> bool , { type Item = & 'a mut [T] ; #[inline] fn next (& mut self) -> Option < & 'a mut [T] > { self . inner . next_back () } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { self . inner . size_hint () } } #[stable (feature = "slice_rsplit" , since = "1.27.0")] impl < 'a , T , P > DoubleEndedIterator for RSplitMut < 'a , T , P > where P : FnMut (& T) -> bool , { #[inline] fn next_back (& mut self) -> Option < & 'a mut [T] > { self . inner . next () } } #[stable (feature = "slice_rsplit" , since = "1.27.0")] impl < T , P > FusedIterator for RSplitMut < '_ , T , P > where P : FnMut (& T) -> bool { } #[doc = " An private iterator over subslices separated by elements that"] #[doc = " match a predicate function, splitting at most a fixed number of"] #[doc = " times."] #[derive (Debug)] struct GenericSplitN < I > { iter : I , count : usize , } impl < T , I : SplitIter < Item = T > > Iterator for GenericSplitN < I > { type Item = T ; #[inline] fn next (& mut self) -> Option < T > { match self . count { 0 => None , 1 => { self . count -= 1 ; self . iter . finish () } _ => { self . count -= 1 ; self . iter . next () } } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { let (lower , upper_opt) = self . iter . size_hint () ; (cmp :: min (self . count , lower) , Some (upper_opt . map_or (self . count , | upper | cmp :: min (self . count , upper))) ,) } } #[doc = " An iterator over subslices separated by elements that match a predicate"] #[doc = " function, limited to a given number of splits."] #[doc = ""] #[doc = " This struct is created by the [`splitn`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let slice = [10, 40, 30, 20, 60, 50];"] #[doc = " let mut iter = slice.splitn(2, |num| *num % 3 == 0);"] #[doc = " assert_eq!(iter.next(), Some(&[10, 40][..]));"] #[doc = " assert_eq!(iter.next(), Some(&[20, 60, 50][..]));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " [`splitn`]: slice::splitn"] #[doc = " [slices]: slice"] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct SplitN < 'a , T : 'a , P > where P : FnMut (& T) -> bool , { inner : GenericSplitN < Split < 'a , T , P > > , } impl < 'a , T : 'a , P : FnMut (& T) -> bool > SplitN < 'a , T , P > { #[inline] pub (super) fn new (s : Split < 'a , T , P > , n : usize) -> Self { Self { inner : GenericSplitN { iter : s , count : n } } } } #[stable (feature = "core_impl_debug" , since = "1.9.0")] impl < T : fmt :: Debug , P > fmt :: Debug for SplitN < '_ , T , P > where P : FnMut (& T) -> bool , { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("SplitN") . field ("inner" , & self . inner) . finish () } } #[doc = " An iterator over subslices separated by elements that match a"] #[doc = " predicate function, limited to a given number of splits, starting"] #[doc = " from the end of the slice."] #[doc = ""] #[doc = " This struct is created by the [`rsplitn`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let slice = [10, 40, 30, 20, 60, 50];"] #[doc = " let mut iter = slice.rsplitn(2, |num| *num % 3 == 0);"] #[doc = " assert_eq!(iter.next(), Some(&[50][..]));"] #[doc = " assert_eq!(iter.next(), Some(&[10, 40, 30, 20][..]));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " [`rsplitn`]: slice::rsplitn"] #[doc = " [slices]: slice"] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct RSplitN < 'a , T : 'a , P > where P : FnMut (& T) -> bool , { inner : GenericSplitN < RSplit < 'a , T , P > > , } impl < 'a , T : 'a , P : FnMut (& T) -> bool > RSplitN < 'a , T , P > { #[inline] pub (super) fn new (s : RSplit < 'a , T , P > , n : usize) -> Self { Self { inner : GenericSplitN { iter : s , count : n } } } } #[stable (feature = "core_impl_debug" , since = "1.9.0")] impl < T : fmt :: Debug , P > fmt :: Debug for RSplitN < '_ , T , P > where P : FnMut (& T) -> bool , { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("RSplitN") . field ("inner" , & self . inner) . finish () } } #[doc = " An iterator over subslices separated by elements that match a predicate"] #[doc = " function, limited to a given number of splits."] #[doc = ""] #[doc = " This struct is created by the [`splitn_mut`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let mut slice = [10, 40, 30, 20, 60, 50];"] #[doc = " let iter = slice.splitn_mut(2, |num| *num % 3 == 0);"] #[doc = " ```"] #[doc = ""] #[doc = " [`splitn_mut`]: slice::splitn_mut"] #[doc = " [slices]: slice"] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct SplitNMut < 'a , T : 'a , P > where P : FnMut (& T) -> bool , { inner : GenericSplitN < SplitMut < 'a , T , P > > , } impl < 'a , T : 'a , P : FnMut (& T) -> bool > SplitNMut < 'a , T , P > { #[inline] pub (super) fn new (s : SplitMut < 'a , T , P > , n : usize) -> Self { Self { inner : GenericSplitN { iter : s , count : n } } } } #[stable (feature = "core_impl_debug" , since = "1.9.0")] impl < T : fmt :: Debug , P > fmt :: Debug for SplitNMut < '_ , T , P > where P : FnMut (& T) -> bool , { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("SplitNMut") . field ("inner" , & self . inner) . finish () } } #[doc = " An iterator over subslices separated by elements that match a"] #[doc = " predicate function, limited to a given number of splits, starting"] #[doc = " from the end of the slice."] #[doc = ""] #[doc = " This struct is created by the [`rsplitn_mut`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let mut slice = [10, 40, 30, 20, 60, 50];"] #[doc = " let iter = slice.rsplitn_mut(2, |num| *num % 3 == 0);"] #[doc = " ```"] #[doc = ""] #[doc = " [`rsplitn_mut`]: slice::rsplitn_mut"] #[doc = " [slices]: slice"] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct RSplitNMut < 'a , T : 'a , P > where P : FnMut (& T) -> bool , { inner : GenericSplitN < RSplitMut < 'a , T , P > > , } impl < 'a , T : 'a , P : FnMut (& T) -> bool > RSplitNMut < 'a , T , P > { #[inline] pub (super) fn new (s : RSplitMut < 'a , T , P > , n : usize) -> Self { Self { inner : GenericSplitN { iter : s , count : n } } } } #[stable (feature = "core_impl_debug" , since = "1.9.0")] impl < T : fmt :: Debug , P > fmt :: Debug for RSplitNMut < '_ , T , P > where P : FnMut (& T) -> bool , { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("RSplitNMut") . field ("inner" , & self . inner) . finish () } } forward_iterator ! { SplitN : T , &'a [T] } forward_iterator ! { RSplitN : T , &'a [T] } forward_iterator ! { SplitNMut : T , &'a mut [T] } forward_iterator ! { RSplitNMut : T , &'a mut [T] } #[doc = " An iterator over overlapping subslices of length `size`."] #[doc = ""] #[doc = " This struct is created by the [`windows`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let slice = ['r', 'u', 's', 't'];"] #[doc = " let mut iter = slice.windows(2);"] #[doc = " assert_eq!(iter.next(), Some(&['r', 'u'][..]));"] #[doc = " assert_eq!(iter.next(), Some(&['u', 's'][..]));"] #[doc = " assert_eq!(iter.next(), Some(&['s', 't'][..]));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " [`windows`]: slice::windows"] #[doc = " [slices]: slice"] #[derive (Debug)] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct Windows < 'a , T : 'a > { v : & 'a [T] , size : NonZero < usize > , } impl < 'a , T : 'a > Windows < 'a , T > { #[inline] pub (super) const fn new (slice : & 'a [T] , size : NonZero < usize >) -> Self { Self { v : slice , size } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < T > Clone for Windows < '_ , T > { fn clone (& self) -> Self { Windows { v : self . v , size : self . size } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T > Iterator for Windows < 'a , T > { type Item = & 'a [T] ; #[inline] fn next (& mut self) -> Option < & 'a [T] > { if self . size . get () > self . v . len () { None } else { let ret = Some (& self . v [.. self . size . get ()]) ; self . v = & self . v [1 ..] ; ret } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { if self . size . get () > self . v . len () { (0 , Some (0)) } else { let size = self . v . len () - self . size . get () + 1 ; (size , Some (size)) } } #[inline] fn count (self) -> usize { self . len () } #[inline] fn nth (& mut self , n : usize) -> Option < Self :: Item > { let size = self . size . get () ; if let Some (rest) = self . v . get (n ..) && let Some (nth) = rest . get (.. size) { self . v = & rest [1 ..] ; Some (nth) } else { self . v = & self . v [.. 0] ; None } } #[inline] fn last (self) -> Option < Self :: Item > { if self . size . get () > self . v . len () { None } else { let start = self . v . len () - self . size . get () ; Some (& self . v [start ..]) } } unsafe fn __iterator_get_unchecked (& mut self , idx : usize) -> Self :: Item { unsafe { from_raw_parts (self . v . as_ptr () . add (idx) , self . size . get ()) } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T > DoubleEndedIterator for Windows < 'a , T > { #[inline] fn next_back (& mut self) -> Option < & 'a [T] > { if self . size . get () > self . v . len () { None } else { let ret = Some (& self . v [self . v . len () - self . size . get () ..]) ; self . v = & self . v [.. self . v . len () - 1] ; ret } } #[inline] fn nth_back (& mut self , n : usize) -> Option < Self :: Item > { let (end , overflow) = self . v . len () . overflowing_sub (n) ; if end < self . size . get () || overflow { self . v = & self . v [.. 0] ; None } else { let ret = & self . v [end - self . size . get () .. end] ; self . v = & self . v [.. end - 1] ; Some (ret) } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < T > ExactSizeIterator for Windows < '_ , T > { } #[unstable (feature = "trusted_len" , issue = "37572")] unsafe impl < T > TrustedLen for Windows < '_ , T > { } #[stable (feature = "fused" , since = "1.26.0")] impl < T > FusedIterator for Windows < '_ , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccess for Windows < 'a , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccessNoCoerce for Windows < 'a , T > { const MAY_HAVE_SIDE_EFFECT : bool = false ; } #[doc = " An iterator over a slice in (non-overlapping) chunks (`chunk_size` elements at a"] #[doc = " time), starting at the beginning of the slice."] #[doc = ""] #[doc = " When the slice len is not evenly divided by the chunk size, the last slice"] #[doc = " of the iteration will be the remainder."] #[doc = ""] #[doc = " This struct is created by the [`chunks`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let slice = ['l', 'o', 'r', 'e', 'm'];"] #[doc = " let mut iter = slice.chunks(2);"] #[doc = " assert_eq!(iter.next(), Some(&['l', 'o'][..]));"] #[doc = " assert_eq!(iter.next(), Some(&['r', 'e'][..]));"] #[doc = " assert_eq!(iter.next(), Some(&['m'][..]));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " [`chunks`]: slice::chunks"] #[doc = " [slices]: slice"] #[derive (Debug)] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct Chunks < 'a , T : 'a > { v : & 'a [T] , chunk_size : usize , } impl < 'a , T : 'a > Chunks < 'a , T > { #[inline] pub (super) const fn new (slice : & 'a [T] , size : usize) -> Self { Self { v : slice , chunk_size : size } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < T > Clone for Chunks < '_ , T > { fn clone (& self) -> Self { Chunks { v : self . v , chunk_size : self . chunk_size } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T > Iterator for Chunks < 'a , T > { type Item = & 'a [T] ; #[inline] fn next (& mut self) -> Option < & 'a [T] > { if self . v . is_empty () { None } else { let chunksz = cmp :: min (self . v . len () , self . chunk_size) ; let (fst , snd) = self . v . split_at (chunksz) ; self . v = snd ; Some (fst) } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { if self . v . is_empty () { (0 , Some (0)) } else { let n = self . v . len () / self . chunk_size ; let rem = self . v . len () % self . chunk_size ; let n = if rem > 0 { n + 1 } else { n } ; (n , Some (n)) } } #[inline] fn count (self) -> usize { self . len () } #[inline] fn nth (& mut self , n : usize) -> Option < Self :: Item > { let (start , overflow) = n . overflowing_mul (self . chunk_size) ; let chunk_start = & self . v [start . min (self . v . len ()) ..] ; let (nth , remainder) = chunk_start . split_at (self . chunk_size . min (chunk_start . len ())) ; if ! overflow && start < self . v . len () { self . v = remainder ; Some (nth) } else { self . v = & self . v [.. 0] ; None } } #[inline] fn last (self) -> Option < Self :: Item > { if self . v . is_empty () { None } else { let start = (self . v . len () - 1) / self . chunk_size * self . chunk_size ; Some (& self . v [start ..]) } } unsafe fn __iterator_get_unchecked (& mut self , idx : usize) -> Self :: Item { let start = idx * self . chunk_size ; unsafe { let len = cmp :: min (self . v . len () . unchecked_sub (start) , self . chunk_size) ; from_raw_parts (self . v . as_ptr () . add (start) , len) } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T > DoubleEndedIterator for Chunks < 'a , T > { #[inline] fn next_back (& mut self) -> Option < & 'a [T] > { if self . v . is_empty () { None } else { let remainder = self . v . len () % self . chunk_size ; let chunksz = if remainder != 0 { remainder } else { self . chunk_size } ; let (fst , snd) = unsafe { self . v . split_at_unchecked (self . v . len () - chunksz) } ; self . v = fst ; Some (snd) } } #[inline] fn nth_back (& mut self , n : usize) -> Option < Self :: Item > { let len = self . len () ; if n >= len { self . v = & self . v [.. 0] ; None } else { let start = (len - 1 - n) * self . chunk_size ; let end = match start . checked_add (self . chunk_size) { Some (res) => cmp :: min (self . v . len () , res) , None => self . v . len () , } ; let nth_back = & self . v [start .. end] ; self . v = & self . v [.. start] ; Some (nth_back) } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < T > ExactSizeIterator for Chunks < '_ , T > { } #[unstable (feature = "trusted_len" , issue = "37572")] unsafe impl < T > TrustedLen for Chunks < '_ , T > { } #[stable (feature = "fused" , since = "1.26.0")] impl < T > FusedIterator for Chunks < '_ , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccess for Chunks < 'a , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccessNoCoerce for Chunks < 'a , T > { const MAY_HAVE_SIDE_EFFECT : bool = false ; } #[doc = " An iterator over a slice in (non-overlapping) mutable chunks (`chunk_size`"] #[doc = " elements at a time), starting at the beginning of the slice."] #[doc = ""] #[doc = " When the slice len is not evenly divided by the chunk size, the last slice"] #[doc = " of the iteration will be the remainder."] #[doc = ""] #[doc = " This struct is created by the [`chunks_mut`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let mut slice = ['l', 'o', 'r', 'e', 'm'];"] #[doc = " let iter = slice.chunks_mut(2);"] #[doc = " ```"] #[doc = ""] #[doc = " [`chunks_mut`]: slice::chunks_mut"] #[doc = " [slices]: slice"] #[derive (Debug)] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct ChunksMut < 'a , T : 'a > { #[doc = " # Safety"] #[doc = " This slice pointer must point at a valid region of `T` with at least length `v.len()`. Normally,"] #[doc = " those requirements would mean that we could instead use a `&mut [T]` here, but we cannot"] #[doc = " because `__iterator_get_unchecked` needs to return `&mut [T]`, which guarantees certain aliasing"] #[doc = " properties that we cannot uphold if we hold on to the full original `&mut [T]`. Wrapping a raw"] #[doc = " slice instead lets us hand out non-overlapping `&mut [T]` subslices of the slice we wrap."] v : * mut [T] , chunk_size : usize , _marker : PhantomData < & 'a mut T > , } impl < 'a , T : 'a > ChunksMut < 'a , T > { #[inline] pub (super) const fn new (slice : & 'a mut [T] , size : usize) -> Self { Self { v : slice , chunk_size : size , _marker : PhantomData } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T > Iterator for ChunksMut < 'a , T > { type Item = & 'a mut [T] ; #[inline] fn next (& mut self) -> Option < & 'a mut [T] > { if self . v . is_empty () { None } else { let sz = cmp :: min (self . v . len () , self . chunk_size) ; let (head , tail) = unsafe { self . v . split_at_mut (sz) } ; self . v = tail ; Some (unsafe { & mut * head }) } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { if self . v . is_empty () { (0 , Some (0)) } else { let n = self . v . len () / self . chunk_size ; let rem = self . v . len () % self . chunk_size ; let n = if rem > 0 { n + 1 } else { n } ; (n , Some (n)) } } #[inline] fn count (self) -> usize { self . len () } #[inline] fn nth (& mut self , n : usize) -> Option < & 'a mut [T] > { let (start , overflow) = n . overflowing_mul (self . chunk_size) ; if start >= self . v . len () || overflow { self . v = & mut [] ; None } else { let end = match start . checked_add (self . chunk_size) { Some (sum) => cmp :: min (self . v . len () , sum) , None => self . v . len () , } ; let (head , tail) = unsafe { self . v . split_at_mut (end) } ; let (_ , nth) = unsafe { head . split_at_mut (start) } ; self . v = tail ; Some (unsafe { & mut * nth }) } } #[inline] fn last (self) -> Option < Self :: Item > { if self . v . is_empty () { None } else { let start = (self . v . len () - 1) / self . chunk_size * self . chunk_size ; Some (unsafe { & mut * self . v . get_unchecked_mut (start ..) }) } } unsafe fn __iterator_get_unchecked (& mut self , idx : usize) -> Self :: Item { let start = idx * self . chunk_size ; unsafe { let len = cmp :: min (self . v . len () . unchecked_sub (start) , self . chunk_size) ; from_raw_parts_mut (self . v . as_mut_ptr () . add (start) , len) } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T > DoubleEndedIterator for ChunksMut < 'a , T > { #[inline] fn next_back (& mut self) -> Option < & 'a mut [T] > { if self . v . is_empty () { None } else { let remainder = self . v . len () % self . chunk_size ; let sz = if remainder != 0 { remainder } else { self . chunk_size } ; let len = self . v . len () ; let (head , tail) = unsafe { self . v . split_at_mut_unchecked (len - sz) } ; self . v = head ; Some (unsafe { & mut * tail }) } } #[inline] fn nth_back (& mut self , n : usize) -> Option < Self :: Item > { let len = self . len () ; if n >= len { self . v = & mut [] ; None } else { let start = (len - 1 - n) * self . chunk_size ; let end = match start . checked_add (self . chunk_size) { Some (res) => cmp :: min (self . v . len () , res) , None => self . v . len () , } ; let (temp , _tail) = unsafe { self . v . split_at_mut (end) } ; let (head , nth_back) = unsafe { temp . split_at_mut (start) } ; self . v = head ; Some (unsafe { & mut * nth_back }) } } } #[stable (feature = "rust1" , since = "1.0.0")] impl < T > ExactSizeIterator for ChunksMut < '_ , T > { } #[unstable (feature = "trusted_len" , issue = "37572")] unsafe impl < T > TrustedLen for ChunksMut < '_ , T > { } #[stable (feature = "fused" , since = "1.26.0")] impl < T > FusedIterator for ChunksMut < '_ , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccess for ChunksMut < 'a , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccessNoCoerce for ChunksMut < 'a , T > { const MAY_HAVE_SIDE_EFFECT : bool = false ; } #[stable (feature = "rust1" , since = "1.0.0")] unsafe impl < T > Send for ChunksMut < '_ , T > where T : Send { } #[stable (feature = "rust1" , since = "1.0.0")] unsafe impl < T > Sync for ChunksMut < '_ , T > where T : Sync { } #[doc = " An iterator over a slice in (non-overlapping) chunks (`chunk_size` elements at a"] #[doc = " time), starting at the beginning of the slice."] #[doc = ""] #[doc = " When the slice len is not evenly divided by the chunk size, the last"] #[doc = " up to `chunk_size-1` elements will be omitted but can be retrieved from"] #[doc = " the [`remainder`] function from the iterator."] #[doc = ""] #[doc = " This struct is created by the [`chunks_exact`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let slice = ['l', 'o', 'r', 'e', 'm'];"] #[doc = " let mut iter = slice.chunks_exact(2);"] #[doc = " assert_eq!(iter.next(), Some(&['l', 'o'][..]));"] #[doc = " assert_eq!(iter.next(), Some(&['r', 'e'][..]));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " [`chunks_exact`]: slice::chunks_exact"] #[doc = " [`remainder`]: ChunksExact::remainder"] #[doc = " [slices]: slice"] #[derive (Debug)] #[stable (feature = "chunks_exact" , since = "1.31.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct ChunksExact < 'a , T : 'a > { v : & 'a [T] , rem : & 'a [T] , chunk_size : usize , } impl < 'a , T > ChunksExact < 'a , T > { #[inline] pub (super) const fn new (slice : & 'a [T] , chunk_size : usize) -> Self { let rem = slice . len () % chunk_size ; let fst_len = slice . len () - rem ; let (fst , snd) = unsafe { slice . split_at_unchecked (fst_len) } ; Self { v : fst , rem : snd , chunk_size } } #[doc = " Returns the remainder of the original slice that is not going to be"] #[doc = " returned by the iterator. The returned slice has at most `chunk_size-1`"] #[doc = " elements."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let slice = ['l', 'o', 'r', 'e', 'm'];"] #[doc = " let mut iter = slice.chunks_exact(2);"] #[doc = " assert_eq!(iter.remainder(), &['m'][..]);"] #[doc = " assert_eq!(iter.next(), Some(&['l', 'o'][..]));"] #[doc = " assert_eq!(iter.remainder(), &['m'][..]);"] #[doc = " assert_eq!(iter.next(), Some(&['r', 'e'][..]));"] #[doc = " assert_eq!(iter.remainder(), &['m'][..]);"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " assert_eq!(iter.remainder(), &['m'][..]);"] #[doc = " ```"] #[must_use] #[stable (feature = "chunks_exact" , since = "1.31.0")] pub fn remainder (& self) -> & 'a [T] { self . rem } } #[stable (feature = "chunks_exact" , since = "1.31.0")] impl < T > Clone for ChunksExact < '_ , T > { fn clone (& self) -> Self { ChunksExact { v : self . v , rem : self . rem , chunk_size : self . chunk_size } } } #[stable (feature = "chunks_exact" , since = "1.31.0")] impl < 'a , T > Iterator for ChunksExact < 'a , T > { type Item = & 'a [T] ; #[inline] fn next (& mut self) -> Option < & 'a [T] > { if self . v . len () < self . chunk_size { None } else { let (fst , snd) = self . v . split_at (self . chunk_size) ; self . v = snd ; Some (fst) } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { let n = self . v . len () / self . chunk_size ; (n , Some (n)) } #[inline] fn count (self) -> usize { self . len () } #[inline] fn nth (& mut self , n : usize) -> Option < Self :: Item > { let (start , overflow) = n . overflowing_mul (self . chunk_size) ; if start >= self . v . len () || overflow { self . v = & self . v [.. 0] ; None } else { let (_ , snd) = self . v . split_at (start) ; self . v = snd ; self . next () } } #[inline] fn last (mut self) -> Option < Self :: Item > { self . next_back () } unsafe fn __iterator_get_unchecked (& mut self , idx : usize) -> Self :: Item { let start = idx * self . chunk_size ; unsafe { from_raw_parts (self . v . as_ptr () . add (start) , self . chunk_size) } } } #[stable (feature = "chunks_exact" , since = "1.31.0")] impl < 'a , T > DoubleEndedIterator for ChunksExact < 'a , T > { #[inline] fn next_back (& mut self) -> Option < & 'a [T] > { if self . v . len () < self . chunk_size { None } else { let (fst , snd) = self . v . split_at (self . v . len () - self . chunk_size) ; self . v = fst ; Some (snd) } } #[inline] fn nth_back (& mut self , n : usize) -> Option < Self :: Item > { let len = self . len () ; if n >= len { self . v = & self . v [.. 0] ; None } else { let start = (len - 1 - n) * self . chunk_size ; let end = start + self . chunk_size ; let nth_back = & self . v [start .. end] ; self . v = & self . v [.. start] ; Some (nth_back) } } } #[stable (feature = "chunks_exact" , since = "1.31.0")] impl < T > ExactSizeIterator for ChunksExact < '_ , T > { fn is_empty (& self) -> bool { self . v . is_empty () } } #[unstable (feature = "trusted_len" , issue = "37572")] unsafe impl < T > TrustedLen for ChunksExact < '_ , T > { } #[stable (feature = "chunks_exact" , since = "1.31.0")] impl < T > FusedIterator for ChunksExact < '_ , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccess for ChunksExact < 'a , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccessNoCoerce for ChunksExact < 'a , T > { const MAY_HAVE_SIDE_EFFECT : bool = false ; } #[doc = " An iterator over a slice in (non-overlapping) mutable chunks (`chunk_size`"] #[doc = " elements at a time), starting at the beginning of the slice."] #[doc = ""] #[doc = " When the slice len is not evenly divided by the chunk size, the last up to"] #[doc = " `chunk_size-1` elements will be omitted but can be retrieved from the"] #[doc = " [`into_remainder`] function from the iterator."] #[doc = ""] #[doc = " This struct is created by the [`chunks_exact_mut`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let mut slice = ['l', 'o', 'r', 'e', 'm'];"] #[doc = " let iter = slice.chunks_exact_mut(2);"] #[doc = " ```"] #[doc = ""] #[doc = " [`chunks_exact_mut`]: slice::chunks_exact_mut"] #[doc = " [`into_remainder`]: ChunksExactMut::into_remainder"] #[doc = " [slices]: slice"] #[derive (Debug)] #[stable (feature = "chunks_exact" , since = "1.31.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct ChunksExactMut < 'a , T : 'a > { #[doc = " # Safety"] #[doc = " This slice pointer must point at a valid region of `T` with at least length `v.len()`. Normally,"] #[doc = " those requirements would mean that we could instead use a `&mut [T]` here, but we cannot"] #[doc = " because `__iterator_get_unchecked` needs to return `&mut [T]`, which guarantees certain aliasing"] #[doc = " properties that we cannot uphold if we hold on to the full original `&mut [T]`. Wrapping a raw"] #[doc = " slice instead lets us hand out non-overlapping `&mut [T]` subslices of the slice we wrap."] v : * mut [T] , rem : & 'a mut [T] , chunk_size : usize , _marker : PhantomData < & 'a mut T > , } impl < 'a , T > ChunksExactMut < 'a , T > { #[inline] pub (super) const fn new (slice : & 'a mut [T] , chunk_size : usize) -> Self { let rem = slice . len () % chunk_size ; let fst_len = slice . len () - rem ; let (fst , snd) = unsafe { slice . split_at_mut_unchecked (fst_len) } ; Self { v : fst , rem : snd , chunk_size , _marker : PhantomData } } #[doc = " Returns the remainder of the original slice that is not going to be"] #[doc = " returned by the iterator. The returned slice has at most `chunk_size-1`"] #[doc = " elements."] #[must_use = "`self` will be dropped if the result is not used"] #[stable (feature = "chunks_exact" , since = "1.31.0")] pub fn into_remainder (self) -> & 'a mut [T] { self . rem } } #[stable (feature = "chunks_exact" , since = "1.31.0")] impl < 'a , T > Iterator for ChunksExactMut < 'a , T > { type Item = & 'a mut [T] ; #[inline] fn next (& mut self) -> Option < & 'a mut [T] > { if self . v . len () < self . chunk_size { None } else { let (head , tail) = unsafe { self . v . split_at_mut (self . chunk_size) } ; self . v = tail ; Some (unsafe { & mut * head }) } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { let n = self . v . len () / self . chunk_size ; (n , Some (n)) } #[inline] fn count (self) -> usize { self . len () } #[inline] fn nth (& mut self , n : usize) -> Option < & 'a mut [T] > { let (start , overflow) = n . overflowing_mul (self . chunk_size) ; if start >= self . v . len () || overflow { self . v = & mut [] ; None } else { let (_ , snd) = unsafe { self . v . split_at_mut (start) } ; self . v = snd ; self . next () } } #[inline] fn last (mut self) -> Option < Self :: Item > { self . next_back () } unsafe fn __iterator_get_unchecked (& mut self , idx : usize) -> Self :: Item { let start = idx * self . chunk_size ; unsafe { from_raw_parts_mut (self . v . as_mut_ptr () . add (start) , self . chunk_size) } } } #[stable (feature = "chunks_exact" , since = "1.31.0")] impl < 'a , T > DoubleEndedIterator for ChunksExactMut < 'a , T > { #[inline] fn next_back (& mut self) -> Option < & 'a mut [T] > { if self . v . len () < self . chunk_size { None } else { let (head , tail) = unsafe { self . v . split_at_mut (self . v . len () - self . chunk_size) } ; self . v = head ; Some (unsafe { & mut * tail }) } } #[inline] fn nth_back (& mut self , n : usize) -> Option < Self :: Item > { let len = self . len () ; if n >= len { self . v = & mut [] ; None } else { let start = (len - 1 - n) * self . chunk_size ; let end = start + self . chunk_size ; let (temp , _tail) = unsafe { mem :: replace (& mut self . v , & mut []) . split_at_mut (end) } ; let (head , nth_back) = unsafe { temp . split_at_mut (start) } ; self . v = head ; Some (unsafe { & mut * nth_back }) } } } #[stable (feature = "chunks_exact" , since = "1.31.0")] impl < T > ExactSizeIterator for ChunksExactMut < '_ , T > { fn is_empty (& self) -> bool { self . v . is_empty () } } #[unstable (feature = "trusted_len" , issue = "37572")] unsafe impl < T > TrustedLen for ChunksExactMut < '_ , T > { } #[stable (feature = "chunks_exact" , since = "1.31.0")] impl < T > FusedIterator for ChunksExactMut < '_ , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccess for ChunksExactMut < 'a , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccessNoCoerce for ChunksExactMut < 'a , T > { const MAY_HAVE_SIDE_EFFECT : bool = false ; } #[stable (feature = "chunks_exact" , since = "1.31.0")] unsafe impl < T > Send for ChunksExactMut < '_ , T > where T : Send { } #[stable (feature = "chunks_exact" , since = "1.31.0")] unsafe impl < T > Sync for ChunksExactMut < '_ , T > where T : Sync { } #[doc = " A windowed iterator over a slice in overlapping chunks (`N` elements at a"] #[doc = " time), starting at the beginning of the slice"] #[doc = ""] #[doc = " This struct is created by the [`array_windows`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(array_windows)]"] #[doc = ""] #[doc = " let slice = [0, 1, 2, 3];"] #[doc = " let mut iter = slice.array_windows::<2>();"] #[doc = " assert_eq!(iter.next(), Some(&[0, 1]));"] #[doc = " assert_eq!(iter.next(), Some(&[1, 2]));"] #[doc = " assert_eq!(iter.next(), Some(&[2, 3]));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " [`array_windows`]: slice::array_windows"] #[doc = " [slices]: slice"] #[derive (Debug , Clone , Copy)] #[unstable (feature = "array_windows" , issue = "75027")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct ArrayWindows < 'a , T : 'a , const N : usize > { slice_head : * const T , num : usize , marker : PhantomData < & 'a [T ; N] > , } impl < 'a , T : 'a , const N : usize > ArrayWindows < 'a , T , N > { #[inline] pub (super) const fn new (slice : & 'a [T]) -> Self { let num_windows = slice . len () . saturating_sub (N - 1) ; Self { slice_head : slice . as_ptr () , num : num_windows , marker : PhantomData } } } #[unstable (feature = "array_windows" , issue = "75027")] impl < 'a , T , const N : usize > Iterator for ArrayWindows < 'a , T , N > { type Item = & 'a [T ; N] ; #[inline] fn next (& mut self) -> Option < Self :: Item > { if self . num == 0 { return None ; } let ret = unsafe { & * self . slice_head . cast :: < [T ; N] > () } ; self . slice_head = unsafe { self . slice_head . add (1) } ; self . num -= 1 ; Some (ret) } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { (self . num , Some (self . num)) } #[inline] fn count (self) -> usize { self . num } #[inline] fn nth (& mut self , n : usize) -> Option < Self :: Item > { if self . num <= n { self . num = 0 ; return None ; } let ret = unsafe { & * self . slice_head . add (n) . cast :: < [T ; N] > () } ; self . slice_head = unsafe { self . slice_head . add (n + 1) } ; self . num -= n + 1 ; Some (ret) } #[inline] fn last (mut self) -> Option < Self :: Item > { self . nth (self . num . checked_sub (1) ?) } } #[unstable (feature = "array_windows" , issue = "75027")] impl < 'a , T , const N : usize > DoubleEndedIterator for ArrayWindows < 'a , T , N > { #[inline] fn next_back (& mut self) -> Option < & 'a [T ; N] > { if self . num == 0 { return None ; } let ret = unsafe { & * self . slice_head . add (self . num - 1) . cast :: < [T ; N] > () } ; self . num -= 1 ; Some (ret) } #[inline] fn nth_back (& mut self , n : usize) -> Option < & 'a [T ; N] > { if self . num <= n { self . num = 0 ; return None ; } let ret = unsafe { & * self . slice_head . add (self . num - (n + 1)) . cast :: < [T ; N] > () } ; self . num -= n + 1 ; Some (ret) } } #[unstable (feature = "array_windows" , issue = "75027")] impl < T , const N : usize > ExactSizeIterator for ArrayWindows < '_ , T , N > { fn is_empty (& self) -> bool { self . num == 0 } } #[doc = " An iterator over a slice in (non-overlapping) chunks (`chunk_size` elements at a"] #[doc = " time), starting at the end of the slice."] #[doc = ""] #[doc = " When the slice len is not evenly divided by the chunk size, the last slice"] #[doc = " of the iteration will be the remainder."] #[doc = ""] #[doc = " This struct is created by the [`rchunks`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let slice = ['l', 'o', 'r', 'e', 'm'];"] #[doc = " let mut iter = slice.rchunks(2);"] #[doc = " assert_eq!(iter.next(), Some(&['e', 'm'][..]));"] #[doc = " assert_eq!(iter.next(), Some(&['o', 'r'][..]));"] #[doc = " assert_eq!(iter.next(), Some(&['l'][..]));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " [`rchunks`]: slice::rchunks"] #[doc = " [slices]: slice"] #[derive (Debug)] #[stable (feature = "rchunks" , since = "1.31.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct RChunks < 'a , T : 'a > { v : & 'a [T] , chunk_size : usize , } impl < 'a , T : 'a > RChunks < 'a , T > { #[inline] pub (super) const fn new (slice : & 'a [T] , size : usize) -> Self { Self { v : slice , chunk_size : size } } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < T > Clone for RChunks < '_ , T > { fn clone (& self) -> Self { RChunks { v : self . v , chunk_size : self . chunk_size } } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < 'a , T > Iterator for RChunks < 'a , T > { type Item = & 'a [T] ; #[inline] fn next (& mut self) -> Option < & 'a [T] > { if self . v . is_empty () { None } else { let len = self . v . len () ; let chunksz = cmp :: min (len , self . chunk_size) ; let (fst , snd) = unsafe { self . v . split_at_unchecked (len - chunksz) } ; self . v = fst ; Some (snd) } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { if self . v . is_empty () { (0 , Some (0)) } else { let n = self . v . len () / self . chunk_size ; let rem = self . v . len () % self . chunk_size ; let n = if rem > 0 { n + 1 } else { n } ; (n , Some (n)) } } #[inline] fn count (self) -> usize { self . len () } #[inline] fn nth (& mut self , n : usize) -> Option < Self :: Item > { let (end , overflow) = n . overflowing_mul (self . chunk_size) ; if end >= self . v . len () || overflow { self . v = & self . v [.. 0] ; None } else { let end = self . v . len () - end ; let start = match end . checked_sub (self . chunk_size) { Some (sum) => sum , None => 0 , } ; let nth = & self . v [start .. end] ; self . v = & self . v [0 .. start] ; Some (nth) } } #[inline] fn last (self) -> Option < Self :: Item > { if self . v . is_empty () { None } else { let rem = self . v . len () % self . chunk_size ; let end = if rem == 0 { self . chunk_size } else { rem } ; Some (& self . v [0 .. end]) } } unsafe fn __iterator_get_unchecked (& mut self , idx : usize) -> Self :: Item { let end = self . v . len () - idx * self . chunk_size ; let start = match end . checked_sub (self . chunk_size) { None => 0 , Some (start) => start , } ; unsafe { from_raw_parts (self . v . as_ptr () . add (start) , end - start) } } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < 'a , T > DoubleEndedIterator for RChunks < 'a , T > { #[inline] fn next_back (& mut self) -> Option < & 'a [T] > { if self . v . is_empty () { None } else { let remainder = self . v . len () % self . chunk_size ; let chunksz = if remainder != 0 { remainder } else { self . chunk_size } ; let (fst , snd) = unsafe { self . v . split_at_unchecked (chunksz) } ; self . v = snd ; Some (fst) } } #[inline] fn nth_back (& mut self , n : usize) -> Option < Self :: Item > { let len = self . len () ; if n >= len { self . v = & self . v [.. 0] ; None } else { let offset_from_end = (len - 1 - n) * self . chunk_size ; let end = self . v . len () - offset_from_end ; let start = end . saturating_sub (self . chunk_size) ; let nth_back = & self . v [start .. end] ; self . v = & self . v [end ..] ; Some (nth_back) } } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < T > ExactSizeIterator for RChunks < '_ , T > { } #[unstable (feature = "trusted_len" , issue = "37572")] unsafe impl < T > TrustedLen for RChunks < '_ , T > { } #[stable (feature = "rchunks" , since = "1.31.0")] impl < T > FusedIterator for RChunks < '_ , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccess for RChunks < 'a , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccessNoCoerce for RChunks < 'a , T > { const MAY_HAVE_SIDE_EFFECT : bool = false ; } #[doc = " An iterator over a slice in (non-overlapping) mutable chunks (`chunk_size`"] #[doc = " elements at a time), starting at the end of the slice."] #[doc = ""] #[doc = " When the slice len is not evenly divided by the chunk size, the last slice"] #[doc = " of the iteration will be the remainder."] #[doc = ""] #[doc = " This struct is created by the [`rchunks_mut`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let mut slice = ['l', 'o', 'r', 'e', 'm'];"] #[doc = " let iter = slice.rchunks_mut(2);"] #[doc = " ```"] #[doc = ""] #[doc = " [`rchunks_mut`]: slice::rchunks_mut"] #[doc = " [slices]: slice"] #[derive (Debug)] #[stable (feature = "rchunks" , since = "1.31.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct RChunksMut < 'a , T : 'a > { #[doc = " # Safety"] #[doc = " This slice pointer must point at a valid region of `T` with at least length `v.len()`. Normally,"] #[doc = " those requirements would mean that we could instead use a `&mut [T]` here, but we cannot"] #[doc = " because `__iterator_get_unchecked` needs to return `&mut [T]`, which guarantees certain aliasing"] #[doc = " properties that we cannot uphold if we hold on to the full original `&mut [T]`. Wrapping a raw"] #[doc = " slice instead lets us hand out non-overlapping `&mut [T]` subslices of the slice we wrap."] v : * mut [T] , chunk_size : usize , _marker : PhantomData < & 'a mut T > , } impl < 'a , T : 'a > RChunksMut < 'a , T > { #[inline] pub (super) const fn new (slice : & 'a mut [T] , size : usize) -> Self { Self { v : slice , chunk_size : size , _marker : PhantomData } } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < 'a , T > Iterator for RChunksMut < 'a , T > { type Item = & 'a mut [T] ; #[inline] fn next (& mut self) -> Option < & 'a mut [T] > { if self . v . is_empty () { None } else { let sz = cmp :: min (self . v . len () , self . chunk_size) ; let len = self . v . len () ; let (head , tail) = unsafe { self . v . split_at_mut_unchecked (len - sz) } ; self . v = head ; Some (unsafe { & mut * tail }) } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { if self . v . is_empty () { (0 , Some (0)) } else { let n = self . v . len () / self . chunk_size ; let rem = self . v . len () % self . chunk_size ; let n = if rem > 0 { n + 1 } else { n } ; (n , Some (n)) } } #[inline] fn count (self) -> usize { self . len () } #[inline] fn nth (& mut self , n : usize) -> Option < & 'a mut [T] > { let (end , overflow) = n . overflowing_mul (self . chunk_size) ; if end >= self . v . len () || overflow { self . v = & mut [] ; None } else { let end = self . v . len () - end ; let start = match end . checked_sub (self . chunk_size) { Some (sum) => sum , None => 0 , } ; let (head , tail) = unsafe { self . v . split_at_mut (start) } ; let (nth , _) = unsafe { tail . split_at_mut (end - start) } ; self . v = head ; Some (unsafe { & mut * nth }) } } #[inline] fn last (self) -> Option < Self :: Item > { if self . v . is_empty () { None } else { let rem = self . v . len () % self . chunk_size ; let end = if rem == 0 { self . chunk_size } else { rem } ; Some (unsafe { & mut * self . v . get_unchecked_mut (0 .. end) }) } } unsafe fn __iterator_get_unchecked (& mut self , idx : usize) -> Self :: Item { let end = self . v . len () - idx * self . chunk_size ; let start = match end . checked_sub (self . chunk_size) { None => 0 , Some (start) => start , } ; unsafe { from_raw_parts_mut (self . v . as_mut_ptr () . add (start) , end - start) } } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < 'a , T > DoubleEndedIterator for RChunksMut < 'a , T > { #[inline] fn next_back (& mut self) -> Option < & 'a mut [T] > { if self . v . is_empty () { None } else { let remainder = self . v . len () % self . chunk_size ; let sz = if remainder != 0 { remainder } else { self . chunk_size } ; let (head , tail) = unsafe { self . v . split_at_mut_unchecked (sz) } ; self . v = tail ; Some (unsafe { & mut * head }) } } #[inline] fn nth_back (& mut self , n : usize) -> Option < Self :: Item > { let len = self . len () ; if n >= len { self . v = & mut [] ; None } else { let offset_from_end = (len - 1 - n) * self . chunk_size ; let end = self . v . len () - offset_from_end ; let start = end . saturating_sub (self . chunk_size) ; let (tmp , tail) = unsafe { self . v . split_at_mut (end) } ; let (_ , nth_back) = unsafe { tmp . split_at_mut (start) } ; self . v = tail ; Some (unsafe { & mut * nth_back }) } } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < T > ExactSizeIterator for RChunksMut < '_ , T > { } #[unstable (feature = "trusted_len" , issue = "37572")] unsafe impl < T > TrustedLen for RChunksMut < '_ , T > { } #[stable (feature = "rchunks" , since = "1.31.0")] impl < T > FusedIterator for RChunksMut < '_ , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccess for RChunksMut < 'a , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccessNoCoerce for RChunksMut < 'a , T > { const MAY_HAVE_SIDE_EFFECT : bool = false ; } #[stable (feature = "rchunks" , since = "1.31.0")] unsafe impl < T > Send for RChunksMut < '_ , T > where T : Send { } #[stable (feature = "rchunks" , since = "1.31.0")] unsafe impl < T > Sync for RChunksMut < '_ , T > where T : Sync { } #[doc = " An iterator over a slice in (non-overlapping) chunks (`chunk_size` elements at a"] #[doc = " time), starting at the end of the slice."] #[doc = ""] #[doc = " When the slice len is not evenly divided by the chunk size, the last"] #[doc = " up to `chunk_size-1` elements will be omitted but can be retrieved from"] #[doc = " the [`remainder`] function from the iterator."] #[doc = ""] #[doc = " This struct is created by the [`rchunks_exact`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let slice = ['l', 'o', 'r', 'e', 'm'];"] #[doc = " let mut iter = slice.rchunks_exact(2);"] #[doc = " assert_eq!(iter.next(), Some(&['e', 'm'][..]));"] #[doc = " assert_eq!(iter.next(), Some(&['o', 'r'][..]));"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " ```"] #[doc = ""] #[doc = " [`rchunks_exact`]: slice::rchunks_exact"] #[doc = " [`remainder`]: RChunksExact::remainder"] #[doc = " [slices]: slice"] #[derive (Debug)] #[stable (feature = "rchunks" , since = "1.31.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct RChunksExact < 'a , T : 'a > { v : & 'a [T] , rem : & 'a [T] , chunk_size : usize , } impl < 'a , T > RChunksExact < 'a , T > { #[inline] pub (super) const fn new (slice : & 'a [T] , chunk_size : usize) -> Self { let rem = slice . len () % chunk_size ; let (fst , snd) = unsafe { slice . split_at_unchecked (rem) } ; Self { v : snd , rem : fst , chunk_size } } #[doc = " Returns the remainder of the original slice that is not going to be"] #[doc = " returned by the iterator. The returned slice has at most `chunk_size-1`"] #[doc = " elements."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let slice = ['l', 'o', 'r', 'e', 'm'];"] #[doc = " let mut iter = slice.rchunks_exact(2);"] #[doc = " assert_eq!(iter.remainder(), &['l'][..]);"] #[doc = " assert_eq!(iter.next(), Some(&['e', 'm'][..]));"] #[doc = " assert_eq!(iter.remainder(), &['l'][..]);"] #[doc = " assert_eq!(iter.next(), Some(&['o', 'r'][..]));"] #[doc = " assert_eq!(iter.remainder(), &['l'][..]);"] #[doc = " assert_eq!(iter.next(), None);"] #[doc = " assert_eq!(iter.remainder(), &['l'][..]);"] #[doc = " ```"] #[must_use] #[stable (feature = "rchunks" , since = "1.31.0")] #[rustc_const_unstable (feature = "const_slice_make_iter" , issue = "137737")] pub const fn remainder (& self) -> & 'a [T] { self . rem } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < 'a , T > Clone for RChunksExact < 'a , T > { fn clone (& self) -> RChunksExact < 'a , T > { RChunksExact { v : self . v , rem : self . rem , chunk_size : self . chunk_size } } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < 'a , T > Iterator for RChunksExact < 'a , T > { type Item = & 'a [T] ; #[inline] fn next (& mut self) -> Option < & 'a [T] > { if self . v . len () < self . chunk_size { None } else { let (fst , snd) = self . v . split_at (self . v . len () - self . chunk_size) ; self . v = fst ; Some (snd) } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { let n = self . v . len () / self . chunk_size ; (n , Some (n)) } #[inline] fn count (self) -> usize { self . len () } #[inline] fn nth (& mut self , n : usize) -> Option < Self :: Item > { let (end , overflow) = n . overflowing_mul (self . chunk_size) ; if end >= self . v . len () || overflow { self . v = & self . v [.. 0] ; None } else { let (fst , _) = self . v . split_at (self . v . len () - end) ; self . v = fst ; self . next () } } #[inline] fn last (mut self) -> Option < Self :: Item > { self . next_back () } unsafe fn __iterator_get_unchecked (& mut self , idx : usize) -> Self :: Item { let end = self . v . len () - idx * self . chunk_size ; let start = end - self . chunk_size ; unsafe { from_raw_parts (self . v . as_ptr () . add (start) , self . chunk_size) } } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < 'a , T > DoubleEndedIterator for RChunksExact < 'a , T > { #[inline] fn next_back (& mut self) -> Option < & 'a [T] > { if self . v . len () < self . chunk_size { None } else { let (fst , snd) = self . v . split_at (self . chunk_size) ; self . v = snd ; Some (fst) } } #[inline] fn nth_back (& mut self , n : usize) -> Option < Self :: Item > { let len = self . len () ; if n >= len { self . v = & self . v [.. 0] ; None } else { let offset = (len - n) * self . chunk_size ; let start = self . v . len () - offset ; let end = start + self . chunk_size ; let nth_back = & self . v [start .. end] ; self . v = & self . v [end ..] ; Some (nth_back) } } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < 'a , T > ExactSizeIterator for RChunksExact < 'a , T > { fn is_empty (& self) -> bool { self . v . is_empty () } } #[unstable (feature = "trusted_len" , issue = "37572")] unsafe impl < T > TrustedLen for RChunksExact < '_ , T > { } #[stable (feature = "rchunks" , since = "1.31.0")] impl < T > FusedIterator for RChunksExact < '_ , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccess for RChunksExact < 'a , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccessNoCoerce for RChunksExact < 'a , T > { const MAY_HAVE_SIDE_EFFECT : bool = false ; } #[doc = " An iterator over a slice in (non-overlapping) mutable chunks (`chunk_size`"] #[doc = " elements at a time), starting at the end of the slice."] #[doc = ""] #[doc = " When the slice len is not evenly divided by the chunk size, the last up to"] #[doc = " `chunk_size-1` elements will be omitted but can be retrieved from the"] #[doc = " [`into_remainder`] function from the iterator."] #[doc = ""] #[doc = " This struct is created by the [`rchunks_exact_mut`] method on [slices]."] #[doc = ""] #[doc = " # Example"] #[doc = ""] #[doc = " ```"] #[doc = " let mut slice = ['l', 'o', 'r', 'e', 'm'];"] #[doc = " let iter = slice.rchunks_exact_mut(2);"] #[doc = " ```"] #[doc = ""] #[doc = " [`rchunks_exact_mut`]: slice::rchunks_exact_mut"] #[doc = " [`into_remainder`]: RChunksExactMut::into_remainder"] #[doc = " [slices]: slice"] #[derive (Debug)] #[stable (feature = "rchunks" , since = "1.31.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct RChunksExactMut < 'a , T : 'a > { #[doc = " # Safety"] #[doc = " This slice pointer must point at a valid region of `T` with at least length `v.len()`. Normally,"] #[doc = " those requirements would mean that we could instead use a `&mut [T]` here, but we cannot"] #[doc = " because `__iterator_get_unchecked` needs to return `&mut [T]`, which guarantees certain aliasing"] #[doc = " properties that we cannot uphold if we hold on to the full original `&mut [T]`. Wrapping a raw"] #[doc = " slice instead lets us hand out non-overlapping `&mut [T]` subslices of the slice we wrap."] v : * mut [T] , rem : & 'a mut [T] , chunk_size : usize , } impl < 'a , T > RChunksExactMut < 'a , T > { #[inline] pub (super) const fn new (slice : & 'a mut [T] , chunk_size : usize) -> Self { let rem = slice . len () % chunk_size ; let (fst , snd) = unsafe { slice . split_at_mut_unchecked (rem) } ; Self { v : snd , rem : fst , chunk_size } } #[doc = " Returns the remainder of the original slice that is not going to be"] #[doc = " returned by the iterator. The returned slice has at most `chunk_size-1`"] #[doc = " elements."] #[must_use = "`self` will be dropped if the result is not used"] #[stable (feature = "rchunks" , since = "1.31.0")] #[rustc_const_unstable (feature = "const_slice_make_iter" , issue = "137737")] pub const fn into_remainder (self) -> & 'a mut [T] { self . rem } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < 'a , T > Iterator for RChunksExactMut < 'a , T > { type Item = & 'a mut [T] ; #[inline] fn next (& mut self) -> Option < & 'a mut [T] > { if self . v . len () < self . chunk_size { None } else { let len = self . v . len () ; let (head , tail) = unsafe { self . v . split_at_mut (len - self . chunk_size) } ; self . v = head ; Some (unsafe { & mut * tail }) } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { let n = self . v . len () / self . chunk_size ; (n , Some (n)) } #[inline] fn count (self) -> usize { self . len () } #[inline] fn nth (& mut self , n : usize) -> Option < & 'a mut [T] > { let (end , overflow) = n . overflowing_mul (self . chunk_size) ; if end >= self . v . len () || overflow { self . v = & mut [] ; None } else { let len = self . v . len () ; let (fst , _) = unsafe { self . v . split_at_mut (len - end) } ; self . v = fst ; self . next () } } #[inline] fn last (mut self) -> Option < Self :: Item > { self . next_back () } unsafe fn __iterator_get_unchecked (& mut self , idx : usize) -> Self :: Item { let end = self . v . len () - idx * self . chunk_size ; let start = end - self . chunk_size ; unsafe { from_raw_parts_mut (self . v . as_mut_ptr () . add (start) , self . chunk_size) } } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < 'a , T > DoubleEndedIterator for RChunksExactMut < 'a , T > { #[inline] fn next_back (& mut self) -> Option < & 'a mut [T] > { if self . v . len () < self . chunk_size { None } else { let (head , tail) = unsafe { self . v . split_at_mut (self . chunk_size) } ; self . v = tail ; Some (unsafe { & mut * head }) } } #[inline] fn nth_back (& mut self , n : usize) -> Option < Self :: Item > { let len = self . len () ; if n >= len { self . v = & mut [] ; None } else { let offset = (len - n) * self . chunk_size ; let start = self . v . len () - offset ; let end = start + self . chunk_size ; let (tmp , tail) = unsafe { self . v . split_at_mut (end) } ; let (_ , nth_back) = unsafe { tmp . split_at_mut (start) } ; self . v = tail ; Some (unsafe { & mut * nth_back }) } } } #[stable (feature = "rchunks" , since = "1.31.0")] impl < T > ExactSizeIterator for RChunksExactMut < '_ , T > { fn is_empty (& self) -> bool { self . v . is_empty () } } #[unstable (feature = "trusted_len" , issue = "37572")] unsafe impl < T > TrustedLen for RChunksExactMut < '_ , T > { } #[stable (feature = "rchunks" , since = "1.31.0")] impl < T > FusedIterator for RChunksExactMut < '_ , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccess for RChunksExactMut < 'a , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccessNoCoerce for RChunksExactMut < 'a , T > { const MAY_HAVE_SIDE_EFFECT : bool = false ; } #[stable (feature = "rchunks" , since = "1.31.0")] unsafe impl < T > Send for RChunksExactMut < '_ , T > where T : Send { } #[stable (feature = "rchunks" , since = "1.31.0")] unsafe impl < T > Sync for RChunksExactMut < '_ , T > where T : Sync { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccess for Iter < 'a , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccessNoCoerce for Iter < 'a , T > { const MAY_HAVE_SIDE_EFFECT : bool = false ; } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccess for IterMut < 'a , T > { } #[doc (hidden)] #[unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < 'a , T > TrustedRandomAccessNoCoerce for IterMut < 'a , T > { const MAY_HAVE_SIDE_EFFECT : bool = false ; } #[doc = " An iterator over slice in (non-overlapping) chunks separated by a predicate."] #[doc = ""] #[doc = " This struct is created by the [`chunk_by`] method on [slices]."] #[doc = ""] #[doc = " [`chunk_by`]: slice::chunk_by"] #[doc = " [slices]: slice"] #[stable (feature = "slice_group_by" , since = "1.77.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct ChunkBy < 'a , T : 'a , P > { slice : & 'a [T] , predicate : P , } #[stable (feature = "slice_group_by" , since = "1.77.0")] impl < 'a , T : 'a , P > ChunkBy < 'a , T , P > { pub (super) const fn new (slice : & 'a [T] , predicate : P) -> Self { ChunkBy { slice , predicate } } } #[stable (feature = "slice_group_by" , since = "1.77.0")] impl < 'a , T : 'a , P > Iterator for ChunkBy < 'a , T , P > where P : FnMut (& T , & T) -> bool , { type Item = & 'a [T] ; #[inline] fn next (& mut self) -> Option < Self :: Item > { if self . slice . is_empty () { None } else { let mut len = 1 ; let mut iter = self . slice . windows (2) ; while let Some ([l , r]) = iter . next () { if (self . predicate) (l , r) { len += 1 } else { break } } let (head , tail) = self . slice . split_at (len) ; self . slice = tail ; Some (head) } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { if self . slice . is_empty () { (0 , Some (0)) } else { (1 , Some (self . slice . len ())) } } #[inline] fn last (mut self) -> Option < Self :: Item > { self . next_back () } } #[stable (feature = "slice_group_by" , since = "1.77.0")] impl < 'a , T : 'a , P > DoubleEndedIterator for ChunkBy < 'a , T , P > where P : FnMut (& T , & T) -> bool , { #[inline] fn next_back (& mut self) -> Option < Self :: Item > { if self . slice . is_empty () { None } else { let mut len = 1 ; let mut iter = self . slice . windows (2) ; while let Some ([l , r]) = iter . next_back () { if (self . predicate) (l , r) { len += 1 } else { break } } let (head , tail) = self . slice . split_at (self . slice . len () - len) ; self . slice = head ; Some (tail) } } } #[stable (feature = "slice_group_by" , since = "1.77.0")] impl < 'a , T : 'a , P > FusedIterator for ChunkBy < 'a , T , P > where P : FnMut (& T , & T) -> bool { } #[stable (feature = "slice_group_by_clone" , since = "1.89.0")] impl < 'a , T : 'a , P : Clone > Clone for ChunkBy < 'a , T , P > { fn clone (& self) -> Self { Self { slice : self . slice , predicate : self . predicate . clone () } } } #[stable (feature = "slice_group_by" , since = "1.77.0")] impl < 'a , T : 'a + fmt :: Debug , P > fmt :: Debug for ChunkBy < 'a , T , P > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("ChunkBy") . field ("slice" , & self . slice) . finish () } } #[doc = " An iterator over slice in (non-overlapping) mutable chunks separated"] #[doc = " by a predicate."] #[doc = ""] #[doc = " This struct is created by the [`chunk_by_mut`] method on [slices]."] #[doc = ""] #[doc = " [`chunk_by_mut`]: slice::chunk_by_mut"] #[doc = " [slices]: slice"] #[stable (feature = "slice_group_by" , since = "1.77.0")] #[must_use = "iterators are lazy and do nothing unless consumed"] pub struct ChunkByMut < 'a , T : 'a , P > { slice : & 'a mut [T] , predicate : P , } #[stable (feature = "slice_group_by" , since = "1.77.0")] impl < 'a , T : 'a , P > ChunkByMut < 'a , T , P > { pub (super) const fn new (slice : & 'a mut [T] , predicate : P) -> Self { ChunkByMut { slice , predicate } } } #[stable (feature = "slice_group_by" , since = "1.77.0")] impl < 'a , T : 'a , P > Iterator for ChunkByMut < 'a , T , P > where P : FnMut (& T , & T) -> bool , { type Item = & 'a mut [T] ; #[inline] fn next (& mut self) -> Option < Self :: Item > { if self . slice . is_empty () { None } else { let mut len = 1 ; let mut iter = self . slice . windows (2) ; while let Some ([l , r]) = iter . next () { if (self . predicate) (l , r) { len += 1 } else { break } } let slice = mem :: take (& mut self . slice) ; let (head , tail) = slice . split_at_mut (len) ; self . slice = tail ; Some (head) } } #[inline] fn size_hint (& self) -> (usize , Option < usize >) { if self . slice . is_empty () { (0 , Some (0)) } else { (1 , Some (self . slice . len ())) } } #[inline] fn last (mut self) -> Option < Self :: Item > { self . next_back () } } #[stable (feature = "slice_group_by" , since = "1.77.0")] impl < 'a , T : 'a , P > DoubleEndedIterator for ChunkByMut < 'a , T , P > where P : FnMut (& T , & T) -> bool , { #[inline] fn next_back (& mut self) -> Option < Self :: Item > { if self . slice . is_empty () { None } else { let mut len = 1 ; let mut iter = self . slice . windows (2) ; while let Some ([l , r]) = iter . next_back () { if (self . predicate) (l , r) { len += 1 } else { break } } let slice = mem :: take (& mut self . slice) ; let (head , tail) = slice . split_at_mut (slice . len () - len) ; self . slice = head ; Some (tail) } } } #[stable (feature = "slice_group_by" , since = "1.77.0")] impl < 'a , T : 'a , P > FusedIterator for ChunkByMut < 'a , T , P > where P : FnMut (& T , & T) -> bool { } #[stable (feature = "slice_group_by" , since = "1.77.0")] impl < 'a , T : 'a + fmt :: Debug , P > fmt :: Debug for ChunkByMut < 'a , T , P > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("ChunkByMut") . field ("slice" , & self . slice) . finish () } }