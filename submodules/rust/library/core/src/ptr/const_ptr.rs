use split_decls_genesis :: ourprelude :: * ; use super :: * ; use crate :: cmp :: Ordering :: { Equal , Greater , Less } ; use crate :: intrinsics :: const_eval_select ; use crate :: mem :: { self , SizedTypeProperties } ; use crate :: slice :: { self , SliceIndex } ; impl < T : PointeeSized > * const T { #[doc = include_str ! ("docs/is_null.md")] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let s: &str = \"Follow the rabbit\";"] #[doc = " let ptr: *const u8 = s.as_ptr();"] #[doc = " assert!(!ptr.is_null());"] #[doc = " ```"] #[stable (feature = "rust1" , since = "1.0.0")] #[rustc_const_stable (feature = "const_ptr_is_null" , since = "1.84.0")] #[rustc_diagnostic_item = "ptr_const_is_null"] #[inline] #[rustc_allow_const_fn_unstable (const_eval_select)] pub const fn is_null (self) -> bool { let ptr = self as * const u8 ; const_eval_select ! (@ capture { ptr : * const u8 } -> bool : if const #[rustc_allow_const_fn_unstable (const_raw_ptr_comparison)] { match (ptr) . guaranteed_eq (null_mut ()) { Some (res) => res , None => panic ! ("null-ness of this pointer cannot be determined in const context") , } } else { ptr . addr () == 0 }) } #[doc = " Casts to a pointer of another type."] #[stable (feature = "ptr_cast" , since = "1.38.0")] #[rustc_const_stable (feature = "const_ptr_cast" , since = "1.38.0")] #[rustc_diagnostic_item = "const_ptr_cast"] #[inline (always)] pub const fn cast < U > (self) -> * const U { self as _ } #[doc = " Try to cast to a pointer of another type by checking alignment."] #[doc = ""] #[doc = " If the pointer is properly aligned to the target type, it will be"] #[doc = " cast to the target type. Otherwise, `None` is returned."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```rust"] #[doc = " #![feature(pointer_try_cast_aligned)]"] #[doc = ""] #[doc = " let x = 0u64;"] #[doc = ""] #[doc = " let aligned: *const u64 = &x;"] #[doc = " let unaligned = unsafe { aligned.byte_add(1) };"] #[doc = ""] #[doc = " assert!(aligned.try_cast_aligned::<u32>().is_some());"] #[doc = " assert!(unaligned.try_cast_aligned::<u32>().is_none());"] #[doc = " ```"] #[unstable (feature = "pointer_try_cast_aligned" , issue = "141221")] #[must_use = "this returns the result of the operation, \
                  without modifying the original"] #[inline] pub fn try_cast_aligned < U > (self) -> Option < * const U > { if self . is_aligned_to (align_of :: < U > ()) { Some (self . cast ()) } else { None } } #[doc = " Uses the address value in a new pointer of another type."] #[doc = ""] #[doc = " This operation will ignore the address part of its `meta` operand and discard existing"] #[doc = " metadata of `self`. For pointers to a sized types (thin pointers), this has the same effect"] #[doc = " as a simple cast. For pointers to an unsized type (fat pointers) this recombines the address"] #[doc = " with new metadata such as slice lengths or `dyn`-vtable."] #[doc = ""] #[doc = " The resulting pointer will have provenance of `self`. This operation is semantically the"] #[doc = " same as creating a new pointer with the data pointer value of `self` but the metadata of"] #[doc = " `meta`, being fat or thin depending on the `meta` operand."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " This function is primarily useful for enabling pointer arithmetic on potentially fat"] #[doc = " pointers. The pointer is cast to a sized pointee to utilize offset operations and then"] #[doc = " recombined with its own original metadata."] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(set_ptr_value)]"] #[doc = " # use core::fmt::Debug;"] #[doc = " let arr: [i32; 3] = [1, 2, 3];"] #[doc = " let mut ptr = arr.as_ptr() as *const dyn Debug;"] #[doc = " let thin = ptr as *const u8;"] #[doc = " unsafe {"] #[doc = "     ptr = thin.add(8).with_metadata_of(ptr);"] #[doc = "     # assert_eq!(*(ptr as *const i32), 3);"] #[doc = "     println!(\"{:?}\", &*ptr); // will print \"3\""] #[doc = " }"] #[doc = " ```"] #[doc = ""] #[doc = " # *Incorrect* usage"] #[doc = ""] #[doc = " The provenance from pointers is *not* combined. The result must only be used to refer to the"] #[doc = " address allowed by `self`."] #[doc = ""] #[doc = " ```rust,no_run"] #[doc = " #![feature(set_ptr_value)]"] #[doc = " let x = 0u32;"] #[doc = " let y = 1u32;"] #[doc = ""] #[doc = " let x = (&x) as *const u32;"] #[doc = " let y = (&y) as *const u32;"] #[doc = ""] #[doc = " let offset = (x as usize - y as usize) / 4;"] #[doc = " let bad = x.wrapping_add(offset).with_metadata_of(y);"] #[doc = ""] #[doc = " // This dereference is UB. The pointer only has provenance for `x` but points to `y`."] #[doc = " println!(\"{:?}\", unsafe { &*bad });"] #[doc = " ```"] #[unstable (feature = "set_ptr_value" , issue = "75091")] #[must_use = "returns a new pointer rather than modifying its argument"] #[inline] pub const fn with_metadata_of < U > (self , meta : * const U) -> * const U where U : PointeeSized , { from_raw_parts :: < U > (self as * const () , metadata (meta)) } #[doc = " Changes constness without changing the type."] #[doc = ""] #[doc = " This is a bit safer than `as` because it wouldn't silently change the type if the code is"] #[doc = " refactored."] #[stable (feature = "ptr_const_cast" , since = "1.65.0")] #[rustc_const_stable (feature = "ptr_const_cast" , since = "1.65.0")] #[rustc_diagnostic_item = "ptr_cast_mut"] #[inline (always)] pub const fn cast_mut (self) -> * mut T { self as _ } #[doc = include_str ! ("./docs/addr.md")] #[must_use] #[inline (always)] #[stable (feature = "strict_provenance" , since = "1.84.0")] pub fn addr (self) -> usize { unsafe { mem :: transmute (self . cast :: < () > ()) } } #[doc = " Exposes the [\"provenance\"][crate::ptr#provenance] part of the pointer for future use in"] #[doc = " [`with_exposed_provenance`] and returns the \"address\" portion."] #[doc = ""] #[doc = " This is equivalent to `self as usize`, which semantically discards provenance information."] #[doc = " Furthermore, this (like the `as` cast) has the implicit side-effect of marking the"] #[doc = " provenance as 'exposed', so on platforms that support it you can later call"] #[doc = " [`with_exposed_provenance`] to reconstitute the original pointer including its provenance."] #[doc = ""] #[doc = " Due to its inherent ambiguity, [`with_exposed_provenance`] may not be supported by tools"] #[doc = " that help you to stay conformant with the Rust memory model. It is recommended to use"] #[doc = " [Strict Provenance][crate::ptr#strict-provenance] APIs such as [`with_addr`][pointer::with_addr]"] #[doc = " wherever possible, in which case [`addr`][pointer::addr] should be used instead of `expose_provenance`."] #[doc = ""] #[doc = " On most platforms this will produce a value with the same bytes as the original pointer,"] #[doc = " because all the bytes are dedicated to describing the address. Platforms which need to store"] #[doc = " additional information in the pointer may not support this operation, since the 'expose'"] #[doc = " side-effect which is required for [`with_exposed_provenance`] to work is typically not"] #[doc = " available."] #[doc = ""] #[doc = " This is an [Exposed Provenance][crate::ptr#exposed-provenance] API."] #[doc = ""] #[doc = " [`with_exposed_provenance`]: with_exposed_provenance"] #[inline (always)] #[stable (feature = "exposed_provenance" , since = "1.84.0")] pub fn expose_provenance (self) -> usize { self . cast :: < () > () as usize } #[doc = " Creates a new pointer with the given address and the [provenance][crate::ptr#provenance] of"] #[doc = " `self`."] #[doc = ""] #[doc = " This is similar to a `addr as *const T` cast, but copies"] #[doc = " the *provenance* of `self` to the new pointer."] #[doc = " This avoids the inherent ambiguity of the unary cast."] #[doc = ""] #[doc = " This is equivalent to using [`wrapping_offset`][pointer::wrapping_offset] to offset"] #[doc = " `self` to the given address, and therefore has all the same capabilities and restrictions."] #[doc = ""] #[doc = " This is a [Strict Provenance][crate::ptr#strict-provenance] API."] #[must_use] #[inline] #[stable (feature = "strict_provenance" , since = "1.84.0")] pub fn with_addr (self , addr : usize) -> Self { let self_addr = self . addr () as isize ; let dest_addr = addr as isize ; let offset = dest_addr . wrapping_sub (self_addr) ; self . wrapping_byte_offset (offset) } #[doc = " Creates a new pointer by mapping `self`'s address to a new one, preserving the"] #[doc = " [provenance][crate::ptr#provenance] of `self`."] #[doc = ""] #[doc = " This is a convenience for [`with_addr`][pointer::with_addr], see that method for details."] #[doc = ""] #[doc = " This is a [Strict Provenance][crate::ptr#strict-provenance] API."] #[must_use] #[inline] #[stable (feature = "strict_provenance" , since = "1.84.0")] pub fn map_addr (self , f : impl FnOnce (usize) -> usize) -> Self { self . with_addr (f (self . addr ())) } #[doc = " Decompose a (possibly wide) pointer into its data pointer and metadata components."] #[doc = ""] #[doc = " The pointer can be later reconstructed with [`from_raw_parts`]."] #[unstable (feature = "ptr_metadata" , issue = "81513")] #[inline] pub const fn to_raw_parts (self) -> (* const () , < T as super :: Pointee > :: Metadata) { (self . cast () , metadata (self)) } #[doc = include_str ! ("./docs/as_ref.md")] #[doc = ""] #[doc = " ```"] #[doc = " let ptr: *const u8 = &10u8 as *const u8;"] #[doc = ""] #[doc = " unsafe {"] #[doc = "     let val_back = &*ptr;"] #[doc = "     assert_eq!(val_back, &10);"] #[doc = " }"] #[doc = " ```"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let ptr: *const u8 = &10u8 as *const u8;"] #[doc = ""] #[doc = " unsafe {"] #[doc = "     if let Some(val_back) = ptr.as_ref() {"] #[doc = "         assert_eq!(val_back, &10);"] #[doc = "     }"] #[doc = " }"] #[doc = " ```"] #[doc = ""] #[doc = ""] #[doc = " [`is_null`]: #method.is_null"] #[doc = " [`as_uninit_ref`]: #method.as_uninit_ref"] #[stable (feature = "ptr_as_ref" , since = "1.9.0")] #[rustc_const_stable (feature = "const_ptr_is_null" , since = "1.84.0")] #[inline] pub const unsafe fn as_ref < 'a > (self) -> Option < & 'a T > { if self . is_null () { None } else { unsafe { Some (& * self) } } } #[doc = " Returns a shared reference to the value behind the pointer."] #[doc = " If the pointer may be null or the value may be uninitialized, [`as_uninit_ref`] must be used instead."] #[doc = " If the pointer may be null, but the value is known to have been initialized, [`as_ref`] must be used instead."] #[doc = ""] #[doc = " [`as_ref`]: #method.as_ref"] #[doc = " [`as_uninit_ref`]: #method.as_uninit_ref"] #[doc = ""] #[doc = " # Safety"] #[doc = ""] #[doc = " When calling this method, you have to ensure that"] #[doc = " the pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion)."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(ptr_as_ref_unchecked)]"] #[doc = " let ptr: *const u8 = &10u8 as *const u8;"] #[doc = ""] #[doc = " unsafe {"] #[doc = "     assert_eq!(ptr.as_ref_unchecked(), &10);"] #[doc = " }"] #[doc = " ```"] #[unstable (feature = "ptr_as_ref_unchecked" , issue = "122034")] #[inline] #[must_use] pub const unsafe fn as_ref_unchecked < 'a > (self) -> & 'a T { unsafe { & * self } } #[doc = include_str ! ("./docs/as_uninit_ref.md")] #[doc = ""] #[doc = " [`is_null`]: #method.is_null"] #[doc = " [`as_ref`]: #method.as_ref"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(ptr_as_uninit)]"] #[doc = ""] #[doc = " let ptr: *const u8 = &10u8 as *const u8;"] #[doc = ""] #[doc = " unsafe {"] #[doc = "     if let Some(val_back) = ptr.as_uninit_ref() {"] #[doc = "         assert_eq!(val_back.assume_init(), 10);"] #[doc = "     }"] #[doc = " }"] #[doc = " ```"] #[inline] #[unstable (feature = "ptr_as_uninit" , issue = "75402")] pub const unsafe fn as_uninit_ref < 'a > (self) -> Option < & 'a MaybeUninit < T > > where T : Sized , { if self . is_null () { None } else { Some (unsafe { & * (self as * const MaybeUninit < T >) }) } } #[doc = include_str ! ("./docs/offset.md")] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let s: &str = \"123\";"] #[doc = " let ptr: *const u8 = s.as_ptr();"] #[doc = ""] #[doc = " unsafe {"] #[doc = "     assert_eq!(*ptr.offset(1) as char, '2');"] #[doc = "     assert_eq!(*ptr.offset(2) as char, '3');"] #[doc = " }"] #[doc = " ```"] #[stable (feature = "rust1" , since = "1.0.0")] #[must_use = "returns a new pointer rather than modifying its argument"] #[rustc_const_stable (feature = "const_ptr_offset" , since = "1.61.0")] #[inline (always)] #[track_caller] pub const unsafe fn offset (self , count : isize) -> * const T where T : Sized , { #[inline] #[rustc_allow_const_fn_unstable (const_eval_select)] const fn runtime_offset_nowrap (this : * const () , count : isize , size : usize) -> bool { const_eval_select ! (@ capture { this : * const () , count : isize , size : usize } -> bool : if const { true } else { let Some (byte_offset) = count . checked_mul (size as isize) else { return false ; } ; let (_ , overflow) = this . addr () . overflowing_add_signed (byte_offset) ; ! overflow }) } ub_checks :: assert_unsafe_precondition ! (check_language_ub , "ptr::offset requires the address calculation to not overflow" , (this : * const () = self as * const () , count : isize = count , size : usize = size_of ::< T > () ,) => runtime_offset_nowrap (this , count , size)) ; unsafe { intrinsics :: offset (self , count) } } #[doc = " Adds a signed offset in bytes to a pointer."] #[doc = ""] #[doc = " `count` is in units of **bytes**."] #[doc = ""] #[doc = " This is purely a convenience for casting to a `u8` pointer and"] #[doc = " using [offset][pointer::offset] on it. See that method for documentation"] #[doc = " and safety requirements."] #[doc = ""] #[doc = " For non-`Sized` pointees this operation changes only the data pointer,"] #[doc = " leaving the metadata untouched."] #[must_use] #[inline (always)] #[stable (feature = "pointer_byte_offsets" , since = "1.75.0")] #[rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] #[track_caller] pub const unsafe fn byte_offset (self , count : isize) -> Self { unsafe { self . cast :: < u8 > () . offset (count) . with_metadata_of (self) } } #[doc = " Adds a signed offset to a pointer using wrapping arithmetic."] #[doc = ""] #[doc = " `count` is in units of T; e.g., a `count` of 3 represents a pointer"] #[doc = " offset of `3 * size_of::<T>()` bytes."] #[doc = ""] #[doc = " # Safety"] #[doc = ""] #[doc = " This operation itself is always safe, but using the resulting pointer is not."] #[doc = ""] #[doc = " The resulting pointer \"remembers\" the [allocation] that `self` points to"] #[doc = " (this is called \"[Provenance](ptr/index.html#provenance)\")."] #[doc = " The pointer must not be used to read or write other allocations."] #[doc = ""] #[doc = " In other words, `let z = x.wrapping_offset((y as isize) - (x as isize))` does *not* make `z`"] #[doc = " the same as `y` even if we assume `T` has size `1` and there is no overflow: `z` is still"] #[doc = " attached to the object `x` is attached to, and dereferencing it is Undefined Behavior unless"] #[doc = " `x` and `y` point into the same allocation."] #[doc = ""] #[doc = " Compared to [`offset`], this method basically delays the requirement of staying within the"] #[doc = " same allocation: [`offset`] is immediate Undefined Behavior when crossing object"] #[doc = " boundaries; `wrapping_offset` produces a pointer but still leads to Undefined Behavior if a"] #[doc = " pointer is dereferenced when it is out-of-bounds of the object it is attached to. [`offset`]"] #[doc = " can be optimized better and is thus preferable in performance-sensitive code."] #[doc = ""] #[doc = " The delayed check only considers the value of the pointer that was dereferenced, not the"] #[doc = " intermediate values used during the computation of the final result. For example,"] #[doc = " `x.wrapping_offset(o).wrapping_offset(o.wrapping_neg())` is always the same as `x`. In other"] #[doc = " words, leaving the allocation and then re-entering it later is permitted."] #[doc = ""] #[doc = " [`offset`]: #method.offset"] #[doc = " [allocation]: crate::ptr#allocation"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " # use std::fmt::Write;"] #[doc = " // Iterate using a raw pointer in increments of two elements"] #[doc = " let data = [1u8, 2, 3, 4, 5];"] #[doc = " let mut ptr: *const u8 = data.as_ptr();"] #[doc = " let step = 2;"] #[doc = " let end_rounded_up = ptr.wrapping_offset(6);"] #[doc = ""] #[doc = " let mut out = String::new();"] #[doc = " while ptr != end_rounded_up {"] #[doc = "     unsafe {"] #[doc = "         write!(&mut out, \"{}, \", *ptr)?;"] #[doc = "     }"] #[doc = "     ptr = ptr.wrapping_offset(step);"] #[doc = " }"] #[doc = " assert_eq!(out.as_str(), \"1, 3, 5, \");"] #[doc = " # std::fmt::Result::Ok(())"] #[doc = " ```"] #[stable (feature = "ptr_wrapping_offset" , since = "1.16.0")] #[must_use = "returns a new pointer rather than modifying its argument"] #[rustc_const_stable (feature = "const_ptr_offset" , since = "1.61.0")] #[inline (always)] pub const fn wrapping_offset (self , count : isize) -> * const T where T : Sized , { unsafe { intrinsics :: arith_offset (self , count) } } #[doc = " Adds a signed offset in bytes to a pointer using wrapping arithmetic."] #[doc = ""] #[doc = " `count` is in units of **bytes**."] #[doc = ""] #[doc = " This is purely a convenience for casting to a `u8` pointer and"] #[doc = " using [wrapping_offset][pointer::wrapping_offset] on it. See that method"] #[doc = " for documentation."] #[doc = ""] #[doc = " For non-`Sized` pointees this operation changes only the data pointer,"] #[doc = " leaving the metadata untouched."] #[must_use] #[inline (always)] #[stable (feature = "pointer_byte_offsets" , since = "1.75.0")] #[rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] pub const fn wrapping_byte_offset (self , count : isize) -> Self { self . cast :: < u8 > () . wrapping_offset (count) . with_metadata_of (self) } #[doc = " Masks out bits of the pointer according to a mask."] #[doc = ""] #[doc = " This is convenience for `ptr.map_addr(|a| a & mask)`."] #[doc = ""] #[doc = " For non-`Sized` pointees this operation changes only the data pointer,"] #[doc = " leaving the metadata untouched."] #[doc = ""] #[doc = " ## Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(ptr_mask)]"] #[doc = " let v = 17_u32;"] #[doc = " let ptr: *const u32 = &v;"] #[doc = ""] #[doc = " // `u32` is 4 bytes aligned,"] #[doc = " // which means that lower 2 bits are always 0."] #[doc = " let tag_mask = 0b11;"] #[doc = " let ptr_mask = !tag_mask;"] #[doc = ""] #[doc = " // We can store something in these lower bits"] #[doc = " let tagged_ptr = ptr.map_addr(|a| a | 0b10);"] #[doc = ""] #[doc = " // Get the \"tag\" back"] #[doc = " let tag = tagged_ptr.addr() & tag_mask;"] #[doc = " assert_eq!(tag, 0b10);"] #[doc = ""] #[doc = " // Note that `tagged_ptr` is unaligned, it's UB to read from it."] #[doc = " // To get original pointer `mask` can be used:"] #[doc = " let masked_ptr = tagged_ptr.mask(ptr_mask);"] #[doc = " assert_eq!(unsafe { *masked_ptr }, 17);"] #[doc = " ```"] #[unstable (feature = "ptr_mask" , issue = "98290")] #[must_use = "returns a new pointer rather than modifying its argument"] #[inline (always)] pub fn mask (self , mask : usize) -> * const T { intrinsics :: ptr_mask (self . cast :: < () > () , mask) . with_metadata_of (self) } #[doc = " Calculates the distance between two pointers within the same allocation. The returned value is in"] #[doc = " units of T: the distance in bytes divided by `size_of::<T>()`."] #[doc = ""] #[doc = " This is equivalent to `(self as isize - origin as isize) / (size_of::<T>() as isize)`,"] #[doc = " except that it has a lot more opportunities for UB, in exchange for the compiler"] #[doc = " better understanding what you are doing."] #[doc = ""] #[doc = " The primary motivation of this method is for computing the `len` of an array/slice"] #[doc = " of `T` that you are currently representing as a \"start\" and \"end\" pointer"] #[doc = " (and \"end\" is \"one past the end\" of the array)."] #[doc = " In that case, `end.offset_from(start)` gets you the length of the array."] #[doc = ""] #[doc = " All of the following safety requirements are trivially satisfied for this usecase."] #[doc = ""] #[doc = " [`offset`]: #method.offset"] #[doc = ""] #[doc = " # Safety"] #[doc = ""] #[doc = " If any of the following conditions are violated, the result is Undefined Behavior:"] #[doc = ""] #[doc = " * `self` and `origin` must either"] #[doc = ""] #[doc = "   * point to the same address, or"] #[doc = "   * both be [derived from][crate::ptr#provenance] a pointer to the same [allocation], and the memory range between"] #[doc = "     the two pointers must be in bounds of that object. (See below for an example.)"] #[doc = ""] #[doc = " * The distance between the pointers, in bytes, must be an exact multiple"] #[doc = "   of the size of `T`."] #[doc = ""] #[doc = " As a consequence, the absolute distance between the pointers, in bytes, computed on"] #[doc = " mathematical integers (without \"wrapping around\"), cannot overflow an `isize`. This is"] #[doc = " implied by the in-bounds requirement, and the fact that no allocation can be larger"] #[doc = " than `isize::MAX` bytes."] #[doc = ""] #[doc = " The requirement for pointers to be derived from the same allocation is primarily"] #[doc = " needed for `const`-compatibility: the distance between pointers into *different* allocated"] #[doc = " objects is not known at compile-time. However, the requirement also exists at"] #[doc = " runtime and may be exploited by optimizations. If you wish to compute the difference between"] #[doc = " pointers that are not guaranteed to be from the same allocation, use `(self as isize -"] #[doc = " origin as isize) / size_of::<T>()`."] #[doc = ""] #[doc = " [`add`]: #method.add"] #[doc = " [allocation]: crate::ptr#allocation"] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " This function panics if `T` is a Zero-Sized Type (\"ZST\")."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Basic usage:"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [0; 5];"] #[doc = " let ptr1: *const i32 = &a[1];"] #[doc = " let ptr2: *const i32 = &a[3];"] #[doc = " unsafe {"] #[doc = "     assert_eq!(ptr2.offset_from(ptr1), 2);"] #[doc = "     assert_eq!(ptr1.offset_from(ptr2), -2);"] #[doc = "     assert_eq!(ptr1.offset(2), ptr2);"] #[doc = "     assert_eq!(ptr2.offset(-2), ptr1);"] #[doc = " }"] #[doc = " ```"] #[doc = ""] #[doc = " *Incorrect* usage:"] #[doc = ""] #[doc = " ```rust,no_run"] #[doc = " let ptr1 = Box::into_raw(Box::new(0u8)) as *const u8;"] #[doc = " let ptr2 = Box::into_raw(Box::new(1u8)) as *const u8;"] #[doc = " let diff = (ptr2 as isize).wrapping_sub(ptr1 as isize);"] #[doc = " // Make ptr2_other an \"alias\" of ptr2.add(1), but derived from ptr1."] #[doc = " let ptr2_other = (ptr1 as *const u8).wrapping_offset(diff).wrapping_offset(1);"] #[doc = " assert_eq!(ptr2 as usize, ptr2_other as usize);"] #[doc = " // Since ptr2_other and ptr2 are derived from pointers to different objects,"] #[doc = " // computing their offset is undefined behavior, even though"] #[doc = " // they point to addresses that are in-bounds of the same object!"] #[doc = " unsafe {"] #[doc = "     let one = ptr2_other.offset_from(ptr2); // Undefined Behavior! ⚠\u{fe0f}"] #[doc = " }"] #[doc = " ```"] #[stable (feature = "ptr_offset_from" , since = "1.47.0")] #[rustc_const_stable (feature = "const_ptr_offset_from" , since = "1.65.0")] #[inline] #[cfg_attr (miri , track_caller)] pub const unsafe fn offset_from (self , origin : * const T) -> isize where T : Sized , { let pointee_size = size_of :: < T > () ; assert ! (0 < pointee_size && pointee_size <= isize :: MAX as usize) ; unsafe { intrinsics :: ptr_offset_from (self , origin) } } #[doc = " Calculates the distance between two pointers within the same allocation. The returned value is in"] #[doc = " units of **bytes**."] #[doc = ""] #[doc = " This is purely a convenience for casting to a `u8` pointer and"] #[doc = " using [`offset_from`][pointer::offset_from] on it. See that method for"] #[doc = " documentation and safety requirements."] #[doc = ""] #[doc = " For non-`Sized` pointees this operation considers only the data pointers,"] #[doc = " ignoring the metadata."] #[inline (always)] #[stable (feature = "pointer_byte_offsets" , since = "1.75.0")] #[rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] #[cfg_attr (miri , track_caller)] pub const unsafe fn byte_offset_from < U : ? Sized > (self , origin : * const U) -> isize { unsafe { self . cast :: < u8 > () . offset_from (origin . cast :: < u8 > ()) } } #[doc = " Calculates the distance between two pointers within the same allocation, *where it's known that"] #[doc = " `self` is equal to or greater than `origin`*. The returned value is in"] #[doc = " units of T: the distance in bytes is divided by `size_of::<T>()`."] #[doc = ""] #[doc = " This computes the same value that [`offset_from`](#method.offset_from)"] #[doc = " would compute, but with the added precondition that the offset is"] #[doc = " guaranteed to be non-negative.  This method is equivalent to"] #[doc = " `usize::try_from(self.offset_from(origin)).unwrap_unchecked()`,"] #[doc = " but it provides slightly more information to the optimizer, which can"] #[doc = " sometimes allow it to optimize slightly better with some backends."] #[doc = ""] #[doc = " This method can be thought of as recovering the `count` that was passed"] #[doc = " to [`add`](#method.add) (or, with the parameters in the other order,"] #[doc = " to [`sub`](#method.sub)).  The following are all equivalent, assuming"] #[doc = " that their safety preconditions are met:"] #[doc = " ```rust"] #[doc = " # unsafe fn blah(ptr: *const i32, origin: *const i32, count: usize) -> bool { unsafe {"] #[doc = " ptr.offset_from_unsigned(origin) == count"] #[doc = " # &&"] #[doc = " origin.add(count) == ptr"] #[doc = " # &&"] #[doc = " ptr.sub(count) == origin"] #[doc = " # } }"] #[doc = " ```"] #[doc = ""] #[doc = " # Safety"] #[doc = ""] #[doc = " - The distance between the pointers must be non-negative (`self >= origin`)"] #[doc = ""] #[doc = " - *All* the safety conditions of [`offset_from`](#method.offset_from)"] #[doc = "   apply to this method as well; see it for the full details."] #[doc = ""] #[doc = " Importantly, despite the return type of this method being able to represent"] #[doc = " a larger offset, it's still *not permitted* to pass pointers which differ"] #[doc = " by more than `isize::MAX` *bytes*.  As such, the result of this method will"] #[doc = " always be less than or equal to `isize::MAX as usize`."] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " This function panics if `T` is a Zero-Sized Type (\"ZST\")."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let a = [0; 5];"] #[doc = " let ptr1: *const i32 = &a[1];"] #[doc = " let ptr2: *const i32 = &a[3];"] #[doc = " unsafe {"] #[doc = "     assert_eq!(ptr2.offset_from_unsigned(ptr1), 2);"] #[doc = "     assert_eq!(ptr1.add(2), ptr2);"] #[doc = "     assert_eq!(ptr2.sub(2), ptr1);"] #[doc = "     assert_eq!(ptr2.offset_from_unsigned(ptr2), 0);"] #[doc = " }"] #[doc = ""] #[doc = " // This would be incorrect, as the pointers are not correctly ordered:"] #[doc = " // ptr1.offset_from_unsigned(ptr2)"] #[doc = " ```"] #[stable (feature = "ptr_sub_ptr" , since = "1.87.0")] #[rustc_const_stable (feature = "const_ptr_sub_ptr" , since = "1.87.0")] #[inline] #[track_caller] pub const unsafe fn offset_from_unsigned (self , origin : * const T) -> usize where T : Sized , { #[rustc_allow_const_fn_unstable (const_eval_select)] const fn runtime_ptr_ge (this : * const () , origin : * const ()) -> bool { const_eval_select ! (@ capture { this : * const () , origin : * const () } -> bool : if const { true } else { this >= origin }) } ub_checks :: assert_unsafe_precondition ! (check_language_ub , "ptr::offset_from_unsigned requires `self >= origin`" , (this : * const () = self as * const () , origin : * const () = origin as * const () ,) => runtime_ptr_ge (this , origin)) ; let pointee_size = size_of :: < T > () ; assert ! (0 < pointee_size && pointee_size <= isize :: MAX as usize) ; unsafe { intrinsics :: ptr_offset_from_unsigned (self , origin) } } #[doc = " Calculates the distance between two pointers within the same allocation, *where it's known that"] #[doc = " `self` is equal to or greater than `origin`*. The returned value is in"] #[doc = " units of **bytes**."] #[doc = ""] #[doc = " This is purely a convenience for casting to a `u8` pointer and"] #[doc = " using [`offset_from_unsigned`][pointer::offset_from_unsigned] on it."] #[doc = " See that method for documentation and safety requirements."] #[doc = ""] #[doc = " For non-`Sized` pointees this operation considers only the data pointers,"] #[doc = " ignoring the metadata."] #[stable (feature = "ptr_sub_ptr" , since = "1.87.0")] #[rustc_const_stable (feature = "const_ptr_sub_ptr" , since = "1.87.0")] #[inline] #[track_caller] pub const unsafe fn byte_offset_from_unsigned < U : ? Sized > (self , origin : * const U) -> usize { unsafe { self . cast :: < u8 > () . offset_from_unsigned (origin . cast :: < u8 > ()) } } #[doc = " Returns whether two pointers are guaranteed to be equal."] #[doc = ""] #[doc = " At runtime this function behaves like `Some(self == other)`."] #[doc = " However, in some contexts (e.g., compile-time evaluation),"] #[doc = " it is not always possible to determine equality of two pointers, so this function may"] #[doc = " spuriously return `None` for pointers that later actually turn out to have its equality known."] #[doc = " But when it returns `Some`, the pointers' equality is guaranteed to be known."] #[doc = ""] #[doc = " The return value may change from `Some` to `None` and vice versa depending on the compiler"] #[doc = " version and unsafe code must not"] #[doc = " rely on the result of this function for soundness. It is suggested to only use this function"] #[doc = " for performance optimizations where spurious `None` return values by this function do not"] #[doc = " affect the outcome, but just the performance."] #[doc = " The consequences of using this method to make runtime and compile-time code behave"] #[doc = " differently have not been explored. This method should not be used to introduce such"] #[doc = " differences, and it should also not be stabilized before we have a better understanding"] #[doc = " of this issue."] #[unstable (feature = "const_raw_ptr_comparison" , issue = "53020")] #[rustc_const_unstable (feature = "const_raw_ptr_comparison" , issue = "53020")] #[inline] pub const fn guaranteed_eq (self , other : * const T) -> Option < bool > where T : Sized , { match intrinsics :: ptr_guaranteed_cmp (self , other) { 2 => None , other => Some (other == 1) , } } #[doc = " Returns whether two pointers are guaranteed to be inequal."] #[doc = ""] #[doc = " At runtime this function behaves like `Some(self != other)`."] #[doc = " However, in some contexts (e.g., compile-time evaluation),"] #[doc = " it is not always possible to determine inequality of two pointers, so this function may"] #[doc = " spuriously return `None` for pointers that later actually turn out to have its inequality known."] #[doc = " But when it returns `Some`, the pointers' inequality is guaranteed to be known."] #[doc = ""] #[doc = " The return value may change from `Some` to `None` and vice versa depending on the compiler"] #[doc = " version and unsafe code must not"] #[doc = " rely on the result of this function for soundness. It is suggested to only use this function"] #[doc = " for performance optimizations where spurious `None` return values by this function do not"] #[doc = " affect the outcome, but just the performance."] #[doc = " The consequences of using this method to make runtime and compile-time code behave"] #[doc = " differently have not been explored. This method should not be used to introduce such"] #[doc = " differences, and it should also not be stabilized before we have a better understanding"] #[doc = " of this issue."] #[unstable (feature = "const_raw_ptr_comparison" , issue = "53020")] #[rustc_const_unstable (feature = "const_raw_ptr_comparison" , issue = "53020")] #[inline] pub const fn guaranteed_ne (self , other : * const T) -> Option < bool > where T : Sized , { match self . guaranteed_eq (other) { None => None , Some (eq) => Some (! eq) , } } #[doc = include_str ! ("./docs/add.md")] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let s: &str = \"123\";"] #[doc = " let ptr: *const u8 = s.as_ptr();"] #[doc = ""] #[doc = " unsafe {"] #[doc = "     assert_eq!(*ptr.add(1), b'2');"] #[doc = "     assert_eq!(*ptr.add(2), b'3');"] #[doc = " }"] #[doc = " ```"] #[stable (feature = "pointer_methods" , since = "1.26.0")] #[must_use = "returns a new pointer rather than modifying its argument"] #[rustc_const_stable (feature = "const_ptr_offset" , since = "1.61.0")] #[inline (always)] #[track_caller] pub const unsafe fn add (self , count : usize) -> Self where T : Sized , { #[cfg (debug_assertions)] #[inline] #[rustc_allow_const_fn_unstable (const_eval_select)] const fn runtime_add_nowrap (this : * const () , count : usize , size : usize) -> bool { const_eval_select ! (@ capture { this : * const () , count : usize , size : usize } -> bool : if const { true } else { let Some (byte_offset) = count . checked_mul (size) else { return false ; } ; let (_ , overflow) = this . addr () . overflowing_add (byte_offset) ; byte_offset <= (isize :: MAX as usize) && ! overflow }) } #[cfg (debug_assertions)] ub_checks :: assert_unsafe_precondition ! (check_language_ub , "ptr::add requires that the address calculation does not overflow" , (this : * const () = self as * const () , count : usize = count , size : usize = size_of ::< T > () ,) => runtime_add_nowrap (this , count , size)) ; unsafe { intrinsics :: offset (self , count) } } #[doc = " Adds an unsigned offset in bytes to a pointer."] #[doc = ""] #[doc = " `count` is in units of bytes."] #[doc = ""] #[doc = " This is purely a convenience for casting to a `u8` pointer and"] #[doc = " using [add][pointer::add] on it. See that method for documentation"] #[doc = " and safety requirements."] #[doc = ""] #[doc = " For non-`Sized` pointees this operation changes only the data pointer,"] #[doc = " leaving the metadata untouched."] #[must_use] #[inline (always)] #[stable (feature = "pointer_byte_offsets" , since = "1.75.0")] #[rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] #[track_caller] pub const unsafe fn byte_add (self , count : usize) -> Self { unsafe { self . cast :: < u8 > () . add (count) . with_metadata_of (self) } } #[doc = " Subtracts an unsigned offset from a pointer."] #[doc = ""] #[doc = " This can only move the pointer backward (or not move it). If you need to move forward or"] #[doc = " backward depending on the value, then you might want [`offset`](#method.offset) instead"] #[doc = " which takes a signed offset."] #[doc = ""] #[doc = " `count` is in units of T; e.g., a `count` of 3 represents a pointer"] #[doc = " offset of `3 * size_of::<T>()` bytes."] #[doc = ""] #[doc = " # Safety"] #[doc = ""] #[doc = " If any of the following conditions are violated, the result is Undefined Behavior:"] #[doc = ""] #[doc = " * The offset in bytes, `count * size_of::<T>()`, computed on mathematical integers (without"] #[doc = "   \"wrapping around\"), must fit in an `isize`."] #[doc = ""] #[doc = " * If the computed offset is non-zero, then `self` must be [derived from][crate::ptr#provenance] a pointer to some"] #[doc = "   [allocation], and the entire memory range between `self` and the result must be in"] #[doc = "   bounds of that allocation. In particular, this range must not \"wrap around\" the edge"] #[doc = "   of the address space."] #[doc = ""] #[doc = " Allocations can never be larger than `isize::MAX` bytes, so if the computed offset"] #[doc = " stays in bounds of the allocation, it is guaranteed to satisfy the first requirement."] #[doc = " This implies, for instance, that `vec.as_ptr().add(vec.len())` (for `vec: Vec<T>`) is always"] #[doc = " safe."] #[doc = ""] #[doc = " Consider using [`wrapping_sub`] instead if these constraints are"] #[doc = " difficult to satisfy. The only advantage of this method is that it"] #[doc = " enables more aggressive compiler optimizations."] #[doc = ""] #[doc = " [`wrapping_sub`]: #method.wrapping_sub"] #[doc = " [allocation]: crate::ptr#allocation"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " let s: &str = \"123\";"] #[doc = ""] #[doc = " unsafe {"] #[doc = "     let end: *const u8 = s.as_ptr().add(3);"] #[doc = "     assert_eq!(*end.sub(1), b'3');"] #[doc = "     assert_eq!(*end.sub(2), b'2');"] #[doc = " }"] #[doc = " ```"] #[stable (feature = "pointer_methods" , since = "1.26.0")] #[must_use = "returns a new pointer rather than modifying its argument"] #[rustc_const_stable (feature = "const_ptr_offset" , since = "1.61.0")] #[inline (always)] #[track_caller] pub const unsafe fn sub (self , count : usize) -> Self where T : Sized , { #[cfg (debug_assertions)] #[inline] #[rustc_allow_const_fn_unstable (const_eval_select)] const fn runtime_sub_nowrap (this : * const () , count : usize , size : usize) -> bool { const_eval_select ! (@ capture { this : * const () , count : usize , size : usize } -> bool : if const { true } else { let Some (byte_offset) = count . checked_mul (size) else { return false ; } ; byte_offset <= (isize :: MAX as usize) && this . addr () >= byte_offset }) } #[cfg (debug_assertions)] ub_checks :: assert_unsafe_precondition ! (check_language_ub , "ptr::sub requires that the address calculation does not overflow" , (this : * const () = self as * const () , count : usize = count , size : usize = size_of ::< T > () ,) => runtime_sub_nowrap (this , count , size)) ; if T :: IS_ZST { self } else { unsafe { intrinsics :: offset (self , intrinsics :: unchecked_sub (0 , count as isize)) } } } #[doc = " Subtracts an unsigned offset in bytes from a pointer."] #[doc = ""] #[doc = " `count` is in units of bytes."] #[doc = ""] #[doc = " This is purely a convenience for casting to a `u8` pointer and"] #[doc = " using [sub][pointer::sub] on it. See that method for documentation"] #[doc = " and safety requirements."] #[doc = ""] #[doc = " For non-`Sized` pointees this operation changes only the data pointer,"] #[doc = " leaving the metadata untouched."] #[must_use] #[inline (always)] #[stable (feature = "pointer_byte_offsets" , since = "1.75.0")] #[rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] #[track_caller] pub const unsafe fn byte_sub (self , count : usize) -> Self { unsafe { self . cast :: < u8 > () . sub (count) . with_metadata_of (self) } } #[doc = " Adds an unsigned offset to a pointer using wrapping arithmetic."] #[doc = ""] #[doc = " `count` is in units of T; e.g., a `count` of 3 represents a pointer"] #[doc = " offset of `3 * size_of::<T>()` bytes."] #[doc = ""] #[doc = " # Safety"] #[doc = ""] #[doc = " This operation itself is always safe, but using the resulting pointer is not."] #[doc = ""] #[doc = " The resulting pointer \"remembers\" the [allocation] that `self` points to; it must not"] #[doc = " be used to read or write other allocations."] #[doc = ""] #[doc = " In other words, `let z = x.wrapping_add((y as usize) - (x as usize))` does *not* make `z`"] #[doc = " the same as `y` even if we assume `T` has size `1` and there is no overflow: `z` is still"] #[doc = " attached to the object `x` is attached to, and dereferencing it is Undefined Behavior unless"] #[doc = " `x` and `y` point into the same allocation."] #[doc = ""] #[doc = " Compared to [`add`], this method basically delays the requirement of staying within the"] #[doc = " same allocation: [`add`] is immediate Undefined Behavior when crossing object"] #[doc = " boundaries; `wrapping_add` produces a pointer but still leads to Undefined Behavior if a"] #[doc = " pointer is dereferenced when it is out-of-bounds of the object it is attached to. [`add`]"] #[doc = " can be optimized better and is thus preferable in performance-sensitive code."] #[doc = ""] #[doc = " The delayed check only considers the value of the pointer that was dereferenced, not the"] #[doc = " intermediate values used during the computation of the final result. For example,"] #[doc = " `x.wrapping_add(o).wrapping_sub(o)` is always the same as `x`. In other words, leaving the"] #[doc = " allocation and then re-entering it later is permitted."] #[doc = ""] #[doc = " [`add`]: #method.add"] #[doc = " [allocation]: crate::ptr#allocation"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " # use std::fmt::Write;"] #[doc = " // Iterate using a raw pointer in increments of two elements"] #[doc = " let data = [1u8, 2, 3, 4, 5];"] #[doc = " let mut ptr: *const u8 = data.as_ptr();"] #[doc = " let step = 2;"] #[doc = " let end_rounded_up = ptr.wrapping_add(6);"] #[doc = ""] #[doc = " let mut out = String::new();"] #[doc = " while ptr != end_rounded_up {"] #[doc = "     unsafe {"] #[doc = "         write!(&mut out, \"{}, \", *ptr)?;"] #[doc = "     }"] #[doc = "     ptr = ptr.wrapping_add(step);"] #[doc = " }"] #[doc = " assert_eq!(out, \"1, 3, 5, \");"] #[doc = " # std::fmt::Result::Ok(())"] #[doc = " ```"] #[stable (feature = "pointer_methods" , since = "1.26.0")] #[must_use = "returns a new pointer rather than modifying its argument"] #[rustc_const_stable (feature = "const_ptr_offset" , since = "1.61.0")] #[inline (always)] pub const fn wrapping_add (self , count : usize) -> Self where T : Sized , { self . wrapping_offset (count as isize) } #[doc = " Adds an unsigned offset in bytes to a pointer using wrapping arithmetic."] #[doc = ""] #[doc = " `count` is in units of bytes."] #[doc = ""] #[doc = " This is purely a convenience for casting to a `u8` pointer and"] #[doc = " using [wrapping_add][pointer::wrapping_add] on it. See that method for documentation."] #[doc = ""] #[doc = " For non-`Sized` pointees this operation changes only the data pointer,"] #[doc = " leaving the metadata untouched."] #[must_use] #[inline (always)] #[stable (feature = "pointer_byte_offsets" , since = "1.75.0")] #[rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] pub const fn wrapping_byte_add (self , count : usize) -> Self { self . cast :: < u8 > () . wrapping_add (count) . with_metadata_of (self) } #[doc = " Subtracts an unsigned offset from a pointer using wrapping arithmetic."] #[doc = ""] #[doc = " `count` is in units of T; e.g., a `count` of 3 represents a pointer"] #[doc = " offset of `3 * size_of::<T>()` bytes."] #[doc = ""] #[doc = " # Safety"] #[doc = ""] #[doc = " This operation itself is always safe, but using the resulting pointer is not."] #[doc = ""] #[doc = " The resulting pointer \"remembers\" the [allocation] that `self` points to; it must not"] #[doc = " be used to read or write other allocations."] #[doc = ""] #[doc = " In other words, `let z = x.wrapping_sub((x as usize) - (y as usize))` does *not* make `z`"] #[doc = " the same as `y` even if we assume `T` has size `1` and there is no overflow: `z` is still"] #[doc = " attached to the object `x` is attached to, and dereferencing it is Undefined Behavior unless"] #[doc = " `x` and `y` point into the same allocation."] #[doc = ""] #[doc = " Compared to [`sub`], this method basically delays the requirement of staying within the"] #[doc = " same allocation: [`sub`] is immediate Undefined Behavior when crossing object"] #[doc = " boundaries; `wrapping_sub` produces a pointer but still leads to Undefined Behavior if a"] #[doc = " pointer is dereferenced when it is out-of-bounds of the object it is attached to. [`sub`]"] #[doc = " can be optimized better and is thus preferable in performance-sensitive code."] #[doc = ""] #[doc = " The delayed check only considers the value of the pointer that was dereferenced, not the"] #[doc = " intermediate values used during the computation of the final result. For example,"] #[doc = " `x.wrapping_add(o).wrapping_sub(o)` is always the same as `x`. In other words, leaving the"] #[doc = " allocation and then re-entering it later is permitted."] #[doc = ""] #[doc = " [`sub`]: #method.sub"] #[doc = " [allocation]: crate::ptr#allocation"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " # use std::fmt::Write;"] #[doc = " // Iterate using a raw pointer in increments of two elements (backwards)"] #[doc = " let data = [1u8, 2, 3, 4, 5];"] #[doc = " let mut ptr: *const u8 = data.as_ptr();"] #[doc = " let start_rounded_down = ptr.wrapping_sub(2);"] #[doc = " ptr = ptr.wrapping_add(4);"] #[doc = " let step = 2;"] #[doc = " let mut out = String::new();"] #[doc = " while ptr != start_rounded_down {"] #[doc = "     unsafe {"] #[doc = "         write!(&mut out, \"{}, \", *ptr)?;"] #[doc = "     }"] #[doc = "     ptr = ptr.wrapping_sub(step);"] #[doc = " }"] #[doc = " assert_eq!(out, \"5, 3, 1, \");"] #[doc = " # std::fmt::Result::Ok(())"] #[doc = " ```"] #[stable (feature = "pointer_methods" , since = "1.26.0")] #[must_use = "returns a new pointer rather than modifying its argument"] #[rustc_const_stable (feature = "const_ptr_offset" , since = "1.61.0")] #[inline (always)] pub const fn wrapping_sub (self , count : usize) -> Self where T : Sized , { self . wrapping_offset ((count as isize) . wrapping_neg ()) } #[doc = " Subtracts an unsigned offset in bytes from a pointer using wrapping arithmetic."] #[doc = ""] #[doc = " `count` is in units of bytes."] #[doc = ""] #[doc = " This is purely a convenience for casting to a `u8` pointer and"] #[doc = " using [wrapping_sub][pointer::wrapping_sub] on it. See that method for documentation."] #[doc = ""] #[doc = " For non-`Sized` pointees this operation changes only the data pointer,"] #[doc = " leaving the metadata untouched."] #[must_use] #[inline (always)] #[stable (feature = "pointer_byte_offsets" , since = "1.75.0")] #[rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] pub const fn wrapping_byte_sub (self , count : usize) -> Self { self . cast :: < u8 > () . wrapping_sub (count) . with_metadata_of (self) } #[doc = " Reads the value from `self` without moving it. This leaves the"] #[doc = " memory in `self` unchanged."] #[doc = ""] #[doc = " See [`ptr::read`] for safety concerns and examples."] #[doc = ""] #[doc = " [`ptr::read`]: crate::ptr::read()"] #[stable (feature = "pointer_methods" , since = "1.26.0")] #[rustc_const_stable (feature = "const_ptr_read" , since = "1.71.0")] #[inline] #[track_caller] pub const unsafe fn read (self) -> T where T : Sized , { unsafe { read (self) } } #[doc = " Performs a volatile read of the value from `self` without moving it. This"] #[doc = " leaves the memory in `self` unchanged."] #[doc = ""] #[doc = " Volatile operations are intended to act on I/O memory, and are guaranteed"] #[doc = " to not be elided or reordered by the compiler across other volatile"] #[doc = " operations."] #[doc = ""] #[doc = " See [`ptr::read_volatile`] for safety concerns and examples."] #[doc = ""] #[doc = " [`ptr::read_volatile`]: crate::ptr::read_volatile()"] #[stable (feature = "pointer_methods" , since = "1.26.0")] #[inline] #[track_caller] pub unsafe fn read_volatile (self) -> T where T : Sized , { unsafe { read_volatile (self) } } #[doc = " Reads the value from `self` without moving it. This leaves the"] #[doc = " memory in `self` unchanged."] #[doc = ""] #[doc = " Unlike `read`, the pointer may be unaligned."] #[doc = ""] #[doc = " See [`ptr::read_unaligned`] for safety concerns and examples."] #[doc = ""] #[doc = " [`ptr::read_unaligned`]: crate::ptr::read_unaligned()"] #[stable (feature = "pointer_methods" , since = "1.26.0")] #[rustc_const_stable (feature = "const_ptr_read" , since = "1.71.0")] #[inline] #[track_caller] pub const unsafe fn read_unaligned (self) -> T where T : Sized , { unsafe { read_unaligned (self) } } #[doc = " Copies `count * size_of::<T>()` bytes from `self` to `dest`. The source"] #[doc = " and destination may overlap."] #[doc = ""] #[doc = " NOTE: this has the *same* argument order as [`ptr::copy`]."] #[doc = ""] #[doc = " See [`ptr::copy`] for safety concerns and examples."] #[doc = ""] #[doc = " [`ptr::copy`]: crate::ptr::copy()"] #[rustc_const_stable (feature = "const_intrinsic_copy" , since = "1.83.0")] #[stable (feature = "pointer_methods" , since = "1.26.0")] #[inline] #[track_caller] pub const unsafe fn copy_to (self , dest : * mut T , count : usize) where T : Sized , { unsafe { copy (self , dest , count) } } #[doc = " Copies `count * size_of::<T>()` bytes from `self` to `dest`. The source"] #[doc = " and destination may *not* overlap."] #[doc = ""] #[doc = " NOTE: this has the *same* argument order as [`ptr::copy_nonoverlapping`]."] #[doc = ""] #[doc = " See [`ptr::copy_nonoverlapping`] for safety concerns and examples."] #[doc = ""] #[doc = " [`ptr::copy_nonoverlapping`]: crate::ptr::copy_nonoverlapping()"] #[rustc_const_stable (feature = "const_intrinsic_copy" , since = "1.83.0")] #[stable (feature = "pointer_methods" , since = "1.26.0")] #[inline] #[track_caller] pub const unsafe fn copy_to_nonoverlapping (self , dest : * mut T , count : usize) where T : Sized , { unsafe { copy_nonoverlapping (self , dest , count) } } #[doc = " Computes the offset that needs to be applied to the pointer in order to make it aligned to"] #[doc = " `align`."] #[doc = ""] #[doc = " If it is not possible to align the pointer, the implementation returns"] #[doc = " `usize::MAX`."] #[doc = ""] #[doc = " The offset is expressed in number of `T` elements, and not bytes. The value returned can be"] #[doc = " used with the `wrapping_add` method."] #[doc = ""] #[doc = " There are no guarantees whatsoever that offsetting the pointer will not overflow or go"] #[doc = " beyond the allocation that the pointer points into. It is up to the caller to ensure that"] #[doc = " the returned offset is correct in all terms other than alignment."] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " The function panics if `align` is not a power-of-two."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " Accessing adjacent `u8` as `u16`"] #[doc = ""] #[doc = " ```"] #[doc = " # unsafe {"] #[doc = " let x = [5_u8, 6, 7, 8, 9];"] #[doc = " let ptr = x.as_ptr();"] #[doc = " let offset = ptr.align_offset(align_of::<u16>());"] #[doc = ""] #[doc = " if offset < x.len() - 1 {"] #[doc = "     let u16_ptr = ptr.add(offset).cast::<u16>();"] #[doc = "     assert!(*u16_ptr == u16::from_ne_bytes([5, 6]) || *u16_ptr == u16::from_ne_bytes([6, 7]));"] #[doc = " } else {"] #[doc = "     // while the pointer can be aligned via `offset`, it would point"] #[doc = "     // outside the allocation"] #[doc = " }"] #[doc = " # }"] #[doc = " ```"] #[must_use] #[inline] #[stable (feature = "align_offset" , since = "1.36.0")] pub fn align_offset (self , align : usize) -> usize where T : Sized , { if ! align . is_power_of_two () { panic ! ("align_offset: align is not a power-of-two") ; } let ret = unsafe { align_offset (self , align) } ; #[cfg (miri)] if ret != usize :: MAX { intrinsics :: miri_promise_symbolic_alignment (self . wrapping_add (ret) . cast () , align) ; } ret } #[doc = " Returns whether the pointer is properly aligned for `T`."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " // On some platforms, the alignment of i32 is less than 4."] #[doc = " #[repr(align(4))]"] #[doc = " struct AlignedI32(i32);"] #[doc = ""] #[doc = " let data = AlignedI32(42);"] #[doc = " let ptr = &data as *const AlignedI32;"] #[doc = ""] #[doc = " assert!(ptr.is_aligned());"] #[doc = " assert!(!ptr.wrapping_byte_add(1).is_aligned());"] #[doc = " ```"] #[must_use] #[inline] #[stable (feature = "pointer_is_aligned" , since = "1.79.0")] pub fn is_aligned (self) -> bool where T : Sized , { self . is_aligned_to (align_of :: < T > ()) } #[doc = " Returns whether the pointer is aligned to `align`."] #[doc = ""] #[doc = " For non-`Sized` pointees this operation considers only the data pointer,"] #[doc = " ignoring the metadata."] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " The function panics if `align` is not a power-of-two (this includes 0)."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(pointer_is_aligned_to)]"] #[doc = ""] #[doc = " // On some platforms, the alignment of i32 is less than 4."] #[doc = " #[repr(align(4))]"] #[doc = " struct AlignedI32(i32);"] #[doc = ""] #[doc = " let data = AlignedI32(42);"] #[doc = " let ptr = &data as *const AlignedI32;"] #[doc = ""] #[doc = " assert!(ptr.is_aligned_to(1));"] #[doc = " assert!(ptr.is_aligned_to(2));"] #[doc = " assert!(ptr.is_aligned_to(4));"] #[doc = ""] #[doc = " assert!(ptr.wrapping_byte_add(2).is_aligned_to(2));"] #[doc = " assert!(!ptr.wrapping_byte_add(2).is_aligned_to(4));"] #[doc = ""] #[doc = " assert_ne!(ptr.is_aligned_to(8), ptr.wrapping_add(1).is_aligned_to(8));"] #[doc = " ```"] #[must_use] #[inline] #[unstable (feature = "pointer_is_aligned_to" , issue = "96284")] pub fn is_aligned_to (self , align : usize) -> bool { if ! align . is_power_of_two () { panic ! ("is_aligned_to: align is not a power-of-two") ; } self . addr () & (align - 1) == 0 } } impl < T > * const T { #[doc = " Casts from a type to its maybe-uninitialized version."] #[must_use] #[inline (always)] #[unstable (feature = "cast_maybe_uninit" , issue = "145036")] pub const fn cast_uninit (self) -> * const MaybeUninit < T > { self as _ } } impl < T > * const MaybeUninit < T > { #[doc = " Casts from a maybe-uninitialized type to its initialized version."] #[doc = ""] #[doc = " This is always safe, since UB can only occur if the pointer is read"] #[doc = " before being initialized."] #[must_use] #[inline (always)] #[unstable (feature = "cast_maybe_uninit" , issue = "145036")] pub const fn cast_init (self) -> * const T { self as _ } } impl < T > * const [T] { #[doc = " Returns the length of a raw slice."] #[doc = ""] #[doc = " The returned value is the number of **elements**, not the number of bytes."] #[doc = ""] #[doc = " This function is safe, even when the raw slice cannot be cast to a slice"] #[doc = " reference because the pointer is null or unaligned."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```rust"] #[doc = " use std::ptr;"] #[doc = ""] #[doc = " let slice: *const [i8] = ptr::slice_from_raw_parts(ptr::null(), 3);"] #[doc = " assert_eq!(slice.len(), 3);"] #[doc = " ```"] #[inline] #[stable (feature = "slice_ptr_len" , since = "1.79.0")] #[rustc_const_stable (feature = "const_slice_ptr_len" , since = "1.79.0")] pub const fn len (self) -> usize { metadata (self) } #[doc = " Returns `true` if the raw slice has a length of 0."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " use std::ptr;"] #[doc = ""] #[doc = " let slice: *const [i8] = ptr::slice_from_raw_parts(ptr::null(), 3);"] #[doc = " assert!(!slice.is_empty());"] #[doc = " ```"] #[inline (always)] #[stable (feature = "slice_ptr_len" , since = "1.79.0")] #[rustc_const_stable (feature = "const_slice_ptr_len" , since = "1.79.0")] pub const fn is_empty (self) -> bool { self . len () == 0 } #[doc = " Returns a raw pointer to the slice's buffer."] #[doc = ""] #[doc = " This is equivalent to casting `self` to `*const T`, but more type-safe."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```rust"] #[doc = " #![feature(slice_ptr_get)]"] #[doc = " use std::ptr;"] #[doc = ""] #[doc = " let slice: *const [i8] = ptr::slice_from_raw_parts(ptr::null(), 3);"] #[doc = " assert_eq!(slice.as_ptr(), ptr::null());"] #[doc = " ```"] #[inline] #[unstable (feature = "slice_ptr_get" , issue = "74265")] pub const fn as_ptr (self) -> * const T { self as * const T } #[doc = " Gets a raw pointer to the underlying array."] #[doc = ""] #[doc = " If `N` is not exactly equal to the length of `self`, then this method returns `None`."] #[unstable (feature = "slice_as_array" , issue = "133508")] #[inline] #[must_use] pub const fn as_array < const N : usize > (self) -> Option < * const [T ; N] > { if self . len () == N { let me = self . as_ptr () as * const [T ; N] ; Some (me) } else { None } } #[doc = " Returns a raw pointer to an element or subslice, without doing bounds"] #[doc = " checking."] #[doc = ""] #[doc = " Calling this method with an out-of-bounds index or when `self` is not dereferenceable"] #[doc = " is *[undefined behavior]* even if the resulting pointer is not used."] #[doc = ""] #[doc = " [undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(slice_ptr_get)]"] #[doc = ""] #[doc = " let x = &[1, 2, 4] as *const [i32];"] #[doc = ""] #[doc = " unsafe {"] #[doc = "     assert_eq!(x.get_unchecked(1), x.as_ptr().add(1));"] #[doc = " }"] #[doc = " ```"] #[unstable (feature = "slice_ptr_get" , issue = "74265")] #[rustc_const_unstable (feature = "const_index" , issue = "143775")] #[inline] pub const unsafe fn get_unchecked < I > (self , index : I) -> * const I :: Output where I : [const] SliceIndex < [T] >, { unsafe { index . get_unchecked (self) } } #[doc = include_str ! ("docs/as_uninit_slice.md")] #[inline] #[unstable (feature = "ptr_as_uninit" , issue = "75402")] pub const unsafe fn as_uninit_slice < 'a > (self) -> Option < & 'a [MaybeUninit < T >] > { if self . is_null () { None } else { Some (unsafe { slice :: from_raw_parts (self as * const MaybeUninit < T > , self . len ()) }) } } } impl < T > * const T { #[doc = " Casts from a pointer-to-`T` to a pointer-to-`[T; N]`."] #[inline] #[unstable (feature = "ptr_cast_array" , issue = "144514")] pub const fn cast_array < const N : usize > (self) -> * const [T ; N] { self . cast () } } impl < T , const N : usize > * const [T ; N] { #[doc = " Returns a raw pointer to the array's buffer."] #[doc = ""] #[doc = " This is equivalent to casting `self` to `*const T`, but more type-safe."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```rust"] #[doc = " #![feature(array_ptr_get)]"] #[doc = " use std::ptr;"] #[doc = ""] #[doc = " let arr: *const [i8; 3] = ptr::null();"] #[doc = " assert_eq!(arr.as_ptr(), ptr::null());"] #[doc = " ```"] #[inline] #[unstable (feature = "array_ptr_get" , issue = "119834")] pub const fn as_ptr (self) -> * const T { self as * const T } #[doc = " Returns a raw pointer to a slice containing the entire array."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(array_ptr_get)]"] #[doc = ""] #[doc = " let arr: *const [i32; 3] = &[1, 2, 4] as *const [i32; 3];"] #[doc = " let slice: *const [i32] = arr.as_slice();"] #[doc = " assert_eq!(slice.len(), 3);"] #[doc = " ```"] #[inline] #[unstable (feature = "array_ptr_get" , issue = "119834")] pub const fn as_slice (self) -> * const [T] { self } } #[doc = " Pointer equality is by address, as produced by the [`<*const T>::addr`](pointer::addr) method."] #[stable (feature = "rust1" , since = "1.0.0")] impl < T : PointeeSized > PartialEq for * const T { #[inline] #[allow (ambiguous_wide_pointer_comparisons)] fn eq (& self , other : & * const T) -> bool { * self == * other } } #[doc = " Pointer equality is an equivalence relation."] #[stable (feature = "rust1" , since = "1.0.0")] impl < T : PointeeSized > Eq for * const T { } #[doc = " Pointer comparison is by address, as produced by the `[`<*const T>::addr`](pointer::addr)` method."] #[stable (feature = "rust1" , since = "1.0.0")] impl < T : PointeeSized > Ord for * const T { #[inline] #[allow (ambiguous_wide_pointer_comparisons)] fn cmp (& self , other : & * const T) -> Ordering { if self < other { Less } else if self == other { Equal } else { Greater } } } #[doc = " Pointer comparison is by address, as produced by the `[`<*const T>::addr`](pointer::addr)` method."] #[stable (feature = "rust1" , since = "1.0.0")] impl < T : PointeeSized > PartialOrd for * const T { #[inline] #[allow (ambiguous_wide_pointer_comparisons)] fn partial_cmp (& self , other : & * const T) -> Option < Ordering > { Some (self . cmp (other)) } #[inline] #[allow (ambiguous_wide_pointer_comparisons)] fn lt (& self , other : & * const T) -> bool { * self < * other } #[inline] #[allow (ambiguous_wide_pointer_comparisons)] fn le (& self , other : & * const T) -> bool { * self <= * other } #[inline] #[allow (ambiguous_wide_pointer_comparisons)] fn gt (& self , other : & * const T) -> bool { * self > * other } #[inline] #[allow (ambiguous_wide_pointer_comparisons)] fn ge (& self , other : & * const T) -> bool { * self >= * other } } #[stable (feature = "raw_ptr_default" , since = "1.88.0")] impl < T : ? Sized + Thin > Default for * const T { #[doc = " Returns the default value of [`null()`][crate::ptr::null]."] fn default () -> Self { crate :: ptr :: null () } }