mkuse!{use super :: * ;}
mkuse!{use crate :: cmp :: Ordering :: { Equal , Greater , Less } ;}
mkuse!{use crate :: intrinsics :: const_eval_select ;}
mkuse!{use crate :: marker :: PointeeSized ;}
mkuse!{use crate :: mem :: { self , SizedTypeProperties } ;}
mkuse!{use crate :: slice :: { self , SliceIndex } ;}
mkitem!{mkimpl!{impl < T : PointeeSized > * mut T { # [doc = include_str ! ("docs/is_null.md")] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut s = [1, 2, 3];"] # [doc = " let ptr: *mut u32 = s.as_mut_ptr();"] # [doc = " assert!(!ptr.is_null());"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_ptr_is_null" , since = "1.84.0")] # [rustc_diagnostic_item = "ptr_is_null"] # [inline] pub const fn is_null (self) -> bool { self . cast_const () . is_null () } # [doc = " Casts to a pointer of another type."] # [stable (feature = "ptr_cast" , since = "1.38.0")] # [rustc_const_stable (feature = "const_ptr_cast" , since = "1.38.0")] # [rustc_diagnostic_item = "ptr_cast"] # [inline (always)] pub const fn cast < U > (self) -> * mut U { self as _ } # [doc = " Try to cast to a pointer of another type by checking alignment."] # [doc = ""] # [doc = " If the pointer is properly aligned to the target type, it will be"] # [doc = " cast to the target type. Otherwise, `None` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " #![feature(pointer_try_cast_aligned)]"] # [doc = ""] # [doc = " let mut x = 0u64;"] # [doc = ""] # [doc = " let aligned: *mut u64 = &mut x;"] # [doc = " let unaligned = unsafe { aligned.byte_add(1) };"] # [doc = ""] # [doc = " assert!(aligned.try_cast_aligned::<u32>().is_some());"] # [doc = " assert!(unaligned.try_cast_aligned::<u32>().is_none());"] # [doc = " ```"] # [unstable (feature = "pointer_try_cast_aligned" , issue = "141221")] # [must_use = "this returns the result of the operation, \
                  without modifying the original"] # [inline] pub fn try_cast_aligned < U > (self) -> Option < * mut U > { if self . is_aligned_to (align_of :: < U > ()) { Some (self . cast ()) } else { None } } # [doc = " Uses the address value in a new pointer of another type."] # [doc = ""] # [doc = " This operation will ignore the address part of its `meta` operand and discard existing"] # [doc = " metadata of `self`. For pointers to a sized types (thin pointers), this has the same effect"] # [doc = " as a simple cast. For pointers to an unsized type (fat pointers) this recombines the address"] # [doc = " with new metadata such as slice lengths or `dyn`-vtable."] # [doc = ""] # [doc = " The resulting pointer will have provenance of `self`. This operation is semantically the"] # [doc = " same as creating a new pointer with the data pointer value of `self` but the metadata of"] # [doc = " `meta`, being fat or thin depending on the `meta` operand."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " This function is primarily useful for enabling pointer arithmetic on potentially fat"] # [doc = " pointers. The pointer is cast to a sized pointee to utilize offset operations and then"] # [doc = " recombined with its own original metadata."] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(set_ptr_value)]"] # [doc = " # use core::fmt::Debug;"] # [doc = " let mut arr: [i32; 3] = [1, 2, 3];"] # [doc = " let mut ptr = arr.as_mut_ptr() as *mut dyn Debug;"] # [doc = " let thin = ptr as *mut u8;"] # [doc = " unsafe {"] # [doc = "     ptr = thin.add(8).with_metadata_of(ptr);"] # [doc = "     # assert_eq!(*(ptr as *mut i32), 3);"] # [doc = "     println!(\"{:?}\", &*ptr); // will print \"3\""] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " # *Incorrect* usage"] # [doc = ""] # [doc = " The provenance from pointers is *not* combined. The result must only be used to refer to the"] # [doc = " address allowed by `self`."] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " #![feature(set_ptr_value)]"] # [doc = " let mut x = 0u32;"] # [doc = " let mut y = 1u32;"] # [doc = ""] # [doc = " let x = (&mut x) as *mut u32;"] # [doc = " let y = (&mut y) as *mut u32;"] # [doc = ""] # [doc = " let offset = (x as usize - y as usize) / 4;"] # [doc = " let bad = x.wrapping_add(offset).with_metadata_of(y);"] # [doc = ""] # [doc = " // This dereference is UB. The pointer only has provenance for `x` but points to `y`."] # [doc = " println!(\"{:?}\", unsafe { &*bad });"] # [doc = " ```"] # [unstable (feature = "set_ptr_value" , issue = "75091")] # [must_use = "returns a new pointer rather than modifying its argument"] # [inline] pub const fn with_metadata_of < U > (self , meta : * const U) -> * mut U where U : PointeeSized , { from_raw_parts_mut :: < U > (self as * mut () , metadata (meta)) } # [doc = " Changes constness without changing the type."] # [doc = ""] # [doc = " This is a bit safer than `as` because it wouldn't silently change the type if the code is"] # [doc = " refactored."] # [doc = ""] # [doc = " While not strictly required (`*mut T` coerces to `*const T`), this is provided for symmetry"] # [doc = " with [`cast_mut`] on `*const T` and may have documentation value if used instead of implicit"] # [doc = " coercion."] # [doc = ""] # [doc = " [`cast_mut`]: pointer::cast_mut"] # [stable (feature = "ptr_const_cast" , since = "1.65.0")] # [rustc_const_stable (feature = "ptr_const_cast" , since = "1.65.0")] # [rustc_diagnostic_item = "ptr_cast_const"] # [inline (always)] pub const fn cast_const (self) -> * const T { self as _ } # [doc = include_str ! ("./docs/addr.md")] # [doc = ""] # [doc = " [without_provenance]: without_provenance_mut"] # [must_use] # [inline (always)] # [stable (feature = "strict_provenance" , since = "1.84.0")] pub fn addr (self) -> usize { unsafe { mem :: transmute (self . cast :: < () > ()) } } # [doc = " Exposes the [\"provenance\"][crate::ptr#provenance] part of the pointer for future use in"] # [doc = " [`with_exposed_provenance_mut`] and returns the \"address\" portion."] # [doc = ""] # [doc = " This is equivalent to `self as usize`, which semantically discards provenance information."] # [doc = " Furthermore, this (like the `as` cast) has the implicit side-effect of marking the"] # [doc = " provenance as 'exposed', so on platforms that support it you can later call"] # [doc = " [`with_exposed_provenance_mut`] to reconstitute the original pointer including its provenance."] # [doc = ""] # [doc = " Due to its inherent ambiguity, [`with_exposed_provenance_mut`] may not be supported by tools"] # [doc = " that help you to stay conformant with the Rust memory model. It is recommended to use"] # [doc = " [Strict Provenance][crate::ptr#strict-provenance] APIs such as [`with_addr`][pointer::with_addr]"] # [doc = " wherever possible, in which case [`addr`][pointer::addr] should be used instead of `expose_provenance`."] # [doc = ""] # [doc = " On most platforms this will produce a value with the same bytes as the original pointer,"] # [doc = " because all the bytes are dedicated to describing the address. Platforms which need to store"] # [doc = " additional information in the pointer may not support this operation, since the 'expose'"] # [doc = " side-effect which is required for [`with_exposed_provenance_mut`] to work is typically not"] # [doc = " available."] # [doc = ""] # [doc = " This is an [Exposed Provenance][crate::ptr#exposed-provenance] API."] # [doc = ""] # [doc = " [`with_exposed_provenance_mut`]: with_exposed_provenance_mut"] # [inline (always)] # [stable (feature = "exposed_provenance" , since = "1.84.0")] pub fn expose_provenance (self) -> usize { self . cast :: < () > () as usize } # [doc = " Creates a new pointer with the given address and the [provenance][crate::ptr#provenance] of"] # [doc = " `self`."] # [doc = ""] # [doc = " This is similar to a `addr as *mut T` cast, but copies"] # [doc = " the *provenance* of `self` to the new pointer."] # [doc = " This avoids the inherent ambiguity of the unary cast."] # [doc = ""] # [doc = " This is equivalent to using [`wrapping_offset`][pointer::wrapping_offset] to offset"] # [doc = " `self` to the given address, and therefore has all the same capabilities and restrictions."] # [doc = ""] # [doc = " This is a [Strict Provenance][crate::ptr#strict-provenance] API."] # [must_use] # [inline] # [stable (feature = "strict_provenance" , since = "1.84.0")] pub fn with_addr (self , addr : usize) -> Self { let self_addr = self . addr () as isize ; let dest_addr = addr as isize ; let offset = dest_addr . wrapping_sub (self_addr) ; self . wrapping_byte_offset (offset) } # [doc = " Creates a new pointer by mapping `self`'s address to a new one, preserving the original"] # [doc = " pointer's [provenance][crate::ptr#provenance]."] # [doc = ""] # [doc = " This is a convenience for [`with_addr`][pointer::with_addr], see that method for details."] # [doc = ""] # [doc = " This is a [Strict Provenance][crate::ptr#strict-provenance] API."] # [must_use] # [inline] # [stable (feature = "strict_provenance" , since = "1.84.0")] pub fn map_addr (self , f : impl FnOnce (usize) -> usize) -> Self { self . with_addr (f (self . addr ())) } # [doc = " Decompose a (possibly wide) pointer into its data pointer and metadata components."] # [doc = ""] # [doc = " The pointer can be later reconstructed with [`from_raw_parts_mut`]."] # [unstable (feature = "ptr_metadata" , issue = "81513")] # [inline] pub const fn to_raw_parts (self) -> (* mut () , < T as super :: Pointee > :: Metadata) { (self . cast () , super :: metadata (self)) } # [doc = include_str ! ("./docs/as_ref.md")] # [doc = ""] # [doc = " ```"] # [doc = " let ptr: *mut u8 = &mut 10u8 as *mut u8;"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     let val_back = &*ptr;"] # [doc = "     println!(\"We got back the value: {val_back}!\");"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let ptr: *mut u8 = &mut 10u8 as *mut u8;"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     if let Some(val_back) = ptr.as_ref() {"] # [doc = "         println!(\"We got back the value: {val_back}!\");"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " # See Also"] # [doc = ""] # [doc = " For the mutable counterpart see [`as_mut`]."] # [doc = ""] # [doc = " [`is_null`]: #method.is_null-1"] # [doc = " [`as_uninit_ref`]: pointer#method.as_uninit_ref-1"] # [doc = " [`as_mut`]: #method.as_mut"] # [stable (feature = "ptr_as_ref" , since = "1.9.0")] # [rustc_const_stable (feature = "const_ptr_is_null" , since = "1.84.0")] # [inline] pub const unsafe fn as_ref < 'a > (self) -> Option < & 'a T > { if self . is_null () { None } else { unsafe { Some (& * self) } } } # [doc = " Returns a shared reference to the value behind the pointer."] # [doc = " If the pointer may be null or the value may be uninitialized, [`as_uninit_ref`] must be used instead."] # [doc = " If the pointer may be null, but the value is known to have been initialized, [`as_ref`] must be used instead."] # [doc = ""] # [doc = " For the mutable counterpart see [`as_mut_unchecked`]."] # [doc = ""] # [doc = " [`as_ref`]: #method.as_ref"] # [doc = " [`as_uninit_ref`]: #method.as_uninit_ref"] # [doc = " [`as_mut_unchecked`]: #method.as_mut_unchecked"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " When calling this method, you have to ensure that the pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(ptr_as_ref_unchecked)]"] # [doc = " let ptr: *mut u8 = &mut 10u8 as *mut u8;"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     println!(\"We got back the value: {}!\", ptr.as_ref_unchecked());"] # [doc = " }"] # [doc = " ```"] # [unstable (feature = "ptr_as_ref_unchecked" , issue = "122034")] # [inline] # [must_use] pub const unsafe fn as_ref_unchecked < 'a > (self) -> & 'a T { unsafe { & * self } } # [doc = include_str ! ("./docs/as_uninit_ref.md")] # [doc = ""] # [doc = " [`is_null`]: #method.is_null-1"] # [doc = " [`as_ref`]: pointer#method.as_ref-1"] # [doc = ""] # [doc = " # See Also"] # [doc = " For the mutable counterpart see [`as_uninit_mut`]."] # [doc = ""] # [doc = " [`as_uninit_mut`]: #method.as_uninit_mut"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(ptr_as_uninit)]"] # [doc = ""] # [doc = " let ptr: *mut u8 = &mut 10u8 as *mut u8;"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     if let Some(val_back) = ptr.as_uninit_ref() {"] # [doc = "         println!(\"We got back the value: {}!\", val_back.assume_init());"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [inline] # [unstable (feature = "ptr_as_uninit" , issue = "75402")] pub const unsafe fn as_uninit_ref < 'a > (self) -> Option < & 'a MaybeUninit < T > > where T : Sized , { if self . is_null () { None } else { Some (unsafe { & * (self as * const MaybeUninit < T >) }) } } # [doc = include_str ! ("./docs/offset.md")] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut s = [1, 2, 3];"] # [doc = " let ptr: *mut u32 = s.as_mut_ptr();"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     assert_eq!(2, *ptr.offset(1));"] # [doc = "     assert_eq!(3, *ptr.offset(2));"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [must_use = "returns a new pointer rather than modifying its argument"] # [rustc_const_stable (feature = "const_ptr_offset" , since = "1.61.0")] # [inline (always)] # [track_caller] pub const unsafe fn offset (self , count : isize) -> * mut T where T : Sized , { # [inline] # [rustc_allow_const_fn_unstable (const_eval_select)] const fn runtime_offset_nowrap (this : * const () , count : isize , size : usize) -> bool { const_eval_select ! (@ capture { this : * const () , count : isize , size : usize } -> bool : if const { true } else { let Some (byte_offset) = count . checked_mul (size as isize) else { return false ; } ; let (_ , overflow) = this . addr () . overflowing_add_signed (byte_offset) ; ! overflow }) } ub_checks :: assert_unsafe_precondition ! (check_language_ub , "ptr::offset requires the address calculation to not overflow" , (this : * const () = self as * const () , count : isize = count , size : usize = size_of ::< T > () ,) => runtime_offset_nowrap (this , count , size)) ; unsafe { intrinsics :: offset (self , count) } } # [doc = " Adds a signed offset in bytes to a pointer."] # [doc = ""] # [doc = " `count` is in units of **bytes**."] # [doc = ""] # [doc = " This is purely a convenience for casting to a `u8` pointer and"] # [doc = " using [offset][pointer::offset] on it. See that method for documentation"] # [doc = " and safety requirements."] # [doc = ""] # [doc = " For non-`Sized` pointees this operation changes only the data pointer,"] # [doc = " leaving the metadata untouched."] # [must_use] # [inline (always)] # [stable (feature = "pointer_byte_offsets" , since = "1.75.0")] # [rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] # [track_caller] pub const unsafe fn byte_offset (self , count : isize) -> Self { unsafe { self . cast :: < u8 > () . offset (count) . with_metadata_of (self) } } # [doc = " Adds a signed offset to a pointer using wrapping arithmetic."] # [doc = ""] # [doc = " `count` is in units of T; e.g., a `count` of 3 represents a pointer"] # [doc = " offset of `3 * size_of::<T>()` bytes."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This operation itself is always safe, but using the resulting pointer is not."] # [doc = ""] # [doc = " The resulting pointer \"remembers\" the [allocation] that `self` points to"] # [doc = " (this is called \"[Provenance](ptr/index.html#provenance)\")."] # [doc = " The pointer must not be used to read or write other allocations."] # [doc = ""] # [doc = " In other words, `let z = x.wrapping_offset((y as isize) - (x as isize))` does *not* make `z`"] # [doc = " the same as `y` even if we assume `T` has size `1` and there is no overflow: `z` is still"] # [doc = " attached to the object `x` is attached to, and dereferencing it is Undefined Behavior unless"] # [doc = " `x` and `y` point into the same allocation."] # [doc = ""] # [doc = " Compared to [`offset`], this method basically delays the requirement of staying within the"] # [doc = " same allocation: [`offset`] is immediate Undefined Behavior when crossing object"] # [doc = " boundaries; `wrapping_offset` produces a pointer but still leads to Undefined Behavior if a"] # [doc = " pointer is dereferenced when it is out-of-bounds of the object it is attached to. [`offset`]"] # [doc = " can be optimized better and is thus preferable in performance-sensitive code."] # [doc = ""] # [doc = " The delayed check only considers the value of the pointer that was dereferenced, not the"] # [doc = " intermediate values used during the computation of the final result. For example,"] # [doc = " `x.wrapping_offset(o).wrapping_offset(o.wrapping_neg())` is always the same as `x`. In other"] # [doc = " words, leaving the allocation and then re-entering it later is permitted."] # [doc = ""] # [doc = " [`offset`]: #method.offset"] # [doc = " [allocation]: crate::ptr#allocation"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " // Iterate using a raw pointer in increments of two elements"] # [doc = " let mut data = [1u8, 2, 3, 4, 5];"] # [doc = " let mut ptr: *mut u8 = data.as_mut_ptr();"] # [doc = " let step = 2;"] # [doc = " let end_rounded_up = ptr.wrapping_offset(6);"] # [doc = ""] # [doc = " while ptr != end_rounded_up {"] # [doc = "     unsafe {"] # [doc = "         *ptr = 0;"] # [doc = "     }"] # [doc = "     ptr = ptr.wrapping_offset(step);"] # [doc = " }"] # [doc = " assert_eq!(&data, &[0, 2, 0, 4, 0]);"] # [doc = " ```"] # [stable (feature = "ptr_wrapping_offset" , since = "1.16.0")] # [must_use = "returns a new pointer rather than modifying its argument"] # [rustc_const_stable (feature = "const_ptr_offset" , since = "1.61.0")] # [inline (always)] pub const fn wrapping_offset (self , count : isize) -> * mut T where T : Sized , { unsafe { intrinsics :: arith_offset (self , count) as * mut T } } # [doc = " Adds a signed offset in bytes to a pointer using wrapping arithmetic."] # [doc = ""] # [doc = " `count` is in units of **bytes**."] # [doc = ""] # [doc = " This is purely a convenience for casting to a `u8` pointer and"] # [doc = " using [wrapping_offset][pointer::wrapping_offset] on it. See that method"] # [doc = " for documentation."] # [doc = ""] # [doc = " For non-`Sized` pointees this operation changes only the data pointer,"] # [doc = " leaving the metadata untouched."] # [must_use] # [inline (always)] # [stable (feature = "pointer_byte_offsets" , since = "1.75.0")] # [rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] pub const fn wrapping_byte_offset (self , count : isize) -> Self { self . cast :: < u8 > () . wrapping_offset (count) . with_metadata_of (self) } # [doc = " Masks out bits of the pointer according to a mask."] # [doc = ""] # [doc = " This is convenience for `ptr.map_addr(|a| a & mask)`."] # [doc = ""] # [doc = " For non-`Sized` pointees this operation changes only the data pointer,"] # [doc = " leaving the metadata untouched."] # [doc = ""] # [doc = " ## Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(ptr_mask)]"] # [doc = " let mut v = 17_u32;"] # [doc = " let ptr: *mut u32 = &mut v;"] # [doc = ""] # [doc = " // `u32` is 4 bytes aligned,"] # [doc = " // which means that lower 2 bits are always 0."] # [doc = " let tag_mask = 0b11;"] # [doc = " let ptr_mask = !tag_mask;"] # [doc = ""] # [doc = " // We can store something in these lower bits"] # [doc = " let tagged_ptr = ptr.map_addr(|a| a | 0b10);"] # [doc = ""] # [doc = " // Get the \"tag\" back"] # [doc = " let tag = tagged_ptr.addr() & tag_mask;"] # [doc = " assert_eq!(tag, 0b10);"] # [doc = ""] # [doc = " // Note that `tagged_ptr` is unaligned, it's UB to read from/write to it."] # [doc = " // To get original pointer `mask` can be used:"] # [doc = " let masked_ptr = tagged_ptr.mask(ptr_mask);"] # [doc = " assert_eq!(unsafe { *masked_ptr }, 17);"] # [doc = ""] # [doc = " unsafe { *masked_ptr = 0 };"] # [doc = " assert_eq!(v, 0);"] # [doc = " ```"] # [unstable (feature = "ptr_mask" , issue = "98290")] # [must_use = "returns a new pointer rather than modifying its argument"] # [inline (always)] pub fn mask (self , mask : usize) -> * mut T { intrinsics :: ptr_mask (self . cast :: < () > () , mask) . cast_mut () . with_metadata_of (self) } # [doc = " Returns `None` if the pointer is null, or else returns a unique reference to"] # [doc = " the value wrapped in `Some`. If the value may be uninitialized, [`as_uninit_mut`]"] # [doc = " must be used instead."] # [doc = ""] # [doc = " For the shared counterpart see [`as_ref`]."] # [doc = ""] # [doc = " [`as_uninit_mut`]: #method.as_uninit_mut"] # [doc = " [`as_ref`]: pointer#method.as_ref-1"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " When calling this method, you have to ensure that *either*"] # [doc = " the pointer is null *or*"] # [doc = " the pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion)."] # [doc = ""] # [doc = " # Panics during const evaluation"] # [doc = ""] # [doc = " This method will panic during const evaluation if the pointer cannot be"] # [doc = " determined to be null or not. See [`is_null`] for more information."] # [doc = ""] # [doc = " [`is_null`]: #method.is_null-1"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut s = [1, 2, 3];"] # [doc = " let ptr: *mut u32 = s.as_mut_ptr();"] # [doc = " let first_value = unsafe { ptr.as_mut().unwrap() };"] # [doc = " *first_value = 4;"] # [doc = " # assert_eq!(s, [4, 2, 3]);"] # [doc = " println!(\"{s:?}\"); // It'll print: \"[4, 2, 3]\"."] # [doc = " ```"] # [doc = ""] # [doc = " # Null-unchecked version"] # [doc = ""] # [doc = " If you are sure the pointer can never be null and are looking for some kind of"] # [doc = " `as_mut_unchecked` that returns the `&mut T` instead of `Option<&mut T>`, know that"] # [doc = " you can dereference the pointer directly."] # [doc = ""] # [doc = " ```"] # [doc = " let mut s = [1, 2, 3];"] # [doc = " let ptr: *mut u32 = s.as_mut_ptr();"] # [doc = " let first_value = unsafe { &mut *ptr };"] # [doc = " *first_value = 4;"] # [doc = " # assert_eq!(s, [4, 2, 3]);"] # [doc = " println!(\"{s:?}\"); // It'll print: \"[4, 2, 3]\"."] # [doc = " ```"] # [stable (feature = "ptr_as_ref" , since = "1.9.0")] # [rustc_const_stable (feature = "const_ptr_is_null" , since = "1.84.0")] # [inline] pub const unsafe fn as_mut < 'a > (self) -> Option < & 'a mut T > { if self . is_null () { None } else { unsafe { Some (& mut * self) } } } # [doc = " Returns a unique reference to the value behind the pointer."] # [doc = " If the pointer may be null or the value may be uninitialized, [`as_uninit_mut`] must be used instead."] # [doc = " If the pointer may be null, but the value is known to have been initialized, [`as_mut`] must be used instead."] # [doc = ""] # [doc = " For the shared counterpart see [`as_ref_unchecked`]."] # [doc = ""] # [doc = " [`as_mut`]: #method.as_mut"] # [doc = " [`as_uninit_mut`]: #method.as_uninit_mut"] # [doc = " [`as_ref_unchecked`]: #method.as_mut_unchecked"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " When calling this method, you have to ensure that"] # [doc = " the pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(ptr_as_ref_unchecked)]"] # [doc = " let mut s = [1, 2, 3];"] # [doc = " let ptr: *mut u32 = s.as_mut_ptr();"] # [doc = " let first_value = unsafe { ptr.as_mut_unchecked() };"] # [doc = " *first_value = 4;"] # [doc = " # assert_eq!(s, [4, 2, 3]);"] # [doc = " println!(\"{s:?}\"); // It'll print: \"[4, 2, 3]\"."] # [doc = " ```"] # [unstable (feature = "ptr_as_ref_unchecked" , issue = "122034")] # [inline] # [must_use] pub const unsafe fn as_mut_unchecked < 'a > (self) -> & 'a mut T { unsafe { & mut * self } } # [doc = " Returns `None` if the pointer is null, or else returns a unique reference to"] # [doc = " the value wrapped in `Some`. In contrast to [`as_mut`], this does not require"] # [doc = " that the value has to be initialized."] # [doc = ""] # [doc = " For the shared counterpart see [`as_uninit_ref`]."] # [doc = ""] # [doc = " [`as_mut`]: #method.as_mut"] # [doc = " [`as_uninit_ref`]: pointer#method.as_uninit_ref-1"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " When calling this method, you have to ensure that *either* the pointer is null *or*"] # [doc = " the pointer is [convertible to a reference](crate::ptr#pointer-to-reference-conversion)."] # [doc = ""] # [doc = " # Panics during const evaluation"] # [doc = ""] # [doc = " This method will panic during const evaluation if the pointer cannot be"] # [doc = " determined to be null or not. See [`is_null`] for more information."] # [doc = ""] # [doc = " [`is_null`]: #method.is_null-1"] # [inline] # [unstable (feature = "ptr_as_uninit" , issue = "75402")] pub const unsafe fn as_uninit_mut < 'a > (self) -> Option < & 'a mut MaybeUninit < T > > where T : Sized , { if self . is_null () { None } else { Some (unsafe { & mut * (self as * mut MaybeUninit < T >) }) } } # [doc = " Returns whether two pointers are guaranteed to be equal."] # [doc = ""] # [doc = " At runtime this function behaves like `Some(self == other)`."] # [doc = " However, in some contexts (e.g., compile-time evaluation),"] # [doc = " it is not always possible to determine equality of two pointers, so this function may"] # [doc = " spuriously return `None` for pointers that later actually turn out to have its equality known."] # [doc = " But when it returns `Some`, the pointers' equality is guaranteed to be known."] # [doc = ""] # [doc = " The return value may change from `Some` to `None` and vice versa depending on the compiler"] # [doc = " version and unsafe code must not"] # [doc = " rely on the result of this function for soundness. It is suggested to only use this function"] # [doc = " for performance optimizations where spurious `None` return values by this function do not"] # [doc = " affect the outcome, but just the performance."] # [doc = " The consequences of using this method to make runtime and compile-time code behave"] # [doc = " differently have not been explored. This method should not be used to introduce such"] # [doc = " differences, and it should also not be stabilized before we have a better understanding"] # [doc = " of this issue."] # [unstable (feature = "const_raw_ptr_comparison" , issue = "53020")] # [rustc_const_unstable (feature = "const_raw_ptr_comparison" , issue = "53020")] # [inline] pub const fn guaranteed_eq (self , other : * mut T) -> Option < bool > where T : Sized , { (self as * const T) . guaranteed_eq (other as _) } # [doc = " Returns whether two pointers are guaranteed to be inequal."] # [doc = ""] # [doc = " At runtime this function behaves like `Some(self != other)`."] # [doc = " However, in some contexts (e.g., compile-time evaluation),"] # [doc = " it is not always possible to determine inequality of two pointers, so this function may"] # [doc = " spuriously return `None` for pointers that later actually turn out to have its inequality known."] # [doc = " But when it returns `Some`, the pointers' inequality is guaranteed to be known."] # [doc = ""] # [doc = " The return value may change from `Some` to `None` and vice versa depending on the compiler"] # [doc = " version and unsafe code must not"] # [doc = " rely on the result of this function for soundness. It is suggested to only use this function"] # [doc = " for performance optimizations where spurious `None` return values by this function do not"] # [doc = " affect the outcome, but just the performance."] # [doc = " The consequences of using this method to make runtime and compile-time code behave"] # [doc = " differently have not been explored. This method should not be used to introduce such"] # [doc = " differences, and it should also not be stabilized before we have a better understanding"] # [doc = " of this issue."] # [unstable (feature = "const_raw_ptr_comparison" , issue = "53020")] # [rustc_const_unstable (feature = "const_raw_ptr_comparison" , issue = "53020")] # [inline] pub const fn guaranteed_ne (self , other : * mut T) -> Option < bool > where T : Sized , { (self as * const T) . guaranteed_ne (other as _) } # [doc = " Calculates the distance between two pointers within the same allocation. The returned value is in"] # [doc = " units of T: the distance in bytes divided by `size_of::<T>()`."] # [doc = ""] # [doc = " This is equivalent to `(self as isize - origin as isize) / (size_of::<T>() as isize)`,"] # [doc = " except that it has a lot more opportunities for UB, in exchange for the compiler"] # [doc = " better understanding what you are doing."] # [doc = ""] # [doc = " The primary motivation of this method is for computing the `len` of an array/slice"] # [doc = " of `T` that you are currently representing as a \"start\" and \"end\" pointer"] # [doc = " (and \"end\" is \"one past the end\" of the array)."] # [doc = " In that case, `end.offset_from(start)` gets you the length of the array."] # [doc = ""] # [doc = " All of the following safety requirements are trivially satisfied for this usecase."] # [doc = ""] # [doc = " [`offset`]: pointer#method.offset-1"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If any of the following conditions are violated, the result is Undefined Behavior:"] # [doc = ""] # [doc = " * `self` and `origin` must either"] # [doc = ""] # [doc = "   * point to the same address, or"] # [doc = "   * both be [derived from][crate::ptr#provenance] a pointer to the same [allocation], and the memory range between"] # [doc = "     the two pointers must be in bounds of that object. (See below for an example.)"] # [doc = ""] # [doc = " * The distance between the pointers, in bytes, must be an exact multiple"] # [doc = "   of the size of `T`."] # [doc = ""] # [doc = " As a consequence, the absolute distance between the pointers, in bytes, computed on"] # [doc = " mathematical integers (without \"wrapping around\"), cannot overflow an `isize`. This is"] # [doc = " implied by the in-bounds requirement, and the fact that no allocation can be larger"] # [doc = " than `isize::MAX` bytes."] # [doc = ""] # [doc = " The requirement for pointers to be derived from the same allocation is primarily"] # [doc = " needed for `const`-compatibility: the distance between pointers into *different* allocated"] # [doc = " objects is not known at compile-time. However, the requirement also exists at"] # [doc = " runtime and may be exploited by optimizations. If you wish to compute the difference between"] # [doc = " pointers that are not guaranteed to be from the same allocation, use `(self as isize -"] # [doc = " origin as isize) / size_of::<T>()`."] # [doc = ""] # [doc = " [`add`]: #method.add"] # [doc = " [allocation]: crate::ptr#allocation"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if `T` is a Zero-Sized Type (\"ZST\")."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " let mut a = [0; 5];"] # [doc = " let ptr1: *mut i32 = &mut a[1];"] # [doc = " let ptr2: *mut i32 = &mut a[3];"] # [doc = " unsafe {"] # [doc = "     assert_eq!(ptr2.offset_from(ptr1), 2);"] # [doc = "     assert_eq!(ptr1.offset_from(ptr2), -2);"] # [doc = "     assert_eq!(ptr1.offset(2), ptr2);"] # [doc = "     assert_eq!(ptr2.offset(-2), ptr1);"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " *Incorrect* usage:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " let ptr1 = Box::into_raw(Box::new(0u8));"] # [doc = " let ptr2 = Box::into_raw(Box::new(1u8));"] # [doc = " let diff = (ptr2 as isize).wrapping_sub(ptr1 as isize);"] # [doc = " // Make ptr2_other an \"alias\" of ptr2.add(1), but derived from ptr1."] # [doc = " let ptr2_other = (ptr1 as *mut u8).wrapping_offset(diff).wrapping_offset(1);"] # [doc = " assert_eq!(ptr2 as usize, ptr2_other as usize);"] # [doc = " // Since ptr2_other and ptr2 are derived from pointers to different objects,"] # [doc = " // computing their offset is undefined behavior, even though"] # [doc = " // they point to addresses that are in-bounds of the same object!"] # [doc = " unsafe {"] # [doc = "     let one = ptr2_other.offset_from(ptr2); // Undefined Behavior! ⚠\u{fe0f}"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "ptr_offset_from" , since = "1.47.0")] # [rustc_const_stable (feature = "const_ptr_offset_from" , since = "1.65.0")] # [inline (always)] # [cfg_attr (miri , track_caller)] pub const unsafe fn offset_from (self , origin : * const T) -> isize where T : Sized , { unsafe { (self as * const T) . offset_from (origin) } } # [doc = " Calculates the distance between two pointers within the same allocation. The returned value is in"] # [doc = " units of **bytes**."] # [doc = ""] # [doc = " This is purely a convenience for casting to a `u8` pointer and"] # [doc = " using [`offset_from`][pointer::offset_from] on it. See that method for"] # [doc = " documentation and safety requirements."] # [doc = ""] # [doc = " For non-`Sized` pointees this operation considers only the data pointers,"] # [doc = " ignoring the metadata."] # [inline (always)] # [stable (feature = "pointer_byte_offsets" , since = "1.75.0")] # [rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] # [cfg_attr (miri , track_caller)] pub const unsafe fn byte_offset_from < U : ? Sized > (self , origin : * const U) -> isize { unsafe { self . cast :: < u8 > () . offset_from (origin . cast :: < u8 > ()) } } # [doc = " Calculates the distance between two pointers within the same allocation, *where it's known that"] # [doc = " `self` is equal to or greater than `origin`*. The returned value is in"] # [doc = " units of T: the distance in bytes is divided by `size_of::<T>()`."] # [doc = ""] # [doc = " This computes the same value that [`offset_from`](#method.offset_from)"] # [doc = " would compute, but with the added precondition that the offset is"] # [doc = " guaranteed to be non-negative.  This method is equivalent to"] # [doc = " `usize::try_from(self.offset_from(origin)).unwrap_unchecked()`,"] # [doc = " but it provides slightly more information to the optimizer, which can"] # [doc = " sometimes allow it to optimize slightly better with some backends."] # [doc = ""] # [doc = " This method can be thought of as recovering the `count` that was passed"] # [doc = " to [`add`](#method.add) (or, with the parameters in the other order,"] # [doc = " to [`sub`](#method.sub)).  The following are all equivalent, assuming"] # [doc = " that their safety preconditions are met:"] # [doc = " ```rust"] # [doc = " # unsafe fn blah(ptr: *mut i32, origin: *mut i32, count: usize) -> bool { unsafe {"] # [doc = " ptr.offset_from_unsigned(origin) == count"] # [doc = " # &&"] # [doc = " origin.add(count) == ptr"] # [doc = " # &&"] # [doc = " ptr.sub(count) == origin"] # [doc = " # } }"] # [doc = " ```"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " - The distance between the pointers must be non-negative (`self >= origin`)"] # [doc = ""] # [doc = " - *All* the safety conditions of [`offset_from`](#method.offset_from)"] # [doc = "   apply to this method as well; see it for the full details."] # [doc = ""] # [doc = " Importantly, despite the return type of this method being able to represent"] # [doc = " a larger offset, it's still *not permitted* to pass pointers which differ"] # [doc = " by more than `isize::MAX` *bytes*.  As such, the result of this method will"] # [doc = " always be less than or equal to `isize::MAX as usize`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if `T` is a Zero-Sized Type (\"ZST\")."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut a = [0; 5];"] # [doc = " let p: *mut i32 = a.as_mut_ptr();"] # [doc = " unsafe {"] # [doc = "     let ptr1: *mut i32 = p.add(1);"] # [doc = "     let ptr2: *mut i32 = p.add(3);"] # [doc = ""] # [doc = "     assert_eq!(ptr2.offset_from_unsigned(ptr1), 2);"] # [doc = "     assert_eq!(ptr1.add(2), ptr2);"] # [doc = "     assert_eq!(ptr2.sub(2), ptr1);"] # [doc = "     assert_eq!(ptr2.offset_from_unsigned(ptr2), 0);"] # [doc = " }"] # [doc = ""] # [doc = " // This would be incorrect, as the pointers are not correctly ordered:"] # [doc = " // ptr1.offset_from(ptr2)"] # [doc = " ```"] # [stable (feature = "ptr_sub_ptr" , since = "1.87.0")] # [rustc_const_stable (feature = "const_ptr_sub_ptr" , since = "1.87.0")] # [inline] # [track_caller] pub const unsafe fn offset_from_unsigned (self , origin : * const T) -> usize where T : Sized , { unsafe { (self as * const T) . offset_from_unsigned (origin) } } # [doc = " Calculates the distance between two pointers within the same allocation, *where it's known that"] # [doc = " `self` is equal to or greater than `origin`*. The returned value is in"] # [doc = " units of **bytes**."] # [doc = ""] # [doc = " This is purely a convenience for casting to a `u8` pointer and"] # [doc = " using [`offset_from_unsigned`][pointer::offset_from_unsigned] on it."] # [doc = " See that method for documentation and safety requirements."] # [doc = ""] # [doc = " For non-`Sized` pointees this operation considers only the data pointers,"] # [doc = " ignoring the metadata."] # [stable (feature = "ptr_sub_ptr" , since = "1.87.0")] # [rustc_const_stable (feature = "const_ptr_sub_ptr" , since = "1.87.0")] # [inline] # [track_caller] pub const unsafe fn byte_offset_from_unsigned < U : ? Sized > (self , origin : * mut U) -> usize { unsafe { (self as * const T) . byte_offset_from_unsigned (origin) } } # [doc = include_str ! ("./docs/add.md")] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut s: String = \"123\".to_string();"] # [doc = " let ptr: *mut u8 = s.as_mut_ptr();"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     assert_eq!('2', *ptr.add(1) as char);"] # [doc = "     assert_eq!('3', *ptr.add(2) as char);"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [must_use = "returns a new pointer rather than modifying its argument"] # [rustc_const_stable (feature = "const_ptr_offset" , since = "1.61.0")] # [inline (always)] # [track_caller] pub const unsafe fn add (self , count : usize) -> Self where T : Sized , { # [cfg (debug_assertions)] # [inline] # [rustc_allow_const_fn_unstable (const_eval_select)] const fn runtime_add_nowrap (this : * const () , count : usize , size : usize) -> bool { const_eval_select ! (@ capture { this : * const () , count : usize , size : usize } -> bool : if const { true } else { let Some (byte_offset) = count . checked_mul (size) else { return false ; } ; let (_ , overflow) = this . addr () . overflowing_add (byte_offset) ; byte_offset <= (isize :: MAX as usize) && ! overflow }) } # [cfg (debug_assertions)] ub_checks :: assert_unsafe_precondition ! (check_language_ub , "ptr::add requires that the address calculation does not overflow" , (this : * const () = self as * const () , count : usize = count , size : usize = size_of ::< T > () ,) => runtime_add_nowrap (this , count , size)) ; unsafe { intrinsics :: offset (self , count) } } # [doc = " Adds an unsigned offset in bytes to a pointer."] # [doc = ""] # [doc = " `count` is in units of bytes."] # [doc = ""] # [doc = " This is purely a convenience for casting to a `u8` pointer and"] # [doc = " using [add][pointer::add] on it. See that method for documentation"] # [doc = " and safety requirements."] # [doc = ""] # [doc = " For non-`Sized` pointees this operation changes only the data pointer,"] # [doc = " leaving the metadata untouched."] # [must_use] # [inline (always)] # [stable (feature = "pointer_byte_offsets" , since = "1.75.0")] # [rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] # [track_caller] pub const unsafe fn byte_add (self , count : usize) -> Self { unsafe { self . cast :: < u8 > () . add (count) . with_metadata_of (self) } } # [doc = " Subtracts an unsigned offset from a pointer."] # [doc = ""] # [doc = " This can only move the pointer backward (or not move it). If you need to move forward or"] # [doc = " backward depending on the value, then you might want [`offset`](#method.offset) instead"] # [doc = " which takes a signed offset."] # [doc = ""] # [doc = " `count` is in units of T; e.g., a `count` of 3 represents a pointer"] # [doc = " offset of `3 * size_of::<T>()` bytes."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If any of the following conditions are violated, the result is Undefined Behavior:"] # [doc = ""] # [doc = " * The offset in bytes, `count * size_of::<T>()`, computed on mathematical integers (without"] # [doc = "   \"wrapping around\"), must fit in an `isize`."] # [doc = ""] # [doc = " * If the computed offset is non-zero, then `self` must be [derived from][crate::ptr#provenance] a pointer to some"] # [doc = "   [allocation], and the entire memory range between `self` and the result must be in"] # [doc = "   bounds of that allocation. In particular, this range must not \"wrap around\" the edge"] # [doc = "   of the address space."] # [doc = ""] # [doc = " Allocations can never be larger than `isize::MAX` bytes, so if the computed offset"] # [doc = " stays in bounds of the allocation, it is guaranteed to satisfy the first requirement."] # [doc = " This implies, for instance, that `vec.as_ptr().add(vec.len())` (for `vec: Vec<T>`) is always"] # [doc = " safe."] # [doc = ""] # [doc = " Consider using [`wrapping_sub`] instead if these constraints are"] # [doc = " difficult to satisfy. The only advantage of this method is that it"] # [doc = " enables more aggressive compiler optimizations."] # [doc = ""] # [doc = " [`wrapping_sub`]: #method.wrapping_sub"] # [doc = " [allocation]: crate::ptr#allocation"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let s: &str = \"123\";"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     let end: *const u8 = s.as_ptr().add(3);"] # [doc = "     assert_eq!('3', *end.sub(1) as char);"] # [doc = "     assert_eq!('2', *end.sub(2) as char);"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [must_use = "returns a new pointer rather than modifying its argument"] # [rustc_const_stable (feature = "const_ptr_offset" , since = "1.61.0")] # [inline (always)] # [track_caller] pub const unsafe fn sub (self , count : usize) -> Self where T : Sized , { # [cfg (debug_assertions)] # [inline] # [rustc_allow_const_fn_unstable (const_eval_select)] const fn runtime_sub_nowrap (this : * const () , count : usize , size : usize) -> bool { const_eval_select ! (@ capture { this : * const () , count : usize , size : usize } -> bool : if const { true } else { let Some (byte_offset) = count . checked_mul (size) else { return false ; } ; byte_offset <= (isize :: MAX as usize) && this . addr () >= byte_offset }) } # [cfg (debug_assertions)] ub_checks :: assert_unsafe_precondition ! (check_language_ub , "ptr::sub requires that the address calculation does not overflow" , (this : * const () = self as * const () , count : usize = count , size : usize = size_of ::< T > () ,) => runtime_sub_nowrap (this , count , size)) ; if T :: IS_ZST { self } else { unsafe { intrinsics :: offset (self , intrinsics :: unchecked_sub (0 , count as isize)) } } } # [doc = " Subtracts an unsigned offset in bytes from a pointer."] # [doc = ""] # [doc = " `count` is in units of bytes."] # [doc = ""] # [doc = " This is purely a convenience for casting to a `u8` pointer and"] # [doc = " using [sub][pointer::sub] on it. See that method for documentation"] # [doc = " and safety requirements."] # [doc = ""] # [doc = " For non-`Sized` pointees this operation changes only the data pointer,"] # [doc = " leaving the metadata untouched."] # [must_use] # [inline (always)] # [stable (feature = "pointer_byte_offsets" , since = "1.75.0")] # [rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] # [track_caller] pub const unsafe fn byte_sub (self , count : usize) -> Self { unsafe { self . cast :: < u8 > () . sub (count) . with_metadata_of (self) } } # [doc = " Adds an unsigned offset to a pointer using wrapping arithmetic."] # [doc = ""] # [doc = " `count` is in units of T; e.g., a `count` of 3 represents a pointer"] # [doc = " offset of `3 * size_of::<T>()` bytes."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This operation itself is always safe, but using the resulting pointer is not."] # [doc = ""] # [doc = " The resulting pointer \"remembers\" the [allocation] that `self` points to; it must not"] # [doc = " be used to read or write other allocations."] # [doc = ""] # [doc = " In other words, `let z = x.wrapping_add((y as usize) - (x as usize))` does *not* make `z`"] # [doc = " the same as `y` even if we assume `T` has size `1` and there is no overflow: `z` is still"] # [doc = " attached to the object `x` is attached to, and dereferencing it is Undefined Behavior unless"] # [doc = " `x` and `y` point into the same allocation."] # [doc = ""] # [doc = " Compared to [`add`], this method basically delays the requirement of staying within the"] # [doc = " same allocation: [`add`] is immediate Undefined Behavior when crossing object"] # [doc = " boundaries; `wrapping_add` produces a pointer but still leads to Undefined Behavior if a"] # [doc = " pointer is dereferenced when it is out-of-bounds of the object it is attached to. [`add`]"] # [doc = " can be optimized better and is thus preferable in performance-sensitive code."] # [doc = ""] # [doc = " The delayed check only considers the value of the pointer that was dereferenced, not the"] # [doc = " intermediate values used during the computation of the final result. For example,"] # [doc = " `x.wrapping_add(o).wrapping_sub(o)` is always the same as `x`. In other words, leaving the"] # [doc = " allocation and then re-entering it later is permitted."] # [doc = ""] # [doc = " [`add`]: #method.add"] # [doc = " [allocation]: crate::ptr#allocation"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " // Iterate using a raw pointer in increments of two elements"] # [doc = " let data = [1u8, 2, 3, 4, 5];"] # [doc = " let mut ptr: *const u8 = data.as_ptr();"] # [doc = " let step = 2;"] # [doc = " let end_rounded_up = ptr.wrapping_add(6);"] # [doc = ""] # [doc = " // This loop prints \"1, 3, 5, \""] # [doc = " while ptr != end_rounded_up {"] # [doc = "     unsafe {"] # [doc = "         print!(\"{}, \", *ptr);"] # [doc = "     }"] # [doc = "     ptr = ptr.wrapping_add(step);"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [must_use = "returns a new pointer rather than modifying its argument"] # [rustc_const_stable (feature = "const_ptr_offset" , since = "1.61.0")] # [inline (always)] pub const fn wrapping_add (self , count : usize) -> Self where T : Sized , { self . wrapping_offset (count as isize) } # [doc = " Adds an unsigned offset in bytes to a pointer using wrapping arithmetic."] # [doc = ""] # [doc = " `count` is in units of bytes."] # [doc = ""] # [doc = " This is purely a convenience for casting to a `u8` pointer and"] # [doc = " using [wrapping_add][pointer::wrapping_add] on it. See that method for documentation."] # [doc = ""] # [doc = " For non-`Sized` pointees this operation changes only the data pointer,"] # [doc = " leaving the metadata untouched."] # [must_use] # [inline (always)] # [stable (feature = "pointer_byte_offsets" , since = "1.75.0")] # [rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] pub const fn wrapping_byte_add (self , count : usize) -> Self { self . cast :: < u8 > () . wrapping_add (count) . with_metadata_of (self) } # [doc = " Subtracts an unsigned offset from a pointer using wrapping arithmetic."] # [doc = ""] # [doc = " `count` is in units of T; e.g., a `count` of 3 represents a pointer"] # [doc = " offset of `3 * size_of::<T>()` bytes."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This operation itself is always safe, but using the resulting pointer is not."] # [doc = ""] # [doc = " The resulting pointer \"remembers\" the [allocation] that `self` points to; it must not"] # [doc = " be used to read or write other allocations."] # [doc = ""] # [doc = " In other words, `let z = x.wrapping_sub((x as usize) - (y as usize))` does *not* make `z`"] # [doc = " the same as `y` even if we assume `T` has size `1` and there is no overflow: `z` is still"] # [doc = " attached to the object `x` is attached to, and dereferencing it is Undefined Behavior unless"] # [doc = " `x` and `y` point into the same allocation."] # [doc = ""] # [doc = " Compared to [`sub`], this method basically delays the requirement of staying within the"] # [doc = " same allocation: [`sub`] is immediate Undefined Behavior when crossing object"] # [doc = " boundaries; `wrapping_sub` produces a pointer but still leads to Undefined Behavior if a"] # [doc = " pointer is dereferenced when it is out-of-bounds of the object it is attached to. [`sub`]"] # [doc = " can be optimized better and is thus preferable in performance-sensitive code."] # [doc = ""] # [doc = " The delayed check only considers the value of the pointer that was dereferenced, not the"] # [doc = " intermediate values used during the computation of the final result. For example,"] # [doc = " `x.wrapping_add(o).wrapping_sub(o)` is always the same as `x`. In other words, leaving the"] # [doc = " allocation and then re-entering it later is permitted."] # [doc = ""] # [doc = " [`sub`]: #method.sub"] # [doc = " [allocation]: crate::ptr#allocation"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " // Iterate using a raw pointer in increments of two elements (backwards)"] # [doc = " let data = [1u8, 2, 3, 4, 5];"] # [doc = " let mut ptr: *const u8 = data.as_ptr();"] # [doc = " let start_rounded_down = ptr.wrapping_sub(2);"] # [doc = " ptr = ptr.wrapping_add(4);"] # [doc = " let step = 2;"] # [doc = " // This loop prints \"5, 3, 1, \""] # [doc = " while ptr != start_rounded_down {"] # [doc = "     unsafe {"] # [doc = "         print!(\"{}, \", *ptr);"] # [doc = "     }"] # [doc = "     ptr = ptr.wrapping_sub(step);"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [must_use = "returns a new pointer rather than modifying its argument"] # [rustc_const_stable (feature = "const_ptr_offset" , since = "1.61.0")] # [inline (always)] pub const fn wrapping_sub (self , count : usize) -> Self where T : Sized , { self . wrapping_offset ((count as isize) . wrapping_neg ()) } # [doc = " Subtracts an unsigned offset in bytes from a pointer using wrapping arithmetic."] # [doc = ""] # [doc = " `count` is in units of bytes."] # [doc = ""] # [doc = " This is purely a convenience for casting to a `u8` pointer and"] # [doc = " using [wrapping_sub][pointer::wrapping_sub] on it. See that method for documentation."] # [doc = ""] # [doc = " For non-`Sized` pointees this operation changes only the data pointer,"] # [doc = " leaving the metadata untouched."] # [must_use] # [inline (always)] # [stable (feature = "pointer_byte_offsets" , since = "1.75.0")] # [rustc_const_stable (feature = "const_pointer_byte_offsets" , since = "1.75.0")] pub const fn wrapping_byte_sub (self , count : usize) -> Self { self . cast :: < u8 > () . wrapping_sub (count) . with_metadata_of (self) } # [doc = " Reads the value from `self` without moving it. This leaves the"] # [doc = " memory in `self` unchanged."] # [doc = ""] # [doc = " See [`ptr::read`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::read`]: crate::ptr::read()"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [rustc_const_stable (feature = "const_ptr_read" , since = "1.71.0")] # [inline (always)] # [track_caller] pub const unsafe fn read (self) -> T where T : Sized , { unsafe { read (self) } } # [doc = " Performs a volatile read of the value from `self` without moving it. This"] # [doc = " leaves the memory in `self` unchanged."] # [doc = ""] # [doc = " Volatile operations are intended to act on I/O memory, and are guaranteed"] # [doc = " to not be elided or reordered by the compiler across other volatile"] # [doc = " operations."] # [doc = ""] # [doc = " See [`ptr::read_volatile`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::read_volatile`]: crate::ptr::read_volatile()"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [inline (always)] # [track_caller] pub unsafe fn read_volatile (self) -> T where T : Sized , { unsafe { read_volatile (self) } } # [doc = " Reads the value from `self` without moving it. This leaves the"] # [doc = " memory in `self` unchanged."] # [doc = ""] # [doc = " Unlike `read`, the pointer may be unaligned."] # [doc = ""] # [doc = " See [`ptr::read_unaligned`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::read_unaligned`]: crate::ptr::read_unaligned()"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [rustc_const_stable (feature = "const_ptr_read" , since = "1.71.0")] # [inline (always)] # [track_caller] pub const unsafe fn read_unaligned (self) -> T where T : Sized , { unsafe { read_unaligned (self) } } # [doc = " Copies `count * size_of::<T>()` bytes from `self` to `dest`. The source"] # [doc = " and destination may overlap."] # [doc = ""] # [doc = " NOTE: this has the *same* argument order as [`ptr::copy`]."] # [doc = ""] # [doc = " See [`ptr::copy`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::copy`]: crate::ptr::copy()"] # [rustc_const_stable (feature = "const_intrinsic_copy" , since = "1.83.0")] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [inline (always)] # [track_caller] pub const unsafe fn copy_to (self , dest : * mut T , count : usize) where T : Sized , { unsafe { copy (self , dest , count) } } # [doc = " Copies `count * size_of::<T>()` bytes from `self` to `dest`. The source"] # [doc = " and destination may *not* overlap."] # [doc = ""] # [doc = " NOTE: this has the *same* argument order as [`ptr::copy_nonoverlapping`]."] # [doc = ""] # [doc = " See [`ptr::copy_nonoverlapping`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::copy_nonoverlapping`]: crate::ptr::copy_nonoverlapping()"] # [rustc_const_stable (feature = "const_intrinsic_copy" , since = "1.83.0")] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [inline (always)] # [track_caller] pub const unsafe fn copy_to_nonoverlapping (self , dest : * mut T , count : usize) where T : Sized , { unsafe { copy_nonoverlapping (self , dest , count) } } # [doc = " Copies `count * size_of::<T>()` bytes from `src` to `self`. The source"] # [doc = " and destination may overlap."] # [doc = ""] # [doc = " NOTE: this has the *opposite* argument order of [`ptr::copy`]."] # [doc = ""] # [doc = " See [`ptr::copy`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::copy`]: crate::ptr::copy()"] # [rustc_const_stable (feature = "const_intrinsic_copy" , since = "1.83.0")] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [inline (always)] # [track_caller] pub const unsafe fn copy_from (self , src : * const T , count : usize) where T : Sized , { unsafe { copy (src , self , count) } } # [doc = " Copies `count * size_of::<T>()` bytes from `src` to `self`. The source"] # [doc = " and destination may *not* overlap."] # [doc = ""] # [doc = " NOTE: this has the *opposite* argument order of [`ptr::copy_nonoverlapping`]."] # [doc = ""] # [doc = " See [`ptr::copy_nonoverlapping`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::copy_nonoverlapping`]: crate::ptr::copy_nonoverlapping()"] # [rustc_const_stable (feature = "const_intrinsic_copy" , since = "1.83.0")] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [inline (always)] # [track_caller] pub const unsafe fn copy_from_nonoverlapping (self , src : * const T , count : usize) where T : Sized , { unsafe { copy_nonoverlapping (src , self , count) } } # [doc = " Executes the destructor (if any) of the pointed-to value."] # [doc = ""] # [doc = " See [`ptr::drop_in_place`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::drop_in_place`]: crate::ptr::drop_in_place()"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [inline (always)] pub unsafe fn drop_in_place (self) { unsafe { drop_in_place (self) } } # [doc = " Overwrites a memory location with the given value without reading or"] # [doc = " dropping the old value."] # [doc = ""] # [doc = " See [`ptr::write`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::write`]: crate::ptr::write()"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [rustc_const_stable (feature = "const_ptr_write" , since = "1.83.0")] # [inline (always)] # [track_caller] pub const unsafe fn write (self , val : T) where T : Sized , { unsafe { write (self , val) } } # [doc = " Invokes memset on the specified pointer, setting `count * size_of::<T>()`"] # [doc = " bytes of memory starting at `self` to `val`."] # [doc = ""] # [doc = " See [`ptr::write_bytes`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::write_bytes`]: crate::ptr::write_bytes()"] # [doc (alias = "memset")] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [rustc_const_stable (feature = "const_ptr_write" , since = "1.83.0")] # [inline (always)] # [track_caller] pub const unsafe fn write_bytes (self , val : u8 , count : usize) where T : Sized , { unsafe { write_bytes (self , val , count) } } # [doc = " Performs a volatile write of a memory location with the given value without"] # [doc = " reading or dropping the old value."] # [doc = ""] # [doc = " Volatile operations are intended to act on I/O memory, and are guaranteed"] # [doc = " to not be elided or reordered by the compiler across other volatile"] # [doc = " operations."] # [doc = ""] # [doc = " See [`ptr::write_volatile`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::write_volatile`]: crate::ptr::write_volatile()"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [inline (always)] # [track_caller] pub unsafe fn write_volatile (self , val : T) where T : Sized , { unsafe { write_volatile (self , val) } } # [doc = " Overwrites a memory location with the given value without reading or"] # [doc = " dropping the old value."] # [doc = ""] # [doc = " Unlike `write`, the pointer may be unaligned."] # [doc = ""] # [doc = " See [`ptr::write_unaligned`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::write_unaligned`]: crate::ptr::write_unaligned()"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [rustc_const_stable (feature = "const_ptr_write" , since = "1.83.0")] # [inline (always)] # [track_caller] pub const unsafe fn write_unaligned (self , val : T) where T : Sized , { unsafe { write_unaligned (self , val) } } # [doc = " Replaces the value at `self` with `src`, returning the old"] # [doc = " value, without dropping either."] # [doc = ""] # [doc = " See [`ptr::replace`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::replace`]: crate::ptr::replace()"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [rustc_const_stable (feature = "const_inherent_ptr_replace" , since = "1.88.0")] # [inline (always)] pub const unsafe fn replace (self , src : T) -> T where T : Sized , { unsafe { replace (self , src) } } # [doc = " Swaps the values at two mutable locations of the same type, without"] # [doc = " deinitializing either. They may overlap, unlike `mem::swap` which is"] # [doc = " otherwise equivalent."] # [doc = ""] # [doc = " See [`ptr::swap`] for safety concerns and examples."] # [doc = ""] # [doc = " [`ptr::swap`]: crate::ptr::swap()"] # [stable (feature = "pointer_methods" , since = "1.26.0")] # [rustc_const_stable (feature = "const_swap" , since = "1.85.0")] # [inline (always)] pub const unsafe fn swap (self , with : * mut T) where T : Sized , { unsafe { swap (self , with) } } # [doc = " Computes the offset that needs to be applied to the pointer in order to make it aligned to"] # [doc = " `align`."] # [doc = ""] # [doc = " If it is not possible to align the pointer, the implementation returns"] # [doc = " `usize::MAX`."] # [doc = ""] # [doc = " The offset is expressed in number of `T` elements, and not bytes. The value returned can be"] # [doc = " used with the `wrapping_add` method."] # [doc = ""] # [doc = " There are no guarantees whatsoever that offsetting the pointer will not overflow or go"] # [doc = " beyond the allocation that the pointer points into. It is up to the caller to ensure that"] # [doc = " the returned offset is correct in all terms other than alignment."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " The function panics if `align` is not a power-of-two."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Accessing adjacent `u8` as `u16`"] # [doc = ""] # [doc = " ```"] # [doc = " # unsafe {"] # [doc = " let mut x = [5_u8, 6, 7, 8, 9];"] # [doc = " let ptr = x.as_mut_ptr();"] # [doc = " let offset = ptr.align_offset(align_of::<u16>());"] # [doc = ""] # [doc = " if offset < x.len() - 1 {"] # [doc = "     let u16_ptr = ptr.add(offset).cast::<u16>();"] # [doc = "     *u16_ptr = 0;"] # [doc = ""] # [doc = "     assert!(x == [0, 0, 7, 8, 9] || x == [5, 0, 0, 8, 9]);"] # [doc = " } else {"] # [doc = "     // while the pointer can be aligned via `offset`, it would point"] # [doc = "     // outside the allocation"] # [doc = " }"] # [doc = " # }"] # [doc = " ```"] # [must_use] # [inline] # [stable (feature = "align_offset" , since = "1.36.0")] pub fn align_offset (self , align : usize) -> usize where T : Sized , { if ! align . is_power_of_two () { panic ! ("align_offset: align is not a power-of-two") ; } let ret = unsafe { align_offset (self , align) } ; # [cfg (miri)] if ret != usize :: MAX { intrinsics :: miri_promise_symbolic_alignment (self . wrapping_add (ret) . cast_const () . cast () , align ,) ; } ret } # [doc = " Returns whether the pointer is properly aligned for `T`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " // On some platforms, the alignment of i32 is less than 4."] # [doc = " #[repr(align(4))]"] # [doc = " struct AlignedI32(i32);"] # [doc = ""] # [doc = " let mut data = AlignedI32(42);"] # [doc = " let ptr = &mut data as *mut AlignedI32;"] # [doc = ""] # [doc = " assert!(ptr.is_aligned());"] # [doc = " assert!(!ptr.wrapping_byte_add(1).is_aligned());"] # [doc = " ```"] # [must_use] # [inline] # [stable (feature = "pointer_is_aligned" , since = "1.79.0")] pub fn is_aligned (self) -> bool where T : Sized , { self . is_aligned_to (align_of :: < T > ()) } # [doc = " Returns whether the pointer is aligned to `align`."] # [doc = ""] # [doc = " For non-`Sized` pointees this operation considers only the data pointer,"] # [doc = " ignoring the metadata."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " The function panics if `align` is not a power-of-two (this includes 0)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(pointer_is_aligned_to)]"] # [doc = ""] # [doc = " // On some platforms, the alignment of i32 is less than 4."] # [doc = " #[repr(align(4))]"] # [doc = " struct AlignedI32(i32);"] # [doc = ""] # [doc = " let mut data = AlignedI32(42);"] # [doc = " let ptr = &mut data as *mut AlignedI32;"] # [doc = ""] # [doc = " assert!(ptr.is_aligned_to(1));"] # [doc = " assert!(ptr.is_aligned_to(2));"] # [doc = " assert!(ptr.is_aligned_to(4));"] # [doc = ""] # [doc = " assert!(ptr.wrapping_byte_add(2).is_aligned_to(2));"] # [doc = " assert!(!ptr.wrapping_byte_add(2).is_aligned_to(4));"] # [doc = ""] # [doc = " assert_ne!(ptr.is_aligned_to(8), ptr.wrapping_add(1).is_aligned_to(8));"] # [doc = " ```"] # [must_use] # [inline] # [unstable (feature = "pointer_is_aligned_to" , issue = "96284")] pub fn is_aligned_to (self , align : usize) -> bool { if ! align . is_power_of_two () { panic ! ("is_aligned_to: align is not a power-of-two") ; } self . addr () & (align - 1) == 0 } }}}
mkitem!{mkimpl!{impl < T > * mut T { # [doc = " Casts from a type to its maybe-uninitialized version."] # [doc = ""] # [doc = " This is always safe, since UB can only occur if the pointer is read"] # [doc = " before being initialized."] # [must_use] # [inline (always)] # [unstable (feature = "cast_maybe_uninit" , issue = "145036")] pub const fn cast_uninit (self) -> * mut MaybeUninit < T > { self as _ } }}}
mkitem!{mkimpl!{impl < T > * mut MaybeUninit < T > { # [doc = " Casts from a maybe-uninitialized type to its initialized version."] # [doc = ""] # [doc = " This is always safe, since UB can only occur if the pointer is read"] # [doc = " before being initialized."] # [must_use] # [inline (always)] # [unstable (feature = "cast_maybe_uninit" , issue = "145036")] pub const fn cast_init (self) -> * mut T { self as _ } }}}
mkitem!{mkimpl!{impl < T > * mut [T] { # [doc = " Returns the length of a raw slice."] # [doc = ""] # [doc = " The returned value is the number of **elements**, not the number of bytes."] # [doc = ""] # [doc = " This function is safe, even when the raw slice cannot be cast to a slice"] # [doc = " reference because the pointer is null or unaligned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::ptr;"] # [doc = ""] # [doc = " let slice: *mut [i8] = ptr::slice_from_raw_parts_mut(ptr::null_mut(), 3);"] # [doc = " assert_eq!(slice.len(), 3);"] # [doc = " ```"] # [inline (always)] # [stable (feature = "slice_ptr_len" , since = "1.79.0")] # [rustc_const_stable (feature = "const_slice_ptr_len" , since = "1.79.0")] pub const fn len (self) -> usize { metadata (self) } # [doc = " Returns `true` if the raw slice has a length of 0."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ptr;"] # [doc = ""] # [doc = " let slice: *mut [i8] = ptr::slice_from_raw_parts_mut(ptr::null_mut(), 3);"] # [doc = " assert!(!slice.is_empty());"] # [doc = " ```"] # [inline (always)] # [stable (feature = "slice_ptr_len" , since = "1.79.0")] # [rustc_const_stable (feature = "const_slice_ptr_len" , since = "1.79.0")] pub const fn is_empty (self) -> bool { self . len () == 0 } # [doc = " Gets a raw, mutable pointer to the underlying array."] # [doc = ""] # [doc = " If `N` is not exactly equal to the length of `self`, then this method returns `None`."] # [unstable (feature = "slice_as_array" , issue = "133508")] # [inline] # [must_use] pub const fn as_mut_array < const N : usize > (self) -> Option < * mut [T ; N] > { if self . len () == N { let me = self . as_mut_ptr () as * mut [T ; N] ; Some (me) } else { None } } # [doc = " Divides one mutable raw slice into two at an index."] # [doc = ""] # [doc = " The first will contain all indices from `[0, mid)` (excluding"] # [doc = " the index `mid` itself) and the second will contain all"] # [doc = " indices from `[mid, len)` (excluding the index `len` itself)."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `mid > len`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `mid` must be [in-bounds] of the underlying [allocation]."] # [doc = " Which means `self` must be dereferenceable and span a single allocation"] # [doc = " that is at least `mid * size_of::<T>()` bytes long. Not upholding these"] # [doc = " requirements is *[undefined behavior]* even if the resulting pointers are not used."] # [doc = ""] # [doc = " Since `len` being in-bounds it is not a safety invariant of `*mut [T]` the"] # [doc = " safety requirements of this method are the same as for [`split_at_mut_unchecked`]."] # [doc = " The explicit bounds check is only as useful as `len` is correct."] # [doc = ""] # [doc = " [`split_at_mut_unchecked`]: #method.split_at_mut_unchecked"] # [doc = " [in-bounds]: #method.add"] # [doc = " [allocation]: crate::ptr#allocation"] # [doc = " [undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(raw_slice_split)]"] # [doc = " #![feature(slice_ptr_get)]"] # [doc = ""] # [doc = " let mut v = [1, 0, 3, 0, 5, 6];"] # [doc = " let ptr = &mut v as *mut [_];"] # [doc = " unsafe {"] # [doc = "     let (left, right) = ptr.split_at_mut(2);"] # [doc = "     assert_eq!(&*left, [1, 0]);"] # [doc = "     assert_eq!(&*right, [3, 0, 5, 6]);"] # [doc = " }"] # [doc = " ```"] # [inline (always)] # [track_caller] # [unstable (feature = "raw_slice_split" , issue = "95595")] pub unsafe fn split_at_mut (self , mid : usize) -> (* mut [T] , * mut [T]) { assert ! (mid <= self . len ()) ; unsafe { self . split_at_mut_unchecked (mid) } } # [doc = " Divides one mutable raw slice into two at an index, without doing bounds checking."] # [doc = ""] # [doc = " The first will contain all indices from `[0, mid)` (excluding"] # [doc = " the index `mid` itself) and the second will contain all"] # [doc = " indices from `[mid, len)` (excluding the index `len` itself)."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `mid` must be [in-bounds] of the underlying [allocation]."] # [doc = " Which means `self` must be dereferenceable and span a single allocation"] # [doc = " that is at least `mid * size_of::<T>()` bytes long. Not upholding these"] # [doc = " requirements is *[undefined behavior]* even if the resulting pointers are not used."] # [doc = ""] # [doc = " [in-bounds]: #method.add"] # [doc = " [out-of-bounds index]: #method.add"] # [doc = " [allocation]: crate::ptr#allocation"] # [doc = " [undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(raw_slice_split)]"] # [doc = ""] # [doc = " let mut v = [1, 0, 3, 0, 5, 6];"] # [doc = " // scoped to restrict the lifetime of the borrows"] # [doc = " unsafe {"] # [doc = "     let ptr = &mut v as *mut [_];"] # [doc = "     let (left, right) = ptr.split_at_mut_unchecked(2);"] # [doc = "     assert_eq!(&*left, [1, 0]);"] # [doc = "     assert_eq!(&*right, [3, 0, 5, 6]);"] # [doc = "     (&mut *left)[1] = 2;"] # [doc = "     (&mut *right)[1] = 4;"] # [doc = " }"] # [doc = " assert_eq!(v, [1, 2, 3, 4, 5, 6]);"] # [doc = " ```"] # [inline (always)] # [unstable (feature = "raw_slice_split" , issue = "95595")] pub unsafe fn split_at_mut_unchecked (self , mid : usize) -> (* mut [T] , * mut [T]) { let len = self . len () ; let ptr = self . as_mut_ptr () ; let tail = unsafe { ptr . add (mid) } ; (crate :: ptr :: slice_from_raw_parts_mut (ptr , mid) , crate :: ptr :: slice_from_raw_parts_mut (tail , len - mid) ,) } # [doc = " Returns a raw pointer to the slice's buffer."] # [doc = ""] # [doc = " This is equivalent to casting `self` to `*mut T`, but more type-safe."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " #![feature(slice_ptr_get)]"] # [doc = " use std::ptr;"] # [doc = ""] # [doc = " let slice: *mut [i8] = ptr::slice_from_raw_parts_mut(ptr::null_mut(), 3);"] # [doc = " assert_eq!(slice.as_mut_ptr(), ptr::null_mut());"] # [doc = " ```"] # [inline (always)] # [unstable (feature = "slice_ptr_get" , issue = "74265")] pub const fn as_mut_ptr (self) -> * mut T { self as * mut T } # [doc = " Returns a raw pointer to an element or subslice, without doing bounds"] # [doc = " checking."] # [doc = ""] # [doc = " Calling this method with an [out-of-bounds index] or when `self` is not dereferenceable"] # [doc = " is *[undefined behavior]* even if the resulting pointer is not used."] # [doc = ""] # [doc = " [out-of-bounds index]: #method.add"] # [doc = " [undefined behavior]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(slice_ptr_get)]"] # [doc = ""] # [doc = " let x = &mut [1, 2, 4] as *mut [i32];"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     assert_eq!(x.get_unchecked_mut(1), x.as_mut_ptr().add(1));"] # [doc = " }"] # [doc = " ```"] # [unstable (feature = "slice_ptr_get" , issue = "74265")] # [rustc_const_unstable (feature = "const_index" , issue = "143775")] # [inline (always)] pub const unsafe fn get_unchecked_mut < I > (self , index : I) -> * mut I :: Output where I : [const] SliceIndex < [T] >, { unsafe { index . get_unchecked_mut (self) } } # [doc = include_str ! ("docs/as_uninit_slice.md")] # [doc = ""] # [doc = " # See Also"] # [doc = " For the mutable counterpart see [`as_uninit_slice_mut`](pointer::as_uninit_slice_mut)."] # [inline] # [unstable (feature = "ptr_as_uninit" , issue = "75402")] pub const unsafe fn as_uninit_slice < 'a > (self) -> Option < & 'a [MaybeUninit < T >] > { if self . is_null () { None } else { Some (unsafe { slice :: from_raw_parts (self as * const MaybeUninit < T > , self . len ()) }) } } # [doc = " Returns `None` if the pointer is null, or else returns a unique slice to"] # [doc = " the value wrapped in `Some`. In contrast to [`as_mut`], this does not require"] # [doc = " that the value has to be initialized."] # [doc = ""] # [doc = " For the shared counterpart see [`as_uninit_slice`]."] # [doc = ""] # [doc = " [`as_mut`]: #method.as_mut"] # [doc = " [`as_uninit_slice`]: #method.as_uninit_slice-1"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " When calling this method, you have to ensure that *either* the pointer is null *or*"] # [doc = " all of the following is true:"] # [doc = ""] # [doc = " * The pointer must be [valid] for reads and writes for `ptr.len() * size_of::<T>()`"] # [doc = "   many bytes, and it must be properly aligned. This means in particular:"] # [doc = ""] # [doc = "     * The entire memory range of this slice must be contained within a single [allocation]!"] # [doc = "       Slices can never span across multiple allocations."] # [doc = ""] # [doc = "     * The pointer must be aligned even for zero-length slices. One"] # [doc = "       reason for this is that enum layout optimizations may rely on references"] # [doc = "       (including slices of any length) being aligned and non-null to distinguish"] # [doc = "       them from other data. You can obtain a pointer that is usable as `data`"] # [doc = "       for zero-length slices using [`NonNull::dangling()`]."] # [doc = ""] # [doc = " * The total size `ptr.len() * size_of::<T>()` of the slice must be no larger than `isize::MAX`."] # [doc = "   See the safety documentation of [`pointer::offset`]."] # [doc = ""] # [doc = " * You must enforce Rust's aliasing rules, since the returned lifetime `'a` is"] # [doc = "   arbitrarily chosen and does not necessarily reflect the actual lifetime of the data."] # [doc = "   In particular, while this reference exists, the memory the pointer points to must"] # [doc = "   not get accessed (read or written) through any other pointer."] # [doc = ""] # [doc = " This applies even if the result of this method is unused!"] # [doc = ""] # [doc = " See also [`slice::from_raw_parts_mut`][]."] # [doc = ""] # [doc = " [valid]: crate::ptr#safety"] # [doc = " [allocation]: crate::ptr#allocation"] # [doc = ""] # [doc = " # Panics during const evaluation"] # [doc = ""] # [doc = " This method will panic during const evaluation if the pointer cannot be"] # [doc = " determined to be null or not. See [`is_null`] for more information."] # [doc = ""] # [doc = " [`is_null`]: #method.is_null-1"] # [inline] # [unstable (feature = "ptr_as_uninit" , issue = "75402")] pub const unsafe fn as_uninit_slice_mut < 'a > (self) -> Option < & 'a mut [MaybeUninit < T >] > { if self . is_null () { None } else { Some (unsafe { slice :: from_raw_parts_mut (self as * mut MaybeUninit < T > , self . len ()) }) } } }}}
mkitem!{mkimpl!{impl < T > * mut T { # [doc = " Casts from a pointer-to-`T` to a pointer-to-`[T; N]`."] # [inline] # [unstable (feature = "ptr_cast_array" , issue = "144514")] pub const fn cast_array < const N : usize > (self) -> * mut [T ; N] { self . cast () } }}}
mkitem!{mkimpl!{impl < T , const N : usize > * mut [T ; N] { # [doc = " Returns a raw pointer to the array's buffer."] # [doc = ""] # [doc = " This is equivalent to casting `self` to `*mut T`, but more type-safe."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " #![feature(array_ptr_get)]"] # [doc = " use std::ptr;"] # [doc = ""] # [doc = " let arr: *mut [i8; 3] = ptr::null_mut();"] # [doc = " assert_eq!(arr.as_mut_ptr(), ptr::null_mut());"] # [doc = " ```"] # [inline] # [unstable (feature = "array_ptr_get" , issue = "119834")] pub const fn as_mut_ptr (self) -> * mut T { self as * mut T } # [doc = " Returns a raw pointer to a mutable slice containing the entire array."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(array_ptr_get)]"] # [doc = ""] # [doc = " let mut arr = [1, 2, 5];"] # [doc = " let ptr: *mut [i32; 3] = &mut arr;"] # [doc = " unsafe {"] # [doc = "     (&mut *ptr.as_mut_slice())[..2].copy_from_slice(&[3, 4]);"] # [doc = " }"] # [doc = " assert_eq!(arr, [3, 4, 5]);"] # [doc = " ```"] # [inline] # [unstable (feature = "array_ptr_get" , issue = "119834")] pub const fn as_mut_slice (self) -> * mut [T] { self } }}}
mkitem!{mkimpl!{# [doc = " Pointer equality is by address, as produced by the [`<*mut T>::addr`](pointer::addr) method."] # [stable (feature = "rust1" , since = "1.0.0")] impl < T : PointeeSized > PartialEq for * mut T { # [inline (always)] # [allow (ambiguous_wide_pointer_comparisons)] fn eq (& self , other : & * mut T) -> bool { * self == * other } }}}
mkitem!{mkimpl!{# [doc = " Pointer equality is an equivalence relation."] # [stable (feature = "rust1" , since = "1.0.0")] impl < T : PointeeSized > Eq for * mut T { }}}
mkitem!{mkimpl!{# [doc = " Pointer comparison is by address, as produced by the [`<*mut T>::addr`](pointer::addr) method."] # [stable (feature = "rust1" , since = "1.0.0")] impl < T : PointeeSized > Ord for * mut T { # [inline] # [allow (ambiguous_wide_pointer_comparisons)] fn cmp (& self , other : & * mut T) -> Ordering { if self < other { Less } else if self == other { Equal } else { Greater } } }}}
mkitem!{mkimpl!{# [doc = " Pointer comparison is by address, as produced by the [`<*mut T>::addr`](pointer::addr) method."] # [stable (feature = "rust1" , since = "1.0.0")] impl < T : PointeeSized > PartialOrd for * mut T { # [inline (always)] # [allow (ambiguous_wide_pointer_comparisons)] fn partial_cmp (& self , other : & * mut T) -> Option < Ordering > { Some (self . cmp (other)) } # [inline (always)] # [allow (ambiguous_wide_pointer_comparisons)] fn lt (& self , other : & * mut T) -> bool { * self < * other } # [inline (always)] # [allow (ambiguous_wide_pointer_comparisons)] fn le (& self , other : & * mut T) -> bool { * self <= * other } # [inline (always)] # [allow (ambiguous_wide_pointer_comparisons)] fn gt (& self , other : & * mut T) -> bool { * self > * other } # [inline (always)] # [allow (ambiguous_wide_pointer_comparisons)] fn ge (& self , other : & * mut T) -> bool { * self >= * other } }}}
mkitem!{mkimpl!{# [stable (feature = "raw_ptr_default" , since = "1.88.0")] impl < T : ? Sized + Thin > Default for * mut T { # [doc = " Returns the default value of [`null_mut()`][crate::ptr::null_mut]."] fn default () -> Self { crate :: ptr :: null_mut () } }}}