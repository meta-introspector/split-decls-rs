use split_decls_genesis :: ourprelude :: * ; use crate :: async_iter :: AsyncIterator ; use crate :: cell :: UnsafeCell ; use crate :: fmt ; use crate :: future :: Future ; use crate :: ops :: { Deref , DerefMut } ; use crate :: pin :: Pin ; use crate :: ptr :: { NonNull , Unique } ; use crate :: task :: { Context , Poll } ; #[doc = " A marker trait which represents \"panic safe\" types in Rust."] #[doc = ""] #[doc = " This trait is implemented by default for many types and behaves similarly in"] #[doc = " terms of inference of implementation to the [`Send`] and [`Sync`] traits. The"] #[doc = " purpose of this trait is to encode what types are safe to cross a [`catch_unwind`]"] #[doc = " boundary with no fear of unwind safety."] #[doc = ""] #[doc = " [`catch_unwind`]: ../../std/panic/fn.catch_unwind.html"] #[doc = ""] #[doc = " ## What is unwind safety?"] #[doc = ""] #[doc = " In Rust a function can \"return\" early if it either panics or calls a"] #[doc = " function which transitively panics. This sort of control flow is not always"] #[doc = " anticipated, and has the possibility of causing subtle bugs through a"] #[doc = " combination of two critical components:"] #[doc = ""] #[doc = " 1. A data structure is in a temporarily invalid state when the thread"] #[doc = "    panics."] #[doc = " 2. This broken invariant is then later observed."] #[doc = ""] #[doc = " Typically in Rust, it is difficult to perform step (2) because catching a"] #[doc = " panic involves either spawning a thread (which in turn makes it difficult"] #[doc = " to later witness broken invariants) or using the `catch_unwind` function in this"] #[doc = " module. Additionally, even if an invariant is witnessed, it typically isn't a"] #[doc = " problem in Rust because there are no uninitialized values (like in C or C++)."] #[doc = ""] #[doc = " It is possible, however, for **logical** invariants to be broken in Rust,"] #[doc = " which can end up causing behavioral bugs. Another key aspect of unwind safety"] #[doc = " in Rust is that, in the absence of `unsafe` code, a panic cannot lead to"] #[doc = " memory unsafety."] #[doc = ""] #[doc = " That was a bit of a whirlwind tour of unwind safety, but for more information"] #[doc = " about unwind safety and how it applies to Rust, see an [associated RFC][rfc]."] #[doc = ""] #[doc = " [rfc]: https://github.com/rust-lang/rfcs/blob/master/text/1236-stabilize-catch-panic.md"] #[doc = ""] #[doc = " ## What is `UnwindSafe`?"] #[doc = ""] #[doc = " Now that we've got an idea of what unwind safety is in Rust, it's also"] #[doc = " important to understand what this trait represents. As mentioned above, one"] #[doc = " way to witness broken invariants is through the `catch_unwind` function in this"] #[doc = " module as it allows catching a panic and then re-using the environment of"] #[doc = " the closure."] #[doc = ""] #[doc = " Simply put, a type `T` implements `UnwindSafe` if it cannot easily allow"] #[doc = " witnessing a broken invariant through the use of `catch_unwind` (catching a"] #[doc = " panic). This trait is an auto trait, so it is automatically implemented for"] #[doc = " many types, and it is also structurally composed (e.g., a struct is unwind"] #[doc = " safe if all of its components are unwind safe)."] #[doc = ""] #[doc = " Note, however, that this is not an unsafe trait, so there is not a succinct"] #[doc = " contract that this trait is providing. Instead it is intended as more of a"] #[doc = " \"speed bump\" to alert users of `catch_unwind` that broken invariants may be"] #[doc = " witnessed and may need to be accounted for."] #[doc = ""] #[doc = " ## Who implements `UnwindSafe`?"] #[doc = ""] #[doc = " Types such as `&mut T` and `&RefCell<T>` are examples which are **not**"] #[doc = " unwind safe. The general idea is that any mutable state which can be shared"] #[doc = " across `catch_unwind` is not unwind safe by default. This is because it is very"] #[doc = " easy to witness a broken invariant outside of `catch_unwind` as the data is"] #[doc = " simply accessed as usual."] #[doc = ""] #[doc = " Types like `&Mutex<T>`, however, are unwind safe because they implement"] #[doc = " poisoning by default. They still allow witnessing a broken invariant, but"] #[doc = " they already provide their own \"speed bumps\" to do so."] #[doc = ""] #[doc = " ## When should `UnwindSafe` be used?"] #[doc = ""] #[doc = " It is not intended that most types or functions need to worry about this trait."] #[doc = " It is only used as a bound on the `catch_unwind` function and as mentioned"] #[doc = " above, the lack of `unsafe` means it is mostly an advisory. The"] #[doc = " [`AssertUnwindSafe`] wrapper struct can be used to force this trait to be"] #[doc = " implemented for any closed over variables passed to `catch_unwind`."] #[stable (feature = "catch_unwind" , since = "1.9.0")] #[rustc_diagnostic_item = "unwind_safe_trait"] #[diagnostic :: on_unimplemented (message = "the type `{Self}` may not be safely transferred across an unwind boundary" , label = "`{Self}` may not be safely transferred across an unwind boundary")] pub auto trait UnwindSafe { } #[doc = " A marker trait representing types where a shared reference is considered"] #[doc = " unwind safe."] #[doc = ""] #[doc = " This trait is namely not implemented by [`UnsafeCell`], the root of all"] #[doc = " interior mutability."] #[doc = ""] #[doc = " This is a \"helper marker trait\" used to provide impl blocks for the"] #[doc = " [`UnwindSafe`] trait, for more information see that documentation."] #[stable (feature = "catch_unwind" , since = "1.9.0")] #[rustc_diagnostic_item = "ref_unwind_safe_trait"] #[diagnostic :: on_unimplemented (message = "the type `{Self}` may contain interior mutability and a reference may not be safely \
               transferrable across a catch_unwind boundary" , label = "`{Self}` may contain interior mutability and a reference may not be safely \
             transferrable across a catch_unwind boundary")] pub auto trait RefUnwindSafe { } #[doc = " A simple wrapper around a type to assert that it is unwind safe."] #[doc = ""] #[doc = " When using [`catch_unwind`] it may be the case that some of the closed over"] #[doc = " variables are not unwind safe. For example if `&mut T` is captured the"] #[doc = " compiler will generate a warning indicating that it is not unwind safe. It"] #[doc = " might not be the case, however, that this is actually a problem due to the"] #[doc = " specific usage of [`catch_unwind`] if unwind safety is specifically taken into"] #[doc = " account. This wrapper struct is useful for a quick and lightweight"] #[doc = " annotation that a variable is indeed unwind safe."] #[doc = ""] #[doc = " [`catch_unwind`]: ../../std/panic/fn.catch_unwind.html"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " One way to use `AssertUnwindSafe` is to assert that the entire closure"] #[doc = " itself is unwind safe, bypassing all checks for all variables:"] #[doc = ""] #[doc = " ```"] #[doc = " use std::panic::{self, AssertUnwindSafe};"] #[doc = ""] #[doc = " let mut variable = 4;"] #[doc = ""] #[doc = " // This code will not compile because the closure captures `&mut variable`"] #[doc = " // which is not considered unwind safe by default."] #[doc = ""] #[doc = " // panic::catch_unwind(|| {"] #[doc = " //     variable += 3;"] #[doc = " // });"] #[doc = ""] #[doc = " // This, however, will compile due to the `AssertUnwindSafe` wrapper"] #[doc = " let result = panic::catch_unwind(AssertUnwindSafe(|| {"] #[doc = "     variable += 3;"] #[doc = " }));"] #[doc = " // ..."] #[doc = " ```"] #[doc = ""] #[doc = " Wrapping the entire closure amounts to a blanket assertion that all captured"] #[doc = " variables are unwind safe. This has the downside that if new captures are"] #[doc = " added in the future, they will also be considered unwind safe. Therefore,"] #[doc = " you may prefer to just wrap individual captures, as shown below. This is"] #[doc = " more annotation, but it ensures that if a new capture is added which is not"] #[doc = " unwind safe, you will get a compilation error at that time, which will"] #[doc = " allow you to consider whether that new capture in fact represent a bug or"] #[doc = " not."] #[doc = ""] #[doc = " ```"] #[doc = " use std::panic::{self, AssertUnwindSafe};"] #[doc = ""] #[doc = " let mut variable = 4;"] #[doc = " let other_capture = 3;"] #[doc = ""] #[doc = " let result = {"] #[doc = "     let mut wrapper = AssertUnwindSafe(&mut variable);"] #[doc = "     panic::catch_unwind(move || {"] #[doc = "         **wrapper += other_capture;"] #[doc = "     })"] #[doc = " };"] #[doc = " // ..."] #[doc = " ```"] #[stable (feature = "catch_unwind" , since = "1.9.0")] pub struct AssertUnwindSafe < T > (#[stable (feature = "catch_unwind" , since = "1.9.0")] pub T) ; #[stable (feature = "catch_unwind" , since = "1.9.0")] impl < T : ? Sized > ! UnwindSafe for & mut T { } #[stable (feature = "catch_unwind" , since = "1.9.0")] impl < T : RefUnwindSafe + ? Sized > UnwindSafe for & T { } #[stable (feature = "catch_unwind" , since = "1.9.0")] impl < T : RefUnwindSafe + ? Sized > UnwindSafe for * const T { } #[stable (feature = "catch_unwind" , since = "1.9.0")] impl < T : RefUnwindSafe + ? Sized > UnwindSafe for * mut T { } #[unstable (feature = "ptr_internals" , issue = "none")] impl < T : UnwindSafe + ? Sized > UnwindSafe for Unique < T > { } #[stable (feature = "nonnull" , since = "1.25.0")] impl < T : RefUnwindSafe + ? Sized > UnwindSafe for NonNull < T > { } #[stable (feature = "catch_unwind" , since = "1.9.0")] impl < T > UnwindSafe for AssertUnwindSafe < T > { } #[stable (feature = "catch_unwind" , since = "1.9.0")] impl < T : ? Sized > ! RefUnwindSafe for UnsafeCell < T > { } #[stable (feature = "catch_unwind" , since = "1.9.0")] impl < T > RefUnwindSafe for AssertUnwindSafe < T > { } #[cfg (target_has_atomic_load_store = "ptr")] #[stable (feature = "unwind_safe_atomic_refs" , since = "1.14.0")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicIsize { } #[cfg (target_has_atomic_load_store = "8")] #[stable (feature = "integer_atomics_stable" , since = "1.34.0")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicI8 { } #[cfg (target_has_atomic_load_store = "16")] #[stable (feature = "integer_atomics_stable" , since = "1.34.0")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicI16 { } #[cfg (target_has_atomic_load_store = "32")] #[stable (feature = "integer_atomics_stable" , since = "1.34.0")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicI32 { } #[cfg (target_has_atomic_load_store = "64")] #[stable (feature = "integer_atomics_stable" , since = "1.34.0")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicI64 { } #[cfg (target_has_atomic_load_store = "128")] #[unstable (feature = "integer_atomics" , issue = "99069")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicI128 { } #[cfg (target_has_atomic_load_store = "ptr")] #[stable (feature = "unwind_safe_atomic_refs" , since = "1.14.0")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicUsize { } #[cfg (target_has_atomic_load_store = "8")] #[stable (feature = "integer_atomics_stable" , since = "1.34.0")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicU8 { } #[cfg (target_has_atomic_load_store = "16")] #[stable (feature = "integer_atomics_stable" , since = "1.34.0")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicU16 { } #[cfg (target_has_atomic_load_store = "32")] #[stable (feature = "integer_atomics_stable" , since = "1.34.0")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicU32 { } #[cfg (target_has_atomic_load_store = "64")] #[stable (feature = "integer_atomics_stable" , since = "1.34.0")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicU64 { } #[cfg (target_has_atomic_load_store = "128")] #[unstable (feature = "integer_atomics" , issue = "99069")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicU128 { } #[cfg (target_has_atomic_load_store = "8")] #[stable (feature = "unwind_safe_atomic_refs" , since = "1.14.0")] impl RefUnwindSafe for crate :: sync :: atomic :: AtomicBool { } #[cfg (target_has_atomic_load_store = "ptr")] #[stable (feature = "unwind_safe_atomic_refs" , since = "1.14.0")] impl < T > RefUnwindSafe for crate :: sync :: atomic :: AtomicPtr < T > { } #[stable (feature = "catch_unwind" , since = "1.9.0")] #[rustc_const_unstable (feature = "const_convert" , issue = "143773")] impl < T > const Deref for AssertUnwindSafe < T > { type Target = T ; fn deref (& self) -> & T { & self . 0 } } #[stable (feature = "catch_unwind" , since = "1.9.0")] #[rustc_const_unstable (feature = "const_convert" , issue = "143773")] impl < T > const DerefMut for AssertUnwindSafe < T > { fn deref_mut (& mut self) -> & mut T { & mut self . 0 } } #[stable (feature = "catch_unwind" , since = "1.9.0")] impl < R , F : FnOnce () -> R > FnOnce < () > for AssertUnwindSafe < F > { type Output = R ; #[inline] extern "rust-call" fn call_once (self , _args : ()) -> R { (self . 0) () } } #[stable (feature = "std_debug" , since = "1.16.0")] impl < T : fmt :: Debug > fmt :: Debug for AssertUnwindSafe < T > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_tuple ("AssertUnwindSafe") . field (& self . 0) . finish () } } #[stable (feature = "assertunwindsafe_default" , since = "1.62.0")] impl < T : Default > Default for AssertUnwindSafe < T > { fn default () -> Self { Self (Default :: default ()) } } #[stable (feature = "futures_api" , since = "1.36.0")] impl < F : Future > Future for AssertUnwindSafe < F > { type Output = F :: Output ; fn poll (self : Pin < & mut Self > , cx : & mut Context < '_ >) -> Poll < Self :: Output > { let pinned_field = unsafe { Pin :: map_unchecked_mut (self , | x | & mut x . 0) } ; F :: poll (pinned_field , cx) } } #[unstable (feature = "async_iterator" , issue = "79024")] impl < S : AsyncIterator > AsyncIterator for AssertUnwindSafe < S > { type Item = S :: Item ; fn poll_next (self : Pin < & mut Self > , cx : & mut Context < '_ >) -> Poll < Option < S :: Item > > { unsafe { self . map_unchecked_mut (| x | & mut x . 0) } . poll_next (cx) } fn size_hint (& self) -> (usize , Option < usize >) { self . 0 . size_hint () } }