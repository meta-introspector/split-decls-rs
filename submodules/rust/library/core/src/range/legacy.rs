# ! [doc = " # Legacy range types"] # ! [doc = ""] # ! [doc = " The types within this module will be replaced by the types"] # ! [doc = " [`Range`], [`RangeInclusive`], [`RangeToInclusive`], and [`RangeFrom`] in the parent"] # ! [doc = " module, [`core::range`]."] # ! [doc = ""] # ! [doc = " The types here are equivalent to those in [`core::ops`]."] use split_decls_genesis :: ourprelude :: * ; #[doc (inline)] pub use crate :: ops :: { Range , RangeFrom , RangeInclusive , RangeToInclusive } ;