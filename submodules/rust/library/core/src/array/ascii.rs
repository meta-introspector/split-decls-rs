mkuse!{use crate :: ascii ;}
mkitem!{mkimpl!{impl < const N : usize > [u8 ; N] { # [doc = " Converts this array of bytes into an array of ASCII characters,"] # [doc = " or returns `None` if any of the characters is non-ASCII."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(ascii_char)]"] # [doc = ""] # [doc = " const HEX_DIGITS: [std::ascii::Char; 16] ="] # [doc = "     *b\"0123456789abcdef\".as_ascii().unwrap();"] # [doc = ""] # [doc = " assert_eq!(HEX_DIGITS[1].as_str(), \"1\");"] # [doc = " assert_eq!(HEX_DIGITS[10].as_str(), \"a\");"] # [doc = " ```"] # [unstable (feature = "ascii_char" , issue = "110998")] # [must_use] # [inline] pub const fn as_ascii (& self) -> Option < & [ascii :: Char ; N] > { if self . is_ascii () { Some (unsafe { self . as_ascii_unchecked () }) } else { None } } # [doc = " Converts this array of bytes into an array of ASCII characters,"] # [doc = " without checking whether they're valid."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Every byte in the array must be in `0..=127`, or else this is UB."] # [unstable (feature = "ascii_char" , issue = "110998")] # [must_use] # [inline] pub const unsafe fn as_ascii_unchecked (& self) -> & [ascii :: Char ; N] { let byte_ptr : * const [u8 ; N] = self ; let ascii_ptr = byte_ptr as * const [ascii :: Char ; N] ; unsafe { & * ascii_ptr } } }}}