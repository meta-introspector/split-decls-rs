mkuse!{use std :: sync :: Mutex ;}
mkuse!{use std :: collections :: HashMap ;}
mkuse!{use std :: sync :: LazyLock ;}
mkitem!{static USE_MATRIX : LazyLock < Mutex < HashMap < String , Vec < String > > > > = LazyLock :: new (| | Mutex :: new (HashMap :: new ())) ;}

macro_rules! get_use_matrix_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function get_use_matrix in module {}", module_path!());
    };
}

mkfn!{
    get_use_matrix_introspect!();
    pub fn get_use_matrix () -> HashMap < String , Vec < String > > { USE_MATRIX . lock () . unwrap () . clone () }
}
mkitem!{macro_rules ! emit_message { ($ ($ arg : tt) *) => { } ; }}
mkitem!{macro_rules ! mkfn { ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_trait_bounds_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_trait_bounds_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: trait_bounds_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: trait_bounds_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn catch_fatal_errors < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn catch_fatal_errors < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: catch_fatal_errors_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: catch_fatal_errors_specific") ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn parse_crate_attrs < $ lifetime : lifetime > ($ ($ param : tt) *) -> PResult < $ lifetime2 : lifetime , ast :: AttrVec > $ body : block) => { $ (# [$ attr]) * fn parse_crate_attrs < $ lifetime > ($ ($ param) *) -> PResult < $ lifetime2 , ast :: AttrVec > { $ introspect ; emit_message ! ("🚀 MARKER: parse_crate_attrs_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: parse_crate_attrs_specific") ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn init_logger_with_additional_layer < F , T > ($ ($ param : tt) *) where F : FnOnce () -> T , T : rustc_log :: BuildSubscriberRet , $ body : block) => { $ (# [$ attr]) * fn init_logger_with_additional_layer < F , T > ($ ($ param) *) where F : FnOnce () -> T , T : rustc_log :: BuildSubscriberRet , { $ introspect ; emit_message ! ("🚀 MARKER: init_logger_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: init_logger_specific") ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < F , T > ($ ($ param : tt) *) $ (-> $ ret : ty) ? where F : FnOnce ($ ($ fnonce_args : tt) *) $ ($ where_rest : tt) * $ body : block) => { $ (# [$ attr]) * fn $ name < F , T > ($ ($ param) *) $ (-> $ ret) ? where F : FnOnce ($ ($ fnonce_args) *) $ ($ where_rest) * { $ introspect ; emit_message ! ("🚀 MARKER: two_generics_where_fnonce - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics_where_fnonce - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? where $ ($ where_clause : tt) * $ body : block) => { $ (# [$ attr]) * fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? where $ ($ where_clause) * { $ introspect ; emit_message ! ("🚀 MARKER: two_generics_where - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics_where - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: two_generics - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_two_generics - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_two_generics - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub ($ vis : ident) fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub ($ vis) fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_vis - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_vis - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < $ gen : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name < $ gen > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: generic_single - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: generic_single - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident < $ gen : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name < $ gen > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_generic_single - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_generic_single - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident < $ lifetime : lifetime > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name < $ lifetime > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: lifetime - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: lifetime - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident < $ lifetime : lifetime > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name < $ lifetime > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_lifetime - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_lifetime - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * pub fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * pub fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_non_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_non_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (# [$ attr : meta]) * fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (# [$ attr]) * fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: non_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: non_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; fn $ name : ident () $ body : block) => { fn $ name () { $ introspect ; emit_message ! ("🚀 MARKER: simple - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: simple - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ ($ anything : tt) *) => { $ ($ anything) * } ; }}
mkitem!{macro_rules ! safe_println { ($ ($ arg : tt) *) => { () } ; }}
mkitem!{macro_rules ! safe_print { ($ ($ arg : tt) *) => { () } ; }}
mkitem!{# [macro_export] macro_rules ! include_rust_compiler { ($ crate_name : literal , $ subpath : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_" , $ subpath , "_" , $ file , ".rs")) ; } ; ($ crate_name : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_src_" , $ file , ".rs")) ; } ; ($ crate_name : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_src_lib.rs")) ; } ; }}
mkitem!{# [macro_export] macro_rules ! include_rust_library { ($ lib_name : literal , $ subpath : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_" , $ subpath , "_" , $ file , ".rs")) ; } ; ($ lib_name : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_src_" , $ file , ".rs")) ; } ; ($ lib_name : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_src_lib.rs")) ; } ; }}
mkitem!{# [macro_export] macro_rules ! include_processed { ($ path : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_" , $ path , ".rs")) ; } ; }}
mkitem!{macro_rules ! mkinclude { ($ path : ident) => { } ; ($ path : literal) => { include ! ($ path) } ; }}
mkitem!{macro_rules ! mkitem { (include ! ($ path : ident) ;) => { } ; ($ macro_name : ident :: $ macro_sub : ident ! { $ string_lit : literal }) => { $ macro_name :: $ macro_sub ! { $ string_lit } } ; ($ macro_name : ident :: $ macro_sub : ident ! { $ ($ args : tt) * }) => { $ macro_name :: $ macro_sub ! { $ ($ args) * } } ; ($ macro_name : ident ! { $ ($ args : tt) * }) => { $ macro_name ! { $ ($ args) * } } ; ($ item : item) => { $ item } ; }}
mkitem!{# [macro_export] macro_rules ! mkmod { ($ name : ident , { $ ($ content : tt) * }) => { compile_error ! (concat ! ("MOD|" , module_path ! () , "|" , stringify ! ($ name))) ; mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; (pub mod $ name : ident { $ ($ content : tt) * }) => { compile_error ! (concat ! ("MOD|" , module_path ! () , "|" , stringify ! ($ name))) ; pub mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; (mod $ name : ident { $ ($ content : tt) * }) => { compile_error ! (concat ! ("MOD|" , module_path ! () , "|" , stringify ! ($ name))) ; mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; }}
mkitem!{# [macro_export] macro_rules ! mkuse { ($ use_stmt : item) => { compile_error ! (concat ! ("USE|" , module_path ! () , "|" , stringify ! ($ use_stmt))) ; } ; }}
mkitem!{macro_rules ! mkstruct { ($ struct_def : item) => { $ struct_def } ; }}
mkitem!{macro_rules ! mkenum { ($ enum_def : item) => { $ enum_def } ; }}
mkitem!{macro_rules ! mktrait { ($ trait_def : item) => { $ trait_def } ; }}
mkitem!{macro_rules ! mkimpl { ($ impl_def : item) => { $ impl_def } ; }}
mkitem!{macro_rules ! getname { ($ name : ident) => { stringify ! ($ name) } ; }}
mkitem!{macro_rules ! getsrc { ($ name : ident) => { "processed file" } ; }}
mkitem!{macro_rules ! getpath { ($ name : ident) => { "processed_path" } ; }}
mkitem!{macro_rules ! get_deps { ($ name : ident) => { vec ! [] } ; }}
mkmod!{rustc_complete, { 
                getname!(rustc_complete);
                getsrc!(rustc_complete);
                getpath!(rustc_complete);
                get_deps!(rustc_complete);
                get_crates!(rustc_complete);
                mkinclude!(rustc_complete);
                mkmod!{emitter, { 
                getname!(emitter);
                getsrc!(emitter);
                getpath!(emitter);
                get_deps!(emitter);
                get_crates!(emitter);
                mkinclude!(emitter);
                
macro_rules! stderr_destination_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function stderr_destination in module {}", module_path!());
    };
}

mkfn!{
    stderr_destination_introspect!();
    pub fn stderr_destination () { }
} 
            }}
mkmod!{registry, { 
                getname!(registry);
                getsrc!(registry);
                getpath!(registry);
                get_deps!(registry);
                get_crates!(registry);
                mkinclude!(registry);
                mkitem!{mkstruct!{pub struct Registry ;}} 
            }}
mkmod!{translation, { 
                getname!(translation);
                getsrc!(translation);
                getpath!(translation);
                get_deps!(translation);
                get_crates!(translation);
                mkinclude!(translation);
                mkitem!{mkstruct!{pub struct Translator ;}} 
            }}
mkitem!{mkstruct!{pub struct ColorConfig ;}}
mkitem!{mkstruct!{pub struct DiagCtxt ;}}
mkitem!{mkstruct!{pub struct ErrCode ;}}
mkitem!{mkstruct!{pub struct FatalError ;}}
mkitem!{mkstruct!{pub struct PResult < T > (pub T) ;}}
mkmod!{markdown, { 
                getname!(markdown);
                getsrc!(markdown);
                getpath!(markdown);
                get_deps!(markdown);
                get_crates!(markdown);
                mkinclude!(markdown);
                 
            }}
mkmod!{config, { 
                getname!(config);
                getsrc!(config);
                getpath!(config);
                get_deps!(config);
                get_crates!(config);
                mkinclude!(config);
                mkitem!{mkstruct!{pub struct CG_OPTIONS ;}}
mkitem!{mkstruct!{pub struct CrateType ;}}
mkitem!{mkstruct!{pub struct ErrorOutputType ;}}
mkitem!{mkstruct!{pub struct Input ;}}
mkitem!{mkstruct!{pub struct OptionDesc ;}}
mkitem!{mkstruct!{pub struct OutFileName ;}}
mkitem!{mkstruct!{pub struct OutputType ;}}
mkitem!{mkstruct!{pub struct Sysroot ;}}
mkitem!{mkstruct!{pub struct UnstableOptions ;}}
mkitem!{mkstruct!{pub struct Z_OPTIONS ;}}

macro_rules! nightly_options_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function nightly_options in module {}", module_path!());
    };
}

mkfn!{
    nightly_options_introspect!();
    pub fn nightly_options () { }
}

macro_rules! parse_target_triple_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function parse_target_triple in module {}", module_path!());
    };
}

mkfn!{
    parse_target_triple_introspect!();
    pub fn parse_target_triple () { }
} 
            }}
mkmod!{getopts, { 
                getname!(getopts);
                getsrc!(getopts);
                getpath!(getopts);
                get_deps!(getopts);
                get_crates!(getopts);
                mkinclude!(getopts);
                mkitem!{mkstruct!{pub struct Matches ;}} 
            }}
mkmod!{lint, { 
                getname!(lint);
                getsrc!(lint);
                getpath!(lint);
                get_deps!(lint);
                get_crates!(lint);
                mkinclude!(lint);
                mkitem!{mkstruct!{pub struct Lint ;}}
mkitem!{mkstruct!{pub struct LintId ;}} 
            }}
mkmod!{output, { 
                getname!(output);
                getsrc!(output);
                getpath!(output);
                get_deps!(output);
                get_crates!(output);
                mkinclude!(output);
                mkitem!{mkstruct!{pub struct CRATE_TYPES ;}}

macro_rules! collect_crate_types_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function collect_crate_types in module {}", module_path!());
    };
}

mkfn!{
    collect_crate_types_introspect!();
    pub fn collect_crate_types () { }
}

macro_rules! invalid_output_for_target_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function invalid_output_for_target in module {}", module_path!());
    };
}

mkfn!{
    invalid_output_for_target_introspect!();
    pub fn invalid_output_for_target () { }
} 
            }}
mkitem!{mkstruct!{pub struct EarlyDiagCtxt ;}}
mkitem!{mkstruct!{pub struct Session ;}}
mkitem!{mkstruct!{pub struct FileName ;}}
mkmod!{def_id, { 
                getname!(def_id);
                getsrc!(def_id);
                getpath!(def_id);
                get_deps!(def_id);
                get_crates!(def_id);
                mkinclude!(def_id);
                mkitem!{mkstruct!{pub struct LOCAL_CRATE ;}} 
            }}
mkmod!{ty, { 
                getname!(ty);
                getsrc!(ty);
                getpath!(ty);
                get_deps!(ty);
                get_crates!(ty);
                mkinclude!(ty);
                mkitem!{mkstruct!{pub struct TyCtxt < T > (pub T) ;}} 
            }} 
            }}
mkmod!{session_diagnostics, { 
                getname!(session_diagnostics);
                getsrc!(session_diagnostics);
                getpath!(session_diagnostics);
                get_deps!(session_diagnostics);
                get_crates!(session_diagnostics);
                mkinclude!(session_diagnostics);
                mkitem!{mkstruct!{pub struct CantEmitMIR ;}}
mkitem!{mkstruct!{pub struct RLinkEmptyVersionNumber ;}}
mkitem!{mkstruct!{pub struct RLinkEncodingVersionMismatch ;}}
mkitem!{mkstruct!{pub struct RLinkRustcVersionMismatch ;}}
mkitem!{mkstruct!{pub struct RLinkWrongFileType ;}}
mkitem!{mkstruct!{pub struct RlinkCorruptFile ;}}
mkitem!{mkstruct!{pub struct RlinkNotAFile ;}}
mkitem!{mkstruct!{pub struct RlinkUnableToRead ;}}
mkitem!{mkstruct!{pub struct UnstableFeatureUsage ;}} 
            }}
mkitem!{macro_rules ! do_not_use_print { ($ ($ t : tt) *) => { compile_error ! ("Don't use print") } ; }}
mkitem!{macro_rules ! do_not_use_safe_print { ($ ($ t : tt) *) => { compile_error ! ("Don't use safe_print") } ; }}
mkitem!{macro_rules ! mktrait { ($ trait_def : item) => { $ trait_def } ; }}
mkitem!{macro_rules ! mkimpl { ($ impl_def : item) => { $ impl_def } ; }}
mkitem!{macro_rules ! getname { ($ name : ident) => { pub fn get_module_name () -> &'static str { stringify ! ($ name) } } ; }}
mkitem!{macro_rules ! getsrc { ($ name : ident) => { pub fn get_source_info () -> &'static str { concat ! ("Module: " , stringify ! ($ name)) } } ; }}
mkitem!{macro_rules ! getpath { ($ name : ident) => { pub fn get_module_path () -> &'static str { module_path ! () } } ; }}
mkitem!{macro_rules ! get_deps { ($ name : ident) => { pub fn get_dependencies () -> &'static [&'static str] { & [] } } ; }}
mkitem!{macro_rules ! get_crates { ($ name : ident) => { pub fn get_required_crates () -> &'static [&'static str] { & [] } } ; }}
mkitem!{macro_rules ! forall_crates { ($ ($ crate_name : ident) ,*) => { $ (extern crate $ crate_name ;) * } ; }}
mkitem!{macro_rules ! emit_extern { ($ crate_name : ident) => { extern crate $ crate_name ; } ; }}
mkitem!{macro_rules ! get_externs { ($ crate_name : ident) => { stringify ! ($ crate_name) } ; }}
mkitem!{macro_rules ! uint_impl { (Self = $ SelfT : ty , ActualT = $ ActualT : ident , SignedT = $ SignedT : ident , BITS = $ BITS : literal , BITS_MINUS_ONE = $ BITS_MINUS_ONE : literal , MAX = $ MaxV : literal , rot = $ rot : literal , rot_op = $ rot_op : literal , rot_result = $ rot_result : literal , fsh_op = $ fsh_op : literal , fshl_result = $ fshl_result : literal , fshr_result = $ fshr_result : literal , swap_op = $ swap_op : literal , swapped = $ swapped : literal , reversed = $ reversed : literal , le_bytes = $ le_bytes : literal , be_bytes = $ be_bytes : literal , to_xe_bytes_doc = $ to_xe_bytes_doc : expr , from_xe_bytes_doc = $ from_xe_bytes_doc : expr , bound_condition = $ bound_condition : literal ,) => { # [doc = " The smallest value that can be represented by this integer type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MIN, 0);")] # [doc = " ```"] # [stable (feature = "assoc_int_consts" , since = "1.43.0")] pub const MIN : Self = 0 ; # [doc = " The largest value that can be represented by this integer type"] # [doc = concat ! ("(2<sup>" , $ BITS , "</sup> &minus; 1" , $ bound_condition , ").")] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX, " , stringify ! ($ MaxV) , ");")] # [doc = " ```"] # [stable (feature = "assoc_int_consts" , since = "1.43.0")] pub const MAX : Self = ! 0 ; # [doc = " The size of this integer type in bits."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::BITS, " , stringify ! ($ BITS) , ");")] # [doc = " ```"] # [stable (feature = "int_bits_const" , since = "1.53.0")] pub const BITS : u32 = Self :: MAX . count_ones () ; # [doc = " Returns the number of ones in the binary representation of `self`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = 0b01001100" , stringify ! ($ SelfT) , ";")] # [doc = " assert_eq!(n.count_ones(), 3);"] # [doc = ""] # [doc = concat ! ("let max = " , stringify ! ($ SelfT) , "::MAX;")] # [doc = concat ! ("assert_eq!(max.count_ones(), " , stringify ! ($ BITS) , ");")] # [doc = ""] # [doc = concat ! ("let zero = 0" , stringify ! ($ SelfT) , ";")] # [doc = " assert_eq!(zero.count_ones(), 0);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_math" , since = "1.32.0")] # [doc (alias = "popcount")] # [doc (alias = "popcnt")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn count_ones (self) -> u32 { return intrinsics :: ctpop (self) ; } # [doc = " Returns the number of zeros in the binary representation of `self`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let zero = 0" , stringify ! ($ SelfT) , ";")] # [doc = concat ! ("assert_eq!(zero.count_zeros(), " , stringify ! ($ BITS) , ");")] # [doc = ""] # [doc = concat ! ("let max = " , stringify ! ($ SelfT) , "::MAX;")] # [doc = " assert_eq!(max.count_zeros(), 0);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn count_zeros (self) -> u32 { (! self) . count_ones () } # [doc = " Returns the number of leading zeros in the binary representation of `self`."] # [doc = ""] # [doc = " Depending on what you're doing with the value, you might also be interested in the"] # [doc = " [`ilog2`] function which returns a consistent number, even if the type widens."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = " , stringify ! ($ SelfT) , "::MAX >> 2;")] # [doc = " assert_eq!(n.leading_zeros(), 2);"] # [doc = ""] # [doc = concat ! ("let zero = 0" , stringify ! ($ SelfT) , ";")] # [doc = concat ! ("assert_eq!(zero.leading_zeros(), " , stringify ! ($ BITS) , ");")] # [doc = ""] # [doc = concat ! ("let max = " , stringify ! ($ SelfT) , "::MAX;")] # [doc = " assert_eq!(max.leading_zeros(), 0);"] # [doc = " ```"] # [doc = concat ! ("[`ilog2`]: " , stringify ! ($ SelfT) , "::ilog2")] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn leading_zeros (self) -> u32 { return intrinsics :: ctlz (self as $ ActualT) ; } # [doc = " Returns the number of trailing zeros in the binary representation"] # [doc = " of `self`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = 0b0101000" , stringify ! ($ SelfT) , ";")] # [doc = " assert_eq!(n.trailing_zeros(), 3);"] # [doc = ""] # [doc = concat ! ("let zero = 0" , stringify ! ($ SelfT) , ";")] # [doc = concat ! ("assert_eq!(zero.trailing_zeros(), " , stringify ! ($ BITS) , ");")] # [doc = ""] # [doc = concat ! ("let max = " , stringify ! ($ SelfT) , "::MAX;")] # [doc = concat ! ("assert_eq!(max.trailing_zeros(), 0);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn trailing_zeros (self) -> u32 { return intrinsics :: cttz (self) ; } # [doc = " Returns the number of leading ones in the binary representation of `self`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = !(" , stringify ! ($ SelfT) , "::MAX >> 2);")] # [doc = " assert_eq!(n.leading_ones(), 2);"] # [doc = ""] # [doc = concat ! ("let zero = 0" , stringify ! ($ SelfT) , ";")] # [doc = " assert_eq!(zero.leading_ones(), 0);"] # [doc = ""] # [doc = concat ! ("let max = " , stringify ! ($ SelfT) , "::MAX;")] # [doc = concat ! ("assert_eq!(max.leading_ones(), " , stringify ! ($ BITS) , ");")] # [doc = " ```"] # [stable (feature = "leading_trailing_ones" , since = "1.46.0")] # [rustc_const_stable (feature = "leading_trailing_ones" , since = "1.46.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn leading_ones (self) -> u32 { (! self) . leading_zeros () } # [doc = " Returns the number of trailing ones in the binary representation"] # [doc = " of `self`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = 0b1010111" , stringify ! ($ SelfT) , ";")] # [doc = " assert_eq!(n.trailing_ones(), 3);"] # [doc = ""] # [doc = concat ! ("let zero = 0" , stringify ! ($ SelfT) , ";")] # [doc = " assert_eq!(zero.trailing_ones(), 0);"] # [doc = ""] # [doc = concat ! ("let max = " , stringify ! ($ SelfT) , "::MAX;")] # [doc = concat ! ("assert_eq!(max.trailing_ones(), " , stringify ! ($ BITS) , ");")] # [doc = " ```"] # [stable (feature = "leading_trailing_ones" , since = "1.46.0")] # [rustc_const_stable (feature = "leading_trailing_ones" , since = "1.46.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn trailing_ones (self) -> u32 { (! self) . trailing_zeros () } # [doc = " Returns the minimum number of bits required to represent `self`."] # [doc = ""] # [doc = " This method returns zero if `self` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(uint_bit_width)]"] # [doc = ""] # [doc = concat ! ("assert_eq!(0_" , stringify ! ($ SelfT) , ".bit_width(), 0);")] # [doc = concat ! ("assert_eq!(0b111_" , stringify ! ($ SelfT) , ".bit_width(), 3);")] # [doc = concat ! ("assert_eq!(0b1110_" , stringify ! ($ SelfT) , ".bit_width(), 4);")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.bit_width(), " , stringify ! ($ BITS) , ");")] # [doc = " ```"] # [unstable (feature = "uint_bit_width" , issue = "142326")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn bit_width (self) -> u32 { Self :: BITS - self . leading_zeros () } # [doc = " Returns `self` with only the most significant bit set, or `0` if"] # [doc = " the input is `0`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(isolate_most_least_significant_one)]"] # [doc = ""] # [doc = concat ! ("let n: " , stringify ! ($ SelfT) , " = 0b_01100100;")] # [doc = ""] # [doc = " assert_eq!(n.isolate_highest_one(), 0b_01000000);"] # [doc = concat ! ("assert_eq!(0_" , stringify ! ($ SelfT) , ".isolate_highest_one(), 0);")] # [doc = " ```"] # [unstable (feature = "isolate_most_least_significant_one" , issue = "136909")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn isolate_highest_one (self) -> Self { self & (((1 as $ SelfT) << (<$ SelfT >:: BITS - 1)) . wrapping_shr (self . leading_zeros ())) } # [doc = " Returns `self` with only the least significant bit set, or `0` if"] # [doc = " the input is `0`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(isolate_most_least_significant_one)]"] # [doc = ""] # [doc = concat ! ("let n: " , stringify ! ($ SelfT) , " = 0b_01100100;")] # [doc = ""] # [doc = " assert_eq!(n.isolate_lowest_one(), 0b_00000100);"] # [doc = concat ! ("assert_eq!(0_" , stringify ! ($ SelfT) , ".isolate_lowest_one(), 0);")] # [doc = " ```"] # [unstable (feature = "isolate_most_least_significant_one" , issue = "136909")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn isolate_lowest_one (self) -> Self { self & self . wrapping_neg () } # [doc = " Returns the index of the highest bit set to one in `self`, or `None`"] # [doc = " if `self` is `0`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(int_lowest_highest_one)]"] # [doc = ""] # [doc = concat ! ("assert_eq!(0x0_" , stringify ! ($ SelfT) , ".highest_one(), None);")] # [doc = concat ! ("assert_eq!(0x1_" , stringify ! ($ SelfT) , ".highest_one(), Some(0));")] # [doc = concat ! ("assert_eq!(0x10_" , stringify ! ($ SelfT) , ".highest_one(), Some(4));")] # [doc = concat ! ("assert_eq!(0x1f_" , stringify ! ($ SelfT) , ".highest_one(), Some(4));")] # [doc = " ```"] # [unstable (feature = "int_lowest_highest_one" , issue = "145203")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn highest_one (self) -> Option < u32 > { match NonZero :: new (self) { Some (v) => Some (v . highest_one ()) , None => None , } } # [doc = " Returns the index of the lowest bit set to one in `self`, or `None`"] # [doc = " if `self` is `0`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(int_lowest_highest_one)]"] # [doc = ""] # [doc = concat ! ("assert_eq!(0x0_" , stringify ! ($ SelfT) , ".lowest_one(), None);")] # [doc = concat ! ("assert_eq!(0x1_" , stringify ! ($ SelfT) , ".lowest_one(), Some(0));")] # [doc = concat ! ("assert_eq!(0x10_" , stringify ! ($ SelfT) , ".lowest_one(), Some(4));")] # [doc = concat ! ("assert_eq!(0x1f_" , stringify ! ($ SelfT) , ".lowest_one(), Some(0));")] # [doc = " ```"] # [unstable (feature = "int_lowest_highest_one" , issue = "145203")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn lowest_one (self) -> Option < u32 > { match NonZero :: new (self) { Some (v) => Some (v . lowest_one ()) , None => None , } } # [doc = " Returns the bit pattern of `self` reinterpreted as a signed integer of the same size."] # [doc = ""] # [doc = " This produces the same result as an `as` cast, but ensures that the bit-width remains"] # [doc = " the same."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = " , stringify ! ($ SelfT) , "::MAX;")] # [doc = ""] # [doc = concat ! ("assert_eq!(n.cast_signed(), -1" , stringify ! ($ SignedT) , ");")] # [doc = " ```"] # [stable (feature = "integer_sign_cast" , since = "1.87.0")] # [rustc_const_stable (feature = "integer_sign_cast" , since = "1.87.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn cast_signed (self) -> $ SignedT { self as $ SignedT } # [doc = " Shifts the bits to the left by a specified amount, `n`,"] # [doc = " wrapping the truncated bits to the end of the resulting integer."] # [doc = ""] # [doc = " Please note this isn't the same operation as the `<<` shifting operator!"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = " , $ rot_op , stringify ! ($ SelfT) , ";")] # [doc = concat ! ("let m = " , $ rot_result , ";")] # [doc = ""] # [doc = concat ! ("assert_eq!(n.rotate_left(" , $ rot , "), m);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn rotate_left (self , n : u32) -> Self { return intrinsics :: rotate_left (self , n) ; } # [doc = " Shifts the bits to the right by a specified amount, `n`,"] # [doc = " wrapping the truncated bits to the beginning of the resulting"] # [doc = " integer."] # [doc = ""] # [doc = " Please note this isn't the same operation as the `>>` shifting operator!"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = " , $ rot_result , stringify ! ($ SelfT) , ";")] # [doc = concat ! ("let m = " , $ rot_op , ";")] # [doc = ""] # [doc = concat ! ("assert_eq!(n.rotate_right(" , $ rot , "), m);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn rotate_right (self , n : u32) -> Self { return intrinsics :: rotate_right (self , n) ; } # [doc = " Performs a left funnel shift (concatenates `self` with `rhs`, with `self`"] # [doc = " making up the most significant half, then shifts the combined value left"] # [doc = " by `n`, and most significant half is extracted to produce the result)."] # [doc = ""] # [doc = " Please note this isn't the same operation as the `<<` shifting operator or"] # [doc = " [`rotate_left`](Self::rotate_left), although `a.funnel_shl(a, n)` is *equivalent*"] # [doc = " to `a.rotate_left(n)`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If `n` is greater than or equal to the number of bits in `self`"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(funnel_shifts)]"] # [doc = concat ! ("let a = " , $ rot_op , stringify ! ($ SelfT) , ";")] # [doc = concat ! ("let b = " , $ fsh_op , stringify ! ($ SelfT) , ";")] # [doc = concat ! ("let m = " , $ fshl_result , ";")] # [doc = ""] # [doc = concat ! ("assert_eq!(a.funnel_shl(b, " , $ rot , "), m);")] # [doc = " ```"] # [rustc_const_unstable (feature = "funnel_shifts" , issue = "145686")] # [unstable (feature = "funnel_shifts" , issue = "145686")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn funnel_shl (self , rhs : Self , n : u32) -> Self { assert ! (n < Self :: BITS , "attempt to funnel shift left with overflow") ; unsafe { intrinsics :: unchecked_funnel_shl (self , rhs , n) } } # [doc = " Performs a right funnel shift (concatenates `self` and `rhs`, with `self`"] # [doc = " making up the most significant half, then shifts the combined value right"] # [doc = " by `n`, and least significant half is extracted to produce the result)."] # [doc = ""] # [doc = " Please note this isn't the same operation as the `>>` shifting operator or"] # [doc = " [`rotate_right`](Self::rotate_right), although `a.funnel_shr(a, n)` is *equivalent*"] # [doc = " to `a.rotate_right(n)`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If `n` is greater than or equal to the number of bits in `self`"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(funnel_shifts)]"] # [doc = concat ! ("let a = " , $ rot_op , stringify ! ($ SelfT) , ";")] # [doc = concat ! ("let b = " , $ fsh_op , stringify ! ($ SelfT) , ";")] # [doc = concat ! ("let m = " , $ fshr_result , ";")] # [doc = ""] # [doc = concat ! ("assert_eq!(a.funnel_shr(b, " , $ rot , "), m);")] # [doc = " ```"] # [rustc_const_unstable (feature = "funnel_shifts" , issue = "145686")] # [unstable (feature = "funnel_shifts" , issue = "145686")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn funnel_shr (self , rhs : Self , n : u32) -> Self { assert ! (n < Self :: BITS , "attempt to funnel shift right with overflow") ; unsafe { intrinsics :: unchecked_funnel_shr (self , rhs , n) } } # [doc = " Reverses the byte order of the integer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = " , $ swap_op , stringify ! ($ SelfT) , ";")] # [doc = " let m = n.swap_bytes();"] # [doc = ""] # [doc = concat ! ("assert_eq!(m, " , $ swapped , ");")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn swap_bytes (self) -> Self { intrinsics :: bswap (self as $ ActualT) as Self } # [doc = " Reverses the order of bits in the integer. The least significant bit becomes the most significant bit,"] # [doc = "                 second least-significant bit becomes second most-significant bit, etc."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = " , $ swap_op , stringify ! ($ SelfT) , ";")] # [doc = " let m = n.reverse_bits();"] # [doc = ""] # [doc = concat ! ("assert_eq!(m, " , $ reversed , ");")] # [doc = concat ! ("assert_eq!(0, 0" , stringify ! ($ SelfT) , ".reverse_bits());")] # [doc = " ```"] # [stable (feature = "reverse_bits" , since = "1.37.0")] # [rustc_const_stable (feature = "reverse_bits" , since = "1.37.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn reverse_bits (self) -> Self { intrinsics :: bitreverse (self as $ ActualT) as Self } # [doc = " Converts an integer from big endian to the target's endianness."] # [doc = ""] # [doc = " On big endian this is a no-op. On little endian the bytes are"] # [doc = " swapped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = 0x1A" , stringify ! ($ SelfT) , ";")] # [doc = ""] # [doc = " if cfg!(target_endian = \"big\") {"] # [doc = concat ! ("    assert_eq!(" , stringify ! ($ SelfT) , "::from_be(n), n)")] # [doc = " } else {"] # [doc = concat ! ("    assert_eq!(" , stringify ! ($ SelfT) , "::from_be(n), n.swap_bytes())")] # [doc = " }"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_math" , since = "1.32.0")] # [must_use] # [inline (always)] pub const fn from_be (x : Self) -> Self { # [cfg (target_endian = "big")] { x } # [cfg (not (target_endian = "big"))] { x . swap_bytes () } } # [doc = " Converts an integer from little endian to the target's endianness."] # [doc = ""] # [doc = " On little endian this is a no-op. On big endian the bytes are"] # [doc = " swapped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = 0x1A" , stringify ! ($ SelfT) , ";")] # [doc = ""] # [doc = " if cfg!(target_endian = \"little\") {"] # [doc = concat ! ("    assert_eq!(" , stringify ! ($ SelfT) , "::from_le(n), n)")] # [doc = " } else {"] # [doc = concat ! ("    assert_eq!(" , stringify ! ($ SelfT) , "::from_le(n), n.swap_bytes())")] # [doc = " }"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_math" , since = "1.32.0")] # [must_use] # [inline (always)] pub const fn from_le (x : Self) -> Self { # [cfg (target_endian = "little")] { x } # [cfg (not (target_endian = "little"))] { x . swap_bytes () } } # [doc = " Converts `self` to big endian from the target's endianness."] # [doc = ""] # [doc = " On big endian this is a no-op. On little endian the bytes are"] # [doc = " swapped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = 0x1A" , stringify ! ($ SelfT) , ";")] # [doc = ""] # [doc = " if cfg!(target_endian = \"big\") {"] # [doc = "     assert_eq!(n.to_be(), n)"] # [doc = " } else {"] # [doc = "     assert_eq!(n.to_be(), n.swap_bytes())"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn to_be (self) -> Self { # [cfg (target_endian = "big")] { self } # [cfg (not (target_endian = "big"))] { self . swap_bytes () } } # [doc = " Converts `self` to little endian from the target's endianness."] # [doc = ""] # [doc = " On little endian this is a no-op. On big endian the bytes are"] # [doc = " swapped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let n = 0x1A" , stringify ! ($ SelfT) , ";")] # [doc = ""] # [doc = " if cfg!(target_endian = \"little\") {"] # [doc = "     assert_eq!(n.to_le(), n)"] # [doc = " } else {"] # [doc = "     assert_eq!(n.to_le(), n.swap_bytes())"] # [doc = " }"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn to_le (self) -> Self { # [cfg (target_endian = "little")] { self } # [cfg (not (target_endian = "little"))] { self . swap_bytes () } } # [doc = " Checked integer addition. Computes `self + rhs`, returning `None`"] # [doc = " if overflow occurred."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SelfT) , "::MAX - 2).checked_add(1), " , "Some(" , stringify ! ($ SelfT) , "::MAX - 1));")] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SelfT) , "::MAX - 2).checked_add(3), None);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_checked_int_methods" , since = "1.47.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_add (self , rhs : Self) -> Option < Self > { if intrinsics :: unlikely (intrinsics :: add_with_overflow (self , rhs) . 1) { None } else { Some (unsafe { intrinsics :: unchecked_add (self , rhs) }) } } # [doc = " Strict integer addition. Computes `self + rhs`, panicking"] # [doc = " if overflow occurred."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " ## Overflow behavior"] # [doc = ""] # [doc = " This function will always panic on overflow, regardless of whether overflow checks are enabled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SelfT) , "::MAX - 2).strict_add(1), " , stringify ! ($ SelfT) , "::MAX - 1);")] # [doc = " ```"] # [doc = ""] # [doc = " The following panics because of overflow:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = (" , stringify ! ($ SelfT) , "::MAX - 2).strict_add(3);")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn strict_add (self , rhs : Self) -> Self { let (a , b) = self . overflowing_add (rhs) ; if b { overflow_panic :: add () } else { a } } # [doc = " Unchecked integer addition. Computes `self + rhs`, assuming overflow"] # [doc = " cannot occur."] # [doc = ""] # [doc = " Calling `x.unchecked_add(y)` is semantically equivalent to calling"] # [doc = " `x.`[`checked_add`]`(y).`[`unwrap_unchecked`]`()`."] # [doc = ""] # [doc = " If you're just trying to avoid the panic in debug mode, then **do not**"] # [doc = " use this.  Instead, you're looking for [`wrapping_add`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This results in undefined behavior when"] # [doc = concat ! ("`self + rhs > " , stringify ! ($ SelfT) , "::MAX` or `self + rhs < " , stringify ! ($ SelfT) , "::MIN`,")] # [doc = " i.e. when [`checked_add`] would return `None`."] # [doc = ""] # [doc = " [`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked"] # [doc = concat ! ("[`checked_add`]: " , stringify ! ($ SelfT) , "::checked_add")] # [doc = concat ! ("[`wrapping_add`]: " , stringify ! ($ SelfT) , "::wrapping_add")] # [stable (feature = "unchecked_math" , since = "1.79.0")] # [rustc_const_stable (feature = "unchecked_math" , since = "1.79.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const unsafe fn unchecked_add (self , rhs : Self) -> Self { assert_unsafe_precondition ! (check_language_ub , concat ! (stringify ! ($ SelfT) , "::unchecked_add cannot overflow") , (lhs : $ SelfT = self , rhs : $ SelfT = rhs ,) => ! lhs . overflowing_add (rhs) . 1 ,) ; unsafe { intrinsics :: unchecked_add (self , rhs) } } # [doc = " Checked addition with a signed integer. Computes `self + rhs`,"] # [doc = " returning `None` if overflow occurred."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".checked_add_signed(2), Some(3));")] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".checked_add_signed(-2), None);")] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SelfT) , "::MAX - 2).checked_add_signed(3), None);")] # [doc = " ```"] # [stable (feature = "mixed_integer_ops" , since = "1.66.0")] # [rustc_const_stable (feature = "mixed_integer_ops" , since = "1.66.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_add_signed (self , rhs : $ SignedT) -> Option < Self > { let (a , b) = self . overflowing_add_signed (rhs) ; if intrinsics :: unlikely (b) { None } else { Some (a) } } # [doc = " Strict addition with a signed integer. Computes `self + rhs`,"] # [doc = " panicking if overflow occurred."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " ## Overflow behavior"] # [doc = ""] # [doc = " This function will always panic on overflow, regardless of whether overflow checks are enabled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".strict_add_signed(2), 3);")] # [doc = " ```"] # [doc = ""] # [doc = " The following panic because of overflow:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = 1" , stringify ! ($ SelfT) , ".strict_add_signed(-2);")] # [doc = " ```"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = (" , stringify ! ($ SelfT) , "::MAX - 2).strict_add_signed(3);")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn strict_add_signed (self , rhs : $ SignedT) -> Self { let (a , b) = self . overflowing_add_signed (rhs) ; if b { overflow_panic :: add () } else { a } } # [doc = " Checked integer subtraction. Computes `self - rhs`, returning"] # [doc = " `None` if overflow occurred."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".checked_sub(1), Some(0));")] # [doc = concat ! ("assert_eq!(0" , stringify ! ($ SelfT) , ".checked_sub(1), None);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_checked_int_methods" , since = "1.47.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_sub (self , rhs : Self) -> Option < Self > { if self < rhs { None } else { Some (unsafe { intrinsics :: unchecked_sub (self , rhs) }) } } # [doc = " Strict integer subtraction. Computes `self - rhs`, panicking if"] # [doc = " overflow occurred."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " ## Overflow behavior"] # [doc = ""] # [doc = " This function will always panic on overflow, regardless of whether overflow checks are enabled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".strict_sub(1), 0);")] # [doc = " ```"] # [doc = ""] # [doc = " The following panics because of overflow:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = 0" , stringify ! ($ SelfT) , ".strict_sub(1);")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn strict_sub (self , rhs : Self) -> Self { let (a , b) = self . overflowing_sub (rhs) ; if b { overflow_panic :: sub () } else { a } } # [doc = " Unchecked integer subtraction. Computes `self - rhs`, assuming overflow"] # [doc = " cannot occur."] # [doc = ""] # [doc = " Calling `x.unchecked_sub(y)` is semantically equivalent to calling"] # [doc = " `x.`[`checked_sub`]`(y).`[`unwrap_unchecked`]`()`."] # [doc = ""] # [doc = " If you're just trying to avoid the panic in debug mode, then **do not**"] # [doc = " use this.  Instead, you're looking for [`wrapping_sub`]."] # [doc = ""] # [doc = " If you find yourself writing code like this:"] # [doc = ""] # [doc = " ```"] # [doc = " # let foo = 30_u32;"] # [doc = " # let bar = 20;"] # [doc = " if foo >= bar {"] # [doc = "     // SAFETY: just checked it will not overflow"] # [doc = "     let diff = unsafe { foo.unchecked_sub(bar) };"] # [doc = "     // ... use diff ..."] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Consider changing it to"] # [doc = ""] # [doc = " ```"] # [doc = " # let foo = 30_u32;"] # [doc = " # let bar = 20;"] # [doc = " if let Some(diff) = foo.checked_sub(bar) {"] # [doc = "     // ... use diff ..."] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " As that does exactly the same thing -- including telling the optimizer"] # [doc = " that the subtraction cannot overflow -- but avoids needing `unsafe`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This results in undefined behavior when"] # [doc = concat ! ("`self - rhs > " , stringify ! ($ SelfT) , "::MAX` or `self - rhs < " , stringify ! ($ SelfT) , "::MIN`,")] # [doc = " i.e. when [`checked_sub`] would return `None`."] # [doc = ""] # [doc = " [`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked"] # [doc = concat ! ("[`checked_sub`]: " , stringify ! ($ SelfT) , "::checked_sub")] # [doc = concat ! ("[`wrapping_sub`]: " , stringify ! ($ SelfT) , "::wrapping_sub")] # [stable (feature = "unchecked_math" , since = "1.79.0")] # [rustc_const_stable (feature = "unchecked_math" , since = "1.79.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const unsafe fn unchecked_sub (self , rhs : Self) -> Self { assert_unsafe_precondition ! (check_language_ub , concat ! (stringify ! ($ SelfT) , "::unchecked_sub cannot overflow") , (lhs : $ SelfT = self , rhs : $ SelfT = rhs ,) => ! lhs . overflowing_sub (rhs) . 1 ,) ; unsafe { intrinsics :: unchecked_sub (self , rhs) } } # [doc = " Checked subtraction with a signed integer. Computes `self - rhs`,"] # [doc = " returning `None` if overflow occurred."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".checked_sub_signed(2), None);")] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".checked_sub_signed(-2), Some(3));")] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SelfT) , "::MAX - 2).checked_sub_signed(-4), None);")] # [doc = " ```"] # [stable (feature = "mixed_integer_ops_unsigned_sub" , since = "1.90.0")] # [rustc_const_stable (feature = "mixed_integer_ops_unsigned_sub" , since = "1.90.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_sub_signed (self , rhs : $ SignedT) -> Option < Self > { let (res , overflow) = self . overflowing_sub_signed (rhs) ; if ! overflow { Some (res) } else { None } } # [doc = " Strict subtraction with a signed integer. Computes `self - rhs`,"] # [doc = " panicking if overflow occurred."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " ## Overflow behavior"] # [doc = ""] # [doc = " This function will always panic on overflow, regardless of whether overflow checks are enabled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(3" , stringify ! ($ SelfT) , ".strict_sub_signed(2), 1);")] # [doc = " ```"] # [doc = ""] # [doc = " The following panic because of overflow:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = 1" , stringify ! ($ SelfT) , ".strict_sub_signed(2);")] # [doc = " ```"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = (" , stringify ! ($ SelfT) , "::MAX).strict_sub_signed(-1);")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn strict_sub_signed (self , rhs : $ SignedT) -> Self { let (a , b) = self . overflowing_sub_signed (rhs) ; if b { overflow_panic :: sub () } else { a } } # [doc = concat ! ("Checked integer subtraction. Computes `self - rhs` and checks if the result fits into an [`" , stringify ! ($ SignedT) , "`], returning `None` if overflow occurred.")] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(10" , stringify ! ($ SelfT) , ".checked_signed_diff(2), Some(8));")] # [doc = concat ! ("assert_eq!(2" , stringify ! ($ SelfT) , ".checked_signed_diff(10), Some(-8));")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.checked_signed_diff(" , stringify ! ($ SignedT) , "::MAX as " , stringify ! ($ SelfT) , "), None);")] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SignedT) , "::MAX as " , stringify ! ($ SelfT) , ").checked_signed_diff(" , stringify ! ($ SelfT) , "::MAX), Some(" , stringify ! ($ SignedT) , "::MIN));")] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SignedT) , "::MAX as " , stringify ! ($ SelfT) , " + 1).checked_signed_diff(0), None);")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.checked_signed_diff(" , stringify ! ($ SelfT) , "::MAX), Some(0));")] # [doc = " ```"] # [stable (feature = "unsigned_signed_diff" , since = "1.91.0")] # [rustc_const_stable (feature = "unsigned_signed_diff" , since = "1.91.0")] # [inline] pub const fn checked_signed_diff (self , rhs : Self) -> Option <$ SignedT > { let res = self . wrapping_sub (rhs) as $ SignedT ; let overflow = (self >= rhs) == (res < 0) ; if ! overflow { Some (res) } else { None } } # [doc = " Checked integer multiplication. Computes `self * rhs`, returning"] # [doc = " `None` if overflow occurred."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".checked_mul(1), Some(5));")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.checked_mul(2), None);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_checked_int_methods" , since = "1.47.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_mul (self , rhs : Self) -> Option < Self > { let (a , b) = self . overflowing_mul (rhs) ; if intrinsics :: unlikely (b) { None } else { Some (a) } } # [doc = " Strict integer multiplication. Computes `self * rhs`, panicking if"] # [doc = " overflow occurred."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " ## Overflow behavior"] # [doc = ""] # [doc = " This function will always panic on overflow, regardless of whether overflow checks are enabled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".strict_mul(1), 5);")] # [doc = " ```"] # [doc = ""] # [doc = " The following panics because of overflow:"] # [doc = ""] # [doc = " ``` should_panic"] # [doc = concat ! ("let _ = " , stringify ! ($ SelfT) , "::MAX.strict_mul(2);")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn strict_mul (self , rhs : Self) -> Self { let (a , b) = self . overflowing_mul (rhs) ; if b { overflow_panic :: mul () } else { a } } # [doc = " Unchecked integer multiplication. Computes `self * rhs`, assuming overflow"] # [doc = " cannot occur."] # [doc = ""] # [doc = " Calling `x.unchecked_mul(y)` is semantically equivalent to calling"] # [doc = " `x.`[`checked_mul`]`(y).`[`unwrap_unchecked`]`()`."] # [doc = ""] # [doc = " If you're just trying to avoid the panic in debug mode, then **do not**"] # [doc = " use this.  Instead, you're looking for [`wrapping_mul`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This results in undefined behavior when"] # [doc = concat ! ("`self * rhs > " , stringify ! ($ SelfT) , "::MAX` or `self * rhs < " , stringify ! ($ SelfT) , "::MIN`,")] # [doc = " i.e. when [`checked_mul`] would return `None`."] # [doc = ""] # [doc = " [`unwrap_unchecked`]: option/enum.Option.html#method.unwrap_unchecked"] # [doc = concat ! ("[`checked_mul`]: " , stringify ! ($ SelfT) , "::checked_mul")] # [doc = concat ! ("[`wrapping_mul`]: " , stringify ! ($ SelfT) , "::wrapping_mul")] # [stable (feature = "unchecked_math" , since = "1.79.0")] # [rustc_const_stable (feature = "unchecked_math" , since = "1.79.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const unsafe fn unchecked_mul (self , rhs : Self) -> Self { assert_unsafe_precondition ! (check_language_ub , concat ! (stringify ! ($ SelfT) , "::unchecked_mul cannot overflow") , (lhs : $ SelfT = self , rhs : $ SelfT = rhs ,) => ! lhs . overflowing_mul (rhs) . 1 ,) ; unsafe { intrinsics :: unchecked_mul (self , rhs) } } # [doc = " Checked integer division. Computes `self / rhs`, returning `None`"] # [doc = " if `rhs == 0`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(128" , stringify ! ($ SelfT) , ".checked_div(2), Some(64));")] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".checked_div(0), None);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_checked_int_div" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_div (self , rhs : Self) -> Option < Self > { if intrinsics :: unlikely (rhs == 0) { None } else { Some (unsafe { intrinsics :: unchecked_div (self , rhs) }) } } # [doc = " Strict integer division. Computes `self / rhs`."] # [doc = ""] # [doc = " Strict division on unsigned types is just normal division. There's no"] # [doc = " way overflow could ever happen. This function exists so that all"] # [doc = " operations are accounted for in the strict operations."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".strict_div(10), 10);")] # [doc = " ```"] # [doc = ""] # [doc = " The following panics because of division by zero:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = (1" , stringify ! ($ SelfT) , ").strict_div(0);")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const fn strict_div (self , rhs : Self) -> Self { self / rhs } # [doc = " Checked Euclidean division. Computes `self.div_euclid(rhs)`, returning `None`"] # [doc = " if `rhs == 0`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(128" , stringify ! ($ SelfT) , ".checked_div_euclid(2), Some(64));")] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".checked_div_euclid(0), None);")] # [doc = " ```"] # [stable (feature = "euclidean_division" , since = "1.38.0")] # [rustc_const_stable (feature = "const_euclidean_int_methods" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_div_euclid (self , rhs : Self) -> Option < Self > { if intrinsics :: unlikely (rhs == 0) { None } else { Some (self . div_euclid (rhs)) } } # [doc = " Strict Euclidean division. Computes `self.div_euclid(rhs)`."] # [doc = ""] # [doc = " Strict division on unsigned types is just normal division. There's no"] # [doc = " way overflow could ever happen. This function exists so that all"] # [doc = " operations are accounted for in the strict operations. Since, for the"] # [doc = " positive integers, all common definitions of division are equal, this"] # [doc = " is exactly equal to `self.strict_div(rhs)`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".strict_div_euclid(10), 10);")] # [doc = " ```"] # [doc = " The following panics because of division by zero:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = (1" , stringify ! ($ SelfT) , ").strict_div_euclid(0);")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const fn strict_div_euclid (self , rhs : Self) -> Self { self / rhs } # [doc = " Checked integer division without remainder. Computes `self / rhs`,"] # [doc = " returning `None` if `rhs == 0` or if `self % rhs != 0`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(exact_div)]"] # [doc = concat ! ("assert_eq!(64" , stringify ! ($ SelfT) , ".checked_exact_div(2), Some(32));")] # [doc = concat ! ("assert_eq!(64" , stringify ! ($ SelfT) , ".checked_exact_div(32), Some(2));")] # [doc = concat ! ("assert_eq!(64" , stringify ! ($ SelfT) , ".checked_exact_div(0), None);")] # [doc = concat ! ("assert_eq!(65" , stringify ! ($ SelfT) , ".checked_exact_div(2), None);")] # [doc = " ```"] # [unstable (feature = "exact_div" , issue = "139911" ,)] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_exact_div (self , rhs : Self) -> Option < Self > { if intrinsics :: unlikely (rhs == 0) { None } else { unsafe { if intrinsics :: unlikely (intrinsics :: unchecked_rem (self , rhs) != 0) { None } else { Some (intrinsics :: exact_div (self , rhs)) } } } } # [doc = " Checked integer division without remainder. Computes `self / rhs`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic  if `rhs == 0` or `self % rhs != 0`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(exact_div)]"] # [doc = concat ! ("assert_eq!(64" , stringify ! ($ SelfT) , ".exact_div(2), 32);")] # [doc = concat ! ("assert_eq!(64" , stringify ! ($ SelfT) , ".exact_div(32), 2);")] # [doc = " ```"] # [doc = ""] # [doc = " ```should_panic"] # [doc = " #![feature(exact_div)]"] # [doc = concat ! ("let _ = 65" , stringify ! ($ SelfT) , ".exact_div(2);")] # [doc = " ```"] # [unstable (feature = "exact_div" , issue = "139911" ,)] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn exact_div (self , rhs : Self) -> Self { match self . checked_exact_div (rhs) { Some (x) => x , None => panic ! ("Failed to divide without remainder") , } } # [doc = " Unchecked integer division without remainder. Computes `self / rhs`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This results in undefined behavior when `rhs == 0` or `self % rhs != 0`,"] # [doc = " i.e. when [`checked_exact_div`](Self::checked_exact_div) would return `None`."] # [unstable (feature = "exact_div" , issue = "139911" ,)] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const unsafe fn unchecked_exact_div (self , rhs : Self) -> Self { assert_unsafe_precondition ! (check_language_ub , concat ! (stringify ! ($ SelfT) , "::unchecked_exact_div divide by zero or leave a remainder") , (lhs : $ SelfT = self , rhs : $ SelfT = rhs ,) => rhs > 0 && lhs % rhs == 0 ,) ; unsafe { intrinsics :: exact_div (self , rhs) } } # [doc = " Checked integer remainder. Computes `self % rhs`, returning `None`"] # [doc = " if `rhs == 0`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".checked_rem(2), Some(1));")] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".checked_rem(0), None);")] # [doc = " ```"] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_checked_int_div" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_rem (self , rhs : Self) -> Option < Self > { if intrinsics :: unlikely (rhs == 0) { None } else { Some (unsafe { intrinsics :: unchecked_rem (self , rhs) }) } } # [doc = " Strict integer remainder. Computes `self % rhs`."] # [doc = ""] # [doc = " Strict remainder calculation on unsigned types is just the regular"] # [doc = " remainder calculation. There's no way overflow could ever happen."] # [doc = " This function exists so that all operations are accounted for in the"] # [doc = " strict operations."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".strict_rem(10), 0);")] # [doc = " ```"] # [doc = ""] # [doc = " The following panics because of division by zero:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = 5" , stringify ! ($ SelfT) , ".strict_rem(0);")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const fn strict_rem (self , rhs : Self) -> Self { self % rhs } # [doc = " Checked Euclidean modulo. Computes `self.rem_euclid(rhs)`, returning `None`"] # [doc = " if `rhs == 0`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".checked_rem_euclid(2), Some(1));")] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".checked_rem_euclid(0), None);")] # [doc = " ```"] # [stable (feature = "euclidean_division" , since = "1.38.0")] # [rustc_const_stable (feature = "const_euclidean_int_methods" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_rem_euclid (self , rhs : Self) -> Option < Self > { if intrinsics :: unlikely (rhs == 0) { None } else { Some (self . rem_euclid (rhs)) } } # [doc = " Strict Euclidean modulo. Computes `self.rem_euclid(rhs)`."] # [doc = ""] # [doc = " Strict modulo calculation on unsigned types is just the regular"] # [doc = " remainder calculation. There's no way overflow could ever happen."] # [doc = " This function exists so that all operations are accounted for in the"] # [doc = " strict operations. Since, for the positive integers, all common"] # [doc = " definitions of division are equal, this is exactly equal to"] # [doc = " `self.strict_rem(rhs)`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".strict_rem_euclid(10), 0);")] # [doc = " ```"] # [doc = ""] # [doc = " The following panics because of division by zero:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = 5" , stringify ! ($ SelfT) , ".strict_rem_euclid(0);")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const fn strict_rem_euclid (self , rhs : Self) -> Self { self % rhs } # [doc = " Same value as `self | other`, but UB if any bit position is set in both inputs."] # [doc = ""] # [doc = " This is a situational micro-optimization for places where you'd rather"] # [doc = " use addition on some platforms and bitwise or on other platforms, based"] # [doc = " on exactly which instructions combine better with whatever else you're"] # [doc = " doing.  Note that there's no reason to bother using this for places"] # [doc = " where it's clear from the operations involved that they can't overlap."] # [doc = " For example, if you're combining `u16`s into a `u32` with"] # [doc = " `((a as u32) << 16) | (b as u32)`, that's fine, as the backend will"] # [doc = " know those sides of the `|` are disjoint without needing help."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(disjoint_bitor)]"] # [doc = ""] # [doc = " // SAFETY: `1` and `4` have no bits in common."] # [doc = " unsafe {"] # [doc = concat ! ("    assert_eq!(1_" , stringify ! ($ SelfT) , ".unchecked_disjoint_bitor(4), 5);")] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Requires that `(self & other) == 0`, otherwise it's immediate UB."] # [doc = ""] # [doc = " Equivalently, requires that `(self | other) == (self + other)`."] # [unstable (feature = "disjoint_bitor" , issue = "135758")] # [rustc_const_unstable (feature = "disjoint_bitor" , issue = "135758")] # [inline] pub const unsafe fn unchecked_disjoint_bitor (self , other : Self) -> Self { assert_unsafe_precondition ! (check_language_ub , concat ! (stringify ! ($ SelfT) , "::unchecked_disjoint_bitor cannot have overlapping bits") , (lhs : $ SelfT = self , rhs : $ SelfT = other ,) => (lhs & rhs) == 0 ,) ; unsafe { intrinsics :: disjoint_bitor (self , other) } } # [doc = " Returns the logarithm of the number with respect to an arbitrary base,"] # [doc = " rounded down."] # [doc = ""] # [doc = " This method might not be optimized owing to implementation details;"] # [doc = " `ilog2` can produce results more efficiently for base 2, and `ilog10`"] # [doc = " can produce results more efficiently for base 10."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `self` is zero, or if `base` is less than 2."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".ilog(5), 1);")] # [doc = " ```"] # [stable (feature = "int_log" , since = "1.67.0")] # [rustc_const_stable (feature = "int_log" , since = "1.67.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn ilog (self , base : Self) -> u32 { assert ! (base >= 2 , "base of integer logarithm must be at least 2") ; if let Some (log) = self . checked_ilog (base) { log } else { int_log10 :: panic_for_nonpositive_argument () } } # [doc = " Returns the base 2 logarithm of the number, rounded down."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `self` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(2" , stringify ! ($ SelfT) , ".ilog2(), 1);")] # [doc = " ```"] # [stable (feature = "int_log" , since = "1.67.0")] # [rustc_const_stable (feature = "int_log" , since = "1.67.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn ilog2 (self) -> u32 { if let Some (log) = self . checked_ilog2 () { log } else { int_log10 :: panic_for_nonpositive_argument () } } # [doc = " Returns the base 10 logarithm of the number, rounded down."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `self` is zero."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(10" , stringify ! ($ SelfT) , ".ilog10(), 1);")] # [doc = " ```"] # [stable (feature = "int_log" , since = "1.67.0")] # [rustc_const_stable (feature = "int_log" , since = "1.67.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn ilog10 (self) -> u32 { if let Some (log) = self . checked_ilog10 () { log } else { int_log10 :: panic_for_nonpositive_argument () } } # [doc = " Returns the logarithm of the number with respect to an arbitrary base,"] # [doc = " rounded down."] # [doc = ""] # [doc = " Returns `None` if the number is zero, or if the base is not at least 2."] # [doc = ""] # [doc = " This method might not be optimized owing to implementation details;"] # [doc = " `checked_ilog2` can produce results more efficiently for base 2, and"] # [doc = " `checked_ilog10` can produce results more efficiently for base 10."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".checked_ilog(5), Some(1));")] # [doc = " ```"] # [stable (feature = "int_log" , since = "1.67.0")] # [rustc_const_stable (feature = "int_log" , since = "1.67.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_ilog (self , base : Self) -> Option < u32 > { if core :: intrinsics :: is_val_statically_known (base) { if base == 2 { return self . checked_ilog2 () ; } else if base == 10 { return self . checked_ilog10 () ; } } if self <= 0 || base <= 1 { None } else if self < base { Some (0) } else { let mut n = 1 ; let mut r = base ; if Self :: BITS == 128 { n = self . ilog2 () / (base . ilog2 () + 1) ; r = base . pow (n) ; } while r <= self / base { n += 1 ; r *= base ; } Some (n) } } # [doc = " Returns the base 2 logarithm of the number, rounded down."] # [doc = ""] # [doc = " Returns `None` if the number is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(2" , stringify ! ($ SelfT) , ".checked_ilog2(), Some(1));")] # [doc = " ```"] # [stable (feature = "int_log" , since = "1.67.0")] # [rustc_const_stable (feature = "int_log" , since = "1.67.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_ilog2 (self) -> Option < u32 > { match NonZero :: new (self) { Some (x) => Some (x . ilog2 ()) , None => None , } } # [doc = " Returns the base 10 logarithm of the number, rounded down."] # [doc = ""] # [doc = " Returns `None` if the number is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(10" , stringify ! ($ SelfT) , ".checked_ilog10(), Some(1));")] # [doc = " ```"] # [stable (feature = "int_log" , since = "1.67.0")] # [rustc_const_stable (feature = "int_log" , since = "1.67.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_ilog10 (self) -> Option < u32 > { match NonZero :: new (self) { Some (x) => Some (x . ilog10 ()) , None => None , } } # [doc = " Checked negation. Computes `-self`, returning `None` unless `self =="] # [doc = " 0`."] # [doc = ""] # [doc = " Note that negating any positive integer will overflow."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(0" , stringify ! ($ SelfT) , ".checked_neg(), Some(0));")] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".checked_neg(), None);")] # [doc = " ```"] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_checked_int_methods" , since = "1.47.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_neg (self) -> Option < Self > { let (a , b) = self . overflowing_neg () ; if intrinsics :: unlikely (b) { None } else { Some (a) } } # [doc = " Strict negation. Computes `-self`, panicking unless `self =="] # [doc = " 0`."] # [doc = ""] # [doc = " Note that negating any positive integer will overflow."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " ## Overflow behavior"] # [doc = ""] # [doc = " This function will always panic on overflow, regardless of whether overflow checks are enabled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(0" , stringify ! ($ SelfT) , ".strict_neg(), 0);")] # [doc = " ```"] # [doc = ""] # [doc = " The following panics because of overflow:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = 1" , stringify ! ($ SelfT) , ".strict_neg();")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn strict_neg (self) -> Self { let (a , b) = self . overflowing_neg () ; if b { overflow_panic :: neg () } else { a } } # [doc = " Checked shift left. Computes `self << rhs`, returning `None`"] # [doc = " if `rhs` is larger than or equal to the number of bits in `self`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(0x1" , stringify ! ($ SelfT) , ".checked_shl(4), Some(0x10));")] # [doc = concat ! ("assert_eq!(0x10" , stringify ! ($ SelfT) , ".checked_shl(129), None);")] # [doc = concat ! ("assert_eq!(0x10" , stringify ! ($ SelfT) , ".checked_shl(" , stringify ! ($ BITS_MINUS_ONE) , "), Some(0));")] # [doc = " ```"] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_checked_int_methods" , since = "1.47.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_shl (self , rhs : u32) -> Option < Self > { if rhs < Self :: BITS { Some (unsafe { self . unchecked_shl (rhs) }) } else { None } } # [doc = " Strict shift left. Computes `self << rhs`, panicking if `rhs` is larger"] # [doc = " than or equal to the number of bits in `self`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " ## Overflow behavior"] # [doc = ""] # [doc = " This function will always panic on overflow, regardless of whether overflow checks are enabled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(0x1" , stringify ! ($ SelfT) , ".strict_shl(4), 0x10);")] # [doc = " ```"] # [doc = ""] # [doc = " The following panics because of overflow:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = 0x10" , stringify ! ($ SelfT) , ".strict_shl(129);")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn strict_shl (self , rhs : u32) -> Self { let (a , b) = self . overflowing_shl (rhs) ; if b { overflow_panic :: shl () } else { a } } # [doc = " Unchecked shift left. Computes `self << rhs`, assuming that"] # [doc = " `rhs` is less than the number of bits in `self`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This results in undefined behavior if `rhs` is larger than"] # [doc = " or equal to the number of bits in `self`,"] # [doc = " i.e. when [`checked_shl`] would return `None`."] # [doc = ""] # [doc = concat ! ("[`checked_shl`]: " , stringify ! ($ SelfT) , "::checked_shl")] # [unstable (feature = "unchecked_shifts" , reason = "niche optimization path" , issue = "85122" ,)] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const unsafe fn unchecked_shl (self , rhs : u32) -> Self { assert_unsafe_precondition ! (check_language_ub , concat ! (stringify ! ($ SelfT) , "::unchecked_shl cannot overflow") , (rhs : u32 = rhs ,) => rhs < <$ ActualT >:: BITS ,) ; unsafe { intrinsics :: unchecked_shl (self , rhs) } } # [doc = " Unbounded shift left. Computes `self << rhs`, without bounding the value of `rhs`."] # [doc = ""] # [doc = " If `rhs` is larger or equal to the number of bits in `self`,"] # [doc = " the entire value is shifted out, and `0` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(0x1" , stringify ! ($ SelfT) , ".unbounded_shl(4), 0x10);")] # [doc = concat ! ("assert_eq!(0x1" , stringify ! ($ SelfT) , ".unbounded_shl(129), 0);")] # [doc = " ```"] # [stable (feature = "unbounded_shifts" , since = "1.87.0")] # [rustc_const_stable (feature = "unbounded_shifts" , since = "1.87.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn unbounded_shl (self , rhs : u32) -> $ SelfT { if rhs < Self :: BITS { unsafe { self . unchecked_shl (rhs) } } else { 0 } } # [doc = " Exact shift left. Computes `self << rhs` as long as it can be reversed losslessly."] # [doc = ""] # [doc = " Returns `None` if any non-zero bits would be shifted out or if `rhs` >="] # [doc = concat ! ("`" , stringify ! ($ SelfT) , "::BITS`.")] # [doc = " Otherwise, returns `Some(self << rhs)`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(exact_bitshifts)]"] # [doc = ""] # [doc = concat ! ("assert_eq!(0x1" , stringify ! ($ SelfT) , ".exact_shl(4), Some(0x10));")] # [doc = concat ! ("assert_eq!(0x1" , stringify ! ($ SelfT) , ".exact_shl(129), None);")] # [doc = " ```"] # [unstable (feature = "exact_bitshifts" , issue = "144336")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn exact_shl (self , rhs : u32) -> Option <$ SelfT > { if rhs <= self . leading_zeros () && rhs < <$ SelfT >:: BITS { Some (unsafe { self . unchecked_shl (rhs) }) } else { None } } # [doc = " Unchecked exact shift left. Computes `self << rhs`, assuming the operation can be"] # [doc = " losslessly reversed `rhs` cannot be larger than"] # [doc = concat ! ("`" , stringify ! ($ SelfT) , "::BITS`.")] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This results in undefined behavior when `rhs > self.leading_zeros() || rhs >="] # [doc = concat ! (stringify ! ($ SelfT) , "::BITS`")] # [doc = " i.e. when"] # [doc = concat ! ("[`" , stringify ! ($ SelfT) , "::exact_shl`]")] # [doc = " would return `None`."] # [unstable (feature = "exact_bitshifts" , issue = "144336")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const unsafe fn unchecked_exact_shl (self , rhs : u32) -> $ SelfT { assert_unsafe_precondition ! (check_language_ub , concat ! (stringify ! ($ SelfT) , "::exact_shl_unchecked cannot shift out non-zero bits") , (zeros : u32 = self . leading_zeros () , bits : u32 = <$ SelfT >:: BITS , rhs : u32 = rhs ,) => rhs <= zeros && rhs < bits ,) ; unsafe { self . unchecked_shl (rhs) } } # [doc = " Checked shift right. Computes `self >> rhs`, returning `None`"] # [doc = " if `rhs` is larger than or equal to the number of bits in `self`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(0x10" , stringify ! ($ SelfT) , ".checked_shr(4), Some(0x1));")] # [doc = concat ! ("assert_eq!(0x10" , stringify ! ($ SelfT) , ".checked_shr(129), None);")] # [doc = " ```"] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_checked_int_methods" , since = "1.47.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_shr (self , rhs : u32) -> Option < Self > { if rhs < Self :: BITS { Some (unsafe { self . unchecked_shr (rhs) }) } else { None } } # [doc = " Strict shift right. Computes `self >> rhs`, panicking `rhs` is"] # [doc = " larger than or equal to the number of bits in `self`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " ## Overflow behavior"] # [doc = ""] # [doc = " This function will always panic on overflow, regardless of whether overflow checks are enabled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(0x10" , stringify ! ($ SelfT) , ".strict_shr(4), 0x1);")] # [doc = " ```"] # [doc = ""] # [doc = " The following panics because of overflow:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = 0x10" , stringify ! ($ SelfT) , ".strict_shr(129);")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn strict_shr (self , rhs : u32) -> Self { let (a , b) = self . overflowing_shr (rhs) ; if b { overflow_panic :: shr () } else { a } } # [doc = " Unchecked shift right. Computes `self >> rhs`, assuming that"] # [doc = " `rhs` is less than the number of bits in `self`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This results in undefined behavior if `rhs` is larger than"] # [doc = " or equal to the number of bits in `self`,"] # [doc = " i.e. when [`checked_shr`] would return `None`."] # [doc = ""] # [doc = concat ! ("[`checked_shr`]: " , stringify ! ($ SelfT) , "::checked_shr")] # [unstable (feature = "unchecked_shifts" , reason = "niche optimization path" , issue = "85122" ,)] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const unsafe fn unchecked_shr (self , rhs : u32) -> Self { assert_unsafe_precondition ! (check_language_ub , concat ! (stringify ! ($ SelfT) , "::unchecked_shr cannot overflow") , (rhs : u32 = rhs ,) => rhs < <$ ActualT >:: BITS ,) ; unsafe { intrinsics :: unchecked_shr (self , rhs) } } # [doc = " Unbounded shift right. Computes `self >> rhs`, without bounding the value of `rhs`."] # [doc = ""] # [doc = " If `rhs` is larger or equal to the number of bits in `self`,"] # [doc = " the entire value is shifted out, and `0` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(0x10" , stringify ! ($ SelfT) , ".unbounded_shr(4), 0x1);")] # [doc = concat ! ("assert_eq!(0x10" , stringify ! ($ SelfT) , ".unbounded_shr(129), 0);")] # [doc = " ```"] # [stable (feature = "unbounded_shifts" , since = "1.87.0")] # [rustc_const_stable (feature = "unbounded_shifts" , since = "1.87.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn unbounded_shr (self , rhs : u32) -> $ SelfT { if rhs < Self :: BITS { unsafe { self . unchecked_shr (rhs) } } else { 0 } } # [doc = " Exact shift right. Computes `self >> rhs` as long as it can be reversed losslessly."] # [doc = ""] # [doc = " Returns `None` if any non-zero bits would be shifted out or if `rhs` >="] # [doc = concat ! ("`" , stringify ! ($ SelfT) , "::BITS`.")] # [doc = " Otherwise, returns `Some(self >> rhs)`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(exact_bitshifts)]"] # [doc = ""] # [doc = concat ! ("assert_eq!(0x10" , stringify ! ($ SelfT) , ".exact_shr(4), Some(0x1));")] # [doc = concat ! ("assert_eq!(0x10" , stringify ! ($ SelfT) , ".exact_shr(5), None);")] # [doc = " ```"] # [unstable (feature = "exact_bitshifts" , issue = "144336")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn exact_shr (self , rhs : u32) -> Option <$ SelfT > { if rhs <= self . trailing_zeros () && rhs < <$ SelfT >:: BITS { Some (unsafe { self . unchecked_shr (rhs) }) } else { None } } # [doc = " Unchecked exact shift right. Computes `self >> rhs`, assuming the operation can be"] # [doc = " losslessly reversed and `rhs` cannot be larger than"] # [doc = concat ! ("`" , stringify ! ($ SelfT) , "::BITS`.")] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This results in undefined behavior when `rhs > self.trailing_zeros() || rhs >="] # [doc = concat ! (stringify ! ($ SelfT) , "::BITS`")] # [doc = " i.e. when"] # [doc = concat ! ("[`" , stringify ! ($ SelfT) , "::exact_shr`]")] # [doc = " would return `None`."] # [unstable (feature = "exact_bitshifts" , issue = "144336")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const unsafe fn unchecked_exact_shr (self , rhs : u32) -> $ SelfT { assert_unsafe_precondition ! (check_language_ub , concat ! (stringify ! ($ SelfT) , "::exact_shr_unchecked cannot shift out non-zero bits") , (zeros : u32 = self . trailing_zeros () , bits : u32 = <$ SelfT >:: BITS , rhs : u32 = rhs ,) => rhs <= zeros && rhs < bits ,) ; unsafe { self . unchecked_shr (rhs) } } # [doc = " Checked exponentiation. Computes `self.pow(exp)`, returning `None` if"] # [doc = " overflow occurred."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(2" , stringify ! ($ SelfT) , ".checked_pow(5), Some(32));")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.checked_pow(2), None);")] # [doc = " ```"] # [stable (feature = "no_panic_pow" , since = "1.34.0")] # [rustc_const_stable (feature = "const_int_pow" , since = "1.50.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_pow (self , mut exp : u32) -> Option < Self > { if exp == 0 { return Some (1) ; } let mut base = self ; let mut acc : Self = 1 ; loop { if (exp & 1) == 1 { acc = try_opt ! (acc . checked_mul (base)) ; if exp == 1 { return Some (acc) ; } } exp /= 2 ; base = try_opt ! (base . checked_mul (base)) ; } } # [doc = " Strict exponentiation. Computes `self.pow(exp)`, panicking if"] # [doc = " overflow occurred."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " ## Overflow behavior"] # [doc = ""] # [doc = " This function will always panic on overflow, regardless of whether overflow checks are enabled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(2" , stringify ! ($ SelfT) , ".strict_pow(5), 32);")] # [doc = " ```"] # [doc = ""] # [doc = " The following panics because of overflow:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = concat ! ("let _ = " , stringify ! ($ SelfT) , "::MAX.strict_pow(2);")] # [doc = " ```"] # [stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [rustc_const_stable (feature = "strict_overflow_ops" , since = "1.91.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn strict_pow (self , mut exp : u32) -> Self { if exp == 0 { return 1 ; } let mut base = self ; let mut acc : Self = 1 ; loop { if (exp & 1) == 1 { acc = acc . strict_mul (base) ; if exp == 1 { return acc ; } } exp /= 2 ; base = base . strict_mul (base) ; } } # [doc = " Saturating integer addition. Computes `self + rhs`, saturating at"] # [doc = " the numeric bounds instead of overflowing."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".saturating_add(1), 101);")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.saturating_add(127), " , stringify ! ($ SelfT) , "::MAX);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [rustc_const_stable (feature = "const_saturating_int_methods" , since = "1.47.0")] # [inline (always)] pub const fn saturating_add (self , rhs : Self) -> Self { intrinsics :: saturating_add (self , rhs) } # [doc = " Saturating addition with a signed integer. Computes `self + rhs`,"] # [doc = " saturating at the numeric bounds instead of overflowing."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".saturating_add_signed(2), 3);")] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".saturating_add_signed(-2), 0);")] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SelfT) , "::MAX - 2).saturating_add_signed(4), " , stringify ! ($ SelfT) , "::MAX);")] # [doc = " ```"] # [stable (feature = "mixed_integer_ops" , since = "1.66.0")] # [rustc_const_stable (feature = "mixed_integer_ops" , since = "1.66.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn saturating_add_signed (self , rhs : $ SignedT) -> Self { let (res , overflow) = self . overflowing_add (rhs as Self) ; if overflow == (rhs < 0) { res } else if overflow { Self :: MAX } else { 0 } } # [doc = " Saturating integer subtraction. Computes `self - rhs`, saturating"] # [doc = " at the numeric bounds instead of overflowing."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".saturating_sub(27), 73);")] # [doc = concat ! ("assert_eq!(13" , stringify ! ($ SelfT) , ".saturating_sub(127), 0);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [rustc_const_stable (feature = "const_saturating_int_methods" , since = "1.47.0")] # [inline (always)] pub const fn saturating_sub (self , rhs : Self) -> Self { intrinsics :: saturating_sub (self , rhs) } # [doc = " Saturating integer subtraction. Computes `self` - `rhs`, saturating at"] # [doc = " the numeric bounds instead of overflowing."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".saturating_sub_signed(2), 0);")] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".saturating_sub_signed(-2), 3);")] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SelfT) , "::MAX - 2).saturating_sub_signed(-4), " , stringify ! ($ SelfT) , "::MAX);")] # [doc = " ```"] # [stable (feature = "mixed_integer_ops_unsigned_sub" , since = "1.90.0")] # [rustc_const_stable (feature = "mixed_integer_ops_unsigned_sub" , since = "1.90.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn saturating_sub_signed (self , rhs : $ SignedT) -> Self { let (res , overflow) = self . overflowing_sub_signed (rhs) ; if ! overflow { res } else if rhs < 0 { Self :: MAX } else { 0 } } # [doc = " Saturating integer multiplication. Computes `self * rhs`,"] # [doc = " saturating at the numeric bounds instead of overflowing."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(2" , stringify ! ($ SelfT) , ".saturating_mul(10), 20);")] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SelfT) , "::MAX).saturating_mul(10), " , stringify ! ($ SelfT) , "::MAX);")] # [doc = " ```"] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_saturating_int_methods" , since = "1.47.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn saturating_mul (self , rhs : Self) -> Self { match self . checked_mul (rhs) { Some (x) => x , None => Self :: MAX , } } # [doc = " Saturating integer division. Computes `self / rhs`, saturating at the"] # [doc = " numeric bounds instead of overflowing."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".saturating_div(2), 2);")] # [doc = ""] # [doc = " ```"] # [stable (feature = "saturating_div" , since = "1.58.0")] # [rustc_const_stable (feature = "saturating_div" , since = "1.58.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn saturating_div (self , rhs : Self) -> Self { self . wrapping_div (rhs) } # [doc = " Saturating integer exponentiation. Computes `self.pow(exp)`,"] # [doc = " saturating at the numeric bounds instead of overflowing."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(4" , stringify ! ($ SelfT) , ".saturating_pow(3), 64);")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.saturating_pow(2), " , stringify ! ($ SelfT) , "::MAX);")] # [doc = " ```"] # [stable (feature = "no_panic_pow" , since = "1.34.0")] # [rustc_const_stable (feature = "const_int_pow" , since = "1.50.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn saturating_pow (self , exp : u32) -> Self { match self . checked_pow (exp) { Some (x) => x , None => Self :: MAX , } } # [doc = " Wrapping (modular) addition. Computes `self + rhs`,"] # [doc = " wrapping around at the boundary of the type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(200" , stringify ! ($ SelfT) , ".wrapping_add(55), 255);")] # [doc = concat ! ("assert_eq!(200" , stringify ! ($ SelfT) , ".wrapping_add(" , stringify ! ($ SelfT) , "::MAX), 199);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_wrapping_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn wrapping_add (self , rhs : Self) -> Self { intrinsics :: wrapping_add (self , rhs) } # [doc = " Wrapping (modular) addition with a signed integer. Computes"] # [doc = " `self + rhs`, wrapping around at the boundary of the type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".wrapping_add_signed(2), 3);")] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".wrapping_add_signed(-2), " , stringify ! ($ SelfT) , "::MAX);")] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SelfT) , "::MAX - 2).wrapping_add_signed(4), 1);")] # [doc = " ```"] # [stable (feature = "mixed_integer_ops" , since = "1.66.0")] # [rustc_const_stable (feature = "mixed_integer_ops" , since = "1.66.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn wrapping_add_signed (self , rhs : $ SignedT) -> Self { self . wrapping_add (rhs as Self) } # [doc = " Wrapping (modular) subtraction. Computes `self - rhs`,"] # [doc = " wrapping around at the boundary of the type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".wrapping_sub(100), 0);")] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".wrapping_sub(" , stringify ! ($ SelfT) , "::MAX), 101);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_wrapping_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn wrapping_sub (self , rhs : Self) -> Self { intrinsics :: wrapping_sub (self , rhs) } # [doc = " Wrapping (modular) subtraction with a signed integer. Computes"] # [doc = " `self - rhs`, wrapping around at the boundary of the type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".wrapping_sub_signed(2), " , stringify ! ($ SelfT) , "::MAX);")] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".wrapping_sub_signed(-2), 3);")] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SelfT) , "::MAX - 2).wrapping_sub_signed(-4), 1);")] # [doc = " ```"] # [stable (feature = "mixed_integer_ops_unsigned_sub" , since = "1.90.0")] # [rustc_const_stable (feature = "mixed_integer_ops_unsigned_sub" , since = "1.90.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn wrapping_sub_signed (self , rhs : $ SignedT) -> Self { self . wrapping_sub (rhs as Self) } # [doc = " Wrapping (modular) multiplication. Computes `self *"] # [doc = " rhs`, wrapping around at the boundary of the type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Please note that this example is shared among integer types, which is why `u8` is used."] # [doc = ""] # [doc = " ```"] # [doc = " assert_eq!(10u8.wrapping_mul(12), 120);"] # [doc = " assert_eq!(25u8.wrapping_mul(12), 44);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_wrapping_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn wrapping_mul (self , rhs : Self) -> Self { intrinsics :: wrapping_mul (self , rhs) } # [doc = " Wrapping (modular) division. Computes `self / rhs`."] # [doc = ""] # [doc = " Wrapped division on unsigned types is just normal division. There's"] # [doc = " no way wrapping could ever happen. This function exists so that all"] # [doc = " operations are accounted for in the wrapping operations."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".wrapping_div(10), 10);")] # [doc = " ```"] # [stable (feature = "num_wrapping" , since = "1.2.0")] # [rustc_const_stable (feature = "const_wrapping_int_methods" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const fn wrapping_div (self , rhs : Self) -> Self { self / rhs } # [doc = " Wrapping Euclidean division. Computes `self.div_euclid(rhs)`."] # [doc = ""] # [doc = " Wrapped division on unsigned types is just normal division. There's"] # [doc = " no way wrapping could ever happen. This function exists so that all"] # [doc = " operations are accounted for in the wrapping operations. Since, for"] # [doc = " the positive integers, all common definitions of division are equal,"] # [doc = " this is exactly equal to `self.wrapping_div(rhs)`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".wrapping_div_euclid(10), 10);")] # [doc = " ```"] # [stable (feature = "euclidean_division" , since = "1.38.0")] # [rustc_const_stable (feature = "const_euclidean_int_methods" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const fn wrapping_div_euclid (self , rhs : Self) -> Self { self / rhs } # [doc = " Wrapping (modular) remainder. Computes `self % rhs`."] # [doc = ""] # [doc = " Wrapped remainder calculation on unsigned types is just the regular"] # [doc = " remainder calculation. There's no way wrapping could ever happen."] # [doc = " This function exists so that all operations are accounted for in the"] # [doc = " wrapping operations."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".wrapping_rem(10), 0);")] # [doc = " ```"] # [stable (feature = "num_wrapping" , since = "1.2.0")] # [rustc_const_stable (feature = "const_wrapping_int_methods" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const fn wrapping_rem (self , rhs : Self) -> Self { self % rhs } # [doc = " Wrapping Euclidean modulo. Computes `self.rem_euclid(rhs)`."] # [doc = ""] # [doc = " Wrapped modulo calculation on unsigned types is just the regular"] # [doc = " remainder calculation. There's no way wrapping could ever happen."] # [doc = " This function exists so that all operations are accounted for in the"] # [doc = " wrapping operations. Since, for the positive integers, all common"] # [doc = " definitions of division are equal, this is exactly equal to"] # [doc = " `self.wrapping_rem(rhs)`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".wrapping_rem_euclid(10), 0);")] # [doc = " ```"] # [stable (feature = "euclidean_division" , since = "1.38.0")] # [rustc_const_stable (feature = "const_euclidean_int_methods" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const fn wrapping_rem_euclid (self , rhs : Self) -> Self { self % rhs } # [doc = " Wrapping (modular) negation. Computes `-self`,"] # [doc = " wrapping around at the boundary of the type."] # [doc = ""] # [doc = " Since unsigned types do not have negative equivalents"] # [doc = " all applications of this function will wrap (except for `-0`)."] # [doc = " For values smaller than the corresponding signed type's maximum"] # [doc = " the result is the same as casting the corresponding signed value."] # [doc = " Any larger values are equivalent to `MAX + 1 - (val - MAX - 1)` where"] # [doc = " `MAX` is the corresponding signed type's maximum."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(0_" , stringify ! ($ SelfT) , ".wrapping_neg(), 0);")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.wrapping_neg(), 1);")] # [doc = concat ! ("assert_eq!(13_" , stringify ! ($ SelfT) , ".wrapping_neg(), (!13) + 1);")] # [doc = concat ! ("assert_eq!(42_" , stringify ! ($ SelfT) , ".wrapping_neg(), !(42 - 1));")] # [doc = " ```"] # [stable (feature = "num_wrapping" , since = "1.2.0")] # [rustc_const_stable (feature = "const_wrapping_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn wrapping_neg (self) -> Self { (0 as $ SelfT) . wrapping_sub (self) } # [doc = " Panic-free bitwise shift-left; yields `self << mask(rhs)`,"] # [doc = " where `mask` removes any high-order bits of `rhs` that"] # [doc = " would cause the shift to exceed the bitwidth of the type."] # [doc = ""] # [doc = " Note that this is *not* the same as a rotate-left; the"] # [doc = " RHS of a wrapping shift-left is restricted to the range"] # [doc = " of the type, rather than the bits shifted out of the LHS"] # [doc = " being returned to the other end. The primitive integer"] # [doc = " types all implement a [`rotate_left`](Self::rotate_left) function,"] # [doc = " which may be what you want instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".wrapping_shl(7), 128);")] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".wrapping_shl(128), 1);")] # [doc = " ```"] # [stable (feature = "num_wrapping" , since = "1.2.0")] # [rustc_const_stable (feature = "const_wrapping_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn wrapping_shl (self , rhs : u32) -> Self { unsafe { self . unchecked_shl (rhs & (Self :: BITS - 1)) } } # [doc = " Panic-free bitwise shift-right; yields `self >> mask(rhs)`,"] # [doc = " where `mask` removes any high-order bits of `rhs` that"] # [doc = " would cause the shift to exceed the bitwidth of the type."] # [doc = ""] # [doc = " Note that this is *not* the same as a rotate-right; the"] # [doc = " RHS of a wrapping shift-right is restricted to the range"] # [doc = " of the type, rather than the bits shifted out of the LHS"] # [doc = " being returned to the other end. The primitive integer"] # [doc = " types all implement a [`rotate_right`](Self::rotate_right) function,"] # [doc = " which may be what you want instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(128" , stringify ! ($ SelfT) , ".wrapping_shr(7), 1);")] # [doc = concat ! ("assert_eq!(128" , stringify ! ($ SelfT) , ".wrapping_shr(128), 128);")] # [doc = " ```"] # [stable (feature = "num_wrapping" , since = "1.2.0")] # [rustc_const_stable (feature = "const_wrapping_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn wrapping_shr (self , rhs : u32) -> Self { unsafe { self . unchecked_shr (rhs & (Self :: BITS - 1)) } } # [doc = " Wrapping (modular) exponentiation. Computes `self.pow(exp)`,"] # [doc = " wrapping around at the boundary of the type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(3" , stringify ! ($ SelfT) , ".wrapping_pow(5), 243);")] # [doc = " assert_eq!(3u8.wrapping_pow(6), 217);"] # [doc = " ```"] # [stable (feature = "no_panic_pow" , since = "1.34.0")] # [rustc_const_stable (feature = "const_int_pow" , since = "1.50.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn wrapping_pow (self , mut exp : u32) -> Self { if exp == 0 { return 1 ; } let mut base = self ; let mut acc : Self = 1 ; if intrinsics :: is_val_statically_known (exp) { while exp > 1 { if (exp & 1) == 1 { acc = acc . wrapping_mul (base) ; } exp /= 2 ; base = base . wrapping_mul (base) ; } acc . wrapping_mul (base) } else { loop { if (exp & 1) == 1 { acc = acc . wrapping_mul (base) ; if exp == 1 { return acc ; } } exp /= 2 ; base = base . wrapping_mul (base) ; } } } # [doc = " Calculates `self` + `rhs`."] # [doc = ""] # [doc = " Returns a tuple of the addition along with a boolean indicating"] # [doc = " whether an arithmetic overflow would occur. If an overflow would"] # [doc = " have occurred then the wrapped value is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".overflowing_add(2), (7, false));")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.overflowing_add(1), (0, true));")] # [doc = " ```"] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_wrapping_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn overflowing_add (self , rhs : Self) -> (Self , bool) { let (a , b) = intrinsics :: add_with_overflow (self as $ ActualT , rhs as $ ActualT) ; (a as Self , b) } # [doc = " Calculates `self` + `rhs` + `carry` and returns a tuple containing"] # [doc = " the sum and the output carry (in that order)."] # [doc = ""] # [doc = " Performs \"ternary addition\" of two integer operands and a carry-in"] # [doc = " bit, and returns an output integer and a carry-out bit. This allows"] # [doc = " chaining together multiple additions to create a wider addition, and"] # [doc = " can be useful for bignum addition."] # [doc = ""] # [doc = concat ! ("This can be thought of as a " , stringify ! ($ BITS) , "-bit \"full adder\", in the electronics sense.")] # [doc = ""] # [doc = " If the input carry is false, this method is equivalent to"] # [doc = " [`overflowing_add`](Self::overflowing_add), and the output carry is"] # [doc = " equal to the overflow flag. Note that although carry and overflow"] # [doc = " flags are similar for unsigned integers, they are different for"] # [doc = " signed integers."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("//    3  MAX    (a = 3 × 2^" , stringify ! ($ BITS) , " + 2^" , stringify ! ($ BITS) , " - 1)")] # [doc = concat ! ("// +  5    7    (b = 5 × 2^" , stringify ! ($ BITS) , " + 7)")] # [doc = " // ---------"] # [doc = concat ! ("//    9    6    (sum = 9 × 2^" , stringify ! ($ BITS) , " + 6)")] # [doc = ""] # [doc = concat ! ("let (a1, a0): (" , stringify ! ($ SelfT) , ", " , stringify ! ($ SelfT) , ") = (3, " , stringify ! ($ SelfT) , "::MAX);")] # [doc = concat ! ("let (b1, b0): (" , stringify ! ($ SelfT) , ", " , stringify ! ($ SelfT) , ") = (5, 7);")] # [doc = " let carry0 = false;"] # [doc = ""] # [doc = " let (sum0, carry1) = a0.carrying_add(b0, carry0);"] # [doc = " assert_eq!(carry1, true);"] # [doc = " let (sum1, carry2) = a1.carrying_add(b1, carry1);"] # [doc = " assert_eq!(carry2, false);"] # [doc = ""] # [doc = " assert_eq!((sum1, sum0), (9, 6));"] # [doc = " ```"] # [stable (feature = "unsigned_bigint_helpers" , since = "1.91.0")] # [rustc_const_unstable (feature = "bigint_helper_methods" , issue = "85532")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn carrying_add (self , rhs : Self , carry : bool) -> (Self , bool) { let (a , c1) = self . overflowing_add (rhs) ; let (b , c2) = a . overflowing_add (carry as $ SelfT) ; (b , unsafe { intrinsics :: disjoint_bitor (c1 , c2) }) } # [doc = " Calculates `self` + `rhs` with a signed `rhs`."] # [doc = ""] # [doc = " Returns a tuple of the addition along with a boolean indicating"] # [doc = " whether an arithmetic overflow would occur. If an overflow would"] # [doc = " have occurred then the wrapped value is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".overflowing_add_signed(2), (3, false));")] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".overflowing_add_signed(-2), (" , stringify ! ($ SelfT) , "::MAX, true));")] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SelfT) , "::MAX - 2).overflowing_add_signed(4), (1, true));")] # [doc = " ```"] # [stable (feature = "mixed_integer_ops" , since = "1.66.0")] # [rustc_const_stable (feature = "mixed_integer_ops" , since = "1.66.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn overflowing_add_signed (self , rhs : $ SignedT) -> (Self , bool) { let (res , overflowed) = self . overflowing_add (rhs as Self) ; (res , overflowed ^ (rhs < 0)) } # [doc = " Calculates `self` - `rhs`."] # [doc = ""] # [doc = " Returns a tuple of the subtraction along with a boolean indicating"] # [doc = " whether an arithmetic overflow would occur. If an overflow would"] # [doc = " have occurred then the wrapped value is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".overflowing_sub(2), (3, false));")] # [doc = concat ! ("assert_eq!(0" , stringify ! ($ SelfT) , ".overflowing_sub(1), (" , stringify ! ($ SelfT) , "::MAX, true));")] # [doc = " ```"] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_wrapping_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn overflowing_sub (self , rhs : Self) -> (Self , bool) { let (a , b) = intrinsics :: sub_with_overflow (self as $ ActualT , rhs as $ ActualT) ; (a as Self , b) } # [doc = " Calculates `self` &minus; `rhs` &minus; `borrow` and returns a tuple"] # [doc = " containing the difference and the output borrow."] # [doc = ""] # [doc = " Performs \"ternary subtraction\" by subtracting both an integer"] # [doc = " operand and a borrow-in bit from `self`, and returns an output"] # [doc = " integer and a borrow-out bit. This allows chaining together multiple"] # [doc = " subtractions to create a wider subtraction, and can be useful for"] # [doc = " bignum subtraction."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("//    9    6    (a = 9 × 2^" , stringify ! ($ BITS) , " + 6)")] # [doc = concat ! ("// -  5    7    (b = 5 × 2^" , stringify ! ($ BITS) , " + 7)")] # [doc = " // ---------"] # [doc = concat ! ("//    3  MAX    (diff = 3 × 2^" , stringify ! ($ BITS) , " + 2^" , stringify ! ($ BITS) , " - 1)")] # [doc = ""] # [doc = concat ! ("let (a1, a0): (" , stringify ! ($ SelfT) , ", " , stringify ! ($ SelfT) , ") = (9, 6);")] # [doc = concat ! ("let (b1, b0): (" , stringify ! ($ SelfT) , ", " , stringify ! ($ SelfT) , ") = (5, 7);")] # [doc = " let borrow0 = false;"] # [doc = ""] # [doc = " let (diff0, borrow1) = a0.borrowing_sub(b0, borrow0);"] # [doc = " assert_eq!(borrow1, true);"] # [doc = " let (diff1, borrow2) = a1.borrowing_sub(b1, borrow1);"] # [doc = " assert_eq!(borrow2, false);"] # [doc = ""] # [doc = concat ! ("assert_eq!((diff1, diff0), (3, " , stringify ! ($ SelfT) , "::MAX));")] # [doc = " ```"] # [stable (feature = "unsigned_bigint_helpers" , since = "1.91.0")] # [rustc_const_unstable (feature = "bigint_helper_methods" , issue = "85532")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn borrowing_sub (self , rhs : Self , borrow : bool) -> (Self , bool) { let (a , c1) = self . overflowing_sub (rhs) ; let (b , c2) = a . overflowing_sub (borrow as $ SelfT) ; (b , unsafe { intrinsics :: disjoint_bitor (c1 , c2) }) } # [doc = " Calculates `self` - `rhs` with a signed `rhs`"] # [doc = ""] # [doc = " Returns a tuple of the subtraction along with a boolean indicating"] # [doc = " whether an arithmetic overflow would occur. If an overflow would"] # [doc = " have occurred then the wrapped value is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".overflowing_sub_signed(2), (" , stringify ! ($ SelfT) , "::MAX, true));")] # [doc = concat ! ("assert_eq!(1" , stringify ! ($ SelfT) , ".overflowing_sub_signed(-2), (3, false));")] # [doc = concat ! ("assert_eq!((" , stringify ! ($ SelfT) , "::MAX - 2).overflowing_sub_signed(-4), (1, true));")] # [doc = " ```"] # [stable (feature = "mixed_integer_ops_unsigned_sub" , since = "1.90.0")] # [rustc_const_stable (feature = "mixed_integer_ops_unsigned_sub" , since = "1.90.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn overflowing_sub_signed (self , rhs : $ SignedT) -> (Self , bool) { let (res , overflow) = self . overflowing_sub (rhs as Self) ; (res , overflow ^ (rhs < 0)) } # [doc = " Computes the absolute difference between `self` and `other`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".abs_diff(80), 20" , stringify ! ($ SelfT) , ");")] # [doc = concat ! ("assert_eq!(100" , stringify ! ($ SelfT) , ".abs_diff(110), 10" , stringify ! ($ SelfT) , ");")] # [doc = " ```"] # [stable (feature = "int_abs_diff" , since = "1.60.0")] # [rustc_const_stable (feature = "int_abs_diff" , since = "1.60.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn abs_diff (self , other : Self) -> Self { if size_of ::< Self > () == 1 { (self as i32) . wrapping_sub (other as i32) . unsigned_abs () as Self } else { if self < other { other - self } else { self - other } } } # [doc = " Calculates the multiplication of `self` and `rhs`."] # [doc = ""] # [doc = " Returns a tuple of the multiplication along with a boolean"] # [doc = " indicating whether an arithmetic overflow would occur. If an"] # [doc = " overflow would have occurred then the wrapped value is returned."] # [doc = ""] # [doc = " If you want the *value* of the overflow, rather than just *whether*"] # [doc = " an overflow occurred, see [`Self::carrying_mul`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Please note that this example is shared among integer types, which is why `u32` is used."] # [doc = ""] # [doc = " ```"] # [doc = " assert_eq!(5u32.overflowing_mul(2), (10, false));"] # [doc = " assert_eq!(1_000_000_000u32.overflowing_mul(10), (1410065408, true));"] # [doc = " ```"] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_wrapping_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                          without modifying the original"] # [inline (always)] pub const fn overflowing_mul (self , rhs : Self) -> (Self , bool) { let (a , b) = intrinsics :: mul_with_overflow (self as $ ActualT , rhs as $ ActualT) ; (a as Self , b) } # [doc = " Calculates the complete double-width product `self * rhs`."] # [doc = ""] # [doc = " This returns the low-order (wrapping) bits and the high-order (overflow) bits"] # [doc = " of the result as two separate values, in that order. As such,"] # [doc = " `a.widening_mul(b).0` produces the same result as `a.wrapping_mul(b)`."] # [doc = ""] # [doc = " If you also need to add a value and carry to the wide result, then you want"] # [doc = " [`Self::carrying_mul_add`] instead."] # [doc = ""] # [doc = " If you also need to add a carry to the wide result, then you want"] # [doc = " [`Self::carrying_mul`] instead."] # [doc = ""] # [doc = " If you just want to know *whether* the multiplication overflowed, then you"] # [doc = " want [`Self::overflowing_mul`] instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(bigint_helper_methods)]"] # [doc = concat ! ("assert_eq!(5_" , stringify ! ($ SelfT) , ".widening_mul(7), (35, 0));")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.widening_mul(" , stringify ! ($ SelfT) , "::MAX), (1, " , stringify ! ($ SelfT) , "::MAX - 1));")] # [doc = " ```"] # [doc = ""] # [doc = " Compared to other `*_mul` methods:"] # [doc = " ```"] # [doc = " #![feature(bigint_helper_methods)]"] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::widening_mul(1 << " , stringify ! ($ BITS_MINUS_ONE) , ", 6), (0, 3));")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::overflowing_mul(1 << " , stringify ! ($ BITS_MINUS_ONE) , ", 6), (0, true));")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::wrapping_mul(1 << " , stringify ! ($ BITS_MINUS_ONE) , ", 6), 0);")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::checked_mul(1 << " , stringify ! ($ BITS_MINUS_ONE) , ", 6), None);")] # [doc = " ```"] # [doc = ""] # [doc = " Please note that this example is shared among integer types, which is why `u32` is used."] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(bigint_helper_methods)]"] # [doc = " assert_eq!(5u32.widening_mul(2), (10, 0));"] # [doc = " assert_eq!(1_000_000_000u32.widening_mul(10), (1410065408, 2));"] # [doc = " ```"] # [unstable (feature = "bigint_helper_methods" , issue = "85532")] # [rustc_const_unstable (feature = "bigint_helper_methods" , issue = "85532")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn widening_mul (self , rhs : Self) -> (Self , Self) { Self :: carrying_mul_add (self , rhs , 0 , 0) } # [doc = " Calculates the \"full multiplication\" `self * rhs + carry`"] # [doc = " without the possibility to overflow."] # [doc = ""] # [doc = " This returns the low-order (wrapping) bits and the high-order (overflow) bits"] # [doc = " of the result as two separate values, in that order."] # [doc = ""] # [doc = " Performs \"long multiplication\" which takes in an extra amount to add, and may return an"] # [doc = " additional amount of overflow. This allows for chaining together multiple"] # [doc = " multiplications to create \"big integers\" which represent larger values."] # [doc = ""] # [doc = " If you also need to add a value, then use [`Self::carrying_mul_add`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Please note that this example is shared among integer types, which is why `u32` is used."] # [doc = ""] # [doc = " ```"] # [doc = " assert_eq!(5u32.carrying_mul(2, 0), (10, 0));"] # [doc = " assert_eq!(5u32.carrying_mul(2, 10), (20, 0));"] # [doc = " assert_eq!(1_000_000_000u32.carrying_mul(10, 0), (1410065408, 2));"] # [doc = " assert_eq!(1_000_000_000u32.carrying_mul(10, 10), (1410065418, 2));"] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.carrying_mul(" , stringify ! ($ SelfT) , "::MAX, " , stringify ! ($ SelfT) , "::MAX), " , "(0, " , stringify ! ($ SelfT) , "::MAX));")] # [doc = " ```"] # [doc = ""] # [doc = " This is the core operation needed for scalar multiplication when"] # [doc = " implementing it for wider-than-native types."] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(bigint_helper_methods)]"] # [doc = " fn scalar_mul_eq(little_endian_digits: &mut Vec<u16>, multiplicand: u16) {"] # [doc = "     let mut carry = 0;"] # [doc = "     for d in little_endian_digits.iter_mut() {"] # [doc = "         (*d, carry) = d.carrying_mul(multiplicand, carry);"] # [doc = "     }"] # [doc = "     if carry != 0 {"] # [doc = "         little_endian_digits.push(carry);"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " let mut v = vec![10, 20];"] # [doc = " scalar_mul_eq(&mut v, 3);"] # [doc = " assert_eq!(v, [30, 60]);"] # [doc = ""] # [doc = " assert_eq!(0x87654321_u64 * 0xFEED, 0x86D3D159E38D);"] # [doc = " let mut v = vec![0x4321, 0x8765];"] # [doc = " scalar_mul_eq(&mut v, 0xFEED);"] # [doc = " assert_eq!(v, [0xE38D, 0xD159, 0x86D3]);"] # [doc = " ```"] # [doc = ""] # [doc = " If `carry` is zero, this is similar to [`overflowing_mul`](Self::overflowing_mul),"] # [doc = " except that it gives the value of the overflow instead of just whether one happened:"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(bigint_helper_methods)]"] # [doc = " let r = u8::carrying_mul(7, 13, 0);"] # [doc = " assert_eq!((r.0, r.1 != 0), u8::overflowing_mul(7, 13));"] # [doc = " let r = u8::carrying_mul(13, 42, 0);"] # [doc = " assert_eq!((r.0, r.1 != 0), u8::overflowing_mul(13, 42));"] # [doc = " ```"] # [doc = ""] # [doc = " The value of the first field in the returned tuple matches what you'd get"] # [doc = " by combining the [`wrapping_mul`](Self::wrapping_mul) and"] # [doc = " [`wrapping_add`](Self::wrapping_add) methods:"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(bigint_helper_methods)]"] # [doc = " assert_eq!("] # [doc = "     789_u16.carrying_mul(456, 123).0,"] # [doc = "     789_u16.wrapping_mul(456).wrapping_add(123),"] # [doc = " );"] # [doc = " ```"] # [stable (feature = "unsigned_bigint_helpers" , since = "1.91.0")] # [rustc_const_unstable (feature = "bigint_helper_methods" , issue = "85532")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn carrying_mul (self , rhs : Self , carry : Self) -> (Self , Self) { Self :: carrying_mul_add (self , rhs , carry , 0) } # [doc = " Calculates the \"full multiplication\" `self * rhs + carry1 + carry2`."] # [doc = ""] # [doc = " This returns the low-order (wrapping) bits and the high-order (overflow) bits"] # [doc = " of the result as two separate values, in that order."] # [doc = ""] # [doc = " This cannot overflow, as the double-width result has exactly enough"] # [doc = " space for the largest possible result. This is equivalent to how, in"] # [doc = " decimal, 9 × 9 + 9 + 9 = 81 + 18 = 99 = 9×10⁰ + 9×10¹ = 10² - 1."] # [doc = ""] # [doc = " Performs \"long multiplication\" which takes in an extra amount to add, and may return an"] # [doc = " additional amount of overflow. This allows for chaining together multiple"] # [doc = " multiplications to create \"big integers\" which represent larger values."] # [doc = ""] # [doc = " If you don't need the `add` part, then you can use [`Self::carrying_mul`] instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Please note that this example is shared between integer types,"] # [doc = " which explains why `u32` is used here."] # [doc = ""] # [doc = " ```"] # [doc = " assert_eq!(5u32.carrying_mul_add(2, 0, 0), (10, 0));"] # [doc = " assert_eq!(5u32.carrying_mul_add(2, 10, 10), (30, 0));"] # [doc = " assert_eq!(1_000_000_000u32.carrying_mul_add(10, 0, 0), (1410065408, 2));"] # [doc = " assert_eq!(1_000_000_000u32.carrying_mul_add(10, 10, 10), (1410065428, 2));"] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.carrying_mul_add(" , stringify ! ($ SelfT) , "::MAX, " , stringify ! ($ SelfT) , "::MAX, " , stringify ! ($ SelfT) , "::MAX), " , "(" , stringify ! ($ SelfT) , "::MAX, " , stringify ! ($ SelfT) , "::MAX));")] # [doc = " ```"] # [doc = ""] # [doc = " This is the core per-digit operation for \"grade school\" O(n²) multiplication."] # [doc = ""] # [doc = " Please note that this example is shared between integer types,"] # [doc = " using `u8` for simplicity of the demonstration."] # [doc = ""] # [doc = " ```"] # [doc = " fn quadratic_mul<const N: usize>(a: [u8; N], b: [u8; N]) -> [u8; N] {"] # [doc = "     let mut out = [0; N];"] # [doc = "     for j in 0..N {"] # [doc = "         let mut carry = 0;"] # [doc = "         for i in 0..(N - j) {"] # [doc = "             (out[j + i], carry) = u8::carrying_mul_add(a[i], b[j], out[j + i], carry);"] # [doc = "         }"] # [doc = "     }"] # [doc = "     out"] # [doc = " }"] # [doc = ""] # [doc = " // -1 * -1 == 1"] # [doc = " assert_eq!(quadratic_mul([0xFF; 3], [0xFF; 3]), [1, 0, 0]);"] # [doc = ""] # [doc = " assert_eq!(u32::wrapping_mul(0x9e3779b9, 0x7f4a7c15), 0xcffc982d);"] # [doc = " assert_eq!("] # [doc = "     quadratic_mul(u32::to_le_bytes(0x9e3779b9), u32::to_le_bytes(0x7f4a7c15)),"] # [doc = "     u32::to_le_bytes(0xcffc982d)"] # [doc = " );"] # [doc = " ```"] # [stable (feature = "unsigned_bigint_helpers" , since = "1.91.0")] # [rustc_const_unstable (feature = "bigint_helper_methods" , issue = "85532")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn carrying_mul_add (self , rhs : Self , carry : Self , add : Self) -> (Self , Self) { intrinsics :: carrying_mul_add (self , rhs , carry , add) } # [doc = " Calculates the divisor when `self` is divided by `rhs`."] # [doc = ""] # [doc = " Returns a tuple of the divisor along with a boolean indicating"] # [doc = " whether an arithmetic overflow would occur. Note that for unsigned"] # [doc = " integers overflow never occurs, so the second value is always"] # [doc = " `false`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".overflowing_div(2), (2, false));")] # [doc = " ```"] # [inline (always)] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_overflowing_int_methods" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [track_caller] pub const fn overflowing_div (self , rhs : Self) -> (Self , bool) { (self / rhs , false) } # [doc = " Calculates the quotient of Euclidean division `self.div_euclid(rhs)`."] # [doc = ""] # [doc = " Returns a tuple of the divisor along with a boolean indicating"] # [doc = " whether an arithmetic overflow would occur. Note that for unsigned"] # [doc = " integers overflow never occurs, so the second value is always"] # [doc = " `false`."] # [doc = " Since, for the positive integers, all common"] # [doc = " definitions of division are equal, this"] # [doc = " is exactly equal to `self.overflowing_div(rhs)`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".overflowing_div_euclid(2), (2, false));")] # [doc = " ```"] # [inline (always)] # [stable (feature = "euclidean_division" , since = "1.38.0")] # [rustc_const_stable (feature = "const_euclidean_int_methods" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [track_caller] pub const fn overflowing_div_euclid (self , rhs : Self) -> (Self , bool) { (self / rhs , false) } # [doc = " Calculates the remainder when `self` is divided by `rhs`."] # [doc = ""] # [doc = " Returns a tuple of the remainder after dividing along with a boolean"] # [doc = " indicating whether an arithmetic overflow would occur. Note that for"] # [doc = " unsigned integers overflow never occurs, so the second value is"] # [doc = " always `false`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".overflowing_rem(2), (1, false));")] # [doc = " ```"] # [inline (always)] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_overflowing_int_methods" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [track_caller] pub const fn overflowing_rem (self , rhs : Self) -> (Self , bool) { (self % rhs , false) } # [doc = " Calculates the remainder `self.rem_euclid(rhs)` as if by Euclidean division."] # [doc = ""] # [doc = " Returns a tuple of the modulo after dividing along with a boolean"] # [doc = " indicating whether an arithmetic overflow would occur. Note that for"] # [doc = " unsigned integers overflow never occurs, so the second value is"] # [doc = " always `false`."] # [doc = " Since, for the positive integers, all common"] # [doc = " definitions of division are equal, this operation"] # [doc = " is exactly equal to `self.overflowing_rem(rhs)`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(5" , stringify ! ($ SelfT) , ".overflowing_rem_euclid(2), (1, false));")] # [doc = " ```"] # [inline (always)] # [stable (feature = "euclidean_division" , since = "1.38.0")] # [rustc_const_stable (feature = "const_euclidean_int_methods" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [track_caller] pub const fn overflowing_rem_euclid (self , rhs : Self) -> (Self , bool) { (self % rhs , false) } # [doc = " Negates self in an overflowing fashion."] # [doc = ""] # [doc = " Returns `!self + 1` using wrapping operations to return the value"] # [doc = " that represents the negation of this unsigned value. Note that for"] # [doc = " positive unsigned values overflow always occurs, but negating 0 does"] # [doc = " not overflow."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(0" , stringify ! ($ SelfT) , ".overflowing_neg(), (0, false));")] # [doc = concat ! ("assert_eq!(2" , stringify ! ($ SelfT) , ".overflowing_neg(), (-2i32 as " , stringify ! ($ SelfT) , ", true));")] # [doc = " ```"] # [inline (always)] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_wrapping_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] pub const fn overflowing_neg (self) -> (Self , bool) { ((! self) . wrapping_add (1) , self != 0) } # [doc = " Shifts self left by `rhs` bits."] # [doc = ""] # [doc = " Returns a tuple of the shifted version of self along with a boolean"] # [doc = " indicating whether the shift value was larger than or equal to the"] # [doc = " number of bits. If the shift value is too large, then value is"] # [doc = " masked (N-1) where N is the number of bits, and this value is then"] # [doc = " used to perform the shift."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(0x1" , stringify ! ($ SelfT) , ".overflowing_shl(4), (0x10, false));")] # [doc = concat ! ("assert_eq!(0x1" , stringify ! ($ SelfT) , ".overflowing_shl(132), (0x10, true));")] # [doc = concat ! ("assert_eq!(0x10" , stringify ! ($ SelfT) , ".overflowing_shl(" , stringify ! ($ BITS_MINUS_ONE) , "), (0, false));")] # [doc = " ```"] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_wrapping_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn overflowing_shl (self , rhs : u32) -> (Self , bool) { (self . wrapping_shl (rhs) , rhs >= Self :: BITS) } # [doc = " Shifts self right by `rhs` bits."] # [doc = ""] # [doc = " Returns a tuple of the shifted version of self along with a boolean"] # [doc = " indicating whether the shift value was larger than or equal to the"] # [doc = " number of bits. If the shift value is too large, then value is"] # [doc = " masked (N-1) where N is the number of bits, and this value is then"] # [doc = " used to perform the shift."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(0x10" , stringify ! ($ SelfT) , ".overflowing_shr(4), (0x1, false));")] # [doc = concat ! ("assert_eq!(0x10" , stringify ! ($ SelfT) , ".overflowing_shr(132), (0x1, true));")] # [doc = " ```"] # [stable (feature = "wrapping" , since = "1.7.0")] # [rustc_const_stable (feature = "const_wrapping_math" , since = "1.32.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] pub const fn overflowing_shr (self , rhs : u32) -> (Self , bool) { (self . wrapping_shr (rhs) , rhs >= Self :: BITS) } # [doc = " Raises self to the power of `exp`, using exponentiation by squaring."] # [doc = ""] # [doc = " Returns a tuple of the exponentiation along with a bool indicating"] # [doc = " whether an overflow happened."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(3" , stringify ! ($ SelfT) , ".overflowing_pow(5), (243, false));")] # [doc = " assert_eq!(3u8.overflowing_pow(6), (217, true));"] # [doc = " ```"] # [stable (feature = "no_panic_pow" , since = "1.34.0")] # [rustc_const_stable (feature = "const_int_pow" , since = "1.50.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn overflowing_pow (self , mut exp : u32) -> (Self , bool) { if exp == 0 { return (1 , false) ; } let mut base = self ; let mut acc : Self = 1 ; let mut overflown = false ; let mut r ; loop { if (exp & 1) == 1 { r = acc . overflowing_mul (base) ; if exp == 1 { r . 1 |= overflown ; return r ; } acc = r . 0 ; overflown |= r . 1 ; } exp /= 2 ; r = base . overflowing_mul (base) ; base = r . 0 ; overflown |= r . 1 ; } } # [doc = " Raises self to the power of `exp`, using exponentiation by squaring."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(2" , stringify ! ($ SelfT) , ".pow(5), 32);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_int_pow" , since = "1.50.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [rustc_inherit_overflow_checks] pub const fn pow (self , mut exp : u32) -> Self { if exp == 0 { return 1 ; } let mut base = self ; let mut acc = 1 ; if intrinsics :: is_val_statically_known (exp) { while exp > 1 { if (exp & 1) == 1 { acc = acc * base ; } exp /= 2 ; base = base * base ; } acc * base } else { loop { if (exp & 1) == 1 { acc = acc * base ; if exp == 1 { return acc ; } } exp /= 2 ; base = base * base ; } } } # [doc = " Returns the square root of the number, rounded down."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(10" , stringify ! ($ SelfT) , ".isqrt(), 3);")] # [doc = " ```"] # [stable (feature = "isqrt" , since = "1.84.0")] # [rustc_const_stable (feature = "isqrt" , since = "1.84.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn isqrt (self) -> Self { let result = crate :: num :: int_sqrt ::$ ActualT (self as $ ActualT) as $ SelfT ; unsafe { const MAX_RESULT : $ SelfT = crate :: num :: int_sqrt ::$ ActualT (<$ ActualT >:: MAX) as $ SelfT ; crate :: hint :: assert_unchecked (result <= MAX_RESULT) ; } result } # [doc = " Performs Euclidean division."] # [doc = ""] # [doc = " Since, for the positive integers, all common"] # [doc = " definitions of division are equal, this"] # [doc = " is exactly equal to `self / rhs`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(7" , stringify ! ($ SelfT) , ".div_euclid(4), 1); // or any other integer type")] # [doc = " ```"] # [stable (feature = "euclidean_division" , since = "1.38.0")] # [rustc_const_stable (feature = "const_euclidean_int_methods" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const fn div_euclid (self , rhs : Self) -> Self { self / rhs } # [doc = " Calculates the least remainder of `self (mod rhs)`."] # [doc = ""] # [doc = " Since, for the positive integers, all common"] # [doc = " definitions of division are equal, this"] # [doc = " is exactly equal to `self % rhs`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(7" , stringify ! ($ SelfT) , ".rem_euclid(4), 3); // or any other integer type")] # [doc = " ```"] # [doc (alias = "modulo" , alias = "mod")] # [stable (feature = "euclidean_division" , since = "1.38.0")] # [rustc_const_stable (feature = "const_euclidean_int_methods" , since = "1.52.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const fn rem_euclid (self , rhs : Self) -> Self { self % rhs } # [doc = " Calculates the quotient of `self` and `rhs`, rounding the result towards negative infinity."] # [doc = ""] # [doc = " This is the same as performing `self / rhs` for all unsigned integers."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(int_roundings)]"] # [doc = concat ! ("assert_eq!(7_" , stringify ! ($ SelfT) , ".div_floor(4), 1);")] # [doc = " ```"] # [unstable (feature = "int_roundings" , issue = "88581")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline (always)] # [track_caller] pub const fn div_floor (self , rhs : Self) -> Self { self / rhs } # [doc = " Calculates the quotient of `self` and `rhs`, rounding the result towards positive infinity."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(7_" , stringify ! ($ SelfT) , ".div_ceil(4), 2);")] # [doc = " ```"] # [stable (feature = "int_roundings1" , since = "1.73.0")] # [rustc_const_stable (feature = "int_roundings1" , since = "1.73.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [track_caller] pub const fn div_ceil (self , rhs : Self) -> Self { let d = self / rhs ; let r = self % rhs ; if r > 0 { d + 1 } else { d } } # [doc = " Calculates the smallest value greater than or equal to `self` that"] # [doc = " is a multiple of `rhs`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if `rhs` is zero."] # [doc = ""] # [doc = " ## Overflow behavior"] # [doc = ""] # [doc = " On overflow, this function will panic if overflow checks are enabled (default in debug"] # [doc = " mode) and wrap if overflow checks are disabled (default in release mode)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(16_" , stringify ! ($ SelfT) , ".next_multiple_of(8), 16);")] # [doc = concat ! ("assert_eq!(23_" , stringify ! ($ SelfT) , ".next_multiple_of(8), 24);")] # [doc = " ```"] # [stable (feature = "int_roundings1" , since = "1.73.0")] # [rustc_const_stable (feature = "int_roundings1" , since = "1.73.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [rustc_inherit_overflow_checks] pub const fn next_multiple_of (self , rhs : Self) -> Self { match self % rhs { 0 => self , r => self + (rhs - r) } } # [doc = " Calculates the smallest value greater than or equal to `self` that"] # [doc = " is a multiple of `rhs`. Returns `None` if `rhs` is zero or the"] # [doc = " operation would result in overflow."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(16_" , stringify ! ($ SelfT) , ".checked_next_multiple_of(8), Some(16));")] # [doc = concat ! ("assert_eq!(23_" , stringify ! ($ SelfT) , ".checked_next_multiple_of(8), Some(24));")] # [doc = concat ! ("assert_eq!(1_" , stringify ! ($ SelfT) , ".checked_next_multiple_of(0), None);")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.checked_next_multiple_of(2), None);")] # [doc = " ```"] # [stable (feature = "int_roundings1" , since = "1.73.0")] # [rustc_const_stable (feature = "int_roundings1" , since = "1.73.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn checked_next_multiple_of (self , rhs : Self) -> Option < Self > { match try_opt ! (self . checked_rem (rhs)) { 0 => Some (self) , r => self . checked_add (rhs - r) } } # [doc = " Returns `true` if `self` is an integer multiple of `rhs`, and false otherwise."] # [doc = ""] # [doc = " This function is equivalent to `self % rhs == 0`, except that it will not panic"] # [doc = " for `rhs == 0`. Instead, `0.is_multiple_of(0) == true`, and for any non-zero `n`,"] # [doc = " `n.is_multiple_of(0) == false`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert!(6_" , stringify ! ($ SelfT) , ".is_multiple_of(2));")] # [doc = concat ! ("assert!(!5_" , stringify ! ($ SelfT) , ".is_multiple_of(2));")] # [doc = ""] # [doc = concat ! ("assert!(0_" , stringify ! ($ SelfT) , ".is_multiple_of(0));")] # [doc = concat ! ("assert!(!6_" , stringify ! ($ SelfT) , ".is_multiple_of(0));")] # [doc = " ```"] # [stable (feature = "unsigned_is_multiple_of" , since = "1.87.0")] # [rustc_const_stable (feature = "unsigned_is_multiple_of" , since = "1.87.0")] # [must_use] # [inline] # [rustc_inherit_overflow_checks] pub const fn is_multiple_of (self , rhs : Self) -> bool { match rhs { 0 => self == 0 , _ => self % rhs == 0 , } } # [doc = " Returns `true` if and only if `self == 2^k` for some unsigned integer `k`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert!(16" , stringify ! ($ SelfT) , ".is_power_of_two());")] # [doc = concat ! ("assert!(!10" , stringify ! ($ SelfT) , ".is_power_of_two());")] # [doc = " ```"] # [must_use] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_is_power_of_two" , since = "1.32.0")] # [inline (always)] pub const fn is_power_of_two (self) -> bool { self . count_ones () == 1 } # [inline] const fn one_less_than_next_power_of_two (self) -> Self { if self <= 1 { return 0 ; } let p = self - 1 ; let z = unsafe { intrinsics :: ctlz_nonzero (p) } ; <$ SelfT >:: MAX >> z } # [doc = " Returns the smallest power of two greater than or equal to `self`."] # [doc = ""] # [doc = " When return value overflows (i.e., `self > (1 << (N-1))` for type"] # [doc = " `uN`), it panics in debug mode and the return value is wrapped to 0 in"] # [doc = " release mode (the only situation in which this method can return 0)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(2" , stringify ! ($ SelfT) , ".next_power_of_two(), 2);")] # [doc = concat ! ("assert_eq!(3" , stringify ! ($ SelfT) , ".next_power_of_two(), 4);")] # [doc = concat ! ("assert_eq!(0" , stringify ! ($ SelfT) , ".next_power_of_two(), 1);")] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_int_pow" , since = "1.50.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] # [rustc_inherit_overflow_checks] pub const fn next_power_of_two (self) -> Self { self . one_less_than_next_power_of_two () + 1 } # [doc = " Returns the smallest power of two greater than or equal to `self`. If"] # [doc = " the next power of two is greater than the type's maximum value,"] # [doc = " `None` is returned, otherwise the power of two is wrapped in `Some`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("assert_eq!(2" , stringify ! ($ SelfT) , ".checked_next_power_of_two(), Some(2));")] # [doc = concat ! ("assert_eq!(3" , stringify ! ($ SelfT) , ".checked_next_power_of_two(), Some(4));")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.checked_next_power_of_two(), None);")] # [doc = " ```"] # [inline] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_int_pow" , since = "1.50.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] pub const fn checked_next_power_of_two (self) -> Option < Self > { self . one_less_than_next_power_of_two () . checked_add (1) } # [doc = " Returns the smallest power of two greater than or equal to `n`. If"] # [doc = " the next power of two is greater than the type's maximum value,"] # [doc = " the return value is wrapped to `0`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(wrapping_next_power_of_two)]"] # [doc = ""] # [doc = concat ! ("assert_eq!(2" , stringify ! ($ SelfT) , ".wrapping_next_power_of_two(), 2);")] # [doc = concat ! ("assert_eq!(3" , stringify ! ($ SelfT) , ".wrapping_next_power_of_two(), 4);")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ SelfT) , "::MAX.wrapping_next_power_of_two(), 0);")] # [doc = " ```"] # [inline] # [unstable (feature = "wrapping_next_power_of_two" , issue = "32463" , reason = "needs decision on wrapping behavior")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] pub const fn wrapping_next_power_of_two (self) -> Self { self . one_less_than_next_power_of_two () . wrapping_add (1) } # [doc = " Returns the memory representation of this integer as a byte array in"] # [doc = " big-endian (network) byte order."] # [doc = ""] # [doc = $ to_xe_bytes_doc] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let bytes = " , $ swap_op , stringify ! ($ SelfT) , ".to_be_bytes();")] # [doc = concat ! ("assert_eq!(bytes, " , $ be_bytes , ");")] # [doc = " ```"] # [stable (feature = "int_to_from_bytes" , since = "1.32.0")] # [rustc_const_stable (feature = "const_int_conversion" , since = "1.44.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn to_be_bytes (self) -> [u8 ; size_of ::< Self > ()] { self . to_be () . to_ne_bytes () } # [doc = " Returns the memory representation of this integer as a byte array in"] # [doc = " little-endian byte order."] # [doc = ""] # [doc = $ to_xe_bytes_doc] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let bytes = " , $ swap_op , stringify ! ($ SelfT) , ".to_le_bytes();")] # [doc = concat ! ("assert_eq!(bytes, " , $ le_bytes , ");")] # [doc = " ```"] # [stable (feature = "int_to_from_bytes" , since = "1.32.0")] # [rustc_const_stable (feature = "const_int_conversion" , since = "1.44.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [inline] pub const fn to_le_bytes (self) -> [u8 ; size_of ::< Self > ()] { self . to_le () . to_ne_bytes () } # [doc = " Returns the memory representation of this integer as a byte array in"] # [doc = " native byte order."] # [doc = ""] # [doc = " As the target platform's native endianness is used, portable code"] # [doc = " should use [`to_be_bytes`] or [`to_le_bytes`], as appropriate,"] # [doc = " instead."] # [doc = ""] # [doc = $ to_xe_bytes_doc] # [doc = ""] # [doc = " [`to_be_bytes`]: Self::to_be_bytes"] # [doc = " [`to_le_bytes`]: Self::to_le_bytes"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let bytes = " , $ swap_op , stringify ! ($ SelfT) , ".to_ne_bytes();")] # [doc = " assert_eq!("] # [doc = "     bytes,"] # [doc = "     if cfg!(target_endian = \"big\") {"] # [doc = concat ! ("        " , $ be_bytes)] # [doc = "     } else {"] # [doc = concat ! ("        " , $ le_bytes)] # [doc = "     }"] # [doc = " );"] # [doc = " ```"] # [stable (feature = "int_to_from_bytes" , since = "1.32.0")] # [rustc_const_stable (feature = "const_int_conversion" , since = "1.44.0")] # [must_use = "this returns the result of the operation, \
                      without modifying the original"] # [allow (unnecessary_transmutes)] # [inline] pub const fn to_ne_bytes (self) -> [u8 ; size_of ::< Self > ()] { unsafe { mem :: transmute (self) } } # [doc = " Creates a native endian integer value from its representation"] # [doc = " as a byte array in big endian."] # [doc = ""] # [doc = $ from_xe_bytes_doc] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let value = " , stringify ! ($ SelfT) , "::from_be_bytes(" , $ be_bytes , ");")] # [doc = concat ! ("assert_eq!(value, " , $ swap_op , ");")] # [doc = " ```"] # [doc = ""] # [doc = " When starting from a slice rather than an array, fallible conversion APIs can be used:"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("fn read_be_" , stringify ! ($ SelfT) , "(input: &mut &[u8]) -> " , stringify ! ($ SelfT) , " {")] # [doc = concat ! ("    let (int_bytes, rest) = input.split_at(size_of::<" , stringify ! ($ SelfT) , ">());")] # [doc = "     *input = rest;"] # [doc = concat ! ("    " , stringify ! ($ SelfT) , "::from_be_bytes(int_bytes.try_into().unwrap())")] # [doc = " }"] # [doc = " ```"] # [stable (feature = "int_to_from_bytes" , since = "1.32.0")] # [rustc_const_stable (feature = "const_int_conversion" , since = "1.44.0")] # [must_use] # [inline] pub const fn from_be_bytes (bytes : [u8 ; size_of ::< Self > ()]) -> Self { Self :: from_be (Self :: from_ne_bytes (bytes)) } # [doc = " Creates a native endian integer value from its representation"] # [doc = " as a byte array in little endian."] # [doc = ""] # [doc = $ from_xe_bytes_doc] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let value = " , stringify ! ($ SelfT) , "::from_le_bytes(" , $ le_bytes , ");")] # [doc = concat ! ("assert_eq!(value, " , $ swap_op , ");")] # [doc = " ```"] # [doc = ""] # [doc = " When starting from a slice rather than an array, fallible conversion APIs can be used:"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("fn read_le_" , stringify ! ($ SelfT) , "(input: &mut &[u8]) -> " , stringify ! ($ SelfT) , " {")] # [doc = concat ! ("    let (int_bytes, rest) = input.split_at(size_of::<" , stringify ! ($ SelfT) , ">());")] # [doc = "     *input = rest;"] # [doc = concat ! ("    " , stringify ! ($ SelfT) , "::from_le_bytes(int_bytes.try_into().unwrap())")] # [doc = " }"] # [doc = " ```"] # [stable (feature = "int_to_from_bytes" , since = "1.32.0")] # [rustc_const_stable (feature = "const_int_conversion" , since = "1.44.0")] # [must_use] # [inline] pub const fn from_le_bytes (bytes : [u8 ; size_of ::< Self > ()]) -> Self { Self :: from_le (Self :: from_ne_bytes (bytes)) } # [doc = " Creates a native endian integer value from its memory representation"] # [doc = " as a byte array in native endianness."] # [doc = ""] # [doc = " As the target platform's native endianness is used, portable code"] # [doc = " likely wants to use [`from_be_bytes`] or [`from_le_bytes`], as"] # [doc = " appropriate instead."] # [doc = ""] # [doc = " [`from_be_bytes`]: Self::from_be_bytes"] # [doc = " [`from_le_bytes`]: Self::from_le_bytes"] # [doc = ""] # [doc = $ from_xe_bytes_doc] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("let value = " , stringify ! ($ SelfT) , "::from_ne_bytes(if cfg!(target_endian = \"big\") {")] # [doc = concat ! ("    " , $ be_bytes , "")] # [doc = " } else {"] # [doc = concat ! ("    " , $ le_bytes , "")] # [doc = " });"] # [doc = concat ! ("assert_eq!(value, " , $ swap_op , ");")] # [doc = " ```"] # [doc = ""] # [doc = " When starting from a slice rather than an array, fallible conversion APIs can be used:"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("fn read_ne_" , stringify ! ($ SelfT) , "(input: &mut &[u8]) -> " , stringify ! ($ SelfT) , " {")] # [doc = concat ! ("    let (int_bytes, rest) = input.split_at(size_of::<" , stringify ! ($ SelfT) , ">());")] # [doc = "     *input = rest;"] # [doc = concat ! ("    " , stringify ! ($ SelfT) , "::from_ne_bytes(int_bytes.try_into().unwrap())")] # [doc = " }"] # [doc = " ```"] # [stable (feature = "int_to_from_bytes" , since = "1.32.0")] # [rustc_const_stable (feature = "const_int_conversion" , since = "1.44.0")] # [allow (unnecessary_transmutes)] # [must_use] # [inline] pub const fn from_ne_bytes (bytes : [u8 ; size_of ::< Self > ()]) -> Self { unsafe { mem :: transmute (bytes) } } # [doc = " New code should prefer to use"] # [doc = concat ! ("[`" , stringify ! ($ SelfT) , "::MIN" , "`] instead.")] # [doc = ""] # [doc = " Returns the smallest value that can be represented by this integer type."] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_promotable] # [inline (always)] # [rustc_const_stable (feature = "const_max_value" , since = "1.32.0")] # [deprecated (since = "TBD" , note = "replaced by the `MIN` associated constant on this type")] # [rustc_diagnostic_item = concat ! (stringify ! ($ SelfT) , "_legacy_fn_min_value")] pub const fn min_value () -> Self { Self :: MIN } # [doc = " New code should prefer to use"] # [doc = concat ! ("[`" , stringify ! ($ SelfT) , "::MAX" , "`] instead.")] # [doc = ""] # [doc = " Returns the largest value that can be represented by this integer type."] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_promotable] # [inline (always)] # [rustc_const_stable (feature = "const_max_value" , since = "1.32.0")] # [deprecated (since = "TBD" , note = "replaced by the `MAX` associated constant on this type")] # [rustc_diagnostic_item = concat ! (stringify ! ($ SelfT) , "_legacy_fn_max_value")] pub const fn max_value () -> Self { Self :: MAX } } }}