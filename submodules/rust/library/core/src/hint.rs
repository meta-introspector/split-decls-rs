mkuse!{use crate :: mem :: MaybeUninit ;}
mkuse!{use crate :: { intrinsics , ub_checks } ;}

macro_rules! unreachable_unchecked_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function unreachable_unchecked in module {}", module_path!());
    };
}

mkfn!{
    unreachable_unchecked_introspect!();
    # [doc = " Informs the compiler that the site which is calling this function is not"] # [doc = " reachable, possibly enabling further optimizations."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Reaching this function is *Undefined Behavior*."] # [doc = ""] # [doc = " As the compiler assumes that all forms of Undefined Behavior can never"] # [doc = " happen, it will eliminate all branches in the surrounding code that it can"] # [doc = " determine will invariably lead to a call to `unreachable_unchecked()`."] # [doc = ""] # [doc = " If the assumptions embedded in using this function turn out to be wrong -"] # [doc = " that is, if the site which is calling `unreachable_unchecked()` is actually"] # [doc = " reachable at runtime - the compiler may have generated nonsensical machine"] # [doc = " instructions for this situation, including in seemingly unrelated code,"] # [doc = " causing difficult-to-debug problems."] # [doc = ""] # [doc = " Use this function sparingly. Consider using the [`unreachable!`] macro,"] # [doc = " which may prevent some optimizations but will safely panic in case it is"] # [doc = " actually reached at runtime. Benchmark your code to find out if using"] # [doc = " `unreachable_unchecked()` comes with a performance benefit."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " `unreachable_unchecked()` can be used in situations where the compiler"] # [doc = " can't prove invariants that were previously established. Such situations"] # [doc = " have a higher chance of occurring if those invariants are upheld by"] # [doc = " external code that the compiler can't analyze."] # [doc = " ```"] # [doc = " fn prepare_inputs(divisors: &mut Vec<u32>) {"] # [doc = "     // Note to future-self when making changes: The invariant established"] # [doc = "     // here is NOT checked in `do_computation()`; if this changes, you HAVE"] # [doc = "     // to change `do_computation()`."] # [doc = "     divisors.retain(|divisor| *divisor != 0)"] # [doc = " }"] # [doc = ""] # [doc = " /// # Safety"] # [doc = " /// All elements of `divisor` must be non-zero."] # [doc = " unsafe fn do_computation(i: u32, divisors: &[u32]) -> u32 {"] # [doc = "     divisors.iter().fold(i, |acc, divisor| {"] # [doc = "         // Convince the compiler that a division by zero can't happen here"] # [doc = "         // and a check is not needed below."] # [doc = "         if *divisor == 0 {"] # [doc = "             // Safety: `divisor` can't be zero because of `prepare_inputs`,"] # [doc = "             // but the compiler does not know about this. We *promise*"] # [doc = "             // that we always call `prepare_inputs`."] # [doc = "             unsafe { std::hint::unreachable_unchecked() }"] # [doc = "         }"] # [doc = "         // The compiler would normally introduce a check here that prevents"] # [doc = "         // a division by zero. However, if `divisor` was zero, the branch"] # [doc = "         // above would reach what we explicitly marked as unreachable."] # [doc = "         // The compiler concludes that `divisor` can't be zero at this point"] # [doc = "         // and removes the - now proven useless - check."] # [doc = "         acc / divisor"] # [doc = "     })"] # [doc = " }"] # [doc = ""] # [doc = " let mut divisors = vec![2, 0, 4];"] # [doc = " prepare_inputs(&mut divisors);"] # [doc = " let result = unsafe {"] # [doc = "     // Safety: prepare_inputs() guarantees that divisors is non-zero"] # [doc = "     do_computation(100, &divisors)"] # [doc = " };"] # [doc = " assert_eq!(result, 12);"] # [doc = ""] # [doc = " ```"] # [doc = ""] # [doc = " While using `unreachable_unchecked()` is perfectly sound in the following"] # [doc = " example, as the compiler is able to prove that a division by zero is not"] # [doc = " possible, benchmarking reveals that `unreachable_unchecked()` provides"] # [doc = " no benefit over using [`unreachable!`], while the latter does not introduce"] # [doc = " the possibility of Undefined Behavior."] # [doc = ""] # [doc = " ```"] # [doc = " fn div_1(a: u32, b: u32) -> u32 {"] # [doc = "     use std::hint::unreachable_unchecked;"] # [doc = ""] # [doc = "     // `b.saturating_add(1)` is always positive (not zero),"] # [doc = "     // hence `checked_div` will never return `None`."] # [doc = "     // Therefore, the else branch is unreachable."] # [doc = "     a.checked_div(b.saturating_add(1))"] # [doc = "         .unwrap_or_else(|| unsafe { unreachable_unchecked() })"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(div_1(7, 0), 7);"] # [doc = " assert_eq!(div_1(9, 1), 4);"] # [doc = " assert_eq!(div_1(11, u32::MAX), 0);"] # [doc = " ```"] # [inline] # [stable (feature = "unreachable" , since = "1.27.0")] # [rustc_const_stable (feature = "const_unreachable_unchecked" , since = "1.57.0")] # [track_caller] pub const unsafe fn unreachable_unchecked () -> ! { ub_checks :: assert_unsafe_precondition ! (check_language_ub , "hint::unreachable_unchecked must never be reached" , () => false) ; unsafe { intrinsics :: unreachable () } }
}

macro_rules! assert_unchecked_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function assert_unchecked in module {}", module_path!());
    };
}

mkfn!{
    assert_unchecked_introspect!();
    # [doc = " Makes a *soundness* promise to the compiler that `cond` holds."] # [doc = ""] # [doc = " This may allow the optimizer to simplify things, but it might also make the generated code"] # [doc = " slower. Either way, calling it will most likely make compilation take longer."] # [doc = ""] # [doc = " You may know this from other places as"] # [doc = " [`llvm.assume`](https://llvm.org/docs/LangRef.html#llvm-assume-intrinsic) or, in C,"] # [doc = " [`__builtin_assume`](https://clang.llvm.org/docs/LanguageExtensions.html#builtin-assume)."] # [doc = ""] # [doc = " This promotes a correctness requirement to a soundness requirement. Don't do that without"] # [doc = " very good reason."] # [doc = ""] # [doc = " # Usage"] # [doc = ""] # [doc = " This is a situational tool for micro-optimization, and is allowed to do nothing. Any use"] # [doc = " should come with a repeatable benchmark to show the value, with the expectation to drop it"] # [doc = " later should the optimizer get smarter and no longer need it."] # [doc = ""] # [doc = " The more complicated the condition, the less likely this is to be useful. For example,"] # [doc = " `assert_unchecked(foo.is_sorted())` is a complex enough value that the compiler is unlikely"] # [doc = " to be able to take advantage of it."] # [doc = ""] # [doc = " There's also no need to `assert_unchecked` basic properties of things.  For example, the"] # [doc = " compiler already knows the range of `count_ones`, so there is no benefit to"] # [doc = " `let n = u32::count_ones(x); assert_unchecked(n <= u32::BITS);`."] # [doc = ""] # [doc = " `assert_unchecked` is logically equivalent to `if !cond { unreachable_unchecked(); }`. If"] # [doc = " ever you are tempted to write `assert_unchecked(false)`, you should instead use"] # [doc = " [`unreachable_unchecked()`] directly."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `cond` must be `true`. It is immediate UB to call this with `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use core::hint;"] # [doc = ""] # [doc = " /// # Safety"] # [doc = " ///"] # [doc = " /// `p` must be nonnull and valid"] # [doc = " pub unsafe fn next_value(p: *const i32) -> i32 {"] # [doc = "     // SAFETY: caller invariants guarantee that `p` is not null"] # [doc = "     unsafe { hint::assert_unchecked(!p.is_null()) }"] # [doc = ""] # [doc = "     if p.is_null() {"] # [doc = "         return -1;"] # [doc = "     } else {"] # [doc = "         // SAFETY: caller invariants guarantee that `p` is valid"] # [doc = "         unsafe { *p + 1 }"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Without the `assert_unchecked`, the above function produces the following with optimizations"] # [doc = " enabled:"] # [doc = ""] # [doc = " ```asm"] # [doc = " next_value:"] # [doc = "         test    rdi, rdi"] # [doc = "         je      .LBB0_1"] # [doc = "         mov     eax, dword ptr [rdi]"] # [doc = "         inc     eax"] # [doc = "         ret"] # [doc = " .LBB0_1:"] # [doc = "         mov     eax, -1"] # [doc = "         ret"] # [doc = " ```"] # [doc = ""] # [doc = " Adding the assertion allows the optimizer to remove the extra check:"] # [doc = ""] # [doc = " ```asm"] # [doc = " next_value:"] # [doc = "         mov     eax, dword ptr [rdi]"] # [doc = "         inc     eax"] # [doc = "         ret"] # [doc = " ```"] # [doc = ""] # [doc = " This example is quite unlike anything that would be used in the real world: it is redundant"] # [doc = " to put an assertion right next to code that checks the same thing, and dereferencing a"] # [doc = " pointer already has the builtin assumption that it is nonnull. However, it illustrates the"] # [doc = " kind of changes the optimizer can make even when the behavior is less obviously related."] # [track_caller] # [inline (always)] # [doc (alias = "assume")] # [stable (feature = "hint_assert_unchecked" , since = "1.81.0")] # [rustc_const_stable (feature = "hint_assert_unchecked" , since = "1.81.0")] pub const unsafe fn assert_unchecked (cond : bool) { unsafe { ub_checks :: assert_unsafe_precondition ! (check_language_ub , "hint::assert_unchecked must never be called when the condition is false" , (cond : bool = cond) => cond ,) ; crate :: intrinsics :: assume (cond) ; } }
}

macro_rules! spin_loop_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function spin_loop in module {}", module_path!());
    };
}

mkfn!{
    spin_loop_introspect!();
    # [doc = " Emits a machine instruction to signal the processor that it is running in"] # [doc = " a busy-wait spin-loop (\"spin lock\")."] # [doc = ""] # [doc = " Upon receiving the spin-loop signal the processor can optimize its behavior by,"] # [doc = " for example, saving power or switching hyper-threads."] # [doc = ""] # [doc = " This function is different from [`thread::yield_now`] which directly"] # [doc = " yields to the system's scheduler, whereas `spin_loop` does not interact"] # [doc = " with the operating system."] # [doc = ""] # [doc = " A common use case for `spin_loop` is implementing bounded optimistic"] # [doc = " spinning in a CAS loop in synchronization primitives. To avoid problems"] # [doc = " like priority inversion, it is strongly recommended that the spin loop is"] # [doc = " terminated after a finite amount of iterations and an appropriate blocking"] # [doc = " syscall is made."] # [doc = ""] # [doc = " **Note**: On platforms that do not support receiving spin-loop hints this"] # [doc = " function does not do anything at all."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```ignore-wasm"] # [doc = " use std::sync::atomic::{AtomicBool, Ordering};"] # [doc = " use std::sync::Arc;"] # [doc = " use std::{hint, thread};"] # [doc = ""] # [doc = " // A shared atomic value that threads will use to coordinate"] # [doc = " let live = Arc::new(AtomicBool::new(false));"] # [doc = ""] # [doc = " // In a background thread we'll eventually set the value"] # [doc = " let bg_work = {"] # [doc = "     let live = live.clone();"] # [doc = "     thread::spawn(move || {"] # [doc = "         // Do some work, then make the value live"] # [doc = "         do_some_work();"] # [doc = "         live.store(true, Ordering::Release);"] # [doc = "     })"] # [doc = " };"] # [doc = ""] # [doc = " // Back on our current thread, we wait for the value to be set"] # [doc = " while !live.load(Ordering::Acquire) {"] # [doc = "     // The spin loop is a hint to the CPU that we're waiting, but probably"] # [doc = "     // not for very long"] # [doc = "     hint::spin_loop();"] # [doc = " }"] # [doc = ""] # [doc = " // The value is now set"] # [doc = " # fn do_some_work() {}"] # [doc = " do_some_work();"] # [doc = " bg_work.join()?;"] # [doc = " # Ok::<(), Box<dyn core::any::Any + Send + 'static>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " [`thread::yield_now`]: ../../std/thread/fn.yield_now.html"] # [inline (always)] # [stable (feature = "renamed_spin_loop" , since = "1.49.0")] pub fn spin_loop () { crate :: cfg_select ! { target_arch = "x86" => { unsafe { crate :: arch :: x86 :: _mm_pause () } } target_arch = "x86_64" => { unsafe { crate :: arch :: x86_64 :: _mm_pause () } } target_arch = "riscv32" => crate :: arch :: riscv32 :: pause () , target_arch = "riscv64" => crate :: arch :: riscv64 :: pause () , any (target_arch = "aarch64" , target_arch = "arm64ec") => { unsafe { crate :: arch :: aarch64 :: __isb (crate :: arch :: aarch64 :: SY) } } all (target_arch = "arm" , target_feature = "v6") => { unsafe { crate :: arch :: arm :: __yield () } } target_arch = "loongarch32" => crate :: arch :: loongarch32 :: ibar ::< 0 > () , target_arch = "loongarch64" => crate :: arch :: loongarch64 :: ibar ::< 0 > () , _ => { } } }
}

macro_rules! black_box_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function black_box in module {}", module_path!());
    };
}

mkfn!{
    black_box_introspect!();
    # [doc = " An identity function that *__hints__* to the compiler to be maximally pessimistic about what"] # [doc = " `black_box` could do."] # [doc = ""] # [doc = " Unlike [`std::convert::identity`], a Rust compiler is encouraged to assume that `black_box` can"] # [doc = " use `dummy` in any possible valid way that Rust code is allowed to without introducing undefined"] # [doc = " behavior in the calling code. This property makes `black_box` useful for writing code in which"] # [doc = " certain optimizations are not desired, such as benchmarks."] # [doc = ""] # [doc = " <div class=\"warning\">"] # [doc = ""] # [doc = " Note however, that `black_box` is only (and can only be) provided on a \"best-effort\" basis. The"] # [doc = " extent to which it can block optimisations may vary depending upon the platform and code-gen"] # [doc = " backend used. Programs cannot rely on `black_box` for *correctness*, beyond it behaving as the"] # [doc = " identity function. As such, it **must not be relied upon to control critical program behavior.**"] # [doc = " This also means that this function does not offer any guarantees for cryptographic or security"] # [doc = " purposes."] # [doc = ""] # [doc = " This limitation is not specific to `black_box`; there is no mechanism in the entire Rust"] # [doc = " language that can provide the guarantees required for constant-time cryptography."] # [doc = " (There is also no such mechanism in LLVM, so the same is true for every other LLVM-based compiler.)"] # [doc = ""] # [doc = " </div>"] # [doc = ""] # [doc = " [`std::convert::identity`]: crate::convert::identity"] # [doc = ""] # [doc = " # When is this useful?"] # [doc = ""] # [doc = " While not suitable in those mission-critical cases, `black_box`'s functionality can generally be"] # [doc = " relied upon for benchmarking, and should be used there. It will try to ensure that the"] # [doc = " compiler doesn't optimize away part of the intended test code based on context. For"] # [doc = " example:"] # [doc = ""] # [doc = " ```"] # [doc = " fn contains(haystack: &[&str], needle: &str) -> bool {"] # [doc = "     haystack.iter().any(|x| x == &needle)"] # [doc = " }"] # [doc = ""] # [doc = " pub fn benchmark() {"] # [doc = "     let haystack = vec![\"abc\", \"def\", \"ghi\", \"jkl\", \"mno\"];"] # [doc = "     let needle = \"ghi\";"] # [doc = "     for _ in 0..10 {"] # [doc = "         contains(&haystack, needle);"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " The compiler could theoretically make optimizations like the following:"] # [doc = ""] # [doc = " - The `needle` and `haystack` do not change, move the call to `contains` outside the loop and"] # [doc = "   delete the loop"] # [doc = " - Inline `contains`"] # [doc = " - `needle` and `haystack` have values known at compile time, `contains` is always true. Remove"] # [doc = "   the call and replace with `true`"] # [doc = " - Nothing is done with the result of `contains`: delete this function call entirely"] # [doc = " - `benchmark` now has no purpose: delete this function"] # [doc = ""] # [doc = " It is not likely that all of the above happens, but the compiler is definitely able to make some"] # [doc = " optimizations that could result in a very inaccurate benchmark. This is where `black_box` comes"] # [doc = " in:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::hint::black_box;"] # [doc = ""] # [doc = " // Same `contains` function."] # [doc = " fn contains(haystack: &[&str], needle: &str) -> bool {"] # [doc = "     haystack.iter().any(|x| x == &needle)"] # [doc = " }"] # [doc = ""] # [doc = " pub fn benchmark() {"] # [doc = "     let haystack = vec![\"abc\", \"def\", \"ghi\", \"jkl\", \"mno\"];"] # [doc = "     let needle = \"ghi\";"] # [doc = "     for _ in 0..10 {"] # [doc = "         // Force the compiler to run `contains`, even though it is a pure function whose"] # [doc = "         // results are unused."] # [doc = "         black_box(contains("] # [doc = "             // Prevent the compiler from making assumptions about the input."] # [doc = "             black_box(&haystack),"] # [doc = "             black_box(needle),"] # [doc = "         ));"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " This essentially tells the compiler to block optimizations across any calls to `black_box`. So,"] # [doc = " it now:"] # [doc = ""] # [doc = " - Treats both arguments to `contains` as unpredictable: the body of `contains` can no longer be"] # [doc = "   optimized based on argument values"] # [doc = " - Treats the call to `contains` and its result as volatile: the body of `benchmark` cannot"] # [doc = "   optimize this away"] # [doc = ""] # [doc = " This makes our benchmark much more realistic to how the function would actually be used, where"] # [doc = " arguments are usually not known at compile time and the result is used in some way."] # [doc = ""] # [doc = " # How to use this"] # [doc = ""] # [doc = " In practice, `black_box` serves two purposes:"] # [doc = ""] # [doc = " 1. It prevents the compiler from making optimizations related to the value returned by `black_box`"] # [doc = " 2. It forces the value passed to `black_box` to be calculated, even if the return value of `black_box` is unused"] # [doc = ""] # [doc = " ```"] # [doc = " use std::hint::black_box;"] # [doc = ""] # [doc = " let zero = 0;"] # [doc = " let five = 5;"] # [doc = ""] # [doc = " // The compiler will see this and remove the `* five` call, because it knows that multiplying"] # [doc = " // any integer by 0 will result in 0."] # [doc = " let c = zero * five;"] # [doc = ""] # [doc = " // Adding `black_box` here disables the compiler's ability to reason about the first operand in the multiplication."] # [doc = " // It is forced to assume that it can be any possible number, so it cannot remove the `* five`"] # [doc = " // operation."] # [doc = " let c = black_box(zero) * five;"] # [doc = " ```"] # [doc = ""] # [doc = " While most cases will not be as clear-cut as the above example, it still illustrates how"] # [doc = " `black_box` can be used. When benchmarking a function, you usually want to wrap its inputs in"] # [doc = " `black_box` so the compiler cannot make optimizations that would be unrealistic in real-life"] # [doc = " use."] # [doc = ""] # [doc = " ```"] # [doc = " use std::hint::black_box;"] # [doc = ""] # [doc = " // This is a simple function that increments its input by 1. Note that it is pure, meaning it"] # [doc = " // has no side-effects. This function has no effect if its result is unused. (An example of a"] # [doc = " // function *with* side-effects is `println!()`.)"] # [doc = " fn increment(x: u8) -> u8 {"] # [doc = "     x + 1"] # [doc = " }"] # [doc = ""] # [doc = " // Here, we call `increment` but discard its result. The compiler, seeing this and knowing that"] # [doc = " // `increment` is pure, will eliminate this function call entirely. This may not be desired,"] # [doc = " // though, especially if we're trying to track how much time `increment` takes to execute."] # [doc = " let _ = increment(black_box(5));"] # [doc = ""] # [doc = " // Here, we force `increment` to be executed. This is because the compiler treats `black_box`"] # [doc = " // as if it has side-effects, and thus must compute its input."] # [doc = " let _ = black_box(increment(black_box(5)));"] # [doc = " ```"] # [doc = ""] # [doc = " There may be additional situations where you want to wrap the result of a function in"] # [doc = " `black_box` to force its execution. This is situational though, and may not have any effect"] # [doc = " (such as when the function returns a zero-sized type such as [`()` unit][unit])."] # [doc = ""] # [doc = " Note that `black_box` has no effect on how its input is treated, only its output. As such,"] # [doc = " expressions passed to `black_box` may still be optimized:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::hint::black_box;"] # [doc = ""] # [doc = " // The compiler sees this..."] # [doc = " let y = black_box(5 * 10);"] # [doc = ""] # [doc = " // ...as this. As such, it will likely simplify `5 * 10` to just `50`."] # [doc = " let _0 = 5 * 10;"] # [doc = " let y = black_box(_0);"] # [doc = " ```"] # [doc = ""] # [doc = " In the above example, the `5 * 10` expression is considered distinct from the `black_box` call,"] # [doc = " and thus is still optimized by the compiler. You can prevent this by moving the multiplication"] # [doc = " operation outside of `black_box`:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::hint::black_box;"] # [doc = ""] # [doc = " // No assumptions can be made about either operand, so the multiplication is not optimized out."] # [doc = " let y = black_box(5) * black_box(10);"] # [doc = " ```"] # [doc = ""] # [doc = " During constant evaluation, `black_box` is treated as a no-op."] # [inline] # [stable (feature = "bench_black_box" , since = "1.66.0")] # [rustc_const_stable (feature = "const_black_box" , since = "1.86.0")] pub const fn black_box < T > (dummy : T) -> T { crate :: intrinsics :: black_box (dummy) }
}

macro_rules! must_use_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function must_use in module {}", module_path!());
    };
}

mkfn!{
    must_use_introspect!();
    # [doc = " An identity function that causes an `unused_must_use` warning to be"] # [doc = " triggered if the given value is not used (returned, stored in a variable,"] # [doc = " etc) by the caller."] # [doc = ""] # [doc = " This is primarily intended for use in macro-generated code, in which a"] # [doc = " [`#[must_use]` attribute][must_use] either on a type or a function would not"] # [doc = " be convenient."] # [doc = ""] # [doc = " [must_use]: https://doc.rust-lang.org/reference/attributes/diagnostics.html#the-must_use-attribute"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(hint_must_use)]"] # [doc = ""] # [doc = " use core::fmt;"] # [doc = ""] # [doc = " pub struct Error(/* ... */);"] # [doc = ""] # [doc = " #[macro_export]"] # [doc = " macro_rules! make_error {"] # [doc = "     ($($args:expr),*) => {"] # [doc = "         core::hint::must_use({"] # [doc = "             let error = $crate::make_error(core::format_args!($($args),*));"] # [doc = "             error"] # [doc = "         })"] # [doc = "     };"] # [doc = " }"] # [doc = ""] # [doc = " // Implementation detail of make_error! macro."] # [doc = " #[doc(hidden)]"] # [doc = " pub fn make_error(args: fmt::Arguments<'_>) -> Error {"] # [doc = "     Error(/* ... */)"] # [doc = " }"] # [doc = ""] # [doc = " fn demo() -> Option<Error> {"] # [doc = "     if true {"] # [doc = "         // Oops, meant to write `return Some(make_error!(\"...\"));`"] # [doc = "         Some(make_error!(\"...\"));"] # [doc = "     }"] # [doc = "     None"] # [doc = " }"] # [doc = " #"] # [doc = " # // Make rustdoc not wrap the whole snippet in fn main, so that $crate::make_error works"] # [doc = " # fn main() {}"] # [doc = " ```"] # [doc = ""] # [doc = " In the above example, we'd like an `unused_must_use` lint to apply to the"] # [doc = " value created by `make_error!`. However, neither `#[must_use]` on a struct"] # [doc = " nor `#[must_use]` on a function is appropriate here, so the macro expands"] # [doc = " using `core::hint::must_use` instead."] # [doc = ""] # [doc = " - We wouldn't want `#[must_use]` on the `struct Error` because that would"] # [doc = "   make the following unproblematic code trigger a warning:"] # [doc = ""] # [doc = "   ```"] # [doc = "   # struct Error;"] # [doc = "   #"] # [doc = "   fn f(arg: &str) -> Result<(), Error>"] # [doc = "   # { Ok(()) }"] # [doc = ""] # [doc = "   #[test]"] # [doc = "   fn t() {"] # [doc = "       // Assert that `f` returns error if passed an empty string."] # [doc = "       // A value of type `Error` is unused here but that's not a problem."] # [doc = "       f(\"\").unwrap_err();"] # [doc = "   }"] # [doc = "   ```"] # [doc = ""] # [doc = " - Using `#[must_use]` on `fn make_error` can't help because the return value"] # [doc = "   *is* used, as the right-hand side of a `let` statement. The `let`"] # [doc = "   statement looks useless but is in fact necessary for ensuring that"] # [doc = "   temporaries within the `format_args` expansion are not kept alive past the"] # [doc = "   creation of the `Error`, as keeping them alive past that point can cause"] # [doc = "   autotrait issues in async code:"] # [doc = ""] # [doc = "   ```"] # [doc = "   # #![feature(hint_must_use)]"] # [doc = "   #"] # [doc = "   # struct Error;"] # [doc = "   #"] # [doc = "   # macro_rules! make_error {"] # [doc = "   #     ($($args:expr),*) => {"] # [doc = "   #         core::hint::must_use({"] # [doc = "   #             // If `let` isn't used, then `f()` produces a non-Send future."] # [doc = "   #             let error = make_error(core::format_args!($($args),*));"] # [doc = "   #             error"] # [doc = "   #         })"] # [doc = "   #     };"] # [doc = "   # }"] # [doc = "   #"] # [doc = "   # fn make_error(args: core::fmt::Arguments<'_>) -> Error {"] # [doc = "   #     Error"] # [doc = "   # }"] # [doc = "   #"] # [doc = "   async fn f() {"] # [doc = "       // Using `let` inside the make_error expansion causes temporaries like"] # [doc = "       // `unsync()` to drop at the semicolon of that `let` statement, which"] # [doc = "       // is prior to the await point. They would otherwise stay around until"] # [doc = "       // the semicolon on *this* statement, which is after the await point,"] # [doc = "       // and the enclosing Future would not implement Send."] # [doc = "       log(make_error!(\"look: {:p}\", unsync())).await;"] # [doc = "   }"] # [doc = ""] # [doc = "   async fn log(error: Error) {/* ... */}"] # [doc = ""] # [doc = "   // Returns something without a Sync impl."] # [doc = "   fn unsync() -> *const () {"] # [doc = "       0 as *const ()"] # [doc = "   }"] # [doc = "   #"] # [doc = "   # fn test() {"] # [doc = "   #     fn assert_send(_: impl Send) {}"] # [doc = "   #     assert_send(f());"] # [doc = "   # }"] # [doc = "   ```"] # [unstable (feature = "hint_must_use" , issue = "94745")] # [must_use] # [inline (always)] pub const fn must_use < T > (value : T) -> T { value }
}

macro_rules! likely_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function likely in module {}", module_path!());
    };
}

mkfn!{
    likely_introspect!();
    # [doc = " Hints to the compiler that a branch condition is likely to be true."] # [doc = " Returns the value passed to it."] # [doc = ""] # [doc = " It can be used with `if` or boolean `match` expressions."] # [doc = ""] # [doc = " When used outside of a branch condition, it may still influence a nearby branch, but"] # [doc = " probably will not have any effect."] # [doc = ""] # [doc = " It can also be applied to parts of expressions, such as `likely(a) && unlikely(b)`, or to"] # [doc = " compound expressions, such as `likely(a && b)`. When applied to compound expressions, it has"] # [doc = " the following effect:"] # [doc = " ```text"] # [doc = "     likely(!a) => !unlikely(a)"] # [doc = "     likely(a && b) => likely(a) && likely(b)"] # [doc = "     likely(a || b) => a || likely(b)"] # [doc = " ```"] # [doc = ""] # [doc = " See also the function [`cold_path()`] which may be more appropriate for idiomatic Rust code."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(likely_unlikely)]"] # [doc = " use core::hint::likely;"] # [doc = ""] # [doc = " fn foo(x: i32) {"] # [doc = "     if likely(x > 0) {"] # [doc = "         println!(\"this branch is likely to be taken\");"] # [doc = "     } else {"] # [doc = "         println!(\"this branch is unlikely to be taken\");"] # [doc = "     }"] # [doc = ""] # [doc = "     match likely(x > 0) {"] # [doc = "         true => println!(\"this branch is likely to be taken\"),"] # [doc = "         false => println!(\"this branch is unlikely to be taken\"),"] # [doc = "     }"] # [doc = ""] # [doc = "     // Use outside of a branch condition may still influence a nearby branch"] # [doc = "     let cond = likely(x != 0);"] # [doc = "     if cond {"] # [doc = "         println!(\"this branch is likely to be taken\");"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [unstable (feature = "likely_unlikely" , issue = "136873")] # [inline (always)] pub const fn likely (b : bool) -> bool { crate :: intrinsics :: likely (b) }
}

macro_rules! unlikely_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function unlikely in module {}", module_path!());
    };
}

mkfn!{
    unlikely_introspect!();
    # [doc = " Hints to the compiler that a branch condition is unlikely to be true."] # [doc = " Returns the value passed to it."] # [doc = ""] # [doc = " It can be used with `if` or boolean `match` expressions."] # [doc = ""] # [doc = " When used outside of a branch condition, it may still influence a nearby branch, but"] # [doc = " probably will not have any effect."] # [doc = ""] # [doc = " It can also be applied to parts of expressions, such as `likely(a) && unlikely(b)`, or to"] # [doc = " compound expressions, such as `unlikely(a && b)`. When applied to compound expressions, it has"] # [doc = " the following effect:"] # [doc = " ```text"] # [doc = "     unlikely(!a) => !likely(a)"] # [doc = "     unlikely(a && b) => a && unlikely(b)"] # [doc = "     unlikely(a || b) => unlikely(a) || unlikely(b)"] # [doc = " ```"] # [doc = ""] # [doc = " See also the function [`cold_path()`] which may be more appropriate for idiomatic Rust code."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(likely_unlikely)]"] # [doc = " use core::hint::unlikely;"] # [doc = ""] # [doc = " fn foo(x: i32) {"] # [doc = "     if unlikely(x > 0) {"] # [doc = "         println!(\"this branch is unlikely to be taken\");"] # [doc = "     } else {"] # [doc = "         println!(\"this branch is likely to be taken\");"] # [doc = "     }"] # [doc = ""] # [doc = "     match unlikely(x > 0) {"] # [doc = "         true => println!(\"this branch is unlikely to be taken\"),"] # [doc = "         false => println!(\"this branch is likely to be taken\"),"] # [doc = "     }"] # [doc = ""] # [doc = "     // Use outside of a branch condition may still influence a nearby branch"] # [doc = "     let cond = unlikely(x != 0);"] # [doc = "     if cond {"] # [doc = "         println!(\"this branch is likely to be taken\");"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [unstable (feature = "likely_unlikely" , issue = "136873")] # [inline (always)] pub const fn unlikely (b : bool) -> bool { crate :: intrinsics :: unlikely (b) }
}

macro_rules! cold_path_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function cold_path in module {}", module_path!());
    };
}

mkfn!{
    cold_path_introspect!();
    # [doc = " Hints to the compiler that given path is cold, i.e., unlikely to be taken. The compiler may"] # [doc = " choose to optimize paths that are not cold at the expense of paths that are cold."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(cold_path)]"] # [doc = " use core::hint::cold_path;"] # [doc = ""] # [doc = " fn foo(x: &[i32]) {"] # [doc = "     if let Some(first) = x.get(0) {"] # [doc = "         // this is the fast path"] # [doc = "     } else {"] # [doc = "         // this path is unlikely"] # [doc = "         cold_path();"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " fn bar(x: i32) -> i32 {"] # [doc = "     match x {"] # [doc = "         1 => 10,"] # [doc = "         2 => 100,"] # [doc = "         3 => { cold_path(); 1000 }, // this branch is unlikely"] # [doc = "         _ => { cold_path(); 10000 }, // this is also unlikely"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [unstable (feature = "cold_path" , issue = "136873")] # [inline (always)] pub const fn cold_path () { crate :: intrinsics :: cold_path () }
}

macro_rules! select_unpredictable_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function select_unpredictable in module {}", module_path!());
    };
}

mkfn!{
    select_unpredictable_introspect!();
    # [doc = " Returns either `true_val` or `false_val` depending on the value of"] # [doc = " `condition`, with a hint to the compiler that `condition` is unlikely to be"] # [doc = " correctly predicted by a CPU’s branch predictor."] # [doc = ""] # [doc = " This method is functionally equivalent to"] # [doc = " ```ignore (this is just for illustrative purposes)"] # [doc = " fn select_unpredictable<T>(b: bool, true_val: T, false_val: T) -> T {"] # [doc = "     if b { true_val } else { false_val }"] # [doc = " }"] # [doc = " ```"] # [doc = " but might generate different assembly. In particular, on platforms with"] # [doc = " a conditional move or select instruction (like `cmov` on x86 or `csel`"] # [doc = " on ARM) the optimizer might use these instructions to avoid branches,"] # [doc = " which can benefit performance if the branch predictor is struggling"] # [doc = " with predicting `condition`, such as in an implementation of binary"] # [doc = " search."] # [doc = ""] # [doc = " Note however that this lowering is not guaranteed (on any platform) and"] # [doc = " should not be relied upon when trying to write cryptographic constant-time"] # [doc = " code. Also be aware that this lowering might *decrease* performance if"] # [doc = " `condition` is well-predictable. It is advisable to perform benchmarks to"] # [doc = " tell if this function is useful."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Distribute values evenly between two buckets:"] # [doc = " ```"] # [doc = " use std::hash::BuildHasher;"] # [doc = " use std::hint;"] # [doc = ""] # [doc = " fn append<H: BuildHasher>(hasher: &H, v: i32, bucket_one: &mut Vec<i32>, bucket_two: &mut Vec<i32>) {"] # [doc = "     let hash = hasher.hash_one(&v);"] # [doc = "     let bucket = hint::select_unpredictable(hash % 2 == 0, bucket_one, bucket_two);"] # [doc = "     bucket.push(v);"] # [doc = " }"] # [doc = " # let hasher = std::collections::hash_map::RandomState::new();"] # [doc = " # let mut bucket_one = Vec::new();"] # [doc = " # let mut bucket_two = Vec::new();"] # [doc = " # append(&hasher, 42, &mut bucket_one, &mut bucket_two);"] # [doc = " # assert_eq!(bucket_one.len() + bucket_two.len(), 1);"] # [doc = " ```"] # [inline (always)] # [stable (feature = "select_unpredictable" , since = "1.88.0")] pub fn select_unpredictable < T > (condition : bool , true_val : T , false_val : T) -> T { let mut true_val = MaybeUninit :: new (true_val) ; let mut false_val = MaybeUninit :: new (false_val) ; struct DropOnPanic < T > { inner : * mut T , } impl < T > Drop for DropOnPanic < T > { fn drop (& mut self) { unsafe { self . inner . drop_in_place () } } } let true_ptr = true_val . as_mut_ptr () ; let false_ptr = false_val . as_mut_ptr () ; unsafe { let guard = crate :: intrinsics :: select_unpredictable (condition , true_ptr , false_ptr) ; let drop = crate :: intrinsics :: select_unpredictable (condition , false_ptr , true_ptr) ; let guard = DropOnPanic { inner : guard } ; drop . drop_in_place () ; crate :: mem :: forget (guard) ; crate :: intrinsics :: select_unpredictable (condition , true_val , false_val) . assume_init () } }
}