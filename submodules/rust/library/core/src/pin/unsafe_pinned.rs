use split_decls_genesis :: ourprelude :: * ; use crate :: cell :: UnsafeCell ; use crate :: marker :: Unpin ; use crate :: ops :: { CoerceUnsized , DispatchFromDyn } ; use crate :: pin :: Pin ; use crate :: { fmt , ptr } ; #[doc = " This type provides a way to entirely opt-out of typical aliasing rules;"] #[doc = " specifically, `&mut UnsafePinned<T>` is not guaranteed to be a unique pointer."] #[doc = " This also subsumes the effects of `UnsafeCell`, i.e., `&UnsafePinned<T>` may point to data"] #[doc = " that is being mutated."] #[doc = ""] #[doc = " However, even if you define your type like `pub struct Wrapper(UnsafePinned<...>)`, it is still"] #[doc = " very risky to have an `&mut Wrapper` that aliases anything else. Many functions that work"] #[doc = " generically on `&mut T` assume that the memory that stores `T` is uniquely owned (such as"] #[doc = " `mem::swap`). In other words, while having aliasing with `&mut Wrapper` is not immediate"] #[doc = " Undefined Behavior, it is still unsound to expose such a mutable reference to code you do not"] #[doc = " control! Techniques such as pinning via [`Pin`] are needed to ensure soundness."] #[doc = ""] #[doc = " Similar to [`UnsafeCell`](crate::cell::UnsafeCell), `UnsafePinned` will not usually show up in"] #[doc = " the public API of a library. It is an internal implementation detail of libraries that need to"] #[doc = " support aliasing mutable references."] #[doc = ""] #[doc = " This type blocks niches the same way `UnsafeCell` does."] #[lang = "unsafe_pinned"] #[repr (transparent)] #[unstable (feature = "unsafe_pinned" , issue = "125735")] pub struct UnsafePinned < T : ? Sized > { value : UnsafeCell < T > , } #[unstable (feature = "unsafe_pinned" , issue = "125735")] unsafe impl < T : ? Sized + Sync > Sync for UnsafePinned < T > { } #[doc = " When this type is used, that almost certainly means safe APIs need to use pinning to avoid the"] #[doc = " aliases from becoming invalidated. Therefore let's mark this as `!Unpin`. You can always opt"] #[doc = " back in to `Unpin` with an `impl` block, provided your API is still sound while unpinned."] #[unstable (feature = "unsafe_pinned" , issue = "125735")] impl < T : ? Sized > ! Unpin for UnsafePinned < T > { } impl < T > UnsafePinned < T > { #[doc = " Constructs a new instance of `UnsafePinned` which will wrap the specified value."] #[doc = ""] #[doc = " All access to the inner value through `&UnsafePinned<T>` or `&mut UnsafePinned<T>` or"] #[doc = " `Pin<&mut UnsafePinned<T>>` requires `unsafe` code."] #[inline (always)] #[must_use] #[unstable (feature = "unsafe_pinned" , issue = "125735")] pub const fn new (value : T) -> Self { UnsafePinned { value : UnsafeCell :: new (value) } } #[doc = " Unwraps the value, consuming this `UnsafePinned`."] #[inline (always)] #[must_use] #[unstable (feature = "unsafe_pinned" , issue = "125735")] #[rustc_allow_const_fn_unstable (const_precise_live_drops)] pub const fn into_inner (self) -> T { self . value . into_inner () } } impl < T : ? Sized > UnsafePinned < T > { #[doc = " Get read-write access to the contents of a pinned `UnsafePinned`."] #[inline (always)] #[must_use] #[unstable (feature = "unsafe_pinned" , issue = "125735")] pub const fn get_mut_pinned (self : Pin < & mut Self >) -> * mut T { unsafe { self . get_unchecked_mut () } . get_mut_unchecked () } #[doc = " Get read-write access to the contents of an `UnsafePinned`."] #[doc = ""] #[doc = " You should usually be using `get_mut_pinned` instead to explicitly track the fact that this"] #[doc = " memory is \"pinned\" due to there being aliases."] #[inline (always)] #[must_use] #[unstable (feature = "unsafe_pinned" , issue = "125735")] pub const fn get_mut_unchecked (& mut self) -> * mut T { ptr :: from_mut (self) as * mut T } #[doc = " Get mutable access to the contents of a shared `UnsafePinned`."] #[doc = ""] #[doc = " This can be cast to a pointer of any kind. When creating references, you must uphold the"] #[doc = " aliasing rules; see [`UnsafeCell`] for more discussion and caveats."] #[doc = ""] #[doc = " [`UnsafeCell`]: crate::cell::UnsafeCell#aliasing-rules"] #[doc = ""] #[doc = " ```rust,no_run"] #[doc = " #![feature(unsafe_pinned)]"] #[doc = " use std::pin::UnsafePinned;"] #[doc = ""] #[doc = " unsafe {"] #[doc = "     let mut x = UnsafePinned::new(0);"] #[doc = "     let ptr = x.get();"] #[doc = "     x.get_mut_unchecked().write(1);"] #[doc = "     assert_eq!(ptr.read(), 1);"] #[doc = " }"] #[doc = " ```"] #[inline (always)] #[must_use] #[unstable (feature = "unsafe_pinned" , issue = "125735")] pub const fn get (& self) -> * mut T { self . value . get () } #[doc = " Gets an immutable pointer to the wrapped value."] #[doc = ""] #[doc = " The difference from [`get`] is that this function accepts a raw pointer, which is useful to"] #[doc = " avoid the creation of temporary references."] #[doc = ""] #[doc = " [`get`]: UnsafePinned::get"] #[inline (always)] #[must_use] #[unstable (feature = "unsafe_pinned" , issue = "125735")] pub const fn raw_get (this : * const Self) -> * mut T { this as * const T as * mut T } #[doc = " Gets a mutable pointer to the wrapped value."] #[doc = ""] #[doc = " The difference from [`get_mut_pinned`] and [`get_mut_unchecked`] is that this function"] #[doc = " accepts a raw pointer, which is useful to avoid the creation of temporary references."] #[doc = ""] #[doc = " [`get_mut_pinned`]: UnsafePinned::get_mut_pinned"] #[doc = " [`get_mut_unchecked`]: UnsafePinned::get_mut_unchecked"] #[inline (always)] #[must_use] #[unstable (feature = "unsafe_pinned" , issue = "125735")] pub const fn raw_get_mut (this : * mut Self) -> * mut T { this as * mut T } } #[unstable (feature = "unsafe_pinned" , issue = "125735")] impl < T : Default > Default for UnsafePinned < T > { #[doc = " Creates an `UnsafePinned`, with the `Default` value for T."] fn default () -> Self { UnsafePinned :: new (T :: default ()) } } #[unstable (feature = "unsafe_pinned" , issue = "125735")] #[rustc_const_unstable (feature = "const_convert" , issue = "143773")] impl < T > const From < T > for UnsafePinned < T > { #[doc = " Creates a new `UnsafePinned<T>` containing the given value."] fn from (value : T) -> Self { UnsafePinned :: new (value) } } #[unstable (feature = "unsafe_pinned" , issue = "125735")] impl < T : ? Sized > fmt :: Debug for UnsafePinned < T > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("UnsafePinned") . finish_non_exhaustive () } } #[unstable (feature = "coerce_unsized" , issue = "18598")] impl < T : CoerceUnsized < U > , U > CoerceUnsized < UnsafePinned < U > > for UnsafePinned < T > { } #[unstable (feature = "dispatch_from_dyn" , issue = "none")] impl < T : DispatchFromDyn < U > , U > DispatchFromDyn < UnsafePinned < U > > for UnsafePinned < T > { }