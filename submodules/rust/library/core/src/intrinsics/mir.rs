# ! [doc = " Rustc internal tooling for hand-writing MIR."] # ! [doc = ""] # ! [doc = " If for some reasons you are not writing rustc tests and have found yourself considering using"] # ! [doc = " this feature, turn back. This is *exceptionally* unstable. There is no attempt at all to make"] # ! [doc = " anything work besides those things which the rustc test suite happened to need. If you make a"] # ! [doc = " typo you'll probably ICE. Really, this is not the solution to your problems. Consider instead"] # ! [doc = " supporting the [stable MIR project group](https://github.com/rust-lang/project-stable-mir)."] # ! [doc = ""] # ! [doc = " The documentation for this module describes how to use this feature. If you are interested in"] # ! [doc = " hacking on the implementation, most of that documentation lives at"] # ! [doc = " `rustc_mir_build/src/build/custom/mod.rs`."] # ! [doc = ""] # ! [doc = " Typical usage will look like this:"] # ! [doc = ""] # ! [doc = " ```rust"] # ! [doc = " #![feature(core_intrinsics, custom_mir)]"] # ! [doc = " #![allow(internal_features)]"] # ! [doc = ""] # ! [doc = " use core::intrinsics::mir::*;"] # ! [doc = ""] # ! [doc = " #[custom_mir(dialect = \"built\")]"] # ! [doc = " pub fn simple(x: i32) -> i32 {"] # ! [doc = "     mir! {"] # ! [doc = "         let temp2: i32;"] # ! [doc = ""] # ! [doc = "         {"] # ! [doc = "             let temp1 = x;"] # ! [doc = "             Goto(my_second_block)"] # ! [doc = "         }"] # ! [doc = ""] # ! [doc = "         my_second_block = {"] # ! [doc = "             temp2 = Move(temp1);"] # ! [doc = "             RET = temp2;"] # ! [doc = "             Return()"] # ! [doc = "         }"] # ! [doc = "     }"] # ! [doc = " }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " The `custom_mir` attribute tells the compiler to treat the function as being custom MIR. This"] # ! [doc = " attribute only works on functions - there is no way to insert custom MIR into the middle of"] # ! [doc = " another function. The `dialect` and `phase` parameters indicate which [version of MIR][dialect"] # ! [doc = " docs] you are inserting here. Generally you'll want to use `#![custom_mir(dialect = \"built\")]`"] # ! [doc = " if you want your MIR to be modified by the full MIR pipeline, or `#![custom_mir(dialect ="] # ! [doc = " \"runtime\", phase = \"optimized\")]` if you don't."] # ! [doc = ""] # ! [doc = " [dialect docs]:"] # ! [doc = "     https://doc.rust-lang.org/nightly/nightly-rustc/rustc_middle/mir/enum.MirPhase.html"] # ! [doc = ""] # ! [doc = " The input to the [`mir!`] macro is:"] # ! [doc = ""] # ! [doc = "  - An optional return type annotation in the form of `type RET = ...;`. This may be required"] # ! [doc = "    if the compiler cannot infer the type of RET."] # ! [doc = "  - A possibly empty list of local declarations. Locals can also be declared inline on"] # ! [doc = "    assignments via `let`. Type inference generally works. Shadowing does not."] # ! [doc = "  - A list of basic blocks. The first of these is the start block and is where execution begins."] # ! [doc = "    All blocks other than the start block need to be given a name, so that they can be referred"] # ! [doc = "    to later."] # ! [doc = "     - Each block is a list of semicolon terminated statements, followed by a terminator. The"] # ! [doc = "       syntax for the various statements and terminators is designed to be as similar as possible"] # ! [doc = "       to the syntax for analogous concepts in native Rust. See below for a list."] # ! [doc = ""] # ! [doc = " # Examples"] # ! [doc = ""] # ! [doc = " ```rust"] # ! [doc = " #![feature(core_intrinsics, custom_mir)]"] # ! [doc = " #![allow(internal_features)]"] # ! [doc = " #![allow(unused_assignments)]"] # ! [doc = ""] # ! [doc = " use core::intrinsics::mir::*;"] # ! [doc = ""] # ! [doc = " #[custom_mir(dialect = \"built\")]"] # ! [doc = " pub fn choose_load(a: &i32, b: &i32, c: bool) -> i32 {"] # ! [doc = "     mir! {"] # ! [doc = "         {"] # ! [doc = "             match c {"] # ! [doc = "                 true => t,"] # ! [doc = "                 _ => f,"] # ! [doc = "             }"] # ! [doc = "         }"] # ! [doc = ""] # ! [doc = "         t = {"] # ! [doc = "             let temp = a;"] # ! [doc = "             Goto(load_and_exit)"] # ! [doc = "         }"] # ! [doc = ""] # ! [doc = "         f = {"] # ! [doc = "             temp = b;"] # ! [doc = "             Goto(load_and_exit)"] # ! [doc = "         }"] # ! [doc = ""] # ! [doc = "         load_and_exit = {"] # ! [doc = "             RET = *temp;"] # ! [doc = "             Return()"] # ! [doc = "         }"] # ! [doc = "     }"] # ! [doc = " }"] # ! [doc = ""] # ! [doc = " #[custom_mir(dialect = \"built\")]"] # ! [doc = " fn unwrap_unchecked<T>(opt: Option<T>) -> T {"] # ! [doc = "     mir! {"] # ! [doc = "         {"] # ! [doc = "             RET = Move(Field(Variant(opt, 1), 0));"] # ! [doc = "             Return()"] # ! [doc = "         }"] # ! [doc = "     }"] # ! [doc = " }"] # ! [doc = ""] # ! [doc = " #[custom_mir(dialect = \"runtime\", phase = \"optimized\")]"] # ! [doc = " fn push_and_pop<T>(v: &mut Vec<T>, value: T) {"] # ! [doc = "     mir! {"] # ! [doc = "         let _unused;"] # ! [doc = "         let popped;"] # ! [doc = ""] # ! [doc = "         {"] # ! [doc = "             Call(_unused = Vec::push(v, value), ReturnTo(pop), UnwindContinue())"] # ! [doc = "         }"] # ! [doc = ""] # ! [doc = "         pop = {"] # ! [doc = "             Call(popped = Vec::pop(v), ReturnTo(drop), UnwindContinue())"] # ! [doc = "         }"] # ! [doc = ""] # ! [doc = "         drop = {"] # ! [doc = "             Drop(popped, ReturnTo(ret), UnwindContinue())"] # ! [doc = "         }"] # ! [doc = ""] # ! [doc = "         ret = {"] # ! [doc = "             Return()"] # ! [doc = "         }"] # ! [doc = "     }"] # ! [doc = " }"] # ! [doc = ""] # ! [doc = " #[custom_mir(dialect = \"runtime\", phase = \"optimized\")]"] # ! [doc = " fn annotated_return_type() -> (i32, bool) {"] # ! [doc = "     mir! {"] # ! [doc = "         type RET = (i32, bool);"] # ! [doc = "         {"] # ! [doc = "             RET.0 = 1;"] # ! [doc = "             RET.1 = true;"] # ! [doc = "             Return()"] # ! [doc = "         }"] # ! [doc = "     }"] # ! [doc = " }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " We can also set off compilation failures that happen in sufficiently late stages of the"] # ! [doc = " compiler:"] # ! [doc = ""] # ! [doc = " ```rust,compile_fail"] # ! [doc = " #![feature(core_intrinsics, custom_mir)]"] # ! [doc = ""] # ! [doc = " extern crate core;"] # ! [doc = " use core::intrinsics::mir::*;"] # ! [doc = ""] # ! [doc = " #[custom_mir(dialect = \"built\")]"] # ! [doc = " fn borrow_error(should_init: bool) -> i32 {"] # ! [doc = "     mir! {"] # ! [doc = "         let temp: i32;"] # ! [doc = ""] # ! [doc = "         {"] # ! [doc = "             match should_init {"] # ! [doc = "                 true => init,"] # ! [doc = "                 _ => use_temp,"] # ! [doc = "             }"] # ! [doc = "         }"] # ! [doc = ""] # ! [doc = "         init = {"] # ! [doc = "             temp = 0;"] # ! [doc = "             Goto(use_temp)"] # ! [doc = "         }"] # ! [doc = ""] # ! [doc = "         use_temp = {"] # ! [doc = "             RET = temp;"] # ! [doc = "             Return()"] # ! [doc = "         }"] # ! [doc = "     }"] # ! [doc = " }"] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " ```text"] # ! [doc = " error[E0381]: used binding is possibly-uninitialized"] # ! [doc = "   --> test.rs:24:13"] # ! [doc = "    |"] # ! [doc = " 8  | /     mir! {"] # ! [doc = " 9  | |         let temp: i32;"] # ! [doc = " 10 | |"] # ! [doc = " 11 | |         {"] # ! [doc = " ...  |"] # ! [doc = " 19 | |             temp = 0;"] # ! [doc = "    | |             -------- binding initialized here in some conditions"] # ! [doc = " ...  |"] # ! [doc = " 24 | |             RET = temp;"] # ! [doc = "    | |             ^^^^^^^^^^ value used here but it is possibly-uninitialized"] # ! [doc = " 25 | |             Return()"] # ! [doc = " 26 | |         }"] # ! [doc = " 27 | |     }"] # ! [doc = "    | |_____- binding declared here but left uninitialized"] # ! [doc = ""] # ! [doc = " error: aborting due to 1 previous error"] # ! [doc = ""] # ! [doc = " For more information about this error, try `rustc --explain E0381`."] # ! [doc = " ```"] # ! [doc = ""] # ! [doc = " # Syntax"] # ! [doc = ""] # ! [doc = " The lists below are an exhaustive description of how various MIR constructs can be created."] # ! [doc = " Anything missing from the list should be assumed to not be supported, PRs welcome."] # ! [doc = ""] # ! [doc = " #### Locals"] # ! [doc = ""] # ! [doc = "  - The `_0` return local can always be accessed via `RET`."] # ! [doc = "  - Arguments can be accessed via their regular name."] # ! [doc = "  - All other locals need to be declared with `let` somewhere and then can be accessed by name."] # ! [doc = ""] # ! [doc = " #### Places"] # ! [doc = "  - Locals implicitly convert to places."] # ! [doc = "  - Field accesses, derefs, and indexing work normally."] # ! [doc = "  - Fields in variants can be accessed via the [`Variant`] and [`Field`] associated functions,"] # ! [doc = "    see their documentation for details."] # ! [doc = ""] # ! [doc = " #### Operands"] # ! [doc = "  - Places implicitly convert to `Copy` operands."] # ! [doc = "  - `Move` operands can be created via [`Move`]."] # ! [doc = "  - Const blocks, literals, named constants, and const params all just work."] # ! [doc = "  - [`Static`] and [`StaticMut`] can be used to create `&T` and `*mut T`s to statics. These are"] # ! [doc = "    constants in MIR and the only way to access statics."] # ! [doc = ""] # ! [doc = " #### Statements"] # ! [doc = "  - Assign statements work via normal Rust assignment."] # ! [doc = "  - [`Retag`], [`StorageLive`], [`StorageDead`], [`Deinit`] statements have an associated function."] # ! [doc = ""] # ! [doc = " #### Rvalues"] # ! [doc = ""] # ! [doc = "  - Operands implicitly convert to `Use` rvalues."] # ! [doc = "  - `&`, `&mut`, `addr_of!`, and `addr_of_mut!` all work to create their associated rvalue."] # ! [doc = "  - [`Discriminant`], [`Len`], and [`CopyForDeref`] have associated functions."] # ! [doc = "  - Unary and binary operations use their normal Rust syntax - `a * b`, `!c`, etc."] # ! [doc = "  - The binary operation `Offset` can be created via [`Offset`]."] # ! [doc = "  - Checked binary operations are represented by wrapping the associated binop in [`Checked`]."] # ! [doc = "  - Array repetition syntax (`[foo; 10]`) creates the associated rvalue."] # ! [doc = ""] # ! [doc = " #### Terminators"] # ! [doc = ""] # ! [doc = "  - [`Goto`], [`Return`], [`Unreachable`] and [`Drop`](Drop()) have associated functions."] # ! [doc = "  - `match some_int_operand` becomes a `SwitchInt`. Each arm should be `literal => basic_block`"] # ! [doc = "     - The exception is the last arm, which must be `_ => basic_block` and corresponds to the"] # ! [doc = "       otherwise branch."] # ! [doc = "  - [`Call`] has an associated function as well, with special syntax:"] # ! [doc = "    `Call(ret_val = function(arg1, arg2, ...), ReturnTo(next_block), UnwindContinue())`."] # ! [doc = "  - [`TailCall`] does not have a return destination or next block, so its syntax is just"] # ! [doc = "    `TailCall(function(arg1, arg2, ...))`."] # ! [doc = ""] # ! [doc = " #### Debuginfo"] # ! [doc = ""] # ! [doc = " Debuginfo associates source code variable names (of variables that may not exist any more) with"] # ! [doc = " MIR expressions that indicate where the value of that variable is stored. The syntax to do so"] # ! [doc = " is:"] # ! [doc = " ```text"] # ! [doc = " debug source_var_name => expression;"] # ! [doc = " ```"] # ! [doc = " Both places and constants are supported in the `expression`."] # ! [doc = ""] # ! [doc = " ```rust"] # ! [doc = " #![allow(internal_features)]"] # ! [doc = " #![feature(core_intrinsics, custom_mir)]"] # ! [doc = ""] # ! [doc = " use core::intrinsics::mir::*;"] # ! [doc = ""] # ! [doc = " #[custom_mir(dialect = \"built\")]"] # ! [doc = " fn debuginfo(arg: Option<&i32>) {"] # ! [doc = "     mir!("] # ! [doc = "         // Debuginfo for a source variable `plain_local` that just duplicates `arg`."] # ! [doc = "         debug plain_local => arg;"] # ! [doc = "         // Debuginfo for a source variable `projection` that can be computed by dereferencing"] # ! [doc = "         // a field of `arg`."] # ! [doc = "         debug projection => *Field::<&i32>(Variant(arg, 1), 0);"] # ! [doc = "         // Debuginfo for a source variable `constant` that always holds the value `5`."] # ! [doc = "         debug constant => 5_usize;"] # ! [doc = "         {"] # ! [doc = "             Return()"] # ! [doc = "         }"] # ! [doc = "     )"] # ! [doc = " }"] # ! [doc = " ```"] # ! [unstable (feature = "custom_mir" , reason = "MIR is an implementation detail and extremely unstable" , issue = "none")] # ! [allow (unused_variables , non_snake_case , missing_debug_implementations)] use split_decls_genesis :: ourprelude :: * ; #[doc = " Type representing basic blocks."] #[doc = ""] #[doc = " All terminators will have this type as a return type. It helps achieve some type safety."] #[rustc_diagnostic_item = "mir_basic_block"] pub enum BasicBlock { #[doc = " A non-cleanup basic block."] Normal , #[doc = " A basic block that lies on an unwind path."] Cleanup , } #[doc = " The reason we are terminating the process during unwinding."] #[rustc_diagnostic_item = "mir_unwind_terminate_reason"] pub enum UnwindTerminateReason { #[doc = " Unwinding is just not possible given the ABI of this function."] Abi , #[doc = " We were already cleaning up for an ongoing unwind, and a *second*, *nested* unwind was"] #[doc = " triggered by the drop glue."] InCleanup , } pub use UnwindTerminateReason :: { Abi as ReasonAbi , InCleanup as ReasonInCleanup } ; macro_rules ! define { ($ name : literal , $ (#[$ meta : meta]) * fn $ ($ sig : tt) *) => { #[rustc_diagnostic_item = $ name] #[inline] $ (#[$ meta]) * pub fn $ ($ sig) * { panic ! () } } } pub struct UnwindActionArg ; define ! ("mir_unwind_continue" , #[doc = " An unwind action that continues unwinding."] fn UnwindContinue () -> UnwindActionArg) ; define ! ("mir_unwind_unreachable" , #[doc = " An unwind action that triggers undefined behavior."] fn UnwindUnreachable () -> UnwindActionArg) ; define ! ("mir_unwind_terminate" , #[doc = " An unwind action that terminates the execution."] #[doc = ""] #[doc = " `UnwindTerminate` can also be used as a terminator."] fn UnwindTerminate (reason : UnwindTerminateReason) -> UnwindActionArg) ; define ! ("mir_unwind_cleanup" , #[doc = " An unwind action that continues execution in a given basic block."] fn UnwindCleanup (goto : BasicBlock) -> UnwindActionArg) ; pub struct ReturnToArg ; define ! ("mir_return_to" , fn ReturnTo (goto : BasicBlock) -> ReturnToArg) ; define ! ("mir_return" , fn Return () -> BasicBlock) ; define ! ("mir_goto" , fn Goto (destination : BasicBlock) -> BasicBlock) ; define ! ("mir_unreachable" , fn Unreachable () -> BasicBlock) ; define ! ("mir_drop" , #[doc = " Drop the contents of a place."] #[doc = ""] #[doc = " The first argument must be a place."] #[doc = ""] #[doc = " The second argument must be of the form `ReturnTo(bb)`, where `bb` is the basic block that"] #[doc = " will be jumped to after the destructor returns."] #[doc = ""] #[doc = " The third argument describes what happens on unwind. It can be one of:"] #[doc = " - [`UnwindContinue`]"] #[doc = " - [`UnwindUnreachable`]"] #[doc = " - [`UnwindTerminate`]"] #[doc = " - [`UnwindCleanup`]"] fn Drop < T > (place : T , goto : ReturnToArg , unwind_action : UnwindActionArg)) ; define ! ("mir_call" , #[doc = " Call a function."] #[doc = ""] #[doc = " The first argument must be of the form `ret_val = fun(arg1, arg2, ...)`."] #[doc = ""] #[doc = " The second argument must be of the form `ReturnTo(bb)`, where `bb` is the basic block that"] #[doc = " will be jumped to after the function returns."] #[doc = ""] #[doc = " The third argument describes what happens on unwind. It can be one of:"] #[doc = " - [`UnwindContinue`]"] #[doc = " - [`UnwindUnreachable`]"] #[doc = " - [`UnwindTerminate`]"] #[doc = " - [`UnwindCleanup`]"] fn Call (call : () , goto : ReturnToArg , unwind_action : UnwindActionArg)) ; define ! ("mir_tail_call" , #[doc = " Call a function."] #[doc = ""] #[doc = " The argument must be of the form `fun(arg1, arg2, ...)`."] fn TailCall < T > (call : T)) ; define ! ("mir_unwind_resume" , #[doc = " A terminator that resumes the unwinding."] fn UnwindResume ()) ; define ! ("mir_storage_live" , fn StorageLive < T > (local : T)) ; define ! ("mir_storage_dead" , fn StorageDead < T > (local : T)) ; define ! ("mir_assume" , fn Assume (operand : bool)) ; define ! ("mir_deinit" , fn Deinit < T > (place : T)) ; define ! ("mir_checked" , fn Checked < T > (binop : T) -> (T , bool)) ; define ! ("mir_len" , fn Len < T > (place : T) -> usize) ; define ! ("mir_ptr_metadata" , fn PtrMetadata < P : ? Sized > (place : * const P) -> < P as :: core :: ptr :: Pointee >:: Metadata) ; define ! ("mir_copy_for_deref" , fn CopyForDeref < T > (place : T) -> T) ; define ! ("mir_retag" , fn Retag < T > (place : T)) ; define ! ("mir_move" , fn Move < T > (place : T) -> T) ; define ! ("mir_static" , fn Static < T > (s : T) -> &'static T) ; define ! ("mir_static_mut" , fn StaticMut < T > (s : T) -> * mut T) ; define ! ("mir_discriminant" , #[doc = " Gets the discriminant of a place."] fn Discriminant < T > (place : T) -> < T as :: core :: marker :: DiscriminantKind >:: Discriminant) ; define ! ("mir_set_discriminant" , fn SetDiscriminant < T > (place : T , index : u32)) ; define ! ("mir_offset" , fn Offset < T , U > (ptr : T , count : U) -> T) ; define ! ("mir_field" , #[doc = " Access the field with the given index of some place."] #[doc = ""] #[doc = " This only makes sense to use in conjunction with [`Variant`]. If the type you are looking to"] #[doc = " access the field of does not have variants, you can use normal field projection syntax."] #[doc = ""] #[doc = " There is no proper way to do a place projection to a variant in Rust, and so these two"] #[doc = " functions are a workaround. You can access a field of a variant via `Field(Variant(place,"] #[doc = " var_idx), field_idx)`, where `var_idx` and `field_idx` are appropriate literals. Some"] #[doc = " caveats:"] #[doc = ""] #[doc = "  - The return type of `Variant` is always `()`. Don't worry about that, the correct MIR will"] #[doc = "    still be generated."] #[doc = "  - In some situations, the return type of `Field` cannot be inferred. You may need to"] #[doc = "    annotate it on the function in these cases."] #[doc = "  - Since `Field` is a function call which is not a place expression, using this on the left"] #[doc = "    hand side of an expression is rejected by the compiler. [`place!`] is a macro provided to"] #[doc = "    work around that issue. Wrap the left hand side of an assignment in the macro to convince"] #[doc = "    the compiler that it's ok."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```rust"] #[doc = " #![allow(internal_features)]"] #[doc = " #![feature(custom_mir, core_intrinsics)]"] #[doc = ""] #[doc = " use core::intrinsics::mir::*;"] #[doc = ""] #[doc = " #[custom_mir(dialect = \"built\")]"] #[doc = " fn unwrap_deref(opt: Option<&i32>) -> i32 {"] #[doc = "     mir! {"] #[doc = "         {"] #[doc = "             RET = *Field::<&i32>(Variant(opt, 1), 0);"] #[doc = "             Return()"] #[doc = "         }"] #[doc = "     }"] #[doc = " }"] #[doc = ""] #[doc = " #[custom_mir(dialect = \"built\")]"] #[doc = " fn set(opt: &mut Option<i32>) {"] #[doc = "     mir! {"] #[doc = "         {"] #[doc = "             place!(Field(Variant(*opt, 1), 0)) = 5;"] #[doc = "             Return()"] #[doc = "         }"] #[doc = "     }"] #[doc = " }"] #[doc = " ```"] fn Field < F > (place : () , field : u32) -> F) ; define ! ("mir_variant" , #[doc = " Adds a variant projection with the given index to the place."] #[doc = ""] #[doc = " See [`Field`] for documentation."] fn Variant < T > (place : T , index : u32) -> ()) ; define ! ("mir_cast_transmute" , #[doc = " Emits a `CastKind::Transmute` cast."] #[doc = ""] #[doc = " Needed to test the UB when `sizeof(T) != sizeof(U)`, which can't be"] #[doc = " generated via the normal `mem::transmute`."] fn CastTransmute < T , U > (operand : T) -> U) ; define ! ("mir_cast_ptr_to_ptr" , #[doc = " Emits a `CastKind::PtrToPtr` cast."] #[doc = ""] #[doc = " This allows bypassing normal validation to generate strange casts."] fn CastPtrToPtr < T , U > (operand : T) -> U) ; define ! ("mir_make_place" , #[doc (hidden)] fn __internal_make_place < T > (place : T) -> * mut T) ; define ! ("mir_debuginfo" , #[doc (hidden)] fn __debuginfo < T > (name : &'static str , s : T)) ; #[doc = " Macro for generating custom MIR."] #[doc = ""] #[doc = " See the module documentation for syntax details. This macro is not magic - it only transforms"] #[doc = " your MIR into something that is easier to parse in the compiler."] #[rustc_macro_transparency = "transparent"] pub macro mir { { $ (type RET = $ ret_ty : ty ;) ? $ (let $ local_decl : ident $ (: $ local_decl_ty : ty) ? ;) * $ (debug $ dbg_name : ident => $ dbg_data : expr ;) * { $ ($ entry : tt) * } $ ($ block_name : ident $ (($ block_cleanup : ident)) ? = { $ ($ block : tt) * }) * } => { { __internal_declare_basic_blocks ! ($ ($ block_name $ (($ block_cleanup)) ?) *) ; { #[allow (non_snake_case)] let RET $ (: $ ret_ty) ?; $ (let $ local_decl $ (: $ local_decl_ty) ? ;) * :: core :: intrinsics :: mir :: __internal_extract_let ! ($ ($ entry) *) ; $ (:: core :: intrinsics :: mir :: __internal_extract_let ! ($ ($ block) *) ;) * { $ (__debuginfo (stringify ! ($ dbg_name) , $ dbg_data) ;) * { :: core :: intrinsics :: mir :: __internal_remove_let ! ({ { } { $ ($ entry) * } }) ; $ (:: core :: intrinsics :: mir :: __internal_remove_let ! ({ { } { $ ($ block) * } }) ;) * RET } } } } } } #[doc = " Helper macro that allows you to treat a value expression like a place expression."] #[doc = ""] #[doc = " See the documentation on [`Variant`] for why this is necessary and how to use it."] pub macro place ($ e : expr) { (*:: core :: intrinsics :: mir :: __internal_make_place ($ e)) } #[doc = " Helper macro that extracts the `let` declarations out of a bunch of statements."] #[doc = ""] #[doc = " This macro is written using the \"statement muncher\" strategy. Each invocation parses the first"] #[doc = " statement out of the input, does the appropriate thing with it, and then recursively calls the"] #[doc = " same macro on the remainder of the input."] #[doc (hidden)] pub macro __internal_extract_let { (let $ var : ident $ (: $ ty : ty) ? = $ expr : expr ; $ ($ rest : tt) *) => { let $ var $ (: $ ty) ?; :: core :: intrinsics :: mir :: __internal_extract_let ! ($ ($ rest) *) ; } , (let $ var : ident $ (: $ ty : ty) ? = const $ block : block ; $ ($ rest : tt) *) => { let $ var $ (: $ ty) ?; :: core :: intrinsics :: mir :: __internal_extract_let ! ($ ($ rest) *) ; } , ($ stmt : stmt ; $ ($ rest : tt) *) => { :: core :: intrinsics :: mir :: __internal_extract_let ! ($ ($ rest) *) ; } , ($ expr : expr) => { } } #[doc = " Helper macro that removes the `let` declarations from a bunch of statements."] #[doc = ""] #[doc = " Because expression position macros cannot expand to statements + expressions, we need to be"] #[doc = " slightly creative here. The general strategy is also statement munching as above, but the output"] #[doc = " of the macro is \"stored\" in the subsequent macro invocation. Easiest understood via example:"] #[doc = " ```text"] #[doc = " invoke!("] #[doc = "     {"] #[doc = "         {"] #[doc = "             x = 5;"] #[doc = "         }"] #[doc = "         {"] #[doc = "             let d = e;"] #[doc = "             Call()"] #[doc = "         }"] #[doc = "     }"] #[doc = " )"] #[doc = " ```"] #[doc = " becomes"] #[doc = " ```text"] #[doc = " invoke!("] #[doc = "     {"] #[doc = "         {"] #[doc = "             x = 5;"] #[doc = "             d = e;"] #[doc = "         }"] #[doc = "         {"] #[doc = "             Call()"] #[doc = "         }"] #[doc = "     }"] #[doc = " )"] #[doc = " ```"] #[doc (hidden)] pub macro __internal_remove_let { ({ { $ ($ already_parsed : tt) * } { let $ var : ident $ (: $ ty : ty) ? = $ expr : expr ; $ ($ rest : tt) * } }) => { :: core :: intrinsics :: mir :: __internal_remove_let ! ({ { $ ($ already_parsed) * $ var = $ expr ; } { $ ($ rest) * } }) } , ({ { $ ($ already_parsed : tt) * } { let $ var : ident $ (: $ ty : ty) ? = const $ block : block ; $ ($ rest : tt) * } }) => { :: core :: intrinsics :: mir :: __internal_remove_let ! ({ { $ ($ already_parsed) * $ var = const $ block ; } { $ ($ rest) * } }) } , ({ { $ ($ already_parsed : tt) * } { $ stmt : stmt ; $ ($ rest : tt) * } }) => { :: core :: intrinsics :: mir :: __internal_remove_let ! ({ { $ ($ already_parsed) * $ stmt ; } { $ ($ rest) * } }) } , ({ { $ ($ already_parsed : tt) * } { $ expr : expr } }) => { { $ ($ already_parsed) * $ expr } } , } #[doc = " Helper macro that declares the basic blocks."] #[doc (hidden)] pub macro __internal_declare_basic_blocks { () => { } , ($ name : ident (cleanup) $ ($ rest : tt) *) => { let $ name = :: core :: intrinsics :: mir :: BasicBlock :: Cleanup ; __internal_declare_basic_blocks ! ($ ($ rest) *) } , ($ name : ident $ ($ rest : tt) *) => { let $ name = :: core :: intrinsics :: mir :: BasicBlock :: Normal ; __internal_declare_basic_blocks ! ($ ($ rest) *) } , }