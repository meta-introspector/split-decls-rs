mkuse!{use core :: borrow :: Borrow ;}
mkuse!{use core :: cmp :: Ordering :: { self , Equal , Greater , Less } ;}
mkuse!{use core :: cmp :: { max , min } ;}
mkuse!{use core :: fmt :: { self , Debug } ;}
mkuse!{use core :: hash :: { Hash , Hasher } ;}
mkuse!{use core :: iter :: { FusedIterator , Peekable } ;}
mkuse!{use core :: mem :: ManuallyDrop ;}
mkuse!{use core :: ops :: { BitAnd , BitOr , BitXor , Bound , RangeBounds , Sub } ;}
mkuse!{use super :: map :: { self , BTreeMap , Keys } ;}
mkuse!{use super :: merge_iter :: MergeIterInner ;}
mkuse!{use super :: set_val :: SetValZST ;}
mkuse!{use crate :: alloc :: { Allocator , Global } ;}
mkuse!{use crate :: vec :: Vec ;}
mkmod!{entry, { 
                getname!(entry);
                getsrc!(entry);
                getpath!(entry);
                get_deps!(entry);
                get_crates!(entry);
                mkinclude!(entry);
                 
            }}
mkuse!{# [unstable (feature = "btree_set_entry" , issue = "133549")] pub use self :: entry :: { Entry , OccupiedEntry , VacantEntry } ;}
mkitem!{mkstruct!{# [doc = " An ordered set based on a B-Tree."] # [doc = ""] # [doc = " See [`BTreeMap`]'s documentation for a detailed discussion of this collection's performance"] # [doc = " benefits and drawbacks."] # [doc = ""] # [doc = " It is a logic error for an item to be modified in such a way that the item's ordering relative"] # [doc = " to any other item, as determined by the [`Ord`] trait, changes while it is in the set. This is"] # [doc = " normally only possible through [`Cell`], [`RefCell`], global state, I/O, or unsafe code."] # [doc = " The behavior resulting from such a logic error is not specified, but will be encapsulated to the"] # [doc = " `BTreeSet` that observed the logic error and not result in undefined behavior. This could"] # [doc = " include panics, incorrect results, aborts, memory leaks, and non-termination."] # [doc = ""] # [doc = " Iterators returned by [`BTreeSet::iter`] and [`BTreeSet::into_iter`] produce their items in order, and take worst-case"] # [doc = " logarithmic and amortized constant time per item returned."] # [doc = ""] # [doc = " [`Cell`]: core::cell::Cell"] # [doc = " [`RefCell`]: core::cell::RefCell"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " // Type inference lets us omit an explicit type signature (which"] # [doc = " // would be `BTreeSet<&str>` in this example)."] # [doc = " let mut books = BTreeSet::new();"] # [doc = ""] # [doc = " // Add some books."] # [doc = " books.insert(\"A Dance With Dragons\");"] # [doc = " books.insert(\"To Kill a Mockingbird\");"] # [doc = " books.insert(\"The Odyssey\");"] # [doc = " books.insert(\"The Great Gatsby\");"] # [doc = ""] # [doc = " // Check for a specific one."] # [doc = " if !books.contains(\"The Winds of Winter\") {"] # [doc = "     println!(\"We have {} books, but The Winds of Winter ain't one.\","] # [doc = "              books.len());"] # [doc = " }"] # [doc = ""] # [doc = " // Remove a book."] # [doc = " books.remove(\"The Odyssey\");"] # [doc = ""] # [doc = " // Iterate over everything."] # [doc = " for book in &books {"] # [doc = "     println!(\"{book}\");"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " A `BTreeSet` with a known list of items can be initialized from an array:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let set = BTreeSet::from([1, 2, 3]);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [cfg_attr (not (test) , rustc_diagnostic_item = "BTreeSet")] pub struct BTreeSet < T , # [unstable (feature = "allocator_api" , issue = "32838")] A : Allocator + Clone = Global , > { map : BTreeMap < T , SetValZST , A > , }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : Hash , A : Allocator + Clone > Hash for BTreeSet < T , A > { fn hash < H : Hasher > (& self , state : & mut H) { self . map . hash (state) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : PartialEq , A : Allocator + Clone > PartialEq for BTreeSet < T , A > { fn eq (& self , other : & BTreeSet < T , A >) -> bool { self . map . eq (& other . map) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : Eq , A : Allocator + Clone > Eq for BTreeSet < T , A > { }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : PartialOrd , A : Allocator + Clone > PartialOrd for BTreeSet < T , A > { fn partial_cmp (& self , other : & BTreeSet < T , A >) -> Option < Ordering > { self . map . partial_cmp (& other . map) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : Ord , A : Allocator + Clone > Ord for BTreeSet < T , A > { fn cmp (& self , other : & BTreeSet < T , A >) -> Ordering { self . map . cmp (& other . map) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : Clone , A : Allocator + Clone > Clone for BTreeSet < T , A > { fn clone (& self) -> Self { BTreeSet { map : self . map . clone () } } fn clone_from (& mut self , source : & Self) { self . map . clone_from (& source . map) ; } }}}
mkitem!{mkstruct!{# [doc = " An iterator over the items of a `BTreeSet`."] # [doc = ""] # [doc = " This `struct` is created by the [`iter`] method on [`BTreeSet`]."] # [doc = " See its documentation for more."] # [doc = ""] # [doc = " [`iter`]: BTreeSet::iter"] # [must_use = "iterators are lazy and do nothing unless consumed"] # [stable (feature = "rust1" , since = "1.0.0")] pub struct Iter < 'a , T : 'a > { iter : Keys < 'a , T , SetValZST > , }}}
mkitem!{mkimpl!{# [stable (feature = "collection_debug" , since = "1.17.0")] impl < T : fmt :: Debug > fmt :: Debug for Iter < '_ , T > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_tuple ("Iter") . field (& self . iter) . finish () } }}}
mkitem!{mkstruct!{# [doc = " An owning iterator over the items of a `BTreeSet` in ascending order."] # [doc = ""] # [doc = " This `struct` is created by the [`into_iter`] method on [`BTreeSet`]"] # [doc = " (provided by the [`IntoIterator`] trait). See its documentation for more."] # [doc = ""] # [doc = " [`into_iter`]: BTreeSet#method.into_iter"] # [stable (feature = "rust1" , since = "1.0.0")] # [derive (Debug)] pub struct IntoIter < T , # [unstable (feature = "allocator_api" , issue = "32838")] A : Allocator + Clone = Global , > { iter : super :: map :: IntoIter < T , SetValZST , A > , }}}
mkitem!{mkstruct!{# [doc = " An iterator over a sub-range of items in a `BTreeSet`."] # [doc = ""] # [doc = " This `struct` is created by the [`range`] method on [`BTreeSet`]."] # [doc = " See its documentation for more."] # [doc = ""] # [doc = " [`range`]: BTreeSet::range"] # [must_use = "iterators are lazy and do nothing unless consumed"] # [derive (Debug)] # [stable (feature = "btree_range" , since = "1.17.0")] pub struct Range < 'a , T : 'a > { iter : super :: map :: Range < 'a , T , SetValZST > , }}}
mkitem!{mkstruct!{# [doc = " A lazy iterator producing elements in the difference of `BTreeSet`s."] # [doc = ""] # [doc = " This `struct` is created by the [`difference`] method on [`BTreeSet`]."] # [doc = " See its documentation for more."] # [doc = ""] # [doc = " [`difference`]: BTreeSet::difference"] # [must_use = "this returns the difference as an iterator, \
              without modifying either input set"] # [stable (feature = "rust1" , since = "1.0.0")] pub struct Difference < 'a , T : 'a , # [unstable (feature = "allocator_api" , issue = "32838")] A : Allocator + Clone = Global , > { inner : DifferenceInner < 'a , T , A > , }}}
mkitem!{mkenum!{enum DifferenceInner < 'a , T : 'a , A : Allocator + Clone > { Stitch { self_iter : Iter < 'a , T > , other_iter : Peekable < Iter < 'a , T > > , } , Search { self_iter : Iter < 'a , T > , other_set : & 'a BTreeSet < T , A > , } , Iterate (Iter < 'a , T >) , }}}
mkitem!{mkimpl!{impl < T : Debug , A : Allocator + Clone > Debug for DifferenceInner < '_ , T , A > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { match self { DifferenceInner :: Stitch { self_iter , other_iter } => f . debug_struct ("Stitch") . field ("self_iter" , self_iter) . field ("other_iter" , other_iter) . finish () , DifferenceInner :: Search { self_iter , other_set } => f . debug_struct ("Search") . field ("self_iter" , self_iter) . field ("other_iter" , other_set) . finish () , DifferenceInner :: Iterate (x) => f . debug_tuple ("Iterate") . field (x) . finish () , } } }}}
mkitem!{mkimpl!{# [stable (feature = "collection_debug" , since = "1.17.0")] impl < T : fmt :: Debug , A : Allocator + Clone > fmt :: Debug for Difference < '_ , T , A > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_tuple ("Difference") . field (& self . inner) . finish () } }}}
mkitem!{mkstruct!{# [doc = " A lazy iterator producing elements in the symmetric difference of `BTreeSet`s."] # [doc = ""] # [doc = " This `struct` is created by the [`symmetric_difference`] method on"] # [doc = " [`BTreeSet`]. See its documentation for more."] # [doc = ""] # [doc = " [`symmetric_difference`]: BTreeSet::symmetric_difference"] # [must_use = "this returns the difference as an iterator, \
              without modifying either input set"] # [stable (feature = "rust1" , since = "1.0.0")] pub struct SymmetricDifference < 'a , T : 'a > (MergeIterInner < Iter < 'a , T > >) ;}}
mkitem!{mkimpl!{# [stable (feature = "collection_debug" , since = "1.17.0")] impl < T : fmt :: Debug > fmt :: Debug for SymmetricDifference < '_ , T > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_tuple ("SymmetricDifference") . field (& self . 0) . finish () } }}}
mkitem!{mkstruct!{# [doc = " A lazy iterator producing elements in the intersection of `BTreeSet`s."] # [doc = ""] # [doc = " This `struct` is created by the [`intersection`] method on [`BTreeSet`]."] # [doc = " See its documentation for more."] # [doc = ""] # [doc = " [`intersection`]: BTreeSet::intersection"] # [must_use = "this returns the intersection as an iterator, \
              without modifying either input set"] # [stable (feature = "rust1" , since = "1.0.0")] pub struct Intersection < 'a , T : 'a , # [unstable (feature = "allocator_api" , issue = "32838")] A : Allocator + Clone = Global , > { inner : IntersectionInner < 'a , T , A > , }}}
mkitem!{mkenum!{enum IntersectionInner < 'a , T : 'a , A : Allocator + Clone > { Stitch { a : Iter < 'a , T > , b : Iter < 'a , T > , } , Search { small_iter : Iter < 'a , T > , large_set : & 'a BTreeSet < T , A > , } , Answer (Option < & 'a T >) , }}}
mkitem!{mkimpl!{impl < T : Debug , A : Allocator + Clone > Debug for IntersectionInner < '_ , T , A > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { match self { IntersectionInner :: Stitch { a , b } => { f . debug_struct ("Stitch") . field ("a" , a) . field ("b" , b) . finish () } IntersectionInner :: Search { small_iter , large_set } => f . debug_struct ("Search") . field ("small_iter" , small_iter) . field ("large_set" , large_set) . finish () , IntersectionInner :: Answer (x) => f . debug_tuple ("Answer") . field (x) . finish () , } } }}}
mkitem!{mkimpl!{# [stable (feature = "collection_debug" , since = "1.17.0")] impl < T : Debug , A : Allocator + Clone > Debug for Intersection < '_ , T , A > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_tuple ("Intersection") . field (& self . inner) . finish () } }}}
mkitem!{mkstruct!{# [doc = " A lazy iterator producing elements in the union of `BTreeSet`s."] # [doc = ""] # [doc = " This `struct` is created by the [`union`] method on [`BTreeSet`]."] # [doc = " See its documentation for more."] # [doc = ""] # [doc = " [`union`]: BTreeSet::union"] # [must_use = "this returns the union as an iterator, \
              without modifying either input set"] # [stable (feature = "rust1" , since = "1.0.0")] pub struct Union < 'a , T : 'a > (MergeIterInner < Iter < 'a , T > >) ;}}
mkitem!{mkimpl!{# [stable (feature = "collection_debug" , since = "1.17.0")] impl < T : fmt :: Debug > fmt :: Debug for Union < '_ , T > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_tuple ("Union") . field (& self . 0) . finish () } }}}
mkitem!{const ITER_PERFORMANCE_TIPPING_SIZE_DIFF : usize = 16 ;}
mkitem!{mkimpl!{impl < T > BTreeSet < T > { # [doc = " Makes a new, empty `BTreeSet`."] # [doc = ""] # [doc = " Does not allocate anything on its own."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #![allow(unused_mut)]"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut set: BTreeSet<i32> = BTreeSet::new();"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_stable (feature = "const_btree_new" , since = "1.66.0")] # [must_use] pub const fn new () -> BTreeSet < T > { BTreeSet { map : BTreeMap :: new () } } }}}
mkitem!{mkimpl!{impl < T , A : Allocator + Clone > BTreeSet < T , A > { # [doc = " Makes a new `BTreeSet` with a reasonable choice of B."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #![allow(unused_mut)]"] # [doc = " # #![feature(allocator_api)]"] # [doc = " # #![feature(btreemap_alloc)]"] # [doc = " use std::collections::BTreeSet;"] # [doc = " use std::alloc::Global;"] # [doc = ""] # [doc = " let mut set: BTreeSet<i32> = BTreeSet::new_in(Global);"] # [doc = " ```"] # [unstable (feature = "btreemap_alloc" , issue = "32838")] pub const fn new_in (alloc : A) -> BTreeSet < T , A > { BTreeSet { map : BTreeMap :: new_in (alloc) } } # [doc = " Constructs a double-ended iterator over a sub-range of elements in the set."] # [doc = " The simplest way is to use the range syntax `min..max`, thus `range(min..max)` will"] # [doc = " yield elements from min (inclusive) to max (exclusive)."] # [doc = " The range may also be entered as `(Bound<T>, Bound<T>)`, so for example"] # [doc = " `range((Excluded(4), Included(10)))` will yield a left-exclusive, right-inclusive"] # [doc = " range from 4 to 10."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if range `start > end`."] # [doc = " Panics if range `start == end` and both bounds are `Excluded`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = " use std::ops::Bound::Included;"] # [doc = ""] # [doc = " let mut set = BTreeSet::new();"] # [doc = " set.insert(3);"] # [doc = " set.insert(5);"] # [doc = " set.insert(8);"] # [doc = " for &elem in set.range((Included(&4), Included(&8))) {"] # [doc = "     println!(\"{elem}\");"] # [doc = " }"] # [doc = " assert_eq!(Some(&5), set.range(4..).next());"] # [doc = " ```"] # [stable (feature = "btree_range" , since = "1.17.0")] pub fn range < K : ? Sized , R > (& self , range : R) -> Range < '_ , T > where K : Ord , T : Borrow < K > + Ord , R : RangeBounds < K > , { Range { iter : self . map . range (range) } } # [doc = " Visits the elements representing the difference,"] # [doc = " i.e., the elements that are in `self` but not in `other`,"] # [doc = " in ascending order."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut a = BTreeSet::new();"] # [doc = " a.insert(1);"] # [doc = " a.insert(2);"] # [doc = ""] # [doc = " let mut b = BTreeSet::new();"] # [doc = " b.insert(2);"] # [doc = " b.insert(3);"] # [doc = ""] # [doc = " let diff: Vec<_> = a.difference(&b).cloned().collect();"] # [doc = " assert_eq!(diff, [1]);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn difference < 'a > (& 'a self , other : & 'a BTreeSet < T , A >) -> Difference < 'a , T , A > where T : Ord , { let (self_min , self_max) = if let (Some (self_min) , Some (self_max)) = (self . first () , self . last ()) { (self_min , self_max) } else { return Difference { inner : DifferenceInner :: Iterate (self . iter ()) } ; } ; let (other_min , other_max) = if let (Some (other_min) , Some (other_max)) = (other . first () , other . last ()) { (other_min , other_max) } else { return Difference { inner : DifferenceInner :: Iterate (self . iter ()) } ; } ; Difference { inner : match (self_min . cmp (other_max) , self_max . cmp (other_min)) { (Greater , _) | (_ , Less) => DifferenceInner :: Iterate (self . iter ()) , (Equal , _) => { let mut self_iter = self . iter () ; self_iter . next () ; DifferenceInner :: Iterate (self_iter) } (_ , Equal) => { let mut self_iter = self . iter () ; self_iter . next_back () ; DifferenceInner :: Iterate (self_iter) } _ if self . len () <= other . len () / ITER_PERFORMANCE_TIPPING_SIZE_DIFF => { DifferenceInner :: Search { self_iter : self . iter () , other_set : other } } _ => DifferenceInner :: Stitch { self_iter : self . iter () , other_iter : other . iter () . peekable () , } , } , } } # [doc = " Visits the elements representing the symmetric difference,"] # [doc = " i.e., the elements that are in `self` or in `other` but not in both,"] # [doc = " in ascending order."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut a = BTreeSet::new();"] # [doc = " a.insert(1);"] # [doc = " a.insert(2);"] # [doc = ""] # [doc = " let mut b = BTreeSet::new();"] # [doc = " b.insert(2);"] # [doc = " b.insert(3);"] # [doc = ""] # [doc = " let sym_diff: Vec<_> = a.symmetric_difference(&b).cloned().collect();"] # [doc = " assert_eq!(sym_diff, [1, 3]);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn symmetric_difference < 'a > (& 'a self , other : & 'a BTreeSet < T , A > ,) -> SymmetricDifference < 'a , T > where T : Ord , { SymmetricDifference (MergeIterInner :: new (self . iter () , other . iter ())) } # [doc = " Visits the elements representing the intersection,"] # [doc = " i.e., the elements that are both in `self` and `other`,"] # [doc = " in ascending order."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut a = BTreeSet::new();"] # [doc = " a.insert(1);"] # [doc = " a.insert(2);"] # [doc = ""] # [doc = " let mut b = BTreeSet::new();"] # [doc = " b.insert(2);"] # [doc = " b.insert(3);"] # [doc = ""] # [doc = " let intersection: Vec<_> = a.intersection(&b).cloned().collect();"] # [doc = " assert_eq!(intersection, [2]);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn intersection < 'a > (& 'a self , other : & 'a BTreeSet < T , A >) -> Intersection < 'a , T , A > where T : Ord , { let (self_min , self_max) = if let (Some (self_min) , Some (self_max)) = (self . first () , self . last ()) { (self_min , self_max) } else { return Intersection { inner : IntersectionInner :: Answer (None) } ; } ; let (other_min , other_max) = if let (Some (other_min) , Some (other_max)) = (other . first () , other . last ()) { (other_min , other_max) } else { return Intersection { inner : IntersectionInner :: Answer (None) } ; } ; Intersection { inner : match (self_min . cmp (other_max) , self_max . cmp (other_min)) { (Greater , _) | (_ , Less) => IntersectionInner :: Answer (None) , (Equal , _) => IntersectionInner :: Answer (Some (self_min)) , (_ , Equal) => IntersectionInner :: Answer (Some (self_max)) , _ if self . len () <= other . len () / ITER_PERFORMANCE_TIPPING_SIZE_DIFF => { IntersectionInner :: Search { small_iter : self . iter () , large_set : other } } _ if other . len () <= self . len () / ITER_PERFORMANCE_TIPPING_SIZE_DIFF => { IntersectionInner :: Search { small_iter : other . iter () , large_set : self } } _ => IntersectionInner :: Stitch { a : self . iter () , b : other . iter () } , } , } } # [doc = " Visits the elements representing the union,"] # [doc = " i.e., all the elements in `self` or `other`, without duplicates,"] # [doc = " in ascending order."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut a = BTreeSet::new();"] # [doc = " a.insert(1);"] # [doc = ""] # [doc = " let mut b = BTreeSet::new();"] # [doc = " b.insert(2);"] # [doc = ""] # [doc = " let union: Vec<_> = a.union(&b).cloned().collect();"] # [doc = " assert_eq!(union, [1, 2]);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn union < 'a > (& 'a self , other : & 'a BTreeSet < T , A >) -> Union < 'a , T > where T : Ord , { Union (MergeIterInner :: new (self . iter () , other . iter ())) } # [doc = " Clears the set, removing all elements."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut v = BTreeSet::new();"] # [doc = " v.insert(1);"] # [doc = " v.clear();"] # [doc = " assert!(v.is_empty());"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn clear (& mut self) where A : Clone , { self . map . clear () } # [doc = " Returns `true` if the set contains an element equal to the value."] # [doc = ""] # [doc = " The value may be any borrowed form of the set's element type,"] # [doc = " but the ordering on the borrowed form *must* match the"] # [doc = " ordering on the element type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let set = BTreeSet::from([1, 2, 3]);"] # [doc = " assert_eq!(set.contains(&1), true);"] # [doc = " assert_eq!(set.contains(&4), false);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn contains < Q : ? Sized > (& self , value : & Q) -> bool where T : Borrow < Q > + Ord , Q : Ord , { self . map . contains_key (value) } # [doc = " Returns a reference to the element in the set, if any, that is equal to"] # [doc = " the value."] # [doc = ""] # [doc = " The value may be any borrowed form of the set's element type,"] # [doc = " but the ordering on the borrowed form *must* match the"] # [doc = " ordering on the element type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let set = BTreeSet::from([1, 2, 3]);"] # [doc = " assert_eq!(set.get(&2), Some(&2));"] # [doc = " assert_eq!(set.get(&4), None);"] # [doc = " ```"] # [stable (feature = "set_recovery" , since = "1.9.0")] pub fn get < Q : ? Sized > (& self , value : & Q) -> Option < & T > where T : Borrow < Q > + Ord , Q : Ord , { self . map . get_key_value (value) . map (| (k , _) | k) } # [doc = " Returns `true` if `self` has no elements in common with `other`."] # [doc = " This is equivalent to checking for an empty intersection."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let a = BTreeSet::from([1, 2, 3]);"] # [doc = " let mut b = BTreeSet::new();"] # [doc = ""] # [doc = " assert_eq!(a.is_disjoint(&b), true);"] # [doc = " b.insert(4);"] # [doc = " assert_eq!(a.is_disjoint(&b), true);"] # [doc = " b.insert(1);"] # [doc = " assert_eq!(a.is_disjoint(&b), false);"] # [doc = " ```"] # [must_use] # [stable (feature = "rust1" , since = "1.0.0")] pub fn is_disjoint (& self , other : & BTreeSet < T , A >) -> bool where T : Ord , { self . intersection (other) . next () . is_none () } # [doc = " Returns `true` if the set is a subset of another,"] # [doc = " i.e., `other` contains at least all the elements in `self`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let sup = BTreeSet::from([1, 2, 3]);"] # [doc = " let mut set = BTreeSet::new();"] # [doc = ""] # [doc = " assert_eq!(set.is_subset(&sup), true);"] # [doc = " set.insert(2);"] # [doc = " assert_eq!(set.is_subset(&sup), true);"] # [doc = " set.insert(4);"] # [doc = " assert_eq!(set.is_subset(&sup), false);"] # [doc = " ```"] # [must_use] # [stable (feature = "rust1" , since = "1.0.0")] pub fn is_subset (& self , other : & BTreeSet < T , A >) -> bool where T : Ord , { if self . len () > other . len () { return false ; } let (self_min , self_max) = if let (Some (self_min) , Some (self_max)) = (self . first () , self . last ()) { (self_min , self_max) } else { return true ; } ; let (other_min , other_max) = if let (Some (other_min) , Some (other_max)) = (other . first () , other . last ()) { (other_min , other_max) } else { return false ; } ; let mut self_iter = self . iter () ; match self_min . cmp (other_min) { Less => return false , Equal => { self_iter . next () ; } Greater => () , } match self_max . cmp (other_max) { Greater => return false , Equal => { self_iter . next_back () ; } Less => () , } if self_iter . len () <= other . len () / ITER_PERFORMANCE_TIPPING_SIZE_DIFF { for next in self_iter { if ! other . contains (next) { return false ; } } } else { let mut other_iter = other . iter () ; other_iter . next () ; other_iter . next_back () ; let mut self_next = self_iter . next () ; while let Some (self1) = self_next { match other_iter . next () . map_or (Less , | other1 | self1 . cmp (other1)) { Less => return false , Equal => self_next = self_iter . next () , Greater => () , } } } true } # [doc = " Returns `true` if the set is a superset of another,"] # [doc = " i.e., `self` contains at least all the elements in `other`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let sub = BTreeSet::from([1, 2]);"] # [doc = " let mut set = BTreeSet::new();"] # [doc = ""] # [doc = " assert_eq!(set.is_superset(&sub), false);"] # [doc = ""] # [doc = " set.insert(0);"] # [doc = " set.insert(1);"] # [doc = " assert_eq!(set.is_superset(&sub), false);"] # [doc = ""] # [doc = " set.insert(2);"] # [doc = " assert_eq!(set.is_superset(&sub), true);"] # [doc = " ```"] # [must_use] # [stable (feature = "rust1" , since = "1.0.0")] pub fn is_superset (& self , other : & BTreeSet < T , A >) -> bool where T : Ord , { other . is_subset (self) } # [doc = " Returns a reference to the first element in the set, if any."] # [doc = " This element is always the minimum of all elements in the set."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut set = BTreeSet::new();"] # [doc = " assert_eq!(set.first(), None);"] # [doc = " set.insert(1);"] # [doc = " assert_eq!(set.first(), Some(&1));"] # [doc = " set.insert(2);"] # [doc = " assert_eq!(set.first(), Some(&1));"] # [doc = " ```"] # [must_use] # [stable (feature = "map_first_last" , since = "1.66.0")] # [rustc_confusables ("front")] pub fn first (& self) -> Option < & T > where T : Ord , { self . map . first_key_value () . map (| (k , _) | k) } # [doc = " Returns a reference to the last element in the set, if any."] # [doc = " This element is always the maximum of all elements in the set."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut set = BTreeSet::new();"] # [doc = " assert_eq!(set.last(), None);"] # [doc = " set.insert(1);"] # [doc = " assert_eq!(set.last(), Some(&1));"] # [doc = " set.insert(2);"] # [doc = " assert_eq!(set.last(), Some(&2));"] # [doc = " ```"] # [must_use] # [stable (feature = "map_first_last" , since = "1.66.0")] # [rustc_confusables ("back")] pub fn last (& self) -> Option < & T > where T : Ord , { self . map . last_key_value () . map (| (k , _) | k) } # [doc = " Removes the first element from the set and returns it, if any."] # [doc = " The first element is always the minimum element in the set."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut set = BTreeSet::new();"] # [doc = ""] # [doc = " set.insert(1);"] # [doc = " while let Some(n) = set.pop_first() {"] # [doc = "     assert_eq!(n, 1);"] # [doc = " }"] # [doc = " assert!(set.is_empty());"] # [doc = " ```"] # [stable (feature = "map_first_last" , since = "1.66.0")] pub fn pop_first (& mut self) -> Option < T > where T : Ord , { self . map . pop_first () . map (| kv | kv . 0) } # [doc = " Removes the last element from the set and returns it, if any."] # [doc = " The last element is always the maximum element in the set."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut set = BTreeSet::new();"] # [doc = ""] # [doc = " set.insert(1);"] # [doc = " while let Some(n) = set.pop_last() {"] # [doc = "     assert_eq!(n, 1);"] # [doc = " }"] # [doc = " assert!(set.is_empty());"] # [doc = " ```"] # [stable (feature = "map_first_last" , since = "1.66.0")] pub fn pop_last (& mut self) -> Option < T > where T : Ord , { self . map . pop_last () . map (| kv | kv . 0) } # [doc = " Adds a value to the set."] # [doc = ""] # [doc = " Returns whether the value was newly inserted. That is:"] # [doc = ""] # [doc = " - If the set did not previously contain an equal value, `true` is"] # [doc = "   returned."] # [doc = " - If the set already contained an equal value, `false` is returned, and"] # [doc = "   the entry is not updated."] # [doc = ""] # [doc = " See the [module-level documentation] for more."] # [doc = ""] # [doc = " [module-level documentation]: index.html#insert-and-complex-keys"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut set = BTreeSet::new();"] # [doc = ""] # [doc = " assert_eq!(set.insert(2), true);"] # [doc = " assert_eq!(set.insert(2), false);"] # [doc = " assert_eq!(set.len(), 1);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_confusables ("push" , "put")] pub fn insert (& mut self , value : T) -> bool where T : Ord , { self . map . insert (value , SetValZST :: default ()) . is_none () } # [doc = " Adds a value to the set, replacing the existing element, if any, that is"] # [doc = " equal to the value. Returns the replaced element."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut set = BTreeSet::new();"] # [doc = " set.insert(Vec::<i32>::new());"] # [doc = ""] # [doc = " assert_eq!(set.get(&[][..]).unwrap().capacity(), 0);"] # [doc = " set.replace(Vec::with_capacity(10));"] # [doc = " assert_eq!(set.get(&[][..]).unwrap().capacity(), 10);"] # [doc = " ```"] # [stable (feature = "set_recovery" , since = "1.9.0")] # [rustc_confusables ("swap")] pub fn replace (& mut self , value : T) -> Option < T > where T : Ord , { self . map . replace (value) } # [doc = " Inserts the given `value` into the set if it is not present, then"] # [doc = " returns a reference to the value in the set."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(btree_set_entry)]"] # [doc = ""] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut set = BTreeSet::from([1, 2, 3]);"] # [doc = " assert_eq!(set.len(), 3);"] # [doc = " assert_eq!(set.get_or_insert(2), &2);"] # [doc = " assert_eq!(set.get_or_insert(100), &100);"] # [doc = " assert_eq!(set.len(), 4); // 100 was inserted"] # [doc = " ```"] # [inline] # [unstable (feature = "btree_set_entry" , issue = "133549")] pub fn get_or_insert (& mut self , value : T) -> & T where T : Ord , { self . map . entry (value) . insert_entry (SetValZST) . into_key () } # [doc = " Inserts a value computed from `f` into the set if the given `value` is"] # [doc = " not present, then returns a reference to the value in the set."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(btree_set_entry)]"] # [doc = ""] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut set: BTreeSet<String> = [\"cat\", \"dog\", \"horse\"]"] # [doc = "     .iter().map(|&pet| pet.to_owned()).collect();"] # [doc = ""] # [doc = " assert_eq!(set.len(), 3);"] # [doc = " for &pet in &[\"cat\", \"dog\", \"fish\"] {"] # [doc = "     let value = set.get_or_insert_with(pet, str::to_owned);"] # [doc = "     assert_eq!(value, pet);"] # [doc = " }"] # [doc = " assert_eq!(set.len(), 4); // a new \"fish\" was inserted"] # [doc = " ```"] # [inline] # [unstable (feature = "btree_set_entry" , issue = "133549")] pub fn get_or_insert_with < Q : ? Sized , F > (& mut self , value : & Q , f : F) -> & T where T : Borrow < Q > + Ord , Q : Ord , F : FnOnce (& Q) -> T , { self . map . get_or_insert_with (value , f) } # [doc = " Gets the given value's corresponding entry in the set for in-place manipulation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(btree_set_entry)]"] # [doc = ""] # [doc = " use std::collections::BTreeSet;"] # [doc = " use std::collections::btree_set::Entry::*;"] # [doc = ""] # [doc = " let mut singles = BTreeSet::new();"] # [doc = " let mut dupes = BTreeSet::new();"] # [doc = ""] # [doc = " for ch in \"a short treatise on fungi\".chars() {"] # [doc = "     if let Vacant(dupe_entry) = dupes.entry(ch) {"] # [doc = "         // We haven't already seen a duplicate, so"] # [doc = "         // check if we've at least seen it once."] # [doc = "         match singles.entry(ch) {"] # [doc = "             Vacant(single_entry) => {"] # [doc = "                 // We found a new character for the first time."] # [doc = "                 single_entry.insert()"] # [doc = "             }"] # [doc = "             Occupied(single_entry) => {"] # [doc = "                 // We've already seen this once, \"move\" it to dupes."] # [doc = "                 single_entry.remove();"] # [doc = "                 dupe_entry.insert();"] # [doc = "             }"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " assert!(!singles.contains(&'t') && dupes.contains(&'t'));"] # [doc = " assert!(singles.contains(&'u') && !dupes.contains(&'u'));"] # [doc = " assert!(!singles.contains(&'v') && !dupes.contains(&'v'));"] # [doc = " ```"] # [inline] # [unstable (feature = "btree_set_entry" , issue = "133549")] pub fn entry (& mut self , value : T) -> Entry < '_ , T , A > where T : Ord , { match self . map . entry (value) { map :: Entry :: Occupied (entry) => Entry :: Occupied (OccupiedEntry { inner : entry }) , map :: Entry :: Vacant (entry) => Entry :: Vacant (VacantEntry { inner : entry }) , } } # [doc = " If the set contains an element equal to the value, removes it from the"] # [doc = " set and drops it. Returns whether such an element was present."] # [doc = ""] # [doc = " The value may be any borrowed form of the set's element type,"] # [doc = " but the ordering on the borrowed form *must* match the"] # [doc = " ordering on the element type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut set = BTreeSet::new();"] # [doc = ""] # [doc = " set.insert(2);"] # [doc = " assert_eq!(set.remove(&2), true);"] # [doc = " assert_eq!(set.remove(&2), false);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] pub fn remove < Q : ? Sized > (& mut self , value : & Q) -> bool where T : Borrow < Q > + Ord , Q : Ord , { self . map . remove (value) . is_some () } # [doc = " Removes and returns the element in the set, if any, that is equal to"] # [doc = " the value."] # [doc = ""] # [doc = " The value may be any borrowed form of the set's element type,"] # [doc = " but the ordering on the borrowed form *must* match the"] # [doc = " ordering on the element type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut set = BTreeSet::from([1, 2, 3]);"] # [doc = " assert_eq!(set.take(&2), Some(2));"] # [doc = " assert_eq!(set.take(&2), None);"] # [doc = " ```"] # [stable (feature = "set_recovery" , since = "1.9.0")] pub fn take < Q : ? Sized > (& mut self , value : & Q) -> Option < T > where T : Borrow < Q > + Ord , Q : Ord , { self . map . remove_entry (value) . map (| (k , _) | k) } # [doc = " Retains only the elements specified by the predicate."] # [doc = ""] # [doc = " In other words, remove all elements `e` for which `f(&e)` returns `false`."] # [doc = " The elements are visited in ascending order."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut set = BTreeSet::from([1, 2, 3, 4, 5, 6]);"] # [doc = " // Keep only the even numbers."] # [doc = " set.retain(|&k| k % 2 == 0);"] # [doc = " assert!(set.iter().eq([2, 4, 6].iter()));"] # [doc = " ```"] # [stable (feature = "btree_retain" , since = "1.53.0")] pub fn retain < F > (& mut self , mut f : F) where T : Ord , F : FnMut (& T) -> bool , { self . extract_if (.. , | v | ! f (v)) . for_each (drop) ; } # [doc = " Moves all elements from `other` into `self`, leaving `other` empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut a = BTreeSet::new();"] # [doc = " a.insert(1);"] # [doc = " a.insert(2);"] # [doc = " a.insert(3);"] # [doc = ""] # [doc = " let mut b = BTreeSet::new();"] # [doc = " b.insert(3);"] # [doc = " b.insert(4);"] # [doc = " b.insert(5);"] # [doc = ""] # [doc = " a.append(&mut b);"] # [doc = ""] # [doc = " assert_eq!(a.len(), 5);"] # [doc = " assert_eq!(b.len(), 0);"] # [doc = ""] # [doc = " assert!(a.contains(&1));"] # [doc = " assert!(a.contains(&2));"] # [doc = " assert!(a.contains(&3));"] # [doc = " assert!(a.contains(&4));"] # [doc = " assert!(a.contains(&5));"] # [doc = " ```"] # [stable (feature = "btree_append" , since = "1.11.0")] pub fn append (& mut self , other : & mut Self) where T : Ord , A : Clone , { self . map . append (& mut other . map) ; } # [doc = " Splits the collection into two at the value. Returns a new collection"] # [doc = " with all elements greater than or equal to the value."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut a = BTreeSet::new();"] # [doc = " a.insert(1);"] # [doc = " a.insert(2);"] # [doc = " a.insert(3);"] # [doc = " a.insert(17);"] # [doc = " a.insert(41);"] # [doc = ""] # [doc = " let b = a.split_off(&3);"] # [doc = ""] # [doc = " assert_eq!(a.len(), 2);"] # [doc = " assert_eq!(b.len(), 3);"] # [doc = ""] # [doc = " assert!(a.contains(&1));"] # [doc = " assert!(a.contains(&2));"] # [doc = ""] # [doc = " assert!(b.contains(&3));"] # [doc = " assert!(b.contains(&17));"] # [doc = " assert!(b.contains(&41));"] # [doc = " ```"] # [stable (feature = "btree_split_off" , since = "1.11.0")] pub fn split_off < Q : ? Sized + Ord > (& mut self , value : & Q) -> Self where T : Borrow < Q > + Ord , A : Clone , { BTreeSet { map : self . map . split_off (value) } } # [doc = " Creates an iterator that visits elements in the specified range in ascending order and"] # [doc = " uses a closure to determine if an element should be removed."] # [doc = ""] # [doc = " If the closure returns `true`, the element is removed from the set and"] # [doc = " yielded. If the closure returns `false`, or panics, the element remains"] # [doc = " in the set and will not be yielded."] # [doc = ""] # [doc = " If the returned `ExtractIf` is not exhausted, e.g. because it is dropped without iterating"] # [doc = " or the iteration short-circuits, then the remaining elements will be retained."] # [doc = " Use [`retain`] with a negated predicate if you do not need the returned iterator."] # [doc = ""] # [doc = " [`retain`]: BTreeSet::retain"] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " // Splitting a set into even and odd values, reusing the original set:"] # [doc = " let mut set: BTreeSet<i32> = (0..8).collect();"] # [doc = " let evens: BTreeSet<_> = set.extract_if(.., |v| v % 2 == 0).collect();"] # [doc = " let odds = set;"] # [doc = " assert_eq!(evens.into_iter().collect::<Vec<_>>(), vec![0, 2, 4, 6]);"] # [doc = " assert_eq!(odds.into_iter().collect::<Vec<_>>(), vec![1, 3, 5, 7]);"] # [doc = ""] # [doc = " // Splitting a set into low and high halves, reusing the original set:"] # [doc = " let mut set: BTreeSet<i32> = (0..8).collect();"] # [doc = " let low: BTreeSet<_> = set.extract_if(0..4, |_v| true).collect();"] # [doc = " let high = set;"] # [doc = " assert_eq!(low.into_iter().collect::<Vec<_>>(), [0, 1, 2, 3]);"] # [doc = " assert_eq!(high.into_iter().collect::<Vec<_>>(), [4, 5, 6, 7]);"] # [doc = " ```"] # [stable (feature = "btree_extract_if" , since = "1.91.0")] pub fn extract_if < F , R > (& mut self , range : R , pred : F) -> ExtractIf < '_ , T , R , F , A > where T : Ord , R : RangeBounds < T > , F : FnMut (& T) -> bool , { let (inner , alloc) = self . map . extract_if_inner (range) ; ExtractIf { pred , inner , alloc } } # [doc = " Gets an iterator that visits the elements in the `BTreeSet` in ascending"] # [doc = " order."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let set = BTreeSet::from([3, 1, 2]);"] # [doc = " let mut set_iter = set.iter();"] # [doc = " assert_eq!(set_iter.next(), Some(&1));"] # [doc = " assert_eq!(set_iter.next(), Some(&2));"] # [doc = " assert_eq!(set_iter.next(), Some(&3));"] # [doc = " assert_eq!(set_iter.next(), None);"] # [doc = " ```"] # [stable (feature = "rust1" , since = "1.0.0")] # [cfg_attr (not (test) , rustc_diagnostic_item = "btreeset_iter")] pub fn iter (& self) -> Iter < '_ , T > { Iter { iter : self . map . keys () } } # [doc = " Returns the number of elements in the set."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut v = BTreeSet::new();"] # [doc = " assert_eq!(v.len(), 0);"] # [doc = " v.insert(1);"] # [doc = " assert_eq!(v.len(), 1);"] # [doc = " ```"] # [must_use] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_unstable (feature = "const_btree_len" , issue = "71835" , implied_by = "const_btree_new")] # [rustc_confusables ("length" , "size")] pub const fn len (& self) -> usize { self . map . len () } # [doc = " Returns `true` if the set contains no elements."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let mut v = BTreeSet::new();"] # [doc = " assert!(v.is_empty());"] # [doc = " v.insert(1);"] # [doc = " assert!(!v.is_empty());"] # [doc = " ```"] # [must_use] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_const_unstable (feature = "const_btree_len" , issue = "71835" , implied_by = "const_btree_new")] pub const fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Returns a [`Cursor`] pointing at the gap before the smallest element"] # [doc = " greater than the given bound."] # [doc = ""] # [doc = " Passing `Bound::Included(x)` will return a cursor pointing to the"] # [doc = " gap before the smallest element greater than or equal to `x`."] # [doc = ""] # [doc = " Passing `Bound::Excluded(x)` will return a cursor pointing to the"] # [doc = " gap before the smallest element greater than `x`."] # [doc = ""] # [doc = " Passing `Bound::Unbounded` will return a cursor pointing to the"] # [doc = " gap before the smallest element in the set."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(btree_cursors)]"] # [doc = ""] # [doc = " use std::collections::BTreeSet;"] # [doc = " use std::ops::Bound;"] # [doc = ""] # [doc = " let set = BTreeSet::from([1, 2, 3, 4]);"] # [doc = ""] # [doc = " let cursor = set.lower_bound(Bound::Included(&2));"] # [doc = " assert_eq!(cursor.peek_prev(), Some(&1));"] # [doc = " assert_eq!(cursor.peek_next(), Some(&2));"] # [doc = ""] # [doc = " let cursor = set.lower_bound(Bound::Excluded(&2));"] # [doc = " assert_eq!(cursor.peek_prev(), Some(&2));"] # [doc = " assert_eq!(cursor.peek_next(), Some(&3));"] # [doc = ""] # [doc = " let cursor = set.lower_bound(Bound::Unbounded);"] # [doc = " assert_eq!(cursor.peek_prev(), None);"] # [doc = " assert_eq!(cursor.peek_next(), Some(&1));"] # [doc = " ```"] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn lower_bound < Q : ? Sized > (& self , bound : Bound < & Q >) -> Cursor < '_ , T > where T : Borrow < Q > + Ord , Q : Ord , { Cursor { inner : self . map . lower_bound (bound) } } # [doc = " Returns a [`CursorMut`] pointing at the gap before the smallest element"] # [doc = " greater than the given bound."] # [doc = ""] # [doc = " Passing `Bound::Included(x)` will return a cursor pointing to the"] # [doc = " gap before the smallest element greater than or equal to `x`."] # [doc = ""] # [doc = " Passing `Bound::Excluded(x)` will return a cursor pointing to the"] # [doc = " gap before the smallest element greater than `x`."] # [doc = ""] # [doc = " Passing `Bound::Unbounded` will return a cursor pointing to the"] # [doc = " gap before the smallest element in the set."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(btree_cursors)]"] # [doc = ""] # [doc = " use std::collections::BTreeSet;"] # [doc = " use std::ops::Bound;"] # [doc = ""] # [doc = " let mut set = BTreeSet::from([1, 2, 3, 4]);"] # [doc = ""] # [doc = " let mut cursor = set.lower_bound_mut(Bound::Included(&2));"] # [doc = " assert_eq!(cursor.peek_prev(), Some(&1));"] # [doc = " assert_eq!(cursor.peek_next(), Some(&2));"] # [doc = ""] # [doc = " let mut cursor = set.lower_bound_mut(Bound::Excluded(&2));"] # [doc = " assert_eq!(cursor.peek_prev(), Some(&2));"] # [doc = " assert_eq!(cursor.peek_next(), Some(&3));"] # [doc = ""] # [doc = " let mut cursor = set.lower_bound_mut(Bound::Unbounded);"] # [doc = " assert_eq!(cursor.peek_prev(), None);"] # [doc = " assert_eq!(cursor.peek_next(), Some(&1));"] # [doc = " ```"] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn lower_bound_mut < Q : ? Sized > (& mut self , bound : Bound < & Q >) -> CursorMut < '_ , T , A > where T : Borrow < Q > + Ord , Q : Ord , { CursorMut { inner : self . map . lower_bound_mut (bound) } } # [doc = " Returns a [`Cursor`] pointing at the gap after the greatest element"] # [doc = " smaller than the given bound."] # [doc = ""] # [doc = " Passing `Bound::Included(x)` will return a cursor pointing to the"] # [doc = " gap after the greatest element smaller than or equal to `x`."] # [doc = ""] # [doc = " Passing `Bound::Excluded(x)` will return a cursor pointing to the"] # [doc = " gap after the greatest element smaller than `x`."] # [doc = ""] # [doc = " Passing `Bound::Unbounded` will return a cursor pointing to the"] # [doc = " gap after the greatest element in the set."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(btree_cursors)]"] # [doc = ""] # [doc = " use std::collections::BTreeSet;"] # [doc = " use std::ops::Bound;"] # [doc = ""] # [doc = " let set = BTreeSet::from([1, 2, 3, 4]);"] # [doc = ""] # [doc = " let cursor = set.upper_bound(Bound::Included(&3));"] # [doc = " assert_eq!(cursor.peek_prev(), Some(&3));"] # [doc = " assert_eq!(cursor.peek_next(), Some(&4));"] # [doc = ""] # [doc = " let cursor = set.upper_bound(Bound::Excluded(&3));"] # [doc = " assert_eq!(cursor.peek_prev(), Some(&2));"] # [doc = " assert_eq!(cursor.peek_next(), Some(&3));"] # [doc = ""] # [doc = " let cursor = set.upper_bound(Bound::Unbounded);"] # [doc = " assert_eq!(cursor.peek_prev(), Some(&4));"] # [doc = " assert_eq!(cursor.peek_next(), None);"] # [doc = " ```"] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn upper_bound < Q : ? Sized > (& self , bound : Bound < & Q >) -> Cursor < '_ , T > where T : Borrow < Q > + Ord , Q : Ord , { Cursor { inner : self . map . upper_bound (bound) } } # [doc = " Returns a [`CursorMut`] pointing at the gap after the greatest element"] # [doc = " smaller than the given bound."] # [doc = ""] # [doc = " Passing `Bound::Included(x)` will return a cursor pointing to the"] # [doc = " gap after the greatest element smaller than or equal to `x`."] # [doc = ""] # [doc = " Passing `Bound::Excluded(x)` will return a cursor pointing to the"] # [doc = " gap after the greatest element smaller than `x`."] # [doc = ""] # [doc = " Passing `Bound::Unbounded` will return a cursor pointing to the"] # [doc = " gap after the greatest element in the set."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(btree_cursors)]"] # [doc = ""] # [doc = " use std::collections::BTreeSet;"] # [doc = " use std::ops::Bound;"] # [doc = ""] # [doc = " let mut set = BTreeSet::from([1, 2, 3, 4]);"] # [doc = ""] # [doc = " let mut cursor = set.upper_bound_mut(Bound::Included(&3));"] # [doc = " assert_eq!(cursor.peek_prev(), Some(&3));"] # [doc = " assert_eq!(cursor.peek_next(), Some(&4));"] # [doc = ""] # [doc = " let mut cursor = set.upper_bound_mut(Bound::Excluded(&3));"] # [doc = " assert_eq!(cursor.peek_prev(), Some(&2));"] # [doc = " assert_eq!(cursor.peek_next(), Some(&3));"] # [doc = ""] # [doc = " let mut cursor = set.upper_bound_mut(Bound::Unbounded);"] # [doc = " assert_eq!(cursor.peek_prev(), Some(&4));"] # [doc = " assert_eq!(cursor.peek_next(), None);"] # [doc = " ```"] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn upper_bound_mut < Q : ? Sized > (& mut self , bound : Bound < & Q >) -> CursorMut < '_ , T , A > where T : Borrow < Q > + Ord , Q : Ord , { CursorMut { inner : self . map . upper_bound_mut (bound) } } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : Ord > FromIterator < T > for BTreeSet < T > { fn from_iter < I : IntoIterator < Item = T > > (iter : I) -> BTreeSet < T > { let mut inputs : Vec < _ > = iter . into_iter () . collect () ; if inputs . is_empty () { return BTreeSet :: new () ; } inputs . sort () ; BTreeSet :: from_sorted_iter (inputs . into_iter () , Global) } }}}
mkitem!{mkimpl!{impl < T : Ord , A : Allocator + Clone > BTreeSet < T , A > { fn from_sorted_iter < I : Iterator < Item = T > > (iter : I , alloc : A) -> BTreeSet < T , A > { let iter = iter . map (| k | (k , SetValZST :: default ())) ; let map = BTreeMap :: bulk_build_from_sorted_iter (iter , alloc) ; BTreeSet { map } } }}}
mkitem!{mkimpl!{# [stable (feature = "std_collections_from_array" , since = "1.56.0")] impl < T : Ord , const N : usize > From < [T ; N] > for BTreeSet < T > { # [doc = " Converts a `[T; N]` into a `BTreeSet<T>`."] # [doc = ""] # [doc = " If the array contains any equal values,"] # [doc = " all but one will be dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let set1 = BTreeSet::from([1, 2, 3, 4]);"] # [doc = " let set2: BTreeSet<_> = [1, 2, 3, 4].into();"] # [doc = " assert_eq!(set1, set2);"] # [doc = " ```"] fn from (mut arr : [T ; N]) -> Self { if N == 0 { return BTreeSet :: new () ; } arr . sort () ; BTreeSet :: from_sorted_iter (IntoIterator :: into_iter (arr) , Global) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T , A : Allocator + Clone > IntoIterator for BTreeSet < T , A > { type Item = T ; type IntoIter = IntoIter < T , A > ; # [doc = " Gets an iterator for moving out the `BTreeSet`'s contents in ascending order."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let set = BTreeSet::from([1, 2, 3, 4]);"] # [doc = ""] # [doc = " let v: Vec<_> = set.into_iter().collect();"] # [doc = " assert_eq!(v, [1, 2, 3, 4]);"] # [doc = " ```"] fn into_iter (self) -> IntoIter < T , A > { IntoIter { iter : self . map . into_iter () } } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T , A : Allocator + Clone > IntoIterator for & 'a BTreeSet < T , A > { type Item = & 'a T ; type IntoIter = Iter < 'a , T > ; fn into_iter (self) -> Iter < 'a , T > { self . iter () } }}}
mkitem!{mkstruct!{# [doc = " An iterator produced by calling `extract_if` on BTreeSet."] # [stable (feature = "btree_extract_if" , since = "1.91.0")] # [must_use = "iterators are lazy and do nothing unless consumed"] pub struct ExtractIf < 'a , T , R , F , # [unstable (feature = "allocator_api" , issue = "32838")] A : Allocator + Clone = Global , > { pred : F , inner : super :: map :: ExtractIfInner < 'a , T , SetValZST , R > , # [doc = " The BTreeMap will outlive this IntoIter so we don't care about drop order for `alloc`."] alloc : A , }}}
mkitem!{mkimpl!{# [stable (feature = "btree_extract_if" , since = "1.91.0")] impl < T , R , F , A > fmt :: Debug for ExtractIf < '_ , T , R , F , A > where T : fmt :: Debug , A : Allocator + Clone , { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("ExtractIf") . field ("peek" , & self . inner . peek () . map (| (k , _) | k)) . finish_non_exhaustive () } }}}
mkitem!{mkimpl!{# [stable (feature = "btree_extract_if" , since = "1.91.0")] impl < T , R , F , A : Allocator + Clone > Iterator for ExtractIf < '_ , T , R , F , A > where T : PartialOrd , R : RangeBounds < T > , F : FnMut (& T) -> bool , { type Item = T ; fn next (& mut self) -> Option < T > { let pred = & mut self . pred ; let mut mapped_pred = | k : & T , _v : & mut SetValZST | pred (k) ; self . inner . next (& mut mapped_pred , self . alloc . clone ()) . map (| (k , _) | k) } fn size_hint (& self) -> (usize , Option < usize >) { self . inner . size_hint () } }}}
mkitem!{mkimpl!{# [stable (feature = "btree_extract_if" , since = "1.91.0")] impl < T , R , F , A : Allocator + Clone > FusedIterator for ExtractIf < '_ , T , R , F , A > where T : PartialOrd , R : RangeBounds < T > , F : FnMut (& T) -> bool , { }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : Ord , A : Allocator + Clone > Extend < T > for BTreeSet < T , A > { # [inline] fn extend < Iter : IntoIterator < Item = T > > (& mut self , iter : Iter) { iter . into_iter () . for_each (move | elem | { self . insert (elem) ; }) ; } # [inline] fn extend_one (& mut self , elem : T) { self . insert (elem) ; } }}}
mkitem!{mkimpl!{# [stable (feature = "extend_ref" , since = "1.2.0")] impl < 'a , T : 'a + Ord + Copy , A : Allocator + Clone > Extend < & 'a T > for BTreeSet < T , A > { fn extend < I : IntoIterator < Item = & 'a T > > (& mut self , iter : I) { self . extend (iter . into_iter () . cloned ()) ; } # [inline] fn extend_one (& mut self , & elem : & 'a T) { self . insert (elem) ; } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T > Default for BTreeSet < T > { # [doc = " Creates an empty `BTreeSet`."] fn default () -> BTreeSet < T > { BTreeSet :: new () } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : Ord + Clone , A : Allocator + Clone > Sub < & BTreeSet < T , A > > for & BTreeSet < T , A > { type Output = BTreeSet < T , A > ; # [doc = " Returns the difference of `self` and `rhs` as a new `BTreeSet<T>`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let a = BTreeSet::from([1, 2, 3]);"] # [doc = " let b = BTreeSet::from([3, 4, 5]);"] # [doc = ""] # [doc = " let result = &a - &b;"] # [doc = " assert_eq!(result, BTreeSet::from([1, 2]));"] # [doc = " ```"] fn sub (self , rhs : & BTreeSet < T , A >) -> BTreeSet < T , A > { BTreeSet :: from_sorted_iter (self . difference (rhs) . cloned () , ManuallyDrop :: into_inner (self . map . alloc . clone ()) ,) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : Ord + Clone , A : Allocator + Clone > BitXor < & BTreeSet < T , A > > for & BTreeSet < T , A > { type Output = BTreeSet < T , A > ; # [doc = " Returns the symmetric difference of `self` and `rhs` as a new `BTreeSet<T>`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let a = BTreeSet::from([1, 2, 3]);"] # [doc = " let b = BTreeSet::from([2, 3, 4]);"] # [doc = ""] # [doc = " let result = &a ^ &b;"] # [doc = " assert_eq!(result, BTreeSet::from([1, 4]));"] # [doc = " ```"] fn bitxor (self , rhs : & BTreeSet < T , A >) -> BTreeSet < T , A > { BTreeSet :: from_sorted_iter (self . symmetric_difference (rhs) . cloned () , ManuallyDrop :: into_inner (self . map . alloc . clone ()) ,) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : Ord + Clone , A : Allocator + Clone > BitAnd < & BTreeSet < T , A > > for & BTreeSet < T , A > { type Output = BTreeSet < T , A > ; # [doc = " Returns the intersection of `self` and `rhs` as a new `BTreeSet<T>`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let a = BTreeSet::from([1, 2, 3]);"] # [doc = " let b = BTreeSet::from([2, 3, 4]);"] # [doc = ""] # [doc = " let result = &a & &b;"] # [doc = " assert_eq!(result, BTreeSet::from([2, 3]));"] # [doc = " ```"] fn bitand (self , rhs : & BTreeSet < T , A >) -> BTreeSet < T , A > { BTreeSet :: from_sorted_iter (self . intersection (rhs) . cloned () , ManuallyDrop :: into_inner (self . map . alloc . clone ()) ,) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : Ord + Clone , A : Allocator + Clone > BitOr < & BTreeSet < T , A > > for & BTreeSet < T , A > { type Output = BTreeSet < T , A > ; # [doc = " Returns the union of `self` and `rhs` as a new `BTreeSet<T>`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " let a = BTreeSet::from([1, 2, 3]);"] # [doc = " let b = BTreeSet::from([3, 4, 5]);"] # [doc = ""] # [doc = " let result = &a | &b;"] # [doc = " assert_eq!(result, BTreeSet::from([1, 2, 3, 4, 5]));"] # [doc = " ```"] fn bitor (self , rhs : & BTreeSet < T , A >) -> BTreeSet < T , A > { BTreeSet :: from_sorted_iter (self . union (rhs) . cloned () , ManuallyDrop :: into_inner (self . map . alloc . clone ()) ,) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T : Debug , A : Allocator + Clone > Debug for BTreeSet < T , A > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_set () . entries (self . iter ()) . finish () } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T > Clone for Iter < '_ , T > { fn clone (& self) -> Self { Iter { iter : self . iter . clone () } } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T > Iterator for Iter < 'a , T > { type Item = & 'a T ; fn next (& mut self) -> Option < & 'a T > { self . iter . next () } fn size_hint (& self) -> (usize , Option < usize >) { self . iter . size_hint () } fn last (mut self) -> Option < & 'a T > { self . next_back () } fn min (mut self) -> Option < & 'a T > where & 'a T : Ord , { self . next () } fn max (mut self) -> Option < & 'a T > where & 'a T : Ord , { self . next_back () } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T > DoubleEndedIterator for Iter < 'a , T > { fn next_back (& mut self) -> Option < & 'a T > { self . iter . next_back () } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T > ExactSizeIterator for Iter < '_ , T > { fn len (& self) -> usize { self . iter . len () } }}}
mkitem!{mkimpl!{# [stable (feature = "fused" , since = "1.26.0")] impl < T > FusedIterator for Iter < '_ , T > { }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T , A : Allocator + Clone > Iterator for IntoIter < T , A > { type Item = T ; fn next (& mut self) -> Option < T > { self . iter . next () . map (| (k , _) | k) } fn size_hint (& self) -> (usize , Option < usize >) { self . iter . size_hint () } }}}
mkitem!{mkimpl!{# [stable (feature = "default_iters" , since = "1.70.0")] impl < T > Default for Iter < '_ , T > { # [doc = " Creates an empty `btree_set::Iter`."] # [doc = ""] # [doc = " ```"] # [doc = " # use std::collections::btree_set;"] # [doc = " let iter: btree_set::Iter<'_, u8> = Default::default();"] # [doc = " assert_eq!(iter.len(), 0);"] # [doc = " ```"] fn default () -> Self { Iter { iter : Default :: default () } } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T , A : Allocator + Clone > DoubleEndedIterator for IntoIter < T , A > { fn next_back (& mut self) -> Option < T > { self . iter . next_back () . map (| (k , _) | k) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T , A : Allocator + Clone > ExactSizeIterator for IntoIter < T , A > { fn len (& self) -> usize { self . iter . len () } }}}
mkitem!{mkimpl!{# [stable (feature = "fused" , since = "1.26.0")] impl < T , A : Allocator + Clone > FusedIterator for IntoIter < T , A > { }}}
mkitem!{mkimpl!{# [stable (feature = "default_iters" , since = "1.70.0")] impl < T , A > Default for IntoIter < T , A > where A : Allocator + Default + Clone , { # [doc = " Creates an empty `btree_set::IntoIter`."] # [doc = ""] # [doc = " ```"] # [doc = " # use std::collections::btree_set;"] # [doc = " let iter: btree_set::IntoIter<u8> = Default::default();"] # [doc = " assert_eq!(iter.len(), 0);"] # [doc = " ```"] fn default () -> Self { IntoIter { iter : Default :: default () } } }}}
mkitem!{mkimpl!{# [stable (feature = "btree_range" , since = "1.17.0")] impl < T > Clone for Range < '_ , T > { fn clone (& self) -> Self { Range { iter : self . iter . clone () } } }}}
mkitem!{mkimpl!{# [stable (feature = "btree_range" , since = "1.17.0")] impl < 'a , T > Iterator for Range < 'a , T > { type Item = & 'a T ; fn next (& mut self) -> Option < & 'a T > { self . iter . next () . map (| (k , _) | k) } fn last (mut self) -> Option < & 'a T > { self . next_back () } fn min (mut self) -> Option < & 'a T > where & 'a T : Ord , { self . next () } fn max (mut self) -> Option < & 'a T > where & 'a T : Ord , { self . next_back () } }}}
mkitem!{mkimpl!{# [stable (feature = "btree_range" , since = "1.17.0")] impl < 'a , T > DoubleEndedIterator for Range < 'a , T > { fn next_back (& mut self) -> Option < & 'a T > { self . iter . next_back () . map (| (k , _) | k) } }}}
mkitem!{mkimpl!{# [stable (feature = "fused" , since = "1.26.0")] impl < T > FusedIterator for Range < '_ , T > { }}}
mkitem!{mkimpl!{# [stable (feature = "default_iters" , since = "1.70.0")] impl < T > Default for Range < '_ , T > { # [doc = " Creates an empty `btree_set::Range`."] # [doc = ""] # [doc = " ```"] # [doc = " # use std::collections::btree_set;"] # [doc = " let iter: btree_set::Range<'_, u8> = Default::default();"] # [doc = " assert_eq!(iter.count(), 0);"] # [doc = " ```"] fn default () -> Self { Range { iter : Default :: default () } } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T , A : Allocator + Clone > Clone for Difference < '_ , T , A > { fn clone (& self) -> Self { Difference { inner : match & self . inner { DifferenceInner :: Stitch { self_iter , other_iter } => DifferenceInner :: Stitch { self_iter : self_iter . clone () , other_iter : other_iter . clone () , } , DifferenceInner :: Search { self_iter , other_set } => { DifferenceInner :: Search { self_iter : self_iter . clone () , other_set } } DifferenceInner :: Iterate (iter) => DifferenceInner :: Iterate (iter . clone ()) , } , } } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T : Ord , A : Allocator + Clone > Iterator for Difference < 'a , T , A > { type Item = & 'a T ; fn next (& mut self) -> Option < & 'a T > { match & mut self . inner { DifferenceInner :: Stitch { self_iter , other_iter } => { let mut self_next = self_iter . next () ? ; loop { match other_iter . peek () . map_or (Less , | other_next | self_next . cmp (other_next)) { Less => return Some (self_next) , Equal => { self_next = self_iter . next () ? ; other_iter . next () ; } Greater => { other_iter . next () ; } } } } DifferenceInner :: Search { self_iter , other_set } => loop { let self_next = self_iter . next () ? ; if ! other_set . contains (& self_next) { return Some (self_next) ; } } , DifferenceInner :: Iterate (iter) => iter . next () , } } fn size_hint (& self) -> (usize , Option < usize >) { let (self_len , other_len) = match & self . inner { DifferenceInner :: Stitch { self_iter , other_iter } => { (self_iter . len () , other_iter . len ()) } DifferenceInner :: Search { self_iter , other_set } => (self_iter . len () , other_set . len ()) , DifferenceInner :: Iterate (iter) => (iter . len () , 0) , } ; (self_len . saturating_sub (other_len) , Some (self_len)) } fn min (mut self) -> Option < & 'a T > { self . next () } }}}
mkitem!{mkimpl!{# [stable (feature = "fused" , since = "1.26.0")] impl < T : Ord , A : Allocator + Clone > FusedIterator for Difference < '_ , T , A > { }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T > Clone for SymmetricDifference < '_ , T > { fn clone (& self) -> Self { SymmetricDifference (self . 0 . clone ()) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T : Ord > Iterator for SymmetricDifference < 'a , T > { type Item = & 'a T ; fn next (& mut self) -> Option < & 'a T > { loop { let (a_next , b_next) = self . 0 . nexts (Self :: Item :: cmp) ; if a_next . and (b_next) . is_none () { return a_next . or (b_next) ; } } } fn size_hint (& self) -> (usize , Option < usize >) { let (a_len , b_len) = self . 0 . lens () ; (0 , Some (a_len + b_len)) } fn min (mut self) -> Option < & 'a T > { self . next () } }}}
mkitem!{mkimpl!{# [stable (feature = "fused" , since = "1.26.0")] impl < T : Ord > FusedIterator for SymmetricDifference < '_ , T > { }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T , A : Allocator + Clone > Clone for Intersection < '_ , T , A > { fn clone (& self) -> Self { Intersection { inner : match & self . inner { IntersectionInner :: Stitch { a , b } => { IntersectionInner :: Stitch { a : a . clone () , b : b . clone () } } IntersectionInner :: Search { small_iter , large_set } => { IntersectionInner :: Search { small_iter : small_iter . clone () , large_set } } IntersectionInner :: Answer (answer) => IntersectionInner :: Answer (* answer) , } , } } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T : Ord , A : Allocator + Clone > Iterator for Intersection < 'a , T , A > { type Item = & 'a T ; fn next (& mut self) -> Option < & 'a T > { match & mut self . inner { IntersectionInner :: Stitch { a , b } => { let mut a_next = a . next () ? ; let mut b_next = b . next () ? ; loop { match a_next . cmp (b_next) { Less => a_next = a . next () ? , Greater => b_next = b . next () ? , Equal => return Some (a_next) , } } } IntersectionInner :: Search { small_iter , large_set } => loop { let small_next = small_iter . next () ? ; if large_set . contains (& small_next) { return Some (small_next) ; } } , IntersectionInner :: Answer (answer) => answer . take () , } } fn size_hint (& self) -> (usize , Option < usize >) { match & self . inner { IntersectionInner :: Stitch { a , b } => (0 , Some (min (a . len () , b . len ()))) , IntersectionInner :: Search { small_iter , .. } => (0 , Some (small_iter . len ())) , IntersectionInner :: Answer (None) => (0 , Some (0)) , IntersectionInner :: Answer (Some (_)) => (1 , Some (1)) , } } fn min (mut self) -> Option < & 'a T > { self . next () } }}}
mkitem!{mkimpl!{# [stable (feature = "fused" , since = "1.26.0")] impl < T : Ord , A : Allocator + Clone > FusedIterator for Intersection < '_ , T , A > { }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T > Clone for Union < '_ , T > { fn clone (& self) -> Self { Union (self . 0 . clone ()) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T : Ord > Iterator for Union < 'a , T > { type Item = & 'a T ; fn next (& mut self) -> Option < & 'a T > { let (a_next , b_next) = self . 0 . nexts (Self :: Item :: cmp) ; a_next . or (b_next) } fn size_hint (& self) -> (usize , Option < usize >) { let (a_len , b_len) = self . 0 . lens () ; (max (a_len , b_len) , Some (a_len + b_len)) } fn min (mut self) -> Option < & 'a T > { self . next () } }}}
mkitem!{mkimpl!{# [stable (feature = "fused" , since = "1.26.0")] impl < T : Ord > FusedIterator for Union < '_ , T > { }}}
mkitem!{mkstruct!{# [doc = " A cursor over a `BTreeSet`."] # [doc = ""] # [doc = " A `Cursor` is like an iterator, except that it can freely seek back-and-forth."] # [doc = ""] # [doc = " Cursors always point to a gap between two elements in the set, and can"] # [doc = " operate on the two immediately adjacent elements."] # [doc = ""] # [doc = " A `Cursor` is created with the [`BTreeSet::lower_bound`] and [`BTreeSet::upper_bound`] methods."] # [derive (Clone)] # [unstable (feature = "btree_cursors" , issue = "107540")] pub struct Cursor < 'a , K : 'a > { inner : super :: map :: Cursor < 'a , K , SetValZST > , }}}
mkitem!{mkimpl!{# [unstable (feature = "btree_cursors" , issue = "107540")] impl < K : Debug > Debug for Cursor < '_ , K > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . write_str ("Cursor") } }}}
mkitem!{mkstruct!{# [doc = " A cursor over a `BTreeSet` with editing operations."] # [doc = ""] # [doc = " A `Cursor` is like an iterator, except that it can freely seek back-and-forth, and can"] # [doc = " safely mutate the set during iteration. This is because the lifetime of its yielded"] # [doc = " references is tied to its own lifetime, instead of just the underlying map. This means"] # [doc = " cursors cannot yield multiple elements at once."] # [doc = ""] # [doc = " Cursors always point to a gap between two elements in the set, and can"] # [doc = " operate on the two immediately adjacent elements."] # [doc = ""] # [doc = " A `CursorMut` is created with the [`BTreeSet::lower_bound_mut`] and [`BTreeSet::upper_bound_mut`]"] # [doc = " methods."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub struct CursorMut < 'a , K : 'a , # [unstable (feature = "allocator_api" , issue = "32838")] A = Global > { inner : super :: map :: CursorMut < 'a , K , SetValZST , A > , }}}
mkitem!{mkimpl!{# [unstable (feature = "btree_cursors" , issue = "107540")] impl < K : Debug , A > Debug for CursorMut < '_ , K , A > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . write_str ("CursorMut") } }}}
mkitem!{mkstruct!{# [doc = " A cursor over a `BTreeSet` with editing operations, and which allows"] # [doc = " mutating elements."] # [doc = ""] # [doc = " A `Cursor` is like an iterator, except that it can freely seek back-and-forth, and can"] # [doc = " safely mutate the set during iteration. This is because the lifetime of its yielded"] # [doc = " references is tied to its own lifetime, instead of just the underlying set. This means"] # [doc = " cursors cannot yield multiple elements at once."] # [doc = ""] # [doc = " Cursors always point to a gap between two elements in the set, and can"] # [doc = " operate on the two immediately adjacent elements."] # [doc = ""] # [doc = " A `CursorMutKey` is created from a [`CursorMut`] with the"] # [doc = " [`CursorMut::with_mutable_key`] method."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Since this cursor allows mutating elements, you must ensure that the"] # [doc = " `BTreeSet` invariants are maintained. Specifically:"] # [doc = ""] # [doc = " * The newly inserted element must be unique in the tree."] # [doc = " * All elements in the tree must remain in sorted order."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub struct CursorMutKey < 'a , K : 'a , # [unstable (feature = "allocator_api" , issue = "32838")] A = Global , > { inner : super :: map :: CursorMutKey < 'a , K , SetValZST , A > , }}}
mkitem!{mkimpl!{# [unstable (feature = "btree_cursors" , issue = "107540")] impl < K : Debug , A > Debug for CursorMutKey < '_ , K , A > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . write_str ("CursorMutKey") } }}}
mkitem!{mkimpl!{impl < 'a , K > Cursor < 'a , K > { # [doc = " Advances the cursor to the next gap, returning the element that it"] # [doc = " moved over."] # [doc = ""] # [doc = " If the cursor is already at the end of the set then `None` is returned"] # [doc = " and the cursor is not moved."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn next (& mut self) -> Option < & 'a K > { self . inner . next () . map (| (k , _) | k) } # [doc = " Advances the cursor to the previous gap, returning the element that it"] # [doc = " moved over."] # [doc = ""] # [doc = " If the cursor is already at the start of the set then `None` is returned"] # [doc = " and the cursor is not moved."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn prev (& mut self) -> Option < & 'a K > { self . inner . prev () . map (| (k , _) | k) } # [doc = " Returns a reference to next element without moving the cursor."] # [doc = ""] # [doc = " If the cursor is at the end of the set then `None` is returned"] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn peek_next (& self) -> Option < & 'a K > { self . inner . peek_next () . map (| (k , _) | k) } # [doc = " Returns a reference to the previous element without moving the cursor."] # [doc = ""] # [doc = " If the cursor is at the start of the set then `None` is returned."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn peek_prev (& self) -> Option < & 'a K > { self . inner . peek_prev () . map (| (k , _) | k) } }}}
mkitem!{mkimpl!{impl < 'a , T , A > CursorMut < 'a , T , A > { # [doc = " Advances the cursor to the next gap, returning the element that it"] # [doc = " moved over."] # [doc = ""] # [doc = " If the cursor is already at the end of the set then `None` is returned"] # [doc = " and the cursor is not moved."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn next (& mut self) -> Option < & T > { self . inner . next () . map (| (k , _) | k) } # [doc = " Advances the cursor to the previous gap, returning the element that it"] # [doc = " moved over."] # [doc = ""] # [doc = " If the cursor is already at the start of the set then `None` is returned"] # [doc = " and the cursor is not moved."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn prev (& mut self) -> Option < & T > { self . inner . prev () . map (| (k , _) | k) } # [doc = " Returns a reference to the next element without moving the cursor."] # [doc = ""] # [doc = " If the cursor is at the end of the set then `None` is returned."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn peek_next (& mut self) -> Option < & T > { self . inner . peek_next () . map (| (k , _) | k) } # [doc = " Returns a reference to the previous element without moving the cursor."] # [doc = ""] # [doc = " If the cursor is at the start of the set then `None` is returned."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn peek_prev (& mut self) -> Option < & T > { self . inner . peek_prev () . map (| (k , _) | k) } # [doc = " Returns a read-only cursor pointing to the same location as the"] # [doc = " `CursorMut`."] # [doc = ""] # [doc = " The lifetime of the returned `Cursor` is bound to that of the"] # [doc = " `CursorMut`, which means it cannot outlive the `CursorMut` and that the"] # [doc = " `CursorMut` is frozen for the lifetime of the `Cursor`."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn as_cursor (& self) -> Cursor < '_ , T > { Cursor { inner : self . inner . as_cursor () } } # [doc = " Converts the cursor into a [`CursorMutKey`], which allows mutating"] # [doc = " elements in the tree."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Since this cursor allows mutating elements, you must ensure that the"] # [doc = " `BTreeSet` invariants are maintained. Specifically:"] # [doc = ""] # [doc = " * The newly inserted element must be unique in the tree."] # [doc = " * All elements in the tree must remain in sorted order."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub unsafe fn with_mutable_key (self) -> CursorMutKey < 'a , T , A > { CursorMutKey { inner : unsafe { self . inner . with_mutable_key () } } } }}}
mkitem!{mkimpl!{impl < 'a , T , A > CursorMutKey < 'a , T , A > { # [doc = " Advances the cursor to the next gap, returning the  element that it"] # [doc = " moved over."] # [doc = ""] # [doc = " If the cursor is already at the end of the set then `None` is returned"] # [doc = " and the cursor is not moved."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn next (& mut self) -> Option < & mut T > { self . inner . next () . map (| (k , _) | k) } # [doc = " Advances the cursor to the previous gap, returning the element that it"] # [doc = " moved over."] # [doc = ""] # [doc = " If the cursor is already at the start of the set then `None` is returned"] # [doc = " and the cursor is not moved."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn prev (& mut self) -> Option < & mut T > { self . inner . prev () . map (| (k , _) | k) } # [doc = " Returns a reference to the next element without moving the cursor."] # [doc = ""] # [doc = " If the cursor is at the end of the set then `None` is returned"] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn peek_next (& mut self) -> Option < & mut T > { self . inner . peek_next () . map (| (k , _) | k) } # [doc = " Returns a reference to the previous element without moving the cursor."] # [doc = ""] # [doc = " If the cursor is at the start of the set then `None` is returned."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn peek_prev (& mut self) -> Option < & mut T > { self . inner . peek_prev () . map (| (k , _) | k) } # [doc = " Returns a read-only cursor pointing to the same location as the"] # [doc = " `CursorMutKey`."] # [doc = ""] # [doc = " The lifetime of the returned `Cursor` is bound to that of the"] # [doc = " `CursorMutKey`, which means it cannot outlive the `CursorMutKey` and that the"] # [doc = " `CursorMutKey` is frozen for the lifetime of the `Cursor`."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn as_cursor (& self) -> Cursor < '_ , T > { Cursor { inner : self . inner . as_cursor () } } }}}
mkitem!{mkimpl!{impl < 'a , T : Ord , A : Allocator + Clone > CursorMut < 'a , T , A > { # [doc = " Inserts a new element into the set in the gap that the"] # [doc = " cursor is currently pointing to."] # [doc = ""] # [doc = " After the insertion the cursor will be pointing at the gap before the"] # [doc = " newly inserted element."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " You must ensure that the `BTreeSet` invariants are maintained."] # [doc = " Specifically:"] # [doc = ""] # [doc = " * The newly inserted element must be unique in the tree."] # [doc = " * All elements in the tree must remain in sorted order."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub unsafe fn insert_after_unchecked (& mut self , value : T) { unsafe { self . inner . insert_after_unchecked (value , SetValZST) } } # [doc = " Inserts a new element into the set in the gap that the"] # [doc = " cursor is currently pointing to."] # [doc = ""] # [doc = " After the insertion the cursor will be pointing at the gap after the"] # [doc = " newly inserted element."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " You must ensure that the `BTreeSet` invariants are maintained."] # [doc = " Specifically:"] # [doc = ""] # [doc = " * The newly inserted element must be unique in the tree."] # [doc = " * All elements in the tree must remain in sorted order."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub unsafe fn insert_before_unchecked (& mut self , value : T) { unsafe { self . inner . insert_before_unchecked (value , SetValZST) } } # [doc = " Inserts a new element into the set in the gap that the"] # [doc = " cursor is currently pointing to."] # [doc = ""] # [doc = " After the insertion the cursor will be pointing at the gap before the"] # [doc = " newly inserted element."] # [doc = ""] # [doc = " If the inserted element is not greater than the element before the"] # [doc = " cursor (if any), or if it not less than the element after the cursor (if"] # [doc = " any), then an [`UnorderedKeyError`] is returned since this would"] # [doc = " invalidate the [`Ord`] invariant between the elements of the set."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn insert_after (& mut self , value : T) -> Result < () , UnorderedKeyError > { self . inner . insert_after (value , SetValZST) } # [doc = " Inserts a new element into the set in the gap that the"] # [doc = " cursor is currently pointing to."] # [doc = ""] # [doc = " After the insertion the cursor will be pointing at the gap after the"] # [doc = " newly inserted element."] # [doc = ""] # [doc = " If the inserted element is not greater than the element before the"] # [doc = " cursor (if any), or if it not less than the element after the cursor (if"] # [doc = " any), then an [`UnorderedKeyError`] is returned since this would"] # [doc = " invalidate the [`Ord`] invariant between the elements of the set."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn insert_before (& mut self , value : T) -> Result < () , UnorderedKeyError > { self . inner . insert_before (value , SetValZST) } # [doc = " Removes the next element from the `BTreeSet`."] # [doc = ""] # [doc = " The element that was removed is returned. The cursor position is"] # [doc = " unchanged (before the removed element)."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn remove_next (& mut self) -> Option < T > { self . inner . remove_next () . map (| (k , _) | k) } # [doc = " Removes the preceding element from the `BTreeSet`."] # [doc = ""] # [doc = " The element that was removed is returned. The cursor position is"] # [doc = " unchanged (after the removed element)."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn remove_prev (& mut self) -> Option < T > { self . inner . remove_prev () . map (| (k , _) | k) } }}}
mkitem!{mkimpl!{impl < 'a , T : Ord , A : Allocator + Clone > CursorMutKey < 'a , T , A > { # [doc = " Inserts a new element into the set in the gap that the"] # [doc = " cursor is currently pointing to."] # [doc = ""] # [doc = " After the insertion the cursor will be pointing at the gap before the"] # [doc = " newly inserted element."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " You must ensure that the `BTreeSet` invariants are maintained."] # [doc = " Specifically:"] # [doc = ""] # [doc = " * The key of the newly inserted element must be unique in the tree."] # [doc = " * All elements in the tree must remain in sorted order."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub unsafe fn insert_after_unchecked (& mut self , value : T) { unsafe { self . inner . insert_after_unchecked (value , SetValZST) } } # [doc = " Inserts a new element into the set in the gap that the"] # [doc = " cursor is currently pointing to."] # [doc = ""] # [doc = " After the insertion the cursor will be pointing at the gap after the"] # [doc = " newly inserted element."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " You must ensure that the `BTreeSet` invariants are maintained."] # [doc = " Specifically:"] # [doc = ""] # [doc = " * The newly inserted element must be unique in the tree."] # [doc = " * All elements in the tree must remain in sorted order."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub unsafe fn insert_before_unchecked (& mut self , value : T) { unsafe { self . inner . insert_before_unchecked (value , SetValZST) } } # [doc = " Inserts a new element into the set in the gap that the"] # [doc = " cursor is currently pointing to."] # [doc = ""] # [doc = " After the insertion the cursor will be pointing at the gap before the"] # [doc = " newly inserted element."] # [doc = ""] # [doc = " If the inserted element is not greater than the element before the"] # [doc = " cursor (if any), or if it not less than the element after the cursor (if"] # [doc = " any), then an [`UnorderedKeyError`] is returned since this would"] # [doc = " invalidate the [`Ord`] invariant between the elements of the set."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn insert_after (& mut self , value : T) -> Result < () , UnorderedKeyError > { self . inner . insert_after (value , SetValZST) } # [doc = " Inserts a new element into the set in the gap that the"] # [doc = " cursor is currently pointing to."] # [doc = ""] # [doc = " After the insertion the cursor will be pointing at the gap after the"] # [doc = " newly inserted element."] # [doc = ""] # [doc = " If the inserted element is not greater than the element before the"] # [doc = " cursor (if any), or if it not less than the element after the cursor (if"] # [doc = " any), then an [`UnorderedKeyError`] is returned since this would"] # [doc = " invalidate the [`Ord`] invariant between the elements of the set."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn insert_before (& mut self , value : T) -> Result < () , UnorderedKeyError > { self . inner . insert_before (value , SetValZST) } # [doc = " Removes the next element from the `BTreeSet`."] # [doc = ""] # [doc = " The element that was removed is returned. The cursor position is"] # [doc = " unchanged (before the removed element)."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn remove_next (& mut self) -> Option < T > { self . inner . remove_next () . map (| (k , _) | k) } # [doc = " Removes the preceding element from the `BTreeSet`."] # [doc = ""] # [doc = " The element that was removed is returned. The cursor position is"] # [doc = " unchanged (after the removed element)."] # [unstable (feature = "btree_cursors" , issue = "107540")] pub fn remove_prev (& mut self) -> Option < T > { self . inner . remove_prev () . map (| (k , _) | k) } }}}
mkuse!{# [unstable (feature = "btree_cursors" , issue = "107540")] pub use super :: map :: UnorderedKeyError ;}
mkmod!{tests, { 
                getname!(tests);
                getsrc!(tests);
                getpath!(tests);
                get_deps!(tests);
                get_crates!(tests);
                mkinclude!(tests);
                 
            }}