mkuse!{use core :: iter :: { FusedIterator , TrustedLen , TrustedRandomAccess , TrustedRandomAccessNoCoerce } ;}
mkuse!{use core :: num :: NonZero ;}
mkuse!{use core :: ops :: Try ;}
mkuse!{use core :: { fmt , mem , slice } ;}
mkitem!{mkstruct!{# [doc = " An iterator over the elements of a `VecDeque`."] # [doc = ""] # [doc = " This `struct` is created by the [`iter`] method on [`super::VecDeque`]. See its"] # [doc = " documentation for more."] # [doc = ""] # [doc = " [`iter`]: super::VecDeque::iter"] # [stable (feature = "rust1" , since = "1.0.0")] pub struct Iter < 'a , T : 'a > { i1 : slice :: Iter < 'a , T > , i2 : slice :: Iter < 'a , T > , }}}
mkitem!{mkimpl!{impl < 'a , T > Iter < 'a , T > { pub (super) fn new (i1 : slice :: Iter < 'a , T > , i2 : slice :: Iter < 'a , T >) -> Self { Self { i1 , i2 } } # [doc = " Views the underlying data as a pair of subslices of the original data."] # [doc = ""] # [doc = " The slices contain, in order, the contents of the deque not yet yielded"] # [doc = " by the iterator."] # [doc = ""] # [doc = " This has the same lifetime as the original `VecDeque`, and so the"] # [doc = " iterator can continue to be used while this exists."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(vec_deque_iter_as_slices)]"] # [doc = ""] # [doc = " use std::collections::VecDeque;"] # [doc = ""] # [doc = " let mut deque = VecDeque::new();"] # [doc = " deque.push_back(0);"] # [doc = " deque.push_back(1);"] # [doc = " deque.push_back(2);"] # [doc = " deque.push_front(10);"] # [doc = " deque.push_front(9);"] # [doc = " deque.push_front(8);"] # [doc = ""] # [doc = " let mut iter = deque.iter();"] # [doc = " iter.next();"] # [doc = " iter.next_back();"] # [doc = ""] # [doc = " assert_eq!(iter.as_slices(), (&[9, 10][..], &[0, 1][..]));"] # [doc = " ```"] # [unstable (feature = "vec_deque_iter_as_slices" , issue = "123947")] pub fn as_slices (& self) -> (& 'a [T] , & 'a [T]) { (self . i1 . as_slice () , self . i2 . as_slice ()) } }}}
mkitem!{mkimpl!{# [stable (feature = "collection_debug" , since = "1.17.0")] impl < T : fmt :: Debug > fmt :: Debug for Iter < '_ , T > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_tuple ("Iter") . field (& self . i1 . as_slice ()) . field (& self . i2 . as_slice ()) . finish () } }}}
mkitem!{mkimpl!{# [stable (feature = "default_iters_sequel" , since = "1.82.0")] impl < T > Default for Iter < '_ , T > { # [doc = " Creates an empty `vec_deque::Iter`."] # [doc = ""] # [doc = " ```"] # [doc = " # use std::collections::vec_deque;"] # [doc = " let iter: vec_deque::Iter<'_, u8> = Default::default();"] # [doc = " assert_eq!(iter.len(), 0);"] # [doc = " ```"] fn default () -> Self { Iter { i1 : Default :: default () , i2 : Default :: default () } } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T > Clone for Iter < '_ , T > { fn clone (& self) -> Self { Iter { i1 : self . i1 . clone () , i2 : self . i2 . clone () } } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T > Iterator for Iter < 'a , T > { type Item = & 'a T ; # [inline] fn next (& mut self) -> Option < & 'a T > { match self . i1 . next () { Some (val) => Some (val) , None => { mem :: swap (& mut self . i1 , & mut self . i2) ; self . i1 . next () } } } fn advance_by (& mut self , n : usize) -> Result < () , NonZero < usize > > { let remaining = self . i1 . advance_by (n) ; match remaining { Ok (()) => Ok (()) , Err (n) => { mem :: swap (& mut self . i1 , & mut self . i2) ; self . i1 . advance_by (n . get ()) } } } # [inline] fn size_hint (& self) -> (usize , Option < usize >) { let len = self . len () ; (len , Some (len)) } fn fold < Acc , F > (self , accum : Acc , mut f : F) -> Acc where F : FnMut (Acc , Self :: Item) -> Acc , { let accum = self . i1 . fold (accum , & mut f) ; self . i2 . fold (accum , & mut f) } fn try_fold < B , F , R > (& mut self , init : B , mut f : F) -> R where F : FnMut (B , Self :: Item) -> R , R : Try < Output = B > , { let acc = self . i1 . try_fold (init , & mut f) ? ; self . i2 . try_fold (acc , & mut f) } # [inline] fn last (mut self) -> Option < & 'a T > { self . next_back () } # [inline] unsafe fn __iterator_get_unchecked (& mut self , idx : usize) -> Self :: Item { unsafe { let i1_len = self . i1 . len () ; if idx < i1_len { self . i1 . __iterator_get_unchecked (idx) } else { self . i2 . __iterator_get_unchecked (idx - i1_len) } } } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < 'a , T > DoubleEndedIterator for Iter < 'a , T > { # [inline] fn next_back (& mut self) -> Option < & 'a T > { match self . i2 . next_back () { Some (val) => Some (val) , None => { mem :: swap (& mut self . i1 , & mut self . i2) ; self . i2 . next_back () } } } fn advance_back_by (& mut self , n : usize) -> Result < () , NonZero < usize > > { match self . i2 . advance_back_by (n) { Ok (()) => Ok (()) , Err (n) => { mem :: swap (& mut self . i1 , & mut self . i2) ; self . i2 . advance_back_by (n . get ()) } } } fn rfold < Acc , F > (self , accum : Acc , mut f : F) -> Acc where F : FnMut (Acc , Self :: Item) -> Acc , { let accum = self . i2 . rfold (accum , & mut f) ; self . i1 . rfold (accum , & mut f) } fn try_rfold < B , F , R > (& mut self , init : B , mut f : F) -> R where F : FnMut (B , Self :: Item) -> R , R : Try < Output = B > , { let acc = self . i2 . try_rfold (init , & mut f) ? ; self . i1 . try_rfold (acc , & mut f) } }}}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl < T > ExactSizeIterator for Iter < '_ , T > { fn len (& self) -> usize { self . i1 . len () + self . i2 . len () } fn is_empty (& self) -> bool { self . i1 . is_empty () && self . i2 . is_empty () } }}}
mkitem!{mkimpl!{# [stable (feature = "fused" , since = "1.26.0")] impl < T > FusedIterator for Iter < '_ , T > { }}}
mkitem!{mkimpl!{# [unstable (feature = "trusted_len" , issue = "37572")] unsafe impl < T > TrustedLen for Iter < '_ , T > { }}}
mkitem!{mkimpl!{# [doc (hidden)] # [unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < T > TrustedRandomAccess for Iter < '_ , T > { }}}
mkitem!{mkimpl!{# [doc (hidden)] # [unstable (feature = "trusted_random_access" , issue = "none")] unsafe impl < T > TrustedRandomAccessNoCoerce for Iter < '_ , T > { const MAY_HAVE_SIDE_EFFECT : bool = false ; }}}