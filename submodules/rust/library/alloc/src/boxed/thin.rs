# ! [doc = " Based on"] # ! [doc = " <https://github.com/matthieu-m/rfc2580/blob/b58d1d3cba0d4b5e859d3617ea2d0943aaa31329/examples/thin.rs>"] # ! [doc = " by matthieu-m"] use split_decls_genesis :: ourprelude :: * ; use core :: error :: Error ; use core :: fmt :: { self , Debug , Display , Formatter } ; #[cfg (not (no_global_oom_handling))] use core :: intrinsics :: { const_allocate , const_make_global } ; use core :: marker :: PhantomData ; #[cfg (not (no_global_oom_handling))] use core :: marker :: Unsize ; #[cfg (not (no_global_oom_handling))] use core :: mem :: { self , SizedTypeProperties } ; use core :: ops :: { Deref , DerefMut } ; use core :: ptr :: { self , NonNull , Pointee } ; use crate :: alloc :: { self , Layout , LayoutError } ; #[doc = " ThinBox."] #[doc = ""] #[doc = " A thin pointer for heap allocation, regardless of T."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(thin_box)]"] #[doc = " use std::boxed::ThinBox;"] #[doc = ""] #[doc = " let five = ThinBox::new(5);"] #[doc = " let thin_slice = ThinBox::<[i32]>::new_unsize([1, 2, 3, 4]);"] #[doc = ""] #[doc = " let size_of_ptr = size_of::<*const ()>();"] #[doc = " assert_eq!(size_of_ptr, size_of_val(&five));"] #[doc = " assert_eq!(size_of_ptr, size_of_val(&thin_slice));"] #[doc = " ```"] #[unstable (feature = "thin_box" , issue = "92791")] pub struct ThinBox < T : ? Sized > { ptr : WithOpaqueHeader , _marker : PhantomData < T > , } #[doc = " `ThinBox<T>` is `Send` if `T` is `Send` because the data is owned."] #[unstable (feature = "thin_box" , issue = "92791")] unsafe impl < T : ? Sized + Send > Send for ThinBox < T > { } #[doc = " `ThinBox<T>` is `Sync` if `T` is `Sync` because the data is owned."] #[unstable (feature = "thin_box" , issue = "92791")] unsafe impl < T : ? Sized + Sync > Sync for ThinBox < T > { } #[unstable (feature = "thin_box" , issue = "92791")] impl < T > ThinBox < T > { #[doc = " Moves a type to the heap with its [`Metadata`] stored in the heap allocation instead of on"] #[doc = " the stack."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(thin_box)]"] #[doc = " use std::boxed::ThinBox;"] #[doc = ""] #[doc = " let five = ThinBox::new(5);"] #[doc = " ```"] #[doc = ""] #[doc = " [`Metadata`]: core::ptr::Pointee::Metadata"] #[cfg (not (no_global_oom_handling))] pub fn new (value : T) -> Self { let meta = ptr :: metadata (& value) ; let ptr = WithOpaqueHeader :: new (meta , value) ; ThinBox { ptr , _marker : PhantomData } } #[doc = " Moves a type to the heap with its [`Metadata`] stored in the heap allocation instead of on"] #[doc = " the stack. Returns an error if allocation fails, instead of aborting."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(allocator_api)]"] #[doc = " #![feature(thin_box)]"] #[doc = " use std::boxed::ThinBox;"] #[doc = ""] #[doc = " let five = ThinBox::try_new(5)?;"] #[doc = " # Ok::<(), std::alloc::AllocError>(())"] #[doc = " ```"] #[doc = ""] #[doc = " [`Metadata`]: core::ptr::Pointee::Metadata"] pub fn try_new (value : T) -> Result < Self , core :: alloc :: AllocError > { let meta = ptr :: metadata (& value) ; WithOpaqueHeader :: try_new (meta , value) . map (| ptr | ThinBox { ptr , _marker : PhantomData }) } } #[unstable (feature = "thin_box" , issue = "92791")] impl < Dyn : ? Sized > ThinBox < Dyn > { #[doc = " Moves a type to the heap with its [`Metadata`] stored in the heap allocation instead of on"] #[doc = " the stack."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " #![feature(thin_box)]"] #[doc = " use std::boxed::ThinBox;"] #[doc = ""] #[doc = " let thin_slice = ThinBox::<[i32]>::new_unsize([1, 2, 3, 4]);"] #[doc = " ```"] #[doc = ""] #[doc = " [`Metadata`]: core::ptr::Pointee::Metadata"] #[cfg (not (no_global_oom_handling))] pub fn new_unsize < T > (value : T) -> Self where T : Unsize < Dyn > , { if size_of :: < T > () == 0 { let ptr = WithOpaqueHeader :: new_unsize_zst :: < Dyn , T > (value) ; ThinBox { ptr , _marker : PhantomData } } else { let meta = ptr :: metadata (& value as & Dyn) ; let ptr = WithOpaqueHeader :: new (meta , value) ; ThinBox { ptr , _marker : PhantomData } } } } #[unstable (feature = "thin_box" , issue = "92791")] impl < T : ? Sized + Debug > Debug for ThinBox < T > { fn fmt (& self , f : & mut Formatter < '_ >) -> fmt :: Result { Debug :: fmt (self . deref () , f) } } #[unstable (feature = "thin_box" , issue = "92791")] impl < T : ? Sized + Display > Display for ThinBox < T > { fn fmt (& self , f : & mut Formatter < '_ >) -> fmt :: Result { Display :: fmt (self . deref () , f) } } #[unstable (feature = "thin_box" , issue = "92791")] impl < T : ? Sized > Deref for ThinBox < T > { type Target = T ; fn deref (& self) -> & T { let value = self . data () ; let metadata = self . meta () ; let pointer = ptr :: from_raw_parts (value as * const () , metadata) ; unsafe { & * pointer } } } #[unstable (feature = "thin_box" , issue = "92791")] impl < T : ? Sized > DerefMut for ThinBox < T > { fn deref_mut (& mut self) -> & mut T { let value = self . data () ; let metadata = self . meta () ; let pointer = ptr :: from_raw_parts_mut :: < T > (value as * mut () , metadata) ; unsafe { & mut * pointer } } } #[unstable (feature = "thin_box" , issue = "92791")] impl < T : ? Sized > Drop for ThinBox < T > { fn drop (& mut self) { unsafe { let value = self . deref_mut () ; let value = value as * mut T ; self . with_header () . drop :: < T > (value) ; } } } #[unstable (feature = "thin_box" , issue = "92791")] impl < T : ? Sized > ThinBox < T > { fn meta (& self) -> < T as Pointee > :: Metadata { unsafe { * self . with_header () . header () } } fn data (& self) -> * mut u8 { self . with_header () . value () } fn with_header (& self) -> & WithHeader < < T as Pointee > :: Metadata > { unsafe { & * ((& raw const self . ptr) as * const WithHeader < _ >) } } } #[doc = " A pointer to type-erased data, guaranteed to either be:"] #[doc = " 1. `NonNull::dangling()`, in the case where both the pointee (`T`) and"] #[doc = "    metadata (`H`) are ZSTs."] #[doc = " 2. A pointer to a valid `T` that has a header `H` directly before the"] #[doc = "    pointed-to location."] #[repr (transparent)] struct WithHeader < H > (NonNull < u8 > , PhantomData < H >) ; #[doc = " An opaque representation of `WithHeader<H>` to avoid the"] #[doc = " projection invariance of `<T as Pointee>::Metadata`."] #[repr (transparent)] struct WithOpaqueHeader (NonNull < u8 >) ; impl WithOpaqueHeader { #[cfg (not (no_global_oom_handling))] fn new < H , T > (header : H , value : T) -> Self { let ptr = WithHeader :: new (header , value) ; Self (ptr . 0) } #[cfg (not (no_global_oom_handling))] fn new_unsize_zst < Dyn , T > (value : T) -> Self where Dyn : ? Sized , T : Unsize < Dyn > , { let ptr = WithHeader :: < < Dyn as Pointee > :: Metadata > :: new_unsize_zst :: < Dyn , T > (value) ; Self (ptr . 0) } fn try_new < H , T > (header : H , value : T) -> Result < Self , core :: alloc :: AllocError > { WithHeader :: try_new (header , value) . map (| ptr | Self (ptr . 0)) } } impl < H > WithHeader < H > { #[cfg (not (no_global_oom_handling))] fn new < T > (header : H , value : T) -> WithHeader < H > { let value_layout = Layout :: new :: < T > () ; let Ok ((layout , value_offset)) = Self :: alloc_layout (value_layout) else { alloc :: handle_alloc_error (Layout :: new :: < () > ()) ; } ; unsafe { let ptr = if layout . size () == 0 { debug_assert ! (value_offset == 0 && T :: IS_ZST && H :: IS_ZST) ; layout . dangling () } else { let ptr = alloc :: alloc (layout) ; if ptr . is_null () { alloc :: handle_alloc_error (layout) ; } let ptr = ptr . add (value_offset) as * mut _ ; NonNull :: new_unchecked (ptr) } ; let result = WithHeader (ptr , PhantomData) ; ptr :: write (result . header () , header) ; ptr :: write (result . value () . cast () , value) ; result } } #[doc = " Non-panicking version of `new`."] #[doc = " Any error is returned as `Err(core::alloc::AllocError)`."] fn try_new < T > (header : H , value : T) -> Result < WithHeader < H > , core :: alloc :: AllocError > { let value_layout = Layout :: new :: < T > () ; let Ok ((layout , value_offset)) = Self :: alloc_layout (value_layout) else { return Err (core :: alloc :: AllocError) ; } ; unsafe { let ptr = if layout . size () == 0 { debug_assert ! (value_offset == 0 && size_of ::< T > () == 0 && size_of ::< H > () == 0) ; layout . dangling () } else { let ptr = alloc :: alloc (layout) ; if ptr . is_null () { return Err (core :: alloc :: AllocError) ; } let ptr = ptr . add (value_offset) as * mut _ ; NonNull :: new_unchecked (ptr) } ; let result = WithHeader (ptr , PhantomData) ; ptr :: write (result . header () , header) ; ptr :: write (result . value () . cast () , value) ; Ok (result) } } #[cfg (not (no_global_oom_handling))] fn new_unsize_zst < Dyn , T > (value : T) -> WithHeader < H > where Dyn : Pointee < Metadata = H > + ? Sized , T : Unsize < Dyn > , { assert ! (size_of ::< T > () == 0) ; const fn max (a : usize , b : usize) -> usize { if a > b { a } else { b } } let alloc : & < Dyn as Pointee > :: Metadata = const { let alloc_align = max (align_of :: < T > () , align_of :: < < Dyn as Pointee > :: Metadata > ()) ; let alloc_size = max (align_of :: < T > () , size_of :: < < Dyn as Pointee > :: Metadata > ()) ; unsafe { let alloc : * mut u8 = const_allocate (alloc_size , alloc_align) ; let metadata_offset = alloc_size . checked_sub (size_of :: < < Dyn as Pointee > :: Metadata > ()) . unwrap () ; let metadata_ptr : * mut < Dyn as Pointee > :: Metadata = alloc . add (metadata_offset) . cast () ; metadata_ptr . write (ptr :: metadata :: < Dyn > (ptr :: dangling :: < T > () as * const Dyn)) ; const_make_global (alloc) ; & * metadata_ptr } } ; let value_ptr = unsafe { (alloc as * const < Dyn as Pointee > :: Metadata) . add (1) } . cast :: < T > () . cast_mut () ; debug_assert ! (value_ptr . is_aligned ()) ; mem :: forget (value) ; WithHeader (NonNull :: new (value_ptr . cast ()) . unwrap () , PhantomData) } unsafe fn drop < T : ? Sized > (& self , value : * mut T) { struct DropGuard < H > { ptr : NonNull < u8 > , value_layout : Layout , _marker : PhantomData < H > , } impl < H > Drop for DropGuard < H > { fn drop (& mut self) { if self . value_layout . size () == 0 { return ; } unsafe { let (layout , value_offset) = WithHeader :: < H > :: alloc_layout (self . value_layout) . unwrap_unchecked () ; debug_assert ! (layout . size () != 0) ; alloc :: dealloc (self . ptr . as_ptr () . sub (value_offset) , layout) ; } } } unsafe { let _guard = DropGuard { ptr : self . 0 , value_layout : Layout :: for_value_raw (value) , _marker : PhantomData :: < H > , } ; ptr :: drop_in_place :: < T > (value) ; } } fn header (& self) -> * mut H { let hp = unsafe { self . 0 . as_ptr () . sub (Self :: header_size ()) as * mut H } ; debug_assert ! (hp . is_aligned ()) ; hp } fn value (& self) -> * mut u8 { self . 0 . as_ptr () } const fn header_size () -> usize { size_of :: < H > () } fn alloc_layout (value_layout : Layout) -> Result < (Layout , usize) , LayoutError > { Layout :: new :: < H > () . extend (value_layout) } } #[unstable (feature = "thin_box" , issue = "92791")] impl < T : ? Sized + Error > Error for ThinBox < T > { fn source (& self) -> Option < & (dyn Error + 'static) > { self . deref () . source () } }