mkuse!{use core :: borrow :: { Borrow , BorrowMut } ;}
mkuse!{use core :: iter :: FusedIterator ;}
mkuse!{use core :: mem :: MaybeUninit ;}
mkuse!{# [stable (feature = "encode_utf16" , since = "1.8.0")] pub use core :: str :: EncodeUtf16 ;}
mkuse!{# [stable (feature = "split_ascii_whitespace" , since = "1.34.0")] pub use core :: str :: SplitAsciiWhitespace ;}
mkuse!{# [stable (feature = "split_inclusive" , since = "1.51.0")] pub use core :: str :: SplitInclusive ;}
mkuse!{# [stable (feature = "rust1" , since = "1.0.0")] pub use core :: str :: SplitWhitespace ;}
mkuse!{# [stable (feature = "rust1" , since = "1.0.0")] pub use core :: str :: pattern ;}
mkuse!{use core :: str :: pattern :: { DoubleEndedSearcher , Pattern , ReverseSearcher , Searcher , Utf8Pattern } ;}
mkuse!{# [stable (feature = "rust1" , since = "1.0.0")] pub use core :: str :: { Bytes , CharIndices , Chars , from_utf8 , from_utf8_mut } ;}
mkuse!{# [stable (feature = "str_escape" , since = "1.34.0")] pub use core :: str :: { EscapeDebug , EscapeDefault , EscapeUnicode } ;}
mkuse!{# [stable (feature = "rust1" , since = "1.0.0")] pub use core :: str :: { FromStr , Utf8Error } ;}
mkuse!{# [allow (deprecated)] # [stable (feature = "rust1" , since = "1.0.0")] pub use core :: str :: { Lines , LinesAny } ;}
mkuse!{# [stable (feature = "rust1" , since = "1.0.0")] pub use core :: str :: { MatchIndices , RMatchIndices } ;}
mkuse!{# [stable (feature = "rust1" , since = "1.0.0")] pub use core :: str :: { Matches , RMatches } ;}
mkuse!{# [stable (feature = "rust1" , since = "1.0.0")] pub use core :: str :: { ParseBoolError , from_utf8_unchecked , from_utf8_unchecked_mut } ;}
mkuse!{# [stable (feature = "rust1" , since = "1.0.0")] pub use core :: str :: { RSplit , Split } ;}
mkuse!{# [stable (feature = "rust1" , since = "1.0.0")] pub use core :: str :: { RSplitN , SplitN } ;}
mkuse!{# [stable (feature = "rust1" , since = "1.0.0")] pub use core :: str :: { RSplitTerminator , SplitTerminator } ;}
mkuse!{# [stable (feature = "utf8_chunks" , since = "1.79.0")] pub use core :: str :: { Utf8Chunk , Utf8Chunks } ;}
mkuse!{# [unstable (feature = "str_from_raw_parts" , issue = "119206")] pub use core :: str :: { from_raw_parts , from_raw_parts_mut } ;}
mkuse!{use core :: unicode :: conversions ;}
mkuse!{use core :: { mem , ptr } ;}
mkuse!{use crate :: borrow :: ToOwned ;}
mkuse!{use crate :: boxed :: Box ;}
mkuse!{use crate :: slice :: { Concat , Join , SliceIndex } ;}
mkuse!{use crate :: string :: String ;}
mkuse!{use crate :: vec :: Vec ;}
mkitem!{mkimpl!{# [doc = " Note: `str` in `Concat<str>` is not meaningful here."] # [doc = " This type parameter of the trait only exists to enable another impl."] # [cfg (not (no_global_oom_handling))] # [unstable (feature = "slice_concat_ext" , issue = "27747")] impl < S : Borrow < str > > Concat < str > for [S] { type Output = String ; fn concat (slice : & Self) -> String { Join :: join (slice , "") } }}}
mkitem!{mkimpl!{# [cfg (not (no_global_oom_handling))] # [unstable (feature = "slice_concat_ext" , issue = "27747")] impl < S : Borrow < str > > Join < & str > for [S] { type Output = String ; fn join (slice : & Self , sep : & str) -> String { unsafe { String :: from_utf8_unchecked (join_generic_copy (slice , sep . as_bytes ())) } } }}}
mkitem!{# [cfg (not (no_global_oom_handling))] macro_rules ! specialize_for_lengths { ($ separator : expr , $ target : expr , $ iter : expr ; $ ($ num : expr) ,*) => { { let mut target = $ target ; let iter = $ iter ; let sep_bytes = $ separator ; match $ separator . len () { $ ($ num => { for s in iter { copy_slice_and_advance ! (target , sep_bytes) ; let content_bytes = s . borrow () . as_ref () ; copy_slice_and_advance ! (target , content_bytes) ; } } ,) * _ => { for s in iter { copy_slice_and_advance ! (target , sep_bytes) ; let content_bytes = s . borrow () . as_ref () ; copy_slice_and_advance ! (target , content_bytes) ; } } } target } } }}
mkitem!{# [cfg (not (no_global_oom_handling))] macro_rules ! copy_slice_and_advance { ($ target : expr , $ bytes : expr) => { let len = $ bytes . len () ; let (head , tail) = { $ target } . split_at_mut (len) ; head . copy_from_slice ($ bytes) ; $ target = tail ; } ; }}

macro_rules! join_generic_copy_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function join_generic_copy in module {}", module_path!());
    };
}

mkfn!{
    join_generic_copy_introspect!();
    # [cfg (not (no_global_oom_handling))] fn join_generic_copy < B , T , S > (slice : & [S] , sep : & [T]) -> Vec < T > where T : Copy , B : AsRef < [T] > + ? Sized , S : Borrow < B > , { let sep_len = sep . len () ; let mut iter = slice . iter () ; let first = match iter . next () { Some (first) => first , None => return vec ! [] , } ; let reserved_len = sep_len . checked_mul (iter . len ()) . and_then (| n | { slice . iter () . map (| s | s . borrow () . as_ref () . len ()) . try_fold (n , usize :: checked_add) }) . expect ("attempt to join into collection with len > usize::MAX") ; let mut result = Vec :: with_capacity (reserved_len) ; debug_assert ! (result . capacity () >= reserved_len) ; result . extend_from_slice (first . borrow () . as_ref ()) ; unsafe { let pos = result . len () ; let target = result . spare_capacity_mut () . get_unchecked_mut (.. reserved_len - pos) ; let sep_uninit = core :: slice :: from_raw_parts (sep . as_ptr () . cast () , sep . len ()) ; let iter_uninit = iter . map (| it | { let it = it . borrow () . as_ref () ; core :: slice :: from_raw_parts (it . as_ptr () . cast () , it . len ()) }) ; let remain = specialize_for_lengths ! (sep_uninit , target , iter_uninit ; 0 , 1 , 2 , 3 , 4) ; let result_len = reserved_len - remain . len () ; result . set_len (result_len) ; } result }
}
mkitem!{mkimpl!{# [stable (feature = "rust1" , since = "1.0.0")] impl Borrow < str > for String { # [inline] fn borrow (& self) -> & str { & self [..] } }}}
mkitem!{mkimpl!{# [stable (feature = "string_borrow_mut" , since = "1.36.0")] impl BorrowMut < str > for String { # [inline] fn borrow_mut (& mut self) -> & mut str { & mut self [..] } }}}
mkitem!{mkimpl!{# [cfg (not (no_global_oom_handling))] # [stable (feature = "rust1" , since = "1.0.0")] impl ToOwned for str { type Owned = String ; # [inline] fn to_owned (& self) -> String { unsafe { String :: from_utf8_unchecked (self . as_bytes () . to_owned ()) } } # [inline] fn clone_into (& self , target : & mut String) { target . clear () ; target . push_str (self) ; } }}}
mkitem!{mkimpl!{# [doc = " Methods for string slices."] impl str { # [doc = " Converts a `Box<str>` into a `Box<[u8]>` without copying or allocating."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let s = \"this is a string\";"] # [doc = " let boxed_str = s.to_owned().into_boxed_str();"] # [doc = " let boxed_bytes = boxed_str.into_boxed_bytes();"] # [doc = " assert_eq!(*boxed_bytes, *s.as_bytes());"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [stable (feature = "str_box_extras" , since = "1.20.0")] # [must_use = "`self` will be dropped if the result is not used"] # [inline] pub fn into_boxed_bytes (self : Box < Self >) -> Box < [u8] > { self . into () } # [doc = " Replaces all matches of a pattern with another string."] # [doc = ""] # [doc = " `replace` creates a new [`String`], and copies the data from this string slice into it."] # [doc = " While doing so, it attempts to find matches of a pattern. If it finds any, it"] # [doc = " replaces them with the replacement string slice."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let s = \"this is old\";"] # [doc = ""] # [doc = " assert_eq!(\"this is new\", s.replace(\"old\", \"new\"));"] # [doc = " assert_eq!(\"than an old\", s.replace(\"is\", \"an\"));"] # [doc = " ```"] # [doc = ""] # [doc = " When the pattern doesn't match, it returns this string slice as [`String`]:"] # [doc = ""] # [doc = " ```"] # [doc = " let s = \"this is old\";"] # [doc = " assert_eq!(s, s.replace(\"cookie monster\", \"little lamb\"));"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [must_use = "this returns the replaced string as a new allocation, \
                  without modifying the original"] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn replace < P : Pattern > (& self , from : P , to : & str) -> String { if let Some (from_byte) = match from . as_utf8_pattern () { Some (Utf8Pattern :: StringPattern ([from_byte])) => Some (* from_byte) , Some (Utf8Pattern :: CharPattern (c)) => c . as_ascii () . map (| ascii_char | ascii_char . to_u8 ()) , _ => None , } { if let [to_byte] = to . as_bytes () { return unsafe { replace_ascii (self . as_bytes () , from_byte , * to_byte) } ; } } let default_capacity = match from . as_utf8_pattern () { Some (Utf8Pattern :: StringPattern (s)) if s . len () <= to . len () => self . len () , Some (Utf8Pattern :: CharPattern (c)) if c . len_utf8 () <= to . len () => self . len () , _ => 0 , } ; let mut result = String :: with_capacity (default_capacity) ; let mut last_end = 0 ; for (start , part) in self . match_indices (from) { result . push_str (unsafe { self . get_unchecked (last_end .. start) }) ; result . push_str (to) ; last_end = start + part . len () ; } result . push_str (unsafe { self . get_unchecked (last_end .. self . len ()) }) ; result } # [doc = " Replaces first N matches of a pattern with another string."] # [doc = ""] # [doc = " `replacen` creates a new [`String`], and copies the data from this string slice into it."] # [doc = " While doing so, it attempts to find matches of a pattern. If it finds any, it"] # [doc = " replaces them with the replacement string slice at most `count` times."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let s = \"foo foo 123 foo\";"] # [doc = " assert_eq!(\"new new 123 foo\", s.replacen(\"foo\", \"new\", 2));"] # [doc = " assert_eq!(\"faa fao 123 foo\", s.replacen('o', \"a\", 3));"] # [doc = " assert_eq!(\"foo foo new23 foo\", s.replacen(char::is_numeric, \"new\", 1));"] # [doc = " ```"] # [doc = ""] # [doc = " When the pattern doesn't match, it returns this string slice as [`String`]:"] # [doc = ""] # [doc = " ```"] # [doc = " let s = \"this is old\";"] # [doc = " assert_eq!(s, s.replacen(\"cookie monster\", \"little lamb\", 10));"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [doc (alias = "replace_first")] # [must_use = "this returns the replaced string as a new allocation, \
                  without modifying the original"] # [stable (feature = "str_replacen" , since = "1.16.0")] pub fn replacen < P : Pattern > (& self , pat : P , to : & str , count : usize) -> String { let mut result = String :: with_capacity (32) ; let mut last_end = 0 ; for (start , part) in self . match_indices (pat) . take (count) { result . push_str (unsafe { self . get_unchecked (last_end .. start) }) ; result . push_str (to) ; last_end = start + part . len () ; } result . push_str (unsafe { self . get_unchecked (last_end .. self . len ()) }) ; result } # [doc = " Returns the lowercase equivalent of this string slice, as a new [`String`]."] # [doc = ""] # [doc = " 'Lowercase' is defined according to the terms of the Unicode Derived Core Property"] # [doc = " `Lowercase`."] # [doc = ""] # [doc = " Since some characters can expand into multiple characters when changing"] # [doc = " the case, this function returns a [`String`] instead of modifying the"] # [doc = " parameter in-place."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " let s = \"HELLO\";"] # [doc = ""] # [doc = " assert_eq!(\"hello\", s.to_lowercase());"] # [doc = " ```"] # [doc = ""] # [doc = " A tricky example, with sigma:"] # [doc = ""] # [doc = " ```"] # [doc = " let sigma = \"Σ\";"] # [doc = ""] # [doc = " assert_eq!(\"σ\", sigma.to_lowercase());"] # [doc = ""] # [doc = " // but at the end of a word, it's ς, not σ:"] # [doc = " let odysseus = \"ὈΔΥΣΣΕΎΣ\";"] # [doc = ""] # [doc = " assert_eq!(\"ὀδυσσεύς\", odysseus.to_lowercase());"] # [doc = " ```"] # [doc = ""] # [doc = " Languages without case are not changed:"] # [doc = ""] # [doc = " ```"] # [doc = " let new_year = \"农历新年\";"] # [doc = ""] # [doc = " assert_eq!(new_year, new_year.to_lowercase());"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [must_use = "this returns the lowercase string as a new String, \
                  without modifying the original"] # [stable (feature = "unicode_case_mapping" , since = "1.2.0")] pub fn to_lowercase (& self) -> String { let (mut s , rest) = convert_while_ascii (self , u8 :: to_ascii_lowercase) ; let prefix_len = s . len () ; for (i , c) in rest . char_indices () { if c == 'Σ' { let sigma_lowercase = map_uppercase_sigma (self , prefix_len + i) ; s . push (sigma_lowercase) ; } else { match conversions :: to_lower (c) { [a , '\0' , _] => s . push (a) , [a , b , '\0'] => { s . push (a) ; s . push (b) ; } [a , b , c] => { s . push (a) ; s . push (b) ; s . push (c) ; } } } } return s ; fn map_uppercase_sigma (from : & str , i : usize) -> char { debug_assert ! ('Σ' . len_utf8 () == 2) ; let is_word_final = case_ignorable_then_cased (from [.. i] . chars () . rev ()) && ! case_ignorable_then_cased (from [i + 2 ..] . chars ()) ; if is_word_final { 'ς' } else { 'σ' } } fn case_ignorable_then_cased < I : Iterator < Item = char > > (iter : I) -> bool { match iter . skip_while (| & c | c . is_case_ignorable ()) . next () { Some (c) => c . is_cased () , None => false , } } } # [doc = " Returns the uppercase equivalent of this string slice, as a new [`String`]."] # [doc = ""] # [doc = " 'Uppercase' is defined according to the terms of the Unicode Derived Core Property"] # [doc = " `Uppercase`."] # [doc = ""] # [doc = " Since some characters can expand into multiple characters when changing"] # [doc = " the case, this function returns a [`String`] instead of modifying the"] # [doc = " parameter in-place."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " let s = \"hello\";"] # [doc = ""] # [doc = " assert_eq!(\"HELLO\", s.to_uppercase());"] # [doc = " ```"] # [doc = ""] # [doc = " Scripts without case are not changed:"] # [doc = ""] # [doc = " ```"] # [doc = " let new_year = \"农历新年\";"] # [doc = ""] # [doc = " assert_eq!(new_year, new_year.to_uppercase());"] # [doc = " ```"] # [doc = ""] # [doc = " One character can become multiple:"] # [doc = " ```"] # [doc = " let s = \"tschüß\";"] # [doc = ""] # [doc = " assert_eq!(\"TSCHÜSS\", s.to_uppercase());"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [must_use = "this returns the uppercase string as a new String, \
                  without modifying the original"] # [stable (feature = "unicode_case_mapping" , since = "1.2.0")] pub fn to_uppercase (& self) -> String { let (mut s , rest) = convert_while_ascii (self , u8 :: to_ascii_uppercase) ; for c in rest . chars () { match conversions :: to_upper (c) { [a , '\0' , _] => s . push (a) , [a , b , '\0'] => { s . push (a) ; s . push (b) ; } [a , b , c] => { s . push (a) ; s . push (b) ; s . push (c) ; } } } s } # [doc = " Converts a [`Box<str>`] into a [`String`] without copying or allocating."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let string = String::from(\"birthday gift\");"] # [doc = " let boxed_str = string.clone().into_boxed_str();"] # [doc = ""] # [doc = " assert_eq!(boxed_str.into_string(), string);"] # [doc = " ```"] # [stable (feature = "box_str" , since = "1.4.0")] # [rustc_allow_incoherent_impl] # [must_use = "`self` will be dropped if the result is not used"] # [inline] pub fn into_string (self : Box < Self >) -> String { let slice = Box :: < [u8] > :: from (self) ; unsafe { String :: from_utf8_unchecked (slice . into_vec ()) } } # [doc = " Creates a new [`String`] by repeating a string `n` times."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if the capacity would overflow."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " assert_eq!(\"abc\".repeat(4), String::from(\"abcabcabcabc\"));"] # [doc = " ```"] # [doc = ""] # [doc = " A panic upon overflow:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = " // this will panic at runtime"] # [doc = " let huge = \"0123456789abcdef\".repeat(usize::MAX);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [must_use] # [stable (feature = "repeat_str" , since = "1.16.0")] # [inline] pub fn repeat (& self , n : usize) -> String { unsafe { String :: from_utf8_unchecked (self . as_bytes () . repeat (n)) } } # [doc = " Returns a copy of this string where each character is mapped to its"] # [doc = " ASCII upper case equivalent."] # [doc = ""] # [doc = " ASCII letters 'a' to 'z' are mapped to 'A' to 'Z',"] # [doc = " but non-ASCII letters are unchanged."] # [doc = ""] # [doc = " To uppercase the value in-place, use [`make_ascii_uppercase`]."] # [doc = ""] # [doc = " To uppercase ASCII characters in addition to non-ASCII characters, use"] # [doc = " [`to_uppercase`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let s = \"Grüße, Jürgen ❤\";"] # [doc = ""] # [doc = " assert_eq!(\"GRüßE, JüRGEN ❤\", s.to_ascii_uppercase());"] # [doc = " ```"] # [doc = ""] # [doc = " [`make_ascii_uppercase`]: str::make_ascii_uppercase"] # [doc = " [`to_uppercase`]: #method.to_uppercase"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [must_use = "to uppercase the value in-place, use `make_ascii_uppercase()`"] # [stable (feature = "ascii_methods_on_intrinsics" , since = "1.23.0")] # [inline] pub fn to_ascii_uppercase (& self) -> String { let mut s = self . to_owned () ; s . make_ascii_uppercase () ; s } # [doc = " Returns a copy of this string where each character is mapped to its"] # [doc = " ASCII lower case equivalent."] # [doc = ""] # [doc = " ASCII letters 'A' to 'Z' are mapped to 'a' to 'z',"] # [doc = " but non-ASCII letters are unchanged."] # [doc = ""] # [doc = " To lowercase the value in-place, use [`make_ascii_lowercase`]."] # [doc = ""] # [doc = " To lowercase ASCII characters in addition to non-ASCII characters, use"] # [doc = " [`to_lowercase`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let s = \"Grüße, Jürgen ❤\";"] # [doc = ""] # [doc = " assert_eq!(\"grüße, jürgen ❤\", s.to_ascii_lowercase());"] # [doc = " ```"] # [doc = ""] # [doc = " [`make_ascii_lowercase`]: str::make_ascii_lowercase"] # [doc = " [`to_lowercase`]: #method.to_lowercase"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [must_use = "to lowercase the value in-place, use `make_ascii_lowercase()`"] # [stable (feature = "ascii_methods_on_intrinsics" , since = "1.23.0")] # [inline] pub fn to_ascii_lowercase (& self) -> String { let mut s = self . to_owned () ; s . make_ascii_lowercase () ; s } }}}

macro_rules! from_boxed_utf8_unchecked_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function from_boxed_utf8_unchecked in module {}", module_path!());
    };
}

mkfn!{
    from_boxed_utf8_unchecked_introspect!();
    # [doc = " Converts a boxed slice of bytes to a boxed string slice without checking"] # [doc = " that the string contains valid UTF-8."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " * The provided bytes must contain a valid UTF-8 sequence."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let smile_utf8 = Box::new([226, 152, 186]);"] # [doc = " let smile = unsafe { std::str::from_boxed_utf8_unchecked(smile_utf8) };"] # [doc = ""] # [doc = " assert_eq!(\"☺\", &*smile);"] # [doc = " ```"] # [stable (feature = "str_box_extras" , since = "1.20.0")] # [must_use] # [inline] pub unsafe fn from_boxed_utf8_unchecked (v : Box < [u8] >) -> Box < str > { unsafe { Box :: from_raw (Box :: into_raw (v) as * mut str) } }
}

macro_rules! convert_while_ascii_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function convert_while_ascii in module {}", module_path!());
    };
}

mkfn!{
    convert_while_ascii_introspect!();
    # [doc = " Converts leading ascii bytes in `s` by calling the `convert` function."] # [doc = ""] # [doc = " For better average performance, this happens in chunks of `2*size_of::<usize>()`."] # [doc = ""] # [doc = " Returns a tuple of the converted prefix and the remainder starting from"] # [doc = " the first non-ascii character."] # [doc = ""] # [doc = " This function is only public so that it can be verified in a codegen test,"] # [doc = " see `issue-123712-str-to-lower-autovectorization.rs`."] # [unstable (feature = "str_internals" , issue = "none")] # [doc (hidden)] # [inline] # [cfg (not (no_global_oom_handling))] pub fn convert_while_ascii (s : & str , convert : fn (& u8) -> u8) -> (String , & str) { const N : usize = 16 ; let mut slice = s . as_bytes () ; let mut out = Vec :: with_capacity (slice . len ()) ; let mut out_slice = out . spare_capacity_mut () ; let mut ascii_prefix_len = 0_usize ; let mut is_ascii = [false ; N] ; while slice . len () >= N { let chunk = unsafe { slice . get_unchecked (.. N) } ; let out_chunk = unsafe { out_slice . get_unchecked_mut (.. N) } ; for j in 0 .. N { is_ascii [j] = chunk [j] <= 127 ; } if is_ascii . iter () . map (| x | * x as u8) . sum :: < u8 > () as usize != N { break ; } for j in 0 .. N { out_chunk [j] = MaybeUninit :: new (convert (& chunk [j])) ; } ascii_prefix_len += N ; slice = unsafe { slice . get_unchecked (N ..) } ; out_slice = unsafe { out_slice . get_unchecked_mut (N ..) } ; } while slice . len () > 0 { let byte = slice [0] ; if byte > 127 { break ; } unsafe { * out_slice . get_unchecked_mut (0) = MaybeUninit :: new (convert (& byte)) ; } ascii_prefix_len += 1 ; slice = unsafe { slice . get_unchecked (1 ..) } ; out_slice = unsafe { out_slice . get_unchecked_mut (1 ..) } ; } unsafe { out . set_len (ascii_prefix_len) ; let ascii_string = String :: from_utf8_unchecked (out) ; let rest = core :: str :: from_utf8_unchecked (slice) ; (ascii_string , rest) } }
}

macro_rules! replace_ascii_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function replace_ascii in module {}", module_path!());
    };
}

mkfn!{
    replace_ascii_introspect!();
    # [inline] # [cfg (not (no_global_oom_handling))] # [allow (dead_code)] # [doc = " Faster implementation of string replacement for ASCII to ASCII cases."] # [doc = " Should produce fast vectorized code."] unsafe fn replace_ascii (utf8_bytes : & [u8] , from : u8 , to : u8) -> String { let result : Vec < u8 > = utf8_bytes . iter () . map (| b | if * b == from { to } else { * b }) . collect () ; unsafe { String :: from_utf8_unchecked (result) } }
}