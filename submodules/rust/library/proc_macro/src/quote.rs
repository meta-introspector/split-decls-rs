mkuse!{use crate :: { BitOr , Delimiter , Group , Ident , Literal , Punct , Spacing , Span , ToTokens , TokenStream , TokenTree , } ;}
mkitem!{mkstruct!{# [doc (hidden)] pub struct HasIterator ;}}
mkitem!{mkstruct!{# [doc (hidden)] pub struct ThereIsNoIteratorInRepetition ;}}
mkitem!{mkimpl!{impl BitOr < ThereIsNoIteratorInRepetition > for ThereIsNoIteratorInRepetition { type Output = ThereIsNoIteratorInRepetition ; fn bitor (self , _rhs : ThereIsNoIteratorInRepetition) -> ThereIsNoIteratorInRepetition { ThereIsNoIteratorInRepetition } }}}
mkitem!{mkimpl!{impl BitOr < ThereIsNoIteratorInRepetition > for HasIterator { type Output = HasIterator ; fn bitor (self , _rhs : ThereIsNoIteratorInRepetition) -> HasIterator { HasIterator } }}}
mkitem!{mkimpl!{impl BitOr < HasIterator > for ThereIsNoIteratorInRepetition { type Output = HasIterator ; fn bitor (self , _rhs : HasIterator) -> HasIterator { HasIterator } }}}
mkitem!{mkimpl!{impl BitOr < HasIterator > for HasIterator { type Output = HasIterator ; fn bitor (self , _rhs : HasIterator) -> HasIterator { HasIterator } }}}
mkmod!{ext, { 
                getname!(ext);
                getsrc!(ext);
                getpath!(ext);
                get_deps!(ext);
                get_crates!(ext);
                mkinclude!(ext);
                mkuse!{use core :: slice ;}
mkuse!{use std :: collections :: btree_set :: { self , BTreeSet } ;}
mkuse!{use super :: { HasIterator as HasIter , RepInterp , ThereIsNoIteratorInRepetition as DoesNotHaveIter , } ;}
mkuse!{use crate :: ToTokens ;}
mkitem!{mktrait!{# [doc = " Extension trait providing the `quote_into_iter` method on iterators."] # [doc (hidden)] pub trait RepIteratorExt : Iterator + Sized { fn quote_into_iter (self) -> (Self , HasIter) { (self , HasIter) } }}}
mkitem!{mkimpl!{impl < T : Iterator > RepIteratorExt for T { }}}
mkitem!{mktrait!{# [doc = " Extension trait providing the `quote_into_iter` method for"] # [doc = " non-iterable types. These types interpolate the same value in each"] # [doc = " iteration of the repetition."] # [doc (hidden)] pub trait RepToTokensExt { # [doc = " Pretend to be an iterator for the purposes of `quote_into_iter`."] # [doc = " This allows repeated calls to `quote_into_iter` to continue"] # [doc = " correctly returning DoesNotHaveIter."] fn next (& self) -> Option < & Self > { Some (self) } fn quote_into_iter (& self) -> (& Self , DoesNotHaveIter) { (self , DoesNotHaveIter) } }}}
mkitem!{mkimpl!{impl < T : ToTokens + ? Sized > RepToTokensExt for T { }}}
mkitem!{mktrait!{# [doc = " Extension trait providing the `quote_into_iter` method for types that"] # [doc = " can be referenced as an iterator."] # [doc (hidden)] pub trait RepAsIteratorExt < 'q > { type Iter : Iterator ; fn quote_into_iter (& 'q self) -> (Self :: Iter , HasIter) ; }}}
mkitem!{mkimpl!{impl < 'q , T : RepAsIteratorExt < 'q > + ? Sized > RepAsIteratorExt < 'q > for & T { type Iter = T :: Iter ; fn quote_into_iter (& 'q self) -> (Self :: Iter , HasIter) { < T as RepAsIteratorExt > :: quote_into_iter (* self) } }}}
mkitem!{mkimpl!{impl < 'q , T : RepAsIteratorExt < 'q > + ? Sized > RepAsIteratorExt < 'q > for & mut T { type Iter = T :: Iter ; fn quote_into_iter (& 'q self) -> (Self :: Iter , HasIter) { < T as RepAsIteratorExt > :: quote_into_iter (* self) } }}}
mkitem!{mkimpl!{impl < 'q , T : 'q > RepAsIteratorExt < 'q > for [T] { type Iter = slice :: Iter < 'q , T > ; fn quote_into_iter (& 'q self) -> (Self :: Iter , HasIter) { (self . iter () , HasIter) } }}}
mkitem!{mkimpl!{impl < 'q , T : 'q , const N : usize > RepAsIteratorExt < 'q > for [T ; N] { type Iter = slice :: Iter < 'q , T > ; fn quote_into_iter (& 'q self) -> (Self :: Iter , HasIter) { (self . iter () , HasIter) } }}}
mkitem!{mkimpl!{impl < 'q , T : 'q > RepAsIteratorExt < 'q > for Vec < T > { type Iter = slice :: Iter < 'q , T > ; fn quote_into_iter (& 'q self) -> (Self :: Iter , HasIter) { (self . iter () , HasIter) } }}}
mkitem!{mkimpl!{impl < 'q , T : 'q > RepAsIteratorExt < 'q > for BTreeSet < T > { type Iter = btree_set :: Iter < 'q , T > ; fn quote_into_iter (& 'q self) -> (Self :: Iter , HasIter) { (self . iter () , HasIter) } }}}
mkitem!{mkimpl!{impl < 'q , T : RepAsIteratorExt < 'q > > RepAsIteratorExt < 'q > for RepInterp < T > { type Iter = T :: Iter ; fn quote_into_iter (& 'q self) -> (Self :: Iter , HasIter) { self . 0 . quote_into_iter () } }}} 
            }}
mkitem!{mkstruct!{# [derive (Copy , Clone)] # [doc (hidden)] pub struct RepInterp < T > (pub T) ;}}
mkitem!{mkimpl!{impl < T > RepInterp < T > { pub fn next (self) -> Option < T > { Some (self . 0) } }}}
mkitem!{mkimpl!{impl < T : Iterator > Iterator for RepInterp < T > { type Item = T :: Item ; fn next (& mut self) -> Option < Self :: Item > { self . 0 . next () } }}}
mkitem!{mkimpl!{impl < T : ToTokens > ToTokens for RepInterp < T > { fn to_tokens (& self , tokens : & mut TokenStream) { self . 0 . to_tokens (tokens) ; } }}}
mkitem!{macro_rules ! minimal_quote_tt { (($ ($ t : tt) *)) => { Group :: new (Delimiter :: Parenthesis , minimal_quote ! ($ ($ t) *)) } ; ([$ ($ t : tt) *]) => { Group :: new (Delimiter :: Bracket , minimal_quote ! ($ ($ t) *)) } ; ({ $ ($ t : tt) * }) => { Group :: new (Delimiter :: Brace , minimal_quote ! ($ ($ t) *)) } ; (,) => { Punct :: new (',' , Spacing :: Alone) } ; (.) => { Punct :: new ('.' , Spacing :: Alone) } ; (;) => { Punct :: new (';' , Spacing :: Alone) } ; (!) => { Punct :: new ('!' , Spacing :: Alone) } ; (<) => { Punct :: new ('<' , Spacing :: Alone) } ; (>) => { Punct :: new ('>' , Spacing :: Alone) } ; (&) => { Punct :: new ('&' , Spacing :: Alone) } ; (=) => { Punct :: new ('=' , Spacing :: Alone) } ; (#) => { Punct :: new ('#' , Spacing :: Alone) } ; (|) => { Punct :: new ('|' , Spacing :: Alone) } ; (:) => { Punct :: new (':' , Spacing :: Alone) } ; (*) => { Punct :: new ('*' , Spacing :: Alone) } ; (_) => { Ident :: new ("_" , Span :: def_site ()) } ; ($ i : ident) => { Ident :: new (stringify ! ($ i) , Span :: def_site ()) } ; ($ lit : literal) => { stringify ! ($ lit) . parse ::< Literal > () . unwrap () } ; }}
mkitem!{macro_rules ! minimal_quote_ts { ((@ $ ($ t : tt) *)) => { $ ($ t) * } ; (::) => { { let mut c = (TokenTree :: from (Punct :: new (':' , Spacing :: Joint)) , TokenTree :: from (Punct :: new (':' , Spacing :: Alone))) ; c . 0 . set_span (Span :: def_site ()) ; c . 1 . set_span (Span :: def_site ()) ; [c . 0 , c . 1] . into_iter () . collect ::< TokenStream > () } } ; (=>) => { { let mut c = (TokenTree :: from (Punct :: new ('=' , Spacing :: Joint)) , TokenTree :: from (Punct :: new ('>' , Spacing :: Alone))) ; c . 0 . set_span (Span :: def_site ()) ; c . 1 . set_span (Span :: def_site ()) ; [c . 0 , c . 1] . into_iter () . collect ::< TokenStream > () } } ; (+=) => { { let mut c = (TokenTree :: from (Punct :: new ('+' , Spacing :: Joint)) , TokenTree :: from (Punct :: new ('=' , Spacing :: Alone))) ; c . 0 . set_span (Span :: def_site ()) ; c . 1 . set_span (Span :: def_site ()) ; [c . 0 , c . 1] . into_iter () . collect ::< TokenStream > () } } ; (!=) => { { let mut c = (TokenTree :: from (Punct :: new ('!' , Spacing :: Joint)) , TokenTree :: from (Punct :: new ('=' , Spacing :: Alone))) ; c . 0 . set_span (Span :: def_site ()) ; c . 1 . set_span (Span :: def_site ()) ; [c . 0 , c . 1] . into_iter () . collect ::< TokenStream > () } } ; ($ t : tt) => { TokenTree :: from (minimal_quote_tt ! ($ t)) } ; }}
mkitem!{# [doc = " Simpler version of the real `quote!` macro, implemented solely"] # [doc = " through `macro_rules`, for bootstrapping the real implementation"] # [doc = " (see the `quote` function), which does not have access to the"] # [doc = " real `quote!` macro due to the `proc_macro` crate not being"] # [doc = " able to depend on itself."] # [doc = ""] # [doc = " Note: supported tokens are a subset of the real `quote!`, but"] # [doc = " unquoting is different: instead of `$x`, this uses `(@ expr)`."] macro_rules ! minimal_quote { ($ ($ t : tt) *) => { { # [allow (unused_mut)] let mut ts = TokenStream :: new () ; $ (ToTokens :: to_tokens (& minimal_quote_ts ! ($ t) , & mut ts) ;) * ts } } ; }}

macro_rules! quote_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function quote in module {}", module_path!());
    };
}

mkfn!{
    quote_introspect!();
    # [doc = " Quote a `TokenStream` into a `TokenStream`."] # [doc = " This is the actual implementation of the `quote!()` proc macro."] # [doc = ""] # [doc = " It is loaded by the compiler in `register_builtin_macros`."] # [unstable (feature = "proc_macro_quote" , issue = "54722")] pub fn quote (stream : TokenStream) -> TokenStream { if stream . is_empty () { return minimal_quote ! (crate :: TokenStream :: new ()) ; } let proc_macro_crate = minimal_quote ! (crate) ; let mut after_dollar = false ; let mut tokens = crate :: TokenStream :: new () ; let mut iter = stream . into_iter () . peekable () ; while let Some (tree) = iter . next () { if after_dollar { after_dollar = false ; match tree { TokenTree :: Group (tt) => { let contents = tt . stream () ; let sep_opt : Option < Punct > = match (iter . next () , iter . peek ()) { (Some (TokenTree :: Punct (sep)) , Some (TokenTree :: Punct (star))) if sep . spacing () == Spacing :: Joint && star . as_char () == '*' => { iter . next () ; Some (sep) } (Some (TokenTree :: Punct (star)) , _) if star . as_char () == '*' => None , _ => panic ! ("`$(...)` must be followed by `*` in `quote!`") , } ; let mut rep_expanded = TokenStream :: new () ; let meta_vars = collect_meta_vars (contents . clone ()) ; minimal_quote ! (use crate :: ext ::*; (@ if sep_opt . is_some () { minimal_quote ! (let mut _i = 0usize ;) } else { minimal_quote ! (() ;) }) let has_iter = crate :: ThereIsNoIteratorInRepetition ;) . to_tokens (& mut rep_expanded) ; for meta_var in & meta_vars { minimal_quote ! (# [allow (unused_mut)] let (mut (@ meta_var) , i) = (@ meta_var) . quote_into_iter () ; let has_iter = has_iter | i ;) . to_tokens (& mut rep_expanded) ; } minimal_quote ! (let _ : crate :: HasIterator = has_iter ;) . to_tokens (& mut rep_expanded) ; let mut while_body = TokenStream :: new () ; for meta_var in & meta_vars { minimal_quote ! (let (@ meta_var) = match (@ meta_var) . next () { Some (_x) => crate :: RepInterp (_x) , None => break , } ;) . to_tokens (& mut while_body) ; } minimal_quote ! ((@ if let Some (sep) = sep_opt { minimal_quote ! (if _i > 0 { (@ minimal_quote ! (crate :: ToTokens :: to_tokens (& crate :: TokenTree :: Punct (crate :: Punct :: new ((@ TokenTree :: from (Literal :: character (sep . as_char ()))) , (@ minimal_quote ! (crate :: Spacing :: Alone)) ,)) , & mut ts) ;)) } _i += 1 ;) } else { minimal_quote ! (() ;) }) (@ quote (contents . clone ())) . to_tokens (& mut ts) ;) . to_tokens (& mut while_body) ; rep_expanded . extend (vec ! [TokenTree :: Ident (Ident :: new ("while" , Span :: call_site ())) , TokenTree :: Ident (Ident :: new ("true" , Span :: call_site ())) , TokenTree :: Group (Group :: new (Delimiter :: Brace , while_body)) ,]) ; minimal_quote ! ((@ TokenTree :: Group (Group :: new (Delimiter :: Brace , rep_expanded)))) . to_tokens (& mut tokens) ; continue ; } TokenTree :: Ident (_) => { minimal_quote ! (crate :: ToTokens :: to_tokens (& (@ tree) , & mut ts) ;) . to_tokens (& mut tokens) ; continue ; } TokenTree :: Punct (ref tt) if tt . as_char () == '$' => { } _ => panic ! ("`$` must be followed by an ident or `$` or a repetition group in `quote!`") , } } else if let TokenTree :: Punct (ref tt) = tree { if tt . as_char () == '$' { after_dollar = true ; continue ; } } match tree { TokenTree :: Punct (tt) => { minimal_quote ! (crate :: ToTokens :: to_tokens (& crate :: TokenTree :: Punct (crate :: Punct :: new ((@ TokenTree :: from (Literal :: character (tt . as_char ()))) , (@ match tt . spacing () { Spacing :: Alone => minimal_quote ! (crate :: Spacing :: Alone) , Spacing :: Joint => minimal_quote ! (crate :: Spacing :: Joint) , }) ,)) , & mut ts) ;) } TokenTree :: Group (tt) => { minimal_quote ! (crate :: ToTokens :: to_tokens (& crate :: TokenTree :: Group (crate :: Group :: new ((@ match tt . delimiter () { Delimiter :: Parenthesis => minimal_quote ! (crate :: Delimiter :: Parenthesis) , Delimiter :: Brace => minimal_quote ! (crate :: Delimiter :: Brace) , Delimiter :: Bracket => minimal_quote ! (crate :: Delimiter :: Bracket) , Delimiter :: None => minimal_quote ! (crate :: Delimiter :: None) , }) , (@ quote (tt . stream ())) ,)) , & mut ts) ;) } TokenTree :: Ident (tt) => { let literal = tt . to_string () ; let (literal , ctor) = if let Some (stripped) = literal . strip_prefix ("r#") { (stripped , minimal_quote ! (crate :: Ident :: new_raw)) } else { (literal . as_str () , minimal_quote ! (crate :: Ident :: new)) } ; minimal_quote ! (crate :: ToTokens :: to_tokens (& crate :: TokenTree :: Ident ((@ ctor) ((@ TokenTree :: from (Literal :: string (literal))) , (@ quote_span (proc_macro_crate . clone () , tt . span ())) ,)) , & mut ts) ;) } TokenTree :: Literal (tt) => { minimal_quote ! (crate :: ToTokens :: to_tokens (& crate :: TokenTree :: Literal ({ let mut iter = (@ TokenTree :: from (Literal :: string (& tt . to_string ()))) . parse ::< crate :: TokenStream > () . unwrap () . into_iter () ; if let (Some (crate :: TokenTree :: Literal (mut lit)) , None) = (iter . next () , iter . next ()) { lit . set_span ((@ quote_span (proc_macro_crate . clone () , tt . span ()))) ; lit } else { unreachable ! () } }) , & mut ts) ;) } } . to_tokens (& mut tokens) ; } if after_dollar { panic ! ("unexpected trailing `$` in `quote!`") ; } minimal_quote ! { { let mut ts = crate :: TokenStream :: new () ; (@ tokens) ts } } }
}

macro_rules! collect_meta_vars_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function collect_meta_vars in module {}", module_path!());
    };
}

mkfn!{
    collect_meta_vars_introspect!();
    # [doc = " Helper function to support macro repetitions like `$( CONTENTS ) SEP_OPT *` in `quote!`."] # [doc = " Recursively collects all `Ident`s (meta-variables) that follow a `$`"] # [doc = " from the given `CONTENTS` stream, preserving their order of appearance."] fn collect_meta_vars (content_stream : TokenStream) -> Vec < Ident > { fn helper (stream : TokenStream , out : & mut Vec < Ident >) { let mut iter = stream . into_iter () . peekable () ; while let Some (tree) = iter . next () { match & tree { TokenTree :: Punct (tt) if tt . as_char () == '$' => { if let Some (TokenTree :: Ident (id)) = iter . peek () { out . push (id . clone ()) ; iter . next () ; } } TokenTree :: Group (tt) => { helper (tt . stream () , out) ; } _ => { } } } } let mut vars = Vec :: new () ; helper (content_stream , & mut vars) ; vars }
}

macro_rules! quote_span_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function quote_span in module {}", module_path!());
    };
}

mkfn!{
    quote_span_introspect!();
    # [doc = " Quote a `Span` into a `TokenStream`."] # [doc = " This is needed to implement a custom quoter."] # [unstable (feature = "proc_macro_quote" , issue = "54722")] pub fn quote_span (proc_macro_crate : TokenStream , span : Span) -> TokenStream { let id = span . save_span () ; minimal_quote ! ((@ proc_macro_crate) :: Span :: recover_proc_macro_span ((@ TokenTree :: from (Literal :: usize_unsuffixed (id))))) }
}