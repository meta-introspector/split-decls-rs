mkuse!{use std :: assert_matches :: assert_matches ;}
mkuse!{use std :: cell :: Cell ;}
mkuse!{use std :: { cmp , iter , mem } ;}
mkuse!{use either :: { Left , Right } ;}
mkuse!{use rustc_const_eval :: check_consts :: { ConstCx , qualifs } ;}
mkuse!{use rustc_data_structures :: fx :: FxHashSet ;}
mkuse!{use rustc_hir as hir ;}
mkuse!{use rustc_index :: { IndexSlice , IndexVec } ;}
mkuse!{use rustc_middle :: mir :: visit :: { MutVisitor , MutatingUseContext , PlaceContext , Visitor } ;}
mkuse!{use rustc_middle :: mir :: * ;}
mkuse!{use rustc_middle :: ty :: { self , GenericArgs , List , Ty , TyCtxt , TypeVisitableExt } ;}
mkuse!{use rustc_middle :: { bug , mir , span_bug } ;}
mkuse!{use rustc_span :: Span ;}
mkuse!{use rustc_span :: source_map :: Spanned ;}
mkuse!{use tracing :: { debug , instrument } ;}
mkitem!{mkstruct!{# [doc = " A `MirPass` for promotion."] # [doc = ""] # [doc = " Promotion is the extraction of promotable temps into separate MIR bodies so they can have"] # [doc = " `'static` lifetime."] # [doc = ""] # [doc = " After this pass is run, `promoted_fragments` will hold the MIR body corresponding to each"] # [doc = " newly created `Constant`."] # [derive (Default)] pub (super) struct PromoteTemps < 'tcx > { pub promoted_fragments : Cell < IndexVec < Promoted , Body < 'tcx > > > , }}}
mkitem!{mkimpl!{impl < 'tcx > crate :: MirPass < 'tcx > for PromoteTemps < 'tcx > { fn run_pass (& self , tcx : TyCtxt < 'tcx > , body : & mut Body < 'tcx >) { if let Err (_) = body . return_ty () . error_reported () { debug ! ("PromoteTemps: MIR had errors") ; return ; } if body . source . promoted . is_some () { return ; } let ccx = ConstCx :: new (tcx , body) ; let (mut temps , all_candidates) = collect_temps_and_candidates (& ccx) ; let promotable_candidates = validate_candidates (& ccx , & mut temps , all_candidates) ; let promoted = promote_candidates (body , tcx , temps , promotable_candidates) ; self . promoted_fragments . set (promoted) ; } fn is_required (& self) -> bool { true } }}}
mkitem!{mkenum!{# [doc = " State of a temporary during collection and promotion."] # [derive (Copy , Clone , PartialEq , Eq , Debug)] enum TempState { # [doc = " No references to this temp."] Undefined , # [doc = " One direct assignment and any number of direct uses."] # [doc = " A borrow of this temp is promotable if the assigned"] # [doc = " value is qualified as constant."] Defined { location : Location , uses : usize , valid : Result < () , () > } , # [doc = " Any other combination of assignments/uses."] Unpromotable , # [doc = " This temp was part of an rvalue which got extracted"] # [doc = " during promotion and needs cleanup."] PromotedOut , }}}
mkitem!{mkstruct!{# [doc = " A \"root candidate\" for promotion, which will become the"] # [doc = " returned value in a promoted MIR, unless it's a subset"] # [doc = " of a larger candidate."] # [derive (Copy , Clone , PartialEq , Eq , Debug)] struct Candidate { location : Location , }}}
mkitem!{mkstruct!{struct Collector < 'a , 'tcx > { ccx : & 'a ConstCx < 'a , 'tcx > , temps : IndexVec < Local , TempState > , candidates : Vec < Candidate > , }}}
mkitem!{mkimpl!{impl < 'tcx > Visitor < 'tcx > for Collector < '_ , 'tcx > { # [instrument (level = "debug" , skip (self))] fn visit_local (& mut self , index : Local , context : PlaceContext , location : Location) { match self . ccx . body . local_kind (index) { LocalKind :: Arg => return , LocalKind :: Temp if self . ccx . body . local_decls [index] . is_user_variable () => return , LocalKind :: ReturnPointer | LocalKind :: Temp => { } } if context . is_drop () || ! context . is_use () { debug ! (is_drop = context . is_drop () , is_use = context . is_use ()) ; return ; } let temp = & mut self . temps [index] ; debug ! (? temp) ; * temp = match * temp { TempState :: Undefined => match context { PlaceContext :: MutatingUse (MutatingUseContext :: Store | MutatingUseContext :: Call) => { TempState :: Defined { location , uses : 0 , valid : Err (()) } } _ => TempState :: Unpromotable , } , TempState :: Defined { ref mut uses , .. } => { let allowed_use = match context { PlaceContext :: MutatingUse (MutatingUseContext :: Borrow) | PlaceContext :: NonMutatingUse (_) => true , PlaceContext :: MutatingUse (_) | PlaceContext :: NonUse (_) => false , } ; debug ! (? allowed_use) ; if allowed_use { * uses += 1 ; return ; } TempState :: Unpromotable } TempState :: Unpromotable | TempState :: PromotedOut => TempState :: Unpromotable , } ; debug ! (? temp) ; } fn visit_rvalue (& mut self , rvalue : & Rvalue < 'tcx > , location : Location) { self . super_rvalue (rvalue , location) ; if let Rvalue :: Ref (..) = * rvalue { self . candidates . push (Candidate { location }) ; } } }}}

macro_rules! collect_temps_and_candidates_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function collect_temps_and_candidates in module {}", module_path!());
    };
}

mkfn!{
    collect_temps_and_candidates_introspect!();
    fn collect_temps_and_candidates < 'tcx > (ccx : & ConstCx < '_ , 'tcx > ,) -> (IndexVec < Local , TempState > , Vec < Candidate >) { let mut collector = Collector { temps : IndexVec :: from_elem (TempState :: Undefined , & ccx . body . local_decls) , candidates : vec ! [] , ccx , } ; for (bb , data) in traversal :: reverse_postorder (ccx . body) { collector . visit_basic_block_data (bb , data) ; } (collector . temps , collector . candidates) }
}
mkitem!{mkstruct!{# [doc = " Checks whether locals that appear in a promotion context (`Candidate`) are actually promotable."] # [doc = ""] # [doc = " This wraps an `Item`, and has access to all fields of that `Item` via `Deref` coercion."] struct Validator < 'a , 'tcx > { ccx : & 'a ConstCx < 'a , 'tcx > , temps : & 'a mut IndexSlice < Local , TempState > , # [doc = " For backwards compatibility, we are promoting function calls in `const`/`static`"] # [doc = " initializers. But we want to avoid evaluating code that might panic and that otherwise would"] # [doc = " not have been evaluated, so we only promote such calls in basic blocks that are guaranteed"] # [doc = " to execute. In other words, we only promote such calls in basic blocks that are definitely"] # [doc = " not dead code. Here we cache the result of computing that set of basic blocks."] promotion_safe_blocks : Option < FxHashSet < BasicBlock > > , }}}
mkitem!{mkimpl!{impl < 'a , 'tcx > std :: ops :: Deref for Validator < 'a , 'tcx > { type Target = ConstCx < 'a , 'tcx > ; fn deref (& self) -> & Self :: Target { self . ccx } }}}
mkitem!{mkstruct!{struct Unpromotable ;}}
mkitem!{mkimpl!{impl < 'tcx > Validator < '_ , 'tcx > { fn validate_candidate (& mut self , candidate : Candidate) -> Result < () , Unpromotable > { let Left (statement) = self . body . stmt_at (candidate . location) else { bug ! () } ; let Some ((_ , Rvalue :: Ref (_ , kind , place))) = statement . kind . as_assign () else { bug ! () } ; self . validate_local (place . local) ? ; self . validate_ref (* kind , place) ? ; if place . projection . contains (& ProjectionElem :: Deref) { return Err (Unpromotable) ; } Ok (()) } fn qualif_local < Q : qualifs :: Qualif > (& mut self , local : Local) -> bool { let TempState :: Defined { location : loc , .. } = self . temps [local] else { return false ; } ; let stmt_or_term = self . body . stmt_at (loc) ; match stmt_or_term { Left (statement) => { let Some ((_ , rhs)) = statement . kind . as_assign () else { span_bug ! (statement . source_info . span , "{:?} is not an assignment" , statement) } ; qualifs :: in_rvalue :: < Q , _ > (self . ccx , & mut | l | self . qualif_local :: < Q > (l) , rhs) } Right (terminator) => { assert_matches ! (terminator . kind , TerminatorKind :: Call { .. }) ; let return_ty = self . body . local_decls [local] . ty ; Q :: in_any_value_of_ty (self . ccx , return_ty) } } } fn validate_local (& mut self , local : Local) -> Result < () , Unpromotable > { let TempState :: Defined { location : loc , uses , valid } = self . temps [local] else { return Err (Unpromotable) ; } ; if self . qualif_local :: < qualifs :: NeedsDrop > (local) { return Err (Unpromotable) ; } if valid . is_ok () { return Ok (()) ; } let ok = { let stmt_or_term = self . body . stmt_at (loc) ; match stmt_or_term { Left (statement) => { let Some ((_ , rhs)) = statement . kind . as_assign () else { span_bug ! (statement . source_info . span , "{:?} is not an assignment" , statement) } ; self . validate_rvalue (rhs) } Right (terminator) => match & terminator . kind { TerminatorKind :: Call { func , args , .. } => { self . validate_call (func , args , loc . block) } TerminatorKind :: Yield { .. } => Err (Unpromotable) , kind => { span_bug ! (terminator . source_info . span , "{:?} not promotable" , kind) ; } } , } } ; self . temps [local] = match ok { Ok (()) => TempState :: Defined { location : loc , uses , valid : Ok (()) } , Err (_) => TempState :: Unpromotable , } ; ok } fn validate_place (& mut self , place : PlaceRef < 'tcx >) -> Result < () , Unpromotable > { let Some ((place_base , elem)) = place . last_projection () else { return self . validate_local (place . local) ; } ; match elem { ProjectionElem :: ConstantIndex { .. } | ProjectionElem :: Subtype (_) | ProjectionElem :: Subslice { .. } | ProjectionElem :: UnwrapUnsafeBinder (_) => { } ProjectionElem :: OpaqueCast (..) | ProjectionElem :: Downcast (..) => { return Err (Unpromotable) ; } ProjectionElem :: Deref => { if let Some (local) = place_base . as_local () && let TempState :: Defined { location , .. } = self . temps [local] && let Left (def_stmt) = self . body . stmt_at (location) && let Some ((_ , Rvalue :: Use (Operand :: Constant (c)))) = def_stmt . kind . as_assign () && let Some (did) = c . check_static_ptr (self . tcx) && let Some (hir :: ConstContext :: Static (..)) = self . const_kind && ! self . tcx . is_thread_local_static (did) { } else { return Err (Unpromotable) ; } } ProjectionElem :: Index (local) => { if let TempState :: Defined { location : loc , .. } = self . temps [local] && let Left (statement) = self . body . stmt_at (loc) && let Some ((_ , Rvalue :: Use (Operand :: Constant (c)))) = statement . kind . as_assign () && let Some (idx) = c . const_ . try_eval_target_usize (self . tcx , self . typing_env) && let ty :: Array (_ , len) = place_base . ty (self . body , self . tcx) . ty . kind () && let Some (len) = len . try_to_target_usize (self . tcx) && idx < len { self . validate_local (local) ? ; } else { return Err (Unpromotable) ; } } ProjectionElem :: Field (..) => { let base_ty = place_base . ty (self . body , self . tcx) . ty ; if base_ty . is_union () { return Err (Unpromotable) ; } } } self . validate_place (place_base) } fn validate_operand (& mut self , operand : & Operand < 'tcx >) -> Result < () , Unpromotable > { match operand { Operand :: Copy (place) | Operand :: Move (place) => self . validate_place (place . as_ref ()) , Operand :: Constant (c) => { if let Some (def_id) = c . check_static_ptr (self . tcx) { let is_static = matches ! (self . const_kind , Some (hir :: ConstContext :: Static (_))) ; if ! is_static { return Err (Unpromotable) ; } let is_thread_local = self . tcx . is_thread_local_static (def_id) ; if is_thread_local { return Err (Unpromotable) ; } } Ok (()) } } } fn validate_ref (& mut self , kind : BorrowKind , place : & Place < 'tcx >) -> Result < () , Unpromotable > { match kind { BorrowKind :: Fake (_) | BorrowKind :: Mut { kind : MutBorrowKind :: ClosureCapture } => { return Err (Unpromotable) ; } BorrowKind :: Shared => { let has_mut_interior = self . qualif_local :: < qualifs :: HasMutInterior > (place . local) ; if has_mut_interior { return Err (Unpromotable) ; } } BorrowKind :: Mut { kind : MutBorrowKind :: Default | MutBorrowKind :: TwoPhaseBorrow } => { let ty = place . ty (self . body , self . tcx) . ty ; if let ty :: Array (_ , len) = ty . kind () { match len . try_to_target_usize (self . tcx) { Some (0) => { } _ => return Err (Unpromotable) , } } else { return Err (Unpromotable) ; } } } Ok (()) } fn validate_rvalue (& mut self , rvalue : & Rvalue < 'tcx >) -> Result < () , Unpromotable > { match rvalue { Rvalue :: Use (operand) | Rvalue :: Repeat (operand , _) | Rvalue :: WrapUnsafeBinder (operand , _) => { self . validate_operand (operand) ? ; } Rvalue :: CopyForDeref (place) => { let op = & Operand :: Copy (* place) ; self . validate_operand (op) ? } Rvalue :: Discriminant (place) | Rvalue :: Len (place) => { self . validate_place (place . as_ref ()) ? } Rvalue :: ThreadLocalRef (_) => return Err (Unpromotable) , Rvalue :: Cast (CastKind :: PointerExposeProvenance , _ , _) => return Err (Unpromotable) , Rvalue :: Cast (_ , operand , _) => { self . validate_operand (operand) ? ; } Rvalue :: NullaryOp (op , _) => match op { NullOp :: SizeOf => { } NullOp :: AlignOf => { } NullOp :: OffsetOf (_) => { } NullOp :: UbChecks => { } NullOp :: ContractChecks => { } } , Rvalue :: ShallowInitBox (_ , _) => return Err (Unpromotable) , Rvalue :: UnaryOp (op , operand) => { match op { UnOp :: Neg | UnOp :: Not | UnOp :: PtrMetadata => { } } self . validate_operand (operand) ? ; } Rvalue :: BinaryOp (op , box (lhs , rhs)) => { let op = * op ; let lhs_ty = lhs . ty (self . body , self . tcx) ; if let ty :: RawPtr (_ , _) | ty :: FnPtr (..) = lhs_ty . kind () { assert_matches ! (op , BinOp :: Eq | BinOp :: Ne | BinOp :: Le | BinOp :: Lt | BinOp :: Ge | BinOp :: Gt | BinOp :: Offset) ; return Err (Unpromotable) ; } match op { BinOp :: Div | BinOp :: Rem => { if lhs_ty . is_integral () { let sz = lhs_ty . primitive_size (self . tcx) ; let rhs_val = match rhs { Operand :: Constant (c) => { c . const_ . try_eval_scalar_int (self . tcx , self . typing_env) } _ => None , } ; match rhs_val . map (| x | x . to_uint (sz)) { Some (x) if x != 0 => { } _ => return Err (Unpromotable) , } if lhs_ty . is_signed () { match rhs_val . map (| x | x . to_int (sz)) { Some (- 1) | None => { let lhs_val = match lhs { Operand :: Constant (c) => c . const_ . try_eval_scalar_int (self . tcx , self . typing_env) , _ => None , } ; let lhs_min = sz . signed_int_min () ; match lhs_val . map (| x | x . to_int (sz)) { Some (x) if x != lhs_min => { } _ => return Err (Unpromotable) , } } _ => { } } } } } BinOp :: Eq | BinOp :: Ne | BinOp :: Le | BinOp :: Lt | BinOp :: Ge | BinOp :: Gt | BinOp :: Cmp | BinOp :: Offset | BinOp :: Add | BinOp :: AddUnchecked | BinOp :: AddWithOverflow | BinOp :: Sub | BinOp :: SubUnchecked | BinOp :: SubWithOverflow | BinOp :: Mul | BinOp :: MulUnchecked | BinOp :: MulWithOverflow | BinOp :: BitXor | BinOp :: BitAnd | BinOp :: BitOr | BinOp :: Shl | BinOp :: ShlUnchecked | BinOp :: Shr | BinOp :: ShrUnchecked => { } } self . validate_operand (lhs) ? ; self . validate_operand (rhs) ? ; } Rvalue :: RawPtr (_ , place) => { if let Some ((place_base , ProjectionElem :: Deref)) = place . as_ref () . last_projection () { let base_ty = place_base . ty (self . body , self . tcx) . ty ; if let ty :: Ref (..) = base_ty . kind () { return self . validate_place (place_base) ; } } return Err (Unpromotable) ; } Rvalue :: Ref (_ , kind , place) => { let mut place_simplified = place . as_ref () ; if let Some ((place_base , ProjectionElem :: Deref)) = place_simplified . last_projection () { let base_ty = place_base . ty (self . body , self . tcx) . ty ; if let ty :: Ref (..) = base_ty . kind () { place_simplified = place_base ; } } self . validate_place (place_simplified) ? ; self . validate_ref (* kind , place) ? ; } Rvalue :: Aggregate (_ , operands) => { for o in operands { self . validate_operand (o) ? ; } } } Ok (()) } # [doc = " Computes the sets of blocks of this MIR that are definitely going to be executed"] # [doc = " if the function returns successfully. That makes it safe to promote calls in them"] # [doc = " that might fail."] fn promotion_safe_blocks (body : & mir :: Body < 'tcx >) -> FxHashSet < BasicBlock > { let mut safe_blocks = FxHashSet :: default () ; let mut safe_block = START_BLOCK ; loop { safe_blocks . insert (safe_block) ; safe_block = match body . basic_blocks [safe_block] . terminator () . kind { TerminatorKind :: Goto { target } => target , TerminatorKind :: Call { target : Some (target) , .. } | TerminatorKind :: Drop { target , .. } => { target } TerminatorKind :: Assert { target , .. } => { target } _ => { break ; } } ; } safe_blocks } # [doc = " Returns whether the block is \"safe\" for promotion, which means it cannot be dead code."] # [doc = " We use this to avoid promoting operations that can fail in dead code."] fn is_promotion_safe_block (& mut self , block : BasicBlock) -> bool { let body = self . body ; let safe_blocks = self . promotion_safe_blocks . get_or_insert_with (| | Self :: promotion_safe_blocks (body)) ; safe_blocks . contains (& block) } fn validate_call (& mut self , callee : & Operand < 'tcx > , args : & [Spanned < Operand < 'tcx > >] , block : BasicBlock ,) -> Result < () , Unpromotable > { self . validate_operand (callee) ? ; for arg in args { self . validate_operand (& arg . node) ? ; } let fn_ty = callee . ty (self . body , self . tcx) ; if let ty :: FnDef (def_id , _) = * fn_ty . kind () { if self . tcx . is_promotable_const_fn (def_id) { return Ok (()) ; } } let promote_all_fn = matches ! (self . const_kind , Some (hir :: ConstContext :: Static (_) | hir :: ConstContext :: Const { inline : false })) ; if ! promote_all_fn { return Err (Unpromotable) ; } let is_const_fn = match * fn_ty . kind () { ty :: FnDef (def_id , _) => self . tcx . is_const_fn (def_id) , _ => false , } ; if ! is_const_fn { return Err (Unpromotable) ; } if ! self . is_promotion_safe_block (block) { return Err (Unpromotable) ; } Ok (()) } }}}

macro_rules! validate_candidates_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function validate_candidates in module {}", module_path!());
    };
}

mkfn!{
    validate_candidates_introspect!();
    fn validate_candidates (ccx : & ConstCx < '_ , '_ > , temps : & mut IndexSlice < Local , TempState > , mut candidates : Vec < Candidate > ,) -> Vec < Candidate > { let mut validator = Validator { ccx , temps , promotion_safe_blocks : None } ; candidates . retain (| & candidate | validator . validate_candidate (candidate) . is_ok ()) ; candidates }
}
mkitem!{mkstruct!{struct Promoter < 'a , 'tcx > { tcx : TyCtxt < 'tcx > , source : & 'a mut Body < 'tcx > , promoted : Body < 'tcx > , temps : & 'a mut IndexVec < Local , TempState > , extra_statements : & 'a mut Vec < (Location , Statement < 'tcx >) > , # [doc = " Used to assemble the required_consts list while building the promoted."] required_consts : Vec < ConstOperand < 'tcx > > , # [doc = " If true, all nested temps are also kept in the"] # [doc = " source MIR, not moved to the promoted MIR."] keep_original : bool , # [doc = " If true, add the new const (the promoted) to the required_consts of the parent MIR."] # [doc = " This is initially false and then set by the visitor when it encounters a `Call` terminator."] add_to_required : bool , }}}
mkitem!{mkimpl!{impl < 'a , 'tcx > Promoter < 'a , 'tcx > { fn new_block (& mut self) -> BasicBlock { let span = self . promoted . span ; self . promoted . basic_blocks_mut () . push (BasicBlockData :: new (Some (Terminator { source_info : SourceInfo :: outermost (span) , kind : TerminatorKind :: Return , }) , false ,)) } fn assign (& mut self , dest : Local , rvalue : Rvalue < 'tcx > , span : Span) { let last = self . promoted . basic_blocks . last_index () . unwrap () ; let data = & mut self . promoted [last] ; data . statements . push (Statement :: new (SourceInfo :: outermost (span) , StatementKind :: Assign (Box :: new ((Place :: from (dest) , rvalue))) ,)) ; } fn is_temp_kind (& self , local : Local) -> bool { self . source . local_kind (local) == LocalKind :: Temp } # [doc = " Copies the initialization of this temp to the"] # [doc = " promoted MIR, recursing through temps."] fn promote_temp (& mut self , temp : Local) -> Local { let old_keep_original = self . keep_original ; let loc = match self . temps [temp] { TempState :: Defined { location , uses , .. } if uses > 0 => { if uses > 1 { self . keep_original = true ; } location } state => { span_bug ! (self . promoted . span , "{:?} not promotable: {:?}" , temp , state) ; } } ; if ! self . keep_original { self . temps [temp] = TempState :: PromotedOut ; } let num_stmts = self . source [loc . block] . statements . len () ; let new_temp = self . promoted . local_decls . push (LocalDecl :: new (self . source . local_decls [temp] . ty , self . source . local_decls [temp] . source_info . span ,)) ; debug ! ("promote({:?} @ {:?}/{:?}, {:?})" , temp , loc , num_stmts , self . keep_original) ; if loc . statement_index < num_stmts { let (mut rvalue , source_info) = { let statement = & mut self . source [loc . block] . statements [loc . statement_index] ; let StatementKind :: Assign (box (_ , rhs)) = & mut statement . kind else { span_bug ! (statement . source_info . span , "{:?} is not an assignment" , statement) ; } ; (if self . keep_original { rhs . clone () } else { let unit = Rvalue :: Use (Operand :: Constant (Box :: new (ConstOperand { span : statement . source_info . span , user_ty : None , const_ : Const :: zero_sized (self . tcx . types . unit) , }))) ; mem :: replace (rhs , unit) } , statement . source_info ,) } ; self . visit_rvalue (& mut rvalue , loc) ; self . assign (new_temp , rvalue , source_info . span) ; } else { let terminator = if self . keep_original { self . source [loc . block] . terminator () . clone () } else { let terminator = self . source [loc . block] . terminator_mut () ; let target = match & terminator . kind { TerminatorKind :: Call { target : Some (target) , .. } => * target , kind => { span_bug ! (terminator . source_info . span , "{:?} not promotable" , kind) ; } } ; Terminator { source_info : terminator . source_info , kind : mem :: replace (& mut terminator . kind , TerminatorKind :: Goto { target }) , } } ; match terminator . kind { TerminatorKind :: Call { mut func , mut args , call_source : desugar , fn_span , .. } => { self . add_to_required = true ; self . visit_operand (& mut func , loc) ; for arg in & mut args { self . visit_operand (& mut arg . node , loc) ; } let last = self . promoted . basic_blocks . last_index () . unwrap () ; let new_target = self . new_block () ; * self . promoted [last] . terminator_mut () = Terminator { kind : TerminatorKind :: Call { func , args , unwind : UnwindAction :: Continue , destination : Place :: from (new_temp) , target : Some (new_target) , call_source : desugar , fn_span , } , source_info : SourceInfo :: outermost (terminator . source_info . span) , .. terminator } ; } kind => { span_bug ! (terminator . source_info . span , "{:?} not promotable" , kind) ; } } ; } ; self . keep_original = old_keep_original ; new_temp } fn promote_candidate (mut self , candidate : Candidate , next_promoted_index : Promoted ,) -> Body < 'tcx > { let def = self . source . source . def_id () ; let (mut rvalue , promoted_op) = { let promoted = & mut self . promoted ; let tcx = self . tcx ; let mut promoted_operand = | ty , span | { promoted . span = span ; promoted . local_decls [RETURN_PLACE] = LocalDecl :: new (ty , span) ; let args = tcx . erase_and_anonymize_regions (GenericArgs :: identity_for_item (tcx , def)) ; let uneval = mir :: UnevaluatedConst { def , args , promoted : Some (next_promoted_index) } ; ConstOperand { span , user_ty : None , const_ : Const :: Unevaluated (uneval , ty) } } ; let blocks = self . source . basic_blocks . as_mut () ; let local_decls = & mut self . source . local_decls ; let loc = candidate . location ; let statement = & mut blocks [loc . block] . statements [loc . statement_index] ; let StatementKind :: Assign (box (_ , Rvalue :: Ref (region , borrow_kind , place))) = & mut statement . kind else { bug ! () } ; debug_assert ! (region . is_erased ()) ; let ty = local_decls [place . local] . ty ; let span = statement . source_info . span ; let ref_ty = Ty :: new_ref (tcx , tcx . lifetimes . re_erased , ty , borrow_kind . to_mutbl_lossy ()) ; let mut projection = vec ! [PlaceElem :: Deref] ; projection . extend (place . projection) ; place . projection = tcx . mk_place_elems (& projection) ; let mut promoted_ref = LocalDecl :: new (ref_ty , span) ; promoted_ref . source_info = statement . source_info ; let promoted_ref = local_decls . push (promoted_ref) ; assert_eq ! (self . temps . push (TempState :: Unpromotable) , promoted_ref) ; let promoted_operand = promoted_operand (ref_ty , span) ; let promoted_ref_statement = Statement :: new (statement . source_info , StatementKind :: Assign (Box :: new ((Place :: from (promoted_ref) , Rvalue :: Use (Operand :: Constant (Box :: new (promoted_operand))) ,))) ,) ; self . extra_statements . push ((loc , promoted_ref_statement)) ; (Rvalue :: Ref (tcx . lifetimes . re_erased , * borrow_kind , Place { local : mem :: replace (& mut place . local , promoted_ref) , projection : List :: empty () , } ,) , promoted_operand ,) } ; assert_eq ! (self . new_block () , START_BLOCK) ; self . visit_rvalue (& mut rvalue , Location { block : START_BLOCK , statement_index : usize :: MAX } ,) ; let span = self . promoted . span ; self . assign (RETURN_PLACE , rvalue , span) ; if self . add_to_required { self . source . required_consts . as_mut () . unwrap () . push (promoted_op) ; } self . promoted . set_required_consts (self . required_consts) ; self . promoted } }}}
mkitem!{mkimpl!{# [doc = " Replaces all temporaries with their promoted counterparts."] impl < 'a , 'tcx > MutVisitor < 'tcx > for Promoter < 'a , 'tcx > { fn tcx (& self) -> TyCtxt < 'tcx > { self . tcx } fn visit_local (& mut self , local : & mut Local , _ : PlaceContext , _ : Location) { if self . is_temp_kind (* local) { * local = self . promote_temp (* local) ; } } fn visit_const_operand (& mut self , constant : & mut ConstOperand < 'tcx > , _location : Location) { if constant . const_ . is_required_const () { self . required_consts . push (* constant) ; } } }}}

macro_rules! promote_candidates_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function promote_candidates in module {}", module_path!());
    };
}

mkfn!{
    promote_candidates_introspect!();
    fn promote_candidates < 'tcx > (body : & mut Body < 'tcx > , tcx : TyCtxt < 'tcx > , mut temps : IndexVec < Local , TempState > , candidates : Vec < Candidate > ,) -> IndexVec < Promoted , Body < 'tcx > > { debug ! (promote_candidates = ? candidates) ; if candidates . is_empty () { return IndexVec :: new () ; } let mut promotions = IndexVec :: new () ; let mut extra_statements = vec ! [] ; for candidate in candidates . into_iter () . rev () { let Location { block , statement_index } = candidate . location ; if let StatementKind :: Assign (box (place , _)) = & body [block] . statements [statement_index] . kind && let Some (local) = place . as_local () { if temps [local] == TempState :: PromotedOut { continue ; } } let initial_locals = iter :: once (LocalDecl :: new (tcx . types . never , body . span)) . collect () ; let mut scope = body . source_scopes [body . source_info (candidate . location) . scope] . clone () ; scope . parent_scope = None ; let mut promoted = Body :: new (body . source , IndexVec :: new () , IndexVec :: from_elem_n (scope , 1) , initial_locals , IndexVec :: new () , 0 , vec ! [] , body . span , None , body . tainted_by_errors ,) ; promoted . phase = MirPhase :: Analysis (AnalysisPhase :: Initial) ; let promoter = Promoter { promoted , tcx , source : body , temps : & mut temps , extra_statements : & mut extra_statements , keep_original : false , add_to_required : false , required_consts : Vec :: new () , } ; let mut promoted = promoter . promote_candidate (candidate , promotions . next_index ()) ; promoted . source . promoted = Some (promotions . next_index ()) ; promotions . push (promoted) ; } extra_statements . sort_by_key (| & (loc , _) | cmp :: Reverse (loc)) ; for (loc , statement) in extra_statements { body [loc . block] . statements . insert (loc . statement_index , statement) ; } let promoted = | index : Local | temps [index] == TempState :: PromotedOut ; for block in body . basic_blocks_mut () { block . statements . retain (| statement | match & statement . kind { StatementKind :: Assign (box (place , _)) => { if let Some (index) = place . as_local () { ! promoted (index) } else { true } } StatementKind :: StorageLive (index) | StatementKind :: StorageDead (index) => { ! promoted (* index) } _ => true , }) ; let terminator = block . terminator_mut () ; if let TerminatorKind :: Drop { place , target , .. } = & terminator . kind && let Some (index) = place . as_local () { if promoted (index) { terminator . kind = TerminatorKind :: Goto { target : * target } ; } } } promotions }
}