# ! [doc = " A folding traversal mechanism for complex data structures that contain type"] # ! [doc = " information."] # ! [doc = ""] # ! [doc = " This is a modifying traversal. It consumes the data structure, producing a"] # ! [doc = " (possibly) modified version of it. Both fallible and infallible versions are"] # ! [doc = " available. The name is potentially confusing, because this traversal is more"] # ! [doc = " like `Iterator::map` than `Iterator::fold`."] # ! [doc = ""] # ! [doc = " This traversal has limited flexibility. Only a small number of \"types of"] # ! [doc = " interest\" within the complex data structures can receive custom"] # ! [doc = " modification. These are the ones containing the most important type-related"] # ! [doc = " information, such as `Ty`, `Predicate`, `Region`, and `Const`."] # ! [doc = ""] # ! [doc = " There are three traits involved in each traversal."] # ! [doc = " - `TypeFoldable`. This is implemented once for many types, including:"] # ! [doc = "   - Types of interest, for which the methods delegate to the folder."] # ! [doc = "   - All other types, including generic containers like `Vec` and `Option`."] # ! [doc = "     It defines a \"skeleton\" of how they should be folded."] # ! [doc = " - `TypeSuperFoldable`. This is implemented only for recursive types of"] # ! [doc = "   interest, and defines the folding \"skeleton\" for these types. (This"] # ! [doc = "   excludes `Region` because it is non-recursive, i.e. it never contains"] # ! [doc = "   other types of interest.)"] # ! [doc = " - `TypeFolder`/`FallibleTypeFolder`. One of these is implemented for each"] # ! [doc = "   folder. This defines how types of interest are folded."] # ! [doc = ""] # ! [doc = " This means each fold is a mixture of (a) generic folding operations, and (b)"] # ! [doc = " custom fold operations that are specific to the folder."] # ! [doc = " - The `TypeFoldable` impls handle most of the traversal, and call into"] # ! [doc = "   `TypeFolder`/`FallibleTypeFolder` when they encounter a type of interest."] # ! [doc = " - A `TypeFolder`/`FallibleTypeFolder` may call into another `TypeFoldable`"] # ! [doc = "   impl, because some of the types of interest are recursive and can contain"] # ! [doc = "   other types of interest."] # ! [doc = " - A `TypeFolder`/`FallibleTypeFolder` may also call into a `TypeSuperFoldable`"] # ! [doc = "   impl, because each folder might provide custom handling only for some types"] # ! [doc = "   of interest, or only for some variants of each type of interest, and then"] # ! [doc = "   use default traversal for the remaining cases."] # ! [doc = ""] # ! [doc = " For example, if you have `struct S(Ty, U)` where `S: TypeFoldable` and `U:"] # ! [doc = " TypeFoldable`, and an instance `s = S(ty, u)`, it would be folded like so:"] # ! [doc = " ```text"] # ! [doc = " s.fold_with(folder) calls"] # ! [doc = " - ty.fold_with(folder) calls"] # ! [doc = "   - folder.fold_ty(ty) may call"] # ! [doc = "     - ty.super_fold_with(folder)"] # ! [doc = " - u.fold_with(folder)"] # ! [doc = " ```"] use split_decls_genesis :: ourprelude :: * ; use std :: convert :: Infallible ; use std :: mem ; use std :: sync :: Arc ; use rustc_index :: { Idx , IndexVec } ; use thin_vec :: ThinVec ; use tracing :: { debug , instrument } ; use crate :: inherent :: * ; use crate :: visit :: { TypeVisitable , TypeVisitableExt as _ } ; use crate :: { self as ty , Interner , TypeFlags } ; #[doc = " This trait is implemented for every type that can be folded,"] #[doc = " providing the skeleton of the traversal."] #[doc = ""] #[doc = " To implement this conveniently, use the derive macro located in"] #[doc = " `rustc_macros`."] #[doc = ""] #[doc = " This trait is a sub-trait of `TypeVisitable`. This is because many"] #[doc = " `TypeFolder` instances use the methods in `TypeVisitableExt` while folding,"] #[doc = " which means in practice almost every foldable type needs to also be"] #[doc = " visitable. (However, there are some types that are visitable without being"] #[doc = " foldable.)"] pub trait TypeFoldable < I : Interner > : TypeVisitable < I > + Clone { #[doc = " The entry point for folding. To fold a value `t` with a folder `f`"] #[doc = " call: `t.try_fold_with(f)`."] #[doc = ""] #[doc = " For most types, this just traverses the value, calling `try_fold_with`"] #[doc = " on each field/element."] #[doc = ""] #[doc = " For types of interest (such as `Ty`), the implementation of this method"] #[doc = " calls a folder method specifically for that type (such as"] #[doc = " `F::try_fold_ty`). This is where control transfers from [`TypeFoldable`]"] #[doc = " to [`FallibleTypeFolder`]."] fn try_fold_with < F : FallibleTypeFolder < I > > (self , folder : & mut F) -> Result < Self , F :: Error > ; #[doc = " The entry point for folding. To fold a value `t` with a folder `f`"] #[doc = " call: `t.fold_with(f)`."] #[doc = ""] #[doc = " For most types, this just traverses the value, calling `fold_with`"] #[doc = " on each field/element."] #[doc = ""] #[doc = " For types of interest (such as `Ty`), the implementation of this method"] #[doc = " calls a folder method specifically for that type (such as"] #[doc = " `F::fold_ty`). This is where control transfers from `TypeFoldable`"] #[doc = " to `TypeFolder`."] #[doc = ""] #[doc = " Same as [`TypeFoldable::try_fold_with`], but not fallible. Make sure to keep"] #[doc = " the behavior in sync across functions."] fn fold_with < F : TypeFolder < I > > (self , folder : & mut F) -> Self ; } pub trait TypeSuperFoldable < I : Interner > : TypeFoldable < I > { #[doc = " Provides a default fold for a recursive type of interest. This should"] #[doc = " only be called within `TypeFolder` methods, when a non-custom traversal"] #[doc = " is desired for the value of the type of interest passed to that method."] #[doc = " For example, in `MyFolder::try_fold_ty(ty)`, it is valid to call"] #[doc = " `ty.try_super_fold_with(self)`, but any other folding should be done"] #[doc = " with `xyz.try_fold_with(self)`."] fn try_super_fold_with < F : FallibleTypeFolder < I > > (self , folder : & mut F ,) -> Result < Self , F :: Error > ; #[doc = " A convenient alternative to `try_super_fold_with` for use with"] #[doc = " infallible folders. Do not override this method, to ensure coherence"] #[doc = " with `try_super_fold_with`."] fn super_fold_with < F : TypeFolder < I > > (self , folder : & mut F) -> Self ; } #[doc = " This trait is implemented for every infallible folding traversal. There is"] #[doc = " a fold method defined for every type of interest. Each such method has a"] #[doc = " default that does an \"identity\" fold. Implementations of these methods"] #[doc = " often fall back to a `super_fold_with` method if the primary argument"] #[doc = " doesn't satisfy a particular condition."] #[doc = ""] #[doc = " A blanket implementation of [`FallibleTypeFolder`] will defer to"] #[doc = " the infallible methods of this trait to ensure that the two APIs"] #[doc = " are coherent."] pub trait TypeFolder < I : Interner > : Sized { fn cx (& self) -> I ; fn fold_binder < T > (& mut self , t : ty :: Binder < I , T >) -> ty :: Binder < I , T > where T : TypeFoldable < I > , { t . super_fold_with (self) } fn fold_ty (& mut self , t : I :: Ty) -> I :: Ty { t . super_fold_with (self) } fn fold_region (& mut self , r : I :: Region) -> I :: Region { r } fn fold_const (& mut self , c : I :: Const) -> I :: Const { c . super_fold_with (self) } fn fold_predicate (& mut self , p : I :: Predicate) -> I :: Predicate { p . super_fold_with (self) } fn fold_clauses (& mut self , c : I :: Clauses) -> I :: Clauses { c . super_fold_with (self) } } #[doc = " This trait is implemented for every folding traversal. There is a fold"] #[doc = " method defined for every type of interest. Each such method has a default"] #[doc = " that does an \"identity\" fold."] #[doc = ""] #[doc = " A blanket implementation of this trait (that defers to the relevant"] #[doc = " method of [`TypeFolder`]) is provided for all infallible folders in"] #[doc = " order to ensure the two APIs are coherent."] pub trait FallibleTypeFolder < I : Interner > : Sized { type Error ; fn cx (& self) -> I ; fn try_fold_binder < T > (& mut self , t : ty :: Binder < I , T >) -> Result < ty :: Binder < I , T > , Self :: Error > where T : TypeFoldable < I > , { t . try_super_fold_with (self) } fn try_fold_ty (& mut self , t : I :: Ty) -> Result < I :: Ty , Self :: Error > { t . try_super_fold_with (self) } fn try_fold_region (& mut self , r : I :: Region) -> Result < I :: Region , Self :: Error > { Ok (r) } fn try_fold_const (& mut self , c : I :: Const) -> Result < I :: Const , Self :: Error > { c . try_super_fold_with (self) } fn try_fold_predicate (& mut self , p : I :: Predicate) -> Result < I :: Predicate , Self :: Error > { p . try_super_fold_with (self) } fn try_fold_clauses (& mut self , c : I :: Clauses) -> Result < I :: Clauses , Self :: Error > { c . try_super_fold_with (self) } } impl < I : Interner , T : TypeFoldable < I > , U : TypeFoldable < I > > TypeFoldable < I > for (T , U) { fn try_fold_with < F : FallibleTypeFolder < I > > (self , folder : & mut F) -> Result < (T , U) , F :: Error > { Ok ((self . 0 . try_fold_with (folder) ? , self . 1 . try_fold_with (folder) ?)) } fn fold_with < F : TypeFolder < I > > (self , folder : & mut F) -> Self { (self . 0 . fold_with (folder) , self . 1 . fold_with (folder)) } } impl < I : Interner , A : TypeFoldable < I > , B : TypeFoldable < I > , C : TypeFoldable < I > > TypeFoldable < I > for (A , B , C) { fn try_fold_with < F : FallibleTypeFolder < I > > (self , folder : & mut F ,) -> Result < (A , B , C) , F :: Error > { Ok ((self . 0 . try_fold_with (folder) ? , self . 1 . try_fold_with (folder) ? , self . 2 . try_fold_with (folder) ? ,)) } fn fold_with < F : TypeFolder < I > > (self , folder : & mut F) -> Self { (self . 0 . fold_with (folder) , self . 1 . fold_with (folder) , self . 2 . fold_with (folder)) } } impl < I : Interner , T : TypeFoldable < I > > TypeFoldable < I > for Option < T > { fn try_fold_with < F : FallibleTypeFolder < I > > (self , folder : & mut F) -> Result < Self , F :: Error > { Ok (match self { Some (v) => Some (v . try_fold_with (folder) ?) , None => None , }) } fn fold_with < F : TypeFolder < I > > (self , folder : & mut F) -> Self { Some (self ? . fold_with (folder)) } } impl < I : Interner , T : TypeFoldable < I > , E : TypeFoldable < I > > TypeFoldable < I > for Result < T , E > { fn try_fold_with < F : FallibleTypeFolder < I > > (self , folder : & mut F) -> Result < Self , F :: Error > { Ok (match self { Ok (v) => Ok (v . try_fold_with (folder) ?) , Err (e) => Err (e . try_fold_with (folder) ?) , }) } fn fold_with < F : TypeFolder < I > > (self , folder : & mut F) -> Self { match self { Ok (v) => Ok (v . fold_with (folder)) , Err (e) => Err (e . fold_with (folder)) , } } } fn fold_arc < T : Clone , E > (mut arc : Arc < T > , fold : impl FnOnce (T) -> Result < T , E > ,) -> Result < Arc < T > , E > { unsafe { Arc :: make_mut (& mut arc) ; let ptr = Arc :: into_raw (arc) . cast :: < mem :: ManuallyDrop < T > > () ; let mut unique = Arc :: from_raw (ptr) ; let slot = Arc :: get_mut (& mut unique) . unwrap_unchecked () ; let owned = mem :: ManuallyDrop :: take (slot) ; let folded = fold (owned) ? ; * slot = mem :: ManuallyDrop :: new (folded) ; Ok (Arc :: from_raw (Arc :: into_raw (unique) . cast ())) } } impl < I : Interner , T : TypeFoldable < I > > TypeFoldable < I > for Arc < T > { fn try_fold_with < F : FallibleTypeFolder < I > > (self , folder : & mut F) -> Result < Self , F :: Error > { fold_arc (self , | t | t . try_fold_with (folder)) } fn fold_with < F : TypeFolder < I > > (self , folder : & mut F) -> Self { match fold_arc :: < T , Infallible > (self , | t | Ok (t . fold_with (folder))) { Ok (t) => t , } } } impl < I : Interner , T : TypeFoldable < I > > TypeFoldable < I > for Box < T > { fn try_fold_with < F : FallibleTypeFolder < I > > (mut self , folder : & mut F) -> Result < Self , F :: Error > { * self = (* self) . try_fold_with (folder) ? ; Ok (self) } fn fold_with < F : TypeFolder < I > > (mut self , folder : & mut F) -> Self { * self = (* self) . fold_with (folder) ; self } } impl < I : Interner , T : TypeFoldable < I > > TypeFoldable < I > for Vec < T > { fn try_fold_with < F : FallibleTypeFolder < I > > (self , folder : & mut F) -> Result < Self , F :: Error > { self . into_iter () . map (| t | t . try_fold_with (folder)) . collect () } fn fold_with < F : TypeFolder < I > > (self , folder : & mut F) -> Self { self . into_iter () . map (| t | t . fold_with (folder)) . collect () } } impl < I : Interner , T : TypeFoldable < I > > TypeFoldable < I > for ThinVec < T > { fn try_fold_with < F : FallibleTypeFolder < I > > (self , folder : & mut F) -> Result < Self , F :: Error > { self . into_iter () . map (| t | t . try_fold_with (folder)) . collect () } fn fold_with < F : TypeFolder < I > > (self , folder : & mut F) -> Self { self . into_iter () . map (| t | t . fold_with (folder)) . collect () } } impl < I : Interner , T : TypeFoldable < I > > TypeFoldable < I > for Box < [T] > { fn try_fold_with < F : FallibleTypeFolder < I > > (self , folder : & mut F) -> Result < Self , F :: Error > { Vec :: from (self) . try_fold_with (folder) . map (Vec :: into_boxed_slice) } fn fold_with < F : TypeFolder < I > > (self , folder : & mut F) -> Self { Vec :: into_boxed_slice (Vec :: from (self) . fold_with (folder)) } } impl < I : Interner , T : TypeFoldable < I > , Ix : Idx > TypeFoldable < I > for IndexVec < Ix , T > { fn try_fold_with < F : FallibleTypeFolder < I > > (self , folder : & mut F) -> Result < Self , F :: Error > { self . raw . try_fold_with (folder) . map (IndexVec :: from_raw) } fn fold_with < F : TypeFolder < I > > (self , folder : & mut F) -> Self { IndexVec :: from_raw (self . raw . fold_with (folder)) } } struct Shifter < I : Interner > { cx : I , current_index : ty :: DebruijnIndex , amount : u32 , } impl < I : Interner > Shifter < I > { fn new (cx : I , amount : u32) -> Self { Shifter { cx , current_index : ty :: INNERMOST , amount } } } impl < I : Interner > TypeFolder < I > for Shifter < I > { fn cx (& self) -> I { self . cx } fn fold_binder < T : TypeFoldable < I > > (& mut self , t : ty :: Binder < I , T >) -> ty :: Binder < I , T > { self . current_index . shift_in (1) ; let t = t . super_fold_with (self) ; self . current_index . shift_out (1) ; t } fn fold_region (& mut self , r : I :: Region) -> I :: Region { match r . kind () { ty :: ReBound (debruijn , br) if debruijn >= self . current_index => { let debruijn = debruijn . shifted_in (self . amount) ; Region :: new_bound (self . cx , debruijn , br) } _ => r , } } fn fold_ty (& mut self , ty : I :: Ty) -> I :: Ty { match ty . kind () { ty :: Bound (debruijn , bound_ty) if debruijn >= self . current_index => { let debruijn = debruijn . shifted_in (self . amount) ; Ty :: new_bound (self . cx , debruijn , bound_ty) } _ if ty . has_vars_bound_at_or_above (self . current_index) => ty . super_fold_with (self) , _ => ty , } } fn fold_const (& mut self , ct : I :: Const) -> I :: Const { match ct . kind () { ty :: ConstKind :: Bound (debruijn , bound_ct) if debruijn >= self . current_index => { let debruijn = debruijn . shifted_in (self . amount) ; Const :: new_bound (self . cx , debruijn , bound_ct) } _ => ct . super_fold_with (self) , } } fn fold_predicate (& mut self , p : I :: Predicate) -> I :: Predicate { if p . has_vars_bound_at_or_above (self . current_index) { p . super_fold_with (self) } else { p } } } pub fn shift_region < I : Interner > (cx : I , region : I :: Region , amount : u32) -> I :: Region { match region . kind () { ty :: ReBound (debruijn , br) if amount > 0 => { Region :: new_bound (cx , debruijn . shifted_in (amount) , br) } _ => region , } } #[instrument (level = "trace" , skip (cx) , ret)] pub fn shift_vars < I : Interner , T > (cx : I , value : T , amount : u32) -> T where T : TypeFoldable < I > , { if amount == 0 || ! value . has_escaping_bound_vars () { value } else { value . fold_with (& mut Shifter :: new (cx , amount)) } } pub fn fold_regions < I : Interner , T > (cx : I , value : T , f : impl FnMut (I :: Region , ty :: DebruijnIndex) -> I :: Region ,) -> T where T : TypeFoldable < I > , { value . fold_with (& mut RegionFolder :: new (cx , f)) } #[doc = " Folds over the substructure of a type, visiting its component"] #[doc = " types and all regions that occur *free* within it."] #[doc = ""] #[doc = " That is, function pointer types and trait object can introduce"] #[doc = " new bound regions which are not visited by this visitors as"] #[doc = " they are not free; only regions that occur free will be"] #[doc = " visited by `fld_r`."] pub struct RegionFolder < I , F > { cx : I , #[doc = " Stores the index of a binder *just outside* the stuff we have"] #[doc = " visited. So this begins as INNERMOST; when we pass through a"] #[doc = " binder, it is incremented (via `shift_in`)."] current_index : ty :: DebruijnIndex , #[doc = " Callback invokes for each free region. The `DebruijnIndex`"] #[doc = " points to the binder *just outside* the ones we have passed"] #[doc = " through."] fold_region_fn : F , } impl < I , F > RegionFolder < I , F > { #[inline] pub fn new (cx : I , fold_region_fn : F) -> RegionFolder < I , F > { RegionFolder { cx , current_index : ty :: INNERMOST , fold_region_fn } } } impl < I , F > TypeFolder < I > for RegionFolder < I , F > where I : Interner , F : FnMut (I :: Region , ty :: DebruijnIndex) -> I :: Region , { fn cx (& self) -> I { self . cx } fn fold_binder < T : TypeFoldable < I > > (& mut self , t : ty :: Binder < I , T >) -> ty :: Binder < I , T > { self . current_index . shift_in (1) ; let t = t . super_fold_with (self) ; self . current_index . shift_out (1) ; t } #[instrument (skip (self) , level = "debug" , ret)] fn fold_region (& mut self , r : I :: Region) -> I :: Region { match r . kind () { ty :: ReBound (debruijn , _) if debruijn < self . current_index => { debug ! (? self . current_index , "skipped bound region") ; r } _ => { debug ! (? self . current_index , "folding free region") ; (self . fold_region_fn) (r , self . current_index) } } } fn fold_ty (& mut self , t : I :: Ty) -> I :: Ty { if t . has_type_flags (TypeFlags :: HAS_FREE_REGIONS | TypeFlags :: HAS_RE_BOUND | TypeFlags :: HAS_RE_ERASED ,) { t . super_fold_with (self) } else { t } } fn fold_const (& mut self , ct : I :: Const) -> I :: Const { if ct . has_type_flags (TypeFlags :: HAS_FREE_REGIONS | TypeFlags :: HAS_RE_BOUND | TypeFlags :: HAS_RE_ERASED ,) { ct . super_fold_with (self) } else { ct } } fn fold_predicate (& mut self , p : I :: Predicate) -> I :: Predicate { if p . has_type_flags (TypeFlags :: HAS_FREE_REGIONS | TypeFlags :: HAS_RE_BOUND | TypeFlags :: HAS_RE_ERASED ,) { p . super_fold_with (self) } else { p } } }