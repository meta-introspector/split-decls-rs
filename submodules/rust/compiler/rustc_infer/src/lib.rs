# ! [doc = " This crates defines the type inference engine."] # ! [doc = ""] # ! [doc = " - **Type inference.** The type inference code can be found in the `infer` module;"] # ! [doc = "   this code handles low-level equality and subtyping operations. The"] # ! [doc = "   type check pass in the compiler is found in the `rustc_hir_analysis` crate."] # ! [doc = ""] # ! [doc = " For more information about how rustc works, see the [rustc dev guide]."] # ! [doc = ""] # ! [doc = " [rustc dev guide]: https://rustc-dev-guide.rust-lang.org/"] # ! [doc = ""] # ! [doc = " # Note"] # ! [doc = ""] # ! [doc = " This API is completely unstable and subject to change."] # ! [allow (internal_features)] # ! [allow (rustc :: direct_use_of_rustc_type_ir)] # ! [doc (html_root_url = "https://doc.rust-lang.org/nightly/nightly-rustc/")] # ! [doc (rust_logo)] # ! [feature (assert_matches)] # ! [feature (extend_one)] # ! [feature (rustdoc_internals)] # ! [recursion_limit = "512"] use split_decls_genesis :: ourprelude :: * ; mod errors ; pub mod infer ; pub mod traits ; rustc_fluent_macro :: fluent_messages ! { "messages.ftl" }