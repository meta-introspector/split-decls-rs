mkuse!{use std :: fmt :: Debug ;}
mkuse!{use std :: iter ;}
mkuse!{use rustc_index :: { Idx , IndexVec } ;}
mkuse!{use rustc_middle :: arena :: ArenaAllocatable ;}
mkuse!{use rustc_middle :: bug ;}
mkuse!{use rustc_middle :: infer :: canonical :: CanonicalVarKind ;}
mkuse!{use rustc_middle :: ty :: { self , BoundVar , GenericArg , GenericArgKind , Ty , TyCtxt , TypeFoldable } ;}
mkuse!{use tracing :: { debug , instrument } ;}
mkuse!{use crate :: infer :: canonical :: instantiate :: { CanonicalExt , instantiate_value } ;}
mkuse!{use crate :: infer :: canonical :: { Canonical , CanonicalQueryResponse , CanonicalVarValues , Certainty , OriginalQueryValues , QueryRegionConstraints , QueryResponse , } ;}
mkuse!{use crate :: infer :: region_constraints :: RegionConstraintData ;}
mkuse!{use crate :: infer :: { DefineOpaqueTypes , InferCtxt , InferOk , InferResult , SubregionOrigin , TypeOutlivesConstraint , } ;}
mkuse!{use crate :: traits :: query :: NoSolution ;}
mkuse!{use crate :: traits :: { ObligationCause , PredicateObligations , ScrubbedTraitError , TraitEngine } ;}
mkitem!{mkimpl!{impl < 'tcx > InferCtxt < 'tcx > { # [doc = " This method is meant to be invoked as the final step of a canonical query"] # [doc = " implementation. It is given:"] # [doc = ""] # [doc = " - the instantiated variables `inference_vars` created from the query key"] # [doc = " - the result `answer` of the query"] # [doc = " - a fulfillment context `fulfill_cx` that may contain various obligations which"] # [doc = "   have yet to be proven."] # [doc = ""] # [doc = " Given this, the function will process the obligations pending"] # [doc = " in `fulfill_cx`:"] # [doc = ""] # [doc = " - If all the obligations can be proven successfully, it will"] # [doc = "   package up any resulting region obligations (extracted from"] # [doc = "   `infcx`) along with the fully resolved value `answer` into a"] # [doc = "   query result (which is then itself canonicalized)."] # [doc = " - If some obligations can be neither proven nor disproven, then"] # [doc = "   the same thing happens, but the resulting query is marked as ambiguous."] # [doc = " - Finally, if any of the obligations result in a hard error,"] # [doc = "   then `Err(NoSolution)` is returned."] # [instrument (skip (self , inference_vars , answer , fulfill_cx) , level = "trace")] pub fn make_canonicalized_query_response < T > (& self , inference_vars : CanonicalVarValues < 'tcx > , answer : T , fulfill_cx : & mut dyn TraitEngine < 'tcx , ScrubbedTraitError < 'tcx > > ,) -> Result < CanonicalQueryResponse < 'tcx , T > , NoSolution > where T : Debug + TypeFoldable < TyCtxt < 'tcx > > , Canonical < 'tcx , QueryResponse < 'tcx , T > > : ArenaAllocatable < 'tcx > , { let query_response = self . make_query_response (inference_vars , answer , fulfill_cx) ? ; debug ! ("query_response = {:#?}" , query_response) ; let canonical_result = self . canonicalize_response (query_response) ; debug ! ("canonical_result = {:#?}" , canonical_result) ; Ok (self . tcx . arena . alloc (canonical_result)) } # [doc = " A version of `make_canonicalized_query_response` that does"] # [doc = " not pack in obligations, for contexts that want to drop"] # [doc = " pending obligations instead of treating them as an ambiguity (e.g."] # [doc = " typeck \"probing\" contexts)."] # [doc = ""] # [doc = " If you DO want to keep track of pending obligations (which"] # [doc = " include all region obligations, so this includes all cases"] # [doc = " that care about regions) with this function, you have to"] # [doc = " do it yourself, by e.g., having them be a part of the answer."] pub fn make_query_response_ignoring_pending_obligations < T > (& self , inference_vars : CanonicalVarValues < 'tcx > , answer : T ,) -> Canonical < 'tcx , QueryResponse < 'tcx , T > > where T : Debug + TypeFoldable < TyCtxt < 'tcx > > , { self . canonicalize_response (QueryResponse { var_values : inference_vars , region_constraints : QueryRegionConstraints :: default () , certainty : Certainty :: Proven , opaque_types : vec ! [] , value : answer , }) } # [doc = " Helper for `make_canonicalized_query_response` that does"] # [doc = " everything up until the final canonicalization."] # [instrument (skip (self , fulfill_cx) , level = "debug")] fn make_query_response < T > (& self , inference_vars : CanonicalVarValues < 'tcx > , answer : T , fulfill_cx : & mut dyn TraitEngine < 'tcx , ScrubbedTraitError < 'tcx > > ,) -> Result < QueryResponse < 'tcx , T > , NoSolution > where T : Debug + TypeFoldable < TyCtxt < 'tcx > > , { let errors = fulfill_cx . select_all_or_error (self) ; if errors . iter () . any (| e | e . is_true_error ()) { return Err (NoSolution) ; } let region_obligations = self . take_registered_region_obligations () ; let region_assumptions = self . take_registered_region_assumptions () ; debug ! (? region_obligations) ; let region_constraints = self . with_region_constraints (| region_constraints | { make_query_region_constraints (region_obligations , region_constraints , region_assumptions ,) }) ; debug ! (? region_constraints) ; let certainty = if errors . is_empty () { Certainty :: Proven } else { Certainty :: Ambiguous } ; let opaque_types = self . inner . borrow_mut () . opaque_type_storage . take_opaque_types () . map (| (k , v) | (k , v . ty)) . collect () ; Ok (QueryResponse { var_values : inference_vars , region_constraints , certainty , value : answer , opaque_types , }) } # [doc = " Given the (canonicalized) result to a canonical query,"] # [doc = " instantiates the result so it can be used, plugging in the"] # [doc = " values from the canonical query. (Note that the result may"] # [doc = " have been ambiguous; you should check the certainty level of"] # [doc = " the query before applying this function.)"] # [doc = ""] # [doc = " To get a good understanding of what is happening here, check"] # [doc = " out the [chapter in the rustc dev guide][c]."] # [doc = ""] # [doc = " [c]: https://rust-lang.github.io/chalk/book/canonical_queries/canonicalization.html#processing-the-canonicalized-query-result"] pub fn instantiate_query_response_and_region_obligations < R > (& self , cause : & ObligationCause < 'tcx > , param_env : ty :: ParamEnv < 'tcx > , original_values : & OriginalQueryValues < 'tcx > , query_response : & Canonical < 'tcx , QueryResponse < 'tcx , R > > ,) -> InferResult < 'tcx , R > where R : Debug + TypeFoldable < TyCtxt < 'tcx > > , { let InferOk { value : result_args , obligations } = self . query_response_instantiation (cause , param_env , original_values , query_response) ? ; for (predicate , _category) in & query_response . value . region_constraints . outlives { let predicate = instantiate_value (self . tcx , & result_args , * predicate) ; self . register_outlives_constraint (predicate , cause) ; } for assumption in & query_response . value . region_constraints . assumptions { let assumption = instantiate_value (self . tcx , & result_args , * assumption) ; self . register_region_assumption (assumption) ; } let user_result : R = query_response . instantiate_projected (self . tcx , & result_args , | q_r | q_r . value . clone ()) ; Ok (InferOk { value : user_result , obligations }) } # [doc = " An alternative to"] # [doc = " `instantiate_query_response_and_region_obligations` that is more"] # [doc = " efficient for NLL. NLL is a bit more advanced in the"] # [doc = " \"transition to chalk\" than the rest of the compiler. During"] # [doc = " the NLL type check, all of the \"processing\" of types and"] # [doc = " things happens in queries -- the NLL checker itself is only"] # [doc = " interested in the region obligations (`'a: 'b` or `T: 'b`)"] # [doc = " that come out of these queries, which it wants to convert into"] # [doc = " MIR-based constraints and solve. Therefore, it is most"] # [doc = " convenient for the NLL Type Checker to **directly consume**"] # [doc = " the `QueryOutlivesConstraint` values that arise from doing a"] # [doc = " query. This is contrast to other parts of the compiler, which"] # [doc = " would prefer for those `QueryOutlivesConstraint` to be converted"] # [doc = " into the older infcx-style constraints (e.g., calls to"] # [doc = " `sub_regions` or `register_region_obligation`)."] # [doc = ""] # [doc = " Therefore, `instantiate_nll_query_response_and_region_obligations` performs the same"] # [doc = " basic operations as `instantiate_query_response_and_region_obligations` but"] # [doc = " it returns its result differently:"] # [doc = ""] # [doc = " - It creates an instantiation `S` that maps from the original"] # [doc = "   query variables to the values computed in the query"] # [doc = "   result. If any errors arise, they are propagated back as an"] # [doc = "   `Err` result."] # [doc = " - In the case of a successful instantiation, we will append"] # [doc = "   `QueryOutlivesConstraint` values onto the"] # [doc = "   `output_query_region_constraints` vector for the solver to"] # [doc = "   use (if an error arises, some values may also be pushed, but"] # [doc = "   they should be ignored)."] # [doc = " - It **can happen** (though it rarely does currently) that"] # [doc = "   equating types and things will give rise to subobligations"] # [doc = "   that must be processed. In this case, those subobligations"] # [doc = "   are propagated back in the return value."] # [doc = " - Finally, the query result (of type `R`) is propagated back,"] # [doc = "   after applying the instantiation `S`."] pub fn instantiate_nll_query_response_and_region_obligations < R > (& self , cause : & ObligationCause < 'tcx > , param_env : ty :: ParamEnv < 'tcx > , original_values : & OriginalQueryValues < 'tcx > , query_response : & Canonical < 'tcx , QueryResponse < 'tcx , R > > , output_query_region_constraints : & mut QueryRegionConstraints < 'tcx > ,) -> InferResult < 'tcx , R > where R : Debug + TypeFoldable < TyCtxt < 'tcx > > , { let InferOk { value : result_args , mut obligations } = self . query_response_instantiation_guess (cause , param_env , original_values , query_response ,) ? ; let constraint_category = cause . to_constraint_category () ; for (index , original_value) in original_values . var_values . iter () . enumerate () { let result_value = query_response . instantiate_projected (self . tcx , & result_args , | v | { v . var_values [BoundVar :: new (index)] }) ; match (original_value . kind () , result_value . kind ()) { (GenericArgKind :: Lifetime (re1) , GenericArgKind :: Lifetime (re2)) if re1 . is_erased () && re2 . is_erased () => { } (GenericArgKind :: Lifetime (v_o) , GenericArgKind :: Lifetime (v_r)) => { if v_o != v_r { output_query_region_constraints . outlives . push ((ty :: OutlivesPredicate (v_o . into () , v_r) , constraint_category)) ; output_query_region_constraints . outlives . push ((ty :: OutlivesPredicate (v_r . into () , v_o) , constraint_category)) ; } } (GenericArgKind :: Type (v1) , GenericArgKind :: Type (v2)) => { obligations . extend (self . at (& cause , param_env) . eq (DefineOpaqueTypes :: Yes , v1 , v2) ? . into_obligations () ,) ; } (GenericArgKind :: Const (v1) , GenericArgKind :: Const (v2)) => { obligations . extend (self . at (& cause , param_env) . eq (DefineOpaqueTypes :: Yes , v1 , v2) ? . into_obligations () ,) ; } _ => { bug ! ("kind mismatch, cannot unify {:?} and {:?}" , original_value , result_value) ; } } } output_query_region_constraints . outlives . extend (query_response . value . region_constraints . outlives . iter () . filter_map (| & r_c | { let r_c = instantiate_value (self . tcx , & result_args , r_c) ; let ty :: OutlivesPredicate (k1 , r2) = r_c . 0 ; if k1 != r2 . into () { Some (r_c) } else { None } }) ,) ; output_query_region_constraints . assumptions . extend (query_response . value . region_constraints . assumptions . iter () . map (| & r_c | instantiate_value (self . tcx , & result_args , r_c)) ,) ; let user_result : R = query_response . instantiate_projected (self . tcx , & result_args , | q_r | q_r . value . clone ()) ; Ok (InferOk { value : user_result , obligations }) } # [doc = " Given the original values and the (canonicalized) result from"] # [doc = " computing a query, returns an instantiation that can be applied"] # [doc = " to the query result to convert the result back into the"] # [doc = " original namespace."] # [doc = ""] # [doc = " The instantiation also comes accompanied with subobligations"] # [doc = " that arose from unification; these might occur if (for"] # [doc = " example) we are doing lazy normalization and the value"] # [doc = " assigned to a type variable is unified with an unnormalized"] # [doc = " projection."] fn query_response_instantiation < R > (& self , cause : & ObligationCause < 'tcx > , param_env : ty :: ParamEnv < 'tcx > , original_values : & OriginalQueryValues < 'tcx > , query_response : & Canonical < 'tcx , QueryResponse < 'tcx , R > > ,) -> InferResult < 'tcx , CanonicalVarValues < 'tcx > > where R : Debug + TypeFoldable < TyCtxt < 'tcx > > , { debug ! ("query_response_instantiation(original_values={:#?}, query_response={:#?})" , original_values , query_response ,) ; let mut value = self . query_response_instantiation_guess (cause , param_env , original_values , query_response ,) ? ; value . obligations . extend (self . unify_query_response_instantiation_guess (cause , param_env , original_values , & value . value , query_response ,) ? . into_obligations () ,) ; Ok (value) } # [doc = " Given the original values and the (canonicalized) result from"] # [doc = " computing a query, returns a **guess** at an instantiation that"] # [doc = " can be applied to the query result to convert the result back"] # [doc = " into the original namespace. This is called a **guess**"] # [doc = " because it uses a quick heuristic to find the values for each"] # [doc = " canonical variable; if that quick heuristic fails, then we"] # [doc = " will instantiate fresh inference variables for each canonical"] # [doc = " variable instead. Therefore, the result of this method must be"] # [doc = " properly unified"] # [instrument (level = "debug" , skip (self , param_env))] fn query_response_instantiation_guess < R > (& self , cause : & ObligationCause < 'tcx > , param_env : ty :: ParamEnv < 'tcx > , original_values : & OriginalQueryValues < 'tcx > , query_response : & Canonical < 'tcx , QueryResponse < 'tcx , R > > ,) -> InferResult < 'tcx , CanonicalVarValues < 'tcx > > where R : Debug + TypeFoldable < TyCtxt < 'tcx > > , { let mut universe_map = original_values . universe_map . clone () ; let num_universes_in_query = original_values . universe_map . len () ; let num_universes_in_response = query_response . max_universe . as_usize () + 1 ; for _ in num_universes_in_query .. num_universes_in_response { universe_map . push (self . create_next_universe ()) ; } assert ! (! universe_map . is_empty ()) ; assert_eq ! (universe_map [ty :: UniverseIndex :: ROOT . as_usize ()] , ty :: UniverseIndex :: ROOT) ; let result_values = & query_response . value . var_values ; assert_eq ! (original_values . var_values . len () , result_values . len ()) ; let mut opt_values : IndexVec < BoundVar , Option < GenericArg < 'tcx > > > = IndexVec :: from_elem_n (None , query_response . variables . len ()) ; for (original_value , result_value) in iter :: zip (& original_values . var_values , result_values) { match result_value . kind () { GenericArgKind :: Type (result_value) => { if let ty :: Bound (debruijn , b) = * result_value . kind () && ! matches ! (query_response . variables [b . var . as_usize ()] , CanonicalVarKind :: Ty { .. }) { assert_eq ! (debruijn , ty :: INNERMOST) ; opt_values [b . var] = Some (* original_value) ; } } GenericArgKind :: Lifetime (result_value) => { if let ty :: ReBound (debruijn , b) = result_value . kind () { assert_eq ! (debruijn , ty :: INNERMOST) ; opt_values [b . var] = Some (* original_value) ; } } GenericArgKind :: Const (result_value) => { if let ty :: ConstKind :: Bound (debruijn , b) = result_value . kind () { assert_eq ! (debruijn , ty :: INNERMOST) ; opt_values [b . var] = Some (* original_value) ; } } } } let tcx = self . tcx ; let variables = query_response . variables ; let var_values = CanonicalVarValues :: instantiate (tcx , variables , | var_values , kind | { if kind . universe () != ty :: UniverseIndex :: ROOT { self . instantiate_canonical_var (cause . span , kind , & var_values , | u | { universe_map [u . as_usize ()] }) } else if kind . is_existential () { match opt_values [BoundVar :: new (var_values . len ())] { Some (k) => k , None => self . instantiate_canonical_var (cause . span , kind , & var_values , | u | { universe_map [u . as_usize ()] }) , } } else { opt_values [BoundVar :: new (var_values . len ())] . expect ("expected placeholder to be unified with itself during response") } }) ; let mut obligations = PredicateObligations :: new () ; for & (a , b) in & query_response . value . opaque_types { let a = instantiate_value (self . tcx , & var_values , a) ; let b = instantiate_value (self . tcx , & var_values , b) ; debug ! (? a , ? b , "constrain opaque type") ; obligations . extend (self . at (cause , param_env) . eq (DefineOpaqueTypes :: Yes , Ty :: new_opaque (self . tcx , a . def_id . to_def_id () , a . args) , b ,) ? . obligations ,) ; } Ok (InferOk { value : var_values , obligations }) } # [doc = " Given a \"guess\" at the values for the canonical variables in"] # [doc = " the input, try to unify with the *actual* values found in the"] # [doc = " query result. Often, but not always, this is a no-op, because"] # [doc = " we already found the mapping in the \"guessing\" step."] # [doc = ""] # [doc = " See also: [`Self::query_response_instantiation_guess`]"] fn unify_query_response_instantiation_guess < R > (& self , cause : & ObligationCause < 'tcx > , param_env : ty :: ParamEnv < 'tcx > , original_values : & OriginalQueryValues < 'tcx > , result_args : & CanonicalVarValues < 'tcx > , query_response : & Canonical < 'tcx , QueryResponse < 'tcx , R > > ,) -> InferResult < 'tcx , () > where R : Debug + TypeFoldable < TyCtxt < 'tcx > > , { let instantiated_query_response = | index : BoundVar | -> GenericArg < 'tcx > { query_response . instantiate_projected (self . tcx , result_args , | v | v . var_values [index]) } ; self . unify_canonical_vars (cause , param_env , original_values , instantiated_query_response) } # [doc = " Given two sets of values for the same set of canonical variables, unify them."] # [doc = " The second set is produced lazily by supplying indices from the first set."] fn unify_canonical_vars (& self , cause : & ObligationCause < 'tcx > , param_env : ty :: ParamEnv < 'tcx > , variables1 : & OriginalQueryValues < 'tcx > , variables2 : impl Fn (BoundVar) -> GenericArg < 'tcx > ,) -> InferResult < 'tcx , () > { let mut obligations = PredicateObligations :: new () ; for (index , value1) in variables1 . var_values . iter () . enumerate () { let value2 = variables2 (BoundVar :: new (index)) ; match (value1 . kind () , value2 . kind ()) { (GenericArgKind :: Type (v1) , GenericArgKind :: Type (v2)) => { obligations . extend (self . at (cause , param_env) . eq (DefineOpaqueTypes :: Yes , v1 , v2) ? . into_obligations () ,) ; } (GenericArgKind :: Lifetime (re1) , GenericArgKind :: Lifetime (re2)) if re1 . is_erased () && re2 . is_erased () => { } (GenericArgKind :: Lifetime (v1) , GenericArgKind :: Lifetime (v2)) => { self . inner . borrow_mut () . unwrap_region_constraints () . make_eqregion (SubregionOrigin :: RelateRegionParamBound (cause . span , None) , v1 , v2 ,) ; } (GenericArgKind :: Const (v1) , GenericArgKind :: Const (v2)) => { let ok = self . at (cause , param_env) . eq (DefineOpaqueTypes :: Yes , v1 , v2) ? ; obligations . extend (ok . into_obligations ()) ; } _ => { bug ! ("kind mismatch, cannot unify {:?} and {:?}" , value1 , value2 ,) ; } } } Ok (InferOk { value : () , obligations }) } }}}

macro_rules! make_query_region_constraints_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function make_query_region_constraints in module {}", module_path!());
    };
}

mkfn!{
    make_query_region_constraints_introspect!();
    # [doc = " Given the region obligations and constraints scraped from the infcx,"] # [doc = " creates query region constraints."] pub fn make_query_region_constraints < 'tcx > (outlives_obligations : Vec < TypeOutlivesConstraint < 'tcx > > , region_constraints : & RegionConstraintData < 'tcx > , assumptions : Vec < ty :: ArgOutlivesPredicate < 'tcx > > ,) -> QueryRegionConstraints < 'tcx > { let RegionConstraintData { constraints , verifys } = region_constraints ; assert ! (verifys . is_empty ()) ; debug ! (? constraints) ; let outlives : Vec < _ > = constraints . iter () . map (| (c , origin) | { let constraint = ty :: OutlivesPredicate (c . sup . into () , c . sub) ; (constraint , origin . to_constraint_category ()) }) . chain (outlives_obligations . into_iter () . map (| obl | { (ty :: OutlivesPredicate (obl . sup_type . into () , obl . sub_region) , obl . origin . to_constraint_category () ,) })) . collect () ; QueryRegionConstraints { outlives , assumptions } }
}