# ! [doc = " This module contains the definitions of most `TypeRelation`s in the type system"] # ! [doc = " (except for some relations used for diagnostics and heuristics in the compiler)."] # ! [doc = " As well as the implementation of `Relate` for interned things (`Ty`/`Const`/etc)."] use split_decls_genesis :: ourprelude :: * ; pub use rustc_middle :: ty :: relate :: combine :: PredicateEmittingRelation ; pub use rustc_middle :: ty :: relate :: { RelateResult , * } ; mod generalize ; mod higher_ranked ; pub (super) mod lattice ; pub (super) mod type_relating ;