# ! [doc = " Crate containing the implementation of the next-generation trait solver."] # ! [doc = ""] # ! [doc = " This crate may also contain things that are used by the old trait solver,"] # ! [doc = " but were uplifted in the process of making the new trait solver generic."] # ! [doc = " So if you got to this crate from the old solver, it's totally normal."] # ! [allow (rustc :: direct_use_of_rustc_type_ir)] # ! [allow (rustc :: usage_of_type_ir_inherent)] # ! [allow (rustc :: usage_of_type_ir_traits)] use split_decls_genesis :: ourprelude :: * ; pub mod canonicalizer ; pub mod coherence ; pub mod delegate ; pub mod placeholder ; pub mod resolve ; pub mod solve ;