# ! [doc = " The data that we will serialize and deserialize."] # ! [doc = ""] # ! [doc = " Notionally, the dep-graph is a sequence of NodeInfo with the dependencies"] # ! [doc = " specified inline. The total number of nodes and edges are stored as the last"] # ! [doc = " 16 bytes of the file, so we can find them easily at decoding time."] # ! [doc = ""] # ! [doc = " The serialisation is performed on-demand when each node is emitted. Using this"] # ! [doc = " scheme, we do not need to keep the current graph in memory."] # ! [doc = ""] # ! [doc = " The deserialization is performed manually, in order to convert from the stored"] # ! [doc = " sequence of NodeInfos to the different arrays in SerializedDepGraph. Since the"] # ! [doc = " node and edge count are stored at the end of the file, all the arrays can be"] # ! [doc = " pre-allocated with the right length."] # ! [doc = ""] # ! [doc = " The encoding of the dep-graph is generally designed around the fact that fixed-size"] # ! [doc = " reads of encoded data are generally faster than variable-sized reads. Ergo we adopt"] # ! [doc = " essentially the same varint encoding scheme used in the rmeta format; the edge lists"] # ! [doc = " for each node on the graph store a 2-bit integer which is the number of bytes per edge"] # ! [doc = " index in that node's edge list. We effectively ignore that an edge index of 0 could be"] # ! [doc = " encoded with 0 bytes in order to not require 3 bits to store the byte width of the edges."] # ! [doc = " The overhead of calculating the correct byte width for each edge is mitigated by"] # ! [doc = " building edge lists with [`EdgesVec`] which keeps a running max of the edges in a node."] # ! [doc = ""] # ! [doc = " When we decode this data, we do not immediately create [`SerializedDepNodeIndex`] and"] # ! [doc = " instead keep the data in its denser serialized form which lets us turn our on-disk size"] # ! [doc = " efficiency directly into a peak memory reduction. When we convert these encoded-in-memory"] # ! [doc = " values into their fully-deserialized type, we use a fixed-size read of the encoded array"] # ! [doc = " then mask off any errant bytes we read. The array of edge index bytes is padded to permit this."] # ! [doc = ""] # ! [doc = " We also encode and decode the entire rest of each node using [`SerializedNodeHeader`]"] # ! [doc = " to let this encoding and decoding be done in one fixed-size operation. These headers contain"] # ! [doc = " two [`Fingerprint`]s along with the serialized [`DepKind`], and the number of edge indices"] # ! [doc = " in the node and the number of bytes used to encode the edge indices for this node. The"] # ! [doc = " [`DepKind`], number of edges, and bytes per edge are all bit-packed together, if they fit."] # ! [doc = " If the number of edges in this node does not fit in the bits available in the header, we"] # ! [doc = " store it directly after the header with leb128."] # ! [doc = ""] # ! [doc = " Dep-graph indices are bulk allocated to threads inside `LocalEncoderState`. Having threads"] # ! [doc = " own these indices helps avoid races when they are conditionally used when marking nodes green."] # ! [doc = " It also reduces congestion on the shared index count."] use split_decls_genesis :: ourprelude :: * ; use std :: cell :: RefCell ; use std :: cmp :: max ; use std :: marker :: PhantomData ; use std :: sync :: Arc ; use std :: sync :: atomic :: Ordering ; use std :: { iter , mem , u64 } ; use rustc_data_structures :: fingerprint :: { Fingerprint , PackedFingerprint } ; use rustc_data_structures :: fx :: FxHashMap ; use rustc_data_structures :: outline ; use rustc_data_structures :: profiling :: SelfProfilerRef ; use rustc_data_structures :: sync :: { AtomicU64 , Lock , WorkerLocal , broadcast } ; use rustc_data_structures :: unhash :: UnhashMap ; use rustc_index :: IndexVec ; use rustc_serialize :: opaque :: mem_encoder :: MemEncoder ; use rustc_serialize :: opaque :: { FileEncodeResult , FileEncoder , IntEncodedWithFixedSize , MemDecoder } ; use rustc_serialize :: { Decodable , Decoder , Encodable , Encoder } ; use rustc_session :: Session ; use tracing :: { debug , instrument } ; use super :: graph :: { CurrentDepGraph , DepNodeColor , DepNodeColorMap } ; use super :: query :: DepGraphQuery ; use super :: { DepKind , DepNode , DepNodeIndex , Deps } ; use crate :: dep_graph :: edges :: EdgesVec ; rustc_index :: newtype_index ! { #[encodable] #[max = 0x7FFF_FFFF] pub struct SerializedDepNodeIndex { } } const DEP_NODE_SIZE : usize = size_of :: < SerializedDepNodeIndex > () ; #[doc = " Amount of padding we need to add to the edge list data so that we can retrieve every"] #[doc = " SerializedDepNodeIndex with a fixed-size read then mask."] const DEP_NODE_PAD : usize = DEP_NODE_SIZE - 1 ; #[doc = " Number of bits we need to store the number of used bytes in a SerializedDepNodeIndex."] #[doc = " Note that wherever we encode byte widths like this we actually store the number of bytes used"] #[doc = " minus 1; for a 4-byte value we technically would have 5 widths to store, but using one byte to"] #[doc = " store zeroes (which are relatively rare) is a decent tradeoff to save a bit in our bitfields."] const DEP_NODE_WIDTH_BITS : usize = DEP_NODE_SIZE / 2 ; #[doc = " Data for use when recompiling the **current crate**."] #[doc = ""] #[doc = " There may be unused indices with DEP_KIND_NULL in this graph due to batch allocation of"] #[doc = " indices to threads."] #[derive (Debug , Default)] pub struct SerializedDepGraph { #[doc = " The set of all DepNodes in the graph"] nodes : IndexVec < SerializedDepNodeIndex , DepNode > , #[doc = " The set of all Fingerprints in the graph. Each Fingerprint corresponds to"] #[doc = " the DepNode at the same index in the nodes vector."] fingerprints : IndexVec < SerializedDepNodeIndex , Fingerprint > , #[doc = " For each DepNode, stores the list of edges originating from that"] #[doc = " DepNode. Encoded as a [start, end) pair indexing into edge_list_data,"] #[doc = " which holds the actual DepNodeIndices of the target nodes."] edge_list_indices : IndexVec < SerializedDepNodeIndex , EdgeHeader > , #[doc = " A flattened list of all edge targets in the graph, stored in the same"] #[doc = " varint encoding that we use on disk. Edge sources are implicit in edge_list_indices."] edge_list_data : Vec < u8 > , #[doc = " Stores a map from fingerprints to nodes per dep node kind."] #[doc = " This is the reciprocal of `nodes`."] index : Vec < UnhashMap < PackedFingerprint , SerializedDepNodeIndex > > , #[doc = " The number of previous compilation sessions. This is used to generate"] #[doc = " unique anon dep nodes per session."] session_count : u64 , } impl SerializedDepGraph { #[inline] pub fn edge_targets_from (& self , source : SerializedDepNodeIndex ,) -> impl Iterator < Item = SerializedDepNodeIndex > + Clone { let header = self . edge_list_indices [source] ; let mut raw = & self . edge_list_data [header . start () ..] ; let bytes_per_index = header . bytes_per_index () ; let mask = header . mask () ; (0 .. header . num_edges) . map (move | _ | { let index = & raw [.. DEP_NODE_SIZE] ; raw = & raw [bytes_per_index ..] ; let index = u32 :: from_le_bytes (index . try_into () . unwrap ()) & mask ; SerializedDepNodeIndex :: from_u32 (index) }) } #[inline] pub fn index_to_node (& self , dep_node_index : SerializedDepNodeIndex) -> DepNode { self . nodes [dep_node_index] } #[inline] pub fn node_to_index_opt (& self , dep_node : & DepNode) -> Option < SerializedDepNodeIndex > { self . index . get (dep_node . kind . as_usize ()) ? . get (& dep_node . hash) . cloned () } #[inline] pub fn fingerprint_by_index (& self , dep_node_index : SerializedDepNodeIndex) -> Fingerprint { self . fingerprints [dep_node_index] } #[inline] pub fn node_count (& self) -> usize { self . nodes . len () } #[inline] pub fn session_count (& self) -> u64 { self . session_count } } #[doc = " A packed representation of an edge's start index and byte width."] #[doc = ""] #[doc = " This is packed by stealing 2 bits from the start index, which means we only accommodate edge"] #[doc = " data arrays up to a quarter of our address space. Which seems fine."] #[derive (Debug , Clone , Copy)] struct EdgeHeader { repr : usize , num_edges : u32 , } impl EdgeHeader { #[inline] fn start (self) -> usize { self . repr >> DEP_NODE_WIDTH_BITS } #[inline] fn bytes_per_index (self) -> usize { (self . repr & mask (DEP_NODE_WIDTH_BITS)) + 1 } #[inline] fn mask (self) -> u32 { mask (self . bytes_per_index () * 8) as u32 } } #[inline] fn mask (bits : usize) -> usize { usize :: MAX >> ((size_of :: < usize > () * 8) - bits) } impl SerializedDepGraph { #[instrument (level = "debug" , skip (d , deps))] pub fn decode < D : Deps > (d : & mut MemDecoder < '_ > , deps : & D) -> Arc < SerializedDepGraph > { debug ! ("position: {:?}" , d . position ()) ; let (node_max , node_count , edge_count) = d . with_position (d . len () - 3 * IntEncodedWithFixedSize :: ENCODED_SIZE , | d | { debug ! ("position: {:?}" , d . position ()) ; let node_max = IntEncodedWithFixedSize :: decode (d) . 0 as usize ; let node_count = IntEncodedWithFixedSize :: decode (d) . 0 as usize ; let edge_count = IntEncodedWithFixedSize :: decode (d) . 0 as usize ; (node_max , node_count , edge_count) }) ; debug ! ("position: {:?}" , d . position ()) ; debug ! (? node_count , ? edge_count) ; let graph_bytes = d . len () - (3 * IntEncodedWithFixedSize :: ENCODED_SIZE) - d . position () ; let mut nodes = IndexVec :: from_elem_n (DepNode { kind : D :: DEP_KIND_NULL , hash : PackedFingerprint :: from (Fingerprint :: ZERO) } , node_max ,) ; let mut fingerprints = IndexVec :: from_elem_n (Fingerprint :: ZERO , node_max) ; let mut edge_list_indices = IndexVec :: from_elem_n (EdgeHeader { repr : 0 , num_edges : 0 } , node_max) ; let mut edge_list_data = Vec :: with_capacity (graph_bytes - node_count * size_of :: < SerializedNodeHeader < D > > ()) ; for _ in 0 .. node_count { let node_header = SerializedNodeHeader :: < D > { bytes : d . read_array () , _marker : PhantomData } ; let index = node_header . index () ; let node = & mut nodes [index] ; assert ! (node_header . node () . kind != D :: DEP_KIND_NULL && node . kind == D :: DEP_KIND_NULL) ; * node = node_header . node () ; fingerprints [index] = node_header . fingerprint () ; let num_edges = node_header . len () . unwrap_or_else (| | d . read_u32 ()) ; let edges_len_bytes = node_header . bytes_per_index () * (num_edges as usize) ; let edges_header = node_header . edges_header (& edge_list_data , num_edges) ; edge_list_data . extend (d . read_raw_bytes (edges_len_bytes)) ; edge_list_indices [index] = edges_header ; } edge_list_data . extend (& [0u8 ; DEP_NODE_PAD]) ; let mut index : Vec < _ > = (0 .. (D :: DEP_KIND_MAX + 1)) . map (| _ | UnhashMap :: with_capacity_and_hasher (d . read_u32 () as usize , Default :: default ())) . collect () ; let session_count = d . read_u64 () ; for (idx , node) in nodes . iter_enumerated () { if index [node . kind . as_usize ()] . insert (node . hash , idx) . is_some () { if node . kind != D :: DEP_KIND_NULL && node . kind != D :: DEP_KIND_SIDE_EFFECT { let name = deps . name (node . kind) ; panic ! ("Error: A dep graph node ({name}) does not have an unique index. \
                     Running a clean build on a nightly compiler with `-Z incremental-verify-ich` \
                     can help narrow down the issue for reporting. A clean build may also work around the issue.\n
                     DepNode: {node:?}") } } } Arc :: new (SerializedDepGraph { nodes , fingerprints , edge_list_indices , edge_list_data , index , session_count , }) } } #[doc = " A packed representation of all the fixed-size fields in a `NodeInfo`."] #[doc = ""] #[doc = " This stores in one byte array:"] #[doc = " * The `Fingerprint` in the `NodeInfo`"] #[doc = " * The `Fingerprint` in `DepNode` that is in this `NodeInfo`"] #[doc = " * The `DepKind`'s discriminant (a u16, but not all bits are used...)"] #[doc = " * The byte width of the encoded edges for this node"] #[doc = " * In whatever bits remain, the length of the edge list for this node, if it fits"] struct SerializedNodeHeader < D > { bytes : [u8 ; 38] , _marker : PhantomData < D > , } struct Unpacked { len : Option < u32 > , bytes_per_index : usize , kind : DepKind , index : SerializedDepNodeIndex , hash : PackedFingerprint , fingerprint : Fingerprint , } impl < D : Deps > SerializedNodeHeader < D > { const TOTAL_BITS : usize = size_of :: < DepKind > () * 8 ; const LEN_BITS : usize = Self :: TOTAL_BITS - Self :: KIND_BITS - Self :: WIDTH_BITS ; const WIDTH_BITS : usize = DEP_NODE_WIDTH_BITS ; const KIND_BITS : usize = Self :: TOTAL_BITS - D :: DEP_KIND_MAX . leading_zeros () as usize ; const MAX_INLINE_LEN : usize = (u16 :: MAX as usize >> (Self :: TOTAL_BITS - Self :: LEN_BITS)) - 1 ; #[inline] fn new (node : DepNode , index : DepNodeIndex , fingerprint : Fingerprint , edge_max_index : u32 , edge_count : usize ,) -> Self { debug_assert_eq ! (Self :: TOTAL_BITS , Self :: LEN_BITS + Self :: WIDTH_BITS + Self :: KIND_BITS) ; let mut head = node . kind . as_inner () ; let free_bytes = edge_max_index . leading_zeros () as usize / 8 ; let bytes_per_index = (DEP_NODE_SIZE - free_bytes) . saturating_sub (1) ; head |= (bytes_per_index as u16) << Self :: KIND_BITS ; if edge_count <= Self :: MAX_INLINE_LEN { head |= (edge_count as u16 + 1) << (Self :: KIND_BITS + Self :: WIDTH_BITS) ; } let hash : Fingerprint = node . hash . into () ; let mut bytes = [0u8 ; 38] ; bytes [.. 2] . copy_from_slice (& head . to_le_bytes ()) ; bytes [2 .. 6] . copy_from_slice (& index . as_u32 () . to_le_bytes ()) ; bytes [6 .. 22] . copy_from_slice (& hash . to_le_bytes ()) ; bytes [22 ..] . copy_from_slice (& fingerprint . to_le_bytes ()) ; #[cfg (debug_assertions)] { let res = Self { bytes , _marker : PhantomData } ; assert_eq ! (fingerprint , res . fingerprint ()) ; assert_eq ! (node , res . node ()) ; if let Some (len) = res . len () { assert_eq ! (edge_count , len as usize) ; } } Self { bytes , _marker : PhantomData } } #[inline] fn unpack (& self) -> Unpacked { let head = u16 :: from_le_bytes (self . bytes [.. 2] . try_into () . unwrap ()) ; let index = u32 :: from_le_bytes (self . bytes [2 .. 6] . try_into () . unwrap ()) ; let hash = self . bytes [6 .. 22] . try_into () . unwrap () ; let fingerprint = self . bytes [22 ..] . try_into () . unwrap () ; let kind = head & mask (Self :: KIND_BITS) as u16 ; let bytes_per_index = (head >> Self :: KIND_BITS) & mask (Self :: WIDTH_BITS) as u16 ; let len = (head as u32) >> (Self :: WIDTH_BITS + Self :: KIND_BITS) ; Unpacked { len : len . checked_sub (1) , bytes_per_index : bytes_per_index as usize + 1 , kind : DepKind :: new (kind) , index : SerializedDepNodeIndex :: from_u32 (index) , hash : Fingerprint :: from_le_bytes (hash) . into () , fingerprint : Fingerprint :: from_le_bytes (fingerprint) , } } #[inline] fn len (& self) -> Option < u32 > { self . unpack () . len } #[inline] fn bytes_per_index (& self) -> usize { self . unpack () . bytes_per_index } #[inline] fn index (& self) -> SerializedDepNodeIndex { self . unpack () . index } #[inline] fn fingerprint (& self) -> Fingerprint { self . unpack () . fingerprint } #[inline] fn node (& self) -> DepNode { let Unpacked { kind , hash , .. } = self . unpack () ; DepNode { kind , hash } } #[inline] fn edges_header (& self , edge_list_data : & [u8] , num_edges : u32) -> EdgeHeader { EdgeHeader { repr : (edge_list_data . len () << DEP_NODE_WIDTH_BITS) | (self . bytes_per_index () - 1) , num_edges , } } } #[derive (Debug)] struct NodeInfo { node : DepNode , fingerprint : Fingerprint , edges : EdgesVec , } impl NodeInfo { fn encode < D : Deps > (& self , e : & mut MemEncoder , index : DepNodeIndex) { let NodeInfo { node , fingerprint , ref edges } = * self ; let header = SerializedNodeHeader :: < D > :: new (node , index , fingerprint , edges . max_index () , edges . len () ,) ; e . write_array (header . bytes) ; if header . len () . is_none () { e . emit_u32 (edges . len () . try_into () . unwrap ()) ; } let bytes_per_index = header . bytes_per_index () ; for node_index in edges . iter () { e . write_with (| dest | { * dest = node_index . as_u32 () . to_le_bytes () ; bytes_per_index }) ; } } #[doc = " Encode a node that was promoted from the previous graph. It reads the edges directly from"] #[doc = " the previous dep graph and expects all edges to already have a new dep node index assigned."] #[doc = " This avoids the overhead of constructing `EdgesVec`, which would be needed to call `encode`."] #[inline] fn encode_promoted < D : Deps > (e : & mut MemEncoder , node : DepNode , index : DepNodeIndex , fingerprint : Fingerprint , prev_index : SerializedDepNodeIndex , colors : & DepNodeColorMap , previous : & SerializedDepGraph ,) -> usize { let edges = previous . edge_targets_from (prev_index) ; let edge_count = edges . size_hint () . 0 ; let edge_max = edges . clone () . map (| i | colors . current (i) . unwrap () . as_u32 ()) . max () . unwrap_or (0) ; let header = SerializedNodeHeader :: < D > :: new (node , index , fingerprint , edge_max , edge_count) ; e . write_array (header . bytes) ; if header . len () . is_none () { e . emit_u32 (edge_count . try_into () . unwrap ()) ; } let bytes_per_index = header . bytes_per_index () ; for node_index in edges { let node_index = colors . current (node_index) . unwrap () ; e . write_with (| dest | { * dest = node_index . as_u32 () . to_le_bytes () ; bytes_per_index }) ; } edge_count } } struct Stat { kind : DepKind , node_counter : u64 , edge_counter : u64 , } struct LocalEncoderState { next_node_index : u32 , remaining_node_index : u32 , encoder : MemEncoder , node_count : usize , edge_count : usize , #[doc = " Stores the number of times we've encoded each dep kind."] kind_stats : Vec < u32 > , } struct LocalEncoderResult { node_max : u32 , node_count : usize , edge_count : usize , #[doc = " Stores the number of times we've encoded each dep kind."] kind_stats : Vec < u32 > , } struct EncoderState < D : Deps > { next_node_index : AtomicU64 , previous : Arc < SerializedDepGraph > , file : Lock < Option < FileEncoder > > , local : WorkerLocal < RefCell < LocalEncoderState > > , stats : Option < Lock < FxHashMap < DepKind , Stat > > > , marker : PhantomData < D > , } impl < D : Deps > EncoderState < D > { fn new (encoder : FileEncoder , record_stats : bool , previous : Arc < SerializedDepGraph >) -> Self { Self { previous , next_node_index : AtomicU64 :: new (0) , stats : record_stats . then (| | Lock :: new (FxHashMap :: default ())) , file : Lock :: new (Some (encoder)) , local : WorkerLocal :: new (| _ | { RefCell :: new (LocalEncoderState { next_node_index : 0 , remaining_node_index : 0 , edge_count : 0 , node_count : 0 , encoder : MemEncoder :: new () , kind_stats : iter :: repeat (0) . take (D :: DEP_KIND_MAX as usize + 1) . collect () , }) }) , marker : PhantomData , } } #[inline] fn next_index (& self , local : & mut LocalEncoderState) -> DepNodeIndex { if local . remaining_node_index == 0 { const COUNT : u32 = 256 ; local . next_node_index = self . next_node_index . fetch_add (COUNT as u64 , Ordering :: Relaxed) . try_into () . unwrap () ; local . next_node_index . checked_add (COUNT) . unwrap () ; local . remaining_node_index = COUNT ; } DepNodeIndex :: from_u32 (local . next_node_index) } #[doc = " Marks the index previously returned by `next_index` as used."] #[inline] fn bump_index (& self , local : & mut LocalEncoderState) { local . remaining_node_index -= 1 ; local . next_node_index += 1 ; local . node_count += 1 ; } #[inline] fn record (& self , node : DepNode , index : DepNodeIndex , edge_count : usize , edges : impl FnOnce (& Self) -> Vec < DepNodeIndex > , record_graph : & Option < Lock < DepGraphQuery > > , local : & mut LocalEncoderState ,) { local . kind_stats [node . kind . as_usize ()] += 1 ; local . edge_count += edge_count ; if let Some (record_graph) = & record_graph { let edges = edges (self) ; outline (move | | { if let Some (record_graph) = & mut record_graph . try_lock () { record_graph . push (index , node , & edges) ; } }) ; } if let Some (stats) = & self . stats { let kind = node . kind ; outline (move | | { let mut stats = stats . lock () ; let stat = stats . entry (kind) . or_insert (Stat { kind , node_counter : 0 , edge_counter : 0 }) ; stat . node_counter += 1 ; stat . edge_counter += edge_count as u64 ; }) ; } } #[inline] fn flush_mem_encoder (& self , local : & mut LocalEncoderState) { let data = & mut local . encoder . data ; if data . len () > 64 * 1024 { self . file . lock () . as_mut () . unwrap () . emit_raw_bytes (& data [..]) ; data . clear () ; } } #[doc = " Encodes a node to the current graph."] fn encode_node (& self , index : DepNodeIndex , node : & NodeInfo , record_graph : & Option < Lock < DepGraphQuery > > , local : & mut LocalEncoderState ,) { node . encode :: < D > (& mut local . encoder , index) ; self . flush_mem_encoder (& mut * local) ; self . record (node . node , index , node . edges . len () , | _ | node . edges [..] . to_vec () , record_graph , & mut * local ,) ; } #[doc = " Encodes a node that was promoted from the previous graph. It reads the information directly from"] #[doc = " the previous dep graph for performance reasons."] #[doc = ""] #[doc = " This differs from `encode_node` where you have to explicitly provide the relevant `NodeInfo`."] #[doc = ""] #[doc = " It expects all edges to already have a new dep node index assigned."] #[inline] fn encode_promoted_node (& self , index : DepNodeIndex , prev_index : SerializedDepNodeIndex , record_graph : & Option < Lock < DepGraphQuery > > , colors : & DepNodeColorMap , local : & mut LocalEncoderState ,) { let node = self . previous . index_to_node (prev_index) ; let fingerprint = self . previous . fingerprint_by_index (prev_index) ; let edge_count = NodeInfo :: encode_promoted :: < D > (& mut local . encoder , node , index , fingerprint , prev_index , colors , & self . previous ,) ; self . flush_mem_encoder (& mut * local) ; self . record (node , index , edge_count , | this | { this . previous . edge_targets_from (prev_index) . map (| i | colors . current (i) . unwrap ()) . collect () } , record_graph , & mut * local ,) ; } fn finish (& self , profiler : & SelfProfilerRef , current : & CurrentDepGraph < D >) -> FileEncodeResult { self . next_node_index . store (u32 :: MAX as u64 + 1 , Ordering :: SeqCst) ; let results = broadcast (| _ | { let mut local = self . local . borrow_mut () ; local . remaining_node_index = 0 ; let data = mem :: replace (& mut local . encoder . data , Vec :: new ()) ; self . file . lock () . as_mut () . unwrap () . emit_raw_bytes (& data) ; LocalEncoderResult { kind_stats : local . kind_stats . clone () , node_max : local . next_node_index , node_count : local . node_count , edge_count : local . edge_count , } }) ; let mut encoder = self . file . lock () . take () . unwrap () ; let mut kind_stats : Vec < u32 > = iter :: repeat (0) . take (D :: DEP_KIND_MAX as usize + 1) . collect () ; let mut node_max = 0 ; let mut node_count = 0 ; let mut edge_count = 0 ; for result in results { node_max = max (node_max , result . node_max) ; node_count += result . node_count ; edge_count += result . edge_count ; for (i , stat) in result . kind_stats . iter () . enumerate () { kind_stats [i] += stat ; } } for count in kind_stats . iter () { count . encode (& mut encoder) ; } self . previous . session_count . checked_add (1) . unwrap () . encode (& mut encoder) ; debug ! (? node_max , ? node_count , ? edge_count) ; debug ! ("position: {:?}" , encoder . position ()) ; IntEncodedWithFixedSize (node_max . try_into () . unwrap ()) . encode (& mut encoder) ; IntEncodedWithFixedSize (node_count . try_into () . unwrap ()) . encode (& mut encoder) ; IntEncodedWithFixedSize (edge_count . try_into () . unwrap ()) . encode (& mut encoder) ; debug ! ("position: {:?}" , encoder . position ()) ; let result = encoder . finish () ; if let Ok (position) = result { profiler . artifact_size ("dep_graph" , "dep-graph.bin" , position as u64) ; } self . print_incremental_info (current , node_count , edge_count) ; result } fn print_incremental_info (& self , current : & CurrentDepGraph < D > , total_node_count : usize , total_edge_count : usize ,) { if let Some (record_stats) = & self . stats { let record_stats = record_stats . lock () ; #[allow (rustc :: potential_query_instability)] let mut stats : Vec < _ > = record_stats . values () . collect () ; stats . sort_by_key (| s | - (s . node_counter as i64)) ; const SEPARATOR : & str = "[incremental] --------------------------------\
                                     ----------------------------------------------\
                                     ------------" ; eprintln ! ("[incremental]") ; eprintln ! ("[incremental] DepGraph Statistics") ; eprintln ! ("{SEPARATOR}") ; eprintln ! ("[incremental]") ; eprintln ! ("[incremental] Total Node Count: {}" , total_node_count) ; eprintln ! ("[incremental] Total Edge Count: {}" , total_edge_count) ; if cfg ! (debug_assertions) { let total_read_count = current . total_read_count . load (Ordering :: Relaxed) ; let total_duplicate_read_count = current . total_duplicate_read_count . load (Ordering :: Relaxed) ; eprintln ! ("[incremental] Total Edge Reads: {total_read_count}") ; eprintln ! ("[incremental] Total Duplicate Edge Reads: {total_duplicate_read_count}") ; } eprintln ! ("[incremental]") ; eprintln ! ("[incremental]  {:<36}| {:<17}| {:<12}| {:<17}|" , "Node Kind" , "Node Frequency" , "Node Count" , "Avg. Edge Count") ; eprintln ! ("{SEPARATOR}") ; for stat in stats { let node_kind_ratio = (100.0 * (stat . node_counter as f64)) / (total_node_count as f64) ; let node_kind_avg_edges = (stat . edge_counter as f64) / (stat . node_counter as f64) ; eprintln ! ("[incremental]  {:<36}|{:>16.1}% |{:>12} |{:>17.1} |" , format ! ("{:?}" , stat . kind) , node_kind_ratio , stat . node_counter , node_kind_avg_edges ,) ; } eprintln ! ("{SEPARATOR}") ; eprintln ! ("[incremental]") ; } } } pub (crate) struct GraphEncoder < D : Deps > { profiler : SelfProfilerRef , status : EncoderState < D > , record_graph : Option < Lock < DepGraphQuery > > , } impl < D : Deps > GraphEncoder < D > { pub (crate) fn new (sess : & Session , encoder : FileEncoder , prev_node_count : usize , previous : Arc < SerializedDepGraph > ,) -> Self { let record_graph = sess . opts . unstable_opts . query_dep_graph . then (| | Lock :: new (DepGraphQuery :: new (prev_node_count))) ; let status = EncoderState :: new (encoder , sess . opts . unstable_opts . incremental_info , previous) ; GraphEncoder { status , record_graph , profiler : sess . prof . clone () } } pub (crate) fn with_query (& self , f : impl Fn (& DepGraphQuery)) { if let Some (record_graph) = & self . record_graph { f (& record_graph . lock ()) } } #[doc = " Encodes a node that does not exists in the previous graph."] pub (crate) fn send_new (& self , node : DepNode , fingerprint : Fingerprint , edges : EdgesVec ,) -> DepNodeIndex { let _prof_timer = self . profiler . generic_activity ("incr_comp_encode_dep_graph") ; let node = NodeInfo { node , fingerprint , edges } ; let mut local = self . status . local . borrow_mut () ; let index = self . status . next_index (& mut * local) ; self . status . bump_index (& mut * local) ; self . status . encode_node (index , & node , & self . record_graph , & mut * local) ; index } #[doc = " Encodes a node that exists in the previous graph, but was re-executed."] #[doc = ""] #[doc = " This will also ensure the dep node is colored either red or green."] pub (crate) fn send_and_color (& self , prev_index : SerializedDepNodeIndex , colors : & DepNodeColorMap , node : DepNode , fingerprint : Fingerprint , edges : EdgesVec , is_green : bool ,) -> DepNodeIndex { let _prof_timer = self . profiler . generic_activity ("incr_comp_encode_dep_graph") ; let node = NodeInfo { node , fingerprint , edges } ; let mut local = self . status . local . borrow_mut () ; let index = self . status . next_index (& mut * local) ; if is_green { match colors . try_mark_green (prev_index , index) { Ok (()) => () , Err (dep_node_index) => return dep_node_index , } } else { colors . insert (prev_index , DepNodeColor :: Red) ; } self . status . bump_index (& mut * local) ; self . status . encode_node (index , & node , & self . record_graph , & mut * local) ; index } #[doc = " Encodes a node that was promoted from the previous graph. It reads the information directly from"] #[doc = " the previous dep graph and expects all edges to already have a new dep node index assigned."] #[doc = ""] #[doc = " This will also ensure the dep node is marked green."] #[inline] pub (crate) fn send_promoted (& self , prev_index : SerializedDepNodeIndex , colors : & DepNodeColorMap ,) -> DepNodeIndex { let _prof_timer = self . profiler . generic_activity ("incr_comp_encode_dep_graph") ; let mut local = self . status . local . borrow_mut () ; let index = self . status . next_index (& mut * local) ; match colors . try_mark_green (prev_index , index) { Ok (()) => { self . status . bump_index (& mut * local) ; self . status . encode_promoted_node (index , prev_index , & self . record_graph , colors , & mut * local ,) ; index } Err (dep_node_index) => dep_node_index , } } pub (crate) fn finish (& self , current : & CurrentDepGraph < D >) -> FileEncodeResult { let _prof_timer = self . profiler . generic_activity ("incr_comp_encode_dep_graph_finish") ; self . status . finish (& self . profiler , current) } }