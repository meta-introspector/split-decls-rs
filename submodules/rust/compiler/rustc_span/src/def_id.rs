use split_decls_genesis :: ourprelude :: * ; use std :: fmt ; use std :: hash :: { BuildHasherDefault , Hash , Hasher } ; use rustc_data_structures :: AtomicRef ; use rustc_data_structures :: fingerprint :: Fingerprint ; use rustc_data_structures :: stable_hasher :: { HashStable , StableHasher , StableOrd , ToStableHashKey } ; use rustc_data_structures :: unhash :: Unhasher ; use rustc_hashes :: Hash64 ; use rustc_index :: Idx ; use rustc_macros :: { Decodable , Encodable , HashStable_Generic } ; use rustc_serialize :: { Decodable , Encodable } ; use crate :: { HashStableContext , SpanDecoder , SpanEncoder , Symbol } ; pub type StableCrateIdMap = indexmap :: IndexMap < StableCrateId , CrateNum , BuildHasherDefault < Unhasher > > ; rustc_index :: newtype_index ! { #[orderable] #[debug_format = "crate{}"] pub struct CrateNum { } } #[doc = " Item definitions in the currently-compiled crate would have the `CrateNum`"] #[doc = " `LOCAL_CRATE` in their `DefId`."] pub const LOCAL_CRATE : CrateNum = CrateNum :: ZERO ; impl CrateNum { #[inline] pub fn new (x : usize) -> CrateNum { CrateNum :: from_usize (x) } #[inline] pub fn as_def_id (self) -> DefId { DefId { krate : self , index : CRATE_DEF_INDEX } } #[inline] pub fn as_mod_def_id (self) -> ModDefId { ModDefId :: new_unchecked (DefId { krate : self , index : CRATE_DEF_INDEX }) } } impl fmt :: Display for CrateNum { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { fmt :: Display :: fmt (& self . as_u32 () , f) } } #[doc = " A `DefPathHash` is a fixed-size representation of a `DefPath` that is"] #[doc = " stable across crate and compilation session boundaries. It consists of two"] #[doc = " separate 64-bit hashes. The first uniquely identifies the crate this"] #[doc = " `DefPathHash` originates from (see [StableCrateId]), and the second"] #[doc = " uniquely identifies the corresponding `DefPath` within that crate. Together"] #[doc = " they form a unique identifier within an entire crate graph."] #[doc = ""] #[doc = " There is a very small chance of hash collisions, which would mean that two"] #[doc = " different `DefPath`s map to the same `DefPathHash`. Proceeding compilation"] #[doc = " with such a hash collision would very probably lead to an ICE, and in the"] #[doc = " worst case lead to a silent mis-compilation. The compiler therefore actively"] #[doc = " and exhaustively checks for such hash collisions and aborts compilation if"] #[doc = " it finds one."] #[doc = ""] #[doc = " `DefPathHash` uses 64-bit hashes for both the crate-id part and the"] #[doc = " crate-internal part, even though it is likely that there are many more"] #[doc = " `LocalDefId`s in a single crate than there are individual crates in a crate"] #[doc = " graph. Since we use the same number of bits in both cases, the collision"] #[doc = " probability for the crate-local part will be quite a bit higher (though"] #[doc = " still very small)."] #[doc = ""] #[doc = " This imbalance is not by accident: A hash collision in the"] #[doc = " crate-local part of a `DefPathHash` will be detected and reported while"] #[doc = " compiling the crate in question. Such a collision does not depend on"] #[doc = " outside factors and can be easily fixed by the crate maintainer (e.g. by"] #[doc = " renaming the item in question or by bumping the crate version in a harmless"] #[doc = " way)."] #[doc = ""] #[doc = " A collision between crate-id hashes on the other hand is harder to fix"] #[doc = " because it depends on the set of crates in the entire crate graph of a"] #[doc = " compilation session. Again, using the same crate with a different version"] #[doc = " number would fix the issue with a high probability -- but that might be"] #[doc = " easier said then done if the crates in questions are dependencies of"] #[doc = " third-party crates."] #[doc = ""] #[doc = " That being said, given a high quality hash function, the collision"] #[doc = " probabilities in question are very small. For example, for a big crate like"] #[doc = " `rustc_middle` (with ~50000 `LocalDefId`s as of the time of writing) there"] #[doc = " is a probability of roughly 1 in 14,750,000,000 of a crate-internal"] #[doc = " collision occurring. For a big crate graph with 1000 crates in it, there is"] #[doc = " a probability of 1 in 36,890,000,000,000 of a `StableCrateId` collision."] #[derive (Copy , Clone , Hash , PartialEq , Eq , PartialOrd , Ord , Debug)] #[derive (HashStable_Generic , Encodable , Decodable)] pub struct DefPathHash (pub Fingerprint) ; impl DefPathHash { #[doc = " Returns the [StableCrateId] identifying the crate this [DefPathHash]"] #[doc = " originates from."] #[inline] pub fn stable_crate_id (& self) -> StableCrateId { StableCrateId (self . 0 . split () . 0) } #[doc = " Returns the crate-local part of the [DefPathHash]."] #[inline] pub fn local_hash (& self) -> Hash64 { self . 0 . split () . 1 } #[doc = " Builds a new [DefPathHash] with the given [StableCrateId] and"] #[doc = " `local_hash`, where `local_hash` must be unique within its crate."] #[inline] pub fn new (stable_crate_id : StableCrateId , local_hash : Hash64) -> DefPathHash { DefPathHash (Fingerprint :: new (stable_crate_id . 0 , local_hash)) } } impl Default for DefPathHash { fn default () -> Self { DefPathHash (Fingerprint :: ZERO) } } impl StableOrd for DefPathHash { const CAN_USE_UNSTABLE_SORT : bool = true ; const THIS_IMPLEMENTATION_HAS_BEEN_TRIPLE_CHECKED : () = () ; } #[doc = " A [`StableCrateId`] is a 64-bit hash of a crate name, together with all"] #[doc = " `-Cmetadata` arguments, and some other data. It is to [`CrateNum`] what [`DefPathHash`] is to"] #[doc = " [`DefId`]. It is stable across compilation sessions."] #[doc = ""] #[doc = " Since the ID is a hash value, there is a small chance that two crates"] #[doc = " end up with the same [`StableCrateId`]. The compiler will check for such"] #[doc = " collisions when loading crates and abort compilation in order to avoid"] #[doc = " further trouble."] #[doc = ""] #[doc = " For more information on the possibility of hash collisions in rustc,"] #[doc = " see the discussion in [`DefId`]."] #[derive (Copy , Clone , PartialEq , Eq , PartialOrd , Ord , Debug)] #[derive (Hash , HashStable_Generic , Encodable , Decodable)] pub struct StableCrateId (pub (crate) Hash64) ; impl StableCrateId { #[doc = " Computes the stable ID for a crate with the given name and"] #[doc = " `-Cmetadata` arguments."] pub fn new (crate_name : Symbol , is_exe : bool , mut metadata : Vec < String > , cfg_version : & 'static str ,) -> StableCrateId { let mut hasher = StableHasher :: new () ; crate_name . as_str () . hash (& mut hasher) ; metadata . sort () ; metadata . dedup () ; hasher . write (b"metadata") ; for s in & metadata { hasher . write_usize (s . len ()) ; hasher . write (s . as_bytes ()) ; } hasher . write (if is_exe { b"exe" } else { b"lib" }) ; if let Some (val) = std :: env :: var_os ("RUSTC_FORCE_RUSTC_VERSION") { hasher . write (val . to_string_lossy () . into_owned () . as_bytes ()) } else { hasher . write (cfg_version . as_bytes ()) } StableCrateId (hasher . finish ()) } #[inline] pub fn as_u64 (self) -> u64 { self . 0 . as_u64 () } } impl fmt :: LowerHex for StableCrateId { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { fmt :: LowerHex :: fmt (& self . 0 , f) } } rustc_index :: newtype_index ! { #[doc = " A DefIndex is an index into the hir-map for a crate, identifying a"] #[doc = " particular definition. It should really be considered an interned"] #[doc = " shorthand for a particular DefPath."] #[orderable] #[debug_format = "DefIndex({})"] pub struct DefIndex { #[doc = " The crate root is always assigned index 0 by the AST Map code,"] #[doc = " thanks to `NodeCollector::new`."] const CRATE_DEF_INDEX = 0 ; } } #[doc = " A `DefId` identifies a particular *definition*, by combining a crate"] #[doc = " index and a def index."] #[doc = ""] #[doc = " You can create a `DefId` from a `LocalDefId` using `local_def_id.to_def_id()`."] #[derive (Clone , PartialEq , Eq , Copy)] #[cfg_attr (not (target_pointer_width = "64") , derive (Hash))] #[repr (C)] #[rustc_pass_by_value] pub struct DefId { #[cfg (not (all (target_pointer_width = "64" , target_endian = "big")))] pub index : DefIndex , pub krate : CrateNum , #[cfg (all (target_pointer_width = "64" , target_endian = "big"))] pub index : DefIndex , } impl ! Ord for DefId { } impl ! PartialOrd for DefId { } #[cfg (target_pointer_width = "64")] impl Hash for DefId { fn hash < H : Hasher > (& self , h : & mut H) { (((self . krate . as_u32 () as u64) << 32) | (self . index . as_u32 () as u64)) . hash (h) } } impl DefId { #[doc = " Makes a local `DefId` from the given `DefIndex`."] #[inline] pub fn local (index : DefIndex) -> DefId { DefId { krate : LOCAL_CRATE , index } } #[doc = " Returns whether the item is defined in the crate currently being compiled."] #[inline] pub fn is_local (self) -> bool { self . krate == LOCAL_CRATE } #[inline] pub fn as_local (self) -> Option < LocalDefId > { self . is_local () . then (| | LocalDefId { local_def_index : self . index }) } #[inline] #[track_caller] pub fn expect_local (self) -> LocalDefId { match self . as_local () { Some (local_def_id) => local_def_id , None => panic ! ("DefId::expect_local: `{self:?}` isn't local") , } } #[inline] pub fn is_crate_root (self) -> bool { self . index == CRATE_DEF_INDEX } #[inline] pub fn as_crate_root (self) -> Option < CrateNum > { self . is_crate_root () . then_some (self . krate) } #[inline] pub fn is_top_level_module (self) -> bool { self . is_local () && self . is_crate_root () } } impl From < LocalDefId > for DefId { fn from (local : LocalDefId) -> DefId { local . to_def_id () } } pub fn default_def_id_debug (def_id : DefId , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { f . debug_struct ("DefId") . field ("krate" , & def_id . krate) . field ("index" , & def_id . index) . finish () } pub static DEF_ID_DEBUG : AtomicRef < fn (DefId , & mut fmt :: Formatter < '_ >) -> fmt :: Result > = AtomicRef :: new (& (default_def_id_debug as fn (_ , & mut fmt :: Formatter < '_ >) -> _)) ; impl fmt :: Debug for DefId { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { (* DEF_ID_DEBUG) (* self , f) } } rustc_data_structures :: define_id_collections ! (DefIdMap , DefIdSet , DefIdMapEntry , DefId) ; #[doc = " A `LocalDefId` is equivalent to a `DefId` with `krate == LOCAL_CRATE`. Since"] #[doc = " we encode this information in the type, we can ensure at compile time that"] #[doc = " no `DefId`s from upstream crates get thrown into the mix. There are quite a"] #[doc = " few cases where we know that only `DefId`s from the local crate are expected;"] #[doc = " a `DefId` from a different crate would signify a bug somewhere. This"] #[doc = " is when `LocalDefId` comes in handy."] #[derive (Clone , Copy , PartialEq , Eq , Hash)] pub struct LocalDefId { pub local_def_index : DefIndex , } impl ! Ord for LocalDefId { } impl ! PartialOrd for LocalDefId { } pub const CRATE_DEF_ID : LocalDefId = LocalDefId { local_def_index : CRATE_DEF_INDEX } ; impl Idx for LocalDefId { #[inline] fn new (idx : usize) -> Self { LocalDefId { local_def_index : Idx :: new (idx) } } #[inline] fn index (self) -> usize { self . local_def_index . index () } } impl LocalDefId { #[inline] pub fn to_def_id (self) -> DefId { DefId { krate : LOCAL_CRATE , index : self . local_def_index } } #[inline] pub fn is_top_level_module (self) -> bool { self == CRATE_DEF_ID } } impl fmt :: Debug for LocalDefId { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { self . to_def_id () . fmt (f) } } impl < E : SpanEncoder > Encodable < E > for LocalDefId { fn encode (& self , s : & mut E) { self . to_def_id () . encode (s) ; } } impl < D : SpanDecoder > Decodable < D > for LocalDefId { fn decode (d : & mut D) -> LocalDefId { DefId :: decode (d) . expect_local () } } rustc_data_structures :: define_id_collections ! (LocalDefIdMap , LocalDefIdSet , LocalDefIdMapEntry , LocalDefId) ; impl < CTX : HashStableContext > HashStable < CTX > for DefId { #[inline] fn hash_stable (& self , hcx : & mut CTX , hasher : & mut StableHasher) { hcx . def_path_hash (* self) . hash_stable (hcx , hasher) ; } } impl < CTX : HashStableContext > HashStable < CTX > for LocalDefId { #[inline] fn hash_stable (& self , hcx : & mut CTX , hasher : & mut StableHasher) { hcx . def_path_hash (self . to_def_id ()) . local_hash () . hash_stable (hcx , hasher) ; } } impl < CTX : HashStableContext > HashStable < CTX > for CrateNum { #[inline] fn hash_stable (& self , hcx : & mut CTX , hasher : & mut StableHasher) { self . as_def_id () . to_stable_hash_key (hcx) . stable_crate_id () . hash_stable (hcx , hasher) ; } } impl < CTX : HashStableContext > ToStableHashKey < CTX > for DefId { type KeyType = DefPathHash ; #[inline] fn to_stable_hash_key (& self , hcx : & CTX) -> DefPathHash { hcx . def_path_hash (* self) } } impl < CTX : HashStableContext > ToStableHashKey < CTX > for LocalDefId { type KeyType = DefPathHash ; #[inline] fn to_stable_hash_key (& self , hcx : & CTX) -> DefPathHash { hcx . def_path_hash (self . to_def_id ()) } } impl < CTX : HashStableContext > ToStableHashKey < CTX > for CrateNum { type KeyType = DefPathHash ; #[inline] fn to_stable_hash_key (& self , hcx : & CTX) -> DefPathHash { self . as_def_id () . to_stable_hash_key (hcx) } } impl < CTX : HashStableContext > ToStableHashKey < CTX > for DefPathHash { type KeyType = DefPathHash ; #[inline] fn to_stable_hash_key (& self , _ : & CTX) -> DefPathHash { * self } } macro_rules ! typed_def_id { ($ Name : ident , $ LocalName : ident) => { #[derive (Debug , Clone , Copy , PartialEq , Eq , Hash , Encodable , Decodable , HashStable_Generic)] pub struct $ Name (DefId) ; impl $ Name { #[inline] pub const fn new_unchecked (def_id : DefId) -> Self { Self (def_id) } #[inline] pub fn to_def_id (self) -> DefId { self . into () } #[inline] pub fn is_local (self) -> bool { self . 0 . is_local () } #[inline] pub fn as_local (self) -> Option <$ LocalName > { self . 0 . as_local () . map ($ LocalName :: new_unchecked) } } impl From <$ LocalName > for $ Name { #[inline] fn from (local : $ LocalName) -> Self { Self (local . 0 . to_def_id ()) } } impl From <$ Name > for DefId { #[inline] fn from (typed : $ Name) -> Self { typed . 0 } } #[derive (Debug , Clone , Copy , PartialEq , Eq , Hash , Encodable , Decodable , HashStable_Generic)] pub struct $ LocalName (LocalDefId) ; impl ! Ord for $ LocalName { } impl ! PartialOrd for $ LocalName { } impl $ LocalName { #[inline] pub const fn new_unchecked (def_id : LocalDefId) -> Self { Self (def_id) } #[inline] pub fn to_def_id (self) -> DefId { self . 0 . into () } #[inline] pub fn to_local_def_id (self) -> LocalDefId { self . 0 } } impl From <$ LocalName > for LocalDefId { #[inline] fn from (typed : $ LocalName) -> Self { typed . 0 } } impl From <$ LocalName > for DefId { #[inline] fn from (typed : $ LocalName) -> Self { typed . 0 . into () } } } ; } typed_def_id ! { ModDefId , LocalModDefId } impl LocalModDefId { pub const CRATE_DEF_ID : Self = Self :: new_unchecked (CRATE_DEF_ID) ; } impl ModDefId { pub fn is_top_level_module (self) -> bool { self . 0 . is_top_level_module () } } impl LocalModDefId { pub fn is_top_level_module (self) -> bool { self . 0 . is_top_level_module () } }