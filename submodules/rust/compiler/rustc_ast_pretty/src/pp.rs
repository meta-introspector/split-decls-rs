mkmod!{convenience, { 
                getname!(convenience);
                getsrc!(convenience);
                getpath!(convenience);
                get_deps!(convenience);
                get_crates!(convenience);
                mkinclude!(convenience);
                 
            }}
mkmod!{ring, { 
                getname!(ring);
                getsrc!(ring);
                getpath!(ring);
                get_deps!(ring);
                get_crates!(ring);
                mkinclude!(ring);
                 
            }}
mkuse!{use std :: borrow :: Cow ;}
mkuse!{use std :: collections :: VecDeque ;}
mkuse!{use std :: { cmp , iter } ;}
mkuse!{use ring :: RingBuffer ;}
mkitem!{mkenum!{# [doc = " How to break. Described in more detail in the module docs."] # [derive (Clone , Copy , PartialEq)] pub enum Breaks { Consistent , Inconsistent , }}}
mkitem!{mkenum!{# [derive (Clone , Copy , PartialEq)] enum IndentStyle { # [doc = " Vertically aligned under whatever column this block begins at."] # [doc = ""] # [doc = "     fn demo(arg1: usize,"] # [doc = "             arg2: usize) {}"] Visual , # [doc = " Indented relative to the indentation level of the previous line."] # [doc = ""] # [doc = "     fn demo("] # [doc = "         arg1: usize,"] # [doc = "         arg2: usize,"] # [doc = "     ) {}"] Block { offset : isize } , }}}
mkitem!{mkstruct!{# [derive (Clone , Copy , Default , PartialEq)] pub (crate) struct BreakToken { offset : isize , blank_space : isize , pre_break : Option < char > , }}}
mkitem!{mkstruct!{# [derive (Clone , Copy , PartialEq)] pub (crate) struct BeginToken { indent : IndentStyle , breaks : Breaks , }}}
mkitem!{mkenum!{# [derive (PartialEq)] pub (crate) enum Token { String (Cow < 'static , str >) , Break (BreakToken) , Begin (BeginToken) , End , }}}
mkitem!{mkenum!{# [derive (Copy , Clone)] enum PrintFrame { Fits , Broken { indent : usize , breaks : Breaks } , }}}
mkitem!{const SIZE_INFINITY : isize = 0xffff ;}
mkitem!{# [doc = " Target line width."] const MARGIN : isize = 78 ;}
mkitem!{# [doc = " Every line is allowed at least this much space, even if highly indented."] const MIN_SPACE : isize = 60 ;}
mkitem!{mkstruct!{pub struct Printer { out : String , # [doc = " Number of spaces left on line"] space : isize , # [doc = " Ring-buffer of tokens and calculated sizes"] buf : RingBuffer < BufEntry > , # [doc = " Running size of stream \"...left\""] left_total : isize , # [doc = " Running size of stream \"...right\""] right_total : isize , # [doc = " Pseudo-stack, really a ring too. Holds the"] # [doc = " primary-ring-buffers index of the Begin that started the"] # [doc = " current block, possibly with the most recent Break after that"] # [doc = " Begin (if there is any) on top of it. Stuff is flushed off the"] # [doc = " bottom as it becomes irrelevant due to the primary ring-buffer"] # [doc = " advancing."] scan_stack : VecDeque < usize > , # [doc = " Stack of blocks-in-progress being flushed by print"] print_stack : Vec < PrintFrame > , # [doc = " Level of indentation of current line"] indent : usize , # [doc = " Buffered indentation to avoid writing trailing whitespace"] pending_indentation : isize , # [doc = " The token most recently popped from the left boundary of the"] # [doc = " ring-buffer for printing"] last_printed : Option < Token > , }}}
mkitem!{mkstruct!{struct BufEntry { token : Token , size : isize , }}}
mkitem!{mkstruct!{# [must_use] pub struct BoxMarker ;}}
mkitem!{mkimpl!{impl ! Clone for BoxMarker { }}}
mkitem!{mkimpl!{impl ! Copy for BoxMarker { }}}
mkitem!{mkimpl!{impl Drop for BoxMarker { fn drop (& mut self) { panic ! ("BoxMarker not ended with `Printer::end()`") ; } }}}
mkitem!{mkimpl!{impl Printer { pub fn new () -> Self { Printer { out : String :: new () , space : MARGIN , buf : RingBuffer :: new () , left_total : 0 , right_total : 0 , scan_stack : VecDeque :: new () , print_stack : Vec :: new () , indent : 0 , pending_indentation : 0 , last_printed : None , } } pub (crate) fn last_token (& self) -> Option < & Token > { self . last_token_still_buffered () . or_else (| | self . last_printed . as_ref ()) } pub (crate) fn last_token_still_buffered (& self) -> Option < & Token > { self . buf . last () . map (| last | & last . token) } # [doc = " Be very careful with this!"] pub (crate) fn replace_last_token_still_buffered (& mut self , token : Token) { self . buf . last_mut () . unwrap () . token = token ; } fn scan_eof (& mut self) { if ! self . scan_stack . is_empty () { self . check_stack (0) ; self . advance_left () ; } } fn scan_begin (& mut self , token : BeginToken) -> BoxMarker { if self . scan_stack . is_empty () { self . left_total = 1 ; self . right_total = 1 ; self . buf . clear () ; } let right = self . buf . push (BufEntry { token : Token :: Begin (token) , size : - self . right_total }) ; self . scan_stack . push_back (right) ; BoxMarker } fn scan_end (& mut self , b : BoxMarker) { if self . scan_stack . is_empty () { self . print_end () ; } else { let right = self . buf . push (BufEntry { token : Token :: End , size : - 1 }) ; self . scan_stack . push_back (right) ; } std :: mem :: forget (b) } fn scan_break (& mut self , token : BreakToken) { if self . scan_stack . is_empty () { self . left_total = 1 ; self . right_total = 1 ; self . buf . clear () ; } else { self . check_stack (0) ; } let right = self . buf . push (BufEntry { token : Token :: Break (token) , size : - self . right_total }) ; self . scan_stack . push_back (right) ; self . right_total += token . blank_space ; } fn scan_string (& mut self , string : Cow < 'static , str >) { if self . scan_stack . is_empty () { self . print_string (& string) ; } else { let len = string . len () as isize ; self . buf . push (BufEntry { token : Token :: String (string) , size : len }) ; self . right_total += len ; self . check_stream () ; } } pub (crate) fn offset (& mut self , offset : isize) { if let Some (BufEntry { token : Token :: Break (token) , .. }) = & mut self . buf . last_mut () { token . offset += offset ; } } fn check_stream (& mut self) { while self . right_total - self . left_total > self . space { if * self . scan_stack . front () . unwrap () == self . buf . index_of_first () { self . scan_stack . pop_front () . unwrap () ; self . buf . first_mut () . unwrap () . size = SIZE_INFINITY ; } self . advance_left () ; if self . buf . is_empty () { break ; } } } fn advance_left (& mut self) { while self . buf . first () . unwrap () . size >= 0 { let left = self . buf . pop_first () . unwrap () ; match & left . token { Token :: String (string) => { self . left_total += string . len () as isize ; self . print_string (string) ; } Token :: Break (token) => { self . left_total += token . blank_space ; self . print_break (* token , left . size) ; } Token :: Begin (token) => self . print_begin (* token , left . size) , Token :: End => self . print_end () , } self . last_printed = Some (left . token) ; if self . buf . is_empty () { break ; } } } fn check_stack (& mut self , mut depth : usize) { while let Some (& index) = self . scan_stack . back () { let entry = & mut self . buf [index] ; match entry . token { Token :: Begin (_) => { if depth == 0 { break ; } self . scan_stack . pop_back () . unwrap () ; entry . size += self . right_total ; depth -= 1 ; } Token :: End => { self . scan_stack . pop_back () . unwrap () ; entry . size = 1 ; depth += 1 ; } _ => { self . scan_stack . pop_back () . unwrap () ; entry . size += self . right_total ; if depth == 0 { break ; } } } } } fn get_top (& self) -> PrintFrame { * self . print_stack . last () . unwrap_or (& PrintFrame :: Broken { indent : 0 , breaks : Breaks :: Inconsistent }) } fn print_begin (& mut self , token : BeginToken , size : isize) { if size > self . space { self . print_stack . push (PrintFrame :: Broken { indent : self . indent , breaks : token . breaks }) ; self . indent = match token . indent { IndentStyle :: Block { offset } => { usize :: try_from (self . indent as isize + offset) . unwrap () } IndentStyle :: Visual => (MARGIN - self . space) as usize , } ; } else { self . print_stack . push (PrintFrame :: Fits) ; } } fn print_end (& mut self) { if let PrintFrame :: Broken { indent , .. } = self . print_stack . pop () . unwrap () { self . indent = indent ; } } fn print_break (& mut self , token : BreakToken , size : isize) { let fits = match self . get_top () { PrintFrame :: Fits => true , PrintFrame :: Broken { breaks : Breaks :: Consistent , .. } => false , PrintFrame :: Broken { breaks : Breaks :: Inconsistent , .. } => size <= self . space , } ; if fits { self . pending_indentation += token . blank_space ; self . space -= token . blank_space ; } else { if let Some (pre_break) = token . pre_break { self . out . push (pre_break) ; } self . out . push ('\n') ; let indent = self . indent as isize + token . offset ; self . pending_indentation = indent ; self . space = cmp :: max (MARGIN - indent , MIN_SPACE) ; } } fn print_string (& mut self , string : & str) { self . out . reserve (self . pending_indentation as usize) ; self . out . extend (iter :: repeat (' ') . take (self . pending_indentation as usize)) ; self . pending_indentation = 0 ; self . out . push_str (string) ; self . space -= string . len () as isize ; } }}}