# ! [doc = " Experimental types for the trait query interface. The methods"] # ! [doc = " defined in this module are all based on **canonicalization**,"] # ! [doc = " which makes a canonical query by replacing unbound inference"] # ! [doc = " variables and regions, so that results can be reused more broadly."] # ! [doc = " The providers for the queries defined here can be found in"] # ! [doc = " `rustc_traits`."] use split_decls_genesis :: ourprelude :: * ; pub mod dropck_outlives ; pub mod evaluate_obligation ; pub mod method_autoderef ; pub mod normalize ; pub mod type_op ; pub use rustc_middle :: traits :: query :: * ;