mkuse!{use rustc_hir :: def :: DefKind ;}
mkuse!{use rustc_middle :: mir ;}
mkuse!{use rustc_middle :: mir :: visit :: MutVisitor ;}
mkuse!{use rustc_middle :: ty :: { self , TyCtxt } ;}
mkitem!{mkstruct!{# [doc = " Builds a monomorphic body for a given instance."] pub (crate) struct BodyBuilder < 'tcx > { tcx : TyCtxt < 'tcx > , instance : ty :: Instance < 'tcx > , }}}
mkitem!{mkimpl!{impl < 'tcx > BodyBuilder < 'tcx > { pub (crate) fn new (tcx : TyCtxt < 'tcx > , instance : ty :: Instance < 'tcx >) -> Self { let instance = match instance . def { ty :: InstanceKind :: Intrinsic (def_id) => ty :: Instance :: new_raw (def_id , instance . args) , _ => instance , } ; BodyBuilder { tcx , instance } } # [doc = " Build a monomorphic body for a given instance based on the MIR body."] # [doc = ""] # [doc = " All constants are also evaluated."] pub (crate) fn build (mut self) -> mir :: Body < 'tcx > { let body = self . tcx . instance_mir (self . instance . def) . clone () ; let mono_body = if ! self . instance . args . is_empty () || self . tcx . def_kind (self . instance . def_id ()) != DefKind :: AnonConst { let mut mono_body = self . instance . instantiate_mir_and_normalize_erasing_regions (self . tcx , ty :: TypingEnv :: fully_monomorphized () , ty :: EarlyBinder :: bind (body) ,) ; self . visit_body (& mut mono_body) ; mono_body } else { body } ; mono_body } }}}
mkitem!{mkimpl!{impl < 'tcx > MutVisitor < 'tcx > for BodyBuilder < 'tcx > { fn visit_const_operand (& mut self , constant : & mut mir :: ConstOperand < 'tcx > , location : mir :: Location ,) { let const_ = constant . const_ ; let val = match const_ . eval (self . tcx , ty :: TypingEnv :: fully_monomorphized () , constant . span) { Ok (v) => v , Err (mir :: interpret :: ErrorHandled :: Reported (..)) => return , Err (mir :: interpret :: ErrorHandled :: TooGeneric (..)) => { unreachable ! ("Failed to evaluate instance constant: {:?}" , const_) } } ; let ty = constant . ty () ; constant . const_ = mir :: Const :: Val (val , ty) ; self . super_const_operand (constant , location) ; } fn tcx (& self) -> TyCtxt < 'tcx > { self . tcx } }}}