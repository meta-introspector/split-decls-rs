# ! [doc = " Simple file-locking apis for each OS."] # ! [doc = ""] # ! [doc = " This is not meant to be in the standard library, it does nothing with"] # ! [doc = " green/native threading. This is just a bare-bones enough solution for"] # ! [doc = " librustdoc, it is not production quality at all."] use split_decls_genesis :: ourprelude :: * ; cfg_select ! { target_os = "linux" => { mod linux ; use linux as imp ; } target_os = "redox" => { mod linux ; use linux as imp ; } unix => { mod unix ; use unix as imp ; } windows => { mod windows ; use self :: windows as imp ; } _ => { mod unsupported ; use unsupported as imp ; } } pub use imp :: Lock ;