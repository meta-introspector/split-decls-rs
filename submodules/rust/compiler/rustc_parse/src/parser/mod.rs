mkmod!{attr, { 
                getname!(attr);
                getsrc!(attr);
                getpath!(attr);
                get_deps!(attr);
                get_crates!(attr);
                mkinclude!(attr);
                 
            }}
mkmod!{attr_wrapper, { 
                getname!(attr_wrapper);
                getsrc!(attr_wrapper);
                getpath!(attr_wrapper);
                get_deps!(attr_wrapper);
                get_crates!(attr_wrapper);
                mkinclude!(attr_wrapper);
                 
            }}
mkmod!{diagnostics, { 
                getname!(diagnostics);
                getsrc!(diagnostics);
                getpath!(diagnostics);
                get_deps!(diagnostics);
                get_crates!(diagnostics);
                mkinclude!(diagnostics);
                 
            }}
mkmod!{expr, { 
                getname!(expr);
                getsrc!(expr);
                getpath!(expr);
                get_deps!(expr);
                get_crates!(expr);
                mkinclude!(expr);
                 
            }}
mkmod!{generics, { 
                getname!(generics);
                getsrc!(generics);
                getpath!(generics);
                get_deps!(generics);
                get_crates!(generics);
                mkinclude!(generics);
                 
            }}
mkmod!{item, { 
                getname!(item);
                getsrc!(item);
                getpath!(item);
                get_deps!(item);
                get_crates!(item);
                mkinclude!(item);
                 
            }}
mkmod!{nonterminal, { 
                getname!(nonterminal);
                getsrc!(nonterminal);
                getpath!(nonterminal);
                get_deps!(nonterminal);
                get_crates!(nonterminal);
                mkinclude!(nonterminal);
                 
            }}
mkmod!{pat, { 
                getname!(pat);
                getsrc!(pat);
                getpath!(pat);
                get_deps!(pat);
                get_crates!(pat);
                mkinclude!(pat);
                 
            }}
mkmod!{path, { 
                getname!(path);
                getsrc!(path);
                getpath!(path);
                get_deps!(path);
                get_crates!(path);
                mkinclude!(path);
                 
            }}
mkmod!{stmt, { 
                getname!(stmt);
                getsrc!(stmt);
                getpath!(stmt);
                get_deps!(stmt);
                get_crates!(stmt);
                mkinclude!(stmt);
                 
            }}
mkmod!{token_type, { 
                getname!(token_type);
                getsrc!(token_type);
                getpath!(token_type);
                get_deps!(token_type);
                get_crates!(token_type);
                mkinclude!(token_type);
                 
            }}
mkmod!{ty, { 
                getname!(ty);
                getsrc!(ty);
                getpath!(ty);
                get_deps!(ty);
                get_crates!(ty);
                mkinclude!(ty);
                 
            }}
mkmod!{asm, { 
                getname!(asm);
                getsrc!(asm);
                getpath!(asm);
                get_deps!(asm);
                get_crates!(asm);
                mkinclude!(asm);
                 
            }}
mkmod!{cfg_select, { 
                getname!(cfg_select);
                getsrc!(cfg_select);
                getpath!(cfg_select);
                get_deps!(cfg_select);
                get_crates!(cfg_select);
                mkinclude!(cfg_select);
                 
            }}
mkuse!{use std :: assert_matches :: debug_assert_matches ;}
mkuse!{use std :: { fmt , mem , slice } ;}
mkuse!{use attr_wrapper :: { AttrWrapper , UsePreAttrPos } ;}
mkuse!{pub use diagnostics :: AttemptLocalParseRecovery ;}
mkuse!{pub (crate) use expr :: ForbiddenLetReason ;}
mkuse!{pub use expr :: LetChainsPolicy ;}
mkuse!{pub (crate) use item :: { FnContext , FnParseMode } ;}
mkuse!{pub use pat :: { CommaRecoveryMode , RecoverColon , RecoverComma } ;}
mkuse!{pub use path :: PathStyle ;}
mkuse!{use rustc_ast :: token :: { self , IdentIsRaw , InvisibleOrigin , MetaVarKind , NtExprKind , NtPatKind , Token , TokenKind , } ;}
mkuse!{use rustc_ast :: tokenstream :: { ParserRange , ParserReplacement , Spacing , TokenCursor , TokenStream , TokenTree , TokenTreeCursor , } ;}
mkuse!{use rustc_ast :: util :: case :: Case ;}
mkuse!{use rustc_ast :: { self as ast , AnonConst , AttrArgs , AttrId , ByRef , Const , CoroutineKind , DUMMY_NODE_ID , DelimArgs , Expr , ExprKind , Extern , HasAttrs , HasTokens , Mutability , Recovered , Safety , StrLit , Visibility , VisibilityKind , } ;}
mkuse!{use rustc_ast_pretty :: pprust ;}
mkuse!{use rustc_data_structures :: fx :: FxHashMap ;}
mkuse!{use rustc_errors :: { Applicability , Diag , FatalError , MultiSpan , PResult } ;}
mkuse!{use rustc_index :: interval :: IntervalSet ;}
mkuse!{use rustc_session :: parse :: ParseSess ;}
mkuse!{use rustc_span :: { Ident , Span , Symbol , kw , sym } ;}
mkuse!{use thin_vec :: ThinVec ;}
mkuse!{use token_type :: TokenTypeSet ;}
mkuse!{pub use token_type :: { ExpKeywordPair , ExpTokenPair , TokenType } ;}
mkuse!{use tracing :: debug ;}
mkuse!{use crate :: errors :: { self , IncorrectVisibilityRestriction , NonStringAbiLiteral } ;}
mkuse!{use crate :: exp ;}
mkmod!{tests, { 
                getname!(tests);
                getsrc!(tests);
                getpath!(tests);
                get_deps!(tests);
                get_crates!(tests);
                mkinclude!(tests);
                 
            }}
mkmod!{tokenstream, { 
                getname!(tokenstream);
                getsrc!(tokenstream);
                getpath!(tokenstream);
                get_deps!(tokenstream);
                get_crates!(tokenstream);
                mkinclude!(tokenstream);
                mkmod!{tests, { 
                getname!(tests);
                getsrc!(tests);
                getpath!(tests);
                get_deps!(tests);
                get_crates!(tests);
                mkinclude!(tests);
                 
            }} 
            }}
mkitem!{bitflags :: bitflags ! { # [doc = " Restrictions applied while parsing."] # [doc = ""] # [doc = " The parser maintains a bitset of restrictions it will honor while"] # [doc = " parsing. This is essentially used as a way of tracking state of what"] # [doc = " is being parsed and to change behavior based on that."] # [derive (Clone , Copy , Debug)] struct Restrictions : u8 { # [doc = " Restricts expressions for use in statement position."] # [doc = ""] # [doc = " When expressions are used in various places, like statements or"] # [doc = " match arms, this is used to stop parsing once certain tokens are"] # [doc = " reached."] # [doc = ""] # [doc = " For example, `if true {} & 1` with `STMT_EXPR` in effect is parsed"] # [doc = " as two separate expression statements (`if` and a reference to 1)."] # [doc = " Otherwise it is parsed as a bitwise AND where `if` is on the left"] # [doc = " and 1 is on the right."] const STMT_EXPR = 1 << 0 ; # [doc = " Do not allow struct literals."] # [doc = ""] # [doc = " There are several places in the grammar where we don't want to"] # [doc = " allow struct literals because they can require lookahead, or"] # [doc = " otherwise could be ambiguous or cause confusion. For example,"] # [doc = " `if Foo {} {}` isn't clear if it is `Foo{}` struct literal, or"] # [doc = " just `Foo` is the condition, followed by a consequent block,"] # [doc = " followed by an empty block."] # [doc = ""] # [doc = " See [RFC 92](https://rust-lang.github.io/rfcs/0092-struct-grammar.html)."] const NO_STRUCT_LITERAL = 1 << 1 ; # [doc = " Used to provide better error messages for const generic arguments."] # [doc = ""] # [doc = " An un-braced const generic argument is limited to a very small"] # [doc = " subset of expressions. This is used to detect the situation where"] # [doc = " an expression outside of that subset is used, and to suggest to"] # [doc = " wrap the expression in braces."] const CONST_EXPR = 1 << 2 ; # [doc = " Allows `let` expressions."] # [doc = ""] # [doc = " `let pattern = scrutinee` is parsed as an expression, but it is"] # [doc = " only allowed in let chains (`if` and `while` conditions)."] # [doc = " Otherwise it is not an expression (note that `let` in statement"] # [doc = " positions is treated as a `StmtKind::Let` statement, which has a"] # [doc = " slightly different grammar)."] const ALLOW_LET = 1 << 3 ; # [doc = " Used to detect a missing `=>` in a match guard."] # [doc = ""] # [doc = " This is used for error handling in a match guard to give a better"] # [doc = " error message if the `=>` is missing. It is set when parsing the"] # [doc = " guard expression."] const IN_IF_GUARD = 1 << 4 ; # [doc = " Used to detect the incorrect use of expressions in patterns."] # [doc = ""] # [doc = " This is used for error handling while parsing a pattern. During"] # [doc = " error recovery, this will be set to try to parse the pattern as an"] # [doc = " expression, but halts parsing the expression when reaching certain"] # [doc = " tokens like `=`."] const IS_PAT = 1 << 5 ; } }}
mkitem!{mkenum!{# [derive (Clone , Copy , PartialEq , Debug)] enum SemiColonMode { Break , Ignore , Comma , }}}
mkitem!{mkenum!{# [derive (Clone , Copy , PartialEq , Debug)] enum BlockMode { Break , Ignore , }}}
mkitem!{mkenum!{# [doc = " Whether or not we should force collection of tokens for an AST node,"] # [doc = " regardless of whether or not it has attributes"] # [derive (Clone , Copy , Debug , PartialEq)] pub enum ForceCollect { Yes , No , }}}
mkitem!{# [doc = " If the next tokens are ill-formed `$ty::` recover them as `<$ty>::`."] # [macro_export] macro_rules ! maybe_recover_from_interpolated_ty_qpath { ($ self : expr , $ allow_qpath_recovery : expr) => { if $ allow_qpath_recovery && $ self . may_recover () && let Some (mv_kind) = $ self . token . is_metavar_seq () && let token :: MetaVarKind :: Ty { .. } = mv_kind && $ self . check_noexpect_past_close_delim (& token :: PathSep) { let ty = $ self . eat_metavar_seq (mv_kind , | this | this . parse_ty_no_question_mark_recover ()) . expect ("metavar seq ty") ; return $ self . maybe_recover_from_bad_qpath_stage_2 ($ self . prev_token . span , ty) ; } } ; }}
mkitem!{mkenum!{# [derive (Clone , Copy , Debug)] pub enum Recovery { Allowed , Forbidden , }}}
mkitem!{mkstruct!{# [derive (Clone)] pub struct Parser < 'a > { pub psess : & 'a ParseSess , # [doc = " The current token."] pub token : Token , # [doc = " The spacing for the current token."] token_spacing : Spacing , # [doc = " The previous token."] pub prev_token : Token , pub capture_cfg : bool , restrictions : Restrictions , expected_token_types : TokenTypeSet , token_cursor : TokenCursor , num_bump_calls : u32 , break_last_token : u32 , # [doc = " This field is used to keep track of how many left angle brackets we have seen. This is"] # [doc = " required in order to detect extra leading left angle brackets (`<` characters) and error"] # [doc = " appropriately."] # [doc = ""] # [doc = " See the comments in the `parse_path_segment` function for more details."] unmatched_angle_bracket_count : u16 , angle_bracket_nesting : u16 , last_unexpected_token_span : Option < Span > , # [doc = " If present, this `Parser` is not parsing Rust code but rather a macro call."] subparser_name : Option < & 'static str > , capture_state : CaptureState , # [doc = " This allows us to recover when the user forget to add braces around"] # [doc = " multiple statements in the closure body."] current_closure : Option < ClosureSpans > , # [doc = " Whether the parser is allowed to do recovery."] # [doc = " This is disabled when parsing macro arguments, see #103534"] recovery : Recovery , }}}
mkitem!{# [cfg (all (target_pointer_width = "64" , any (target_arch = "aarch64" , target_arch = "x86_64")))] rustc_data_structures :: static_assert_size ! (Parser <'_ >, 288) ;}
mkitem!{mkstruct!{# [doc = " Stores span information about a closure."] # [derive (Clone , Debug)] struct ClosureSpans { whole_closure : Span , closing_pipe : Span , body : Span , }}}
mkitem!{mkenum!{# [doc = " Controls how we capture tokens. Capturing can be expensive,"] # [doc = " so we try to avoid performing capturing in cases where"] # [doc = " we will never need an `AttrTokenStream`."] # [derive (Copy , Clone , Debug)] enum Capturing { # [doc = " We aren't performing any capturing - this is the default mode."] No , # [doc = " We are capturing tokens"] Yes , }}}
mkitem!{mkstruct!{# [derive (Clone , Debug)] struct CaptureState { capturing : Capturing , parser_replacements : Vec < ParserReplacement > , inner_attr_parser_ranges : FxHashMap < AttrId , ParserRange > , seen_attrs : IntervalSet < AttrId > , }}}
mkitem!{mkstruct!{# [doc = " A sequence separator."] # [derive (Debug)] struct SeqSep { # [doc = " The separator token."] sep : Option < ExpTokenPair > , # [doc = " `true` if a trailing separator is allowed."] trailing_sep_allowed : bool , }}}
mkitem!{mkimpl!{impl SeqSep { fn trailing_allowed (sep : ExpTokenPair) -> SeqSep { SeqSep { sep : Some (sep) , trailing_sep_allowed : true } } fn none () -> SeqSep { SeqSep { sep : None , trailing_sep_allowed : false } } }}}
mkitem!{mkenum!{# [derive (Debug)] pub enum FollowedByType { Yes , No , }}}
mkitem!{mkenum!{# [derive (Copy , Clone , Debug)] pub enum Trailing { No , Yes , }}}
mkitem!{mkimpl!{impl From < bool > for Trailing { fn from (b : bool) -> Trailing { if b { Trailing :: Yes } else { Trailing :: No } } }}}
mkitem!{mkenum!{# [derive (Clone , Copy , Debug , PartialEq , Eq)] pub (super) enum TokenDescription { ReservedIdentifier , Keyword , ReservedKeyword , DocComment , MetaVar (MetaVarKind) , }}}
mkitem!{mkimpl!{impl TokenDescription { pub (super) fn from_token (token : & Token) -> Option < Self > { match token . kind { _ if token . is_special_ident () => Some (TokenDescription :: ReservedIdentifier) , _ if token . is_used_keyword () => Some (TokenDescription :: Keyword) , _ if token . is_unused_keyword () => Some (TokenDescription :: ReservedKeyword) , token :: DocComment (..) => Some (TokenDescription :: DocComment) , token :: OpenInvisible (InvisibleOrigin :: MetaVar (kind)) => { Some (TokenDescription :: MetaVar (kind)) } _ => None , } } }}}

macro_rules! token_descr_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function token_descr in module {}", module_path!());
    };
}

mkfn!{
    token_descr_introspect!();
    pub fn token_descr (token : & Token) -> String { let s = pprust :: token_to_string (token) . to_string () ; match (TokenDescription :: from_token (token) , & token . kind) { (Some (TokenDescription :: ReservedIdentifier) , _) => format ! ("reserved identifier `{s}`") , (Some (TokenDescription :: Keyword) , _) => format ! ("keyword `{s}`") , (Some (TokenDescription :: ReservedKeyword) , _) => format ! ("reserved keyword `{s}`") , (Some (TokenDescription :: DocComment) , _) => format ! ("doc comment `{s}`") , (Some (TokenDescription :: MetaVar (kind)) , _) => format ! ("`{kind}` metavariable") , (None , TokenKind :: NtIdent (..)) => format ! ("identifier `{s}`") , (None , TokenKind :: NtLifetime (..)) => format ! ("lifetime `{s}`") , (None , _) => format ! ("`{s}`") , } }
}
mkitem!{mkimpl!{impl < 'a > Parser < 'a > { pub fn new (psess : & 'a ParseSess , stream : TokenStream , subparser_name : Option < & 'static str > ,) -> Self { let mut parser = Parser { psess , token : Token :: dummy () , token_spacing : Spacing :: Alone , prev_token : Token :: dummy () , capture_cfg : false , restrictions : Restrictions :: empty () , expected_token_types : TokenTypeSet :: new () , token_cursor : TokenCursor { curr : TokenTreeCursor :: new (stream) , stack : Vec :: new () } , num_bump_calls : 0 , break_last_token : 0 , unmatched_angle_bracket_count : 0 , angle_bracket_nesting : 0 , last_unexpected_token_span : None , subparser_name , capture_state : CaptureState { capturing : Capturing :: No , parser_replacements : Vec :: new () , inner_attr_parser_ranges : Default :: default () , seen_attrs : IntervalSet :: new (u32 :: MAX as usize) , } , current_closure : None , recovery : Recovery :: Allowed , } ; parser . bump () ; parser . num_bump_calls = 0 ; parser } # [inline] pub fn recovery (mut self , recovery : Recovery) -> Self { self . recovery = recovery ; self } # [inline] fn with_recovery < T > (& mut self , recovery : Recovery , f : impl FnOnce (& mut Self) -> T) -> T { let old = mem :: replace (& mut self . recovery , recovery) ; let res = f (self) ; self . recovery = old ; res } # [doc = " Whether the parser is allowed to recover from broken code."] # [doc = ""] # [doc = " If this returns false, recovering broken code into valid code (especially if this recovery does lookahead)"] # [doc = " is not allowed. All recovery done by the parser must be gated behind this check."] # [doc = ""] # [doc = " Technically, this only needs to restrict eager recovery by doing lookahead at more tokens."] # [doc = " But making the distinction is very subtle, and simply forbidding all recovery is a lot simpler to uphold."] # [inline] fn may_recover (& self) -> bool { matches ! (self . recovery , Recovery :: Allowed) } # [doc = " Version of [`unexpected`](Parser::unexpected) that \"returns\" any type in the `Ok`"] # [doc = " (both those functions never return \"Ok\", and so can lie like that in the type)."] pub fn unexpected_any < T > (& mut self) -> PResult < 'a , T > { match self . expect_one_of (& [] , & []) { Err (e) => Err (e) , Ok (_) => FatalError . raise () , } } pub fn unexpected (& mut self) -> PResult < 'a , () > { self . unexpected_any () } # [doc = " Expects and consumes the token `t`. Signals an error if the next token is not `t`."] pub fn expect (& mut self , exp : ExpTokenPair) -> PResult < 'a , Recovered > { if self . expected_token_types . is_empty () { if self . token == exp . tok { self . bump () ; Ok (Recovered :: No) } else { self . unexpected_try_recover (& exp . tok) } } else { self . expect_one_of (slice :: from_ref (& exp) , & []) } } # [doc = " Expect next token to be edible or inedible token. If edible,"] # [doc = " then consume it; if inedible, then return without consuming"] # [doc = " anything. Signal a fatal error if next token is unexpected."] fn expect_one_of (& mut self , edible : & [ExpTokenPair] , inedible : & [ExpTokenPair] ,) -> PResult < 'a , Recovered > { if edible . iter () . any (| exp | exp . tok == self . token . kind) { self . bump () ; Ok (Recovered :: No) } else if inedible . iter () . any (| exp | exp . tok == self . token . kind) { Ok (Recovered :: No) } else if self . token != token :: Eof && self . last_unexpected_token_span == Some (self . token . span) { FatalError . raise () ; } else { self . expected_one_of_not_found (edible , inedible) . map (| error_guaranteed | Recovered :: Yes (error_guaranteed)) } } pub fn parse_ident (& mut self) -> PResult < 'a , Ident > { self . parse_ident_common (true) } fn parse_ident_common (& mut self , recover : bool) -> PResult < 'a , Ident > { let (ident , is_raw) = self . ident_or_err (recover) ? ; if matches ! (is_raw , IdentIsRaw :: No) && ident . is_reserved () { let err = self . expected_ident_found_err () ; if recover { err . emit () ; } else { return Err (err) ; } } self . bump () ; Ok (ident) } fn ident_or_err (& mut self , recover : bool) -> PResult < 'a , (Ident , IdentIsRaw) > { match self . token . ident () { Some (ident) => Ok (ident) , None => self . expected_ident_found (recover) , } } # [doc = " Checks if the next token is `tok`, and returns `true` if so."] # [doc = ""] # [doc = " This method will automatically add `tok` to `expected_token_types` if `tok` is not"] # [doc = " encountered."] # [inline] pub fn check (& mut self , exp : ExpTokenPair) -> bool { let is_present = self . token == exp . tok ; if ! is_present { self . expected_token_types . insert (exp . token_type) ; } is_present } # [inline] # [must_use] fn check_noexpect (& self , tok : & TokenKind) -> bool { self . token == * tok } fn check_noexpect_past_close_delim (& self , tok : & TokenKind) -> bool { let mut tree_cursor = self . token_cursor . stack . last () . unwrap () . clone () ; tree_cursor . bump () ; matches ! (tree_cursor . curr () , Some (TokenTree :: Token (token :: Token { kind , .. } , _)) if kind == tok) } # [doc = " Consumes a token 'tok' if it exists. Returns whether the given token was present."] # [doc = ""] # [doc = " the main purpose of this function is to reduce the cluttering of the suggestions list"] # [doc = " which using the normal eat method could introduce in some cases."] # [inline] # [must_use] fn eat_noexpect (& mut self , tok : & TokenKind) -> bool { let is_present = self . check_noexpect (tok) ; if is_present { self . bump () } is_present } # [doc = " Consumes a token 'tok' if it exists. Returns whether the given token was present."] # [inline] # [must_use] pub fn eat (& mut self , exp : ExpTokenPair) -> bool { let is_present = self . check (exp) ; if is_present { self . bump () } is_present } # [doc = " If the next token is the given keyword, returns `true` without eating it."] # [doc = " An expectation is also added for diagnostics purposes."] # [inline] # [must_use] fn check_keyword (& mut self , exp : ExpKeywordPair) -> bool { let is_keyword = self . token . is_keyword (exp . kw) ; if ! is_keyword { self . expected_token_types . insert (exp . token_type) ; } is_keyword } # [inline] # [must_use] fn check_keyword_case (& mut self , exp : ExpKeywordPair , case : Case) -> bool { if self . check_keyword (exp) { true } else if case == Case :: Insensitive && let Some ((ident , IdentIsRaw :: No)) = self . token . ident () && ident . as_str () . eq_ignore_ascii_case (exp . kw . as_str ()) { true } else { false } } # [doc = " If the next token is the given keyword, eats it and returns `true`."] # [doc = " Otherwise, returns `false`. An expectation is also added for diagnostics purposes."] # [inline] # [must_use] pub fn eat_keyword (& mut self , exp : ExpKeywordPair) -> bool { let is_keyword = self . check_keyword (exp) ; if is_keyword { self . bump () ; } is_keyword } # [doc = " Eats a keyword, optionally ignoring the case."] # [doc = " If the case differs (and is ignored) an error is issued."] # [doc = " This is useful for recovery."] # [inline] # [must_use] fn eat_keyword_case (& mut self , exp : ExpKeywordPair , case : Case) -> bool { if self . eat_keyword (exp) { true } else if case == Case :: Insensitive && let Some ((ident , IdentIsRaw :: No)) = self . token . ident () && ident . as_str () . eq_ignore_ascii_case (exp . kw . as_str ()) { self . dcx () . emit_err (errors :: KwBadCase { span : ident . span , kw : exp . kw . as_str () }) ; self . bump () ; true } else { false } } # [doc = " If the next token is the given keyword, eats it and returns `true`."] # [doc = " Otherwise, returns `false`. No expectation is added."] # [inline] # [must_use] pub fn eat_keyword_noexpect (& mut self , kw : Symbol) -> bool { let is_keyword = self . token . is_keyword (kw) ; if is_keyword { self . bump () ; } is_keyword } # [doc = " If the given word is not a keyword, signals an error."] # [doc = " If the next token is not the given word, signals an error."] # [doc = " Otherwise, eats it."] pub fn expect_keyword (& mut self , exp : ExpKeywordPair) -> PResult < 'a , () > { if ! self . eat_keyword (exp) { self . unexpected () } else { Ok (()) } } # [doc = " Consume a sequence produced by a metavar expansion, if present."] pub fn eat_metavar_seq < T > (& mut self , mv_kind : MetaVarKind , f : impl FnMut (& mut Parser < 'a >) -> PResult < 'a , T > ,) -> Option < T > { self . eat_metavar_seq_with_matcher (| mvk | mvk == mv_kind , f) } # [doc = " A slightly more general form of `eat_metavar_seq`, for use with the"] # [doc = " `MetaVarKind` variants that have parameters, where an exact match isn't"] # [doc = " desired."] fn eat_metavar_seq_with_matcher < T > (& mut self , match_mv_kind : impl Fn (MetaVarKind) -> bool , mut f : impl FnMut (& mut Parser < 'a >) -> PResult < 'a , T > ,) -> Option < T > { if let token :: OpenInvisible (InvisibleOrigin :: MetaVar (mv_kind)) = self . token . kind && match_mv_kind (mv_kind) { self . bump () ; let res = self . with_recovery (Recovery :: Forbidden , | this | f (this)) ; let res = match res { Ok (res) => res , Err (err) => { err . delay_as_bug () ; return None ; } } ; if let token :: CloseInvisible (InvisibleOrigin :: MetaVar (mv_kind)) = self . token . kind && match_mv_kind (mv_kind) { self . bump () ; Some (res) } else { self . dcx () . span_delayed_bug (self . token . span , "no close delim with reparsing {mv_kind:?}") ; None } } else { None } } # [doc = " Is the given keyword `kw` followed by a non-reserved identifier?"] fn is_kw_followed_by_ident (& self , kw : Symbol) -> bool { self . token . is_keyword (kw) && self . look_ahead (1 , | t | t . is_non_reserved_ident ()) } # [inline] fn check_or_expected (& mut self , ok : bool , token_type : TokenType) -> bool { if ! ok { self . expected_token_types . insert (token_type) ; } ok } fn check_ident (& mut self) -> bool { self . check_or_expected (self . token . is_ident () , TokenType :: Ident) } fn check_path (& mut self) -> bool { self . check_or_expected (self . token . is_path_start () , TokenType :: Path) } fn check_type (& mut self) -> bool { self . check_or_expected (self . token . can_begin_type () , TokenType :: Type) } fn check_const_arg (& mut self) -> bool { self . check_or_expected (self . token . can_begin_const_arg () , TokenType :: Const) } fn check_const_closure (& self) -> bool { self . is_keyword_ahead (0 , & [kw :: Const]) && self . look_ahead (1 , | t | match & t . kind { token :: Ident (kw :: Move | kw :: Use | kw :: Static , IdentIsRaw :: No) | token :: OrOr | token :: Or => true , _ => false , }) } fn check_inline_const (& self , dist : usize) -> bool { self . is_keyword_ahead (dist , & [kw :: Const]) && self . look_ahead (dist + 1 , | t | match & t . kind { token :: OpenBrace => true , token :: OpenInvisible (InvisibleOrigin :: MetaVar (MetaVarKind :: Block)) => true , _ => false , }) } # [doc = " Checks to see if the next token is either `+` or `+=`."] # [doc = " Otherwise returns `false`."] # [inline] fn check_plus (& mut self) -> bool { self . check_or_expected (self . token . is_like_plus () , TokenType :: Plus) } # [doc = " Eats the expected token if it's present possibly breaking"] # [doc = " compound tokens like multi-character operators in process."] # [doc = " Returns `true` if the token was eaten."] fn break_and_eat (& mut self , exp : ExpTokenPair) -> bool { if self . token == exp . tok { self . bump () ; return true ; } match self . token . kind . break_two_token_op (1) { Some ((first , second)) if first == exp . tok => { let first_span = self . psess . source_map () . start_point (self . token . span) ; let second_span = self . token . span . with_lo (first_span . hi ()) ; self . token = Token :: new (first , first_span) ; self . break_last_token += 1 ; self . bump_with ((Token :: new (second , second_span) , self . token_spacing)) ; true } _ => { self . expected_token_types . insert (exp . token_type) ; false } } } # [doc = " Eats `+` possibly breaking tokens like `+=` in process."] fn eat_plus (& mut self) -> bool { self . break_and_eat (exp ! (Plus)) } # [doc = " Eats `&` possibly breaking tokens like `&&` in process."] # [doc = " Signals an error if `&` is not eaten."] fn expect_and (& mut self) -> PResult < 'a , () > { if self . break_and_eat (exp ! (And)) { Ok (()) } else { self . unexpected () } } # [doc = " Eats `|` possibly breaking tokens like `||` in process."] # [doc = " Signals an error if `|` was not eaten."] fn expect_or (& mut self) -> PResult < 'a , () > { if self . break_and_eat (exp ! (Or)) { Ok (()) } else { self . unexpected () } } # [doc = " Eats `<` possibly breaking tokens like `<<` in process."] fn eat_lt (& mut self) -> bool { let ate = self . break_and_eat (exp ! (Lt)) ; if ate { self . unmatched_angle_bracket_count += 1 ; debug ! ("eat_lt: (increment) count={:?}" , self . unmatched_angle_bracket_count) ; } ate } # [doc = " Eats `<` possibly breaking tokens like `<<` in process."] # [doc = " Signals an error if `<` was not eaten."] fn expect_lt (& mut self) -> PResult < 'a , () > { if self . eat_lt () { Ok (()) } else { self . unexpected () } } # [doc = " Eats `>` possibly breaking tokens like `>>` in process."] # [doc = " Signals an error if `>` was not eaten."] fn expect_gt (& mut self) -> PResult < 'a , () > { if self . break_and_eat (exp ! (Gt)) { if self . unmatched_angle_bracket_count > 0 { self . unmatched_angle_bracket_count -= 1 ; debug ! ("expect_gt: (decrement) count={:?}" , self . unmatched_angle_bracket_count) ; } Ok (()) } else { self . unexpected () } } # [doc = " Checks if the next token is contained within `closes`, and returns `true` if so."] fn expect_any_with_type (& mut self , closes_expected : & [ExpTokenPair] , closes_not_expected : & [& TokenKind] ,) -> bool { closes_expected . iter () . any (| & close | self . check (close)) || closes_not_expected . iter () . any (| k | self . check_noexpect (k)) } # [doc = " Parses a sequence until the specified delimiters. The function"] # [doc = " `f` must consume tokens until reaching the next separator or"] # [doc = " closing bracket."] fn parse_seq_to_before_tokens < T > (& mut self , closes_expected : & [ExpTokenPair] , closes_not_expected : & [& TokenKind] , sep : SeqSep , mut f : impl FnMut (& mut Parser < 'a >) -> PResult < 'a , T > ,) -> PResult < 'a , (ThinVec < T > , Trailing , Recovered) > { let mut first = true ; let mut recovered = Recovered :: No ; let mut trailing = Trailing :: No ; let mut v = ThinVec :: new () ; while ! self . expect_any_with_type (closes_expected , closes_not_expected) { if self . token . kind . is_close_delim_or_eof () { break ; } if let Some (exp) = sep . sep { if first { first = false ; } else { match self . expect (exp) { Ok (Recovered :: No) => { self . current_closure . take () ; } Ok (Recovered :: Yes (guar)) => { self . current_closure . take () ; recovered = Recovered :: Yes (guar) ; break ; } Err (mut expect_err) => { let sp = self . prev_token . span . shrink_to_hi () ; let token_str = pprust :: token_kind_to_string (& exp . tok) ; match self . current_closure . take () { Some (closure_spans) if self . token == TokenKind :: Semi => { self . recover_missing_braces_around_closure_body (closure_spans , expect_err ,) ? ; continue ; } _ => { if exp . tok . similar_tokens () . contains (& self . token . kind) { self . bump () ; } } } if self . prev_token . is_ident () && self . token == token :: DotDot { let msg = format ! ("if you meant to bind the contents of the rest of the array \
                                     pattern into `{}`, use `@`" , pprust :: token_to_string (& self . prev_token)) ; expect_err . with_span_suggestion_verbose (self . prev_token . span . shrink_to_hi () . until (self . token . span) , msg , " @ " , Applicability :: MaybeIncorrect ,) . emit () ; break ; } self . last_unexpected_token_span = None ; match f (self) { Ok (t) => { expect_err . with_span_suggestion_short (sp , format ! ("missing `{token_str}`") , token_str , Applicability :: MaybeIncorrect ,) . emit () ; v . push (t) ; continue ; } Err (e) => { for xx in & e . children { expect_err . children . push (xx . clone ()) ; } e . cancel () ; if self . token == token :: Colon { return Err (expect_err) ; } else if let [exp] = closes_expected && exp . token_type == TokenType :: CloseParen { return Err (expect_err) ; } else { expect_err . emit () ; break ; } } } } } } } if sep . trailing_sep_allowed && self . expect_any_with_type (closes_expected , closes_not_expected) { trailing = Trailing :: Yes ; break ; } let t = f (self) ? ; v . push (t) ; } Ok ((v , trailing , recovered)) } fn recover_missing_braces_around_closure_body (& mut self , closure_spans : ClosureSpans , mut expect_err : Diag < '_ > ,) -> PResult < 'a , () > { let initial_semicolon = self . token . span ; while self . eat (exp ! (Semi)) { let _ = self . parse_stmt_without_recovery (false , ForceCollect :: No , false) . unwrap_or_else (| e | { e . cancel () ; None }) ; } expect_err . primary_message ("closure bodies that contain statements must be surrounded by braces") ; let preceding_pipe_span = closure_spans . closing_pipe ; let following_token_span = self . token . span ; let mut first_note = MultiSpan :: from (vec ! [initial_semicolon]) ; first_note . push_span_label (initial_semicolon , "this `;` turns the preceding closure into a statement" ,) ; first_note . push_span_label (closure_spans . body , "this expression is a statement because of the trailing semicolon" ,) ; expect_err . span_note (first_note , "statement found outside of a block") ; let mut second_note = MultiSpan :: from (vec ! [closure_spans . whole_closure]) ; second_note . push_span_label (closure_spans . whole_closure , "this is the parsed closure...") ; second_note . push_span_label (following_token_span , "...but likely you meant the closure to end here" ,) ; expect_err . span_note (second_note , "the closure body may be incorrectly delimited") ; expect_err . span (vec ! [preceding_pipe_span , following_token_span]) ; let opening_suggestion_str = " {" . to_string () ; let closing_suggestion_str = "}" . to_string () ; expect_err . multipart_suggestion ("try adding braces" , vec ! [(preceding_pipe_span . shrink_to_hi () , opening_suggestion_str) , (following_token_span . shrink_to_lo () , closing_suggestion_str) ,] , Applicability :: MaybeIncorrect ,) ; expect_err . emit () ; Ok (()) } # [doc = " Parses a sequence, not including the delimiters. The function"] # [doc = " `f` must consume tokens until reaching the next separator or"] # [doc = " closing bracket."] fn parse_seq_to_before_end < T > (& mut self , close : ExpTokenPair , sep : SeqSep , f : impl FnMut (& mut Parser < 'a >) -> PResult < 'a , T > ,) -> PResult < 'a , (ThinVec < T > , Trailing , Recovered) > { self . parse_seq_to_before_tokens (& [close] , & [] , sep , f) } # [doc = " Parses a sequence, including only the closing delimiter. The function"] # [doc = " `f` must consume tokens until reaching the next separator or"] # [doc = " closing bracket."] fn parse_seq_to_end < T > (& mut self , close : ExpTokenPair , sep : SeqSep , f : impl FnMut (& mut Parser < 'a >) -> PResult < 'a , T > ,) -> PResult < 'a , (ThinVec < T > , Trailing) > { let (val , trailing , recovered) = self . parse_seq_to_before_end (close , sep , f) ? ; if matches ! (recovered , Recovered :: No) && ! self . eat (close) { self . dcx () . span_delayed_bug (self . token . span , "recovered but `parse_seq_to_before_end` did not give us the close token" ,) ; } Ok ((val , trailing)) } # [doc = " Parses a sequence, including both delimiters. The function"] # [doc = " `f` must consume tokens until reaching the next separator or"] # [doc = " closing bracket."] fn parse_unspanned_seq < T > (& mut self , open : ExpTokenPair , close : ExpTokenPair , sep : SeqSep , f : impl FnMut (& mut Parser < 'a >) -> PResult < 'a , T > ,) -> PResult < 'a , (ThinVec < T > , Trailing) > { self . expect (open) ? ; self . parse_seq_to_end (close , sep , f) } # [doc = " Parses a comma-separated sequence, including both delimiters."] # [doc = " The function `f` must consume tokens until reaching the next separator or"] # [doc = " closing bracket."] fn parse_delim_comma_seq < T > (& mut self , open : ExpTokenPair , close : ExpTokenPair , f : impl FnMut (& mut Parser < 'a >) -> PResult < 'a , T > ,) -> PResult < 'a , (ThinVec < T > , Trailing) > { self . parse_unspanned_seq (open , close , SeqSep :: trailing_allowed (exp ! (Comma)) , f) } # [doc = " Parses a comma-separated sequence delimited by parentheses (e.g. `(x, y)`)."] # [doc = " The function `f` must consume tokens until reaching the next separator or"] # [doc = " closing bracket."] pub fn parse_paren_comma_seq < T > (& mut self , f : impl FnMut (& mut Parser < 'a >) -> PResult < 'a , T > ,) -> PResult < 'a , (ThinVec < T > , Trailing) > { self . parse_delim_comma_seq (exp ! (OpenParen) , exp ! (CloseParen) , f) } # [doc = " Advance the parser by one token using provided token as the next one."] fn bump_with (& mut self , next : (Token , Spacing)) { self . inlined_bump_with (next) } # [doc = " This always-inlined version should only be used on hot code paths."] # [inline (always)] fn inlined_bump_with (& mut self , (next_token , next_spacing) : (Token , Spacing)) { self . prev_token = mem :: replace (& mut self . token , next_token) ; self . token_spacing = next_spacing ; self . expected_token_types . clear () ; } # [doc = " Advance the parser by one token."] pub fn bump (& mut self) { let mut next = self . token_cursor . inlined_next () ; self . num_bump_calls += 1 ; self . break_last_token = 0 ; if next . 0 . span . is_dummy () { let fallback_span = self . token . span ; next . 0 . span = fallback_span . with_ctxt (next . 0 . span . ctxt ()) ; } debug_assert ! (! matches ! (next . 0 . kind , token :: OpenInvisible (origin) | token :: CloseInvisible (origin) if origin . skip ())) ; self . inlined_bump_with (next) } # [doc = " Look-ahead `dist` tokens of `self.token` and get access to that token there."] # [doc = " When `dist == 0` then the current token is looked at. `Eof` will be"] # [doc = " returned if the look-ahead is any distance past the end of the tokens."] pub fn look_ahead < R > (& self , dist : usize , looker : impl FnOnce (& Token) -> R) -> R { if dist == 0 { return looker (& self . token) ; } if dist == 1 { match self . token_cursor . curr . curr () { Some (tree) => { match tree { TokenTree :: Token (token , _) => return looker (token) , & TokenTree :: Delimited (dspan , _ , delim , _) => { if ! delim . skip () { return looker (& Token :: new (delim . as_open_token_kind () , dspan . open)) ; } } } } None => { if let Some (last) = self . token_cursor . stack . last () && let Some (& TokenTree :: Delimited (span , _ , delim , _)) = last . curr () && ! delim . skip () { return looker (& Token :: new (delim . as_close_token_kind () , span . close)) ; } } } } let mut cursor = self . token_cursor . clone () ; let mut i = 0 ; let mut token = Token :: dummy () ; while i < dist { token = cursor . next () . 0 ; if matches ! (token . kind , token :: OpenInvisible (origin) | token :: CloseInvisible (origin) if origin . skip ()) { continue ; } i += 1 ; } looker (& token) } # [doc = " Like `lookahead`, but skips over token trees rather than tokens. Useful"] # [doc = " when looking past possible metavariable pasting sites."] pub fn tree_look_ahead < R > (& self , dist : usize , looker : impl FnOnce (& TokenTree) -> R ,) -> Option < R > { assert_ne ! (dist , 0) ; self . token_cursor . curr . look_ahead (dist - 1) . map (looker) } # [doc = " Returns whether any of the given keywords are `dist` tokens ahead of the current one."] pub (crate) fn is_keyword_ahead (& self , dist : usize , kws : & [Symbol]) -> bool { self . look_ahead (dist , | t | kws . iter () . any (| & kw | t . is_keyword (kw))) } # [doc = " Parses asyncness: `async` or nothing."] fn parse_coroutine_kind (& mut self , case : Case) -> Option < CoroutineKind > { let span = self . token_uninterpolated_span () ; if self . eat_keyword_case (exp ! (Async) , case) { if self . token_uninterpolated_span () . at_least_rust_2024 () && self . eat_keyword_case (exp ! (Gen) , case) { let gen_span = self . prev_token_uninterpolated_span () ; Some (CoroutineKind :: AsyncGen { span : span . to (gen_span) , closure_id : DUMMY_NODE_ID , return_impl_trait_id : DUMMY_NODE_ID , }) } else { Some (CoroutineKind :: Async { span , closure_id : DUMMY_NODE_ID , return_impl_trait_id : DUMMY_NODE_ID , }) } } else if self . token_uninterpolated_span () . at_least_rust_2024 () && self . eat_keyword_case (exp ! (Gen) , case) { Some (CoroutineKind :: Gen { span , closure_id : DUMMY_NODE_ID , return_impl_trait_id : DUMMY_NODE_ID , }) } else { None } } # [doc = " Parses fn unsafety: `unsafe`, `safe` or nothing."] fn parse_safety (& mut self , case : Case) -> Safety { if self . eat_keyword_case (exp ! (Unsafe) , case) { Safety :: Unsafe (self . prev_token_uninterpolated_span ()) } else if self . eat_keyword_case (exp ! (Safe) , case) { Safety :: Safe (self . prev_token_uninterpolated_span ()) } else { Safety :: Default } } # [doc = " Parses constness: `const` or nothing."] fn parse_constness (& mut self , case : Case) -> Const { self . parse_constness_ (case , false) } # [doc = " Parses constness for closures (case sensitive, feature-gated)"] fn parse_closure_constness (& mut self) -> Const { let constness = self . parse_constness_ (Case :: Sensitive , true) ; if let Const :: Yes (span) = constness { self . psess . gated_spans . gate (sym :: const_closures , span) ; } constness } fn parse_constness_ (& mut self , case : Case , is_closure : bool) -> Const { if (self . check_const_closure () == is_closure) && ! self . look_ahead (1 , | t | * t == token :: OpenBrace || t . is_metavar_block ()) && self . eat_keyword_case (exp ! (Const) , case) { Const :: Yes (self . prev_token_uninterpolated_span ()) } else { Const :: No } } # [doc = " Parses inline const expressions."] fn parse_const_block (& mut self , span : Span , pat : bool) -> PResult < 'a , Box < Expr > > { self . expect_keyword (exp ! (Const)) ? ; let (attrs , blk) = self . parse_inner_attrs_and_block (None) ? ; let anon_const = AnonConst { id : DUMMY_NODE_ID , value : self . mk_expr (blk . span , ExprKind :: Block (blk , None)) , } ; let blk_span = anon_const . value . span ; let kind = if pat { let guar = self . dcx () . struct_span_err (blk_span , "const blocks cannot be used as patterns") . with_help ("use a named `const`-item or an `if`-guard (`x if x == const { ... }`) instead" ,) . emit () ; ExprKind :: Err (guar) } else { ExprKind :: ConstBlock (anon_const) } ; Ok (self . mk_expr_with_attrs (span . to (blk_span) , kind , attrs)) } # [doc = " Parses mutability (`mut` or nothing)."] fn parse_mutability (& mut self) -> Mutability { if self . eat_keyword (exp ! (Mut)) { Mutability :: Mut } else { Mutability :: Not } } # [doc = " Parses reference binding mode (`ref`, `ref mut`, or nothing)."] fn parse_byref (& mut self) -> ByRef { if self . eat_keyword (exp ! (Ref)) { ByRef :: Yes (self . parse_mutability ()) } else { ByRef :: No } } # [doc = " Possibly parses mutability (`const` or `mut`)."] fn parse_const_or_mut (& mut self) -> Option < Mutability > { if self . eat_keyword (exp ! (Mut)) { Some (Mutability :: Mut) } else if self . eat_keyword (exp ! (Const)) { Some (Mutability :: Not) } else { None } } fn parse_field_name (& mut self) -> PResult < 'a , Ident > { if let token :: Literal (token :: Lit { kind : token :: Integer , symbol , suffix }) = self . token . kind { if let Some (suffix) = suffix { self . dcx () . emit_err (errors :: InvalidLiteralSuffixOnTupleIndex { span : self . token . span , suffix , }) ; } self . bump () ; Ok (Ident :: new (symbol , self . prev_token . span)) } else { self . parse_ident_common (true) } } fn parse_delim_args (& mut self) -> PResult < 'a , Box < DelimArgs > > { if let Some (args) = self . parse_delim_args_inner () { Ok (Box :: new (args)) } else { self . unexpected_any () } } fn parse_attr_args (& mut self) -> PResult < 'a , AttrArgs > { Ok (if let Some (args) = self . parse_delim_args_inner () { AttrArgs :: Delimited (args) } else if self . eat (exp ! (Eq)) { let eq_span = self . prev_token . span ; let expr = self . parse_expr_force_collect () ? ; AttrArgs :: Eq { eq_span , expr } } else { AttrArgs :: Empty }) } fn parse_delim_args_inner (& mut self) -> Option < DelimArgs > { let delimited = self . check (exp ! (OpenParen)) || self . check (exp ! (OpenBracket)) || self . check (exp ! (OpenBrace)) ; delimited . then (| | { let TokenTree :: Delimited (dspan , _ , delim , tokens) = self . parse_token_tree () else { unreachable ! () } ; DelimArgs { dspan , delim , tokens } }) } # [doc = " Parses a single token tree from the input."] pub fn parse_token_tree (& mut self) -> TokenTree { if self . token . kind . open_delim () . is_some () { let tree = self . token_cursor . stack . last () . unwrap () . curr () . unwrap () . clone () ; debug_assert_matches ! (tree , TokenTree :: Delimited (..)) ; let target_depth = self . token_cursor . stack . len () - 1 ; if let Capturing :: No = self . capture_state . capturing { self . token_cursor . curr . bump_to_end () ; self . bump () ; debug_assert_eq ! (self . token_cursor . stack . len () , target_depth) ; } else { loop { self . bump () ; if self . token_cursor . stack . len () == target_depth { break ; } } } debug_assert ! (self . token . kind . close_delim () . is_some ()) ; self . bump () ; tree } else { assert ! (! self . token . kind . is_close_delim_or_eof ()) ; let prev_spacing = self . token_spacing ; self . bump () ; TokenTree :: Token (self . prev_token , prev_spacing) } } pub fn parse_tokens (& mut self) -> TokenStream { let mut result = Vec :: new () ; loop { if self . token . kind . is_close_delim_or_eof () { break ; } else { result . push (self . parse_token_tree ()) ; } } TokenStream :: new (result) } # [doc = " Evaluates the closure with restrictions in place."] # [doc = ""] # [doc = " Afters the closure is evaluated, restrictions are reset."] fn with_res < T > (& mut self , res : Restrictions , f : impl FnOnce (& mut Self) -> T) -> T { let old = self . restrictions ; self . restrictions = res ; let res = f (self) ; self . restrictions = old ; res } # [doc = " Parses `pub` and `pub(in path)` plus shortcuts `pub(crate)` for `pub(in crate)`, `pub(self)`"] # [doc = " for `pub(in self)` and `pub(super)` for `pub(in super)`."] # [doc = " If the following element can't be a tuple (i.e., it's a function definition), then"] # [doc = " it's not a tuple struct field), and the contents within the parentheses aren't valid,"] # [doc = " so emit a proper diagnostic."] pub fn parse_visibility (& mut self , fbt : FollowedByType) -> PResult < 'a , Visibility > { if let Some (vis) = self . eat_metavar_seq (MetaVarKind :: Vis , | this | this . parse_visibility (FollowedByType :: Yes)) { return Ok (vis) ; } if ! self . eat_keyword (exp ! (Pub)) { return Ok (Visibility { span : self . token . span . shrink_to_lo () , kind : VisibilityKind :: Inherited , tokens : None , }) ; } let lo = self . prev_token . span ; if self . check (exp ! (OpenParen)) { if self . is_keyword_ahead (1 , & [kw :: In]) { self . bump () ; self . bump () ; let path = self . parse_path (PathStyle :: Mod) ? ; self . expect (exp ! (CloseParen)) ? ; let vis = VisibilityKind :: Restricted { path : Box :: new (path) , id : ast :: DUMMY_NODE_ID , shorthand : false , } ; return Ok (Visibility { span : lo . to (self . prev_token . span) , kind : vis , tokens : None , }) ; } else if self . look_ahead (2 , | t | t == & token :: CloseParen) && self . is_keyword_ahead (1 , & [kw :: Crate , kw :: Super , kw :: SelfLower]) { self . bump () ; let path = self . parse_path (PathStyle :: Mod) ? ; self . expect (exp ! (CloseParen)) ? ; let vis = VisibilityKind :: Restricted { path : Box :: new (path) , id : ast :: DUMMY_NODE_ID , shorthand : true , } ; return Ok (Visibility { span : lo . to (self . prev_token . span) , kind : vis , tokens : None , }) ; } else if let FollowedByType :: No = fbt { self . recover_incorrect_vis_restriction () ? ; } } Ok (Visibility { span : lo , kind : VisibilityKind :: Public , tokens : None }) } # [doc = " Recovery for e.g. `pub(something) fn ...` or `struct X { pub(something) y: Z }`"] fn recover_incorrect_vis_restriction (& mut self) -> PResult < 'a , () > { self . bump () ; let path = self . parse_path (PathStyle :: Mod) ? ; self . expect (exp ! (CloseParen)) ? ; let path_str = pprust :: path_to_string (& path) ; self . dcx () . emit_err (IncorrectVisibilityRestriction { span : path . span , inner_str : path_str }) ; Ok (()) } # [doc = " Parses `extern string_literal?`."] fn parse_extern (& mut self , case : Case) -> Extern { if self . eat_keyword_case (exp ! (Extern) , case) { let mut extern_span = self . prev_token . span ; let abi = self . parse_abi () ; if let Some (abi) = abi { extern_span = extern_span . to (abi . span) ; } Extern :: from_abi (abi , extern_span) } else { Extern :: None } } # [doc = " Parses a string literal as an ABI spec."] fn parse_abi (& mut self) -> Option < StrLit > { match self . parse_str_lit () { Ok (str_lit) => Some (str_lit) , Err (Some (lit)) => match lit . kind { ast :: LitKind :: Err (_) => None , _ => { self . dcx () . emit_err (NonStringAbiLiteral { span : lit . span }) ; None } } , Err (None) => None , } } fn collect_tokens_no_attrs < R : HasAttrs + HasTokens > (& mut self , f : impl FnOnce (& mut Self) -> PResult < 'a , R > ,) -> PResult < 'a , R > { self . collect_tokens (None , AttrWrapper :: empty () , ForceCollect :: Yes , | this , _attrs | { Ok ((f (this) ? , Trailing :: No , UsePreAttrPos :: No)) }) } # [doc = " Checks for `::` or, potentially, `:::` and then look ahead after it."] fn check_path_sep_and_look_ahead (& mut self , looker : impl Fn (& Token) -> bool) -> bool { if self . check (exp ! (PathSep)) { if self . may_recover () && self . look_ahead (1 , | t | t . kind == token :: Colon) { debug_assert ! (! self . look_ahead (1 , & looker) , "Looker must not match on colon") ; self . look_ahead (2 , looker) } else { self . look_ahead (1 , looker) } } else { false } } # [doc = " `::{` or `::*`"] fn is_import_coupler (& mut self) -> bool { self . check_path_sep_and_look_ahead (| t | matches ! (t . kind , token :: OpenBrace | token :: Star)) } # [allow (unused)] pub (crate) fn debug_lookahead (& self , lookahead : usize) -> impl fmt :: Debug { fmt :: from_fn (move | f | { let mut dbg_fmt = f . debug_struct ("Parser") ; dbg_fmt . field ("prev_token" , & self . prev_token) ; let mut tokens = vec ! [] ; for i in 0 .. lookahead { let tok = self . look_ahead (i , | tok | tok . kind) ; let is_eof = tok == TokenKind :: Eof ; tokens . push (tok) ; if is_eof { break ; } } dbg_fmt . field_with ("tokens" , | field | field . debug_list () . entries (tokens) . finish ()) ; dbg_fmt . field ("approx_token_stream_pos" , & self . num_bump_calls) ; if let Some (subparser) = self . subparser_name { dbg_fmt . field ("subparser_name" , & subparser) ; } if let Recovery :: Forbidden = self . recovery { dbg_fmt . field ("recovery" , & self . recovery) ; } dbg_fmt . finish_non_exhaustive () }) } pub fn clear_expected_token_types (& mut self) { self . expected_token_types . clear () ; } pub fn approx_token_stream_pos (& self) -> u32 { self . num_bump_calls } # [doc = " For interpolated `self.token`, returns a span of the fragment to which"] # [doc = " the interpolated token refers. For all other tokens this is just a"] # [doc = " regular span. It is particularly important to use this for identifiers"] # [doc = " and lifetimes for which spans affect name resolution and edition"] # [doc = " checks. Note that keywords are also identifiers, so they should use"] # [doc = " this if they keep spans or perform edition checks."] pub fn token_uninterpolated_span (& self) -> Span { match & self . token . kind { token :: NtIdent (ident , _) | token :: NtLifetime (ident , _) => ident . span , token :: OpenInvisible (InvisibleOrigin :: MetaVar (_)) => self . look_ahead (1 , | t | t . span) , _ => self . token . span , } } # [doc = " Like `token_uninterpolated_span`, but works on `self.prev_token`."] pub fn prev_token_uninterpolated_span (& self) -> Span { match & self . prev_token . kind { token :: NtIdent (ident , _) | token :: NtLifetime (ident , _) => ident . span , token :: OpenInvisible (InvisibleOrigin :: MetaVar (_)) => self . look_ahead (0 , | t | t . span) , _ => self . prev_token . span , } } }}}
mkitem!{mkenum!{# [derive (Clone , Debug)] pub enum ParseNtResult { Tt (TokenTree) , Ident (Ident , IdentIsRaw) , Lifetime (Ident , IdentIsRaw) , Item (Box < ast :: Item >) , Block (Box < ast :: Block >) , Stmt (Box < ast :: Stmt >) , Pat (Box < ast :: Pat > , NtPatKind) , Expr (Box < ast :: Expr > , NtExprKind) , Literal (Box < ast :: Expr >) , Ty (Box < ast :: Ty >) , Meta (Box < ast :: AttrItem >) , Path (Box < ast :: Path >) , Vis (Box < ast :: Visibility >) , }}}