mkuse!{use std :: sync :: Mutex ;}
mkuse!{use std :: collections :: HashMap ;}
mkuse!{use std :: sync :: LazyLock ;}
mkitem!{static USE_MATRIX : LazyLock < Mutex < HashMap < String , Vec < String > > > > = LazyLock :: new (| | Mutex :: new (HashMap :: new ())) ;}

macro_rules! get_use_matrix_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function get_use_matrix in module {}", module_path!());
    };
}

mkfn!{
    get_use_matrix_introspect!();
    pub fn get_use_matrix () -> HashMap < String , Vec < String > > { USE_MATRIX . lock () . unwrap () . clone () }
}
mkitem!{macro_rules ! emit_message { ($ ($ arg : tt) *) => { { use std :: fs :: OpenOptions ; use std :: io :: Write ; let message = format ! ($ ($ arg) *) ; if let Ok (mut file) = OpenOptions :: new () . create (true) . append (true) . open ("macro_report.txt") { let _ = writeln ! (file , "{}" , message) ; } } } ; }}
mkitem!{macro_rules ! mkfn { ($ introspect : expr ; $ (#[$ attr : meta]) * pub fn $ name : ident < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * pub fn $ name < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_trait_bounds_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_trait_bounds_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * fn $ name < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: trait_bounds_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: trait_bounds_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn catch_fatal_errors < F : FnOnce () -> R , R > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * fn catch_fatal_errors < F : FnOnce () -> R , R > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: catch_fatal_errors_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: catch_fatal_errors_specific") ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn parse_crate_attrs < $ lifetime : lifetime > ($ ($ param : tt) *) -> PResult < $ lifetime2 : lifetime , ast :: AttrVec > $ body : block) => { $ (#[$ attr]) * fn parse_crate_attrs < $ lifetime > ($ ($ param) *) -> PResult < $ lifetime2 , ast :: AttrVec > { $ introspect ; emit_message ! ("🚀 MARKER: parse_crate_attrs_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: parse_crate_attrs_specific") ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn init_logger_with_additional_layer < F , T > ($ ($ param : tt) *) where F : FnOnce () -> T , T : rustc_log :: BuildSubscriberRet , $ body : block) => { $ (#[$ attr]) * fn init_logger_with_additional_layer < F , T > ($ ($ param) *) where F : FnOnce () -> T , T : rustc_log :: BuildSubscriberRet , { $ introspect ; emit_message ! ("🚀 MARKER: init_logger_specific") ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: init_logger_specific") ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident < F , T > ($ ($ param : tt) *) $ (-> $ ret : ty) ? where F : FnOnce ($ ($ fnonce_args : tt) *) $ ($ where_rest : tt) * $ body : block) => { $ (#[$ attr]) * fn $ name < F , T > ($ ($ param) *) $ (-> $ ret) ? where F : FnOnce ($ ($ fnonce_args) *) $ ($ where_rest) * { $ introspect ; emit_message ! ("🚀 MARKER: two_generics_where_fnonce - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics_where_fnonce - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? where $ ($ where_clause : tt) * $ body : block) => { $ (#[$ attr]) * fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? where $ ($ where_clause) * { $ introspect ; emit_message ! ("🚀 MARKER: two_generics_where - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics_where - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: two_generics - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: two_generics - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * pub fn $ name : ident < $ gen1 : ident , $ gen2 : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * pub fn $ name < $ gen1 , $ gen2 > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_two_generics - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_two_generics - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * pub ($ vis : ident) fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * pub ($ vis) fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_vis - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_vis - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident < $ gen : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * fn $ name < $ gen > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: generic_single - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: generic_single - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * pub fn $ name : ident < $ gen : ident > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * pub fn $ name < $ gen > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_generic_single - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_generic_single - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident < $ lifetime : lifetime > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * fn $ name < $ lifetime > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: lifetime - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: lifetime - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * pub fn $ name : ident < $ lifetime : lifetime > ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * pub fn $ name < $ lifetime > ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_lifetime - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_lifetime - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * pub fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * pub fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: pub_non_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: pub_non_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ (#[$ attr : meta]) * fn $ name : ident ($ ($ param : tt) *) $ (-> $ ret : ty) ? $ body : block) => { $ (#[$ attr]) * fn $ name ($ ($ param) *) $ (-> $ ret) ? { $ introspect ; emit_message ! ("🚀 MARKER: non_generic - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: non_generic - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; fn $ name : ident () $ body : block) => { fn $ name () { $ introspect ; emit_message ! ("🚀 MARKER: simple - {}" , stringify ! ($ name)) ; let result = (|| $ body) () ; emit_message ! ("🎯 MARKER: simple - {}" , stringify ! ($ name)) ; result } } ; ($ introspect : expr ; $ ($ anything : tt) *) => { $ ($ anything) * } ; }}
mkitem!{macro_rules ! safe_println { ($ ($ arg : tt) *) => { () } ; }}
mkitem!{macro_rules ! safe_print { ($ ($ arg : tt) *) => { () } ; }}
mkitem!{#[macro_export] macro_rules ! include_rust_compiler { ($ crate_name : literal , $ subpath : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_" , $ subpath , "_" , $ file , ".rs")) ; } ; ($ crate_name : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_src_" , $ file , ".rs")) ; } ; ($ crate_name : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_compiler_rustc_" , $ crate_name , "_src_lib.rs")) ; } ; }}
mkitem!{#[macro_export] macro_rules ! include_rust_library { ($ lib_name : literal , $ subpath : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_" , $ subpath , "_" , $ file , ".rs")) ; } ; ($ lib_name : literal , $ file : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_src_" , $ file , ".rs")) ; } ; ($ lib_name : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_submodules_rust_library_" , $ lib_name , "_src_lib.rs")) ; } ; }}
mkitem!{#[macro_export] macro_rules ! include_processed { ($ path : literal) => { include ! (concat ! (env ! ("CARGO_MANIFEST_DIR") , "/processed_" , $ path , ".rs")) ; } ; }}
mkitem!{macro_rules ! mkinclude { ($ path : ident) => { } ; ($ path : literal) => { include ! ($ path) } ; }}
mkitem!{macro_rules ! mkitem { (include ! ($ path : ident) ;) => { } ; ($ macro_name : ident :: $ macro_sub : ident ! { $ string_lit : literal }) => { $ macro_name :: $ macro_sub ! { $ string_lit } } ; ($ macro_name : ident :: $ macro_sub : ident ! { $ ($ args : tt) * }) => { $ macro_name :: $ macro_sub ! { $ ($ args) * } } ; ($ macro_name : ident ! { $ ($ args : tt) * }) => { $ macro_name ! { $ ($ args) * } } ; ($ item : item) => { $ item } ; }}
mkitem!{#[macro_export] macro_rules ! mkmod { ($ name : ident , { $ ($ content : tt) * }) => { emit_message ! ("MOD|{}|{}" , module_path ! () , stringify ! ($ name)) ; mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; (pub mod $ name : ident { $ ($ content : tt) * }) => { emit_message ! ("MOD|{}|{}" , module_path ! () , stringify ! ($ name)) ; pub mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; (mod $ name : ident { $ ($ content : tt) * }) => { emit_message ! ("MOD|{}|{}" , module_path ! () , stringify ! ($ name)) ; mod $ name { const MODULE_NAME : & str = stringify ! ($ name) ; $ ($ content) * } } ; }}
mkitem!{#[macro_export] macro_rules ! mkuse { ($ use_stmt : item) => { emit_message ! ("USE|{}|{}" , module_path ! () , stringify ! ($ use_stmt)) ; $ use_stmt } ; }}
mkitem!{macro_rules ! mkstruct { ($ struct_def : item) => { $ struct_def } ; }}
mkitem!{macro_rules ! mkenum { ($ enum_def : item) => { $ enum_def } ; }}
mkitem!{macro_rules ! mktrait { ($ trait_def : item) => { $ trait_def } ; }}
mkitem!{macro_rules ! mkimpl { ($ impl_def : item) => { $ impl_def } ; }}
mkitem!{macro_rules ! getname { ($ name : ident) => { stringify ! ($ name) } ; }}
mkitem!{macro_rules ! getsrc { ($ name : ident) => { "processed file" } ; }}
mkitem!{macro_rules ! getpath { ($ name : ident) => { "processed_path" } ; }}
mkitem!{macro_rules ! get_deps { ($ name : ident) => { vec ! [] } ; }}
mkmod!{rustc_complete, { 
                getname!(rustc_complete);
                getsrc!(rustc_complete);
                getpath!(rustc_complete);
                get_deps!(rustc_complete);
                get_crates!(rustc_complete);
                mkinclude!(rustc_complete);
                mkmod!{emitter, { 
                getname!(emitter);
                getsrc!(emitter);
                getpath!(emitter);
                get_deps!(emitter);
                get_crates!(emitter);
                mkinclude!(emitter);
                
macro_rules! stderr_destination_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function stderr_destination in module {}", module_path!());
    };
}

mkfn!{
    stderr_destination_introspect!();
    pub fn stderr_destination () { }
} 
            }}
mkmod!{registry, { 
                getname!(registry);
                getsrc!(registry);
                getpath!(registry);
                get_deps!(registry);
                get_crates!(registry);
                mkinclude!(registry);
                mkitem!{mkstruct!{pub struct Registry ;}} 
            }}
mkmod!{translation, { 
                getname!(translation);
                getsrc!(translation);
                getpath!(translation);
                get_deps!(translation);
                get_crates!(translation);
                mkinclude!(translation);
                mkitem!{mkstruct!{pub struct Translator ;}} 
            }}
mkitem!{mkstruct!{pub struct ColorConfig ;}}
mkitem!{mkstruct!{pub struct DiagCtxt ;}}
mkitem!{mkstruct!{pub struct ErrCode ;}}
mkitem!{mkstruct!{pub struct FatalError ;}}
mkitem!{mkstruct!{pub struct PResult < T > (pub T) ;}}
mkmod!{markdown, { 
                getname!(markdown);
                getsrc!(markdown);
                getpath!(markdown);
                get_deps!(markdown);
                get_crates!(markdown);
                mkinclude!(markdown);
                 
            }}
mkmod!{config, { 
                getname!(config);
                getsrc!(config);
                getpath!(config);
                get_deps!(config);
                get_crates!(config);
                mkinclude!(config);
                mkitem!{mkstruct!{pub struct CG_OPTIONS ;}}
mkitem!{mkstruct!{pub struct CrateType ;}}
mkitem!{mkstruct!{pub struct ErrorOutputType ;}}
mkitem!{mkstruct!{pub struct Input ;}}
mkitem!{mkstruct!{pub struct OptionDesc ;}}
mkitem!{mkstruct!{pub struct OutFileName ;}}
mkitem!{mkstruct!{pub struct OutputType ;}}
mkitem!{mkstruct!{pub struct Sysroot ;}}
mkitem!{mkstruct!{pub struct UnstableOptions ;}}
mkitem!{mkstruct!{pub struct Z_OPTIONS ;}}

macro_rules! nightly_options_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function nightly_options in module {}", module_path!());
    };
}

mkfn!{
    nightly_options_introspect!();
    pub fn nightly_options () { }
}

macro_rules! parse_target_triple_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function parse_target_triple in module {}", module_path!());
    };
}

mkfn!{
    parse_target_triple_introspect!();
    pub fn parse_target_triple () { }
} 
            }}
mkmod!{getopts, { 
                getname!(getopts);
                getsrc!(getopts);
                getpath!(getopts);
                get_deps!(getopts);
                get_crates!(getopts);
                mkinclude!(getopts);
                mkitem!{mkstruct!{pub struct Matches ;}} 
            }}
mkmod!{lint, { 
                getname!(lint);
                getsrc!(lint);
                getpath!(lint);
                get_deps!(lint);
                get_crates!(lint);
                mkinclude!(lint);
                mkitem!{mkstruct!{pub struct Lint ;}}
mkitem!{mkstruct!{pub struct LintId ;}} 
            }}
mkmod!{output, { 
                getname!(output);
                getsrc!(output);
                getpath!(output);
                get_deps!(output);
                get_crates!(output);
                mkinclude!(output);
                mkitem!{mkstruct!{pub struct CRATE_TYPES ;}}

macro_rules! collect_crate_types_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function collect_crate_types in module {}", module_path!());
    };
}

mkfn!{
    collect_crate_types_introspect!();
    pub fn collect_crate_types () { }
}

macro_rules! invalid_output_for_target_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function invalid_output_for_target in module {}", module_path!());
    };
}

mkfn!{
    invalid_output_for_target_introspect!();
    pub fn invalid_output_for_target () { }
} 
            }}
mkitem!{mkstruct!{pub struct EarlyDiagCtxt ;}}
mkitem!{mkstruct!{pub struct Session ;}}
mkitem!{mkstruct!{pub struct FileName ;}}
mkmod!{def_id, { 
                getname!(def_id);
                getsrc!(def_id);
                getpath!(def_id);
                get_deps!(def_id);
                get_crates!(def_id);
                mkinclude!(def_id);
                mkitem!{mkstruct!{pub struct LOCAL_CRATE ;}} 
            }}
mkmod!{ty, { 
                getname!(ty);
                getsrc!(ty);
                getpath!(ty);
                get_deps!(ty);
                get_crates!(ty);
                mkinclude!(ty);
                mkitem!{mkstruct!{pub struct TyCtxt < T > (pub T) ;}} 
            }} 
            }}
mkmod!{session_diagnostics, { 
                getname!(session_diagnostics);
                getsrc!(session_diagnostics);
                getpath!(session_diagnostics);
                get_deps!(session_diagnostics);
                get_crates!(session_diagnostics);
                mkinclude!(session_diagnostics);
                mkitem!{mkstruct!{pub struct CantEmitMIR ;}}
mkitem!{mkstruct!{pub struct RLinkEmptyVersionNumber ;}}
mkitem!{mkstruct!{pub struct RLinkEncodingVersionMismatch ;}}
mkitem!{mkstruct!{pub struct RLinkRustcVersionMismatch ;}}
mkitem!{mkstruct!{pub struct RLinkWrongFileType ;}}
mkitem!{mkstruct!{pub struct RlinkCorruptFile ;}}
mkitem!{mkstruct!{pub struct RlinkNotAFile ;}}
mkitem!{mkstruct!{pub struct RlinkUnableToRead ;}}
mkitem!{mkstruct!{pub struct UnstableFeatureUsage ;}} 
            }}
mkitem!{macro_rules ! do_not_use_print { ($ ($ t : tt) *) => { compile_error ! ("Don't use print") } ; }}
mkitem!{macro_rules ! do_not_use_safe_print { ($ ($ t : tt) *) => { compile_error ! ("Don't use safe_print") } ; }}
mkitem!{macro_rules ! mktrait { ($ trait_def : item) => { $ trait_def } ; }}
mkitem!{macro_rules ! mkimpl { ($ impl_def : item) => { $ impl_def } ; }}
mkitem!{macro_rules ! getname { ($ name : ident) => { pub fn get_module_name () -> &'static str { stringify ! ($ name) } } ; }}
mkitem!{macro_rules ! getsrc { ($ name : ident) => { pub fn get_source_info () -> &'static str { concat ! ("Module: " , stringify ! ($ name)) } } ; }}
mkitem!{macro_rules ! getpath { ($ name : ident) => { pub fn get_module_path () -> &'static str { module_path ! () } } ; }}
mkitem!{macro_rules ! get_deps { ($ name : ident) => { pub fn get_dependencies () -> &'static [&'static str] { & [] } } ; }}
mkitem!{macro_rules ! get_crates { ($ name : ident) => { pub fn get_required_crates () -> &'static [&'static str] { & [] } } ; }}
mkitem!{macro_rules ! forall_crates { ($ ($ crate_name : ident) ,*) => { $ (extern crate $ crate_name ;) * } ; }}
mkitem!{macro_rules ! emit_extern { ($ crate_name : ident) => { extern crate $ crate_name ; } ; }}
mkitem!{macro_rules ! get_externs { ($ crate_name : ident) => { stringify ! ($ crate_name) } ; }}
mkuse!{use std :: cmp ;}
mkuse!{use std :: collections :: hash_map :: Entry :: { Occupied , Vacant } ;}
mkuse!{use rustc_abi :: FieldIdx ;}
mkuse!{use rustc_ast as ast ;}
mkuse!{use rustc_data_structures :: fx :: FxHashMap ;}
mkuse!{use rustc_errors :: codes :: * ;}
mkuse!{use rustc_errors :: { Applicability , Diag , ErrorGuaranteed , MultiSpan , pluralize , struct_span_code_err , } ;}
mkuse!{use rustc_hir :: def :: { CtorKind , DefKind , Res } ;}
mkuse!{use rustc_hir :: def_id :: DefId ;}
mkuse!{use rustc_hir :: pat_util :: EnumerateAndAdjustIterator ;}
mkuse!{use rustc_hir :: { self as hir , BindingMode , ByRef , ExprKind , HirId , LangItem , Mutability , Pat , PatExpr , PatExprKind , PatKind , expr_needs_parens , } ;}
mkuse!{use rustc_hir_analysis :: autoderef :: report_autoderef_recursion_limit_error ;}
mkuse!{use rustc_infer :: infer :: RegionVariableOrigin ;}
mkuse!{use rustc_middle :: traits :: PatternOriginExpr ;}
mkuse!{use rustc_middle :: ty :: { self , Ty , TypeVisitableExt } ;}
mkuse!{use rustc_middle :: { bug , span_bug } ;}
mkuse!{use rustc_session :: lint :: builtin :: NON_EXHAUSTIVE_OMITTED_PATTERNS ;}
mkuse!{use rustc_session :: parse :: feature_err ;}
mkuse!{use rustc_span :: edit_distance :: find_best_match_for_name ;}
mkuse!{use rustc_span :: edition :: Edition ;}
mkuse!{use rustc_span :: source_map :: Spanned ;}
mkuse!{use rustc_span :: { BytePos , DUMMY_SP , Ident , Span , kw , sym } ;}
mkuse!{use rustc_trait_selection :: infer :: InferCtxtExt ;}
mkuse!{use rustc_trait_selection :: traits :: { ObligationCause , ObligationCauseCode } ;}
mkuse!{use tracing :: { debug , instrument , trace } ;}
mkuse!{use ty :: VariantDef ;}
mkuse!{use ty :: adjustment :: { PatAdjust , PatAdjustment } ;}
mkuse!{use super :: report_unexpected_variant_res ;}
mkuse!{use crate :: expectation :: Expectation ;}
mkuse!{use crate :: gather_locals :: DeclOrigin ;}
mkuse!{use crate :: { FnCtxt , errors } ;}
mkitem!{const CANNOT_IMPLICITLY_DEREF_POINTER_TRAIT_OBJ : & str = "\
This error indicates that a pointer to a trait type cannot be implicitly dereferenced by a \
pattern. Every trait defines a type, but because the size of trait implementors isn't fixed, \
this type has no compile-time size. Therefore, all accesses to trait types must be through \
pointers. If you encounter this error you should try to avoid dereferencing the pointer.

You can read more about trait objects in the Trait Objects section of the Reference: \
https://doc.rust-lang.org/reference/types.html#trait-objects" ;}

macro_rules! is_number_introspect {
    () => {
        emit_message!("📊 INTROSPECT: Function is_number in module {}", module_path!());
    };
}

mkfn!{
    is_number_introspect!();
    fn is_number (text : & str) -> bool { text . chars () . all (| c : char | c . is_digit (10)) }
}
mkitem!{mkstruct!{#[doc = " Information about the expected type at the top level of type checking a pattern."] #[doc = ""] #[doc = " **NOTE:** This is only for use by diagnostics. Do NOT use for type checking logic!"] #[derive (Copy , Clone)] struct TopInfo < 'tcx > { #[doc = " The `expected` type at the top level of type checking a pattern."] expected : Ty < 'tcx > , #[doc = " Was the origin of the `span` from a scrutinee expression?"] #[doc = ""] #[doc = " Otherwise there is no scrutinee and it could be e.g. from the type of a formal parameter."] origin_expr : Option < & 'tcx hir :: Expr < 'tcx > > , #[doc = " The span giving rise to the `expected` type, if one could be provided."] #[doc = ""] #[doc = " If `origin_expr` is `true`, then this is the span of the scrutinee as in:"] #[doc = ""] #[doc = " - `match scrutinee { ... }`"] #[doc = " - `let _ = scrutinee;`"] #[doc = ""] #[doc = " This is used to point to add context in type errors."] #[doc = " In the following example, `span` corresponds to the `a + b` expression:"] #[doc = ""] #[doc = " ```text"] #[doc = " error[E0308]: mismatched types"] #[doc = "  --> src/main.rs:L:C"] #[doc = "   |"] #[doc = " L |    let temp: usize = match a + b {"] #[doc = "   |                            ----- this expression has type `usize`"] #[doc = " L |         Ok(num) => num,"] #[doc = "   |         ^^^^^^^ expected `usize`, found enum `std::result::Result`"] #[doc = "   |"] #[doc = "   = note: expected type `usize`"] #[doc = "              found type `std::result::Result<_, _>`"] #[doc = " ```"] span : Option < Span > , #[doc = " The [`HirId`] of the top-level pattern."] hir_id : HirId , }}}
mkitem!{mkstruct!{#[derive (Copy , Clone)] struct PatInfo < 'tcx > { binding_mode : ByRef , max_ref_mutbl : MutblCap , top_info : TopInfo < 'tcx > , decl_origin : Option < DeclOrigin < 'tcx > > , #[doc = " The depth of current pattern"] current_depth : u32 , }}}
mkitem!{mkimpl!{impl < 'a , 'tcx > FnCtxt < 'a , 'tcx > { fn pattern_cause (& self , ti : & TopInfo < 'tcx > , cause_span : Span) -> ObligationCause < 'tcx > { let origin_expr_info = ti . origin_expr . map (| mut cur_expr | { let mut count = 0 ; while let ExprKind :: AddrOf (.. , inner) = & cur_expr . kind { cur_expr = inner ; count += 1 ; } PatternOriginExpr { peeled_span : cur_expr . span , peeled_count : count , peeled_prefix_suggestion_parentheses : expr_needs_parens (cur_expr) , } }) ; let code = ObligationCauseCode :: Pattern { span : ti . span , root_ty : ti . expected , origin_expr : origin_expr_info , } ; self . cause (cause_span , code) } fn demand_eqtype_pat_diag (& 'a self , cause_span : Span , expected : Ty < 'tcx > , actual : Ty < 'tcx > , ti : & TopInfo < 'tcx > ,) -> Result < () , Diag < 'a > > { self . demand_eqtype_with_origin (& self . pattern_cause (ti , cause_span) , expected , actual) . map_err (| mut diag | { if let Some (expr) = ti . origin_expr { self . suggest_fn_call (& mut diag , expr , expected , | output | { self . can_eq (self . param_env , output , actual) }) ; } diag }) } fn demand_eqtype_pat (& self , cause_span : Span , expected : Ty < 'tcx > , actual : Ty < 'tcx > , ti : & TopInfo < 'tcx > ,) -> Result < () , ErrorGuaranteed > { self . demand_eqtype_pat_diag (cause_span , expected , actual , ti) . map_err (| err | err . emit ()) } }}}
mkitem!{mkenum!{#[doc = " Mode for adjusting the expected type and binding mode."] #[derive (Clone , Copy , Debug , PartialEq , Eq)] enum AdjustMode { #[doc = " Peel off all immediate reference types. If the `deref_patterns` feature is enabled, this"] #[doc = " also peels smart pointer ADTs."] Peel { kind : PeelKind } , #[doc = " Pass on the input binding mode and expected type."] Pass , }}}
mkitem!{mkenum!{#[doc = " Restrictions on what types to peel when adjusting the expected type and binding mode."] #[derive (Clone , Copy , Debug , PartialEq , Eq)] enum PeelKind { #[doc = " Only peel reference types. This is used for explicit `deref!(_)` patterns, which dereference"] #[doc = " any number of `&`/`&mut` references, plus a single smart pointer."] ExplicitDerefPat , #[doc = " Implicitly peel references, and if `deref_patterns` is enabled, smart pointer ADTs."] Implicit { #[doc = " The ADT the pattern is a constructor for, if applicable, so that we don't peel it. See"] #[doc = " [`ResolvedPat`] for more information."] until_adt : Option < DefId > , #[doc = " The number of references at the head of the pattern's type, so we can leave that many"] #[doc = " untouched. This is `1` for string literals, and `0` for most patterns."] pat_ref_layers : usize , } , }}}
mkitem!{mkimpl!{impl AdjustMode { const fn peel_until_adt (opt_adt_def : Option < DefId >) -> AdjustMode { AdjustMode :: Peel { kind : PeelKind :: Implicit { until_adt : opt_adt_def , pat_ref_layers : 0 } } } const fn peel_all () -> AdjustMode { AdjustMode :: peel_until_adt (None) } }}}
mkitem!{mkenum!{#[doc = " `ref mut` bindings (explicit or match-ergonomics) are not allowed behind an `&` reference."] #[doc = " Normally, the borrow checker enforces this, but for (currently experimental) match ergonomics,"] #[doc = " we track this when typing patterns for two purposes:"] #[doc = ""] #[doc = " - For RFC 3627's Rule 3, when this would prevent us from binding with `ref mut`, we limit the"] #[doc = "   default binding mode to be by shared `ref` when it would otherwise be `ref mut`."] #[doc = ""] #[doc = " - For RFC 3627's Rule 5, we allow `&` patterns to match against `&mut` references, treating them"] #[doc = "   as if they were shared references. Since the scrutinee is mutable in this case, the borrow"] #[doc = "   checker won't catch if we bind with `ref mut`, so we need to throw an error ourselves."] #[derive (Clone , Copy , Debug , PartialEq , Eq)] enum MutblCap { #[doc = " Mutability restricted to immutable."] Not , #[doc = " Mutability restricted to immutable, but only because of the pattern"] #[doc = " (not the scrutinee type)."] #[doc = ""] #[doc = " The contained span, if present, points to an `&` pattern"] #[doc = " that is the reason for the restriction,"] #[doc = " and which will be reported in a diagnostic."] WeaklyNot (Option < Span >) , #[doc = " No restriction on mutability"] Mut , }}}
mkitem!{mkimpl!{impl MutblCap { #[must_use] fn cap_to_weakly_not (self , span : Option < Span >) -> Self { match self { MutblCap :: Not => MutblCap :: Not , _ => MutblCap :: WeaklyNot (span) , } } #[must_use] fn as_mutbl (self) -> Mutability { match self { MutblCap :: Not | MutblCap :: WeaklyNot (_) => Mutability :: Not , MutblCap :: Mut => Mutability :: Mut , } } }}}
mkitem!{mkenum!{#[doc = " Variations on RFC 3627's Rule 4: when do reference patterns match against inherited references?"] #[doc = ""] #[doc = " \"Inherited reference\" designates the `&`/`&mut` types that arise from using match ergonomics, i.e."] #[doc = " from matching a reference type with a non-reference pattern. E.g. when `Some(x)` matches on"] #[doc = " `&mut Option<&T>`, `x` gets type `&mut &T` and the outer `&mut` is considered \"inherited\"."] #[derive (Clone , Copy , Debug , PartialEq , Eq)] enum InheritedRefMatchRule { #[doc = " Reference patterns consume only the inherited reference if possible, regardless of whether"] #[doc = " the underlying type being matched against is a reference type. If there is no inherited"] #[doc = " reference, a reference will be consumed from the underlying type."] EatOuter , #[doc = " Reference patterns consume only a reference from the underlying type if possible. If the"] #[doc = " underlying type is not a reference type, the inherited reference will be consumed."] EatInner , #[doc = " When the underlying type is a reference type, reference patterns consume both layers of"] #[doc = " reference, i.e. they both reset the binding mode and consume the reference type."] EatBoth { #[doc = " If `true`, an inherited reference will be considered when determining whether a reference"] #[doc = " pattern matches a given type:"] #[doc = " - If the underlying type is not a reference, a reference pattern may eat the inherited reference;"] #[doc = " - If the underlying type is a reference, a reference pattern matches if it can eat either one"] #[doc = "    of the underlying and inherited references. E.g. a `&mut` pattern is allowed if either the"] #[doc = "    underlying type is `&mut` or the inherited reference is `&mut`."] #[doc = " If `false`, a reference pattern is only matched against the underlying type."] #[doc = " This is `false` for stable Rust and `true` for both the `ref_pat_eat_one_layer_2024` and"] #[doc = " `ref_pat_eat_one_layer_2024_structural` feature gates."] consider_inherited_ref : bool , } , }}}
mkitem!{mkstruct!{#[doc = " When checking patterns containing paths, we need to know the path's resolution to determine"] #[doc = " whether to apply match ergonomics and implicitly dereference the scrutinee. For instance, when"] #[doc = " the `deref_patterns` feature is enabled and we're matching against a scrutinee of type"] #[doc = " `Cow<'a, Option<u8>>`, we insert an implicit dereference to allow the pattern `Some(_)` to type,"] #[doc = " but we must not dereference it when checking the pattern `Cow::Borrowed(_)`."] #[doc = ""] #[doc = " `ResolvedPat` contains the information from resolution needed to determine match ergonomics"] #[doc = " adjustments, and to finish checking the pattern once we know its adjusted type."] #[derive (Clone , Copy , Debug)] struct ResolvedPat < 'tcx > { #[doc = " The type of the pattern, to be checked against the type of the scrutinee after peeling. This"] #[doc = " is also used to avoid peeling the scrutinee's constructors (see the `Cow` example above)."] ty : Ty < 'tcx > , kind : ResolvedPatKind < 'tcx > , }}}
mkitem!{mkenum!{#[derive (Clone , Copy , Debug)] enum ResolvedPatKind < 'tcx > { Path { res : Res , pat_res : Res , segments : & 'tcx [hir :: PathSegment < 'tcx >] } , Struct { variant : & 'tcx VariantDef } , TupleStruct { res : Res , variant : & 'tcx VariantDef } , }}}
mkitem!{mkimpl!{impl < 'tcx > ResolvedPat < 'tcx > { fn adjust_mode (& self) -> AdjustMode { if let ResolvedPatKind :: Path { res , .. } = self . kind && matches ! (res , Res :: Def (DefKind :: Const | DefKind :: AssocConst , _)) { AdjustMode :: Pass } else { AdjustMode :: peel_until_adt (self . ty . ty_adt_def () . map (| adt | adt . did ())) } } }}}
mkitem!{mkimpl!{impl < 'a , 'tcx > FnCtxt < 'a , 'tcx > { #[doc = " Experimental pattern feature: after matching against a shared reference, do we limit the"] #[doc = " default binding mode in subpatterns to be `ref` when it would otherwise be `ref mut`?"] #[doc = " This corresponds to Rule 3 of RFC 3627."] fn downgrade_mut_inside_shared (& self) -> bool { self . tcx . features () . ref_pat_eat_one_layer_2024_structural () } #[doc = " Experimental pattern feature: when do reference patterns match against inherited references?"] #[doc = " This corresponds to variations on Rule 4 of RFC 3627."] fn ref_pat_matches_inherited_ref (& self , edition : Edition) -> InheritedRefMatchRule { if edition . at_least_rust_2024 () { if self . tcx . features () . ref_pat_eat_one_layer_2024 () { InheritedRefMatchRule :: EatOuter } else if self . tcx . features () . ref_pat_eat_one_layer_2024_structural () { InheritedRefMatchRule :: EatInner } else { InheritedRefMatchRule :: EatBoth { consider_inherited_ref : false } } } else { InheritedRefMatchRule :: EatBoth { consider_inherited_ref : self . tcx . features () . ref_pat_eat_one_layer_2024 () || self . tcx . features () . ref_pat_eat_one_layer_2024_structural () , } } } #[doc = " Experimental pattern feature: do `&` patterns match against `&mut` references, treating them"] #[doc = " as if they were shared references? This corresponds to Rule 5 of RFC 3627."] fn ref_pat_matches_mut_ref (& self) -> bool { self . tcx . features () . ref_pat_eat_one_layer_2024 () || self . tcx . features () . ref_pat_eat_one_layer_2024_structural () } #[doc = " Type check the given top level pattern against the `expected` type."] #[doc = ""] #[doc = " If a `Some(span)` is provided and `origin_expr` holds,"] #[doc = " then the `span` represents the scrutinee's span."] #[doc = " The scrutinee is found in e.g. `match scrutinee { ... }` and `let pat = scrutinee;`."] #[doc = ""] #[doc = " Otherwise, `Some(span)` represents the span of a type expression"] #[doc = " which originated the `expected` type."] pub (crate) fn check_pat_top (& self , pat : & 'tcx Pat < 'tcx > , expected : Ty < 'tcx > , span : Option < Span > , origin_expr : Option < & 'tcx hir :: Expr < 'tcx > > , decl_origin : Option < DeclOrigin < 'tcx > > ,) { let top_info = TopInfo { expected , origin_expr , span , hir_id : pat . hir_id } ; let pat_info = PatInfo { binding_mode : ByRef :: No , max_ref_mutbl : MutblCap :: Mut , top_info , decl_origin , current_depth : 0 , } ; self . check_pat (pat , expected , pat_info) ; } #[doc = " Type check the given `pat` against the `expected` type"] #[doc = " with the provided `binding_mode` (default binding mode)."] #[doc = ""] #[doc = " Outside of this module, `check_pat_top` should always be used."] #[doc = " Conversely, inside this module, `check_pat_top` should never be used."] #[instrument (level = "debug" , skip (self , pat_info))] fn check_pat (& self , pat : & 'tcx Pat < 'tcx > , expected : Ty < 'tcx > , pat_info : PatInfo < 'tcx >) { let opt_path_res = match pat . kind { PatKind :: Expr (PatExpr { kind : PatExprKind :: Path (qpath) , hir_id , span }) => { Some (self . resolve_pat_path (* hir_id , * span , qpath)) } PatKind :: Struct (ref qpath , ..) => Some (self . resolve_pat_struct (pat , qpath)) , PatKind :: TupleStruct (ref qpath , ..) => Some (self . resolve_pat_tuple_struct (pat , qpath)) , _ => None , } ; let adjust_mode = self . calc_adjust_mode (pat , opt_path_res) ; let ty = self . check_pat_inner (pat , opt_path_res , adjust_mode , expected , pat_info) ; self . write_ty (pat . hir_id , ty) ; if let Some (derefed_tys) = self . typeck_results . borrow () . pat_adjustments () . get (pat . hir_id) && derefed_tys . iter () . any (| adjust | adjust . kind == PatAdjust :: OverloadedDeref) { self . register_deref_mut_bounds_if_needed (pat . span , pat , derefed_tys . iter () . filter_map (| adjust | match adjust . kind { PatAdjust :: OverloadedDeref => Some (adjust . source) , PatAdjust :: BuiltinDeref => None , }) ,) ; } } fn check_pat_inner (& self , pat : & 'tcx Pat < 'tcx > , opt_path_res : Option < Result < ResolvedPat < 'tcx > , ErrorGuaranteed > > , adjust_mode : AdjustMode , expected : Ty < 'tcx > , pat_info : PatInfo < 'tcx > ,) -> Ty < 'tcx > { #[cfg (debug_assertions)] if pat_info . binding_mode == ByRef :: Yes (Mutability :: Mut) && pat_info . max_ref_mutbl != MutblCap :: Mut && self . downgrade_mut_inside_shared () { span_bug ! (pat . span , "Pattern mutability cap violated!") ; } let expected = if let AdjustMode :: Peel { .. } = adjust_mode && pat . default_binding_modes { self . try_structurally_resolve_type (pat . span , expected) } else { expected } ; let old_pat_info = pat_info ; let pat_info = PatInfo { current_depth : old_pat_info . current_depth + 1 , .. old_pat_info } ; match pat . kind { _ if let AdjustMode :: Peel { kind : peel_kind } = adjust_mode && pat . default_binding_modes && let ty :: Ref (_ , inner_ty , inner_mutability) = * expected . kind () && self . should_peel_ref (peel_kind , expected) => { debug ! ("inspecting {:?}" , expected) ; debug ! ("current discriminant is Ref, inserting implicit deref") ; self . typeck_results . borrow_mut () . pat_adjustments_mut () . entry (pat . hir_id) . or_default () . push (PatAdjustment { kind : PatAdjust :: BuiltinDeref , source : expected }) ; let mut binding_mode = ByRef :: Yes (match pat_info . binding_mode { ByRef :: No | ByRef :: Yes (Mutability :: Mut) => inner_mutability , ByRef :: Yes (Mutability :: Not) => Mutability :: Not , }) ; let mut max_ref_mutbl = pat_info . max_ref_mutbl ; if self . downgrade_mut_inside_shared () { binding_mode = binding_mode . cap_ref_mutability (max_ref_mutbl . as_mutbl ()) ; } if binding_mode == ByRef :: Yes (Mutability :: Not) { max_ref_mutbl = MutblCap :: Not ; } debug ! ("default binding mode is now {:?}" , binding_mode) ; let new_pat_info = PatInfo { binding_mode , max_ref_mutbl , .. old_pat_info } ; self . check_pat_inner (pat , opt_path_res , adjust_mode , inner_ty , new_pat_info) } _ if self . tcx . features () . deref_patterns () && let AdjustMode :: Peel { kind : peel_kind } = adjust_mode && pat . default_binding_modes && self . should_peel_smart_pointer (peel_kind , expected) => { debug ! ("scrutinee ty {expected:?} is a smart pointer, inserting overloaded deref") ; let mut inner_ty = self . deref_pat_target (pat . span , expected) ; let mut typeck_results = self . typeck_results . borrow_mut () ; let mut pat_adjustments_table = typeck_results . pat_adjustments_mut () ; let pat_adjustments = pat_adjustments_table . entry (pat . hir_id) . or_default () ; if self . tcx . recursion_limit () . value_within_limit (pat_adjustments . len ()) { pat_adjustments . push (PatAdjustment { kind : PatAdjust :: OverloadedDeref , source : expected }) ; } else { let guar = report_autoderef_recursion_limit_error (self . tcx , pat . span , expected) ; inner_ty = Ty :: new_error (self . tcx , guar) ; } drop (typeck_results) ; self . check_pat_inner (pat , opt_path_res , adjust_mode , inner_ty , old_pat_info) } PatKind :: Missing | PatKind :: Wild | PatKind :: Err (_) => expected , PatKind :: Never => expected , PatKind :: Expr (PatExpr { kind : PatExprKind :: Path (_) , hir_id , .. }) => { let ty = match opt_path_res . unwrap () { Ok (ref pr) => { self . check_pat_path (pat . hir_id , pat . span , pr , expected , & pat_info . top_info) } Err (guar) => Ty :: new_error (self . tcx , guar) , } ; self . write_ty (* hir_id , ty) ; ty } PatKind :: Expr (lt) => self . check_pat_lit (pat . span , lt , expected , & pat_info . top_info) , PatKind :: Range (lhs , rhs , _) => { self . check_pat_range (pat . span , lhs , rhs , expected , & pat_info . top_info) } PatKind :: Binding (ba , var_id , ident , sub) => { self . check_pat_ident (pat , ba , var_id , ident , sub , expected , pat_info) } PatKind :: TupleStruct (ref qpath , subpats , ddpos) => match opt_path_res . unwrap () { Ok (ResolvedPat { ty , kind : ResolvedPatKind :: TupleStruct { res , variant } }) => self . check_pat_tuple_struct (pat , qpath , subpats , ddpos , res , ty , variant , expected , pat_info ,) , Err (guar) => { let ty_err = Ty :: new_error (self . tcx , guar) ; for subpat in subpats { self . check_pat (subpat , ty_err , pat_info) ; } ty_err } Ok (pr) => span_bug ! (pat . span , "tuple struct pattern resolved to {pr:?}") , } , PatKind :: Struct (_ , fields , has_rest_pat) => match opt_path_res . unwrap () { Ok (ResolvedPat { ty , kind : ResolvedPatKind :: Struct { variant } }) => self . check_pat_struct (pat , fields , has_rest_pat . is_some () , ty , variant , expected , pat_info ,) , Err (guar) => { let ty_err = Ty :: new_error (self . tcx , guar) ; for field in fields { self . check_pat (field . pat , ty_err , pat_info) ; } ty_err } Ok (pr) => span_bug ! (pat . span , "struct pattern resolved to {pr:?}") , } , PatKind :: Guard (pat , cond) => { self . check_pat (pat , expected , pat_info) ; self . check_expr_has_type_or_error (cond , self . tcx . types . bool , | _ | { }) ; expected } PatKind :: Or (pats) => { for pat in pats { self . check_pat (pat , expected , pat_info) ; } expected } PatKind :: Tuple (elements , ddpos) => { self . check_pat_tuple (pat . span , elements , ddpos , expected , pat_info) } PatKind :: Box (inner) => self . check_pat_box (pat . span , inner , expected , pat_info) , PatKind :: Deref (inner) => self . check_pat_deref (pat . span , inner , expected , pat_info) , PatKind :: Ref (inner , mutbl) => self . check_pat_ref (pat , inner , mutbl , expected , pat_info) , PatKind :: Slice (before , slice , after) => { self . check_pat_slice (pat . span , before , slice , after , expected , pat_info) } } } #[doc = " How should the binding mode and expected type be adjusted?"] #[doc = ""] #[doc = " When the pattern contains a path, `opt_path_res` must be `Some(path_res)`."] fn calc_adjust_mode (& self , pat : & 'tcx Pat < 'tcx > , opt_path_res : Option < Result < ResolvedPat < 'tcx > , ErrorGuaranteed > > ,) -> AdjustMode { match & pat . kind { PatKind :: Tuple (..) | PatKind :: Range (..) | PatKind :: Slice (..) => AdjustMode :: peel_all () , PatKind :: Box (_) | PatKind :: Deref (_) => { AdjustMode :: Peel { kind : PeelKind :: ExplicitDerefPat } } PatKind :: Never => AdjustMode :: peel_all () , PatKind :: Struct (..) | PatKind :: TupleStruct (..) | PatKind :: Expr (PatExpr { kind : PatExprKind :: Path (_) , .. }) => { opt_path_res . unwrap () . map_or (AdjustMode :: peel_all () , | pr | pr . adjust_mode ()) } PatKind :: Expr (lt) => { if cfg ! (debug_assertions) && self . tcx . features () . deref_patterns () && ! matches ! (lt . kind , PatExprKind :: Lit { .. }) { span_bug ! (lt . span , "FIXME(deref_patterns): adjust mode unimplemented for {:?}" , lt . kind) ; } let lit_ty = self . resolve_vars_if_possible (self . check_pat_expr_unadjusted (lt)) ; if self . tcx . features () . deref_patterns () { let mut peeled_ty = lit_ty ; let mut pat_ref_layers = 0 ; while let ty :: Ref (_ , inner_ty , mutbl) = * self . try_structurally_resolve_type (pat . span , peeled_ty) . kind () { debug_assert ! (mutbl . is_not ()) ; pat_ref_layers += 1 ; peeled_ty = inner_ty ; } AdjustMode :: Peel { kind : PeelKind :: Implicit { until_adt : None , pat_ref_layers } , } } else { if lit_ty . is_ref () { AdjustMode :: Pass } else { AdjustMode :: peel_all () } } } PatKind :: Ref (..) | PatKind :: Missing | PatKind :: Wild | PatKind :: Err (_) | PatKind :: Binding (..) | PatKind :: Or (_) | PatKind :: Guard (..) => AdjustMode :: Pass , } } #[doc = " Assuming `expected` is a reference type, determine whether to peel it before matching."] fn should_peel_ref (& self , peel_kind : PeelKind , mut expected : Ty < 'tcx >) -> bool { debug_assert ! (expected . is_ref ()) ; let pat_ref_layers = match peel_kind { PeelKind :: ExplicitDerefPat => 0 , PeelKind :: Implicit { pat_ref_layers , .. } => pat_ref_layers , } ; if pat_ref_layers == 0 { return true ; } debug_assert ! (self . tcx . features () . deref_patterns () , "Peeling for patterns with reference types is gated by `deref_patterns`.") ; let mut expected_ref_layers = 0 ; while let ty :: Ref (_ , inner_ty , mutbl) = * expected . kind () { if mutbl . is_mut () { return true ; } expected_ref_layers += 1 ; expected = inner_ty ; } pat_ref_layers < expected_ref_layers || self . should_peel_smart_pointer (peel_kind , expected) } #[doc = " Determine whether `expected` is a smart pointer type that should be peeled before matching."] fn should_peel_smart_pointer (& self , peel_kind : PeelKind , expected : Ty < 'tcx >) -> bool { if let PeelKind :: Implicit { until_adt , .. } = peel_kind && let ty :: Adt (scrutinee_adt , _) = * expected . kind () && until_adt != Some (scrutinee_adt . did ()) && let Some (deref_trait) = self . tcx . lang_items () . deref_trait () && self . type_implements_trait (deref_trait , [expected] , self . param_env) . may_apply () { true } else { false } } fn check_pat_expr_unadjusted (& self , lt : & 'tcx hir :: PatExpr < 'tcx >) -> Ty < 'tcx > { let ty = match & lt . kind { rustc_hir :: PatExprKind :: Lit { lit , negated } => { let ty = self . check_expr_lit (lit , Expectation :: NoExpectation) ; if * negated { self . register_bound (ty , self . tcx . require_lang_item (LangItem :: Neg , lt . span) , ObligationCause :: dummy_with_span (lt . span) ,) ; } ty } rustc_hir :: PatExprKind :: ConstBlock (c) => { self . check_expr_const_block (c , Expectation :: NoExpectation) } rustc_hir :: PatExprKind :: Path (qpath) => { let (res , opt_ty , segments) = self . resolve_ty_and_res_fully_qualified_call (qpath , lt . hir_id , lt . span) ; self . instantiate_value_path (segments , opt_ty , res , lt . span , lt . span , lt . hir_id) . 0 } } ; self . write_ty (lt . hir_id , ty) ; ty } fn check_pat_lit (& self , span : Span , lt : & hir :: PatExpr < 'tcx > , expected : Ty < 'tcx > , ti : & TopInfo < 'tcx > ,) -> Ty < 'tcx > { let ty = self . node_ty (lt . hir_id) ; let mut pat_ty = ty ; if let hir :: PatExprKind :: Lit { lit : Spanned { node : ast :: LitKind :: ByteStr (..) , .. } , .. } = lt . kind { let tcx = self . tcx ; let expected = self . structurally_resolve_type (span , expected) ; match * expected . kind () { ty :: Ref (_ , inner_ty , _) if self . try_structurally_resolve_type (span , inner_ty) . is_slice () => { trace ! (? lt . hir_id . local_id , "polymorphic byte string lit") ; pat_ty = Ty :: new_imm_ref (tcx , tcx . lifetimes . re_static , Ty :: new_slice (tcx , tcx . types . u8) ,) ; } ty :: Array (..) if tcx . features () . deref_patterns () => { pat_ty = match * ty . kind () { ty :: Ref (_ , inner_ty , _) => inner_ty , _ => span_bug ! (span , "found byte string literal with non-ref type {ty:?}") , } } ty :: Slice (..) if tcx . features () . deref_patterns () => { pat_ty = Ty :: new_slice (tcx , tcx . types . u8) ; } _ => { } } } if self . tcx . features () . deref_patterns () && let hir :: PatExprKind :: Lit { lit : Spanned { node : ast :: LitKind :: Str (..) , .. } , .. } = lt . kind && self . try_structurally_resolve_type (span , expected) . is_str () { pat_ty = self . tcx . types . str_ ; } if self . tcx . features () . string_deref_patterns () && let hir :: PatExprKind :: Lit { lit : Spanned { node : ast :: LitKind :: Str (..) , .. } , .. } = lt . kind { let tcx = self . tcx ; let expected = self . resolve_vars_if_possible (expected) ; pat_ty = match expected . kind () { ty :: Adt (def , _) if tcx . is_lang_item (def . did () , LangItem :: String) => expected , ty :: Str => Ty :: new_static_str (tcx) , _ => pat_ty , } ; } let cause = self . pattern_cause (ti , span) ; if let Err (err) = self . demand_suptype_with_origin (& cause , expected , pat_ty) { err . emit () ; } pat_ty } fn check_pat_range (& self , span : Span , lhs : Option < & 'tcx hir :: PatExpr < 'tcx > > , rhs : Option < & 'tcx hir :: PatExpr < 'tcx > > , expected : Ty < 'tcx > , ti : & TopInfo < 'tcx > ,) -> Ty < 'tcx > { let calc_side = | opt_expr : Option < & 'tcx hir :: PatExpr < 'tcx > > | match opt_expr { None => None , Some (expr) => { let ty = self . check_pat_expr_unadjusted (expr) ; let ty = self . try_structurally_resolve_type (expr . span , ty) ; let fail = ! (ty . is_numeric () || ty . is_char () || ty . is_ty_var () || ty . references_error ()) ; Some ((fail , ty , expr . span)) } } ; let mut lhs = calc_side (lhs) ; let mut rhs = calc_side (rhs) ; if let (Some ((true , ..)) , _) | (_ , Some ((true , ..))) = (lhs , rhs) { let guar = self . emit_err_pat_range (span , lhs , rhs) ; return Ty :: new_error (self . tcx , guar) ; } let demand_eqtype = | x : & mut _ , y | { if let Some ((ref mut fail , x_ty , x_span)) = * x && let Err (mut err) = self . demand_eqtype_pat_diag (x_span , expected , x_ty , ti) { if let Some ((_ , y_ty , y_span)) = y { self . endpoint_has_type (& mut err , y_span , y_ty) ; } err . emit () ; * fail = true ; } } ; demand_eqtype (& mut lhs , rhs) ; demand_eqtype (& mut rhs , lhs) ; if let (Some ((true , ..)) , _) | (_ , Some ((true , ..))) = (lhs , rhs) { return Ty :: new_misc_error (self . tcx) ; } let ty = self . structurally_resolve_type (span , expected) ; if ! (ty . is_numeric () || ty . is_char () || ty . references_error ()) { if let Some ((ref mut fail , _ , _)) = lhs { * fail = true ; } if let Some ((ref mut fail , _ , _)) = rhs { * fail = true ; } let guar = self . emit_err_pat_range (span , lhs , rhs) ; return Ty :: new_error (self . tcx , guar) ; } ty } fn endpoint_has_type (& self , err : & mut Diag < '_ > , span : Span , ty : Ty < '_ >) { if ! ty . references_error () { err . span_label (span , format ! ("this is of type `{ty}`")) ; } } fn emit_err_pat_range (& self , span : Span , lhs : Option < (bool , Ty < 'tcx > , Span) > , rhs : Option < (bool , Ty < 'tcx > , Span) > ,) -> ErrorGuaranteed { let span = match (lhs , rhs) { (Some ((true , ..)) , Some ((true , ..))) => span , (Some ((true , _ , sp)) , _) => sp , (_ , Some ((true , _ , sp))) => sp , _ => span_bug ! (span , "emit_err_pat_range: no side failed or exists but still error?") , } ; let mut err = struct_span_code_err ! (self . dcx () , span , E0029 , "only `char` and numeric types are allowed in range patterns") ; let msg = | ty | { let ty = self . resolve_vars_if_possible (ty) ; format ! ("this is of type `{ty}` but it should be `char` or numeric") } ; let mut one_side_err = | first_span , first_ty , second : Option < (bool , Ty < 'tcx > , Span) > | { err . span_label (first_span , msg (first_ty)) ; if let Some ((_ , ty , sp)) = second { let ty = self . resolve_vars_if_possible (ty) ; self . endpoint_has_type (& mut err , sp , ty) ; } } ; match (lhs , rhs) { (Some ((true , lhs_ty , lhs_sp)) , Some ((true , rhs_ty , rhs_sp))) => { err . span_label (lhs_sp , msg (lhs_ty)) ; err . span_label (rhs_sp , msg (rhs_ty)) ; } (Some ((true , lhs_ty , lhs_sp)) , rhs) => one_side_err (lhs_sp , lhs_ty , rhs) , (lhs , Some ((true , rhs_ty , rhs_sp))) => one_side_err (rhs_sp , rhs_ty , lhs) , _ => span_bug ! (span , "Impossible, verified above.") , } if (lhs , rhs) . references_error () { err . downgrade_to_delayed_bug () ; } if self . tcx . sess . teach (err . code . unwrap ()) { err . note ("In a match expression, only numbers and characters can be matched \
                    against a range. This is because the compiler checks that the range \
                    is non-empty at compile-time, and is unable to evaluate arbitrary \
                    comparison functions. If you want to capture values of an orderable \
                    type between two end-points, you can use a guard." ,) ; } err . emit () } fn check_pat_ident (& self , pat : & 'tcx Pat < 'tcx > , user_bind_annot : BindingMode , var_id : HirId , ident : Ident , sub : Option < & 'tcx Pat < 'tcx > > , expected : Ty < 'tcx > , pat_info : PatInfo < 'tcx > ,) -> Ty < 'tcx > { let PatInfo { binding_mode : def_br , top_info : ti , .. } = pat_info ; let bm = match user_bind_annot { BindingMode (ByRef :: No , Mutability :: Mut) if let ByRef :: Yes (def_br_mutbl) = def_br => { if pat . span . at_least_rust_2024 () && (self . tcx . features () . ref_pat_eat_one_layer_2024 () || self . tcx . features () . ref_pat_eat_one_layer_2024_structural ()) { if ! self . tcx . features () . mut_ref () { feature_err (& self . tcx . sess , sym :: mut_ref , pat . span . until (ident . span) , "binding cannot be both mutable and by-reference" ,) . emit () ; } BindingMode (def_br , Mutability :: Mut) } else { self . add_rust_2024_migration_desugared_pat (pat_info . top_info . hir_id , pat , 't' , def_br_mutbl ,) ; BindingMode (ByRef :: No , Mutability :: Mut) } } BindingMode (ByRef :: No , mutbl) => BindingMode (def_br , mutbl) , BindingMode (ByRef :: Yes (user_br_mutbl) , _) => { if let ByRef :: Yes (def_br_mutbl) = def_br { self . add_rust_2024_migration_desugared_pat (pat_info . top_info . hir_id , pat , match user_br_mutbl { Mutability :: Not => 'f' , Mutability :: Mut => 't' , } , def_br_mutbl ,) ; } user_bind_annot } } ; if bm . 0 == ByRef :: Yes (Mutability :: Mut) && let MutblCap :: WeaklyNot (and_pat_span) = pat_info . max_ref_mutbl { let mut err = struct_span_code_err ! (self . dcx () , ident . span , E0596 , "cannot borrow as mutable inside an `&` pattern") ; if let Some (span) = and_pat_span { err . span_suggestion (span , "replace this `&` with `&mut`" , "&mut " , Applicability :: MachineApplicable ,) ; } err . emit () ; } self . typeck_results . borrow_mut () . pat_binding_modes_mut () . insert (pat . hir_id , bm) ; debug ! ("check_pat_ident: pat.hir_id={:?} bm={:?}" , pat . hir_id , bm) ; let local_ty = self . local_ty (pat . span , pat . hir_id) ; let eq_ty = match bm . 0 { ByRef :: Yes (mutbl) => { self . new_ref_ty (pat . span , mutbl , expected) } ByRef :: No => expected , } ; let _ = self . demand_eqtype_pat (pat . span , eq_ty , local_ty , & ti) ; if var_id != pat . hir_id { self . check_binding_alt_eq_ty (user_bind_annot , pat . span , var_id , local_ty , & ti) ; } if let Some (p) = sub { self . check_pat (p , expected , pat_info) ; } local_ty } #[doc = " When a variable is bound several times in a `PatKind::Or`, it'll resolve all of the"] #[doc = " subsequent bindings of the same name to the first usage. Verify that all of these"] #[doc = " bindings have the same type by comparing them all against the type of that first pat."] fn check_binding_alt_eq_ty (& self , ba : BindingMode , span : Span , var_id : HirId , ty : Ty < 'tcx > , ti : & TopInfo < 'tcx > ,) { let var_ty = self . local_ty (span , var_id) ; if let Err (mut err) = self . demand_eqtype_pat_diag (span , var_ty , ty , ti) { let var_ty = self . resolve_vars_if_possible (var_ty) ; let msg = format ! ("first introduced with type `{var_ty}` here") ; err . span_label (self . tcx . hir_span (var_id) , msg) ; let in_match = self . tcx . hir_parent_iter (var_id) . any (| (_ , n) | { matches ! (n , hir :: Node :: Expr (hir :: Expr { kind : hir :: ExprKind :: Match (.., hir :: MatchSource :: Normal) , .. })) }) ; let pre = if in_match { "in the same arm, " } else { "" } ; err . note (format ! ("{pre}a binding must have the same type in all alternatives")) ; self . suggest_adding_missing_ref_or_removing_ref (& mut err , span , var_ty , self . resolve_vars_if_possible (ty) , ba ,) ; err . emit () ; } } fn suggest_adding_missing_ref_or_removing_ref (& self , err : & mut Diag < '_ > , span : Span , expected : Ty < 'tcx > , actual : Ty < 'tcx > , ba : BindingMode ,) { match (expected . kind () , actual . kind () , ba) { (ty :: Ref (_ , inner_ty , _) , _ , BindingMode :: NONE) if self . can_eq (self . param_env , * inner_ty , actual) => { err . span_suggestion_verbose (span . shrink_to_lo () , "consider adding `ref`" , "ref " , Applicability :: MaybeIncorrect ,) ; } (_ , ty :: Ref (_ , inner_ty , _) , BindingMode :: REF) if self . can_eq (self . param_env , expected , * inner_ty) => { err . span_suggestion_verbose (span . with_hi (span . lo () + BytePos (4)) , "consider removing `ref`" , "" , Applicability :: MaybeIncorrect ,) ; } _ => () , } } #[doc = " Precondition: pat is a `Ref(_)` pattern"] fn borrow_pat_suggestion (& self , err : & mut Diag < '_ > , pat : & Pat < '_ >) { let tcx = self . tcx ; if let PatKind :: Ref (inner , mutbl) = pat . kind && let PatKind :: Binding (_ , _ , binding , ..) = inner . kind { let binding_parent = tcx . parent_hir_node (pat . hir_id) ; debug ! (? inner , ? pat , ? binding_parent) ; let mutability = match mutbl { ast :: Mutability :: Mut => "mut" , ast :: Mutability :: Not => "" , } ; let mut_var_suggestion = 'block : { if mutbl . is_not () { break 'block None ; } let ident_kind = match binding_parent { hir :: Node :: Param (_) => "parameter" , hir :: Node :: LetStmt (_) => "variable" , hir :: Node :: Arm (_) => "binding" , hir :: Node :: Pat (Pat { kind , .. }) => match kind { PatKind :: Struct (..) | PatKind :: TupleStruct (..) | PatKind :: Or (..) | PatKind :: Guard (..) | PatKind :: Tuple (..) | PatKind :: Slice (..) => "binding" , PatKind :: Missing | PatKind :: Wild | PatKind :: Never | PatKind :: Binding (..) | PatKind :: Box (..) | PatKind :: Deref (_) | PatKind :: Ref (..) | PatKind :: Expr (..) | PatKind :: Range (..) | PatKind :: Err (_) => break 'block None , } , _ => break 'block None , } ; Some ((pat . span , format ! ("to declare a mutable {ident_kind} use") , format ! ("mut {binding}") ,)) } ; match binding_parent { hir :: Node :: Param (hir :: Param { ty_span , pat , .. }) if pat . span != * ty_span => { err . multipart_suggestion_verbose (format ! ("to take parameter `{binding}` by reference, move `&{mutability}` to the type") , vec ! [(pat . span . until (inner . span) , "" . to_owned ()) , (ty_span . shrink_to_lo () , mutbl . ref_prefix_str () . to_owned ()) ,] , Applicability :: MachineApplicable) ; if let Some ((sp , msg , sugg)) = mut_var_suggestion { err . span_note (sp , format ! ("{msg}: `{sugg}`")) ; } } hir :: Node :: Pat (pt) if let PatKind :: TupleStruct (_ , pat_arr , _) = pt . kind => { for i in pat_arr . iter () { if let PatKind :: Ref (the_ref , _) = i . kind && let PatKind :: Binding (mt , _ , ident , _) = the_ref . kind { let BindingMode (_ , mtblty) = mt ; err . span_suggestion_verbose (i . span , format ! ("consider removing `&{mutability}` from the pattern") , mtblty . prefix_str () . to_string () + & ident . name . to_string () , Applicability :: MaybeIncorrect ,) ; } } if let Some ((sp , msg , sugg)) = mut_var_suggestion { err . span_note (sp , format ! ("{msg}: `{sugg}`")) ; } } hir :: Node :: Param (_) | hir :: Node :: Arm (_) | hir :: Node :: Pat (_) => { err . span_suggestion_verbose (pat . span . until (inner . span) , format ! ("consider removing `&{mutability}` from the pattern") , "" , Applicability :: MaybeIncorrect ,) ; if let Some ((sp , msg , sugg)) = mut_var_suggestion { err . span_note (sp , format ! ("{msg}: `{sugg}`")) ; } } _ if let Some ((sp , msg , sugg)) = mut_var_suggestion => { err . span_suggestion (sp , msg , sugg , Applicability :: MachineApplicable) ; } _ => { } } } } fn check_dereferenceable (& self , span : Span , expected : Ty < 'tcx > , inner : & Pat < '_ > ,) -> Result < () , ErrorGuaranteed > { if let PatKind :: Binding (..) = inner . kind && let Some (pointee_ty) = self . shallow_resolve (expected) . builtin_deref (true) && let ty :: Dynamic (..) = pointee_ty . kind () { let type_str = self . ty_to_string (expected) ; let mut err = struct_span_code_err ! (self . dcx () , span , E0033 , "type `{}` cannot be dereferenced" , type_str) ; err . span_label (span , format ! ("type `{type_str}` cannot be dereferenced")) ; if self . tcx . sess . teach (err . code . unwrap ()) { err . note (CANNOT_IMPLICITLY_DEREF_POINTER_TRAIT_OBJ) ; } return Err (err . emit ()) ; } Ok (()) } fn resolve_pat_struct (& self , pat : & 'tcx Pat < 'tcx > , qpath : & hir :: QPath < 'tcx > ,) -> Result < ResolvedPat < 'tcx > , ErrorGuaranteed > { let (variant , pat_ty) = self . check_struct_path (qpath , pat . hir_id) ? ; Ok (ResolvedPat { ty : pat_ty , kind : ResolvedPatKind :: Struct { variant } }) } fn check_pat_struct (& self , pat : & 'tcx Pat < 'tcx > , fields : & 'tcx [hir :: PatField < 'tcx >] , has_rest_pat : bool , pat_ty : Ty < 'tcx > , variant : & 'tcx VariantDef , expected : Ty < 'tcx > , pat_info : PatInfo < 'tcx > ,) -> Ty < 'tcx > { let _ = self . demand_eqtype_pat (pat . span , expected , pat_ty , & pat_info . top_info) ; match self . check_struct_pat_fields (pat_ty , pat , variant , fields , has_rest_pat , pat_info) { Ok (()) => pat_ty , Err (guar) => Ty :: new_error (self . tcx , guar) , } } fn resolve_pat_path (& self , path_id : HirId , span : Span , qpath : & 'tcx hir :: QPath < '_ > ,) -> Result < ResolvedPat < 'tcx > , ErrorGuaranteed > { let tcx = self . tcx ; let (res , opt_ty , segments) = self . resolve_ty_and_res_fully_qualified_call (qpath , path_id , span) ; match res { Res :: Err => { let e = self . dcx () . span_delayed_bug (qpath . span () , "`Res::Err` but no error emitted") ; self . set_tainted_by_errors (e) ; return Err (e) ; } Res :: Def (DefKind :: AssocFn | DefKind :: Ctor (_ , CtorKind :: Fn) | DefKind :: Variant , _) => { let expected = "unit struct, unit variant or constant" ; let e = report_unexpected_variant_res (tcx , res , None , qpath , span , E0533 , expected) ; return Err (e) ; } Res :: SelfCtor (def_id) => { if let ty :: Adt (adt_def , _) = * tcx . type_of (def_id) . skip_binder () . kind () && adt_def . is_struct () && let Some ((CtorKind :: Const , _)) = adt_def . non_enum_variant () . ctor { } else { let e = report_unexpected_variant_res (tcx , res , None , qpath , span , E0533 , "unit struct" ,) ; return Err (e) ; } } Res :: Def (DefKind :: Ctor (_ , CtorKind :: Const) | DefKind :: Const | DefKind :: AssocConst | DefKind :: ConstParam , _ ,) => { } _ => bug ! ("unexpected pattern resolution: {:?}" , res) , } let (pat_ty , pat_res) = self . instantiate_value_path (segments , opt_ty , res , span , span , path_id) ; Ok (ResolvedPat { ty : pat_ty , kind : ResolvedPatKind :: Path { res , pat_res , segments } }) } fn check_pat_path (& self , pat_id_for_diag : HirId , span : Span , resolved : & ResolvedPat < 'tcx > , expected : Ty < 'tcx > , ti : & TopInfo < 'tcx > ,) -> Ty < 'tcx > { if let Err (err) = self . demand_suptype_with_origin (& self . pattern_cause (ti , span) , expected , resolved . ty) { self . emit_bad_pat_path (err , pat_id_for_diag , span , resolved) ; } resolved . ty } fn maybe_suggest_range_literal (& self , e : & mut Diag < '_ > , opt_def_id : Option < hir :: def_id :: DefId > , ident : Ident ,) -> bool { match opt_def_id { Some (def_id) => match self . tcx . hir_get_if_local (def_id) { Some (hir :: Node :: Item (hir :: Item { kind : hir :: ItemKind :: Const (_ , _ , _ , body_id) , .. })) => match self . tcx . hir_node (body_id . hir_id) { hir :: Node :: Expr (expr) => { if hir :: is_range_literal (expr) { let span = self . tcx . hir_span (body_id . hir_id) ; if let Ok (snip) = self . tcx . sess . source_map () . span_to_snippet (span) { e . span_suggestion_verbose (ident . span , "you may want to move the range into the match block" , snip , Applicability :: MachineApplicable ,) ; return true ; } } } _ => () , } , _ => () , } , _ => () , } false } fn emit_bad_pat_path (& self , mut e : Diag < '_ > , hir_id : HirId , pat_span : Span , resolved_pat : & ResolvedPat < 'tcx > ,) { let ResolvedPatKind :: Path { res , pat_res , segments } = resolved_pat . kind else { span_bug ! (pat_span , "unexpected resolution for path pattern: {resolved_pat:?}") ; } ; if let Some (span) = self . tcx . hir_res_span (pat_res) { e . span_label (span , format ! ("{} defined here" , res . descr ())) ; if let [hir :: PathSegment { ident , .. }] = & * segments { e . span_label (pat_span , format ! ("`{}` is interpreted as {} {}, not a new binding" , ident , res . article () , res . descr () ,) ,) ; match self . tcx . parent_hir_node (hir_id) { hir :: Node :: PatField (..) => { e . span_suggestion_verbose (ident . span . shrink_to_hi () , "bind the struct field to a different name instead" , format ! (": other_{}" , ident . as_str () . to_lowercase ()) , Applicability :: HasPlaceholders ,) ; } _ => { let (type_def_id , item_def_id) = match resolved_pat . ty . kind () { ty :: Adt (def , _) => match res { Res :: Def (DefKind :: Const , def_id) => (Some (def . did ()) , Some (def_id)) , _ => (None , None) , } , _ => (None , None) , } ; let is_range = match type_def_id . and_then (| id | self . tcx . as_lang_item (id)) { Some (LangItem :: Range | LangItem :: RangeFrom | LangItem :: RangeTo | LangItem :: RangeFull | LangItem :: RangeInclusiveStruct | LangItem :: RangeToInclusive ,) => true , _ => false , } ; if is_range { if ! self . maybe_suggest_range_literal (& mut e , item_def_id , * ident) { let msg = "constants only support matching by type, \
                                    if you meant to match against a range of values, \
                                    consider using a range pattern like `min ..= max` in the match block" ; e . note (msg) ; } } else { let msg = "introduce a new binding instead" ; let sugg = format ! ("other_{}" , ident . as_str () . to_lowercase ()) ; e . span_suggestion (ident . span , msg , sugg , Applicability :: HasPlaceholders ,) ; } } } ; } } e . emit () ; } fn resolve_pat_tuple_struct (& self , pat : & 'tcx Pat < 'tcx > , qpath : & 'tcx hir :: QPath < 'tcx > ,) -> Result < ResolvedPat < 'tcx > , ErrorGuaranteed > { let tcx = self . tcx ; let report_unexpected_res = | res : Res | { let expected = "tuple struct or tuple variant" ; let e = report_unexpected_variant_res (tcx , res , None , qpath , pat . span , E0164 , expected) ; Err (e) } ; let (res , opt_ty , segments) = self . resolve_ty_and_res_fully_qualified_call (qpath , pat . hir_id , pat . span) ; if res == Res :: Err { let e = self . dcx () . span_delayed_bug (pat . span , "`Res::Err` but no error emitted") ; self . set_tainted_by_errors (e) ; return Err (e) ; } let (pat_ty , res) = self . instantiate_value_path (segments , opt_ty , res , pat . span , pat . span , pat . hir_id) ; if ! pat_ty . is_fn () { return report_unexpected_res (res) ; } let variant = match res { Res :: Err => { self . dcx () . span_bug (pat . span , "`Res::Err` but no error emitted") ; } Res :: Def (DefKind :: AssocConst | DefKind :: AssocFn , _) => { return report_unexpected_res (res) ; } Res :: Def (DefKind :: Ctor (_ , CtorKind :: Fn) , _) => tcx . expect_variant_res (res) , _ => bug ! ("unexpected pattern resolution: {:?}" , res) , } ; let pat_ty = pat_ty . fn_sig (tcx) . output () ; let pat_ty = pat_ty . no_bound_vars () . expect ("expected fn type") ; Ok (ResolvedPat { ty : pat_ty , kind : ResolvedPatKind :: TupleStruct { res , variant } }) } fn check_pat_tuple_struct (& self , pat : & 'tcx Pat < 'tcx > , qpath : & 'tcx hir :: QPath < 'tcx > , subpats : & 'tcx [Pat < 'tcx >] , ddpos : hir :: DotDotPos , res : Res , pat_ty : Ty < 'tcx > , variant : & 'tcx VariantDef , expected : Ty < 'tcx > , pat_info : PatInfo < 'tcx > ,) -> Ty < 'tcx > { let tcx = self . tcx ; let on_error = | e | { for pat in subpats { self . check_pat (pat , Ty :: new_error (tcx , e) , pat_info) ; } } ; let diag = self . demand_eqtype_pat_diag (pat . span , expected , pat_ty , & pat_info . top_info) ; let had_err = diag . map_err (| diag | diag . emit ()) ; if subpats . len () == variant . fields . len () || subpats . len () < variant . fields . len () && ddpos . as_opt_usize () . is_some () { let ty :: Adt (_ , args) = pat_ty . kind () else { bug ! ("unexpected pattern type {:?}" , pat_ty) ; } ; for (i , subpat) in subpats . iter () . enumerate_and_adjust (variant . fields . len () , ddpos) { let field = & variant . fields [FieldIdx :: from_usize (i)] ; let field_ty = self . field_ty (subpat . span , field , args) ; self . check_pat (subpat , field_ty , pat_info) ; self . tcx . check_stability (variant . fields [FieldIdx :: from_usize (i)] . did , Some (subpat . hir_id) , subpat . span , None ,) ; } if let Err (e) = had_err { on_error (e) ; return Ty :: new_error (tcx , e) ; } } else { let e = self . emit_err_pat_wrong_number_of_fields (pat . span , res , qpath , subpats , & variant . fields . raw , expected , had_err ,) ; on_error (e) ; return Ty :: new_error (tcx , e) ; } pat_ty } fn emit_err_pat_wrong_number_of_fields (& self , pat_span : Span , res : Res , qpath : & hir :: QPath < '_ > , subpats : & 'tcx [Pat < 'tcx >] , fields : & 'tcx [ty :: FieldDef] , expected : Ty < 'tcx > , had_err : Result < () , ErrorGuaranteed > ,) -> ErrorGuaranteed { let subpats_ending = pluralize ! (subpats . len ()) ; let fields_ending = pluralize ! (fields . len ()) ; let subpat_spans = if subpats . is_empty () { vec ! [pat_span] } else { subpats . iter () . map (| p | p . span) . collect () } ; let last_subpat_span = * subpat_spans . last () . unwrap () ; let res_span = self . tcx . def_span (res . def_id ()) ; let def_ident_span = self . tcx . def_ident_span (res . def_id ()) . unwrap_or (res_span) ; let field_def_spans = if fields . is_empty () { vec ! [res_span] } else { fields . iter () . map (| f | f . ident (self . tcx) . span) . collect () } ; let last_field_def_span = * field_def_spans . last () . unwrap () ; let mut err = struct_span_code_err ! (self . dcx () , MultiSpan :: from_spans (subpat_spans) , E0023 , "this pattern has {} field{}, but the corresponding {} has {} field{}" , subpats . len () , subpats_ending , res . descr () , fields . len () , fields_ending ,) ; err . span_label (last_subpat_span , format ! ("expected {} field{}, found {}" , fields . len () , fields_ending , subpats . len ()) ,) ; if self . tcx . sess . source_map () . is_multiline (qpath . span () . between (last_subpat_span)) { err . span_label (qpath . span () , "") ; } if self . tcx . sess . source_map () . is_multiline (def_ident_span . between (last_field_def_span)) { err . span_label (def_ident_span , format ! ("{} defined here" , res . descr ())) ; } for span in & field_def_spans [.. field_def_spans . len () - 1] { err . span_label (* span , "") ; } err . span_label (last_field_def_span , format ! ("{} has {} field{}" , res . descr () , fields . len () , fields_ending) ,) ; let missing_parentheses = match (expected . kind () , fields , had_err) { (ty :: Adt (_ , args) , [field] , Ok (())) => { let field_ty = self . field_ty (pat_span , field , args) ; match field_ty . kind () { ty :: Tuple (fields) => fields . len () == subpats . len () , _ => false , } } _ => false , } ; if missing_parentheses { let (left , right) = match subpats { [] => (qpath . span () . shrink_to_hi () , pat_span) , [first , ..] => (first . span . shrink_to_lo () , subpats . last () . unwrap () . span) , } ; err . multipart_suggestion ("missing parentheses" , vec ! [(left , "(" . to_string ()) , (right . shrink_to_hi () , ")" . to_string ())] , Applicability :: MachineApplicable ,) ; } else if fields . len () > subpats . len () && pat_span != DUMMY_SP { let after_fields_span = pat_span . with_hi (pat_span . hi () - BytePos (1)) . shrink_to_hi () ; let all_fields_span = match subpats { [] => after_fields_span , [field] => field . span , [first , .. , last] => first . span . to (last . span) , } ; let all_wildcards = subpats . iter () . all (| pat | matches ! (pat . kind , PatKind :: Wild)) ; let first_tail_wildcard = subpats . iter () . enumerate () . fold (None , | acc , (pos , pat) | match (acc , & pat . kind) { (None , PatKind :: Wild) => Some (pos) , (Some (_) , PatKind :: Wild) => acc , _ => None , }) ; let tail_span = match first_tail_wildcard { None => after_fields_span , Some (0) => subpats [0] . span . to (after_fields_span) , Some (pos) => subpats [pos - 1] . span . shrink_to_hi () . to (after_fields_span) , } ; let mut wildcard_sugg = vec ! ["_" ; fields . len () - subpats . len ()] . join (", ") ; if ! subpats . is_empty () { wildcard_sugg = String :: from (", ") + & wildcard_sugg ; } err . span_suggestion_verbose (after_fields_span , "use `_` to explicitly ignore each field" , wildcard_sugg , Applicability :: MaybeIncorrect ,) ; if fields . len () - subpats . len () > 1 || all_wildcards { if subpats . is_empty () || all_wildcards { err . span_suggestion_verbose (all_fields_span , "use `..` to ignore all fields" , ".." , Applicability :: MaybeIncorrect ,) ; } else { err . span_suggestion_verbose (tail_span , "use `..` to ignore the rest of the fields" , ", .." , Applicability :: MaybeIncorrect ,) ; } } } err . emit () } fn check_pat_tuple (& self , span : Span , elements : & 'tcx [Pat < 'tcx >] , ddpos : hir :: DotDotPos , expected : Ty < 'tcx > , pat_info : PatInfo < 'tcx > ,) -> Ty < 'tcx > { let tcx = self . tcx ; let mut expected_len = elements . len () ; if ddpos . as_opt_usize () . is_some () { if let ty :: Tuple (tys) = self . structurally_resolve_type (span , expected) . kind () { expected_len = tys . len () ; } } let max_len = cmp :: max (expected_len , elements . len ()) ; let element_tys_iter = (0 .. max_len) . map (| _ | self . next_ty_var (span)) ; let element_tys = tcx . mk_type_list_from_iter (element_tys_iter) ; let pat_ty = Ty :: new_tup (tcx , element_tys) ; if let Err (reported) = self . demand_eqtype_pat (span , expected , pat_ty , & pat_info . top_info) { let element_tys_iter = (0 .. max_len) . map (| _ | Ty :: new_error (tcx , reported)) ; for (_ , elem) in elements . iter () . enumerate_and_adjust (max_len , ddpos) { self . check_pat (elem , Ty :: new_error (tcx , reported) , pat_info) ; } Ty :: new_tup_from_iter (tcx , element_tys_iter) } else { for (i , elem) in elements . iter () . enumerate_and_adjust (max_len , ddpos) { self . check_pat (elem , element_tys [i] , pat_info) ; } pat_ty } } fn check_struct_pat_fields (& self , adt_ty : Ty < 'tcx > , pat : & 'tcx Pat < 'tcx > , variant : & 'tcx ty :: VariantDef , fields : & 'tcx [hir :: PatField < 'tcx >] , has_rest_pat : bool , pat_info : PatInfo < 'tcx > ,) -> Result < () , ErrorGuaranteed > { let tcx = self . tcx ; let ty :: Adt (adt , args) = adt_ty . kind () else { span_bug ! (pat . span , "struct pattern is not an ADT") ; } ; let field_map = variant . fields . iter_enumerated () . map (| (i , field) | (field . ident (self . tcx) . normalize_to_macros_2_0 () , (i , field))) . collect :: < FxHashMap < _ , _ > > () ; let mut used_fields = FxHashMap :: default () ; let mut result = Ok (()) ; let mut inexistent_fields = vec ! [] ; for field in fields { let span = field . span ; let ident = tcx . adjust_ident (field . ident , variant . def_id) ; let field_ty = match used_fields . entry (ident) { Occupied (occupied) => { let guar = self . error_field_already_bound (span , field . ident , * occupied . get ()) ; result = Err (guar) ; Ty :: new_error (tcx , guar) } Vacant (vacant) => { vacant . insert (span) ; field_map . get (& ident) . map (| (i , f) | { self . write_field_index (field . hir_id , * i) ; self . tcx . check_stability (f . did , Some (field . hir_id) , span , None) ; self . field_ty (span , f , args) }) . unwrap_or_else (| | { inexistent_fields . push (field) ; Ty :: new_misc_error (tcx) }) } } ; self . check_pat (field . pat , field_ty , pat_info) ; } let mut unmentioned_fields = variant . fields . iter () . map (| field | (field , field . ident (self . tcx) . normalize_to_macros_2_0 ())) . filter (| (_ , ident) | ! used_fields . contains_key (ident)) . collect :: < Vec < _ > > () ; let inexistent_fields_err = if ! inexistent_fields . is_empty () && ! inexistent_fields . iter () . any (| field | field . ident . name == kw :: Underscore) { variant . has_errors () ? ; Some (self . error_inexistent_fields (adt . variant_descr () , & inexistent_fields , & mut unmentioned_fields , pat , variant , args ,)) } else { None } ; let non_exhaustive = variant . field_list_has_applicable_non_exhaustive () ; if non_exhaustive && ! has_rest_pat { self . error_foreign_non_exhaustive_spat (pat , adt . variant_descr () , fields . is_empty ()) ; } let mut unmentioned_err = None ; if adt . is_union () { if fields . len () != 1 { self . dcx () . emit_err (errors :: UnionPatMultipleFields { span : pat . span }) ; } if has_rest_pat { self . dcx () . emit_err (errors :: UnionPatDotDot { span : pat . span }) ; } } else if ! unmentioned_fields . is_empty () { let accessible_unmentioned_fields : Vec < _ > = unmentioned_fields . iter () . copied () . filter (| (field , _) | self . is_field_suggestable (field , pat . hir_id , pat . span)) . collect () ; if ! has_rest_pat { if accessible_unmentioned_fields . is_empty () { unmentioned_err = Some (self . error_no_accessible_fields (pat , fields)) ; } else { unmentioned_err = Some (self . error_unmentioned_fields (pat , & accessible_unmentioned_fields , accessible_unmentioned_fields . len () != unmentioned_fields . len () , fields ,)) ; } } else if non_exhaustive && ! accessible_unmentioned_fields . is_empty () { self . lint_non_exhaustive_omitted_patterns (pat , & accessible_unmentioned_fields , adt_ty ,) } } match (inexistent_fields_err , unmentioned_err) { (Some (i) , Some (u)) => { if let Err (e) = self . error_tuple_variant_as_struct_pat (pat , fields , variant) { i . delay_as_bug () ; u . delay_as_bug () ; Err (e) } else { i . emit () ; Err (u . emit ()) } } (None , Some (u)) => { if let Err (e) = self . error_tuple_variant_as_struct_pat (pat , fields , variant) { u . delay_as_bug () ; Err (e) } else { Err (u . emit ()) } } (Some (err) , None) => Err (err . emit ()) , (None , None) => { self . error_tuple_variant_index_shorthand (variant , pat , fields) ? ; result } } } fn error_tuple_variant_index_shorthand (& self , variant : & VariantDef , pat : & '_ Pat < '_ > , fields : & [hir :: PatField < '_ >] ,) -> Result < () , ErrorGuaranteed > { if let (Some (CtorKind :: Fn) , PatKind :: Struct (qpath , field_patterns , ..)) = (variant . ctor_kind () , & pat . kind) { let has_shorthand_field_name = field_patterns . iter () . any (| field | field . is_shorthand) ; if has_shorthand_field_name { let path = rustc_hir_pretty :: qpath_to_string (& self . tcx , qpath) ; let mut err = struct_span_code_err ! (self . dcx () , pat . span , E0769 , "tuple variant `{path}` written as struct variant" ,) ; err . span_suggestion_verbose (qpath . span () . shrink_to_hi () . to (pat . span . shrink_to_hi ()) , "use the tuple variant pattern syntax instead" , format ! ("({})" , self . get_suggested_tuple_struct_pattern (fields , variant)) , Applicability :: MaybeIncorrect ,) ; return Err (err . emit ()) ; } } Ok (()) } fn error_foreign_non_exhaustive_spat (& self , pat : & Pat < '_ > , descr : & str , no_fields : bool) { let sess = self . tcx . sess ; let sm = sess . source_map () ; let sp_brace = sm . end_point (pat . span) ; let sp_comma = sm . end_point (pat . span . with_hi (sp_brace . hi ())) ; let sugg = if no_fields || sp_brace != sp_comma { ".. }" } else { ", .. }" } ; struct_span_code_err ! (self . dcx () , pat . span , E0638 , "`..` required with {descr} marked as non-exhaustive" ,) . with_span_suggestion_verbose (sp_comma , "add `..` at the end of the field list to ignore all other fields" , sugg , Applicability :: MachineApplicable ,) . emit () ; } fn error_field_already_bound (& self , span : Span , ident : Ident , other_field : Span ,) -> ErrorGuaranteed { struct_span_code_err ! (self . dcx () , span , E0025 , "field `{}` bound multiple times in the pattern" , ident) . with_span_label (span , format ! ("multiple uses of `{ident}` in pattern")) . with_span_label (other_field , format ! ("first use of `{ident}`")) . emit () } fn error_inexistent_fields (& self , kind_name : & str , inexistent_fields : & [& hir :: PatField < 'tcx >] , unmentioned_fields : & mut Vec < (& 'tcx ty :: FieldDef , Ident) > , pat : & 'tcx Pat < 'tcx > , variant : & ty :: VariantDef , args : ty :: GenericArgsRef < 'tcx > ,) -> Diag < 'a > { let tcx = self . tcx ; let (field_names , t , plural) = if let [field] = inexistent_fields { (format ! ("a field named `{}`" , field . ident) , "this" , "") } else { (format ! ("fields named {}" , inexistent_fields . iter () . map (| field | format ! ("`{}`" , field . ident)) . collect ::< Vec < String >> () . join (", ")) , "these" , "s" ,) } ; let spans = inexistent_fields . iter () . map (| field | field . ident . span) . collect :: < Vec < _ > > () ; let mut err = struct_span_code_err ! (self . dcx () , spans , E0026 , "{} `{}` does not have {}" , kind_name , tcx . def_path_str (variant . def_id) , field_names) ; if let Some (pat_field) = inexistent_fields . last () { err . span_label (pat_field . ident . span , format ! ("{} `{}` does not have {} field{}" , kind_name , tcx . def_path_str (variant . def_id) , t , plural) ,) ; if let [(field_def , field)] = unmentioned_fields . as_slice () && self . is_field_suggestable (field_def , pat . hir_id , pat . span) { let suggested_name = find_best_match_for_name (& [field . name] , pat_field . ident . name , None) ; if let Some (suggested_name) = suggested_name { err . span_suggestion (pat_field . ident . span , "a field with a similar name exists" , suggested_name , Applicability :: MaybeIncorrect ,) ; if suggested_name . to_ident_string () . parse :: < usize > () . is_err () { unmentioned_fields . retain (| & (_ , x) | x . name != suggested_name) ; } } else if inexistent_fields . len () == 1 { match pat_field . pat . kind { PatKind :: Expr (_) if ! self . may_coerce (self . typeck_results . borrow () . node_type (pat_field . pat . hir_id) , self . field_ty (field . span , field_def , args) ,) => { } _ => { err . span_suggestion_short (pat_field . ident . span , format ! ("`{}` has a field named `{}`" , tcx . def_path_str (variant . def_id) , field . name ,) , field . name , Applicability :: MaybeIncorrect ,) ; } } } } } if tcx . sess . teach (err . code . unwrap ()) { err . note ("This error indicates that a struct pattern attempted to \
                 extract a nonexistent field from a struct. Struct fields \
                 are identified by the name used before the colon : so struct \
                 patterns should resemble the declaration of the struct type \
                 being matched.\n\n\
                 If you are using shorthand field patterns but want to refer \
                 to the struct field by a different name, you should rename \
                 it explicitly." ,) ; } err } fn error_tuple_variant_as_struct_pat (& self , pat : & Pat < '_ > , fields : & 'tcx [hir :: PatField < 'tcx >] , variant : & ty :: VariantDef ,) -> Result < () , ErrorGuaranteed > { if let (Some (CtorKind :: Fn) , PatKind :: Struct (qpath , pattern_fields , ..)) = (variant . ctor_kind () , & pat . kind) { let is_tuple_struct_match = ! pattern_fields . is_empty () && pattern_fields . iter () . map (| field | field . ident . name . as_str ()) . all (is_number) ; if is_tuple_struct_match { return Ok (()) ; } variant . has_errors () ? ; let path = rustc_hir_pretty :: qpath_to_string (& self . tcx , qpath) ; let mut err = struct_span_code_err ! (self . dcx () , pat . span , E0769 , "tuple variant `{}` written as struct variant" , path) ; let (sugg , appl) = if fields . len () == variant . fields . len () { (self . get_suggested_tuple_struct_pattern (fields , variant) , Applicability :: MachineApplicable ,) } else { (variant . fields . iter () . map (| _ | "_") . collect :: < Vec < & str > > () . join (", ") , Applicability :: MaybeIncorrect ,) } ; err . span_suggestion_verbose (qpath . span () . shrink_to_hi () . to (pat . span . shrink_to_hi ()) , "use the tuple variant pattern syntax instead" , format ! ("({sugg})") , appl ,) ; return Err (err . emit ()) ; } Ok (()) } fn get_suggested_tuple_struct_pattern (& self , fields : & [hir :: PatField < '_ >] , variant : & VariantDef ,) -> String { let variant_field_idents = variant . fields . iter () . map (| f | f . ident (self . tcx)) . collect :: < Vec < Ident > > () ; fields . iter () . map (| field | { match self . tcx . sess . source_map () . span_to_snippet (field . pat . span) { Ok (f) => { if variant_field_idents . contains (& field . ident) { String :: from ("_") } else { f } } Err (_) => rustc_hir_pretty :: pat_to_string (& self . tcx , field . pat) , } }) . collect :: < Vec < String > > () . join (", ") } #[doc = " Returns a diagnostic reporting a struct pattern which is missing an `..` due to"] #[doc = " inaccessible fields."] #[doc = ""] #[doc = " ```text"] #[doc = " error: pattern requires `..` due to inaccessible fields"] #[doc = "   --> src/main.rs:10:9"] #[doc = "    |"] #[doc = " LL |     let foo::Foo {} = foo::Foo::default();"] #[doc = "    |         ^^^^^^^^^^^"] #[doc = "    |"] #[doc = " help: add a `..`"] #[doc = "    |"] #[doc = " LL |     let foo::Foo { .. } = foo::Foo::default();"] #[doc = "    |                  ^^^^^^"] #[doc = " ```"] fn error_no_accessible_fields (& self , pat : & Pat < '_ > , fields : & 'tcx [hir :: PatField < 'tcx >] ,) -> Diag < 'a > { let mut err = self . dcx () . struct_span_err (pat . span , "pattern requires `..` due to inaccessible fields") ; if let Some (field) = fields . last () { err . span_suggestion_verbose (field . span . shrink_to_hi () , "ignore the inaccessible and unused fields" , ", .." , Applicability :: MachineApplicable ,) ; } else { let qpath_span = if let PatKind :: Struct (qpath , ..) = & pat . kind { qpath . span () } else { bug ! ("`error_no_accessible_fields` called on non-struct pattern") ; } ; let span = pat . span . with_lo (qpath_span . shrink_to_hi () . hi ()) ; err . span_suggestion_verbose (span , "ignore the inaccessible and unused fields" , " { .. }" , Applicability :: MachineApplicable ,) ; } err } #[doc = " Report that a pattern for a `#[non_exhaustive]` struct marked with `non_exhaustive_omitted_patterns`"] #[doc = " is not exhaustive enough."] #[doc = ""] #[doc = " Nb: the partner lint for enums lives in `compiler/rustc_mir_build/src/thir/pattern/usefulness.rs`."] fn lint_non_exhaustive_omitted_patterns (& self , pat : & Pat < '_ > , unmentioned_fields : & [(& ty :: FieldDef , Ident)] , ty : Ty < 'tcx > ,) { fn joined_uncovered_patterns (witnesses : & [& Ident]) -> String { const LIMIT : usize = 3 ; match witnesses { [] => { unreachable ! ("expected an uncovered pattern, otherwise why are we emitting an error?") } [witness] => format ! ("`{witness}`") , [head @ .. , tail] if head . len () < LIMIT => { let head : Vec < _ > = head . iter () . map (< _ > :: to_string) . collect () ; format ! ("`{}` and `{}`" , head . join ("`, `") , tail) } _ => { let (head , tail) = witnesses . split_at (LIMIT) ; let head : Vec < _ > = head . iter () . map (< _ > :: to_string) . collect () ; format ! ("`{}` and {} more" , head . join ("`, `") , tail . len ()) } } } let joined_patterns = joined_uncovered_patterns (& unmentioned_fields . iter () . map (| (_ , i) | i) . collect :: < Vec < _ > > () ,) ; self . tcx . node_span_lint (NON_EXHAUSTIVE_OMITTED_PATTERNS , pat . hir_id , pat . span , | lint | { lint . primary_message ("some fields are not explicitly listed") ; lint . span_label (pat . span , format ! ("field{} {} not listed" , rustc_errors :: pluralize ! (unmentioned_fields . len ()) , joined_patterns)) ; lint . help ("ensure that all fields are mentioned explicitly by adding the suggested fields" ,) ; lint . note (format ! ("the pattern is of type `{ty}` and the `non_exhaustive_omitted_patterns` attribute was found" ,)) ; }) ; } #[doc = " Returns a diagnostic reporting a struct pattern which does not mention some fields."] #[doc = ""] #[doc = " ```text"] #[doc = " error[E0027]: pattern does not mention field `bar`"] #[doc = "   --> src/main.rs:15:9"] #[doc = "    |"] #[doc = " LL |     let foo::Foo {} = foo::Foo::new();"] #[doc = "    |         ^^^^^^^^^^^ missing field `bar`"] #[doc = " ```"] fn error_unmentioned_fields (& self , pat : & Pat < '_ > , unmentioned_fields : & [(& ty :: FieldDef , Ident)] , have_inaccessible_fields : bool , fields : & 'tcx [hir :: PatField < 'tcx >] ,) -> Diag < 'a > { let inaccessible = if have_inaccessible_fields { " and inaccessible fields" } else { "" } ; let field_names = if let [(_ , field)] = unmentioned_fields { format ! ("field `{field}`{inaccessible}") } else { let fields = unmentioned_fields . iter () . map (| (_ , name) | format ! ("`{name}`")) . collect :: < Vec < String > > () . join (", ") ; format ! ("fields {fields}{inaccessible}") } ; let mut err = struct_span_code_err ! (self . dcx () , pat . span , E0027 , "pattern does not mention {}" , field_names) ; err . span_label (pat . span , format ! ("missing {field_names}")) ; let len = unmentioned_fields . len () ; let (prefix , postfix , sp) = match fields { [] => match & pat . kind { PatKind :: Struct (path , [] , None) => { (" { " , " }" , path . span () . shrink_to_hi () . until (pat . span . shrink_to_hi ())) } _ => return err , } , [.. , field] => { let tail = field . span . shrink_to_hi () . with_hi (pat . span . hi ()) ; match & pat . kind { PatKind :: Struct (..) => (", " , " }" , tail) , _ => return err , } } } ; err . span_suggestion (sp , format ! ("include the missing field{} in the pattern{}" , pluralize ! (len) , if have_inaccessible_fields { " and ignore the inaccessible fields" } else { "" }) , format ! ("{}{}{}{}" , prefix , unmentioned_fields . iter () . map (| (_ , name) | { let field_name = name . to_string () ; if is_number (& field_name) { format ! ("{field_name}: _") } else { field_name } }) . collect ::< Vec < _ >> () . join (", ") , if have_inaccessible_fields { ", .." } else { "" } , postfix ,) , Applicability :: MachineApplicable ,) ; err . span_suggestion (sp , format ! ("if you don't care about {these} missing field{s}, you can explicitly ignore {them}" , these = pluralize ! ("this" , len) , s = pluralize ! (len) , them = if len == 1 { "it" } else { "them" } ,) , format ! ("{}{}{}{}" , prefix , unmentioned_fields . iter () . map (| (_ , name) | { let field_name = name . to_string () ; format ! ("{field_name}: _") }) . collect ::< Vec < _ >> () . join (", ") , if have_inaccessible_fields { ", .." } else { "" } , postfix ,) , Applicability :: MachineApplicable ,) ; err . span_suggestion (sp , "or always ignore missing fields here" , format ! ("{prefix}..{postfix}") , Applicability :: MachineApplicable ,) ; err } fn check_pat_box (& self , span : Span , inner : & 'tcx Pat < 'tcx > , expected : Ty < 'tcx > , pat_info : PatInfo < 'tcx > ,) -> Ty < 'tcx > { let tcx = self . tcx ; let (box_ty , inner_ty) = self . check_dereferenceable (span , expected , inner) . and_then (| () | { let inner_ty = self . next_ty_var (inner . span) ; let box_ty = Ty :: new_box (tcx , inner_ty) ; self . demand_eqtype_pat (span , expected , box_ty , & pat_info . top_info) ? ; Ok ((box_ty , inner_ty)) }) . unwrap_or_else (| guar | { let err = Ty :: new_error (tcx , guar) ; (err , err) }) ; self . check_pat (inner , inner_ty , pat_info) ; box_ty } fn check_pat_deref (& self , span : Span , inner : & 'tcx Pat < 'tcx > , expected : Ty < 'tcx > , pat_info : PatInfo < 'tcx > ,) -> Ty < 'tcx > { let target_ty = self . deref_pat_target (span , expected) ; self . check_pat (inner , target_ty , pat_info) ; self . register_deref_mut_bounds_if_needed (span , inner , [expected]) ; expected } fn deref_pat_target (& self , span : Span , source_ty : Ty < 'tcx >) -> Ty < 'tcx > { let tcx = self . tcx ; self . register_bound (source_ty , tcx . require_lang_item (hir :: LangItem :: DerefPure , span) , self . misc (span) ,) ; let target_ty = Ty :: new_projection (tcx , tcx . require_lang_item (hir :: LangItem :: DerefTarget , span) , [source_ty] ,) ; let target_ty = self . normalize (span , target_ty) ; self . try_structurally_resolve_type (span , target_ty) } #[doc = " Check if the interior of a deref pattern (either explicit or implicit) has any `ref mut`"] #[doc = " bindings, which would require `DerefMut` to be emitted in MIR building instead of just"] #[doc = " `Deref`. We do this *after* checking the inner pattern, since we want to make sure to"] #[doc = " account for `ref mut` binding modes inherited from implicitly dereferencing `&mut` refs."] fn register_deref_mut_bounds_if_needed (& self , span : Span , inner : & 'tcx Pat < 'tcx > , derefed_tys : impl IntoIterator < Item = Ty < 'tcx > > ,) { if self . typeck_results . borrow () . pat_has_ref_mut_binding (inner) { for mutably_derefed_ty in derefed_tys { self . register_bound (mutably_derefed_ty , self . tcx . require_lang_item (hir :: LangItem :: DerefMut , span) , self . misc (span) ,) ; } } } fn check_pat_ref (& self , pat : & 'tcx Pat < 'tcx > , inner : & 'tcx Pat < 'tcx > , pat_mutbl : Mutability , mut expected : Ty < 'tcx > , mut pat_info : PatInfo < 'tcx > ,) -> Ty < 'tcx > { let tcx = self . tcx ; let pat_prefix_span = inner . span . find_ancestor_inside (pat . span) . map (| end | pat . span . until (end)) ; let ref_pat_matches_mut_ref = self . ref_pat_matches_mut_ref () ; if ref_pat_matches_mut_ref && pat_mutbl == Mutability :: Not { pat_info . max_ref_mutbl = pat_info . max_ref_mutbl . cap_to_weakly_not (pat_prefix_span) ; } expected = self . try_structurally_resolve_type (pat . span , expected) ; if let ByRef :: Yes (inh_mut) = pat_info . binding_mode { match self . ref_pat_matches_inherited_ref (pat . span . edition ()) { InheritedRefMatchRule :: EatOuter => { if pat_mutbl > inh_mut { debug_assert ! (ref_pat_matches_mut_ref) ; self . error_inherited_ref_mutability_mismatch (pat , pat_prefix_span) ; } pat_info . binding_mode = ByRef :: No ; self . typeck_results . borrow_mut () . skipped_ref_pats_mut () . insert (pat . hir_id) ; self . check_pat (inner , expected , pat_info) ; return expected ; } InheritedRefMatchRule :: EatInner => { if let ty :: Ref (_ , _ , r_mutbl) = * expected . kind () && pat_mutbl <= r_mutbl { debug_assert ! (ref_pat_matches_mut_ref) ; debug_assert ! (self . downgrade_mut_inside_shared ()) ; let mutbl_cap = cmp :: min (r_mutbl , pat_info . max_ref_mutbl . as_mutbl ()) ; pat_info . binding_mode = pat_info . binding_mode . cap_ref_mutability (mutbl_cap) ; } else { if pat_mutbl > inh_mut { debug_assert ! (ref_pat_matches_mut_ref) ; self . error_inherited_ref_mutability_mismatch (pat , pat_prefix_span) ; } pat_info . binding_mode = ByRef :: No ; self . typeck_results . borrow_mut () . skipped_ref_pats_mut () . insert (pat . hir_id) ; self . check_pat (inner , expected , pat_info) ; return expected ; } } InheritedRefMatchRule :: EatBoth { consider_inherited_ref : true } => { pat_info . binding_mode = ByRef :: No ; if let ty :: Ref (_ , inner_ty , _) = * expected . kind () { if pat_mutbl . is_mut () && inh_mut . is_mut () { self . check_pat (inner , inner_ty , pat_info) ; return expected ; } else { } } else { if pat_mutbl > inh_mut { self . error_inherited_ref_mutability_mismatch (pat , pat_prefix_span) ; } self . typeck_results . borrow_mut () . skipped_ref_pats_mut () . insert (pat . hir_id) ; self . check_pat (inner , expected , pat_info) ; return expected ; } } InheritedRefMatchRule :: EatBoth { consider_inherited_ref : false } => { pat_info . binding_mode = ByRef :: No ; self . add_rust_2024_migration_desugared_pat (pat_info . top_info . hir_id , pat , match pat_mutbl { Mutability :: Not => '&' , Mutability :: Mut => 't' , } , inh_mut ,) } } } let (ref_ty , inner_ty) = match self . check_dereferenceable (pat . span , expected , inner) { Ok (()) => { debug ! ("check_pat_ref: expected={:?}" , expected) ; match * expected . kind () { ty :: Ref (_ , r_ty , r_mutbl) if (ref_pat_matches_mut_ref && r_mutbl >= pat_mutbl) || r_mutbl == pat_mutbl => { if r_mutbl == Mutability :: Not { pat_info . max_ref_mutbl = MutblCap :: Not ; } (expected , r_ty) } _ => { let inner_ty = self . next_ty_var (inner . span) ; let ref_ty = self . new_ref_ty (pat . span , pat_mutbl , inner_ty) ; debug ! ("check_pat_ref: demanding {:?} = {:?}" , expected , ref_ty) ; let err = self . demand_eqtype_pat_diag (pat . span , expected , ref_ty , & pat_info . top_info ,) ; if let Err (mut err) = err { self . borrow_pat_suggestion (& mut err , pat) ; err . emit () ; } (ref_ty , inner_ty) } } } Err (guar) => { let err = Ty :: new_error (tcx , guar) ; (err , err) } } ; self . check_pat (inner , inner_ty , pat_info) ; ref_ty } #[doc = " Create a reference type with a fresh region variable."] fn new_ref_ty (& self , span : Span , mutbl : Mutability , ty : Ty < 'tcx >) -> Ty < 'tcx > { let region = self . next_region_var (RegionVariableOrigin :: PatternRegion (span)) ; Ty :: new_ref (self . tcx , region , ty , mutbl) } fn error_inherited_ref_mutability_mismatch (& self , pat : & 'tcx Pat < 'tcx > , pat_prefix_span : Option < Span > ,) -> ErrorGuaranteed { let err_msg = "mismatched types" ; let err = if let Some (span) = pat_prefix_span { let mut err = self . dcx () . struct_span_err (span , err_msg) ; err . code (E0308) ; err . note ("cannot match inherited `&` with `&mut` pattern") ; err . span_suggestion_verbose (span , "replace this `&mut` pattern with `&`" , "&" , Applicability :: MachineApplicable ,) ; err } else { self . dcx () . struct_span_err (pat . span , err_msg) } ; err . emit () } fn try_resolve_slice_ty_to_array_ty (& self , before : & 'tcx [Pat < 'tcx >] , slice : Option < & 'tcx Pat < 'tcx > > , span : Span ,) -> Option < Ty < 'tcx > > { if slice . is_some () { return None ; } let tcx = self . tcx ; let len = before . len () ; let inner_ty = self . next_ty_var (span) ; Some (Ty :: new_array (tcx , inner_ty , len . try_into () . unwrap ())) } #[doc = " Used to determines whether we can infer the expected type in the slice pattern to be of type array."] #[doc = " This is only possible if we're in an irrefutable pattern. If we were to allow this in refutable"] #[doc = " patterns we wouldn't e.g. report ambiguity in the following situation:"] #[doc = ""] #[doc = " ```ignore(rust)"] #[doc = " struct Zeroes;"] #[doc = "    const ARR: [usize; 2] = [0; 2];"] #[doc = "    const ARR2: [usize; 2] = [2; 2];"] #[doc = ""] #[doc = "    impl Into<&'static [usize; 2]> for Zeroes {"] #[doc = "        fn into(self) -> &'static [usize; 2] {"] #[doc = "            &ARR"] #[doc = "        }"] #[doc = "    }"] #[doc = ""] #[doc = "    impl Into<&'static [usize]> for Zeroes {"] #[doc = "        fn into(self) -> &'static [usize] {"] #[doc = "            &ARR2"] #[doc = "        }"] #[doc = "    }"] #[doc = ""] #[doc = "    fn main() {"] #[doc = "        let &[a, b]: &[usize] = Zeroes.into() else {"] #[doc = "           .."] #[doc = "        };"] #[doc = "    }"] #[doc = " ```"] #[doc = ""] #[doc = " If we're in an irrefutable pattern we prefer the array impl candidate given that"] #[doc = " the slice impl candidate would be rejected anyway (if no ambiguity existed)."] fn pat_is_irrefutable (& self , decl_origin : Option < DeclOrigin < '_ > >) -> bool { match decl_origin { Some (DeclOrigin :: LocalDecl { els : None }) => true , Some (DeclOrigin :: LocalDecl { els : Some (_) } | DeclOrigin :: LetExpr) | None => false , } } #[doc = " Type check a slice pattern."] #[doc = ""] #[doc = " Syntactically, these look like `[pat_0, ..., pat_n]`."] #[doc = " Semantically, we are type checking a pattern with structure:"] #[doc = " ```ignore (not-rust)"] #[doc = " [before_0, ..., before_n, (slice, after_0, ... after_n)?]"] #[doc = " ```"] #[doc = " The type of `slice`, if it is present, depends on the `expected` type."] #[doc = " If `slice` is missing, then so is `after_i`."] #[doc = " If `slice` is present, it can still represent 0 elements."] fn check_pat_slice (& self , span : Span , before : & 'tcx [Pat < 'tcx >] , slice : Option < & 'tcx Pat < 'tcx > > , after : & 'tcx [Pat < 'tcx >] , expected : Ty < 'tcx > , pat_info : PatInfo < 'tcx > ,) -> Ty < 'tcx > { let expected = self . try_structurally_resolve_type (span , expected) ; if self . pat_is_irrefutable (pat_info . decl_origin) && expected . is_ty_var () { if let Some (resolved_arr_ty) = self . try_resolve_slice_ty_to_array_ty (before , slice , span) { debug ! (? resolved_arr_ty) ; let _ = self . demand_eqtype (span , expected , resolved_arr_ty) ; } } let expected = self . structurally_resolve_type (span , expected) ; debug ! (? expected) ; let (element_ty , opt_slice_ty , inferred) = match * expected . kind () { ty :: Array (element_ty , len) => { let min = before . len () as u64 + after . len () as u64 ; let (opt_slice_ty , expected) = self . check_array_pat_len (span , element_ty , expected , slice , len , min) ; assert ! (opt_slice_ty . is_some () || slice . is_none ()) ; (element_ty , opt_slice_ty , expected) } ty :: Slice (element_ty) => (element_ty , Some (expected) , expected) , _ => { let guar = expected . error_reported () . err () . unwrap_or_else (| | { self . error_expected_array_or_slice (span , expected , pat_info) }) ; let err = Ty :: new_error (self . tcx , guar) ; (err , Some (err) , err) } } ; for elt in before { self . check_pat (elt , element_ty , pat_info) ; } if let Some (slice) = slice { self . check_pat (slice , opt_slice_ty . unwrap () , pat_info) ; } for elt in after { self . check_pat (elt , element_ty , pat_info) ; } inferred } #[doc = " Type check the length of an array pattern."] #[doc = ""] #[doc = " Returns both the type of the variable length pattern (or `None`), and the potentially"] #[doc = " inferred array type. We only return `None` for the slice type if `slice.is_none()`."] fn check_array_pat_len (& self , span : Span , element_ty : Ty < 'tcx > , arr_ty : Ty < 'tcx > , slice : Option < & 'tcx Pat < 'tcx > > , len : ty :: Const < 'tcx > , min_len : u64 ,) -> (Option < Ty < 'tcx > > , Ty < 'tcx >) { let len = self . try_structurally_resolve_const (span , len) . try_to_target_usize (self . tcx) ; let guar = if let Some (len) = len { if slice . is_none () { if min_len == len { return (None , arr_ty) ; } self . error_scrutinee_inconsistent_length (span , min_len , len) } else if let Some (pat_len) = len . checked_sub (min_len) { return (Some (Ty :: new_array (self . tcx , element_ty , pat_len)) , arr_ty) ; } else { self . error_scrutinee_with_rest_inconsistent_length (span , min_len , len) } } else if slice . is_none () { let updated_arr_ty = Ty :: new_array (self . tcx , element_ty , min_len) ; self . demand_eqtype (span , updated_arr_ty , arr_ty) ; return (None , updated_arr_ty) ; } else { self . error_scrutinee_unfixed_length (span) } ; (Some (Ty :: new_error (self . tcx , guar)) , arr_ty) } fn error_scrutinee_inconsistent_length (& self , span : Span , min_len : u64 , size : u64 ,) -> ErrorGuaranteed { struct_span_code_err ! (self . dcx () , span , E0527 , "pattern requires {} element{} but array has {}" , min_len , pluralize ! (min_len) , size ,) . with_span_label (span , format ! ("expected {} element{}" , size , pluralize ! (size))) . emit () } fn error_scrutinee_with_rest_inconsistent_length (& self , span : Span , min_len : u64 , size : u64 ,) -> ErrorGuaranteed { struct_span_code_err ! (self . dcx () , span , E0528 , "pattern requires at least {} element{} but array has {}" , min_len , pluralize ! (min_len) , size ,) . with_span_label (span , format ! ("pattern cannot match array of {} element{}" , size , pluralize ! (size) ,) ,) . emit () } fn error_scrutinee_unfixed_length (& self , span : Span) -> ErrorGuaranteed { struct_span_code_err ! (self . dcx () , span , E0730 , "cannot pattern-match on an array without a fixed length" ,) . emit () } fn error_expected_array_or_slice (& self , span : Span , expected_ty : Ty < 'tcx > , pat_info : PatInfo < 'tcx > ,) -> ErrorGuaranteed { let PatInfo { top_info : ti , current_depth , .. } = pat_info ; let mut slice_pat_semantics = false ; let mut as_deref = None ; let mut slicing = None ; if let ty :: Ref (_ , ty , _) = expected_ty . kind () && let ty :: Array (..) | ty :: Slice (..) = ty . kind () { slice_pat_semantics = true ; } else if self . autoderef (span , expected_ty) . silence_errors () . any (| (ty , _) | matches ! (ty . kind () , ty :: Slice (..) | ty :: Array (..))) && let Some (span) = ti . span && let Some (_) = ti . origin_expr { let resolved_ty = self . resolve_vars_if_possible (ti . expected) ; let (is_slice_or_array_or_vector , resolved_ty) = self . is_slice_or_array_or_vector (resolved_ty) ; match resolved_ty . kind () { ty :: Adt (adt_def , _) if self . tcx . is_diagnostic_item (sym :: Option , adt_def . did ()) || self . tcx . is_diagnostic_item (sym :: Result , adt_def . did ()) => { as_deref = Some (errors :: AsDerefSuggestion { span : span . shrink_to_hi () }) ; } _ => () , } let is_top_level = current_depth <= 1 ; if is_slice_or_array_or_vector && is_top_level { slicing = Some (errors :: SlicingSuggestion { span : span . shrink_to_hi () }) ; } } self . dcx () . emit_err (errors :: ExpectedArrayOrSlice { span , ty : expected_ty , slice_pat_semantics , as_deref , slicing , }) } fn is_slice_or_array_or_vector (& self , ty : Ty < 'tcx >) -> (bool , Ty < 'tcx >) { match ty . kind () { ty :: Adt (adt_def , _) if self . tcx . is_diagnostic_item (sym :: Vec , adt_def . did ()) => { (true , ty) } ty :: Ref (_ , ty , _) => self . is_slice_or_array_or_vector (* ty) , ty :: Slice (..) | ty :: Array (..) => (true , ty) , _ => (false , ty) , } } #[doc = " Record a pattern that's invalid under Rust 2024 match ergonomics, along with a problematic"] #[doc = " span, so that the pattern migration lint can desugar it during THIR construction."] fn add_rust_2024_migration_desugared_pat (& self , pat_id : HirId , subpat : & 'tcx Pat < 'tcx > , final_char : char , def_br_mutbl : Mutability ,) { let from_expansion = subpat . span . from_expansion () ; let trimmed_span = if from_expansion { subpat . span } else { let trimmed = self . tcx . sess . source_map () . span_through_char (subpat . span , final_char) ; trimmed . with_ctxt (subpat . span . ctxt ()) } ; let mut typeck_results = self . typeck_results . borrow_mut () ; let mut table = typeck_results . rust_2024_migration_desugared_pats_mut () ; let info = table . entry (pat_id) . or_insert_with (| | ty :: Rust2024IncompatiblePatInfo { primary_labels : Vec :: new () , bad_modifiers : false , bad_ref_pats : false , suggest_eliding_modes : ! self . tcx . features () . ref_pat_eat_one_layer_2024 () && ! self . tcx . features () . ref_pat_eat_one_layer_2024_structural () , }) ; let pat_kind = if let PatKind :: Binding (user_bind_annot , _ , _ , _) = subpat . kind { info . bad_modifiers = true ; info . suggest_eliding_modes &= user_bind_annot == BindingMode (ByRef :: Yes (def_br_mutbl) , Mutability :: Not) ; "binding modifier" } else { info . bad_ref_pats = true ; info . suggest_eliding_modes = false ; "reference pattern" } ; let primary_label = if from_expansion { info . suggest_eliding_modes = false ; "occurs within macro expansion" . to_owned () } else { let dbm_str = match def_br_mutbl { Mutability :: Not => "ref" , Mutability :: Mut => "ref mut" , } ; format ! ("{pat_kind} not allowed under `{dbm_str}` default binding mode") } ; info . primary_labels . push ((trimmed_span , primary_label)) ; } }}}