mkuse!{use std :: sync :: OnceLock ;}
mkuse!{use rustc_data_structures :: graph ;}
mkuse!{use rustc_data_structures :: graph :: dominators :: { Dominators , dominators } ;}
mkuse!{use rustc_data_structures :: stable_hasher :: { HashStable , StableHasher } ;}
mkuse!{use rustc_index :: { IndexSlice , IndexVec } ;}
mkuse!{use rustc_macros :: { HashStable , TyDecodable , TyEncodable , TypeFoldable , TypeVisitable } ;}
mkuse!{use rustc_serialize :: { Decodable , Decoder , Encodable , Encoder } ;}
mkuse!{use smallvec :: SmallVec ;}
mkuse!{use crate :: mir :: traversal :: Postorder ;}
mkuse!{use crate :: mir :: { BasicBlock , BasicBlockData , START_BLOCK } ;}
mkitem!{mkstruct!{# [derive (Clone , TyEncodable , TyDecodable , Debug , HashStable , TypeFoldable , TypeVisitable)] pub struct BasicBlocks < 'tcx > { basic_blocks : IndexVec < BasicBlock , BasicBlockData < 'tcx > > , cache : Cache , }}}
mkitem!{type Predecessors = IndexVec < BasicBlock , SmallVec < [BasicBlock ; 4] > > ;}
mkitem!{mkenum!{# [derive (Debug , Clone , Copy)] pub enum SwitchTargetValue { Normal (u128) , Otherwise , }}}
mkitem!{mkstruct!{# [derive (Clone , Default , Debug)] struct Cache { predecessors : OnceLock < Predecessors > , reverse_postorder : OnceLock < Vec < BasicBlock > > , dominators : OnceLock < Dominators < BasicBlock > > , }}}
mkitem!{mkimpl!{impl < 'tcx > BasicBlocks < 'tcx > { # [inline] pub fn new (basic_blocks : IndexVec < BasicBlock , BasicBlockData < 'tcx > >) -> Self { BasicBlocks { basic_blocks , cache : Cache :: default () } } pub fn dominators (& self) -> & Dominators < BasicBlock > { self . cache . dominators . get_or_init (| | dominators (self)) } # [doc = " Returns predecessors for each basic block."] # [inline] pub fn predecessors (& self) -> & Predecessors { self . cache . predecessors . get_or_init (| | { let mut preds = IndexVec :: from_elem (SmallVec :: new () , & self . basic_blocks) ; for (bb , data) in self . basic_blocks . iter_enumerated () { if let Some (term) = & data . terminator { for succ in term . successors () { preds [succ] . push (bb) ; } } } preds }) } # [doc = " Returns basic blocks in a reverse postorder."] # [doc = ""] # [doc = " See [`traversal::reverse_postorder`]'s docs to learn what is preorder traversal."] # [doc = ""] # [doc = " [`traversal::reverse_postorder`]: crate::mir::traversal::reverse_postorder"] # [inline] pub fn reverse_postorder (& self) -> & [BasicBlock] { self . cache . reverse_postorder . get_or_init (| | { let mut rpo : Vec < _ > = Postorder :: new (& self . basic_blocks , START_BLOCK , None) . collect () ; rpo . reverse () ; rpo }) } # [doc = " Returns mutable reference to basic blocks. Invalidates CFG cache."] # [inline] pub fn as_mut (& mut self) -> & mut IndexVec < BasicBlock , BasicBlockData < 'tcx > > { self . invalidate_cfg_cache () ; & mut self . basic_blocks } # [doc = " Get mutable access to basic blocks without invalidating the CFG cache."] # [doc = ""] # [doc = " By calling this method instead of e.g. [`BasicBlocks::as_mut`] you promise not to change"] # [doc = " the CFG. This means that"] # [doc = ""] # [doc = "  1) The number of basic blocks remains unchanged"] # [doc = "  2) The set of successors of each terminator remains unchanged."] # [doc = "  3) For each `TerminatorKind::SwitchInt`, the `targets` remains the same and the terminator"] # [doc = "     kind is not changed."] # [doc = ""] # [doc = " If any of these conditions cannot be upheld, you should call [`BasicBlocks::invalidate_cfg_cache`]."] # [inline] pub fn as_mut_preserves_cfg (& mut self) -> & mut IndexVec < BasicBlock , BasicBlockData < 'tcx > > { & mut self . basic_blocks } # [doc = " Invalidates cached information about the CFG."] # [doc = ""] # [doc = " You will only ever need this if you have also called [`BasicBlocks::as_mut_preserves_cfg`]."] # [doc = " All other methods that allow you to mutate the basic blocks also call this method"] # [doc = " themselves, thereby avoiding any risk of accidentally cache invalidation."] pub fn invalidate_cfg_cache (& mut self) { self . cache = Cache :: default () ; } }}}
mkitem!{mkimpl!{impl < 'tcx > std :: ops :: Deref for BasicBlocks < 'tcx > { type Target = IndexSlice < BasicBlock , BasicBlockData < 'tcx > > ; # [inline] fn deref (& self) -> & IndexSlice < BasicBlock , BasicBlockData < 'tcx > > { & self . basic_blocks } }}}
mkitem!{mkimpl!{impl < 'tcx > graph :: DirectedGraph for BasicBlocks < 'tcx > { type Node = BasicBlock ; # [inline] fn num_nodes (& self) -> usize { self . basic_blocks . len () } }}}
mkitem!{mkimpl!{impl < 'tcx > graph :: StartNode for BasicBlocks < 'tcx > { # [inline] fn start_node (& self) -> Self :: Node { START_BLOCK } }}}
mkitem!{mkimpl!{impl < 'tcx > graph :: Successors for BasicBlocks < 'tcx > { # [inline] fn successors (& self , node : Self :: Node) -> impl Iterator < Item = Self :: Node > { self . basic_blocks [node] . terminator () . successors () } }}}
mkitem!{mkimpl!{impl < 'tcx > graph :: Predecessors for BasicBlocks < 'tcx > { # [inline] fn predecessors (& self , node : Self :: Node) -> impl Iterator < Item = Self :: Node > { self . predecessors () [node] . iter () . copied () } }}}
mkitem!{TrivialTypeTraversalImpls ! { Cache }}
mkitem!{mkimpl!{impl < S : Encoder > Encodable < S > for Cache { # [inline] fn encode (& self , _s : & mut S) { } }}}
mkitem!{mkimpl!{impl < D : Decoder > Decodable < D > for Cache { # [inline] fn decode (_ : & mut D) -> Self { Default :: default () } }}}
mkitem!{mkimpl!{impl < CTX > HashStable < CTX > for Cache { # [inline] fn hash_stable (& self , _ : & mut CTX , _ : & mut StableHasher) { } }}}