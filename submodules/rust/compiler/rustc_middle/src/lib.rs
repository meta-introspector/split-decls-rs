# ! [doc = " The \"main crate\" of the Rust compiler. This crate contains common"] # ! [doc = " type definitions that are used by the other crates in the rustc"] # ! [doc = " \"family\". The following are some prominent examples."] # ! [doc = ""] # ! [doc = " - **HIR.** The \"high-level (H) intermediate representation (IR)\" is"] # ! [doc = "   defined in the [`hir`] module."] # ! [doc = " - **THIR.** The \"typed high-level (H) intermediate representation (IR)\""] # ! [doc = "   is defined in the [`thir`] module."] # ! [doc = " - **MIR.** The \"mid-level (M) intermediate representation (IR)\" is"] # ! [doc = "   defined in the [`mir`] module. This module contains only the"] # ! [doc = "   *definition* of the MIR; the passes that transform and operate"] # ! [doc = "   on MIR are found in `rustc_const_eval` crate."] # ! [doc = " - **Types.** The internal representation of types used in rustc is"] # ! [doc = "   defined in the [`ty`] module. This includes the"] # ! [doc = "   [**type context**][ty::TyCtxt] (or `tcx`), which is the central"] # ! [doc = "   context during most of compilation, containing the interners and"] # ! [doc = "   other things."] # ! [doc = ""] # ! [doc = " For more information about how rustc works, see the [rustc dev guide]."] # ! [doc = ""] # ! [doc = " [rustc dev guide]: https://rustc-dev-guide.rust-lang.org/"] # ! [doc = ""] # ! [doc = " # Note"] # ! [doc = ""] # ! [doc = " This API is completely unstable and subject to change."] # ! [allow (internal_features)] # ! [allow (rustc :: diagnostic_outside_of_impl)] # ! [allow (rustc :: direct_use_of_rustc_type_ir)] # ! [allow (rustc :: untranslatable_diagnostic)] # ! [cfg_attr (bootstrap , feature (round_char_boundary))] # ! [doc (html_root_url = "https://doc.rust-lang.org/nightly/nightly-rustc/")] # ! [doc (rust_logo)] # ! [feature (allocator_api)] # ! [feature (array_windows)] # ! [feature (assert_matches)] # ! [feature (associated_type_defaults)] # ! [feature (box_as_ptr)] # ! [feature (box_patterns)] # ! [feature (closure_track_caller)] # ! [feature (core_intrinsics)] # ! [feature (debug_closure_helpers)] # ! [feature (decl_macro)] # ! [feature (discriminant_kind)] # ! [feature (extern_types)] # ! [feature (file_buffered)] # ! [feature (gen_blocks)] # ! [feature (if_let_guard)] # ! [feature (intra_doc_pointers)] # ! [feature (min_specialization)] # ! [feature (negative_impls)] # ! [feature (never_type)] # ! [feature (ptr_alignment_type)] # ! [feature (rustc_attrs)] # ! [feature (rustdoc_internals)] # ! [feature (sized_hierarchy)] # ! [feature (try_blocks)] # ! [feature (try_trait_v2)] # ! [feature (try_trait_v2_residual)] # ! [feature (try_trait_v2_yeet)] # ! [feature (type_alias_impl_trait)] # ! [feature (unwrap_infallible)] # ! [feature (yeet_expr)] # ! [recursion_limit = "256"] use split_decls_genesis :: ourprelude :: * ; #[cfg (test)] mod tests ; #[macro_use] mod macros ; #[macro_use] pub mod arena ; pub mod error ; pub mod hir ; pub mod hooks ; pub mod infer ; pub mod lint ; pub mod metadata ; pub mod middle ; pub mod mir ; pub mod thir ; pub mod traits ; pub mod ty ; pub mod util ; mod values ; #[macro_use] pub mod query ; #[macro_use] pub mod dep_graph ; extern crate self as rustc_middle ; rustc_fluent_macro :: fluent_messages ! { "messages.ftl" }