# ! [doc = " The public parts of this private module are used to create traits"] # ! [doc = " that cannot be implemented outside of our own crate. This way we"] # ! [doc = " can feel free to extend those traits without worrying about it"] # ! [doc = " being a breaking change for other implementations."] use split_decls_genesis :: ourprelude :: * ; #[doc = " If this type is pub but not publicly reachable, third parties"] #[doc = " can't name it and can't implement traits using it."] #[allow (missing_debug_implementations)] pub struct PrivateMarker ; macro_rules ! private_decl { () => { #[doc = " This trait is private; this method exists to make it"] #[doc = " impossible to implement outside the crate."] #[doc (hidden)] fn __rayon_private__ (& self) -> crate :: private :: PrivateMarker ; } ; } macro_rules ! private_impl { () => { fn __rayon_private__ (& self) -> crate :: private :: PrivateMarker { crate :: private :: PrivateMarker } } ; }