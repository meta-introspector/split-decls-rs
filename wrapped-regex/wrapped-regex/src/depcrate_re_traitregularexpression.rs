// Generated macro for RegularExpression (trait)
macro_rules! Depcrate_re_traitRegularExpression {
() => {
// Module: crate::re_trait
// Provides: {"RegularExpression"}
// Dependencies: {}
# [doc = " RegularExpression describes types that can implement regex searching."] # [doc = ""] # [doc = " This trait is my attempt at reducing code duplication and to standardize"] # [doc = " the internal API. Specific duplication that is avoided are the `find`"] # [doc = " and `capture` iterators, which are slightly tricky."] # [doc = ""] # [doc = " It's not clear whether this trait is worth it, and it also isn't"] # [doc = " clear whether it's useful as a public trait or not. Methods like"] # [doc = " `next_after_empty` reak of bad design, but the rest of the methods seem"] # [doc = " somewhat reasonable. One particular thing this trait would expose would be"] # [doc = " the ability to start the search of a regex anywhere in a haystack, which"] # [doc = " isn't possible in the current public API."] pub trait RegularExpression : Sized { # [doc = " The type of the haystack."] type Text : ? Sized ; # [doc = " The number of capture slots in the compiled regular expression. This is"] # [doc = " always two times the number of capture groups (two slots per group)."] fn slots_len (& self) -> usize ; # [doc = " Returns the position of the next character after `i`."] # [doc = ""] # [doc = " For example, a haystack with type `&[u8]` probably returns `i+1`,"] # [doc = " whereas a haystack with type `&str` probably returns `i` plus the"] # [doc = " length of the next UTF-8 sequence."] fn next_after_empty (& self , text : & Self :: Text , i : usize) -> usize ; # [doc = " Returns the location of the shortest match."] fn shortest_match_at (& self , text : & Self :: Text , start : usize ,) -> Option < usize > ; # [doc = " Returns whether the regex matches the text given."] fn is_match_at (& self , text : & Self :: Text , start : usize ,) -> bool ; # [doc = " Returns the leftmost-first match location if one exists."] fn find_at (& self , text : & Self :: Text , start : usize ,) -> Option < (usize , usize) > ; # [doc = " Returns the leftmost-first match location if one exists, and also"] # [doc = " fills in any matching capture slot locations."] fn read_captures_at (& self , slots : & mut [Slot] , text : & Self :: Text , start : usize ,) -> Option < (usize , usize) > ; # [doc = " Returns an iterator over all non-overlapping successive leftmost-first"] # [doc = " matches."] fn find_iter < 't > (self , text : & 't Self :: Text ,) -> FindMatches < 't , Self > { FindMatches { re : self , text : text , last_end : 0 , last_match : None , } } # [doc = " Returns an iterator over all non-overlapping successive leftmost-first"] # [doc = " matches with captures."] fn captures_iter < 't > (self , text : & 't Self :: Text ,) -> FindCaptures < 't , Self > { FindCaptures (self . find_iter (text)) } }
};
}
