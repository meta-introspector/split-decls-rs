// Generated macro for Inst (enum)
macro_rules! Depcrate_progInst {
() => {
// Module: crate::prog
// Provides: {"Inst"}
// Dependencies: {}
# [doc = " Inst is an instruction code in a Regex program."] # [doc = ""] # [doc = " Regrettably, a regex program either contains Unicode codepoint"] # [doc = " instructions (Char and Ranges) or it contains byte instructions (Bytes)."] # [doc = " A regex program can never contain both."] # [doc = ""] # [doc = " It would be worth investigating splitting this into two distinct types and"] # [doc = " then figuring out how to make the matching engines polymorphic over those"] # [doc = " types without sacrificing performance."] # [doc = ""] # [doc = " Other than the benefit of moving invariants into the type system, another"] # [doc = " benefit is the decreased size. If we remove the `Char` and `Ranges`"] # [doc = " instructions from the `Inst` enum, then its size shrinks from 40 bytes to"] # [doc = " 24 bytes. (This is because of the removal of a `Vec` in the `Ranges`"] # [doc = " variant.) Given that byte based machines are typically much bigger than"] # [doc = " their Unicode analogues (because they can decode UTF-8 directly), this ends"] # [doc = " up being a pretty significant savings."] # [derive (Clone , Debug)] pub enum Inst { # [doc = " Match indicates that the program has reached a match state."] # [doc = ""] # [doc = " The number in the match corresponds to the Nth logical regular"] # [doc = " expression in this program. This index is always 0 for normal regex"] # [doc = " programs. Values greater than 0 appear when compiling regex sets, and"] # [doc = " each match instruction gets its own unique value. The value corresponds"] # [doc = " to the Nth regex in the set."] Match (usize) , # [doc = " Save causes the program to save the current location of the input in"] # [doc = " the slot indicated by InstSave."] Save (InstSave) , # [doc = " Split causes the program to diverge to one of two paths in the"] # [doc = " program, preferring goto1 in InstSplit."] Split (InstSplit) , # [doc = " EmptyLook represents a zero-width assertion in a regex program. A"] # [doc = " zero-width assertion does not consume any of the input text."] EmptyLook (InstEmptyLook) , # [doc = " Char requires the regex program to match the character in InstChar at"] # [doc = " the current position in the input."] Char (InstChar) , # [doc = " Ranges requires the regex program to match the character at the current"] # [doc = " position in the input with one of the ranges specified in InstRanges."] Ranges (InstRanges) , # [doc = " Bytes is like Ranges, except it expresses a single byte range. It is"] # [doc = " used in conjunction with Split instructions to implement multi-byte"] # [doc = " character classes."] Bytes (InstBytes) , }
};
}
