// Generated macro for StatePtr (type)
macro_rules! Depcrate_dfaStatePtr {
() => {
// Module: crate::dfa
// Provides: {"StatePtr"}
// Dependencies: {}
# [doc = " StatePtr is a 32 bit pointer to the start of a row in the transition table."] # [doc = ""] # [doc = " It has many special values. There are two types of special values:"] # [doc = " sentinels and flags."] # [doc = ""] # [doc = " Sentinels corresponds to special states that carry some kind of"] # [doc = " significance. There are three such states: unknown, dead and quit states."] # [doc = ""] # [doc = " Unknown states are states that haven't been computed yet. They indicate"] # [doc = " that a transition should be filled in that points to either an existing"] # [doc = " cached state or a new state altogether. In general, an unknown state means"] # [doc = " \"follow the NFA's epsilon transitions.\""] # [doc = ""] # [doc = " Dead states are states that can never lead to a match, no matter what"] # [doc = " subsequent input is observed. This means that the DFA should quit"] # [doc = " immediately and return the longest match it has found thus far."] # [doc = ""] # [doc = " Quit states are states that imply the DFA is not capable of matching the"] # [doc = " regex correctly. Currently, this is only used when a Unicode word boundary"] # [doc = " exists in the regex *and* a non-ASCII byte is observed."] # [doc = ""] # [doc = " The other type of state pointer is a state pointer with special flag bits."] # [doc = " There are two flags: a start flag and a match flag. The lower bits of both"] # [doc = " kinds always contain a \"valid\" StatePtr (indicated by the STATE_MAX mask)."] # [doc = ""] # [doc = " The start flag means that the state is a start state, and therefore may be"] # [doc = " subject to special prefix scanning optimizations."] # [doc = ""] # [doc = " The match flag means that the state is a match state, and therefore the"] # [doc = " current position in the input (while searching) should be recorded."] # [doc = ""] # [doc = " The above exists mostly in the service of making the inner loop fast."] # [doc = " In particular, the inner *inner* loop looks something like this:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " while state <= STATE_MAX and i < len(text):"] # [doc = "     state = state.next[i]"] # [doc = " ```"] # [doc = ""] # [doc = " This is nice because it lets us execute a lazy DFA as if it were an"] # [doc = " entirely offline DFA (i.e., with very few instructions). The loop will"] # [doc = " quit only when we need to examine a case that needs special attention."] type StatePtr = u32 ;
};
}
