// Generated macro for impl_120 (impl)
macro_rules! Depcrate_regex_stringimpl_120 {
() => {
// Module: crate::regex::string
// Provides: {"impl_120"}
// Dependencies: {}
impl CaptureLocations { # [doc = " Returns the start and end byte offsets of the capture group at index"] # [doc = " `i`. This returns `None` if `i` is not a valid capture group or if the"] # [doc = " capture group did not match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?<first>\\w+)\\s+(?<last>\\w+)\").unwrap();"] # [doc = " let mut locs = re.capture_locations();"] # [doc = " re.captures_read(&mut locs, \"Bruce Springsteen\").unwrap();"] # [doc = " assert_eq!(Some((0, 17)), locs.get(0));"] # [doc = " assert_eq!(Some((0, 5)), locs.get(1));"] # [doc = " assert_eq!(Some((6, 17)), locs.get(2));"] # [doc = " ```"] # [inline] pub fn get (& self , i : usize) -> Option < (usize , usize) > { self . 0 . get_group (i) . map (| sp | (sp . start , sp . end)) } # [doc = " Returns the total number of capture groups (even if they didn't match)."] # [doc = " That is, the length returned is unaffected by the result of a search."] # [doc = ""] # [doc = " This is always at least `1` since every regex has at least `1`"] # [doc = " capturing group that corresponds to the entire match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?<first>\\w+)\\s+(?<last>\\w+)\").unwrap();"] # [doc = " let mut locs = re.capture_locations();"] # [doc = " assert_eq!(3, locs.len());"] # [doc = " re.captures_read(&mut locs, \"Bruce Springsteen\").unwrap();"] # [doc = " assert_eq!(3, locs.len());"] # [doc = " ```"] # [doc = ""] # [doc = " Notice that the length is always at least `1`, regardless of the regex:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\").unwrap();"] # [doc = " let locs = re.capture_locations();"] # [doc = " assert_eq!(1, locs.len());"] # [doc = ""] # [doc = " // [a&&b] is a regex that never matches anything."] # [doc = " let re = Regex::new(r\"[a&&b]\").unwrap();"] # [doc = " let locs = re.capture_locations();"] # [doc = " assert_eq!(1, locs.len());"] # [doc = " ```"] # [inline] pub fn len (& self) -> usize { self . 0 . group_info () . group_len (PatternID :: ZERO) } # [doc = " An alias for the `get` method for backwards compatibility."] # [doc = ""] # [doc = " Previously, we exported `get` as `pos` in an undocumented API. To"] # [doc = " prevent breaking that code (e.g., in `regex-capi`), we continue"] # [doc = " re-exporting the same undocumented API."] # [doc (hidden)] # [inline] pub fn pos (& self , i : usize) -> Option < (usize , usize) > { self . get (i) } }
};
}
