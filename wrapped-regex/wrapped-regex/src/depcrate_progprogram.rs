// Generated macro for Program (struct)
macro_rules! Depcrate_progProgram {
() => {
// Module: crate::prog
// Provides: {"Program"}
// Dependencies: {}
# [doc = " Program is a sequence of instructions and various facts about thos"] # [doc = " instructions."] # [derive (Clone)] pub struct Program { # [doc = " A sequence of instructions that represents an NFA."] pub insts : Vec < Inst > , # [doc = " Pointers to each Match instruction in the sequence."] # [doc = ""] # [doc = " This is always length 1 unless this program represents a regex set."] pub matches : Vec < InstPtr > , # [doc = " The ordered sequence of all capture groups extracted from the AST."] # [doc = " Unnamed groups are `None`."] pub captures : Vec < Option < String > > , # [doc = " Pointers to all named capture groups into `captures`."] pub capture_name_idx : Arc < HashMap < String , usize > > , # [doc = " A pointer to the start instruction. This can vary depending on how"] # [doc = " the program was compiled. For example, programs for use with the DFA"] # [doc = " engine have a `.*?` inserted at the beginning of unanchored regular"] # [doc = " expressions. The actual starting point of the program is after the"] # [doc = " `.*?`."] pub start : InstPtr , # [doc = " A set of equivalence classes for discriminating bytes in the compiled"] # [doc = " program."] pub byte_classes : Vec < u8 > , # [doc = " When true, this program can only match valid UTF-8."] pub only_utf8 : bool , # [doc = " When true, this program uses byte range instructions instead of Unicode"] # [doc = " range instructions."] pub is_bytes : bool , # [doc = " When true, the program is compiled for DFA matching. For example, this"] # [doc = " implies `is_bytes` and also inserts a preceding `.*?` for unanchored"] # [doc = " regexes."] pub is_dfa : bool , # [doc = " When true, the program matches text in reverse (for use only in the"] # [doc = " DFA)."] pub is_reverse : bool , # [doc = " Whether the regex must match from the start of the input."] pub is_anchored_start : bool , # [doc = " Whether the regex must match at the end of the input."] pub is_anchored_end : bool , # [doc = " Whether this program contains a Unicode word boundary instruction."] pub has_unicode_word_boundary : bool , # [doc = " A possibly empty machine for very quickly matching prefix literals."] pub prefixes : LiteralSearcher , # [doc = " A limit on the size of the cache that the DFA is allowed to use while"] # [doc = " matching."] # [doc = ""] # [doc = " The cache limit specifies approximately how much space we're willing to"] # [doc = " give to the state cache. Once the state cache exceeds the size, it is"] # [doc = " wiped and all states must be re-computed."] # [doc = ""] # [doc = " Note that this value does not impact correctness. It can be set to 0"] # [doc = " and the DFA will run just fine. (It will only ever store exactly one"] # [doc = " state in the cache, and will likely run very slowly, but it will work.)"] # [doc = ""] # [doc = " Also note that this limit is *per thread of execution*. That is,"] # [doc = " if the same regex is used to search text across multiple threads"] # [doc = " simultaneously, then the DFA cache is not shared. Instead, copies are"] # [doc = " made."] pub dfa_size_limit : usize , }
};
}
