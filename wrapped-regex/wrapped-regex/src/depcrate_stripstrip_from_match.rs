// Generated macro for strip_from_match (function)
macro_rules! Depcrate_stripstrip_from_match {
() => {
// Module: crate::strip
// Provides: {"strip_from_match"}
// Dependencies: {}
# [doc = " Return an HIR that is guaranteed to never match the given line terminator,"] # [doc = " if possible."] # [doc = ""] # [doc = " If the transformation isn't possible, then an error is returned."] # [doc = ""] # [doc = " In general, if a literal line terminator occurs anywhere in the HIR, then"] # [doc = " this will return an error. However, if the line terminator occurs within"] # [doc = " a character class with at least one other character (that isn't also a line"] # [doc = " terminator), then the line terminator is simply stripped from that class."] # [doc = ""] # [doc = " If the given line terminator is not ASCII, then this function returns an"] # [doc = " error."] # [doc = ""] # [doc = " Note that as of regex 1.9, this routine could theoretically be implemented"] # [doc = " without returning an error. Namely, for example, we could turn"] # [doc = " `foo\\nbar` into `foo[a&&b]bar`. That is, replace line terminator with a"] # [doc = " sub-expression that can never match anything. Thus, ripgrep would accept"] # [doc = " such regexes and just silently not match anything. Regex versions prior"] # [doc = " to 1.8 don't support such constructs. I ended up deciding to leave the"] # [doc = " existing behavior of returning an error instead. For example:"] # [doc = ""] # [doc = " ```text"] # [doc = " $ echo -n 'foo\\nbar\\n' | rg 'foo\\nbar'"] # [doc = " the literal '\"\\n\"' is not allowed in a regex"] # [doc = ""] # [doc = " Consider enabling multiline mode with the --multiline flag (or -U for short)."] # [doc = " When multiline mode is enabled, new line characters can be matched."] # [doc = " ```"] # [doc = ""] # [doc = " This looks like a good error message to me, and even suggests a flag that"] # [doc = " the user can use instead."] pub (crate) fn strip_from_match (expr : Hir , line_term : LineTerminator ,) -> Result < Hir , Error > { if line_term . is_crlf () { let expr1 = strip_from_match_ascii (expr , b'\r') ? ; strip_from_match_ascii (expr1 , b'\n') } else { strip_from_match_ascii (expr , line_term . as_byte ()) } }
};
}
