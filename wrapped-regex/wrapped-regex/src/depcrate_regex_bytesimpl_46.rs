// Generated macro for impl_46 (impl)
macro_rules! Depcrate_regex_bytesimpl_46 {
() => {
// Module: crate::regex::bytes
// Provides: {"impl_46"}
// Dependencies: {}
# [doc = " Auxiliary methods."] impl Regex { # [doc = " Returns the original string of this regex."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"foo\\w+bar\").unwrap();"] # [doc = " assert_eq!(re.as_str(), r\"foo\\w+bar\");"] # [doc = " ```"] # [inline] pub fn as_str (& self) -> & str { & self . pattern } # [doc = " Returns an iterator over the capture names in this regex."] # [doc = ""] # [doc = " The iterator returned yields elements of type `Option<&str>`. That is,"] # [doc = " the iterator yields values for all capture groups, even ones that are"] # [doc = " unnamed. The order of the groups corresponds to the order of the group's"] # [doc = " corresponding opening parenthesis."] # [doc = ""] # [doc = " The first element of the iterator always yields the group corresponding"] # [doc = " to the overall match, and this group is always unnamed. Therefore, the"] # [doc = " iterator always yields at least one group."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows basic usage with a mix of named and unnamed capture groups:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?<a>.(?<b>.))(.)(?:.)(?<c>.)\").unwrap();"] # [doc = " let mut names = re.capture_names();"] # [doc = " assert_eq!(names.next(), Some(None));"] # [doc = " assert_eq!(names.next(), Some(Some(\"a\")));"] # [doc = " assert_eq!(names.next(), Some(Some(\"b\")));"] # [doc = " assert_eq!(names.next(), Some(None));"] # [doc = " // the '(?:.)' group is non-capturing and so doesn't appear here!"] # [doc = " assert_eq!(names.next(), Some(Some(\"c\")));"] # [doc = " assert_eq!(names.next(), None);"] # [doc = " ```"] # [doc = ""] # [doc = " The iterator always yields at least one element, even for regexes with"] # [doc = " no capture groups and even for regexes that can never match:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\").unwrap();"] # [doc = " let mut names = re.capture_names();"] # [doc = " assert_eq!(names.next(), Some(None));"] # [doc = " assert_eq!(names.next(), None);"] # [doc = ""] # [doc = " let re = Regex::new(r\"[a&&b]\").unwrap();"] # [doc = " let mut names = re.capture_names();"] # [doc = " assert_eq!(names.next(), Some(None));"] # [doc = " assert_eq!(names.next(), None);"] # [doc = " ```"] # [inline] pub fn capture_names (& self) -> CaptureNames < '_ > { CaptureNames (self . meta . group_info () . pattern_names (PatternID :: ZERO)) } # [doc = " Returns the number of captures groups in this regex."] # [doc = ""] # [doc = " This includes all named and unnamed groups, including the implicit"] # [doc = " unnamed group that is always present and corresponds to the entire"] # [doc = " match."] # [doc = ""] # [doc = " Since the implicit unnamed group is always included in this length, the"] # [doc = " length returned is guaranteed to be greater than zero."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"foo\").unwrap();"] # [doc = " assert_eq!(1, re.captures_len());"] # [doc = ""] # [doc = " let re = Regex::new(r\"(foo)\").unwrap();"] # [doc = " assert_eq!(2, re.captures_len());"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?<a>.(?<b>.))(.)(?:.)(?<c>.)\").unwrap();"] # [doc = " assert_eq!(5, re.captures_len());"] # [doc = ""] # [doc = " let re = Regex::new(r\"[a&&b]\").unwrap();"] # [doc = " assert_eq!(1, re.captures_len());"] # [doc = " ```"] # [inline] pub fn captures_len (& self) -> usize { self . meta . group_info () . group_len (PatternID :: ZERO) } # [doc = " Returns the total number of capturing groups that appear in every"] # [doc = " possible match."] # [doc = ""] # [doc = " If the number of capture groups can vary depending on the match, then"] # [doc = " this returns `None`. That is, a value is only returned when the number"] # [doc = " of matching groups is invariant or \"static.\""] # [doc = ""] # [doc = " Note that like [`Regex::captures_len`], this **does** include the"] # [doc = " implicit capturing group corresponding to the entire match. Therefore,"] # [doc = " when a non-None value is returned, it is guaranteed to be at least `1`."] # [doc = " Stated differently, a return value of `Some(0)` is impossible."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows a few cases where a static number of capture groups is"] # [doc = " available and a few cases where it is not."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let len = |pattern| {"] # [doc = "     Regex::new(pattern).map(|re| re.static_captures_len())"] # [doc = " };"] # [doc = ""] # [doc = " assert_eq!(Some(1), len(\"a\")?);"] # [doc = " assert_eq!(Some(2), len(\"(a)\")?);"] # [doc = " assert_eq!(Some(2), len(\"(a)|(b)\")?);"] # [doc = " assert_eq!(Some(3), len(\"(a)(b)|(c)(d)\")?);"] # [doc = " assert_eq!(None, len(\"(a)|b\")?);"] # [doc = " assert_eq!(None, len(\"a|(b)\")?);"] # [doc = " assert_eq!(None, len(\"(b)*\")?);"] # [doc = " assert_eq!(Some(2), len(\"(b)+\")?);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn static_captures_len (& self) -> Option < usize > { self . meta . static_captures_len () } # [doc = " Returns a fresh allocated set of capture locations that can"] # [doc = " be reused in multiple calls to [`Regex::captures_read`] or"] # [doc = " [`Regex::captures_read_at`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(.)(.)(\\w+)\").unwrap();"] # [doc = " let mut locs = re.capture_locations();"] # [doc = " assert!(re.captures_read(&mut locs, b\"Padron\").is_some());"] # [doc = " assert_eq!(locs.get(0), Some((0, 6)));"] # [doc = " assert_eq!(locs.get(1), Some((0, 1)));"] # [doc = " assert_eq!(locs.get(2), Some((1, 2)));"] # [doc = " assert_eq!(locs.get(3), Some((2, 6)));"] # [doc = " ```"] # [inline] pub fn capture_locations (& self) -> CaptureLocations { CaptureLocations (self . meta . create_captures ()) } # [doc = " An alias for `capture_locations` to preserve backward compatibility."] # [doc = ""] # [doc = " The `regex-capi` crate uses this method, so to avoid breaking that"] # [doc = " crate, we continue to export it as an undocumented API."] # [doc (hidden)] # [inline] pub fn locations (& self) -> CaptureLocations { self . capture_locations () } }
};
}
