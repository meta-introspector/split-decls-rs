// Generated macro for impl_160 (impl)
macro_rules! Depcrate_execimpl_160 {
() => {
// Module: crate::exec
// Provides: {"impl_160"}
// Dependencies: {}
impl ExecReadOnly { fn choose_match_type (& self , hint : Option < MatchType >) -> MatchType { use self :: MatchType :: * ; if let Some (Nfa (_)) = hint { return hint . unwrap () ; } if self . nfa . insts . is_empty () { return Nothing ; } if self . res . len () == 1 { if self . nfa . prefixes . complete () { return if self . nfa . is_anchored_start { Literal (MatchLiteralType :: AnchoredStart) } else { Literal (MatchLiteralType :: Unanchored) } ; } if self . suffixes . complete () { return if self . nfa . is_anchored_end { Literal (MatchLiteralType :: AnchoredEnd) } else { Literal (MatchLiteralType :: Unanchored) } ; } } if dfa :: can_exec (& self . dfa) { if self . res . len () >= 2 { return DfaMany ; } if ! self . nfa . is_anchored_start && self . nfa . is_anchored_end { return DfaAnchoredReverse ; } if self . should_suffix_scan () { return DfaSuffix ; } return Dfa ; } Nfa (MatchNfaType :: Auto) } # [doc = " Returns true if the program is amenable to suffix scanning."] # [doc = ""] # [doc = " When this is true, as a heuristic, we assume it is OK to quickly scan"] # [doc = " for suffix literals and then do a *reverse* DFA match from any matches"] # [doc = " produced by the literal scan. (And then followed by a forward DFA"] # [doc = " search, since the previously found suffix literal maybe not actually be"] # [doc = " the end of a match.)"] # [doc = ""] # [doc = " This is a bit of a specialized optimization, but can result in pretty"] # [doc = " big performance wins if 1) there are no prefix literals and 2) the"] # [doc = " suffix literals are pretty rare in the text. (1) is obviously easy to"] # [doc = " account for but (2) is harder. As a proxy, we assume that longer"] # [doc = " strings are generally rarer, so we only enable this optimization when"] # [doc = " we have a meaty suffix."] fn should_suffix_scan (& self) -> bool { if self . suffixes . is_empty () { return false ; } let lcs_len = self . suffixes . lcs () . char_len () ; lcs_len >= 3 && lcs_len > self . dfa . prefixes . lcp () . char_len () } }
};
}
