// Generated macro for impl_17 (impl)
macro_rules! Depcrate_configimpl_17 {
() => {
// Module: crate::config
// Provides: {"impl_17"}
// Dependencies: {}
impl Config { # [doc = " Use this configuration to build an HIR from the given patterns. The HIR"] # [doc = " returned corresponds to a single regex that is an alternation of the"] # [doc = " patterns given."] pub (crate) fn build_many < P : AsRef < str > > (& self , patterns : & [P] ,) -> Result < ConfiguredHIR , Error > { ConfiguredHIR :: new (self . clone () , patterns) } # [doc = " Accounting for the `smart_case` config knob, return true if and only if"] # [doc = " this pattern should be matched case insensitively."] fn is_case_insensitive (& self , analysis : & AstAnalysis) -> bool { if self . case_insensitive { return true ; } if ! self . case_smart { return false ; } analysis . any_literal () && ! analysis . any_uppercase () } # [doc = " Returns whether the given patterns should be treated as \"fixed strings\""] # [doc = " literals. This is different from just querying the `fixed_strings` knob"] # [doc = " in that if the knob is false, this will still return true in some cases"] # [doc = " if the patterns are themselves indistinguishable from literals."] # [doc = ""] # [doc = " The main idea here is that if this returns true, then it is safe"] # [doc = " to build an `regex_syntax::hir::Hir` value directly from the given"] # [doc = " patterns as an alternation of `hir::Literal` values."] fn is_fixed_strings < P : AsRef < str > > (& self , patterns : & [P]) -> bool { if self . case_insensitive || self . case_smart { return false ; } if self . fixed_strings { if let Some (lineterm) = self . line_terminator { for p in patterns . iter () { if has_line_terminator (lineterm , p . as_ref ()) { return false ; } } } return true ; } for p in patterns . iter () { let p = p . as_ref () ; if p . chars () . any (regex_syntax :: is_meta_character) { return false ; } if let Some (lineterm) = self . line_terminator { if has_line_terminator (lineterm , p) { return false ; } } } true } }
};
}
