// Generated macro for impl_31 (impl)
macro_rules! Depcrate_literalimpl_31 {
() => {
// Module: crate::literal
// Provides: {"impl_31"}
// Dependencies: {}
impl InnerLiterals { # [doc = " Create a set of inner literals from the given HIR expression."] # [doc = ""] # [doc = " If no line terminator was configured, then this always declines to"] # [doc = " extract literals because the inner literal optimization may not be"] # [doc = " valid."] # [doc = ""] # [doc = " Note that this requires the actual regex that will be used for a search"] # [doc = " because it will query some state about the compiled regex. That state"] # [doc = " may influence inner literal extraction."] pub (crate) fn new (chir : & ConfiguredHIR , re : & Regex) -> InnerLiterals { if chir . config () . line_terminator . is_none () { log :: trace ! ("skipping inner literal extraction, \
                 no line terminator is set") ; return InnerLiterals :: none () ; } if re . is_accelerated () { if ! chir . hir () . properties () . look_set () . contains_word_unicode () { log :: trace ! ("skipping inner literal extraction, \
                     existing regex is believed to already be accelerated" ,) ; return InnerLiterals :: none () ; } } if chir . hir () . properties () . is_alternation_literal () { log :: trace ! ("skipping inner literal extraction, \
                 found alternation of literals, deferring to regex engine" ,) ; return InnerLiterals :: none () ; } let seq = Extractor :: new () . extract_untagged (chir . hir ()) ; InnerLiterals { seq } } # [doc = " Returns a infinite set of inner literals, such that it can never"] # [doc = " produce a matcher."] pub (crate) fn none () -> InnerLiterals { InnerLiterals { seq : Seq :: infinite () } } # [doc = " If it is deemed advantageous to do so (via various suspicious"] # [doc = " heuristics), this will return a single regular expression pattern that"] # [doc = " matches a subset of the language matched by the regular expression that"] # [doc = " generated these literal sets. The idea here is that the pattern"] # [doc = " returned by this method is much cheaper to search for. i.e., It is"] # [doc = " usually a single literal or an alternation of literals."] pub (crate) fn one_regex (& self) -> Result < Option < Regex > , Error > { let Some (lits) = self . seq . literals () else { return Ok (None) } ; if lits . is_empty () { return Ok (None) ; } let mut alts = vec ! [] ; for lit in lits . iter () { alts . push (Hir :: literal (lit . as_bytes ())) ; } let hir = Hir :: alternation (alts) ; log :: debug ! ("extracted fast line regex: {:?}" , hir . to_string ()) ; let re = Regex :: builder () . configure (Regex :: config () . utf8_empty (false)) . build_from_hir (& hir) . map_err (Error :: regex) ? ; Ok (Some (re)) } }
};
}
