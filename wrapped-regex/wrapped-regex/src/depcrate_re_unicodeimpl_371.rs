// Generated macro for impl_371 (impl)
macro_rules! Depcrate_re_unicodeimpl_371 {
() => {
// Module: crate::re_unicode
// Provides: {"impl_371"}
// Dependencies: {}
impl Regex { # [doc = " Compiles a regular expression. Once compiled, it can be used repeatedly"] # [doc = " to search, split or replace text in a string."] # [doc = ""] # [doc = " If an invalid expression is given, then an error is returned."] pub fn new (re : & str) -> Result < Regex , Error > { Regex :: with_size_limit (10 * (1 << 20) , re) } # [doc = " Compiles a regular expression with the given size limit."] # [doc = ""] # [doc = " The size limit is applied to the size of the *compiled* data structure."] # [doc = " If the data structure exceeds the size given, then an error is"] # [doc = " returned."] pub fn with_size_limit (size : usize , re : & str) -> Result < Regex , Error > { RegexBuilder :: new (re) . size_limit (size) . compile () } # [doc = " Returns true if and only if the regex matches the string given."] # [doc = ""] # [doc = " It is recommended to use this method if all you need to do is test"] # [doc = " a match, since the underlying matching engine may be able to do less"] # [doc = " work."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Test if some text contains at least one word with exactly 13"] # [doc = " Unicode word characters:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # fn main() {"] # [doc = " let text = \"I categorically deny having triskaidekaphobia.\";"] # [doc = " assert!(Regex::new(r\"\\b\\w{13}\\b\").unwrap().is_match(text));"] # [doc = " # }"] # [doc = " ```"] pub fn is_match (& self , text : & str) -> bool { self . is_match_at (text , 0) } # [doc = " Returns the same as is_match, but starts the search at the given"] # [doc = " offset."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `start == 0`."] # [doc (hidden)] pub fn is_match_at (& self , text : & str , start : usize) -> bool { self . shortest_match_at (text , start) . is_some () } # [doc = " Returns the start and end byte range of the leftmost-first match in"] # [doc = " `text`. If no match exists, then `None` is returned."] # [doc = ""] # [doc = " Note that this should only be used if you want to discover the position"] # [doc = " of the match. Testing the existence of a match is faster if you use"] # [doc = " `is_match`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Find the start and end location of the first word with exactly 13"] # [doc = " Unicode word characters:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # fn main() {"] # [doc = " let text = \"I categorically deny having triskaidekaphobia.\";"] # [doc = " let pos = Regex::new(r\"\\b\\w{13}\\b\").unwrap().find(text);"] # [doc = " assert_eq!(pos, Some((2, 15)));"] # [doc = " # }"] # [doc = " ```"] pub fn find (& self , text : & str) -> Option < (usize , usize) > { self . find_at (text , 0) } # [doc = " Returns the same as find, but starts the search at the given"] # [doc = " offset."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `start == 0`."] # [doc (hidden)] pub fn find_at (& self , text : & str , start : usize) -> Option < (usize , usize) > { match self . 0 { _Regex :: Dynamic (ref exec) => { exec . searcher_str () . find_at (text , start) } _Regex :: Plugin (ref plug) => plug . find_at (text , start) , } } # [doc = " Returns an iterator for each successive non-overlapping match in"] # [doc = " `text`, returning the start and end byte indices with respect to"] # [doc = " `text`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Find the start and end location of every word with exactly 13 Unicode"] # [doc = " word characters:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # fn main() {"] # [doc = " let text = \"Retroactively relinquishing remunerations is reprehensible.\";"] # [doc = " for pos in Regex::new(r\"\\b\\w{13}\\b\").unwrap().find_iter(text) {"] # [doc = "     println!(\"{:?}\", pos);"] # [doc = " }"] # [doc = " // Output:"] # [doc = " // (0, 13)"] # [doc = " // (14, 27)"] # [doc = " // (28, 41)"] # [doc = " // (45, 58)"] # [doc = " # }"] # [doc = " ```"] pub fn find_iter < 'r , 't > (& 'r self , text : & 't str) -> FindMatches < 'r , 't > { match self . 0 { _Regex :: Dynamic (ref exec) => { let it = exec . searcher_str () . find_iter (text) ; FindMatches (FindMatchesInner :: Dynamic (it)) } _Regex :: Plugin (ref plug) => { let it = plug . find_iter (text) ; FindMatches (FindMatchesInner :: Plugin (it)) } } } # [doc = " Returns the capture groups corresponding to the leftmost-first"] # [doc = " match in `text`. Capture group `0` always corresponds to the entire"] # [doc = " match. If no match is found, then `None` is returned."] # [doc = ""] # [doc = " You should only use `captures` if you need access to submatches."] # [doc = " Otherwise, `find` is faster for discovering the location of the overall"] # [doc = " match."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Say you have some text with movie names and their release years,"] # [doc = " like \"'Citizen Kane' (1941)\". It'd be nice if we could search for text"] # [doc = " looking like that, while also extracting the movie name and its release"] # [doc = " year separately."] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # fn main() {"] # [doc = " let re = Regex::new(r\"'([^']+)'\\s+\\((\\d{4})\\)\").unwrap();"] # [doc = " let text = \"Not my favorite movie: 'Citizen Kane' (1941).\";"] # [doc = " let caps = re.captures(text).unwrap();"] # [doc = " assert_eq!(caps.at(1), Some(\"Citizen Kane\"));"] # [doc = " assert_eq!(caps.at(2), Some(\"1941\"));"] # [doc = " assert_eq!(caps.at(0), Some(\"'Citizen Kane' (1941)\"));"] # [doc = " // You can also access the groups by index using the Index notation."] # [doc = " // Note that this will panic on an invalid index."] # [doc = " assert_eq!(&caps[1], \"Citizen Kane\");"] # [doc = " assert_eq!(&caps[2], \"1941\");"] # [doc = " assert_eq!(&caps[0], \"'Citizen Kane' (1941)\");"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " Note that the full match is at capture group `0`. Each subsequent"] # [doc = " capture group is indexed by the order of its opening `(`."] # [doc = ""] # [doc = " We can make this example a bit clearer by using *named* capture groups:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # fn main() {"] # [doc = " let re = Regex::new(r\"'(?P<title>[^']+)'\\s+\\((?P<year>\\d{4})\\)\")"] # [doc = "                .unwrap();"] # [doc = " let text = \"Not my favorite movie: 'Citizen Kane' (1941).\";"] # [doc = " let caps = re.captures(text).unwrap();"] # [doc = " assert_eq!(caps.name(\"title\"), Some(\"Citizen Kane\"));"] # [doc = " assert_eq!(caps.name(\"year\"), Some(\"1941\"));"] # [doc = " assert_eq!(caps.at(0), Some(\"'Citizen Kane' (1941)\"));"] # [doc = " // You can also access the groups by name using the Index notation."] # [doc = " // Note that this will panic on an invalid group name."] # [doc = " assert_eq!(&caps[\"title\"], \"Citizen Kane\");"] # [doc = " assert_eq!(&caps[\"year\"], \"1941\");"] # [doc = " assert_eq!(&caps[0], \"'Citizen Kane' (1941)\");"] # [doc = ""] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " Here we name the capture groups, which we can access with the `name`"] # [doc = " method or the `Index` notation with a `&str`. Note that the named"] # [doc = " capture groups are still accessible with `at` or the `Index` notation"] # [doc = " with a `usize`."] # [doc = ""] # [doc = " The `0`th capture group is always unnamed, so it must always be"] # [doc = " accessed with `at(0)` or `[0]`."] pub fn captures < 't > (& self , text : & 't str) -> Option < Captures < 't > > { let mut slots = vec ! [None ; 2 * self . captures_len ()] ; self . read_captures_at (& mut slots , text , 0) . map (| _ | Captures { text : text , slots : slots , named_groups : NamedGroups :: from_regex (self) }) } # [doc = " Returns the same as captures, but starts the search at the given"] # [doc = " offset and populates the capture locations given."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `start == 0`."] # [doc (hidden)] pub fn read_captures_at (& self , slots : & mut [Slot] , text : & str , start : usize ,) -> Option < (usize , usize) > { match self . 0 { _Regex :: Dynamic (ref exec) => { exec . searcher_str () . read_captures_at (slots , text , start) } _Regex :: Plugin (ref plug) => { plug . read_captures_at (slots , text , start) } } } # [doc = " Returns an iterator over all the non-overlapping capture groups matched"] # [doc = " in `text`. This is operationally the same as `find_iter`, except it"] # [doc = " yields information about submatches."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " We can use this to find all movie titles and their release years in"] # [doc = " some text, where the movie is formatted like \"'Title' (xxxx)\":"] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # fn main() {"] # [doc = " let re = Regex::new(r\"'(?P<title>[^']+)'\\s+\\((?P<year>\\d{4})\\)\")"] # [doc = "                .unwrap();"] # [doc = " let text = \"'Citizen Kane' (1941), 'The Wizard of Oz' (1939), 'M' (1931).\";"] # [doc = " for caps in re.captures_iter(text) {"] # [doc = "     println!(\"Movie: {:?}, Released: {:?}\","] # [doc = "              caps.name(\"title\"), caps.name(\"year\"));"] # [doc = " }"] # [doc = " // Output:"] # [doc = " // Movie: Citizen Kane, Released: 1941"] # [doc = " // Movie: The Wizard of Oz, Released: 1939"] # [doc = " // Movie: M, Released: 1931"] # [doc = " # }"] # [doc = " ```"] pub fn captures_iter < 'r , 't > (& 'r self , text : & 't str ,) -> FindCaptures < 'r , 't > { match self . 0 { _Regex :: Dynamic (ref exec) => { let it = exec . searcher_str () . captures_iter (text) ; FindCaptures (FindCapturesInner :: Dynamic (it)) } _Regex :: Plugin (ref plug) => { let it = plug . captures_iter (text) ; FindCaptures (FindCapturesInner :: Plugin (it)) } } } # [doc = " Returns an iterator of substrings of `text` delimited by a match of the"] # [doc = " regular expression. Namely, each element of the iterator corresponds to"] # [doc = " text that *isn't* matched by the regular expression."] # [doc = ""] # [doc = " This method will *not* copy the text given."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " To split a string delimited by arbitrary amounts of spaces or tabs:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # fn main() {"] # [doc = " let re = Regex::new(r\"[ \\t]+\").unwrap();"] # [doc = " let fields: Vec<&str> = re.split(\"a b \\t  c\\td    e\").collect();"] # [doc = " assert_eq!(fields, vec![\"a\", \"b\", \"c\", \"d\", \"e\"]);"] # [doc = " # }"] # [doc = " ```"] pub fn split < 'r , 't > (& 'r self , text : & 't str) -> RegexSplits < 'r , 't > { RegexSplits { finder : self . find_iter (text) , last : 0 , } } # [doc = " Returns an iterator of at most `limit` substrings of `text` delimited"] # [doc = " by a match of the regular expression. (A `limit` of `0` will return no"] # [doc = " substrings.) Namely, each element of the iterator corresponds to text"] # [doc = " that *isn't* matched by the regular expression. The remainder of the"] # [doc = " string that is not split will be the last element in the iterator."] # [doc = ""] # [doc = " This method will *not* copy the text given."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Get the first two words in some text:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # fn main() {"] # [doc = " let re = Regex::new(r\"\\W+\").unwrap();"] # [doc = " let fields: Vec<&str> = re.splitn(\"Hey! How are you?\", 3).collect();"] # [doc = " assert_eq!(fields, vec!(\"Hey\", \"How\", \"are you?\"));"] # [doc = " # }"] # [doc = " ```"] pub fn splitn < 'r , 't > (& 'r self , text : & 't str , limit : usize) -> RegexSplitsN < 'r , 't > { RegexSplitsN { splits : self . split (text) , n : limit , } } # [doc = " Replaces the leftmost-first match with the replacement provided."] # [doc = " The replacement can be a regular string (where `$N` and `$name` are"] # [doc = " expanded to match capture groups) or a function that takes the matches'"] # [doc = " `Captures` and returns the replaced string."] # [doc = ""] # [doc = " If no match is found, then a copy of the string is returned unchanged."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Note that this function is polymorphic with respect to the replacement."] # [doc = " In typical usage, this can just be a normal string:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # fn main() {"] # [doc = " let re = Regex::new(\"[^01]+\").unwrap();"] # [doc = " assert_eq!(re.replace(\"1078910\", \"\"), \"1010\");"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " But anything satisfying the `Replacer` trait will work. For example,"] # [doc = " a closure of type `|&Captures| -> String` provides direct access to the"] # [doc = " captures corresponding to a match. This allows one to access"] # [doc = " submatches easily:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # use regex::Captures; fn main() {"] # [doc = " let re = Regex::new(r\"([^,\\s]+),\\s+(\\S+)\").unwrap();"] # [doc = " let result = re.replace(\"Springsteen, Bruce\", |caps: &Captures| {"] # [doc = "     format!(\"{} {}\", caps.at(2).unwrap_or(\"\"), caps.at(1).unwrap_or(\"\"))"] # [doc = " });"] # [doc = " assert_eq!(result, \"Bruce Springsteen\");"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " But this is a bit cumbersome to use all the time. Instead, a simple"] # [doc = " syntax is supported that expands `$name` into the corresponding capture"] # [doc = " group. Here's the last example, but using this expansion technique"] # [doc = " with named capture groups:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # fn main() {"] # [doc = " let re = Regex::new(r\"(?P<last>[^,\\s]+),\\s+(?P<first>\\S+)\").unwrap();"] # [doc = " let result = re.replace(\"Springsteen, Bruce\", \"$first $last\");"] # [doc = " assert_eq!(result, \"Bruce Springsteen\");"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " Note that using `$2` instead of `$first` or `$1` instead of `$last`"] # [doc = " would produce the same result. To write a literal `$` use `$$`."] # [doc = ""] # [doc = " Finally, sometimes you just want to replace a literal string with no"] # [doc = " submatch expansion. This can be done by wrapping a string with"] # [doc = " `NoExpand`:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # fn main() {"] # [doc = " use regex::NoExpand;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?P<last>[^,\\s]+),\\s+(\\S+)\").unwrap();"] # [doc = " let result = re.replace(\"Springsteen, Bruce\", NoExpand(\"$2 $last\"));"] # [doc = " assert_eq!(result, \"$2 $last\");"] # [doc = " # }"] # [doc = " ```"] pub fn replace < R : Replacer > (& self , text : & str , rep : R) -> String { self . replacen (text , 1 , rep) } # [doc = " Replaces all non-overlapping matches in `text` with the replacement"] # [doc = " provided. This is the same as calling `replacen` with `limit` set to"] # [doc = " `0`."] # [doc = ""] # [doc = " See the documentation for `replace` for details on how to access"] # [doc = " submatches in the replacement string."] pub fn replace_all < R : Replacer > (& self , text : & str , rep : R) -> String { self . replacen (text , 0 , rep) } # [doc = " Replaces at most `limit` non-overlapping matches in `text` with the"] # [doc = " replacement provided. If `limit` is 0, then all non-overlapping matches"] # [doc = " are replaced."] # [doc = ""] # [doc = " See the documentation for `replace` for details on how to access"] # [doc = " submatches in the replacement string."] pub fn replacen < R : Replacer > (& self , text : & str , limit : usize , mut rep : R ,) -> String { if let Some (rep) = rep . no_expand () { let mut new = String :: with_capacity (text . len ()) ; let mut last_match = 0 ; for (i , (s , e)) in self . find_iter (text) . enumerate () { if limit > 0 && i >= limit { break } new . push_str (& text [last_match .. s]) ; new . push_str (& rep) ; last_match = e ; } new . push_str (& text [last_match ..]) ; return new ; } let mut new = String :: with_capacity (text . len ()) ; let mut last_match = 0 ; for (i , cap) in self . captures_iter (text) . enumerate () { if limit > 0 && i >= limit { break } let (s , e) = cap . pos (0) . unwrap () ; new . push_str (& text [last_match .. s]) ; new . push_str (& rep . reg_replace (& cap)) ; last_match = e ; } new . push_str (& text [last_match ..]) ; new } # [doc = " Returns the end location of a match in the text given."] # [doc = ""] # [doc = " This method may have the same performance characteristics as"] # [doc = " `is_match`, except it provides an end location for a match. In"] # [doc = " particular, the location returned *may be shorter* than the proper end"] # [doc = " of the leftmost-first match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Typically, `a+` would match the entire first sequence of `a` in some"] # [doc = " text, but `shortest_match` can give up as soon as it sees the first"] # [doc = " `a`."] # [doc = ""] # [doc = " ```rust"] # [doc = " # extern crate regex; use regex::Regex;"] # [doc = " # fn main() {"] # [doc = " let text = \"aaaaa\";"] # [doc = " let pos = Regex::new(r\"a+\").unwrap().shortest_match(text);"] # [doc = " assert_eq!(pos, Some(1));"] # [doc = " # }"] # [doc = " ```"] pub fn shortest_match (& self , text : & str) -> Option < usize > { self . shortest_match_at (text , 0) } # [doc = " Returns the same as shortest_match, but starts the search at the given"] # [doc = " offset."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `start == 0`."] # [doc (hidden)] pub fn shortest_match_at (& self , text : & str , start : usize ,) -> Option < usize > { match self . 0 { _Regex :: Dynamic (ref exec) => { exec . searcher_str () . shortest_match_at (text , start) } _Regex :: Plugin (ref plug) => plug . shortest_match_at (text , start) , } } # [doc = " Returns the original string of this regex."] pub fn as_str (& self) -> & str { match self . 0 { _Regex :: Dynamic (ref exec) => & exec . regex_strings () [0] , _Regex :: Plugin (ref plug) => & plug . original , } } # [doc = " Returns an iterator over the capture names."] pub fn capture_names (& self) -> CaptureNames { CaptureNames (match self . 0 { _Regex :: Plugin (ref n) => _CaptureNames :: Plugin (n . names . iter ()) , _Regex :: Dynamic (ref d) => { _CaptureNames :: Dynamic (d . capture_names () . iter ()) } }) } # [doc = " Returns the number of captures."] pub fn captures_len (& self) -> usize { match self . 0 { _Regex :: Plugin (ref n) => n . names . len () , _Regex :: Dynamic (ref d) => d . capture_names () . len () } } }
};
}
