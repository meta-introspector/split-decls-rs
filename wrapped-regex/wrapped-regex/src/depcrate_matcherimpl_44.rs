// Generated macro for impl_44 (impl)
macro_rules! Depcrate_matcherimpl_44 {
() => {
// Module: crate::matcher
// Provides: {"impl_44"}
// Dependencies: {}
impl RegexMatcherBuilder { # [doc = " Create a new builder for configuring a regex matcher."] pub fn new () -> RegexMatcherBuilder { RegexMatcherBuilder { config : Config :: default () } } # [doc = " Build a new matcher using the current configuration for the provided"] # [doc = " pattern."] # [doc = ""] # [doc = " The syntax supported is documented as part of the regex crate:"] # [doc = " <https://docs.rs/regex/#syntax>."] pub fn build (& self , pattern : & str) -> Result < RegexMatcher , Error > { self . build_many (& [pattern]) } # [doc = " Build a new matcher using the current configuration for the provided"] # [doc = " patterns. The resulting matcher behaves as if all of the patterns"] # [doc = " given are joined together into a single alternation. That is, it"] # [doc = " reports matches where at least one of the given patterns matches."] pub fn build_many < P : AsRef < str > > (& self , patterns : & [P] ,) -> Result < RegexMatcher , Error > { let mut chir = self . config . build_many (patterns) ? ; if chir . config () . whole_line { chir = chir . into_whole_line () ; } else if chir . config () . word { chir = chir . into_word () ; } let regex = chir . to_regex () ? ; log :: trace ! ("final regex: {:?}" , chir . hir () . to_string ()) ; let non_matching_bytes = chir . non_matching_bytes () ; let fast_line_regex = InnerLiterals :: new (& chir , & regex) . one_regex () ? ; let mut config = self . config . clone () ; config . line_terminator = chir . line_terminator () ; Ok (RegexMatcher { config , regex , fast_line_regex , non_matching_bytes }) } # [doc = " Build a new matcher from a plain alternation of literals."] # [doc = ""] # [doc = " Depending on the configuration set by the builder, this may be able to"] # [doc = " build a matcher substantially faster than by joining the patterns with"] # [doc = " a `|` and calling `build`."] pub fn build_literals < B : AsRef < str > > (& self , literals : & [B] ,) -> Result < RegexMatcher , Error > { self . build_many (literals) } # [doc = " Set the value for the case insensitive (`i`) flag."] # [doc = ""] # [doc = " When enabled, letters in the pattern will match both upper case and"] # [doc = " lower case variants."] pub fn case_insensitive (& mut self , yes : bool) -> & mut RegexMatcherBuilder { self . config . case_insensitive = yes ; self } # [doc = " Whether to enable \"smart case\" or not."] # [doc = ""] # [doc = " When smart case is enabled, the builder will automatically enable"] # [doc = " case insensitive matching based on how the pattern is written. Namely,"] # [doc = " case insensitive mode is enabled when both of the following things"] # [doc = " are true:"] # [doc = ""] # [doc = " 1. The pattern contains at least one literal character. For example,"] # [doc = "    `a\\w` contains a literal (`a`) but `\\w` does not."] # [doc = " 2. Of the literals in the pattern, none of them are considered to be"] # [doc = "    uppercase according to Unicode. For example, `foo\\pL` has no"] # [doc = "    uppercase literals but `Foo\\pL` does."] pub fn case_smart (& mut self , yes : bool) -> & mut RegexMatcherBuilder { self . config . case_smart = yes ; self } # [doc = " Set the value for the multi-line matching (`m`) flag."] # [doc = ""] # [doc = " When enabled, `^` matches the beginning of lines and `$` matches the"] # [doc = " end of lines."] # [doc = ""] # [doc = " By default, they match beginning/end of the input."] pub fn multi_line (& mut self , yes : bool) -> & mut RegexMatcherBuilder { self . config . multi_line = yes ; self } # [doc = " Set the value for the any character (`s`) flag, where in `.` matches"] # [doc = " anything when `s` is set and matches anything except for new line when"] # [doc = " it is not set (the default)."] # [doc = ""] # [doc = " N.B. \"matches anything\" means \"any byte\" when Unicode is disabled and"] # [doc = " means \"any valid UTF-8 encoding of any Unicode scalar value\" when"] # [doc = " Unicode is enabled."] pub fn dot_matches_new_line (& mut self , yes : bool ,) -> & mut RegexMatcherBuilder { self . config . dot_matches_new_line = yes ; self } # [doc = " Set the value for the greedy swap (`U`) flag."] # [doc = ""] # [doc = " When enabled, a pattern like `a*` is lazy (tries to find shortest"] # [doc = " match) and `a*?` is greedy (tries to find longest match)."] # [doc = ""] # [doc = " By default, `a*` is greedy and `a*?` is lazy."] pub fn swap_greed (& mut self , yes : bool) -> & mut RegexMatcherBuilder { self . config . swap_greed = yes ; self } # [doc = " Set the value for the ignore whitespace (`x`) flag."] # [doc = ""] # [doc = " When enabled, whitespace such as new lines and spaces will be ignored"] # [doc = " between expressions of the pattern, and `#` can be used to start a"] # [doc = " comment until the next new line."] pub fn ignore_whitespace (& mut self , yes : bool ,) -> & mut RegexMatcherBuilder { self . config . ignore_whitespace = yes ; self } # [doc = " Set the value for the Unicode (`u`) flag."] # [doc = ""] # [doc = " Enabled by default. When disabled, character classes such as `\\w` only"] # [doc = " match ASCII word characters instead of all Unicode word characters."] pub fn unicode (& mut self , yes : bool) -> & mut RegexMatcherBuilder { self . config . unicode = yes ; self } # [doc = " Whether to support octal syntax or not."] # [doc = ""] # [doc = " Octal syntax is a little-known way of uttering Unicode codepoints in"] # [doc = " a regular expression. For example, `a`, `\\x61`, `\\u0061` and"] # [doc = " `\\141` are all equivalent regular expressions, where the last example"] # [doc = " shows octal syntax."] # [doc = ""] # [doc = " While supporting octal syntax isn't in and of itself a problem, it does"] # [doc = " make good error messages harder. That is, in PCRE based regex engines,"] # [doc = " syntax like `\\0` invokes a backreference, which is explicitly"] # [doc = " unsupported in Rust's regex engine. However, many users expect it to"] # [doc = " be supported. Therefore, when octal support is disabled, the error"] # [doc = " message will explicitly mention that backreferences aren't supported."] # [doc = ""] # [doc = " Octal syntax is disabled by default."] pub fn octal (& mut self , yes : bool) -> & mut RegexMatcherBuilder { self . config . octal = yes ; self } # [doc = " Set the approximate size limit of the compiled regular expression."] # [doc = ""] # [doc = " This roughly corresponds to the number of bytes occupied by a single"] # [doc = " compiled program. If the program exceeds this number, then a"] # [doc = " compilation error is returned."] pub fn size_limit (& mut self , bytes : usize) -> & mut RegexMatcherBuilder { self . config . size_limit = bytes ; self } # [doc = " Set the approximate size of the cache used by the DFA."] # [doc = ""] # [doc = " This roughly corresponds to the number of bytes that the DFA will"] # [doc = " use while searching."] # [doc = ""] # [doc = " Note that this is a *per thread* limit. There is no way to set a global"] # [doc = " limit. In particular, if a regex is used from multiple threads"] # [doc = " simultaneously, then each thread may use up to the number of bytes"] # [doc = " specified here."] pub fn dfa_size_limit (& mut self , bytes : usize ,) -> & mut RegexMatcherBuilder { self . config . dfa_size_limit = bytes ; self } # [doc = " Set the nesting limit for this parser."] # [doc = ""] # [doc = " The nesting limit controls how deep the abstract syntax tree is allowed"] # [doc = " to be. If the AST exceeds the given limit (e.g., with too many nested"] # [doc = " groups), then an error is returned by the parser."] # [doc = ""] # [doc = " The purpose of this limit is to act as a heuristic to prevent stack"] # [doc = " overflow for consumers that do structural induction on an `Ast` using"] # [doc = " explicit recursion. While this crate never does this (instead using"] # [doc = " constant stack space and moving the call stack to the heap), other"] # [doc = " crates may."] # [doc = ""] # [doc = " This limit is not checked until the entire Ast is parsed. Therefore,"] # [doc = " if callers want to put a limit on the amount of heap space used, then"] # [doc = " they should impose a limit on the length, in bytes, of the concrete"] # [doc = " pattern string. In particular, this is viable since this parser"] # [doc = " implementation will limit itself to heap space proportional to the"] # [doc = " length of the pattern string."] # [doc = ""] # [doc = " Note that a nest limit of `0` will return a nest limit error for most"] # [doc = " patterns but not all. For example, a nest limit of `0` permits `a` but"] # [doc = " not `ab`, since `ab` requires a concatenation, which results in a nest"] # [doc = " depth of `1`. In general, a nest limit is not something that manifests"] # [doc = " in an obvious way in the concrete syntax, therefore, it should not be"] # [doc = " used in a granular way."] pub fn nest_limit (& mut self , limit : u32) -> & mut RegexMatcherBuilder { self . config . nest_limit = limit ; self } # [doc = " Set an ASCII line terminator for the matcher."] # [doc = ""] # [doc = " The purpose of setting a line terminator is to enable a certain class"] # [doc = " of optimizations that can make line oriented searching faster. Namely,"] # [doc = " when a line terminator is enabled, then the builder will guarantee that"] # [doc = " the resulting matcher will never be capable of producing a match that"] # [doc = " contains the line terminator. Because of this guarantee, users of the"] # [doc = " resulting matcher do not need to slowly execute a search line by line"] # [doc = " for line oriented search."] # [doc = ""] # [doc = " If the aforementioned guarantee about not matching a line terminator"] # [doc = " cannot be made because of how the pattern was written, then the builder"] # [doc = " will return an error when attempting to construct the matcher. For"] # [doc = " example, the pattern `a\\sb` will be transformed such that it can never"] # [doc = " match `a\\nb` (when `\\n` is the line terminator), but the pattern `a\\nb`"] # [doc = " will result in an error since the `\\n` cannot be easily removed without"] # [doc = " changing the fundamental intent of the pattern."] # [doc = ""] # [doc = " If the given line terminator isn't an ASCII byte (`<=127`), then the"] # [doc = " builder will return an error when constructing the matcher."] pub fn line_terminator (& mut self , line_term : Option < u8 > ,) -> & mut RegexMatcherBuilder { self . config . line_terminator = line_term . map (LineTerminator :: byte) ; self } # [doc = " Ban a byte from occurring in a regular expression pattern."] # [doc = ""] # [doc = " If this byte is found in the regex pattern, then an error will be"] # [doc = " returned at construction time."] # [doc = ""] # [doc = " This is useful when binary detection is enabled. Callers will likely"] # [doc = " want to ban the same byte that is used to detect binary data, i.e.,"] # [doc = " the NUL byte. The reason for this is that when binary detection is"] # [doc = " enabled, it's impossible to match a NUL byte because binary detection"] # [doc = " will either quit when one is found, or will convert NUL bytes to line"] # [doc = " terminators to avoid exorbitant heap usage."] pub fn ban_byte (& mut self , byte : Option < u8 >) -> & mut RegexMatcherBuilder { self . config . ban = byte ; self } # [doc = " Set the line terminator to `\\r\\n` and enable CRLF matching for `$` in"] # [doc = " regex patterns."] # [doc = ""] # [doc = " This method sets two distinct settings:"] # [doc = ""] # [doc = " 1. It causes the line terminator for the matcher to be `\\r\\n`. Namely,"] # [doc = "    this prevents the matcher from ever producing a match that contains"] # [doc = "    a `\\r` or `\\n`."] # [doc = " 2. It enables CRLF mode for `^` and `$`. This means that line anchors"] # [doc = "    will treat both `\\r` and `\\n` as line terminators, but will never"] # [doc = "    match between a `\\r` and `\\n`."] # [doc = ""] # [doc = " Note that if you do not wish to set the line terminator but would"] # [doc = " still like `$` to match `\\r\\n` line terminators, then it is valid to"] # [doc = " call `crlf(true)` followed by `line_terminator(None)`. Ordering is"] # [doc = " important, since `crlf` sets the line terminator, but `line_terminator`"] # [doc = " does not touch the `crlf` setting."] pub fn crlf (& mut self , yes : bool) -> & mut RegexMatcherBuilder { if yes { self . config . line_terminator = Some (LineTerminator :: crlf ()) ; } else { self . config . line_terminator = None ; } self . config . crlf = yes ; self } # [doc = " Require that all matches occur on word boundaries."] # [doc = ""] # [doc = " Enabling this option is subtly different than putting `\\b` assertions"] # [doc = " on both sides of your pattern. In particular, a `\\b` assertion requires"] # [doc = " that one side of it match a word character while the other match a"] # [doc = " non-word character. This option, in contrast, merely requires that"] # [doc = " one side match a non-word character."] # [doc = ""] # [doc = " For example, `\\b-2\\b` will not match `foo -2 bar` since `-` is not a"] # [doc = " word character. However, `-2` with this `word` option enabled will"] # [doc = " match the `-2` in `foo -2 bar`."] pub fn word (& mut self , yes : bool) -> & mut RegexMatcherBuilder { self . config . word = yes ; self } # [doc = " Whether the patterns should be treated as literal strings or not. When"] # [doc = " this is active, all characters, including ones that would normally be"] # [doc = " special regex meta characters, are matched literally."] pub fn fixed_strings (& mut self , yes : bool) -> & mut RegexMatcherBuilder { self . config . fixed_strings = yes ; self } # [doc = " Whether each pattern should match the entire line or not. This is"] # [doc = " equivalent to surrounding the pattern with `(?m:^)` and `(?m:$)`."] pub fn whole_line (& mut self , yes : bool) -> & mut RegexMatcherBuilder { self . config . whole_line = yes ; self } }
};
}
