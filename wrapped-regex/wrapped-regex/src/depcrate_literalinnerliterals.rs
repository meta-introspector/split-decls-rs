// Generated macro for InnerLiterals (struct)
macro_rules! Depcrate_literalInnerLiterals {
() => {
// Module: crate::literal
// Provides: {"InnerLiterals"}
// Dependencies: {}
# [doc = " A type that encapsulates \"inner\" literal extractiong from a regex."] # [doc = ""] # [doc = " It uses a huge pile of heuristics to try to pluck out literals from a regex"] # [doc = " that are in turn used to build a simpler regex that is more amenable to"] # [doc = " optimization."] # [doc = ""] # [doc = " The main idea underlying the validity of this technique is the fact"] # [doc = " that ripgrep searches individuals lines and not across lines. (Unless"] # [doc = " -U/--multiline is enabled.) Namely, we can pluck literals out of the regex,"] # [doc = " search for them, find the bounds of the line in which that literal occurs"] # [doc = " and then run the original regex on only that line. This overall works"] # [doc = " really really well in throughput oriented searches because it potentially"] # [doc = " allows ripgrep to spend a lot more time in a fast vectorized routine for"] # [doc = " finding literals as opposed to the (much) slower regex engine."] # [doc = ""] # [doc = " This optimization was far more important in the old days, but since then,"] # [doc = " Rust's regex engine has actually grown its own (albeit limited) support for"] # [doc = " inner literal optimizations. So this technique doesn't apply as much as it"] # [doc = " used to."] # [doc = ""] # [doc = " A good example of a regex where this particular extractor helps is"] # [doc = " `\\s+(Sherlock|[A-Z]atso[a-z]|Moriarty)\\s+`. The `[A-Z]` before the `atso`"] # [doc = " in particular is what inhibits the regex engine's own inner literal"] # [doc = " optimizations from kicking in. This particular regex also did not have any"] # [doc = " inner literals extracted in the old implementation (ripgrep <=13). So this"] # [doc = " particular implementation represents a strict improvement from both the old"] # [doc = " implementation and from the regex engine's own optimizations. (Which could"] # [doc = " in theory be improved still.)"] # [derive (Clone , Debug)] pub (crate) struct InnerLiterals { seq : Seq , }
};
}
