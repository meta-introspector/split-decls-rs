// Generated macro for impl_463 (impl)
macro_rules! Depcrate_simd_accel_teddy128impl_463 {
() => {
// Module: crate::simd_accel::teddy128
// Provides: {"impl_463"}
// Dependencies: {}
impl Masks { # [doc = " Create a new set of masks of size `n`, where `n` corresponds to the"] # [doc = " number of bytes in a fingerprint."] fn new (n : usize) -> Masks { Masks (vec ! [Mask :: new () ; n]) } # [doc = " Returns the number of masks."] fn len (& self) -> usize { self . 0 . len () } # [doc = " Adds the given pattern to the given bucket. The bucket should be a"] # [doc = " power of `2 <= 2^7`."] fn add (& mut self , bucket : u8 , pat : & [u8]) { for (i , mask) in self . 0 . iter_mut () . enumerate () { mask . add (bucket , pat [i]) ; } } # [doc = " Finds the fingerprints that are in the given haystack block. i.e., this"] # [doc = " returns `C` as described in the module documentation."] # [doc = ""] # [doc = " More specifically, `for i in 0..16` and `j in 0..8, C[i][j] == 1` if and"] # [doc = " only if `haystack_block[i]` corresponds to a fingerprint that is part"] # [doc = " of a pattern in bucket `j`."] # [inline (always)] fn members1 (& self , haystack_block : u8x16) -> u8x16 { let masklo = u8x16 :: splat (0xF) ; let hlo = haystack_block & masklo ; let hhi = (haystack_block >> 4) & masklo ; self . 0 [0] . lo . shuffle_bytes (hlo) & self . 0 [0] . hi . shuffle_bytes (hhi) } # [doc = " Like members1, but computes C for the first and second bytes in the"] # [doc = " fingerprint."] # [inline (always)] fn members2 (& self , haystack_block : u8x16) -> (u8x16 , u8x16) { let masklo = u8x16 :: splat (0xF) ; let hlo = haystack_block & masklo ; let hhi = (haystack_block >> 4) & masklo ; let res0 = self . 0 [0] . lo . shuffle_bytes (hlo) & self . 0 [0] . hi . shuffle_bytes (hhi) ; let res1 = self . 0 [1] . lo . shuffle_bytes (hlo) & self . 0 [1] . hi . shuffle_bytes (hhi) ; (res0 , res1) } # [doc = " Like `members1`, but computes `C` for the first, second and third bytes"] # [doc = " in the fingerprint."] # [inline (always)] fn members3 (& self , haystack_block : u8x16) -> (u8x16 , u8x16 , u8x16) { let masklo = u8x16 :: splat (0xF) ; let hlo = haystack_block & masklo ; let hhi = (haystack_block >> 4) & masklo ; let res0 = self . 0 [0] . lo . shuffle_bytes (hlo) & self . 0 [0] . hi . shuffle_bytes (hhi) ; let res1 = self . 0 [1] . lo . shuffle_bytes (hlo) & self . 0 [1] . hi . shuffle_bytes (hhi) ; let res2 = self . 0 [2] . lo . shuffle_bytes (hlo) & self . 0 [2] . hi . shuffle_bytes (hhi) ; (res0 , res1 , res2) } }
};
}
