// Generated macro for Regex (struct)
macro_rules! Depcrate_re_unicodeRegex {
() => {
// Module: crate::re_unicode
// Provides: {"Regex"}
// Dependencies: {}
# [doc = " A compiled regular expression for matching Unicode strings."] # [doc = ""] # [doc = " It is represented as either a sequence of bytecode instructions (dynamic)"] # [doc = " or as a specialized Rust function (native). It can be used to search, split"] # [doc = " or replace text. All searching is done with an implicit `.*?` at the"] # [doc = " beginning and end of an expression. To force an expression to match the"] # [doc = " whole string (or a prefix or a suffix), you must use an anchor like `^` or"] # [doc = " `$` (or `\\A` and `\\z`)."] # [doc = ""] # [doc = " While this crate will handle Unicode strings (whether in the regular"] # [doc = " expression or in the search text), all positions returned are **byte"] # [doc = " indices**. Every byte index is guaranteed to be at a Unicode code point"] # [doc = " boundary."] # [doc = ""] # [doc = " The lifetimes `'r` and `'t` in this crate correspond to the lifetime of a"] # [doc = " compiled regular expression and text to search, respectively."] # [doc = ""] # [doc = " The only methods that allocate new strings are the string replacement"] # [doc = " methods. All other methods (searching and splitting) return borrowed"] # [doc = " pointers into the string given."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Find the location of a US phone number:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use regex::Regex;"] # [doc = " let re = Regex::new(\"[0-9]{3}-[0-9]{3}-[0-9]{4}\").unwrap();"] # [doc = " assert_eq!(re.find(\"phone: 111-222-3333\"), Some((7, 19)));"] # [doc = " ```"] # [doc = ""] # [doc = " # Using the `std::str::StrExt` methods with `Regex`"] # [doc = ""] # [doc = " > **Note**: This section requires that this crate is currently compiled with"] # [doc = " >           the `pattern` Cargo feature enabled."] # [doc = ""] # [doc = " Since `Regex` implements `Pattern`, you can use regexes with methods"] # [doc = " defined on `std::str::StrExt`. For example, `is_match`, `find`, `find_iter`"] # [doc = " and `split` can be replaced with `StrExt::contains`, `StrExt::find`,"] # [doc = " `StrExt::match_indices` and `StrExt::split`."] # [doc = ""] # [doc = " Here are some examples:"] # [doc = ""] # [doc = " ```rust,ignore"] # [doc = " # use regex::Regex;"] # [doc = " let re = Regex::new(r\"\\d+\").unwrap();"] # [doc = " let haystack = \"a111b222c\";"] # [doc = ""] # [doc = " assert!(haystack.contains(&re));"] # [doc = " assert_eq!(haystack.find(&re), Some(1));"] # [doc = " assert_eq!(haystack.match_indices(&re).collect::<Vec<_>>(),"] # [doc = "            vec![(1, 4), (5, 8)]);"] # [doc = " assert_eq!(haystack.split(&re).collect::<Vec<_>>(), vec![\"a\", \"b\", \"c\"]);"] # [doc = " ```"] # [derive (Clone)] pub struct Regex (# [doc (hidden)] pub _Regex) ;
};
}
