// Generated macro for CacheInner (struct)
macro_rules! Depcrate_dfaCacheInner {
() => {
// Module: crate::dfa
// Provides: {"CacheInner"}
// Dependencies: {}
# [doc = " CacheInner is logically just a part of Cache, but groups together fields"] # [doc = " that aren't passed as function parameters throughout search. (This split"] # [doc = " is mostly an artifact of the borrow checker. It is happily paid.)"] # [derive (Clone , Debug)] struct CacheInner { # [doc = " A cache of pre-compiled DFA states, keyed by the set of NFA states"] # [doc = " and the set of empty-width flags set at the byte in the input when the"] # [doc = " state was observed."] # [doc = ""] # [doc = " A StatePtr is effectively a `*State`, but to avoid various inconvenient"] # [doc = " things, we just pass indexes around manually. The performance impact of"] # [doc = " this is probably an instruction or two in the inner loop. However, on"] # [doc = " 64 bit, each StatePtr is half the size of a *State."] compiled : HashMap < State , StatePtr > , # [doc = " The transition table."] # [doc = ""] # [doc = " The transition table is laid out in row-major order, where states are"] # [doc = " rows and the transitions for each state are columns. At a high level,"] # [doc = " given state `s` and byte `b`, the next state can be found at index"] # [doc = " `s * 256 + b`."] # [doc = ""] # [doc = " This is, of course, a lie. A StatePtr is actually a pointer to the"] # [doc = " *start* of a row in this table. When indexing in the DFA's inner loop,"] # [doc = " this removes the need to multiply the StatePtr by the stride. Yes, it"] # [doc = " matters. This reduces the number of states we can store, but: the"] # [doc = " stride is rarely 256 since we define transitions in terms of"] # [doc = " *equivalence classes* of bytes. Each class corresponds to a set of"] # [doc = " bytes that never discriminate a distinct path through the DFA from each"] # [doc = " other."] trans : Transitions , # [doc = " Our set of states. Note that `StatePtr / num_byte_classes` indexes"] # [doc = " this Vec rather than just a `StatePtr`."] states : Vec < State > , # [doc = " A set of cached start states, which are limited to the number of"] # [doc = " permutations of flags set just before the initial byte of input. (The"] # [doc = " index into this vec is a `EmptyFlags`.)"] # [doc = ""] # [doc = " N.B. A start state can be \"dead\" (i.e., no possible match), so we"] # [doc = " represent it with a StatePtr."] start_states : Vec < StatePtr > , # [doc = " Stack scratch space used to follow epsilon transitions in the NFA."] # [doc = " (This permits us to avoid recursion.)"] # [doc = ""] # [doc = " The maximum stack size is the number of NFA states."] stack : Vec < InstPtr > , # [doc = " The total number of times this cache has been flushed by the DFA"] # [doc = " because of space constraints."] flush_count : u64 , # [doc = " The total heap size of the DFA's cache. We use this to determine when"] # [doc = " we should flush the cache."] size : usize , }
};
}
