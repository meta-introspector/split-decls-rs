// Generated macro for bytes (module)
macro_rules! Depcrate_buildersbytes {
() => {
// Module: crate::builders
// Provides: {"bytes"}
// Dependencies: {}
pub (crate) mod bytes { use crate :: { bytes :: { Regex , RegexSet } , error :: Error , } ; use super :: Builder ; # [doc = " A configurable builder for a [`Regex`]."] # [doc = ""] # [doc = " This builder can be used to programmatically set flags such as `i`"] # [doc = " (case insensitive) and `x` (for verbose mode). This builder can also be"] # [doc = " used to configure things like the line terminator and a size limit on"] # [doc = " the compiled regular expression."] # [derive (Clone , Debug)] pub struct RegexBuilder { builder : Builder , } impl RegexBuilder { # [doc = " Create a new builder with a default configuration for the given"] # [doc = " pattern."] # [doc = ""] # [doc = " If the pattern is invalid or exceeds the configured size limits,"] # [doc = " then an error will be returned when [`RegexBuilder::build`] is"] # [doc = " called."] pub fn new (pattern : & str) -> RegexBuilder { RegexBuilder { builder : Builder :: new ([pattern]) } } # [doc = " Compiles the pattern given to `RegexBuilder::new` with the"] # [doc = " configuration set on this builder."] # [doc = ""] # [doc = " If the pattern isn't a valid regex or if a configured size limit"] # [doc = " was exceeded, then an error is returned."] pub fn build (& self) -> Result < Regex , Error > { self . builder . build_one_bytes () } # [doc = " This configures Unicode mode for the entire pattern."] # [doc = ""] # [doc = " Enabling Unicode mode does a number of things:"] # [doc = ""] # [doc = " * Most fundamentally, it causes the fundamental atom of matching"] # [doc = " to be a single codepoint. When Unicode mode is disabled, it's a"] # [doc = " single byte. For example, when Unicode mode is enabled, `.` will"] # [doc = " match `💩` once, where as it will match 4 times when Unicode mode"] # [doc = " is disabled. (Since the UTF-8 encoding of `💩` is 4 bytes long.)"] # [doc = " * Case insensitive matching uses Unicode simple case folding rules."] # [doc = " * Unicode character classes like `\\p{Letter}` and `\\p{Greek}` are"] # [doc = " available."] # [doc = " * Perl character classes are Unicode aware. That is, `\\w`, `\\s` and"] # [doc = " `\\d`."] # [doc = " * The word boundary assertions, `\\b` and `\\B`, use the Unicode"] # [doc = " definition of a word character."] # [doc = ""] # [doc = " Note that unlike the top-level `Regex` for searching `&str`, it"] # [doc = " is permitted to disable Unicode mode even if the resulting pattern"] # [doc = " could match invalid UTF-8. For example, `(?-u:.)` is not a valid"] # [doc = " pattern for a top-level `Regex`, but is valid for a `bytes::Regex`."] # [doc = ""] # [doc = " For more details on the Unicode support in this crate, see the"] # [doc = " [Unicode section](crate#unicode) in this crate's top-level"] # [doc = " documentation."] # [doc = ""] # [doc = " The default for this is `true`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"\\w\")"] # [doc = "     .unicode(false)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " // Normally greek letters would be included in \\w, but since"] # [doc = " // Unicode mode is disabled, it only matches ASCII letters."] # [doc = " assert!(!re.is_match(\"δ\".as_bytes()));"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"s\")"] # [doc = "     .case_insensitive(true)"] # [doc = "     .unicode(false)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " // Normally 'ſ' is included when searching for 's' case"] # [doc = " // insensitively due to Unicode's simple case folding rules. But"] # [doc = " // when Unicode mode is disabled, only ASCII case insensitive rules"] # [doc = " // are used."] # [doc = " assert!(!re.is_match(\"ſ\".as_bytes()));"] # [doc = " ```"] # [doc = ""] # [doc = " Since this builder is for constructing a [`bytes::Regex`](Regex),"] # [doc = " one can disable Unicode mode even if it would match invalid UTF-8:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\".\")"] # [doc = "     .unicode(false)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " // Normally greek letters would be included in \\w, but since"] # [doc = " // Unicode mode is disabled, it only matches ASCII letters."] # [doc = " assert!(re.is_match(b\"\\xFF\"));"] # [doc = " ```"] pub fn unicode (& mut self , yes : bool) -> & mut RegexBuilder { self . builder . unicode (yes) ; self } # [doc = " This configures whether to enable case insensitive matching for the"] # [doc = " entire pattern."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `i`"] # [doc = " in the pattern. For example, `(?i:foo)` matches `foo` case"] # [doc = " insensitively while `(?-i:foo)` matches `foo` case sensitively."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"foo(?-i:bar)quux\")"] # [doc = "     .case_insensitive(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert!(re.is_match(b\"FoObarQuUx\"));"] # [doc = " // Even though case insensitive matching is enabled in the builder,"] # [doc = " // it can be locally disabled within the pattern. In this case,"] # [doc = " // `bar` is matched case sensitively."] # [doc = " assert!(!re.is_match(b\"fooBARquux\"));"] # [doc = " ```"] pub fn case_insensitive (& mut self , yes : bool) -> & mut RegexBuilder { self . builder . case_insensitive (yes) ; self } # [doc = " This configures multi-line mode for the entire pattern."] # [doc = ""] # [doc = " Enabling multi-line mode changes the behavior of the `^` and `$`"] # [doc = " anchor assertions. Instead of only matching at the beginning and"] # [doc = " end of a haystack, respectively, multi-line mode causes them to"] # [doc = " match at the beginning and end of a line *in addition* to the"] # [doc = " beginning and end of a haystack. More precisely, `^` will match at"] # [doc = " the position immediately following a `\\n` and `$` will match at the"] # [doc = " position immediately preceding a `\\n`."] # [doc = ""] # [doc = " The behavior of this option can be impacted by other settings too:"] # [doc = ""] # [doc = " * The [`RegexBuilder::line_terminator`] option changes `\\n` above"] # [doc = " to any ASCII byte."] # [doc = " * The [`RegexBuilder::crlf`] option changes the line terminator to"] # [doc = " be either `\\r` or `\\n`, but never at the position between a `\\r`"] # [doc = " and `\\n`."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `m` in"] # [doc = " the pattern."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"^foo$\")"] # [doc = "     .multi_line(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert_eq!(Some(1..4), re.find(b\"\\nfoo\\n\").map(|m| m.range()));"] # [doc = " ```"] pub fn multi_line (& mut self , yes : bool) -> & mut RegexBuilder { self . builder . multi_line (yes) ; self } # [doc = " This configures dot-matches-new-line mode for the entire pattern."] # [doc = ""] # [doc = " Perhaps surprisingly, the default behavior for `.` is not to match"] # [doc = " any character, but rather, to match any character except for the"] # [doc = " line terminator (which is `\\n` by default). When this mode is"] # [doc = " enabled, the behavior changes such that `.` truly matches any"] # [doc = " character."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `s` in"] # [doc = " the pattern. For example, `(?s:.)` and `\\p{any}` are equivalent"] # [doc = " regexes."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"foo.bar\")"] # [doc = "     .dot_matches_new_line(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " let hay = b\"foo\\nbar\";"] # [doc = " assert_eq!(Some(&b\"foo\\nbar\"[..]), re.find(hay).map(|m| m.as_bytes()));"] # [doc = " ```"] pub fn dot_matches_new_line (& mut self , yes : bool ,) -> & mut RegexBuilder { self . builder . dot_matches_new_line (yes) ; self } # [doc = " This configures CRLF mode for the entire pattern."] # [doc = ""] # [doc = " When CRLF mode is enabled, both `\\r` (\"carriage return\" or CR for"] # [doc = " short) and `\\n` (\"line feed\" or LF for short) are treated as line"] # [doc = " terminators. This results in the following:"] # [doc = ""] # [doc = " * Unless dot-matches-new-line mode is enabled, `.` will now match"] # [doc = " any character except for `\\n` and `\\r`."] # [doc = " * When multi-line mode is enabled, `^` will match immediately"] # [doc = " following a `\\n` or a `\\r`. Similarly, `$` will match immediately"] # [doc = " preceding a `\\n` or a `\\r`. Neither `^` nor `$` will ever match"] # [doc = " between `\\r` and `\\n`."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `R` in"] # [doc = " the pattern."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"^foo$\")"] # [doc = "     .multi_line(true)"] # [doc = "     .crlf(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " let hay = b\"\\r\\nfoo\\r\\n\";"] # [doc = " // If CRLF mode weren't enabled here, then '$' wouldn't match"] # [doc = " // immediately after 'foo', and thus no match would be found."] # [doc = " assert_eq!(Some(&b\"foo\"[..]), re.find(hay).map(|m| m.as_bytes()));"] # [doc = " ```"] # [doc = ""] # [doc = " This example demonstrates that `^` will never match at a position"] # [doc = " between `\\r` and `\\n`. (`$` will similarly not match between a `\\r`"] # [doc = " and a `\\n`.)"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"^\")"] # [doc = "     .multi_line(true)"] # [doc = "     .crlf(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " let hay = b\"\\r\\n\\r\\n\";"] # [doc = " let ranges: Vec<_> = re.find_iter(hay).map(|m| m.range()).collect();"] # [doc = " assert_eq!(ranges, vec![0..0, 2..2, 4..4]);"] # [doc = " ```"] pub fn crlf (& mut self , yes : bool) -> & mut RegexBuilder { self . builder . crlf (yes) ; self } # [doc = " Configures the line terminator to be used by the regex."] # [doc = ""] # [doc = " The line terminator is relevant in two ways for a particular regex:"] # [doc = ""] # [doc = " * When dot-matches-new-line mode is *not* enabled (the default),"] # [doc = " then `.` will match any character except for the configured line"] # [doc = " terminator."] # [doc = " * When multi-line mode is enabled (not the default), then `^` and"] # [doc = " `$` will match immediately after and before, respectively, a line"] # [doc = " terminator."] # [doc = ""] # [doc = " In both cases, if CRLF mode is enabled in a particular context,"] # [doc = " then it takes precedence over any configured line terminator."] # [doc = ""] # [doc = " This option cannot be configured from within the pattern."] # [doc = ""] # [doc = " The default line terminator is `\\n`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to treat the NUL byte as a line terminator. This can"] # [doc = " be a useful heuristic when searching binary data."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"^foo$\")"] # [doc = "     .multi_line(true)"] # [doc = "     .line_terminator(b'\\x00')"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " let hay = b\"\\x00foo\\x00\";"] # [doc = " assert_eq!(Some(1..4), re.find(hay).map(|m| m.range()));"] # [doc = " ```"] # [doc = ""] # [doc = " This example shows that the behavior of `.` is impacted by this"] # [doc = " setting as well:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\".\")"] # [doc = "     .line_terminator(b'\\x00')"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert!(re.is_match(b\"\\n\"));"] # [doc = " assert!(!re.is_match(b\"\\x00\"));"] # [doc = " ```"] # [doc = ""] # [doc = " This shows that building a regex will work even when the byte"] # [doc = " given is not ASCII. This is unlike the top-level `Regex` API where"] # [doc = " matching invalid UTF-8 is not allowed."] # [doc = ""] # [doc = " Note though that you must disable Unicode mode. This is required"] # [doc = " because Unicode mode requires matching one codepoint at a time,"] # [doc = " and there is no way to match a non-ASCII byte as if it were a"] # [doc = " codepoint."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " assert!("] # [doc = "     RegexBuilder::new(r\".\")"] # [doc = "         .unicode(false)"] # [doc = "         .line_terminator(0x80)"] # [doc = "         .build()"] # [doc = "         .is_ok(),"] # [doc = " );"] # [doc = " ```"] pub fn line_terminator (& mut self , byte : u8) -> & mut RegexBuilder { self . builder . line_terminator (byte) ; self } # [doc = " This configures swap-greed mode for the entire pattern."] # [doc = ""] # [doc = " When swap-greed mode is enabled, patterns like `a+` will become"] # [doc = " non-greedy and patterns like `a+?` will become greedy. In other"] # [doc = " words, the meanings of `a+` and `a+?` are switched."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `U` in"] # [doc = " the pattern."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"a+\")"] # [doc = "     .swap_greed(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert_eq!(Some(&b\"a\"[..]), re.find(b\"aaa\").map(|m| m.as_bytes()));"] # [doc = " ```"] pub fn swap_greed (& mut self , yes : bool) -> & mut RegexBuilder { self . builder . swap_greed (yes) ; self } # [doc = " This configures verbose mode for the entire pattern."] # [doc = ""] # [doc = " When enabled, whitespace will treated as insignificant in the"] # [doc = " pattern and `#` can be used to start a comment until the next new"] # [doc = " line."] # [doc = ""] # [doc = " Normally, in most places in a pattern, whitespace is treated"] # [doc = " literally. For example ` +` will match one or more ASCII whitespace"] # [doc = " characters."] # [doc = ""] # [doc = " When verbose mode is enabled, `\\#` can be used to match a literal"] # [doc = " `#` and `\\ ` can be used to match a literal ASCII whitespace"] # [doc = " character."] # [doc = ""] # [doc = " Verbose mode is useful for permitting regexes to be formatted and"] # [doc = " broken up more nicely. This may make them more easily readable."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `x` in"] # [doc = " the pattern."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " let pat = r\""] # [doc = "     \\b"] # [doc = "     (?<first>\\p{Uppercase}\\w*)  # always start with uppercase letter"] # [doc = "     [\\s--\\n]+                   # whitespace should separate names"] # [doc = "     (?: # middle name can be an initial!"] # [doc = "         (?:(?<initial>\\p{Uppercase})\\.|(?<middle>\\p{Uppercase}\\w*))"] # [doc = "         [\\s--\\n]+"] # [doc = "     )?"] # [doc = "     (?<last>\\p{Uppercase}\\w*)"] # [doc = "     \\b"] # [doc = " \";"] # [doc = " let re = RegexBuilder::new(pat)"] # [doc = "     .ignore_whitespace(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = ""] # [doc = " let caps = re.captures(b\"Harry Potter\").unwrap();"] # [doc = " assert_eq!(&b\"Harry\"[..], &caps[\"first\"]);"] # [doc = " assert_eq!(&b\"Potter\"[..], &caps[\"last\"]);"] # [doc = ""] # [doc = " let caps = re.captures(b\"Harry J. Potter\").unwrap();"] # [doc = " assert_eq!(&b\"Harry\"[..], &caps[\"first\"]);"] # [doc = " // Since a middle name/initial isn't required for an overall match,"] # [doc = " // we can't assume that 'initial' or 'middle' will be populated!"] # [doc = " assert_eq!("] # [doc = "     Some(&b\"J\"[..]),"] # [doc = "     caps.name(\"initial\").map(|m| m.as_bytes()),"] # [doc = " );"] # [doc = " assert_eq!(None, caps.name(\"middle\").map(|m| m.as_bytes()));"] # [doc = " assert_eq!(&b\"Potter\"[..], &caps[\"last\"]);"] # [doc = ""] # [doc = " let caps = re.captures(b\"Harry James Potter\").unwrap();"] # [doc = " assert_eq!(&b\"Harry\"[..], &caps[\"first\"]);"] # [doc = " // Since a middle name/initial isn't required for an overall match,"] # [doc = " // we can't assume that 'initial' or 'middle' will be populated!"] # [doc = " assert_eq!(None, caps.name(\"initial\").map(|m| m.as_bytes()));"] # [doc = " assert_eq!("] # [doc = "     Some(&b\"James\"[..]),"] # [doc = "     caps.name(\"middle\").map(|m| m.as_bytes()),"] # [doc = " );"] # [doc = " assert_eq!(&b\"Potter\"[..], &caps[\"last\"]);"] # [doc = " ```"] pub fn ignore_whitespace (& mut self , yes : bool) -> & mut RegexBuilder { self . builder . ignore_whitespace (yes) ; self } # [doc = " This configures octal mode for the entire pattern."] # [doc = ""] # [doc = " Octal syntax is a little-known way of uttering Unicode codepoints"] # [doc = " in a pattern. For example, `a`, `\\x61`, `\\u0061` and `\\141` are all"] # [doc = " equivalent patterns, where the last example shows octal syntax."] # [doc = ""] # [doc = " While supporting octal syntax isn't in and of itself a problem,"] # [doc = " it does make good error messages harder. That is, in PCRE based"] # [doc = " regex engines, syntax like `\\1` invokes a backreference, which is"] # [doc = " explicitly unsupported this library. However, many users expect"] # [doc = " backreferences to be supported. Therefore, when octal support"] # [doc = " is disabled, the error message will explicitly mention that"] # [doc = " backreferences aren't supported."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " // Normally this pattern would not compile, with an error message"] # [doc = " // about backreferences not being supported. But with octal mode"] # [doc = " // enabled, octal escape sequences work."] # [doc = " let re = RegexBuilder::new(r\"\\141\")"] # [doc = "     .octal(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert!(re.is_match(b\"a\"));"] # [doc = " ```"] pub fn octal (& mut self , yes : bool) -> & mut RegexBuilder { self . builder . octal (yes) ; self } # [doc = " Sets the approximate size limit, in bytes, of the compiled regex."] # [doc = ""] # [doc = " This roughly corresponds to the number of heap memory, in"] # [doc = " bytes, occupied by a single regex. If the regex would otherwise"] # [doc = " approximately exceed this limit, then compiling that regex will"] # [doc = " fail."] # [doc = ""] # [doc = " The main utility of a method like this is to avoid compiling"] # [doc = " regexes that use an unexpected amount of resources, such as"] # [doc = " time and memory. Even if the memory usage of a large regex is"] # [doc = " acceptable, its search time may not be. Namely, worst case time"] # [doc = " complexity for search is `O(m * n)`, where `m ~ len(pattern)` and"] # [doc = " `n ~ len(haystack)`. That is, search time depends, in part, on the"] # [doc = " size of the compiled regex. This means that putting a limit on the"] # [doc = " size of the regex limits how much a regex can impact search time."] # [doc = ""] # [doc = " For more information about regex size limits, see the section on"] # [doc = " [untrusted inputs](crate#untrusted-input) in the top-level crate"] # [doc = " documentation."] # [doc = ""] # [doc = " The default for this is some reasonable number that permits most"] # [doc = " patterns to compile successfully."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # if !cfg!(target_pointer_width = \"64\") { return; } // see #1041"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " // It may surprise you how big some seemingly small patterns can"] # [doc = " // be! Since \\w is Unicode aware, this generates a regex that can"] # [doc = " // match approximately 140,000 distinct codepoints."] # [doc = " assert!(RegexBuilder::new(r\"\\w\").size_limit(45_000).build().is_err());"] # [doc = " ```"] pub fn size_limit (& mut self , bytes : usize) -> & mut RegexBuilder { self . builder . size_limit (bytes) ; self } # [doc = " Set the approximate capacity, in bytes, of the cache of transitions"] # [doc = " used by the lazy DFA."] # [doc = ""] # [doc = " While the lazy DFA isn't always used, in tends to be the most"] # [doc = " commonly use regex engine in default configurations. It tends to"] # [doc = " adopt the performance profile of a fully build DFA, but without the"] # [doc = " downside of taking worst case exponential time to build."] # [doc = ""] # [doc = " The downside is that it needs to keep a cache of transitions and"] # [doc = " states that are built while running a search, and this cache"] # [doc = " can fill up. When it fills up, the cache will reset itself. Any"] # [doc = " previously generated states and transitions will then need to be"] # [doc = " re-generated. If this happens too many times, then this library"] # [doc = " will bail out of using the lazy DFA and switch to a different regex"] # [doc = " engine."] # [doc = ""] # [doc = " If your regex provokes this particular downside of the lazy DFA,"] # [doc = " then it may be beneficial to increase its cache capacity. This will"] # [doc = " potentially reduce the frequency of cache resetting (ideally to"] # [doc = " `0`). While it won't fix all potential performance problems with"] # [doc = " the lazy DFA, increasing the cache capacity does fix some."] # [doc = ""] # [doc = " There is no easy way to determine, a priori, whether increasing"] # [doc = " this cache capacity will help. In general, the larger your regex,"] # [doc = " the more cache it's likely to use. But that isn't an ironclad rule."] # [doc = " For example, a regex like `[01]*1[01]{N}` would normally produce a"] # [doc = " fully build DFA that is exponential in size with respect to `N`."] # [doc = " The lazy DFA will prevent exponential space blow-up, but it cache"] # [doc = " is likely to fill up, even when it's large and even for smallish"] # [doc = " values of `N`."] # [doc = ""] # [doc = " If you aren't sure whether this helps or not, it is sensible to"] # [doc = " set this to some arbitrarily large number in testing, such as"] # [doc = " `usize::MAX`. Namely, this represents the amount of capacity that"] # [doc = " *may* be used. It's probably not a good idea to use `usize::MAX` in"] # [doc = " production though, since it implies there are no controls on heap"] # [doc = " memory used by this library during a search. In effect, set it to"] # [doc = " whatever you're willing to allocate for a single regex search."] pub fn dfa_size_limit (& mut self , bytes : usize) -> & mut RegexBuilder { self . builder . dfa_size_limit (bytes) ; self } # [doc = " Set the nesting limit for this parser."] # [doc = ""] # [doc = " The nesting limit controls how deep the abstract syntax tree is"] # [doc = " allowed to be. If the AST exceeds the given limit (e.g., with too"] # [doc = " many nested groups), then an error is returned by the parser."] # [doc = ""] # [doc = " The purpose of this limit is to act as a heuristic to prevent stack"] # [doc = " overflow for consumers that do structural induction on an AST using"] # [doc = " explicit recursion. While this crate never does this (instead using"] # [doc = " constant stack space and moving the call stack to the heap), other"] # [doc = " crates may."] # [doc = ""] # [doc = " This limit is not checked until the entire AST is parsed."] # [doc = " Therefore, if callers want to put a limit on the amount of heap"] # [doc = " space used, then they should impose a limit on the length, in"] # [doc = " bytes, of the concrete pattern string. In particular, this is"] # [doc = " viable since this parser implementation will limit itself to heap"] # [doc = " space proportional to the length of the pattern string. See also"] # [doc = " the [untrusted inputs](crate#untrusted-input) section in the"] # [doc = " top-level crate documentation for more information about this."] # [doc = ""] # [doc = " Note that a nest limit of `0` will return a nest limit error for"] # [doc = " most patterns but not all. For example, a nest limit of `0` permits"] # [doc = " `a` but not `ab`, since `ab` requires an explicit concatenation,"] # [doc = " which results in a nest depth of `1`. In general, a nest limit is"] # [doc = " not something that manifests in an obvious way in the concrete"] # [doc = " syntax, therefore, it should not be used in a granular way."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexBuilder;"] # [doc = ""] # [doc = " assert!(RegexBuilder::new(r\"a\").nest_limit(0).build().is_ok());"] # [doc = " assert!(RegexBuilder::new(r\"ab\").nest_limit(0).build().is_err());"] # [doc = " ```"] pub fn nest_limit (& mut self , limit : u32) -> & mut RegexBuilder { self . builder . nest_limit (limit) ; self } } # [doc = " A configurable builder for a [`RegexSet`]."] # [doc = ""] # [doc = " This builder can be used to programmatically set flags such as `i`"] # [doc = " (case insensitive) and `x` (for verbose mode). This builder can also be"] # [doc = " used to configure things like the line terminator and a size limit on"] # [doc = " the compiled regular expression."] # [derive (Clone , Debug)] pub struct RegexSetBuilder { builder : Builder , } impl RegexSetBuilder { # [doc = " Create a new builder with a default configuration for the given"] # [doc = " patterns."] # [doc = ""] # [doc = " If the patterns are invalid or exceed the configured size limits,"] # [doc = " then an error will be returned when [`RegexSetBuilder::build`] is"] # [doc = " called."] pub fn new < I , S > (patterns : I) -> RegexSetBuilder where I : IntoIterator < Item = S > , S : AsRef < str > , { RegexSetBuilder { builder : Builder :: new (patterns) } } # [doc = " Compiles the patterns given to `RegexSetBuilder::new` with the"] # [doc = " configuration set on this builder."] # [doc = ""] # [doc = " If the patterns aren't valid regexes or if a configured size limit"] # [doc = " was exceeded, then an error is returned."] pub fn build (& self) -> Result < RegexSet , Error > { self . builder . build_many_bytes () } # [doc = " This configures Unicode mode for the all of the patterns."] # [doc = ""] # [doc = " Enabling Unicode mode does a number of things:"] # [doc = ""] # [doc = " * Most fundamentally, it causes the fundamental atom of matching"] # [doc = " to be a single codepoint. When Unicode mode is disabled, it's a"] # [doc = " single byte. For example, when Unicode mode is enabled, `.` will"] # [doc = " match `💩` once, where as it will match 4 times when Unicode mode"] # [doc = " is disabled. (Since the UTF-8 encoding of `💩` is 4 bytes long.)"] # [doc = " * Case insensitive matching uses Unicode simple case folding rules."] # [doc = " * Unicode character classes like `\\p{Letter}` and `\\p{Greek}` are"] # [doc = " available."] # [doc = " * Perl character classes are Unicode aware. That is, `\\w`, `\\s` and"] # [doc = " `\\d`."] # [doc = " * The word boundary assertions, `\\b` and `\\B`, use the Unicode"] # [doc = " definition of a word character."] # [doc = ""] # [doc = " Note that unlike the top-level `RegexSet` for searching `&str`,"] # [doc = " it is permitted to disable Unicode mode even if the resulting"] # [doc = " pattern could match invalid UTF-8. For example, `(?-u:.)` is not"] # [doc = " a valid pattern for a top-level `RegexSet`, but is valid for a"] # [doc = " `bytes::RegexSet`."] # [doc = ""] # [doc = " For more details on the Unicode support in this crate, see the"] # [doc = " [Unicode section](crate#unicode) in this crate's top-level"] # [doc = " documentation."] # [doc = ""] # [doc = " The default for this is `true`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " let re = RegexSetBuilder::new([r\"\\w\"])"] # [doc = "     .unicode(false)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " // Normally greek letters would be included in \\w, but since"] # [doc = " // Unicode mode is disabled, it only matches ASCII letters."] # [doc = " assert!(!re.is_match(\"δ\".as_bytes()));"] # [doc = ""] # [doc = " let re = RegexSetBuilder::new([r\"s\"])"] # [doc = "     .case_insensitive(true)"] # [doc = "     .unicode(false)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " // Normally 'ſ' is included when searching for 's' case"] # [doc = " // insensitively due to Unicode's simple case folding rules. But"] # [doc = " // when Unicode mode is disabled, only ASCII case insensitive rules"] # [doc = " // are used."] # [doc = " assert!(!re.is_match(\"ſ\".as_bytes()));"] # [doc = " ```"] # [doc = ""] # [doc = " Since this builder is for constructing a"] # [doc = " [`bytes::RegexSet`](RegexSet), one can disable Unicode mode even if"] # [doc = " it would match invalid UTF-8:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " let re = RegexSetBuilder::new([r\".\"])"] # [doc = "     .unicode(false)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " // Normally greek letters would be included in \\w, but since"] # [doc = " // Unicode mode is disabled, it only matches ASCII letters."] # [doc = " assert!(re.is_match(b\"\\xFF\"));"] # [doc = " ```"] pub fn unicode (& mut self , yes : bool) -> & mut RegexSetBuilder { self . builder . unicode (yes) ; self } # [doc = " This configures whether to enable case insensitive matching for all"] # [doc = " of the patterns."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `i`"] # [doc = " in the pattern. For example, `(?i:foo)` matches `foo` case"] # [doc = " insensitively while `(?-i:foo)` matches `foo` case sensitively."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " let re = RegexSetBuilder::new([r\"foo(?-i:bar)quux\"])"] # [doc = "     .case_insensitive(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert!(re.is_match(b\"FoObarQuUx\"));"] # [doc = " // Even though case insensitive matching is enabled in the builder,"] # [doc = " // it can be locally disabled within the pattern. In this case,"] # [doc = " // `bar` is matched case sensitively."] # [doc = " assert!(!re.is_match(b\"fooBARquux\"));"] # [doc = " ```"] pub fn case_insensitive (& mut self , yes : bool) -> & mut RegexSetBuilder { self . builder . case_insensitive (yes) ; self } # [doc = " This configures multi-line mode for all of the patterns."] # [doc = ""] # [doc = " Enabling multi-line mode changes the behavior of the `^` and `$`"] # [doc = " anchor assertions. Instead of only matching at the beginning and"] # [doc = " end of a haystack, respectively, multi-line mode causes them to"] # [doc = " match at the beginning and end of a line *in addition* to the"] # [doc = " beginning and end of a haystack. More precisely, `^` will match at"] # [doc = " the position immediately following a `\\n` and `$` will match at the"] # [doc = " position immediately preceding a `\\n`."] # [doc = ""] # [doc = " The behavior of this option can be impacted by other settings too:"] # [doc = ""] # [doc = " * The [`RegexSetBuilder::line_terminator`] option changes `\\n`"] # [doc = " above to any ASCII byte."] # [doc = " * The [`RegexSetBuilder::crlf`] option changes the line terminator"] # [doc = " to be either `\\r` or `\\n`, but never at the position between a `\\r`"] # [doc = " and `\\n`."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `m` in"] # [doc = " the pattern."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " let re = RegexSetBuilder::new([r\"^foo$\"])"] # [doc = "     .multi_line(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert!(re.is_match(b\"\\nfoo\\n\"));"] # [doc = " ```"] pub fn multi_line (& mut self , yes : bool) -> & mut RegexSetBuilder { self . builder . multi_line (yes) ; self } # [doc = " This configures dot-matches-new-line mode for the entire pattern."] # [doc = ""] # [doc = " Perhaps surprisingly, the default behavior for `.` is not to match"] # [doc = " any character, but rather, to match any character except for the"] # [doc = " line terminator (which is `\\n` by default). When this mode is"] # [doc = " enabled, the behavior changes such that `.` truly matches any"] # [doc = " character."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `s` in"] # [doc = " the pattern. For example, `(?s:.)` and `\\p{any}` are equivalent"] # [doc = " regexes."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " let re = RegexSetBuilder::new([r\"foo.bar\"])"] # [doc = "     .dot_matches_new_line(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " let hay = b\"foo\\nbar\";"] # [doc = " assert!(re.is_match(hay));"] # [doc = " ```"] pub fn dot_matches_new_line (& mut self , yes : bool ,) -> & mut RegexSetBuilder { self . builder . dot_matches_new_line (yes) ; self } # [doc = " This configures CRLF mode for all of the patterns."] # [doc = ""] # [doc = " When CRLF mode is enabled, both `\\r` (\"carriage return\" or CR for"] # [doc = " short) and `\\n` (\"line feed\" or LF for short) are treated as line"] # [doc = " terminators. This results in the following:"] # [doc = ""] # [doc = " * Unless dot-matches-new-line mode is enabled, `.` will now match"] # [doc = " any character except for `\\n` and `\\r`."] # [doc = " * When multi-line mode is enabled, `^` will match immediately"] # [doc = " following a `\\n` or a `\\r`. Similarly, `$` will match immediately"] # [doc = " preceding a `\\n` or a `\\r`. Neither `^` nor `$` will ever match"] # [doc = " between `\\r` and `\\n`."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `R` in"] # [doc = " the pattern."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " let re = RegexSetBuilder::new([r\"^foo$\"])"] # [doc = "     .multi_line(true)"] # [doc = "     .crlf(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " let hay = b\"\\r\\nfoo\\r\\n\";"] # [doc = " // If CRLF mode weren't enabled here, then '$' wouldn't match"] # [doc = " // immediately after 'foo', and thus no match would be found."] # [doc = " assert!(re.is_match(hay));"] # [doc = " ```"] # [doc = ""] # [doc = " This example demonstrates that `^` will never match at a position"] # [doc = " between `\\r` and `\\n`. (`$` will similarly not match between a `\\r`"] # [doc = " and a `\\n`.)"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " let re = RegexSetBuilder::new([r\"^\\n\"])"] # [doc = "     .multi_line(true)"] # [doc = "     .crlf(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert!(!re.is_match(b\"\\r\\n\"));"] # [doc = " ```"] pub fn crlf (& mut self , yes : bool) -> & mut RegexSetBuilder { self . builder . crlf (yes) ; self } # [doc = " Configures the line terminator to be used by the regex."] # [doc = ""] # [doc = " The line terminator is relevant in two ways for a particular regex:"] # [doc = ""] # [doc = " * When dot-matches-new-line mode is *not* enabled (the default),"] # [doc = " then `.` will match any character except for the configured line"] # [doc = " terminator."] # [doc = " * When multi-line mode is enabled (not the default), then `^` and"] # [doc = " `$` will match immediately after and before, respectively, a line"] # [doc = " terminator."] # [doc = ""] # [doc = " In both cases, if CRLF mode is enabled in a particular context,"] # [doc = " then it takes precedence over any configured line terminator."] # [doc = ""] # [doc = " This option cannot be configured from within the pattern."] # [doc = ""] # [doc = " The default line terminator is `\\n`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to treat the NUL byte as a line terminator. This can"] # [doc = " be a useful heuristic when searching binary data."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " let re = RegexSetBuilder::new([r\"^foo$\"])"] # [doc = "     .multi_line(true)"] # [doc = "     .line_terminator(b'\\x00')"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " let hay = b\"\\x00foo\\x00\";"] # [doc = " assert!(re.is_match(hay));"] # [doc = " ```"] # [doc = ""] # [doc = " This example shows that the behavior of `.` is impacted by this"] # [doc = " setting as well:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " let re = RegexSetBuilder::new([r\".\"])"] # [doc = "     .line_terminator(b'\\x00')"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert!(re.is_match(b\"\\n\"));"] # [doc = " assert!(!re.is_match(b\"\\x00\"));"] # [doc = " ```"] # [doc = ""] # [doc = " This shows that building a regex will work even when the byte given"] # [doc = " is not ASCII. This is unlike the top-level `RegexSet` API where"] # [doc = " matching invalid UTF-8 is not allowed."] # [doc = ""] # [doc = " Note though that you must disable Unicode mode. This is required"] # [doc = " because Unicode mode requires matching one codepoint at a time,"] # [doc = " and there is no way to match a non-ASCII byte as if it were a"] # [doc = " codepoint."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " assert!("] # [doc = "     RegexSetBuilder::new([r\".\"])"] # [doc = "         .unicode(false)"] # [doc = "         .line_terminator(0x80)"] # [doc = "         .build()"] # [doc = "         .is_ok(),"] # [doc = " );"] # [doc = " ```"] pub fn line_terminator (& mut self , byte : u8) -> & mut RegexSetBuilder { self . builder . line_terminator (byte) ; self } # [doc = " This configures swap-greed mode for all of the patterns."] # [doc = ""] # [doc = " When swap-greed mode is enabled, patterns like `a+` will become"] # [doc = " non-greedy and patterns like `a+?` will become greedy. In other"] # [doc = " words, the meanings of `a+` and `a+?` are switched."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `U` in"] # [doc = " the pattern."] # [doc = ""] # [doc = " Note that this is generally not useful for a `RegexSet` since a"] # [doc = " `RegexSet` can only report whether a pattern matches or not. Since"] # [doc = " greediness never impacts whether a match is found or not (only the"] # [doc = " offsets of the match), it follows that whether parts of a pattern"] # [doc = " are greedy or not doesn't matter for a `RegexSet`."] # [doc = ""] # [doc = " The default for this is `false`."] pub fn swap_greed (& mut self , yes : bool) -> & mut RegexSetBuilder { self . builder . swap_greed (yes) ; self } # [doc = " This configures verbose mode for all of the patterns."] # [doc = ""] # [doc = " When enabled, whitespace will treated as insignificant in the"] # [doc = " pattern and `#` can be used to start a comment until the next new"] # [doc = " line."] # [doc = ""] # [doc = " Normally, in most places in a pattern, whitespace is treated"] # [doc = " literally. For example ` +` will match one or more ASCII whitespace"] # [doc = " characters."] # [doc = ""] # [doc = " When verbose mode is enabled, `\\#` can be used to match a literal"] # [doc = " `#` and `\\ ` can be used to match a literal ASCII whitespace"] # [doc = " character."] # [doc = ""] # [doc = " Verbose mode is useful for permitting regexes to be formatted and"] # [doc = " broken up more nicely. This may make them more easily readable."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `x` in"] # [doc = " the pattern."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " let pat = r\""] # [doc = "     \\b"] # [doc = "     (?<first>\\p{Uppercase}\\w*)  # always start with uppercase letter"] # [doc = "     [\\s--\\n]+                   # whitespace should separate names"] # [doc = "     (?: # middle name can be an initial!"] # [doc = "         (?:(?<initial>\\p{Uppercase})\\.|(?<middle>\\p{Uppercase}\\w*))"] # [doc = "         [\\s--\\n]+"] # [doc = "     )?"] # [doc = "     (?<last>\\p{Uppercase}\\w*)"] # [doc = "     \\b"] # [doc = " \";"] # [doc = " let re = RegexSetBuilder::new([pat])"] # [doc = "     .ignore_whitespace(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert!(re.is_match(b\"Harry Potter\"));"] # [doc = " assert!(re.is_match(b\"Harry J. Potter\"));"] # [doc = " assert!(re.is_match(b\"Harry James Potter\"));"] # [doc = " assert!(!re.is_match(b\"harry J. Potter\"));"] # [doc = " ```"] pub fn ignore_whitespace (& mut self , yes : bool ,) -> & mut RegexSetBuilder { self . builder . ignore_whitespace (yes) ; self } # [doc = " This configures octal mode for all of the patterns."] # [doc = ""] # [doc = " Octal syntax is a little-known way of uttering Unicode codepoints"] # [doc = " in a pattern. For example, `a`, `\\x61`, `\\u0061` and `\\141` are all"] # [doc = " equivalent patterns, where the last example shows octal syntax."] # [doc = ""] # [doc = " While supporting octal syntax isn't in and of itself a problem,"] # [doc = " it does make good error messages harder. That is, in PCRE based"] # [doc = " regex engines, syntax like `\\1` invokes a backreference, which is"] # [doc = " explicitly unsupported this library. However, many users expect"] # [doc = " backreferences to be supported. Therefore, when octal support"] # [doc = " is disabled, the error message will explicitly mention that"] # [doc = " backreferences aren't supported."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " // Normally this pattern would not compile, with an error message"] # [doc = " // about backreferences not being supported. But with octal mode"] # [doc = " // enabled, octal escape sequences work."] # [doc = " let re = RegexSetBuilder::new([r\"\\141\"])"] # [doc = "     .octal(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert!(re.is_match(b\"a\"));"] # [doc = " ```"] pub fn octal (& mut self , yes : bool) -> & mut RegexSetBuilder { self . builder . octal (yes) ; self } # [doc = " Sets the approximate size limit, in bytes, of the compiled regex."] # [doc = ""] # [doc = " This roughly corresponds to the number of heap memory, in"] # [doc = " bytes, occupied by a single regex. If the regex would otherwise"] # [doc = " approximately exceed this limit, then compiling that regex will"] # [doc = " fail."] # [doc = ""] # [doc = " The main utility of a method like this is to avoid compiling"] # [doc = " regexes that use an unexpected amount of resources, such as"] # [doc = " time and memory. Even if the memory usage of a large regex is"] # [doc = " acceptable, its search time may not be. Namely, worst case time"] # [doc = " complexity for search is `O(m * n)`, where `m ~ len(pattern)` and"] # [doc = " `n ~ len(haystack)`. That is, search time depends, in part, on the"] # [doc = " size of the compiled regex. This means that putting a limit on the"] # [doc = " size of the regex limits how much a regex can impact search time."] # [doc = ""] # [doc = " For more information about regex size limits, see the section on"] # [doc = " [untrusted inputs](crate#untrusted-input) in the top-level crate"] # [doc = " documentation."] # [doc = ""] # [doc = " The default for this is some reasonable number that permits most"] # [doc = " patterns to compile successfully."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # if !cfg!(target_pointer_width = \"64\") { return; } // see #1041"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " // It may surprise you how big some seemingly small patterns can"] # [doc = " // be! Since \\w is Unicode aware, this generates a regex that can"] # [doc = " // match approximately 140,000 distinct codepoints."] # [doc = " assert!("] # [doc = "     RegexSetBuilder::new([r\"\\w\"])"] # [doc = "         .size_limit(45_000)"] # [doc = "         .build()"] # [doc = "         .is_err()"] # [doc = " );"] # [doc = " ```"] pub fn size_limit (& mut self , bytes : usize) -> & mut RegexSetBuilder { self . builder . size_limit (bytes) ; self } # [doc = " Set the approximate capacity, in bytes, of the cache of transitions"] # [doc = " used by the lazy DFA."] # [doc = ""] # [doc = " While the lazy DFA isn't always used, in tends to be the most"] # [doc = " commonly use regex engine in default configurations. It tends to"] # [doc = " adopt the performance profile of a fully build DFA, but without the"] # [doc = " downside of taking worst case exponential time to build."] # [doc = ""] # [doc = " The downside is that it needs to keep a cache of transitions and"] # [doc = " states that are built while running a search, and this cache"] # [doc = " can fill up. When it fills up, the cache will reset itself. Any"] # [doc = " previously generated states and transitions will then need to be"] # [doc = " re-generated. If this happens too many times, then this library"] # [doc = " will bail out of using the lazy DFA and switch to a different regex"] # [doc = " engine."] # [doc = ""] # [doc = " If your regex provokes this particular downside of the lazy DFA,"] # [doc = " then it may be beneficial to increase its cache capacity. This will"] # [doc = " potentially reduce the frequency of cache resetting (ideally to"] # [doc = " `0`). While it won't fix all potential performance problems with"] # [doc = " the lazy DFA, increasing the cache capacity does fix some."] # [doc = ""] # [doc = " There is no easy way to determine, a priori, whether increasing"] # [doc = " this cache capacity will help. In general, the larger your regex,"] # [doc = " the more cache it's likely to use. But that isn't an ironclad rule."] # [doc = " For example, a regex like `[01]*1[01]{N}` would normally produce a"] # [doc = " fully build DFA that is exponential in size with respect to `N`."] # [doc = " The lazy DFA will prevent exponential space blow-up, but it cache"] # [doc = " is likely to fill up, even when it's large and even for smallish"] # [doc = " values of `N`."] # [doc = ""] # [doc = " If you aren't sure whether this helps or not, it is sensible to"] # [doc = " set this to some arbitrarily large number in testing, such as"] # [doc = " `usize::MAX`. Namely, this represents the amount of capacity that"] # [doc = " *may* be used. It's probably not a good idea to use `usize::MAX` in"] # [doc = " production though, since it implies there are no controls on heap"] # [doc = " memory used by this library during a search. In effect, set it to"] # [doc = " whatever you're willing to allocate for a single regex search."] pub fn dfa_size_limit (& mut self , bytes : usize ,) -> & mut RegexSetBuilder { self . builder . dfa_size_limit (bytes) ; self } # [doc = " Set the nesting limit for this parser."] # [doc = ""] # [doc = " The nesting limit controls how deep the abstract syntax tree is"] # [doc = " allowed to be. If the AST exceeds the given limit (e.g., with too"] # [doc = " many nested groups), then an error is returned by the parser."] # [doc = ""] # [doc = " The purpose of this limit is to act as a heuristic to prevent stack"] # [doc = " overflow for consumers that do structural induction on an AST using"] # [doc = " explicit recursion. While this crate never does this (instead using"] # [doc = " constant stack space and moving the call stack to the heap), other"] # [doc = " crates may."] # [doc = ""] # [doc = " This limit is not checked until the entire AST is parsed."] # [doc = " Therefore, if callers want to put a limit on the amount of heap"] # [doc = " space used, then they should impose a limit on the length, in"] # [doc = " bytes, of the concrete pattern string. In particular, this is"] # [doc = " viable since this parser implementation will limit itself to heap"] # [doc = " space proportional to the length of the pattern string. See also"] # [doc = " the [untrusted inputs](crate#untrusted-input) section in the"] # [doc = " top-level crate documentation for more information about this."] # [doc = ""] # [doc = " Note that a nest limit of `0` will return a nest limit error for"] # [doc = " most patterns but not all. For example, a nest limit of `0` permits"] # [doc = " `a` but not `ab`, since `ab` requires an explicit concatenation,"] # [doc = " which results in a nest depth of `1`. In general, a nest limit is"] # [doc = " not something that manifests in an obvious way in the concrete"] # [doc = " syntax, therefore, it should not be used in a granular way."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSetBuilder;"] # [doc = ""] # [doc = " assert!(RegexSetBuilder::new([r\"a\"]).nest_limit(0).build().is_ok());"] # [doc = " assert!(RegexSetBuilder::new([r\"ab\"]).nest_limit(0).build().is_err());"] # [doc = " ```"] pub fn nest_limit (& mut self , limit : u32) -> & mut RegexSetBuilder { self . builder . nest_limit (limit) ; self } } }
};
}
