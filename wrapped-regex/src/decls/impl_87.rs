macro_rules! deps {
    () => {
        Regex!();
        Captures!();
        CaptureLocations!();
        Match!();
    };
}

macro_rules! impl_87 {
    () => {
        deps!();
        # [doc = " A group of advanced or \"lower level\" search methods. Some methods permit"] # [doc = " starting the search at a position greater than `0` in the haystack. Other"] # [doc = " methods permit reusing allocations, for example, when extracting the"] # [doc = " matches for capture groups."] impl Regex { # [doc = " Returns the end byte offset of the first match in the haystack given."] # [doc = ""] # [doc = " This method may have the same performance characteristics as"] # [doc = " `is_match`. Behaviorally, it doesn't just report whether it match"] # [doc = " occurs, but also the end offset for a match. In particular, the offset"] # [doc = " returned *may be shorter* than the proper end of the leftmost-first"] # [doc = " match that you would find via [`Regex::find`]."] # [doc = ""] # [doc = " Note that it is not guaranteed that this routine finds the shortest or"] # [doc = " \"earliest\" possible match. Instead, the main idea of this API is that"] # [doc = " it returns the offset at the point at which the internal regex engine"] # [doc = " has determined that a match has occurred. This may vary depending on"] # [doc = " which internal regex engine is used, and thus, the offset itself may"] # [doc = " change based on internal heuristics."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Typically, `a+` would match the entire first sequence of `a` in some"] # [doc = " haystack, but `shortest_match` *may* give up as soon as it sees the"] # [doc = " first `a`."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"a+\").unwrap();"] # [doc = " let offset = re.shortest_match(\"aaaaa\").unwrap();"] # [doc = " assert_eq!(offset, 1);"] # [doc = " ```"] # [inline] pub fn shortest_match (& self , haystack : & str) -> Option < usize > { self . shortest_match_at (haystack , 0) } # [doc = " Returns the same as [`Regex::shortest_match`], but starts the search at"] # [doc = " the given offset."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only match"] # [doc = " when `start == 0`."] # [doc = ""] # [doc = " If a match is found, the offset returned is relative to the beginning"] # [doc = " of the haystack, not the beginning of the search."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics when `start >= haystack.len() + 1`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the significance of `start` by demonstrating how it"] # [doc = " can be used to permit look-around assertions in a regex to take the"] # [doc = " surrounding context into account."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\\bchew\\b\").unwrap();"] # [doc = " let hay = \"eschew\";"] # [doc = " // We get a match here, but it's probably not intended."] # [doc = " assert_eq!(re.shortest_match(&hay[2..]), Some(4));"] # [doc = " // No match because the  assertions take the context into account."] # [doc = " assert_eq!(re.shortest_match_at(hay, 2), None);"] # [doc = " ```"] # [inline] pub fn shortest_match_at (& self , haystack : & str , start : usize ,) -> Option < usize > { let input = Input :: new (haystack) . earliest (true) . span (start .. haystack . len ()) ; self . meta . search_half (& input) . map (| hm | hm . offset ()) } # [doc = " Returns the same as [`Regex::is_match`], but starts the search at the"] # [doc = " given offset."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `start == 0`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics when `start >= haystack.len() + 1`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the significance of `start` by demonstrating how it"] # [doc = " can be used to permit look-around assertions in a regex to take the"] # [doc = " surrounding context into account."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\\bchew\\b\").unwrap();"] # [doc = " let hay = \"eschew\";"] # [doc = " // We get a match here, but it's probably not intended."] # [doc = " assert!(re.is_match(&hay[2..]));"] # [doc = " // No match because the  assertions take the context into account."] # [doc = " assert!(!re.is_match_at(hay, 2));"] # [doc = " ```"] # [inline] pub fn is_match_at (& self , haystack : & str , start : usize) -> bool { let input = Input :: new (haystack) . earliest (true) . span (start .. haystack . len ()) ; self . meta . search_half (& input) . is_some () } # [doc = " Returns the same as [`Regex::find`], but starts the search at the given"] # [doc = " offset."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `start == 0`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics when `start >= haystack.len() + 1`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the significance of `start` by demonstrating how it"] # [doc = " can be used to permit look-around assertions in a regex to take the"] # [doc = " surrounding context into account."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\\bchew\\b\").unwrap();"] # [doc = " let hay = \"eschew\";"] # [doc = " // We get a match here, but it's probably not intended."] # [doc = " assert_eq!(re.find(&hay[2..]).map(|m| m.range()), Some(0..4));"] # [doc = " // No match because the  assertions take the context into account."] # [doc = " assert_eq!(re.find_at(hay, 2), None);"] # [doc = " ```"] # [inline] pub fn find_at < 'h > (& self , haystack : & 'h str , start : usize ,) -> Option < Match < 'h > > { let input = Input :: new (haystack) . span (start .. haystack . len ()) ; self . meta . search (& input) . map (| m | Match :: new (haystack , m . start () , m . end ())) } # [doc = " Returns the same as [`Regex::captures`], but starts the search at the"] # [doc = " given offset."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `start == 0`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics when `start >= haystack.len() + 1`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the significance of `start` by demonstrating how it"] # [doc = " can be used to permit look-around assertions in a regex to take the"] # [doc = " surrounding context into account."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\\bchew\\b\").unwrap();"] # [doc = " let hay = \"eschew\";"] # [doc = " // We get a match here, but it's probably not intended."] # [doc = " assert_eq!(&re.captures(&hay[2..]).unwrap()[0], \"chew\");"] # [doc = " // No match because the  assertions take the context into account."] # [doc = " assert!(re.captures_at(hay, 2).is_none());"] # [doc = " ```"] # [inline] pub fn captures_at < 'h > (& self , haystack : & 'h str , start : usize ,) -> Option < Captures < 'h > > { let input = Input :: new (haystack) . span (start .. haystack . len ()) ; let mut caps = self . meta . create_captures () ; self . meta . search_captures (& input , & mut caps) ; if caps . is_match () { let static_captures_len = self . static_captures_len () ; Some (Captures { haystack , caps , static_captures_len }) } else { None } } # [doc = " This is like [`Regex::captures`], but writes the byte offsets of each"] # [doc = " capture group match into the locations given."] # [doc = ""] # [doc = " A [`CaptureLocations`] stores the same byte offsets as a [`Captures`],"] # [doc = " but does *not* store a reference to the haystack. This makes its API"] # [doc = " a bit lower level and less convenient. But in exchange, callers"] # [doc = " may allocate their own `CaptureLocations` and reuse it for multiple"] # [doc = " searches. This may be helpful if allocating a `Captures` shows up in a"] # [doc = " profile as too costly."] # [doc = ""] # [doc = " To create a `CaptureLocations` value, use the"] # [doc = " [`Regex::capture_locations`] method."] # [doc = ""] # [doc = " This also returns the overall match if one was found. When a match is"] # [doc = " found, its offsets are also always stored in `locs` at index `0`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This routine may panic if the given `CaptureLocations` was not created"] # [doc = " by this regex."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"^([a-z]+)=(\\S*)$\").unwrap();"] # [doc = " let mut locs = re.capture_locations();"] # [doc = " assert!(re.captures_read(&mut locs, \"id=foo123\").is_some());"] # [doc = " assert_eq!(Some((0, 9)), locs.get(0));"] # [doc = " assert_eq!(Some((0, 2)), locs.get(1));"] # [doc = " assert_eq!(Some((3, 9)), locs.get(2));"] # [doc = " ```"] # [inline] pub fn captures_read < 'h > (& self , locs : & mut CaptureLocations , haystack : & 'h str ,) -> Option < Match < 'h > > { self . captures_read_at (locs , haystack , 0) } # [doc = " Returns the same as [`Regex::captures_read`], but starts the search at"] # [doc = " the given offset."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `start == 0`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics when `start >= haystack.len() + 1`."] # [doc = ""] # [doc = " This routine may also panic if the given `CaptureLocations` was not"] # [doc = " created by this regex."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the significance of `start` by demonstrating how it"] # [doc = " can be used to permit look-around assertions in a regex to take the"] # [doc = " surrounding context into account."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\\bchew\\b\").unwrap();"] # [doc = " let hay = \"eschew\";"] # [doc = " let mut locs = re.capture_locations();"] # [doc = " // We get a match here, but it's probably not intended."] # [doc = " assert!(re.captures_read(&mut locs, &hay[2..]).is_some());"] # [doc = " // No match because the  assertions take the context into account."] # [doc = " assert!(re.captures_read_at(&mut locs, hay, 2).is_none());"] # [doc = " ```"] # [inline] pub fn captures_read_at < 'h > (& self , locs : & mut CaptureLocations , haystack : & 'h str , start : usize ,) -> Option < Match < 'h > > { let input = Input :: new (haystack) . span (start .. haystack . len ()) ; self . meta . search_captures (& input , & mut locs . 0) ; locs . 0 . get_match () . map (| m | Match :: new (haystack , m . start () , m . end ())) } # [doc = " An undocumented alias for `captures_read_at`."] # [doc = ""] # [doc = " The `regex-capi` crate previously used this routine, so to avoid"] # [doc = " breaking that crate, we continue to provide the name as an undocumented"] # [doc = " alias."] # [doc (hidden)] # [inline] pub fn read_captures_at < 'h > (& self , locs : & mut CaptureLocations , haystack : & 'h str , start : usize ,) -> Option < Match < 'h > > { self . captures_read_at (locs , haystack , start) } }
    };
}

impl_87!()