macro_rules! deps {
    () => {
        SetMatches!();
        RegexSet!();
        Error!();
    };
}

macro_rules! impl_137 {
    () => {
        deps!();
        impl RegexSet { # [doc = " Create a new regex set with the given regular expressions."] # [doc = ""] # [doc = " This takes an iterator of `S`, where `S` is something that can produce"] # [doc = " a `&str`. If any of the strings in the iterator are not valid regular"] # [doc = " expressions, then an error is returned."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Create a new regex set from an iterator of strings:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSet;"] # [doc = ""] # [doc = " let set = RegexSet::new([r\"\\w+\", r\"\\d+\"]).unwrap();"] # [doc = " assert!(set.is_match(b\"foo\"));"] # [doc = " ```"] pub fn new < I , S > (exprs : I) -> Result < RegexSet , Error > where S : AsRef < str > , I : IntoIterator < Item = S > , { RegexSetBuilder :: new (exprs) . build () } # [doc = " Create a new empty regex set."] # [doc = ""] # [doc = " An empty regex never matches anything."] # [doc = ""] # [doc = " This is a convenience function for `RegexSet::new([])`, but doesn't"] # [doc = " require one to specify the type of the input."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSet;"] # [doc = ""] # [doc = " let set = RegexSet::empty();"] # [doc = " assert!(set.is_empty());"] # [doc = " // an empty set matches nothing"] # [doc = " assert!(!set.is_match(b\"\"));"] # [doc = " ```"] pub fn empty () -> RegexSet { let empty : [& str ; 0] = [] ; RegexSetBuilder :: new (empty) . build () . unwrap () } # [doc = " Returns true if and only if one of the regexes in this set matches"] # [doc = " the haystack given."] # [doc = ""] # [doc = " This method should be preferred if you only need to test whether any"] # [doc = " of the regexes in the set should match, but don't care about *which*"] # [doc = " regexes matched. This is because the underlying matching engine will"] # [doc = " quit immediately after seeing the first match instead of continuing to"] # [doc = " find all matches."] # [doc = ""] # [doc = " Note that as with searches using [`Regex`](crate::bytes::Regex), the"] # [doc = " expression is unanchored by default. That is, if the regex does not"] # [doc = " start with `^` or `\\A`, or end with `$` or `\\z`, then it is permitted"] # [doc = " to match anywhere in the haystack."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Tests whether a set matches somewhere in a haystack:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSet;"] # [doc = ""] # [doc = " let set = RegexSet::new([r\"\\w+\", r\"\\d+\"]).unwrap();"] # [doc = " assert!(set.is_match(b\"foo\"));"] # [doc = " assert!(!set.is_match(\"☃\".as_bytes()));"] # [doc = " ```"] # [inline] pub fn is_match (& self , haystack : & [u8]) -> bool { self . is_match_at (haystack , 0) } # [doc = " Returns true if and only if one of the regexes in this set matches the"] # [doc = " haystack given, with the search starting at the offset given."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `start == 0`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics when `start >= haystack.len() + 1`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the significance of `start`. Namely, consider a"] # [doc = " haystack `foobar` and a desire to execute a search starting at offset"] # [doc = " `3`. You could search a substring explicitly, but then the look-around"] # [doc = " assertions won't work correctly. Instead, you can use this method to"] # [doc = " specify the start position of a search."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSet;"] # [doc = ""] # [doc = " let set = RegexSet::new([r\"\\bbar\\b\", r\"(?m)^bar$\"]).unwrap();"] # [doc = " let hay = b\"foobar\";"] # [doc = " // We get a match here, but it's probably not intended."] # [doc = " assert!(set.is_match(&hay[3..]));"] # [doc = " // No match because the  assertions take the context into account."] # [doc = " assert!(!set.is_match_at(hay, 3));"] # [doc = " ```"] # [inline] pub fn is_match_at (& self , haystack : & [u8] , start : usize) -> bool { self . meta . is_match (Input :: new (haystack) . span (start .. haystack . len ())) } # [doc = " Returns the set of regexes that match in the given haystack."] # [doc = ""] # [doc = " The set returned contains the index of each regex that matches in"] # [doc = " the given haystack. The index is in correspondence with the order of"] # [doc = " regular expressions given to `RegexSet`'s constructor."] # [doc = ""] # [doc = " The set can also be used to iterate over the matched indices. The order"] # [doc = " of iteration is always ascending with respect to the matching indices."] # [doc = ""] # [doc = " Note that as with searches using [`Regex`](crate::bytes::Regex), the"] # [doc = " expression is unanchored by default. That is, if the regex does not"] # [doc = " start with `^` or `\\A`, or end with `$` or `\\z`, then it is permitted"] # [doc = " to match anywhere in the haystack."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Tests which regular expressions match the given haystack:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSet;"] # [doc = ""] # [doc = " let set = RegexSet::new(["] # [doc = "     r\"\\w+\","] # [doc = "     r\"\\d+\","] # [doc = "     r\"\\pL+\","] # [doc = "     r\"foo\","] # [doc = "     r\"bar\","] # [doc = "     r\"barfoo\","] # [doc = "     r\"foobar\","] # [doc = " ]).unwrap();"] # [doc = " let matches: Vec<_> = set.matches(b\"foobar\").into_iter().collect();"] # [doc = " assert_eq!(matches, vec![0, 2, 3, 4, 6]);"] # [doc = ""] # [doc = " // You can also test whether a particular regex matched:"] # [doc = " let matches = set.matches(b\"foobar\");"] # [doc = " assert!(!matches.matched(5));"] # [doc = " assert!(matches.matched(6));"] # [doc = " ```"] # [inline] pub fn matches (& self , haystack : & [u8]) -> SetMatches { self . matches_at (haystack , 0) } # [doc = " Returns the set of regexes that match in the given haystack."] # [doc = ""] # [doc = " The set returned contains the index of each regex that matches in"] # [doc = " the given haystack. The index is in correspondence with the order of"] # [doc = " regular expressions given to `RegexSet`'s constructor."] # [doc = ""] # [doc = " The set can also be used to iterate over the matched indices. The order"] # [doc = " of iteration is always ascending with respect to the matching indices."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `start == 0`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics when `start >= haystack.len() + 1`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Tests which regular expressions match the given haystack:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSet;"] # [doc = ""] # [doc = " let set = RegexSet::new([r\"\\bbar\\b\", r\"(?m)^bar$\"]).unwrap();"] # [doc = " let hay = b\"foobar\";"] # [doc = " // We get matches here, but it's probably not intended."] # [doc = " let matches: Vec<_> = set.matches(&hay[3..]).into_iter().collect();"] # [doc = " assert_eq!(matches, vec![0, 1]);"] # [doc = " // No matches because the  assertions take the context into account."] # [doc = " let matches: Vec<_> = set.matches_at(hay, 3).into_iter().collect();"] # [doc = " assert_eq!(matches, vec![]);"] # [doc = " ```"] # [inline] pub fn matches_at (& self , haystack : & [u8] , start : usize) -> SetMatches { let input = Input :: new (haystack) . span (start .. haystack . len ()) ; let mut patset = PatternSet :: new (self . meta . pattern_len ()) ; self . meta . which_overlapping_matches (& input , & mut patset) ; SetMatches (patset) } # [doc = " Returns the same as matches, but starts the search at the given"] # [doc = " offset and stores the matches into the slice given."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `start == 0`."] # [doc = ""] # [doc = " `matches` must have a length that is at least the number of regexes"] # [doc = " in this set."] # [doc = ""] # [doc = " This method returns true if and only if at least one member of"] # [doc = " `matches` is true after executing the set against `haystack`."] # [doc (hidden)] # [inline] pub fn matches_read_at (& self , matches : & mut [bool] , haystack : & [u8] , start : usize ,) -> bool { let mut patset = PatternSet :: new (self . meta . pattern_len ()) ; let mut input = Input :: new (haystack) ; input . set_start (start) ; self . meta . which_overlapping_matches (& input , & mut patset) ; for pid in patset . iter () { matches [pid] = true ; } ! patset . is_empty () } # [doc = " An alias for `matches_read_at` to preserve backward compatibility."] # [doc = ""] # [doc = " The `regex-capi` crate used this method, so to avoid breaking that"] # [doc = " crate, we continue to export it as an undocumented API."] # [doc (hidden)] # [inline] pub fn read_matches_at (& self , matches : & mut [bool] , haystack : & [u8] , start : usize ,) -> bool { self . matches_read_at (matches , haystack , start) } # [doc = " Returns the total number of regexes in this set."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSet;"] # [doc = ""] # [doc = " assert_eq!(0, RegexSet::empty().len());"] # [doc = " assert_eq!(1, RegexSet::new([r\"[0-9]\"]).unwrap().len());"] # [doc = " assert_eq!(2, RegexSet::new([r\"[0-9]\", r\"[a-z]\"]).unwrap().len());"] # [doc = " ```"] # [inline] pub fn len (& self) -> usize { self . meta . pattern_len () } # [doc = " Returns `true` if this set contains no regexes."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSet;"] # [doc = ""] # [doc = " assert!(RegexSet::empty().is_empty());"] # [doc = " assert!(!RegexSet::new([r\"[0-9]\"]).unwrap().is_empty());"] # [doc = " ```"] # [inline] pub fn is_empty (& self) -> bool { self . meta . pattern_len () == 0 } # [doc = " Returns the regex patterns that this regex set was constructed from."] # [doc = ""] # [doc = " This function can be used to determine the pattern for a match. The"] # [doc = " slice returned has exactly as many patterns givens to this regex set,"] # [doc = " and the order of the slice is the same as the order of the patterns"] # [doc = " provided to the set."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::RegexSet;"] # [doc = ""] # [doc = " let set = RegexSet::new(&["] # [doc = "     r\"\\w+\","] # [doc = "     r\"\\d+\","] # [doc = "     r\"\\pL+\","] # [doc = "     r\"foo\","] # [doc = "     r\"bar\","] # [doc = "     r\"barfoo\","] # [doc = "     r\"foobar\","] # [doc = " ]).unwrap();"] # [doc = " let matches: Vec<_> = set"] # [doc = "     .matches(b\"foobar\")"] # [doc = "     .into_iter()"] # [doc = "     .map(|index| &set.patterns()[index])"] # [doc = "     .collect();"] # [doc = " assert_eq!(matches, vec![r\"\\w+\", r\"\\pL+\", r\"foo\", r\"bar\", r\"foobar\"]);"] # [doc = " ```"] # [inline] pub fn patterns (& self) -> & [String] { & self . patterns } }
    };
}

impl_137!()