macro_rules! deps {
    () => {
        Match!();
        Captures!();
        SubCaptureMatches!();
    };
}

macro_rules! impl_37 {
    () => {
        deps!();
        impl < 'h > Captures < 'h > { # [doc = " Returns the `Match` associated with the capture group at index `i`. If"] # [doc = " `i` does not correspond to a capture group, or if the capture group did"] # [doc = " not participate in the match, then `None` is returned."] # [doc = ""] # [doc = " When `i == 0`, this is guaranteed to return a non-`None` value."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Get the substring that matched with a default of an empty string if the"] # [doc = " group didn't participate in the match:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"[a-z]+(?:([0-9]+)|([A-Z]+))\").unwrap();"] # [doc = " let caps = re.captures(b\"abc123\").unwrap();"] # [doc = ""] # [doc = " let substr1 = caps.get(1).map_or(&b\"\"[..], |m| m.as_bytes());"] # [doc = " let substr2 = caps.get(2).map_or(&b\"\"[..], |m| m.as_bytes());"] # [doc = " assert_eq!(substr1, b\"123\");"] # [doc = " assert_eq!(substr2, b\"\");"] # [doc = " ```"] # [inline] pub fn get (& self , i : usize) -> Option < Match < 'h > > { self . caps . get_group (i) . map (| sp | Match :: new (self . haystack , sp . start , sp . end)) } # [doc = " Return the overall match for the capture."] # [doc = ""] # [doc = " This returns the match for index `0`. That is it is equivalent to"] # [doc = " `m.get(0).unwrap()`"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"[a-z]+([0-9]+)\").unwrap();"] # [doc = " let caps = re.captures(b\"   abc123-def\").unwrap();"] # [doc = ""] # [doc = " assert_eq!(caps.get_match().as_bytes(), b\"abc123\");"] # [doc = " ```"] # [inline] pub fn get_match (& self) -> Match < 'h > { self . get (0) . unwrap () } # [doc = " Returns the `Match` associated with the capture group named `name`. If"] # [doc = " `name` isn't a valid capture group or it refers to a group that didn't"] # [doc = " match, then `None` is returned."] # [doc = ""] # [doc = " Note that unlike `caps[\"name\"]`, this returns a `Match` whose lifetime"] # [doc = " matches the lifetime of the haystack in this `Captures` value."] # [doc = " Conversely, the substring returned by `caps[\"name\"]` has a lifetime"] # [doc = " of the `Captures` value, which is likely shorter than the lifetime of"] # [doc = " the haystack. In some cases, it may be necessary to use this method to"] # [doc = " access the matching substring instead of the `caps[\"name\"]` notation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Get the substring that matched with a default of an empty string if the"] # [doc = " group didn't participate in the match:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new("] # [doc = "     r\"[a-z]+(?:(?<numbers>[0-9]+)|(?<letters>[A-Z]+))\","] # [doc = " ).unwrap();"] # [doc = " let caps = re.captures(b\"abc123\").unwrap();"] # [doc = ""] # [doc = " let numbers = caps.name(\"numbers\").map_or(&b\"\"[..], |m| m.as_bytes());"] # [doc = " let letters = caps.name(\"letters\").map_or(&b\"\"[..], |m| m.as_bytes());"] # [doc = " assert_eq!(numbers, b\"123\");"] # [doc = " assert_eq!(letters, b\"\");"] # [doc = " ```"] # [inline] pub fn name (& self , name : & str) -> Option < Match < 'h > > { self . caps . get_group_by_name (name) . map (| sp | Match :: new (self . haystack , sp . start , sp . end)) } # [doc = " This is a convenience routine for extracting the substrings"] # [doc = " corresponding to matching capture groups."] # [doc = ""] # [doc = " This returns a tuple where the first element corresponds to the full"] # [doc = " substring of the haystack that matched the regex. The second element is"] # [doc = " an array of substrings, with each corresponding to the substring that"] # [doc = " matched for a particular capture group."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics if the number of possible matching groups in this"] # [doc = " `Captures` value is not fixed to `N` in all circumstances."] # [doc = " More precisely, this routine only works when `N` is equivalent to"] # [doc = " [`Regex::static_captures_len`]."] # [doc = ""] # [doc = " Stated more plainly, if the number of matching capture groups in a"] # [doc = " regex can vary from match to match, then this function always panics."] # [doc = ""] # [doc = " For example, `(a)(b)|(c)` could produce two matching capture groups"] # [doc = " or one matching capture group for any given match. Therefore, one"] # [doc = " cannot use `extract` with such a pattern."] # [doc = ""] # [doc = " But a pattern like `(a)(b)|(c)(d)` can be used with `extract` because"] # [doc = " the number of capture groups in every match is always equivalent,"] # [doc = " even if the capture _indices_ in each match are not."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"([0-9]{4})-([0-9]{2})-([0-9]{2})\").unwrap();"] # [doc = " let hay = b\"On 2010-03-14, I became a Tennessee lamb.\";"] # [doc = " let Some((full, [year, month, day])) ="] # [doc = "     re.captures(hay).map(|caps| caps.extract()) else { return };"] # [doc = " assert_eq!(b\"2010-03-14\", full);"] # [doc = " assert_eq!(b\"2010\", year);"] # [doc = " assert_eq!(b\"03\", month);"] # [doc = " assert_eq!(b\"14\", day);"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: iteration"] # [doc = ""] # [doc = " This example shows how to use this method when iterating over all"] # [doc = " `Captures` matches in a haystack."] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"([0-9]{4})-([0-9]{2})-([0-9]{2})\").unwrap();"] # [doc = " let hay = b\"1973-01-05, 1975-08-25 and 1980-10-18\";"] # [doc = ""] # [doc = " let mut dates: Vec<(&[u8], &[u8], &[u8])> = vec![];"] # [doc = " for (_, [y, m, d]) in re.captures_iter(hay).map(|c| c.extract()) {"] # [doc = "     dates.push((y, m, d));"] # [doc = " }"] # [doc = " assert_eq!(dates, vec!["] # [doc = "     (&b\"1973\"[..], &b\"01\"[..], &b\"05\"[..]),"] # [doc = "     (&b\"1975\"[..], &b\"08\"[..], &b\"25\"[..]),"] # [doc = "     (&b\"1980\"[..], &b\"10\"[..], &b\"18\"[..]),"] # [doc = " ]);"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: parsing different formats"] # [doc = ""] # [doc = " This API is particularly useful when you need to extract a particular"] # [doc = " value that might occur in a different format. Consider, for example,"] # [doc = " an identifier that might be in double quotes or single quotes:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r#\"id:(?:\"([^\"]+)\"|'([^']+)')\"#).unwrap();"] # [doc = " let hay = br#\"The first is id:\"foo\" and the second is id:'bar'.\"#;"] # [doc = " let mut ids = vec![];"] # [doc = " for (_, [id]) in re.captures_iter(hay).map(|c| c.extract()) {"] # [doc = "     ids.push(id);"] # [doc = " }"] # [doc = " assert_eq!(ids, vec![b\"foo\", b\"bar\"]);"] # [doc = " ```"] pub fn extract < const N : usize > (& self) -> (& 'h [u8] , [& 'h [u8] ; N]) { let len = self . static_captures_len . expect ("number of capture groups can vary in a match") . checked_sub (1) . expect ("number of groups is always greater than zero") ; assert_eq ! (N , len , "asked for {N} groups, but must ask for {len}") ; self . caps . extract_bytes (self . haystack) } # [doc = " Expands all instances of `$ref` in `replacement` to the corresponding"] # [doc = " capture group, and writes them to the `dst` buffer given. A `ref` can"] # [doc = " be a capture group index or a name. If `ref` doesn't refer to a capture"] # [doc = " group that participated in the match, then it is replaced with the"] # [doc = " empty string."] # [doc = ""] # [doc = " # Format"] # [doc = ""] # [doc = " The format of the replacement string supports two different kinds of"] # [doc = " capture references: unbraced and braced."] # [doc = ""] # [doc = " For the unbraced format, the format supported is `$ref` where `name`"] # [doc = " can be any character in the class `[0-9A-Za-z_]`. `ref` is always"] # [doc = " the longest possible parse. So for example, `$1a` corresponds to the"] # [doc = " capture group named `1a` and not the capture group at index `1`. If"] # [doc = " `ref` matches `^[0-9]+$`, then it is treated as a capture group index"] # [doc = " itself and not a name."] # [doc = ""] # [doc = " For the braced format, the format supported is `${ref}` where `ref` can"] # [doc = " be any sequence of bytes except for `}`. If no closing brace occurs,"] # [doc = " then it is not considered a capture reference. As with the unbraced"] # [doc = " format, if `ref` matches `^[0-9]+$`, then it is treated as a capture"] # [doc = " group index and not a name."] # [doc = ""] # [doc = " The braced format is useful for exerting precise control over the name"] # [doc = " of the capture reference. For example, `${1}a` corresponds to the"] # [doc = " capture group reference `1` followed by the letter `a`, where as `$1a`"] # [doc = " (as mentioned above) corresponds to the capture group reference `1a`."] # [doc = " The braced format is also useful for expressing capture group names"] # [doc = " that use characters not supported by the unbraced format. For example,"] # [doc = " `${foo[bar].baz}` refers to the capture group named `foo[bar].baz`."] # [doc = ""] # [doc = " If a capture group reference is found and it does not refer to a valid"] # [doc = " capture group, then it will be replaced with the empty string."] # [doc = ""] # [doc = " To write a literal `$`, use `$$`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new("] # [doc = "     r\"(?<day>[0-9]{2})-(?<month>[0-9]{2})-(?<year>[0-9]{4})\","] # [doc = " ).unwrap();"] # [doc = " let hay = b\"On 14-03-2010, I became a Tennessee lamb.\";"] # [doc = " let caps = re.captures(hay).unwrap();"] # [doc = ""] # [doc = " let mut dst = vec![];"] # [doc = " caps.expand(b\"year=$year, month=$month, day=$day\", &mut dst);"] # [doc = " assert_eq!(dst, b\"year=2010, month=03, day=14\");"] # [doc = " ```"] # [inline] pub fn expand (& self , replacement : & [u8] , dst : & mut Vec < u8 >) { self . caps . interpolate_bytes_into (self . haystack , replacement , dst) ; } # [doc = " Returns an iterator over all capture groups. This includes both"] # [doc = " matching and non-matching groups."] # [doc = ""] # [doc = " The iterator always yields at least one matching group: the first group"] # [doc = " (at index `0`) with no name. Subsequent groups are returned in the order"] # [doc = " of their opening parenthesis in the regex."] # [doc = ""] # [doc = " The elements yielded have type `Option<Match<'h>>`, where a non-`None`"] # [doc = " value is present if the capture group matches."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(\\w)(\\d)?(\\w)\").unwrap();"] # [doc = " let caps = re.captures(b\"AZ\").unwrap();"] # [doc = ""] # [doc = " let mut it = caps.iter();"] # [doc = " assert_eq!(it.next().unwrap().map(|m| m.as_bytes()), Some(&b\"AZ\"[..]));"] # [doc = " assert_eq!(it.next().unwrap().map(|m| m.as_bytes()), Some(&b\"A\"[..]));"] # [doc = " assert_eq!(it.next().unwrap().map(|m| m.as_bytes()), None);"] # [doc = " assert_eq!(it.next().unwrap().map(|m| m.as_bytes()), Some(&b\"Z\"[..]));"] # [doc = " assert_eq!(it.next(), None);"] # [doc = " ```"] # [inline] pub fn iter < 'c > (& 'c self) -> SubCaptureMatches < 'c , 'h > { SubCaptureMatches { haystack : self . haystack , it : self . caps . iter () } } # [doc = " Returns the total number of capture groups. This includes both"] # [doc = " matching and non-matching groups."] # [doc = ""] # [doc = " The length returned is always equivalent to the number of elements"] # [doc = " yielded by [`Captures::iter`]. Consequently, the length is always"] # [doc = " greater than zero since every `Captures` value always includes the"] # [doc = " match for the entire regex."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex::bytes::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(\\w)(\\d)?(\\w)\").unwrap();"] # [doc = " let caps = re.captures(b\"AZ\").unwrap();"] # [doc = " assert_eq!(caps.len(), 4);"] # [doc = " ```"] # [inline] pub fn len (& self) -> usize { self . caps . group_len () } }
    };
}

impl_37!()