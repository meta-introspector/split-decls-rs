// Generated macro for impl_11 (impl)
macro_rules! Depcrateimpl_11 {
() => {
// Module: crate
// Provides: {"impl_11"}
// Dependencies: {}
impl MimeGuess { # [doc = " Guess the MIME type of a file (real or otherwise) with the given extension."] # [doc = ""] # [doc = " The search is case-insensitive."] # [doc = ""] # [doc = " If `ext` is empty or has no (currently) known MIME type mapping, then an empty guess is"] # [doc = " returned."] pub fn from_ext (ext : & str) -> MimeGuess { if ext . is_empty () { return MimeGuess (& []) ; } impl_ :: get_mime_types (ext) . map_or (MimeGuess (& []) , | v | MimeGuess (v)) } # [doc = " Guess the MIME type of `path` by its extension (as defined by"] # [doc = " [`Path::extension()`]). **No disk access is performed.**"] # [doc = ""] # [doc = " If `path` has no extension, the extension cannot be converted to `str`, or has"] # [doc = " no known MIME type mapping, then an empty guess is returned."] # [doc = ""] # [doc = " The search is case-insensitive."] # [doc = ""] # [doc = " ## Note"] # [doc = " **Guess** is the operative word here, as there are no guarantees that the contents of the"] # [doc = " file that `path` points to match the MIME type associated with the path's extension."] # [doc = ""] # [doc = " Take care when processing files with assumptions based on the return value of this function."] # [doc = ""] # [doc = " [`Path::extension()`]: https://doc.rust-lang.org/std/path/struct.Path.html#method.extension"] pub fn from_path < P : AsRef < Path > > (path : P) -> MimeGuess { path . as_ref () . extension () . and_then (OsStr :: to_str) . map_or (MimeGuess (& []) , Self :: from_ext) } # [doc = " `true` if the guess did not return any known mappings for the given path or extension."] pub fn is_empty (& self) -> bool { self . 0 . is_empty () } # [doc = " Get the number of MIME types in the current guess."] pub fn count (& self) -> usize { self . 0 . len () } # [doc = " Get the first guessed `Mime`, if applicable."] # [doc = ""] # [doc = " See [Note: Ordering](#note-ordering) above."] pub fn first (& self) -> Option < Mime > { self . first_raw () . map (expect_mime) } # [doc = " Get the first guessed Media Type as a string, if applicable."] # [doc = ""] # [doc = " See [Note: Ordering](#note-ordering) above."] pub fn first_raw (& self) -> Option < & 'static str > { self . 0 . get (0) . cloned () } # [doc = " Get the first guessed `Mime`, or if the guess is empty, return"] # [doc = " [`application/octet-stream`] instead."] # [doc = ""] # [doc = " See [Note: Ordering](#note-ordering) above."] # [doc = ""] # [doc = " ### Note: HTTP Applications"] # [doc = " For HTTP request and response bodies if a value for the `Content-Type` header"] # [doc = " cannot be determined it might be preferable to not send one at all instead of defaulting to"] # [doc = " `application/octet-stream` as the recipient will expect to infer the format directly from"] # [doc = " the content instead. ([RFC 7231, Section 3.1.1.5][rfc7231])"] # [doc = ""] # [doc = " On the contrary, for `multipart/form-data` bodies, the `Content-Type` of a form-data part is"] # [doc = " assumed to be `text/plain` unless specified so a default of `application/octet-stream`"] # [doc = " for non-text parts is safer. ([RFC 7578, Section 4.4][rfc7578])"] # [doc = ""] # [doc = " [`application/octet-stream`]: https://docs.rs/mime/0.3/mime/constant.APPLICATION_OCTET_STREAM.html"] # [doc = " [rfc7231]: https://tools.ietf.org/html/rfc7231#section-3.1.1.5"] # [doc = " [rfc7578]: https://tools.ietf.org/html/rfc7578#section-4.4"] pub fn first_or_octet_stream (& self) -> Mime { self . first_or (mime :: APPLICATION_OCTET_STREAM) } # [doc = " Get the first guessed `Mime`, or if the guess is empty, return"] # [doc = " [`text/plain`](::mime::TEXT_PLAIN) instead."] # [doc = ""] # [doc = " See [Note: Ordering](#note-ordering) above."] pub fn first_or_text_plain (& self) -> Mime { self . first_or (mime :: TEXT_PLAIN) } # [doc = " Get the first guessed `Mime`, or if the guess is empty, return the given `Mime` instead."] # [doc = ""] # [doc = " See [Note: Ordering](#note-ordering) above."] pub fn first_or (& self , default : Mime) -> Mime { self . first () . unwrap_or (default) } # [doc = " Get the first guessed `Mime`, or if the guess is empty, execute the closure and return its"] # [doc = " result."] # [doc = ""] # [doc = " See [Note: Ordering](#note-ordering) above."] pub fn first_or_else < F > (& self , default_fn : F) -> Mime where F : FnOnce () -> Mime , { self . first () . unwrap_or_else (default_fn) } # [doc = " Get an iterator over the `Mime` values contained in this guess."] # [doc = ""] # [doc = " See [Note: Ordering](#note-ordering) above."] pub fn iter (& self) -> Iter { Iter (self . iter_raw () . map (expect_mime)) } # [doc = " Get an iterator over the raw media-type strings in this guess."] # [doc = ""] # [doc = " See [Note: Ordering](#note-ordering) above."] pub fn iter_raw (& self) -> IterRaw { IterRaw (self . 0 . iter () . cloned ()) } }
};
}
