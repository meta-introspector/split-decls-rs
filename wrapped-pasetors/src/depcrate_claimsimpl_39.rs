// Generated macro for impl_39 (impl)
macro_rules! Depcrate_claimsimpl_39 {
() => {
// Module: crate::claims
// Provides: {"impl_39"}
// Dependencies: {}
impl Claims { # [doc = " Keys for registered claims, that are reserved for usage by PASETO in top-level."] pub const REGISTERED_CLAIMS : [& 'static str ; 7] = ["iss" , "sub" , "aud" , "exp" , "nbf" , "iat" , "jti"] ; # [doc = " Create a new `Claims` instance, setting:"] # [doc = " - `iat`, `nbf` to current UTC time"] # [doc = " - `exp` to one hour"] # [doc = ""] # [doc = " Errors:"] # [doc = " - If adding current time with one hour would overflow"] pub fn new () -> Result < Self , Error > { Self :: new_expires_in (& core :: time :: Duration :: from_secs (3600)) } # [doc = " Create a new `Claims` instance expiring in `duration`, setting:"] # [doc = " - `iat`, `nbf` to current UTC time"] # [doc = " - `iat + duration`"] # [doc = ""] # [doc = " Errors:"] # [doc = " - If adding current time with `duration` would overflow"] # [doc = " - If `core::time::Duration` failed to convert to `time::Duration`"] pub fn new_expires_in (duration : & core :: time :: Duration) -> Result < Self , Error > { let mut claims = Self { list_of : HashMap :: new () , } ; claims . set_expires_in (duration) ? ; Ok (claims) } # [doc = " Format a DateTime claim in ISO 8601 without fractional seconds."] # [doc = ""] # [doc = " Omitting fractional seconds is recommended by PASETO spec:"] # [doc = " > While arbitrary fractional seconds SHOULD be supported in parsers, is RECOMMENDED to omit them on creation."] fn format_datetime (time : OffsetDateTime) -> Result < String , Error > { time . replace_nanosecond (0) . map_err (| _ | Error :: InvalidClaim) ? . format (& Rfc3339) . map_err (| _ | Error :: InvalidClaim) } # [doc = " Set `iat`, `nbf`, `exp` claims expiring in `duration`, setting:"] # [doc = " - `iat`, `nbf` to current UTC time"] # [doc = " - `iat + duration`"] # [doc = ""] # [doc = " Errors:"] # [doc = " - If adding current time with `duration` would overflow"] # [doc = " - If `core::time::Duration` failed to convert to `time::Duration`"] pub fn set_expires_in (& mut self , duration : & core :: time :: Duration) -> Result < () , Error > { let iat = OffsetDateTime :: now_utc () ; let nbf = iat ; let mut exp = iat ; exp += Duration :: try_from (* duration) . map_err (| _ | Error :: InvalidClaim) ? ; self . issued_at (& Self :: format_datetime (iat) ?) ? ; self . not_before (& Self :: format_datetime (nbf) ?) ? ; self . expiration (& Self :: format_datetime (exp) ?) ? ; Ok (()) } # [doc = " Removes the `exp` claim, indicating a token that never expires."] pub fn non_expiring (& mut self) { self . remove_claim ("exp") ; } # [doc = " Add additional claims. If `claim` already exists, it is replaced with the new."] # [doc = ""] # [doc = " Errors:"] # [doc = " - `claim` is a reserved claim (see [`Self::REGISTERED_CLAIMS`])"] pub fn add_additional (& mut self , claim : & str , value : impl Into < Value >) -> Result < () , Error > { if ! Self :: REGISTERED_CLAIMS . contains (& claim) { self . list_of . insert (claim . into () , value . into ()) ; Ok (()) } else { Err (Error :: InvalidClaim) } } # [doc = " Checks whether a specific claim has been added to the list."] # [doc = ""] # [doc = " E.g `contains_claim(\"iss\") == true` if `iss` has been added before."] pub fn contains_claim (& self , claim : & str) -> bool { self . list_of . contains_key (claim) } # [doc = " Return Some(claim value) if claims list contains the `claim`."] # [doc = " None otherwise."] pub fn get_claim (& self , claim : & str) -> Option < & Value > { self . list_of . get (claim) } # [doc = " Remove a claim from the claims list."] # [doc = ""] # [doc = " Return Some(claim value) if claims list contains the `claim`."] # [doc = " None otherwise."] pub fn remove_claim (& mut self , claim : & str) -> Option < Value > { self . list_of . remove (claim) } # [doc = " Set the `iss` claim. If it already exists, replace it with the new."] # [doc = ""] # [doc = " Errors:"] # [doc = " - `iss` is empty"] pub fn issuer (& mut self , iss : & str) -> Result < () , Error > { if ! iss . is_empty () { self . list_of . insert ("iss" . into () , iss . into ()) ; Ok (()) } else { Err (Error :: InvalidClaim) } } # [doc = " Set the `sub` claim. If it already exists, replace it with the new."] # [doc = ""] # [doc = " Errors:"] # [doc = " - `sub` is empty"] pub fn subject (& mut self , sub : & str) -> Result < () , Error > { if ! sub . is_empty () { self . list_of . insert ("sub" . into () , sub . into ()) ; Ok (()) } else { Err (Error :: InvalidClaim) } } # [doc = " Set the `aud` claim. If it already exists, replace it with the new."] # [doc = ""] # [doc = " Errors:"] # [doc = " - `aud` is empty"] pub fn audience (& mut self , aud : & str) -> Result < () , Error > { if ! aud . is_empty () { self . list_of . insert ("aud" . into () , aud . into ()) ; Ok (()) } else { Err (Error :: InvalidClaim) } } # [doc = " Set the `exp` claim. If it already exists, replace it with the new."] # [doc = ""] # [doc = " Errors:"] # [doc = " - `exp` is empty"] # [doc = " - `exp` cannot be parsed as a ISO 8601 compliant DateTime string."] pub fn expiration (& mut self , exp : & str) -> Result < () , Error > { if let Ok (_exp_str) = OffsetDateTime :: parse (exp , & Rfc3339) { self . list_of . insert ("exp" . into () , exp . into ()) ; Ok (()) } else { Err (Error :: InvalidClaim) } } # [doc = " Set the `nbf` claim. If it already exists, replace it with the new."] # [doc = ""] # [doc = " Errors:"] # [doc = " - `nbf` is empty"] # [doc = " - `nbf` cannot be parsed as a ISO 8601 compliant DateTime string."] pub fn not_before (& mut self , nbf : & str) -> Result < () , Error > { if let Ok (_nbf_str) = OffsetDateTime :: parse (nbf , & Rfc3339) { self . list_of . insert ("nbf" . into () , nbf . into ()) ; Ok (()) } else { Err (Error :: InvalidClaim) } } # [doc = " Set the `iat` claim. If it already exists, replace it with the new."] # [doc = ""] # [doc = " Errors:"] # [doc = " - `iat` is empty"] # [doc = " - `iat` cannot be parsed as a ISO 8601 compliant DateTime string."] pub fn issued_at (& mut self , iat : & str) -> Result < () , Error > { if let Ok (_iat_str) = OffsetDateTime :: parse (iat , & Rfc3339) { self . list_of . insert ("iat" . into () , iat . into ()) ; Ok (()) } else { Err (Error :: InvalidClaim) } } # [doc = " Set the `jti` claim. If it already exists, replace it with the new."] # [doc = ""] # [doc = " Errors:"] # [doc = " - `jti` is empty"] pub fn token_identifier (& mut self , jti : & str) -> Result < () , Error > { if ! jti . is_empty () { self . list_of . insert ("jti" . into () , jti . into ()) ; Ok (()) } else { Err (Error :: InvalidClaim) } } # [doc = " Attempt to create `Claims` from a sequence of bytes."] # [doc = ""] # [doc = " Errors:"] # [doc = " - `bytes` contains non-UTF-8 sequences"] # [doc = " - `bytes` does not decode as valid JSON"] # [doc = " - `bytes` top-most JSON object does not decode to a map"] # [doc = " - if any registered claims exist and they are not a `String`"] # [doc = " - if `exp`, `nbf` or `iat` exist and they cannot be parsed as `DateTime`"] pub fn from_bytes (bytes : & [u8]) -> Result < Self , Error > { let input = bytes . to_vec () ; Self :: from_string (& String :: from_utf8 (input) . map_err (| _ | Error :: ClaimInvalidUtf8) ?) } # [doc = " Attempt to create `Claims` from a string."] # [doc = ""] # [doc = " Errors:"] # [doc = " - `string` does not decode as valid JSON"] # [doc = " - `string` top-most JSON object does not decode to a map"] # [doc = " - if any registered claims exist and they are not a `String`"] # [doc = " - if `exp`, `nbf` or `iat` exist and they cannot be parsed as `DateTime`"] pub fn from_string (string : & str) -> Result < Self , Error > { let list_of : HashMap < String , Value > = serde_json :: from_str (string) . map_err (| _ | Error :: ClaimInvalidJson) ? ; for registered_claim in Self :: REGISTERED_CLAIMS { if let Some (claim) = list_of . get (registered_claim) { if let Some (claim_value) = claim . as_str () { if registered_claim == "exp" || registered_claim == "nbf" || registered_claim == "iat" { OffsetDateTime :: parse (claim_value , & Rfc3339) . map_err (| _ | Error :: InvalidClaim) ? ; } } else { return Err (Error :: InvalidClaim) ; } } } Ok (Self { list_of }) } # [doc = " Return the JSON serialized representation of `Self`."] # [doc = ""] # [doc = " Errors:"] # [doc = " - `self` cannot be serialized as JSON"] pub fn to_string (& self) -> Result < String , Error > { match serde_json :: to_string (& self . list_of) { Ok (ret) => Ok (ret) , Err (_) => Err (Error :: ClaimInvalidJson) , } } }
};
}
