// Generated macro for WithInfo (struct)
macro_rules! Depcrate_runnableWithInfo {
() => {
// Module: crate::runnable
// Provides: {"WithInfo"}
// Dependencies: {}
# [doc = " Pass a scheduling function with more scheduling information - a.k.a."] # [doc = " [`ScheduleInfo`]."] # [doc = ""] # [doc = " Sometimes, it's useful to pass the runnable's state directly to the"] # [doc = " scheduling function, such as whether it's woken up while running. The"] # [doc = " scheduler can thus use the information to determine its scheduling"] # [doc = " strategy."] # [doc = ""] # [doc = " The data source of [`ScheduleInfo`] is directly from the actual"] # [doc = " implementation of the crate itself, different from [`Runnable`]'s metadata,"] # [doc = " which is managed by the caller."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_task::{ScheduleInfo, WithInfo};"] # [doc = " use std::sync::{Arc, Mutex};"] # [doc = ""] # [doc = " // The future inside the task."] # [doc = " let future = async {"] # [doc = "     println!(\"Hello, world!\");"] # [doc = " };"] # [doc = ""] # [doc = " // If the task gets woken up while running, it will be sent into this channel."] # [doc = " let (s, r) = flume::unbounded();"] # [doc = " // Otherwise, it will be placed into this slot."] # [doc = " let lifo_slot = Arc::new(Mutex::new(None));"] # [doc = " let schedule = move |runnable, info: ScheduleInfo| {"] # [doc = "     if info.woken_while_running {"] # [doc = "         s.send(runnable).unwrap()"] # [doc = "     } else {"] # [doc = "         let last = lifo_slot.lock().unwrap().replace(runnable);"] # [doc = "         if let Some(last) = last {"] # [doc = "             s.send(last).unwrap()"] # [doc = "         }"] # [doc = "     }"] # [doc = " };"] # [doc = ""] # [doc = " // Create a task with the future and the schedule function."] # [doc = " let (runnable, task) = async_task::spawn(future, WithInfo(schedule));"] # [doc = " ```"] # [derive (Debug)] pub struct WithInfo < F > (pub F) ;
};
}
