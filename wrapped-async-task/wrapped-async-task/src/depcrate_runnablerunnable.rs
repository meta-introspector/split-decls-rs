// Generated macro for Runnable (struct)
macro_rules! Depcrate_runnableRunnable {
() => {
// Module: crate::runnable
// Provides: {"Runnable"}
// Dependencies: {}
# [doc = " A handle to a runnable task."] # [doc = ""] # [doc = " Every spawned task has a single [`Runnable`] handle, which only exists when the task is"] # [doc = " scheduled for running."] # [doc = ""] # [doc = " Method [`run()`][`Runnable::run()`] polls the task's future once. Then, the [`Runnable`]"] # [doc = " vanishes and only reappears when its [`Waker`] wakes the task, thus scheduling it to be run"] # [doc = " again."] # [doc = ""] # [doc = " Dropping a [`Runnable`] cancels the task, which means its future won't be polled again, and"] # [doc = " awaiting the [`Task`] after that will result in a panic."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_task::Runnable;"] # [doc = " use once_cell::sync::Lazy;"] # [doc = " use std::{panic, thread};"] # [doc = ""] # [doc = " // A simple executor."] # [doc = " static QUEUE: Lazy<flume::Sender<Runnable>> = Lazy::new(|| {"] # [doc = "     let (sender, receiver) = flume::unbounded::<Runnable>();"] # [doc = "     thread::spawn(|| {"] # [doc = "         for runnable in receiver {"] # [doc = "             let _ignore_panic = panic::catch_unwind(|| runnable.run());"] # [doc = "         }"] # [doc = "     });"] # [doc = "     sender"] # [doc = " });"] # [doc = ""] # [doc = " // Create a task with a simple future."] # [doc = " let schedule = |runnable| QUEUE.send(runnable).unwrap();"] # [doc = " let (runnable, task) = async_task::spawn(async { 1 + 2 }, schedule);"] # [doc = ""] # [doc = " // Schedule the task and await its output."] # [doc = " runnable.schedule();"] # [doc = " assert_eq!(smol::future::block_on(task), 3);"] # [doc = " ```"] pub struct Runnable < M = () > { # [doc = " A pointer to the heap-allocated task."] pub (crate) ptr : NonNull < () > , # [doc = " A marker capturing generic type `M`."] pub (crate) _marker : PhantomData < M > , }
};
}
