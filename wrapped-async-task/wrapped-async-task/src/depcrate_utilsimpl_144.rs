// Generated macro for impl_144 (impl)
macro_rules! Depcrate_utilsimpl_144 {
() => {
// Module: crate::utils
// Provides: {"impl_144"}
// Dependencies: {}
impl Layout { # [doc = " Creates a new `Layout` with the given size and alignment."] # [inline] pub (crate) const fn from_size_align (size : usize , align : usize) -> Self { Self { size , align } } # [doc = " Creates a new `Layout` for the given sized type."] # [inline] pub (crate) const fn new < T > () -> Self { Self :: from_size_align (mem :: size_of :: < T > () , mem :: align_of :: < T > ()) } # [doc = " Convert this into the standard library's layout type."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " - `align` must be non-zero and a power of two."] # [doc = " - When rounded up to the nearest multiple of `align`, the size"] # [doc = "   must not overflow."] # [inline] pub (crate) const unsafe fn into_std (self) -> StdLayout { StdLayout :: from_size_align_unchecked (self . size , self . align) } # [doc = " Get the alignment of this layout."] # [inline] pub (crate) const fn align (& self) -> usize { self . align } # [doc = " Get the size of this layout."] # [inline] pub (crate) const fn size (& self) -> usize { self . size } # [doc = " Returns the layout for `a` followed by `b` and the offset of `b`."] # [doc = ""] # [doc = " This function was adapted from the `Layout::extend()`:"] # [doc = " https://doc.rust-lang.org/nightly/std/alloc/struct.Layout.html#method.extend"] # [inline] pub (crate) const fn extend (self , other : Layout) -> Option < (Layout , usize) > { let new_align = max (self . align () , other . align ()) ; let pad = self . padding_needed_for (other . align ()) ; let offset = leap ! (self . size () . checked_add (pad)) ; let new_size = leap ! (offset . checked_add (other . size ())) ; if ! new_align . is_power_of_two () || new_size > isize :: MAX as usize - (new_align - 1) { return None ; } let layout = Layout :: from_size_align (new_size , new_align) ; Some ((layout , offset)) } # [doc = " Returns the padding after `layout` that aligns the following address to `align`."] # [doc = ""] # [doc = " This function was adapted from the `Layout::padding_needed_for()`:"] # [doc = " https://doc.rust-lang.org/nightly/std/alloc/struct.Layout.html#method.padding_needed_for"] # [inline] const fn padding_needed_for (self , align : usize) -> usize { let len = self . size () ; let len_rounded_up = len . wrapping_add (align) . wrapping_sub (1) & ! align . wrapping_sub (1) ; len_rounded_up . wrapping_sub (len) } }
};
}
