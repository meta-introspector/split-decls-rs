// Generated macro for impl_125 (impl)
macro_rules! Depcrate_taskimpl_125 {
() => {
// Module: crate::task
// Provides: {"impl_125"}
// Dependencies: {}
impl < T , M > Task < T , M > { # [doc = " Detaches the task to let it keep running in the background."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use smol::{Executor, Timer};"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " let ex = Executor::new();"] # [doc = ""] # [doc = " // Spawn a daemon future."] # [doc = " ex.spawn(async {"] # [doc = "     loop {"] # [doc = "         println!(\"I'm a daemon task looping forever.\");"] # [doc = "         Timer::after(Duration::from_secs(1)).await;"] # [doc = "     }"] # [doc = " })"] # [doc = " .detach();"] # [doc = " ```"] pub fn detach (self) { let this = self ; let _out = set_detached :: < T > (this . ptr . as_ptr ()) ; mem :: forget (this) ; } # [doc = " Cancels the task and waits for it to stop running."] # [doc = ""] # [doc = " Returns the task's output if it was completed just before it got canceled, or [`None`] if"] # [doc = " it didn't complete."] # [doc = ""] # [doc = " While it's possible to simply drop the [`Task`] to cancel it, this is a cleaner way of"] # [doc = " canceling because it also waits for the task to stop running."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return; } // Miri does not support epoll"] # [doc = " use smol::{future, Executor, Timer};"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " let ex = Executor::new();"] # [doc = ""] # [doc = " // Spawn a daemon future."] # [doc = " let task = ex.spawn(async {"] # [doc = "     loop {"] # [doc = "         println!(\"Even though I'm in an infinite loop, you can still cancel me!\");"] # [doc = "         Timer::after(Duration::from_secs(1)).await;"] # [doc = "     }"] # [doc = " });"] # [doc = ""] # [doc = " // Run an executor thread."] # [doc = " thread::spawn(move || future::block_on(ex.run(future::pending::<()>())));"] # [doc = ""] # [doc = " future::block_on(async {"] # [doc = "     Timer::after(Duration::from_secs(3)).await;"] # [doc = "     task.cancel().await;"] # [doc = " });"] # [doc = " ```"] pub async fn cancel (self) -> Option < T > { let this = self ; set_canceled (this . ptr . as_ptr ()) ; this . fallible () . await } # [doc = " Converts this task into a [`FallibleTask`]."] # [doc = ""] # [doc = " Like [`Task`], a fallible task will poll the task's output until it is"] # [doc = " completed or cancelled due to its [`Runnable`][`super::Runnable`] being"] # [doc = " dropped without being run. Resolves to the task's output when completed,"] # [doc = " or [`None`] if it didn't complete."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use smol::{future, Executor};"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let ex = Executor::new();"] # [doc = ""] # [doc = " // Spawn a future onto the executor."] # [doc = " let task = ex.spawn(async {"] # [doc = "     println!(\"Hello from a task!\");"] # [doc = "     1 + 2"] # [doc = " })"] # [doc = " .fallible();"] # [doc = ""] # [doc = " // Run an executor thread."] # [doc = " thread::spawn(move || future::block_on(ex.run(future::pending::<()>())));"] # [doc = ""] # [doc = " // Wait for the task's output."] # [doc = " assert_eq!(future::block_on(task), Some(3));"] # [doc = " ```"] # [doc = ""] # [doc = " ```"] # [doc = " use smol::future;"] # [doc = ""] # [doc = " // Schedule function which drops the runnable without running it."] # [doc = " let schedule = move |runnable| drop(runnable);"] # [doc = ""] # [doc = " // Create a task with the future and the schedule function."] # [doc = " let (runnable, task) = async_task::spawn(async {"] # [doc = "     println!(\"Hello from a task!\");"] # [doc = "     1 + 2"] # [doc = " }, schedule);"] # [doc = " runnable.schedule();"] # [doc = ""] # [doc = " // Wait for the task's output."] # [doc = " assert_eq!(future::block_on(task.fallible()), None);"] # [doc = " ```"] pub fn fallible (self) -> FallibleTask < T , M > { FallibleTask { task : self } } fn header_with_metadata (& self) -> & HeaderWithMetadata < M > { let ptr = self . ptr . as_ptr () ; let header = ptr as * const HeaderWithMetadata < M > ; unsafe { & * header } } # [doc = " Returns `true` if the current task is finished."] # [doc = ""] # [doc = " Note that in a multithreaded environment, this task can change finish immediately after calling this function."] pub fn is_finished (& self) -> bool { let ptr = self . ptr . as_ptr () ; let header = ptr as * const Header ; unsafe { let state = (* header) . state . load (Ordering :: Acquire) ; state & (CLOSED | COMPLETED) != 0 } } # [doc = " Get the metadata associated with this task."] # [doc = ""] # [doc = " Tasks can be created with a metadata object associated with them; by default, this"] # [doc = " is a `()` value. See the [`Builder::metadata()`] method for more information."] pub fn metadata (& self) -> & M { let ptr = self . ptr . as_ptr () ; let header = ptr as * const HeaderWithMetadata < M > ; & unsafe { & * header } . metadata } }
};
}
