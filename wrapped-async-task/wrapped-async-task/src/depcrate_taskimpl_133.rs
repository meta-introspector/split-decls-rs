// Generated macro for impl_133 (impl)
macro_rules! Depcrate_taskimpl_133 {
() => {
// Module: crate::task
// Provides: {"impl_133"}
// Dependencies: {}
impl < T , M > FallibleTask < T , M > { # [doc = " Detaches the task to let it keep running in the background."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use smol::{Executor, Timer};"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " let ex = Executor::new();"] # [doc = ""] # [doc = " // Spawn a daemon future."] # [doc = " ex.spawn(async {"] # [doc = "     loop {"] # [doc = "         println!(\"I'm a daemon task looping forever.\");"] # [doc = "         Timer::after(Duration::from_secs(1)).await;"] # [doc = "     }"] # [doc = " })"] # [doc = " .fallible()"] # [doc = " .detach();"] # [doc = " ```"] pub fn detach (self) { self . task . detach () } # [doc = " Cancels the task and waits for it to stop running."] # [doc = ""] # [doc = " Returns the task's output if it was completed just before it got canceled, or [`None`] if"] # [doc = " it didn't complete."] # [doc = ""] # [doc = " While it's possible to simply drop the [`Task`] to cancel it, this is a cleaner way of"] # [doc = " canceling because it also waits for the task to stop running."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return; } // Miri does not support epoll"] # [doc = " use smol::{future, Executor, Timer};"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " let ex = Executor::new();"] # [doc = ""] # [doc = " // Spawn a daemon future."] # [doc = " let task = ex.spawn(async {"] # [doc = "     loop {"] # [doc = "         println!(\"Even though I'm in an infinite loop, you can still cancel me!\");"] # [doc = "         Timer::after(Duration::from_secs(1)).await;"] # [doc = "     }"] # [doc = " })"] # [doc = " .fallible();"] # [doc = ""] # [doc = " // Run an executor thread."] # [doc = " thread::spawn(move || future::block_on(ex.run(future::pending::<()>())));"] # [doc = ""] # [doc = " future::block_on(async {"] # [doc = "     Timer::after(Duration::from_secs(3)).await;"] # [doc = "     task.cancel().await;"] # [doc = " });"] # [doc = " ```"] pub async fn cancel (self) -> Option < T > { self . task . cancel () . await } # [doc = " Returns `true` if the current task is finished."] # [doc = ""] # [doc = " Note that in a multithreaded environment, this task can change finish immediately after calling this function."] pub fn is_finished (& self) -> bool { self . task . is_finished () } }
};
}
