// Generated macro for impl_93 (impl)
macro_rules! Depcrate_runnableimpl_93 {
() => {
// Module: crate::runnable
// Provides: {"impl_93"}
// Dependencies: {}
impl < M > Runnable < M > { # [doc = " Get the metadata associated with this task."] # [doc = ""] # [doc = " Tasks can be created with a metadata object associated with them; by default, this"] # [doc = " is a `()` value. See the [`Builder::metadata()`] method for more information."] pub fn metadata (& self) -> & M { & self . header_with_metadata () . metadata } # [doc = " Schedules the task."] # [doc = ""] # [doc = " This is a convenience method that passes the [`Runnable`] to the schedule function."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " // A function that schedules the task when it gets woken up."] # [doc = " let (s, r) = flume::unbounded();"] # [doc = " let schedule = move |runnable| s.send(runnable).unwrap();"] # [doc = ""] # [doc = " // Create a task with a simple future and the schedule function."] # [doc = " let (runnable, task) = async_task::spawn(async {}, schedule);"] # [doc = ""] # [doc = " // Schedule the task."] # [doc = " assert_eq!(r.len(), 0);"] # [doc = " runnable.schedule();"] # [doc = " assert_eq!(r.len(), 1);"] # [doc = " # let handle = std::thread::spawn(move || { for runnable in r { runnable.run(); }});"] # [doc = " # smol::future::block_on(task);"] # [doc = " # handle.join().unwrap();"] # [doc = " ```"] pub fn schedule (self) { let ptr = self . ptr . as_ptr () ; let header = ptr as * const Header ; mem :: forget (self) ; unsafe { ((* header) . vtable . schedule) (ptr , ScheduleInfo :: new (false)) ; } } # [doc = " Runs the task by polling its future."] # [doc = ""] # [doc = " Returns `true` if the task was woken while running, in which case the [`Runnable`] gets"] # [doc = " rescheduled at the end of this method invocation. Otherwise, returns `false` and the"] # [doc = " [`Runnable`] vanishes until the task is woken."] # [doc = " The return value is just a hint: `true` usually indicates that the task has yielded, i.e."] # [doc = " it woke itself and then gave the control back to the executor."] # [doc = ""] # [doc = " If the [`Task`] handle was dropped or if [`cancel()`][`Task::cancel()`] was called, then"] # [doc = " this method simply destroys the task."] # [doc = ""] # [doc = " If the polled future panics, this method propagates the panic, and awaiting the [`Task`]"] # [doc = " after that will also result in a panic."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " // A function that schedules the task when it gets woken up."] # [doc = " let (s, r) = flume::unbounded();"] # [doc = " let schedule = move |runnable| s.send(runnable).unwrap();"] # [doc = ""] # [doc = " // Create a task with a simple future and the schedule function."] # [doc = " let (runnable, task) = async_task::spawn(async { 1 + 2 }, schedule);"] # [doc = ""] # [doc = " // Run the task and check its output."] # [doc = " runnable.run();"] # [doc = " assert_eq!(smol::future::block_on(task), 3);"] # [doc = " ```"] pub fn run (self) -> bool { let ptr = self . ptr . as_ptr () ; let header = ptr as * const Header ; mem :: forget (self) ; unsafe { ((* header) . vtable . run) (ptr) } } # [doc = " Returns a waker associated with this task."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use smol::future;"] # [doc = ""] # [doc = " // A function that schedules the task when it gets woken up."] # [doc = " let (s, r) = flume::unbounded();"] # [doc = " let schedule = move |runnable| s.send(runnable).unwrap();"] # [doc = ""] # [doc = " // Create a task with a simple future and the schedule function."] # [doc = " let (runnable, task) = async_task::spawn(future::pending::<()>(), schedule);"] # [doc = ""] # [doc = " // Take a waker and run the task."] # [doc = " let waker = runnable.waker();"] # [doc = " runnable.run();"] # [doc = ""] # [doc = " // Reschedule the task by waking it."] # [doc = " assert_eq!(r.len(), 0);"] # [doc = " waker.wake();"] # [doc = " assert_eq!(r.len(), 1);"] # [doc = " # let handle = std::thread::spawn(move || { for runnable in r { runnable.run(); }});"] # [doc = " # smol::future::block_on(task.cancel()); // cancel because the future is future::pending"] # [doc = " # handle.join().unwrap();"] # [doc = " ```"] pub fn waker (& self) -> Waker { unsafe { let raw_waker = Header :: clone_waker (self . ptr . as_ptr ()) ; Waker :: from_raw (raw_waker) } } fn header (& self) -> & Header { unsafe { & * (self . ptr . as_ptr () as * const Header) } } fn header_with_metadata (& self) -> & HeaderWithMetadata < M > { unsafe { & * (self . ptr . as_ptr () as * const HeaderWithMetadata < M >) } } # [doc = " Converts this task into a raw pointer."] # [doc = ""] # [doc = " To avoid a memory leak the pointer must be converted back to a Runnable using [`Runnable<M>::from_raw`][from_raw]."] # [doc = ""] # [doc = " `into_raw` does not change the state of the [`Task`], but there is no guarantee that it will be in the same state after calling [`Runnable<M>::from_raw`][from_raw],"] # [doc = " as the corresponding [`Task`] might have been dropped or cancelled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use async_task::{Runnable, spawn};"] # [doc = ""] # [doc = " let (runnable, task) = spawn(async {}, |_| {});"] # [doc = " let runnable_pointer = runnable.into_raw();"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     // Convert back to an `Runnable` to prevent leak."] # [doc = "     let runnable = Runnable::<()>::from_raw(runnable_pointer);"] # [doc = "     runnable.run();"] # [doc = "     // Further calls to `Runnable::from_raw(runnable_pointer)` would be memory-unsafe."] # [doc = " }"] # [doc = " // The memory was freed when `x` went out of scope above, so `runnable_pointer` is now dangling!"] # [doc = " ```"] # [doc = " [from_raw]: #method.from_raw"] pub fn into_raw (self) -> NonNull < () > { let ptr = self . ptr ; mem :: forget (self) ; ptr } # [doc = " Converts a raw pointer into a Runnable."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method should only be used with raw pointers returned from [`Runnable<M>::into_raw`][into_raw]."] # [doc = " It is not safe to use the provided pointer once it is passed to `from_raw`."] # [doc = " Crucially, it is unsafe to call `from_raw` multiple times with the same pointer - even if the resulting [`Runnable`] is not used -"] # [doc = " as internally `async-task` uses reference counting."] # [doc = ""] # [doc = " It is however safe to call [`Runnable<M>::into_raw`][into_raw] on a [`Runnable`] created with `from_raw` or"] # [doc = " after the [`Task`] associated with a given Runnable has been dropped or cancelled."] # [doc = ""] # [doc = " The state of the [`Runnable`] created with `from_raw` is not specified."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use async_task::{Runnable, spawn};"] # [doc = ""] # [doc = " let (runnable, task) = spawn(async {}, |_| {});"] # [doc = " let runnable_pointer = runnable.into_raw();"] # [doc = ""] # [doc = " drop(task);"] # [doc = " unsafe {"] # [doc = "     // Convert back to an `Runnable` to prevent leak."] # [doc = "     let runnable = Runnable::<()>::from_raw(runnable_pointer);"] # [doc = "     let did_poll = runnable.run();"] # [doc = "     assert!(!did_poll);"] # [doc = "     // Further calls to `Runnable::from_raw(runnable_pointer)` would be memory-unsafe."] # [doc = " }"] # [doc = " // The memory was freed when `x` went out of scope above, so `runnable_pointer` is now dangling!"] # [doc = " ```"] # [doc = ""] # [doc = " [into_raw]: #method.into_raw"] pub unsafe fn from_raw (ptr : NonNull < () >) -> Self { Self { ptr , _marker : Default :: default () , } } }
};
}
