// Generated macro for ScheduleInfo (struct)
macro_rules! Depcrate_runnableScheduleInfo {
() => {
// Module: crate::runnable
// Provides: {"ScheduleInfo"}
// Dependencies: {}
# [doc = " Extra scheduling information that can be passed to the scheduling function."] # [doc = ""] # [doc = " The data source of this struct is directly from the actual implementation"] # [doc = " of the crate itself, different from [`Runnable`]'s metadata, which is"] # [doc = " managed by the caller."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_task::{Runnable, ScheduleInfo, WithInfo};"] # [doc = " use std::sync::{Arc, Mutex};"] # [doc = ""] # [doc = " // The future inside the task."] # [doc = " let future = async {"] # [doc = "     println!(\"Hello, world!\");"] # [doc = " };"] # [doc = ""] # [doc = " // If the task gets woken up while running, it will be sent into this channel."] # [doc = " let (s, r) = flume::unbounded();"] # [doc = " // Otherwise, it will be placed into this slot."] # [doc = " let lifo_slot = Arc::new(Mutex::new(None));"] # [doc = " let schedule = move |runnable: Runnable, info: ScheduleInfo| {"] # [doc = "     if info.woken_while_running {"] # [doc = "         s.send(runnable).unwrap()"] # [doc = "     } else {"] # [doc = "         let last = lifo_slot.lock().unwrap().replace(runnable);"] # [doc = "         if let Some(last) = last {"] # [doc = "             s.send(last).unwrap()"] # [doc = "         }"] # [doc = "     }"] # [doc = " };"] # [doc = ""] # [doc = " // Create the actual scheduler to be spawned with some future."] # [doc = " let scheduler = WithInfo(schedule);"] # [doc = " // Create a task with the future and the scheduler."] # [doc = " let (runnable, task) = async_task::spawn(future, scheduler);"] # [doc = " ```"] # [derive (Debug , Copy , Clone)] # [non_exhaustive] pub struct ScheduleInfo { # [doc = " Indicates whether the task gets woken up while running."] # [doc = ""] # [doc = " It is set to true usually because the task has yielded itself to the"] # [doc = " scheduler."] pub woken_while_running : bool , }
};
}
