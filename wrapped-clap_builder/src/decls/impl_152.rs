macro_rules! deps {
    () => {
        ValueRange!();
    };
}

macro_rules! impl_152 {
    () => {
        deps!();
        impl ValueRange { # [doc = " Nor argument values, or a flag"] pub const EMPTY : Self = Self { start_inclusive : 0 , end_inclusive : 0 , } ; # [doc = " A single argument value, the most common case for options"] pub const SINGLE : Self = Self { start_inclusive : 1 , end_inclusive : 1 , } ; # [cfg (debug_assertions)] pub (crate) const OPTIONAL : Self = Self { start_inclusive : 0 , end_inclusive : 1 , } ; pub (crate) const FULL : Self = Self { start_inclusive : 0 , end_inclusive : usize :: MAX , } ; # [doc = " Create a range"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the end is less than the start (debug builds)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use clap_builder as clap;"] # [doc = " # use clap::builder::ValueRange;"] # [doc = " let range = ValueRange::new(5);"] # [doc = " let range = ValueRange::new(5..10);"] # [doc = " let range = ValueRange::new(5..=10);"] # [doc = " let range = ValueRange::new(5..);"] # [doc = " let range = ValueRange::new(..10);"] # [doc = " let range = ValueRange::new(..=10);"] # [doc = " ```"] # [doc = ""] # [doc = " While this will panic:"] # [doc = " ```should_panic"] # [doc = " # use clap_builder as clap;"] # [doc = " # use clap::builder::ValueRange;"] # [doc = " let range = ValueRange::new(10..5);  // Panics!"] # [doc = " ```"] pub fn new (range : impl Into < Self >) -> Self { range . into () } pub (crate) fn raw (start_inclusive : usize , end_inclusive : usize) -> Self { debug_assert ! (start_inclusive <= end_inclusive) ; Self { start_inclusive , end_inclusive , } } # [doc = " Fewest number of values the argument accepts"] pub fn min_values (& self) -> usize { self . start_inclusive } # [doc = " Most number of values the argument accepts"] pub fn max_values (& self) -> usize { self . end_inclusive } # [doc = " Report whether the argument takes any values (ie is a flag)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use clap_builder as clap;"] # [doc = " # use clap::builder::ValueRange;"] # [doc = " let range = ValueRange::new(5);"] # [doc = " assert!(range.takes_values());"] # [doc = ""] # [doc = " let range = ValueRange::new(0);"] # [doc = " assert!(!range.takes_values());"] # [doc = " ```"] pub fn takes_values (& self) -> bool { self . end_inclusive != 0 } pub (crate) fn is_unbounded (& self) -> bool { self . end_inclusive == usize :: MAX } pub (crate) fn is_fixed (& self) -> bool { self . start_inclusive == self . end_inclusive } pub (crate) fn is_multiple (& self) -> bool { self . start_inclusive != self . end_inclusive || 1 < self . start_inclusive } pub (crate) fn num_values (& self) -> Option < usize > { self . is_fixed () . then_some (self . start_inclusive) } pub (crate) fn accepts_more (& self , current : usize) -> bool { current < self . end_inclusive } }
    };
}

impl_152!();