// Generated macro for Parser (trait)
macro_rules! Depcrate_deriveParser {
() => {
// Module: crate::derive
// Provides: {"Parser"}
// Dependencies: {}
# [doc = " Parse command-line arguments into `Self`."] # [doc = ""] # [doc = " The primary one-stop-shop trait used to create an instance of a `clap`"] # [doc = " [`Command`], conduct the parsing, and turn the resulting [`ArgMatches`] back"] # [doc = " into concrete instance of the user struct."] # [doc = ""] # [doc = " This trait is primarily a convenience on top of [`FromArgMatches`] +"] # [doc = " [`CommandFactory`] which uses those two underlying traits to build the two"] # [doc = " fundamental functions `parse` which uses the `std::env::args_os` iterator,"] # [doc = " and `parse_from` which allows the consumer to supply the iterator (along"] # [doc = " with fallible options for each)."] # [doc = ""] # [doc = " See also [`Subcommand`] and [`Args`]."] # [doc = ""] # [doc = " <div class=\"warning\">"] # [doc = ""] # [doc = " **NOTE:** Deriving requires the `derive` feature flag"] # [doc = ""] # [doc = " </div>"] pub trait Parser : FromArgMatches + CommandFactory + Sized { # [doc = " Parse from `std::env::args_os()`, [exit][Error::exit] on error."] fn parse () -> Self { let mut matches = < Self as CommandFactory > :: command () . get_matches () ; let res = < Self as FromArgMatches > :: from_arg_matches_mut (& mut matches) . map_err (format_error :: < Self >) ; match res { Ok (s) => s , Err (e) => { e . exit () } } } # [doc = " Parse from `std::env::args_os()`, return Err on error."] fn try_parse () -> Result < Self , Error > { let mut matches = ok ! (< Self as CommandFactory >:: command () . try_get_matches ()) ; < Self as FromArgMatches > :: from_arg_matches_mut (& mut matches) . map_err (format_error :: < Self >) } # [doc = " Parse from iterator, [exit][Error::exit] on error."] fn parse_from < I , T > (itr : I) -> Self where I : IntoIterator < Item = T > , T : Into < OsString > + Clone , { let mut matches = < Self as CommandFactory > :: command () . get_matches_from (itr) ; let res = < Self as FromArgMatches > :: from_arg_matches_mut (& mut matches) . map_err (format_error :: < Self >) ; match res { Ok (s) => s , Err (e) => { e . exit () } } } # [doc = " Parse from iterator, return Err on error."] fn try_parse_from < I , T > (itr : I) -> Result < Self , Error > where I : IntoIterator < Item = T > , T : Into < OsString > + Clone , { let mut matches = ok ! (< Self as CommandFactory >:: command () . try_get_matches_from (itr)) ; < Self as FromArgMatches > :: from_arg_matches_mut (& mut matches) . map_err (format_error :: < Self >) } # [doc = " Update from iterator, [exit][Error::exit] on error."] # [doc = ""] # [doc = " Unlike [`Parser::parse`], this works with an existing instance of `self`."] # [doc = " The assumption is that all required fields are already provided and any [`Args`] or"] # [doc = " [`Subcommand`]s provided by the user will modify only what is specified."] fn update_from < I , T > (& mut self , itr : I) where I : IntoIterator < Item = T > , T : Into < OsString > + Clone , { let mut matches = < Self as CommandFactory > :: command_for_update () . get_matches_from (itr) ; let res = < Self as FromArgMatches > :: update_from_arg_matches_mut (self , & mut matches) . map_err (format_error :: < Self >) ; if let Err (e) = res { e . exit () } } # [doc = " Update from iterator, return Err on error."] fn try_update_from < I , T > (& mut self , itr : I) -> Result < () , Error > where I : IntoIterator < Item = T > , T : Into < OsString > + Clone , { let mut matches = ok ! (< Self as CommandFactory >:: command_for_update () . try_get_matches_from (itr)) ; < Self as FromArgMatches > :: update_from_arg_matches_mut (self , & mut matches) . map_err (format_error :: < Self >) } }
};
}
