// Generated macro for display_width (function)
macro_rules! Depcrate_output_textwrap_coredisplay_width {
() => {
// Module: crate::output::textwrap::core
// Provides: {"display_width"}
// Dependencies: {}
# [doc = " Compute the display width of `text`"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " **Note:** When the `unicode` Cargo feature is disabled, all characters are presumed to take up"] # [doc = " 1 width.  With the feature enabled, function will correctly deal with [combining characters] in"] # [doc = " their decomposed form (see [Unicode equivalence])."] # [doc = ""] # [doc = " An example of a decomposed character is “é”, which can be decomposed into: “e” followed by a"] # [doc = " combining acute accent: “◌\u{301}”.  Without the `unicode` Cargo feature, every `char` has a width of"] # [doc = " 1. This includes the combining accent:"] # [doc = ""] # [doc = " ## Emojis and CJK Characters"] # [doc = ""] # [doc = " Characters such as emojis and [CJK characters] used in the"] # [doc = " Chinese, Japanese, and Korean languages are seen as double-width,"] # [doc = " even if the `unicode-width` feature is disabled:"] # [doc = ""] # [doc = " # Limitations"] # [doc = ""] # [doc = " The displayed width of a string cannot always be computed from the"] # [doc = " string alone. This is because the width depends on the rendering"] # [doc = " engine used. This is particularly visible with [emoji modifier"] # [doc = " sequences] where a base emoji is modified with, e.g., skin tone or"] # [doc = " hair color modifiers. It is up to the rendering engine to detect"] # [doc = " this and to produce a suitable emoji."] # [doc = ""] # [doc = " A simple example is “❤\u{fe0f}”, which consists of “❤” (U+2764: Black"] # [doc = " Heart Symbol) followed by U+FE0F (Variation Selector-16). By"] # [doc = " itself, “❤” is a black heart, but if you follow it with the"] # [doc = " variant selector, you may get a wider red heart."] # [doc = ""] # [doc = " A more complex example would be “👨\u{200d}🦰” which should depict a man"] # [doc = " with red hair. Here the computed width is too large — and the"] # [doc = " width differs depending on the use of the `unicode-width` feature:"] # [doc = ""] # [doc = " This happens because the grapheme consists of three code points:"] # [doc = " “👨” (U+1F468: Man), Zero Width Joiner (U+200D), and “🦰”"] # [doc = " (U+1F9B0: Red Hair). You can see them above in the test. With"] # [doc = " `unicode-width` enabled, the ZWJ is correctly seen as having zero"] # [doc = " width, without it is counted as a double-width character."] # [doc = ""] # [doc = " ## Terminal Support"] # [doc = ""] # [doc = " Modern browsers typically do a great job at combining characters"] # [doc = " as shown above, but terminals often struggle more. As an example,"] # [doc = " Gnome Terminal version 3.38.1, shows “❤\u{fe0f}” as a big red heart, but"] # [doc = " shows \"👨\u{200d}🦰\" as “👨🦰”."] # [doc = ""] # [doc = " [combining characters]: https://en.wikipedia.org/wiki/Combining_character"] # [doc = " [Unicode equivalence]: https://en.wikipedia.org/wiki/Unicode_equivalence"] # [doc = " [CJK characters]: https://en.wikipedia.org/wiki/CJK_characters"] # [doc = " [emoji modifier sequences]: https://unicode.org/emoji/charts/full-emoji-modifiers.html"] # [inline (never)] pub (crate) fn display_width (text : & str) -> usize { let mut width = 0 ; let mut control_sequence = false ; let control_terminate : char = 'm' ; for ch in text . chars () { if ch . is_ascii_control () { control_sequence = true ; } else if control_sequence && ch == control_terminate { control_sequence = false ; continue ; } if ! control_sequence { width += ch_width (ch) ; } } width }
};
}
