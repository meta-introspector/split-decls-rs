macro_rules! deps {
    () => {
        Vtable!();
    };
}

macro_rules! Bytes {
    () => {
        deps!();
        # [doc = " A cheaply cloneable and sliceable chunk of contiguous memory."] # [doc = ""] # [doc = " `Bytes` is an efficient container for storing and operating on contiguous"] # [doc = " slices of memory. It is intended for use primarily in networking code, but"] # [doc = " could have applications elsewhere as well."] # [doc = ""] # [doc = " `Bytes` values facilitate zero-copy network programming by allowing multiple"] # [doc = " `Bytes` objects to point to the same underlying memory."] # [doc = ""] # [doc = " `Bytes` does not have a single implementation. It is an interface, whose"] # [doc = " exact behavior is implemented through dynamic dispatch in several underlying"] # [doc = " implementations of `Bytes`."] # [doc = ""] # [doc = " All `Bytes` implementations must fulfill the following requirements:"] # [doc = " - They are cheaply cloneable and thereby shareable between an unlimited amount"] # [doc = "   of components, for example by modifying a reference count."] # [doc = " - Instances can be sliced to refer to a subset of the original buffer."] # [doc = ""] # [doc = " ```"] # [doc = " use bytes::Bytes;"] # [doc = ""] # [doc = " let mut mem = Bytes::from(\"Hello world\");"] # [doc = " let a = mem.slice(0..5);"] # [doc = ""] # [doc = " assert_eq!(a, \"Hello\");"] # [doc = ""] # [doc = " let b = mem.split_to(6);"] # [doc = ""] # [doc = " assert_eq!(mem, \"world\");"] # [doc = " assert_eq!(b, \"Hello \");"] # [doc = " ```"] # [doc = ""] # [doc = " # Memory layout"] # [doc = ""] # [doc = " The `Bytes` struct itself is fairly small, limited to 4 `usize` fields used"] # [doc = " to track information about which segment of the underlying memory the"] # [doc = " `Bytes` handle has access to."] # [doc = ""] # [doc = " `Bytes` keeps both a pointer to the shared state containing the full memory"] # [doc = " slice and a pointer to the start of the region visible by the handle."] # [doc = " `Bytes` also tracks the length of its view into the memory."] # [doc = ""] # [doc = " # Sharing"] # [doc = ""] # [doc = " `Bytes` contains a vtable, which allows implementations of `Bytes` to define"] # [doc = " how sharing/cloning is implemented in detail."] # [doc = " When `Bytes::clone()` is called, `Bytes` will call the vtable function for"] # [doc = " cloning the backing storage in order to share it behind multiple `Bytes`"] # [doc = " instances."] # [doc = ""] # [doc = " For `Bytes` implementations which refer to constant memory (e.g. created"] # [doc = " via `Bytes::from_static()`) the cloning implementation will be a no-op."] # [doc = ""] # [doc = " For `Bytes` implementations which point to a reference counted shared storage"] # [doc = " (e.g. an `Arc<[u8]>`), sharing will be implemented by increasing the"] # [doc = " reference count."] # [doc = ""] # [doc = " Due to this mechanism, multiple `Bytes` instances may point to the same"] # [doc = " shared memory region."] # [doc = " Each `Bytes` instance can point to different sections within that"] # [doc = " memory region, and `Bytes` instances may or may not have overlapping views"] # [doc = " into the memory."] # [doc = ""] # [doc = " The following diagram visualizes a scenario where 2 `Bytes` instances make"] # [doc = " use of an `Arc`-based backing storage, and provide access to different views:"] # [doc = ""] # [doc = " ```text"] # [doc = ""] # [doc = "    Arc ptrs                   ┌─────────┐"] # [doc = "    ________________________ / │ Bytes 2 │"] # [doc = "   /                           └─────────┘"] # [doc = "  /          ┌───────────┐     |         |"] # [doc = " |_________/ │  Bytes 1  │     |         |"] # [doc = " |           └───────────┘     |         |"] # [doc = " |           |           | ___/ data     | tail"] # [doc = " |      data |      tail |/              |"] # [doc = " v           v           v               v"] # [doc = " ┌─────┬─────┬───────────┬───────────────┬─────┐"] # [doc = " │ Arc │     │           │               │     │"] # [doc = " └─────┴─────┴───────────┴───────────────┴─────┘"] # [doc = " ```"] pub struct Bytes { ptr : * const u8 , len : usize , data : AtomicPtr < () > , vtable : & 'static Vtable , }
    };
}

Bytes!()