// Generated macro for Backend (trait)
macro_rules! Depcrate_backendBackend {
() => {
// Module: crate::backend
// Provides: {"Backend"}
// Dependencies: {}
# [doc = " The `Backend` trait provides an abstraction over different terminal libraries. It defines the"] # [doc = " methods required to draw content, manipulate the cursor, and clear the terminal screen."] # [doc = ""] # [doc = " Most applications should not need to interact with the `Backend` trait directly as the"] # [doc = " [`Terminal`] struct provides a higher level interface for interacting with the terminal."] # [doc = ""] # [doc = " [`Terminal`]: https://docs.rs/ratatui/latest/ratatui/struct.Terminal.html"] pub trait Backend { # [doc = " Error type associated with this Backend."] type Error : core :: error :: Error ; # [doc = " Draw the given content to the terminal screen."] # [doc = ""] # [doc = " The content is provided as an iterator over `(u16, u16, &Cell)` tuples, where the first two"] # [doc = " elements represent the x and y coordinates, and the third element is a reference to the"] # [doc = " [`Cell`] to be drawn."] fn draw < 'a , I > (& mut self , content : I) -> Result < () , Self :: Error > where I : Iterator < Item = (u16 , u16 , & 'a Cell) > ; # [doc = " Insert `n` line breaks to the terminal screen."] # [doc = ""] # [doc = " This method is optional and may not be implemented by all backends."] fn append_lines (& mut self , _n : u16) -> Result < () , Self :: Error > { Ok (()) } # [doc = " Hide the cursor on the terminal screen."] # [doc = ""] # [doc = ""] # [doc = " See also [`show_cursor`]."] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust,ignore"] # [doc = " # use ratatui::backend::{TestBackend};"] # [doc = " # let mut backend = TestBackend::new(80, 25);"] # [doc = " use ratatui::backend::Backend;"] # [doc = ""] # [doc = " backend.hide_cursor()?;"] # [doc = " // do something with hidden cursor"] # [doc = " backend.show_cursor()?;"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] # [doc = ""] # [doc = " [`show_cursor`]: Self::show_cursor"] fn hide_cursor (& mut self) -> Result < () , Self :: Error > ; # [doc = " Show the cursor on the terminal screen."] # [doc = ""] # [doc = " See [`hide_cursor`] for an example."] # [doc = ""] # [doc = " [`hide_cursor`]: Self::hide_cursor"] fn show_cursor (& mut self) -> Result < () , Self :: Error > ; # [doc = " Get the current cursor position on the terminal screen."] # [doc = ""] # [doc = " The returned tuple contains the x and y coordinates of the cursor."] # [doc = " The origin (0, 0) is at the top left corner of the screen."] # [doc = ""] # [doc = " See [`set_cursor_position`] for an example."] # [doc = ""] # [doc = " [`set_cursor_position`]: Self::set_cursor_position"] fn get_cursor_position (& mut self) -> Result < Position , Self :: Error > ; # [doc = " Set the cursor position on the terminal screen to the given x and y coordinates."] # [doc = ""] # [doc = " The origin (0, 0) is at the top left corner of the screen."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust,ignore"] # [doc = " # use ratatui::backend::{TestBackend};"] # [doc = " # let mut backend = TestBackend::new(80, 25);"] # [doc = " use ratatui::{backend::Backend, layout::Position};"] # [doc = ""] # [doc = " backend.set_cursor_position(Position { x: 10, y: 20 })?;"] # [doc = " assert_eq!(backend.get_cursor_position()?, Position { x: 10, y: 20 });"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] fn set_cursor_position < P : Into < Position > > (& mut self , position : P) -> Result < () , Self :: Error > ; # [doc = " Get the current cursor position on the terminal screen."] # [doc = ""] # [doc = " The returned tuple contains the x and y coordinates of the cursor. The origin"] # [doc = " (0, 0) is at the top left corner of the screen."] # [deprecated = "use `get_cursor_position()` instead which returns `Result<Position>`"] fn get_cursor (& mut self) -> Result < (u16 , u16) , Self :: Error > { let Position { x , y } = self . get_cursor_position () ? ; Ok ((x , y)) } # [doc = " Set the cursor position on the terminal screen to the given x and y coordinates."] # [doc = ""] # [doc = " The origin (0, 0) is at the top left corner of the screen."] # [deprecated = "use `set_cursor_position((x, y))` instead which takes `impl Into<Position>`"] fn set_cursor (& mut self , x : u16 , y : u16) -> Result < () , Self :: Error > { self . set_cursor_position (Position { x , y }) } # [doc = " Clears the whole terminal screen"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust,ignore"] # [doc = " # use ratatui::backend::{TestBackend};"] # [doc = " # let mut backend = TestBackend::new(80, 25);"] # [doc = " use ratatui::backend::Backend;"] # [doc = ""] # [doc = " backend.clear()?;"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] fn clear (& mut self) -> Result < () , Self :: Error > ; # [doc = " Clears a specific region of the terminal specified by the [`ClearType`] parameter"] # [doc = ""] # [doc = " This method is optional and may not be implemented by all backends. The default"] # [doc = " implementation calls [`clear`] if the `clear_type` is [`ClearType::All`] and returns an"] # [doc = " error otherwise."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust,ignore"] # [doc = " # use ratatui::{backend::{TestBackend}};"] # [doc = " # let mut backend = TestBackend::new(80, 25);"] # [doc = " use ratatui::backend::{Backend, ClearType};"] # [doc = ""] # [doc = " backend.clear_region(ClearType::All)?;"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This method will return an error if the terminal screen could not be cleared. It will also"] # [doc = " return an error if the `clear_type` is not supported by the backend."] # [doc = ""] # [doc = " [`clear`]: Self::clear"] fn clear_region (& mut self , clear_type : ClearType) -> Result < () , Self :: Error > ; # [doc = " Get the size of the terminal screen in columns/rows as a [`Size`]."] # [doc = ""] # [doc = " The returned [`Size`] contains the width and height of the terminal screen."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust,ignore"] # [doc = " # use ratatui::{backend::{TestBackend}};"] # [doc = " # let backend = TestBackend::new(80, 25);"] # [doc = " use ratatui::{backend::Backend, layout::Size};"] # [doc = ""] # [doc = " assert_eq!(backend.size()?, Size::new(80, 25));"] # [doc = " # Result::Ok(())"] # [doc = " ```"] fn size (& self) -> Result < Size , Self :: Error > ; # [doc = " Get the size of the terminal screen in columns/rows and pixels as a [`WindowSize`]."] # [doc = ""] # [doc = " The reason for this not returning only the pixel size, given the redundancy with the"] # [doc = " `size()` method, is that the underlying backends most likely get both values with one"] # [doc = " syscall, and the user is also most likely to need columns and rows along with pixel size."] fn window_size (& mut self) -> Result < WindowSize , Self :: Error > ; # [doc = " Flush any buffered content to the terminal screen."] fn flush (& mut self) -> Result < () , Self :: Error > ; # [doc = " Scroll a region of the screen upwards, where a region is specified by a (half-open) range"] # [doc = " of rows."] # [doc = ""] # [doc = " Each row in the region is replaced by the row `line_count` rows below it, except the bottom"] # [doc = " `line_count` rows, which are replaced by empty rows. If `line_count` is equal to or larger"] # [doc = " than the number of rows in the region, then all rows are replaced with empty rows."] # [doc = ""] # [doc = " If the region includes row 0, then `line_count` rows are copied into the bottom of the"] # [doc = " scrollback buffer. These rows are first taken from the old contents of the region, starting"] # [doc = " from the top. If there aren't sufficient rows in the region, then the remainder are empty"] # [doc = " rows."] # [doc = ""] # [doc = " The position of the cursor afterwards is undefined."] # [doc = ""] # [doc = " The behavior is designed to match what ANSI terminals do when scrolling regions are"] # [doc = " established. With ANSI terminals, a scrolling region can be established with the \"^[[X;Yr\""] # [doc = " sequence, where X and Y define the lines of the region. The scrolling region can be reset"] # [doc = " to be the whole screen with the \"^[[r\" sequence."] # [doc = ""] # [doc = " When a scrolling region is established in an ANSI terminal, various operations' behaviors"] # [doc = " are changed in such a way that the scrolling region acts like a \"virtual screen\". In"] # [doc = " particular, the scrolling sequence \"^[[NS\", which scrolls lines up by a count of N."] # [doc = ""] # [doc = " On an ANSI terminal, this method will probably translate to something like:"] # [doc = " \"^[[X;Yr^[[NS^[[r\". That is, set the scrolling region, scroll up, then reset the scrolling"] # [doc = " region."] # [doc = ""] # [doc = " For examples of how this function is expected to work, refer to the tests for"] # [doc = " [`TestBackend::scroll_region_up`]."] # [cfg (feature = "scrolling-regions")] fn scroll_region_up (& mut self , region : core :: ops :: Range < u16 > , line_count : u16 ,) -> Result < () , Self :: Error > ; # [doc = " Scroll a region of the screen downwards, where a region is specified by a (half-open) range"] # [doc = " of rows."] # [doc = ""] # [doc = " Each row in the region is replaced by the row `line_count` rows above it, except the top"] # [doc = " `line_count` rows, which are replaced by empty rows. If `line_count` is equal to or larger"] # [doc = " than the number of rows in the region, then all rows are replaced with empty rows."] # [doc = ""] # [doc = " The position of the cursor afterwards is undefined."] # [doc = ""] # [doc = " See the documentation for [`Self::scroll_region_down`] for more information about how this"] # [doc = " is expected to be implemented for ANSI terminals. All of that applies, except the ANSI"] # [doc = " sequence to scroll down is \"^[[NT\"."] # [doc = ""] # [doc = " This function is asymmetrical with regards to the scrollback buffer. The reason is that"] # [doc = " this how terminals seem to implement things."] # [doc = ""] # [doc = " For examples of how this function is expected to work, refer to the tests for"] # [doc = " [`TestBackend::scroll_region_down`]."] # [cfg (feature = "scrolling-regions")] fn scroll_region_down (& mut self , region : core :: ops :: Range < u16 > , line_count : u16 ,) -> Result < () , Self :: Error > ; }
};
}
