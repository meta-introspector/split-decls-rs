// Generated macro for CollationSpecialPrimaries (struct)
macro_rules! Depcrate_providerCollationSpecialPrimaries {
() => {
// Module: crate::provider
// Provides: {"CollationSpecialPrimaries"}
// Dependencies: {}
# [doc = " Root-associated additional data that doesn't change in tailorings"] # [doc = ""] # [doc = " These are the fields that logically belong to the root data but"] # [doc = " don't belong to the tailoring data and that are on this separate"] # [doc = " struct, since we have the same struct for a tailoring and the"] # [doc = " bulk of the root."] # [doc = ""] # [doc = " As a practical matter, this struct happens to only carry"] # [doc = " information about what concrete numeric values for primary"] # [doc = " weights are special in particular ways. In principle, when the"] # [doc = " root data is built, the root builder is allowed to assign the"] # [doc = " numeric values as it sees fit, which is why these aren't"] # [doc = " hard-coded."] # [doc = ""] # [doc = " Note: In 2.0.0 and prior, this struct was loaded only if"] # [doc = " it was known at collator construction time (based on options)"] # [doc = " that the data here was going to be needed. With the introduction"] # [doc = " of collation keys and the decision not to introduce a collator"] # [doc = " key generator object separate from the collator, this struct"] # [doc = " is now always loaded."] # [doc = ""] # [doc = " <div class=\"stab unstable\">"] # [doc = " 🚧 This code is considered unstable; it may change at any time, in breaking or non-breaking ways,"] # [doc = " including in SemVer minor releases. While the serde representation of data structs is guaranteed"] # [doc = " to be stable, their Rust representation might not be. Use with caution."] # [doc = " </div>"] # [derive (Debug , PartialEq , Clone , yoke :: Yokeable , zerofrom :: ZeroFrom)] # [cfg_attr (feature = "datagen" , derive (serde :: Serialize , databake :: Bake))] # [cfg_attr (feature = "datagen" , databake (path = icu_collator :: provider))] # [cfg_attr (feature = "serde" , derive (serde :: Deserialize))] pub struct CollationSpecialPrimaries < 'data > { # [doc = " The primaries corresponding to `MaxVariable`"] # [doc = " character classes packed so that each fits in"] # [doc = " 16 bits. Length must match the number of enum"] # [doc = " variants in `MaxVariable`, currently 4."] # [doc = ""] # [doc = " This is potentially followed by 256 bits"] # [doc = " (packed in 16 u16s) to classify every possible"] # [doc = " byte into compressible or non-compressible."] # [cfg_attr (feature = "serde" , serde (borrow))] pub last_primaries : ZeroVec < 'data , u16 > , # [doc = " The high 8 bits of the numeric primary"] pub numeric_primary : u8 , }
};
}
