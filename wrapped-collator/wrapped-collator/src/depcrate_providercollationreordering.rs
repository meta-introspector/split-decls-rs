// Generated macro for CollationReordering (struct)
macro_rules! Depcrate_providerCollationReordering {
() => {
// Module: crate::provider
// Provides: {"CollationReordering"}
// Dependencies: {}
# [doc = " Script reordering data"] # [doc = ""] # [doc = " <div class=\"stab unstable\">"] # [doc = " 🚧 This code is considered unstable; it may change at any time, in breaking or non-breaking ways,"] # [doc = " including in SemVer minor releases. While the serde representation of data structs is guaranteed"] # [doc = " to be stable, their Rust representation might not be. Use with caution."] # [doc = " </div>"] # [derive (Debug , PartialEq , Clone , yoke :: Yokeable , zerofrom :: ZeroFrom)] # [cfg_attr (feature = "datagen" , derive (serde :: Serialize , databake :: Bake))] # [cfg_attr (feature = "datagen" , databake (path = icu_collator :: provider))] # [cfg_attr (feature = "serde" , derive (serde :: Deserialize))] pub struct CollationReordering < 'data > { # [doc = " Limit of last reordered range. 0 if no reordering or no split bytes."] # [doc = ""] # [doc = " Comment from ICU4C's `collationsettings.h`"] pub min_high_no_reorder : u32 , # [doc = " 256-byte table for reordering permutation of primary lead"] # [doc = " bytes; NULL if no reordering. A 0 entry at a non-zero index means"] # [doc = " that the primary lead byte is \"split\" (there are different offsets"] # [doc = " for primaries that share that lead byte) and the reordering offset"] # [doc = " must be determined via the reorderRanges."] # [doc = ""] # [doc = " Comment from ICU4C's `collationsettings.h`"] # [cfg_attr (feature = "serde" , serde (borrow))] pub reorder_table : ZeroVec < 'data , u8 > , # [doc = " Primary-weight ranges for script reordering, to be used by"] # [doc = " reorder(p) for split-reordered primary lead bytes."] # [doc = ""] # [doc = " Each entry is a (limit, offset) pair. The upper 16 bits of the"] # [doc = " entry are the upper 16 bits of the exclusive primary limit of"] # [doc = " a range. Primaries between the previous limit and this one have"] # [doc = " their lead bytes modified by the signed offset (-0xff..+0xff)"] # [doc = " stored in the lower 16 bits."] # [doc = ""] # [doc = " CollationData::makeReorderRanges() writes a full list where the"] # [doc = " first range (at least for terminators and separators) has a 0"] # [doc = " offset. The last range has a non-zero offset. minHighNoReorder"] # [doc = " is set to the limit of that last range."] # [doc = ""] # [doc = " In the settings object, the initial ranges before the first"] # [doc = " split lead byte are omitted for efficiency; they are handled"] # [doc = " by reorder(p) via the reorderTable. If there are no"] # [doc = " split-reordered lead bytes, then no ranges are needed."] # [doc = ""] # [doc = " Comment from ICU4C's `collationsettings.h`; names refer to"] # [doc = " ICU4C."] # [cfg_attr (feature = "serde" , serde (borrow))] pub reorder_ranges : ZeroVec < 'data , u32 > , }
};
}
