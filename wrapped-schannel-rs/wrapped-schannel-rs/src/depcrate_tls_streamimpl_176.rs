// Generated macro for impl_176 (impl)
macro_rules! Depcrate_tls_streamimpl_176 {
() => {
// Module: crate::tls_stream
// Provides: {"impl_176"}
// Dependencies: {}
impl Builder { # [doc = " Returns a new `Builder`."] pub fn new () -> Builder { Builder :: default () } # [doc = " Sets the domain associated with connections created with this `Builder`."] # [doc = ""] # [doc = " The domain will be used for Server Name Indication as well as"] # [doc = " certificate validation."] pub fn domain (& mut self , domain : & str) -> & mut Builder { self . domain = Some (domain . encode_utf16 () . chain (Some (0)) . collect ()) ; self } # [doc = " Determines if Server Name Indication (SNI) will be used."] # [doc = ""] # [doc = " Defaults to `true`."] pub fn use_sni (& mut self , use_sni : bool) -> & mut Builder { self . use_sni = use_sni ; self } # [doc = " Determines if the server's hostname will be checked during certificate verification."] # [doc = ""] # [doc = " Defaults to `false`."] pub fn accept_invalid_hostnames (& mut self , accept_invalid_hostnames : bool) -> & mut Builder { self . accept_invalid_hostnames = accept_invalid_hostnames ; self } # [doc = " Set a verification callback to be used for connections created with this `Builder`."] # [doc = ""] # [doc = " The callback is provided with an io::Result indicating if the (pre)validation was"] # [doc = " successful. The Ok() variant indicates a successful validation while the Err() variant"] # [doc = " contains the errorcode returned from the internal verification process."] # [doc = " The validated certificate, is accessible through the second argument of the closure."] pub fn verify_callback < F > (& mut self , callback : F) -> & mut Builder where F : Fn (CertValidationResult) -> io :: Result < () > + 'static + Sync + Send , { self . verify_callback = Some (Arc :: new (callback)) ; self } # [doc = " Specifies a custom certificate store which is later used when validating"] # [doc = " a server's certificate."] # [doc = ""] # [doc = " This option is only used for client connections and is used to construct"] # [doc = " the certificate chain which the server's certificate is validated"] # [doc = " against."] # [doc = ""] # [doc = " Note that adding certificates here means that they are"] # [doc = " implicitly trusted."] pub fn cert_store (& mut self , cert_store : CertStore) -> & mut Builder { self . cert_store = Some (cert_store) ; self } # [doc = " Requests one of a set of application protocols using alpn"] pub fn request_application_protocols (& mut self , alpns : & [& [u8]]) -> & mut Builder { self . requested_application_protocols = Some (alpns . iter () . map (| bytes | bytes . to_vec ()) . collect :: < Vec < _ > > ()) ; self } # [doc = " Initialize a new TLS session where the stream provided will be"] # [doc = " connecting to a remote TLS server."] # [doc = ""] # [doc = " If the stream provided is a blocking stream then the entire handshake"] # [doc = " will be performed if possible, but if the stream is in nonblocking mode"] # [doc = " then a `HandshakeError::Interrupted` variant may be returned. This"] # [doc = " type can then be extracted to later call"] # [doc = " `MidHandshakeTlsStream::handshake` when data becomes available."] pub fn connect < S > (& mut self , cred : SchannelCred , stream : S ,) -> Result < TlsStream < S > , HandshakeError < S > > where S : Read + Write , { self . initialize (cred , false , stream) } # [doc = " Initialize a new TLS session where the stream provided will be"] # [doc = " accepting a connection."] # [doc = ""] # [doc = " This method will tweak the protocol for \"who talks first\" and also"] # [doc = " currently disables validation of the client that's connecting to us."] # [doc = ""] # [doc = " If the stream provided is a blocking stream then the entire handshake"] # [doc = " will be performed if possible, but if the stream is in nonblocking mode"] # [doc = " then a `HandshakeError::Interrupted` variant may be returned. This"] # [doc = " type can then be extracted to later call"] # [doc = " `MidHandshakeTlsStream::handshake` when data becomes available."] pub fn accept < S > (& mut self , cred : SchannelCred , stream : S ,) -> Result < TlsStream < S > , HandshakeError < S > > where S : Read + Write , { self . initialize (cred , true , stream) } fn initialize < S > (& mut self , mut cred : SchannelCred , server : bool , stream : S ,) -> Result < TlsStream < S > , HandshakeError < S > > where S : Read + Write , { let domain = match self . domain { Some (ref domain) if self . use_sni => Some (& domain [..]) , _ => None , } ; let (ctxt , buf) = match SecurityContext :: initialize (& mut cred , server , domain , & self . requested_application_protocols ,) { Ok (pair) => pair , Err (e) => return Err (HandshakeError :: Failure (e)) , } ; let stream = TlsStream { cred , context : ctxt , cert_store : self . cert_store . clone () , domain : self . domain . clone () , use_sni : self . use_sni , accept_invalid_hostnames : self . accept_invalid_hostnames , verify_callback : self . verify_callback . clone () , stream , server , accept_first : true , state : State :: Initializing { needs_flush : false , more_calls : true , shutting_down : false , validated : false , } , needs_read : 1 , dec_in : Cursor :: new (Vec :: new ()) , enc_in : Cursor :: new (Vec :: new ()) , out_buf : Cursor :: new (buf . map (| b | b . to_owned ()) . unwrap_or_else (Vec :: new)) , last_write_len : 0 , requested_application_protocols : self . requested_application_protocols . clone () , } ; MidHandshakeTlsStream { inner : stream } . handshake () } }
};
}
