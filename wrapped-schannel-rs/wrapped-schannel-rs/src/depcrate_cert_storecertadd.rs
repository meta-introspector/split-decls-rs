// Generated macro for CertAdd (enum)
macro_rules! Depcrate_cert_storeCertAdd {
() => {
// Module: crate::cert_store
// Provides: {"CertAdd"}
// Dependencies: {}
# [doc = " Argument to the `add_cert` function indicating how a certificate should be"] # [doc = " added to a `CertStore`."] pub enum CertAdd { # [doc = " The function makes no check for an existing matching certificate or link"] # [doc = " to a matching certificate. A new certificate is always added to the"] # [doc = " store. This can lead to duplicates in a store."] Always = Cryptography :: CERT_STORE_ADD_ALWAYS as isize , # [doc = " If a matching certificate or a link to a matching certificate exists,"] # [doc = " the operation fails."] New = Cryptography :: CERT_STORE_ADD_NEW as isize , # [doc = " If a matching certificate or a link to a matching certificate exists and"] # [doc = " the NotBefore time of the existing context is equal to or greater than"] # [doc = " the NotBefore time of the new context being added, the operation fails."] # [doc = ""] # [doc = " If the NotBefore time of the existing context is less than the NotBefore"] # [doc = " time of the new context being added, the existing certificate or link is"] # [doc = " deleted and a new certificate is created and added to the store. If a"] # [doc = " matching certificate or a link to a matching certificate does not exist,"] # [doc = " a new link is added."] Newer = Cryptography :: CERT_STORE_ADD_NEWER as isize , # [doc = " If a matching certificate or a link to a matching certificate exists and"] # [doc = " the NotBefore time of the existing context is equal to or greater than"] # [doc = " the NotBefore time of the new context being added, the operation fails."] # [doc = ""] # [doc = " If the NotBefore time of the existing context is less than the NotBefore"] # [doc = " time of the new context being added, the existing context is deleted"] # [doc = " before creating and adding the new context. The new added context"] # [doc = " inherits properties from the existing certificate."] NewerInheritProperties = Cryptography :: CERT_STORE_ADD_NEWER_INHERIT_PROPERTIES as isize , # [doc = " If a link to a matching certificate exists, that existing certificate or"] # [doc = " link is deleted and a new certificate is created and added to the store."] # [doc = " If a matching certificate or a link to a matching certificate does not"] # [doc = " exist, a new link is added."] ReplaceExisting = Cryptography :: CERT_STORE_ADD_REPLACE_EXISTING as isize , # [doc = " If a matching certificate exists in the store, the existing context is"] # [doc = " not replaced. The existing context inherits properties from the new"] # [doc = " certificate."] ReplaceExistingInheritProperties = Cryptography :: CERT_STORE_ADD_REPLACE_EXISTING_INHERIT_PROPERTIES as isize , # [doc = " If a matching certificate or a link to a matching certificate exists,"] # [doc = " that existing certificate or link is used and properties from the"] # [doc = " new certificate are added. The function does not fail, but it does"] # [doc = " not add a new context. The existing context is duplicated and returned."] # [doc = ""] # [doc = " If a matching certificate or a link to a matching certificate does"] # [doc = " not exist, a new certificate is added."] UseExisting = Cryptography :: CERT_STORE_ADD_USE_EXISTING as isize , }
};
}
