// Generated macro for StreamCipher (trait)
macro_rules! Depcrate_dev_streamStreamCipher {
() => {
// Module: crate::dev::stream
// Provides: {"StreamCipher"}
// Dependencies: {}
# [doc = " Stream cipher trait."] # [doc = ""] # [doc = " This trait applies only to synchronous stream ciphers, which generate a keystream and"] # [doc = " XOR data with it during both encryption and decryption. Therefore, instead of separate methods"] # [doc = " for encryption and decryption, this trait provides methods for keystream application."] # [doc = ""] # [doc = " # Notes on Keystream Repetition"] # [doc = " All stream ciphers have a finite state, so the generated keystream inevitably repeats itself,"] # [doc = " making the cipher vulnerable to chosen plaintext attack. Typically, the repetition period is"] # [doc = " astronomically large, rendering keystream repetition impossible to encounter in practice."] # [doc = ""] # [doc = " However, counter-based stream ciphers allow seeking across the keystream, and some also use"] # [doc = " small counters (e.g. 32 bits). This can result in triggering keystream repetition in practice."] # [doc = ""] # [doc = " To guard against this, methods either panic (e.g. [`StreamCipher::apply_keystream`]) or"] # [doc = " return [`StreamCipherError`] (e.g. [`StreamCipher::try_apply_keystream`]) when"] # [doc = " keystream repetition occurs. We also provide a number of \"unchecked\" methods"] # [doc = " (e.g. [`StreamCipher::unchecked_apply_keystream`]), but they should be used with extreme care."] # [doc = ""] # [doc = " For efficiency reasons, the check for keystream repetition is typically implemented by"] # [doc = " forbidding the generation of the last keystream block in both the keystream application methods"] # [doc = " and the seeking methods defined in the [`StreamCipherSeek`] trait."] pub trait StreamCipher { # [doc = " Check that the cipher can generate a keystream with a length of `data_len` bytes."] fn check_remaining (& self , data_len : usize) -> Result < () , StreamCipherError > ; # [doc = " Apply keystream to `inout` without checking for keystream repetition."] # [doc = ""] # [doc = " # WARNING"] # [doc = " This method should be used with extreme caution! Triggering keystream repetition can expose"] # [doc = " the stream cipher to chosen plaintext attacks."] fn unchecked_apply_keystream_inout (& mut self , buf : InOutBuf < '_ , '_ , u8 >) ; # [doc = " Apply keystream to `buf` without checking for keystream repetition."] # [doc = ""] # [doc = " # WARNING"] # [doc = " This method should be used with extreme caution! Triggering keystream repetition can expose"] # [doc = " the stream cipher to chosen plaintext attacks."] fn unchecked_write_keystream (& mut self , buf : & mut [u8]) ; # [doc = " Apply keystream to data behind `buf` without checking for keystream repetition."] # [doc = ""] # [doc = " # WARNING"] # [doc = " This method should be used with extreme caution! Triggering keystream repetition can expose"] # [doc = " the stream cipher to chosen plaintext attacks."] # [inline] fn unchecked_apply_keystream (& mut self , buf : & mut [u8]) { self . unchecked_apply_keystream_inout (buf . into ()) } # [doc = " Apply keystream to data buffer-to-buffer without checking for keystream repetition."] # [doc = ""] # [doc = " It will XOR generated keystream with data from the `input` buffer"] # [doc = " and will write result to the `output` buffer."] # [doc = ""] # [doc = " Returns [`NotEqualError`] if the `input` and `output` buffers have different lengths."] # [doc = ""] # [doc = " # WARNING"] # [doc = " This method should be used with extreme caution! Triggering keystream repetition can expose"] # [doc = " the stream cipher to chosen plaintext attacks."] # [inline] fn unchecked_apply_keystream_b2b (& mut self , input : & [u8] , output : & mut [u8] ,) -> Result < () , NotEqualError > { let buf = InOutBuf :: new (input , output) ? ; self . unchecked_apply_keystream_inout (buf) ; Ok (()) } # [doc = " Apply keystream to `inout` data."] # [doc = ""] # [doc = " If the end of the keystream is reached with the given buffer length,"] # [doc = " the method will return [`StreamCipherError`] without modifying `buf`."] fn try_apply_keystream_inout (& mut self , buf : InOutBuf < '_ , '_ , u8 > ,) -> Result < () , StreamCipherError > { self . check_remaining (buf . len ()) ? ; self . unchecked_apply_keystream_inout (buf) ; Ok (()) } # [doc = " Apply keystream to data behind `buf`."] # [doc = ""] # [doc = " If the end of the keystream is reached with the given buffer length,"] # [doc = " the method will return [`StreamCipherError`] without modifying `buf`."] # [inline] fn try_apply_keystream (& mut self , buf : & mut [u8]) -> Result < () , StreamCipherError > { self . try_apply_keystream_inout (buf . into ()) } # [doc = " Apply keystream to data buffer-to-buffer."] # [doc = ""] # [doc = " It will XOR generated keystream with data from the `input` buffer"] # [doc = " and will write result to the `output` buffer."] # [doc = ""] # [doc = " Returns [`StreamCipherError`] without modifying the buffers if the `input` and `output`"] # [doc = " buffers have different lengths, or if the end of the keystream is reached with"] # [doc = " the given data length."] # [inline] fn try_apply_keystream_b2b (& mut self , input : & [u8] , output : & mut [u8] ,) -> Result < () , StreamCipherError > { InOutBuf :: new (input , output) . map_err (| _ | StreamCipherError) . and_then (| buf | self . try_apply_keystream_inout (buf)) } # [doc = " Write keystream to `buf`."] # [doc = ""] # [doc = " If the end of the keystream is reached with the given buffer length,"] # [doc = " the method will return [`StreamCipherError`] without modifying `buf`."] # [inline] fn try_write_keystream (& mut self , buf : & mut [u8]) -> Result < () , StreamCipherError > { self . check_remaining (buf . len ()) ? ; self . unchecked_write_keystream (buf) ; Ok (()) } # [doc = " Apply keystream to `inout` data."] # [doc = ""] # [doc = " It will XOR generated keystream with the data behind `in` pointer"] # [doc = " and will write result to `out` pointer."] # [doc = ""] # [doc = " # Panics"] # [doc = " If the end of the keystream is reached with the given buffer length."] # [inline] fn apply_keystream_inout (& mut self , buf : InOutBuf < '_ , '_ , u8 >) { self . try_apply_keystream_inout (buf) . unwrap () ; } # [doc = " Apply keystream to data in-place."] # [doc = ""] # [doc = " It will XOR generated keystream with `data` and will write result"] # [doc = " to the same buffer."] # [doc = ""] # [doc = " # Panics"] # [doc = " If the end of the keystream is reached with the given buffer length."] # [inline] fn apply_keystream (& mut self , buf : & mut [u8]) { self . try_apply_keystream (buf) . unwrap () ; } # [doc = " Apply keystream to data buffer-to-buffer."] # [doc = ""] # [doc = " It will XOR generated keystream with data from the `input` buffer"] # [doc = " and will write result to the `output` buffer."] # [doc = ""] # [doc = " # Panics"] # [doc = " If the end of the keystream is reached with the given buffer length,"] # [doc = " of if the `input` and `output` buffers have different lengths."] # [inline] fn apply_keystream_b2b (& mut self , input : & [u8] , output : & mut [u8]) { let Ok (buf) = InOutBuf :: new (input , output) else { panic ! ("Lengths of input and output buffers are not equal to each other!") ; } ; self . apply_keystream_inout (buf) } # [doc = " Write keystream to `buf`."] # [doc = ""] # [doc = " # Panics"] # [doc = " If the end of the keystream is reached with the given buffer length."] # [inline] fn write_keystream (& mut self , buf : & mut [u8]) { self . try_write_keystream (buf) . unwrap () ; } }
};
}
