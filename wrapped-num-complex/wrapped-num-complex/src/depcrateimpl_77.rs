// Generated macro for impl_77 (impl)
macro_rules! Depcrateimpl_77 {
() => {
// Module: crate
// Provides: {"impl_77"}
// Dependencies: {}
# [cfg (any (feature = "std" , feature = "libm"))] impl < T : Float > Complex < T > { # [doc = " Create a new Complex with a given phase: `exp(i * phase)`."] # [doc = " See [cis (mathematics)](https://en.wikipedia.org/wiki/Cis_(mathematics))."] # [inline] pub fn cis (phase : T) -> Self { Self :: new (phase . cos () , phase . sin ()) } # [doc = " Calculate |self|"] # [inline] pub fn norm (self) -> T { self . re . hypot (self . im) } # [doc = " Calculate the principal Arg of self."] # [inline] pub fn arg (self) -> T { self . im . atan2 (self . re) } # [doc = " Convert to polar form (r, theta), such that"] # [doc = " `self = r * exp(i * theta)`"] # [inline] pub fn to_polar (self) -> (T , T) { (self . norm () , self . arg ()) } # [doc = " Convert a polar representation into a complex number."] # [inline] pub fn from_polar (r : T , theta : T) -> Self { Self :: new (r * theta . cos () , r * theta . sin ()) } # [doc = " Computes `e^(self)`, where `e` is the base of the natural logarithm."] # [inline] pub fn exp (self) -> Self { let Complex { re , mut im } = self ; if re . is_infinite () { if re < T :: zero () { if ! im . is_finite () { return Self :: new (T :: zero () , T :: zero ()) ; } } else if im == T :: zero () || ! im . is_finite () { if im . is_infinite () { im = T :: nan () ; } return Self :: new (re , im) ; } } else if re . is_nan () && im == T :: zero () { return self ; } Self :: from_polar (re . exp () , im) } # [doc = " Computes the principal value of natural logarithm of `self`."] # [doc = ""] # [doc = " This function has one branch cut:"] # [doc = ""] # [doc = " * `(-∞, 0]`, continuous from above."] # [doc = ""] # [doc = " The branch satisfies `-π ≤ arg(ln(z)) ≤ π`."] # [inline] pub fn ln (self) -> Self { let (r , theta) = self . to_polar () ; Self :: new (r . ln () , theta) } # [doc = " Computes the principal value of the square root of `self`."] # [doc = ""] # [doc = " This function has one branch cut:"] # [doc = ""] # [doc = " * `(-∞, 0)`, continuous from above."] # [doc = ""] # [doc = " The branch satisfies `-π/2 ≤ arg(sqrt(z)) ≤ π/2`."] # [inline] pub fn sqrt (self) -> Self { if self . im . is_zero () { if self . re . is_sign_positive () { Self :: new (self . re . sqrt () , self . im) } else { let re = T :: zero () ; let im = (- self . re) . sqrt () ; if self . im . is_sign_positive () { Self :: new (re , im) } else { Self :: new (re , - im) } } } else if self . re . is_zero () { let one = T :: one () ; let two = one + one ; let x = (self . im . abs () / two) . sqrt () ; if self . im . is_sign_positive () { Self :: new (x , x) } else { Self :: new (x , - x) } } else { let one = T :: one () ; let two = one + one ; let (r , theta) = self . to_polar () ; Self :: from_polar (r . sqrt () , theta / two) } } # [doc = " Computes the principal value of the cube root of `self`."] # [doc = ""] # [doc = " This function has one branch cut:"] # [doc = ""] # [doc = " * `(-∞, 0)`, continuous from above."] # [doc = ""] # [doc = " The branch satisfies `-π/3 ≤ arg(cbrt(z)) ≤ π/3`."] # [doc = ""] # [doc = " Note that this does not match the usual result for the cube root of"] # [doc = " negative real numbers. For example, the real cube root of `-8` is `-2`,"] # [doc = " but the principal complex cube root of `-8` is `1 + i√3`."] # [inline] pub fn cbrt (self) -> Self { if self . im . is_zero () { if self . re . is_sign_positive () { Self :: new (self . re . cbrt () , self . im) } else { let one = T :: one () ; let two = one + one ; let three = two + one ; let re = (- self . re) . cbrt () / two ; let im = three . sqrt () * re ; if self . im . is_sign_positive () { Self :: new (re , im) } else { Self :: new (re , - im) } } } else if self . re . is_zero () { let one = T :: one () ; let two = one + one ; let three = two + one ; let im = self . im . abs () . cbrt () / two ; let re = three . sqrt () * im ; if self . im . is_sign_positive () { Self :: new (re , im) } else { Self :: new (re , - im) } } else { let one = T :: one () ; let three = one + one + one ; let (r , theta) = self . to_polar () ; Self :: from_polar (r . cbrt () , theta / three) } } # [doc = " Raises `self` to a floating point power."] # [inline] pub fn powf (self , exp : T) -> Self { if exp . is_zero () { return Self :: one () ; } let (r , theta) = self . to_polar () ; Self :: from_polar (r . powf (exp) , theta * exp) } # [doc = " Returns the logarithm of `self` with respect to an arbitrary base."] # [inline] pub fn log (self , base : T) -> Self { let (r , theta) = self . to_polar () ; Self :: new (r . log (base) , theta / base . ln ()) } # [doc = " Raises `self` to a complex power."] # [inline] pub fn powc (self , exp : Self) -> Self { if exp . is_zero () { return Self :: one () ; } (exp * self . ln ()) . exp () } # [doc = " Raises a floating point number to the complex power `self`."] # [inline] pub fn expf (self , base : T) -> Self { Self :: from_polar (base . powf (self . re) , self . im * base . ln ()) } # [doc = " Computes the sine of `self`."] # [inline] pub fn sin (self) -> Self { Self :: new (self . re . sin () * self . im . cosh () , self . re . cos () * self . im . sinh () ,) } # [doc = " Computes the cosine of `self`."] # [inline] pub fn cos (self) -> Self { Self :: new (self . re . cos () * self . im . cosh () , - self . re . sin () * self . im . sinh () ,) } # [doc = " Computes the tangent of `self`."] # [inline] pub fn tan (self) -> Self { let (two_re , two_im) = (self . re + self . re , self . im + self . im) ; Self :: new (two_re . sin () , two_im . sinh ()) . unscale (two_re . cos () + two_im . cosh ()) } # [doc = " Computes the principal value of the inverse sine of `self`."] # [doc = ""] # [doc = " This function has two branch cuts:"] # [doc = ""] # [doc = " * `(-∞, -1)`, continuous from above."] # [doc = " * `(1, ∞)`, continuous from below."] # [doc = ""] # [doc = " The branch satisfies `-π/2 ≤ Re(asin(z)) ≤ π/2`."] # [inline] pub fn asin (self) -> Self { let i = Self :: i () ; - i * ((Self :: one () - self * self) . sqrt () + i * self) . ln () } # [doc = " Computes the principal value of the inverse cosine of `self`."] # [doc = ""] # [doc = " This function has two branch cuts:"] # [doc = ""] # [doc = " * `(-∞, -1)`, continuous from above."] # [doc = " * `(1, ∞)`, continuous from below."] # [doc = ""] # [doc = " The branch satisfies `0 ≤ Re(acos(z)) ≤ π`."] # [inline] pub fn acos (self) -> Self { let i = Self :: i () ; - i * (i * (Self :: one () - self * self) . sqrt () + self) . ln () } # [doc = " Computes the principal value of the inverse tangent of `self`."] # [doc = ""] # [doc = " This function has two branch cuts:"] # [doc = ""] # [doc = " * `(-∞i, -i]`, continuous from the left."] # [doc = " * `[i, ∞i)`, continuous from the right."] # [doc = ""] # [doc = " The branch satisfies `-π/2 ≤ Re(atan(z)) ≤ π/2`."] # [inline] pub fn atan (self) -> Self { let i = Self :: i () ; let one = Self :: one () ; let two = one + one ; if self == i { return Self :: new (T :: zero () , T :: infinity ()) ; } else if self == - i { return Self :: new (T :: zero () , - T :: infinity ()) ; } ((one + i * self) . ln () - (one - i * self) . ln ()) / (two * i) } # [doc = " Computes the hyperbolic sine of `self`."] # [inline] pub fn sinh (self) -> Self { Self :: new (self . re . sinh () * self . im . cos () , self . re . cosh () * self . im . sin () ,) } # [doc = " Computes the hyperbolic cosine of `self`."] # [inline] pub fn cosh (self) -> Self { Self :: new (self . re . cosh () * self . im . cos () , self . re . sinh () * self . im . sin () ,) } # [doc = " Computes the hyperbolic tangent of `self`."] # [inline] pub fn tanh (self) -> Self { let (two_re , two_im) = (self . re + self . re , self . im + self . im) ; Self :: new (two_re . sinh () , two_im . sin ()) . unscale (two_re . cosh () + two_im . cos ()) } # [doc = " Computes the principal value of inverse hyperbolic sine of `self`."] # [doc = ""] # [doc = " This function has two branch cuts:"] # [doc = ""] # [doc = " * `(-∞i, -i)`, continuous from the left."] # [doc = " * `(i, ∞i)`, continuous from the right."] # [doc = ""] # [doc = " The branch satisfies `-π/2 ≤ Im(asinh(z)) ≤ π/2`."] # [inline] pub fn asinh (self) -> Self { let one = Self :: one () ; (self + (one + self * self) . sqrt ()) . ln () } # [doc = " Computes the principal value of inverse hyperbolic cosine of `self`."] # [doc = ""] # [doc = " This function has one branch cut:"] # [doc = ""] # [doc = " * `(-∞, 1)`, continuous from above."] # [doc = ""] # [doc = " The branch satisfies `-π ≤ Im(acosh(z)) ≤ π` and `0 ≤ Re(acosh(z)) < ∞`."] # [inline] pub fn acosh (self) -> Self { let one = Self :: one () ; let two = one + one ; two * (((self + one) / two) . sqrt () + ((self - one) / two) . sqrt ()) . ln () } # [doc = " Computes the principal value of inverse hyperbolic tangent of `self`."] # [doc = ""] # [doc = " This function has two branch cuts:"] # [doc = ""] # [doc = " * `(-∞, -1]`, continuous from above."] # [doc = " * `[1, ∞)`, continuous from below."] # [doc = ""] # [doc = " The branch satisfies `-π/2 ≤ Im(atanh(z)) ≤ π/2`."] # [inline] pub fn atanh (self) -> Self { let one = Self :: one () ; let two = one + one ; if self == one { return Self :: new (T :: infinity () , T :: zero ()) ; } else if self == - one { return Self :: new (- T :: infinity () , T :: zero ()) ; } ((one + self) . ln () - (one - self) . ln ()) / two } # [doc = " Returns `1/self` using floating-point operations."] # [doc = ""] # [doc = " This may be more accurate than the generic `self.inv()` in cases"] # [doc = " where `self.norm_sqr()` would overflow to ∞ or underflow to 0."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use num_complex::Complex64;"] # [doc = " let c = Complex64::new(1e300, 1e300);"] # [doc = ""] # [doc = " // The generic `inv()` will overflow."] # [doc = " assert!(!c.inv().is_normal());"] # [doc = ""] # [doc = " // But we can do better for `Float` types."] # [doc = " let inv = c.finv();"] # [doc = " assert!(inv.is_normal());"] # [doc = " println!(\"{:e}\", inv);"] # [doc = ""] # [doc = " let expected = Complex64::new(5e-301, -5e-301);"] # [doc = " assert!((inv - expected).norm() < 1e-315);"] # [doc = " ```"] # [inline] pub fn finv (self) -> Complex < T > { let norm = self . norm () ; self . conj () / norm / norm } # [doc = " Returns `self/other` using floating-point operations."] # [doc = ""] # [doc = " This may be more accurate than the generic `Div` implementation in cases"] # [doc = " where `other.norm_sqr()` would overflow to ∞ or underflow to 0."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use num_complex::Complex64;"] # [doc = " let a = Complex64::new(2.0, 3.0);"] # [doc = " let b = Complex64::new(1e300, 1e300);"] # [doc = ""] # [doc = " // Generic division will overflow."] # [doc = " assert!(!(a / b).is_normal());"] # [doc = ""] # [doc = " // But we can do better for `Float` types."] # [doc = " let quotient = a.fdiv(b);"] # [doc = " assert!(quotient.is_normal());"] # [doc = " println!(\"{:e}\", quotient);"] # [doc = ""] # [doc = " let expected = Complex64::new(2.5e-300, 5e-301);"] # [doc = " assert!((quotient - expected).norm() < 1e-315);"] # [doc = " ```"] # [inline] pub fn fdiv (self , other : Complex < T >) -> Complex < T > { self * other . finv () } }
};
}
