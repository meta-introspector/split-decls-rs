// Generated macro for impl_1234 (impl)
macro_rules! Depcrate_services_fs_serve_fileimpl_1234 {
() => {
// Module: crate::services::fs::serve_file
// Provides: {"impl_1234"}
// Dependencies: {}
impl ServeFile { # [doc = " Create a new [`ServeFile`]."] # [doc = ""] # [doc = " The `Content-Type` will be guessed from the file extension."] pub fn new < P : AsRef < Path > > (path : P) -> Self { let guess = mime_guess :: from_path (path . as_ref ()) ; let mime = guess . first_raw () . map (HeaderValue :: from_static) . unwrap_or_else (| | { HeaderValue :: from_str (mime :: APPLICATION_OCTET_STREAM . as_ref ()) . unwrap () }) ; Self (ServeDir :: new_single_file (path , mime)) } # [doc = " Create a new [`ServeFile`] with a specific mime type."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Will panic if the mime type isn't a valid [header value]."] # [doc = ""] # [doc = " [header value]: https://docs.rs/http/latest/http/header/struct.HeaderValue.html"] pub fn new_with_mime < P : AsRef < Path > > (path : P , mime : & Mime) -> Self { let mime = HeaderValue :: from_str (mime . as_ref ()) . expect ("mime isn't a valid header value") ; Self (ServeDir :: new_single_file (path , mime)) } # [doc = " Informs the service that it should also look for a precompressed gzip"] # [doc = " version of the file."] # [doc = ""] # [doc = " If the client has an `Accept-Encoding` header that allows the gzip encoding,"] # [doc = " the file `foo.txt.gz` will be served instead of `foo.txt`."] # [doc = " If the precompressed file is not available, or the client doesn't support it,"] # [doc = " the uncompressed version will be served instead."] # [doc = " Both the precompressed version and the uncompressed version are expected"] # [doc = " to be present in the same directory. Different precompressed"] # [doc = " variants can be combined."] pub fn precompressed_gzip (self) -> Self { Self (self . 0 . precompressed_gzip ()) } # [doc = " Informs the service that it should also look for a precompressed brotli"] # [doc = " version of the file."] # [doc = ""] # [doc = " If the client has an `Accept-Encoding` header that allows the brotli encoding,"] # [doc = " the file `foo.txt.br` will be served instead of `foo.txt`."] # [doc = " If the precompressed file is not available, or the client doesn't support it,"] # [doc = " the uncompressed version will be served instead."] # [doc = " Both the precompressed version and the uncompressed version are expected"] # [doc = " to be present in the same directory. Different precompressed"] # [doc = " variants can be combined."] pub fn precompressed_br (self) -> Self { Self (self . 0 . precompressed_br ()) } # [doc = " Informs the service that it should also look for a precompressed deflate"] # [doc = " version of the file."] # [doc = ""] # [doc = " If the client has an `Accept-Encoding` header that allows the deflate encoding,"] # [doc = " the file `foo.txt.zz` will be served instead of `foo.txt`."] # [doc = " If the precompressed file is not available, or the client doesn't support it,"] # [doc = " the uncompressed version will be served instead."] # [doc = " Both the precompressed version and the uncompressed version are expected"] # [doc = " to be present in the same directory. Different precompressed"] # [doc = " variants can be combined."] pub fn precompressed_deflate (self) -> Self { Self (self . 0 . precompressed_deflate ()) } # [doc = " Informs the service that it should also look for a precompressed zstd"] # [doc = " version of the file."] # [doc = ""] # [doc = " If the client has an `Accept-Encoding` header that allows the zstd encoding,"] # [doc = " the file `foo.txt.zst` will be served instead of `foo.txt`."] # [doc = " If the precompressed file is not available, or the client doesn't support it,"] # [doc = " the uncompressed version will be served instead."] # [doc = " Both the precompressed version and the uncompressed version are expected"] # [doc = " to be present in the same directory. Different precompressed"] # [doc = " variants can be combined."] pub fn precompressed_zstd (self) -> Self { Self (self . 0 . precompressed_zstd ()) } # [doc = " Set a specific read buffer chunk size."] # [doc = ""] # [doc = " The default capacity is 64kb."] pub fn with_buf_chunk_size (self , chunk_size : usize) -> Self { Self (self . 0 . with_buf_chunk_size (chunk_size)) } # [doc = " Call the service and get a future that contains any `std::io::Error` that might have"] # [doc = " happened."] # [doc = ""] # [doc = " See [`ServeDir::try_call`] for more details."] pub fn try_call < ReqBody > (& mut self , req : Request < ReqBody > ,) -> super :: serve_dir :: future :: ResponseFuture < ReqBody > where ReqBody : Send + 'static , { self . 0 . try_call (req) } }
};
}
