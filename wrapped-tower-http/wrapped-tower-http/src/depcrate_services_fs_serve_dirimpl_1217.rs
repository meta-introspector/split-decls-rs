// Generated macro for impl_1217 (impl)
macro_rules! Depcrate_services_fs_serve_dirimpl_1217 {
() => {
// Module: crate::services::fs::serve_dir
// Provides: {"impl_1217"}
// Dependencies: {}
impl < F > ServeDir < F > { # [doc = " If the requested path is a directory append `index.html`."] # [doc = ""] # [doc = " This is useful for static sites."] # [doc = ""] # [doc = " Defaults to `true`."] pub fn append_index_html_on_directories (mut self , append : bool) -> Self { match & mut self . variant { ServeVariant :: Directory { append_index_html_on_directories , } => { * append_index_html_on_directories = append ; self } ServeVariant :: SingleFile { mime : _ } => self , } } # [doc = " Set a specific read buffer chunk size."] # [doc = ""] # [doc = " The default capacity is 64kb."] pub fn with_buf_chunk_size (mut self , chunk_size : usize) -> Self { self . buf_chunk_size = chunk_size ; self } # [doc = " Informs the service that it should also look for a precompressed gzip"] # [doc = " version of _any_ file in the directory."] # [doc = ""] # [doc = " Assuming the `dir` directory is being served and `dir/foo.txt` is requested,"] # [doc = " a client with an `Accept-Encoding` header that allows the gzip encoding"] # [doc = " will receive the file `dir/foo.txt.gz` instead of `dir/foo.txt`."] # [doc = " If the precompressed file is not available, or the client doesn't support it,"] # [doc = " the uncompressed version will be served instead."] # [doc = " Both the precompressed version and the uncompressed version are expected"] # [doc = " to be present in the directory. Different precompressed variants can be combined."] pub fn precompressed_gzip (mut self) -> Self { self . precompressed_variants . get_or_insert (Default :: default ()) . gzip = true ; self } # [doc = " Informs the service that it should also look for a precompressed brotli"] # [doc = " version of _any_ file in the directory."] # [doc = ""] # [doc = " Assuming the `dir` directory is being served and `dir/foo.txt` is requested,"] # [doc = " a client with an `Accept-Encoding` header that allows the brotli encoding"] # [doc = " will receive the file `dir/foo.txt.br` instead of `dir/foo.txt`."] # [doc = " If the precompressed file is not available, or the client doesn't support it,"] # [doc = " the uncompressed version will be served instead."] # [doc = " Both the precompressed version and the uncompressed version are expected"] # [doc = " to be present in the directory. Different precompressed variants can be combined."] pub fn precompressed_br (mut self) -> Self { self . precompressed_variants . get_or_insert (Default :: default ()) . br = true ; self } # [doc = " Informs the service that it should also look for a precompressed deflate"] # [doc = " version of _any_ file in the directory."] # [doc = ""] # [doc = " Assuming the `dir` directory is being served and `dir/foo.txt` is requested,"] # [doc = " a client with an `Accept-Encoding` header that allows the deflate encoding"] # [doc = " will receive the file `dir/foo.txt.zz` instead of `dir/foo.txt`."] # [doc = " If the precompressed file is not available, or the client doesn't support it,"] # [doc = " the uncompressed version will be served instead."] # [doc = " Both the precompressed version and the uncompressed version are expected"] # [doc = " to be present in the directory. Different precompressed variants can be combined."] pub fn precompressed_deflate (mut self) -> Self { self . precompressed_variants . get_or_insert (Default :: default ()) . deflate = true ; self } # [doc = " Informs the service that it should also look for a precompressed zstd"] # [doc = " version of _any_ file in the directory."] # [doc = ""] # [doc = " Assuming the `dir` directory is being served and `dir/foo.txt` is requested,"] # [doc = " a client with an `Accept-Encoding` header that allows the zstd encoding"] # [doc = " will receive the file `dir/foo.txt.zst` instead of `dir/foo.txt`."] # [doc = " If the precompressed file is not available, or the client doesn't support it,"] # [doc = " the uncompressed version will be served instead."] # [doc = " Both the precompressed version and the uncompressed version are expected"] # [doc = " to be present in the directory. Different precompressed variants can be combined."] pub fn precompressed_zstd (mut self) -> Self { self . precompressed_variants . get_or_insert (Default :: default ()) . zstd = true ; self } # [doc = " Set the fallback service."] # [doc = ""] # [doc = " This service will be called if there is no file at the path of the request."] # [doc = ""] # [doc = " The status code returned by the fallback will not be altered. Use"] # [doc = " [`ServeDir::not_found_service`] to set a fallback and always respond with `404 Not Found`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This can be used to respond with a different file:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use tower_http::services::{ServeDir, ServeFile};"] # [doc = ""] # [doc = " let service = ServeDir::new(\"assets\")"] # [doc = "     // respond with `not_found.html` for missing files"] # [doc = "     .fallback(ServeFile::new(\"assets/not_found.html\"));"] # [doc = " ```"] pub fn fallback < F2 > (self , new_fallback : F2) -> ServeDir < F2 > { ServeDir { base : self . base , buf_chunk_size : self . buf_chunk_size , precompressed_variants : self . precompressed_variants , variant : self . variant , fallback : Some (new_fallback) , call_fallback_on_method_not_allowed : self . call_fallback_on_method_not_allowed , } } # [doc = " Set the fallback service and override the fallback's status code to `404 Not Found`."] # [doc = ""] # [doc = " This service will be called if there is no file at the path of the request."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This can be used to respond with a different file:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use tower_http::services::{ServeDir, ServeFile};"] # [doc = ""] # [doc = " let service = ServeDir::new(\"assets\")"] # [doc = "     // respond with `404 Not Found` and the contents of `not_found.html` for missing files"] # [doc = "     .not_found_service(ServeFile::new(\"assets/not_found.html\"));"] # [doc = " ```"] # [doc = ""] # [doc = " Setups like this are often found in single page applications."] pub fn not_found_service < F2 > (self , new_fallback : F2) -> ServeDir < SetStatus < F2 > > { self . fallback (SetStatus :: new (new_fallback , StatusCode :: NOT_FOUND)) } # [doc = " Customize whether or not to call the fallback for requests that aren't `GET` or `HEAD`."] # [doc = ""] # [doc = " Defaults to not calling the fallback and instead returning `405 Method Not Allowed`."] pub fn call_fallback_on_method_not_allowed (mut self , call_fallback : bool) -> Self { self . call_fallback_on_method_not_allowed = call_fallback ; self } # [doc = " Call the service and get a future that contains any `std::io::Error` that might have"] # [doc = " happened."] # [doc = ""] # [doc = " By default `<ServeDir as Service<_>>::call` will handle IO errors and convert them into"] # [doc = " responses. It does that by converting [`std::io::ErrorKind::NotFound`] and"] # [doc = " [`std::io::ErrorKind::PermissionDenied`] to `404 Not Found` and any other error to `500"] # [doc = " Internal Server Error`. The error will also be logged with `tracing`."] # [doc = ""] # [doc = " If you want to manually control how the error response is generated you can make a new"] # [doc = " service that wraps a `ServeDir` and calls `try_call` instead of `call`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use tower_http::services::ServeDir;"] # [doc = " use std::{io, convert::Infallible};"] # [doc = " use http::{Request, Response, StatusCode};"] # [doc = " use http_body::Body as _;"] # [doc = " use http_body_util::{Full, BodyExt, combinators::UnsyncBoxBody};"] # [doc = " use bytes::Bytes;"] # [doc = " use tower::{service_fn, ServiceExt, BoxError};"] # [doc = ""] # [doc = " async fn serve_dir("] # [doc = "     request: Request<Full<Bytes>>"] # [doc = " ) -> Result<Response<UnsyncBoxBody<Bytes, BoxError>>, Infallible> {"] # [doc = "     let mut service = ServeDir::new(\"assets\");"] # [doc = ""] # [doc = "     // You only need to worry about backpressure, and thus call `ServiceExt::ready`, if"] # [doc = "     // your adding a fallback to `ServeDir` that cares about backpressure."] # [doc = "     //"] # [doc = "     // Its shown here for demonstration but you can do `service.try_call(request)`"] # [doc = "     // otherwise"] # [doc = "     let ready_service = match ServiceExt::<Request<Full<Bytes>>>::ready(&mut service).await {"] # [doc = "         Ok(ready_service) => ready_service,"] # [doc = "         Err(infallible) => match infallible {},"] # [doc = "     };"] # [doc = ""] # [doc = "     match ready_service.try_call(request).await {"] # [doc = "         Ok(response) => {"] # [doc = "             Ok(response.map(|body| body.map_err(Into::into).boxed_unsync()))"] # [doc = "         }"] # [doc = "         Err(err) => {"] # [doc = "             let body = Full::from(\"Something went wrong...\")"] # [doc = "                 .map_err(Into::into)"] # [doc = "                 .boxed_unsync();"] # [doc = "             let response = Response::builder()"] # [doc = "                 .status(StatusCode::INTERNAL_SERVER_ERROR)"] # [doc = "                 .body(body)"] # [doc = "                 .unwrap();"] # [doc = "             Ok(response)"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn try_call < ReqBody , FResBody > (& mut self , req : Request < ReqBody > ,) -> ResponseFuture < ReqBody , F > where F : Service < Request < ReqBody > , Response = Response < FResBody > , Error = Infallible > + Clone , F :: Future : Send + 'static , FResBody : http_body :: Body < Data = Bytes > + Send + 'static , FResBody :: Error : Into < Box < dyn std :: error :: Error + Send + Sync > > , { if req . method () != Method :: GET && req . method () != Method :: HEAD { if self . call_fallback_on_method_not_allowed { if let Some (fallback) = & mut self . fallback { return ResponseFuture { inner : future :: call_fallback (fallback , req) , } ; } } else { return ResponseFuture :: method_not_allowed () ; } } let (mut parts , body) = req . into_parts () ; let extensions = std :: mem :: take (& mut parts . extensions) ; let req = Request :: from_parts (parts , Empty :: < Bytes > :: new ()) ; let fallback_and_request = self . fallback . as_mut () . map (| fallback | { let mut fallback_req = Request :: new (body) ; * fallback_req . method_mut () = req . method () . clone () ; * fallback_req . uri_mut () = req . uri () . clone () ; * fallback_req . headers_mut () = req . headers () . clone () ; * fallback_req . extensions_mut () = extensions ; let clone = fallback . clone () ; let fallback = std :: mem :: replace (fallback , clone) ; (fallback , fallback_req) }) ; let path_to_file = match self . variant . build_and_validate_path (& self . base , req . uri () . path ()) { Some (path_to_file) => path_to_file , None => { return ResponseFuture :: invalid_path (fallback_and_request) ; } } ; let buf_chunk_size = self . buf_chunk_size ; let range_header = req . headers () . get (header :: RANGE) . and_then (| value | value . to_str () . ok ()) . map (| s | s . to_owned ()) ; let negotiated_encodings : Vec < _ > = encodings (req . headers () , self . precompressed_variants . unwrap_or_default () ,) . collect () ; let variant = self . variant . clone () ; let open_file_future = Box :: pin (open_file :: open_file (variant , path_to_file , req , negotiated_encodings , range_header , buf_chunk_size ,)) ; ResponseFuture :: open_file_future (open_file_future , fallback_and_request) } }
};
}
