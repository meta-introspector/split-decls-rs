macro_rules! deps {
    () => {
        Pointable!();
        Shared!();
        Atomic!();
        Owned!();
    };
}

macro_rules! impl_55 {
    () => {
        deps!();
        impl < 'g , T : ? Sized + Pointable > Shared < 'g , T > { # [doc = " Returns a new null pointer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::Shared;"] # [doc = ""] # [doc = " let p = Shared::<i32>::null();"] # [doc = " assert!(p.is_null());"] # [doc = " ```"] pub fn null () -> Self { Self { data : ptr :: null_mut () , _marker : PhantomData , } } # [doc = " Returns `true` if the pointer is null."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic, Owned};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::null();"] # [doc = " let guard = &epoch::pin();"] # [doc = " assert!(a.load(SeqCst, guard).is_null());"] # [doc = " a.store(Owned::new(1234), SeqCst);"] # [doc = " assert!(!a.load(SeqCst, guard).is_null());"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn is_null (& self) -> bool { let (raw , _) = decompose_tag :: < T > (self . data) ; raw . is_null () } # [doc = " Dereferences the pointer."] # [doc = ""] # [doc = " Returns a reference to the pointee that is valid during the lifetime `'g`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Dereferencing a pointer is unsafe because it could be pointing to invalid memory."] # [doc = ""] # [doc = " Another concern is the possibility of data races due to lack of proper synchronization."] # [doc = " For example, consider the following scenario:"] # [doc = ""] # [doc = " 1. A thread creates a new object: `a.store(Owned::new(10), Relaxed)`"] # [doc = " 2. Another thread reads it: `*a.load(Relaxed, guard).as_ref().unwrap()`"] # [doc = ""] # [doc = " The problem is that relaxed orderings don't synchronize initialization of the object with"] # [doc = " the read from the second thread. This is a data race. A possible solution would be to use"] # [doc = " `Release` and `Acquire` orderings."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(1234);"] # [doc = " let guard = &epoch::pin();"] # [doc = " let p = a.load(SeqCst, guard);"] # [doc = " unsafe {"] # [doc = "     assert_eq!(p.deref(), &1234);"] # [doc = " }"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub unsafe fn deref (& self) -> & 'g T { let (raw , _) = decompose_tag :: < T > (self . data) ; unsafe { T :: deref (raw) } } # [doc = " Dereferences the pointer."] # [doc = ""] # [doc = " Returns a mutable reference to the pointee that is valid during the lifetime `'g`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " * There is no guarantee that there are no more threads attempting to read/write from/to the"] # [doc = "   actual object at the same time."] # [doc = ""] # [doc = "   The user must know that there are no concurrent accesses towards the object itself."] # [doc = ""] # [doc = " * Other than the above, all safety concerns of `deref()` applies here."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(vec![1, 2, 3, 4]);"] # [doc = " let guard = &epoch::pin();"] # [doc = ""] # [doc = " let mut p = a.load(SeqCst, guard);"] # [doc = " unsafe {"] # [doc = "     assert!(!p.is_null());"] # [doc = "     let b = p.deref_mut();"] # [doc = "     assert_eq!(b, &vec![1, 2, 3, 4]);"] # [doc = "     b.push(5);"] # [doc = "     assert_eq!(b, &vec![1, 2, 3, 4, 5]);"] # [doc = " }"] # [doc = ""] # [doc = " let p = a.load(SeqCst, guard);"] # [doc = " unsafe {"] # [doc = "     assert_eq!(p.deref(), &vec![1, 2, 3, 4, 5]);"] # [doc = " }"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub unsafe fn deref_mut (& mut self) -> & 'g mut T { let (raw , _) = decompose_tag :: < T > (self . data) ; unsafe { T :: deref_mut (raw) } } # [doc = " Converts the pointer to a reference."] # [doc = ""] # [doc = " Returns `None` if the pointer is null, or else a reference to the object wrapped in `Some`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Dereferencing a pointer is unsafe because it could be pointing to invalid memory."] # [doc = ""] # [doc = " Another concern is the possibility of data races due to lack of proper synchronization."] # [doc = " For example, consider the following scenario:"] # [doc = ""] # [doc = " 1. A thread creates a new object: `a.store(Owned::new(10), Relaxed)`"] # [doc = " 2. Another thread reads it: `*a.load(Relaxed, guard).as_ref().unwrap()`"] # [doc = ""] # [doc = " The problem is that relaxed orderings don't synchronize initialization of the object with"] # [doc = " the read from the second thread. This is a data race. A possible solution would be to use"] # [doc = " `Release` and `Acquire` orderings."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(1234);"] # [doc = " let guard = &epoch::pin();"] # [doc = " let p = a.load(SeqCst, guard);"] # [doc = " unsafe {"] # [doc = "     assert_eq!(p.as_ref(), Some(&1234));"] # [doc = " }"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub unsafe fn as_ref (& self) -> Option < & 'g T > { let (raw , _) = decompose_tag :: < T > (self . data) ; if raw . is_null () { None } else { Some (unsafe { T :: deref (raw) }) } } # [doc = " Takes ownership of the pointee."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if this pointer is null, but only in debug mode."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method may be called only if the pointer is valid and nobody else is holding a"] # [doc = " reference to the same object."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(1234);"] # [doc = " unsafe {"] # [doc = "     let guard = &epoch::unprotected();"] # [doc = "     let p = a.load(SeqCst, guard);"] # [doc = "     drop(p.into_owned());"] # [doc = " }"] # [doc = " ```"] pub unsafe fn into_owned (self) -> Owned < T > { debug_assert ! (! self . is_null () , "converting a null `Shared` into `Owned`") ; unsafe { Owned :: from_ptr (self . data) } } # [doc = " Takes ownership of the pointee if it is not null."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method may be called only if the pointer is valid and nobody else is holding a"] # [doc = " reference to the same object, or if the pointer is null."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(1234);"] # [doc = " unsafe {"] # [doc = "     let guard = &epoch::unprotected();"] # [doc = "     let p = a.load(SeqCst, guard);"] # [doc = "     if let Some(x) = p.try_into_owned() {"] # [doc = "         drop(x);"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub unsafe fn try_into_owned (self) -> Option < Owned < T > > { if self . is_null () { None } else { Some (unsafe { Owned :: from_ptr (self . data) }) } } # [doc = " Returns the tag stored within the pointer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic, Owned};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::<u64>::from(Owned::new(0u64).with_tag(2));"] # [doc = " let guard = &epoch::pin();"] # [doc = " let p = a.load(SeqCst, guard);"] # [doc = " assert_eq!(p.tag(), 2);"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn tag (& self) -> usize { let (_ , tag) = decompose_tag :: < T > (self . data) ; tag } # [doc = " Returns the same pointer, but tagged with `tag`. `tag` is truncated to be fit into the"] # [doc = " unused bits of the pointer to `T`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(0u64);"] # [doc = " let guard = &epoch::pin();"] # [doc = " let p1 = a.load(SeqCst, guard);"] # [doc = " let p2 = p1.with_tag(2);"] # [doc = ""] # [doc = " assert_eq!(p1.tag(), 0);"] # [doc = " assert_eq!(p2.tag(), 2);"] # [doc = " assert_eq!(p1.as_raw(), p2.as_raw());"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn with_tag (& self , tag : usize) -> Shared < 'g , T > { unsafe { Self :: from_ptr (compose_tag :: < T > (self . data , tag)) } } }
    };
}

impl_55!();