macro_rules! deps {
    () => {
        CompareExchangeError!();
        Pointable!();
        Guard!();
        Atomic!();
        Shared!();
        Pointer!();
        Owned!();
    };
}

macro_rules! impl_22 {
    () => {
        deps!();
        impl < T : ? Sized + Pointable > Atomic < T > { # [doc = " Allocates `value` on the heap and returns a new atomic pointer pointing to it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::Atomic;"] # [doc = ""] # [doc = " let a = Atomic::<i32>::init(1234);"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn init (init : T :: Init) -> Self { Self :: from (Owned :: init (init)) } # [doc = " Returns a new atomic pointer pointing to the tagged pointer `data`."] fn from_ptr (data : * mut ()) -> Self { Self { data : AtomicPtr :: new (data) , _marker : PhantomData , } } # [doc = " Returns a new null atomic pointer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::Atomic;"] # [doc = ""] # [doc = " let a = Atomic::<i32>::null();"] # [doc = " ```"] # [cfg (not (crossbeam_loom))] pub const fn null () -> Self { Self { data : AtomicPtr :: new (ptr :: null_mut ()) , _marker : PhantomData , } } # [doc = " Returns a new null atomic pointer."] # [cfg (crossbeam_loom)] pub fn null () -> Self { Self { data : AtomicPtr :: new (ptr :: null_mut ()) , _marker : PhantomData , } } # [doc = " Loads a `Shared` from the atomic pointer."] # [doc = ""] # [doc = " This method takes an [`Ordering`] argument which describes the memory ordering of this"] # [doc = " operation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(1234);"] # [doc = " let guard = &epoch::pin();"] # [doc = " let p = a.load(SeqCst, guard);"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn load < 'g > (& self , order : Ordering , _ : & 'g Guard) -> Shared < 'g , T > { unsafe { Shared :: from_ptr (self . data . load (order)) } } # [doc = " Loads a `Shared` from the atomic pointer using a \"consume\" memory ordering."] # [doc = ""] # [doc = " This is similar to the \"acquire\" ordering, except that an ordering is"] # [doc = " only guaranteed with operations that \"depend on\" the result of the load."] # [doc = " However consume loads are usually much faster than acquire loads on"] # [doc = " architectures with a weak memory model since they don't require memory"] # [doc = " fence instructions."] # [doc = ""] # [doc = " The exact definition of \"depend on\" is a bit vague, but it works as you"] # [doc = " would expect in practice since a lot of software, especially the Linux"] # [doc = " kernel, rely on this behavior."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic};"] # [doc = ""] # [doc = " let a = Atomic::new(1234);"] # [doc = " let guard = &epoch::pin();"] # [doc = " let p = a.load_consume(guard);"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn load_consume < 'g > (& self , _ : & 'g Guard) -> Shared < 'g , T > { unsafe { Shared :: from_ptr (self . data . load_consume ()) } } # [doc = " Stores a `Shared` or `Owned` pointer into the atomic pointer."] # [doc = ""] # [doc = " This method takes an [`Ordering`] argument which describes the memory ordering of this"] # [doc = " operation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{Atomic, Owned, Shared};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(1234);"] # [doc = " # unsafe { drop(a.load(SeqCst, &crossbeam_epoch::pin()).into_owned()); } // avoid leak"] # [doc = " a.store(Shared::null(), SeqCst);"] # [doc = " a.store(Owned::new(1234), SeqCst);"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn store < P : Pointer < T > > (& self , new : P , order : Ordering) { self . data . store (new . into_ptr () , order) ; } # [doc = " Stores a `Shared` or `Owned` pointer into the atomic pointer, returning the previous"] # [doc = " `Shared`."] # [doc = ""] # [doc = " This method takes an [`Ordering`] argument which describes the memory ordering of this"] # [doc = " operation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic, Shared};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(1234);"] # [doc = " let guard = &epoch::pin();"] # [doc = " let p = a.swap(Shared::null(), SeqCst, guard);"] # [doc = " # unsafe { drop(p.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn swap < 'g , P : Pointer < T > > (& self , new : P , order : Ordering , _ : & 'g Guard) -> Shared < 'g , T > { unsafe { Shared :: from_ptr (self . data . swap (new . into_ptr () , order)) } } # [doc = " Stores the pointer `new` (either `Shared` or `Owned`) into the atomic pointer if the current"] # [doc = " value is the same as `current`. The tag is also taken into account, so two pointers to the"] # [doc = " same object, but with different tags, will not be considered equal."] # [doc = ""] # [doc = " The return value is a result indicating whether the new pointer was written. On success the"] # [doc = " pointer that was written is returned. On failure the actual current value and `new` are"] # [doc = " returned."] # [doc = ""] # [doc = " This method takes two `Ordering` arguments to describe the memory"] # [doc = " ordering of this operation. `success` describes the required ordering for the"] # [doc = " read-modify-write operation that takes place if the comparison with `current` succeeds."] # [doc = " `failure` describes the required ordering for the load operation that takes place when"] # [doc = " the comparison fails. Using `Acquire` as success ordering makes the store part"] # [doc = " of this operation `Relaxed`, and using `Release` makes the successful load"] # [doc = " `Relaxed`. The failure ordering can only be `SeqCst`, `Acquire` or `Relaxed`"] # [doc = " and must be equivalent to or weaker than the success ordering."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic, Owned, Shared};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(1234);"] # [doc = ""] # [doc = " let guard = &epoch::pin();"] # [doc = " let curr = a.load(SeqCst, guard);"] # [doc = " let res1 = a.compare_exchange(curr, Shared::null(), SeqCst, SeqCst, guard);"] # [doc = " let res2 = a.compare_exchange(curr, Owned::new(5678), SeqCst, SeqCst, guard);"] # [doc = " # unsafe { drop(curr.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn compare_exchange < 'g , P > (& self , current : Shared < '_ , T > , new : P , success : Ordering , failure : Ordering , _ : & 'g Guard ,) -> Result < Shared < 'g , T > , CompareExchangeError < 'g , T , P > > where P : Pointer < T > , { let new = new . into_ptr () ; self . data . compare_exchange (current . into_ptr () , new , success , failure) . map (| _ | unsafe { Shared :: from_ptr (new) }) . map_err (| current | unsafe { CompareExchangeError { current : Shared :: from_ptr (current) , new : P :: from_ptr (new) , } }) } # [doc = " Stores the pointer `new` (either `Shared` or `Owned`) into the atomic pointer if the current"] # [doc = " value is the same as `current`. The tag is also taken into account, so two pointers to the"] # [doc = " same object, but with different tags, will not be considered equal."] # [doc = ""] # [doc = " Unlike [`compare_exchange`], this method is allowed to spuriously fail even when comparison"] # [doc = " succeeds, which can result in more efficient code on some platforms.  The return value is a"] # [doc = " result indicating whether the new pointer was written. On success the pointer that was"] # [doc = " written is returned. On failure the actual current value and `new` are returned."] # [doc = ""] # [doc = " This method takes two `Ordering` arguments to describe the memory"] # [doc = " ordering of this operation. `success` describes the required ordering for the"] # [doc = " read-modify-write operation that takes place if the comparison with `current` succeeds."] # [doc = " `failure` describes the required ordering for the load operation that takes place when"] # [doc = " the comparison fails. Using `Acquire` as success ordering makes the store part"] # [doc = " of this operation `Relaxed`, and using `Release` makes the successful load"] # [doc = " `Relaxed`. The failure ordering can only be `SeqCst`, `Acquire` or `Relaxed`"] # [doc = " and must be equivalent to or weaker than the success ordering."] # [doc = ""] # [doc = " [`compare_exchange`]: Atomic::compare_exchange"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic, Owned, Shared};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(1234);"] # [doc = " let guard = &epoch::pin();"] # [doc = ""] # [doc = " let mut new = Owned::new(5678);"] # [doc = " let mut ptr = a.load(SeqCst, guard);"] # [doc = " # unsafe { drop(a.load(SeqCst, guard).into_owned()); } // avoid leak"] # [doc = " loop {"] # [doc = "     match a.compare_exchange_weak(ptr, new, SeqCst, SeqCst, guard) {"] # [doc = "         Ok(p) => {"] # [doc = "             ptr = p;"] # [doc = "             break;"] # [doc = "         }"] # [doc = "         Err(err) => {"] # [doc = "             ptr = err.current;"] # [doc = "             new = err.new;"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " let mut curr = a.load(SeqCst, guard);"] # [doc = " loop {"] # [doc = "     match a.compare_exchange_weak(curr, Shared::null(), SeqCst, SeqCst, guard) {"] # [doc = "         Ok(_) => break,"] # [doc = "         Err(err) => curr = err.current,"] # [doc = "     }"] # [doc = " }"] # [doc = " # unsafe { drop(curr.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn compare_exchange_weak < 'g , P > (& self , current : Shared < '_ , T > , new : P , success : Ordering , failure : Ordering , _ : & 'g Guard ,) -> Result < Shared < 'g , T > , CompareExchangeError < 'g , T , P > > where P : Pointer < T > , { let new = new . into_ptr () ; self . data . compare_exchange_weak (current . into_ptr () , new , success , failure) . map (| _ | unsafe { Shared :: from_ptr (new) }) . map_err (| current | unsafe { CompareExchangeError { current : Shared :: from_ptr (current) , new : P :: from_ptr (new) , } }) } # [doc = " Fetches the pointer, and then applies a function to it that returns a new value."] # [doc = " Returns a `Result` of `Ok(previous_value)` if the function returned `Some`, else `Err(_)`."] # [doc = ""] # [doc = " Note that the given function may be called multiple times if the value has been changed by"] # [doc = " other threads in the meantime, as long as the function returns `Some(_)`, but the function"] # [doc = " will have been applied only once to the stored value."] # [doc = ""] # [doc = " `fetch_update` takes two [`Ordering`] arguments to describe the memory"] # [doc = " ordering of this operation. The first describes the required ordering for"] # [doc = " when the operation finally succeeds while the second describes the"] # [doc = " required ordering for loads. These correspond to the success and failure"] # [doc = " orderings of [`Atomic::compare_exchange`] respectively."] # [doc = ""] # [doc = " Using [`Acquire`] as success ordering makes the store part of this"] # [doc = " operation [`Relaxed`], and using [`Release`] makes the final successful"] # [doc = " load [`Relaxed`]. The (failed) load ordering can only be [`SeqCst`],"] # [doc = " [`Acquire`] or [`Relaxed`] and must be equivalent to or weaker than the"] # [doc = " success ordering."] # [doc = ""] # [doc = " [`Relaxed`]: Ordering::Relaxed"] # [doc = " [`Acquire`]: Ordering::Acquire"] # [doc = " [`Release`]: Ordering::Release"] # [doc = " [`SeqCst`]: Ordering::SeqCst"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(1234);"] # [doc = " let guard = &epoch::pin();"] # [doc = ""] # [doc = " let res1 = a.fetch_update(SeqCst, SeqCst, guard, |x| Some(x.with_tag(1)));"] # [doc = " assert!(res1.is_ok());"] # [doc = ""] # [doc = " let res2 = a.fetch_update(SeqCst, SeqCst, guard, |x| None);"] # [doc = " assert!(res2.is_err());"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn fetch_update < 'g , F > (& self , set_order : Ordering , fail_order : Ordering , guard : & 'g Guard , mut func : F ,) -> Result < Shared < 'g , T > , Shared < 'g , T > > where F : FnMut (Shared < 'g , T >) -> Option < Shared < 'g , T > > , { let mut prev = self . load (fail_order , guard) ; while let Some (next) = func (prev) { match self . compare_exchange_weak (prev , next , set_order , fail_order , guard) { Ok (shared) => return Ok (shared) , Err (next_prev) => prev = next_prev . current , } } Err (prev) } # [doc = " Bitwise \"and\" with the current tag."] # [doc = ""] # [doc = " Performs a bitwise \"and\" operation on the current tag and the argument `val`, and sets the"] # [doc = " new tag to the result. Returns the previous pointer."] # [doc = ""] # [doc = " This method takes an [`Ordering`] argument which describes the memory ordering of this"] # [doc = " operation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic, Shared};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::<i32>::from(Shared::null().with_tag(3));"] # [doc = " let guard = &epoch::pin();"] # [doc = " assert_eq!(a.fetch_and(2, SeqCst, guard).tag(), 3);"] # [doc = " assert_eq!(a.load(SeqCst, guard).tag(), 2);"] # [doc = " ```"] pub fn fetch_and < 'g > (& self , val : usize , order : Ordering , _ : & 'g Guard) -> Shared < 'g , T > { # [cfg (miri)] unsafe { let val = val | ! low_bits :: < T > () ; let fetch_order = strongest_failure_ordering (order) ; Shared :: from_ptr (self . data . fetch_update (order , fetch_order , | x | Some (map_addr (x , | a | a & val))) . unwrap () ,) } # [cfg (not (miri))] unsafe { Shared :: from_ptr ((* (& self . data as * const AtomicPtr < _ > as * const AtomicUsize)) . fetch_and (val | ! low_bits :: < T > () , order) as * mut () ,) } } # [doc = " Bitwise \"or\" with the current tag."] # [doc = ""] # [doc = " Performs a bitwise \"or\" operation on the current tag and the argument `val`, and sets the"] # [doc = " new tag to the result. Returns the previous pointer."] # [doc = ""] # [doc = " This method takes an [`Ordering`] argument which describes the memory ordering of this"] # [doc = " operation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic, Shared};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::<i32>::from(Shared::null().with_tag(1));"] # [doc = " let guard = &epoch::pin();"] # [doc = " assert_eq!(a.fetch_or(2, SeqCst, guard).tag(), 1);"] # [doc = " assert_eq!(a.load(SeqCst, guard).tag(), 3);"] # [doc = " ```"] pub fn fetch_or < 'g > (& self , val : usize , order : Ordering , _ : & 'g Guard) -> Shared < 'g , T > { # [cfg (miri)] unsafe { let val = val & low_bits :: < T > () ; let fetch_order = strongest_failure_ordering (order) ; Shared :: from_ptr (self . data . fetch_update (order , fetch_order , | x | Some (map_addr (x , | a | a | val))) . unwrap () ,) } # [cfg (not (miri))] unsafe { Shared :: from_ptr ((* (& self . data as * const AtomicPtr < _ > as * const AtomicUsize)) . fetch_or (val & low_bits :: < T > () , order) as * mut () ,) } } # [doc = " Bitwise \"xor\" with the current tag."] # [doc = ""] # [doc = " Performs a bitwise \"xor\" operation on the current tag and the argument `val`, and sets the"] # [doc = " new tag to the result. Returns the previous pointer."] # [doc = ""] # [doc = " This method takes an [`Ordering`] argument which describes the memory ordering of this"] # [doc = " operation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic, Shared};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::<i32>::from(Shared::null().with_tag(1));"] # [doc = " let guard = &epoch::pin();"] # [doc = " assert_eq!(a.fetch_xor(3, SeqCst, guard).tag(), 1);"] # [doc = " assert_eq!(a.load(SeqCst, guard).tag(), 2);"] # [doc = " ```"] pub fn fetch_xor < 'g > (& self , val : usize , order : Ordering , _ : & 'g Guard) -> Shared < 'g , T > { # [cfg (miri)] unsafe { let val = val & low_bits :: < T > () ; let fetch_order = strongest_failure_ordering (order) ; Shared :: from_ptr (self . data . fetch_update (order , fetch_order , | x | Some (map_addr (x , | a | a ^ val))) . unwrap () ,) } # [cfg (not (miri))] unsafe { Shared :: from_ptr ((* (& self . data as * const AtomicPtr < _ > as * const AtomicUsize)) . fetch_xor (val & low_bits :: < T > () , order) as * mut () ,) } } # [doc = " Takes ownership of the pointee."] # [doc = ""] # [doc = " This consumes the atomic and converts it into [`Owned`]. As [`Atomic`] doesn't have a"] # [doc = " destructor and doesn't drop the pointee while [`Owned`] does, this is suitable for"] # [doc = " destructors of data structures."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if this pointer is null, but only in debug mode."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method may be called only if the pointer is valid and nobody else is holding a"] # [doc = " reference to the same object."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use std::mem;"] # [doc = " # use crossbeam_epoch::Atomic;"] # [doc = " struct DataStructure {"] # [doc = "     ptr: Atomic<usize>,"] # [doc = " }"] # [doc = ""] # [doc = " impl Drop for DataStructure {"] # [doc = "     fn drop(&mut self) {"] # [doc = "         // By now the DataStructure lives only in our thread and we are sure we don't hold"] # [doc = "         // any Shared or & to it ourselves."] # [doc = "         unsafe {"] # [doc = "             drop(mem::replace(&mut self.ptr, Atomic::null()).into_owned());"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub unsafe fn into_owned (self) -> Owned < T > { unsafe { Owned :: from_ptr (self . data . into_inner ()) } } # [doc = " Takes ownership of the pointee if it is non-null."] # [doc = ""] # [doc = " This consumes the atomic and converts it into [`Owned`]. As [`Atomic`] doesn't have a"] # [doc = " destructor and doesn't drop the pointee while [`Owned`] does, this is suitable for"] # [doc = " destructors of data structures."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method may be called only if the pointer is valid and nobody else is holding a"] # [doc = " reference to the same object, or the pointer is null."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use std::mem;"] # [doc = " # use crossbeam_epoch::Atomic;"] # [doc = " struct DataStructure {"] # [doc = "     ptr: Atomic<usize>,"] # [doc = " }"] # [doc = ""] # [doc = " impl Drop for DataStructure {"] # [doc = "     fn drop(&mut self) {"] # [doc = "         // By now the DataStructure lives only in our thread and we are sure we don't hold"] # [doc = "         // any Shared or & to it ourselves, but it may be null, so we have to be careful."] # [doc = "         let old = mem::replace(&mut self.ptr, Atomic::null());"] # [doc = "         unsafe {"] # [doc = "             if let Some(x) = old.try_into_owned() {"] # [doc = "                 drop(x)"] # [doc = "             }"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub unsafe fn try_into_owned (self) -> Option < Owned < T > > { let data = self . data . into_inner () ; if decompose_tag :: < T > (data) . 0 . is_null () { None } else { Some (unsafe { Owned :: from_ptr (data) }) } } }
    };
}

impl_22!();