// Generated macro for impl_157 (impl)
macro_rules! Depcrate_internalimpl_157 {
() => {
// Module: crate::internal
// Provides: {"impl_157"}
// Dependencies: {}
impl Global { # [doc = " Number of bags to destroy."] const COLLECT_STEPS : usize = 8 ; # [doc = " Creates a new global data for garbage collection."] # [inline] pub (crate) fn new () -> Self { Self { locals : List :: new () , queue : Queue :: new () , epoch : CachePadded :: new (AtomicEpoch :: new (Epoch :: starting ())) , } } # [doc = " Pushes the bag into the global queue and replaces the bag with a new empty bag."] pub (crate) fn push_bag (& self , bag : & mut Bag , guard : & Guard) { let bag = mem :: replace (bag , Bag :: new ()) ; atomic :: fence (Ordering :: SeqCst) ; let epoch = self . epoch . load (Ordering :: Relaxed) ; self . queue . push (bag . seal (epoch) , guard) ; } # [doc = " Collects several bags from the global queue and executes deferred functions in them."] # [doc = ""] # [doc = " Note: This may itself produce garbage and in turn allocate new bags."] # [doc = ""] # [doc = " `pin()` rarely calls `collect()`, so we want the compiler to place that call on a cold"] # [doc = " path. In other words, we want the compiler to optimize branching for the case when"] # [doc = " `collect()` is not called."] # [cold] pub (crate) fn collect (& self , guard : & Guard) { let global_epoch = self . try_advance (guard) ; let steps = if cfg ! (crossbeam_sanitize) { usize :: MAX } else { Self :: COLLECT_STEPS } ; for _ in 0 .. steps { match self . queue . try_pop_if (| sealed_bag : & SealedBag | sealed_bag . is_expired (global_epoch) , guard ,) { None => break , Some (sealed_bag) => drop (sealed_bag) , } } } # [doc = " Attempts to advance the global epoch."] # [doc = ""] # [doc = " The global epoch can advance only if all currently pinned participants have been pinned in"] # [doc = " the current epoch."] # [doc = ""] # [doc = " Returns the current global epoch."] # [doc = ""] # [doc = " `try_advance()` is annotated `#[cold]` because it is rarely called."] # [cold] pub (crate) fn try_advance (& self , guard : & Guard) -> Epoch { let global_epoch = self . epoch . load (Ordering :: Relaxed) ; atomic :: fence (Ordering :: SeqCst) ; for local in self . locals . iter (guard) { match local { Err (IterError :: Stalled) => { return global_epoch ; } Ok (local) => { let local_epoch = local . epoch . load (Ordering :: Relaxed) ; if local_epoch . is_pinned () && local_epoch . unpinned () != global_epoch { return global_epoch ; } } } } atomic :: fence (Ordering :: Acquire) ; let new_epoch = global_epoch . successor () ; self . epoch . store (new_epoch , Ordering :: Release) ; new_epoch } }
};
}
