// Generated macro for Pointable (trait)
macro_rules! Depcrate_atomicPointable {
() => {
// Module: crate::atomic
// Provides: {"Pointable"}
// Dependencies: {}
# [doc = " Types that are pointed to by a single word."] # [doc = ""] # [doc = " In concurrent programming, it is necessary to represent an object within a word because atomic"] # [doc = " operations (e.g., reads, writes, read-modify-writes) support only single words.  This trait"] # [doc = " qualifies such types that are pointed to by a single word."] # [doc = ""] # [doc = " The trait generalizes `Box<T>` for a sized type `T`.  In a box, an object of type `T` is"] # [doc = " allocated in heap and it is owned by a single-word pointer.  This trait is also implemented for"] # [doc = " `[MaybeUninit<T>]` by storing its size along with its elements and pointing to the pair of array"] # [doc = " size and elements."] # [doc = ""] # [doc = " Pointers to `Pointable` types can be stored in [`Atomic`], [`Owned`], and [`Shared`].  In"] # [doc = " particular, Crossbeam supports dynamically sized slices as follows."] # [doc = ""] # [doc = " ```"] # [doc = " use std::mem::MaybeUninit;"] # [doc = " use crossbeam_epoch::Owned;"] # [doc = ""] # [doc = " let o = Owned::<[MaybeUninit<i32>]>::init(10); // allocating [i32; 10]"] # [doc = " ```"] pub trait Pointable { # [doc = " The alignment of pointer."] const ALIGN : usize ; # [doc = " The type for initializers."] type Init ; # [doc = " Initializes a with the given initializer."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The result should be a multiple of `ALIGN`."] unsafe fn init (init : Self :: Init) -> * mut () ; # [doc = " Dereferences the given pointer."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " - The given `ptr` should have been initialized with [`Pointable::init`]."] # [doc = " - `ptr` should not have yet been dropped by [`Pointable::drop`]."] # [doc = " - `ptr` should not be mutably dereferenced by [`Pointable::deref_mut`] concurrently."] unsafe fn deref < 'a > (ptr : * mut ()) -> & 'a Self ; # [doc = " Mutably dereferences the given pointer."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " - The given `ptr` should have been initialized with [`Pointable::init`]."] # [doc = " - `ptr` should not have yet been dropped by [`Pointable::drop`]."] # [doc = " - `ptr` should not be dereferenced by [`Pointable::deref`] or [`Pointable::deref_mut`]"] # [doc = "   concurrently."] unsafe fn deref_mut < 'a > (ptr : * mut ()) -> & 'a mut Self ; # [doc = " Drops the object pointed to by the given pointer."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " - The given `ptr` should have been initialized with [`Pointable::init`]."] # [doc = " - `ptr` should not have yet been dropped by [`Pointable::drop`]."] # [doc = " - `ptr` should not be dereferenced by [`Pointable::deref`] or [`Pointable::deref_mut`]"] # [doc = "   concurrently."] unsafe fn drop (ptr : * mut ()) ; }
};
}
