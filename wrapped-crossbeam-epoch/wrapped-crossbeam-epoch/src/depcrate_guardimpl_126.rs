// Generated macro for impl_126 (impl)
macro_rules! Depcrate_guardimpl_126 {
() => {
// Module: crate::guard
// Provides: {"impl_126"}
// Dependencies: {}
impl Guard { # [doc = " Stores a function so that it can be executed at some point after all currently pinned"] # [doc = " threads get unpinned."] # [doc = ""] # [doc = " This method first stores `f` into the thread-local (or handle-local) cache. If this cache"] # [doc = " becomes full, some functions are moved into the global cache. At the same time, some"] # [doc = " functions from both local and global caches may get executed in order to incrementally"] # [doc = " clean up the caches as they fill up."] # [doc = ""] # [doc = " There is no guarantee when exactly `f` will be executed. The only guarantee is that it"] # [doc = " won't be executed until all currently pinned threads get unpinned. In theory, `f` might"] # [doc = " never run, but the epoch-based garbage collection will make an effort to execute it"] # [doc = " reasonably soon."] # [doc = ""] # [doc = " If this method is called from an [`unprotected`] guard, the function will simply be"] # [doc = " executed immediately."] pub fn defer < F , R > (& self , f : F) where F : FnOnce () -> R , F : Send + 'static , { unsafe { self . defer_unchecked (f) ; } } # [doc = " Stores a function so that it can be executed at some point after all currently pinned"] # [doc = " threads get unpinned."] # [doc = ""] # [doc = " This method first stores `f` into the thread-local (or handle-local) cache. If this cache"] # [doc = " becomes full, some functions are moved into the global cache. At the same time, some"] # [doc = " functions from both local and global caches may get executed in order to incrementally"] # [doc = " clean up the caches as they fill up."] # [doc = ""] # [doc = " There is no guarantee when exactly `f` will be executed. The only guarantee is that it"] # [doc = " won't be executed until all currently pinned threads get unpinned. In theory, `f` might"] # [doc = " never run, but the epoch-based garbage collection will make an effort to execute it"] # [doc = " reasonably soon."] # [doc = ""] # [doc = " If this method is called from an [`unprotected`] guard, the function will simply be"] # [doc = " executed immediately."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The given function must not hold reference onto the stack. It is highly recommended that"] # [doc = " the passed function is **always** marked with `move` in order to prevent accidental"] # [doc = " borrows."] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch as epoch;"] # [doc = ""] # [doc = " let guard = &epoch::pin();"] # [doc = " let message = \"Hello!\";"] # [doc = " unsafe {"] # [doc = "     // ALWAYS use `move` when sending a closure into `defer_unchecked`."] # [doc = "     guard.defer_unchecked(move || {"] # [doc = "         println!(\"{}\", message);"] # [doc = "     });"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Apart from that, keep in mind that another thread may execute `f`, so anything accessed by"] # [doc = " the closure must be `Send`."] # [doc = ""] # [doc = " We intentionally didn't require `F: Send`, because Rust's type systems usually cannot prove"] # [doc = " `F: Send` for typical use cases. For example, consider the following code snippet, which"] # [doc = " exemplifies the typical use case of deferring the deallocation of a shared reference:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " let shared = Owned::new(7i32).into_shared(guard);"] # [doc = " guard.defer_unchecked(move || shared.into_owned()); // `Shared` is not `Send`!"] # [doc = " ```"] # [doc = ""] # [doc = " While `Shared` is not `Send`, it's safe for another thread to call the deferred function,"] # [doc = " because it's called only after the grace period and `shared` is no longer shared with other"] # [doc = " threads. But we don't expect type systems to prove this."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " When a heap-allocated object in a data structure becomes unreachable, it has to be"] # [doc = " deallocated. However, the current thread and other threads may be still holding references"] # [doc = " on the stack to that same object. Therefore it cannot be deallocated before those references"] # [doc = " get dropped. This method can defer deallocation until all those threads get unpinned and"] # [doc = " consequently drop all their references on the stack."] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic, Owned};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(\"foo\");"] # [doc = ""] # [doc = " // Now suppose that `a` is shared among multiple threads and concurrently"] # [doc = " // accessed and modified..."] # [doc = ""] # [doc = " // Pin the current thread."] # [doc = " let guard = &epoch::pin();"] # [doc = ""] # [doc = " // Steal the object currently stored in `a` and swap it with another one."] # [doc = " let p = a.swap(Owned::new(\"bar\").into_shared(guard), SeqCst, guard);"] # [doc = ""] # [doc = " if !p.is_null() {"] # [doc = "     // The object `p` is pointing to is now unreachable."] # [doc = "     // Defer its deallocation until all currently pinned threads get unpinned."] # [doc = "     unsafe {"] # [doc = "         // ALWAYS use `move` when sending a closure into `defer_unchecked`."] # [doc = "         guard.defer_unchecked(move || {"] # [doc = "             println!(\"{} is now being deallocated.\", p.deref());"] # [doc = "             // Now we have unique access to the object pointed to by `p` and can turn it"] # [doc = "             // into an `Owned`. Dropping the `Owned` will deallocate the object."] # [doc = "             drop(p.into_owned());"] # [doc = "         });"] # [doc = "     }"] # [doc = " }"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub unsafe fn defer_unchecked < F , R > (& self , f : F) where F : FnOnce () -> R , { unsafe { if let Some (local) = self . local . as_ref () { local . defer (Deferred :: new (move | | drop (f ())) , self) ; } else { drop (f ()) ; } } } # [doc = " Stores a destructor for an object so that it can be deallocated and dropped at some point"] # [doc = " after all currently pinned threads get unpinned."] # [doc = ""] # [doc = " This method first stores the destructor into the thread-local (or handle-local) cache. If"] # [doc = " this cache becomes full, some destructors are moved into the global cache. At the same"] # [doc = " time, some destructors from both local and global caches may get executed in order to"] # [doc = " incrementally clean up the caches as they fill up."] # [doc = ""] # [doc = " There is no guarantee when exactly the destructor will be executed. The only guarantee is"] # [doc = " that it won't be executed until all currently pinned threads get unpinned. In theory, the"] # [doc = " destructor might never run, but the epoch-based garbage collection will make an effort to"] # [doc = " execute it reasonably soon."] # [doc = ""] # [doc = " If this method is called from an [`unprotected`] guard, the destructor will simply be"] # [doc = " executed immediately."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The object must not be reachable by other threads anymore, otherwise it might be still in"] # [doc = " use when the destructor runs."] # [doc = ""] # [doc = " Apart from that, keep in mind that another thread may execute the destructor, so the object"] # [doc = " must be sendable to other threads."] # [doc = ""] # [doc = " We intentionally didn't require `T: Send`, because Rust's type systems usually cannot prove"] # [doc = " `T: Send` for typical use cases. For example, consider the following code snippet, which"] # [doc = " exemplifies the typical use case of deferring the deallocation of a shared reference:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " let shared = Owned::new(7i32).into_shared(guard);"] # [doc = " guard.defer_destroy(shared); // `Shared` is not `Send`!"] # [doc = " ```"] # [doc = ""] # [doc = " While `Shared` is not `Send`, it's safe for another thread to call the destructor, because"] # [doc = " it's called only after the grace period and `shared` is no longer shared with other"] # [doc = " threads. But we don't expect type systems to prove this."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " When a heap-allocated object in a data structure becomes unreachable, it has to be"] # [doc = " deallocated. However, the current thread and other threads may be still holding references"] # [doc = " on the stack to that same object. Therefore it cannot be deallocated before those references"] # [doc = " get dropped. This method can defer deallocation until all those threads get unpinned and"] # [doc = " consequently drop all their references on the stack."] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic, Owned};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(\"foo\");"] # [doc = ""] # [doc = " // Now suppose that `a` is shared among multiple threads and concurrently"] # [doc = " // accessed and modified..."] # [doc = ""] # [doc = " // Pin the current thread."] # [doc = " let guard = &epoch::pin();"] # [doc = ""] # [doc = " // Steal the object currently stored in `a` and swap it with another one."] # [doc = " let p = a.swap(Owned::new(\"bar\").into_shared(guard), SeqCst, guard);"] # [doc = ""] # [doc = " if !p.is_null() {"] # [doc = "     // The object `p` is pointing to is now unreachable."] # [doc = "     // Defer its deallocation until all currently pinned threads get unpinned."] # [doc = "     unsafe {"] # [doc = "         guard.defer_destroy(p);"] # [doc = "     }"] # [doc = " }"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub unsafe fn defer_destroy < T > (& self , ptr : Shared < '_ , T >) { unsafe { self . defer_unchecked (move | | ptr . into_owned ()) } } # [doc = " Clears up the thread-local cache of deferred functions by executing them or moving into the"] # [doc = " global cache."] # [doc = ""] # [doc = " Call this method after deferring execution of a function if you want to get it executed as"] # [doc = " soon as possible. Flushing will make sure it is residing in the global cache, so that"] # [doc = " any thread has a chance of taking the function and executing it."] # [doc = ""] # [doc = " If this method is called from an [`unprotected`] guard, it is a no-op (nothing happens)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch as epoch;"] # [doc = ""] # [doc = " let guard = &epoch::pin();"] # [doc = " guard.defer(move || {"] # [doc = "     println!(\"This better be printed as soon as possible!\");"] # [doc = " });"] # [doc = " guard.flush();"] # [doc = " ```"] pub fn flush (& self) { if let Some (local) = unsafe { self . local . as_ref () } { local . flush (self) ; } } # [doc = " Unpins and then immediately re-pins the thread."] # [doc = ""] # [doc = " This method is useful when you don't want delay the advancement of the global epoch by"] # [doc = " holding an old epoch. For safety, you should not maintain any guard-based reference across"] # [doc = " the call (the latter is enforced by `&mut self`). The thread will only be repinned if this"] # [doc = " is the only active guard for the current thread."] # [doc = ""] # [doc = " If this method is called from an [`unprotected`] guard, then the call will be just no-op."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " let a = Atomic::new(777);"] # [doc = " let mut guard = epoch::pin();"] # [doc = " {"] # [doc = "     let p = a.load(SeqCst, &guard);"] # [doc = "     assert_eq!(unsafe { p.as_ref() }, Some(&777));"] # [doc = " }"] # [doc = " guard.repin();"] # [doc = " {"] # [doc = "     let p = a.load(SeqCst, &guard);"] # [doc = "     assert_eq!(unsafe { p.as_ref() }, Some(&777));"] # [doc = " }"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn repin (& mut self) { if let Some (local) = unsafe { self . local . as_ref () } { local . repin () ; } } # [doc = " Temporarily unpins the thread, executes the given function and then re-pins the thread."] # [doc = ""] # [doc = " This method is useful when you need to perform a long-running operation (e.g. sleeping)"] # [doc = " and don't need to maintain any guard-based reference across the call (the latter is enforced"] # [doc = " by `&mut self`). The thread will only be unpinned if this is the only active guard for the"] # [doc = " current thread."] # [doc = ""] # [doc = " If this method is called from an [`unprotected`] guard, then the passed function is called"] # [doc = " directly without unpinning the thread."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch::{self as epoch, Atomic};"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " let a = Atomic::new(777);"] # [doc = " let mut guard = epoch::pin();"] # [doc = " {"] # [doc = "     let p = a.load(SeqCst, &guard);"] # [doc = "     assert_eq!(unsafe { p.as_ref() }, Some(&777));"] # [doc = " }"] # [doc = " guard.repin_after(|| thread::sleep(Duration::from_millis(50)));"] # [doc = " {"] # [doc = "     let p = a.load(SeqCst, &guard);"] # [doc = "     assert_eq!(unsafe { p.as_ref() }, Some(&777));"] # [doc = " }"] # [doc = " # unsafe { drop(a.into_owned()); } // avoid leak"] # [doc = " ```"] pub fn repin_after < F , R > (& mut self , f : F) -> R where F : FnOnce () -> R , { struct ScopeGuard (* const Local) ; impl Drop for ScopeGuard { fn drop (& mut self) { if let Some (local) = unsafe { self . 0 . as_ref () } { mem :: forget (local . pin ()) ; local . release_handle () ; } } } if let Some (local) = unsafe { self . local . as_ref () } { local . acquire_handle () ; local . unpin () ; } let _guard = ScopeGuard (self . local) ; f () } # [doc = " Returns the `Collector` associated with this guard."] # [doc = ""] # [doc = " This method is useful when you need to ensure that all guards used with"] # [doc = " a data structure come from the same collector."] # [doc = ""] # [doc = " If this method is called from an [`unprotected`] guard, then `None` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_epoch as epoch;"] # [doc = ""] # [doc = " let guard1 = epoch::pin();"] # [doc = " let guard2 = epoch::pin();"] # [doc = " assert!(guard1.collector() == guard2.collector());"] # [doc = " ```"] pub fn collector (& self) -> Option < & Collector > { unsafe { self . local . as_ref () . map (| local | local . collector ()) } } }
};
}
