// Generated macro for impl_53 (impl)
macro_rules! Depcrate_dns_recordimpl_53 {
() => {
// Module: crate::dns_record
// Provides: {"impl_53"}
// Dependencies: {}
impl DnsRecord { # [doc = " # Errors"] # [doc = " Returns an error when `buf` does not contain a valid resource record."] pub fn read_rdata < const N : usize > (buf : & mut FixedBuf < N >) -> Result < FixedBuf < 65535 > , DnsError > { let len = read_u16_be (buf) ? ; if buf . len () < (len as usize) { return Err (DnsError :: Truncated) ; } let borrowed_rdata = buf . read_bytes (len as usize) ; let mut rdata : FixedBuf < 65535 > = FixedBuf :: new () ; rdata . write_bytes (borrowed_rdata) . map_err (| _ | DnsError :: Unreachable (file ! () , line ! ())) ? ; Ok (rdata) } # [doc = " # Errors"] # [doc = " Returns an error when `buf` is full or `bytes` is longer than 65,535 bytes."] pub fn write_rdata < const N : usize > (bytes : & [u8] , out : & mut FixedBuf < N > ,) -> Result < () , DnsError > { let len = u16 :: try_from (bytes . len ()) . map_err (| _ | DnsError :: Unreachable (file ! () , line ! ())) ? ; write_u16_be (out , len) ? ; write_bytes (out , bytes) ? ; Ok (()) } # [doc = " # Errors"] # [doc = " Returns an error when `name` is not a valid DNS name"] # [doc = " or `ipv4_addr` is not a valid IPv4 address."] pub fn new_a (name : & str , ipv4_addr : & str) -> Result < Self , String > { let dns_name = DnsName :: new (name) ? ; let ip_addr : IpAddr = ipv4_addr . parse () . map_err (| e | format ! ("failed parsing {ipv4_addr:?} as an IP address: {e}")) ? ; match ip_addr { IpAddr :: V4 (addr) => Ok (Self :: A (dns_name , addr)) , IpAddr :: V6 (addr) => Err (format ! ("cannot create an A record with ipv6 address {addr:?}")) , } } # [doc = " # Errors"] # [doc = " Returns an error when `name` is not a valid DNS name"] # [doc = " or `ipv6_addr` is not a valid IPv6 address."] pub fn new_aaaa (name : & str , ipv6_addr : & str) -> Result < Self , String > { let dns_name = DnsName :: new (name) ? ; let ip_addr : IpAddr = ipv6_addr . parse () . map_err (| e | format ! ("failed parsing {ipv6_addr:?} as an IP address: {e}")) ? ; match ip_addr { IpAddr :: V4 (addr) => Err (format ! ("cannot create an AAAA record with ipv4 address {addr:?}")) , IpAddr :: V6 (addr) => Ok (Self :: AAAA (dns_name , addr)) , } } # [doc = " # Errors"] # [doc = " Returns an error when `name` or `target` are not both valid DNS names."] pub fn new_cname (name : & str , target : & str) -> Result < Self , String > { let dns_name = DnsName :: new (name) ? ; let dns_name_target = DnsName :: new (target) ? ; Ok (Self :: CNAME (dns_name , dns_name_target)) } # [doc = " # Errors"] # [doc = " Returns an error when `name` or `target` are not both valid DNS names."] pub fn new_ns (name : & str , target : & str) -> Result < Self , String > { let dns_name = DnsName :: new (name) ? ; let dns_name_target = DnsName :: new (target) ? ; Ok (Self :: NS (dns_name , dns_name_target)) } # [doc = " # Errors"] # [doc = " Returns an error when `name` or `target` are not both valid DNS names."] pub fn new_txt (name : & str , content : & str) -> Result < Self , String > { let dns_name = DnsName :: new (name) ? ; let dns_string = DnsString :: new (content) ? ; Ok (Self :: TXT (dns_name , vec ! [dns_string])) } # [doc = " # Errors"] # [doc = " Returns an error when `name` or `target` are not both valid DNS names."] pub fn new_txt_multi (name : & str , lines : & [& str]) -> Result < Self , String > { let dns_name = DnsName :: new (name) ? ; let mut dns_strings = Vec :: new () ; for line in lines { dns_strings . push (DnsString :: new (line) ?) ; } Ok (Self :: TXT (dns_name , dns_strings)) } # [must_use] pub fn name (& self) -> & DnsName { match self { DnsRecord :: A (dns_name , _) | DnsRecord :: AAAA (dns_name , _) | DnsRecord :: CNAME (dns_name , _) | DnsRecord :: NS (dns_name , _) | DnsRecord :: TXT (dns_name , _) | DnsRecord :: Unknown (dns_name , _) => dns_name , } } # [must_use] pub fn typ (& self) -> DnsType { match self { DnsRecord :: A (..) => DnsType :: A , DnsRecord :: AAAA (..) => DnsType :: AAAA , DnsRecord :: CNAME (..) => DnsType :: CNAME , DnsRecord :: NS (..) => DnsType :: NS , DnsRecord :: TXT (..) => DnsType :: TXT , DnsRecord :: Unknown (_ , typ) => DnsType :: Unknown (typ . num ()) , } } # [doc = " # Errors"] # [doc = " Returns an error when `buf` does not contain a valid resource record."] pub fn read < const N : usize > (buf : & mut FixedBuf < N >) -> Result < Self , DnsError > { let name = DnsName :: read (buf) ? ; let typ = DnsType :: read (buf) ? ; let class = DnsClass :: read (buf) ? ; if class != DnsClass :: Internet && class != DnsClass :: Any { return Err (DnsError :: InvalidClass) ; } let _ttl_seconds = read_u32_be (buf) ? ; let mut rdata = Self :: read_rdata (buf) ? ; let record = match typ { DnsType :: A => { let octets : [u8 ; 4] = read_exact (& mut rdata) ? ; check_empty (& rdata) ? ; DnsRecord :: A (name , Ipv4Addr :: from (octets)) } DnsType :: AAAA => { let octets : [u8 ; 16] = read_exact (& mut rdata) ? ; check_empty (& rdata) ? ; DnsRecord :: AAAA (name , Ipv6Addr :: from (octets)) } DnsType :: CNAME => { let target = DnsName :: read (& mut rdata) ? ; check_empty (& rdata) ? ; DnsRecord :: CNAME (name , target) } DnsType :: NS => { let target = DnsName :: read (& mut rdata) ? ; check_empty (& rdata) ? ; DnsRecord :: NS (name , target) } DnsType :: TXT => { let strings = DnsString :: read_multiple (& mut rdata) ? ; DnsRecord :: TXT (name , strings) } DnsType :: MX | DnsType :: PTR | DnsType :: SOA | DnsType :: ANY | DnsType :: Unknown (_) => { DnsRecord :: Unknown (name , typ) } } ; Ok (record) } # [doc = " # Errors"] # [doc = " Returns an error when `buf` is full."] pub fn write < const N : usize > (& self , out : & mut FixedBuf < N >) -> Result < () , DnsError > { self . name () . write (out) ? ; self . typ () . write (out) ? ; DnsClass :: Internet . write (out) ? ; write_u32_be (out , 300) ? ; # [allow (clippy :: match_same_arms)] match self { DnsRecord :: A (_ , ipv4_addr) => Self :: write_rdata (& ipv4_addr . octets () , out) ? , DnsRecord :: AAAA (_ , ipv6_addr) => Self :: write_rdata (& ipv6_addr . octets () , out) ? , DnsRecord :: CNAME (_ , target_name) => { Self :: write_rdata (target_name . as_bytes () ? . readable () , out) ? ; } DnsRecord :: NS (_ , target_name) => { Self :: write_rdata (target_name . as_bytes () ? . readable () , out) ? ; } DnsRecord :: TXT (_ , strings) => { let mut buf = Vec :: new () ; for string in strings { let bytes = string . as_bytes () ? ; buf . extend (bytes . readable ()) ; } Self :: write_rdata (& buf , out) ? ; } DnsRecord :: Unknown (_ , _) => { return Err (DnsError :: Internal (format ! ("cannot write record {self:?}"))) } } Ok (()) } }
};
}
