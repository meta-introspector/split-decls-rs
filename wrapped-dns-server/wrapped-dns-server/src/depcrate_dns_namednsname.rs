// Generated macro for DnsName (struct)
macro_rules! Depcrate_dns_nameDnsName {
() => {
// Module: crate::dns_name
// Provides: {"DnsName"}
// Dependencies: {}
# [doc = " > 2.3.1. Preferred name syntax"] # [doc = " >"] # [doc = " > The DNS specifications attempt to be as general as possible in the rules for constructing"] # [doc = " > domain names.  The idea is that the name of any existing object can be expressed as a domain"] # [doc = " > name with minimal changes."] # [doc = " >"] # [doc = " > However, when assigning a domain name for an object, the prudent user will select a name"] # [doc = " > which satisfies both the rules of the domain system and any existing rules for the object,"] # [doc = " > whether these rules are published or implied by existing programs."] # [doc = " >"] # [doc = " > For example, when naming a mail domain, the user should satisfy both the rules of this memo"] # [doc = " > and those in [RFC-822](https://datatracker.ietf.org/doc/html/rfc822).  When creating a new"] # [doc = " > host name, the old rules for HOSTS.TXT should be followed.  This avoids problems when old"] # [doc = " > software is converted to use domain names."] # [doc = " >"] # [doc = " > The following syntax will result in fewer problems with many"] # [doc = " >"] # [doc = " > applications that use domain names (e.g., mail, TELNET)."] # [doc = " >"] # [doc = " > `<domain> ::= <subdomain> | \" \"`"] # [doc = " >"] # [doc = " > `<subdomain> ::= <label> | <subdomain> \".\" <label>`"] # [doc = " >"] # [doc = " > `<label> ::= <letter> [ [ <ldh-str> ] <let-dig> ]`"] # [doc = " >"] # [doc = " > `<ldh-str> ::= <let-dig-hyp> | <let-dig-hyp> <ldh-str>`"] # [doc = " >"] # [doc = " > `<let-dig-hyp> ::= <let-dig> | \"-\"`"] # [doc = " >"] # [doc = " > `<let-dig> ::= <letter> | <digit>`"] # [doc = " >"] # [doc = " > `<letter> ::=` any one of the 52 alphabetic characters `A` through `Z` in upper case"] # [doc = " > and `a` through `z` in lower case"] # [doc = " >"] # [doc = " > `<digit> ::=` any one of the ten digits `0` through `9`"] # [doc = " >"] # [doc = " > Note that while upper and lower case letters are allowed in domain names, no significance is"] # [doc = " > attached to the case.  That is, two names with the same spelling but different case are to be"] # [doc = " > treated as if identical."] # [doc = " >"] # [doc = " > The labels must follow the rules for ARPANET host names.  They must start with a letter, end"] # [doc = " > with a letter or digit, and have as interior characters only letters, digits, and hyphen."] # [doc = " > There are also some restrictions on the length.  Labels must be 63 characters or less."] # [doc = " >"] # [doc = " > For example, the following strings identify hosts in the Internet:"] # [doc = " >"] # [doc = " > `A.ISI.EDU XX.LCS.MIT.EDU SRI-NIC.ARPA`"] # [doc = ""] # [doc = " <https://datatracker.ietf.org/doc/html/rfc1035#section-2.3.1>"] # [doc = ""] # [doc = " > Various objects and parameters in the DNS have size limits.  They are listed below.  Some"] # [doc = " > could be easily changed, others are more fundamental."] # [doc = " >"] # [doc = " > - labels: 63 octets or less"] # [doc = " > - names: 255 octets or less"] # [doc = " > - TTL: positive values of a signed 32 bit number."] # [doc = " > - UDP messages: 512 octets or less"] # [doc = ""] # [doc = " <https://datatracker.ietf.org/doc/html/rfc1035#section-2.3.4>"] # [derive (Clone , Debug , Eq , Hash , Ord , PartialEq , PartialOrd)] pub struct DnsName (String) ;
};
}
