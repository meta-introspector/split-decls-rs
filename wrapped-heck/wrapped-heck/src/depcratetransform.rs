// Generated macro for transform (function)
macro_rules! Depcratetransform {
() => {
// Module: crate
// Provides: {"transform"}
// Dependencies: {}
fn transform < F , G > (s : & str , mut with_word : F , mut boundary : G , f : & mut fmt :: Formatter ,) -> fmt :: Result where F : FnMut (& str , & mut fmt :: Formatter) -> fmt :: Result , G : FnMut (& mut fmt :: Formatter) -> fmt :: Result , { # [doc = " Tracks the current 'mode' of the transformation algorithm as it scans"] # [doc = " the input string."] # [doc = ""] # [doc = " The mode is a tri-state which tracks the case of the last cased"] # [doc = " character of the current word. If there is no cased character"] # [doc = " (either lowercase or uppercase) since the previous word boundary,"] # [doc = " than the mode is `Boundary`. If the last cased character is lowercase,"] # [doc = " then the mode is `Lowercase`. Othertherwise, the mode is"] # [doc = " `Uppercase`."] # [derive (Clone , Copy , PartialEq)] enum WordMode { # [doc = " There have been no lowercase or uppercase characters in the current"] # [doc = " word."] Boundary , # [doc = " The previous cased character in the current word is lowercase."] Lowercase , # [doc = " The previous cased character in the current word is uppercase."] Uppercase , } let mut first_word = true ; for word in s . split (| c : char | ! c . is_alphanumeric ()) { let mut char_indices = word . char_indices () . peekable () ; let mut init = 0 ; let mut mode = WordMode :: Boundary ; while let Some ((i , c)) = char_indices . next () { if let Some (& (next_i , next)) = char_indices . peek () { let next_mode = if c . is_lowercase () { WordMode :: Lowercase } else if c . is_uppercase () { WordMode :: Uppercase } else { mode } ; if next_mode == WordMode :: Lowercase && next . is_uppercase () { if ! first_word { boundary (f) ? ; } with_word (& word [init .. next_i] , f) ? ; first_word = false ; init = next_i ; mode = WordMode :: Boundary ; } else if mode == WordMode :: Uppercase && c . is_uppercase () && next . is_lowercase () { if ! first_word { boundary (f) ? ; } else { first_word = false ; } with_word (& word [init .. i] , f) ? ; init = i ; mode = WordMode :: Boundary ; } else { mode = next_mode ; } } else { if ! first_word { boundary (f) ? ; } else { first_word = false ; } with_word (& word [init ..] , f) ? ; break ; } } } Ok (()) }
};
}
