macro_rules! deps {
    () => {
        IvSizeUser!();
        Iv!();
        KeySizeUser!();
        Key!();
        InvalidLength!();
        WeakKeyError!();
    };
}

macro_rules! KeyIvInit {
    () => {
        deps!();
        # [doc = " Types which can be initialized from key and initialization vector (nonce)."] pub trait KeyIvInit : KeySizeUser + IvSizeUser + Sized { # [doc = " Create new value from fixed length key and nonce."] fn new (key : & Key < Self > , iv : & Iv < Self >) -> Self ; # [doc = " Check if the key might be considered weak."] # [inline] fn weak_key_test (_key : & Key < Self >) -> Result < () , WeakKeyError > { Ok (()) } # [doc = " Create new value from fixed length key and nonce after checking the key for weakness."] # [inline] fn new_checked (key : & Key < Self > , iv : & Iv < Self >) -> Result < Self , WeakKeyError > { Self :: weak_key_test (key) ? ; Ok (Self :: new (key , iv)) } # [doc = " Create new value from variable length key and nonce."] # [inline] fn new_from_slices (key : & [u8] , iv : & [u8]) -> Result < Self , InvalidLength > { let key = < & Key < Self > > :: try_from (key) . map_err (| _ | InvalidLength) ? ; let iv = < & Iv < Self > > :: try_from (iv) . map_err (| _ | InvalidLength) ? ; Ok (Self :: new (key , iv)) } # [doc = " Generate random key using the operating system's secure RNG."] # [cfg (feature = "getrandom")] # [inline] fn generate_key () -> Result < Key < Self > , getrandom :: Error > { let mut key = Key :: < Self > :: default () ; getrandom :: fill (& mut key) ? ; Ok (key) } # [doc = " Generate random key using the provided [`CryptoRng`]."] # [cfg (feature = "rand_core")] # [inline] fn generate_key_with_rng < R : CryptoRng + ? Sized > (rng : & mut R) -> Key < Self > { let mut key = Key :: < Self > :: default () ; rng . fill_bytes (& mut key) ; key } # [doc = " Generate random key using the provided [`TryCryptoRng`]."] # [cfg (feature = "rand_core")] # [inline] fn try_generate_key_with_rng < R : TryCryptoRng + ? Sized > (rng : & mut R ,) -> Result < Key < Self > , R :: Error > { let mut key = Key :: < Self > :: default () ; rng . try_fill_bytes (& mut key) ? ; Ok (key) } # [doc = " Generate random IV using the operating system's secure RNG."] # [cfg (feature = "getrandom")] # [inline] fn generate_iv () -> Result < Iv < Self > , getrandom :: Error > { let mut iv = Iv :: < Self > :: default () ; getrandom :: fill (& mut iv) ? ; Ok (iv) } # [doc = " Generate random IV using the provided [`CryptoRng`]."] # [cfg (feature = "rand_core")] # [inline] fn generate_iv_with_rng < R : CryptoRng + ? Sized > (rng : & mut R) -> Iv < Self > { let mut iv = Iv :: < Self > :: default () ; rng . fill_bytes (& mut iv) ; iv } # [doc = " Generate random IV using the provided [`TryCryptoRng`]."] # [cfg (feature = "rand_core")] # [inline] fn try_generate_iv_with_rng < R : TryCryptoRng + ? Sized > (rng : & mut R ,) -> Result < Iv < Self > , R :: Error > { let mut iv = Iv :: < Self > :: default () ; rng . try_fill_bytes (& mut iv) ? ; Ok (iv) } # [doc = " Generate random key and IV using the operating system's secure RNG."] # [cfg (feature = "getrandom")] # [inline] fn generate_key_iv () -> Result < (Key < Self > , Iv < Self >) , getrandom :: Error > { let key = Self :: generate_key () ? ; let iv = Self :: generate_iv () ? ; Ok ((key , iv)) } # [doc = " Generate random key and IV using the provided [`CryptoRng`]."] # [cfg (feature = "rand_core")] # [inline] fn generate_key_iv_with_rng < R : CryptoRng + ? Sized > (rng : & mut R) -> (Key < Self > , Iv < Self >) { let key = Self :: generate_key_with_rng (rng) ; let iv = Self :: generate_iv_with_rng (rng) ; (key , iv) } # [doc = " Generate random key and IV using the provided [`TryCryptoRng`]."] # [cfg (feature = "rand_core")] # [inline] fn try_generate_key_iv_with_rng < R : TryCryptoRng + ? Sized > (rng : & mut R ,) -> Result < (Key < Self > , Iv < Self >) , R :: Error > { let key = Self :: try_generate_key_with_rng (rng) ? ; let iv = Self :: try_generate_iv_with_rng (rng) ? ; Ok ((key , iv)) } }
    };
}

KeyIvInit!()