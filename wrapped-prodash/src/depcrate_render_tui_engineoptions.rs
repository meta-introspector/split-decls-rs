// Generated macro for Options (struct)
macro_rules! Depcrate_render_tui_engineOptions {
() => {
// Module: crate::render::tui::engine
// Provides: {"Options"}
// Dependencies: {}
# [doc = " Configure the terminal user interface"] # [derive (Clone)] pub struct Options { # [doc = " The initial title to show for the whole window."] # [doc = ""] # [doc = " Can be adjusted later by sending `Event::SetTitle(…)`"] # [doc = " into the event stream, see see [`tui::render_with_input(…events)`](./fn.render_with_input.html) function."] pub title : String , # [doc = " The amount of frames to draw per second. If below 1.0, it determines the amount of seconds between the frame."] # [doc = ""] # [doc = " *e.g.* 1.0/4.0 is one frame every 4 seconds."] pub frames_per_second : f32 , # [doc = " If true, (default false), we will keep track of the previous progress state to derive"] # [doc = " continuous throughput information from. Throughput will only show for units which have"] # [doc = " explicitly enabled it, it is opt-in."] # [doc = ""] # [doc = " This comes at the cost of additional memory and CPU time."] pub throughput : bool , # [doc = " If set, recompute the column width of the task tree only every given frame. Otherwise the width will be recomputed every frame."] # [doc = ""] # [doc = " Use this if there are many short-running tasks with varying names paired with high refresh rates of multiple frames per second to"] # [doc = " stabilize the appearance of the TUI."] # [doc = ""] # [doc = " For example, setting the value to 40 will with a frame rate of 20 per second will recompute the column width to fit all task names"] # [doc = " every 2 seconds."] pub recompute_column_width_every_nth_frame : Option < usize > , # [doc = " The initial window size."] # [doc = ""] # [doc = " If unset, it will be retrieved from the current terminal."] pub window_size : Option < Rect > , # [doc = " If true (default: true), we will stop running the TUI once the progress isn't available anymore (went out of scope)."] pub stop_if_progress_missing : bool , }
};
}
