// Generated macro for impl_22 (impl)
macro_rules! Depcrate_tree_rootimpl_22 {
() => {
// Module: crate::tree::root
// Provides: {"impl_22"}
// Dependencies: {}
impl Root { # [doc = " Create a new tree with default configuration."] # [doc = ""] # [doc = " As opposed to [Item](./struct.Item.html) instances, this type can be closed and sent"] # [doc = " safely across threads."] pub fn new () -> Arc < Root > { Options :: default () . into () } # [doc = " Returns the maximum amount of messages we can keep before overwriting older ones."] pub fn messages_capacity (& self) -> usize { self . inner . lock () . messages . lock () . buf . capacity () } # [doc = " Returns the current amount of `Item`s stored in the tree."] # [doc = " **Note** that this is at most a guess as tasks can be added and removed in parallel."] pub fn num_tasks (& self) -> usize { # [cfg (feature = "progress-tree-hp-hashmap")] { self . inner . lock () . tree . len () } # [cfg (not (feature = "progress-tree-hp-hashmap"))] { self . inner . lock () . tree . len () } } # [doc = " Adds a new child `tree::Item`, whose parent is this instance, with the given `name`."] # [doc = ""] # [doc = " This builds a hierarchy of `tree::Item`s, each having their own progress."] # [doc = " Use this method to [track progress](./struct.Item.html) of your first tasks."] pub fn add_child (& self , name : impl Into < String >) -> Item { self . inner . lock () . add_child (name) } # [doc = " Adds a new child `tree::Item`, whose parent is this instance, with the given `name` and `id`."] # [doc = ""] # [doc = " This builds a hierarchy of `tree::Item`s, each having their own progress."] # [doc = " Use this method to [track progress](./struct.Item.html) of your first tasks."] pub fn add_child_with_id (& self , name : impl Into < String > , id : Id) -> Item { self . inner . lock () . add_child_with_id (name , id) } # [doc = " Copy the entire progress tree into the given `out` vector, so that"] # [doc = " it can be traversed from beginning to end in order of hierarchy."] pub fn sorted_snapshot (& self , out : & mut Vec < (Key , Task) >) { out . clear () ; # [cfg (feature = "progress-tree-hp-hashmap")] out . extend (self . inner . lock () . tree . iter () . map (| r | (* r . key () , r . value () . clone ()))) ; # [cfg (not (feature = "progress-tree-hp-hashmap"))] self . inner . lock () . tree . extend_to (out) ; out . sort_by_key (| t | t . 0) ; } # [doc = " Copy all messages from the internal ring buffer into the given `out`"] # [doc = " vector. Messages are ordered from oldest to newest."] pub fn copy_messages (& self , out : & mut Vec < Message >) { self . inner . lock () . messages . lock () . copy_all (out) ; } # [doc = " Copy only new messages from the internal ring buffer into the given `out`"] # [doc = " vector. Messages are ordered from oldest to newest."] pub fn copy_new_messages (& self , out : & mut Vec < Message > , prev : Option < MessageCopyState >) -> MessageCopyState { self . inner . lock () . messages . lock () . copy_new (out , prev) } # [doc = " Duplicate all content and return it."] # [doc = ""] # [doc = " This is an expensive operation, whereas `clone()` is not as it is shallow."] pub fn deep_clone (& self) -> Arc < Root > { Arc :: new (Root { inner : Mutex :: new (self . inner . lock () . deep_clone ()) , }) } }
};
}
