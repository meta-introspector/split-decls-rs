// Generated macro for impl_61 (impl)
macro_rules! Depcrate_ordered_mapimpl_61 {
() => {
// Module: crate::ordered_map
// Provides: {"impl_61"}
// Dependencies: {}
impl < K , V > OrderedMap < K , V > { # [doc = " Returns the number of entries in the `OrderedMap`."] # [inline] pub const fn len (& self) -> usize { self . entries . len () } # [doc = " Returns true if the `OrderedMap` is empty."] # [inline] pub const fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Returns a reference to the value that `key` maps to."] pub fn get < T > (& self , key : & T) -> Option < & V > where T : Eq + PhfHash + ? Sized , K : PhfBorrow < T > , { self . get_entry (key) . map (| e | e . 1) } # [doc = " Returns a reference to the map's internal static instance of the given"] # [doc = " key."] # [doc = ""] # [doc = " This can be useful for interning schemes."] pub fn get_key < T > (& self , key : & T) -> Option < & K > where T : Eq + PhfHash + ? Sized , K : PhfBorrow < T > , { self . get_entry (key) . map (| e | e . 0) } # [doc = " Determines if `key` is in the `OrderedMap`."] pub fn contains_key < T > (& self , key : & T) -> bool where T : Eq + PhfHash + ? Sized , K : PhfBorrow < T > , { self . get (key) . is_some () } # [doc = " Returns the index of the key within the list used to initialize"] # [doc = " the ordered map."] pub fn get_index < T > (& self , key : & T) -> Option < usize > where T : Eq + PhfHash + ? Sized , K : PhfBorrow < T > , { self . get_internal (key) . map (| (i , _) | i) } # [doc = " Returns references to both the key and values at an index"] # [doc = " within the list used to initialize the ordered map. See `.get_index(key)`."] pub fn index (& self , index : usize) -> Option < (& K , & V) > { self . entries . get (index) . map (| (k , v) | (k , v)) } # [doc = " Like `get`, but returns both the key and the value."] pub fn get_entry < T > (& self , key : & T) -> Option < (& K , & V) > where T : Eq + PhfHash + ? Sized , K : PhfBorrow < T > , { self . get_internal (key) . map (| (_ , e) | e) } fn get_internal < T > (& self , key : & T) -> Option < (usize , (& K , & V)) > where T : Eq + PhfHash + ? Sized , K : PhfBorrow < T > , { if self . disps . is_empty () { return None ; } let hashes = phf_shared :: hash (key , & self . key) ; let idx_index = phf_shared :: get_index (& hashes , self . disps , self . idxs . len ()) ; let idx = self . idxs [idx_index as usize] ; let entry = & self . entries [idx] ; let b : & T = entry . 0 . borrow () ; if b == key { Some ((idx , (& entry . 0 , & entry . 1))) } else { None } } # [doc = " Returns an iterator over the key/value pairs in the map."] # [doc = ""] # [doc = " Entries are returned in the same order in which they were defined."] pub fn entries (& self) -> Entries < '_ , K , V > { Entries { iter : self . entries . iter () , } } # [doc = " Returns an iterator over the keys in the map."] # [doc = ""] # [doc = " Keys are returned in the same order in which they were defined."] pub fn keys (& self) -> Keys < '_ , K , V > { Keys { iter : self . entries () , } } # [doc = " Returns an iterator over the values in the map."] # [doc = ""] # [doc = " Values are returned in the same order in which they were defined."] pub fn values (& self) -> Values < '_ , K , V > { Values { iter : self . entries () , } } }
};
}
