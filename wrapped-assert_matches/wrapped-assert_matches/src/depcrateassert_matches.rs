// Generated macro for assert_matches (macro)
macro_rules! Depcrateassert_matches {
() => {
// Module: crate
// Provides: {"assert_matches"}
// Dependencies: {}
# [doc = " Asserts that an expression matches a given pattern."] # [doc = ""] # [doc = " A guard expression may be supplied to add further restrictions to the"] # [doc = " expected value of the expression."] # [doc = ""] # [doc = " A `match` arm may be supplied to perform additional assertions or to yield"] # [doc = " a value from the macro invocation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #[macro_use] extern crate assert_matches;"] # [doc = ""] # [doc = " #[derive(Debug)]"] # [doc = " enum Foo {"] # [doc = "     A(i32),"] # [doc = "     B(&'static str),"] # [doc = " }"] # [doc = ""] # [doc = " # fn main() {"] # [doc = " let a = Foo::A(1);"] # [doc = ""] # [doc = " // Assert that `a` matches the pattern `Foo::A(_)`."] # [doc = " assert_matches!(a, Foo::A(_));"] # [doc = ""] # [doc = " // Assert that `a` matches the pattern and"] # [doc = " // that the contained value meets the condition `i > 0`."] # [doc = " assert_matches!(a, Foo::A(i) if i > 0);"] # [doc = ""] # [doc = " let b = Foo::B(\"foobar\");"] # [doc = " "] # [doc = " // Assert that `b` matches the pattern `Foo::B(_)`."] # [doc = " assert_matches!(b, Foo::B(s) => {"] # [doc = "     // Perform additional assertions on the variable binding `s`."] # [doc = "     assert!(s.starts_with(\"foo\"));"] # [doc = "     assert!(s.ends_with(\"bar\"));"] # [doc = " });"] # [doc = ""] # [doc = " // Assert that `b` matches the pattern and yield the string `s`."] # [doc = " let s = assert_matches!(b, Foo::B(s) => s);"] # [doc = ""] # [doc = " // Perform an assertion on the value `s`."] # [doc = " assert_eq!(s, \"foobar\");"] # [doc = " # }"] # [doc = " ```"] # [macro_export] macro_rules ! assert_matches { ($ e : expr , $ ($ pat : pat) |+) => { match $ e { $ ($ pat) |+ => () , ref e => panic ! ("assertion failed: `{:?}` does not match `{}`" , e , stringify ! ($ ($ pat) |+)) } } ; ($ e : expr , $ ($ pat : pat) |+ if $ cond : expr) => { match $ e { $ ($ pat) |+ if $ cond => () , ref e => panic ! ("assertion failed: `{:?}` does not match `{}`" , e , stringify ! ($ ($ pat) |+ if $ cond)) } } ; ($ e : expr , $ ($ pat : pat) |+ => $ arm : expr) => { match $ e { $ ($ pat) |+ => $ arm , ref e => panic ! ("assertion failed: `{:?}` does not match `{}`" , e , stringify ! ($ ($ pat) |+)) } } ; ($ e : expr , $ ($ pat : pat) |+ if $ cond : expr => $ arm : expr) => { match $ e { $ ($ pat) |+ if $ cond => $ arm , ref e => panic ! ("assertion failed: `{:?}` does not match `{}`" , e , stringify ! ($ ($ pat) |+ if $ cond)) } } ; ($ e : expr , $ ($ pat : pat) |+ , $ ($ arg : tt) *) => { match $ e { $ ($ pat) |+ => () , ref e => panic ! ("assertion failed: `{:?}` does not match `{}`: {}" , e , stringify ! ($ ($ pat) |+) , format_args ! ($ ($ arg) *)) } } ; ($ e : expr , $ ($ pat : pat) |+ if $ cond : expr , $ ($ arg : tt) *) => { match $ e { $ ($ pat) |+ if $ cond => () , ref e => panic ! ("assertion failed: `{:?}` does not match `{}`: {}" , e , stringify ! ($ ($ pat) |+ if $ cond) , format_args ! ($ ($ arg) *)) } } ; ($ e : expr , $ ($ pat : pat) |+ => $ arm : expr , $ ($ arg : tt) *) => { match $ e { $ ($ pat) |+ => $ arm , ref e => panic ! ("assertion failed: `{:?}` does not match `{}`: {}" , e , stringify ! ($ ($ pat) |+) , format_args ! ($ ($ arg) *)) } } ; ($ e : expr , $ ($ pat : pat) |+ if $ cond : expr => $ arm : expr , $ ($ arg : tt) *) => { match $ e { $ ($ pat) |+ if $ cond => $ arm , ref e => panic ! ("assertion failed: `{:?}` does not match `{}`: {}" , e , stringify ! ($ ($ pat) |+ if $ cond) , format_args ! ($ ($ arg) *)) } } ; }
};
}
