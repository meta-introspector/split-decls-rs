// Generated macro for OrderedFloat (struct)
macro_rules! DepcrateOrderedFloat {
() => {
// Module: crate
// Provides: {"OrderedFloat"}
// Dependencies: {}
# [doc = " A wrapper around floats providing implementations of `Eq`, `Ord`, and `Hash`."] # [doc = ""] # [doc = " NaN is sorted as *greater* than all other values and *equal*"] # [doc = " to itself, in contradiction with the IEEE standard."] # [doc = ""] # [doc = " ```"] # [doc = " use ordered_float::OrderedFloat;"] # [doc = " use std::f32::NAN;"] # [doc = ""] # [doc = " let mut v = [OrderedFloat(NAN), OrderedFloat(2.0), OrderedFloat(1.0)];"] # [doc = " v.sort();"] # [doc = " assert_eq!(v, [OrderedFloat(1.0), OrderedFloat(2.0), OrderedFloat(NAN)]);"] # [doc = " ```"] # [doc = ""] # [doc = " Because `OrderedFloat` implements `Ord` and `Eq`, it can be used as a key in a `HashSet`,"] # [doc = " `HashMap`, `BTreeMap`, or `BTreeSet` (unlike the primitive `f32` or `f64` types):"] # [doc = ""] # [doc = " ```"] # [doc = " # use ordered_float::OrderedFloat;"] # [doc = " # use std::collections::HashSet;"] # [doc = " # use std::f32::NAN;"] # [doc = " let mut s: HashSet<OrderedFloat<f32>> = HashSet::new();"] # [doc = " s.insert(OrderedFloat(NAN));"] # [doc = " assert!(s.contains(&OrderedFloat(NAN)));"] # [doc = " ```"] # [doc = ""] # [doc = " Some non-identical values are still considered equal by the [`PartialEq`] implementation,"] # [doc = " and will therefore also be considered equal by maps, sets, and the `==` operator:"] # [doc = ""] # [doc = " * `-0.0` and `+0.0` are considered equal."] # [doc = "   This different sign may show up in printing, or when dividing by zero (the sign of the zero"] # [doc = "   becomes the sign of the resulting infinity)."] # [doc = " * All NaN values are considered equal, even though they may have different"] # [doc = "   [bits](https://doc.rust-lang.org/std/primitive.f64.html#method.to_bits), and therefore"] # [doc = "   different [sign](https://doc.rust-lang.org/std/primitive.f64.html#method.is_sign_positive),"] # [doc = "   signaling/quiet status, and NaN payload bits."] # [doc = "   "] # [doc = " Therefore, `OrderedFloat` may be unsuitable for use as a key in interning and memoization"] # [doc = " applications which require equal results from equal inputs, unless these cases make no"] # [doc = " difference or are canonicalized before insertion."] # [doc = ""] # [doc = " # Representation"] # [doc = ""] # [doc = " `OrderedFloat` has `#[repr(transparent)]` and permits any value, so it is sound to use"] # [doc = " [transmute](core::mem::transmute) or pointer casts to convert between any type `T` and"] # [doc = " `OrderedFloat<T>`."] # [doc = " However, consider using [`bytemuck`] as a safe alternative if possible."] # [doc = ""] # [cfg_attr (not (feature = "bytemuck") , doc = "[`bytemuck`]: https://docs.rs/bytemuck/1/")] # [derive (Default , Clone , Copy)] # [repr (transparent)] pub struct OrderedFloat < T > (pub T) ;
};
}
