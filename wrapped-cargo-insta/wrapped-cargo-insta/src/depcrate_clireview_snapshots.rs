// Generated macro for review_snapshots (function)
macro_rules! Depcrate_clireview_snapshots {
() => {
// Module: crate::cli
// Provides: {"review_snapshots"}
// Dependencies: {}
# [doc = " Processes snapshot files for reviewing, accepting, or rejecting."] fn review_snapshots (quiet : bool , snapshot_filter : Option < & [String] > , loc : & LocationInfo < '_ > , op : Option < Operation > ,) -> Result < () , Box < dyn Error > > { let term = Term :: stdout () ; let (mut snapshot_containers , roots) = load_snapshot_containers (loc) ? ; let snapshot_count = snapshot_containers . iter () . map (| x | x . 0 . len ()) . sum () ; if snapshot_count == 0 { if ! quiet { println ! ("{}: no snapshots to review" , style ("done") . bold ()) ; if loc . tool_config . review_warn_undiscovered () { show_undiscovered_hint (loc . find_flags , & snapshot_containers . iter () . map (| x | x . 0 . clone ()) . collect_vec () , & roots , & loc . exts ,) ; } } return Ok (()) ; } let mut accepted = vec ! [] ; let mut rejected = vec ! [] ; let mut skipped = vec ! [] ; let mut num = 0 ; let mut show_info = true ; let mut show_diff = true ; let mut apply_to_all : Option < Operation > = None ; for (snapshot_container , package) in snapshot_containers . iter_mut () { let target_file = snapshot_container . target_file () . to_path_buf () ; let snapshot_file = snapshot_container . snapshot_file () . map (| x | x . to_path_buf ()) ; for snapshot_ref in snapshot_container . iter_snapshots () { if let Some (filter) = snapshot_filter { let key = if let Some (line) = snapshot_ref . line { format ! ("{}:{}" , target_file . display () , line) } else { format ! ("{}" , target_file . display ()) } ; if ! filter . contains (& key) { skipped . push (snapshot_ref . summary ()) ; continue ; } } num += 1 ; let op = match (op , apply_to_all) { (Some (op) , _) => op , (_ , Some (op)) => op , _ => { let choice = query_snapshot (& loc . workspace_root , & term , & snapshot_ref . new , snapshot_ref . old . as_ref () , package , snapshot_ref . line , num , snapshot_count , snapshot_file . as_deref () , & mut show_info , & mut show_diff ,) ? ; match choice { Operation :: AcceptAll => { apply_to_all = Some (Operation :: Accept) ; Operation :: Accept } Operation :: RejectAll => { apply_to_all = Some (Operation :: Reject) ; Operation :: Reject } Operation :: SkipAll => { apply_to_all = Some (Operation :: Skip) ; Operation :: Skip } op => op , } } } ; match op { Operation :: Accept | Operation :: AcceptAll => { snapshot_ref . op = Operation :: Accept ; accepted . push (snapshot_ref . summary ()) ; } Operation :: Reject | Operation :: RejectAll => { snapshot_ref . op = Operation :: Reject ; rejected . push (snapshot_ref . summary ()) ; } Operation :: Skip | Operation :: SkipAll => { skipped . push (snapshot_ref . summary ()) ; } } } snapshot_container . commit () ? ; } if op . is_none () && apply_to_all . is_none () { term . clear_screen () ? ; } if ! quiet { println ! ("{}" , style ("insta review finished") . bold ()) ; if ! accepted . is_empty () { println ! ("{}:" , style ("accepted") . green ()) ; for item in accepted { println ! ("  {item}") ; } } if ! rejected . is_empty () { println ! ("{}:" , style ("rejected") . red ()) ; for item in rejected { println ! ("  {item}") ; } } if ! skipped . is_empty () { println ! ("{}:" , style ("skipped") . yellow ()) ; for item in skipped { println ! ("  {item}") ; } } } Ok (()) }
};
}
