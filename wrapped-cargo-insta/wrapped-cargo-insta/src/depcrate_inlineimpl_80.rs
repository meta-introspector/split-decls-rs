// Generated macro for impl_80 (impl)
macro_rules! Depcrate_inlineimpl_80 {
() => {
// Module: crate::inline
// Provides: {"impl_80"}
// Dependencies: {}
impl FilePatcher { pub (crate) fn open (p : & Path) -> Result < FilePatcher , Box < dyn Error > > { let filename = p . to_path_buf () ; let contents = fs :: read_to_string (p) ? ; let source = syn :: parse_file (& contents) ? ; let lines : Vec < String > = contents . lines () . map (| x | x . into ()) . collect () ; Ok (FilePatcher { filename , source , lines , inline_snapshots : vec ! [] , }) } pub (crate) fn save (& self) -> Result < () , Box < dyn Error > > { let mut temp_file = tempfile :: Builder :: new () . suffix (".snap.tmp") . tempfile_in (self . filename . parent () . ok_or ("Parent directory not found") ?) ? ; for line in & self . lines { writeln ! (temp_file , "{line}") ? ; } temp_file . flush () ? ; fs :: rename (temp_file . path () , & self . filename) ? ; Ok (()) } pub (crate) fn add_snapshot_macro (& mut self , line : usize) -> bool { match self . find_snapshot_macro (line) { Some (snapshot) => { if self . inline_snapshots . last () . map_or (false , | x | x . end . 0 >= line - 1) { return false ; } self . inline_snapshots . push (snapshot) ; true } None => false , } } pub (crate) fn get_new_line (& self , id : usize) -> usize { self . inline_snapshots [id] . start . 0 + 1 } pub (crate) fn set_new_content (& mut self , id : usize , snapshot : & TextSnapshotContents) { let inline = & mut self . inline_snapshots [id] ; let prefix : String = self . lines [inline . start . 0] . chars () . take (inline . start . 1) . collect () ; let suffix : String = self . lines [inline . end . 0] . chars () . skip (inline . end . 1) . collect () ; let snapshot_line_contents = [prefix , snapshot . to_inline (& inline . indentation) , suffix] . join ("") ; self . lines . splice (inline . start . 0 ..= inline . end . 0 , snapshot_line_contents . lines () . map (| l | l . to_string ()) ,) ; let old_lines_count = inline . end . 0 - inline . start . 0 + 1 ; let line_count_diff = (snapshot_line_contents . lines () . count () as isize) - (old_lines_count as isize) ; for inl in & mut self . inline_snapshots [id ..] { inl . start . 0 = ((inl . start . 0 as isize) + line_count_diff) as usize ; inl . end . 0 = ((inl . end . 0 as isize) + line_count_diff) as usize ; } } fn find_snapshot_macro (& self , line : usize) -> Option < InlineSnapshot > { struct Visitor < 'a > (usize , Option < InlineSnapshot > , & 'a [String]) ; fn indentation (macro_start : LineColumn , code_lines : & [String]) -> String { code_lines [macro_start . line - 1] [.. macro_start . column] . to_owned () } fn scan_for_path_start (tokens : & [TokenTree] , pos : usize , code_lines : & [String]) -> String { let mut rev_tokens = tokens [..= pos] . iter () . rev () ; let mut start = rev_tokens . next () . unwrap () ; loop { if let Some (TokenTree :: Punct (ref punct)) = rev_tokens . next () { if punct . as_char () == ':' { if let Some (TokenTree :: Punct (ref punct)) = rev_tokens . next () { if punct . as_char () == ':' { if let Some (ident @ TokenTree :: Ident (_)) = rev_tokens . next () { start = ident ; continue ; } } } } } break ; } indentation (start . span () . start () , code_lines) } impl Visitor < '_ > { fn scan_nested_macros (& mut self , tokens : & [TokenTree]) { for idx in 0 .. tokens . len () { if let Some (TokenTree :: Ident (_)) = tokens . get (idx) { if let Some (TokenTree :: Punct (ref punct)) = tokens . get (idx + 1) { if punct . as_char () == '!' { if let Some (TokenTree :: Group (ref group)) = tokens . get (idx + 2) { let indentation = scan_for_path_start (tokens , idx , self . 2) ; let tokens : Vec < _ > = group . stream () . into_iter () . collect () ; self . try_extract_snapshot (& tokens , indentation) ; } } } } } for token in tokens { if let TokenTree :: Group (group) = token { let tokens : Vec < _ > = group . stream () . into_iter () . collect () ; self . scan_nested_macros (& tokens) ; } } } fn try_extract_snapshot (& mut self , tokens : & [TokenTree] , indentation : String) -> bool { let tokens = match tokens . last () { Some (TokenTree :: Punct (ref punct)) if punct . as_char () == ',' => { & tokens [.. tokens . len () - 1] } _ => tokens , } ; if tokens . len () < 2 { return false ; } match tokens [tokens . len () - 2] { TokenTree :: Punct (ref punct) if punct . as_char () == '@' => { } _ => { return false ; } } let (start , end) = match & tokens [tokens . len () - 1] { TokenTree :: Literal (lit) => { let span = lit . span () ; ((span . start () . line - 1 , span . start () . column) , (span . end () . line - 1 , span . end () . column) ,) } _ => return false , } ; self . 1 = Some (InlineSnapshot { start , end , indentation , }) ; true } } impl < 'ast > syn :: visit :: Visit < 'ast > for Visitor < '_ > { fn visit_attribute (& mut self , i : & 'ast syn :: Attribute) { let start = i . span () . start () . line ; let end = i . span () . end () . line ; if start > self . 0 || end < self . 0 || i . path () . segments . is_empty () { return ; } let tokens : Vec < _ > = i . meta . to_token_stream () . into_iter () . collect () ; if ! tokens . is_empty () { self . scan_nested_macros (& tokens) ; } } fn visit_macro (& mut self , i : & 'ast syn :: Macro) { let span_start = i . span () . start () ; let start = span_start . line ; let end = i . tokens . clone () . into_iter () . last () . map_or (start , | t | t . span () . end () . line) ; if start > self . 0 || end < self . 0 || i . path . segments . is_empty () { return ; } let tokens : Vec < _ > = i . tokens . clone () . into_iter () . collect () ; if tokens . len () < 2 { return ; } if matches ! (i . delimiter , syn :: MacroDelimiter :: Brace (_)) { if let Ok (stmts) = i . parse_body_with (syn :: Block :: parse_within) { for stmt in & stmts { self . visit_stmt (stmt) ; } return ; } } let indentation = indentation (span_start , self . 2) ; if ! self . try_extract_snapshot (& tokens , indentation) { self . scan_nested_macros (& tokens) ; } } } let mut visitor = Visitor (line , None , & self . lines) ; syn :: visit :: visit_file (& mut visitor , & self . source) ; visitor . 1 } }
};
}
