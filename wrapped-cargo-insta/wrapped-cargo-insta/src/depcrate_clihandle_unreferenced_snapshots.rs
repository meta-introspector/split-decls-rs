// Generated macro for handle_unreferenced_snapshots (function)
macro_rules! Depcrate_clihandle_unreferenced_snapshots {
() => {
// Module: crate::cli
// Provides: {"handle_unreferenced_snapshots"}
// Dependencies: {}
# [doc = " Scan for any snapshots that were not referenced by any test."] fn handle_unreferenced_snapshots (snapshot_ref_path : & Path , loc : & LocationInfo < '_ > , unreferenced : UnreferencedSnapshots ,) -> Result < () , Box < dyn Error > > { enum Action { Delete , Reject , Warn , } let action = match unreferenced { UnreferencedSnapshots :: Auto => { if is_ci () { Action :: Reject } else { Action :: Delete } } UnreferencedSnapshots :: Reject => Action :: Reject , UnreferencedSnapshots :: Delete => Action :: Delete , UnreferencedSnapshots :: Warn => Action :: Warn , UnreferencedSnapshots :: Ignore => return Ok (()) , } ; let snapshot_files_from_test = fs :: read_to_string (snapshot_ref_path) . map (| s | { s . lines () . filter_map (| line | fs :: canonicalize (line) . ok ()) . collect () }) . or_else (| err | { if err . kind () == io :: ErrorKind :: NotFound { Ok (HashSet :: new ()) } else { Err (err) } }) ? ; let mut encountered_any = false ; for package in loc . packages . clone () { let unreferenced_snapshots = make_snapshot_walker (package . manifest_path . parent () . unwrap () . as_std_path () , & loc . exts , FindFlags { include_ignored : true , include_hidden : true , } ,) . filter_map (| e | e . ok ()) . filter (| e | e . file_type () . map (| ft | ft . is_file ()) . unwrap_or (false)) . filter_map (| e | e . path () . canonicalize () . ok ()) . filter (| path | ! snapshot_files_from_test . contains (path)) . filter (| path | { path . extension () . map (| x | x != "new" && x != "pending-snap") . unwrap_or (true) }) ; for path in unreferenced_snapshots { if ! encountered_any { match action { Action :: Delete => { eprintln ! ("{}: deleted unreferenced snapshots:" , style ("info") . bold ()) ; } _ => { eprintln ! ("{}: encountered unreferenced snapshots:" , style ("warning") . bold ()) ; } } encountered_any = true ; } eprintln ! ("  {}" , path . display ()) ; if matches ! (action , Action :: Delete) { if path . extension () == Some (std :: ffi :: OsStr :: new ("pending-snap")) { if let Err (e) = fs :: remove_file (path) { eprintln ! ("Failed to remove file: {e}") ; } } else { let snapshot = match Snapshot :: from_file (& path) { Ok (snapshot) => snapshot , Err (e) => { eprintln ! ("Error loading snapshot at {:?}: {}" , & path , e) ; continue ; } } ; if let Some (binary_path) = snapshot . build_binary_path (& path) { fs :: remove_file (& binary_path) . ok () ; } fs :: remove_file (& path) . ok () ; } } } } fs :: remove_file (snapshot_ref_path) . ok () ; if ! encountered_any { eprintln ! ("{}: no unreferenced snapshots found" , style ("info") . bold ()) ; } else if matches ! (action , Action :: Reject) { return Err (err_msg ("aborting because of unreferenced snapshots")) ; } Ok (()) }
};
}
