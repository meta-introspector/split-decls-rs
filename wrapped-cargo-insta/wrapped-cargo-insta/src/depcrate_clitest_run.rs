// Generated macro for test_run (function)
macro_rules! Depcrate_clitest_run {
() => {
// Module: crate::cli
// Provides: {"test_run"}
// Dependencies: {}
# [doc = " Run the tests"] fn test_run (mut cmd : TestCommand , color : ColorWhen) -> Result < () , Box < dyn Error > > { let loc = handle_target_args (& cmd . target_args , & cmd . test_runner_options . package) ? ; if cmd . accept_unseen { eprintln ! ("{} If this option is materially helpful to you, please add a comment at https://github.com/mitsuhiko/insta/issues/659." , style ("`--accept-unseen` is pending deprecation.") . bold () . yellow ()) } match loc . tool_config . snapshot_update () { SnapshotUpdate :: Auto => { if is_ci () { cmd . check = true ; } } SnapshotUpdate :: New | SnapshotUpdate :: No => { } SnapshotUpdate :: Always => { if ! cmd . accept && ! cmd . accept_unseen && ! cmd . review { cmd . review = false ; cmd . accept = true ; } } SnapshotUpdate :: Unseen => { if ! cmd . accept { cmd . accept_unseen = true ; cmd . review = true ; cmd . accept = false ; } } SnapshotUpdate :: Force => { cmd . force_update_snapshots = true ; } } if cmd . force_update_snapshots { cmd . accept = true ; } if cmd . no_force_pass { cmd . check = true ; eprintln ! ("{}: `--no-force-pass` is deprecated. Please use --check to immediately raise an error on any non-matching snapshots." , style ("warning") . bold () . yellow ()) } if cmd . keep_pending { eprintln ! ("{}: `--keep-pending` is deprecated; its behavior is implied: pending snapshots are never removed before a test run." , style ("warning") . bold () . yellow ()) } if loc . tool_config . auto_accept_unseen () && ! cmd . accept && ! cmd . review { cmd . accept_unseen = true ; } if loc . tool_config . auto_review () && ! cmd . review && ! cmd . accept { cmd . review = true ; } if cmd . delete_unreferenced_snapshots { eprintln ! ("Warning: `--delete-unreferenced-snapshots` is deprecated. Use `--unreferenced=delete` instead.") ; cmd . unreferenced = Some (UnreferencedSnapshots :: Delete) ; } cmd . unreferenced = cmd . unreferenced . or_else (| | Some (loc . tool_config . test_unreferenced ())) ; let test_runner = match cmd . test_runner { TestRunner :: Auto => loc . tool_config . test_runner () , TestRunner :: CargoTest => TestRunner :: CargoTest , TestRunner :: Nextest => TestRunner :: Nextest , } ; let test_runner = test_runner . resolve_fallback (cmd . test_runner_fallback_value () . unwrap_or (loc . tool_config . test_runner_fallback ()) ,) ; let (mut proc , snapshot_ref_file , prevents_doc_run) = prepare_test_runner (& cmd , test_runner , color , & [] , None , & loc) ? ; if let Some (workspace_root) = & cmd . target_args . workspace_root { proc . current_dir (workspace_root) ; } let status = proc . status () ? ; let mut success = status . success () ; if matches ! (cmd . test_runner , TestRunner :: Nextest) && ! prevents_doc_run && ! cmd . disable_nextest_doctest { if has_doctests (& loc . packages) { eprintln ! ("{}: insta won't run a separate doctest process when using nextest in the future. \
                 Pass `--disable-nextest-doctest` to update to this behavior now and silence this warning." , style ("warning") . bold () . yellow ()) ; } let (mut proc , _ , _) = prepare_test_runner (& cmd , & TestRunner :: CargoTest , color , & ["--doc"] , snapshot_ref_file . as_deref () , & loc ,) ? ; success = success && proc . status () ? . success () ; } if ! success && cmd . review { eprintln ! ("{} non snapshot tests failed, skipping review" , style ("warning:") . bold () . yellow ()) ; return Err (QuietExit (1) . into ()) ; } if success { if let Some (ref snapshot_ref_path) = snapshot_ref_file { handle_unreferenced_snapshots (snapshot_ref_path . borrow () , & loc , cmd . unreferenced . unwrap () ,) ? ; } } if cmd . review || cmd . accept { review_snapshots (false , None , & loc , if cmd . accept { Some (Operation :: Accept) } else { None } ,) ? } else { let (snapshot_containers , roots) = load_snapshot_containers (& loc) ? ; let snapshot_containers = snapshot_containers . into_iter () . map (| x | x . 0) . collect_vec () ; let snapshot_count = snapshot_containers . iter () . map (| x | x . len ()) . sum :: < usize > () ; if snapshot_count > 0 { eprintln ! ("{}: {} snapshot{} to review" , style ("info") . bold () , style (snapshot_count) . yellow () , if snapshot_count != 1 { "s" } else { "" }) ; eprintln ! ("use `cargo insta review` to review snapshots") ; return Err (QuietExit (1) . into ()) ; } else { println ! ("{}: no snapshots to review" , style ("info") . bold ()) ; if loc . tool_config . review_warn_undiscovered () { show_undiscovered_hint (loc . find_flags , & snapshot_containers , & roots , & loc . exts) ; } } } if ! success { Err (QuietExit (1) . into ()) } else { Ok (()) } }
};
}
