// Generated by add_generated_rust_header! macro at submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs:72 from submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs
// Do not edit this file by hand. Your changes will be overwritten.

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
pub trait Callbacks {
    #[doc = " Called before creating the compiler instance"]
    fn config(&mut self, _config: &mut interface::Config) {}
    #[doc = " Called after parsing the crate root. Submodules are not yet parsed when"]
    #[doc = " this callback is called. Return value instructs the compiler whether to"]
    #[doc = " continue the compilation afterwards (defaults to `Compilation::Continue`)"]
    fn after_crate_root_parsing(
        &mut self,
        _compiler: &interface::Compiler,
        _krate: &mut ast::Crate,
    ) -> Compilation {
        Compilation::Continue
    }
    #[doc = " Called after expansion. Return value instructs the compiler whether to"]
    #[doc = " continue the compilation afterwards (defaults to `Compilation::Continue`)"]
    fn after_expansion<'tcx>(
        &mut self,
        _compiler: &interface::Compiler,
        _tcx: TyCtxt<'tcx>,
    ) -> Compilation {
        Compilation::Continue
    }
    #[doc = " Called after analysis. Return value instructs the compiler whether to"]
    #[doc = " continue the compilation afterwards (defaults to `Compilation::Continue`)"]
    fn after_analysis<'tcx>(
        &mut self,
        _compiler: &interface::Compiler,
        _tcx: TyCtxt<'tcx>,
    ) -> Compilation {
        Compilation::Continue
    }
}
