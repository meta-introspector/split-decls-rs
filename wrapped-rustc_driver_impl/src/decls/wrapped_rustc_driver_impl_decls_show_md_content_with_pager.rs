// Generated by add_generated_rust_header! macro at submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs:72 from submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs 
// Do not edit this file by hand. Your changes will be overwritten.

 use serde :: { Deserialize , Serialize } ; use std :: collections :: HashMap ; # [doc = " If `color` is `always` or `auto`, try to print pretty (formatted & colorized) markdown. If"] # [doc = " that fails or `color` is `never`, print the raw markdown."] # [doc = ""] # [doc = " Uses a pager if possible, falls back to stdout."] fn show_md_content_with_pager (content : & str , color : ColorConfig) { let pager_name = env :: var_os ("PAGER") . unwrap_or_else (| | { if cfg ! (windows) { OsString :: from ("more.com") } else { OsString :: from ("less") } }) ; let mut cmd = Command :: new (& pager_name) ; if pager_name == "less" { cmd . arg ("-R") ; } let pretty_on_pager = match color { ColorConfig :: Auto => { ["less" , "bat" , "batcat" , "delta"] . iter () . any (| v | * v == pager_name) } ColorConfig :: Always => true , ColorConfig :: Never => false , } ; let mut pretty_data = { let mdstream = markdown :: MdStream :: parse_str (content) ; let bufwtr = markdown :: create_stdout_bufwtr () ; let mut mdbuf = Vec :: new () ; if mdstream . write_anstream_buf (& mut mdbuf) . is_ok () { Some ((bufwtr , mdbuf)) } else { None } } ; let pager_res : Option < () > = try { let mut pager = cmd . stdin (Stdio :: piped ()) . spawn () . ok () ? ; let pager_stdin = pager . stdin . as_mut () ? ; if pretty_on_pager && let Some ((_ , mdbuf)) = & pretty_data { pager_stdin . write_all (mdbuf . as_slice ()) . ok () ? ; } else { pager_stdin . write_all (content . as_bytes ()) . ok () ? ; } ; pager . wait () . ok () ? ; } ; if pager_res . is_some () { return ; } if let Some ((bufwtr , mdbuf)) = & mut pretty_data && bufwtr . write_all (& mdbuf) . is_ok () { return ; } safe_print ! ("{content}") ; }