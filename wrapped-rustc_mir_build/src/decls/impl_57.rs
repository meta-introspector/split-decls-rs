macro_rules! deps {
    () => {
        Builder!();
        NeedsTemporary!();
        BlockAnd!();
        Category!();
        Scope!();
    };
}

macro_rules! impl_57 {
    () => {
        deps!();
        impl < 'a , 'tcx > Builder < 'a , 'tcx > { # [doc = " Construct a temporary lifetime restricted to just the local scope"] pub (crate) fn local_temp_lifetime (& self) -> TempLifetime { let local_scope = self . local_scope () ; TempLifetime { temp_lifetime : Some (local_scope) , backwards_incompatible : None } } # [doc = " Returns an operand suitable for use until the end of the current"] # [doc = " scope expression."] # [doc = ""] # [doc = " The operand returned from this function will *not be valid*"] # [doc = " after the current enclosing `ExprKind::Scope` has ended, so"] # [doc = " please do *not* return it from functions to avoid bad"] # [doc = " miscompiles."] pub (crate) fn as_local_operand (& mut self , block : BasicBlock , expr_id : ExprId ,) -> BlockAnd < Operand < 'tcx > > { self . as_operand (block , self . local_temp_lifetime () , expr_id , LocalInfo :: Boring , NeedsTemporary :: Maybe ,) } # [doc = " Returns an operand suitable for use until the end of the current scope expression and"] # [doc = " suitable also to be passed as function arguments."] # [doc = ""] # [doc = " The operand returned from this function will *not be valid* after an ExprKind::Scope is"] # [doc = " passed, so please do *not* return it from functions to avoid bad miscompiles. Returns an"] # [doc = " operand suitable for use as a call argument. This is almost always equivalent to"] # [doc = " `as_operand`, except for the particular case of passing values of (potentially) unsized"] # [doc = " types \"by value\" (see details below)."] # [doc = ""] # [doc = " The operand returned from this function will *not be valid*"] # [doc = " after the current enclosing `ExprKind::Scope` has ended, so"] # [doc = " please do *not* return it from functions to avoid bad"] # [doc = " miscompiles."] # [doc = ""] # [doc = " # Parameters of unsized types"] # [doc = ""] # [doc = " We tweak the handling of parameters of unsized type slightly to avoid the need to create a"] # [doc = " local variable of unsized type. For example, consider this program:"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(unsized_fn_params)]"] # [doc = " # use core::fmt::Debug;"] # [doc = " fn foo(_p: dyn Debug) {"] # [doc = "     /* ... */"] # [doc = " }"] # [doc = ""] # [doc = " fn bar(box_p: Box<dyn Debug>) { foo(*box_p); }"] # [doc = " ```"] # [doc = ""] # [doc = " Ordinarily, for sized types, we would compile the call `foo(*p)` like so:"] # [doc = ""] # [doc = " ```ignore (illustrative)"] # [doc = " let tmp0 = *box_p; // tmp0 would be the operand returned by this function call"] # [doc = " foo(tmp0)"] # [doc = " ```"] # [doc = ""] # [doc = " But because the parameter to `foo` is of the unsized type `dyn Debug`, and because it is"] # [doc = " being moved the deref of a box, we compile it slightly differently. The temporary `tmp0`"] # [doc = " that we create *stores the entire box*, and the parameter to the call itself will be"] # [doc = " `*tmp0`:"] # [doc = ""] # [doc = " ```ignore (illustrative)"] # [doc = " let tmp0 = box_p; call foo(*tmp0)"] # [doc = " ```"] # [doc = ""] # [doc = " This way, the temporary `tmp0` that we create has type `Box<dyn Debug>`, which is sized."] # [doc = " The value passed to the call (`*tmp0`) still has the `dyn Debug` type -- but the way that"] # [doc = " calls are compiled means that this parameter will be passed \"by reference\", meaning that we"] # [doc = " will actually provide a pointer to the interior of the box, and not move the `dyn Debug`"] # [doc = " value to the stack."] # [doc = ""] # [doc = " See <https://github.com/rust-lang/rust/issues/68304> for more details."] pub (crate) fn as_local_call_operand (& mut self , block : BasicBlock , expr : ExprId ,) -> BlockAnd < Operand < 'tcx > > { self . as_call_operand (block , self . local_temp_lifetime () , expr) } # [doc = " Compile `expr` into a value that can be used as an operand."] # [doc = " If `expr` is a place like `x`, this will introduce a"] # [doc = " temporary `tmp = x`, so that we capture the value of `x` at"] # [doc = " this time."] # [doc = ""] # [doc = " If we end up needing to create a temporary, then we will use"] # [doc = " `local_info` as its `LocalInfo`, unless `as_temporary`"] # [doc = " has already assigned it a non-`None` `LocalInfo`."] # [doc = " Normally, you should use `None` for `local_info`"] # [doc = ""] # [doc = " The operand is known to be live until the end of `scope`."] # [doc = ""] # [doc = " Like `as_local_call_operand`, except that the argument will"] # [doc = " not be valid once `scope` ends."] # [instrument (level = "debug" , skip (self , scope))] pub (crate) fn as_operand (& mut self , mut block : BasicBlock , scope : TempLifetime , expr_id : ExprId , local_info : LocalInfo < 'tcx > , needs_temporary : NeedsTemporary ,) -> BlockAnd < Operand < 'tcx > > { let this = self ; let expr = & this . thir [expr_id] ; if let ExprKind :: Scope { region_scope , lint_level , value } = expr . kind { let source_info = this . source_info (expr . span) ; let region_scope = (region_scope , source_info) ; return this . in_scope (region_scope , lint_level , | this | { this . as_operand (block , scope , value , local_info , needs_temporary) }) ; } let category = Category :: of (& expr . kind) . unwrap () ; debug ! (? category , ? expr . kind) ; match category { Category :: Constant if matches ! (needs_temporary , NeedsTemporary :: No) || ! expr . ty . needs_drop (this . tcx , this . typing_env ()) => { let constant = this . as_constant (expr) ; block . and (Operand :: Constant (Box :: new (constant))) } Category :: Constant | Category :: Place | Category :: Rvalue (..) => { let operand = unpack ! (block = this . as_temp (block , scope , expr_id , Mutability :: Mut)) ; if ! matches ! (local_info , LocalInfo :: Boring) { let decl_info = this . local_decls [operand] . local_info . as_mut () . unwrap_crate_local () ; if let LocalInfo :: Boring | LocalInfo :: BlockTailTemp (_) = * * decl_info { * * decl_info = local_info ; } } block . and (Operand :: Move (Place :: from (operand))) } } } pub (crate) fn as_call_operand (& mut self , mut block : BasicBlock , scope : TempLifetime , expr_id : ExprId ,) -> BlockAnd < Operand < 'tcx > > { let this = self ; let expr = & this . thir [expr_id] ; debug ! ("as_call_operand(block={:?}, expr={:?})" , block , expr) ; if let ExprKind :: Scope { region_scope , lint_level , value } = expr . kind { let source_info = this . source_info (expr . span) ; let region_scope = (region_scope , source_info) ; return this . in_scope (region_scope , lint_level , | this | { this . as_call_operand (block , scope , value) }) ; } let tcx = this . tcx ; if tcx . features () . unsized_fn_params () { let ty = expr . ty ; if ! ty . is_sized (tcx , this . typing_env ()) { assert ! (! tcx . type_is_copy_modulo_regions (this . typing_env () , ty)) ; if let ExprKind :: Deref { arg } = expr . kind { let operand = unpack ! (block = this . as_temp (block , scope , arg , Mutability :: Mut)) ; let place = Place { local : operand , projection : tcx . mk_place_elems (& [PlaceElem :: Deref]) , } ; return block . and (Operand :: Move (place)) ; } } } this . as_operand (block , scope , expr_id , LocalInfo :: Boring , NeedsTemporary :: Maybe) } }
    };
}

impl_57!()