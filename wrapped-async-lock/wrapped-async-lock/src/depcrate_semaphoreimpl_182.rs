// Generated macro for impl_182 (impl)
macro_rules! Depcrate_semaphoreimpl_182 {
() => {
// Module: crate::semaphore
// Provides: {"impl_182"}
// Dependencies: {}
impl Semaphore { const_fn ! { const_if : # [cfg (not (loom))] ; # [doc = " Creates a new semaphore with a limit of `n` concurrent operations."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_lock::Semaphore;"] # [doc = ""] # [doc = " let s = Semaphore::new(5);"] # [doc = " ```"] pub const fn new (n : usize) -> Semaphore { Semaphore { count : AtomicUsize :: new (n) , event : Event :: new () , } } } # [doc = " Attempts to get a permit for a concurrent operation."] # [doc = ""] # [doc = " If the permit could not be acquired at this time, then [`None`] is returned. Otherwise, a"] # [doc = " guard is returned that releases the mutex when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_lock::Semaphore;"] # [doc = ""] # [doc = " let s = Semaphore::new(2);"] # [doc = ""] # [doc = " let g1 = s.try_acquire().unwrap();"] # [doc = " let g2 = s.try_acquire().unwrap();"] # [doc = ""] # [doc = " assert!(s.try_acquire().is_none());"] # [doc = " drop(g2);"] # [doc = " assert!(s.try_acquire().is_some());"] # [doc = " ```"] pub fn try_acquire (& self) -> Option < SemaphoreGuard < '_ > > { let mut count = self . count . load (Ordering :: Acquire) ; loop { if count == 0 { return None ; } match self . count . compare_exchange_weak (count , count - 1 , Ordering :: AcqRel , Ordering :: Acquire ,) { Ok (_) => return Some (SemaphoreGuard (self)) , Err (c) => count = c , } } } # [doc = " Waits for a permit for a concurrent operation."] # [doc = ""] # [doc = " Returns a guard that releases the permit when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_lock::Semaphore;"] # [doc = ""] # [doc = " let s = Semaphore::new(2);"] # [doc = " let guard = s.acquire().await;"] # [doc = " # });"] # [doc = " ```"] pub fn acquire (& self) -> Acquire < '_ > { Acquire :: _new (AcquireInner { semaphore : self , listener : None , _pin : PhantomPinned , }) } # [doc = " Waits for a permit for a concurrent operation."] # [doc = ""] # [doc = " Returns a guard that releases the permit when dropped."] # [doc = ""] # [doc = " # Blocking"] # [doc = ""] # [doc = " Rather than using asynchronous waiting, like the [`acquire`][Semaphore::acquire] method,"] # [doc = " this method will block the current thread until the permit is acquired."] # [doc = ""] # [doc = " This method should not be used in an asynchronous context. It is intended to be"] # [doc = " used in a way that a semaphore can be used in both asynchronous and synchronous contexts."] # [doc = " Calling this method in an asynchronous context may result in a deadlock."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_lock::Semaphore;"] # [doc = ""] # [doc = " let s = Semaphore::new(2);"] # [doc = " let guard = s.acquire_blocking();"] # [doc = " ```"] # [cfg (all (feature = "std" , not (target_family = "wasm")))] # [inline] pub fn acquire_blocking (& self) -> SemaphoreGuard < '_ > { self . acquire () . wait () } # [doc = " Attempts to get an owned permit for a concurrent operation."] # [doc = ""] # [doc = " If the permit could not be acquired at this time, then [`None`] is returned. Otherwise, an"] # [doc = " owned guard is returned that releases the mutex when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_lock::Semaphore;"] # [doc = " use std::sync::Arc;"] # [doc = ""] # [doc = " let s = Arc::new(Semaphore::new(2));"] # [doc = ""] # [doc = " let g1 = s.try_acquire_arc().unwrap();"] # [doc = " let g2 = s.try_acquire_arc().unwrap();"] # [doc = ""] # [doc = " assert!(s.try_acquire_arc().is_none());"] # [doc = " drop(g2);"] # [doc = " assert!(s.try_acquire_arc().is_some());"] # [doc = " ```"] pub fn try_acquire_arc (self : & Arc < Self >) -> Option < SemaphoreGuardArc > { let mut count = self . count . load (Ordering :: Acquire) ; loop { if count == 0 { return None ; } match self . count . compare_exchange_weak (count , count - 1 , Ordering :: AcqRel , Ordering :: Acquire ,) { Ok (_) => return Some (SemaphoreGuardArc (Some (self . clone ()))) , Err (c) => count = c , } } } # [doc = " Waits for an owned permit for a concurrent operation."] # [doc = ""] # [doc = " Returns a guard that releases the permit when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_lock::Semaphore;"] # [doc = " use std::sync::Arc;"] # [doc = ""] # [doc = " let s = Arc::new(Semaphore::new(2));"] # [doc = " let guard = s.acquire_arc().await;"] # [doc = " # });"] # [doc = " ```"] pub fn acquire_arc (self : & Arc < Self >) -> AcquireArc { AcquireArc :: _new (AcquireArcInner { semaphore : self . clone () , listener : None , _pin : PhantomPinned , }) } # [doc = " Waits for an owned permit for a concurrent operation."] # [doc = ""] # [doc = " Returns a guard that releases the permit when dropped."] # [doc = ""] # [doc = " # Blocking"] # [doc = ""] # [doc = " Rather than using asynchronous waiting, like the [`acquire_arc`][Semaphore::acquire_arc] method,"] # [doc = " this method will block the current thread until the permit is acquired."] # [doc = ""] # [doc = " This method should not be used in an asynchronous context. It is intended to be"] # [doc = " used in a way that a semaphore can be used in both asynchronous and synchronous contexts."] # [doc = " Calling this method in an asynchronous context may result in a deadlock."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::sync::Arc;"] # [doc = " use async_lock::Semaphore;"] # [doc = ""] # [doc = " let s = Arc::new(Semaphore::new(2));"] # [doc = " let guard = s.acquire_arc_blocking();"] # [doc = " ```"] # [cfg (all (feature = "std" , not (target_family = "wasm")))] # [inline] pub fn acquire_arc_blocking (self : & Arc < Self >) -> SemaphoreGuardArc { self . acquire_arc () . wait () } # [doc = " Adds `n` additional permits to the semaphore."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_lock::Semaphore;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let s = Semaphore::new(1);"] # [doc = ""] # [doc = " let _guard = s.acquire().await;"] # [doc = " assert!(s.try_acquire().is_none());"] # [doc = ""] # [doc = " s.add_permits(2);"] # [doc = ""] # [doc = " let _guard = s.acquire().await;"] # [doc = " let _guard = s.acquire().await;"] # [doc = " # });"] # [doc = " ```"] pub fn add_permits (& self , n : usize) { self . count . fetch_add (n , Ordering :: AcqRel) ; self . event . notify (n) ; } }
};
}
