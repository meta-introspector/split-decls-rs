// Generated macro for impl_39 (impl)
macro_rules! Depcrate_muteximpl_39 {
() => {
// Module: crate::mutex
// Provides: {"impl_39"}
// Dependencies: {}
impl < T : ? Sized > Mutex < T > { # [doc = " Acquires the mutex and clones a reference to it."] # [doc = ""] # [doc = " Returns an owned guard that releases the mutex when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_lock::Mutex;"] # [doc = " use std::sync::Arc;"] # [doc = ""] # [doc = " let mutex = Arc::new(Mutex::new(10));"] # [doc = " let guard = mutex.lock_arc().await;"] # [doc = " assert_eq!(*guard, 10);"] # [doc = " # })"] # [doc = " ```"] # [inline] pub fn lock_arc (self : & Arc < Self >) -> LockArc < T > { LockArc :: _new (LockArcInnards :: Unpolled { mutex : Some (self . clone ()) , }) } # [doc = " Acquires the mutex and clones a reference to it using the blocking strategy."] # [doc = ""] # [doc = " Returns an owned guard that releases the mutex when dropped."] # [doc = ""] # [doc = " # Blocking"] # [doc = ""] # [doc = " Rather than using asynchronous waiting, like the [`lock_arc`][Mutex::lock_arc] method,"] # [doc = " this method will block the current thread until the lock is acquired."] # [doc = ""] # [doc = " This method should not be used in an asynchronous context. It is intended to be"] # [doc = " used in a way that a mutex can be used in both asynchronous and synchronous contexts."] # [doc = " Calling this method in an asynchronous context may result in a deadlock."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_lock::Mutex;"] # [doc = " use std::sync::Arc;"] # [doc = ""] # [doc = " let mutex = Arc::new(Mutex::new(10));"] # [doc = " let guard = mutex.lock_arc_blocking();"] # [doc = " assert_eq!(*guard, 10);"] # [doc = " ```"] # [cfg (all (feature = "std" , not (target_family = "wasm")))] # [inline] pub fn lock_arc_blocking (self : & Arc < Self >) -> MutexGuardArc < T > { self . lock_arc () . wait () } # [doc = " Attempts to acquire the mutex and clone a reference to it."] # [doc = ""] # [doc = " If the mutex could not be acquired at this time, then [`None`] is returned. Otherwise, an"] # [doc = " owned guard is returned that releases the mutex when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_lock::Mutex;"] # [doc = " use std::sync::Arc;"] # [doc = ""] # [doc = " let mutex = Arc::new(Mutex::new(10));"] # [doc = " if let Some(guard) = mutex.try_lock() {"] # [doc = "     assert_eq!(*guard, 10);"] # [doc = " }"] # [doc = " # ;"] # [doc = " ```"] # [inline] pub fn try_lock_arc (self : & Arc < Self >) -> Option < MutexGuardArc < T > > { if self . state . compare_exchange (0 , 1 , Ordering :: Acquire , Ordering :: Acquire) . is_ok () { Some (MutexGuardArc (self . clone ())) } else { None } } }
};
}
