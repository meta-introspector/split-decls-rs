// Generated macro for impl_38 (impl)
macro_rules! Depcrate_muteximpl_38 {
() => {
// Module: crate::mutex
// Provides: {"impl_38"}
// Dependencies: {}
impl < T : ? Sized > Mutex < T > { # [doc = " Acquires the mutex."] # [doc = ""] # [doc = " Returns a guard that releases the mutex when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_lock::Mutex;"] # [doc = ""] # [doc = " let mutex = Mutex::new(10);"] # [doc = " let guard = mutex.lock().await;"] # [doc = " assert_eq!(*guard, 10);"] # [doc = " # })"] # [doc = " ```"] # [inline] pub fn lock (& self) -> Lock < '_ , T > { Lock :: _new (LockInner { mutex : self , acquire_slow : None , }) } # [doc = " Acquires the mutex using the blocking strategy."] # [doc = ""] # [doc = " Returns a guard that releases the mutex when dropped."] # [doc = ""] # [doc = " # Blocking"] # [doc = ""] # [doc = " Rather than using asynchronous waiting, like the [`lock`][Mutex::lock] method,"] # [doc = " this method will block the current thread until the lock is acquired."] # [doc = ""] # [doc = " This method should not be used in an asynchronous context. It is intended to be"] # [doc = " used in a way that a mutex can be used in both asynchronous and synchronous contexts."] # [doc = " Calling this method in an asynchronous context may result in a deadlock."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_lock::Mutex;"] # [doc = ""] # [doc = " let mutex = Mutex::new(10);"] # [doc = " let guard = mutex.lock_blocking();"] # [doc = " assert_eq!(*guard, 10);"] # [doc = " ```"] # [cfg (all (feature = "std" , not (target_family = "wasm")))] # [inline] pub fn lock_blocking (& self) -> MutexGuard < '_ , T > { self . lock () . wait () } # [doc = " Attempts to acquire the mutex."] # [doc = ""] # [doc = " If the mutex could not be acquired at this time, then [`None`] is returned. Otherwise, a"] # [doc = " guard is returned that releases the mutex when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_lock::Mutex;"] # [doc = ""] # [doc = " let mutex = Mutex::new(10);"] # [doc = " if let Some(guard) = mutex.try_lock() {"] # [doc = "     assert_eq!(*guard, 10);"] # [doc = " }"] # [doc = " # ;"] # [doc = " ```"] # [inline] pub fn try_lock (& self) -> Option < MutexGuard < '_ , T > > { if self . state . compare_exchange (0 , 1 , Ordering :: Acquire , Ordering :: Acquire) . is_ok () { Some (MutexGuard (self)) } else { None } } # [doc = " Returns a mutable reference to the underlying data."] # [doc = ""] # [doc = " Since this call borrows the mutex mutably, no actual locking takes place -- the mutable"] # [doc = " borrow statically guarantees the mutex is not already acquired."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_lock::Mutex;"] # [doc = ""] # [doc = " let mut mutex = Mutex::new(0);"] # [doc = " *mutex.get_mut() = 10;"] # [doc = " assert_eq!(*mutex.lock().await, 10);"] # [doc = " # })"] # [doc = " ```"] pub fn get_mut (& mut self) -> & mut T { self . data . get_mut () } # [doc = " Unlocks the mutex directly."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is intended to be used only in the case where the mutex is locked,"] # [doc = " and the guard is subsequently forgotten. Calling this while you don't hold a lock"] # [doc = " on the mutex will likely lead to UB."] pub (crate) unsafe fn unlock_unchecked (& self) { self . state . fetch_sub (1 , Ordering :: Release) ; self . lock_ops . notify (1) ; } }
};
}
