// Generated macro for impl_116 (impl)
macro_rules! Depcrate_rwlockimpl_116 {
() => {
// Module: crate::rwlock
// Provides: {"impl_116"}
// Dependencies: {}
impl < T : ? Sized > RwLock < T > { # [doc = " Attempts to acquire a read lock."] # [doc = ""] # [doc = " If a read lock could not be acquired at this time, then [`None`] is returned. Otherwise, a"] # [doc = " guard is returned that releases the lock when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_lock::RwLock;"] # [doc = ""] # [doc = " let lock = RwLock::new(1);"] # [doc = ""] # [doc = " let reader = lock.read().await;"] # [doc = " assert_eq!(*reader, 1);"] # [doc = ""] # [doc = " assert!(lock.try_read().is_some());"] # [doc = " # })"] # [doc = " ```"] # [inline] pub fn try_read (& self) -> Option < RwLockReadGuard < '_ , T > > { if self . raw . try_read () { Some (RwLockReadGuard { lock : & self . raw , value : self . value . get () , }) } else { None } } # [doc = " Acquires a read lock."] # [doc = ""] # [doc = " Returns a guard that releases the lock when dropped."] # [doc = ""] # [doc = " Note that attempts to acquire a read lock will block if there are also concurrent attempts"] # [doc = " to acquire a write lock."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_lock::RwLock;"] # [doc = ""] # [doc = " let lock = RwLock::new(1);"] # [doc = ""] # [doc = " let reader = lock.read().await;"] # [doc = " assert_eq!(*reader, 1);"] # [doc = ""] # [doc = " assert!(lock.try_read().is_some());"] # [doc = " # })"] # [doc = " ```"] # [inline] pub fn read (& self) -> Read < '_ , T > { Read :: new (self . raw . read () , self . value . get ()) } # [doc = " Acquires a read lock."] # [doc = ""] # [doc = " Returns a guard that releases the lock when dropped."] # [doc = ""] # [doc = " Note that attempts to acquire a read lock will block if there are also concurrent attempts"] # [doc = " to acquire a write lock."] # [doc = ""] # [doc = " # Blocking"] # [doc = ""] # [doc = " Rather than using asynchronous waiting, like the [`read`][`RwLock::read`] method,"] # [doc = " this method will block the current thread until the read lock is acquired."] # [doc = ""] # [doc = " This method should not be used in an asynchronous context. It is intended to be"] # [doc = " used in a way that a lock can be used in both asynchronous and synchronous contexts."] # [doc = " Calling this method in an asynchronous context may result in a deadlock."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_lock::RwLock;"] # [doc = ""] # [doc = " let lock = RwLock::new(1);"] # [doc = ""] # [doc = " let reader = lock.read_blocking();"] # [doc = " assert_eq!(*reader, 1);"] # [doc = ""] # [doc = " assert!(lock.try_read().is_some());"] # [doc = " ```"] # [cfg (all (feature = "std" , not (target_family = "wasm")))] # [inline] pub fn read_blocking (& self) -> RwLockReadGuard < '_ , T > { self . read () . wait () } # [doc = " Attempts to acquire a read lock with the possibility to upgrade to a write lock."] # [doc = ""] # [doc = " If a read lock could not be acquired at this time, then [`None`] is returned. Otherwise, a"] # [doc = " guard is returned that releases the lock when dropped."] # [doc = ""] # [doc = " Upgradable read lock reserves the right to be upgraded to a write lock, which means there"] # [doc = " can be at most one upgradable read lock at a time."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_lock::{RwLock, RwLockUpgradableReadGuard};"] # [doc = ""] # [doc = " let lock = RwLock::new(1);"] # [doc = ""] # [doc = " let reader = lock.upgradable_read().await;"] # [doc = " assert_eq!(*reader, 1);"] # [doc = " assert_eq!(*lock.try_read().unwrap(), 1);"] # [doc = ""] # [doc = " let mut writer = RwLockUpgradableReadGuard::upgrade(reader).await;"] # [doc = " *writer = 2;"] # [doc = " # })"] # [doc = " ```"] # [inline] pub fn try_upgradable_read (& self) -> Option < RwLockUpgradableReadGuard < '_ , T > > { if self . raw . try_upgradable_read () { Some (RwLockUpgradableReadGuard { lock : & self . raw , value : self . value . get () , }) } else { None } } # [doc = " Acquires a read lock with the possibility to upgrade to a write lock."] # [doc = ""] # [doc = " Returns a guard that releases the lock when dropped."] # [doc = ""] # [doc = " Upgradable read lock reserves the right to be upgraded to a write lock, which means there"] # [doc = " can be at most one upgradable read lock at a time."] # [doc = ""] # [doc = " Note that attempts to acquire an upgradable read lock will block if there are concurrent"] # [doc = " attempts to acquire another upgradable read lock or a write lock."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_lock::{RwLock, RwLockUpgradableReadGuard};"] # [doc = ""] # [doc = " let lock = RwLock::new(1);"] # [doc = ""] # [doc = " let reader = lock.upgradable_read().await;"] # [doc = " assert_eq!(*reader, 1);"] # [doc = " assert_eq!(*lock.try_read().unwrap(), 1);"] # [doc = ""] # [doc = " let mut writer = RwLockUpgradableReadGuard::upgrade(reader).await;"] # [doc = " *writer = 2;"] # [doc = " # })"] # [doc = " ```"] # [inline] pub fn upgradable_read (& self) -> UpgradableRead < '_ , T > { UpgradableRead :: new (self . raw . upgradable_read () , self . value . get ()) } # [doc = " Attempts to acquire a read lock with the possibility to upgrade to a write lock."] # [doc = ""] # [doc = " Returns a guard that releases the lock when dropped."] # [doc = ""] # [doc = " Upgradable read lock reserves the right to be upgraded to a write lock, which means there"] # [doc = " can be at most one upgradable read lock at a time."] # [doc = ""] # [doc = " Note that attempts to acquire an upgradable read lock will block if there are concurrent"] # [doc = " attempts to acquire another upgradable read lock or a write lock."] # [doc = ""] # [doc = " # Blocking"] # [doc = ""] # [doc = " Rather than using asynchronous waiting, like the [`upgradable_read`][`RwLock::upgradable_read`]"] # [doc = " method, this method will block the current thread until the read lock is acquired."] # [doc = ""] # [doc = " This method should not be used in an asynchronous context. It is intended to be"] # [doc = " used in a way that a lock can be used in both asynchronous and synchronous contexts."] # [doc = " Calling this method in an asynchronous context may result in a deadlock."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_lock::{RwLock, RwLockUpgradableReadGuard};"] # [doc = ""] # [doc = " let lock = RwLock::new(1);"] # [doc = ""] # [doc = " let reader = lock.upgradable_read_blocking();"] # [doc = " assert_eq!(*reader, 1);"] # [doc = " assert_eq!(*lock.try_read().unwrap(), 1);"] # [doc = ""] # [doc = " let mut writer = RwLockUpgradableReadGuard::upgrade_blocking(reader);"] # [doc = " *writer = 2;"] # [doc = " ```"] # [cfg (all (feature = "std" , not (target_family = "wasm")))] # [inline] pub fn upgradable_read_blocking (& self) -> RwLockUpgradableReadGuard < '_ , T > { self . upgradable_read () . wait () } # [doc = " Attempts to acquire an owned, reference-counted read lock"] # [doc = " with the possibility to upgrade to a write lock."] # [doc = ""] # [doc = " Returns a guard that releases the lock when dropped."] # [doc = ""] # [doc = " Upgradable read lock reserves the right to be upgraded to a write lock, which means there"] # [doc = " can be at most one upgradable read lock at a time."] # [doc = ""] # [doc = " Note that attempts to acquire an upgradable read lock will block if there are concurrent"] # [doc = " attempts to acquire another upgradable read lock or a write lock."] # [doc = ""] # [doc = " # Blocking"] # [doc = ""] # [doc = " Rather than using asynchronous waiting, like the [`upgradable_read_arc`][`RwLock::upgradable_read_arc`]"] # [doc = " method, this method will block the current thread until the read lock is acquired."] # [doc = ""] # [doc = " This method should not be used in an asynchronous context. It is intended to be"] # [doc = " used in a way that a lock can be used in both asynchronous and synchronous contexts."] # [doc = " Calling this method in an asynchronous context may result in a deadlock."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::sync::Arc;"] # [doc = " use async_lock::{RwLock, RwLockUpgradableReadGuardArc};"] # [doc = ""] # [doc = " let lock = Arc::new(RwLock::new(1));"] # [doc = ""] # [doc = " let reader = lock.upgradable_read_arc_blocking();"] # [doc = " assert_eq!(*reader, 1);"] # [doc = " assert_eq!(*lock.try_read().unwrap(), 1);"] # [doc = ""] # [doc = " let mut writer = RwLockUpgradableReadGuardArc::upgrade_blocking(reader);"] # [doc = " *writer = 2;"] # [doc = " ```"] # [cfg (all (feature = "std" , not (target_family = "wasm")))] # [inline] pub fn upgradable_read_arc_blocking (self : & Arc < Self >) -> RwLockUpgradableReadGuardArc < T > { self . upgradable_read_arc () . wait () } # [doc = " Attempts to acquire an owned, reference-counted read lock with the possibility to"] # [doc = " upgrade to a write lock."] # [doc = ""] # [doc = " If a read lock could not be acquired at this time, then [`None`] is returned. Otherwise, a"] # [doc = " guard is returned that releases the lock when dropped."] # [doc = ""] # [doc = " Upgradable read lock reserves the right to be upgraded to a write lock, which means there"] # [doc = " can be at most one upgradable read lock at a time."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use std::sync::Arc;"] # [doc = " use async_lock::{RwLock, RwLockUpgradableReadGuardArc};"] # [doc = ""] # [doc = " let lock = Arc::new(RwLock::new(1));"] # [doc = ""] # [doc = " let reader = lock.upgradable_read_arc().await;"] # [doc = " assert_eq!(*reader, 1);"] # [doc = " assert_eq!(*lock.try_read_arc().unwrap(), 1);"] # [doc = ""] # [doc = " let mut writer = RwLockUpgradableReadGuardArc::upgrade(reader).await;"] # [doc = " *writer = 2;"] # [doc = " # })"] # [doc = " ```"] # [inline] pub fn try_upgradable_read_arc (self : & Arc < Self >) -> Option < RwLockUpgradableReadGuardArc < T > > { if self . raw . try_upgradable_read () { Some (RwLockUpgradableReadGuardArc { lock : self . clone () }) } else { None } } # [doc = " Acquires an owned, reference-counted read lock with the possibility"] # [doc = " to upgrade to a write lock."] # [doc = ""] # [doc = " Returns a guard that releases the lock when dropped."] # [doc = ""] # [doc = " Upgradable read lock reserves the right to be upgraded to a write lock, which means there"] # [doc = " can be at most one upgradable read lock at a time."] # [doc = ""] # [doc = " Note that attempts to acquire an upgradable read lock will block if there are concurrent"] # [doc = " attempts to acquire another upgradable read lock or a write lock."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use std::sync::Arc;"] # [doc = " use async_lock::{RwLock, RwLockUpgradableReadGuardArc};"] # [doc = ""] # [doc = " let lock = Arc::new(RwLock::new(1));"] # [doc = ""] # [doc = " let reader = lock.upgradable_read_arc().await;"] # [doc = " assert_eq!(*reader, 1);"] # [doc = " assert_eq!(*lock.try_read_arc().unwrap(), 1);"] # [doc = ""] # [doc = " let mut writer = RwLockUpgradableReadGuardArc::upgrade(reader).await;"] # [doc = " *writer = 2;"] # [doc = " # })"] # [doc = " ```"] # [inline] pub fn upgradable_read_arc < 'a > (self : & 'a Arc < Self >) -> UpgradableReadArc < 'a , T > { UpgradableReadArc :: new (self . raw . upgradable_read () , self) } # [doc = " Attempts to acquire a write lock."] # [doc = ""] # [doc = " If a write lock could not be acquired at this time, then [`None`] is returned. Otherwise, a"] # [doc = " guard is returned that releases the lock when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_lock::RwLock;"] # [doc = ""] # [doc = " let lock = RwLock::new(1);"] # [doc = ""] # [doc = " assert!(lock.try_write().is_some());"] # [doc = " let reader = lock.read().await;"] # [doc = " assert!(lock.try_write().is_none());"] # [doc = " # })"] # [doc = " ```"] # [inline] pub fn try_write (& self) -> Option < RwLockWriteGuard < '_ , T > > { if self . raw . try_write () { Some (RwLockWriteGuard { lock : & self . raw , value : self . value . get () , }) } else { None } } # [doc = " Acquires a write lock."] # [doc = ""] # [doc = " Returns a guard that releases the lock when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_lock::RwLock;"] # [doc = ""] # [doc = " let lock = RwLock::new(1);"] # [doc = ""] # [doc = " let writer = lock.write().await;"] # [doc = " assert!(lock.try_read().is_none());"] # [doc = " # })"] # [doc = " ```"] # [inline] pub fn write (& self) -> Write < '_ , T > { Write :: new (self . raw . write () , self . value . get ()) } # [doc = " Acquires a write lock."] # [doc = ""] # [doc = " Returns a guard that releases the lock when dropped."] # [doc = ""] # [doc = " # Blocking"] # [doc = ""] # [doc = " Rather than using asynchronous waiting, like the [`write`] method, this method will"] # [doc = " block the current thread until the write lock is acquired."] # [doc = ""] # [doc = " This method should not be used in an asynchronous context. It is intended to be"] # [doc = " used in a way that a lock can be used in both asynchronous and synchronous contexts."] # [doc = " Calling this method in an asynchronous context may result in a deadlock."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_lock::RwLock;"] # [doc = ""] # [doc = " let lock = RwLock::new(1);"] # [doc = ""] # [doc = " let writer = lock.write_blocking();"] # [doc = " assert!(lock.try_read().is_none());"] # [doc = " ```"] # [cfg (all (feature = "std" , not (target_family = "wasm")))] # [inline] pub fn write_blocking (& self) -> RwLockWriteGuard < '_ , T > { self . write () . wait () } # [doc = " Attempts to acquire an owned, reference-counted write lock."] # [doc = ""] # [doc = " If a write lock could not be acquired at this time, then [`None`] is returned. Otherwise, a"] # [doc = " guard is returned that releases the lock when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use std::sync::Arc;"] # [doc = " use async_lock::RwLock;"] # [doc = ""] # [doc = " let lock = Arc::new(RwLock::new(1));"] # [doc = ""] # [doc = " assert!(lock.try_write_arc().is_some());"] # [doc = " let reader = lock.read_arc().await;"] # [doc = " assert!(lock.try_write_arc().is_none());"] # [doc = " # })"] # [doc = " ```"] # [inline] pub fn try_write_arc (self : & Arc < Self >) -> Option < RwLockWriteGuardArc < T > > { if self . raw . try_write () { Some (RwLockWriteGuardArc { lock : self . clone () }) } else { None } } # [doc = " Acquires an owned, reference-counted write lock."] # [doc = ""] # [doc = " Returns a guard that releases the lock when dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use std::sync::Arc;"] # [doc = " use async_lock::RwLock;"] # [doc = ""] # [doc = " let lock = Arc::new(RwLock::new(1));"] # [doc = ""] # [doc = " let writer = lock.write_arc().await;"] # [doc = " assert!(lock.try_read_arc().is_none());"] # [doc = " # })"] # [doc = " ```"] # [inline] pub fn write_arc < 'a > (self : & 'a Arc < Self >) -> WriteArc < 'a , T > { WriteArc :: new (self . raw . write () , self) } # [doc = " Acquires an owned, reference-counted write lock."] # [doc = ""] # [doc = " Returns a guard that releases the lock when dropped."] # [doc = ""] # [doc = " # Blocking"] # [doc = ""] # [doc = " Rather than using asynchronous waiting, like the [`write_arc`][RwLock::write_arc] method, this method will"] # [doc = " block the current thread until the write lock is acquired."] # [doc = ""] # [doc = " This method should not be used in an asynchronous context. It is intended to be"] # [doc = " used in a way that a lock can be used in both asynchronous and synchronous contexts."] # [doc = " Calling this method in an asynchronous context may result in a deadlock."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::sync::Arc;"] # [doc = " use async_lock::RwLock;"] # [doc = ""] # [doc = " let lock = Arc::new(RwLock::new(1));"] # [doc = ""] # [doc = " let writer = lock.write_arc_blocking();"] # [doc = " assert!(lock.try_read().is_none());"] # [doc = " ```"] # [cfg (all (feature = "std" , not (target_family = "wasm")))] # [inline] pub fn write_arc_blocking (self : & Arc < Self >) -> RwLockWriteGuardArc < T > { self . write_arc () . wait () } # [doc = " Returns a mutable reference to the inner value."] # [doc = ""] # [doc = " Since this call borrows the lock mutably, no actual locking takes place. The mutable borrow"] # [doc = " statically guarantees no locks exist."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_lock::RwLock;"] # [doc = ""] # [doc = " let mut lock = RwLock::new(1);"] # [doc = ""] # [doc = " *lock.get_mut() = 2;"] # [doc = " assert_eq!(*lock.read().await, 2);"] # [doc = " # })"] # [doc = " ```"] # [must_use] # [inline] pub fn get_mut (& mut self) -> & mut T { unsafe { & mut * self . value . get () } } }
};
}
