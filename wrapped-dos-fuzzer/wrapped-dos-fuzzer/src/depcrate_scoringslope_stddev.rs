// Generated macro for slope_stddev (function)
macro_rules! Depcrate_scoringslope_stddev {
() => {
// Module: crate::scoring
// Provides: {"slope_stddev"}
// Dependencies: {}
# [doc = " Calculates the stdandard deviation of the slopes between all combinations of points."] # [doc = ""] # [doc = " We interpret the `(len, time)`-tuples of the array as `(x, y)`-coordinates / points on a graph."] # [doc = " Each tuple in the array is one point / coordinate."] # [doc = " If the time increase is perfectly linear, the slopes of the line running through each two"] # [doc = " adjacent points are all equal."] # [doc = " In fact the slope between any two points from the array will be the same."] # [doc = " If all slopes are the same, the standard deviation of all of those slopes will be zero."] # [doc = ""] # [doc = " However, in the real world there are inconsistencies, deviations, rounding and outliers."] # [doc = " As such, the points won't be on a perfect line."] # [doc = " Nevertheless, if it's linear, the slopes will be close enough to each other, that small outliers"] # [doc = " won't increase the standard deviation too much."] # [doc = " The more points we add and the more slopes we add, the more resilient to outliers the result"] # [doc = " will become. That's also why we don't only take into account the slopes between neighboring"] # [doc = " points (`A-B`, `B-C`, `C-D`), but the slopes between any two points (`A-B`, `A-C`, `A-D`,"] # [doc = " `B-C`, `B-D`, `C-D`)."] # [doc = ""] # [doc = " If the function is not linear, the slopes will all be different, resulting in a higher stddev."] # [allow (dead_code)] pub fn slope_stddev (time_samples : & [(f64 , f64)]) -> (f64 , bool) { let mut slopes = [0f64 ; SAMPLE_SIZE * 2 * (SAMPLE_SIZE * 2 - 1) / 2] ; let mut i = 0 ; for (point1 , point2) in (0 .. time_samples . len ()) . tuple_combinations () { let (x1 , y1) = time_samples [point1] ; let (x2 , y2) = time_samples [point2] ; let dx = x2 - x1 ; let dy = y2 - y1 ; let slope = dy / dx ; slopes [i] = slope ; i += 1 ; } let slopes = & mut slopes [.. i] ; if super :: DEBUG_LEVEL >= 2 { println ! ("slopes: {:?}" , slopes) ; } let mut time_samples = ArrayViewMut1 :: from (slopes) ; let mean = time_samples . sum () / time_samples . len () as f64 ; time_samples . mapv_inplace (| slope | (slope - mean) . powi (2)) ; let sum = time_samples . sum () ; let in_sqrt = sum / time_samples . len () as f64 ; let stddev = in_sqrt . sqrt () ; (stddev , stddev > super :: ACCEPTANCE_STDDEV) }
};
}
