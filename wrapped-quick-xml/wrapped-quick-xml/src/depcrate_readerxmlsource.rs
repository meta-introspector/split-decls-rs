// Generated macro for XmlSource (trait)
macro_rules! Depcrate_readerXmlSource {
() => {
// Module: crate::reader
// Provides: {"XmlSource"}
// Dependencies: {}
# [doc = " Represents an input for a reader that can return borrowed data."] # [doc = ""] # [doc = " There are two implementors of this trait: generic one that read data from"] # [doc = " `Self`, copies some part of it into a provided buffer of type `B` and then"] # [doc = " returns data that borrow from that buffer."] # [doc = ""] # [doc = " The other implementor is for `&[u8]` and instead of copying data returns"] # [doc = " borrowed data from `Self` instead. This implementation allows zero-copy"] # [doc = " deserialization."] # [doc = ""] # [doc = " # Parameters"] # [doc = " - `'r`: lifetime of a buffer from which events will borrow"] # [doc = " - `B`: a type of a buffer that can be used to store data read from `Self` and"] # [doc = "   from which events can borrow"] trait XmlSource < 'r , B > { # [doc = " Removes UTF-8 BOM if it is present"] # [cfg (not (feature = "encoding"))] fn remove_utf8_bom (& mut self) -> io :: Result < () > ; # [doc = " Determines encoding from the start of input and removes BOM if it is present"] # [cfg (feature = "encoding")] fn detect_encoding (& mut self) -> io :: Result < Option < & 'static Encoding > > ; # [doc = " Read input until start of markup (the `<`) is found, start of general entity"] # [doc = " reference (the `&`) is found or end of input is reached."] # [doc = ""] # [doc = " # Parameters"] # [doc = " - `buf`: Buffer that could be filled from an input (`Self`) and"] # [doc = "   from which [events] could borrow their data"] # [doc = " - `position`: Will be increased by amount of bytes consumed"] # [doc = ""] # [doc = " [events]: crate::events::Event"] fn read_text (& mut self , buf : B , position : & mut u64) -> ReadTextResult < 'r , B > ; # [doc = " Read input until end of general reference (the `;`) is found, start of"] # [doc = " another general reference (the `&`) is found or end of input is reached."] # [doc = ""] # [doc = " This method must be called when current character is `&`."] # [doc = ""] # [doc = " # Parameters"] # [doc = " - `buf`: Buffer that could be filled from an input (`Self`) and"] # [doc = "   from which [events] could borrow their data"] # [doc = " - `position`: Will be increased by amount of bytes consumed"] # [doc = ""] # [doc = " [events]: crate::events::Event"] fn read_ref (& mut self , buf : B , position : & mut u64) -> ReadRefResult < 'r > ; # [doc = " Read input until processing instruction is finished."] # [doc = ""] # [doc = " This method expect that start sequence of a parser already was read."] # [doc = ""] # [doc = " Returns a slice of data read up to the end of the thing being parsed."] # [doc = " The end of thing and the returned content is determined by the used parser."] # [doc = ""] # [doc = " If input (`Self`) is exhausted and no bytes was read, or if the specified"] # [doc = " parser could not find the ending sequence of the thing, returns `SyntaxError`."] # [doc = ""] # [doc = " # Parameters"] # [doc = " - `buf`: Buffer that could be filled from an input (`Self`) and"] # [doc = "   from which [events] could borrow their data"] # [doc = " - `position`: Will be increased by amount of bytes consumed"] # [doc = ""] # [doc = " A `P` type parameter is used to preserve state between calls to the underlying"] # [doc = " reader which provides bytes fed into the parser."] # [doc = ""] # [doc = " [events]: crate::events::Event"] fn read_with < P > (& mut self , parser : P , buf : B , position : & mut u64) -> Result < & 'r [u8] , Error > where P : Parser ; # [doc = " Read input until comment or CDATA is finished."] # [doc = ""] # [doc = " This method expect that `<` already was read."] # [doc = ""] # [doc = " Returns a slice of data read up to end of comment or CDATA (`>`),"] # [doc = " which does not include into result."] # [doc = ""] # [doc = " If input (`Self`) is exhausted and nothing was read, returns `None`."] # [doc = ""] # [doc = " # Parameters"] # [doc = " - `buf`: Buffer that could be filled from an input (`Self`) and"] # [doc = "   from which [events] could borrow their data"] # [doc = " - `position`: Will be increased by amount of bytes consumed"] # [doc = ""] # [doc = " [events]: crate::events::Event"] fn read_bang_element (& mut self , buf : B , position : & mut u64 ,) -> Result < (BangType , & 'r [u8]) , Error > ; # [doc = " Consume and discard all the whitespace until the next non-whitespace"] # [doc = " character or EOF."] # [doc = ""] # [doc = " # Parameters"] # [doc = " - `position`: Will be increased by amount of bytes consumed"] fn skip_whitespace (& mut self , position : & mut u64) -> io :: Result < () > ; # [doc = " Return one character without consuming it, so that future `read_*` calls"] # [doc = " will still include it. On EOF, return `None`."] fn peek_one (& mut self) -> io :: Result < Option < u8 > > ; }
};
}
