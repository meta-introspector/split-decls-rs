// Generated macro for ElementParser (enum)
macro_rules! Depcrate_parser_elementElementParser {
() => {
// Module: crate::parser::element
// Provides: {"ElementParser"}
// Dependencies: {}
# [doc = " A parser that search a `>` symbol in the slice outside of quoted regions."] # [doc = ""] # [doc = " The parser considers two quoted regions: a double-quoted (`\"...\"`) and"] # [doc = " a single-quoted (`'...'`) region. Matches found inside those regions are not"] # [doc = " considered as results. Each region starts and ends by its quote symbol,"] # [doc = " which cannot be escaped (but can be encoded as XML character entity or named"] # [doc = " entity. Anyway, that encoding does not contain literal quotes)."] # [doc = ""] # [doc = " To use a parser create an instance of parser and [`feed`] data into it."] # [doc = " After successful search the parser will return [`Some`] with position of"] # [doc = " found symbol. If search is unsuccessful, a [`None`] will be returned. You"] # [doc = " typically would expect positive result of search, so that you should feed"] # [doc = " new data until you get it."] # [doc = ""] # [doc = " NOTE: after successful match the parser does not returned to the initial"] # [doc = " state and should not be used anymore. Create a new parser if you want to perform"] # [doc = " new search."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use pretty_assertions::assert_eq;"] # [doc = " use quick_xml::parser::{ElementParser, Parser};"] # [doc = ""] # [doc = " let mut parser = ElementParser::default();"] # [doc = ""] # [doc = " // Parse `<my-element  with = 'some > inside'>and the text follow...`"] # [doc = " // splitted into three chunks"] # [doc = " assert_eq!(parser.feed(b\"<my-element\"), None);"] # [doc = " // ...get new chunk of data"] # [doc = " assert_eq!(parser.feed(b\" with = 'some >\"), None);"] # [doc = " // ...get another chunk of data"] # [doc = " assert_eq!(parser.feed(b\" inside'>and the text follow...\"), Some(8));"] # [doc = " //                       ^       ^"] # [doc = " //                       0       8"] # [doc = " ```"] # [doc = ""] # [doc = " [`feed`]: Self::feed()"] # [derive (Clone , Copy , Debug , Eq , PartialEq)] pub enum ElementParser { # [doc = " The initial state (inside element, but outside of attribute value)."] Outside , # [doc = " Inside a single-quoted region (`'...'`)."] SingleQ , # [doc = " Inside a double-quoted region (`\"...\"`)."] DoubleQ , }
};
}
