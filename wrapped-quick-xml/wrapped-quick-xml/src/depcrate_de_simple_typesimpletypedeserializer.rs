// Generated macro for SimpleTypeDeserializer (struct)
macro_rules! Depcrate_de_simple_typeSimpleTypeDeserializer {
() => {
// Module: crate::de::simple_type
// Provides: {"SimpleTypeDeserializer"}
// Dependencies: {}
# [doc = " A deserializer for an xml probably escaped and encoded value of XSD [simple types]."] # [doc = " This deserializer will borrow from the input as much as possible."] # [doc = ""] # [doc = " `deserialize_any()` returns the whole string that deserializer contains."] # [doc = ""] # [doc = " Escaping the value is actually not always necessary, for instance when"] # [doc = " converting to a float, we don't expect any escapable character anyway."] # [doc = " In that cases deserializer skips unescaping step."] # [doc = ""] # [doc = " Used for deserialize values from:"] # [doc = " - attribute values (`<... ...=\"value\" ...>`)"] # [doc = " - mixed text / CDATA content (`<...>text<![CDATA[cdata]]></...>`)"] # [doc = ""] # [doc = " This deserializer processes items as following:"] # [doc = " - numbers are parsed from a text content using [`FromStr`]; in case of error"] # [doc = "   [`Visitor::visit_borrowed_str`], [`Visitor::visit_str`], or [`Visitor::visit_string`]"] # [doc = "   is called; it is responsibility of the type to return an error if it does"] # [doc = "   not able to process passed data;"] # [doc = " - booleans converted from the text according to the XML [specification]:"] # [doc = "   - `\"true\"` and `\"1\"` converted to `true`;"] # [doc = "   - `\"false\"` and `\"0\"` converted to `false`;"] # [doc = "   - everything else calls [`Visitor::visit_borrowed_str`], [`Visitor::visit_str`],"] # [doc = "     or [`Visitor::visit_string`]; it is responsibility of the type to return"] # [doc = "     an error if it does not able to process passed data;"] # [doc = " - strings returned as is;"] # [doc = " - characters also returned as strings. If string contain more than one character"] # [doc = "   or empty, it is responsibility of a type to return an error;"] # [doc = " - `Option` always deserialized as `Some` using the same deserializer."] # [doc = "   If attribute or text content is missed, then the deserializer even wouldn't"] # [doc = "   be used, so if it is used, then the value should be;"] # [doc = " - units (`()`) and unit structs always deserialized successfully, the content is ignored;"] # [doc = " - newtype structs forwards deserialization to the inner type using the same"] # [doc = "   deserializer;"] # [doc = " - sequences, tuples and tuple structs are deserialized as `xs:list`s. Only"] # [doc = "   sequences of primitive types is possible to deserialize this way and they"] # [doc = "   should be delimited by a space (` `, `\\t`, `\\r`, or `\\n`);"] # [doc = " - structs and maps delegates to [`Self::deserialize_str`] which calls"] # [doc = "   [`Visitor::visit_borrowed_str`] or [`Visitor::visit_string`]; it is responsibility"] # [doc = "   of the type to return an error if it does not able to process passed data;"] # [doc = " - enums:"] # [doc = "   - the variant name is deserialized using the same deserializer;"] # [doc = "   - the content is deserialized using the deserializer that always returns unit (`()`):"] # [doc = "     - unit variants: just return `()`;"] # [doc = "     - newtype variants: deserialize from [`UnitDeserializer`];"] # [doc = "     - tuple and struct variants: call [`Visitor::visit_unit`];"] # [doc = " - identifiers are deserialized as strings."] # [doc = ""] # [doc = " [simple types]: https://www.w3.org/TR/xmlschema11-1/#Simple_Type_Definition"] # [doc = " [`FromStr`]: std::str::FromStr"] # [doc = " [specification]: https://www.w3.org/TR/xmlschema11-2/#boolean"] pub struct SimpleTypeDeserializer < 'de , 'a > { # [doc = " - In case of attribute contains escaped attribute value"] # [doc = " - In case of text contains unescaped text value"] content : CowRef < 'de , 'a , [u8] > , # [doc = " If `true`, `content` in escaped form and should be unescaped before use"] escaped : bool , # [doc = " Decoder used to deserialize string data, numeric and boolean data."] # [doc = " Not used for deserializing raw byte buffers"] decoder : Decoder , }
};
}
