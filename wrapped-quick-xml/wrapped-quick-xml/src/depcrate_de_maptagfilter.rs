// Generated macro for TagFilter (enum)
macro_rules! Depcrate_de_mapTagFilter {
() => {
// Module: crate::de::map
// Provides: {"TagFilter"}
// Dependencies: {}
# [doc = " A filter that determines, what tags should form a sequence."] # [doc = ""] # [doc = " There are two types of sequences:"] # [doc = " - sequence where each element represented by tags with the same name"] # [doc = " - sequence where each element can have a different tag"] # [doc = ""] # [doc = " The first variant could represent a collection of structs, the second --"] # [doc = " a collection of enum variants."] # [doc = ""] # [doc = " In the second case we don't know what tag name should be expected as a"] # [doc = " sequence element, so we accept any element. Since the sequence are flattened"] # [doc = " into maps, we skip elements which have dedicated fields in a struct by using an"] # [doc = " `Exclude` filter that filters out elements with names matching field names"] # [doc = " from the struct."] # [doc = ""] # [doc = " # Lifetimes"] # [doc = ""] # [doc = " `'de` represents a lifetime of the XML input, when filter stores the"] # [doc = " dedicated tag name"] # [derive (Debug)] enum TagFilter < 'de > { # [doc = " A `SeqAccess` interested only in tags with specified name to deserialize"] # [doc = " an XML like this:"] # [doc = ""] # [doc = " ```xml"] # [doc = " <...>"] # [doc = "   <tag/>"] # [doc = "   <tag/>"] # [doc = "   <tag/>"] # [doc = "   ..."] # [doc = " </...>"] # [doc = " ```"] # [doc = ""] # [doc = " The tag name is stored inside (`b\"tag\"` for that example)"] Include (BytesStart < 'de >) , # [doc = " A `SeqAccess` interested in tags with any name, except explicitly listed."] # [doc = " Excluded tags are used as struct field names and therefore should not"] # [doc = " fall into a `$value` category."] # [doc = ""] # [doc = " The `bool` represents the having of a `$text` special field in fields array."] # [doc = " It is used to exclude text events when `$text` fields is defined together with"] # [doc = " `$value` fieldб and `$value` accepts sequence."] Exclude (& 'static [& 'static str] , bool) , }
};
}
