// Generated macro for impl_270 (impl)
macro_rules! Depcrate_events_attributesimpl_270 {
() => {
// Module: crate::events::attributes
// Provides: {"impl_270"}
// Dependencies: {}
impl IterState { pub const fn new (offset : usize , html : bool) -> Self { Self { state : State :: Next (offset) , html , check_duplicates : true , keys : Vec :: new () , } } # [doc = " Recover from an error that could have been made on a previous step."] # [doc = " Returns an offset from which parsing should continue."] # [doc = " If there no input left, returns `None`."] fn recover (& self , slice : & [u8]) -> Option < usize > { match self . state { State :: Done => None , State :: Next (offset) => Some (offset) , State :: SkipValue (offset) => self . skip_value (slice , offset) , State :: SkipEqValue (offset) => self . skip_eq_value (slice , offset) , } } # [doc = " Skip all characters up to first space symbol or end-of-input"] # [inline] # [allow (clippy :: manual_map)] fn skip_value (& self , slice : & [u8] , offset : usize) -> Option < usize > { let mut iter = (offset ..) . zip (slice [offset ..] . iter ()) ; match iter . find (| (_ , & b) | is_whitespace (b)) { Some ((e , _)) => Some (e) , None => None , } } # [doc = " Skip all characters up to first space symbol or end-of-input"] # [inline] fn skip_eq_value (& self , slice : & [u8] , offset : usize) -> Option < usize > { let mut iter = (offset ..) . zip (slice [offset ..] . iter ()) ; let quote = match iter . find (| (_ , & b) | ! is_whitespace (b)) { Some ((_ , b'"')) => b'"' , Some ((_ , b'\'')) => b'\'' , Some ((offset , _)) => return self . skip_value (slice , offset) , None => return None , } ; match iter . find (| (_ , & b) | b == quote) { Some ((e , b'"')) => Some (e) , Some ((e , _)) => Some (e) , None => None , } } # [inline] fn check_for_duplicates (& mut self , slice : & [u8] , key : Range < usize > ,) -> Result < Range < usize > , AttrError > { if self . check_duplicates { if let Some (prev) = self . keys . iter () . find (| r | slice [(* r) . clone ()] == slice [key . clone ()]) { return Err (AttrError :: Duplicated (key . start , prev . start)) ; } self . keys . push (key . clone ()) ; } Ok (key) } # [doc = " # Parameters"] # [doc = ""] # [doc = " - `slice`: content of the tag, used for checking for duplicates"] # [doc = " - `key`: Range of key in slice, if iterator in HTML mode"] # [doc = " - `offset`: Position of error if iterator in XML mode"] # [inline] fn key_only (& mut self , slice : & [u8] , key : Range < usize > , offset : usize) -> Option < AttrResult > { Some (if self . html { self . check_for_duplicates (slice , key) . map (Attr :: Empty) } else { Err (AttrError :: ExpectedEq (offset)) }) } # [inline] fn double_q (& mut self , key : Range < usize > , value : Range < usize >) -> Option < AttrResult > { self . state = State :: Next (value . end + 1) ; Some (Ok (Attr :: DoubleQ (key , value))) } # [inline] fn single_q (& mut self , key : Range < usize > , value : Range < usize >) -> Option < AttrResult > { self . state = State :: Next (value . end + 1) ; Some (Ok (Attr :: SingleQ (key , value))) } pub fn next (& mut self , slice : & [u8]) -> Option < AttrResult > { let mut iter = match self . recover (slice) { Some (offset) => (offset ..) . zip (slice [offset ..] . iter ()) , None => return None , } ; let start_key = match iter . find (| (_ , & b) | ! is_whitespace (b)) { Some ((s , _)) => s , None => { self . state = State :: Done ; return None ; } } ; let (key , offset) = match iter . find (| (_ , & b) | b == b'=' || is_whitespace (b)) { Some ((e , b'=')) => (start_key .. e , e) , Some ((e , _)) => match iter . find (| (_ , & b) | ! is_whitespace (b)) { Some ((offset , b'=')) => (start_key .. e , offset) , Some ((offset , _)) => { self . state = State :: Next (offset) ; return self . key_only (slice , start_key .. e , offset) ; } None => { self . state = State :: Done ; return self . key_only (slice , start_key .. e , slice . len ()) ; } } , None => { self . state = State :: Done ; let e = slice . len () ; return self . key_only (slice , start_key .. e , e) ; } } ; let key = match self . check_for_duplicates (slice , key) { Err (e) => { self . state = State :: SkipEqValue (offset) ; return Some (Err (e)) ; } Ok (key) => key , } ; let (start_value , quote) = match iter . find (| (_ , & b) | ! is_whitespace (b)) { Some ((s , b'"')) => (s + 1 , b'"') , Some ((s , b'\'')) => (s + 1 , b'\'') , Some ((s , _)) if self . html => { let end = match iter . find (| (_ , & b) | is_whitespace (b)) { Some ((e , _)) => e , None => slice . len () , } ; self . state = State :: Next (end) ; return Some (Ok (Attr :: Unquoted (key , s .. end))) ; } Some ((s , _)) => { self . state = State :: SkipValue (s) ; return Some (Err (AttrError :: UnquotedValue (s))) ; } None => { self . state = State :: Done ; return Some (Err (AttrError :: ExpectedValue (slice . len ()))) ; } } ; match iter . find (| (_ , & b) | b == quote) { Some ((e , b'"')) => self . double_q (key , start_value .. e) , Some ((e , _)) => self . single_q (key , start_value .. e) , None => { self . state = State :: Done ; Some (Err (AttrError :: ExpectedQuote (slice . len () , quote))) } } } }
};
}
