// Generated macro for ElementSerializer (struct)
macro_rules! Depcrate_se_elementElementSerializer {
() => {
// Module: crate::se::element
// Provides: {"ElementSerializer"}
// Dependencies: {}
# [doc = " A serializer used to serialize element with specified name. Unlike the [`ContentSerializer`],"] # [doc = " this serializer never uses variant names of enum variants, and because of that"] # [doc = " it is unable to serialize any enum values, except unit variants."] # [doc = ""] # [doc = " Returns the classification of the last written type."] # [doc = ""] # [doc = " This serializer is used for an ordinary fields in structs, which are not special"] # [doc = " fields named `$text` ([`TEXT_KEY`]) or `$value` ([`VALUE_KEY`]). `$text` field"] # [doc = " should be serialized using [`SimpleTypeSerializer`] and `$value` field should be"] # [doc = " serialized using [`ContentSerializer`]."] # [doc = ""] # [doc = " This serializer does the following:"] # [doc = " - numbers converted to a decimal representation and serialized as `<key>value</key>`;"] # [doc = " - booleans serialized ether as `<key>true</key>` or `<key>false</key>`;"] # [doc = " - strings and characters are serialized as `<key>value</key>`. In particular,"] # [doc = "   an empty string is serialized as `<key/>`;"] # [doc = " - `None` is serialized as `<key/>`;"] # [doc = " - `Some` and newtypes are serialized as an inner type using the same serializer;"] # [doc = " - units (`()`) and unit structs are serialized as `<key/>`;"] # [doc = " - sequences, tuples and tuple structs are serialized as repeated `<key>` tag."] # [doc = "   In particular, empty sequence is serialized to nothing;"] # [doc = " - structs are serialized as a sequence of fields wrapped in a `<key>` tag. Each"] # [doc = "   field is serialized recursively using either `ElementSerializer`, [`ContentSerializer`]"] # [doc = "   (`$value` fields), or [`SimpleTypeSerializer`] (`$text` fields)."] # [doc = "   In particular, the empty struct is serialized as `<key/>`;"] # [doc = " - maps are serialized as a sequence of entries wrapped in a `<key>` tag. If key is"] # [doc = "   serialized to a special name, the same rules as for struct fields are applied."] # [doc = "   In particular, the empty map is serialized as `<key/>`;"] # [doc = " - enums:"] # [doc = "   - unit variants are serialized as `<key>variant</key>`;"] # [doc = "   - other variants are not supported ([`SeError::Unsupported`] is returned);"] # [doc = ""] # [doc = " Usage of empty tags depends on the [`ContentSerializer::expand_empty_elements`] setting."] pub struct ElementSerializer < 'w , 'k , W : Write > { # [doc = " The inner serializer that contains the settings and mostly do the actual work"] pub ser : ContentSerializer < 'w , 'k , W > , # [doc = " Tag name used to wrap serialized types except enum variants which uses the variant name"] pub (super) key : XmlName < 'k > , }
};
}
