// Generated macro for BytesText (struct)
macro_rules! Depcrate_eventsBytesText {
() => {
// Module: crate::events
// Provides: {"BytesText"}
// Dependencies: {}
# [doc = " Data from various events (most notably, `Event::Text`) that stored in XML"] # [doc = " in escaped form. Internally data is stored in escaped form."] # [doc = ""] # [doc = " This event implements `Deref<Target = [u8]>`. The `deref()` implementation"] # [doc = " returns the content of this event. In case of comment this is everything"] # [doc = " between `<!--` and `-->` and the text of comment will not contain `-->` inside."] # [doc = " In case of DTD this is everything between `<!DOCTYPE` + spaces and closing `>`"] # [doc = " (i.e. in case of DTD the first character is never space):"] # [doc = ""] # [doc = " ```"] # [doc = " # use quick_xml::events::{BytesText, Event};"] # [doc = " # use quick_xml::reader::Reader;"] # [doc = " # use pretty_assertions::assert_eq;"] # [doc = " // Remember, that \\ at the end of string literal strips"] # [doc = " // all space characters to the first non-space character"] # [doc = " let mut reader = Reader::from_str(\"\\"] # [doc = "     <!DOCTYPE comment or text >\\"] # [doc = "     comment or text \\"] # [doc = "     <!--comment or text -->\""] # [doc = " );"] # [doc = " let content = \"comment or text \";"] # [doc = " let event = BytesText::new(content);"] # [doc = ""] # [doc = " assert_eq!(reader.read_event().unwrap(), Event::DocType(event.borrow()));"] # [doc = " assert_eq!(reader.read_event().unwrap(), Event::Text(event.borrow()));"] # [doc = " assert_eq!(reader.read_event().unwrap(), Event::Comment(event.borrow()));"] # [doc = " // deref coercion of &BytesText to &[u8]"] # [doc = " assert_eq!(&event as &[u8], content.as_bytes());"] # [doc = " // AsRef<[u8]> for &T + deref coercion"] # [doc = " assert_eq!(event.as_ref(), content.as_bytes());"] # [doc = " ```"] # [derive (Clone , Eq , PartialEq)] pub struct BytesText < 'a > { # [doc = " Escaped then encoded content of the event. Content is encoded in the XML"] # [doc = " document encoding when event comes from the reader and should be in the"] # [doc = " document encoding when event passed to the writer"] content : Cow < 'a , [u8] > , # [doc = " Encoding in which the `content` is stored inside the event"] decoder : Decoder , }
};
}
