// Generated macro for impl_544 (impl)
macro_rules! Depcrate_se_elementimpl_544 {
() => {
// Module: crate::se::element
// Provides: {"impl_544"}
// Dependencies: {}
impl < 'w , 'k , W : Write > Struct < 'w , 'k , W > { # [inline] fn write_field < T > (& mut self , key : & str , value : & T) -> Result < () , SeError > where T : ? Sized + Serialize , { if let Some (key) = key . strip_prefix ('@') { let key = XmlName :: try_from (key) ? ; self . write_attribute (key , value) } else { self . write_element (key , value) } } # [doc = " Writes `value` as an attribute"] # [inline] fn write_attribute < T > (& mut self , key : XmlName , value : & T) -> Result < () , SeError > where T : ? Sized + Serialize , { self . ser . ser . writer . write_char (' ') ? ; self . ser . ser . writer . write_str (key . 0) ? ; self . ser . ser . writer . write_char ('=') ? ; self . ser . ser . writer . write_char ('"') ? ; value . serialize (SimpleTypeSerializer { writer : & mut self . ser . ser . writer , target : QuoteTarget :: DoubleQAttr , level : self . ser . ser . level , }) ? ; self . ser . ser . writer . write_char ('"') ? ; Ok (()) } # [doc = " Writes `value` either as a text content, or as an element."] # [doc = ""] # [doc = " If `key` has a magic value [`TEXT_KEY`], then `value` serialized as a"] # [doc = " [simple type]."] # [doc = ""] # [doc = " If `key` has a magic value [`VALUE_KEY`], then `value` serialized as a"] # [doc = " [content] without wrapping in tags, otherwise it is wrapped in"] # [doc = " `<${key}>...</${key}>`."] # [doc = ""] # [doc = " [simple type]: SimpleTypeSerializer"] # [doc = " [content]: ContentSerializer"] fn write_element < T > (& mut self , key : & str , value : & T) -> Result < () , SeError > where T : ? Sized + Serialize , { let ser = ContentSerializer { writer : & mut self . children , level : self . ser . ser . level , indent : self . ser . ser . indent . borrow () , write_indent : self . write_indent , text_format : self . ser . ser . text_format , allow_primitive : true , expand_empty_elements : self . ser . ser . expand_empty_elements , } ; if key == TEXT_KEY { value . serialize (TextSerializer (ser . into_simple_type_serializer () ?)) ? ; self . write_indent = false ; } else if key == VALUE_KEY { self . write_indent = value . serialize (ser) ? . allow_indent () ; } else { value . serialize (ElementSerializer { key : XmlName :: try_from (key) ? , ser , }) ? ; self . write_indent = true ; } Ok (()) } }
};
}
