// Generated macro for impl_443 (impl)
macro_rules! Depcrate_reader_buffered_readerimpl_443 {
() => {
// Module: crate::reader::buffered_reader
// Provides: {"impl_443"}
// Dependencies: {}
# [doc = " This is an implementation for reading from a [`BufRead`] as underlying byte stream."] impl < R : BufRead > Reader < R > { # [doc = " Reads the next `Event`."] # [doc = ""] # [doc = " This is the main entry point for reading XML `Event`s."] # [doc = ""] # [doc = " `Event`s borrow `buf` and can be converted to own their data if needed (uses `Cow`"] # [doc = " internally)."] # [doc = ""] # [doc = " Having the possibility to control the internal buffers gives you some additional benefits"] # [doc = " such as:"] # [doc = ""] # [doc = " - Reduce the number of allocations by reusing the same buffer. For constrained systems,"] # [doc = "   you can call `buf.clear()` once you are done with processing the event (typically at the"] # [doc = "   end of your loop)."] # [doc = " - Reserve the buffer length if you know the file size (using `Vec::with_capacity`)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use pretty_assertions::assert_eq;"] # [doc = " use quick_xml::events::Event;"] # [doc = " use quick_xml::reader::Reader;"] # [doc = ""] # [doc = " let xml = r#\"<tag1 att1 = \"test\">"] # [doc = "                 <tag2><!--Test comment-->Test</tag2>"] # [doc = "                 <tag2>Test 2</tag2>"] # [doc = "              </tag1>\"#;"] # [doc = " let mut reader = Reader::from_str(xml);"] # [doc = " reader.config_mut().trim_text(true);"] # [doc = " let mut count = 0;"] # [doc = " let mut buf = Vec::new();"] # [doc = " let mut txt = Vec::new();"] # [doc = " loop {"] # [doc = "     match reader.read_event_into(&mut buf) {"] # [doc = "         Ok(Event::Start(_)) => count += 1,"] # [doc = "         Ok(Event::Text(e)) => txt.push(e.decode().unwrap().into_owned()),"] # [doc = "         Err(e) => panic!(\"Error at position {}: {:?}\", reader.error_position(), e),"] # [doc = "         Ok(Event::Eof) => break,"] # [doc = "         _ => (),"] # [doc = "     }"] # [doc = "     buf.clear();"] # [doc = " }"] # [doc = " assert_eq!(count, 3);"] # [doc = " assert_eq!(txt, vec![\"Test\".to_string(), \"Test 2\".to_string()]);"] # [doc = " ```"] # [inline] pub fn read_event_into < 'b > (& mut self , buf : & 'b mut Vec < u8 >) -> Result < Event < 'b > > { self . read_event_impl (buf) } # [doc = " Reads until end element is found using provided buffer as intermediate"] # [doc = " storage for events content. This function is supposed to be called after"] # [doc = " you already read a [`Start`] event."] # [doc = ""] # [doc = " Returns a span that cover content between `>` of an opening tag and `<` of"] # [doc = " a closing tag or an empty slice, if [`expand_empty_elements`] is set and"] # [doc = " this method was called after reading expanded [`Start`] event."] # [doc = ""] # [doc = " Manages nested cases where parent and child elements have the _literally_"] # [doc = " same name."] # [doc = ""] # [doc = " If a corresponding [`End`] event is not found, an error of type [`Error::IllFormed`]"] # [doc = " will be returned. In particularly, that error will be returned if you call"] # [doc = " this method without consuming the corresponding [`Start`] event first."] # [doc = ""] # [doc = " If your reader created from a string slice or byte array slice, it is"] # [doc = " better to use [`read_to_end()`] method, because it will not copy bytes"] # [doc = " into intermediate buffer."] # [doc = ""] # [doc = " The provided `buf` buffer will be filled only by one event content at time."] # [doc = " Before reading of each event the buffer will be cleared. If you know an"] # [doc = " appropriate size of each event, you can preallocate the buffer to reduce"] # [doc = " number of reallocations."] # [doc = ""] # [doc = " The `end` parameter should contain name of the end element _in the reader"] # [doc = " encoding_. It is good practice to always get that parameter using"] # [doc = " [`BytesStart::to_end()`] method."] # [doc = ""] # [doc = " The correctness of the skipped events does not checked, if you disabled"] # [doc = " the [`check_end_names`] option."] # [doc = ""] # [doc = " # Namespaces"] # [doc = ""] # [doc = " While the `Reader` does not support namespace resolution, namespaces"] # [doc = " does not change the algorithm for comparing names. Although the names"] # [doc = " `a:name` and `b:name` where both prefixes `a` and `b` resolves to the"] # [doc = " same namespace, are semantically equivalent, `</b:name>` cannot close"] # [doc = " `<a:name>`, because according to [the specification]"] # [doc = ""] # [doc = " > The end of every element that begins with a **start-tag** MUST be marked"] # [doc = " > by an **end-tag** containing a name that echoes the element's type as"] # [doc = " > given in the **start-tag**"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " This example shows, how you can skip XML content after you read the"] # [doc = " start event."] # [doc = ""] # [doc = " ```"] # [doc = " # use pretty_assertions::assert_eq;"] # [doc = " use quick_xml::events::{BytesStart, Event};"] # [doc = " use quick_xml::reader::Reader;"] # [doc = ""] # [doc = " let mut reader = Reader::from_str(r#\""] # [doc = "     <outer>"] # [doc = "         <inner>"] # [doc = "             <inner></inner>"] # [doc = "             <inner/>"] # [doc = "             <outer></outer>"] # [doc = "             <outer/>"] # [doc = "         </inner>"] # [doc = "     </outer>"] # [doc = " \"#);"] # [doc = " reader.config_mut().trim_text(true);"] # [doc = " let mut buf = Vec::new();"] # [doc = ""] # [doc = " let start = BytesStart::new(\"outer\");"] # [doc = " let end   = start.to_end().into_owned();"] # [doc = ""] # [doc = " // First, we read a start event..."] # [doc = " assert_eq!(reader.read_event_into(&mut buf).unwrap(), Event::Start(start));"] # [doc = ""] # [doc = " // ...then, we could skip all events to the corresponding end event."] # [doc = " // This call will correctly handle nested <outer> elements."] # [doc = " // Note, however, that this method does not handle namespaces."] # [doc = " reader.read_to_end_into(end.name(), &mut buf).unwrap();"] # [doc = ""] # [doc = " // At the end we should get an Eof event, because we ate the whole XML"] # [doc = " assert_eq!(reader.read_event_into(&mut buf).unwrap(), Event::Eof);"] # [doc = " ```"] # [doc = ""] # [doc = " [`Start`]: Event::Start"] # [doc = " [`End`]: Event::End"] # [doc = " [`BytesStart::to_end()`]: crate::events::BytesStart::to_end"] # [doc = " [`read_to_end()`]: Self::read_to_end"] # [doc = " [`expand_empty_elements`]: crate::reader::Config::expand_empty_elements"] # [doc = " [`check_end_names`]: crate::reader::Config::check_end_names"] # [doc = " [the specification]: https://www.w3.org/TR/xml11/#dt-etag"] pub fn read_to_end_into (& mut self , end : QName , buf : & mut Vec < u8 >) -> Result < Span > { Ok (read_to_end ! (self , end , buf , read_event_impl , { buf . clear () ; })) } }
};
}
