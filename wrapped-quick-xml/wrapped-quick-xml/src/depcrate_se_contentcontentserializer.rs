// Generated macro for ContentSerializer (struct)
macro_rules! Depcrate_se_contentContentSerializer {
() => {
// Module: crate::se::content
// Provides: {"ContentSerializer"}
// Dependencies: {}
# [doc = " A serializer used to serialize content of an element. It does not write"] # [doc = " surrounding tags. Unlike the [`ElementSerializer`], this serializer serializes"] # [doc = " enums using variant names as tag names, i. e. as `<variant>...</variant>`."] # [doc = ""] # [doc = " Returns the classification of the last written type."] # [doc = ""] # [doc = " This serializer does the following:"] # [doc = " - numbers converted to a decimal representation and serialized as naked strings;"] # [doc = " - booleans serialized ether as `\"true\"` or `\"false\"`;"] # [doc = " - strings and characters are serialized as naked strings;"] # [doc = " - `None` does not write anything;"] # [doc = " - `Some` and newtypes are serialized as an inner type using the same serializer;"] # [doc = " - units (`()`) and unit structs does not write anything;"] # [doc = " - sequences, tuples and tuple structs are serialized without delimiters."] # [doc = "   `[1, 2, 3]` would be serialized as `123` (if not using indent);"] # [doc = " - structs and maps are not supported ([`SeError::Unsupported`] is returned);"] # [doc = " - enums:"] # [doc = "   - unit variants are serialized as self-closed `<variant/>`;"] # [doc = "   - newtype variants are serialized as inner value wrapped in `<variant>...</variant>`;"] # [doc = "   - tuple variants are serialized as sequences where each element is wrapped"] # [doc = "     in `<variant>...</variant>`;"] # [doc = "   - struct variants are serialized as a sequence of fields wrapped in"] # [doc = "     `<variant>...</variant>`. Each field is serialized recursively using"] # [doc = "     either [`ElementSerializer`], `ContentSerializer` (`$value` fields), or"] # [doc = "     [`SimpleTypeSerializer`] (`$text` fields). In particular, the empty struct"] # [doc = "     is serialized as `<variant/>`;"] # [doc = ""] # [doc = " Usage of empty tags depends on the [`Self::expand_empty_elements`] setting."] # [doc = ""] # [doc = " The difference between this serializer and [`SimpleTypeSerializer`] is in how"] # [doc = " sequences and maps are serialized. Unlike `SimpleTypeSerializer` it supports"] # [doc = " any types in sequences and serializes them as list of elements, but that has"] # [doc = " drawbacks. Sequence of primitives would be serialized without delimiters and"] # [doc = " it will be impossible to distinguish between them. Even worse, when serializing"] # [doc = " with indent, sequence of strings become one big string with additional content"] # [doc = " and it would be impossible to distinguish between content of the original"] # [doc = " strings and inserted indent characters."] pub struct ContentSerializer < 'w , 'i , W : Write > { pub writer : & 'w mut W , # [doc = " Defines which XML characters need to be escaped in text content"] pub level : QuoteLevel , # [doc = " Current indentation level. Note, that `Indent::None` means that there is"] # [doc = " no indentation at all, but `write_indent == false` means only, that indent"] # [doc = " writing is disabled in this instantiation of `ContentSerializer`, but"] # [doc = " child serializers should have access to the actual state of indentation."] pub (super) indent : Indent < 'i > , # [doc = " If `true`, then current indent will be written before writing the content,"] # [doc = " but only if content is not empty. This flag is reset after writing indent."] pub write_indent : bool , # [doc = " Defines how text content should be serialized (as escaped text or CDATA)"] pub text_format : TextFormat , # [doc = " If `true`, then primitive types that serializes to a text content without"] # [doc = " surrounding tag will be allowed, otherwise the [`SeError::Unsupported`]"] # [doc = " will be returned."] # [doc = ""] # [doc = " This method protects from the situation when two consequent values serialized"] # [doc = " as a text that makes it impossible to distinguish between them during"] # [doc = " deserialization. Instead of ambiguous serialization the error is returned."] pub allow_primitive : bool , pub expand_empty_elements : bool , }
};
}
