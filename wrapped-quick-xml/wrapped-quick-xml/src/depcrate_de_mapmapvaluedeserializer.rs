// Generated macro for MapValueDeserializer (struct)
macro_rules! Depcrate_de_mapMapValueDeserializer {
() => {
// Module: crate::de::map
// Provides: {"MapValueDeserializer"}
// Dependencies: {}
# [doc = " A deserializer for a value of map or struct. That deserializer slightly"] # [doc = " differently processes events for a primitive types and sequences than"] # [doc = " a [`Deserializer`]."] # [doc = ""] # [doc = " This deserializer used to deserialize two kinds of fields:"] # [doc = " - usual fields with a dedicated name, such as `field_one` or `field_two`, in"] # [doc = "   that case field [`Self::fixed_name`] is `true`;"] # [doc = " - the special `$value` field which represents any tag or a textual content"] # [doc = "   in the XML which would be found in the document, in that case field"] # [doc = "   [`Self::fixed_name`] is `false`."] # [doc = ""] # [doc = " This deserializer can see two kind of events at the start:"] # [doc = " - [`DeEvent::Text`]"] # [doc = " - [`DeEvent::Start`]"] # [doc = ""] # [doc = " which represents two possible variants of items:"] # [doc = " ```xml"] # [doc = " <item>A tag item</item>"] # [doc = " A text item"] # [doc = " <yet another=\"tag item\"/>"] # [doc = " ```"] # [doc = ""] # [doc = " This deserializer are very similar to a [`ElementDeserializer`]. The only difference"] # [doc = " in the `deserialize_seq` method. This deserializer will act as an iterator"] # [doc = " over tags / text within it's parent tag, whereas the [`ElementDeserializer`]"] # [doc = " will represent sequences as an `xs:list`."] # [doc = ""] # [doc = " This deserializer processes items as following:"] # [doc = " - primitives (numbers, booleans, strings, characters) are deserialized either"] # [doc = "   from a text content, or unwrapped from a one level of a tag. So, `123` and"] # [doc = "   `<int>123</int>` both can be deserialized into an `u32`;"] # [doc = " - `Option`:"] # [doc = "   - empty text of [`DeEvent::Text`] is deserialized as `None`;"] # [doc = "   - everything else are deserialized as `Some` using the same deserializer,"] # [doc = "     including `<tag/>` or `<tag></tag>`;"] # [doc = " - units (`()`) and unit structs consumes the whole text or element subtree;"] # [doc = " - newtype structs are deserialized by forwarding deserialization of inner type"] # [doc = "   with the same deserializer;"] # [doc = " - sequences, tuples and tuple structs are deserialized by iterating within the"] # [doc = "   parent tag and deserializing each tag or text content using [`ElementDeserializer`];"] # [doc = " - structs and maps are deserialized using new instance of [`ElementMapAccess`];"] # [doc = " - enums:"] # [doc = "   - in case of [`DeEvent::Text`] event the text content is deserialized as"] # [doc = "     a `$text` variant. Enum content is deserialized from the text using"] # [doc = "     [`SimpleTypeDeserializer`];"] # [doc = "   - in case of [`DeEvent::Start`] event the tag name is deserialized as"] # [doc = "     an enum tag, and the content inside are deserialized as an enum content."] # [doc = "     Depending on a variant kind deserialization is performed as:"] # [doc = "     - unit variants: consuming text content or a subtree;"] # [doc = "     - newtype variants: forward deserialization to the inner type using"] # [doc = "       this deserializer;"] # [doc = "     - tuple variants: call [`deserialize_tuple`] of this deserializer;"] # [doc = "     - struct variants: call [`deserialize_struct`] of this deserializer."] # [doc = ""] # [doc = " [`deserialize_tuple`]: #method.deserialize_tuple"] # [doc = " [`deserialize_struct`]: #method.deserialize_struct"] struct MapValueDeserializer < 'de , 'd , 'm , R , E > where R : XmlRead < 'de > , E : EntityResolver , { # [doc = " Access to the map that created this deserializer. Gives access to the"] # [doc = " context, such as list of fields, that current map known about."] map : & 'm mut ElementMapAccess < 'de , 'd , R , E > , # [doc = " Whether this deserializer was created for deserialization from an element"] # [doc = " with fixed name, or the elements with different names or even text are allowed."] # [doc = ""] # [doc = " If this field is `true`, we process `<tag>` element in the following XML shape:"] # [doc = ""] # [doc = " ```xml"] # [doc = " <any-tag>"] # [doc = "   <tag>...</tag>"] # [doc = " </any-tag>"] # [doc = " ```"] # [doc = ""] # [doc = " The whole map represented by an `<any-tag>` element, the map key is a `tag`,"] # [doc = " and the value starts with is a `Start(\"tag\")` (the value deserializer will"] # [doc = " see that event first) and extended to the matching `End(\"tag\")` event."] # [doc = " In order to deserialize primitives (such as `usize`) we need to allow to"] # [doc = " look inside the one levels of tags, so the"] # [doc = ""] # [doc = " ```xml"] # [doc = " <tag>42<tag>"] # [doc = " ```"] # [doc = ""] # [doc = " could be deserialized into `42usize` without problems, and at the same time"] # [doc = ""] # [doc = " ```xml"] # [doc = " <tag>"] # [doc = "   <key1/>"] # [doc = "   <key2/>"] # [doc = "   <!--...-->"] # [doc = " <tag>"] # [doc = " ```"] # [doc = " could be deserialized to a struct."] # [doc = ""] # [doc = " If this field is `false`, we processes the one of following XML shapes:"] # [doc = ""] # [doc = " ```xml"] # [doc = " <any-tag>"] # [doc = "   text value"] # [doc = " </any-tag>"] # [doc = " ```"] # [doc = " ```xml"] # [doc = " <any-tag>"] # [doc = "   <![CDATA[cdata value]]>"] # [doc = " </any-tag>"] # [doc = " ```"] # [doc = " ```xml"] # [doc = " <any-tag>"] # [doc = "   <any>...</any>"] # [doc = " </any-tag>"] # [doc = " ```"] # [doc = ""] # [doc = " The whole map represented by an `<any-tag>` element, the map key is"] # [doc = " implicit and equals to the [`VALUE_KEY`] constant, and the value is"] # [doc = " a [`Text`], or a [`Start`] event (the value deserializer will see one of"] # [doc = " those events). In the first two cases the value of this field do not matter"] # [doc = " (because we already see the textual event and there no reasons to look"] # [doc = " \"inside\" something), but in the last case the primitives should raise"] # [doc = " a deserialization error, because that means that you trying to deserialize"] # [doc = " the following struct:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " struct AnyName {"] # [doc = "   #[serde(rename = \"$value\")]"] # [doc = "   any_name: String,"] # [doc = " }"] # [doc = " ```"] # [doc = " which means that `any_name` should get a content of the `<any-tag>` element."] # [doc = ""] # [doc = " Changing this can be valuable for <https://github.com/tafia/quick-xml/issues/383>,"] # [doc = " but those fields should be explicitly marked that they want to get any"] # [doc = " possible markup as a `String` and that mark is different from marking them"] # [doc = " as accepting \"text content\" which the currently `$text` means."] # [doc = ""] # [doc = " [`Text`]: DeEvent::Text"] # [doc = " [`Start`]: DeEvent::Start"] fixed_name : bool , }
};
}
