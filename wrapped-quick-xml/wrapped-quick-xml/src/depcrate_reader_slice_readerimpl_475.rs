// Generated macro for impl_475 (impl)
macro_rules! Depcrate_reader_slice_readerimpl_475 {
() => {
// Module: crate::reader::slice_reader
// Provides: {"impl_475"}
// Dependencies: {}
# [doc = " This is an implementation for reading from a `&[u8]` as underlying byte stream."] # [doc = " This implementation supports not using an intermediate buffer as the byte slice"] # [doc = " itself can be used to borrow from."] impl < 'a > Reader < & 'a [u8] > { # [doc = " Creates an XML reader from a string slice."] # [allow (clippy :: should_implement_trait)] pub fn from_str (s : & 'a str) -> Self { # [cfg (feature = "encoding")] { let mut reader = Self :: from_reader (s . as_bytes ()) ; reader . state . encoding = EncodingRef :: Explicit (UTF_8) ; reader } # [cfg (not (feature = "encoding"))] Self :: from_reader (s . as_bytes ()) } # [doc = " Read an event that borrows from the input rather than a buffer."] # [doc = ""] # [doc = " There is no asynchronous `read_event_async()` version of this function,"] # [doc = " because it is not necessary -- the contents are already in memory and no IO"] # [doc = " is needed, therefore there is no potential for blocking."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use pretty_assertions::assert_eq;"] # [doc = " use quick_xml::events::Event;"] # [doc = " use quick_xml::reader::Reader;"] # [doc = ""] # [doc = " let mut reader = Reader::from_str(r#\""] # [doc = "     <tag1 att1 = \"test\">"] # [doc = "        <tag2><!--Test comment-->Test</tag2>"] # [doc = "        <tag2>Test 2</tag2>"] # [doc = "     </tag1>"] # [doc = " \"#);"] # [doc = " reader.config_mut().trim_text(true);"] # [doc = ""] # [doc = " let mut count = 0;"] # [doc = " let mut txt = Vec::new();"] # [doc = " loop {"] # [doc = "     match reader.read_event().unwrap() {"] # [doc = "         Event::Start(e) => count += 1,"] # [doc = "         Event::Text(e) => txt.push(e.decode().unwrap().into_owned()),"] # [doc = "         Event::Eof => break,"] # [doc = "         _ => (),"] # [doc = "     }"] # [doc = " }"] # [doc = " assert_eq!(count, 3);"] # [doc = " assert_eq!(txt, vec![\"Test\".to_string(), \"Test 2\".to_string()]);"] # [doc = " ```"] # [inline] pub fn read_event (& mut self) -> Result < Event < 'a > > { self . read_event_impl (()) } # [doc = " Reads until end element is found. This function is supposed to be called"] # [doc = " after you already read a [`Start`] event."] # [doc = ""] # [doc = " Returns a span that cover content between `>` of an opening tag and `<` of"] # [doc = " a closing tag or an empty slice, if [`expand_empty_elements`] is set and"] # [doc = " this method was called after reading expanded [`Start`] event."] # [doc = ""] # [doc = " Manages nested cases where parent and child elements have the _literally_"] # [doc = " same name."] # [doc = ""] # [doc = " If a corresponding [`End`] event is not found, an error of type [`Error::IllFormed`]"] # [doc = " will be returned. In particularly, that error will be returned if you call"] # [doc = " this method without consuming the corresponding [`Start`] event first."] # [doc = ""] # [doc = " The `end` parameter should contain name of the end element _in the reader"] # [doc = " encoding_. It is good practice to always get that parameter using"] # [doc = " [`BytesStart::to_end()`] method."] # [doc = ""] # [doc = " The correctness of the skipped events does not checked, if you disabled"] # [doc = " the [`check_end_names`] option."] # [doc = ""] # [doc = " There is no asynchronous `read_to_end_async()` version of this function,"] # [doc = " because it is not necessary -- the contents are already in memory and no IO"] # [doc = " is needed, therefore there is no potential for blocking."] # [doc = ""] # [doc = " # Namespaces"] # [doc = ""] # [doc = " While the `Reader` does not support namespace resolution, namespaces"] # [doc = " does not change the algorithm for comparing names. Although the names"] # [doc = " `a:name` and `b:name` where both prefixes `a` and `b` resolves to the"] # [doc = " same namespace, are semantically equivalent, `</b:name>` cannot close"] # [doc = " `<a:name>`, because according to [the specification]"] # [doc = ""] # [doc = " > The end of every element that begins with a **start-tag** MUST be marked"] # [doc = " > by an **end-tag** containing a name that echoes the element's type as"] # [doc = " > given in the **start-tag**"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " This example shows, how you can skip XML content after you read the"] # [doc = " start event."] # [doc = ""] # [doc = " ```"] # [doc = " # use pretty_assertions::assert_eq;"] # [doc = " use quick_xml::events::{BytesStart, Event};"] # [doc = " use quick_xml::reader::Reader;"] # [doc = ""] # [doc = " let mut reader = Reader::from_str(r#\""] # [doc = "     <outer>"] # [doc = "         <inner>"] # [doc = "             <inner></inner>"] # [doc = "             <inner/>"] # [doc = "             <outer></outer>"] # [doc = "             <outer/>"] # [doc = "         </inner>"] # [doc = "     </outer>"] # [doc = " \"#);"] # [doc = " reader.config_mut().trim_text(true);"] # [doc = ""] # [doc = " let start = BytesStart::new(\"outer\");"] # [doc = " let end   = start.to_end().into_owned();"] # [doc = ""] # [doc = " // First, we read a start event..."] # [doc = " assert_eq!(reader.read_event().unwrap(), Event::Start(start));"] # [doc = ""] # [doc = " // ...then, we could skip all events to the corresponding end event."] # [doc = " // This call will correctly handle nested <outer> elements."] # [doc = " // Note, however, that this method does not handle namespaces."] # [doc = " reader.read_to_end(end.name()).unwrap();"] # [doc = ""] # [doc = " // At the end we should get an Eof event, because we ate the whole XML"] # [doc = " assert_eq!(reader.read_event().unwrap(), Event::Eof);"] # [doc = " ```"] # [doc = ""] # [doc = " [`Start`]: Event::Start"] # [doc = " [`End`]: Event::End"] # [doc = " [`BytesStart::to_end()`]: crate::events::BytesStart::to_end"] # [doc = " [`expand_empty_elements`]: crate::reader::Config::expand_empty_elements"] # [doc = " [`check_end_names`]: crate::reader::Config::check_end_names"] # [doc = " [the specification]: https://www.w3.org/TR/xml11/#dt-etag"] pub fn read_to_end (& mut self , end : QName) -> Result < Span > { Ok (read_to_end ! (self , end , () , read_event_impl , { })) } # [doc = " Reads content between start and end tags, including any markup. This"] # [doc = " function is supposed to be called after you already read a [`Start`] event."] # [doc = ""] # [doc = " Manages nested cases where parent and child elements have the _literally_"] # [doc = " same name."] # [doc = ""] # [doc = " This method does not unescape read data, instead it returns content"] # [doc = " \"as is\" of the XML document. This is because it has no idea what text"] # [doc = " it reads, and if, for example, it contains CDATA section, attempt to"] # [doc = " unescape it content will spoil data."] # [doc = ""] # [doc = " Any text will be decoded using the XML current [`decoder()`]."] # [doc = ""] # [doc = " Actually, this method perform the following code:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " let span = reader.read_to_end(end)?;"] # [doc = " let text = reader.decoder().decode(&reader.inner_slice[span]);"] # [doc = " ```"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " This example shows, how you can read a HTML content from your XML document."] # [doc = ""] # [doc = " ```"] # [doc = " # use pretty_assertions::assert_eq;"] # [doc = " # use std::borrow::Cow;"] # [doc = " use quick_xml::events::{BytesStart, Event};"] # [doc = " use quick_xml::reader::Reader;"] # [doc = ""] # [doc = " let mut reader = Reader::from_str(\""] # [doc = "     <html>"] # [doc = "         <title>This is a HTML text</title>"] # [doc = "         <p>Usual XML rules does not apply inside it"] # [doc = "         <p>For example, elements not needed to be &quot;closed&quot;"] # [doc = "     </html>"] # [doc = " \");"] # [doc = " reader.config_mut().trim_text(true);"] # [doc = ""] # [doc = " let start = BytesStart::new(\"html\");"] # [doc = " let end   = start.to_end().into_owned();"] # [doc = ""] # [doc = " // First, we read a start event..."] # [doc = " assert_eq!(reader.read_event().unwrap(), Event::Start(start));"] # [doc = " // ...and disable checking of end names because we expect HTML further..."] # [doc = " reader.config_mut().check_end_names = false;"] # [doc = ""] # [doc = " // ...then, we could read text content until close tag."] # [doc = " // This call will correctly handle nested <html> elements."] # [doc = " let text = reader.read_text(end.name()).unwrap();"] # [doc = " assert_eq!(text, Cow::Borrowed(r#\""] # [doc = "         <title>This is a HTML text</title>"] # [doc = "         <p>Usual XML rules does not apply inside it"] # [doc = "         <p>For example, elements not needed to be &quot;closed&quot;"] # [doc = "     \"#));"] # [doc = " assert!(matches!(text, Cow::Borrowed(_)));"] # [doc = ""] # [doc = " // Now we can enable checks again"] # [doc = " reader.config_mut().check_end_names = true;"] # [doc = ""] # [doc = " // At the end we should get an Eof event, because we ate the whole XML"] # [doc = " assert_eq!(reader.read_event().unwrap(), Event::Eof);"] # [doc = " ```"] # [doc = ""] # [doc = " [`Start`]: Event::Start"] # [doc = " [`decoder()`]: Self::decoder()"] pub fn read_text (& mut self , end : QName) -> Result < Cow < 'a , str > > { let buffer = self . reader ; let span = self . read_to_end (end) ? ; let len = span . end - span . start ; Ok (self . decoder () . decode (& buffer [0 .. len as usize]) ?) } }
};
}
