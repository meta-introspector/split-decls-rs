// Generated macro for impl_427 (impl)
macro_rules! Depcrate_reader_async_tokioimpl_427 {
() => {
// Module: crate::reader::async_tokio
// Provides: {"impl_427"}
// Dependencies: {}
impl < R : AsyncBufRead + Unpin > Reader < R > { # [doc = " An asynchronous version of [`read_event_into()`]. Reads the next event into"] # [doc = " given buffer."] # [doc = ""] # [doc = " This is the main entry point for reading XML `Event`s when using an async reader."] # [doc = ""] # [doc = " See the documentation of [`read_event_into()`] for more information."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # tokio_test::block_on(async {"] # [doc = " # use pretty_assertions::assert_eq;"] # [doc = " use quick_xml::events::Event;"] # [doc = " use quick_xml::reader::Reader;"] # [doc = ""] # [doc = " // This explicitly uses `from_reader(\"...\".as_bytes())` to use a buffered"] # [doc = " // reader instead of relying on the zero-copy optimizations for reading"] # [doc = " // from byte slices, which provides the sync interface anyway."] # [doc = " let mut reader = Reader::from_reader(r#\""] # [doc = "     <tag1 att1 = \"test\">"] # [doc = "        <tag2><!--Test comment-->Test</tag2>"] # [doc = "        <tag2>Test 2</tag2>"] # [doc = "     </tag1>"] # [doc = " \"#.as_bytes());"] # [doc = " reader.config_mut().trim_text(true);"] # [doc = ""] # [doc = " let mut count = 0;"] # [doc = " let mut buf = Vec::new();"] # [doc = " let mut txt = Vec::new();"] # [doc = " loop {"] # [doc = "     match reader.read_event_into_async(&mut buf).await {"] # [doc = "         Ok(Event::Start(_)) => count += 1,"] # [doc = "         Ok(Event::Text(e)) => txt.push(e.decode().unwrap().into_owned()),"] # [doc = "         Err(e) => panic!(\"Error at position {}: {:?}\", reader.error_position(), e),"] # [doc = "         Ok(Event::Eof) => break,"] # [doc = "         _ => (),"] # [doc = "     }"] # [doc = "     buf.clear();"] # [doc = " }"] # [doc = " assert_eq!(count, 3);"] # [doc = " assert_eq!(txt, vec![\"Test\".to_string(), \"Test 2\".to_string()]);"] # [doc = " # }) // tokio_test::block_on"] # [doc = " ```"] # [doc = ""] # [doc = " [`read_event_into()`]: Reader::read_event_into"] pub async fn read_event_into_async < 'b > (& mut self , mut buf : & 'b mut Vec < u8 > ,) -> Result < Event < 'b > > { read_event_impl ! (self , buf , TokioAdapter (& mut self . reader) , read_until_close_async , await) } # [doc = " An asynchronous version of [`read_to_end_into()`]."] # [doc = " Reads asynchronously until end element is found using provided buffer as"] # [doc = " intermediate storage for events content. This function is supposed to be"] # [doc = " called after you already read a [`Start`] event."] # [doc = ""] # [doc = " See the documentation of [`read_to_end_into()`] for more information."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " This example shows, how you can skip XML content after you read the"] # [doc = " start event."] # [doc = ""] # [doc = " ```"] # [doc = " # tokio_test::block_on(async {"] # [doc = " # use pretty_assertions::assert_eq;"] # [doc = " use quick_xml::events::{BytesStart, Event};"] # [doc = " use quick_xml::reader::Reader;"] # [doc = ""] # [doc = " let mut reader = Reader::from_reader(r#\""] # [doc = "     <outer>"] # [doc = "         <inner>"] # [doc = "             <inner></inner>"] # [doc = "             <inner/>"] # [doc = "             <outer></outer>"] # [doc = "             <outer/>"] # [doc = "         </inner>"] # [doc = "     </outer>"] # [doc = " \"#.as_bytes());"] # [doc = " reader.config_mut().trim_text(true);"] # [doc = " let mut buf = Vec::new();"] # [doc = ""] # [doc = " let start = BytesStart::new(\"outer\");"] # [doc = " let end   = start.to_end().into_owned();"] # [doc = ""] # [doc = " // First, we read a start event..."] # [doc = " assert_eq!(reader.read_event_into_async(&mut buf).await.unwrap(), Event::Start(start));"] # [doc = ""] # [doc = " // ...then, we could skip all events to the corresponding end event."] # [doc = " // This call will correctly handle nested <outer> elements."] # [doc = " // Note, however, that this method does not handle namespaces."] # [doc = " reader.read_to_end_into_async(end.name(), &mut buf).await.unwrap();"] # [doc = ""] # [doc = " // At the end we should get an Eof event, because we ate the whole XML"] # [doc = " assert_eq!(reader.read_event_into_async(&mut buf).await.unwrap(), Event::Eof);"] # [doc = " # }) // tokio_test::block_on"] # [doc = " ```"] # [doc = ""] # [doc = " [`read_to_end_into()`]: Self::read_to_end_into"] # [doc = " [`Start`]: Event::Start"] pub async fn read_to_end_into_async < 'n > (& mut self , end : QName < 'n > , buf : & mut Vec < u8 > ,) -> Result < Span > { Ok (read_to_end ! (self , end , buf , read_event_into_async , { buf . clear () ; } , await)) } # [doc = " Private function to read until `>` is found. This function expects that"] # [doc = " it was called just after encounter a `<` symbol."] async fn read_until_close_async < 'b > (& mut self , buf : & 'b mut Vec < u8 >) -> Result < Event < 'b > > { read_until_close ! (self , buf , TokioAdapter (& mut self . reader) , await) } }
};
}
