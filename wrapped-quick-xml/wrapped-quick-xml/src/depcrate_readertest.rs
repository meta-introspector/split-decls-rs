// Generated macro for test (module)
macro_rules! Depcrate_readertest {
() => {
// Module: crate::reader
// Provides: {"test"}
// Dependencies: {}
# [cfg (test)] mod test { # [doc = " Checks the internal implementation of the various reader methods"] macro_rules ! check { (# [$ test : meta] $ read_event : ident , $ read_until_close : ident , $ source : path , $ buf : expr $ (, $ async : ident , $ await : ident) ?) => { mod read_bang_element { use super ::*; use crate :: errors :: { Error , SyntaxError } ; use crate :: reader :: BangType ; use crate :: utils :: Bytes ; # [doc = " Checks that reading CDATA content works correctly"] mod cdata { use super ::*; use pretty_assertions :: assert_eq ; # [doc = " Checks that if input begins like CDATA element, but CDATA start sequence"] # [doc = " is not finished, parsing ends with an error"] # [$ test] # [ignore = "start CDATA sequence fully checked outside of `read_bang_element`"] $ ($ async) ? fn not_properly_start () { let buf = $ buf ; let mut position = 1 ; let mut input = b"![]]>other content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedCData) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 1) ; } # [doc = " Checks that if CDATA startup sequence was matched, but an end sequence"] # [doc = " is not found, parsing ends with an error"] # [$ test] $ ($ async) ? fn not_closed () { let buf = $ buf ; let mut position = 1 ; let mut input = b"![CDATA[other content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedCData) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 22) ; } # [doc = " Checks that CDATA element without content inside parsed successfully"] # [$ test] $ ($ async) ? fn empty () { let buf = $ buf ; let mut position = 1 ; let mut input = b"![CDATA[]]>other content" . as_ref () ; let (ty , bytes) = $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? . unwrap () ; assert_eq ! ((ty , Bytes (bytes)) , (BangType :: CData , Bytes (b"![CDATA[]]"))) ; assert_eq ! (position , 12) ; } # [doc = " Checks that CDATA element with content parsed successfully."] # [doc = " Additionally checks that sequences inside CDATA that may look like"] # [doc = " a CDATA end sequence do not interrupt CDATA parsing"] # [$ test] $ ($ async) ? fn with_content () { let buf = $ buf ; let mut position = 1 ; let mut input = b"![CDATA[cdata]] ]>content]]>other content]]>" . as_ref () ; let (ty , bytes) = $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? . unwrap () ; assert_eq ! ((ty , Bytes (bytes)) , (BangType :: CData , Bytes (b"![CDATA[cdata]] ]>content]]"))) ; assert_eq ! (position , 29) ; } } # [doc = " Checks that reading XML comments works correctly. According to the [specification],"] # [doc = " comment data can contain any sequence except `--`:"] # [doc = ""] # [doc = " ```peg"] # [doc = " comment = '<--' (!'--' char)* '-->';"] # [doc = " char = [#x1-#x2C]"] # [doc = "      / [#x2E-#xD7FF]"] # [doc = "      / [#xE000-#xFFFD]"] # [doc = "      / [#x10000-#x10FFFF]"] # [doc = " ```"] # [doc = ""] # [doc = " The presence of this limitation, however, is simply a poorly designed specification"] # [doc = " (maybe for purpose of building of LL(1) XML parser) and quick-xml does not check for"] # [doc = " presence of these sequences by default. This tests allow such content."] # [doc = ""] # [doc = " [specification]: https://www.w3.org/TR/xml11/#dt-comment"] mod comment { use super ::*; use pretty_assertions :: assert_eq ; # [$ test] # [ignore = "start comment sequence fully checked outside of `read_bang_element`"] $ ($ async) ? fn not_properly_start () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!- -->other content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedComment) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 1) ; } # [$ test] $ ($ async) ? fn not_properly_end () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!->other content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedComment) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 17) ; } # [$ test] $ ($ async) ? fn not_closed1 () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!--other content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedComment) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 17) ; } # [$ test] $ ($ async) ? fn not_closed2 () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!-->other content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedComment) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 18) ; } # [$ test] $ ($ async) ? fn not_closed3 () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!--->other content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedComment) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 19) ; } # [$ test] $ ($ async) ? fn empty () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!---->other content" . as_ref () ; let (ty , bytes) = $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? . unwrap () ; assert_eq ! ((ty , Bytes (bytes)) , (BangType :: Comment , Bytes (b"!----"))) ; assert_eq ! (position , 7) ; } # [$ test] $ ($ async) ? fn with_content () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!--->comment<--->other content" . as_ref () ; let (ty , bytes) = $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? . unwrap () ; assert_eq ! ((ty , Bytes (bytes)) , (BangType :: Comment , Bytes (b"!--->comment<---"))) ; assert_eq ! (position , 18) ; } } # [doc = " Checks that reading DOCTYPE definition works correctly"] mod doctype { use super ::*; mod uppercase { use super ::*; use pretty_assertions :: assert_eq ; # [$ test] $ ($ async) ? fn not_properly_start () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!D other content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedDoctype) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 17) ; } # [$ test] $ ($ async) ? fn without_space () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!DOCTYPEother content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedDoctype) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 22) ; } # [$ test] $ ($ async) ? fn empty () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!DOCTYPE>other content" . as_ref () ; let (ty , bytes) = $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? . unwrap () ; assert_eq ! ((ty , Bytes (bytes)) , (BangType :: DocType (0) , Bytes (b"!DOCTYPE"))) ; assert_eq ! (position , 10) ; } # [$ test] $ ($ async) ? fn not_closed () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!DOCTYPE other content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedDoctype) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 23) ; } } mod lowercase { use super ::*; use pretty_assertions :: assert_eq ; # [$ test] $ ($ async) ? fn not_properly_start () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!d other content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedDoctype) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 17) ; } # [$ test] $ ($ async) ? fn without_space () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!doctypeother content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedDoctype) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 22) ; } # [$ test] $ ($ async) ? fn empty () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!doctype>other content" . as_ref () ; let (ty , bytes) = $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? . unwrap () ; assert_eq ! ((ty , Bytes (bytes)) , (BangType :: DocType (0) , Bytes (b"!doctype"))) ; assert_eq ! (position , 10) ; } # [$ test] $ ($ async) ? fn not_closed () { let buf = $ buf ; let mut position = 1 ; let mut input = b"!doctype other content" . as_ref () ; match $ source (& mut input) . read_bang_element (buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedDoctype) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 23) ; } } } } mod read_text { use super ::*; use crate :: reader :: ReadTextResult ; use crate :: utils :: Bytes ; use pretty_assertions :: assert_eq ; # [$ test] $ ($ async) ? fn empty () { let buf = $ buf ; let mut position = 1 ; let mut input = b"" . as_ref () ; match $ source (& mut input) . read_text (buf , & mut position) $ (.$ await) ? { ReadTextResult :: UpToEof (bytes) => assert_eq ! (Bytes (bytes) , Bytes (b"")) , x => panic ! ("Expected `UpToEof(_)`, but got `{:?}`" , x) , } assert_eq ! (position , 1) ; } # [$ test] $ ($ async) ? fn markup () { let buf = $ buf ; let mut position = 1 ; let mut input = b"<" . as_ref () ; match $ source (& mut input) . read_text (buf , & mut position) $ (.$ await) ? { ReadTextResult :: Markup (b) => assert_eq ! (b , $ buf) , x => panic ! ("Expected `Markup(_)`, but got `{:?}`" , x) , } assert_eq ! (position , 2) ; } # [$ test] $ ($ async) ? fn ref_ () { let buf = $ buf ; let mut position = 1 ; let mut input = b"&" . as_ref () ; match $ source (& mut input) . read_text (buf , & mut position) $ (.$ await) ? { ReadTextResult :: Ref (b) => assert_eq ! (b , $ buf) , x => panic ! ("Expected `Ref(_)`, but got `{:?}`" , x) , } assert_eq ! (position , 1) ; } # [$ test] $ ($ async) ? fn up_to_markup () { let buf = $ buf ; let mut position = 1 ; let mut input = b"a<" . as_ref () ; match $ source (& mut input) . read_text (buf , & mut position) $ (.$ await) ? { ReadTextResult :: UpToMarkup (bytes) => assert_eq ! (Bytes (bytes) , Bytes (b"a")) , x => panic ! ("Expected `UpToMarkup(_)`, but got `{:?}`" , x) , } assert_eq ! (position , 3) ; } # [$ test] $ ($ async) ? fn up_to_ref () { let buf = $ buf ; let mut position = 1 ; let mut input = b"a&" . as_ref () ; match $ source (& mut input) . read_text (buf , & mut position) $ (.$ await) ? { ReadTextResult :: UpToRef (bytes) => assert_eq ! (Bytes (bytes) , Bytes (b"a")) , x => panic ! ("Expected `UpToRef(_)`, but got `{:?}`" , x) , } assert_eq ! (position , 2) ; } # [$ test] $ ($ async) ? fn up_to_eof () { let buf = $ buf ; let mut position = 1 ; let mut input = b"a" . as_ref () ; match $ source (& mut input) . read_text (buf , & mut position) $ (.$ await) ? { ReadTextResult :: UpToEof (bytes) => assert_eq ! (Bytes (bytes) , Bytes (b"a")) , x => panic ! ("Expected `UpToEof(_)`, but got `{:?}`" , x) , } assert_eq ! (position , 2) ; } } mod read_ref { use super ::*; use crate :: reader :: ReadRefResult ; use crate :: utils :: Bytes ; use pretty_assertions :: assert_eq ; # [$ test] $ ($ async) ? fn up_to_eof () { let buf = $ buf ; let mut position = 1 ; let mut input = b"&" . as_ref () ; match $ source (& mut input) . read_ref (buf , & mut position) $ (.$ await) ? { ReadRefResult :: UpToEof (bytes) => assert_eq ! (Bytes (bytes) , Bytes (b"&")) , x => panic ! ("Expected `UpToEof(_)`, but got `{:?}`" , x) , } assert_eq ! (position , 2) ; } # [$ test] $ ($ async) ? fn up_to_ref () { let buf = $ buf ; let mut position = 1 ; let mut input = b"&&" . as_ref () ; match $ source (& mut input) . read_ref (buf , & mut position) $ (.$ await) ? { ReadRefResult :: UpToRef (bytes) => assert_eq ! (Bytes (bytes) , Bytes (b"&")) , x => panic ! ("Expected `UpToRef(_)`, but got `{:?}`" , x) , } assert_eq ! (position , 2) ; } # [$ test] $ ($ async) ? fn up_to_markup () { let buf = $ buf ; let mut position = 1 ; let mut input = b"&<" . as_ref () ; match $ source (& mut input) . read_ref (buf , & mut position) $ (.$ await) ? { ReadRefResult :: UpToMarkup (bytes) => assert_eq ! (Bytes (bytes) , Bytes (b"&")) , x => panic ! ("Expected `UpToMarkup(_)`, but got `{:?}`" , x) , } assert_eq ! (position , 3) ; } # [$ test] $ ($ async) ? fn empty_ref () { let buf = $ buf ; let mut position = 1 ; let mut input = b"&;" . as_ref () ; match $ source (& mut input) . read_ref (buf , & mut position) $ (.$ await) ? { ReadRefResult :: Ref (bytes) => assert_eq ! (Bytes (bytes) , Bytes (b"&")) , x => panic ! ("Expected `Ref(_)`, but got `{:?}`" , x) , } assert_eq ! (position , 3) ; } # [$ test] $ ($ async) ? fn normal () { let buf = $ buf ; let mut position = 1 ; let mut input = b"&lt;" . as_ref () ; match $ source (& mut input) . read_ref (buf , & mut position) $ (.$ await) ? { ReadRefResult :: Ref (bytes) => assert_eq ! (Bytes (bytes) , Bytes (b"&lt")) , x => panic ! ("Expected `Ref(_)`, but got `{:?}`" , x) , } assert_eq ! (position , 5) ; } } mod read_element { use super ::*; use crate :: errors :: { Error , SyntaxError } ; use crate :: parser :: ElementParser ; use crate :: utils :: Bytes ; use pretty_assertions :: assert_eq ; # [doc = " Checks that nothing was read from empty buffer"] # [$ test] $ ($ async) ? fn empty () { let buf = $ buf ; let mut position = 1 ; let mut input = b"" . as_ref () ; match $ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? { Err (Error :: Syntax (cause)) => assert_eq ! (cause , SyntaxError :: UnclosedTag) , x => panic ! ("Expected `Err(Syntax(_))`, but got `{:?}`" , x) , } assert_eq ! (position , 1) ; } mod open { use super ::*; use pretty_assertions :: assert_eq ; # [$ test] $ ($ async) ? fn empty_tag () { let buf = $ buf ; let mut position = 1 ; let mut input = b">" . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (b"")) ; assert_eq ! (position , 2) ; } # [$ test] $ ($ async) ? fn normal () { let buf = $ buf ; let mut position = 1 ; let mut input = b"tag>" . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (b"tag")) ; assert_eq ! (position , 5) ; } # [$ test] $ ($ async) ? fn empty_ns_empty_tag () { let buf = $ buf ; let mut position = 1 ; let mut input = b":>" . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (b":")) ; assert_eq ! (position , 3) ; } # [$ test] $ ($ async) ? fn empty_ns () { let buf = $ buf ; let mut position = 1 ; let mut input = b":tag>" . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (b":tag")) ; assert_eq ! (position , 6) ; } # [$ test] $ ($ async) ? fn with_attributes () { let buf = $ buf ; let mut position = 1 ; let mut input = br#"tag  attr-1=">"  attr2  =  '>'  3attr>"# . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (br#"tag  attr-1=">"  attr2  =  '>'  3attr"#)) ; assert_eq ! (position , 39) ; } } mod self_closed { use super ::*; use pretty_assertions :: assert_eq ; # [$ test] $ ($ async) ? fn empty_tag () { let buf = $ buf ; let mut position = 1 ; let mut input = b"/>" . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (b"/")) ; assert_eq ! (position , 3) ; } # [$ test] $ ($ async) ? fn normal () { let buf = $ buf ; let mut position = 1 ; let mut input = b"tag/>" . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (b"tag/")) ; assert_eq ! (position , 6) ; } # [$ test] $ ($ async) ? fn empty_ns_empty_tag () { let buf = $ buf ; let mut position = 1 ; let mut input = b":/>" . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (b":/")) ; assert_eq ! (position , 4) ; } # [$ test] $ ($ async) ? fn empty_ns () { let buf = $ buf ; let mut position = 1 ; let mut input = b":tag/>" . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (b":tag/")) ; assert_eq ! (position , 7) ; } # [$ test] $ ($ async) ? fn with_attributes () { let buf = $ buf ; let mut position = 1 ; let mut input = br#"tag  attr-1="/>"  attr2  =  '/>'  3attr/>"# . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (br#"tag  attr-1="/>"  attr2  =  '/>'  3attr/"#)) ; assert_eq ! (position , 42) ; } } mod close { use super ::*; use pretty_assertions :: assert_eq ; # [$ test] $ ($ async) ? fn empty_tag () { let buf = $ buf ; let mut position = 1 ; let mut input = b"/ >" . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (b"/ ")) ; assert_eq ! (position , 4) ; } # [$ test] $ ($ async) ? fn normal () { let buf = $ buf ; let mut position = 1 ; let mut input = b"/tag>" . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (b"/tag")) ; assert_eq ! (position , 6) ; } # [$ test] $ ($ async) ? fn empty_ns_empty_tag () { let buf = $ buf ; let mut position = 1 ; let mut input = b"/:>" . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (b"/:")) ; assert_eq ! (position , 4) ; } # [$ test] $ ($ async) ? fn empty_ns () { let buf = $ buf ; let mut position = 1 ; let mut input = b"/:tag>" . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (b"/:tag")) ; assert_eq ! (position , 7) ; } # [$ test] $ ($ async) ? fn with_attributes () { let buf = $ buf ; let mut position = 1 ; let mut input = br#"/tag  attr-1=">"  attr2  =  '>'  3attr>"# . as_ref () ; assert_eq ! (Bytes ($ source (& mut input) . read_with (ElementParser :: default () , buf , & mut position) $ (.$ await) ? . unwrap ()) , Bytes (br#"/tag  attr-1=">"  attr2  =  '>'  3attr"#)) ; assert_eq ! (position , 40) ; } } } # [doc = " Ensures, that no empty `Text` events are generated"] mod $ read_event { use crate :: events :: { BytesCData , BytesDecl , BytesEnd , BytesPI , BytesStart , BytesText , Event } ; use crate :: reader :: Reader ; use pretty_assertions :: assert_eq ; # [doc = " When `encoding` feature is enabled, encoding should be detected"] # [doc = " from BOM (UTF-8) and BOM should be stripped."] # [doc = ""] # [doc = " When `encoding` feature is disabled, UTF-8 is assumed and BOM"] # [doc = " character should be stripped for consistency"] # [$ test] $ ($ async) ? fn bom_from_reader () { let mut reader = Reader :: from_reader ("\u{feff}\u{feff}" . as_bytes ()) ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: Text (BytesText :: from_escaped ("\u{feff}"))) ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: Eof) ; } # [doc = " When parsing from &str, encoding is fixed (UTF-8), so"] # [doc = " - when `encoding` feature is disabled, the behavior the"] # [doc = "   same as in `bom_from_reader` text"] # [doc = " - when `encoding` feature is enabled, the behavior should"] # [doc = "   stay consistent, so the first BOM character is stripped"] # [$ test] $ ($ async) ? fn bom_from_str () { let mut reader = Reader :: from_str ("\u{feff}\u{feff}") ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: Text (BytesText :: from_escaped ("\u{feff}"))) ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: Eof) ; } # [$ test] $ ($ async) ? fn declaration () { let mut reader = Reader :: from_str ("<?xml ?>") ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: Decl (BytesDecl :: from_start (BytesStart :: from_content ("xml " , 3)))) ; } # [$ test] $ ($ async) ? fn doctype () { let mut reader = Reader :: from_str ("<!DOCTYPE x>") ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: DocType (BytesText :: from_escaped ("x"))) ; } # [$ test] $ ($ async) ? fn processing_instruction () { let mut reader = Reader :: from_str ("<?xml-stylesheet '? >\" ?>") ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: PI (BytesPI :: new ("xml-stylesheet '? >\" "))) ; } # [doc = " Lone closing tags are not allowed, so testing it together with start tag"] # [$ test] $ ($ async) ? fn start_and_end () { let mut reader = Reader :: from_str ("<tag></tag>") ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: Start (BytesStart :: new ("tag"))) ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: End (BytesEnd :: new ("tag"))) ; } # [$ test] $ ($ async) ? fn empty () { let mut reader = Reader :: from_str ("<tag/>") ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: Empty (BytesStart :: new ("tag"))) ; } # [$ test] $ ($ async) ? fn text () { let mut reader = Reader :: from_str ("text") ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: Text (BytesText :: from_escaped ("text"))) ; } # [$ test] $ ($ async) ? fn cdata () { let mut reader = Reader :: from_str ("<![CDATA[]]>") ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: CData (BytesCData :: new (""))) ; } # [$ test] $ ($ async) ? fn comment () { let mut reader = Reader :: from_str ("<!---->") ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: Comment (BytesText :: from_escaped (""))) ; } # [$ test] $ ($ async) ? fn eof () { let mut reader = Reader :: from_str ("") ; assert_eq ! (reader .$ read_event ($ buf) $ (.$ await) ? . unwrap () , Event :: Eof) ; } } } ; } pub (super) use check ; }
};
}
