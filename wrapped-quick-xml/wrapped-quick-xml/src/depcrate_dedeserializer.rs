// Generated macro for Deserializer (struct)
macro_rules! Depcrate_deDeserializer {
() => {
// Module: crate::de
// Provides: {"Deserializer"}
// Dependencies: {}
# [doc = " A structure that deserializes XML into Rust values."] pub struct Deserializer < 'de , R , E : EntityResolver = PredefinedEntityResolver > where R : XmlRead < 'de > , { # [doc = " An XML reader that streams events into this deserializer"] reader : XmlReader < 'de , R , E > , # [doc = " When deserializing sequences sometimes we have to skip unwanted events."] # [doc = " That events should be stored and then replayed. This is a replay buffer,"] # [doc = " that streams events while not empty. When it exhausted, events will"] # [doc = " requested from [`Self::reader`]."] # [cfg (feature = "overlapped-lists")] read : VecDeque < DeEvent < 'de > > , # [doc = " When deserializing sequences sometimes we have to skip events, because XML"] # [doc = " is tolerant to elements order and even if in the XSD order is strictly"] # [doc = " specified (using `xs:sequence`) most of XML parsers allows order violations."] # [doc = " That means, that elements, forming a sequence, could be overlapped with"] # [doc = " other elements, do not related to that sequence."] # [doc = ""] # [doc = " In order to support this, deserializer will scan events and skip unwanted"] # [doc = " events, store them here. After call [`Self::start_replay()`] all events"] # [doc = " moved from this to [`Self::read`]."] # [cfg (feature = "overlapped-lists")] write : VecDeque < DeEvent < 'de > > , # [doc = " Maximum number of events that can be skipped when processing sequences"] # [doc = " that occur out-of-order. This field is used to prevent potential"] # [doc = " denial-of-service (DoS) attacks which could cause infinite memory"] # [doc = " consumption when parsing a very large amount of XML into a sequence field."] # [cfg (feature = "overlapped-lists")] limit : Option < NonZeroUsize > , # [cfg (not (feature = "overlapped-lists"))] peek : Option < DeEvent < 'de > > , # [doc = " Buffer to store attribute name as a field name exposed to serde consumers"] key_buf : String , }
};
}
