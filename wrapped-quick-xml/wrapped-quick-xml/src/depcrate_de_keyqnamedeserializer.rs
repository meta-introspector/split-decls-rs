// Generated macro for QNameDeserializer (struct)
macro_rules! Depcrate_de_keyQNameDeserializer {
() => {
// Module: crate::de::key
// Provides: {"QNameDeserializer"}
// Dependencies: {}
# [doc = " A deserializer for xml names of elements and attributes."] # [doc = ""] # [doc = " Used for deserializing values from:"] # [doc = " - attribute names (`<... name=\"...\" ...>`)"] # [doc = " - element names (`<name>...</name>`)"] # [doc = ""] # [doc = " Converts a name to an identifier string using the following rules:"] # [doc = ""] # [doc = " - if it is an [`attribute`] name, put `@` in front of the identifier"] # [doc = " - if it is a namespace binding (`xmlns` or `xmlns:xxx`) put the decoded name"] # [doc = "   to the identifier"] # [doc = " - if it is an attribute in the `xml` namespace, put the decoded name"] # [doc = "   to the identifier"] # [doc = " - put the decoded [`local_name()`] of a name to the identifier"] # [doc = ""] # [doc = " The final identifier looks like `[@]local_name`, or `@xmlns`, or `@xmlns:binding` or"] # [doc = " `xml:attribute` (where `[]` means optional element)."] # [doc = ""] # [doc = " The deserializer also supports deserializing names as other primitive types:"] # [doc = " - numbers"] # [doc = " - booleans"] # [doc = " - unit (`()`) and unit structs"] # [doc = " - unit variants of the enumerations"] # [doc = ""] # [doc = " Because `serde` does not define on which side type conversion should be"] # [doc = " performed, and because [`Deserialize`] implementation for that primitives"] # [doc = " in serde does not accept strings, the deserializer will perform conversion"] # [doc = " by itself."] # [doc = ""] # [doc = " The deserializer is able to deserialize unit and unit structs, but any name"] # [doc = " will be converted to the same unit instance. This is asymmetry with a serializer,"] # [doc = " which not able to serialize those types, because empty names are impossible"] # [doc = " in XML."] # [doc = ""] # [doc = " `deserialize_any()` returns the same result as `deserialize_identifier()`."] # [doc = ""] # [doc = " # Lifetimes"] # [doc = ""] # [doc = " - `'i`: lifetime of the data that the deserializer borrows from the parsed input"] # [doc = " - `'d`: lifetime of a deserializer that holds a buffer with content of events"] # [doc = ""] # [doc = " [`attribute`]: Self::from_attr"] # [doc = " [`local_name()`]: QName::local_name"] # [doc = " [`Deserialize`]: serde::Deserialize"] pub struct QNameDeserializer < 'i , 'd > { name : CowRef < 'i , 'd , str > , }
};
}
