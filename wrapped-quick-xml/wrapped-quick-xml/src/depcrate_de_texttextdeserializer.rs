// Generated macro for TextDeserializer (struct)
macro_rules! Depcrate_de_textTextDeserializer {
() => {
// Module: crate::de::text
// Provides: {"TextDeserializer"}
// Dependencies: {}
# [doc = " A deserializer for a single text node of a mixed sequence of tags and text."] # [doc = ""] # [doc = " This deserializer are very similar to a [`MapValueDeserializer`] (when it"] # [doc = " processes the [`DeEvent::Text`] event). The only difference in the"] # [doc = " `deserialize_seq` method. This deserializer will perform deserialization"] # [doc = " from a textual content, whereas the [`MapValueDeserializer`] will iterate"] # [doc = " over tags / text within it's parent tag."] # [doc = ""] # [doc = " This deserializer processes items as following:"] # [doc = " - numbers are parsed from a text content using [`FromStr`]; in case of error"] # [doc = "   [`Visitor::visit_borrowed_str`], [`Visitor::visit_str`], or [`Visitor::visit_string`]"] # [doc = "   is called; it is responsibility of the type to return an error if it does"] # [doc = "   not able to process passed data;"] # [doc = " - booleans converted from the text according to the XML [specification]:"] # [doc = "   - `\"true\"` and `\"1\"` converted to `true`;"] # [doc = "   - `\"false\"` and `\"0\"` converted to `false`;"] # [doc = "   - everything else calls [`Visitor::visit_borrowed_str`], [`Visitor::visit_str`],"] # [doc = "     or [`Visitor::visit_string`]; it is responsibility of the type to return"] # [doc = "     an error if it does not able to process passed data;"] # [doc = " - strings returned as is;"] # [doc = " - characters also returned as strings. If string contain more than one character"] # [doc = "   or empty, it is responsibility of a type to return an error;"] # [doc = " - `Option`:"] # [doc = "   - empty text is deserialized as `None`;"] # [doc = "   - everything else is deserialized as `Some` using the same deserializer;"] # [doc = " - units (`()`) and unit structs always deserialized successfully, the content is ignored;"] # [doc = " - newtype structs forwards deserialization to the inner type using the same"] # [doc = "   deserializer;"] # [doc = " - sequences, tuples and tuple structs are deserialized using [`SimpleTypeDeserializer`]"] # [doc = "   (this is the difference): text content passed to the deserializer directly;"] # [doc = " - structs and maps calls [`Visitor::visit_borrowed_str`] or [`Visitor::visit_string`],"] # [doc = "   it is responsibility of the type to return an error if it do not able to process"] # [doc = "   this data;"] # [doc = " - enums:"] # [doc = "   - the variant name is deserialized as `$text`;"] # [doc = "   - the content is deserialized using the same deserializer:"] # [doc = "     - unit variants: just return `()`;"] # [doc = "     - newtype variants forwards deserialization to the inner type using the"] # [doc = "       same deserializer;"] # [doc = "     - tuple and struct variants are deserialized using [`SimpleTypeDeserializer`]."] # [doc = ""] # [doc = " [`MapValueDeserializer`]: ../map/struct.MapValueDeserializer.html"] # [doc = " [`DeEvent::Text`]: crate::de::DeEvent::Text"] # [doc = " [`FromStr`]: std::str::FromStr"] # [doc = " [specification]: https://www.w3.org/TR/xmlschema11-2/#boolean"] pub struct TextDeserializer < 'de > (pub Text < 'de >) ;
};
}
