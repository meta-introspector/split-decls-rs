// Generated macro for Reader (struct)
macro_rules! Depcrate_readerReader {
() => {
// Module: crate::reader
// Provides: {"Reader"}
// Dependencies: {}
# [doc = " A low level encoding-agnostic XML event reader."] # [doc = ""] # [doc = " Consumes bytes and streams XML [`Event`]s."] # [doc = ""] # [doc = " This reader does not manage namespace declarations and not able to resolve"] # [doc = " prefixes. If you want these features, use the [`NsReader`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use quick_xml::events::Event;"] # [doc = " use quick_xml::reader::Reader;"] # [doc = ""] # [doc = " let xml = r#\"<tag1 att1 = \"test\">"] # [doc = "                 <tag2><!--Test comment-->Test</tag2>"] # [doc = "                 <tag2>Test 2</tag2>"] # [doc = "              </tag1>\"#;"] # [doc = " let mut reader = Reader::from_str(xml);"] # [doc = " reader.config_mut().trim_text(true);"] # [doc = ""] # [doc = " let mut count = 0;"] # [doc = " let mut txt = Vec::new();"] # [doc = " let mut buf = Vec::new();"] # [doc = ""] # [doc = " // The `Reader` does not implement `Iterator` because it outputs borrowed data (`Cow`s)"] # [doc = " loop {"] # [doc = "     // NOTE: this is the generic case when we don't know about the input BufRead."] # [doc = "     // when the input is a &str or a &[u8], we don't actually need to use another"] # [doc = "     // buffer, we could directly call `reader.read_event()`"] # [doc = "     match reader.read_event_into(&mut buf) {"] # [doc = "         Err(e) => panic!(\"Error at position {}: {:?}\", reader.error_position(), e),"] # [doc = "         // exits the loop when reaching end of file"] # [doc = "         Ok(Event::Eof) => break,"] # [doc = ""] # [doc = "         Ok(Event::Start(e)) => {"] # [doc = "             match e.name().as_ref() {"] # [doc = "                 b\"tag1\" => println!(\"attributes values: {:?}\","] # [doc = "                                     e.attributes().map(|a| a.unwrap().value)"] # [doc = "                                     .collect::<Vec<_>>()),"] # [doc = "                 b\"tag2\" => count += 1,"] # [doc = "                 _ => (),"] # [doc = "             }"] # [doc = "         }"] # [doc = "         Ok(Event::Text(e)) => txt.push(e.decode().unwrap().into_owned()),"] # [doc = ""] # [doc = "         // There are several other `Event`s we do not consider here"] # [doc = "         _ => (),"] # [doc = "     }"] # [doc = "     // if we don't keep a borrow elsewhere, we can clear the buffer to keep memory usage low"] # [doc = "     buf.clear();"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [`NsReader`]: crate::reader::NsReader"] # [derive (Debug , Clone)] pub struct Reader < R > { # [doc = " Source of data for parse"] reader : R , # [doc = " Configuration and current parse state"] state : ReaderState , }
};
}
