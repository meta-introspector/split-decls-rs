// Generated macro for impl_51 (impl)
macro_rules! Depcrate_export_blob_exporterimpl_51 {
() => {
// Module: crate::export::blob_exporter
// Provides: {"impl_51"}
// Dependencies: {}
impl BlobExporter < '_ > { fn finalize_buffers (& mut self) -> FinalizedBuffers { let sorted : Vec < (Vec < u8 > , usize) > = { let mut unique_resources = self . unique_resources . lock () . expect ("poison") ; let mut sorted : Vec < (Vec < u8 > , usize) > = unique_resources . drain () . collect () ; sorted . sort () ; sorted } ; let remap : HashMap < usize , usize > = sorted . iter () . enumerate () . map (| (new_id , (_ , old_id)) | (* old_id , new_id)) . collect () ; let vzv : VarZeroVec < [u8] , Index32 > = { let buffers : Vec < Vec < u8 > > = sorted . into_iter () . map (| (blob , _) | blob) . collect () ; buffers . as_slice () . into () } ; FinalizedBuffers { vzv , remap } } fn close_internal (& mut self) -> Result < ExporterCloseMetadata , DataError > { let FinalizedBuffers { mut vzv , remap } = self . finalize_buffers () ; let all_markers = self . all_markers . lock () . expect ("poison") ; let resources = self . resources . lock () . expect ("poison") ; let checksums = self . checksums . lock () . expect ("poison") ; let markers : ZeroVec < DataMarkerIdHash > = all_markers . iter () . copied () . collect () ; let locales_vec : Vec < Vec < u8 > > = all_markers . iter () . map (| marker_path_hash | { (resources . get (marker_path_hash) , checksums . get (marker_path_hash) ,) }) . map (| (option_sub_map , checksum) | { let mut sub_map = BTreeMap :: new () ; if let Some (sub_map_wrong) = option_sub_map { if let Some (& checksum) = checksum { sub_map . insert (CHECKSUM_KEY , vzv . len ()) ; vzv . zvl_push (checksum . to_le_bytes () . as_slice ()) ; } sub_map . extend (sub_map_wrong . iter () . map (| (key , id) | { (key . as_slice () , * remap . get (id) . expect ("in-bound index")) })) ; } ZeroTrieSimpleAscii :: try_from (& sub_map) . expect ("in-bounds") . into_store () }) . collect () ; if ! markers . is_empty () { if let Ok (locales_vzv) = VarZeroVecOwned :: < [u8] > :: try_from_elements (locales_vec . as_slice ()) { let blob = BlobSchema :: V003 (BlobSchemaV1 { markers : & markers , locales : & locales_vzv , buffers : & vzv , }) ; log :: info ! ("Serializing blob to output stream...") ; let output = postcard :: to_allocvec (& blob) ? ; self . sink . write_all (& output) ? ; } else { log :: info ! ("Upgrading to BlobSchema (bigger)...") ; let locales_vzv = VarZeroVecOwned :: < [u8] , Index32 > :: try_from_elements (locales_vec . as_slice ()) . expect ("Locales vector does not fit in Index32 buffer!") ; let blob = BlobSchema :: V003Bigger (BlobSchemaV1 { markers : & markers , locales : & locales_vzv , buffers : & vzv , }) ; log :: info ! ("Serializing blob to output stream...") ; let output = postcard :: to_allocvec (& blob) ? ; self . sink . write_all (& output) ? ; } } Ok (Default :: default ()) } }
};
}
