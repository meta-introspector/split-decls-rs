// Generated macro for impl_68 (impl)
macro_rules! Depcrate_verifying_keyimpl_68 {
() => {
// Module: crate::verifying_key
// Provides: {"impl_68"}
// Dependencies: {}
impl VerifyingKey { # [doc = " Construct a new public key from the common components and the public component"] pub fn from_components (components : Components , y : BoxedUint) -> signature :: Result < Self > { let y = NonZero :: new (y) . into_option () . ok_or_else (signature :: Error :: new) ? ; let params = BoxedMontyParams :: new_vartime (components . p () . clone ()) ; let form = BoxedMontyForm :: new ((* y) . clone () , params) ; if * y < two () || form . pow (components . q ()) . retrieve () != BoxedUint :: one () { return Err (signature :: Error :: new ()) ; } Ok (Self { components , y }) } # [doc = " DSA common components"] pub const fn components (& self) -> & Components { & self . components } # [doc = " DSA public component"] # [must_use] pub const fn y (& self) -> & NonZero < BoxedUint > { & self . y } # [doc = " Verify some prehashed data"] # [must_use] fn verify_prehashed (& self , hash : & [u8] , signature : & Signature) -> Option < bool > { let components = self . components () ; let (p , q , g) = (components . p () , components . q () , components . g ()) ; let (r , s) = (signature . r () , signature . s ()) ; let y = self . y () ; if r >= q || s >= q { return Some (false) ; } let q = & q . resize (p . bits_precision ()) ; let r = & r . resize (p . bits_precision ()) ; let s = & s . resize (p . bits_precision ()) ; let w : BoxedUint = Option :: from (s . invert_mod (q)) ? ; let n = q . bits () / 8 ; let block_size = hash . len () ; let z_len = min (n as usize , block_size) ; let z = BoxedUint :: from_be_slice (& hash [.. z_len] , z_len as u32 * 8) . expect ("invariant violation") ; let z = z . resize (p . bits_precision ()) ; let w = w . resize (q . bits_precision ()) ; let u1 = (& z * & w) % q . resize (p . bits_precision ()) ; let u2 = r . mul_mod (& w , q) ; let p1_params = BoxedMontyParams :: new (p . clone ()) ; let p2_params = BoxedMontyParams :: new (p . clone ()) ; let g_form = BoxedMontyForm :: new ((* * g) . clone () , p1_params) ; let y_form = BoxedMontyForm :: new ((* * y) . clone () , p2_params) ; let v1 = g_form . pow (& u1) . retrieve () ; let v2 = y_form . pow (& u2) . retrieve () ; let v3 = v1 * v2 ; let p = p . resize (v3 . bits_precision ()) ; let q = q . resize (v3 . bits_precision ()) ; let v4 = v3 % p . as_nz_ref () ; let v = v4 % q ; Some (v == * * r) } }
};
}
