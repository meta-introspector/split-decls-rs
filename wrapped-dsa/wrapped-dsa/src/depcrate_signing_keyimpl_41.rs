// Generated macro for impl_41 (impl)
macro_rules! Depcrate_signing_keyimpl_41 {
() => {
// Module: crate::signing_key
// Provides: {"impl_41"}
// Dependencies: {}
impl SigningKey { # [doc = " Construct a new private key from the public key and private component"] pub fn from_components (verifying_key : VerifyingKey , x : BoxedUint) -> signature :: Result < Self > { let x = NonZero :: new (x) . into_option () . ok_or_else (signature :: Error :: new) ? ; if x > * verifying_key . components () . q () { return Err (signature :: Error :: new ()) ; } Ok (Self { verifying_key , x : Zeroizing :: new (x) , }) } # [doc = " Generate a new DSA keypair"] # [cfg (feature = "hazmat")] # [inline] pub fn generate < R : CryptoRng + ? Sized > (rng : & mut R , components : Components) -> SigningKey { crate :: generate :: keypair (rng , components) } # [doc = " DSA public key"] pub const fn verifying_key (& self) -> & VerifyingKey { & self . verifying_key } # [doc = " DSA private component"] # [doc = ""] # [doc = " If you decide to clone this value, please consider using [`Zeroize::zeroize`](::zeroize::Zeroize::zeroize()) to zero out the memory after you're done using the clone"] # [must_use] pub fn x (& self) -> & NonZero < BoxedUint > { & self . x } # [doc = " Try to sign the given message digest deterministically with a prehashed digest."] # [doc = " The parameter `D` must match the hash function used to sign the digest."] # [doc = ""] # [doc = " [RFC6979]: https://datatracker.ietf.org/doc/html/rfc6979"] # [cfg (feature = "hazmat")] pub fn sign_prehashed_rfc6979 < D > (& self , prehash : & [u8]) -> Result < Signature , signature :: Error > where D : EagerHash , { let k_kinv = crate :: generate :: secret_number_rfc6979 :: < D > (self , prehash) ? ; self . sign_prehashed (k_kinv , prehash) } # [doc = " Sign some pre-hashed data"] fn sign_prehashed (& self , (k , inv_k) : (BoxedUint , BoxedUint) , hash : & [u8] ,) -> signature :: Result < Signature > { let components = self . verifying_key () . components () ; let key_size = & components . key_size ; let (p , q , g) = (components . p () , components . q () , components . g ()) ; let x = self . x () ; debug_assert_eq ! (key_size . n_aligned () , q . bits_precision ()) ; let x = x . resize (p . bits_precision ()) ; let x = & x ; let k = k . resize (p . bits_precision ()) ; let inv_k = inv_k . resize (p . bits_precision ()) ; let params = BoxedMontyParams :: new (p . clone ()) ; let form = BoxedMontyForm :: new ((* * g) . clone () , params) ; let r = form . pow (& k) . retrieve () % q . resize (p . bits_precision ()) ; debug_assert_eq ! (key_size . l_aligned () , r . bits_precision ()) ; let r_short = r . clone () . resize (key_size . n_aligned ()) ; let n = q . bits () / 8 ; let block_size = hash . len () ; let z_len = min (n as usize , block_size) ; let z = BoxedUint :: from_be_slice (& hash [.. z_len] , z_len as u32 * 8) . expect ("invariant violation") ; let s = inv_k . mul_mod (& (z + & * * x * & r) , & q . resize (key_size . l_aligned ())) ; let s = s . resize (key_size . n_aligned ()) ; debug_assert_eq ! (key_size . n_aligned () , r_short . bits_precision ()) ; debug_assert_eq ! (key_size . n_aligned () , s . bits_precision ()) ; let signature = Signature :: from_components (r_short , s) . ok_or_else (signature :: Error :: new) ? ; if signature . r () < q && signature . s () < q { Ok (signature) } else { Err (signature :: Error :: new ()) } } }
};
}
