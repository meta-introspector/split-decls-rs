// Generated macro for Driver (struct)
macro_rules! Depcrate_blobDriver {
() => {
// Module: crate::blob
// Provides: {"Driver"}
// Dependencies: {}
# [doc = " A set of values to define how to diff something that is associated with it using `git-attributes`, relevant for regular files."] # [doc = ""] # [doc = " Some values are related to diffing, some are related to conversions."] # [derive (Default , Debug , Clone , PartialEq , Eq)] pub struct Driver { # [doc = " The name of the driver, as referred to by `[diff \"name\"]` in the git configuration."] pub name : BString , # [doc = " The command to execute to perform the diff entirely like `<command> old-file old-hex old-mode new-file new-hex new-mode`."] # [doc = ""] # [doc = " Please note that we don't make this call ourselves, but use it to determine that we should not run the our standard"] # [doc = " built-in algorithm but bail instead as the output of such a program isn't standardized."] pub command : Option < BString > , # [doc = " The per-driver algorithm to use."] pub algorithm : Option < Algorithm > , # [doc = " The external filter program to call like `<binary_to_text_command> /path/to/blob` which outputs a textual version of the provided"] # [doc = " binary file."] # [doc = " Note that it's invoked with a shell if arguments are given."] # [doc = " Further, if present, it will always be executed, whether `is_binary` is set or not."] pub binary_to_text_command : Option < BString > , # [doc = " `Some(true)` if this driver deals with binary files, which means that a `binary_to_text_command` should be used to convert binary"] # [doc = " into a textual representation."] # [doc = " Without such a command, anything that is considered binary is not diffed, but only the size of its data is made available."] # [doc = " If `Some(false)`, it won't be considered binary, and the its data will not be sampled for the null-byte either."] # [doc = " Leaving it to `None` means binary detection is automatic, and is based on the presence of the `0` byte in the first 8kB of the buffer."] pub is_binary : Option < bool > , }
};
}
