// Generated macro for impl_27 (impl)
macro_rules! Depcrateimpl_27 {
() => {
// Module: crate
// Provides: {"impl_27"}
// Dependencies: {}
impl Client { # [doc = " Creates a new jobserver initialized with the given parallelism limit."] # [doc = ""] # [doc = " A client to the jobserver created will be returned. This client will"] # [doc = " allow at most `limit` tokens to be acquired from it in parallel. More"] # [doc = " calls to [`Client::acquire`] will cause the calling thread to block."] # [doc = ""] # [doc = " Note that the created [`Client`] is not automatically inherited into"] # [doc = " spawned child processes from this program. Manual usage of the"] # [doc = " [`Client::configure`] function is required for a child process to have"] # [doc = " access to a job server."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use jobserver::Client;"] # [doc = ""] # [doc = " let client = Client::new(4).expect(\"failed to create jobserver\");"] # [doc = " ```"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns an error if any I/O error happens when attempting to create the"] # [doc = " jobserver client."] pub fn new (limit : usize) -> io :: Result < Client > { Ok (Client { inner : Arc :: new (imp :: Client :: new (limit) ?) , }) } # [doc = " Attempts to connect to the jobserver specified in this process's"] # [doc = " environment."] # [doc = ""] # [doc = " When the a `make` executable calls a child process it will configure the"] # [doc = " environment of the child to ensure that it has handles to the jobserver"] # [doc = " it's passing down. This function will attempt to look for these details"] # [doc = " and connect to the jobserver."] # [doc = ""] # [doc = " Note that the created [`Client`] is not automatically inherited into"] # [doc = " spawned child processes from this program. Manual usage of the"] # [doc = " [`Client::configure`] function is required for a child process to have"] # [doc = " access to a job server."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " [`FromEnv`] contains result and relevant environment variable."] # [doc = " If a jobserver was found in the environment and it looks correct then"] # [doc = " result with the connected client will be returned. In other cases"] # [doc = " result will contain `Err(FromEnvErr)`."] # [doc = ""] # [doc = " Additionally on Unix this function will configure the file descriptors"] # [doc = " with `CLOEXEC` so they're not automatically inherited by spawned"] # [doc = " children."] # [doc = ""] # [doc = " On unix if `check_pipe` enabled this function will check if provided"] # [doc = " files are actually pipes."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is `unsafe` to call on Unix specifically as it"] # [doc = " transitively requires usage of the `from_raw_fd` function, which is"] # [doc = " itself unsafe in some circumstances."] # [doc = ""] # [doc = " It's recommended to call this function very early in the lifetime of a"] # [doc = " program before any other file descriptors are opened. That way you can"] # [doc = " make sure to take ownership properly of the file descriptors passed"] # [doc = " down, if any."] # [doc = ""] # [doc = " It is ok to call this function any number of times."] pub unsafe fn from_env_ext (check_pipe : bool) -> FromEnv { let (env , var_os) = match ["CARGO_MAKEFLAGS" , "MAKEFLAGS" , "MFLAGS"] . iter () . map (| & env | env :: var_os (env) . map (| var | (env , var))) . find_map (| p | p) { Some ((env , var_os)) => (env , var_os) , None => return FromEnv :: new_err (FromEnvErrorInner :: NoEnvVar , "" , Default :: default ()) , } ; let var = match var_os . to_str () { Some (var) => var , None => { let err = FromEnvErrorInner :: CannotParse ("not valid UTF-8" . to_string ()) ; return FromEnv :: new_err (err , env , var_os) ; } } ; let s = match find_jobserver_auth (var) { Some (s) => s , None => return FromEnv :: new_err (FromEnvErrorInner :: NoJobserver , env , var_os) , } ; match imp :: Client :: open (s , check_pipe) { Ok (c) => FromEnv :: new_ok (Client { inner : Arc :: new (c) } , env , var_os) , Err (err) => FromEnv :: new_err (err , env , var_os) , } } # [doc = " Attempts to connect to the jobserver specified in this process's"] # [doc = " environment."] # [doc = ""] # [doc = " Wraps [`Client::from_env_ext`] and discards error details."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is `unsafe` to call on Unix specifically as it"] # [doc = " transitively requires usage of the `from_raw_fd` function, which is"] # [doc = " itself unsafe in some circumstances."] # [doc = ""] # [doc = " It's recommended to call this function very early in the lifetime of a"] # [doc = " program before any other file descriptors are opened. That way you can"] # [doc = " make sure to take ownership properly of the file descriptors passed"] # [doc = " down, if any."] # [doc = ""] # [doc = " It is ok to call this function any number of times."] pub unsafe fn from_env () -> Option < Client > { Self :: from_env_ext (false) . client . ok () } # [doc = " Acquires a token from this jobserver client."] # [doc = ""] # [doc = " This function will block the calling thread until a new token can be"] # [doc = " acquired from the jobserver."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " On successful acquisition of a token an instance of [`Acquired`] is"] # [doc = " returned. This structure, when dropped, will release the token back to"] # [doc = " the jobserver. It's recommended to avoid leaking this value."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If an I/O error happens while acquiring a token then this function will"] # [doc = " return immediately with the error. If an error is returned then a token"] # [doc = " was not acquired."] pub fn acquire (& self) -> io :: Result < Acquired > { let data = self . inner . acquire () ? ; Ok (Acquired { client : self . inner . clone () , data , disabled : false , }) } # [doc = " Acquires a token from this jobserver client in a non-blocking way."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " On successful acquisition of a token an instance of [`Acquired`] is"] # [doc = " returned. This structure, when dropped, will release the token back to"] # [doc = " the jobserver. It's recommended to avoid leaking this value."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If an I/O error happens while acquiring a token then this function will"] # [doc = " return immediately with the error. If an error is returned then a token"] # [doc = " was not acquired."] # [doc = ""] # [doc = " If non-blocking acquire is not supported, the return error will have its `kind()`"] # [doc = " set to [`io::ErrorKind::Unsupported`]."] pub fn try_acquire (& self) -> io :: Result < Option < Acquired > > { let ret = self . inner . try_acquire () ? ; Ok (ret . map (| data | Acquired { client : self . inner . clone () , data , disabled : false , })) } # [doc = " Returns amount of tokens in the read-side pipe."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " Number of bytes available to be read from the jobserver pipe"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Underlying errors from the ioctl will be passed up."] pub fn available (& self) -> io :: Result < usize > { self . inner . available () } # [doc = " Configures a child process to have access to this client's jobserver as"] # [doc = " well."] # [doc = ""] # [doc = " This function is required to be called to ensure that a jobserver is"] # [doc = " properly inherited to a child process. If this function is *not* called"] # [doc = " then this [`Client`] will not be accessible in the child process. In"] # [doc = " other words, if not called, then [`Client::from_env`] will return `None`"] # [doc = " in the child process (or the equivalent of [`Client::from_env`] that"] # [doc = " `make` uses)."] # [doc = ""] # [doc = " ## Platform-specific behavior"] # [doc = ""] # [doc = " On Unix and Windows this will clobber the `CARGO_MAKEFLAGS` environment"] # [doc = " variables for the child process, and on Unix this will also allow the"] # [doc = " two file descriptors for this client to be inherited to the child."] # [doc = ""] # [doc = " On platforms other than Unix and Windows this panics."] pub fn configure (& self , cmd : & mut Command) { cmd . env ("CARGO_MAKEFLAGS" , & self . mflags_env ()) ; self . inner . configure (cmd) ; } # [doc = " Configures a child process to have access to this client's jobserver as"] # [doc = " well."] # [doc = ""] # [doc = " This function is required to be called to ensure that a jobserver is"] # [doc = " properly inherited to a child process. If this function is *not* called"] # [doc = " then this [`Client`] will not be accessible in the child process. In"] # [doc = " other words, if not called, then [`Client::from_env`] will return `None`"] # [doc = " in the child process (or the equivalent of [`Client::from_env`] that"] # [doc = " `make` uses)."] # [doc = ""] # [doc = " ## Platform-specific behavior"] # [doc = ""] # [doc = " On Unix and Windows this will clobber the `CARGO_MAKEFLAGS`,"] # [doc = " `MAKEFLAGS` and `MFLAGS` environment variables for the child process,"] # [doc = " and on Unix this will also allow the two file descriptors for"] # [doc = " this client to be inherited to the child."] # [doc = ""] # [doc = " On platforms other than Unix and Windows this panics."] pub fn configure_make (& self , cmd : & mut Command) { let value = self . mflags_env () ; cmd . env ("CARGO_MAKEFLAGS" , & value) ; cmd . env ("MAKEFLAGS" , & value) ; cmd . env ("MFLAGS" , & value) ; self . inner . configure (cmd) ; } fn mflags_env (& self) -> String { let arg = self . inner . string_arg () ; format ! ("-j --jobserver-fds={0} --jobserver-auth={0}" , arg) } # [doc = " Converts this [`Client`] into a helper thread to deal with a blocking"] # [doc = " [`Client::acquire`] function a little more easily."] # [doc = ""] # [doc = " The fact that the [`Client::acquire`] isn't always the easiest to work"] # [doc = " with. Typically you're using a jobserver to manage running other events"] # [doc = " in parallel! This means that you need to either (a) wait for an existing"] # [doc = " job to finish or (b) wait for a new token to become available."] # [doc = ""] # [doc = " Unfortunately the blocking in [`Client::acquire`] happens at the"] # [doc = " implementation layer of jobservers. On Unix this requires a blocking"] # [doc = " call to `read` and on Windows this requires one of the `WaitFor*`"] # [doc = " functions. Both of these situations aren't the easiest to deal with:"] # [doc = ""] # [doc = " * On Unix there's basically only one way to wake up a `read` early, and"] # [doc = "   that's through a signal. This is what the `make` implementation"] # [doc = "   itself uses, relying on `SIGCHLD` to wake up a blocking acquisition"] # [doc = "   of a new job token. Unfortunately nonblocking I/O is not an option"] # [doc = "   here, so it means that \"waiting for one of two events\" means that"] # [doc = "   the latter event must generate a signal! This is not always the case"] # [doc = "   on unix for all jobservers."] # [doc = ""] # [doc = " * On Windows you'd have to basically use the `WaitForMultipleObjects`"] # [doc = "   which means that you've got to canonicalize all your event sources"] # [doc = "   into a `HANDLE` which also isn't the easiest thing to do"] # [doc = "   unfortunately."] # [doc = ""] # [doc = " This function essentially attempts to ease these limitations by"] # [doc = " converting this [`Client`] into a helper thread spawned into this"] # [doc = " process. The application can then request that the helper thread"] # [doc = " acquires tokens and the provided closure will be invoked for each token"] # [doc = " acquired."] # [doc = ""] # [doc = " The intention is that this function can be used to translate the event"] # [doc = " of a token acquisition into an arbitrary user-defined event."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " This function will consume the [`Client`] provided to be transferred to"] # [doc = " the helper thread that is spawned. Additionally a closure `f` is"] # [doc = " provided to be invoked whenever a token is acquired."] # [doc = ""] # [doc = " This closure is only invoked after calls to"] # [doc = " [`HelperThread::request_token`] have been made and a token itself has"] # [doc = " been acquired. If an error happens while acquiring the token then"] # [doc = " an error will be yielded to the closure as well."] # [doc = ""] # [doc = " # Return Value"] # [doc = ""] # [doc = " This function will return an instance of the [`HelperThread`] structure"] # [doc = " which is used to manage the helper thread associated with this client."] # [doc = " Through the [`HelperThread`] you'll request that tokens are acquired."] # [doc = " When acquired, the closure provided here is invoked."] # [doc = ""] # [doc = " When the [`HelperThread`] structure is returned it will be gracefully"] # [doc = " torn down, and the calling thread will be blocked until the thread is"] # [doc = " torn down (which should be prompt)."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function may fail due to creation of the helper thread or"] # [doc = " auxiliary I/O objects to manage the helper thread. In any of these"] # [doc = " situations the error is propagated upwards."] # [doc = ""] # [doc = " # Platform-specific behavior"] # [doc = ""] # [doc = " On Windows this function behaves pretty normally as expected, but on"] # [doc = " Unix the implementation is... a little heinous. As mentioned above"] # [doc = " we're forced into blocking I/O for token acquisition, namely a blocking"] # [doc = " call to `read`. We must be able to unblock this, however, to tear down"] # [doc = " the helper thread gracefully!"] # [doc = ""] # [doc = " Essentially what happens is that we'll send a signal to the helper"] # [doc = " thread spawned and rely on `EINTR` being returned to wake up the helper"] # [doc = " thread. This involves installing a global `SIGUSR1` handler that does"] # [doc = " nothing along with sending signals to that thread. This may cause"] # [doc = " odd behavior in some applications, so it's recommended to review and"] # [doc = " test thoroughly before using this."] pub fn into_helper_thread < F > (self , f : F) -> io :: Result < HelperThread > where F : FnMut (io :: Result < Acquired >) + Send + 'static , { let state = Arc :: new (HelperState :: default ()) ; Ok (HelperThread { inner : Some (imp :: spawn_helper (self , state . clone () , Box :: new (f)) ?) , state , }) } # [doc = " Blocks the current thread until a token is acquired."] # [doc = ""] # [doc = " This is the same as [`Client::acquire`], except that it doesn't return"] # [doc = " an RAII helper. If successful the process will need to guarantee that"] # [doc = " [`Client::release_raw`] is called in the future."] pub fn acquire_raw (& self) -> io :: Result < () > { self . inner . acquire () ? ; Ok (()) } # [doc = " Releases a jobserver token back to the original jobserver."] # [doc = ""] # [doc = " This is intended to be paired with [`Client::acquire_raw`] if it was"] # [doc = " called, but in some situations it could also be called to relinquish a"] # [doc = " process's implicit token temporarily which is then re-acquired later."] pub fn release_raw (& self) -> io :: Result < () > { self . inner . release (None) ? ; Ok (()) } }
};
}
