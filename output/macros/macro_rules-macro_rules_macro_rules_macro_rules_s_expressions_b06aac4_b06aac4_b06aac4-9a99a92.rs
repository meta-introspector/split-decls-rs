#[doc = " So far, we've been working with a homogeneous untyped tree."] #[doc = " It's nice to provide generic tree operations, like traversals,"] #[doc = " but it's a bad fit for semantic analysis."] #[doc = " This crate itself does not provide AST facilities directly,"] #[doc = " but it is possible to layer AST on top of `SyntaxNode` API."] #[doc = " Let's write a function to evaluate S-expression."] #[doc = ""] #[doc = " For that, let's define AST nodes."] #[doc = " It'll be quite a bunch of repetitive code, so we'll use a macro."] #[doc = ""] #[doc = " For a real language, you'd want to generate an AST. I find a"] #[doc = " combination of `serde`, `ron` and `tera` crates invaluable for that!"] macro_rules ! ast_node { ($ ast : ident , $ kind : ident) => { #[derive (PartialEq , Eq , Hash)] #[repr (transparent)] struct $ ast (SyntaxNode) ; impl $ ast { #[allow (unused)] fn cast (node : SyntaxNode) -> Option < Self > { if node . kind () == $ kind { Some (Self (node))}
else { None}
}}
} ; }