// Generated macro for allocator (module)
macro_rules! Depcrateallocator {
() => {
// Module: crate
// Provides: {"allocator"}
// Dependencies: {}
# [cfg (all (target_arch = "wasm32" , not (feature = "std")))] # [expect (unsafe_code , reason = "no way to implement `GlobalAlloc` without unsafe")] mod allocator { # ! [doc = " Implementing [`GlobalAlloc`]."] # ! [doc = ""] # ! [doc = " See <https://github.com/rust-lang/rust/blob/1.82.0/library/std/src/sys/alloc/wasm.rs>."] use core :: alloc :: { GlobalAlloc , Layout } ; use dlmalloc :: Dlmalloc ; # [doc = " The allocator."] static mut DLMALLOC : Dlmalloc = Dlmalloc :: new () ; # [doc = " Global allocator."] # [global_allocator] static ALLOC : System = System ; # [doc = " Implementing [`GlobalAlloc`]."] struct System ; unsafe impl GlobalAlloc for System { # [inline] unsafe fn alloc (& self , layout : Layout) -> * mut u8 { let _lock = lock :: lock () ; unsafe { (* core :: ptr :: addr_of_mut ! (DLMALLOC)) . malloc (layout . size () , layout . align ()) } } # [inline] unsafe fn alloc_zeroed (& self , layout : Layout) -> * mut u8 { let _lock = lock :: lock () ; unsafe { (* core :: ptr :: addr_of_mut ! (DLMALLOC)) . calloc (layout . size () , layout . align ()) } } # [inline] unsafe fn dealloc (& self , ptr : * mut u8 , layout : Layout) { let _lock = lock :: lock () ; unsafe { (* core :: ptr :: addr_of_mut ! (DLMALLOC)) . free (ptr , layout . size () , layout . align ()) } } # [inline] unsafe fn realloc (& self , ptr : * mut u8 , layout : Layout , new_size : usize) -> * mut u8 { let _lock = lock :: lock () ; unsafe { (* core :: ptr :: addr_of_mut ! (DLMALLOC)) . realloc (ptr , layout . size () , layout . align () , new_size ,) } } } # [doc = " The lock guard."] # [must_use = "if unused it will immediately unlock"] struct DropLock ; # [doc = " Lock implementation."] # [cfg (target_feature = "atomics")] mod lock { use core :: sync :: atomic :: { AtomicBool , Ordering } ; use super :: DropLock ; # [doc = " The lock flag."] static LOCKED : AtomicBool = AtomicBool :: new (false) ; # [doc = " Locks the thread until available."] pub (super) fn lock () -> DropLock { loop { if ! LOCKED . swap (true , Ordering :: Acquire) { return DropLock ; } } } impl Drop for DropLock { fn drop (& mut self) { LOCKED . swap (false , Ordering :: Release) ; } } } # [doc = " Empty lock implementation when threads are not available."] # [cfg (not (target_feature = "atomics"))] mod lock { use super :: DropLock ; # [doc = " Locks the thread until available."] # [expect (clippy :: missing_const_for_fn , reason = "compatibility with non-atomic lock")] pub (super) fn lock () -> DropLock { DropLock } } }
};
}
