// Generated macro for impl_195 (impl)
macro_rules! Depcrateimpl_195 {
() => {
// Module: crate
// Provides: {"impl_195"}
// Dependencies: {}
impl Hash { # [doc = " The raw bytes of the `Hash`. Note that byte arrays don't provide"] # [doc = " constant-time equality checking, so if  you need to compare hashes,"] # [doc = " prefer the `Hash` type."] # [inline] pub const fn as_bytes (& self) -> & [u8 ; OUT_LEN] { & self . 0 } # [doc = " Create a `Hash` from its raw bytes representation."] pub const fn from_bytes (bytes : [u8 ; OUT_LEN]) -> Self { Self (bytes) } # [doc = " Create a `Hash` from its raw bytes representation as a slice."] # [doc = ""] # [doc = " Returns an error if the slice is not exactly 32 bytes long."] pub fn from_slice (bytes : & [u8]) -> Result < Self , core :: array :: TryFromSliceError > { Ok (Self :: from_bytes (bytes . try_into () ?)) } # [doc = " Encode a `Hash` in lowercase hexadecimal."] # [doc = ""] # [doc = " The returned [`ArrayString`] is a fixed size and doesn't allocate memory"] # [doc = " on the heap. Note that [`ArrayString`] doesn't provide constant-time"] # [doc = " equality checking, so if you need to compare hashes, prefer the `Hash`"] # [doc = " type."] # [doc = ""] # [doc = " [`ArrayString`]: https://docs.rs/arrayvec/0.5.1/arrayvec/struct.ArrayString.html"] pub fn to_hex (& self) -> ArrayString < { 2 * OUT_LEN } > { let mut s = ArrayString :: new () ; let table = b"0123456789abcdef" ; for & b in self . 0 . iter () { s . push (table [(b >> 4) as usize] as char) ; s . push (table [(b & 0xf) as usize] as char) ; } s } # [doc = " Decode a `Hash` from hexadecimal. Both uppercase and lowercase ASCII"] # [doc = " bytes are supported."] # [doc = ""] # [doc = " Any byte outside the ranges `'0'...'9'`, `'a'...'f'`, and `'A'...'F'`"] # [doc = " results in an error. An input length other than 64 also results in an"] # [doc = " error."] # [doc = ""] # [doc = " Note that `Hash` also implements `FromStr`, so `Hash::from_hex(\"...\")`"] # [doc = " is equivalent to `\"...\".parse()`."] pub fn from_hex (hex : impl AsRef < [u8] >) -> Result < Self , HexError > { fn hex_val (byte : u8) -> Result < u8 , HexError > { match byte { b'A' ..= b'F' => Ok (byte - b'A' + 10) , b'a' ..= b'f' => Ok (byte - b'a' + 10) , b'0' ..= b'9' => Ok (byte - b'0') , _ => Err (HexError (HexErrorInner :: InvalidByte (byte))) , } } let hex_bytes : & [u8] = hex . as_ref () ; if hex_bytes . len () != OUT_LEN * 2 { return Err (HexError (HexErrorInner :: InvalidLen (hex_bytes . len ()))) ; } let mut hash_bytes : [u8 ; OUT_LEN] = [0 ; OUT_LEN] ; for i in 0 .. OUT_LEN { hash_bytes [i] = 16 * hex_val (hex_bytes [2 * i]) ? + hex_val (hex_bytes [2 * i + 1]) ? ; } Ok (Hash :: from (hash_bytes)) } }
};
}
