// Generated macro for derive_key (function)
macro_rules! Depcratederive_key {
() => {
// Module: crate
// Provides: {"derive_key"}
// Dependencies: {}
# [doc = " The key derivation function."] # [doc = ""] # [doc = " Given cryptographic key material of any length and a context string of any"] # [doc = " length, this function outputs a 32-byte derived subkey. **The context string"] # [doc = " should be hardcoded, globally unique, and application-specific.** A good"] # [doc = " default format for such strings is `\"[application] [commit timestamp]"] # [doc = " [purpose]\"`, e.g., `\"example.com 2019-12-25 16:18:03 session tokens v1\"`."] # [doc = ""] # [doc = " Key derivation is important when you want to use the same key in multiple"] # [doc = " algorithms or use cases. Using the same key with different cryptographic"] # [doc = " algorithms is generally forbidden, and deriving a separate subkey for each"] # [doc = " use case protects you from bad interactions. Derived keys also mitigate the"] # [doc = " damage from one part of your application accidentally leaking its key."] # [doc = ""] # [doc = " As a rare exception to that general rule, however, it is possible to use"] # [doc = " `derive_key` itself with key material that you are already using with"] # [doc = " another algorithm. You might need to do this if you're adding features to"] # [doc = " an existing application, which does not yet use key derivation internally."] # [doc = " However, you still must not share key material with algorithms that forbid"] # [doc = " key reuse entirely, like a one-time pad. For more on this, see sections 6.2"] # [doc = " and 7.8 of the [BLAKE3 paper](https://github.com/BLAKE3-team/BLAKE3-specs/blob/master/blake3.pdf)."] # [doc = ""] # [doc = " Note that BLAKE3 is not a password hash, and **`derive_key` should never be"] # [doc = " used with passwords.** Instead, use a dedicated password hash like"] # [doc = " [Argon2]. Password hashes are entirely different from generic hash"] # [doc = " functions, with opposite design requirements."] # [doc = ""] # [doc = " For an incremental version that accepts multiple writes, see [`Hasher::new_derive_key`],"] # [doc = " [`Hasher::update`], and [`Hasher::finalize`]. These two statements are equivalent:"] # [doc = ""] # [doc = " ```"] # [doc = " # const CONTEXT: &str = \"example.com 2019-12-25 16:18:03 session tokens v1\";"] # [doc = " let key = blake3::derive_key(CONTEXT, b\"key material, not a password\");"] # [doc = " # let key1 = key;"] # [doc = ""] # [doc = " let key: [u8; 32] = blake3::Hasher::new_derive_key(CONTEXT)"] # [doc = "     .update(b\"key material, not a password\")"] # [doc = "     .finalize()"] # [doc = "     .into();"] # [doc = " # let key2 = key;"] # [doc = " # assert_eq!(key1, key2);"] # [doc = " ```"] # [doc = ""] # [doc = " For output sizes other than 32 bytes, see [`Hasher::finalize_xof`], and [`OutputReader`]."] # [doc = ""] # [doc = " This function is always single-threaded. For multithreading support, see"] # [doc = " [`Hasher::update_rayon`](struct.Hasher.html#method.update_rayon)."] # [doc = ""] # [doc = " [Argon2]: https://en.wikipedia.org/wiki/Argon2"] pub fn derive_key (context : & str , key_material : & [u8]) -> [u8 ; OUT_LEN] { let context_key = hazmat :: hash_derive_key_context (context) ; let context_key_words = platform :: words_from_le_bytes_32 (& context_key) ; hash_all_at_once :: < join :: SerialJoin > (key_material , & context_key_words , DERIVE_KEY_MATERIAL) . root_hash () . 0 }
};
}
