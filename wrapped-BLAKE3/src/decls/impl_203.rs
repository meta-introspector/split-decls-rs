macro_rules! deps {
    () => {
        OutputReader!();
        ChunkState!();
        Output!();
        Hash!();
        SerialJoin!();
        RayonJoin!();
        Hasher!();
        CVWords!();
        Join!();
        CVBytes!();
        Platform!();
    };
}

macro_rules! impl_203 {
    () => {
        deps!();
        impl Hasher { fn new_internal (key : & CVWords , flags : u8) -> Self { Self { key : * key , chunk_state : ChunkState :: new (key , 0 , flags , Platform :: detect ()) , initial_chunk_counter : 0 , cv_stack : ArrayVec :: new () , } } # [doc = " Construct a new `Hasher` for the regular hash function."] pub fn new () -> Self { Self :: new_internal (IV , 0) } # [doc = " Construct a new `Hasher` for the keyed hash function. See"] # [doc = " [`keyed_hash`]."] # [doc = ""] # [doc = " [`keyed_hash`]: fn.keyed_hash.html"] pub fn new_keyed (key : & [u8 ; KEY_LEN]) -> Self { let key_words = platform :: words_from_le_bytes_32 (key) ; Self :: new_internal (& key_words , KEYED_HASH) } # [doc = " Construct a new `Hasher` for the key derivation function. See"] # [doc = " [`derive_key`]. The context string should be hardcoded, globally"] # [doc = " unique, and application-specific."] # [doc = ""] # [doc = " [`derive_key`]: fn.derive_key.html"] pub fn new_derive_key (context : & str) -> Self { let context_key = hazmat :: hash_derive_key_context (context) ; let context_key_words = platform :: words_from_le_bytes_32 (& context_key) ; Self :: new_internal (& context_key_words , DERIVE_KEY_MATERIAL) } # [doc = " Reset the `Hasher` to its initial state."] # [doc = ""] # [doc = " This is functionally the same as overwriting the `Hasher` with a new"] # [doc = " one, using the same key or context string if any."] pub fn reset (& mut self) -> & mut Self { self . chunk_state = ChunkState :: new (& self . key , 0 , self . chunk_state . flags , self . chunk_state . platform ,) ; self . cv_stack . clear () ; self } fn merge_cv_stack (& mut self , chunk_counter : u64) { let post_merge_stack_len = (chunk_counter - self . initial_chunk_counter) . count_ones () as usize ; while self . cv_stack . len () > post_merge_stack_len { let right_child = self . cv_stack . pop () . unwrap () ; let left_child = self . cv_stack . pop () . unwrap () ; let parent_output = parent_node_output (& left_child , & right_child , & self . key , self . chunk_state . flags , self . chunk_state . platform ,) ; self . cv_stack . push (parent_output . chaining_value ()) ; } } fn push_cv (& mut self , new_cv : & CVBytes , chunk_counter : u64) { self . merge_cv_stack (chunk_counter) ; self . cv_stack . push (* new_cv) ; } # [doc = " Add input bytes to the hash state. You can call this any number of times."] # [doc = ""] # [doc = " This method is always single-threaded. For multithreading support, see"] # [doc = " [`update_rayon`](#method.update_rayon) (enabled with the `rayon` Cargo feature)."] # [doc = ""] # [doc = " Note that the degree of SIMD parallelism that `update` can use is limited by the size of"] # [doc = " this input buffer. See [`update_reader`](#method.update_reader)."] pub fn update (& mut self , input : & [u8]) -> & mut Self { self . update_with_join :: < join :: SerialJoin > (input) } fn update_with_join < J : join :: Join > (& mut self , mut input : & [u8]) -> & mut Self { let input_offset = self . initial_chunk_counter * CHUNK_LEN as u64 ; if let Some (max) = hazmat :: max_subtree_len (input_offset) { let remaining = max - self . count () ; assert ! (input . len () as u64 <= remaining , "the subtree starting at {} contains at most {} bytes (found {})" , CHUNK_LEN as u64 * self . initial_chunk_counter , max , input . len () ,) ; } if self . chunk_state . count () > 0 { let want = CHUNK_LEN - self . chunk_state . count () ; let take = cmp :: min (want , input . len ()) ; self . chunk_state . update (& input [.. take]) ; input = & input [take ..] ; if ! input . is_empty () { debug_assert_eq ! (self . chunk_state . count () , CHUNK_LEN) ; let chunk_cv = self . chunk_state . output () . chaining_value () ; self . push_cv (& chunk_cv , self . chunk_state . chunk_counter) ; self . chunk_state = ChunkState :: new (& self . key , self . chunk_state . chunk_counter + 1 , self . chunk_state . flags , self . chunk_state . platform ,) ; } else { return self ; } } while input . len () > CHUNK_LEN { debug_assert_eq ! (self . chunk_state . count () , 0 , "no partial chunk data") ; debug_assert_eq ! (CHUNK_LEN . count_ones () , 1 , "power of 2 chunk len") ; let mut subtree_len = largest_power_of_two_leq (input . len ()) ; let count_so_far = self . chunk_state . chunk_counter * CHUNK_LEN as u64 ; while (subtree_len - 1) as u64 & count_so_far != 0 { subtree_len /= 2 ; } let subtree_chunks = (subtree_len / CHUNK_LEN) as u64 ; if subtree_len <= CHUNK_LEN { debug_assert_eq ! (subtree_len , CHUNK_LEN) ; self . push_cv (& ChunkState :: new (& self . key , self . chunk_state . chunk_counter , self . chunk_state . flags , self . chunk_state . platform ,) . update (& input [.. subtree_len]) . output () . chaining_value () , self . chunk_state . chunk_counter ,) ; } else { let cv_pair = compress_subtree_to_parent_node :: < J > (& input [.. subtree_len] , & self . key , self . chunk_state . chunk_counter , self . chunk_state . flags , self . chunk_state . platform ,) ; let left_cv = array_ref ! (cv_pair , 0 , 32) ; let right_cv = array_ref ! (cv_pair , 32 , 32) ; self . push_cv (left_cv , self . chunk_state . chunk_counter) ; self . push_cv (right_cv , self . chunk_state . chunk_counter + (subtree_chunks / 2) ,) ; } self . chunk_state . chunk_counter += subtree_chunks ; input = & input [subtree_len ..] ; } debug_assert ! (input . len () <= CHUNK_LEN) ; if ! input . is_empty () { self . chunk_state . update (input) ; self . merge_cv_stack (self . chunk_state . chunk_counter) ; } self } fn final_output (& self) -> Output { if self . cv_stack . is_empty () { debug_assert_eq ! (self . chunk_state . chunk_counter , self . initial_chunk_counter) ; return self . chunk_state . output () ; } let mut output : Output ; let mut num_cvs_remaining = self . cv_stack . len () ; if self . chunk_state . count () > 0 { debug_assert_eq ! (self . cv_stack . len () , (self . chunk_state . chunk_counter - self . initial_chunk_counter) . count_ones () as usize , "cv stack does not need a merge" ,) ; output = self . chunk_state . output () ; } else { debug_assert ! (self . cv_stack . len () >= 2) ; output = parent_node_output (& self . cv_stack [num_cvs_remaining - 2] , & self . cv_stack [num_cvs_remaining - 1] , & self . key , self . chunk_state . flags , self . chunk_state . platform ,) ; num_cvs_remaining -= 2 ; } while num_cvs_remaining > 0 { output = parent_node_output (& self . cv_stack [num_cvs_remaining - 1] , & output . chaining_value () , & self . key , self . chunk_state . flags , self . chunk_state . platform ,) ; num_cvs_remaining -= 1 ; } output } # [doc = " Finalize the hash state and return the [`Hash`](struct.Hash.html) of"] # [doc = " the input."] # [doc = ""] # [doc = " This method is idempotent. Calling it twice will give the same result."] # [doc = " You can also add more input and finalize again."] pub fn finalize (& self) -> Hash { assert_eq ! (self . initial_chunk_counter , 0 , "set_input_offset must be used with finalize_non_root" ,) ; self . final_output () . root_hash () } # [doc = " Finalize the hash state and return an [`OutputReader`], which can"] # [doc = " supply any number of output bytes."] # [doc = ""] # [doc = " This method is idempotent. Calling it twice will give the same result."] # [doc = " You can also add more input and finalize again."] # [doc = ""] # [doc = " [`OutputReader`]: struct.OutputReader.html"] pub fn finalize_xof (& self) -> OutputReader { assert_eq ! (self . initial_chunk_counter , 0 , "set_input_offset must be used with finalize_non_root" ,) ; OutputReader :: new (self . final_output ()) } # [doc = " Return the total number of bytes hashed so far."] # [doc = ""] # [doc = " [`hazmat::HasherExt::set_input_offset`] does not affect this value. This only counts bytes"] # [doc = " passed to [`update`](Hasher::update)."] pub fn count (& self) -> u64 { (self . chunk_state . chunk_counter - self . initial_chunk_counter) * CHUNK_LEN as u64 + self . chunk_state . count () as u64 } # [doc = " As [`update`](Hasher::update), but reading from a"] # [doc = " [`std::io::Read`](https://doc.rust-lang.org/std/io/trait.Read.html) implementation."] # [doc = ""] # [doc = " [`Hasher`] implements"] # [doc = " [`std::io::Write`](https://doc.rust-lang.org/std/io/trait.Write.html), so it's possible to"] # [doc = " use [`std::io::copy`](https://doc.rust-lang.org/std/io/fn.copy.html) to update a [`Hasher`]"] # [doc = " from any reader. Unfortunately, this standard approach can limit performance, because"] # [doc = " `copy` currently uses an internal 8 KiB buffer that isn't big enough to take advantage of"] # [doc = " all SIMD instruction sets. (In particular, [AVX-512](https://en.wikipedia.org/wiki/AVX-512)"] # [doc = " needs a 16 KiB buffer.) `update_reader` avoids this performance problem and is slightly"] # [doc = " more convenient."] # [doc = ""] # [doc = " The internal buffer size this method uses may change at any time, and it may be different"] # [doc = " for different targets. The only guarantee is that it will be large enough for all of this"] # [doc = " crate's SIMD implementations on the current platform."] # [doc = ""] # [doc = " The most common implementer of"] # [doc = " [`std::io::Read`](https://doc.rust-lang.org/std/io/trait.Read.html) might be"] # [doc = " [`std::fs::File`](https://doc.rust-lang.org/std/fs/struct.File.html), but note that memory"] # [doc = " mapping can be faster than this method for hashing large files. See"] # [doc = " [`update_mmap`](Hasher::update_mmap) and [`update_mmap_rayon`](Hasher::update_mmap_rayon),"] # [doc = " which require the `mmap` and (for the latter) `rayon` Cargo features."] # [doc = ""] # [doc = " This method requires the `std` Cargo feature, which is enabled by default."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```no_run"] # [doc = " # use std::fs::File;"] # [doc = " # use std::io;"] # [doc = " # fn main() -> io::Result<()> {"] # [doc = " // Hash standard input."] # [doc = " let mut hasher = blake3::Hasher::new();"] # [doc = " hasher.update_reader(std::io::stdin().lock())?;"] # [doc = " println!(\"{}\", hasher.finalize());"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] # [cfg (feature = "std")] pub fn update_reader (& mut self , reader : impl std :: io :: Read) -> std :: io :: Result < & mut Self > { io :: copy_wide (reader , self) ? ; Ok (self) } # [doc = " As [`update`](Hasher::update), but using Rayon-based multithreading"] # [doc = " internally."] # [doc = ""] # [doc = " This method is gated by the `rayon` Cargo feature, which is disabled by"] # [doc = " default but enabled on [docs.rs](https://docs.rs)."] # [doc = ""] # [doc = " To get any performance benefit from multithreading, the input buffer"] # [doc = " needs to be large. As a rule of thumb on x86_64, `update_rayon` is"] # [doc = " _slower_ than `update` for inputs under 128 KiB. That threshold varies"] # [doc = " quite a lot across different processors, and it's important to benchmark"] # [doc = " your specific use case. See also the performance warning associated with"] # [doc = " [`update_mmap_rayon`](Hasher::update_mmap_rayon)."] # [doc = ""] # [doc = " If you already have a large buffer in memory, and you want to hash it"] # [doc = " with multiple threads, this method is a good option. However, reading a"] # [doc = " file into memory just to call this method can be a performance mistake,"] # [doc = " both because it requires lots of memory and because single-threaded"] # [doc = " reads can be slow. For hashing whole files, see"] # [doc = " [`update_mmap_rayon`](Hasher::update_mmap_rayon), which is gated by both"] # [doc = " the `rayon` and `mmap` Cargo features."] # [cfg (feature = "rayon")] pub fn update_rayon (& mut self , input : & [u8]) -> & mut Self { self . update_with_join :: < join :: RayonJoin > (input) } # [doc = " As [`update`](Hasher::update), but reading the contents of a file using memory mapping."] # [doc = ""] # [doc = " Not all files can be memory mapped, and memory mapping small files can be slower than"] # [doc = " reading them the usual way. In those cases, this method will fall back to standard file IO."] # [doc = " The heuristic for whether to use memory mapping is currently very simple (file size >="] # [doc = " 16 KiB), and it might change at any time."] # [doc = ""] # [doc = " Like [`update`](Hasher::update), this method is single-threaded. In this author's"] # [doc = " experience, memory mapping improves single-threaded performance by ~10% for large files"] # [doc = " that are already in cache. This probably varies between platforms, and as always it's a"] # [doc = " good idea to benchmark your own use case. In comparison, the multithreaded"] # [doc = " [`update_mmap_rayon`](Hasher::update_mmap_rayon) method can have a much larger impact on"] # [doc = " performance."] # [doc = ""] # [doc = " There's a correctness reason that this method takes"] # [doc = " [`Path`](https://doc.rust-lang.org/stable/std/path/struct.Path.html) instead of"] # [doc = " [`File`](https://doc.rust-lang.org/std/fs/struct.File.html): reading from a memory-mapped"] # [doc = " file ignores the seek position of the original file handle (it neither respects the current"] # [doc = " position nor updates the position). This difference in behavior would've caused"] # [doc = " `update_mmap` and [`update_reader`](Hasher::update_reader) to give different answers and"] # [doc = " have different side effects in some cases. Taking a"] # [doc = " [`Path`](https://doc.rust-lang.org/stable/std/path/struct.Path.html) avoids this problem by"] # [doc = " making it clear that a new [`File`](https://doc.rust-lang.org/std/fs/struct.File.html) is"] # [doc = " opened internally."] # [doc = ""] # [doc = " This method requires the `mmap` Cargo feature, which is disabled by default but enabled on"] # [doc = " [docs.rs](https://docs.rs)."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```no_run"] # [doc = " # use std::io;"] # [doc = " # use std::path::Path;"] # [doc = " # fn main() -> io::Result<()> {"] # [doc = " let path = Path::new(\"file.dat\");"] # [doc = " let mut hasher = blake3::Hasher::new();"] # [doc = " hasher.update_mmap(path)?;"] # [doc = " println!(\"{}\", hasher.finalize());"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] # [cfg (feature = "mmap")] pub fn update_mmap (& mut self , path : impl AsRef < std :: path :: Path >) -> std :: io :: Result < & mut Self > { let file = std :: fs :: File :: open (path . as_ref ()) ? ; if let Some (mmap) = io :: maybe_mmap_file (& file) ? { self . update (& mmap) ; } else { io :: copy_wide (& file , self) ? ; } Ok (self) } # [doc = " As [`update_rayon`](Hasher::update_rayon), but reading the contents of a file using"] # [doc = " memory mapping. This is the default behavior of `b3sum`."] # [doc = ""] # [doc = " For large files that are likely to be in cache, this can be much faster than"] # [doc = " single-threaded hashing. When benchmarks report that BLAKE3 is 10x or 20x faster than other"] # [doc = " cryptographic hashes, this is usually what they're measuring. However..."] # [doc = ""] # [doc = " **Performance Warning:** There are cases where multithreading hurts performance. The worst"] # [doc = " case is [a large file on a spinning disk](https://github.com/BLAKE3-team/BLAKE3/issues/31),"] # [doc = " where simultaneous reads from multiple threads can cause \"thrashing\" (i.e. the disk spends"] # [doc = " more time seeking around than reading data). Windows tends to be somewhat worse about this,"] # [doc = " in part because it's less likely than Linux to keep very large files in cache. More"] # [doc = " generally, if your CPU cores are already busy, then multithreading will add overhead"] # [doc = " without improving performance. If your code runs in different environments that you don't"] # [doc = " control and can't measure, then unfortunately there's no one-size-fits-all answer for"] # [doc = " whether multithreading is a good idea."] # [doc = ""] # [doc = " The memory mapping behavior of this function is the same as"] # [doc = " [`update_mmap`](Hasher::update_mmap), and the heuristic for when to fall back to standard"] # [doc = " file IO might change at any time."] # [doc = ""] # [doc = " This method requires both the `mmap` and `rayon` Cargo features, which are disabled by"] # [doc = " default but enabled on [docs.rs](https://docs.rs)."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```no_run"] # [doc = " # use std::io;"] # [doc = " # use std::path::Path;"] # [doc = " # fn main() -> io::Result<()> {"] # [doc = " # #[cfg(feature = \"rayon\")]"] # [doc = " # {"] # [doc = " let path = Path::new(\"big_file.dat\");"] # [doc = " let mut hasher = blake3::Hasher::new();"] # [doc = " hasher.update_mmap_rayon(path)?;"] # [doc = " println!(\"{}\", hasher.finalize());"] # [doc = " # }"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] # [cfg (feature = "mmap")] # [cfg (feature = "rayon")] pub fn update_mmap_rayon (& mut self , path : impl AsRef < std :: path :: Path > ,) -> std :: io :: Result < & mut Self > { let file = std :: fs :: File :: open (path . as_ref ()) ? ; if let Some (mmap) = io :: maybe_mmap_file (& file) ? { self . update_rayon (& mmap) ; } else { io :: copy_wide (& file , self) ? ; } Ok (self) } }
    };
}

impl_203!()