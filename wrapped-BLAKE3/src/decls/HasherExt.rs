macro_rules! deps {
    () => {
        Hasher!();
        ChainingValue!();
        ContextKey!();
    };
}

macro_rules! HasherExt {
    () => {
        deps!();
        # [doc = " Extension methods for [`Hasher`]. This is the main entrypoint to the `hazmat` module."] pub trait HasherExt { # [doc = " Similar to [`Hasher::new_derive_key`] but using a pre-hashed [`ContextKey`] from"] # [doc = " [`hash_derive_key_context`]."] # [doc = ""] # [doc = " The [`hash_derive_key_context`] function is the _only_ valid source of the [`ContextKey`]."] # [doc = " Any other source ([`hash`](crate::hash), [`keyed_hash`](crate::keyed_hash), arbitrary bytes"] # [doc = " from the caller) violates the security requirements."] # [doc = ""] # [doc = " Calling [`derive_key`](crate::derive_key) or [`Hasher::new_derive_key`] in a loop will"] # [doc = " re-hash the context string every time. This constructor function is a performance"] # [doc = " optimization to avoid that repeated work. If you hardcode the [`ContextKey`], the"] # [doc = " derive-key mode becomes zero-overhead, like the keyed mode."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use blake3::Hasher;"] # [doc = " use blake3::hazmat::HasherExt;"] # [doc = ""] # [doc = " let context_key = blake3::hazmat::hash_derive_key_context(\"foo\");"] # [doc = " let mut hasher = Hasher::new_from_context_key(&context_key);"] # [doc = " hasher.update(b\"bar\");"] # [doc = " let derived_key = *hasher.finalize().as_bytes();"] # [doc = ""] # [doc = " assert_eq!(derived_key, blake3::derive_key(\"foo\", b\"bar\"));"] # [doc = " ```"] fn new_from_context_key (context_key : & ContextKey) -> Self ; # [doc = " Configure the `Hasher` to process a chunk or subtree starting at `offset` bytes into the"] # [doc = " whole input."] # [doc = ""] # [doc = " You must call this function before processing any input with [`update`](Hasher::update) or"] # [doc = " similar. This step isn't required for the first chunk, or for a subtree that includes the"] # [doc = " first chunk (i.e. when the `offset` is zero), but it's required for all other chunks and"] # [doc = " subtrees."] # [doc = ""] # [doc = " The starting input offset of a subtree implies a maximum possible length for that subtree."] # [doc = " See [`max_subtree_len`] and section 2.1 of [the BLAKE3"] # [doc = " paper](https://github.com/BLAKE3-team/BLAKE3-specs/blob/master/blake3.pdf). Note that only"] # [doc = " subtrees along the right edge of the whole tree can have a length less than their maximum"] # [doc = " possible length."] # [doc = ""] # [doc = " See the [module level examples](index.html#examples)."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if the `Hasher` has already accepted any input with"] # [doc = " [`update`](Hasher::update) or similar."] # [doc = ""] # [doc = " This should always be paired with [`finalize_non_root`](HasherExt::finalize_non_root). It's"] # [doc = " never correct to use a non-zero input offset with [`finalize`](Hasher::finalize) or"] # [doc = " [`finalize_xof`](Hasher::finalize_xof). The `offset` must also be a multiple of"] # [doc = " `CHUNK_LEN`. Violating either of these rules will currently fail an assertion and panic,"] # [doc = " but this is not guaranteed."] fn set_input_offset (& mut self , offset : u64) -> & mut Self ; # [doc = " Finalize the non-root hash (\"chaining value\") of the current chunk or subtree."] # [doc = ""] # [doc = " Afterwards you can merge subtree chaining values into parent nodes using"] # [doc = " [`merge_subtrees_non_root`] and ultimately into the root node with either"] # [doc = " [`merge_subtrees_root`] (similar to [`Hasher::finalize`]) or [`merge_subtrees_root_xof`]"] # [doc = " (similar to [`Hasher::finalize_xof`])."] # [doc = ""] # [doc = " See the [module level examples](index.html#examples), particularly the discussion of valid"] # [doc = " tree structures."] fn finalize_non_root (& self) -> ChainingValue ; }
    };
}

HasherExt!()