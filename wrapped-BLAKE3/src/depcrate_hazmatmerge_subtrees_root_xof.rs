// Generated macro for merge_subtrees_root_xof (function)
macro_rules! Depcrate_hazmatmerge_subtrees_root_xof {
() => {
// Module: crate::hazmat
// Provides: {"merge_subtrees_root_xof"}
// Dependencies: {}
# [doc = " Build a root [`OutputReader`](crate::OutputReader) from two child chaining values."] # [doc = ""] # [doc = " See also the [module level examples](index.html#examples), particularly the discussion of valid"] # [doc = " tree structures. The left and right child chaining values can come from either"] # [doc = " [`Hasher::finalize_non_root`](HasherExt::finalize_non_root) or [`merge_subtrees_non_root`]."] # [doc = " \"Chaining value\" is the academic term for a non-root or non-final hash."] # [doc = ""] # [doc = " Note that inputs of [`CHUNK_LEN`] or less don't produce any parent nodes and can't be hashed"] # [doc = " using this function. In that case you must get the `OutputReader` from"] # [doc = " [`Hasher::finalize_xof`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use blake3::hazmat::{merge_subtrees_root_xof, HasherExt, Mode};"] # [doc = " use blake3::{Hasher, CHUNK_LEN};"] # [doc = ""] # [doc = " // Hash a 2-chunk subtree in steps. Note that only"] # [doc = " // the final chunk can be shorter than CHUNK_LEN."] # [doc = " let chunk0 = &[42; CHUNK_LEN];"] # [doc = " let chunk1 = b\"hello world\";"] # [doc = " let chunk0_cv = Hasher::new()"] # [doc = "     .update(chunk0)"] # [doc = "     .finalize_non_root();"] # [doc = " let chunk1_cv = Hasher::new()"] # [doc = "     .set_input_offset(CHUNK_LEN as u64)"] # [doc = "     .update(chunk1)"] # [doc = "     .finalize_non_root();"] # [doc = ""] # [doc = " // Obtain a blake3::OutputReader at the root and extract 1000 bytes."] # [doc = " let mut output_reader = merge_subtrees_root_xof(&chunk0_cv, &chunk1_cv, Mode::Hash);"] # [doc = " let mut output_bytes = [0; 1_000];"] # [doc = " output_reader.fill(&mut output_bytes);"] # [doc = ""] # [doc = " // Double check the answer."] # [doc = " let mut hasher = Hasher::new();"] # [doc = " hasher.update(chunk0);"] # [doc = " hasher.update(chunk1);"] # [doc = " let mut expected = [0; 1_000];"] # [doc = " hasher.finalize_xof().fill(&mut expected);"] # [doc = " assert_eq!(output_bytes, expected);"] # [doc = " ```"] pub fn merge_subtrees_root_xof (left_child : & ChainingValue , right_child : & ChainingValue , mode : Mode ,) -> crate :: OutputReader { crate :: OutputReader :: new (merge_subtrees_inner (left_child , right_child , mode)) }
};
}
