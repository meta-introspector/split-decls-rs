macro_rules! unsafe_pinned {
    () => {
        # [doc = " A pinned projection of a struct field."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " To make using this macro safe, three things need to be ensured:"] # [doc = " - If the struct implements [`Drop`], the [`drop`] method is not allowed to"] # [doc = "   move the value of the field."] # [doc = " - If the struct wants to implement [`Unpin`], it has to do so conditionally:"] # [doc = "   The struct can only implement [`Unpin`] if the field's type is [`Unpin`]."] # [doc = " - The struct must not be `#[repr(packed)]`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use pin_utils::unsafe_pinned;"] # [doc = " use std::marker::Unpin;"] # [doc = " use std::pin::Pin;"] # [doc = ""] # [doc = " struct Foo<T> {"] # [doc = "     field: T,"] # [doc = " }"] # [doc = ""] # [doc = " impl<T> Foo<T> {"] # [doc = "     unsafe_pinned!(field: T);"] # [doc = ""] # [doc = "     fn baz(mut self: Pin<&mut Self>) {"] # [doc = "         let _: Pin<&mut T> = self.field(); // Pinned reference to the field"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " impl<T: Unpin> Unpin for Foo<T> {} // Conditional Unpin impl"] # [doc = " ```"] # [doc = ""] # [doc = " Note: borrowing the field multiple times requires using `.as_mut()` to"] # [doc = " avoid consuming the `Pin`."] # [doc = ""] # [doc = " [`Unpin`]: core::marker::Unpin"] # [doc = " [`drop`]: Drop::drop"] # [deprecated (since = "0.1.1" , note = "this macro is not safe; use safe pin-project or pin-project-lite crate instead")] # [macro_export] macro_rules ! unsafe_pinned { ($ f : tt : $ t : ty) => { # [allow (unsafe_code)] fn $ f <'__a > (self : $ crate :: __private :: Pin <&'__a mut Self >,) -> $ crate :: __private :: Pin <&'__a mut $ t > { unsafe { $ crate :: __private :: Pin :: map_unchecked_mut (self , | x | & mut x .$ f) } } } ; }
    };
}

unsafe_pinned!();