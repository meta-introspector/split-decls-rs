macro_rules! unsafe_unpinned {
    () => {
        # [doc = " An unpinned projection of a struct field."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This macro is unsafe because it creates a method that returns a normal"] # [doc = " non-pin reference to the struct field. It is up to the programmer to ensure"] # [doc = " that the contained value can be considered not pinned in the current"] # [doc = " context."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use pin_utils::unsafe_unpinned;"] # [doc = " use std::pin::Pin;"] # [doc = ""] # [doc = " struct Bar;"] # [doc = " struct Foo {"] # [doc = "     field: Bar,"] # [doc = " }"] # [doc = ""] # [doc = " impl Foo {"] # [doc = "     unsafe_unpinned!(field: Bar);"] # [doc = ""] # [doc = "     fn baz(mut self: Pin<&mut Self>) {"] # [doc = "         let _: &mut Bar = self.field(); // Normal reference to the field"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Note: borrowing the field multiple times requires using `.as_mut()` to"] # [doc = " avoid consuming the [`Pin`]."] # [doc = ""] # [doc = " [`Pin`]: core::pin::Pin"] # [deprecated (since = "0.1.1" , note = "this macro is not safe; use safe pin-project or pin-project-lite crate instead")] # [macro_export] macro_rules ! unsafe_unpinned { ($ f : tt : $ t : ty) => { # [allow (unsafe_code)] fn $ f <'__a > (self : $ crate :: __private :: Pin <&'__a mut Self >) -> &'__a mut $ t { unsafe { & mut $ crate :: __private :: Pin :: get_unchecked_mut (self) .$ f } } } ; }
    };
}

unsafe_unpinned!();