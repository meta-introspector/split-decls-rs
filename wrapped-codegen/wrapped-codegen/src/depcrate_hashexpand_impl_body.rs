// Generated macro for expand_impl_body (function)
macro_rules! Depcrate_hashexpand_impl_body {
() => {
// Module: crate::hash
// Provides: {"expand_impl_body"}
// Dependencies: {}
fn expand_impl_body (defs : & Definitions , node : & Node) -> TokenStream { let type_name = & node . ident ; let ident = Ident :: new (type_name , Span :: call_site ()) ; match & node . data { Data :: Enum (variants) if variants . is_empty () => quote ! (match * self { }) , Data :: Enum (variants) => { let mixed_derive_full = full :: is_mixed_derive_full_enum (defs , node) ; let arms = variants . iter () . enumerate () . map (| (i , (variant_name , fields)) | { let i = u8 :: try_from (i) . unwrap () ; let variant = Ident :: new (variant_name , Span :: call_site ()) ; if fields . is_empty () { quote ! { crate ::# ident ::# variant => { state . write_u8 (# i) ; } } } else { let mut pats = Vec :: new () ; let mut hashes = Vec :: new () ; for (i , field) in fields . iter () . enumerate () { if skip (field) { pats . push (format_ident ! ("_")) ; continue ; } let var = format_ident ! ("v{}" , i) ; let mut hashed_val = quote ! (# var) ; match field { Type :: Ext (ty) if ty == "TokenStream" => { hashed_val = quote ! (TokenStreamHelper (# hashed_val)) ; } Type :: Ext (ty) if ty == "Literal" => { hashed_val = quote ! (# hashed_val . to_string ()) ; } _ => { } } hashes . push (quote ! { # hashed_val . hash (state) ; }) ; pats . push (var) ; } let mut cfg = None ; if mixed_derive_full { if let Type :: Syn (ty) = & fields [0] { let features = & lookup :: node (defs , ty) . features ; if features . any . contains ("full") && ! features . any . contains ("derive") { cfg = Some (quote ! (# [cfg (feature = "full")])) ; } } } quote ! { # cfg crate ::# ident ::# variant (# (# pats) ,*) => { state . write_u8 (# i) ; # (# hashes) * } } } }) ; let nonexhaustive = if mixed_derive_full { Some (quote ! { # [cfg (not (feature = "full"))] _ => unreachable ! () , }) } else { None } ; quote ! { match self { # (# arms) * # nonexhaustive } } } Data :: Struct (fields) => fields . iter () . filter_map (| (f , ty) | { if skip (ty) { return None ; } let ident = Ident :: new (f , Span :: call_site ()) ; let mut val = quote ! (self .# ident) ; if let Type :: Ext (ty) = ty { if ty == "TokenStream" { val = quote ! (TokenStreamHelper (&# val)) ; } } Some (quote ! { # val . hash (state) ; }) }) . collect () , Data :: Private => unreachable ! () , } }
};
}
