// Generated macro for node (function)
macro_rules! Depcrate_visitnode {
() => {
// Module: crate::visit
// Provides: {"node"}
// Dependencies: {}
fn node (traits : & mut TokenStream , impls : & mut TokenStream , s : & Node , defs : & Definitions) { let under_name = gen :: under_name (& s . ident) ; let ident = Ident :: new (& s . ident , Span :: call_site ()) ; let ty = if gen :: TERMINAL_TYPES . contains (& s . ident . as_str ()) { quote ! (proc_macro2 ::# ident) } else { quote ! (crate ::# ident) } ; let visit_fn = format_ident ! ("visit_{}" , under_name) ; let mut visit_impl = TokenStream :: new () ; match & s . data { Data :: Enum (variants) if variants . is_empty () => { visit_impl . extend (quote ! { match * node { } }) ; } Data :: Enum (variants) => { let mut visit_variants = TokenStream :: new () ; for (variant , fields) in variants { let variant_ident = Ident :: new (variant , Span :: call_site ()) ; if fields . is_empty () { visit_variants . extend (quote ! { # ty ::# variant_ident => { } }) ; } else { let mut bind_visit_fields = TokenStream :: new () ; let mut visit_fields = TokenStream :: new () ; for (idx , ty) in fields . iter () . enumerate () { let binding = format_ident ! ("_binding_{}" , idx) ; bind_visit_fields . extend (quote ! { # binding , }) ; let borrowed_binding = Borrowed (quote ! (# binding)) ; visit_fields . extend (visit (ty , & s . features , defs , & borrowed_binding) . unwrap_or_else (| | noop_visit (& borrowed_binding)) ,) ; visit_fields . extend (quote ! (;)) ; } visit_variants . extend (quote ! { # ty ::# variant_ident (# bind_visit_fields) => { # visit_fields } }) ; } } visit_impl . extend (quote ! { match node { # visit_variants } }) ; } Data :: Struct (fields) => { for (field , ty) in fields { let id = Ident :: new (field , Span :: call_site ()) ; let ref_toks = Owned (quote ! (node .# id)) ; let visit_field = visit (ty , & s . features , defs , & ref_toks) . unwrap_or_else (| | noop_visit (& ref_toks)) ; visit_impl . extend (quote ! { # visit_field ; }) ; } } Data :: Private => { if s . ident == "Ident" { visit_impl . extend (quote ! { v . visit_span (& node . span ()) ; }) ; } } } let ast_lifetime = if s . ident == "Span" { None } else { Some (quote ! ('ast)) } ; let traits_body = if s . ident == "Span" || s . ident == "TokenStream" { None } else { Some (quote ! { # visit_fn (self , i) ; }) } ; traits . extend (quote ! { fn # visit_fn (& mut self , i : &# ast_lifetime # ty) { # traits_body } }) ; if s . ident != "TokenStream" { impls . extend (quote ! { pub fn # visit_fn <'ast , V > (v : & mut V , node : &# ast_lifetime # ty) where V : Visit <'ast > + ? Sized , { # visit_impl } }) ; } }
};
}
