// Generated macro for do_load_file (function)
macro_rules! Depcrate_parsedo_load_file {
() => {
// Module: crate::parse
// Provides: {"do_load_file"}
// Dependencies: {}
fn do_load_file (relative_to_workspace_root : impl AsRef < Path > , features : & [Attribute] , lookup : & mut Lookup ,) -> Result < () > { let relative_to_workspace_root = relative_to_workspace_root . as_ref () ; let parent = relative_to_workspace_root . parent () . expect ("no parent path") ; let src = fs :: read_to_string (workspace_path :: get (relative_to_workspace_root)) ? ; let file = syn :: parse_file (& src) ? ; 'items : for item in file . items { match item { Item :: Mod (item) => { if item . content . is_some () { continue ; } for name in IGNORED_MODS { if item . ident == name { continue 'items ; } } let features = if item . ident == "derive" { vec ! [parse_quote ! (# [cfg (feature = "derive")])] } else { get_features (& item . attrs , features) } ; let filename = if let Some (filename) = parsing :: path_attr (& item . attrs) ? { filename . value () } else { format ! ("{}.rs" , item . ident) } ; let path = parent . join (filename) ; load_file (path , & features , lookup) ? ; } Item :: Macro (item) => { let features = get_features (& item . attrs , features) ; let tokens = item . mac . tokens . clone () ; let mut found = if item . mac . path . is_ident ("ast_struct") { parsing :: ast_struct . parse2 (tokens) } else if item . mac . path . is_ident ("ast_enum") { parsing :: ast_enum . parse2 (tokens) } else if item . mac . path . is_ident ("ast_enum_of_structs") { parsing :: ast_enum_of_structs . parse2 (tokens) } else { continue ; } ? ; found . features . extend (clone_features (& features)) ; lookup . items . insert (found . ast . ident . clone () , found) ; } Item :: Struct (item) => { let ident = item . ident ; if EXTRA_TYPES . contains (& & ident . to_string () [..]) { lookup . items . insert (ident . clone () , AstItem { ast : DeriveInput { ident , vis : item . vis , attrs : item . attrs , generics : item . generics , data : Data :: Struct (DataStruct { fields : item . fields , struct_token : item . struct_token , semi_token : item . semi_token , }) , } , features : clone_features (features) , } ,) ; } } Item :: Use (item) if relative_to_workspace_root == Path :: new ("src/pat.rs") && matches ! (item . vis , Visibility :: Public (_)) => { load_aliases (item . tree , lookup) ; } _ => { } } } Ok (()) }
};
}
