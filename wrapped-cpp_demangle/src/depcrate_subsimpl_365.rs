// Generated macro for impl_365 (impl)
macro_rules! Depcrate_subsimpl_365 {
() => {
// Module: crate::subs
// Provides: {"impl_365"}
// Dependencies: {}
impl SubstitutionTable { # [doc = " Construct a new `SubstitutionTable`."] pub fn new () -> SubstitutionTable { Default :: default () } # [doc = " Insert a freshly-parsed substitutable component into the table and"] # [doc = " return the index at which it now lives."] pub fn insert (& mut self , entity : Substitutable) -> usize { let idx = self . substitutions . len () ; log ! ("SubstitutionTable::insert @ {}: {:?}" , idx , entity) ; self . substitutions . push (entity) ; idx } # [doc = " Insert a an entity into the table that is not a candidate for"] # [doc = " substitution."] pub fn insert_non_substitution (& mut self , entity : Substitutable) -> usize { let idx = self . non_substitutions . len () ; self . non_substitutions . push (entity) ; idx } # [doc = " Does this substitution table contain a component at the given index?"] pub fn contains (& self , idx : usize) -> bool { idx < self . substitutions . len () } # [doc = " Get the type referenced by the given handle, or None if there is no such"] # [doc = " entry, or there is an entry that is not a type."] pub fn get_type (& self , handle : & ast :: TypeHandle) -> Option < & ast :: Type > { if let ast :: TypeHandle :: BackReference (idx) = * handle { self . substitutions . get (idx) . and_then (| s | match * s { Substitutable :: Type (ref ty) => Some (ty) , _ => None , }) } else { None } } # [doc = " Remove the last entry from the substitutions table and return it, or"] # [doc = " `None` if the table is empty."] pub fn pop (& mut self) -> Option < Substitutable > { log ! ("SubstitutionTable::pop @ {}: {:?}" , self . len () , self . last ()) ; self . substitutions . pop () } # [doc = " Get the `idx`th entity that is not a candidate for substitution. Panics"] # [doc = " if `idx` is out of bounds."] pub fn non_substitution (& self , idx : usize) -> & Substitutable { & self . non_substitutions [idx] } # [doc = " Get the `idx`th entity that is not a candidate for substitution. Returns"] # [doc = " `None` if `idx` is out of bounds."] pub fn get_non_substitution (& self , idx : usize) -> Option < & Substitutable > { self . non_substitutions . get (idx) } }
};
}
