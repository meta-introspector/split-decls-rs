// Generated macro for define_vocabulary (macro)
macro_rules! Depcrate_astdefine_vocabulary {
() => {
// Module: crate::ast
// Provides: {"define_vocabulary"}
// Dependencies: {}
# [doc = " Define a \"vocabulary\" nonterminal, something like `OperatorName` or"] # [doc = " `CtorDtorName` that's basically a big list of constant strings."] # [doc = ""] # [doc = " This declares:"] # [doc = ""] # [doc = " - the enum itself"] # [doc = " - a `Parse` impl"] # [doc = " - a `Demangle` impl"] # [doc = ""] # [doc = " See the definition of `CTorDtorName` for an example of its use."] # [doc = ""] # [doc = " Optionally, a piece of user data can be attached to the definitions"] # [doc = " and be returned by a generated accessor. See `SimpleOperatorName` for"] # [doc = " an example."] macro_rules ! define_vocabulary { ($ (# [$ attr : meta]) * pub enum $ typename : ident { $ ($ variant : ident ($ mangled : expr , $ printable : expr)) ,* }) => { $ (# [$ attr]) * pub enum $ typename { $ (# [doc =$ printable] $ variant) ,* } impl Parse for $ typename { fn parse <'a , 'b > (ctx : &'a ParseContext , _subs : &'a mut SubstitutionTable , input : IndexStr <'b >) -> Result < ($ typename , IndexStr <'b >) > { try_begin_parse ! (stringify ! ($ typename) , ctx , input) ; let mut found_prefix = false ; $ (if let Some ((head , tail)) = input . try_split_at ($ mangled . len ()) { if head . as_ref () == $ mangled { return Ok (($ typename ::$ variant , tail)) ; } } else { found_prefix |= 0 < input . len () && input . len () < $ mangled . len () && input . as_ref () == &$ mangled [.. input . len ()] ; }) * if input . is_empty () || found_prefix { Err (error :: Error :: UnexpectedEnd) } else { Err (error :: Error :: UnexpectedText) } } } impl <'subs , W > Demangle <'subs , W > for $ typename where W : 'subs + DemangleWrite , { fn demangle <'prev , 'ctx > (&'subs self , ctx : &'ctx mut DemangleContext <'subs , W >, scope : Option < ArgScopeStack <'prev , 'subs >>) -> fmt :: Result { let ctx = try_begin_demangle ! (self , ctx , scope) ; write ! (ctx , "{}" , match * self { $ ($ typename ::$ variant => $ printable) ,* }) } } impl $ typename { # [allow (dead_code)] # [inline] fn starts_with (byte : u8) -> bool { $ (if $ mangled [0] == byte { return true ; }) * false } } } ; ($ (# [$ attr : meta]) * pub enum $ typename : ident { $ ($ variant : ident ($ mangled : expr , $ printable : expr , $ userdata : expr)) ,* } impl $ typename2 : ident { fn $ fn_name : ident (& self) -> $ userdata_ty : ty ; }) => { define_vocabulary ! { $ (# [$ attr]) * pub enum $ typename { $ ($ variant ($ mangled , $ printable)) ,* } } impl $ typename2 { fn $ fn_name (& self) -> $ userdata_ty { match * self { $ ($ typename2 ::$ variant => $ userdata ,) * } } } } ; }
};
}
