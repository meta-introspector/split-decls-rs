// Generated macro for impl_345 (impl)
macro_rules! Depcrate_index_strimpl_345 {
() => {
// Module: crate::index_str
// Provides: {"impl_345"}
// Dependencies: {}
# [allow (dead_code)] impl < 'a > IndexStr < 'a > { # [doc = " Construct a new `IndexStr` (with `index == 0`) from the given input."] # [inline] pub fn new (string : & 'a [u8]) -> IndexStr < 'a > { IndexStr { idx : 0 , string : string , } } # [doc = " Return the length of the string."] # [inline] pub fn len (& self) -> usize { self . string . len () } # [doc = " Return true if the string is empty, false otherwise."] # [inline] pub fn is_empty (& self) -> bool { self . string . is_empty () } # [doc = " Get the index into the original input that this `IndexStr` is at."] # [inline] pub fn index (& self) -> usize { self . idx } # [doc = " Peek at the next byte in this `IndexStr`."] # [inline] pub fn peek (& self) -> Option < u8 > { self . as_ref () . get (0) . cloned () } # [doc = " Peek at the second next byte in this `IndexStr`."] # [inline] pub fn peek_second (& self) -> Option < u8 > { self . as_ref () . get (1) . cloned () } # [doc = " Split the string in two at the given index, resulting in the tuple where"] # [doc = " the first item has range `[0, idx)`, and the second has range `[idx,"] # [doc = " len)`."] # [doc = ""] # [doc = " Panics if the index is out of bounds."] # [inline] pub fn split_at (& self , idx : usize) -> (IndexStr < 'a > , IndexStr < 'a >) { (self . range_to (.. idx) , self . range_from (idx ..)) } # [doc = " The same as `split_at`, but returns an `Option` rather than panicking"] # [doc = " when the index is out of bounds."] # [inline] pub fn try_split_at (& self , idx : usize) -> Option < (IndexStr < 'a > , IndexStr < 'a >) > { if idx > self . len () { None } else { Some (self . split_at (idx)) } } # [doc = " Pop the next byte off the front of this string, returning it and the new"] # [doc = " tail string, or `None` if this string is empty."] # [inline] pub fn next (& self) -> Option < (u8 , IndexStr < 'a >) > { if self . is_empty () { None } else { let byte = self . string [0] ; Some ((byte , self . range_from (1 ..))) } } # [doc = " Pop the next byte off the front of this string, returning it and the new"] # [doc = " tail string, or the given error if this string is empty."] # [inline] pub fn next_or < E > (& self , error : E) -> Result < (u8 , IndexStr < 'a >) , E > { self . next () . ok_or (error) } }
};
}
