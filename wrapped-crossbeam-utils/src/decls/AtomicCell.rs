macro_rules! AtomicCell {
    () => {
        # [doc = " A thread-safe mutable memory location."] # [doc = ""] # [doc = " This type is equivalent to [`Cell`], except it can also be shared among multiple threads."] # [doc = ""] # [doc = " Operations on `AtomicCell`s use atomic instructions whenever possible, and synchronize using"] # [doc = " global locks otherwise. You can call [`AtomicCell::<T>::is_lock_free()`] to check whether"] # [doc = " atomic instructions or locks will be used."] # [doc = ""] # [doc = " Atomic loads use the [`Acquire`] ordering and atomic stores use the [`Release`] ordering."] # [doc = ""] # [doc = " [`Cell`]: std::cell::Cell"] # [doc = " [`AtomicCell::<T>::is_lock_free()`]: AtomicCell::is_lock_free"] # [doc = " [`Acquire`]: std::sync::atomic::Ordering::Acquire"] # [doc = " [`Release`]: std::sync::atomic::Ordering::Release"] # [repr (transparent)] pub struct AtomicCell < T > { # [doc = " The inner value."] # [doc = ""] # [doc = " If this value can be transmuted into a primitive atomic type, it will be treated as such."] # [doc = " Otherwise, all potentially concurrent operations on this data will be protected by a global"] # [doc = " lock."] # [doc = ""] # [doc = " Using MaybeUninit to prevent code outside the cell from observing partially initialized state:"] # [doc = " <https://github.com/crossbeam-rs/crossbeam/issues/833>"] # [doc = " (This rustc bug has been fixed in Rust 1.64.)"] # [doc = ""] # [doc = " Note:"] # [doc = " - we'll never store uninitialized `T` due to our API only using initialized `T`."] # [doc = " - this `MaybeUninit` does *not* fix <https://github.com/crossbeam-rs/crossbeam/issues/315>."] value : UnsafeCell < MaybeUninit < T > > , }
    };
}

AtomicCell!()