macro_rules! Backoff {
    () => {
        # [doc = " Performs exponential backoff in spin loops."] # [doc = ""] # [doc = " Backing off in spin loops reduces contention and improves overall performance."] # [doc = ""] # [doc = " This primitive can execute *YIELD* and *PAUSE* instructions, yield the current thread to the OS"] # [doc = " scheduler, and tell when is a good time to block the thread using a different synchronization"] # [doc = " mechanism. Each step of the back off procedure takes roughly twice as long as the previous"] # [doc = " step."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Backing off in a lock-free loop:"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_utils::Backoff;"] # [doc = " use std::sync::atomic::AtomicUsize;"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " fn fetch_mul(a: &AtomicUsize, b: usize) -> usize {"] # [doc = "     let backoff = Backoff::new();"] # [doc = "     loop {"] # [doc = "         let val = a.load(SeqCst);"] # [doc = "         if a.compare_exchange(val, val.wrapping_mul(b), SeqCst, SeqCst).is_ok() {"] # [doc = "             return val;"] # [doc = "         }"] # [doc = "         backoff.spin();"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Waiting for an [`AtomicBool`] to become `true`:"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_utils::Backoff;"] # [doc = " use std::sync::atomic::AtomicBool;"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = ""] # [doc = " fn spin_wait(ready: &AtomicBool) {"] # [doc = "     let backoff = Backoff::new();"] # [doc = "     while !ready.load(SeqCst) {"] # [doc = "         backoff.snooze();"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Waiting for an [`AtomicBool`] to become `true` and parking the thread after a long wait."] # [doc = " Note that whoever sets the atomic variable to `true` must notify the parked thread by calling"] # [doc = " [`unpark()`]:"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_utils::Backoff;"] # [doc = " use std::sync::atomic::AtomicBool;"] # [doc = " use std::sync::atomic::Ordering::SeqCst;"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " fn blocking_wait(ready: &AtomicBool) {"] # [doc = "     let backoff = Backoff::new();"] # [doc = "     while !ready.load(SeqCst) {"] # [doc = "         if backoff.is_completed() {"] # [doc = "             thread::park();"] # [doc = "         } else {"] # [doc = "             backoff.snooze();"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [`is_completed`]: Backoff::is_completed"] # [doc = " [`std::thread::park()`]: std::thread::park"] # [doc = " [`Condvar`]: std::sync::Condvar"] # [doc = " [`AtomicBool`]: std::sync::atomic::AtomicBool"] # [doc = " [`unpark()`]: std::thread::Thread::unpark"] pub struct Backoff { step : Cell < u32 > , }
    };
}

Backoff!();