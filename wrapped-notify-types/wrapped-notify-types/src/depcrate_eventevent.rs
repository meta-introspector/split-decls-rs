// Generated macro for Event (struct)
macro_rules! Depcrate_eventEvent {
() => {
// Module: crate::event
// Provides: {"Event"}
// Dependencies: {}
# [doc = " Notify event."] # [doc = ""] # [doc = " You might want to check [`Event::need_rescan`] to make sure no event was missed before you"] # [doc = " received this one."] # [derive (Clone)] # [cfg_attr (feature = "serde" , derive (Serialize , Deserialize))] pub struct Event { # [doc = " Kind or type of the event."] # [doc = ""] # [doc = " This is a hierarchy of enums describing the event as precisely as possible. All enums in"] # [doc = " the hierarchy have two variants always present, `Any` and `Other`, accompanied by one or"] # [doc = " more specific variants."] # [doc = ""] # [doc = " `Any` should be used when more detail about the event is not known beyond the variant"] # [doc = " already selected. For example, `AccessMode::Any` means a file has been accessed, but that's"] # [doc = " all we know."] # [doc = ""] # [doc = " `Other` should be used when more detail _is_ available, but cannot be encoded as one of the"] # [doc = " defined variants. When specifying `Other`, the event attributes should contain an `Info`"] # [doc = " entry with a short string identifying this detail. That string is to be considered part of"] # [doc = " the interface of the backend (i.e. a change should probably be breaking)."] # [doc = ""] # [doc = " For example, `CreateKind::Other` with an `Info(\"mount\")` may indicate the binding of a"] # [doc = " mount. The documentation of the particular backend should indicate if any `Other` events"] # [doc = " are generated, and what their description means."] # [doc = ""] # [doc = " The `EventKind::Any` variant should be used as the \"else\" case when mapping native kernel"] # [doc = " bitmasks or bitmaps, such that if the mask is ever extended with new event types the"] # [doc = " backend will not gain bugs due to not matching new unknown event types."] # [cfg_attr (all (feature = "serde" , not (feature = "serialization-compat-6")) , serde (flatten))] # [cfg_attr (all (feature = "serde" , feature = "serialization-compat-6") , serde (rename = "type"))] pub kind : EventKind , # [doc = " Paths the event is about, if known."] # [doc = ""] # [doc = " If an event concerns two or more paths, and the paths are known at the time of event"] # [doc = " creation, they should all go in this `Vec`. Otherwise, using the `Tracker` attr may be more"] # [doc = " appropriate."] # [doc = ""] # [doc = " The order of the paths is likely to be significant! For example, renames where both ends of"] # [doc = " the name change are known will have the \"source\" path first, and the \"target\" path last."] pub paths : Vec < PathBuf > , # [doc = " Additional attributes of the event."] # [doc = ""] # [doc = " Arbitrary data may be added to this field, without restriction beyond the `Sync` and"] # [doc = " `Clone` properties. Some data added here is considered for comparing and hashing, but not"] # [doc = " all: at this writing this is `Tracker`, `Flag`, `Info`, and `Source`."] # [cfg_attr (feature = "serde" , serde (default))] pub attrs : EventAttributes , }
};
}
