macro_rules! deps {
    () => {
        Emit!();
        Check!();
        Parser!();
    };
}

macro_rules! Mode {
    () => {
        deps!();
        # [doc = " Parser mode: influences how combinators build values"] # [doc = ""] # [doc = " the [Parser] trait is generic over types implementing [Mode]. Its method are"] # [doc = " called to produce and manipulate output values or errors."] # [doc = ""] # [doc = " The main implementations of this trait are:"] # [doc = " * [Emit]: produce a value"] # [doc = " * [Check]: apply the parser but do not generate a value"] pub trait Mode { # [doc = " The output type that may be generated"] type Output < T > ; # [doc = " Produces a value"] fn bind < T , F : FnOnce () -> T > (f : F) -> Self :: Output < T > ; # [doc = " Applies a function over the produced value"] fn map < T , U , F : FnOnce (T) -> U > (x : Self :: Output < T > , f : F) -> Self :: Output < U > ; # [doc = " Combines two values generated by previous parsers"] fn combine < T , U , V , F : FnOnce (T , U) -> V > (x : Self :: Output < T > , y : Self :: Output < U > , f : F ,) -> Self :: Output < V > ; }
    };
}

Mode!()