// Generated macro for impl_135 (impl)
macro_rules! Depcrateimpl_135 {
() => {
// Module: crate
// Provides: {"impl_135"}
// Dependencies: {}
# [doc = " Invoke or fetch the compiler or archiver."] impl Build { # [doc = " Run the compiler to test if it accepts the given flag."] # [doc = ""] # [doc = " For a convenience method for setting flags conditionally,"] # [doc = " see `flag_if_supported()`."] # [doc = ""] # [doc = " It may return error if it's unable to run the compiler with a test file"] # [doc = " (e.g. the compiler is missing or a write to the `out_dir` failed)."] # [doc = ""] # [doc = " Note: Once computed, the result of this call is stored in the"] # [doc = " `known_flag_support` field. If `is_flag_supported(flag)`"] # [doc = " is called again, the result will be read from the hash table."] pub fn is_flag_supported (& self , flag : impl AsRef < OsStr >) -> Result < bool , Error > { self . is_flag_supported_inner (flag . as_ref () , & self . get_base_compiler () ? , & self . get_target () ? ,) } fn ensure_check_file (& self) -> Result < PathBuf , Error > { let out_dir = self . get_out_dir () ? ; let src = if self . cuda { assert ! (self . cpp) ; out_dir . join ("flag_check.cu") } else if self . cpp { out_dir . join ("flag_check.cpp") } else { out_dir . join ("flag_check.c") } ; if ! src . exists () { let mut f = fs :: File :: create (& src) ? ; write ! (f , "int main(void) {{ return 0; }}") ? ; } Ok (src) } fn is_flag_supported_inner (& self , flag : & OsStr , tool : & Tool , target : & TargetInfo < '_ > ,) -> Result < bool , Error > { let compiler_flag = CompilerFlag { compiler : tool . path () . into () , flag : flag . into () , } ; if let Some (is_supported) = self . build_cache . known_flag_support_status_cache . read () . unwrap () . get (& compiler_flag) . cloned () { return Ok (is_supported) ; } let out_dir = self . get_out_dir () ? ; let src = self . ensure_check_file () ? ; let obj = out_dir . join ("flag_check") ; let mut compiler = { let mut cfg = Build :: new () ; cfg . flag (flag) . compiler (tool . path ()) . cargo_metadata (self . cargo_output . metadata) . opt_level (0) . debug (false) . cpp (self . cpp) . cuda (self . cuda) . inherit_rustflags (false) . emit_rerun_if_env_changed (self . emit_rerun_if_env_changed) ; if let Some (target) = & self . target { cfg . target (target) ; } if let Some (host) = & self . host { cfg . host (host) ; } cfg . try_get_compiler () ? } ; if compiler . family . verbose_stderr () { compiler . remove_arg ("-v" . into ()) ; } if compiler . is_like_clang () { compiler . push_cc_arg ("-Wno-unused-command-line-argument" . into ()) ; } let mut cmd = compiler . to_command () ; command_add_output_file (& mut cmd , & obj , CmdAddOutputFileArgs { cuda : self . cuda , is_assembler_msvc : false , msvc : compiler . is_like_msvc () , clang : compiler . is_like_clang () , gnu : compiler . is_like_gnu () , is_asm : false , is_arm : is_arm (target) , } ,) ; cmd . arg ("-c") ; if compiler . supports_path_delimiter () { cmd . arg ("--") ; } cmd . arg (& src) ; if compiler . is_like_msvc () { for (key , value) in & tool . env { if key == "LIB" { cmd . env ("LIB" , value) ; break ; } } } let output = cmd . current_dir (out_dir) . output () ? ; let is_supported = output . status . success () && output . stderr . is_empty () ; self . build_cache . known_flag_support_status_cache . write () . unwrap () . insert (compiler_flag , is_supported) ; Ok (is_supported) } # [doc = " Run the compiler, generating the file `output`"] # [doc = ""] # [doc = " This will return a result instead of panicking; see [`Self::compile()`] for"] # [doc = " the complete description."] pub fn try_compile (& self , output : & str) -> Result < () , Error > { let mut output_components = Path :: new (output) . components () ; match (output_components . next () , output_components . next ()) { (Some (Component :: Normal (_)) , None) => { } _ => { return Err (Error :: new (ErrorKind :: InvalidArgument , "argument of `compile` must be a single normal path component" ,)) ; } } let (lib_name , gnu_lib_name) = if output . starts_with ("lib") && output . ends_with (".a") { (& output [3 .. output . len () - 2] , output . to_owned ()) } else { let mut gnu = String :: with_capacity (5 + output . len ()) ; gnu . push_str ("lib") ; gnu . push_str (output) ; gnu . push_str (".a") ; (output , gnu) } ; let dst = self . get_out_dir () ? ; let objects = objects_from_files (& self . files , & dst) ? ; self . compile_objects (& objects) ? ; self . assemble (lib_name , & dst . join (gnu_lib_name) , & objects) ? ; let target = self . get_target () ? ; if target . env == "msvc" { let compiler = self . get_base_compiler () ? ; let atlmfc_lib = compiler . env () . iter () . find (| & (var , _) | var . as_os_str () == OsStr :: new ("LIB")) . and_then (| (_ , lib_paths) | { env :: split_paths (lib_paths) . find (| path | { let sub = Path :: new ("atlmfc/lib") ; path . ends_with (sub) || path . parent () . map_or (false , | p | p . ends_with (sub)) }) }) ; if let Some (atlmfc_lib) = atlmfc_lib { self . cargo_output . print_metadata (& format_args ! ("cargo:rustc-link-search=native={}" , atlmfc_lib . display ())) ; } } if self . link_lib_modifiers . is_empty () { self . cargo_output . print_metadata (& format_args ! ("cargo:rustc-link-lib=static={lib_name}")) ; } else { self . cargo_output . print_metadata (& format_args ! ("cargo:rustc-link-lib=static:{}={}" , JoinOsStrs { slice : & self . link_lib_modifiers , delimiter : ',' } , lib_name)) ; } self . cargo_output . print_metadata (& format_args ! ("cargo:rustc-link-search=native={}" , dst . display ())) ; if self . cpp { if let Some (stdlib) = self . get_cpp_link_stdlib () ? { if self . cpp_link_stdlib_static { self . cargo_output . print_metadata (& format_args ! ("cargo:rustc-link-lib=static={}" , stdlib . display ())) ; } else { self . cargo_output . print_metadata (& format_args ! ("cargo:rustc-link-lib={}" , stdlib . display ())) ; } } if target . arch == "wasm32" { if target . os == "wasi" { if let Ok (wasi_sysroot) = self . wasi_sysroot () { self . cargo_output . print_metadata (& format_args ! ("cargo:rustc-flags=-L {}/lib/{} -lstatic=c++ -lstatic=c++abi" , Path :: new (& wasi_sysroot) . display () , self . get_raw_target () ?)) ; } } else if target . os == "linux" { let musl_sysroot = self . wasm_musl_sysroot () . unwrap () ; self . cargo_output . print_metadata (& format_args ! ("cargo:rustc-flags=-L {}/lib -lstatic=c++ -lstatic=c++abi" , Path :: new (& musl_sysroot) . display () ,)) ; } } } let cudart = match & self . cudart { Some (opt) => opt , None => "none" , } ; if cudart != "none" { if let Some (nvcc) = self . which (& self . get_compiler () . path , None) { let mut libtst = false ; let mut libdir = nvcc ; libdir . pop () ; libdir . push ("..") ; if cfg ! (target_os = "linux") { libdir . push ("targets") ; libdir . push (format ! ("{}-linux" , target . arch)) ; libdir . push ("lib") ; libtst = true ; } else if cfg ! (target_env = "msvc") { libdir . push ("lib") ; match target . arch { "x86_64" => { libdir . push ("x64") ; libtst = true ; } "x86" => { libdir . push ("Win32") ; libtst = true ; } _ => libtst = false , } } if libtst && libdir . is_dir () { self . cargo_output . print_metadata (& format_args ! ("cargo:rustc-link-search=native={}" , libdir . to_str () . unwrap ())) ; } let lib = match cudart { "shared" => "cudart" , "static" => "cudart_static" , bad => panic ! ("unsupported cudart option: {}" , bad) , } ; self . cargo_output . print_metadata (& format_args ! ("cargo:rustc-link-lib={lib}")) ; } } Ok (()) } # [doc = " Run the compiler, generating the file `output`"] # [doc = ""] # [doc = " # Library name"] # [doc = ""] # [doc = " The `output` string argument determines the file name for the compiled"] # [doc = " library. The Rust compiler will create an assembly named \"lib\"+output+\".a\"."] # [doc = " MSVC will create a file named output+\".lib\"."] # [doc = ""] # [doc = " The choice of `output` is close to arbitrary, but:"] # [doc = ""] # [doc = " - must be nonempty,"] # [doc = " - must not contain a path separator (`/`),"] # [doc = " - must be unique across all `compile` invocations made by the same build"] # [doc = "   script."] # [doc = ""] # [doc = " If your build script compiles a single source file, the base name of"] # [doc = " that source file would usually be reasonable:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " cc::Build::new().file(\"blobstore.c\").compile(\"blobstore\");"] # [doc = " ```"] # [doc = ""] # [doc = " Compiling multiple source files, some people use their crate's name, or"] # [doc = " their crate's name + \"-cc\"."] # [doc = ""] # [doc = " Otherwise, please use your imagination."] # [doc = ""] # [doc = " For backwards compatibility, if `output` starts with \"lib\" *and* ends"] # [doc = " with \".a\", a second \"lib\" prefix and \".a\" suffix do not get added on,"] # [doc = " but this usage is deprecated; please omit `lib` and `.a` in the argument"] # [doc = " that you pass."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `output` is not formatted correctly or if one of the underlying"] # [doc = " compiler commands fails. It can also panic if it fails reading file names"] # [doc = " or creating directories."] pub fn compile (& self , output : & str) { if let Err (e) = self . try_compile (output) { fail (& e . message) ; } } # [doc = " Run the compiler, generating intermediate files, but without linking"] # [doc = " them into an archive file."] # [doc = ""] # [doc = " This will return a list of compiled object files, in the same order"] # [doc = " as they were passed in as `file`/`files` methods."] pub fn compile_intermediates (& self) -> Vec < PathBuf > { match self . try_compile_intermediates () { Ok (v) => v , Err (e) => fail (& e . message) , } } # [doc = " Run the compiler, generating intermediate files, but without linking"] # [doc = " them into an archive file."] # [doc = ""] # [doc = " This will return a result instead of panicking; see `compile_intermediates()` for the complete description."] pub fn try_compile_intermediates (& self) -> Result < Vec < PathBuf > , Error > { let dst = self . get_out_dir () ? ; let objects = objects_from_files (& self . files , & dst) ? ; self . compile_objects (& objects) ? ; Ok (objects . into_iter () . map (| v | v . dst) . collect ()) } fn compile_objects (& self , objs : & [Object]) -> Result < () , Error > { check_disabled () ? ; # [cfg (feature = "parallel")] if objs . len () > 1 { return parallel :: run_commands_in_parallel (& self . cargo_output , & mut objs . iter () . map (| obj | self . create_compile_object_cmd (obj)) ,) ; } for obj in objs { let mut cmd = self . create_compile_object_cmd (obj) ? ; run (& mut cmd , & self . cargo_output) ? ; } Ok (()) } fn create_compile_object_cmd (& self , obj : & Object) -> Result < Command , Error > { let asm_ext = AsmFileExt :: from_path (& obj . src) ; let is_asm = asm_ext . is_some () ; let target = self . get_target () ? ; let msvc = target . env == "msvc" ; let compiler = self . try_get_compiler () ? ; let is_assembler_msvc = msvc && asm_ext == Some (AsmFileExt :: DotAsm) ; let mut cmd = if is_assembler_msvc { self . msvc_macro_assembler () ? } else { let mut cmd = compiler . to_command () ; for (a , b) in self . env . iter () { cmd . env (a , b) ; } cmd } ; let is_arm = is_arm (& target) ; command_add_output_file (& mut cmd , & obj . dst , CmdAddOutputFileArgs { cuda : self . cuda , is_assembler_msvc , msvc : compiler . is_like_msvc () , clang : compiler . is_like_clang () , gnu : compiler . is_like_gnu () , is_asm , is_arm , } ,) ; if ! is_assembler_msvc || ! is_arm { cmd . arg ("-c") ; } if self . cuda && self . cuda_file_count () > 1 { cmd . arg ("--device-c") ; } if is_asm { cmd . args (self . asm_flags . iter () . map (std :: ops :: Deref :: deref)) ; } if compiler . supports_path_delimiter () && ! is_assembler_msvc { cmd . arg ("--") ; } cmd . arg (& obj . src) ; if cfg ! (target_os = "macos") { self . fix_env_for_apple_os (& mut cmd) ? ; } Ok (cmd) } # [doc = " This will return a result instead of panicking; see [`Self::expand()`] for"] # [doc = " the complete description."] pub fn try_expand (& self) -> Result < Vec < u8 > , Error > { let compiler = self . try_get_compiler () ? ; let mut cmd = compiler . to_command () ; for (a , b) in self . env . iter () { cmd . env (a , b) ; } cmd . arg ("-E") ; assert ! (self . files . len () <= 1 , "Expand may only be called for a single file") ; let is_asm = self . files . iter () . map (std :: ops :: Deref :: deref) . find_map (AsmFileExt :: from_path) . is_some () ; if compiler . family == (ToolFamily :: Msvc { clang_cl : true }) && ! is_asm { cmd . arg ("--") ; } cmd . args (self . files . iter () . map (std :: ops :: Deref :: deref)) ; run_output (& mut cmd , & self . cargo_output) } # [doc = " Run the compiler, returning the macro-expanded version of the input files."] # [doc = ""] # [doc = " This is only relevant for C and C++ files."] # [doc = ""] # [doc = " # Panics"] # [doc = " Panics if more than one file is present in the config, or if compiler"] # [doc = " path has an invalid file name."] # [doc = ""] # [doc = " # Example"] # [doc = " ```no_run"] # [doc = " let out = cc::Build::new().file(\"src/foo.c\").expand();"] # [doc = " ```"] pub fn expand (& self) -> Vec < u8 > { match self . try_expand () { Err (e) => fail (& e . message) , Ok (v) => v , } } # [doc = " Get the compiler that's in use for this configuration."] # [doc = ""] # [doc = " This function will return a `Tool` which represents the culmination"] # [doc = " of this configuration at a snapshot in time. The returned compiler can"] # [doc = " be inspected (e.g. the path, arguments, environment) to forward along to"] # [doc = " other tools, or the `to_command` method can be used to invoke the"] # [doc = " compiler itself."] # [doc = ""] # [doc = " This method will take into account all configuration such as debug"] # [doc = " information, optimization level, include directories, defines, etc."] # [doc = " Additionally, the compiler binary in use follows the standard"] # [doc = " conventions for this path, e.g. looking at the explicitly set compiler,"] # [doc = " environment variables (a number of which are inspected here), and then"] # [doc = " falling back to the default configuration."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if an error occurred while determining the architecture."] pub fn get_compiler (& self) -> Tool { match self . try_get_compiler () { Ok (tool) => tool , Err (e) => fail (& e . message) , } } # [doc = " Get the compiler that's in use for this configuration."] # [doc = ""] # [doc = " This will return a result instead of panicking; see"] # [doc = " [`get_compiler()`](Self::get_compiler) for the complete description."] pub fn try_get_compiler (& self) -> Result < Tool , Error > { let opt_level = self . get_opt_level () ? ; let target = self . get_target () ? ; let mut cmd = self . get_base_compiler () ? ; if matches ! (cmd . family , ToolFamily :: Msvc { clang_cl : false }) { cmd . env . push (("VSLANG" . into () , "1033" . into ())) ; } else { cmd . env . push (("LC_ALL" . into () , "C" . into ())) ; } let no_defaults = self . no_default_flags || self . getenv_boolean ("CRATE_CC_NO_DEFAULTS") ; if ! no_defaults { self . add_default_flags (& mut cmd , & target , & opt_level) ? ; } if let Some (ref std) = self . std { let separator = match cmd . family { ToolFamily :: Msvc { .. } => ':' , ToolFamily :: Gnu | ToolFamily :: Clang { .. } => '=' , } ; cmd . push_cc_arg (format ! ("-std{separator}{std}") . into ()) ; } for directory in self . include_directories . iter () { cmd . args . push ("-I" . into ()) ; cmd . args . push (directory . as_os_str () . into ()) ; } if self . warnings_into_errors { let warnings_to_errors_flag = cmd . family . warnings_to_errors_flag () . into () ; cmd . push_cc_arg (warnings_to_errors_flag) ; } let envflags = self . envflags (if self . cpp { "CXXFLAGS" } else { "CFLAGS" }) ? ; if self . warnings . unwrap_or (envflags . is_none ()) { let wflags = cmd . family . warnings_flags () . into () ; cmd . push_cc_arg (wflags) ; } if self . extra_warnings . unwrap_or (envflags . is_none ()) { if let Some (wflags) = cmd . family . extra_warnings_flags () { cmd . push_cc_arg (wflags . into ()) ; } } if self . inherit_rustflags { self . add_inherited_rustflags (& mut cmd , & target) ? ; } for flag in self . flags . iter () { cmd . args . push ((* * flag) . into ()) ; } for flag in self . flags_supported . iter () { if self . is_flag_supported_inner (flag , & cmd , & target) . unwrap_or (false) { cmd . push_cc_arg ((* * flag) . into ()) ; } } for (key , value) in self . definitions . iter () { if let Some (ref value) = * value { cmd . args . push (format ! ("-D{key}={value}") . into ()) ; } else { cmd . args . push (format ! ("-D{key}") . into ()) ; } } if let Some (flags) = & envflags { for arg in flags { cmd . push_cc_arg (arg . into ()) ; } } Ok (cmd) } fn add_default_flags (& self , cmd : & mut Tool , target : & TargetInfo < '_ > , opt_level : & str ,) -> Result < () , Error > { let raw_target = self . get_raw_target () ? ; match cmd . family { ToolFamily :: Msvc { .. } => { cmd . push_cc_arg ("-nologo" . into ()) ; let crt_flag = match self . static_crt { Some (true) => "-MT" , Some (false) => "-MD" , None => { let features = self . getenv ("CARGO_CFG_TARGET_FEATURE") ; let features = features . as_deref () . unwrap_or_default () ; if features . to_string_lossy () . contains ("crt-static") { "-MT" } else { "-MD" } } } ; cmd . push_cc_arg (crt_flag . into ()) ; match opt_level { "z" | "s" | "1" => cmd . push_opt_unless_duplicate ("-O1" . into ()) , "2" | "3" => cmd . push_opt_unless_duplicate ("-O2" . into ()) , _ => { } } } ToolFamily :: Gnu | ToolFamily :: Clang { .. } => { if opt_level == "z" && ! cmd . is_like_clang () { cmd . push_opt_unless_duplicate ("-Os" . into ()) ; } else { cmd . push_opt_unless_duplicate (format ! ("-O{opt_level}") . into ()) ; } if cmd . is_like_clang () && target . os == "android" { cmd . push_opt_unless_duplicate ("-DANDROID" . into ()) ; } if target . os != "ios" && target . os != "watchos" && target . os != "tvos" && target . os != "visionos" { cmd . push_cc_arg ("-ffunction-sections" . into ()) ; cmd . push_cc_arg ("-fdata-sections" . into ()) ; } if self . pic . unwrap_or (target . os != "windows" && target . os != "none" && target . os != "uefi" && target . arch != "wasm32" && target . arch != "wasm64" ,) { cmd . push_cc_arg ("-fPIC" . into ()) ; if (target . os == "linux" || target . os == "android") && ! self . use_plt . unwrap_or (true) { cmd . push_cc_arg ("-fno-plt" . into ()) ; } } if target . arch == "wasm32" || target . arch == "wasm64" { cmd . push_cc_arg ("-fno-exceptions" . into ()) ; } if target . os == "wasi" { if let Ok (wasi_sysroot) = self . wasi_sysroot () { cmd . push_cc_arg (format ! ("--sysroot={}" , Path :: new (& wasi_sysroot) . display ()) . into () ,) ; } if raw_target . contains ("threads") { cmd . push_cc_arg ("-pthread" . into ()) ; } } if target . os == "nto" { let arg = match target . full_arch { "x86" | "i586" => "-Vgcc_ntox86_cxx" , "aarch64" => "-Vgcc_ntoaarch64le_cxx" , "x86_64" => "-Vgcc_ntox86_64_cxx" , _ => { return Err (Error :: new (ErrorKind :: InvalidTarget , format ! ("Unknown architecture for Neutrino QNX: {}" , target . arch) ,)) } } ; cmd . push_cc_arg (arg . into ()) ; } } } if self . get_debug () { if self . cuda { cmd . args . push ("-G" . into ()) ; } let family = cmd . family ; family . add_debug_flags (cmd , self . get_dwarf_version ()) ; } if self . get_force_frame_pointer () { let family = cmd . family ; family . add_force_frame_pointer (cmd) ; } if ! cmd . is_like_msvc () { if target . arch == "x86" { cmd . args . push ("-m32" . into ()) ; } else if target . abi == "x32" { cmd . args . push ("-mx32" . into ()) ; } else if target . os == "aix" { if cmd . family == ToolFamily :: Gnu { cmd . args . push ("-maix64" . into ()) ; } else { cmd . args . push ("-m64" . into ()) ; } } else if target . arch == "x86_64" || target . arch == "powerpc64" { cmd . args . push ("-m64" . into ()) ; } } match cmd . family { ToolFamily :: Clang { .. } => { if ! (cmd . has_internal_target_arg || (target . os == "android" && android_clang_compiler_uses_target_arg_internally (& cmd . path))) { if target . os == "freebsd" { if self . cpp && self . cpp_set_stdlib . is_none () { cmd . push_cc_arg ("-stdlib=libc++" . into ()) ; } } else if target . arch == "wasm32" && target . os == "linux" { for x in & ["atomics" , "bulk-memory" , "mutable-globals" , "sign-ext" , "exception-handling" ,] { cmd . push_cc_arg (format ! ("-m{x}") . into ()) ; } for x in & ["wasm-exceptions" , "declspec"] { cmd . push_cc_arg (format ! ("-f{x}") . into ()) ; } let musl_sysroot = self . wasm_musl_sysroot () . unwrap () ; cmd . push_cc_arg (format ! ("--sysroot={}" , Path :: new (& musl_sysroot) . display ()) . into () ,) ; cmd . push_cc_arg ("-pthread" . into ()) ; } let version = if target . os == "visionos" || target . env == "macabi" { Some (self . apple_deployment_target (target)) } else { None } ; let clang_target = target . llvm_target (& self . get_raw_target () ? , version . as_deref ()) ; cmd . push_cc_arg (format ! ("--target={clang_target}") . into ()) ; } } ToolFamily :: Msvc { clang_cl } => { cmd . push_cc_arg ("-Brepro" . into ()) ; if clang_cl { if target . arch == "x86_64" { cmd . push_cc_arg ("-m64" . into ()) ; } else if target . arch == "x86" { cmd . push_cc_arg ("-m32" . into ()) ; cmd . push_cc_arg ("-arch:SSE2" . into ()) ; } else { cmd . push_cc_arg (format ! ("--target={}" , target . llvm_target (& self . get_raw_target () ?, None)) . into () ,) ; } } else if target . full_arch == "i586" { cmd . push_cc_arg ("-arch:IA32" . into ()) ; } else if target . full_arch == "arm64ec" { cmd . push_cc_arg ("-arm64EC" . into ()) ; } if target . arch == "arm" { cmd . args . push ("-D_ARM_WINAPI_PARTITION_DESKTOP_SDK_AVAILABLE=1" . into ()) ; } } ToolFamily :: Gnu => { if target . vendor == "kmc" { cmd . args . push ("-finput-charset=utf-8" . into ()) ; } if self . static_flag . is_none () { let features = self . getenv ("CARGO_CFG_TARGET_FEATURE") ; let features = features . as_deref () . unwrap_or_default () ; if features . to_string_lossy () . contains ("crt-static") { cmd . args . push ("-static" . into ()) ; } } if (target . full_arch . starts_with ("armv7") || target . full_arch . starts_with ("thumbv7")) && (target . os == "linux" || target . vendor == "kmc") { cmd . args . push ("-march=armv7-a" . into ()) ; if target . abi == "eabihf" { cmd . args . push ("-mfpu=vfpv3-d16" . into ()) ; cmd . args . push ("-mfloat-abi=hard" . into ()) ; } } if target . os == "android" && target . full_arch . contains ("v7") { cmd . args . push ("-march=armv7-a" . into ()) ; cmd . args . push ("-mthumb" . into ()) ; if ! target . full_arch . contains ("neon") { cmd . args . push ("-mfpu=vfpv3-d16" . into ()) ; } cmd . args . push ("-mfloat-abi=softfp" . into ()) ; } if target . full_arch . contains ("neon") { cmd . args . push ("-mfpu=neon-vfpv4" . into ()) ; } if target . full_arch == "armv4t" && target . os == "linux" { cmd . args . push ("-march=armv4t" . into ()) ; cmd . args . push ("-marm" . into ()) ; cmd . args . push ("-mfloat-abi=soft" . into ()) ; } if target . full_arch == "armv5te" && target . os == "linux" { cmd . args . push ("-march=armv5te" . into ()) ; cmd . args . push ("-marm" . into ()) ; cmd . args . push ("-mfloat-abi=soft" . into ()) ; } if target . full_arch == "arm" && target . os == "linux" { cmd . args . push ("-march=armv6" . into ()) ; cmd . args . push ("-marm" . into ()) ; if target . abi == "eabihf" { cmd . args . push ("-mfpu=vfp" . into ()) ; } else { cmd . args . push ("-mfloat-abi=soft" . into ()) ; } } if target . full_arch == "i586" && target . os == "linux" { cmd . args . push ("-march=pentium" . into ()) ; } if target . full_arch == "i686" && target . os == "linux" { cmd . args . push ("-march=i686" . into ()) ; } if target . arch == "x86" && target . env == "musl" { cmd . args . push ("-Wl,-melf_i386" . into ()) ; } if target . arch == "arm" && target . os == "none" && target . abi == "eabihf" { cmd . args . push ("-mfloat-abi=hard" . into ()) } if target . full_arch . starts_with ("thumb") { cmd . args . push ("-mthumb" . into ()) ; } if target . full_arch . starts_with ("thumbv6m") { cmd . args . push ("-march=armv6s-m" . into ()) ; } if target . full_arch . starts_with ("thumbv7em") { cmd . args . push ("-march=armv7e-m" . into ()) ; if target . abi == "eabihf" { cmd . args . push ("-mfpu=fpv4-sp-d16" . into ()) } } if target . full_arch . starts_with ("thumbv7m") { cmd . args . push ("-march=armv7-m" . into ()) ; } if target . full_arch . starts_with ("thumbv8m.base") { cmd . args . push ("-march=armv8-m.base" . into ()) ; } if target . full_arch . starts_with ("thumbv8m.main") { cmd . args . push ("-march=armv8-m.main" . into ()) ; if target . abi == "eabihf" { cmd . args . push ("-mfpu=fpv5-sp-d16" . into ()) } } if target . full_arch . starts_with ("armebv7r") | target . full_arch . starts_with ("armv7r") { if target . full_arch . starts_with ("armeb") { cmd . args . push ("-mbig-endian" . into ()) ; } else { cmd . args . push ("-mlittle-endian" . into ()) ; } cmd . args . push ("-marm" . into ()) ; cmd . args . push ("-march=armv7-r" . into ()) ; if target . abi == "eabihf" { cmd . args . push ("-mfpu=vfpv3-d16" . into ()) } } if target . full_arch . starts_with ("armv7a") { cmd . args . push ("-march=armv7-a" . into ()) ; if target . abi == "eabihf" { cmd . args . push ("-mfpu=vfpv3-d16" . into ()) ; } } if target . arch == "riscv32" || target . arch == "riscv64" { let arch = & target . full_arch [5 ..] ; if arch . starts_with ("64") { if matches ! (target . os , "linux" | "freebsd" | "netbsd") { cmd . args . push (("-march=rv64gc") . into ()) ; cmd . args . push ("-mabi=lp64d" . into ()) ; } else { cmd . args . push (("-march=rv" . to_owned () + arch) . into ()) ; cmd . args . push ("-mabi=lp64" . into ()) ; } } else if arch . starts_with ("32") { if target . os == "linux" { cmd . args . push (("-march=rv32gc") . into ()) ; cmd . args . push ("-mabi=ilp32d" . into ()) ; } else { cmd . args . push (("-march=rv" . to_owned () + arch) . into ()) ; cmd . args . push ("-mabi=ilp32" . into ()) ; } } else { cmd . args . push ("-mcmodel=medany" . into ()) ; } } } } if raw_target == "wasm32v1-none" { cmd . push_cc_arg ("-mcpu=mvp" . into ()) ; cmd . push_cc_arg ("-mmutable-globals" . into ()) ; } if target . os == "solaris" || target . os == "illumos" { cmd . args . push ("-D_REENTRANT" . into ()) ; } if target . vendor == "apple" { self . apple_flags (cmd) ? ; } if self . static_flag . unwrap_or (false) { cmd . args . push ("-static" . into ()) ; } if self . shared_flag . unwrap_or (false) { cmd . args . push ("-shared" . into ()) ; } if self . cpp { match (self . cpp_set_stdlib . as_ref () , cmd . family) { (None , _) => { } (Some (stdlib) , ToolFamily :: Gnu) | (Some (stdlib) , ToolFamily :: Clang { .. }) => { cmd . push_cc_arg (format ! ("-stdlib=lib{stdlib}") . into ()) ; } _ => { self . cargo_output . print_warning (& format_args ! ("cpp_set_stdlib is specified, but the {:?} compiler does not support this option, ignored" , cmd . family)) ; } } } Ok (()) } fn add_inherited_rustflags (& self , cmd : & mut Tool , target : & TargetInfo < '_ > ,) -> Result < () , Error > { let env_os = match self . getenv ("CARGO_ENCODED_RUSTFLAGS") { Some (env) => env , None => return Ok (()) , } ; let env = env_os . to_string_lossy () ; let codegen_flags = RustcCodegenFlags :: parse (& env) ? ; codegen_flags . cc_flags (self , cmd , target) ; Ok (()) } fn msvc_macro_assembler (& self) -> Result < Command , Error > { let target = self . get_target () ? ; let tool = match target . arch { "x86_64" => "ml64.exe" , "arm" => "armasm.exe" , "aarch64" | "arm64ec" => "armasm64.exe" , _ => "ml.exe" , } ; let mut cmd = self . find_msvc_tools_find (& target , tool) . unwrap_or_else (| | self . cmd (tool)) ; cmd . arg ("-nologo") ; for directory in self . include_directories . iter () { cmd . arg ("-I") . arg (& * * directory) ; } if is_arm (& target) { if self . get_debug () { cmd . arg ("-g") ; } if target . arch == "arm64ec" { cmd . args (["-machine" , "ARM64EC"]) ; } for (key , value) in self . definitions . iter () { cmd . arg ("-PreDefine") ; if let Some (ref value) = * value { if let Ok (i) = value . parse :: < i32 > () { cmd . arg (format ! ("{key} SETA {i}")) ; } else if value . starts_with ('"') && value . ends_with ('"') { cmd . arg (format ! ("{key} SETS {value}")) ; } else { cmd . arg (format ! ("{key} SETS \"{value}\"")) ; } } else { cmd . arg (format ! ("{} SETL {}" , key , "{TRUE}")) ; } } } else { if self . get_debug () { cmd . arg ("-Zi") ; } for (key , value) in self . definitions . iter () { if let Some (ref value) = * value { cmd . arg (format ! ("-D{key}={value}")) ; } else { cmd . arg (format ! ("-D{key}")) ; } } } if target . arch == "x86" { cmd . arg ("-safeseh") ; } Ok (cmd) } fn assemble (& self , lib_name : & str , dst : & Path , objs : & [Object]) -> Result < () , Error > { let _ = fs :: remove_file (dst) ; let objs : Vec < _ > = objs . iter () . map (| o | o . dst . as_path ()) . chain (self . objects . iter () . map (std :: ops :: Deref :: deref)) . collect () ; for chunk in objs . chunks (100) { self . assemble_progressive (dst , chunk) ? ; } if self . cuda && self . cuda_file_count () > 0 { let out_dir = self . get_out_dir () ? ; let dlink = out_dir . join (lib_name . to_owned () + "_dlink.o") ; let mut nvcc = self . get_compiler () . to_command () ; nvcc . arg ("--device-link") . arg ("-o") . arg (& dlink) . arg (dst) ; run (& mut nvcc , & self . cargo_output) ? ; self . assemble_progressive (dst , & [dlink . as_path ()]) ? ; } let target = self . get_target () ? ; if target . env == "msvc" { let lib_dst = dst . with_file_name (format ! ("{lib_name}.lib")) ; let _ = fs :: remove_file (& lib_dst) ; match fs :: hard_link (dst , & lib_dst) . or_else (| _ | { fs :: copy (dst , & lib_dst) . map (| _ | ()) }) { Ok (_) => () , Err (_) => { return Err (Error :: new (ErrorKind :: IOError , "Could not copy or create a hard-link to the generated lib file." ,)) ; } } ; } else { let mut ar = self . try_get_archiver () ? ; run (ar . arg ("s") . arg (dst) , & self . cargo_output) ? ; } Ok (()) } fn assemble_progressive (& self , dst : & Path , objs : & [& Path]) -> Result < () , Error > { let target = self . get_target () ? ; let (mut cmd , program , any_flags) = self . try_get_archiver_and_flags () ? ; if target . env == "msvc" && ! program . to_string_lossy () . contains ("llvm-ar") { let mut out = OsString :: from ("-out:") ; out . push (dst) ; cmd . arg (out) ; if ! any_flags { cmd . arg ("-nologo") ; } if dst . exists () { cmd . arg (dst) ; } cmd . args (objs) ; run (& mut cmd , & self . cargo_output) ? ; } else { cmd . env ("ZERO_AR_DATE" , "1") ; run (cmd . arg ("cq") . arg (dst) . args (objs) , & self . cargo_output) ? ; } Ok (()) } fn apple_flags (& self , cmd : & mut Tool) -> Result < () , Error > { let target = self . get_target () ? ; if cmd . is_like_gnu () { let arch = map_darwin_target_from_rust_to_compiler_architecture (& target) ; cmd . args . push ("-arch" . into ()) ; cmd . args . push (arch . into ()) ; } if cmd . is_like_gnu () || ! (target . os == "visionos" || target . env == "macabi") { let min_version = self . apple_deployment_target (& target) ; cmd . args . push (target . apple_version_flag (& min_version) . into ()) ; } if cmd . is_xctoolchain_clang () || target . os != "macos" { self . cargo_output . print_metadata (& format_args ! ("Detecting {:?} SDK path for {}" , target . os , target . apple_sdk_name () ,)) ; let sdk_path = self . apple_sdk_root (& target) ? ; cmd . args . push ("-isysroot" . into ()) ; cmd . args . push (OsStr :: new (& sdk_path) . to_owned ()) ; cmd . env . push (("SDKROOT" . into () , OsStr :: new (& sdk_path) . to_owned ())) ; if target . env == "macabi" { let ios_support = Path :: new (& sdk_path) . join ("System/iOSSupport") ; cmd . args . extend ([OsString :: from ("-isystem") , ios_support . join ("usr/include") . into () , OsString :: from ("-iframework") , ios_support . join ("System/Library/Frameworks") . into () , { let mut s = OsString :: from ("-L") ; s . push (ios_support . join ("usr/lib")) ; s } , { let mut s = OsString :: from ("-F") ; s . push (ios_support . join ("System/Library/Frameworks")) ; s } ,]) ; } } Ok (()) } fn cmd < P : AsRef < OsStr > > (& self , prog : P) -> Command { let mut cmd = Command :: new (prog) ; for (a , b) in self . env . iter () { cmd . env (a , b) ; } cmd } fn prefer_clang (& self) -> bool { if let Some (env) = self . getenv ("CARGO_ENCODED_RUSTFLAGS") { env . to_string_lossy () . contains ("linker-plugin-lto") } else { false } } fn get_base_compiler (& self) -> Result < Tool , Error > { let out_dir = self . get_out_dir () . ok () ; let out_dir = out_dir . as_deref () ; if let Some (c) = & self . compiler { return Ok (Tool :: new ((* * c) . to_owned () , & self . build_cache . cached_compiler_family , & self . cargo_output , out_dir ,)) ; } let target = self . get_target () ? ; let raw_target = self . get_raw_target () ? ; let msvc = if self . prefer_clang_cl_over_msvc { "clang-cl.exe" } else { "cl.exe" } ; let (env , gnu , traditional , clang) = if self . cpp { ("CXX" , "g++" , "c++" , "clang++") } else { ("CC" , "gcc" , "cc" , "clang") } ; let fallback = Cow :: Borrowed (Path :: new (traditional)) ; let default = if cfg ! (target_os = "solaris") || cfg ! (target_os = "illumos") { Cow :: Borrowed (Path :: new (gnu)) } else if self . prefer_clang () { self . which (Path :: new (clang) , None) . map (Cow :: Owned) . unwrap_or (fallback) } else { fallback } ; let cl_exe = self . find_msvc_tools_find_tool (& target , msvc) ; let tool_opt : Option < Tool > = self . env_tool (env) . map (| (tool , wrapper , args) | { let mut t = Tool :: with_args (tool , args . clone () , & self . build_cache . cached_compiler_family , & self . cargo_output , out_dir ,) ; if let Some (cc_wrapper) = wrapper { t . cc_wrapper_path = Some (Path :: new (& cc_wrapper) . to_owned ()) ; } for arg in args { t . cc_wrapper_args . push (arg . into ()) ; } t }) . or_else (| | { if target . os == "emscripten" { let tool = if self . cpp { "em++" } else { "emcc" } ; if cfg ! (windows) { let mut t = Tool :: with_family (PathBuf :: from ("cmd") , ToolFamily :: Clang { zig_cc : false } ,) ; t . args . push ("/c" . into ()) ; t . args . push (format ! ("{tool}.bat") . into ()) ; Some (t) } else { Some (Tool :: new (PathBuf :: from (tool) , & self . build_cache . cached_compiler_family , & self . cargo_output , out_dir ,)) } } else { None } }) . or_else (| | cl_exe . clone ()) ; let tool = match tool_opt { Some (t) => t , None => { let compiler : PathBuf = if cfg ! (windows) && target . os == "windows" { if target . env == "msvc" { msvc . into () } else { let cc = if target . abi == "llvm" { clang } else { gnu } ; format ! ("{cc}.exe") . into () } } else if target . os == "ios" || target . os == "watchos" || target . os == "tvos" || target . os == "visionos" { clang . into () } else if target . os == "android" { autodetect_android_compiler (& raw_target , gnu , clang) } else if target . os == "cloudabi" { format ! ("{}-{}-{}-{}" , target . full_arch , target . vendor , target . os , traditional) . into () } else if target . os == "wasi" { self . autodetect_wasi_compiler (& raw_target , clang) } else if target . arch == "wasm32" || target . arch == "wasm64" { clang . into () } else if target . os == "vxworks" { if self . cpp { "wr-c++" } else { "wr-cc" } . into () } else if target . arch == "arm" && target . vendor == "kmc" { format ! ("arm-kmc-eabi-{gnu}") . into () } else if target . arch == "aarch64" && target . vendor == "kmc" { format ! ("aarch64-kmc-elf-{gnu}") . into () } else if target . os == "nto" { if self . cpp { "q++" } else { "qcc" } . into () } else if self . get_is_cross_compile () ? { let prefix = self . prefix_for_target (& raw_target) ; match prefix { Some (prefix) => { let cc = if target . abi == "llvm" { clang } else { gnu } ; format ! ("{prefix}-{cc}") . into () } None => default . into () , } } else { default . into () } ; let mut t = Tool :: new (compiler , & self . build_cache . cached_compiler_family , & self . cargo_output , out_dir ,) ; if let Some (cc_wrapper) = self . rustc_wrapper_fallback () { t . cc_wrapper_path = Some (Path :: new (& cc_wrapper) . to_owned ()) ; } t } } ; let mut tool = if self . cuda { assert ! (tool . args . is_empty () , "CUDA compilation currently assumes empty pre-existing args") ; let nvcc = match self . getenv_with_target_prefixes ("NVCC") { Err (_) => PathBuf :: from ("nvcc") , Ok (nvcc) => PathBuf :: from (& * nvcc) , } ; let mut nvcc_tool = Tool :: with_features (nvcc , vec ! [] , self . cuda , & self . build_cache . cached_compiler_family , & self . cargo_output , out_dir ,) ; if self . ccbin { nvcc_tool . args . push (format ! ("-ccbin={}" , tool . path . display ()) . into ()) ; } if let Some (cc_wrapper) = self . rustc_wrapper_fallback () { nvcc_tool . cc_wrapper_path = Some (Path :: new (& cc_wrapper) . to_owned ()) ; } nvcc_tool . family = tool . family ; nvcc_tool } else { tool } ; if cfg ! (windows) && android_clang_compiler_uses_target_arg_internally (& tool . path) { if let Some (path) = tool . path . file_name () { let file_name = path . to_str () . unwrap () . to_owned () ; let (target , clang) = file_name . split_at (file_name . rfind ('-') . unwrap ()) ; tool . has_internal_target_arg = true ; tool . path . set_file_name (clang . trim_start_matches ('-')) ; tool . path . set_extension ("exe") ; tool . args . push (format ! ("--target={target}") . into ()) ; if target . contains ("i686-linux-android") { let (_ , version) = target . split_at (target . rfind ('d') . unwrap () + 1) ; if let Ok (version) = version . parse :: < u32 > () { if version > 15 && version < 25 { tool . args . push ("-mstackrealign" . into ()) ; } } } } ; } if ! cfg ! (windows) && target . os == "windows" && is_llvm_mingw_wrapper (& tool . path) { tool . has_internal_target_arg = true ; } if let Some (cl_exe) = cl_exe { if tool . family == (ToolFamily :: Msvc { clang_cl : true }) && tool . env . is_empty () && target . env == "msvc" { for (k , v) in cl_exe . env . iter () { tool . env . push ((k . to_owned () , v . to_owned ())) ; } } } if target . env == "msvc" && tool . family == ToolFamily :: Gnu { self . cargo_output . print_warning (& "GNU compiler is not supported for this target") ; } Ok (tool) } # [doc = " Returns a fallback `cc_compiler_wrapper` by introspecting `RUSTC_WRAPPER`"] fn rustc_wrapper_fallback (& self) -> Option < Arc < OsStr > > { const VALID_WRAPPERS : & [& str] = & ["sccache" , "cachepot" , "buildcache"] ; let rustc_wrapper = self . getenv ("RUSTC_WRAPPER") ? ; let wrapper_path = Path :: new (& rustc_wrapper) ; let wrapper_stem = wrapper_path . file_stem () ? ; if VALID_WRAPPERS . contains (& wrapper_stem . to_str () ?) { Some (rustc_wrapper) } else { None } } # [doc = " Returns compiler path, optional modifier name from whitelist, and arguments vec"] fn env_tool (& self , name : & str) -> Option < (PathBuf , Option < Arc < OsStr > > , Vec < String >) > { let tool = self . getenv_with_target_prefixes (name) . ok () ? ; let tool = tool . to_string_lossy () ; let tool = tool . trim () ; if tool . is_empty () { return None ; } if Path :: new (tool) . exists () { return Some ((PathBuf :: from (tool) , self . rustc_wrapper_fallback () , Vec :: new () ,)) ; } let mut known_wrappers = vec ! ["ccache" , "distcc" , "sccache" , "icecc" , "cachepot" , "buildcache" ,] ; let custom_wrapper = self . getenv ("CC_KNOWN_WRAPPER_CUSTOM") ; if custom_wrapper . is_some () { known_wrappers . push (custom_wrapper . as_deref () . unwrap () . to_str () . unwrap ()) ; } let mut parts = tool . split_whitespace () ; let maybe_wrapper = parts . next () ? ; let file_stem = Path :: new (maybe_wrapper) . file_stem () ? . to_str () ? ; if known_wrappers . contains (& file_stem) { if let Some (compiler) = parts . next () { return Some ((compiler . into () , Some (Arc :: < OsStr > :: from (OsStr :: new (& maybe_wrapper))) , parts . map (| s | s . to_string ()) . collect () ,)) ; } } Some ((maybe_wrapper . into () , self . rustc_wrapper_fallback () , parts . map (| s | s . to_string ()) . collect () ,)) } # [doc = " Returns the C++ standard library:"] # [doc = " 1. If [`cpp_link_stdlib`](cc::Build::cpp_link_stdlib) is set, uses its value."] # [doc = " 2. Else if the `CXXSTDLIB` environment variable is set, uses its value."] # [doc = " 3. Else the default is `c++` for OS X and BSDs, `c++_shared` for Android,"] # [doc = "    `None` for MSVC and `stdc++` for anything else."] fn get_cpp_link_stdlib (& self) -> Result < Option < Cow < '_ , Path > > , Error > { match & self . cpp_link_stdlib { Some (s) => Ok (s . as_deref () . map (Path :: new) . map (Cow :: Borrowed)) , None => { if let Ok (stdlib) = self . getenv_with_target_prefixes ("CXXSTDLIB") { if stdlib . is_empty () { Ok (None) } else { Ok (Some (Cow :: Owned (Path :: new (& stdlib) . to_owned ()))) } } else { let target = self . get_target () ? ; if target . env == "msvc" { Ok (None) } else if target . vendor == "apple" || target . os == "freebsd" || target . os == "openbsd" || target . os == "aix" || (target . os == "linux" && target . env == "ohos") || target . os == "wasi" { Ok (Some (Cow :: Borrowed (Path :: new ("c++")))) } else if target . os == "android" { Ok (Some (Cow :: Borrowed (Path :: new ("c++_shared")))) } else { Ok (Some (Cow :: Borrowed (Path :: new ("stdc++")))) } } } } } # [doc = " Get the archiver (ar) that's in use for this configuration."] # [doc = ""] # [doc = " You can use [`Command::get_program`] to get just the path to the command."] # [doc = ""] # [doc = " This method will take into account all configuration such as debug"] # [doc = " information, optimization level, include directories, defines, etc."] # [doc = " Additionally, the compiler binary in use follows the standard"] # [doc = " conventions for this path, e.g. looking at the explicitly set compiler,"] # [doc = " environment variables (a number of which are inspected here), and then"] # [doc = " falling back to the default configuration."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if an error occurred while determining the architecture."] pub fn get_archiver (& self) -> Command { match self . try_get_archiver () { Ok (tool) => tool , Err (e) => fail (& e . message) , } } # [doc = " Get the archiver that's in use for this configuration."] # [doc = ""] # [doc = " This will return a result instead of panicking;"] # [doc = " see [`Self::get_archiver`] for the complete description."] pub fn try_get_archiver (& self) -> Result < Command , Error > { Ok (self . try_get_archiver_and_flags () ? . 0) } fn try_get_archiver_and_flags (& self) -> Result < (Command , PathBuf , bool) , Error > { let (mut cmd , name) = self . get_base_archiver () ? ; let mut any_flags = false ; if let Some (flags) = self . envflags ("ARFLAGS") ? { any_flags = true ; cmd . args (flags) ; } for flag in & self . ar_flags { any_flags = true ; cmd . arg (& * * flag) ; } Ok ((cmd , name , any_flags)) } fn get_base_archiver (& self) -> Result < (Command , PathBuf) , Error > { if let Some (ref a) = self . archiver { let archiver = & * * a ; return Ok ((self . cmd (archiver) , archiver . into ())) ; } self . get_base_archiver_variant ("AR" , "ar") } # [doc = " Get the ranlib that's in use for this configuration."] # [doc = ""] # [doc = " You can use [`Command::get_program`] to get just the path to the command."] # [doc = ""] # [doc = " This method will take into account all configuration such as debug"] # [doc = " information, optimization level, include directories, defines, etc."] # [doc = " Additionally, the compiler binary in use follows the standard"] # [doc = " conventions for this path, e.g. looking at the explicitly set compiler,"] # [doc = " environment variables (a number of which are inspected here), and then"] # [doc = " falling back to the default configuration."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if an error occurred while determining the architecture."] pub fn get_ranlib (& self) -> Command { match self . try_get_ranlib () { Ok (tool) => tool , Err (e) => fail (& e . message) , } } # [doc = " Get the ranlib that's in use for this configuration."] # [doc = ""] # [doc = " This will return a result instead of panicking;"] # [doc = " see [`Self::get_ranlib`] for the complete description."] pub fn try_get_ranlib (& self) -> Result < Command , Error > { let mut cmd = self . get_base_ranlib () ? ; if let Some (flags) = self . envflags ("RANLIBFLAGS") ? { cmd . args (flags) ; } Ok (cmd) } fn get_base_ranlib (& self) -> Result < Command , Error > { if let Some (ref r) = self . ranlib { return Ok (self . cmd (& * * r)) ; } Ok (self . get_base_archiver_variant ("RANLIB" , "ranlib") ? . 0) } fn get_base_archiver_variant (& self , env : & str , tool : & str ,) -> Result < (Command , PathBuf) , Error > { let target = self . get_target () ? ; let mut name = PathBuf :: new () ; let tool_opt : Option < Command > = self . env_tool (env) . map (| (tool , _wrapper , args) | { name . clone_from (& tool) ; let mut cmd = self . cmd (tool) ; cmd . args (args) ; cmd }) . or_else (| | { if target . os == "emscripten" { if cfg ! (windows) { let mut cmd = self . cmd ("cmd") ; name = format ! ("em{tool}.bat") . into () ; cmd . arg ("/c") . arg (& name) ; Some (cmd) } else { name = format ! ("em{tool}") . into () ; Some (self . cmd (& name)) } } else if target . arch == "wasm32" || target . arch == "wasm64" { let compiler = self . get_base_compiler () . ok () ? ; if compiler . is_like_clang () { name = format ! ("llvm-{tool}") . into () ; self . search_programs (& compiler . path , & name , & self . cargo_output) . map (| name | self . cmd (name)) } else { None } } else { None } }) ; let tool = match tool_opt { Some (t) => t , None => { if target . os == "android" { name = format ! ("llvm-{tool}") . into () ; match Command :: new (& name) . arg ("--version") . status () { Ok (status) if status . success () => () , _ => { let raw_target = self . get_raw_target () ? ; name = format ! ("{}-{}" , raw_target . replace ("armv7" , "arm") , tool) . into () } } self . cmd (& name) } else if target . env == "msvc" { let compiler = self . get_base_compiler () ? ; let lib = if compiler . family == (ToolFamily :: Msvc { clang_cl : true }) { self . search_programs (& compiler . path , Path :: new ("llvm-lib") , & self . cargo_output ,) . or_else (| | { if let Some (mut cmd) = self . which (& compiler . path , None) { cmd . pop () ; cmd . push ("llvm-lib") ; self . which (& cmd , None) } else { None } }) } else { None } ; if let Some (lib) = lib { name = lib ; self . cmd (& name) } else { name = PathBuf :: from ("lib.exe") ; let mut cmd = match self . find_msvc_tools_find (& target , "lib.exe") { Some (t) => t , None => self . cmd ("lib.exe") , } ; if target . full_arch == "arm64ec" { cmd . arg ("/machine:arm64ec") ; } cmd } } else if target . os == "illumos" { name = format ! ("g{tool}") . into () ; self . cmd (& name) } else if target . os == "vxworks" { name = format ! ("wr-{tool}") . into () ; self . cmd (& name) } else if target . os == "nto" { name = match target . full_arch { "i586" => format ! ("ntox86-{tool}") . into () , "x86" | "aarch64" | "x86_64" => { format ! ("nto{}-{}" , target . arch , tool) . into () } _ => { return Err (Error :: new (ErrorKind :: InvalidTarget , format ! ("Unknown architecture for Neutrino QNX: {}" , target . arch) ,)) } } ; self . cmd (& name) } else if self . get_is_cross_compile () ? { match self . prefix_for_target (& self . get_raw_target () ?) { Some (prefix) => { let chosen = ["" , "-gcc"] . iter () . filter_map (| infix | { let target_p = format ! ("{prefix}{infix}-{tool}") ; let status = Command :: new (& target_p) . arg ("--version") . stdin (Stdio :: null ()) . stdout (Stdio :: null ()) . stderr (Stdio :: null ()) . status () . ok () ? ; status . success () . then_some (target_p) }) . next () . unwrap_or_else (| | tool . to_string ()) ; name = chosen . into () ; self . cmd (& name) } None => { name = tool . into () ; self . cmd (& name) } } } else { name = tool . into () ; self . cmd (& name) } } } ; Ok ((tool , name)) } fn prefix_for_target (& self , target : & str) -> Option < Cow < 'static , str > > { self . getenv ("CROSS_COMPILE") . as_deref () . map (| s | s . to_string_lossy () . trim_end_matches ('-') . to_owned ()) . map (Cow :: Owned) . or_else (| | { self . getenv ("RUSTC_LINKER") . and_then (| var | { var . to_string_lossy () . strip_suffix ("-gcc") . map (str :: to_string) . map (Cow :: Owned) }) }) . or_else (| | { match target { "aarch64-pc-windows-gnullvm" => Some ("aarch64-w64-mingw32") , "aarch64-uwp-windows-gnu" => Some ("aarch64-w64-mingw32") , "aarch64-unknown-linux-gnu" => Some ("aarch64-linux-gnu") , "aarch64-unknown-linux-musl" => Some ("aarch64-linux-musl") , "aarch64-unknown-netbsd" => Some ("aarch64--netbsd") , "arm-unknown-linux-gnueabi" => Some ("arm-linux-gnueabi") , "armv4t-unknown-linux-gnueabi" => Some ("arm-linux-gnueabi") , "armv5te-unknown-linux-gnueabi" => Some ("arm-linux-gnueabi") , "armv5te-unknown-linux-musleabi" => Some ("arm-linux-gnueabi") , "arm-unknown-linux-gnueabihf" => Some ("arm-linux-gnueabihf") , "arm-unknown-linux-musleabi" => Some ("arm-linux-musleabi") , "arm-unknown-linux-musleabihf" => Some ("arm-linux-musleabihf") , "arm-unknown-netbsd-eabi" => Some ("arm--netbsdelf-eabi") , "armv6-unknown-netbsd-eabihf" => Some ("armv6--netbsdelf-eabihf") , "armv7-unknown-linux-gnueabi" => Some ("arm-linux-gnueabi") , "armv7-unknown-linux-gnueabihf" => Some ("arm-linux-gnueabihf") , "armv7-unknown-linux-musleabihf" => Some ("arm-linux-musleabihf") , "armv7neon-unknown-linux-gnueabihf" => Some ("arm-linux-gnueabihf") , "armv7neon-unknown-linux-musleabihf" => Some ("arm-linux-musleabihf") , "thumbv7-unknown-linux-gnueabihf" => Some ("arm-linux-gnueabihf") , "thumbv7-unknown-linux-musleabihf" => Some ("arm-linux-musleabihf") , "thumbv7neon-unknown-linux-gnueabihf" => Some ("arm-linux-gnueabihf") , "thumbv7neon-unknown-linux-musleabihf" => Some ("arm-linux-musleabihf") , "armv7-unknown-netbsd-eabihf" => Some ("armv7--netbsdelf-eabihf") , "hexagon-unknown-linux-musl" => Some ("hexagon-linux-musl") , "i586-unknown-linux-musl" => Some ("musl") , "i686-pc-windows-gnu" => Some ("i686-w64-mingw32") , "i686-pc-windows-gnullvm" => Some ("i686-w64-mingw32") , "i686-uwp-windows-gnu" => Some ("i686-w64-mingw32") , "i686-unknown-linux-gnu" => self . find_working_gnu_prefix (& ["i686-linux-gnu" , "x86_64-linux-gnu" ,]) , "i686-unknown-linux-musl" => Some ("musl") , "i686-unknown-netbsd" => Some ("i486--netbsdelf") , "loongarch64-unknown-linux-gnu" => Some ("loongarch64-linux-gnu") , "m68k-unknown-linux-gnu" => Some ("m68k-linux-gnu") , "mips-unknown-linux-gnu" => Some ("mips-linux-gnu") , "mips-unknown-linux-musl" => Some ("mips-linux-musl") , "mipsel-unknown-linux-gnu" => Some ("mipsel-linux-gnu") , "mipsel-unknown-linux-musl" => Some ("mipsel-linux-musl") , "mips64-unknown-linux-gnuabi64" => Some ("mips64-linux-gnuabi64") , "mips64el-unknown-linux-gnuabi64" => Some ("mips64el-linux-gnuabi64") , "mipsisa32r6-unknown-linux-gnu" => Some ("mipsisa32r6-linux-gnu") , "mipsisa32r6el-unknown-linux-gnu" => Some ("mipsisa32r6el-linux-gnu") , "mipsisa64r6-unknown-linux-gnuabi64" => Some ("mipsisa64r6-linux-gnuabi64") , "mipsisa64r6el-unknown-linux-gnuabi64" => Some ("mipsisa64r6el-linux-gnuabi64") , "powerpc-unknown-linux-gnu" => Some ("powerpc-linux-gnu") , "powerpc-unknown-linux-gnuspe" => Some ("powerpc-linux-gnuspe") , "powerpc-unknown-netbsd" => Some ("powerpc--netbsd") , "powerpc64-unknown-linux-gnu" => Some ("powerpc64-linux-gnu") , "powerpc64le-unknown-linux-gnu" => Some ("powerpc64le-linux-gnu") , "riscv32i-unknown-none-elf" => self . find_working_gnu_prefix (& ["riscv32-unknown-elf" , "riscv64-unknown-elf" , "riscv-none-embed" ,]) , "riscv32imac-esp-espidf" => Some ("riscv32-esp-elf") , "riscv32imac-unknown-none-elf" => self . find_working_gnu_prefix (& ["riscv32-unknown-elf" , "riscv64-unknown-elf" , "riscv-none-embed" ,]) , "riscv32imac-unknown-xous-elf" => self . find_working_gnu_prefix (& ["riscv32-unknown-elf" , "riscv64-unknown-elf" , "riscv-none-embed" ,]) , "riscv32imc-esp-espidf" => Some ("riscv32-esp-elf") , "riscv32imc-unknown-none-elf" => self . find_working_gnu_prefix (& ["riscv32-unknown-elf" , "riscv64-unknown-elf" , "riscv-none-embed" ,]) , "riscv64gc-unknown-none-elf" => self . find_working_gnu_prefix (& ["riscv64-unknown-elf" , "riscv32-unknown-elf" , "riscv-none-embed" ,]) , "riscv64imac-unknown-none-elf" => self . find_working_gnu_prefix (& ["riscv64-unknown-elf" , "riscv32-unknown-elf" , "riscv-none-embed" ,]) , "riscv64gc-unknown-linux-gnu" => Some ("riscv64-linux-gnu") , "riscv32gc-unknown-linux-gnu" => Some ("riscv32-linux-gnu") , "riscv64gc-unknown-linux-musl" => Some ("riscv64-linux-musl") , "riscv32gc-unknown-linux-musl" => Some ("riscv32-linux-musl") , "riscv64gc-unknown-netbsd" => Some ("riscv64--netbsd") , "s390x-unknown-linux-gnu" => Some ("s390x-linux-gnu") , "sparc-unknown-linux-gnu" => Some ("sparc-linux-gnu") , "sparc64-unknown-linux-gnu" => Some ("sparc64-linux-gnu") , "sparc64-unknown-netbsd" => Some ("sparc64--netbsd") , "sparcv9-sun-solaris" => Some ("sparcv9-sun-solaris") , "armv7a-none-eabi" => Some ("arm-none-eabi") , "armv7a-none-eabihf" => Some ("arm-none-eabi") , "armebv7r-none-eabi" => Some ("arm-none-eabi") , "armebv7r-none-eabihf" => Some ("arm-none-eabi") , "armv7r-none-eabi" => Some ("arm-none-eabi") , "armv7r-none-eabihf" => Some ("arm-none-eabi") , "armv8r-none-eabihf" => Some ("arm-none-eabi") , "thumbv6m-none-eabi" => Some ("arm-none-eabi") , "thumbv7em-none-eabi" => Some ("arm-none-eabi") , "thumbv7em-none-eabihf" => Some ("arm-none-eabi") , "thumbv7m-none-eabi" => Some ("arm-none-eabi") , "thumbv8m.base-none-eabi" => Some ("arm-none-eabi") , "thumbv8m.main-none-eabi" => Some ("arm-none-eabi") , "thumbv8m.main-none-eabihf" => Some ("arm-none-eabi") , "x86_64-pc-windows-gnu" => Some ("x86_64-w64-mingw32") , "x86_64-pc-windows-gnullvm" => Some ("x86_64-w64-mingw32") , "x86_64-uwp-windows-gnu" => Some ("x86_64-w64-mingw32") , "x86_64-rumprun-netbsd" => Some ("x86_64-rumprun-netbsd") , "x86_64-unknown-linux-gnu" => self . find_working_gnu_prefix (& ["x86_64-linux-gnu" ,]) , "x86_64-unknown-linux-musl" => { self . find_working_gnu_prefix (& ["x86_64-linux-musl" , "musl"]) } "x86_64-unknown-netbsd" => Some ("x86_64--netbsd") , "xtensa-esp32-espidf" | "xtensa-esp32-none-elf" | "xtensa-esp32s2-espidf" | "xtensa-esp32s2-none-elf" | "xtensa-esp32s3-espidf" | "xtensa-esp32s3-none-elf" => Some ("xtensa-esp-elf") , _ => None , } . map (Cow :: Borrowed) }) } # [doc = " Some platforms have multiple, compatible, canonical prefixes. Look through"] # [doc = " each possible prefix for a compiler that exists and return it. The prefixes"] # [doc = " should be ordered from most-likely to least-likely."] fn find_working_gnu_prefix (& self , prefixes : & [& 'static str]) -> Option < & 'static str > { let suffix = if self . cpp { "-g++" } else { "-gcc" } ; let extension = std :: env :: consts :: EXE_SUFFIX ; self . getenv ("PATH") . as_ref () . and_then (| path_entries | { env :: split_paths (path_entries) . find_map (| path_entry | { for prefix in prefixes { let target_compiler = format ! ("{prefix}{suffix}{extension}") ; if path_entry . join (& target_compiler) . exists () { return Some (prefix) ; } } None }) }) . copied () . or_else (| | prefixes . first () . copied ()) } fn get_target (& self) -> Result < TargetInfo < '_ > , Error > { match & self . target { Some (t) if Some (& * * t) != self . getenv_unwrap_str ("TARGET") . ok () . as_deref () => { TargetInfo :: from_rustc_target (t) } _ => self . build_cache . target_info_parser . parse_from_cargo_environment_variables () , } } fn get_raw_target (& self) -> Result < Cow < '_ , str > , Error > { match & self . target { Some (t) => Ok (Cow :: Borrowed (t)) , None => self . getenv_unwrap_str ("TARGET") . map (Cow :: Owned) , } } fn get_is_cross_compile (& self) -> Result < bool , Error > { let target = self . get_raw_target () ? ; let host : Cow < '_ , str > = match & self . host { Some (h) => Cow :: Borrowed (h) , None => Cow :: Owned (self . getenv_unwrap_str ("HOST") ?) , } ; Ok (host != target) } fn get_opt_level (& self) -> Result < Cow < '_ , str > , Error > { match & self . opt_level { Some (ol) => Ok (Cow :: Borrowed (ol)) , None => self . getenv_unwrap_str ("OPT_LEVEL") . map (Cow :: Owned) , } } fn get_debug (& self) -> bool { self . debug . unwrap_or_else (| | self . getenv_boolean ("DEBUG")) } fn get_shell_escaped_flags (& self) -> bool { self . shell_escaped_flags . unwrap_or_else (| | self . getenv_boolean ("CC_SHELL_ESCAPED_FLAGS")) } fn get_dwarf_version (& self) -> Option < u32 > { let target = self . get_target () . ok () ? ; if matches ! (target . os , "android" | "dragonfly" | "freebsd" | "netbsd" | "openbsd") || target . vendor == "apple" || (target . os == "windows" && target . env == "gnu") { Some (2) } else if target . os == "linux" { Some (4) } else { None } } fn get_force_frame_pointer (& self) -> bool { self . force_frame_pointer . unwrap_or_else (| | self . get_debug ()) } fn get_out_dir (& self) -> Result < Cow < '_ , Path > , Error > { match & self . out_dir { Some (p) => Ok (Cow :: Borrowed (& * * p)) , None => self . getenv ("OUT_DIR") . as_deref () . map (PathBuf :: from) . map (Cow :: Owned) . ok_or_else (| | { Error :: new (ErrorKind :: EnvVarNotFound , "Environment variable OUT_DIR not defined." ,) }) , } } # [allow (clippy :: disallowed_methods)] fn getenv (& self , v : & str) -> Option < Arc < OsStr > > { fn provided_by_cargo (envvar : & str) -> bool { match envvar { v if v . starts_with ("CARGO") || v . starts_with ("RUSTC") => true , "HOST" | "TARGET" | "RUSTDOC" | "OUT_DIR" | "OPT_LEVEL" | "DEBUG" | "PROFILE" | "NUM_JOBS" | "RUSTFLAGS" => true , _ => false , } } if let Some (val) = self . build_cache . env_cache . read () . unwrap () . get (v) . cloned () { return val ; } if self . emit_rerun_if_env_changed && ! provided_by_cargo (v) && v != "PATH" { self . cargo_output . print_metadata (& format_args ! ("cargo:rerun-if-env-changed={v}")) ; } let r = self . env . iter () . find (| (k , _) | k . as_ref () == v) . map (| (_ , value) | value . clone ()) . or_else (| | env :: var_os (v) . map (Arc :: from)) ; self . cargo_output . print_metadata (& format_args ! ("{} = {}" , v , OptionOsStrDisplay (r . as_deref ()))) ; self . build_cache . env_cache . write () . unwrap () . insert (v . into () , r . clone ()) ; r } # [doc = " get boolean flag that is either true or false"] fn getenv_boolean (& self , v : & str) -> bool { match self . getenv (v) { Some (s) => & * s != "0" && & * s != "false" && ! s . is_empty () , None => false , } } fn getenv_unwrap (& self , v : & str) -> Result < Arc < OsStr > , Error > { match self . getenv (v) { Some (s) => Ok (s) , None => Err (Error :: new (ErrorKind :: EnvVarNotFound , format ! ("Environment variable {v} not defined.") ,)) , } } fn getenv_unwrap_str (& self , v : & str) -> Result < String , Error > { let env = self . getenv_unwrap (v) ? ; env . to_str () . map (String :: from) . ok_or_else (| | { Error :: new (ErrorKind :: EnvVarNotFound , format ! ("Environment variable {v} is not valid utf-8.") ,) }) } # [doc = " The list of environment variables to check for a given env, in order of priority."] fn target_envs (& self , env : & str) -> Result < [String ; 4] , Error > { let target = self . get_raw_target () ? ; let kind = if self . get_is_cross_compile () ? { "TARGET" } else { "HOST" } ; let target_u = target . replace (['-' , '.'] , "_") ; Ok ([format ! ("{env}_{target}") , format ! ("{env}_{target_u}") , format ! ("{kind}_{env}") , env . to_string () ,]) } # [doc = " Get a single-valued environment variable with target variants."] fn getenv_with_target_prefixes (& self , env : & str) -> Result < Arc < OsStr > , Error > { let res = self . target_envs (env) ? . iter () . filter_map (| env | self . getenv (env)) . next () ; match res { Some (res) => Ok (res) , None => Err (Error :: new (ErrorKind :: EnvVarNotFound , format ! ("could not find environment variable {env}") ,)) , } } # [doc = " Get values from CFLAGS-style environment variable."] fn envflags (& self , env : & str) -> Result < Option < Vec < String > > , Error > { let mut any_set = false ; let mut res = vec ! [] ; for env in self . target_envs (env) ? . iter () . rev () { if let Some (var) = self . getenv (env) { any_set = true ; let var = var . to_string_lossy () ; if self . get_shell_escaped_flags () { res . extend (Shlex :: new (& var)) ; } else { res . extend (var . split_ascii_whitespace () . map (ToString :: to_string)) ; } } } Ok (if any_set { Some (res) } else { None }) } fn fix_env_for_apple_os (& self , cmd : & mut Command) -> Result < () , Error > { let target = self . get_target () ? ; if cfg ! (target_os = "macos") && target . os == "macos" { cmd . env_remove ("IPHONEOS_DEPLOYMENT_TARGET") ; } Ok (()) } fn apple_sdk_root_inner (& self , sdk : & str) -> Result < Arc < OsStr > , Error > { if let Some (sdkroot) = self . getenv ("SDKROOT") { let p = Path :: new (& sdkroot) ; let does_sdkroot_contain = | strings : & [& str] | { let sdkroot_str = p . to_string_lossy () ; strings . iter () . any (| s | sdkroot_str . contains (s)) } ; match sdk { "appletvos" if does_sdkroot_contain (& ["TVSimulator.platform" , "MacOSX.platform"]) => { } "appletvsimulator" if does_sdkroot_contain (& ["TVOS.platform" , "MacOSX.platform"]) => { } "iphoneos" if does_sdkroot_contain (& ["iPhoneSimulator.platform" , "MacOSX.platform"]) => { } "iphonesimulator" if does_sdkroot_contain (& ["iPhoneOS.platform" , "MacOSX.platform"]) => { } "macosx10.15" if does_sdkroot_contain (& ["iPhoneOS.platform" , "iPhoneSimulator.platform"]) => { } "watchos" if does_sdkroot_contain (& ["WatchSimulator.platform" , "MacOSX.platform"]) => { } "watchsimulator" if does_sdkroot_contain (& ["WatchOS.platform" , "MacOSX.platform"]) => { } "xros" if does_sdkroot_contain (& ["XRSimulator.platform" , "MacOSX.platform"]) => { } "xrsimulator" if does_sdkroot_contain (& ["XROS.platform" , "MacOSX.platform"]) => { } _ if ! p . is_absolute () || p == Path :: new ("/") || ! p . exists () => { } _ => return Ok (sdkroot) , } } let sdk_path = run_output (self . cmd ("xcrun") . arg ("--show-sdk-path") . arg ("--sdk") . arg (sdk) , & self . cargo_output ,) ? ; let sdk_path = match String :: from_utf8 (sdk_path) { Ok (p) => p , Err (_) => { return Err (Error :: new (ErrorKind :: IOError , "Unable to determine Apple SDK path." ,)) ; } } ; Ok (Arc :: from (OsStr :: new (sdk_path . trim ()))) } fn apple_sdk_root (& self , target : & TargetInfo < '_ >) -> Result < Arc < OsStr > , Error > { let sdk = target . apple_sdk_name () ; if let Some (ret) = self . build_cache . apple_sdk_root_cache . read () . expect ("apple_sdk_root_cache lock failed") . get (sdk) . cloned () { return Ok (ret) ; } let sdk_path = self . apple_sdk_root_inner (sdk) ? ; self . build_cache . apple_sdk_root_cache . write () . expect ("apple_sdk_root_cache lock failed") . insert (sdk . into () , sdk_path . clone ()) ; Ok (sdk_path) } fn apple_deployment_target (& self , target : & TargetInfo < '_ >) -> Arc < str > { let sdk = target . apple_sdk_name () ; if let Some (ret) = self . build_cache . apple_versions_cache . read () . expect ("apple_versions_cache lock failed") . get (sdk) . cloned () { return ret ; } let default_deployment_from_sdk = | | -> Option < Arc < str > > { let version = run_output (self . cmd ("xcrun") . arg ("--show-sdk-version") . arg ("--sdk") . arg (sdk) , & self . cargo_output ,) . ok () ? ; Some (Arc :: from (std :: str :: from_utf8 (& version) . ok () ? . trim ())) } ; let deployment_from_env = | name : & str | -> Option < Arc < str > > { self . env . iter () . find (| (k , _) | & * * k == OsStr :: new (name)) . map (| (_ , v) | v) . cloned () . or_else (| | self . getenv (name)) ? . to_str () . map (Arc :: from) } ; let maybe_cpp_version_baseline = | deployment_target_ver : Arc < str > | -> Option < Arc < str > > { if ! self . cpp { return Some (deployment_target_ver) ; } let mut deployment_target = deployment_target_ver . split ('.') . map (| v | v . parse :: < u32 > () . expect ("integer version")) ; match target . os { "macos" => { let major = deployment_target . next () . unwrap_or (0) ; let minor = deployment_target . next () . unwrap_or (0) ; if major == 10 && minor < 9 { self . cargo_output . print_warning (& format_args ! ("macOS deployment target ({deployment_target_ver}) too low, it will be increased")) ; return None ; } } "ios" => { let major = deployment_target . next () . unwrap_or (0) ; if major < 7 { self . cargo_output . print_warning (& format_args ! ("iOS deployment target ({deployment_target_ver}) too low, it will be increased")) ; return None ; } } _ => { } } Some (deployment_target_ver) } ; let version : Arc < str > = match target . os { "macos" => deployment_from_env ("MACOSX_DEPLOYMENT_TARGET") . and_then (maybe_cpp_version_baseline) . or_else (default_deployment_from_sdk) . unwrap_or_else (| | { if target . arch == "aarch64" { "11.0" . into () } else { let default : Arc < str > = Arc :: from ("10.7") ; maybe_cpp_version_baseline (default . clone ()) . unwrap_or (default) } }) , "ios" => deployment_from_env ("IPHONEOS_DEPLOYMENT_TARGET") . and_then (maybe_cpp_version_baseline) . or_else (default_deployment_from_sdk) . unwrap_or_else (| | "7.0" . into ()) , "watchos" => deployment_from_env ("WATCHOS_DEPLOYMENT_TARGET") . or_else (default_deployment_from_sdk) . unwrap_or_else (| | "5.0" . into ()) , "tvos" => deployment_from_env ("TVOS_DEPLOYMENT_TARGET") . or_else (default_deployment_from_sdk) . unwrap_or_else (| | "9.0" . into ()) , "visionos" => deployment_from_env ("XROS_DEPLOYMENT_TARGET") . or_else (default_deployment_from_sdk) . unwrap_or_else (| | "1.0" . into ()) , os => unreachable ! ("unknown Apple OS: {}" , os) , } ; self . build_cache . apple_versions_cache . write () . expect ("apple_versions_cache lock failed") . insert (sdk . into () , version . clone ()) ; version } fn wasm_musl_sysroot (& self) -> Result < Arc < OsStr > , Error > { if let Some (musl_sysroot_path) = self . getenv ("WASM_MUSL_SYSROOT") { Ok (musl_sysroot_path) } else { Err (Error :: new (ErrorKind :: EnvVarNotFound , "Environment variable WASM_MUSL_SYSROOT not defined for wasm32. Download sysroot from GitHub & setup environment variable MUSL_SYSROOT targeting the folder." ,)) } } fn wasi_sysroot (& self) -> Result < Arc < OsStr > , Error > { if let Some (wasi_sysroot_path) = self . getenv ("WASI_SYSROOT") { Ok (wasi_sysroot_path) } else { Err (Error :: new (ErrorKind :: EnvVarNotFound , "Environment variable WASI_SYSROOT not defined. Download sysroot from GitHub & setup environment variable WASI_SYSROOT targeting the folder." ,)) } } fn cuda_file_count (& self) -> usize { self . files . iter () . filter (| file | file . extension () == Some (OsStr :: new ("cu"))) . count () } fn which (& self , tool : & Path , path_entries : Option < & OsStr >) -> Option < PathBuf > { fn check_exe (mut exe : PathBuf) -> Option < PathBuf > { let exe_ext = std :: env :: consts :: EXE_EXTENSION ; let check = exe . exists () || (! exe_ext . is_empty () && exe . set_extension (exe_ext) && exe . exists ()) ; check . then_some (exe) } let find_exe_in_path = | path_entries : & OsStr | -> Option < PathBuf > { env :: split_paths (path_entries) . find_map (| path_entry | check_exe (path_entry . join (tool))) } ; if tool . components () . count () > 1 { check_exe (PathBuf :: from (tool)) } else { path_entries . and_then (find_exe_in_path) . or_else (| | find_exe_in_path (& self . getenv ("PATH") ?)) } } # [doc = " search for |prog| on 'programs' path in '|cc| --print-search-dirs' output"] fn search_programs (& self , cc : & Path , prog : & Path , cargo_output : & CargoOutput ,) -> Option < PathBuf > { let search_dirs = run_output (self . cmd (cc) . arg ("--print-search-dirs") , cargo_output ,) . ok () ? ; let search_dirs = std :: str :: from_utf8 (& search_dirs) . ok () ? ; for dirs in search_dirs . split (['\r' , '\n']) { if let Some (path) = dirs . strip_prefix ("programs: =") { return self . which (prog , Some (OsStr :: new (path))) ; } } None } fn find_msvc_tools_find (& self , target : & TargetInfo < '_ > , tool : & str) -> Option < Command > { self . find_msvc_tools_find_tool (target , tool) . map (| c | c . to_command ()) } fn find_msvc_tools_find_tool (& self , target : & TargetInfo < '_ > , tool : & str) -> Option < Tool > { struct BuildEnvGetter < 's > (& 's Build) ; impl :: find_msvc_tools :: EnvGetter for BuildEnvGetter < '_ > { fn get_env (& self , name : & str) -> Option < :: find_msvc_tools :: Env > { self . 0 . getenv (name) . map (:: find_msvc_tools :: Env :: Arced) } } if target . env != "msvc" { return None ; } :: find_msvc_tools :: find_tool_with_env (target . full_arch , tool , & BuildEnvGetter (self)) . map (Tool :: from_find_msvc_tools) } # [doc = " Compiling for WASI targets typically uses the [wasi-sdk] project and"] # [doc = " installations of wasi-sdk are typically indicated with the"] # [doc = " `WASI_SDK_PATH` environment variable. Check to see if that environment"] # [doc = " variable exists, and check to see if an appropriate compiler is located"] # [doc = " there. If that all passes then use that compiler by default, but"] # [doc = " otherwise fall back to whatever the clang default is since gcc doesn't"] # [doc = " have support for compiling to wasm."] # [doc = ""] # [doc = " [wasi-sdk]: https://github.com/WebAssembly/wasi-sdk"] fn autodetect_wasi_compiler (& self , raw_target : & str , clang : & str) -> PathBuf { if let Some (path) = self . getenv ("WASI_SDK_PATH") { let target_clang = Path :: new (& path) . join ("bin") . join (format ! ("{raw_target}-clang")) ; if let Some (path) = self . which (& target_clang , None) { return path ; } } clang . into () } }
};
}
