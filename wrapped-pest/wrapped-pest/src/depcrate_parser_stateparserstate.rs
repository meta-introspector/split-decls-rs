// Generated macro for ParserState (struct)
macro_rules! Depcrate_parser_stateParserState {
() => {
// Module: crate::parser_state
// Provides: {"ParserState"}
// Dependencies: {}
# [doc = " The complete state of a [`Parser`]."] # [doc = ""] # [doc = " [`Parser`]: trait.Parser.html"] # [derive (Debug)] pub struct ParserState < 'i , R : RuleType > { # [doc = " Current position from which we try to apply some parser function."] # [doc = " Initially is 0."] # [doc = " E.g., we are parsing `create user 'Bobby'` query, we parsed \"create\" via `match_insensitive`"] # [doc = " and switched our `position` from 0 to the length of \"create\"."] # [doc = ""] # [doc = " E.g., see `match_string` -> `self.position.match_string(string)` which updates `self.pos`."] position : Position < 'i > , # [doc = " Queue representing rules partially (`QueueableToken::Start`) and"] # [doc = " totally (`QueueableToken::End`) parsed. When entering rule we put it in the queue in a state"] # [doc = " of `Start` and after all its sublogic (subrules or strings) are parsed, we change it to"] # [doc = " `End` state."] queue : Vec < QueueableToken < 'i , R > > , # [doc = " Status set in case specific lookahead logic is used in grammar."] # [doc = " See `Lookahead` for more information."] lookahead : Lookahead , # [doc = " Rules that we HAVE expected, tried to parse, but failed."] pos_attempts : Vec < R > , # [doc = " Rules that we have NOT expected, tried to parse, but failed."] neg_attempts : Vec < R > , # [doc = " Max position in the query from which we've tried to parse some rule but failed."] attempt_pos : usize , # [doc = " Current atomicity status. For more information see `Atomicity`."] atomicity : Atomicity , # [doc = " Helper structure tracking `Stack` status (used in case grammar contains stack PUSH/POP"] # [doc = " invocations)."] stack : Stack < SpanOrLiteral < 'i > > , # [doc = " Used for setting max parser calls limit."] call_tracker : CallLimitTracker , # [doc = " Together with tracking of `pos_attempts` and `attempt_pos`"] # [doc = " as a pair of (list of rules that we've tried to parse but failed, max parsed position)"] # [doc = " we track those rules (which we've tried to parse at the same max pos) at this helper struct."] # [doc = ""] # [doc = " Note, that we may try to parse several rules on different positions. We want to track only"] # [doc = " those rules, which attempt position is bigger, because we consider that it's nearer to the"] # [doc = " query that user really wanted to pass."] # [doc = ""] # [doc = " E.g. we have a query `create user \"Bobby\"` and two root rules:"] # [doc = " * CreateUser  = { \"create\" ~ \"user\"  ~ Name }"] # [doc = " * CreateTable = { \"create\" ~ \"table\" ~ Name }"] # [doc = " * Name = { SOME_DEFINITION }"] # [doc = ""] # [doc = " While parsing the query we'll update tracker position to the start of \"Bobby\", because we'd"] # [doc = " successfully parse \"create\" + \"user\" (and not \"table\")."] parse_attempts : ParseAttempts < R > , }
};
}
