// Generated macro for impl_40 (impl)
macro_rules! Depcrate_errorimpl_40 {
() => {
// Module: crate::error
// Provides: {"impl_40"}
// Dependencies: {}
impl < R : RuleType > Error < R > { # [doc = " Creates `Error` from `ErrorVariant` and `Position`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use pest::error::{Error, ErrorVariant};"] # [doc = " # use pest::Position;"] # [doc = " # #[allow(non_camel_case_types)]"] # [doc = " # #[allow(dead_code)]"] # [doc = " # #[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]"] # [doc = " # enum Rule {"] # [doc = " #     open_paren,"] # [doc = " #     closed_paren"] # [doc = " # }"] # [doc = " # let input = \"\";"] # [doc = " # let pos = Position::from_start(input);"] # [doc = " let error = Error::new_from_pos("] # [doc = "     ErrorVariant::ParsingError {"] # [doc = "         positives: vec![Rule::open_paren],"] # [doc = "         negatives: vec![Rule::closed_paren],"] # [doc = "     },"] # [doc = "     pos"] # [doc = " );"] # [doc = ""] # [doc = " println!(\"{}\", error);"] # [doc = " ```"] pub fn new_from_pos (variant : ErrorVariant < R > , pos : Position < '_ >) -> Error < R > { let visualize_ws = pos . match_char ('\n') || pos . match_char ('\r') ; let line_of = pos . line_of () ; let line = if visualize_ws { visualize_whitespace (line_of) } else { line_of . replace (& ['\r' , '\n'] [..] , "") } ; Error { variant , location : InputLocation :: Pos (pos . pos ()) , path : None , line , continued_line : None , line_col : LineColLocation :: Pos (pos . line_col ()) , parse_attempts : None , } } # [doc = " Wrapper function to track `parse_attempts` as a result"] # [doc = " of `state` function call in `parser_state.rs`."] pub (crate) fn new_from_pos_with_parsing_attempts (variant : ErrorVariant < R > , pos : Position < '_ > , parse_attempts : ParseAttempts < R > ,) -> Error < R > { let mut error = Self :: new_from_pos (variant , pos) ; error . parse_attempts = Some (parse_attempts) ; error } # [doc = " Creates `Error` from `ErrorVariant` and `Span`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use pest::error::{Error, ErrorVariant};"] # [doc = " # use pest::{Position, Span};"] # [doc = " # #[allow(non_camel_case_types)]"] # [doc = " # #[allow(dead_code)]"] # [doc = " # #[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]"] # [doc = " # enum Rule {"] # [doc = " #     open_paren,"] # [doc = " #     closed_paren"] # [doc = " # }"] # [doc = " # let input = \"\";"] # [doc = " # let start = Position::from_start(input);"] # [doc = " # let end = start.clone();"] # [doc = " # let span = start.span(&end);"] # [doc = " let error = Error::new_from_span("] # [doc = "     ErrorVariant::ParsingError {"] # [doc = "         positives: vec![Rule::open_paren],"] # [doc = "         negatives: vec![Rule::closed_paren],"] # [doc = "     },"] # [doc = "     span"] # [doc = " );"] # [doc = ""] # [doc = " println!(\"{}\", error);"] # [doc = " ```"] pub fn new_from_span (variant : ErrorVariant < R > , span : Span < '_ >) -> Error < R > { let end = span . end_pos () ; let mut end_line_col = end . line_col () ; if end_line_col . 1 == 1 { let mut visual_end = end ; visual_end . skip_back (1) ; let lc = visual_end . line_col () ; end_line_col = (lc . 0 , lc . 1 + 1) ; } ; let mut line_iter = span . lines () ; let sl = line_iter . next () . unwrap_or ("") ; let mut chars = span . as_str () . chars () ; let visualize_ws = matches ! (chars . next () , Some ('\n') | Some ('\r')) || matches ! (chars . last () , Some ('\n') | Some ('\r')) ; let start_line = if visualize_ws { visualize_whitespace (sl) } else { sl . to_owned () . replace (& ['\r' , '\n'] [..] , "") } ; let ll = line_iter . last () ; let continued_line = if visualize_ws { ll . map (str :: to_owned) } else { ll . map (visualize_whitespace) } ; Error { variant , location : InputLocation :: Span ((span . start () , end . pos ())) , path : None , line : start_line , continued_line , line_col : LineColLocation :: Span (span . start_pos () . line_col () , end_line_col) , parse_attempts : None , } } # [doc = " Returns `Error` variant with `path` which is shown when formatted with `Display`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use pest::error::{Error, ErrorVariant};"] # [doc = " # use pest::Position;"] # [doc = " # #[allow(non_camel_case_types)]"] # [doc = " # #[allow(dead_code)]"] # [doc = " # #[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]"] # [doc = " # enum Rule {"] # [doc = " #     open_paren,"] # [doc = " #     closed_paren"] # [doc = " # }"] # [doc = " # let input = \"\";"] # [doc = " # let pos = Position::from_start(input);"] # [doc = " Error::new_from_pos("] # [doc = "     ErrorVariant::ParsingError {"] # [doc = "         positives: vec![Rule::open_paren],"] # [doc = "         negatives: vec![Rule::closed_paren],"] # [doc = "     },"] # [doc = "     pos"] # [doc = " ).with_path(\"file.rs\");"] # [doc = " ```"] pub fn with_path (mut self , path : & str) -> Error < R > { self . path = Some (path . to_owned ()) ; self } # [doc = " Returns the path set using [`Error::with_path()`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use pest::error::{Error, ErrorVariant};"] # [doc = " # use pest::Position;"] # [doc = " # #[allow(non_camel_case_types)]"] # [doc = " # #[allow(dead_code)]"] # [doc = " # #[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]"] # [doc = " # enum Rule {"] # [doc = " #     open_paren,"] # [doc = " #     closed_paren"] # [doc = " # }"] # [doc = " # let input = \"\";"] # [doc = " # let pos = Position::from_start(input);"] # [doc = " # let error = Error::new_from_pos("] # [doc = " #     ErrorVariant::ParsingError {"] # [doc = " #         positives: vec![Rule::open_paren],"] # [doc = " #         negatives: vec![Rule::closed_paren],"] # [doc = " #     },"] # [doc = " #     pos);"] # [doc = " let error = error.with_path(\"file.rs\");"] # [doc = " assert_eq!(Some(\"file.rs\"), error.path());"] # [doc = " ```"] pub fn path (& self) -> Option < & str > { self . path . as_deref () } # [doc = " Returns the line that the error is on."] pub fn line (& self) -> & str { self . line . as_str () } # [doc = " Renames all `Rule`s if this is a [`ParsingError`]. It does nothing when called on a"] # [doc = " [`CustomError`]."] # [doc = ""] # [doc = " Useful in order to rename verbose rules or have detailed per-`Rule` formatting."] # [doc = ""] # [doc = " [`ParsingError`]: enum.ErrorVariant.html#variant.ParsingError"] # [doc = " [`CustomError`]: enum.ErrorVariant.html#variant.CustomError"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use pest::error::{Error, ErrorVariant};"] # [doc = " # use pest::Position;"] # [doc = " # #[allow(non_camel_case_types)]"] # [doc = " # #[allow(dead_code)]"] # [doc = " # #[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]"] # [doc = " # enum Rule {"] # [doc = " #     open_paren,"] # [doc = " #     closed_paren"] # [doc = " # }"] # [doc = " # let input = \"\";"] # [doc = " # let pos = Position::from_start(input);"] # [doc = " Error::new_from_pos("] # [doc = "     ErrorVariant::ParsingError {"] # [doc = "         positives: vec![Rule::open_paren],"] # [doc = "         negatives: vec![Rule::closed_paren],"] # [doc = "     },"] # [doc = "     pos"] # [doc = " ).renamed_rules(|rule| {"] # [doc = "     match *rule {"] # [doc = "         Rule::open_paren => \"(\".to_owned(),"] # [doc = "         Rule::closed_paren => \"closed paren\".to_owned()"] # [doc = "     }"] # [doc = " });"] # [doc = " ```"] pub fn renamed_rules < F > (mut self , f : F) -> Error < R > where F : FnMut (& R) -> String , { let variant = match self . variant { ErrorVariant :: ParsingError { positives , negatives , } => { let message = Error :: parsing_error_message (& positives , & negatives , f) ; ErrorVariant :: CustomError { message } } variant => variant , } ; self . variant = variant ; self } # [doc = " Get detailed information about errored rules sequence."] # [doc = " Returns `Some(results)` only for `ParsingError`."] pub fn parse_attempts (& self) -> Option < ParseAttempts < R > > { self . parse_attempts . clone () } # [doc = " Get error message based on parsing attempts."] # [doc = " Returns `None` in case self `parse_attempts` is `None`."] pub fn parse_attempts_error (& self , input : & str , rule_to_message : & RuleToMessageFn < R > , is_whitespace : & IsWhitespaceFn ,) -> Option < Error < R > > { let attempts = if let Some (ref parse_attempts) = self . parse_attempts { parse_attempts . clone () } else { return None ; } ; let spacing = self . spacing () + "   " ; let error_position = attempts . max_position ; let message = { let mut help_lines : Vec < String > = Vec :: new () ; help_lines . push (String :: from ("error: parsing error occurred.")) ; for tokens_helper_message in attempts . tokens_helper_messages (is_whitespace , & spacing) { help_lines . push (tokens_helper_message) ; } let call_stacks = attempts . call_stacks () ; let mut call_stacks_parents_groups : BTreeMap < Option < R > , Vec < RulesCallStack < R > > > = BTreeMap :: new () ; for call_stack in call_stacks { call_stacks_parents_groups . entry (call_stack . parent) . or_default () . push (call_stack) ; } for (group_parent , group) in call_stacks_parents_groups { if let Some (parent_rule) = group_parent { let mut contains_meaningful_info = false ; help_lines . push (format ! ("{spacing}help: {}" , if let Some (message) = rule_to_message (& parent_rule) { contains_meaningful_info = true ; message } else { String :: from ("[Unknown parent rule]") })) ; for call_stack in group { if let Some (r) = call_stack . deepest . get_rule () { if let Some (message) = rule_to_message (r) { contains_meaningful_info = true ; help_lines . push (format ! ("{spacing}      - {message}")) ; } } } if ! contains_meaningful_info { help_lines . pop () ; } } else { for call_stack in group { if let Some (r) = call_stack . deepest . get_rule () { let helper_message = rule_to_message (r) ; if let Some (helper_message) = helper_message { help_lines . push (format ! ("{spacing}help: {helper_message}")) ; } } } } } help_lines . join ("\n") } ; let error = Error :: new_from_pos (ErrorVariant :: CustomError { message } , Position :: new_internal (input , error_position) ,) ; Some (error) } fn start (& self) -> (usize , usize) { match self . line_col { LineColLocation :: Pos (line_col) => line_col , LineColLocation :: Span (start_line_col , _) => start_line_col , } } fn spacing (& self) -> String { let line = match self . line_col { LineColLocation :: Pos ((line , _)) => line , LineColLocation :: Span ((start_line , _) , (end_line , _)) => cmp :: max (start_line , end_line) , } ; let line_str_len = format ! ("{}" , line) . len () ; let mut spacing = String :: new () ; for _ in 0 .. line_str_len { spacing . push (' ') ; } spacing } fn underline (& self) -> String { let mut underline = String :: new () ; let mut start = self . start () . 1 ; let end = match self . line_col { LineColLocation :: Span (_ , (_ , mut end)) => { let inverted_cols = start > end ; if inverted_cols { mem :: swap (& mut start , & mut end) ; start -= 1 ; end += 1 ; } Some (end) } _ => None , } ; let offset = start - 1 ; let line_chars = self . line . chars () ; for c in line_chars . take (offset) { match c { '\t' => underline . push ('\t') , _ => underline . push (' ') , } } if let Some (end) = end { underline . push ('^') ; if end - start > 1 { for _ in 2 .. (end - start) { underline . push ('-') ; } underline . push ('^') ; } } else { underline . push_str ("^---") } underline } fn message (& self) -> String { self . variant . message () . to_string () } fn parsing_error_message < F > (positives : & [R] , negatives : & [R] , mut f : F) -> String where F : FnMut (& R) -> String , { match (negatives . is_empty () , positives . is_empty ()) { (false , false) => format ! ("unexpected {}; expected {}" , Error :: enumerate (negatives , & mut f) , Error :: enumerate (positives , & mut f)) , (false , true) => format ! ("unexpected {}" , Error :: enumerate (negatives , & mut f)) , (true , false) => format ! ("expected {}" , Error :: enumerate (positives , & mut f)) , (true , true) => "unknown parsing error" . to_owned () , } } fn enumerate < F > (rules : & [R] , f : & mut F) -> String where F : FnMut (& R) -> String , { match rules . len () { 1 => f (& rules [0]) , 2 => format ! ("{} or {}" , f (& rules [0]) , f (& rules [1])) , l => { let non_separated = f (& rules [l - 1]) ; let separated = rules . iter () . take (l - 1) . map (f) . collect :: < Vec < _ > > () . join (", ") ; format ! ("{}, or {}" , separated , non_separated) } } } pub (crate) fn format (& self) -> String { let spacing = self . spacing () ; let path = self . path . as_ref () . map (| path | format ! ("{}:" , path)) . unwrap_or_default () ; let pair = (self . line_col . clone () , & self . continued_line) ; if let (LineColLocation :: Span (_ , end) , Some (ref continued_line)) = pair { let has_line_gap = end . 0 - self . start () . 0 > 1 ; if has_line_gap { format ! ("{s    }--> {p}{ls}:{c}\n\
                     {s    } |\n\
                     {ls:w$} | {line}\n\
                     {s    } | ...\n\
                     {le:w$} | {continued_line}\n\
                     {s    } | {underline}\n\
                     {s    } |\n\
                     {s    } = {message}" , s = spacing , w = spacing . len () , p = path , ls = self . start () . 0 , le = end . 0 , c = self . start () . 1 , line = self . line , continued_line = continued_line , underline = self . underline () , message = self . message ()) } else { format ! ("{s    }--> {p}{ls}:{c}\n\
                     {s    } |\n\
                     {ls:w$} | {line}\n\
                     {le:w$} | {continued_line}\n\
                     {s    } | {underline}\n\
                     {s    } |\n\
                     {s    } = {message}" , s = spacing , w = spacing . len () , p = path , ls = self . start () . 0 , le = end . 0 , c = self . start () . 1 , line = self . line , continued_line = continued_line , underline = self . underline () , message = self . message ()) } } else { format ! ("{s}--> {p}{l}:{c}\n\
                 {s} |\n\
                 {l} | {line}\n\
                 {s} | {underline}\n\
                 {s} |\n\
                 {s} = {message}" , s = spacing , p = path , l = self . start () . 0 , c = self . start () . 1 , line = self . line , underline = self . underline () , message = self . message ()) } } # [cfg (feature = "miette-error")] # [doc = " Turns an error into a [miette](crates.io/miette) Diagnostic."] pub fn into_miette (self) -> impl :: miette :: Diagnostic { miette_adapter :: MietteAdapter (self) } }
};
}
