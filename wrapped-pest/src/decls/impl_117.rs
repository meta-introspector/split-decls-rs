macro_rules! deps {
    () => {
        Span!();
        Position!();
    };
}

macro_rules! impl_117 {
    () => {
        deps!();
        impl < 'i > Position < 'i > { # [doc = " Create a new `Position` without checking invariants. (Checked with `debug_assertions`.)"] pub (crate) fn new_internal (input : & str , pos : usize) -> Position < '_ > { debug_assert ! (input . get (pos ..) . is_some ()) ; Position { input , pos } } # [doc = " Attempts to create a new `Position` at the given position. If the specified position is"] # [doc = " an invalid index, or the specified position is not a valid UTF8 boundary, then None is"] # [doc = " returned."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # use pest::Position;"] # [doc = " let cheart = '💖';"] # [doc = " let heart = \"💖\";"] # [doc = " assert_eq!(Position::new(heart, 1), None);"] # [doc = " assert_ne!(Position::new(heart, cheart.len_utf8()), None);"] # [doc = " ```"] pub fn new (input : & str , pos : usize) -> Option < Position < '_ > > { input . get (pos ..) . map (| _ | Position { input , pos }) } # [doc = " Creates a `Position` at the start of a `&str`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use pest::Position;"] # [doc = " let start = Position::from_start(\"\");"] # [doc = " assert_eq!(start.pos(), 0);"] # [doc = " ```"] # [inline] pub fn from_start (input : & 'i str) -> Position < 'i > { Position { input , pos : 0 } } # [doc = " Returns the byte position of this `Position` as a `usize`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use pest::Position;"] # [doc = " let input = \"ab\";"] # [doc = " let mut start = Position::from_start(input);"] # [doc = ""] # [doc = " assert_eq!(start.pos(), 0);"] # [doc = " ```"] # [inline] pub fn pos (& self) -> usize { self . pos } # [doc = " Creates a `Span` from two `Position`s."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the positions come from different inputs."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use pest::Position;"] # [doc = " let input = \"ab\";"] # [doc = " let start = Position::from_start(input);"] # [doc = " let span = start.span(&start.clone());"] # [doc = ""] # [doc = " assert_eq!(span.start(), 0);"] # [doc = " assert_eq!(span.end(), 0);"] # [doc = " ```"] # [inline] pub fn span (& self , other : & Position < 'i >) -> span :: Span < 'i > { if ptr :: eq (self . input , other . input) { span :: Span :: new_internal (self . input , self . pos , other . pos) } else { panic ! ("span created from positions from different inputs") } } # [doc = " Returns the line and column number of this `Position`."] # [doc = ""] # [doc = " This is an O(n) operation, where n is the number of chars in the input."] # [doc = " You better use [`pair.line_col()`](struct.Pair.html#method.line_col) instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use pest;"] # [doc = " # #[allow(non_camel_case_types)]"] # [doc = " # #[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]"] # [doc = " enum Rule {}"] # [doc = ""] # [doc = " let input = \"\\na\";"] # [doc = " let mut state: Box<pest::ParserState<'_, Rule>> = pest::ParserState::new(input);"] # [doc = " let mut result = state.match_string(\"\\na\");"] # [doc = " assert!(result.is_ok());"] # [doc = " assert_eq!(result.unwrap().position().line_col(), (2, 2));"] # [doc = " ```"] # [inline] pub fn line_col (& self) -> (usize , usize) { if self . pos > self . input . len () { panic ! ("position out of bounds") ; } let mut pos = self . pos ; let slice = & self . input [.. pos] ; let mut chars = slice . chars () . peekable () ; let mut line_col = (1 , 1) ; while pos != 0 { match chars . next () { Some ('\r') => { if let Some (& '\n') = chars . peek () { chars . next () ; if pos == 1 { pos -= 1 ; } else { pos -= 2 ; } line_col = (line_col . 0 + 1 , 1) ; } else { pos -= 1 ; line_col = (line_col . 0 , line_col . 1 + 1) ; } } Some ('\n') => { pos -= 1 ; line_col = (line_col . 0 + 1 , 1) ; } Some (c) => { pos -= c . len_utf8 () ; line_col = (line_col . 0 , line_col . 1 + 1) ; } None => unreachable ! () , } } line_col } # [doc = " Returns the entire line of the input that contains this `Position`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use pest;"] # [doc = " # #[allow(non_camel_case_types)]"] # [doc = " # #[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]"] # [doc = " enum Rule {}"] # [doc = ""] # [doc = " let input = \"\\na\";"] # [doc = " let mut state: Box<pest::ParserState<'_, Rule>> = pest::ParserState::new(input);"] # [doc = " let mut result = state.match_string(\"\\na\");"] # [doc = " assert!(result.is_ok());"] # [doc = " assert_eq!(result.unwrap().position().line_of(), \"a\");"] # [doc = " ```"] # [inline] pub fn line_of (& self) -> & 'i str { if self . pos > self . input . len () { panic ! ("position out of bounds") ; } ; & self . input [self . find_line_start () .. self . find_line_end ()] } pub (crate) fn find_line_start (& self) -> usize { if self . input . is_empty () { return 0 ; } ; let start = self . input . char_indices () . rev () . skip_while (| & (i , _) | i >= self . pos) . find (| & (_ , c) | c == '\n') ; match start { Some ((i , _)) => i + 1 , None => 0 , } } pub (crate) fn find_line_end (& self) -> usize { if self . input . is_empty () { 0 } else if self . pos == self . input . len () - 1 { self . input . len () } else { let end = self . input . char_indices () . skip_while (| & (i , _) | i < self . pos) . find (| & (_ , c) | c == '\n') ; match end { Some ((i , _)) => i + 1 , None => self . input . len () , } } } # [doc = " Returns `true` when the `Position` points to the start of the input `&str`."] # [inline] pub (crate) fn at_start (& self) -> bool { self . pos == 0 } # [doc = " Returns `true` when the `Position` points to the end of the input `&str`."] # [inline] pub (crate) fn at_end (& self) -> bool { self . pos == self . input . len () } # [doc = " Skips `n` `char`s from the `Position` and returns `true` if the skip was possible or `false`"] # [doc = " otherwise. If the return value is `false`, `pos` will not be updated."] # [inline] pub (crate) fn skip (& mut self , n : usize) -> bool { let skipped = { let mut len = 0 ; let mut chars = self . input [self . pos ..] . chars () ; for _ in 0 .. n { if let Some (c) = chars . next () { len += c . len_utf8 () ; } else { return false ; } } len } ; self . pos += skipped ; true } # [doc = " Goes back `n` `char`s from the `Position` and returns `true` if the skip was possible or `false`"] # [doc = " otherwise. If the return value is `false`, `pos` will not be updated."] # [inline] pub (crate) fn skip_back (& mut self , n : usize) -> bool { let skipped = { let mut len = 0 ; let mut chars = self . input [.. self . pos] . chars () . rev () ; for _ in 0 .. n { if let Some (c) = chars . next () { len += c . len_utf8 () ; } else { return false ; } } len } ; self . pos -= skipped ; true } # [doc = " Skips until one of the given `strings` is found. If none of the `strings` can be found,"] # [doc = " this function will return `false` but its `pos` will *still* be updated."] # [inline] pub (crate) fn skip_until (& mut self , strings : & [& str]) -> bool { # [cfg (not (feature = "memchr"))] { self . skip_until_basic (strings) } # [cfg (feature = "memchr")] { match strings { [] => () , [s1] => { if let Some (from) = memchr :: memmem :: find (& self . input . as_bytes () [self . pos ..] , s1 . as_bytes ()) { self . pos += from ; return true ; } } [s1 , s2] if ! s1 . is_empty () && ! s2 . is_empty () => { let b1 = s1 . as_bytes () [0] ; let b2 = s2 . as_bytes () [0] ; let miter = memchr :: memchr2_iter (b1 , b2 , & self . input . as_bytes () [self . pos ..]) ; for from in miter { let start = & self . input [self . pos + from ..] ; if start . starts_with (s1) || start . starts_with (s2) { self . pos += from ; return true ; } } } [s1 , s2 , s3] if ! s1 . is_empty () && ! s2 . is_empty () && s3 . is_empty () => { let b1 = s1 . as_bytes () [0] ; let b2 = s2 . as_bytes () [0] ; let b3 = s2 . as_bytes () [0] ; let miter = memchr :: memchr3_iter (b1 , b2 , b3 , & self . input . as_bytes () [self . pos ..]) ; for from in miter { let start = & self . input [self . pos + from ..] ; if start . starts_with (s1) || start . starts_with (s2) || start . starts_with (s3) { self . pos += from ; return true ; } } } _ => { return self . skip_until_basic (strings) ; } } self . pos = self . input . len () ; false } } # [inline] fn skip_until_basic (& mut self , strings : & [& str]) -> bool { for from in self . pos .. self . input . len () { let bytes = if let Some (string) = self . input . get (from ..) { string . as_bytes () } else { continue ; } ; for slice in strings . iter () { let to = slice . len () ; if Some (slice . as_bytes ()) == bytes . get (0 .. to) { self . pos = from ; return true ; } } } self . pos = self . input . len () ; false } # [doc = " Matches the char at the `Position` against a specified character and returns `true` if a match"] # [doc = " was made. If no match was made, returns `false`."] # [doc = " `pos` will not be updated in either case."] # [inline] pub (crate) fn match_char (& self , c : char) -> bool { matches ! (self . input [self . pos ..] . chars () . next () , Some (cc) if c == cc) } # [doc = " Matches the char at the `Position` against a filter function and returns `true` if a match"] # [doc = " was made. If no match was made, returns `false` and `pos` will not be updated."] # [inline] pub (crate) fn match_char_by < F > (& mut self , f : F) -> bool where F : FnOnce (char) -> bool , { if let Some (c) = self . input [self . pos ..] . chars () . next () { if f (c) { self . pos += c . len_utf8 () ; true } else { false } } else { false } } # [doc = " Matches `string` from the `Position` and returns `true` if a match was made or `false`"] # [doc = " otherwise. If no match was made, `pos` will not be updated."] # [inline] pub (crate) fn match_string (& mut self , string : & str) -> bool { let to = self . pos + string . len () ; if Some (string . as_bytes ()) == self . input . as_bytes () . get (self . pos .. to) { self . pos = to ; true } else { false } } # [doc = " Case-insensitively matches `string` from the `Position` and returns `true` if a match was"] # [doc = " made or `false` otherwise. If no match was made, `pos` will not be updated."] # [inline] pub (crate) fn match_insensitive (& mut self , string : & str) -> bool { let matched = { let slice = & self . input [self . pos ..] ; if let Some (slice) = slice . get (0 .. string . len ()) { slice . eq_ignore_ascii_case (string) } else { false } } ; if matched { self . pos += string . len () ; true } else { false } } # [doc = " Matches `char` `range` from the `Position` and returns `true` if a match was made or `false`"] # [doc = " otherwise. If no match was made, `pos` will not be updated."] # [inline] pub (crate) fn match_range (& mut self , range : Range < char >) -> bool { if let Some (c) = self . input [self . pos ..] . chars () . next () { if range . start <= c && c <= range . end { self . pos += c . len_utf8 () ; return true ; } } false } }
    };
}

impl_117!();