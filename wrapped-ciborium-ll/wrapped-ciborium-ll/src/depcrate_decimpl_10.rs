// Generated macro for impl_10 (impl)
macro_rules! Depcrate_decimpl_10 {
() => {
// Module: crate::dec
// Provides: {"impl_10"}
// Dependencies: {}
impl < R : Read > Decoder < R > { # [inline] fn pull_title (& mut self) -> Result < Title , Error < R :: Error > > { if let Some (title) = self . buffer . take () { self . offset += title . 1 . as_ref () . len () + 1 ; return Ok (title) ; } let mut prefix = [0u8 ; 1] ; self . read_exact (& mut prefix [..]) ? ; let major = match prefix [0] >> 5 { 0 => Major :: Positive , 1 => Major :: Negative , 2 => Major :: Bytes , 3 => Major :: Text , 4 => Major :: Array , 5 => Major :: Map , 6 => Major :: Tag , 7 => Major :: Other , _ => unreachable ! () , } ; let mut minor = match prefix [0] & 0b00011111 { x if x < 24 => Minor :: This (x) , 24 => Minor :: Next1 ([0 ; 1]) , 25 => Minor :: Next2 ([0 ; 2]) , 26 => Minor :: Next4 ([0 ; 4]) , 27 => Minor :: Next8 ([0 ; 8]) , 31 => Minor :: More , _ => return Err (Error :: Syntax (self . offset - 1)) , } ; self . read_exact (minor . as_mut ()) ? ; Ok (Title (major , minor)) } # [inline] fn push_title (& mut self , item : Title) { assert ! (self . buffer . is_none ()) ; self . buffer = Some (item) ; self . offset -= item . 1 . as_ref () . len () + 1 ; } # [doc = " Pulls the next header from the input"] # [inline] pub fn pull (& mut self) -> Result < Header , Error < R :: Error > > { let offset = self . offset ; self . pull_title () ? . try_into () . map_err (| _ | Error :: Syntax (offset)) } # [doc = " Push a single header into the input buffer"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if called while there is already a header in the"] # [doc = " input buffer. You should take care to call this function only after"] # [doc = " pulling a header to ensure there is nothing in the input buffer."] # [inline] pub fn push (& mut self , item : Header) { self . push_title (Title :: from (item)) } # [doc = " Gets the current byte offset into the stream"] # [doc = ""] # [doc = " The offset starts at zero when the decoder is created. Therefore, if"] # [doc = " bytes were already read from the reader before the decoder was created,"] # [doc = " you must account for this."] # [inline] pub fn offset (& mut self) -> usize { self . offset } # [doc = " Process an incoming bytes item"] # [doc = ""] # [doc = " In CBOR, bytes can be segmented. The logic for this can be a bit tricky,"] # [doc = " so we encapsulate that logic using this function. This function **MUST**"] # [doc = " be called immediately after first pulling a `Header::Bytes(len)` from"] # [doc = " the wire and `len` must be provided to this function from that value."] # [doc = ""] # [doc = " The `buf` parameter provides a buffer used when reading in the segmented"] # [doc = " bytes. A large buffer will result in fewer calls to read incoming bytes"] # [doc = " at the cost of memory usage. You should consider this trade off when"] # [doc = " deciding the size of your buffer."] # [inline] pub fn bytes < 'a > (& 'a mut self , len : Option < usize >) -> Segments < 'a , R , crate :: seg :: Bytes > { self . push (Header :: Bytes (len)) ; Segments :: new (self , | header | match header { Header :: Bytes (len) => Ok (len) , _ => Err (()) , }) } # [doc = " Process an incoming text item"] # [doc = ""] # [doc = " In CBOR, text can be segmented. The logic for this can be a bit tricky,"] # [doc = " so we encapsulate that logic using this function. This function **MUST**"] # [doc = " be called immediately after first pulling a `Header::Text(len)` from"] # [doc = " the wire and `len` must be provided to this function from that value."] # [doc = ""] # [doc = " The `buf` parameter provides a buffer used when reading in the segmented"] # [doc = " text. A large buffer will result in fewer calls to read incoming bytes"] # [doc = " at the cost of memory usage. You should consider this trade off when"] # [doc = " deciding the size of your buffer."] # [inline] pub fn text < 'a > (& 'a mut self , len : Option < usize >) -> Segments < 'a , R , crate :: seg :: Text > { self . push (Header :: Text (len)) ; Segments :: new (self , | header | match header { Header :: Text (len) => Ok (len) , _ => Err (()) , }) } }
};
}
