// Generated macro for Header (enum)
macro_rules! Depcrate_hdrHeader {
() => {
// Module: crate::hdr
// Provides: {"Header"}
// Dependencies: {}
# [doc = " A semantic representation of a CBOR item header"] # [doc = ""] # [doc = " This structure represents the valid values of a CBOR item header and is"] # [doc = " used extensively when serializing or deserializing CBOR items. Note well"] # [doc = " that this structure **DOES NOT** represent the body (i.e. suffix) of the"] # [doc = " CBOR item. You must parse the body yourself based on the contents of the"] # [doc = " `Header`. However, utility functions are provided for this (see:"] # [doc = " `Decoder::bytes()` and `Decoder::text()`)."] # [derive (Copy , Clone , Debug , PartialEq)] pub enum Header { # [doc = " A positive integer"] Positive (u64) , # [doc = " A negative integer"] # [doc = ""] # [doc = " Note well that this value has all bits inverted from a normal signed"] # [doc = " integer. For example, to convert the `u64` to a `i128` you would do"] # [doc = " this: `neg as i128 ^ !0`."] Negative (u64) , # [doc = " A floating point value"] Float (f64) , # [doc = " A \"simple\" value"] Simple (u8) , # [doc = " A tag"] Tag (u64) , # [doc = " The \"break\" value"] # [doc = ""] # [doc = " This value is used to terminate indefinite length arrays and maps,"] # [doc = " as well as segmented byte or text items."] Break , # [doc = " A bytes item"] # [doc = ""] # [doc = " The value contained in this variant indicates the length of the bytes"] # [doc = " which follow or, if `None`, segmented bytes input."] # [doc = ""] # [doc = " A best practice is to call `Decoder::bytes()` immediately after"] # [doc = " first pulling a bytes item header since this utility function"] # [doc = " encapsulates all the logic needed to handle segmentation."] Bytes (Option < usize >) , # [doc = " A text item"] # [doc = ""] # [doc = " The value contained in this variant indicates the length of the text"] # [doc = " which follows (in bytes) or, if `None`, segmented text input."] # [doc = ""] # [doc = " A best practice is to call `Decoder::text()` immediately after"] # [doc = " first pulling a text item header since this utility function"] # [doc = " encapsulates all the logic needed to handle segmentation."] Text (Option < usize >) , # [doc = " An array item"] # [doc = ""] # [doc = " The value contained in this variant indicates the length of the array"] # [doc = " which follows (in items) or, if `None`, an indefinite length array"] # [doc = " terminated by a \"break\" value."] Array (Option < usize >) , # [doc = " An map item"] # [doc = ""] # [doc = " The value contained in this variant indicates the length of the map"] # [doc = " which follows (in item pairs) or, if `None`, an indefinite length map"] # [doc = " terminated by a \"break\" value."] Map (Option < usize >) , }
};
}
