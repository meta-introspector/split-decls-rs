macro_rules! deps {
    () => {
        Unblock!();
        State!();
        Executor!();
    };
}

macro_rules! impl_10 {
    () => {
        deps!();
        impl < T > Unblock < T > { # [doc = " Wraps a blocking I/O handle into the async [`Unblock`] interface."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use blocking::Unblock;"] # [doc = ""] # [doc = " let stdin = Unblock::new(std::io::stdin());"] # [doc = " ```"] pub fn new (io : T) -> Unblock < T > { Unblock { state : State :: Idle (Some (Box :: new (io))) , cap : None , } } # [doc = " Wraps a blocking I/O handle into the async [`Unblock`] interface with a custom buffer"] # [doc = " capacity."] # [doc = ""] # [doc = " When communicating with the inner [`Stream`]/[`Read`]/[`Write`] type from async code, data"] # [doc = " transferred between blocking and async code goes through a buffer of limited capacity. This"] # [doc = " constructor configures that capacity."] # [doc = ""] # [doc = " The default capacity is:"] # [doc = ""] # [doc = " * For [`Iterator`] types: 8192 items."] # [doc = " * For [`Read`]/[`Write`] types: 8 MB."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use blocking::Unblock;"] # [doc = ""] # [doc = " let stdout = Unblock::with_capacity(64 * 1024, std::io::stdout());"] # [doc = " ```"] pub fn with_capacity (cap : usize , io : T) -> Unblock < T > { Unblock { state : State :: Idle (Some (Box :: new (io))) , cap : Some (cap) , } } # [doc = " Gets a mutable reference to the blocking I/O handle."] # [doc = ""] # [doc = " This is an async method because the I/O handle might be on the thread pool and needs to"] # [doc = " be moved onto the current thread before we can get a reference to it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use blocking::{unblock, Unblock};"] # [doc = " use std::fs::File;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let file = unblock(|| File::create(\"file.txt\")).await?;"] # [doc = " let mut file = Unblock::new(file);"] # [doc = ""] # [doc = " let metadata = file.get_mut().await.metadata()?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn get_mut (& mut self) -> & mut T { future :: poll_fn (| cx | self . poll_stop (cx)) . await . ok () ; match & mut self . state { State :: Idle (t) => t . as_mut () . expect ("inner value was taken out") , State :: WithMut (..) | State :: Streaming (..) | State :: Reading (..) | State :: Writing (..) | State :: Seeking (..) => { unreachable ! ("when stopped, the state machine must be in idle state") ; } } } # [doc = " Performs a blocking operation on the I/O handle."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use blocking::{unblock, Unblock};"] # [doc = " use std::fs::File;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let file = unblock(|| File::create(\"file.txt\")).await?;"] # [doc = " let mut file = Unblock::new(file);"] # [doc = ""] # [doc = " let metadata = file.with_mut(|f| f.metadata()).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn with_mut < R , F > (& mut self , op : F) -> R where F : FnOnce (& mut T) -> R + Send + 'static , R : Send + 'static , T : Send + 'static , { future :: poll_fn (| cx | self . poll_stop (cx)) . await . ok () ; let mut t = match & mut self . state { State :: Idle (t) => t . take () . expect ("inner value was taken out") , State :: WithMut (..) | State :: Streaming (..) | State :: Reading (..) | State :: Writing (..) | State :: Seeking (..) => { unreachable ! ("when stopped, the state machine must be in idle state") ; } } ; let (sender , receiver) = bounded (1) ; let task = Executor :: spawn (async move { sender . try_send (op (& mut t)) . ok () ; t }) ; self . state = State :: WithMut (task) ; receiver . recv () . await . expect ("`Unblock::with_mut()` operation has panicked") } # [doc = " Extracts the inner blocking I/O handle."] # [doc = ""] # [doc = " This is an async method because the I/O handle might be on the thread pool and needs to"] # [doc = " be moved onto the current thread before we can extract it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use blocking::{unblock, Unblock};"] # [doc = " use futures_lite::prelude::*;"] # [doc = " use std::fs::File;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let file = unblock(|| File::create(\"file.txt\")).await?;"] # [doc = " let file = Unblock::new(file);"] # [doc = ""] # [doc = " let file = file.into_inner().await;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn into_inner (self) -> T { let mut this = self ; future :: poll_fn (| cx | this . poll_stop (cx)) . await . ok () ; match & mut this . state { State :: Idle (t) => * t . take () . expect ("inner value was taken out") , State :: WithMut (..) | State :: Streaming (..) | State :: Reading (..) | State :: Writing (..) | State :: Seeking (..) => { unreachable ! ("when stopped, the state machine must be in idle state") ; } } } # [doc = " Waits for the running task to stop."] # [doc = ""] # [doc = " On success, the state machine is moved into the idle state."] fn poll_stop (& mut self , cx : & mut Context < '_ >) -> Poll < io :: Result < () > > { loop { match & mut self . state { State :: Idle (_) => return Poll :: Ready (Ok (())) , State :: WithMut (task) => { let io = ready ! (Pin :: new (task) . poll (cx)) ; self . state = State :: Idle (Some (io)) ; } State :: Streaming (any , task) => { any . take () ; let iter = ready ! (Pin :: new (task) . poll (cx)) ; self . state = State :: Idle (Some (iter)) ; } State :: Reading (reader , task) => { reader . take () ; let (res , io) = ready ! (Pin :: new (task) . poll (cx)) ; self . state = State :: Idle (Some (io)) ; res ? ; } State :: Writing (writer , task) => { writer . take () ; let (res , io) = ready ! (Pin :: new (task) . poll (cx)) ; self . state = State :: Idle (Some (io)) ; res ? ; } State :: Seeking (task) => { let (_ , res , io) = ready ! (Pin :: new (task) . poll (cx)) ; self . state = State :: Idle (Some (io)) ; res ? ; } } } } }
    };
}

impl_10!()