// Generated macro for Unblock (struct)
macro_rules! DepcrateUnblock {
() => {
// Module: crate
// Provides: {"Unblock"}
// Dependencies: {}
# [doc = " Runs blocking I/O on a thread pool."] # [doc = ""] # [doc = " Blocking I/O must be isolated from async code. This type moves blocking I/O operations onto a"] # [doc = " special thread pool while exposing a familiar async interface."] # [doc = ""] # [doc = " This type implements traits [`Stream`], [`AsyncRead`], [`AsyncWrite`], or [`AsyncSeek`] if the"] # [doc = " inner type implements [`Iterator`], [`Read`], [`Write`], or [`Seek`], respectively."] # [doc = ""] # [doc = " # Caveats"] # [doc = ""] # [doc = " [`Unblock`] is a low-level primitive, and as such it comes with some caveats."] # [doc = ""] # [doc = " For higher-level primitives built on top of [`Unblock`], look into [`async-fs`] or"] # [doc = " [`async-process`] (on Windows)."] # [doc = ""] # [doc = " [`async-fs`]: https://github.com/smol-rs/async-fs"] # [doc = " [`async-process`]: https://github.com/smol-rs/async-process"] # [doc = ""] # [doc = " [`Unblock`] communicates with I/O operations on the thread pool through a pipe. That means an"] # [doc = " async read/write operation simply receives/sends some bytes from/into the pipe. When in reading"] # [doc = " mode, the thread pool reads bytes from the I/O handle and forwards them into the pipe until it"] # [doc = " becomes full. When in writing mode, the thread pool reads bytes from the pipe and forwards them"] # [doc = " into the I/O handle."] # [doc = ""] # [doc = " Use [`Unblock::with_capacity()`] to configure the capacity of the pipe."] # [doc = ""] # [doc = " ### Reading"] # [doc = ""] # [doc = " If you create an [`Unblock`]`<`[`Stdin`][`std::io::Stdin`]`>`, read some bytes from it,"] # [doc = " and then drop it, a blocked read operation may keep hanging on the thread pool. The next"] # [doc = " attempt to read from stdin will lose bytes read by the hanging operation. This is a difficult"] # [doc = " problem to solve, so make sure you only use a single stdin handle for the duration of the"] # [doc = " entire program."] # [doc = ""] # [doc = " ### Writing"] # [doc = ""] # [doc = " If writing data through the [`AsyncWrite`] trait, make sure to flush before dropping the"] # [doc = " [`Unblock`] handle or some buffered data might get lost."] # [doc = ""] # [doc = " ### Seeking"] # [doc = ""] # [doc = " Because of buffering in the pipe, if [`Unblock`] wraps a [`File`][`std::fs::File`], a single"] # [doc = " read operation may move the file cursor farther than is the span of the operation. In fact,"] # [doc = " reading just keeps going in the background until the pipe gets full. Keep this mind when"] # [doc = " using [`AsyncSeek`] with [relative][`SeekFrom::Current`] offsets."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use blocking::Unblock;"] # [doc = " use futures_lite::prelude::*;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut stdout = Unblock::new(std::io::stdout());"] # [doc = " stdout.write_all(b\"Hello world!\").await?;"] # [doc = " stdout.flush().await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub struct Unblock < T > { state : State < T > , cap : Option < usize > , }
};
}
