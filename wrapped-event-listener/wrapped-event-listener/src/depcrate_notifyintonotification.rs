// Generated macro for IntoNotification (trait)
macro_rules! Depcrate_notifyIntoNotification {
() => {
// Module: crate::notify
// Provides: {"IntoNotification"}
// Dependencies: {}
# [doc = " A value that can be converted into a [`Notification`]."] # [doc = ""] # [doc = " This trait adds onto the [`Notification`] trait by providing combinators that can be applied to all"] # [doc = " notification types as well as numeric literals. This transforms what would normally be:"] # [doc = ""] # [doc = " ```"] # [doc = " use event_listener::Event;"] # [doc = ""] # [doc = " let event = Event::new();"] # [doc = ""] # [doc = " // Note that each use case needs its own function, leading to bloat."] # [doc = " event.notify(1);"] # [doc = " event.notify_additional(3);"] # [doc = " event.notify_relaxed(5);"] # [doc = " event.notify_additional_relaxed(2);"] # [doc = " ```"] # [doc = ""] # [doc = " into this:"] # [doc = ""] # [doc = " ```"] # [doc = " use event_listener::{Event, IntoNotification, Listener};"] # [doc = ""] # [doc = " let event = Event::new();"] # [doc = ""] # [doc = " event.notify(1);"] # [doc = " event.notify(3.additional());"] # [doc = " event.notify(5.relaxed());"] # [doc = " event.notify(2.additional().relaxed());"] # [doc = " ```"] # [doc = ""] # [doc = " This trait is implemented for all types that implement [`Notification`], as well as for non-floating-point"] # [doc = " numeric literals (`usize`, `i32`, etc)."] # [doc = ""] # [doc = " This function can be thought of as being analogous to [`std::iter::IntoIterator`], but for [`Notification`]."] pub trait IntoNotification : __private :: Sealed { # [doc = " The tag data associated with a notification."] # [doc = ""] # [doc = " By default, most [`Event`]s will use the unit type, `()`. However, this can be used to pass data along to"] # [doc = " the listener."] type Tag ; # [doc = " The notification type."] # [doc = ""] # [doc = " Tells what kind of underlying type that the [`Notification`] is. You probably don't need to worry about"] # [doc = " this."] type Notify : Notification < Tag = Self :: Tag > ; # [doc = " Convert this value into a notification."] # [doc = ""] # [doc = " This allows the user to convert an [`IntoNotification`] into a [`Notification`]."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if the value represents a negative number of notifications."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use event_listener::IntoNotification;"] # [doc = ""] # [doc = " let _ = 3.into_notification();"] # [doc = " ```"] fn into_notification (self) -> Self :: Notify ; # [doc = " Convert this value into an additional notification."] # [doc = ""] # [doc = " By default, notifications ignore listeners that are already notified. Generally, this happens when there"] # [doc = " is an [`EventListener`] that has been woken up, but hasn't been polled to completion or waited on yet."] # [doc = " For instance, if you have three notified listeners and you call `event.notify(5)`, only two listeners"] # [doc = " will be woken up."] # [doc = ""] # [doc = " This default behavior is generally desired. For instance, if you are writing a `Mutex` implementation"] # [doc = " powered by an [`Event`], you usually only want one consumer to be notified at a time. If you notified"] # [doc = " a listener when another listener is already notified, you would have unnecessary contention for your"] # [doc = " lock, as both listeners fight over the lock. Therefore, you would call `event.notify(1)` to make sure"] # [doc = " *at least* one listener is awake."] # [doc = ""] # [doc = " Sometimes, this behavior is not desired. For instance, if you are writing an MPMC channel, it is desirable"] # [doc = " for multiple listeners to be reading from the underlying queue at once. In this case, you would instead"] # [doc = " call `event.notify(1.additional())`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use event_listener::{Event, IntoNotification, Listener};"] # [doc = ""] # [doc = " let event = Event::new();"] # [doc = ""] # [doc = " let mut l1 = event.listen();"] # [doc = " let mut l2 = event.listen();"] # [doc = ""] # [doc = " // This will only wake up the first listener, as the second call observes that there is already a"] # [doc = " // notified listener."] # [doc = " event.notify(1);"] # [doc = " event.notify(1);"] # [doc = ""] # [doc = " // This call wakes up the other listener."] # [doc = " event.notify(1.additional());"] # [doc = " ```"] fn additional (self) -> Additional < Self :: Notify > where Self : Sized , { Additional :: new (self . into_notification ()) } # [doc = " Don't emit a fence for this notification."] # [doc = ""] # [doc = " Usually, notifications emit a `SeqCst` atomic fence before any listeners are woken up. This ensures"] # [doc = " that notification state isn't inconsistent before any wakers are woken up. However, it may be"] # [doc = " desirable to omit this fence in certain cases."] # [doc = ""] # [doc = " - You are running the [`Event`] on a single thread, where no synchronization needs to occur."] # [doc = " - You are emitting the `SeqCst` fence yourself."] # [doc = ""] # [doc = " In these cases, `relaxed()` can be used to avoid emitting the `SeqCst` fence."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use event_listener::{Event, IntoNotification, Listener};"] # [doc = " use std::sync::atomic::{self, Ordering};"] # [doc = ""] # [doc = " let event = Event::new();"] # [doc = ""] # [doc = " let listener1 = event.listen();"] # [doc = " let listener2 = event.listen();"] # [doc = " let listener3 = event.listen();"] # [doc = ""] # [doc = " // We should emit a fence manually when using relaxed notifications."] # [doc = " atomic::fence(Ordering::SeqCst);"] # [doc = ""] # [doc = " // Notifies two listeners."] # [doc = " //"] # [doc = " // Listener queueing is fair, which means `listener1` and `listener2`"] # [doc = " // get notified here since they start listening before `listener3`."] # [doc = " event.notify(1.relaxed());"] # [doc = " event.notify(1.relaxed());"] # [doc = " ```"] fn relaxed (self) -> Relaxed < Self :: Notify > where Self : Sized , { Relaxed :: new (self . into_notification ()) } # [doc = " Use a tag with this notification."] # [doc = ""] # [doc = " In many cases, it is desired to send additional information to the listener of the [`Event`]. For instance,"] # [doc = " it is possible to optimize a `Mutex` implementation by locking directly on the next listener, without"] # [doc = " needing to ever unlock the mutex at all."] # [doc = ""] # [doc = " The tag provided is cloned to provide the tag for all listeners. In cases where this is not flexible"] # [doc = " enough, use [`IntoNotification::with_tag()`] instead."] # [doc = ""] # [doc = " Tagging functions cannot be implemented efficiently for `no_std`, so this is only available"] # [doc = " when the `std` feature is enabled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use event_listener::{IntoNotification, Listener, Event};"] # [doc = ""] # [doc = " let event = Event::<bool>::with_tag();"] # [doc = ""] # [doc = " let mut listener1 = event.listen();"] # [doc = " let mut listener2 = event.listen();"] # [doc = ""] # [doc = " // Notify with `true` then `false`."] # [doc = " event.notify(1.additional().tag(true));"] # [doc = " event.notify(1.additional().tag(false));"] # [doc = ""] # [doc = " # #[cfg(not(target_family = \"wasm\"))] { // Listener::wait is unavailable on WASM"] # [doc = " assert_eq!(listener1.wait(), true);"] # [doc = " assert_eq!(listener2.wait(), false);"] # [doc = " # }"] # [doc = " ```"] # [cfg (feature = "std")] fn tag < T : Clone > (self , tag : T) -> Tag < Self :: Notify , T > where Self : Sized + IntoNotification < Tag = () > , { Tag :: new (tag , self . into_notification ()) } # [doc = " Use a function to generate a tag with this notification."] # [doc = ""] # [doc = " In many cases, it is desired to send additional information to the listener of the [`Event`]. For instance,"] # [doc = " it is possible to optimize a `Mutex` implementation by locking directly on the next listener, without"] # [doc = " needing to ever unlock the mutex at all."] # [doc = ""] # [doc = " Tagging functions cannot be implemented efficiently for `no_std`, so this is only available"] # [doc = " when the `std` feature is enabled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use event_listener::{IntoNotification, Listener, Event};"] # [doc = ""] # [doc = " let event = Event::<bool>::with_tag();"] # [doc = ""] # [doc = " let mut listener1 = event.listen();"] # [doc = " let mut listener2 = event.listen();"] # [doc = ""] # [doc = " // Notify with `true` then `false`."] # [doc = " event.notify(1.additional().tag_with(|| true));"] # [doc = " event.notify(1.additional().tag_with(|| false));"] # [doc = ""] # [doc = " # #[cfg(not(target_family = \"wasm\"))] { // Listener::wait is unavailable on WASM"] # [doc = " assert_eq!(listener1.wait(), true);"] # [doc = " assert_eq!(listener2.wait(), false);"] # [doc = " # }"] # [doc = " ```"] # [cfg (feature = "std")] fn tag_with < T , F > (self , tag : F) -> TagWith < Self :: Notify , F > where Self : Sized + IntoNotification < Tag = () > , F : FnMut () -> T , { TagWith :: new (tag , self . into_notification ()) } }
};
}
