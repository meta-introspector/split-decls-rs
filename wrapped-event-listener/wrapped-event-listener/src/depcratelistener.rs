// Generated macro for listener (macro)
macro_rules! Depcratelistener {
() => {
// Module: crate
// Provides: {"listener"}
// Dependencies: {}
# [doc = " Create a stack-based event listener for an [`Event`]."] # [doc = ""] # [doc = " [`EventListener`] allocates the listener on the heap. While this works for most use cases, in"] # [doc = " practice this heap allocation can be expensive for repeated uses. This method allows for"] # [doc = " allocating the listener on the stack instead."] # [doc = ""] # [doc = " There are limitations to using this macro instead of the [`EventListener`] type, however."] # [doc = " Firstly, it is significantly less flexible. The listener is locked to the current stack"] # [doc = " frame, meaning that it can't be returned or put into a place where it would go out of"] # [doc = " scope. For instance, this will not work:"] # [doc = ""] # [doc = " ```compile_fail"] # [doc = " use event_listener::{Event, Listener, listener};"] # [doc = ""] # [doc = " fn get_listener(event: &Event) -> impl Listener {"] # [doc = "     listener!(event => cant_return_this);"] # [doc = "     cant_return_this"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " In addition, the types involved in creating this listener are not able to be named. Therefore"] # [doc = " it cannot be used in hand-rolled futures or similar structures."] # [doc = ""] # [doc = " The type created by this macro implements [`Listener`], allowing it to be used in cases where"] # [doc = " [`EventListener`] would normally be used."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " To use this macro, replace cases where you would normally use this..."] # [doc = ""] # [doc = " ```no_compile"] # [doc = " let listener = event.listen();"] # [doc = " ```"] # [doc = ""] # [doc = " ...with this:"] # [doc = ""] # [doc = " ```no_compile"] # [doc = " listener!(event => listener);"] # [doc = " ```"] # [doc = ""] # [doc = " Here is the top level example from this crate's documentation, but using [`listener`] instead"] # [doc = " of [`EventListener`]."] # [doc = ""] # [doc = " ```"] # [doc = " # #[cfg(not(target_family = \"wasm\"))] { // Listener::wait is unavailable on WASM"] # [doc = " use std::sync::atomic::{AtomicBool, Ordering};"] # [doc = " use std::sync::Arc;"] # [doc = " use std::thread;"] # [doc = " use std::time::Duration;"] # [doc = " use std::usize;"] # [doc = " use event_listener::{Event, listener, IntoNotification, Listener};"] # [doc = ""] # [doc = " let flag = Arc::new(AtomicBool::new(false));"] # [doc = " let event = Arc::new(Event::new());"] # [doc = ""] # [doc = " // Spawn a thread that will set the flag after 1 second."] # [doc = " thread::spawn({"] # [doc = "     let flag = flag.clone();"] # [doc = "     let event = event.clone();"] # [doc = "     move || {"] # [doc = "         // Wait for a second."] # [doc = "         thread::sleep(Duration::from_secs(1));"] # [doc = ""] # [doc = "         // Set the flag."] # [doc = "         flag.store(true, Ordering::SeqCst);"] # [doc = ""] # [doc = "         // Notify all listeners that the flag has been set."] # [doc = "         event.notify(usize::MAX);"] # [doc = "     }"] # [doc = " });"] # [doc = ""] # [doc = " // Wait until the flag is set."] # [doc = " loop {"] # [doc = "     // Check the flag."] # [doc = "     if flag.load(Ordering::SeqCst) {"] # [doc = "         break;"] # [doc = "     }"] # [doc = ""] # [doc = "     // Start listening for events."] # [doc = "     // NEW: Changed to a stack-based listener."] # [doc = "     listener!(event => listener);"] # [doc = ""] # [doc = "     // Check the flag again after creating the listener."] # [doc = "     if flag.load(Ordering::SeqCst) {"] # [doc = "         break;"] # [doc = "     }"] # [doc = ""] # [doc = "     // Wait for a notification and continue the loop."] # [doc = "     listener.wait();"] # [doc = " }"] # [doc = " # }"] # [doc = " ```"] # [macro_export] macro_rules ! listener { ($ event : expr => $ listener : ident) => { let mut $ listener = $ crate :: __private :: StackSlot :: new (&$ event) ; let mut $ listener = unsafe { $ crate :: __private :: Pin :: new_unchecked (& mut $ listener) } ; # [allow (unused_mut)] let mut $ listener = $ listener . listen () ; } ; }
};
}
