// Generated macro for impl_20 (impl)
macro_rules! Depcrate_dirimpl_20 {
() => {
// Module: crate::dir
// Provides: {"impl_20"}
// Dependencies: {}
impl TempDir { # [doc = " Attempts to make a temporary directory inside of `env::temp_dir()`."] # [doc = ""] # [doc = " See [`Builder`] for more configuration."] # [doc = ""] # [doc = " The directory and everything inside it will be automatically deleted"] # [doc = " once the returned `TempDir` is destroyed."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the directory can not be created, `Err` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::fs::File;"] # [doc = " use std::io::Write;"] # [doc = " use tempfile::TempDir;"] # [doc = ""] # [doc = " // Create a directory inside of `env::temp_dir()`"] # [doc = " let tmp_dir = TempDir::new()?;"] # [doc = ""] # [doc = " let file_path = tmp_dir.path().join(\"my-temporary-note.txt\");"] # [doc = " let mut tmp_file = File::create(file_path)?;"] # [doc = " writeln!(tmp_file, \"Brian was here. Briefly.\")?;"] # [doc = ""] # [doc = " // `tmp_dir` goes out of scope, the directory as well as"] # [doc = " // `tmp_file` will be deleted here."] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] # [doc = ""] # [doc = " [`Builder`]: struct.Builder.html"] pub fn new () -> io :: Result < TempDir > { Builder :: new () . tempdir () } # [doc = " Attempts to make a temporary directory inside of `dir`."] # [doc = " The directory and everything inside it will be automatically"] # [doc = " deleted once the returned `TempDir` is destroyed."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the directory can not be created, `Err` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::fs::{self, File};"] # [doc = " use std::io::Write;"] # [doc = " use tempfile::TempDir;"] # [doc = ""] # [doc = " // Create a directory inside of the current directory"] # [doc = " let tmp_dir = TempDir::new_in(\".\")?;"] # [doc = " let file_path = tmp_dir.path().join(\"my-temporary-note.txt\");"] # [doc = " let mut tmp_file = File::create(file_path)?;"] # [doc = " writeln!(tmp_file, \"Brian was here. Briefly.\")?;"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] pub fn new_in < P : AsRef < Path > > (dir : P) -> io :: Result < TempDir > { Builder :: new () . tempdir_in (dir) } # [doc = " Attempts to make a temporary directory with the specified prefix inside of"] # [doc = " `env::temp_dir()`. The directory and everything inside it will be automatically"] # [doc = " deleted once the returned `TempDir` is destroyed."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the directory can not be created, `Err` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::fs::{self, File};"] # [doc = " use std::io::Write;"] # [doc = " use tempfile::TempDir;"] # [doc = ""] # [doc = " // Create a directory inside of the current directory"] # [doc = " let tmp_dir = TempDir::with_prefix(\"foo-\")?;"] # [doc = " let tmp_name = tmp_dir.path().file_name().unwrap().to_str().unwrap();"] # [doc = " assert!(tmp_name.starts_with(\"foo-\"));"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] pub fn with_prefix < S : AsRef < OsStr > > (prefix : S) -> io :: Result < TempDir > { Builder :: new () . prefix (& prefix) . tempdir () } # [doc = " Attempts to make a temporary directory with the specified suffix inside of"] # [doc = " `env::temp_dir()`. The directory and everything inside it will be automatically"] # [doc = " deleted once the returned `TempDir` is destroyed."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the directory can not be created, `Err` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::fs::{self, File};"] # [doc = " use std::io::Write;"] # [doc = " use tempfile::TempDir;"] # [doc = ""] # [doc = " // Create a directory inside of the current directory"] # [doc = " let tmp_dir = TempDir::with_suffix(\"-foo\")?;"] # [doc = " let tmp_name = tmp_dir.path().file_name().unwrap().to_str().unwrap();"] # [doc = " assert!(tmp_name.ends_with(\"-foo\"));"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] pub fn with_suffix < S : AsRef < OsStr > > (suffix : S) -> io :: Result < TempDir > { Builder :: new () . suffix (& suffix) . tempdir () } # [doc = " Attempts to make a temporary directory with the specified prefix inside"] # [doc = " the specified directory. The directory and everything inside it will be"] # [doc = " automatically deleted once the returned `TempDir` is destroyed."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the directory can not be created, `Err` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::fs::{self, File};"] # [doc = " use std::io::Write;"] # [doc = " use tempfile::TempDir;"] # [doc = ""] # [doc = " // Create a directory inside of the current directory"] # [doc = " let tmp_dir = TempDir::with_suffix_in(\"-foo\", \".\")?;"] # [doc = " let tmp_name = tmp_dir.path().file_name().unwrap().to_str().unwrap();"] # [doc = " assert!(tmp_name.ends_with(\"-foo\"));"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] pub fn with_suffix_in < S : AsRef < OsStr > , P : AsRef < Path > > (suffix : S , dir : P ,) -> io :: Result < TempDir > { Builder :: new () . suffix (& suffix) . tempdir_in (dir) } # [doc = " Attempts to make a temporary directory with the specified prefix inside"] # [doc = " the specified directory. The directory and everything inside it will be"] # [doc = " automatically deleted once the returned `TempDir` is destroyed."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the directory can not be created, `Err` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::fs::{self, File};"] # [doc = " use std::io::Write;"] # [doc = " use tempfile::TempDir;"] # [doc = ""] # [doc = " // Create a directory inside of the current directory"] # [doc = " let tmp_dir = TempDir::with_prefix_in(\"foo-\", \".\")?;"] # [doc = " let tmp_name = tmp_dir.path().file_name().unwrap().to_str().unwrap();"] # [doc = " assert!(tmp_name.starts_with(\"foo-\"));"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] pub fn with_prefix_in < S : AsRef < OsStr > , P : AsRef < Path > > (prefix : S , dir : P ,) -> io :: Result < TempDir > { Builder :: new () . prefix (& prefix) . tempdir_in (dir) } # [doc = " Accesses the [`Path`] to the temporary directory."] # [doc = ""] # [doc = " [`Path`]: http://doc.rust-lang.org/std/path/struct.Path.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use tempfile::TempDir;"] # [doc = ""] # [doc = " let tmp_path;"] # [doc = ""] # [doc = " {"] # [doc = "    let tmp_dir = TempDir::new()?;"] # [doc = "    tmp_path = tmp_dir.path().to_owned();"] # [doc = ""] # [doc = "    // Check that the temp directory actually exists."] # [doc = "    assert!(tmp_path.exists());"] # [doc = ""] # [doc = "    // End of `tmp_dir` scope, directory will be deleted"] # [doc = " }"] # [doc = ""] # [doc = " // Temp directory should be deleted by now"] # [doc = " assert_eq!(tmp_path.exists(), false);"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] # [must_use] pub fn path (& self) -> & path :: Path { self . path . as_ref () } # [doc = " Deprecated alias for [`TempDir::keep`]."] # [must_use] # [deprecated = "use TempDir::keep()"] pub fn into_path (self) -> PathBuf { self . keep () } # [doc = " Persist the temporary directory to disk, returning the [`PathBuf`] where it is located."] # [doc = ""] # [doc = " This consumes the [`TempDir`] without deleting directory on the filesystem, meaning that"] # [doc = " the directory will no longer be automatically deleted."] # [doc = ""] # [doc = " If you want to disable automatic cleanup of the temporary directory in-place, keeping the"] # [doc = " `TempDir` as-is, use [`TempDir::disable_cleanup`] instead."] # [doc = ""] # [doc = " [`TempDir`]: struct.TempDir.html"] # [doc = " [`PathBuf`]: http://doc.rust-lang.org/std/path/struct.PathBuf.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::fs;"] # [doc = " use tempfile::TempDir;"] # [doc = ""] # [doc = " let tmp_dir = TempDir::new()?;"] # [doc = ""] # [doc = " // Persist the temporary directory to disk,"] # [doc = " // getting the path where it is."] # [doc = " let tmp_path = tmp_dir.keep();"] # [doc = ""] # [doc = " // Delete the temporary directory ourselves."] # [doc = " fs::remove_dir_all(tmp_path)?;"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] # [must_use] pub fn keep (mut self) -> PathBuf { self . disable_cleanup (true) ; mem :: replace (& mut self . path , PathBuf :: new () . into_boxed_path ()) . into () } # [doc = " Disable cleanup of the temporary directory. If `disable_cleanup` is `true`, the temporary"] # [doc = " directory will not be deleted when this `TempDir` is dropped. This method is equivalent to"] # [doc = " calling [`Builder::disable_cleanup`] when creating the `TempDir`."] # [doc = ""] # [doc = " **NOTE:** this method is primarily useful for testing/debugging. If you want to simply turn"] # [doc = " a temporary directory into a non-temporary directory, prefer [`TempDir::keep`]."] pub fn disable_cleanup (& mut self , disable_cleanup : bool) { self . disable_cleanup = disable_cleanup } # [doc = " Closes and removes the temporary directory, returning a `Result`."] # [doc = ""] # [doc = " Although `TempDir` removes the directory on drop, in the destructor"] # [doc = " any errors are ignored. To detect errors cleaning up the temporary"] # [doc = " directory, call `close` instead."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function may return a variety of [`std::io::Error`]s that result from deleting"] # [doc = " the files and directories contained with the temporary directory,"] # [doc = " as well as from deleting the temporary directory itself. These errors"] # [doc = " may be platform specific."] # [doc = ""] # [doc = " [`std::io::Error`]: http://doc.rust-lang.org/std/io/struct.Error.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::fs::File;"] # [doc = " use std::io::Write;"] # [doc = " use tempfile::TempDir;"] # [doc = ""] # [doc = " // Create a directory inside of `env::temp_dir()`."] # [doc = " let tmp_dir = TempDir::new()?;"] # [doc = " let file_path = tmp_dir.path().join(\"my-temporary-note.txt\");"] # [doc = " let mut tmp_file = File::create(file_path)?;"] # [doc = " writeln!(tmp_file, \"Brian was here. Briefly.\")?;"] # [doc = ""] # [doc = " // By closing the `TempDir` explicitly we can check that it has"] # [doc = " // been deleted successfully. If we don't close it explicitly,"] # [doc = " // the directory will still be deleted when `tmp_dir` goes out"] # [doc = " // of scope, but we won't know whether deleting the directory"] # [doc = " // succeeded."] # [doc = " drop(tmp_file);"] # [doc = " tmp_dir.close()?;"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] pub fn close (mut self) -> io :: Result < () > { let result = remove_dir_all (self . path ()) . with_err_path (| | self . path ()) ; self . path = PathBuf :: new () . into_boxed_path () ; mem :: forget (self) ; result } }
};
}
