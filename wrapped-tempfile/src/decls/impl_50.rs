macro_rules! deps {
    () => {
        PersistError!();
        NamedTempFile!();
        PathPersistError!();
        TempPath!();
    };
}

macro_rules! impl_50 {
    () => {
        deps!();
        impl < F > NamedTempFile < F > { # [doc = " Get the temporary file's path."] # [doc = ""] # [doc = " # Security"] # [doc = ""] # [doc = " Referring to a temporary file's path may not be secure in all cases."] # [doc = " Please read the security section on the top level documentation of this"] # [doc = " type for details."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use tempfile::NamedTempFile;"] # [doc = ""] # [doc = " let file = NamedTempFile::new()?;"] # [doc = ""] # [doc = " println!(\"{:?}\", file.path());"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] # [inline] pub fn path (& self) -> & Path { & self . path } # [doc = " Close and remove the temporary file."] # [doc = ""] # [doc = " Use this if you want to detect errors in deleting the file."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the file cannot be deleted, `Err` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use tempfile::NamedTempFile;"] # [doc = ""] # [doc = " let file = NamedTempFile::new()?;"] # [doc = ""] # [doc = " // By closing the `NamedTempFile` explicitly, we can check that it has"] # [doc = " // been deleted successfully. If we don't close it explicitly,"] # [doc = " // the file will still be deleted when `file` goes out"] # [doc = " // of scope, but we won't know whether deleting the file"] # [doc = " // succeeded."] # [doc = " file.close()?;"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] pub fn close (self) -> io :: Result < () > { let NamedTempFile { path , .. } = self ; path . close () } # [doc = " Persist the temporary file at the target path."] # [doc = ""] # [doc = " If a file exists at the target path, persist will atomically replace it."] # [doc = " If this method fails, it will return `self` in the resulting"] # [doc = " [`PersistError`]."] # [doc = ""] # [doc = " **Note:** Temporary files cannot be persisted across filesystems. Also"] # [doc = " neither the file contents nor the containing directory are"] # [doc = " synchronized, so the update may not yet have reached the disk when"] # [doc = " `persist` returns."] # [doc = ""] # [doc = " # Security"] # [doc = ""] # [doc = " This method persists the temporary file using its path and may not be"] # [doc = " secure in all cases. Please read the security section on the top"] # [doc = " level documentation of this type for details."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the file cannot be moved to the new location, `Err` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::Write;"] # [doc = " use tempfile::NamedTempFile;"] # [doc = ""] # [doc = " let file = NamedTempFile::new()?;"] # [doc = ""] # [doc = " let mut persisted_file = file.persist(\"./saved_file.txt\")?;"] # [doc = " writeln!(persisted_file, \"Brian was here. Briefly.\")?;"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] # [doc = ""] # [doc = " [`PersistError`]: struct.PersistError.html"] pub fn persist < P : AsRef < Path > > (self , new_path : P) -> Result < F , PersistError < F > > { let NamedTempFile { path , file } = self ; match path . persist (new_path) { Ok (_) => Ok (file) , Err (err) => { let PathPersistError { error , path } = err ; Err (PersistError { file : NamedTempFile { path , file } , error , }) } } } # [doc = " Persist the temporary file at the target path if and only if no file exists there."] # [doc = ""] # [doc = " If a file exists at the target path, fail. If this method fails, it will"] # [doc = " return `self` in the resulting PersistError."] # [doc = ""] # [doc = " **Note:** Temporary files cannot be persisted across filesystems."] # [doc = ""] # [doc = " **Atomicity:** This method is not guaranteed to be atomic on all platforms, although it will"] # [doc = " generally be atomic on Windows and modern Linux filesystems. While it will never overwrite a"] # [doc = " file at the target path, it may leave the original link to the temporary file behind leaving"] # [doc = " you with two [hard links][hardlink] in your filesystem pointing at the same underlying file."] # [doc = " This can happen if either (a) we lack permission to \"unlink\" the original filename; (b) this"] # [doc = " program crashes while persisting the temporary file; or (c) the filesystem is removed,"] # [doc = " unmounted, etc. while we're performing this operation."] # [doc = ""] # [doc = " # Security"] # [doc = ""] # [doc = " This method persists the temporary file using its path and may not be"] # [doc = " secure in all cases. Please read the security section on the top"] # [doc = " level documentation of this type for details."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the file cannot be moved to the new location or a file already exists there,"] # [doc = " `Err` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::Write;"] # [doc = " use tempfile::NamedTempFile;"] # [doc = ""] # [doc = " let file = NamedTempFile::new()?;"] # [doc = ""] # [doc = " let mut persisted_file = file.persist_noclobber(\"./saved_file.txt\")?;"] # [doc = " writeln!(persisted_file, \"Brian was here. Briefly.\")?;"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] # [doc = ""] # [doc = " [hardlink]: https://en.wikipedia.org/wiki/Hard_link"] pub fn persist_noclobber < P : AsRef < Path > > (self , new_path : P) -> Result < F , PersistError < F > > { let NamedTempFile { path , file } = self ; match path . persist_noclobber (new_path) { Ok (_) => Ok (file) , Err (err) => { let PathPersistError { error , path } = err ; Err (PersistError { file : NamedTempFile { path , file } , error , }) } } } # [doc = " Keep the temporary file from being deleted. This function will turn the"] # [doc = " temporary file into a non-temporary file without moving it."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " On some platforms (e.g., Windows), we need to mark the file as"] # [doc = " non-temporary. This operation could fail."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::io::Write;"] # [doc = " use tempfile::NamedTempFile;"] # [doc = ""] # [doc = " let mut file = NamedTempFile::new()?;"] # [doc = " writeln!(file, \"Brian was here. Briefly.\")?;"] # [doc = ""] # [doc = " let (file, path) = file.keep()?;"] # [doc = " # Ok::<(), std::io::Error>(())"] # [doc = " ```"] # [doc = ""] # [doc = " [`PathPersistError`]: struct.PathPersistError.html"] pub fn keep (self) -> Result < (F , PathBuf) , PersistError < F > > { let (file , path) = (self . file , self . path) ; match path . keep () { Ok (path) => Ok ((file , path)) , Err (PathPersistError { error , path }) => Err (PersistError { file : NamedTempFile { path , file } , error , }) , } } # [doc = " Disable cleanup of the temporary file. If `disable_cleanup` is `true`, the temporary file"] # [doc = " will not be deleted when this `TempPath` is dropped. This method is equivalent to calling"] # [doc = " [`Builder::disable_cleanup`] when creating the original `NamedTempFile`, which see for"] # [doc = " relevant warnings."] # [doc = ""] # [doc = " **NOTE:** this method is primarily useful for testing/debugging. If you want to simply turn"] # [doc = " a temporary file into a non-temporary file, prefer [`NamedTempFile::keep`]."] pub fn disable_cleanup (& mut self , disable_cleanup : bool) { self . path . disable_cleanup (disable_cleanup) } # [doc = " Get a reference to the underlying file."] pub fn as_file (& self) -> & F { & self . file } # [doc = " Get a mutable reference to the underlying file."] pub fn as_file_mut (& mut self) -> & mut F { & mut self . file } # [doc = " Turn this named temporary file into an \"unnamed\" temporary file as if you"] # [doc = " had constructed it with [`tempfile()`]."] # [doc = ""] # [doc = " The underlying file will be removed from the filesystem but the returned [`File`]"] # [doc = " can still be read/written."] pub fn into_file (self) -> F { self . file } # [doc = " Closes the file, leaving only the temporary file path."] # [doc = ""] # [doc = " This is useful when another process must be able to open the temporary"] # [doc = " file."] pub fn into_temp_path (self) -> TempPath { self . path } # [doc = " Converts the named temporary file into its constituent parts."] # [doc = ""] # [doc = " Note: When the path is dropped, the underlying file will be removed from the filesystem but"] # [doc = " the returned [`File`] can still be read/written."] pub fn into_parts (self) -> (F , TempPath) { (self . file , self . path) } # [doc = " Creates a `NamedTempFile` from its constituent parts."] # [doc = ""] # [doc = " This can be used with [`NamedTempFile::into_parts`] to reconstruct the"] # [doc = " `NamedTempFile`."] pub fn from_parts (file : F , path : TempPath) -> Self { Self { file , path } } }
    };
}

impl_50!();