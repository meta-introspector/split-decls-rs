// Generated macro for span_lint_and_help (function)
macro_rules! Depcrate_diagnosticsspan_lint_and_help {
() => {
// Module: crate::diagnostics
// Provides: {"span_lint_and_help"}
// Dependencies: {}
# [doc = " Same as [`span_lint`] but with an extra `help` message."] # [doc = ""] # [doc = " Use this if you want to provide some general help but"] # [doc = " can't provide a specific machine applicable suggestion."] # [doc = ""] # [doc = " The `help` message can be optionally attached to a `Span`."] # [doc = ""] # [doc = " If you change the signature, remember to update the internal lint `CollapsibleCalls`"] # [doc = ""] # [doc = " NOTE: Lint emissions are always bound to a node in the HIR, which is used to determine"] # [doc = " the lint level."] # [doc = " For the `span_lint_and_help` function, the node that was passed into the `LintPass::check_*`"] # [doc = " function is used."] # [doc = ""] # [doc = " If you're emitting the lint at the span of a different node than the one provided by the"] # [doc = " `LintPass::check_*` function, consider using [`span_lint_hir_and_then`] instead."] # [doc = " This is needed for `#[allow]` and `#[expect]` attributes to work on the node"] # [doc = " highlighted in the displayed warning."] # [doc = ""] # [doc = " If you're unsure which function you should use, you can test if the `#[expect]` attribute works"] # [doc = " where you would expect it to."] # [doc = " If it doesn't, you likely need to use [`span_lint_hir_and_then`] instead."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```text"] # [doc = " error: constant division of 0.0 with 0.0 will always result in NaN"] # [doc = "   --> tests/ui/zero_div_zero.rs:6:25"] # [doc = "    |"] # [doc = " 6  |     let other_f64_nan = 0.0f64 / 0.0;"] # [doc = "    |                         ^^^^^^^^^^^^"] # [doc = "    |"] # [doc = "    = help: consider using `f64::NAN` if you would like a constant representing NaN"] # [doc = " ```"] # [track_caller] pub fn span_lint_and_help < T : LintContext > (cx : & T , lint : & 'static Lint , span : impl Into < MultiSpan > , msg : impl Into < DiagMessage > , help_span : Option < Span > , help : impl Into < SubdiagMessage > ,) { # [expect (clippy :: disallowed_methods)] cx . span_lint (lint , span , | diag | { diag . primary_message (msg) ; if let Some (help_span) = help_span { diag . span_help (help_span , help . into ()) ; } else { diag . help (help . into ()) ; } docs_link (diag , lint) ; # [cfg (debug_assertions)] validate_diag (diag) ; }) ; }
};
}
