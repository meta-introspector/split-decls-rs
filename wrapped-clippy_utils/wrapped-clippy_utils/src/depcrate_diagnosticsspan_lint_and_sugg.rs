// Generated macro for span_lint_and_sugg (function)
macro_rules! Depcrate_diagnosticsspan_lint_and_sugg {
() => {
// Module: crate::diagnostics
// Provides: {"span_lint_and_sugg"}
// Dependencies: {}
# [doc = " Add a span lint with a suggestion on how to fix it."] # [doc = ""] # [doc = " These suggestions can be parsed by rustfix to allow it to automatically fix your code."] # [doc = " In the example below, `help` is `\"try\"` and `sugg` is the suggested replacement `\".any(|x| x >"] # [doc = " 2)\"`."] # [doc = ""] # [doc = " If you change the signature, remember to update the internal lint `CollapsibleCalls`"] # [doc = ""] # [doc = " NOTE: Lint emissions are always bound to a node in the HIR, which is used to determine"] # [doc = " the lint level."] # [doc = " For the `span_lint_and_sugg` function, the node that was passed into the `LintPass::check_*`"] # [doc = " function is used."] # [doc = ""] # [doc = " If you're emitting the lint at the span of a different node than the one provided by the"] # [doc = " `LintPass::check_*` function, consider using [`span_lint_hir_and_then`] instead."] # [doc = " This is needed for `#[allow]` and `#[expect]` attributes to work on the node"] # [doc = " highlighted in the displayed warning."] # [doc = ""] # [doc = " If you're unsure which function you should use, you can test if the `#[expect]` attribute works"] # [doc = " where you would expect it to."] # [doc = " If it doesn't, you likely need to use [`span_lint_hir_and_then`] instead."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```text"] # [doc = " error: This `.fold` can be more succinctly expressed as `.any`"] # [doc = " --> tests/ui/methods.rs:390:13"] # [doc = "     |"] # [doc = " 390 |     let _ = (0..3).fold(false, |acc, x| acc || x > 2);"] # [doc = "     |                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ help: try: `.any(|x| x > 2)`"] # [doc = "     |"] # [doc = "     = note: `-D fold-any` implied by `-D warnings`"] # [doc = " ```"] # [cfg_attr (not (debug_assertions) , expect (clippy :: collapsible_span_lint_calls))] # [track_caller] pub fn span_lint_and_sugg < T : LintContext > (cx : & T , lint : & 'static Lint , sp : Span , msg : impl Into < DiagMessage > , help : impl Into < SubdiagMessage > , sugg : String , applicability : Applicability ,) { span_lint_and_then (cx , lint , sp , msg . into () , | diag | { diag . span_suggestion (sp , help . into () , sugg , applicability) ; # [cfg (debug_assertions)] validate_diag (diag) ; }) ; }
};
}
