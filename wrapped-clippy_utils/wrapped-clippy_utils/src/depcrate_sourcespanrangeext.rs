// Generated macro for SpanRangeExt (trait)
macro_rules! Depcrate_sourceSpanRangeExt {
() => {
// Module: crate::source
// Provides: {"SpanRangeExt"}
// Dependencies: {}
pub trait SpanRangeExt : SpanRange { # [doc = " Attempts to get a handle to the source text. Returns `None` if either the span is malformed,"] # [doc = " or the source text is not accessible."] fn get_source_text (self , cx : & impl HasSession) -> Option < SourceText > { get_source_range (cx . sess () . source_map () , self . into_range ()) . and_then (SourceText :: new) } # [doc = " Gets the source file, and range in the file, of the given span. Returns `None` if the span"] # [doc = " extends through multiple files, or is malformed."] fn get_source_range (self , cx : & impl HasSession) -> Option < SourceFileRange > { get_source_range (cx . sess () . source_map () , self . into_range ()) } # [doc = " Calls the given function with the source text referenced and returns the value. Returns"] # [doc = " `None` if the source text cannot be retrieved."] fn with_source_text < T > (self , cx : & impl HasSession , f : impl for < 'a > FnOnce (& 'a str) -> T) -> Option < T > { with_source_text (cx . sess () . source_map () , self . into_range () , f) } # [doc = " Checks if the referenced source text satisfies the given predicate. Returns `false` if the"] # [doc = " source text cannot be retrieved."] fn check_source_text (self , cx : & impl HasSession , pred : impl for < 'a > FnOnce (& 'a str) -> bool) -> bool { self . with_source_text (cx , pred) . unwrap_or (false) } # [doc = " Calls the given function with the both the text of the source file and the referenced range,"] # [doc = " and returns the value. Returns `None` if the source text cannot be retrieved."] fn with_source_text_and_range < T > (self , cx : & impl HasSession , f : impl for < 'a > FnOnce (& 'a str , Range < usize >) -> T ,) -> Option < T > { with_source_text_and_range (cx . sess () . source_map () , self . into_range () , f) } # [doc = " Calls the given function with the both the text of the source file and the referenced range,"] # [doc = " and creates a new span with the returned range. Returns `None` if the source text cannot be"] # [doc = " retrieved, or no result is returned."] # [doc = ""] # [doc = " The new range must reside within the same source file."] fn map_range (self , cx : & impl HasSession , f : impl for < 'a > FnOnce (& 'a SourceFile , & 'a str , Range < usize >) -> Option < Range < usize > > ,) -> Option < Range < BytePos > > { map_range (cx . sess () . source_map () , self . into_range () , f) } # [doc = " Extends the range to include all preceding whitespace characters."] # [doc = ""] # [doc = " The range will not be expanded if it would cross a line boundary, the line the range would"] # [doc = " be extended to ends with a line comment and the text after the range contains a"] # [doc = " non-whitespace character on the same line. e.g."] # [doc = ""] # [doc = " ```ignore"] # [doc = " ( // Some comment"] # [doc = " foo)"] # [doc = " ```"] # [doc = ""] # [doc = " When the range points to `foo`, suggesting to remove the range after it's been extended will"] # [doc = " cause the `)` to be placed inside the line comment as `( // Some comment)`."] fn with_leading_whitespace (self , cx : & impl HasSession) -> Range < BytePos > { with_leading_whitespace (cx . sess () . source_map () , self . into_range ()) } # [doc = " Trims the leading whitespace from the range."] fn trim_start (self , cx : & impl HasSession) -> Range < BytePos > { trim_start (cx . sess () . source_map () , self . into_range ()) } }
};
}
