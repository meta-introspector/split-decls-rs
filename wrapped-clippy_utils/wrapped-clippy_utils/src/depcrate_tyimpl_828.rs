// Generated macro for impl_828 (impl)
macro_rules! Depcrate_tyimpl_828 {
() => {
// Module: crate::ty
// Provides: {"impl_828"}
// Dependencies: {}
impl < 'tcx > InteriorMut < 'tcx > { pub fn new (tcx : TyCtxt < 'tcx > , ignore_interior_mutability : & [String]) -> Self { let ignored_def_ids = ignore_interior_mutability . iter () . flat_map (| ignored_ty | lookup_path_str (tcx , PathNS :: Type , ignored_ty)) . collect () ; Self { ignored_def_ids , .. Self :: default () } } pub fn without_pointers (tcx : TyCtxt < 'tcx > , ignore_interior_mutability : & [String]) -> Self { Self { ignore_pointers : true , .. Self :: new (tcx , ignore_interior_mutability) } } # [doc = " Check if given type has interior mutability such as [`std::cell::Cell`] or"] # [doc = " [`std::cell::RefCell`] etc. and if it does, returns a chain of types that causes"] # [doc = " this type to be interior mutable.  False negatives may be expected for infinitely recursive"] # [doc = " types, and `None` will be returned there."] pub fn interior_mut_ty_chain (& mut self , cx : & LateContext < 'tcx > , ty : Ty < 'tcx >) -> Option < & 'tcx ty :: List < Ty < 'tcx > > > { self . interior_mut_ty_chain_inner (cx , ty , 0) } fn interior_mut_ty_chain_inner (& mut self , cx : & LateContext < 'tcx > , ty : Ty < 'tcx > , depth : usize ,) -> Option < & 'tcx ty :: List < Ty < 'tcx > > > { if ! cx . tcx . recursion_limit () . value_within_limit (depth) { return None ; } match self . tys . entry (ty) { Entry :: Occupied (o) => return * o . get () , Entry :: Vacant (v) => v . insert (None) , } ; let depth = depth + 1 ; let chain = match * ty . kind () { ty :: RawPtr (inner_ty , _) if ! self . ignore_pointers => self . interior_mut_ty_chain_inner (cx , inner_ty , depth) , ty :: Ref (_ , inner_ty , _) | ty :: Slice (inner_ty) => self . interior_mut_ty_chain_inner (cx , inner_ty , depth) , ty :: Array (inner_ty , size) if size . try_to_target_usize (cx . tcx) != Some (0) => { self . interior_mut_ty_chain_inner (cx , inner_ty , depth) } , ty :: Tuple (fields) => fields . iter () . find_map (| ty | self . interior_mut_ty_chain_inner (cx , ty , depth)) , ty :: Adt (def , _) if def . is_unsafe_cell () => Some (ty :: List :: empty ()) , ty :: Adt (def , args) => { let is_std_collection = matches ! (cx . tcx . get_diagnostic_name (def . did ()) , Some (sym :: LinkedList | sym :: Vec | sym :: VecDeque | sym :: BTreeMap | sym :: BTreeSet | sym :: HashMap | sym :: HashSet | sym :: Arc | sym :: Rc)) ; if is_std_collection || def . is_box () { args . types () . find_map (| ty | self . interior_mut_ty_chain_inner (cx , ty , depth)) } else if self . ignored_def_ids . contains (& def . did ()) || def . is_phantom_data () { None } else { def . all_fields () . find_map (| f | self . interior_mut_ty_chain_inner (cx , f . ty (cx . tcx , args) , depth)) } } , ty :: Alias (ty :: Projection , _) => match cx . tcx . try_normalize_erasing_regions (cx . typing_env () , ty) { Ok (normalized_ty) if ty != normalized_ty => self . interior_mut_ty_chain_inner (cx , normalized_ty , depth) , _ => None , } , _ => None , } ; chain . map (| chain | { let list = cx . tcx . mk_type_list_from_iter (chain . iter () . chain ([ty])) ; self . tys . insert (ty , Some (list)) ; list }) } # [doc = " Check if given type has interior mutability such as [`std::cell::Cell`] or"] # [doc = " [`std::cell::RefCell`] etc."] pub fn is_interior_mut_ty (& mut self , cx : & LateContext < 'tcx > , ty : Ty < 'tcx >) -> bool { self . interior_mut_ty_chain (cx , ty) . is_some () } }
};
}
