// Generated macro for DeflateDecoder (struct)
macro_rules! Depcrate_deflate_readDeflateDecoder {
() => {
// Module: crate::deflate::read
// Provides: {"DeflateDecoder"}
// Dependencies: {}
# [doc = " A DEFLATE decoder, or decompressor."] # [doc = ""] # [doc = " This structure implements a [`Read`] interface. When read from, it reads"] # [doc = " compressed data from the underlying [`Read`] and provides the uncompressed data."] # [doc = ""] # [doc = " After reading a single member of the DEFLATE data this reader will return"] # [doc = " Ok(0) even if there are more bytes available in the underlying reader."] # [doc = " `DeflateDecoder` may have read additional bytes past the end of the DEFLATE data."] # [doc = " If you need the following bytes, wrap the `Reader` in a `std::io::BufReader`"] # [doc = " and use `bufread::DeflateDecoder` instead."] # [doc = ""] # [doc = " [`Read`]: https://doc.rust-lang.org/std/io/trait.Read.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io::prelude::*;"] # [doc = " use std::io;"] # [doc = " # use flate2::Compression;"] # [doc = " # use flate2::write::DeflateEncoder;"] # [doc = " use flate2::read::DeflateDecoder;"] # [doc = ""] # [doc = " # fn main() {"] # [doc = " #    let mut e = DeflateEncoder::new(Vec::new(), Compression::default());"] # [doc = " #    e.write_all(b\"Hello World\").unwrap();"] # [doc = " #    let bytes = e.finish().unwrap();"] # [doc = " #    println!(\"{}\", decode_reader(bytes).unwrap());"] # [doc = " # }"] # [doc = " // Uncompresses a Deflate Encoded vector of bytes and returns a string or error"] # [doc = " // Here &[u8] implements Read"] # [doc = " fn decode_reader(bytes: Vec<u8>) -> io::Result<String> {"] # [doc = "    let mut deflater = DeflateDecoder::new(&bytes[..]);"] # [doc = "    let mut s = String::new();"] # [doc = "    deflater.read_to_string(&mut s)?;"] # [doc = "    Ok(s)"] # [doc = " }"] # [doc = " ```"] # [derive (Debug)] pub struct DeflateDecoder < R > { inner : bufread :: DeflateDecoder < BufReader < R > > , }
};
}
