// Generated macro for impl_345 (impl)
macro_rules! Depcrate_zlib_writeimpl_345 {
() => {
// Module: crate::zlib::write
// Provides: {"impl_345"}
// Dependencies: {}
impl < W : Write > ZlibEncoder < W > { # [doc = " Creates a new encoder which will write compressed data to the stream"] # [doc = " given at the given compression level."] # [doc = ""] # [doc = " When this encoder is dropped or unwrapped the final pieces of data will"] # [doc = " be flushed."] pub fn new (w : W , level : crate :: Compression) -> ZlibEncoder < W > { ZlibEncoder { inner : zio :: Writer :: new (w , Compress :: new (level , true)) , } } # [doc = " Creates a new encoder which will write compressed data to the stream"] # [doc = " `w` with the given `compression` settings."] pub fn new_with_compress (w : W , compression : Compress) -> ZlibEncoder < W > { ZlibEncoder { inner : zio :: Writer :: new (w , compression) , } } # [doc = " Acquires a reference to the underlying writer."] pub fn get_ref (& self) -> & W { self . inner . get_ref () } # [doc = " Acquires a mutable reference to the underlying writer."] # [doc = ""] # [doc = " Note that mutating the output/input state of the stream may corrupt this"] # [doc = " object, so care must be taken when using this method."] pub fn get_mut (& mut self) -> & mut W { self . inner . get_mut () } # [doc = " Resets the state of this encoder entirely, swapping out the output"] # [doc = " stream for another."] # [doc = ""] # [doc = " This function will finish encoding the current stream into the current"] # [doc = " output stream before swapping out the two output streams."] # [doc = ""] # [doc = " After the current stream has been finished, this will reset the internal"] # [doc = " state of this encoder and replace the output stream with the one"] # [doc = " provided, returning the previous output stream. Future data written to"] # [doc = " this encoder will be the compressed into the stream `w` provided."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will perform I/O to complete this stream, and any I/O"] # [doc = " errors which occur will be returned from this function."] pub fn reset (& mut self , w : W) -> io :: Result < W > { self . inner . finish () ? ; self . inner . data . reset () ; Ok (self . inner . replace (w)) } # [doc = " Attempt to finish this output stream, writing out final chunks of data."] # [doc = ""] # [doc = " Note that this function can only be used once data has finished being"] # [doc = " written to the output stream. After this function is called then further"] # [doc = " calls to `write` may result in a panic."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Attempts to write data to this stream may result in a panic after this"] # [doc = " function is called."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will perform I/O to complete this stream, and any I/O"] # [doc = " errors which occur will be returned from this function."] pub fn try_finish (& mut self) -> io :: Result < () > { self . inner . finish () } # [doc = " Consumes this encoder, flushing the output stream."] # [doc = ""] # [doc = " This will flush the underlying data stream, close off the compressed"] # [doc = " stream and, if successful, return the contained writer."] # [doc = ""] # [doc = " Note that this function may not be suitable to call in a situation where"] # [doc = " the underlying stream is an asynchronous I/O stream. To finish a stream"] # [doc = " the `try_finish` (or `shutdown`) method should be used instead. To"] # [doc = " re-acquire ownership of a stream it is safe to call this method after"] # [doc = " `try_finish` or `shutdown` has returned `Ok`."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will perform I/O to complete this stream, and any I/O"] # [doc = " errors which occur will be returned from this function."] pub fn finish (mut self) -> io :: Result < W > { self . inner . finish () ? ; Ok (self . inner . take_inner ()) } # [doc = " Consumes this encoder, flushing the output stream."] # [doc = ""] # [doc = " This will flush the underlying data stream and then return the contained"] # [doc = " writer if the flush succeeded."] # [doc = " The compressed stream will not be closed but only flushed. This"] # [doc = " means that obtained byte array can by extended by another deflated"] # [doc = " stream. To close the stream add the two bytes 0x3 and 0x0."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will perform I/O to complete this stream, and any I/O"] # [doc = " errors which occur will be returned from this function."] pub fn flush_finish (mut self) -> io :: Result < W > { self . inner . flush () ? ; Ok (self . inner . take_inner ()) } # [doc = " Returns the number of bytes that have been written to this compressor."] # [doc = ""] # [doc = " Note that not all bytes written to this object may be accounted for,"] # [doc = " there may still be some active buffering."] pub fn total_in (& self) -> u64 { self . inner . data . total_in () } # [doc = " Returns the number of bytes that the compressor has produced."] # [doc = ""] # [doc = " Note that not all bytes may have been written yet, some may still be"] # [doc = " buffered."] pub fn total_out (& self) -> u64 { self . inner . data . total_out () } }
};
}
