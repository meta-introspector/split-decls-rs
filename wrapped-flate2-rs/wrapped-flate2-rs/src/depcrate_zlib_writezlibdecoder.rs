// Generated macro for ZlibDecoder (struct)
macro_rules! Depcrate_zlib_writeZlibDecoder {
() => {
// Module: crate::zlib::write
// Provides: {"ZlibDecoder"}
// Dependencies: {}
# [doc = " A ZLIB decoder, or decompressor."] # [doc = ""] # [doc = " This structure implements a [`Write`] and will emit a stream of decompressed"] # [doc = " data when fed a stream of compressed data."] # [doc = ""] # [doc = " After decoding a single member of the ZLIB data this writer will return the number of bytes up"] # [doc = " to the end of the ZLIB member and subsequent writes will return Ok(0) allowing the caller to"] # [doc = " handle any data following the ZLIB member."] # [doc = ""] # [doc = " [`Write`]: https://doc.rust-lang.org/std/io/trait.Write.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io::prelude::*;"] # [doc = " use std::io;"] # [doc = " # use flate2::Compression;"] # [doc = " # use flate2::write::ZlibEncoder;"] # [doc = " use flate2::write::ZlibDecoder;"] # [doc = ""] # [doc = " # fn main() {"] # [doc = " #    let mut e = ZlibEncoder::new(Vec::new(), Compression::default());"] # [doc = " #    e.write_all(b\"Hello World\").unwrap();"] # [doc = " #    let bytes = e.finish().unwrap();"] # [doc = " #    println!(\"{}\", decode_reader(bytes).unwrap());"] # [doc = " # }"] # [doc = " #"] # [doc = " // Uncompresses a Zlib Encoded vector of bytes and returns a string or error"] # [doc = " // Here Vec<u8> implements Write"] # [doc = ""] # [doc = " fn decode_reader(bytes: Vec<u8>) -> io::Result<String> {"] # [doc = "    let mut writer = Vec::new();"] # [doc = "    let mut z = ZlibDecoder::new(writer);"] # [doc = "    z.write_all(&bytes[..])?;"] # [doc = "    writer = z.finish()?;"] # [doc = "    let return_string = String::from_utf8(writer).expect(\"String parsing error\");"] # [doc = "    Ok(return_string)"] # [doc = " }"] # [doc = " ```"] # [derive (Debug)] pub struct ZlibDecoder < W : Write > { inner : zio :: Writer < W , Decompress > , }
};
}
