// Generated macro for impl_276 (impl)
macro_rules! Depcrate_memimpl_276 {
() => {
// Module: crate::mem
// Provides: {"impl_276"}
// Dependencies: {}
impl Decompress { # [doc = " Creates a new object ready for decompressing data that it's given."] # [doc = ""] # [doc = " The `zlib_header` argument indicates whether the input data is expected"] # [doc = " to have a zlib header or not."] pub fn new (zlib_header : bool) -> Decompress { Decompress { inner : Inflate :: make (zlib_header , ffi :: MZ_DEFAULT_WINDOW_BITS as u8) , } } # [doc = " Creates a new object ready for decompressing data that it's given."] # [doc = ""] # [doc = " The `zlib_header` argument indicates whether the input data is expected"] # [doc = " to have a zlib header or not. The `window_bits` parameter indicates the"] # [doc = " base-2 logarithm of the sliding window size and must be between 9 and 15."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If `window_bits` does not fall into the range 9 ..= 15,"] # [doc = " `new_with_window_bits` will panic."] # [cfg (feature = "any_zlib")] pub fn new_with_window_bits (zlib_header : bool , window_bits : u8) -> Decompress { assert ! (window_bits > 8 && window_bits < 16 , "window_bits must be within 9 ..= 15") ; Decompress { inner : Inflate :: make (zlib_header , window_bits) , } } # [doc = " Creates a new object ready for decompressing data that it's given."] # [doc = ""] # [doc = " The Decompress object produced by this constructor expects gzip headers"] # [doc = " for the compressed data."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If `window_bits` does not fall into the range 9 ..= 15,"] # [doc = " `new_with_window_bits` will panic."] # [cfg (feature = "any_zlib")] pub fn new_gzip (window_bits : u8) -> Decompress { assert ! (window_bits > 8 && window_bits < 16 , "window_bits must be within 9 ..= 15") ; Decompress { inner : Inflate :: make (true , window_bits + 16) , } } # [doc = " Returns the total number of input bytes which have been processed by"] # [doc = " this decompression object."] pub fn total_in (& self) -> u64 { self . inner . total_in () } # [doc = " Returns the total number of output bytes which have been produced by"] # [doc = " this decompression object."] pub fn total_out (& self) -> u64 { self . inner . total_out () } # [doc = " Decompresses the input data into the output, consuming only as much"] # [doc = " input as needed and writing as much output as possible."] # [doc = ""] # [doc = " The flush option can be any of the available `FlushDecompress` parameters."] # [doc = ""] # [doc = " If the first call passes `FlushDecompress::Finish` it is assumed that"] # [doc = " the input and output buffers are both sized large enough to decompress"] # [doc = " the entire stream in a single call."] # [doc = ""] # [doc = " A flush value of `FlushDecompress::Finish` indicates that there are no"] # [doc = " more source bytes available beside what's already in the input buffer,"] # [doc = " and the output buffer is large enough to hold the rest of the"] # [doc = " decompressed data."] # [doc = ""] # [doc = " To learn how much data was consumed or how much output was produced, use"] # [doc = " the `total_in` and `total_out` functions before/after this is called."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the input data to this instance of `Decompress` is not a valid"] # [doc = " zlib/deflate stream then this function may return an instance of"] # [doc = " `DecompressError` to indicate that the stream of input bytes is corrupted."] pub fn decompress (& mut self , input : & [u8] , output : & mut [u8] , flush : FlushDecompress ,) -> Result < Status , DecompressError > { self . inner . decompress (input , output , flush) } # [doc = " Similar to [`Self::decompress`] but accepts uninitialized buffer"] # [doc = ""] # [doc = " If you want to avoid the overhead of zero initializing the"] # [doc = " buffer and you don't want to use a [`Vec`], then please use"] # [doc = " this API."] pub fn decompress_uninit (& mut self , input : & [u8] , output : & mut [MaybeUninit < u8 >] , flush : FlushDecompress ,) -> Result < Status , DecompressError > { self . inner . decompress_uninit (input , output , flush) } # [doc = " Decompresses the input data into the extra space in the output vector"] # [doc = " specified by `output`."] # [doc = ""] # [doc = " This function has the same semantics as `decompress`, except that the"] # [doc = " length of `vec` is managed by this function. This will not reallocate"] # [doc = " the vector provided or attempt to grow it, so space for the output must"] # [doc = " be reserved in the output vector by the caller before calling this"] # [doc = " function."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the input data to this instance of `Decompress` is not a valid"] # [doc = " zlib/deflate stream then this function may return an instance of"] # [doc = " `DecompressError` to indicate that the stream of input bytes is corrupted."] pub fn decompress_vec (& mut self , input : & [u8] , output : & mut Vec < u8 > , flush : FlushDecompress ,) -> Result < Status , DecompressError > { unsafe { write_to_spare_capacity_of_vec (output , | out | { let before = self . total_out () ; let ret = self . decompress_uninit (input , out , flush) ; let bytes_written = self . total_out () - before ; (bytes_written as usize , ret) }) } } # [doc = " Specifies the decompression dictionary to use."] # [cfg (feature = "any_zlib")] pub fn set_dictionary (& mut self , dictionary : & [u8]) -> Result < u32 , DecompressError > { let stream = self . inner . inner . stream_wrapper . inner ; let rc = unsafe { (* stream) . msg = std :: ptr :: null_mut () ; assert ! (dictionary . len () < ffi :: uInt :: MAX as usize) ; ffi :: inflateSetDictionary (stream , dictionary . as_ptr () , dictionary . len () as ffi :: uInt) } ; # [allow (clippy :: unnecessary_cast)] match rc { ffi :: MZ_STREAM_ERROR => decompress_failed (self . inner . inner . msg ()) , ffi :: MZ_DATA_ERROR => decompress_need_dict (unsafe { (* stream) . adler } as u32) , ffi :: MZ_OK => Ok (unsafe { (* stream) . adler } as u32) , c => panic ! ("unknown return code: {}" , c) , } } # [doc = " Performs the equivalent of replacing this decompression state with a"] # [doc = " freshly allocated copy."] # [doc = ""] # [doc = " This function may not allocate memory, though, and attempts to reuse any"] # [doc = " previously existing resources."] # [doc = ""] # [doc = " The argument provided here indicates whether the reset state will"] # [doc = " attempt to decode a zlib header first or not."] pub fn reset (& mut self , zlib_header : bool) { self . inner . reset (zlib_header) ; } }
};
}
