// Generated macro for ZlibDecoder (struct)
macro_rules! Depcrate_zlib_readZlibDecoder {
() => {
// Module: crate::zlib::read
// Provides: {"ZlibDecoder"}
// Dependencies: {}
# [doc = " A ZLIB decoder, or decompressor."] # [doc = ""] # [doc = " This structure implements a [`Read`] interface. When read from, it reads"] # [doc = " compressed data from the underlying [`Read`] and provides the uncompressed data."] # [doc = ""] # [doc = " After reading a single member of the ZLIB data this reader will return"] # [doc = " Ok(0) even if there are more bytes available in the underlying reader."] # [doc = " `ZlibDecoder` may have read additional bytes past the end of the ZLIB data."] # [doc = " If you need the following bytes, wrap the `Reader` in a `std::io::BufReader`"] # [doc = " and use `bufread::ZlibDecoder` instead."] # [doc = ""] # [doc = " [`Read`]: https://doc.rust-lang.org/std/io/trait.Read.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io::prelude::*;"] # [doc = " use std::io;"] # [doc = " # use flate2::Compression;"] # [doc = " # use flate2::write::ZlibEncoder;"] # [doc = " use flate2::read::ZlibDecoder;"] # [doc = ""] # [doc = " # fn main() {"] # [doc = " # let mut e = ZlibEncoder::new(Vec::new(), Compression::default());"] # [doc = " # e.write_all(b\"Hello World\").unwrap();"] # [doc = " # let bytes = e.finish().unwrap();"] # [doc = " # println!(\"{}\", decode_reader(bytes).unwrap());"] # [doc = " # }"] # [doc = " #"] # [doc = " // Uncompresses a Zlib Encoded vector of bytes and returns a string or error"] # [doc = " // Here &[u8] implements Read"] # [doc = ""] # [doc = " fn decode_reader(bytes: Vec<u8>) -> io::Result<String> {"] # [doc = "     let mut z = ZlibDecoder::new(&bytes[..]);"] # [doc = "     let mut s = String::new();"] # [doc = "     z.read_to_string(&mut s)?;"] # [doc = "     Ok(s)"] # [doc = " }"] # [doc = " ```"] # [derive (Debug)] pub struct ZlibDecoder < R > { inner : bufread :: ZlibDecoder < BufReader < R > > , }
};
}
