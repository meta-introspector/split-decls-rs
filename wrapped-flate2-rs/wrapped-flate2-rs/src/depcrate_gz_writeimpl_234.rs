// Generated macro for impl_234 (impl)
macro_rules! Depcrate_gz_writeimpl_234 {
() => {
// Module: crate::gz::write
// Provides: {"impl_234"}
// Dependencies: {}
impl < W : Write > GzDecoder < W > { # [doc = " Creates a new decoder which will write uncompressed data to the stream."] # [doc = ""] # [doc = " When this encoder is dropped or unwrapped the final pieces of data will"] # [doc = " be flushed."] pub fn new (w : W) -> GzDecoder < W > { GzDecoder { inner : zio :: Writer :: new (CrcWriter :: new (w) , Decompress :: new (false)) , crc_bytes : Vec :: with_capacity (CRC_BYTES_LEN) , header_parser : GzHeaderParser :: new () , } } # [doc = " Returns the header associated with this stream."] pub fn header (& self) -> Option < & GzHeader > { self . header_parser . header () } # [doc = " Acquires a reference to the underlying writer."] pub fn get_ref (& self) -> & W { self . inner . get_ref () . get_ref () } # [doc = " Acquires a mutable reference to the underlying writer."] # [doc = ""] # [doc = " Note that mutating the output/input state of the stream may corrupt this"] # [doc = " object, so care must be taken when using this method."] pub fn get_mut (& mut self) -> & mut W { self . inner . get_mut () . get_mut () } # [doc = " Attempt to finish this output stream, writing out final chunks of data."] # [doc = ""] # [doc = " Note that this function can only be used once data has finished being"] # [doc = " written to the output stream. After this function is called then further"] # [doc = " calls to `write` may result in a panic."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Attempts to write data to this stream may result in a panic after this"] # [doc = " function is called."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will perform I/O to finish the stream, returning any"] # [doc = " errors which happen."] pub fn try_finish (& mut self) -> io :: Result < () > { self . finish_and_check_crc () ? ; Ok (()) } # [doc = " Consumes this decoder, flushing the output stream."] # [doc = ""] # [doc = " This will flush the underlying data stream and then return the contained"] # [doc = " writer if the flush succeeded."] # [doc = ""] # [doc = " Note that this function may not be suitable to call in a situation where"] # [doc = " the underlying stream is an asynchronous I/O stream. To finish a stream"] # [doc = " the `try_finish` (or `shutdown`) method should be used instead. To"] # [doc = " re-acquire ownership of a stream it is safe to call this method after"] # [doc = " `try_finish` or `shutdown` has returned `Ok`."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will perform I/O to complete this stream, and any I/O"] # [doc = " errors which occur will be returned from this function."] pub fn finish (mut self) -> io :: Result < W > { self . finish_and_check_crc () ? ; Ok (self . inner . take_inner () . into_inner ()) } fn finish_and_check_crc (& mut self) -> io :: Result < () > { self . inner . finish () ? ; if self . crc_bytes . len () != 8 { return Err (corrupt ()) ; } let crc = (self . crc_bytes [0] as u32) | ((self . crc_bytes [1] as u32) << 8) | ((self . crc_bytes [2] as u32) << 16) | ((self . crc_bytes [3] as u32) << 24) ; let amt = (self . crc_bytes [4] as u32) | ((self . crc_bytes [5] as u32) << 8) | ((self . crc_bytes [6] as u32) << 16) | ((self . crc_bytes [7] as u32) << 24) ; if crc != self . inner . get_ref () . crc () . sum () { return Err (corrupt ()) ; } if amt != self . inner . get_ref () . crc () . amount () { return Err (corrupt ()) ; } Ok (()) } }
};
}
