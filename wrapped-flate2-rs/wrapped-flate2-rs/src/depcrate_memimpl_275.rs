// Generated macro for impl_275 (impl)
macro_rules! Depcrate_memimpl_275 {
() => {
// Module: crate::mem
// Provides: {"impl_275"}
// Dependencies: {}
impl Compress { # [doc = " Creates a new object ready for compressing data that it's given."] # [doc = ""] # [doc = " The `level` argument here indicates what level of compression is going"] # [doc = " to be performed, and the `zlib_header` argument indicates whether the"] # [doc = " output data should have a zlib header or not."] pub fn new (level : Compression , zlib_header : bool) -> Compress { Compress { inner : Deflate :: make (level , zlib_header , ffi :: MZ_DEFAULT_WINDOW_BITS as u8) , } } # [doc = " Creates a new object ready for compressing data that it's given."] # [doc = ""] # [doc = " The `level` argument here indicates what level of compression is going"] # [doc = " to be performed, and the `zlib_header` argument indicates whether the"] # [doc = " output data should have a zlib header or not. The `window_bits` parameter"] # [doc = " indicates the base-2 logarithm of the sliding window size and must be"] # [doc = " between 9 and 15."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If `window_bits` does not fall into the range 9 ..= 15,"] # [doc = " `new_with_window_bits` will panic."] # [cfg (feature = "any_zlib")] pub fn new_with_window_bits (level : Compression , zlib_header : bool , window_bits : u8 ,) -> Compress { assert ! (window_bits > 8 && window_bits < 16 , "window_bits must be within 9 ..= 15") ; Compress { inner : Deflate :: make (level , zlib_header , window_bits) , } } # [doc = " Creates a new object ready for compressing data that it's given."] # [doc = ""] # [doc = " The `level` argument here indicates what level of compression is going"] # [doc = " to be performed."] # [doc = ""] # [doc = " The Compress object produced by this constructor outputs gzip headers"] # [doc = " for the compressed data."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If `window_bits` does not fall into the range 9 ..= 15,"] # [doc = " `new_with_window_bits` will panic."] # [cfg (feature = "any_zlib")] pub fn new_gzip (level : Compression , window_bits : u8) -> Compress { assert ! (window_bits > 8 && window_bits < 16 , "window_bits must be within 9 ..= 15") ; Compress { inner : Deflate :: make (level , true , window_bits + 16) , } } # [doc = " Returns the total number of input bytes which have been processed by"] # [doc = " this compression object."] pub fn total_in (& self) -> u64 { self . inner . total_in () } # [doc = " Returns the total number of output bytes which have been produced by"] # [doc = " this compression object."] pub fn total_out (& self) -> u64 { self . inner . total_out () } # [doc = " Specifies the compression dictionary to use."] # [doc = ""] # [doc = " Returns the Adler-32 checksum of the dictionary."] # [cfg (feature = "any_zlib")] pub fn set_dictionary (& mut self , dictionary : & [u8]) -> Result < u32 , CompressError > { let stream = self . inner . inner . stream_wrapper . inner ; let rc = unsafe { (* stream) . msg = std :: ptr :: null_mut () ; assert ! (dictionary . len () < ffi :: uInt :: MAX as usize) ; ffi :: deflateSetDictionary (stream , dictionary . as_ptr () , dictionary . len () as ffi :: uInt) } ; match rc { ffi :: MZ_STREAM_ERROR => compress_failed (self . inner . inner . msg ()) , # [allow (clippy :: unnecessary_cast)] ffi :: MZ_OK => Ok (unsafe { (* stream) . adler } as u32) , c => panic ! ("unknown return code: {}" , c) , } } # [doc = " Quickly resets this compressor without having to reallocate anything."] # [doc = ""] # [doc = " This is equivalent to dropping this object and then creating a new one."] pub fn reset (& mut self) { self . inner . reset () ; } # [doc = " Dynamically updates the compression level."] # [doc = ""] # [doc = " This can be used to switch between compression levels for different"] # [doc = " kinds of data, or it can be used in conjunction with a call to reset"] # [doc = " to reuse the compressor."] # [doc = ""] # [doc = " This may return an error if there wasn't enough output space to complete"] # [doc = " the compression of the available input data before changing the"] # [doc = " compression level. Flushing the stream before calling this method"] # [doc = " ensures that the function will succeed on the first call."] # [cfg (feature = "any_zlib")] pub fn set_level (& mut self , level : Compression) -> Result < () , CompressError > { use std :: os :: raw :: c_int ; let stream = self . inner . inner . stream_wrapper . inner ; unsafe { (* stream) . msg = std :: ptr :: null_mut () ; } let rc = unsafe { ffi :: deflateParams (stream , level . 0 as c_int , ffi :: MZ_DEFAULT_STRATEGY) } ; match rc { ffi :: MZ_OK => Ok (()) , ffi :: MZ_BUF_ERROR => compress_failed (self . inner . inner . msg ()) , c => panic ! ("unknown return code: {}" , c) , } } # [doc = " Compresses the input data into the output, consuming only as much"] # [doc = " input as needed and writing as much output as possible."] # [doc = ""] # [doc = " The flush option can be any of the available `FlushCompress` parameters."] # [doc = ""] # [doc = " To learn how much data was consumed or how much output was produced, use"] # [doc = " the `total_in` and `total_out` functions before/after this is called."] pub fn compress (& mut self , input : & [u8] , output : & mut [u8] , flush : FlushCompress ,) -> Result < Status , CompressError > { self . inner . compress (input , output , flush) } # [doc = " Similar to [`Self::compress`] but accepts uninitialized buffer."] # [doc = ""] # [doc = " If you want to avoid the overhead of zero initializing the"] # [doc = " buffer and you don't want to use a [`Vec`], then please use"] # [doc = " this API."] pub fn compress_uninit (& mut self , input : & [u8] , output : & mut [MaybeUninit < u8 >] , flush : FlushCompress ,) -> Result < Status , CompressError > { self . inner . compress_uninit (input , output , flush) } # [doc = " Compresses the input data into the extra space of the output, consuming"] # [doc = " only as much input as needed and writing as much output as possible."] # [doc = ""] # [doc = " This function has the same semantics as `compress`, except that the"] # [doc = " length of `vec` is managed by this function. This will not reallocate"] # [doc = " the vector provided or attempt to grow it, so space for the output must"] # [doc = " be reserved in the output vector by the caller before calling this"] # [doc = " function."] pub fn compress_vec (& mut self , input : & [u8] , output : & mut Vec < u8 > , flush : FlushCompress ,) -> Result < Status , CompressError > { unsafe { write_to_spare_capacity_of_vec (output , | out | { let before = self . total_out () ; let ret = self . compress_uninit (input , out , flush) ; let bytes_written = self . total_out () - before ; (bytes_written as usize , ret) }) } } }
};
}
