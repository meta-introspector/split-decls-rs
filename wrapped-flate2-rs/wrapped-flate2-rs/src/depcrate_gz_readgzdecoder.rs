// Generated macro for GzDecoder (struct)
macro_rules! Depcrate_gz_readGzDecoder {
() => {
// Module: crate::gz::read
// Provides: {"GzDecoder"}
// Dependencies: {}
# [doc = " A decoder for a single member of a [gzip file]."] # [doc = ""] # [doc = " This structure implements a [`Read`] interface. When read from, it reads"] # [doc = " compressed data from the underlying [`Read`] and provides the uncompressed data."] # [doc = ""] # [doc = " After reading a single member of the gzip data this reader will return"] # [doc = " Ok(0) even if there are more bytes available in the underlying reader."] # [doc = " `GzDecoder` may have read additional bytes past the end of the gzip data."] # [doc = " If you need the following bytes, wrap the `Reader` in a `std::io::BufReader`"] # [doc = " and use `bufread::GzDecoder` instead."] # [doc = ""] # [doc = " To handle gzip files that may have multiple members, see [`MultiGzDecoder`]"] # [doc = " or read more"] # [doc = " [in the introduction](../index.html#about-multi-member-gzip-files)."] # [doc = ""] # [doc = " [gzip file]: https://www.rfc-editor.org/rfc/rfc1952#page-5"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io::prelude::*;"] # [doc = " use std::io;"] # [doc = " # use flate2::Compression;"] # [doc = " # use flate2::write::GzEncoder;"] # [doc = " use flate2::read::GzDecoder;"] # [doc = ""] # [doc = " # fn main() {"] # [doc = " #    let mut e = GzEncoder::new(Vec::new(), Compression::default());"] # [doc = " #    e.write_all(b\"Hello World\").unwrap();"] # [doc = " #    let bytes = e.finish().unwrap();"] # [doc = " #    println!(\"{}\", decode_reader(bytes).unwrap());"] # [doc = " # }"] # [doc = " #"] # [doc = " // Uncompresses a Gz Encoded vector of bytes and returns a string or error"] # [doc = " // Here &[u8] implements Read"] # [doc = ""] # [doc = " fn decode_reader(bytes: Vec<u8>) -> io::Result<String> {"] # [doc = "    let mut gz = GzDecoder::new(&bytes[..]);"] # [doc = "    let mut s = String::new();"] # [doc = "    gz.read_to_string(&mut s)?;"] # [doc = "    Ok(s)"] # [doc = " }"] # [doc = " ```"] # [derive (Debug)] pub struct GzDecoder < R > { inner : bufread :: GzDecoder < BufReader < R > > , }
};
}
