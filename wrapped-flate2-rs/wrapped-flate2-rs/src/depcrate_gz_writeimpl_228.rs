// Generated macro for impl_228 (impl)
macro_rules! Depcrate_gz_writeimpl_228 {
() => {
// Module: crate::gz::write
// Provides: {"impl_228"}
// Dependencies: {}
impl < W : Write > GzEncoder < W > { # [doc = " Creates a new encoder which will use the given compression level."] # [doc = ""] # [doc = " The encoder is not configured specially for the emitted header. For"] # [doc = " header configuration, see the `GzBuilder` type."] # [doc = ""] # [doc = " The data written to the returned encoder will be compressed and then"] # [doc = " written to the stream `w`."] pub fn new (w : W , level : Compression) -> GzEncoder < W > { GzBuilder :: new () . write (w , level) } # [doc = " Acquires a reference to the underlying writer."] pub fn get_ref (& self) -> & W { self . inner . get_ref () } # [doc = " Acquires a mutable reference to the underlying writer."] # [doc = ""] # [doc = " Note that mutation of the writer may result in surprising results if"] # [doc = " this encoder is continued to be used."] pub fn get_mut (& mut self) -> & mut W { self . inner . get_mut () } # [doc = " Attempt to finish this output stream, writing out final chunks of data."] # [doc = ""] # [doc = " Note that this function can only be used once data has finished being"] # [doc = " written to the output stream. After this function is called then further"] # [doc = " calls to `write` may result in a panic."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Attempts to write data to this stream may result in a panic after this"] # [doc = " function is called."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will perform I/O to complete this stream, and any I/O"] # [doc = " errors which occur will be returned from this function."] pub fn try_finish (& mut self) -> io :: Result < () > { self . write_header () ? ; self . inner . finish () ? ; while self . crc_bytes_written < 8 { let (sum , amt) = (self . crc . sum () , self . crc . amount ()) ; let buf = [sum as u8 , (sum >> 8) as u8 , (sum >> 16) as u8 , (sum >> 24) as u8 , amt as u8 , (amt >> 8) as u8 , (amt >> 16) as u8 , (amt >> 24) as u8 ,] ; let inner = self . inner . get_mut () ; let n = inner . write (& buf [self . crc_bytes_written ..]) ? ; self . crc_bytes_written += n ; } Ok (()) } # [doc = " Finish encoding this stream, returning the underlying writer once the"] # [doc = " encoding is done."] # [doc = ""] # [doc = " Note that this function may not be suitable to call in a situation where"] # [doc = " the underlying stream is an asynchronous I/O stream. To finish a stream"] # [doc = " the `try_finish` (or `shutdown`) method should be used instead. To"] # [doc = " re-acquire ownership of a stream it is safe to call this method after"] # [doc = " `try_finish` or `shutdown` has returned `Ok`."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will perform I/O to complete this stream, and any I/O"] # [doc = " errors which occur will be returned from this function."] pub fn finish (mut self) -> io :: Result < W > { self . try_finish () ? ; Ok (self . inner . take_inner ()) } fn write_header (& mut self) -> io :: Result < () > { while ! self . header . is_empty () { let n = self . inner . get_mut () . write (& self . header) ? ; self . header . drain (.. n) ; } Ok (()) } }
};
}
