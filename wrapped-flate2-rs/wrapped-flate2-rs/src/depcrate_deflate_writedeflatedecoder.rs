// Generated macro for DeflateDecoder (struct)
macro_rules! Depcrate_deflate_writeDeflateDecoder {
() => {
// Module: crate::deflate::write
// Provides: {"DeflateDecoder"}
// Dependencies: {}
# [doc = " A DEFLATE decoder, or decompressor."] # [doc = ""] # [doc = " This structure implements a [`Write`] and will emit a stream of decompressed"] # [doc = " data when fed a stream of compressed data."] # [doc = ""] # [doc = " After decoding a single member of the DEFLATE data this writer will return the number of bytes up to"] # [doc = " to the end of the DEFLATE member and subsequent writes will return Ok(0) allowing the caller to"] # [doc = " handle any data following the DEFLATE member."] # [doc = ""] # [doc = " [`Write`]: https://doc.rust-lang.org/std/io/trait.Read.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io::prelude::*;"] # [doc = " use std::io;"] # [doc = " # use flate2::Compression;"] # [doc = " # use flate2::write::DeflateEncoder;"] # [doc = " use flate2::write::DeflateDecoder;"] # [doc = ""] # [doc = " # fn main() {"] # [doc = " #    let mut e = DeflateEncoder::new(Vec::new(), Compression::default());"] # [doc = " #    e.write_all(b\"Hello World\").unwrap();"] # [doc = " #    let bytes = e.finish().unwrap();"] # [doc = " #    println!(\"{}\", decode_writer(bytes).unwrap());"] # [doc = " # }"] # [doc = " // Uncompresses a Deflate Encoded vector of bytes and returns a string or error"] # [doc = " // Here Vec<u8> implements Write"] # [doc = " fn decode_writer(bytes: Vec<u8>) -> io::Result<String> {"] # [doc = "    let mut writer = Vec::new();"] # [doc = "    let mut deflater = DeflateDecoder::new(writer);"] # [doc = "    deflater.write_all(&bytes[..])?;"] # [doc = "    writer = deflater.finish()?;"] # [doc = "    let return_string = String::from_utf8(writer).expect(\"String parsing error\");"] # [doc = "    Ok(return_string)"] # [doc = " }"] # [doc = " ```"] # [derive (Debug)] pub struct DeflateDecoder < W : Write > { inner : zio :: Writer < W , Decompress > , }
};
}
