// Generated macro for GzDecoder (struct)
macro_rules! Depcrate_gz_writeGzDecoder {
() => {
// Module: crate::gz::write
// Provides: {"GzDecoder"}
// Dependencies: {}
# [doc = " A decoder for a single member of a [gzip file]."] # [doc = ""] # [doc = " This structure exposes a [`Write`] interface, receiving compressed data and"] # [doc = " writing uncompressed data to the underlying writer."] # [doc = ""] # [doc = " After decoding a single member of the gzip data this writer will return the number of bytes up to"] # [doc = " to the end of the gzip member and subsequent writes will return Ok(0) allowing the caller to"] # [doc = " handle any data following the gzip member."] # [doc = ""] # [doc = " To handle gzip files that may have multiple members, see [`MultiGzDecoder`]"] # [doc = " or read more"] # [doc = " [in the introduction](../index.html#about-multi-member-gzip-files)."] # [doc = ""] # [doc = " [gzip file]: https://www.rfc-editor.org/rfc/rfc1952#page-5"] # [doc = " [`Write`]: https://doc.rust-lang.org/std/io/trait.Write.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io::prelude::*;"] # [doc = " use std::io;"] # [doc = " use flate2::Compression;"] # [doc = " use flate2::write::{GzEncoder, GzDecoder};"] # [doc = ""] # [doc = " # fn main() {"] # [doc = " #    let mut e = GzEncoder::new(Vec::new(), Compression::default());"] # [doc = " #    e.write(b\"Hello World\").unwrap();"] # [doc = " #    let bytes = e.finish().unwrap();"] # [doc = " #    assert_eq!(\"Hello World\", decode_writer(bytes).unwrap());"] # [doc = " # }"] # [doc = " // Uncompresses a gzip encoded vector of bytes and returns a string or error"] # [doc = " // Here Vec<u8> implements Write"] # [doc = " fn decode_writer(bytes: Vec<u8>) -> io::Result<String> {"] # [doc = "    let mut writer = Vec::new();"] # [doc = "    let mut decoder = GzDecoder::new(writer);"] # [doc = "    decoder.write_all(&bytes[..])?;"] # [doc = "    writer = decoder.finish()?;"] # [doc = "    let return_string = String::from_utf8(writer).expect(\"String parsing error\");"] # [doc = "    Ok(return_string)"] # [doc = " }"] # [doc = " ```"] # [derive (Debug)] pub struct GzDecoder < W : Write > { inner : zio :: Writer < CrcWriter < W > , Decompress > , crc_bytes : Vec < u8 > , header_parser : GzHeaderParser , }
};
}
