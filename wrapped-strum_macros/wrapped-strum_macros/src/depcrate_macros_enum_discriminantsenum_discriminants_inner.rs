// Generated macro for enum_discriminants_inner (function)
macro_rules! Depcrate_macros_enum_discriminantsenum_discriminants_inner {
() => {
// Module: crate::macros::enum_discriminants
// Provides: {"enum_discriminants_inner"}
// Dependencies: {}
pub fn enum_discriminants_inner (ast : & DeriveInput) -> syn :: Result < TokenStream > { let name = & ast . ident ; let vis = & ast . vis ; let variants = match & ast . data { Data :: Enum (v) => & v . variants , _ => return Err (non_enum_error ()) , } ; let type_properties = ast . get_type_properties () ? ; let strum_module_path = type_properties . crate_module_path () ; let derives = type_properties . discriminant_derives ; let derives = quote ! { # [derive (Clone , Copy , Debug , PartialEq , Eq , # (# derives) ,*)] } ; let docs = type_properties . discriminant_docs ; let docs = quote ! { # (# [doc = # docs]) * } ; let default_name = syn :: Ident :: new (& format ! ("{}Discriminants" , name) , Span :: call_site ()) ; let discriminants_name = type_properties . discriminant_name . unwrap_or (default_name) ; let discriminants_vis = type_properties . discriminant_vis . as_ref () . unwrap_or_else (| | & vis) ; let pass_though_attributes = type_properties . discriminant_others ; let repr = type_properties . enum_repr . map (| repr | quote ! (# [repr (# repr)])) ; let mut discriminants = Vec :: new () ; for variant in variants { let ident = & variant . ident ; let discriminant = variant . discriminant . as_ref () . map (| (_ , expr) | quote ! (= # expr)) ; let attrs = variant . attrs . iter () . filter (| attr | { ATTRIBUTES_TO_COPY . iter () . any (| attr_whitelisted | attr . path () . is_ident (attr_whitelisted)) }) . map (| attr | { if attr . path () . is_ident ("strum_discriminants") { let mut ts = attr . meta . require_list () ? . to_token_stream () . into_iter () ; let _ = ts . next () ; let passthrough_group = ts . next () . ok_or_else (| | strum_discriminants_passthrough_error (attr)) ? ; let passthrough_attribute = match passthrough_group { TokenTree :: Group (ref group) => group . stream () , _ => { return Err (strum_discriminants_passthrough_error (& passthrough_group)) ; } } ; if passthrough_attribute . is_empty () { return Err (strum_discriminants_passthrough_error (& passthrough_group)) ; } Ok (quote ! { # [# passthrough_attribute] }) } else { Ok (attr . to_token_stream ()) } }) . collect :: < Result < Vec < _ > , _ > > () ? ; discriminants . push (quote ! { # (# attrs) * # ident # discriminant }) ; } let arms = variants . iter () . map (| variant | { let ident = & variant . ident ; let params = match & variant . fields { Fields :: Unit => quote ! { } , Fields :: Unnamed (_fields) => { quote ! { (..) } } Fields :: Named (_fields) => { quote ! { { .. } } } } ; quote ! { # name ::# ident # params => # discriminants_name ::# ident } }) . collect :: < Vec < _ > > () ; let from_fn_body = if variants . is_empty () { quote ! { unreachable ! () } } else { quote ! { match val { # (# arms) ,* } } } ; let (impl_generics , ty_generics , where_clause) = ast . generics . split_for_impl () ; let impl_from = quote ! { # [automatically_derived] impl # impl_generics :: core :: convert :: From < # name # ty_generics > for # discriminants_name # where_clause { # [inline] fn from (val : # name # ty_generics) -> # discriminants_name { # from_fn_body } } } ; let impl_from_ref = { let mut generics = ast . generics . clone () ; let lifetime = parse_quote ! ('_enum) ; let enum_life = quote ! { & # lifetime } ; generics . params . push (lifetime) ; let (impl_generics , _ , _) = generics . split_for_impl () ; quote ! { # [automatically_derived] impl # impl_generics :: core :: convert :: From < # enum_life # name # ty_generics > for # discriminants_name # where_clause { # [inline] fn from (val : # enum_life # name # ty_generics) -> # discriminants_name { # from_fn_body } } } } ; let impl_into_discriminant = match type_properties . discriminant_vis { None | Some (syn :: Visibility :: Public (..)) => quote ! { # [automatically_derived] impl # impl_generics # strum_module_path :: IntoDiscriminant for # name # ty_generics # where_clause { type Discriminant = # discriminants_name ; # [inline] fn discriminant (& self) -> Self :: Discriminant { < Self :: Discriminant as :: core :: convert :: From <& Self >>:: from (self) } } } , _ => quote ! { } , } ; Ok (quote ! { # docs # derives # repr # (# [# pass_though_attributes]) * # discriminants_vis enum # discriminants_name { # (# discriminants) ,* } # impl_into_discriminant # impl_from # impl_from_ref }) }
};
}
