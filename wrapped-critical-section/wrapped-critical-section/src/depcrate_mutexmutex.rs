// Generated macro for Mutex (struct)
macro_rules! Depcrate_mutexMutex {
() => {
// Module: crate::mutex
// Provides: {"Mutex"}
// Dependencies: {}
# [doc = " A mutex based on critical sections."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```no_run"] # [doc = " # use critical_section::Mutex;"] # [doc = " # use std::cell::Cell;"] # [doc = ""] # [doc = " static FOO: Mutex<Cell<i32>> = Mutex::new(Cell::new(42));"] # [doc = ""] # [doc = " fn main() {"] # [doc = "     critical_section::with(|cs| {"] # [doc = "         FOO.borrow(cs).set(43);"] # [doc = "     });"] # [doc = " }"] # [doc = ""] # [doc = " fn interrupt_handler() {"] # [doc = "     let _x = critical_section::with(|cs| FOO.borrow(cs).get());"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = ""] # [doc = " # Design"] # [doc = ""] # [doc = " [`std::sync::Mutex`] has two purposes. It converts types that are [`Send`]"] # [doc = " but not [`Sync`] into types that are both; and it provides"] # [doc = " [interior mutability]. `critical_section::Mutex`, on the other hand, only adds"] # [doc = " `Sync`. It does *not* provide interior mutability."] # [doc = ""] # [doc = " This was a conscious design choice. It is possible to create multiple"] # [doc = " [`CriticalSection`] tokens, either by nesting critical sections or `Copy`ing"] # [doc = " an existing token. As a result, it would not be sound for [`Mutex::borrow`]"] # [doc = " to return `&mut T`, because there would be nothing to prevent calling"] # [doc = " `borrow` multiple times to create aliased `&mut T` references."] # [doc = ""] # [doc = " The solution is to include a runtime check to ensure that each resource is"] # [doc = " borrowed only once. This is what `std::sync::Mutex` does. However, this is"] # [doc = " a runtime cost that may not be required in all circumstances. For instance,"] # [doc = " `Mutex<Cell<T>>` never needs to create `&mut T` or equivalent."] # [doc = ""] # [doc = " If `&mut T` is needed, the simplest solution is to use `Mutex<RefCell<T>>`,"] # [doc = " which is the closest analogy to `std::sync::Mutex`. [`RefCell`] inserts the"] # [doc = " exact runtime check necessary to guarantee that the `&mut T` reference is"] # [doc = " unique."] # [doc = ""] # [doc = " To reduce verbosity when using `Mutex<RefCell<T>>`, we reimplement some of"] # [doc = " `RefCell`'s methods on it directly."] # [doc = ""] # [doc = " ```no_run"] # [doc = " # use critical_section::Mutex;"] # [doc = " # use std::cell::RefCell;"] # [doc = ""] # [doc = " static FOO: Mutex<RefCell<i32>> = Mutex::new(RefCell::new(42));"] # [doc = ""] # [doc = " fn main() {"] # [doc = "     critical_section::with(|cs| {"] # [doc = "         // Instead of calling this"] # [doc = "         let _ = FOO.borrow(cs).take();"] # [doc = "         // Call this"] # [doc = "         let _ = FOO.take(cs);"] # [doc = "         // `RefCell::borrow` and `RefCell::borrow_mut` are renamed to"] # [doc = "         // `borrow_ref` and `borrow_ref_mut` to avoid name collisions"] # [doc = "         let _: &mut i32 = &mut *FOO.borrow_ref_mut(cs);"] # [doc = "     })"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [`std::sync::Mutex`]: https://doc.rust-lang.org/std/sync/struct.Mutex.html"] # [doc = " [interior mutability]: https://doc.rust-lang.org/reference/interior-mutability.html"] # [derive (Debug)] pub struct Mutex < T > { inner : UnsafeCell < T > , }
};
}
