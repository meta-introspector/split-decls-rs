// Generated macro for impl_2682 (impl)
macro_rules! Depcrate_urlimpl_2682 {
() => {
// Module: crate::url
// Provides: {"impl_2682"}
// Dependencies: {}
# [doc = " [`Path`] conversion."] impl NSURL { pub fn from_path (path : & Path , is_directory : bool , base_url : Option < & NSURL > ,) -> Option < Retained < Self > > { let bytes = path . as_os_str () . as_bytes () ; if bytes . is_empty () { return None ; } let cstr = CString :: new (bytes) . ok () ? ; let ptr = NonNull :: new (cstr . as_ptr () . cast_mut ()) . unwrap () ; Some (unsafe { Self :: initFileURLWithFileSystemRepresentation_isDirectory_relativeToURL (Self :: alloc () , ptr , is_directory , base_url ,) }) } # [doc = " Create a file url from a [`Path`]."] # [doc = ""] # [doc = " If the path is relative, it will be considered relative to the current"] # [doc = " directory."] # [doc = ""] # [doc = " Returns `None` when given an invalid path (such as a path containing"] # [doc = " interior NUL bytes). The exact checks are not guaranteed."] # [doc = ""] # [doc = ""] # [doc = " # Non-unicode and HFS+ support"] # [doc = ""] # [doc = " Modern Apple disk drives use APFS nowadays, which forces all paths to"] # [doc = " be valid unicode. The URL standard also uses unicode, and non-unicode"] # [doc = " parts of the URL will be percent-encoded, and the url will be given"] # [doc = " the scheme `file://`. All of this is as it should be."] # [doc = ""] # [doc = " Unfortunately, a lot of Foundation APIs (including the `NSFileManager`"] # [doc = " and `NSData` APIs) currently assume that they can always get unicode"] # [doc = " paths _back_ by calling [`NSURL::path`] internally, which is not true."] # [doc = ""] # [doc = " If you need to support non-unicode paths in HFS+ with these APIs, you"] # [doc = " can work around this issue by percent-encoding any non-unicode parts"] # [doc = " of the path yourself beforehand, similar to [what's done in the"] # [doc = " `trash-rs` crate](https://github.com/Byron/trash-rs/pull/127)."] # [doc = " (this function cannot do that for you, since it relies on a quirk of"] # [doc = " HFS+ that b\"\\xf8\" and b\"%F8\" refer to the same file)."] # [doc = ""] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::path::Path;"] # [doc = " use objc2_foundation::NSURL;"] # [doc = ""] # [doc = " // Absolute paths work as you'd expect."] # [doc = " let url = NSURL::from_file_path(\"/tmp/file.txt\").unwrap();"] # [doc = " assert_eq!(url.to_file_path().unwrap(), Path::new(\"/tmp/file.txt\"));"] # [doc = ""] # [doc = " // Relative paths are relative to the current directory."] # [doc = " let url = NSURL::from_file_path(\"foo.txt\").unwrap();"] # [doc = " assert_eq!(url.to_file_path().unwrap(), std::env::current_dir().unwrap().join(\"foo.txt\"));"] # [doc = ""] # [doc = " // Some invalid paths return `None`."] # [doc = " assert!(NSURL::from_file_path(\"\").is_none());"] # [doc = " // Another example of an invalid path containing interior NUL bytes."] # [doc = " assert!(NSURL::from_file_path(\"/a/\\0a\").is_none());"] # [doc = " ```"] # [inline] # [doc (alias = "fileURLWithFileSystemRepresentation:isDirectory:relativeToURL:")] # [doc (alias = "initFileURLWithFileSystemRepresentation:isDirectory:relativeToURL:")] pub fn from_file_path < P : AsRef < Path > > (path : P) -> Option < Retained < Self > > { Self :: from_path (path . as_ref () , false , None) } # [doc = " Create a directory url from a [`Path`]."] # [doc = ""] # [doc = " This differs from [`from_file_path`][Self::from_file_path] in that the"] # [doc = " path is treated as a directory, which means that other normalization"] # [doc = " rules are applied to it (to make it end with a `/`)."] # [doc = ""] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::path::Path;"] # [doc = " use objc2_foundation::NSURL;"] # [doc = ""] # [doc = " // Directory paths get trailing slashes appended"] # [doc = " let url = NSURL::from_directory_path(\"/Library\").unwrap();"] # [doc = " assert_eq!(url.to_file_path().unwrap(), Path::new(\"/Library/\"));"] # [doc = ""] # [doc = " // Unless they already have them."] # [doc = " let url = NSURL::from_directory_path(\"/Library/\").unwrap();"] # [doc = " assert_eq!(url.to_file_path().unwrap(), Path::new(\"/Library/\"));"] # [doc = ""] # [doc = " // Similarly for relative paths."] # [doc = " let url = NSURL::from_directory_path(\"foo\").unwrap();"] # [doc = " assert_eq!(url.to_file_path().unwrap(), std::env::current_dir().unwrap().join(\"foo/\"));"] # [doc = ""] # [doc = " // Various dots may be stripped."] # [doc = " let url = NSURL::from_directory_path(\"/Library/././.\").unwrap();"] # [doc = " assert_eq!(url.to_file_path().unwrap(), Path::new(\"/Library/\"));"] # [doc = ""] # [doc = " // Though of course not if they have semantic meaning."] # [doc = " let url = NSURL::from_directory_path(\"/Library/..\").unwrap();"] # [doc = " assert_eq!(url.to_file_path().unwrap(), Path::new(\"/Library/..\"));"] # [doc = " ```"] # [inline] # [doc (alias = "fileURLWithFileSystemRepresentation:isDirectory:relativeToURL:")] # [doc (alias = "initFileURLWithFileSystemRepresentation:isDirectory:relativeToURL:")] pub fn from_directory_path < P : AsRef < Path > > (path : P) -> Option < Retained < Self > > { Self :: from_path (path . as_ref () , true , None) } # [doc = " Extract the path part of the URL as a `PathBuf`."] # [doc = ""] # [doc = " This will return a path regardless of [`isFileURL`][Self::isFileURL]."] # [doc = " It is the responsibility of the caller to ensure that the URL is valid"] # [doc = " to use as a file URL."] # [doc = ""] # [doc = ""] # [doc = " # Compatibility note"] # [doc = ""] # [doc = " This currently does not work for non-unicode paths (which are fairly"] # [doc = " rare on macOS since HFS+ was been superseded by APFS)."] # [doc = ""] # [doc = " This also currently always returns absolute paths (it converts"] # [doc = " relative URL paths to absolute), but that may change in the future."] # [doc = ""] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::path::Path;"] # [doc = " use objc2_foundation::{NSURL, NSString};"] # [doc = ""] # [doc = " let url = unsafe { NSURL::URLWithString(&NSString::from_str(\"file:///tmp/foo.txt\")).unwrap() };"] # [doc = " assert_eq!(url.to_file_path().unwrap(), Path::new(\"/tmp/foo.txt\"));"] # [doc = " ```"] # [doc = ""] # [doc = " See also the examples in [`from_file_path`][Self::from_file_path]."] # [doc (alias = "getFileSystemRepresentation:maxLength:")] # [doc (alias = "fileSystemRepresentation")] pub fn to_file_path (& self) -> Option < PathBuf > { let mut buf = [0u8 ; PATH_MAX] ; let ptr = NonNull :: new (buf . as_mut_ptr ()) . unwrap () . cast () ; let result = unsafe { self . getFileSystemRepresentation_maxLength (ptr , buf . len ()) } ; if ! result { return None ; } let cstr = unsafe { CStr :: from_bytes_until_nul (& buf) . unwrap_unchecked () } ; let path = OsStr :: from_bytes (cstr . to_bytes ()) ; Some (PathBuf :: from (path)) } }
};
}
