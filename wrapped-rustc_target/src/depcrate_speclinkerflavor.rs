// Generated macro for LinkerFlavor (enum)
macro_rules! Depcrate_specLinkerFlavor {
() => {
// Module: crate::spec
// Provides: {"LinkerFlavor"}
// Dependencies: {}
# [doc = " All linkers have some kinds of command line interfaces and rustc needs to know which commands"] # [doc = " to use with each of them. So we cluster all such interfaces into a (somewhat arbitrary) number"] # [doc = " of classes that we call \"linker flavors\"."] # [doc = ""] # [doc = " Technically, it's not even necessary, we can nearly always infer the flavor from linker name"] # [doc = " and target properties like `is_like_windows`/`is_like_darwin`/etc. However, the PRs originally"] # [doc = " introducing `-Clinker-flavor` (#40018 and friends) were aiming to reduce this kind of inference"] # [doc = " and provide something certain and explicitly specified instead, and that design goal is still"] # [doc = " relevant now."] # [doc = ""] # [doc = " The second goal is to keep the number of flavors to the minimum if possible."] # [doc = " LLD somewhat forces our hand here because that linker is self-sufficient only if its executable"] # [doc = " (`argv[0]`) is named in specific way, otherwise it doesn't work and requires a"] # [doc = " `-flavor LLD_FLAVOR` argument to choose which logic to use. Our shipped `rust-lld` in"] # [doc = " particular is not named in such specific way, so it needs the flavor option, so we make our"] # [doc = " linker flavors sufficiently fine-grained to satisfy LLD without inferring its flavor from other"] # [doc = " target properties, in accordance with the first design goal."] # [doc = ""] # [doc = " The first component of the flavor is tightly coupled with the compilation target,"] # [doc = " while the `Cc` and `Lld` flags can vary within the same target."] # [derive (Clone , Copy , Debug , Eq , Ord , PartialEq , PartialOrd)] pub enum LinkerFlavor { # [doc = " Unix-like linker with GNU extensions (both naked and compiler-wrapped forms)."] # [doc = " Besides similar \"default\" Linux/BSD linkers this also includes Windows/GNU linker,"] # [doc = " which is somewhat different because it doesn't produce ELFs."] Gnu (Cc , Lld) , # [doc = " Unix-like linker for Apple targets (both naked and compiler-wrapped forms)."] # [doc = " Extracted from the \"umbrella\" `Unix` flavor due to its corresponding LLD flavor."] Darwin (Cc , Lld) , # [doc = " Unix-like linker for Wasm targets (both naked and compiler-wrapped forms)."] # [doc = " Extracted from the \"umbrella\" `Unix` flavor due to its corresponding LLD flavor."] # [doc = " Non-LLD version does not exist, so the lld flag is currently hardcoded here."] WasmLld (Cc) , # [doc = " Basic Unix-like linker for \"any other Unix\" targets (Solaris/illumos, L4Re, MSP430, etc),"] # [doc = " possibly with non-GNU extensions (both naked and compiler-wrapped forms)."] # [doc = " LLD doesn't support any of these."] Unix (Cc) , # [doc = " MSVC-style linker for Windows and UEFI, LLD supports it."] Msvc (Lld) , # [doc = " Emscripten Compiler Frontend, a wrapper around `WasmLld(Cc::Yes)` that has a different"] # [doc = " interface and produces some additional JavaScript output."] EmCc , # [doc = " Linker tool for BPF."] Bpf , # [doc = " Linker tool for Nvidia PTX."] Ptx , # [doc = " LLVM bitcode linker that can be used as a `self-contained` linker"] Llbc , }
};
}
