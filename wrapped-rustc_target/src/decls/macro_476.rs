macro_rules! macro_476 {
    () => {
        bitflags :: bitflags ! { # [doc = " The `-C linker-features` components that can individually be enabled or disabled."] # [doc = ""] # [doc = " They are feature flags intended to be a more flexible mechanism than linker flavors, and"] # [doc = " also to prevent a combinatorial explosion of flavors whenever a new linker feature is"] # [doc = " required. These flags are \"generic\", in the sense that they can work on multiple targets on"] # [doc = " the CLI. Otherwise, one would have to select different linkers flavors for each target."] # [doc = ""] # [doc = " Here are some examples of the advantages they offer:"] # [doc = " - default feature sets for principal flavors, or for specific targets."] # [doc = " - flavor-specific features: for example, clang offers automatic cross-linking with"] # [doc = "   `--target`, which gcc-style compilers don't support. The *flavor* is still a C/C++"] # [doc = "   compiler, and we don't need to multiply the number of flavors for this use-case. Instead,"] # [doc = "   we can have a single `+target` feature."] # [doc = " - umbrella features: for example if clang accumulates more features in the future than just"] # [doc = "   the `+target` above. That could be modeled as `+clang`."] # [doc = " - niche features for resolving specific issues: for example, on Apple targets the linker"] # [doc = "   flag implementing the `as-needed` native link modifier (#99424) is only possible on"] # [doc = "   sufficiently recent linker versions."] # [doc = " - still allows for discovery and automation, for example via feature detection. This can be"] # [doc = "   useful in exotic environments/build systems."] # [derive (Clone , Copy , PartialEq , Eq , Default)] pub struct LinkerFeatures : u8 { # [doc = " Invoke the linker via a C/C++ compiler (e.g. on most unix targets)."] const CC = 1 << 0 ; # [doc = " Use the lld linker, either the system lld or the self-contained linker `rust-lld`."] const LLD = 1 << 1 ; } }
    };
}

macro_476!();