// Generated by add_generated_rust_header! macro at submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs:72 from submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs
// Do not edit this file by hand. Your changes will be overwritten.

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
#[macro_export]
macro_rules ! target_spec_enum { ($ (# [$ attr : meta]) * pub enum $ Name : ident { $ ($ (# [$ variant_attr : meta]) * $ Variant : ident = $ string : literal ,) * } $ (# [$ other_variant_attr : meta]) * other_variant = $ OtherVariant : ident ;) => { $ (# [$ attr]) * # [derive (Clone , PartialEq , Eq , Hash , Debug , PartialOrd , Ord , serde :: Deserialize)] pub enum $ Name { $ ($ (# [$ variant_attr : meta]) * $ Variant ,) * # [doc = " The vast majority of the time, the compiler deals with a fixed"] # [doc = " set of values, so it is convenient for them to be represented in"] # [doc = " an enum. However, it is possible to have arbitrary values in a"] # [doc = " target JSON file (which can be parsed when `--target` is"] # [doc = " specified). This might occur, for example, for an out-of-tree"] # [doc = " codegen backend that supports a value (e.g. architecture or OS)"] # [doc = " that rustc currently doesn't know about. This variant exists as"] # [doc = " an escape hatch for such cases."] $ (# [$ other_variant_attr]) * $ OtherVariant (crate :: spec :: StaticCow < str >) , } impl schemars :: JsonSchema for $ Name { fn schema_name () -> std :: borrow :: Cow <'static , str > { std :: borrow :: Cow :: Borrowed (stringify ! ($ Name)) } fn json_schema (_ : & mut schemars :: SchemaGenerator) -> schemars :: Schema { schemars :: json_schema ! ({ "type" : "string" }) } } impl FromStr for $ Name { type Err = core :: convert :: Infallible ; fn from_str (s : & str) -> Result < Self , Self :: Err > { Ok (match s { $ ($ string => Self ::$ Variant ,) * _ => Self ::$ OtherVariant (s . to_owned () . into ()) , }) } } impl $ Name { pub fn desc (& self) -> & str { match self { $ (Self ::$ Variant => $ string ,) * Self ::$ OtherVariant (name) => name . as_ref () , } } } impl std :: fmt :: Display for $ Name { fn fmt (& self , f : & mut std :: fmt :: Formatter <'_ >) -> std :: fmt :: Result { f . write_str (self . desc ()) } } } ; ($ (# [$ attr : meta]) * pub enum $ Name : ident { $ ($ (# [$ variant_attr : meta]) * $ Variant : ident = $ string : literal ,) * } parse_error_type = $ parse_error_type : literal ;) => { $ (# [$ attr]) * # [derive (Clone , Copy , PartialEq , Eq , Hash , Debug , PartialOrd , Ord , serde :: Deserialize)] # [derive (schemars :: JsonSchema)] pub enum $ Name { $ ($ (# [$ variant_attr]) * # [serde (rename = $ string)] $ Variant ,) * } impl FromStr for $ Name { type Err = String ; fn from_str (s : & str) -> Result < Self , Self :: Err > { Ok (match s { $ ($ string => Self ::$ Variant ,) * _ => { let all = [$ (concat ! ("'" , $ string , "'")) ,*] . join (", ") ; return Err (format ! ("invalid {}: '{s}'. allowed values: {all}" , $ parse_error_type)) ; } }) } } impl $ Name { pub fn desc (& self) -> & str { match self { $ (Self ::$ Variant => $ string ,) * } } } impl std :: fmt :: Display for $ Name { fn fmt (& self , f : & mut std :: fmt :: Formatter <'_ >) -> std :: fmt :: Result { f . write_str (self . desc ()) } } } ; (@ common_impls $ Name : ident) => { impl crate :: json :: ToJson for $ Name { fn to_json (& self) -> crate :: json :: Json { self . desc () . to_json () } } serde_deserialize_from_str ! ($ Name) ; impl std :: fmt :: Display for $ Name { fn fmt (& self , f : & mut std :: fmt :: Formatter <'_ >) -> std :: fmt :: Result { f . write_str (self . desc ()) } } } }
