// Generated macro for impl_10 (impl)
macro_rules! Depcrate_analysisimpl_10 {
() => {
// Module: crate::analysis
// Provides: {"impl_10"}
// Dependencies: {}
impl ProfilingData { # [doc = " Collects accumulated summary data for the given ProfilingData."] # [doc = ""] # [doc = " The main result we are interested in is the query \"self-time\". This is the"] # [doc = " time spent computing the result of a query `q` minus the time spent in"] # [doc = " any other queries that `q` might have called. This \"self-time\" can be"] # [doc = " computed by looking at invocation stacks as follows:"] # [doc = ""] # [doc = " When we encounter a query provider event, we first add its entire duration"] # [doc = " to the self-time counter of the query. Then, when we encounter a direct"] # [doc = " child of that query provider event, we subtract the duration of the child"] # [doc = " from the self-time counter of the query. Thus, after we've encountered all"] # [doc = " direct children we'll end up with the self-time."] # [doc = ""] # [doc = " For example, take the following query invocation trace:"] # [doc = " ```ignore"] # [doc = "                                      <== q4 ==>"] # [doc = "           <== q2 ==>           <====== q3 ======>"] # [doc = "     <===================== q1 =====================>"] # [doc = " -------------------------------------------------------> time"] # [doc = " ```"] # [doc = ""] # [doc = " Query `q1` calls `q2` and later `q3`, which in turn calls `q4`. In order"] # [doc = " to get the self-time of `q1`, we take it's entire duration and subtract the"] # [doc = " durations of `q2` and `q3`. We do not subtract the duration of `q4` because"] # [doc = " that is already accounted for by the duration of `q3`."] # [doc = ""] # [doc = " The function below uses an algorithm that computes the self-times of all"] # [doc = " queries in a single pass over the profiling data. As the algorithm walks the"] # [doc = " data, it needs to keep track of invocation stacks. Because interval events"] # [doc = " occur in the stream at their \"end\" time, a parent event comes after all"] # [doc = " child events. For this reason we have to walk the events in *reverse order*."] # [doc = " This way we always encounter the parent before its children, which makes it"] # [doc = " simple to keep an up-to-date stack of invocations."] # [doc = ""] # [doc = " The algorithm goes as follows:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " for event in profiling_data.reversed()"] # [doc = "    // Keep the stack up-to-date by popping all events that"] # [doc = "    // don't contain the current event. After this loop, the"] # [doc = "    // parent of the current event will be the top of the stack."] # [doc = "    while !stack.top.is_ancestor_of(event)"] # [doc = "        stack.pop()"] # [doc = ""] # [doc = "    // Update the parents self-time if needed"] # [doc = "    let parent = stack.top()"] # [doc = "    if parent.is_some()"] # [doc = "        self_time_for(parent) -= event.duration"] # [doc = ""] # [doc = "    // Update the self-time for the current-event"] # [doc = "    self_time_for(event) += event.duration"] # [doc = ""] # [doc = "    // Push the current event onto the stack"] # [doc = "    stack.push(event)"] # [doc = " ```"] # [doc = ""] # [doc = " Here is an example of what updating the stack looks like:"] # [doc = ""] # [doc = " ```ignore"] # [doc = "      <--e2-->   <--e3-->"] # [doc = "  <-----------e1----------->"] # [doc = " ```"] # [doc = ""] # [doc = " In the event stream this shows up as something like:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " ["] # [doc = "     { label=e2, start= 5, end=10 },"] # [doc = "     { label=e3, start=15, end=20 },"] # [doc = "     { label=e1, start= 0, end=25 },"] # [doc = " ]"] # [doc = " ```"] # [doc = ""] # [doc = " because events are emitted in the order of their end timestamps. So, as we"] # [doc = " walk backwards, we"] # [doc = ""] # [doc = " 1. encounter `e1`, push it onto the stack, then"] # [doc = " 2. encounter `e3`, the stack contains `e1`, but that is fine since the"] # [doc = "    time-interval of `e1` includes the time interval of `e3`. `e3` goes onto"] # [doc = "    the stack and then we"] # [doc = " 3. encounter `e2`. The stack is `[e1, e3]`, but now `e3` needs to be popped"] # [doc = "    because we are past its range, so we pop `e3` and push `e2`."] # [doc = ""] # [doc = " Why is popping done in a `while` loop? consider the following"] # [doc = ""] # [doc = " ```ignore"] # [doc = "                  <-e4->"] # [doc = "      <--e2-->   <--e3-->"] # [doc = "  <-----------e1----------->"] # [doc = " ```"] # [doc = ""] # [doc = " This looks as follows in the stream:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " ["] # [doc = "     { label=e2, start= 5, end=10 },"] # [doc = "     { label=e4, start=17, end=19 },"] # [doc = "     { label=e3, start=15, end=20 },"] # [doc = "     { label=e1, start= 0, end=25 },"] # [doc = " ]"] # [doc = " ```"] # [doc = ""] # [doc = " In this case when we encounter `e2`, the stack is `[e1, e3, e4]`, and both"] # [doc = " `e4` and `e3` need to be popped in the same step."] pub fn perform_analysis (self) -> AnalysisResults { struct PerThreadState < 'a > { stack : Vec < Event < 'a > > , start : SystemTime , end : SystemTime , } let mut query_data = FxHashMap :: < String , QueryData > :: default () ; let mut artifact_sizes = BTreeMap :: < Cow < '_ , str > , ArtifactSize > :: default () ; let mut threads = FxHashMap :: < _ , PerThreadState < '_ > > :: default () ; let mut record_event_data = | label : & Cow < '_ , str > , f : & dyn Fn (& mut QueryData) | { if let Some (data) = query_data . get_mut (& label [..]) { f (data) ; } else { let mut data = QueryData :: new (label . clone () . into_owned ()) ; f (& mut data) ; query_data . insert (label . clone () . into_owned () , data) ; } } ; let mut query_cache_hit_counts_found : FxHashSet < String > = Default :: default () ; for current_event in self . iter_full () . rev () { match current_event . payload { EventPayload :: Timestamp (Timestamp :: Instant (_)) => { if & current_event . event_kind [..] == QUERY_CACHE_HIT_EVENT_KIND { record_event_data (& current_event . label , & | data | { if ! query_cache_hit_counts_found . contains (current_event . label . as_ref ()) { data . number_of_cache_hits += 1 ; } }) ; } } EventPayload :: Timestamp (Timestamp :: Interval { start , end }) => { let thread = threads . entry (current_event . thread_id) . or_insert_with (| | PerThreadState { stack : Vec :: new () , start , end , }) ; while let Some (current_top) = thread . stack . last () . cloned () { if current_top . contains (& current_event) { break ; } thread . stack . pop () ; } let current_event_duration = current_event . duration () . unwrap () ; if let Some (current_top) = thread . stack . last () { record_event_data (& current_top . label , & | data | match & current_top . event_kind [..] { QUERY_EVENT_KIND | GENERIC_ACTIVITY_EVENT_KIND => { data . self_time -= current_event_duration ; } INCREMENTAL_RESULT_HASHING_EVENT_KIND => { data . self_time -= current_event_duration ; data . incremental_hashing_time -= current_event_duration ; } INCREMENTAL_LOAD_RESULT_EVENT_KIND => { data . self_time -= current_event_duration ; data . incremental_load_time -= current_event_duration ; } _ => { data . self_time -= current_event_duration ; } } ,) ; } match & current_event . event_kind [..] { QUERY_EVENT_KIND | GENERIC_ACTIVITY_EVENT_KIND => { record_event_data (& current_event . label , & | data | { data . self_time += current_event_duration ; data . time += current_event_duration ; data . number_of_cache_misses += 1 ; data . invocation_count += 1 ; }) ; } QUERY_BLOCKED_EVENT_KIND => { record_event_data (& current_event . label , & | data | { data . self_time += current_event_duration ; data . time += current_event_duration ; data . blocked_time += current_event_duration ; }) ; } INCREMENTAL_LOAD_RESULT_EVENT_KIND => { record_event_data (& current_event . label , & | data | { data . self_time += current_event_duration ; data . time += current_event_duration ; data . incremental_load_time += current_event_duration ; }) ; } INCREMENTAL_RESULT_HASHING_EVENT_KIND => { record_event_data (& current_event . label , & | data | { data . self_time += current_event_duration ; data . incremental_hashing_time += current_event_duration ; }) ; } _ => { record_event_data (& current_event . label , & | data | { data . self_time += current_event_duration ; data . time += current_event_duration ; data . invocation_count += 1 ; }) ; } } ; thread . start = std :: cmp :: min (thread . start , start) ; thread . end = std :: cmp :: max (thread . end , end) ; thread . stack . push (current_event) } EventPayload :: Integer (value) => { match current_event . event_kind . as_ref () { ARTIFACT_SIZE_EVENT_KIND => { artifact_sizes . entry (current_event . label . clone ()) . or_insert_with (| | ArtifactSize :: new (current_event . label . into_owned ())) . add_value (value) ; } QUERY_CACHE_HIT_COUNT_EVENT_KIND => { record_event_data (& current_event . label , & | data | { data . number_of_cache_hits += value as usize ; }) ; query_cache_hit_counts_found . insert (current_event . label . into_owned ()) ; } _ => { } } } } } let total_time = threads . values () . map (| t | t . end . duration_since (t . start) . unwrap ()) . sum () ; AnalysisResults { query_data : query_data . drain () . map (| (_ , value) | value) . collect () , artifact_sizes : artifact_sizes . into_values () . collect () , total_time , } } }
};
}
