// Generated macro for Command (enum)
macro_rules! DepcrateCommand {
() => {
// Module: crate
// Provides: {"Command"}
// Dependencies: {}
# [derive (Clone , Debug , Subcommand)] pub enum Command { # [doc = " Installs the miri driver and cargo-miri to the sysroot of the active toolchain."] # [doc = ""] # [doc = " Sets up the rpath such that the installed binary should work in any"] # [doc = " working directory."] Install { # [doc = " Pass features to cargo invocations on the \"miri\" crate in the root. This option does"] # [doc = " **not** apply to other crates, so e.g. these features won't be used on \"cargo-miri\"."] # [arg (long , value_delimiter = ',' , action = clap :: ArgAction :: Append)] features : Vec < String > , # [doc = " Flags that are passed through to `cargo install`."] # [arg (trailing_var_arg = true , allow_hyphen_values = true)] flags : Vec < String > , } , # [doc = " Build Miri."] Build { # [doc = " Pass features to cargo invocations on the \"miri\" crate in the root. This option does"] # [doc = " **not** apply to other crates, so e.g. these features won't be used on \"cargo-miri\"."] # [arg (long , value_delimiter = ',' , action = clap :: ArgAction :: Append)] features : Vec < String > , # [doc = " Flags that are passed through to `cargo build`."] # [arg (trailing_var_arg = true , allow_hyphen_values = true)] flags : Vec < String > , } , # [doc = " Check Miri."] Check { # [doc = " Pass features to cargo invocations on the \"miri\" crate in the root. This option does"] # [doc = " **not** apply to other crates, so e.g. these features won't be used on \"cargo-miri\"."] # [arg (long , value_delimiter = ',' , action = clap :: ArgAction :: Append)] features : Vec < String > , # [doc = " Flags that are passed through to `cargo check`."] # [arg (trailing_var_arg = true , allow_hyphen_values = true)] flags : Vec < String > , } , # [doc = " Check Miri with Clippy."] Clippy { # [doc = " Pass features to cargo invocations on the \"miri\" crate in the root. This option does"] # [doc = " **not** apply to other crates, so e.g. these features won't be used on \"cargo-miri\"."] # [arg (long , value_delimiter = ',' , action = clap :: ArgAction :: Append)] features : Vec < String > , # [doc = " Flags that are passed through to `cargo clippy`."] # [arg (trailing_var_arg = true , allow_hyphen_values = true)] flags : Vec < String > , } , # [doc = " Run the Miri test suite."] Test { # [doc = " Update stdout/stderr reference files."] # [arg (long)] bless : bool , # [doc = " The cross-interpretation target."] # [arg (long)] target : Option < String > , # [doc = " Produce coverage report."] # [arg (long)] coverage : bool , # [doc = " Pass features to cargo invocations on the \"miri\" crate in the root. This option does"] # [doc = " **not** apply to other crates, so e.g. these features won't be used on \"cargo-miri\"."] # [arg (long , value_delimiter = ',' , action = clap :: ArgAction :: Append)] features : Vec < String > , # [doc = " Flags that are passed through to the test harness."] # [arg (trailing_var_arg = true , allow_hyphen_values = true)] flags : Vec < String > , } , # [doc = " Run the Miri driver."] # [doc = ""] # [doc = " Also respects MIRIFLAGS environment variable."] Run { # [doc = " Build the program with the dependencies declared in `tests/deps/Cargo.toml`."] # [arg (long)] dep : bool , # [doc = " Show build progress."] # [arg (long , short)] verbose : bool , # [doc = " The cross-interpretation target."] # [arg (long)] target : Option < String > , # [doc = " The Rust edition."] # [arg (long)] edition : Option < String > , # [doc = " Pass features to cargo invocations on the \"miri\" crate in the root. This option does"] # [doc = " **not** apply to other crates, so e.g. these features won't be used on \"cargo-miri\"."] # [arg (long , value_delimiter = ',' , action = clap :: ArgAction :: Append)] features : Vec < String > , # [doc = " Flags that are passed through to `miri`."] # [doc = ""] # [doc = " The flags set in `MIRIFLAGS` are added in front of these flags."] # [arg (trailing_var_arg = true , allow_hyphen_values = true)] flags : Vec < String > , } , # [doc = " Build documentation."] Doc { # [doc = " Pass features to cargo invocations on the \"miri\" crate in the root. This option does"] # [doc = " **not** apply to other crates, so e.g. these features won't be used on \"cargo-miri\"."] # [arg (long , value_delimiter = ',' , action = clap :: ArgAction :: Append)] features : Vec < String > , # [doc = " Flags that are passed through to `cargo doc`."] # [arg (trailing_var_arg = true , allow_hyphen_values = true)] flags : Vec < String > , } , # [doc = " Format all sources and tests."] Fmt { # [doc = " Flags that are passed through to `rustfmt`."] # [arg (trailing_var_arg = true , allow_hyphen_values = true)] flags : Vec < String > , } , # [doc = " Runs the benchmarks from bench-cargo-miri in hyperfine."] # [doc = ""] # [doc = " hyperfine needs to be installed."] Bench { # [arg (long)] target : Option < String > , # [doc = " When `true`, skip the `./miri install` step."] # [arg (long)] no_install : bool , # [doc = " Store the benchmark result in the given file, so it can be used"] # [doc = " as the baseline for a future run."] # [arg (long)] save_baseline : Option < String > , # [doc = " Load previous stored benchmark results as baseline, and print an analysis of how the"] # [doc = " current run compares."] # [arg (long)] load_baseline : Option < String > , # [doc = " List of benchmarks to run (default: run all benchmarks)."] benches : Vec < String > , } , # [doc = " Update and activate the rustup toolchain 'miri'."] # [doc = ""] # [doc = " The `rust-version` file is used to determine the commit that will be intsalled."] # [doc = " `rustup-toolchain-install-master` must be installed for this to work."] Toolchain { # [doc = " Flags that are passed through to `rustup-toolchain-install-master`."] # [arg (trailing_var_arg = true , allow_hyphen_values = true)] flags : Vec < String > , } , # [doc = " Squash the commits of the current feature branch into one."] Squash , }
};
}
