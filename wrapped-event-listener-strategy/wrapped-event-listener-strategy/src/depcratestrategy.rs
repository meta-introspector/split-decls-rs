// Generated macro for Strategy (trait)
macro_rules! DepcrateStrategy {
() => {
// Module: crate
// Provides: {"Strategy"}
// Dependencies: {}
# [doc = " A strategy for polling an [`EventListenerFuture`] or an [`EventListener`]."] # [doc = ""] # [doc = " This trait is used by the [`EventListenerFuture::poll_with_strategy`] method to determine"] # [doc = " how to poll the future. It can also be used standalone, by calling the [`Strategy::wait`]"] # [doc = " method."] # [doc = ""] # [doc = " [`EventListenerFuture::poll_with_strategy`]: EventListenerFuture::poll_with_strategy"] # [doc = " [`EventListener`]: event_listener::EventListener"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[cfg(not(target_family = \"wasm\"))] { // Blocking is unavailable on WASM"] # [doc = " use event_listener_strategy::{"] # [doc = "    event_listener::{Event, EventListener},"] # [doc = "    EventListenerFuture, Strategy, Blocking, NonBlocking"] # [doc = " };"] # [doc = " use std::pin::Pin;"] # [doc = ""] # [doc = " async fn wait_on<'a, S: Strategy<'a>>(evl: EventListener, strategy: &mut S) {"] # [doc = "     strategy.wait(evl).await;"] # [doc = " }"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " // Block on the future."] # [doc = " let ev = Event::new();"] # [doc = " let listener = ev.listen();"] # [doc = " ev.notify(1);"] # [doc = ""] # [doc = " wait_on(listener, &mut Blocking::default()).await;"] # [doc = ""] # [doc = " // Poll the future."] # [doc = " let listener = ev.listen();"] # [doc = " ev.notify(1);"] # [doc = ""] # [doc = " wait_on(listener, &mut NonBlocking::default()).await;"] # [doc = " # });"] # [doc = " # }"] # [doc = " ```"] pub trait Strategy < 'a > { # [doc = " The context needed to poll the future."] type Context : ? Sized ; # [doc = " The future returned by the [`Strategy::wait`] method."] type Future : Future + 'a ; # [doc = " Poll the event listener until it is ready."] fn poll < T , L : Listener < T > + Unpin > (& mut self , event_listener : & mut Option < L > , context : & mut Self :: Context ,) -> Poll < T > ; # [doc = " Wait for the event listener to become ready."] fn wait (& mut self , evl : EventListener) -> Self :: Future ; }
};
}
