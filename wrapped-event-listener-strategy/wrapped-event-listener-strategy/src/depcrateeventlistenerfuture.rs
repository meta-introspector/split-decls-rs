// Generated macro for EventListenerFuture (trait)
macro_rules! DepcrateEventListenerFuture {
() => {
// Module: crate
// Provides: {"EventListenerFuture"}
// Dependencies: {}
# [doc = " A future that runs using the [`event-listener`] crate."] # [doc = ""] # [doc = " This is similar to the [`Future`] trait from libstd, with one notable difference: it takes"] # [doc = " a strategy that tells it whether to operate in a blocking or non-blocking context. The"] # [doc = " `poll_with_strategy` method is the equivalent of the `poll` method in this regard; it uses"] # [doc = " the [`Strategy`] trait to determine how to poll the future."] # [doc = ""] # [doc = " From here, there are two additional things one can do with this trait:"] # [doc = ""] # [doc = " - The `wait` method, which uses the [`Blocking`] strategy to poll the future until it is"] # [doc = "   ready, blocking the current thread until it is."] # [doc = " - The [`FutureWrapper`] type, which implements [`Future`] and uses the [`NonBlocking`]"] # [doc = "   strategy to poll the future."] pub trait EventListenerFuture { # [doc = " The type of value produced on completion."] type Output ; # [doc = " Poll the future using the provided strategy."] # [doc = ""] # [doc = " This function should use the `Strategy::poll` method to poll the future, and proceed"] # [doc = " based on the result."] fn poll_with_strategy < 'a , S : Strategy < 'a > > (self : Pin < & mut Self > , strategy : & mut S , context : & mut S :: Context ,) -> Poll < Self :: Output > ; # [doc = " Wait for the future to complete, blocking the current thread."] # [doc = ""] # [doc = " This function uses the [`Blocking`] strategy to poll the future until it is ready."] # [doc = ""] # [doc = " The future should only return `Pending` if `Strategy::poll` returns error. Otherwise,"] # [doc = " this function polls the future in a hot loop."] # [cfg (all (feature = "std" , not (target_family = "wasm")))] # [cfg_attr (docsrs , doc (all (feature = "std" , not (target_family = "wasm"))))] fn wait (mut self) -> Self :: Output where Self : Sized , { let mut this = unsafe { Pin :: new_unchecked (& mut self) } ; loop { if let Poll :: Ready (res) = this . as_mut () . poll_with_strategy (& mut Blocking :: default () , & mut ()) { return res ; } } } }
};
}
