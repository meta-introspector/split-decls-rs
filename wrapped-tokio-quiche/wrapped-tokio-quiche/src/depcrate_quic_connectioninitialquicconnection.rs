// Generated macro for InitialQuicConnection (struct)
macro_rules! Depcrate_quic_connectionInitialQuicConnection {
() => {
// Module: crate::quic::connection
// Provides: {"InitialQuicConnection"}
// Dependencies: {}
# [doc = " A QUIC connection that has not performed a handshake yet."] # [doc = ""] # [doc = " This type is currently only used for server-side connections. It is created"] # [doc = " and added to the listener's connection stream after an initial packet from"] # [doc = " a client has been received and (optionally) the client's IP address has been"] # [doc = " validated."] # [doc = ""] # [doc = " To turn the initial connection into a fully established one, a QUIC"] # [doc = " handshake must be performed. Users have multiple options to facilitate this:"] # [doc = " - `start` is a simple entrypoint which spawns a task to handle the entire"] # [doc = "   lifetime of the QUIC connection. The caller can then only communicate with"] # [doc = "   the connection via their [`ApplicationOverQuic`]."] # [doc = " - `handshake` spawns a task for the handshake and awaits its completion."] # [doc = "   Afterwards, it pauses the connection and allows the caller to resume it"] # [doc = "   later via an opaque struct. We spawn a separate task to allow the tokio"] # [doc = "   scheduler free choice in where to run the handshake."] # [doc = " - `handshake_fut` returns a future to drive the handshake for maximum"] # [doc = "   flexibility."] # [must_use = "call InitialQuicConnection::start to establish the connection"] pub struct InitialQuicConnection < Tx , M > where Tx : DatagramSocketSend + Send + 'static + ? Sized , M : Metrics , { # [doc = " An internal ID, to uniquely identify the connection across multiple QUIC"] # [doc = " connection IDs."] pub (crate) id : u64 , params : QuicConnectionParams < Tx , M > , pub (crate) audit_log_stats : Arc < QuicAuditStats > , stats : QuicConnectionStatsShared , pub (crate) incoming_ev_sender : mpsc :: Sender < Incoming > , incoming_ev_receiver : mpsc :: Receiver < Incoming > , }
};
}
