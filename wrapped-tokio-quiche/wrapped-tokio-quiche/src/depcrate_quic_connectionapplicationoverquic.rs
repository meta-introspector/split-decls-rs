// Generated macro for ApplicationOverQuic (trait)
macro_rules! Depcrate_quic_connectionApplicationOverQuic {
() => {
// Module: crate::quic::connection
// Provides: {"ApplicationOverQuic"}
// Dependencies: {}
# [doc = " A trait to implement an application served over QUIC."] # [doc = ""] # [doc = " The application is driven by an internal worker task, which also handles I/O"] # [doc = " for the connection. The worker feeds inbound packets into the"] # [doc = " [quiche::Connection], calls [`ApplicationOverQuic::process_reads`] followed"] # [doc = " by [`ApplicationOverQuic::process_writes`], and then flushes any pending"] # [doc = " outbound packets to the network. This repeats in a loop until either the"] # [doc = " connection is closed or the [`ApplicationOverQuic`] returns an error."] # [doc = ""] # [doc = " In between loop iterations, the worker yields until a new packet arrives, a"] # [doc = " timer expires, or [`ApplicationOverQuic::wait_for_data`] resolves."] # [doc = " Implementors can interact with the underlying connection via the mutable"] # [doc = " reference passed to trait methods."] # [allow (unused_variables)] pub trait ApplicationOverQuic : Send + 'static { # [doc = " Callback to customize the [`ApplicationOverQuic`] after the QUIC"] # [doc = " handshake completed successfully."] # [doc = ""] # [doc = " # Errors"] # [doc = " Returning an error from this method immediately stops the worker loop"] # [doc = " and transitions to the connection closing stage."] fn on_conn_established (& mut self , qconn : & mut QuicheConnection , handshake_info : & HandshakeInfo ,) -> QuicResult < () > ; # [doc = " Determines whether the application's methods will be called by the"] # [doc = " worker."] # [doc = ""] # [doc = " The function is checked in each iteration of the worker loop. Only"] # [doc = " `on_conn_established()` and `buffer()` bypass this check."] fn should_act (& self) -> bool ; # [doc = " A borrowed buffer for the worker to write outbound packets into."] # [doc = ""] # [doc = " This method allows sharing a buffer between the worker and the"] # [doc = " application, efficiently using the allocated memory while the"] # [doc = " application is inactive. It can also be used to artificially"] # [doc = " restrict the size of outbound network packets."] # [doc = ""] # [doc = " Any data in the buffer may be overwritten by the worker. If necessary,"] # [doc = " the application should save the contents when this method is called."] fn buffer (& mut self) -> & mut [u8] ; # [doc = " Waits for an event to trigger the next iteration of the worker loop."] # [doc = ""] # [doc = " The returned future is awaited in parallel to inbound packets and the"] # [doc = " connection's timers. Any one of those futures resolving triggers the"] # [doc = " next loop iteration, so implementations should not rely on"] # [doc = " `wait_for_data` for the bulk of their processing. Instead, after"] # [doc = " `wait_for_data` resolves, `process_writes` should be used to pull all"] # [doc = " available data out of the event source (for example, a channel)."] # [doc = ""] # [doc = " As for any future, it is **very important** that this method does not"] # [doc = " block the runtime. If it does, the other concurrent futures will be"] # [doc = " starved."] # [doc = ""] # [doc = " # Errors"] # [doc = " Returning an error from this method immediately stops the worker loop"] # [doc = " and transitions to the connection closing stage."] fn wait_for_data (& mut self , qconn : & mut QuicheConnection ,) -> impl Future < Output = QuicResult < () > > + Send ; # [doc = " Processes data received on the connection."] # [doc = ""] # [doc = " This method is only called if `should_act()` returns `true` and any"] # [doc = " packets were received since the last worker loop iteration. It"] # [doc = " should be used to read from the connection's open streams."] # [doc = ""] # [doc = " # Errors"] # [doc = " Returning an error from this method immediately stops the worker loop"] # [doc = " and transitions to the connection closing stage."] fn process_reads (& mut self , qconn : & mut QuicheConnection) -> QuicResult < () > ; # [doc = " Adds data to be sent on the connection."] # [doc = ""] # [doc = " Unlike `process_reads`, this method is called on every iteration of the"] # [doc = " worker loop (provided `should_act()` returns true). It is called"] # [doc = " after `process_reads` and immediately before packets are pushed to"] # [doc = " the socket. The main use case is providing already-buffered data to"] # [doc = " the [quiche::Connection]."] # [doc = ""] # [doc = " # Errors"] # [doc = " Returning an error from this method immediately stops the worker loop"] # [doc = " and transitions to the connection closing stage."] fn process_writes (& mut self , qconn : & mut QuicheConnection) -> QuicResult < () > ; # [doc = " Callback to inspect the result of the worker task, before a final packet"] # [doc = " with a `CONNECTION_CLOSE` frame is flushed to the network."] # [doc = ""] # [doc = " `connection_result` is [`Ok`] only if the connection was closed without"] # [doc = " any local error. Otherwise, the state of `qconn` depends on the"] # [doc = " error type and application behavior."] fn on_conn_close < M : Metrics > (& mut self , qconn : & mut QuicheConnection , metrics : & M , connection_result : & QuicResult < () > ,) { } }
};
}
