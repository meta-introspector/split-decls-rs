// Generated macro for impl_179 (impl)
macro_rules! Depcrate_extensions_privateimpl_179 {
() => {
// Module: crate::extensions::private
// Provides: {"impl_179"}
// Dependencies: {}
impl Private { # [doc = " Returns a new empty list of private-use extensions. Same as [`default()`](Default::default()), but is `const`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::extensions::private::Private;"] # [doc = ""] # [doc = " assert_eq!(Private::new(), Private::default());"] # [doc = " ```"] # [inline] pub const fn new () -> Self { Self (ShortBoxSlice :: new ()) } # [doc = " A constructor which takes a str slice, parses it and"] # [doc = " produces a well-formed [`Private`]."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [inline] # [cfg (feature = "alloc")] pub fn try_from_str (s : & str) -> Result < Self , ParseError > { Self :: try_from_utf8 (s . as_bytes ()) } # [doc = " See [`Self::try_from_str`]"] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [cfg (feature = "alloc")] pub fn try_from_utf8 (code_units : & [u8]) -> Result < Self , ParseError > { let mut iter = SubtagIterator :: new (code_units) ; let ext = iter . next () . ok_or (ParseError :: InvalidExtension) ? ; if let ExtensionType :: Private = ExtensionType :: try_from_byte_slice (ext) ? { return Self :: try_from_iter (& mut iter) ; } Err (ParseError :: InvalidExtension) } # [doc = " A constructor which takes a pre-sorted list of [`Subtag`]."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::extensions::private::{Private, Subtag};"] # [doc = ""] # [doc = " let subtag1: Subtag = \"foo\".parse().expect(\"Failed to parse a Subtag.\");"] # [doc = " let subtag2: Subtag = \"bar\".parse().expect(\"Failed to parse a Subtag.\");"] # [doc = ""] # [doc = " let private = Private::from_vec_unchecked(vec![subtag1, subtag2]);"] # [doc = " assert_eq!(&private.to_string(), \"x-foo-bar\");"] # [doc = " ```"] # [cfg (feature = "alloc")] pub fn from_vec_unchecked (input : Vec < Subtag >) -> Self { Self (input . into ()) } # [doc = " A constructor which takes a single [`Subtag`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::extensions::private::{Private, Subtag};"] # [doc = ""] # [doc = " let subtag: Subtag = \"foo\".parse().expect(\"Failed to parse a Subtag.\");"] # [doc = ""] # [doc = " let private = Private::new_single(subtag);"] # [doc = " assert_eq!(&private.to_string(), \"x-foo\");"] # [doc = " ```"] pub const fn new_single (input : Subtag) -> Self { Self (ShortBoxSlice :: new_single (input)) } # [doc = " Empties the [`Private`] list."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::extensions::private::{Private, Subtag};"] # [doc = ""] # [doc = " let subtag1: Subtag = \"foo\".parse().expect(\"Failed to parse a Subtag.\");"] # [doc = " let subtag2: Subtag = \"bar\".parse().expect(\"Failed to parse a Subtag.\");"] # [doc = " let mut private = Private::from_vec_unchecked(vec![subtag1, subtag2]);"] # [doc = ""] # [doc = " assert_eq!(&private.to_string(), \"x-foo-bar\");"] # [doc = ""] # [doc = " private.clear();"] # [doc = ""] # [doc = " assert_eq!(private, Private::new());"] # [doc = " ```"] pub fn clear (& mut self) { self . 0 . clear () ; } # [cfg (feature = "alloc")] pub (crate) fn try_from_iter (iter : & mut SubtagIterator) -> Result < Self , ParseError > { let keys = iter . map (Subtag :: try_from_utf8) . collect :: < Result < ShortBoxSlice < _ > , _ > > () ? ; if keys . is_empty () { Err (ParseError :: InvalidExtension) } else { Ok (Self (keys)) } } pub (crate) fn for_each_subtag_str < E , F > (& self , f : & mut F , with_ext : bool) -> Result < () , E > where F : FnMut (& str) -> Result < () , E > , { if self . is_empty () { return Ok (()) ; } if with_ext { f (PRIVATE_EXT_STR) ? ; } self . deref () . iter () . map (| t | t . as_str ()) . try_for_each (f) } }
};
}
