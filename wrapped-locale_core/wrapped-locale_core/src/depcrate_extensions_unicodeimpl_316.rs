// Generated macro for impl_316 (impl)
macro_rules! Depcrate_extensions_unicodeimpl_316 {
() => {
// Module: crate::extensions::unicode
// Provides: {"impl_316"}
// Dependencies: {}
impl Unicode { # [doc = " Returns a new empty map of Unicode extensions. Same as [`default()`](Default::default()), but is `const`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::extensions::unicode::Unicode;"] # [doc = ""] # [doc = " assert_eq!(Unicode::new(), Unicode::default());"] # [doc = " ```"] # [inline] pub const fn new () -> Self { Self { keywords : Keywords :: new () , attributes : Attributes :: new () , } } # [doc = " A constructor which takes a str slice, parses it and"] # [doc = " produces a well-formed [`Unicode`]."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [inline] # [cfg (feature = "alloc")] pub fn try_from_str (s : & str) -> Result < Self , ParseError > { Self :: try_from_utf8 (s . as_bytes ()) } # [doc = " See [`Self::try_from_str`]"] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [cfg (feature = "alloc")] pub fn try_from_utf8 (code_units : & [u8]) -> Result < Self , ParseError > { let mut iter = SubtagIterator :: new (code_units) ; let ext = iter . next () . ok_or (ParseError :: InvalidExtension) ? ; if let ExtensionType :: Unicode = ExtensionType :: try_from_byte_slice (ext) ? { return Self :: try_from_iter (& mut iter) ; } Err (ParseError :: InvalidExtension) } # [doc = " Returns [`true`] if there list of keywords and attributes is empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::Locale;"] # [doc = ""] # [doc = " let loc: Locale = \"en-US-u-foo\".parse().expect(\"Parsing failed.\");"] # [doc = ""] # [doc = " assert!(!loc.extensions.unicode.is_empty());"] # [doc = " ```"] pub fn is_empty (& self) -> bool { self . keywords . is_empty () && self . attributes . is_empty () } # [doc = " Clears all Unicode extension keywords and attributes, effectively removing"] # [doc = " the Unicode extension."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::Locale;"] # [doc = ""] # [doc = " let mut loc: Locale ="] # [doc = "     \"und-t-mul-u-hello-ca-buddhist-hc-h12\".parse().unwrap();"] # [doc = " loc.extensions.unicode.clear();"] # [doc = " assert_eq!(loc, \"und-t-mul\".parse().unwrap());"] # [doc = " ```"] pub fn clear (& mut self) { self . keywords . clear () ; self . attributes . clear () ; } pub (crate) fn as_tuple (& self) -> (& Attributes , & Keywords) { (& self . attributes , & self . keywords) } # [doc = " Returns an ordering suitable for use in [`BTreeSet`]."] # [doc = ""] # [doc = " The ordering may or may not be equivalent to string ordering, and it"] # [doc = " may or may not be stable across ICU4X releases."] # [doc = ""] # [doc = " [`BTreeSet`]: alloc::collections::BTreeSet"] pub fn total_cmp (& self , other : & Self) -> Ordering { self . as_tuple () . cmp (& other . as_tuple ()) } # [cfg (feature = "alloc")] pub (crate) fn try_from_iter (iter : & mut SubtagIterator) -> Result < Self , ParseError > { let attributes = Attributes :: try_from_iter (iter) ? ; let keywords = Keywords :: try_from_iter (iter) ? ; if attributes . is_empty () && keywords . is_empty () { return Err (ParseError :: InvalidExtension) ; } Ok (Self { keywords , attributes , }) } pub (crate) fn for_each_subtag_str < E , F > (& self , f : & mut F , with_ext : bool) -> Result < () , E > where F : FnMut (& str) -> Result < () , E > , { if ! self . is_empty () { if with_ext { f (UNICODE_EXT_STR) ? ; } self . attributes . for_each_subtag_str (f) ? ; self . keywords . for_each_subtag_str (f) ? ; } Ok (()) } # [doc = " Extends the `Unicode` with values from  another `Unicode`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::extensions::unicode::Unicode;"] # [doc = ""] # [doc = " let mut ue: Unicode = \"u-foobar-ca-buddhist\".parse().unwrap();"] # [doc = " let ue2: Unicode = \"u-ca-gregory-hc-h12\".parse().unwrap();"] # [doc = ""] # [doc = " ue.extend(ue2);"] # [doc = ""] # [doc = " assert_eq!(ue, \"u-foobar-ca-gregory-hc-h12\".parse().unwrap());"] # [doc = " ```"] # [cfg (feature = "alloc")] pub fn extend (& mut self , other : Unicode) { self . keywords . extend_from_keywords (other . keywords) ; self . attributes . extend_from_attributes (other . attributes) ; } }
};
}
