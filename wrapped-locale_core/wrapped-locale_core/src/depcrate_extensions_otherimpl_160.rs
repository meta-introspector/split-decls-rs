// Generated macro for impl_160 (impl)
macro_rules! Depcrate_extensions_otherimpl_160 {
() => {
// Module: crate::extensions::other
// Provides: {"impl_160"}
// Dependencies: {}
impl Other { # [doc = " A constructor which takes a str slice, parses it and"] # [doc = " produces a well-formed [`Other`]."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [inline] # [cfg (feature = "alloc")] pub fn try_from_str (s : & str) -> Result < Self , ParseError > { Self :: try_from_utf8 (s . as_bytes ()) } # [doc = " See [`Self::try_from_str`]"] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [cfg (feature = "alloc")] pub fn try_from_utf8 (code_units : & [u8]) -> Result < Self , ParseError > { let mut iter = SubtagIterator :: new (code_units) ; let ext = iter . next () . ok_or (ParseError :: InvalidExtension) ? ; if let ExtensionType :: Other (b) = ExtensionType :: try_from_byte_slice (ext) ? { return Self :: try_from_iter (b , & mut iter) ; } Err (ParseError :: InvalidExtension) } # [doc = " A constructor which takes a pre-sorted list of [`Subtag`]."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `ext` is not ASCII alphabetic."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::extensions::other::Other;"] # [doc = " use icu::locale::subtags::Subtag;"] # [doc = ""] # [doc = " let subtag1: Subtag = \"foo\".parse().expect(\"Failed to parse a Subtag.\");"] # [doc = " let subtag2: Subtag = \"bar\".parse().expect(\"Failed to parse a Subtag.\");"] # [doc = ""] # [doc = " let other = Other::from_vec_unchecked(b'a', vec![subtag1, subtag2]);"] # [doc = " assert_eq!(&other.to_string(), \"a-foo-bar\");"] # [doc = " ```"] # [cfg (feature = "alloc")] pub fn from_vec_unchecked (ext : u8 , keys : Vec < Subtag >) -> Self { Self :: from_short_slice_unchecked (ext , keys . into ()) } # [allow (dead_code)] pub (crate) fn from_short_slice_unchecked (ext : u8 , keys : ShortBoxSlice < Subtag >) -> Self { assert ! (ext . is_ascii_alphabetic ()) ; Self { ext , keys } } # [cfg (feature = "alloc")] pub (crate) fn try_from_iter (ext : u8 , iter : & mut SubtagIterator) -> Result < Self , ParseError > { debug_assert ! (matches ! (ExtensionType :: try_from_byte (ext) , Ok (ExtensionType :: Other (_)) ,)) ; let mut keys = ShortBoxSlice :: new () ; while let Some (subtag) = iter . peek () { if ! Subtag :: valid_key (subtag) { break ; } if let Ok (key) = Subtag :: try_from_utf8 (subtag) { keys . push (key) ; } iter . next () ; } if keys . is_empty () { Err (ParseError :: InvalidExtension) } else { Ok (Self :: from_short_slice_unchecked (ext , keys)) } } # [doc = " Gets the tag character for this extension as a &str."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::Locale;"] # [doc = ""] # [doc = " let loc: Locale = \"und-a-hello-world\".parse().unwrap();"] # [doc = " let other_ext = &loc.extensions.other[0];"] # [doc = " assert_eq!(other_ext.get_ext_str(), \"a\");"] # [doc = " ```"] pub fn get_ext_str (& self) -> & str { debug_assert ! (self . ext . is_ascii_alphabetic ()) ; unsafe { core :: str :: from_utf8_unchecked (core :: slice :: from_ref (& self . ext)) } } # [doc = " Gets the tag character for this extension as a char."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::Locale;"] # [doc = ""] # [doc = " let loc: Locale = \"und-a-hello-world\".parse().unwrap();"] # [doc = " let other_ext = &loc.extensions.other[0];"] # [doc = " assert_eq!(other_ext.get_ext(), 'a');"] # [doc = " ```"] pub fn get_ext (& self) -> char { self . ext as char } # [doc = " Gets the tag character for this extension as a byte."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::locale::Locale;"] # [doc = ""] # [doc = " let loc: Locale = \"und-a-hello-world\".parse().unwrap();"] # [doc = " let other_ext = &loc.extensions.other[0];"] # [doc = " assert_eq!(other_ext.get_ext_byte(), b'a');"] # [doc = " ```"] pub fn get_ext_byte (& self) -> u8 { self . ext } pub (crate) fn for_each_subtag_str < E , F > (& self , f : & mut F , with_ext : bool) -> Result < () , E > where F : FnMut (& str) -> Result < () , E > , { if self . keys . is_empty () { return Ok (()) ; } if with_ext { f (self . get_ext_str ()) ? ; } self . keys . iter () . map (| t | t . as_str ()) . try_for_each (f) } }
};
}
