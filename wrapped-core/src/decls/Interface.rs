macro_rules! deps {
    () => {
        IUnknown!();
        InterfaceRef!();
        GUID!();
        HRESULT!();
        Weak!();
        ComObject!();
        IUnknownImpl!();
        ComObjectInner!();
    };
}

macro_rules! Interface {
    () => {
        deps!();
        # [doc = " Provides low-level access to an interface vtable."] # [doc = ""] # [doc = " This trait is automatically implemented by the generated bindings and should not be"] # [doc = " implemented manually."] # [doc = ""] # [doc = " # Safety"] pub unsafe trait Interface : Sized + Clone { # [doc (hidden)] type Vtable ; # [doc = " The `GUID` associated with the interface."] const IID : GUID ; # [doc (hidden)] const UNKNOWN : bool = true ; # [doc = " A reference to the interface's vtable"] # [doc (hidden)] # [inline (always)] fn vtable (& self) -> & Self :: Vtable { unsafe { self . assume_vtable :: < Self > () } } # [doc = " Cast this interface as a reference to the supplied interfaces `Vtable`"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This is safe if `T` is an equivalent interface to `Self` or a super interface."] # [doc = " In other words, `T::Vtable` must be equivalent to the beginning of `Self::Vtable`."] # [doc (hidden)] # [inline (always)] unsafe fn assume_vtable < T : Interface > (& self) -> & T :: Vtable { unsafe { & * * (self . as_raw () as * mut * mut T :: Vtable) } } # [doc = " Returns the raw COM interface pointer. The resulting pointer continues to be owned by the `Interface` implementation."] # [inline (always)] fn as_raw (& self) -> * mut c_void { unsafe { transmute_copy (self) } } # [doc = " Returns the raw COM interface pointer and releases ownership. It the caller's responsibility to release the COM interface pointer."] # [inline (always)] fn into_raw (self) -> * mut c_void { let raw = self . as_raw () ; forget (self) ; raw } # [doc = " Creates an `Interface` by taking ownership of the `raw` COM interface pointer."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The `raw` pointer must be owned by the caller and represent a valid COM interface pointer. In other words,"] # [doc = " it must point to a vtable beginning with the `IUnknown` function pointers and match the vtable of `Interface`."] unsafe fn from_raw (raw : * mut c_void) -> Self { unsafe { transmute_copy (& raw) } } # [doc = " Creates an `Interface` that is valid so long as the `raw` COM interface pointer is valid."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The `raw` pointer must be a valid COM interface pointer. In other words, it must point to a vtable"] # [doc = " beginning with the `IUnknown` function pointers and match the vtable of `Interface`."] # [inline (always)] unsafe fn from_raw_borrowed (raw : & * mut c_void) -> Option < & Self > { unsafe { if raw . is_null () { None } else { Some (transmute_copy (& raw)) } } } # [doc = " Attempts to cast the current interface to another interface using `QueryInterface`."] # [doc = ""] # [doc = " The name `cast` is preferred to `query` because there is a WinRT method named query but not one"] # [doc = " named cast."] # [inline (always)] fn cast < T : Interface > (& self) -> Result < T > { unsafe { let mut result = MaybeUninit :: < Option < T > > :: zeroed () ; self . query (& T :: IID , result . as_mut_ptr () as _) . ok () ? ; if let Some (obj) = result . assume_init () { Ok (obj) } else { Err (imp :: E_POINTER . into ()) } } } # [doc = " This casts the given COM interface to [`&dyn Any`]."] # [doc = ""] # [doc = " Applications should generally _not_ call this method directly. Instead, use the"] # [doc = " [`Interface::cast_object_ref`] or [`Interface::cast_object`] methods."] # [doc = ""] # [doc = " `T` must be a type that has been annotated with `#[implement]`; this is checked at"] # [doc = " compile-time by the generic constraints of this method. However, note that the"] # [doc = " returned `&dyn Any` refers to the _outer_ implementation object that was generated by"] # [doc = " `#[implement]`, i.e. the `MyApp_Impl` type, not the inner `MyApp` type."] # [doc = ""] # [doc = " If the given object is not a Rust object, or is a Rust object but not `T`, or is a Rust"] # [doc = " object that contains non-static lifetimes, then this function will return `Err(E_NOINTERFACE)`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " **IMPORTANT!!**  This uses a non-standard protocol for QueryInterface!  The `DYNAMIC_CAST_IID`"] # [doc = " IID identifies this protocol, but there is no `IDynamicCast` interface. Instead, objects"] # [doc = " that recognize `DYNAMIC_CAST_IID` simply store their `&dyn Any` directly at the interface"] # [doc = " pointer that was passed to `QueryInterface. This means that the returned value has a"] # [doc = " size that is twice as large (`size_of::<&dyn Any>() == 2 * size_of::<*const c_void>()`)."] # [doc = ""] # [doc = " This means that callers that use this protocol cannot simply pass `&mut ptr` for"] # [doc = " an ordinary single-pointer-sized pointer. Only this method understands this protocol."] # [doc = ""] # [doc = " Another part of this protocol is that the implementation of `QueryInterface` _does not_"] # [doc = " AddRef the object. The caller must guarantee the liveness of the COM object. In Rust,"] # [doc = " this means tying the lifetime of the IUnknown* that we used for the QueryInterface"] # [doc = " call to the lifetime of the returned `&dyn Any` value."] # [doc = ""] # [doc = " This method preserves type safety and relies on these invariants:"] # [doc = ""] # [doc = " * All `QueryInterface` implementations that recognize `DYNAMIC_CAST_IID` are generated by"] # [doc = "   the `#[implement]` macro and respect the rules described here."] # [inline (always)] fn cast_to_any < T > (& self) -> Result < & dyn Any > where T : ComObjectInner , T :: Outer : Any + 'static + IUnknownImpl < Impl = T > , { unsafe { let mut any_ref_arg : MaybeUninit < & dyn Any > = MaybeUninit :: zeroed () ; self . query (& DYNAMIC_CAST_IID , any_ref_arg . as_mut_ptr () as * mut * mut c_void ,) . ok () ? ; Ok (any_ref_arg . assume_init ()) } } # [doc = " Returns `true` if the given COM interface refers to an implementation of `T`."] # [doc = ""] # [doc = " `T` must be a type that has been annotated with `#[implement]`; this is checked at"] # [doc = " compile-time by the generic constraints of this method."] # [doc = ""] # [doc = " If the given object is not a Rust object, or is a Rust object but not `T`, or is a Rust"] # [doc = " object that contains non-static lifetimes, then this function will return `false`."] # [inline (always)] fn is_object < T > (& self) -> bool where T : ComObjectInner , T :: Outer : Any + 'static + IUnknownImpl < Impl = T > , { if let Ok (any) = self . cast_to_any :: < T > () { any . is :: < T :: Outer > () } else { false } } # [doc = " This casts the given COM interface to [`&dyn Any`]. It returns a reference to the \"outer\""] # [doc = " object, e.g. `&MyApp_Impl`, not the inner `&MyApp` object."] # [doc = ""] # [doc = " `T` must be a type that has been annotated with `#[implement]`; this is checked at"] # [doc = " compile-time by the generic constraints of this method. However, note that the"] # [doc = " returned `&dyn Any` refers to the _outer_ implementation object that was generated by"] # [doc = " `#[implement]`, i.e. the `MyApp_Impl` type, not the inner `MyApp` type."] # [doc = ""] # [doc = " If the given object is not a Rust object, or is a Rust object but not `T`, or is a Rust"] # [doc = " object that contains non-static lifetimes, then this function will return `Err(E_NOINTERFACE)`."] # [doc = ""] # [doc = " The returned value is borrowed. If you need an owned (counted) reference, then use"] # [doc = " [`Interface::cast_object`]."] # [inline (always)] fn cast_object_ref < T > (& self) -> Result < & T :: Outer > where T : ComObjectInner , T :: Outer : Any + 'static + IUnknownImpl < Impl = T > , { let any : & dyn Any = self . cast_to_any :: < T > () ? ; if let Some (outer) = any . downcast_ref :: < T :: Outer > () { Ok (outer) } else { Err (imp :: E_NOINTERFACE . into ()) } } # [doc = " This casts the given COM interface to [`&dyn Any`]. It returns a reference to the \"outer\""] # [doc = " object, e.g. `MyApp_Impl`, not the inner `MyApp` object."] # [doc = ""] # [doc = " `T` must be a type that has been annotated with `#[implement]`; this is checked at"] # [doc = " compile-time by the generic constraints of this method. However, note that the"] # [doc = " returned `&dyn Any` refers to the _outer_ implementation object that was generated by"] # [doc = " `#[implement]`, i.e. the `MyApp_Impl` type, not the inner `MyApp` type."] # [doc = ""] # [doc = " If the given object is not a Rust object, or is a Rust object but not `T`, or is a Rust"] # [doc = " object that contains non-static lifetimes, then this function will return `Err(E_NOINTERFACE)`."] # [doc = ""] # [doc = " The returned value is an owned (counted) reference; this function calls `AddRef` on the"] # [doc = " underlying COM object. If you do not need an owned reference, then you can use the"] # [doc = " [`Interface::cast_object_ref`] method instead, and avoid the cost of `AddRef` / `Release`."] # [inline (always)] fn cast_object < T > (& self) -> Result < ComObject < T > > where T : ComObjectInner , T :: Outer : Any + 'static + IUnknownImpl < Impl = T > , { let object_ref = self . cast_object_ref :: < T > () ? ; Ok (object_ref . to_object ()) } # [doc = " Attempts to create a [`Weak`] reference to this object."] fn downgrade (& self) -> Result < Weak < Self > > { self . cast :: < imp :: IWeakReferenceSource > () . map (| source | Weak :: downgrade (& source)) } # [doc = " Call `QueryInterface` on this interface"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `interface` must be a non-null, valid pointer for writing an interface pointer."] # [inline (always)] unsafe fn query (& self , iid : * const GUID , interface : * mut * mut c_void) -> HRESULT { unsafe { if Self :: UNKNOWN { (self . assume_vtable :: < IUnknown > () . QueryInterface) (self . as_raw () , iid , interface) } else { panic ! ("Non-COM interfaces cannot be queried.") } } } # [doc = " Creates an `InterfaceRef` for this reference. The `InterfaceRef` tracks lifetimes statically,"] # [doc = " and eliminates the need for dynamic reference count adjustments (AddRef/Release)."] fn to_ref (& self) -> InterfaceRef < '_ , Self > { InterfaceRef :: from_interface (self) } }
    };
}

Interface!()