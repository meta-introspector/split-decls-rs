// Generated macro for Bindgen (trait)
macro_rules! Depcrate_abiBindgen {
() => {
// Module: crate::abi
// Provides: {"Bindgen"}
// Dependencies: {}
# [doc = " Trait for language implementors to use to generate glue code between native"] # [doc = " WebAssembly signatures and interface types signatures."] # [doc = ""] # [doc = " This is used as an implementation detail in interpreting the ABI between"] # [doc = " interface types and wasm types. Eventually this will be driven by interface"] # [doc = " types adapters themselves, but for now the ABI of a function dictates what"] # [doc = " instructions are fed in."] # [doc = ""] # [doc = " Types implementing `Bindgen` are incrementally fed `Instruction` values to"] # [doc = " generate code for. Instructions operate like a stack machine where each"] # [doc = " instruction has a list of inputs and a list of outputs (provided by the"] # [doc = " `emit` function)."] pub trait Bindgen { # [doc = " The intermediate type for fragments of code for this type."] # [doc = ""] # [doc = " For most languages `String` is a suitable intermediate type."] type Operand : Clone + fmt :: Debug ; # [doc = " Emit code to implement the given instruction."] # [doc = ""] # [doc = " Each operand is given in `operands` and can be popped off if ownership"] # [doc = " is required. It's guaranteed that `operands` has the appropriate length"] # [doc = " for the `inst` given, as specified with [`Instruction`]."] # [doc = ""] # [doc = " Each result variable should be pushed onto `results`. This function must"] # [doc = " push the appropriate number of results or binding generation will panic."] fn emit (& mut self , resolve : & Resolve , inst : & Instruction < '_ > , operands : & mut Vec < Self :: Operand > , results : & mut Vec < Self :: Operand > ,) ; # [doc = " Gets a operand reference to the return pointer area."] # [doc = ""] # [doc = " The provided size and alignment is for the function's return type."] fn return_pointer (& mut self , size : ArchitectureSize , align : Alignment) -> Self :: Operand ; # [doc = " Enters a new block of code to generate code for."] # [doc = ""] # [doc = " This is currently exclusively used for constructing variants. When a"] # [doc = " variant is constructed a block here will be pushed for each case of a"] # [doc = " variant, generating the code necessary to translate a variant case."] # [doc = ""] # [doc = " Blocks are completed with `finish_block` below. It's expected that `emit`"] # [doc = " will always push code (if necessary) into the \"current block\", which is"] # [doc = " updated by calling this method and `finish_block` below."] fn push_block (& mut self) ; # [doc = " Indicates to the code generator that a block is completed, and the"] # [doc = " `operand` specified was the resulting value of the block."] # [doc = ""] # [doc = " This method will be used to compute the value of each arm of lifting a"] # [doc = " variant. The `operand` will be `None` if the variant case didn't"] # [doc = " actually have any type associated with it. Otherwise it will be `Some`"] # [doc = " as the last value remaining on the stack representing the value"] # [doc = " associated with a variant's `case`."] # [doc = ""] # [doc = " It's expected that this will resume code generation in the previous"] # [doc = " block before `push_block` was called. This must also save the results"] # [doc = " of the current block internally for instructions like `ResultLift` to"] # [doc = " use later."] fn finish_block (& mut self , operand : & mut Vec < Self :: Operand >) ; # [doc = " Returns size information that was previously calculated for all types."] fn sizes (& self) -> & SizeAlign ; # [doc = " Returns whether or not the specified element type is represented in a"] # [doc = " \"canonical\" form for lists. This dictates whether the `ListCanonLower`"] # [doc = " and `ListCanonLift` instructions are used or not."] fn is_list_canonical (& self , resolve : & Resolve , element : & Type) -> bool ; }
};
}
