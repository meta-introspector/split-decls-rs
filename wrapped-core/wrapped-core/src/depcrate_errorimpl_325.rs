// Generated macro for impl_325 (impl)
macro_rules! Depcrate_errorimpl_325 {
() => {
// Module: crate::error
// Provides: {"impl_325"}
// Dependencies: {}
impl Accumulator { # [doc = " Runs a closure, returning the successful value as `Some`, or collecting the error"] # [doc = ""] # [doc = " The closure's return type is `darling::Result`, so inside it one can use `?`."] pub fn handle_in < T , F : FnOnce () -> Result < T > > (& mut self , f : F) -> Option < T > { self . handle (f ()) } # [doc = " Handles a possible error."] # [doc = ""] # [doc = " Returns a successful value as `Some`, or collects the error and returns `None`."] pub fn handle < T > (& mut self , result : Result < T >) -> Option < T > { match result { Ok (y) => Some (y) , Err (e) => { self . push (e) ; None } } } # [doc = " Stop accumulating errors, producing `Ok` if there are no errors or producing"] # [doc = " an error with all those encountered by the accumulator."] pub fn finish (self) -> Result < () > { self . finish_with (()) } # [doc = " Bundles the collected errors if there were any, or returns the success value"] # [doc = ""] # [doc = " Call this at the end of your input processing."] # [doc = ""] # [doc = " If there were no errors recorded, returns `Ok(success)`."] # [doc = " Otherwise calls [`Error::multiple`] and returns the result as an `Err`."] pub fn finish_with < T > (self , success : T) -> Result < T > { let errors = self . into_inner () ; if errors . is_empty () { Ok (success) } else { Err (Error :: multiple (errors)) } } fn errors (& mut self) -> & mut Vec < Error > { match & mut self . 0 { Some (errors) => errors , None => panic ! ("darling internal error: Accumulator accessed after defuse") , } } # [doc = " Returns the accumulated errors as a `Vec`."] # [doc = ""] # [doc = " This function defuses the drop bomb."] # [must_use = "Accumulated errors should be handled or propagated to the caller"] pub fn into_inner (mut self) -> Vec < Error > { match self . 0 . take () { Some (errors) => errors , None => panic ! ("darling internal error: Accumulator accessed after defuse") , } } # [doc = " Add one error to the collection."] pub fn push (& mut self , error : Error) { self . errors () . push (error) } # [doc = " Finish the current accumulation, and if there are no errors create a new `Self` so processing may continue."] # [doc = ""] # [doc = " This is shorthand for:"] # [doc = ""] # [doc = " ```rust,ignore"] # [doc = " errors.finish()?;"] # [doc = " errors = Error::accumulator();"] # [doc = " ```"] # [doc = ""] # [doc = " # Drop Behavior"] # [doc = " This function returns a new [`Accumulator`] in the success case."] # [doc = " This new accumulator is \"armed\" and will detonate if dropped without being finished."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # extern crate darling_core as darling;"] # [doc = " # struct Thing;"] # [doc = " # struct Output;"] # [doc = " # impl Thing { fn validate(&self) -> darling::Result<Output> { Ok(Output) } }"] # [doc = " fn validate(lorem_inputs: &[Thing], ipsum_inputs: &[Thing])"] # [doc = "             -> darling::Result<(Vec<Output>, Vec<Output>)> {"] # [doc = "     let mut errors = darling::Error::accumulator();"] # [doc = ""] # [doc = "     let lorems = lorem_inputs.iter().filter_map(|l| {"] # [doc = "         errors.handle(l.validate())"] # [doc = "     }).collect();"] # [doc = ""] # [doc = "     errors = errors.checkpoint()?;"] # [doc = ""] # [doc = "     let ipsums = ipsum_inputs.iter().filter_map(|l| {"] # [doc = "         errors.handle(l.validate())"] # [doc = "     }).collect();"] # [doc = ""] # [doc = "     errors.finish_with((lorems, ipsums))"] # [doc = " }"] # [doc = " # validate(&[], &[]).unwrap();"] # [doc = " ```"] pub fn checkpoint (self) -> Result < Accumulator > { self . finish () ? ; Ok (Self :: default ()) } }
};
}
