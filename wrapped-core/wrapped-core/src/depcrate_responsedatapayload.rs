// Generated macro for DataPayload (struct)
macro_rules! Depcrate_responseDataPayload {
() => {
// Module: crate::response
// Provides: {"DataPayload"}
// Dependencies: {}
# [doc = " A container for data payloads returned from a data provider."] # [doc = ""] # [doc = " [`DataPayload`] is built on top of the [`yoke`] framework, which allows for cheap, zero-copy"] # [doc = " operations on data via the use of self-references."] # [doc = ""] # [doc = " The type of the data stored in [`DataPayload`] is determined by the [`DynamicDataMarker`] type parameter."] # [doc = ""] # [doc = " ## Accessing the data"] # [doc = ""] # [doc = " To get a reference to the data inside [`DataPayload`], use [`DataPayload::get()`]. If you need"] # [doc = " to store the data for later use, you need to store the [`DataPayload`] itself, since `get` only"] # [doc = " returns a reference with an ephemeral lifetime."] # [doc = ""] # [doc = " ## Mutating the data"] # [doc = ""] # [doc = " To modify the data stored in a [`DataPayload`], use [`DataPayload::with_mut()`]."] # [doc = ""] # [doc = " ## Transforming the data to a different type"] # [doc = ""] # [doc = " To transform a [`DataPayload`] to a different type backed by the same data store (cart), use"] # [doc = " [`DataPayload::map_project()`] or one of its sister methods."] # [doc = ""] # [doc = " # Cargo feature: `sync`"] # [doc = ""] # [doc = " By default, the payload uses non-concurrent reference counting internally, and hence is neither"] # [doc = " [`Sync`] nor [`Send`]; if these traits are required, the `sync` Cargo feature can be enabled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage, using the `HelloWorldV1` marker:"] # [doc = ""] # [doc = " ```"] # [doc = " use icu_provider::hello_world::*;"] # [doc = " use icu_provider::prelude::*;"] # [doc = " use std::borrow::Cow;"] # [doc = ""] # [doc = " let payload = DataPayload::<HelloWorldV1>::from_owned(HelloWorld {"] # [doc = "     message: Cow::Borrowed(\"Demo\"),"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!(\"Demo\", payload.get().message);"] # [doc = " ```"] pub struct DataPayload < M : DynamicDataMarker > (pub (crate) DataPayloadInner < M >) ;
};
}
