// Generated macro for macro_11 (macro)
macro_rules! Depcrate_abimacro_11 {
() => {
// Module: crate::abi
// Provides: {"macro_11"}
// Dependencies: {}
def_instruction ! { # [derive (Debug)] pub enum Instruction <'a > { # [doc = " Acquires the specified parameter and places it on the stack."] # [doc = " Depending on the context this may refer to wasm parameters or"] # [doc = " interface types parameters."] GetArg { nth : usize } : [0] => [1] , # [doc = " Pushes the constant `val` onto the stack."] I32Const { val : i32 } : [0] => [1] , # [doc = " Casts the top N items on the stack using the `Bitcast` enum"] # [doc = " provided. Consumes the same number of operands that this produces."] Bitcasts { casts : &'a [Bitcast] } : [casts . len ()] => [casts . len ()] , # [doc = " Pushes a number of constant zeros for each wasm type on the stack."] ConstZero { tys : &'a [WasmType] } : [0] => [tys . len ()] , # [doc = " Pops a pointer from the stack and loads a little-endian `i32` from"] # [doc = " it, using the specified constant offset."] I32Load { offset : ArchitectureSize } : [1] => [1] , # [doc = " Pops a pointer from the stack and loads a little-endian `i8` from"] # [doc = " it, using the specified constant offset. The value loaded is the"] # [doc = " zero-extended to 32-bits"] I32Load8U { offset : ArchitectureSize } : [1] => [1] , # [doc = " Pops a pointer from the stack and loads a little-endian `i8` from"] # [doc = " it, using the specified constant offset. The value loaded is the"] # [doc = " sign-extended to 32-bits"] I32Load8S { offset : ArchitectureSize } : [1] => [1] , # [doc = " Pops a pointer from the stack and loads a little-endian `i16` from"] # [doc = " it, using the specified constant offset. The value loaded is the"] # [doc = " zero-extended to 32-bits"] I32Load16U { offset : ArchitectureSize } : [1] => [1] , # [doc = " Pops a pointer from the stack and loads a little-endian `i16` from"] # [doc = " it, using the specified constant offset. The value loaded is the"] # [doc = " sign-extended to 32-bits"] I32Load16S { offset : ArchitectureSize } : [1] => [1] , # [doc = " Pops a pointer from the stack and loads a little-endian `i64` from"] # [doc = " it, using the specified constant offset."] I64Load { offset : ArchitectureSize } : [1] => [1] , # [doc = " Pops a pointer from the stack and loads a little-endian `f32` from"] # [doc = " it, using the specified constant offset."] F32Load { offset : ArchitectureSize } : [1] => [1] , # [doc = " Pops a pointer from the stack and loads a little-endian `f64` from"] # [doc = " it, using the specified constant offset."] F64Load { offset : ArchitectureSize } : [1] => [1] , # [doc = " Like `I32Load` or `I64Load`, but for loading pointer values."] PointerLoad { offset : ArchitectureSize } : [1] => [1] , # [doc = " Like `I32Load` or `I64Load`, but for loading array length values."] LengthLoad { offset : ArchitectureSize } : [1] => [1] , # [doc = " Pops a pointer from the stack and then an `i32` value."] # [doc = " Stores the value in little-endian at the pointer specified plus the"] # [doc = " constant `offset`."] I32Store { offset : ArchitectureSize } : [2] => [0] , # [doc = " Pops a pointer from the stack and then an `i32` value."] # [doc = " Stores the low 8 bits of the value in little-endian at the pointer"] # [doc = " specified plus the constant `offset`."] I32Store8 { offset : ArchitectureSize } : [2] => [0] , # [doc = " Pops a pointer from the stack and then an `i32` value."] # [doc = " Stores the low 16 bits of the value in little-endian at the pointer"] # [doc = " specified plus the constant `offset`."] I32Store16 { offset : ArchitectureSize } : [2] => [0] , # [doc = " Pops a pointer from the stack and then an `i64` value."] # [doc = " Stores the value in little-endian at the pointer specified plus the"] # [doc = " constant `offset`."] I64Store { offset : ArchitectureSize } : [2] => [0] , # [doc = " Pops a pointer from the stack and then an `f32` value."] # [doc = " Stores the value in little-endian at the pointer specified plus the"] # [doc = " constant `offset`."] F32Store { offset : ArchitectureSize } : [2] => [0] , # [doc = " Pops a pointer from the stack and then an `f64` value."] # [doc = " Stores the value in little-endian at the pointer specified plus the"] # [doc = " constant `offset`."] F64Store { offset : ArchitectureSize } : [2] => [0] , # [doc = " Like `I32Store` or `I64Store`, but for storing pointer values."] PointerStore { offset : ArchitectureSize } : [2] => [0] , # [doc = " Like `I32Store` or `I64Store`, but for storing array length values."] LengthStore { offset : ArchitectureSize } : [2] => [0] , # [doc = " Converts an interface type `char` value to a 32-bit integer"] # [doc = " representing the unicode scalar value."] I32FromChar : [1] => [1] , # [doc = " Converts an interface type `u64` value to a wasm `i64`."] I64FromU64 : [1] => [1] , # [doc = " Converts an interface type `s64` value to a wasm `i64`."] I64FromS64 : [1] => [1] , # [doc = " Converts an interface type `u32` value to a wasm `i32`."] I32FromU32 : [1] => [1] , # [doc = " Converts an interface type `s32` value to a wasm `i32`."] I32FromS32 : [1] => [1] , # [doc = " Converts an interface type `u16` value to a wasm `i32`."] I32FromU16 : [1] => [1] , # [doc = " Converts an interface type `s16` value to a wasm `i32`."] I32FromS16 : [1] => [1] , # [doc = " Converts an interface type `u8` value to a wasm `i32`."] I32FromU8 : [1] => [1] , # [doc = " Converts an interface type `s8` value to a wasm `i32`."] I32FromS8 : [1] => [1] , # [doc = " Conversion an interface type `f32` value to a wasm `f32`."] # [doc = ""] # [doc = " This may be a noop for some implementations, but it's here in case the"] # [doc = " native language representation of `f32` is different than the wasm"] # [doc = " representation of `f32`."] CoreF32FromF32 : [1] => [1] , # [doc = " Conversion an interface type `f64` value to a wasm `f64`."] # [doc = ""] # [doc = " This may be a noop for some implementations, but it's here in case the"] # [doc = " native language representation of `f64` is different than the wasm"] # [doc = " representation of `f64`."] CoreF64FromF64 : [1] => [1] , # [doc = " Converts a native wasm `i32` to an interface type `s8`."] # [doc = ""] # [doc = " This will truncate the upper bits of the `i32`."] S8FromI32 : [1] => [1] , # [doc = " Converts a native wasm `i32` to an interface type `u8`."] # [doc = ""] # [doc = " This will truncate the upper bits of the `i32`."] U8FromI32 : [1] => [1] , # [doc = " Converts a native wasm `i32` to an interface type `s16`."] # [doc = ""] # [doc = " This will truncate the upper bits of the `i32`."] S16FromI32 : [1] => [1] , # [doc = " Converts a native wasm `i32` to an interface type `u16`."] # [doc = ""] # [doc = " This will truncate the upper bits of the `i32`."] U16FromI32 : [1] => [1] , # [doc = " Converts a native wasm `i32` to an interface type `s32`."] S32FromI32 : [1] => [1] , # [doc = " Converts a native wasm `i32` to an interface type `u32`."] U32FromI32 : [1] => [1] , # [doc = " Converts a native wasm `i64` to an interface type `s64`."] S64FromI64 : [1] => [1] , # [doc = " Converts a native wasm `i64` to an interface type `u64`."] U64FromI64 : [1] => [1] , # [doc = " Converts a native wasm `i32` to an interface type `char`."] # [doc = ""] # [doc = " It's safe to assume that the `i32` is indeed a valid unicode code point."] CharFromI32 : [1] => [1] , # [doc = " Converts a native wasm `f32` to an interface type `f32`."] F32FromCoreF32 : [1] => [1] , # [doc = " Converts a native wasm `f64` to an interface type `f64`."] F64FromCoreF64 : [1] => [1] , # [doc = " Creates a `bool` from an `i32` input, trapping if the `i32` isn't"] # [doc = " zero or one."] BoolFromI32 : [1] => [1] , # [doc = " Creates an `i32` from a `bool` input, must return 0 or 1."] I32FromBool : [1] => [1] , # [doc = " Lowers a list where the element's layout in the native language is"] # [doc = " expected to match the canonical ABI definition of interface types."] # [doc = ""] # [doc = " Pops a list value from the stack and pushes the pointer/length onto"] # [doc = " the stack. If `realloc` is set to `Some` then this is expected to"] # [doc = " *consume* the list which means that the data needs to be copied. An"] # [doc = " allocation/copy is expected when:"] # [doc = ""] # [doc = " * A host is calling a wasm export with a list (it needs to copy the"] # [doc = "   list in to the callee's module, allocating space with `realloc`)"] # [doc = " * A wasm export is returning a list (it's expected to use `realloc`"] # [doc = "   to give ownership of the list to the caller."] # [doc = " * A host is returning a list in a import definition, meaning that"] # [doc = "   space needs to be allocated in the caller with `realloc`)."] # [doc = ""] # [doc = " A copy does not happen (e.g. `realloc` is `None`) when:"] # [doc = ""] # [doc = " * A wasm module calls an import with the list. In this situation"] # [doc = "   it's expected the caller will know how to access this module's"] # [doc = "   memory (e.g. the host has raw access or wasm-to-wasm communication"] # [doc = "   would copy the list)."] # [doc = ""] # [doc = " If `realloc` is `Some` then the adapter is not responsible for"] # [doc = " cleaning up this list because the other end is receiving the"] # [doc = " allocation. If `realloc` is `None` then the adapter is responsible"] # [doc = " for cleaning up any temporary allocation it created, if any."] ListCanonLower { element : &'a Type , realloc : Option <&'a str >, } : [1] => [2] , # [doc = " Same as `ListCanonLower`, but used for strings"] StringLower { realloc : Option <&'a str >, } : [1] => [2] , # [doc = " Lowers a list where the element's layout in the native language is"] # [doc = " not expected to match the canonical ABI definition of interface"] # [doc = " types."] # [doc = ""] # [doc = " Pops a list value from the stack and pushes the pointer/length onto"] # [doc = " the stack. This operation also pops a block from the block stack"] # [doc = " which is used as the iteration body of writing each element of the"] # [doc = " list consumed."] # [doc = ""] # [doc = " The `realloc` field here behaves the same way as `ListCanonLower`."] # [doc = " It's only set to `None` when a wasm module calls a declared import."] # [doc = " Otherwise lowering in other contexts requires allocating memory for"] # [doc = " the receiver to own."] ListLower { element : &'a Type , realloc : Option <&'a str >, } : [1] => [2] , # [doc = " Lifts a list which has a canonical representation into an interface"] # [doc = " types value."] # [doc = ""] # [doc = " The term \"canonical\" representation here means that the"] # [doc = " representation of the interface types value in the native language"] # [doc = " exactly matches the canonical ABI definition of the type."] # [doc = ""] # [doc = " This will consume two `i32` values from the stack, a pointer and a"] # [doc = " length, and then produces an interface value list."] ListCanonLift { element : &'a Type , ty : TypeId , } : [2] => [1] , # [doc = " Same as `ListCanonLift`, but used for strings"] StringLift : [2] => [1] , # [doc = " Lifts a list which into an interface types value."] # [doc = ""] # [doc = " This will consume two `i32` values from the stack, a pointer and a"] # [doc = " length, and then produces an interface value list."] # [doc = ""] # [doc = " This will also pop a block from the block stack which is how to"] # [doc = " read each individual element from the list."] ListLift { element : &'a Type , ty : TypeId , } : [2] => [1] , # [doc = " Pushes an operand onto the stack representing the list item from"] # [doc = " each iteration of the list."] # [doc = ""] # [doc = " This is only used inside of blocks related to lowering lists."] IterElem { element : &'a Type } : [0] => [1] , # [doc = " Pushes an operand onto the stack representing the base pointer of"] # [doc = " the next element in a list."] # [doc = ""] # [doc = " This is used for both lifting and lowering lists."] IterBasePointer : [0] => [1] , # [doc = " Pops a record value off the stack, decomposes the record to all of"] # [doc = " its fields, and then pushes the fields onto the stack."] RecordLower { record : &'a Record , name : &'a str , ty : TypeId , } : [1] => [record . fields . len ()] , # [doc = " Pops all fields for a record off the stack and then composes them"] # [doc = " into a record."] RecordLift { record : &'a Record , name : &'a str , ty : TypeId , } : [record . fields . len ()] => [1] , # [doc = " Create an `i32` from a handle."] HandleLower { handle : &'a Handle , name : &'a str , ty : TypeId , } : [1] => [1] , # [doc = " Create a handle from an `i32`."] HandleLift { handle : &'a Handle , name : &'a str , ty : TypeId , } : [1] => [1] , # [doc = " Create an `i32` from a future."] FutureLower { payload : &'a Option < Type >, ty : TypeId , } : [1] => [1] , # [doc = " Create a future from an `i32`."] FutureLift { payload : &'a Option < Type >, ty : TypeId , } : [1] => [1] , # [doc = " Create an `i32` from a stream."] StreamLower { payload : &'a Option < Type >, ty : TypeId , } : [1] => [1] , # [doc = " Create a stream from an `i32`."] StreamLift { payload : &'a Option < Type >, ty : TypeId , } : [1] => [1] , # [doc = " Create an `i32` from an error-context."] ErrorContextLower : [1] => [1] , # [doc = " Create a error-context from an `i32`."] ErrorContextLift : [1] => [1] , # [doc = " Pops a tuple value off the stack, decomposes the tuple to all of"] # [doc = " its fields, and then pushes the fields onto the stack."] TupleLower { tuple : &'a Tuple , ty : TypeId , } : [1] => [tuple . types . len ()] , # [doc = " Pops all fields for a tuple off the stack and then composes them"] # [doc = " into a tuple."] TupleLift { tuple : &'a Tuple , ty : TypeId , } : [tuple . types . len ()] => [1] , # [doc = " Converts a language-specific record-of-bools to a list of `i32`."] FlagsLower { flags : &'a Flags , name : &'a str , ty : TypeId , } : [1] => [flags . repr () . count ()] , # [doc = " Converts a list of native wasm `i32` to a language-specific"] # [doc = " record-of-bools."] FlagsLift { flags : &'a Flags , name : &'a str , ty : TypeId , } : [flags . repr () . count ()] => [1] , # [doc = " This is a special instruction used for `VariantLower`"] # [doc = " instruction to determine the name of the payload, if present, to use"] # [doc = " within each block."] # [doc = ""] # [doc = " Each sub-block will have this be the first instruction, and if it"] # [doc = " lowers a payload it will expect something bound to this name."] VariantPayloadName : [0] => [1] , # [doc = " Pops a variant off the stack as well as `ty.cases.len()` blocks"] # [doc = " from the code generator. Uses each of those blocks and the value"] # [doc = " from the stack to produce `nresults` of items."] VariantLower { variant : &'a Variant , name : &'a str , ty : TypeId , results : &'a [WasmType] , } : [1] => [results . len ()] , # [doc = " Pops an `i32` off the stack as well as `ty.cases.len()` blocks"] # [doc = " from the code generator. Uses each of those blocks and the value"] # [doc = " from the stack to produce a final variant."] VariantLift { variant : &'a Variant , name : &'a str , ty : TypeId , } : [1] => [1] , # [doc = " Pops an enum off the stack and pushes the `i32` representation."] EnumLower { enum_ : &'a Enum , name : &'a str , ty : TypeId , } : [1] => [1] , # [doc = " Pops an `i32` off the stack and lifts it into the `enum` specified."] EnumLift { enum_ : &'a Enum , name : &'a str , ty : TypeId , } : [1] => [1] , # [doc = " Specialization of `VariantLower` for specifically `option<T>` types,"] # [doc = " otherwise behaves the same as `VariantLower` (e.g. two blocks for"] # [doc = " the two cases."] OptionLower { payload : &'a Type , ty : TypeId , results : &'a [WasmType] , } : [1] => [results . len ()] , # [doc = " Specialization of `VariantLift` for specifically the `option<T>`"] # [doc = " type. Otherwise behaves the same as the `VariantLift` instruction"] # [doc = " with two blocks for the lift."] OptionLift { payload : &'a Type , ty : TypeId , } : [1] => [1] , # [doc = " Specialization of `VariantLower` for specifically `result<T, E>`"] # [doc = " types, otherwise behaves the same as `VariantLower` (e.g. two blocks"] # [doc = " for the two cases."] ResultLower { result : &'a Result_ ty : TypeId , results : &'a [WasmType] , } : [1] => [results . len ()] , # [doc = " Specialization of `VariantLift` for specifically the `result<T,"] # [doc = " E>` type. Otherwise behaves the same as the `VariantLift`"] # [doc = " instruction with two blocks for the lift."] ResultLift { result : &'a Result_ , ty : TypeId , } : [1] => [1] , # [doc = " Represents a call to a raw WebAssembly API. The module/name are"] # [doc = " provided inline as well as the types if necessary."] CallWasm { name : &'a str , sig : &'a WasmSignature , } : [sig . params . len ()] => [sig . results . len ()] , # [doc = " Same as `CallWasm`, except the dual where an interface is being"] # [doc = " called rather than a raw wasm function."] # [doc = ""] # [doc = " Note that this will be used for async functions, and `async_`"] # [doc = " indicates whether the function should be invoked in an async"] # [doc = " fashion."] CallInterface { func : &'a Function , async_ : bool , } : [func . params . len ()] => [usize :: from (func . result . is_some ())] , # [doc = " Returns `amt` values on the stack. This is always the last"] # [doc = " instruction."] Return { amt : usize , func : &'a Function } : [* amt] => [0] , # [doc = " Calls the `realloc` function specified in a malloc-like fashion"] # [doc = " allocating `size` bytes with alignment `align`."] # [doc = ""] # [doc = " Pushes the returned pointer onto the stack."] Malloc { realloc : &'static str , size : ArchitectureSize , align : Alignment , } : [0] => [1] , # [doc = " Used exclusively for guest-code generation this indicates that"] # [doc = " the standard memory deallocation function needs to be invoked with"] # [doc = " the specified parameters."] # [doc = ""] # [doc = " This will pop a pointer from the stack and push nothing."] GuestDeallocate { size : ArchitectureSize , align : Alignment , } : [1] => [0] , # [doc = " Used exclusively for guest-code generation this indicates that"] # [doc = " a string is being deallocated. The ptr/length are on the stack and"] # [doc = " are poppped off and used to deallocate the string."] GuestDeallocateString : [2] => [0] , # [doc = " Used exclusively for guest-code generation this indicates that"] # [doc = " a list is being deallocated. The ptr/length are on the stack and"] # [doc = " are poppped off and used to deallocate the list."] # [doc = ""] # [doc = " This variant also pops a block off the block stack to be used as the"] # [doc = " body of the deallocation loop."] GuestDeallocateList { element : &'a Type , } : [2] => [0] , # [doc = " Used exclusively for guest-code generation this indicates that"] # [doc = " a variant is being deallocated. The integer discriminant is popped"] # [doc = " off the stack as well as `blocks` number of blocks popped from the"] # [doc = " blocks stack. The variant is used to select, at runtime, which of"] # [doc = " the blocks is executed to deallocate the variant."] GuestDeallocateVariant { blocks : usize , } : [1] => [0] , # [doc = " Deallocates the language-specific handle representation on the top"] # [doc = " of the stack. Used for async imports."] DropHandle { ty : &'a Type } : [1] => [0] , # [doc = " Call `task.return` for an async-lifted export."] # [doc = ""] # [doc = " This will call core wasm import `name` which will be mapped to"] # [doc = " `task.return` later on. The function given has `params` as its"] # [doc = " parameters and it will return no results. This is used to pass the"] # [doc = " lowered representation of a function's results to `task.return`."] AsyncTaskReturn { name : &'a str , params : &'a [WasmType] } : [params . len ()] => [0] , # [doc = " Force the evaluation of the specified number of expressions and push"] # [doc = " the results to the stack."] # [doc = ""] # [doc = " This is useful prior to disposing of temporary variables and/or"] # [doc = " allocations which are referenced by one or more not-yet-evaluated"] # [doc = " expressions."] Flush { amt : usize } : [* amt] => [* amt] , } }
};
}
