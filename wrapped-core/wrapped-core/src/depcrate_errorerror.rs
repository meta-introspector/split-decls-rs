// Generated macro for Error (struct)
macro_rules! Depcrate_errorError {
() => {
// Module: crate::error
// Provides: {"Error"}
// Dependencies: {}
# [doc = " An error encountered during attribute parsing."] # [doc = ""] # [doc = " Given that most errors darling encounters represent code bugs in dependent crates,"] # [doc = " the internal structure of the error is deliberately opaque."] # [doc = ""] # [doc = " # Usage"] # [doc = " Proc-macro expansion happens very infrequently compared to runtime tasks such as"] # [doc = " deserialization, and it happens in the context of an expensive compilation taks."] # [doc = " For that reason, darling prefers not to fail on the first error it encounters, instead"] # [doc = " doing as much work as it can, accumulating errors into a single report."] # [doc = ""] # [doc = " As a result, `darling::Error` is more of guaranteed-non-empty error collection"] # [doc = " than a single problem. These errors also have some notion of hierarchy, stemming from"] # [doc = " the hierarchical nature of darling's input."] # [doc = ""] # [doc = " These characteristics make for great experiences when using darling-powered crates,"] # [doc = " provided crates using darling adhere to some best practices:"] # [doc = ""] # [doc = " 1. Do not attempt to simplify a `darling::Error` into some other error type, such as"] # [doc = "    `syn::Error`. To surface compile errors, instead use `darling::Error::write_errors`."] # [doc = "    This preserves all span information, suggestions, etc. Wrapping a `darling::Error` in"] # [doc = "    a custom error enum works as-expected and does not force any loss of fidelity."] # [doc = " 2. Do not use early return (e.g. the `?` operator) for custom validations. Instead,"] # [doc = "    create an [`error::Accumulator`](Accumulator) to collect errors as they are encountered.  Then use"] # [doc = "    [`Accumulator::finish`] to return your validated result; it will give `Ok` if and only if"] # [doc = "    no errors were encountered.  This can create very complex custom validation functions;"] # [doc = "    in those cases, split independent \"validation chains\" out into their own functions to"] # [doc = "    keep the main validator manageable."] # [doc = " 3. Use `darling::Error::custom` to create additional errors as-needed, then call `with_span`"] # [doc = "    to ensure those errors appear in the right place. Use `darling::util::SpannedValue` to keep"] # [doc = "    span information around on parsed fields so that custom diagnostics can point to the correct"] # [doc = "    parts of the input AST."] # [derive (Debug , Clone)] pub struct Error { kind : ErrorKind , locations : Vec < String > , # [doc = " The span to highlight in the emitted diagnostic."] span : Option < Span > , # [doc = " Additional diagnostic messages to show with the error."] # [cfg (feature = "diagnostics")] children : Vec < child :: ChildDiagnostic > , }
};
}
