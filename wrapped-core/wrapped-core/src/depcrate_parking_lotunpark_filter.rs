// Generated macro for unpark_filter (function)
macro_rules! Depcrate_parking_lotunpark_filter {
() => {
// Module: crate::parking_lot
// Provides: {"unpark_filter"}
// Dependencies: {}
# [doc = " Unparks a number of threads from the front of the queue associated with"] # [doc = " `key` depending on the results of a filter function which inspects the"] # [doc = " `ParkToken` associated with each thread."] # [doc = ""] # [doc = " The `filter` function is called for each thread in the queue or until"] # [doc = " `FilterOp::Stop` is returned. This function is passed the `ParkToken`"] # [doc = " associated with a particular thread, which is unparked if `FilterOp::Unpark`"] # [doc = " is returned."] # [doc = ""] # [doc = " The `callback` function is also called while both queues are locked. It is"] # [doc = " passed an `UnparkResult` indicating the number of threads that were unparked"] # [doc = " and whether there are still parked threads in the queue. This `UnparkResult`"] # [doc = " value is also returned by `unpark_filter`."] # [doc = ""] # [doc = " The `callback` function should return an `UnparkToken` value which will be"] # [doc = " passed to all threads that are unparked. If no thread is unparked then the"] # [doc = " returned value is ignored."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " You should only call this function with an address that you control, since"] # [doc = " you could otherwise interfere with the operation of other synchronization"] # [doc = " primitives."] # [doc = ""] # [doc = " The `filter` and `callback` functions are called while the queue is locked"] # [doc = " and must not panic or call into any function in `parking_lot`."] # [inline] pub unsafe fn unpark_filter (key : usize , mut filter : impl FnMut (ParkToken) -> FilterOp , callback : impl FnOnce (UnparkResult) -> UnparkToken ,) -> UnparkResult { let bucket = lock_bucket (key) ; let mut link = & bucket . queue_head ; let mut current = bucket . queue_head . get () ; let mut previous = ptr :: null () ; let mut threads = SmallVec :: < (* const ThreadData , Option < < ThreadParker as ThreadParkerT > :: UnparkHandle >) , 8 > :: new () ; let mut result = UnparkResult :: default () ; while ! current . is_null () { if (* current) . key . load (Ordering :: Relaxed) == key { let next = (* current) . next_in_queue . get () ; match filter ((* current) . park_token . get ()) { FilterOp :: Unpark => { link . set (next) ; if bucket . queue_tail . get () == current { bucket . queue_tail . set (previous) ; } threads . push ((current , None)) ; current = next ; } FilterOp :: Skip => { result . have_more_threads = true ; link = & (* current) . next_in_queue ; previous = current ; current = link . get () ; } FilterOp :: Stop => { result . have_more_threads = true ; break ; } } } else { link = & (* current) . next_in_queue ; previous = current ; current = link . get () ; } } result . unparked_threads = threads . len () ; if result . unparked_threads != 0 { result . be_fair = (* bucket . fair_timeout . get ()) . should_timeout () ; } let token = callback (result) ; for t in threads . iter_mut () { (* t . 0) . unpark_token . set (token) ; t . 1 = Some ((* t . 0) . parker . unpark_lock ()) ; } bucket . mutex . unlock () ; for (_ , handle) in threads . into_iter () { handle . unchecked_unwrap () . unpark () ; } result }
};
}
