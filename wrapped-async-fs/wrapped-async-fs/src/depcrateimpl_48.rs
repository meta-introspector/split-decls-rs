// Generated macro for impl_48 (impl)
macro_rules! Depcrateimpl_48 {
() => {
// Module: crate
// Provides: {"impl_48"}
// Dependencies: {}
impl File { # [doc = " Creates an async file from a blocking file."] fn new (inner : std :: fs :: File , is_dirty : bool) -> File { let file = Arc :: new (inner) ; let unblock = Mutex :: new (Unblock :: new (ArcFile (file . clone ()))) ; let read_pos = None ; File { file , unblock , read_pos , is_dirty , } } # [doc = " Opens a file in read-only mode."] # [doc = ""] # [doc = " See the [`OpenOptions::open()`] function for more options."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " An error will be returned in the following situations:"] # [doc = ""] # [doc = " * `path` does not point to an existing file."] # [doc = " * The current process lacks permissions to read the file."] # [doc = " * Some other I/O error occurred."] # [doc = ""] # [doc = " For more details, see the list of errors documented by [`OpenOptions::open()`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_fs::File;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let file = File::open(\"a.txt\").await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn open < P : AsRef < Path > > (path : P) -> io :: Result < File > { let path = path . as_ref () . to_owned () ; let file = unblock (move | | std :: fs :: File :: open (path)) . await ? ; Ok (File :: new (file , false)) } # [doc = " Opens a file in write-only mode."] # [doc = ""] # [doc = " This method will create a file if it does not exist, and will truncate it if it does."] # [doc = ""] # [doc = " See the [`OpenOptions::open`] function for more options."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " An error will be returned in the following situations:"] # [doc = ""] # [doc = " * The file's parent directory does not exist."] # [doc = " * The current process lacks permissions to write to the file."] # [doc = " * Some other I/O error occurred."] # [doc = ""] # [doc = " For more details, see the list of errors documented by [`OpenOptions::open()`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_fs::File;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let file = File::create(\"a.txt\").await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn create < P : AsRef < Path > > (path : P) -> io :: Result < File > { let path = path . as_ref () . to_owned () ; let file = unblock (move | | std :: fs :: File :: create (path)) . await ? ; Ok (File :: new (file , false)) } # [doc = " Synchronizes OS-internal buffered contents and metadata to disk."] # [doc = ""] # [doc = " This function will ensure that all in-memory data reaches the filesystem."] # [doc = ""] # [doc = " This can be used to handle errors that would otherwise only be caught by closing the file."] # [doc = " When a file is dropped, errors in synchronizing this in-memory data are ignored."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_fs::File;"] # [doc = " use futures_lite::io::AsyncWriteExt;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut file = File::create(\"a.txt\").await?;"] # [doc = ""] # [doc = " file.write_all(b\"Hello, world!\").await?;"] # [doc = " file.sync_all().await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn sync_all (& self) -> io :: Result < () > { let mut inner = self . unblock . lock () . await ; inner . flush () . await ? ; let file = self . file . clone () ; unblock (move | | file . sync_all ()) . await } # [doc = " Synchronizes OS-internal buffered contents to disk."] # [doc = ""] # [doc = " This is similar to [`sync_all()`][`File::sync_all()`], except that file metadata may not"] # [doc = " be synchronized."] # [doc = ""] # [doc = " This is intended for use cases that must synchronize the contents of the file, but don't"] # [doc = " need the file metadata synchronized to disk."] # [doc = ""] # [doc = " Note that some platforms may simply implement this in terms of"] # [doc = " [`sync_all()`][`File::sync_all()`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_fs::File;"] # [doc = " use futures_lite::io::AsyncWriteExt;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut file = File::create(\"a.txt\").await?;"] # [doc = ""] # [doc = " file.write_all(b\"Hello, world!\").await?;"] # [doc = " file.sync_data().await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn sync_data (& self) -> io :: Result < () > { let mut inner = self . unblock . lock () . await ; inner . flush () . await ? ; let file = self . file . clone () ; unblock (move | | file . sync_data ()) . await } # [doc = " Truncates or extends the file."] # [doc = ""] # [doc = " If `size` is less than the current file size, then the file will be truncated. If it is"] # [doc = " greater than the current file size, then the file will be extended to `size` and have all"] # [doc = " intermediate data filled with zeros."] # [doc = ""] # [doc = " The file's cursor stays at the same position, even if the cursor ends up being past the end"] # [doc = " of the file after this operation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_fs::File;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut file = File::create(\"a.txt\").await?;"] # [doc = " file.set_len(10).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn set_len (& self , size : u64) -> io :: Result < () > { let mut inner = self . unblock . lock () . await ; inner . flush () . await ? ; let file = self . file . clone () ; unblock (move | | file . set_len (size)) . await } # [doc = " Reads the file's metadata."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_fs::File;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let file = File::open(\"a.txt\").await?;"] # [doc = " let metadata = file.metadata().await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn metadata (& self) -> io :: Result < Metadata > { let file = self . file . clone () ; unblock (move | | file . metadata ()) . await } # [doc = " Changes the permissions on the file."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " An error will be returned in the following situations:"] # [doc = ""] # [doc = " * The current process lacks permissions to change attributes on the file."] # [doc = " * Some other I/O error occurred."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_fs::File;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let file = File::create(\"a.txt\").await?;"] # [doc = ""] # [doc = " let mut perms = file.metadata().await?.permissions();"] # [doc = " perms.set_readonly(true);"] # [doc = " file.set_permissions(perms).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn set_permissions (& self , perm : Permissions) -> io :: Result < () > { let file = self . file . clone () ; unblock (move | | file . set_permissions (perm)) . await } # [doc = " Repositions the cursor after reading."] # [doc = ""] # [doc = " When reading from a file, actual file reads run asynchronously in the background, which"] # [doc = " means the real file cursor is usually ahead of the logical cursor, and the data between"] # [doc = " them is buffered in memory. This kind of buffering is an important optimization."] # [doc = ""] # [doc = " After reading ends, if we decide to perform a write or a seek operation, the real file"] # [doc = " cursor must first be repositioned back to the correct logical position."] fn poll_reposition (& mut self , cx : & mut Context < '_ >) -> Poll < io :: Result < () > > { if let Some (Ok (read_pos)) = self . read_pos { ready ! (Pin :: new (self . unblock . get_mut ()) . poll_seek (cx , SeekFrom :: Start (read_pos))) ? ; } self . read_pos = None ; Poll :: Ready (Ok (())) } }
};
}
