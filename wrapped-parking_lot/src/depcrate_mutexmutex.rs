// Generated macro for Mutex (type)
macro_rules! Depcrate_mutexMutex {
() => {
// Module: crate::mutex
// Provides: {"Mutex"}
// Dependencies: {}
# [doc = " A mutual exclusion primitive useful for protecting shared data"] # [doc = ""] # [doc = " This mutex will block threads waiting for the lock to become available. The"] # [doc = " mutex can be statically initialized or created by the `new`"] # [doc = " constructor. Each mutex has a type parameter which represents the data that"] # [doc = " it is protecting. The data can only be accessed through the RAII guards"] # [doc = " returned from `lock` and `try_lock`, which guarantees that the data is only"] # [doc = " ever accessed when the mutex is locked."] # [doc = ""] # [doc = " # Fairness"] # [doc = ""] # [doc = " A typical unfair lock can often end up in a situation where a single thread"] # [doc = " quickly acquires and releases the same mutex in succession, which can starve"] # [doc = " other threads waiting to acquire the mutex. While this improves throughput"] # [doc = " because it doesn't force a context switch when a thread tries to re-acquire"] # [doc = " a mutex it has just released, this can starve other threads."] # [doc = ""] # [doc = " This mutex uses [eventual fairness](https://trac.webkit.org/changeset/203350)"] # [doc = " to ensure that the lock will be fair on average without sacrificing"] # [doc = " throughput. This is done by forcing a fair unlock on average every 0.5ms,"] # [doc = " which will force the lock to go to the next thread waiting for the mutex."] # [doc = ""] # [doc = " Additionally, any critical section longer than 1ms will always use a fair"] # [doc = " unlock, which has a negligible impact on throughput considering the length"] # [doc = " of the critical section."] # [doc = ""] # [doc = " You can also force a fair unlock by calling `MutexGuard::unlock_fair` when"] # [doc = " unlocking a mutex instead of simply dropping the `MutexGuard`."] # [doc = ""] # [doc = " # Differences from the standard library `Mutex`"] # [doc = ""] # [doc = " - No poisoning, the lock is released normally on panic."] # [doc = " - Only requires 1 byte of space, whereas the standard library boxes the"] # [doc = "   `Mutex` due to platform limitations."] # [doc = " - Can be statically constructed."] # [doc = " - Does not require any drop glue when dropped."] # [doc = " - Inline fast path for the uncontended case."] # [doc = " - Efficient handling of micro-contention using adaptive spinning."] # [doc = " - Allows raw locking & unlocking without a guard."] # [doc = " - Supports eventual fairness so that the mutex is fair on average."] # [doc = " - Optionally allows making the mutex fair by calling `MutexGuard::unlock_fair`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use parking_lot::Mutex;"] # [doc = " use std::sync::{Arc, mpsc::channel};"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " const N: usize = 10;"] # [doc = ""] # [doc = " // Spawn a few threads to increment a shared variable (non-atomically), and"] # [doc = " // let the main thread know once all increments are done."] # [doc = " //"] # [doc = " // Here we're using an Arc to share memory among threads, and the data inside"] # [doc = " // the Arc is protected with a mutex."] # [doc = " let data = Arc::new(Mutex::new(0));"] # [doc = ""] # [doc = " let (tx, rx) = channel();"] # [doc = " for _ in 0..10 {"] # [doc = "     let (data, tx) = (Arc::clone(&data), tx.clone());"] # [doc = "     thread::spawn(move || {"] # [doc = "         // The shared state can only be accessed once the lock is held."] # [doc = "         // Our non-atomic increment is safe because we're the only thread"] # [doc = "         // which can access the shared state when the lock is held."] # [doc = "         let mut data = data.lock();"] # [doc = "         *data += 1;"] # [doc = "         if *data == N {"] # [doc = "             tx.send(()).unwrap();"] # [doc = "         }"] # [doc = "         // the lock is unlocked here when `data` goes out of scope."] # [doc = "     });"] # [doc = " }"] # [doc = ""] # [doc = " rx.recv().unwrap();"] # [doc = " ```"] pub type Mutex < T > = lock_api :: Mutex < RawMutex , T > ;
};
}
