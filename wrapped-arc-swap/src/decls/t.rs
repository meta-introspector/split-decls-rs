macro_rules! deps {
    () => {
        Guard!();
        ArcSwapAny!();
        ArcSwapOption!();
        ArcSwap!();
    };
}

macro_rules! t {
    () => {
        deps!();
        macro_rules ! t { ($ name : ident , $ strategy : ty) => { # [cfg (test)] mod $ name { use alloc :: borrow :: ToOwned ; use alloc :: string :: String ; use alloc :: vec :: Vec ; use core :: sync :: atomic :: { self , AtomicUsize } ; use adaptive_barrier :: { Barrier , PanicMode } ; use crossbeam_utils :: thread ; use super ::*; const ITERATIONS : usize = 10 ; # [allow (deprecated)] type As < T > = ArcSwapAny < Arc < T >, $ strategy >; # [allow (deprecated)] type Aso < T > = ArcSwapAny < Option < Arc < T >>, $ strategy >; # [doc = " Similar to the one in doc tests of the lib, but more times and more intensive (we"] # [doc = " want to torture it a bit)."] # [test] # [cfg_attr (miri , ignore)] fn publish () { const READERS : usize = 2 ; for _ in 0 .. ITERATIONS { let config = As ::< String >:: default () ; let ended = AtomicUsize :: new (0) ; thread :: scope (| scope | { for _ in 0 .. READERS { scope . spawn (| _ | loop { let cfg = config . load_full () ; if ! cfg . is_empty () { assert_eq ! (* cfg , "New configuration") ; ended . fetch_add (1 , Ordering :: Relaxed) ; return ; } atomic :: spin_loop_hint () ; }) ; } scope . spawn (| _ | { let new_conf = Arc :: new ("New configuration" . to_owned ()) ; config . store (new_conf) ; }) ; }) . unwrap () ; assert_eq ! (READERS , ended . load (Ordering :: Relaxed)) ; let arc = config . load_full () ; assert_eq ! (2 , Arc :: strong_count (& arc)) ; assert_eq ! (0 , Arc :: weak_count (& arc)) ; } } # [doc = " Similar to the doc tests of ArcSwap, but happens more times."] # [test] fn swap_load () { for _ in 0 .. 100 { let arc = Arc :: new (42) ; let arc_swap = As :: from (Arc :: clone (& arc)) ; assert_eq ! (42 , ** arc_swap . load ()) ; assert_eq ! (42 , ** arc_swap . load ()) ; let new_arc = Arc :: new (0) ; assert_eq ! (42 , * arc_swap . swap (Arc :: clone (& new_arc))) ; assert_eq ! (0 , ** arc_swap . load ()) ; let loaded = arc_swap . load_full () ; assert_eq ! (3 , Arc :: strong_count (& loaded)) ; assert_eq ! (0 , Arc :: weak_count (& loaded)) ; assert_eq ! (1 , Arc :: strong_count (& arc)) ; assert_eq ! (0 , Arc :: weak_count (& arc)) ; } } # [doc = " Two different writers publish two series of values. The readers check that it is"] # [doc = " always increasing in each series."] # [doc = ""] # [doc = " For performance, we try to reuse the threads here."] # [test] fn multi_writers () { let first_value = Arc :: new ((0 , 0)) ; let shared = As :: from (Arc :: clone (& first_value)) ; const WRITER_CNT : usize = 2 ; const READER_CNT : usize = 3 ; # [cfg (miri)] const ITERATIONS : usize = 5 ; # [cfg (not (miri))] const ITERATIONS : usize = 100 ; const SEQ : usize = 50 ; let barrier = Barrier :: new (PanicMode :: Poison) ; thread :: scope (| scope | { for w in 0 .. WRITER_CNT { let mut barrier = barrier . clone () ; let shared = & shared ; let first_value = & first_value ; scope . spawn (move | _ | { for _ in 0 .. ITERATIONS { barrier . wait () ; shared . store (Arc :: clone (& first_value)) ; barrier . wait () ; for i in 0 .. SEQ { shared . store (Arc :: new ((w , i + 1))) ; } } }) ; } for _ in 0 .. READER_CNT { let mut barrier = barrier . clone () ; let shared = & shared ; let first_value = & first_value ; scope . spawn (move | _ | { for _ in 0 .. ITERATIONS { barrier . wait () ; barrier . wait () ; let mut previous = [0 ; WRITER_CNT] ; let mut last = Arc :: clone (& first_value) ; loop { let cur = shared . load () ; if Arc :: ptr_eq (& last , & cur) { atomic :: spin_loop_hint () ; continue ; } let (w , s) = ** cur ; assert ! (previous [w] < s , "{:?} vs {:?}" , previous , cur) ; previous [w] = s ; last = Guard :: into_inner (cur) ; if s == SEQ { break ; } } } }) ; } drop (barrier) ; }) . unwrap () ; } # [test] fn load_null () { let shared = Aso ::< usize >:: default () ; let guard = shared . load () ; assert ! (guard . is_none ()) ; shared . store (Some (Arc :: new (42))) ; assert_eq ! (42 , ** shared . load () . as_ref () . unwrap ()) ; } # [test] fn from_into () { let a = Arc :: new (42) ; let shared = As :: new (a) ; let guard = shared . load () ; let a = shared . into_inner () ; assert_eq ! (42 , * a) ; assert_eq ! (2 , Arc :: strong_count (& a)) ; drop (guard) ; assert_eq ! (1 , Arc :: strong_count (& a)) ; } # [derive (Default)] struct ReportDrop (Arc < AtomicUsize >) ; impl Drop for ReportDrop { fn drop (& mut self) { self . 0 . fetch_add (1 , Ordering :: Relaxed) ; } } # [doc = " Interaction of two threads about a guard and dropping it."] # [doc = ""] # [doc = " We make sure everything works in timely manner (eg. dropping of stuff) even if multiple"] # [doc = " threads interact."] # [doc = ""] # [doc = " The idea is:"] # [doc = " * Thread 1 loads a value."] # [doc = " * Thread 2 replaces the shared value. The original value is not destroyed."] # [doc = " * Thread 1 drops the guard. The value is destroyed and this is observable in both threads."] # [test] fn guard_drop_in_thread () { for _ in 0 .. ITERATIONS { let cnt = Arc :: new (AtomicUsize :: new (0)) ; let shared = As :: from_pointee (ReportDrop (cnt . clone ())) ; assert_eq ! (cnt . load (Ordering :: Relaxed) , 0 , "Dropped prematurely") ; let sync = Barrier :: new (PanicMode :: Poison) ; thread :: scope (| scope | { scope . spawn ({ let sync = sync . clone () ; | _ | { let mut sync = sync ; let guard = shared . load () ; sync . wait () ; sync . wait () ; drop (guard) ; assert_eq ! (cnt . load (Ordering :: Relaxed) , 1 , "Value not dropped") ; sync . wait () ; } }) ; scope . spawn (| _ | { let mut sync = sync ; sync . wait () ; shared . store (Default :: default ()) ; assert_eq ! (cnt . load (Ordering :: Relaxed) , 0 , "Dropped while still in use") ; sync . wait () ; sync . wait () ; assert_eq ! (cnt . load (Ordering :: Relaxed) , 1 , "Value not dropped") ; }) ; }) . unwrap () ; } } # [doc = " Check dropping a lease in a different thread than it was created doesn't cause any"] # [doc = " problems."] # [test] fn guard_drop_in_another_thread () { for _ in 0 .. ITERATIONS { let cnt = Arc :: new (AtomicUsize :: new (0)) ; let shared = As :: from_pointee (ReportDrop (cnt . clone ())) ; assert_eq ! (cnt . load (Ordering :: Relaxed) , 0 , "Dropped prematurely") ; let guard = shared . load () ; drop (shared) ; assert_eq ! (cnt . load (Ordering :: Relaxed) , 0 , "Dropped prematurely") ; thread :: scope (| scope | { scope . spawn (| _ | { drop (guard) ; }) ; }) . unwrap () ; assert_eq ! (cnt . load (Ordering :: Relaxed) , 1 , "Not dropped") ; } } # [test] fn load_option () { let shared = Aso :: from_pointee (42) ; let opt : Option < _ > = Guard :: into_inner (shared . load ()) ; assert_eq ! (42 , * opt . unwrap ()) ; shared . store (None) ; assert ! (shared . load () . is_none ()) ; } # [test] fn debug_impl () { let shared = As :: from_pointee (42) ; assert_eq ! ("ArcSwapAny(42)" , & format ! ("{:?}" , shared)) ; assert_eq ! ("42" , & format ! ("{:?}" , shared . load ())) ; } # [test] fn display_impl () { let shared = As :: from_pointee (42) ; assert_eq ! ("42" , & format ! ("{}" , shared)) ; assert_eq ! ("42" , & format ! ("{}" , shared . load ())) ; } fn _check_stuff_is_send_sync () { let shared = As :: from_pointee (42) ; let moved = As :: from_pointee (42) ; let shared_ref = & shared ; let lease = shared . load () ; let lease_ref = & lease ; let lease = shared . load () ; thread :: scope (| s | { s . spawn (move | _ | { let _ = lease ; let _ = lease_ref ; let _ = shared_ref ; let _ = moved ; }) ; }) . unwrap () ; } # [doc = " We have a callback in RCU. Check what happens if we access the value from within."] # [test] fn recursive () { let shared = ArcSwap :: from (Arc :: new (0)) ; shared . rcu (| i | { if ** i < 10 { shared . rcu (| i | ** i + 1) ; } ** i }) ; assert_eq ! (10 , ** shared . load ()) ; assert_eq ! (2 , Arc :: strong_count (& shared . load_full ())) ; } # [doc = " A panic from within the rcu callback should not change anything."] # [test] # [cfg (not (feature = "experimental-thread-local"))] fn rcu_panic () { use std :: panic ; let shared = ArcSwap :: from (Arc :: new (0)) ; assert ! (panic :: catch_unwind (|| shared . rcu (| _ | -> usize { panic ! () })) . is_err ()) ; assert_eq ! (1 , Arc :: strong_count (& shared . swap (Arc :: new (42)))) ; } # [doc = " Handling null/none values"] # [test] fn nulls () { let shared = ArcSwapOption :: from (Some (Arc :: new (0))) ; let orig = shared . swap (None) ; assert_eq ! (1 , Arc :: strong_count (& orig . unwrap ())) ; let null = shared . load () ; assert ! (null . is_none ()) ; let a = Arc :: new (42) ; let orig = shared . compare_and_swap (ptr :: null () , Some (Arc :: clone (& a))) ; assert ! (orig . is_none ()) ; assert_eq ! (2 , Arc :: strong_count (& a)) ; let orig = Guard :: into_inner (shared . compare_and_swap (& None ::< Arc < _ >>, None)) ; assert_eq ! (3 , Arc :: strong_count (& a)) ; assert ! (ptr_eq (& a , & orig)) ; } # [test] # [doc = " Multiple RCUs interacting."] fn rcu () { const ITERATIONS : usize = 50 ; const THREADS : usize = 10 ; let shared = ArcSwap :: from (Arc :: new (0)) ; thread :: scope (| scope | { for _ in 0 .. THREADS { scope . spawn (| _ | { for _ in 0 .. ITERATIONS { shared . rcu (| old | ** old + 1) ; } }) ; } }) . unwrap () ; assert_eq ! (THREADS * ITERATIONS , ** shared . load ()) ; } # [test] # [doc = " Make sure the reference count and compare_and_swap works as expected."] fn cas_ref_cnt () { # [cfg (miri)] const ITERATIONS : usize = 10 ; # [cfg (not (miri))] const ITERATIONS : usize = 50 ; let shared = ArcSwap :: from (Arc :: new (0)) ; for i in 0 .. ITERATIONS { let orig = shared . load_full () ; assert_eq ! (i , * orig) ; if i % 2 == 1 { assert_eq ! (2 , Arc :: strong_count (& orig)) ; } let n1 = Arc :: new (i + 1) ; let fillup = || { if i % 2 == 0 { Some ((0 .. ITERATIONS) . map (| _ | shared . load ()) . collect ::< Vec < _ >> ()) } else { None } } ; let guards = fillup () ; let prev = shared . compare_and_swap (& orig , Arc :: clone (& n1)) ; assert ! (ptr_eq (& orig , & prev)) ; drop (guards) ; assert_eq ! (2 , Arc :: strong_count (& orig)) ; assert_eq ! (2 , Arc :: strong_count (& n1)) ; assert_eq ! (i + 1 , ** shared . load ()) ; let n2 = Arc :: new (i) ; drop (prev) ; let guards = fillup () ; let prev = Guard :: into_inner (shared . compare_and_swap (& orig , Arc :: clone (& n2))) ; drop (guards) ; assert ! (ptr_eq (& n1 , & prev)) ; assert_eq ! (1 , Arc :: strong_count (& orig)) ; assert_eq ! (3 , Arc :: strong_count (& n1)) ; assert_eq ! (1 , Arc :: strong_count (& n2)) ; assert_eq ! (i + 1 , ** shared . load ()) ; } let a = shared . load_full () ; assert_eq ! (2 , Arc :: strong_count (& a)) ; drop (shared) ; assert_eq ! (1 , Arc :: strong_count (& a)) ; } } } ; }
    };
}

t!()