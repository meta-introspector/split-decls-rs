// Generated macro for impl_315 (impl)
macro_rules! Depcrate_tableimpl_315 {
() => {
// Module: crate::table
// Provides: {"impl_315"}
// Dependencies: {}
impl < 'a > Table < 'a > { # [doc = " Creates a new [`Table`] widget with the given rows."] # [doc = ""] # [doc = " The `rows` parameter accepts any value that can be converted into an iterator of [`Row`]s."] # [doc = " This includes arrays, slices, and [`Vec`]s."] # [doc = ""] # [doc = " The `widths` parameter accepts any type that implements `IntoIterator<Item ="] # [doc = " Into<Constraint>>`. This includes arrays, slices, vectors, iterators. `Into<Constraint>` is"] # [doc = " implemented on u16, so you can pass an array, vec, etc. of u16 to this function to create a"] # [doc = " table with fixed width columns."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::layout::Constraint;"] # [doc = " use ratatui::widgets::{Row, Table};"] # [doc = ""] # [doc = " let rows = ["] # [doc = "     Row::new(vec![\"Cell1\", \"Cell2\"]),"] # [doc = "     Row::new(vec![\"Cell3\", \"Cell4\"]),"] # [doc = " ];"] # [doc = " let widths = [Constraint::Length(5), Constraint::Length(5)];"] # [doc = " let table = Table::new(rows, widths);"] # [doc = " ```"] pub fn new < R , C > (rows : R , widths : C) -> Self where R : IntoIterator , R :: Item : Into < Row < 'a > > , C : IntoIterator , C :: Item : Into < Constraint > , { let widths = widths . into_iter () . map (Into :: into) . collect_vec () ; ensure_percentages_less_than_100 (& widths) ; let rows = rows . into_iter () . map (Into :: into) . collect () ; Self { rows , widths , .. Default :: default () } } # [doc = " Set the rows"] # [doc = ""] # [doc = " The `rows` parameter accepts any value that can be converted into an iterator of [`Row`]s."] # [doc = " This includes arrays, slices, and [`Vec`]s."] # [doc = ""] # [doc = " # Warning"] # [doc = ""] # [doc = " This method does not currently set the column widths. You will need to set them manually by"] # [doc = " calling [`Table::widths`]."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::widgets::{Row, Table};"] # [doc = ""] # [doc = " let rows = ["] # [doc = "     Row::new(vec![\"Cell1\", \"Cell2\"]),"] # [doc = "     Row::new(vec![\"Cell3\", \"Cell4\"]),"] # [doc = " ];"] # [doc = " let table = Table::default().rows(rows);"] # [doc = " ```"] # [must_use = "method moves the value of self and returns the modified value"] pub fn rows < T > (mut self , rows : T) -> Self where T : IntoIterator < Item = Row < 'a > > , { self . rows = rows . into_iter () . collect () ; self } # [doc = " Sets the header row"] # [doc = ""] # [doc = " The `header` parameter is a [`Row`] which will be displayed at the top of the [`Table`]"] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::widgets::{Cell, Row, Table};"] # [doc = ""] # [doc = " let header = Row::new(vec!["] # [doc = "     Cell::from(\"Header Cell 1\"),"] # [doc = "     Cell::from(\"Header Cell 2\"),"] # [doc = " ]);"] # [doc = " let table = Table::default().header(header);"] # [doc = " ```"] # [must_use = "method moves the value of self and returns the modified value"] pub fn header (mut self , header : Row < 'a >) -> Self { self . header = Some (header) ; self } # [doc = " Sets the footer row"] # [doc = ""] # [doc = " The `footer` parameter is a [`Row`] which will be displayed at the bottom of the [`Table`]"] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::widgets::{Cell, Row, Table};"] # [doc = ""] # [doc = " let footer = Row::new(vec!["] # [doc = "     Cell::from(\"Footer Cell 1\"),"] # [doc = "     Cell::from(\"Footer Cell 2\"),"] # [doc = " ]);"] # [doc = " let table = Table::default().footer(footer);"] # [doc = " ```"] # [must_use = "method moves the value of self and returns the modified value"] pub fn footer (mut self , footer : Row < 'a >) -> Self { self . footer = Some (footer) ; self } # [doc = " Set the widths of the columns."] # [doc = ""] # [doc = " The `widths` parameter accepts any type that implements `IntoIterator<Item ="] # [doc = " Into<Constraint>>`. This includes arrays, slices, vectors, iterators. `Into<Constraint>` is"] # [doc = " implemented on u16, so you can pass an array, vec, etc. of u16 to this function to create a"] # [doc = " table with fixed width columns."] # [doc = ""] # [doc = " If the widths are empty, the table will be rendered with equal widths."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::layout::Constraint;"] # [doc = " use ratatui::widgets::{Cell, Row, Table};"] # [doc = ""] # [doc = " let table = Table::default().widths([Constraint::Length(5), Constraint::Length(5)]);"] # [doc = " let table = Table::default().widths(vec![Constraint::Length(5); 2]);"] # [doc = ""] # [doc = " // widths could also be computed at runtime"] # [doc = " let widths = [10, 10, 20].into_iter().map(|c| Constraint::Length(c));"] # [doc = " let table = Table::default().widths(widths);"] # [doc = " ```"] # [must_use = "method moves the value of self and returns the modified value"] pub fn widths < I > (mut self , widths : I) -> Self where I : IntoIterator , I :: Item : Into < Constraint > , { let widths = widths . into_iter () . map (Into :: into) . collect_vec () ; ensure_percentages_less_than_100 (& widths) ; self . widths = widths ; self } # [doc = " Set the spacing between columns"] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::layout::Constraint;"] # [doc = " use ratatui::widgets::{Row, Table};"] # [doc = ""] # [doc = " let rows = [Row::new(vec![\"Cell1\", \"Cell2\"])];"] # [doc = " let widths = [Constraint::Length(5), Constraint::Length(5)];"] # [doc = " let table = Table::new(rows, widths).column_spacing(1);"] # [doc = " ```"] # [must_use = "method moves the value of self and returns the modified value"] pub const fn column_spacing (mut self , spacing : u16) -> Self { self . column_spacing = spacing ; self } # [doc = " Wraps the table with a custom [`Block`] widget."] # [doc = ""] # [doc = " The `block` parameter is of type [`Block`]. This holds the specified block to be"] # [doc = " created around the [`Table`]"] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::layout::Constraint;"] # [doc = " use ratatui::widgets::{Block, Cell, Row, Table};"] # [doc = ""] # [doc = " let rows = [Row::new(vec![\"Cell1\", \"Cell2\"])];"] # [doc = " let widths = [Constraint::Length(5), Constraint::Length(5)];"] # [doc = " let block = Block::bordered().title(\"Table\");"] # [doc = " let table = Table::new(rows, widths).block(block);"] # [doc = " ```"] # [must_use = "method moves the value of self and returns the modified value"] pub fn block (mut self , block : Block < 'a >) -> Self { self . block = Some (block) ; self } # [doc = " Sets the base style of the widget"] # [doc = ""] # [doc = " `style` accepts any type that is convertible to [`Style`] (e.g. [`Style`], [`Color`], or"] # [doc = " your own type that implements [`Into<Style>`])."] # [doc = ""] # [doc = " All text rendered by the widget will use this style, unless overridden by [`Block::style`],"] # [doc = " [`Row::style`], [`Cell::style`], or the styles of cell's content."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::layout::Constraint;"] # [doc = " use ratatui::style::{Style, Stylize};"] # [doc = " use ratatui::widgets::{Row, Table};"] # [doc = ""] # [doc = " # let rows = [Row::new(vec![\"Cell1\", \"Cell2\"])];"] # [doc = " # let widths = [Constraint::Length(5), Constraint::Length(5)];"] # [doc = " let table = Table::new(rows, widths).style(Style::new().red().italic());"] # [doc = " ```"] # [doc = ""] # [doc = " `Table` also implements the [`Styled`] trait, which means you can use style shorthands from"] # [doc = " the [`Stylize`] trait to set the style of the widget more concisely."] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::layout::Constraint;"] # [doc = " use ratatui::style::Stylize;"] # [doc = " use ratatui::widgets::{Cell, Row, Table};"] # [doc = ""] # [doc = " # let rows = [Row::new(vec![\"Cell1\", \"Cell2\"])];"] # [doc = " # let widths = vec![Constraint::Length(5), Constraint::Length(5)];"] # [doc = " let table = Table::new(rows, widths).red().italic();"] # [doc = " ```"] # [doc = ""] # [doc = " [`Color`]: ratatui_core::style::Color"] # [doc = " [`Stylize`]: ratatui_core::style::Stylize"] # [must_use = "method moves the value of self and returns the modified value"] pub fn style < S : Into < Style > > (mut self , style : S) -> Self { self . style = style . into () ; self } # [doc = " Set the style of the selected row"] # [doc = ""] # [doc = " `style` accepts any type that is convertible to [`Style`] (e.g. [`Style`], [`Color`], or"] # [doc = " your own type that implements [`Into<Style>`])."] # [doc = ""] # [doc = " This style will be applied to the entire row, including the selection symbol if it is"] # [doc = " displayed, and will override any style set on the row or on the individual cells."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::layout::Constraint;"] # [doc = " use ratatui::style::{Style, Stylize};"] # [doc = " use ratatui::widgets::{Cell, Row, Table};"] # [doc = ""] # [doc = " let rows = [Row::new(vec![\"Cell1\", \"Cell2\"])];"] # [doc = " let widths = [Constraint::Length(5), Constraint::Length(5)];"] # [doc = " let table = Table::new(rows, widths).highlight_style(Style::new().red().italic());"] # [doc = " ```"] # [doc = ""] # [doc = " [`Color`]: ratatui_core::style::Color"] # [must_use = "method moves the value of self and returns the modified value"] # [deprecated (note = "use `row_highlight_style()` instead")] pub fn highlight_style < S : Into < Style > > (self , highlight_style : S) -> Self { self . row_highlight_style (highlight_style) } # [doc = " Set the style of the selected row"] # [doc = ""] # [doc = " `style` accepts any type that is convertible to [`Style`] (e.g. [`Style`], [`Color`], or"] # [doc = " your own type that implements [`Into<Style>`])."] # [doc = ""] # [doc = " This style will be applied to the entire row, including the selection symbol if it is"] # [doc = " displayed, and will override any style set on the row or on the individual cells."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use ratatui::{layout::Constraint, style::{Style, Stylize}, widgets::{Row, Table}};"] # [doc = " # let rows = [Row::new(vec![\"Cell1\", \"Cell2\"])];"] # [doc = " # let widths = [Constraint::Length(5), Constraint::Length(5)];"] # [doc = " let table = Table::new(rows, widths).row_highlight_style(Style::new().red().italic());"] # [doc = " ```"] # [doc = " [`Color`]: ratatui_core::style::Color"] # [must_use = "method moves the value of self and returns the modified value"] pub fn row_highlight_style < S : Into < Style > > (mut self , highlight_style : S) -> Self { self . row_highlight_style = highlight_style . into () ; self } # [doc = " Set the style of the selected column"] # [doc = ""] # [doc = " `style` accepts any type that is convertible to [`Style`] (e.g. [`Style`], [`Color`], or"] # [doc = " your own type that implements [`Into<Style>`])."] # [doc = ""] # [doc = " This style will be applied to the entire column, and will override any style set on the"] # [doc = " row or on the individual cells."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use ratatui::{layout::Constraint, style::{Style, Stylize}, widgets::{Row, Table}};"] # [doc = " # let rows = [Row::new(vec![\"Cell1\", \"Cell2\"])];"] # [doc = " # let widths = [Constraint::Length(5), Constraint::Length(5)];"] # [doc = " let table = Table::new(rows, widths).column_highlight_style(Style::new().red().italic());"] # [doc = " ```"] # [doc = " [`Color`]: ratatui_core::style::Color"] # [must_use = "method moves the value of self and returns the modified value"] pub fn column_highlight_style < S : Into < Style > > (mut self , highlight_style : S) -> Self { self . column_highlight_style = highlight_style . into () ; self } # [doc = " Set the style of the selected cell"] # [doc = ""] # [doc = " `style` accepts any type that is convertible to [`Style`] (e.g. [`Style`], [`Color`], or"] # [doc = " your own type that implements [`Into<Style>`])."] # [doc = ""] # [doc = " This style will be applied to the selected cell, and will override any style set on the"] # [doc = " row or on the individual cells."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use ratatui::{layout::Constraint, style::{Style, Stylize}, widgets::{Row, Table}};"] # [doc = " # let rows = [Row::new(vec![\"Cell1\", \"Cell2\"])];"] # [doc = " # let widths = [Constraint::Length(5), Constraint::Length(5)];"] # [doc = " let table = Table::new(rows, widths).cell_highlight_style(Style::new().red().italic());"] # [doc = " ```"] # [doc = " [`Color`]: ratatui_core::style::Color"] # [must_use = "method moves the value of self and returns the modified value"] pub fn cell_highlight_style < S : Into < Style > > (mut self , highlight_style : S) -> Self { self . cell_highlight_style = highlight_style . into () ; self } # [doc = " Set the symbol to be displayed in front of the selected row"] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::layout::Constraint;"] # [doc = " use ratatui::widgets::{Cell, Row, Table};"] # [doc = ""] # [doc = " # let rows = [Row::new(vec![\"Cell1\", \"Cell2\"])];"] # [doc = " # let widths = [Constraint::Length(5), Constraint::Length(5)];"] # [doc = " let table = Table::new(rows, widths).highlight_symbol(\">>\");"] # [doc = " ```"] # [must_use = "method moves the value of self and returns the modified value"] pub fn highlight_symbol < T : Into < Text < 'a > > > (mut self , highlight_symbol : T) -> Self { self . highlight_symbol = highlight_symbol . into () ; self } # [doc = " Set when to show the highlight spacing"] # [doc = ""] # [doc = " The highlight spacing is the spacing that is allocated for the selection symbol column (if"] # [doc = " enabled) and is used to shift the table when a row is selected. This method allows you to"] # [doc = " configure when this spacing is allocated."] # [doc = ""] # [doc = " - [`HighlightSpacing::Always`] will always allocate the spacing, regardless of whether a row"] # [doc = "   is selected or not. This means that the table will never change size, regardless of if a"] # [doc = "   row is selected or not."] # [doc = " - [`HighlightSpacing::WhenSelected`] will only allocate the spacing if a row is selected."] # [doc = "   This means that the table will shift when a row is selected. This is the default setting"] # [doc = "   for backwards compatibility, but it is recommended to use `HighlightSpacing::Always` for a"] # [doc = "   better user experience."] # [doc = " - [`HighlightSpacing::Never`] will never allocate the spacing, regardless of whether a row"] # [doc = "   is selected or not. This means that the highlight symbol will never be drawn."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::layout::Constraint;"] # [doc = " use ratatui::widgets::{HighlightSpacing, Row, Table};"] # [doc = ""] # [doc = " let rows = [Row::new(vec![\"Cell1\", \"Cell2\"])];"] # [doc = " let widths = [Constraint::Length(5), Constraint::Length(5)];"] # [doc = " let table = Table::new(rows, widths).highlight_spacing(HighlightSpacing::Always);"] # [doc = " ```"] # [must_use = "method moves the value of self and returns the modified value"] pub const fn highlight_spacing (mut self , value : HighlightSpacing) -> Self { self . highlight_spacing = value ; self } # [doc = " Set how extra space is distributed amongst columns."] # [doc = ""] # [doc = " This determines how the space is distributed when the constraints are satisfied. By default,"] # [doc = " the extra space is not distributed at all.  But this can be changed to distribute all extra"] # [doc = " space to the last column or to distribute it equally."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Create a table that needs at least 30 columns to display.  Any extra space will be assigned"] # [doc = " to the last column."] # [doc = " ```"] # [doc = " use ratatui::layout::{Constraint, Flex};"] # [doc = " use ratatui::widgets::{Row, Table};"] # [doc = ""] # [doc = " let widths = ["] # [doc = "     Constraint::Min(10),"] # [doc = "     Constraint::Min(10),"] # [doc = "     Constraint::Min(10),"] # [doc = " ];"] # [doc = " let table = Table::new(Vec::<Row>::new(), widths).flex(Flex::Legacy);"] # [doc = " ```"] # [must_use = "method moves the value of self and returns the modified value"] pub const fn flex (mut self , flex : Flex) -> Self { self . flex = flex ; self } }
};
}
