// Generated macro for impl_388 (impl)
macro_rules! Depcrate_calendarimpl_388 {
() => {
// Module: crate::calendar
// Provides: {"impl_388"}
// Dependencies: {}
impl < 'a , DS : DateStyler > Monthly < 'a , DS > { # [doc = " Construct a calendar for the `display_date` and highlight the `events`"] pub const fn new (display_date : Date , events : DS) -> Self { Self { display_date , events , show_surrounding : None , show_weekday : None , show_month : None , default_style : Style :: new () , block : None , } } # [doc = " Fill the calendar slots for days not in the current month also, this causes each line to be"] # [doc = " completely filled. If there is an event style for a date, this style will be patched with"] # [doc = " the event's style"] # [doc = ""] # [doc = " `style` accepts any type that is convertible to [`Style`] (e.g. [`Style`], [`Color`], or"] # [doc = " your own type that implements [`Into<Style>`])."] # [doc = ""] # [doc = " [`Color`]: ratatui_core::style::Color"] # [must_use = "method moves the value of self and returns the modified value"] pub fn show_surrounding < S : Into < Style > > (mut self , style : S) -> Self { self . show_surrounding = Some (style . into ()) ; self } # [doc = " Display a header containing weekday abbreviations"] # [doc = ""] # [doc = " `style` accepts any type that is convertible to [`Style`] (e.g. [`Style`], [`Color`], or"] # [doc = " your own type that implements [`Into<Style>`])."] # [doc = ""] # [doc = " [`Color`]: ratatui_core::style::Color"] # [must_use = "method moves the value of self and returns the modified value"] pub fn show_weekdays_header < S : Into < Style > > (mut self , style : S) -> Self { self . show_weekday = Some (style . into ()) ; self } # [doc = " Display a header containing the month and year"] # [doc = ""] # [doc = " `style` accepts any type that is convertible to [`Style`] (e.g. [`Style`], [`Color`], or"] # [doc = " your own type that implements [`Into<Style>`])."] # [doc = ""] # [doc = " [`Color`]: ratatui_core::style::Color"] # [must_use = "method moves the value of self and returns the modified value"] pub fn show_month_header < S : Into < Style > > (mut self , style : S) -> Self { self . show_month = Some (style . into ()) ; self } # [doc = " How to render otherwise unstyled dates"] # [doc = ""] # [doc = " `style` accepts any type that is convertible to [`Style`] (e.g. [`Style`], [`Color`], or"] # [doc = " your own type that implements [`Into<Style>`])."] # [doc = ""] # [doc = " [`Color`]: ratatui_core::style::Color"] # [must_use = "method moves the value of self and returns the modified value"] pub fn default_style < S : Into < Style > > (mut self , style : S) -> Self { self . default_style = style . into () ; self } # [doc = " Render the calendar within a [Block]"] # [must_use = "method moves the value of self and returns the modified value"] pub fn block (mut self , block : Block < 'a >) -> Self { self . block = Some (block) ; self } # [doc = " Return a style with only the background from the default style"] const fn default_bg (& self) -> Style { match self . default_style . bg { None => Style :: new () , Some (c) => Style :: new () . bg (c) , } } # [doc = " All logic to style a date goes here."] fn format_date (& self , date : Date) -> Span < '_ > { if date . month () == self . display_date . month () { Span :: styled (format ! ("{:2?}" , date . day ()) , self . default_style . patch (self . events . get_style (date)) ,) } else { match self . show_surrounding { None => Span :: styled ("  " , self . default_bg ()) , Some (s) => { let style = self . default_style . patch (s) . patch (self . events . get_style (date)) ; Span :: styled (format ! ("{:2?}" , date . day ()) , style) } } } } }
};
}
