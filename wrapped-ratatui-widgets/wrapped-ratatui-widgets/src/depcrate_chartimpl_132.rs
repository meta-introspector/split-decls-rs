// Generated macro for impl_132 (impl)
macro_rules! Depcrate_chartimpl_132 {
() => {
// Module: crate::chart
// Provides: {"impl_132"}
// Dependencies: {}
impl < 'a > Dataset < 'a > { # [doc = " Sets the name of the dataset"] # [doc = ""] # [doc = " The dataset's name is used when displaying the chart legend. Datasets don't require a name"] # [doc = " and can be created without specifying one. Once assigned, a name can't be removed, only"] # [doc = " changed"] # [doc = ""] # [doc = " The name can be styled (see [`Line`] for that), but the dataset's style will always have"] # [doc = " precedence."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [must_use = "method moves the value of self and returns the modified value"] pub fn name < S > (mut self , name : S) -> Self where S : Into < Line < 'a > > , { self . name = Some (name . into ()) ; self } # [doc = " Sets the data points of this dataset"] # [doc = ""] # [doc = " Points will then either be rendered as scattered points or with lines between them"] # [doc = " depending on [`Dataset::graph_type`]."] # [doc = ""] # [doc = " Data consist in an array of `f64` tuples (`(f64, f64)`), the first element being X and the"] # [doc = " second Y. It's also worth noting that, unlike the [`Rect`], here the Y axis is bottom to"] # [doc = " top, as in math."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [must_use = "method moves the value of self and returns the modified value"] pub const fn data (mut self , data : & 'a [(f64 , f64)]) -> Self { self . data = data ; self } # [doc = " Sets the kind of character to use to display this dataset"] # [doc = ""] # [doc = " You can use dots (`•`), blocks (`█`), bars (`▄`), braille (`⠓`, `⣇`, `⣿`) or half-blocks"] # [doc = " (`█`, `▄`, and `▀`). See [`symbols::Marker`] for more details."] # [doc = ""] # [doc = " Note [`Marker::Braille`](symbols::Marker::Braille) requires a font that supports Unicode"] # [doc = " Braille Patterns."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [must_use = "method moves the value of self and returns the modified value"] pub const fn marker (mut self , marker : symbols :: Marker) -> Self { self . marker = marker ; self } # [doc = " Sets how the dataset should be drawn"] # [doc = ""] # [doc = " [`Chart`] can draw [scatter](GraphType::Scatter), [line](GraphType::Line) or"] # [doc = " [bar](GraphType::Bar) charts. A scatter chart draws only the points in the dataset, a line"] # [doc = " char draws a line between each point, and a bar chart draws a line from the x axis to the"] # [doc = " point.  See [`GraphType`] for more details"] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [must_use = "method moves the value of self and returns the modified value"] pub const fn graph_type (mut self , graph_type : GraphType) -> Self { self . graph_type = graph_type ; self } # [doc = " Sets the style of this dataset"] # [doc = ""] # [doc = " The given style will be used to draw the legend and the data points. Currently the legend"] # [doc = " will use the entire style whereas the data points will only use the foreground."] # [doc = ""] # [doc = " `style` accepts any type that is convertible to [`Style`] (e.g. [`Style`], [`Color`], or"] # [doc = " your own type that implements [`Into<Style>`])."] # [doc = ""] # [doc = " This is a fluent setter method which must be chained or used as it consumes self"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " [`Dataset`] also implements [`Stylize`](ratatui_core::style::Stylize) which mean you can"] # [doc = " style it like so"] # [doc = ""] # [doc = " ```rust"] # [doc = " use ratatui::style::Stylize;"] # [doc = " use ratatui::widgets::Dataset;"] # [doc = ""] # [doc = " let dataset = Dataset::default().red();"] # [doc = " ```"] # [must_use = "method moves the value of self and returns the modified value"] pub fn style < S : Into < Style > > (mut self , style : S) -> Self { self . style = style . into () ; self } }
};
}
