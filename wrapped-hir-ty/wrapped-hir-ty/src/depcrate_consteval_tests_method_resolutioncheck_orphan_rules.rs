// Generated macro for check_orphan_rules (function)
macro_rules! Depcrate_consteval_tests_method_resolutioncheck_orphan_rules {
() => {
// Module: crate::consteval::tests::method_resolution
// Provides: {"check_orphan_rules"}
// Dependencies: {}
# [doc = " Checks whether the impl satisfies the orphan rules."] # [doc = ""] # [doc = " Given `impl<P1..=Pn> Trait<T1..=Tn> for T0`, an `impl`` is valid only if at least one of the following is true:"] # [doc = " - Trait is a local trait"] # [doc = " - All of"] # [doc = "   - At least one of the types `T0..=Tn`` must be a local type. Let `Ti`` be the first such type."] # [doc = "   - No uncovered type parameters `P1..=Pn` may appear in `T0..Ti`` (excluding `Ti`)"] pub fn check_orphan_rules (db : & dyn HirDatabase , impl_ : ImplId) -> bool { let substs = TyBuilder :: placeholder_subst (db , impl_) ; let Some (impl_trait) = db . impl_trait (impl_) else { return true ; } ; let local_crate = impl_ . lookup (db) . container . krate () ; let is_local = | tgt_crate | tgt_crate == local_crate ; let trait_ref = impl_trait . substitute (Interner , & substs) ; let trait_id = from_chalk_trait_id (trait_ref . trait_id) ; if is_local (trait_id . module (db) . krate ()) { return true ; } let unwrap_fundamental = | mut ty : Ty | { loop { match ty . kind (Interner) { TyKind :: Ref (_ , _ , referenced) => ty = referenced . clone () , & TyKind :: Adt (AdtId (hir_def :: AdtId :: StructId (s)) , ref subs) => { let struct_signature = db . struct_signature (s) ; if struct_signature . flags . contains (StructFlags :: FUNDAMENTAL) { let next = subs . type_parameters (Interner) . next () ; match next { Some (it) => ty = it , None => break ty , } } else { break ty ; } } _ => break ty , } } } ; let is_not_orphan = trait_ref . substitution . type_parameters (Interner) . any (| ty | { match unwrap_fundamental (ty) . kind (Interner) { & TyKind :: Adt (AdtId (id) , _) => is_local (id . module (db) . krate ()) , TyKind :: Error => true , TyKind :: Dyn (it) => it . principal_id () . is_some_and (| trait_id | is_local (from_chalk_trait_id (trait_id) . module (db) . krate ())) , _ => false , } }) ; # [allow (clippy :: let_and_return)] is_not_orphan }
};
}
