// Generated macro for impl_93 (impl)
macro_rules! Depcrate_bencherimpl_93 {
() => {
// Module: crate::bencher
// Provides: {"impl_93"}
// Dependencies: {}
# [cfg (feature = "async")] impl < 'a , 'b , A : AsyncExecutor , M : Measurement > AsyncBencher < 'a , 'b , A , M > { # [doc = " Times a `routine` by executing it many times and timing the total elapsed time."] # [doc = ""] # [doc = " Prefer this timing loop when `routine` returns a value that doesn't have a destructor."] # [doc = ""] # [doc = " # Timing model"] # [doc = ""] # [doc = " Note that the `AsyncBencher` also times the time required to destroy the output of `routine()`."] # [doc = " Therefore prefer this timing loop when the runtime of `mem::drop(O)` is negligible compared"] # [doc = " to the runtime of the `routine`."] # [doc = ""] # [doc = " ```text"] # [doc = " elapsed = Instant::now + iters * (routine + mem::drop(O) + Range::next)"] # [doc = " ```"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use criterion::{criterion_group, criterion_main, Criterion};"] # [doc = " use criterion::async_executor::FuturesExecutor;"] # [doc = ""] # [doc = " // The function to benchmark"] # [doc = " async fn foo() {"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " fn bench(c: &mut Criterion) {"] # [doc = "     c.bench_function(\"iter\", move |b| {"] # [doc = "         b.to_async(FuturesExecutor).iter(|| async { foo().await } )"] # [doc = "     });"] # [doc = " }"] # [doc = ""] # [doc = " criterion_group!(benches, bench);"] # [doc = " criterion_main!(benches);"] # [doc = " ```"] # [doc = ""] # [inline (never)] pub fn iter < O , R , F > (& mut self , mut routine : R) where R : FnMut () -> F , F : Future < Output = O > , { let AsyncBencher { b , runner } = self ; runner . block_on (async { b . iterated = true ; let time_start = Instant :: now () ; let start = b . measurement . start () ; for _ in 0 .. b . iters { black_box (routine () . await) ; } b . value = b . measurement . end (start) ; b . elapsed_time = time_start . elapsed () ; }) ; } # [doc = " Times a `routine` by executing it many times and relying on `routine` to measure its own execution time."] # [doc = ""] # [doc = " Prefer this timing loop in cases where `routine` has to do its own measurements to"] # [doc = " get accurate timing information (for example in multi-threaded scenarios where you spawn"] # [doc = " and coordinate with multiple threads)."] # [doc = ""] # [doc = " # Timing model"] # [doc = " Custom, the timing model is whatever is returned as the [`Duration`] from `routine`."] # [doc = ""] # [doc = " # Example"] # [doc = " ```rust"] # [doc = " use criterion::{criterion_group, criterion_main, Criterion};"] # [doc = " use criterion::async_executor::FuturesExecutor;"] # [doc = " use std::time::Instant;"] # [doc = ""] # [doc = " async fn foo() {"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " fn bench(c: &mut Criterion) {"] # [doc = "     c.bench_function(\"iter\", move |b| {"] # [doc = "         b.to_async(FuturesExecutor).iter_custom(|iters| {"] # [doc = "             async move {"] # [doc = "                 let start = Instant::now();"] # [doc = "                 for _i in 0..iters {"] # [doc = "                     std::hint::black_box(foo().await);"] # [doc = "                 }"] # [doc = "                 start.elapsed()"] # [doc = "             }"] # [doc = "         })"] # [doc = "     });"] # [doc = " }"] # [doc = ""] # [doc = " criterion_group!(benches, bench);"] # [doc = " criterion_main!(benches);"] # [doc = " ```"] # [doc = ""] # [inline (never)] pub fn iter_custom < R , F > (& mut self , mut routine : R) where R : FnMut (u64) -> F , F : Future < Output = M :: Value > , { let AsyncBencher { b , runner } = self ; runner . block_on (async { b . iterated = true ; let time_start = Instant :: now () ; b . value = routine (b . iters) . await ; b . elapsed_time = time_start . elapsed () ; }) } # [doc (hidden)] pub fn iter_with_setup < I , O , S , R , F > (& mut self , setup : S , routine : R) where S : FnMut () -> I , R : FnMut (I) -> F , F : Future < Output = O > , { self . iter_batched (setup , routine , BatchSize :: PerIteration) ; } # [doc = " Times a `routine` by collecting its output on each iteration. This avoids timing the"] # [doc = " destructor of the value returned by `routine`."] # [doc = ""] # [doc = " WARNING: This requires `O(iters * mem::size_of::<O>())` of memory, and `iters`"] # [doc = " is not under the control of the caller. If this causes out-of-memory errors, use"] # [doc = " [`iter_batched`](Self::iter_batched) instead."] # [doc = ""] # [doc = " # Timing model"] # [doc = ""] # [doc = " ``` text"] # [doc = " elapsed = Instant::now + iters * (routine) + Iterator::collect::<Vec<_>>"] # [doc = " ```"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use criterion::{criterion_group, criterion_main, Criterion};"] # [doc = " use criterion::async_executor::FuturesExecutor;"] # [doc = ""] # [doc = " async fn create_vector() -> Vec<u64> {"] # [doc = "     # vec![]"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " fn bench(c: &mut Criterion) {"] # [doc = "     c.bench_function(\"with_drop\", move |b| {"] # [doc = "         // This will avoid timing the Vec::drop."] # [doc = "         b.to_async(FuturesExecutor).iter_with_large_drop(|| async { create_vector().await })"] # [doc = "     });"] # [doc = " }"] # [doc = ""] # [doc = " criterion_group!(benches, bench);"] # [doc = " criterion_main!(benches);"] # [doc = " ```"] # [doc = ""] pub fn iter_with_large_drop < O , R , F > (& mut self , mut routine : R) where R : FnMut () -> F , F : Future < Output = O > , { self . iter_batched (| | () , | _ | routine () , BatchSize :: SmallInput) ; } # [doc (hidden)] pub fn iter_with_large_setup < I , O , S , R , F > (& mut self , setup : S , routine : R) where S : FnMut () -> I , R : FnMut (I) -> F , F : Future < Output = O > , { self . iter_batched (setup , routine , BatchSize :: NumBatches (1)) ; } # [doc = " Times a `routine` that requires some input by generating a batch of input, then timing the"] # [doc = " iteration of the benchmark over the input. See [`BatchSize`] for"] # [doc = " details on choosing the batch size. Use this when the routine must consume its input."] # [doc = ""] # [doc = " For example, use this loop to benchmark sorting algorithms, because they require unsorted"] # [doc = " data on each iteration."] # [doc = ""] # [doc = " # Timing model"] # [doc = ""] # [doc = " ```text"] # [doc = " elapsed = (Instant::now * num_batches) + (iters * (routine + O::drop)) + Vec::extend"] # [doc = " ```"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use criterion::{criterion_group, criterion_main, BatchSize, Criterion};"] # [doc = " use criterion::async_executor::FuturesExecutor;"] # [doc = ""] # [doc = " fn create_scrambled_data() -> Vec<u64> {"] # [doc = "     # vec![]"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " // The sorting algorithm to test"] # [doc = " async fn sort(data: &mut [u64]) {"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " fn bench(c: &mut Criterion) {"] # [doc = "     let data = create_scrambled_data();"] # [doc = ""] # [doc = "     c.bench_function(\"with_setup\", move |b| {"] # [doc = "         // This will avoid timing the clone call."] # [doc = "         b.iter_batched(|| data.clone(), |mut data| async move { sort(&mut data).await }, BatchSize::SmallInput)"] # [doc = "     });"] # [doc = " }"] # [doc = ""] # [doc = " criterion_group!(benches, bench);"] # [doc = " criterion_main!(benches);"] # [doc = " ```"] # [doc = ""] # [inline (never)] pub fn iter_batched < I , O , S , R , F > (& mut self , mut setup : S , mut routine : R , size : BatchSize) where S : FnMut () -> I , R : FnMut (I) -> F , F : Future < Output = O > , { let AsyncBencher { b , runner } = self ; runner . block_on (async { b . iterated = true ; let batch_size = size . iters_per_batch (b . iters) ; assert ! (batch_size != 0 , "Batch size must not be zero.") ; let time_start = Instant :: now () ; b . value = b . measurement . zero () ; if batch_size == 1 { for _ in 0 .. b . iters { let input = black_box (setup ()) ; let start = b . measurement . start () ; let output = routine (input) . await ; let end = b . measurement . end (start) ; b . value = b . measurement . add (& b . value , & end) ; drop (black_box (output)) ; } } else { let mut iteration_counter = 0 ; while iteration_counter < b . iters { let batch_size = :: std :: cmp :: min (batch_size , b . iters - iteration_counter) ; let inputs = black_box ((0 .. batch_size) . map (| _ | setup ()) . collect :: < Vec < _ > > ()) ; let mut outputs = Vec :: with_capacity (batch_size as usize) ; let start = b . measurement . start () ; for input in inputs { outputs . push (routine (input) . await) ; } let end = b . measurement . end (start) ; b . value = b . measurement . add (& b . value , & end) ; black_box (outputs) ; iteration_counter += batch_size ; } } b . elapsed_time = time_start . elapsed () ; }) } # [doc = " Times a `routine` that requires some input by generating a batch of input, then timing the"] # [doc = " iteration of the benchmark over the input. See [`BatchSize`] for"] # [doc = " details on choosing the batch size. Use this when the routine should accept the input by"] # [doc = " mutable reference."] # [doc = ""] # [doc = " For example, use this loop to benchmark sorting algorithms, because they require unsorted"] # [doc = " data on each iteration."] # [doc = ""] # [doc = " # Timing model"] # [doc = ""] # [doc = " ```text"] # [doc = " elapsed = (Instant::now * num_batches) + (iters * routine) + Vec::extend"] # [doc = " ```"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use criterion::{criterion_group, criterion_main, BatchSize, Criterion};"] # [doc = " use criterion::async_executor::FuturesExecutor;"] # [doc = ""] # [doc = " fn create_scrambled_data() -> Vec<u64> {"] # [doc = "     # vec![]"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " // The sorting algorithm to test"] # [doc = " async fn sort(data: &mut [u64]) {"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " fn bench(c: &mut Criterion) {"] # [doc = "     let data = create_scrambled_data();"] # [doc = ""] # [doc = "     c.bench_function(\"with_setup\", move |b| {"] # [doc = "         // This will avoid timing the clone call."] # [doc = "         b.iter_batched(|| data.clone(), |mut data| async move { sort(&mut data).await }, BatchSize::SmallInput)"] # [doc = "     });"] # [doc = " }"] # [doc = ""] # [doc = " criterion_group!(benches, bench);"] # [doc = " criterion_main!(benches);"] # [doc = " ```"] # [doc = ""] # [inline (never)] pub fn iter_batched_ref < I , O , S , R , F > (& mut self , mut setup : S , mut routine : R , size : BatchSize) where S : FnMut () -> I , R : FnMut (& mut I) -> F , F : Future < Output = O > , { let AsyncBencher { b , runner } = self ; runner . block_on (async { b . iterated = true ; let batch_size = size . iters_per_batch (b . iters) ; assert ! (batch_size != 0 , "Batch size must not be zero.") ; let time_start = Instant :: now () ; b . value = b . measurement . zero () ; if batch_size == 1 { for _ in 0 .. b . iters { let mut input = black_box (setup ()) ; let start = b . measurement . start () ; let output = routine (& mut input) . await ; let end = b . measurement . end (start) ; b . value = b . measurement . add (& b . value , & end) ; drop (black_box (output)) ; drop (black_box (input)) ; } } else { let mut iteration_counter = 0 ; while iteration_counter < b . iters { let batch_size = :: std :: cmp :: min (batch_size , b . iters - iteration_counter) ; let inputs = black_box ((0 .. batch_size) . map (| _ | setup ()) . collect :: < Vec < _ > > ()) ; let mut outputs = Vec :: with_capacity (batch_size as usize) ; let start = b . measurement . start () ; for mut input in inputs { outputs . push (routine (& mut input) . await) ; } let end = b . measurement . end (start) ; b . value = b . measurement . add (& b . value , & end) ; black_box (outputs) ; iteration_counter += batch_size ; } } b . elapsed_time = time_start . elapsed () ; }) ; } }
};
}
