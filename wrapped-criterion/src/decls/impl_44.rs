macro_rules! deps {
    () => {
        Duration!();
        Criterion!();
        AsyncExecutor!();
        BatchSize!();
        AsyncBencher!();
        Bencher!();
        Measurement!();
    };
}

macro_rules! impl_44 {
    () => {
        deps!();
        impl < 'a , M : Measurement > Bencher < 'a , M > { # [doc = " Times a `routine` by executing it many times and timing the total elapsed time."] # [doc = ""] # [doc = " Prefer this timing loop when `routine` returns a value that doesn't have a destructor."] # [doc = ""] # [doc = " # Timing model"] # [doc = ""] # [doc = " Note that the `Bencher` also times the time required to destroy the output of `routine()`."] # [doc = " Therefore prefer this timing loop when the runtime of `mem::drop(O)` is negligible compared"] # [doc = " to the runtime of the `routine`."] # [doc = ""] # [doc = " ```text"] # [doc = " elapsed = Instant::now + iters * (routine + mem::drop(O) + Range::next)"] # [doc = " ```"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use criterion::{criterion_group, criterion_main, Criterion};"] # [doc = ""] # [doc = " // The function to benchmark"] # [doc = " fn foo() {"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " fn bench(c: &mut Criterion) {"] # [doc = "     c.bench_function(\"iter\", move |b| {"] # [doc = "         b.iter(|| foo())"] # [doc = "     });"] # [doc = " }"] # [doc = ""] # [doc = " criterion_group!(benches, bench);"] # [doc = " criterion_main!(benches);"] # [doc = " ```"] # [doc = ""] # [inline (never)] pub fn iter < O , R > (& mut self , mut routine : R) where R : FnMut () -> O , { self . iterated = true ; let time_start = Instant :: now () ; let start = self . measurement . start () ; for _ in 0 .. self . iters { black_box (routine ()) ; } self . value = self . measurement . end (start) ; self . elapsed_time = time_start . elapsed () ; } # [doc = " Times a `routine` by executing it many times and relying on `routine` to measure its own execution time."] # [doc = ""] # [doc = " Prefer this timing loop in cases where `routine` has to do its own measurements to"] # [doc = " get accurate timing information (for example in multi-threaded scenarios where you spawn"] # [doc = " and coordinate with multiple threads)."] # [doc = ""] # [doc = " # Timing model"] # [doc = " Custom, the timing model is whatever is returned as the [`Duration`] from `routine`."] # [doc = ""] # [doc = " # Example"] # [doc = " ```rust"] # [doc = " use criterion::{criterion_group, criterion_main, Criterion};"] # [doc = " use std::time::Instant;"] # [doc = ""] # [doc = " fn foo() {"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " fn bench(c: &mut Criterion) {"] # [doc = "     c.bench_function(\"iter\", move |b| {"] # [doc = "         b.iter_custom(|iters| {"] # [doc = "             let start = Instant::now();"] # [doc = "             for _i in 0..iters {"] # [doc = "                 std::hint::black_box(foo());"] # [doc = "             }"] # [doc = "             start.elapsed()"] # [doc = "         })"] # [doc = "     });"] # [doc = " }"] # [doc = ""] # [doc = " criterion_group!(benches, bench);"] # [doc = " criterion_main!(benches);"] # [doc = " ```"] # [doc = ""] # [inline (never)] pub fn iter_custom < R > (& mut self , mut routine : R) where R : FnMut (u64) -> M :: Value , { self . iterated = true ; let time_start = Instant :: now () ; self . value = routine (self . iters) ; self . elapsed_time = time_start . elapsed () ; } # [doc (hidden)] pub fn iter_with_setup < I , O , S , R > (& mut self , setup : S , routine : R) where S : FnMut () -> I , R : FnMut (I) -> O , { self . iter_batched (setup , routine , BatchSize :: PerIteration) ; } # [doc = " Times a `routine` by collecting its output on each iteration. This avoids timing the"] # [doc = " destructor of the value returned by `routine`."] # [doc = ""] # [doc = " WARNING: This requires `O(iters * mem::size_of::<O>())` of memory, and `iters` is not"] # [doc = " under the control of the caller. If this causes out-of-memory errors, use"] # [doc = " [`iter_batched`](Self::iter_batched) instead."] # [doc = ""] # [doc = " # Timing model"] # [doc = ""] # [doc = " ``` text"] # [doc = " elapsed = Instant::now + iters * (routine) + Iterator::collect::<Vec<_>>"] # [doc = " ```"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use criterion::{criterion_group, criterion_main, Criterion};"] # [doc = ""] # [doc = " fn create_vector() -> Vec<u64> {"] # [doc = "     # vec![]"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " fn bench(c: &mut Criterion) {"] # [doc = "     c.bench_function(\"with_drop\", move |b| {"] # [doc = "         // This will avoid timing the Vec::drop."] # [doc = "         b.iter_with_large_drop(|| create_vector())"] # [doc = "     });"] # [doc = " }"] # [doc = ""] # [doc = " criterion_group!(benches, bench);"] # [doc = " criterion_main!(benches);"] # [doc = " ```"] # [doc = ""] pub fn iter_with_large_drop < O , R > (& mut self , mut routine : R) where R : FnMut () -> O , { self . iter_batched (| | () , | _ | routine () , BatchSize :: SmallInput) ; } # [doc = " Times a `routine` that requires some input by generating a batch of input, then timing the"] # [doc = " iteration of the benchmark over the input. See [`BatchSize`] for"] # [doc = " details on choosing the batch size. Use this when the routine must consume its input."] # [doc = ""] # [doc = " For example, use this loop to benchmark sorting algorithms, because they require unsorted"] # [doc = " data on each iteration."] # [doc = ""] # [doc = " # Timing model"] # [doc = ""] # [doc = " ```text"] # [doc = " elapsed = (Instant::now * num_batches) + (iters * (routine + O::drop)) + Vec::extend"] # [doc = " ```"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use criterion::{criterion_group, criterion_main, BatchSize, Criterion};"] # [doc = ""] # [doc = " fn create_scrambled_data() -> Vec<u64> {"] # [doc = "     # vec![]"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " // The sorting algorithm to test"] # [doc = " fn sort(data: &mut [u64]) {"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " fn bench(c: &mut Criterion) {"] # [doc = "     let data = create_scrambled_data();"] # [doc = ""] # [doc = "     c.bench_function(\"with_setup\", move |b| {"] # [doc = "         // This will avoid timing the clone call."] # [doc = "         b.iter_batched(|| data.clone(), |mut data| sort(&mut data), BatchSize::SmallInput)"] # [doc = "     });"] # [doc = " }"] # [doc = ""] # [doc = " criterion_group!(benches, bench);"] # [doc = " criterion_main!(benches);"] # [doc = " ```"] # [doc = ""] # [inline (never)] pub fn iter_batched < I , O , S , R > (& mut self , mut setup : S , mut routine : R , size : BatchSize) where S : FnMut () -> I , R : FnMut (I) -> O , { self . iterated = true ; let batch_size = size . iters_per_batch (self . iters) ; assert ! (batch_size != 0 , "Batch size must not be zero.") ; let time_start = Instant :: now () ; self . value = self . measurement . zero () ; if batch_size == 1 { for _ in 0 .. self . iters { let input = black_box (setup ()) ; let start = self . measurement . start () ; let output = routine (input) ; let end = self . measurement . end (start) ; self . value = self . measurement . add (& self . value , & end) ; drop (black_box (output)) ; } } else { let mut iteration_counter = 0 ; while iteration_counter < self . iters { let batch_size = :: std :: cmp :: min (batch_size , self . iters - iteration_counter) ; let inputs = black_box ((0 .. batch_size) . map (| _ | setup ()) . collect :: < Vec < _ > > ()) ; let mut outputs = Vec :: with_capacity (batch_size as usize) ; let start = self . measurement . start () ; outputs . extend (inputs . into_iter () . map (& mut routine)) ; let end = self . measurement . end (start) ; self . value = self . measurement . add (& self . value , & end) ; black_box (outputs) ; iteration_counter += batch_size ; } } self . elapsed_time = time_start . elapsed () ; } # [doc = " Times a `routine` that requires some input by generating a batch of input, then timing the"] # [doc = " iteration of the benchmark over the input. See [`BatchSize`] for"] # [doc = " details on choosing the batch size. Use this when the routine should accept the input by"] # [doc = " mutable reference."] # [doc = ""] # [doc = " For example, use this loop to benchmark sorting algorithms, because they require unsorted"] # [doc = " data on each iteration."] # [doc = ""] # [doc = " # Timing model"] # [doc = ""] # [doc = " ```text"] # [doc = " elapsed = (Instant::now * num_batches) + (iters * routine) + Vec::extend"] # [doc = " ```"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use criterion::{criterion_group, criterion_main, BatchSize, Criterion};"] # [doc = ""] # [doc = " fn create_scrambled_data() -> Vec<u64> {"] # [doc = "     # vec![]"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " // The sorting algorithm to test"] # [doc = " fn sort(data: &mut [u64]) {"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " fn bench(c: &mut Criterion) {"] # [doc = "     let data = create_scrambled_data();"] # [doc = ""] # [doc = "     c.bench_function(\"with_setup\", move |b| {"] # [doc = "         // This will avoid timing the clone call."] # [doc = "         b.iter_batched_ref(|| data.clone(), |mut data| sort(&mut data), BatchSize::SmallInput)"] # [doc = "     });"] # [doc = " }"] # [doc = ""] # [doc = " criterion_group!(benches, bench);"] # [doc = " criterion_main!(benches);"] # [doc = " ```"] # [doc = ""] # [inline (never)] pub fn iter_batched_ref < I , O , S , R > (& mut self , mut setup : S , mut routine : R , size : BatchSize) where S : FnMut () -> I , R : FnMut (& mut I) -> O , { self . iterated = true ; let batch_size = size . iters_per_batch (self . iters) ; assert ! (batch_size != 0 , "Batch size must not be zero.") ; let time_start = Instant :: now () ; self . value = self . measurement . zero () ; if batch_size == 1 { for _ in 0 .. self . iters { let mut input = black_box (setup ()) ; let start = self . measurement . start () ; let output = routine (& mut input) ; let end = self . measurement . end (start) ; self . value = self . measurement . add (& self . value , & end) ; drop (black_box (output)) ; drop (black_box (input)) ; } } else { let mut iteration_counter = 0 ; while iteration_counter < self . iters { let batch_size = :: std :: cmp :: min (batch_size , self . iters - iteration_counter) ; let mut inputs = black_box ((0 .. batch_size) . map (| _ | setup ()) . collect :: < Vec < _ > > ()) ; let mut outputs = Vec :: with_capacity (batch_size as usize) ; let start = self . measurement . start () ; outputs . extend (inputs . iter_mut () . map (& mut routine)) ; let end = self . measurement . end (start) ; self . value = self . measurement . add (& self . value , & end) ; black_box (outputs) ; iteration_counter += batch_size ; } } self . elapsed_time = time_start . elapsed () ; } pub (crate) fn assert_iterated (& mut self) { assert ! (self . iterated , "Benchmark function must call Bencher::iter or related method.") ; self . iterated = false ; } # [doc = " Convert this bencher into an [`AsyncBencher`], which enables async/await support."] # [cfg (feature = "async")] pub fn to_async < 'b , A : AsyncExecutor > (& 'b mut self , runner : A) -> AsyncBencher < 'a , 'b , A , M > { AsyncBencher { b : self , runner } } }
    };
}

impl_44!()