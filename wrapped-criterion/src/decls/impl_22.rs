macro_rules! deps {
    () => {
        Bencher!();
        Throughput!();
        PlotConfiguration!();
        Duration!();
        BenchmarkId!();
        IntoBenchmarkId!();
        Function!();
        PartialBenchmarkConfig!();
        OutgoingMessage!();
        Sample!();
        Mode!();
        Measurement!();
        BenchmarkGroup!();
        Criterion!();
        ReportContext!();
        SamplingMode!();
    };
}

macro_rules! impl_22 {
    () => {
        deps!();
        impl < 'a , M : Measurement > BenchmarkGroup < 'a , M > { # [doc = " Changes the size of the sample for this benchmark"] # [doc = ""] # [doc = " A bigger sample should yield more accurate results if paired with a sufficiently large"] # [doc = " measurement time."] # [doc = ""] # [doc = " Sample size must be at least 10."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if n < 10."] pub fn sample_size (& mut self , n : usize) -> & mut Self { assert ! (n >= 10) ; self . partial_config . sample_size = Some (n) ; self } # [doc = " Changes the warm up time for this benchmark"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the input duration is zero"] pub fn warm_up_time (& mut self , dur : Duration) -> & mut Self { assert ! (dur . as_nanos () > 0) ; self . partial_config . warm_up_time = Some (dur) ; self } # [doc = " Changes the target measurement time for this benchmark group."] # [doc = ""] # [doc = " Criterion will attempt to spent approximately this amount of time measuring each"] # [doc = " benchmark on a best-effort basis. If it is not possible to perform the measurement in"] # [doc = " the requested time (eg. because each iteration of the benchmark is long) then Criterion"] # [doc = " will spend as long as is needed to collect the desired number of samples. With a longer"] # [doc = " time, the measurement will become more resilient to interference from other programs."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the input duration is zero"] pub fn measurement_time (& mut self , dur : Duration) -> & mut Self { assert ! (dur . as_nanos () > 0) ; self . partial_config . measurement_time = Some (dur) ; self } # [doc = " Changes the number of resamples for this benchmark group"] # [doc = ""] # [doc = " Number of resamples to use for the"] # [doc = " [bootstrap](http://en.wikipedia.org/wiki/Bootstrapping_(statistics)#Case_resampling)"] # [doc = ""] # [doc = " A larger number of resamples reduces the random sampling errors which are inherent to the"] # [doc = " bootstrap method, but also increases the analysis time."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the number of resamples is set to zero"] pub fn nresamples (& mut self , n : usize) -> & mut Self { assert ! (n > 0) ; if n <= 1000 { eprintln ! ("\nWarning: It is not recommended to reduce nresamples below 1000.") ; } self . partial_config . nresamples = Some (n) ; self } # [doc = " Changes the noise threshold for benchmarks in this group. The noise threshold"] # [doc = " is used to filter out small changes in performance from one run to the next, even if they"] # [doc = " are statistically significant. Sometimes benchmarking the same code twice will result in"] # [doc = " small but statistically significant differences solely because of noise. This provides a way"] # [doc = " to filter out some of these false positives at the cost of making it harder to detect small"] # [doc = " changes to the true performance of the benchmark."] # [doc = ""] # [doc = " The default is 0.01, meaning that changes smaller than 1% will be ignored."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the threshold is set to a negative value"] pub fn noise_threshold (& mut self , threshold : f64) -> & mut Self { assert ! (threshold >= 0.0) ; self . partial_config . noise_threshold = Some (threshold) ; self } # [doc = " Changes the confidence level for benchmarks in this group. The confidence"] # [doc = " level is the desired probability that the true runtime lies within the estimated"] # [doc = " [confidence interval](https://en.wikipedia.org/wiki/Confidence_interval). The default is"] # [doc = " 0.95, meaning that the confidence interval should capture the true value 95% of the time."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the confidence level is set to a value outside the `(0, 1)` range"] pub fn confidence_level (& mut self , cl : f64) -> & mut Self { assert ! (cl > 0.0 && cl < 1.0) ; if cl < 0.5 { eprintln ! ("\nWarning: It is not recommended to reduce confidence level below 0.5.") ; } self . partial_config . confidence_level = Some (cl) ; self } # [doc = " Changes the [significance level](https://en.wikipedia.org/wiki/Statistical_significance)"] # [doc = " for benchmarks in this group. This is used to perform a"] # [doc = " [hypothesis test](https://en.wikipedia.org/wiki/Statistical_hypothesis_testing) to see if"] # [doc = " the measurements from this run are different from the measured performance of the last run."] # [doc = " The significance level is the desired probability that two measurements of identical code"] # [doc = " will be considered 'different' due to noise in the measurements. The default value is 0.05,"] # [doc = " meaning that approximately 5% of identical benchmarks will register as different due to"] # [doc = " noise."] # [doc = ""] # [doc = " This presents a trade-off. By setting the significance level closer to 0.0, you can increase"] # [doc = " the statistical robustness against noise, but it also weakens Criterion.rs' ability to"] # [doc = " detect small but real changes in the performance. By setting the significance level"] # [doc = " closer to 1.0, Criterion.rs will be more able to detect small true changes, but will also"] # [doc = " report more spurious differences."] # [doc = ""] # [doc = " See also the noise threshold setting."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the significance level is set to a value outside the `(0, 1)` range"] pub fn significance_level (& mut self , sl : f64) -> & mut Self { assert ! (sl > 0.0 && sl < 1.0) ; self . partial_config . significance_level = Some (sl) ; self } # [doc = " Changes the plot configuration for this benchmark group."] pub fn plot_config (& mut self , new_config : PlotConfiguration) -> & mut Self { self . partial_config . plot_config = new_config ; self } # [doc = " Set the input size for this benchmark group. Used for reporting the"] # [doc = " throughput."] pub fn throughput (& mut self , throughput : Throughput) -> & mut Self { self . throughput = Some (throughput) ; self } # [doc = " Set the sampling mode for this benchmark group."] pub fn sampling_mode (& mut self , new_mode : SamplingMode) -> & mut Self { self . partial_config . sampling_mode = Some (new_mode) ; self } pub (crate) fn new (criterion : & mut Criterion < M > , group_name : String) -> BenchmarkGroup < '_ , M > { BenchmarkGroup { criterion , group_name , all_ids : vec ! [] , any_matched : false , partial_config : PartialBenchmarkConfig :: default () , throughput : None , } } # [doc = " Benchmark the given parameterless function inside this benchmark group."] pub fn bench_function < ID : IntoBenchmarkId , F > (& mut self , id : ID , mut f : F) -> & mut Self where F : FnMut (& mut Bencher < '_ , M >) , { self . run_bench (id . into_benchmark_id () , & () , | b , _ | f (b)) ; self } # [doc = " Benchmark the given parameterized function inside this benchmark group."] pub fn bench_with_input < ID : IntoBenchmarkId , F , I > (& mut self , id : ID , input : & I , f : F ,) -> & mut Self where F : FnMut (& mut Bencher < '_ , M > , & I) , I : ? Sized , { self . run_bench (id . into_benchmark_id () , input , f) ; self } fn run_bench < F , I > (& mut self , id : BenchmarkId , input : & I , f : F) where F : FnMut (& mut Bencher < '_ , M > , & I) , I : ? Sized , { let config = self . partial_config . to_complete (& self . criterion . config) ; let report_context = ReportContext { output_directory : self . criterion . output_directory . clone () , plot_config : self . partial_config . plot_config . clone () , } ; let mut id = InternalBenchmarkId :: new (self . group_name . clone () , id . function_name , id . parameter , self . throughput . clone () ,) ; assert ! (! self . all_ids . contains (& id) , "Benchmark IDs must be unique within a group. Encountered duplicated benchmark ID {}" , & id) ; id . ensure_directory_name_unique (& self . criterion . all_directories) ; self . criterion . all_directories . insert (id . as_directory_name () . to_owned ()) ; id . ensure_title_unique (& self . criterion . all_titles) ; self . criterion . all_titles . insert (id . as_title () . to_owned ()) ; let do_run = self . criterion . filter_matches (id . id ()) ; self . any_matched |= do_run ; let mut func = Function :: new (f) ; match & self . criterion . mode { Mode :: Benchmark => { if let Some (conn) = & self . criterion . connection { if do_run { conn . send (& OutgoingMessage :: BeginningBenchmark { id : (& id) . into () }) . unwrap () ; } else { conn . send (& OutgoingMessage :: SkippingBenchmark { id : (& id) . into () }) . unwrap () ; } } if do_run { analysis :: common (& id , & mut func , & config , self . criterion , & report_context , input , self . throughput . clone () ,) ; } } Mode :: List (_) => { if do_run { println ! ("{}: benchmark" , id) ; } } Mode :: Test => { if do_run { self . criterion . report . test_start (& id , & report_context) ; func . test (& self . criterion . measurement , input) ; self . criterion . report . test_pass (& id , & report_context) ; } } & Mode :: Profile (duration) => { if do_run { func . profile (& self . criterion . measurement , & id , self . criterion , & report_context , duration , input ,) ; } } } self . all_ids . push (id) ; } # [doc = " Consume the benchmark group and generate the summary reports for the group."] # [doc = ""] # [doc = " It is recommended to call this explicitly, but if you forget it will be called when the"] # [doc = " group is dropped."] pub fn finish (self) { :: std :: mem :: drop (self) ; } }
    };
}

impl_22!();