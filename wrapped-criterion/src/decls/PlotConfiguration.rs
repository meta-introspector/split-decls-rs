macro_rules! deps {
    () => {
        Criterion!();
        AxisScale!();
    };
}

macro_rules! PlotConfiguration {
    () => {
        deps!();
        # [doc = " Contains the configuration options for the plots generated by a particular benchmark"] # [doc = " or benchmark group."] # [doc = ""] # [doc = " ```rust"] # [doc = " use self::criterion::{Bencher, Criterion, PlotConfiguration, AxisScale};"] # [doc = ""] # [doc = " let plot_config = PlotConfiguration::default()"] # [doc = "     .summary_scale(AxisScale::Logarithmic);"] # [doc = ""] # [doc = " // Using Criterion::default() for simplicity; normally you'd use the macros."] # [doc = " let mut criterion = Criterion::default();"] # [doc = " let mut benchmark_group = criterion.benchmark_group(\"Group name\");"] # [doc = " benchmark_group.plot_config(plot_config);"] # [doc = " // Use benchmark group"] # [doc = " ```"] # [derive (Debug , Default , Clone)] pub struct PlotConfiguration { summary_scale : AxisScale , }
    };
}

PlotConfiguration!()