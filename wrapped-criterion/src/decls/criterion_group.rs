macro_rules! deps {
    () => {
        Criterion!();
    };
}

macro_rules! criterion_group {
    () => {
        deps!();
        # [doc = " Macro used to define a function group for the benchmark harness; see the"] # [doc = " [`criterion_main!`](crate::criterion_main) macro for more details."] # [doc = ""] # [doc = " This is used to define a function group; a collection of functions to call with a common"] # [doc = " Criterion configuration. Accepts two forms which can be seen below."] # [doc = ""] # [doc = " Note that the group name given here is not important, it must simply be"] # [doc = " unique. Note also that this macro is not related to the"] # [doc = " [`Criterion::benchmark_group`](crate::Criterion::benchmark_group)  function"] # [doc = " or the [`BenchmarkGroup`](crate::BenchmarkGroup) type."] # [doc = ""] # [doc = " # Examples:"] # [doc = ""] # [doc = " Complete form:"] # [doc = ""] # [doc = " ```"] # [doc = " # use criterion::{criterion_group, Criterion};"] # [doc = " # fn bench_method1(c: &mut Criterion) {"] # [doc = " # }"] # [doc = " #"] # [doc = " # fn bench_method2(c: &mut Criterion) {"] # [doc = " # }"] # [doc = " #"] # [doc = " criterion_group!{"] # [doc = "     name = benches;"] # [doc = "     config = Criterion::default();"] # [doc = "     targets = bench_method1, bench_method2"] # [doc = " }"] # [doc = " #"] # [doc = " # fn main() {}"] # [doc = " ```"] # [doc = ""] # [doc = " In this form, all of the options are clearly spelled out. This expands to"] # [doc = " a function named `benches`, which uses the given config expression to create"] # [doc = " an instance of the [`Criterion`](crate::Criterion) struct. This is then"] # [doc = " passed by mutable reference to the targets."] # [doc = ""] # [doc = " Compact Form:"] # [doc = ""] # [doc = " ```"] # [doc = " # use criterion::{criterion_group, Criterion};"] # [doc = " # fn bench_method1(c: &mut Criterion) {"] # [doc = " # }"] # [doc = " #"] # [doc = " # fn bench_method2(c: &mut Criterion) {"] # [doc = " # }"] # [doc = " #"] # [doc = " criterion_group!(benches, bench_method1, bench_method2);"] # [doc = " #"] # [doc = " # fn main() {}"] # [doc = " ```"] # [doc = " In this form, the first parameter is the name of the group and subsequent"] # [doc = " parameters are the target methods. The Criterion struct will be created using"] # [doc = " the `Criterion::default()` function. If you wish to customize the"] # [doc = " configuration, use the complete form and provide your own configuration"] # [doc = " function."] # [macro_export] macro_rules ! criterion_group { (name = $ name : ident ; config = $ config : expr ; targets = $ ($ target : path) ,+ $ (,) *) => { pub fn $ name () { let mut criterion : $ crate :: Criterion < _ > = $ config . configure_from_args () ; $ ($ target (& mut criterion) ;) + } } ; ($ name : ident , $ ($ target : path) ,+ $ (,) *) => { $ crate :: criterion_group ! { name = $ name ; config = $ crate :: Criterion :: default () ; targets = $ ($ target) ,+ } } }
    };
}

criterion_group!()