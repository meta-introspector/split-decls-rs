macro_rules! deps {
    () => {
        BenchmarkFilter!();
        PlotConfiguration!();
        Duration!();
        Baseline!();
        Profiler!();
        Gnuplot!();
        Sample!();
        Criterion!();
        Html!();
        BenchmarkGroup!();
        CliVerbosity!();
        OutgoingMessage!();
        Mode!();
        Error!();
        PlottingBackend!();
        Measurement!();
        CliReport!();
        ListFormat!();
        ReportContext!();
    };
}

macro_rules! impl_403 {
    () => {
        deps!();
        impl < M : Measurement > Criterion < M > { # [doc = " Changes the measurement for the benchmarks run with this runner. See the"] # [doc = " [`Measurement`] trait for more details"] pub fn with_measurement < M2 : Measurement > (self , m : M2) -> Criterion < M2 > { Criterion { config : self . config , filter : self . filter , report : self . report , baseline_directory : self . baseline_directory , baseline : self . baseline , load_baseline : self . load_baseline , output_directory : self . output_directory , all_directories : self . all_directories , all_titles : self . all_titles , measurement : m , profiler : self . profiler , connection : self . connection , mode : self . mode , } } # [must_use] # [doc = " Changes the internal profiler for benchmarks run with this runner. See"] # [doc = " the [`Profiler`] trait for more details."] pub fn with_profiler < P : Profiler + 'static > (self , p : P) -> Criterion < M > { Criterion { profiler : Box :: new (RefCell :: new (p)) , .. self } } # [must_use] # [doc = " Set the [plotting backend]. By default, Criterion will use `gnuplot` if available,"] # [doc = " or `plotters` if not."] # [doc = ""] # [doc = " Panics if `backend` is [`PlottingBackend::Gnuplot`] and `gnuplot` is not available."] # [doc = ""] # [doc = " [plotting backend]: PlottingBackend"] pub fn plotting_backend (mut self , backend : PlottingBackend) -> Criterion < M > { if let PlottingBackend :: Gnuplot = backend { assert ! (! gnuplot_version () . is_err () , "Gnuplot plotting backend was requested, but gnuplot is not available. \
                To continue, either install Gnuplot or allow Criterion.rs to fall back \
                to using plotters.") ; } self . report . html = backend . create_plotter () . map (Html :: new) ; self } # [must_use] # [doc = " Changes the default size of the sample for benchmarks run with this runner."] # [doc = ""] # [doc = " A bigger sample should yield more accurate results if paired with a sufficiently large"] # [doc = " measurement time."] # [doc = ""] # [doc = " Sample size must be at least 10."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if n < 10"] pub fn sample_size (mut self , n : usize) -> Criterion < M > { assert ! (n >= 10) ; self . config . sample_size = n ; self } # [must_use] # [doc = " Changes the default warm up time for benchmarks run with this runner."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the input duration is zero"] pub fn warm_up_time (mut self , dur : Duration) -> Criterion < M > { assert ! (dur . as_nanos () > 0) ; self . config . warm_up_time = dur ; self } # [must_use] # [doc = " Changes the default measurement time for benchmarks run with this runner."] # [doc = ""] # [doc = " With a longer time, the measurement will become more resilient to transitory peak loads"] # [doc = " caused by external programs"] # [doc = ""] # [doc = " **Note**: If the measurement time is too \"low\", Criterion will automatically increase it"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the input duration in zero"] pub fn measurement_time (mut self , dur : Duration) -> Criterion < M > { assert ! (dur . as_nanos () > 0) ; self . config . measurement_time = dur ; self } # [must_use] # [doc = " Changes the default number of resamples for benchmarks run with this runner."] # [doc = ""] # [doc = " Number of resamples to use for the"] # [doc = " [bootstrap](http://en.wikipedia.org/wiki/Bootstrapping_(statistics)#Case_resampling)"] # [doc = ""] # [doc = " A larger number of resamples reduces the random sampling errors, which are inherent to the"] # [doc = " bootstrap method, but also increases the analysis time"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the number of resamples is set to zero"] pub fn nresamples (mut self , n : usize) -> Criterion < M > { assert ! (n > 0) ; if n <= 1000 { eprintln ! ("\nWarning: It is not recommended to reduce nresamples below 1000.") ; } self . config . nresamples = n ; self } # [must_use] # [doc = " Changes the default noise threshold for benchmarks run with this runner. The noise threshold"] # [doc = " is used to filter out small changes in performance, even if they are statistically"] # [doc = " significant. Sometimes benchmarking the same code twice will result in small but"] # [doc = " statistically significant differences solely because of noise. This provides a way to filter"] # [doc = " out some of these false positives at the cost of making it harder to detect small changes"] # [doc = " to the true performance of the benchmark."] # [doc = ""] # [doc = " The default is 0.01, meaning that changes smaller than 1% will be ignored."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the threshold is set to a negative value"] pub fn noise_threshold (mut self , threshold : f64) -> Criterion < M > { assert ! (threshold >= 0.0) ; self . config . noise_threshold = threshold ; self } # [must_use] # [doc = " Changes the default confidence level for benchmarks run with this runner. The confidence"] # [doc = " level is the desired probability that the true runtime lies within the estimated"] # [doc = " [confidence interval](https://en.wikipedia.org/wiki/Confidence_interval). The default is"] # [doc = " 0.95, meaning that the confidence interval should capture the true value 95% of the time."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the confidence level is set to a value outside the `(0, 1)` range"] pub fn confidence_level (mut self , cl : f64) -> Criterion < M > { assert ! (cl > 0.0 && cl < 1.0) ; if cl < 0.5 { eprintln ! ("\nWarning: It is not recommended to reduce confidence level below 0.5.") ; } self . config . confidence_level = cl ; self } # [must_use] # [doc = " Changes the default [significance level](https://en.wikipedia.org/wiki/Statistical_significance)"] # [doc = " for benchmarks run with this runner. This is used to perform a"] # [doc = " [hypothesis test](https://en.wikipedia.org/wiki/Statistical_hypothesis_testing) to see if"] # [doc = " the measurements from this run are different from the measured performance of the last run."] # [doc = " The significance level is the desired probability that two measurements of identical code"] # [doc = " will be considered 'different' due to noise in the measurements. The default value is 0.05,"] # [doc = " meaning that approximately 5% of identical benchmarks will register as different due to"] # [doc = " noise."] # [doc = ""] # [doc = " This presents a trade-off. By setting the significance level closer to 0.0, you can increase"] # [doc = " the statistical robustness against noise, but it also weakens Criterion.rs' ability to"] # [doc = " detect small but real changes in the performance. By setting the significance level"] # [doc = " closer to 1.0, Criterion.rs will be more able to detect small true changes, but will also"] # [doc = " report more spurious differences."] # [doc = ""] # [doc = " See also the noise threshold setting."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the significance level is set to a value outside the `(0, 1)` range"] pub fn significance_level (mut self , sl : f64) -> Criterion < M > { assert ! (sl > 0.0 && sl < 1.0) ; self . config . significance_level = sl ; self } # [must_use] # [doc = " Enables plotting"] pub fn with_plots (mut self) -> Criterion < M > { if self . connection . is_none () && self . report . html . is_none () { let default_backend = default_plotting_backend () . create_plotter () ; if let Some (backend) = default_backend { self . report . html = Some (Html :: new (backend)) ; } else { panic ! ("Cannot find a default plotting backend!") ; } } self } # [must_use] # [doc = " Disables plotting"] pub fn without_plots (mut self) -> Criterion < M > { self . report . html = None ; self } # [must_use] # [doc = " Names an explicit baseline and enables overwriting the previous results."] pub fn save_baseline (mut self , baseline : String) -> Criterion < M > { self . baseline_directory = baseline ; self . baseline = Baseline :: Save ; self } # [must_use] # [doc = " Names an explicit baseline and disables overwriting the previous results."] pub fn retain_baseline (mut self , baseline : String , strict : bool) -> Criterion < M > { self . baseline_directory = baseline ; self . baseline = if strict { Baseline :: CompareStrict } else { Baseline :: CompareLenient } ; self } # [must_use] # [doc = " Filters the benchmarks. Only benchmarks with names that contain the"] # [doc = " given string will be executed."] # [doc = ""] # [doc = " This overwrites [`Self::with_benchmark_filter`]."] pub fn with_filter < S : Into < String > > (mut self , filter : S) -> Criterion < M > { let filter_text = filter . into () ; let filter = Regex :: new (& filter_text) . unwrap_or_else (| err | { panic ! ("Unable to parse '{}' as a regular expression: {}" , filter_text , err) }) ; self . filter = BenchmarkFilter :: Regex (filter) ; self } # [doc = " Only run benchmarks specified by the given filter."] # [doc = ""] # [doc = " This overwrites [`Self::with_filter`]."] pub fn with_benchmark_filter (mut self , filter : BenchmarkFilter) -> Criterion < M > { self . filter = filter ; self } # [must_use] # [doc = " Override whether the CLI output will be colored or not. Usually you would use the `--color`"] # [doc = " CLI argument, but this is available for programmmatic use as well."] pub fn with_output_color (mut self , enabled : bool) -> Criterion < M > { self . report . cli . enable_text_coloring = enabled ; self } # [doc = " Set the output directory (currently for testing only)"] # [must_use] # [doc (hidden)] pub fn output_directory (mut self , path : & Path) -> Criterion < M > { path . clone_into (& mut self . output_directory) ; self } # [doc = " Set the profile time (currently for testing only)"] # [must_use] # [doc (hidden)] pub fn profile_time (mut self , profile_time : Option < Duration >) -> Criterion < M > { match profile_time { Some (time) => self . mode = Mode :: Profile (time) , None => self . mode = Mode :: Benchmark , } self } # [doc = " Generate the final summary at the end of a run."] # [doc (hidden)] pub fn final_summary (& self) { if ! self . mode . is_benchmark () { return ; } let report_context = ReportContext { output_directory : self . output_directory . clone () , plot_config : PlotConfiguration :: default () , } ; self . report . final_summary (& report_context) ; } # [doc = " Configure this criterion struct based on the command-line arguments to"] # [doc = " this process."] # [must_use] # [allow (clippy :: cognitive_complexity)] pub fn configure_from_args (mut self) -> Criterion < M > { use clap :: { value_parser , Arg , Command } ; let matches = Command :: new ("Criterion Benchmark") . arg (Arg :: new ("FILTER") . help ("Skip benchmarks whose names do not contain FILTER.") . index (1)) . arg (Arg :: new ("color") . short ('c') . long ("color") . alias ("colour") . value_parser (["auto" , "always" , "never"]) . default_value ("auto") . help ("Configure coloring of output. always = always colorize output, never = never colorize output, auto = colorize output if output is a tty and compiled for unix.")) . arg (Arg :: new ("verbose") . short ('v') . long ("verbose") . num_args (0) . help ("Print additional statistical information.")) . arg (Arg :: new ("quiet") . long ("quiet") . num_args (0) . conflicts_with ("verbose") . help ("Print only the benchmark results.")) . arg (Arg :: new ("noplot") . short ('n') . long ("noplot") . num_args (0) . help ("Disable plot and HTML generation.")) . arg (Arg :: new ("save-baseline") . short ('s') . long ("save-baseline") . default_value ("base") . help ("Save results under a named baseline.")) . arg (Arg :: new ("discard-baseline") . long ("discard-baseline") . num_args (0) . conflicts_with_all (["save-baseline" , "baseline" , "baseline-lenient"]) . help ("Discard benchmark results.")) . arg (Arg :: new ("baseline") . short ('b') . long ("baseline") . conflicts_with_all (["save-baseline" , "baseline-lenient"]) . help ("Compare to a named baseline. If any benchmarks do not have the specified baseline this command fails.")) . arg (Arg :: new ("baseline-lenient") . long ("baseline-lenient") . conflicts_with_all (["save-baseline" , "baseline"]) . help ("Compare to a named baseline. If any benchmarks do not have the specified baseline then just those benchmarks are not compared against the baseline while every other benchmark is compared against the baseline.")) . arg (Arg :: new ("list") . long ("list") . num_args (0) . help ("List all benchmarks") . conflicts_with_all (["test" , "profile-time"])) . arg (Arg :: new ("format") . long ("format") . value_parser (["pretty" , "terse"]) . default_value ("pretty") . help ("Output formatting")) . arg (Arg :: new ("ignored") . long ("ignored") . num_args (0) . help ("List or run ignored benchmarks (currently means skip all benchmarks)")) . arg (Arg :: new ("exact") . long ("exact") . num_args (0) . help ("Run benchmarks that exactly match the provided filter")) . arg (Arg :: new ("profile-time") . long ("profile-time") . value_parser (value_parser ! (f64)) . help ("Iterate each benchmark for approximately the given number of seconds, doing no analysis and without storing the results. Useful for running the benchmarks in a profiler.") . conflicts_with_all (["test" , "list"])) . arg (Arg :: new ("load-baseline") . long ("load-baseline") . conflicts_with ("profile-time") . requires ("baseline") . help ("Load a previous baseline instead of sampling new data.")) . arg (Arg :: new ("sample-size") . long ("sample-size") . value_parser (value_parser ! (usize)) . help (format ! ("Changes the default size of the sample for this run. [default: {}]" , self . config . sample_size))) . arg (Arg :: new ("warm-up-time") . long ("warm-up-time") . value_parser (value_parser ! (f64)) . help (format ! ("Changes the default warm up time for this run. [default: {}]" , self . config . warm_up_time . as_secs ()))) . arg (Arg :: new ("measurement-time") . long ("measurement-time") . value_parser (value_parser ! (f64)) . help (format ! ("Changes the default measurement time for this run. [default: {}]" , self . config . measurement_time . as_secs ()))) . arg (Arg :: new ("nresamples") . long ("nresamples") . value_parser (value_parser ! (usize)) . help (format ! ("Changes the default number of resamples for this run. [default: {}]" , self . config . nresamples))) . arg (Arg :: new ("noise-threshold") . long ("noise-threshold") . value_parser (value_parser ! (f64)) . help (format ! ("Changes the default noise threshold for this run. [default: {}]" , self . config . noise_threshold))) . arg (Arg :: new ("confidence-level") . long ("confidence-level") . value_parser (value_parser ! (f64)) . help (format ! ("Changes the default confidence level for this run. [default: {}]" , self . config . confidence_level))) . arg (Arg :: new ("significance-level") . long ("significance-level") . value_parser (value_parser ! (f64)) . help (format ! ("Changes the default significance level for this run. [default: {}]" , self . config . significance_level))) . arg (Arg :: new ("quick") . long ("quick") . num_args (0) . conflicts_with ("sample-size") . help (format ! ("Benchmark only until the significance level has been reached [default: {}]" , self . config . quick_mode))) . arg (Arg :: new ("test") . hide (true) . long ("test") . num_args (0) . help ("Run the benchmarks once, to verify that they execute successfully, but do not measure or report the results.") . conflicts_with_all (["list" , "profile-time"])) . arg (Arg :: new ("bench") . hide (true) . long ("bench") . num_args (0)) . arg (Arg :: new ("plotting-backend") . long ("plotting-backend") . value_parser (["gnuplot" , "plotters"]) . help ("Set the plotting backend. By default, Criterion.rs will use the gnuplot backend if gnuplot is available, or the plotters backend if it isn't.")) . arg (Arg :: new ("output-format") . long ("output-format") . value_parser (["criterion" , "bencher"]) . default_value ("criterion") . help ("Change the CLI output format. By default, Criterion.rs will use its own format. If output format is set to 'bencher', Criterion.rs will print output in a format that resembles the 'bencher' crate.")) . arg (Arg :: new ("nocapture") . long ("nocapture") . num_args (0) . hide (true) . help ("Ignored, but added for compatibility with libtest.")) . arg (Arg :: new ("show-output") . long ("show-output") . num_args (0) . hide (true) . help ("Ignored, but added for compatibility with libtest.")) . arg (Arg :: new ("include-ignored") . long ("include-ignored") . num_args (0) . hide (true) . help ("Ignored, but added for compatibility with libtest.")) . arg (Arg :: new ("version") . hide (true) . short ('V') . long ("version") . num_args (0)) . after_help ("
This executable is a Criterion.rs benchmark.
See https://github.com/bheisler/criterion.rs for more details.

To enable debug output, define the environment variable CRITERION_DEBUG.
Criterion.rs will output more debug information and will save the gnuplot
scripts alongside the generated plots.

To test that the benchmarks work, run `cargo test --benches`

NOTE: If you see an 'unrecognized option' error using any of the options above, see:
https://bheisler.github.io/criterion.rs/book/faq.html
") . get_matches () ; if self . connection . is_some () { if let Some (color) = matches . get_one :: < String > ("color") { if color != "auto" { eprintln ! ("Warning: --color will be ignored when running with cargo-criterion. Use `cargo criterion --color {} -- <args>` instead." , color) ; } } if matches . get_flag ("verbose") { eprintln ! ("Warning: --verbose will be ignored when running with cargo-criterion. Use `cargo criterion --output-format verbose -- <args>` instead.") ; } if matches . get_flag ("noplot") { eprintln ! ("Warning: --noplot will be ignored when running with cargo-criterion. Use `cargo criterion --plotting-backend disabled -- <args>` instead.") ; } if let Some (backend) = matches . get_one :: < String > ("plotting-backend") { eprintln ! ("Warning: --plotting-backend will be ignored when running with cargo-criterion. Use `cargo criterion --plotting-backend {} -- <args>` instead." , backend) ; } if let Some (format) = matches . get_one :: < String > ("output-format") { if format != "criterion" { eprintln ! ("Warning: --output-format will be ignored when running with cargo-criterion. Use `cargo criterion --output-format {} -- <args>` instead." , format) ; } } if matches . contains_id ("baseline") || matches . get_one :: < String > ("save-baseline") . is_some_and (| base | base != "base") || matches . contains_id ("load-baseline") { eprintln ! ("Error: baselines are not supported when running with cargo-criterion.") ; std :: process :: exit (1) ; } } let bench = matches . get_flag ("bench") ; let test = matches . get_flag ("test") ; let test_mode = match (bench , test) { (true , true) => true , (true , false) => false , (false , _) => true , } ; self . mode = if matches . get_flag ("list") { let list_format = match matches . get_one :: < String > ("format") . expect ("a default value was provided for this") . as_str () { "pretty" => ListFormat :: Pretty , "terse" => ListFormat :: Terse , other => unreachable ! ("unrecognized value for --format that isn't part of possible-values: {}" , other) , } ; Mode :: List (list_format) } else if test_mode { Mode :: Test } else if let Some (& num_seconds) = matches . get_one ("profile-time") { if num_seconds < 1.0 { eprintln ! ("Profile time must be at least one second.") ; std :: process :: exit (1) ; } Mode :: Profile (Duration :: from_secs_f64 (num_seconds)) } else { Mode :: Benchmark } ; if ! self . mode . is_benchmark () { self . connection = None ; } let filter = if matches . get_flag ("ignored") { BenchmarkFilter :: RejectAll } else if let Some (filter) = matches . get_one :: < String > ("FILTER") { if matches . get_flag ("exact") { BenchmarkFilter :: Exact (filter . to_owned ()) } else { let regex = Regex :: new (filter) . unwrap_or_else (| err | { panic ! ("Unable to parse '{}' as a regular expression: {}" , filter , err) }) ; BenchmarkFilter :: Regex (regex) } } else { BenchmarkFilter :: AcceptAll } ; self = self . with_benchmark_filter (filter) ; match matches . get_one ("plotting-backend") . map (String :: as_str) { Some ("gnuplot") => self = self . plotting_backend (PlottingBackend :: Gnuplot) , Some ("plotters") => self = self . plotting_backend (PlottingBackend :: Plotters) , Some (val) => panic ! ("Unexpected plotting backend '{}'" , val) , None => { } } if matches . get_flag ("noplot") { self = self . without_plots () ; } if let Some (dir) = matches . get_one :: < String > ("save-baseline") { self . baseline = Baseline :: Save ; dir . clone_into (& mut self . baseline_directory) ; } if matches . get_flag ("discard-baseline") { self . baseline = Baseline :: Discard ; } if let Some (dir) = matches . get_one :: < String > ("baseline") { self . baseline = Baseline :: CompareStrict ; dir . clone_into (& mut self . baseline_directory) ; } if let Some (dir) = matches . get_one :: < String > ("baseline-lenient") { self . baseline = Baseline :: CompareLenient ; dir . clone_into (& mut self . baseline_directory) ; } if self . connection . is_some () { self . report . cli_enabled = false ; self . report . bencher_enabled = false ; self . report . csv_enabled = false ; self . report . html = None ; } else { match matches . get_one ("output-format") . map (String :: as_str) { Some ("bencher") => { self . report . bencher_enabled = true ; self . report . cli_enabled = false ; } _ => { let verbose = matches . get_flag ("verbose") ; let verbosity = if verbose { CliVerbosity :: Verbose } else if matches . get_flag ("quiet") { CliVerbosity :: Quiet } else { CliVerbosity :: Normal } ; let stdout_isatty = stdout () . is_terminal () ; let mut enable_text_overwrite = stdout_isatty && ! verbose && ! debug_enabled () ; let enable_text_coloring ; match matches . get_one ("color") . map (String :: as_str) { Some ("always") => { enable_text_coloring = true ; } Some ("never") => { enable_text_coloring = false ; enable_text_overwrite = false ; } _ => enable_text_coloring = stdout_isatty , } ; self . report . bencher_enabled = false ; self . report . cli_enabled = true ; self . report . cli = CliReport :: new (enable_text_overwrite , enable_text_coloring , verbosity) ; } } ; } if let Some (dir) = matches . get_one :: < String > ("load-baseline") { self . load_baseline = Some (dir . to_owned ()) ; } if let Some (& num_size) = matches . get_one ("sample-size") { assert ! (num_size >= 10) ; self . config . sample_size = num_size ; } if let Some (& num_seconds) = matches . get_one ("warm-up-time") { let dur = std :: time :: Duration :: from_secs_f64 (num_seconds) ; assert ! (dur . as_nanos () > 0) ; self . config . warm_up_time = dur ; } if let Some (& num_seconds) = matches . get_one ("measurement-time") { let dur = std :: time :: Duration :: from_secs_f64 (num_seconds) ; assert ! (dur . as_nanos () > 0) ; self . config . measurement_time = dur ; } if let Some (& num_resamples) = matches . get_one ("nresamples") { assert ! (num_resamples > 0) ; self . config . nresamples = num_resamples ; } if let Some (& num_noise_threshold) = matches . get_one ("noise-threshold") { assert ! (num_noise_threshold > 0.0) ; self . config . noise_threshold = num_noise_threshold ; } if let Some (& num_confidence_level) = matches . get_one ("confidence-level") { assert ! (num_confidence_level > 0.0 && num_confidence_level < 1.0) ; self . config . confidence_level = num_confidence_level ; } if let Some (& num_significance_level) = matches . get_one ("significance-level") { assert ! (num_significance_level > 0.0 && num_significance_level < 1.0) ; self . config . significance_level = num_significance_level ; } if matches . get_flag ("quick") { self . config . quick_mode = true ; } self } fn filter_matches (& self , id : & str) -> bool { match & self . filter { BenchmarkFilter :: AcceptAll => true , BenchmarkFilter :: Regex (regex) => regex . is_match (id) , BenchmarkFilter :: Exact (exact) => id == exact , BenchmarkFilter :: RejectAll => false , } } # [doc = " Returns true iff we should save the benchmark results in"] # [doc = " json files on the local disk."] fn should_save_baseline (& self) -> bool { self . connection . is_none () && self . load_baseline . is_none () && ! matches ! (self . baseline , Baseline :: Discard) } # [doc = " Return a benchmark group. All benchmarks performed using a benchmark group will be"] # [doc = " grouped together in the final report."] # [doc = ""] # [doc = " # Examples:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use criterion::{criterion_group, criterion_main, Criterion};"] # [doc = ""] # [doc = " fn bench_simple(c: &mut Criterion) {"] # [doc = "     let mut group = c.benchmark_group(\"My Group\");"] # [doc = ""] # [doc = "     // Now we can perform benchmarks with this group"] # [doc = "     group.bench_function(\"Bench 1\", |b| b.iter(|| 1 ));"] # [doc = "     group.bench_function(\"Bench 2\", |b| b.iter(|| 2 ));"] # [doc = ""] # [doc = "     group.finish();"] # [doc = " }"] # [doc = " criterion_group!(benches, bench_simple);"] # [doc = " criterion_main!(benches);"] # [doc = " ```"] # [doc = " # Panics:"] # [doc = " Panics if the group name is empty"] pub fn benchmark_group < S : Into < String > > (& mut self , group_name : S) -> BenchmarkGroup < '_ , M > { let group_name = group_name . into () ; assert ! (! group_name . is_empty () , "Group name must not be empty.") ; if let Some (conn) = & self . connection { conn . send (& OutgoingMessage :: BeginningBenchmarkGroup { group : & group_name }) . unwrap () ; } BenchmarkGroup :: new (self , group_name) } }
    };
}

impl_403!();