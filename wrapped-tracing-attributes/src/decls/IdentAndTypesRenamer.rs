macro_rules! IdentAndTypesRenamer {
    () => {
        # [doc = " A visitor struct to replace idents and types in some piece"] # [doc = " of code (e.g. the \"self\" and \"Self\" tokens in user-supplied"] # [doc = " fields expressions when the function is generated by an old"] # [doc = " version of async-trait)."] struct IdentAndTypesRenamer < 'a > { types : Vec < (& 'a str , TypePath) > , idents : Vec < (Ident , Ident) > , }
    };
}

IdentAndTypesRenamer!();