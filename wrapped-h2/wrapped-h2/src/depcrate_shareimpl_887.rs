// Generated macro for impl_887 (impl)
macro_rules! Depcrate_shareimpl_887 {
() => {
// Module: crate::share
// Provides: {"impl_887"}
// Dependencies: {}
impl < B : Buf > SendStream < B > { pub (crate) fn new (inner : proto :: StreamRef < B >) -> Self { SendStream { inner } } # [doc = " Requests capacity to send data."] # [doc = ""] # [doc = " This function is used to express intent to send data. This requests"] # [doc = " connection level capacity. Once the capacity is available, it is"] # [doc = " assigned to the stream and not reused by other streams."] # [doc = ""] # [doc = " This function may be called repeatedly. The `capacity` argument is the"] # [doc = " **total** amount of requested capacity. Sequential calls to"] # [doc = " `reserve_capacity` are *not* additive. Given the following:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use h2::*;"] # [doc = " # fn doc(mut send_stream: SendStream<&'static [u8]>) {"] # [doc = " send_stream.reserve_capacity(100);"] # [doc = " send_stream.reserve_capacity(200);"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " After the second call to `reserve_capacity`, the *total* requested"] # [doc = " capacity will be 200."] # [doc = ""] # [doc = " `reserve_capacity` is also used to cancel previous capacity requests."] # [doc = " Given the following:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use h2::*;"] # [doc = " # fn doc(mut send_stream: SendStream<&'static [u8]>) {"] # [doc = " send_stream.reserve_capacity(100);"] # [doc = " send_stream.reserve_capacity(0);"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " After the second call to `reserve_capacity`, the *total* requested"] # [doc = " capacity will be 0, i.e. there is no requested capacity for the stream."] # [doc = ""] # [doc = " If `reserve_capacity` is called with a lower value than the amount of"] # [doc = " capacity **currently** assigned to the stream, this capacity will be"] # [doc = " returned to the connection to be re-assigned to other streams."] # [doc = ""] # [doc = " Also, the amount of capacity that is reserved gets decremented as data"] # [doc = " is sent. For example:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use h2::*;"] # [doc = " # async fn doc(mut send_stream: SendStream<&'static [u8]>) {"] # [doc = " send_stream.reserve_capacity(100);"] # [doc = ""] # [doc = " send_stream.send_data(b\"hello\", false).unwrap();"] # [doc = " // At this point, the total amount of requested capacity is 95 bytes."] # [doc = ""] # [doc = " // Calling `reserve_capacity` with `100` again essentially requests an"] # [doc = " // additional 5 bytes."] # [doc = " send_stream.reserve_capacity(100);"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " See [Flow control](struct.SendStream.html#flow-control) for an overview"] # [doc = " of how send flow control works."] pub fn reserve_capacity (& mut self , capacity : usize) { self . inner . reserve_capacity (capacity as WindowSize) } # [doc = " Returns the stream's current send capacity."] # [doc = ""] # [doc = " This allows the caller to check the current amount of available capacity"] # [doc = " before sending data."] pub fn capacity (& self) -> usize { self . inner . capacity () as usize } # [doc = " Requests to be notified when the stream's capacity increases."] # [doc = ""] # [doc = " Before calling this, capacity should be requested with"] # [doc = " `reserve_capacity`. Once capacity is requested, the connection will"] # [doc = " assign capacity to the stream **as it becomes available**. There is no"] # [doc = " guarantee as to when and in what increments capacity gets assigned to"] # [doc = " the stream."] # [doc = ""] # [doc = " To get notified when the available capacity increases, the caller calls"] # [doc = " `poll_capacity`, which returns `Ready(Some(n))` when `n` has been"] # [doc = " increased by the connection. Note that `n` here represents the **total**"] # [doc = " amount of assigned capacity at that point in time. It is also possible"] # [doc = " that `n` is lower than the previous call if, since then, the caller has"] # [doc = " sent data."] pub fn poll_capacity (& mut self , cx : & mut Context) -> Poll < Option < Result < usize , crate :: Error > > > { self . inner . poll_capacity (cx) . map_ok (| w | w as usize) . map_err (Into :: into) } # [doc = " Sends a single data frame to the remote peer."] # [doc = ""] # [doc = " This function may be called repeatedly as long as `end_of_stream` is set"] # [doc = " to `false`. Setting `end_of_stream` to `true` sets the end stream flag"] # [doc = " on the data frame. Any further calls to `send_data` or `send_trailers`"] # [doc = " will return an [`Error`]."] # [doc = ""] # [doc = " `send_data` can be called without reserving capacity. In this case, the"] # [doc = " data is buffered and the capacity is implicitly requested. Once the"] # [doc = " capacity becomes available, the data is flushed to the connection."] # [doc = " However, this buffering is unbounded. As such, sending large amounts of"] # [doc = " data without reserving capacity before hand could result in large"] # [doc = " amounts of data being buffered in memory."] # [doc = ""] # [doc = " [`Error`]: struct.Error.html"] pub fn send_data (& mut self , data : B , end_of_stream : bool) -> Result < () , crate :: Error > { self . inner . send_data (data , end_of_stream) . map_err (Into :: into) } # [doc = " Sends trailers to the remote peer."] # [doc = ""] # [doc = " Sending trailers implicitly closes the send stream. Once the send stream"] # [doc = " is closed, no more data can be sent."] pub fn send_trailers (& mut self , trailers : HeaderMap) -> Result < () , crate :: Error > { self . inner . send_trailers (trailers) . map_err (Into :: into) } # [doc = " Resets the stream."] # [doc = ""] # [doc = " This cancels the request / response exchange. If the response has not"] # [doc = " yet been received, the associated `ResponseFuture` will return an"] # [doc = " [`Error`] to reflect the canceled exchange."] # [doc = ""] # [doc = " [`Error`]: struct.Error.html"] pub fn send_reset (& mut self , reason : Reason) { self . inner . send_reset (reason) } # [doc = " Polls to be notified when the client resets this stream."] # [doc = ""] # [doc = " If stream is still open, this returns `Poll::Pending`, and"] # [doc = " registers the task to be notified if a `RST_STREAM` is received."] # [doc = ""] # [doc = " If a `RST_STREAM` frame is received for this stream, calling this"] # [doc = " method will yield the `Reason` for the reset."] # [doc = ""] # [doc = " # Error"] # [doc = ""] # [doc = " If connection sees an error, this returns that error instead of a"] # [doc = " `Reason`."] pub fn poll_reset (& mut self , cx : & mut Context) -> Poll < Result < Reason , crate :: Error > > { self . inner . poll_reset (cx , proto :: PollReset :: Streaming) } # [doc = " Returns the stream ID of this `SendStream`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the lock on the stream store has been poisoned."] pub fn stream_id (& self) -> StreamId { StreamId :: from_internal (self . inner . stream_id ()) } }
};
}
