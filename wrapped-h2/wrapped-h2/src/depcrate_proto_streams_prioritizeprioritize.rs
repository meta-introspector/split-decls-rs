// Generated macro for Prioritize (struct)
macro_rules! Depcrate_proto_streams_prioritizePrioritize {
() => {
// Module: crate::proto::streams::prioritize
// Provides: {"Prioritize"}
// Dependencies: {}
# [doc = " # Warning"] # [doc = ""] # [doc = " Queued streams are ordered by stream ID, as we need to ensure that"] # [doc = " lower-numbered streams are sent headers before higher-numbered ones."] # [doc = " This is because \"idle\" stream IDs – those which have been initiated but"] # [doc = " have yet to receive frames – will be implicitly closed on receipt of a"] # [doc = " frame on a higher stream ID. If these queues was not ordered by stream"] # [doc = " IDs, some mechanism would be necessary to ensure that the lowest-numbered]"] # [doc = " idle stream is opened first."] # [derive (Debug)] pub (super) struct Prioritize { # [doc = " Queue of streams waiting for socket capacity to send a frame."] pending_send : store :: Queue < stream :: NextSend > , # [doc = " Queue of streams waiting for window capacity to produce data."] pending_capacity : store :: Queue < stream :: NextSendCapacity > , # [doc = " Streams waiting for capacity due to max concurrency"] # [doc = ""] # [doc = " The `SendRequest` handle is `Clone`. This enables initiating requests"] # [doc = " from many tasks. However, offering this capability while supporting"] # [doc = " backpressure at some level is tricky. If there are many `SendRequest`"] # [doc = " handles and a single stream becomes available, which handle gets"] # [doc = " assigned that stream? Maybe that handle is no longer ready to send a"] # [doc = " request."] # [doc = ""] # [doc = " The strategy used is to allow each `SendRequest` handle one buffered"] # [doc = " request. A `SendRequest` handle is ready to send a request if it has no"] # [doc = " associated buffered requests. This is the same strategy as `mpsc` in the"] # [doc = " futures library."] pending_open : store :: Queue < stream :: NextOpen > , # [doc = " Connection level flow control governing sent data"] flow : FlowControl , # [doc = " Stream ID of the last stream opened."] last_opened_id : StreamId , # [doc = " What `DATA` frame is currently being sent in the codec."] in_flight_data_frame : InFlightData , # [doc = " The maximum amount of bytes a stream should buffer."] max_buffer_size : usize , }
};
}
