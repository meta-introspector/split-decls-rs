// Generated macro for SendStream (struct)
macro_rules! Depcrate_shareSendStream {
() => {
// Module: crate::share
// Provides: {"SendStream"}
// Dependencies: {}
# [doc = " Sends the body stream and trailers to the remote peer."] # [doc = ""] # [doc = " # Overview"] # [doc = ""] # [doc = " A `SendStream` is provided by [`SendRequest`] and [`SendResponse`] once the"] # [doc = " HTTP/2 message header has been sent sent. It is used to stream the message"] # [doc = " body and send the message trailers. See method level documentation for more"] # [doc = " details."] # [doc = ""] # [doc = " The `SendStream` instance is also used to manage outbound flow control."] # [doc = ""] # [doc = " If a `SendStream` is dropped without explicitly closing the send stream, a"] # [doc = " `RST_STREAM` frame will be sent. This essentially cancels the request /"] # [doc = " response exchange."] # [doc = ""] # [doc = " The ways to explicitly close the send stream are:"] # [doc = ""] # [doc = " * Set `end_of_stream` to true when calling [`send_request`],"] # [doc = "   [`send_response`], or [`send_data`]."] # [doc = " * Send trailers with [`send_trailers`]."] # [doc = " * Explicitly reset the stream with [`send_reset`]."] # [doc = ""] # [doc = " # Flow control"] # [doc = ""] # [doc = " In HTTP/2, data cannot be sent to the remote peer unless there is"] # [doc = " available window capacity on both the stream and the connection. When a data"] # [doc = " frame is sent, both the stream window and the connection window are"] # [doc = " decremented. When the stream level window reaches zero, no further data can"] # [doc = " be sent on that stream. When the connection level window reaches zero, no"] # [doc = " further data can be sent on any stream for that connection."] # [doc = ""] # [doc = " When the remote peer is ready to receive more data, it sends `WINDOW_UPDATE`"] # [doc = " frames. These frames increment the windows. See the [specification] for more"] # [doc = " details on the principles of HTTP/2 flow control."] # [doc = ""] # [doc = " The implications for sending data are that the caller **should** ensure that"] # [doc = " both the stream and the connection has available window capacity before"] # [doc = " loading the data to send into memory. The `SendStream` instance provides the"] # [doc = " necessary APIs to perform this logic. This, however, is not an obligation."] # [doc = " If the caller attempts to send data on a stream when there is no available"] # [doc = " window capacity, the library will buffer the data until capacity becomes"] # [doc = " available, at which point the buffer will be flushed to the connection."] # [doc = ""] # [doc = " **NOTE**: There is no bound on the amount of data that the library will"] # [doc = " buffer. If you are sending large amounts of data, you really should hook"] # [doc = " into the flow control lifecycle. Otherwise, you risk using up significant"] # [doc = " amounts of memory."] # [doc = ""] # [doc = " To hook into the flow control lifecycle, the caller signals to the library"] # [doc = " that it intends to send data by calling [`reserve_capacity`], specifying the"] # [doc = " amount of data, in octets, that the caller intends to send. After this,"] # [doc = " `poll_capacity` is used to be notified when the requested capacity is"] # [doc = " assigned to the stream. Once [`poll_capacity`] returns `Ready` with the number"] # [doc = " of octets available to the stream, the caller is able to actually send the"] # [doc = " data using [`send_data`]."] # [doc = ""] # [doc = " Because there is also a connection level window that applies to **all**"] # [doc = " streams on a connection, when capacity is assigned to a stream (indicated by"] # [doc = " `poll_capacity` returning `Ready`), this capacity is reserved on the"] # [doc = " connection and will **not** be assigned to any other stream. If data is"] # [doc = " never written to the stream, that capacity is effectively lost to other"] # [doc = " streams and this introduces the risk of deadlocking a connection."] # [doc = ""] # [doc = " To avoid throttling data on a connection, the caller should not reserve"] # [doc = " capacity until ready to send data and once any capacity is assigned to the"] # [doc = " stream, the caller should immediately send data consuming this capacity."] # [doc = " There is no guarantee as to when the full capacity requested will become"] # [doc = " available. For example, if the caller requests 64 KB of data and 512 bytes"] # [doc = " become available, the caller should immediately send 512 bytes of data."] # [doc = ""] # [doc = " See [`reserve_capacity`] documentation for more details."] # [doc = ""] # [doc = " [`SendRequest`]: client/struct.SendRequest.html"] # [doc = " [`SendResponse`]: server/struct.SendResponse.html"] # [doc = " [specification]: http://httpwg.org/specs/rfc7540.html#FlowControl"] # [doc = " [`reserve_capacity`]: #method.reserve_capacity"] # [doc = " [`poll_capacity`]: #method.poll_capacity"] # [doc = " [`send_data`]: #method.send_data"] # [doc = " [`send_request`]: client/struct.SendRequest.html#method.send_request"] # [doc = " [`send_response`]: server/struct.SendResponse.html#method.send_response"] # [doc = " [`send_data`]: #method.send_data"] # [doc = " [`send_trailers`]: #method.send_trailers"] # [doc = " [`send_reset`]: #method.send_reset"] # [derive (Debug)] pub struct SendStream < B > { inner : proto :: StreamRef < B > , }
};
}
