macro_rules! deps {
    () => {
        RwLock!();
        ArcRwLockWriteGuard!();
        RawRwLock!();
        RwLockWriteGuard!();
        RwLockReadGuard!();
        ArcRwLockReadGuard!();
    };
}

macro_rules! impl_112 {
    () => {
        deps!();
        impl < R : RawRwLock , T : ? Sized > RwLock < R , T > { # [doc = " Creates a new `RwLockReadGuard` without checking if the lock is held."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the thread logically holds a read lock."] # [doc = ""] # [doc = " This function does not increment the read count of the lock. Calling this function when a"] # [doc = " guard has already been produced is undefined behaviour unless the guard was forgotten"] # [doc = " with `mem::forget`."] # [inline] pub unsafe fn make_read_guard_unchecked (& self) -> RwLockReadGuard < '_ , R , T > { RwLockReadGuard { rwlock : self , marker : PhantomData , } } # [doc = " Creates a new `RwLockReadGuard` without checking if the lock is held."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the thread logically holds a write lock."] # [doc = ""] # [doc = " Calling this function when a guard has already been produced is undefined behaviour unless"] # [doc = " the guard was forgotten with `mem::forget`."] # [inline] pub unsafe fn make_write_guard_unchecked (& self) -> RwLockWriteGuard < '_ , R , T > { RwLockWriteGuard { rwlock : self , marker : PhantomData , } } # [doc = " Locks this `RwLock` with shared read access, blocking the current thread"] # [doc = " until it can be acquired."] # [doc = ""] # [doc = " The calling thread will be blocked until there are no more writers which"] # [doc = " hold the lock. There may be other readers currently inside the lock when"] # [doc = " this method returns."] # [doc = ""] # [doc = " Note that attempts to recursively acquire a read lock on a `RwLock` when"] # [doc = " the current thread already holds one may result in a deadlock."] # [doc = ""] # [doc = " Returns an RAII guard which will release this thread's shared access"] # [doc = " once it is dropped."] # [inline] # [track_caller] pub fn read (& self) -> RwLockReadGuard < '_ , R , T > { self . raw . lock_shared () ; unsafe { self . make_read_guard_unchecked () } } # [doc = " Attempts to acquire this `RwLock` with shared read access."] # [doc = ""] # [doc = " If the access could not be granted at this time, then `None` is returned."] # [doc = " Otherwise, an RAII guard is returned which will release the shared access"] # [doc = " when it is dropped."] # [doc = ""] # [doc = " This function does not block."] # [inline] # [track_caller] pub fn try_read (& self) -> Option < RwLockReadGuard < '_ , R , T > > { if self . raw . try_lock_shared () { Some (unsafe { self . make_read_guard_unchecked () }) } else { None } } # [doc = " Locks this `RwLock` with exclusive write access, blocking the current"] # [doc = " thread until it can be acquired."] # [doc = ""] # [doc = " This function will not return while other writers or other readers"] # [doc = " currently have access to the lock."] # [doc = ""] # [doc = " Returns an RAII guard which will drop the write access of this `RwLock`"] # [doc = " when dropped."] # [inline] # [track_caller] pub fn write (& self) -> RwLockWriteGuard < '_ , R , T > { self . raw . lock_exclusive () ; unsafe { self . make_write_guard_unchecked () } } # [doc = " Attempts to lock this `RwLock` with exclusive write access."] # [doc = ""] # [doc = " If the lock could not be acquired at this time, then `None` is returned."] # [doc = " Otherwise, an RAII guard is returned which will release the lock when"] # [doc = " it is dropped."] # [doc = ""] # [doc = " This function does not block."] # [inline] # [track_caller] pub fn try_write (& self) -> Option < RwLockWriteGuard < '_ , R , T > > { if self . raw . try_lock_exclusive () { Some (unsafe { self . make_write_guard_unchecked () }) } else { None } } # [doc = " Returns a mutable reference to the underlying data."] # [doc = ""] # [doc = " Since this call borrows the `RwLock` mutably, no actual locking needs to"] # [doc = " take place---the mutable borrow statically guarantees no locks exist."] # [inline] pub fn get_mut (& mut self) -> & mut T { unsafe { & mut * self . data . get () } } # [doc = " Checks whether this `RwLock` is currently locked in any way."] # [inline] # [track_caller] pub fn is_locked (& self) -> bool { self . raw . is_locked () } # [doc = " Check if this `RwLock` is currently exclusively locked."] # [inline] # [track_caller] pub fn is_locked_exclusive (& self) -> bool { self . raw . is_locked_exclusive () } # [doc = " Forcibly unlocks a read lock."] # [doc = ""] # [doc = " This is useful when combined with `mem::forget` to hold a lock without"] # [doc = " the need to maintain a `RwLockReadGuard` object alive, for example when"] # [doc = " dealing with FFI."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the current thread logically owns a"] # [doc = " `RwLockReadGuard` but that guard has be discarded using `mem::forget`."] # [doc = " Behavior is undefined if a rwlock is read-unlocked when not read-locked."] # [inline] # [track_caller] pub unsafe fn force_unlock_read (& self) { self . raw . unlock_shared () ; } # [doc = " Forcibly unlocks a write lock."] # [doc = ""] # [doc = " This is useful when combined with `mem::forget` to hold a lock without"] # [doc = " the need to maintain a `RwLockWriteGuard` object alive, for example when"] # [doc = " dealing with FFI."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the current thread logically owns a"] # [doc = " `RwLockWriteGuard` but that guard has be discarded using `mem::forget`."] # [doc = " Behavior is undefined if a rwlock is write-unlocked when not write-locked."] # [inline] # [track_caller] pub unsafe fn force_unlock_write (& self) { self . raw . unlock_exclusive () ; } # [doc = " Returns the underlying raw reader-writer lock object."] # [doc = ""] # [doc = " Note that you will most likely need to import the `RawRwLock` trait from"] # [doc = " `lock_api` to be able to call functions on the raw"] # [doc = " reader-writer lock."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method is unsafe because it allows unlocking a mutex while"] # [doc = " still holding a reference to a lock guard."] pub unsafe fn raw (& self) -> & R { & self . raw } # [doc = " Returns a raw pointer to the underlying data."] # [doc = ""] # [doc = " This is useful when combined with `mem::forget` to hold a lock without"] # [doc = " the need to maintain a `RwLockReadGuard` or `RwLockWriteGuard` object"] # [doc = " alive, for example when dealing with FFI."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " You must ensure that there are no data races when dereferencing the"] # [doc = " returned pointer, for example if the current thread logically owns a"] # [doc = " `RwLockReadGuard` or `RwLockWriteGuard` but that guard has been discarded"] # [doc = " using `mem::forget`."] # [inline] pub fn data_ptr (& self) -> * mut T { self . data . get () } # [doc = " Creates a new `RwLockReadGuard` without checking if the lock is held."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the thread logically holds a read lock."] # [doc = ""] # [doc = " This function does not increment the read count of the lock. Calling this function when a"] # [doc = " guard has already been produced is undefined behaviour unless the guard was forgotten"] # [doc = " with `mem::forget`.`"] # [cfg (feature = "arc_lock")] # [inline] pub unsafe fn make_arc_read_guard_unchecked (self : & Arc < Self >) -> ArcRwLockReadGuard < R , T > { ArcRwLockReadGuard { rwlock : self . clone () , marker : PhantomData , } } # [doc = " Creates a new `RwLockWriteGuard` without checking if the lock is held."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the thread logically holds a write lock."] # [doc = ""] # [doc = " Calling this function when a guard has already been produced is undefined behaviour unless"] # [doc = " the guard was forgotten with `mem::forget`."] # [cfg (feature = "arc_lock")] # [inline] pub unsafe fn make_arc_write_guard_unchecked (self : & Arc < Self >) -> ArcRwLockWriteGuard < R , T > { ArcRwLockWriteGuard { rwlock : self . clone () , marker : PhantomData , } } # [doc = " Locks this `RwLock` with read access, through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `read` method; however, it requires the `RwLock` to be inside of an `Arc`"] # [doc = " and the resulting read guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn read_arc (self : & Arc < Self >) -> ArcRwLockReadGuard < R , T > { self . raw . lock_shared () ; unsafe { self . make_arc_read_guard_unchecked () } } # [doc = " Attempts to lock this `RwLock` with read access, through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `try_read` method; however, it requires the `RwLock` to be inside of an"] # [doc = " `Arc` and the resulting read guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn try_read_arc (self : & Arc < Self >) -> Option < ArcRwLockReadGuard < R , T > > { if self . raw . try_lock_shared () { Some (unsafe { self . make_arc_read_guard_unchecked () }) } else { None } } # [doc = " Locks this `RwLock` with write access, through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `write` method; however, it requires the `RwLock` to be inside of an `Arc`"] # [doc = " and the resulting write guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn write_arc (self : & Arc < Self >) -> ArcRwLockWriteGuard < R , T > { self . raw . lock_exclusive () ; unsafe { self . make_arc_write_guard_unchecked () } } # [doc = " Attempts to lock this `RwLock` with writ access, through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `try_write` method; however, it requires the `RwLock` to be inside of an"] # [doc = " `Arc` and the resulting write guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn try_write_arc (self : & Arc < Self >) -> Option < ArcRwLockWriteGuard < R , T > > { if self . raw . try_lock_exclusive () { Some (unsafe { self . make_arc_write_guard_unchecked () }) } else { None } } }
    };
}

impl_112!()