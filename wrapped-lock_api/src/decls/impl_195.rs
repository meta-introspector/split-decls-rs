macro_rules! deps {
    () => {
        RawRwLock!();
        MappedRwLockWriteGuard!();
    };
}

macro_rules! impl_195 {
    () => {
        deps!();
        impl < 'a , R : RawRwLock + 'a , T : ? Sized + 'a > MappedRwLockWriteGuard < 'a , R , T > { # [doc = " Make a new `MappedRwLockWriteGuard` for a component of the locked data."] # [doc = ""] # [doc = " This operation cannot fail as the `MappedRwLockWriteGuard` passed"] # [doc = " in already locked the data."] # [doc = ""] # [doc = " This is an associated function that needs to be"] # [doc = " used as `MappedRwLockWriteGuard::map(...)`. A method would interfere with methods of"] # [doc = " the same name on the contents of the locked data."] # [inline] pub fn map < U : ? Sized , F > (s : Self , f : F) -> MappedRwLockWriteGuard < 'a , R , U > where F : FnOnce (& mut T) -> & mut U , { let raw = s . raw ; let data = f (unsafe { & mut * s . data }) ; mem :: forget (s) ; MappedRwLockWriteGuard { raw , data , marker : PhantomData , } } # [doc = " Attempts to make  a new `MappedRwLockWriteGuard` for a component of the"] # [doc = " locked data. The original guard is return if the closure returns `None`."] # [doc = ""] # [doc = " This operation cannot fail as the `MappedRwLockWriteGuard` passed"] # [doc = " in already locked the data."] # [doc = ""] # [doc = " This is an associated function that needs to be"] # [doc = " used as `MappedRwLockWriteGuard::try_map(...)`. A method would interfere with methods of"] # [doc = " the same name on the contents of the locked data."] # [inline] pub fn try_map < U : ? Sized , F > (s : Self , f : F) -> Result < MappedRwLockWriteGuard < 'a , R , U > , Self > where F : FnOnce (& mut T) -> Option < & mut U > , { let raw = s . raw ; let data = match f (unsafe { & mut * s . data }) { Some (data) => data , None => return Err (s) , } ; mem :: forget (s) ; Ok (MappedRwLockWriteGuard { raw , data , marker : PhantomData , }) } # [doc = " Attempts to make  a new `MappedRwLockWriteGuard` for a component of the"] # [doc = " locked data. The original guard is returned alongside arbitrary user data"] # [doc = " if the closure returns `Err`."] # [doc = ""] # [doc = " This operation cannot fail as the `MappedRwLockWriteGuard` passed"] # [doc = " in already locked the data."] # [doc = ""] # [doc = " This is an associated function that needs to be"] # [doc = " used as `MappedRwLockWriteGuard::try_map_or_err(...)`. A method would interfere with methods of"] # [doc = " the same name on the contents of the locked data."] # [inline] pub fn try_map_or_err < U : ? Sized , F , E > (s : Self , f : F ,) -> Result < MappedRwLockWriteGuard < 'a , R , U > , (Self , E) > where F : FnOnce (& mut T) -> Result < & mut U , E > , { let raw = s . raw ; let data = match f (unsafe { & mut * s . data }) { Ok (data) => data , Err (e) => return Err ((s , e)) , } ; mem :: forget (s) ; Ok (MappedRwLockWriteGuard { raw , data , marker : PhantomData , }) } }
    };
}

impl_195!();