macro_rules! deps {
    () => {
        GetThreadId!();
        ArcReentrantMutexGuard!();
        ReentrantMutexGuard!();
        RawMutex!();
        ReentrantMutex!();
    };
}

macro_rules! impl_63 {
    () => {
        deps!();
        impl < R : RawMutex , G : GetThreadId , T : ? Sized > ReentrantMutex < R , G , T > { # [doc = " Creates a new `ReentrantMutexGuard` without checking if the lock is held."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the thread logically holds the lock."] # [doc = ""] # [doc = " Calling this function when a guard has already been produced is undefined behaviour unless"] # [doc = " the guard was forgotten with `mem::forget`."] # [inline] pub unsafe fn make_guard_unchecked (& self) -> ReentrantMutexGuard < '_ , R , G , T > { ReentrantMutexGuard { remutex : & self , marker : PhantomData , } } # [doc = " Acquires a reentrant mutex, blocking the current thread until it is able"] # [doc = " to do so."] # [doc = ""] # [doc = " If the mutex is held by another thread then this function will block the"] # [doc = " local thread until it is available to acquire the mutex. If the mutex is"] # [doc = " already held by the current thread then this function will increment the"] # [doc = " lock reference count and return immediately. Upon returning,"] # [doc = " the thread is the only thread with the mutex held. An RAII guard is"] # [doc = " returned to allow scoped unlock of the lock. When the guard goes out of"] # [doc = " scope, the mutex will be unlocked."] # [inline] # [track_caller] pub fn lock (& self) -> ReentrantMutexGuard < '_ , R , G , T > { self . raw . lock () ; unsafe { self . make_guard_unchecked () } } # [doc = " Attempts to acquire this lock."] # [doc = ""] # [doc = " If the lock could not be acquired at this time, then `None` is returned."] # [doc = " Otherwise, an RAII guard is returned. The lock will be unlocked when the"] # [doc = " guard is dropped."] # [doc = ""] # [doc = " This function does not block."] # [inline] # [track_caller] pub fn try_lock (& self) -> Option < ReentrantMutexGuard < '_ , R , G , T > > { if self . raw . try_lock () { Some (unsafe { self . make_guard_unchecked () }) } else { None } } # [doc = " Returns a mutable reference to the underlying data."] # [doc = ""] # [doc = " Since this call borrows the `ReentrantMutex` mutably, no actual locking needs to"] # [doc = " take place---the mutable borrow statically guarantees no locks exist."] # [inline] pub fn get_mut (& mut self) -> & mut T { unsafe { & mut * self . data . get () } } # [doc = " Checks whether the mutex is currently locked."] # [inline] # [track_caller] pub fn is_locked (& self) -> bool { self . raw . is_locked () } # [doc = " Checks whether the mutex is currently held by the current thread."] # [inline] # [track_caller] pub fn is_owned_by_current_thread (& self) -> bool { self . raw . is_owned_by_current_thread () } # [doc = " Forcibly unlocks the mutex."] # [doc = ""] # [doc = " This is useful when combined with `mem::forget` to hold a lock without"] # [doc = " the need to maintain a `ReentrantMutexGuard` object alive, for example when"] # [doc = " dealing with FFI."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the current thread logically owns a"] # [doc = " `ReentrantMutexGuard` but that guard has be discarded using `mem::forget`."] # [doc = " Behavior is undefined if a mutex is unlocked when not locked."] # [inline] # [track_caller] pub unsafe fn force_unlock (& self) { self . raw . unlock () ; } # [doc = " Returns the underlying raw mutex object."] # [doc = ""] # [doc = " Note that you will most likely need to import the `RawMutex` trait from"] # [doc = " `lock_api` to be able to call functions on the raw mutex."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method is unsafe because it allows unlocking a mutex while"] # [doc = " still holding a reference to a `ReentrantMutexGuard`."] # [inline] pub unsafe fn raw (& self) -> & R { & self . raw . mutex } # [doc = " Returns a raw pointer to the underlying data."] # [doc = ""] # [doc = " This is useful when combined with `mem::forget` to hold a lock without"] # [doc = " the need to maintain a `ReentrantMutexGuard` object alive, for example"] # [doc = " when dealing with FFI."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " You must ensure that there are no data races when dereferencing the"] # [doc = " returned pointer, for example if the current thread logically owns a"] # [doc = " `ReentrantMutexGuard` but that guard has been discarded using"] # [doc = " `mem::forget`."] # [inline] pub fn data_ptr (& self) -> * mut T { self . data . get () } # [doc = " Creates a new `ArcReentrantMutexGuard` without checking if the lock is held."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the thread logically holds the lock."] # [doc = ""] # [doc = " Calling this function when a guard has already been produced is undefined behaviour unless"] # [doc = " the guard was forgotten with `mem::forget`."] # [cfg (feature = "arc_lock")] # [inline] pub unsafe fn make_arc_guard_unchecked (self : & Arc < Self >) -> ArcReentrantMutexGuard < R , G , T > { ArcReentrantMutexGuard { remutex : self . clone () , marker : PhantomData , } } # [doc = " Acquires a reentrant mutex through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `lock` method; however, it requires the `ReentrantMutex` to be inside of an"] # [doc = " `Arc` and the resulting mutex guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn lock_arc (self : & Arc < Self >) -> ArcReentrantMutexGuard < R , G , T > { self . raw . lock () ; unsafe { self . make_arc_guard_unchecked () } } # [doc = " Attempts to acquire a reentrant mutex through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `try_lock` method; however, it requires the `ReentrantMutex` to be inside"] # [doc = " of an `Arc` and the resulting mutex guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn try_lock_arc (self : & Arc < Self >) -> Option < ArcReentrantMutexGuard < R , G , T > > { if self . raw . try_lock () { Some (unsafe { self . make_arc_guard_unchecked () }) } else { None } } }
    };
}

impl_63!();