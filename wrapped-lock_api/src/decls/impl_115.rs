macro_rules! deps {
    () => {
        RwLockReadGuard!();
        RwLock!();
        RawRwLockRecursive!();
        ArcRwLockReadGuard!();
    };
}

macro_rules! impl_115 {
    () => {
        deps!();
        impl < R : RawRwLockRecursive , T : ? Sized > RwLock < R , T > { # [doc = " Locks this `RwLock` with shared read access, blocking the current thread"] # [doc = " until it can be acquired."] # [doc = ""] # [doc = " The calling thread will be blocked until there are no more writers which"] # [doc = " hold the lock. There may be other readers currently inside the lock when"] # [doc = " this method returns."] # [doc = ""] # [doc = " Unlike `read`, this method is guaranteed to succeed without blocking if"] # [doc = " another read lock is held at the time of the call. This allows a thread"] # [doc = " to recursively lock a `RwLock`. However using this method can cause"] # [doc = " writers to starve since readers no longer block if a writer is waiting"] # [doc = " for the lock."] # [doc = ""] # [doc = " Returns an RAII guard which will release this thread's shared access"] # [doc = " once it is dropped."] # [inline] # [track_caller] pub fn read_recursive (& self) -> RwLockReadGuard < '_ , R , T > { self . raw . lock_shared_recursive () ; unsafe { self . make_read_guard_unchecked () } } # [doc = " Attempts to acquire this `RwLock` with shared read access."] # [doc = ""] # [doc = " If the access could not be granted at this time, then `None` is returned."] # [doc = " Otherwise, an RAII guard is returned which will release the shared access"] # [doc = " when it is dropped."] # [doc = ""] # [doc = " This method is guaranteed to succeed if another read lock is held at the"] # [doc = " time of the call. See the documentation for `read_recursive` for details."] # [doc = ""] # [doc = " This function does not block."] # [inline] # [track_caller] pub fn try_read_recursive (& self) -> Option < RwLockReadGuard < '_ , R , T > > { if self . raw . try_lock_shared_recursive () { Some (unsafe { self . make_read_guard_unchecked () }) } else { None } } # [doc = " Locks this `RwLock` with shared read access, through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `read_recursive` method; however, it requires the `RwLock` to be inside of"] # [doc = " an `Arc` and the resulting read guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn read_arc_recursive (self : & Arc < Self >) -> ArcRwLockReadGuard < R , T > { self . raw . lock_shared_recursive () ; unsafe { self . make_arc_read_guard_unchecked () } } # [doc = " Attempts to lock this `RwLock` with shared read access, through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `try_read_recursive` method; however, it requires the `RwLock` to be inside"] # [doc = " of an `Arc` and the resulting read guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn try_read_recursive_arc (self : & Arc < Self >) -> Option < ArcRwLockReadGuard < R , T > > { if self . raw . try_lock_shared_recursive () { Some (unsafe { self . make_arc_read_guard_unchecked () }) } else { None } } }
    };
}

impl_115!()