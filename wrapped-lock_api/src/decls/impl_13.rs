macro_rules! deps {
    () => {
        ArcMutexGuard!();
        Mutex!();
        MutexGuard!();
        RawMutex!();
    };
}

macro_rules! impl_13 {
    () => {
        deps!();
        impl < R : RawMutex , T : ? Sized > Mutex < R , T > { # [doc = " Creates a new `MutexGuard` without checking if the mutex is locked."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the thread logically holds the lock."] # [doc = ""] # [doc = " Calling this function when a guard has already been produced is undefined behaviour unless"] # [doc = " the guard was forgotten with `mem::forget`."] # [inline] pub unsafe fn make_guard_unchecked (& self) -> MutexGuard < '_ , R , T > { MutexGuard { mutex : self , marker : PhantomData , } } # [doc = " Acquires a mutex, blocking the current thread until it is able to do so."] # [doc = ""] # [doc = " This function will block the local thread until it is available to acquire"] # [doc = " the mutex. Upon returning, the thread is the only thread with the mutex"] # [doc = " held. An RAII guard is returned to allow scoped unlock of the lock. When"] # [doc = " the guard goes out of scope, the mutex will be unlocked."] # [doc = ""] # [doc = " Attempts to lock a mutex in the thread which already holds the lock will"] # [doc = " result in a deadlock."] # [inline] # [track_caller] pub fn lock (& self) -> MutexGuard < '_ , R , T > { self . raw . lock () ; unsafe { self . make_guard_unchecked () } } # [doc = " Attempts to acquire this lock."] # [doc = ""] # [doc = " If the lock could not be acquired at this time, then `None` is returned."] # [doc = " Otherwise, an RAII guard is returned. The lock will be unlocked when the"] # [doc = " guard is dropped."] # [doc = ""] # [doc = " This function does not block."] # [inline] # [track_caller] pub fn try_lock (& self) -> Option < MutexGuard < '_ , R , T > > { if self . raw . try_lock () { Some (unsafe { self . make_guard_unchecked () }) } else { None } } # [doc = " Returns a mutable reference to the underlying data."] # [doc = ""] # [doc = " Since this call borrows the `Mutex` mutably, no actual locking needs to"] # [doc = " take place---the mutable borrow statically guarantees no locks exist."] # [inline] pub fn get_mut (& mut self) -> & mut T { unsafe { & mut * self . data . get () } } # [doc = " Checks whether the mutex is currently locked."] # [inline] # [track_caller] pub fn is_locked (& self) -> bool { self . raw . is_locked () } # [doc = " Forcibly unlocks the mutex."] # [doc = ""] # [doc = " This is useful when combined with `mem::forget` to hold a lock without"] # [doc = " the need to maintain a `MutexGuard` object alive, for example when"] # [doc = " dealing with FFI."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the current thread logically owns a"] # [doc = " `MutexGuard` but that guard has been discarded using `mem::forget`."] # [doc = " Behavior is undefined if a mutex is unlocked when not locked."] # [inline] # [track_caller] pub unsafe fn force_unlock (& self) { self . raw . unlock () ; } # [doc = " Returns the underlying raw mutex object."] # [doc = ""] # [doc = " Note that you will most likely need to import the `RawMutex` trait from"] # [doc = " `lock_api` to be able to call functions on the raw mutex."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method is unsafe because it allows unlocking a mutex while"] # [doc = " still holding a reference to a `MutexGuard`."] # [inline] pub unsafe fn raw (& self) -> & R { & self . raw } # [doc = " Returns a raw pointer to the underlying data."] # [doc = ""] # [doc = " This is useful when combined with `mem::forget` to hold a lock without"] # [doc = " the need to maintain a `MutexGuard` object alive, for example when"] # [doc = " dealing with FFI."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " You must ensure that there are no data races when dereferencing the"] # [doc = " returned pointer, for example if the current thread logically owns"] # [doc = " a `MutexGuard` but that guard has been discarded using `mem::forget`."] # [inline] pub fn data_ptr (& self) -> * mut T { self . data . get () } # [doc = " Creates a new `ArcMutexGuard` without checking if the mutex is locked."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the thread logically holds the lock."] # [doc = ""] # [doc = " Calling this function when a guard has already been produced is undefined behaviour unless"] # [doc = " the guard was forgotten with `mem::forget`."] # [cfg (feature = "arc_lock")] # [inline] unsafe fn make_arc_guard_unchecked (self : & Arc < Self >) -> ArcMutexGuard < R , T > { ArcMutexGuard { mutex : self . clone () , marker : PhantomData , } } # [doc = " Acquires a lock through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `lock` method; however, it requires the `Mutex` to be inside of an `Arc`"] # [doc = " and the resulting mutex guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn lock_arc (self : & Arc < Self >) -> ArcMutexGuard < R , T > { self . raw . lock () ; unsafe { self . make_arc_guard_unchecked () } } # [doc = " Attempts to acquire a lock through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `try_lock` method; however, it requires the `Mutex` to be inside of an"] # [doc = " `Arc` and the resulting mutex guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn try_lock_arc (self : & Arc < Self >) -> Option < ArcMutexGuard < R , T > > { if self . raw . try_lock () { Some (unsafe { self . make_arc_guard_unchecked () }) } else { None } } }
    };
}

impl_13!()