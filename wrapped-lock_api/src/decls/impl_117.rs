macro_rules! deps {
    () => {
        ArcRwLockUpgradableReadGuard!();
        RawRwLockUpgrade!();
        RwLock!();
        RwLockUpgradableReadGuard!();
    };
}

macro_rules! impl_117 {
    () => {
        deps!();
        impl < R : RawRwLockUpgrade , T : ? Sized > RwLock < R , T > { # [doc = " Creates a new `RwLockUpgradableReadGuard` without checking if the lock is held."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the thread logically holds an upgradable read lock."] # [doc = ""] # [doc = " This function does not increment the read count of the lock. Calling this function when a"] # [doc = " guard has already been produced is undefined behaviour unless the guard was forgotten"] # [doc = " with `mem::forget`."] # [inline] pub unsafe fn make_upgradable_guard_unchecked (& self) -> RwLockUpgradableReadGuard < '_ , R , T > { RwLockUpgradableReadGuard { rwlock : self , marker : PhantomData , } } # [doc = " Locks this `RwLock` with upgradable read access, blocking the current thread"] # [doc = " until it can be acquired."] # [doc = ""] # [doc = " The calling thread will be blocked until there are no more writers or other"] # [doc = " upgradable reads which hold the lock. There may be other readers currently"] # [doc = " inside the lock when this method returns."] # [doc = ""] # [doc = " Returns an RAII guard which will release this thread's shared access"] # [doc = " once it is dropped."] # [inline] # [track_caller] pub fn upgradable_read (& self) -> RwLockUpgradableReadGuard < '_ , R , T > { self . raw . lock_upgradable () ; unsafe { self . make_upgradable_guard_unchecked () } } # [doc = " Attempts to acquire this `RwLock` with upgradable read access."] # [doc = ""] # [doc = " If the access could not be granted at this time, then `None` is returned."] # [doc = " Otherwise, an RAII guard is returned which will release the shared access"] # [doc = " when it is dropped."] # [doc = ""] # [doc = " This function does not block."] # [inline] # [track_caller] pub fn try_upgradable_read (& self) -> Option < RwLockUpgradableReadGuard < '_ , R , T > > { if self . raw . try_lock_upgradable () { Some (unsafe { self . make_upgradable_guard_unchecked () }) } else { None } } # [doc = " Creates a new `ArcRwLockUpgradableReadGuard` without checking if the lock is held."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method must only be called if the thread logically holds an upgradable read lock."] # [doc = ""] # [doc = " This function does not increment the read count of the lock. Calling this function when a"] # [doc = " guard has already been produced is undefined behaviour unless the guard was forgotten"] # [doc = " with `mem::forget`.`"] # [cfg (feature = "arc_lock")] # [inline] pub unsafe fn make_upgradable_arc_guard_unchecked (self : & Arc < Self > ,) -> ArcRwLockUpgradableReadGuard < R , T > { ArcRwLockUpgradableReadGuard { rwlock : self . clone () , marker : PhantomData , } } # [doc = " Locks this `RwLock` with upgradable read access, through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `upgradable_read` method; however, it requires the `RwLock` to be"] # [doc = " inside of an `Arc` and the resulting read guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn upgradable_read_arc (self : & Arc < Self >) -> ArcRwLockUpgradableReadGuard < R , T > { self . raw . lock_upgradable () ; unsafe { self . make_upgradable_arc_guard_unchecked () } } # [doc = " Attempts to lock this `RwLock` with upgradable read access, through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `try_upgradable_read` method; however, it requires the `RwLock` to be"] # [doc = " inside of an `Arc` and the resulting read guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn try_upgradable_read_arc (self : & Arc < Self >) -> Option < ArcRwLockUpgradableReadGuard < R , T > > { if self . raw . try_lock_upgradable () { Some (unsafe { self . make_upgradable_arc_guard_unchecked () }) } else { None } } }
    };
}

impl_117!();