macro_rules! deps {
    () => {
        RwLockWriteGuard!();
        RwLockReadGuard!();
        RawRwLockTimed!();
        RwLock!();
        ArcRwLockWriteGuard!();
        ArcRwLockReadGuard!();
    };
}

macro_rules! impl_114 {
    () => {
        deps!();
        impl < R : RawRwLockTimed , T : ? Sized > RwLock < R , T > { # [doc = " Attempts to acquire this `RwLock` with shared read access until a timeout"] # [doc = " is reached."] # [doc = ""] # [doc = " If the access could not be granted before the timeout expires, then"] # [doc = " `None` is returned. Otherwise, an RAII guard is returned which will"] # [doc = " release the shared access when it is dropped."] # [inline] # [track_caller] pub fn try_read_for (& self , timeout : R :: Duration) -> Option < RwLockReadGuard < '_ , R , T > > { if self . raw . try_lock_shared_for (timeout) { Some (unsafe { self . make_read_guard_unchecked () }) } else { None } } # [doc = " Attempts to acquire this `RwLock` with shared read access until a timeout"] # [doc = " is reached."] # [doc = ""] # [doc = " If the access could not be granted before the timeout expires, then"] # [doc = " `None` is returned. Otherwise, an RAII guard is returned which will"] # [doc = " release the shared access when it is dropped."] # [inline] # [track_caller] pub fn try_read_until (& self , timeout : R :: Instant) -> Option < RwLockReadGuard < '_ , R , T > > { if self . raw . try_lock_shared_until (timeout) { Some (unsafe { self . make_read_guard_unchecked () }) } else { None } } # [doc = " Attempts to acquire this `RwLock` with exclusive write access until a"] # [doc = " timeout is reached."] # [doc = ""] # [doc = " If the access could not be granted before the timeout expires, then"] # [doc = " `None` is returned. Otherwise, an RAII guard is returned which will"] # [doc = " release the exclusive access when it is dropped."] # [inline] # [track_caller] pub fn try_write_for (& self , timeout : R :: Duration) -> Option < RwLockWriteGuard < '_ , R , T > > { if self . raw . try_lock_exclusive_for (timeout) { Some (unsafe { self . make_write_guard_unchecked () }) } else { None } } # [doc = " Attempts to acquire this `RwLock` with exclusive write access until a"] # [doc = " timeout is reached."] # [doc = ""] # [doc = " If the access could not be granted before the timeout expires, then"] # [doc = " `None` is returned. Otherwise, an RAII guard is returned which will"] # [doc = " release the exclusive access when it is dropped."] # [inline] # [track_caller] pub fn try_write_until (& self , timeout : R :: Instant) -> Option < RwLockWriteGuard < '_ , R , T > > { if self . raw . try_lock_exclusive_until (timeout) { Some (unsafe { self . make_write_guard_unchecked () }) } else { None } } # [doc = " Attempts to acquire this `RwLock` with read access until a timeout is reached, through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `try_read_for` method; however, it requires the `RwLock` to be inside of an"] # [doc = " `Arc` and the resulting read guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn try_read_arc_for (self : & Arc < Self > , timeout : R :: Duration ,) -> Option < ArcRwLockReadGuard < R , T > > { if self . raw . try_lock_shared_for (timeout) { Some (unsafe { self . make_arc_read_guard_unchecked () }) } else { None } } # [doc = " Attempts to acquire this `RwLock` with read access until a timeout is reached, through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `try_read_until` method; however, it requires the `RwLock` to be inside of"] # [doc = " an `Arc` and the resulting read guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn try_read_arc_until (self : & Arc < Self > , timeout : R :: Instant ,) -> Option < ArcRwLockReadGuard < R , T > > { if self . raw . try_lock_shared_until (timeout) { Some (unsafe { self . make_arc_read_guard_unchecked () }) } else { None } } # [doc = " Attempts to acquire this `RwLock` with write access until a timeout is reached, through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `try_write_for` method; however, it requires the `RwLock` to be inside of"] # [doc = " an `Arc` and the resulting write guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn try_write_arc_for (self : & Arc < Self > , timeout : R :: Duration ,) -> Option < ArcRwLockWriteGuard < R , T > > { if self . raw . try_lock_exclusive_for (timeout) { Some (unsafe { self . make_arc_write_guard_unchecked () }) } else { None } } # [doc = " Attempts to acquire this `RwLock` with read access until a timeout is reached, through an `Arc`."] # [doc = ""] # [doc = " This method is similar to the `try_write_until` method; however, it requires the `RwLock` to be inside of"] # [doc = " an `Arc` and the resulting read guard has no lifetime requirements."] # [cfg (feature = "arc_lock")] # [inline] # [track_caller] pub fn try_write_arc_until (self : & Arc < Self > , timeout : R :: Instant ,) -> Option < ArcRwLockWriteGuard < R , T > > { if self . raw . try_lock_exclusive_until (timeout) { Some (unsafe { self . make_arc_write_guard_unchecked () }) } else { None } } }
    };
}

impl_114!();