macro_rules! deps {
    () => {
        LocalSpawner!();
        LocalPool!();
    };
}

macro_rules! impl_9 {
    () => {
        deps!();
        impl LocalPool { # [doc = " Create a new, empty pool of tasks."] pub fn new () -> Self { Self { pool : FuturesUnordered :: new () , incoming : Default :: default () } } # [doc = " Get a clonable handle to the pool as a [`Spawn`]."] pub fn spawner (& self) -> LocalSpawner { LocalSpawner { incoming : Rc :: downgrade (& self . incoming) } } # [doc = " Run all tasks in the pool to completion."] # [doc = ""] # [doc = " ```"] # [doc = " use futures::executor::LocalPool;"] # [doc = ""] # [doc = " let mut pool = LocalPool::new();"] # [doc = ""] # [doc = " // ... spawn some initial tasks using `spawn.spawn()` or `spawn.spawn_local()`"] # [doc = ""] # [doc = " // run *all* tasks in the pool to completion, including any newly-spawned ones."] # [doc = " pool.run();"] # [doc = " ```"] # [doc = ""] # [doc = " The function will block the calling thread until *all* tasks in the pool"] # [doc = " are complete, including any spawned while running existing tasks."] pub fn run (& mut self) { run_executor (| cx | self . poll_pool (cx)) } # [doc = " Runs all the tasks in the pool until the given future completes."] # [doc = ""] # [doc = " ```"] # [doc = " use futures::executor::LocalPool;"] # [doc = ""] # [doc = " let mut pool = LocalPool::new();"] # [doc = " # let my_app  = async {};"] # [doc = ""] # [doc = " // run tasks in the pool until `my_app` completes"] # [doc = " pool.run_until(my_app);"] # [doc = " ```"] # [doc = ""] # [doc = " The function will block the calling thread *only* until the future `f`"] # [doc = " completes; there may still be incomplete tasks in the pool, which will"] # [doc = " be inert after the call completes, but can continue with further use of"] # [doc = " one of the pool's run or poll methods. While the function is running,"] # [doc = " however, all tasks in the pool will try to make progress."] pub fn run_until < F : Future > (& mut self , future : F) -> F :: Output { let mut future = pin ! (future) ; run_executor (| cx | { { let result = future . as_mut () . poll (cx) ; if let Poll :: Ready (output) = result { return Poll :: Ready (output) ; } } let _ = self . poll_pool (cx) ; Poll :: Pending }) } # [doc = " Runs all tasks and returns after completing one future or until no more progress"] # [doc = " can be made. Returns `true` if one future was completed, `false` otherwise."] # [doc = ""] # [doc = " ```"] # [doc = " use futures::executor::LocalPool;"] # [doc = " use futures::task::LocalSpawnExt;"] # [doc = " use futures::future::{ready, pending};"] # [doc = ""] # [doc = " let mut pool = LocalPool::new();"] # [doc = " let spawner = pool.spawner();"] # [doc = ""] # [doc = " spawner.spawn_local(ready(())).unwrap();"] # [doc = " spawner.spawn_local(ready(())).unwrap();"] # [doc = " spawner.spawn_local(pending()).unwrap();"] # [doc = ""] # [doc = " // Run the two ready tasks and return true for them."] # [doc = " pool.try_run_one(); // returns true after completing one of the ready futures"] # [doc = " pool.try_run_one(); // returns true after completing the other ready future"] # [doc = ""] # [doc = " // the remaining task can not be completed"] # [doc = " assert!(!pool.try_run_one()); // returns false"] # [doc = " ```"] # [doc = ""] # [doc = " This function will not block the calling thread and will return the moment"] # [doc = " that there are no tasks left for which progress can be made or after exactly one"] # [doc = " task was completed; Remaining incomplete tasks in the pool can continue with"] # [doc = " further use of one of the pool's run or poll methods."] # [doc = " Though only one task will be completed, progress may be made on multiple tasks."] pub fn try_run_one (& mut self) -> bool { run_executor (| cx | { loop { self . drain_incoming () ; match self . pool . poll_next_unpin (cx) { Poll :: Ready (Some (())) => return Poll :: Ready (true) , Poll :: Ready (None) => return Poll :: Ready (false) , Poll :: Pending => () , } if ! self . incoming . borrow () . is_empty () { continue ; } else if woken () { return Poll :: Pending ; } else { return Poll :: Ready (false) ; } } }) } # [doc = " Runs all tasks in the pool and returns if no more progress can be made"] # [doc = " on any task."] # [doc = ""] # [doc = " ```"] # [doc = " use futures::executor::LocalPool;"] # [doc = " use futures::task::LocalSpawnExt;"] # [doc = " use futures::future::{ready, pending};"] # [doc = ""] # [doc = " let mut pool = LocalPool::new();"] # [doc = " let spawner = pool.spawner();"] # [doc = ""] # [doc = " spawner.spawn_local(ready(())).unwrap();"] # [doc = " spawner.spawn_local(ready(())).unwrap();"] # [doc = " spawner.spawn_local(pending()).unwrap();"] # [doc = ""] # [doc = " // Runs the two ready task and returns."] # [doc = " // The empty task remains in the pool."] # [doc = " pool.run_until_stalled();"] # [doc = " ```"] # [doc = ""] # [doc = " This function will not block the calling thread and will return the moment"] # [doc = " that there are no tasks left for which progress can be made;"] # [doc = " remaining incomplete tasks in the pool can continue with further use of one"] # [doc = " of the pool's run or poll methods. While the function is running, all tasks"] # [doc = " in the pool will try to make progress."] pub fn run_until_stalled (& mut self) { run_executor (| cx | match self . poll_pool (cx) { Poll :: Ready (()) => Poll :: Ready (()) , Poll :: Pending => { if woken () { Poll :: Pending } else { Poll :: Ready (()) } } }) ; } # [doc = " Poll `self.pool`, re-filling it with any newly-spawned tasks."] # [doc = " Repeat until either the pool is empty, or it returns `Pending`."] # [doc = ""] # [doc = " Returns `Ready` if the pool was empty, and `Pending` otherwise."] # [doc = ""] # [doc = " NOTE: the pool may call `wake`, so `Pending` doesn't necessarily"] # [doc = " mean that the pool can't make progress."] fn poll_pool (& mut self , cx : & mut Context < '_ >) -> Poll < () > { loop { self . drain_incoming () ; let pool_ret = self . pool . poll_next_unpin (cx) ; if ! self . incoming . borrow () . is_empty () { continue ; } match pool_ret { Poll :: Ready (Some (())) => continue , Poll :: Ready (None) => return Poll :: Ready (()) , Poll :: Pending => return Poll :: Pending , } } } # [doc = " Empty the incoming queue of newly-spawned tasks."] fn drain_incoming (& mut self) { let mut incoming = self . incoming . borrow_mut () ; for task in incoming . drain (..) { self . pool . push (task) } } }
    };
}

impl_9!()