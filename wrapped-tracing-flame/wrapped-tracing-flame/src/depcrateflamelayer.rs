// Generated macro for FlameLayer (struct)
macro_rules! DepcrateFlameLayer {
() => {
// Module: crate
// Provides: {"FlameLayer"}
// Dependencies: {}
# [doc = " A `Layer` that records span open/close events as folded flamegraph stack"] # [doc = " samples."] # [doc = ""] # [doc = " The output of `FlameLayer` emulates the output of commands like `perf` once"] # [doc = " they've been collapsed by `inferno-flamegraph`. The output of this layer"] # [doc = " should look similar to the output of the following commands:"] # [doc = ""] # [doc = " ```sh"] # [doc = " perf record --call-graph dwarf target/release/mybin"] # [doc = " perf script | inferno-collapse-perf > stacks.folded"] # [doc = " ```"] # [doc = ""] # [doc = " # Sample Counts"] # [doc = ""] # [doc = " Because `tracing-flame` doesn't use sampling, the number at the end of each"] # [doc = " folded stack trace does not represent a number of samples of that stack."] # [doc = " Instead, the numbers on each line are the number of nanoseconds since the"] # [doc = " last event in the same thread."] # [doc = ""] # [doc = " # Dropping and Flushing"] # [doc = ""] # [doc = " If you use a global subscriber the drop implementations on your various"] # [doc = " layers will not get called when your program exits. This means that if"] # [doc = " you're using a buffered writer as the inner writer for the `FlameLayer`"] # [doc = " you're not guaranteed to see all the events that have been emitted in the"] # [doc = " file by default."] # [doc = ""] # [doc = " To ensure all data is flushed when the program exits, `FlameLayer` exposes"] # [doc = " the [`flush_on_drop`] function, which returns a [`FlushGuard`]. The `FlushGuard`"] # [doc = " will flush the writer when it is dropped. If necessary, it can also be used to manually"] # [doc = " flush the writer."] # [doc = ""] # [doc = " [`flush_on_drop`]: FlameLayer::flush_on_drop"] # [derive (Debug)] pub struct FlameLayer < S , W > { out : Arc < Mutex < W > > , config : Config , _inner : PhantomData < S > , }
};
}
