// Generated macro for TreeSink (trait)
macro_rules! Depcrate_tree_builder_interfaceTreeSink {
() => {
// Module: crate::tree_builder::interface
// Provides: {"TreeSink"}
// Dependencies: {}
# [doc = " Types which can process tree modifications from the tree builder."] pub trait TreeSink { # [doc = " The overall result of parsing."] # [doc = ""] # [doc = " This should default to Self, but default associated types are not stable yet."] # [doc = " (https://github.com/rust-lang/rust/issues/29661)"] type Output ; # [doc = " Consume this sink and return the overall result of parsing."] # [doc = ""] # [doc = " This should default to `fn finish(self) -> Self::Output { self }`,"] # [doc = " but default associated types are not stable yet."] # [doc = " (https://github.com/rust-lang/rust/issues/29661)"] fn finish (self) -> Self :: Output ; # [doc = " `Handle` is a reference to a DOM node.  The tree builder requires"] # [doc = " that a `Handle` implements `Clone` to get another reference to"] # [doc = " the same node."] type Handle : Clone ; # [doc = " Signal a parse error."] fn parse_error (& mut self , msg : Cow < 'static , str >) ; # [doc = " Get a handle to the `Document` node."] fn get_document (& mut self) -> Self :: Handle ; # [doc = " Get a handle to a template's template contents. The tree builder"] # [doc = " promises this will never be called with something else than"] # [doc = " a template element."] fn get_template_contents (& mut self , target : Self :: Handle) -> Self :: Handle ; # [doc = " Do two handles refer to the same node?"] fn same_node (& self , x : Self :: Handle , y : Self :: Handle) -> bool ; # [doc = " What is the name of this element?"] # [doc = ""] # [doc = " Should never be called on a non-element node;"] # [doc = " feel free to `panic!`."] fn elem_name (& self , target : Self :: Handle) -> QualName ; # [doc = " Set the document's quirks mode."] fn set_quirks_mode (& mut self , mode : QuirksMode) ; # [doc = " Create an element."] # [doc = ""] # [doc = " When creating a template element (`name == qualname!(html, \"template\")`),"] # [doc = " an associated document fragment called the \"template contents\" should"] # [doc = " also be created. Later calls to self.get_template_contents() with that"] # [doc = " given element return it."] # [doc = " https://html.spec.whatwg.org/multipage/#htmltemplateelement"] fn create_element (& mut self , name : QualName , attrs : Vec < Attribute >) -> Self :: Handle ; # [doc = " Create a comment node."] fn create_comment (& mut self , text : StrTendril) -> Self :: Handle ; # [doc = " Append a node as the last child of the given node.  If this would"] # [doc = " produce adjacent sibling text nodes, it should concatenate the text"] # [doc = " instead."] # [doc = ""] # [doc = " The child node will not already have a parent."] fn append (& mut self , parent : Self :: Handle , child : NodeOrText < Self :: Handle >) ; # [doc = " Append a node as the sibling immediately before the given node.  If that node"] # [doc = " has no parent, do nothing and return Err(new_node)."] # [doc = ""] # [doc = " The tree builder promises that `sibling` is not a text node.  However its"] # [doc = " old previous sibling, which would become the new node's previous sibling,"] # [doc = " could be a text node.  If the new node is also a text node, the two should"] # [doc = " be merged, as in the behavior of `append`."] # [doc = ""] # [doc = " NB: `new_node` may have an old parent, from which it should be removed."] fn append_before_sibling (& mut self , sibling : Self :: Handle , new_node : NodeOrText < Self :: Handle >) -> Result < () , NodeOrText < Self :: Handle > > ; # [doc = " Append a `DOCTYPE` element to the `Document` node."] fn append_doctype_to_document (& mut self , name : StrTendril , public_id : StrTendril , system_id : StrTendril) ; # [doc = " Add each attribute to the given element, if no attribute with that name"] # [doc = " already exists. The tree builder promises this will never be called"] # [doc = " with something else than an element."] fn add_attrs_if_missing (& mut self , target : Self :: Handle , attrs : Vec < Attribute >) ; # [doc = " Detach the given node from its parent."] fn remove_from_parent (& mut self , target : Self :: Handle) ; # [doc = " Remove all the children from node and append them to new_parent."] fn reparent_children (& mut self , node : Self :: Handle , new_parent : Self :: Handle) ; # [doc = " Mark a HTML `<script>` element as \"already started\"."] fn mark_script_already_started (& mut self , node : Self :: Handle) ; # [doc = " Indicate that a `<script>` element is complete."] fn complete_script (& mut self , _node : Self :: Handle) -> NextParserState { NextParserState :: Continue } fn is_mathml_annotation_xml_integration_point (& self , handle : Self :: Handle) -> bool { false } }
};
}
