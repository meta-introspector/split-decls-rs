// Generated macro for impl_224 (impl)
macro_rules! Depcrate_tree_builderimpl_224 {
() => {
// Module: crate::tree_builder
// Provides: {"impl_224"}
// Dependencies: {}
impl < Handle , Sink > TreeBuilder < Handle , Sink > where Handle : Clone , Sink : TreeSink < Handle = Handle > , { # [doc = " Create a new tree builder which sends tree modifications to a particular `TreeSink`."] # [doc = ""] # [doc = " The tree builder is also a `TokenSink`."] pub fn new (mut sink : Sink , opts : TreeBuilderOpts) -> TreeBuilder < Handle , Sink > { let doc_handle = sink . get_document () ; TreeBuilder { opts : opts , sink : sink , mode : Initial , orig_mode : None , template_modes : vec ! () , pending_table_text : vec ! () , quirks_mode : opts . quirks_mode , doc_handle : doc_handle , open_elems : vec ! () , active_formatting : vec ! () , head_elem : None , form_elem : None , next_tokenizer_state : None , frameset_ok : true , ignore_lf : false , foster_parenting : false , context_elem : None , } } # [doc = " Create a new tree builder which sends tree modifications to a particular `TreeSink`."] # [doc = " This is for parsing fragments."] # [doc = ""] # [doc = " The tree builder is also a `TokenSink`."] pub fn new_for_fragment (mut sink : Sink , context_elem : Handle , form_elem : Option < Handle > , opts : TreeBuilderOpts) -> TreeBuilder < Handle , Sink > { let doc_handle = sink . get_document () ; let context_is_template = sink . elem_name (context_elem . clone ()) == qualname ! (html , "template") ; let mut tb = TreeBuilder { opts : opts , sink : sink , mode : Initial , orig_mode : None , template_modes : if context_is_template { vec ! [InTemplate] } else { vec ! [] } , pending_table_text : vec ! () , quirks_mode : opts . quirks_mode , doc_handle : doc_handle , open_elems : vec ! () , active_formatting : vec ! () , head_elem : None , form_elem : form_elem , next_tokenizer_state : None , frameset_ok : true , ignore_lf : false , foster_parenting : false , context_elem : Some (context_elem) , } ; tb . create_root (vec ! ()) ; tb . mode = tb . reset_insertion_mode () ; tb } pub fn tokenizer_state_for_context_elem (& self) -> tok_state :: State { let elem = self . context_elem . clone () . expect ("no context element") ; let name = match self . sink . elem_name (elem) { QualName { ns : ns ! (html) , local } => local , _ => return tok_state :: Data } ; match name { atom ! ("title") | atom ! ("textarea") => tok_state :: RawData (tok_state :: Rcdata) , atom ! ("style") | atom ! ("xmp") | atom ! ("iframe") | atom ! ("noembed") | atom ! ("noframes") => tok_state :: RawData (tok_state :: Rawtext) , atom ! ("script") => tok_state :: RawData (tok_state :: ScriptData) , atom ! ("noscript") => if self . opts . scripting_enabled { tok_state :: RawData (tok_state :: Rawtext) } else { tok_state :: Data } , atom ! ("plaintext") => tok_state :: Plaintext , _ => tok_state :: Data } } pub fn unwrap (self) -> Sink { self . sink } pub fn sink < 'a > (& 'a self) -> & 'a Sink { & self . sink } pub fn sink_mut < 'a > (& 'a mut self) -> & 'a mut Sink { & mut self . sink } # [doc = " Call the `Tracer`'s `trace_handle` method on every `Handle` in the tree builder's"] # [doc = " internal state.  This is intended to support garbage-collected DOMs."] pub fn trace_handles (& self , tracer : & Tracer < Handle = Handle >) { tracer . trace_handle (& self . doc_handle) ; for e in & self . open_elems { tracer . trace_handle (e) ; } for e in & self . active_formatting { match e { & Element (ref h , _) => tracer . trace_handle (h) , _ => () , } } self . head_elem . as_ref () . map (| h | tracer . trace_handle (h)) ; self . form_elem . as_ref () . map (| h | tracer . trace_handle (h)) ; self . context_elem . as_ref () . map (| h | tracer . trace_handle (h)) ; } # [allow (dead_code)] fn dump_state (& self , label : String) { use string_cache :: QualName ; println ! ("dump_state on {}" , label) ; print ! ("    open_elems:") ; for node in self . open_elems . iter () { let QualName { ns , local } = self . sink . elem_name (node . clone ()) ; match ns { ns ! (html) => print ! (" {}" , & local [..]) , _ => panic ! () , } } println ! ("") ; print ! ("    active_formatting:") ; for entry in self . active_formatting . iter () { match entry { & Marker => print ! (" Marker") , & Element (ref h , _) => { let QualName { ns , local } = self . sink . elem_name (h . clone ()) ; match ns { ns ! (html) => print ! (" {}" , & local [..]) , _ => panic ! () , } } } } println ! ("") ; } fn debug_step (& self , mode : InsertionMode , token : & Token) { use util :: str :: to_escaped_string ; debug ! ("processing {} in insertion mode {:?}" , to_escaped_string (token) , mode) ; } fn process_to_completion (& mut self , mut token : Token) { let mut more_tokens = VecDeque :: new () ; loop { let should_have_acknowledged_self_closing_flag = matches ! (token , TagToken (Tag { self_closing : true , kind : StartTag , .. })) ; let result = if self . is_foreign (& token) { self . step_foreign (token) } else { let mode = self . mode ; self . step (mode , token) } ; match result { Done => { if should_have_acknowledged_self_closing_flag { self . sink . parse_error (Borrowed ("Unacknowledged self-closing tag")) ; } token = unwrap_or_return ! (more_tokens . pop_front () , ()) ; } DoneAckSelfClosing => { token = unwrap_or_return ! (more_tokens . pop_front () , ()) ; } Reprocess (m , t) => { self . mode = m ; token = t ; } ReprocessForeign (t) => { token = t ; } SplitWhitespace (mut buf) => { let p = buf . pop_front_char_run (is_ascii_whitespace) ; let (first , is_ws) = unwrap_or_return ! (p , ()) ; let status = if is_ws { Whitespace } else { NotWhitespace } ; token = CharacterTokens (status , first) ; if buf . len32 () > 0 { more_tokens . push_back (CharacterTokens (NotSplit , buf)) ; } } } } } # [doc = " Are we parsing a HTML fragment?"] pub fn is_fragment (& self) -> bool { self . context_elem . is_some () } }
};
}
