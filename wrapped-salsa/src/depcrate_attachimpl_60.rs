// Generated macro for impl_60 (impl)
macro_rules! Depcrate_attachimpl_60 {
() => {
// Module: crate::attach
// Provides: {"impl_60"}
// Dependencies: {}
impl Attached { const fn new () -> Self { Self { database : Cell :: new (None) , } } # [inline] fn attach < Db , R > (& self , db : & Db , op : impl FnOnce () -> R) -> R where Db : ? Sized + Database , { struct DbGuard < 's > { state : Option < & 's Attached > , } impl < 's > DbGuard < 's > { # [inline] fn new (attached : & 's Attached , db : & dyn Database) -> Self { match attached . database . get () { Some (current_db) => { let new_db = NonNull :: from (db) ; if ! std :: ptr :: addr_eq (current_db . as_ptr () , new_db . as_ptr ()) { panic ! ("Cannot change database mid-query. current: {current_db:?}, new: {new_db:?}") ; } Self { state : None } } None => { attached . database . set (Some (NonNull :: from (db))) ; Self { state : Some (attached) , } } } } } impl Drop for DbGuard < '_ > { # [inline] fn drop (& mut self) { if let Some (attached) = self . state { attached . database . set (None) ; } } } let _guard = DbGuard :: new (self , db . as_dyn_database ()) ; op () } # [inline] fn attach_allow_change < Db , R > (& self , db : & Db , op : impl FnOnce () -> R) -> R where Db : ? Sized + Database , { struct DbGuard < 's > { state : & 's Attached , prev : Option < NonNull < dyn Database > > , } impl < 's > DbGuard < 's > { # [inline] fn new (attached : & 's Attached , db : & dyn Database) -> Self { let prev = attached . database . replace (Some (NonNull :: from (db))) ; Self { state : attached , prev , } } } impl Drop for DbGuard < '_ > { # [inline] fn drop (& mut self) { self . state . database . set (self . prev) ; } } let _guard = DbGuard :: new (self , db . as_dyn_database ()) ; op () } # [doc = " Access the \"attached\" database. Returns `None` if no database is attached."] # [doc = " Databases are attached with `attach_database`."] # [inline] fn with < R > (& self , op : impl FnOnce (& dyn Database) -> R) -> Option < R > { let db = self . database . get () ? ; Some (op (unsafe { db . as_ref () })) } }
};
}
