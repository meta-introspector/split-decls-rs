macro_rules! Update {
    () => {
        # [doc = " # Safety"] # [doc = ""] # [doc = " Implementing this trait requires the implementor to verify:"] # [doc = ""] # [doc = " * `maybe_update` ensures the properties it is intended to ensure."] # [doc = " * If the value implements `Eq`, it is safe to compare an instance"] # [doc = "   of the value from an older revision with one from the newer"] # [doc = "   revision. If the value compares as equal, no update is needed to"] # [doc = "   bring it into the newer revision."] # [doc = ""] # [doc = " NB: The second point implies that `Update` cannot be implemented for any"] # [doc = " `&'db T` -- (i.e., any Rust reference tied to the database)."] # [doc = " Such a value could refer to memory that was freed in some"] # [doc = " earlier revision. Even if the memory is still valid, it could also"] # [doc = " have been part of a tracked struct whose values were mutated,"] # [doc = " thus invalidating the `'db` lifetime (from a stacked borrows perspective)."] # [doc = " Either way, the `Eq` implementation would be invalid."] pub unsafe trait Update { # [doc = " # Returns"] # [doc = ""] # [doc = " True if the value should be considered to have changed in the new revision."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " ## Requires"] # [doc = ""] # [doc = " Informally, requires that `old_value` points to a value in the"] # [doc = " database that is potentially from a previous revision and `new_value`"] # [doc = " points to a value produced in this revision."] # [doc = ""] # [doc = " More formally, requires that"] # [doc = ""] # [doc = " * all parameters meet the [validity and safety invariants][i] for their type"] # [doc = " * `old_value` further points to allocated memory that meets the [validity invariant][i] for `Self`"] # [doc = " * all data *owned* by `old_value` further meets its safety invariant"] # [doc = "     * not that borrowed data in `old_value` only meets its validity invariant"] # [doc = "       and hence cannot be dereferenced; essentially, a `&T` may point to memory"] # [doc = "       in the database which has been modified or even freed in the newer revision."] # [doc = ""] # [doc = " [i]: https://www.ralfj.de/blog/2018/08/22/two-kinds-of-invariants.html"] # [doc = ""] # [doc = " ## Ensures"] # [doc = ""] # [doc = " That `old_value` is updated with"] unsafe fn maybe_update (old_pointer : * mut Self , new_value : Self) -> bool ; }
    };
}

Update!()