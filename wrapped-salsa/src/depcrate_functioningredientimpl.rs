// Generated macro for IngredientImpl (struct)
macro_rules! Depcrate_functionIngredientImpl {
() => {
// Module: crate::function
// Provides: {"IngredientImpl"}
// Dependencies: {}
# [doc = " Function ingredients are the \"workhorse\" of salsa."] # [doc = ""] # [doc = " They are used for tracked functions, for the \"value\" fields of tracked structs, and for the fields of input structs."] # [doc = " The function ingredient is fairly complex and so its code is spread across multiple modules, typically one per method."] # [doc = " The main entry points are:"] # [doc = ""] # [doc = " * the `fetch` method, which is invoked when the function is called by the user's code;"] # [doc = "   it will return a memoized value if one exists, or execute the function otherwise."] # [doc = " * the `specify` method, which can only be used when the key is an entity created by the active query."] # [doc = "   It sets the value of the function imperatively, so that when later fetches occur, they'll return this value."] # [doc = " * the `store` method, which can only be invoked with an `&mut` reference, and is to set input fields."] pub struct IngredientImpl < C : Configuration > { # [doc = " The ingredient index we were assigned in the database."] # [doc = " Used to construct `DatabaseKeyIndex` values."] index : IngredientIndex , # [doc = " The index for the memo/sync tables"] # [doc = ""] # [doc = " This may be a [`crate::memo_ingredient_indices::MemoIngredientSingletonIndex`] or a"] # [doc = " [`crate::memo_ingredient_indices::MemoIngredientIndices`], depending on whether the"] # [doc = " tracked function's struct is a plain salsa struct or an enum `#[derive(Supertype)]`."] memo_ingredient_indices : < C :: SalsaStruct < 'static > as SalsaStructInDb > :: MemoIngredientMap , # [doc = " Used to find memos to throw out when we have too many memoized values."] lru : lru :: Lru , # [doc = " An downcaster to `C::DbView`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The supplied database must be be the same as the database used to construct the [`Views`]"] # [doc = " instances that this downcaster was derived from."] view_caster : OnceLock < DatabaseDownCaster < C :: DbView > > , sync_table : SyncTable , # [doc = " When `fetch` and friends executes, they return a reference to the"] # [doc = " value stored in the memo that is extended to live as long as the `&self`"] # [doc = " reference we start with. This means that whenever we remove something"] # [doc = " from `memo_map` with an `&self` reference, there *could* be references to its"] # [doc = " internals still in use. Therefore we push the memo into this queue and"] # [doc = " only *actually* free up memory when a new revision starts (which means"] # [doc = " we have an `&mut` reference to self)."] # [doc = ""] # [doc = " You might think that we could do this only if the memo was verified in the"] # [doc = " current revision: you would be right, but we are being defensive, because"] # [doc = " we don't know that we can trust the database to give us the same runtime"] # [doc = " everytime and so forth."] deleted_entries : DeletedEntries < C > , }
};
}
