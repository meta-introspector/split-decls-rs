// Generated macro for Value (struct)
macro_rules! Depcrate_tracked_structValue {
() => {
// Module: crate::tracked_struct
// Provides: {"Value"}
// Dependencies: {}
# [derive (Debug)] pub struct Value < C > where C : Configuration , { # [doc = " The minimum durability of all inputs consumed by the creator"] # [doc = " query prior to creating this tracked struct. If any of those"] # [doc = " inputs changes, then the creator query may create this struct"] # [doc = " with different values."] durability : Durability , # [doc = " The revision when this tracked struct was last updated."] # [doc = " This field also acts as a kind of \"lock\". Once it is equal"] # [doc = " to `Some(current_revision)`, the fields are locked and"] # [doc = " cannot change further. This makes it safe to give out `&`-references"] # [doc = " so long as they do not live longer than the current revision"] # [doc = " (which is assured by tying their lifetime to the lifetime of an `&`-ref"] # [doc = " to the database)."] # [doc = ""] # [doc = " The struct is updated from an older revision `R0` to the current revision `R1`"] # [doc = " when the struct is first accessed in `R1`, whether that be because the original"] # [doc = " query re-created the struct (i.e., by user calling `Struct::new`) or because"] # [doc = " the struct was read from. (Structs may not be recreated in the new revision if"] # [doc = " the inputs to the query have not changed.)"] # [doc = ""] # [doc = " When re-creating the struct, the field is temporarily set to `None`."] # [doc = " This is signal that there is an active `&mut` modifying the other fields:"] # [doc = " even reading from those fields in that situation would create UB."] # [doc = " This `None` value should never be observable by users unless they have"] # [doc = " leaked a reference across threads somehow."] updated_at : OptionalAtomicRevision , # [doc = " Fields of this tracked struct. They can change across revisions,"] # [doc = " but they do not change within a particular revision."] fields : C :: Fields < 'static > , # [doc = " The revision information for each field: when did this field last change."] # [doc = " When tracked structs are re-created, this revision may be updated to the"] # [doc = " current revision if the value is different."] revisions : C :: Revisions , # [doc = " Memo table storing the results of query functions etc."] memos : MemoTable , }
};
}
