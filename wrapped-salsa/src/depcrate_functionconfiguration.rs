// Generated macro for Configuration (trait)
macro_rules! Depcrate_functionConfiguration {
() => {
// Module: crate::function
// Provides: {"Configuration"}
// Dependencies: {}
pub trait Configuration : Any { const DEBUG_NAME : & 'static str ; const LOCATION : crate :: ingredient :: Location ; const PERSIST : bool ; # [doc = " The database that this function is associated with."] type DbView : ? Sized + crate :: Database ; # [doc = " The \"salsa struct type\" that this function is associated with."] # [doc = " This can be just `salsa::Id` for functions that intern their arguments"] # [doc = " and are not clearly associated with any one salsa struct."] type SalsaStruct < 'db > : SalsaStructInDb ; # [doc = " The input to the function"] type Input < 'db > : Send + Sync ; # [doc = " The value computed by the function."] type Output < 'db > : Send + Sync ; # [doc = " Determines whether this function can recover from being a participant in a cycle"] # [doc = " (and, if so, how)."] const CYCLE_STRATEGY : CycleRecoveryStrategy ; # [doc = " Invokes after a new result `new_value` has been computed for which an older memoized value"] # [doc = " existed `old_value`, or in fixpoint iteration. Returns true if the new value is equal to"] # [doc = " the older one."] # [doc = ""] # [doc = " This invokes user code in form of the `Eq` impl."] fn values_equal < 'db > (old_value : & Self :: Output < 'db > , new_value : & Self :: Output < 'db >) -> bool ; # [doc = " Convert from the id used internally to the value that execute is expecting."] # [doc = " This is a no-op if the input to the function is a salsa struct."] fn id_to_input (zalsa : & Zalsa , key : Id) -> Self :: Input < '_ > ; # [doc = " Returns the size of any heap allocations in the output value, in bytes."] fn heap_size (_value : & Self :: Output < '_ >) -> Option < usize > { None } # [doc = " Invoked when we need to compute the value for the given key, either because we've never"] # [doc = " computed it before or because the old one relied on inputs that have changed."] # [doc = ""] # [doc = " This invokes the function the user wrote."] fn execute < 'db > (db : & 'db Self :: DbView , input : Self :: Input < 'db >) -> Self :: Output < 'db > ; # [doc = " Get the cycle recovery initial value."] fn cycle_initial < 'db > (db : & 'db Self :: DbView , id : Id , input : Self :: Input < 'db > ,) -> Self :: Output < 'db > ; # [doc = " Decide what value to use for this cycle iteration. Takes ownership of the new value"] # [doc = " and returns an owned value to use."] # [doc = ""] # [doc = " The function is called for every iteration of the cycle head, regardless of whether the cycle"] # [doc = " has converged (the values are equal)."] # [doc = ""] # [doc = " # Id"] # [doc = ""] # [doc = " The id can be used to uniquely identify the query instance. This can be helpful"] # [doc = " if the cycle function has to re-identify a value it returned previously."] # [doc = ""] # [doc = " # Values"] # [doc = ""] # [doc = " The `last_provisional_value` is the value from the previous iteration of this cycle"] # [doc = " and `value` is the new value that was computed in the current iteration."] # [doc = ""] # [doc = " # Iteration count"] # [doc = ""] # [doc = " The `iteration` parameter isn't guaranteed to start from zero or to be contiguous:"] # [doc = ""] # [doc = " * **Initial value**: `iteration` may be non-zero on the first call for a given query if that"] # [doc = "   query becomes the outermost cycle head after a nested cycle complete a few iterations. In this case,"] # [doc = "   `iteration` continues from the nested cycle's iteration count rather than resetting to zero."] # [doc = " * **Non-contiguous values**: The iteration count can be non-contigious for cycle heads"] # [doc = "   that are only conditionally part of a cycle."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " The function should return the value to use for this iteration. This can be the `value`"] # [doc = " that was computed, or a different value (e.g., a fallback value). This cycle will continue"] # [doc = " iterating until the returned value equals the previous iteration's value."] fn recover_from_cycle < 'db > (db : & 'db Self :: DbView , cycle : & Cycle , last_provisional_value : & Self :: Output < 'db > , value : Self :: Output < 'db > , input : Self :: Input < 'db > ,) -> Self :: Output < 'db > ; # [doc = " Serialize the output type using `serde`."] # [doc = ""] # [doc = " Panics if the value is not persistable, i.e. `Configuration::PERSIST` is `false`."] fn serialize < S > (value : & Self :: Output < '_ > , serializer : S) -> Result < S :: Ok , S :: Error > where S : plumbing :: serde :: Serializer ; # [doc = " Deserialize the output type using `serde`."] # [doc = ""] # [doc = " Panics if the value is not persistable, i.e. `Configuration::PERSIST` is `false`."] fn deserialize < 'de , D > (deserializer : D) -> Result < Self :: Output < 'static > , D :: Error > where D : plumbing :: serde :: Deserializer < 'de > ; }
};
}
