// Generated macro for Database (trait)
macro_rules! Depcrate_databaseDatabase {
() => {
// Module: crate::database
// Provides: {"Database"}
// Dependencies: {}
# [doc = " The trait implemented by all Salsa databases."] # [doc = " You can create your own subtraits of this trait using the `#[salsa::db]`(`crate::db`) procedural macro."] pub trait Database : Send + ZalsaDatabase + AsDynDatabase { # [doc = " Enforces current LRU limits, evicting entries if necessary."] # [doc = ""] # [doc = " **WARNING:** Just like an ordinary write, this method triggers"] # [doc = " cancellation. If you invoke it while a snapshot exists, it"] # [doc = " will block until that snapshot is dropped -- if that snapshot"] # [doc = " is owned by the current thread, this could trigger deadlock."] fn trigger_lru_eviction (& mut self) { let zalsa_mut = self . zalsa_mut () ; zalsa_mut . evict_lru () ; } # [doc = " A \"synthetic write\" causes the system to act *as though* some"] # [doc = " input of durability `durability` has changed, triggering a new revision."] # [doc = " This is mostly useful for profiling scenarios."] # [doc = ""] # [doc = " **WARNING:** Just like an ordinary write, this method triggers"] # [doc = " cancellation. If you invoke it while a snapshot exists, it"] # [doc = " will block until that snapshot is dropped -- if that snapshot"] # [doc = " is owned by the current thread, this could trigger deadlock."] fn synthetic_write (& mut self , durability : Durability) { let zalsa_mut = self . zalsa_mut () ; zalsa_mut . new_revision () ; zalsa_mut . runtime_mut () . report_tracked_write (durability) ; } # [doc = " This method triggers cancellation."] # [doc = " If you invoke it while a snapshot exists, it"] # [doc = " will block until that snapshot is dropped -- if that snapshot"] # [doc = " is owned by the current thread, this could trigger deadlock."] fn trigger_cancellation (& mut self) { let _ = self . zalsa_mut () ; } # [doc = " Reports that the query depends on some state unknown to salsa."] # [doc = ""] # [doc = " Queries which report untracked reads will be re-executed in the next"] # [doc = " revision."] fn report_untracked_read (& self) { let (zalsa , zalsa_local) = self . zalsas () ; zalsa_local . report_untracked_read (zalsa . current_revision ()) } # [doc = " Return the \"debug name\" (i.e., the struct name, etc) for an \"ingredient\","] # [doc = " which are the fine-grained components we use to track data. This is intended"] # [doc = " for debugging and the contents of the returned string are not semver-guaranteed."] # [doc = ""] # [doc = " Ingredient indices can be extracted from [`DatabaseKeyIndex`](`crate::DatabaseKeyIndex`) values."] fn ingredient_debug_name (& self , ingredient_index : IngredientIndex) -> Cow < '_ , str > { Cow :: Borrowed (self . zalsa () . lookup_ingredient (ingredient_index) . debug_name () ,) } # [doc = " Starts unwinding the stack if the current revision is cancelled."] # [doc = ""] # [doc = " This method can be called by query implementations that perform"] # [doc = " potentially expensive computations, in order to speed up propagation of"] # [doc = " cancellation."] # [doc = ""] # [doc = " Cancellation will automatically be triggered by salsa on any query"] # [doc = " invocation."] # [doc = ""] # [doc = " This method should not be overridden by `Database` implementors. A"] # [doc = " `salsa_event` is emitted when this method is called, so that should be"] # [doc = " used instead."] fn unwind_if_revision_cancelled (& self) { let (zalsa , zalsa_local) = self . zalsas () ; zalsa . unwind_if_revision_cancelled (zalsa_local) ; } # [doc = " Execute `op` with the database in thread-local storage for debug print-outs."] # [inline (always)] fn attach < R > (& self , op : impl FnOnce (& Self) -> R) -> R where Self : Sized , { crate :: attach :: attach (self , | | op (self)) } # [cold] # [inline (never)] # [doc (hidden)] fn zalsa_register_downcaster (& self) -> & DatabaseDownCaster < dyn Database > { self . zalsa () . views () . downcaster_for :: < dyn Database > () } # [doc (hidden)] # [inline (always)] fn downcast (& self) -> & dyn Database where Self : Sized , { self } }
};
}
