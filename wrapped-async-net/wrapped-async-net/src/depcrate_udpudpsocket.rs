// Generated macro for UdpSocket (struct)
macro_rules! Depcrate_udpUdpSocket {
() => {
// Module: crate::udp
// Provides: {"UdpSocket"}
// Dependencies: {}
# [doc = " A UDP socket."] # [doc = ""] # [doc = " After creating a [`UdpSocket`] by [`bind`][`UdpSocket::bind()`]ing it to a socket address, data"] # [doc = " can be [sent to] and [received from] any other socket address."] # [doc = ""] # [doc = " Cloning a [`UdpSocket`] creates another handle to the same socket. The socket will be closed"] # [doc = " when all handles to it are dropped."] # [doc = ""] # [doc = " Although UDP is a connectionless protocol, this implementation provides an interface to set an"] # [doc = " address where data should be sent and received from. After setting a remote address with"] # [doc = " [`connect()`][`UdpSocket::connect()`], data can be sent to and received from that address with"] # [doc = " [`send()`][`UdpSocket::send()`] and [`recv()`][`UdpSocket::recv()`]."] # [doc = ""] # [doc = " As stated in the User Datagram Protocol's specification in [IETF RFC 768], UDP is an unordered,"] # [doc = " unreliable protocol. Refer to [`TcpListener`][`super::TcpListener`] and"] # [doc = " [`TcpStream`][`super::TcpStream`] for TCP primitives."] # [doc = ""] # [doc = " [received from]: UdpSocket::recv_from()"] # [doc = " [sent to]: UdpSocket::send_to()"] # [doc = " [IETF RFC 768]: https://tools.ietf.org/html/rfc768"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_net::UdpSocket;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let socket = UdpSocket::bind(\"127.0.0.1:8080\").await?;"] # [doc = " let mut buf = vec![0u8; 20];"] # [doc = ""] # [doc = " loop {"] # [doc = "     // Receive a single datagram message."] # [doc = "     // If `buf` is too small to hold the entire message, it will be cut off."] # [doc = "     let (n, addr) = socket.recv_from(&mut buf).await?;"] # [doc = ""] # [doc = "     // Send the message back to the same address that has sent it."] # [doc = "     socket.send_to(&buf[..n], &addr).await?;"] # [doc = " }"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] # [derive (Clone , Debug)] pub struct UdpSocket { inner : Arc < Async < std :: net :: UdpSocket > > , }
};
}
