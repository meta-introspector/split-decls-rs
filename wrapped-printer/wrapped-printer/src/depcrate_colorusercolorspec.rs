// Generated macro for UserColorSpec (struct)
macro_rules! Depcrate_colorUserColorSpec {
() => {
// Module: crate::color
// Provides: {"UserColorSpec"}
// Dependencies: {}
# [doc = " A single color specification provided by the user."] # [doc = ""] # [doc = " ## Format"] # [doc = ""] # [doc = " The format of a `Spec` is a triple: `{type}:{attribute}:{value}`. Each"] # [doc = " component is defined as follows:"] # [doc = ""] # [doc = " * `{type}` can be one of `path`, `line`, `column`, `match` or `highlight`."] # [doc = " * `{attribute}` can be one of `fg`, `bg` or `style`. `{attribute}` may also"] # [doc = "   be the special value `none`, in which case, `{value}` can be omitted."] # [doc = " * `{value}` is either a color name (for `fg`/`bg`) or a style instruction."] # [doc = ""] # [doc = " `{type}` controls which part of the output should be styled."] # [doc = ""] # [doc = " When `{attribute}` is `none`, then this should cause any existing style"] # [doc = " settings to be cleared for the specified `type`."] # [doc = ""] # [doc = " `{value}` should be a color when `{attribute}` is `fg` or `bg`, or it"] # [doc = " should be a style instruction when `{attribute}` is `style`. When"] # [doc = " `{attribute}` is `none`, `{value}` must be omitted."] # [doc = ""] # [doc = " Valid colors are `black`, `blue`, `green`, `red`, `cyan`, `magenta`,"] # [doc = " `yellow`, `white`. Extended colors can also be specified, and are formatted"] # [doc = " as `x` (for 256-bit colors) or `x,x,x` (for 24-bit true color), where"] # [doc = " `x` is a number between 0 and 255 inclusive. `x` may be given as a normal"] # [doc = " decimal number of a hexadecimal number, where the latter is prefixed by"] # [doc = " `0x`."] # [doc = ""] # [doc = " Valid style instructions are `nobold`, `bold`, `intense`, `nointense`,"] # [doc = " `underline`, `nounderline`, `italic`, `noitalic`."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " The standard way to build a `UserColorSpec` is to parse it from a string."] # [doc = " Once multiple `UserColorSpec`s have been constructed, they can be provided"] # [doc = " to the standard printer where they will automatically be applied to the"] # [doc = " output."] # [doc = ""] # [doc = " A `UserColorSpec` can also be converted to a `termcolor::ColorSpec`:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # fn main() {"] # [doc = " use termcolor::{Color, ColorSpec};"] # [doc = " use grep_printer::UserColorSpec;"] # [doc = ""] # [doc = " let user_spec1: UserColorSpec = \"path:fg:blue\".parse().unwrap();"] # [doc = " let user_spec2: UserColorSpec = \"match:bg:0xff,0x7f,0x00\".parse().unwrap();"] # [doc = ""] # [doc = " let spec1 = user_spec1.to_color_spec();"] # [doc = " let spec2 = user_spec2.to_color_spec();"] # [doc = ""] # [doc = " assert_eq!(spec1.fg(), Some(&Color::Blue));"] # [doc = " assert_eq!(spec2.bg(), Some(&Color::Rgb(0xFF, 0x7F, 0x00)));"] # [doc = " # }"] # [doc = " ```"] # [derive (Clone , Debug , Eq , PartialEq)] pub struct UserColorSpec { ty : OutType , value : SpecValue , }
};
}
