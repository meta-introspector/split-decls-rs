// Generated macro for impl_162 (impl)
macro_rules! Depcrate_summaryimpl_162 {
() => {
// Module: crate::summary
// Provides: {"impl_162"}
// Dependencies: {}
impl SummaryBuilder { # [doc = " Return a new builder for configuring the summary printer."] pub fn new () -> SummaryBuilder { SummaryBuilder { config : Config :: default () } } # [doc = " Build a printer using any implementation of `termcolor::WriteColor`."] # [doc = ""] # [doc = " The implementation of `WriteColor` used here controls whether colors"] # [doc = " are used or not when colors have been configured using the"] # [doc = " `color_specs` method."] # [doc = ""] # [doc = " For maximum portability, callers should generally use either"] # [doc = " `termcolor::StandardStream` or `termcolor::BufferedStandardStream`"] # [doc = " where appropriate, which will automatically enable colors on Windows"] # [doc = " when possible."] # [doc = ""] # [doc = " However, callers may also provide an arbitrary writer using the"] # [doc = " `termcolor::Ansi` or `termcolor::NoColor` wrappers, which always enable"] # [doc = " colors via ANSI escapes or always disable colors, respectively."] # [doc = ""] # [doc = " As a convenience, callers may use `build_no_color` to automatically"] # [doc = " select the `termcolor::NoColor` wrapper to avoid needing to import"] # [doc = " from `termcolor` explicitly."] pub fn build < W : WriteColor > (& self , wtr : W) -> Summary < W > { Summary { config : self . config . clone () , wtr : RefCell :: new (CounterWriter :: new (wtr)) , } } # [doc = " Build a printer from any implementation of `io::Write` and never emit"] # [doc = " any colors, regardless of the user color specification settings."] # [doc = ""] # [doc = " This is a convenience routine for"] # [doc = " `SummaryBuilder::build(termcolor::NoColor::new(wtr))`."] pub fn build_no_color < W : io :: Write > (& self , wtr : W) -> Summary < NoColor < W > > { self . build (NoColor :: new (wtr)) } # [doc = " Set the output mode for this printer."] # [doc = ""] # [doc = " The output mode controls how aggregate results of a search are printed."] # [doc = ""] # [doc = " By default, this printer uses the `Count` mode."] pub fn kind (& mut self , kind : SummaryKind) -> & mut SummaryBuilder { self . config . kind = kind ; self } # [doc = " Set the user color specifications to use for coloring in this printer."] # [doc = ""] # [doc = " A [`UserColorSpec`](crate::UserColorSpec) can be constructed from"] # [doc = " a string in accordance with the color specification format. See"] # [doc = " the `UserColorSpec` type documentation for more details on the"] # [doc = " format. A [`ColorSpecs`] can then be generated from zero or more"] # [doc = " `UserColorSpec`s."] # [doc = ""] # [doc = " Regardless of the color specifications provided here, whether color"] # [doc = " is actually used or not is determined by the implementation of"] # [doc = " `WriteColor` provided to `build`. For example, if `termcolor::NoColor`"] # [doc = " is provided to `build`, then no color will ever be printed regardless"] # [doc = " of the color specifications provided here."] # [doc = ""] # [doc = " This completely overrides any previous color specifications. This does"] # [doc = " not add to any previously provided color specifications on this"] # [doc = " builder."] # [doc = ""] # [doc = " The default color specifications provide no styling."] pub fn color_specs (& mut self , specs : ColorSpecs) -> & mut SummaryBuilder { self . config . colors = specs ; self } # [doc = " Set the configuration to use for hyperlinks output by this printer."] # [doc = ""] # [doc = " Regardless of the hyperlink format provided here, whether hyperlinks"] # [doc = " are actually used or not is determined by the implementation of"] # [doc = " `WriteColor` provided to `build`. For example, if `termcolor::NoColor`"] # [doc = " is provided to `build`, then no hyperlinks will ever be printed"] # [doc = " regardless of the format provided here."] # [doc = ""] # [doc = " This completely overrides any previous hyperlink format."] # [doc = ""] # [doc = " The default configuration results in not emitting any hyperlinks."] pub fn hyperlink (& mut self , config : HyperlinkConfig ,) -> & mut SummaryBuilder { self . config . hyperlink = config ; self } # [doc = " Enable the gathering of various aggregate statistics."] # [doc = ""] # [doc = " When this is enabled (it's disabled by default), statistics will be"] # [doc = " gathered for all uses of `Summary` printer returned by `build`,"] # [doc = " including but not limited to, the total number of matches, the total"] # [doc = " number of bytes searched and the total number of bytes printed."] # [doc = ""] # [doc = " Aggregate statistics can be accessed via the sink's"] # [doc = " [`SummarySink::stats`] method."] # [doc = ""] # [doc = " When this is enabled, this printer may need to do extra work in order"] # [doc = " to compute certain statistics, which could cause the search to take"] # [doc = " longer. For example, in `QuietWithMatch` mode, a search can quit after"] # [doc = " finding the first match, but if `stats` is enabled, then the search"] # [doc = " will continue after the first match in order to compute statistics."] # [doc = ""] # [doc = " For a complete description of available statistics, see [`Stats`]."] # [doc = ""] # [doc = " Note that some output modes, such as `CountMatches`, automatically"] # [doc = " enable this option even if it has been explicitly disabled."] pub fn stats (& mut self , yes : bool) -> & mut SummaryBuilder { self . config . stats = yes ; self } # [doc = " When enabled, if a path was given to the printer, then it is shown in"] # [doc = " the output (either as a heading or as a prefix to each matching line)."] # [doc = " When disabled, then no paths are ever included in the output even when"] # [doc = " a path is provided to the printer."] # [doc = ""] # [doc = " This setting has no effect in `PathWithMatch` and `PathWithoutMatch`"] # [doc = " modes."] # [doc = ""] # [doc = " This is enabled by default."] pub fn path (& mut self , yes : bool) -> & mut SummaryBuilder { self . config . path = yes ; self } # [doc = " Exclude count-related summary results with no matches."] # [doc = ""] # [doc = " When enabled and the mode is either `Count` or `CountMatches`, then"] # [doc = " results are not printed if no matches were found. Otherwise, every"] # [doc = " search prints a result with a possibly `0` number of matches."] # [doc = ""] # [doc = " This is enabled by default."] pub fn exclude_zero (& mut self , yes : bool) -> & mut SummaryBuilder { self . config . exclude_zero = yes ; self } # [doc = " Set the separator used between fields for the `Count` and"] # [doc = " `CountMatches` modes."] # [doc = ""] # [doc = " By default, this is set to `:`."] pub fn separator_field (& mut self , sep : Vec < u8 >) -> & mut SummaryBuilder { self . config . separator_field = Arc :: new (sep) ; self } # [doc = " Set the path separator used when printing file paths."] # [doc = ""] # [doc = " Typically, printing is done by emitting the file path as is. However,"] # [doc = " this setting provides the ability to use a different path separator"] # [doc = " from what the current environment has configured."] # [doc = ""] # [doc = " A typical use for this option is to permit cygwin users on Windows to"] # [doc = " set the path separator to `/` instead of using the system default of"] # [doc = " `\\`."] # [doc = ""] # [doc = " This is disabled by default."] pub fn separator_path (& mut self , sep : Option < u8 >) -> & mut SummaryBuilder { self . config . separator_path = sep ; self } # [doc = " Set the path terminator used."] # [doc = ""] # [doc = " The path terminator is a byte that is printed after every file path"] # [doc = " emitted by this printer."] # [doc = ""] # [doc = " If no path terminator is set (the default), then paths are terminated"] # [doc = " by either new lines or the configured field separator."] pub fn path_terminator (& mut self , terminator : Option < u8 > ,) -> & mut SummaryBuilder { self . config . path_terminator = terminator ; self } }
};
}
