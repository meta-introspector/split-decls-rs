// Generated macro for impl_135 (impl)
macro_rules! Depcrate_standardimpl_135 {
() => {
// Module: crate::standard
// Provides: {"impl_135"}
// Dependencies: {}
impl < 'p , 's , M : Matcher , W : WriteColor > StandardSink < 'p , 's , M , W > { # [doc = " Returns true if and only if this printer received a match in the"] # [doc = " previous search."] # [doc = ""] # [doc = " This is unaffected by the result of searches before the previous"] # [doc = " search on this sink."] pub fn has_match (& self) -> bool { self . match_count > 0 } # [doc = " Return the total number of matches reported to this sink."] # [doc = ""] # [doc = " This corresponds to the number of times `Sink::matched` is called"] # [doc = " on the previous search."] # [doc = ""] # [doc = " This is unaffected by the result of searches before the previous"] # [doc = " search on this sink."] pub fn match_count (& self) -> u64 { self . match_count } # [doc = " If binary data was found in the previous search, this returns the"] # [doc = " offset at which the binary data was first detected."] # [doc = ""] # [doc = " The offset returned is an absolute offset relative to the entire"] # [doc = " set of bytes searched."] # [doc = ""] # [doc = " This is unaffected by the result of searches before the previous"] # [doc = " search. e.g., If the search prior to the previous search found binary"] # [doc = " data but the previous search found no binary data, then this will"] # [doc = " return `None`."] pub fn binary_byte_offset (& self) -> Option < u64 > { self . binary_byte_offset } # [doc = " Return a reference to the stats produced by the printer for all"] # [doc = " searches executed on this sink."] # [doc = ""] # [doc = " This only returns stats if they were requested via the"] # [doc = " [`StandardBuilder`] configuration."] pub fn stats (& self) -> Option < & Stats > { self . stats . as_ref () } # [doc = " Execute the matcher over the given bytes and record the match"] # [doc = " locations if the current configuration demands match granularity."] fn record_matches (& mut self , searcher : & Searcher , bytes : & [u8] , range : std :: ops :: Range < usize > ,) -> io :: Result < () > { self . standard . matches . clear () ; if ! self . needs_match_granularity { return Ok (()) ; } let matches = & mut self . standard . matches ; find_iter_at_in_context (searcher , & self . matcher , bytes , range . clone () , | m | { let (s , e) = (m . start () - range . start , m . end () - range . start) ; matches . push (Match :: new (s , e)) ; true } ,) ? ; if ! matches . is_empty () && matches . last () . unwrap () . is_empty () && matches . last () . unwrap () . start () >= range . end { matches . pop () . unwrap () ; } Ok (()) } # [doc = " If the configuration specifies a replacement, then this executes the"] # [doc = " replacement, lazily allocating memory if necessary."] # [doc = ""] # [doc = " To access the result of a replacement, use `replacer.replacement()`."] fn replace (& mut self , searcher : & Searcher , bytes : & [u8] , range : std :: ops :: Range < usize > ,) -> io :: Result < () > { self . replacer . clear () ; if self . standard . config . replacement . is_some () { let replacement = (* self . standard . config . replacement) . as_ref () . unwrap () ; self . replacer . replace_all (searcher , & self . matcher , bytes , range , replacement ,) ? ; } Ok (()) } }
};
}
