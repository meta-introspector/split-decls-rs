// Generated macro for impl_68 (impl)
macro_rules! Depcrate_hyperlinkimpl_68 {
() => {
// Module: crate::hyperlink
// Provides: {"impl_68"}
// Dependencies: {}
impl Interpolator { # [doc = " Create a new interpolator for the given hyperlink format configuration."] pub (crate) fn new (config : & HyperlinkConfig) -> Interpolator { Interpolator { config : config . clone () , buf : RefCell :: new (vec ! []) } } # [doc = " Start interpolation with the given values by writing a hyperlink"] # [doc = " to `wtr`. Subsequent writes to `wtr`, until `Interpolator::end` is"] # [doc = " called, are the label for the hyperlink."] # [doc = ""] # [doc = " This returns an interpolator status which indicates whether the"] # [doc = " hyperlink was written. It might not be written, for example, if the"] # [doc = " underlying writer doesn't support hyperlinks or if the hyperlink"] # [doc = " format is empty. The status should be provided to `Interpolator::end`"] # [doc = " as an instruction for whether to close the hyperlink or not."] pub (crate) fn begin < W : WriteColor > (& self , values : & Values , mut wtr : W ,) -> io :: Result < InterpolatorStatus > { if self . config . format () . is_empty () || ! wtr . supports_hyperlinks () || ! wtr . supports_color () { return Ok (InterpolatorStatus :: inactive ()) ; } let mut buf = self . buf . borrow_mut () ; buf . clear () ; for part in self . config . format () . parts . iter () { part . interpolate_to (self . config . environment () , values , & mut buf) ; } let spec = HyperlinkSpec :: open (& buf) ; wtr . set_hyperlink (& spec) ? ; Ok (InterpolatorStatus { active : true }) } # [doc = " Writes the correct escape sequences to `wtr` to close any extant"] # [doc = " hyperlink, marking the end of a hyperlink's label."] # [doc = ""] # [doc = " The status given should be returned from a corresponding"] # [doc = " `Interpolator::begin` call. Since `begin` may not write a hyperlink"] # [doc = " (e.g., if the underlying writer doesn't support hyperlinks), it follows"] # [doc = " that `finish` must not close a hyperlink that was never opened. The"] # [doc = " status indicates whether the hyperlink was opened or not."] pub (crate) fn finish < W : WriteColor > (& self , status : InterpolatorStatus , mut wtr : W ,) -> io :: Result < () > { if ! status . active { return Ok (()) ; } wtr . set_hyperlink (& HyperlinkSpec :: close ()) } }
};
}
