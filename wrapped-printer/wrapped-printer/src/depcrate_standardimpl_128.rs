// Generated macro for impl_128 (impl)
macro_rules! Depcrate_standardimpl_128 {
() => {
// Module: crate::standard
// Provides: {"impl_128"}
// Dependencies: {}
impl StandardBuilder { # [doc = " Return a new builder for configuring the standard printer."] pub fn new () -> StandardBuilder { StandardBuilder { config : Config :: default () } } # [doc = " Build a printer using any implementation of `termcolor::WriteColor`."] # [doc = ""] # [doc = " The implementation of `WriteColor` used here controls whether colors"] # [doc = " are used or not when colors have been configured using the"] # [doc = " `color_specs` method."] # [doc = ""] # [doc = " For maximum portability, callers should generally use either"] # [doc = " `termcolor::StandardStream` or `termcolor::BufferedStandardStream`"] # [doc = " where appropriate, which will automatically enable colors on Windows"] # [doc = " when possible."] # [doc = ""] # [doc = " However, callers may also provide an arbitrary writer using the"] # [doc = " `termcolor::Ansi` or `termcolor::NoColor` wrappers, which always enable"] # [doc = " colors via ANSI escapes or always disable colors, respectively."] # [doc = ""] # [doc = " As a convenience, callers may use `build_no_color` to automatically"] # [doc = " select the `termcolor::NoColor` wrapper to avoid needing to import"] # [doc = " from `termcolor` explicitly."] pub fn build < W : WriteColor > (& self , wtr : W) -> Standard < W > { Standard { config : self . config . clone () , wtr : RefCell :: new (CounterWriter :: new (wtr)) , matches : vec ! [] , } } # [doc = " Build a printer from any implementation of `io::Write` and never emit"] # [doc = " any colors, regardless of the user color specification settings."] # [doc = ""] # [doc = " This is a convenience routine for"] # [doc = " `StandardBuilder::build(termcolor::NoColor::new(wtr))`."] pub fn build_no_color < W : io :: Write > (& self , wtr : W ,) -> Standard < NoColor < W > > { self . build (NoColor :: new (wtr)) } # [doc = " Set the user color specifications to use for coloring in this printer."] # [doc = ""] # [doc = " A [`UserColorSpec`](crate::UserColorSpec) can be constructed from"] # [doc = " a string in accordance with the color specification format. See"] # [doc = " the `UserColorSpec` type documentation for more details on the"] # [doc = " format. A [`ColorSpecs`] can then be generated from zero or more"] # [doc = " `UserColorSpec`s."] # [doc = ""] # [doc = " Regardless of the color specifications provided here, whether color"] # [doc = " is actually used or not is determined by the implementation of"] # [doc = " `WriteColor` provided to `build`. For example, if `termcolor::NoColor`"] # [doc = " is provided to `build`, then no color will ever be printed regardless"] # [doc = " of the color specifications provided here."] # [doc = ""] # [doc = " This completely overrides any previous color specifications. This does"] # [doc = " not add to any previously provided color specifications on this"] # [doc = " builder."] pub fn color_specs (& mut self , specs : ColorSpecs) -> & mut StandardBuilder { self . config . colors = specs ; self } # [doc = " Set the configuration to use for hyperlinks output by this printer."] # [doc = ""] # [doc = " Regardless of the hyperlink format provided here, whether hyperlinks"] # [doc = " are actually used or not is determined by the implementation of"] # [doc = " `WriteColor` provided to `build`. For example, if `termcolor::NoColor`"] # [doc = " is provided to `build`, then no hyperlinks will ever be printed"] # [doc = " regardless of the format provided here."] # [doc = ""] # [doc = " This completely overrides any previous hyperlink format."] # [doc = ""] # [doc = " The default configuration results in not emitting any hyperlinks."] pub fn hyperlink (& mut self , config : HyperlinkConfig ,) -> & mut StandardBuilder { self . config . hyperlink = config ; self } # [doc = " Enable the gathering of various aggregate statistics."] # [doc = ""] # [doc = " When this is enabled (it's disabled by default), statistics will be"] # [doc = " gathered for all uses of `Standard` printer returned by `build`,"] # [doc = " including but not limited to, the total number of matches, the total"] # [doc = " number of bytes searched and the total number of bytes printed."] # [doc = ""] # [doc = " Aggregate statistics can be accessed via the sink's"] # [doc = " [`StandardSink::stats`] method."] # [doc = ""] # [doc = " When this is enabled, this printer may need to do extra work in order"] # [doc = " to compute certain statistics, which could cause the search to take"] # [doc = " longer."] # [doc = ""] # [doc = " For a complete description of available statistics, see [`Stats`]."] pub fn stats (& mut self , yes : bool) -> & mut StandardBuilder { self . config . stats = yes ; self } # [doc = " Enable the use of \"headings\" in the printer."] # [doc = ""] # [doc = " When this is enabled, and if a file path has been given to the printer,"] # [doc = " then the file path will be printed once on its own line before showing"] # [doc = " any matches. If the heading is not the first thing emitted by the"] # [doc = " printer, then a line terminator is printed before the heading."] # [doc = ""] # [doc = " By default, this option is disabled. When disabled, the printer will"] # [doc = " not show any heading and will instead print the file path (if one is"] # [doc = " given) on the same line as each matching (or context) line."] pub fn heading (& mut self , yes : bool) -> & mut StandardBuilder { self . config . heading = yes ; self } # [doc = " When enabled, if a path was given to the printer, then it is shown in"] # [doc = " the output (either as a heading or as a prefix to each matching line)."] # [doc = " When disabled, then no paths are ever included in the output even when"] # [doc = " a path is provided to the printer."] # [doc = ""] # [doc = " This is enabled by default."] pub fn path (& mut self , yes : bool) -> & mut StandardBuilder { self . config . path = yes ; self } # [doc = " Only print the specific matches instead of the entire line containing"] # [doc = " each match. Each match is printed on its own line. When multi line"] # [doc = " search is enabled, then matches spanning multiple lines are printed"] # [doc = " such that only the matching portions of each line are shown."] pub fn only_matching (& mut self , yes : bool) -> & mut StandardBuilder { self . config . only_matching = yes ; self } # [doc = " Print at least one line for every match."] # [doc = ""] # [doc = " This is similar to the `only_matching` option, except the entire line"] # [doc = " is printed for each match. This is typically useful in conjunction with"] # [doc = " the `column` option, which will show the starting column number for"] # [doc = " every match on every line."] # [doc = ""] # [doc = " When multi-line mode is enabled, each match is printed, including every"] # [doc = " line in the match. As with single line matches, if a line contains"] # [doc = " multiple matches (even if only partially), then that line is printed"] # [doc = " once for each match it participates in, assuming it's the first line in"] # [doc = " that match. In multi-line mode, column numbers only indicate the start"] # [doc = " of a match. Subsequent lines in a multi-line match always have a column"] # [doc = " number of `1`."] # [doc = ""] # [doc = " When a match contains multiple lines, enabling `per_match_one_line`"] # [doc = " will cause only the first line each in match to be printed."] pub fn per_match (& mut self , yes : bool) -> & mut StandardBuilder { self . config . per_match = yes ; self } # [doc = " Print at most one line per match when `per_match` is enabled."] # [doc = ""] # [doc = " By default, every line in each match found is printed when `per_match`"] # [doc = " is enabled. However, this is sometimes undesirable, e.g., when you"] # [doc = " only ever want one line per match."] # [doc = ""] # [doc = " This is only applicable when multi-line matching is enabled, since"] # [doc = " otherwise, matches are guaranteed to span one line."] # [doc = ""] # [doc = " This is disabled by default."] pub fn per_match_one_line (& mut self , yes : bool) -> & mut StandardBuilder { self . config . per_match_one_line = yes ; self } # [doc = " Set the bytes that will be used to replace each occurrence of a match"] # [doc = " found."] # [doc = ""] # [doc = " The replacement bytes given may include references to capturing groups,"] # [doc = " which may either be in index form (e.g., `$2`) or can reference named"] # [doc = " capturing groups if present in the original pattern (e.g., `$foo`)."] # [doc = ""] # [doc = " For documentation on the full format, please see the `Capture` trait's"] # [doc = " `interpolate` method in the"] # [doc = " [grep-printer](https://docs.rs/grep-printer) crate."] pub fn replacement (& mut self , replacement : Option < Vec < u8 > > ,) -> & mut StandardBuilder { self . config . replacement = Arc :: new (replacement) ; self } # [doc = " Set the maximum number of columns allowed for each line printed. A"] # [doc = " single column is heuristically defined as a single byte."] # [doc = ""] # [doc = " If a line is found which exceeds this maximum, then it is replaced"] # [doc = " with a message indicating that the line has been omitted."] # [doc = ""] # [doc = " The default is to not specify a limit, in which each matching or"] # [doc = " contextual line is printed regardless of how long it is."] pub fn max_columns (& mut self , limit : Option < u64 >) -> & mut StandardBuilder { self . config . max_columns = limit ; self } # [doc = " When enabled, if a line is found to be over the configured maximum"] # [doc = " column limit (measured in terms of bytes), then a preview of the long"] # [doc = " line will be printed instead."] # [doc = ""] # [doc = " The preview will correspond to the first `N` *grapheme clusters* of"] # [doc = " the line, where `N` is the limit configured by `max_columns`."] # [doc = ""] # [doc = " If no limit is set, then enabling this has no effect."] # [doc = ""] # [doc = " This is disabled by default."] pub fn max_columns_preview (& mut self , yes : bool) -> & mut StandardBuilder { self . config . max_columns_preview = yes ; self } # [doc = " Print the column number of the first match in a line."] # [doc = ""] # [doc = " This option is convenient for use with `per_match` which will print a"] # [doc = " line for every match along with the starting offset for that match."] # [doc = ""] # [doc = " Column numbers are computed in terms of bytes from the start of the"] # [doc = " line being printed."] # [doc = ""] # [doc = " This is disabled by default."] pub fn column (& mut self , yes : bool) -> & mut StandardBuilder { self . config . column = yes ; self } # [doc = " Print the absolute byte offset of the beginning of each line printed."] # [doc = ""] # [doc = " The absolute byte offset starts from the beginning of each search and"] # [doc = " is zero based."] # [doc = ""] # [doc = " If the `only_matching` option is set, then this will print the absolute"] # [doc = " byte offset of the beginning of each match."] pub fn byte_offset (& mut self , yes : bool) -> & mut StandardBuilder { self . config . byte_offset = yes ; self } # [doc = " When enabled, all lines will have prefix ASCII whitespace trimmed"] # [doc = " before being written."] # [doc = ""] # [doc = " This is disabled by default."] pub fn trim_ascii (& mut self , yes : bool) -> & mut StandardBuilder { self . config . trim_ascii = yes ; self } # [doc = " Set the separator used between sets of search results."] # [doc = ""] # [doc = " When this is set, then it will be printed on its own line immediately"] # [doc = " before the results for a single search if and only if a previous search"] # [doc = " had already printed results. In effect, this permits showing a divider"] # [doc = " between sets of search results that does not appear at the beginning"] # [doc = " or end of all search results."] # [doc = ""] # [doc = " To reproduce the classic grep format, this is typically set to `--`"] # [doc = " (the same as the context separator) if and only if contextual lines"] # [doc = " have been requested, but disabled otherwise."] # [doc = ""] # [doc = " By default, this is disabled."] pub fn separator_search (& mut self , sep : Option < Vec < u8 > > ,) -> & mut StandardBuilder { self . config . separator_search = Arc :: new (sep) ; self } # [doc = " Set the separator used between discontiguous runs of search context,"] # [doc = " but only when the searcher is configured to report contextual lines."] # [doc = ""] # [doc = " The separator is always printed on its own line, even if it's empty."] # [doc = ""] # [doc = " If no separator is set, then nothing is printed when a context break"] # [doc = " occurs."] # [doc = ""] # [doc = " By default, this is set to `--`."] pub fn separator_context (& mut self , sep : Option < Vec < u8 > > ,) -> & mut StandardBuilder { self . config . separator_context = Arc :: new (sep) ; self } # [doc = " Set the separator used between fields emitted for matching lines."] # [doc = ""] # [doc = " For example, when the searcher has line numbers enabled, this printer"] # [doc = " will print the line number before each matching line. The bytes given"] # [doc = " here will be written after the line number but before the matching"] # [doc = " line."] # [doc = ""] # [doc = " By default, this is set to `:`."] pub fn separator_field_match (& mut self , sep : Vec < u8 > ,) -> & mut StandardBuilder { self . config . separator_field_match = Arc :: new (sep) ; self } # [doc = " Set the separator used between fields emitted for context lines."] # [doc = ""] # [doc = " For example, when the searcher has line numbers enabled, this printer"] # [doc = " will print the line number before each context line. The bytes given"] # [doc = " here will be written after the line number but before the context"] # [doc = " line."] # [doc = ""] # [doc = " By default, this is set to `-`."] pub fn separator_field_context (& mut self , sep : Vec < u8 > ,) -> & mut StandardBuilder { self . config . separator_field_context = Arc :: new (sep) ; self } # [doc = " Set the path separator used when printing file paths."] # [doc = ""] # [doc = " When a printer is configured with a file path, and when a match is"] # [doc = " found, that file path will be printed (either as a heading or as a"] # [doc = " prefix to each matching or contextual line, depending on other"] # [doc = " configuration settings). Typically, printing is done by emitting the"] # [doc = " file path as is. However, this setting provides the ability to use a"] # [doc = " different path separator from what the current environment has"] # [doc = " configured."] # [doc = ""] # [doc = " A typical use for this option is to permit cygwin users on Windows to"] # [doc = " set the path separator to `/` instead of using the system default of"] # [doc = " `\\`."] pub fn separator_path (& mut self , sep : Option < u8 >) -> & mut StandardBuilder { self . config . separator_path = sep ; self } # [doc = " Set the path terminator used."] # [doc = ""] # [doc = " The path terminator is a byte that is printed after every file path"] # [doc = " emitted by this printer."] # [doc = ""] # [doc = " If no path terminator is set (the default), then paths are terminated"] # [doc = " by either new lines (for when `heading` is enabled) or the match or"] # [doc = " context field separators (e.g., `:` or `-`)."] pub fn path_terminator (& mut self , terminator : Option < u8 > ,) -> & mut StandardBuilder { self . config . path_terminator = terminator ; self } }
};
}
