// Generated macro for impl_380 (impl)
macro_rules! Depcrate_os_macos_encrypt_transformimpl_380 {
() => {
// Module: crate::os::macos::encrypt_transform
// Provides: {"impl_380"}
// Dependencies: {}
impl Builder { # [doc = " Creates a new `Builder` with a default configuration."] # [inline (always)] # [must_use] pub fn new () -> Self { Self :: default () } # [doc = " Selects the padding scheme to use."] # [doc = ""] # [doc = " If not set, an appropriate scheme will be selected for you."] # [inline (always)] pub fn padding (& mut self , padding : Padding) -> & mut Self { self . padding = Some (padding) ; self } # [doc = " Selects the encryption mode to use."] # [doc = ""] # [doc = " If not set, an appropriate mode will be selected for you."] # [inline (always)] pub fn mode (& mut self , mode : Mode) -> & mut Self { self . mode = Some (mode) ; self } # [doc = " Sets the initialization vector to use."] # [doc = ""] # [doc = " If not set, an appropriate value will be supplied for you."] # [inline (always)] pub fn iv (& mut self , iv : CFData) -> & mut Self { self . iv = Some (iv) ; self } # [doc = " Encrypts data with a provided key."] pub fn encrypt (& self , key : & SecKey , data : & CFData) -> Result < CFData , CFError > { unsafe { let mut error = ptr :: null_mut () ; let transform = SecEncryptTransformCreate (key . as_concrete_TypeRef () , & mut error) ; if transform . is_null () { return Err (CFError :: wrap_under_create_rule (error)) ; } let transform = SecTransform :: wrap_under_create_rule (transform) ; self . finish (transform , data) } } # [doc = " Decrypts data with a provided key."] pub fn decrypt (& self , key : & SecKey , data : & CFData) -> Result < CFData , CFError > { unsafe { let mut error = ptr :: null_mut () ; let transform = SecDecryptTransformCreate (key . as_concrete_TypeRef () , & mut error) ; if transform . is_null () { return Err (CFError :: wrap_under_create_rule (error)) ; } let transform = SecTransform :: wrap_under_create_rule (transform) ; self . finish (transform , data) } } fn finish (& self , mut transform : SecTransform , data : & CFData) -> Result < CFData , CFError > { unsafe { if let Some (padding) = & self . padding { let key = CFString :: wrap_under_get_rule (kSecPaddingKey) ; transform . set_attribute (& key , & padding . to_str ()) ? ; } if let Some (mode) = & self . mode { let key = CFString :: wrap_under_get_rule (kSecEncryptionMode) ; transform . set_attribute (& key , & mode . to_str ()) ? ; } if let Some (iv) = & self . iv { let key = CFString :: wrap_under_get_rule (kSecIVKey) ; transform . set_attribute (& key , iv) ? ; } let key = CFString :: wrap_under_get_rule (kSecTransformInputAttributeName) ; transform . set_attribute (& key , data) ? ; let result = transform . execute () ? ; Ok (CFData :: wrap_under_get_rule (result . as_CFTypeRef () as CFDataRef)) } } }
};
}
