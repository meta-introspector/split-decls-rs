// Generated macro for impl_651 (impl)
macro_rules! Depcrate_provider_pattern_reference_parserimpl_651 {
() => {
// Module: crate::provider::pattern::reference::parser
// Provides: {"impl_651"}
// Dependencies: {}
impl < 'p > Parser < 'p > { pub fn new (source : & 'p str) -> Self { Self { source , state : Segment :: Literal (SegmentLiteral { literal : String :: new () , quoted : false , }) , } } fn handle_quoted_literal (& mut self , ch : char , chars : & mut core :: iter :: Peekable < core :: str :: Chars > , result : & mut Vec < PatternItem > ,) -> Result < bool , PatternError > { if ch == '\'' { match (& mut self . state , chars . peek () == Some (& '\'')) { (Segment :: Literal (ref mut literal) , true) => { literal . literal . push ('\'') ; chars . next () ; } (Segment :: Literal (ref mut literal) , false) => { literal . quoted = ! literal . quoted ; } (state , true) => { mem :: replace (state , Segment :: Literal (SegmentLiteral { literal : String :: from (ch) , quoted : false , }) ,) . finish (result) ? ; chars . next () ; } (state , false) => { mem :: replace (state , Segment :: Literal (SegmentLiteral { literal : String :: new () , quoted : true , }) ,) . finish (result) ? ; } } Ok (true) } else if let Segment :: Literal (SegmentLiteral { ref mut literal , quoted : true , }) = self . state { literal . push (ch) ; Ok (true) } else { Ok (false) } } fn handle_generic_quoted_literal (& mut self , ch : char , chars : & mut core :: iter :: Peekable < core :: str :: Chars > ,) -> Result < bool , PatternError > { if ch == '\'' { match (& mut self . state , chars . peek () == Some (& '\'')) { (Segment :: Literal (literal) , true) => { literal . literal . push ('\'') ; chars . next () ; } (Segment :: Literal (literal) , false) => { literal . quoted = ! literal . quoted ; } _ => unreachable ! ("Generic pattern has no symbols.") , } Ok (true) } else if let Segment :: Literal (SegmentLiteral { ref mut literal , quoted : true , }) = self . state { literal . push (ch) ; Ok (true) } else { Ok (false) } } pub fn parse (mut self) -> Result < Vec < PatternItem > , PatternError > { let mut chars = self . source . chars () . peekable () ; let mut result = vec ! [] ; while let Some (ch) = chars . next () { if ! self . handle_quoted_literal (ch , & mut chars , & mut result) ? { if let Ok (new_symbol) = FieldSymbol :: try_from (ch) { match & mut self . state { Segment :: Symbol (SegmentSymbol { ref symbol , ref mut length , }) if new_symbol == * symbol => { * length += 1 ; } Segment :: SecondSymbol (SegmentSecondSymbol { ref mut integer_digits , seen_decimal_separator : false , .. }) if matches ! (new_symbol , FieldSymbol :: Second (fields :: Second :: Second)) => { * integer_digits += 1 ; } state => { mem :: replace (state , if matches ! (new_symbol , FieldSymbol :: Second (fields :: Second :: Second)) { Segment :: SecondSymbol (SegmentSecondSymbol { integer_digits : 1 , seen_decimal_separator : false , fraction_digits : 0 , }) } else { Segment :: Symbol (SegmentSymbol { symbol : new_symbol , length : 1 , }) } ,) . finish (& mut result) ? ; } } } else if let Some (alias) = SymbolAlias :: try_new (ch) { match & mut self . state { Segment :: SymbolAlias (SymbolAlias { ch : ch2 , length }) if * ch2 == ch => { * length += 1 ; } state => { mem :: replace (state , Segment :: SymbolAlias (alias)) . finish (& mut result) ? ; } } } else { match & mut self . state { Segment :: SecondSymbol (second_symbol @ SegmentSecondSymbol { seen_decimal_separator : false , .. } ,) if ch == '.' => second_symbol . seen_decimal_separator = true , Segment :: SecondSymbol (second_symbol) if ch == 'S' => { second_symbol . seen_decimal_separator = true ; second_symbol . fraction_digits += 1 ; } Segment :: Literal (literal) => literal . literal . push (ch) , state => { mem :: replace (state , Segment :: Literal (SegmentLiteral { literal : String :: from (ch) , quoted : false , }) ,) . finish (& mut result) ? ; } } } } } if matches ! (self . state , Segment :: Literal (SegmentLiteral { quoted : true , .. })) { return Err (PatternError :: UnclosedLiteral) ; } self . state . finish (& mut result) ? ; Ok (result) } pub fn parse_generic (mut self) -> Result < Vec < GenericPatternItem > , PatternError > { let mut chars = self . source . chars () . peekable () ; let mut result = vec ! [] ; while let Some (ch) = chars . next () { if ! self . handle_generic_quoted_literal (ch , & mut chars) ? { if ch == '{' { mem :: replace (& mut self . state , Segment :: Literal (SegmentLiteral { literal : String :: new () , quoted : false , }) ,) . finish_generic (& mut result) ? ; let ch = chars . next () . ok_or (PatternError :: UnclosedPlaceholder) ? ; let idx = ch . to_digit (10) . ok_or (PatternError :: UnknownSubstitution (ch)) ? as u8 ; result . push (GenericPatternItem :: Placeholder (idx)) ; let ch = chars . next () . ok_or (PatternError :: UnclosedPlaceholder) ? ; if ch != '}' { return Err (PatternError :: UnclosedPlaceholder) ; } } else if let Segment :: Literal (SegmentLiteral { ref mut literal , .. }) = self . state { literal . push (ch) ; } else { unreachable ! () } } } if matches ! (self . state , Segment :: Literal (SegmentLiteral { quoted : true , .. })) { return Err (PatternError :: UnclosedLiteral) ; } self . state . finish_generic (& mut result) ? ; Ok (result) } # [cfg (test)] pub fn parse_placeholders (self , replacements : Vec < Pattern > ,) -> Result < Vec < PatternItem > , PatternError > { let generic_items = self . parse_generic () ? ; let gp = GenericPattern :: from (generic_items) ; Ok (gp . combined (replacements) ? . items . to_vec ()) } }
};
}
