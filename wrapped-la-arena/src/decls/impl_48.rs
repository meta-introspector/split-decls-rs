macro_rules! deps {
    () => {
        Idx!();
        IdxRange!();
        Arena!();
        RawIdx!();
    };
}

macro_rules! impl_48 {
    () => {
        deps!();
        impl < T > Arena < T > { # [doc = " Creates a new empty arena."] # [doc = ""] # [doc = " ```"] # [doc = " let arena: la_arena::Arena<i32> = la_arena::Arena::new();"] # [doc = " assert!(arena.is_empty());"] # [doc = " ```"] pub const fn new () -> Arena < T > { Arena { data : Vec :: new () } } # [doc = " Create a new empty arena with specific capacity."] # [doc = ""] # [doc = " ```"] # [doc = " let arena: la_arena::Arena<i32> = la_arena::Arena::with_capacity(42);"] # [doc = " assert!(arena.is_empty());"] # [doc = " ```"] pub fn with_capacity (capacity : usize) -> Arena < T > { Arena { data : Vec :: with_capacity (capacity) } } # [doc = " Empties the arena, removing all contained values."] # [doc = ""] # [doc = " ```"] # [doc = " let mut arena = la_arena::Arena::new();"] # [doc = ""] # [doc = " arena.alloc(1);"] # [doc = " arena.alloc(2);"] # [doc = " arena.alloc(3);"] # [doc = " assert_eq!(arena.len(), 3);"] # [doc = ""] # [doc = " arena.clear();"] # [doc = " assert!(arena.is_empty());"] # [doc = " ```"] pub fn clear (& mut self) { self . data . clear () ; } # [doc = " Returns the length of the arena."] # [doc = ""] # [doc = " ```"] # [doc = " let mut arena = la_arena::Arena::new();"] # [doc = " assert_eq!(arena.len(), 0);"] # [doc = ""] # [doc = " arena.alloc(\"foo\");"] # [doc = " assert_eq!(arena.len(), 1);"] # [doc = ""] # [doc = " arena.alloc(\"bar\");"] # [doc = " assert_eq!(arena.len(), 2);"] # [doc = ""] # [doc = " arena.alloc(\"baz\");"] # [doc = " assert_eq!(arena.len(), 3);"] # [doc = " ```"] pub fn len (& self) -> usize { self . data . len () } # [doc = " Returns whether the arena contains no elements."] # [doc = ""] # [doc = " ```"] # [doc = " let mut arena = la_arena::Arena::new();"] # [doc = " assert!(arena.is_empty());"] # [doc = ""] # [doc = " arena.alloc(0.5);"] # [doc = " assert!(!arena.is_empty());"] # [doc = " ```"] pub fn is_empty (& self) -> bool { self . data . is_empty () } # [doc = " Allocates a new value on the arena, returning the value’s index."] # [doc = ""] # [doc = " ```"] # [doc = " let mut arena = la_arena::Arena::new();"] # [doc = " let idx = arena.alloc(50);"] # [doc = ""] # [doc = " assert_eq!(arena[idx], 50);"] # [doc = " ```"] pub fn alloc (& mut self , value : T) -> Idx < T > { let idx = self . next_idx () ; self . data . push (value) ; idx } # [doc = " Densely allocates multiple values, returning the values’ index range."] # [doc = ""] # [doc = " ```"] # [doc = " let mut arena = la_arena::Arena::new();"] # [doc = " let range = arena.alloc_many(0..4);"] # [doc = ""] # [doc = " assert_eq!(arena[range], [0, 1, 2, 3]);"] # [doc = " ```"] pub fn alloc_many < II : IntoIterator < Item = T > > (& mut self , iter : II) -> IdxRange < T > { let start = self . next_idx () ; self . extend (iter) ; let end = self . next_idx () ; IdxRange :: new (start .. end) } # [doc = " Returns an iterator over the arena’s elements."] # [doc = ""] # [doc = " ```"] # [doc = " let mut arena = la_arena::Arena::new();"] # [doc = " let idx1 = arena.alloc(20);"] # [doc = " let idx2 = arena.alloc(40);"] # [doc = " let idx3 = arena.alloc(60);"] # [doc = ""] # [doc = " let mut iterator = arena.iter();"] # [doc = " assert_eq!(iterator.next(), Some((idx1, &20)));"] # [doc = " assert_eq!(iterator.next(), Some((idx2, &40)));"] # [doc = " assert_eq!(iterator.next(), Some((idx3, &60)));"] # [doc = " ```"] pub fn iter (& self ,) -> impl ExactSizeIterator < Item = (Idx < T > , & T) > + DoubleEndedIterator + Clone { self . data . iter () . enumerate () . map (| (idx , value) | (Idx :: from_raw (RawIdx (idx as u32)) , value)) } # [doc = " Returns an iterator over the arena’s mutable elements."] # [doc = ""] # [doc = " ```"] # [doc = " let mut arena = la_arena::Arena::new();"] # [doc = " let idx1 = arena.alloc(20);"] # [doc = ""] # [doc = " assert_eq!(arena[idx1], 20);"] # [doc = ""] # [doc = " let mut iterator = arena.iter_mut();"] # [doc = " *iterator.next().unwrap().1 = 10;"] # [doc = " drop(iterator);"] # [doc = ""] # [doc = " assert_eq!(arena[idx1], 10);"] # [doc = " ```"] pub fn iter_mut (& mut self ,) -> impl ExactSizeIterator < Item = (Idx < T > , & mut T) > + DoubleEndedIterator { self . data . iter_mut () . enumerate () . map (| (idx , value) | (Idx :: from_raw (RawIdx (idx as u32)) , value)) } # [doc = " Returns an iterator over the arena’s values."] # [doc = ""] # [doc = " ```"] # [doc = " let mut arena = la_arena::Arena::new();"] # [doc = " let idx1 = arena.alloc(20);"] # [doc = " let idx2 = arena.alloc(40);"] # [doc = " let idx3 = arena.alloc(60);"] # [doc = ""] # [doc = " let mut iterator = arena.values();"] # [doc = " assert_eq!(iterator.next(), Some(&20));"] # [doc = " assert_eq!(iterator.next(), Some(&40));"] # [doc = " assert_eq!(iterator.next(), Some(&60));"] # [doc = " ```"] pub fn values (& self) -> impl ExactSizeIterator < Item = & T > + DoubleEndedIterator { self . data . iter () } # [doc = " Returns an iterator over the arena’s mutable values."] # [doc = ""] # [doc = " ```"] # [doc = " let mut arena = la_arena::Arena::new();"] # [doc = " let idx1 = arena.alloc(20);"] # [doc = ""] # [doc = " assert_eq!(arena[idx1], 20);"] # [doc = ""] # [doc = " let mut iterator = arena.values_mut();"] # [doc = " *iterator.next().unwrap() = 10;"] # [doc = " drop(iterator);"] # [doc = ""] # [doc = " assert_eq!(arena[idx1], 10);"] # [doc = " ```"] pub fn values_mut (& mut self) -> impl ExactSizeIterator < Item = & mut T > + DoubleEndedIterator { self . data . iter_mut () } # [doc = " Reallocates the arena to make it take up as little space as possible."] pub fn shrink_to_fit (& mut self) { self . data . shrink_to_fit () ; } # [doc = " Returns the index of the next value allocated on the arena."] # [doc = ""] # [doc = " This method should remain private to make creating invalid `Idx`s harder."] fn next_idx (& self) -> Idx < T > { Idx :: from_raw (RawIdx (self . data . len () as u32)) } }
    };
}

impl_48!();