/// Helper macro for generating match arms for `one` fragments' `visit_with`.
/// This macro is temporarily empty to allow compilation.
macro_rules! __generate_visit_with_one_arm {
    ($($_:tt)*) => {}; // Empty expansion
}