// Generated macro for fork (function)
macro_rules! Depcrate_forkfork {
() => {
// Module: crate::fork
// Provides: {"fork"}
// Dependencies: {}
# [doc = " Simulate a process fork."] # [doc = ""] # [doc = " The function documentation here only lists information unique to calling it"] # [doc = " directly; please see the crate documentation for more details on how the"] # [doc = " forking process works."] # [doc = ""] # [doc = " Since this is not a true process fork, the calling code must be structured"] # [doc = " to ensure that the child process, upon starting from the same entry point,"] # [doc = " also reaches this same `fork()` call. Recursive forks are supported; the"] # [doc = " child branch is taken from all child processes of the fork even if it is"] # [doc = " not directly the child of a particular branch. However, encountering the"] # [doc = " same fork point more than once in a single execution sequence of a child"] # [doc = " process is not (e.g., putting this call in a recursive function) and"] # [doc = " results in unspecified behaviour."] # [doc = ""] # [doc = " The child's output is buffered into an anonymous temporary file. Before"] # [doc = " this call returns, this output is copied to the parent's standard output"] # [doc = " (passing through the redirect mechanism Rust test uses)."] # [doc = ""] # [doc = " `test_name` must exactly match the full path of the test function being"] # [doc = " run."] # [doc = ""] # [doc = " `fork_id` is a unique identifier identifying this particular fork location."] # [doc = " This *must* be stable across processes of the same executable; pointers are"] # [doc = " not suitable stable, and string constants may not be suitably unique. The"] # [doc = " [`rusty_fork_id!()`](macro.rusty_fork_id.html) macro is the recommended way"] # [doc = " to supply this parameter."] # [doc = ""] # [doc = " If this is the parent process, `in_parent` is invoked, and the return value"] # [doc = " becomes the return value from this function. The callback is passed a"] # [doc = " handle to the file which receives the child's output. If is the callee's"] # [doc = " responsibility to wait for the child to exit. If this is the child process,"] # [doc = " `in_child` is invoked, and when the callback returns, the child process"] # [doc = " exits."] # [doc = ""] # [doc = " If `in_parent` returns or panics before the child process has terminated,"] # [doc = " the child process is killed."] # [doc = ""] # [doc = " If `in_child` panics, the child process exits with a failure code"] # [doc = " immediately rather than let the panic propagate out of the `fork()` call."] # [doc = ""] # [doc = " `process_modifier` is invoked on the `std::process::Command` immediately"] # [doc = " before spawning the new process. The callee may modify the process"] # [doc = " parameters if desired, but should not do anything that would modify or"] # [doc = " remove any environment variables beginning with `RUSTY_FORK_`."] # [doc = ""] # [doc = " ## Panics"] # [doc = ""] # [doc = " Panics if the environment indicates that there are already at least 16"] # [doc = " levels of fork nesting."] # [doc = ""] # [doc = " Panics if `std::env::current_exe()` fails determine the path to the current"] # [doc = " executable."] # [doc = ""] # [doc = " Panics if any argument to the current process is not valid UTF-8."] pub fn fork < ID , MODIFIER , PARENT , CHILD , R > (test_name : & str , fork_id : ID , process_modifier : MODIFIER , in_parent : PARENT , in_child : CHILD) -> Result < R > where ID : Hash , MODIFIER : FnOnce (& mut process :: Command) , PARENT : FnOnce (& mut ChildWrapper , & mut fs :: File) -> R , CHILD : FnOnce () { let fork_id = id_str (fork_id) ; let mut return_value = None ; let mut process_modifier = Some (process_modifier) ; let mut in_parent = Some (in_parent) ; let mut in_child = Some (in_child) ; fork_impl (test_name , fork_id , & mut | cmd | process_modifier . take () . unwrap () (cmd) , & mut | child , file | return_value = Some (in_parent . take () . unwrap () (child , file)) , & mut | | in_child . take () . unwrap () ()) . map (| _ | return_value . unwrap ()) }
};
}
