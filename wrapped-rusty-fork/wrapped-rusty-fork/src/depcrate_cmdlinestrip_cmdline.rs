// Generated macro for strip_cmdline (function)
macro_rules! Depcrate_cmdlinestrip_cmdline {
() => {
// Module: crate::cmdline
// Provides: {"strip_cmdline"}
// Dependencies: {}
# [doc = " Parse the full command line as would be given to the Rust test harness, and"] # [doc = " strip out any flags that should be dropped as well as all filters. The"] # [doc = " resulting argument list is also guaranteed to not have \"--\", so that new"] # [doc = " flags can be appended."] # [doc = ""] # [doc = " The zeroth argument (the command name) is also dropped."] pub (crate) fn strip_cmdline < A : Iterator < Item = String > > (args : A) -> Result < Vec < String > > { # [derive (Clone , Copy)] enum State { Ground , PassingArg , DroppingArg , } let mut state = State :: DroppingArg ; let mut ret = Vec :: new () ; for arg in args { match state { State :: DroppingArg => { state = State :: Ground ; } , State :: PassingArg => { ret . push (arg) ; state = State :: Ground ; } , State :: Ground => { if & arg == "--" { break ; } else if & arg == "-" { continue ; } else if arg . starts_with ("--") { let (pass , has_arg) = look_up_flag_or_err (arg . split ('=') . next () . expect ("split returned empty")) ? ; let has_arg = has_arg && ! arg . contains ('=') ; if pass { ret . push (arg) ; if has_arg { state = State :: PassingArg ; } } else if has_arg { state = State :: DroppingArg ; } } else if arg . starts_with ("-") { let mut chars = arg . chars () ; let mut to_pass = "-" . to_owned () ; chars . next () ; while let Some (flag_ch) = chars . next () { let flag = format ! ("-{}" , flag_ch) ; let (pass , has_arg) = look_up_flag_or_err (& flag) ? ; if pass { to_pass . push (flag_ch) ; if has_arg { if chars . clone () . next () . is_some () { to_pass . extend (chars) ; } else { state = State :: PassingArg ; } break ; } } else if has_arg { if chars . clone () . next () . is_none () { state = State :: DroppingArg ; } break ; } } if "-" != & to_pass { ret . push (to_pass) ; } } else { } } , } } Ok (ret) }
};
}
