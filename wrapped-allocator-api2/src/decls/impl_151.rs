macro_rules! deps {
    () => {
        Vec!();
        RawVec!();
        Drain!();
        Box!();
        TryReserveError!();
        Allocator!();
    };
}

macro_rules! impl_151 {
    () => {
        deps!();
        impl < T , A : Allocator > Vec < T , A > { # [doc = " Constructs a new, empty `Vec<T, A>`."] # [doc = ""] # [doc = " The vector will not allocate until elements are pushed onto it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec::Vec, alloc::System};"] # [doc = ""] # [doc = " # #[allow(unused_mut)]"] # [doc = " let mut vec: Vec<i32, _> = Vec::new_in(System);"] # [doc = " ```"] # [inline (always)] pub const fn new_in (alloc : A) -> Self { Vec { buf : RawVec :: new_in (alloc) , len : 0 , } } # [doc = " Constructs a new, empty `Vec<T, A>` with at least the specified capacity"] # [doc = " with the provided allocator."] # [doc = ""] # [doc = " The vector will be able to hold at least `capacity` elements without"] # [doc = " reallocating. This method is allowed to allocate for more elements than"] # [doc = " `capacity`. If `capacity` is 0, the vector will not allocate."] # [doc = ""] # [doc = " It is important to note that although the returned vector has the"] # [doc = " minimum *capacity* specified, the vector will have a zero *length*. For"] # [doc = " an explanation of the difference between length and capacity, see"] # [doc = " *[Capacity and reallocation]*."] # [doc = ""] # [doc = " If it is important to know the exact allocated capacity of a `Vec`,"] # [doc = " always use the [`capacity`] method after construction."] # [doc = ""] # [doc = " For `Vec<T, A>` where `T` is a zero-sized type, there will be no allocation"] # [doc = " and the capacity will always be `usize::MAX`."] # [doc = ""] # [doc = " [Capacity and reallocation]: #capacity-and-reallocation"] # [doc = " [`capacity`]: Vec::capacity"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` bytes."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec::Vec, alloc::System};"] # [doc = ""] # [doc = " let mut vec = Vec::with_capacity_in(10, System);"] # [doc = ""] # [doc = " // The vector contains no items, even though it has capacity for more"] # [doc = " assert_eq!(vec.len(), 0);"] # [doc = " assert_eq!(vec.capacity(), 10);"] # [doc = ""] # [doc = " // These are all done without reallocating..."] # [doc = " for i in 0..10 {"] # [doc = "     vec.push(i);"] # [doc = " }"] # [doc = " assert_eq!(vec.len(), 10);"] # [doc = " assert_eq!(vec.capacity(), 10);"] # [doc = ""] # [doc = " // ...but this may make the vector reallocate"] # [doc = " vec.push(11);"] # [doc = " assert_eq!(vec.len(), 11);"] # [doc = " assert!(vec.capacity() >= 11);"] # [doc = ""] # [doc = " // A vector of a zero-sized type will always over-allocate, since no"] # [doc = " // allocation is necessary"] # [doc = " let vec_units = Vec::<(), System>::with_capacity_in(10, System);"] # [doc = " assert_eq!(vec_units.capacity(), usize::MAX);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn with_capacity_in (capacity : usize , alloc : A) -> Self { Vec { buf : RawVec :: with_capacity_in (capacity , alloc) , len : 0 , } } # [doc = " Creates a `Vec<T, A>` directly from a pointer, a capacity, a length,"] # [doc = " and an allocator."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This is highly unsafe, due to the number of invariants that aren't"] # [doc = " checked:"] # [doc = ""] # [doc = " * `T` needs to have the same alignment as what `ptr` was allocated with."] # [doc = "   (`T` having a less strict alignment is not sufficient, the alignment really"] # [doc = "   needs to be equal to satisfy the [`dealloc`] requirement that memory must be"] # [doc = "   allocated and deallocated with the same layout.)"] # [doc = " * The size of `T` times the `capacity` (ie. the allocated size in bytes) needs"] # [doc = "   to be the same size as the pointer was allocated with. (Because similar to"] # [doc = "   alignment, [`dealloc`] must be called with the same layout `size`.)"] # [doc = " * `length` needs to be less than or equal to `capacity`."] # [doc = " * The first `length` values must be properly initialized values of type `T`."] # [doc = " * `capacity` needs to [*fit*] the layout size that the pointer was allocated with."] # [doc = " * The allocated size in bytes must be no larger than `isize::MAX`."] # [doc = "   See the safety documentation of [`pointer::offset`](https://doc.rust-lang.org/nightly/std/primitive.pointer.html#method.offset)."] # [doc = ""] # [doc = " These requirements are always upheld by any `ptr` that has been allocated"] # [doc = " via `Vec<T, A>`. Other allocation sources are allowed if the invariants are"] # [doc = " upheld."] # [doc = ""] # [doc = " Violating these may cause problems like corrupting the allocator's"] # [doc = " internal data structures. For example it is **not** safe"] # [doc = " to build a `Vec<u8>` from a pointer to a C `char` array with length `size_t`."] # [doc = " It's also not safe to build one from a `Vec<u16>` and its length, because"] # [doc = " the allocator cares about the alignment, and these two types have different"] # [doc = " alignments. The buffer was allocated with alignment 2 (for `u16`), but after"] # [doc = " turning it into a `Vec<u8>` it'll be deallocated with alignment 1."] # [doc = ""] # [doc = " The ownership of `ptr` is effectively transferred to the"] # [doc = " `Vec<T>` which may then deallocate, reallocate or change the"] # [doc = " contents of memory pointed to by the pointer at will. Ensure"] # [doc = " that nothing else uses the pointer after calling this"] # [doc = " function."] # [doc = ""] # [doc = " [`String`]: alloc_crate::string::String"] # [doc = " [`dealloc`]: crate::alloc::GlobalAlloc::dealloc"] # [doc = " [*fit*]: crate::alloc::Allocator#memory-fitting"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec::Vec, alloc::System};"] # [doc = ""] # [doc = " use std::ptr;"] # [doc = " use std::mem;"] # [doc = ""] # [doc = " let mut v = Vec::with_capacity_in(3, System);"] # [doc = " v.push(1);"] # [doc = " v.push(2);"] # [doc = " v.push(3);"] # [doc = ""] # [doc = " // Prevent running `v`'s destructor so we are in complete control"] # [doc = " // of the allocation."] # [doc = " let mut v = mem::ManuallyDrop::new(v);"] # [doc = ""] # [doc = " // Pull out the various important pieces of information about `v`"] # [doc = " let p = v.as_mut_ptr();"] # [doc = " let len = v.len();"] # [doc = " let cap = v.capacity();"] # [doc = " let alloc = v.allocator();"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     // Overwrite memory with 4, 5, 6"] # [doc = "     for i in 0..len {"] # [doc = "         ptr::write(p.add(i), 4 + i);"] # [doc = "     }"] # [doc = ""] # [doc = "     // Put everything back together into a Vec"] # [doc = "     let rebuilt = Vec::from_raw_parts_in(p, len, cap, alloc.clone());"] # [doc = "     assert_eq!(rebuilt, [4, 5, 6]);"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Using memory that was allocated elsewhere:"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec::Vec, alloc::{alloc, Layout}};"] # [doc = ""] # [doc = " fn main() {"] # [doc = "     let layout = Layout::array::<u32>(16).expect(\"overflow cannot happen\");"] # [doc = "     let vec = unsafe {"] # [doc = "         let mem = alloc(layout).cast::<u32>();"] # [doc = "         if mem.is_null() {"] # [doc = "             return;"] # [doc = "         }"] # [doc = ""] # [doc = "         mem.write(1_000_000);"] # [doc = ""] # [doc = "         Vec::from_raw_parts(mem, 1, 16)"] # [doc = "     };"] # [doc = ""] # [doc = "     assert_eq!(vec, &[1_000_000]);"] # [doc = "     assert_eq!(vec.capacity(), 16);"] # [doc = " }"] # [doc = " ```"] # [inline (always)] pub unsafe fn from_raw_parts_in (ptr : * mut T , length : usize , capacity : usize , alloc : A) -> Self { unsafe { Vec { buf : RawVec :: from_raw_parts_in (ptr , capacity , alloc) , len : length , } } } # [doc = " Decomposes a `Vec<T>` into its raw components."] # [doc = ""] # [doc = " Returns the raw pointer to the underlying data, the length of"] # [doc = " the vector (in elements), and the allocated capacity of the"] # [doc = " data (in elements). These are the same arguments in the same"] # [doc = " order as the arguments to [`from_raw_parts`]."] # [doc = ""] # [doc = " After calling this function, the caller is responsible for the"] # [doc = " memory previously managed by the `Vec`. The only way to do"] # [doc = " this is to convert the raw pointer, length, and capacity back"] # [doc = " into a `Vec` with the [`from_raw_parts`] function, allowing"] # [doc = " the destructor to perform the cleanup."] # [doc = ""] # [doc = " [`from_raw_parts`]: Vec::from_raw_parts"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec, vec::Vec};"] # [doc = ""] # [doc = " let v: Vec<i32> = vec![-1, 0, 1];"] # [doc = ""] # [doc = " let (ptr, len, cap) = v.into_raw_parts();"] # [doc = ""] # [doc = " let rebuilt = unsafe {"] # [doc = "     // We can now make changes to the components, such as"] # [doc = "     // transmuting the raw pointer to a compatible type."] # [doc = "     let ptr = ptr as *mut u32;"] # [doc = ""] # [doc = "     Vec::from_raw_parts(ptr, len, cap)"] # [doc = " };"] # [doc = " assert_eq!(rebuilt, [4294967295, 0, 1]);"] # [doc = " ```"] pub fn into_raw_parts (self) -> (* mut T , usize , usize) { let mut me = ManuallyDrop :: new (self) ; (me . as_mut_ptr () , me . len () , me . capacity ()) } # [doc = " Decomposes a `Vec<T>` into its raw components."] # [doc = ""] # [doc = " Returns the raw pointer to the underlying data, the length of the vector (in elements),"] # [doc = " the allocated capacity of the data (in elements), and the allocator. These are the same"] # [doc = " arguments in the same order as the arguments to [`from_raw_parts_in`]."] # [doc = ""] # [doc = " After calling this function, the caller is responsible for the"] # [doc = " memory previously managed by the `Vec`. The only way to do"] # [doc = " this is to convert the raw pointer, length, and capacity back"] # [doc = " into a `Vec` with the [`from_raw_parts_in`] function, allowing"] # [doc = " the destructor to perform the cleanup."] # [doc = ""] # [doc = " [`from_raw_parts_in`]: Vec::from_raw_parts_in"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec::Vec, alloc::System};"] # [doc = ""] # [doc = " let mut v: Vec<i32, System> = Vec::new_in(System);"] # [doc = " v.push(-1);"] # [doc = " v.push(0);"] # [doc = " v.push(1);"] # [doc = ""] # [doc = " let (ptr, len, cap, alloc) = v.into_raw_parts_with_alloc();"] # [doc = ""] # [doc = " let rebuilt = unsafe {"] # [doc = "     // We can now make changes to the components, such as"] # [doc = "     // transmuting the raw pointer to a compatible type."] # [doc = "     let ptr = ptr as *mut u32;"] # [doc = ""] # [doc = "     Vec::from_raw_parts_in(ptr, len, cap, alloc)"] # [doc = " };"] # [doc = " assert_eq!(rebuilt, [4294967295, 0, 1]);"] # [doc = " ```"] pub fn into_raw_parts_with_alloc (self) -> (* mut T , usize , usize , A) { let mut me = ManuallyDrop :: new (self) ; let len = me . len () ; let capacity = me . capacity () ; let ptr = me . as_mut_ptr () ; let alloc = unsafe { ptr :: read (me . allocator ()) } ; (ptr , len , capacity , alloc) } # [doc = " Returns the total number of elements the vector can hold without"] # [doc = " reallocating."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec::Vec;"] # [doc = ""] # [doc = " let mut vec: Vec<i32> = Vec::with_capacity(10);"] # [doc = " vec.push(42);"] # [doc = " assert_eq!(vec.capacity(), 10);"] # [doc = " ```"] # [inline (always)] pub fn capacity (& self) -> usize { self . buf . capacity () } # [doc = " Reserves capacity for at least `additional` more elements to be inserted"] # [doc = " in the given `Vec<T>`. The collection may reserve more space to"] # [doc = " speculatively avoid frequent reallocations. After calling `reserve`,"] # [doc = " capacity will be greater than or equal to `self.len() + additional`."] # [doc = " Does nothing if capacity is already sufficient."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` bytes."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1];"] # [doc = " vec.reserve(10);"] # [doc = " assert!(vec.capacity() >= 11);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn reserve (& mut self , additional : usize) { self . buf . reserve (self . len , additional) ; } # [doc = " Reserves the minimum capacity for at least `additional` more elements to"] # [doc = " be inserted in the given `Vec<T>`. Unlike [`reserve`], this will not"] # [doc = " deliberately over-allocate to speculatively avoid frequent allocations."] # [doc = " After calling `reserve_exact`, capacity will be greater than or equal to"] # [doc = " `self.len() + additional`. Does nothing if the capacity is already"] # [doc = " sufficient."] # [doc = ""] # [doc = " Note that the allocator may give the collection more space than it"] # [doc = " requests. Therefore, capacity can not be relied upon to be precisely"] # [doc = " minimal. Prefer [`reserve`] if future insertions are expected."] # [doc = ""] # [doc = " [`reserve`]: Vec::reserve"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` bytes."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1];"] # [doc = " vec.reserve_exact(10);"] # [doc = " assert!(vec.capacity() >= 11);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn reserve_exact (& mut self , additional : usize) { self . buf . reserve_exact (self . len , additional) ; } # [doc = " Tries to reserve capacity for at least `additional` more elements to be inserted"] # [doc = " in the given `Vec<T>`. The collection may reserve more space to speculatively avoid"] # [doc = " frequent reallocations. After calling `try_reserve`, capacity will be"] # [doc = " greater than or equal to `self.len() + additional` if it returns"] # [doc = " `Ok(())`. Does nothing if capacity is already sufficient. This method"] # [doc = " preserves the contents even if an error occurs."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the capacity overflows, or the allocator reports a failure, then an error"] # [doc = " is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec::Vec, collections::TryReserveError};"] # [doc = ""] # [doc = " fn process_data(data: &[u32]) -> Result<Vec<u32>, TryReserveError> {"] # [doc = "     let mut output = Vec::new();"] # [doc = ""] # [doc = "     // Pre-reserve the memory, exiting if we can't"] # [doc = "     output.try_reserve(data.len())?;"] # [doc = ""] # [doc = "     // Now we know this can't OOM in the middle of our complex work"] # [doc = "     output.extend(data.iter().map(|&val| {"] # [doc = "         val * 2 + 5 // very complicated"] # [doc = "     }));"] # [doc = ""] # [doc = "     Ok(output)"] # [doc = " }"] # [doc = " # process_data(&[1, 2, 3]).expect(\"why is the test harness OOMing on 12 bytes?\");"] # [doc = " ```"] # [inline (always)] pub fn try_reserve (& mut self , additional : usize) -> Result < () , TryReserveError > { self . buf . try_reserve (self . len , additional) } # [doc = " Tries to reserve the minimum capacity for at least `additional`"] # [doc = " elements to be inserted in the given `Vec<T>`. Unlike [`try_reserve`],"] # [doc = " this will not deliberately over-allocate to speculatively avoid frequent"] # [doc = " allocations. After calling `try_reserve_exact`, capacity will be greater"] # [doc = " than or equal to `self.len() + additional` if it returns `Ok(())`."] # [doc = " Does nothing if the capacity is already sufficient."] # [doc = ""] # [doc = " Note that the allocator may give the collection more space than it"] # [doc = " requests. Therefore, capacity can not be relied upon to be precisely"] # [doc = " minimal. Prefer [`try_reserve`] if future insertions are expected."] # [doc = ""] # [doc = " [`try_reserve`]: Vec::try_reserve"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the capacity overflows, or the allocator reports a failure, then an error"] # [doc = " is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec::Vec, collections::TryReserveError};"] # [doc = ""] # [doc = " fn process_data(data: &[u32]) -> Result<Vec<u32>, TryReserveError> {"] # [doc = "     let mut output = Vec::new();"] # [doc = ""] # [doc = "     // Pre-reserve the memory, exiting if we can't"] # [doc = "     output.try_reserve_exact(data.len())?;"] # [doc = ""] # [doc = "     // Now we know this can't OOM in the middle of our complex work"] # [doc = "     output.extend(data.iter().map(|&val| {"] # [doc = "         val * 2 + 5 // very complicated"] # [doc = "     }));"] # [doc = ""] # [doc = "     Ok(output)"] # [doc = " }"] # [doc = " # process_data(&[1, 2, 3]).expect(\"why is the test harness OOMing on 12 bytes?\");"] # [doc = " ```"] # [inline (always)] pub fn try_reserve_exact (& mut self , additional : usize) -> Result < () , TryReserveError > { self . buf . try_reserve_exact (self . len , additional) } # [doc = " Shrinks the capacity of the vector as much as possible."] # [doc = ""] # [doc = " It will drop down as close as possible to the length but the allocator"] # [doc = " may still inform the vector that there is space for a few more elements."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec::Vec;"] # [doc = ""] # [doc = " let mut vec = Vec::with_capacity(10);"] # [doc = " vec.extend([1, 2, 3]);"] # [doc = " assert_eq!(vec.capacity(), 10);"] # [doc = " vec.shrink_to_fit();"] # [doc = " assert!(vec.capacity() >= 3);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn shrink_to_fit (& mut self) { if self . capacity () > self . len { self . buf . shrink_to_fit (self . len) ; } } # [doc = " Shrinks the capacity of the vector with a lower bound."] # [doc = ""] # [doc = " The capacity will remain at least as large as both the length"] # [doc = " and the supplied value."] # [doc = ""] # [doc = " If the current capacity is less than the lower limit, this is a no-op."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec::Vec;"] # [doc = ""] # [doc = " let mut vec = Vec::with_capacity(10);"] # [doc = " vec.extend([1, 2, 3]);"] # [doc = " assert_eq!(vec.capacity(), 10);"] # [doc = " vec.shrink_to(4);"] # [doc = " assert!(vec.capacity() >= 4);"] # [doc = " vec.shrink_to(0);"] # [doc = " assert!(vec.capacity() >= 3);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn shrink_to (& mut self , min_capacity : usize) { if self . capacity () > min_capacity { self . buf . shrink_to_fit (cmp :: max (self . len , min_capacity)) ; } } # [doc = " Converts the vector into [`Box<[T]>`][owned slice]."] # [doc = ""] # [doc = " If the vector has excess capacity, its items will be moved into a"] # [doc = " newly-allocated buffer with exactly the right capacity."] # [doc = ""] # [doc = " [owned slice]: Box"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let v = vec![1, 2, 3];"] # [doc = ""] # [doc = " let slice = v.into_boxed_slice();"] # [doc = " ```"] # [doc = ""] # [doc = " Any excess capacity is removed:"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec::Vec;"] # [doc = ""] # [doc = " let mut vec = Vec::with_capacity(10);"] # [doc = " vec.extend([1, 2, 3]);"] # [doc = ""] # [doc = " assert_eq!(vec.capacity(), 10);"] # [doc = " let slice = vec.into_boxed_slice();"] # [doc = " assert_eq!(slice.into_vec().capacity(), 3);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn into_boxed_slice (mut self) -> Box < [T] , A > { unsafe { self . shrink_to_fit () ; let me = ManuallyDrop :: new (self) ; let buf = ptr :: read (& me . buf) ; let len = me . len () ; Box :: < [mem :: MaybeUninit < T >] , A > :: assume_init (buf . into_box (len)) } } # [doc = " Shortens the vector, keeping the first `len` elements and dropping"] # [doc = " the rest."] # [doc = ""] # [doc = " If `len` is greater than the vector's current length, this has no"] # [doc = " effect."] # [doc = ""] # [doc = " The [`drain`] method can emulate `truncate`, but causes the excess"] # [doc = " elements to be returned instead of dropped."] # [doc = ""] # [doc = " Note that this method has no effect on the allocated capacity"] # [doc = " of the vector."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Truncating a five element vector to two elements:"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1, 2, 3, 4, 5];"] # [doc = " vec.truncate(2);"] # [doc = " assert_eq!(vec, [1, 2]);"] # [doc = " ```"] # [doc = ""] # [doc = " No truncation occurs when `len` is greater than the vector's current"] # [doc = " length:"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1, 2, 3];"] # [doc = " vec.truncate(8);"] # [doc = " assert_eq!(vec, [1, 2, 3]);"] # [doc = " ```"] # [doc = ""] # [doc = " Truncating when `len == 0` is equivalent to calling the [`clear`]"] # [doc = " method."] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1, 2, 3];"] # [doc = " vec.truncate(0);"] # [doc = " assert_eq!(vec, []);"] # [doc = " ```"] # [doc = ""] # [doc = " [`clear`]: Vec::clear"] # [doc = " [`drain`]: Vec::drain"] # [inline (always)] pub fn truncate (& mut self , len : usize) { unsafe { if len > self . len { return ; } let remaining_len = self . len - len ; let s = ptr :: slice_from_raw_parts_mut (self . as_mut_ptr () . add (len) , remaining_len) ; self . len = len ; ptr :: drop_in_place (s) ; } } # [doc = " Extracts a slice containing the entire vector."] # [doc = ""] # [doc = " Equivalent to `&s[..]`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " use std::io::{self, Write};"] # [doc = ""] # [doc = " let buffer = vec![1, 2, 3, 5, 8];"] # [doc = " io::sink().write(buffer.as_slice()).unwrap();"] # [doc = " ```"] # [inline (always)] pub fn as_slice (& self) -> & [T] { self } # [doc = " Extracts a mutable slice of the entire vector."] # [doc = ""] # [doc = " Equivalent to `&mut s[..]`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " use std::io::{self, Read};"] # [doc = ""] # [doc = " let mut buffer = vec![0; 3];"] # [doc = " io::repeat(0b101).read_exact(buffer.as_mut_slice()).unwrap();"] # [doc = " ```"] # [inline (always)] pub fn as_mut_slice (& mut self) -> & mut [T] { self } # [doc = " Returns a raw pointer to the vector's buffer, or a dangling raw pointer"] # [doc = " valid for zero sized reads if the vector didn't allocate."] # [doc = ""] # [doc = " The caller must ensure that the vector outlives the pointer this"] # [doc = " function returns, or else it will end up pointing to garbage."] # [doc = " Modifying the vector may cause its buffer to be reallocated,"] # [doc = " which would also make any pointers to it invalid."] # [doc = ""] # [doc = " The caller must also ensure that the memory the pointer (non-transitively) points to"] # [doc = " is never written to (except inside an `UnsafeCell`) using this pointer or any pointer"] # [doc = " derived from it. If you need to mutate the contents of the slice, use [`as_mut_ptr`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let x = vec![1, 2, 4];"] # [doc = " let x_ptr = x.as_ptr();"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     for i in 0..x.len() {"] # [doc = "         assert_eq!(*x_ptr.add(i), 1 << i);"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [`as_mut_ptr`]: Vec::as_mut_ptr"] # [inline (always)] pub fn as_ptr (& self) -> * const T { let ptr = self . buf . ptr () ; unsafe { assume (! ptr . is_null ()) ; } ptr } # [doc = " Returns an unsafe mutable pointer to the vector's buffer, or a dangling"] # [doc = " raw pointer valid for zero sized reads if the vector didn't allocate."] # [doc = ""] # [doc = " The caller must ensure that the vector outlives the pointer this"] # [doc = " function returns, or else it will end up pointing to garbage."] # [doc = " Modifying the vector may cause its buffer to be reallocated,"] # [doc = " which would also make any pointers to it invalid."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec::Vec;"] # [doc = ""] # [doc = " // Allocate vector big enough for 4 elements."] # [doc = " let size = 4;"] # [doc = " let mut x: Vec<i32> = Vec::with_capacity(size);"] # [doc = " let x_ptr = x.as_mut_ptr();"] # [doc = ""] # [doc = " // Initialize elements via raw pointer writes, then set length."] # [doc = " unsafe {"] # [doc = "     for i in 0..size {"] # [doc = "         *x_ptr.add(i) = i as i32;"] # [doc = "     }"] # [doc = "     x.set_len(size);"] # [doc = " }"] # [doc = " assert_eq!(&*x, &[0, 1, 2, 3]);"] # [doc = " ```"] # [inline (always)] pub fn as_mut_ptr (& mut self) -> * mut T { let ptr = self . buf . ptr () ; unsafe { assume (! ptr . is_null ()) ; } ptr } # [doc = " Returns a reference to the underlying allocator."] # [inline (always)] pub fn allocator (& self) -> & A { self . buf . allocator () } # [doc = " Forces the length of the vector to `new_len`."] # [doc = ""] # [doc = " This is a low-level operation that maintains none of the normal"] # [doc = " invariants of the type. Normally changing the length of a vector"] # [doc = " is done using one of the safe operations instead, such as"] # [doc = " [`truncate`], [`resize`], [`extend`], or [`clear`]."] # [doc = ""] # [doc = " [`truncate`]: Vec::truncate"] # [doc = " [`resize`]: Vec::resize"] # [doc = " [`extend`]: Extend::extend"] # [doc = " [`clear`]: Vec::clear"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " - `new_len` must be less than or equal to [`capacity()`]."] # [doc = " - The elements at `old_len..new_len` must be initialized."] # [doc = ""] # [doc = " [`capacity()`]: Vec::capacity"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " This method can be useful for situations in which the vector"] # [doc = " is serving as a buffer for other code, particularly over FFI:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " # #![allow(dead_code)]"] # [doc = " use allocator_api2::vec::Vec;"] # [doc = ""] # [doc = " # // This is just a minimal skeleton for the doc example;"] # [doc = " # // don't use this as a starting point for a real library."] # [doc = " # pub struct StreamWrapper { strm: *mut std::ffi::c_void }"] # [doc = " # const Z_OK: i32 = 0;"] # [doc = " # extern \"C\" {"] # [doc = " #     fn deflateGetDictionary("] # [doc = " #         strm: *mut std::ffi::c_void,"] # [doc = " #         dictionary: *mut u8,"] # [doc = " #         dictLength: *mut usize,"] # [doc = " #     ) -> i32;"] # [doc = " # }"] # [doc = " # impl StreamWrapper {"] # [doc = " pub fn get_dictionary(&self) -> Option<Vec<u8>> {"] # [doc = "     // Per the FFI method's docs, \"32768 bytes is always enough\"."] # [doc = "     let mut dict = Vec::with_capacity(32_768);"] # [doc = "     let mut dict_length = 0;"] # [doc = "     // SAFETY: When `deflateGetDictionary` returns `Z_OK`, it holds that:"] # [doc = "     // 1. `dict_length` elements were initialized."] # [doc = "     // 2. `dict_length` <= the capacity (32_768)"] # [doc = "     // which makes `set_len` safe to call."] # [doc = "     unsafe {"] # [doc = "         // Make the FFI call..."] # [doc = "         let r = deflateGetDictionary(self.strm, dict.as_mut_ptr(), &mut dict_length);"] # [doc = "         if r == Z_OK {"] # [doc = "             // ...and update the length to what was initialized."] # [doc = "             dict.set_len(dict_length);"] # [doc = "             Some(dict)"] # [doc = "         } else {"] # [doc = "             None"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " While the following example is sound, there is a memory leak since"] # [doc = " the inner vectors were not freed prior to the `set_len` call:"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![vec![1, 0, 0],"] # [doc = "                    vec![0, 1, 0],"] # [doc = "                    vec![0, 0, 1]];"] # [doc = " // SAFETY:"] # [doc = " // 1. `old_len..0` is empty so no elements need to be initialized."] # [doc = " // 2. `0 <= capacity` always holds whatever `capacity` is."] # [doc = " unsafe {"] # [doc = "     vec.set_len(0);"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Normally, here, one would use [`clear`] instead to correctly drop"] # [doc = " the contents and thus not leak memory."] # [inline (always)] pub unsafe fn set_len (& mut self , new_len : usize) { debug_assert ! (new_len <= self . capacity ()) ; self . len = new_len ; } # [doc = " Removes an element from the vector and returns it."] # [doc = ""] # [doc = " The removed element is replaced by the last element of the vector."] # [doc = ""] # [doc = " This does not preserve ordering, but is *O*(1)."] # [doc = " If you need to preserve the element order, use [`remove`] instead."] # [doc = ""] # [doc = " [`remove`]: Vec::remove"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `index` is out of bounds."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut v = vec![\"foo\", \"bar\", \"baz\", \"qux\"];"] # [doc = ""] # [doc = " assert_eq!(v.swap_remove(1), \"bar\");"] # [doc = " assert_eq!(v, [\"foo\", \"qux\", \"baz\"]);"] # [doc = ""] # [doc = " assert_eq!(v.swap_remove(0), \"foo\");"] # [doc = " assert_eq!(v, [\"baz\", \"qux\"]);"] # [doc = " ```"] # [inline (always)] pub fn swap_remove (& mut self , index : usize) -> T { # [cold] # [inline (never)] fn assert_failed (index : usize , len : usize) -> ! { panic ! ("swap_remove index (is {}) should be < len (is {})" , index , len) ; } let len = self . len () ; if index >= len { assert_failed (index , len) ; } unsafe { let value = ptr :: read (self . as_ptr () . add (index)) ; let base_ptr = self . as_mut_ptr () ; ptr :: copy (base_ptr . add (len - 1) , base_ptr . add (index) , 1) ; self . set_len (len - 1) ; value } } # [doc = " Inserts an element at position `index` within the vector, shifting all"] # [doc = " elements after it to the right."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `index > len`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1, 2, 3];"] # [doc = " vec.insert(1, 4);"] # [doc = " assert_eq!(vec, [1, 4, 2, 3]);"] # [doc = " vec.insert(4, 5);"] # [doc = " assert_eq!(vec, [1, 4, 2, 3, 5]);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] pub fn insert (& mut self , index : usize , element : T) { # [cold] # [inline (never)] fn assert_failed (index : usize , len : usize) -> ! { panic ! ("insertion index (is {}) should be <= len (is {})" , index , len) ; } let len = self . len () ; if len == self . buf . capacity () { self . reserve (1) ; } unsafe { { let p = self . as_mut_ptr () . add (index) ; match cmp :: Ord :: cmp (& index , & len) { Ordering :: Less => { ptr :: copy (p , p . add (1) , len - index) ; } Ordering :: Equal => { } Ordering :: Greater => { assert_failed (index , len) ; } } ptr :: write (p , element) ; } self . set_len (len + 1) ; } } # [doc = " Removes and returns the element at position `index` within the vector,"] # [doc = " shifting all elements after it to the left."] # [doc = ""] # [doc = " Note: Because this shifts over the remaining elements, it has a"] # [doc = " worst-case performance of *O*(*n*). If you don't need the order of elements"] # [doc = " to be preserved, use [`swap_remove`] instead. If you'd like to remove"] # [doc = " elements from the beginning of the `Vec`, consider using"] # [doc = " [`VecDeque::pop_front`] instead."] # [doc = ""] # [doc = " [`swap_remove`]: Vec::swap_remove"] # [doc = " [`VecDeque::pop_front`]: alloc_crate::collections::VecDeque::pop_front"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `index` is out of bounds."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut v = vec![1, 2, 3];"] # [doc = " assert_eq!(v.remove(1), 2);"] # [doc = " assert_eq!(v, [1, 3]);"] # [doc = " ```"] # [track_caller] # [inline (always)] pub fn remove (& mut self , index : usize) -> T { # [cold] # [inline (never)] # [track_caller] fn assert_failed (index : usize , len : usize) -> ! { panic ! ("removal index (is {}) should be < len (is {})" , index , len) ; } let len = self . len () ; if index >= len { assert_failed (index , len) ; } unsafe { let ret ; { let ptr = self . as_mut_ptr () . add (index) ; ret = ptr :: read (ptr) ; ptr :: copy (ptr . add (1) , ptr , len - index - 1) ; } self . set_len (len - 1) ; ret } } # [doc = " Retains only the elements specified by the predicate."] # [doc = ""] # [doc = " In other words, remove all elements `e` for which `f(&e)` returns `false`."] # [doc = " This method operates in place, visiting each element exactly once in the"] # [doc = " original order, and preserves the order of the retained elements."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1, 2, 3, 4];"] # [doc = " vec.retain(|&x| x % 2 == 0);"] # [doc = " assert_eq!(vec, [2, 4]);"] # [doc = " ```"] # [doc = ""] # [doc = " Because the elements are visited exactly once in the original order,"] # [doc = " external state may be used to decide which elements to keep."] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1, 2, 3, 4, 5];"] # [doc = " let keep = [false, true, true, false, true];"] # [doc = " let mut iter = keep.iter();"] # [doc = " vec.retain(|_| *iter.next().unwrap());"] # [doc = " assert_eq!(vec, [2, 3, 5]);"] # [doc = " ```"] # [inline (always)] pub fn retain < F > (& mut self , mut f : F) where F : FnMut (& T) -> bool , { self . retain_mut (| elem | f (elem)) ; } # [doc = " Retains only the elements specified by the predicate, passing a mutable reference to it."] # [doc = ""] # [doc = " In other words, remove all elements `e` such that `f(&mut e)` returns `false`."] # [doc = " This method operates in place, visiting each element exactly once in the"] # [doc = " original order, and preserves the order of the retained elements."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1, 2, 3, 4];"] # [doc = " vec.retain_mut(|x| if *x <= 3 {"] # [doc = "     *x += 1;"] # [doc = "     true"] # [doc = " } else {"] # [doc = "     false"] # [doc = " });"] # [doc = " assert_eq!(vec, [2, 3, 4]);"] # [doc = " ```"] # [inline] pub fn retain_mut < F > (& mut self , mut f : F) where F : FnMut (& mut T) -> bool , { let original_len = self . len () ; unsafe { self . set_len (0) } ; struct BackshiftOnDrop < 'a , T , A : Allocator > { v : & 'a mut Vec < T , A > , processed_len : usize , deleted_cnt : usize , original_len : usize , } impl < T , A : Allocator > Drop for BackshiftOnDrop < '_ , T , A > { fn drop (& mut self) { if self . deleted_cnt > 0 { unsafe { ptr :: copy (self . v . as_ptr () . add (self . processed_len) , self . v . as_mut_ptr () . add (self . processed_len - self . deleted_cnt) , self . original_len - self . processed_len ,) ; } } unsafe { self . v . set_len (self . original_len - self . deleted_cnt) ; } } } let mut g = BackshiftOnDrop { v : self , processed_len : 0 , deleted_cnt : 0 , original_len , } ; fn process_loop < F , T , A : Allocator , const DELETED : bool > (original_len : usize , f : & mut F , g : & mut BackshiftOnDrop < '_ , T , A > ,) where F : FnMut (& mut T) -> bool , { while g . processed_len != original_len { let cur = unsafe { & mut * g . v . as_mut_ptr () . add (g . processed_len) } ; if ! f (cur) { g . processed_len += 1 ; g . deleted_cnt += 1 ; unsafe { ptr :: drop_in_place (cur) } ; if DELETED { continue ; } else { break ; } } if DELETED { unsafe { let hole_slot = g . v . as_mut_ptr () . add (g . processed_len - g . deleted_cnt) ; ptr :: copy_nonoverlapping (cur , hole_slot , 1) ; } } g . processed_len += 1 ; } } process_loop :: < F , T , A , false > (original_len , & mut f , & mut g) ; process_loop :: < F , T , A , true > (original_len , & mut f , & mut g) ; drop (g) ; } # [doc = " Removes all but the first of consecutive elements in the vector that resolve to the same"] # [doc = " key."] # [doc = ""] # [doc = " If the vector is sorted, this removes all duplicates."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![10, 20, 21, 30, 20];"] # [doc = ""] # [doc = " vec.dedup_by_key(|i| *i / 10);"] # [doc = ""] # [doc = " assert_eq!(vec, [10, 20, 30, 20]);"] # [doc = " ```"] # [inline (always)] pub fn dedup_by_key < F , K > (& mut self , mut key : F) where F : FnMut (& mut T) -> K , K : PartialEq , { self . dedup_by (| a , b | key (a) == key (b)) } # [doc = " Removes all but the first of consecutive elements in the vector satisfying a given equality"] # [doc = " relation."] # [doc = ""] # [doc = " The `same_bucket` function is passed references to two elements from the vector and"] # [doc = " must determine if the elements compare equal. The elements are passed in opposite order"] # [doc = " from their order in the slice, so if `same_bucket(a, b)` returns `true`, `a` is removed."] # [doc = ""] # [doc = " If the vector is sorted, this removes all duplicates."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![\"foo\", \"bar\", \"Bar\", \"baz\", \"bar\"];"] # [doc = ""] # [doc = " vec.dedup_by(|a, b| a.eq_ignore_ascii_case(b));"] # [doc = ""] # [doc = " assert_eq!(vec, [\"foo\", \"bar\", \"baz\", \"bar\"]);"] # [doc = " ```"] # [inline] pub fn dedup_by < F > (& mut self , mut same_bucket : F) where F : FnMut (& mut T , & mut T) -> bool , { let len = self . len () ; if len <= 1 { return ; } struct FillGapOnDrop < 'a , T , A : Allocator > { read : usize , write : usize , vec : & 'a mut Vec < T , A > , } impl < 'a , T , A : Allocator > Drop for FillGapOnDrop < 'a , T , A > { fn drop (& mut self) { unsafe { let ptr = self . vec . as_mut_ptr () ; let len = self . vec . len () ; let items_left = len . wrapping_sub (self . read) ; let dropped_ptr = ptr . add (self . write) ; let valid_ptr = ptr . add (self . read) ; ptr :: copy (valid_ptr , dropped_ptr , items_left) ; let dropped = self . read . wrapping_sub (self . write) ; self . vec . set_len (len - dropped) ; } } } let mut gap = FillGapOnDrop { read : 1 , write : 1 , vec : self , } ; let ptr = gap . vec . as_mut_ptr () ; unsafe { while gap . read < len { let read_ptr = ptr . add (gap . read) ; let prev_ptr = ptr . add (gap . write . wrapping_sub (1)) ; if same_bucket (& mut * read_ptr , & mut * prev_ptr) { gap . read += 1 ; ptr :: drop_in_place (read_ptr) ; } else { let write_ptr = ptr . add (gap . write) ; ptr :: copy (read_ptr , write_ptr , 1) ; gap . write += 1 ; gap . read += 1 ; } } gap . vec . set_len (gap . write) ; mem :: forget (gap) ; } } # [doc = " Appends an element to the back of a collection."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` bytes."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1, 2];"] # [doc = " vec.push(3);"] # [doc = " assert_eq!(vec, [1, 2, 3]);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn push (& mut self , value : T) { if self . len == self . buf . capacity () { self . buf . reserve_for_push (self . len) ; } unsafe { let end = self . as_mut_ptr () . add (self . len) ; ptr :: write (end , value) ; self . len += 1 ; } } # [doc = " Appends an element if there is sufficient spare capacity, otherwise an error is returned"] # [doc = " with the element."] # [doc = ""] # [doc = " Unlike [`push`] this method will not reallocate when there's insufficient capacity."] # [doc = " The caller should use [`reserve`] or [`try_reserve`] to ensure that there is enough capacity."] # [doc = ""] # [doc = " [`push`]: Vec::push"] # [doc = " [`reserve`]: Vec::reserve"] # [doc = " [`try_reserve`]: Vec::try_reserve"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " A manual, panic-free alternative to [`FromIterator`]:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::iter::FromIterator;"] # [doc = ""] # [doc = " use allocator_api2::{vec::Vec, collections::TryReserveError};"] # [doc = ""] # [doc = " fn from_iter_fallible<T>(iter: impl Iterator<Item=T>) -> Result<Vec<T>, TryReserveError> {"] # [doc = "     let mut vec = Vec::new();"] # [doc = "     for value in iter {"] # [doc = "         if let Err(value) = vec.push_within_capacity(value) {"] # [doc = "             vec.try_reserve(1)?;"] # [doc = "             // this cannot fail, the previous line either returned or added at least 1 free slot"] # [doc = "             let _ = vec.push_within_capacity(value);"] # [doc = "         }"] # [doc = "     }"] # [doc = "     Ok(vec)"] # [doc = " }"] # [doc = " assert_eq!(from_iter_fallible(0..100), Ok(Vec::from_iter(0..100)));"] # [doc = " ```"] # [inline (always)] pub fn push_within_capacity (& mut self , value : T) -> Result < () , T > { if self . len == self . buf . capacity () { return Err (value) ; } unsafe { let end = self . as_mut_ptr () . add (self . len) ; ptr :: write (end , value) ; self . len += 1 ; } Ok (()) } # [doc = " Removes the last element from a vector and returns it, or [`None`] if it"] # [doc = " is empty."] # [doc = ""] # [doc = " If you'd like to pop the first element, consider using"] # [doc = " [`VecDeque::pop_front`] instead."] # [doc = ""] # [doc = " [`VecDeque::pop_front`]: alloc_crate::collections::VecDeque::pop_front"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1, 2, 3];"] # [doc = " assert_eq!(vec.pop(), Some(3));"] # [doc = " assert_eq!(vec, [1, 2]);"] # [doc = " ```"] # [inline (always)] pub fn pop (& mut self) -> Option < T > { if self . len == 0 { None } else { unsafe { self . len -= 1 ; Some (ptr :: read (self . as_ptr () . add (self . len ()))) } } } # [doc = " Moves all the elements of `other` into `self`, leaving `other` empty."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` bytes."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1, 2, 3];"] # [doc = " let mut vec2 = vec![4, 5, 6];"] # [doc = " vec.append(&mut vec2);"] # [doc = " assert_eq!(vec, [1, 2, 3, 4, 5, 6]);"] # [doc = " assert_eq!(vec2, []);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn append (& mut self , other : & mut Self) { unsafe { self . append_elements (other . as_slice () as _) ; other . set_len (0) ; } } # [doc = " Appends elements to `self` from other buffer."] # [cfg (not (no_global_oom_handling))] # [inline (always)] unsafe fn append_elements (& mut self , other : * const [T]) { let count = unsafe { (& (* other)) . len () } ; self . reserve (count) ; let len = self . len () ; unsafe { ptr :: copy_nonoverlapping (other as * const T , self . as_mut_ptr () . add (len) , count) } ; self . len += count ; } # [doc = " Removes the specified range from the vector in bulk, returning all"] # [doc = " removed elements as an iterator. If the iterator is dropped before"] # [doc = " being fully consumed, it drops the remaining removed elements."] # [doc = ""] # [doc = " The returned iterator keeps a mutable borrow on the vector to optimize"] # [doc = " its implementation."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the starting point is greater than the end point or if"] # [doc = " the end point is greater than the length of the vector."] # [doc = ""] # [doc = " # Leaking"] # [doc = ""] # [doc = " If the returned iterator goes out of scope without being dropped (due to"] # [doc = " [`mem::forget`], for example), the vector may have lost and leaked"] # [doc = " elements arbitrarily, including elements outside the range."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec, vec::Vec};"] # [doc = ""] # [doc = " let mut v = vec![1, 2, 3];"] # [doc = " let u: Vec<_> = v.drain(1..).collect();"] # [doc = " assert_eq!(v, &[1]);"] # [doc = " assert_eq!(u, &[2, 3]);"] # [doc = ""] # [doc = " // A full range clears the vector, like `clear()` does"] # [doc = " v.drain(..);"] # [doc = " assert_eq!(v, &[]);"] # [doc = " ```"] # [inline (always)] pub fn drain < R > (& mut self , range : R) -> Drain < '_ , T , A > where R : RangeBounds < usize > , { let len = self . len () ; let _ = & self . as_slice () [(range . start_bound () . cloned () , range . end_bound () . cloned ())] ; let start = match range . start_bound () { Bound :: Included (& n) => n , Bound :: Excluded (& n) => n + 1 , Bound :: Unbounded => 0 , } ; let end = match range . end_bound () { Bound :: Included (& n) => n + 1 , Bound :: Excluded (& n) => n , Bound :: Unbounded => len , } ; unsafe { self . set_len (start) ; let range_slice = slice :: from_raw_parts (self . as_ptr () . add (start) , end - start) ; Drain { tail_start : end , tail_len : len - end , iter : range_slice . iter () , vec : NonNull :: from (self) , } } } # [doc = " Clears the vector, removing all values."] # [doc = ""] # [doc = " Note that this method has no effect on the allocated capacity"] # [doc = " of the vector."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut v = vec![1, 2, 3];"] # [doc = ""] # [doc = " v.clear();"] # [doc = ""] # [doc = " assert!(v.is_empty());"] # [doc = " ```"] # [inline (always)] pub fn clear (& mut self) { let elems : * mut [T] = self . as_mut_slice () ; unsafe { self . len = 0 ; ptr :: drop_in_place (elems) ; } } # [doc = " Returns the number of elements in the vector, also referred to"] # [doc = " as its 'length'."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let a = vec![1, 2, 3];"] # [doc = " assert_eq!(a.len(), 3);"] # [doc = " ```"] # [inline (always)] pub fn len (& self) -> usize { self . len } # [doc = " Returns `true` if the vector contains no elements."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec::Vec;"] # [doc = ""] # [doc = " let mut v = Vec::new();"] # [doc = " assert!(v.is_empty());"] # [doc = ""] # [doc = " v.push(1);"] # [doc = " assert!(!v.is_empty());"] # [doc = " ```"] # [inline (always)] pub fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Splits the collection into two at the given index."] # [doc = ""] # [doc = " Returns a newly allocated vector containing the elements in the range"] # [doc = " `[at, len)`. After the call, the original vector will be left containing"] # [doc = " the elements `[0, at)` with its previous capacity unchanged."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `at > len`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1, 2, 3];"] # [doc = " let vec2 = vec.split_off(1);"] # [doc = " assert_eq!(vec, [1]);"] # [doc = " assert_eq!(vec2, [2, 3]);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] # [must_use = "use `.truncate()` if you don't need the other half"] pub fn split_off (& mut self , at : usize) -> Self where A : Clone , { # [cold] # [inline (never)] fn assert_failed (at : usize , len : usize) -> ! { panic ! ("`at` split index (is {}) should be <= len (is {})" , at , len) ; } if at > self . len () { assert_failed (at , self . len ()) ; } if at == 0 { return mem :: replace (self , Vec :: with_capacity_in (self . capacity () , self . allocator () . clone ()) ,) ; } let other_len = self . len - at ; let mut other = Vec :: with_capacity_in (other_len , self . allocator () . clone ()) ; unsafe { self . set_len (at) ; other . set_len (other_len) ; ptr :: copy_nonoverlapping (self . as_ptr () . add (at) , other . as_mut_ptr () , other . len ()) ; } other } # [doc = " Resizes the `Vec` in-place so that `len` is equal to `new_len`."] # [doc = ""] # [doc = " If `new_len` is greater than `len`, the `Vec` is extended by the"] # [doc = " difference, with each additional slot filled with the result of"] # [doc = " calling the closure `f`. The return values from `f` will end up"] # [doc = " in the `Vec` in the order they have been generated."] # [doc = ""] # [doc = " If `new_len` is less than `len`, the `Vec` is simply truncated."] # [doc = ""] # [doc = " This method uses a closure to create new values on every push. If"] # [doc = " you'd rather [`Clone`] a given value, use [`Vec::resize`]. If you"] # [doc = " want to use the [`Default`] trait to generate values, you can"] # [doc = " pass [`Default::default`] as the second argument."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![1, 2, 3];"] # [doc = " vec.resize_with(5, Default::default);"] # [doc = " assert_eq!(vec, [1, 2, 3, 0, 0]);"] # [doc = ""] # [doc = " let mut vec = vec![];"] # [doc = " let mut p = 1;"] # [doc = " vec.resize_with(4, || { p *= 2; p });"] # [doc = " assert_eq!(vec, [2, 4, 8, 16]);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn resize_with < F > (& mut self , new_len : usize , f : F) where F : FnMut () -> T , { let len = self . len () ; if new_len > len { self . extend (iter :: repeat_with (f) . take (new_len - len)) ; } else { self . truncate (new_len) ; } } # [doc = " Consumes and leaks the `Vec`, returning a mutable reference to the contents,"] # [doc = " `&'a mut [T]`. Note that the type `T` must outlive the chosen lifetime"] # [doc = " `'a`. If the type has only static references, or none at all, then this"] # [doc = " may be chosen to be `'static`."] # [doc = ""] # [doc = " As of Rust 1.57, this method does not reallocate or shrink the `Vec`,"] # [doc = " so the leaked allocation may include unused capacity that is not part"] # [doc = " of the returned slice."] # [doc = ""] # [doc = " This function is mainly useful for data that lives for the remainder of"] # [doc = " the program's life. Dropping the returned reference will cause a memory"] # [doc = " leak."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Simple usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let x = vec![1, 2, 3];"] # [doc = " let static_ref: &'static mut [usize] = x.leak();"] # [doc = " static_ref[0] += 1;"] # [doc = " assert_eq!(static_ref, &[2, 2, 3]);"] # [doc = " ```"] # [inline (always)] pub fn leak < 'a > (self) -> & 'a mut [T] where A : 'a , { let mut me = ManuallyDrop :: new (self) ; unsafe { slice :: from_raw_parts_mut (me . as_mut_ptr () , me . len) } } # [doc = " Returns the remaining spare capacity of the vector as a slice of"] # [doc = " `MaybeUninit<T>`."] # [doc = ""] # [doc = " The returned slice can be used to fill the vector with data (e.g. by"] # [doc = " reading from a file) before marking the data as initialized using the"] # [doc = " [`set_len`] method."] # [doc = ""] # [doc = " [`set_len`]: Vec::set_len"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec::Vec;"] # [doc = ""] # [doc = " // Allocate vector big enough for 10 elements."] # [doc = " let mut v = Vec::with_capacity(10);"] # [doc = ""] # [doc = " // Fill in the first 3 elements."] # [doc = " let uninit = v.spare_capacity_mut();"] # [doc = " uninit[0].write(0);"] # [doc = " uninit[1].write(1);"] # [doc = " uninit[2].write(2);"] # [doc = ""] # [doc = " // Mark the first 3 elements of the vector as being initialized."] # [doc = " unsafe {"] # [doc = "     v.set_len(3);"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(&v, &[0, 1, 2]);"] # [doc = " ```"] # [inline (always)] pub fn spare_capacity_mut (& mut self) -> & mut [MaybeUninit < T >] { unsafe { slice :: from_raw_parts_mut (self . as_mut_ptr () . add (self . len) as * mut MaybeUninit < T > , self . buf . capacity () - self . len ,) } } # [doc = " Returns vector content as a slice of `T`, along with the remaining spare"] # [doc = " capacity of the vector as a slice of `MaybeUninit<T>`."] # [doc = ""] # [doc = " The returned spare capacity slice can be used to fill the vector with data"] # [doc = " (e.g. by reading from a file) before marking the data as initialized using"] # [doc = " the [`set_len`] method."] # [doc = ""] # [doc = " [`set_len`]: Vec::set_len"] # [doc = ""] # [doc = " Note that this is a low-level API, which should be used with care for"] # [doc = " optimization purposes. If you need to append data to a `Vec`"] # [doc = " you can use [`push`], [`extend`], [`extend_from_slice`],"] # [doc = " [`extend_from_within`], [`insert`], [`append`], [`resize`] or"] # [doc = " [`resize_with`], depending on your exact needs."] # [doc = ""] # [doc = " [`push`]: Vec::push"] # [doc = " [`extend`]: Vec::extend"] # [doc = " [`extend_from_slice`]: Vec::extend_from_slice"] # [doc = " [`extend_from_within`]: Vec::extend_from_within"] # [doc = " [`insert`]: Vec::insert"] # [doc = " [`append`]: Vec::append"] # [doc = " [`resize`]: Vec::resize"] # [doc = " [`resize_with`]: Vec::resize_with"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut v = vec![1, 1, 2];"] # [doc = ""] # [doc = " // Reserve additional space big enough for 10 elements."] # [doc = " v.reserve(10);"] # [doc = ""] # [doc = " let (init, uninit) = v.split_at_spare_mut();"] # [doc = " let sum = init.iter().copied().sum::<u32>();"] # [doc = ""] # [doc = " // Fill in the next 4 elements."] # [doc = " uninit[0].write(sum);"] # [doc = " uninit[1].write(sum * 2);"] # [doc = " uninit[2].write(sum * 3);"] # [doc = " uninit[3].write(sum * 4);"] # [doc = ""] # [doc = " // Mark the 4 elements of the vector as being initialized."] # [doc = " unsafe {"] # [doc = "     let len = v.len();"] # [doc = "     v.set_len(len + 4);"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(&v, &[1, 1, 2, 4, 8, 12, 16]);"] # [doc = " ```"] # [inline (always)] pub fn split_at_spare_mut (& mut self) -> (& mut [T] , & mut [MaybeUninit < T >]) { let (init , spare , _) = unsafe { self . split_at_spare_mut_with_len () } ; (init , spare) } # [doc = " Safety: changing returned .2 (&mut usize) is considered the same as calling `.set_len(_)`."] # [doc = ""] # [doc = " This method provides unique access to all vec parts at once in `extend_from_within`."] unsafe fn split_at_spare_mut_with_len (& mut self ,) -> (& mut [T] , & mut [MaybeUninit < T >] , & mut usize) { let ptr = self . as_mut_ptr () ; let spare_ptr = unsafe { ptr . add (self . len) } ; let spare_ptr = spare_ptr . cast :: < MaybeUninit < T > > () ; let spare_len = self . buf . capacity () - self . len ; unsafe { let initialized = slice :: from_raw_parts_mut (ptr , self . len) ; let spare = slice :: from_raw_parts_mut (spare_ptr , spare_len) ; (initialized , spare , & mut self . len) } } }
    };
}

impl_151!();