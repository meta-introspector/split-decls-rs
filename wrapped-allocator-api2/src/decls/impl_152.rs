macro_rules! deps {
    () => {
        Vec!();
        Allocator!();
        ExtendElement!();
    };
}

macro_rules! impl_152 {
    () => {
        deps!();
        impl < T : Clone , A : Allocator > Vec < T , A > { # [doc = " Resizes the `Vec` in-place so that `len` is equal to `new_len`."] # [doc = ""] # [doc = " If `new_len` is greater than `len`, the `Vec` is extended by the"] # [doc = " difference, with each additional slot filled with `value`."] # [doc = " If `new_len` is less than `len`, the `Vec` is simply truncated."] # [doc = ""] # [doc = " This method requires `T` to implement [`Clone`],"] # [doc = " in order to be able to clone the passed value."] # [doc = " If you need more flexibility (or want to rely on [`Default`] instead of"] # [doc = " [`Clone`]), use [`Vec::resize_with`]."] # [doc = " If you only need to resize to a smaller size, use [`Vec::truncate`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![\"hello\"];"] # [doc = " vec.resize(3, \"world\");"] # [doc = " assert_eq!(vec, [\"hello\", \"world\", \"world\"]);"] # [doc = ""] # [doc = " let mut vec = vec![1, 2, 3, 4];"] # [doc = " vec.resize(2, 0);"] # [doc = " assert_eq!(vec, [1, 2]);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn resize (& mut self , new_len : usize , value : T) { let len = self . len () ; if new_len > len { self . extend_with (new_len - len , ExtendElement (value)) } else { self . truncate (new_len) ; } } # [doc = " Clones and appends all elements in a slice to the `Vec`."] # [doc = ""] # [doc = " Iterates over the slice `other`, clones each element, and then appends"] # [doc = " it to this `Vec`. The `other` slice is traversed in-order."] # [doc = ""] # [doc = " Note that this function is same as [`extend`] except that it is"] # [doc = " specialized to work with slices instead. If and when Rust gets"] # [doc = " specialization this function will likely be deprecated (but still"] # [doc = " available)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut vec = vec![1];"] # [doc = " vec.extend_from_slice(&[2, 3, 4]);"] # [doc = " assert_eq!(vec, [1, 2, 3, 4]);"] # [doc = " ```"] # [doc = ""] # [doc = " [`extend`]: Vec::extend"] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn extend_from_slice (& mut self , other : & [T]) { self . extend (other . iter () . cloned ()) } # [doc = " Copies elements from `src` range to the end of the vector."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the starting point is greater than the end point or if"] # [doc = " the end point is greater than the length of the vector."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec![0, 1, 2, 3, 4];"] # [doc = ""] # [doc = " vec.extend_from_within(2..);"] # [doc = " assert_eq!(vec, [0, 1, 2, 3, 4, 2, 3, 4]);"] # [doc = ""] # [doc = " vec.extend_from_within(..2);"] # [doc = " assert_eq!(vec, [0, 1, 2, 3, 4, 2, 3, 4, 0, 1]);"] # [doc = ""] # [doc = " vec.extend_from_within(4..8);"] # [doc = " assert_eq!(vec, [0, 1, 2, 3, 4, 2, 3, 4, 0, 1, 4, 2, 3, 4]);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn extend_from_within < R > (& mut self , src : R) where R : RangeBounds < usize > , { let _ = & self . as_slice () [(src . start_bound () . cloned () , src . end_bound () . cloned ())] ; let len = self . len () ; let start : ops :: Bound < & usize > = src . start_bound () ; let start = match start { ops :: Bound :: Included (& start) => start , ops :: Bound :: Excluded (start) => start + 1 , ops :: Bound :: Unbounded => 0 , } ; let end : ops :: Bound < & usize > = src . end_bound () ; let end = match end { ops :: Bound :: Included (end) => end + 1 , ops :: Bound :: Excluded (& end) => end , ops :: Bound :: Unbounded => len , } ; let range = start .. end ; self . reserve (range . len ()) ; let (this , spare , len) = unsafe { self . split_at_spare_mut_with_len () } ; let to_clone = unsafe { this . get_unchecked (range) } ; iter :: zip (to_clone , spare) . map (| (src , dst) | dst . write (src . clone ())) . for_each (| _ | * len += 1) ; } }
    };
}

impl_152!();