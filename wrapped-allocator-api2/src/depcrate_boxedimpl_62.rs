// Generated macro for impl_62 (impl)
macro_rules! Depcrate_boxedimpl_62 {
() => {
// Module: crate::boxed
// Provides: {"impl_62"}
// Dependencies: {}
impl < T : ? Sized , A : Allocator > Box < T , A > { # [doc = " Constructs a box from a raw pointer in the given allocator."] # [doc = ""] # [doc = " After calling this function, the raw pointer is owned by the"] # [doc = " resulting `Box`. Specifically, the `Box` destructor will call"] # [doc = " the destructor of `T` and free the allocated memory. For this"] # [doc = " to be safe, the memory must have been allocated in accordance"] # [doc = " with the [memory layout] used by `Box` ."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe because improper use may lead to"] # [doc = " memory problems. For example, a double-free may occur if the"] # [doc = " function is called twice on the same raw pointer."] # [doc = ""] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Recreate a `Box` which was previously converted to a raw pointer"] # [doc = " using [`Box::into_raw_with_allocator`]:"] # [doc = " ```"] # [doc = " use allocator_api2::{boxed::Box, alloc::System};"] # [doc = ""] # [doc = " let x = Box::new_in(5, System);"] # [doc = " let (ptr, alloc) = Box::into_raw_with_allocator(x);"] # [doc = " let x = unsafe { Box::from_raw_in(ptr, alloc) };"] # [doc = " ```"] # [doc = " Manually create a `Box` from scratch by using the system allocator:"] # [doc = " ```"] # [doc = " use allocator_api2::{boxed::Box, alloc::{Allocator, Layout, System}};"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     let ptr = System.allocate(Layout::new::<i32>())?.as_ptr().cast::<i32>();"] # [doc = "     // In general .write is required to avoid attempting to destruct"] # [doc = "     // the (uninitialized) previous contents of `ptr`, though for this"] # [doc = "     // simple example `*ptr = 5` would have worked as well."] # [doc = "     ptr.write(5);"] # [doc = "     let x = Box::from_raw_in(ptr, System);"] # [doc = " }"] # [doc = " # Ok::<(), allocator_api2::alloc::AllocError>(())"] # [doc = " ```"] # [doc = ""] # [doc = " [memory layout]: self#memory-layout"] # [doc = " [`Layout`]: crate::Layout"] # [inline (always)] pub const unsafe fn from_raw_in (raw : * mut T , alloc : A) -> Self { Box (unsafe { Unique :: new_unchecked (raw) } , alloc) } # [doc = " Constructs a box from a `NonNull` pointer in the given allocator."] # [doc = ""] # [doc = " After calling this function, the `NonNull` pointer is owned by"] # [doc = " the resulting `Box`. Specifically, the `Box` destructor will call"] # [doc = " the destructor of `T` and free the allocated memory. For this"] # [doc = " to be safe, the memory must have been allocated in accordance"] # [doc = " with the [memory layout] used by `Box` ."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe because improper use may lead to"] # [doc = " memory problems. For example, a double-free may occur if the"] # [doc = " function is called twice on the same raw pointer."] # [doc = ""] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Recreate a `Box` which was previously converted to a `NonNull` pointer"] # [doc = " using [`Box::into_non_null_with_allocator`]:"] # [doc = " ```"] # [doc = " use allocator_api2::{boxed::Box, alloc::System};"] # [doc = ""] # [doc = " let x = Box::new_in(5, System);"] # [doc = " let (non_null, alloc) = Box::into_non_null_with_allocator(x);"] # [doc = " let x = unsafe { Box::from_non_null_in(non_null, alloc) };"] # [doc = " ```"] # [doc = " Manually create a `Box` from scratch by using the system allocator:"] # [doc = " ```"] # [doc = " use allocator_api2::{boxed::Box, alloc::{Allocator, Layout, System}};"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     let non_null = System.allocate(Layout::new::<i32>())?.cast::<i32>();"] # [doc = "     // In general .write is required to avoid attempting to destruct"] # [doc = "     // the (uninitialized) previous contents of `non_null`."] # [doc = "     non_null.write(5);"] # [doc = "     let x = Box::from_non_null_in(non_null, System);"] # [doc = " }"] # [doc = " # Ok::<(), allocator_api2::alloc::AllocError>(())"] # [doc = " ```"] # [doc = ""] # [doc = " [memory layout]: self#memory-layout"] # [doc = " [`Layout`]: crate::Layout"] # [inline (always)] pub const unsafe fn from_non_null_in (raw : NonNull < T > , alloc : A) -> Self { unsafe { Box :: from_raw_in (raw . as_ptr () , alloc) } } # [doc = " Consumes the `Box`, returning a wrapped raw pointer."] # [doc = ""] # [doc = " The pointer will be properly aligned and non-null."] # [doc = ""] # [doc = " After calling this function, the caller is responsible for the"] # [doc = " memory previously managed by the `Box`. In particular, the"] # [doc = " caller should properly destroy `T` and release the memory, taking"] # [doc = " into account the [memory layout] used by `Box`. The easiest way to"] # [doc = " do this is to convert the raw pointer back into a `Box` with the"] # [doc = " [`Box::from_raw`] function, allowing the `Box` destructor to perform"] # [doc = " the cleanup."] # [doc = ""] # [doc = " Note: this is an associated function, which means that you have"] # [doc = " to call it as `Box::into_raw(b)` instead of `b.into_raw()`. This"] # [doc = " is so that there is no conflict with a method on the inner type."] # [doc = ""] # [doc = " # Examples"] # [doc = " Converting the raw pointer back into a `Box` with [`Box::from_raw`]"] # [doc = " for automatic cleanup:"] # [doc = " ```"] # [doc = " use allocator_api2::boxed::Box;"] # [doc = ""] # [doc = " let x = Box::new(String::from(\"Hello\"));"] # [doc = " let ptr = Box::into_raw(x);"] # [doc = " let x = unsafe { Box::from_raw(ptr) };"] # [doc = " ```"] # [doc = " Manual cleanup by explicitly running the destructor and deallocating"] # [doc = " the memory:"] # [doc = " ```"] # [doc = " use std::ptr;"] # [doc = ""] # [doc = " use allocator_api2::{boxed::Box, alloc::{dealloc, Layout}};"] # [doc = ""] # [doc = " let x = Box::new(String::from(\"Hello\"));"] # [doc = " let p = Box::into_raw(x);"] # [doc = " unsafe {"] # [doc = "     ptr::drop_in_place(p);"] # [doc = "     dealloc(p as *mut u8, Layout::new::<String>());"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [memory layout]: self#memory-layout"] # [inline (always)] pub fn into_raw (b : Self) -> * mut T { Self :: into_raw_with_allocator (b) . 0 } # [doc = " Consumes the `Box`, returning a wrapped `NonNull` pointer."] # [doc = ""] # [doc = " The pointer will be properly aligned."] # [doc = ""] # [doc = " After calling this function, the caller is responsible for the"] # [doc = " memory previously managed by the `Box`. In particular, the"] # [doc = " caller should properly destroy `T` and release the memory, taking"] # [doc = " into account the [memory layout] used by `Box`. The easiest way to"] # [doc = " do this is to convert the `NonNull` pointer back into a `Box` with the"] # [doc = " [`Box::from_non_null`] function, allowing the `Box` destructor to"] # [doc = " perform the cleanup."] # [doc = ""] # [doc = " Note: this is an associated function, which means that you have"] # [doc = " to call it as `Box::into_non_null(b)` instead of `b.into_non_null()`."] # [doc = " This is so that there is no conflict with a method on the inner type."] # [doc = ""] # [doc = " # Examples"] # [doc = " Converting the `NonNull` pointer back into a `Box` with [`Box::from_non_null`]"] # [doc = " for automatic cleanup:"] # [doc = " ```"] # [doc = " use allocator_api2::boxed::Box;"] # [doc = ""] # [doc = " let x = Box::new(String::from(\"Hello\"));"] # [doc = " let non_null = Box::into_non_null(x);"] # [doc = " let x = unsafe { Box::from_non_null(non_null) };"] # [doc = " ```"] # [doc = " Manual cleanup by explicitly running the destructor and deallocating"] # [doc = " the memory:"] # [doc = " ```"] # [doc = " use allocator_api2::{boxed::Box, alloc::{dealloc, Layout}};"] # [doc = ""] # [doc = " let x = Box::new(String::from(\"Hello\"));"] # [doc = " let non_null = Box::into_non_null(x);"] # [doc = " unsafe {"] # [doc = "     non_null.drop_in_place();"] # [doc = "     dealloc(non_null.as_ptr().cast::<u8>(), Layout::new::<String>());"] # [doc = " }"] # [doc = " ```"] # [doc = " Note: This is equivalent to the following:"] # [doc = " ```"] # [doc = " use allocator_api2::boxed::Box;"] # [doc = ""] # [doc = " let x = Box::new(String::from(\"Hello\"));"] # [doc = " let non_null = Box::into_non_null(x);"] # [doc = " unsafe {"] # [doc = "     drop(Box::from_non_null(non_null));"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [memory layout]: self#memory-layout"] # [must_use = "losing the pointer will leak memory"] # [inline (always)] pub fn into_non_null (b : Self) -> NonNull < T > { unsafe { NonNull :: new_unchecked (Self :: into_raw (b)) } } # [doc = " Consumes the `Box`, returning a wrapped raw pointer and the allocator."] # [doc = ""] # [doc = " The pointer will be properly aligned and non-null."] # [doc = ""] # [doc = " After calling this function, the caller is responsible for the"] # [doc = " memory previously managed by the `Box`. In particular, the"] # [doc = " caller should properly destroy `T` and release the memory, taking"] # [doc = " into account the [memory layout] used by `Box`. The easiest way to"] # [doc = " do this is to convert the raw pointer back into a `Box` with the"] # [doc = " [`Box::from_raw_in`] function, allowing the `Box` destructor to perform"] # [doc = " the cleanup."] # [doc = ""] # [doc = " Note: this is an associated function, which means that you have"] # [doc = " to call it as `Box::into_raw_with_allocator(b)` instead of `b.into_raw_with_allocator()`. This"] # [doc = " is so that there is no conflict with a method on the inner type."] # [doc = ""] # [doc = " # Examples"] # [doc = " Converting the raw pointer back into a `Box` with [`Box::from_raw_in`]"] # [doc = " for automatic cleanup:"] # [doc = " ```"] # [doc = " use allocator_api2::{boxed::Box, alloc::System};"] # [doc = ""] # [doc = " let x = Box::new_in(String::from(\"Hello\"), System);"] # [doc = " let (ptr, alloc) = Box::into_raw_with_allocator(x);"] # [doc = " let x = unsafe { Box::from_raw_in(ptr, alloc) };"] # [doc = " ```"] # [doc = " Manual cleanup by explicitly running the destructor and deallocating"] # [doc = " the memory:"] # [doc = " ```"] # [doc = " use std::ptr::{self, NonNull};"] # [doc = ""] # [doc = " use allocator_api2::{boxed::Box, alloc::{Allocator, Layout, System}};"] # [doc = ""] # [doc = " let x = Box::new_in(String::from(\"Hello\"), System);"] # [doc = " let (ptr, alloc) = Box::into_raw_with_allocator(x);"] # [doc = " unsafe {"] # [doc = "     ptr::drop_in_place(ptr);"] # [doc = "     let non_null = NonNull::new_unchecked(ptr);"] # [doc = "     alloc.deallocate(non_null.cast(), Layout::new::<String>());"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [memory layout]: self#memory-layout"] # [inline (always)] pub fn into_raw_with_allocator (b : Self) -> (* mut T , A) { let (leaked , alloc) = Box :: into_non_null_with_allocator (b) ; (leaked . as_ptr () , alloc) } # [doc = " Consumes the `Box`, returning a wrapped `NonNull` pointer and the allocator."] # [doc = ""] # [doc = " The pointer will be properly aligned."] # [doc = ""] # [doc = " After calling this function, the caller is responsible for the"] # [doc = " memory previously managed by the `Box`. In particular, the"] # [doc = " caller should properly destroy `T` and release the memory, taking"] # [doc = " into account the [memory layout] used by `Box`. The easiest way to"] # [doc = " do this is to convert the `NonNull` pointer back into a `Box` with the"] # [doc = " [`Box::from_non_null_in`] function, allowing the `Box` destructor to"] # [doc = " perform the cleanup."] # [doc = ""] # [doc = " Note: this is an associated function, which means that you have"] # [doc = " to call it as `Box::into_non_null_with_allocator(b)` instead of"] # [doc = " `b.into_non_null_with_allocator()`. This is so that there is no"] # [doc = " conflict with a method on the inner type."] # [doc = ""] # [doc = " # Examples"] # [doc = " Converting the `NonNull` pointer back into a `Box` with"] # [doc = " [`Box::from_non_null_in`] for automatic cleanup:"] # [doc = " ```"] # [doc = " use allocator_api2::{boxed::Box, alloc::System};"] # [doc = ""] # [doc = " let x = Box::new_in(String::from(\"Hello\"), System);"] # [doc = " let (non_null, alloc) = Box::into_non_null_with_allocator(x);"] # [doc = " let x = unsafe { Box::from_non_null_in(non_null, alloc) };"] # [doc = " ```"] # [doc = " Manual cleanup by explicitly running the destructor and deallocating"] # [doc = " the memory:"] # [doc = " ```"] # [doc = " use allocator_api2::{boxed::Box, alloc::{Allocator, Layout, System}};"] # [doc = ""] # [doc = " let x = Box::new_in(String::from(\"Hello\"), System);"] # [doc = " let (non_null, alloc) = Box::into_non_null_with_allocator(x);"] # [doc = " unsafe {"] # [doc = "     non_null.drop_in_place();"] # [doc = "     alloc.deallocate(non_null.cast::<u8>(), Layout::new::<String>());"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [memory layout]: self#memory-layout"] # [inline (always)] pub fn into_non_null_with_allocator (b : Self) -> (NonNull < T > , A) { let alloc = unsafe { ptr :: read (& b . 1) } ; (NonNull :: from (Box :: leak (b)) , alloc) } # [doc = " Returns a raw mutable pointer to the `Box`'s contents."] # [doc = ""] # [doc = " The caller must ensure that the `Box` outlives the pointer this"] # [doc = " function returns, or else it will end up dangling."] # [doc = ""] # [doc = " This method guarantees that for the purpose of the aliasing model, this method"] # [doc = " does not materialize a reference to the underlying memory, and thus the returned pointer"] # [doc = " will remain valid when mixed with other calls to [`as_ptr`] and [`as_mut_ptr`]."] # [doc = " Note that calling other methods that materialize references to the memory"] # [doc = " may still invalidate this pointer."] # [doc = " See the example below for how this guarantee can be used."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Due to the aliasing guarantee, the following code is legal:"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::boxed::Box;"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     let mut b = Box::new(0);"] # [doc = "     let ptr1 = Box::as_mut_ptr(&mut b);"] # [doc = "     ptr1.write(1);"] # [doc = "     let ptr2 = Box::as_mut_ptr(&mut b);"] # [doc = "     ptr2.write(2);"] # [doc = "     // Notably, the write to `ptr2` did *not* invalidate `ptr1`:"] # [doc = "     ptr1.write(3);"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [`as_mut_ptr`]: Self::as_mut_ptr"] # [doc = " [`as_ptr`]: Self::as_ptr"] # [inline (always)] pub fn as_mut_ptr (b : & mut Self) -> * mut T { b . 0 . as_ptr () } # [doc = " Returns a raw pointer to the `Box`'s contents."] # [doc = ""] # [doc = " The caller must ensure that the `Box` outlives the pointer this"] # [doc = " function returns, or else it will end up dangling."] # [doc = ""] # [doc = " The caller must also ensure that the memory the pointer (non-transitively) points to"] # [doc = " is never written to (except inside an `UnsafeCell`) using this pointer or any pointer"] # [doc = " derived from it. If you need to mutate the contents of the `Box`, use [`as_mut_ptr`]."] # [doc = ""] # [doc = " This method guarantees that for the purpose of the aliasing model, this method"] # [doc = " does not materialize a reference to the underlying memory, and thus the returned pointer"] # [doc = " will remain valid when mixed with other calls to [`as_ptr`] and [`as_mut_ptr`]."] # [doc = " Note that calling other methods that materialize mutable references to the memory,"] # [doc = " as well as writing to this memory, may still invalidate this pointer."] # [doc = " See the example below for how this guarantee can be used."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Due to the aliasing guarantee, the following code is legal:"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::boxed::Box;"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     let mut v = Box::new(0);"] # [doc = "     let ptr1 = Box::as_ptr(&v);"] # [doc = "     let ptr2 = Box::as_mut_ptr(&mut v);"] # [doc = "     let _val = ptr2.read();"] # [doc = "     // No write to this memory has happened yet, so `ptr1` is still valid."] # [doc = "     let _val = ptr1.read();"] # [doc = "     // However, once we do a write..."] # [doc = "     ptr2.write(1);"] # [doc = "     // ... `ptr1` is no longer valid."] # [doc = "     // This would be UB: let _val = ptr1.read();"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [`as_mut_ptr`]: Self::as_mut_ptr"] # [doc = " [`as_ptr`]: Self::as_ptr"] # [inline (always)] pub fn as_ptr (b : & Self) -> * const T { b . 0 . as_ptr () } # [doc = " Returns a reference to the underlying allocator."] # [doc = ""] # [doc = " Note: this is an associated function, which means that you have"] # [doc = " to call it as `Box::allocator(&b)` instead of `b.allocator()`. This"] # [doc = " is so that there is no conflict with a method on the inner type."] # [inline (always)] pub const fn allocator (b : & Self) -> & A { & b . 1 } # [doc = " Consumes and leaks the `Box`, returning a mutable reference,"] # [doc = " `&'a mut T`. Note that the type `T` must outlive the chosen lifetime"] # [doc = " `'a`. If the type has only static references, or none at all, then this"] # [doc = " may be chosen to be `'static`."] # [doc = ""] # [doc = " This function is mainly useful for data that lives for the remainder of"] # [doc = " the program's life. Dropping the returned reference will cause a memory"] # [doc = " leak. If this is not acceptable, the reference should first be wrapped"] # [doc = " with the [`Box::from_raw`] function producing a `Box`. This `Box` can"] # [doc = " then be dropped which will properly destroy `T` and release the"] # [doc = " allocated memory."] # [doc = ""] # [doc = " Note: this is an associated function, which means that you have"] # [doc = " to call it as `Box::leak(b)` instead of `b.leak()`. This"] # [doc = " is so that there is no conflict with a method on the inner type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Simple usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::boxed::Box;"] # [doc = ""] # [doc = " let x = Box::new(41);"] # [doc = " let static_ref: &'static mut usize = Box::leak(x);"] # [doc = " *static_ref += 1;"] # [doc = " assert_eq!(*static_ref, 42);"] # [doc = " ```"] # [doc = ""] # [doc = " Unsized data:"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec, boxed::Box};"] # [doc = ""] # [doc = " let x = vec![1, 2, 3].into_boxed_slice();"] # [doc = " let static_ref = Box::leak(x);"] # [doc = " static_ref[0] = 4;"] # [doc = " assert_eq!(*static_ref, [4, 2, 3]);"] # [doc = " ```"] # [inline (always)] pub fn leak < 'a > (b : Self) -> & 'a mut T where A : 'a , { unsafe { & mut * mem :: ManuallyDrop :: new (b) . 0 . as_ptr () } } # [doc = " Converts a `Box<T>` into a `Pin<Box<T>>`. If `T` does not implement [`Unpin`], then"] # [doc = " `*boxed` will be pinned in memory and unable to be moved."] # [doc = ""] # [doc = " This conversion does not allocate on the heap and happens in place."] # [doc = ""] # [doc = " This is also available via [`From`]."] # [doc = ""] # [doc = " Constructing and pinning a `Box` with <code>Box::into_pin([Box::new]\\(x))</code>"] # [doc = " can also be written more concisely using <code>[Box::pin]\\(x)</code>."] # [doc = " This `into_pin` method is useful if you already have a `Box<T>`, or you are"] # [doc = " constructing a (pinned) `Box` in a different way than with [`Box::new`]."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " It's not recommended that crates add an impl like `From<Box<T>> for Pin<T>`,"] # [doc = " as it'll introduce an ambiguity when calling `Pin::from`."] # [doc = " A demonstration of such a poor impl is shown below."] # [doc = ""] # [doc = " ```compile_fail"] # [doc = " # use std::pin::Pin;"] # [doc = " use allocator_api2::boxed::Box;"] # [doc = ""] # [doc = " struct Foo; // A type defined in this crate."] # [doc = " impl From<Box<()>> for Pin<Foo> {"] # [doc = "     fn from(_: Box<()>) -> Pin<Foo> {"] # [doc = "         Pin::new(Foo)"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " let foo = Box::new(());"] # [doc = " let bar = Pin::from(foo);"] # [doc = " ```"] # [inline (always)] pub fn into_pin (boxed : Self) -> Pin < Self > where A : 'static , { unsafe { Pin :: new_unchecked (boxed) } } }
};
}
