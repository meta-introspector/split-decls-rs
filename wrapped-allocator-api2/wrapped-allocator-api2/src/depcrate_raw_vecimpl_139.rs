// Generated macro for impl_139 (impl)
macro_rules! Depcrate_raw_vecimpl_139 {
() => {
// Module: crate::raw_vec
// Provides: {"impl_139"}
// Dependencies: {}
impl < T , A : Allocator > RawVec < T , A > { pub (crate) const MIN_NON_ZERO_CAP : usize = if mem :: size_of :: < T > () == 1 { 8 } else if mem :: size_of :: < T > () <= 1024 { 4 } else { 1 } ; # [doc = " Like `new`, but parameterized over the choice of allocator for"] # [doc = " the returned `RawVec`."] # [inline (always)] pub const fn new_in (alloc : A) -> Self { Self { ptr : NonNull :: dangling () , cap : 0 , alloc , } } # [doc = " Like `with_capacity`, but parameterized over the choice of"] # [doc = " allocator for the returned `RawVec`."] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn with_capacity_in (capacity : usize , alloc : A) -> Self { Self :: allocate_in (capacity , AllocInit :: Uninitialized , alloc) } # [doc = " Like `with_capacity_zeroed`, but parameterized over the choice"] # [doc = " of allocator for the returned `RawVec`."] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn with_capacity_zeroed_in (capacity : usize , alloc : A) -> Self { Self :: allocate_in (capacity , AllocInit :: Zeroed , alloc) } # [doc = " Converts the entire buffer into `Box<[MaybeUninit<T>]>` with the specified `len`."] # [doc = ""] # [doc = " Note that this will correctly reconstitute any `cap` changes"] # [doc = " that may have been performed. (See description of type for details.)"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " * `len` must be greater than or equal to the most recently requested capacity, and"] # [doc = " * `len` must be less than or equal to `self.capacity()`."] # [doc = ""] # [doc = " Note, that the requested capacity and `self.capacity()` could differ, as"] # [doc = " an allocator could overallocate and return a greater memory block than requested."] # [inline (always)] pub unsafe fn into_box (self , len : usize) -> Box < [MaybeUninit < T >] , A > { debug_assert ! (len <= self . capacity () , "`len` must be smaller than or equal to `self.capacity()`") ; let me = ManuallyDrop :: new (self) ; unsafe { let slice = slice :: from_raw_parts_mut (me . ptr () as * mut MaybeUninit < T > , len) ; Box :: < [MaybeUninit < T >] , A > :: from_raw_in (slice , ptr :: read (& me . alloc)) } } # [cfg (not (no_global_oom_handling))] # [inline (always)] fn allocate_in (capacity : usize , init : AllocInit , alloc : A) -> Self { if mem :: size_of :: < T > () == 0 || capacity == 0 { Self :: new_in (alloc) } else { let layout = match Layout :: array :: < T > (capacity) { Ok (layout) => layout , Err (_) => capacity_overflow () , } ; match alloc_guard (layout . size ()) { Ok (_) => { } Err (_) => capacity_overflow () , } let result = match init { AllocInit :: Uninitialized => alloc . allocate (layout) , AllocInit :: Zeroed => alloc . allocate_zeroed (layout) , } ; let ptr = match result { Ok (ptr) => ptr , Err (_) => handle_alloc_error (layout) , } ; Self { ptr : unsafe { NonNull :: new_unchecked (ptr . cast () . as_ptr ()) } , cap : capacity , alloc , } } } # [doc = " Reconstitutes a `RawVec` from a pointer, capacity, and allocator."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The `ptr` must be allocated (via the given allocator `alloc`), and with the given"] # [doc = " `capacity`."] # [doc = " The `capacity` cannot exceed `isize::MAX` for sized types. (only a concern on 32-bit"] # [doc = " systems). ZST vectors may have a capacity up to `usize::MAX`."] # [doc = " If the `ptr` and `capacity` come from a `RawVec` created via `alloc`, then this is"] # [doc = " guaranteed."] # [inline (always)] pub unsafe fn from_raw_parts_in (ptr : * mut T , capacity : usize , alloc : A) -> Self { Self { ptr : unsafe { NonNull :: new_unchecked (ptr) } , cap : capacity , alloc , } } # [doc = " Gets a raw pointer to the start of the allocation. Note that this is"] # [doc = " `NonNull::dangling()` if `capacity == 0` or `T` is zero-sized. In the former case, you must"] # [doc = " be careful."] # [inline (always)] pub fn ptr (& self) -> * mut T { self . ptr . as_ptr () } # [doc = " Gets the capacity of the allocation."] # [doc = ""] # [doc = " This will always be `usize::MAX` if `T` is zero-sized."] # [inline (always)] pub fn capacity (& self) -> usize { if mem :: size_of :: < T > () == 0 { usize :: MAX } else { self . cap } } # [doc = " Returns a shared reference to the allocator backing this `RawVec`."] # [inline (always)] pub fn allocator (& self) -> & A { & self . alloc } # [inline (always)] fn current_memory (& self) -> Option < (NonNull < u8 > , Layout) > { if mem :: size_of :: < T > () == 0 || self . cap == 0 { None } else { unsafe { let layout = Layout :: array :: < T > (self . cap) . unwrap_unchecked () ; Some ((self . ptr . cast () , layout)) } } } # [doc = " Ensures that the buffer contains at least enough space to hold `len +"] # [doc = " additional` elements. If it doesn't already have enough capacity, will"] # [doc = " reallocate enough space plus comfortable slack space to get amortized"] # [doc = " *O*(1) behavior. Will limit this behavior if it would needlessly cause"] # [doc = " itself to panic."] # [doc = ""] # [doc = " If `len` exceeds `self.capacity()`, this may fail to actually allocate"] # [doc = " the requested space. This is not really unsafe, but the unsafe"] # [doc = " code *you* write that relies on the behavior of this function may break."] # [doc = ""] # [doc = " This is ideal for implementing a bulk-push operation like `extend`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` bytes."] # [doc = ""] # [doc = " # Aborts"] # [doc = ""] # [doc = " Aborts on OOM."] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn reserve (& mut self , len : usize , additional : usize) { # [cold] # [inline (always)] fn do_reserve_and_handle < T , A : Allocator > (slf : & mut RawVec < T , A > , len : usize , additional : usize ,) { handle_reserve (slf . grow_amortized (len , additional)) ; } if self . needs_to_grow (len , additional) { do_reserve_and_handle (self , len , additional) ; } } # [doc = " A specialized version of `reserve()` used only by the hot and"] # [doc = " oft-instantiated `Vec::push()`, which does its own capacity check."] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn reserve_for_push (& mut self , len : usize) { handle_reserve (self . grow_amortized (len , 1)) ; } # [doc = " The same as `reserve`, but returns on errors instead of panicking or aborting."] # [inline (always)] pub fn try_reserve (& mut self , len : usize , additional : usize) -> Result < () , TryReserveError > { if self . needs_to_grow (len , additional) { self . grow_amortized (len , additional) } else { Ok (()) } } # [doc = " Ensures that the buffer contains at least enough space to hold `len +"] # [doc = " additional` elements. If it doesn't already, will reallocate the"] # [doc = " minimum possible amount of memory necessary. Generally this will be"] # [doc = " exactly the amount of memory necessary, but in principle the allocator"] # [doc = " is free to give back more than we asked for."] # [doc = ""] # [doc = " If `len` exceeds `self.capacity()`, this may fail to actually allocate"] # [doc = " the requested space. This is not really unsafe, but the unsafe code"] # [doc = " *you* write that relies on the behavior of this function may break."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` bytes."] # [doc = ""] # [doc = " # Aborts"] # [doc = ""] # [doc = " Aborts on OOM."] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn reserve_exact (& mut self , len : usize , additional : usize) { handle_reserve (self . try_reserve_exact (len , additional)) ; } # [doc = " The same as `reserve_exact`, but returns on errors instead of panicking or aborting."] # [inline (always)] pub fn try_reserve_exact (& mut self , len : usize , additional : usize ,) -> Result < () , TryReserveError > { if self . needs_to_grow (len , additional) { self . grow_exact (len , additional) } else { Ok (()) } } # [doc = " Shrinks the buffer down to the specified capacity. If the given amount"] # [doc = " is 0, actually completely deallocates."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the given amount is *larger* than the current capacity."] # [doc = ""] # [doc = " # Aborts"] # [doc = ""] # [doc = " Aborts on OOM."] # [cfg (not (no_global_oom_handling))] # [inline (always)] pub fn shrink_to_fit (& mut self , cap : usize) { handle_reserve (self . shrink (cap)) ; } }
};
}
