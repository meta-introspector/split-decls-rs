// Generated macro for impl_237 (impl)
macro_rules! Depcrate_vecimpl_237 {
() => {
// Module: crate::vec
// Provides: {"impl_237"}
// Dependencies: {}
impl < T > Vec < T > { # [doc = " Constructs a new, empty `Vec<T>`."] # [doc = ""] # [doc = " The vector will not allocate until elements are pushed onto it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec, vec::Vec};"] # [doc = ""] # [doc = " let mut vec: Vec<i32> = Vec::new();"] # [doc = " ```"] # [inline (always)] # [must_use] pub const fn new () -> Self { Vec { buf : RawVec :: new () , len : 0 , } } # [doc = " Constructs a new, empty `Vec<T>` with at least the specified capacity."] # [doc = ""] # [doc = " The vector will be able to hold at least `capacity` elements without"] # [doc = " reallocating. This method is allowed to allocate for more elements than"] # [doc = " `capacity`. If `capacity` is 0, the vector will not allocate."] # [doc = ""] # [doc = " It is important to note that although the returned vector has the"] # [doc = " minimum *capacity* specified, the vector will have a zero *length*. For"] # [doc = " an explanation of the difference between length and capacity, see"] # [doc = " *[Capacity and reallocation]*."] # [doc = ""] # [doc = " If it is important to know the exact allocated capacity of a `Vec`,"] # [doc = " always use the [`capacity`] method after construction."] # [doc = ""] # [doc = " For `Vec<T>` where `T` is a zero-sized type, there will be no allocation"] # [doc = " and the capacity will always be `usize::MAX`."] # [doc = ""] # [doc = " [Capacity and reallocation]: #capacity-and-reallocation"] # [doc = " [`capacity`]: Vec::capacity"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` bytes."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec::Vec;"] # [doc = ""] # [doc = " let mut vec = Vec::with_capacity(10);"] # [doc = ""] # [doc = " // The vector contains no items, even though it has capacity for more"] # [doc = " assert_eq!(vec.len(), 0);"] # [doc = " assert!(vec.capacity() >= 10);"] # [doc = ""] # [doc = " // These are all done without reallocating..."] # [doc = " for i in 0..10 {"] # [doc = "     vec.push(i);"] # [doc = " }"] # [doc = " assert_eq!(vec.len(), 10);"] # [doc = " assert!(vec.capacity() >= 10);"] # [doc = ""] # [doc = " // ...but this may make the vector reallocate"] # [doc = " vec.push(11);"] # [doc = " assert_eq!(vec.len(), 11);"] # [doc = " assert!(vec.capacity() >= 11);"] # [doc = ""] # [doc = " // A vector of a zero-sized type will always over-allocate, since no"] # [doc = " // allocation is necessary"] # [doc = " let vec_units = Vec::<()>::with_capacity(10);"] # [doc = " assert_eq!(vec_units.capacity(), usize::MAX);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline (always)] # [must_use] pub fn with_capacity (capacity : usize) -> Self { Self :: with_capacity_in (capacity , Global) } # [doc = " Creates a `Vec<T>` directly from a pointer, a capacity, and a length."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This is highly unsafe, due to the number of invariants that aren't"] # [doc = " checked:"] # [doc = ""] # [doc = " * `T` needs to have the same alignment as what `ptr` was allocated with."] # [doc = "   (`T` having a less strict alignment is not sufficient, the alignment really"] # [doc = "   needs to be equal to satisfy the [`dealloc`] requirement that memory must be"] # [doc = "   allocated and deallocated with the same layout.)"] # [doc = " * The size of `T` times the `capacity` (ie. the allocated size in bytes) needs"] # [doc = "   to be the same size as the pointer was allocated with. (Because similar to"] # [doc = "   alignment, [`dealloc`] must be called with the same layout `size`.)"] # [doc = " * `length` needs to be less than or equal to `capacity`."] # [doc = " * The first `length` values must be properly initialized values of type `T`."] # [doc = " * `capacity` needs to be the capacity that the pointer was allocated with."] # [doc = " * The allocated size in bytes must be no larger than `isize::MAX`."] # [doc = "   See the safety documentation of [`pointer::offset`](https://doc.rust-lang.org/nightly/std/primitive.pointer.html#method.offset)."] # [doc = ""] # [doc = " These requirements are always upheld by any `ptr` that has been allocated"] # [doc = " via `Vec<T>`. Other allocation sources are allowed if the invariants are"] # [doc = " upheld."] # [doc = ""] # [doc = " Violating these may cause problems like corrupting the allocator's"] # [doc = " internal data structures. For example it is normally **not** safe"] # [doc = " to build a `Vec<u8>` from a pointer to a C `char` array with length"] # [doc = " `size_t`, doing so is only safe if the array was initially allocated by"] # [doc = " a `Vec` or `String`."] # [doc = " It's also not safe to build one from a `Vec<u16>` and its length, because"] # [doc = " the allocator cares about the alignment, and these two types have different"] # [doc = " alignments. The buffer was allocated with alignment 2 (for `u16`), but after"] # [doc = " turning it into a `Vec<u8>` it'll be deallocated with alignment 1. To avoid"] # [doc = " these issues, it is often preferable to do casting/transmuting using"] # [doc = " [`slice::from_raw_parts`] instead."] # [doc = ""] # [doc = " The ownership of `ptr` is effectively transferred to the"] # [doc = " `Vec<T>` which may then deallocate, reallocate or change the"] # [doc = " contents of memory pointed to by the pointer at will. Ensure"] # [doc = " that nothing else uses the pointer after calling this"] # [doc = " function."] # [doc = ""] # [doc = " [`String`]: alloc_crate::string::String"] # [doc = " [`dealloc`]: crate::alloc::GlobalAlloc::dealloc"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec, vec::Vec};"] # [doc = ""] # [doc = " use std::ptr;"] # [doc = " use std::mem;"] # [doc = ""] # [doc = " let v = vec![1, 2, 3];"] # [doc = ""] # [doc = " // Prevent running `v`'s destructor so we are in complete control"] # [doc = " // of the allocation."] # [doc = " let mut v = mem::ManuallyDrop::new(v);"] # [doc = ""] # [doc = " // Pull out the various important pieces of information about `v`"] # [doc = " let p = v.as_mut_ptr();"] # [doc = " let len = v.len();"] # [doc = " let cap = v.capacity();"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     // Overwrite memory with 4, 5, 6"] # [doc = "     for i in 0..len {"] # [doc = "         ptr::write(p.add(i), 4 + i);"] # [doc = "     }"] # [doc = ""] # [doc = "     // Put everything back together into a Vec"] # [doc = "     let rebuilt = Vec::from_raw_parts(p, len, cap);"] # [doc = "     assert_eq!(rebuilt, [4, 5, 6]);"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Using memory that was allocated elsewhere:"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::{vec, vec::Vec, alloc::{AllocError, Allocator, Global, Layout}};"] # [doc = ""] # [doc = " fn main() {"] # [doc = "     let layout = Layout::array::<u32>(16).expect(\"overflow cannot happen\");"] # [doc = ""] # [doc = "     let vec = unsafe {"] # [doc = "         let mem = match Global.allocate(layout) {"] # [doc = "             Ok(mem) => mem.cast::<u32>().as_ptr(),"] # [doc = "             Err(AllocError) => return,"] # [doc = "         };"] # [doc = ""] # [doc = "         mem.write(1_000_000);"] # [doc = ""] # [doc = "         Vec::from_raw_parts_in(mem, 1, 16, Global)"] # [doc = "     };"] # [doc = ""] # [doc = "     assert_eq!(vec, &[1_000_000]);"] # [doc = "     assert_eq!(vec.capacity(), 16);"] # [doc = " }"] # [doc = " ```"] # [inline (always)] pub unsafe fn from_raw_parts (ptr : * mut T , length : usize , capacity : usize) -> Self { unsafe { Self :: from_raw_parts_in (ptr , length , capacity , Global) } } }
};
}
