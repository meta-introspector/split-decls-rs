// Generated macro for impl_184 (impl)
macro_rules! Depcrate_vec_drainimpl_184 {
() => {
// Module: crate::vec::drain
// Provides: {"impl_184"}
// Dependencies: {}
impl < 'a , T , A : Allocator > Drain < 'a , T , A > { # [doc = " Returns the remaining items of this iterator as a slice."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec!['a', 'b', 'c'];"] # [doc = " let mut drain = vec.drain(..);"] # [doc = " assert_eq!(drain.as_slice(), &['a', 'b', 'c']);"] # [doc = " let _ = drain.next().unwrap();"] # [doc = " assert_eq!(drain.as_slice(), &['b', 'c']);"] # [doc = " ```"] # [must_use] # [inline (always)] pub fn as_slice (& self) -> & [T] { self . iter . as_slice () } # [doc = " Returns a reference to the underlying allocator."] # [must_use] # [inline (always)] pub fn allocator (& self) -> & A { unsafe { self . vec . as_ref () . allocator () } } # [doc = " Keep unyielded elements in the source `Vec`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use allocator_api2::vec;"] # [doc = ""] # [doc = " let mut vec = vec!['a', 'b', 'c'];"] # [doc = " let mut drain = vec.drain(..);"] # [doc = ""] # [doc = " assert_eq!(drain.next().unwrap(), 'a');"] # [doc = ""] # [doc = " // This call keeps 'b' and 'c' in the vec."] # [doc = " drain.keep_rest();"] # [doc = ""] # [doc = " // If we wouldn't call `keep_rest()`,"] # [doc = " // `vec` would be empty."] # [doc = " assert_eq!(vec, ['b', 'c']);"] # [doc = " ```"] # [inline (always)] pub fn keep_rest (self) { let mut this = ManuallyDrop :: new (self) ; unsafe { let source_vec = this . vec . as_mut () ; let start = source_vec . len () ; let tail = this . tail_start ; let unyielded_len = this . iter . len () ; let unyielded_ptr = this . iter . as_slice () . as_ptr () ; let needs_move = mem :: size_of :: < T > () != 0 ; if needs_move { let start_ptr = source_vec . as_mut_ptr () . add (start) ; if unyielded_ptr != start_ptr { let src = unyielded_ptr ; let dst = start_ptr ; ptr :: copy (src , dst , unyielded_len) ; } if tail != (start + unyielded_len) { let src = source_vec . as_ptr () . add (tail) ; let dst = start_ptr . add (unyielded_len) ; ptr :: copy (src , dst , this . tail_len) ; } } source_vec . set_len (start + unyielded_len + this . tail_len) ; } } }
};
}
