// Generated macro for sign_prehashed (function)
macro_rules! Depcrate_hazmatsign_prehashed {
() => {
// Module: crate::hazmat
// Provides: {"sign_prehashed"}
// Dependencies: {}
# [doc = " Sign a prehashed message digest using the provided secret scalar and"] # [doc = " ephemeral scalar, returning an ECDSA signature."] # [doc = ""] # [doc = " Accepts the following arguments:"] # [doc = ""] # [doc = " - `d`: signing key. MUST BE UNIFORMLY RANDOM!!!"] # [doc = " - `k`: ephemeral scalar value. MUST BE UNIFORMLY RANDOM!!!"] # [doc = " - `z`: message digest to be signed. MUST BE OUTPUT OF A CRYPTOGRAPHICALLY"] # [doc = "   SECURE DIGEST ALGORITHM!!!"] # [doc = ""] # [doc = " # Low-S Normalization"] # [doc = ""] # [doc = " This function will apply low-S normalization if `<C as EcdsaCurve>::NORMALIZE_S` is true."] # [doc = ""] # [doc = " # Returns"] # [doc = ""] # [doc = " ECDSA [`Signature`] and a [`RecoveryId`] which can be used to recover the verifying key for a"] # [doc = " given signature."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This will return an error if a zero-scalar was generated. It can be tried again with a"] # [doc = " different `k`."] # [cfg (feature = "algorithm")] # [allow (non_snake_case)] pub fn sign_prehashed < C > (d : & NonZeroScalar < C > , k : & NonZeroScalar < C > , z : & FieldBytes < C > ,) -> Result < (Signature < C > , RecoveryId) > where C : EcdsaCurve + CurveArithmetic , SignatureSize < C > : ArraySize , { let z = Scalar :: < C > :: reduce (z) ; let k_inv = k . invert () ; let R = ProjectivePoint :: < C > :: mul_by_generator (k) . to_affine () ; let r = Scalar :: < C > :: reduce (& R . x ()) ; let s = * k_inv * (z + (r * d . as_ref ())) ; let mut signature = Signature :: from_scalars (r , s) ? ; let x_is_reduced = r . to_repr () != R . x () ; let y_is_odd = R . y_is_odd () ; let mut recovery_id = RecoveryId :: new (y_is_odd . into () , x_is_reduced) ; if C :: NORMALIZE_S { recovery_id . 0 ^= s . is_high () . unwrap_u8 () ; signature = signature . normalize_s () ; } Ok ((signature , recovery_id)) }
};
}
