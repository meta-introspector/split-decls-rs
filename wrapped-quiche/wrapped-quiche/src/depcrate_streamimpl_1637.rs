// Generated macro for impl_1637 (impl)
macro_rules! Depcrate_streamimpl_1637 {
() => {
// Module: crate::stream
// Provides: {"impl_1637"}
// Dependencies: {}
impl < F : BufFactory > Stream < F > { # [doc = " Creates a new stream with the given flow control limits."] pub fn new (id : u64 , max_rx_data : u64 , max_tx_data : u64 , bidi : bool , local : bool , max_window : u64 ,) -> Self { let priority_key = Arc :: new (StreamPriorityKey { id , .. Default :: default () }) ; Stream { recv : recv_buf :: RecvBuf :: new (max_rx_data , max_window) , send : send_buf :: SendBuf :: new (max_tx_data) , send_lowat : 1 , bidi , local , urgency : priority_key . urgency , incremental : priority_key . incremental , priority_key , } } # [doc = " Returns true if the stream has data to read."] pub fn is_readable (& self) -> bool { self . recv . ready () } # [doc = " Returns true if the stream has enough flow control capacity to be"] # [doc = " written to, and is not finished."] pub fn is_writable (& self) -> bool { ! self . send . is_shutdown () && ! self . send . is_fin () && (self . send . off_back () + self . send_lowat as u64) < self . send . max_off () } # [doc = " Returns true if the stream has data to send and is allowed to send at"] # [doc = " least some of it."] pub fn is_flushable (& self) -> bool { let off_front = self . send . off_front () ; ! self . send . is_empty () && off_front < self . send . off_back () && off_front < self . send . max_off () } # [doc = " Returns true if the stream is complete."] # [doc = ""] # [doc = " For bidirectional streams this happens when both the receive and send"] # [doc = " sides are complete. That is when all incoming data has been read by the"] # [doc = " application, and when all outgoing data has been acked by the peer."] # [doc = ""] # [doc = " For unidirectional streams this happens when either the receive or send"] # [doc = " side is complete, depending on whether the stream was created locally"] # [doc = " or not."] pub fn is_complete (& self) -> bool { match (self . bidi , self . local) { (true , _) => self . recv . is_fin () && self . send . is_complete () , (false , true) => self . send . is_complete () , (false , false) => self . recv . is_fin () , } } }
};
}
