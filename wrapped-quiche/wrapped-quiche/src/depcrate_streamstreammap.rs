// Generated macro for StreamMap (struct)
macro_rules! Depcrate_streamStreamMap {
() => {
// Module: crate::stream
// Provides: {"StreamMap"}
// Dependencies: {}
# [doc = " Keeps track of QUIC streams and enforces stream limits."] # [derive (Default)] pub struct StreamMap < F : BufFactory = DefaultBufFactory > { # [doc = " Map of streams indexed by stream ID."] streams : StreamIdHashMap < Stream < F > > , # [doc = " Set of streams that were completed and garbage collected."] # [doc = ""] # [doc = " Instead of keeping the full stream state forever, we collect completed"] # [doc = " streams to save memory, but we still need to keep track of previously"] # [doc = " created streams, to prevent peers from re-creating them."] collected : StreamIdHashSet , # [doc = " Peer's maximum bidirectional stream count limit."] peer_max_streams_bidi : u64 , # [doc = " Peer's maximum unidirectional stream count limit."] peer_max_streams_uni : u64 , # [doc = " The total number of bidirectional streams opened by the peer."] peer_opened_streams_bidi : u64 , # [doc = " The total number of unidirectional streams opened by the peer."] peer_opened_streams_uni : u64 , # [doc = " Local maximum bidirectional stream count limit."] local_max_streams_bidi : u64 , local_max_streams_bidi_next : u64 , # [doc = " Local maximum unidirectional stream count limit."] local_max_streams_uni : u64 , local_max_streams_uni_next : u64 , # [doc = " The total number of bidirectional streams opened by the local endpoint."] local_opened_streams_bidi : u64 , # [doc = " The total number of unidirectional streams opened by the local endpoint."] local_opened_streams_uni : u64 , # [doc = " Queue of stream IDs corresponding to streams that have buffered data"] # [doc = " ready to be sent to the peer. This also implies that the stream has"] # [doc = " enough flow control credits to send at least some of that data."] flushable : RBTree < StreamFlushablePriorityAdapter > , # [doc = " Set of stream IDs corresponding to streams that have outstanding data"] # [doc = " to read. This is used to generate a `StreamIter` of streams without"] # [doc = " having to iterate over the full list of streams."] pub readable : RBTree < StreamReadablePriorityAdapter > , # [doc = " Set of stream IDs corresponding to streams that have enough flow control"] # [doc = " capacity to be written to, and is not finished. This is used to generate"] # [doc = " a `StreamIter` of streams without having to iterate over the full list"] # [doc = " of streams."] pub writable : RBTree < StreamWritablePriorityAdapter > , # [doc = " Set of stream IDs corresponding to streams that are almost out of flow"] # [doc = " control credit and need to send MAX_STREAM_DATA. This is used to"] # [doc = " generate a `StreamIter` of streams without having to iterate over the"] # [doc = " full list of streams."] almost_full : StreamIdHashSet , # [doc = " Set of stream IDs corresponding to streams that are blocked. The value"] # [doc = " of the map elements represents the offset of the stream at which the"] # [doc = " blocking occurred."] blocked : StreamIdHashMap < u64 > , # [doc = " Set of stream IDs corresponding to streams that are reset. The value"] # [doc = " of the map elements is a tuple of the error code and final size values"] # [doc = " to include in the RESET_STREAM frame."] reset : StreamIdHashMap < (u64 , u64) > , # [doc = " Set of stream IDs corresponding to streams that are shutdown on the"] # [doc = " receive side, and need to send a STOP_SENDING frame. The value of the"] # [doc = " map elements is the error code to include in the STOP_SENDING frame."] stopped : StreamIdHashMap < u64 > , # [doc = " The maximum size of a stream window."] max_stream_window : u64 , }
};
}
