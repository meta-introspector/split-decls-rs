// Generated macro for retry (function)
macro_rules! Depcrateretry {
() => {
// Module: crate
// Provides: {"retry"}
// Dependencies: {}
# [doc = " Writes a stateless retry packet."] # [doc = ""] # [doc = " The `scid` and `dcid` parameters are the source connection ID and the"] # [doc = " destination connection ID extracted from the received client's Initial"] # [doc = " packet, while `new_scid` is the server's new source connection ID and"] # [doc = " `token` is the address validation token the client needs to echo back."] # [doc = ""] # [doc = " The application is responsible for generating the address validation"] # [doc = " token to be sent to the client, and verifying tokens sent back by the"] # [doc = " client. The generated token should include the `dcid` parameter, such"] # [doc = " that it can be later extracted from the token and passed to the"] # [doc = " [`accept()`] function as its `odcid` parameter."] # [doc = ""] # [doc = " [`accept()`]: fn.accept.html"] # [doc = ""] # [doc = " ## Examples:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " # let mut config = quiche::Config::new(0xbabababa)?;"] # [doc = " # let mut buf = [0; 512];"] # [doc = " # let mut out = [0; 512];"] # [doc = " # let scid = quiche::ConnectionId::from_ref(&[0xba; 16]);"] # [doc = " # let socket = std::net::UdpSocket::bind(\"127.0.0.1:0\").unwrap();"] # [doc = " # let local = socket.local_addr().unwrap();"] # [doc = " # fn mint_token(hdr: &quiche::Header, src: &std::net::SocketAddr) -> Vec<u8> {"] # [doc = " #     vec![]"] # [doc = " # }"] # [doc = " # fn validate_token<'a>(src: &std::net::SocketAddr, token: &'a [u8]) -> Option<quiche::ConnectionId<'a>> {"] # [doc = " #     None"] # [doc = " # }"] # [doc = " let (len, peer) = socket.recv_from(&mut buf).unwrap();"] # [doc = ""] # [doc = " let hdr = quiche::Header::from_slice(&mut buf[..len], quiche::MAX_CONN_ID_LEN)?;"] # [doc = ""] # [doc = " let token = hdr.token.as_ref().unwrap();"] # [doc = ""] # [doc = " // No token sent by client, create a new one."] # [doc = " if token.is_empty() {"] # [doc = "     let new_token = mint_token(&hdr, &peer);"] # [doc = ""] # [doc = "     let len = quiche::retry("] # [doc = "         &hdr.scid, &hdr.dcid, &scid, &new_token, hdr.version, &mut out,"] # [doc = "     )?;"] # [doc = ""] # [doc = "     socket.send_to(&out[..len], &peer).unwrap();"] # [doc = "     return Ok(());"] # [doc = " }"] # [doc = ""] # [doc = " // Client sent token, validate it."] # [doc = " let odcid = validate_token(&peer, token);"] # [doc = ""] # [doc = " if odcid.is_none() {"] # [doc = "     // Invalid address validation token."] # [doc = "     return Ok(());"] # [doc = " }"] # [doc = ""] # [doc = " let conn = quiche::accept(&scid, odcid.as_ref(), local, peer, &mut config)?;"] # [doc = " # Ok::<(), quiche::Error>(())"] # [doc = " ```"] # [inline] pub fn retry (scid : & ConnectionId , dcid : & ConnectionId , new_scid : & ConnectionId , token : & [u8] , version : u32 , out : & mut [u8] ,) -> Result < usize > { packet :: retry (scid , dcid , new_scid , token , version , out) }
};
}
