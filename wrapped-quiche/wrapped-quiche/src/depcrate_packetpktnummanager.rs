// Generated macro for PktNumManager (struct)
macro_rules! Depcrate_packetPktNumManager {
() => {
// Module: crate::packet
// Provides: {"PktNumManager"}
// Dependencies: {}
# [doc = " QUIC recommends skipping packet numbers to elicit a [faster ACK] or to"] # [doc = " mitigate an [optimistic ACK attack] (OACK attack). quiche currently skips"] # [doc = " packets only for the purposes of optimistic attack mitigation."] # [doc = ""] # [doc = " ## What is an Optimistic ACK attack"] # [doc = " A typical endpoint is responsible for making concurrent progress on multiple"] # [doc = " connections and needs to fairly allocate resources across those connection."] # [doc = " In order to ensure fairness, an endpoint relies on \"recovery signals\" to"] # [doc = " determine the optimal sending rate per connection. For example, when a new"] # [doc = " flow joins a shared network, it might induce packet loss for other flows and"] # [doc = " cause those flows to yield bandwidth on the network. ACKs are the primary"] # [doc = " source of recovery signals for a QUIC connection."] # [doc = ""] # [doc = " The goal of an OACK attack is to mount a DDoS attack by exploiting recovery"] # [doc = " signals and causing a server to expand its sending rate. A server with an"] # [doc = " inflated sending rate would then be able to flood a shared network and"] # [doc = " cripple all other flows. The fundamental reason that makes OACK"] # [doc = " attach possible is the use of unvalidated ACK data, which is then used to"] # [doc = " modify internal state. Therefore at a high level, a mitigation should"] # [doc = " validate the incoming ACK data before use."] # [doc = ""] # [doc = " ## Optimistic ACK attack mitigation"] # [doc = " quiche follows the RFC's recommendation for mitigating an [optimistic ACK"] # [doc = " attack] by skipping packets and validating that the peer does NOT send ACKs"] # [doc = " for those skipped packets. If an ACK for a skipped packet is received, the"] # [doc = " connection is closed with a [PROTOCOL_VIOLATION] error."] # [doc = ""] # [doc = " A robust mitigation should skip packets randomly to ensure that an attacker"] # [doc = " can't predict which packet number was skipped. Skip/validation should happen"] # [doc = " \"periodically\" over the lifetime of the connection. Since skipping packets"] # [doc = " also elicits a faster ACK, we need to balance the skip frequency to"] # [doc = " sufficiently validate the peer without impacting other aspects of recovery."] # [doc = ""] # [doc = " A naive approach could be to skip a random packet number in the range"] # [doc = " 200-500 (pick some static range). While this might work, its not apparent if"] # [doc = " a static range is effective for all networks with varying bandwidths/RTTs."] # [doc = ""] # [doc = " Since an attacker can potentially influence the sending rate once per"] # [doc = " \"round\", it would be ideal to validate the peer once per round. Therefore,"] # [doc = " an ideal range seems to be one that dynamically adjusts based on packets"] # [doc = " sent per round, ie. adjust skip range based on the current CWND."] # [doc = ""] # [doc = " [faster ACK]: https://www.rfc-editor.org/rfc/rfc9002.html#section-6.2.4"] # [doc = " [optimistic ACK attack]: https://www.rfc-editor.org/rfc/rfc9000.html#section-21.4"] # [doc = " [PROTOCOL_VIOLATION]: https://www.rfc-editor.org/rfc/rfc9000#section-13.1"] pub struct PktNumManager { # [doc = " Track if we have skipped a packet number."] skip_pn : Option < u64 > , # [doc = " Track when to skip the next packet number"] # [doc = ""] # [doc = " None indicates the counter is not armed while Some(0) indicates that the"] # [doc = " counter has expired."] pub skip_pn_counter : Option < u64 > , }
};
}
