// Generated macro for dont_coalesce_probes (function)
macro_rules! Depcrate_testsdont_coalesce_probes {
() => {
// Module: crate::tests
// Provides: {"dont_coalesce_probes"}
// Dependencies: {}
# [rstest] # [doc = " Tests that PTO probe packets are not coalesced together."] fn dont_coalesce_probes (# [values ("cubic" , "bbr2" , "bbr2_gcongestion")] cc_algorithm_name : & str ,) { let mut buf = [0 ; 65535] ; let mut pipe = test_utils :: Pipe :: new (cc_algorithm_name) . unwrap () ; let (len , _) = pipe . client . send (& mut buf) . unwrap () ; assert_eq ! (len , 1200) ; assert_eq ! (pipe . client . path_stats () . next () . unwrap () . total_pto_count , 0) ; let timer = pipe . client . timeout () . unwrap () ; std :: thread :: sleep (timer + Duration :: from_millis (1)) ; pipe . client . on_timeout () ; assert_eq ! (pipe . client . path_stats () . next () . unwrap () . total_pto_count , 1) ; let epoch = packet :: Epoch :: Initial ; assert_eq ! (pipe . client . paths . get_active () . expect ("no active") . recovery . loss_probes (epoch) , 1 ,) ; let (len , _) = pipe . client . send (& mut buf) . unwrap () ; assert_eq ! (len , 1200) ; assert_eq ! (pipe . client . paths . get_active () . expect ("no active") . recovery . loss_probes (epoch) , 0 ,) ; let timer = pipe . client . timeout () . unwrap () ; std :: thread :: sleep (timer + Duration :: from_millis (1)) ; pipe . client . on_timeout () ; assert_eq ! (pipe . client . path_stats () . next () . unwrap () . total_pto_count , 2) ; assert_eq ! (pipe . client . paths . get_active () . expect ("no active") . recovery . loss_probes (epoch) , 2 ,) ; let (len , _) = pipe . client . send (& mut buf) . unwrap () ; assert_eq ! (len , 1200) ; assert_eq ! (pipe . client . paths . get_active () . expect ("no active") . recovery . loss_probes (epoch) , 1 ,) ; let (len , _) = pipe . client . send (& mut buf) . unwrap () ; assert_eq ! (len , 1200) ; assert_eq ! (pipe . client . paths . get_active () . expect ("no active") . recovery . loss_probes (epoch) , 0 ,) ; }
};
}
