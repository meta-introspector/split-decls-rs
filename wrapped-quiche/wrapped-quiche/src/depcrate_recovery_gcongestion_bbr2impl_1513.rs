// Generated macro for impl_1513 (impl)
macro_rules! Depcrate_recovery_gcongestion_bbr2impl_1513 {
() => {
// Module: crate::recovery::gcongestion::bbr2
// Provides: {"impl_1513"}
// Dependencies: {}
impl BBRv2 { pub fn new (initial_congestion_window : usize , max_congestion_window : usize , max_segment_size : usize , smoothed_rtt : Duration , custom_bbr_params : Option < & BbrParams > ,) -> Self { let cwnd = initial_congestion_window * max_segment_size ; let params = if let Some (custom_bbr_settings) = custom_bbr_params { DEFAULT_PARAMS . with_overrides (custom_bbr_settings) } else { DEFAULT_PARAMS } ; BBRv2 { mode : Mode :: startup (BBRv2NetworkModel :: new (& params , smoothed_rtt)) , cwnd , pacing_rate : initial_pacing_rate (cwnd , smoothed_rtt , & params) , cwnd_limits : Limits { lo : initial_congestion_window * max_segment_size , hi : max_congestion_window * max_segment_size , } , initial_cwnd : initial_congestion_window * max_segment_size , last_sample_is_app_limited : false , has_non_app_limited_sample : false , last_quiescence_start : None , mss : max_segment_size , params , } } fn on_exit_quiescence (& mut self , now : Instant) { if let Some (last_quiescence_start) = self . last_quiescence_start . take () { self . mode . do_on_exit_quiescence (now , last_quiescence_start , & self . params ,) } } fn get_target_congestion_window (& self , gain : f32) -> usize { let network_model = self . mode . network_model () ; network_model . bdp (network_model . bandwidth_estimate () , gain) . max (self . cwnd_limits . min ()) } fn update_pacing_rate (& mut self , bytes_acked : usize) { let network_model = self . mode . network_model () ; let bandwidth_estimate = match network_model . bandwidth_estimate () { e if e == Bandwidth :: zero () => return , e => e , } ; if network_model . total_bytes_acked () == bytes_acked { self . pacing_rate = Bandwidth :: from_bytes_and_time_delta (self . cwnd , network_model . min_rtt () ,) ; if let Some (pacing_rate) = self . params . initial_pacing_rate_bytes_per_second { let initial_pacing_rate = Bandwidth :: from_bytes_per_second (pacing_rate) ; self . pacing_rate = self . pacing_rate . min (initial_pacing_rate) ; } return ; } let target_rate = bandwidth_estimate * network_model . pacing_gain () ; if network_model . full_bandwidth_reached () { self . pacing_rate = target_rate ; return ; } if self . params . decrease_startup_pacing_at_end_of_round && network_model . pacing_gain () < self . params . startup_pacing_gain { self . pacing_rate = target_rate ; return ; } if self . params . bw_lo_mode != BwLoMode :: Default && network_model . loss_events_in_round () > 0 { self . pacing_rate = target_rate ; return ; } self . pacing_rate = self . pacing_rate . max (target_rate) ; } fn update_congestion_window (& mut self , bytes_acked : usize) { let network_model = self . mode . network_model () ; let mut target_cwnd = self . get_target_congestion_window (network_model . cwnd_gain ()) ; let prior_cwnd = self . cwnd ; if network_model . full_bandwidth_reached () { target_cwnd += network_model . max_ack_height () ; self . cwnd = target_cwnd . min (prior_cwnd + bytes_acked) ; } else if prior_cwnd < target_cwnd || prior_cwnd < 2 * self . initial_cwnd { self . cwnd = prior_cwnd + bytes_acked ; } self . cwnd = self . mode . get_cwnd_limits (& self . params) . apply_limits (self . cwnd) ; self . cwnd = self . cwnd_limits . apply_limits (self . cwnd) ; } fn on_enter_quiescence (& mut self , time : Instant) { self . last_quiescence_start = Some (time) ; } fn target_bytes_inflight (& self) -> usize { let network_model = & self . mode . network_model () ; let bdp = network_model . bdp1 (network_model . bandwidth_estimate ()) ; bdp . min (self . get_congestion_window ()) } }
};
}
