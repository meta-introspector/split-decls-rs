// Generated macro for impl_1361 (impl)
macro_rules! Depcrate_recovery_congestion_delivery_rateimpl_1361 {
() => {
// Module: crate::recovery::congestion::delivery_rate
// Provides: {"impl_1361"}
// Dependencies: {}
impl Rate { pub fn on_packet_sent (& mut self , pkt : & mut Sent , bytes_in_flight : usize , bytes_lost : u64 ,) { if bytes_in_flight == 0 { self . first_sent_time = pkt . time_sent ; self . delivered_time = pkt . time_sent ; } pkt . first_sent_time = self . first_sent_time ; pkt . delivered_time = self . delivered_time ; pkt . delivered = self . delivered ; pkt . is_app_limited = self . app_limited () ; pkt . tx_in_flight = bytes_in_flight ; pkt . lost = bytes_lost ; self . last_sent_packet = pkt . pkt_num ; } pub fn update_rate_sample (& mut self , pkt : & Acked , now : Instant) { self . delivered += pkt . size ; self . delivered_time = now ; if self . rate_sample . prior_time . is_none () || pkt . delivered >= self . rate_sample . prior_delivered { self . rate_sample . prior_delivered = pkt . delivered ; self . rate_sample . prior_time = Some (pkt . delivered_time) ; self . rate_sample . is_app_limited = pkt . is_app_limited ; self . rate_sample . send_elapsed = pkt . time_sent . saturating_duration_since (pkt . first_sent_time) ; self . rate_sample . rtt = pkt . rtt ; self . rate_sample . ack_elapsed = self . delivered_time . saturating_duration_since (pkt . delivered_time) ; self . first_sent_time = pkt . time_sent ; } self . largest_acked = self . largest_acked . max (pkt . pkt_num) ; } pub fn generate_rate_sample (& mut self , min_rtt : Duration) { if self . app_limited () && self . largest_acked > self . end_of_app_limited { self . update_app_limited (false) ; } if self . rate_sample . prior_time . is_some () { let interval = self . rate_sample . send_elapsed . max (self . rate_sample . ack_elapsed) ; self . rate_sample . delivered = self . delivered - self . rate_sample . prior_delivered ; self . rate_sample . interval = interval ; if interval < min_rtt { self . rate_sample . interval = Duration :: ZERO ; return ; } if ! interval . is_zero () { let rate_sample_bandwidth = { let rate_sample_bytes_per_second = (self . rate_sample . delivered as f64 / interval . as_secs_f64 ()) as u64 ; Bandwidth :: from_bytes_per_second (rate_sample_bytes_per_second) } ; if ! self . rate_sample . is_app_limited || rate_sample_bandwidth > self . rate_sample . bandwidth { self . update_delivery_rate (rate_sample_bandwidth) ; } } } } fn update_delivery_rate (& mut self , bandwidth : Bandwidth) { self . rate_sample . bandwidth = bandwidth ; } pub fn update_app_limited (& mut self , v : bool) { self . end_of_app_limited = if v { self . last_sent_packet . max (1) } else { 0 } ; } pub fn app_limited (& mut self) -> bool { self . end_of_app_limited != 0 } pub fn delivered (& self) -> usize { self . delivered } pub fn sample_delivery_rate (& self) -> Bandwidth { self . rate_sample . bandwidth } pub fn sample_rtt (& self) -> Duration { self . rate_sample . rtt } pub fn sample_is_app_limited (& self) -> bool { self . rate_sample . is_app_limited } pub fn sample_delivered (& self) -> usize { self . rate_sample . delivered } pub fn sample_prior_delivered (& self) -> usize { self . rate_sample . prior_delivered } }
};
}
