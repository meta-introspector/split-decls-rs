// Generated macro for generate (macro)
macro_rules! Depcrate_joingenerate {
() => {
// Module: crate::join
// Provides: {"generate"}
// Dependencies: {}
macro_rules ! generate { ($ (($ Join : ident , $ new : ident , < A , $ ($ B : ident) ,*>) ,) *) => ($ (# [doc = " Future for the `join` combinator, waiting for two futures to"] # [doc = " complete."] # [doc = ""] # [doc = " This is created by this `Future::join` method."] pub struct $ Join < A , $ ($ B) ,*> where A : Future , $ ($ B : Future < Error = A :: Error >) ,* { a : MaybeDone < A >, $ ($ B : MaybeDone <$ B >,) * } pub fn $ new < A , $ ($ B) ,*> (a : A , $ ($ B : $ B) ,*) -> $ Join < A , $ ($ B) ,*> where A : Future , $ ($ B : Future < Error = A :: Error >) ,* { let a = Collapsed :: Start (a) ; $ (let $ B = Collapsed :: Start ($ B) ;) * $ Join { a : MaybeDone :: NotYet (a) , $ ($ B : MaybeDone :: NotYet ($ B)) ,* } } impl < A , $ ($ B) ,*> $ Join < A , $ ($ B) ,*> where A : Future , $ ($ B : Future < Error = A :: Error >) ,* { fn erase (& mut self) { self . a = MaybeDone :: Gone ; $ (self .$ B = MaybeDone :: Gone ;) * } } impl < A , $ ($ B) ,*> Future for $ Join < A , $ ($ B) ,*> where A : Future , $ ($ B : Future < Error = A :: Error >) ,* { type Item = (A :: Item , $ ($ B :: Item) ,*) ; type Error = A :: Error ; fn poll (& mut self , task : & mut Task) -> Poll < Self :: Item , Self :: Error > { let mut all_done = match self . a . poll (task) { Ok (done) => done , Err (e) => { self . erase () ; return Poll :: Err (e) } } ; $ (all_done = match self .$ B . poll (task) { Ok (done) => all_done && done , Err (e) => { self . erase () ; return Poll :: Err (e) } } ;) * if all_done { Poll :: Ok ((self . a . take () , $ (self .$ B . take ()) ,*)) } else { Poll :: NotReady } } fn schedule (& mut self , task : & mut Task) { if let MaybeDone :: NotYet (ref mut a) = self . a { a . schedule (task) ; } $ (if let MaybeDone :: NotYet (ref mut a) = self .$ B { a . schedule (task) ; }) * } fn tailcall (& mut self) -> Option < Box < Future < Item = Self :: Item , Error = Self :: Error >>> { self . a . collapse () ; $ (self .$ B . collapse () ;) * None } }) *) }
};
}
