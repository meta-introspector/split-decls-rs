// Generated macro for Stream (trait)
macro_rules! Depcrate_streamStream {
() => {
// Module: crate::stream
// Provides: {"Stream"}
// Dependencies: {}
# [doc = " A stream of values, not all of which have been produced yet."] # [doc = ""] # [doc = " `Stream` is a trait to represent any source of sequential events or items"] # [doc = " which acts like an iterator but may block over time. Like `Future` the"] # [doc = " methods of `Stream` never block and it is thus suitable for programming in"] # [doc = " an asynchronous fashion. This trait is very similar to the `Iterator` trait"] # [doc = " in the standard library where `Some` is used to signal elements of the"] # [doc = " stream and `None` is used to indicate that the stream is finished."] # [doc = ""] # [doc = " Like futures a stream has basic combinators to transform the stream, perform"] # [doc = " more work on each item, etc."] # [doc = ""] # [doc = " # Basic methods"] # [doc = ""] # [doc = " Like futures, a `Stream` has two core methods which drive processing of data"] # [doc = " and notifications of when new data might be ready. The `poll` method checks"] # [doc = " the status of a stream and the `schedule` method is used to receive"] # [doc = " notifications for when it may be ready to call `poll` again."] # [doc = ""] # [doc = " Also like future, a stream has an associated error type to represent that an"] # [doc = " element of the computation failed for some reason. Errors, however, do not"] # [doc = " signal the end of the stream."] # [doc = ""] # [doc = " # Streams as Futures"] # [doc = ""] # [doc = " Any instance of `Stream` can also be viewed as a `Future` where the resolved"] # [doc = " value is the next item in the stream along with the rest of the stream. The"] # [doc = " `into_future` adaptor can be used here to convert any stream into a future"] # [doc = " for use with other future methods like `join` and `select`."] pub trait Stream : Send + 'static { # [doc = " The type of item this stream will yield on success."] type Item : Send + 'static ; # [doc = " The type of error this stream may generate."] type Error : Send + 'static ; # [doc = " Attempt to pull out the next value of this stream, returning `None` if"] # [doc = " it's not ready yet."] # [doc = ""] # [doc = " This method, like `Future::poll`, is the sole method of pulling out a"] # [doc = " value from a stream. The `task` argument is the task of computation that"] # [doc = " this stream is running within, and it contains information like"] # [doc = " task-local data and tokens of interest."] # [doc = ""] # [doc = " Implementors of this trait must ensure that implementations of this"] # [doc = " method do not block, as it may cause consumers to behave badly."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " If `Poll::NotReady` is returned then this stream's next value is not"] # [doc = " ready yet, and `schedule` can be used to receive a notification for when"] # [doc = " the value may become ready in the future. If `Some` is returned then the"] # [doc = " returned value represents the next value on the stream. `Err` indicates"] # [doc = " an error happened, while `Ok` indicates whether there was a new item on"] # [doc = " the stream or whether the stream has terminated."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Once a stream is finished, that is `Poll::Ok(None)` has been returned,"] # [doc = " further calls to `poll` may result in a panic or other \"bad behavior\"."] # [doc = " If this is difficult to guard against then the `fuse` adapter can be"] # [doc = " used to ensure that `poll` always has well-defined semantics."] fn poll (& mut self , task : & mut Task) -> Poll < Option < Self :: Item > , Self :: Error > ; # [doc = " Schedule a task to be notified when this future is ready."] # [doc = ""] # [doc = " This is very similar to the `Future::schedule` method which registers"] # [doc = " interest. The task provided will only be notified once for the next"] # [doc = " value on a stream. If an application is interested in more values on a"] # [doc = " stream, then a task needs to be re-scheduled."] # [doc = ""] # [doc = " Multiple calls to `schedule` while waiting for one value to be produced"] # [doc = " will only result in the final `task` getting notified. Consumers must"] # [doc = " take care that if `schedule` is called twice the previous task does not"] # [doc = " need to be invoked."] # [doc = ""] # [doc = " Implementors of the `Stream` trait are recommended to just blindly pass"] # [doc = " around this task rather than manufacture new tasks for contained"] # [doc = " futures."] # [doc = ""] # [doc = " When the task is notified it will be provided a set of tokens that"] # [doc = " represent the set of events which have happened since it was last called"] # [doc = " (or the last call to `poll`). These events can later be read during the"] # [doc = " `poll` phase to prevent polling too much."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Once a stream has returned `Ok(None)` (it's been completed) then further"] # [doc = " calls to either `poll` or this function, `schedule`, should not be"] # [doc = " expected to behave well. A call to `schedule` after a poll has succeeded"] # [doc = " may panic, block forever, or otherwise exhibit odd behavior."] # [doc = ""] # [doc = " Callers who may call `schedule` after a stream is finished may want to"] # [doc = " consider using the `fuse` adaptor which defines the behavior of"] # [doc = " `schedule` after a successful poll, but comes with a little bit of"] # [doc = " extra cost."] fn schedule (& mut self , task : & mut Task) ; # [doc = " Convenience function for turning this stream into a trait object."] # [doc = ""] # [doc = " This simply avoids the need to write `Box::new` and can often help with"] # [doc = " type inference as well by always returning a trait object."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::stream::*;"] # [doc = ""] # [doc = " let (_tx, rx) = channel();"] # [doc = " let a: Box<Stream<Item=i32, Error=i32>> = rx.boxed();"] # [doc = " ```"] fn boxed (self) -> Box < Stream < Item = Self :: Item , Error = Self :: Error > > where Self : Sized { Box :: new (self) } # [doc = " Converts this stream into a `Future`."] # [doc = ""] # [doc = " A stream can be viewed as simply a future which will resolve to the next"] # [doc = " element of the stream as well as the stream itself. The returned future"] # [doc = " can be used to compose streams and futures together by placing"] # [doc = " everything into the \"world of futures\"."] fn into_future (self) -> StreamFuture < Self > where Self : Sized { future :: new (self) } # [doc = " Converts a stream of type `T` to a stream of type `U`."] # [doc = ""] # [doc = " The provided closure is executed over all elements of this stream as"] # [doc = " they are made available, and the callback will be executed inline with"] # [doc = " calls to `poll`."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it, similar to the existing `map` methods in the"] # [doc = " standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::stream::*;"] # [doc = ""] # [doc = " let (_tx, rx) = channel::<i32, u32>();"] # [doc = " let rx = rx.map(|x| x + 3);"] # [doc = " ```"] fn map < U , F > (self , f : F) -> Map < Self , F > where F : FnMut (Self :: Item) -> U + Send + 'static , U : Send + 'static , Self : Sized { map :: new (self , f) } # [doc = " Converts a stream of error type `T` to a stream of error type `U`."] # [doc = ""] # [doc = " The provided closure is executed over all errors of this stream as"] # [doc = " they are made available, and the callback will be executed inline with"] # [doc = " calls to `poll`."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it, similar to the existing `map_err` methods in the"] # [doc = " standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::stream::*;"] # [doc = ""] # [doc = " let (_tx, rx) = channel::<i32, u32>();"] # [doc = " let rx = rx.map_err(|x| x + 3);"] # [doc = " ```"] fn map_err < U , F > (self , f : F) -> MapErr < Self , F > where F : FnMut (Self :: Error) -> U + Send + 'static , U : Send + 'static , Self : Sized { map_err :: new (self , f) } # [doc = " Filters the values produced by this stream according to the provided"] # [doc = " predicate."] # [doc = ""] # [doc = " As values of this stream are made available, the provided predicate will"] # [doc = " be run against them. If the predicate returns `true` then the stream"] # [doc = " will yield the value, but if the predicate returns `false` then the"] # [doc = " value will be discarded and the next value will be produced."] # [doc = ""] # [doc = " All errors are passed through without filtering in this combinator."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it, similar to the existing `filter` methods in the"] # [doc = " standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::stream::*;"] # [doc = ""] # [doc = " let (_tx, rx) = channel::<i32, u32>();"] # [doc = " let evens = rx.filter(|x| x % 0 == 2);"] # [doc = " ```"] fn filter < F > (self , f : F) -> Filter < Self , F > where F : FnMut (& Self :: Item) -> bool + Send + 'static , Self : Sized { filter :: new (self , f) } # [doc = " Filters the values produced by this stream while simultaneously mapping"] # [doc = " them to a different type."] # [doc = ""] # [doc = " As values of this stream are made available, the provided function will"] # [doc = " be run on them. If the predicate returns `Some(e)` then the stream will"] # [doc = " yield the value `e`, but if the predicate returns `None` then the next"] # [doc = " value will be produced."] # [doc = ""] # [doc = " All errors are passed through without filtering in this combinator."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it, similar to the existing `filter_map` methods in the"] # [doc = " standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::stream::*;"] # [doc = ""] # [doc = " let (_tx, rx) = channel::<i32, u32>();"] # [doc = " let evens_plus_one = rx.filter_map(|x| {"] # [doc = "     if x % 0 == 2 {"] # [doc = "         Some(x + 1)"] # [doc = "     } else {"] # [doc = "         None"] # [doc = "     }"] # [doc = " });"] # [doc = " ```"] fn filter_map < F , B > (self , f : F) -> FilterMap < Self , F > where F : FnMut (Self :: Item) -> Option < B > + Send + 'static , Self : Sized { filter_map :: new (self , f) } # [doc = " Chain on a computation for when a value is ready, passing the resulting"] # [doc = " item to the provided closure `f`."] # [doc = ""] # [doc = " This function can be used to ensure a computation runs regardless of"] # [doc = " the next value on the stream. The closure provided will be yielded a"] # [doc = " `Result` once a value is ready, and the returned future will then be run"] # [doc = " to completion to produce the next value on this stream."] # [doc = ""] # [doc = " The returned value of the closure must implement the `IntoFuture` trait"] # [doc = " and can represent some more work to be done before the composed stream"] # [doc = " is finished. Note that the `Result` type implements the `IntoFuture`"] # [doc = " trait so it is possible to simply alter the `Result` yielded to the"] # [doc = " closure and return it."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::stream::*;"] # [doc = ""] # [doc = " let (_tx, rx) = channel::<i32, u32>();"] # [doc = ""] # [doc = " let rx = rx.then(|result| {"] # [doc = "     match result {"] # [doc = "         Ok(e) => Ok(e + 3),"] # [doc = "         Err(e) => Err(e - 4),"] # [doc = "     }"] # [doc = " });"] # [doc = " ```"] fn then < F , U > (self , f : F) -> Then < Self , F , U > where F : FnMut (Result < Self :: Item , Self :: Error >) -> U + Send + 'static , U : IntoFuture , Self : Sized { then :: new (self , f) } # [doc = " Chain on a computation for when a value is ready, passing the successful"] # [doc = " results to the provided closure `f`."] # [doc = ""] # [doc = " This function can be used run a unit of work when the next successful"] # [doc = " value on a stream is ready. The closure provided will be yielded a value"] # [doc = " when ready, and the returned future will then be run to completion to"] # [doc = " produce the next value on this stream."] # [doc = ""] # [doc = " Any errors produced by this stream will not be passed to the closure,"] # [doc = " and will be passed through."] # [doc = ""] # [doc = " The returned value of the closure must implement the `IntoFuture` trait"] # [doc = " and can represent some more work to be done before the composed stream"] # [doc = " is finished. Note that the `Result` type implements the `IntoFuture`"] # [doc = " trait so it is possible to simply alter the `Result` yielded to the"] # [doc = " closure and return it."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::stream::*;"] # [doc = ""] # [doc = " let (_tx, rx) = channel::<i32, u32>();"] # [doc = ""] # [doc = " let rx = rx.and_then(|result| {"] # [doc = "     if result % 2 == 0 {"] # [doc = "         Ok(result)"] # [doc = "     } else {"] # [doc = "         Err(result as u32)"] # [doc = "     }"] # [doc = " });"] # [doc = " ```"] fn and_then < F , U > (self , f : F) -> AndThen < Self , F , U > where F : FnMut (Self :: Item) -> U + Send + 'static , U : IntoFuture < Error = Self :: Error > , Self : Sized { and_then :: new (self , f) } # [doc = " Chain on a computation for when an error happens, passing the"] # [doc = " erroneous result to the provided closure `f`."] # [doc = ""] # [doc = " This function can be used run a unit of work and attempt to recover from"] # [doc = " an error if one happens. The closure provided will be yielded an error"] # [doc = " when one appears, and the returned future will then be run to completion"] # [doc = " to produce the next value on this stream."] # [doc = ""] # [doc = " Any successful values produced by this stream will not be passed to the"] # [doc = " closure, and will be passed through."] # [doc = ""] # [doc = " The returned value of the closure must implement the `IntoFuture` trait"] # [doc = " and can represent some more work to be done before the composed stream"] # [doc = " is finished. Note that the `Result` type implements the `IntoFuture`"] # [doc = " trait so it is possible to simply alter the `Result` yielded to the"] # [doc = " closure and return it."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::stream::*;"] # [doc = ""] # [doc = " let (_tx, rx) = channel::<i32, u32>();"] # [doc = ""] # [doc = " let rx = rx.or_else(|result| {"] # [doc = "     if result % 2 == 0 {"] # [doc = "         Ok(result as i32)"] # [doc = "     } else {"] # [doc = "         Err(result)"] # [doc = "     }"] # [doc = " });"] # [doc = " ```"] fn or_else < F , U > (self , f : F) -> OrElse < Self , F , U > where F : FnMut (Self :: Error) -> U + Send + 'static , U : IntoFuture < Item = Self :: Item > , Self : Sized { or_else :: new (self , f) } # [doc = " Collect all of the values of this stream into a vector, returning a"] # [doc = " future representing the result of that computation."] # [doc = ""] # [doc = " This combinator will collect all successful results of this stream and"] # [doc = " collect them into a `Vec<Self::Item>`. If an error happens then all"] # [doc = " collected elements will be dropped and the error will be returned."] # [doc = ""] # [doc = " The returned future will be resolved whenever an error happens or when"] # [doc = " the stream returns `Ok(None)`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::{finished, Future, Task, Poll};"] # [doc = " use futures::stream::*;"] # [doc = ""] # [doc = " let (tx, rx) = channel::<i32, u32>();"] # [doc = ""] # [doc = " fn send(n: i32, tx: Sender<i32, u32>)"] # [doc = "         -> Box<Future<Item=(), Error=()>> {"] # [doc = "     if n == 0 {"] # [doc = "         return finished(()).boxed()"] # [doc = "     }"] # [doc = "     tx.send(Ok(n)).map_err(|_| ()).and_then(move |tx| {"] # [doc = "         send(n - 1, tx)"] # [doc = "     }).boxed()"] # [doc = " }"] # [doc = ""] # [doc = " send(5, tx).forget();"] # [doc = ""] # [doc = " let mut result = rx.collect();"] # [doc = " assert_eq!(result.poll(&mut Task::new()),"] # [doc = "            Poll::Ok(vec![5, 4, 3, 2, 1]));"] # [doc = " ```"] fn collect (self) -> Collect < Self > where Self : Sized { collect :: new (self) } # [doc = " Execute an accumulating computation over a stream, collecting all the"] # [doc = " values into one final result."] # [doc = ""] # [doc = " This combinator will collect all successful results of this stream"] # [doc = " according to the closure provided. The initial state is also provided to"] # [doc = " this method and then is returned again by each execution of the closure."] # [doc = " Once the entire stream has been exhausted the returned future will"] # [doc = " resolve to this value."] # [doc = ""] # [doc = " If an error happens then collected state will be dropped and the error"] # [doc = " will be returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::{finished, Future, Task, Poll};"] # [doc = " use futures::stream::*;"] # [doc = ""] # [doc = " let (tx, rx) = channel::<i32, u32>();"] # [doc = ""] # [doc = " fn send(n: i32, tx: Sender<i32, u32>)"] # [doc = "         -> Box<Future<Item=(), Error=()>> {"] # [doc = "     if n == 0 {"] # [doc = "         return finished(()).boxed()"] # [doc = "     }"] # [doc = "     tx.send(Ok(n)).map_err(|_| ()).and_then(move |tx| {"] # [doc = "         send(n - 1, tx)"] # [doc = "     }).boxed()"] # [doc = " }"] # [doc = ""] # [doc = " send(5, tx).forget();"] # [doc = ""] # [doc = " let mut result = rx.fold(0, |a, b| finished::<i32, u32>(a + b));"] # [doc = " assert_eq!(result.poll(&mut Task::new()), Poll::Ok(15));"] # [doc = " ```"] fn fold < F , T , Fut > (self , init : T , f : F) -> Fold < Self , F , Fut , T > where F : FnMut (T , Self :: Item) -> Fut + Send + 'static , Fut : IntoFuture < Item = T > , Fut :: Error : Into < Self :: Error > , T : Send + 'static , Self : Sized { fold :: new (self , f , init) } # [doc = " Flattens a stream of streams into just one continuous stream."] # [doc = ""] # [doc = " If this stream's elements are themselves streams then this combinator"] # [doc = " will flatten out the entire stream to one long chain of elements. Any"] # [doc = " errors are passed through without looking at them, but otherwise each"] # [doc = " individual stream will get exhausted before moving on to the next."] # [doc = ""] # [doc = " ```"] # [doc = " use futures::{finished, Future, Task, Poll};"] # [doc = " use futures::stream::*;"] # [doc = ""] # [doc = " let (tx1, rx1) = channel::<i32, u32>();"] # [doc = " let (tx2, rx2) = channel::<i32, u32>();"] # [doc = " let (tx3, rx3) = channel::<_, u32>();"] # [doc = ""] # [doc = " tx1.send(Ok(1)).and_then(|tx1| tx1.send(Ok(2))).forget();"] # [doc = " tx2.send(Ok(3)).and_then(|tx2| tx2.send(Ok(4))).forget();"] # [doc = ""] # [doc = " tx3.send(Ok(rx1)).and_then(|tx3| tx3.send(Ok(rx2))).forget();"] # [doc = ""] # [doc = " let mut result = rx3.flatten().collect();"] # [doc = " assert_eq!(result.poll(&mut Task::new()), Poll::Ok(vec![1, 2, 3, 4]));"] # [doc = " ```"] fn flatten (self) -> Flatten < Self > where Self :: Item : Stream , < Self :: Item as Stream > :: Error : From < Self :: Error > , Self : Sized { flatten :: new (self) } # [doc = " Skip elements on this stream while the predicate provided resolves to"] # [doc = " `true`."] # [doc = ""] # [doc = " This function, like `Iterator::skip_while`, will skip elements on the"] # [doc = " stream until the `predicate` resolves to `false`. Once one element"] # [doc = " returns false all future elements will be returned from the underlying"] # [doc = " stream."] fn skip_while < P , R > (self , pred : P) -> SkipWhile < Self , P , R > where P : FnMut (& Self :: Item) -> R + Send + 'static , R : IntoFuture < Item = bool , Error = Self :: Error > , Self : Sized { skip_while :: new (self , pred) } # [allow (missing_docs)] fn for_each < F > (self , f : F) -> ForEach < Self , F > where F : FnMut (Self :: Item) -> Result < () , Self :: Error > + Send + 'static , Self : Sized { for_each :: new (self , f) } # [doc = " Creates a new stream of at most `amt` items."] # [doc = ""] # [doc = " Once `amt` items have been yielded from this stream then it will always"] # [doc = " return that the stream is done."] fn take (self , amt : u64) -> Take < Self > where Self : Sized { take :: new (self , amt) } # [doc = " Creates a new stream which skips `amt` items of the underlying stream."] # [doc = ""] # [doc = " Once `amt` items have been skipped from this stream then it will always"] # [doc = " return the remaining items on this stream."] fn skip (self , amt : u64) -> Skip < Self > where Self : Sized { skip :: new (self , amt) } # [doc = " Fuse a stream such that `poll`/`schedule` will never again be called"] # [doc = " once it has terminated (signaled emptyness or an error)."] # [doc = ""] # [doc = " Currently once a stream has returned `Some(Ok(None))` from `poll` any further"] # [doc = " calls could exhibit bad behavior such as block forever, panic, never"] # [doc = " return, etc. If it is known that `poll` may be called too often then"] # [doc = " this method can be used to ensure that it has defined semantics."] # [doc = ""] # [doc = " Once a stream has been `fuse`d and it terminates, then"] # [doc = " it will forever return `None` from `poll` again (never resolve). This,"] # [doc = " unlike the trait's `poll` method, is guaranteed."] # [doc = ""] # [doc = " Additionally, once a stream has completed, this `Fuse` combinator will"] # [doc = " never call `schedule` on the underlying stream."] fn fuse (self) -> Fuse < Self > where Self : Sized { fuse :: new (self) } # [doc = " An adaptor for creating a buffered list of pending futures."] # [doc = ""] # [doc = " If this stream's item can be converted into a future, then this adaptor"] # [doc = " will buffer up to `amt` futures and then return results in the order"] # [doc = " that the futures are completed. No more than `amt` futures will be"] # [doc = " buffered at any point in time, and less than `amt` may also be buffered"] # [doc = " depending on the state of each future."] # [doc = ""] # [doc = " The returned stream will be a stream of each future's result, with"] # [doc = " errors passed through whenever they occur."] fn buffered (self , amt : usize) -> Buffered < Self > where Self :: Item : IntoFuture < Error = < Self as Stream > :: Error > , Self : Sized { buffered :: new (self , amt) } # [doc = " An adapter for merging the output of two streams."] # [doc = ""] # [doc = " The merged stream produces items from one or both of the underlying"] # [doc = " streams as they become available. Errors, however, are not merged: you"] # [doc = " get at most one error at a time."] fn merge < S > (self , other : S) -> Merge < Self , S > where S : Stream < Error = Self :: Error > , Self : Sized , { merge :: new (self , other) } }
};
}
