// Generated macro for impl_67 (impl)
macro_rules! Depcrate_taskimpl_67 {
() => {
// Module: crate::task
// Provides: {"impl_67"}
// Dependencies: {}
impl Task { # [doc = " Creates a new task ready to drive a future."] pub fn new () -> Task { Task { handle : TaskHandle { inner : Arc :: new (Inner { slot : Slot :: new (None) , registered : AtomicBool :: new (false) , }) , } , _marker : marker :: PhantomData , } } # [doc = " Inserts a new piece of task-local data into this task, returning a"] # [doc = " reference to it."] # [doc = ""] # [doc = " Ownership of the data will be transferred to the task, and the data will"] # [doc = " be destroyed when the task itself is destroyed. The returned value can"] # [doc = " be passed to the `Task::{get, get_mut}` methods to get a reference back"] # [doc = " to the original data."] # [doc = ""] # [doc = " Note that the returned handle is cloneable and copyable and can be sent"] # [doc = " to other futures which will be associated with the same task. All"] # [doc = " futures will then have access to this data when passed the reference"] # [doc = " back."] pub fn insert < A > (& mut self , a : A) -> TaskData < A > where A : Any + Send + 'static , { TaskData { task_inner : self . inner_usize () , ptr : Arc :: new (UnsafeCell :: new (a)) , } } fn inner_usize (& self) -> usize { & * self . handle . inner as * const Inner as usize } # [doc = " Get a reference to the task-local data inside this task."] # [doc = ""] # [doc = " This method should be passed a handle previously returned by"] # [doc = " `Task::insert`. That handle, when passed back into this method, will"] # [doc = " retrieve a reference to the original data."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This method will panic if `data` does not belong to this task. That is,"] # [doc = " if another task generated the `data` handle passed in, this method will"] # [doc = " panic."] pub fn get < A > (& self , data : & TaskData < A >) -> & A { assert_eq ! (data . task_inner , self . inner_usize ()) ; unsafe { & * data . ptr . get () } } # [doc = " Get a mutable reference to the task-local data inside this task."] # [doc = ""] # [doc = " This method should be passed a handle previously returned by"] # [doc = " `Task::insert`. That handle, when passed back into this method, will"] # [doc = " retrieve a reference to the original data."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This method will panic if `data` does not belong to this task. That is,"] # [doc = " if another task generated the `data` handle passed in, this method will"] # [doc = " panic."] pub fn get_mut < A > (& mut self , data : & TaskData < A >) -> & mut A { assert_eq ! (data . task_inner , self . inner_usize ()) ; unsafe { & mut * data . ptr . get () } } # [doc = " During the `Future::schedule` method, notify to the task that a value is"] # [doc = " immediately ready."] # [doc = ""] # [doc = " This method, more optimized than `TaskHandle::notify`, will inform the"] # [doc = " task that the future which is being scheduled is immediately ready to be"] # [doc = " `poll`ed again."] pub fn notify (& mut self) { self . handle () . notify () } # [doc = " Gets a handle to this task which can be cloned to a piece of"] # [doc = " `Send+'static` data."] # [doc = ""] # [doc = " This handle returned can be used to notify the task that a future is"] # [doc = " ready to get polled again. The returned handle implements the `Clone`"] # [doc = " trait and all clones will refer to this same task."] # [doc = ""] # [doc = " Note that if data is immediately ready then the `Task::notify` method"] # [doc = " should be preferred."] pub fn handle (& self) -> & TaskHandle { & self . handle } # [doc = " Consumes this task to run a future to completion."] # [doc = ""] # [doc = " This function will consume the task provided and the task will be used"] # [doc = " to execute the `future` provided until it has been completed. The future"] # [doc = " wil be `poll`'ed until it is resolved, at which point the `Result<(),"] # [doc = " ()>` will be discarded."] # [doc = ""] # [doc = " The future will be `poll`ed on the threads that events arrive on. That"] # [doc = " is, this method does not attempt to control which thread a future is"] # [doc = " polled on."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Currently, if `poll` panics, then this method will propagate the panic"] # [doc = " to the thread that `poll` was called on. This is bad and it will change."] pub fn run (self , mut future : Box < Future < Item = () , Error = () > >) { let mut me = self ; loop { let result = catch_unwind (move | | { (future . poll (& mut me) , future , me) }) ; match result { Ok ((ref r , _ , _)) if r . is_ready () => return , Ok ((_ , f , t)) => { future = f ; me = t ; } Err (e) => panic :: resume_unwind (e) , } future = match future . tailcall () { Some (f) => f , None => future , } ; break } future . schedule (& mut me) ; let inner = me . handle . inner . clone () ; inner . slot . try_produce ((me , future)) . ok () . unwrap () ; } }
};
}
