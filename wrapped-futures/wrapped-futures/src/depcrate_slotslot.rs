// Generated macro for Slot (struct)
macro_rules! Depcrate_slotSlot {
() => {
// Module: crate::slot
// Provides: {"Slot"}
// Dependencies: {}
# [doc = " A slot in memory intended to represent the communication channel between one"] # [doc = " producer and one consumer."] # [doc = ""] # [doc = " Each slot contains space for a piece of data of type `T`, and can have"] # [doc = " callbacks registered to run when the slot is either full or empty."] # [doc = ""] # [doc = " Slots are only intended to be shared between exactly one producer and"] # [doc = " exactly one consumer. If there are multiple concurrent producers or"] # [doc = " consumers then this is still memory safe but will have unpredictable results"] # [doc = " (and maybe panics). Note that this does not require that the \"consumer\" is"] # [doc = " the same for the entire lifetime of a slot, simply that there is only one"] # [doc = " consumer at a time."] # [doc = ""] # [doc = " # Registering callbacks"] # [doc = ""] # [doc = " [`on_empty`](#method.on_empty) registers a callback to run when the slot"] # [doc = " becomes empty, and [`on_full`](#method.on_full) registers one to run when it"] # [doc = " becomes full. In both cases, the callback will run immediately if possible."] # [doc = ""] # [doc = " At most one callback can be registered at any given time: it is an error to"] # [doc = " attempt to register a callback with `on_full` if one is currently registered"] # [doc = " via `on_empty`, or any other combination."] # [doc = ""] # [doc = " # Cancellation"] # [doc = ""] # [doc = " Registering a callback returns a `Token` which can be used to"] # [doc = " [`cancel`](#method.cancel) the callback. Only callbacks that have not yet"] # [doc = " started running can be canceled. Canceling a callback that has already run"] # [doc = " is not an error, and `cancel` does not signal whether or not the callback"] # [doc = " was actually canceled to the caller."] pub struct Slot < T > { state : AtomicUsize , slot : Lock < Option < T > > , on_full : Lock < Option < Box < FnBox < T > > > > , on_empty : Lock < Option < Box < FnBox < T > > > > , }
};
}
