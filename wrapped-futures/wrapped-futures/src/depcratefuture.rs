// Generated macro for Future (trait)
macro_rules! DepcrateFuture {
() => {
// Module: crate
// Provides: {"Future"}
// Dependencies: {}
# [doc = " Trait for types which represent a placeholder of a value that will become"] # [doc = " available at possible some later point in time."] # [doc = ""] # [doc = " Futures are used to provide a sentinel through which a value can be"] # [doc = " referenced. They crucially allow chaining operations through consumption"] # [doc = " which allows expressing entire trees of computation as one sentinel value."] # [doc = ""] # [doc = " The ergonomics and implementation of the `Future` trait are very similar to"] # [doc = " the `Iterator` trait in Rust which is where there is a small handful of"] # [doc = " methods to implement and a load of default methods that consume a `Future`,"] # [doc = " producing a new value."] # [doc = ""] # [doc = " # Core methods"] # [doc = ""] # [doc = " The core methods of futures, currently `poll`, `schedule`, and `tailcall`,"] # [doc = " are not intended to be called in general. These are used to drive an entire"] # [doc = " task of many futures composed together only from the top level."] # [doc = ""] # [doc = " More documentation can be found on each method about what its purpose is,"] # [doc = " but in general all of the combinators are the main methods that should be"] # [doc = " used."] # [doc = ""] # [doc = " # Combinators"] # [doc = ""] # [doc = " Like iterators, futures provide a large number of combinators to work with"] # [doc = " futures to express computations in a much more natural method than"] # [doc = " scheduling a number of callbacks. For example the `map` method can change"] # [doc = " a `Future<Item=T>` to a `Future<Item=U>` or an `and_then` combinator could"] # [doc = " create a future after the first one is done and only be resolved when the"] # [doc = " second is done."] # [doc = ""] # [doc = " Combinators act very similarly to the methods on the `Iterator` trait itself"] # [doc = " or those on `Option` and `Result`. Like with iterators, the combinators are"] # [doc = " zero-cost and don't impose any extra layers of indirection you wouldn't"] # [doc = " otherwise have to write down."] pub trait Future : Send + 'static { # [doc = " The type of value that this future will resolved with if it is"] # [doc = " successful."] type Item : Send + 'static ; # [doc = " The type of error that this future will resolve with if it fails in a"] # [doc = " normal fashion."] # [doc = ""] # [doc = " Futures may also fail due to panics or cancellation, but that is"] # [doc = " expressed through the `PollError` type, not this type."] type Error : Send + 'static ; # [doc = " Query this future to see if its value has become available."] # [doc = ""] # [doc = " This function will check the internal state of the future and assess"] # [doc = " whether the value is ready to be produced. Implementors of this function"] # [doc = " should ensure that a call to this **never blocks** as event loops may"] # [doc = " not work properly otherwise."] # [doc = ""] # [doc = " Callers of this function must provide the \"task\" in which the future is"] # [doc = " running through the `task` argument. This task contains information like"] # [doc = " task-local variables which the future may have stored references to"] # [doc = " internally."] # [doc = ""] # [doc = " # Runtime characteristics"] # [doc = ""] # [doc = " This function, `poll`, is the primary method for 'making progress'"] # [doc = " within a tree of futures. For example this method will be called"] # [doc = " repeatedly as the internal state machine makes its various transitions."] # [doc = " Additionally, this function may not necessarily have many guarantees"] # [doc = " about *where* it's run (e.g. always on an I/O thread or not). Unless it"] # [doc = " is otherwise arranged to be so, it should be ensured that"] # [doc = " **implementations of this function finish very quickly**."] # [doc = ""] # [doc = " This prevents unnecessarily clogging up threads and/or event loops while"] # [doc = " a `poll` function call, for example, takes up compute resources to"] # [doc = " perform some expensive computation. If it is known ahead of time that a"] # [doc = " call to `poll` may end up taking awhile, the work should be offloaded to"] # [doc = " a thread pool (or something similar) to ensure that `poll` can return"] # [doc = " quickly."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " This function returns `Poll::NotReady` if the future is not ready yet,"] # [doc = " or `Poll::{Ok,Err}` with the result of this future if it's ready. Once"] # [doc = " a future has returned `Some` it is considered a contract error to"] # [doc = " continue polling it."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Once a future has completed (returned `Poll::{Ok, Err}` from `poll`),"] # [doc = " then any future calls to `poll` may panic, block forever, or otherwise"] # [doc = " cause wrong behavior. The `Future` trait itself provides no guarantees"] # [doc = " about the behavior of `poll` after `Some` has been returned at least"] # [doc = " once."] # [doc = ""] # [doc = " Callers who may call `poll` too many times may want to consider using"] # [doc = " the `fuse` adaptor which defines the behavior of `poll`, but comes with"] # [doc = " a little bit of extra cost."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This future may have failed to finish the computation, in which case"] # [doc = " the `Poll::Err` variant will be returned with an appropriate payload of"] # [doc = " an error."] fn poll (& mut self , task : & mut Task) -> Poll < Self :: Item , Self :: Error > ; # [doc = " Schedule a task to be notified when this future is ready."] # [doc = ""] # [doc = " Throughout the lifetime of a future it may frequently be `poll`'d on to"] # [doc = " test whether the value is ready yet. If `None` is returned, however, the"] # [doc = " caller may then register interest via this function to get a"] # [doc = " notification when the future can indeed make progress."] # [doc = ""] # [doc = " The `task` argument provided is the same task as provided to `poll`, and"] # [doc = " it's the overall task which is driving this future. The task will be"] # [doc = " notified through the `TaskHandle` type generated from the `handle`"] # [doc = " method, and spurious notifications are allowed. That is, it's ok for a"] # [doc = " notification to be received which when the future is poll'd it still"] # [doc = " isn't complete."] # [doc = ""] # [doc = " Implementors of the `Future` trait are recommended to just blindly pass"] # [doc = " around this task rather than attempt to manufacture new tasks."] # [doc = ""] # [doc = " When the `task` is notified it will be provided a set of tokens that"] # [doc = " represent the set of events which have happened since it was last called"] # [doc = " (or the last call to `poll`). These events can then be used by the task"] # [doc = " to later inform `poll` calls to not poll too much."] # [doc = ""] # [doc = " # Multiple calls to `schedule`"] # [doc = ""] # [doc = " This function cannot be used to queue up multiple tasks to be notified"] # [doc = " when a future is ready to make progress. Only the most recent call to"] # [doc = " `schedule` is guaranteed to have notifications received when `schedule`"] # [doc = " is called multiple times."] # [doc = ""] # [doc = " If this function is called twice, it may be the case that the previous"] # [doc = " task is never notified. It is recommended that this function is called"] # [doc = " with the same task for the entire lifetime of this future."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Once a future has returned `Some` (it's been completed) then future"] # [doc = " calls to either `poll` or this function, `schedule`, should not be"] # [doc = " expected to behave well. A call to `schedule` after a poll has succeeded"] # [doc = " may panic, block forever, or otherwise exhibit odd behavior."] # [doc = ""] # [doc = " Callers who may call `schedule` after a future is finished may want to"] # [doc = " consider using the `fuse` adaptor which defines the behavior of"] # [doc = " `schedule` after a successful poll, but comes with a little bit of"] # [doc = " extra cost."] fn schedule (& mut self , task : & mut Task) ; # [doc = " Perform tail-call optimization on this future."] # [doc = ""] # [doc = " A particular future may actually represent a large tree of computation,"] # [doc = " the structure of which can be optimized periodically after some of the"] # [doc = " work has completed. This function is intended to be called after an"] # [doc = " unsuccessful `poll` to ensure that the computation graph of a future"] # [doc = " remains at a reasonable size."] # [doc = ""] # [doc = " This function is intended to be idempotent. If `None` is returned then"] # [doc = " the internal structure may have been optimized, but this future itself"] # [doc = " must stick around to represent the computation at hand."] # [doc = ""] # [doc = " If `Some` is returned then the returned future will be realized with the"] # [doc = " same value that this future *would* have been had this method not been"] # [doc = " called. Essentially, if `Some` is returned, then this future can be"] # [doc = " forgotten and instead the returned value is used."] # [doc = ""] # [doc = " Note that this is a default method which returns `None`, but any future"] # [doc = " adaptor should implement it to flatten the underlying future, if any."] fn tailcall (& mut self) -> Option < Box < Future < Item = Self :: Item , Error = Self :: Error > > > { None } # [doc = " Convenience function for turning this future into a trait object."] # [doc = ""] # [doc = " This simply avoids the need to write `Box::new` and can often help with"] # [doc = " type inference as well by always returning a trait object."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::*;"] # [doc = ""] # [doc = " let a: Box<Future<Item=i32, Error=i32>> = done(Ok(1)).boxed();"] # [doc = " ```"] fn boxed (self) -> Box < Future < Item = Self :: Item , Error = Self :: Error > > where Self : Sized { Box :: new (self) } # [doc = " Map this future's result to a different type, returning a new future of"] # [doc = " the resulting type."] # [doc = ""] # [doc = " This function is similar to the `Option::map` or `Iterator::map` where"] # [doc = " it will change the type of the underlying future. This is useful to"] # [doc = " chain along a computation once a future has been resolved."] # [doc = ""] # [doc = " The closure provided will only be called if this future is resolved"] # [doc = " successfully. If this future returns an error, panics, or is canceled,"] # [doc = " then the closure provided will never be invoked."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it, similar to the existing `map` methods in the"] # [doc = " standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::*;"] # [doc = ""] # [doc = " let future_of_1 = finished::<u32, u32>(1);"] # [doc = " let future_of_4 = future_of_1.map(|x| x + 3);"] # [doc = " ```"] fn map < F , U > (self , f : F) -> Map < Self , F > where F : FnOnce (Self :: Item) -> U + Send + 'static , U : Send + 'static , Self : Sized , { assert_future :: < U , Self :: Error , _ > (map :: new (self , f)) } # [doc = " Map this future's error to a different error, returning a new future."] # [doc = ""] # [doc = " This function is similar to the `Result::map_err` where it will change"] # [doc = " the error type of the underlying future. This is useful for example to"] # [doc = " ensure that futures have the same error type when used with combinators"] # [doc = " like `select` and `join`."] # [doc = ""] # [doc = " The closure provided will only be called if this future is resolved"] # [doc = " with an error. If this future returns a success, panics, or is"] # [doc = " canceled, then the closure provided will never be invoked."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::*;"] # [doc = ""] # [doc = " let future_of_err_1 = failed::<u32, u32>(1);"] # [doc = " let future_of_err_4 = future_of_err_1.map_err(|x| x + 3);"] # [doc = " ```"] fn map_err < F , E > (self , f : F) -> MapErr < Self , F > where F : FnOnce (Self :: Error) -> E + Send + 'static , E : Send + 'static , Self : Sized , { assert_future :: < Self :: Item , E , _ > (map_err :: new (self , f)) } # [doc = " Chain on a computation for when a future finished, passing the result of"] # [doc = " the future to the provided closure `f`."] # [doc = ""] # [doc = " This function can be used to ensure a computation runs regardless of"] # [doc = " the conclusion of the future. The closure provided will be yielded a"] # [doc = " `Result` once the future is complete."] # [doc = ""] # [doc = " The returned value of the closure must implement the `IntoFuture` trait"] # [doc = " and can represent some more work to be done before the composed future"] # [doc = " is finished. Note that the `Result` type implements the `IntoFuture`"] # [doc = " trait so it is possible to simply alter the `Result` yielded to the"] # [doc = " closure and return it."] # [doc = ""] # [doc = " If this future is canceled or panics then the closure `f` will not be"] # [doc = " run."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::*;"] # [doc = ""] # [doc = " let future_of_1 = finished::<u32, u32>(1);"] # [doc = " let future_of_4 = future_of_1.then(|x| {"] # [doc = "     x.map(|y| y + 3)"] # [doc = " });"] # [doc = ""] # [doc = " let future_of_err_1 = failed::<u32, u32>(1);"] # [doc = " let future_of_4 = future_of_err_1.then(|x| {"] # [doc = "     match x {"] # [doc = "         Ok(_) => panic!(\"expected an error\"),"] # [doc = "         Err(y) => finished::<u32, u32>(y + 3),"] # [doc = "     }"] # [doc = " });"] # [doc = " ```"] fn then < F , B > (self , f : F) -> Then < Self , B , F > where F : FnOnce (Result < Self :: Item , Self :: Error >) -> B + Send + 'static , B : IntoFuture , Self : Sized , { assert_future :: < B :: Item , B :: Error , _ > (then :: new (self , f)) } # [doc = " Execute another future after this one has resolved successfully."] # [doc = ""] # [doc = " This function can be used to chain two futures together and ensure that"] # [doc = " the final future isn't resolved until both have finished. The closure"] # [doc = " provided is yielded the successful result of this future and returns"] # [doc = " another value which can be converted into a future."] # [doc = ""] # [doc = " Note that because `Result` implements the `IntoFuture` trait this method"] # [doc = " can also be useful for chaining fallible and serial computations onto"] # [doc = " the end of one future."] # [doc = ""] # [doc = " If this future is canceled, panics, or completes with an error then the"] # [doc = " provided closure `f` is never called."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::*;"] # [doc = ""] # [doc = " let future_of_1 = finished::<u32, u32>(1);"] # [doc = " let future_of_4 = future_of_1.and_then(|x| {"] # [doc = "     Ok(x + 3)"] # [doc = " });"] # [doc = ""] # [doc = " let future_of_err_1 = failed::<u32, u32>(1);"] # [doc = " future_of_err_1.and_then(|_| -> Done<u32, u32> {"] # [doc = "     panic!(\"should not be called in case of an error\");"] # [doc = " });"] # [doc = " ```"] fn and_then < F , B > (self , f : F) -> AndThen < Self , B , F > where F : FnOnce (Self :: Item) -> B + Send + 'static , B : IntoFuture < Error = Self :: Error > , Self : Sized , { assert_future :: < B :: Item , Self :: Error , _ > (and_then :: new (self , f)) } # [doc = " Execute another future after this one has resolved with an error."] # [doc = ""] # [doc = " This function can be used to chain two futures together and ensure that"] # [doc = " the final future isn't resolved until both have finished. The closure"] # [doc = " provided is yielded the error of this future and returns another value"] # [doc = " which can be converted into a future."] # [doc = ""] # [doc = " Note that because `Result` implements the `IntoFuture` trait this method"] # [doc = " can also be useful for chaining fallible and serial computations onto"] # [doc = " the end of one future."] # [doc = ""] # [doc = " If this future is canceled, panics, or completes successfully then the"] # [doc = " provided closure `f` is never called."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::*;"] # [doc = ""] # [doc = " let future_of_err_1 = failed::<u32, u32>(1);"] # [doc = " let future_of_4 = future_of_err_1.or_else(|x| -> Result<u32, u32> {"] # [doc = "     Ok(x + 3)"] # [doc = " });"] # [doc = ""] # [doc = " let future_of_1 = finished::<u32, u32>(1);"] # [doc = " future_of_1.or_else(|_| -> Done<u32, u32> {"] # [doc = "     panic!(\"should not be called in case of success\");"] # [doc = " });"] # [doc = " ```"] fn or_else < F , B > (self , f : F) -> OrElse < Self , B , F > where F : FnOnce (Self :: Error) -> B + Send + 'static , B : IntoFuture < Item = Self :: Item > , Self : Sized , { assert_future :: < Self :: Item , B :: Error , _ > (or_else :: new (self , f)) } # [doc = " Waits for either one of two futures to complete."] # [doc = ""] # [doc = " This function will return a new future which awaits for either this or"] # [doc = " the `other` future to complete. The returned future will finish with"] # [doc = " both the value resolved and a future representing the completion of the"] # [doc = " other work. Both futures must have the same item and error type."] # [doc = ""] # [doc = " If either future is canceled or panics, the other is canceled and the"] # [doc = " original error is propagated upwards."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::*;"] # [doc = ""] # [doc = " // A poor-man's join implemented on top of select"] # [doc = ""] # [doc = " fn join<A>(a: A, b: A)"] # [doc = "            -> Box<Future<Item=(A::Item, A::Item), Error=A::Error>>"] # [doc = "     where A: Future,"] # [doc = " {"] # [doc = "     a.select(b).then(|res| {"] # [doc = "         match res {"] # [doc = "             Ok((a, b)) => b.map(|b| (a, b)).boxed(),"] # [doc = "             Err((a, _)) => failed(a).boxed(),"] # [doc = "         }"] # [doc = "     }).boxed()"] # [doc = " }"] # [doc = " ```"] fn select < B > (self , other : B) -> Select < Self , B :: Future > where B : IntoFuture < Item = Self :: Item , Error = Self :: Error > , Self : Sized , { let f = select :: new (self , other . into_future ()) ; assert_future :: < (Self :: Item , SelectNext < Self , B :: Future >) , (Self :: Error , SelectNext < Self , B :: Future >) , _ > (f) } # [doc = " Joins the result of two futures, waiting for them both to complete."] # [doc = ""] # [doc = " This function will return a new future which awaits both this and the"] # [doc = " `other` future to complete. The returned future will finish with a tuple"] # [doc = " of both results."] # [doc = ""] # [doc = " Both futures must have the same error type, and if either finishes with"] # [doc = " an error then the other will be canceled and that error will be"] # [doc = " returned."] # [doc = ""] # [doc = " If either future is canceled or panics, the other is canceled and the"] # [doc = " original error is propagated upwards."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::*;"] # [doc = ""] # [doc = " let a = finished::<u32, u32>(1);"] # [doc = " let b = finished::<u32, u32>(2);"] # [doc = " let pair = a.join(b);"] # [doc = ""] # [doc = " pair.map(|(a, b)| {"] # [doc = "     assert_eq!(a, 1);"] # [doc = "     assert_eq!(b, 1);"] # [doc = " });"] # [doc = " ```"] fn join < B > (self , other : B) -> Join < Self , B :: Future > where B : IntoFuture < Error = Self :: Error > , Self : Sized , { let f = join :: new (self , other . into_future ()) ; assert_future :: < (Self :: Item , B :: Item) , Self :: Error , _ > (f) } join ! { (join3 , Join3 , new3 , < b : B , c : C >) , (join4 , Join4 , new4 , < b : B , c : C , d : D >) , (join5 , Join5 , new5 , < b : B , c : C , d : D , e : E >) , } # [doc = " Flatten the execution of this future when the successful result of this"] # [doc = " future is itself another future."] # [doc = ""] # [doc = " This can be useful when combining futures together to flatten the"] # [doc = " computation out the the final result. This method can only be called"] # [doc = " when the successful result of this future itself implements the"] # [doc = " `IntoFuture` trait and the error can be created from this future's error"] # [doc = " type."] # [doc = ""] # [doc = " This method is equivalent to `self.then(|x| x)`."] # [doc = ""] # [doc = " Note that this function consumes the receiving future and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures::*;"] # [doc = ""] # [doc = " let future_of_a_future = finished::<_, u32>(finished::<u32, u32>(1));"] # [doc = " let future_of_1 = future_of_a_future.flatten();"] # [doc = " ```"] fn flatten (self) -> Flatten < Self > where Self :: Item : IntoFuture , < < Self as Future > :: Item as IntoFuture > :: Error : From < < Self as Future > :: Error > , Self : Sized { let f = flatten :: new (self) ; assert_future :: < < < Self as Future > :: Item as IntoFuture > :: Item , < < Self as Future > :: Item as IntoFuture > :: Error , _ > (f) } # [doc = " Fuse a future such that `poll` will never again be called once it has"] # [doc = " returned a success."] # [doc = ""] # [doc = " Currently once a future has returned `Some` from `poll` any further"] # [doc = " calls could exhibit bad behavior such as block forever, panic, never"] # [doc = " return, etc. If it is known that `poll` may be called too often then"] # [doc = " this method can be used to ensure that it has defined semantics."] # [doc = ""] # [doc = " Once a future has been `fuse`d and it returns success from `poll`, then"] # [doc = " it will forever return `None` from `poll` again (never resolve). This,"] # [doc = " unlike the trait's `poll` method, is guaranteed."] # [doc = ""] # [doc = " Additionally, once a future has completed, this `Fuse` combinator will"] # [doc = " ensure that all registered callbacks will not be registered with the"] # [doc = " underlying future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use futures::*;"] # [doc = ""] # [doc = " let mut task = Task::new();"] # [doc = " let mut future = finished::<i32, u32>(2);"] # [doc = " assert!(future.poll(&mut task).is_ready());"] # [doc = ""] # [doc = " // Normally, a call such as this would panic:"] # [doc = " //future.poll(&mut task);"] # [doc = ""] # [doc = " // This, however, is guaranteed to not panic"] # [doc = " let mut future = finished::<i32, u32>(2).fuse();"] # [doc = " assert!(future.poll(&mut task).is_ready());"] # [doc = " assert!(future.poll(&mut task).is_not_ready());"] # [doc = " ```"] fn fuse (self) -> Fuse < Self > where Self : Sized { let f = fuse :: new (self) ; assert_future :: < Self :: Item , Self :: Error , _ > (f) } # [doc = " Consume this future and allow it to execute without cancelling it."] # [doc = ""] # [doc = " Normally whenever a future is dropped it signals that the underlying"] # [doc = " computation should be cancelled ASAP. This function, however, will"] # [doc = " consume the future and arrange for the future itself to get dropped only"] # [doc = " when the computation has completed."] # [doc = ""] # [doc = " This function can be useful to ensure that futures with side effects can"] # [doc = " run \"in the background\", but it is discouraged as it doesn't allow any"] # [doc = " control over the future in terms of cancellation."] # [doc = ""] # [doc = " Generally applications should retain handles on futures to ensure"] # [doc = " they're properly cleaned up if something unexpected happens."] fn forget (self) where Self : Sized { forget :: forget (self) ; } }
};
}
