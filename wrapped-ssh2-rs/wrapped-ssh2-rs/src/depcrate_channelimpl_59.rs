// Generated macro for impl_59 (impl)
macro_rules! Depcrate_channelimpl_59 {
() => {
// Module: crate::channel
// Provides: {"impl_59"}
// Dependencies: {}
impl Channel { # [doc = " Set an environment variable in the remote channel's process space."] # [doc = ""] # [doc = " Note that this does not make sense for all channel types and may be"] # [doc = " ignored by the server despite returning success."] pub fn setenv (& mut self , var : & str , val : & str) -> Result < () , Error > { let var = CString :: new (var) ? ; let var = var . as_bytes () ; let val = CString :: new (val) ? ; let val = val . as_bytes () ; let locked = self . lock () ; unsafe { locked . sess . rc (raw :: libssh2_channel_setenv_ex (locked . raw , var . as_ptr () as * const _ , var . len () as c_uint , val . as_ptr () as * const _ , val . len () as c_uint ,)) } } # [doc = " Request a PTY on an established channel."] # [doc = ""] # [doc = " Note that this does not make sense for all channel types and may be"] # [doc = " ignored by the server despite returning success."] # [doc = ""] # [doc = " The dimensions argument is a tuple of (width, height, width_px,"] # [doc = " height_px)"] # [doc = ""] # [doc = " The mode parameter is optional and specifies modes to apply to"] # [doc = " the pty.  Use the `PtyModes` type construct these modes."] # [doc = " A contrived example of this is below:"] # [doc = ""] # [doc = " ```"] # [doc = " let mut mode = ssh2::PtyModes::new();"] # [doc = " // Set the interrupt character to CTRL-C (ASCII 3: ETX)."] # [doc = " // This is typically the default, but we're showing how to"] # [doc = " // set a relatable option for the sake of example!"] # [doc = " mode.set_character(ssh2::PtyModeOpcode::VINTR, Some(3 as char));"] # [doc = " ```"] pub fn request_pty (& mut self , term : & str , mode : Option < PtyModes > , dim : Option < (u32 , u32 , u32 , u32) > ,) -> Result < () , Error > { let term = CString :: new (term) ? ; let term = term . as_bytes () ; let locked = self . lock () ; let mode = mode . map (PtyModes :: finish) ; let mode = mode . as_ref () . map (Vec :: as_slice) . unwrap_or (& []) ; locked . sess . rc (unsafe { let (width , height , width_px , height_px) = dim . unwrap_or ((80 , 24 , 0 , 0)) ; raw :: libssh2_channel_request_pty_ex (locked . raw , term . as_ptr () as * const _ , term . len () as c_uint , mode . as_ptr () as * const _ , mode . len () as c_uint , width as c_int , height as c_int , width_px as c_int , height_px as c_int ,) }) } # [doc = " Request that the PTY size be changed to the specified size."] # [doc = " width and height are the number of character cells, and you"] # [doc = " may optionally include the size specified in pixels."] pub fn request_pty_size (& mut self , width : u32 , height : u32 , width_px : Option < u32 > , height_px : Option < u32 > ,) -> Result < () , Error > { let locked = self . lock () ; let width_px = width_px . unwrap_or (0) ; let height_px = height_px . unwrap_or (0) ; locked . sess . rc (unsafe { raw :: libssh2_channel_request_pty_size_ex (locked . raw , width as c_int , height as c_int , width_px as c_int , height_px as c_int ,) }) } # [doc = " Requests that the remote host start an authentication agent;"] # [doc = " if successful requests to that agent will be forwarded from"] # [doc = " the server back to the local authentication agent on the client side."] # [doc = ""] # [doc = " Note that some hosts are configured to disallow agent forwarding,"] # [doc = " and that even if enabled, there is a possibility that starting"] # [doc = " the agent on the remote system can fail."] pub fn request_auth_agent_forwarding (& mut self) -> Result < () , Error > { let locked = self . lock () ; locked . sess . rc (unsafe { raw :: libssh2_channel_request_auth_agent (locked . raw) }) } # [doc = " Execute a command"] # [doc = ""] # [doc = " An execution is one of the standard process services defined by the SSH2"] # [doc = " protocol."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```no_run"] # [doc = " # use std::io::prelude::*;"] # [doc = " # use ssh2::Session;"] # [doc = " # let session: Session = panic!();"] # [doc = " let mut channel = session.channel_session().unwrap();"] # [doc = " channel.exec(\"ls\").unwrap();"] # [doc = " let mut s = String::new();"] # [doc = " channel.read_to_string(&mut s).unwrap();"] # [doc = " println!(\"{}\", s);"] # [doc = " ```"] pub fn exec (& mut self , command : & str) -> Result < () , Error > { self . process_startup ("exec" , Some (command)) } # [doc = " Start a shell"] # [doc = ""] # [doc = " A shell is one of the standard process services defined by the SSH2"] # [doc = " protocol."] pub fn shell (& mut self) -> Result < () , Error > { self . process_startup ("shell" , None) } # [doc = " Request a subsystem be started."] # [doc = ""] # [doc = " A subsystem is one of the standard process services defined by the SSH2"] # [doc = " protocol."] pub fn subsystem (& mut self , system : & str) -> Result < () , Error > { self . process_startup ("subsystem" , Some (system)) } # [doc = " Initiate a request on a session type channel."] # [doc = ""] # [doc = " The SSH2 protocol currently defines shell, exec, and subsystem as"] # [doc = " standard process services."] pub fn process_startup (& mut self , request : & str , message : Option < & str >) -> Result < () , Error > { let message = message . map (| s | CString :: new (s)) . transpose () ? ; let (message , message_len) = message . as_ref () . map (| s | (s . as_ptr () , s . as_bytes () . len ())) . unwrap_or ((null () , 0)) ; let locked = self . lock () ; unsafe { let rc = raw :: libssh2_channel_process_startup (locked . raw , request . as_ptr () as * const _ , request . len () as c_uint , message , message_len as c_uint ,) ; locked . sess . rc (rc) } } # [doc = " Get a handle to the stderr stream of this channel."] # [doc = ""] # [doc = " The returned handle implements the `Read` and `Write` traits."] pub fn stderr (& self) -> Stream { self . stream (:: EXTENDED_DATA_STDERR) } # [doc = " Get a handle to a particular stream for this channel."] # [doc = ""] # [doc = " The returned handle implements the `Read` and `Write` traits."] # [doc = ""] # [doc = " Groups of substreams may be flushed by passing one of the following"] # [doc = " constants and then calling `flush()`."] # [doc = ""] # [doc = " * FLUSH_EXTENDED_DATA - Flush all extended data substreams"] # [doc = " * FLUSH_ALL - Flush all substreams"] pub fn stream (& self , stream_id : i32) -> Stream { Stream { channel_inner : Arc :: clone (& self . channel_inner) , id : stream_id , } } # [doc = " Change how extended data (such as stderr) is handled"] pub fn handle_extended_data (& mut self , mode : ExtendedData) -> Result < () , Error > { let locked = self . lock () ; unsafe { let rc = raw :: libssh2_channel_handle_extended_data2 (locked . raw , mode as c_int) ; locked . sess . rc (rc) } } # [doc = " Returns the exit code raised by the process running on the remote host"] # [doc = " at the other end of the named channel."] # [doc = ""] # [doc = " Note that the exit status may not be available if the remote end has not"] # [doc = " yet set its status to closed."] pub fn exit_status (& self) -> Result < i32 , Error > { let locked = self . lock () ; Ok (unsafe { raw :: libssh2_channel_get_exit_status (locked . raw) }) } # [doc = " Get the remote exit signal."] pub fn exit_signal (& self) -> Result < ExitSignal , Error > { let locked = self . lock () ; unsafe { let mut sig = null_mut () ; let mut siglen = 0 ; let mut msg = null_mut () ; let mut msglen = 0 ; let mut lang = null_mut () ; let mut langlen = 0 ; let rc = raw :: libssh2_channel_get_exit_signal (locked . raw , & mut sig , & mut siglen , & mut msg , & mut msglen , & mut lang , & mut langlen ,) ; locked . sess . rc (rc) ? ; return Ok (ExitSignal { exit_signal : convert (& locked , sig , siglen) , error_message : convert (& locked , msg , msglen) , lang_tag : convert (& locked , lang , langlen) , }) ; } unsafe fn convert (locked : & LockedChannel , ptr : * mut c_char , len : size_t) -> Option < String > { if ptr . is_null () { return None ; } let slice = slice :: from_raw_parts (ptr as * const u8 , len as usize) ; let ret = slice . to_vec () ; raw :: libssh2_free (locked . sess . raw , ptr as * mut c_void) ; String :: from_utf8 (ret) . ok () } } # [doc = " Check the status of the read window."] pub fn read_window (& self) -> ReadWindow { let locked = self . lock () ; unsafe { let mut avail = 0 ; let mut init = 0 ; let remaining = raw :: libssh2_channel_window_read_ex (locked . raw , & mut avail , & mut init) ; ReadWindow { remaining : remaining as u32 , available : avail as u32 , window_size_initial : init as u32 , } } } # [doc = " Check the status of the write window."] pub fn write_window (& self) -> WriteWindow { let locked = self . lock () ; unsafe { let mut init = 0 ; let remaining = raw :: libssh2_channel_window_write_ex (locked . raw , & mut init) ; WriteWindow { remaining : remaining as u32 , window_size_initial : init as u32 , } } } # [doc = " Adjust the receive window for a channel by adjustment bytes."] # [doc = ""] # [doc = " If the amount to be adjusted is less than the minimum adjustment and"] # [doc = " force is false, the adjustment amount will be queued for a later packet."] # [doc = ""] # [doc = " This function returns the new size of the receive window (as understood"] # [doc = " by remote end) on success."] pub fn adjust_receive_window (& mut self , adjust : u64 , force : bool) -> Result < u64 , Error > { let locked = self . lock () ; let mut ret = 0 ; let rc = unsafe { raw :: libssh2_channel_receive_window_adjust2 (locked . raw , adjust as c_ulong , force as c_uchar , & mut ret ,) } ; locked . sess . rc (rc) ? ; Ok (ret as u64) } # [doc = " Artificially limit the number of bytes that will be read from this"] # [doc = " channel. Hack intended for use by scp_recv only."] # [doc (hidden)] pub (crate) fn limit_read (& mut self , limit : u64) { * self . channel_inner . read_limit . lock () = Some (limit) ; } # [doc = " Check if the remote host has sent an EOF status for the channel."] # [doc = " Take care: the EOF status is for the entire channel which can be confusing"] # [doc = " because the reading from the channel reads only the stdout stream."] # [doc = " unread, buffered, stderr data will cause eof() to return false."] pub fn eof (& self) -> bool { let locked = self . lock () ; * self . channel_inner . read_limit . lock () == Some (0) || unsafe { raw :: libssh2_channel_eof (locked . raw) != 0 } } # [doc = " Tell the remote host that no further data will be sent on the specified"] # [doc = " channel."] # [doc = ""] # [doc = " Processes typically interpret this as a closed stdin descriptor."] pub fn send_eof (& mut self) -> Result < () , Error > { let locked = self . lock () ; unsafe { locked . sess . rc (raw :: libssh2_channel_send_eof (locked . raw)) } } # [doc = " Wait for the remote end to send EOF."] # [doc = " Note that unread buffered stdout and stderr will cause this function"] # [doc = " to return `Ok(())` without waiting."] # [doc = " You should call the eof() function after calling this to check the"] # [doc = " status of the channel."] pub fn wait_eof (& mut self) -> Result < () , Error > { let locked = self . lock () ; unsafe { locked . sess . rc (raw :: libssh2_channel_wait_eof (locked . raw)) } } # [doc = " Close an active data channel."] # [doc = ""] # [doc = " In practice this means sending an SSH_MSG_CLOSE packet to the remote"] # [doc = " host which serves as instruction that no further data will be sent to"] # [doc = " it. The remote host may still send data back until it sends its own"] # [doc = " close message in response."] # [doc = ""] # [doc = " To wait for the remote end to close its connection as well, follow this"] # [doc = " command with `wait_close`"] pub fn close (& mut self) -> Result < () , Error > { let locked = self . lock () ; unsafe { locked . sess . rc (raw :: libssh2_channel_close (locked . raw)) } } # [doc = " Enter a temporary blocking state until the remote host closes the named"] # [doc = " channel."] # [doc = ""] # [doc = " Typically sent after `close` in order to examine the exit status."] pub fn wait_close (& mut self) -> Result < () , Error > { let locked = self . lock () ; unsafe { locked . sess . rc (raw :: libssh2_channel_wait_closed (locked . raw)) } } }
};
}
