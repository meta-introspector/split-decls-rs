// Generated macro for impl_97 (impl)
macro_rules! Depcrate_knownhostsimpl_97 {
() => {
// Module: crate::knownhosts
// Provides: {"impl_97"}
// Dependencies: {}
impl KnownHosts { pub (crate) fn from_raw_opt (raw : * mut raw :: LIBSSH2_KNOWNHOSTS , err : Option < Error > , sess : & Arc < Mutex < SessionInner > > ,) -> Result < Self , Error > { if raw . is_null () { Err (err . unwrap_or_else (Error :: unknown)) } else { Ok (Self { raw , sess : Arc :: clone (sess) , }) } } # [doc = " Reads a collection of known hosts from a specified file and adds them to"] # [doc = " the collection of known hosts."] pub fn read_file (& mut self , file : & Path , kind : KnownHostFileKind) -> Result < u32 , Error > { let file = CString :: new (util :: path2bytes (file) ?) ? ; let sess = self . sess . lock () ; let n = unsafe { raw :: libssh2_knownhost_readfile (self . raw , file . as_ptr () , kind as c_int) } ; if n < 0 { sess . rc (n) ? } Ok (n as u32) } # [doc = " Read a line as if it were from a known hosts file."] pub fn read_str (& mut self , s : & str , kind : KnownHostFileKind) -> Result < () , Error > { let s = CString :: new (s) ? ; let s = s . as_bytes () ; let sess = self . sess . lock () ; sess . rc (unsafe { raw :: libssh2_knownhost_readline (self . raw , s . as_ptr () as * const _ , s . len () as size_t , kind as c_int ,) }) } # [doc = " Writes all the known hosts to the specified file using the specified"] # [doc = " file format."] pub fn write_file (& self , file : & Path , kind : KnownHostFileKind) -> Result < () , Error > { let file = CString :: new (util :: path2bytes (file) ?) ? ; let sess = self . sess . lock () ; let n = unsafe { raw :: libssh2_knownhost_writefile (self . raw , file . as_ptr () , kind as c_int) } ; sess . rc (n) } # [doc = " Converts a single known host to a single line of output for storage,"] # [doc = " using the 'type' output format."] pub fn write_string (& self , host : & Host , kind : KnownHostFileKind) -> Result < String , Error > { let mut v = Vec :: with_capacity (128) ; let sess = self . sess . lock () ; let raw_host = self . resolve_to_raw_host (& sess , host) ? . ok_or_else (| | { Error :: new (ErrorCode :: Session (raw :: LIBSSH2_ERROR_BAD_USE) , "Host is not in the set of known hosts" ,) }) ? ; loop { let mut outlen = 0 ; unsafe { let rc = raw :: libssh2_knownhost_writeline (self . raw , raw_host , v . as_mut_ptr () as * mut _ , v . capacity () as size_t , & mut outlen , kind as c_int ,) ; if rc == raw :: LIBSSH2_ERROR_BUFFER_TOO_SMALL { v . reserve (outlen as usize + 1) ; } else { sess . rc (rc) ? ; v . set_len (outlen as usize) ; break ; } } } Ok (String :: from_utf8 (v) . unwrap ()) } # [doc = " Create an iterator over all of the known hosts in this structure."] pub fn iter (& self) -> Result < Vec < Host > , Error > { self . hosts () } # [doc = " Retrieves the list of known hosts"] pub fn hosts (& self) -> Result < Vec < Host > , Error > { let mut next = null_mut () ; let mut prev = null_mut () ; let sess = self . sess . lock () ; let mut hosts = vec ! [] ; loop { match unsafe { raw :: libssh2_knownhost_get (self . raw , & mut next , prev) } { 0 => { prev = next ; hosts . push (unsafe { Host :: from_raw (next) }) ; } 1 => break , rc => return Err (Error :: from_session_error_raw (sess . raw , rc)) , } } Ok (hosts) } # [doc = " Given a Host object, find the matching raw node in the internal list."] # [doc = " The returned value is only valid while the session is locked."] fn resolve_to_raw_host (& self , sess : & MutexGuard < SessionInner > , host : & Host ,) -> Result < Option < * mut raw :: libssh2_knownhost > , Error > { let mut next = null_mut () ; let mut prev = null_mut () ; loop { match unsafe { raw :: libssh2_knownhost_get (self . raw , & mut next , prev) } { 0 => { prev = next ; let current = unsafe { Host :: from_raw (next) } ; if current == * host { return Ok (Some (next)) ; } } 1 => break , rc => return Err (Error :: from_session_error_raw (sess . raw , rc)) , } } Ok (None) } # [doc = " Delete a known host entry from the collection of known hosts."] pub fn remove (& self , host : & Host) -> Result < () , Error > { let sess = self . sess . lock () ; if let Some (raw_host) = self . resolve_to_raw_host (& sess , host) ? { return sess . rc (unsafe { raw :: libssh2_knownhost_del (self . raw , raw_host) }) ; } else { Ok (()) } } # [doc = " Checks a host and its associated key against the collection of known"] # [doc = " hosts, and returns info back about the (partially) matched entry."] # [doc = ""] # [doc = " The host name can be the IP numerical address of the host or the full"] # [doc = " name. The key must be the raw data of the key."] pub fn check (& self , host : & str , key : & [u8]) -> CheckResult { self . check_port_ (host , - 1 , key) } # [doc = " Same as `check`, but takes a port as well."] pub fn check_port (& self , host : & str , port : u16 , key : & [u8]) -> CheckResult { self . check_port_ (host , port as i32 , key) } fn check_port_ (& self , host : & str , port : i32 , key : & [u8]) -> CheckResult { let host = CString :: new (host) . unwrap () ; let flags = raw :: LIBSSH2_KNOWNHOST_TYPE_PLAIN | raw :: LIBSSH2_KNOWNHOST_KEYENC_RAW ; unsafe { let rc = raw :: libssh2_knownhost_checkp (self . raw , host . as_ptr () , port as c_int , key . as_ptr () as * const _ , key . len () as size_t , flags , null_mut () ,) ; match rc { raw :: LIBSSH2_KNOWNHOST_CHECK_MATCH => CheckResult :: Match , raw :: LIBSSH2_KNOWNHOST_CHECK_MISMATCH => CheckResult :: Mismatch , raw :: LIBSSH2_KNOWNHOST_CHECK_NOTFOUND => CheckResult :: NotFound , _ => CheckResult :: Failure , } } } # [doc = " Adds a known host to the collection of known hosts."] # [doc = ""] # [doc = " The host is the host name in plain text. The host name can be the IP"] # [doc = " numerical address of the host or the full name. If you want to add a key"] # [doc = " for a specific port number for the given host, you must provide the host"] # [doc = " name like `\"[host]:port\"` with the actual characters `[` and `]` enclosing"] # [doc = " the host name and a colon separating the host part from the port number."] # [doc = " For example: `\"[host.example.com]:222\"`."] # [doc = ""] # [doc = " The key provided must be the raw key for the host."] pub fn add (& mut self , host : & str , key : & [u8] , comment : & str , fmt : :: KnownHostKeyFormat ,) -> Result < () , Error > { let host = CString :: new (host) ? ; let comment = CString :: new (comment) ? ; let flags = raw :: LIBSSH2_KNOWNHOST_TYPE_PLAIN | raw :: LIBSSH2_KNOWNHOST_KEYENC_RAW | (fmt as c_int) ; let sess = self . sess . lock () ; unsafe { let rc = raw :: libssh2_knownhost_addc (self . raw , host . as_ptr () as * mut _ , null_mut () , key . as_ptr () as * mut _ , key . len () as size_t , comment . as_ptr () as * const _ , comment . as_bytes () . len () as size_t , flags , null_mut () ,) ; sess . rc (rc) } } }
};
}
