// Generated macro for tests (module)
macro_rules! Depcratetests {
() => {
// Module: crate
// Provides: {"tests"}
// Dependencies: {}
# [cfg (test)] mod tests { use super :: * ; # [test] fn test_sharding () { const SHARDS : usize = 3 ; const MAX_IN_SHARD : usize = 2 ; let pool = Box :: leak (Box :: new (Pool :: < SHARDS , Vec < u8 > > :: new (SHARDS * MAX_IN_SHARD , 4 ,))) ; let bufs = (0 .. SHARDS * 4) . map (| _ | pool . get ()) . collect :: < Vec < _ > > () ; for shard in pool . queues . iter () { assert_eq ! (shard . elem_cnt . load (Ordering :: Relaxed) , 0) ; } for (i , buf) in bufs . iter () . enumerate () { assert ! (buf . is_empty ()) ; assert_eq ! (buf . pool as * const _ , & pool . queues [i % SHARDS] as * const _) ; } for shard in pool . queues . iter () { assert_eq ! (shard . elem_cnt . load (Ordering :: Relaxed) , 0) ; } drop (bufs) ; let bufs = (0 .. SHARDS * 4) . map (| _ | pool . get_with (| b | b . extend (& [0 , 1]))) . collect :: < Vec < _ > > () ; for (i , buf) in bufs . iter () . enumerate () { assert_eq ! (buf . pool as * const _ , & pool . queues [i % SHARDS] as * const _) ; assert_eq ! (& buf [..] , & [0 , 1]) ; } drop (bufs) ; for shard in pool . queues . iter () { assert_eq ! (shard . elem_cnt . load (Ordering :: Relaxed) , MAX_IN_SHARD) ; } let bufs = (0 .. SHARDS) . map (| _ | pool . get ()) . collect :: < Vec < _ > > () ; for (i , buf) in bufs . iter () . enumerate () { assert ! (buf . is_empty ()) ; assert_eq ! (buf . pool as * const _ , & pool . queues [i % SHARDS] as * const _) ; } for shard in pool . queues . iter () { assert_eq ! (shard . elem_cnt . load (Ordering :: Relaxed) , 1) ; } let bufs2 = (0 .. SHARDS) . map (| _ | pool . get ()) . collect :: < Vec < _ > > () ; for shard in pool . queues . iter () { assert_eq ! (shard . elem_cnt . load (Ordering :: Relaxed) , 0) ; } let bufs3 = (0 .. SHARDS) . map (| _ | pool . get ()) . collect :: < Vec < _ > > () ; for shard in pool . queues . iter () { assert_eq ! (shard . elem_cnt . load (Ordering :: Relaxed) , 0) ; } drop (bufs) ; for shard in pool . queues . iter () { assert_eq ! (shard . elem_cnt . load (Ordering :: Relaxed) , 1) ; } drop (bufs2) ; for shard in pool . queues . iter () { assert_eq ! (shard . elem_cnt . load (Ordering :: Relaxed) , MAX_IN_SHARD) ; } drop (bufs3) ; for shard in pool . queues . iter () { assert_eq ! (shard . elem_cnt . load (Ordering :: Relaxed) , MAX_IN_SHARD) ; } } }
};
}
