// Generated macro for capture_connection (function)
macro_rules! Depcrate_client_legacy_connect_capturecapture_connection {
() => {
// Module: crate::client::legacy::connect::capture
// Provides: {"capture_connection"}
// Dependencies: {}
# [doc = " Capture the connection for a given request"] # [doc = ""] # [doc = " When making a request with Hyper, the underlying connection must implement the [`Connection`] trait."] # [doc = " [`capture_connection`] allows a caller to capture the returned [`Connected`] structure as soon"] # [doc = " as the connection is established."] # [doc = ""] # [doc = " [`Connection`]: crate::client::legacy::connect::Connection"] # [doc = ""] # [doc = " *Note*: If establishing a connection fails, [`CaptureConnection::connection_metadata`] will always return none."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " **Synchronous access**:"] # [doc = " The [`CaptureConnection::connection_metadata`] method allows callers to check if a connection has been"] # [doc = " established. This is ideal for situations where you are certain the connection has already"] # [doc = " been established (e.g. after the response future has already completed)."] # [doc = " ```rust"] # [doc = " use hyper_util::client::legacy::connect::capture_connection;"] # [doc = " let mut request = http::Request::builder()"] # [doc = "   .uri(\"http://foo.com\")"] # [doc = "   .body(())"] # [doc = "   .unwrap();"] # [doc = ""] # [doc = " let captured_connection = capture_connection(&mut request);"] # [doc = " // some time later after the request has been sent..."] # [doc = " let connection_info = captured_connection.connection_metadata();"] # [doc = " println!(\"we are connected! {:?}\", connection_info.as_ref());"] # [doc = " ```"] # [doc = ""] # [doc = " **Asynchronous access**:"] # [doc = " The [`CaptureConnection::wait_for_connection_metadata`] method returns a future resolves as soon as the"] # [doc = " connection is available."] # [doc = ""] # [doc = " ```rust"] # [doc = " # #[cfg(feature  = \"tokio\")]"] # [doc = " # async fn example() {"] # [doc = " use hyper_util::client::legacy::connect::capture_connection;"] # [doc = " use hyper_util::client::legacy::Client;"] # [doc = " use hyper_util::rt::TokioExecutor;"] # [doc = " use bytes::Bytes;"] # [doc = " use http_body_util::Empty;"] # [doc = " let mut request = http::Request::builder()"] # [doc = "   .uri(\"http://foo.com\")"] # [doc = "   .body(Empty::<Bytes>::new())"] # [doc = "   .unwrap();"] # [doc = ""] # [doc = " let mut captured = capture_connection(&mut request);"] # [doc = " tokio::task::spawn(async move {"] # [doc = "     let connection_info = captured.wait_for_connection_metadata().await;"] # [doc = "     println!(\"we are connected! {:?}\", connection_info.as_ref());"] # [doc = " });"] # [doc = ""] # [doc = " let client = Client::builder(TokioExecutor::new()).build_http();"] # [doc = " client.request(request).await.expect(\"request failed\");"] # [doc = " # }"] # [doc = " ```"] pub fn capture_connection < B > (request : & mut Request < B >) -> CaptureConnection { let (tx , rx) = CaptureConnection :: new () ; request . extensions_mut () . insert (tx) ; rx }
};
}
