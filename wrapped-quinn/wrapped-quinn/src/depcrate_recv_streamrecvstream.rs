// Generated macro for RecvStream (struct)
macro_rules! Depcrate_recv_streamRecvStream {
() => {
// Module: crate::recv_stream
// Provides: {"RecvStream"}
// Dependencies: {}
# [doc = " A stream that can only be used to receive data"] # [doc = ""] # [doc = " `stop(0)` is implicitly called on drop unless:"] # [doc = " - A variant of [`ReadError`] has been yielded by a read call"] # [doc = " - [`stop()`] was called explicitly"] # [doc = ""] # [doc = " # Cancellation"] # [doc = ""] # [doc = " A `read` method is said to be *cancel-safe* when dropping its future before the future becomes"] # [doc = " ready cannot lead to loss of stream data. This is true of methods which succeed immediately when"] # [doc = " any progress is made, and is not true of methods which might need to perform multiple reads"] # [doc = " internally before succeeding. Each `read` method documents whether it is cancel-safe."] # [doc = ""] # [doc = " # Common issues"] # [doc = ""] # [doc = " ## Data never received on a locally-opened stream"] # [doc = ""] # [doc = " Peers are not notified of streams until they or a later-numbered stream are used to send"] # [doc = " data. If a bidirectional stream is locally opened but never used to send, then the peer may"] # [doc = " never see it. Application protocols should always arrange for the endpoint which will first"] # [doc = " transmit on a stream to be the endpoint responsible for opening it."] # [doc = ""] # [doc = " ## Data never received on a remotely-opened stream"] # [doc = ""] # [doc = " Verify that the stream you are receiving is the same one that the server is sending on, e.g. by"] # [doc = " logging the [`id`] of each. Streams are always accepted in the same order as they are created,"] # [doc = " i.e. ascending order by [`StreamId`]. For example, even if a sender first transmits on"] # [doc = " bidirectional stream 1, the first stream yielded by [`Connection::accept_bi`] on the receiver"] # [doc = " will be bidirectional stream 0."] # [doc = ""] # [doc = " [`ReadError`]: crate::ReadError"] # [doc = " [`stop()`]: RecvStream::stop"] # [doc = " [`SendStream::finish`]: crate::SendStream::finish"] # [doc = " [`WriteError::Stopped`]: crate::WriteError::Stopped"] # [doc = " [`id`]: RecvStream::id"] # [doc = " [`Connection::accept_bi`]: crate::Connection::accept_bi"] # [derive (Debug)] pub struct RecvStream { conn : ConnectionRef , stream : StreamId , is_0rtt : bool , all_data_read : bool , reset : Option < VarInt > , }
};
}
