// Generated macro for impl_per (macro)
macro_rules! Depcrate_convertimpl_per {
() => {
// Module: crate::convert
// Provides: {"impl_per"}
// Dependencies: {}
# [doc = " Declare and implement `Per` for all relevant types. Identity implementations are automatic."] macro_rules ! impl_per { ($ ($ t : ident ($ str : literal) per { $ ($ larger : ident : [$ default_output : ty] $ ($ int_output : ty) |+ = $ int_value : expr ; $ ($ float_output : ty) |+ = $ float_value : expr ;) + }) *) => { $ (# [doc = concat ! ("A unit of time representing exactly one " , $ str , ".")] # [derive (Debug , Clone , Copy)] pub struct $ t ; impl $ t { # [doc = concat ! ("Obtain the number of times `" , stringify ! ($ t) , "` can fit into `T`.")] # [doc = concat ! ("If `T` is smaller than `" , stringify ! ($ t) , "`, the code will fail to")] # [doc = " compile. The return type is the smallest unsigned integer type that can represent"] # [doc = " the value."] # [doc = ""] # [doc = " Valid calls:"] # [doc = ""] $ (# [doc = concat ! ("  - `" , stringify ! ($ t) , "::per(" , stringify ! ($ larger) , ")` (returns `" , stringify ! ($ default_output) , "`)")]) + # [inline] pub const fn per < T > (_larger : T) -> < T as DefaultOutput < Self >>:: Output where T : MultipleOf < Self , T :: Output > + DefaultOutput < Self > + Copy , { T :: VALUE } # [doc = concat ! ("Obtain the number of times `" , stringify ! ($ t) , "` can fit into `T`.")] # [doc = concat ! ("If `T` is smaller than `" , stringify ! ($ t) , "`, the code will fail to")] # [doc = " compile. The return type is any primitive numeric type that can represent the value."] # [doc = ""] # [doc = " Valid calls:"] # [doc = ""] $ (# [doc = concat ! ("  - `" , stringify ! ($ t) , "::per(" , stringify ! ($ larger) , ")` (returns " , stringify_outputs ! ($ ($ int_output) ,+ , $ ($ float_output) ,+) , ")")]) + # [inline] pub const fn per_t < Output > (larger : impl MultipleOf < Self , Output > + Copy) -> Output { multiple_of_value (larger) } } $ ($ (impl MultipleOf <$ t , $ int_output > for $ larger { const VALUE : $ int_output = $ int_value ; }) + $ (impl MultipleOf <$ t , $ float_output > for $ larger { const VALUE : $ float_output = $ float_value ; }) + impl DefaultOutput <$ t > for $ larger { type Output = $ default_output ; }) +) * } ; }
};
}
