// Generated macro for Reducer (trait)
macro_rules! DepcrateReducer {
() => {
// Module: crate
// Provides: {"Reducer"}
// Dependencies: {}
# [doc = " A modular reducer that can ensure that the operations on integers are all performed"] # [doc = " in a modular ring."] # [doc = ""] # [doc = " Essential information for performing the modulo operation will be stored in the reducer."] pub trait Reducer < T > { # [doc = " Create a reducer for a modulus m"] fn new (m : & T) -> Self ; # [doc = " Transform a normal integer into reduced form"] fn transform (& self , target : T) -> T ; # [doc = " Check whether target is a valid reduced form"] fn check (& self , target : & T) -> bool ; # [doc = " Get the modulus in original integer type"] fn modulus (& self) -> T ; # [doc = " Transform a reduced form back to normal integer"] fn residue (& self , target : T) -> T ; # [doc = " Test if the residue() == 0"] fn is_zero (& self , target : & T) -> bool ; # [doc = " Calculate (lhs + rhs) mod m in reduced form"] fn add (& self , lhs : & T , rhs : & T) -> T ; # [inline] fn add_in_place (& self , lhs : & mut T , rhs : & T) { * lhs = self . add (lhs , rhs) } # [doc = " Calculate 2*target mod m"] fn dbl (& self , target : T) -> T ; # [doc = " Calculate (lhs - rhs) mod m in reduced form"] fn sub (& self , lhs : & T , rhs : & T) -> T ; # [inline] fn sub_in_place (& self , lhs : & mut T , rhs : & T) { * lhs = self . sub (lhs , rhs) ; } # [doc = " Calculate -monty mod m in reduced form"] fn neg (& self , target : T) -> T ; # [doc = " Calculate (lhs * rhs) mod m in reduced form"] fn mul (& self , lhs : & T , rhs : & T) -> T ; # [inline] fn mul_in_place (& self , lhs : & mut T , rhs : & T) { * lhs = self . mul (lhs , rhs) ; } # [doc = " Calculate target^-1 mod m in reduced form,"] # [doc = " it may return None when there is no modular inverse."] fn inv (& self , target : T) -> Option < T > ; # [doc = " Calculate target^2 mod m in reduced form"] fn sqr (& self , target : T) -> T ; # [doc = " Calculate base ^ exp mod m in reduced form"] fn pow (& self , base : T , exp : & T) -> T ; }
};
}
