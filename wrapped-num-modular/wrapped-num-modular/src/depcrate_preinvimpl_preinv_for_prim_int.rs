// Generated macro for impl_preinv_for_prim_int (macro)
macro_rules! Depcrate_preinvimpl_preinv_for_prim_int {
() => {
// Module: crate::preinv
// Provides: {"impl_preinv_for_prim_int"}
// Dependencies: {}
macro_rules ! impl_preinv_for_prim_int { ($ t : ident , $ ns : ident) => { mod $ ns { use super ::*; use crate :: word ::$ t ::*; impl PreModInv <$ t > { # [doc = " Construct the preinv instance with raw values."] # [doc = ""] # [doc = " This function can be used to initialize preinv in a constant context, the divisor d"] # [doc = " is required only for verification of d_inv and q_lim."] # [inline] pub const fn new (d_inv : $ t , q_lim : $ t) -> Self { Self { d_inv , q_lim } } # [inline] fn debug_check (& self , d : $ t) { debug_assert ! (d % 2 != 0 , "only odd divisors are supported") ; debug_assert ! (d . wrapping_mul (self . d_inv) == 1) ; debug_assert ! (self . q_lim * d > (<$ t >:: MAX - d)) ; } } impl From <$ t > for PreModInv <$ t > { # [inline] fn from (v : $ t) -> Self { use crate :: word ::$ t ::*; debug_assert ! (v % 2 != 0 , "only odd divisors are supported") ; let d_inv = extend (v) . invm (& merge (0 , 1)) . unwrap () as $ t ; let q_lim = <$ t >:: MAX / v ; Self { d_inv , q_lim } } } impl DivExact <$ t , PreModInv <$ t >> for $ t { type Output = $ t ; # [inline] fn div_exact (self , d : $ t , pre : & PreModInv <$ t >) -> Option < Self > { pre . debug_check (d) ; let q = self . wrapping_mul (pre . d_inv) ; if q <= pre . q_lim { Some (q) } else { None } } } impl DivExact <$ t , PreModInv <$ t >> for DoubleWord { type Output = DoubleWord ; # [inline] fn div_exact (self , d : $ t , pre : & PreModInv <$ t >) -> Option < Self :: Output > { pre . debug_check (d) ; let (n0 , n1) = split (self) ; let q0 = n0 . wrapping_mul (pre . d_inv) ; let nr0 = wmul (q0 , d) ; let nr0 = split (nr0) . 1 ; if nr0 > n1 { return None ; } let nr1 = n1 - nr0 ; let q1 = nr1 . wrapping_mul (pre . d_inv) ; if q1 > pre . q_lim { return None ; } Some (merge (q0 , q1)) } } } } ; }
};
}
