// Generated macro for impl_zerotrie_subtype (macro)
macro_rules! Depcrate_zerotrieimpl_zerotrie_subtype {
() => {
// Module: crate::zerotrie
// Provides: {"impl_zerotrie_subtype"}
// Dependencies: {}
macro_rules ! impl_zerotrie_subtype { ($ name : ident , $ iter_element : ty , $ iter_fn : path , $ iter_ty : ty , $ cnv_fn : path) => { impl < Store > $ name < Store > { # [doc = " Create a trie directly from a store."] # [doc = ""] # [doc = " If the store does not contain valid bytes, unexpected behavior may occur."] # [inline] pub const fn from_store (store : Store) -> Self { Self { store } } # [doc = " Takes the byte store from this trie."] # [inline] pub fn into_store (self) -> Store { self . store } # [doc = " Converts this trie's store to a different store implementing the `From` trait."] # [doc = ""] # [doc = concat ! ("For example, use this to change `" , stringify ! ($ name) , "<Vec<u8>>` to `" , stringify ! ($ name) , "<Cow<[u8]>>`.")] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::borrow::Cow;"] # [doc = concat ! ("use zerotrie::" , stringify ! ($ name) , ";")] # [doc = ""] # [doc = concat ! ("let trie: " , stringify ! ($ name) , "<Vec<u8>> = " , stringify ! ($ name) , "::from_bytes(b\"abc\\x85\").to_owned();")] # [doc = concat ! ("let cow: " , stringify ! ($ name) , "<Cow<[u8]>> = trie.convert_store();")] # [doc = ""] # [doc = " assert_eq!(cow.get(b\"abc\"), Some(5));"] # [doc = " ```"] pub fn convert_store < X : From < Store >> (self) -> $ name < X > { $ name ::< X >:: from_store (X :: from (self . store)) } } impl < Store > $ name < Store > where Store : AsRef < [u8] > + ? Sized , { # [doc = " Queries the trie for a string."] pub fn get < K > (& self , key : K) -> Option < usize > where K : AsRef < [u8] > { reader :: get_parameterized ::< Self > (self . store . as_ref () , key . as_ref ()) } # [doc = " Returns `true` if the trie is empty."] # [inline] pub fn is_empty (& self) -> bool { self . store . as_ref () . is_empty () } # [doc = " Returns the size of the trie in number of bytes."] # [doc = ""] # [doc = " To get the number of keys in the trie, use `.iter().count()`:"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("use zerotrie::" , stringify ! ($ name) , ";")] # [doc = ""] # [doc = " // A trie with two values: \"abc\" and \"abcdef\""] # [doc = concat ! ("let trie: &" , stringify ! ($ name) , "<[u8]> = " , stringify ! ($ name) , "::from_bytes(b\"abc\\x80def\\x81\");")] # [doc = ""] # [doc = " assert_eq!(8, trie.byte_len());"] # [doc = " assert_eq!(2, trie.iter().count());"] # [doc = " ```"] # [inline] pub fn byte_len (& self) -> usize { self . store . as_ref () . len () } # [doc = " Returns the bytes contained in the underlying store."] # [inline] pub fn as_bytes (& self) -> & [u8] { self . store . as_ref () } # [doc = " Returns this trie as a reference transparent over a byte slice."] # [inline] pub fn as_borrowed (& self) -> &$ name < [u8] > { $ name :: from_bytes (self . store . as_ref ()) } # [doc = " Returns a trie with a store borrowing from this trie."] # [inline] pub fn as_borrowed_slice (& self) -> $ name <& [u8] > { $ name :: from_store (self . store . as_ref ()) } } impl < Store > AsRef <$ name < [u8] >> for $ name < Store > where Store : AsRef < [u8] > + ? Sized , { # [inline] fn as_ref (& self) -> &$ name < [u8] > { self . as_borrowed () } } # [cfg (feature = "alloc")] impl < Store > $ name < Store > where Store : AsRef < [u8] > + ? Sized , { # [doc = " Converts a possibly-borrowed $name to an owned one."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("use zerotrie::" , stringify ! ($ name) , ";")] # [doc = ""] # [doc = concat ! ("let trie: &" , stringify ! ($ name) , "<[u8]> = " , stringify ! ($ name) , "::from_bytes(b\"abc\\x85\");")] # [doc = concat ! ("let owned: " , stringify ! ($ name) , "<Vec<u8>> = trie.to_owned();")] # [doc = ""] # [doc = " assert_eq!(trie.get(b\"abc\"), Some(5));"] # [doc = " assert_eq!(owned.get(b\"abc\"), Some(5));"] # [doc = " ```"] # [inline] pub fn to_owned (& self) -> $ name < Vec < u8 >> { $ name :: from_store (Vec :: from (self . store . as_ref ()) ,) } # [doc = " Returns an iterator over the key/value pairs in this trie."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("use zerotrie::" , stringify ! ($ name) , ";")] # [doc = ""] # [doc = " // A trie with two values: \"abc\" and \"abcdef\""] # [doc = concat ! ("let trie: &" , stringify ! ($ name) , "<[u8]> = " , stringify ! ($ name) , "::from_bytes(b\"abc\\x80def\\x81\");")] # [doc = ""] # [doc = " let mut it = trie.iter();"] # [doc = " assert_eq!(it.next(), Some((\"abc\".into(), 0)));"] # [doc = " assert_eq!(it.next(), Some((\"abcdef\".into(), 1)));"] # [doc = " assert_eq!(it.next(), None);"] # [doc = " ```"] # [inline] pub fn iter (& self) -> $ iter_ty { $ iter_fn (self . as_bytes ()) } } impl $ name < [u8] > { # [doc = " Casts from a byte slice to a reference to a trie with the same lifetime."] # [doc = ""] # [doc = " If the bytes are not a valid trie, unexpected behavior may occur."] # [inline] pub fn from_bytes (trie : & [u8]) -> & Self { Self :: transparent_ref_from_store (trie) } } # [cfg (feature = "alloc")] impl $ name < Vec < u8 >> { pub (crate) fn try_from_tuple_slice (items : & [(& ByteStr , usize)]) -> Result < Self , ZeroTrieBuildError > { use crate :: options :: ZeroTrieWithOptions ; ZeroTrieBuilder ::< VecDeque < u8 >>:: from_sorted_tuple_slice (items , Self :: OPTIONS ,) . map (| s | Self { store : s . to_bytes () , }) } } # [cfg (feature = "alloc")] impl <'a , K > FromIterator < (K , usize) > for $ name < Vec < u8 >> where K : AsRef < [u8] > { fn from_iter < T : IntoIterator < Item = (K , usize) >> (iter : T) -> Self { use crate :: options :: ZeroTrieWithOptions ; use crate :: builder :: nonconst :: ZeroTrieBuilder ; ZeroTrieBuilder ::< VecDeque < u8 >>:: from_bytes_iter (iter , Self :: OPTIONS) . map (| s | Self { store : s . to_bytes () , }) . unwrap () } } # [cfg (feature = "alloc")] impl <'a , K > TryFrom <&'a BTreeMap < K , usize >> for $ name < Vec < u8 >> where K : Borrow < [u8] > { type Error = crate :: error :: ZeroTrieBuildError ; fn try_from (map : &'a BTreeMap < K , usize >) -> Result < Self , Self :: Error > { let tuples : Vec < (& [u8] , usize) > = map . iter () . map (| (k , v) | (k . borrow () , * v)) . collect () ; let byte_str_slice = ByteStr :: from_byte_slice_with_value (& tuples) ; Self :: try_from_tuple_slice (byte_str_slice) } } # [cfg (feature = "alloc")] impl < Store > $ name < Store > where Store : AsRef < [u8] > + ? Sized { # [doc = " Exports the data from this ZeroTrie type into a BTreeMap."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("use zerotrie::" , stringify ! ($ name) , ";")] # [doc = " use std::collections::BTreeMap;"] # [doc = ""] # [doc = concat ! ("let trie = " , stringify ! ($ name) , "::from_bytes(b\"abc\\x81def\\x82\");")] # [doc = " let items = trie.to_btreemap();"] # [doc = ""] # [doc = " assert_eq!(items.len(), 2);"] # [doc = ""] # [doc = concat ! ("let recovered_trie: " , stringify ! ($ name) , "<Vec<u8>> = items")] # [doc = "     .into_iter()"] # [doc = "     .collect();"] # [doc = " assert_eq!(trie.as_bytes(), recovered_trie.as_bytes());"] # [doc = " ```"] pub fn to_btreemap (& self) -> BTreeMap <$ iter_element , usize > { self . iter () . collect () } # [allow (dead_code)] pub (crate) fn to_btreemap_bytes (& self) -> BTreeMap < Box < [u8] >, usize > { self . iter () . map (| (k , v) | ($ cnv_fn (k) , v)) . collect () } } # [cfg (feature = "alloc")] impl < Store > From <&$ name < Store >> for BTreeMap <$ iter_element , usize > where Store : AsRef < [u8] > + ? Sized , { # [inline] fn from (other : &$ name < Store >) -> Self { other . to_btreemap () } } # [cfg (feature = "litemap")] impl <'a , K , S > TryFrom <&'a LiteMap < K , usize , S >> for $ name < Vec < u8 >> where K : Borrow < [u8] >, S : litemap :: store :: StoreIterable <'a , K , usize >, { type Error = crate :: error :: ZeroTrieBuildError ; fn try_from (map : &'a LiteMap < K , usize , S >) -> Result < Self , Self :: Error > { let tuples : Vec < (& [u8] , usize) > = map . iter () . map (| (k , v) | (k . borrow () , * v)) . collect () ; let byte_str_slice = ByteStr :: from_byte_slice_with_value (& tuples) ; Self :: try_from_tuple_slice (byte_str_slice) } } # [cfg (feature = "litemap")] impl < Store > $ name < Store > where Store : AsRef < [u8] > + ? Sized , { # [doc = " Exports the data from this ZeroTrie type into a LiteMap."] # [doc = ""] # [doc = " ✨ *Enabled with the `litemap` Cargo feature.*"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("use zerotrie::" , stringify ! ($ name) , ";")] # [doc = " use litemap::LiteMap;"] # [doc = ""] # [doc = concat ! ("let trie = " , stringify ! ($ name) , "::from_bytes(b\"abc\\x81def\\x82\");")] # [doc = ""] # [doc = " let items = trie.to_litemap();"] # [doc = " assert_eq!(items.len(), 2);"] # [doc = ""] # [doc = concat ! ("let recovered_trie: " , stringify ! ($ name) , "<Vec<u8>> = items")] # [doc = "     .iter()"] # [doc = "     .map(|(k, v)| (k, *v))"] # [doc = "     .collect();"] # [doc = " assert_eq!(trie.as_bytes(), recovered_trie.as_bytes());"] # [doc = " ```"] pub fn to_litemap (& self) -> LiteMap <$ iter_element , usize > { self . iter () . collect () } # [allow (dead_code)] pub (crate) fn to_litemap_bytes (& self) -> LiteMap < Box < [u8] >, usize > { self . iter () . map (| (k , v) | ($ cnv_fn (k) , v)) . collect () } } # [cfg (feature = "litemap")] impl < Store > From <&$ name < Store >> for LiteMap <$ iter_element , usize > where Store : AsRef < [u8] > + ? Sized , { # [inline] fn from (other : &$ name < Store >) -> Self { other . to_litemap () } } # [cfg (feature = "litemap")] impl $ name < Vec < u8 >> { # [cfg (feature = "serde")] pub (crate) fn try_from_serde_litemap (items : & LiteMap < Box < ByteStr >, usize >) -> Result < Self , ZeroTrieBuildError > { let lm_borrowed : LiteMap <& ByteStr , usize > = items . to_borrowed_keys () ; Self :: try_from_tuple_slice (lm_borrowed . as_slice ()) } } impl Borrow <$ name < [u8] >> for $ name <& [u8] > { # [inline] fn borrow (& self) -> &$ name < [u8] > { self . as_borrowed () } } # [cfg (feature = "alloc")] impl Borrow <$ name < [u8] >> for $ name < Box < [u8] >> { # [inline] fn borrow (& self) -> &$ name < [u8] > { self . as_borrowed () } } # [cfg (feature = "alloc")] impl Borrow <$ name < [u8] >> for $ name < Vec < u8 >> { # [inline] fn borrow (& self) -> &$ name < [u8] > { self . as_borrowed () } } # [cfg (feature = "alloc")] impl alloc :: borrow :: ToOwned for $ name < [u8] > { type Owned = $ name < Box < [u8] >>; # [doc = concat ! ("This impl allows [`" , stringify ! ($ name) , "`] to be used inside of a [`Cow`](alloc::borrow::Cow).")] # [doc = ""] # [doc = concat ! ("Note that it is also possible to use `" , stringify ! ($ name) , "<ZeroVec<u8>>` for a similar result.")] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::borrow::Cow;"] # [doc = concat ! ("use zerotrie::" , stringify ! ($ name) , ";")] # [doc = ""] # [doc = concat ! ("let trie: Cow<" , stringify ! ($ name) , "<[u8]>> = Cow::Borrowed(" , stringify ! ($ name) , "::from_bytes(b\"abc\\x85\"));")] # [doc = " assert_eq!(trie.get(b\"abc\"), Some(5));"] # [doc = " ```"] fn to_owned (& self) -> Self :: Owned { let bytes : & [u8] = self . store . as_ref () ; $ name :: from_store (Vec :: from (bytes) . into_boxed_slice () ,) } } # [cfg (feature = "zerovec")] unsafe impl < Store > zerovec :: ule :: VarULE for $ name < Store > where Store : zerovec :: ule :: VarULE , { # [inline] fn validate_bytes (bytes : & [u8]) -> Result < () , zerovec :: ule :: UleError > { Store :: validate_bytes (bytes) } # [inline] unsafe fn from_bytes_unchecked (bytes : & [u8]) -> & Self { Self :: transparent_ref_from_store (Store :: from_bytes_unchecked (bytes)) } } # [cfg (feature = "zerofrom")] impl <'zf , Store1 , Store2 > zerofrom :: ZeroFrom <'zf , $ name < Store1 >> for $ name < Store2 > where Store2 : zerofrom :: ZeroFrom <'zf , Store1 >, { # [inline] fn zero_from (other : &'zf $ name < Store1 >) -> Self { $ name :: from_store (zerofrom :: ZeroFrom :: zero_from (& other . store)) } } } ; }
};
}
