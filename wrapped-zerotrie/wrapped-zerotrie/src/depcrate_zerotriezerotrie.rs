// Generated macro for ZeroTrie (struct)
macro_rules! Depcrate_zerotrieZeroTrie {
() => {
// Module: crate::zerotrie
// Provides: {"ZeroTrie"}
// Dependencies: {}
# [doc = " A data structure that compactly maps from byte sequences to integers."] # [doc = ""] # [doc = " There are several variants of `ZeroTrie` which are very similar but are optimized"] # [doc = " for different use cases:"] # [doc = ""] # [doc = " - [`ZeroTrieSimpleAscii`] is the most compact structure. Very fast for small data."] # [doc = "   Only stores ASCII-encoded strings. Can be const-constructed!"] # [doc = " - [`ZeroTriePerfectHash`] is also compact, but it also supports arbitrary binary"] # [doc = "   strings. It also scales better to large data. Cannot be const-constructed."] # [doc = " - [`ZeroTrieExtendedCapacity`] can be used if more than 2^32 bytes are required."] # [doc = ""] # [doc = " You can create a `ZeroTrie` directly, in which case the most appropriate"] # [doc = " backing implementation will be chosen."] # [doc = ""] # [doc = " # Backing Store"] # [doc = ""] # [doc = " The data structure has a flexible backing data store. The only requirement for most"] # [doc = " functionality is that it implement `AsRef<[u8]>`. All of the following are valid"] # [doc = " ZeroTrie types:"] # [doc = ""] # [doc = " - `ZeroTrie<[u8]>` (dynamically sized type: must be stored in a reference or Box)"] # [doc = " - `ZeroTrie<&[u8]>` (borrows its data from a u8 buffer)"] # [doc = " - `ZeroTrie<Vec<u8>>` (fully owned data)"] # [doc = " - `ZeroTrie<ZeroVec<u8>>` (the recommended borrowed-or-owned signature)"] # [doc = " - `Cow<ZeroTrie<[u8]>>` (another borrowed-or-owned signature)"] # [doc = " - `ZeroTrie<Cow<[u8]>>` (another borrowed-or-owned signature)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use litemap::LiteMap;"] # [doc = " use zerotrie::ZeroTrie;"] # [doc = ""] # [doc = " let mut map = LiteMap::<&[u8], usize>::new_vec();"] # [doc = " map.insert(\"foo\".as_bytes(), 1);"] # [doc = " map.insert(\"bar\".as_bytes(), 2);"] # [doc = " map.insert(\"bazzoo\".as_bytes(), 3);"] # [doc = ""] # [doc = " let trie = ZeroTrie::try_from(&map)?;"] # [doc = ""] # [doc = " assert_eq!(trie.get(\"foo\"), Some(1));"] # [doc = " assert_eq!(trie.get(\"bar\"), Some(2));"] # [doc = " assert_eq!(trie.get(\"bazzoo\"), Some(3));"] # [doc = " assert_eq!(trie.get(\"unknown\"), None);"] # [doc = ""] # [doc = " # Ok::<_, zerotrie::ZeroTrieBuildError>(())"] # [doc = " ```"] # [derive (Debug , Clone , Copy , PartialEq , Eq)] # [cfg_attr (feature = "yoke" , derive (yoke :: Yokeable))] pub struct ZeroTrie < Store > (pub (crate) ZeroTrieFlavor < Store >) ;
};
}
