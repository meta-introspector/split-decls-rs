// Generated macro for impl_115 (impl)
macro_rules! Depcrate_byte_phfimpl_115 {
() => {
// Module: crate::byte_phf
// Provides: {"impl_115"}
// Dependencies: {}
impl < Store > PerfectByteHashMap < Store > where Store : AsRef < [u8] > + ? Sized , { # [doc = " Gets the usize for the given byte, or `None` if it is not in the map."] pub fn get (& self , key : u8) -> Option < usize > { let (p , buffer) = self . 0 . as_ref () . split_first () ? ; let n_usize = buffer . len () / 2 ; if n_usize == 0 { return None ; } let n = n_usize as u8 ; let (qq , eks) = buffer . debug_split_at (n_usize) ; debug_assert_eq ! (qq . len () , eks . len ()) ; let l1 = f1 (key , * p , n) as usize ; let q = debug_unwrap ! (qq . get (l1) , return None) ; let l2 = f2 (key , * q , n) as usize ; let ek = debug_unwrap ! (eks . get (l2) , return None) ; if * ek == key { Some (l2) } else { None } } # [doc = " This is called `num_items` because `len` is ambiguous: it could refer"] # [doc = " to the number of items or the number of bytes."] pub fn num_items (& self) -> usize { self . 0 . as_ref () . len () / 2 } # [doc = " Get an iterator over the keys in the order in which they are stored in the map."] pub fn keys (& self) -> & [u8] { let n = self . num_items () ; self . 0 . as_ref () . debug_split_at (1 + n) . 1 } # [doc = " Diagnostic function that returns `p` and the maximum value of `q`"] # [cfg (test)] pub fn p_qmax (& self) -> Option < (u8 , u8) > { let (p , buffer) = self . 0 . as_ref () . split_first () ? ; let n = buffer . len () / 2 ; if n == 0 { return None ; } let (qq , _) = buffer . debug_split_at (n) ; Some ((* p , * qq . iter () . max () . unwrap ())) } # [doc = " Returns the map as bytes. The map can be recovered with [`Self::from_store`]"] # [doc = " or [`Self::from_bytes`]."] pub fn as_bytes (& self) -> & [u8] { self . 0 . as_ref () } # [cfg (all (feature = "alloc" , test))] pub (crate) fn check (& self) -> Result < () , (& 'static str , u8) > { use alloc :: vec ; let len = self . num_items () ; let mut seen = vec ! [false ; len] ; for b in 0 ..= 255u8 { let get_result = self . get (b) ; if self . keys () . contains (& b) { let i = get_result . ok_or (("expected to find" , b)) ? ; if seen [i] { return Err (("seen" , b)) ; } seen [i] = true ; } else if get_result . is_some () { return Err (("did not expect to find" , b)) ; } } Ok (()) } }
};
}
