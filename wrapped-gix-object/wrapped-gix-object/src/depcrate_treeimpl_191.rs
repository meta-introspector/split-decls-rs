// Generated macro for impl_191 (impl)
macro_rules! Depcrate_treeimpl_191 {
() => {
// Module: crate::tree
// Provides: {"impl_191"}
// Dependencies: {}
impl EntryMode { # [doc = " Expose the value as u16 (lossy, unlike the internal representation that is hidden)."] pub const fn value (self) -> u16 { if self . internal & IFMT == 0o140000 { 0o040000 } else { self . internal } } # [doc = " Return the representation as used in the git internal format, which is octal and written"] # [doc = " to the `backing` buffer. The respective sub-slice that was written to is returned."] pub fn as_bytes < 'a > (& self , backing : & 'a mut [u8 ; 6]) -> & 'a BStr { if self . internal == 0 { std :: slice :: from_ref (& b'0') } else { for (idx , backing_octet) in backing . iter_mut () . enumerate () { let bit_pos = 3 * (6 - idx - 1) ; let oct_mask = 0b111 << bit_pos ; let digit = (self . internal & oct_mask) >> bit_pos ; * backing_octet = b'0' + digit as u8 ; } if backing [1] == b'4' { if backing [0] == b'1' { backing [0] = b'0' ; & backing [0 .. 6] } else { & backing [1 .. 6] } } else { & backing [0 .. 6] } } . into () } # [doc = " Construct an EntryMode from bytes represented as in the git internal format"] # [doc = " Return the mode and the remainder of the bytes."] pub (crate) fn extract_from_bytes (i : & [u8]) -> Option < (Self , & '_ [u8]) > { let mut mode = 0 ; let mut idx = 0 ; let mut space_pos = 0 ; if i . is_empty () { return None ; } while idx < i . len () { let b = i [idx] ; if b == b' ' { space_pos = idx ; break ; } # [allow (clippy :: manual_range_contains)] if b < b'0' || b > b'7' { return None ; } if idx > 6 { return None ; } mode = (mode << 3) + (b - b'0') as u16 ; idx += 1 ; } if mode == 0o040000 && i [0] == b'0' { mode += 0o100000 ; } Some ((Self { internal : mode } , & i [(space_pos + 1) ..])) } # [doc = " Construct an EntryMode from bytes represented as in the git internal format."] pub fn from_bytes (i : & [u8]) -> Option < Self > { Self :: extract_from_bytes (i) . map (| (mode , _rest) | mode) } }
};
}
