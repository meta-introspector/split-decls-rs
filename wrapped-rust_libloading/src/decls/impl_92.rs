macro_rules! deps {
    () => {
        Library!();
        AsSymbolName!();
        AsFilename!();
        Symbol!();
        Error!();
    };
}

macro_rules! impl_92 {
    () => {
        deps!();
        impl Library { # [doc = " Find and eagerly load a shared library (module)."] # [doc = ""] # [doc = " If the `filename` contains a [path separator], the `filename` is interpreted as a `path` to"] # [doc = " a file. Otherwise, platform-specific algorithms are employed to find a library with a"] # [doc = " matching file name."] # [doc = ""] # [doc = " This is equivalent to <code>[Library::open](filename, [RTLD_LAZY] | [RTLD_LOCAL])</code>."] # [doc = ""] # [doc = " [path separator]: std::path::MAIN_SEPARATOR"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " When a library is loaded, initialisation routines contained within the library are executed."] # [doc = " For the purposes of safety, the execution of these routines is conceptually the same calling an"] # [doc = " unknown foreign function and may impose arbitrary requirements on the caller for the call"] # [doc = " to be sound."] # [doc = ""] # [doc = " Additionally, the callers of this function must also ensure that execution of the"] # [doc = " termination routines contained within the library is safe as well. These routines may be"] # [doc = " executed when the library is unloaded."] # [inline] pub unsafe fn new (filename : impl AsFilename) -> Result < Library , crate :: Error > { Library :: open (Some (filename) , RTLD_LAZY | RTLD_LOCAL) } # [doc = " Load the `Library` representing the current executable."] # [doc = ""] # [doc = " [`Library::get`] calls of the returned `Library` will look for symbols in following"] # [doc = " locations in order:"] # [doc = ""] # [doc = " 1. The original program image;"] # [doc = " 2. Any executable object files (e.g. shared libraries) loaded at program startup;"] # [doc = " 3. Any executable object files loaded at runtime (e.g. via other `Library::new` calls or via"] # [doc = "    calls to the `dlopen` function)."] # [doc = ""] # [doc = " Note that the behaviour of a `Library` loaded with this method is different from that of"] # [doc = " Libraries loaded with [`os::windows::Library::this`]."] # [doc = ""] # [doc = " This is equivalent to <code>[Library::open](None, [RTLD_LAZY] | [RTLD_LOCAL])</code>."] # [doc = ""] # [doc = " [`os::windows::Library::this`]: crate::os::windows::Library::this"] # [inline] pub fn this () -> Library { unsafe { Library :: open_char_ptr (null () , RTLD_LAZY | RTLD_LOCAL) . expect ("this should never fail") } } # [doc = " Find and load an executable object file (shared library)."] # [doc = ""] # [doc = " See documentation for [`Library::this`] for further description of the behaviour"] # [doc = " when the `filename` is `None`. Otherwise see [`Library::new`]."] # [doc = ""] # [doc = " Corresponds to `dlopen(filename, flags)`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " When a library is loaded, initialisation routines contained within the library are executed."] # [doc = " For the purposes of safety, the execution of these routines is conceptually the same calling an"] # [doc = " unknown foreign function and may impose arbitrary requirements on the caller for the call"] # [doc = " to be sound."] # [doc = ""] # [doc = " Additionally, the callers of this function must also ensure that execution of the"] # [doc = " termination routines contained within the library is safe as well. These routines may be"] # [doc = " executed when the library is unloaded."] pub unsafe fn open < P > (filename : Option < P > , flags : core :: ffi :: c_int ,) -> Result < Library , crate :: Error > where P : AsFilename , { let Some (filename) = filename else { return Self :: open_char_ptr (null () , flags) ; } ; filename . posix_filename (| posix_filename | Library :: open_char_ptr (posix_filename , flags)) } # [doc = " private helper to call dlopen+dlerror once we de-tangled the string into a raw pointer to a 0 terminated utf-8 string."] # [doc = " caller must ensure that the string is actually 0 terminated."] unsafe fn open_char_ptr (filename : * const core :: ffi :: c_char , flags : core :: ffi :: c_int ,) -> Result < Library , crate :: Error > { with_dlerror (move | | { let result = dlopen (filename , flags) ; if result . is_null () { None } else { Some (Library { handle : result }) } } , | desc | crate :: Error :: DlOpen { source : desc . into () , } ,) . map_err (| e | e . unwrap_or (crate :: Error :: DlOpenUnknown)) } unsafe fn get_impl < T , F > (& self , symbol : impl AsSymbolName , on_null : F ,) -> Result < Symbol < T > , crate :: Error > where F : FnOnce () -> Result < Symbol < T > , crate :: Error > , { ensure_compatible_types :: < T , * mut core :: ffi :: c_void > () ? ; symbol . symbol_name (| posix_symbol | { let result = with_dlerror (| | { dlerror () ; let symbol = dlsym (self . handle , posix_symbol) ; if symbol . is_null () { None } else { Some (Symbol { pointer : symbol , pd : marker :: PhantomData , }) } } , | desc | crate :: Error :: DlSym { source : desc . into () , } ,) ; match result { Err (None) => on_null () , Err (Some (e)) => Err (e) , Ok (x) => Ok (x) , } }) } # [doc = " Get a pointer to a function or static variable by symbol name."] # [doc = ""] # [doc = " The `symbol` may not contain any null bytes, with the exception of the last byte. Providing a"] # [doc = " null terminated `symbol` may help to avoid an allocation."] # [doc = ""] # [doc = " Symbol is interpreted as-is; no mangling is done. This means that symbols like `x::y` are"] # [doc = " most likely invalid."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Users of this API must specify the correct type of the function or variable loaded. Using a"] # [doc = " `Symbol` with a wrong type is undefined."] # [doc = ""] # [doc = " # Platform-specific behaviour"] # [doc = ""] # [doc = " Implementation of thread local variables is extremely platform specific and uses of such"] # [doc = " variables that work on e.g. Linux may have unintended behaviour on other targets."] # [doc = ""] # [doc = " On POSIX implementations where the `dlerror` function is not confirmed to be MT-safe (such"] # [doc = " as FreeBSD), this function will unconditionally return an error when the underlying `dlsym`"] # [doc = " call returns a null pointer. There are rare situations where `dlsym` returns a genuine null"] # [doc = " pointer without it being an error. If loading a null pointer is something you care about,"] # [doc = " consider using the [`Library::get_singlethreaded`] call."] # [inline (always)] pub unsafe fn get < T > (& self , symbol : impl AsSymbolName) -> Result < Symbol < T > , crate :: Error > { # [cfg_attr (libloading_docs , allow (unused_extern_crates))] # [cfg (libloading_docs)] extern crate cfg_if ; cfg_if :: cfg_if ! { if # [cfg (any (target_os = "linux" , target_os = "android" , target_os = "openbsd" , target_os = "macos" , target_os = "ios" , target_os = "solaris" , target_os = "illumos" , target_os = "redox" , target_os = "fuchsia" , target_os = "cygwin" ,))] { self . get_singlethreaded (symbol) } else { self . get_impl (symbol , || Err (crate :: Error :: DlSymUnknown)) } } } # [doc = " Get a pointer to function or static variable by symbol name."] # [doc = ""] # [doc = " The `symbol` may not contain any null bytes, with the exception of the last byte. Providing a"] # [doc = " null terminated `symbol` may help to avoid an allocation."] # [doc = ""] # [doc = " Symbol is interpreted as-is; no mangling is done. This means that symbols like `x::y` are"] # [doc = " most likely invalid."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Users of this API must specify the correct type of the function or variable loaded."] # [doc = ""] # [doc = " It is up to the user of this library to ensure that no other calls to an MT-unsafe"] # [doc = " implementation of `dlerror` occur during the execution of this function. Failing that, the"] # [doc = " behaviour of this function is not defined."] # [doc = ""] # [doc = " # Platform-specific behaviour"] # [doc = ""] # [doc = " The implementation of thread-local variables is extremely platform specific and uses of such"] # [doc = " variables that work on e.g. Linux may have unintended behaviour on other targets."] # [inline (always)] pub unsafe fn get_singlethreaded < T > (& self , symbol : impl AsSymbolName ,) -> Result < Symbol < T > , crate :: Error > { self . get_impl (symbol , | | { Ok (Symbol { pointer : ptr :: null_mut () , pd : marker :: PhantomData , }) }) } # [doc = " Convert the `Library` to a raw handle."] # [doc = ""] # [doc = " The handle returned by this function shall be usable with APIs which accept handles"] # [doc = " as returned by `dlopen`."] pub fn into_raw (self) -> * mut core :: ffi :: c_void { let handle = self . handle ; mem :: forget (self) ; handle } # [doc = " Convert a raw handle returned by `dlopen`-family of calls to a `Library`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The pointer shall be a result of a successful call of the `dlopen`-family of functions or a"] # [doc = " pointer previously returned by `Library::into_raw` call. It must be valid to call `dlclose`"] # [doc = " with this pointer as an argument."] pub unsafe fn from_raw (handle : * mut core :: ffi :: c_void) -> Library { Library { handle } } # [doc = " Unload the library."] # [doc = ""] # [doc = " This method might be a no-op, depending on the flags with which the `Library` was opened,"] # [doc = " what library was opened or other platform specifics."] # [doc = ""] # [doc = " You only need to call this if you are interested in handling any errors that may arise when"] # [doc = " library is unloaded. Otherwise the implementation of `Drop` for `Library` will close the"] # [doc = " library and ignore the errors were they arise."] # [doc = ""] # [doc = " The underlying data structures may still get leaked if an error does occur."] pub fn close (self) -> Result < () , crate :: Error > { let result = with_dlerror (| | { if unsafe { dlclose (self . handle) } == 0 { Some (()) } else { None } } , | desc | crate :: Error :: DlClose { source : desc . into () , } ,) . map_err (| e | e . unwrap_or (crate :: Error :: DlCloseUnknown)) ; mem :: forget (self) ; result } }
    };
}

impl_92!()