macro_rules! deps {
    () => {
        Error!();
        LOAD_LIBRARY_FLAGS!();
        AsFilename!();
        Library!();
        AsSymbolName!();
        FARPROC!();
        HMODULE!();
        Symbol!();
        ErrorModeGuard!();
    };
}

macro_rules! impl_111 {
    () => {
        deps!();
        impl Library { # [doc = " Find and load a module."] # [doc = ""] # [doc = " If the `filename` specifies a full path, the function only searches that path for the"] # [doc = " module. Otherwise, if the `filename` specifies a relative path or a module name without a"] # [doc = " path, the function uses a Windows-specific search strategy to find the module. For more"] # [doc = " information, see the [Remarks on MSDN][msdn]."] # [doc = ""] # [doc = " If the `filename` specifies a library filename without a path and with the extension omitted,"] # [doc = " the `.dll` extension is implicitly added. This behaviour may be suppressed by appending a"] # [doc = " trailing `.` to the `filename`."] # [doc = ""] # [doc = " This is equivalent to <code>[Library::load_with_flags](filename, 0)</code>."] # [doc = ""] # [doc = " [msdn]: https://docs.microsoft.com/en-us/windows/win32/api/libloaderapi/nf-libloaderapi-loadlibraryw#remarks"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " When a library is loaded, initialisation routines contained within the library are executed."] # [doc = " For the purposes of safety, the execution of these routines is conceptually the same calling an"] # [doc = " unknown foreign function and may impose arbitrary requirements on the caller for the call"] # [doc = " to be sound."] # [doc = ""] # [doc = " Additionally, the callers of this function must also ensure that execution of the"] # [doc = " termination routines contained within the library is safe as well. These routines may be"] # [doc = " executed when the library is unloaded."] # [inline] pub unsafe fn new (filename : impl AsFilename) -> Result < Library , crate :: Error > { Library :: load_with_flags (filename , 0) } # [doc = " Get the `Library` representing the original program executable."] # [doc = ""] # [doc = " Note that the behaviour of the `Library` loaded with this method is different from"] # [doc = " Libraries loaded with [`os::unix::Library::this`]. For more information refer to [MSDN]."] # [doc = ""] # [doc = " Corresponds to `GetModuleHandleExW(0, NULL, _)`."] # [doc = ""] # [doc = " [`os::unix::Library::this`]: crate::os::unix::Library::this"] # [doc = " [MSDN]: https://docs.microsoft.com/en-us/windows/win32/api/libloaderapi/nf-libloaderapi-getmodulehandleexw"] pub fn this () -> Result < Library , crate :: Error > { unsafe { let mut handle : HMODULE = 0 ; with_get_last_error (| source | crate :: Error :: GetModuleHandleExW { source } , | | { let result = GetModuleHandleExW (0 , ptr :: null_mut () , & mut handle) ; if result == 0 { None } else { Some (Library (handle)) } } ,) . map_err (| e | e . unwrap_or (crate :: Error :: GetModuleHandleExWUnknown)) } } # [doc = " Get a module that is already loaded by the program."] # [doc = ""] # [doc = " This function returns a `Library` corresponding to a module with the given name that is"] # [doc = " already mapped into the address space of the process. If the module isn't found, an error is"] # [doc = " returned."] # [doc = ""] # [doc = " If the `filename` does not include a full path and there are multiple different loaded"] # [doc = " modules corresponding to the `filename`, it is impossible to predict which module handle"] # [doc = " will be returned. For more information refer to [MSDN]."] # [doc = ""] # [doc = " If the `filename` specifies a library filename without a path and with the extension omitted,"] # [doc = " the `.dll` extension is implicitly added. This behaviour may be suppressed by appending a"] # [doc = " trailing `.` to the `filename`."] # [doc = ""] # [doc = " This is equivalent to `GetModuleHandleExW(0, filename, _)`."] # [doc = ""] # [doc = " [MSDN]: https://docs.microsoft.com/en-us/windows/win32/api/libloaderapi/nf-libloaderapi-getmodulehandleexw"] pub fn open_already_loaded (filename : impl AsFilename) -> Result < Library , crate :: Error > { filename . windows_filename (| windows_filename | { unsafe { let mut handle : HMODULE = 0 ; with_get_last_error (| source | crate :: Error :: GetModuleHandleExW { source } , | | { let result = GetModuleHandleExW (0 , windows_filename , & mut handle) ; if result == 0 { None } else { Some (Library (handle)) } } ,) . map_err (| e | e . unwrap_or (crate :: Error :: GetModuleHandleExWUnknown)) } }) } # [doc = " Find and load a module, additionally adjusting behaviour with flags."] # [doc = ""] # [doc = " See [`Library::new`] for documentation on the handling of the `filename` argument. See the"] # [doc = " [flag table on MSDN][flags] for information on applicable values for the `flags` argument."] # [doc = ""] # [doc = " Corresponds to `LoadLibraryExW(filename, reserved: NULL, flags)`."] # [doc = ""] # [doc = " [flags]: https://docs.microsoft.com/en-us/windows/win32/api/libloaderapi/nf-libloaderapi-loadlibraryexw#parameters"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " When a library is loaded, initialisation routines contained within the library are executed."] # [doc = " For the purposes of safety, the execution of these routines is conceptually the same calling an"] # [doc = " unknown foreign function and may impose arbitrary requirements on the caller for the call"] # [doc = " to be sound."] # [doc = ""] # [doc = " Additionally, the callers of this function must also ensure that execution of the"] # [doc = " termination routines contained within the library is safe as well. These routines may be"] # [doc = " executed when the library is unloaded."] pub unsafe fn load_with_flags (filename : impl AsFilename , flags : LOAD_LIBRARY_FLAGS ,) -> Result < Library , crate :: Error > { filename . windows_filename (| windows_filename | { let _guard = ErrorModeGuard :: new () ; with_get_last_error (| source | crate :: Error :: LoadLibraryExW { source } , | | { let handle = LoadLibraryExW (windows_filename , 0 , flags) ; if handle == 0 { None } else { Some (Library (handle)) } } ,) . map_err (| e | e . unwrap_or (crate :: Error :: LoadLibraryExWUnknown)) }) } # [doc = " Attempts to pin the module represented by the current `Library` into memory."] # [doc = ""] # [doc = " Calls `GetModuleHandleExW` with the flag `GET_MODULE_HANDLE_EX_FLAG_PIN` to pin the module."] # [doc = " See the [MSDN documentation][msdn] for more information."] # [doc = ""] # [doc = " [msdn]: https://learn.microsoft.com/en-us/windows/win32/api/libloaderapi/nf-libloaderapi-getmodulehandleexw"] # [doc = ""] # [doc = " If successful, the module will remain in memory regardless of the refcount for this `Library`"] pub fn pin (& self) -> Result < () , crate :: Error > { const GET_MODULE_HANDLE_EX_FLAG_PIN : u32 = 0x1 ; const GET_MODULE_HANDLE_EX_FLAG_FROM_ADDRESS : u32 = 0x4 ; unsafe { let mut handle : HMODULE = 0 ; with_get_last_error (| source | crate :: Error :: GetModuleHandleExW { source } , | | { let result = GetModuleHandleExW (GET_MODULE_HANDLE_EX_FLAG_PIN | GET_MODULE_HANDLE_EX_FLAG_FROM_ADDRESS , self . 0 as * const u16 , & mut handle ,) ; if result == 0 { None } else { Some (()) } } ,) . map_err (| e | e . unwrap_or (crate :: Error :: GetModuleHandleExWUnknown)) } } # [doc = " Get a pointer to a function or static variable by symbol name."] # [doc = ""] # [doc = " The `symbol` may not contain any null bytes, with the exception of the last byte. A null"] # [doc = " terminated `symbol` may avoid a string allocation in some cases."] # [doc = ""] # [doc = " Symbol is interpreted as-is; no mangling is done. This means that symbols like `x::y` are"] # [doc = " most likely invalid."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Users of this API must specify the correct type of the function or variable loaded."] pub unsafe fn get < T > (& self , symbol : impl AsSymbolName) -> Result < Symbol < T > , crate :: Error > { ensure_compatible_types :: < T , FARPROC > () ? ; symbol . symbol_name (| windows_symbol | { with_get_last_error (| source | crate :: Error :: GetProcAddress { source } , | | { let symbol = GetProcAddress (self . 0 , windows_symbol . cast ()) ; if symbol . is_none () { None } else { Some (Symbol { pointer : symbol , pd : marker :: PhantomData , }) } } ,) . map_err (| e | e . unwrap_or (crate :: Error :: GetProcAddressUnknown)) }) } # [doc = " Get a pointer to a function or static variable by ordinal number."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Users of this API must specify the correct type of the function or variable loaded."] pub unsafe fn get_ordinal < T > (& self , ordinal : u16) -> Result < Symbol < T > , crate :: Error > { ensure_compatible_types :: < T , FARPROC > () ? ; with_get_last_error (| source | crate :: Error :: GetProcAddress { source } , | | { let ordinal = ordinal as usize as * const _ ; let symbol = GetProcAddress (self . 0 , ordinal) ; if symbol . is_none () { None } else { Some (Symbol { pointer : symbol , pd : marker :: PhantomData , }) } } ,) . map_err (| e | e . unwrap_or (crate :: Error :: GetProcAddressUnknown)) } # [doc = " Convert the `Library` to a raw handle."] pub fn into_raw (self) -> HMODULE { let handle = self . 0 ; mem :: forget (self) ; handle } # [doc = " Convert a raw handle to a `Library`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The handle must be the result of a successful call of `LoadLibraryA`, `LoadLibraryW`,"] # [doc = " `LoadLibraryExW`, or `LoadLibraryExA`, or a handle previously returned by the"] # [doc = " `Library::into_raw` call."] pub unsafe fn from_raw (handle : HMODULE) -> Library { Library (handle) } # [doc = " Unload the library."] # [doc = ""] # [doc = " You only need to call this if you are interested in handling any errors that may arise when"] # [doc = " library is unloaded. Otherwise this will be done when `Library` is dropped."] # [doc = ""] # [doc = " The underlying data structures may still get leaked if an error does occur."] pub fn close (self) -> Result < () , crate :: Error > { let result = with_get_last_error (| source | crate :: Error :: FreeLibrary { source } , | | { if unsafe { FreeLibrary (self . 0) == 0 } { None } else { Some (()) } } ,) . map_err (| e | e . unwrap_or (crate :: Error :: FreeLibraryUnknown)) ; mem :: forget (self) ; result } }
    };
}

impl_111!()