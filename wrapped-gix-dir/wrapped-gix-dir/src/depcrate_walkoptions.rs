// Generated macro for Options (struct)
macro_rules! Depcrate_walkOptions {
() => {
// Module: crate::walk
// Provides: {"Options"}
// Dependencies: {}
# [doc = " Options for use in [`walk()`](function::walk()) function."] # [derive (Default , Debug , Clone , Copy , PartialEq , Eq , Hash , Ord , PartialOrd)] pub struct Options < 'a > { # [doc = " If `true`, the filesystem will store paths as decomposed unicode, i.e. `ä` becomes `\"a\\u{308}\"`, which means that"] # [doc = " we have to turn these forms back from decomposed to precomposed unicode before storing it in the index or generally"] # [doc = " using it. This also applies to input received from the command-line, so callers may have to be aware of this and"] # [doc = " perform conversions accordingly."] # [doc = " If `false`, no conversions will be performed."] pub precompose_unicode : bool , # [doc = " If true, the filesystem ignores the case of input, which makes `A` the same file as `a`."] # [doc = " This is also called case-folding."] # [doc = " Note that [pathspecs](Context::pathspec) must also be using the same defaults, which makes them match case-insensitive"] # [doc = " automatically."] pub ignore_case : bool , # [doc = " If `true`, we will stop figuring out if any directory that is a candidate for recursion is also a nested repository,"] # [doc = " which saves time but leads to recurse into it. If `false`, nested repositories will not be traversed."] pub recurse_repositories : bool , # [doc = " If `true`, entries that are pruned and whose [Kind](crate::entry::Kind) is known will be emitted."] pub emit_pruned : bool , # [doc = " If `Some(mode)`, entries that are ignored will be emitted according to the given `mode`."] # [doc = " If `None`, ignored entries will not be emitted at all."] pub emit_ignored : Option < EmissionMode > , # [doc = " When the walk is for deletion, this must be `Some(_)` to assure we don't collapse directories that have precious files in"] # [doc = " them, and otherwise assure that no entries are observable that shouldn't be deleted."] # [doc = " If `None`, precious files are treated like expendable files, which is usually what you want when displaying them"] # [doc = " for addition to the repository, and the collapse of folders can be more generous in relation to ignored files."] pub for_deletion : Option < ForDeletionMode > , # [doc = " If `true`, we will not only find non-bare repositories in untracked directories, but also bare ones."] # [doc = ""] # [doc = " Note that this is very costly, but without it, bare repositories will appear like untracked directories when collapsed,"] # [doc = " and they will be recursed into."] pub classify_untracked_bare_repositories : bool , # [doc = " If `true`, we will also emit entries for tracked items. Otherwise these will remain 'hidden', even if a pathspec directly"] # [doc = " refers to it."] pub emit_tracked : bool , # [doc = " Controls the way untracked files are emitted. By default, this is happening immediately and without any simplification."] pub emit_untracked : EmissionMode , # [doc = " If `true`, emit empty directories as well. Note that a directory also counts as empty if it has any amount or depth of nested"] # [doc = " subdirectories, as long as none of them includes a file."] # [doc = " Thus, this makes leaf-level empty directories visible, as those don't have any content."] pub emit_empty_directories : bool , # [doc = " If `None`, no entries inside of collapsed directories are emitted. Otherwise, act as specified by `Some(mode)`."] pub emit_collapsed : Option < CollapsedEntriesEmissionMode > , # [doc = " This is a `libgit2` compatibility flag, and if enabled, symlinks that point to directories will be considered a directory"] # [doc = " when checking for exclusion."] # [doc = ""] # [doc = " This is relevant if `src2` points to `src`, and is excluded with `src2/`. If `false`, `src2` will not be excluded,"] # [doc = " if `true` it will be excluded as the symlink is considered a directory."] # [doc = ""] # [doc = " In other words, for Git compatibility this flag should be `false`, the default, for `git2` compatibility it should be `true`."] pub symlinks_to_directories_are_ignored_like_directories : bool , # [doc = " A set of all git worktree checkouts that are located within the main worktree directory."] # [doc = ""] # [doc = " They will automatically be detected as 'tracked', but without providing index information (as there is no actual index entry)."] # [doc = " Note that the unicode composition must match the `precompose_unicode` field so that paths will match verbatim."] pub worktree_relative_worktree_dirs : Option < & 'a BTreeSet < BString > > , }
};
}
