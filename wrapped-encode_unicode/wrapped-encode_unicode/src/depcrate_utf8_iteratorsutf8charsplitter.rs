// Generated macro for Utf8CharSplitter (struct)
macro_rules! Depcrate_utf8_iteratorsUtf8CharSplitter {
() => {
// Module: crate::utf8_iterators
// Provides: {"Utf8CharSplitter"}
// Dependencies: {}
# [doc = " Converts an iterator of `Utf8Char` (or `&Utf8Char`)"] # [doc = " to an iterator of `u8`s."] # [doc = ""] # [doc = " Is equivalent to calling `.flatten()` or `.flat_map()` on the original iterator,"] # [doc = " but the returned iterator is ~40% faster."] # [doc = ""] # [doc = " The iterator also implements `Read` (if the `std` feature isn't disabled)."] # [doc = " Reading will never produce an error, and calls to `.read()` and `.next()`"] # [doc = " can be mixed."] # [doc = ""] # [doc = " The exact number of bytes cannot be known in advance, but `size_hint()`"] # [doc = " gives the possible range."] # [doc = " (min: all remaining characters are ASCII, max: all require four bytes)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " From iterator of values:"] # [doc = ""] # [doc = " ```"] # [doc = " use encode_unicode::{IterExt, CharExt};"] # [doc = ""] # [doc = " let iterator = \"foo\".chars().map(|c| c.to_utf8() );"] # [doc = " let mut bytes = [0; 4];"] # [doc = " iterator.to_bytes().zip(&mut bytes).for_each(|(b,dst)| *dst = b );"] # [doc = " assert_eq!(&bytes, b\"foo\\0\");"] # [doc = " ```"] # [doc = ""] # [doc = " From iterator of references:"] # [doc = ""] # [cfg_attr (feature = "std" , doc = " ```")] # [cfg_attr (not (feature = "std") , doc = " ```no_compile")] # [doc = " use encode_unicode::{IterExt, CharExt, Utf8Char};"] # [doc = ""] # [doc = " let chars: Vec<Utf8Char> = \"💣 bomb 💣\".chars().map(|c| c.to_utf8() ).collect();"] # [doc = " let bytes: Vec<u8> = chars.iter().to_bytes().collect();"] # [doc = " let flat_map: Vec<u8> = chars.iter().cloned().flatten().collect();"] # [doc = " assert_eq!(bytes, flat_map);"] # [doc = " ```"] # [doc = ""] # [doc = " `Read`ing from it:"] # [doc = ""] # [cfg_attr (feature = "std" , doc = " ```")] # [cfg_attr (not (feature = "std") , doc = " ```no_compile")] # [doc = " use encode_unicode::{IterExt, CharExt};"] # [doc = " use std::io::Read;"] # [doc = ""] # [doc = " let s = \"Ååh‽\";"] # [doc = " assert_eq!(s.len(), 8);"] # [doc = " let mut buf = [b'E'; 9];"] # [doc = " let mut reader = s.chars().map(|c| c.to_utf8() ).to_bytes();"] # [doc = " assert_eq!(reader.read(&mut buf[..]).unwrap(), 8);"] # [doc = " assert_eq!(reader.read(&mut buf[..]).unwrap(), 0);"] # [doc = " assert_eq!(&buf[..8], s.as_bytes());"] # [doc = " assert_eq!(buf[8], b'E');"] # [doc = " ```"] # [derive (Clone)] pub struct Utf8CharSplitter < U : Borrow < Utf8Char > , I : Iterator < Item = U > > { inner : I , prev : u32 , }
};
}
