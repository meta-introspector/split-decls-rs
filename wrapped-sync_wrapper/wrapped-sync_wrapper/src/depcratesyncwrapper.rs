// Generated macro for SyncWrapper (struct)
macro_rules! DepcrateSyncWrapper {
() => {
// Module: crate
// Provides: {"SyncWrapper"}
// Dependencies: {}
# [doc = " A mutual exclusion primitive that relies on static type information only"] # [doc = ""] # [doc = " In some cases synchronization can be proven statically: whenever you hold an exclusive `&mut`"] # [doc = " reference, the Rust type system ensures that no other part of the program can hold another"] # [doc = " reference to the data. Therefore it is safe to access it even if the current thread obtained"] # [doc = " this reference via a channel. Whenever this is the case, the overhead of allocating and locking"] # [doc = " a [`Mutex`] can be avoided by using this static version."] # [doc = ""] # [doc = " One example where this is often applicable is [`Future`], which requires an exclusive reference"] # [doc = " for its [`poll`] method: While a given `Future` implementation may not be safe to access by"] # [doc = " multiple threads concurrently, the executor can only run the `Future` on one thread at any"] # [doc = " given time, making it [`Sync`] in practice as long as the implementation is `Send`. You can"] # [doc = " therefore use the static mutex to prove that your data structure is `Sync` even though it"] # [doc = " contains such a `Future`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use sync_wrapper::SyncWrapper;"] # [doc = " use std::future::Future;"] # [doc = ""] # [doc = " struct MyThing {"] # [doc = "     future: SyncWrapper<Box<dyn Future<Output = String> + Send>>,"] # [doc = " }"] # [doc = ""] # [doc = " impl MyThing {"] # [doc = "     // all accesses to `self.future` now require an exclusive reference or ownership"] # [doc = " }"] # [doc = ""] # [doc = " fn assert_sync<T: Sync>() {}"] # [doc = ""] # [doc = " assert_sync::<MyThing>();"] # [doc = " ```"] # [doc = ""] # [doc = " [`Mutex`]: https://doc.rust-lang.org/std/sync/struct.Mutex.html"] # [doc = " [`Future`]: https://doc.rust-lang.org/std/future/trait.Future.html"] # [doc = " [`poll`]: https://doc.rust-lang.org/std/future/trait.Future.html#method.poll"] # [doc = " [`Sync`]: https://doc.rust-lang.org/std/marker/trait.Sync.html"] # [repr (transparent)] pub struct SyncWrapper < T > (T) ;
};
}
