macro_rules! deps {
    () => {
        ScriptWithExt!();
        Script!();
    };
}

macro_rules! ScriptWithExtensionsProperty {
    () => {
        deps!();
        # [doc = " A struct that efficiently stores `Script` and `Script_Extensions` property data."] # [doc = ""] # [doc = " <div class=\"stab unstable\">"] # [doc = " 🚧 This code is considered unstable; it may change at any time, in breaking or non-breaking ways,"] # [doc = " including in SemVer minor releases. While the serde representation of data structs is guaranteed"] # [doc = " to be stable, their Rust representation might not be. Use with caution."] # [doc = " </div>"] # [derive (Debug , Eq , PartialEq , Clone , yoke :: Yokeable , zerofrom :: ZeroFrom)] # [cfg_attr (feature = "datagen" , derive (serde :: Serialize , databake :: Bake))] # [cfg_attr (feature = "datagen" , databake (path = icu_properties :: provider))] # [cfg_attr (feature = "serde" , derive (serde :: Deserialize))] pub struct ScriptWithExtensionsProperty < 'data > { # [doc = " Note: The `ScriptWithExt` values in this array will assume a 12-bit layout. The 2"] # [doc = " higher order bits 11..10 will indicate how to deduce the Script value and"] # [doc = " Script_Extensions value, nearly matching the representation"] # [doc = " [in ICU](https://github.com/unicode-org/icu/blob/main/icu4c/source/common/uprops.h):"] # [doc = ""] # [doc = " | High order 2 bits value | Script                                                 | Script_Extensions                                              |"] # [doc = " |-------------------------|--------------------------------------------------------|----------------------------------------------------------------|"] # [doc = " | 3                       | First value in sub-array, index given by lower 10 bits | Sub-array excluding first value, index given by lower 10 bits  |"] # [doc = " | 2                       | Script=Inherited                                       | Entire sub-array, index given by lower 10 bits                 |"] # [doc = " | 1                       | Script=Common                                          | Entire sub-array, index given by lower 10 bits                 |"] # [doc = " | 0                       | Value in lower 10 bits                                 | `[ Script value ]` single-element array                        |"] # [doc = ""] # [doc = " When the lower 10 bits of the value are used as an index, that index is"] # [doc = " used for the outer-level vector of the nested `extensions` structure."] # [cfg_attr (feature = "serde" , serde (borrow))] pub trie : CodePointTrie < 'data , ScriptWithExt > , # [doc = " This companion structure stores Script_Extensions values, which are"] # [doc = " themselves arrays / vectors. This structure only stores the values for"] # [doc = " cases in which `scx(cp) != [ sc(cp) ]`. Each sub-vector is distinct. The"] # [doc = " sub-vector represents the Script_Extensions array value for a code point,"] # [doc = " and may also indicate Script value, as described for the `trie` field."] # [cfg_attr (feature = "serde" , serde (borrow))] pub extensions : VarZeroVec < 'data , ZeroSlice < Script > > , }
    };
}

ScriptWithExtensionsProperty!();