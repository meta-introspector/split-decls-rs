macro_rules! deps {
    () => {
        CodePointMapData!();
        CodePointMapDataBorrowed!();
        CodePointSetData!();
    };
}

macro_rules! impl_12 {
    () => {
        deps!();
        impl < 'a , T : TrieValue > CodePointMapDataBorrowed < 'a , T > { # [doc = " Get the value this map has associated with code point `ch`"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::properties::CodePointMapData;"] # [doc = " use icu::properties::props::GeneralCategory;"] # [doc = ""] # [doc = " let gc = CodePointMapData::<GeneralCategory>::new();"] # [doc = ""] # [doc = " assert_eq!(gc.get('木'), GeneralCategory::OtherLetter);  // U+6728"] # [doc = " assert_eq!(gc.get('🎃'), GeneralCategory::OtherSymbol);  // U+1F383 JACK-O-LANTERN"] # [doc = " ```"] # [inline] pub fn get (self , ch : char) -> T { self . map . get (ch) } # [doc = " See [`Self::get`]."] # [inline] pub fn get32 (self , ch : u32) -> T { self . map . get32 (ch) } # [doc = " Get a [`CodePointSetData`] for all elements corresponding to a particular value"] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::properties::props::GeneralCategory;"] # [doc = " use icu::properties::CodePointMapData;"] # [doc = ""] # [doc = " let gc = CodePointMapData::<GeneralCategory>::new();"] # [doc = ""] # [doc = " let other_letter_set_data ="] # [doc = "     gc.get_set_for_value(GeneralCategory::OtherLetter);"] # [doc = " let other_letter_set = other_letter_set_data.as_borrowed();"] # [doc = ""] # [doc = " assert!(other_letter_set.contains('木')); // U+6728"] # [doc = " assert!(!other_letter_set.contains('🎃')); // U+1F383 JACK-O-LANTERN"] # [doc = " ```"] # [cfg (feature = "alloc")] pub fn get_set_for_value (self , value : T) -> CodePointSetData { let set = self . map . get_set_for_value (value) ; CodePointSetData :: from_code_point_inversion_list (set) } # [doc = " Yields an [`Iterator`] returning ranges of consecutive code points that"] # [doc = " share the same value in the [`CodePointMapData`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::properties::props::GeneralCategory;"] # [doc = " use icu::properties::CodePointMapData;"] # [doc = ""] # [doc = " let gc = CodePointMapData::<GeneralCategory>::new();"] # [doc = " let mut ranges = gc.iter_ranges();"] # [doc = " let next = ranges.next().unwrap();"] # [doc = " assert_eq!(next.range, 0..=31);"] # [doc = " assert_eq!(next.value, GeneralCategory::Control);"] # [doc = " let next = ranges.next().unwrap();"] # [doc = " assert_eq!(next.range, 32..=32);"] # [doc = " assert_eq!(next.value, GeneralCategory::SpaceSeparator);"] # [doc = " ```"] pub fn iter_ranges (self) -> impl Iterator < Item = CodePointMapRange < T > > + 'a { self . map . iter_ranges () } # [doc = " Yields an [`Iterator`] returning ranges of consecutive code points that"] # [doc = " share the same value `v` in the [`CodePointMapData`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = ""] # [doc = " ```"] # [doc = " use icu::properties::props::GeneralCategory;"] # [doc = " use icu::properties::CodePointMapData;"] # [doc = ""] # [doc = " let gc = CodePointMapData::<GeneralCategory>::new();"] # [doc = " let mut ranges = gc.iter_ranges_for_value(GeneralCategory::UppercaseLetter);"] # [doc = " assert_eq!(ranges.next().unwrap(), 'A' as u32..='Z' as u32);"] # [doc = " assert_eq!(ranges.next().unwrap(), 'À' as u32..='Ö' as u32);"] # [doc = " assert_eq!(ranges.next().unwrap(), 'Ø' as u32..='Þ' as u32);"] # [doc = " ```"] pub fn iter_ranges_for_value (self , val : T) -> impl Iterator < Item = RangeInclusive < u32 > > + 'a { self . map . iter_ranges () . filter (move | r | r . value == val) . map (| r | r . range) } # [doc = " Yields an [`Iterator`] returning ranges of consecutive code points that"] # [doc = " do *not* have the value `v` in the [`CodePointMapData`]."] pub fn iter_ranges_for_value_complemented (self , val : T ,) -> impl Iterator < Item = RangeInclusive < u32 > > + 'a { self . map . iter_ranges_mapped (move | value | value != val) . filter (| v | v . value) . map (| v | v . range) } # [doc = " Exposed for FFI needs, could be exposed in general in the future but we should"] # [doc = " have a use case first."] # [doc = ""] # [doc = " FFI needs this since it operates on erased maps and can't use `iter_ranges_for_group()`"] # [doc (hidden)] pub fn iter_ranges_mapped < U : Eq + 'a > (self , predicate : impl FnMut (T) -> U + Copy + 'a ,) -> impl Iterator < Item = CodePointMapRange < U > > + 'a { self . map . iter_ranges_mapped (predicate) } }
    };
}

impl_12!();