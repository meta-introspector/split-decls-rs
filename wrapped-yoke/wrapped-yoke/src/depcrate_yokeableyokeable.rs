// Generated macro for Yokeable (trait)
macro_rules! Depcrate_yokeableYokeable {
() => {
// Module: crate::yokeable
// Provides: {"Yokeable"}
// Dependencies: {}
# [doc = " The `Yokeable<'a>` trait is implemented on the `'static` version of any zero-copy type; for"] # [doc = " example, `Cow<'static, T>` implements `Yokeable<'a>` (for all `'a`)."] # [doc = ""] # [doc = " One can use"] # [doc = " `Yokeable::Output` on this trait to obtain the \"lifetime'd\" value of the `Cow<'static, T>`,"] # [doc = " e.g. `<Cow<'static, T> as Yokeable<'a>'>::Output` is `Cow<'a, T>`."] # [doc = ""] # [doc = " A [`Yokeable`] type is essentially one with a covariant lifetime parameter,"] # [doc = " matched to the parameter in the trait definition. The trait allows one to cast"] # [doc = " the covariant lifetime to and from `'static`."] # [doc = ""] # [doc = " **Most of the time, if you need to implement [`Yokeable`], you should be able to use the safe"] # [doc = " [`#[derive(Yokeable)]`](yoke_derive::Yokeable) custom derive.**"] # [doc = ""] # [doc = " While Rust does not yet have GAT syntax, for the purpose of this documentation"] # [doc = " we shall refer to \"`Self` with a lifetime `'a`\" with the syntax `Self<'a>`."] # [doc = " Self<'static> is a stand-in for the HKT Self<'_>: lifetime -> type."] # [doc = ""] # [doc = " With this terminology, [`Yokeable`]  exposes ways to cast between `Self<'static>` and `Self<'a>` generically."] # [doc = " This is useful for turning covariant lifetimes to _dynamic_ lifetimes, where `'static` is"] # [doc = " used as a way to \"erase\" the lifetime."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This trait is safe to implement on types with a _covariant_ lifetime parameter, i.e. one where"] # [doc = " [`Self::transform()`]'s body can simply be `{ self }`. This will occur when the lifetime"] # [doc = " parameter is used within references, but not in the arguments of function pointers or in mutable"] # [doc = " positions (either in `&mut` or via interior mutability)"] # [doc = ""] # [doc = " This trait must be implemented on the `'static` version of such a type, e.g. one should"] # [doc = " implement `Yokeable<'a>` (for all `'a`) on `Cow<'static, T>`."] # [doc = ""] # [doc = " This trait is also safe to implement on types that do not borrow memory."] # [doc = ""] # [doc = " There are further constraints on implementation safety on individual methods."] # [doc = ""] # [doc = " # Implementation example"] # [doc = ""] # [doc = " Implementing this trait manually is unsafe. Where possible, you should use the safe"] # [doc = " [`#[derive(Yokeable)]`](yoke_derive::Yokeable) custom derive instead. We include an example"] # [doc = " in case you have your own zero-copy abstractions you wish to make yokeable."] # [doc = ""] # [doc = " ```rust"] # [doc = " # use yoke::Yokeable;"] # [doc = " # use std::borrow::Cow;"] # [doc = " # use std::{mem, ptr};"] # [doc = " struct Bar<'a> {"] # [doc = "     numbers: Cow<'a, [u8]>,"] # [doc = "     string: Cow<'a, str>,"] # [doc = "     owned: Vec<u8>,"] # [doc = " }"] # [doc = ""] # [doc = " unsafe impl<'a> Yokeable<'a> for Bar<'static> {"] # [doc = "     type Output = Bar<'a>;"] # [doc = "     fn transform(&'a self) -> &'a Bar<'a> {"] # [doc = "         // covariant lifetime cast, can be done safely"] # [doc = "         self"] # [doc = "     }"] # [doc = ""] # [doc = "     fn transform_owned(self) -> Bar<'a> {"] # [doc = "         // covariant lifetime cast, can be done safely"] # [doc = "         self"] # [doc = "     }"] # [doc = ""] # [doc = "     unsafe fn make(from: Bar<'a>) -> Self {"] # [doc = "         unsafe { mem::transmute(from) }"] # [doc = "     }"] # [doc = ""] # [doc = "     fn transform_mut<F>(&'a mut self, f: F)"] # [doc = "     where"] # [doc = "         F: 'static + FnOnce(&'a mut Self::Output),"] # [doc = "     {"] # [doc = "         unsafe { f(mem::transmute::<&mut Self, &mut Self::Output>(self)) }"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub unsafe trait Yokeable < 'a > : 'static { # [doc = " This type MUST be `Self` with the `'static` replaced with `'a`, i.e. `Self<'a>`"] type Output : 'a ; # [doc = " This method must cast `self` between `&'a Self<'static>` and `&'a Self<'a>`."] # [doc = ""] # [doc = " # Implementation safety"] # [doc = ""] # [doc = " If the invariants of [`Yokeable`] are being satisfied, the body of this method"] # [doc = " should simply be `{ self }`, though it's acceptable to include additional assertions"] # [doc = " if desired."] fn transform (& 'a self) -> & 'a Self :: Output ; # [doc = " This method must cast `self` between `Self<'static>` and `Self<'a>`."] # [doc = ""] # [doc = " # Implementation safety"] # [doc = ""] # [doc = " If the invariants of [`Yokeable`] are being satisfied, the body of this method"] # [doc = " should simply be `{ self }`, though it's acceptable to include additional assertions"] # [doc = " if desired."] fn transform_owned (self) -> Self :: Output ; # [doc = " This method can be used to cast away `Self<'a>`'s lifetime."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The returned value must be destroyed before the data `from` was borrowing from is."] # [doc = ""] # [doc = " # Implementation safety"] # [doc = ""] # [doc = " A safe implementation of this method must be equivalent to a transmute between"] # [doc = " `Self<'a>` and `Self<'static>`"] unsafe fn make (from : Self :: Output) -> Self ; # [doc = " This method must cast `self` between `&'a mut Self<'static>` and `&'a mut Self<'a>`,"] # [doc = " and pass it to `f`."] # [doc = ""] # [doc = " # Implementation safety"] # [doc = ""] # [doc = " A safe implementation of this method must be equivalent to a pointer cast/transmute between"] # [doc = " `&mut Self<'a>` and `&mut Self<'static>` being passed to `f`"] # [doc = ""] # [doc = " # Why is this safe?"] # [doc = ""] # [doc = " Typically covariant lifetimes become invariant when hidden behind an `&mut`,"] # [doc = " which is why the implementation of this method cannot just be `f(self)`."] # [doc = " The reason behind this is that while _reading_ a covariant lifetime that has been cast to a shorter"] # [doc = " one is always safe (this is roughly the definition of a covariant lifetime), writing"] # [doc = " may not necessarily be safe since you could write a smaller reference to it. For example,"] # [doc = " the following code is unsound because it manages to stuff a `'a` lifetime into a `Cow<'static>`"] # [doc = ""] # [doc = " ```rust,compile_fail"] # [doc = " # use std::borrow::Cow;"] # [doc = " # use yoke::Yokeable;"] # [doc = " struct Foo {"] # [doc = "     str: String,"] # [doc = "     cow: Cow<'static, str>,"] # [doc = " }"] # [doc = ""] # [doc = " fn unsound<'a>(foo: &'a mut Foo) {"] # [doc = "     let a: &str = &foo.str;"] # [doc = "     foo.cow.transform_mut(|cow| *cow = Cow::Borrowed(a));"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " However, this code will not compile because [`Yokeable::transform_mut()`] requires `F: 'static`."] # [doc = " This enforces that while `F` may mutate `Self<'a>`, it can only mutate it in a way that does"] # [doc = " not insert additional references. For example, `F` may call `to_owned()` on a `Cow` and mutate it,"] # [doc = " but it cannot insert a new _borrowed_ reference because it has nowhere to borrow _from_ --"] # [doc = " `f` does not contain any borrowed references, and while we give it `Self<'a>` (which contains borrowed"] # [doc = " data), that borrowed data is known to be valid"] # [doc = ""] # [doc = " Note that the `for<'b>` is also necessary, otherwise the following code would compile:"] # [doc = ""] # [doc = " ```rust,compile_fail"] # [doc = " # use std::borrow::Cow;"] # [doc = " # use yoke::Yokeable;"] # [doc = " # use std::mem;"] # [doc = " #"] # [doc = " // also safely implements Yokeable<'a>"] # [doc = " struct Bar<'a> {"] # [doc = "     num: u8,"] # [doc = "     cow: Cow<'a, u8>,"] # [doc = " }"] # [doc = ""] # [doc = " fn unsound<'a>(bar: &'a mut Bar<'static>) {"] # [doc = "     bar.transform_mut(move |bar| bar.cow = Cow::Borrowed(&bar.num));"] # [doc = " }"] # [doc = " #"] # [doc = " # unsafe impl<'a> Yokeable<'a> for Bar<'static> {"] # [doc = " #     type Output = Bar<'a>;"] # [doc = " #     fn transform(&'a self) -> &'a Bar<'a> {"] # [doc = " #         self"] # [doc = " #     }"] # [doc = " #"] # [doc = " #     fn transform_owned(self) -> Bar<'a> {"] # [doc = " #         // covariant lifetime cast, can be done safely"] # [doc = " #         self"] # [doc = " #     }"] # [doc = " #"] # [doc = " #     unsafe fn make(from: Bar<'a>) -> Self {"] # [doc = " #         let ret = mem::transmute_copy(&from);"] # [doc = " #         mem::forget(from);"] # [doc = " #         ret"] # [doc = " #     }"] # [doc = " #"] # [doc = " #     fn transform_mut<F>(&'a mut self, f: F)"] # [doc = " #     where"] # [doc = " #         F: 'static + FnOnce(&'a mut Self::Output),"] # [doc = " #     {"] # [doc = " #         unsafe { f(mem::transmute(self)) }"] # [doc = " #     }"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " which is unsound because `bar` could be moved later, and we do not want to be able to"] # [doc = " self-insert references to it."] # [doc = ""] # [doc = " The `for<'b>` enforces this by stopping the author of the closure from matching up the input"] # [doc = " `&'b Self::Output` lifetime with `'a` and borrowing directly from it."] # [doc = ""] # [doc = " Thus the only types of mutations allowed are ones that move around already-borrowed data, or"] # [doc = " introduce new owned data:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use std::borrow::Cow;"] # [doc = " # use yoke::Yokeable;"] # [doc = " struct Foo {"] # [doc = "     str: String,"] # [doc = "     cow: Cow<'static, str>,"] # [doc = " }"] # [doc = ""] # [doc = " fn sound(foo: &mut Foo) {"] # [doc = "     foo.cow.transform_mut(move |cow| cow.to_mut().push('a'));"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " More formally, a reference to an object that `f` assigns to a reference"] # [doc = " in Self<'a> could be obtained from:"] # [doc = "  - a local variable: the compiler rejects the assignment because 'a certainly"] # [doc = "    outlives local variables in f."] # [doc = "  - a field in its argument: because of the for<'b> bound, the call to `f`"] # [doc = "    must be valid for a particular 'b that is strictly shorter than 'a. Thus,"] # [doc = "    the compiler rejects the assignment."] # [doc = "  - a reference field in Self<'a>: this does not extend the set of"] # [doc = "    non-static lifetimes reachable from Self<'a>, so this is fine."] # [doc = "  - one of f's captures: since F: 'static, the resulting reference must refer"] # [doc = "    to 'static data."] # [doc = "  - a static or thread_local variable: ditto."] fn transform_mut < F > (& 'a mut self , f : F) where F : 'static + for < 'b > FnOnce (& 'b mut Self :: Output) ; }
};
}
