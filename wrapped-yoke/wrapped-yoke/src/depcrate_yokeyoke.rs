// Generated macro for Yoke (struct)
macro_rules! Depcrate_yokeYoke {
() => {
// Module: crate::yoke
// Provides: {"Yoke"}
// Dependencies: {}
# [doc = " A Cow-like borrowed object \"yoked\" to its backing data."] # [doc = ""] # [doc = " This allows things like zero copy deserialized data to carry around"] # [doc = " shared references to their backing buffer, by \"erasing\" their static lifetime"] # [doc = " and turning it into a dynamically managed one."] # [doc = ""] # [doc = " `Y` (the [`Yokeable`]) is the object containing the references,"] # [doc = " and will typically be of the form `Foo<'static>`. The `'static` is"] # [doc = " not the actual lifetime of the data, rather it is a convenient way to mark the"] # [doc = " erased lifetime and make it dynamic."] # [doc = ""] # [doc = " `C` is the \"cart\", which `Y` may contain references to. After the yoke is constructed,"] # [doc = " the cart serves little purpose except to guarantee that `Y`'s references remain valid"] # [doc = " for as long as the yoke remains in memory (by calling the destructor at the appropriate moment)."] # [doc = ""] # [doc = " The primary constructor for [`Yoke`] is [`Yoke::attach_to_cart()`]. Several variants of that"] # [doc = " constructor are provided to serve numerous types of call sites and `Yoke` signatures."] # [doc = ""] # [doc = " The key behind this type is [`Yoke::get()`], where calling [`.get()`][Yoke::get] on a type like"] # [doc = " `Yoke<Cow<'static, str>, _>` will get you a short-lived `&'a Cow<'a, str>`, restricted to the"] # [doc = " lifetime of the borrow used during `.get()`. This is entirely safe since the `Cow` borrows from"] # [doc = " the cart type `C`, which cannot be interfered with as long as the `Yoke` is borrowed by `.get"] # [doc = " ()`. `.get()` protects access by essentially reifying the erased lifetime to a safe local one"] # [doc = " when necessary."] # [doc = ""] # [doc = " Furthermore, there are various [`.map_project()`][Yoke::map_project] methods that allow turning a `Yoke`"] # [doc = " into another `Yoke` containing a different type that may contain elements of the original yoked"] # [doc = " value. See the [`Yoke::map_project()`] docs for more details."] # [doc = ""] # [doc = " In general, `C` is a concrete type, but it is also possible for it to be a trait object."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " For example, we can use this to store zero-copy deserialized data in a cache:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use yoke::Yoke;"] # [doc = " # use std::rc::Rc;"] # [doc = " # use std::borrow::Cow;"] # [doc = " # fn load_from_cache(_filename: &str) -> Rc<[u8]> {"] # [doc = " #     // dummy implementation"] # [doc = " #     Rc::new([0x5, 0, 0, 0, 0, 0, 0, 0, 0x68, 0x65, 0x6c, 0x6c, 0x6f])"] # [doc = " # }"] # [doc = ""] # [doc = " fn load_object(filename: &str) -> Yoke<Cow<'static, str>, Rc<[u8]>> {"] # [doc = "     let rc: Rc<[u8]> = load_from_cache(filename);"] # [doc = "     Yoke::<Cow<'static, str>, Rc<[u8]>>::attach_to_cart(rc, |data: &[u8]| {"] # [doc = "         // essentially forcing a #[serde(borrow)]"] # [doc = "         Cow::Borrowed(bincode::deserialize(data).unwrap())"] # [doc = "     })"] # [doc = " }"] # [doc = ""] # [doc = " let yoke = load_object(\"filename.bincode\");"] # [doc = " assert_eq!(&**yoke.get(), \"hello\");"] # [doc = " assert!(matches!(yoke.get(), &Cow::Borrowed(_)));"] # [doc = " ```"] pub struct Yoke < Y : for < 'a > Yokeable < 'a > , C > { yokeable : KindaSortaDangling < Y > , cart : C , }
};
}
