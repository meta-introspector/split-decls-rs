// Generated macro for KindaSortaDangling (struct)
macro_rules! Depcrate_kinda_sorta_danglingKindaSortaDangling {
() => {
// Module: crate::kinda_sorta_dangling
// Provides: {"KindaSortaDangling"}
// Dependencies: {}
# [doc = " This type is intended to be similar to the type `MaybeDangling<T>`"] # [doc = " proposed in [RFC 3336]."] # [doc = ""] # [doc = " The effect of this is that in Rust's safety model, types inside here are not"] # [doc = " expected to have any memory dependent validity properties (`dereferenceable`, `noalias`)."] # [doc = ""] # [doc = " See [#3696] for a testcase where `Yoke` fails under miri's field-retagging mode if not using"] # [doc = " KindaSortaDangling."] # [doc = ""] # [doc = " This has `T: 'static` since we don't need anything"] # [doc = " else and we don't want to have to think (more) about variance over lifetimes or dropck."] # [doc = ""] # [doc = " After [RFC 3336] lands we can use `MaybeDangling` instead."] # [doc = ""] # [doc = " Note that a version of this type also exists publicly as the [`maybe_dangling`]"] # [doc = " crate; which also exports a patched `ManuallyDrop` with similar semantics and"] # [doc = " does not require `T: 'static`. Consider using this if you need something more general"] # [doc = " and are okay with adding dependencies."] # [doc = ""] # [doc = " [RFC 3336]: https://github.com/rust-lang/rfcs/pull/3336"] # [doc = " [#3696]: https://github.com/unicode-org/icu4x/issues/3696"] # [doc = " [`maybe_dangling`](https://docs.rs/maybe-dangling/0.1.0/maybe_dangling/struct.MaybeDangling.html)"] # [repr (transparent)] pub (crate) struct KindaSortaDangling < T : 'static > { # [doc = " Safety invariant: This is always an initialized T, never uninit or other"] # [doc = " invalid bit patterns. Its drop glue will execute during Drop::drop rather than"] # [doc = " during the drop glue for KindaSortaDangling, which means that we have to be careful about"] # [doc = " not touching the values as initialized during `drop` after that, but that's a short period of time."] dangle : MaybeUninit < T > , }
};
}
