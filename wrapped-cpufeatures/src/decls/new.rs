macro_rules! new {
    () => {
        # [doc = " Create module with CPU feature detection code."] # [macro_export] macro_rules ! new { ($ mod_name : ident , $ ($ tf : tt) ,+ $ (,) ?) => { mod $ mod_name { use core :: sync :: atomic :: { AtomicU8 , Ordering :: Relaxed } ; const UNINIT : u8 = u8 :: max_value () ; static STORAGE : AtomicU8 = AtomicU8 :: new (UNINIT) ; # [doc = " Initialization token"] # [derive (Copy , Clone , Debug)] pub struct InitToken (()) ; impl InitToken { # [doc = " Get initialized value"] # [inline (always)] pub fn get (& self) -> bool { $ crate :: __unless_target_features ! { $ ($ tf) ,+ => { STORAGE . load (Relaxed) == 1 } } } } # [doc = " Get stored value and initialization token,"] # [doc = " initializing underlying storage if needed."] # [inline] pub fn init_get () -> (InitToken , bool) { let res = $ crate :: __unless_target_features ! { $ ($ tf) ,+ => { # [cold] fn init_inner () -> bool { let res = $ crate :: __detect_target_features ! ($ ($ tf) ,+) ; STORAGE . store (res as u8 , Relaxed) ; res } let val = STORAGE . load (Relaxed) ; if val == UNINIT { init_inner () } else { val == 1 } } } ; (InitToken (()) , res) } # [doc = " Initialize underlying storage if needed and get initialization token."] # [inline] pub fn init () -> InitToken { init_get () . 0 } # [doc = " Initialize underlying storage if needed and get stored value."] # [inline] pub fn get () -> bool { init_get () . 1 } } } ; }
    };
}

new!()