// Generated macro for chinese_based_date_from_fixed (function)
macro_rules! Depcrate_chinese_basedchinese_based_date_from_fixed {
() => {
// Module: crate::chinese_based
// Provides: {"chinese_based_date_from_fixed"}
// Dependencies: {}
# [doc = " Get a chinese based date from a fixed date, with the related Gregorian year"] # [doc = ""] # [doc = " Months are calculated by iterating through the dates of new moons until finding the last month which"] # [doc = " does not exceed the given fixed date. The day of month is calculated by subtracting the fixed date"] # [doc = " from the fixed date of the beginning of the month."] # [doc = ""] # [doc = " The calculation for `elapsed_years` and `month` in this function are based on code from _Calendrical Calculations_ by Reingold & Dershowitz."] # [doc = " Lisp reference code: <https://github.com/EdReingold/calendar-code2/blob/main/calendar.l#L5414-L5459>"] pub fn chinese_based_date_from_fixed < C : ChineseBased > (date : RataDie) -> ChineseFromFixedResult { let year_bounds = YearBounds :: compute :: < C > (date) ; let first_day_of_year = year_bounds . new_year ; let year_float = (1.5 - 1.0 / 12.0 + ((first_day_of_year - C :: EPOCH) as f64) / MEAN_TROPICAL_YEAR) . floor () ; let year_int = i64_to_i32 (year_float as i64) ; debug_assert ! (year_int . is_ok () , "Year should be in range of i32") ; let year = year_int . unwrap_or_else (| e | e . saturate ()) ; let new_moon = new_moon_before :: < C > ((date + 1) . as_moment ()) ; let month_i64 = ((new_moon - first_day_of_year) as f64 / MEAN_SYNODIC_MONTH) . round () as i64 + 1 ; debug_assert ! (((u8 :: MIN as i64) ..= (u8 :: MAX as i64)) . contains (& month_i64) , "Month should be in range of u8! Value {month_i64} failed for RD {date:?}") ; let month = month_i64 as u8 ; let day_i64 = date - new_moon + 1 ; debug_assert ! (((u8 :: MIN as i64) ..= (u8 :: MAX as i64)) . contains (& month_i64) , "Day should be in range of u8! Value {month_i64} failed for RD {date:?}") ; let day = day_i64 as u8 ; let leap_month = if year_bounds . is_leap () { NonZeroU8 :: new (get_leap_month_from_new_year :: < C > (first_day_of_year)) } else { None } ; ChineseFromFixedResult { year , month , day , year_bounds , leap_month , } }
};
}
