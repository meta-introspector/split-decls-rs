// Generated macro for impl_50 (impl)
macro_rules! Depcrate_inotifyimpl_50 {
() => {
// Module: crate::inotify
// Provides: {"impl_50"}
// Dependencies: {}
impl Inotify { # [doc = " Creates an [`Inotify`] instance"] # [doc = ""] # [doc = " Initializes an inotify instance by calling [`inotify_init1`]."] # [doc = ""] # [doc = " This method passes both flags accepted by [`inotify_init1`], not giving"] # [doc = " the user any choice in the matter, as not passing the flags would be"] # [doc = " inappropriate in the context of this wrapper:"] # [doc = ""] # [doc = " - [`IN_CLOEXEC`] prevents leaking file descriptors to other processes."] # [doc = " - [`IN_NONBLOCK`] controls the blocking behavior of the inotify API,"] # [doc = "   which is entirely managed by this wrapper."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Directly returns the error from the call to [`inotify_init1`], without"] # [doc = " adding any error conditions of its own."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use inotify::Inotify;"] # [doc = ""] # [doc = " let inotify = Inotify::init()"] # [doc = "     .expect(\"Failed to initialize an inotify instance\");"] # [doc = " ```"] # [doc = ""] # [doc = " [`inotify_init1`]: inotify_sys::inotify_init1"] # [doc = " [`IN_CLOEXEC`]: inotify_sys::IN_CLOEXEC"] # [doc = " [`IN_NONBLOCK`]: inotify_sys::IN_NONBLOCK"] pub fn init () -> io :: Result < Inotify > { let fd = unsafe { ffi :: inotify_init1 (ffi :: IN_CLOEXEC | ffi :: IN_NONBLOCK) } ; if fd == - 1 { return Err (io :: Error :: last_os_error ()) ; } Ok (Inotify { fd : Arc :: new (FdGuard { fd , close_on_drop : AtomicBool :: new (true) , }) , }) } # [doc = " Gets an interface that allows adding and removing watches."] # [doc = " See [`Watches::add`] and [`Watches::remove`]."] pub fn watches (& self) -> Watches { Watches :: new (self . fd . clone ()) } # [doc = " Deprecated: use `Inotify.watches().add()` instead"] # [deprecated = "use `Inotify.watches().add()` instead"] pub fn add_watch < P > (& mut self , path : P , mask : WatchMask) -> io :: Result < WatchDescriptor > where P : AsRef < Path > , { self . watches () . add (path , mask) } # [doc = " Deprecated: use `Inotify.watches().remove()` instead"] # [deprecated = "use `Inotify.watches().remove()` instead"] pub fn rm_watch (& mut self , wd : WatchDescriptor) -> io :: Result < () > { self . watches () . remove (wd) } # [doc = " Waits until events are available, then returns them"] # [doc = ""] # [doc = " Blocks the current thread until at least one event is available. If this"] # [doc = " is not desirable, please consider [`Inotify::read_events`]."] # [doc = ""] # [doc = " This method calls [`Inotify::read_events`] internally and behaves"] # [doc = " essentially the same, apart from the blocking behavior. Please refer to"] # [doc = " the documentation of [`Inotify::read_events`] for more information."] # [doc = ""] # [doc = " Care should be taken when using a [`Vec`] as buffer. See the [crate"] # [doc = " documentation](crate) for details."] pub fn read_events_blocking < 'a > (& mut self , buffer : & 'a mut [u8]) -> io :: Result < Events < 'a > > { unsafe { let res = fcntl (* * self . fd , F_GETFL) ; if res == - 1 { return Err (io :: Error :: last_os_error ()) ; } if fcntl (* * self . fd , F_SETFL , res & ! O_NONBLOCK) == - 1 { return Err (io :: Error :: last_os_error ()) ; } } ; let result = self . read_events (buffer) ; unsafe { let res = fcntl (* * self . fd , F_GETFL) ; if res == - 1 { return Err (io :: Error :: last_os_error ()) ; } if fcntl (* * self . fd , F_SETFL , res | O_NONBLOCK) == - 1 { return Err (io :: Error :: last_os_error ()) ; } } ; result } # [doc = " Returns one buffer's worth of available events"] # [doc = ""] # [doc = " Reads as many events as possible into `buffer`, and returns an iterator"] # [doc = " over them. If no events are available, an iterator is still returned. If"] # [doc = " you need a method that will block until at least one event is available,"] # [doc = " please consider [`read_events_blocking`]."] # [doc = ""] # [doc = " Please note that inotify will merge identical successive unread events"] # [doc = " into a single event. This means this method can not be used to count the"] # [doc = " number of file system events."] # [doc = ""] # [doc = " The `buffer` argument, as the name indicates, is used as a buffer for"] # [doc = " the inotify events. Its contents may be overwritten."] # [doc = ""] # [doc = " Care should be taken when using a [`Vec`] as buffer. See the [crate"] # [doc = " documentation](crate) for details."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function directly returns all errors from the call to [`read`]."] # [doc = " In addition, [`ErrorKind::UnexpectedEof`] is returned, if the call to"] # [doc = " [`read`] returns `0`, signaling end-of-file."] # [doc = ""] # [doc = " If `buffer` is too small, this will result in an error with"] # [doc = " [`ErrorKind::InvalidInput`]. On very old Linux kernels,"] # [doc = " [`ErrorKind::UnexpectedEof`] will be returned instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use inotify::Inotify;"] # [doc = " use std::io::ErrorKind;"] # [doc = ""] # [doc = " let mut inotify = Inotify::init()"] # [doc = "     .expect(\"Failed to initialize an inotify instance\");"] # [doc = ""] # [doc = " let mut buffer = [0; 1024];"] # [doc = " let events = loop {"] # [doc = "     match inotify.read_events(&mut buffer) {"] # [doc = "         Ok(events) => break events,"] # [doc = "         Err(error) if error.kind() == ErrorKind::WouldBlock => continue,"] # [doc = "         _ => panic!(\"Error while reading events\"),"] # [doc = "     }"] # [doc = " };"] # [doc = ""] # [doc = " for event in events {"] # [doc = "     // Handle event"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [`read_events_blocking`]: Self::read_events_blocking"] # [doc = " [`read`]: libc::read"] # [doc = " [`ErrorKind::UnexpectedEof`]: std::io::ErrorKind::UnexpectedEof"] # [doc = " [`ErrorKind::InvalidInput`]: std::io::ErrorKind::InvalidInput"] pub fn read_events < 'a > (& mut self , buffer : & 'a mut [u8]) -> io :: Result < Events < 'a > > { let num_bytes = read_into_buffer (* * self . fd , buffer) ; let num_bytes = match num_bytes { 0 => { return Err (io :: Error :: new (io :: ErrorKind :: UnexpectedEof , "`read` return `0`, signaling end-of-file" ,)) ; } - 1 => { let error = io :: Error :: last_os_error () ; return Err (error) ; } _ if num_bytes < 0 => { panic ! ("{} {} {} {} {} {}" , "Unexpected return value from `read`. Received a negative" , "value that was not `-1`. According to the `read` man page" , "this shouldn't happen, as either `-1` is returned on" , "error, `0` on end-of-file, or a positive value for the" , "number of bytes read. Returned value:" , num_bytes ,) ; } _ => { let num_bytes : isize = num_bytes ; debug_assert ! (num_bytes > 0) ; num_bytes as usize } } ; Ok (Events :: new (Arc :: downgrade (& self . fd) , buffer , num_bytes)) } # [doc = " Deprecated: use `into_event_stream()` instead, which enforces a single `Stream` and predictable reads."] # [doc = " Using this method to create multiple `EventStream` instances from one `Inotify` is unsupported,"] # [doc = " as they will contend over one event source and each produce unpredictable stream contents."] # [deprecated = "use `into_event_stream()` instead, which enforces a single Stream and predictable reads"] # [cfg (feature = "stream")] pub fn event_stream < T > (& mut self , buffer : T) -> io :: Result < EventStream < T > > where T : AsMut < [u8] > + AsRef < [u8] > , { EventStream :: new (self . fd . clone () , buffer) } # [doc = " Create a stream which collects events. Consumes the `Inotify` instance."] # [doc = ""] # [doc = " Returns a asynchronous `Stream` over the Inotify instance's events."] # [doc = ""] # [doc = " Care should be taken when using a [`Vec`] as buffer. See the [crate"] # [doc = " documentation](crate) for details."] # [cfg (feature = "stream")] pub fn into_event_stream < T > (self , buffer : T) -> io :: Result < EventStream < T > > where T : AsMut < [u8] > + AsRef < [u8] > , { EventStream :: new (self . fd , buffer) } # [doc = " Creates an `Inotify` instance using the file descriptor which was originally"] # [doc = " initialized in `Inotify::init`. This is intended to be used to transform an"] # [doc = " `EventStream` back into an `Inotify`. Do not attempt to clone `Inotify` with this."] # [cfg (feature = "stream")] pub (crate) fn from_file_descriptor (fd : Arc < FdGuard >) -> Self { Inotify { fd } } # [doc = " Closes the inotify instance"] # [doc = ""] # [doc = " Closes the file descriptor referring to the inotify instance. The user"] # [doc = " usually doesn't have to call this function, as the underlying inotify"] # [doc = " instance is closed automatically, when [`Inotify`] is dropped."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Directly returns the error from the call to [`close`], without adding any"] # [doc = " error conditions of its own."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use inotify::Inotify;"] # [doc = ""] # [doc = " let mut inotify = Inotify::init()"] # [doc = "     .expect(\"Failed to initialize an inotify instance\");"] # [doc = ""] # [doc = " inotify.close()"] # [doc = "     .expect(\"Failed to close inotify instance\");"] # [doc = " ```"] # [doc = ""] # [doc = " [`close`]: libc::close"] pub fn close (self) -> io :: Result < () > { self . fd . should_not_close () ; match unsafe { ffi :: close (* * self . fd) } { 0 => Ok (()) , _ => Err (io :: Error :: last_os_error ()) , } } }
};
}
