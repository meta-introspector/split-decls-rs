// Generated macro for impl_103 (impl)
macro_rules! Depcrate_arch_all_rabinkarpimpl_103 {
() => {
// Module: crate::arch::all::rabinkarp
// Provides: {"impl_103"}
// Dependencies: {}
impl Finder { # [doc = " Create a new Rabin-Karp forward searcher for the given `needle`."] # [doc = ""] # [doc = " The needle may be empty. The empty needle matches at every byte offset."] # [doc = ""] # [doc = " Note that callers must pass the same needle to all search calls using"] # [doc = " this `Finder`."] # [inline] pub fn new (needle : & [u8]) -> Finder { let mut s = Finder { hash : Hash :: new () , hash_2pow : 1 } ; let first_byte = match needle . get (0) { None => return s , Some (& first_byte) => first_byte , } ; s . hash . add (first_byte) ; for b in needle . iter () . copied () . skip (1) { s . hash . add (b) ; s . hash_2pow = s . hash_2pow . wrapping_shl (1) ; } s } # [doc = " Return the first occurrence of the `needle` in the `haystack`"] # [doc = " given. If no such occurrence exists, then `None` is returned."] # [doc = ""] # [doc = " The `needle` provided must match the needle given to this finder at"] # [doc = " construction time."] # [doc = ""] # [doc = " The maximum value this can return is `haystack.len()`, which can only"] # [doc = " occur when the needle and haystack both have length zero. Otherwise,"] # [doc = " for non-empty haystacks, the maximum value is `haystack.len() - 1`."] # [inline] pub fn find (& self , haystack : & [u8] , needle : & [u8]) -> Option < usize > { unsafe { let hstart = haystack . as_ptr () ; let hend = hstart . add (haystack . len ()) ; let nstart = needle . as_ptr () ; let nend = nstart . add (needle . len ()) ; let found = self . find_raw (hstart , hend , nstart , nend) ? ; Some (found . distance (hstart)) } } # [doc = " Like `find`, but accepts and returns raw pointers."] # [doc = ""] # [doc = " When a match is found, the pointer returned is guaranteed to be"] # [doc = " `>= start` and `<= end`. The pointer returned is only ever equivalent"] # [doc = " to `end` when both the needle and haystack are empty. (That is, the"] # [doc = " empty string matches the empty string.)"] # [doc = ""] # [doc = " This routine is useful if you're already using raw pointers and would"] # [doc = " like to avoid converting back to a slice before executing a search."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Note that `start` and `end` below refer to both pairs of pointers given"] # [doc = " to this routine. That is, the conditions apply to both `hstart`/`hend`"] # [doc = " and `nstart`/`nend`."] # [doc = ""] # [doc = " * Both `start` and `end` must be valid for reads."] # [doc = " * Both `start` and `end` must point to an initialized value."] # [doc = " * Both `start` and `end` must point to the same allocated object and"] # [doc = " must either be in bounds or at most one byte past the end of the"] # [doc = " allocated object."] # [doc = " * Both `start` and `end` must be _derived from_ a pointer to the same"] # [doc = " object."] # [doc = " * The distance between `start` and `end` must not overflow `isize`."] # [doc = " * The distance being in bounds must not rely on \"wrapping around\" the"] # [doc = " address space."] # [doc = " * It must be the case that `start <= end`."] # [inline] pub unsafe fn find_raw (& self , hstart : * const u8 , hend : * const u8 , nstart : * const u8 , nend : * const u8 ,) -> Option < * const u8 > { let hlen = hend . distance (hstart) ; let nlen = nend . distance (nstart) ; if nlen > hlen { return None ; } let mut cur = hstart ; let end = hend . sub (nlen) ; let mut hash = Hash :: forward (cur , cur . add (nlen)) ; loop { if self . hash == hash && is_equal_raw (cur , nstart , nlen) { return Some (cur) ; } if cur >= end { return None ; } hash . roll (self , cur . read () , cur . add (nlen) . read ()) ; cur = cur . add (1) ; } } }
};
}
