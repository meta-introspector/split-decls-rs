// Generated macro for PrefilterConfig (enum)
macro_rules! Depcrate_memmem_searcherPrefilterConfig {
() => {
// Module: crate::memmem::searcher
// Provides: {"PrefilterConfig"}
// Dependencies: {}
# [doc = " Prefilter controls whether heuristics are used to accelerate searching."] # [doc = ""] # [doc = " A prefilter refers to the idea of detecting candidate matches very quickly,"] # [doc = " and then confirming whether those candidates are full matches. This"] # [doc = " idea can be quite effective since it's often the case that looking for"] # [doc = " candidates can be a lot faster than running a complete substring search"] # [doc = " over the entire input. Namely, looking for candidates can be done with"] # [doc = " extremely fast vectorized code."] # [doc = ""] # [doc = " The downside of a prefilter is that it assumes false positives (which are"] # [doc = " candidates generated by a prefilter that aren't matches) are somewhat rare"] # [doc = " relative to the frequency of full matches. That is, if a lot of false"] # [doc = " positives are generated, then it's possible for search time to be worse"] # [doc = " than if the prefilter wasn't enabled in the first place."] # [doc = ""] # [doc = " Another downside of a prefilter is that it can result in highly variable"] # [doc = " performance, where some cases are extraordinarily fast and others aren't."] # [doc = " Typically, variable performance isn't a problem, but it may be for your use"] # [doc = " case."] # [doc = ""] # [doc = " The use of prefilters in this implementation does use a heuristic to detect"] # [doc = " when a prefilter might not be carrying its weight, and will dynamically"] # [doc = " disable its use. Nevertheless, this configuration option gives callers"] # [doc = " the ability to disable prefilters if you have knowledge that they won't be"] # [doc = " useful."] # [derive (Clone , Copy , Debug)] # [non_exhaustive] pub enum PrefilterConfig { # [doc = " Never used a prefilter in substring search."] None , # [doc = " Automatically detect whether a heuristic prefilter should be used. If"] # [doc = " it is used, then heuristics will be used to dynamically disable the"] # [doc = " prefilter if it is believed to not be carrying its weight."] Auto , }
};
}
