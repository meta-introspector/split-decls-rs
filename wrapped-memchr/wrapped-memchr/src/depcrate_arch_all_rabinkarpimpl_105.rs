// Generated macro for impl_105 (impl)
macro_rules! Depcrate_arch_all_rabinkarpimpl_105 {
() => {
// Module: crate::arch::all::rabinkarp
// Provides: {"impl_105"}
// Dependencies: {}
impl FinderRev { # [doc = " Create a new Rabin-Karp reverse searcher for the given `needle`."] # [inline] pub fn new (needle : & [u8]) -> FinderRev { let mut s = FinderRev (Finder { hash : Hash :: new () , hash_2pow : 1 }) ; let last_byte = match needle . last () { None => return s , Some (& last_byte) => last_byte , } ; s . 0 . hash . add (last_byte) ; for b in needle . iter () . rev () . copied () . skip (1) { s . 0 . hash . add (b) ; s . 0 . hash_2pow = s . 0 . hash_2pow . wrapping_shl (1) ; } s } # [doc = " Return the last occurrence of the `needle` in the `haystack`"] # [doc = " given. If no such occurrence exists, then `None` is returned."] # [doc = ""] # [doc = " The `needle` provided must match the needle given to this finder at"] # [doc = " construction time."] # [doc = ""] # [doc = " The maximum value this can return is `haystack.len()`, which can only"] # [doc = " occur when the needle and haystack both have length zero. Otherwise,"] # [doc = " for non-empty haystacks, the maximum value is `haystack.len() - 1`."] # [inline] pub fn rfind (& self , haystack : & [u8] , needle : & [u8]) -> Option < usize > { unsafe { let hstart = haystack . as_ptr () ; let hend = hstart . add (haystack . len ()) ; let nstart = needle . as_ptr () ; let nend = nstart . add (needle . len ()) ; let found = self . rfind_raw (hstart , hend , nstart , nend) ? ; Some (found . distance (hstart)) } } # [doc = " Like `rfind`, but accepts and returns raw pointers."] # [doc = ""] # [doc = " When a match is found, the pointer returned is guaranteed to be"] # [doc = " `>= start` and `<= end`. The pointer returned is only ever equivalent"] # [doc = " to `end` when both the needle and haystack are empty. (That is, the"] # [doc = " empty string matches the empty string.)"] # [doc = ""] # [doc = " This routine is useful if you're already using raw pointers and would"] # [doc = " like to avoid converting back to a slice before executing a search."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Note that `start` and `end` below refer to both pairs of pointers given"] # [doc = " to this routine. That is, the conditions apply to both `hstart`/`hend`"] # [doc = " and `nstart`/`nend`."] # [doc = ""] # [doc = " * Both `start` and `end` must be valid for reads."] # [doc = " * Both `start` and `end` must point to an initialized value."] # [doc = " * Both `start` and `end` must point to the same allocated object and"] # [doc = " must either be in bounds or at most one byte past the end of the"] # [doc = " allocated object."] # [doc = " * Both `start` and `end` must be _derived from_ a pointer to the same"] # [doc = " object."] # [doc = " * The distance between `start` and `end` must not overflow `isize`."] # [doc = " * The distance being in bounds must not rely on \"wrapping around\" the"] # [doc = " address space."] # [doc = " * It must be the case that `start <= end`."] # [inline] pub unsafe fn rfind_raw (& self , hstart : * const u8 , hend : * const u8 , nstart : * const u8 , nend : * const u8 ,) -> Option < * const u8 > { let hlen = hend . distance (hstart) ; let nlen = nend . distance (nstart) ; if nlen > hlen { return None ; } let mut cur = hend . sub (nlen) ; let start = hstart ; let mut hash = Hash :: reverse (cur , cur . add (nlen)) ; loop { if self . 0 . hash == hash && is_equal_raw (cur , nstart , nlen) { return Some (cur) ; } if cur <= start { return None ; } cur = cur . sub (1) ; hash . roll (& self . 0 , cur . add (nlen) . read () , cur . read ()) ; } } }
};
}
