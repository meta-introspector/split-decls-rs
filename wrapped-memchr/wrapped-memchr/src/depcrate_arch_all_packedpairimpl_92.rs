// Generated macro for impl_92 (impl)
macro_rules! Depcrate_arch_all_packedpairimpl_92 {
() => {
// Module: crate::arch::all::packedpair
// Provides: {"impl_92"}
// Dependencies: {}
impl Finder { # [doc = " Create a new prefilter that reports possible locations where the given"] # [doc = " needle matches."] # [inline] pub fn new (needle : & [u8]) -> Option < Finder > { Finder :: with_pair (needle , Pair :: new (needle) ?) } # [doc = " Create a new prefilter using the pair given."] # [doc = ""] # [doc = " If the prefilter could not be constructed, then `None` is returned."] # [doc = ""] # [doc = " This constructor permits callers to control precisely which pair of"] # [doc = " bytes is used as a predicate."] # [inline] pub fn with_pair (needle : & [u8] , pair : Pair) -> Option < Finder > { let byte1 = needle [usize :: from (pair . index1 ())] ; let byte2 = needle [usize :: from (pair . index2 ())] ; Some (Finder { pair , byte1 , byte2 }) } # [doc = " Run this finder on the given haystack as a prefilter."] # [doc = ""] # [doc = " If a candidate match is found, then an offset where the needle *could*"] # [doc = " begin in the haystack is returned."] # [inline] pub fn find_prefilter (& self , haystack : & [u8]) -> Option < usize > { let mut i = 0 ; let index1 = usize :: from (self . pair . index1 ()) ; let index2 = usize :: from (self . pair . index2 ()) ; loop { i += memchr (self . byte1 , & haystack [i ..]) ? ; let found = i ; i += 1 ; let aligned1 = match found . checked_sub (index1) { None => continue , Some (aligned1) => aligned1 , } ; let aligned2 = match aligned1 . checked_add (index2) { None => continue , Some (aligned_index2) => aligned_index2 , } ; if haystack . get (aligned2) . map_or (true , | & b | b != self . byte2) { continue ; } return Some (aligned1) ; } } # [doc = " Returns the pair of offsets (into the needle) used to check as a"] # [doc = " predicate before confirming whether a needle exists at a particular"] # [doc = " position."] # [inline] pub fn pair (& self) -> & Pair { & self . pair } }
};
}
