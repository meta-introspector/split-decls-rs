// Generated macro for impl_268 (impl)
macro_rules! Depcrate_arch_x86_64_avx2_packedpairimpl_268 {
() => {
// Module: crate::arch::x86_64::avx2::packedpair
// Provides: {"impl_268"}
// Dependencies: {}
impl Finder { # [doc = " Create a new pair searcher. The searcher returned can either report"] # [doc = " exact matches of `needle` or act as a prefilter and report candidate"] # [doc = " positions of `needle`."] # [doc = ""] # [doc = " If AVX2 is unavailable in the current environment or if a [`Pair`]"] # [doc = " could not be constructed from the needle given, then `None` is"] # [doc = " returned."] # [inline] pub fn new (needle : & [u8]) -> Option < Finder > { Finder :: with_pair (needle , Pair :: new (needle) ?) } # [doc = " Create a new \"packed pair\" finder using the pair of bytes given."] # [doc = ""] # [doc = " This constructor permits callers to control precisely which pair of"] # [doc = " bytes is used as a predicate."] # [doc = ""] # [doc = " If AVX2 is unavailable in the current environment, then `None` is"] # [doc = " returned."] # [inline] pub fn with_pair (needle : & [u8] , pair : Pair) -> Option < Finder > { if Finder :: is_available () { unsafe { Some (Finder :: with_pair_impl (needle , pair)) } } else { None } } # [doc = " Create a new `Finder` specific to SSE2 vectors and routines."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Same as the safety for `packedpair::Finder::new`, and callers must also"] # [doc = " ensure that both SSE2 and AVX2 are available."] # [target_feature (enable = "sse2" , enable = "avx2")] # [inline] unsafe fn with_pair_impl (needle : & [u8] , pair : Pair) -> Finder { let sse2 = packedpair :: Finder :: < __m128i > :: new (needle , pair) ; let avx2 = packedpair :: Finder :: < __m256i > :: new (needle , pair) ; Finder { sse2 , avx2 } } # [doc = " Returns true when this implementation is available in the current"] # [doc = " environment."] # [doc = ""] # [doc = " When this is true, it is guaranteed that [`Finder::with_pair`] will"] # [doc = " return a `Some` value. Similarly, when it is false, it is guaranteed"] # [doc = " that `Finder::with_pair` will return a `None` value. Notice that this"] # [doc = " does not guarantee that [`Finder::new`] will return a `Finder`. Namely,"] # [doc = " even when `Finder::is_available` is true, it is not guaranteed that a"] # [doc = " valid [`Pair`] can be found from the needle given."] # [doc = ""] # [doc = " Note also that for the lifetime of a single program, if this returns"] # [doc = " true then it will always return true."] # [inline] pub fn is_available () -> bool { # [cfg (not (target_feature = "sse2"))] { false } # [cfg (target_feature = "sse2")] { # [cfg (target_feature = "avx2")] { true } # [cfg (not (target_feature = "avx2"))] { # [cfg (feature = "std")] { std :: is_x86_feature_detected ! ("avx2") } # [cfg (not (feature = "std"))] { false } } } } # [doc = " Execute a search using AVX2 vectors and routines."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " When `haystack.len()` is less than [`Finder::min_haystack_len`]."] # [inline] pub fn find (& self , haystack : & [u8] , needle : & [u8]) -> Option < usize > { unsafe { self . find_impl (haystack , needle) } } # [doc = " Run this finder on the given haystack as a prefilter."] # [doc = ""] # [doc = " If a candidate match is found, then an offset where the needle *could*"] # [doc = " begin in the haystack is returned."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " When `haystack.len()` is less than [`Finder::min_haystack_len`]."] # [inline] pub fn find_prefilter (& self , haystack : & [u8]) -> Option < usize > { unsafe { self . find_prefilter_impl (haystack) } } # [doc = " Execute a search using AVX2 vectors and routines."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " When `haystack.len()` is less than [`Finder::min_haystack_len`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " (The target feature safety obligation is automatically fulfilled by"] # [doc = " virtue of being a method on `Finder`, which can only be constructed"] # [doc = " when it is safe to call `sse2` and `avx2` routines.)"] # [target_feature (enable = "sse2" , enable = "avx2")] # [inline] unsafe fn find_impl (& self , haystack : & [u8] , needle : & [u8] ,) -> Option < usize > { if haystack . len () < self . avx2 . min_haystack_len () { self . sse2 . find (haystack , needle) } else { self . avx2 . find (haystack , needle) } } # [doc = " Execute a prefilter search using AVX2 vectors and routines."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " When `haystack.len()` is less than [`Finder::min_haystack_len`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " (The target feature safety obligation is automatically fulfilled by"] # [doc = " virtue of being a method on `Finder`, which can only be constructed"] # [doc = " when it is safe to call `sse2` and `avx2` routines.)"] # [target_feature (enable = "sse2" , enable = "avx2")] # [inline] unsafe fn find_prefilter_impl (& self , haystack : & [u8]) -> Option < usize > { if haystack . len () < self . avx2 . min_haystack_len () { self . sse2 . find_prefilter (haystack) } else { self . avx2 . find_prefilter (haystack) } } # [doc = " Returns the pair of offsets (into the needle) used to check as a"] # [doc = " predicate before confirming whether a needle exists at a particular"] # [doc = " position."] # [inline] pub fn pair (& self) -> & Pair { self . avx2 . pair () } # [doc = " Returns the minimum haystack length that this `Finder` can search."] # [doc = ""] # [doc = " Using a haystack with length smaller than this in a search will result"] # [doc = " in a panic. The reason for this restriction is that this finder is"] # [doc = " meant to be a low-level component that is part of a larger substring"] # [doc = " strategy. In that sense, it avoids trying to handle all cases and"] # [doc = " instead only handles the cases that it can handle very well."] # [inline] pub fn min_haystack_len (& self) -> usize { self . sse2 . min_haystack_len () } }
};
}
