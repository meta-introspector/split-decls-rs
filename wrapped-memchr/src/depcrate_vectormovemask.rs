// Generated macro for MoveMask (trait)
macro_rules! Depcrate_vectorMoveMask {
() => {
// Module: crate::vector
// Provides: {"MoveMask"}
// Dependencies: {}
# [doc = " A trait that abstracts over a vector-to-scalar operation called"] # [doc = " \"move mask.\""] # [doc = ""] # [doc = " On x86-64, this is `_mm_movemask_epi8` for SSE2 and `_mm256_movemask_epi8`"] # [doc = " for AVX2. It takes a vector of `u8` lanes and returns a scalar where the"] # [doc = " `i`th bit is set if and only if the most significant bit in the `i`th lane"] # [doc = " of the vector is set. The simd128 ISA for wasm32 also supports this"] # [doc = " exact same operation natively."] # [doc = ""] # [doc = " ... But aarch64 doesn't. So we have to fake it with more instructions and"] # [doc = " a slightly different representation. We could do extra work to unify the"] # [doc = " representations, but then would require additional costs in the hot path"] # [doc = " for `memchr` and `packedpair`. So instead, we abstraction over the specific"] # [doc = " representation with this trait and define the operations we actually need."] pub (crate) trait MoveMask : Copy + core :: fmt :: Debug { # [doc = " Return a mask that is all zeros except for the least significant `n`"] # [doc = " lanes in a corresponding vector."] fn all_zeros_except_least_significant (n : usize) -> Self ; # [doc = " Returns true if and only if this mask has a a non-zero bit anywhere."] fn has_non_zero (self) -> bool ; # [doc = " Returns the number of bits set to 1 in this mask."] fn count_ones (self) -> usize ; # [doc = " Does a bitwise `and` operation between `self` and `other`."] fn and (self , other : Self) -> Self ; # [doc = " Does a bitwise `or` operation between `self` and `other`."] fn or (self , other : Self) -> Self ; # [doc = " Returns a mask that is equivalent to `self` but with the least"] # [doc = " significant 1-bit set to 0."] fn clear_least_significant_bit (self) -> Self ; # [doc = " Returns the offset of the first non-zero lane this mask represents."] fn first_offset (self) -> usize ; # [doc = " Returns the offset of the last non-zero lane this mask represents."] fn last_offset (self) -> usize ; }
};
}
