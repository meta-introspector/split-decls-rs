macro_rules! deps {
    () => {
        TwoIter!();
        Two!();
        Iter!();
    };
}

macro_rules! impl_223 {
    () => {
        deps!();
        impl Two { # [doc = " Create a new searcher that finds occurrences of the needle bytes given."] # [doc = ""] # [doc = " This particular searcher is specialized to use AVX2 vector instructions"] # [doc = " that typically make it quite fast. (SSE2 is used for haystacks that"] # [doc = " are too short to accommodate an AVX2 vector.)"] # [doc = ""] # [doc = " If either SSE2 or AVX2 is unavailable in the current environment, then"] # [doc = " `None` is returned."] # [inline] pub fn new (needle1 : u8 , needle2 : u8) -> Option < Two > { if Two :: is_available () { unsafe { Some (Two :: new_unchecked (needle1 , needle2)) } } else { None } } # [doc = " Create a new finder specific to AVX2 vectors and routines without"] # [doc = " checking that either SSE2 or AVX2 is available."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Callers must guarantee that it is safe to execute both `sse2` and"] # [doc = " `avx2` instructions in the current environment."] # [doc = ""] # [doc = " Note that it is a common misconception that if one compiles for an"] # [doc = " `x86_64` target, then they therefore automatically have access to SSE2"] # [doc = " instructions. While this is almost always the case, it isn't true in"] # [doc = " 100% of cases."] # [target_feature (enable = "sse2" , enable = "avx2")] # [inline] pub unsafe fn new_unchecked (needle1 : u8 , needle2 : u8) -> Two { Two { sse2 : generic :: Two :: new (needle1 , needle2) , avx2 : generic :: Two :: new (needle1 , needle2) , } } # [doc = " Returns true when this implementation is available in the current"] # [doc = " environment."] # [doc = ""] # [doc = " When this is true, it is guaranteed that [`Two::new`] will return"] # [doc = " a `Some` value. Similarly, when it is false, it is guaranteed that"] # [doc = " `Two::new` will return a `None` value."] # [doc = ""] # [doc = " Note also that for the lifetime of a single program, if this returns"] # [doc = " true then it will always return true."] # [inline] pub fn is_available () -> bool { # [cfg (not (target_feature = "sse2"))] { false } # [cfg (target_feature = "sse2")] { # [cfg (target_feature = "avx2")] { true } # [cfg (not (target_feature = "avx2"))] { # [cfg (feature = "std")] { std :: is_x86_feature_detected ! ("avx2") } # [cfg (not (feature = "std"))] { false } } } } # [doc = " Return the first occurrence of one of the needle bytes in the given"] # [doc = " haystack. If no such occurrence exists, then `None` is returned."] # [doc = ""] # [doc = " The occurrence is reported as an offset into `haystack`. Its maximum"] # [doc = " value is `haystack.len() - 1`."] # [inline] pub fn find (& self , haystack : & [u8]) -> Option < usize > { unsafe { generic :: search_slice_with_raw (haystack , | s , e | { self . find_raw (s , e) }) } } # [doc = " Return the last occurrence of one of the needle bytes in the given"] # [doc = " haystack. If no such occurrence exists, then `None` is returned."] # [doc = ""] # [doc = " The occurrence is reported as an offset into `haystack`. Its maximum"] # [doc = " value is `haystack.len() - 1`."] # [inline] pub fn rfind (& self , haystack : & [u8]) -> Option < usize > { unsafe { generic :: search_slice_with_raw (haystack , | s , e | { self . rfind_raw (s , e) }) } } # [doc = " Like `find`, but accepts and returns raw pointers."] # [doc = ""] # [doc = " When a match is found, the pointer returned is guaranteed to be"] # [doc = " `>= start` and `< end`."] # [doc = ""] # [doc = " This routine is useful if you're already using raw pointers and would"] # [doc = " like to avoid converting back to a slice before executing a search."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " * Both `start` and `end` must be valid for reads."] # [doc = " * Both `start` and `end` must point to an initialized value."] # [doc = " * Both `start` and `end` must point to the same allocated object and"] # [doc = " must either be in bounds or at most one byte past the end of the"] # [doc = " allocated object."] # [doc = " * Both `start` and `end` must be _derived from_ a pointer to the same"] # [doc = " object."] # [doc = " * The distance between `start` and `end` must not overflow `isize`."] # [doc = " * The distance being in bounds must not rely on \"wrapping around\" the"] # [doc = " address space."] # [doc = ""] # [doc = " Note that callers may pass a pair of pointers such that `start >= end`."] # [doc = " In that case, `None` will always be returned."] # [inline] pub unsafe fn find_raw (& self , start : * const u8 , end : * const u8 ,) -> Option < * const u8 > { if start >= end { return None ; } let len = end . distance (start) ; if len < __m256i :: BYTES { return if len < __m128i :: BYTES { generic :: fwd_byte_by_byte (start , end , | b | { b == self . sse2 . needle1 () || b == self . sse2 . needle2 () }) } else { self . find_raw_sse2 (start , end) } ; } self . find_raw_avx2 (start , end) } # [doc = " Like `rfind`, but accepts and returns raw pointers."] # [doc = ""] # [doc = " When a match is found, the pointer returned is guaranteed to be"] # [doc = " `>= start` and `< end`."] # [doc = ""] # [doc = " This routine is useful if you're already using raw pointers and would"] # [doc = " like to avoid converting back to a slice before executing a search."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " * Both `start` and `end` must be valid for reads."] # [doc = " * Both `start` and `end` must point to an initialized value."] # [doc = " * Both `start` and `end` must point to the same allocated object and"] # [doc = " must either be in bounds or at most one byte past the end of the"] # [doc = " allocated object."] # [doc = " * Both `start` and `end` must be _derived from_ a pointer to the same"] # [doc = " object."] # [doc = " * The distance between `start` and `end` must not overflow `isize`."] # [doc = " * The distance being in bounds must not rely on \"wrapping around\" the"] # [doc = " address space."] # [doc = ""] # [doc = " Note that callers may pass a pair of pointers such that `start >= end`."] # [doc = " In that case, `None` will always be returned."] # [inline] pub unsafe fn rfind_raw (& self , start : * const u8 , end : * const u8 ,) -> Option < * const u8 > { if start >= end { return None ; } let len = end . distance (start) ; if len < __m256i :: BYTES { return if len < __m128i :: BYTES { generic :: rev_byte_by_byte (start , end , | b | { b == self . sse2 . needle1 () || b == self . sse2 . needle2 () }) } else { self . rfind_raw_sse2 (start , end) } ; } self . rfind_raw_avx2 (start , end) } # [doc = " Execute a search using SSE2 vectors and routines."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Same as [`Two::find_raw`], except the distance between `start` and"] # [doc = " `end` must be at least the size of an SSE2 vector (in bytes)."] # [doc = ""] # [doc = " (The target feature safety obligation is automatically fulfilled by"] # [doc = " virtue of being a method on `Two`, which can only be constructed"] # [doc = " when it is safe to call `sse2`/`avx2` routines.)"] # [target_feature (enable = "sse2")] # [inline] unsafe fn find_raw_sse2 (& self , start : * const u8 , end : * const u8 ,) -> Option < * const u8 > { self . sse2 . find_raw (start , end) } # [doc = " Execute a search using SSE2 vectors and routines."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Same as [`Two::rfind_raw`], except the distance between `start` and"] # [doc = " `end` must be at least the size of an SSE2 vector (in bytes)."] # [doc = ""] # [doc = " (The target feature safety obligation is automatically fulfilled by"] # [doc = " virtue of being a method on `Two`, which can only be constructed"] # [doc = " when it is safe to call `sse2`/`avx2` routines.)"] # [target_feature (enable = "sse2")] # [inline] unsafe fn rfind_raw_sse2 (& self , start : * const u8 , end : * const u8 ,) -> Option < * const u8 > { self . sse2 . rfind_raw (start , end) } # [doc = " Execute a search using AVX2 vectors and routines."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Same as [`Two::find_raw`], except the distance between `start` and"] # [doc = " `end` must be at least the size of an AVX2 vector (in bytes)."] # [doc = ""] # [doc = " (The target feature safety obligation is automatically fulfilled by"] # [doc = " virtue of being a method on `Two`, which can only be constructed"] # [doc = " when it is safe to call `sse2`/`avx2` routines.)"] # [target_feature (enable = "avx2")] # [inline] unsafe fn find_raw_avx2 (& self , start : * const u8 , end : * const u8 ,) -> Option < * const u8 > { self . avx2 . find_raw (start , end) } # [doc = " Execute a search using AVX2 vectors and routines."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Same as [`Two::rfind_raw`], except the distance between `start` and"] # [doc = " `end` must be at least the size of an AVX2 vector (in bytes)."] # [doc = ""] # [doc = " (The target feature safety obligation is automatically fulfilled by"] # [doc = " virtue of being a method on `Two`, which can only be constructed"] # [doc = " when it is safe to call `sse2`/`avx2` routines.)"] # [target_feature (enable = "avx2")] # [inline] unsafe fn rfind_raw_avx2 (& self , start : * const u8 , end : * const u8 ,) -> Option < * const u8 > { self . avx2 . rfind_raw (start , end) } # [doc = " Returns an iterator over all occurrences of the needle bytes in the"] # [doc = " given haystack."] # [doc = ""] # [doc = " The iterator returned implements `DoubleEndedIterator`. This means it"] # [doc = " can also be used to find occurrences in reverse order."] # [inline] pub fn iter < 'a , 'h > (& 'a self , haystack : & 'h [u8]) -> TwoIter < 'a , 'h > { TwoIter { searcher : self , it : generic :: Iter :: new (haystack) } } }
    };
}

impl_223!()