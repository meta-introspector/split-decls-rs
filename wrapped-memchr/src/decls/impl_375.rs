macro_rules! deps {
    () => {
        FinderRev!();
        FinderBuilder!();
        FindRevIter!();
        CowBytes!();
    };
}

macro_rules! impl_375 {
    () => {
        deps!();
        impl < 'n > FinderRev < 'n > { # [doc = " Create a new reverse finder for the given needle."] # [inline] pub fn new < B : ? Sized + AsRef < [u8] > > (needle : & 'n B) -> FinderRev < 'n > { FinderBuilder :: new () . build_reverse (needle) } # [doc = " Returns the index of the last occurrence of this needle in the given"] # [doc = " haystack."] # [doc = ""] # [doc = " The haystack may be any type that can be cheaply converted into a"] # [doc = " `&[u8]`. This includes, but is not limited to, `&str` and `&[u8]`."] # [doc = ""] # [doc = " # Complexity"] # [doc = ""] # [doc = " This routine is guaranteed to have worst case linear time complexity"] # [doc = " with respect to both the needle and the haystack. That is, this runs"] # [doc = " in `O(needle.len() + haystack.len())` time."] # [doc = ""] # [doc = " This routine is also guaranteed to have worst case constant space"] # [doc = " complexity."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use memchr::memmem::FinderRev;"] # [doc = ""] # [doc = " let haystack = b\"foo bar baz\";"] # [doc = " assert_eq!(Some(0), FinderRev::new(\"foo\").rfind(haystack));"] # [doc = " assert_eq!(Some(4), FinderRev::new(\"bar\").rfind(haystack));"] # [doc = " assert_eq!(None, FinderRev::new(\"quux\").rfind(haystack));"] # [doc = " ```"] pub fn rfind < B : AsRef < [u8] > > (& self , haystack : B) -> Option < usize > { self . searcher . rfind (haystack . as_ref () , self . needle . as_slice ()) } # [doc = " Returns a reverse iterator over all occurrences of a substring in a"] # [doc = " haystack."] # [doc = ""] # [doc = " # Complexity"] # [doc = ""] # [doc = " This routine is guaranteed to have worst case linear time complexity"] # [doc = " with respect to both the needle and the haystack. That is, this runs"] # [doc = " in `O(needle.len() + haystack.len())` time."] # [doc = ""] # [doc = " This routine is also guaranteed to have worst case constant space"] # [doc = " complexity."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use memchr::memmem::FinderRev;"] # [doc = ""] # [doc = " let haystack = b\"foo bar foo baz foo\";"] # [doc = " let finder = FinderRev::new(b\"foo\");"] # [doc = " let mut it = finder.rfind_iter(haystack);"] # [doc = " assert_eq!(Some(16), it.next());"] # [doc = " assert_eq!(Some(8), it.next());"] # [doc = " assert_eq!(Some(0), it.next());"] # [doc = " assert_eq!(None, it.next());"] # [doc = " ```"] # [inline] pub fn rfind_iter < 'a , 'h > (& 'a self , haystack : & 'h [u8] ,) -> FindRevIter < 'h , 'a > { FindRevIter :: new (haystack , self . as_ref ()) } # [doc = " Convert this finder into its owned variant, such that it no longer"] # [doc = " borrows the needle."] # [doc = ""] # [doc = " If this is already an owned finder, then this is a no-op. Otherwise,"] # [doc = " this copies the needle."] # [doc = ""] # [doc = " This is only available when the `std` feature is enabled."] # [cfg (feature = "alloc")] # [inline] pub fn into_owned (self) -> FinderRev < 'static > { FinderRev { needle : self . needle . into_owned () , searcher : self . searcher . clone () , } } # [doc = " Convert this finder into its borrowed variant."] # [doc = ""] # [doc = " This is primarily useful if your finder is owned and you'd like to"] # [doc = " store its borrowed variant in some intermediate data structure."] # [doc = ""] # [doc = " Note that the lifetime parameter of the returned finder is tied to the"] # [doc = " lifetime of `self`, and may be shorter than the `'n` lifetime of the"] # [doc = " needle itself. Namely, a finder's needle can be either borrowed or"] # [doc = " owned, so the lifetime of the needle returned must necessarily be the"] # [doc = " shorter of the two."] # [inline] pub fn as_ref (& self) -> FinderRev < '_ > { FinderRev { needle : CowBytes :: new (self . needle ()) , searcher : self . searcher . clone () , } } # [doc = " Returns the needle that this finder searches for."] # [doc = ""] # [doc = " Note that the lifetime of the needle returned is tied to the lifetime"] # [doc = " of the finder, and may be shorter than the `'n` lifetime. Namely, a"] # [doc = " finder's needle can be either borrowed or owned, so the lifetime of the"] # [doc = " needle returned must necessarily be the shorter of the two."] # [inline] pub fn needle (& self) -> & [u8] { self . needle . as_slice () } }
    };
}

impl_375!()