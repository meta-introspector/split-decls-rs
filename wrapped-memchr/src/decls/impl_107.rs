macro_rules! deps {
    () => {
        TwoWay!();
        Suffix!();
        SuffixKind!();
        Finder!();
        ApproximateByteSet!();
        Shift!();
        Pre!();
    };
}

macro_rules! impl_107 {
    () => {
        deps!();
        impl Finder { # [doc = " Create a searcher that finds occurrences of the given `needle`."] # [doc = ""] # [doc = " An empty `needle` results in a match at every position in a haystack,"] # [doc = " including at `haystack.len()`."] # [inline] pub fn new (needle : & [u8]) -> Finder { let byteset = ApproximateByteSet :: new (needle) ; let min_suffix = Suffix :: forward (needle , SuffixKind :: Minimal) ; let max_suffix = Suffix :: forward (needle , SuffixKind :: Maximal) ; let (period_lower_bound , critical_pos) = if min_suffix . pos > max_suffix . pos { (min_suffix . period , min_suffix . pos) } else { (max_suffix . period , max_suffix . pos) } ; let shift = Shift :: forward (needle , period_lower_bound , critical_pos) ; Finder (TwoWay { byteset , critical_pos , shift }) } # [doc = " Returns the first occurrence of `needle` in the given `haystack`, or"] # [doc = " `None` if no such occurrence could be found."] # [doc = ""] # [doc = " The `needle` given must be the same as the `needle` provided to"] # [doc = " [`Finder::new`]."] # [doc = ""] # [doc = " An empty `needle` results in a match at every position in a haystack,"] # [doc = " including at `haystack.len()`."] # [inline] pub fn find (& self , haystack : & [u8] , needle : & [u8]) -> Option < usize > { self . find_with_prefilter (None , haystack , needle) } # [doc = " This is like [`Finder::find`], but it accepts a prefilter for"] # [doc = " accelerating searches."] # [doc = ""] # [doc = " Currently this is not exposed in the public API because, at the time"] # [doc = " of writing, I didn't want to spend time thinking about how to expose"] # [doc = " the prefilter infrastructure (if at all). If you have a compelling use"] # [doc = " case for exposing this routine, please create an issue. Do *not* open"] # [doc = " a PR that just exposes `Pre` and friends. Exporting this routine will"] # [doc = " require API design."] # [inline (always)] pub (crate) fn find_with_prefilter (& self , pre : Option < Pre < '_ > > , haystack : & [u8] , needle : & [u8] ,) -> Option < usize > { match self . 0 . shift { Shift :: Small { period } => { self . find_small_imp (pre , haystack , needle , period) } Shift :: Large { shift } => { self . find_large_imp (pre , haystack , needle , shift) } } } # [inline (always)] fn find_small_imp (& self , mut pre : Option < Pre < '_ > > , haystack : & [u8] , needle : & [u8] , period : usize ,) -> Option < usize > { let mut pos = 0 ; let mut shift = 0 ; let last_byte_pos = match needle . len () . checked_sub (1) { None => return Some (pos) , Some (last_byte) => last_byte , } ; while pos + needle . len () <= haystack . len () { let mut i = cmp :: max (self . 0 . critical_pos , shift) ; if let Some (pre) = pre . as_mut () { if pre . is_effective () { pos += pre . find (& haystack [pos ..]) ? ; shift = 0 ; i = self . 0 . critical_pos ; if pos + needle . len () > haystack . len () { return None ; } } } if ! self . 0 . byteset . contains (haystack [pos + last_byte_pos]) { pos += needle . len () ; shift = 0 ; continue ; } while i < needle . len () && needle [i] == haystack [pos + i] { i += 1 ; } if i < needle . len () { pos += i - self . 0 . critical_pos + 1 ; shift = 0 ; } else { let mut j = self . 0 . critical_pos ; while j > shift && needle [j] == haystack [pos + j] { j -= 1 ; } if j <= shift && needle [shift] == haystack [pos + shift] { return Some (pos) ; } pos += period ; shift = needle . len () - period ; } } None } # [inline (always)] fn find_large_imp (& self , mut pre : Option < Pre < '_ > > , haystack : & [u8] , needle : & [u8] , shift : usize ,) -> Option < usize > { let mut pos = 0 ; let last_byte_pos = match needle . len () . checked_sub (1) { None => return Some (pos) , Some (last_byte) => last_byte , } ; 'outer : while pos + needle . len () <= haystack . len () { if let Some (pre) = pre . as_mut () { if pre . is_effective () { pos += pre . find (& haystack [pos ..]) ? ; if pos + needle . len () > haystack . len () { return None ; } } } if ! self . 0 . byteset . contains (haystack [pos + last_byte_pos]) { pos += needle . len () ; continue ; } let mut i = self . 0 . critical_pos ; while i < needle . len () && needle [i] == haystack [pos + i] { i += 1 ; } if i < needle . len () { pos += i - self . 0 . critical_pos + 1 ; } else { for j in (0 .. self . 0 . critical_pos) . rev () { if needle [j] != haystack [pos + j] { pos += shift ; continue 'outer ; } } return Some (pos) ; } } None } }
    };
}

impl_107!();