macro_rules! deps {
    () => {
        CowBytes!();
        Finder!();
        PrefilterState!();
        FindIter!();
        FinderBuilder!();
    };
}

macro_rules! impl_373 {
    () => {
        deps!();
        impl < 'n > Finder < 'n > { # [doc = " Create a new finder for the given needle."] # [inline] pub fn new < B : ? Sized + AsRef < [u8] > > (needle : & 'n B) -> Finder < 'n > { FinderBuilder :: new () . build_forward (needle) } # [doc = " Returns the index of the first occurrence of this needle in the given"] # [doc = " haystack."] # [doc = ""] # [doc = " # Complexity"] # [doc = ""] # [doc = " This routine is guaranteed to have worst case linear time complexity"] # [doc = " with respect to both the needle and the haystack. That is, this runs"] # [doc = " in `O(needle.len() + haystack.len())` time."] # [doc = ""] # [doc = " This routine is also guaranteed to have worst case constant space"] # [doc = " complexity."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use memchr::memmem::Finder;"] # [doc = ""] # [doc = " let haystack = b\"foo bar baz\";"] # [doc = " assert_eq!(Some(0), Finder::new(\"foo\").find(haystack));"] # [doc = " assert_eq!(Some(4), Finder::new(\"bar\").find(haystack));"] # [doc = " assert_eq!(None, Finder::new(\"quux\").find(haystack));"] # [doc = " ```"] # [inline] pub fn find (& self , haystack : & [u8]) -> Option < usize > { let mut prestate = PrefilterState :: new () ; let needle = self . needle . as_slice () ; self . searcher . find (& mut prestate , haystack , needle) } # [doc = " Returns an iterator over all occurrences of a substring in a haystack."] # [doc = ""] # [doc = " # Complexity"] # [doc = ""] # [doc = " This routine is guaranteed to have worst case linear time complexity"] # [doc = " with respect to both the needle and the haystack. That is, this runs"] # [doc = " in `O(needle.len() + haystack.len())` time."] # [doc = ""] # [doc = " This routine is also guaranteed to have worst case constant space"] # [doc = " complexity."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use memchr::memmem::Finder;"] # [doc = ""] # [doc = " let haystack = b\"foo bar foo baz foo\";"] # [doc = " let finder = Finder::new(b\"foo\");"] # [doc = " let mut it = finder.find_iter(haystack);"] # [doc = " assert_eq!(Some(0), it.next());"] # [doc = " assert_eq!(Some(8), it.next());"] # [doc = " assert_eq!(Some(16), it.next());"] # [doc = " assert_eq!(None, it.next());"] # [doc = " ```"] # [inline] pub fn find_iter < 'a , 'h > (& 'a self , haystack : & 'h [u8] ,) -> FindIter < 'h , 'a > { FindIter :: new (haystack , self . as_ref ()) } # [doc = " Convert this finder into its owned variant, such that it no longer"] # [doc = " borrows the needle."] # [doc = ""] # [doc = " If this is already an owned finder, then this is a no-op. Otherwise,"] # [doc = " this copies the needle."] # [doc = ""] # [doc = " This is only available when the `alloc` feature is enabled."] # [cfg (feature = "alloc")] # [inline] pub fn into_owned (self) -> Finder < 'static > { Finder { needle : self . needle . into_owned () , searcher : self . searcher . clone () , } } # [doc = " Convert this finder into its borrowed variant."] # [doc = ""] # [doc = " This is primarily useful if your finder is owned and you'd like to"] # [doc = " store its borrowed variant in some intermediate data structure."] # [doc = ""] # [doc = " Note that the lifetime parameter of the returned finder is tied to the"] # [doc = " lifetime of `self`, and may be shorter than the `'n` lifetime of the"] # [doc = " needle itself. Namely, a finder's needle can be either borrowed or"] # [doc = " owned, so the lifetime of the needle returned must necessarily be the"] # [doc = " shorter of the two."] # [inline] pub fn as_ref (& self) -> Finder < '_ > { Finder { needle : CowBytes :: new (self . needle ()) , searcher : self . searcher . clone () , } } # [doc = " Returns the needle that this finder searches for."] # [doc = ""] # [doc = " Note that the lifetime of the needle returned is tied to the lifetime"] # [doc = " of the finder, and may be shorter than the `'n` lifetime. Namely, a"] # [doc = " finder's needle can be either borrowed or owned, so the lifetime of the"] # [doc = " needle returned must necessarily be the shorter of the two."] # [inline] pub fn needle (& self) -> & [u8] { self . needle . as_slice () } }
    };
}

impl_373!();