macro_rules! deps {
    () => {
        Pair!();
        Finder!();
    };
}

macro_rules! impl_167 {
    () => {
        deps!();
        # [doc = " A \"packed pair\" finder that uses 128-bit vector operations."] # [doc = ""] # [doc = " This finder picks two bytes that it believes have high predictive power"] # [doc = " for indicating an overall match of a needle. Depending on whether"] # [doc = " `Finder::find` or `Finder::find_prefilter` is used, it reports offsets"] # [doc = " where the needle matches or could match. In the prefilter case, candidates"] # [doc = " are reported whenever the [`Pair`] of bytes given matches."] impl Finder { # [doc = " Create a new pair searcher. The searcher returned can either report"] # [doc = " exact matches of `needle` or act as a prefilter and report candidate"] # [doc = " positions of `needle`."] # [doc = ""] # [doc = " If neon is unavailable in the current environment or if a [`Pair`]"] # [doc = " could not be constructed from the needle given, then `None` is"] # [doc = " returned."] # [inline] pub fn new (needle : & [u8]) -> Option < Finder > { Finder :: with_pair (needle , Pair :: new (needle) ?) } # [doc = " Create a new \"packed pair\" finder using the pair of bytes given."] # [doc = ""] # [doc = " This constructor permits callers to control precisely which pair of"] # [doc = " bytes is used as a predicate."] # [doc = ""] # [doc = " If neon is unavailable in the current environment, then `None` is"] # [doc = " returned."] # [inline] pub fn with_pair (needle : & [u8] , pair : Pair) -> Option < Finder > { if Finder :: is_available () { unsafe { Some (Finder :: with_pair_impl (needle , pair)) } } else { None } } # [doc = " Create a new `Finder` specific to neon vectors and routines."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Same as the safety for `packedpair::Finder::new`, and callers must also"] # [doc = " ensure that neon is available."] # [target_feature (enable = "neon")] # [inline] unsafe fn with_pair_impl (needle : & [u8] , pair : Pair) -> Finder { let finder = packedpair :: Finder :: < uint8x16_t > :: new (needle , pair) ; Finder (finder) } # [doc = " Returns true when this implementation is available in the current"] # [doc = " environment."] # [doc = ""] # [doc = " When this is true, it is guaranteed that [`Finder::with_pair`] will"] # [doc = " return a `Some` value. Similarly, when it is false, it is guaranteed"] # [doc = " that `Finder::with_pair` will return a `None` value. Notice that this"] # [doc = " does not guarantee that [`Finder::new`] will return a `Finder`. Namely,"] # [doc = " even when `Finder::is_available` is true, it is not guaranteed that a"] # [doc = " valid [`Pair`] can be found from the needle given."] # [doc = ""] # [doc = " Note also that for the lifetime of a single program, if this returns"] # [doc = " true then it will always return true."] # [inline] pub fn is_available () -> bool { # [cfg (target_feature = "neon")] { true } # [cfg (not (target_feature = "neon"))] { false } } # [doc = " Execute a search using neon vectors and routines."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " When `haystack.len()` is less than [`Finder::min_haystack_len`]."] # [inline] pub fn find (& self , haystack : & [u8] , needle : & [u8]) -> Option < usize > { unsafe { self . find_impl (haystack , needle) } } # [doc = " Execute a search using neon vectors and routines."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " When `haystack.len()` is less than [`Finder::min_haystack_len`]."] # [inline] pub fn find_prefilter (& self , haystack : & [u8]) -> Option < usize > { unsafe { self . find_prefilter_impl (haystack) } } # [doc = " Execute a search using neon vectors and routines."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " When `haystack.len()` is less than [`Finder::min_haystack_len`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " (The target feature safety obligation is automatically fulfilled by"] # [doc = " virtue of being a method on `Finder`, which can only be constructed"] # [doc = " when it is safe to call `neon` routines.)"] # [target_feature (enable = "neon")] # [inline] unsafe fn find_impl (& self , haystack : & [u8] , needle : & [u8] ,) -> Option < usize > { self . 0 . find (haystack , needle) } # [doc = " Execute a prefilter search using neon vectors and routines."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " When `haystack.len()` is less than [`Finder::min_haystack_len`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " (The target feature safety obligation is automatically fulfilled by"] # [doc = " virtue of being a method on `Finder`, which can only be constructed"] # [doc = " when it is safe to call `neon` routines.)"] # [target_feature (enable = "neon")] # [inline] unsafe fn find_prefilter_impl (& self , haystack : & [u8]) -> Option < usize > { self . 0 . find_prefilter (haystack) } # [doc = " Returns the pair of offsets (into the needle) used to check as a"] # [doc = " predicate before confirming whether a needle exists at a particular"] # [doc = " position."] # [inline] pub fn pair (& self) -> & Pair { self . 0 . pair () } # [doc = " Returns the minimum haystack length that this `Finder` can search."] # [doc = ""] # [doc = " Using a haystack with length smaller than this in a search will result"] # [doc = " in a panic. The reason for this restriction is that this finder is"] # [doc = " meant to be a low-level component that is part of a larger substring"] # [doc = " strategy. In that sense, it avoids trying to handle all cases and"] # [doc = " instead only handles the cases that it can handle very well."] # [inline] pub fn min_haystack_len (& self) -> usize { self . 0 . min_haystack_len () } }
    };
}

impl_167!()