macro_rules! deps {
    () => {
        Date!();
        Offset!();
        NaiveDateTime!();
        Utc!();
        Parsed!();
        ParseResult!();
        NaiveDate!();
        StrftimeItems!();
        Weekday!();
        MappedLocalTime!();
        FixedOffset!();
        DateTime!();
    };
}

macro_rules! TimeZone {
    () => {
        deps!();
        # [doc = " The time zone."] # [doc = ""] # [doc = " The methods here are the primary constructors for the [`DateTime`] type."] pub trait TimeZone : Sized + Clone { # [doc = " An associated offset type."] # [doc = " This type is used to store the actual offset in date and time types."] # [doc = " The original `TimeZone` value can be recovered via `TimeZone::from_offset`."] type Offset : Offset ; # [doc = " Make a new `DateTime` from year, month, day, time components and current time zone."] # [doc = ""] # [doc = " This assumes the proleptic Gregorian calendar, with the year 0 being 1 BCE."] # [doc = ""] # [doc = " Returns `MappedLocalTime::None` on invalid input data."] fn with_ymd_and_hms (& self , year : i32 , month : u32 , day : u32 , hour : u32 , min : u32 , sec : u32 ,) -> MappedLocalTime < DateTime < Self > > { match NaiveDate :: from_ymd_opt (year , month , day) . and_then (| d | d . and_hms_opt (hour , min , sec)) { Some (dt) => self . from_local_datetime (& dt) , None => MappedLocalTime :: None , } } # [doc = " Makes a new `Date` from year, month, day and the current time zone."] # [doc = " This assumes the proleptic Gregorian calendar, with the year 0 being 1 BCE."] # [doc = ""] # [doc = " The time zone normally does not affect the date (unless it is between UTC-24 and UTC+24),"] # [doc = " but it will propagate to the `DateTime` values constructed via this date."] # [doc = ""] # [doc = " Panics on the out-of-range date, invalid month and/or day."] # [deprecated (since = "0.4.23" , note = "use `with_ymd_and_hms()` instead")] # [allow (deprecated)] fn ymd (& self , year : i32 , month : u32 , day : u32) -> Date < Self > { self . ymd_opt (year , month , day) . unwrap () } # [doc = " Makes a new `Date` from year, month, day and the current time zone."] # [doc = " This assumes the proleptic Gregorian calendar, with the year 0 being 1 BCE."] # [doc = ""] # [doc = " The time zone normally does not affect the date (unless it is between UTC-24 and UTC+24),"] # [doc = " but it will propagate to the `DateTime` values constructed via this date."] # [doc = ""] # [doc = " Returns `None` on the out-of-range date, invalid month and/or day."] # [deprecated (since = "0.4.23" , note = "use `with_ymd_and_hms()` instead")] # [allow (deprecated)] fn ymd_opt (& self , year : i32 , month : u32 , day : u32) -> MappedLocalTime < Date < Self > > { match NaiveDate :: from_ymd_opt (year , month , day) { Some (d) => self . from_local_date (& d) , None => MappedLocalTime :: None , } } # [doc = " Makes a new `Date` from year, day of year (DOY or \"ordinal\") and the current time zone."] # [doc = " This assumes the proleptic Gregorian calendar, with the year 0 being 1 BCE."] # [doc = ""] # [doc = " The time zone normally does not affect the date (unless it is between UTC-24 and UTC+24),"] # [doc = " but it will propagate to the `DateTime` values constructed via this date."] # [doc = ""] # [doc = " Panics on the out-of-range date and/or invalid DOY."] # [deprecated (since = "0.4.23" , note = "use `from_local_datetime()` with a `NaiveDateTime` instead")] # [allow (deprecated)] fn yo (& self , year : i32 , ordinal : u32) -> Date < Self > { self . yo_opt (year , ordinal) . unwrap () } # [doc = " Makes a new `Date` from year, day of year (DOY or \"ordinal\") and the current time zone."] # [doc = " This assumes the proleptic Gregorian calendar, with the year 0 being 1 BCE."] # [doc = ""] # [doc = " The time zone normally does not affect the date (unless it is between UTC-24 and UTC+24),"] # [doc = " but it will propagate to the `DateTime` values constructed via this date."] # [doc = ""] # [doc = " Returns `None` on the out-of-range date and/or invalid DOY."] # [deprecated (since = "0.4.23" , note = "use `from_local_datetime()` with a `NaiveDateTime` instead")] # [allow (deprecated)] fn yo_opt (& self , year : i32 , ordinal : u32) -> MappedLocalTime < Date < Self > > { match NaiveDate :: from_yo_opt (year , ordinal) { Some (d) => self . from_local_date (& d) , None => MappedLocalTime :: None , } } # [doc = " Makes a new `Date` from ISO week date (year and week number), day of the week (DOW) and"] # [doc = " the current time zone."] # [doc = " This assumes the proleptic Gregorian calendar, with the year 0 being 1 BCE."] # [doc = " The resulting `Date` may have a different year from the input year."] # [doc = ""] # [doc = " The time zone normally does not affect the date (unless it is between UTC-24 and UTC+24),"] # [doc = " but it will propagate to the `DateTime` values constructed via this date."] # [doc = ""] # [doc = " Panics on the out-of-range date and/or invalid week number."] # [deprecated (since = "0.4.23" , note = "use `from_local_datetime()` with a `NaiveDateTime` instead")] # [allow (deprecated)] fn isoywd (& self , year : i32 , week : u32 , weekday : Weekday) -> Date < Self > { self . isoywd_opt (year , week , weekday) . unwrap () } # [doc = " Makes a new `Date` from ISO week date (year and week number), day of the week (DOW) and"] # [doc = " the current time zone."] # [doc = " This assumes the proleptic Gregorian calendar, with the year 0 being 1 BCE."] # [doc = " The resulting `Date` may have a different year from the input year."] # [doc = ""] # [doc = " The time zone normally does not affect the date (unless it is between UTC-24 and UTC+24),"] # [doc = " but it will propagate to the `DateTime` values constructed via this date."] # [doc = ""] # [doc = " Returns `None` on the out-of-range date and/or invalid week number."] # [deprecated (since = "0.4.23" , note = "use `from_local_datetime()` with a `NaiveDateTime` instead")] # [allow (deprecated)] fn isoywd_opt (& self , year : i32 , week : u32 , weekday : Weekday) -> MappedLocalTime < Date < Self > > { match NaiveDate :: from_isoywd_opt (year , week , weekday) { Some (d) => self . from_local_date (& d) , None => MappedLocalTime :: None , } } # [doc = " Makes a new `DateTime` from the number of non-leap seconds"] # [doc = " since January 1, 1970 0:00:00 UTC (aka \"UNIX timestamp\")"] # [doc = " and the number of nanoseconds since the last whole non-leap second."] # [doc = ""] # [doc = " The nanosecond part can exceed 1,000,000,000 in order to represent a"] # [doc = " [leap second](crate::NaiveTime#leap-second-handling), but only when `secs % 60 == 59`."] # [doc = " (The true \"UNIX timestamp\" cannot represent a leap second unambiguously.)"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics on the out-of-range number of seconds and/or invalid nanosecond,"] # [doc = " for a non-panicking version see [`timestamp_opt`](#method.timestamp_opt)."] # [deprecated (since = "0.4.23" , note = "use `timestamp_opt()` instead")] fn timestamp (& self , secs : i64 , nsecs : u32) -> DateTime < Self > { self . timestamp_opt (secs , nsecs) . unwrap () } # [doc = " Makes a new `DateTime` from the number of non-leap seconds"] # [doc = " since January 1, 1970 0:00:00 UTC (aka \"UNIX timestamp\")"] # [doc = " and the number of nanoseconds since the last whole non-leap second."] # [doc = ""] # [doc = " The nanosecond part can exceed 1,000,000,000 in order to represent a"] # [doc = " [leap second](crate::NaiveTime#leap-second-handling), but only when `secs % 60 == 59`."] # [doc = " (The true \"UNIX timestamp\" cannot represent a leap second unambiguously.)"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `MappedLocalTime::None` on out-of-range number of seconds and/or"] # [doc = " invalid nanosecond, otherwise always returns `MappedLocalTime::Single`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use chrono::{TimeZone, Utc};"] # [doc = ""] # [doc = " assert_eq!(Utc.timestamp_opt(1431648000, 0).unwrap().to_string(), \"2015-05-15 00:00:00 UTC\");"] # [doc = " ```"] fn timestamp_opt (& self , secs : i64 , nsecs : u32) -> MappedLocalTime < DateTime < Self > > { match DateTime :: from_timestamp (secs , nsecs) { Some (dt) => MappedLocalTime :: Single (self . from_utc_datetime (& dt . naive_utc ())) , None => MappedLocalTime :: None , } } # [doc = " Makes a new `DateTime` from the number of non-leap milliseconds"] # [doc = " since January 1, 1970 0:00:00 UTC (aka \"UNIX timestamp\")."] # [doc = ""] # [doc = " Panics on out-of-range number of milliseconds for a non-panicking"] # [doc = " version see [`timestamp_millis_opt`](#method.timestamp_millis_opt)."] # [deprecated (since = "0.4.23" , note = "use `timestamp_millis_opt()` instead")] fn timestamp_millis (& self , millis : i64) -> DateTime < Self > { self . timestamp_millis_opt (millis) . unwrap () } # [doc = " Makes a new `DateTime` from the number of non-leap milliseconds"] # [doc = " since January 1, 1970 0:00:00 UTC (aka \"UNIX timestamp\")."] # [doc = ""] # [doc = ""] # [doc = " Returns `MappedLocalTime::None` on out-of-range number of milliseconds"] # [doc = " and/or invalid nanosecond, otherwise always returns"] # [doc = " `MappedLocalTime::Single`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use chrono::{MappedLocalTime, TimeZone, Utc};"] # [doc = " match Utc.timestamp_millis_opt(1431648000) {"] # [doc = "     MappedLocalTime::Single(dt) => assert_eq!(dt.timestamp(), 1431648),"] # [doc = "     _ => panic!(\"Incorrect timestamp_millis\"),"] # [doc = " };"] # [doc = " ```"] fn timestamp_millis_opt (& self , millis : i64) -> MappedLocalTime < DateTime < Self > > { match DateTime :: from_timestamp_millis (millis) { Some (dt) => MappedLocalTime :: Single (self . from_utc_datetime (& dt . naive_utc ())) , None => MappedLocalTime :: None , } } # [doc = " Makes a new `DateTime` from the number of non-leap nanoseconds"] # [doc = " since January 1, 1970 0:00:00 UTC (aka \"UNIX timestamp\")."] # [doc = ""] # [doc = " Unlike [`timestamp_millis_opt`](#method.timestamp_millis_opt), this never fails."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use chrono::{TimeZone, Utc};"] # [doc = ""] # [doc = " assert_eq!(Utc.timestamp_nanos(1431648000000000).timestamp(), 1431648);"] # [doc = " ```"] fn timestamp_nanos (& self , nanos : i64) -> DateTime < Self > { self . from_utc_datetime (& DateTime :: from_timestamp_nanos (nanos) . naive_utc ()) } # [doc = " Makes a new `DateTime` from the number of non-leap microseconds"] # [doc = " since January 1, 1970 0:00:00 UTC (aka \"UNIX timestamp\")."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use chrono::{TimeZone, Utc};"] # [doc = ""] # [doc = " assert_eq!(Utc.timestamp_micros(1431648000000).unwrap().timestamp(), 1431648);"] # [doc = " ```"] fn timestamp_micros (& self , micros : i64) -> MappedLocalTime < DateTime < Self > > { match DateTime :: from_timestamp_micros (micros) { Some (dt) => MappedLocalTime :: Single (self . from_utc_datetime (& dt . naive_utc ())) , None => MappedLocalTime :: None , } } # [doc = " Parses a string with the specified format string and returns a"] # [doc = " `DateTime` with the current offset."] # [doc = ""] # [doc = " See the [`crate::format::strftime`] module on the"] # [doc = " supported escape sequences."] # [doc = ""] # [doc = " If the to-be-parsed string includes an offset, it *must* match the"] # [doc = " offset of the TimeZone, otherwise an error will be returned."] # [doc = ""] # [doc = " See also [`DateTime::parse_from_str`] which gives a [`DateTime`] with"] # [doc = " parsed [`FixedOffset`]."] # [doc = ""] # [doc = " See also [`NaiveDateTime::parse_from_str`] which gives a [`NaiveDateTime`] without"] # [doc = " an offset, but can be converted to a [`DateTime`] with [`NaiveDateTime::and_utc`] or"] # [doc = " [`NaiveDateTime::and_local_timezone`]."] # [deprecated (since = "0.4.29" , note = "use `DateTime::parse_from_str` or `NaiveDateTime::parse_from_str` with `and_utc()` or `and_local_timezone()` instead")] fn datetime_from_str (& self , s : & str , fmt : & str) -> ParseResult < DateTime < Self > > { let mut parsed = Parsed :: new () ; parse (& mut parsed , s , StrftimeItems :: new (fmt)) ? ; parsed . to_datetime_with_timezone (self) } # [doc = " Reconstructs the time zone from the offset."] fn from_offset (offset : & Self :: Offset) -> Self ; # [doc = " Creates the offset(s) for given local `NaiveDate` if possible."] fn offset_from_local_date (& self , local : & NaiveDate) -> MappedLocalTime < Self :: Offset > ; # [doc = " Creates the offset(s) for given local `NaiveDateTime` if possible."] fn offset_from_local_datetime (& self , local : & NaiveDateTime) -> MappedLocalTime < Self :: Offset > ; # [doc = " Converts the local `NaiveDate` to the timezone-aware `Date` if possible."] # [allow (clippy :: wrong_self_convention)] # [deprecated (since = "0.4.23" , note = "use `from_local_datetime()` instead")] # [allow (deprecated)] fn from_local_date (& self , local : & NaiveDate) -> MappedLocalTime < Date < Self > > { self . offset_from_local_date (local) . map (| offset | { Date :: from_utc (* local , offset) }) } # [doc = " Converts the local `NaiveDateTime` to the timezone-aware `DateTime` if possible."] # [allow (clippy :: wrong_self_convention)] fn from_local_datetime (& self , local : & NaiveDateTime) -> MappedLocalTime < DateTime < Self > > { self . offset_from_local_datetime (local) . and_then (| off | { local . checked_sub_offset (off . fix ()) . map (| dt | DateTime :: from_naive_utc_and_offset (dt , off)) }) } # [doc = " Creates the offset for given UTC `NaiveDate`. This cannot fail."] fn offset_from_utc_date (& self , utc : & NaiveDate) -> Self :: Offset ; # [doc = " Creates the offset for given UTC `NaiveDateTime`. This cannot fail."] fn offset_from_utc_datetime (& self , utc : & NaiveDateTime) -> Self :: Offset ; # [doc = " Converts the UTC `NaiveDate` to the local time."] # [doc = " The UTC is continuous and thus this cannot fail (but can give the duplicate local time)."] # [allow (clippy :: wrong_self_convention)] # [deprecated (since = "0.4.23" , note = "use `from_utc_datetime()` instead")] # [allow (deprecated)] fn from_utc_date (& self , utc : & NaiveDate) -> Date < Self > { Date :: from_utc (* utc , self . offset_from_utc_date (utc)) } # [doc = " Converts the UTC `NaiveDateTime` to the local time."] # [doc = " The UTC is continuous and thus this cannot fail (but can give the duplicate local time)."] # [allow (clippy :: wrong_self_convention)] fn from_utc_datetime (& self , utc : & NaiveDateTime) -> DateTime < Self > { DateTime :: from_naive_utc_and_offset (* utc , self . offset_from_utc_datetime (utc)) } }
    };
}

TimeZone!()