macro_rules! deps {
    () => {
        NaiveWeek!();
        NaiveDate!();
        Weekday!();
    };
}

macro_rules! impl_526 {
    () => {
        deps!();
        impl NaiveWeek { # [doc = " Create a new `NaiveWeek`"] pub (crate) const fn new (date : NaiveDate , start : Weekday) -> Self { Self { date , start } } # [doc = " Returns a date representing the first day of the week."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the first day of the week happens to fall just out of range of `NaiveDate`"] # [doc = " (more than ca. 262,000 years away from common era)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use chrono::{NaiveDate, Weekday};"] # [doc = ""] # [doc = " let date = NaiveDate::from_ymd_opt(2022, 4, 18).unwrap();"] # [doc = " let week = date.week(Weekday::Mon);"] # [doc = " assert!(week.first_day() <= date);"] # [doc = " ```"] # [inline] # [must_use] pub const fn first_day (& self) -> NaiveDate { expect (self . checked_first_day () , "first weekday out of range for `NaiveDate`") } # [doc = " Returns a date representing the first day of the week or"] # [doc = " `None` if the date is out of `NaiveDate`'s range"] # [doc = " (more than ca. 262,000 years away from common era)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use chrono::{NaiveDate, Weekday};"] # [doc = ""] # [doc = " let date = NaiveDate::MIN;"] # [doc = " let week = date.week(Weekday::Mon);"] # [doc = " if let Some(first_day) = week.checked_first_day() {"] # [doc = "     assert!(first_day == date);"] # [doc = " } else {"] # [doc = "     // error handling code"] # [doc = "     return;"] # [doc = " };"] # [doc = " ```"] # [inline] # [must_use] pub const fn checked_first_day (& self) -> Option < NaiveDate > { let start = self . start . num_days_from_monday () as i32 ; let ref_day = self . date . weekday () . num_days_from_monday () as i32 ; let days = start - ref_day - if start > ref_day { 7 } else { 0 } ; self . date . add_days (days) } # [doc = " Returns a date representing the last day of the week."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the last day of the week happens to fall just out of range of `NaiveDate`"] # [doc = " (more than ca. 262,000 years away from common era)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use chrono::{NaiveDate, Weekday};"] # [doc = ""] # [doc = " let date = NaiveDate::from_ymd_opt(2022, 4, 18).unwrap();"] # [doc = " let week = date.week(Weekday::Mon);"] # [doc = " assert!(week.last_day() >= date);"] # [doc = " ```"] # [inline] # [must_use] pub const fn last_day (& self) -> NaiveDate { expect (self . checked_last_day () , "last weekday out of range for `NaiveDate`") } # [doc = " Returns a date representing the last day of the week or"] # [doc = " `None` if the date is out of `NaiveDate`'s range"] # [doc = " (more than ca. 262,000 years away from common era)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use chrono::{NaiveDate, Weekday};"] # [doc = ""] # [doc = " let date = NaiveDate::MAX;"] # [doc = " let week = date.week(Weekday::Mon);"] # [doc = " if let Some(last_day) = week.checked_last_day() {"] # [doc = "     assert!(last_day == date);"] # [doc = " } else {"] # [doc = "     // error handling code"] # [doc = "     return;"] # [doc = " };"] # [doc = " ```"] # [inline] # [must_use] pub const fn checked_last_day (& self) -> Option < NaiveDate > { let end = self . start . pred () . num_days_from_monday () as i32 ; let ref_day = self . date . weekday () . num_days_from_monday () as i32 ; let days = end - ref_day + if end < ref_day { 7 } else { 0 } ; self . date . add_days (days) } # [doc = " Returns a [`RangeInclusive<T>`] representing the whole week bounded by"] # [doc = " [first_day](NaiveWeek::first_day) and [last_day](NaiveWeek::last_day) functions."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the either the first or last day of the week happens to fall just out of range of"] # [doc = " `NaiveDate` (more than ca. 262,000 years away from common era)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use chrono::{NaiveDate, Weekday};"] # [doc = ""] # [doc = " let date = NaiveDate::from_ymd_opt(2022, 4, 18).unwrap();"] # [doc = " let week = date.week(Weekday::Mon);"] # [doc = " let days = week.days();"] # [doc = " assert!(days.contains(&date));"] # [doc = " ```"] # [inline] # [must_use] pub const fn days (& self) -> RangeInclusive < NaiveDate > { match self . checked_days () { Some (val) => val , None => panic ! ("{}" , "first or last weekday is out of range for `NaiveDate`") , } } # [doc = " Returns an [`Option<RangeInclusive<T>>`] representing the whole week bounded by"] # [doc = " [checked_first_day](NaiveWeek::checked_first_day) and"] # [doc = " [checked_last_day](NaiveWeek::checked_last_day) functions."] # [doc = ""] # [doc = " Returns `None` if either of the boundaries are out of `NaiveDate`'s range"] # [doc = " (more than ca. 262,000 years away from common era)."] # [doc = ""] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use chrono::{NaiveDate, Weekday};"] # [doc = ""] # [doc = " let date = NaiveDate::MAX;"] # [doc = " let week = date.week(Weekday::Mon);"] # [doc = " let _days = match week.checked_days() {"] # [doc = "     Some(d) => d,"] # [doc = "     None => {"] # [doc = "         // error handling code"] # [doc = "         return;"] # [doc = "     }"] # [doc = " };"] # [doc = " ```"] # [inline] # [must_use] pub const fn checked_days (& self) -> Option < RangeInclusive < NaiveDate > > { match (self . checked_first_day () , self . checked_last_day ()) { (Some (first) , Some (last)) => Some (first ..= last) , (_ , _) => None , } } }
    };
}

impl_526!()