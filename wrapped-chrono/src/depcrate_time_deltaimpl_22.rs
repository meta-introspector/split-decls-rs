// Generated macro for impl_22 (impl)
macro_rules! Depcrate_time_deltaimpl_22 {
() => {
// Module: crate::time_delta
// Provides: {"impl_22"}
// Dependencies: {}
impl TimeDelta { # [doc = " Makes a new `TimeDelta` with given number of seconds and nanoseconds."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` when the duration is out of bounds, or if `nanos` ≥ 1,000,000,000."] pub const fn new (secs : i64 , nanos : u32) -> Option < TimeDelta > { if secs < MIN . secs || secs > MAX . secs || nanos >= 1_000_000_000 || (secs == MAX . secs && nanos > MAX . nanos as u32) || (secs == MIN . secs && nanos < MIN . nanos as u32) { return None ; } Some (TimeDelta { secs , nanos : nanos as i32 }) } # [doc = " Makes a new `TimeDelta` with the given number of weeks."] # [doc = ""] # [doc = " Equivalent to `TimeDelta::seconds(weeks * 7 * 24 * 60 * 60)` with"] # [doc = " overflow checks."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics when the duration is out of bounds."] # [inline] # [must_use] pub const fn weeks (weeks : i64) -> TimeDelta { expect (TimeDelta :: try_weeks (weeks) , "TimeDelta::weeks out of bounds") } # [doc = " Makes a new `TimeDelta` with the given number of weeks."] # [doc = ""] # [doc = " Equivalent to `TimeDelta::try_seconds(weeks * 7 * 24 * 60 * 60)` with"] # [doc = " overflow checks."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` when the `TimeDelta` would be out of bounds."] # [inline] pub const fn try_weeks (weeks : i64) -> Option < TimeDelta > { TimeDelta :: try_seconds (try_opt ! (weeks . checked_mul (SECS_PER_WEEK))) } # [doc = " Makes a new `TimeDelta` with the given number of days."] # [doc = ""] # [doc = " Equivalent to `TimeDelta::seconds(days * 24 * 60 * 60)` with overflow"] # [doc = " checks."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics when the `TimeDelta` would be out of bounds."] # [inline] # [must_use] pub const fn days (days : i64) -> TimeDelta { expect (TimeDelta :: try_days (days) , "TimeDelta::days out of bounds") } # [doc = " Makes a new `TimeDelta` with the given number of days."] # [doc = ""] # [doc = " Equivalent to `TimeDelta::try_seconds(days * 24 * 60 * 60)` with overflow"] # [doc = " checks."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` when the `TimeDelta` would be out of bounds."] # [inline] pub const fn try_days (days : i64) -> Option < TimeDelta > { TimeDelta :: try_seconds (try_opt ! (days . checked_mul (SECS_PER_DAY))) } # [doc = " Makes a new `TimeDelta` with the given number of hours."] # [doc = ""] # [doc = " Equivalent to `TimeDelta::seconds(hours * 60 * 60)` with overflow checks."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics when the `TimeDelta` would be out of bounds."] # [inline] # [must_use] pub const fn hours (hours : i64) -> TimeDelta { expect (TimeDelta :: try_hours (hours) , "TimeDelta::hours out of bounds") } # [doc = " Makes a new `TimeDelta` with the given number of hours."] # [doc = ""] # [doc = " Equivalent to `TimeDelta::try_seconds(hours * 60 * 60)` with overflow checks."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` when the `TimeDelta` would be out of bounds."] # [inline] pub const fn try_hours (hours : i64) -> Option < TimeDelta > { TimeDelta :: try_seconds (try_opt ! (hours . checked_mul (SECS_PER_HOUR))) } # [doc = " Makes a new `TimeDelta` with the given number of minutes."] # [doc = ""] # [doc = " Equivalent to `TimeDelta::seconds(minutes * 60)` with overflow checks."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics when the `TimeDelta` would be out of bounds."] # [inline] # [must_use] pub const fn minutes (minutes : i64) -> TimeDelta { expect (TimeDelta :: try_minutes (minutes) , "TimeDelta::minutes out of bounds") } # [doc = " Makes a new `TimeDelta` with the given number of minutes."] # [doc = ""] # [doc = " Equivalent to `TimeDelta::try_seconds(minutes * 60)` with overflow checks."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` when the `TimeDelta` would be out of bounds."] # [inline] pub const fn try_minutes (minutes : i64) -> Option < TimeDelta > { TimeDelta :: try_seconds (try_opt ! (minutes . checked_mul (SECS_PER_MINUTE))) } # [doc = " Makes a new `TimeDelta` with the given number of seconds."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics when `seconds` is more than `i64::MAX / 1_000` or less than `-i64::MAX / 1_000`"] # [doc = " (in this context, this is the same as `i64::MIN / 1_000` due to rounding)."] # [inline] # [must_use] pub const fn seconds (seconds : i64) -> TimeDelta { expect (TimeDelta :: try_seconds (seconds) , "TimeDelta::seconds out of bounds") } # [doc = " Makes a new `TimeDelta` with the given number of seconds."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` when `seconds` is more than `i64::MAX / 1_000` or less than"] # [doc = " `-i64::MAX / 1_000` (in this context, this is the same as `i64::MIN / 1_000` due to"] # [doc = " rounding)."] # [inline] pub const fn try_seconds (seconds : i64) -> Option < TimeDelta > { TimeDelta :: new (seconds , 0) } # [doc = " Makes a new `TimeDelta` with the given number of milliseconds."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics when the `TimeDelta` would be out of bounds, i.e. when `milliseconds` is more than"] # [doc = " `i64::MAX` or less than `-i64::MAX`. Notably, this is not the same as `i64::MIN`."] # [inline] pub const fn milliseconds (milliseconds : i64) -> TimeDelta { expect (TimeDelta :: try_milliseconds (milliseconds) , "TimeDelta::milliseconds out of bounds") } # [doc = " Makes a new `TimeDelta` with the given number of milliseconds."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` the `TimeDelta` would be out of bounds, i.e. when `milliseconds` is more"] # [doc = " than `i64::MAX` or less than `-i64::MAX`. Notably, this is not the same as `i64::MIN`."] # [inline] pub const fn try_milliseconds (milliseconds : i64) -> Option < TimeDelta > { if milliseconds < - i64 :: MAX { return None ; } let (secs , millis) = div_mod_floor_64 (milliseconds , MILLIS_PER_SEC) ; let d = TimeDelta { secs , nanos : millis as i32 * NANOS_PER_MILLI } ; Some (d) } # [doc = " Makes a new `TimeDelta` with the given number of microseconds."] # [doc = ""] # [doc = " The number of microseconds acceptable by this constructor is less than"] # [doc = " the total number that can actually be stored in a `TimeDelta`, so it is"] # [doc = " not possible to specify a value that would be out of bounds. This"] # [doc = " function is therefore infallible."] # [inline] pub const fn microseconds (microseconds : i64) -> TimeDelta { let (secs , micros) = div_mod_floor_64 (microseconds , MICROS_PER_SEC) ; let nanos = micros as i32 * NANOS_PER_MICRO ; TimeDelta { secs , nanos } } # [doc = " Makes a new `TimeDelta` with the given number of nanoseconds."] # [doc = ""] # [doc = " The number of nanoseconds acceptable by this constructor is less than"] # [doc = " the total number that can actually be stored in a `TimeDelta`, so it is"] # [doc = " not possible to specify a value that would be out of bounds. This"] # [doc = " function is therefore infallible."] # [inline] pub const fn nanoseconds (nanos : i64) -> TimeDelta { let (secs , nanos) = div_mod_floor_64 (nanos , NANOS_PER_SEC as i64) ; TimeDelta { secs , nanos : nanos as i32 } } # [doc = " Returns the total number of whole weeks in the `TimeDelta`."] # [inline] pub const fn num_weeks (& self) -> i64 { self . num_days () / 7 } # [doc = " Returns the total number of whole days in the `TimeDelta`."] # [inline] pub const fn num_days (& self) -> i64 { self . num_seconds () / SECS_PER_DAY } # [doc = " Returns the total number of whole hours in the `TimeDelta`."] # [inline] pub const fn num_hours (& self) -> i64 { self . num_seconds () / SECS_PER_HOUR } # [doc = " Returns the total number of whole minutes in the `TimeDelta`."] # [inline] pub const fn num_minutes (& self) -> i64 { self . num_seconds () / SECS_PER_MINUTE } # [doc = " Returns the total number of whole seconds in the `TimeDelta`."] pub const fn num_seconds (& self) -> i64 { if self . secs < 0 && self . nanos > 0 { self . secs + 1 } else { self . secs } } # [doc = " Returns the fractional number of seconds in the `TimeDelta`."] pub fn as_seconds_f64 (self) -> f64 { self . secs as f64 + self . nanos as f64 / NANOS_PER_SEC as f64 } # [doc = " Returns the fractional number of seconds in the `TimeDelta`."] pub fn as_seconds_f32 (self) -> f32 { self . secs as f32 + self . nanos as f32 / NANOS_PER_SEC as f32 } # [doc = " Returns the total number of whole milliseconds in the `TimeDelta`."] pub const fn num_milliseconds (& self) -> i64 { let secs_part = self . num_seconds () * MILLIS_PER_SEC ; let nanos_part = self . subsec_nanos () / NANOS_PER_MILLI ; secs_part + nanos_part as i64 } # [doc = " Returns the number of milliseconds in the fractional part of the duration."] # [doc = ""] # [doc = " This is the number of milliseconds such that"] # [doc = " `subsec_millis() + num_seconds() * 1_000` is the truncated number of"] # [doc = " milliseconds in the duration."] pub const fn subsec_millis (& self) -> i32 { self . subsec_nanos () / NANOS_PER_MILLI } # [doc = " Returns the total number of whole microseconds in the `TimeDelta`,"] # [doc = " or `None` on overflow (exceeding 2^63 microseconds in either direction)."] pub const fn num_microseconds (& self) -> Option < i64 > { let secs_part = try_opt ! (self . num_seconds () . checked_mul (MICROS_PER_SEC)) ; let nanos_part = self . subsec_nanos () / NANOS_PER_MICRO ; secs_part . checked_add (nanos_part as i64) } # [doc = " Returns the number of microseconds in the fractional part of the duration."] # [doc = ""] # [doc = " This is the number of microseconds such that"] # [doc = " `subsec_micros() + num_seconds() * 1_000_000` is the truncated number of"] # [doc = " microseconds in the duration."] pub const fn subsec_micros (& self) -> i32 { self . subsec_nanos () / NANOS_PER_MICRO } # [doc = " Returns the total number of whole nanoseconds in the `TimeDelta`,"] # [doc = " or `None` on overflow (exceeding 2^63 nanoseconds in either direction)."] pub const fn num_nanoseconds (& self) -> Option < i64 > { let secs_part = try_opt ! (self . num_seconds () . checked_mul (NANOS_PER_SEC as i64)) ; let nanos_part = self . subsec_nanos () ; secs_part . checked_add (nanos_part as i64) } # [doc = " Returns the number of nanoseconds in the fractional part of the duration."] # [doc = ""] # [doc = " This is the number of nanoseconds such that"] # [doc = " `subsec_nanos() + num_seconds() * 1_000_000_000` is the total number of"] # [doc = " nanoseconds in the `TimeDelta`."] pub const fn subsec_nanos (& self) -> i32 { if self . secs < 0 && self . nanos > 0 { self . nanos - NANOS_PER_SEC } else { self . nanos } } # [doc = " Add two `TimeDelta`s, returning `None` if overflow occurred."] # [must_use] pub const fn checked_add (& self , rhs : & TimeDelta) -> Option < TimeDelta > { let mut secs = self . secs + rhs . secs ; let mut nanos = self . nanos + rhs . nanos ; if nanos >= NANOS_PER_SEC { nanos -= NANOS_PER_SEC ; secs += 1 ; } TimeDelta :: new (secs , nanos as u32) } # [doc = " Subtract two `TimeDelta`s, returning `None` if overflow occurred."] # [must_use] pub const fn checked_sub (& self , rhs : & TimeDelta) -> Option < TimeDelta > { let mut secs = self . secs - rhs . secs ; let mut nanos = self . nanos - rhs . nanos ; if nanos < 0 { nanos += NANOS_PER_SEC ; secs -= 1 ; } TimeDelta :: new (secs , nanos as u32) } # [doc = " Multiply a `TimeDelta` with a i32, returning `None` if overflow occurred."] # [must_use] pub const fn checked_mul (& self , rhs : i32) -> Option < TimeDelta > { let total_nanos = self . nanos as i64 * rhs as i64 ; let (extra_secs , nanos) = div_mod_floor_64 (total_nanos , NANOS_PER_SEC as i64) ; let secs : i128 = self . secs as i128 * rhs as i128 + extra_secs as i128 ; if secs <= i64 :: MIN as i128 || secs >= i64 :: MAX as i128 { return None ; } ; Some (TimeDelta { secs : secs as i64 , nanos : nanos as i32 }) } # [doc = " Divide a `TimeDelta` with a i32, returning `None` if dividing by 0."] # [must_use] pub const fn checked_div (& self , rhs : i32) -> Option < TimeDelta > { if rhs == 0 { return None ; } let secs = self . secs / rhs as i64 ; let carry = self . secs % rhs as i64 ; let extra_nanos = carry * NANOS_PER_SEC as i64 / rhs as i64 ; let nanos = self . nanos / rhs + extra_nanos as i32 ; let (secs , nanos) = match nanos { i32 :: MIN ..= - 1 => (secs - 1 , nanos + NANOS_PER_SEC) , NANOS_PER_SEC ..= i32 :: MAX => (secs + 1 , nanos - NANOS_PER_SEC) , _ => (secs , nanos) , } ; Some (TimeDelta { secs , nanos }) } # [doc = " Returns the `TimeDelta` as an absolute (non-negative) value."] # [inline] pub const fn abs (& self) -> TimeDelta { if self . secs < 0 && self . nanos != 0 { TimeDelta { secs : (self . secs + 1) . abs () , nanos : NANOS_PER_SEC - self . nanos } } else { TimeDelta { secs : self . secs . abs () , nanos : self . nanos } } } # [doc = " The minimum possible `TimeDelta`: `-i64::MAX` milliseconds."] # [deprecated (since = "0.4.39" , note = "Use `TimeDelta::MIN` instead")] # [inline] pub const fn min_value () -> TimeDelta { MIN } # [doc = " The maximum possible `TimeDelta`: `i64::MAX` milliseconds."] # [deprecated (since = "0.4.39" , note = "Use `TimeDelta::MAX` instead")] # [inline] pub const fn max_value () -> TimeDelta { MAX } # [doc = " A `TimeDelta` where the stored seconds and nanoseconds are equal to zero."] # [inline] pub const fn zero () -> TimeDelta { TimeDelta { secs : 0 , nanos : 0 } } # [doc = " Returns `true` if the `TimeDelta` equals `TimeDelta::zero()`."] # [inline] pub const fn is_zero (& self) -> bool { self . secs == 0 && self . nanos == 0 } # [doc = " Creates a `TimeDelta` object from `std::time::Duration`"] # [doc = ""] # [doc = " This function errors when original duration is larger than the maximum"] # [doc = " value supported for this type."] pub const fn from_std (duration : Duration) -> Result < TimeDelta , OutOfRangeError > { if duration . as_secs () > MAX . secs as u64 { return Err (OutOfRangeError (())) ; } match TimeDelta :: new (duration . as_secs () as i64 , duration . subsec_nanos ()) { Some (d) => Ok (d) , None => Err (OutOfRangeError (())) , } } # [doc = " Creates a `std::time::Duration` object from a `TimeDelta`."] # [doc = ""] # [doc = " This function errors when duration is less than zero. As standard"] # [doc = " library implementation is limited to non-negative values."] pub const fn to_std (& self) -> Result < Duration , OutOfRangeError > { if self . secs < 0 { return Err (OutOfRangeError (())) ; } Ok (Duration :: new (self . secs as u64 , self . nanos as u32)) } # [doc = " This duplicates `Neg::neg` because trait methods can't be const yet."] pub (crate) const fn neg (self) -> TimeDelta { let (secs_diff , nanos) = match self . nanos { 0 => (0 , 0) , nanos => (1 , NANOS_PER_SEC - nanos) , } ; TimeDelta { secs : - self . secs - secs_diff , nanos } } # [doc = " The minimum possible `TimeDelta`: `-i64::MAX` milliseconds."] pub const MIN : Self = MIN ; # [doc = " The maximum possible `TimeDelta`: `i64::MAX` milliseconds."] pub const MAX : Self = MAX ; }
};
}
