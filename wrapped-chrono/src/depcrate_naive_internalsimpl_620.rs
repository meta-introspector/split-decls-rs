// Generated macro for impl_620 (impl)
macro_rules! Depcrate_naive_internalsimpl_620 {
() => {
// Module: crate::naive::internals
// Provides: {"impl_620"}
// Dependencies: {}
impl Mdf { # [doc = " Makes a new `Mdf` value from month, day and `YearFlags`."] # [doc = ""] # [doc = " This method doesn't fully validate the range of the `month` and `day` parameters, only as"] # [doc = " much as what can't be deferred until later. The year `flags` are trusted to be correct."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` if `month > 12` or `day > 31`."] # [inline] pub (super) const fn new (month : u32 , day : u32 , YearFlags (flags) : YearFlags) -> Option < Mdf > { match month <= 12 && day <= 31 { true => Some (Mdf ((month << 9) | (day << 4) | flags as u32)) , false => None , } } # [doc = " Makes a new `Mdf` value from an `i32` with an ordinal and a leap year flag, and year"] # [doc = " `flags`."] # [doc = ""] # [doc = " The `ol` is trusted to be valid, and the `flags` are trusted to match it."] # [inline] pub (super) const fn from_ol (ol : i32 , YearFlags (flags) : YearFlags) -> Mdf { debug_assert ! (ol > 1 && ol <= MAX_OL as i32) ; Mdf (((ol as u32 + OL_TO_MDL [ol as usize] as u32) << 3) | flags as u32) } # [doc = " Returns the month of this `Mdf`."] # [inline] pub (super) const fn month (& self) -> u32 { let Mdf (mdf) = * self ; mdf >> 9 } # [doc = " Replaces the month of this `Mdf`, keeping the day and flags."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` if `month > 12`."] # [inline] pub (super) const fn with_month (& self , month : u32) -> Option < Mdf > { if month > 12 { return None ; } let Mdf (mdf) = * self ; Some (Mdf ((mdf & 0b1_1111_1111) | (month << 9))) } # [doc = " Returns the day of this `Mdf`."] # [inline] pub (super) const fn day (& self) -> u32 { let Mdf (mdf) = * self ; (mdf >> 4) & 0b1_1111 } # [doc = " Replaces the day of this `Mdf`, keeping the month and flags."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` if `day > 31`."] # [inline] pub (super) const fn with_day (& self , day : u32) -> Option < Mdf > { if day > 31 { return None ; } let Mdf (mdf) = * self ; Some (Mdf ((mdf & ! 0b1_1111_0000) | (day << 4))) } # [doc = " Replaces the flags of this `Mdf`, keeping the month and day."] # [inline] pub (super) const fn with_flags (& self , YearFlags (flags) : YearFlags) -> Mdf { let Mdf (mdf) = * self ; Mdf ((mdf & ! 0b1111) | flags as u32) } # [doc = " Returns the ordinal that corresponds to this `Mdf`."] # [doc = ""] # [doc = " This does a table lookup to calculate the corresponding ordinal. It will return an error if"] # [doc = " the `Mdl` turns out not to be a valid date."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` if `month == 0` or `day == 0`, or if a the given day does not exist in the"] # [doc = " given month."] # [inline] pub (super) const fn ordinal (& self) -> Option < u32 > { let mdl = self . 0 >> 3 ; match MDL_TO_OL [mdl as usize] { XX => None , v => Some ((mdl - v as u8 as u32) >> 1) , } } # [doc = " Returns the year flags of this `Mdf`."] # [inline] pub (super) const fn year_flags (& self) -> YearFlags { YearFlags ((self . 0 & 0b1111) as u8) } # [doc = " Returns the ordinal that corresponds to this `Mdf`, encoded as a value including year flags."] # [doc = ""] # [doc = " This does a table lookup to calculate the corresponding ordinal. It will return an error if"] # [doc = " the `Mdl` turns out not to be a valid date."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` if `month == 0` or `day == 0`, or if a the given day does not exist in the"] # [doc = " given month."] # [inline] pub (super) const fn ordinal_and_flags (& self) -> Option < i32 > { let mdl = self . 0 >> 3 ; match MDL_TO_OL [mdl as usize] { XX => None , v => Some (self . 0 as i32 - ((v as i32) << 3)) , } } # [cfg (test)] fn valid (& self) -> bool { let mdl = self . 0 >> 3 ; MDL_TO_OL [mdl as usize] > 0 } }
};
}
