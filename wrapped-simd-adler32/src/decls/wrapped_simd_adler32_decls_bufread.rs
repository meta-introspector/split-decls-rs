// Generated by add_generated_rust_header! macro at submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs:72 from submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs
// Do not edit this file by hand. Your changes will be overwritten.

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
#[cfg(feature = "std")]
pub mod bufread {
    #![doc = " BufRead-based hashing."]
    #![doc = ""]
    #![doc = " Separate `BufRead` trait implemented to allow for custom buffer size optimization."]
    #![doc = ""]
    #![doc = " # Example"]
    #![doc = " ```rust"]
    #![doc = " use std::io::{Cursor, BufReader};"]
    #![doc = " use simd_adler32::bufread::adler32;"]
    #![doc = ""]
    #![doc = " let mut reader = Cursor::new(b\"Hello there\");"]
    #![doc = " let mut reader = BufReader::new(reader);"]
    #![doc = " let hash = adler32(&mut reader).unwrap();"]
    #![doc = ""]
    #![doc = " println!(\"{}\", hash) // 800813569"]
    #![doc = " ```"]
    use crate::Adler32;
    use std::io::{BufRead, ErrorKind, Result};
    #[doc = " Compute Adler-32 hash on buf reader until EOF."]
    #[doc = ""]
    #[doc = " # Example"]
    #[doc = " ```rust"]
    #[doc = " use std::io::{Cursor, BufReader};"]
    #[doc = " use simd_adler32::bufread::adler32;"]
    #[doc = ""]
    #[doc = " let mut reader = Cursor::new(b\"Hello there\");"]
    #[doc = " let mut reader = BufReader::new(reader);"]
    #[doc = " let hash = adler32(&mut reader).unwrap();"]
    #[doc = ""]
    #[doc = " println!(\"{}\", hash) // 800813569"]
    #[doc = " ```"]
    pub fn adler32<R: BufRead>(reader: &mut R) -> Result<u32> {
        let mut hash = Adler32::new();
        loop {
            let consumed = match reader.fill_buf() {
                Ok(buf) => {
                    if buf.is_empty() {
                        return Ok(hash.finish());
                    }
                    hash.write(buf);
                    buf.len()
                }
                Err(err) => match err.kind() {
                    ErrorKind::Interrupted => continue,
                    ErrorKind::UnexpectedEof => return Ok(hash.finish()),
                    _ => return Err(err),
                },
            };
            reader.consume(consumed);
        }
    }
}
