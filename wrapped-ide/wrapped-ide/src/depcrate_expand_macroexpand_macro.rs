// Generated macro for expand_macro (function)
macro_rules! Depcrate_expand_macroexpand_macro {
() => {
// Module: crate::expand_macro
// Provides: {"expand_macro"}
// Dependencies: {}
pub (crate) fn expand_macro (db : & RootDatabase , position : FilePosition) -> Option < ExpandedMacro > { let sema = Semantics :: new (db) ; let file_id = sema . attach_first_edition (position . file_id) ? ; let file = sema . parse (file_id) ; let krate = sema . file_to_module_def (file_id . file_id (db)) ? . krate () . into () ; let tok = pick_best_token (file . syntax () . token_at_offset (position . offset) , | kind | match kind { SyntaxKind :: IDENT => 1 , _ => 0 , }) ? ; let derive = sema . descend_into_macros_exact (tok . clone ()) . into_iter () . find_map (| descended | { let macro_file = sema . hir_file_for (& descended . parent () ?) . macro_file () ? ; if ! macro_file . is_derive_attr_pseudo_expansion (db) { return None ; } let name = descended . parent_ancestors () . filter_map (ast :: Path :: cast) . last () ? . to_string () ; let InFile { file_id , value : tokens } = hir :: InMacroFile :: new (macro_file , descended) . upmap_once (db) ; let token = sema . parse_or_expand (file_id) . covering_element (tokens [0]) . into_token () ? ; let attr = token . parent_ancestors () . find_map (ast :: Attr :: cast) ? ; let expansions = sema . expand_derive_macro (& attr) ? ; let idx = attr . token_tree () ? . token_trees_and_tokens () . filter_map (NodeOrToken :: into_token) . take_while (| it | it != & token) . filter (| it | it . kind () == T ! [,]) . count () ; let ExpandResult { err , value : expansion } = expansions . get (idx) ? . clone () ; let expansion_file_id = sema . hir_file_for (& expansion) . macro_file () ? ; let expansion_span_map = db . expansion_span_map (expansion_file_id) ; let mut expansion = format (db , SyntaxKind :: MACRO_ITEMS , position . file_id , expansion , & expansion_span_map , krate ,) ; if let Some (err) = err { expansion . insert_str (0 , & format ! ("Expansion had errors: {}\n\n" , err . render_to_string (sema . db)) ,) ; } Some (ExpandedMacro { name , expansion }) }) ; if derive . is_some () { return derive ; } let syntax_token = sema . descend_into_macros_exact (tok) ; 'tokens : for syntax_token in syntax_token { let mut anc = syntax_token . parent_ancestors () ; let mut span_map = SpanMap :: empty () ; let mut error = String :: new () ; let (name , expanded , kind) = loop { let Some (node) = anc . next () else { continue 'tokens ; } ; if let Some (item) = ast :: Item :: cast (node . clone ()) && let Some (def) = sema . resolve_attr_macro_call (& item) { break (def . name (db) . display (db , file_id . edition (db)) . to_string () , expand_macro_recur (& sema , & item , & mut error , & mut span_map , TextSize :: new (0)) ? , SyntaxKind :: MACRO_ITEMS ,) ; } if let Some (mac) = ast :: MacroCall :: cast (node) { let mut name = mac . path () ? . segment () ? . name_ref () ? . to_string () ; name . push ('!') ; let syntax_kind = mac . syntax () . parent () . map (| it | it . kind ()) . unwrap_or (SyntaxKind :: MACRO_ITEMS) ; break (name , expand_macro_recur (& sema , & ast :: Item :: MacroCall (mac) , & mut error , & mut span_map , TextSize :: new (0) ,) ? , syntax_kind ,) ; } } ; let mut expansion = format (db , kind , position . file_id , expanded , & span_map , krate) ; if ! error . is_empty () { expansion . insert_str (0 , & format ! ("Expansion had errors:{error}\n\n")) ; } return Some (ExpandedMacro { name , expansion }) ; } None }
};
}
