macro_rules! assert_e2e {
    () => {
        # [doc = " Assertion policy for functional end-to-end tests"] # [doc = ""] # [doc = " This emphasizes showing as much content as possible at the cost of more brittleness"] # [doc = ""] # [doc = " # Snapshots"] # [doc = ""] # [doc = " Updating of snapshots is controlled with the `SNAPSHOTS` environment variable:"] # [doc = ""] # [doc = " - `skip`: do not run the tests"] # [doc = " - `ignore`: run the tests but ignore their failure"] # [doc = " - `verify`: run the tests"] # [doc = " - `overwrite`: update the snapshots based on the output of the tests"] # [doc = ""] # [doc = " # Patterns"] # [doc = ""] # [doc = " - `[..]` is a character wildcard, stopping at line breaks"] # [doc = " - `\\n...\\n` is a multi-line wildcard"] # [doc = " - `[EXE]` matches the exe suffix for the current platform"] # [doc = " - `[ROOT]` matches [`paths::root()`][crate::paths::root]"] # [doc = " - `[ROOTURL]` matches [`paths::root()`][crate::paths::root] as a URL"] # [doc = ""] # [doc = " # Normalization"] # [doc = ""] # [doc = " In addition to the patterns described above, text is normalized"] # [doc = " in such a way to avoid unwanted differences. The normalizations are:"] # [doc = ""] # [doc = " - Backslashes are converted to forward slashes to deal with Windows paths."] # [doc = "   This helps so that all tests can be written assuming forward slashes."] # [doc = "   Other heuristics are applied to try to ensure Windows-style paths aren't"] # [doc = "   a problem."] # [doc = " - Carriage returns are removed, which can help when running on Windows."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```no_run"] # [doc = " # use cargo_test_support::compare::assert_e2e;"] # [doc = " # use cargo_test_support::str;"] # [doc = " # let p = cargo_test_support::project().build();"] # [doc = " assert_e2e().eq(p.read_lockfile(), str![]);"] # [doc = " ```"] # [doc = " ```console"] # [doc = " $ SNAPSHOTS=overwrite cargo test"] # [doc = " ```"] pub fn assert_e2e () -> snapbox :: Assert { let mut subs = snapbox :: Redactions :: new () ; subs . extend (MIN_LITERAL_REDACTIONS . into_iter () . cloned ()) . unwrap () ; subs . extend (E2E_LITERAL_REDACTIONS . into_iter () . cloned ()) . unwrap () ; add_test_support_redactions (& mut subs) ; add_regex_redactions (& mut subs) ; snapbox :: Assert :: new () . action_env (snapbox :: assert :: DEFAULT_ACTION_ENV) . redact_with (subs) }
    };
}

assert_e2e!();