macro_rules! deps {
    () => {
        CargoPathExt!();
    };
}

macro_rules! impl_82 {
    () => {
        deps!();
        impl CargoPathExt for Path { fn to_url (& self) -> url :: Url { url :: Url :: from_file_path (self) . ok () . unwrap () } fn rm_rf (& self) { let meta = match self . symlink_metadata () { Ok (meta) => meta , Err (e) => { if e . kind () == ErrorKind :: NotFound { return ; } panic ! ("failed to remove {:?}, could not read: {:?}" , self , e) ; } } ; if meta . is_dir () { if let Err (e) = fs :: remove_dir_all (self) { panic ! ("failed to remove {:?}: {:?}" , self , e) } } else if let Err (e) = fs :: remove_file (self) { panic ! ("failed to remove {:?}: {:?}" , self , e) } } fn mkdir_p (& self) { fs :: create_dir_all (self) . unwrap_or_else (| e | panic ! ("failed to mkdir_p {}: {}" , self . display () , e)) } fn ls_r (& self) -> Vec < PathBuf > { walkdir :: WalkDir :: new (self) . sort_by_file_name () . into_iter () . filter_map (| e | e . map (| e | e . path () . to_owned ()) . ok ()) . collect () } fn move_in_time < F > (& self , travel_amount : F) where F : Fn (i64 , u32) -> (i64 , u32) , { if self . is_file () { time_travel (self , & travel_amount) ; } else { recurse (self , & self . join ("target") , & travel_amount) ; } fn recurse < F > (p : & Path , bad : & Path , travel_amount : & F) where F : Fn (i64 , u32) -> (i64 , u32) , { if p . is_file () { time_travel (p , travel_amount) } else if ! p . starts_with (bad) { for f in t ! (fs :: read_dir (p)) { let f = t ! (f) . path () ; recurse (& f , bad , travel_amount) ; } } } fn time_travel < F > (path : & Path , travel_amount : & F) where F : Fn (i64 , u32) -> (i64 , u32) , { let stat = t ! (path . symlink_metadata ()) ; let mtime = FileTime :: from_last_modification_time (& stat) ; let (sec , nsec) = travel_amount (mtime . unix_seconds () , mtime . nanoseconds ()) ; let newtime = FileTime :: from_unix_time (sec , nsec) ; do_op (path , "set file times" , | path | { filetime :: set_file_times (path , newtime , newtime) }) ; } } # [track_caller] fn assert_build_dir_layout (& self , expected : impl snapbox :: IntoData) { self . assert_dir_layout (expected . unordered () , & build_dir_ignored_path_patterns ()) ; } # [track_caller] fn assert_dir_layout (& self , expected : impl snapbox :: IntoData , ignored_path_patterns : & [String] ,) { let assert = assert_e2e () ; let actual = WalkDir :: new (self) . sort_by_file_name () . into_iter () . filter_map (| e | e . ok ()) . filter (| e | e . file_type () . is_file ()) . map (| e | e . path () . to_string_lossy () . into_owned ()) . filter (| file | { for ignored in ignored_path_patterns { if match_contains (& ignored , file , & assert . redactions ()) . is_ok () { return false ; } } return true ; }) . join ("\n") ; assert . eq (format ! ("{actual}\n") , expected) ; } }
    };
}

impl_82!();