// Generated macro for Scanner (struct)
macro_rules! Depcrate_scannerScanner {
() => {
// Module: crate::scanner
// Provides: {"Scanner"}
// Dependencies: {}
# [doc = " The YAML scanner."] # [doc = ""] # [doc = " This corresponds to the low-level interface when reading YAML. The scanner emits token as they"] # [doc = " are read (akin to a lexer), but it also holds sufficient context to be able to disambiguate"] # [doc = " some of the constructs. It has understanding of indentation and whitespace and is able to"] # [doc = " generate error messages for some invalid YAML constructs."] # [doc = ""] # [doc = " It is however not a full parser and needs [`crate::parser::Parser`] to fully detect invalid"] # [doc = " YAML documents."] # [derive (Debug)] # [allow (clippy :: struct_excessive_bools)] pub struct Scanner < T > { # [doc = " The reader, providing with characters."] rdr : T , # [doc = " The position of the cursor within the reader."] mark : Marker , # [doc = " Buffer for tokens to be returned."] # [doc = ""] # [doc = " This buffer can hold some temporary tokens that are not yet ready to be returned. For"] # [doc = " instance, if we just read a scalar, it can be a value or a key if an implicit mapping"] # [doc = " follows. In this case, the token stays in the `VecDeque` but cannot be returned from"] # [doc = " [`Self::next`] until we have more context."] tokens : VecDeque < Token > , # [doc = " Buffer for the next characters to consume."] buffer : ArrayDeque < char , BUFFER_LEN > , # [doc = " The last error that happened."] error : Option < ScanError > , # [doc = " Whether we have already emitted the `StreamStart` token."] stream_start_produced : bool , # [doc = " Whether we have already emitted the `StreamEnd` token."] stream_end_produced : bool , # [doc = " In some flow contexts, the value of a mapping is allowed to be adjacent to the `:`. When it"] # [doc = " is, the index at which the `:` may be must be stored in `adjacent_value_allowed_at`."] adjacent_value_allowed_at : usize , # [doc = " Whether a simple key could potentially start at the current position."] # [doc = ""] # [doc = " Simple keys are the opposite of complex keys which are keys starting with `?`."] simple_key_allowed : bool , # [doc = " A stack of potential simple keys."] # [doc = ""] # [doc = " Refer to the documentation of [`SimpleKey`] for a more in-depth explanation of what they"] # [doc = " are."] simple_keys : Vec < SimpleKey > , # [doc = " The current indentation level."] indent : isize , # [doc = " List of all block indentation levels we are in (except the current one)."] indents : Vec < Indent > , # [doc = " Level of nesting of flow sequences."] flow_level : u8 , # [doc = " The number of tokens that have been returned from the scanner."] # [doc = ""] # [doc = " This excludes the tokens from [`Self::tokens`]."] tokens_parsed : usize , # [doc = " Whether a token is ready to be taken from [`Self::tokens`]."] token_available : bool , # [doc = " Whether all characters encountered since the last newline were whitespace."] leading_whitespace : bool , # [doc = " Whether we started a flow mapping."] # [doc = ""] # [doc = " This is used to detect implicit flow mapping starts such as:"] # [doc = " ```yaml"] # [doc = " [ : foo ] # { null: \"foo\" }"] # [doc = " ```"] flow_mapping_started : bool , # [doc = " Whether we currently are in an implicit flow mapping."] implicit_flow_mapping : bool , }
};
}
