// Generated macro for SimpleKey (struct)
macro_rules! Depcrate_scannerSimpleKey {
() => {
// Module: crate::scanner
// Provides: {"SimpleKey"}
// Dependencies: {}
# [doc = " A scalar that was parsed and may correspond to a simple key."] # [doc = ""] # [doc = " Upon scanning the following yaml:"] # [doc = " ```yaml"] # [doc = " a: b"] # [doc = " ```"] # [doc = " We do not know that `a` is a key for a map until we have reached the following `:`. For this"] # [doc = " YAML, we would store `a` as a scalar token in the [`Scanner`], but not emit it yet. It would be"] # [doc = " kept inside the scanner until more context is fetched and we are able to know whether it is a"] # [doc = " plain scalar or a key."] # [doc = ""] # [doc = " For example, see the following 2 yaml documents:"] # [doc = " ```yaml"] # [doc = " ---"] # [doc = " a: b # Here, `a` is a key."] # [doc = " ..."] # [doc = " ---"] # [doc = " a # Here, `a` is a plain scalar."] # [doc = " ..."] # [doc = " ```"] # [doc = " An instance of [`SimpleKey`] is created in the [`Scanner`] when such ambiguity occurs."] # [doc = ""] # [doc = " In both documents, scanning `a` would lead to the creation of a [`SimpleKey`] with"] # [doc = " [`Self::possible`] set to `true`. The token for `a` would be pushed in the [`Scanner`] but not"] # [doc = " yet emitted. Instead, more context would be fetched (through [`Scanner::fetch_more_tokens`])."] # [doc = ""] # [doc = " In the first document, upon reaching the `:`, the [`SimpleKey`] would be inspected and our"] # [doc = " scalar `a` since it is a possible key, would be \"turned\" into a key. This is done by prepending"] # [doc = " a [`TokenType::Key`] to our scalar token in the [`Scanner`]. This way, the"] # [doc = " [`crate::parser::Parser`] would read the [`TokenType::Key`] token before the"] # [doc = " [`TokenType::Scalar`] token."] # [doc = ""] # [doc = " In the second document however, reaching the EOF would stale the [`SimpleKey`] and no"] # [doc = " [`TokenType::Key`] would be emitted by the scanner."] # [derive (Clone , PartialEq , Debug , Eq)] struct SimpleKey { # [doc = " Whether the token this [`SimpleKey`] refers to may still be a key."] # [doc = ""] # [doc = " Sometimes, when we have more context, we notice that what we thought could be a key no"] # [doc = " longer can be. In that case, [`Self::possible`] is set to `false`."] # [doc = ""] # [doc = " For instance, let us consider the following invalid YAML:"] # [doc = " ```yaml"] # [doc = " key"] # [doc = "   : value"] # [doc = " ```"] # [doc = " Upon reading the `\\n` after `key`, the [`SimpleKey`] that was created for `key` is staled"] # [doc = " and [`Self::possible`] set to `false`."] possible : bool , # [doc = " Whether the token this [`SimpleKey`] refers to is required to be a key."] # [doc = ""] # [doc = " With more context, we may know for sure that the token must be a key. If the YAML is"] # [doc = " invalid, it may happen that the token be deemed not a key. In such event, an error has to"] # [doc = " be raised. This boolean helps us know when to raise such error."] # [doc = ""] # [doc = " TODO(ethiraric, 30/12/2023): Example of when this happens."] required : bool , # [doc = " The index of the token referred to by the [`SimpleKey`]."] # [doc = ""] # [doc = " This is the index in the scanner, which takes into account both the tokens that have been"] # [doc = " emitted and those about to be emitted. See [`Scanner::tokens_parsed`] and"] # [doc = " [`Scanner::tokens`] for more details."] token_number : usize , # [doc = " The position at which the token the [`SimpleKey`] refers to is."] mark : Marker , }
};
}
