// Generated macro for impl_77 (impl)
macro_rules! Depcrate_scannerimpl_77 {
() => {
// Module: crate::scanner
// Provides: {"impl_77"}
// Dependencies: {}
impl < T : Iterator < Item = char > > Scanner < T > { # [doc = " Creates the YAML tokenizer."] pub fn new (rdr : T) -> Scanner < T > { Scanner { rdr , buffer : ArrayDeque :: new () , mark : Marker :: new (0 , 1 , 0) , tokens : VecDeque :: new () , error : None , stream_start_produced : false , stream_end_produced : false , adjacent_value_allowed_at : 0 , simple_key_allowed : true , simple_keys : Vec :: new () , indent : - 1 , indents : Vec :: new () , flow_level : 0 , tokens_parsed : 0 , token_available : false , leading_whitespace : true , flow_mapping_started : false , implicit_flow_mapping : false , } } # [doc = " Get a copy of the last error that was encountered, if any."] # [doc = ""] # [doc = " This does not clear the error state and further calls to [`Self::get_error`] will return (a"] # [doc = " clone of) the same error."] # [inline] pub fn get_error (& self) -> Option < ScanError > { self . error . clone () } # [doc = " Fill `self.buffer` with at least `count` characters."] # [doc = ""] # [doc = " The characters that are extracted this way are not consumed but only placed in the buffer."] # [inline] fn lookahead (& mut self , count : usize) { if self . buffer . len () >= count { return ; } for _ in 0 .. (count - self . buffer . len ()) { self . buffer . push_back (self . rdr . next () . unwrap_or ('\0')) . unwrap () ; } } # [doc = " Consume the next character. It is assumed the next character is a blank."] # [inline] fn skip_blank (& mut self) { self . buffer . pop_front () ; self . mark . index += 1 ; self . mark . col += 1 ; } # [doc = " Consume the next character. It is assumed the next character is not a blank."] # [inline] fn skip_non_blank (& mut self) { self . buffer . pop_front () ; self . mark . index += 1 ; self . mark . col += 1 ; self . leading_whitespace = false ; } # [doc = " Consume the next characters. It is assumed none of the next characters are blanks."] # [inline] fn skip_n_non_blank (& mut self , n : usize) { self . buffer . drain (0 .. n) ; self . mark . index += n ; self . mark . col += n ; self . leading_whitespace = false ; } # [doc = " Consume the next character. It is assumed the next character is a newline."] # [inline] fn skip_nl (& mut self) { self . buffer . pop_front () ; self . mark . index += 1 ; self . mark . col = 0 ; self . mark . line += 1 ; self . leading_whitespace = true ; } # [doc = " Consume a linebreak (either CR, LF or CRLF), if any. Do nothing if there's none."] # [inline] fn skip_linebreak (& mut self) { if self . buffer [0] == '\r' && self . buffer [1] == '\n' { self . skip_blank () ; self . skip_nl () ; } else if is_break (self . buffer [0]) { self . skip_nl () ; } } # [doc = " Return the next character in the buffer."] # [doc = ""] # [doc = " The character is not consumed."] # [inline] fn ch (& self) -> char { self . buffer [0] } # [doc = " Look for the next character and return it."] # [doc = ""] # [doc = " The character is not consumed."] # [doc = " Equivalent to calling [`Self::lookahead`] and [`Self::ch`]."] # [inline] fn look_ch (& mut self) -> char { self . lookahead (1) ; self . ch () } # [doc = " Read a character from the input stream, returning it directly."] # [doc = ""] # [doc = " The buffer is bypassed and `self.mark` needs to be updated manually."] # [inline] # [must_use] fn raw_read_ch (& mut self) -> char { self . rdr . next () . unwrap_or ('\0') } # [doc = " Return whether the next character is `c`."] # [inline] fn ch_is (& self , c : char) -> bool { self . buffer [0] == c } # [doc = " Return whether the [`TokenType::StreamStart`] event has been emitted."] # [inline] pub fn stream_started (& self) -> bool { self . stream_start_produced } # [doc = " Return whether the [`TokenType::StreamEnd`] event has been emitted."] # [inline] pub fn stream_ended (& self) -> bool { self . stream_end_produced } # [doc = " Get the current position in the input stream."] # [inline] pub fn mark (& self) -> Marker { self . mark } # [inline] fn read_break (& mut self , s : & mut String) { let c = self . buffer [0] ; let nc = self . buffer [1] ; debug_assert ! (is_break (c)) ; if c == '\r' && nc == '\n' { self . skip_blank () ; } self . skip_nl () ; s . push ('\n') ; } # [doc = " Check whether the next characters correspond to an end of document."] # [doc = ""] # [doc = " [`Self::lookahead`] must have been called before calling this function."] fn next_is_document_end (& self) -> bool { assert ! (self . buffer . len () >= 4) ; self . buffer [0] == '.' && self . buffer [1] == '.' && self . buffer [2] == '.' && is_blank_or_breakz (self . buffer [3]) } # [doc = " Check whether the next characters correspond to a document indicator."] # [doc = ""] # [doc = " [`Self::lookahead`] must have been called before calling this function."] # [inline] fn next_is_document_indicator (& self) -> bool { assert ! (self . buffer . len () >= 4) ; self . mark . col == 0 && (((self . buffer [0] == '-') && (self . buffer [1] == '-') && (self . buffer [2] == '-')) || ((self . buffer [0] == '.') && (self . buffer [1] == '.') && (self . buffer [2] == '.'))) && is_blank_or_breakz (self . buffer [3]) } # [doc = " Insert a token at the given position."] fn insert_token (& mut self , pos : usize , tok : Token) { let old_len = self . tokens . len () ; assert ! (pos <= old_len) ; self . tokens . insert (pos , tok) ; } fn allow_simple_key (& mut self) { self . simple_key_allowed = true ; } fn disallow_simple_key (& mut self) { self . simple_key_allowed = false ; } # [doc = " Fetch the next token in the stream."] # [doc = " # Errors"] # [doc = " Returns `ScanError` when the scanner does not find the next expected token."] pub fn fetch_next_token (& mut self) -> ScanResult { self . lookahead (1) ; if ! self . stream_start_produced { self . fetch_stream_start () ; return Ok (()) ; } self . skip_to_next_token () ? ; debug_print ! ("  \x1B[38;5;244m\u{2192} fetch_next_token after whitespace {:?} {:?}\x1B[m" , self . mark , self . ch ()) ; self . stale_simple_keys () ? ; let mark = self . mark ; self . unroll_indent (mark . col as isize) ; self . lookahead (4) ; if is_z (self . ch ()) { self . fetch_stream_end () ? ; return Ok (()) ; } if self . mark . col == 0 && self . ch_is ('%') { return self . fetch_directive () ; } if self . mark . col == 0 && self . buffer [0] == '-' && self . buffer [1] == '-' && self . buffer [2] == '-' && is_blank_or_breakz (self . buffer [3]) { self . fetch_document_indicator (TokenType :: DocumentStart) ? ; return Ok (()) ; } if self . mark . col == 0 && self . buffer [0] == '.' && self . buffer [1] == '.' && self . buffer [2] == '.' && is_blank_or_breakz (self . buffer [3]) { self . fetch_document_indicator (TokenType :: DocumentEnd) ? ; self . skip_ws_to_eol (SkipTabs :: Yes) ? ; if ! is_breakz (self . ch ()) { return Err (ScanError :: new (self . mark , "invalid content after document end marker" ,)) ; } return Ok (()) ; } if (self . mark . col as isize) < self . indent { return Err (ScanError :: new (self . mark , "invalid indentation")) ; } let c = self . buffer [0] ; let nc = self . buffer [1] ; match c { '[' => self . fetch_flow_collection_start (TokenType :: FlowSequenceStart) , '{' => self . fetch_flow_collection_start (TokenType :: FlowMappingStart) , ']' => self . fetch_flow_collection_end (TokenType :: FlowSequenceEnd) , '}' => self . fetch_flow_collection_end (TokenType :: FlowMappingEnd) , ',' => self . fetch_flow_entry () , '-' if is_blank_or_breakz (nc) => self . fetch_block_entry () , '?' if is_blank_or_breakz (nc) => self . fetch_key () , ':' if is_blank_or_breakz (nc) || (self . flow_level > 0 && (is_flow (nc) || self . mark . index == self . adjacent_value_allowed_at)) => { self . fetch_value () } '*' => self . fetch_anchor (true) , '&' => self . fetch_anchor (false) , '!' => self . fetch_tag () , '|' if self . flow_level == 0 => self . fetch_block_scalar (true) , '>' if self . flow_level == 0 => self . fetch_block_scalar (false) , '\'' => self . fetch_flow_scalar (true) , '"' => self . fetch_flow_scalar (false) , '-' if ! is_blank_or_breakz (nc) => self . fetch_plain_scalar () , ':' | '?' if ! is_blank_or_breakz (nc) && self . flow_level == 0 => { self . fetch_plain_scalar () } '%' | '@' | '`' => Err (ScanError :: new (self . mark , & format ! ("unexpected character: `{c}'") ,)) , _ => self . fetch_plain_scalar () , } } # [doc = " Return the next token in the stream."] # [doc = " # Errors"] # [doc = " Returns `ScanError` when scanning fails to find an expected next token."] pub fn next_token (& mut self) -> Result < Option < Token > , ScanError > { if self . stream_end_produced { return Ok (None) ; } if ! self . token_available { self . fetch_more_tokens () ? ; } let Some (t) = self . tokens . pop_front () else { return Err (ScanError :: new (self . mark , "did not find expected next token" ,)) ; } ; self . token_available = false ; self . tokens_parsed += 1 ; if let TokenType :: StreamEnd = t . 1 { self . stream_end_produced = true ; } Ok (Some (t)) } # [doc = " Fetch tokens from the token stream."] # [doc = " # Errors"] # [doc = " Returns `ScanError` when loading fails."] pub fn fetch_more_tokens (& mut self) -> ScanResult { let mut need_more ; loop { if self . tokens . is_empty () { need_more = true ; } else { need_more = false ; self . stale_simple_keys () ? ; for sk in & self . simple_keys { if sk . possible && sk . token_number == self . tokens_parsed { need_more = true ; break ; } } } if ! need_more { break ; } self . fetch_next_token () ? ; } self . token_available = true ; Ok (()) } # [doc = " Mark simple keys that can no longer be keys as such."] # [doc = ""] # [doc = " This function sets `possible` to `false` to each key that, now we have more context, we"] # [doc = " know will not be keys."] # [doc = ""] # [doc = " # Errors"] # [doc = " This function returns an error if one of the key we would stale was required to be a key."] fn stale_simple_keys (& mut self) -> ScanResult { for sk in & mut self . simple_keys { if sk . possible && self . flow_level == 0 && (sk . mark . line < self . mark . line || sk . mark . index + 1024 < self . mark . index) { if sk . required { return Err (ScanError :: new (self . mark , "simple key expect ':'")) ; } sk . possible = false ; } } Ok (()) } # [doc = " Skip over all whitespace and comments until the next token."] # [doc = ""] # [doc = " # Errors"] # [doc = " This function returns an error if a tabulation is encountered where there should not be"] # [doc = " one."] fn skip_to_next_token (& mut self) -> ScanResult { loop { match self . look_ch () { '\t' if self . is_within_block () && self . leading_whitespace && (self . mark . col as isize) < self . indent => { self . skip_ws_to_eol (SkipTabs :: Yes) ? ; if ! is_breakz (self . ch ()) { return Err (ScanError :: new (self . mark , "tabs disallowed within this context (block indentation)" ,)) ; } } '\t' | ' ' => self . skip_blank () , '\n' | '\r' => { self . lookahead (2) ; self . skip_linebreak () ; if self . flow_level == 0 { self . allow_simple_key () ; } } '#' => { while ! is_breakz (self . look_ch ()) { self . skip_non_blank () ; } } _ => break , } } Ok (()) } # [doc = " Skip over YAML whitespace (` `, `\\n`, `\\r`)."] # [doc = ""] # [doc = " # Errors"] # [doc = " This function returns an error if no whitespace was found."] fn skip_yaml_whitespace (& mut self) -> ScanResult { let mut need_whitespace = true ; loop { match self . look_ch () { ' ' => { self . skip_blank () ; need_whitespace = false ; } '\n' | '\r' => { self . lookahead (2) ; self . skip_linebreak () ; if self . flow_level == 0 { self . allow_simple_key () ; } need_whitespace = false ; } '#' => { while ! is_breakz (self . look_ch ()) { self . skip_non_blank () ; } } _ => break , } } if need_whitespace { Err (ScanError :: new (self . mark () , "expected whitespace")) } else { Ok (()) } } # [doc = " Skip yaml whitespace at most up to eol. Also skips comments."] fn skip_ws_to_eol (& mut self , skip_tabs : SkipTabs) -> Result < SkipTabs , ScanError > { let mut encountered_tab = false ; let mut has_yaml_ws = false ; loop { match self . look_ch () { ' ' => { has_yaml_ws = true ; self . skip_blank () ; } '\t' if skip_tabs != SkipTabs :: No => { encountered_tab = true ; self . skip_blank () ; } '#' if ! encountered_tab && ! has_yaml_ws => { return Err (ScanError :: new (self . mark , "comments must be separated from other tokens by whitespace" ,)) ; } '#' => { while ! is_breakz (self . look_ch ()) { self . skip_non_blank () ; } } _ => break , } } Ok (SkipTabs :: Result (encountered_tab , has_yaml_ws)) } fn fetch_stream_start (& mut self) { let mark = self . mark ; self . indent = - 1 ; self . stream_start_produced = true ; self . allow_simple_key () ; self . tokens . push_back (Token (mark , TokenType :: StreamStart (TEncoding :: Utf8))) ; self . simple_keys . push (SimpleKey :: new (Marker :: new (0 , 0 , 0))) ; } fn fetch_stream_end (& mut self) -> ScanResult { if self . mark . col != 0 { self . mark . col = 0 ; self . mark . line += 1 ; } for sk in & mut self . simple_keys { if sk . required && sk . possible { return Err (ScanError :: new (self . mark , "simple key expected")) ; } sk . possible = false ; } self . unroll_indent (- 1) ; self . remove_simple_key () ? ; self . disallow_simple_key () ; self . tokens . push_back (Token (self . mark , TokenType :: StreamEnd)) ; Ok (()) } fn fetch_directive (& mut self) -> ScanResult { self . unroll_indent (- 1) ; self . remove_simple_key () ? ; self . disallow_simple_key () ; let tok = self . scan_directive () ? ; self . tokens . push_back (tok) ; Ok (()) } fn scan_directive (& mut self) -> Result < Token , ScanError > { let start_mark = self . mark ; self . skip_non_blank () ; let name = self . scan_directive_name () ? ; let tok = match name . as_ref () { "YAML" => self . scan_version_directive_value (& start_mark) ? , "TAG" => self . scan_tag_directive_value (& start_mark) ? , _ => { while ! is_breakz (self . look_ch ()) { self . skip_non_blank () ; } Token (start_mark , TokenType :: TagDirective (String :: new () , String :: new ()) ,) } } ; self . skip_ws_to_eol (SkipTabs :: Yes) ? ; if is_breakz (self . ch ()) { self . lookahead (2) ; self . skip_linebreak () ; Ok (tok) } else { Err (ScanError :: new (start_mark , "while scanning a directive, did not find expected comment or line break" ,)) } } fn scan_version_directive_value (& mut self , mark : & Marker) -> Result < Token , ScanError > { while is_blank (self . look_ch ()) { self . skip_blank () ; } let major = self . scan_version_directive_number (mark) ? ; if self . ch () != '.' { return Err (ScanError :: new (* mark , "while scanning a YAML directive, did not find expected digit or '.' character" ,)) ; } self . skip_non_blank () ; let minor = self . scan_version_directive_number (mark) ? ; Ok (Token (* mark , TokenType :: VersionDirective (major , minor))) } fn scan_directive_name (& mut self) -> Result < String , ScanError > { let start_mark = self . mark ; let mut string = String :: new () ; while is_alpha (self . look_ch ()) { string . push (self . ch ()) ; self . skip_non_blank () ; } if string . is_empty () { return Err (ScanError :: new (start_mark , "while scanning a directive, could not find expected directive name" ,)) ; } if ! is_blank_or_breakz (self . ch ()) { return Err (ScanError :: new (start_mark , "while scanning a directive, found unexpected non-alphabetical character" ,)) ; } Ok (string) } fn scan_version_directive_number (& mut self , mark : & Marker) -> Result < u32 , ScanError > { let mut val = 0u32 ; let mut length = 0usize ; while let Some (digit) = self . look_ch () . to_digit (10) { if length + 1 > 9 { return Err (ScanError :: new (* mark , "while scanning a YAML directive, found extremely long version number" ,)) ; } length += 1 ; val = val * 10 + digit ; self . skip_non_blank () ; } if length == 0 { return Err (ScanError :: new (* mark , "while scanning a YAML directive, did not find expected version number" ,)) ; } Ok (val) } fn scan_tag_directive_value (& mut self , mark : & Marker) -> Result < Token , ScanError > { while is_blank (self . look_ch ()) { self . skip_blank () ; } let handle = self . scan_tag_handle (true , mark) ? ; while is_blank (self . look_ch ()) { self . skip_blank () ; } let prefix = self . scan_tag_prefix (mark) ? ; self . lookahead (1) ; if is_blank_or_breakz (self . ch ()) { Ok (Token (* mark , TokenType :: TagDirective (handle , prefix))) } else { Err (ScanError :: new (* mark , "while scanning TAG, did not find expected whitespace or line break" ,)) } } fn fetch_tag (& mut self) -> ScanResult { self . save_simple_key () ; self . disallow_simple_key () ; let tok = self . scan_tag () ? ; self . tokens . push_back (tok) ; Ok (()) } fn scan_tag (& mut self) -> Result < Token , ScanError > { let start_mark = self . mark ; let mut handle = String :: new () ; let mut suffix ; self . lookahead (2) ; if self . buffer [1] == '<' { suffix = self . scan_verbatim_tag (& start_mark) ? ; } else { handle = self . scan_tag_handle (false , & start_mark) ? ; if handle . len () >= 2 && handle . starts_with ('!') && handle . ends_with ('!') { let is_secondary_handle = handle == "!!" ; suffix = self . scan_tag_shorthand_suffix (false , is_secondary_handle , "" , & start_mark) ? ; } else { suffix = self . scan_tag_shorthand_suffix (false , false , & handle , & start_mark) ? ; "!" . clone_into (& mut handle) ; if suffix . is_empty () { handle . clear () ; suffix = "!" . to_owned () ; } } } if is_blank_or_breakz (self . look_ch ()) || (self . flow_level > 0 && is_flow (self . ch ())) { Ok (Token (start_mark , TokenType :: Tag (handle , suffix))) } else { Err (ScanError :: new (start_mark , "while scanning a tag, did not find expected whitespace or line break" ,)) } } fn scan_tag_handle (& mut self , directive : bool , mark : & Marker) -> Result < String , ScanError > { let mut string = String :: new () ; if self . look_ch () != '!' { return Err (ScanError :: new (* mark , "while scanning a tag, did not find expected '!'" ,)) ; } string . push (self . ch ()) ; self . skip_non_blank () ; while is_alpha (self . look_ch ()) { string . push (self . ch ()) ; self . skip_non_blank () ; } if self . ch () == '!' { string . push (self . ch ()) ; self . skip_non_blank () ; } else if directive && string != "!" { return Err (ScanError :: new (* mark , "while parsing a tag directive, did not find expected '!'" ,)) ; } Ok (string) } # [doc = " Scan for a tag prefix (6.8.2.2)."] # [doc = ""] # [doc = " There are 2 kinds of tag prefixes:"] # [doc = "   - Local: Starts with a `!`, contains only URI chars (`!foo`)"] # [doc = "   - Global: Starts with a tag char, contains then URI chars (`!foo,2000:app/`)"] fn scan_tag_prefix (& mut self , start_mark : & Marker) -> Result < String , ScanError > { let mut string = String :: new () ; if self . look_ch () == '!' { string . push (self . ch ()) ; self . skip_non_blank () ; } else if ! is_tag_char (self . ch ()) { return Err (ScanError :: new (* start_mark , "invalid global tag character")) ; } else if self . ch () == '%' { string . push (self . scan_uri_escapes (start_mark) ?) ; } else { string . push (self . ch ()) ; self . skip_non_blank () ; } while is_uri_char (self . look_ch ()) { if self . ch () == '%' { string . push (self . scan_uri_escapes (start_mark) ?) ; } else { string . push (self . ch ()) ; self . skip_non_blank () ; } } Ok (string) } # [doc = " Scan for a verbatim tag."] # [doc = ""] # [doc = " The prefixing `!<` must _not_ have been skipped."] fn scan_verbatim_tag (& mut self , start_mark : & Marker) -> Result < String , ScanError > { self . skip_non_blank () ; self . skip_non_blank () ; let mut string = String :: new () ; while is_uri_char (self . look_ch ()) { if self . ch () == '%' { string . push (self . scan_uri_escapes (start_mark) ?) ; } else { string . push (self . ch ()) ; self . skip_non_blank () ; } } if self . ch () != '>' { return Err (ScanError :: new (* start_mark , "while scanning a verbatim tag, did not find the expected '>'" ,)) ; } self . skip_non_blank () ; Ok (string) } fn scan_tag_shorthand_suffix (& mut self , _directive : bool , _is_secondary : bool , head : & str , mark : & Marker ,) -> Result < String , ScanError > { let mut length = head . len () ; let mut string = String :: new () ; if length > 1 { string . extend (head . chars () . skip (1)) ; } while is_tag_char (self . look_ch ()) { if self . ch () == '%' { string . push (self . scan_uri_escapes (mark) ?) ; } else { string . push (self . ch ()) ; self . skip_non_blank () ; } length += 1 ; } if length == 0 { return Err (ScanError :: new (* mark , "while parsing a tag, did not find expected tag URI" ,)) ; } Ok (string) } fn scan_uri_escapes (& mut self , mark : & Marker) -> Result < char , ScanError > { let mut width = 0usize ; let mut code = 0u32 ; loop { self . lookahead (3) ; if ! (self . ch () == '%' && is_hex (self . buffer [1]) && is_hex (self . buffer [2])) { return Err (ScanError :: new (* mark , "while parsing a tag, did not find URI escaped octet" ,)) ; } let octet = (as_hex (self . buffer [1]) << 4) + as_hex (self . buffer [2]) ; if width == 0 { width = match octet { _ if octet & 0x80 == 0x00 => 1 , _ if octet & 0xE0 == 0xC0 => 2 , _ if octet & 0xF0 == 0xE0 => 3 , _ if octet & 0xF8 == 0xF0 => 4 , _ => { return Err (ScanError :: new (* mark , "while parsing a tag, found an incorrect leading UTF-8 octet" ,)) ; } } ; code = octet ; } else { if octet & 0xc0 != 0x80 { return Err (ScanError :: new (* mark , "while parsing a tag, found an incorrect trailing UTF-8 octet" ,)) ; } code = (code << 8) + octet ; } self . skip_n_non_blank (3) ; width -= 1 ; if width == 0 { break ; } } match char :: from_u32 (code) { Some (ch) => Ok (ch) , None => Err (ScanError :: new (* mark , "while parsing a tag, found an invalid UTF-8 codepoint" ,)) , } } fn fetch_anchor (& mut self , alias : bool) -> ScanResult { self . save_simple_key () ; self . disallow_simple_key () ; let tok = self . scan_anchor (alias) ? ; self . tokens . push_back (tok) ; Ok (()) } fn scan_anchor (& mut self , alias : bool) -> Result < Token , ScanError > { let mut string = String :: new () ; let start_mark = self . mark ; self . skip_non_blank () ; while is_anchor_char (self . look_ch ()) { string . push (self . ch ()) ; self . skip_non_blank () ; } if string . is_empty () { return Err (ScanError :: new (start_mark , "while scanning an anchor or alias, did not find expected alphabetic or numeric character")) ; } if alias { Ok (Token (start_mark , TokenType :: Alias (string))) } else { Ok (Token (start_mark , TokenType :: Anchor (string))) } } fn fetch_flow_collection_start (& mut self , tok : TokenType) -> ScanResult { self . save_simple_key () ; self . roll_one_col_indent () ; self . increase_flow_level () ? ; self . allow_simple_key () ; let start_mark = self . mark ; self . skip_non_blank () ; if tok == TokenType :: FlowMappingStart { self . flow_mapping_started = true ; } self . skip_ws_to_eol (SkipTabs :: Yes) ? ; self . tokens . push_back (Token (start_mark , tok)) ; Ok (()) } fn fetch_flow_collection_end (& mut self , tok : TokenType) -> ScanResult { self . remove_simple_key () ? ; self . decrease_flow_level () ; self . disallow_simple_key () ; self . end_implicit_mapping (self . mark) ; let start_mark = self . mark ; self . skip_non_blank () ; self . skip_ws_to_eol (SkipTabs :: Yes) ? ; if self . flow_level > 0 { self . adjacent_value_allowed_at = self . mark . index ; } self . tokens . push_back (Token (start_mark , tok)) ; Ok (()) } # [doc = " Push the `FlowEntry` token and skip over the `,`."] fn fetch_flow_entry (& mut self) -> ScanResult { self . remove_simple_key () ? ; self . allow_simple_key () ; self . end_implicit_mapping (self . mark) ; let start_mark = self . mark ; self . skip_non_blank () ; self . skip_ws_to_eol (SkipTabs :: Yes) ? ; self . tokens . push_back (Token (start_mark , TokenType :: FlowEntry)) ; Ok (()) } fn increase_flow_level (& mut self) -> ScanResult { self . simple_keys . push (SimpleKey :: new (Marker :: new (0 , 0 , 0))) ; self . flow_level = self . flow_level . checked_add (1) . ok_or_else (| | ScanError :: new (self . mark , "recursion limit exceeded")) ? ; Ok (()) } fn decrease_flow_level (& mut self) { if self . flow_level > 0 { self . flow_level -= 1 ; self . simple_keys . pop () . unwrap () ; } } # [doc = " Push the `Block*` token(s) and skip over the `-`."] # [doc = ""] # [doc = " Add an indentation level and push a `BlockSequenceStart` token if needed, then push a"] # [doc = " `BlockEntry` token."] # [doc = " This function only skips over the `-` and does not fetch the entry value."] fn fetch_block_entry (& mut self) -> ScanResult { if self . flow_level > 0 { return Err (ScanError :: new (self . mark , r#""-" is only valid inside a block"# ,)) ; } if ! self . simple_key_allowed { return Err (ScanError :: new (self . mark , "block sequence entries are not allowed in this context" ,)) ; } if let Some (Token (mark , TokenType :: Anchor (..) | TokenType :: Tag (..))) = self . tokens . back () { if self . mark . col == 0 && mark . col == 0 && self . indent > - 1 { return Err (ScanError :: new (* mark , "invalid indentation for anchor")) ; } } let mark = self . mark ; self . skip_non_blank () ; self . roll_indent (mark . col , None , TokenType :: BlockSequenceStart , mark) ; let found_tabs = self . skip_ws_to_eol (SkipTabs :: Yes) ? . found_tabs () ; self . lookahead (2) ; if found_tabs && self . buffer [0] == '-' && is_blank_or_breakz (self . buffer [1]) { return Err (ScanError :: new (self . mark , "'-' must be followed by a valid YAML whitespace" ,)) ; } self . skip_ws_to_eol (SkipTabs :: No) ? ; if is_break (self . look_ch ()) || is_flow (self . ch ()) { self . roll_one_col_indent () ; } self . remove_simple_key () ? ; self . allow_simple_key () ; self . tokens . push_back (Token (self . mark , TokenType :: BlockEntry)) ; Ok (()) } fn fetch_document_indicator (& mut self , t : TokenType) -> ScanResult { self . unroll_indent (- 1) ; self . remove_simple_key () ? ; self . disallow_simple_key () ; let mark = self . mark ; self . skip_n_non_blank (3) ; self . tokens . push_back (Token (mark , t)) ; Ok (()) } fn fetch_block_scalar (& mut self , literal : bool) -> ScanResult { self . save_simple_key () ; self . allow_simple_key () ; let tok = self . scan_block_scalar (literal) ? ; self . tokens . push_back (tok) ; Ok (()) } # [allow (clippy :: too_many_lines)] fn scan_block_scalar (& mut self , literal : bool) -> Result < Token , ScanError > { let start_mark = self . mark ; let mut chomping = Chomping :: Clip ; let mut increment : usize = 0 ; let mut indent : usize = 0 ; let mut trailing_blank : bool ; let mut leading_blank : bool = false ; let style = if literal { TScalarStyle :: Literal } else { TScalarStyle :: Folded } ; let mut string = String :: new () ; let mut leading_break = String :: new () ; let mut trailing_breaks = String :: new () ; let mut chomping_break = String :: new () ; self . skip_non_blank () ; self . unroll_non_block_indents () ; if self . look_ch () == '+' || self . ch () == '-' { if self . ch () == '+' { chomping = Chomping :: Keep ; } else { chomping = Chomping :: Strip ; } self . skip_non_blank () ; if is_digit (self . look_ch ()) { if self . ch () == '0' { return Err (ScanError :: new (start_mark , "while scanning a block scalar, found an indentation indicator equal to 0" ,)) ; } increment = (self . ch () as usize) - ('0' as usize) ; self . skip_non_blank () ; } } else if is_digit (self . ch ()) { if self . ch () == '0' { return Err (ScanError :: new (start_mark , "while scanning a block scalar, found an indentation indicator equal to 0" ,)) ; } increment = (self . ch () as usize) - ('0' as usize) ; self . skip_non_blank () ; self . lookahead (1) ; if self . ch () == '+' || self . ch () == '-' { if self . ch () == '+' { chomping = Chomping :: Keep ; } else { chomping = Chomping :: Strip ; } self . skip_non_blank () ; } } self . skip_ws_to_eol (SkipTabs :: Yes) ? ; if ! is_breakz (self . look_ch ()) { return Err (ScanError :: new (start_mark , "while scanning a block scalar, did not find expected comment or line break" ,)) ; } if is_break (self . ch ()) { self . lookahead (2) ; self . read_break (& mut chomping_break) ; } if self . look_ch () == '\t' { return Err (ScanError :: new (start_mark , "a block scalar content cannot start with a tab" ,)) ; } if increment > 0 { indent = if self . indent >= 0 { (self . indent + increment as isize) as usize } else { increment } } if indent == 0 { self . skip_block_scalar_first_line_indent (& mut indent , & mut trailing_breaks) ; } else { self . skip_block_scalar_indent (indent , & mut trailing_breaks) ; } if is_z (self . ch ()) { let contents = match chomping { Chomping :: Strip => String :: new () , _ if self . mark . line == start_mark . line () => String :: new () , Chomping :: Clip => chomping_break , Chomping :: Keep if trailing_breaks . is_empty () => chomping_break , Chomping :: Keep => trailing_breaks , } ; return Ok (Token (start_mark , TokenType :: Scalar (style , contents))) ; } if self . mark . col < indent && (self . mark . col as isize) > self . indent { return Err (ScanError :: new (self . mark , "wrongly indented line in block scalar" ,)) ; } let mut line_buffer = String :: with_capacity (100) ; let start_mark = self . mark ; while self . mark . col == indent && ! is_z (self . ch ()) { if indent == 0 { self . lookahead (4) ; if self . next_is_document_end () { break ; } } trailing_blank = is_blank (self . ch ()) ; if ! literal && ! leading_break . is_empty () && ! leading_blank && ! trailing_blank { string . push_str (& trailing_breaks) ; if trailing_breaks . is_empty () { string . push (' ') ; } } else { string . push_str (& leading_break) ; string . push_str (& trailing_breaks) ; } leading_break . clear () ; trailing_breaks . clear () ; leading_blank = is_blank (self . ch ()) ; self . scan_block_scalar_content_line (& mut string , & mut line_buffer) ; if is_z (self . ch ()) { break ; } self . lookahead (2) ; self . read_break (& mut leading_break) ; self . skip_block_scalar_indent (indent , & mut trailing_breaks) ; } if chomping != Chomping :: Strip { string . push_str (& leading_break) ; if is_z (self . ch ()) && self . mark . col >= indent . max (1) { string . push ('\n') ; } } if chomping == Chomping :: Keep { string . push_str (& trailing_breaks) ; } Ok (Token (start_mark , TokenType :: Scalar (style , string))) } # [doc = " Retrieve the contents of the line, parsing it as a block scalar."] # [doc = ""] # [doc = " The contents will be appended to `string`. `line_buffer` is used as a temporary buffer to"] # [doc = " store bytes before pushing them to `string` and thus avoiding reallocating more than"] # [doc = " necessary. `line_buffer` is assumed to be empty upon calling this function. It will be"] # [doc = " `clear`ed before the end of the function."] # [doc = ""] # [doc = " This function assumed the first character to read is the first content character in the"] # [doc = " line. This function does not consume the line break character(s) after the line."] fn scan_block_scalar_content_line (& mut self , string : & mut String , line_buffer : & mut String) { while ! self . buffer . is_empty () && ! is_breakz (self . ch ()) { string . push (self . ch ()) ; self . skip_blank () ; } if self . buffer . is_empty () { let mut c = self . raw_read_ch () ; while ! is_breakz (c) { line_buffer . push (c) ; c = self . raw_read_ch () ; } self . buffer . push_back (c) . unwrap () ; self . mark . col += line_buffer . len () ; self . mark . index += line_buffer . len () ; string . reserve (line_buffer . len ()) ; string . push_str (line_buffer) ; line_buffer . clear () ; } } # [doc = " Skip the block scalar indentation and empty lines."] fn skip_block_scalar_indent (& mut self , indent : usize , breaks : & mut String) { loop { if indent < BUFFER_LEN - 2 { self . lookahead (BUFFER_LEN) ; while self . mark . col < indent && self . ch () == ' ' { self . skip_blank () ; } } else { loop { self . lookahead (BUFFER_LEN) ; while ! self . buffer . is_empty () && self . mark . col < indent && self . ch () == ' ' { self . skip_blank () ; } if self . mark . col == indent || (! self . buffer . is_empty () && self . ch () != ' ') { break ; } } self . lookahead (2) ; } if is_break (self . ch ()) { self . read_break (breaks) ; } else { break ; } } } # [doc = " Determine the indentation level for a block scalar from the first line of its contents."] # [doc = ""] # [doc = " The function skips over whitespace-only lines and sets `indent` to the the longest"] # [doc = " whitespace line that was encountered."] fn skip_block_scalar_first_line_indent (& mut self , indent : & mut usize , breaks : & mut String) { let mut max_indent = 0 ; loop { while self . look_ch () == ' ' { self . skip_blank () ; } if self . mark . col > max_indent { max_indent = self . mark . col ; } if is_break (self . ch ()) { self . lookahead (2) ; self . read_break (breaks) ; } else { break ; } } * indent = max_indent . max ((self . indent + 1) as usize) ; if self . indent > 0 { * indent = (* indent) . max (1) ; } } fn fetch_flow_scalar (& mut self , single : bool) -> ScanResult { self . save_simple_key () ; self . disallow_simple_key () ; let tok = self . scan_flow_scalar (single) ? ; self . skip_to_next_token () ? ; self . adjacent_value_allowed_at = self . mark . index ; self . tokens . push_back (tok) ; Ok (()) } # [allow (clippy :: too_many_lines)] fn scan_flow_scalar (& mut self , single : bool) -> Result < Token , ScanError > { let start_mark = self . mark ; let mut string = String :: new () ; let mut leading_break = String :: new () ; let mut trailing_breaks = String :: new () ; let mut whitespaces = String :: new () ; let mut leading_blanks ; self . skip_non_blank () ; loop { self . lookahead (4) ; if self . mark . col == 0 && (((self . buffer [0] == '-') && (self . buffer [1] == '-') && (self . buffer [2] == '-')) || ((self . buffer [0] == '.') && (self . buffer [1] == '.') && (self . buffer [2] == '.'))) && is_blank_or_breakz (self . buffer [3]) { return Err (ScanError :: new (start_mark , "while scanning a quoted scalar, found unexpected document indicator" ,)) ; } if is_z (self . ch ()) { return Err (ScanError :: new (start_mark , "while scanning a quoted scalar, found unexpected end of stream" ,)) ; } if (self . mark . col as isize) < self . indent { return Err (ScanError :: new (start_mark , "invalid indentation in quoted scalar" ,)) ; } leading_blanks = false ; self . consume_flow_scalar_non_whitespace_chars (single , & mut string , & mut leading_blanks , & start_mark ,) ? ; match self . look_ch () { '\'' if single => break , '"' if ! single => break , _ => { } } while is_blank (self . ch ()) || is_break (self . ch ()) { if is_blank (self . ch ()) { if leading_blanks { if self . ch () == '\t' && (self . mark . col as isize) < self . indent { return Err (ScanError :: new (self . mark , "tab cannot be used as indentation" ,)) ; } self . skip_blank () ; } else { whitespaces . push (self . ch ()) ; self . skip_blank () ; } } else { self . lookahead (2) ; if leading_blanks { self . read_break (& mut trailing_breaks) ; } else { whitespaces . clear () ; self . read_break (& mut leading_break) ; leading_blanks = true ; } } self . lookahead (1) ; } if leading_blanks { if leading_break . is_empty () { string . push_str (& leading_break) ; string . push_str (& trailing_breaks) ; trailing_breaks . clear () ; leading_break . clear () ; } else { if trailing_breaks . is_empty () { string . push (' ') ; } else { string . push_str (& trailing_breaks) ; trailing_breaks . clear () ; } leading_break . clear () ; } } else { string . push_str (& whitespaces) ; whitespaces . clear () ; } } self . skip_non_blank () ; self . skip_ws_to_eol (SkipTabs :: Yes) ? ; match self . ch () { ',' | '}' | ']' if self . flow_level > 0 => { } c if is_breakz (c) => { } ':' if self . flow_level == 0 && start_mark . line == self . mark . line => { } ':' if self . flow_level > 0 => { } _ => { return Err (ScanError :: new (self . mark , "invalid trailing content after double-quoted scalar" ,)) ; } } let style = if single { TScalarStyle :: SingleQuoted } else { TScalarStyle :: DoubleQuoted } ; Ok (Token (start_mark , TokenType :: Scalar (style , string))) } # [doc = " Consume successive non-whitespace characters from a flow scalar."] # [doc = ""] # [doc = " This function resolves escape sequences and stops upon encountering a whitespace, the end"] # [doc = " of the stream or the closing character for the scalar (`'` for single quoted scalars, `\"`"] # [doc = " for double quoted scalars)."] # [doc = ""] # [doc = " # Errors"] # [doc = " Return an error if an invalid escape sequence is found."] fn consume_flow_scalar_non_whitespace_chars (& mut self , single : bool , string : & mut String , leading_blanks : & mut bool , start_mark : & Marker ,) -> Result < () , ScanError > { self . lookahead (2) ; while ! is_blank_or_breakz (self . ch ()) { match self . ch () { '\'' if self . buffer [1] == '\'' && single => { string . push ('\'') ; self . skip_n_non_blank (2) ; } '\'' if single => break , '"' if ! single => break , '\\' if ! single && is_break (self . buffer [1]) => { self . lookahead (3) ; self . skip_non_blank () ; self . skip_linebreak () ; * leading_blanks = true ; break ; } '\\' if ! single => { string . push (self . resolve_flow_scalar_escape_sequence (start_mark) ?) ; } c => { string . push (c) ; self . skip_non_blank () ; } } self . lookahead (2) ; } Ok (()) } # [doc = " Escape the sequence we encounter in a flow scalar."] # [doc = ""] # [doc = " `self.ch()` must point to the `\\` starting the escape sequence."] # [doc = ""] # [doc = " # Errors"] # [doc = " Return an error if an invalid escape sequence is found."] fn resolve_flow_scalar_escape_sequence (& mut self , start_mark : & Marker ,) -> Result < char , ScanError > { let mut code_length = 0usize ; let mut ret = '\0' ; match self . buffer [1] { '0' => ret = '\0' , 'a' => ret = '\x07' , 'b' => ret = '\x08' , 't' | '\t' => ret = '\t' , 'n' => ret = '\n' , 'v' => ret = '\x0b' , 'f' => ret = '\x0c' , 'r' => ret = '\x0d' , 'e' => ret = '\x1b' , ' ' => ret = '\x20' , '"' => ret = '"' , '/' => ret = '/' , '\\' => ret = '\\' , 'N' => ret = char :: from_u32 (0x85) . unwrap () , '_' => ret = char :: from_u32 (0xA0) . unwrap () , 'L' => ret = char :: from_u32 (0x2028) . unwrap () , 'P' => ret = char :: from_u32 (0x2029) . unwrap () , 'x' => code_length = 2 , 'u' => code_length = 4 , 'U' => code_length = 8 , _ => { return Err (ScanError :: new (* start_mark , "while parsing a quoted scalar, found unknown escape character" ,)) } } self . skip_n_non_blank (2) ; if code_length > 0 { self . lookahead (code_length) ; let mut value = 0u32 ; for i in 0 .. code_length { if ! is_hex (self . buffer [i]) { return Err (ScanError :: new (* start_mark , "while parsing a quoted scalar, did not find expected hexadecimal number" ,)) ; } value = (value << 4) + as_hex (self . buffer [i]) ; } let Some (ch) = char :: from_u32 (value) else { return Err (ScanError :: new (* start_mark , "while parsing a quoted scalar, found invalid Unicode character escape code" ,)) ; } ; ret = ch ; self . skip_n_non_blank (code_length) ; } Ok (ret) } fn fetch_plain_scalar (& mut self) -> ScanResult { self . save_simple_key () ; self . disallow_simple_key () ; let tok = self . scan_plain_scalar () ? ; self . tokens . push_back (tok) ; Ok (()) } # [doc = " Scan for a plain scalar."] # [doc = ""] # [doc = " Plain scalars are the most readable but restricted style. They may span multiple lines in"] # [doc = " some contexts."] # [allow (clippy :: too_many_lines)] fn scan_plain_scalar (& mut self) -> Result < Token , ScanError > { self . unroll_non_block_indents () ; let indent = self . indent + 1 ; let start_mark = self . mark ; if self . flow_level > 0 && (start_mark . col as isize) < indent { return Err (ScanError :: new (start_mark , "invalid indentation in flow construct" ,)) ; } let mut string = String :: with_capacity (32) ; let mut leading_break = String :: with_capacity (32) ; let mut trailing_breaks = String :: with_capacity (32) ; let mut whitespaces = String :: with_capacity (32) ; loop { self . lookahead (4) ; if self . next_is_document_indicator () || self . ch () == '#' { break ; } if self . flow_level > 0 && self . ch () == '-' && is_flow (self . buffer [1]) { return Err (ScanError :: new (self . mark , "plain scalar cannot start with '-' followed by ,[]{}" ,)) ; } if ! is_blank_or_breakz (self . ch ()) && self . next_can_be_plain_scalar () { if self . leading_whitespace { if leading_break . is_empty () { string . push_str (& leading_break) ; string . push_str (& trailing_breaks) ; trailing_breaks . clear () ; leading_break . clear () ; } else { if trailing_breaks . is_empty () { string . push (' ') ; } else { string . push_str (& trailing_breaks) ; trailing_breaks . clear () ; } leading_break . clear () ; } self . leading_whitespace = false ; } else if ! whitespaces . is_empty () { string . push_str (& whitespaces) ; whitespaces . clear () ; } string . push (self . ch ()) ; self . skip_non_blank () ; self . lookahead (2) ; while ! is_blank_or_breakz (self . ch ()) { if ! self . next_can_be_plain_scalar () { break ; } string . push (self . ch ()) ; self . skip_non_blank () ; self . lookahead (2) ; } } if ! (is_blank (self . ch ()) || is_break (self . ch ())) { break ; } while is_blank (self . look_ch ()) || is_break (self . ch ()) { if is_blank (self . ch ()) { if ! self . leading_whitespace { whitespaces . push (self . ch ()) ; self . skip_blank () ; } else if (self . mark . col as isize) < indent && self . ch () == '\t' { self . skip_ws_to_eol (SkipTabs :: Yes) ? ; if ! is_breakz (self . ch ()) { return Err (ScanError :: new (start_mark , "while scanning a plain scalar, found a tab" ,)) ; } } else { self . skip_blank () ; } } else { self . lookahead (2) ; if self . leading_whitespace { self . read_break (& mut trailing_breaks) ; } else { whitespaces . clear () ; self . read_break (& mut leading_break) ; self . leading_whitespace = true ; } } } if self . flow_level == 0 && (self . mark . col as isize) < indent { break ; } } if self . leading_whitespace { self . allow_simple_key () ; } Ok (Token (start_mark , TokenType :: Scalar (TScalarStyle :: Plain , string) ,)) } fn fetch_key (& mut self) -> ScanResult { let start_mark = self . mark ; if self . flow_level == 0 { if ! self . simple_key_allowed { return Err (ScanError :: new (self . mark , "mapping keys are not allowed in this context" ,)) ; } self . roll_indent (start_mark . col , None , TokenType :: BlockMappingStart , start_mark ,) ; } else { self . flow_mapping_started = true ; } self . remove_simple_key () ? ; if self . flow_level == 0 { self . allow_simple_key () ; } else { self . disallow_simple_key () ; } self . skip_non_blank () ; self . skip_yaml_whitespace () ? ; if self . ch () == '\t' { return Err (ScanError :: new (self . mark () , "tabs disallowed in this context" ,)) ; } self . tokens . push_back (Token (start_mark , TokenType :: Key)) ; Ok (()) } # [doc = " Fetch a value from a mapping (after a `:`)."] fn fetch_value (& mut self) -> ScanResult { let sk = self . simple_keys . last () . unwrap () . clone () ; let start_mark = self . mark ; self . implicit_flow_mapping = self . flow_level > 0 && ! self . flow_mapping_started ; self . skip_non_blank () ; if self . look_ch () == '\t' && ! self . skip_ws_to_eol (SkipTabs :: Yes) ? . has_valid_yaml_ws () && (self . ch () == '-' || is_alpha (self . ch ())) { return Err (ScanError :: new (self . mark , "':' must be followed by a valid YAML whitespace" ,)) ; } if sk . possible { let tok = Token (sk . mark , TokenType :: Key) ; self . insert_token (sk . token_number - self . tokens_parsed , tok) ; if self . implicit_flow_mapping { if sk . mark . line < start_mark . line { return Err (ScanError :: new (start_mark , "illegal placement of ':' indicator" ,)) ; } self . insert_token (sk . token_number - self . tokens_parsed , Token (self . mark , TokenType :: FlowMappingStart) ,) ; } self . roll_indent (sk . mark . col , Some (sk . token_number) , TokenType :: BlockMappingStart , start_mark ,) ; self . roll_one_col_indent () ; self . simple_keys . last_mut () . unwrap () . possible = false ; self . disallow_simple_key () ; } else { if self . implicit_flow_mapping { self . tokens . push_back (Token (self . mark , TokenType :: FlowMappingStart)) ; } if self . flow_level == 0 { if ! self . simple_key_allowed { return Err (ScanError :: new (start_mark , "mapping values are not allowed in this context" ,)) ; } self . roll_indent (start_mark . col , None , TokenType :: BlockMappingStart , start_mark ,) ; } self . roll_one_col_indent () ; if self . flow_level == 0 { self . allow_simple_key () ; } else { self . disallow_simple_key () ; } } self . tokens . push_back (Token (start_mark , TokenType :: Value)) ; Ok (()) } # [doc = " Add an indentation level to the stack with the given block token, if needed."] # [doc = ""] # [doc = " An indentation level is added only if:"] # [doc = "   - We are not in a flow-style construct (which don't have indentation per-se)."] # [doc = "   - The current column is further indented than the last indent we have registered."] fn roll_indent (& mut self , col : usize , number : Option < usize > , tok : TokenType , mark : Marker) { if self . flow_level > 0 { return ; } if self . indent <= col as isize { if let Some (indent) = self . indents . last () { if ! indent . needs_block_end { self . indent = indent . indent ; self . indents . pop () ; } } } if self . indent < col as isize { self . indents . push (Indent { indent : self . indent , needs_block_end : true , }) ; self . indent = col as isize ; let tokens_parsed = self . tokens_parsed ; match number { Some (n) => self . insert_token (n - tokens_parsed , Token (mark , tok)) , None => self . tokens . push_back (Token (mark , tok)) , } } } # [doc = " Pop indentation levels from the stack as much as needed."] # [doc = ""] # [doc = " Indentation levels are popped from the stack while they are further indented than `col`."] # [doc = " If we are in a flow-style construct (which don't have indentation per-se), this function"] # [doc = " does nothing."] fn unroll_indent (& mut self , col : isize) { if self . flow_level > 0 { return ; } while self . indent > col { let indent = self . indents . pop () . unwrap () ; self . indent = indent . indent ; if indent . needs_block_end { self . tokens . push_back (Token (self . mark , TokenType :: BlockEnd)) ; } } } # [doc = " Add an indentation level of 1 column that does not start a block."] # [doc = ""] # [doc = " See the documentation of [`Indent::needs_block_end`] for more details."] # [doc = " An indentation is not added if we are inside a flow level or if the last indent is already"] # [doc = " a non-block indent."] fn roll_one_col_indent (& mut self) { if self . flow_level == 0 && self . indents . last () . map_or (false , | indent | indent . needs_block_end) { self . indents . push (Indent { indent : self . indent , needs_block_end : false , }) ; self . indent += 1 ; } } # [doc = " Unroll all last indents created with [`Self::roll_one_col_indent`]."] fn unroll_non_block_indents (& mut self) { while let Some (indent) = self . indents . last () { if indent . needs_block_end { break ; } self . indent = indent . indent ; self . indents . pop () ; } } # [doc = " Mark the next token to be inserted as a potential simple key."] fn save_simple_key (& mut self) { if self . simple_key_allowed { let required = self . flow_level == 0 && self . indent == (self . mark . col as isize) && self . indents . last () . unwrap () . needs_block_end ; let mut sk = SimpleKey :: new (self . mark) ; sk . possible = true ; sk . required = required ; sk . token_number = self . tokens_parsed + self . tokens . len () ; self . simple_keys . pop () ; self . simple_keys . push (sk) ; } } fn remove_simple_key (& mut self) -> ScanResult { let last = self . simple_keys . last_mut () . unwrap () ; if last . possible && last . required { return Err (ScanError :: new (self . mark , "simple key expected")) ; } last . possible = false ; Ok (()) } # [doc = " Check whether the next characters may be part of a plain scalar."] # [doc = ""] # [doc = " This function assumes we are not given a blankz character."] # [allow (clippy :: inline_always)] # [inline (always)] fn next_can_be_plain_scalar (& self) -> bool { match self . ch () { ':' if is_blank_or_breakz (self . buffer [1]) || (self . flow_level > 0 && is_flow (self . buffer [1])) => { false } c if self . flow_level > 0 && is_flow (c) => false , _ => true , } } # [doc = " Return whether the scanner is inside a block but outside of a flow sequence."] fn is_within_block (& self) -> bool { ! self . indents . is_empty () } # [doc = " If an implicit mapping had started, end it."] fn end_implicit_mapping (& mut self , mark : Marker) { if self . implicit_flow_mapping { self . implicit_flow_mapping = false ; self . flow_mapping_started = false ; self . tokens . push_back (Token (mark , TokenType :: FlowMappingEnd)) ; } } }
};
}
