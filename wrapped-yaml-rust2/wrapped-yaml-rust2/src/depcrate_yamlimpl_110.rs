// Generated macro for impl_110 (impl)
macro_rules! Depcrate_yamlimpl_110 {
() => {
// Module: crate::yaml
// Provides: {"impl_110"}
// Dependencies: {}
impl IntoIterator for Yaml { type Item = Yaml ; type IntoIter = YamlIter ; # [doc = " Extract the [`Array`] from `self` and iterate over it."] # [doc = ""] # [doc = " If `self` is **not** of the [`Yaml::Array`] variant, this function will not panic or return"] # [doc = " an error (as per the [`IntoIterator`] trait it cannot) but will instead return an iterator"] # [doc = " over an empty [`Array`]. Callers have to ensure (using [`Yaml::is_array`], [`matches`] or"] # [doc = " something similar) that the [`Yaml`] object is a [`Yaml::Array`] if they want to do error"] # [doc = " handling."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # use yaml_rust2::{Yaml, YamlLoader};"] # [doc = ""] # [doc = " // An array of 2 integers, 1 and 2."] # [doc = " let arr = &YamlLoader::load_from_str(\"- 1\\n- 2\").unwrap()[0];"] # [doc = ""] # [doc = " assert_eq!(arr.clone().into_iter().count(), 2);"] # [doc = " assert_eq!(arr.clone().into_iter().next(), Some(Yaml::Integer(1)));"] # [doc = " assert_eq!(arr.clone().into_iter().nth(1), Some(Yaml::Integer(2)));"] # [doc = ""] # [doc = " // An empty array returns an empty iterator."] # [doc = " let empty = Yaml::Array(vec![]);"] # [doc = " assert_eq!(empty.into_iter().count(), 0);"] # [doc = ""] # [doc = " // A hash with 2 key-value pairs, `(a, b)` and `(c, d)`."] # [doc = " let hash = YamlLoader::load_from_str(\"a: b\\nc: d\").unwrap().remove(0);"] # [doc = " // The hash has 2 elements."] # [doc = " assert_eq!(hash.as_hash().unwrap().iter().count(), 2);"] # [doc = " // But since `into_iter` can't be used with a `Yaml::Hash`, `into_iter` returns an empty"] # [doc = " // iterator."] # [doc = " assert_eq!(hash.into_iter().count(), 0);"] # [doc = " ```"] fn into_iter (self) -> Self :: IntoIter { YamlIter { yaml : self . into_vec () . unwrap_or_default () . into_iter () , } } }
};
}
