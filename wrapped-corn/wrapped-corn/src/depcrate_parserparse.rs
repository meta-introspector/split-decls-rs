// Generated macro for parse (function)
macro_rules! Depcrate_parserparse {
() => {
// Module: crate::parser
// Provides: {"parse"}
// Dependencies: {}
# [doc = " Parses the input string into a `Config`"] # [doc = " containing the resolved inputs"] # [doc = " and a map of values representing the top-level object."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use corn::parse;"] # [doc = ""] # [doc = " let corn = \"{foo = 42}\";"] # [doc = ""] # [doc = " let config = parse(corn).unwrap();"] # [doc = " let json = serde_json::to_string(&config).unwrap();"] # [doc = ""] # [doc = " assert_eq!(json, \"{\\\"foo\\\":42}\");"] # [doc = " ```"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Will fail if the input contains a syntax error."] # [doc = " Will fail if the input contains invalid Corn for another reason,"] # [doc = " including references to undefined inputs or dot-notation for non-object values."] # [doc = " Will fail if the input cannot be deserialized for any reaon."] # [doc = ""] # [doc = " Any of the above will return a specific error type with details."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the internal AST parser produces a tree in an invalid structure,"] # [doc = " the function will panic."] # [doc = " This indicates a severe error in the library and should never occur."] pub fn parse (file : & str) -> Result < Value > { let rules = AstParser :: parse (Rule :: config , file) ; match rules { Ok (mut rules) => { let first_block = rules . next () . expect ("should be at least 1 rule") ; match first_block . as_rule () { Rule :: assign_block => { let parser = CornParser :: new (Some (first_block)) ; let object_block = rules . next () . expect ("should always be an object block") ; parser . parse (object_block) } Rule :: object => { let parser = CornParser :: new (None) ; parser . parse (first_block) } _ => unreachable ! () , } } Err (error) => Err (Error :: ParserError (Box :: new (error))) , } }
};
}
