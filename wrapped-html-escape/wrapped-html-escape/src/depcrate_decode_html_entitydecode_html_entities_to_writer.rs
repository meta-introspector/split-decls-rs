// Generated macro for decode_html_entities_to_writer (function)
macro_rules! Depcrate_decode_html_entitydecode_html_entities_to_writer {
() => {
// Module: crate::decode::html_entity
// Provides: {"decode_html_entities_to_writer"}
// Dependencies: {}
# [cfg (feature = "std")] # [doc = " Decode html entities in a given string to a writer."] pub fn decode_html_entities_to_writer < S : AsRef < str > , W : Write > (text : S , output : & mut W ,) -> Result < () , io :: Error > { let text = text . as_ref () ; let text_bytes = text . as_bytes () ; let mut start = 0 ; let mut end = 0 ; let mut ep = 0 ; let mut step = 0 ; for e in text_bytes . iter () . copied () { match step { 0 => { if e == b'&' { step = 1 ; ep = end ; } } , 1 => { match e { b'#' => { step = 3 ; } , b';' => { step = 0 ; } , _ => { step = 2 ; } , } } , 2 => { if e == b';' { step = 0 ; let name = & text_bytes [(ep + 1) .. end] ; if let Ok (index) = NAMED_ENTITIES . binary_search_by (| (t_name , _) | t_name . cmp (& name)) { output . write_all (& text_bytes [start .. ep]) ? ; start = end + 1 ; output . write_all (NAMED_ENTITIES [index] . 1 . as_bytes ()) ? ; } } } , 3 => { match e { b'x' | b'X' => { step = 5 ; } , b';' => { step = 0 ; } , _ => step = 4 , } } , 4 => { if e == b';' { step = 0 ; let number = unsafe { text . get_unchecked ((ep + 2) .. end) } ; if let Ok (number) = number . parse :: < u32 > () { if let Ok (c) = char :: try_from (number) { output . write_all (& text_bytes [start .. ep]) ? ; start = end + 1 ; write_char_to_writer (c , output) ? ; } } } } , 5 => { match e { b';' => { step = 0 ; } , _ => step = 6 , } } , 6 => { if e == b';' { step = 0 ; let hex = unsafe { text . get_unchecked ((ep + 3) .. end) } ; if let Ok (number) = u32 :: from_str_radix (hex , 16) { if let Ok (c) = char :: try_from (number) { output . write_all (& text_bytes [start .. ep]) ? ; start = end + 1 ; write_char_to_writer (c , output) ? ; } } } } , _ => unreachable ! () , } end += 1 ; } output . write_all (& text_bytes [start .. end]) }
};
}
