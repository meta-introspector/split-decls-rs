// Generated macro for index (module)
macro_rules! Depcrate_plumbing_options_freeindex {
() => {
// Module: crate::plumbing::options::free
// Provides: {"index"}
// Dependencies: {}
pub mod index { use std :: path :: PathBuf ; # [derive (Debug , clap :: Parser)] pub struct Platform { # [doc = " The object format to assume when reading files that don't inherently know about it, or when writing files."] # [clap (long , default_value_t = gix :: hash :: Kind :: default () , value_parser = crate :: shared :: AsHashKind)] pub object_hash : gix :: hash :: Kind , # [doc = " The path to the index file."] # [clap (short = 'i' , long , default_value = ".git/index")] pub index_path : PathBuf , # [doc = " Subcommands"] # [clap (subcommand)] pub cmd : Subcommands , } # [derive (Debug , clap :: Subcommand)] pub enum Subcommands { # [doc = " Create an index from a list of empty files, one per line of the input."] FromList { # [doc = " Overwrite the specified index file if it already exists."] # [clap (long , short = 'f')] force : bool , # [doc = " Path to the index file to be written."] # [doc = " If none is given it will be kept in memory only as a way to measure performance. One day we will probably write the index"] # [doc = " back by default, but that requires us to write more of the index to work."] # [clap (long , short = 'i')] index_output_path : Option < PathBuf > , # [doc = " Don't write the trailing hash for a performance gain."] # [clap (long , short = 's')] skip_hash : bool , # [doc = " The file to read the index entries from, one path per line."] file : PathBuf , } , # [doc = " Validate constraints and assumptions of an index along with its integrity."] Verify , # [doc = " Print information about the index structure"] Info { # [doc = " Do not extract specific extension information to gain only a superficial idea of the index's composition."] # [clap (long)] no_details : bool , } , # [doc = " Checkout the index into a directory with exclusive write access, similar to what would happen during clone."] CheckoutExclusive { # [doc = " The path to `.git` repository from which objects can be obtained to write the actual files referenced"] # [doc = " in the index. Use this measure the impact on extracting objects on overall performance."] # [clap (long , short = 'r')] repository : Option < PathBuf > , # [doc = " Ignore errors and keep checking out as many files as possible, and report all errors at the end of the operation."] # [clap (long , short = 'k')] keep_going : bool , # [doc = " Enable to query the object database yet write only empty files. This is useful to measure the overhead of ODB query"] # [doc = " compared to writing the bytes to disk."] # [clap (long , short = 'e' , requires = "repository")] empty_files : bool , # [doc = " The directory into which to write all index entries."] directory : PathBuf , } , } }
};
}
