// Generated macro for impl_47 (impl)
macro_rules! Depcrate_bitmapimpl_47 {
() => {
// Module: crate::bitmap
// Provides: {"impl_47"}
// Dependencies: {}
impl < 'a > BitMapBackend < 'a , RGBPixel > { # [doc = " Create a new bitmap backend"] # [cfg (all (not (target_arch = "wasm32") , feature = "image"))] pub fn new < T : AsRef < Path > + ? Sized > (path : & 'a T , (w , h) : (u32 , u32)) -> Self { Self { target : Target :: File (path . as_ref ()) , size : (w , h) , buffer : Buffer :: Owned (vec ! [0 ; Self :: PIXEL_SIZE * (w * h) as usize]) , saved : false , _phantomdata : PhantomData , } } # [doc = " Create a new bitmap backend that generate GIF animation"] # [doc = ""] # [doc = " When this is used, the bitmap backend acts similar to a real-time rendering backend."] # [doc = " When the program finished drawing one frame, use `present` function to flush the frame"] # [doc = " into the GIF file."] # [doc = ""] # [doc = " - `path`: The path to the GIF file to create"] # [doc = " - `dimension`: The size of the GIF image"] # [doc = " - `speed`: The amount of time for each frame to display"] # [cfg (all (feature = "gif" , not (target_arch = "wasm32") , feature = "image"))] pub fn gif < T : AsRef < Path > > (path : T , (w , h) : (u32 , u32) , frame_delay : u32 ,) -> Result < Self , BitMapBackendError > { Ok (Self { target : Target :: Gif (Box :: new (crate :: gif_support :: GifFile :: new (path , (w , h) , frame_delay ,) ?)) , size : (w , h) , buffer : Buffer :: Owned (vec ! [0 ; Self :: PIXEL_SIZE * (w * h) as usize]) , saved : false , _phantomdata : PhantomData , }) } # [doc = " Create a new bitmap backend which only lives in-memory"] # [doc = ""] # [doc = " When this is used, the bitmap backend will write to a user provided [u8] array (or `Vec<u8>`)"] # [doc = " in RGB pixel format."] # [doc = ""] # [doc = " Note: This function provides backward compatibility for those code that assumes Plotters"] # [doc = " uses RGB pixel format and manipulates the in-memory framebuffer."] # [doc = " For more pixel format option, use `with_buffer_and_format` instead."] # [doc = ""] # [doc = " - `buf`: The buffer to operate"] # [doc = " - `dimension`: The size of the image in pixels"] # [doc = " - **returns**: The newly created bitmap backend"] pub fn with_buffer (buf : & 'a mut [u8] , (w , h) : (u32 , u32)) -> Self { Self :: with_buffer_and_format (buf , (w , h)) . expect ("Wrong buffer size") } }
};
}
