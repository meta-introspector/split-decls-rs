// Generated macro for impl_48 (impl)
macro_rules! Depcrate_bitmapimpl_48 {
() => {
// Module: crate::bitmap
// Provides: {"impl_48"}
// Dependencies: {}
impl < 'a , P : PixelFormat > BitMapBackend < 'a , P > { # [doc = " Create a new bitmap backend with a in-memory buffer with specific pixel format."] # [doc = ""] # [doc = " Note: This can be used as a way to manipulate framebuffer, `mmap` can be used on the top of this"] # [doc = " as well."] # [doc = ""] # [doc = " - `buf`: The buffer to operate"] # [doc = " - `dimension`: The size of the image in pixels"] # [doc = " - **returns**: The newly created bitmap backend"] pub fn with_buffer_and_format (buf : & 'a mut [u8] , (w , h) : (u32 , u32) ,) -> Result < Self , BitMapBackendError > { if (w * h) as usize * Self :: PIXEL_SIZE > buf . len () { return Err (BitMapBackendError :: InvalidBuffer) ; } Ok (Self { target : Target :: Buffer (PhantomData) , size : (w , h) , buffer : Buffer :: Borrowed (buf) , saved : false , _phantomdata : PhantomData , }) } # [inline (always)] pub (crate) fn get_raw_pixel_buffer (& mut self) -> & mut [u8] { self . buffer . borrow_buffer () } # [doc = " Split a bitmap backend vertically into several sub drawing area which allows"] # [doc = " multi-threading rendering."] # [doc = ""] # [doc = " - `area_size`: The size of the area"] # [doc = " - **returns**: The split backends that can be rendered in parallel"] pub fn split (& mut self , area_size : & [u32]) -> Vec < BitMapBackend < P > > { let (w , h) = self . get_size () ; let buf = self . get_raw_pixel_buffer () ; let base_addr = & mut buf [0] as * mut u8 ; let mut split_points = vec ! [0] ; for size in area_size { let next = split_points . last () . unwrap () + size ; if next >= h { break ; } split_points . push (next) ; } split_points . push (h) ; split_points . iter () . zip (split_points . iter () . skip (1)) . map (| (begin , end) | { let actual_buf = unsafe { std :: slice :: from_raw_parts_mut (base_addr . offset ((begin * w) as isize * Self :: PIXEL_SIZE as isize) , ((end - begin) * w) as usize * Self :: PIXEL_SIZE ,) } ; Self :: with_buffer_and_format (actual_buf , (w , end - begin)) . unwrap () }) . collect () } }
};
}
