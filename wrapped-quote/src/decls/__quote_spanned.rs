macro_rules! deps {
    () => {
        TokenStreamExt!();
        ToTokens!();
    };
}

macro_rules! __quote_spanned {
    () => {
        deps!();
        macro_rules ! __quote_spanned { ($ quote_spanned : item) => { # [doc = " Same as `quote!`, but applies a given span to all tokens originating within"] # [doc = " the macro invocation."] # [doc = ""] # [doc = " <br>"] # [doc = ""] # [doc = " # Syntax"] # [doc = ""] # [doc = " A span expression of type [`Span`], followed by `=>`, followed by the tokens"] # [doc = " to quote. The span expression should be brief &mdash; use a variable for"] # [doc = " anything more than a few characters. There should be no space before the"] # [doc = " `=>` token."] # [doc = ""] # [doc = " [`Span`]: proc_macro2::Span"] # [doc = ""] # [doc = " ```"] # [doc = " # use proc_macro2::Span;"] # [doc = " # use quote::quote_spanned;"] # [doc = " #"] # [doc = " # const IGNORE_TOKENS: &'static str = stringify! {"] # [doc = " let span = /* ... */;"] # [doc = " # };"] # [doc = " # let span = Span::call_site();"] # [doc = " # let init = 0;"] # [doc = ""] # [doc = " // On one line, use parentheses."] # [doc = " let tokens = quote_spanned!(span=> Box::into_raw(Box::new(#init)));"] # [doc = ""] # [doc = " // On multiple lines, place the span at the top and use braces."] # [doc = " let tokens = quote_spanned! {span=>"] # [doc = "     Box::into_raw(Box::new(#init))"] # [doc = " };"] # [doc = " ```"] # [doc = ""] # [doc = " The lack of space before the `=>` should look jarring to Rust programmers"] # [doc = " and this is intentional. The formatting is designed to be visibly"] # [doc = " off-balance and draw the eye a particular way, due to the span expression"] # [doc = " being evaluated in the context of the procedural macro and the remaining"] # [doc = " tokens being evaluated in the generated code."] # [doc = ""] # [doc = " <br>"] # [doc = ""] # [doc = " # Hygiene"] # [doc = ""] # [doc = " Any interpolated tokens preserve the `Span` information provided by their"] # [doc = " `ToTokens` implementation. Tokens that originate within the `quote_spanned!`"] # [doc = " invocation are spanned with the given span argument."] # [doc = ""] # [doc = " <br>"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " The following procedural macro code uses `quote_spanned!` to assert that a"] # [doc = " particular Rust type implements the [`Sync`] trait so that references can be"] # [doc = " safely shared between threads."] # [doc = ""] # [doc = " ```"] # [doc = " # use quote::{quote_spanned, TokenStreamExt, ToTokens};"] # [doc = " # use proc_macro2::{Span, TokenStream};"] # [doc = " #"] # [doc = " # struct Type;"] # [doc = " #"] # [doc = " # impl Type {"] # [doc = " #     fn span(&self) -> Span {"] # [doc = " #         Span::call_site()"] # [doc = " #     }"] # [doc = " # }"] # [doc = " #"] # [doc = " # impl ToTokens for Type {"] # [doc = " #     fn to_tokens(&self, _tokens: &mut TokenStream) {}"] # [doc = " # }"] # [doc = " #"] # [doc = " # let ty = Type;"] # [doc = " # let call_site = Span::call_site();"] # [doc = " #"] # [doc = " let ty_span = ty.span();"] # [doc = " let assert_sync = quote_spanned! {ty_span=>"] # [doc = "     struct _AssertSync where #ty: Sync;"] # [doc = " };"] # [doc = " ```"] # [doc = ""] # [doc = " If the assertion fails, the user will see an error like the following. The"] # [doc = " input span of their type is highlighted in the error."] # [doc = ""] # [doc = " ```text"] # [doc = " error[E0277]: the trait bound `*const (): std::marker::Sync` is not satisfied"] # [doc = "   --> src/main.rs:10:21"] # [doc = "    |"] # [doc = " 10 |     static ref PTR: *const () = &();"] # [doc = "    |                     ^^^^^^^^^ `*const ()` cannot be shared between threads safely"] # [doc = " ```"] # [doc = ""] # [doc = " In this example it is important for the where-clause to be spanned with the"] # [doc = " line/column information of the user's input type so that error messages are"] # [doc = " placed appropriately by the compiler."] $ quote_spanned } ; }
    };
}

__quote_spanned!()