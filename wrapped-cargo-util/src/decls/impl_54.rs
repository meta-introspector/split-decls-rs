macro_rules! deps {
    () => {
        ProcessBuilder!();
        ProcessError!();
    };
}

macro_rules! impl_54 {
    () => {
        deps!();
        impl ProcessBuilder { # [doc = " Creates a new [`ProcessBuilder`] with the given executable path."] pub fn new < T : AsRef < OsStr > > (cmd : T) -> ProcessBuilder { ProcessBuilder { program : cmd . as_ref () . to_os_string () , arg0 : None , args : Vec :: new () , cwd : None , env : BTreeMap :: new () , wrappers : Vec :: new () , jobserver : None , display_env_vars : false , retry_with_argfile : false , stdin : None , } } # [doc = " (chainable) Sets the executable for the process."] pub fn program < T : AsRef < OsStr > > (& mut self , program : T) -> & mut ProcessBuilder { self . program = program . as_ref () . to_os_string () ; self } # [doc = " (chainable) Overrides `arg0` for this program."] pub fn arg0 < T : AsRef < OsStr > > (& mut self , arg : T) -> & mut ProcessBuilder { self . arg0 = Some (arg . as_ref () . to_os_string ()) ; self } # [doc = " (chainable) Adds `arg` to the args list."] pub fn arg < T : AsRef < OsStr > > (& mut self , arg : T) -> & mut ProcessBuilder { self . args . push (arg . as_ref () . to_os_string ()) ; self } # [doc = " (chainable) Adds multiple `args` to the args list."] pub fn args < T : AsRef < OsStr > > (& mut self , args : & [T]) -> & mut ProcessBuilder { self . args . extend (args . iter () . map (| t | t . as_ref () . to_os_string ())) ; self } # [doc = " (chainable) Replaces the args list with the given `args`."] pub fn args_replace < T : AsRef < OsStr > > (& mut self , args : & [T]) -> & mut ProcessBuilder { if let Some (program) = self . wrappers . pop () { self . program = program ; self . wrappers = Vec :: new () ; } self . args = args . iter () . map (| t | t . as_ref () . to_os_string ()) . collect () ; self } # [doc = " (chainable) Sets the current working directory of the process."] pub fn cwd < T : AsRef < OsStr > > (& mut self , path : T) -> & mut ProcessBuilder { self . cwd = Some (path . as_ref () . to_os_string ()) ; self } # [doc = " (chainable) Sets an environment variable for the process."] pub fn env < T : AsRef < OsStr > > (& mut self , key : & str , val : T) -> & mut ProcessBuilder { self . env . insert (key . to_string () , Some (val . as_ref () . to_os_string ())) ; self } # [doc = " (chainable) Unsets an environment variable for the process."] pub fn env_remove (& mut self , key : & str) -> & mut ProcessBuilder { self . env . insert (key . to_string () , None) ; self } # [doc = " Gets the executable name."] pub fn get_program (& self) -> & OsString { self . wrappers . last () . unwrap_or (& self . program) } # [doc = " Gets the program arg0."] pub fn get_arg0 (& self) -> Option < & OsStr > { self . arg0 . as_deref () } # [doc = " Gets the program arguments."] pub fn get_args (& self) -> impl Iterator < Item = & OsString > { self . wrappers . iter () . rev () . chain (once (& self . program)) . chain (self . args . iter ()) . skip (1) } # [doc = " Gets the current working directory for the process."] pub fn get_cwd (& self) -> Option < & Path > { self . cwd . as_ref () . map (Path :: new) } # [doc = " Gets an environment variable as the process will see it (will inherit from environment"] # [doc = " unless explicitly unset)."] pub fn get_env (& self , var : & str) -> Option < OsString > { self . env . get (var) . cloned () . or_else (| | Some (env :: var_os (var))) . and_then (| s | s) } # [doc = " Gets all environment variables explicitly set or unset for the process (not inherited"] # [doc = " vars)."] pub fn get_envs (& self) -> & BTreeMap < String , Option < OsString > > { & self . env } # [doc = " Sets the `make` jobserver. See the [jobserver crate][jobserver_docs] for"] # [doc = " more information."] # [doc = ""] # [doc = " [jobserver_docs]: https://docs.rs/jobserver/latest/jobserver/"] pub fn inherit_jobserver (& mut self , jobserver : & Client) -> & mut Self { self . jobserver = Some (jobserver . clone ()) ; self } # [doc = " Enables environment variable display."] pub fn display_env_vars (& mut self) -> & mut Self { self . display_env_vars = true ; self } # [doc = " Enables retrying with an argfile if hitting \"command line too big\" error"] # [doc = ""] # [doc = " This is primarily for the `@path` arg of rustc and rustdoc, which treat"] # [doc = " each line as an command-line argument, so `LF` and `CRLF` bytes are not"] # [doc = " valid as an argument for argfile at this moment."] # [doc = " For example, `RUSTDOCFLAGS=\"--crate-version foo\\nbar\" cargo doc` is"] # [doc = " valid when invoking from command-line but not from argfile."] # [doc = ""] # [doc = " To sum up, the limitations of the argfile are:"] # [doc = ""] # [doc = " - Must be valid UTF-8 encoded."] # [doc = " - Must not contain any newlines in each argument."] # [doc = ""] # [doc = " Ref:"] # [doc = ""] # [doc = " - <https://doc.rust-lang.org/rustdoc/command-line-arguments.html#path-load-command-line-flags-from-a-path>"] # [doc = " - <https://doc.rust-lang.org/rustc/command-line-arguments.html#path-load-command-line-flags-from-a-path>"] pub fn retry_with_argfile (& mut self , enabled : bool) -> & mut Self { self . retry_with_argfile = enabled ; self } # [doc = " Sets a value that will be written to stdin of the process on launch."] pub fn stdin < T : Into < Vec < u8 > > > (& mut self , stdin : T) -> & mut Self { self . stdin = Some (stdin . into ()) ; self } fn should_retry_with_argfile (& self , err : & io :: Error) -> bool { self . retry_with_argfile && imp :: command_line_too_big (err) } # [doc = " Like [`Command::status`] but with a better error message."] pub fn status (& self) -> Result < ExitStatus > { self . _status () . with_context (| | ProcessError :: could_not_execute (self)) } fn _status (& self) -> io :: Result < ExitStatus > { if ! debug_force_argfile (self . retry_with_argfile) { let mut cmd = self . build_command () ; match cmd . spawn () { Err (ref e) if self . should_retry_with_argfile (e) => { } Err (e) => return Err (e) , Ok (mut child) => return child . wait () , } } let (mut cmd , argfile) = self . build_command_with_argfile () ? ; let status = cmd . spawn () ? . wait () ; close_tempfile_and_log_error (argfile) ; status } # [doc = " Runs the process, waiting for completion, and mapping non-success exit codes to an error."] pub fn exec (& self) -> Result < () > { let exit = self . status () ? ; if exit . success () { Ok (()) } else { let cmd_str = format ! ("{}" , self) ; eprintln ! ("\n./rerun.sh {}" , cmd_str) ; let err_msg = format ! ("process didn't exit successfully: {}" , cmd_str) ; Err (ProcessError :: new (& err_msg , Some (exit) , None ,) . into ()) } } # [doc = " Replaces the current process with the target process."] # [doc = ""] # [doc = " On Unix, this executes the process using the Unix syscall `execvp`, which will block"] # [doc = " this process, and will only return if there is an error."] # [doc = ""] # [doc = " On Windows this isn't technically possible. Instead we emulate it to the best of our"] # [doc = " ability. One aspect we fix here is that we specify a handler for the Ctrl-C handler."] # [doc = " In doing so (and by effectively ignoring it) we should emulate proxying Ctrl-C"] # [doc = " handling to the application at hand, which will either terminate or handle it itself."] # [doc = " According to Microsoft's documentation at"] # [doc = " <https://docs.microsoft.com/en-us/windows/console/ctrl-c-and-ctrl-break-signals>."] # [doc = " the Ctrl-C signal is sent to all processes attached to a terminal, which should"] # [doc = " include our child process. If the child terminates then we'll reap them in Cargo"] # [doc = " pretty quickly, and if the child handles the signal then we won't terminate"] # [doc = " (and we shouldn't!) until the process itself later exits."] pub fn exec_replace (& self) -> Result < () > { imp :: exec_replace (self) } # [doc = " Like [`Command::output`] but with a better error message."] pub fn output (& self) -> Result < Output > { self . _output () . with_context (| | ProcessError :: could_not_execute (self)) } fn _output (& self) -> io :: Result < Output > { if ! debug_force_argfile (self . retry_with_argfile) { let mut cmd = self . build_command () ; match piped (& mut cmd , self . stdin . is_some ()) . spawn () { Err (ref e) if self . should_retry_with_argfile (e) => { } Err (e) => return Err (e) , Ok (mut child) => { if let Some (stdin) = & self . stdin { child . stdin . take () . unwrap () . write_all (stdin) ? ; } return child . wait_with_output () ; } } } let (mut cmd , argfile) = self . build_command_with_argfile () ? ; let mut child = piped (& mut cmd , self . stdin . is_some ()) . spawn () ? ; if let Some (stdin) = & self . stdin { child . stdin . take () . unwrap () . write_all (stdin) ? ; } let output = child . wait_with_output () ; close_tempfile_and_log_error (argfile) ; output } # [doc = " Executes the process, returning the stdio output, or an error if non-zero exit status."] pub fn exec_with_output (& self) -> Result < Output > { let output = self . output () ? ; if output . status . success () { Ok (output) } else { let cmd_str = format ! ("{}" , self) ; eprintln ! ("\n./rerun.sh {}" , cmd_str) ; let err_msg = format ! ("process didn't exit successfully: {}" , cmd_str) ; Err (ProcessError :: new (& err_msg , Some (output . status) , Some (& output) ,) . into ()) } } # [doc = " Executes a command, passing each line of stdout and stderr to the supplied callbacks, which"] # [doc = " can mutate the string data."] # [doc = ""] # [doc = " If any invocations of these function return an error, it will be propagated."] # [doc = ""] # [doc = " If `capture_output` is true, then all the output will also be buffered"] # [doc = " and stored in the returned `Output` object. If it is false, no caching"] # [doc = " is done, and the callbacks are solely responsible for handling the"] # [doc = " output."] pub fn exec_with_streaming (& self , on_stdout_line : & mut dyn FnMut (& str) -> Result < () > , on_stderr_line : & mut dyn FnMut (& str) -> Result < () > , capture_output : bool ,) -> Result < Output > { let mut stdout = Vec :: new () ; let mut stderr = Vec :: new () ; let mut callback_error = None ; let mut stdout_pos = 0 ; let mut stderr_pos = 0 ; let spawn = | mut cmd | { if ! debug_force_argfile (self . retry_with_argfile) { match piped (& mut cmd , false) . spawn () { Err (ref e) if self . should_retry_with_argfile (e) => { } Err (e) => return Err (e) , Ok (child) => return Ok ((child , None)) , } } let (mut cmd , argfile) = self . build_command_with_argfile () ? ; Ok ((piped (& mut cmd , false) . spawn () ? , Some (argfile))) } ; let status = (| | { let cmd = self . build_command () ; let (mut child , argfile) = spawn (cmd) ? ; let out = child . stdout . take () . unwrap () ; let err = child . stderr . take () . unwrap () ; read2 (out , err , & mut | is_out , data , eof | { let pos = if is_out { & mut stdout_pos } else { & mut stderr_pos } ; let idx = if eof { data . len () } else { match data [* pos ..] . iter () . rposition (| b | * b == b'\n') { Some (i) => * pos + i + 1 , None => { * pos = data . len () ; return ; } } } ; let new_lines = & data [.. idx] ; for line in String :: from_utf8_lossy (new_lines) . lines () { if callback_error . is_some () { break ; } let callback_result = if is_out { on_stdout_line (line) } else { on_stderr_line (line) } ; if let Err (e) = callback_result { callback_error = Some (e) ; break ; } } if capture_output { let dst = if is_out { & mut stdout } else { & mut stderr } ; dst . extend (new_lines) ; } data . drain (.. idx) ; * pos = 0 ; }) ? ; let status = child . wait () ; if let Some (argfile) = argfile { close_tempfile_and_log_error (argfile) ; } status }) () . with_context (| | ProcessError :: could_not_execute (self)) ? ; let output = Output { status , stdout , stderr , } ; { let to_print = if capture_output { Some (& output) } else { None } ; if let Some (e) = callback_error { let cx = ProcessError :: new (& format ! ("failed to parse process output: {}" , self) , Some (output . status) , to_print ,) ; bail ! (anyhow :: Error :: new (cx) . context (e)) ; } else if ! output . status . success () { let cmd_str = format ! ("{}" , self) ; eprintln ! ("\n./rerun.sh {}" , cmd_str) ; let err_msg = format ! ("process didn't exit successfully: {}" , cmd_str) ; bail ! (ProcessError :: new (& err_msg , Some (output . status) , to_print ,)) ; } } Ok (output) } # [doc = " Builds the command with an `@<path>` argfile that contains all the"] # [doc = " arguments. This is primarily served for rustc/rustdoc command family."] fn build_command_with_argfile (& self) -> io :: Result < (Command , NamedTempFile) > { use std :: io :: Write as _ ; let mut tmp = tempfile :: Builder :: new () . prefix ("cargo-argfile.") . tempfile () ? ; let mut arg = OsString :: from ("@") ; arg . push (tmp . path ()) ; let mut cmd = self . build_command_without_args () ; cmd . arg (arg) ; tracing :: debug ! ("created argfile at {} for {self}" , tmp . path () . display ()) ; let cap = self . get_args () . map (| arg | arg . len () + 1) . sum :: < usize > () ; let mut buf = Vec :: with_capacity (cap) ; for arg in & self . args { let arg = arg . to_str () . ok_or_else (| | { io :: Error :: new (io :: ErrorKind :: Other , format ! ("argument for argfile contains invalid UTF-8 characters: `{}`" , arg . to_string_lossy ()) ,) }) ? ; if arg . contains ('\n') { return Err (io :: Error :: new (io :: ErrorKind :: Other , format ! ("argument for argfile contains newlines: `{arg}`") ,)) ; } writeln ! (buf , "{arg}") ? ; } tmp . write_all (& mut buf) ? ; Ok ((cmd , tmp)) } # [doc = " Builds a command from `ProcessBuilder` for everything but not `args`."] fn build_command_without_args (& self) -> Command { let mut command = { let mut iter = self . wrappers . iter () . rev () . chain (once (& self . program)) ; let mut cmd = Command :: new (iter . next () . expect ("at least one `program` exists")) ; cmd . args (iter) ; cmd } ; # [cfg (unix)] if let Some (arg0) = self . get_arg0 () { use std :: os :: unix :: process :: CommandExt as _ ; command . arg0 (arg0) ; } if let Some (cwd) = self . get_cwd () { command . current_dir (cwd) ; } for (k , v) in & self . env { match * v { Some (ref v) => { command . env (k , v) ; } None => { command . env_remove (k) ; } } } if let Some (ref c) = self . jobserver { c . configure (& mut command) ; } command } # [doc = " Converts `ProcessBuilder` into a `std::process::Command`, and handles"] # [doc = " the jobserver, if present."] # [doc = ""] # [doc = " Note that this method doesn't take argfile fallback into account. The"] # [doc = " caller should handle it by themselves."] pub fn build_command (& self) -> Command { let mut command = self . build_command_without_args () ; for arg in & self . args { command . arg (arg) ; } command } # [doc = " Wraps an existing command with the provided wrapper, if it is present and valid."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use cargo_util::ProcessBuilder;"] # [doc = " // Running this would execute `rustc`"] # [doc = " let cmd = ProcessBuilder::new(\"rustc\");"] # [doc = ""] # [doc = " // Running this will execute `sccache rustc`"] # [doc = " let cmd = cmd.wrapped(Some(\"sccache\"));"] # [doc = " ```"] pub fn wrapped (mut self , wrapper : Option < impl AsRef < OsStr > >) -> Self { if let Some (wrapper) = wrapper . as_ref () { let wrapper = wrapper . as_ref () ; if ! wrapper . is_empty () { self . wrappers . push (wrapper . to_os_string ()) ; } } self } }
    };
}

impl_54!();