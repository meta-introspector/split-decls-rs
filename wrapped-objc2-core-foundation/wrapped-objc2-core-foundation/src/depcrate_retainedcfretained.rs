// Generated macro for CFRetained (struct)
macro_rules! Depcrate_retainedCFRetained {
() => {
// Module: crate::retained
// Provides: {"CFRetained"}
// Dependencies: {}
# [doc = " A reference counted pointer type for CoreFoundation types."] # [doc = ""] # [doc = " [`CFRetained`] strongly references or \"retains\" the given object `T`, and"] # [doc = " decrements the retain count or \"releases\" it again when dropped, thereby"] # [doc = " ensuring it will be deallocated at the right time."] # [doc = ""] # [doc = " The type `T` inside `CFRetained<T>` can be anything that implements"] # [doc = " [`Type`], i.e. any CoreFoundation-like type."] # [doc = ""] # [doc = ""] # [doc = " # Comparison to other types"] # [doc = ""] # [doc = " `CFRetained<T>` is equivalent to [`objc2::rc::Retained`], and can be"] # [doc = " converted to/from that when the `\"objc2\"` feature is enabled. Note though"] # [doc = " that this uses the underlying CoreFoundation primitives `CFRetain` /"] # [doc = " `CFRelease` / `CFAutorelease` instead of `objc_retain` / `objc_release` /"] # [doc = " `objc_autorelease`, to avoid depending on the Objective-C runtime if not"] # [doc = " needed."] # [doc = ""] # [doc = " You can also view `CFRetained<T>` as the CoreFoundation equivalent of"] # [doc = " [`std::sync::Arc`], that is, it is a thread-safe reference-counting smart"] # [doc = " pointer that allows cloning by bumping the reference count."] # [doc = ""] # [doc = " Unlike `Arc`, objects can be retained directly from a `&T` using"] # [doc = " [`Type::retain`] (for `Arc` you need `&Arc<T>`)."] # [doc = ""] # [doc = " Weak references are not supported though without the Objective-C runtime."] # [doc = ""] # [cfg_attr (not (feature = "objc2") , doc = "[`objc2::rc::Retained`]: #objc2-not-available")] # [doc = ""] # [doc = ""] # [doc = " # Forwarding implementations"] # [doc = ""] # [doc = " Since `CFRetained<T>` is a smart pointer, it [`Deref`]s to `T`."] # [doc = ""] # [doc = " It also forwards the implementation of a bunch of standard library traits"] # [doc = " such as [`PartialEq`], [`AsRef`], and so on, so that it becomes possible"] # [doc = " to use e.g. `CFRetained<CFString>` as-if it was `CFString`. Note that"] # [doc = " having a `CFString` directly is not possible since CoreFoundation objects"] # [doc = " cannot live on the stack, but instead must reside on the heap, and as such"] # [doc = " must be accessed behind a pointer or a reference (i.e. `&CFString`)."] # [doc = ""] # [doc = ""] # [doc = " # Memory layout"] # [doc = ""] # [doc = " This is guaranteed to have the same size and alignment as a pointer to the"] # [doc = " object, `*const T`."] # [doc = ""] # [doc = " Additionally, it participates in the null-pointer optimization, that is,"] # [doc = " `Option<CFRetained<T>>` is guaranteed to have the same size as"] # [doc = " `CFRetained<T>`."] # [repr (transparent)] # [doc (alias = "id")] # [doc (alias = "Retained")] # [doc (alias = "objc2::rc::Retained")] # [cfg_attr (feature = "unstable-coerce-pointee" , derive (std :: marker :: CoercePointee))] pub struct CFRetained < T : ? Sized > { # [doc = " A pointer to the contained type. The pointer is always retained."] # [doc = ""] # [doc = " It is important that this is `NonNull`, since we want to dereference"] # [doc = " it later, and be able to use the null-pointer optimization."] ptr : NonNull < T > , # [doc = " Necessary for dropck even though we never actually run T's destructor,"] # [doc = " because it might have a `dealloc` that assumes that contained"] # [doc = " references outlive the type."] # [doc = ""] # [doc = " See <https://doc.rust-lang.org/nightly/nomicon/phantom-data.html>"] item : PhantomData < T > , # [doc = " Marks the type as !UnwindSafe. Later on we'll re-enable this."] # [doc = ""] # [doc = " See <https://github.com/rust-lang/rust/issues/93367> for why this is"] # [doc = " required."] notunwindsafe : PhantomData < & 'static mut () > , }
};
}
