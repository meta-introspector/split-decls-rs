// Generated macro for impl_107 (impl)
macro_rules! Depcrate_dictionaryimpl_107 {
() => {
// Module: crate::dictionary
// Provides: {"impl_107"}
// Dependencies: {}
# [doc = " Direct, unsafe object accessors."] # [doc = ""] # [doc = " CFDictionary stores its keys and values directly, and you can get"] # [doc = " references to those without having to retain them first - but only if the"] # [doc = " dictionary isn't mutated while doing so - otherwise, you might end up"] # [doc = " accessing a deallocated object."] impl < K : ? Sized , V : ? Sized > CFDictionary < K , V > { # [doc = " Get a direct reference to one of the dictionary's values."] # [doc = ""] # [doc = " Consider using the [`get`](Self::get) method instead, unless you're"] # [doc = " seeing performance issues from the retaining."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The dictionary must not be mutated while the returned reference is"] # [doc = " live."] # [inline] # [doc (alias = "CFDictionaryGetValue")] pub unsafe fn get_unchecked (& self , key : & K) -> Option < & V > where K : Type + Sized , V : Type + Sized , { let value = unsafe { self . as_opaque () . value (to_void (key)) } ; unsafe { value . cast :: < V > () . as_ref () } } # [doc = " A vector containing direct references to the dictionary's keys and"] # [doc = " values."] # [doc = ""] # [doc = " Consider using the [`to_vecs`](Self::to_vecs) method instead, unless"] # [doc = " you're seeing performance issues from the retaining."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The array must not be mutated while the returned references are alive."] # [cfg (feature = "alloc")] pub unsafe fn to_vecs_unchecked (& self) -> (Vec < & K > , Vec < & V >) where K : Type , V : Type , { let len = self . len () ; let mut keys = Vec :: < & K > :: with_capacity (len) ; let mut values = Vec :: < & V > :: with_capacity (len) ; let keys_ptr = keys . as_mut_ptr () . cast :: < * const c_void > () ; let values_ptr = values . as_mut_ptr () . cast :: < * const c_void > () ; unsafe { self . as_opaque () . keys_and_values (keys_ptr , values_ptr) } ; unsafe { keys . set_len (len) ; values . set_len (len) ; } (keys , values) } }
};
}
