// Generated macro for ConcreteType (trait)
macro_rules! Depcrate_type_traitsConcreteType {
() => {
// Module: crate::type_traits
// Provides: {"ConcreteType"}
// Dependencies: {}
# [doc = " A concrete CoreFoundation type."] # [doc = ""] # [doc = " This trait is implemented for CoreFoundation types which have a"] # [doc = " `CFTypeID`, which should be most types except for mutable variants, types"] # [doc = " with generics, as well as the root `CFType`."] # [doc = ""] # [doc = ""] # [doc = " # Mutable types"] # [doc = ""] # [doc = " Some types have immutable and mutable variants, the prime example being"] # [doc = " `CFString` and `CFMutableString`. Internally, these are very complex class"] # [doc = " clusters, but in the simplest case they're sometimes the same type, the"] # [doc = " only difference being that the mutable variant has a boolean flag set."] # [doc = " This means that they also share the same type ID, and thus we cannot"] # [doc = " (stably) differentiate between them at runtime."] # [doc = ""] # [doc = " Therefore, this trait is only implemented for the immutable variant, to"] # [doc = " prevent it from accidentally being misused (it is unclear whether it would"] # [doc = " be unsound or not). If you're looking to convert to a mutable type, you'll"] # [doc = " have to either construct a new one with APIs like"] # [doc = " `CFStringCreateMutableCopy`, or use an unchecked cast."] # [doc = ""] # [doc = ""] # [doc = " # Generic types"] # [doc = ""] # [doc = " Only the \"base\" type / the type without generics implements this."] # [doc = ""] # [doc = " So for example, `CFArray` implements this trait, but `CFArray<CFString>`"] # [doc = " does not (as we cannot query the type of the inner element without"] # [doc = " actually inspecting the element, and since `CFArray`s aren't required to"] # [doc = " hold CF types)."] # [doc = ""] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " - The type must not be mutable."] # [doc = " - The type must not be generic (e.g. this can only be implemented for the"] # [doc = "   base `CFArray`, not for all `CFArray<T>`)."] # [doc = " - The [`type_id`][Self::type_id] must be implemented correctly, and must"] # [doc = "   uniquely identify the type."] pub unsafe trait ConcreteType : Type { # [doc = " Get the unique `CFTypeID` identifier for the type."] # [doc = ""] # [doc = " For example, this corresponds to `CFStringGetTypeID` for `CFString`"] # [doc = " and `CGColorGetTypeID` for `CGColor`."] # [doc (alias = "GetTypeID")] fn type_id () -> CFTypeID ; }
};
}
