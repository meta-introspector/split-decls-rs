// Generated macro for impl_1302 (impl)
macro_rules! Depcrate_matrix_graphimpl_1302 {
() => {
// Module: crate::matrix_graph
// Provides: {"impl_1302"}
// Dependencies: {}
impl < N , E , S : BuildHasher , Ty : EdgeType , Null : Nullable < Wrapped = E > , Ix : IndexType > MatrixGraph < N , E , S , Ty , Null , Ix > { # [doc = " Create a new `MatrixGraph` with estimated capacity for nodes."] pub fn with_capacity (node_capacity : usize) -> Self where S : Default , { Self :: with_capacity_and_hasher (node_capacity , Default :: default ()) } # [doc = " Create a new `MatrixGraph` with estimated capacity for nodes and a provided hasher."] pub fn with_capacity_and_hasher (node_capacity : usize , hasher : S) -> Self { let mut m = Self { node_adjacencies : vec ! [] , node_capacity : 0 , nodes : IdStorage :: with_capacity_and_hasher (node_capacity , hasher) , nb_edges : 0 , ty : PhantomData , ix : PhantomData , } ; debug_assert ! (node_capacity <= < Ix as IndexType >:: max () . index ()) ; if node_capacity > 0 { m . extend_capacity_for_node (NodeIndex :: new (node_capacity - 1) , true) ; } m } # [inline] fn to_edge_position (& self , a : NodeIndex < Ix > , b : NodeIndex < Ix >) -> Option < usize > { if cmp :: max (a . index () , b . index ()) >= self . node_capacity { return None ; } Some (self . to_edge_position_unchecked (a , b)) } # [inline] fn to_edge_position_unchecked (& self , a : NodeIndex < Ix > , b : NodeIndex < Ix >) -> usize { to_linearized_matrix_position :: < Ty > (a . index () , b . index () , self . node_capacity) } # [doc = " Remove all nodes and edges."] pub fn clear (& mut self) { for edge in self . node_adjacencies . iter_mut () { * edge = Default :: default () ; } self . nodes . clear () ; self . nb_edges = 0 ; } # [doc = " Return the number of nodes (also called vertices) in the graph."] # [doc = ""] # [doc = " Computes in **O(1)** time."] # [inline] pub fn node_count (& self) -> usize { self . nodes . len () } # [doc = " Return the number of edges in the graph."] # [doc = ""] # [doc = " Computes in **O(1)** time."] # [inline] pub fn edge_count (& self) -> usize { self . nb_edges } # [doc = " Return whether the graph has directed edges or not."] # [inline] pub fn is_directed (& self) -> bool { Ty :: is_directed () } # [doc = " Add a node (also called vertex) with associated data `weight` to the graph."] # [doc = ""] # [doc = " Computes in **O(1)** time."] # [doc = ""] # [doc = " Return the index of the new node."] # [doc = ""] # [doc = " **Panics** if the MatrixGraph is at the maximum number of nodes for its index type."] # [track_caller] pub fn add_node (& mut self , weight : N) -> NodeIndex < Ix > { NodeIndex :: new (self . nodes . add (weight)) } # [doc = " Try to add a node (also called vertex) with associated data `weight` to the graph."] # [doc = ""] # [doc = " Computes in **O(1)** time."] # [doc = ""] # [doc = " Return the index of the new node."] # [doc = ""] # [doc = " Possible errors:"] # [doc = " - [`MatrixError::NodeIxLimit`] if the `MatrixGraph` is at the maximum number of nodes for its index type."] pub fn try_add_node (& mut self , weight : N) -> Result < NodeIndex < Ix > , MatrixError > { let node_idx = NodeIndex :: < Ix > :: new (self . nodes . len ()) ; if ! (< Ix as IndexType > :: max () . index () == ! 0 || NodeIndex :: end () != node_idx) { return Err (MatrixError :: NodeIxLimit) ; } Ok (NodeIndex :: new (self . nodes . add (weight))) } # [doc = " Remove `a` from the graph."] # [doc = ""] # [doc = " Computes in **O(V)** time, due to the removal of edges with other nodes."] # [doc = ""] # [doc = " **Panics** if the node `a` does not exist."] # [track_caller] pub fn remove_node (& mut self , a : NodeIndex < Ix >) -> N { for id in self . nodes . iter_ids () { let position = self . to_edge_position (a , NodeIndex :: new (id)) ; if let Some (pos) = position { self . node_adjacencies [pos] = Default :: default () ; } if Ty :: is_directed () { let position = self . to_edge_position (NodeIndex :: new (id) , a) ; if let Some (pos) = position { self . node_adjacencies [pos] = Default :: default () ; } } } self . nodes . remove (a . index ()) } # [inline] fn extend_capacity_for_node (& mut self , min_node : NodeIndex < Ix > , exact : bool) { self . node_capacity = extend_linearized_matrix :: < Ty , _ > (& mut self . node_adjacencies , self . node_capacity , min_node . index () + 1 , exact ,) ; } # [inline] fn extend_capacity_for_edge (& mut self , a : NodeIndex < Ix > , b : NodeIndex < Ix >) { let min_node = cmp :: max (a , b) ; if min_node . index () >= self . node_capacity { self . extend_capacity_for_node (min_node , false) ; } } # [doc = " Update the edge from `a` to `b` to the graph, with its associated data `weight`."] # [doc = ""] # [doc = " Return the previous data, if any."] # [doc = ""] # [doc = " Computes in **O(1)** time, best case."] # [doc = " Computes in **O(|V|^2)** time, worst case (matrix needs to be re-allocated)."] # [doc = ""] # [doc = " **Panics** if any of the nodes don't exist."] # [track_caller] pub fn update_edge (& mut self , a : NodeIndex < Ix > , b : NodeIndex < Ix > , weight : E) -> Option < E > { self . extend_capacity_for_edge (a , b) ; let p = self . to_edge_position_unchecked (a , b) ; let old_weight = mem :: replace (& mut self . node_adjacencies [p] , Null :: new (weight)) ; if old_weight . is_null () { self . nb_edges += 1 ; } old_weight . into () } # [doc = " Try to update the edge from `a` to `b`, with its associated data `weight`."] # [doc = ""] # [doc = " Return the previous data, if any."] # [doc = ""] # [doc = " Computes in **O(1)** time, best case."] # [doc = " Computes in **O(|V|^2)** time, worst case (matrix needs to be re-allocated)."] # [doc = ""] # [doc = " Possible errors:"] # [doc = " - [`MatrixError::NodeMissed`] if any of the nodes don't exist."] pub fn try_update_edge (& mut self , a : NodeIndex < Ix > , b : NodeIndex < Ix > , weight : E ,) -> Result < Option < E > , MatrixError > { self . assert_node_bounds (a , b) ? ; Ok (self . update_edge (a , b , weight)) } # [doc = " Add an edge from `a` to `b` to the graph, with its associated"] # [doc = " data `weight`."] # [doc = ""] # [doc = " Computes in **O(1)** time, best case."] # [doc = " Computes in **O(|V|^2)** time, worst case (matrix needs to be re-allocated)."] # [doc = ""] # [doc = " **Panics** if any of the nodes don't exist."] # [doc = " **Panics** if an edge already exists from `a` to `b`."] # [doc = ""] # [doc = " **Note:** `MatrixGraph` does not allow adding parallel (“duplicate”) edges. If you want to avoid"] # [doc = " this, use [`.update_edge(a, b, weight)`](#method.update_edge) instead."] # [track_caller] pub fn add_edge (& mut self , a : NodeIndex < Ix > , b : NodeIndex < Ix > , weight : E) { let old_edge_id = self . update_edge (a , b , weight) ; assert ! (old_edge_id . is_none ()) ; } # [doc = " Add or update edge from `a` to `b` to the graph, with its associated"] # [doc = " data `weight`."] # [doc = ""] # [doc = " Return the previous data, if any."] # [doc = ""] # [doc = " Computes in **O(1)** time, best case."] # [doc = " Computes in **O(|V|^2)** time, worst case (matrix needs to be re-allocated)."] # [doc = ""] # [doc = " Possible errors:"] # [doc = " - [`MatrixError::NodeMissed`] if any of the nodes don't exist."] pub fn add_or_update_edge (& mut self , a : NodeIndex < Ix > , b : NodeIndex < Ix > , weight : E ,) -> Result < Option < E > , MatrixError > { self . extend_capacity_for_edge (a , b) ; self . try_update_edge (a , b , weight) } # [doc = " Remove the edge from `a` to `b` to the graph."] # [doc = ""] # [doc = " **Panics** if any of the nodes don't exist."] # [doc = " **Panics** if no edge exists between `a` and `b`."] # [track_caller] pub fn remove_edge (& mut self , a : NodeIndex < Ix > , b : NodeIndex < Ix >) -> E { let p = self . to_edge_position (a , b) . expect ("No edge found between the nodes.") ; let old_weight = mem :: take (& mut self . node_adjacencies [p]) . into () . unwrap () ; let old_weight : Option < _ > = old_weight . into () ; self . nb_edges -= 1 ; old_weight . unwrap () } # [doc = " Try to remove the edge from `a` to `b`."] # [doc = ""] # [doc = " Return old value if present."] pub fn try_remove_edge (& mut self , a : NodeIndex < Ix > , b : NodeIndex < Ix >) -> Option < E > { let p = self . to_edge_position (a , b) ? ; if let Some (entry) = self . node_adjacencies . get_mut (p) { let old_weight = mem :: take (entry) . into () ? ; self . nb_edges -= 1 ; return Some (old_weight) ; } None } # [doc = " Return `true` if there is an edge between `a` and `b`."] # [doc = ""] # [doc = " If any of the nodes don't exist - returns `false`."] # [doc = " **Panics** if any of the nodes don't exist."] # [track_caller] pub fn has_edge (& self , a : NodeIndex < Ix > , b : NodeIndex < Ix >) -> bool { if let Some (p) = self . to_edge_position (a , b) { return self . node_adjacencies . get (p) . map (| e | ! e . is_null ()) . unwrap_or (false) ; } false } # [doc = " Access the weight for node `a`."] # [doc = ""] # [doc = " Also available with indexing syntax: `&graph[a]`."] # [doc = ""] # [doc = " **Panics** if the node doesn't exist."] # [track_caller] pub fn node_weight (& self , a : NodeIndex < Ix >) -> & N { & self . nodes [a . index ()] } # [doc = " Try to access the weight for node `a`."] # [doc = ""] # [doc = " Return `None` if the node doesn't exist."] pub fn get_node_weight (& self , a : NodeIndex < Ix >) -> Option < & N > { self . nodes . elements . get (a . index ()) ? . as_ref () } # [doc = " Access the weight for node `a`, mutably."] # [doc = ""] # [doc = " Also available with indexing syntax: `&mut graph[a]`."] # [doc = ""] # [doc = " **Panics** if the node doesn't exist."] # [track_caller] pub fn node_weight_mut (& mut self , a : NodeIndex < Ix >) -> & mut N { & mut self . nodes [a . index ()] } # [doc = " Try to access the weight for node `a`, mutably."] # [doc = ""] # [doc = " Return `None` if the node doesn't exist."] pub fn get_node_weight_mut (& mut self , a : NodeIndex < Ix >) -> Option < & mut N > { self . nodes . elements . get_mut (a . index ()) ? . as_mut () } # [doc = " Access the weight for edge `e`."] # [doc = ""] # [doc = " Also available with indexing syntax: `&graph[e]`."] # [doc = ""] # [doc = " **Panics** if no edge exists between `a` and `b`."] # [track_caller] pub fn edge_weight (& self , a : NodeIndex < Ix > , b : NodeIndex < Ix >) -> & E { let p = self . to_edge_position (a , b) . expect ("No edge found between the nodes.") ; self . node_adjacencies [p] . as_ref () . expect ("No edge found between the nodes.") } # [doc = " Access the weight for edge from `a` to `b`."] # [doc = ""] # [doc = " Return `None` if the edge doesn't exist."] pub fn get_edge_weight (& self , a : NodeIndex < Ix > , b : NodeIndex < Ix >) -> Option < & E > { let p = self . to_edge_position (a , b) ? ; self . node_adjacencies . get (p) ? . as_ref () } # [doc = " Access the weight for edge `e`, mutably."] # [doc = ""] # [doc = " Also available with indexing syntax: `&mut graph[e]`."] # [doc = ""] # [doc = " **Panics** if no edge exists between `a` and `b`."] # [track_caller] pub fn edge_weight_mut (& mut self , a : NodeIndex < Ix > , b : NodeIndex < Ix >) -> & mut E { let p = self . to_edge_position (a , b) . expect ("No edge found between the nodes.") ; self . node_adjacencies [p] . as_mut () . expect ("No edge found between the nodes.") } # [doc = " Access the weight for edge from `a` to `b`, mutably."] # [doc = ""] # [doc = " Return `None` if the edge doesn't exist."] pub fn get_edge_weight_mut (& mut self , a : NodeIndex < Ix > , b : NodeIndex < Ix >) -> Option < & mut E > { let p = self . to_edge_position (a , b) ? ; self . node_adjacencies . get_mut (p) ? . as_mut () } # [doc = " Return an iterator of all nodes with an edge starting from `a`."] # [doc = ""] # [doc = " - `Directed`: Outgoing edges from `a`."] # [doc = " - `Undirected`: All edges from or to `a`."] # [doc = ""] # [doc = " Produces an empty iterator if the node doesn't exist.<br>"] # [doc = " Iterator element type is [`NodeIndex<Ix>`](../graph/struct.NodeIndex.html)."] pub fn neighbors (& self , a : NodeIndex < Ix >) -> Neighbors < '_ , Ty , Null , Ix > { Neighbors (Edges :: on_columns (a . index () , & self . node_adjacencies , self . node_capacity ,)) } # [doc = " Return an iterator of all edges of `a`."] # [doc = ""] # [doc = " - `Directed`: Outgoing edges from `a`."] # [doc = " - `Undirected`: All edges connected to `a`."] # [doc = ""] # [doc = " Produces an empty iterator if the node doesn't exist.<br>"] # [doc = " Iterator element type is `(NodeIndex<Ix>, NodeIndex<Ix>, &E)`."] pub fn edges (& self , a : NodeIndex < Ix >) -> Edges < '_ , Ty , Null , Ix > { Edges :: on_columns (a . index () , & self . node_adjacencies , self . node_capacity) } # [doc = " Create a new `MatrixGraph` from an iterable of edges."] # [doc = ""] # [doc = " Node weights `N` are set to default values."] # [doc = " Edge weights `E` may either be specified in the list,"] # [doc = " or they are filled with default values."] # [doc = ""] # [doc = " Nodes are inserted automatically to match the edges."] # [doc = ""] # [doc = " ```"] # [doc = " use petgraph::matrix_graph::MatrixGraph;"] # [doc = ""] # [doc = " let gr = MatrixGraph::<(), i32>::from_edges(&["] # [doc = "     (0, 1), (0, 2), (0, 3),"] # [doc = "     (1, 2), (1, 3),"] # [doc = "     (2, 3),"] # [doc = " ]);"] # [doc = " ```"] pub fn from_edges < I > (iterable : I) -> Self where I : IntoIterator , I :: Item : IntoWeightedEdge < E > , < I :: Item as IntoWeightedEdge < E > > :: NodeId : Into < NodeIndex < Ix > > , N : Default , S : Default , { let mut g = Self :: default () ; g . extend_with_edges (iterable) ; g } # [doc = " Extend the graph from an iterable of edges."] # [doc = ""] # [doc = " Node weights `N` are set to default values."] # [doc = " Edge weights `E` may either be specified in the list,"] # [doc = " or they are filled with default values."] # [doc = ""] # [doc = " Nodes are inserted automatically to match the edges."] pub fn extend_with_edges < I > (& mut self , iterable : I) where I : IntoIterator , I :: Item : IntoWeightedEdge < E > , < I :: Item as IntoWeightedEdge < E > > :: NodeId : Into < NodeIndex < Ix > > , N : Default , { for elt in iterable { let (source , target , weight) = elt . into_weighted_edge () ; let (source , target) = (source . into () , target . into ()) ; let nx = cmp :: max (source , target) ; while nx . index () >= self . node_count () { self . add_node (N :: default ()) ; } self . add_edge (source , target , weight) ; } } fn assert_node_bounds (& self , a : NodeIndex < Ix > , b : NodeIndex < Ix >) -> Result < () , MatrixError > { if a . index () >= self . node_capacity { Err (MatrixError :: NodeMissed (a . index ())) } else if b . index () >= self . node_capacity { Err (MatrixError :: NodeMissed (b . index ())) } else { Ok (()) } } }
};
}
