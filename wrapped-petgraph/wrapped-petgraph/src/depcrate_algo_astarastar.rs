// Generated macro for astar (function)
macro_rules! Depcrate_algo_astarastar {
() => {
// Module: crate::algo::astar
// Provides: {"astar"}
// Dependencies: {}
# [doc = " A* shortest path algorithm."] # [doc = ""] # [doc = " Computes the shortest path from `start` to `finish`, including the total path cost."] # [doc = ""] # [doc = " `finish` is implicitly given via the `is_goal` callback, which should return `true` if the"] # [doc = " given node is the finish node."] # [doc = ""] # [doc = " The function `edge_cost` should return the cost for a particular edge. Edge costs must be"] # [doc = " non-negative."] # [doc = ""] # [doc = " The function `estimate_cost` should return the estimated cost to the finish for a particular"] # [doc = " node. For the algorithm to find the actual shortest path, it should be admissible, meaning that"] # [doc = " it should never overestimate the actual cost to get to the nearest goal node. Estimate costs"] # [doc = " must also be non-negative."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `graph`: weighted graph."] # [doc = " * `start`: the start node."] # [doc = " * `is_goal`: the callback defines the goal node."] # [doc = " * `edge_cost`: closure that returns cost of a particular edge."] # [doc = " * `estimate_cost`: closure that returns the estimated cost to the finish for particular node."] # [doc = ""] # [doc = " # Returns"] # [doc = " * `Some(K, Vec<G::NodeId>)` - the total cost and path from start to finish, if one was found."] # [doc = " * `None` - if such a path was not found."] # [doc = ""] # [doc = " # Complexity"] # [doc = " The time complexity largely depends on the heuristic used. Feel free to contribute and provide the exact time complexity :)"] # [doc = ""] # [doc = " With a trivial heuristic, the algorithm will behave like [`fn@crate::algo::dijkstra`]."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " use petgraph::Graph;"] # [doc = " use petgraph::algo::astar;"] # [doc = ""] # [doc = " let mut g = Graph::new();"] # [doc = " let a = g.add_node((0., 0.));"] # [doc = " let b = g.add_node((2., 0.));"] # [doc = " let c = g.add_node((1., 1.));"] # [doc = " let d = g.add_node((0., 2.));"] # [doc = " let e = g.add_node((3., 3.));"] # [doc = " let f = g.add_node((4., 2.));"] # [doc = " g.extend_with_edges(&["] # [doc = "     (a, b, 2),"] # [doc = "     (a, d, 4),"] # [doc = "     (b, c, 1),"] # [doc = "     (b, f, 7),"] # [doc = "     (c, e, 5),"] # [doc = "     (e, f, 1),"] # [doc = "     (d, e, 1),"] # [doc = " ]);"] # [doc = ""] # [doc = " // Graph represented with the weight of each edge"] # [doc = " // Edges with '*' are part of the optimal path."] # [doc = " //"] # [doc = " //     2       1"] # [doc = " // a ----- b ----- c"] # [doc = " // | 4*    | 7     |"] # [doc = " // d       f       | 5"] # [doc = " // | 1*    | 1*    |"] # [doc = " // \\------ e ------/"] # [doc = ""] # [doc = " let path = astar(&g, a, |finish| finish == f, |e| *e.weight(), |_| 0);"] # [doc = " assert_eq!(path, Some((6, vec![a, d, e, f])));"] # [doc = " ```"] pub fn astar < G , F , H , K , IsGoal > (graph : G , start : G :: NodeId , mut is_goal : IsGoal , mut edge_cost : F , mut estimate_cost : H ,) -> Option < (K , Vec < G :: NodeId >) > where G : IntoEdges + Visitable , IsGoal : FnMut (G :: NodeId) -> bool , G :: NodeId : Eq + Hash , F : FnMut (G :: EdgeRef) -> K , H : FnMut (G :: NodeId) -> K , K : Measure + Copy , { let mut visit_next = BinaryHeap :: new () ; let mut scores = HashMap :: new () ; let mut path_tracker = PathTracker :: < G > :: new () ; let zero : K = K :: default () ; let g : K = zero ; let h : K = estimate_cost (start) ; let f : K = g + h ; scores . insert (start , (f , h , g)) ; visit_next . push (MinScored ((f , h , g) , start)) ; while let Some (MinScored ((f , h , g) , node)) = visit_next . pop () { if is_goal (node) { let path = path_tracker . reconstruct_path_to (node) ; let (goal_f , goal_h , goal_g) = scores [& node] ; debug_assert_eq ! (goal_h , zero) ; debug_assert_eq ! (goal_f , goal_g) ; return Some ((goal_f , path)) ; } match scores . entry (node) { Occupied (mut entry) => { let (_ , _ , old_g) = * entry . get () ; if old_g < g { continue ; } entry . insert ((f , h , g)) ; } Vacant (entry) => { entry . insert ((f , h , g)) ; } } for edge in graph . edges (node) { let neigh = edge . target () ; let neigh_g = g + edge_cost (edge) ; let neigh_h = estimate_cost (neigh) ; let neigh_f = neigh_g + neigh_h ; let neigh_score = (neigh_f , neigh_h , neigh_g) ; match scores . entry (neigh) { Occupied (mut entry) => { let (_ , _ , old_neigh_g) = * entry . get () ; if neigh_g >= old_neigh_g { continue ; } entry . insert (neigh_score) ; } Vacant (entry) => { entry . insert (neigh_score) ; } } path_tracker . set_predecessor (neigh , node) ; visit_next . push (MinScored (neigh_score , neigh)) ; } } None }
};
}
