// Generated macro for k_shortest_path (function)
macro_rules! Depcrate_algo_k_shortest_pathk_shortest_path {
() => {
// Module: crate::algo::k_shortest_path
// Provides: {"k_shortest_path"}
// Dependencies: {}
# [doc = " k'th shortest path algorithm."] # [doc = ""] # [doc = " Compute the length of the k-th shortest path from `start` to every reachable"] # [doc = " node. Edge costs must be non-negative."] # [doc = ""] # [doc = " If `goal` is not `None`, then the algorithm terminates once the `goal` node's"] # [doc = " cost is calculated."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `graph`: an input graph."] # [doc = " * `start`: the *start* node."] # [doc = " * `goal`: optional *goal* node."] # [doc = " * `k`: sequence number of the required shortest paths."] # [doc = " * `edge_cost`: closure that should return the cost for a particular edge, which is used"] # [doc = "   to compute path costs. Edge costs must be non-negative."] # [doc = ""] # [doc = " # Returns"] # [doc = " * `HashMap`: [`struct@hashbrown::HashMap`] that maps `NodeId` to path cost."] # [doc = ""] # [doc = " # Complexity"] # [doc = " * Time complexity: **O(k|E| log(k|E|))**."] # [doc = " * Auxiliary space: **O(|V| + k|E|)**."] # [doc = ""] # [doc = " where **|V|** is the number of nodes, **|E|** is the number of edges and **k** is the provided parameter."] # [doc = ""] # [doc = " # Example"] # [doc = " ```rust"] # [doc = " use petgraph::Graph;"] # [doc = " use petgraph::algo::k_shortest_path;"] # [doc = " use petgraph::prelude::*;"] # [doc = " use hashbrown::HashMap;"] # [doc = ""] # [doc = " let mut graph : Graph<(),(),Directed>= Graph::new();"] # [doc = " let a = graph.add_node(()); // node with no weight"] # [doc = " let b = graph.add_node(());"] # [doc = " let c = graph.add_node(());"] # [doc = " let d = graph.add_node(());"] # [doc = " let e = graph.add_node(());"] # [doc = " let f = graph.add_node(());"] # [doc = " let g = graph.add_node(());"] # [doc = " let h = graph.add_node(());"] # [doc = " // z will be in another connected component"] # [doc = " let z = graph.add_node(());"] # [doc = ""] # [doc = " graph.extend_with_edges(&["] # [doc = "     (a, b),"] # [doc = "     (b, c),"] # [doc = "     (c, d),"] # [doc = "     (d, a),"] # [doc = "     (e, f),"] # [doc = "     (b, e),"] # [doc = "     (f, g),"] # [doc = "     (g, h),"] # [doc = "     (h, e)"] # [doc = " ]);"] # [doc = " // a ----> b ----> e ----> f"] # [doc = " // ^       |       ^       |"] # [doc = " // |       v       |       v"] # [doc = " // d <---- c       h <---- g"] # [doc = ""] # [doc = " let expected_res: HashMap<NodeIndex, usize> = ["] # [doc = "      (a, 7),"] # [doc = "      (b, 4),"] # [doc = "      (c, 5),"] # [doc = "      (d, 6),"] # [doc = "      (e, 5),"] # [doc = "      (f, 6),"] # [doc = "      (g, 7),"] # [doc = "      (h, 8)"] # [doc = "     ].iter().cloned().collect();"] # [doc = " let res = k_shortest_path(&graph,b,None,2, |_| 1);"] # [doc = " assert_eq!(res, expected_res);"] # [doc = " // z is not inside res because there is not path from b to z."] # [doc = " ```"] pub fn k_shortest_path < G , F , K > (graph : G , start : G :: NodeId , goal : Option < G :: NodeId > , k : usize , mut edge_cost : F ,) -> HashMap < G :: NodeId , K > where G : IntoEdges + Visitable + NodeCount + NodeIndexable , G :: NodeId : Eq + Hash , F : FnMut (G :: EdgeRef) -> K , K : Measure + Copy , { let mut counter : Vec < usize > = vec ! [0 ; graph . node_count ()] ; let mut scores = HashMap :: new () ; let mut visit_next = BinaryHeap :: new () ; let zero_score = K :: default () ; visit_next . push (MinScored (zero_score , start)) ; while let Some (MinScored (node_score , node)) = visit_next . pop () { counter [graph . to_index (node)] += 1 ; let current_counter = counter [graph . to_index (node)] ; if current_counter > k { continue ; } if current_counter == k { scores . insert (node , node_score) ; } if goal . as_ref () == Some (& node) && current_counter == k { break ; } for edge in graph . edges (node) { visit_next . push (MinScored (node_score + edge_cost (edge) , edge . target ())) ; } } scores }
};
}
