// Generated macro for dinics (function)
macro_rules! Depcrate_algo_maximum_flow_dinicsdinics {
() => {
// Module: crate::algo::maximum_flow::dinics
// Provides: {"dinics"}
// Dependencies: {}
# [doc = " Compute the maximum flow from `source` to `destination` in a directed graph."] # [doc = " Implements [Dinic's (or Dinitz's) algorithm][dinics], which builds successive"] # [doc = " level graphs using breadth-first search and finds blocking flows within"] # [doc = " them through depth-first searches."] # [doc = ""] # [doc = " For simplicity, the algorithm requires `N::EdgeWeight` to implement"] # [doc = " only [PartialOrd] trait, and not [Ord], but will panic if it tries to"] # [doc = " compare two elements that aren't comparable (i.e., given two edge weights `a`"] # [doc = " and `b`, where neither `a >= b` nor `a < b`)."] # [doc = ""] # [doc = " See also [`maximum_flow`][max flow mod] module for other maximum flow algorithms."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `network` — A directed graph with positive edge weights, namely \"flow capacities\"."] # [doc = " * `source` — The source node where flow originates."] # [doc = " * `destination` — The destination node where flow terminates."] # [doc = ""] # [doc = " # Returns"] # [doc = " Returns a tuple of two values:"] # [doc = " * `N::EdgeWeight`: computed maximum flow;"] # [doc = " * `Vec<N::EdgeWeight>`: the flow of each edge. The vector is indexed by the graph's edge indices."] # [doc = ""] # [doc = " # Complexity"] # [doc = " * Time complexity:"] # [doc = "   * In general: **O(|V|²|E|)**"] # [doc = "   * In networks with only unit capacities: **O(min{|V|²ᐟ³, |E|¹ᐟ²} |E|)**"] # [doc = " * Auxiliary space: **O(|V| + |E|)**."] # [doc = ""] # [doc = " where **|V|** is the number of nodes and **|E|** is the number of edges."] # [doc = ""] # [doc = " [dinics]: https://en.wikipedia.org/wiki/Dinic%27s_algorithm"] # [doc = " [max flow mod]: index.html"] # [doc = ""] # [doc = " # Example"] # [doc = " ```rust"] # [doc = " use petgraph::Graph;"] # [doc = " use petgraph::algo::dinics;"] # [doc = " // Example from CLRS book"] # [doc = " let mut graph = Graph::<u8, u8>::new();"] # [doc = " let source = graph.add_node(0);"] # [doc = " let _ = graph.add_node(1);"] # [doc = " let _ = graph.add_node(2);"] # [doc = " let _ = graph.add_node(3);"] # [doc = " let _ = graph.add_node(4);"] # [doc = " let destination = graph.add_node(5);"] # [doc = " graph.extend_with_edges(&["] # [doc = "    (0, 1, 16),"] # [doc = "    (0, 2, 13),"] # [doc = "    (1, 2, 10),"] # [doc = "    (1, 3, 12),"] # [doc = "    (2, 1, 4),"] # [doc = "    (2, 4, 14),"] # [doc = "    (3, 2, 9),"] # [doc = "    (3, 5, 20),"] # [doc = "    (4, 3, 7),"] # [doc = "    (4, 5, 4),"] # [doc = " ]);"] # [doc = " let (max_flow, _) = dinics(&graph, source, destination);"] # [doc = " assert_eq!(23, max_flow);"] # [doc = " ```"] pub fn dinics < G > (network : G , source : G :: NodeId , destination : G :: NodeId ,) -> (G :: EdgeWeight , Vec < G :: EdgeWeight >) where G : NodeCount + EdgeCount + IntoEdgesDirected + EdgeIndexable + NodeIndexable + Visitable , G :: EdgeWeight : Sub < Output = G :: EdgeWeight > + PositiveMeasure , { let mut max_flow = G :: EdgeWeight :: zero () ; let mut flows = vec ! [G :: EdgeWeight :: zero () ; network . edge_count ()] ; let mut visited = network . visit_map () ; let mut level_edges = vec ! [Default :: default () ; network . node_bound ()] ; let dest_index = NodeIndexable :: to_index (& network , destination) ; while build_level_graph (& network , source , destination , & flows , & mut level_edges) [dest_index] > 0 { let flow_increase = find_blocking_flow (network , source , destination , & mut flows , & mut level_edges , & mut visited ,) ; max_flow = max_flow + flow_increase ; } (max_flow , flows) }
};
}
