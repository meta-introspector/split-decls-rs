// Generated macro for kosaraju_scc (function)
macro_rules! Depcrate_algo_scc_kosaraju_scckosaraju_scc {
() => {
// Module: crate::algo::scc::kosaraju_scc
// Provides: {"kosaraju_scc"}
// Dependencies: {}
# [doc = " Compute the *strongly connected components* using [Kosaraju's algorithm][1]."] # [doc = ""] # [doc = " This implementation is iterative and does two passes over the nodes."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `g`: a directed or undirected graph."] # [doc = ""] # [doc = " # Returns"] # [doc = " Return a vector where each element is a strongly connected component (scc)."] # [doc = " The order of node ids within each scc is arbitrary, but the order of"] # [doc = " the sccs is their postorder (reverse topological sort)."] # [doc = ""] # [doc = " For an undirected graph, the sccs are simply the connected components."] # [doc = ""] # [doc = " # Complexity"] # [doc = " * Time complexity: **O(|V| + |E|)**."] # [doc = " * Auxiliary space: **O(|V|)**."] # [doc = ""] # [doc = " where **|V|** is the number of nodes and **|E|** is the number of edges."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use petgraph::Graph;"] # [doc = " use petgraph::algo::kosaraju_scc;"] # [doc = " use petgraph::prelude::*;"] # [doc = ""] # [doc = " let mut graph: Graph<i32, (), Directed> = Graph::new();"] # [doc = " let a = graph.add_node(1);"] # [doc = " let b = graph.add_node(2);"] # [doc = " let c = graph.add_node(3);"] # [doc = " let d = graph.add_node(4);"] # [doc = " let e = graph.add_node(5);"] # [doc = " let f = graph.add_node(6);"] # [doc = ""] # [doc = " graph.extend_with_edges(&["] # [doc = "     (a, b), (b, c), (c, a),  // First SCC: a -> b -> c -> a"] # [doc = "     (d, e), (e, f), (f, d),  // Second SCC: d -> e -> f -> d"] # [doc = "     (c, d),                  // Connection between SCCs"] # [doc = " ]);"] # [doc = ""] # [doc = " // Graph structure:"] # [doc = " // a ---> b       e ---> f"] # [doc = " // ↑     ↓       ↑      |"] # [doc = " // └---- c --->  d <----┘"] # [doc = ""] # [doc = " let sccs = kosaraju_scc(&graph);"] # [doc = " assert_eq!(sccs.len(), 2); // Two strongly connected components"] # [doc = ""] # [doc = " // Each SCC contains 3 nodes"] # [doc = " assert_eq!(sccs[0].len(), 3);"] # [doc = " assert_eq!(sccs[1].len(), 3);"] # [doc = " ```"] # [doc = ""] # [doc = " For a simple directed acyclic graph (DAG):"] # [doc = ""] # [doc = " ```rust"] # [doc = " use petgraph::Graph;"] # [doc = " use petgraph::algo::kosaraju_scc;"] # [doc = " use petgraph::prelude::*;"] # [doc = ""] # [doc = " let mut dag: Graph<&str, (), Directed> = Graph::new();"] # [doc = " let a = dag.add_node(\"A\");"] # [doc = " let b = dag.add_node(\"B\");"] # [doc = " let c = dag.add_node(\"C\");"] # [doc = ""] # [doc = " dag.extend_with_edges(&[(a, b), (b, c)]);"] # [doc = " // A -> B -> C"] # [doc = ""] # [doc = " let sccs = kosaraju_scc(&dag);"] # [doc = " assert_eq!(sccs.len(), 3); // Each node is its own SCC"] # [doc = ""] # [doc = " // Each SCC contains exactly one node"] # [doc = " for scc in &sccs {"] # [doc = "     assert_eq!(scc.len(), 1);"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [1]: https://en.wikipedia.org/wiki/Kosaraju%27s_algorithm"] pub fn kosaraju_scc < G > (g : G) -> Vec < Vec < G :: NodeId > > where G : IntoNeighborsDirected + Visitable + IntoNodeIdentifiers , { let mut dfs = DfsPostOrder :: empty (g) ; let mut finish_order = Vec :: with_capacity (0) ; for i in g . node_identifiers () { if dfs . discovered . is_visited (& i) { continue ; } dfs . move_to (i) ; while let Some (nx) = dfs . next (Reversed (g)) { finish_order . push (nx) ; } } let mut dfs = Dfs :: from_parts (dfs . stack , dfs . discovered) ; dfs . reset (g) ; let mut sccs = Vec :: new () ; for i in finish_order . into_iter () . rev () { if dfs . discovered . is_visited (& i) { continue ; } dfs . move_to (i) ; let mut scc = Vec :: new () ; while let Some (nx) = dfs . next (g) { scc . push (nx) ; } sccs . push (scc) ; } sccs }
};
}
