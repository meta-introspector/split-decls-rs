// Generated macro for depth_first_search (function)
macro_rules! Depcrate_visit_dfsvisitdepth_first_search {
() => {
// Module: crate::visit::dfsvisit
// Provides: {"depth_first_search"}
// Dependencies: {}
# [doc = " A recursive depth first search."] # [doc = ""] # [doc = " Starting points are the nodes in the iterator `starts` (specify just one"] # [doc = " start node *x* by using `Some(x)`)."] # [doc = ""] # [doc = " The traversal emits discovery and finish events for each reachable node,"] # [doc = " and edge classification of each reachable edge. `visitor` is called for each"] # [doc = " event, see [`DfsEvent`][de] for possible values."] # [doc = ""] # [doc = " The return value should implement the trait `ControlFlow`, and can be used to change"] # [doc = " the control flow of the search."] # [doc = ""] # [doc = " `Control` Implements `ControlFlow` such that `Control::Continue` resumes the search."] # [doc = " `Control::Break` will stop the visit early, returning the contained value."] # [doc = " `Control::Prune` will stop traversing any additional edges from the current"] # [doc = " node and proceed immediately to the `Finish` event."] # [doc = ""] # [doc = " There are implementations of `ControlFlow` for `()`, and `Result<C, E>` where"] # [doc = " `C: ControlFlow`. The implementation for `()` will continue until finished."] # [doc = " For `Result`, upon encountering an `E` it will break, otherwise acting the same as `C`."] # [doc = ""] # [doc = " **Panics** if you attempt to prune a node from its `Finish` event."] # [doc = ""] # [doc = " [de]: enum.DfsEvent.html"] # [doc = ""] # [doc = " # Example returning `Control`."] # [doc = ""] # [doc = " Find a path from node 0 to 5, and exit the visit as soon as we reach"] # [doc = " the goal node."] # [doc = ""] # [doc = " ```"] # [doc = " use petgraph::prelude::*;"] # [doc = " use petgraph::graph::node_index as n;"] # [doc = " use petgraph::visit::depth_first_search;"] # [doc = " use petgraph::visit::{DfsEvent, Control};"] # [doc = ""] # [doc = " let gr: Graph<(), ()> = Graph::from_edges(&["] # [doc = "     (0, 1), (0, 2), (0, 3),"] # [doc = "     (1, 3),"] # [doc = "     (2, 3), (2, 4),"] # [doc = "     (4, 0), (4, 5),"] # [doc = " ]);"] # [doc = ""] # [doc = " // record each predecessor, mapping node → node"] # [doc = " let mut predecessor = vec![NodeIndex::end(); gr.node_count()];"] # [doc = " let start = n(0);"] # [doc = " let goal = n(5);"] # [doc = " depth_first_search(&gr, Some(start), |event| {"] # [doc = "     if let DfsEvent::TreeEdge(u, v) = event {"] # [doc = "         predecessor[v.index()] = u;"] # [doc = "         if v == goal {"] # [doc = "             return Control::Break(v);"] # [doc = "         }"] # [doc = "     }"] # [doc = "     Control::Continue"] # [doc = " });"] # [doc = ""] # [doc = " let mut next = goal;"] # [doc = " let mut path = vec![next];"] # [doc = " while next != start {"] # [doc = "     let pred = predecessor[next.index()];"] # [doc = "     path.push(pred);"] # [doc = "     next = pred;"] # [doc = " }"] # [doc = " path.reverse();"] # [doc = " assert_eq!(&path, &[n(0), n(2), n(4), n(5)]);"] # [doc = " ```"] # [doc = ""] # [doc = " # Example returning a `Result`."] # [doc = " ```"] # [doc = " use petgraph::graph::node_index as n;"] # [doc = " use petgraph::prelude::*;"] # [doc = " use petgraph::visit::depth_first_search;"] # [doc = " use petgraph::visit::{DfsEvent, Time};"] # [doc = ""] # [doc = " let gr: Graph<(), ()> = Graph::from_edges(&[(0, 1), (1, 2), (1, 1), (2, 1)]);"] # [doc = " let start = n(0);"] # [doc = " let mut back_edges = 0;"] # [doc = " let mut discover_time = 0;"] # [doc = " // Stop the search, the first time a BackEdge is encountered."] # [doc = " let result = depth_first_search(&gr, Some(start), |event| {"] # [doc = "     match event {"] # [doc = "         // In the cases where Ok(()) is returned,"] # [doc = "         // Result falls back to the implementation of Control on the value ()."] # [doc = "         // In the case of (), this is to always return Control::Continue."] # [doc = "         // continuing the search."] # [doc = "         DfsEvent::Discover(_, Time(t)) => {"] # [doc = "             discover_time = t;"] # [doc = "             Ok(())"] # [doc = "         }"] # [doc = "         DfsEvent::BackEdge(_, _) => {"] # [doc = "             back_edges += 1;"] # [doc = "             // the implementation of ControlFlow for Result,"] # [doc = "             // treats this Err value as Continue::Break"] # [doc = "             Err(event)"] # [doc = "         }"] # [doc = "         _ => Ok(()),"] # [doc = "     }"] # [doc = " });"] # [doc = ""] # [doc = " // Even though the graph has more than one cycle,"] # [doc = " // The number of back_edges visited by the search should always be 1."] # [doc = " assert_eq!(back_edges, 1);"] # [doc = " println!(\"discover time:{:?}\", discover_time);"] # [doc = " println!(\"number of backedges encountered: {}\", back_edges);"] # [doc = " println!(\"back edge: {:?}\", result);"] # [doc = " ```"] # [track_caller] pub fn depth_first_search < G , I , F , C > (graph : G , starts : I , mut visitor : F) -> C where G : IntoNeighbors + Visitable , I : IntoIterator < Item = G :: NodeId > , F : FnMut (DfsEvent < G :: NodeId >) -> C , C : ControlFlow , { let time = & mut Time (0) ; let discovered = & mut graph . visit_map () ; let finished = & mut graph . visit_map () ; for start in starts { try_control ! (dfs_visitor (graph , start , & mut visitor , discovered , finished , time) , unreachable ! ()) ; } C :: continuing () }
};
}
