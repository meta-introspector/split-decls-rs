macro_rules! deps {
    () => {
        UnionFind!();
    };
}

macro_rules! impl_1066 {
    () => {
        deps!();
        impl < K > UnionFind < K > { # [doc = " Constructs a new, empty `UnionFind<K>` with at least the specified capacity."] # [doc = ""] # [doc = " This acts similarly to [`Vec::with_capacity`]."] pub fn with_capacity (capacity : usize) -> Self { UnionFind { parent : Vec :: with_capacity (capacity) , rank : Vec :: with_capacity (capacity) , } } # [doc = " Returns the total number of elements the `UnionFind` can hold without reallocating."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use petgraph::unionfind::UnionFind;"] # [doc = ""] # [doc = " let mut uf: UnionFind<u32> = UnionFind::with_capacity(10);"] # [doc = " uf.new_set();"] # [doc = " assert!(uf.capacity() >= 10);"] # [doc = " ```"] # [inline] pub fn capacity (& self) -> usize { self . parent . capacity () . min (self . rank . capacity ()) } # [doc = " Reserves capacity for at least `additional` more elements to be inserted"] # [doc = " in the given `UnionFind<K>`. The collection may reserve more space to"] # [doc = " speculatively avoid frequent reallocations. After calling `reserve`,"] # [doc = " capacity will be greater than or equal to `self.len() + additional`."] # [doc = " Does nothing if capacity is already sufficient."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` _bytes_."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use petgraph::unionfind::UnionFind;"] # [doc = ""] # [doc = " let mut uf: UnionFind<u32> = UnionFind::new(3);"] # [doc = " uf.reserve(10);"] # [doc = " assert!(uf.capacity() >= 13);"] # [doc = " ```"] # [inline] pub fn reserve (& mut self , additional : usize) { self . parent . reserve (additional) ; self . rank . reserve (additional) ; } # [doc = " Reserves the minimum capacity for at least `additional` more elements to"] # [doc = " be inserted in the given `UnionFind<K>`. Unlike [`reserve`], this will not"] # [doc = " deliberately over-allocate to speculatively avoid frequent allocations."] # [doc = " After calling `reserve_exact`, capacity will be greater than or equal to"] # [doc = " `self.len() + additional`. Does nothing if the capacity is already"] # [doc = " sufficient."] # [doc = ""] # [doc = " Note that the allocator may give the collection more space than it"] # [doc = " requests. Therefore, capacity can not be relied upon to be precisely"] # [doc = " minimal. Prefer [`reserve`] if future insertions are expected."] # [doc = ""] # [doc = " [`reserve`]: UnionFind::reserve"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` _bytes_."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use petgraph::unionfind::UnionFind;"] # [doc = ""] # [doc = " let mut uf: UnionFind<u32> =  UnionFind::new_empty();"] # [doc = " uf.reserve_exact(10);"] # [doc = " assert!(uf.capacity() >= 10);"] # [doc = " ```"] # [inline] pub fn reserve_exact (& mut self , additional : usize) { self . parent . reserve_exact (additional) ; self . rank . reserve_exact (additional) ; } # [doc = " Tries to reserve capacity for at least `additional` more elements to be inserted"] # [doc = " in the given `UnionFind<K>`. The collection may reserve more space to speculatively avoid"] # [doc = " frequent reallocations. After calling `try_reserve`, capacity will be"] # [doc = " greater than or equal to `self.len() + additional` if it returns"] # [doc = " `Ok(())`. Does nothing if capacity is already sufficient. This method"] # [doc = " preserves the contents even if an error occurs."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the capacity overflows, or the allocator reports a failure, then an error"] # [doc = " is returned."] # [inline] pub fn try_reserve (& mut self , additional : usize) -> Result < () , TryReserveError > { self . parent . try_reserve (additional) . and_then (| _ | self . rank . try_reserve (additional)) } # [doc = " Tries to reserve the minimum capacity for at least `additional`"] # [doc = " elements to be inserted in the given `UnionFind<K>`. Unlike [`try_reserve`],"] # [doc = " this will not deliberately over-allocate to speculatively avoid frequent"] # [doc = " allocations. After calling `try_reserve_exact`, capacity will be greater"] # [doc = " than or equal to `self.len() + additional` if it returns `Ok(())`."] # [doc = " Does nothing if the capacity is already sufficient."] # [doc = ""] # [doc = " Note that the allocator may give the collection more space than it"] # [doc = " requests. Therefore, capacity can not be relied upon to be precisely"] # [doc = " minimal. Prefer [`try_reserve`] if future insertions are expected."] # [doc = ""] # [doc = " [`try_reserve`]: UnionFind::try_reserve"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the capacity overflows, or the allocator reports a failure, then an error"] # [doc = " is returned."] # [inline] pub fn try_reserve_exact (& mut self , additional : usize) -> Result < () , TryReserveError > { self . parent . try_reserve_exact (additional) . and_then (| _ | self . rank . try_reserve_exact (additional)) } # [doc = " Shrinks the capacity of the `UnionFind` as much as possible."] # [doc = ""] # [doc = " The behavior of this method depends on the allocator, which may either shrink the"] # [doc = " collection in-place or reallocate. The resulting `UnionFind` might still have some excess capacity, just as"] # [doc = " is the case for [`with_capacity`]. See [`Vec::shrink_to_fit`] for more details, since the implementation is based on this method."] # [doc = ""] # [doc = " [`with_capacity`]: UnionFind::with_capacity"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use petgraph::unionfind::UnionFind;"] # [doc = ""] # [doc = " let mut uf: UnionFind<u32> = UnionFind::with_capacity(10);"] # [doc = ""] # [doc = " for _ in 0..3 {"] # [doc = "     uf.new_set();"] # [doc = " }"] # [doc = ""] # [doc = " assert!(uf.capacity() >= 10);"] # [doc = " uf.shrink_to_fit();"] # [doc = " assert!(uf.capacity() >= 3);"] # [doc = " ```"] # [inline] pub fn shrink_to_fit (& mut self) { self . parent . shrink_to_fit () ; self . rank . shrink_to_fit () ; } # [doc = " Shrinks the capacity of the `UnionFind` with a lower bound."] # [doc = ""] # [doc = " The capacity will remain at least as large as both the length"] # [doc = " and the supplied value."] # [doc = ""] # [doc = " If the current capacity is less than the lower limit, this is a no-op."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use petgraph::unionfind::UnionFind;"] # [doc = ""] # [doc = " let mut uf: UnionFind<u32> = UnionFind::with_capacity(10);"] # [doc = ""] # [doc = " for _ in 0..3 {"] # [doc = "     uf.new_set();"] # [doc = " }"] # [doc = ""] # [doc = " assert!(uf.capacity() >= 10);"] # [doc = " uf.shrink_to(4);"] # [doc = " assert!(uf.capacity() >= 4);"] # [doc = " uf.shrink_to(0);"] # [doc = " assert!(uf.capacity() >= 3);"] # [doc = " ```"] # [inline] pub fn shrink_to (& mut self , min_capacity : usize) { self . parent . shrink_to (min_capacity) ; self . rank . shrink_to (min_capacity) ; } }
    };
}

impl_1066!();