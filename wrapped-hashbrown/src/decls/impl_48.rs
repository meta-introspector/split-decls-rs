macro_rules! deps {
    () => {
        RawTableInner!();
        RawTable!();
        Bucket!();
    };
}

macro_rules! impl_48 {
    () => {
        deps!();
        impl < T > Bucket < T > { # [doc = " Creates a [`Bucket`] that contain pointer to the data."] # [doc = " The pointer calculation is performed by calculating the"] # [doc = " offset from given `base` pointer (convenience for"] # [doc = " `base.as_ptr().sub(index)`)."] # [doc = ""] # [doc = " `index` is in units of `T`; e.g., an `index` of 3 represents a pointer"] # [doc = " offset of `3 * size_of::<T>()` bytes."] # [doc = ""] # [doc = " If the `T` is a ZST, then we instead track the index of the element"] # [doc = " in the table so that `erase` works properly (return"] # [doc = " `NonNull::new_unchecked((index + 1) as *mut T)`)"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If `mem::size_of::<T>() != 0`, then the safety rules are directly derived"] # [doc = " from the safety rules for [`<*mut T>::sub`] method of `*mut T` and the safety"] # [doc = " rules of [`NonNull::new_unchecked`] function."] # [doc = ""] # [doc = " Thus, in order to uphold the safety contracts for the [`<*mut T>::sub`] method"] # [doc = " and [`NonNull::new_unchecked`] function, as well as for the correct"] # [doc = " logic of the work of this crate, the following rules are necessary and"] # [doc = " sufficient:"] # [doc = ""] # [doc = " * the `base` pointer must not be `dangling` and must points to the"] # [doc = "   end of the first `value element` from the `data part` of the table, i.e."] # [doc = "   must be the pointer that returned by [`RawTable::data_end`] or by"] # [doc = "   [`RawTableInner::data_end<T>`];"] # [doc = ""] # [doc = " * `index` must not be greater than `RawTableInner.bucket_mask`, i.e."] # [doc = "   `index <= RawTableInner.bucket_mask` or, in other words, `(index + 1)`"] # [doc = "   must be no greater than the number returned by the function"] # [doc = "   [`RawTable::buckets`] or [`RawTableInner::buckets`]."] # [doc = ""] # [doc = " If `mem::size_of::<T>() == 0`, then the only requirement is that the"] # [doc = " `index` must not be greater than `RawTableInner.bucket_mask`, i.e."] # [doc = " `index <= RawTableInner.bucket_mask` or, in other words, `(index + 1)`"] # [doc = " must be no greater than the number returned by the function"] # [doc = " [`RawTable::buckets`] or [`RawTableInner::buckets`]."] # [doc = ""] # [doc = " [`Bucket`]: crate::raw::Bucket"] # [doc = " [`<*mut T>::sub`]: https://doc.rust-lang.org/core/primitive.pointer.html#method.sub-1"] # [doc = " [`NonNull::new_unchecked`]: https://doc.rust-lang.org/stable/std/ptr/struct.NonNull.html#method.new_unchecked"] # [doc = " [`RawTable::data_end`]: crate::raw::RawTable::data_end"] # [doc = " [`RawTableInner::data_end<T>`]: RawTableInner::data_end<T>"] # [doc = " [`RawTable::buckets`]: crate::raw::RawTable::buckets"] # [doc = " [`RawTableInner::buckets`]: RawTableInner::buckets"] # [inline] unsafe fn from_base_index (base : NonNull < T > , index : usize) -> Self { let ptr = if T :: IS_ZERO_SIZED { invalid_mut (index + 1) } else { base . as_ptr () . sub (index) } ; Self { ptr : NonNull :: new_unchecked (ptr) , } } # [doc = " Calculates the index of a [`Bucket`] as distance between two pointers"] # [doc = " (convenience for `base.as_ptr().offset_from(self.ptr.as_ptr()) as usize`)."] # [doc = " The returned value is in units of T: the distance in bytes divided by"] # [doc = " [`core::mem::size_of::<T>()`]."] # [doc = ""] # [doc = " If the `T` is a ZST, then we return the index of the element in"] # [doc = " the table so that `erase` works properly (return `self.ptr.as_ptr() as usize - 1`)."] # [doc = ""] # [doc = " This function is the inverse of [`from_base_index`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If `mem::size_of::<T>() != 0`, then the safety rules are directly derived"] # [doc = " from the safety rules for [`<*const T>::offset_from`] method of `*const T`."] # [doc = ""] # [doc = " Thus, in order to uphold the safety contracts for [`<*const T>::offset_from`]"] # [doc = " method, as well as for the correct logic of the work of this crate, the"] # [doc = " following rules are necessary and sufficient:"] # [doc = ""] # [doc = " * `base` contained pointer must not be `dangling` and must point to the"] # [doc = "   end of the first `element` from the `data part` of the table, i.e."] # [doc = "   must be a pointer that returns by [`RawTable::data_end`] or by"] # [doc = "   [`RawTableInner::data_end<T>`];"] # [doc = ""] # [doc = " * `self` also must not contain dangling pointer;"] # [doc = ""] # [doc = " * both `self` and `base` must be created from the same [`RawTable`]"] # [doc = "   (or [`RawTableInner`])."] # [doc = ""] # [doc = " If `mem::size_of::<T>() == 0`, this function is always safe."] # [doc = ""] # [doc = " [`Bucket`]: crate::raw::Bucket"] # [doc = " [`from_base_index`]: crate::raw::Bucket::from_base_index"] # [doc = " [`RawTable::data_end`]: crate::raw::RawTable::data_end"] # [doc = " [`RawTableInner::data_end<T>`]: RawTableInner::data_end<T>"] # [doc = " [`RawTable`]: crate::raw::RawTable"] # [doc = " [`RawTableInner`]: RawTableInner"] # [doc = " [`<*const T>::offset_from`]: https://doc.rust-lang.org/nightly/core/primitive.pointer.html#method.offset_from"] # [inline] unsafe fn to_base_index (& self , base : NonNull < T >) -> usize { if T :: IS_ZERO_SIZED { self . ptr . as_ptr () as usize - 1 } else { offset_from (base . as_ptr () , self . ptr . as_ptr ()) } } # [doc = " Acquires the underlying raw pointer `*mut T` to `data`."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " If `T` is not [`Copy`], do not use `*mut T` methods that can cause calling the"] # [doc = " destructor of `T` (for example the [`<*mut T>::drop_in_place`] method), because"] # [doc = " for properly dropping the data we also need to clear `data` control bytes. If we"] # [doc = " drop data, but do not clear `data control byte` it leads to double drop when"] # [doc = " [`RawTable`] goes out of scope."] # [doc = ""] # [doc = " If you modify an already initialized `value`, so [`Hash`] and [`Eq`] on the new"] # [doc = " `T` value and its borrowed form *must* match those for the old `T` value, as the map"] # [doc = " will not re-evaluate where the new value should go, meaning the value may become"] # [doc = " \"lost\" if their location does not reflect their state."] # [doc = ""] # [doc = " [`RawTable`]: crate::raw::RawTable"] # [doc = " [`<*mut T>::drop_in_place`]: https://doc.rust-lang.org/core/primitive.pointer.html#method.drop_in_place"] # [doc = " [`Hash`]: https://doc.rust-lang.org/core/hash/trait.Hash.html"] # [doc = " [`Eq`]: https://doc.rust-lang.org/core/cmp/trait.Eq.html"] # [inline] pub fn as_ptr (& self) -> * mut T { if T :: IS_ZERO_SIZED { invalid_mut (mem :: align_of :: < T > ()) } else { unsafe { self . ptr . as_ptr () . sub (1) } } } # [doc = " Acquires the underlying non-null pointer `*mut T` to `data`."] # [inline] fn as_non_null (& self) -> NonNull < T > { unsafe { NonNull :: new_unchecked (self . as_ptr ()) } } # [doc = " Create a new [`Bucket`] that is offset from the `self` by the given"] # [doc = " `offset`. The pointer calculation is performed by calculating the"] # [doc = " offset from `self` pointer (convenience for `self.ptr.as_ptr().sub(offset)`)."] # [doc = " This function is used for iterators."] # [doc = ""] # [doc = " `offset` is in units of `T`; e.g., a `offset` of 3 represents a pointer"] # [doc = " offset of `3 * size_of::<T>()` bytes."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If `mem::size_of::<T>() != 0`, then the safety rules are directly derived"] # [doc = " from the safety rules for [`<*mut T>::sub`] method of `*mut T` and safety"] # [doc = " rules of [`NonNull::new_unchecked`] function."] # [doc = ""] # [doc = " Thus, in order to uphold the safety contracts for [`<*mut T>::sub`] method"] # [doc = " and [`NonNull::new_unchecked`] function, as well as for the correct"] # [doc = " logic of the work of this crate, the following rules are necessary and"] # [doc = " sufficient:"] # [doc = ""] # [doc = " * `self` contained pointer must not be `dangling`;"] # [doc = ""] # [doc = " * `self.to_base_index() + offset` must not be greater than `RawTableInner.bucket_mask`,"] # [doc = "   i.e. `(self.to_base_index() + offset) <= RawTableInner.bucket_mask` or, in other"] # [doc = "   words, `self.to_base_index() + offset + 1` must be no greater than the number returned"] # [doc = "   by the function [`RawTable::buckets`] or [`RawTableInner::buckets`]."] # [doc = ""] # [doc = " If `mem::size_of::<T>() == 0`, then the only requirement is that the"] # [doc = " `self.to_base_index() + offset` must not be greater than `RawTableInner.bucket_mask`,"] # [doc = " i.e. `(self.to_base_index() + offset) <= RawTableInner.bucket_mask` or, in other words,"] # [doc = " `self.to_base_index() + offset + 1` must be no greater than the number returned by the"] # [doc = " function [`RawTable::buckets`] or [`RawTableInner::buckets`]."] # [doc = ""] # [doc = " [`Bucket`]: crate::raw::Bucket"] # [doc = " [`<*mut T>::sub`]: https://doc.rust-lang.org/core/primitive.pointer.html#method.sub-1"] # [doc = " [`NonNull::new_unchecked`]: https://doc.rust-lang.org/stable/std/ptr/struct.NonNull.html#method.new_unchecked"] # [doc = " [`RawTable::buckets`]: crate::raw::RawTable::buckets"] # [doc = " [`RawTableInner::buckets`]: RawTableInner::buckets"] # [inline] unsafe fn next_n (& self , offset : usize) -> Self { let ptr = if T :: IS_ZERO_SIZED { invalid_mut (self . ptr . as_ptr () as usize + offset) } else { self . ptr . as_ptr () . sub (offset) } ; Self { ptr : NonNull :: new_unchecked (ptr) , } } # [doc = " Executes the destructor (if any) of the pointed-to `data`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " See [`ptr::drop_in_place`] for safety concerns."] # [doc = ""] # [doc = " You should use [`RawTable::erase`] instead of this function,"] # [doc = " or be careful with calling this function directly, because for"] # [doc = " properly dropping the data we need also clear `data` control bytes."] # [doc = " If we drop data, but do not erase `data control byte` it leads to"] # [doc = " double drop when [`RawTable`] goes out of scope."] # [doc = ""] # [doc = " [`ptr::drop_in_place`]: https://doc.rust-lang.org/core/ptr/fn.drop_in_place.html"] # [doc = " [`RawTable`]: crate::raw::RawTable"] # [doc = " [`RawTable::erase`]: crate::raw::RawTable::erase"] # [cfg_attr (feature = "inline-more" , inline)] pub (crate) unsafe fn drop (& self) { self . as_ptr () . drop_in_place () ; } # [doc = " Reads the `value` from `self` without moving it. This leaves the"] # [doc = " memory in `self` unchanged."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " See [`ptr::read`] for safety concerns."] # [doc = ""] # [doc = " You should use [`RawTable::remove`] instead of this function,"] # [doc = " or be careful with calling this function directly, because compiler"] # [doc = " calls its destructor when the read `value` goes out of scope. It"] # [doc = " can cause double dropping when [`RawTable`] goes out of scope,"] # [doc = " because of not erased `data control byte`."] # [doc = ""] # [doc = " [`ptr::read`]: https://doc.rust-lang.org/core/ptr/fn.read.html"] # [doc = " [`RawTable`]: crate::raw::RawTable"] # [doc = " [`RawTable::remove`]: crate::raw::RawTable::remove"] # [inline] pub (crate) unsafe fn read (& self) -> T { self . as_ptr () . read () } # [doc = " Overwrites a memory location with the given `value` without reading"] # [doc = " or dropping the old value (like [`ptr::write`] function)."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " See [`ptr::write`] for safety concerns."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " [`Hash`] and [`Eq`] on the new `T` value and its borrowed form *must* match"] # [doc = " those for the old `T` value, as the map will not re-evaluate where the new"] # [doc = " value should go, meaning the value may become \"lost\" if their location"] # [doc = " does not reflect their state."] # [doc = ""] # [doc = " [`ptr::write`]: https://doc.rust-lang.org/core/ptr/fn.write.html"] # [doc = " [`Hash`]: https://doc.rust-lang.org/core/hash/trait.Hash.html"] # [doc = " [`Eq`]: https://doc.rust-lang.org/core/cmp/trait.Eq.html"] # [inline] pub (crate) unsafe fn write (& self , val : T) { self . as_ptr () . write (val) ; } # [doc = " Returns a shared immutable reference to the `value`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " See [`NonNull::as_ref`] for safety concerns."] # [doc = ""] # [doc = " [`NonNull::as_ref`]: https://doc.rust-lang.org/core/ptr/struct.NonNull.html#method.as_ref"] # [inline] pub unsafe fn as_ref < 'a > (& self) -> & 'a T { & * self . as_ptr () } # [doc = " Returns a unique mutable reference to the `value`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " See [`NonNull::as_mut`] for safety concerns."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " [`Hash`] and [`Eq`] on the new `T` value and its borrowed form *must* match"] # [doc = " those for the old `T` value, as the map will not re-evaluate where the new"] # [doc = " value should go, meaning the value may become \"lost\" if their location"] # [doc = " does not reflect their state."] # [doc = ""] # [doc = " [`NonNull::as_mut`]: https://doc.rust-lang.org/core/ptr/struct.NonNull.html#method.as_mut"] # [doc = " [`Hash`]: https://doc.rust-lang.org/core/hash/trait.Hash.html"] # [doc = " [`Eq`]: https://doc.rust-lang.org/core/cmp/trait.Eq.html"] # [inline] pub unsafe fn as_mut < 'a > (& self) -> & 'a mut T { & mut * self . as_ptr () } }
    };
}

impl_48!();