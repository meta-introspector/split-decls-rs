macro_rules! deps {
    () => {
        Fallibility!();
        TableLayout!();
        RawTable!();
        Tag!();
        Bucket!();
        ProbeSeq!();
        TryReserveError!();
        RawTableInner!();
        ScopeGuard!();
        RawIter!();
        FullBucketsIndices!();
        RawIterRange!();
    };
}

macro_rules! impl_58 {
    () => {
        deps!();
        impl RawTableInner { # [doc = " Allocates a new [`RawTableInner`] with the given number of buckets."] # [doc = " The control bytes and buckets are left uninitialized."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller of this function must ensure that the `buckets` is power of two"] # [doc = " and also initialize all control bytes of the length `self.bucket_mask + 1 +"] # [doc = " Group::WIDTH` with the [`Tag::EMPTY`] bytes."] # [doc = ""] # [doc = " See also [`Allocator`] API for other safety concerns."] # [doc = ""] # [doc = " [`Allocator`]: https://doc.rust-lang.org/alloc/alloc/trait.Allocator.html"] # [cfg_attr (feature = "inline-more" , inline)] unsafe fn new_uninitialized < A > (alloc : & A , table_layout : TableLayout , mut buckets : usize , fallibility : Fallibility ,) -> Result < Self , TryReserveError > where A : Allocator , { debug_assert ! (buckets . is_power_of_two ()) ; let (layout , mut ctrl_offset) = match table_layout . calculate_layout_for (buckets) { Some (lco) => lco , None => return Err (fallibility . capacity_overflow ()) , } ; let ptr : NonNull < u8 > = match do_alloc (alloc , layout) { Ok (block) => { if block . len () != layout . size () { let x = maximum_buckets_in (block . len () , table_layout , Group :: WIDTH) ; debug_assert ! (x >= buckets) ; let (oversized_layout , oversized_ctrl_offset) = match table_layout . calculate_layout_for (x) { Some (lco) => lco , None => unsafe { hint :: unreachable_unchecked () } , } ; debug_assert ! (oversized_layout . size () <= block . len ()) ; debug_assert ! (oversized_ctrl_offset >= ctrl_offset) ; ctrl_offset = oversized_ctrl_offset ; buckets = x ; } block . cast () } Err (_) => return Err (fallibility . alloc_err (layout)) , } ; let ctrl = NonNull :: new_unchecked (ptr . as_ptr () . add (ctrl_offset)) ; Ok (Self { ctrl , bucket_mask : buckets - 1 , items : 0 , growth_left : bucket_mask_to_capacity (buckets - 1) , }) } # [doc = " Attempts to allocate a new [`RawTableInner`] with at least enough"] # [doc = " capacity for inserting the given number of elements without reallocating."] # [doc = ""] # [doc = " All the control bytes are initialized with the [`Tag::EMPTY`] bytes."] # [inline] fn fallible_with_capacity < A > (alloc : & A , table_layout : TableLayout , capacity : usize , fallibility : Fallibility ,) -> Result < Self , TryReserveError > where A : Allocator , { if capacity == 0 { Ok (Self :: NEW) } else { unsafe { let buckets = capacity_to_buckets (capacity , table_layout) . ok_or_else (| | fallibility . capacity_overflow ()) ? ; let mut result = Self :: new_uninitialized (alloc , table_layout , buckets , fallibility) ? ; result . ctrl_slice () . fill_empty () ; Ok (result) } } } # [doc = " Allocates a new [`RawTableInner`] with at least enough capacity for inserting"] # [doc = " the given number of elements without reallocating."] # [doc = ""] # [doc = " Panics if the new capacity exceeds [`isize::MAX`] bytes and [`abort`] the program"] # [doc = " in case of allocation error. Use [`fallible_with_capacity`] instead if you want to"] # [doc = " handle memory allocation failure."] # [doc = ""] # [doc = " All the control bytes are initialized with the [`Tag::EMPTY`] bytes."] # [doc = ""] # [doc = " [`fallible_with_capacity`]: RawTableInner::fallible_with_capacity"] # [doc = " [`abort`]: https://doc.rust-lang.org/alloc/alloc/fn.handle_alloc_error.html"] fn with_capacity < A > (alloc : & A , table_layout : TableLayout , capacity : usize) -> Self where A : Allocator , { match Self :: fallible_with_capacity (alloc , table_layout , capacity , Fallibility :: Infallible) { Ok (table_inner) => table_inner , Err (_) => unsafe { hint :: unreachable_unchecked () } , } } # [doc = " Fixes up an insertion index returned by the [`RawTableInner::find_insert_index_in_group`] method."] # [doc = ""] # [doc = " In tables smaller than the group width (`self.buckets() < Group::WIDTH`), trailing control"] # [doc = " bytes outside the range of the table are filled with [`Tag::EMPTY`] entries. These will unfortunately"] # [doc = " trigger a match of [`RawTableInner::find_insert_index_in_group`] function. This is because"] # [doc = " the `Some(bit)` returned by `group.match_empty_or_deleted().lowest_set_bit()` after masking"] # [doc = " (`(probe_seq.pos + bit) & self.bucket_mask`) may point to a full bucket that is already occupied."] # [doc = " We detect this situation here and perform a second scan starting at the beginning of the table."] # [doc = " This second scan is guaranteed to find an empty slot (due to the load factor) before hitting the"] # [doc = " trailing control bytes (containing [`Tag::EMPTY`] bytes)."] # [doc = ""] # [doc = " If this function is called correctly, it is guaranteed to return an index of an empty or"] # [doc = " deleted bucket in the range `0..self.buckets()` (see `Warning` and `Safety`)."] # [doc = ""] # [doc = " # Warning"] # [doc = ""] # [doc = " The table must have at least 1 empty or deleted `bucket`, otherwise if the table is less than"] # [doc = " the group width (`self.buckets() < Group::WIDTH`) this function returns an index outside of the"] # [doc = " table indices range `0..self.buckets()` (`0..=self.bucket_mask`). Attempt to write data at that"] # [doc = " index will cause immediate [`undefined behavior`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The safety rules are directly derived from the safety rules for [`RawTableInner::ctrl`] method."] # [doc = " Thus, in order to uphold those safety contracts, as well as for the correct logic of the work"] # [doc = " of this crate, the following rules are necessary and sufficient:"] # [doc = ""] # [doc = " * The [`RawTableInner`] must have properly initialized control bytes otherwise calling this"] # [doc = "   function results in [`undefined behavior`]."] # [doc = ""] # [doc = " * This function must only be used on insertion indices found by [`RawTableInner::find_insert_index_in_group`]"] # [doc = "   (after the `find_insert_index_in_group` function, but before insertion into the table)."] # [doc = ""] # [doc = " * The `index` must not be greater than the `self.bucket_mask`, i.e. `(index + 1) <= self.buckets()`"] # [doc = "   (this one is provided by the [`RawTableInner::find_insert_index_in_group`] function)."] # [doc = ""] # [doc = " Calling this function with an index not provided by [`RawTableInner::find_insert_index_in_group`]"] # [doc = " may result in [`undefined behavior`] even if the index satisfies the safety rules of the"] # [doc = " [`RawTableInner::ctrl`] function (`index < self.bucket_mask + 1 + Group::WIDTH`)."] # [doc = ""] # [doc = " [`RawTableInner::ctrl`]: RawTableInner::ctrl"] # [doc = " [`RawTableInner::find_insert_index_in_group`]: RawTableInner::find_insert_index_in_group"] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] unsafe fn fix_insert_index (& self , mut index : usize) -> usize { if unlikely (self . is_bucket_full (index)) { debug_assert ! (self . bucket_mask < Group :: WIDTH) ; index = Group :: load_aligned (self . ctrl (0)) . match_empty_or_deleted () . lowest_set_bit () . unwrap_unchecked () ; } index } # [doc = " Finds the position to insert something in a group."] # [doc = ""] # [doc = " **This may have false positives and must be fixed up with `fix_insert_index`"] # [doc = " before it's used.**"] # [doc = ""] # [doc = " The function is guaranteed to return the index of an empty or deleted [`Bucket`]"] # [doc = " in the range `0..self.buckets()` (`0..=self.bucket_mask`)."] # [inline] fn find_insert_index_in_group (& self , group : & Group , probe_seq : & ProbeSeq) -> Option < usize > { let bit = group . match_empty_or_deleted () . lowest_set_bit () ; if likely (bit . is_some ()) { Some ((probe_seq . pos + bit . unwrap ()) & self . bucket_mask) } else { None } } # [doc = " Searches for an element in the table, or a potential slot where that element could"] # [doc = " be inserted (an empty or deleted [`Bucket`] index)."] # [doc = ""] # [doc = " This uses dynamic dispatch to reduce the amount of code generated, but that is"] # [doc = " eliminated by LLVM optimizations."] # [doc = ""] # [doc = " This function does not make any changes to the `data` part of the table, or any"] # [doc = " changes to the `items` or `growth_left` field of the table."] # [doc = ""] # [doc = " The table must have at least 1 empty or deleted `bucket`, otherwise, if the"] # [doc = " `eq: &mut dyn FnMut(usize) -> bool` function does not return `true`, this function"] # [doc = " will never return (will go into an infinite loop) for tables larger than the group"] # [doc = " width, or return an index outside of the table indices range if the table is less"] # [doc = " than the group width."] # [doc = ""] # [doc = " This function is guaranteed to provide the `eq: &mut dyn FnMut(usize) -> bool`"] # [doc = " function with only `FULL` buckets' indices and return the `index` of the found"] # [doc = " element (as `Ok(index)`). If the element is not found and there is at least 1"] # [doc = " empty or deleted [`Bucket`] in the table, the function is guaranteed to return"] # [doc = " an index in the range `0..self.buckets()`, but in any case, if this function"] # [doc = " returns `Err`, it will contain an index in the range `0..=self.buckets()`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The [`RawTableInner`] must have properly initialized control bytes otherwise calling"] # [doc = " this function results in [`undefined behavior`]."] # [doc = ""] # [doc = " Attempt to write data at the index returned by this function when the table is less than"] # [doc = " the group width and if there was not at least one empty or deleted bucket in the table"] # [doc = " will cause immediate [`undefined behavior`]. This is because in this case the function"] # [doc = " will return `self.bucket_mask + 1` as an index due to the trailing [`Tag::EMPTY`] control"] # [doc = " bytes outside the table range."] # [doc = ""] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] unsafe fn find_or_find_insert_index_inner (& self , hash : u64 , eq : & mut dyn FnMut (usize) -> bool ,) -> Result < usize , usize > { let mut insert_index = None ; let tag_hash = Tag :: full (hash) ; let mut probe_seq = self . probe_seq (hash) ; loop { let group = unsafe { Group :: load (self . ctrl (probe_seq . pos)) } ; for bit in group . match_tag (tag_hash) { let index = (probe_seq . pos + bit) & self . bucket_mask ; if likely (eq (index)) { return Ok (index) ; } } if likely (insert_index . is_none ()) { insert_index = self . find_insert_index_in_group (& group , & probe_seq) ; } if let Some (insert_index) = insert_index { if likely (group . match_empty () . any_bit_set ()) { unsafe { return Err (self . fix_insert_index (insert_index)) ; } } } probe_seq . move_next (self . bucket_mask) ; } } # [doc = " Searches for an empty or deleted bucket which is suitable for inserting a new"] # [doc = " element and sets the hash for that slot. Returns an index of that slot and the"] # [doc = " old control byte stored in the found index."] # [doc = ""] # [doc = " This function does not check if the given element exists in the table. Also,"] # [doc = " this function does not check if there is enough space in the table to insert"] # [doc = " a new element. The caller of the function must make sure that the table has at"] # [doc = " least 1 empty or deleted `bucket`, otherwise this function will never return"] # [doc = " (will go into an infinite loop) for tables larger than the group width, or"] # [doc = " return an index outside of the table indices range if the table is less than"] # [doc = " the group width."] # [doc = ""] # [doc = " If there is at least 1 empty or deleted `bucket` in the table, the function is"] # [doc = " guaranteed to return an `index` in the range `0..self.buckets()`, but in any case,"] # [doc = " if this function returns an `index` it will be in the range `0..=self.buckets()`."] # [doc = ""] # [doc = " This function does not make any changes to the `data` parts of the table,"] # [doc = " or any changes to the `items` or `growth_left` field of the table."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The safety rules are directly derived from the safety rules for the"] # [doc = " [`RawTableInner::set_ctrl_hash`] and [`RawTableInner::find_insert_index`] methods."] # [doc = " Thus, in order to uphold the safety contracts for that methods, as well as for"] # [doc = " the correct logic of the work of this crate, you must observe the following rules"] # [doc = " when calling this function:"] # [doc = ""] # [doc = " * The [`RawTableInner`] has already been allocated and has properly initialized"] # [doc = "   control bytes otherwise calling this function results in [`undefined behavior`]."] # [doc = ""] # [doc = " * The caller of this function must ensure that the \"data\" parts of the table"] # [doc = "   will have an entry in the returned index (matching the given hash) right"] # [doc = "   after calling this function."] # [doc = ""] # [doc = " Attempt to write data at the `index` returned by this function when the table is"] # [doc = " less than the group width and if there was not at least one empty or deleted bucket in"] # [doc = " the table will cause immediate [`undefined behavior`]. This is because in this case the"] # [doc = " function will return `self.bucket_mask + 1` as an index due to the trailing [`Tag::EMPTY`]"] # [doc = " control bytes outside the table range."] # [doc = ""] # [doc = " The caller must independently increase the `items` field of the table, and also,"] # [doc = " if the old control byte was [`Tag::EMPTY`], then decrease the table's `growth_left`"] # [doc = " field, and do not change it if the old control byte was [`Tag::DELETED`]."] # [doc = ""] # [doc = " See also [`Bucket::as_ptr`] method, for more information about of properly removing"] # [doc = " or saving `element` from / into the [`RawTable`] / [`RawTableInner`]."] # [doc = ""] # [doc = " [`Bucket::as_ptr`]: Bucket::as_ptr"] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [doc = " [`RawTableInner::ctrl`]: RawTableInner::ctrl"] # [doc = " [`RawTableInner::set_ctrl_hash`]: RawTableInner::set_ctrl_hash"] # [doc = " [`RawTableInner::find_insert_index`]: RawTableInner::find_insert_index"] # [inline] unsafe fn prepare_insert_index (& mut self , hash : u64) -> (usize , Tag) { let index : usize = self . find_insert_index (hash) ; let old_ctrl = * self . ctrl (index) ; self . set_ctrl_hash (index , hash) ; (index , old_ctrl) } # [doc = " Searches for an empty or deleted bucket which is suitable for inserting"] # [doc = " a new element, returning the `index` for the new [`Bucket`]."] # [doc = ""] # [doc = " This function does not make any changes to the `data` part of the table, or any"] # [doc = " changes to the `items` or `growth_left` field of the table."] # [doc = ""] # [doc = " The table must have at least 1 empty or deleted `bucket`, otherwise this function"] # [doc = " will never return (will go into an infinite loop) for tables larger than the group"] # [doc = " width, or return an index outside of the table indices range if the table is less"] # [doc = " than the group width."] # [doc = ""] # [doc = " If there is at least 1 empty or deleted `bucket` in the table, the function is"] # [doc = " guaranteed to return an index in the range `0..self.buckets()`, but in any case,"] # [doc = " it will contain an index in the range `0..=self.buckets()`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The [`RawTableInner`] must have properly initialized control bytes otherwise calling"] # [doc = " this function results in [`undefined behavior`]."] # [doc = ""] # [doc = " Attempt to write data at the index returned by this function when the table is"] # [doc = " less than the group width and if there was not at least one empty or deleted bucket in"] # [doc = " the table will cause immediate [`undefined behavior`]. This is because in this case the"] # [doc = " function will return `self.bucket_mask + 1` as an index due to the trailing [`Tag::EMPTY`]"] # [doc = " control bytes outside the table range."] # [doc = ""] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] unsafe fn find_insert_index (& self , hash : u64) -> usize { let mut probe_seq = self . probe_seq (hash) ; loop { let group = unsafe { Group :: load (self . ctrl (probe_seq . pos)) } ; let index = self . find_insert_index_in_group (& group , & probe_seq) ; if likely (index . is_some ()) { unsafe { return self . fix_insert_index (index . unwrap_unchecked ()) ; } } probe_seq . move_next (self . bucket_mask) ; } } # [doc = " Searches for an element in a table, returning the `index` of the found element."] # [doc = " This uses dynamic dispatch to reduce the amount of code generated, but it is"] # [doc = " eliminated by LLVM optimizations."] # [doc = ""] # [doc = " This function does not make any changes to the `data` part of the table, or any"] # [doc = " changes to the `items` or `growth_left` field of the table."] # [doc = ""] # [doc = " The table must have at least 1 empty `bucket`, otherwise, if the"] # [doc = " `eq: &mut dyn FnMut(usize) -> bool` function does not return `true`,"] # [doc = " this function will also never return (will go into an infinite loop)."] # [doc = ""] # [doc = " This function is guaranteed to provide the `eq: &mut dyn FnMut(usize) -> bool`"] # [doc = " function with only `FULL` buckets' indices and return the `index` of the found"] # [doc = " element as `Some(index)`, so the index will always be in the range"] # [doc = " `0..self.buckets()`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The [`RawTableInner`] must have properly initialized control bytes otherwise calling"] # [doc = " this function results in [`undefined behavior`]."] # [doc = ""] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline (always)] unsafe fn find_inner (& self , hash : u64 , eq : & mut dyn FnMut (usize) -> bool) -> Option < usize > { let tag_hash = Tag :: full (hash) ; let mut probe_seq = self . probe_seq (hash) ; loop { let group = unsafe { Group :: load (self . ctrl (probe_seq . pos)) } ; for bit in group . match_tag (tag_hash) { let index = (probe_seq . pos + bit) & self . bucket_mask ; if likely (eq (index)) { return Some (index) ; } } if likely (group . match_empty () . any_bit_set ()) { return None ; } probe_seq . move_next (self . bucket_mask) ; } } # [doc = " Prepares for rehashing data in place (that is, without allocating new memory)."] # [doc = " Converts all full index `control bytes` to `Tag::DELETED` and all `Tag::DELETED` control"] # [doc = " bytes to `Tag::EMPTY`, i.e. performs the following conversion:"] # [doc = ""] # [doc = " - `Tag::EMPTY` control bytes   -> `Tag::EMPTY`;"] # [doc = " - `Tag::DELETED` control bytes -> `Tag::EMPTY`;"] # [doc = " - `FULL` control bytes    -> `Tag::DELETED`."] # [doc = ""] # [doc = " This function does not make any changes to the `data` parts of the table,"] # [doc = " or any changes to the `items` or `growth_left` field of the table."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " You must observe the following safety rules when calling this function:"] # [doc = ""] # [doc = " * The [`RawTableInner`] has already been allocated;"] # [doc = ""] # [doc = " * The caller of this function must convert the `Tag::DELETED` bytes back to `FULL`"] # [doc = "   bytes when re-inserting them into their ideal position (which was impossible"] # [doc = "   to do during the first insert due to tombstones). If the caller does not do"] # [doc = "   this, then calling this function may result in a memory leak."] # [doc = ""] # [doc = " * The [`RawTableInner`] must have properly initialized control bytes otherwise"] # [doc = "   calling this function results in [`undefined behavior`]."] # [doc = ""] # [doc = " Calling this function on a table that has not been allocated results in"] # [doc = " [`undefined behavior`]."] # [doc = ""] # [doc = " See also [`Bucket::as_ptr`] method, for more information about of properly removing"] # [doc = " or saving `data element` from / into the [`RawTable`] / [`RawTableInner`]."] # [doc = ""] # [doc = " [`Bucket::as_ptr`]: Bucket::as_ptr"] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [allow (clippy :: mut_mut)] # [inline] unsafe fn prepare_rehash_in_place (& mut self) { for i in (0 .. self . buckets ()) . step_by (Group :: WIDTH) { let group = Group :: load_aligned (self . ctrl (i)) ; let group = group . convert_special_to_empty_and_full_to_deleted () ; group . store_aligned (self . ctrl (i)) ; } if unlikely (self . buckets () < Group :: WIDTH) { self . ctrl (0) . copy_to (self . ctrl (Group :: WIDTH) , self . buckets ()) ; } else { self . ctrl (0) . copy_to (self . ctrl (self . buckets ()) , Group :: WIDTH) ; } } # [doc = " Returns an iterator over every element in the table."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If any of the following conditions are violated, the result"] # [doc = " is [`undefined behavior`]:"] # [doc = ""] # [doc = " * The caller has to ensure that the `RawTableInner` outlives the"] # [doc = "   `RawIter`. Because we cannot make the `next` method unsafe on"] # [doc = "   the `RawIter` struct, we have to make the `iter` method unsafe."] # [doc = ""] # [doc = " * The [`RawTableInner`] must have properly initialized control bytes."] # [doc = ""] # [doc = " The type `T` must be the actual type of the elements stored in the table,"] # [doc = " otherwise using the returned [`RawIter`] results in [`undefined behavior`]."] # [doc = ""] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] unsafe fn iter < T > (& self) -> RawIter < T > { let data = Bucket :: from_base_index (self . data_end () , 0) ; RawIter { iter : RawIterRange :: new (self . ctrl . as_ptr () , data , self . buckets ()) , items : self . items , } } # [doc = " Executes the destructors (if any) of the values stored in the table."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " This function does not erase the control bytes of the table and does"] # [doc = " not make any changes to the `items` or `growth_left` fields of the"] # [doc = " table. If necessary, the caller of this function must manually set"] # [doc = " up these table fields, for example using the [`clear_no_drop`] function."] # [doc = ""] # [doc = " Be careful during calling this function, because drop function of"] # [doc = " the elements can panic, and this can leave table in an inconsistent"] # [doc = " state."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The type `T` must be the actual type of the elements stored in the table,"] # [doc = " otherwise calling this function may result in [`undefined behavior`]."] # [doc = ""] # [doc = " If `T` is a type that should be dropped and **the table is not empty**,"] # [doc = " calling this function more than once results in [`undefined behavior`]."] # [doc = ""] # [doc = " If `T` is not [`Copy`], attempting to use values stored in the table after"] # [doc = " calling this function may result in [`undefined behavior`]."] # [doc = ""] # [doc = " It is safe to call this function on a table that has not been allocated,"] # [doc = " on a table with uninitialized control bytes, and on a table with no actual"] # [doc = " data but with `Full` control bytes if `self.items == 0`."] # [doc = ""] # [doc = " See also [`Bucket::drop`] / [`Bucket::as_ptr`] methods, for more information"] # [doc = " about of properly removing or saving `element` from / into the [`RawTable`] /"] # [doc = " [`RawTableInner`]."] # [doc = ""] # [doc = " [`Bucket::drop`]: Bucket::drop"] # [doc = " [`Bucket::as_ptr`]: Bucket::as_ptr"] # [doc = " [`clear_no_drop`]: RawTableInner::clear_no_drop"] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] unsafe fn drop_elements < T > (& mut self) { if T :: NEEDS_DROP && self . items != 0 { for item in self . iter :: < T > () { item . drop () ; } } } # [doc = " Executes the destructors (if any) of the values stored in the table and than"] # [doc = " deallocates the table."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " Calling this function automatically makes invalid (dangling) all instances of"] # [doc = " buckets ([`Bucket`]) and makes invalid (dangling) the `ctrl` field of the table."] # [doc = ""] # [doc = " This function does not make any changes to the `bucket_mask`, `items` or `growth_left`"] # [doc = " fields of the table. If necessary, the caller of this function must manually set"] # [doc = " up these table fields."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If any of the following conditions are violated, the result is [`undefined behavior`]:"] # [doc = ""] # [doc = " * Calling this function more than once;"] # [doc = ""] # [doc = " * The type `T` must be the actual type of the elements stored in the table."] # [doc = ""] # [doc = " * The `alloc` must be the same [`Allocator`] as the `Allocator` that was used"] # [doc = "   to allocate this table."] # [doc = ""] # [doc = " * The `table_layout` must be the same [`TableLayout`] as the `TableLayout` that"] # [doc = "   was used to allocate this table."] # [doc = ""] # [doc = " The caller of this function should pay attention to the possibility of the"] # [doc = " elements' drop function panicking, because this:"] # [doc = ""] # [doc = "    * May leave the table in an inconsistent state;"] # [doc = ""] # [doc = "    * Memory is never deallocated, so a memory leak may occur."] # [doc = ""] # [doc = " Attempt to use the `ctrl` field of the table (dereference) after calling this"] # [doc = " function results in [`undefined behavior`]."] # [doc = ""] # [doc = " It is safe to call this function on a table that has not been allocated,"] # [doc = " on a table with uninitialized control bytes, and on a table with no actual"] # [doc = " data but with `Full` control bytes if `self.items == 0`."] # [doc = ""] # [doc = " See also [`RawTableInner::drop_elements`] or [`RawTableInner::free_buckets`]"] # [doc = " for more  information."] # [doc = ""] # [doc = " [`RawTableInner::drop_elements`]: RawTableInner::drop_elements"] # [doc = " [`RawTableInner::free_buckets`]: RawTableInner::free_buckets"] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] unsafe fn drop_inner_table < T , A : Allocator > (& mut self , alloc : & A , table_layout : TableLayout) { if ! self . is_empty_singleton () { unsafe { self . drop_elements :: < T > () ; self . free_buckets (alloc , table_layout) ; } } } # [doc = " Returns a pointer to an element in the table (convenience for"] # [doc = " `Bucket::from_base_index(self.data_end::<T>(), index)`)."] # [doc = ""] # [doc = " The caller must ensure that the `RawTableInner` outlives the returned [`Bucket<T>`],"] # [doc = " otherwise using it may result in [`undefined behavior`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If `mem::size_of::<T>() != 0`, then the safety rules are directly derived from the"] # [doc = " safety rules of the [`Bucket::from_base_index`] function. Therefore, when calling"] # [doc = " this function, the following safety rules must be observed:"] # [doc = ""] # [doc = " * The table must already be allocated;"] # [doc = ""] # [doc = " * The `index` must not be greater than the number returned by the [`RawTableInner::buckets`]"] # [doc = "   function, i.e. `(index + 1) <= self.buckets()`."] # [doc = ""] # [doc = " * The type `T` must be the actual type of the elements stored in the table, otherwise"] # [doc = "   using the returned [`Bucket`] may result in [`undefined behavior`]."] # [doc = ""] # [doc = " It is safe to call this function with index of zero (`index == 0`) on a table that has"] # [doc = " not been allocated, but using the returned [`Bucket`] results in [`undefined behavior`]."] # [doc = ""] # [doc = " If `mem::size_of::<T>() == 0`, then the only requirement is that the `index` must"] # [doc = " not be greater than the number returned by the [`RawTable::buckets`] function, i.e."] # [doc = " `(index + 1) <= self.buckets()`."] # [doc = ""] # [doc = " ```none"] # [doc = " If mem::size_of::<T>() != 0 then return a pointer to the `element` in the `data part` of the table"] # [doc = " (we start counting from \"0\", so that in the expression T[n], the \"n\" index actually one less than"] # [doc = " the \"buckets\" number of our `RawTableInner`, i.e. \"n = RawTableInner::buckets() - 1\"):"] # [doc = ""] # [doc = "           `table.bucket(3).as_ptr()` returns a pointer that points here in the `data`"] # [doc = "           part of the `RawTableInner`, i.e. to the start of T3 (see [`Bucket::as_ptr`])"] # [doc = "                  |"] # [doc = "                  |               `base = table.data_end::<T>()` points here"] # [doc = "                  |               (to the start of CT0 or to the end of T0)"] # [doc = "                  v                 v"] # [doc = " [Pad], T_n, ..., |T3|, T2, T1, T0, |CT0, CT1, CT2, CT3, ..., CT_n, CTa_0, CTa_1, ..., CTa_m"] # [doc = "                     ^                                              \\__________  __________/"] # [doc = "        `table.bucket(3)` returns a pointer that points                        \\/"] # [doc = "         here in the `data` part of the `RawTableInner`             additional control bytes"] # [doc = "         (to the end of T3)                                          `m = Group::WIDTH - 1`"] # [doc = ""] # [doc = " where: T0...T_n  - our stored data;"] # [doc = "        CT0...CT_n - control bytes or metadata for `data`;"] # [doc = "        CTa_0...CTa_m - additional control bytes (so that the search with loading `Group` bytes from"] # [doc = "                        the heap works properly, even if the result of `h1(hash) & self.bucket_mask`"] # [doc = "                        is equal to `self.bucket_mask`). See also `RawTableInner::set_ctrl` function."] # [doc = ""] # [doc = " P.S. `h1(hash) & self.bucket_mask` is the same as `hash as usize % self.buckets()` because the number"] # [doc = " of buckets is a power of two, and `self.bucket_mask = self.buckets() - 1`."] # [doc = " ```"] # [doc = ""] # [doc = " [`Bucket::from_base_index`]: Bucket::from_base_index"] # [doc = " [`RawTableInner::buckets`]: RawTableInner::buckets"] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] unsafe fn bucket < T > (& self , index : usize) -> Bucket < T > { debug_assert_ne ! (self . bucket_mask , 0) ; debug_assert ! (index < self . buckets ()) ; Bucket :: from_base_index (self . data_end () , index) } # [doc = " Returns a raw `*mut u8` pointer to the start of the `data` element in the table"] # [doc = " (convenience for `self.data_end::<u8>().as_ptr().sub((index + 1) * size_of)`)."] # [doc = ""] # [doc = " The caller must ensure that the `RawTableInner` outlives the returned `*mut u8`,"] # [doc = " otherwise using it may result in [`undefined behavior`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If any of the following conditions are violated, the result is [`undefined behavior`]:"] # [doc = ""] # [doc = " * The table must already be allocated;"] # [doc = ""] # [doc = " * The `index` must not be greater than the number returned by the [`RawTableInner::buckets`]"] # [doc = "   function, i.e. `(index + 1) <= self.buckets()`;"] # [doc = ""] # [doc = " * The `size_of` must be equal to the size of the elements stored in the table;"] # [doc = ""] # [doc = " ```none"] # [doc = " If mem::size_of::<T>() != 0 then return a pointer to the `element` in the `data part` of the table"] # [doc = " (we start counting from \"0\", so that in the expression T[n], the \"n\" index actually one less than"] # [doc = " the \"buckets\" number of our `RawTableInner`, i.e. \"n = RawTableInner::buckets() - 1\"):"] # [doc = ""] # [doc = "           `table.bucket_ptr(3, mem::size_of::<T>())` returns a pointer that points here in the"] # [doc = "           `data` part of the `RawTableInner`, i.e. to the start of T3"] # [doc = "                  |"] # [doc = "                  |               `base = table.data_end::<u8>()` points here"] # [doc = "                  |               (to the start of CT0 or to the end of T0)"] # [doc = "                  v                 v"] # [doc = " [Pad], T_n, ..., |T3|, T2, T1, T0, |CT0, CT1, CT2, CT3, ..., CT_n, CTa_0, CTa_1, ..., CTa_m"] # [doc = "                                                                    \\__________  __________/"] # [doc = "                                                                               \\/"] # [doc = "                                                                    additional control bytes"] # [doc = "                                                                     `m = Group::WIDTH - 1`"] # [doc = ""] # [doc = " where: T0...T_n  - our stored data;"] # [doc = "        CT0...CT_n - control bytes or metadata for `data`;"] # [doc = "        CTa_0...CTa_m - additional control bytes (so that the search with loading `Group` bytes from"] # [doc = "                        the heap works properly, even if the result of `h1(hash) & self.bucket_mask`"] # [doc = "                        is equal to `self.bucket_mask`). See also `RawTableInner::set_ctrl` function."] # [doc = ""] # [doc = " P.S. `h1(hash) & self.bucket_mask` is the same as `hash as usize % self.buckets()` because the number"] # [doc = " of buckets is a power of two, and `self.bucket_mask = self.buckets() - 1`."] # [doc = " ```"] # [doc = ""] # [doc = " [`RawTableInner::buckets`]: RawTableInner::buckets"] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] unsafe fn bucket_ptr (& self , index : usize , size_of : usize) -> * mut u8 { debug_assert_ne ! (self . bucket_mask , 0) ; debug_assert ! (index < self . buckets ()) ; let base : * mut u8 = self . data_end () . as_ptr () ; base . sub ((index + 1) * size_of) } # [doc = " Returns pointer to one past last `data` element in the table as viewed from"] # [doc = " the start point of the allocation (convenience for `self.ctrl.cast()`)."] # [doc = ""] # [doc = " This function actually returns a pointer to the end of the `data element` at"] # [doc = " index \"0\" (zero)."] # [doc = ""] # [doc = " The caller must ensure that the `RawTableInner` outlives the returned [`NonNull<T>`],"] # [doc = " otherwise using it may result in [`undefined behavior`]."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " The type `T` must be the actual type of the elements stored in the table, otherwise"] # [doc = " using the returned [`NonNull<T>`] may result in [`undefined behavior`]."] # [doc = ""] # [doc = " ```none"] # [doc = "                        `table.data_end::<T>()` returns pointer that points here"] # [doc = "                        (to the end of `T0`)"] # [doc = "                          ∨"] # [doc = " [Pad], T_n, ..., T1, T0, |CT0, CT1, ..., CT_n|, CTa_0, CTa_1, ..., CTa_m"] # [doc = "                           \\________  ________/"] # [doc = "                                    \\/"] # [doc = "       `n = buckets - 1`, i.e. `RawTableInner::buckets() - 1`"] # [doc = ""] # [doc = " where: T0...T_n  - our stored data;"] # [doc = "        CT0...CT_n - control bytes or metadata for `data`."] # [doc = "        CTa_0...CTa_m - additional control bytes, where `m = Group::WIDTH - 1` (so that the search"] # [doc = "                        with loading `Group` bytes from the heap works properly, even if the result"] # [doc = "                        of `h1(hash) & self.bucket_mask` is equal to `self.bucket_mask`). See also"] # [doc = "                        `RawTableInner::set_ctrl` function."] # [doc = ""] # [doc = " P.S. `h1(hash) & self.bucket_mask` is the same as `hash as usize % self.buckets()` because the number"] # [doc = " of buckets is a power of two, and `self.bucket_mask = self.buckets() - 1`."] # [doc = " ```"] # [doc = ""] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] fn data_end < T > (& self) -> NonNull < T > { self . ctrl . cast () } # [doc = " Returns an iterator-like object for a probe sequence on the table."] # [doc = ""] # [doc = " This iterator never terminates, but is guaranteed to visit each bucket"] # [doc = " group exactly once. The loop using `probe_seq` must terminate upon"] # [doc = " reaching a group containing an empty bucket."] # [inline] fn probe_seq (& self , hash : u64) -> ProbeSeq { ProbeSeq { pos : h1 (hash) & self . bucket_mask , stride : 0 , } } # [inline] unsafe fn record_item_insert_at (& mut self , index : usize , old_ctrl : Tag , hash : u64) { self . growth_left -= usize :: from (old_ctrl . special_is_empty ()) ; self . set_ctrl_hash (index , hash) ; self . items += 1 ; } # [inline] fn is_in_same_group (& self , i : usize , new_i : usize , hash : u64) -> bool { let probe_seq_pos = self . probe_seq (hash) . pos ; let probe_index = | pos : usize | (pos . wrapping_sub (probe_seq_pos) & self . bucket_mask) / Group :: WIDTH ; probe_index (i) == probe_index (new_i) } # [doc = " Sets a control byte to the hash, and possibly also the replicated control byte at"] # [doc = " the end of the array."] # [doc = ""] # [doc = " This function does not make any changes to the `data` parts of the table,"] # [doc = " or any changes to the `items` or `growth_left` field of the table."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The safety rules are directly derived from the safety rules for [`RawTableInner::set_ctrl`]"] # [doc = " method. Thus, in order to uphold the safety contracts for the method, you must observe the"] # [doc = " following rules when calling this function:"] # [doc = ""] # [doc = " * The [`RawTableInner`] has already been allocated;"] # [doc = ""] # [doc = " * The `index` must not be greater than the `RawTableInner.bucket_mask`, i.e."] # [doc = "   `index <= RawTableInner.bucket_mask` or, in other words, `(index + 1)` must"] # [doc = "   be no greater than the number returned by the function [`RawTableInner::buckets`]."] # [doc = ""] # [doc = " Calling this function on a table that has not been allocated results in [`undefined behavior`]."] # [doc = ""] # [doc = " See also [`Bucket::as_ptr`] method, for more information about of properly removing"] # [doc = " or saving `data element` from / into the [`RawTable`] / [`RawTableInner`]."] # [doc = ""] # [doc = " [`RawTableInner::set_ctrl`]: RawTableInner::set_ctrl"] # [doc = " [`RawTableInner::buckets`]: RawTableInner::buckets"] # [doc = " [`Bucket::as_ptr`]: Bucket::as_ptr"] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] unsafe fn set_ctrl_hash (& mut self , index : usize , hash : u64) { self . set_ctrl (index , Tag :: full (hash)) ; } # [doc = " Replaces the hash in the control byte at the given index with the provided one,"] # [doc = " and possibly also replicates the new control byte at the end of the array of control"] # [doc = " bytes, returning the old control byte."] # [doc = ""] # [doc = " This function does not make any changes to the `data` parts of the table,"] # [doc = " or any changes to the `items` or `growth_left` field of the table."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The safety rules are directly derived from the safety rules for [`RawTableInner::set_ctrl_hash`]"] # [doc = " and [`RawTableInner::ctrl`] methods. Thus, in order to uphold the safety contracts for both"] # [doc = " methods, you must observe the following rules when calling this function:"] # [doc = ""] # [doc = " * The [`RawTableInner`] has already been allocated;"] # [doc = ""] # [doc = " * The `index` must not be greater than the `RawTableInner.bucket_mask`, i.e."] # [doc = "   `index <= RawTableInner.bucket_mask` or, in other words, `(index + 1)` must"] # [doc = "   be no greater than the number returned by the function [`RawTableInner::buckets`]."] # [doc = ""] # [doc = " Calling this function on a table that has not been allocated results in [`undefined behavior`]."] # [doc = ""] # [doc = " See also [`Bucket::as_ptr`] method, for more information about of properly removing"] # [doc = " or saving `data element` from / into the [`RawTable`] / [`RawTableInner`]."] # [doc = ""] # [doc = " [`RawTableInner::set_ctrl_hash`]: RawTableInner::set_ctrl_hash"] # [doc = " [`RawTableInner::buckets`]: RawTableInner::buckets"] # [doc = " [`Bucket::as_ptr`]: Bucket::as_ptr"] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] unsafe fn replace_ctrl_hash (& mut self , index : usize , hash : u64) -> Tag { let prev_ctrl = * self . ctrl (index) ; self . set_ctrl_hash (index , hash) ; prev_ctrl } # [doc = " Sets a control byte, and possibly also the replicated control byte at"] # [doc = " the end of the array."] # [doc = ""] # [doc = " This function does not make any changes to the `data` parts of the table,"] # [doc = " or any changes to the `items` or `growth_left` field of the table."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " You must observe the following safety rules when calling this function:"] # [doc = ""] # [doc = " * The [`RawTableInner`] has already been allocated;"] # [doc = ""] # [doc = " * The `index` must not be greater than the `RawTableInner.bucket_mask`, i.e."] # [doc = "   `index <= RawTableInner.bucket_mask` or, in other words, `(index + 1)` must"] # [doc = "   be no greater than the number returned by the function [`RawTableInner::buckets`]."] # [doc = ""] # [doc = " Calling this function on a table that has not been allocated results in [`undefined behavior`]."] # [doc = ""] # [doc = " See also [`Bucket::as_ptr`] method, for more information about of properly removing"] # [doc = " or saving `data element` from / into the [`RawTable`] / [`RawTableInner`]."] # [doc = ""] # [doc = " [`RawTableInner::buckets`]: RawTableInner::buckets"] # [doc = " [`Bucket::as_ptr`]: Bucket::as_ptr"] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] unsafe fn set_ctrl (& mut self , index : usize , ctrl : Tag) { let index2 = ((index . wrapping_sub (Group :: WIDTH)) & self . bucket_mask) + Group :: WIDTH ; * self . ctrl (index) = ctrl ; * self . ctrl (index2) = ctrl ; } # [doc = " Returns a pointer to a control byte."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " For the allocated [`RawTableInner`], the result is [`Undefined Behavior`],"] # [doc = " if the `index` is greater than the `self.bucket_mask + 1 + Group::WIDTH`."] # [doc = " In that case, calling this function with `index == self.bucket_mask + 1 + Group::WIDTH`"] # [doc = " will return a pointer to the end of the allocated table and it is useless on its own."] # [doc = ""] # [doc = " Calling this function with `index >= self.bucket_mask + 1 + Group::WIDTH` on a"] # [doc = " table that has not been allocated results in [`Undefined Behavior`]."] # [doc = ""] # [doc = " So to satisfy both requirements you should always follow the rule that"] # [doc = " `index < self.bucket_mask + 1 + Group::WIDTH`"] # [doc = ""] # [doc = " Calling this function on [`RawTableInner`] that are not already allocated is safe"] # [doc = " for read-only purpose."] # [doc = ""] # [doc = " See also [`Bucket::as_ptr()`] method, for more information about of properly removing"] # [doc = " or saving `data element` from / into the [`RawTable`] / [`RawTableInner`]."] # [doc = ""] # [doc = " [`Bucket::as_ptr()`]: Bucket::as_ptr()"] # [doc = " [`Undefined Behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] unsafe fn ctrl (& self , index : usize) -> * mut Tag { debug_assert ! (index < self . num_ctrl_bytes ()) ; self . ctrl . as_ptr () . add (index) . cast () } # [doc = " Gets the slice of all control bytes."] fn ctrl_slice (& mut self) -> & mut [Tag] { unsafe { slice :: from_raw_parts_mut (self . ctrl . as_ptr () . cast () , self . num_ctrl_bytes ()) } } # [inline] fn buckets (& self) -> usize { self . bucket_mask + 1 } # [doc = " Checks whether the bucket at `index` is full."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure `index` is less than the number of buckets."] # [inline] unsafe fn is_bucket_full (& self , index : usize) -> bool { debug_assert ! (index < self . buckets ()) ; (* self . ctrl (index)) . is_full () } # [inline] fn num_ctrl_bytes (& self) -> usize { self . bucket_mask + 1 + Group :: WIDTH } # [inline] fn is_empty_singleton (& self) -> bool { self . bucket_mask == 0 } # [doc = " Attempts to allocate a new hash table with at least enough capacity"] # [doc = " for inserting the given number of elements without reallocating,"] # [doc = " and return it inside `ScopeGuard` to protect against panic in the hash"] # [doc = " function."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " It is recommended (but not required):"] # [doc = ""] # [doc = " * That the new table's `capacity` be greater than or equal to `self.items`."] # [doc = ""] # [doc = " * The `alloc` is the same [`Allocator`] as the `Allocator` used"] # [doc = "   to allocate this table."] # [doc = ""] # [doc = " * The `table_layout` is the same [`TableLayout`] as the `TableLayout` used"] # [doc = "   to allocate this table."] # [doc = ""] # [doc = " If `table_layout` does not match the `TableLayout` that was used to allocate"] # [doc = " this table, then using `mem::swap` with the `self` and the new table returned"] # [doc = " by this function results in [`undefined behavior`]."] # [doc = ""] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [allow (clippy :: mut_mut)] # [inline] fn prepare_resize < 'a , A > (& self , alloc : & 'a A , table_layout : TableLayout , capacity : usize , fallibility : Fallibility ,) -> Result < crate :: scopeguard :: ScopeGuard < Self , impl FnMut (& mut Self) + 'a > , TryReserveError > where A : Allocator , { debug_assert ! (self . items <= capacity) ; let new_table = RawTableInner :: fallible_with_capacity (alloc , table_layout , capacity , fallibility) ? ; Ok (guard (new_table , move | self_ | { if ! self_ . is_empty_singleton () { unsafe { self_ . free_buckets (alloc , table_layout) } ; } })) } # [doc = " Reserves or rehashes to make room for `additional` more elements."] # [doc = ""] # [doc = " This uses dynamic dispatch to reduce the amount of"] # [doc = " code generated, but it is eliminated by LLVM optimizations when inlined."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If any of the following conditions are violated, the result is"] # [doc = " [`undefined behavior`]:"] # [doc = ""] # [doc = " * The `alloc` must be the same [`Allocator`] as the `Allocator` used"] # [doc = "   to allocate this table."] # [doc = ""] # [doc = " * The `layout` must be the same [`TableLayout`] as the `TableLayout`"] # [doc = "   used to allocate this table."] # [doc = ""] # [doc = " * The `drop` function (`fn(*mut u8)`) must be the actual drop function of"] # [doc = "   the elements stored in the table."] # [doc = ""] # [doc = " * The [`RawTableInner`] must have properly initialized control bytes."] # [doc = ""] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [allow (clippy :: inline_always)] # [inline (always)] unsafe fn reserve_rehash_inner < A > (& mut self , alloc : & A , additional : usize , hasher : & dyn Fn (& mut Self , usize) -> u64 , fallibility : Fallibility , layout : TableLayout , drop : Option < unsafe fn (* mut u8) > ,) -> Result < () , TryReserveError > where A : Allocator , { let new_items = match self . items . checked_add (additional) { Some (new_items) => new_items , None => return Err (fallibility . capacity_overflow ()) , } ; let full_capacity = bucket_mask_to_capacity (self . bucket_mask) ; if new_items <= full_capacity / 2 { self . rehash_in_place (hasher , layout . size , drop) ; Ok (()) } else { self . resize_inner (alloc , usize :: max (new_items , full_capacity + 1) , hasher , fallibility , layout ,) } } # [doc = " Returns an iterator over full buckets indices in the table."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Behavior is undefined if any of the following conditions are violated:"] # [doc = ""] # [doc = " * The caller has to ensure that the `RawTableInner` outlives the"] # [doc = "   `FullBucketsIndices`. Because we cannot make the `next` method"] # [doc = "   unsafe on the `FullBucketsIndices` struct, we have to make the"] # [doc = "   `full_buckets_indices` method unsafe."] # [doc = ""] # [doc = " * The [`RawTableInner`] must have properly initialized control bytes."] # [inline (always)] unsafe fn full_buckets_indices (& self) -> FullBucketsIndices { let ctrl = NonNull :: new_unchecked (self . ctrl (0) . cast :: < u8 > ()) ; FullBucketsIndices { current_group : Group :: load_aligned (ctrl . as_ptr () . cast ()) . match_full () . into_iter () , group_first_index : 0 , ctrl , items : self . items , } } # [doc = " Allocates a new table of a different size and moves the contents of the"] # [doc = " current table into it."] # [doc = ""] # [doc = " This uses dynamic dispatch to reduce the amount of"] # [doc = " code generated, but it is eliminated by LLVM optimizations when inlined."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If any of the following conditions are violated, the result is"] # [doc = " [`undefined behavior`]:"] # [doc = ""] # [doc = " * The `alloc` must be the same [`Allocator`] as the `Allocator` used"] # [doc = "   to allocate this table;"] # [doc = ""] # [doc = " * The `layout` must be the same [`TableLayout`] as the `TableLayout`"] # [doc = "   used to allocate this table;"] # [doc = ""] # [doc = " * The [`RawTableInner`] must have properly initialized control bytes."] # [doc = ""] # [doc = " The caller of this function must ensure that `capacity >= self.items`"] # [doc = " otherwise:"] # [doc = ""] # [doc = " * If `self.items != 0`, calling of this function with `capacity == 0`"] # [doc = "   results in [`undefined behavior`]."] # [doc = ""] # [doc = " * If `capacity_to_buckets(capacity) < Group::WIDTH` and"] # [doc = "   `self.items > capacity_to_buckets(capacity)` calling this function"] # [doc = "   results in [`undefined behavior`]."] # [doc = ""] # [doc = " * If `capacity_to_buckets(capacity) >= Group::WIDTH` and"] # [doc = "   `self.items > capacity_to_buckets(capacity)` calling this function"] # [doc = "   are never return (will go into an infinite loop)."] # [doc = ""] # [doc = " Note: It is recommended (but not required) that the new table's `capacity`"] # [doc = " be greater than or equal to `self.items`. In case if `capacity <= self.items`"] # [doc = " this function can never return. See [`RawTableInner::find_insert_index`] for"] # [doc = " more information."] # [doc = ""] # [doc = " [`RawTableInner::find_insert_index`]: RawTableInner::find_insert_index"] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [allow (clippy :: inline_always)] # [inline (always)] unsafe fn resize_inner < A > (& mut self , alloc : & A , capacity : usize , hasher : & dyn Fn (& mut Self , usize) -> u64 , fallibility : Fallibility , layout : TableLayout ,) -> Result < () , TryReserveError > where A : Allocator , { let mut new_table = self . prepare_resize (alloc , layout , capacity , fallibility) ? ; for full_byte_index in self . full_buckets_indices () { let hash = hasher (self , full_byte_index) ; let (new_index , _) = new_table . prepare_insert_index (hash) ; ptr :: copy_nonoverlapping (self . bucket_ptr (full_byte_index , layout . size) , new_table . bucket_ptr (new_index , layout . size) , layout . size ,) ; } new_table . growth_left -= self . items ; new_table . items = self . items ; mem :: swap (self , & mut new_table) ; Ok (()) } # [doc = " Rehashes the contents of the table in place (i.e. without changing the"] # [doc = " allocation)."] # [doc = ""] # [doc = " If `hasher` panics then some the table's contents may be lost."] # [doc = ""] # [doc = " This uses dynamic dispatch to reduce the amount of"] # [doc = " code generated, but it is eliminated by LLVM optimizations when inlined."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If any of the following conditions are violated, the result is [`undefined behavior`]:"] # [doc = ""] # [doc = " * The `size_of` must be equal to the size of the elements stored in the table;"] # [doc = ""] # [doc = " * The `drop` function (`fn(*mut u8)`) must be the actual drop function of"] # [doc = "   the elements stored in the table."] # [doc = ""] # [doc = " * The [`RawTableInner`] has already been allocated;"] # [doc = ""] # [doc = " * The [`RawTableInner`] must have properly initialized control bytes."] # [doc = ""] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [allow (clippy :: inline_always)] # [cfg_attr (feature = "inline-more" , inline (always))] # [cfg_attr (not (feature = "inline-more") , inline)] unsafe fn rehash_in_place (& mut self , hasher : & dyn Fn (& mut Self , usize) -> u64 , size_of : usize , drop : Option < unsafe fn (* mut u8) > ,) { self . prepare_rehash_in_place () ; let mut guard = guard (self , move | self_ | { if let Some (drop) = drop { for i in 0 .. self_ . buckets () { if * self_ . ctrl (i) == Tag :: DELETED { self_ . set_ctrl (i , Tag :: EMPTY) ; drop (self_ . bucket_ptr (i , size_of)) ; self_ . items -= 1 ; } } } self_ . growth_left = bucket_mask_to_capacity (self_ . bucket_mask) - self_ . items ; }) ; 'outer : for i in 0 .. guard . buckets () { if * guard . ctrl (i) != Tag :: DELETED { continue ; } let i_p = guard . bucket_ptr (i , size_of) ; 'inner : loop { let hash = hasher (* guard , i) ; let new_i = guard . find_insert_index (hash) ; if likely (guard . is_in_same_group (i , new_i , hash)) { guard . set_ctrl_hash (i , hash) ; continue 'outer ; } let new_i_p = guard . bucket_ptr (new_i , size_of) ; let prev_ctrl = guard . replace_ctrl_hash (new_i , hash) ; if prev_ctrl == Tag :: EMPTY { guard . set_ctrl (i , Tag :: EMPTY) ; ptr :: copy_nonoverlapping (i_p , new_i_p , size_of) ; continue 'outer ; } else { debug_assert_eq ! (prev_ctrl , Tag :: DELETED) ; ptr :: swap_nonoverlapping (i_p , new_i_p , size_of) ; continue 'inner ; } } } guard . growth_left = bucket_mask_to_capacity (guard . bucket_mask) - guard . items ; mem :: forget (guard) ; } # [doc = " Deallocates the table without dropping any entries."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " This function must be called only after [`drop_elements`](RawTableInner::drop_elements),"] # [doc = " else it can lead to leaking of memory. Also calling this function automatically"] # [doc = " makes invalid (dangling) all instances of buckets ([`Bucket`]) and makes invalid"] # [doc = " (dangling) the `ctrl` field of the table."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If any of the following conditions are violated, the result is [`Undefined Behavior`]:"] # [doc = ""] # [doc = " * The [`RawTableInner`] has already been allocated;"] # [doc = ""] # [doc = " * The `alloc` must be the same [`Allocator`] as the `Allocator` that was used"] # [doc = "   to allocate this table."] # [doc = ""] # [doc = " * The `table_layout` must be the same [`TableLayout`] as the `TableLayout` that was used"] # [doc = "   to allocate this table."] # [doc = ""] # [doc = " See also [`GlobalAlloc::dealloc`] or [`Allocator::deallocate`] for more  information."] # [doc = ""] # [doc = " [`Undefined Behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [doc = " [`GlobalAlloc::dealloc`]: https://doc.rust-lang.org/alloc/alloc/trait.GlobalAlloc.html#tymethod.dealloc"] # [doc = " [`Allocator::deallocate`]: https://doc.rust-lang.org/alloc/alloc/trait.Allocator.html#tymethod.deallocate"] # [inline] unsafe fn free_buckets < A > (& mut self , alloc : & A , table_layout : TableLayout) where A : Allocator , { let (ptr , layout) = self . allocation_info (table_layout) ; alloc . deallocate (ptr , layout) ; } # [doc = " Returns a pointer to the allocated memory and the layout that was used to"] # [doc = " allocate the table."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Caller of this function must observe the following safety rules:"] # [doc = ""] # [doc = " * The [`RawTableInner`] has already been allocated, otherwise"] # [doc = "   calling this function results in [`undefined behavior`]"] # [doc = ""] # [doc = " * The `table_layout` must be the same [`TableLayout`] as the `TableLayout`"] # [doc = "   that was used to allocate this table. Failure to comply with this condition"] # [doc = "   may result in [`undefined behavior`]."] # [doc = ""] # [doc = " See also [`GlobalAlloc::dealloc`] or [`Allocator::deallocate`] for more  information."] # [doc = ""] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [doc = " [`GlobalAlloc::dealloc`]: https://doc.rust-lang.org/alloc/alloc/trait.GlobalAlloc.html#tymethod.dealloc"] # [doc = " [`Allocator::deallocate`]: https://doc.rust-lang.org/alloc/alloc/trait.Allocator.html#tymethod.deallocate"] # [inline] unsafe fn allocation_info (& self , table_layout : TableLayout) -> (NonNull < u8 > , Layout) { debug_assert ! (! self . is_empty_singleton () , "this function can only be called on non-empty tables") ; let (layout , ctrl_offset) = match table_layout . calculate_layout_for (self . buckets ()) { Some (lco) => lco , None => unsafe { hint :: unreachable_unchecked () } , } ; (unsafe { NonNull :: new_unchecked (self . ctrl . as_ptr () . sub (ctrl_offset)) } , layout ,) } # [doc = " Returns the total amount of memory allocated internally by the hash"] # [doc = " table, in bytes."] # [doc = ""] # [doc = " The returned number is informational only. It is intended to be"] # [doc = " primarily used for memory profiling."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The `table_layout` must be the same [`TableLayout`] as the `TableLayout`"] # [doc = " that was used to allocate this table. Failure to comply with this condition"] # [doc = " may result in [`undefined behavior`]."] # [doc = ""] # [doc = ""] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] unsafe fn allocation_size_or_zero (& self , table_layout : TableLayout) -> usize { if self . is_empty_singleton () { 0 } else { unsafe { self . allocation_info (table_layout) . 1 . size () } } } # [doc = " Marks all table buckets as empty without dropping their contents."] # [inline] fn clear_no_drop (& mut self) { if ! self . is_empty_singleton () { self . ctrl_slice () . fill_empty () ; } self . items = 0 ; self . growth_left = bucket_mask_to_capacity (self . bucket_mask) ; } # [doc = " Erases the [`Bucket`]'s control byte at the given index so that it does not"] # [doc = " triggered as full, decreases the `items` of the table and, if it can be done,"] # [doc = " increases `self.growth_left`."] # [doc = ""] # [doc = " This function does not actually erase / drop the [`Bucket`] itself, i.e. it"] # [doc = " does not make any changes to the `data` parts of the table. The caller of this"] # [doc = " function must take care to properly drop the `data`, otherwise calling this"] # [doc = " function may result in a memory leak."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " You must observe the following safety rules when calling this function:"] # [doc = ""] # [doc = " * The [`RawTableInner`] has already been allocated;"] # [doc = ""] # [doc = " * It must be the full control byte at the given position;"] # [doc = ""] # [doc = " * The `index` must not be greater than the `RawTableInner.bucket_mask`, i.e."] # [doc = "   `index <= RawTableInner.bucket_mask` or, in other words, `(index + 1)` must"] # [doc = "   be no greater than the number returned by the function [`RawTableInner::buckets`]."] # [doc = ""] # [doc = " Calling this function on a table that has not been allocated results in [`undefined behavior`]."] # [doc = ""] # [doc = " Calling this function on a table with no elements is unspecified, but calling subsequent"] # [doc = " functions is likely to result in [`undefined behavior`] due to overflow subtraction"] # [doc = " (`self.items -= 1 cause overflow when self.items == 0`)."] # [doc = ""] # [doc = " See also [`Bucket::as_ptr`] method, for more information about of properly removing"] # [doc = " or saving `data element` from / into the [`RawTable`] / [`RawTableInner`]."] # [doc = ""] # [doc = " [`RawTableInner::buckets`]: RawTableInner::buckets"] # [doc = " [`Bucket::as_ptr`]: Bucket::as_ptr"] # [doc = " [`undefined behavior`]: https://doc.rust-lang.org/reference/behavior-considered-undefined.html"] # [inline] unsafe fn erase (& mut self , index : usize) { debug_assert ! (self . is_bucket_full (index)) ; let index_before = index . wrapping_sub (Group :: WIDTH) & self . bucket_mask ; let empty_before = Group :: load (self . ctrl (index_before)) . match_empty () ; let empty_after = Group :: load (self . ctrl (index)) . match_empty () ; let ctrl = if empty_before . leading_zeros () + empty_after . trailing_zeros () >= Group :: WIDTH { Tag :: DELETED } else { self . growth_left += 1 ; Tag :: EMPTY } ; self . set_ctrl (index , ctrl) ; self . items -= 1 ; } }
    };
}

impl_58!();