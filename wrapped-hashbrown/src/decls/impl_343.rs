macro_rules! deps {
    () => {
        HashMap!();
        RawEntryMut!();
        RawEntryBuilder!();
        RawEntryBuilderMut!();
    };
}

macro_rules! impl_343 {
    () => {
        deps!();
        impl < K , V , S , A : Allocator > HashMap < K , V , S , A > { # [doc = " Creates a raw entry builder for the `HashMap`."] # [doc = ""] # [doc = " Raw entries provide the lowest level of control for searching and"] # [doc = " manipulating a map. They must be manually initialized with a hash and"] # [doc = " then manually searched. After this, insertions into a vacant entry"] # [doc = " still require an owned key to be provided."] # [doc = ""] # [doc = " Raw entries are useful for such exotic situations as:"] # [doc = ""] # [doc = " * Hash memoization"] # [doc = " * Deferring the creation of an owned key until it is known to be required"] # [doc = " * Using a search key that doesn't work with the Borrow trait"] # [doc = " * Using custom comparison logic without newtype wrappers"] # [doc = ""] # [doc = " Because raw entries provide much more low-level control, it's much easier"] # [doc = " to put the `HashMap` into an inconsistent state which, while memory-safe,"] # [doc = " will cause the map to produce seemingly random results. Higher-level and"] # [doc = " more foolproof APIs like `entry` should be preferred when possible."] # [doc = ""] # [doc = " In particular, the hash used to initialized the raw entry must still be"] # [doc = " consistent with the hash of the key that is ultimately stored in the entry."] # [doc = " This is because implementations of `HashMap` may need to recompute hashes"] # [doc = " when resizing, at which point only the keys are available."] # [doc = ""] # [doc = " Raw entries give mutable access to the keys. This must not be used"] # [doc = " to modify how the key would compare or hash, as the map will not re-evaluate"] # [doc = " where the key should go, meaning the keys may become \"lost\" if their"] # [doc = " location does not reflect their state. For instance, if you change a key"] # [doc = " so that the map now contains keys which compare equal, search may start"] # [doc = " acting erratically, with two keys randomly masking each other. Implementations"] # [doc = " are free to assume this doesn't happen (within the limits of memory-safety)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use core::hash::{BuildHasher, Hash};"] # [doc = " use hashbrown::hash_map::{HashMap, RawEntryMut};"] # [doc = ""] # [doc = " let mut map = HashMap::new();"] # [doc = " map.extend([(\"a\", 100), (\"b\", 200), (\"c\", 300)]);"] # [doc = ""] # [doc = " fn compute_hash<K: Hash + ?Sized, S: BuildHasher>(hash_builder: &S, key: &K) -> u64 {"] # [doc = "     use core::hash::Hasher;"] # [doc = "     let mut state = hash_builder.build_hasher();"] # [doc = "     key.hash(&mut state);"] # [doc = "     state.finish()"] # [doc = " }"] # [doc = ""] # [doc = " // Existing key (insert and update)"] # [doc = " match map.raw_entry_mut().from_key(&\"a\") {"] # [doc = "     RawEntryMut::Vacant(_) => unreachable!(),"] # [doc = "     RawEntryMut::Occupied(mut view) => {"] # [doc = "         assert_eq!(view.get(), &100);"] # [doc = "         let v = view.get_mut();"] # [doc = "         let new_v = (*v) * 10;"] # [doc = "         *v = new_v;"] # [doc = "         assert_eq!(view.insert(1111), 1000);"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(map[&\"a\"], 1111);"] # [doc = " assert_eq!(map.len(), 3);"] # [doc = ""] # [doc = " // Existing key (take)"] # [doc = " let hash = compute_hash(map.hasher(), &\"c\");"] # [doc = " match map.raw_entry_mut().from_key_hashed_nocheck(hash, &\"c\") {"] # [doc = "     RawEntryMut::Vacant(_) => unreachable!(),"] # [doc = "     RawEntryMut::Occupied(view) => {"] # [doc = "         assert_eq!(view.remove_entry(), (\"c\", 300));"] # [doc = "     }"] # [doc = " }"] # [doc = " assert_eq!(map.raw_entry().from_key(&\"c\"), None);"] # [doc = " assert_eq!(map.len(), 2);"] # [doc = ""] # [doc = " // Nonexistent key (insert and update)"] # [doc = " let key = \"d\";"] # [doc = " let hash = compute_hash(map.hasher(), &key);"] # [doc = " match map.raw_entry_mut().from_hash(hash, |q| *q == key) {"] # [doc = "     RawEntryMut::Occupied(_) => unreachable!(),"] # [doc = "     RawEntryMut::Vacant(view) => {"] # [doc = "         let (k, value) = view.insert(\"d\", 4000);"] # [doc = "         assert_eq!((*k, *value), (\"d\", 4000));"] # [doc = "         *value = 40000;"] # [doc = "     }"] # [doc = " }"] # [doc = " assert_eq!(map[&\"d\"], 40000);"] # [doc = " assert_eq!(map.len(), 3);"] # [doc = ""] # [doc = " match map.raw_entry_mut().from_hash(hash, |q| *q == key) {"] # [doc = "     RawEntryMut::Vacant(_) => unreachable!(),"] # [doc = "     RawEntryMut::Occupied(view) => {"] # [doc = "         assert_eq!(view.remove_entry(), (\"d\", 40000));"] # [doc = "     }"] # [doc = " }"] # [doc = " assert_eq!(map.get(&\"d\"), None);"] # [doc = " assert_eq!(map.len(), 2);"] # [doc = " ```"] # [cfg_attr (feature = "inline-more" , inline)] pub fn raw_entry_mut (& mut self) -> RawEntryBuilderMut < '_ , K , V , S , A > { RawEntryBuilderMut { map : self } } # [doc = " Creates a raw immutable entry builder for the `HashMap`."] # [doc = ""] # [doc = " Raw entries provide the lowest level of control for searching and"] # [doc = " manipulating a map. They must be manually initialized with a hash and"] # [doc = " then manually searched."] # [doc = ""] # [doc = " This is useful for"] # [doc = " * Hash memoization"] # [doc = " * Using a search key that doesn't work with the Borrow trait"] # [doc = " * Using custom comparison logic without newtype wrappers"] # [doc = ""] # [doc = " Unless you are in such a situation, higher-level and more foolproof APIs like"] # [doc = " `get` should be preferred."] # [doc = ""] # [doc = " Immutable raw entries have very limited use; you might instead want `raw_entry_mut`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use core::hash::{BuildHasher, Hash};"] # [doc = " use hashbrown::HashMap;"] # [doc = ""] # [doc = " let mut map = HashMap::new();"] # [doc = " map.extend([(\"a\", 100), (\"b\", 200), (\"c\", 300)]);"] # [doc = ""] # [doc = " fn compute_hash<K: Hash + ?Sized, S: BuildHasher>(hash_builder: &S, key: &K) -> u64 {"] # [doc = "     use core::hash::Hasher;"] # [doc = "     let mut state = hash_builder.build_hasher();"] # [doc = "     key.hash(&mut state);"] # [doc = "     state.finish()"] # [doc = " }"] # [doc = ""] # [doc = " for k in [\"a\", \"b\", \"c\", \"d\", \"e\", \"f\"] {"] # [doc = "     let hash = compute_hash(map.hasher(), k);"] # [doc = "     let v = map.get(&k).cloned();"] # [doc = "     let kv = v.as_ref().map(|v| (&k, v));"] # [doc = ""] # [doc = "     println!(\"Key: {} and value: {:?}\", k, v);"] # [doc = ""] # [doc = "     assert_eq!(map.raw_entry().from_key(&k), kv);"] # [doc = "     assert_eq!(map.raw_entry().from_hash(hash, |q| *q == k), kv);"] # [doc = "     assert_eq!(map.raw_entry().from_key_hashed_nocheck(hash, &k), kv);"] # [doc = " }"] # [doc = " ```"] # [cfg_attr (feature = "inline-more" , inline)] pub fn raw_entry (& self) -> RawEntryBuilder < '_ , K , V , S , A > { RawEntryBuilder { map : self } } }
    };
}

impl_343!()