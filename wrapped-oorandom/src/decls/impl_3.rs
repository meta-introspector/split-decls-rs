macro_rules! deps {
    () => {
        Rand64!();
    };
}

macro_rules! impl_3 {
    () => {
        deps!();
        impl Rand64 { # [doc = " The default value for `increment`."] # [doc = ""] # [doc = " The value used here is from the PCG default C implementation: http://www.pcg-random.org/download.html"] pub const DEFAULT_INC : u128 = 0x2FE0E169_FFBD06E3_5BC307BD_4D2F814F ; # [doc = " This is the number that you have to Really Get Right."] # [doc = ""] # [doc = " The value used here is from the PCG C implementation:"] # [doc = " https://github.com/imneme/pcg-c/blob/master/include/pcg_variants.h#L288"] pub (crate) const MULTIPLIER : u128 = 47026247687942121848144207491837523525 ; # [doc = " Creates a new PRNG with the given seed and a default increment."] pub fn new (seed : u128) -> Self { Self :: new_inc (seed , Self :: DEFAULT_INC) } # [doc = " Same as `Rand32::new_inc()`"] pub fn new_inc (seed : u128 , increment : u128) -> Self { let mut rng = Self { state : 0 , inc : increment . wrapping_shl (1) | 1 , } ; let _ = rng . rand_u64 () ; rng . state = rng . state . wrapping_add (seed) ; let _ = rng . rand_u64 () ; rng } # [doc = " Returns the internal state of the PRNG.  This allows"] # [doc = " you to save a PRNG and create a new one that will resume"] # [doc = " from the same spot in the sequence."] pub fn state (& self) -> (u128 , u128) { (self . state , self . inc) } # [doc = " Creates a new PRNG from a saved state from `Rand32::state()`."] # [doc = " This is NOT quite the same as `new_inc()` because `new_inc()` does"] # [doc = " a little extra setup work to initialize the state."] pub fn from_state (state : (u128 , u128)) -> Self { let (state , inc) = state ; Self { state , inc } } # [doc = " Produces a random `u64` in the range`[0, u64::MAX]`."] pub fn rand_u64 (& mut self) -> u64 { let oldstate : u128 = self . state ; self . state = oldstate . wrapping_mul (Self :: MULTIPLIER) . wrapping_add (self . inc) ; let xorshifted : u64 = (((oldstate >> 29) ^ oldstate) >> 58) as u64 ; let rot : u32 = (oldstate >> 122) as u32 ; xorshifted . rotate_right (rot) } # [doc = " Produces a random `i64` in the range `[i64::MIN, i64::MAX]`."] pub fn rand_i64 (& mut self) -> i64 { self . rand_u64 () as i64 } # [doc = " Produces a random `f64` in the range `[0.0, 1.0)`."] pub fn rand_float (& mut self) -> f64 { const TOTAL_BITS : u32 = 64 ; const PRECISION : u32 = core :: f64 :: MANTISSA_DIGITS ; const MANTISSA_SCALE : f64 = 1.0 / ((1u64 << PRECISION) as f64) ; let mut u = self . rand_u64 () ; u >>= TOTAL_BITS - PRECISION ; u as f64 * MANTISSA_SCALE } # [doc = " Produces a random within the given bounds.  Like any `Range`,"] # [doc = " it includes the lower bound and excludes the upper one."] # [doc = ""] # [doc = " This should be faster than `Self::rand() % end + start`, but the"] # [doc = " real advantage is it's more convenient.  Requires that"] # [doc = " `range.end <= range.start`."] pub fn rand_range (& mut self , range : Range < u64 >) -> u64 { debug_assert ! (range . start < range . end) ; let range_starting_from_zero = 0 .. (range . end - range . start) ; let s : u64 = range_starting_from_zero . end ; let mut m : u128 = u128 :: from (self . rand_u64 ()) * u128 :: from (s) ; let mut leftover : u64 = (m & 0xFFFFFFFF_FFFFFFFF) as u64 ; if leftover < s { let threshold : u64 = s . wrapping_neg () % s ; while leftover < threshold { m = u128 :: from (self . rand_u64 ()) * u128 :: from (s) ; leftover = (m & 0xFFFFFFFF_FFFFFFFF) as u64 ; } } (m . wrapping_shr (64)) as u64 + range . start } }
    };
}

impl_3!()