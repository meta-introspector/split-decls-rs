// Generated macro for Aead (trait)
macro_rules! Depcrate_aeadAead {
() => {
// Module: crate::aead
// Provides: {"Aead"}
// Dependencies: {}
# [doc = " Authenticated Encryption with Associated Data (AEAD) algorithm trait."] pub trait Aead { # [doc = " The type of tags produced by this AEAD. Generally a u8 array of fixed"] # [doc = " length."] type Tag : AsRef < [u8] > ; # [doc = " The type of nonces used by this AEAD. Generally a u8 array of fixed"] # [doc = " length."] type Nonce : AsRef < [u8] > ; # [doc = " Encrypt and authenticate `plaintext`, and authenticate `ad`, returning"] # [doc = " the result as a freshly allocated [`Vec`]. The `nonce` must never"] # [doc = " be used in any sealing operation with the same key, ever again."] fn seal (& self , nonce : & Self :: Nonce , plaintext : & [u8] , ad : & [u8]) -> Vec < u8 > ; # [doc = " Encrypt and authenticate `plaintext`, and authenticate `ad`, writing"] # [doc = " the ciphertext over `plaintext` and additionally returning the calculated"] # [doc = " tag, which is usually appended to the ciphertext. The `nonce` must never"] # [doc = " be used in any sealing operation with the same key, ever again."] fn seal_in_place (& self , nonce : & Self :: Nonce , plaintext : & mut [u8] , ad : & [u8]) -> Self :: Tag ; # [doc = " Authenticate `ciphertext` and `ad` and, if valid, decrypt `ciphertext`,"] # [doc = " returning the original plaintext in a newly allocated [`Vec`]. The `nonce`"] # [doc = " must be the same value as given to the sealing operation that produced"] # [doc = " `ciphertext`."] fn open (& self , nonce : & Self :: Nonce , ciphertext : & [u8] , ad : & [u8]) -> Option < Vec < u8 > > ; # [doc = " Authenticate `ciphertext` and `ad` using `tag` and, if valid, decrypt"] # [doc = " `ciphertext` in place. The `nonce` must be the same value as given to"] # [doc = " the sealing operation that produced `ciphertext`."] fn open_in_place (& self , nonce : & Self :: Nonce , ciphertext : & mut [u8] , tag : & Self :: Tag , ad : & [u8] ,) -> Result < () , InvalidCiphertext > ; }
};
}
