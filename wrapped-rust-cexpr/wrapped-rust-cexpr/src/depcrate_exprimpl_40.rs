// Generated macro for impl_40 (impl)
macro_rules! Depcrate_exprimpl_40 {
() => {
// Module: crate::expr
// Provides: {"impl_40"}
// Dependencies: {}
impl < 'ident > IdentifierParser < 'ident > { fn as_ref (& self) -> PRef < '_ > { PRef (self) } # [doc = " Create a new `IdentifierParser` with a set of known identifiers. When"] # [doc = " a known identifier is encountered during parsing, it is substituted"] # [doc = " for the value specified."] pub fn new (identifiers : & HashMap < Vec < u8 > , EvalResult >) -> IdentifierParser < '_ > { IdentifierParser { identifiers } } # [doc = " Parse and evaluate an expression of a list of tokens."] # [doc = ""] # [doc = " Returns an error if the input is not a valid expression or if the token"] # [doc = " stream contains comments, keywords or unknown identifiers."] pub fn expr < 'a > (& self , input : & 'a [Token]) -> CResult < 'a , EvalResult > { self . as_ref () . expr (input) } # [doc = " Parse and evaluate a macro definition from a list of tokens."] # [doc = ""] # [doc = " Returns the identifier for the macro and its replacement evaluated as an"] # [doc = " expression. The input should not include `#define`."] # [doc = ""] # [doc = " Returns an error if the replacement is not a valid expression, if called"] # [doc = " on most function-like macros, or if the token stream contains comments,"] # [doc = " keywords or unknown identifiers."] # [doc = ""] # [doc = " N.B. This is intended to fail on function-like macros, but if it the"] # [doc = " macro takes a single argument, the argument name is defined as an"] # [doc = " identifier, and the macro otherwise parses as an expression, it will"] # [doc = " return a result even on function-like macros."] # [doc = ""] # [doc = " ```c"] # [doc = " // will evaluate into IDENTIFIER"] # [doc = " #define DELETE(IDENTIFIER)"] # [doc = " // will evaluate into IDENTIFIER-3"] # [doc = " #define NEGATIVE_THREE(IDENTIFIER)  -3"] # [doc = " ```"] pub fn macro_definition < 'a > (& self , input : & 'a [Token]) -> CResult < 'a , (& 'a [u8] , EvalResult) > { crate :: assert_full_parse (self . as_ref () . macro_definition (input)) } }
};
}
