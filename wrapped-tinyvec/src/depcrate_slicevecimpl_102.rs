// Generated macro for impl_102 (impl)
macro_rules! Depcrate_slicevecimpl_102 {
() => {
// Module: crate::slicevec
// Provides: {"impl_102"}
// Dependencies: {}
impl < 's , T > SliceVec < 's , T > { # [inline] pub fn append (& mut self , other : & mut Self) where T : Default , { for item in other . drain (..) { self . push (item) } } # [doc = " A `*mut` pointer to the backing slice."] # [doc = ""] # [doc = " ## Safety"] # [doc = ""] # [doc = " This pointer has provenance over the _entire_ backing slice."] # [inline (always)] # [must_use] pub fn as_mut_ptr (& mut self) -> * mut T { self . data . as_mut_ptr () } # [doc = " Performs a `deref_mut`, into unique slice form."] # [inline (always)] # [must_use] pub fn as_mut_slice (& mut self) -> & mut [T] { self . deref_mut () } # [doc = " A `*const` pointer to the backing slice."] # [doc = ""] # [doc = " ## Safety"] # [doc = ""] # [doc = " This pointer has provenance over the _entire_ backing slice."] # [inline (always)] # [must_use] pub fn as_ptr (& self) -> * const T { self . data . as_ptr () } # [doc = " Performs a `deref`, into shared slice form."] # [inline (always)] # [must_use] pub fn as_slice (& self) -> & [T] { self . deref () } # [doc = " The capacity of the `SliceVec`."] # [doc = ""] # [doc = " This the length of the initial backing slice."] # [inline (always)] # [must_use] pub fn capacity (& self) -> usize { self . data . len () } # [doc = " Truncates the `SliceVec` down to length 0."] # [inline (always)] pub fn clear (& mut self) where T : Default , { self . truncate (0) } # [doc = " Creates a draining iterator that removes the specified range in the vector"] # [doc = " and yields the removed items."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If the start is greater than the end"] # [doc = " * If the end is past the edge of the vec."] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut arr = [6, 7, 8];"] # [doc = " let mut sv = SliceVec::from(&mut arr);"] # [doc = " let drained_values: ArrayVec<[i32; 4]> = sv.drain(1..).collect();"] # [doc = " assert_eq!(sv.as_slice(), &[6][..]);"] # [doc = " assert_eq!(drained_values.as_slice(), &[7, 8][..]);"] # [doc = ""] # [doc = " sv.drain(..);"] # [doc = " assert_eq!(sv.as_slice(), &[]);"] # [doc = " ```"] # [inline] pub fn drain < 'p , R : RangeBounds < usize > > (& 'p mut self , range : R ,) -> SliceVecDrain < 'p , 's , T > where T : Default , { use core :: ops :: Bound ; let start = match range . start_bound () { Bound :: Included (x) => * x , Bound :: Excluded (x) => x . saturating_add (1) , Bound :: Unbounded => 0 , } ; let end = match range . end_bound () { Bound :: Included (x) => x . saturating_add (1) , Bound :: Excluded (x) => * x , Bound :: Unbounded => self . len , } ; assert ! (start <= end , "SliceVec::drain> Illegal range, {} to {}" , start , end) ; assert ! (end <= self . len , "SliceVec::drain> Range ends at {} but length is only {}!" , end , self . len) ; SliceVecDrain { parent : self , target_start : start , target_index : start , target_end : end , } } # [inline] pub fn extend_from_slice (& mut self , sli : & [T]) where T : Clone , { if sli . is_empty () { return ; } let new_len = self . len + sli . len () ; if new_len > self . capacity () { panic ! ("SliceVec::extend_from_slice> total length {} exceeds capacity {}" , new_len , self . capacity ()) } let target = & mut self . data [self . len .. new_len] ; target . clone_from_slice (sli) ; self . set_len (new_len) ; } # [doc = " Fill the vector until its capacity has been reached."] # [doc = ""] # [doc = " Successively fills unused space in the spare slice of the vector with"] # [doc = " elements from the iterator. It then returns the remaining iterator"] # [doc = " without exhausting it. This also allows appending the head of an"] # [doc = " infinite iterator."] # [doc = ""] # [doc = " This is an alternative to `Extend::extend` method for cases where the"] # [doc = " length of the iterator can not be checked. Since this vector can not"] # [doc = " reallocate to increase its capacity, it is unclear what to do with"] # [doc = " remaining elements in the iterator and the iterator itself. The"] # [doc = " interface also provides no way to communicate this to the caller."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If the `next` method of the provided iterator panics."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut arr = [7, 7, 7, 7];"] # [doc = " let mut sv = SliceVec::from_slice_len(&mut arr, 0);"] # [doc = " let mut to_inf = sv.fill(0..);"] # [doc = " assert_eq!(&sv[..], [0, 1, 2, 3]);"] # [doc = " assert_eq!(to_inf.next(), Some(4));"] # [doc = " ```"] # [inline] pub fn fill < I : IntoIterator < Item = T > > (& mut self , iter : I) -> I :: IntoIter { let mut iter = iter . into_iter () ; for element in iter . by_ref () . take (self . capacity () - self . len ()) { self . push (element) ; } iter } # [doc = " Wraps up a slice and uses the given length as the initial length."] # [doc = ""] # [doc = " If you want to simply use the full slice, use `from` instead."] # [doc = ""] # [doc = " ## Panics"] # [doc = ""] # [doc = " * The length specified must be less than or equal to the capacity of the"] # [doc = "   slice."] # [inline] # [must_use] # [allow (clippy :: match_wild_err_arm)] pub fn from_slice_len (data : & 's mut [T] , len : usize) -> Self { assert ! (len <= data . len ()) ; Self { data , len } } # [doc = " Inserts an item at the position given, moving all following elements +1"] # [doc = " index."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If `index` > `len`"] # [doc = " * If the capacity is exhausted"] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut arr = [1, 2, 3, 0, 0];"] # [doc = " let mut sv = SliceVec::from_slice_len(&mut arr, 3);"] # [doc = " sv.insert(1, 4);"] # [doc = " assert_eq!(sv.as_slice(), &[1, 4, 2, 3]);"] # [doc = " sv.insert(4, 5);"] # [doc = " assert_eq!(sv.as_slice(), &[1, 4, 2, 3, 5]);"] # [doc = " ```"] # [inline] pub fn insert (& mut self , index : usize , item : T) { if index > self . len { panic ! ("SliceVec::insert> index {} is out of bounds {}" , index , self . len) ; } self . push (item) ; self . as_mut_slice () [index ..] . rotate_right (1) ; } # [doc = " Checks if the length is 0."] # [inline (always)] # [must_use] pub fn is_empty (& self) -> bool { self . len == 0 } # [doc = " The length of the `SliceVec` (in elements)."] # [inline (always)] # [must_use] pub fn len (& self) -> usize { self . len } # [doc = " Remove and return the last element of the vec, if there is one."] # [doc = ""] # [doc = " ## Failure"] # [doc = " * If the vec is empty you get `None`."] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut arr = [1, 2];"] # [doc = " let mut sv = SliceVec::from(&mut arr);"] # [doc = " assert_eq!(sv.pop(), Some(2));"] # [doc = " assert_eq!(sv.pop(), Some(1));"] # [doc = " assert_eq!(sv.pop(), None);"] # [doc = " ```"] # [inline] pub fn pop (& mut self) -> Option < T > where T : Default , { if self . len > 0 { self . len -= 1 ; let out = core :: mem :: take (& mut self . data [self . len]) ; Some (out) } else { None } } # [doc = " Place an element onto the end of the vec."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If the length of the vec would overflow the capacity."] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut arr = [0, 0];"] # [doc = " let mut sv = SliceVec::from_slice_len(&mut arr, 0);"] # [doc = " assert_eq!(&sv[..], []);"] # [doc = " sv.push(1);"] # [doc = " assert_eq!(&sv[..], [1]);"] # [doc = " sv.push(2);"] # [doc = " assert_eq!(&sv[..], [1, 2]);"] # [doc = " // sv.push(3); this would overflow the ArrayVec and panic!"] # [doc = " ```"] # [inline (always)] pub fn push (& mut self , val : T) { if self . len < self . capacity () { self . data [self . len] = val ; self . len += 1 ; } else { panic ! ("SliceVec::push> capacity overflow") } } # [doc = " Removes the item at `index`, shifting all others down by one index."] # [doc = ""] # [doc = " Returns the removed element."] # [doc = ""] # [doc = " ## Panics"] # [doc = ""] # [doc = " * If the index is out of bounds."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut arr = [1, 2, 3];"] # [doc = " let mut sv = SliceVec::from(&mut arr);"] # [doc = " assert_eq!(sv.remove(1), 2);"] # [doc = " assert_eq!(&sv[..], [1, 3]);"] # [doc = " ```"] # [inline] pub fn remove (& mut self , index : usize) -> T where T : Default , { let targets : & mut [T] = & mut self . deref_mut () [index ..] ; let item = core :: mem :: take (& mut targets [0]) ; targets . rotate_left (1) ; self . len -= 1 ; item } # [doc = " As [`resize_with`](SliceVec::resize_with)"] # [doc = " and it clones the value as the closure."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " // bigger"] # [doc = " let mut arr = [\"hello\", \"\", \"\", \"\", \"\"];"] # [doc = " let mut sv = SliceVec::from_slice_len(&mut arr, 1);"] # [doc = " sv.resize(3, \"world\");"] # [doc = " assert_eq!(&sv[..], [\"hello\", \"world\", \"world\"]);"] # [doc = ""] # [doc = " // smaller"] # [doc = " let mut arr = ['a', 'b', 'c', 'd'];"] # [doc = " let mut sv = SliceVec::from(&mut arr);"] # [doc = " sv.resize(2, 'z');"] # [doc = " assert_eq!(&sv[..], ['a', 'b']);"] # [doc = " ```"] # [inline] pub fn resize (& mut self , new_len : usize , new_val : T) where T : Clone , { self . resize_with (new_len , | | new_val . clone ()) } # [doc = " Resize the vec to the new length."] # [doc = ""] # [doc = " * If it needs to be longer, it's filled with repeated calls to the"] # [doc = "   provided function."] # [doc = " * If it needs to be shorter, it's truncated."] # [doc = "   * If the type needs to drop the truncated slots are filled with calls to"] # [doc = "     the provided function."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut arr = [1, 2, 3, 7, 7, 7, 7];"] # [doc = " let mut sv = SliceVec::from_slice_len(&mut arr, 3);"] # [doc = " sv.resize_with(5, Default::default);"] # [doc = " assert_eq!(&sv[..], [1, 2, 3, 0, 0]);"] # [doc = ""] # [doc = " let mut arr = [0, 0, 0, 0];"] # [doc = " let mut sv = SliceVec::from_slice_len(&mut arr, 0);"] # [doc = " let mut p = 1;"] # [doc = " sv.resize_with(4, || {"] # [doc = "   p *= 2;"] # [doc = "   p"] # [doc = " });"] # [doc = " assert_eq!(&sv[..], [2, 4, 8, 16]);"] # [doc = " ```"] # [inline] pub fn resize_with < F : FnMut () -> T > (& mut self , new_len : usize , mut f : F) { match new_len . checked_sub (self . len) { None => { if needs_drop :: < T > () { while self . len () > new_len { self . len -= 1 ; self . data [self . len] = f () ; } } else { self . len = new_len ; } } Some (new_elements) => { for _ in 0 .. new_elements { self . push (f ()) ; } } } } # [doc = " Walk the vec and keep only the elements that pass the predicate given."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = ""] # [doc = " let mut arr = [1, 1, 2, 3, 3, 4];"] # [doc = " let mut sv = SliceVec::from(&mut arr);"] # [doc = " sv.retain(|&x| x % 2 == 0);"] # [doc = " assert_eq!(&sv[..], [2, 4]);"] # [doc = " ```"] # [inline] pub fn retain < F : FnMut (& T) -> bool > (& mut self , mut acceptable : F) where T : Default , { struct JoinOnDrop < 'vec , Item > { items : & 'vec mut [Item] , done_end : usize , tail_start : usize , } impl < Item > Drop for JoinOnDrop < '_ , Item > { fn drop (& mut self) { self . items [self . done_end ..] . rotate_left (self . tail_start) ; } } let mut rest = JoinOnDrop { items : self . data , done_end : 0 , tail_start : 0 } ; for idx in 0 .. self . len { if ! acceptable (& rest . items [idx]) { let _ = core :: mem :: take (& mut rest . items [idx]) ; self . len -= 1 ; rest . tail_start += 1 ; } else { rest . items . swap (rest . done_end , idx) ; rest . done_end += 1 ; } } } # [doc = " Forces the length of the vector to `new_len`."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If `new_len` is greater than the vec's capacity."] # [doc = ""] # [doc = " ## Safety"] # [doc = " * This is a fully safe operation! The inactive memory already counts as"] # [doc = "   \"initialized\" by Rust's rules."] # [doc = " * Other than \"the memory is initialized\" there are no other guarantees"] # [doc = "   regarding what you find in the inactive portion of the vec."] # [inline (always)] pub fn set_len (& mut self , new_len : usize) { if new_len > self . capacity () { panic ! ("SliceVec::set_len> new length {} exceeds capacity {}" , new_len , self . capacity ()) } else { self . len = new_len ; } } # [doc = " Splits the collection at the point given."] # [doc = ""] # [doc = " * `[0, at)` stays in this vec (and this vec is now full)."] # [doc = " * `[at, len)` ends up in the new vec (with any spare capacity)."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * if `at` > `self.len()`"] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut arr = [1, 2, 3];"] # [doc = " let mut sv = SliceVec::from(&mut arr);"] # [doc = " let sv2 = sv.split_off(1);"] # [doc = " assert_eq!(&sv[..], [1]);"] # [doc = " assert_eq!(&sv2[..], [2, 3]);"] # [doc = " ```"] # [inline] pub fn split_off < 'a > (& 'a mut self , at : usize) -> SliceVec < 's , T > { let mut new = Self :: default () ; let backing : & 's mut [T] = core :: mem :: take (& mut self . data) ; let (me , other) = backing . split_at_mut (at) ; new . len = self . len - at ; new . data = other ; self . len = me . len () ; self . data = me ; new } # [doc = " Remove an element, swapping the end of the vec into its place."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If the index is out of bounds."] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut arr = [\"foo\", \"bar\", \"quack\", \"zap\"];"] # [doc = " let mut sv = SliceVec::from(&mut arr);"] # [doc = ""] # [doc = " assert_eq!(sv.swap_remove(1), \"bar\");"] # [doc = " assert_eq!(&sv[..], [\"foo\", \"zap\", \"quack\"]);"] # [doc = ""] # [doc = " assert_eq!(sv.swap_remove(0), \"foo\");"] # [doc = " assert_eq!(&sv[..], [\"quack\", \"zap\"]);"] # [doc = " ```"] # [inline] pub fn swap_remove (& mut self , index : usize) -> T where T : Default , { assert ! (index < self . len , "SliceVec::swap_remove> index {} is out of bounds {}" , index , self . len) ; if index == self . len - 1 { self . pop () . unwrap () } else { let i = self . pop () . unwrap () ; replace (& mut self [index] , i) } } # [doc = " Reduces the vec's length to the given value."] # [doc = ""] # [doc = " If the vec is already shorter than the input, nothing happens."] # [inline] pub fn truncate (& mut self , new_len : usize) where T : Default , { if needs_drop :: < T > () { while self . len > new_len { self . pop () ; } } else { self . len = self . len . min (new_len) ; } } # [doc = " Wraps a slice, using the given length as the starting length."] # [doc = ""] # [doc = " If you want to use the whole length of the slice, you can just use the"] # [doc = " `From` impl."] # [doc = ""] # [doc = " ## Failure"] # [doc = ""] # [doc = " If the given length is greater than the length of the slice you get"] # [doc = " `None`."] # [inline] pub fn try_from_slice_len (data : & 's mut [T] , len : usize) -> Option < Self > { if len <= data . len () { Some (Self { data , len }) } else { None } } }
};
}
