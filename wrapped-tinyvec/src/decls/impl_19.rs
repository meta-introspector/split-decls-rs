macro_rules! deps {
    () => {
        ArrayVecSplice!();
        Array!();
        ArrayVec!();
        ArrayVecDrain!();
    };
}

macro_rules! impl_19 {
    () => {
        deps!();
        impl < A : Array > ArrayVec < A > { # [doc = " Move all values from `other` into this vec."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If the vec overflows its capacity"] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut av = array_vec!([i32; 10] => 1, 2, 3);"] # [doc = " let mut av2 = array_vec!([i32; 10] => 4, 5, 6);"] # [doc = " av.append(&mut av2);"] # [doc = " assert_eq!(av, &[1, 2, 3, 4, 5, 6][..]);"] # [doc = " assert_eq!(av2, &[][..]);"] # [doc = " ```"] # [inline] pub fn append (& mut self , other : & mut Self) { assert ! (self . try_append (other) . is_none () , "ArrayVec::append> total length {} exceeds capacity {}!" , self . len () + other . len () , A :: CAPACITY) ; } # [doc = " Move all values from `other` into this vec."] # [doc = " If appending would overflow the capacity, Some(other) is returned."] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut av = array_vec!([i32; 7] => 1, 2, 3);"] # [doc = " let mut av2 = array_vec!([i32; 7] => 4, 5, 6);"] # [doc = " av.append(&mut av2);"] # [doc = " assert_eq!(av, &[1, 2, 3, 4, 5, 6][..]);"] # [doc = " assert_eq!(av2, &[][..]);"] # [doc = ""] # [doc = " let mut av3 = array_vec!([i32; 7] => 7, 8, 9);"] # [doc = " assert!(av.try_append(&mut av3).is_some());"] # [doc = " assert_eq!(av, &[1, 2, 3, 4, 5, 6][..]);"] # [doc = " assert_eq!(av3, &[7, 8, 9][..]);"] # [doc = " ```"] # [inline] pub fn try_append < 'other > (& mut self , other : & 'other mut Self ,) -> Option < & 'other mut Self > { let new_len = self . len () + other . len () ; if new_len > A :: CAPACITY { return Some (other) ; } let iter = other . iter_mut () . map (core :: mem :: take) ; for item in iter { self . push (item) ; } other . set_len (0) ; return None ; } # [doc = " A `*mut` pointer to the backing array."] # [doc = ""] # [doc = " ## Safety"] # [doc = ""] # [doc = " This pointer has provenance over the _entire_ backing array."] # [inline (always)] # [must_use] pub fn as_mut_ptr (& mut self) -> * mut A :: Item { self . data . as_slice_mut () . as_mut_ptr () } # [doc = " Performs a `deref_mut`, into unique slice form."] # [inline (always)] # [must_use] pub fn as_mut_slice (& mut self) -> & mut [A :: Item] { self . deref_mut () } # [doc = " A `*const` pointer to the backing array."] # [doc = ""] # [doc = " ## Safety"] # [doc = ""] # [doc = " This pointer has provenance over the _entire_ backing array."] # [inline (always)] # [must_use] pub fn as_ptr (& self) -> * const A :: Item { self . data . as_slice () . as_ptr () } # [doc = " Performs a `deref`, into shared slice form."] # [inline (always)] # [must_use] pub fn as_slice (& self) -> & [A :: Item] { self . deref () } # [doc = " The capacity of the `ArrayVec`."] # [doc = ""] # [doc = " This is fixed based on the array type, but can't yet be made a `const fn`"] # [doc = " on Stable Rust."] # [inline (always)] # [must_use] pub fn capacity (& self) -> usize { self . data . as_slice () . len () . min (u16 :: MAX as usize) } # [doc = " Truncates the `ArrayVec` down to length 0."] # [inline (always)] pub fn clear (& mut self) { self . truncate (0) } # [doc = " Creates a draining iterator that removes the specified range in the vector"] # [doc = " and yields the removed items."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If the start is greater than the end"] # [doc = " * If the end is past the edge of the vec."] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut av = array_vec!([i32; 4] => 1, 2, 3);"] # [doc = " let av2: ArrayVec<[i32; 4]> = av.drain(1..).collect();"] # [doc = " assert_eq!(av.as_slice(), &[1][..]);"] # [doc = " assert_eq!(av2.as_slice(), &[2, 3][..]);"] # [doc = ""] # [doc = " av.drain(..);"] # [doc = " assert_eq!(av.as_slice(), &[]);"] # [doc = " ```"] # [inline] pub fn drain < R > (& mut self , range : R) -> ArrayVecDrain < '_ , A :: Item > where R : RangeBounds < usize > , { ArrayVecDrain :: new (self , range) } # [doc = " Returns the inner array of the `ArrayVec`."] # [doc = ""] # [doc = " This returns the full array, even if the `ArrayVec` length is currently"] # [doc = " less than that."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::{array_vec, ArrayVec};"] # [doc = " let mut favorite_numbers = array_vec!([i32; 5] => 87, 48, 33, 9, 26);"] # [doc = " assert_eq!(favorite_numbers.clone().into_inner(), [87, 48, 33, 9, 26]);"] # [doc = ""] # [doc = " favorite_numbers.pop();"] # [doc = " assert_eq!(favorite_numbers.into_inner(), [87, 48, 33, 9, 0]);"] # [doc = " ```"] # [doc = ""] # [doc = " A use for this function is to build an array from an iterator by first"] # [doc = " collecting it into an `ArrayVec`."] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::ArrayVec;"] # [doc = " let arr_vec: ArrayVec<[i32; 10]> = (1..=3).cycle().take(10).collect();"] # [doc = " let inner = arr_vec.into_inner();"] # [doc = " assert_eq!(inner, [1, 2, 3, 1, 2, 3, 1, 2, 3, 1]);"] # [doc = " ```"] # [inline] pub fn into_inner (self) -> A { self . data } # [doc = " Clone each element of the slice into this `ArrayVec`."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If the `ArrayVec` would overflow, this will panic."] # [inline] pub fn extend_from_slice (& mut self , sli : & [A :: Item]) where A :: Item : Clone , { if sli . is_empty () { return ; } let new_len = self . len as usize + sli . len () ; assert ! (new_len <= A :: CAPACITY , "ArrayVec::extend_from_slice> total length {} exceeds capacity {}!" , new_len , A :: CAPACITY) ; let target = & mut self . data . as_slice_mut () [self . len as usize .. new_len] ; target . clone_from_slice (sli) ; self . set_len (new_len) ; } # [doc = " Fill the vector until its capacity has been reached."] # [doc = ""] # [doc = " Successively fills unused space in the spare slice of the vector with"] # [doc = " elements from the iterator. It then returns the remaining iterator"] # [doc = " without exhausting it. This also allows appending the head of an"] # [doc = " infinite iterator."] # [doc = ""] # [doc = " This is an alternative to `Extend::extend` method for cases where the"] # [doc = " length of the iterator can not be checked. Since this vector can not"] # [doc = " reallocate to increase its capacity, it is unclear what to do with"] # [doc = " remaining elements in the iterator and the iterator itself. The"] # [doc = " interface also provides no way to communicate this to the caller."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If the `next` method of the provided iterator panics."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut av = array_vec!([i32; 4]);"] # [doc = " let mut to_inf = av.fill(0..);"] # [doc = " assert_eq!(&av[..], [0, 1, 2, 3]);"] # [doc = " assert_eq!(to_inf.next(), Some(4));"] # [doc = " ```"] # [inline] pub fn fill < I : IntoIterator < Item = A :: Item > > (& mut self , iter : I ,) -> I :: IntoIter { let mut iter = iter . into_iter () ; let mut pushed = 0 ; let to_take = self . capacity () - self . len () ; let target = & mut self . data . as_slice_mut () [self . len as usize ..] ; for element in iter . by_ref () . take (to_take) { target [pushed] = element ; pushed += 1 ; } self . len += pushed as u16 ; iter } # [doc = " Wraps up an array and uses the given length as the initial length."] # [doc = ""] # [doc = " If you want to simply use the full array, use `from` instead."] # [doc = ""] # [doc = " ## Panics"] # [doc = ""] # [doc = " * The length specified must be less than or equal to the capacity of the"] # [doc = "   array."] # [inline] # [must_use] # [allow (clippy :: match_wild_err_arm)] pub fn from_array_len (data : A , len : usize) -> Self { match Self :: try_from_array_len (data , len) { Ok (out) => out , Err (_) => panic ! ("ArrayVec::from_array_len> length {} exceeds capacity {}!" , len , A :: CAPACITY) , } } # [doc = " Inserts an item at the position given, moving all following elements +1"] # [doc = " index."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If `index` > `len`"] # [doc = " * If the capacity is exhausted"] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " use tinyvec::*;"] # [doc = " let mut av = array_vec!([i32; 10] => 1, 2, 3);"] # [doc = " av.insert(1, 4);"] # [doc = " assert_eq!(av.as_slice(), &[1, 4, 2, 3]);"] # [doc = " av.insert(4, 5);"] # [doc = " assert_eq!(av.as_slice(), &[1, 4, 2, 3, 5]);"] # [doc = " ```"] # [inline] pub fn insert (& mut self , index : usize , item : A :: Item) { let x = self . try_insert (index , item) ; assert ! (x . is_none () , "ArrayVec::insert> capacity overflow!") ; } # [doc = " Tries to insert an item at the position given, moving all following"] # [doc = " elements +1 index."] # [doc = " Returns back the element if the capacity is exhausted,"] # [doc = " otherwise returns None."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If `index` > `len`"] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " use tinyvec::*;"] # [doc = " let mut av = array_vec!([&'static str; 4] => \"one\", \"two\", \"three\");"] # [doc = " av.insert(1, \"four\");"] # [doc = " assert_eq!(av.as_slice(), &[\"one\", \"four\", \"two\", \"three\"]);"] # [doc = " assert_eq!(av.try_insert(4, \"five\"), Some(\"five\"));"] # [doc = " ```"] # [inline] pub fn try_insert (& mut self , index : usize , mut item : A :: Item ,) -> Option < A :: Item > { assert ! (index <= self . len as usize , "ArrayVec::try_insert> index {} is out of bounds {}" , index , self . len) ; if (self . len as usize) < A :: CAPACITY { self . len += 1 ; } else { return Some (item) ; } let target = & mut self . as_mut_slice () [index ..] ; # [allow (clippy :: needless_range_loop)] for i in 0 .. target . len () { core :: mem :: swap (& mut item , & mut target [i]) ; } return None ; } # [doc = " Checks if the length is 0."] # [inline (always)] # [must_use] pub fn is_empty (& self) -> bool { self . len == 0 } # [doc = " The length of the `ArrayVec` (in elements)."] # [inline (always)] # [must_use] pub fn len (& self) -> usize { self . len as usize } # [doc = " Makes a new, empty `ArrayVec`."] # [inline (always)] # [must_use] pub fn new () -> Self { Self :: default () } # [doc = " Remove and return the last element of the vec, if there is one."] # [doc = ""] # [doc = " ## Failure"] # [doc = " * If the vec is empty you get `None`."] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut av = array_vec!([i32; 10] => 1, 2);"] # [doc = " assert_eq!(av.pop(), Some(2));"] # [doc = " assert_eq!(av.pop(), Some(1));"] # [doc = " assert_eq!(av.pop(), None);"] # [doc = " ```"] # [inline] pub fn pop (& mut self) -> Option < A :: Item > { if self . len > 0 { self . len -= 1 ; let out = core :: mem :: take (& mut self . data . as_slice_mut () [self . len as usize]) ; Some (out) } else { None } } # [doc = " Place an element onto the end of the vec."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If the length of the vec would overflow the capacity."] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut av = array_vec!([i32; 2]);"] # [doc = " assert_eq!(&av[..], []);"] # [doc = " av.push(1);"] # [doc = " assert_eq!(&av[..], [1]);"] # [doc = " av.push(2);"] # [doc = " assert_eq!(&av[..], [1, 2]);"] # [doc = " // av.push(3); this would overflow the ArrayVec and panic!"] # [doc = " ```"] # [inline (always)] pub fn push (& mut self , val : A :: Item) { let x = self . try_push (val) ; assert ! (x . is_none () , "ArrayVec::push> capacity overflow!") ; } # [doc = " Tries to place an element onto the end of the vec.\\"] # [doc = " Returns back the element if the capacity is exhausted,"] # [doc = " otherwise returns None."] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut av = array_vec!([i32; 2]);"] # [doc = " assert_eq!(av.as_slice(), []);"] # [doc = " assert_eq!(av.try_push(1), None);"] # [doc = " assert_eq!(&av[..], [1]);"] # [doc = " assert_eq!(av.try_push(2), None);"] # [doc = " assert_eq!(&av[..], [1, 2]);"] # [doc = " assert_eq!(av.try_push(3), Some(3));"] # [doc = " ```"] # [inline (always)] pub fn try_push (& mut self , val : A :: Item) -> Option < A :: Item > { debug_assert ! (self . len as usize <= A :: CAPACITY) ; let itemref = match self . data . as_slice_mut () . get_mut (self . len as usize) { None => return Some (val) , Some (x) => x , } ; * itemref = val ; self . len += 1 ; return None ; } # [doc = " Removes the item at `index`, shifting all others down by one index."] # [doc = ""] # [doc = " Returns the removed element."] # [doc = ""] # [doc = " ## Panics"] # [doc = ""] # [doc = " * If the index is out of bounds."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut av = array_vec!([i32; 4] => 1, 2, 3);"] # [doc = " assert_eq!(av.remove(1), 2);"] # [doc = " assert_eq!(&av[..], [1, 3]);"] # [doc = " ```"] # [inline] pub fn remove (& mut self , index : usize) -> A :: Item { let targets : & mut [A :: Item] = & mut self . deref_mut () [index ..] ; let item = core :: mem :: take (& mut targets [0]) ; for i in 0 .. targets . len () - 1 { targets . swap (i , i + 1) ; } self . len -= 1 ; item } # [doc = " As [`resize_with`](ArrayVec::resize_with)"] # [doc = " and it clones the value as the closure."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = ""] # [doc = " let mut av = array_vec!([&str; 10] => \"hello\");"] # [doc = " av.resize(3, \"world\");"] # [doc = " assert_eq!(&av[..], [\"hello\", \"world\", \"world\"]);"] # [doc = ""] # [doc = " let mut av = array_vec!([i32; 10] => 1, 2, 3, 4);"] # [doc = " av.resize(2, 0);"] # [doc = " assert_eq!(&av[..], [1, 2]);"] # [doc = " ```"] # [inline] pub fn resize (& mut self , new_len : usize , new_val : A :: Item) where A :: Item : Clone , { self . resize_with (new_len , | | new_val . clone ()) } # [doc = " Resize the vec to the new length."] # [doc = ""] # [doc = " If it needs to be longer, it's filled with repeated calls to the provided"] # [doc = " function. If it needs to be shorter, it's truncated."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = ""] # [doc = " let mut av = array_vec!([i32; 10] => 1, 2, 3);"] # [doc = " av.resize_with(5, Default::default);"] # [doc = " assert_eq!(&av[..], [1, 2, 3, 0, 0]);"] # [doc = ""] # [doc = " let mut av = array_vec!([i32; 10]);"] # [doc = " let mut p = 1;"] # [doc = " av.resize_with(4, || {"] # [doc = "   p *= 2;"] # [doc = "   p"] # [doc = " });"] # [doc = " assert_eq!(&av[..], [2, 4, 8, 16]);"] # [doc = " ```"] # [inline] pub fn resize_with < F : FnMut () -> A :: Item > (& mut self , new_len : usize , mut f : F ,) { match new_len . checked_sub (self . len as usize) { None => self . truncate (new_len) , Some (new_elements) => { for _ in 0 .. new_elements { self . push (f ()) ; } } } } # [doc = " Walk the vec and keep only the elements that pass the predicate given."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = ""] # [doc = " let mut av = array_vec!([i32; 10] => 1, 1, 2, 3, 3, 4);"] # [doc = " av.retain(|&x| x % 2 == 0);"] # [doc = " assert_eq!(&av[..], [2, 4]);"] # [doc = " ```"] # [inline] pub fn retain < F : FnMut (& A :: Item) -> bool > (& mut self , mut acceptable : F) { struct JoinOnDrop < 'vec , Item > { items : & 'vec mut [Item] , done_end : usize , tail_start : usize , } impl < Item > Drop for JoinOnDrop < '_ , Item > { fn drop (& mut self) { self . items [self . done_end ..] . rotate_left (self . tail_start) ; } } let mut rest = JoinOnDrop { items : & mut self . data . as_slice_mut () [.. self . len as usize] , done_end : 0 , tail_start : 0 , } ; let len = self . len as usize ; for idx in 0 .. len { if ! acceptable (& rest . items [idx]) { let _ = core :: mem :: take (& mut rest . items [idx]) ; self . len -= 1 ; rest . tail_start += 1 ; } else { rest . items . swap (rest . done_end , idx) ; rest . done_end += 1 ; } } } # [doc = " Retains only the elements specified by the predicate, passing a mutable"] # [doc = " reference to it."] # [doc = ""] # [doc = " In other words, remove all elements e such that f(&mut e) returns false."] # [doc = " This method operates in place, visiting each element exactly once in the"] # [doc = " original order, and preserves the order of the retained elements."] # [doc = ""] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = ""] # [doc = " let mut av = array_vec!([i32; 10] => 1, 1, 2, 3, 3, 4);"] # [doc = " av.retain_mut(|x| if *x % 2 == 0 { *x *= 2; true } else { false });"] # [doc = " assert_eq!(&av[..], [4, 8]);"] # [doc = " ```"] # [inline] pub fn retain_mut < F > (& mut self , mut acceptable : F) where F : FnMut (& mut A :: Item) -> bool , { struct JoinOnDrop < 'vec , Item > { items : & 'vec mut [Item] , done_end : usize , tail_start : usize , } impl < Item > Drop for JoinOnDrop < '_ , Item > { fn drop (& mut self) { self . items [self . done_end ..] . rotate_left (self . tail_start) ; } } let mut rest = JoinOnDrop { items : & mut self . data . as_slice_mut () [.. self . len as usize] , done_end : 0 , tail_start : 0 , } ; let len = self . len as usize ; for idx in 0 .. len { if ! acceptable (& mut rest . items [idx]) { let _ = core :: mem :: take (& mut rest . items [idx]) ; self . len -= 1 ; rest . tail_start += 1 ; } else { rest . items . swap (rest . done_end , idx) ; rest . done_end += 1 ; } } } # [doc = " Forces the length of the vector to `new_len`."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If `new_len` is greater than the vec's capacity."] # [doc = ""] # [doc = " ## Safety"] # [doc = " * This is a fully safe operation! The inactive memory already counts as"] # [doc = "   \"initialized\" by Rust's rules."] # [doc = " * Other than \"the memory is initialized\" there are no other guarantees"] # [doc = "   regarding what you find in the inactive portion of the vec."] # [inline (always)] pub fn set_len (& mut self , new_len : usize) { if new_len > A :: CAPACITY { panic ! ("ArrayVec::set_len> new length {} exceeds capacity {}" , new_len , A :: CAPACITY) } let new_len : u16 = new_len . try_into () . expect ("ArrayVec::set_len> new length is not in range 0..=u16::MAX") ; self . len = new_len ; } # [doc = " Splits the collection at the point given."] # [doc = ""] # [doc = " * `[0, at)` stays in this vec"] # [doc = " * `[at, len)` ends up in the new vec."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * if at > len"] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut av = array_vec!([i32; 4] => 1, 2, 3);"] # [doc = " let av2 = av.split_off(1);"] # [doc = " assert_eq!(&av[..], [1]);"] # [doc = " assert_eq!(&av2[..], [2, 3]);"] # [doc = " ```"] # [inline] pub fn split_off (& mut self , at : usize) -> Self { if at > self . len () { panic ! ("ArrayVec::split_off> at value {} exceeds length of {}" , at , self . len) ; } let mut new = Self :: default () ; let moves = & mut self . as_mut_slice () [at ..] ; let split_len = moves . len () ; let targets = & mut new . data . as_slice_mut () [.. split_len] ; moves . swap_with_slice (targets) ; new . len = split_len as u16 ; self . len = at as u16 ; new } # [doc = " Creates a splicing iterator that removes the specified range in the"] # [doc = " vector, yields the removed items, and replaces them with elements from"] # [doc = " the provided iterator."] # [doc = ""] # [doc = " `splice` fuses the provided iterator, so elements after the first `None`"] # [doc = " are ignored."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If the start is greater than the end."] # [doc = " * If the end is past the edge of the vec."] # [doc = " * If the provided iterator panics."] # [doc = " * If the new length would overflow the capacity of the array. Because"] # [doc = "   `ArrayVecSplice` adds elements to this vec in its destructor when"] # [doc = "   necessary, this panic would occur when it is dropped."] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " use tinyvec::*;"] # [doc = " let mut av = array_vec!([i32; 4] => 1, 2, 3);"] # [doc = " let av2: ArrayVec<[i32; 4]> = av.splice(1.., 4..=6).collect();"] # [doc = " assert_eq!(av.as_slice(), &[1, 4, 5, 6][..]);"] # [doc = " assert_eq!(av2.as_slice(), &[2, 3][..]);"] # [doc = ""] # [doc = " av.splice(.., None);"] # [doc = " assert_eq!(av.as_slice(), &[]);"] # [doc = " ```"] # [inline] pub fn splice < R , I > (& mut self , range : R , replacement : I ,) -> ArrayVecSplice < '_ , A , core :: iter :: Fuse < I :: IntoIter > > where R : RangeBounds < usize > , I : IntoIterator < Item = A :: Item > , { use core :: ops :: Bound ; let start = match range . start_bound () { Bound :: Included (x) => * x , Bound :: Excluded (x) => x . saturating_add (1) , Bound :: Unbounded => 0 , } ; let end = match range . end_bound () { Bound :: Included (x) => x . saturating_add (1) , Bound :: Excluded (x) => * x , Bound :: Unbounded => self . len () , } ; assert ! (start <= end , "ArrayVec::splice> Illegal range, {} to {}" , start , end) ; assert ! (end <= self . len () , "ArrayVec::splice> Range ends at {} but length is only {}!" , end , self . len ()) ; ArrayVecSplice { removal_start : start , removal_end : end , parent : self , replacement : replacement . into_iter () . fuse () , } } # [doc = " Remove an element, swapping the end of the vec into its place."] # [doc = ""] # [doc = " ## Panics"] # [doc = " * If the index is out of bounds."] # [doc = ""] # [doc = " ## Example"] # [doc = " ```rust"] # [doc = " # use tinyvec::*;"] # [doc = " let mut av = array_vec!([&str; 4] => \"foo\", \"bar\", \"quack\", \"zap\");"] # [doc = ""] # [doc = " assert_eq!(av.swap_remove(1), \"bar\");"] # [doc = " assert_eq!(&av[..], [\"foo\", \"zap\", \"quack\"]);"] # [doc = ""] # [doc = " assert_eq!(av.swap_remove(0), \"foo\");"] # [doc = " assert_eq!(&av[..], [\"quack\", \"zap\"]);"] # [doc = " ```"] # [inline] pub fn swap_remove (& mut self , index : usize) -> A :: Item { assert ! (index < self . len () , "ArrayVec::swap_remove> index {} is out of bounds {}" , index , self . len) ; if index == self . len () - 1 { self . pop () . unwrap () } else { let i = self . pop () . unwrap () ; replace (& mut self [index] , i) } } # [doc = " Reduces the vec's length to the given value."] # [doc = ""] # [doc = " If the vec is already shorter than the input, nothing happens."] # [inline] pub fn truncate (& mut self , new_len : usize) { if new_len >= self . len as usize { return ; } if needs_drop :: < A :: Item > () { let len = self . len as usize ; self . data . as_slice_mut () [new_len .. len] . iter_mut () . map (core :: mem :: take) . for_each (drop) ; } self . len = new_len as u16 ; } # [doc = " Wraps an array, using the given length as the starting length."] # [doc = ""] # [doc = " If you want to use the whole length of the array, you can just use the"] # [doc = " `From` impl."] # [doc = ""] # [doc = " ## Failure"] # [doc = ""] # [doc = " If the given length is greater than the capacity of the array this will"] # [doc = " error, and you'll get the array back in the `Err`."] # [inline] # [cfg (not (feature = "latest_stable_rust"))] pub fn try_from_array_len (data : A , len : usize) -> Result < Self , A > { if len <= A :: CAPACITY { Ok (Self { data , len : len as u16 }) } else { Err (data) } } # [doc = " Wraps an array, using the given length as the starting length."] # [doc = ""] # [doc = " If you want to use the whole length of the array, you can just use the"] # [doc = " `From` impl."] # [doc = ""] # [doc = " ## Failure"] # [doc = ""] # [doc = " If the given length is greater than the capacity of the array this will"] # [doc = " error, and you'll get the array back in the `Err`."] # [inline] # [cfg (feature = "latest_stable_rust")] pub const fn try_from_array_len (data : A , len : usize) -> Result < Self , A > { if len <= A :: CAPACITY { Ok (Self { data , len : len as u16 }) } else { Err (data) } } }
    };
}

impl_19!();