macro_rules! deps {
    () => {
        ArrayVec!();
        TinyVec!();
        Array!();
    };
}

macro_rules! impl_132 {
    () => {
        deps!();
        impl < A : Array > TinyVec < A > { # [doc = " Returns whether elements are on heap"] # [inline (always)] # [must_use] pub fn is_heap (& self) -> bool { match self { TinyVec :: Heap (_) => true , TinyVec :: Inline (_) => false , } } # [doc = " Returns whether elements are on stack"] # [inline (always)] # [must_use] pub fn is_inline (& self) -> bool { ! self . is_heap () } # [doc = " Shrinks the capacity of the vector as much as possible.\\"] # [doc = " It is inlined if length is less than `A::CAPACITY`."] # [doc = " ```rust"] # [doc = " use tinyvec::*;"] # [doc = " let mut tv = tiny_vec!([i32; 2] => 1, 2, 3);"] # [doc = " assert!(tv.is_heap());"] # [doc = " let _ = tv.pop();"] # [doc = " assert!(tv.is_heap());"] # [doc = " tv.shrink_to_fit();"] # [doc = " assert!(tv.is_inline());"] # [doc = " ```"] # [inline] pub fn shrink_to_fit (& mut self) { let vec = match self { TinyVec :: Inline (_) => return , TinyVec :: Heap (h) => h , } ; if vec . len () > A :: CAPACITY { return vec . shrink_to_fit () ; } let moved_vec = core :: mem :: take (vec) ; let mut av = ArrayVec :: default () ; let mut rest = av . fill (moved_vec) ; debug_assert ! (rest . next () . is_none ()) ; * self = TinyVec :: Inline (av) ; } # [doc = " Moves the content of the TinyVec to the heap, if it's inline."] # [doc = " ```rust"] # [doc = " use tinyvec::*;"] # [doc = " let mut tv = tiny_vec!([i32; 4] => 1, 2, 3);"] # [doc = " assert!(tv.is_inline());"] # [doc = " tv.move_to_the_heap();"] # [doc = " assert!(tv.is_heap());"] # [doc = " ```"] # [allow (clippy :: missing_inline_in_public_items)] pub fn move_to_the_heap (& mut self) { let arr = match self { TinyVec :: Heap (_) => return , TinyVec :: Inline (a) => a , } ; let v = arr . drain_to_vec () ; * self = TinyVec :: Heap (v) ; } # [doc = " Tries to move the content of the TinyVec to the heap, if it's inline."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the allocator reports a failure, then an error is returned and the"] # [doc = " content is kept on the stack."] # [doc = ""] # [doc = " ```rust"] # [doc = " use tinyvec::*;"] # [doc = " let mut tv = tiny_vec!([i32; 4] => 1, 2, 3);"] # [doc = " assert!(tv.is_inline());"] # [doc = " assert_eq!(Ok(()), tv.try_move_to_the_heap());"] # [doc = " assert!(tv.is_heap());"] # [doc = " ```"] # [inline] # [cfg (feature = "rustc_1_57")] pub fn try_move_to_the_heap (& mut self) -> Result < () , TryReserveError > { let arr = match self { TinyVec :: Heap (_) => return Ok (()) , TinyVec :: Inline (a) => a , } ; let v = arr . try_drain_to_vec () ? ; * self = TinyVec :: Heap (v) ; return Ok (()) ; } # [doc = " If TinyVec is inline, moves the content of it to the heap."] # [doc = " Also reserves additional space."] # [doc = " ```rust"] # [doc = " use tinyvec::*;"] # [doc = " let mut tv = tiny_vec!([i32; 4] => 1, 2, 3);"] # [doc = " assert!(tv.is_inline());"] # [doc = " tv.move_to_the_heap_and_reserve(32);"] # [doc = " assert!(tv.is_heap());"] # [doc = " assert!(tv.capacity() >= 35);"] # [doc = " ```"] # [inline] pub fn move_to_the_heap_and_reserve (& mut self , n : usize) { let arr = match self { TinyVec :: Heap (h) => return h . reserve (n) , TinyVec :: Inline (a) => a , } ; let v = arr . drain_to_vec_and_reserve (n) ; * self = TinyVec :: Heap (v) ; } # [doc = " If TinyVec is inline, try to move the content of it to the heap."] # [doc = " Also reserves additional space."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the allocator reports a failure, then an error is returned."] # [doc = ""] # [doc = " ```rust"] # [doc = " use tinyvec::*;"] # [doc = " let mut tv = tiny_vec!([i32; 4] => 1, 2, 3);"] # [doc = " assert!(tv.is_inline());"] # [doc = " assert_eq!(Ok(()), tv.try_move_to_the_heap_and_reserve(32));"] # [doc = " assert!(tv.is_heap());"] # [doc = " assert!(tv.capacity() >= 35);"] # [doc = " ```"] # [inline] # [cfg (feature = "rustc_1_57")] pub fn try_move_to_the_heap_and_reserve (& mut self , n : usize ,) -> Result < () , TryReserveError > { let arr = match self { TinyVec :: Heap (h) => return h . try_reserve (n) , TinyVec :: Inline (a) => a , } ; let v = arr . try_drain_to_vec_and_reserve (n) ? ; * self = TinyVec :: Heap (v) ; return Ok (()) ; } # [doc = " Reserves additional space."] # [doc = " Moves to the heap if array can't hold `n` more items"] # [doc = " ```rust"] # [doc = " use tinyvec::*;"] # [doc = " let mut tv = tiny_vec!([i32; 4] => 1, 2, 3, 4);"] # [doc = " assert!(tv.is_inline());"] # [doc = " tv.reserve(1);"] # [doc = " assert!(tv.is_heap());"] # [doc = " assert!(tv.capacity() >= 5);"] # [doc = " ```"] # [inline] pub fn reserve (& mut self , n : usize) { let arr = match self { TinyVec :: Heap (h) => return h . reserve (n) , TinyVec :: Inline (a) => a , } ; if n > arr . capacity () - arr . len () { let v = arr . drain_to_vec_and_reserve (n) ; * self = TinyVec :: Heap (v) ; } return ; } # [doc = " Tries to reserve additional space."] # [doc = " Moves to the heap if array can't hold `n` more items."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the allocator reports a failure, then an error is returned."] # [doc = ""] # [doc = " ```rust"] # [doc = " use tinyvec::*;"] # [doc = " let mut tv = tiny_vec!([i32; 4] => 1, 2, 3, 4);"] # [doc = " assert!(tv.is_inline());"] # [doc = " assert_eq!(Ok(()), tv.try_reserve(1));"] # [doc = " assert!(tv.is_heap());"] # [doc = " assert!(tv.capacity() >= 5);"] # [doc = " ```"] # [inline] # [cfg (feature = "rustc_1_57")] pub fn try_reserve (& mut self , n : usize) -> Result < () , TryReserveError > { let arr = match self { TinyVec :: Heap (h) => return h . try_reserve (n) , TinyVec :: Inline (a) => a , } ; if n > arr . capacity () - arr . len () { let v = arr . try_drain_to_vec_and_reserve (n) ? ; * self = TinyVec :: Heap (v) ; } return Ok (()) ; } # [doc = " Reserves additional space."] # [doc = " Moves to the heap if array can't hold `n` more items"] # [doc = ""] # [doc = " From [Vec::reserve_exact](https://doc.rust-lang.org/std/vec/struct.Vec.html#method.reserve_exact)"] # [doc = " ```text"] # [doc = " Note that the allocator may give the collection more space than it requests."] # [doc = " Therefore, capacity can not be relied upon to be precisely minimal."] # [doc = " Prefer `reserve` if future insertions are expected."] # [doc = " ```"] # [doc = " ```rust"] # [doc = " use tinyvec::*;"] # [doc = " let mut tv = tiny_vec!([i32; 4] => 1, 2, 3, 4);"] # [doc = " assert!(tv.is_inline());"] # [doc = " tv.reserve_exact(1);"] # [doc = " assert!(tv.is_heap());"] # [doc = " assert!(tv.capacity() >= 5);"] # [doc = " ```"] # [inline] pub fn reserve_exact (& mut self , n : usize) { let arr = match self { TinyVec :: Heap (h) => return h . reserve_exact (n) , TinyVec :: Inline (a) => a , } ; if n > arr . capacity () - arr . len () { let v = arr . drain_to_vec_and_reserve (n) ; * self = TinyVec :: Heap (v) ; } return ; } # [doc = " Tries to reserve additional space."] # [doc = " Moves to the heap if array can't hold `n` more items"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the allocator reports a failure, then an error is returned."] # [doc = ""] # [doc = " From [Vec::try_reserve_exact](https://doc.rust-lang.org/std/vec/struct.Vec.html#method.try_reserve_exact)"] # [doc = " ```text"] # [doc = " Note that the allocator may give the collection more space than it requests."] # [doc = " Therefore, capacity can not be relied upon to be precisely minimal."] # [doc = " Prefer `reserve` if future insertions are expected."] # [doc = " ```"] # [doc = " ```rust"] # [doc = " use tinyvec::*;"] # [doc = " let mut tv = tiny_vec!([i32; 4] => 1, 2, 3, 4);"] # [doc = " assert!(tv.is_inline());"] # [doc = " assert_eq!(Ok(()), tv.try_reserve_exact(1));"] # [doc = " assert!(tv.is_heap());"] # [doc = " assert!(tv.capacity() >= 5);"] # [doc = " ```"] # [inline] # [cfg (feature = "rustc_1_57")] pub fn try_reserve_exact (& mut self , n : usize) -> Result < () , TryReserveError > { let arr = match self { TinyVec :: Heap (h) => return h . try_reserve_exact (n) , TinyVec :: Inline (a) => a , } ; if n > arr . capacity () - arr . len () { let v = arr . try_drain_to_vec_and_reserve (n) ? ; * self = TinyVec :: Heap (v) ; } return Ok (()) ; } # [doc = " Makes a new TinyVec with _at least_ the given capacity."] # [doc = ""] # [doc = " If the requested capacity is less than or equal to the array capacity you"] # [doc = " get an inline vec. If it's greater than you get a heap vec."] # [doc = " ```"] # [doc = " # use tinyvec::*;"] # [doc = " let t = TinyVec::<[u8; 10]>::with_capacity(5);"] # [doc = " assert!(t.is_inline());"] # [doc = " assert!(t.capacity() >= 5);"] # [doc = ""] # [doc = " let t = TinyVec::<[u8; 10]>::with_capacity(20);"] # [doc = " assert!(t.is_heap());"] # [doc = " assert!(t.capacity() >= 20);"] # [doc = " ```"] # [inline] # [must_use] pub fn with_capacity (cap : usize) -> Self { if cap <= A :: CAPACITY { TinyVec :: Inline (ArrayVec :: default ()) } else { TinyVec :: Heap (Vec :: with_capacity (cap)) } } # [doc = " Converts a `TinyVec<[T; N]>` into a `Box<[T]>`."] # [doc = ""] # [doc = " - For `TinyVec::Heap(Vec<T>)`, it takes the `Vec<T>` and converts it into"] # [doc = "   a `Box<[T]>` without heap reallocation."] # [doc = " - For `TinyVec::Inline(inner_data)`, it first converts the `inner_data` to"] # [doc = "   `Vec<T>`, then into a `Box<[T]>`. Requiring only a single heap"] # [doc = "   allocation."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```"] # [doc = " use core::mem::size_of_val as mem_size_of;"] # [doc = " use tinyvec::TinyVec;"] # [doc = ""] # [doc = " // Initialize TinyVec with 256 elements (exceeding inline capacity)"] # [doc = " let v: TinyVec<[_; 128]> = (0u8..=255).collect();"] # [doc = ""] # [doc = " assert!(v.is_heap());"] # [doc = " assert_eq!(mem_size_of(&v), 136); // mem size of TinyVec<[u8; N]>: N+8"] # [doc = " assert_eq!(v.len(), 256);"] # [doc = ""] # [doc = " let boxed = v.into_boxed_slice();"] # [doc = " assert_eq!(mem_size_of(&boxed), 16); // mem size of Box<[u8]>: 16 bytes (fat pointer)"] # [doc = " assert_eq!(boxed.len(), 256);"] # [doc = " ```"] # [inline] # [must_use] pub fn into_boxed_slice (self) -> alloc :: boxed :: Box < [A :: Item] > { self . into_vec () . into_boxed_slice () } # [doc = " Converts a `TinyVec<[T; N]>` into a `Vec<T>`."] # [doc = ""] # [doc = " `v.into_vec()` is equivalent to `Into::<Vec<_>>::into(v)`."] # [doc = ""] # [doc = " - For `TinyVec::Inline(_)`, `.into_vec()` **does not** offer a performance"] # [doc = "   advantage over `.to_vec()`."] # [doc = " - For `TinyVec::Heap(vec_data)`, `.into_vec()` will take `vec_data`"] # [doc = "   without heap reallocation."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```"] # [doc = " use tinyvec::TinyVec;"] # [doc = ""] # [doc = " let v = TinyVec::from([0u8; 8]);"] # [doc = " let v2 = v.clone();"] # [doc = ""] # [doc = " let vec = v.into_vec();"] # [doc = " let vec2: Vec<_> = v2.into();"] # [doc = ""] # [doc = " assert_eq!(vec, vec2);"] # [doc = " ```"] # [inline] # [must_use] pub fn into_vec (self) -> Vec < A :: Item > { self . into () } }
    };
}

impl_132!();