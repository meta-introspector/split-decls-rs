// Generated macro for GraphQLType (trait)
macro_rules! Depcrate_types_baseGraphQLType {
() => {
// Module: crate::types::base
// Provides: {"GraphQLType"}
// Dependencies: {}
# [doc = " Primary trait used to expose Rust types in a GraphQL schema."] # [doc = ""] # [doc = " All of the convenience macros ultimately expand into an implementation of"] # [doc = " this trait for the given type. This can all be done manually."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Manually deriving an [object][3] is straightforward, but tedious. This is the equivalent of the"] # [doc = " `User` object as shown in the example in the documentation root:"] # [doc = " ```"] # [doc = " # use std::collections::HashMap;"] # [doc = " use juniper::{"] # [doc = "     arcstr, meta::MetaType, ArcStr, Arguments, Context, DefaultScalarValue, Executor,"] # [doc = "     ExecutionResult, FieldResult, GraphQLType, GraphQLValue, Registry,"] # [doc = " };"] # [doc = ""] # [doc = " #[derive(Debug)]"] # [doc = " struct Database { users: HashMap<String, User> }"] # [doc = " impl Context for Database {}"] # [doc = ""] # [doc = " #[derive(Debug)]"] # [doc = " struct User { id: String, name: String, friend_ids: Vec<String> }"] # [doc = ""] # [doc = " impl GraphQLType<DefaultScalarValue> for User {"] # [doc = "    fn name(_: &()) -> Option<ArcStr> {"] # [doc = "        Some(arcstr::literal!(\"User\"))"] # [doc = "    }"] # [doc = ""] # [doc = "    fn meta(_: &(), registry: &mut Registry) -> MetaType {"] # [doc = "        // First, we need to define all fields and their types on this type."] # [doc = "        //"] # [doc = "        // If we need arguments, want to implement interfaces, or want to add documentation"] # [doc = "        // strings, we can do it here."] # [doc = "        let fields = &["] # [doc = "            registry.field::<String>(arcstr::literal!(\"id\"), &()),"] # [doc = "            registry.field::<String>(arcstr::literal!(\"name\"), &()),"] # [doc = "            registry.field::<Vec<User>>(arcstr::literal!(\"friends\"), &()),"] # [doc = "        ];"] # [doc = "        registry.build_object_type::<User>(&(), fields).into_meta()"] # [doc = "    }"] # [doc = " }"] # [doc = ""] # [doc = " impl GraphQLValue<DefaultScalarValue> for User {"] # [doc = "     type Context = Database;"] # [doc = "     type TypeInfo = ();"] # [doc = ""] # [doc = "     fn type_name(&self, _: &()) -> Option<ArcStr> {"] # [doc = "         <User as GraphQLType>::name(&())"] # [doc = "     }"] # [doc = ""] # [doc = "     fn resolve_field("] # [doc = "         &self,"] # [doc = "         info: &(),"] # [doc = "         field_name: &str,"] # [doc = "         args: &Arguments,"] # [doc = "         executor: &Executor<Database>"] # [doc = "     ) -> ExecutionResult"] # [doc = "     {"] # [doc = "         // Next, we need to match the queried field name. All arms of this match statement"] # [doc = "         // return `ExecutionResult`, which makes it hard to statically verify that the type you"] # [doc = "         // pass on to `executor.resolve*` actually matches the one that you defined in `meta()`"] # [doc = "         // above."] # [doc = "         let database = executor.context();"] # [doc = "         match field_name {"] # [doc = "             // Because scalars are defined with another `Context` associated type, you must use"] # [doc = "             // `resolve_with_ctx` here to make the `executor` perform automatic type conversion"] # [doc = "             // of its argument."] # [doc = "             \"id\" => executor.resolve_with_ctx(info, &self.id),"] # [doc = "             \"name\" => executor.resolve_with_ctx(info, &self.name),"] # [doc = ""] # [doc = "             // You pass a vector of `User` objects to `executor.resolve`, and it will determine"] # [doc = "             // which fields of the sub-objects to actually resolve based on the query."] # [doc = "             // The `executor` instance keeps track of its current position in the query."] # [doc = "             \"friends\" => executor.resolve(info,"] # [doc = "                 &self.friend_ids.iter()"] # [doc = "                     .filter_map(|id| database.users.get(id))"] # [doc = "                     .collect::<Vec<_>>()"] # [doc = "             ),"] # [doc = ""] # [doc = "             // We can only reach this panic in two cases: either a mismatch between the defined"] # [doc = "             // schema in `meta()` above, or a validation failed because of a this library bug."] # [doc = "             //"] # [doc = "             // In either of those two cases, the only reasonable way out is to panic the thread."] # [doc = "             _ => panic!(\"Field {field_name} not found on type User\"),"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " [3]: https://spec.graphql.org/October2021#sec-Objects"] pub trait GraphQLType < S = DefaultScalarValue > : GraphQLValue < S > where S : ScalarValue , { # [doc = " Returns name of this [`GraphQLType`] to expose."] # [doc = ""] # [doc = " This function will be called multiple times during schema construction. It must _not_"] # [doc = " perform any calculation and _always_ return the same value."] fn name (info : & Self :: TypeInfo) -> Option < ArcStr > ; # [doc = " Returns [`MetaType`] representing this [`GraphQLType`]."] fn meta (info : & Self :: TypeInfo , registry : & mut Registry < S >) -> MetaType < S > ; }
};
}
