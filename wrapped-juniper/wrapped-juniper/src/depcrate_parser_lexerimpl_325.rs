// Generated macro for impl_325 (impl)
macro_rules! Depcrate_parser_lexerimpl_325 {
() => {
// Module: crate::parser::lexer
// Provides: {"impl_325"}
// Dependencies: {}
impl < 'a > Lexer < 'a > { # [doc (hidden)] pub fn new (source : & 'a str) -> Lexer < 'a > { Lexer { iterator : itertools :: peek_nth (source . char_indices ()) , source , length : source . len () , position : SourcePosition :: new_origin () , has_reached_eof : false , } } fn peek_char (& mut self) -> Option < (usize , char) > { assert ! (self . position . index () <= self . length) ; assert ! (! self . has_reached_eof) ; self . iterator . peek () . map (| & (idx , ch) | (idx , ch)) } fn next_char (& mut self) -> Option < (usize , char) > { assert ! (self . position . index () <= self . length) ; assert ! (! self . has_reached_eof) ; let next = self . iterator . next () ; if let Some ((_ , ch)) = next { if ch == '\n' { self . position . advance_line () ; } else { self . position . advance_col () ; } } next } fn emit_single_char (& mut self , t : Token < 'a >) -> Spanning < Token < 'a > > { assert ! (self . position . index () <= self . length) ; let start_pos = self . position ; self . next_char () . expect ("Internal error in GraphQL lexer: emit_single_char reached EOF") ; Spanning :: single_width (& start_pos , t) } # [doc = " Advances this [`Lexer`] over any [ignored] character until a non-[ignored] is met."] # [doc = ""] # [doc = " [ignored]: https://spec.graphql.org/September2025#Ignored"] fn scan_over_whitespace (& mut self) { while let Some ((_ , ch)) = self . peek_char () { match ch { '\u{FEFF}' | '\t' | ' ' | '\n' | '\r' | ',' => _ = self . next_char () , '#' => { _ = self . next_char () ; while let Some ((_ , ch)) = self . peek_char () { _ = self . next_char () ; match ch { '\r' if matches ! (self . peek_char () , Some ((_ , '\n'))) => { _ = self . next_char () ; break ; } '\n' | '\r' => break , _ => { } } } } _ => break , } } } fn scan_ellipsis (& mut self) -> LexerResult < 'a > { let start_pos = self . position ; for _ in 0 .. 3 { let (_ , ch) = self . next_char () . ok_or_else (| | { Spanning :: zero_width (& self . position , LexerError :: UnexpectedEndOfFile) }) ? ; if ch != '.' { return Err (Spanning :: zero_width (& start_pos , LexerError :: UnexpectedCharacter ('.') ,)) ; } } Ok (Spanning :: start_end (& start_pos , & self . position , Token :: Ellipsis ,)) } fn scan_name (& mut self) -> LexerResult < 'a > { let start_pos = self . position ; let (start_idx , start_ch) = self . next_char () . ok_or_else (| | Spanning :: zero_width (& self . position , LexerError :: UnexpectedEndOfFile)) ? ; assert ! (is_name_start (start_ch)) ; let mut end_idx = start_idx ; while let Some ((idx , ch)) = self . peek_char () { if is_name_cont (ch) { self . next_char () ; end_idx = idx ; } else { break ; } } Ok (Spanning :: start_end (& start_pos , & self . position , Token :: Name (& self . source [start_idx ..= end_idx]) ,)) } # [doc = " Scans a [string] by this [`Lexer`], but not a [block string]."] # [doc = ""] # [doc = " [string]: https://spec.graphql.org/September2025#StringValue"] # [doc = " [block string]: https://spec.graphql.org/September2025#BlockString"] fn scan_string (& mut self) -> LexerResult < 'a > { let start_pos = self . position ; let (start_idx , start_ch) = self . next_char () . ok_or_else (| | Spanning :: zero_width (& self . position , LexerError :: UnexpectedEndOfFile)) ? ; if start_ch != '"' { return Err (Spanning :: zero_width (& self . position , LexerError :: UnterminatedString ,)) ; } let mut escaped = false ; let mut old_pos = self . position ; while let Some ((idx , ch)) = self . next_char () { match ch { '"' | '\\' | '/' | 'b' | 'f' | 'n' | 'r' | 't' if escaped => { escaped = false ; } 'u' if escaped => { let mut code_point = self . scan_escaped_unicode (& old_pos) ? ; if code_point . is_high_surrogate () { let new_pos = self . position ; let (Some ((_ , '\\')) , Some ((_ , 'u'))) = (self . next_char () , self . next_char ()) else { return Err (Spanning :: zero_width (& old_pos , LexerError :: UnknownEscapeSequence (code_point . to_string ()) ,)) ; } ; let trailing_code_point = self . scan_escaped_unicode (& new_pos) ? ; if ! trailing_code_point . is_low_surrogate () { return Err (Spanning :: zero_width (& old_pos , LexerError :: UnknownEscapeSequence (code_point . to_string ()) ,)) ; } code_point = UnicodeCodePoint :: from_surrogate_pair (code_point , trailing_code_point) ; } _ = code_point . try_into_char () . map_err (| e | Spanning :: zero_width (& old_pos , e)) ? ; escaped = false ; } c if escaped => { return Err (Spanning :: zero_width (& old_pos , LexerError :: UnknownEscapeSequence (format ! ("\\{c}")) ,)) ; } '\\' => escaped = true , '"' if ! escaped => { return Ok (Spanning :: start_end (& start_pos , & self . position , Token :: Scalar (ScalarToken :: String (StringLiteral :: Quoted (& self . source [start_idx ..= idx] ,))) ,)) ; } '\n' | '\r' => { return Err (Spanning :: zero_width (& old_pos , LexerError :: UnterminatedString ,)) ; } _ => { } } old_pos = self . position ; } Err (Spanning :: zero_width (& self . position , LexerError :: UnterminatedString ,)) } # [doc = " Scans a [block string] by this [`Lexer`]."] # [doc = ""] # [doc = " [block string]: https://spec.graphql.org/September2025#BlockString"] fn scan_block_string (& mut self) -> LexerResult < 'a > { let start_pos = self . position ; let (start_idx , mut start_ch) = self . next_char () . ok_or_else (| | Spanning :: zero_width (& self . position , LexerError :: UnexpectedEndOfFile)) ? ; if start_ch != '"' { return Err (Spanning :: zero_width (& self . position , LexerError :: UnterminatedString ,)) ; } for _ in 0 .. 2 { (_ , start_ch) = self . next_char () . ok_or_else (| | { Spanning :: zero_width (& self . position , LexerError :: UnexpectedEndOfFile) }) ? ; if start_ch != '"' { return Err (Spanning :: zero_width (& self . position , LexerError :: UnexpectedCharacter (start_ch) ,)) ; } } let (mut quotes , mut escaped) = (0 , false) ; while let Some ((idx , ch)) = self . next_char () { match ch { '\\' => (quotes , escaped) = (0 , true) , '"' if escaped => (quotes , escaped) = (0 , false) , '"' if quotes < 2 => quotes += 1 , '"' if quotes == 2 => { return Ok (Spanning :: start_end (& start_pos , & self . position , Token :: Scalar (ScalarToken :: String (StringLiteral :: Block (& self . source [start_idx ..= idx] ,))) ,)) ; } _ => (quotes , escaped) = (0 , false) , } } Err (Spanning :: zero_width (& self . position , LexerError :: UnterminatedBlockString ,)) } # [doc = " Scans an [escaped unicode] character by this [`Lexer`]."] # [doc = ""] # [doc = " [escaped unicode]: https://spec.graphql.org/September2025#EscapedUnicode"] fn scan_escaped_unicode (& mut self , start_pos : & SourcePosition ,) -> Result < UnicodeCodePoint , Spanning < LexerError > > { let (start_idx , mut curr_ch) = self . peek_char () . ok_or_else (| | Spanning :: zero_width (& self . position , LexerError :: UnterminatedString)) ? ; let mut end_idx = start_idx ; let mut len = 0 ; let is_variable_width = curr_ch == '{' ; if is_variable_width { _ = self . next_char () ; loop { let (idx , ch) = self . next_char () . ok_or_else (| | { Spanning :: zero_width (& self . position , LexerError :: UnterminatedString) }) ? ; curr_ch = ch ; end_idx = idx ; len += 1 ; if ! curr_ch . is_alphanumeric () { break ; } } } else { for _ in 0 .. 4 { let (idx , ch) = self . next_char () . ok_or_else (| | { Spanning :: zero_width (& self . position , LexerError :: UnterminatedString) }) ? ; curr_ch = ch ; if ! curr_ch . is_alphanumeric () { break ; } end_idx = idx ; len += 1 ; } } let escape = self . source . get (start_idx ..= end_idx) . ok_or_else (| | Spanning :: zero_width (& self . position , LexerError :: UnterminatedString)) ? ; let code_point = if is_variable_width { if curr_ch != '}' { return Err (Spanning :: zero_width (start_pos , LexerError :: UnknownEscapeSequence (format ! (r"\u{}" , & escape [.. escape . len () - 1] ,)) ,)) ; } if escape . len () - 2 > 6 { return Err (Spanning :: zero_width (start_pos , LexerError :: UnknownEscapeSequence (format ! (r"\u{}" , & escape [.. escape . len ()])) ,)) ; } u32 :: from_str_radix (& escape [1 .. escape . len () - 1] , 16) } else { if len != 4 { return Err (Spanning :: zero_width (start_pos , LexerError :: UnknownEscapeSequence (format ! (r"\u{escape}")) ,)) ; } u32 :: from_str_radix (escape , 16) } . map_err (| _ | { Spanning :: zero_width (start_pos , LexerError :: UnknownEscapeSequence (format ! (r"\u{escape}")) ,) }) ? ; Ok (UnicodeCodePoint { code : code_point , is_variable_width , }) } fn scan_number (& mut self) -> LexerResult < 'a > { let start_pos = self . position ; let (start_idx , _) = self . peek_char () . ok_or_else (| | Spanning :: zero_width (& self . position , LexerError :: UnexpectedEndOfFile)) ? ; let mut last_idx = start_idx ; let mut last_char = '1' ; let mut is_float = false ; let mut end_idx = loop { if let Some ((idx , ch)) = self . peek_char () { if ch . is_ascii_digit () || (ch == '-' && last_idx == start_idx) { if ch == '0' && last_char == '0' && last_idx == start_idx { return Err (Spanning :: zero_width (& self . position , LexerError :: UnexpectedCharacter ('0') ,)) ; } self . next_char () ; last_char = ch ; } else if last_char == '-' { return Err (Spanning :: zero_width (& self . position , LexerError :: UnexpectedCharacter (ch) ,)) ; } else { break idx ; } last_idx = idx ; } else { break last_idx + 1 ; } } ; if let Some ((start_idx , '.')) = self . peek_char () { is_float = true ; let mut last_idx = start_idx ; self . next_char () ; end_idx = loop { if let Some ((idx , ch)) = self . peek_char () { if ch . is_ascii_digit () { self . next_char () ; } else if last_idx == start_idx { return Err (Spanning :: zero_width (& self . position , LexerError :: UnexpectedCharacter (ch) ,)) ; } else { break idx ; } last_idx = idx ; } else if last_idx == start_idx { return Err (Spanning :: zero_width (& self . position , LexerError :: UnexpectedEndOfFile ,)) ; } else { break last_idx + 1 ; } } ; } if let Some ((start_idx , ch)) = self . peek_char () { if ch == 'e' || ch == 'E' { is_float = true ; self . next_char () ; let mut last_idx = start_idx ; end_idx = loop { if let Some ((idx , ch)) = self . peek_char () { if ch . is_ascii_digit () || (last_idx == start_idx && (ch == '-' || ch == '+')) { self . next_char () ; } else if last_idx == start_idx { return Err (Spanning :: zero_width (& self . position , LexerError :: UnexpectedCharacter (ch) ,)) ; } else { break idx ; } last_idx = idx ; } else if last_idx == start_idx { return Err (Spanning :: zero_width (& self . position , LexerError :: UnexpectedEndOfFile ,)) ; } else { break last_idx + 1 ; } } ; } } let number = & self . source [start_idx .. end_idx] ; let end_pos = & self . position ; let token = if is_float { Token :: Scalar (ScalarToken :: Float (number)) } else { Token :: Scalar (ScalarToken :: Int (number)) } ; Ok (Spanning :: start_end (& start_pos , end_pos , token)) } }
};
}
