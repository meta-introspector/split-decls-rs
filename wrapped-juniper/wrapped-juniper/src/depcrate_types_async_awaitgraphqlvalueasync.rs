// Generated macro for GraphQLValueAsync (trait)
macro_rules! Depcrate_types_async_awaitGraphQLValueAsync {
() => {
// Module: crate::types::async_await
// Provides: {"GraphQLValueAsync"}
// Dependencies: {}
# [doc = " Extension of [`GraphQLValue`] trait with asynchronous queries/mutations resolvers."] # [doc = ""] # [doc = " Convenience macros related to asynchronous queries/mutations expand into an implementation of"] # [doc = " this trait and [`GraphQLValue`] for the given type."] pub trait GraphQLValueAsync < S = DefaultScalarValue > : GraphQLValue < S > + Sync where Self :: TypeInfo : Sync , Self :: Context : Sync , S : ScalarValue + Send + Sync , { # [doc = " Resolves the value of a single field on this [`GraphQLValueAsync`]."] # [doc = ""] # [doc = " The `arguments` object contains all the specified arguments, with default values being"] # [doc = " substituted for the ones not provided by the query."] # [doc = ""] # [doc = " The `executor` can be used to drive selections into sub-[objects][3]."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " The default implementation panics."] # [doc = ""] # [doc = " [3]: https://spec.graphql.org/October2021#sec-Objects"] fn resolve_field_async < 'a > (& 'a self , _info : & 'a Self :: TypeInfo , _field_name : & 'a str , _arguments : & 'a Arguments < S > , _executor : & 'a Executor < Self :: Context , S > ,) -> BoxFuture < 'a , ExecutionResult < S > > { panic ! ("GraphQLValueAsync::resolve_field_async() must be implemented by objects and \
             interfaces" ,) ; } # [doc = " Resolves this [`GraphQLValueAsync`] (being an [interface][1] or an [union][2]) into a"] # [doc = " concrete downstream [object][3] type."] # [doc = ""] # [doc = " Tries to resolve this [`GraphQLValueAsync`] into the provided `type_name`. If the type"] # [doc = " matches, then passes the instance along to [`Executor::resolve`]."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " The default implementation panics."] # [doc = ""] # [doc = " [1]: https://spec.graphql.org/October2021#sec-Interfaces"] # [doc = " [2]: https://spec.graphql.org/October2021#sec-Unions"] # [doc = " [3]: https://spec.graphql.org/October2021#sec-Objects"] fn resolve_into_type_async < 'a > (& 'a self , info : & 'a Self :: TypeInfo , type_name : & str , selection_set : Option < & 'a [Selection < 'a , S >] > , executor : & 'a Executor < 'a , 'a , Self :: Context , S > ,) -> BoxFuture < 'a , ExecutionResult < S > > { if self . type_name (info) . unwrap () == type_name { self . resolve_async (info , selection_set , executor) } else { panic ! ("GraphQLValueAsync::resolve_into_type_async() must be implemented by unions and \
                 interfaces" ,) ; } } # [doc = " Resolves the provided `selection_set` against this [`GraphQLValueAsync`]."] # [doc = ""] # [doc = " For non-[object][3] types, the `selection_set` will be [`None`] and the value should simply"] # [doc = " be returned."] # [doc = ""] # [doc = " For [objects][3], all fields in the `selection_set` should be resolved. The default"] # [doc = " implementation uses [`GraphQLValueAsync::resolve_field_async`] to resolve all fields,"] # [doc = " including those through a fragment expansion."] # [doc = ""] # [doc = " Since the [GraphQL spec specifies][0] that errors during field processing should result in"] # [doc = " a null-value, this might return `Ok(Null)` in case of a failure. Errors are recorded"] # [doc = " internally."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " The default implementation panics, if `selection_set` is [`None`]."] # [doc = ""] # [doc = " [0]: https://spec.graphql.org/October2021#sec-Handling-Field-Errors"] # [doc = " [3]: https://spec.graphql.org/October2021#sec-Objects"] fn resolve_async < 'a > (& 'a self , info : & 'a Self :: TypeInfo , selection_set : Option < & 'a [Selection < S >] > , executor : & 'a Executor < Self :: Context , S > ,) -> BoxFuture < 'a , ExecutionResult < S > > { if let Some (sel) = selection_set { Box :: pin (async move { Ok (resolve_selection_set_into_async (self , info , sel , executor) . await) }) } else { panic ! ("GraphQLValueAsync::resolve_async() must be implemented by non-object output types" ,) ; } } }
};
}
