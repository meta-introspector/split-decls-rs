// Generated macro for Subcommands (enum)
macro_rules! Depcrate_argsSubcommands {
() => {
// Module: crate::args
// Provides: {"Subcommands"}
// Dependencies: {}
# [derive (Debug , clap :: Subcommand)] pub enum Subcommands { # [doc = " Generate a shell script that creates a git repository containing all commits that are"] # [doc = " traversed when following a given file through the Git history just as `git blame` would."] # [doc = ""] # [doc = " This command extracts the file’s history so that blame, when run on the repository created"] # [doc = " by the script, shows the same characteristics, in particular bugs, as the original, but in"] # [doc = " a way that does not resemble the original source file's content to any greater extent than"] # [doc = " is useful and necessary."] # [doc = ""] # [doc = " Note that this should not be used to redact sensitive information. The obfuscation leaves"] # [doc = " numerous properties of the source intact, such that it may be feasible to reconstruct the"] # [doc = " input."] # [doc = ""] # [doc = " This command can also be helpful in debugging the blame algorithm itself."] # [doc = ""] # [doc = " ### Terminology"] # [doc = ""] # [doc = " A **blame history** is the set of commits that the blame algorithm, at some point, treated"] # [doc = " as potential suspects for any line in a file. It is a subset of all commits that ever"] # [doc = " changed a file in its history."] # [doc = ""] # [doc = " With respect to branches and merge commits, the **blame history** will not necessarily be"] # [doc = " identical to the file's history in the source repository. This is because the blame"] # [doc = " algorithm will stop following a file's history for branches that only touch lines for which"] # [doc = " the source has already been found. The **blame history**, thus, looks likely \"cleaner\" and"] # [doc = " \"simpler\" than the source history."] # [clap (visible_alias = "bcr")] BlameCopyRoyal { # [doc = " Don't really copy anything."] # [clap (long , short = 'n')] dry_run : bool , # [doc = " The git root whose history to extract the blame-relevant parts from."] worktree_dir : PathBuf , # [doc = " The directory into which to copy the files."] destination_dir : PathBuf , # [doc = " The directory to place assets in."] # [clap (long)] asset_dir : Option < BString > , # [doc = " The file to extract the history for."] file : std :: ffi :: OsString , # [doc = " Do not use `copy-royal` to obfuscate the content of blobs, but copy it verbatim."] # [doc = ""] # [doc = " Note that, for producing cases for the gitoxide test suite, we usually prefer only to"] # [doc = " take blobs verbatim if the source repository was purely for testing."] # [clap (long)] verbatim : bool , } , # [doc = " Copy a tree so that it diffs the same but does not resemble the original files' content to"] # [doc = " any greater extent than is useful and necessary."] # [doc = ""] # [doc = " The idea is that this preserves the patterns that are usually sufficient to reproduce cases"] # [doc = " for tests of diffs, both for making the tests work and for keeping the diffs understandable"] # [doc = " to developers working on the tests, while avoiding keeping large verbatim fragments of code"] # [doc = " based on which the test cases were created. The benefits of \"reducing\" the code to these"] # [doc = " patterns include that the original meaning and function of code will not be confused with"] # [doc = " the code of gitoxide itself, will not distract from the effects observed in their diffs,"] # [doc = " and will not inadvertently be caught up in code cleanup efforts (e.g. attempting to adjust"] # [doc = " style or fix bugs) that would make sense in code of gitoxide itself but that would subtly"] # [doc = " break data test fixtures if done on their data."] # [doc = ""] # [doc = " Note that this should not be used to redact sensitive information. The obfuscation leaves"] # [doc = " numerous properties of the source intact, such that it may be feasible to reconstruct the"] # [doc = " input."] # [clap (visible_alias = "cr")] CopyRoyal { # [doc = " Don't really copy anything."] # [clap (long , short = 'n')] dry_run : bool , # [doc = " The git root whose tracked files to copy."] worktree_dir : PathBuf , # [doc = " The directory into which to copy the files."] destination_dir : PathBuf , # [doc = " The pathspecs to determine which paths to copy from `worktree_dir`."] # [doc = ""] # [doc = " None will copy everything."] # [clap (value_parser = AsPathSpec)] patterns : Vec < gix :: pathspec :: Pattern > , } , # [doc = " Serialize a git repository as linear history while degenerating content into a shell script that reproduces it."] # [clap (visible_alias = "gts")] GitToSh { # [doc = " The amount of commits to copy from `committish`."] # [doc = ""] # [doc = " If 0, all traversable commits will be copied."] # [clap (long , short = 'c' , default_value_t = 0)] count : usize , # [doc = " Do not use `copy-royal` to degenerate information of blobs, but take blobs verbatim."] # [doc = ""] # [doc = " Note that, for producing cases for the gitoxide test suite, we usually prefer only to"] # [doc = " take blobs verbatim if the source repository was purely for testing."] # [clap (long)] verbatim : bool , # [doc = " The directory into which the blobs and tree declarations will be written."] # [clap (long , short = 'o' , default_value = ".")] output_dir : PathBuf , # [doc = " The path to the git repository to serialize."] repo_dir : PathBuf , # [doc = " The name of the directory within `output_dir` for storing blobs and trees."] name : String , # [doc = " A revspec of the commit to start the iteration from, like `@`."] # [doc = ""] # [doc = " Note that the history will be serialized, and multiple parents aren't allowed."] committish : String , # [doc = " The pathspecs to determine which paths to copy from each commit's tree."] # [doc = ""] # [doc = " None will copy everything."] # [clap (value_parser = AsPathSpec)] patterns : Vec < gix :: pathspec :: Pattern > , } , # [doc = " Take a slider file generated with the help of [diff-slider-tools] and turn it into a series"] # [doc = " of baseline diffs to be used in [slider-rs]."] # [doc = ""] # [doc = " See [make-diff-for-sliders-repo] for details."] # [doc = ""] # [doc = " [diff-slider-tools]: https://github.com/mhagger/diff-slider-tools"] # [doc = " [slider-rs]: gix-diff/tests/diff/blob/slider.rs"] # [doc = " [make-diff-for-sliders-repo]: gix-diff/tests/fixtures/make_diff_for_sliders_repo.sh"] CreateDiffCases { # [doc = " Don't really copy anything."] # [clap (long , short = 'n')] dry_run : bool , # [doc = " The `.sliders` file that contains a list of sliders."] # [clap (long)] sliders_file : PathBuf , # [doc = " The git root to extract the diff-related parts from."] # [clap (long)] worktree_dir : PathBuf , # [doc = " The directory into which to copy the files."] # [clap (long)] destination_dir : PathBuf , # [doc = " The number of sliders to generate test cases for."] # [clap (long , default_value_t = 10)] count : usize , # [doc = " The directory to place assets in."] # [clap (long)] asset_dir : Option < BString > , } , # [doc = " Check for executable bits that disagree with shebangs."] # [doc = ""] # [doc = " This checks committed and staged files, but not anything unstaged, to find shell scripts"] # [doc = " that either begin with a `#!` but not `+x` permissions, or do not begin with `#!` but do"] # [doc = " have `+x` permissions. Such mismatches are reported but not automatically corrected. Some"] # [doc = " platforms (at least Windows) do not have such permissions, but Git still represents them."] # [doc = ""] # [doc = " This currently only checks files name with an `.sh` suffix, and only operates on the"] # [doc = " current repository. Its main use is checking that fixture scripts are have correct modes."] # [clap (visible_alias = "cm")] CheckMode { } , # [doc = " Print environment variables as `NAME=value` lines."] # [doc = ""] # [doc = " It is useful to be able to observe environment variables that are set when running code"] # [doc = " with tools such as `cargo` or `cross`. Commands like `cargo run -p internal-tools -- env`"] # [doc = " include environment changes from `cargo` itself. With `cross`, changes are more extensive,"] # [doc = " due to effects of `build.env.passthrough`, container customization, and preexisting special"] # [doc = " cases in wrapper scripts shipped in default `cross` containers (such as to `LD_PRELOAD`)."] # [doc = ""] # [doc = " Since one use for checking environment variables is to investigate the effects of"] # [doc = " environments that contain variable names or values that are not valid Unicode, this avoids"] # [doc = " requiring that environment variables all be Unicode. Any name or value that is not Unicode"] # [doc = " is shown in its Rust debug representation. This is always quoted. To decrease ambiguity,"] # [doc = " any name or value containing a literal double quote or newline is also shown in its debug"] # [doc = " representation. Names and values without such content are shown literally and not quoted."] # [clap (visible_alias = "e")] Env { } , }
};
}
