// Generated macro for RngAlgorithm (enum)
macro_rules! Depcrate_test_runner_rngRngAlgorithm {
() => {
// Module: crate::test_runner::rng
// Provides: {"RngAlgorithm"}
// Dependencies: {}
# [doc = " Identifies a particular RNG algorithm supported by proptest."] # [doc = ""] # [doc = " Proptest supports dynamic configuration of algorithms to allow it to"] # [doc = " continue operating with persisted regression files and to allow the"] # [doc = " configuration to be expressed in the `Config` struct."] # [derive (Clone , Copy , Debug , PartialEq , Eq)] pub enum RngAlgorithm { # [doc = " The [XorShift](https://rust-random.github.io/rand/rand_xorshift/struct.XorShiftRng.html)"] # [doc = " algorithm. This was the default up through and including Proptest 0.9.0."] # [doc = ""] # [doc = " It is faster than ChaCha but produces lower quality randomness and has"] # [doc = " some pathological cases where it may fail to produce outputs that are"] # [doc = " random even to casual observation."] # [doc = ""] # [doc = " The seed must be exactly 16 bytes."] XorShift , # [doc = " The [ChaCha](https://rust-random.github.io/rand/rand_chacha/struct.ChaChaRng.html)"] # [doc = " algorithm. This became the default with Proptest 0.9.1."] # [doc = ""] # [doc = " The seed must be exactly 32 bytes."] ChaCha , # [doc = " This is not an actual RNG algorithm, but instead returns data directly"] # [doc = " from its \"seed\"."] # [doc = ""] # [doc = " This is useful when Proptest is being driven from some other entropy"] # [doc = " source, such as a fuzzer."] # [doc = ""] # [doc = " If the seed is depleted, the RNG will return 0s forever."] # [doc = ""] # [doc = " Note that in cases where a new RNG is to be derived from an existing"] # [doc = " one, *the data is split evenly between them*, regardless of how much"] # [doc = " entropy is actually needed. This means that combinators like"] # [doc = " `prop_perturb` and `prop_flat_map` can require extremely large inputs."] PassThrough , # [doc = " This is equivalent to the `ChaCha` RNG, with the addition that it"] # [doc = " records the bytes used to create a value."] # [doc = ""] # [doc = " This is useful when Proptest is used for fuzzing, and a corpus of"] # [doc = " initial inputs need to be created. Note that in these cases, you need"] # [doc = " to use the `TestRunner` API directly yourself instead of using the"] # [doc = " `proptest!` macro, as otherwise there is no way to obtain the bytes"] # [doc = " this captures."] Recorder , # [allow (missing_docs)] # [doc (hidden)] _NonExhaustive , }
};
}
