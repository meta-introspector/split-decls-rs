// Generated macro for CharStrategy (struct)
macro_rules! Depcrate_charCharStrategy {
() => {
// Module: crate::char
// Provides: {"CharStrategy"}
// Dependencies: {}
# [doc = " Strategy for generating `char`s."] # [doc = ""] # [doc = " Character selection is more sophisticated than integer selection. Naïve"] # [doc = " selection (particularly in the larger context of generating strings) would"] # [doc = " result in starting inputs like `ꂡ螧轎ቶᢹ糦狥芹ᘆ㶏曊ᒀ踔虙ჲ` and \"simplified\""] # [doc = " inputs consisting mostly of control characters. It also has difficulty"] # [doc = " locating edge cases, since the vast majority of code points (such as the"] # [doc = " enormous CJK regions) don't cause problems for anything with even basic"] # [doc = " Unicode support."] # [doc = ""] # [doc = " Instead, character selection is always based on explicit ranges, and is"] # [doc = " designed to bias to specifically chosen characters and character ranges to"] # [doc = " produce inputs that are both more useful and easier for humans to"] # [doc = " understand. There are also hard-wired simplification targets based on ASCII"] # [doc = " instead of simply simplifying towards NUL to avoid problematic inputs being"] # [doc = " reduced to a bunch of NUL characters."] # [doc = ""] # [doc = " Shrinking never crosses ranges. If you have a complex range like `[A-Za-z]`"] # [doc = " and the starting point `x` is chosen, it will not shrink to the first `A-Z`"] # [doc = " group, but rather simply to `a`."] # [doc = ""] # [doc = " The usual way to get instances of this class is with the module-level `ANY`"] # [doc = " constant or `range` function. Directly constructing a `CharStrategy` is"] # [doc = " only necessary for complex ranges or to override the default biases."] # [derive (Debug , Clone)] # [must_use = "strategies do nothing unless used"] pub struct CharStrategy < 'a > { special : Cow < 'a , [char] > , preferred : Cow < 'a , [CharRange] > , ranges : Cow < 'a , [CharRange] > , }
};
}
