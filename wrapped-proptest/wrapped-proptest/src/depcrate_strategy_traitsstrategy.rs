// Generated macro for Strategy (trait)
macro_rules! Depcrate_strategy_traitsStrategy {
() => {
// Module: crate::strategy::traits
// Provides: {"Strategy"}
// Dependencies: {}
# [doc = " A strategy for producing arbitrary values of a given type."] # [doc = ""] # [doc = " `fmt::Debug` is a hard requirement for all strategies currently due to"] # [doc = " `prop_flat_map()`. This constraint will be removed when specialisation"] # [doc = " becomes stable."] # [must_use = "strategies do nothing unless used"] pub trait Strategy : fmt :: Debug { # [doc = " The value tree generated by this `Strategy`."] type Tree : ValueTree < Value = Self :: Value > ; # [doc = " The type of value used by functions under test generated by this Strategy."] # [doc = ""] # [doc = " This corresponds to the same type as the associated type `Value`"] # [doc = " in `Self::Tree`. It is provided here to simplify usage particularly"] # [doc = " in conjunction with `-> impl Strategy<Value = MyType>`."] type Value : fmt :: Debug ; # [doc = " Generate a new value tree from the given runner."] # [doc = ""] # [doc = " This may fail if there are constraints on the generated value and the"] # [doc = " generator is unable to produce anything that satisfies them. Any"] # [doc = " failure is wrapped in `TestError::Abort`."] # [doc = ""] # [doc = " This method is generally expected to be deterministic. That is, given a"] # [doc = " `TestRunner` with its RNG in a particular state, this should produce an"] # [doc = " identical `ValueTree` every time. Non-deterministic strategies do not"] # [doc = " cause problems during normal operation, but they do break failure"] # [doc = " persistence since it is implemented by simply saving the seed used to"] # [doc = " generate the test case."] fn new_tree (& self , runner : & mut TestRunner) -> NewTree < Self > ; # [doc = " Returns a strategy which produces values transformed by the function"] # [doc = " `fun`."] # [doc = ""] # [doc = " There is no need (or possibility, for that matter) to define how the"] # [doc = " output is to be shrunken. Shrinking continues to take place in terms of"] # [doc = " the source value."] # [doc = ""] # [doc = " `fun` should be a deterministic function. That is, for a given input"] # [doc = " value, it should produce an equivalent output value on every call."] # [doc = " Proptest assumes that it can call the function as many times as needed"] # [doc = " to generate as many identical values as needed. For this reason, `F` is"] # [doc = " `Fn` rather than `FnMut`."] fn prop_map < O : fmt :: Debug , F : Fn (Self :: Value) -> O > (self , fun : F ,) -> Map < Self , F > where Self : Sized , { Map { source : self , fun : Arc :: new (fun) , } } # [doc = " Returns a strategy which produces values of type `O` by transforming"] # [doc = " `Self` with `Into<O>`."] # [doc = ""] # [doc = " You should always prefer this operation instead of `prop_map` when"] # [doc = " you can as it is both clearer and also currently more efficient."] # [doc = ""] # [doc = " There is no need (or possibility, for that matter) to define how the"] # [doc = " output is to be shrunken. Shrinking continues to take place in terms of"] # [doc = " the source value."] fn prop_map_into < O : fmt :: Debug > (self) -> MapInto < Self , O > where Self : Sized , Self :: Value : Into < O > , { MapInto :: new (self) } # [doc = " Returns a strategy which produces values transformed by the function"] # [doc = " `fun`, which is additionally given a random number generator."] # [doc = ""] # [doc = " This is exactly like `prop_map()` except for the addition of the second"] # [doc = " argument to the function. This allows introducing chaotic variations to"] # [doc = " generated values that are not easily expressed otherwise while allowing"] # [doc = " shrinking to proceed reasonably."] # [doc = ""] # [doc = " During shrinking, `fun` is always called with an identical random"] # [doc = " number generator, so if it is a pure function it will always perform"] # [doc = " the same perturbation."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```"] # [doc = " // The prelude also gets us the `Rng` trait."] # [doc = " use proptest::prelude::*;"] # [doc = ""] # [doc = " proptest! {"] # [doc = "   #[test]"] # [doc = "   fn test_something(a in (0i32..10).prop_perturb("] # [doc = "       // Perturb the integer `a` (range 0..10) to a pair of that"] # [doc = "       // integer and another that's ± 10 of it."] # [doc = "       // Note that this particular case would be better implemented as"] # [doc = "       // `(0i32..10, -10i32..10).prop_map(|(a, b)| (a, a + b))`"] # [doc = "       // but is shown here for simplicity."] # [doc = "       |centre, rng| (centre, centre + rng.random_range(-10, 10))))"] # [doc = "   {"] # [doc = "       // Test stuff"] # [doc = "   }"] # [doc = " }"] # [doc = " # fn main() { }"] # [doc = " ```"] fn prop_perturb < O : fmt :: Debug , F : Fn (Self :: Value , TestRng) -> O > (self , fun : F ,) -> Perturb < Self , F > where Self : Sized , { Perturb { source : self , fun : Arc :: new (fun) , } } # [doc = " Maps values produced by this strategy into new strategies and picks"] # [doc = " values from those strategies."] # [doc = ""] # [doc = " `fun` is used to transform the values produced by this strategy into"] # [doc = " other strategies. Values are then chosen from the derived strategies."] # [doc = " Shrinking proceeds by shrinking individual values as well as shrinking"] # [doc = " the input used to generate the internal strategies."] # [doc = ""] # [doc = " ## Shrinking"] # [doc = ""] # [doc = " In the case of test failure, shrinking will not only shrink the output"] # [doc = " from the combinator itself, but also the input, i.e., the strategy used"] # [doc = " to generate the output itself. Doing this requires searching the new"] # [doc = " derived strategy for a new failing input. The combinator will generate"] # [doc = " up to `Config::cases` values for this search."] # [doc = ""] # [doc = " As a result, nested `prop_flat_map`/`Flatten` combinators risk"] # [doc = " exponential run time on this search for new failing values. To ensure"] # [doc = " that test failures occur within a reasonable amount of time, all of"] # [doc = " these combinators share a single \"flat map regen\" counter, and will"] # [doc = " stop generating new values if it exceeds `Config::max_flat_map_regens`."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " Generate two integers, where the second is always less than the first,"] # [doc = " without using filtering:"] # [doc = ""] # [doc = " ```"] # [doc = " use proptest::prelude::*;"] # [doc = ""] # [doc = " proptest! {"] # [doc = "   # /*"] # [doc = "   #[test]"] # [doc = "   # */"] # [doc = "   fn test_two("] # [doc = "     // Pick integers in the 1..65536 range, and derive a strategy"] # [doc = "     // which emits a tuple of that integer and another one which is"] # [doc = "     // some value less than it."] # [doc = "     (a, b) in (1..65536).prop_flat_map(|a| (Just(a), 0..a))"] # [doc = "   ) {"] # [doc = "     prop_assert!(b < a);"] # [doc = "   }"] # [doc = " }"] # [doc = " #"] # [doc = " # fn main() { test_two(); }"] # [doc = " ```"] # [doc = ""] # [doc = " ## Choosing the right flat-map"] # [doc = ""] # [doc = " `Strategy` has three \"flat-map\" combinators. They look very similar at"] # [doc = " first, and can be used to produce superficially identical test results."] # [doc = " For example, the following three expressions all produce inputs which"] # [doc = " are 2-tuples `(a,b)` where the `b` component is less than `a`."] # [doc = ""] # [doc = " ```no_run"] # [doc = " # #![allow(unused_variables)]"] # [doc = " use proptest::prelude::*;"] # [doc = ""] # [doc = " let flat_map = (1..10).prop_flat_map(|a| (Just(a), 0..a));"] # [doc = " let ind_flat_map = (1..10).prop_ind_flat_map(|a| (Just(a), 0..a));"] # [doc = " let ind_flat_map2 = (1..10).prop_ind_flat_map2(|a| 0..a);"] # [doc = " ```"] # [doc = ""] # [doc = " The three do differ however in terms of how they shrink."] # [doc = ""] # [doc = " For `flat_map`, both `a` and `b` will shrink, and the invariant that"] # [doc = " `b < a` is maintained. This is a \"dependent\" or \"higher-order\" strategy"] # [doc = " in that it remembers that the strategy for choosing `b` is dependent on"] # [doc = " the value chosen for `a`."] # [doc = ""] # [doc = " For `ind_flat_map`, the invariant `b < a` is maintained, but only"] # [doc = " because `a` does not shrink. This is due to the fact that the"] # [doc = " dependency between the strategies is not tracked; `a` is simply seen as"] # [doc = " a constant."] # [doc = ""] # [doc = " Finally, for `ind_flat_map2`, the invariant `b < a` is _not_"] # [doc = " maintained, because `a` can shrink independently of `b`, again because"] # [doc = " the dependency between the two variables is not tracked, but in this"] # [doc = " case the derivation of `a` is still exposed to the shrinking system."] # [doc = ""] # [doc = " The use-cases for the independent flat-map variants is pretty narrow."] # [doc = " For the majority of cases where invariants need to be maintained and"] # [doc = " you want all components to shrink, `prop_flat_map` is the way to go."] # [doc = " `prop_ind_flat_map` makes the most sense when the input to the map"] # [doc = " function is not exposed in the output and shrinking across strategies"] # [doc = " is not expected to be useful. `prop_ind_flat_map2` is useful for using"] # [doc = " related values as starting points while not constraining them to that"] # [doc = " relation."] fn prop_flat_map < S : Strategy , F : Fn (Self :: Value) -> S > (self , fun : F ,) -> Flatten < Map < Self , F > > where Self : Sized , { Flatten :: new (Map { source : self , fun : Arc :: new (fun) , }) } # [doc = " Maps values produced by this strategy into new strategies and picks"] # [doc = " values from those strategies while considering the new strategies to be"] # [doc = " independent."] # [doc = ""] # [doc = " This is very similar to `prop_flat_map()`, but shrinking will *not*"] # [doc = " attempt to shrink the input that produces the derived strategies. This"] # [doc = " is appropriate for when the derived strategies already fully shrink in"] # [doc = " the desired way."] # [doc = ""] # [doc = " In most cases, you want `prop_flat_map()`."] # [doc = ""] # [doc = " See `prop_flat_map()` for a more detailed explanation on how the"] # [doc = " three flat-map combinators differ."] fn prop_ind_flat_map < S : Strategy , F : Fn (Self :: Value) -> S > (self , fun : F ,) -> IndFlatten < Map < Self , F > > where Self : Sized , { IndFlatten (Map { source : self , fun : Arc :: new (fun) , }) } # [doc = " Similar to `prop_ind_flat_map()`, but produces 2-tuples with the input"] # [doc = " generated from `self` in slot 0 and the derived strategy in slot 1."] # [doc = ""] # [doc = " See `prop_flat_map()` for a more detailed explanation on how the"] # [doc = " three flat-map combinators differ."] fn prop_ind_flat_map2 < S : Strategy , F : Fn (Self :: Value) -> S > (self , fun : F ,) -> IndFlattenMap < Self , F > where Self : Sized , { IndFlattenMap { source : self , fun : Arc :: new (fun) , } } # [doc = " Returns a strategy which only produces values accepted by `fun`."] # [doc = ""] # [doc = " This results in a very naïve form of rejection sampling and should only"] # [doc = " be used if (a) relatively few values will actually be rejected; (b) it"] # [doc = " isn't easy to express what you want by using another strategy and/or"] # [doc = " `map()`."] # [doc = ""] # [doc = " There are a lot of downsides to this form of filtering. It slows"] # [doc = " testing down, since values must be generated but then discarded."] # [doc = " Proptest only allows a limited number of rejects this way (across the"] # [doc = " entire `TestRunner`). Rejection can interfere with shrinking;"] # [doc = " particularly, complex filters may largely or entirely prevent shrinking"] # [doc = " from substantially altering the original value."] # [doc = ""] # [doc = " Local rejection sampling is still preferable to rejecting the entire"] # [doc = " input to a test (via `TestCaseError::Reject`), however, and the default"] # [doc = " number of local rejections allowed is much higher than the number of"] # [doc = " whole-input rejections."] # [doc = ""] # [doc = " `whence` is used to record where and why the rejection occurred."] fn prop_filter < R : Into < Reason > , F : Fn (& Self :: Value) -> bool > (self , whence : R , fun : F ,) -> Filter < Self , F > where Self : Sized , { Filter :: new (self , whence . into () , fun) } # [doc = " Returns a strategy which only produces transformed values where `fun`"] # [doc = " returns `Some(value)` and rejects those where `fun` returns `None`."] # [doc = ""] # [doc = " Using this method is preferable to using `.prop_map(..).prop_filter(..)`."] # [doc = ""] # [doc = " This results in a very naïve form of rejection sampling and should only"] # [doc = " be used if (a) relatively few values will actually be rejected; (b) it"] # [doc = " isn't easy to express what you want by using another strategy and/or"] # [doc = " `map()`."] # [doc = ""] # [doc = " There are a lot of downsides to this form of filtering. It slows"] # [doc = " testing down, since values must be generated but then discarded."] # [doc = " Proptest only allows a limited number of rejects this way (across the"] # [doc = " entire `TestRunner`). Rejection can interfere with shrinking;"] # [doc = " particularly, complex filters may largely or entirely prevent shrinking"] # [doc = " from substantially altering the original value."] # [doc = ""] # [doc = " Local rejection sampling is still preferable to rejecting the entire"] # [doc = " input to a test (via `TestCaseError::Reject`), however, and the default"] # [doc = " number of local rejections allowed is much higher than the number of"] # [doc = " whole-input rejections."] # [doc = ""] # [doc = " `whence` is used to record where and why the rejection occurred."] fn prop_filter_map < F : Fn (Self :: Value) -> Option < O > , O : fmt :: Debug > (self , whence : impl Into < Reason > , fun : F ,) -> FilterMap < Self , F > where Self : Sized , { FilterMap :: new (self , whence . into () , fun) } # [doc = " Returns a strategy which picks uniformly from `self` and `other`."] # [doc = ""] # [doc = " When shrinking, if a value from `other` was originally chosen but that"] # [doc = " value can be shrunken no further, it switches to a value from `self`"] # [doc = " and starts shrinking that."] # [doc = ""] # [doc = " Be aware that chaining `prop_union` calls will result in a very"] # [doc = " right-skewed distribution. If this is not what you want, you can call"] # [doc = " the `.or()` method on the `Union` to add more values to the same union,"] # [doc = " or directly call `Union::new()`."] # [doc = ""] # [doc = " Both `self` and `other` must be of the same type. To combine"] # [doc = " heterogeneous strategies, call the `boxed()` method on both `self` and"] # [doc = " `other` to erase the type differences before calling `prop_union()`."] fn prop_union (self , other : Self) -> Union < Self > where Self : Sized , { Union :: new (vec ! [self , other]) } # [doc = " Generate a recursive structure with `self` items as leaves."] # [doc = ""] # [doc = " `recurse` is applied to various strategies that produce the same type"] # [doc = " as `self` with nesting depth _n_ to create a strategy that produces the"] # [doc = " same type with nesting depth _n+1_. Generated structures will have a"] # [doc = " depth between 0 and `depth` and will usually have up to `desired_size`"] # [doc = " total elements, though they may have more. `expected_branch_size` gives"] # [doc = " the expected maximum size for any collection which may contain"] # [doc = " recursive elements and is used to control branch probability to achieve"] # [doc = " the desired size. Passing a too small value can result in trees vastly"] # [doc = " larger than desired."] # [doc = ""] # [doc = " Note that `depth` only counts branches; i.e., `depth = 0` is a single"] # [doc = " leaf, and `depth = 1` is a leaf or a branch containing only leaves."] # [doc = ""] # [doc = " In practise, generated values usually have a lower depth than `depth`"] # [doc = " (but `depth` is a hard limit) and almost always under"] # [doc = " `expected_branch_size` (though it is not a hard limit) since the"] # [doc = " underlying code underestimates probabilities."] # [doc = ""] # [doc = " Shrinking shrinks both the inner values and attempts switching from"] # [doc = " recursive to non-recursive cases."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # #![allow(unused_variables)]"] # [doc = " use std::collections::HashMap;"] # [doc = ""] # [doc = " use proptest::prelude::*;"] # [doc = ""] # [doc = " /// Define our own JSON AST type"] # [doc = " #[derive(Debug, Clone)]"] # [doc = " enum JsonNode {"] # [doc = "   Null,"] # [doc = "   Bool(bool),"] # [doc = "   Number(f64),"] # [doc = "   String(String),"] # [doc = "   Array(Vec<JsonNode>),"] # [doc = "   Map(HashMap<String, JsonNode>),"] # [doc = " }"] # [doc = ""] # [doc = " # fn main() {"] # [doc = " #"] # [doc = " // Define a strategy for generating leaf nodes of the AST"] # [doc = " let json_leaf = prop_oneof!["] # [doc = "   Just(JsonNode::Null),"] # [doc = "   prop::bool::ANY.prop_map(JsonNode::Bool),"] # [doc = "   prop::num::f64::ANY.prop_map(JsonNode::Number),"] # [doc = "   \".*\".prop_map(JsonNode::String),"] # [doc = " ];"] # [doc = ""] # [doc = " // Now define a strategy for a whole tree"] # [doc = " let json_tree = json_leaf.prop_recursive("] # [doc = "   4, // No more than 4 branch levels deep"] # [doc = "   64, // Target around 64 total elements"] # [doc = "   16, // Each collection is up to 16 elements long"] # [doc = "   |element| prop_oneof!["] # [doc = "     // NB `element` is an `Arc` and we'll need to reference it twice,"] # [doc = "     // so we clone it the first time."] # [doc = "     prop::collection::vec(element.clone(), 0..16)"] # [doc = "       .prop_map(JsonNode::Array),"] # [doc = "     prop::collection::hash_map(\".*\", element, 0..16)"] # [doc = "       .prop_map(JsonNode::Map)"] # [doc = "   ]);"] # [doc = " # }"] # [doc = " ```"] fn prop_recursive < R : Strategy < Value = Self :: Value > + 'static , F : Fn (BoxedStrategy < Self :: Value >) -> R , > (self , depth : u32 , desired_size : u32 , expected_branch_size : u32 , recurse : F ,) -> Recursive < Self :: Value , F > where Self : Sized + 'static , { Recursive :: new (self , depth , desired_size , expected_branch_size , recurse) } # [doc = " Shuffle the contents of the values produced by this strategy."] # [doc = ""] # [doc = " That is, this modifies a strategy producing a `Vec`, slice, etc, to"] # [doc = " shuffle the contents of that `Vec`/slice/etc."] # [doc = ""] # [doc = " Initially, the value is fully shuffled. During shrinking, the input"] # [doc = " value will initially be unchanged while the result will gradually be"] # [doc = " restored to its original order. Once de-shuffling either completes or"] # [doc = " is cancelled by calls to `complicate()` pinning it to a particular"] # [doc = " permutation, the inner value will be simplified."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```"] # [doc = " use proptest::prelude::*;"] # [doc = ""] # [doc = " static VALUES: &'static [u32] = &[0, 1, 2, 3, 4];"] # [doc = ""] # [doc = " fn is_permutation(orig: &[u32], mut actual: Vec<u32>) -> bool {"] # [doc = "   actual.sort();"] # [doc = "   orig == &actual[..]"] # [doc = " }"] # [doc = ""] # [doc = " proptest! {"] # [doc = "   # /*"] # [doc = "   #[test]"] # [doc = "   # */"] # [doc = "   fn test_is_permutation("] # [doc = "       ref perm in Just(VALUES.to_owned()).prop_shuffle()"] # [doc = "   ) {"] # [doc = "       assert!(is_permutation(VALUES, perm.clone()));"] # [doc = "   }"] # [doc = " }"] # [doc = " #"] # [doc = " # fn main() { test_is_permutation(); }"] # [doc = " ```"] fn prop_shuffle (self) -> Shuffle < Self > where Self : Sized , Self :: Value : Shuffleable , { Shuffle (self) } # [doc = " Erases the type of this `Strategy` so it can be passed around as a"] # [doc = " simple trait object."] # [doc = ""] # [doc = " See also `sboxed()` if this `Strategy` is `Send` and `Sync` and you"] # [doc = " want to preserve that information."] # [doc = ""] # [doc = " Strategies of this type afford cheap shallow cloning via reference"] # [doc = " counting by using an `Arc` internally."] fn boxed (self) -> BoxedStrategy < Self :: Value > where Self : Sized + 'static , { BoxedStrategy (Arc :: new (BoxedStrategyWrapper (self))) } # [doc = " Erases the type of this `Strategy` so it can be passed around as a"] # [doc = " simple trait object."] # [doc = ""] # [doc = " Unlike `boxed()`, this conversion retains the `Send` and `Sync` traits"] # [doc = " on the output."] # [doc = ""] # [doc = " Strategies of this type afford cheap shallow cloning via reference"] # [doc = " counting by using an `Arc` internally."] fn sboxed (self) -> SBoxedStrategy < Self :: Value > where Self : Sized + Send + Sync + 'static , { SBoxedStrategy (Arc :: new (BoxedStrategyWrapper (self))) } # [doc = " Wraps this strategy to prevent values from being subject to shrinking."] # [doc = ""] # [doc = " Suppressing shrinking is useful when testing things like linear"] # [doc = " approximation functions. Ordinarily, proptest will tend to shrink the"] # [doc = " input to the function until the result is just barely outside the"] # [doc = " acceptable range whereas the original input may have produced a result"] # [doc = " far outside of it. Since this makes it harder to see what the actual"] # [doc = " problem is, making the input `NoShrink` allows learning about inputs"] # [doc = " that produce more incorrect results."] fn no_shrink (self) -> NoShrink < Self > where Self : Sized , { NoShrink (self) } }
};
}
