// Generated macro for Index (struct)
macro_rules! Depcrate_sampleIndex {
() => {
// Module: crate::sample
// Provides: {"Index"}
// Dependencies: {}
# [doc = " A stand-in for an index into a slice or similar collection or conceptually"] # [doc = " similar things."] # [doc = ""] # [doc = " At the lowest level, `Index` is a mechanism for generating `usize` values"] # [doc = " in the range [0..N), for some N whose value is not known until it is"] # [doc = " needed. (Contrast with using `0..N` itself as a strategy, where you need to"] # [doc = " know N when you define the strategy.)"] # [doc = ""] # [doc = " For any upper bound, the actual index produced by an `Index` is the same no"] # [doc = " matter how many times it is used. Different upper bounds will produce"] # [doc = " different but not independent values."] # [doc = ""] # [doc = " Shrinking will cause the index to binary search through the underlying"] # [doc = " collection(s) it is used to sample."] # [doc = ""] # [doc = " Note that `Index` _cannot_ currently be used as a slice index (e.g.,"] # [doc = " `slice[index]`) due to the trait coherence rules."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " If the collection itself being indexed is itself generated by a strategy,"] # [doc = " you can make separately define that strategy and a strategy generating one"] # [doc = " or more `Index`es and then join the two after input generation, avoiding a"] # [doc = " call to `prop_flat_map()`."] # [doc = ""] # [doc = " ```"] # [doc = " use proptest::prelude::*;"] # [doc = ""] # [doc = " proptest! {"] # [doc = "     # /*"] # [doc = "     #[test]"] # [doc = "     # */"] # [doc = "     fn my_test("] # [doc = "         names in prop::collection::vec(\"[a-z]+\", 10..20),"] # [doc = "         indices in prop::collection::vec(any::<prop::sample::Index>(), 5..10)"] # [doc = "     ) {"] # [doc = "         // We now have Vec<String> of ten to twenty names, and a Vec<Index>"] # [doc = "         // of five to ten indices and can combine them however we like."] # [doc = "         for index in &indices {"] # [doc = "             println!(\"Accessing item by index: {}\", names[index.index(names.len())]);"] # [doc = "             println!(\"Accessing item by convenience method: {}\", index.get(&names));"] # [doc = "         }"] # [doc = "         // Test stuff..."] # [doc = "     }"] # [doc = " }"] # [doc = " #"] # [doc = " # fn main() { my_test(); }"] # [doc = " ```"] # [derive (Clone , Copy , Debug)] pub struct Index (usize) ;
};
}
