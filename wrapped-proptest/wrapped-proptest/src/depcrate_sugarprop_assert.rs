// Generated macro for prop_assert (macro)
macro_rules! Depcrate_sugarprop_assert {
() => {
// Module: crate::sugar
// Provides: {"prop_assert"}
// Dependencies: {}
# [doc = " Similar to `assert!` from std, but returns a test failure instead of"] # [doc = " panicking if the condition fails."] # [doc = ""] # [doc = " This can be used in any function that returns a `Result<_, TestCaseError>`,"] # [doc = " including the top-level function inside `proptest!`."] # [doc = ""] # [doc = " Both panicking via `assert!` and returning a test case failure have the"] # [doc = " same effect as far as proptest is concerned; however, the Rust runtime"] # [doc = " implicitly prints every panic to stderr by default (including a backtrace"] # [doc = " if enabled), which can make test failures unnecessarily noisy. By using"] # [doc = " `prop_assert!` instead, the only output on a failing test case is the final"] # [doc = " panic including the minimal test case."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```"] # [doc = " use proptest::prelude::*;"] # [doc = ""] # [doc = " proptest! {"] # [doc = "   # /*"] # [doc = "   #[test]"] # [doc = "   # */"] # [doc = "   fn triangle_inequality(a in 0.0f64..10.0, b in 0.0f64..10.0) {"] # [doc = "     // Called with just a condition will print the condition on failure"] # [doc = "     prop_assert!((a*a + b*b).sqrt() <= a + b);"] # [doc = "     // You can also provide a custom failure message"] # [doc = "     prop_assert!((a*a + b*b).sqrt() <= a + b,"] # [doc = "                  \"Triangle inequality didn't hold for ({}, {})\", a, b);"] # [doc = "     // If calling another function that can return failure, don't forget"] # [doc = "     // the `?` to propagate the failure."] # [doc = "     assert_from_other_function(a, b)?;"] # [doc = "   }"] # [doc = " }"] # [doc = ""] # [doc = " // The macro can be used from another function provided it has a compatible"] # [doc = " // return type."] # [doc = " fn assert_from_other_function(a: f64, b: f64) -> Result<(), TestCaseError> {"] # [doc = "   prop_assert!((a*a + b*b).sqrt() <= a + b);"] # [doc = "   Ok(())"] # [doc = " }"] # [doc = " #"] # [doc = " # fn main() { triangle_inequality(); }"] # [doc = " ```"] # [macro_export] macro_rules ! prop_assert { ($ cond : expr $ (,) ?) => { $ crate :: prop_assert ! ($ cond , :: core :: concat ! ("assertion failed: " , :: core :: stringify ! ($ cond))) } ; ($ cond : expr , $ ($ fmt : tt) *) => { if !$ cond { extern crate alloc ; let message = alloc :: format ! ($ ($ fmt) *) ; let message = alloc :: format ! ("{} at {}:{}" , message , :: core :: file ! () , :: core :: line ! ()) ; return :: core :: result :: Result :: Err ($ crate :: test_runner :: TestCaseError :: fail (message)) ; } } ; }
};
}
