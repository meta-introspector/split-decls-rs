// Generated macro for ValueTree (trait)
macro_rules! Depcrate_strategy_traitsValueTree {
() => {
// Module: crate::strategy::traits
// Provides: {"ValueTree"}
// Dependencies: {}
# [doc = " A generated value and its associated shrinker."] # [doc = ""] # [doc = " Conceptually, a `ValueTree` represents a spectrum between a \"minimally"] # [doc = " complex\" value and a starting, randomly-chosen value. For values such as"] # [doc = " numbers, this can be thought of as a simple binary search, and this is how"] # [doc = " the `ValueTree` state machine is defined."] # [doc = ""] # [doc = " The `ValueTree` state machine notionally has three fields: low, current,"] # [doc = " and high. Initially, low is the \"minimally complex\" value for the type, and"] # [doc = " high and current are both the initially chosen value. It can be queried for"] # [doc = " its current state. When shrinking, the controlling code tries simplifying"] # [doc = " the value one step. If the test failure still happens with the simplified"] # [doc = " value, further simplification occurs. Otherwise, the code steps back up"] # [doc = " towards the prior complexity."] # [doc = ""] # [doc = " The main invariants here are that the \"high\" value always corresponds to a"] # [doc = " failing test case, and that repeated calls to `complicate()` will return"] # [doc = " `false` only once the \"current\" value has returned to what it was before"] # [doc = " the last call to `simplify()`."] # [doc = ""] # [doc = " While it would be possible for default do-nothing implementations of"] # [doc = " `simplify()` and `complicate()` to be provided, this was not done"] # [doc = " deliberately since the majority of strategies will want to define their own"] # [doc = " shrinking anyway, and the minority that do not must call it out explicitly"] # [doc = " by their own implementation."] pub trait ValueTree { # [doc = " The type of the value produced by this `ValueTree`."] type Value : fmt :: Debug ; # [doc = " Returns the current value."] fn current (& self) -> Self :: Value ; # [doc = " Attempts to simplify the current value. Notionally, this sets the"] # [doc = " \"high\" value to the current value, and the current value to a \"halfway"] # [doc = " point\" between high and low, rounding towards low."] # [doc = ""] # [doc = " Returns whether any state changed as a result of this call. This does"] # [doc = " not necessarily imply that the value of `current()` has changed, since"] # [doc = " in the most general case, it is not possible for an implementation to"] # [doc = " determine this."] # [doc = ""] # [doc = " This call needs to correctly handle being called even immediately after"] # [doc = " it had been called previously and returned `false`."] fn simplify (& mut self) -> bool ; # [doc = " Attempts to partially undo the last simplification. Notionally, this"] # [doc = " sets the \"low\" value to one plus the current value, and the current"] # [doc = " value to a \"halfway point\" between high and the new low, rounding"] # [doc = " towards low."] # [doc = ""] # [doc = " Returns whether any state changed as a result of this call. This does"] # [doc = " not necessarily imply that the value of `current()` has changed, since"] # [doc = " in the most general case, it is not possible for an implementation to"] # [doc = " determine this."] # [doc = ""] # [doc = " It is usually expected that, immediately after a call to `simplify()`"] # [doc = " which returns true, this call will itself return true. However, this is"] # [doc = " not always the case; in some strategies, particularly those that use"] # [doc = " some form of rejection sampling, the act of trying to simplify may"] # [doc = " change the state such that `simplify()` returns true, yet ultimately"] # [doc = " left the resulting value unchanged, in which case there is nothing left"] # [doc = " to complicate."] # [doc = ""] # [doc = " This call does not need to gracefully handle being called before"] # [doc = " `simplify()` was ever called, but does need to correctly handle being"] # [doc = " called even immediately after it had been called previously and"] # [doc = " returned `false`."] fn complicate (& mut self) -> bool ; }
};
}
