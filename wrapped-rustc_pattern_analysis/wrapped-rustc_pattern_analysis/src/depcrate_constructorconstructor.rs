// Generated macro for Constructor (enum)
macro_rules! Depcrate_constructorConstructor {
() => {
// Module: crate::constructor
// Provides: {"Constructor"}
// Dependencies: {}
# [doc = " A value can be decomposed into a constructor applied to some fields. This struct represents"] # [doc = " the constructor. See also `Fields`."] # [doc = ""] # [doc = " `pat_constructor` retrieves the constructor corresponding to a pattern."] # [doc = " `specialize_constructor` returns the list of fields corresponding to a pattern, given a"] # [doc = " constructor. `Constructor::apply` reconstructs the pattern from a pair of `Constructor` and"] # [doc = " `Fields`."] # [derive (Debug)] pub enum Constructor < Cx : PatCx > { # [doc = " Tuples and structs."] Struct , # [doc = " Enum variants."] Variant (Cx :: VariantIdx) , # [doc = " References"] Ref , # [doc = " Array and slice patterns."] Slice (Slice) , # [doc = " Union field accesses."] UnionField , # [doc = " Booleans"] Bool (bool) , # [doc = " Ranges of integer literal values (`2`, `2..=5` or `2..5`)."] IntRange (IntRange) , # [doc = " Ranges of floating-point literal values (`2.0..=5.2`)."] F16Range (IeeeFloat < HalfS > , IeeeFloat < HalfS > , RangeEnd) , F32Range (IeeeFloat < SingleS > , IeeeFloat < SingleS > , RangeEnd) , F64Range (IeeeFloat < DoubleS > , IeeeFloat < DoubleS > , RangeEnd) , F128Range (IeeeFloat < QuadS > , IeeeFloat < QuadS > , RangeEnd) , # [doc = " String literals. Strings are not quite the same as `&[u8]` so we treat them separately."] Str (Cx :: StrLit) , # [doc = " Deref patterns (enabled by the `deref_patterns` feature) provide a way of matching on a"] # [doc = " smart pointer ADT through its pointee. They don't directly correspond to ADT constructors,"] # [doc = " and currently are not supported alongside them. Carries the type of the pointee."] DerefPattern (Cx :: Ty) , # [doc = " Constants that must not be matched structurally. They are treated as black boxes for the"] # [doc = " purposes of exhaustiveness: we must not inspect them, and they don't count towards making a"] # [doc = " match exhaustive."] # [doc = " Carries an id that must be unique within a match. We need this to ensure the invariants of"] # [doc = " [`SplitConstructorSet`]."] Opaque (OpaqueId) , # [doc = " Or-pattern."] Or , # [doc = " Wildcard pattern."] Wildcard , # [doc = " Never pattern. Only used in `WitnessPat`. An actual never pattern should be lowered as"] # [doc = " `Wildcard`."] Never , # [doc = " Fake extra constructor for enums that aren't allowed to be matched exhaustively. Also used"] # [doc = " for those types for which we cannot list constructors explicitly, like `f64` and `str`. Only"] # [doc = " used in `WitnessPat`."] NonExhaustive , # [doc = " Fake extra constructor for variants that should not be mentioned in diagnostics. We use this"] # [doc = " for variants behind an unstable gate as well as `#[doc(hidden)]` ones. Only used in"] # [doc = " `WitnessPat`."] Hidden , # [doc = " Fake extra constructor for constructors that are not seen in the matrix, as explained at the"] # [doc = " top of the file. Only used for specialization."] Missing , # [doc = " Fake extra constructor that indicates and empty field that is private. When we encounter one"] # [doc = " we skip the column entirely so we don't observe its emptiness. Only used for specialization."] PrivateUninhabited , }
};
}
