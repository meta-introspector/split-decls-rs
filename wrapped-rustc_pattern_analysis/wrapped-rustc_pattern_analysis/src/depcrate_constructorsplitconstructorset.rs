// Generated macro for SplitConstructorSet (struct)
macro_rules! Depcrate_constructorSplitConstructorSet {
() => {
// Module: crate::constructor
// Provides: {"SplitConstructorSet"}
// Dependencies: {}
# [doc = " Describes the result of analyzing the constructors in a column of a match."] # [doc = ""] # [doc = " `present` is morally the set of constructors present in the column, and `missing` is the set of"] # [doc = " constructors that exist in the type but are not present in the column."] # [doc = ""] # [doc = " More formally, if we discard wildcards from the column, this respects the following constraints:"] # [doc = " 1. the union of `present`, `missing` and `missing_empty` covers all the constructors of the type"] # [doc = " 2. each constructor in `present` is covered by something in the column"] # [doc = " 3. no constructor in `missing` or `missing_empty` is covered by anything in the column"] # [doc = " 4. each constructor in the column is equal to the union of one or more constructors in `present`"] # [doc = " 5. `missing` does not contain empty constructors (see discussion about emptiness at the top of"] # [doc = "    the file);"] # [doc = " 6. `missing_empty` contains only empty constructors"] # [doc = " 7. constructors in `present`, `missing` and `missing_empty` are split for the column; in other"] # [doc = "    words, they are either fully included in or fully disjoint from each constructor in the"] # [doc = "    column. In yet other words, there are no non-trivial intersections like between `0..10` and"] # [doc = "    `5..15`."] # [doc = ""] # [doc = " We must be particularly careful with weird constructors like `Opaque`: they're not formally part"] # [doc = " of the `ConstructorSet` for the type, yet if we forgot to include them in `present` we would be"] # [doc = " ignoring any row with `Opaque`s in the algorithm. Hence the importance of point 4."] # [derive (Debug)] pub struct SplitConstructorSet < Cx : PatCx > { pub present : SmallVec < [Constructor < Cx > ; 1] > , pub missing : Vec < Constructor < Cx > > , pub missing_empty : Vec < Constructor < Cx > > , }
};
}
