macro_rules! macro_26 {
    () => {
        declare_lint ! { # [doc = " The `unused_macro_rules` lint detects macro rules that were not used."] # [doc = ""] # [doc = " Note that the lint is distinct from the `unused_macros` lint, which"] # [doc = " fires if the entire macro is never called, while this lint fires for"] # [doc = " single unused rules of the macro that is otherwise used."] # [doc = " `unused_macro_rules` fires only if `unused_macros` wouldn't fire."] # [doc = ""] # [doc = " ### Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " #[warn(unused_macro_rules)]"] # [doc = " macro_rules! unused_empty {"] # [doc = "     (hello) => { println!(\"Hello, world!\") }; // This rule is unused"] # [doc = "     () => { println!(\"empty\") }; // This rule is used"] # [doc = " }"] # [doc = ""] # [doc = " fn main() {"] # [doc = "     unused_empty!(hello);"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " {{produces}}"] # [doc = ""] # [doc = " ### Explanation"] # [doc = ""] # [doc = " Unused macro rules may signal a mistake or unfinished code. Furthermore,"] # [doc = " they slow down compilation. Right now, silencing the warning is not"] # [doc = " supported on a single rule level, so you have to add an allow to the"] # [doc = " entire macro definition."] # [doc = ""] # [doc = " If you intended to export the macro to make it"] # [doc = " available outside of the crate, use the [`macro_export` attribute]."] # [doc = ""] # [doc = " [`macro_export` attribute]: https://doc.rust-lang.org/reference/macros-by-example.html#path-based-scope"] pub UNUSED_MACRO_RULES , Allow , "detects macro rules that were not used" }
    };
}

macro_26!()