macro_rules! macro_67 {
    () => {
        declare_lint ! { # [doc = " The `fuzzy_provenance_casts` lint detects an `as` cast between an integer"] # [doc = " and a pointer."] # [doc = ""] # [doc = " ### Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " #![feature(strict_provenance_lints)]"] # [doc = " #![warn(fuzzy_provenance_casts)]"] # [doc = ""] # [doc = " fn main() {"] # [doc = "     let _dangling = 16_usize as *const u8;"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " {{produces}}"] # [doc = ""] # [doc = " ### Explanation"] # [doc = ""] # [doc = " This lint is part of the strict provenance effort, see [issue #95228]."] # [doc = " Casting an integer to a pointer is considered bad style, as a pointer"] # [doc = " contains, besides the *address* also a *provenance*, indicating what"] # [doc = " memory the pointer is allowed to read/write. Casting an integer, which"] # [doc = " doesn't have provenance, to a pointer requires the compiler to assign"] # [doc = " (guess) provenance. The compiler assigns \"all exposed valid\" (see the"] # [doc = " docs of [`ptr::with_exposed_provenance`] for more information about this"] # [doc = " \"exposing\"). This penalizes the optimiser and is not well suited for"] # [doc = " dynamic analysis/dynamic program verification (e.g. Miri or CHERI"] # [doc = " platforms)."] # [doc = ""] # [doc = " It is much better to use [`ptr::with_addr`] instead to specify the"] # [doc = " provenance you want. If using this function is not possible because the"] # [doc = " code relies on exposed provenance then there is as an escape hatch"] # [doc = " [`ptr::with_exposed_provenance`]."] # [doc = ""] # [doc = " [issue #95228]: https://github.com/rust-lang/rust/issues/95228"] # [doc = " [`ptr::with_addr`]: https://doc.rust-lang.org/core/primitive.pointer.html#method.with_addr"] # [doc = " [`ptr::with_exposed_provenance`]: https://doc.rust-lang.org/core/ptr/fn.with_exposed_provenance.html"] pub FUZZY_PROVENANCE_CASTS , Allow , "a fuzzy integer to pointer cast is used" , @ feature_gate = strict_provenance_lints ; }
    };
}

macro_67!()