macro_rules! macro_37 {
    () => {
        declare_lint ! { # [doc = " The `const_item_mutation` lint detects attempts to mutate a `const`"] # [doc = " item."] # [doc = ""] # [doc = " ### Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " const FOO: [i32; 1] = [0];"] # [doc = ""] # [doc = " fn main() {"] # [doc = "     FOO[0] = 1;"] # [doc = "     // This will print \"[0]\"."] # [doc = "     println!(\"{:?}\", FOO);"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " {{produces}}"] # [doc = ""] # [doc = " ### Explanation"] # [doc = ""] # [doc = " Trying to directly mutate a `const` item is almost always a mistake."] # [doc = " What is happening in the example above is that a temporary copy of the"] # [doc = " `const` is mutated, but the original `const` is not. Each time you"] # [doc = " refer to the `const` by name (such as `FOO` in the example above), a"] # [doc = " separate copy of the value is inlined at that location."] # [doc = ""] # [doc = " This lint checks for writing directly to a field (`FOO.field ="] # [doc = " some_value`) or array entry (`FOO[0] = val`), or taking a mutable"] # [doc = " reference to the const item (`&mut FOO`), including through an"] # [doc = " autoderef (`FOO.some_mut_self_method()`)."] # [doc = ""] # [doc = " There are various alternatives depending on what you are trying to"] # [doc = " accomplish:"] # [doc = ""] # [doc = " * First, always reconsider using mutable globals, as they can be"] # [doc = "   difficult to use correctly, and can make the code more difficult to"] # [doc = "   use or understand."] # [doc = " * If you are trying to perform a one-time initialization of a global:"] # [doc = "     * If the value can be computed at compile-time, consider using"] # [doc = "       const-compatible values (see [Constant Evaluation])."] # [doc = "     * For more complex single-initialization cases, consider using"] # [doc = "       [`std::sync::LazyLock`]."] # [doc = " * If you truly need a mutable global, consider using a [`static`],"] # [doc = "   which has a variety of options:"] # [doc = "   * Simple data types can be directly defined and mutated with an"] # [doc = "     [`atomic`] type."] # [doc = "   * More complex types can be placed in a synchronization primitive"] # [doc = "     like a [`Mutex`], which can be initialized with one of the options"] # [doc = "     listed above."] # [doc = "   * A [mutable `static`] is a low-level primitive, requiring unsafe."] # [doc = "     Typically This should be avoided in preference of something"] # [doc = "     higher-level like one of the above."] # [doc = ""] # [doc = " [Constant Evaluation]: https://doc.rust-lang.org/reference/const_eval.html"] # [doc = " [`static`]: https://doc.rust-lang.org/reference/items/static-items.html"] # [doc = " [mutable `static`]: https://doc.rust-lang.org/reference/items/static-items.html#mutable-statics"] # [doc = " [`std::sync::LazyLock`]: https://doc.rust-lang.org/stable/std/sync/struct.LazyLock.html"] # [doc = " [`atomic`]: https://doc.rust-lang.org/std/sync/atomic/index.html"] # [doc = " [`Mutex`]: https://doc.rust-lang.org/std/sync/struct.Mutex.html"] pub CONST_ITEM_MUTATION , Warn , "detects attempts to mutate a `const` item" , }
    };
}

macro_37!();