macro_rules! macro_11 {
    () => {
        declare_lint ! { # [doc = " The `unused_extern_crates` lint guards against `extern crate` items"] # [doc = " that are never used."] # [doc = ""] # [doc = " ### Example"] # [doc = ""] # [doc = " ```rust,compile_fail"] # [doc = " #![deny(unused_extern_crates)]"] # [doc = " #![deny(warnings)]"] # [doc = " extern crate proc_macro;"] # [doc = " ```"] # [doc = ""] # [doc = " {{produces}}"] # [doc = ""] # [doc = " ### Explanation"] # [doc = ""] # [doc = " `extern crate` items that are unused have no effect and should be"] # [doc = " removed. Note that there are some cases where specifying an `extern"] # [doc = " crate` is desired for the side effect of ensuring the given crate is"] # [doc = " linked, even though it is not otherwise directly referenced. The lint"] # [doc = " can be silenced by aliasing the crate to an underscore, such as"] # [doc = " `extern crate foo as _`. Also note that it is no longer idiomatic to"] # [doc = " use `extern crate` in the [2018 edition], as extern crates are now"] # [doc = " automatically added in scope."] # [doc = ""] # [doc = " This lint is \"allow\" by default because it can be noisy, and produce"] # [doc = " false-positives. If a dependency is being removed from a project, it"] # [doc = " is recommended to remove it from the build configuration (such as"] # [doc = " `Cargo.toml`) to ensure stale build entries aren't left behind."] # [doc = ""] # [doc = " [2018 edition]: https://doc.rust-lang.org/edition-guide/rust-2018/module-system/path-clarity.html#no-more-extern-crate"] pub UNUSED_EXTERN_CRATES , Allow , "extern crates that are never used" }
    };
}

macro_11!();