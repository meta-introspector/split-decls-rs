macro_rules! macro_12 {
    () => {
        declare_lint ! { # [doc = " The `unused_crate_dependencies` lint detects crate dependencies that"] # [doc = " are never used."] # [doc = ""] # [doc = " ### Example"] # [doc = ""] # [doc = " ```rust,ignore (needs extern crate)"] # [doc = " #![deny(unused_crate_dependencies)]"] # [doc = " ```"] # [doc = ""] # [doc = " This will produce:"] # [doc = ""] # [doc = " ```text"] # [doc = " error: extern crate `regex` is unused in crate `lint_example`"] # [doc = "   |"] # [doc = "   = help: remove the dependency or add `use regex as _;` to the crate root"] # [doc = " note: the lint level is defined here"] # [doc = "  --> src/lib.rs:1:9"] # [doc = "   |"] # [doc = " 1 | #![deny(unused_crate_dependencies)]"] # [doc = "   |         ^^^^^^^^^^^^^^^^^^^^^^^^^"] # [doc = " ```"] # [doc = ""] # [doc = " ### Explanation"] # [doc = ""] # [doc = " After removing the code that uses a dependency, this usually also"] # [doc = " requires removing the dependency from the build configuration."] # [doc = " However, sometimes that step can be missed, which leads to time wasted"] # [doc = " building dependencies that are no longer used. This lint can be"] # [doc = " enabled to detect dependencies that are never used (more specifically,"] # [doc = " any dependency passed with the `--extern` command-line flag that is"] # [doc = " never referenced via [`use`], [`extern crate`], or in any [path])."] # [doc = ""] # [doc = " This lint is \"allow\" by default because it can provide false positives"] # [doc = " depending on how the build system is configured. For example, when"] # [doc = " using Cargo, a \"package\" consists of multiple crates (such as a"] # [doc = " library and a binary), but the dependencies are defined for the"] # [doc = " package as a whole. If there is a dependency that is only used in the"] # [doc = " binary, but not the library, then the lint will be incorrectly issued"] # [doc = " in the library."] # [doc = ""] # [doc = " [path]: https://doc.rust-lang.org/reference/paths.html"] # [doc = " [`use`]: https://doc.rust-lang.org/reference/items/use-declarations.html"] # [doc = " [`extern crate`]: https://doc.rust-lang.org/reference/items/extern-crates.html"] pub UNUSED_CRATE_DEPENDENCIES , Allow , "crate dependencies that are never used" , crate_level_only }
    };
}

macro_12!()