// Generated macro for macro_125 (macro)
macro_rules! Depcrate_builtinmacro_125 {
() => {
// Module: crate::builtin
// Provides: {"macro_125"}
// Dependencies: {}
declare_lint ! { # [doc = " The `never_type_fallback_flowing_into_unsafe` lint detects cases where never type fallback"] # [doc = " affects unsafe function calls."] # [doc = ""] # [doc = " ### Never type fallback"] # [doc = ""] # [doc = " When the compiler sees a value of type [`!`] it implicitly inserts a coercion (if possible),"] # [doc = " to allow type check to infer any type:"] # [doc = ""] # [doc = " ```ignore (illustrative-and-has-placeholders)"] # [doc = " // this"] # [doc = " let x: u8 = panic!();"] # [doc = ""] # [doc = " // is (essentially) turned by the compiler into"] # [doc = " let x: u8 = absurd(panic!());"] # [doc = ""] # [doc = " // where absurd is a function with the following signature"] # [doc = " // (it's sound, because `!` always marks unreachable code):"] # [doc = " fn absurd<T>(never: !) -> T { ... }"] # [doc = " ```"] # [doc = ""] # [doc = " While it's convenient to be able to use non-diverging code in one of the branches (like"] # [doc = " `if a { b } else { return }`) this could lead to compilation errors:"] # [doc = ""] # [doc = " ```compile_fail"] # [doc = " // this"] # [doc = " { panic!() };"] # [doc = ""] # [doc = " // gets turned into this"] # [doc = " { absurd(panic!()) }; // error: can't infer the type of `absurd`"] # [doc = " ```"] # [doc = ""] # [doc = " To prevent such errors, compiler remembers where it inserted `absurd` calls, and if it"] # [doc = " can't infer their type, it sets the type to fallback. `{ absurd::<Fallback>(panic!()) };`."] # [doc = " This is what is known as \"never type fallback\"."] # [doc = ""] # [doc = " ### Example"] # [doc = ""] # [doc = " ```rust,compile_fail"] # [doc = " #![deny(never_type_fallback_flowing_into_unsafe)]"] # [doc = " fn main() {"] # [doc = "     if true {"] # [doc = "         // return has type `!` which, is some cases, causes never type fallback"] # [doc = "         return"] # [doc = "     } else {"] # [doc = "         // `zeroed` is an unsafe function, which returns an unbounded type"] # [doc = "         unsafe { std::mem::zeroed() }"] # [doc = "     };"] # [doc = "     // depending on the fallback, `zeroed` may create `()` (which is completely sound),"] # [doc = "     // or `!` (which is instant undefined behavior)"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " {{produces}}"] # [doc = ""] # [doc = " ### Explanation"] # [doc = ""] # [doc = " Due to historic reasons never type fallback was `()`, meaning that `!` got spontaneously"] # [doc = " coerced to `()`. There are plans to change that, but they may make the code such as above"] # [doc = " unsound. Instead of depending on the fallback, you should specify the type explicitly:"] # [doc = " ```"] # [doc = " if true {"] # [doc = "     return"] # [doc = " } else {"] # [doc = "     // type is explicitly specified, fallback can't hurt us no more"] # [doc = "     unsafe { std::mem::zeroed::<()>() }"] # [doc = " };"] # [doc = " ```"] # [doc = ""] # [doc = " See [Tracking Issue for making `!` fall back to `!`](https://github.com/rust-lang/rust/issues/123748)."] # [doc = ""] # [doc = " [`!`]: https://doc.rust-lang.org/core/primitive.never.html"] # [doc = " [`()`]: https://doc.rust-lang.org/core/primitive.unit.html"] pub NEVER_TYPE_FALLBACK_FLOWING_INTO_UNSAFE , Warn , "never type fallback affecting unsafe function calls" , @ future_incompatible = FutureIncompatibleInfo { reason : FutureIncompatibilityReason :: EditionAndFutureReleaseSemanticsChange (Edition :: Edition2024) , reference : "<https://doc.rust-lang.org/edition-guide/rust-2024/never-type-fallback.html>" , report_in_deps : true , } ; @ edition Edition2024 => Deny ; report_in_external_macro }
};
}
