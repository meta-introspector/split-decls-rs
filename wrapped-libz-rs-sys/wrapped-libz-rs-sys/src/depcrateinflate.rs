// Generated macro for inflate (function)
macro_rules! Depcrateinflate {
() => {
// Module: crate
// Provides: {"inflate"}
// Dependencies: {}
# [doc = " Decompresses as much data as possible, and stops when the input buffer becomes empty or the output buffer becomes full."] # [doc = ""] # [doc = " # Returns"] # [doc = ""] # [doc = " - [`Z_OK`] if success"] # [doc = " - [`Z_STREAM_END`] if the end of the compressed data has been reached and all uncompressed output has been produced"] # [doc = " - [`Z_NEED_DICT`] if a preset dictionary is needed at this point"] # [doc = " - [`Z_STREAM_ERROR`] if the stream state was inconsistent"] # [doc = " - [`Z_DATA_ERROR`] if the input data was corrupted"] # [doc = " - [`Z_MEM_ERROR`] if there was not enough memory"] # [doc = " - [`Z_BUF_ERROR`] if no progress was possible or if there was not enough room in the output buffer when [`Z_FINISH`] is used"] # [doc = ""] # [doc = " Note that [`Z_BUF_ERROR`] is not fatal, and [`inflate`] can be called again with more input and more output space to continue decompressing."] # [doc = " If [`Z_DATA_ERROR`] is returned, the application may then call [`inflateSync`] to look for a good compression block if a partial recovery of the data is to be attempted."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " * Either"] # [doc = "     - `strm` is `NULL`"] # [doc = "     - `strm` satisfies the requirements of `&mut *strm` and was initialized with [`inflateInit_`] or similar"] # [doc = " * Either"] # [doc = "     - `strm.next_out` is `NULL`"] # [doc = "     - `strm.next_out` and `strm.avail_out` satisfy the requirements of [`core::slice::from_raw_parts_mut::<MaybeUninit<u8>>`]"] # [doc = " * Either"] # [doc = "     - `strm.next_in` is `NULL`"] # [doc = "     - `strm.next_in` and `strm.avail_in` satisfy the requirements of [`core::slice::from_raw_parts::<u8>`]"] # [cfg_attr (feature = "export-symbols" , export_name = prefix ! (inflate))] pub unsafe extern "C-unwind" fn inflate (strm : * mut z_stream , flush : i32) -> i32 { if let Some (stream) = InflateStream :: from_stream_mut (strm) { let flush = InflateFlush :: try_from (flush) . unwrap_or_default () ; zlib_rs :: inflate :: inflate (stream , flush) as _ } else { ReturnCode :: StreamError as _ } }
};
}
