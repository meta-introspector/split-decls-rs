// Generated macro for deflateGetDictionary (function)
macro_rules! DepcratedeflateGetDictionary {
() => {
// Module: crate
// Provides: {"deflateGetDictionary"}
// Dependencies: {}
# [doc = " Returns the sliding dictionary being maintained by deflate.  "] # [doc = ""] # [doc = " `dictLength` is set to the number of bytes in the dictionary, and that many bytes are copied"] # [doc = " to `dictionary`. `dictionary` must have enough space, where `32768` bytes is"] # [doc = " always enough.  If [`deflateGetDictionary`] is called with `dictionary` equal to"] # [doc = " `NULL`, then only the dictionary length is returned, and nothing is copied."] # [doc = " Similarly, if `dictLength` is `NULL`, then it is not set."] # [doc = ""] # [doc = " [`deflateGetDictionary`] may return a length less than the window size, even"] # [doc = " when more than the window size in input has been provided. It may return up"] # [doc = " to 258 bytes less in that case, due to how zlib's implementation of deflate"] # [doc = " manages the sliding window and lookahead for matches, where matches can be"] # [doc = " up to 258 bytes long. If the application needs the last window-size bytes of"] # [doc = " input, then that would need to be saved by the application outside of zlib."] # [doc = ""] # [doc = " # Returns"] # [doc = ""] # [doc = " * [`Z_OK`] if success"] # [doc = " * [`Z_STREAM_ERROR`] if the stream state is inconsistent"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " - `dictionary` must `NULL` or writable for the dictionary length (`32768` is always enough)"] # [doc = " - `dictLength` must `NULL` or satisfy the requirements of [`pointer::as_mut`]"] # [doc = ""] # [doc = " [`pointer::as_mut`]: https://doc.rust-lang.org/core/primitive.pointer.html#method.as_mut"] # [cfg_attr (feature = "export-symbols" , export_name = prefix ! (deflateGetDictionary))] pub unsafe extern "C-unwind" fn deflateGetDictionary (strm : * const z_stream , dictionary : * mut c_uchar , dictLength : * mut c_uint ,) -> c_int { let Some (stream) = DeflateStream :: from_stream_ref (strm) else { return ReturnCode :: StreamError as c_int ; } ; let len = zlib_rs :: deflate :: get_dictionary (stream , dictionary) ; if let Some (dictLength) = unsafe { dictLength . as_mut () } { * dictLength = len as c_uint ; } ReturnCode :: Ok as _ }
};
}
