macro_rules! compress {
    () => {
        # [doc = " Compresses `source` into `dest`, and writes the final deflated size into `destLen`."] # [doc = ""] # [doc = "`sourceLen` is the byte length of the source buffer."] # [doc = " Upon entry, `destLen` is the total size of the destination buffer,"] # [doc = " which must be at least the value returned by [`compressBound`]`(sourceLen)`."] # [doc = " Upon exit, `destLen` is the actual size of the compressed data."] # [doc = ""] # [doc = " A call to [`compress`] is equivalent to [`compress2`] with a level parameter of [`Z_DEFAULT_COMPRESSION`]."] # [doc = ""] # [doc = " # Returns"] # [doc = ""] # [doc = " * [`Z_OK`] if success"] # [doc = " * [`Z_MEM_ERROR`] if there was not enough memory"] # [doc = " * [`Z_BUF_ERROR`] if there was not enough room in the output buffer"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must guarantee that"] # [doc = ""] # [doc = " * The `destLen` pointer satisfies the requirements of [`core::ptr::read`]"] # [doc = " * Either"] # [doc = "     - `dest` is `NULL`"] # [doc = "     - `dest` and `*destLen` satisfy the requirements of [`core::slice::from_raw_parts_mut::<MaybeUninit<u8>>`]"] # [doc = " * Either"] # [doc = "     - `source` is `NULL`"] # [doc = "     - `source` and `sourceLen` satisfy the requirements of [`core::slice::from_raw_parts`]"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use libz_rs_sys::{Z_OK, compress};"] # [doc = ""] # [doc = " let source = b\"Ferris\";"] # [doc = ""] # [doc = " let mut dest = vec![0u8; 100];"] # [doc = " let mut dest_len = dest.len() as _;"] # [doc = ""] # [doc = " let err = unsafe {"] # [doc = "     compress("] # [doc = "         dest.as_mut_ptr(),"] # [doc = "         &mut dest_len,"] # [doc = "         source.as_ptr(),"] # [doc = "         source.len() as _,"] # [doc = "     )"] # [doc = " };"] # [doc = ""] # [doc = " assert_eq!(err, Z_OK);"] # [doc = " assert_eq!(dest_len, 14);"] # [doc = ""] # [doc = " dest.truncate(dest_len as usize);"] # [doc = " assert_eq!(dest, [120, 156, 115, 75, 45, 42, 202, 44, 6, 0, 8, 6, 2, 108]);"] # [doc = " ```"] # [cfg_attr (feature = "export-symbols" , export_name = prefix ! (compress))] pub unsafe extern "C-unwind" fn compress (dest : * mut Bytef , destLen : * mut c_ulong , source : * const Bytef , sourceLen : c_ulong ,) -> c_int { compress2 (dest , destLen , source , sourceLen , DeflateConfig :: default () . level ,) }
    };
}

compress!()