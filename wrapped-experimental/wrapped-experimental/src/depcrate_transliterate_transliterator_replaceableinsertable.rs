// Generated macro for Insertable (struct)
macro_rules! Depcrate_transliterate_transliterator_replaceableInsertable {
() => {
// Module: crate::transliterate::transliterator::replaceable
// Provides: {"Insertable"}
// Dependencies: {}
# [doc = " This provides (append-only) replacement APIs for a [`Replaceable`]."] # [doc = ""] # [doc = " It can only be constructed with a complete match using a [`RepMatcher`]."] # [doc = ""] # [doc = " A main goal of this type is to avoid unnecessary allocations when applying a rule replacement."] # [doc = " For this, it keeps track of the range of the `Replaceable`'s content that we are replacing"] # [doc = " using `start` and `end_len`,"] # [doc = " and lazily starts replacing this range from the left, in a grow-only fashion. That is,"] # [doc = " there might be a suffix in this replace range that is invalid UTF-8."] # [doc = ""] # [doc = " A noteworthy feature of `Insertable`'s are getting back a *valid* sub-`Replaceable` for"] # [doc = " full-blown transliteration of the replacement. This means that despite the grow-only"] # [doc = " nature of this Insertable, we can do arbitrary replacements."] # [doc = ""] # [doc = " Furthermore, an `Insertable` takes care of updating its parent's cursor."] # [doc = ""] # [doc = " # Safety"] # [doc = " While we hold onto the associated `Replaceable`, we may temporarily break its invariants."] # [doc = " As such, we cannot use any methods on `Replaceable` itself that rely on its invariants."] # [doc = ""] # [doc = " Specifically, we temporarily change the validity of `_rep.content`. During a replacement"] # [doc = " with this `Insertable`, the following invariants hold:"] # [doc = " - start is a valid UTF-8 index into `_rep.content` that signifies the start of the range we are replacing."] # [doc = "   It may not be changed."] # [doc = " - `end_len` is a valid UTF-8 suffix length into `_rep.content` that signifies the suffix of"] # [doc = "   the `Replaceable`'s internal text that we are *not* replacing."] # [doc = " - `curr` is the growing cursor into the range we are replacing. It is relative to `_rep.content`,"] # [doc = "   i.e., `_rep.content[start..curr]` is the replaced text already. That range must be valid"] # [doc = "   UTF-8. We guarantee this by only exposing UTF-8 APIs (pushing `&str`s and `char`s, and"] # [doc = "   creating a sub-`Replaceable` that guarantees UTF-8 through its invariants)."] # [doc = " - `_rep.content` has valid UTF-8 in the non-replaced portions as defined by `start` and"] # [doc = "   `end_len`."] # [doc = ""] # [doc = " In particular, these mean that the range of the `Replaceable`'s internal text that we have"] # [doc = " *not yet replaced*, is allowed to be invalid UTF-8."] # [doc = ""] # [doc = " We fix the `Replaceable`'s invariants in the `Drop` impl using [`Insertable::cleanup`]."] pub (crate) struct Insertable < 'a , 'b > { _rep : & 'b mut Replaceable < 'a > , start : usize , end_len : usize , curr : usize , match_lens : MatchLengths , cursor_offset : CursorOffset , }
};
}
