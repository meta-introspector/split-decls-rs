macro_rules! SliceFindSplit {
    () => {
        # [doc = " Element-finding methods for slices"] pub trait SliceFindSplit { type Item ; # [doc = " Linear search for the first occurrence  `elt` in the slice."] # [doc = ""] # [doc = " Return the part before and the part including and after the element."] # [doc = " If the element is not found, the second half is empty."] fn find_split < U : ? Sized > (& self , elt : & U) -> (& Self , & Self) where Self :: Item : PartialEq < U > ; # [doc = " Linear search for the last occurrence  `elt` in the slice."] # [doc = ""] # [doc = " Return the part before and the part including and after the element."] # [doc = " If the element is not found, the first half is empty."] fn rfind_split < U : ? Sized > (& self , elt : & U) -> (& Self , & Self) where Self :: Item : PartialEq < U > ; # [doc = " Linear search for the first occurrence  `elt` in the slice."] # [doc = ""] # [doc = " Return the part before and the part including and after the element."] # [doc = " If the element is not found, the second half is empty."] fn find_split_mut < U : ? Sized > (& mut self , elt : & U) -> (& mut Self , & mut Self) where Self :: Item : PartialEq < U > ; # [doc = " Linear search for the last occurrence  `elt` in the slice."] # [doc = ""] # [doc = " Return the part before and the part including and after the element."] # [doc = " If the element is not found, the first half is empty."] fn rfind_split_mut < U : ? Sized > (& mut self , elt : & U) -> (& mut Self , & mut Self) where Self :: Item : PartialEq < U > ; }
    };
}

SliceFindSplit!()