// Generated macro for impl_4 (impl)
macro_rules! Depcrateimpl_4 {
() => {
// Module: crate
// Provides: {"impl_4"}
// Dependencies: {}
impl < H > Cap < H > { # [doc = " Create a new allocator, wrapping the supplied allocator and enforcing the specified limit."] # [doc = ""] # [doc = " For no limit, simply set the limit to the theoretical maximum `usize::max_value()`."] pub const fn new (allocator : H , limit : usize) -> Self { Self { allocator , remaining : AtomicUsize :: new (limit) , limit : AtomicUsize :: new (limit) , # [cfg (feature = "stats")] total_allocated : AtomicUsize :: new (0) , # [cfg (feature = "stats")] max_allocated : AtomicUsize :: new (0) , } } # [doc = " Return the number of bytes remaining within the limit."] # [doc = ""] # [doc = " i.e. `limit - allocated`"] pub fn remaining (& self) -> usize { self . remaining . load (Ordering :: Relaxed) } # [doc = " Return the limit in bytes."] pub fn limit (& self) -> usize { self . limit . load (Ordering :: Relaxed) } # [doc = " Set the limit in bytes."] # [doc = ""] # [doc = " For no limit, simply set the limit to the theoretical maximum `usize::max_value()`."] # [doc = ""] # [doc = " This method will return `Err` if the specified limit is less than the number of bytes already allocated."] pub fn set_limit (& self , limit : usize) -> Result < () , () > { loop { let limit_old = self . limit . load (Ordering :: Relaxed) ; if limit < limit_old { if self . remaining . fetch_sub (limit_old - limit , Ordering :: Relaxed) < limit_old - limit { let _ = self . remaining . fetch_add (limit_old - limit , Ordering :: Relaxed) ; break Err (()) ; } if self . limit . compare_exchange (limit_old , limit , Ordering :: Relaxed , Ordering :: Relaxed) . is_err () { continue ; } } else { if self . limit . compare_exchange (limit_old , limit , Ordering :: Relaxed , Ordering :: Relaxed) . is_err () { continue ; } let _ = self . remaining . fetch_add (limit - limit_old , Ordering :: Relaxed) ; } break Ok (()) ; } } # [doc = " Return the number of bytes allocated. Always less than the limit."] pub fn allocated (& self) -> usize { loop { let limit_old = self . limit . load (Ordering :: SeqCst) ; let remaining = self . remaining . load (Ordering :: SeqCst) ; let limit = self . limit . load (Ordering :: SeqCst) ; if limit_old == limit && limit >= remaining { break limit - remaining ; } } } # [doc = " Get total amount of allocated memory. This includes already deallocated memory."] # [cfg (feature = "stats")] pub fn total_allocated (& self) -> usize { self . total_allocated . load (Ordering :: Relaxed) } # [doc = " Get maximum amount of memory that was allocated at any point in time."] # [cfg (feature = "stats")] pub fn max_allocated (& self) -> usize { self . max_allocated . load (Ordering :: Relaxed) } fn update_stats (& self , size : usize) { # [cfg (feature = "stats")] { let _ = self . total_allocated . fetch_add (size , Ordering :: Relaxed) ; let _ = self . max_allocated . fetch_max (self . allocated () , Ordering :: Relaxed) ; } # [cfg (not (feature = "stats"))] { let _ = (self , size) ; } } }
};
}
