// Generated macro for impl_27 (impl)
macro_rules! Depcrate_static_executorsimpl_27 {
() => {
// Module: crate::static_executors
// Provides: {"impl_27"}
// Dependencies: {}
impl StaticExecutor { # [doc = " Creates a new StaticExecutor."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_executor::StaticExecutor;"] # [doc = ""] # [doc = " static EXECUTOR: StaticExecutor = StaticExecutor::new();"] # [doc = " ```"] pub const fn new () -> Self { Self { state : State :: new () , } } # [doc = " Spawns a task onto the executor."] # [doc = ""] # [doc = " Note: unlike [`Executor::spawn`], this function requires being called with a `'static`"] # [doc = " borrow on the executor."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_executor::StaticExecutor;"] # [doc = ""] # [doc = " static EXECUTOR: StaticExecutor = StaticExecutor::new();"] # [doc = ""] # [doc = " let task = EXECUTOR.spawn(async {"] # [doc = "     println!(\"Hello world\");"] # [doc = " });"] # [doc = " ```"] pub fn spawn < T : Send + 'static > (& 'static self , future : impl Future < Output = T > + Send + 'static ,) -> Task < T > { let (runnable , task) = Builder :: new () . propagate_panic (true) . spawn (| () | future , self . schedule ()) ; runnable . schedule () ; task } # [doc = " Spawns a non-`'static` task onto the executor."] # [doc = ""] # [doc = " ## Safety"] # [doc = ""] # [doc = " The caller must ensure that the returned task terminates"] # [doc = " or is cancelled before the end of 'a."] pub unsafe fn spawn_scoped < 'a , T : Send + 'a > (& 'static self , future : impl Future < Output = T > + Send + 'a ,) -> Task < T > { let (runnable , task) = unsafe { Builder :: new () . propagate_panic (true) . spawn_unchecked (| () | future , self . schedule ()) } ; runnable . schedule () ; task } # [doc = " Attempts to run a task if at least one is scheduled."] # [doc = ""] # [doc = " Running a scheduled task means simply polling its future once."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_executor::StaticExecutor;"] # [doc = ""] # [doc = " static EXECUTOR: StaticExecutor = StaticExecutor::new();"] # [doc = ""] # [doc = " assert!(!EXECUTOR.try_tick()); // no tasks to run"] # [doc = ""] # [doc = " let task = EXECUTOR.spawn(async {"] # [doc = "     println!(\"Hello world\");"] # [doc = " });"] # [doc = ""] # [doc = " assert!(EXECUTOR.try_tick()); // a task was found"] # [doc = " ```"] pub fn try_tick (& self) -> bool { self . state . try_tick () } # [doc = " Runs a single task."] # [doc = ""] # [doc = " Running a task means simply polling its future once."] # [doc = ""] # [doc = " If no tasks are scheduled when this method is called, it will wait until one is scheduled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_executor::StaticExecutor;"] # [doc = " use futures_lite::future;"] # [doc = ""] # [doc = " static EXECUTOR: StaticExecutor = StaticExecutor::new();"] # [doc = ""] # [doc = " let task = EXECUTOR.spawn(async {"] # [doc = "     println!(\"Hello world\");"] # [doc = " });"] # [doc = ""] # [doc = " future::block_on(EXECUTOR.tick()); // runs the task"] # [doc = " ```"] pub async fn tick (& self) { self . state . tick () . await ; } # [doc = " Runs the executor until the given future completes."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_executor::StaticExecutor;"] # [doc = " use futures_lite::future;"] # [doc = ""] # [doc = " static EXECUTOR: StaticExecutor = StaticExecutor::new();"] # [doc = ""] # [doc = " let task = EXECUTOR.spawn(async { 1 + 2 });"] # [doc = " let res = future::block_on(EXECUTOR.run(async { task.await * 2 }));"] # [doc = ""] # [doc = " assert_eq!(res, 6);"] # [doc = " ```"] pub async fn run < T > (& self , future : impl Future < Output = T >) -> T { self . state . run (future) . await } # [doc = " Returns a function that schedules a runnable task when it gets woken up."] fn schedule (& 'static self) -> impl Fn (Runnable) + Send + Sync + 'static { let state : & 'static State = & self . state ; move | runnable | { let result = state . queue . push (runnable) ; debug_assert ! (result . is_ok ()) ; state . notify () ; } } }
};
}
