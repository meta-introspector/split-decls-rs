// Generated macro for impl_50 (impl)
macro_rules! Depcrateimpl_50 {
() => {
// Module: crate
// Provides: {"impl_50"}
// Dependencies: {}
impl < 'a > LocalExecutor < 'a > { # [doc = " Creates a single-threaded executor."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_executor::LocalExecutor;"] # [doc = ""] # [doc = " let local_ex = LocalExecutor::new();"] # [doc = " ```"] pub const fn new () -> LocalExecutor < 'a > { LocalExecutor { inner : Executor :: new () , _marker : PhantomData , } } # [doc = " Returns `true` if there are no unfinished tasks."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_executor::LocalExecutor;"] # [doc = ""] # [doc = " let local_ex = LocalExecutor::new();"] # [doc = " assert!(local_ex.is_empty());"] # [doc = ""] # [doc = " let task = local_ex.spawn(async {"] # [doc = "     println!(\"Hello world\");"] # [doc = " });"] # [doc = " assert!(!local_ex.is_empty());"] # [doc = ""] # [doc = " assert!(local_ex.try_tick());"] # [doc = " assert!(local_ex.is_empty());"] # [doc = " ```"] pub fn is_empty (& self) -> bool { self . inner () . is_empty () } # [doc = " Spawns a task onto the executor."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_executor::LocalExecutor;"] # [doc = ""] # [doc = " let local_ex = LocalExecutor::new();"] # [doc = ""] # [doc = " let task = local_ex.spawn(async {"] # [doc = "     println!(\"Hello world\");"] # [doc = " });"] # [doc = " ```"] pub fn spawn < T : 'a > (& self , future : impl Future < Output = T > + 'a) -> Task < T > { let state = self . inner () . state () ; let mut active = state . active () ; unsafe { Executor :: spawn_inner (state , future , & mut active) } } # [doc = " Spawns many tasks onto the executor."] # [doc = ""] # [doc = " As opposed to the [`spawn`] method, this locks the executor's inner task lock once and"] # [doc = " spawns all of the tasks in one go. With large amounts of tasks this can improve"] # [doc = " contention."] # [doc = ""] # [doc = " It is assumed that the iterator provided does not block; blocking iterators can lock up"] # [doc = " the internal mutex and therefore the entire executor. Unlike [`Executor::spawn`], the"] # [doc = " mutex is not released, as there are no other threads that can poll this executor."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```"] # [doc = " use async_executor::LocalExecutor;"] # [doc = " use futures_lite::{stream, prelude::*};"] # [doc = " use std::future::ready;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut ex = LocalExecutor::new();"] # [doc = ""] # [doc = " let futures = ["] # [doc = "     ready(1),"] # [doc = "     ready(2),"] # [doc = "     ready(3)"] # [doc = " ];"] # [doc = ""] # [doc = " // Spawn all of the futures onto the executor at once."] # [doc = " let mut tasks = vec![];"] # [doc = " ex.spawn_many(futures, &mut tasks);"] # [doc = ""] # [doc = " // Await all of them."] # [doc = " let results = ex.run(async move {"] # [doc = "     stream::iter(tasks).then(|x| x).collect::<Vec<_>>().await"] # [doc = " }).await;"] # [doc = " assert_eq!(results, [1, 2, 3]);"] # [doc = " # });"] # [doc = " ```"] # [doc = ""] # [doc = " [`spawn`]: LocalExecutor::spawn"] # [doc = " [`Executor::spawn_many`]: Executor::spawn_many"] pub fn spawn_many < T : 'a , F : Future < Output = T > + 'a > (& self , futures : impl IntoIterator < Item = F > , handles : & mut impl Extend < Task < F :: Output > > ,) { let state = self . inner () . state () ; let mut active = state . active () ; let tasks = futures . into_iter () . map (| future | { unsafe { Executor :: spawn_inner (state , future , & mut active) } }) ; handles . extend (tasks) ; } # [doc = " Attempts to run a task if at least one is scheduled."] # [doc = ""] # [doc = " Running a scheduled task means simply polling its future once."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_executor::LocalExecutor;"] # [doc = ""] # [doc = " let ex = LocalExecutor::new();"] # [doc = " assert!(!ex.try_tick()); // no tasks to run"] # [doc = ""] # [doc = " let task = ex.spawn(async {"] # [doc = "     println!(\"Hello world\");"] # [doc = " });"] # [doc = " assert!(ex.try_tick()); // a task was found"] # [doc = " ```"] pub fn try_tick (& self) -> bool { self . inner () . try_tick () } # [doc = " Runs a single task."] # [doc = ""] # [doc = " Running a task means simply polling its future once."] # [doc = ""] # [doc = " If no tasks are scheduled when this method is called, it will wait until one is scheduled."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_executor::LocalExecutor;"] # [doc = " use futures_lite::future;"] # [doc = ""] # [doc = " let ex = LocalExecutor::new();"] # [doc = ""] # [doc = " let task = ex.spawn(async {"] # [doc = "     println!(\"Hello world\");"] # [doc = " });"] # [doc = " future::block_on(ex.tick()); // runs the task"] # [doc = " ```"] pub async fn tick (& self) { self . inner () . tick () . await } # [doc = " Runs the executor until the given future completes."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_executor::LocalExecutor;"] # [doc = " use futures_lite::future;"] # [doc = ""] # [doc = " let local_ex = LocalExecutor::new();"] # [doc = ""] # [doc = " let task = local_ex.spawn(async { 1 + 2 });"] # [doc = " let res = future::block_on(local_ex.run(async { task.await * 2 }));"] # [doc = ""] # [doc = " assert_eq!(res, 6);"] # [doc = " ```"] pub async fn run < T > (& self , future : impl Future < Output = T >) -> T { self . inner () . run (future) . await } # [doc = " Returns a reference to the inner executor."] fn inner (& self) -> & Executor < 'a > { & self . inner } }
};
}
