// Generated macro for impl_536 (impl)
macro_rules! Depcrate_md_ctximpl_536 {
() => {
// Module: crate::md_ctx
// Provides: {"impl_536"}
// Dependencies: {}
impl MdCtxRef { # [doc = " Initializes the context to compute the digest of data."] # [corresponds (EVP_DigestInit_ex)] # [inline] pub fn digest_init (& mut self , digest : & MdRef) -> Result < () , ErrorStack > { unsafe { cvt (ffi :: EVP_DigestInit_ex (self . as_ptr () , digest . as_ptr () , ptr :: null_mut () ,)) ? ; } Ok (()) } # [doc = " Initializes the context to compute the signature of data."] # [doc = ""] # [doc = " A reference to the context's inner `PkeyCtx` is returned, allowing signature settings to be configured."] # [corresponds (EVP_DigestSignInit)] # [inline] pub fn digest_sign_init < 'a , T > (& 'a mut self , digest : Option < & MdRef > , pkey : & PKeyRef < T > ,) -> Result < & 'a mut PkeyCtxRef < T > , ErrorStack > where T : HasPrivate , { unsafe { let mut p = ptr :: null_mut () ; cvt (ffi :: EVP_DigestSignInit (self . as_ptr () , & mut p , digest . map_or (ptr :: null () , | p | p . as_ptr ()) , ptr :: null_mut () , pkey . as_ptr () ,)) ? ; Ok (PkeyCtxRef :: from_ptr_mut (p)) } } # [doc = " Initializes the context to verify the signature of data."] # [doc = ""] # [doc = " A reference to the context's inner `PkeyCtx` is returned, allowing signature settings to be configured."] # [corresponds (EVP_DigestVerifyInit)] # [inline] pub fn digest_verify_init < 'a , T > (& 'a mut self , digest : Option < & MdRef > , pkey : & PKeyRef < T > ,) -> Result < & 'a mut PkeyCtxRef < T > , ErrorStack > where T : HasPublic , { unsafe { let mut p = ptr :: null_mut () ; cvt (ffi :: EVP_DigestVerifyInit (self . as_ptr () , & mut p , digest . map_or (ptr :: null () , | p | p . as_ptr ()) , ptr :: null_mut () , pkey . as_ptr () ,)) ? ; Ok (PkeyCtxRef :: from_ptr_mut (p)) } } # [doc = " Updates the context with more data."] # [corresponds (EVP_DigestUpdate)] # [inline] pub fn digest_update (& mut self , data : & [u8]) -> Result < () , ErrorStack > { unsafe { cvt (ffi :: EVP_DigestUpdate (self . as_ptr () , data . as_ptr () as * const _ , data . len () ,)) ? ; } Ok (()) } # [doc = " Updates the context with more data."] # [corresponds (EVP_DigestSignUpdate)] # [inline] pub fn digest_sign_update (& mut self , data : & [u8]) -> Result < () , ErrorStack > { unsafe { cvt (ffi :: EVP_DigestSignUpdate (self . as_ptr () , data . as_ptr () as * const _ , data . len () ,)) ? ; } Ok (()) } # [doc = " Updates the context with more data."] # [corresponds (EVP_DigestVerifyUpdate)] # [inline] pub fn digest_verify_update (& mut self , data : & [u8]) -> Result < () , ErrorStack > { unsafe { cvt (ffi :: EVP_DigestVerifyUpdate (self . as_ptr () , data . as_ptr () as * const _ , data . len () ,)) ? ; } Ok (()) } # [doc = " Copies the computed digest into the buffer, returning the number of bytes written."] # [corresponds (EVP_DigestFinal)] # [inline] pub fn digest_final (& mut self , out : & mut [u8]) -> Result < usize , ErrorStack > { let mut len = u32 :: try_from (out . len ()) . unwrap_or (u32 :: MAX) ; unsafe { cvt (ffi :: EVP_DigestFinal (self . as_ptr () , out . as_mut_ptr () , & mut len ,)) ? ; } Ok (len as usize) } # [doc = " Copies the computed digest into the buffer."] # [doc = ""] # [doc = " Requires OpenSSL 1.1.1 or newer."] # [corresponds (EVP_DigestFinalXOF)] # [inline] # [cfg (any (ossl111 , awslc))] pub fn digest_final_xof (& mut self , out : & mut [u8]) -> Result < () , ErrorStack > { unsafe { cvt (ffi :: EVP_DigestFinalXOF (self . as_ptr () , out . as_mut_ptr () , out . len () ,)) ? ; } Ok (()) } # [doc = " Signs the computed digest."] # [doc = ""] # [doc = " If `out` is set to `None`, an upper bound on the number of bytes required for the output buffer will be"] # [doc = " returned."] # [corresponds (EVP_DigestSignFinal)] # [inline] pub fn digest_sign_final (& mut self , out : Option < & mut [u8] >) -> Result < usize , ErrorStack > { let mut len = out . as_ref () . map_or (0 , | b | b . len ()) ; unsafe { cvt (ffi :: EVP_DigestSignFinal (self . as_ptr () , out . map_or (ptr :: null_mut () , | b | b . as_mut_ptr ()) , & mut len ,)) ? ; } Ok (len) } # [doc = " Like [`Self::digest_sign_final`] but appends the signature to a [`Vec`]."] pub fn digest_sign_final_to_vec (& mut self , out : & mut Vec < u8 >) -> Result < usize , ErrorStack > { let base = out . len () ; let len = self . digest_sign_final (None) ? ; out . resize (base + len , 0) ; let len = self . digest_sign_final (Some (& mut out [base ..])) ? ; out . truncate (base + len) ; Ok (len) } # [doc = " Verifies the provided signature."] # [doc = ""] # [doc = " Returns `Ok(true)` if the signature is valid, `Ok(false)` if the signature is invalid, and `Err` if an error"] # [doc = " occurred."] # [corresponds (EVP_DigestVerifyFinal)] # [inline] pub fn digest_verify_final (& mut self , signature : & [u8]) -> Result < bool , ErrorStack > { unsafe { let r = ffi :: EVP_DigestVerifyFinal (self . as_ptr () , signature . as_ptr () as * mut _ , signature . len () ,) ; if r == 1 { Ok (true) } else { let errors = ErrorStack :: get () ; if errors . errors () . is_empty () { Ok (false) } else { Err (errors) } } } } # [doc = " Computes the signature of the data in `from`."] # [doc = ""] # [doc = " If `to` is set to `None`, an upper bound on the number of bytes required for the output buffer will be"] # [doc = " returned."] # [doc = ""] # [doc = " Requires OpenSSL 1.1.1 or newer."] # [corresponds (EVP_DigestSign)] # [cfg (ossl111)] # [inline] pub fn digest_sign (& mut self , from : & [u8] , to : Option < & mut [u8] >) -> Result < usize , ErrorStack > { let mut len = to . as_ref () . map_or (0 , | b | b . len ()) ; unsafe { cvt (ffi :: EVP_DigestSign (self . as_ptr () , to . map_or (ptr :: null_mut () , | b | b . as_mut_ptr ()) , & mut len , from . as_ptr () , from . len () ,)) ? ; } Ok (len) } # [doc = " Like [`Self::digest_sign`] but appends the signature to a [`Vec`]."] # [cfg (ossl111)] pub fn digest_sign_to_vec (& mut self , from : & [u8] , to : & mut Vec < u8 > ,) -> Result < usize , ErrorStack > { let base = to . len () ; let len = self . digest_sign (from , None) ? ; to . resize (base + len , 0) ; let len = self . digest_sign (from , Some (& mut to [base ..])) ? ; to . truncate (base + len) ; Ok (len) } # [doc = " Verifies the signature of the data in `data`."] # [doc = ""] # [doc = " Returns `Ok(true)` if the signature is valid, `Ok(false)` if the signature is invalid, and `Err` if an error"] # [doc = " occurred."] # [doc = ""] # [doc = " Requires OpenSSL 1.1.1 or newer."] # [corresponds (EVP_DigestVerify)] # [cfg (ossl111)] # [inline] pub fn digest_verify (& mut self , data : & [u8] , signature : & [u8]) -> Result < bool , ErrorStack > { unsafe { let r = cvt (ffi :: EVP_DigestVerify (self . as_ptr () , signature . as_ptr () , signature . len () , data . as_ptr () , data . len () ,)) ? ; Ok (r == 1) } } # [doc = " Returns the size of the message digest, i.e. the size of the hash"] # [corresponds (EVP_MD_CTX_size)] # [inline] pub fn size (& self) -> usize { unsafe { ffi :: EVP_MD_CTX_size (self . as_ptr ()) as usize } } # [doc = " Resets the underlying EVP_MD_CTX instance"] # [corresponds (EVP_MD_CTX_reset)] # [cfg (ossl111)] # [inline] pub fn reset (& mut self) -> Result < () , ErrorStack > { unsafe { let _ = cvt (ffi :: EVP_MD_CTX_reset (self . as_ptr ())) ? ; Ok (()) } } }
};
}
