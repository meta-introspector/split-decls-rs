// Generated macro for impl_237 (impl)
macro_rules! Depcrate_cipher_ctximpl_237 {
() => {
// Module: crate::cipher_ctx
// Provides: {"impl_237"}
// Dependencies: {}
impl CipherCtxRef { # [corresponds (EVP_CIPHER_CTX_copy)] pub fn copy (& mut self , src : & CipherCtxRef) -> Result < () , ErrorStack > { unsafe { cvt (ffi :: EVP_CIPHER_CTX_copy (self . as_ptr () , src . as_ptr ())) ? ; Ok (()) } } # [doc = " Initializes the context for encryption."] # [doc = ""] # [doc = " Normally this is called once to set all of the cipher, key, and IV. However, this process can be split up"] # [doc = " by first setting the cipher with no key or IV and then setting the key and IV with no cipher. This can be used"] # [doc = " to, for example, use a nonstandard IV size."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the key buffer is smaller than the key size of the cipher, the IV buffer is smaller than the IV size"] # [doc = " of the cipher, or if a key or IV is provided before a cipher."] # [corresponds (EVP_EncryptInit_ex)] pub fn encrypt_init (& mut self , type_ : Option < & CipherRef > , key : Option < & [u8] > , iv : Option < & [u8] > ,) -> Result < () , ErrorStack > { self . cipher_init (type_ , key , iv , ffi :: EVP_EncryptInit_ex) } # [doc = " Initializes the context for decryption."] # [doc = ""] # [doc = " Normally this is called once to set all of the cipher, key, and IV. However, this process can be split up"] # [doc = " by first setting the cipher with no key or IV and then setting the key and IV with no cipher. This can be used"] # [doc = " to, for example, use a nonstandard IV size."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the key buffer is smaller than the key size of the cipher, the IV buffer is smaller than the IV size"] # [doc = " of the cipher, or if a key or IV is provided before a cipher."] # [corresponds (EVP_DecryptInit_ex)] pub fn decrypt_init (& mut self , type_ : Option < & CipherRef > , key : Option < & [u8] > , iv : Option < & [u8] > ,) -> Result < () , ErrorStack > { self . cipher_init (type_ , key , iv , ffi :: EVP_DecryptInit_ex) } fn cipher_init (& mut self , type_ : Option < & CipherRef > , key : Option < & [u8] > , iv : Option < & [u8] > , f : unsafe extern "C" fn (* mut ffi :: EVP_CIPHER_CTX , * const ffi :: EVP_CIPHER , * mut ffi :: ENGINE , * const c_uchar , * const c_uchar ,) -> c_int ,) -> Result < () , ErrorStack > { if let Some (key) = key { let key_len = type_ . map_or_else (| | self . key_length () , | c | c . key_length ()) ; assert ! (key_len <= key . len ()) ; } if let Some (iv) = iv { let iv_len = type_ . map_or_else (| | self . iv_length () , | c | c . iv_length ()) ; assert ! (iv_len <= iv . len ()) ; } unsafe { cvt (f (self . as_ptr () , type_ . map_or (ptr :: null () , | p | p . as_ptr ()) , ptr :: null_mut () , key . map_or (ptr :: null () , | k | k . as_ptr ()) , iv . map_or (ptr :: null () , | iv | iv . as_ptr ()) ,)) ? ; } Ok (()) } # [doc = " Initializes the context to perform envelope encryption."] # [doc = ""] # [doc = " Normally this is called once to set both the cipher and public keys. However, this process may be split up by"] # [doc = " first providing the cipher with no public keys and then setting the public keys with no cipher."] # [doc = ""] # [doc = " `encrypted_keys` will contain the generated symmetric key encrypted with each corresponding asymmetric private"] # [doc = " key. The generated IV will be written to `iv`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `pub_keys` is not the same size as `encrypted_keys`, the IV buffer is smaller than the cipher's IV"] # [doc = " size, or if an IV is provided before the cipher."] # [corresponds (EVP_SealInit)] # [cfg (not (any (boringssl , awslc)))] pub fn seal_init < T > (& mut self , type_ : Option < & CipherRef > , pub_keys : & [PKey < T >] , encrypted_keys : & mut [Vec < u8 >] , iv : Option < & mut [u8] > ,) -> Result < () , ErrorStack > where T : HasPublic , { assert_eq ! (pub_keys . len () , encrypted_keys . len ()) ; if ! pub_keys . is_empty () { let iv_len = type_ . map_or_else (| | self . iv_length () , | c | c . iv_length ()) ; assert ! (iv . as_ref () . map_or (0 , | b | b . len ()) >= iv_len) ; } for (pub_key , buf) in pub_keys . iter () . zip (& mut * encrypted_keys) { buf . resize (pub_key . size () , 0) ; } let mut keys = encrypted_keys . iter_mut () . map (| b | b . as_mut_ptr ()) . collect :: < Vec < _ > > () ; let mut key_lengths = vec ! [0 ; pub_keys . len ()] ; let pub_keys_len = i32 :: try_from (pub_keys . len ()) . unwrap () ; unsafe { cvt (ffi :: EVP_SealInit (self . as_ptr () , type_ . map_or (ptr :: null () , | p | p . as_ptr ()) , keys . as_mut_ptr () , key_lengths . as_mut_ptr () , iv . map_or (ptr :: null_mut () , | b | b . as_mut_ptr ()) , pub_keys . as_ptr () as * mut _ , pub_keys_len ,)) ? ; } for (buf , len) in encrypted_keys . iter_mut () . zip (key_lengths) { buf . truncate (len as usize) ; } Ok (()) } # [doc = " Initializes the context to perform envelope decryption."] # [doc = ""] # [doc = " Normally this is called once with all of the arguments present. However, this process may be split up by first"] # [doc = " providing the cipher alone and then after providing the rest of the arguments in a second call."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the IV buffer is smaller than the cipher's required IV size or if the IV is provided before the"] # [doc = " cipher."] # [corresponds (EVP_OpenInit)] # [cfg (not (any (boringssl , awslc)))] pub fn open_init < T > (& mut self , type_ : Option < & CipherRef > , encrypted_key : & [u8] , iv : Option < & [u8] > , priv_key : Option < & PKeyRef < T > > ,) -> Result < () , ErrorStack > where T : HasPrivate , { if priv_key . is_some () { let iv_len = type_ . map_or_else (| | self . iv_length () , | c | c . iv_length ()) ; assert ! (iv . map_or (0 , | b | b . len ()) >= iv_len) ; } let len = c_int :: try_from (encrypted_key . len ()) . unwrap () ; unsafe { cvt (ffi :: EVP_OpenInit (self . as_ptr () , type_ . map_or (ptr :: null () , | p | p . as_ptr ()) , encrypted_key . as_ptr () , len , iv . map_or (ptr :: null () , | b | b . as_ptr ()) , priv_key . map_or (ptr :: null_mut () , ForeignTypeRef :: as_ptr) ,)) ? ; } Ok (()) } fn assert_cipher (& self) { unsafe { assert ! (! EVP_CIPHER_CTX_get0_cipher (self . as_ptr ()) . is_null ()) ; } } # [doc = " Returns the block size of the context's cipher."] # [doc = ""] # [doc = " Stream ciphers will report a block size of 1."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the context has not been initialized with a cipher."] # [corresponds (EVP_CIPHER_CTX_block_size)] pub fn block_size (& self) -> usize { self . assert_cipher () ; unsafe { ffi :: EVP_CIPHER_CTX_block_size (self . as_ptr ()) as usize } } # [doc = " Returns the key length of the context's cipher."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the context has not been initialized with a cipher."] # [corresponds (EVP_CIPHER_CTX_key_length)] pub fn key_length (& self) -> usize { self . assert_cipher () ; unsafe { ffi :: EVP_CIPHER_CTX_key_length (self . as_ptr ()) as usize } } # [doc = " Generates a random key based on the configured cipher."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the context has not been initialized with a cipher or if the buffer is smaller than the cipher's key"] # [doc = " length."] # [corresponds (EVP_CIPHER_CTX_rand_key)] # [cfg (not (any (boringssl , awslc)))] pub fn rand_key (& self , buf : & mut [u8]) -> Result < () , ErrorStack > { assert ! (buf . len () >= self . key_length ()) ; unsafe { cvt (ffi :: EVP_CIPHER_CTX_rand_key (self . as_ptr () , buf . as_mut_ptr () ,)) ? ; } Ok (()) } # [doc = " Sets the length of the key expected by the context."] # [doc = ""] # [doc = " Only some ciphers support configurable key lengths."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the context has not been initialized with a cipher."] # [corresponds (EVP_CIPHER_CTX_set_key_length)] pub fn set_key_length (& mut self , len : usize) -> Result < () , ErrorStack > { self . assert_cipher () ; unsafe { cvt (ffi :: EVP_CIPHER_CTX_set_key_length (self . as_ptr () , len . try_into () . unwrap () ,)) ? ; } Ok (()) } # [doc = " Returns the length of the IV expected by this context."] # [doc = ""] # [doc = " Returns 0 if the cipher does not use an IV."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the context has not been initialized with a cipher."] # [corresponds (EVP_CIPHER_CTX_iv_length)] pub fn iv_length (& self) -> usize { self . assert_cipher () ; unsafe { ffi :: EVP_CIPHER_CTX_iv_length (self . as_ptr ()) as usize } } # [doc = " Returns the `num` parameter of the cipher."] # [doc = ""] # [doc = " Built-in ciphers typically use this to track how much of the"] # [doc = " current underlying block has been \"used\" already."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the context has not been initialized with a cipher."] # [corresponds (EVP_CIPHER_CTX_num)] # [cfg (ossl110)] pub fn num (& self) -> usize { self . assert_cipher () ; unsafe { ffi :: EVP_CIPHER_CTX_num (self . as_ptr ()) as usize } } # [doc = " Sets the length of the IV expected by this context."] # [doc = ""] # [doc = " Only some ciphers support configurable IV lengths."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the context has not been initialized with a cipher."] # [corresponds (EVP_CIPHER_CTX_ctrl)] pub fn set_iv_length (& mut self , len : usize) -> Result < () , ErrorStack > { self . assert_cipher () ; let len = c_int :: try_from (len) . unwrap () ; unsafe { cvt (ffi :: EVP_CIPHER_CTX_ctrl (self . as_ptr () , ffi :: EVP_CTRL_GCM_SET_IVLEN , len , ptr :: null_mut () ,)) ? ; } Ok (()) } # [doc = " Returns the length of the authentication tag expected by this context."] # [doc = ""] # [doc = " Returns 0 if the cipher is not authenticated."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the context has not been initialized with a cipher."] # [doc = ""] # [doc = " Requires OpenSSL 3.0.0 or newer."] # [corresponds (EVP_CIPHER_CTX_get_tag_length)] # [cfg (ossl300)] pub fn tag_length (& self) -> usize { self . assert_cipher () ; unsafe { ffi :: EVP_CIPHER_CTX_get_tag_length (self . as_ptr ()) as usize } } # [doc = " Retrieves the calculated authentication tag from the context."] # [doc = ""] # [doc = " This should be called after [`Self::cipher_final`], and is only supported by authenticated ciphers."] # [doc = ""] # [doc = " The size of the buffer indicates the size of the tag. While some ciphers support a range of tag sizes, it is"] # [doc = " recommended to pick the maximum size."] # [corresponds (EVP_CIPHER_CTX_ctrl)] pub fn tag (& self , tag : & mut [u8]) -> Result < () , ErrorStack > { let len = c_int :: try_from (tag . len ()) . unwrap () ; unsafe { cvt (ffi :: EVP_CIPHER_CTX_ctrl (self . as_ptr () , ffi :: EVP_CTRL_GCM_GET_TAG , len , tag . as_mut_ptr () as * mut _ ,)) ? ; } Ok (()) } # [doc = " Sets the length of the generated authentication tag."] # [doc = ""] # [doc = " This must be called when encrypting with a cipher in CCM mode to use a tag size other than the default."] # [corresponds (EVP_CIPHER_CTX_ctrl)] pub fn set_tag_length (& mut self , len : usize) -> Result < () , ErrorStack > { let len = c_int :: try_from (len) . unwrap () ; unsafe { cvt (ffi :: EVP_CIPHER_CTX_ctrl (self . as_ptr () , ffi :: EVP_CTRL_GCM_SET_TAG , len , ptr :: null_mut () ,)) ? ; } Ok (()) } # [doc = " Sets the authentication tag for verification during decryption."] # [corresponds (EVP_CIPHER_CTX_ctrl)] pub fn set_tag (& mut self , tag : & [u8]) -> Result < () , ErrorStack > { let len = c_int :: try_from (tag . len ()) . unwrap () ; unsafe { cvt (ffi :: EVP_CIPHER_CTX_ctrl (self . as_ptr () , ffi :: EVP_CTRL_GCM_SET_TAG , len , tag . as_ptr () as * mut _ ,)) ? ; } Ok (()) } # [doc = " Enables or disables padding."] # [doc = ""] # [doc = " If padding is disabled, the plaintext must be an exact multiple of the cipher's block size."] # [corresponds (EVP_CIPHER_CTX_set_padding)] pub fn set_padding (& mut self , padding : bool) { unsafe { ffi :: EVP_CIPHER_CTX_set_padding (self . as_ptr () , padding as c_int) ; } } # [doc = " Sets the total length of plaintext data."] # [doc = ""] # [doc = " This is required for ciphers operating in CCM mode."] # [corresponds (EVP_CipherUpdate)] pub fn set_data_len (& mut self , len : usize) -> Result < () , ErrorStack > { let len = c_int :: try_from (len) . unwrap () ; unsafe { cvt (ffi :: EVP_CipherUpdate (self . as_ptr () , ptr :: null_mut () , & mut 0 , ptr :: null () , len ,)) ? ; } Ok (()) } # [doc = " Set ctx flags."] # [doc = ""] # [doc = " This function is currently used to enable AES key wrap feature supported by OpenSSL 1.0.2 or newer."] # [corresponds (EVP_CIPHER_CTX_set_flags)] # [cfg (ossl102)] pub fn set_flags (& mut self , flags : CipherCtxFlags) { unsafe { ffi :: EVP_CIPHER_CTX_set_flags (self . as_ptr () , flags . bits ()) ; } } # [doc = " Writes data into the context."] # [doc = ""] # [doc = " Providing no output buffer will cause the input to be considered additional authenticated data (AAD)."] # [doc = ""] # [doc = " Returns the number of bytes written to `output`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `output` doesn't contain enough space for data to be"] # [doc = " written."] # [corresponds (EVP_CipherUpdate)] pub fn cipher_update (& mut self , input : & [u8] , output : Option < & mut [u8] > ,) -> Result < usize , ErrorStack > { if let Some (output) = & output { let mut block_size = self . block_size () ; if block_size == 1 { block_size = 0 ; } let min_output_size = input . len () + block_size ; assert ! (output . len () >= min_output_size , "Output buffer size should be at least {} bytes." , min_output_size) ; } unsafe { self . cipher_update_unchecked (input , output) } } # [doc = " Writes data into the context."] # [doc = ""] # [doc = " Providing no output buffer will cause the input to be considered additional authenticated data (AAD)."] # [doc = ""] # [doc = " Returns the number of bytes written to `output`."] # [doc = ""] # [doc = " This function is the same as [`Self::cipher_update`] but with the"] # [doc = " output size check removed. It can be used when the exact"] # [doc = " buffer size control is maintained by the caller."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller is expected to provide `output` buffer"] # [doc = " large enough to contain correct number of bytes. For streaming"] # [doc = " ciphers the output buffer size should be at least as big as"] # [doc = " the input buffer. For block ciphers the size of the output"] # [doc = " buffer depends on the state of partially updated blocks."] # [corresponds (EVP_CipherUpdate)] pub unsafe fn cipher_update_unchecked (& mut self , input : & [u8] , output : Option < & mut [u8] > ,) -> Result < usize , ErrorStack > { let inlen = c_int :: try_from (input . len ()) . unwrap () ; let mut outlen = 0 ; cvt (ffi :: EVP_CipherUpdate (self . as_ptr () , output . map_or (ptr :: null_mut () , | b | b . as_mut_ptr ()) , & mut outlen , input . as_ptr () , inlen ,)) ? ; Ok (outlen as usize) } # [doc = " Like [`Self::cipher_update`] except that it appends output to a [`Vec`]."] pub fn cipher_update_vec (& mut self , input : & [u8] , output : & mut Vec < u8 > ,) -> Result < usize , ErrorStack > { let base = output . len () ; output . resize (base + input . len () + self . block_size () , 0) ; let len = self . cipher_update (input , Some (& mut output [base ..])) ? ; output . truncate (base + len) ; Ok (len) } # [doc = " Like [`Self::cipher_update`] except that it writes output into the"] # [doc = " `data` buffer. The `inlen` parameter specifies the number of bytes in"] # [doc = " `data` that are considered the input. For streaming ciphers, the size of"] # [doc = " `data` must be at least the input size. Otherwise, it must be at least"] # [doc = " an additional block size larger."] # [doc = ""] # [doc = " Note: Use [`Self::cipher_update`] with no output argument to write AAD."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if the input size cannot be represented as `int` or"] # [doc = " exceeds the buffer size, or if the output buffer does not contain enough"] # [doc = " additional space."] # [corresponds (EVP_CipherUpdate)] pub fn cipher_update_inplace (& mut self , data : & mut [u8] , inlen : usize ,) -> Result < usize , ErrorStack > { assert ! (inlen <= data . len () , "Input size may not exceed buffer size") ; let block_size = self . block_size () ; if block_size != 1 { assert ! (data . len () >= inlen + block_size , "Output buffer size must be at least {} bytes." , inlen + block_size) ; } let inlen = c_int :: try_from (inlen) . unwrap () ; let mut outlen = 0 ; unsafe { cvt (ffi :: EVP_CipherUpdate (self . as_ptr () , data . as_mut_ptr () , & mut outlen , data . as_ptr () , inlen ,)) } ? ; Ok (outlen as usize) } # [doc = " Finalizes the encryption or decryption process."] # [doc = ""] # [doc = " Any remaining data will be written to the output buffer."] # [doc = ""] # [doc = " Returns the number of bytes written to `output`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `output` is smaller than the cipher's block size."] # [corresponds (EVP_CipherFinal)] pub fn cipher_final (& mut self , output : & mut [u8]) -> Result < usize , ErrorStack > { let block_size = self . block_size () ; if block_size > 1 { assert ! (output . len () >= block_size) ; } unsafe { self . cipher_final_unchecked (output) } } # [doc = " Finalizes the encryption or decryption process."] # [doc = ""] # [doc = " Any remaining data will be written to the output buffer."] # [doc = ""] # [doc = " Returns the number of bytes written to `output`."] # [doc = ""] # [doc = " This function is the same as [`Self::cipher_final`] but with"] # [doc = " the output buffer size check removed."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller is expected to provide `output` buffer"] # [doc = " large enough to contain correct number of bytes. For streaming"] # [doc = " ciphers the output buffer can be empty, for block ciphers the"] # [doc = " output buffer should be at least as big as the block."] # [corresponds (EVP_CipherFinal)] pub unsafe fn cipher_final_unchecked (& mut self , output : & mut [u8] ,) -> Result < usize , ErrorStack > { let mut outl = 0 ; cvt (ffi :: EVP_CipherFinal (self . as_ptr () , output . as_mut_ptr () , & mut outl ,)) ? ; Ok (outl as usize) } # [doc = " Like [`Self::cipher_final`] except that it appends output to a [`Vec`]."] pub fn cipher_final_vec (& mut self , output : & mut Vec < u8 >) -> Result < usize , ErrorStack > { let base = output . len () ; output . resize (base + self . block_size () , 0) ; let len = self . cipher_final (& mut output [base ..]) ? ; output . truncate (base + len) ; Ok (len) } }
};
}
