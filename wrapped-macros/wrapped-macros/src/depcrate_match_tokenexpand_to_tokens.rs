// Generated macro for expand_to_tokens (function)
macro_rules! Depcrate_match_tokenexpand_to_tokens {
() => {
// Module: crate::match_token
// Provides: {"expand_to_tokens"}
// Dependencies: {}
# [doc = " Expand the `match_token!` macro."] pub fn expand_to_tokens (cx : & mut ExtCtxt , span : Span , toks : & [ast :: TokenTree]) -> Result < Vec < ast :: TokenTree > , (Span , & 'static str) > { let Match { discriminant , mut arms } = panictry ! (parse (cx , toks)) ; let last_arm = match arms . pop () { Some (x) => x , None => ext_err ! (span , "need at least one match arm") , } ; let mut arm_code : Tokens = vec ! () ; let mut seen_tags : HashSet < Tag > = HashSet :: new () ; let mut wildcards : Vec < WildcardArm > = vec ! () ; let mut wild_excluded : HashMap < TagKind , Vec < Tag > > = HashMap :: new () ; for Arm { binding , lhs , rhs } in arms . into_iter () { let binding = match binding { Some (i) => quote_tokens ! (& mut * cx , $ i @) , None => vec ! () , } ; match (lhs . node , rhs . node) { (Pat (_) , Else) => ext_err ! (rhs . span , "'else' may not appear with an ordinary pattern") , (Pat (pat) , Expr (expr)) => { ext_err_if ! (! wildcards . is_empty () , lhs . span , "ordinary patterns may not appear after wildcard tags") ; push_all (& mut arm_code , quote_tokens ! (& mut * cx , $ binding $ pat => $ expr ,)) ; } (Tags (tags) , Else) => { for Spanned { span , node : tag } in tags . into_iter () { ext_err_if ! (! seen_tags . insert (tag . clone ()) , span , "duplicate tag") ; ext_err_if ! (tag . name . is_none () , rhs . span , "'else' may not appear with a wildcard tag") ; match wild_excluded . entry (tag . kind) { Occupied (e) => { e . into_mut () . push (tag . clone ()) ; } Vacant (e) => { e . insert (vec ! [tag . clone ()]) ; } } } } (Tags (tags) , Expr (expr)) => { let mut wildcard = None ; for Spanned { span , node : tag } in tags . into_iter () { ext_err_if ! (! seen_tags . insert (tag . clone ()) , span , "duplicate tag") ; match tag . name { Some (_) => { ext_err_if ! (! wildcards . is_empty () , lhs . span , "specific tags may not appear after wildcard tags") ; ext_err_if ! (wildcard == Some (true) , span , "wildcard tags must appear alone") ; if wildcard . is_some () { push_all (& mut arm_code , quote_tokens ! (& mut * cx , |)) ; } push_all (& mut arm_code , make_tag_pattern (cx , binding . clone () , tag)) ; wildcard = Some (false) ; } None => { ext_err_if ! (wildcard . is_some () , span , "wildcard tags must appear alone") ; wildcard = Some (true) ; wildcards . push (WildcardArm { binding : binding . clone () , kind : tag . kind , expr : expr . clone () , }) ; } } } match wildcard { None => ext_err ! (lhs . span , "[internal macro error] tag arm with no tags") , Some (false) => { push_all (& mut arm_code , quote_tokens ! (& mut * cx , => $ expr ,)) ; } Some (true) => () } } } } let Arm { binding , lhs , rhs } = last_arm ; let last_arm_token = token :: gensym_ident ("last_arm_token") ; let enable_wildcards = token :: gensym_ident ("enable_wildcards") ; let (last_pat , last_expr) = match (binding , lhs . node , rhs . node) { (Some (id) , _ , _) => ext_err ! (id . span , "the last arm cannot have an @-binding") , (None , Tags (_) , _) => ext_err ! (lhs . span , "the last arm cannot have tag patterns") , (None , _ , Else) => ext_err ! (rhs . span , "the last arm cannot use 'else'") , (None , Pat (p) , Expr (e)) => match p . node { ast :: PatWild | ast :: PatIdent (..) => (p , e) , _ => ext_err ! (lhs . span , "the last arm must have a wildcard or ident pattern") , } , } ; let mut enable_wildcards_code = vec ! () ; for (_ , tags) in wild_excluded . into_iter () { for tag in tags . into_iter () { push_all (& mut enable_wildcards_code , make_tag_pattern (cx , vec ! () , tag)) ; push_all (& mut enable_wildcards_code , quote_tokens ! (& mut * cx , => false ,)) ; } } let mut wildcard_code = vec ! () ; for WildcardArm { binding , kind , expr } in wildcards . into_iter () { let pat = make_tag_pattern (cx , binding , Tag { kind : kind , name : None }) ; push_all (& mut wildcard_code , quote_tokens ! (& mut * cx , (true , $ pat) => $ expr ,)) ; } Ok (quote_tokens ! (& mut * cx , match $ discriminant { $ arm_code $ last_arm_token => { let $ enable_wildcards = match $ last_arm_token { $ enable_wildcards_code _ => true , } ; match ($ enable_wildcards , $ last_arm_token) { $ wildcard_code (_ , $ last_pat) => $ last_expr , } } , })) }
};
}
