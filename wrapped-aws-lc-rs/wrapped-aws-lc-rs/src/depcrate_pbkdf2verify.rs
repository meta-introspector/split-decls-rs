// Generated macro for verify (function)
macro_rules! Depcrate_pbkdf2verify {
() => {
// Module: crate::pbkdf2
// Provides: {"verify"}
// Dependencies: {}
# [doc = " Verifies that a previously-derived (e.g., using `derive`) PBKDF2 value"] # [doc = " matches the PBKDF2 value derived from the other inputs."] # [doc = ""] # [doc = " The comparison is done in constant time to prevent timing attacks. The"] # [doc = " comparison will fail if `previously_derived` is empty (has a length of"] # [doc = " zero)."] # [doc = ""] # [doc = " | Parameter                  | RFC 2898 Section 5.2 Term"] # [doc = " |----------------------------|--------------------------------------------"] # [doc = " | `digest_alg`                 | PRF (HMAC with the given digest algorithm)."] # [doc = " | `iterations`               | c (iteration count)"] # [doc = " | `salt`                     | S (salt)"] # [doc = " | `secret`                   | P (password)"] # [doc = " | `previously_derived`       | dk (derived key)"] # [doc = " | `previously_derived.len()` | dkLen (derived key length)"] # [doc = ""] # [doc = " # Errors"] # [doc = " `error::Unspecified` is the inputs were not verified."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " `verify` panics if `previously_derived.len()` is larger than (2**32 - 1) * the digest"] # [doc = " algorithm's output length, per the PBKDF2 specification."] # [inline] pub fn verify (algorithm : Algorithm , iterations : NonZeroU32 , salt : & [u8] , secret : & [u8] , previously_derived : & [u8] ,) -> Result < () , Unspecified > { if previously_derived . is_empty () { return Err (Unspecified) ; } assert ! (previously_derived . len () as u64 <= algorithm . max_output_len , "derived key too long") ; let mut derived_buf = vec ! [0u8 ; previously_derived . len ()] ; try_derive (algorithm , iterations , salt , secret , & mut derived_buf) ? ; let result = constant_time :: verify_slices_are_equal (& derived_buf , previously_derived) ; derived_buf . zeroize () ; result }
};
}
