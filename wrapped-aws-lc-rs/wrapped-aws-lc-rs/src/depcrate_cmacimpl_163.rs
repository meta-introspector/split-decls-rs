// Generated macro for impl_163 (impl)
macro_rules! Depcrate_cmacimpl_163 {
() => {
// Module: crate::cmac
// Provides: {"impl_163"}
// Dependencies: {}
impl Context { # [doc = " Constructs a new CMAC signing context using the given key."] # [inline] # [must_use] pub fn with_key (key : & Key) -> Self { Self { key : key . clone () } } # [doc = " Updates the CMAC with all the data in `data`. `update` may be called"] # [doc = " zero or more times until `sign` is called."] # [doc = ""] # [doc = " # Errors"] # [doc = " `error::Unspecified` if the CMAC cannot be updated."] pub fn update (& mut self , data : & [u8]) -> Result < () , Unspecified > { unsafe { if 1 != CMAC_Update (* self . key . ctx . as_mut () , data . as_ptr () , data . len ()) { return Err (Unspecified) ; } } Ok (()) } # [doc = " Finalizes the CMAC calculation and returns the CMAC value. `sign`"] # [doc = " consumes the context so it cannot be (mis-)used after `sign` has been"] # [doc = " called."] # [doc = ""] # [doc = " It is generally not safe to implement CMAC verification by comparing"] # [doc = " the return value of `sign` to a tag. Use `verify` for verification"] # [doc = " instead."] # [doc = ""] # [doc = " # Errors"] # [doc = " `error::Unspecified` if the CMAC calculation cannot be finalized."] # [doc = ""] # [doc = " # Panics"] # [doc = " Panics if the CMAC tag length exceeds the maximum allowed length, indicating memory corruption."] pub fn sign (mut self) -> Result < Tag , Unspecified > { let mut output = [0u8 ; MAX_CMAC_TAG_LEN] ; let mut out_len = MaybeUninit :: < usize > :: uninit () ; unsafe { if 1 != indicator_check ! (CMAC_Final (* self . key . ctx . as_mut () , output . as_mut_ptr () , out_len . as_mut_ptr () ,)) { return Err (Unspecified) ; } let actual_len = out_len . assume_init () ; assert ! (actual_len <= MAX_CMAC_TAG_LEN , "CMAC tag length {actual_len} exceeds maximum {MAX_CMAC_TAG_LEN}") ; if actual_len != self . key . algorithm . tag_len () { return Err (Unspecified) ; } Ok (Tag { bytes : output , len : actual_len , }) } } }
};
}
