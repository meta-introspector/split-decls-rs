// Generated macro for impl_33 (impl)
macro_rules! Depcrate_aeadimpl_33 {
() => {
// Module: crate::aead
// Provides: {"impl_33"}
// Dependencies: {}
impl < N : NonceSequence > OpeningKey < N > { # [doc = " Authenticates and decrypts (“opens”) data in place."] # [doc = ""] # [doc = " `aad` is the additional authenticated data (AAD), if any."] # [doc = ""] # [doc = " On input, `in_out` must be the ciphertext followed by the tag. When"] # [doc = " `open_in_place()` returns `Ok(plaintext)`, the input ciphertext"] # [doc = " has been overwritten by the plaintext; `plaintext` will refer to the"] # [doc = " plaintext without the tag."] # [doc = ""] # [doc = " Prefer [`RandomizedNonceKey::open_in_place`]."] # [doc = " # Errors"] # [doc = " `error::Unspecified` when ciphertext is invalid. In this case, `in_out` may have been"] # [doc = " overwritten in an unspecified way."] # [inline] # [allow (clippy :: needless_pass_by_value)] pub fn open_in_place < 'in_out , A > (& mut self , aad : Aad < A > , in_out : & 'in_out mut [u8] ,) -> Result < & 'in_out mut [u8] , Unspecified > where A : AsRef < [u8] > , { self . key . open_within (self . nonce_sequence . advance () ? , aad . as_ref () , in_out , 0 ..) } # [doc = " Authenticates and decrypts (“opens”) data in place, with a shift."] # [doc = ""] # [doc = " `aad` is the additional authenticated data (AAD), if any."] # [doc = ""] # [doc = " On input, `in_out[ciphertext_and_tag]` must be the ciphertext followed"] # [doc = " by the tag. When `open_within()` returns `Ok(plaintext)`, the plaintext"] # [doc = " will be at `in_out[0..plaintext.len()]`. In other words, the following"] # [doc = " two code fragments are equivalent for valid values of"] # [doc = " `ciphertext_and_tag`, except `open_within` will often be more efficient:"] # [doc = ""] # [doc = ""] # [doc = " ```skip"] # [doc = " let plaintext = key.open_within(aad, in_out, cipertext_and_tag)?;"] # [doc = " ```"] # [doc = ""] # [doc = " ```skip"] # [doc = " let ciphertext_and_tag_len = in_out[ciphertext_and_tag].len();"] # [doc = " in_out.copy_within(ciphertext_and_tag, 0);"] # [doc = " let plaintext = key.open_in_place(aad, &mut in_out[..ciphertext_and_tag_len])?;"] # [doc = " ```"] # [doc = ""] # [doc = " Similarly, `key.open_within(aad, in_out, 0..)` is equivalent to"] # [doc = " `key.open_in_place(aad, in_out)`."] # [doc = ""] # [doc = ""] # [doc = " The shifting feature is useful in the case where multiple packets are"] # [doc = " being reassembled in place. Consider this example where the peer has"] # [doc = " sent the message “Split stream reassembled in place” split into"] # [doc = " three sealed packets:"] # [doc = ""] # [doc = " ```ascii-art"] # [doc = "                 Packet 1                  Packet 2                 Packet 3"] # [doc = " Input:  [Header][Ciphertext][Tag][Header][Ciphertext][Tag][Header][Ciphertext][Tag]"] # [doc = "                      |         +--------------+                        |"] # [doc = "               +------+   +-----+    +----------------------------------+"] # [doc = "               v          v          v"] # [doc = " Output: [Plaintext][Plaintext][Plaintext]"] # [doc = "        “Split stream reassembled in place”"] # [doc = " ```"] # [doc = ""] # [doc = " This reassembly be accomplished with three calls to `open_within()`."] # [doc = ""] # [doc = " Prefer [`RandomizedNonceKey::open_in_place`]."] # [doc = " # Errors"] # [doc = " `error::Unspecified` when ciphertext is invalid. In this case, `in_out` may have been"] # [doc = " overwritten in an unspecified way."] # [inline] # [allow (clippy :: needless_pass_by_value)] pub fn open_within < 'in_out , A > (& mut self , aad : Aad < A > , in_out : & 'in_out mut [u8] , ciphertext_and_tag : RangeFrom < usize > ,) -> Result < & 'in_out mut [u8] , Unspecified > where A : AsRef < [u8] > , { self . key . open_within (self . nonce_sequence . advance () ? , aad . as_ref () , in_out , ciphertext_and_tag ,) } # [doc = " Returns a `OpeningKeyPreparedNonce` containing the next computed `Nonce` consumed from `NonceSequence`."] # [doc = ""] # [doc = " The encapsulated Nonce will be used **if and only if** either"] # [doc = " [OpeningKeyPreparedNonce::open_in_place] or [OpeningKeyPreparedNonce::open_within]"] # [doc = " are invoked. Dropping `OpeningKeyPreparedNonce` without invoking either method results in the nonce remaining"] # [doc = " consumed and unused within the associated `NonceSequence`. Subsequent calls to [OpeningKey] methods will"] # [doc = " always use a proceeding nonce from the `NonceSequence` regardless of whether"] # [doc = " a `OpeningKeyPreparedNonce` is consumed or not."] # [doc = ""] # [doc = " # Errors"] # [doc = " `Unspecified` if there is a failure computing the nonce for the next operation, i.e. `NonceSequence` exhausted."] pub fn prepare_nonce (& mut self) -> Result < OpeningKeyPreparedNonce < '_ , N > , Unspecified > { OpeningKeyPreparedNonce :: new (self) } }
};
}
