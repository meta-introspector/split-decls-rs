// Generated macro for impl_24 (impl)
macro_rules! Depcrateimpl_24 {
() => {
// Module: crate
// Provides: {"impl_24"}
// Dependencies: {}
impl < 'a , T : Target > Serializer < 'a , T > { # [doc = " Create a new `application/x-www-form-urlencoded` serializer for the given target."] # [doc = ""] # [doc = " If the target is non-empty,"] # [doc = " its content is assumed to already be in `application/x-www-form-urlencoded` syntax."] pub fn new (target : T) -> Self { Self :: for_suffix (target , 0) } # [doc = " Create a new `application/x-www-form-urlencoded` serializer"] # [doc = " for a suffix of the given target."] # [doc = ""] # [doc = " If that suffix is non-empty,"] # [doc = " its content is assumed to already be in `application/x-www-form-urlencoded` syntax."] pub fn for_suffix (mut target : T , start_position : usize) -> Self { if target . as_mut_string () . len () < start_position { panic ! ("invalid length {} for target of length {}" , start_position , target . as_mut_string () . len ()) ; } Serializer { target : Some (target) , start_position , encoding : None , } } # [doc = " Remove any existing name/value pair."] # [doc = ""] # [doc = " Panics if called after `.finish()`."] pub fn clear (& mut self) -> & mut Self { string (& mut self . target) . truncate (self . start_position) ; self } # [doc = " Set the character encoding to be used for names and values before percent-encoding."] pub fn encoding_override (& mut self , new : EncodingOverride < 'a >) -> & mut Self { self . encoding = new ; self } # [doc = " Serialize and append a name/value pair."] # [doc = ""] # [doc = " Panics if called after `.finish()`."] pub fn append_pair (& mut self , name : & str , value : & str) -> & mut Self { append_pair (string (& mut self . target) , self . start_position , self . encoding , name , value ,) ; self } # [doc = " Serialize and append a name of parameter without any value."] # [doc = ""] # [doc = " Panics if called after `.finish()`."] pub fn append_key_only (& mut self , name : & str) -> & mut Self { append_key_only (string (& mut self . target) , self . start_position , self . encoding , name ,) ; self } # [doc = " Serialize and append a number of name/value pairs."] # [doc = ""] # [doc = " This simply calls `append_pair` repeatedly."] # [doc = " This can be more convenient, so the user doesn’t need to introduce a block"] # [doc = " to limit the scope of `Serializer`’s borrow of its string."] # [doc = ""] # [doc = " Panics if called after `.finish()`."] pub fn extend_pairs < I , K , V > (& mut self , iter : I) -> & mut Self where I : IntoIterator , I :: Item : Borrow < (K , V) > , K : AsRef < str > , V : AsRef < str > , { { let string = string (& mut self . target) ; for pair in iter { let (k , v) = pair . borrow () ; append_pair (string , self . start_position , self . encoding , k . as_ref () , v . as_ref () ,) ; } } self } # [doc = " Serialize and append a number of names without values."] # [doc = ""] # [doc = " This simply calls `append_key_only` repeatedly."] # [doc = " This can be more convenient, so the user doesn’t need to introduce a block"] # [doc = " to limit the scope of `Serializer`’s borrow of its string."] # [doc = ""] # [doc = " Panics if called after `.finish()`."] pub fn extend_keys_only < I , K > (& mut self , iter : I) -> & mut Self where I : IntoIterator , I :: Item : Borrow < K > , K : AsRef < str > , { { let string = string (& mut self . target) ; for key in iter { let k = key . borrow () . as_ref () ; append_key_only (string , self . start_position , self . encoding , k) ; } } self } # [doc = " If this serializer was constructed with a string, take and return that string."] # [doc = ""] # [doc = " ```rust"] # [doc = " use form_urlencoded;"] # [doc = " let encoded: String = form_urlencoded::Serializer::new(String::new())"] # [doc = "     .append_pair(\"foo\", \"bar & baz\")"] # [doc = "     .append_pair(\"saison\", \"Été+hiver\")"] # [doc = "     .finish();"] # [doc = " assert_eq!(encoded, \"foo=bar+%26+baz&saison=%C3%89t%C3%A9%2Bhiver\");"] # [doc = " ```"] # [doc = ""] # [doc = " Panics if called more than once."] pub fn finish (& mut self) -> T :: Finished { self . target . take () . expect ("url::form_urlencoded::Serializer double finish") . finish () } }
};
}
