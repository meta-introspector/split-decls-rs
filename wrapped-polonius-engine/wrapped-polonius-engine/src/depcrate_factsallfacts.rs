// Generated macro for AllFacts (struct)
macro_rules! Depcrate_factsAllFacts {
() => {
// Module: crate::facts
// Provides: {"AllFacts"}
// Dependencies: {}
# [doc = " The \"facts\" which are the basis of the NLL borrow analysis."] # [derive (Clone , Debug)] pub struct AllFacts < T : FactTypes > { # [doc = " `loan_issued_at(origin, loan, point)` indicates that the `loan` was \"issued\""] # [doc = " at the given `point`, creating a reference with the `origin`."] # [doc = " Effectively, `origin` may refer to data from `loan` starting at `point` (this is usually"] # [doc = " the point *after* a borrow rvalue)."] pub loan_issued_at : Vec < (T :: Origin , T :: Loan , T :: Point) > , # [doc = " `universal_region(origin)` -- this is a \"free region\" within fn body"] pub universal_region : Vec < T :: Origin > , # [doc = " `cfg_edge(point1, point2)` for each edge `point1 -> point2` in the control flow"] pub cfg_edge : Vec < (T :: Point , T :: Point) > , # [doc = " `loan_killed_at(loan, point)` when some prefix of the path borrowed at `loan`"] # [doc = " is assigned at `point`."] # [doc = " Indicates that the path borrowed by the `loan` has changed in some way that the loan no"] # [doc = " longer needs to be tracked. (In particular, mutations to the path that was borrowed"] # [doc = " no longer invalidate the loan)"] pub loan_killed_at : Vec < (T :: Loan , T :: Point) > , # [doc = " `subset_base(origin1, origin2, point)` when we require `origin1@point: origin2@point`."] # [doc = " Indicates that `origin1 <= origin2` -- i.e., the set of loans in `origin1` are a subset"] # [doc = " of those in `origin2`."] pub subset_base : Vec < (T :: Origin , T :: Origin , T :: Point) > , # [doc = " `loan_invalidated_at(point, loan)` indicates that the `loan` is invalidated by some action"] # [doc = " taking place at `point`; if any origin that references this loan is live, this is an error."] pub loan_invalidated_at : Vec < (T :: Point , T :: Loan) > , # [doc = " `var_used_at(var, point)` when the variable `var` is used for anything"] # [doc = " but a drop at `point`"] pub var_used_at : Vec < (T :: Variable , T :: Point) > , # [doc = " `var_defined_at(var, point)` when the variable `var` is overwritten at `point`"] pub var_defined_at : Vec < (T :: Variable , T :: Point) > , # [doc = " `var_dropped_at(var, point)` when the variable `var` is used in a drop at `point`"] pub var_dropped_at : Vec < (T :: Variable , T :: Point) > , # [doc = " `use_of_var_derefs_origin(variable, origin)`: References with the given"] # [doc = " `origin` may be dereferenced when the `variable` is used."] # [doc = ""] # [doc = " In rustc, we generate this whenever the type of the variable includes the"] # [doc = " given origin."] pub use_of_var_derefs_origin : Vec < (T :: Variable , T :: Origin) > , # [doc = " `drop_of_var_derefs_origin(var, origin)` when the type of `var` includes"] # [doc = " the `origin` and uses it when dropping"] pub drop_of_var_derefs_origin : Vec < (T :: Variable , T :: Origin) > , # [doc = " `child_path(child, parent)` when the path `child` is the direct child of"] # [doc = " `parent`, e.g. `child_path(x.y, x)`, but not `child_path(x.y.z, x)`."] pub child_path : Vec < (T :: Path , T :: Path) > , # [doc = " `path_is_var(path, var)` the root path `path` starting in variable `var`."] pub path_is_var : Vec < (T :: Path , T :: Variable) > , # [doc = " `path_assigned_at_base(path, point)` when the `path` was initialized at point"] # [doc = " `point`. This fact is only emitted for a prefix `path`, and not for the"] # [doc = " implicit initialization of all of `path`'s children. E.g. a statement like"] # [doc = " `x.y = 3` at `point` would give the fact `path_assigned_at_base(x.y, point)` (but"] # [doc = " neither `path_assigned_at_base(x.y.z, point)` nor `path_assigned_at_base(x, point)`)."] pub path_assigned_at_base : Vec < (T :: Path , T :: Point) > , # [doc = " `path_moved_at_base(path, point)` when the `path` was moved at `point`. The"] # [doc = " same logic is applied as for `path_assigned_at_base` above."] pub path_moved_at_base : Vec < (T :: Path , T :: Point) > , # [doc = " `path_accessed_at_base(path, point)` when the `path` was accessed at point"] # [doc = " `point`. The same logic as for `path_assigned_at_base` and `path_moved_at_base` applies."] pub path_accessed_at_base : Vec < (T :: Path , T :: Point) > , # [doc = " These reflect the `'a: 'b` relations that are either declared by the user on function"] # [doc = " declarations or which are inferred via implied bounds."] # [doc = " For example: `fn foo<'a, 'b: 'a, 'c>(x: &'c &'a u32)` would have two entries:"] # [doc = " - one for the user-supplied subset `'b: 'a`"] # [doc = " - and one for the `'a: 'c` implied bound from the `x` parameter,"] # [doc = " (note that the transitive relation `'b: 'c` is not necessarily included"] # [doc = " explicitly, but rather inferred by polonius)."] pub known_placeholder_subset : Vec < (T :: Origin , T :: Origin) > , # [doc = " `placeholder(origin, loan)` describes a placeholder `origin`, with its associated"] # [doc = "  placeholder `loan`."] pub placeholder : Vec < (T :: Origin , T :: Loan) > , }
};
}
