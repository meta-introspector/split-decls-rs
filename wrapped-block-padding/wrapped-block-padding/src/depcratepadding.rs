// Generated macro for Padding (trait)
macro_rules! DepcratePadding {
() => {
// Module: crate
// Provides: {"Padding"}
// Dependencies: {}
# [doc = " Trait for message padding algorithms."] pub trait Padding { # [doc = " Pads `block` filled with data up to `pos` (i.e the message length"] # [doc = " stored in `block` is equal to `pos`)."] # [doc = ""] # [doc = " # Panics"] # [doc = " If `pos` is bigger than `block.len()`. Most padding algorithms also"] # [doc = " panic if they are equal."] fn raw_pad (block : & mut [u8] , pos : usize) ; # [doc = " Unpad data in `block`."] # [doc = ""] # [doc = " Returns error if the block contains malformed padding."] fn raw_unpad (block : & [u8]) -> Result < & [u8] , Error > ; # [doc = " Pads `block` filled with data up to `pos` (i.e the message length"] # [doc = " stored in `block` is equal to `pos`)."] # [doc = ""] # [doc = " # Panics"] # [doc = " If `pos` is bigger than `BlockSize`. Most padding algorithms also"] # [doc = " panic if they are equal."] # [inline] fn pad < BlockSize : ArraySize > (block : & mut Array < u8 , BlockSize > , pos : usize) { Self :: raw_pad (block . as_mut_slice () , pos) ; } # [doc = " Unpad data in `block`."] # [doc = ""] # [doc = " Returns error if the block contains malformed padding."] # [inline] fn unpad < BlockSize : ArraySize > (block : & Array < u8 , BlockSize >) -> Result < & [u8] , Error > { Self :: raw_unpad (block . as_slice ()) } # [doc = " Pad message and return padded tail block."] # [doc = ""] # [doc = " [`PaddedData::Error`] is returned only by [`NoPadding`] if `data` length is not multiple"] # [doc = " of the block size. [`NoPadding`] and [`ZeroPadding`] return [`PaddedData::NoPad`]"] # [doc = " if `data` length is multiple of block size. All other padding implementations"] # [doc = " should always return [`PaddedData::Pad`]."] # [inline] fn pad_detached < BlockSize : ArraySize > (data : & [u8]) -> PaddedData < '_ , BlockSize > { let (blocks , tail) = Array :: slice_as_chunks (data) ; let mut tail_block = Array :: default () ; let pos = tail . len () ; tail_block [.. pos] . copy_from_slice (tail) ; Self :: pad (& mut tail_block , pos) ; PaddedData :: Pad { blocks , tail_block } } # [doc = " Unpad data in `blocks` and return unpadded byte slice."] # [doc = ""] # [doc = " Returns error if `blocks` contain malformed padding."] # [inline] fn unpad_blocks < BlockSize : ArraySize > (blocks : & [Array < u8 , BlockSize >]) -> Result < & [u8] , Error > { let bs = BlockSize :: USIZE ; let (last_block , full_blocks) = blocks . split_last () . ok_or (Error) ? ; let unpad_len = Self :: unpad (last_block) ? . len () ; assert ! (unpad_len <= bs) ; let buf = Array :: slice_as_flattened (blocks) ; let data_len = full_blocks . len () * bs + unpad_len ; Ok (& buf [.. data_len]) } }
};
}
