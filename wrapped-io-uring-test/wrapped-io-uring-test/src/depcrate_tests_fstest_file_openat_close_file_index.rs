// Generated macro for test_file_openat_close_file_index (function)
macro_rules! Depcrate_tests_fstest_file_openat_close_file_index {
() => {
// Module: crate::tests::fs
// Provides: {"test_file_openat_close_file_index"}
// Dependencies: {}
pub fn test_file_openat_close_file_index < S : squeue :: EntryMarker , C : cqueue :: EntryMarker > (ring : & mut IoUring < S , C > , test : & Test ,) -> anyhow :: Result < () > { require ! (test ; test . probe . is_supported (opcode :: OpenAt :: CODE) ; test . probe . is_supported (opcode :: Close :: CODE) ; test . probe . is_supported (opcode :: Socket :: CODE) ;) ; let _ = ring . submitter () . unregister_files () ; ring . submitter () . register_files_sparse (2) . unwrap () ; use tempfile :: tempdir ; println ! ("test file_openat_close_file_index") ; let dir = tempdir () ? ; let dirfd = types :: Fd (libc :: AT_FDCWD) ; for round in 0 .. 3 { let path = dir . path () . join (format ! ("test-io-uring-openat-file_index-a-round-{}" , round)) ; let path = CString :: new (path . as_os_str () . as_bytes ()) ? ; let file_index = types :: DestinationSlot :: auto_target () ; let op = opcode :: OpenAt :: new (dirfd , path . as_ptr ()) ; let op = op . flags (libc :: O_CREAT as _) ; let op = op . file_index (Some (file_index)) ; unsafe { ring . submission () . push (& op . build () . user_data (0x11) . into ()) . expect ("queue is full") ; } ring . submit_and_wait (1) ? ; let cqes : Vec < cqueue :: Entry > = ring . completion () . map (Into :: into) . collect () ; assert_eq ! (cqes . len () , 1) ; assert_eq ! (cqes [0] . user_data () , 0x11) ; if round == 2 { assert ! (cqes [0] . result () < 0) ; } else { assert_eq ! (cqes [0] . result () , round) ; } } for round in 0 .. 2 { let op = opcode :: Close :: new (types :: Fixed (round)) ; unsafe { ring . submission () . push (& op . build () . user_data (0x12) . into ()) . expect ("queue is full") ; } ring . submit_and_wait (1) ? ; let cqes : Vec < cqueue :: Entry > = ring . completion () . map (Into :: into) . collect () ; assert_eq ! (cqes . len () , 1) ; assert_eq ! (cqes [0] . user_data () , 0x12) ; assert_eq ! (cqes [0] . result () , 0) ; } for round in (0 .. 3) . rev () { let path = dir . path () . join (format ! ("test-io-uring-openat-file_index-b-round-{}" , round)) ; let path = CString :: new (path . as_os_str () . as_bytes ()) ? ; let file_index = types :: DestinationSlot :: try_from_slot_target (round) . unwrap () ; let op = opcode :: OpenAt :: new (dirfd , path . as_ptr ()) ; let op = op . flags (libc :: O_CREAT as _) ; let op = op . file_index (Some (file_index)) ; unsafe { ring . submission () . push (& op . build () . user_data (0x11) . into ()) . expect ("queue is full") ; } ring . submit_and_wait (1) ? ; let cqes : Vec < cqueue :: Entry > = ring . completion () . map (Into :: into) . collect () ; assert_eq ! (cqes . len () , 1) ; assert_eq ! (cqes [0] . user_data () , 0x11) ; if round == 2 { assert ! (cqes [0] . result () < 0) ; } else { assert_eq ! (cqes [0] . result () , 0) ; } } for round in 0 .. 2 { let op = opcode :: Close :: new (types :: Fixed (round)) ; unsafe { ring . submission () . push (& op . build () . user_data (0x12) . into ()) . expect ("queue is full") ; } ring . submit_and_wait (1) ? ; let cqes : Vec < cqueue :: Entry > = ring . completion () . map (Into :: into) . collect () ; assert_eq ! (cqes . len () , 1) ; assert_eq ! (cqes [0] . user_data () , 0x12) ; assert_eq ! (cqes [0] . result () , 0) ; } ring . submitter () . unregister_files () . unwrap () ; Ok (()) }
};
}
