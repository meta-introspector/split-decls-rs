// Generated macro for test_tcp_recv_bundle (function)
macro_rules! Depcrate_tests_nettest_tcp_recv_bundle {
() => {
// Module: crate::tests::net
// Provides: {"test_tcp_recv_bundle"}
// Dependencies: {}
pub fn test_tcp_recv_bundle < S : squeue :: EntryMarker , C : cqueue :: EntryMarker > (ring : & mut IoUring < S , C > , test : & Test ,) -> anyhow :: Result < () > { use std :: io :: Write ; require ! (test ; test . probe . is_supported (opcode :: RecvBundle :: CODE) ; ring . params () . is_feature_recvsend_bundle () ;) ; println ! ("test tcp_recv_bundle") ; let (mut send_stream , recv_stream) = tcp_pair () ? ; let recv_fd = types :: Fd (recv_stream . as_raw_fd ()) ; let mut input = vec ! [0x0d ; 256] ; input . extend_from_slice (& [0x0e ; 256]) ; input . extend_from_slice (& [0x0a ; 256]) ; input . extend_from_slice (& [0x0d ; 128]) ; let buf_ring = register_buf_ring :: Builder :: new (0xdeff) . ring_entries (16) . buf_cnt (32) . buf_len (256) . build () ? ; buf_ring . rc . register (ring) ? ; send_stream . write_all (& input) ? ; send_stream . shutdown (Shutdown :: Write) ? ; let mut input = input . as_slice () ; loop { let recv_e = opcode :: RecvBundle :: new (recv_fd , 0xdeff) . build () . user_data (0x30) . into () ; unsafe { ring . submission () . push (& recv_e) . expect ("queue is full") ; } ring . submit_and_wait (1) ? ; let cqe : cqueue :: Entry = ring . completion () . next () . expect ("cqueue is empty") . into () ; assert_eq ! (cqe . user_data () , 0x30) ; assert ! (cqueue :: buffer_select (cqe . flags ()) . is_some ()) ; let mut remaining = cqe . result () as usize ; let bufs = buf_ring . rc . get_bufs (& buf_ring , remaining as u32 , cqe . flags ()) ; let mut section ; for buf in & bufs { let to_check = std :: cmp :: min (256 , remaining) ; (section , input) = input . split_at (to_check) ; assert_eq ! (buf . as_slice () , section) ; remaining -= to_check ; } assert_eq ! (remaining , 0) ; if input . is_empty () { break ; } assert ! (cqueue :: sock_nonempty (cqe . flags ())) ; } buf_ring . rc . unregister (ring) ? ; Ok (()) }
};
}
