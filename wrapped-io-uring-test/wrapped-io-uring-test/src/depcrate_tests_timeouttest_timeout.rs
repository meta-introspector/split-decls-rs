// Generated macro for test_timeout (function)
macro_rules! Depcrate_tests_timeouttest_timeout {
() => {
// Module: crate::tests::timeout
// Provides: {"test_timeout"}
// Dependencies: {}
pub fn test_timeout < S : squeue :: EntryMarker , C : cqueue :: EntryMarker > (ring : & mut IoUring < S , C > , test : & Test ,) -> anyhow :: Result < () > { require ! (test ; test . probe . is_supported (opcode :: Timeout :: CODE) ;) ; println ! ("test timeout") ; let ts = types :: Timespec :: new () . sec (1) ; let timeout_e = opcode :: Timeout :: new (& ts) ; unsafe { let mut queue = ring . submission () ; queue . push (& timeout_e . build () . user_data (0x09) . into ()) . expect ("queue is full") ; } let start = Instant :: now () ; ring . submit_and_wait (1) ? ; assert_eq ! (start . elapsed () . as_secs () , 1) ; let cqes : Vec < cqueue :: Entry > = ring . completion () . map (Into :: into) . collect () ; assert_eq ! (cqes . len () , 1) ; assert_eq ! (cqes [0] . user_data () , 0x09) ; assert_eq ! (cqes [0] . result () , - libc :: ETIME) ; let ts = types :: Timespec :: new () . sec (1) ; let timeout_e = opcode :: Timeout :: new (& ts) ; let nop_e = opcode :: Nop :: new () ; unsafe { let mut queue = ring . submission () ; queue . push (& timeout_e . build () . user_data (0x0a) . into ()) . expect ("queue is full") ; queue . push (& nop_e . build () . user_data (0x0b) . into ()) . expect ("queue is full") ; } let start = Instant :: now () ; ring . submit_and_wait (1) ? ; assert_eq ! (start . elapsed () . as_secs () , 0) ; let cqes : Vec < cqueue :: Entry > = ring . completion () . map (Into :: into) . collect () ; assert_eq ! (cqes . len () , 1) ; assert_eq ! (cqes [0] . user_data () , 0x0b) ; assert_eq ! (cqes [0] . result () , 0) ; ring . submit_and_wait (1) ? ; assert_eq ! (start . elapsed () . as_secs () , 1) ; let cqes : Vec < cqueue :: Entry > = ring . completion () . map (Into :: into) . collect () ; assert_eq ! (cqes . len () , 1) ; assert_eq ! (cqes [0] . user_data () , 0x0a) ; assert_eq ! (cqes [0] . result () , - libc :: ETIME) ; Ok (()) }
};
}
