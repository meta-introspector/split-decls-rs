// Generated macro for test_sqpoll_cq_overflow (function)
macro_rules! Depcrate_tests_sqpolltest_sqpoll_cq_overflow {
() => {
// Module: crate::tests::sqpoll
// Provides: {"test_sqpoll_cq_overflow"}
// Dependencies: {}
# [doc = " Test to reproduce SQPOLL CQ overflow issue"] # [doc = ""] # [doc = " This test demonstrates the issue when:"] # [doc = " 1) SQPOLL is enabled"] # [doc = " 2) IORING_FEAT_NODROP feature is supported"] # [doc = " 3) We submit more concurrent I/O requests than the CQ can handle"] pub fn test_sqpoll_cq_overflow < S : squeue :: EntryMarker , C : cqueue :: EntryMarker > (ring : & mut IoUring < S , C > , test : & Test ,) -> anyhow :: Result < () > { if ! ring . params () . is_feature_nodrop () { println ! ("IORING_FEAT_NODROP is not supported by the kernel, skip") ; return Ok (()) ; } require ! { test ; } println ! ("test sqpoll_cq_overflow") ; let idle = ring . params () . sq_thread_idle () ; let entries = ring . params () . sq_entries () as usize ; let num_requests = entries * 4 ; let temp_dir = tempdir () . expect ("Failed to create temp directory") ; let test_dir = temp_dir . path () ; let test_files = create_test_files (test_dir , num_requests) ; let mut bufs = (0 .. num_requests) . map (| _ | vec ! [0u8 ; 1024]) . collect :: < Vec < _ > > () ; let start = std :: time :: Instant :: now () ; test_files . iter () . zip (bufs . iter_mut ()) . for_each (| (file , buf) | { let fd = file . as_raw_fd () ; let entry = opcode :: Read :: new (types :: Fd (fd) , buf . as_mut_ptr () , buf . len () as _) . build () . into () ; while unsafe { ring . submission () . push (& entry) . is_err () } { ring . submit () . expect ("Failed to submit") ; } }) ; let mut completed_count = 0 ; while completed_count < num_requests { while ring . completion () . next () . is_some () { completed_count += 1 ; } if ring . submission () . cq_overflow () { println ! ("CQ overflow, syscall to flush") ; ring . submit () . expect ("Failed to submit") ; } } let end = start . elapsed () ; assert_eq ! (completed_count , num_requests) ; assert ! (end . as_millis () < idle as _ , "Overflown completions should be flushed within the idle time") ; Ok (()) }
};
}
