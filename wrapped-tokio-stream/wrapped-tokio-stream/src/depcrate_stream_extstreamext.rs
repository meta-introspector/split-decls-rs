// Generated macro for StreamExt (trait)
macro_rules! Depcrate_stream_extStreamExt {
() => {
// Module: crate::stream_ext
// Provides: {"StreamExt"}
// Dependencies: {}
# [doc = " An extension trait for the [`Stream`] trait that provides a variety of"] # [doc = " convenient combinator functions."] # [doc = ""] # [doc = " Be aware that the `Stream` trait in Tokio is a re-export of the trait found"] # [doc = " in the [futures] crate, however both Tokio and futures provide separate"] # [doc = " `StreamExt` utility traits, and some utilities are only available on one of"] # [doc = " these traits. Click [here][futures-StreamExt] to see the other `StreamExt`"] # [doc = " trait in the futures crate."] # [doc = ""] # [doc = " If you need utilities from both `StreamExt` traits, you should prefer to"] # [doc = " import one of them, and use the other through the fully qualified call"] # [doc = " syntax. For example:"] # [doc = " ```"] # [doc = " // import one of the traits:"] # [doc = " use futures::stream::StreamExt;"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = ""] # [doc = " let a = tokio_stream::iter(vec![1, 3, 5]);"] # [doc = " let b = tokio_stream::iter(vec![2, 4, 6]);"] # [doc = ""] # [doc = " // use the fully qualified call syntax for the other trait:"] # [doc = " let merged = tokio_stream::StreamExt::merge(a, b);"] # [doc = ""] # [doc = " // use normal call notation for futures::stream::StreamExt::collect"] # [doc = " let output: Vec<_> = merged.collect().await;"] # [doc = " assert_eq!(output, vec![1, 2, 3, 4, 5, 6]);"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " [`Stream`]: crate::Stream"] # [doc = " [futures]: https://docs.rs/futures"] # [doc = " [futures-StreamExt]: https://docs.rs/futures/0.3/futures/stream/trait.StreamExt.html"] pub trait StreamExt : Stream { # [doc = " Consumes and returns the next value in the stream or `None` if the"] # [doc = " stream is finished."] # [doc = ""] # [doc = " Equivalent to:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " async fn next(&mut self) -> Option<Self::Item>;"] # [doc = " ```"] # [doc = ""] # [doc = " Note that because `next` doesn't take ownership over the stream,"] # [doc = " the [`Stream`] type must be [`Unpin`]. If you want to use `next` with a"] # [doc = " [`!Unpin`](Unpin) stream, you'll first have to pin the stream. This can"] # [doc = " be done by boxing the stream using [`Box::pin`] or"] # [doc = " pinning it to the stack using the `pin_mut!` macro from the `pin_utils`"] # [doc = " crate."] # [doc = ""] # [doc = " # Cancel safety"] # [doc = ""] # [doc = " This method is cancel safe. The returned future only"] # [doc = " holds onto a reference to the underlying stream,"] # [doc = " so dropping it will never lose a value."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let mut stream = stream::iter(1..=3);"] # [doc = ""] # [doc = " assert_eq!(stream.next().await, Some(1));"] # [doc = " assert_eq!(stream.next().await, Some(2));"] # [doc = " assert_eq!(stream.next().await, Some(3));"] # [doc = " assert_eq!(stream.next().await, None);"] # [doc = " # }"] # [doc = " ```"] fn next (& mut self) -> Next < '_ , Self > where Self : Unpin , { Next :: new (self) } # [doc = " Consumes and returns the next item in the stream. If an error is"] # [doc = " encountered before the next item, the error is returned instead."] # [doc = ""] # [doc = " Equivalent to:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " async fn try_next(&mut self) -> Result<Option<T>, E>;"] # [doc = " ```"] # [doc = ""] # [doc = " This is similar to the [`next`](StreamExt::next) combinator,"] # [doc = " but returns a [`Result<Option<T>, E>`](Result) rather than"] # [doc = " an [`Option<Result<T, E>>`](Option), making for easy use"] # [doc = " with the [`?`](std::ops::Try) operator."] # [doc = ""] # [doc = " # Cancel safety"] # [doc = ""] # [doc = " This method is cancel safe. The returned future only"] # [doc = " holds onto a reference to the underlying stream,"] # [doc = " so dropping it will never lose a value."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = ""] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let mut stream = stream::iter(vec![Ok(1), Ok(2), Err(\"nope\")]);"] # [doc = ""] # [doc = " assert_eq!(stream.try_next().await, Ok(Some(1)));"] # [doc = " assert_eq!(stream.try_next().await, Ok(Some(2)));"] # [doc = " assert_eq!(stream.try_next().await, Err(\"nope\"));"] # [doc = " # }"] # [doc = " ```"] fn try_next < T , E > (& mut self) -> TryNext < '_ , Self > where Self : Stream < Item = Result < T , E > > + Unpin , { TryNext :: new (self) } # [doc = " Maps this stream's items to a different type, returning a new stream of"] # [doc = " the resulting type."] # [doc = ""] # [doc = " The provided closure is executed over all elements of this stream as"] # [doc = " they are made available. It is executed inline with calls to"] # [doc = " [`poll_next`](Stream::poll_next)."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it, similar to the existing `map` methods in the"] # [doc = " standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=3);"] # [doc = " let mut stream = stream.map(|x| x + 3);"] # [doc = ""] # [doc = " assert_eq!(stream.next().await, Some(4));"] # [doc = " assert_eq!(stream.next().await, Some(5));"] # [doc = " assert_eq!(stream.next().await, Some(6));"] # [doc = " # }"] # [doc = " ```"] fn map < T , F > (self , f : F) -> Map < Self , F > where F : FnMut (Self :: Item) -> T , Self : Sized , { Map :: new (self , f) } # [doc = " Map this stream's items to a different type for as long as determined by"] # [doc = " the provided closure. A stream of the target type will be returned,"] # [doc = " which will yield elements until the closure returns `None`."] # [doc = ""] # [doc = " The provided closure is executed over all elements of this stream as"] # [doc = " they are made available, until it returns `None`. It is executed inline"] # [doc = " with calls to [`poll_next`](Stream::poll_next). Once `None` is returned,"] # [doc = " the underlying stream will not be polled again."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it, similar to the [`Iterator::map_while`] method in the"] # [doc = " standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=10);"] # [doc = " let mut stream = stream.map_while(|x| {"] # [doc = "     if x < 4 {"] # [doc = "         Some(x + 3)"] # [doc = "     } else {"] # [doc = "         None"] # [doc = "     }"] # [doc = " });"] # [doc = " assert_eq!(stream.next().await, Some(4));"] # [doc = " assert_eq!(stream.next().await, Some(5));"] # [doc = " assert_eq!(stream.next().await, Some(6));"] # [doc = " assert_eq!(stream.next().await, None);"] # [doc = " # }"] # [doc = " ```"] fn map_while < T , F > (self , f : F) -> MapWhile < Self , F > where F : FnMut (Self :: Item) -> Option < T > , Self : Sized , { MapWhile :: new (self , f) } # [doc = " Maps this stream's items asynchronously to a different type, returning a"] # [doc = " new stream of the resulting type."] # [doc = ""] # [doc = " The provided closure is executed over all elements of this stream as"] # [doc = " they are made available, and the returned future is executed. Only one"] # [doc = " future is executed at the time."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it, similar to the existing `then` methods in the"] # [doc = " standard library."] # [doc = ""] # [doc = " Be aware that if the future is not `Unpin`, then neither is the `Stream`"] # [doc = " returned by this method. To handle this, you can use `tokio::pin!` as in"] # [doc = " the example below or put the stream in a `Box` with `Box::pin(stream)`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " async fn do_async_work(value: i32) -> i32 {"] # [doc = "     value + 3"] # [doc = " }"] # [doc = ""] # [doc = " let stream = stream::iter(1..=3);"] # [doc = " let stream = stream.then(do_async_work);"] # [doc = ""] # [doc = " tokio::pin!(stream);"] # [doc = ""] # [doc = " assert_eq!(stream.next().await, Some(4));"] # [doc = " assert_eq!(stream.next().await, Some(5));"] # [doc = " assert_eq!(stream.next().await, Some(6));"] # [doc = " # }"] # [doc = " ```"] fn then < F , Fut > (self , f : F) -> Then < Self , Fut , F > where F : FnMut (Self :: Item) -> Fut , Fut : Future , Self : Sized , { Then :: new (self , f) } # [doc = " Combine two streams into one by interleaving the output of both as it"] # [doc = " is produced."] # [doc = ""] # [doc = " Values are produced from the merged stream in the order they arrive from"] # [doc = " the two source streams. If both source streams provide values"] # [doc = " simultaneously, the merge stream alternates between them. This provides"] # [doc = " some level of fairness. You should not chain calls to `merge`, as this"] # [doc = " will break the fairness of the merging."] # [doc = ""] # [doc = " The merged stream completes once **both** source streams complete. When"] # [doc = " one source stream completes before the other, the merge stream"] # [doc = " exclusively polls the remaining stream."] # [doc = ""] # [doc = " For merging multiple streams, consider using [`StreamMap`] instead."] # [doc = ""] # [doc = " [`StreamMap`]: crate::StreamMap"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use tokio_stream::{StreamExt, Stream};"] # [doc = " use tokio::sync::mpsc;"] # [doc = " use tokio::time;"] # [doc = ""] # [doc = " use std::time::Duration;"] # [doc = " use std::pin::Pin;"] # [doc = ""] # [doc = " # /*"] # [doc = " #[tokio::main]"] # [doc = " # */"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " async fn main() {"] # [doc = " # time::pause();"] # [doc = "     let (tx1, mut rx1) = mpsc::channel::<usize>(10);"] # [doc = "     let (tx2, mut rx2) = mpsc::channel::<usize>(10);"] # [doc = ""] # [doc = "     // Convert the channels to a `Stream`."] # [doc = "     let rx1 = Box::pin(async_stream::stream! {"] # [doc = "           while let Some(item) = rx1.recv().await {"] # [doc = "               yield item;"] # [doc = "           }"] # [doc = "     }) as Pin<Box<dyn Stream<Item = usize> + Send>>;"] # [doc = ""] # [doc = "     let rx2 = Box::pin(async_stream::stream! {"] # [doc = "           while let Some(item) = rx2.recv().await {"] # [doc = "               yield item;"] # [doc = "           }"] # [doc = "     }) as Pin<Box<dyn Stream<Item = usize> + Send>>;"] # [doc = ""] # [doc = "     let mut rx = rx1.merge(rx2);"] # [doc = ""] # [doc = "     tokio::spawn(async move {"] # [doc = "         // Send some values immediately"] # [doc = "         tx1.send(1).await.unwrap();"] # [doc = "         tx1.send(2).await.unwrap();"] # [doc = ""] # [doc = "         // Let the other task send values"] # [doc = "         time::sleep(Duration::from_millis(20)).await;"] # [doc = ""] # [doc = "         tx1.send(4).await.unwrap();"] # [doc = "     });"] # [doc = ""] # [doc = "     tokio::spawn(async move {"] # [doc = "         // Wait for the first task to send values"] # [doc = "         time::sleep(Duration::from_millis(5)).await;"] # [doc = ""] # [doc = "         tx2.send(3).await.unwrap();"] # [doc = ""] # [doc = "         time::sleep(Duration::from_millis(25)).await;"] # [doc = ""] # [doc = "         // Send the final value"] # [doc = "         tx2.send(5).await.unwrap();"] # [doc = "     });"] # [doc = ""] # [doc = "    assert_eq!(1, rx.next().await.unwrap());"] # [doc = "    assert_eq!(2, rx.next().await.unwrap());"] # [doc = "    assert_eq!(3, rx.next().await.unwrap());"] # [doc = "    assert_eq!(4, rx.next().await.unwrap());"] # [doc = "    assert_eq!(5, rx.next().await.unwrap());"] # [doc = ""] # [doc = "    // The merged stream is consumed"] # [doc = "    assert!(rx.next().await.is_none());"] # [doc = " }"] # [doc = " ```"] fn merge < U > (self , other : U) -> Merge < Self , U > where U : Stream < Item = Self :: Item > , Self : Sized , { Merge :: new (self , other) } # [doc = " Filters the values produced by this stream according to the provided"] # [doc = " predicate."] # [doc = ""] # [doc = " As values of this stream are made available, the provided predicate `f`"] # [doc = " will be run against them. If the predicate"] # [doc = " resolves to `true`, then the stream will yield the value, but if the"] # [doc = " predicate resolves to `false`, then the value"] # [doc = " will be discarded and the next value will be produced."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it, similar to [`Iterator::filter`] method in the"] # [doc = " standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=8);"] # [doc = " let mut evens = stream.filter(|x| x % 2 == 0);"] # [doc = ""] # [doc = " assert_eq!(Some(2), evens.next().await);"] # [doc = " assert_eq!(Some(4), evens.next().await);"] # [doc = " assert_eq!(Some(6), evens.next().await);"] # [doc = " assert_eq!(Some(8), evens.next().await);"] # [doc = " assert_eq!(None, evens.next().await);"] # [doc = " # }"] # [doc = " ```"] fn filter < F > (self , f : F) -> Filter < Self , F > where F : FnMut (& Self :: Item) -> bool , Self : Sized , { Filter :: new (self , f) } # [doc = " Filters the values produced by this stream while simultaneously mapping"] # [doc = " them to a different type according to the provided closure."] # [doc = ""] # [doc = " As values of this stream are made available, the provided function will"] # [doc = " be run on them. If the predicate `f` resolves to"] # [doc = " [`Some(item)`](Some) then the stream will yield the value `item`, but if"] # [doc = " it resolves to [`None`], then the value will be skipped."] # [doc = ""] # [doc = " Note that this function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it, similar to [`Iterator::filter_map`] method in the"] # [doc = " standard library."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let stream = stream::iter(1..=8);"] # [doc = " let mut evens = stream.filter_map(|x| {"] # [doc = "     if x % 2 == 0 { Some(x + 1) } else { None }"] # [doc = " });"] # [doc = ""] # [doc = " assert_eq!(Some(3), evens.next().await);"] # [doc = " assert_eq!(Some(5), evens.next().await);"] # [doc = " assert_eq!(Some(7), evens.next().await);"] # [doc = " assert_eq!(Some(9), evens.next().await);"] # [doc = " assert_eq!(None, evens.next().await);"] # [doc = " # }"] # [doc = " ```"] fn filter_map < T , F > (self , f : F) -> FilterMap < Self , F > where F : FnMut (Self :: Item) -> Option < T > , Self : Sized , { FilterMap :: new (self , f) } # [doc = " Creates a stream which ends after the first `None`."] # [doc = ""] # [doc = " After a stream returns `None`, behavior is undefined. Future calls to"] # [doc = " `poll_next` may or may not return `Some(T)` again or they may panic."] # [doc = " `fuse()` adapts a stream, ensuring that after `None` is given, it will"] # [doc = " return `None` forever."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use tokio_stream::{Stream, StreamExt};"] # [doc = ""] # [doc = " use std::pin::Pin;"] # [doc = " use std::task::{Context, Poll};"] # [doc = ""] # [doc = " // a stream which alternates between Some and None"] # [doc = " struct Alternate {"] # [doc = "     state: i32,"] # [doc = " }"] # [doc = ""] # [doc = " impl Stream for Alternate {"] # [doc = "     type Item = i32;"] # [doc = ""] # [doc = "     fn poll_next(mut self: Pin<&mut Self>, _cx: &mut Context<'_>) -> Poll<Option<i32>> {"] # [doc = "         let val = self.state;"] # [doc = "         self.state = self.state + 1;"] # [doc = ""] # [doc = "         // if it's even, Some(i32), else None"] # [doc = "         if val % 2 == 0 {"] # [doc = "             Poll::Ready(Some(val))"] # [doc = "         } else {"] # [doc = "             Poll::Ready(None)"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " # /*"] # [doc = " #[tokio::main]"] # [doc = " # */"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " async fn main() {"] # [doc = "     let mut stream = Alternate { state: 0 };"] # [doc = ""] # [doc = "     // the stream goes back and forth"] # [doc = "     assert_eq!(stream.next().await, Some(0));"] # [doc = "     assert_eq!(stream.next().await, None);"] # [doc = "     assert_eq!(stream.next().await, Some(2));"] # [doc = "     assert_eq!(stream.next().await, None);"] # [doc = ""] # [doc = "     // however, once it is fused"] # [doc = "     let mut stream = stream.fuse();"] # [doc = ""] # [doc = "     assert_eq!(stream.next().await, Some(4));"] # [doc = "     assert_eq!(stream.next().await, None);"] # [doc = ""] # [doc = "     // it will always return `None` after the first time."] # [doc = "     assert_eq!(stream.next().await, None);"] # [doc = "     assert_eq!(stream.next().await, None);"] # [doc = "     assert_eq!(stream.next().await, None);"] # [doc = " }"] # [doc = " ```"] fn fuse (self) -> Fuse < Self > where Self : Sized , { Fuse :: new (self) } # [doc = " Creates a new stream of at most `n` items of the underlying stream."] # [doc = ""] # [doc = " Once `n` items have been yielded from this stream then it will always"] # [doc = " return that the stream is done."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let mut stream = stream::iter(1..=10).take(3);"] # [doc = ""] # [doc = " assert_eq!(Some(1), stream.next().await);"] # [doc = " assert_eq!(Some(2), stream.next().await);"] # [doc = " assert_eq!(Some(3), stream.next().await);"] # [doc = " assert_eq!(None, stream.next().await);"] # [doc = " # }"] # [doc = " ```"] fn take (self , n : usize) -> Take < Self > where Self : Sized , { Take :: new (self , n) } # [doc = " Take elements from this stream while the provided predicate"] # [doc = " resolves to `true`."] # [doc = ""] # [doc = " This function, like `Iterator::take_while`, will take elements from the"] # [doc = " stream until the predicate `f` resolves to `false`. Once one element"] # [doc = " returns false it will always return that the stream is done."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let mut stream = stream::iter(1..=10).take_while(|x| *x <= 3);"] # [doc = ""] # [doc = " assert_eq!(Some(1), stream.next().await);"] # [doc = " assert_eq!(Some(2), stream.next().await);"] # [doc = " assert_eq!(Some(3), stream.next().await);"] # [doc = " assert_eq!(None, stream.next().await);"] # [doc = " # }"] # [doc = " ```"] fn take_while < F > (self , f : F) -> TakeWhile < Self , F > where F : FnMut (& Self :: Item) -> bool , Self : Sized , { TakeWhile :: new (self , f) } # [doc = " Creates a new stream that will skip the `n` first items of the"] # [doc = " underlying stream."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let mut stream = stream::iter(1..=10).skip(7);"] # [doc = ""] # [doc = " assert_eq!(Some(8), stream.next().await);"] # [doc = " assert_eq!(Some(9), stream.next().await);"] # [doc = " assert_eq!(Some(10), stream.next().await);"] # [doc = " assert_eq!(None, stream.next().await);"] # [doc = " # }"] # [doc = " ```"] fn skip (self , n : usize) -> Skip < Self > where Self : Sized , { Skip :: new (self , n) } # [doc = " Skip elements from the underlying stream while the provided predicate"] # [doc = " resolves to `true`."] # [doc = ""] # [doc = " This function, like [`Iterator::skip_while`], will ignore elements from the"] # [doc = " stream until the predicate `f` resolves to `false`. Once one element"] # [doc = " returns false, the rest of the elements will be yielded."] # [doc = ""] # [doc = " [`Iterator::skip_while`]: std::iter::Iterator::skip_while()"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = " let mut stream = stream::iter(vec![1,2,3,4,1]).skip_while(|x| *x < 3);"] # [doc = ""] # [doc = " assert_eq!(Some(3), stream.next().await);"] # [doc = " assert_eq!(Some(4), stream.next().await);"] # [doc = " assert_eq!(Some(1), stream.next().await);"] # [doc = " assert_eq!(None, stream.next().await);"] # [doc = " # }"] # [doc = " ```"] fn skip_while < F > (self , f : F) -> SkipWhile < Self , F > where F : FnMut (& Self :: Item) -> bool , Self : Sized , { SkipWhile :: new (self , f) } # [doc = " Tests if every element of the stream matches a predicate."] # [doc = ""] # [doc = " Equivalent to:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " async fn all<F>(&mut self, f: F) -> bool;"] # [doc = " ```"] # [doc = ""] # [doc = " `all()` takes a closure that returns `true` or `false`. It applies"] # [doc = " this closure to each element of the stream, and if they all return"] # [doc = " `true`, then so does `all`. If any of them return `false`, it"] # [doc = " returns `false`. An empty stream returns `true`."] # [doc = ""] # [doc = " `all()` is short-circuiting; in other words, it will stop processing"] # [doc = " as soon as it finds a `false`, given that no matter what else happens,"] # [doc = " the result will also be `false`."] # [doc = ""] # [doc = " An empty stream returns `true`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let a = [1, 2, 3];"] # [doc = ""] # [doc = " assert!(stream::iter(&a).all(|&x| x > 0).await);"] # [doc = ""] # [doc = " assert!(!stream::iter(&a).all(|&x| x > 2).await);"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " Stopping at the first `false`:"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let a = [1, 2, 3];"] # [doc = ""] # [doc = " let mut iter = stream::iter(&a);"] # [doc = ""] # [doc = " assert!(!iter.all(|&x| x != 2).await);"] # [doc = ""] # [doc = " // we can still use `iter`, as there are more elements."] # [doc = " assert_eq!(iter.next().await, Some(&3));"] # [doc = " # }"] # [doc = " ```"] fn all < F > (& mut self , f : F) -> AllFuture < '_ , Self , F > where Self : Unpin , F : FnMut (Self :: Item) -> bool , { AllFuture :: new (self , f) } # [doc = " Tests if any element of the stream matches a predicate."] # [doc = ""] # [doc = " Equivalent to:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " async fn any<F>(&mut self, f: F) -> bool;"] # [doc = " ```"] # [doc = ""] # [doc = " `any()` takes a closure that returns `true` or `false`. It applies"] # [doc = " this closure to each element of the stream, and if any of them return"] # [doc = " `true`, then so does `any()`. If they all return `false`, it"] # [doc = " returns `false`."] # [doc = ""] # [doc = " `any()` is short-circuiting; in other words, it will stop processing"] # [doc = " as soon as it finds a `true`, given that no matter what else happens,"] # [doc = " the result will also be `true`."] # [doc = ""] # [doc = " An empty stream returns `false`."] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let a = [1, 2, 3];"] # [doc = ""] # [doc = " assert!(stream::iter(&a).any(|&x| x > 0).await);"] # [doc = ""] # [doc = " assert!(!stream::iter(&a).any(|&x| x > 5).await);"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " Stopping at the first `true`:"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " let a = [1, 2, 3];"] # [doc = ""] # [doc = " let mut iter = stream::iter(&a);"] # [doc = ""] # [doc = " assert!(iter.any(|&x| x != 2).await);"] # [doc = ""] # [doc = " // we can still use `iter`, as there are more elements."] # [doc = " assert_eq!(iter.next().await, Some(&2));"] # [doc = " # }"] # [doc = " ```"] fn any < F > (& mut self , f : F) -> AnyFuture < '_ , Self , F > where Self : Unpin , F : FnMut (Self :: Item) -> bool , { AnyFuture :: new (self , f) } # [doc = " Combine two streams into one by first returning all values from the"] # [doc = " first stream then all values from the second stream."] # [doc = ""] # [doc = " As long as `self` still has values to emit, no values from `other` are"] # [doc = " emitted, even if some are ready."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " let one = stream::iter(vec![1, 2, 3]);"] # [doc = " let two = stream::iter(vec![4, 5, 6]);"] # [doc = ""] # [doc = " let mut stream = one.chain(two);"] # [doc = ""] # [doc = " assert_eq!(stream.next().await, Some(1));"] # [doc = " assert_eq!(stream.next().await, Some(2));"] # [doc = " assert_eq!(stream.next().await, Some(3));"] # [doc = " assert_eq!(stream.next().await, Some(4));"] # [doc = " assert_eq!(stream.next().await, Some(5));"] # [doc = " assert_eq!(stream.next().await, Some(6));"] # [doc = " assert_eq!(stream.next().await, None);"] # [doc = " # }"] # [doc = " ```"] fn chain < U > (self , other : U) -> Chain < Self , U > where U : Stream < Item = Self :: Item > , Self : Sized , { Chain :: new (self , other) } # [doc = " A combinator that applies a function to every element in a stream"] # [doc = " producing a single, final value."] # [doc = ""] # [doc = " Equivalent to:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " async fn fold<B, F>(self, init: B, f: F) -> B;"] # [doc = " ```"] # [doc = ""] # [doc = " # Examples"] # [doc = " Basic usage:"] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, *};"] # [doc = ""] # [doc = " let s = stream::iter(vec![1u8, 2, 3]);"] # [doc = " let sum = s.fold(0, |acc, x| acc + x).await;"] # [doc = ""] # [doc = " assert_eq!(sum, 6);"] # [doc = " # }"] # [doc = " ```"] fn fold < B , F > (self , init : B , f : F) -> FoldFuture < Self , B , F > where Self : Sized , F : FnMut (B , Self :: Item) -> B , { FoldFuture :: new (self , init , f) } # [doc = " Drain stream pushing all emitted values into a collection."] # [doc = ""] # [doc = " Equivalent to:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " async fn collect<T>(self) -> T;"] # [doc = " ```"] # [doc = ""] # [doc = " `collect` streams all values, awaiting as needed. Values are pushed into"] # [doc = " a collection. A number of different target collection types are"] # [doc = " supported, including [`Vec`], [`String`], and [`Bytes`]."] # [doc = ""] # [doc = " [`Bytes`]: https://docs.rs/bytes/0.6.0/bytes/struct.Bytes.html"] # [doc = ""] # [doc = " # `Result`"] # [doc = ""] # [doc = " `collect()` can also be used with streams of type `Result<T, E>` where"] # [doc = " `T: FromStream<_>`. In this case, `collect()` will stream as long as"] # [doc = " values yielded from the stream are `Ok(_)`. If `Err(_)` is encountered,"] # [doc = " streaming is terminated and `collect()` returns the `Err`."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " `FromStream` is currently a sealed trait. Stabilization is pending"] # [doc = " enhancements to the Rust language."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " let doubled: Vec<i32> ="] # [doc = "     stream::iter(vec![1, 2, 3])"] # [doc = "         .map(|x| x * 2)"] # [doc = "         .collect()"] # [doc = "         .await;"] # [doc = ""] # [doc = " assert_eq!(vec![2, 4, 6], doubled);"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " Collecting a stream of `Result` values"] # [doc = ""] # [doc = " ```"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " // A stream containing only `Ok` values will be collected"] # [doc = " let values: Result<Vec<i32>, &str> ="] # [doc = "     stream::iter(vec![Ok(1), Ok(2), Ok(3)])"] # [doc = "         .collect()"] # [doc = "         .await;"] # [doc = ""] # [doc = " assert_eq!(Ok(vec![1, 2, 3]), values);"] # [doc = ""] # [doc = " // A stream containing `Err` values will return the first error."] # [doc = " let results = vec![Ok(1), Err(\"no\"), Ok(2), Ok(3), Err(\"nein\")];"] # [doc = ""] # [doc = " let values: Result<Vec<i32>, &str> ="] # [doc = "     stream::iter(results)"] # [doc = "         .collect()"] # [doc = "         .await;"] # [doc = ""] # [doc = " assert_eq!(Err(\"no\"), values);"] # [doc = " # }"] # [doc = " ```"] fn collect < T > (self) -> Collect < Self , T > where T : FromStream < Self :: Item > , Self : Sized , { Collect :: new (self) } # [doc = " Applies a per-item timeout to the passed stream."] # [doc = ""] # [doc = " `timeout()` takes a `Duration` that represents the maximum amount of"] # [doc = " time each element of the stream has to complete before timing out."] # [doc = ""] # [doc = " If the wrapped stream yields a value before the deadline is reached, the"] # [doc = " value is returned. Otherwise, an error is returned. The caller may decide"] # [doc = " to continue consuming the stream and will eventually get the next source"] # [doc = " stream value once it becomes available. See"] # [doc = " [`timeout_repeating`](StreamExt::timeout_repeating) for an alternative"] # [doc = " where the timeouts will repeat."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " This function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " Polling the returned stream will continue to poll the inner stream even"] # [doc = " if one or more items time out."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Suppose we have a stream `int_stream` that yields 3 numbers (1, 2, 3):"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = " use std::time::Duration;"] # [doc = " # let int_stream = stream::iter(1..=3);"] # [doc = ""] # [doc = " let int_stream = int_stream.timeout(Duration::from_secs(1));"] # [doc = " tokio::pin!(int_stream);"] # [doc = ""] # [doc = " // When no items time out, we get the 3 elements in succession:"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(1)));"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(2)));"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(3)));"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(None));"] # [doc = ""] # [doc = " // If the second item times out, we get an error and continue polling the stream:"] # [doc = " # let mut int_stream = stream::iter(vec![Ok(1), Err(()), Ok(2), Ok(3)]);"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(1)));"] # [doc = " assert!(int_stream.try_next().await.is_err());"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(2)));"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(3)));"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(None));"] # [doc = ""] # [doc = " // If we want to stop consuming the source stream the first time an"] # [doc = " // element times out, we can use the `take_while` operator:"] # [doc = " # let int_stream = stream::iter(vec![Ok(1), Err(()), Ok(2), Ok(3)]);"] # [doc = " let mut int_stream = int_stream.take_while(Result::is_ok);"] # [doc = ""] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(1)));"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(None));"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " Once a timeout error is received, no further events will be received"] # [doc = " unless the wrapped stream yields a value (timeouts do not repeat)."] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\", start_paused = true)]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{StreamExt, wrappers::IntervalStream};"] # [doc = " use std::time::Duration;"] # [doc = " let interval_stream = IntervalStream::new(tokio::time::interval(Duration::from_millis(100)));"] # [doc = " let timeout_stream = interval_stream.timeout(Duration::from_millis(10));"] # [doc = " tokio::pin!(timeout_stream);"] # [doc = ""] # [doc = " // Only one timeout will be received between values in the source stream."] # [doc = " assert!(timeout_stream.try_next().await.is_ok());"] # [doc = " assert!(timeout_stream.try_next().await.is_err(), \"expected one timeout\");"] # [doc = " assert!(timeout_stream.try_next().await.is_ok(), \"expected no more timeouts\");"] # [doc = " # }"] # [doc = " ```"] # [cfg (feature = "time")] # [cfg_attr (docsrs , doc (cfg (feature = "time")))] fn timeout (self , duration : Duration) -> Timeout < Self > where Self : Sized , { Timeout :: new (self , duration) } # [doc = " Applies a per-item timeout to the passed stream."] # [doc = ""] # [doc = " `timeout_repeating()` takes an [`Interval`] that controls the time each"] # [doc = " element of the stream has to complete before timing out."] # [doc = ""] # [doc = " If the wrapped stream yields a value before the deadline is reached, the"] # [doc = " value is returned. Otherwise, an error is returned. The caller may decide"] # [doc = " to continue consuming the stream and will eventually get the next source"] # [doc = " stream value once it becomes available. Unlike `timeout()`, if no value"] # [doc = " becomes available before the deadline is reached, additional errors are"] # [doc = " returned at the specified interval. See [`timeout`](StreamExt::timeout)"] # [doc = " for an alternative where the timeouts do not repeat."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " This function consumes the stream passed into it and returns a"] # [doc = " wrapped version of it."] # [doc = ""] # [doc = " Polling the returned stream will continue to poll the inner stream even"] # [doc = " if one or more items time out."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Suppose we have a stream `int_stream` that yields 3 numbers (1, 2, 3):"] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\")]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = " use std::time::Duration;"] # [doc = " # let int_stream = stream::iter(1..=3);"] # [doc = ""] # [doc = " let int_stream = int_stream.timeout_repeating(tokio::time::interval(Duration::from_secs(1)));"] # [doc = " tokio::pin!(int_stream);"] # [doc = ""] # [doc = " // When no items time out, we get the 3 elements in succession:"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(1)));"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(2)));"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(3)));"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(None));"] # [doc = ""] # [doc = " // If the second item times out, we get an error and continue polling the stream:"] # [doc = " # let mut int_stream = stream::iter(vec![Ok(1), Err(()), Ok(2), Ok(3)]);"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(1)));"] # [doc = " assert!(int_stream.try_next().await.is_err());"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(2)));"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(3)));"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(None));"] # [doc = ""] # [doc = " // If we want to stop consuming the source stream the first time an"] # [doc = " // element times out, we can use the `take_while` operator:"] # [doc = " # let int_stream = stream::iter(vec![Ok(1), Err(()), Ok(2), Ok(3)]);"] # [doc = " let mut int_stream = int_stream.take_while(Result::is_ok);"] # [doc = ""] # [doc = " assert_eq!(int_stream.try_next().await, Ok(Some(1)));"] # [doc = " assert_eq!(int_stream.try_next().await, Ok(None));"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " Timeout errors will be continuously produced at the specified interval"] # [doc = " until the wrapped stream yields a value."] # [doc = ""] # [doc = " ```"] # [doc = " # #[tokio::main(flavor = \"current_thread\", start_paused = true)]"] # [doc = " # async fn main() {"] # [doc = " use tokio_stream::{StreamExt, wrappers::IntervalStream};"] # [doc = " use std::time::Duration;"] # [doc = " let interval_stream = IntervalStream::new(tokio::time::interval(Duration::from_millis(23)));"] # [doc = " let timeout_stream = interval_stream.timeout_repeating(tokio::time::interval(Duration::from_millis(9)));"] # [doc = " tokio::pin!(timeout_stream);"] # [doc = ""] # [doc = " // Multiple timeouts will be received between values in the source stream."] # [doc = " assert!(timeout_stream.try_next().await.is_ok());"] # [doc = " assert!(timeout_stream.try_next().await.is_err(), \"expected one timeout\");"] # [doc = " assert!(timeout_stream.try_next().await.is_err(), \"expected a second timeout\");"] # [doc = " // Will eventually receive another value from the source stream..."] # [doc = " assert!(timeout_stream.try_next().await.is_ok(), \"expected non-timeout\");"] # [doc = " # }"] # [doc = " ```"] # [cfg (feature = "time")] # [cfg_attr (docsrs , doc (cfg (feature = "time")))] fn timeout_repeating (self , interval : Interval) -> TimeoutRepeating < Self > where Self : Sized , { TimeoutRepeating :: new (self , interval) } # [doc = " Slows down a stream by enforcing a delay between items."] # [doc = ""] # [doc = " The underlying timer behind this utility has a granularity of one millisecond."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Create a throttled stream."] # [doc = " ```rust,no_run"] # [doc = " use std::time::Duration;"] # [doc = " use tokio_stream::StreamExt;"] # [doc = ""] # [doc = " # async fn dox() {"] # [doc = " let item_stream = futures::stream::repeat(\"one\").throttle(Duration::from_secs(2));"] # [doc = " tokio::pin!(item_stream);"] # [doc = ""] # [doc = " loop {"] # [doc = "     // The string will be produced at most every 2 seconds"] # [doc = "     println!(\"{:?}\", item_stream.next().await);"] # [doc = " }"] # [doc = " # }"] # [doc = " ```"] # [cfg (feature = "time")] # [cfg_attr (docsrs , doc (cfg (feature = "time")))] fn throttle (self , duration : Duration) -> Throttle < Self > where Self : Sized , { throttle (duration , self) } # [doc = " Batches the items in the given stream using a maximum duration and size for each batch."] # [doc = ""] # [doc = " This stream returns the next batch of items in the following situations:"] # [doc = "  1. The inner stream has returned at least `max_size` many items since the last batch."] # [doc = "  2. The time since the first item of a batch is greater than the given duration."] # [doc = "  3. The end of the stream is reached."] # [doc = ""] # [doc = " The length of the returned vector is never empty or greater than the maximum size. Empty batches"] # [doc = " will not be emitted if no items are received upstream."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if `max_size` is zero"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::time::Duration;"] # [doc = " use tokio::time;"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = " use futures::FutureExt;"] # [doc = ""] # [doc = " #[tokio::main]"] # [doc = " # async fn _unused() {}"] # [doc = " # #[tokio::main(flavor = \"current_thread\", start_paused = true)]"] # [doc = " async fn main() {"] # [doc = "     let iter = vec![1, 2, 3, 4].into_iter();"] # [doc = "     let stream0 = stream::iter(iter);"] # [doc = ""] # [doc = "     let iter = vec![5].into_iter();"] # [doc = "     let stream1 = stream::iter(iter)"] # [doc = "          .then(move |n| time::sleep(Duration::from_secs(5)).map(move |_| n));"] # [doc = ""] # [doc = "     let chunk_stream = stream0"] # [doc = "         .chain(stream1)"] # [doc = "         .chunks_timeout(3, Duration::from_secs(2));"] # [doc = "     tokio::pin!(chunk_stream);"] # [doc = ""] # [doc = "     // a full batch was received"] # [doc = "     assert_eq!(chunk_stream.next().await, Some(vec![1,2,3]));"] # [doc = "     // deadline was reached before max_size was reached"] # [doc = "     assert_eq!(chunk_stream.next().await, Some(vec![4]));"] # [doc = "     // last element in the stream"] # [doc = "     assert_eq!(chunk_stream.next().await, Some(vec![5]));"] # [doc = " }"] # [doc = " ```"] # [cfg (feature = "time")] # [cfg_attr (docsrs , doc (cfg (feature = "time")))] # [track_caller] fn chunks_timeout (self , max_size : usize , duration : Duration) -> ChunksTimeout < Self > where Self : Sized , { assert ! (max_size > 0 , "`max_size` must be non-zero.") ; ChunksTimeout :: new (self , max_size , duration) } # [doc = " Turns the stream into a peekable stream, whose next element can be peeked at without being"] # [doc = " consumed."] # [doc = " ```rust"] # [doc = " use tokio_stream::{self as stream, StreamExt};"] # [doc = ""] # [doc = " #[tokio::main]"] # [doc = " # async fn _unused() {}"] # [doc = " # #[tokio::main(flavor = \"current_thread\", start_paused = true)]"] # [doc = " async fn main() {"] # [doc = "     let iter = vec![1, 2, 3, 4].into_iter();"] # [doc = "     let mut stream = stream::iter(iter).peekable();"] # [doc = ""] # [doc = "     assert_eq!(*stream.peek().await.unwrap(), 1);"] # [doc = "     assert_eq!(*stream.peek().await.unwrap(), 1);"] # [doc = "     assert_eq!(stream.next().await.unwrap(), 1);"] # [doc = "     assert_eq!(*stream.peek().await.unwrap(), 2);"] # [doc = " }"] # [doc = " ```"] fn peekable (self) -> Peekable < Self > where Self : Sized , { Peekable :: new (self) } }
};
}
