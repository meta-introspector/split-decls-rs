// Generated macro for AuxImport (enum)
macro_rules! Depcrate_wit_nonstandardAuxImport {
() => {
// Module: crate::wit::nonstandard
// Provides: {"AuxImport"}
// Dependencies: {}
# [doc = " All possible types of imports that can be imported by a Wasm module."] # [doc = ""] # [doc = " This `enum` is intended to map out what an imported value is. For example"] # [doc = " this contains a ton of shims and various ways you can call a function. The"] # [doc = " base variant here is `Value` which simply means \"hook this up to the import\""] # [doc = " and the signatures will match up."] # [doc = ""] # [doc = " Note that this is *not* the same as the webidl bindings section. This is"] # [doc = " intended to be coupled with that to map out what actually gets hooked up to"] # [doc = " an import in the Wasm module. The two work in tandem."] # [doc = ""] # [doc = " Some of these items here are native to JS (like `Value`, indexing"] # [doc = " operations, etc). Others are shims generated by wasm-bindgen (like `Closure`"] # [doc = " or `Instanceof`)."] # [derive (Debug)] pub enum AuxImport { # [doc = " This import is expected to simply be whatever is the value that's"] # [doc = " imported"] Value (AuxValue) , # [doc = " A static method on a class is being imported, and the `this` of the"] # [doc = " function call is expected to always be the class."] ValueWithThis (JsImport , String) , # [doc = " This import is expected to be a function that takes an `externref` and"] # [doc = " returns a `bool`. It's expected that it tests if the argument is an"] # [doc = " instance of (using `instanceof`) the name specified."] # [doc = ""] # [doc = " TODO: can we use `Reflect` or something like that to avoid an extra kind"] # [doc = " of import here?"] Instanceof (JsImport) , # [doc = " This import is expected to be a shim that returns the JS value named by"] # [doc = " `JsImport`."] Static { js : JsImport , optional : bool } , # [doc = " This import is expected to be a shim that returns an exported `JsString`."] String (String) , # [doc = " This import is a generic cast function, monomorphised for a specific `fn(A) -> R` signature."] Cast { sig_comment : String } , # [doc = " This import is expected to be a shim that simply calls the `foo` method"] # [doc = " on the first object, passing along all other parameters and returning"] # [doc = " the resulting value."] StructuralMethod (String) , # [doc = " This import is a \"structural getter\" which simply returns the `.field`"] # [doc = " value of the first argument as an object."] # [doc = ""] # [doc = " e.g. `function(x) { return x.foo; }`"] StructuralGetter (String) , # [doc = " This import is a \"structural getter\" which simply returns the `.field`"] # [doc = " value of the specified class"] # [doc = ""] # [doc = " e.g. `function() { return TheClass.foo; }`"] StructuralClassGetter (JsImport , String) , # [doc = " This import is a \"structural setter\" which simply sets the `.field`"] # [doc = " value of the first argument to the second argument."] # [doc = ""] # [doc = " e.g. `function(x, y) { x.foo = y; }`"] StructuralSetter (String) , # [doc = " This import is a \"structural setter\" which simply sets the `.field`"] # [doc = " value of the specified class to the first argument of the function."] # [doc = ""] # [doc = " e.g. `function(x) { TheClass.foo = x; }`"] StructuralClassSetter (JsImport , String) , # [doc = " This import is expected to be a shim that is an indexing getter of the"] # [doc = " JS class here, where the first argument of the function is the field to"] # [doc = " look up. The return value is the value of the field."] # [doc = ""] # [doc = " e.g. `function(x) { return TheClass[x]; }`"] # [doc = ""] # [doc = " TODO: can we use `Reflect` or something like that to avoid an extra kind"] # [doc = " of import here?"] IndexingGetterOfClass (JsImport) , # [doc = " This import is expected to be a shim that is an indexing getter of the"] # [doc = " first argument interpreted as an object where the field to look up is"] # [doc = " the second argument."] # [doc = ""] # [doc = " e.g. `function(x, y) { return x[y]; }`"] # [doc = ""] # [doc = " TODO: can we use `Reflect` or something like that to avoid an extra kind"] # [doc = " of import here?"] IndexingGetterOfObject , # [doc = " This import is expected to be a shim that is an indexing setter of the"] # [doc = " JS class here, where the first argument of the function is the field to"] # [doc = " set and the second is the value to set it to."] # [doc = ""] # [doc = " e.g. `function(x, y) { TheClass[x] = y; }`"] # [doc = ""] # [doc = " TODO: can we use `Reflect` or something like that to avoid an extra kind"] # [doc = " of import here?"] IndexingSetterOfClass (JsImport) , # [doc = " This import is expected to be a shim that is an indexing setter of the"] # [doc = " first argument interpreted as an object where the next two fields are"] # [doc = " the field to set and the value to set it to."] # [doc = ""] # [doc = " e.g. `function(x, y, z) { x[y] = z; }`"] # [doc = ""] # [doc = " TODO: can we use `Reflect` or something like that to avoid an extra kind"] # [doc = " of import here?"] IndexingSetterOfObject , # [doc = " This import is expected to be a shim that is an indexing deleter of the"] # [doc = " JS class here, where the first argument of the function is the field to"] # [doc = " delete."] # [doc = ""] # [doc = " e.g. `function(x) { delete TheClass[x]; }`"] # [doc = ""] # [doc = " TODO: can we use `Reflect` or something like that to avoid an extra kind"] # [doc = " of import here?"] IndexingDeleterOfClass (JsImport) , # [doc = " This import is expected to be a shim that is an indexing deleter of the"] # [doc = " first argument interpreted as an object where the second argument is"] # [doc = " the field to delete."] # [doc = ""] # [doc = " e.g. `function(x, y) { delete x[y]; }`"] # [doc = ""] # [doc = " TODO: can we use `Reflect` or something like that to avoid an extra kind"] # [doc = " of import here?"] IndexingDeleterOfObject , # [doc = " This import is a generated shim which will wrap the provided pointer in"] # [doc = " a JS object corresponding to the Class name given here. The class name"] # [doc = " is one that is exported from the Rust/wasm."] # [doc = ""] # [doc = " TODO: sort of like the export map below we should ideally create the"] # [doc = " `externref` from within Rust itself and then return it directly rather than"] # [doc = " requiring an intrinsic here to do so."] WrapInExportedClass (String) , # [doc = " This is an intrinsic function expected to be implemented with a JS glue"] # [doc = " shim. Each intrinsic has its own expected signature and implementation."] Intrinsic (Intrinsic) , # [doc = " This is a function which returns a URL pointing to a specific file,"] # [doc = " usually a JS snippet. The supplied path is relative to the JS glue shim."] # [doc = " The Option may contain the contents of the linked file, so it can be"] # [doc = " embedded."] LinkTo (String , Option < String >) , # [doc = " This import is a generated shim which will attempt to unwrap JsValue to an"] # [doc = " instance of the given exported class. The class name is one that is"] # [doc = " exported from the Rust/wasm."] UnwrapExportedClass (String) , }
};
}
