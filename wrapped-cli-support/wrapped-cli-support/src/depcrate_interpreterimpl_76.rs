// Generated macro for impl_76 (impl)
macro_rules! Depcrate_interpreterimpl_76 {
() => {
// Module: crate::interpreter
// Provides: {"impl_76"}
// Dependencies: {}
impl Frame < '_ > { fn eval (& mut self , seq : InstrSeqId) -> anyhow :: Result < () > { use walrus :: ir :: * ; for (instr , _) in self . func . block (seq) . iter () { let stack = & mut self . interp . scratch ; match instr { Instr :: Const (c) => match c . value { Value :: I32 (n) => stack . push (n) , _ => bail ! ("non-i32 constant") , } , Instr :: LocalGet (e) => stack . push (self . locals . get (& e . local) . cloned () . unwrap_or (0)) , Instr :: LocalSet (e) => { let val = stack . pop () . unwrap () ; self . locals . insert (e . local , val) ; } Instr :: LocalTee (e) => { let val = * stack . last () . unwrap () ; self . locals . insert (e . local , val) ; } Instr :: GlobalGet (_) => stack . push (self . interp . sp) , Instr :: GlobalSet (_) => { let val = stack . pop () . unwrap () ; self . interp . sp = val ; } Instr :: Binop (e) => { let rhs = stack . pop () . unwrap () ; let lhs = stack . pop () . unwrap () ; stack . push (match e . op { BinaryOp :: I32Sub => lhs - rhs , BinaryOp :: I32Add => lhs + rhs , op => bail ! ("invalid binary op {op:?}") , }) ; } Instr :: Load (e) => { let address = stack . pop () . unwrap () ; let address = address as u32 + e . arg . offset ; ensure ! (address > 0 , "Read a negative or zero address value from the stack. Did we run out of memory?") ; let width = e . kind . width () ; ensure ! (address % width == 0) ; let val = self . interp . mem [address as usize / 4] ; if width == 4 { stack . push (val) } else if width == 1 { let result = val . to_le_bytes () [(address % 4) as usize] ; let LoadKind :: I32_8 { kind } = e . kind else { panic ! ("Unhandled load kind {:?}" , e . kind) } ; match kind { ExtendedLoad :: SignExtend => { stack . push (result as i8 as i32) ; } ExtendedLoad :: ZeroExtend | ExtendedLoad :: ZeroExtendAtomic => { stack . push (result as i32) ; } } ; } else { panic ! ("Unhandled load width {width}") ; } } Instr :: Store (e) => { let value = stack . pop () . unwrap () ; let address = stack . pop () . unwrap () ; let address = address as u32 + e . arg . offset ; ensure ! (address > 0 , "Read a negative or zero address value from the stack. Did we run out of memory?") ; let width = e . kind . width () ; ensure ! (address % width == 0) ; let index = address as usize / 4 ; if width == 8 { self . interp . mem [index] = value ; self . interp . mem [index + 1] = 0 ; } else if width == 4 { self . interp . mem [index] = value ; } else if width == 1 { let mut bytes = self . interp . mem [index] . to_le_bytes () ; bytes [(address % 4) as usize] = value as u8 ; self . interp . mem [index] = i32 :: from_le_bytes (bytes) ; } else { panic ! ("Unhandled store width {width}") ; } } Instr :: Return (_) => { log :: trace ! ("return") ; break ; } Instr :: Drop (_) => { log :: trace ! ("drop") ; stack . pop () . unwrap () ; } Instr :: Call (Call { func }) | Instr :: ReturnCall (ReturnCall { func }) => { let func = * func ; if Some (func) == self . interp . describe_id { let val = stack . pop () . unwrap () ; log :: trace ! ("__wbindgen_describe({val})") ; self . interp . descriptor . push (val as u32) ; } else if Some (func) == self . interp . describe_cast_id { log :: trace ! ("__wbindgen_describe_cast()") ; self . interp . sp = self . interp . mem . len () as i32 ; break ; } else { if self . interp . skip_calls . contains (& func) { continue ; } if self . module . funcs . get (func) . name . as_ref () . is_some_and (| name | { name . starts_with ("__llvm_profile_init") || name . starts_with ("__llvm_profile_register_function") || name . starts_with ("__llvm_profile_register_function") }) { continue ; } let ty = self . module . types . get (self . module . funcs . get (func) . ty ()) ; let args = (0 .. ty . params () . len ()) . map (| _ | stack . pop () . unwrap ()) . collect :: < Vec < _ > > () ; self . interp . call (func , self . module , & args) ; } if let Instr :: ReturnCall (_) = instr { log :: trace ! ("return_call") ; break ; } } Instr :: Block (block) => { self . eval (block . seq) ? ; } Instr :: Try (block) => { self . eval (block . seq) ? ; } s => bail ! ("unknown instruction {s:?}") , } } Ok (()) } }
};
}
