// Generated macro for AuxExportKind (enum)
macro_rules! Depcrate_wit_nonstandardAuxExportKind {
() => {
// Module: crate::wit::nonstandard
// Provides: {"AuxExportKind"}
// Dependencies: {}
# [doc = " All possible kinds of exports from a Wasm module."] # [doc = ""] # [doc = " This `enum` says where to place an exported Wasm function. For example it"] # [doc = " may want to get hooked up to a JS class, or it may want to be exported as a"] # [doc = " free function (etc)."] # [doc = ""] # [doc = " TODO: it feels like this should not really be here per se. We probably want"] # [doc = " to either construct the JS object itself from within Wasm or somehow move"] # [doc = " more of this information into some other section. Really what this is is"] # [doc = " sort of an \"export map\" saying how to wire up all the free functions from"] # [doc = " the Wasm module into the output expected JS module. All our functions here"] # [doc = " currently take integer parameters and require a JS wrapper, but ideally"] # [doc = " we'd change them one day to taking/receiving `externref` which then use some"] # [doc = " sort of webidl import to customize behavior or something like that. In any"] # [doc = " case this doesn't feel quite right in terms of privilege separation, so"] # [doc = " we'll want to work on this. For now though it works."] # [derive (Debug)] pub enum AuxExportKind { # [doc = " A free function that's just listed on the exported module"] Function (String) , # [doc = " A free function that receives JS `this` as its first parameter"] FunctionThis (String) , # [doc = " A function that's used to create an instance of a class. The function"] # [doc = " actually return just an integer which is put on an JS object currently."] Constructor (String) , # [doc = " A function that's associated with a class."] # [doc = ""] # [doc = " This can either be a static method (indicated by `AuxReceiverKind::None`),"] # [doc = " which is basically just a free function namespaced under the class, or"] # [doc = " a proper method."] # [doc = ""] # [doc = " It can also be a getter or a setter for a (possibly static) field of"] # [doc = " the class, in which case `name` is the name of the field."] # [doc = ""] # [doc = " If the function isn't static, the first argument is the index of the"] # [doc = " Rust object in the JS heap."] Method { class : String , name : String , receiver : AuxReceiverKind , kind : AuxExportedMethodKind , } , }
};
}
