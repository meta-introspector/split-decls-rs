// Generated macro for Actor (trait)
macro_rules! Depcrate_actorActor {
() => {
// Module: crate::actor
// Provides: {"Actor"}
// Dependencies: {}
# [doc = " Actors are objects which encapsulate state and behavior."] # [doc = ""] # [doc = " Actors run within a specific execution context"] # [doc = " [`Context<A>`](struct.Context.html). The context object is available"] # [doc = " only during execution. Each actor has a separate execution"] # [doc = " context. The execution context also controls the lifecycle of an"] # [doc = " actor."] # [doc = ""] # [doc = " Actors communicate exclusively by exchanging messages. The sender"] # [doc = " actor can wait for a response. Actors are not referenced directly,"] # [doc = " but by address [`Addr`](struct.Addr.html) To be able to handle a"] # [doc = " specific message actor has to provide a"] # [doc = " [`Handler<M>`](trait.Handler.html) implementation for this"] # [doc = " message. All messages are statically typed. A message can be"] # [doc = " handled in asynchronous fashion. An actor can spawn other actors"] # [doc = " or add futures or streams to the execution context. The actor"] # [doc = " trait provides several methods that allow controlling the actor"] # [doc = " lifecycle."] # [doc = ""] # [doc = " # Actor lifecycle"] # [doc = ""] # [doc = " ## Started"] # [doc = ""] # [doc = " An actor starts in the `Started` state, during this state the"] # [doc = " `started` method gets called."] # [doc = ""] # [doc = " ## Running"] # [doc = ""] # [doc = " After an actor's `started` method got called, the actor"] # [doc = " transitions to the `Running` state. An actor can stay in the"] # [doc = " `running` state for an indefinite amount of time."] # [doc = ""] # [doc = " ## Stopping"] # [doc = ""] # [doc = " The actor's execution state changes to `stopping` in the following"] # [doc = " situations:"] # [doc = ""] # [doc = " * `Context::stop` gets called by actor itself"] # [doc = " * all addresses to the actor get dropped"] # [doc = " * no evented objects are registered in its context."] # [doc = ""] # [doc = " An actor can return from the `stopping` state to the `running`"] # [doc = " state by creating a new address or adding an evented object, like"] # [doc = " a future or stream, in its `Actor::stopping` method."] # [doc = ""] # [doc = " If an actor changed to a `stopping` state because"] # [doc = " `Context::stop()` got called, the context then immediately stops"] # [doc = " processing incoming messages and calls the `Actor::stopping()`"] # [doc = " method. If an actor does not return back to a `running` state,"] # [doc = " all unprocessed messages get dropped."] # [doc = ""] # [doc = " ## Stopped"] # [doc = ""] # [doc = " If an actor does not modify execution context while in stopping"] # [doc = " state, the actor state changes to `Stopped`. This state is"] # [doc = " considered final and at this point the actor gets dropped."] # [allow (unused_variables)] pub trait Actor : Sized + Unpin + 'static { # [doc = " Actor execution context type"] type Context : ActorContext ; # [doc = " Called when an actor gets polled the first time."] fn started (& mut self , ctx : & mut Self :: Context) { } # [doc = " Called after an actor is in `Actor::Stopping` state."] # [doc = ""] # [doc = " There can be several reasons for stopping:"] # [doc = ""] # [doc = " - `Context::stop` gets called by the actor itself."] # [doc = " - All addresses to the current actor get dropped and no more"] # [doc = "   evented objects are left in the context."] # [doc = ""] # [doc = " An actor can return from the stopping state to the running"] # [doc = " state by returning `Running::Continue`."] fn stopping (& mut self , ctx : & mut Self :: Context) -> Running { Running :: Stop } # [doc = " Called after an actor is stopped."] # [doc = ""] # [doc = " This method can be used to perform any needed cleanup work or"] # [doc = " to spawn more actors. This is the final state, after this"] # [doc = " method got called, the actor will be dropped."] fn stopped (& mut self , ctx : & mut Self :: Context) { } # [doc = " Start a new asynchronous actor, returning its address."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use actix::prelude::*;"] # [doc = ""] # [doc = " struct MyActor;"] # [doc = " impl Actor for MyActor {"] # [doc = "     type Context = Context<Self>;"] # [doc = " }"] # [doc = ""] # [doc = " #[actix::main]"] # [doc = " async fn main() {"] # [doc = "     // start actor and get its address"] # [doc = "     let addr = MyActor.start();"] # [doc = "     # System::current().stop();"] # [doc = " }"] # [doc = " ```"] fn start (self) -> Addr < Self > where Self : Actor < Context = Context < Self > > , { Context :: new () . run (self) } # [doc = " Construct and start a new asynchronous actor, returning its"] # [doc = " address."] # [doc = ""] # [doc = " This is constructs a new actor using the `Default` trait, and"] # [doc = " invokes its `start` method."] fn start_default () -> Addr < Self > where Self : Actor < Context = Context < Self > > + Default , { Self :: default () . start () } # [doc = " Start new actor in arbiter's thread."] fn start_in_arbiter < F > (wrk : & ArbiterHandle , f : F) -> Addr < Self > where Self : Actor < Context = Context < Self > > , F : FnOnce (& mut Context < Self >) -> Self + Send + 'static , { let (tx , rx) = channel :: channel (DEFAULT_CAPACITY) ; wrk . spawn_fn (move | | { let mut ctx = Context :: with_receiver (rx) ; let act = f (& mut ctx) ; let fut = ctx . into_future (act) ; actix_rt :: spawn (fut) ; }) ; Addr :: new (tx) } # [doc = " Start a new asynchronous actor given a `Context`."] # [doc = ""] # [doc = " Use this method if you need the `Context` object during actor"] # [doc = " initialization."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use actix::prelude::*;"] # [doc = ""] # [doc = " struct MyActor {"] # [doc = "     val: usize,"] # [doc = " }"] # [doc = " impl Actor for MyActor {"] # [doc = "     type Context = Context<Self>;"] # [doc = " }"] # [doc = ""] # [doc = " #[actix::main]"] # [doc = " async fn main() {"] # [doc = "     let addr = MyActor::create(|ctx: &mut Context<MyActor>| MyActor { val: 10 });"] # [doc = "     # System::current().stop();"] # [doc = " }"] # [doc = " ```"] fn create < F > (f : F) -> Addr < Self > where Self : Actor < Context = Context < Self > > , F : FnOnce (& mut Context < Self >) -> Self , { let mut ctx = Context :: new () ; let act = f (& mut ctx) ; ctx . run (act) } }
};
}
