// Generated macro for State (struct)
macro_rules! DepcrateState {
() => {
// Module: crate
// Provides: {"State"}
// Dependencies: {}
# [doc = " An in-memory cache of a fully parsed git index file."] # [doc = ""] # [doc = " As opposed to a snapshot, it's meant to be altered and eventually be written back to disk or converted into a tree."] # [doc = " We treat index and its state synonymous."] # [doc = ""] # [doc = " # A note on safety"] # [doc = ""] # [doc = " An index (i.e. [`State`]) created by hand is not guaranteed to have valid entry paths as they are entirely controlled"] # [doc = " by the caller, without applying any level of validation."] # [doc = ""] # [doc = " This means that before using these paths to recreate files on disk, *they must be validated*."] # [doc = ""] # [doc = " It's notable that it's possible to manufacture tree objects which contain names like `.git/hooks/pre-commit`"] # [doc = " which then will look like `.git/hooks/pre-commit` in the index, which doesn't care that the name came from a single"] # [doc = " tree instead of from trees named `.git`, `hooks` and a blob named `pre-commit`. The effect is still the same - an invalid"] # [doc = " path is presented in the index and its consumer must validate each path component before usage."] # [doc = ""] # [doc = " It's recommended to do that using `gix_worktree::Stack` which has it built-in if it's created `for_checkout()`. Alternatively"] # [doc = " one can validate component names with `gix_validate::path::component()`."] # [derive (Clone)] pub struct State { # [doc = " The kind of object hash used when storing the underlying file."] # [doc = ""] # [doc = " Empty states for example won't have a single object id, so deduction of the hash used isn't always possible."] object_hash : gix_hash :: Kind , # [doc = " The time at which the state was created, indicating its freshness compared to other files on disk."] # [doc = ""] # [doc = " Note that on platforms that only have a precisions of a second for this time, we will treat all entries with the"] # [doc = " same timestamp as this as potentially changed, checking more thoroughly if a change actually happened."] timestamp : FileTime , version : Version , entries : Vec < Entry > , # [doc = " A memory area keeping all index paths, in full length, independently of the index version."] # [doc = ""] # [doc = " Ranges into this storage are referred to by parts of `entries`."] path_backing : PathStorage , # [doc = " True if one entry in the index has a special marker mode"] is_sparse : bool , end_of_index_at_decode_time : bool , offset_table_at_decode_time : bool , tree : Option < extension :: Tree > , link : Option < extension :: Link > , resolve_undo : Option < extension :: resolve_undo :: Paths > , untracked : Option < extension :: UntrackedCache > , fs_monitor : Option < extension :: FsMonitor > , }
};
}
