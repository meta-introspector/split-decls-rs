// Generated macro for ParseError (trait)
macro_rules! Depcrate_errorParseError {
() => {
// Module: crate::error
// Provides: {"ParseError"}
// Dependencies: {}
# [doc = " Trait which defines a combine parse error."] # [doc = ""] # [doc = " A parse error is composed of zero or more `StreamError` instances which gets added to it as"] # [doc = " errors are encountered during parsing."] pub trait ParseError < Item , Range , Position > : Sized + PartialEq { type StreamError : StreamError < Item , Range > ; # [doc = " Constructs an empty error."] # [doc = ""] # [doc = " An empty error is expected to be cheap to create as it is frequently created and discarded."] fn empty (position : Position) -> Self ; # [doc = " Creates a `ParseError` from a single `Self::StreamError`"] fn from_error (position : Position , err : Self :: StreamError) -> Self { let mut errors = Self :: empty (position) ; errors . add (err) ; errors } fn position (& self) -> Position { unimplemented ! () } # [doc = " Sets the position of this `ParseError`"] fn set_position (& mut self , position : Position) ; # [doc = " Merges two errors. If they exist at the same position the errors of `other` are"] # [doc = " added to `self` (using the semantics of `add`). If they are not at the same"] # [doc = " position the error furthest ahead are returned, ignoring the other `ParseError`."] fn merge (self , other : Self) -> Self { other } # [doc = " Adds a `StreamError` to `self`."] # [doc = ""] # [doc = " It is up to each individual error type to define what adding an error does, some may push"] # [doc = " it to a vector while others may only keep `self` or `err` to avoid allocation"] fn add (& mut self , err : Self :: StreamError) ; fn add_expected < E > (& mut self , info : E) where E : for < 's > ErrorInfo < 's , Item , Range > , { self . add (Self :: StreamError :: expected (info)) } fn add_unexpected < E > (& mut self , info : E) where E : for < 's > ErrorInfo < 's , Item , Range > , { self . add (Self :: StreamError :: unexpected (info)) } fn add_message < E > (& mut self , info : E) where E : for < 's > ErrorInfo < 's , Item , Range > , { self . add (Self :: StreamError :: message (info)) } # [doc = " Sets `info` as the *only* `Expected` error of `self`"] fn set_expected < F > (self_ : & mut Tracked < Self > , info : Self :: StreamError , f : F) where F : FnOnce (& mut Tracked < Self >) ; # [doc = " Removes any expected errors currently in `self`"] fn clear_expected (& mut self) { } fn is_unexpected_end_of_input (& self) -> bool ; # [doc = " Does a best-effort conversion of `self` into another `ParseError`"] fn into_other < T > (self) -> T where T : ParseError < Item , Range , Position > ; }
};
}
