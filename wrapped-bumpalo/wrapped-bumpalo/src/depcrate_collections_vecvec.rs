// Generated macro for vec (macro)
macro_rules! Depcrate_collections_vecvec {
() => {
// Module: crate::collections::vec
// Provides: {"vec"}
// Dependencies: {}
# [doc = " Creates a [`Vec`] containing the arguments."] # [doc = ""] # [doc = " `vec!` allows `Vec`s to be defined with the same syntax as array expressions."] # [doc = " There are two forms of this macro:"] # [doc = ""] # [doc = " - Create a [`Vec`] containing a given list of elements:"] # [doc = ""] # [doc = " ```"] # [doc = " use bumpalo::Bump;"] # [doc = ""] # [doc = " let b = Bump::new();"] # [doc = " let v = bumpalo::vec![in &b; 1, 2, 3];"] # [doc = " assert_eq!(v, [1, 2, 3]);"] # [doc = " ```"] # [doc = ""] # [doc = " - Create a [`Vec`] from a given element and size:"] # [doc = ""] # [doc = " ```"] # [doc = " use bumpalo::Bump;"] # [doc = ""] # [doc = " let b = Bump::new();"] # [doc = " let v = bumpalo::vec![in &b; 1; 3];"] # [doc = " assert_eq!(v, [1, 1, 1]);"] # [doc = " ```"] # [doc = ""] # [doc = " Note that unlike array expressions, this syntax supports all elements"] # [doc = " which implement [`Clone`] and the number of elements doesn't have to be"] # [doc = " a constant."] # [doc = ""] # [doc = " This will use `clone` to duplicate an expression, so one should be careful"] # [doc = " using this with types having a non-standard `Clone` implementation. For"] # [doc = " example, `bumpalo::vec![in &bump; Rc::new(1); 5]` will create a vector of five references"] # [doc = " to the same boxed integer value, not five references pointing to independently"] # [doc = " boxed integers."] # [doc = ""] # [doc = " [`Vec`]: collections/vec/struct.Vec.html"] # [doc = " [`Clone`]: https://doc.rust-lang.org/std/clone/trait.Clone.html"] # [macro_export] macro_rules ! vec { (in $ bump : expr ; $ elem : expr ; $ n : expr) => { { let n = $ n ; let mut v = $ crate :: collections :: Vec :: with_capacity_in (n , $ bump) ; if n > 0 { let elem = $ elem ; for _ in 0 .. n - 1 { v . push (elem . clone ()) ; } v . push (elem) ; } v } } ; (in $ bump : expr) => { $ crate :: collections :: Vec :: new_in ($ bump) } ; (in $ bump : expr ; $ ($ x : expr) ,*) => { { let mut v = $ crate :: collections :: Vec :: new_in ($ bump) ; $ (v . push ($ x) ;) * v } } ; (in $ bump : expr ; $ ($ x : expr ,) *) => (bumpalo :: vec ! [in $ bump ; $ ($ x) ,*]) }
};
}
