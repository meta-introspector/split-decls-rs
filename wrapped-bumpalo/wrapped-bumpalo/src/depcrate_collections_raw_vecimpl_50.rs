// Generated macro for impl_50 (impl)
macro_rules! Depcrate_collections_raw_vecimpl_50 {
() => {
// Module: crate::collections::raw_vec
// Provides: {"impl_50"}
// Dependencies: {}
impl < 'a , T > RawVec < 'a , T > { # [doc = " Gets a raw pointer to the start of the allocation. Note that this is"] # [doc = " Unique::empty() if `cap = 0` or T is zero-sized. In the former case, you must"] # [doc = " be careful."] pub fn ptr (& self) -> * mut T { self . ptr . as_ptr () } # [doc = " Gets the capacity of the allocation."] # [doc = ""] # [doc = " This will always be `usize::MAX` if `T` is zero-sized."] # [inline (always)] pub fn cap (& self) -> usize { if mem :: size_of :: < T > () == 0 { ! 0 } else { self . cap } } # [doc = " Returns a shared reference to the allocator backing this RawVec."] pub fn bump (& self) -> & 'a Bump { self . a } fn current_layout (& self) -> Option < Layout > { if self . cap == 0 { None } else { unsafe { let align = mem :: align_of :: < T > () ; let size = mem :: size_of :: < T > () * self . cap ; Some (Layout :: from_size_align_unchecked (size , align)) } } } # [doc = " Doubles the size of the type's backing allocation. This is common enough"] # [doc = " to want to do that it's easiest to just have a dedicated method. Slightly"] # [doc = " more efficient logic can be provided for this than the general case."] # [doc = ""] # [doc = " This function is ideal for when pushing elements one-at-a-time because"] # [doc = " you don't need to incur the costs of the more general computations"] # [doc = " reserve needs to do to guard against overflow. You do however need to"] # [doc = " manually check if your `len == cap`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " * Panics if T is zero-sized on the assumption that you managed to exhaust"] # [doc = "   all `usize::MAX` slots in your imaginary buffer."] # [doc = " * Panics on 32-bit platforms if the requested capacity exceeds"] # [doc = "   `isize::MAX` bytes."] # [doc = ""] # [doc = " # Aborts"] # [doc = ""] # [doc = " Aborts on OOM"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```ignore"] # [doc = " # #![feature(alloc, raw_vec_internals)]"] # [doc = " # extern crate alloc;"] # [doc = " # use std::ptr;"] # [doc = " # use alloc::raw_vec::RawVec;"] # [doc = " struct MyVec<T> {"] # [doc = "     buf: RawVec<T>,"] # [doc = "     len: usize,"] # [doc = " }"] # [doc = ""] # [doc = " impl<T> MyVec<T> {"] # [doc = "     pub fn push(&mut self, elem: T) {"] # [doc = "         if self.len == self.buf.cap() { self.buf.double(); }"] # [doc = "         // double would have aborted or panicked if the len exceeded"] # [doc = "         // `isize::MAX` so this is safe to do unchecked now."] # [doc = "         unsafe {"] # [doc = "             ptr::write(self.buf.ptr().add(self.len), elem);"] # [doc = "         }"] # [doc = "         self.len += 1;"] # [doc = "     }"] # [doc = " }"] # [doc = " # fn main() {"] # [doc = " #   let mut vec = MyVec { buf: RawVec::new(), len: 0 };"] # [doc = " #   vec.push(1);"] # [doc = " # }"] # [doc = " ```"] # [inline (never)] # [cold] pub fn double (& mut self) { unsafe { let elem_size = mem :: size_of :: < T > () ; assert ! (elem_size != 0 , "capacity overflow") ; let (new_cap , uniq) = match self . current_layout () { Some (cur) => { let new_cap = 2 * self . cap ; let new_size = new_cap * elem_size ; alloc_guard (new_size) . unwrap_or_else (| _ | capacity_overflow ()) ; let ptr_res = self . a . realloc (self . ptr . cast () , cur , new_size) ; match ptr_res { Ok (ptr) => (new_cap , ptr . cast ()) , Err (_) => handle_alloc_error (Layout :: from_size_align_unchecked (new_size , cur . align () ,)) , } } None => { let new_cap = if elem_size > (! 0) / 8 { 1 } else { 4 } ; match self . a . alloc_array :: < T > (new_cap) { Ok (ptr) => (new_cap , ptr) , Err (_) => handle_alloc_error (Layout :: array :: < T > (new_cap) . unwrap ()) , } } } ; self . ptr = uniq ; self . cap = new_cap ; } } # [doc = " Attempts to double the size of the type's backing allocation in place. This is common"] # [doc = " enough to want to do that it's easiest to just have a dedicated method. Slightly"] # [doc = " more efficient logic can be provided for this than the general case."] # [doc = ""] # [doc = " Returns true if the reallocation attempt has succeeded, or false otherwise."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " * Panics if T is zero-sized on the assumption that you managed to exhaust"] # [doc = "   all `usize::MAX` slots in your imaginary buffer."] # [doc = " * Panics on 32-bit platforms if the requested capacity exceeds"] # [doc = "   `isize::MAX` bytes."] # [inline (never)] # [cold] pub fn double_in_place (& mut self) -> bool { unsafe { let elem_size = mem :: size_of :: < T > () ; let old_layout = match self . current_layout () { Some (layout) => layout , None => return false , } ; assert ! (elem_size != 0 , "capacity overflow") ; let new_cap = 2 * self . cap ; let new_size = new_cap * elem_size ; alloc_guard (new_size) . unwrap_or_else (| _ | capacity_overflow ()) ; match self . a . grow_in_place (self . ptr . cast () , old_layout , new_size) { Ok (_) => { self . cap = new_cap ; true } Err (_) => false , } } } # [doc = " The same as `reserve_exact`, but returns on errors instead of panicking or aborting."] pub fn try_reserve_exact (& mut self , used_cap : usize , needed_extra_cap : usize ,) -> Result < () , CollectionAllocErr > { self . fallible_reserve_internal (used_cap , needed_extra_cap , Exact) } # [doc = " Ensures that the buffer contains at least enough space to hold"] # [doc = " `used_cap + needed_extra_cap` elements. If it doesn't already,"] # [doc = " will reallocate the minimum possible amount of memory necessary."] # [doc = " Generally this will be exactly the amount of memory necessary,"] # [doc = " but in principle the allocator is free to give back more than"] # [doc = " we asked for."] # [doc = ""] # [doc = " If `used_cap` exceeds `self.cap()`, this may fail to actually allocate"] # [doc = " the requested space. This is not really unsafe, but the unsafe"] # [doc = " code *you* write that relies on the behavior of this function may break."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " * Panics if the requested capacity exceeds `usize::MAX` bytes."] # [doc = " * Panics on 32-bit platforms if the requested capacity exceeds"] # [doc = "   `isize::MAX` bytes."] # [doc = ""] # [doc = " # Aborts"] # [doc = ""] # [doc = " Aborts on OOM"] pub fn reserve_exact (& mut self , used_cap : usize , needed_extra_cap : usize) { self . infallible_reserve_internal (used_cap , needed_extra_cap , Exact) } # [doc = " Calculates the buffer's new size given that it'll hold `used_cap +"] # [doc = " needed_extra_cap` elements. This logic is used in amortized reserve methods."] # [doc = " Returns `(new_capacity, new_alloc_size)`."] fn amortized_new_size (& self , used_cap : usize , needed_extra_cap : usize ,) -> Result < usize , CollectionAllocErr > { let required_cap = used_cap . checked_add (needed_extra_cap) . ok_or (CapacityOverflow) ? ; let double_cap = self . cap * 2 ; Ok (cmp :: max (double_cap , required_cap)) } # [doc = " The same as `reserve`, but returns on errors instead of panicking or aborting."] pub fn try_reserve (& mut self , used_cap : usize , needed_extra_cap : usize ,) -> Result < () , CollectionAllocErr > { self . fallible_reserve_internal (used_cap , needed_extra_cap , Amortized) } # [doc = " Ensures that the buffer contains at least enough space to hold"] # [doc = " `used_cap + needed_extra_cap` elements. If it doesn't already have"] # [doc = " enough capacity, will reallocate enough space plus comfortable slack"] # [doc = " space to get amortized `O(1)` behavior. Will limit this behavior"] # [doc = " if it would needlessly cause itself to panic."] # [doc = ""] # [doc = " If `used_cap` exceeds `self.cap()`, this may fail to actually allocate"] # [doc = " the requested space. This is not really unsafe, but the unsafe"] # [doc = " code *you* write that relies on the behavior of this function may break."] # [doc = ""] # [doc = " This is ideal for implementing a bulk-push operation like `extend`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " * Panics if the requested capacity exceeds `usize::MAX` bytes."] # [doc = " * Panics on 32-bit platforms if the requested capacity exceeds"] # [doc = "   `isize::MAX` bytes."] # [doc = ""] # [doc = " # Aborts"] # [doc = ""] # [doc = " Aborts on OOM"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```ignore"] # [doc = " # #![feature(alloc, raw_vec_internals)]"] # [doc = " # extern crate alloc;"] # [doc = " # use std::ptr;"] # [doc = " # use alloc::raw_vec::RawVec;"] # [doc = " struct MyVec<T> {"] # [doc = "     buf: RawVec<T>,"] # [doc = "     len: usize,"] # [doc = " }"] # [doc = ""] # [doc = " impl<T: Clone> MyVec<T> {"] # [doc = "     pub fn push_all(&mut self, elems: &[T]) {"] # [doc = "         self.buf.reserve(self.len, elems.len());"] # [doc = "         // reserve would have aborted or panicked if the len exceeded"] # [doc = "         // `isize::MAX` so this is safe to do unchecked now."] # [doc = "         for x in elems {"] # [doc = "             unsafe {"] # [doc = "                 ptr::write(self.buf.ptr().add(self.len), x.clone());"] # [doc = "             }"] # [doc = "             self.len += 1;"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " # fn main() {"] # [doc = " #   let mut vector = MyVec { buf: RawVec::new(), len: 0 };"] # [doc = " #   vector.push_all(&[1, 3, 5, 7, 9]);"] # [doc = " # }"] # [doc = " ```"] # [inline (always)] pub fn reserve (& mut self , used_cap : usize , needed_extra_cap : usize) { self . infallible_reserve_internal (used_cap , needed_extra_cap , Amortized) } # [doc = " Attempts to ensure that the buffer contains at least enough space to hold"] # [doc = " `used_cap + needed_extra_cap` elements. If it doesn't already have"] # [doc = " enough capacity, will reallocate in place enough space plus comfortable slack"] # [doc = " space to get amortized `O(1)` behavior. Will limit this behaviour"] # [doc = " if it would needlessly cause itself to panic."] # [doc = ""] # [doc = " If `used_cap` exceeds `self.cap()`, this may fail to actually allocate"] # [doc = " the requested space. This is not really unsafe, but the unsafe"] # [doc = " code *you* write that relies on the behavior of this function may break."] # [doc = ""] # [doc = " Returns true if the reallocation attempt has succeeded, or false otherwise."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " * Panics if the requested capacity exceeds `usize::MAX` bytes."] # [doc = " * Panics on 32-bit platforms if the requested capacity exceeds"] # [doc = "   `isize::MAX` bytes."] pub fn reserve_in_place (& mut self , used_cap : usize , needed_extra_cap : usize) -> bool { unsafe { let old_layout = match self . current_layout () { Some (layout) => layout , None => return false , } ; if self . cap () . wrapping_sub (used_cap) >= needed_extra_cap { return false ; } let new_cap = self . amortized_new_size (used_cap , needed_extra_cap) . unwrap_or_else (| _ | capacity_overflow ()) ; let new_layout = Layout :: new :: < T > () . repeat (new_cap) . unwrap () . 0 ; alloc_guard (new_layout . size ()) . unwrap_or_else (| _ | capacity_overflow ()) ; match self . a . grow_in_place (self . ptr . cast () , old_layout , new_layout . size ()) { Ok (_) => { self . cap = new_cap ; true } Err (_) => false , } } } # [doc = " Shrinks the allocation down to the specified amount. If the given amount"] # [doc = " is 0, actually completely deallocates."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the given amount is *larger* than the current capacity."] # [doc = ""] # [doc = " # Aborts"] # [doc = ""] # [doc = " Aborts on OOM."] pub fn shrink_to_fit (& mut self , amount : usize) { let elem_size = mem :: size_of :: < T > () ; if elem_size == 0 { self . cap = amount ; return ; } assert ! (self . cap >= amount , "Tried to shrink to a larger capacity") ; if amount == 0 { unsafe { let a = self . a ; self . dealloc_buffer () ; ptr :: write (self , RawVec :: new_in (a)) ; } } else if self . cap != amount { unsafe { let old_size = elem_size * self . cap ; let new_size = elem_size * amount ; let align = mem :: align_of :: < T > () ; let old_layout = Layout :: from_size_align_unchecked (old_size , align) ; match self . a . realloc (self . ptr . cast () , old_layout , new_size) { Ok (p) => self . ptr = p . cast () , Err (_) => { handle_alloc_error (Layout :: from_size_align_unchecked (new_size , align)) } } } self . cap = amount ; } } }
};
}
