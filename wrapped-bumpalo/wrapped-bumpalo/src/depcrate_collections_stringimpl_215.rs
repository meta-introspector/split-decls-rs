// Generated macro for impl_215 (impl)
macro_rules! Depcrate_collections_stringimpl_215 {
() => {
// Module: crate::collections::string
// Provides: {"impl_215"}
// Dependencies: {}
# [doc = " Implements the `+` operator for concatenating two strings."] # [doc = ""] # [doc = " This consumes the `String<'bump>` on the left-hand side and re-uses its buffer (growing it if"] # [doc = " necessary). This is done to avoid allocating a new `String<'bump>` and copying the entire contents on"] # [doc = " every operation, which would lead to `O(n^2)` running time when building an `n`-byte string by"] # [doc = " repeated concatenation."] # [doc = ""] # [doc = " The string on the right-hand side is only borrowed; its contents are copied into the returned"] # [doc = " `String<'bump>`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Concatenating two `String<'bump>`s takes the first by value and borrows the second:"] # [doc = ""] # [doc = " ```"] # [doc = " use bumpalo::{Bump, collections::String};"] # [doc = ""] # [doc = " let bump = Bump::new();"] # [doc = ""] # [doc = " let a = String::from_str_in(\"hello\", &bump);"] # [doc = " let b = String::from_str_in(\" world\", &bump);"] # [doc = " let c = a + &b;"] # [doc = " // `a` is moved and can no longer be used here."] # [doc = " ```"] # [doc = ""] # [doc = " If you want to keep using the first `String`, you can clone it and append to the clone instead:"] # [doc = ""] # [doc = " ```"] # [doc = " use bumpalo::{Bump, collections::String};"] # [doc = ""] # [doc = " let bump = Bump::new();"] # [doc = ""] # [doc = " let a = String::from_str_in(\"hello\", &bump);"] # [doc = " let b = String::from_str_in(\" world\", &bump);"] # [doc = " let c = a.clone() + &b;"] # [doc = " // `a` is still valid here."] # [doc = " ```"] # [doc = ""] # [doc = " Concatenating `&str` slices can be done by converting the first to a `String`:"] # [doc = ""] # [doc = " ```"] # [doc = " use bumpalo::{Bump, collections::String};"] # [doc = ""] # [doc = " let bump = Bump::new();"] # [doc = ""] # [doc = " let a = \"hello\";"] # [doc = " let b = \" world\";"] # [doc = " let c = String::from_str_in(a, &bump) + b;"] # [doc = " ```"] impl < 'a , 'bump > Add < & 'a str > for String < 'bump > { type Output = String < 'bump > ; # [inline] fn add (mut self , other : & str) -> String < 'bump > { self . push_str (other) ; self } }
};
}
