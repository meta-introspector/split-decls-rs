// Generated macro for Alloc (trait)
macro_rules! Depcrate_allocAlloc {
() => {
// Module: crate::alloc
// Provides: {"Alloc"}
// Dependencies: {}
# [doc = " An implementation of `Alloc` can allocate, reallocate, and"] # [doc = " deallocate arbitrary blocks of data described via `Layout`."] # [doc = ""] # [doc = " Some of the methods require that a memory block be *currently"] # [doc = " allocated* via an allocator. This means that:"] # [doc = ""] # [doc = " * the starting address for that memory block was previously"] # [doc = "   returned by a previous call to an allocation method (`alloc`,"] # [doc = "   `alloc_zeroed`, `alloc_excess`, `alloc_one`, `alloc_array`) or"] # [doc = "   reallocation method (`realloc`, `realloc_excess`, or"] # [doc = "   `realloc_array`), and"] # [doc = ""] # [doc = " * the memory block has not been subsequently deallocated, where"] # [doc = "   blocks are deallocated either by being passed to a deallocation"] # [doc = "   method (`dealloc`, `dealloc_one`, `dealloc_array`) or by being"] # [doc = "   passed to a reallocation method (see above) that returns `Ok`."] # [doc = ""] # [doc = " A note regarding zero-sized types and zero-sized layouts: many"] # [doc = " methods in the `Alloc` trait state that allocation requests"] # [doc = " must be non-zero size, or else undefined behavior can result."] # [doc = ""] # [doc = " * However, some higher-level allocation methods (`alloc_one`,"] # [doc = "   `alloc_array`) are well-defined on zero-sized types and can"] # [doc = "   optionally support them: it is left up to the implementor"] # [doc = "   whether to return `Err`, or to return `Ok` with some pointer."] # [doc = ""] # [doc = " * If an `Alloc` implementation chooses to return `Ok` in this"] # [doc = "   case (i.e. the pointer denotes a zero-sized inaccessible block)"] # [doc = "   then that returned pointer must be considered \"currently"] # [doc = "   allocated\". On such an allocator, *all* methods that take"] # [doc = "   currently-allocated pointers as inputs must accept these"] # [doc = "   zero-sized pointers, *without* causing undefined behavior."] # [doc = ""] # [doc = " * In other words, if a zero-sized pointer can flow out of an"] # [doc = "   allocator, then that allocator must likewise accept that pointer"] # [doc = "   flowing back into its deallocation and reallocation methods."] # [doc = ""] # [doc = " Some of the methods require that a layout *fit* a memory block."] # [doc = " What it means for a layout to \"fit\" a memory block means (or"] # [doc = " equivalently, for a memory block to \"fit\" a layout) is that the"] # [doc = " following two conditions must hold:"] # [doc = ""] # [doc = " 1. The block's starting address must be aligned to `layout.align()`."] # [doc = ""] # [doc = " 2. The block's size must fall in the range `[use_min, use_max]`, where:"] # [doc = ""] # [doc = "    * `use_min` is `self.usable_size(layout).0`, and"] # [doc = ""] # [doc = "    * `use_max` is the capacity that was (or would have been)"] # [doc = "      returned when (if) the block was allocated via a call to"] # [doc = "      `alloc_excess` or `realloc_excess`."] # [doc = ""] # [doc = " Note that:"] # [doc = ""] # [doc = "  * the size of the layout most recently used to allocate the block"] # [doc = "    is guaranteed to be in the range `[use_min, use_max]`, and"] # [doc = ""] # [doc = "  * a lower-bound on `use_max` can be safely approximated by a call to"] # [doc = "    `usable_size`."] # [doc = ""] # [doc = "  * if a layout `k` fits a memory block (denoted by `ptr`)"] # [doc = "    currently allocated via an allocator `a`, then it is legal to"] # [doc = "    use that layout to deallocate it, i.e. `a.dealloc(ptr, k);`."] # [doc = ""] # [doc = " # Unsafety"] # [doc = ""] # [doc = " The `Alloc` trait is an `unsafe` trait for a number of reasons, and"] # [doc = " implementors must ensure that they adhere to these contracts:"] # [doc = ""] # [doc = " * Pointers returned from allocation functions must point to valid memory and"] # [doc = "   retain their validity until at least the instance of `Alloc` is dropped"] # [doc = "   itself."] # [doc = ""] # [doc = " * `Layout` queries and calculations in general must be correct. Callers of"] # [doc = "   this trait are allowed to rely on the contracts defined on each method,"] # [doc = "   and implementors must ensure such contracts remain true."] # [doc = ""] # [doc = " Note that this list may get tweaked over time as clarifications are made in"] # [doc = " the future."] pub unsafe trait Alloc { # [doc = " Returns a pointer meeting the size and alignment guarantees of"] # [doc = " `layout`."] # [doc = ""] # [doc = " If this method returns an `Ok(addr)`, then the `addr` returned"] # [doc = " will be non-null address pointing to a block of storage"] # [doc = " suitable for holding an instance of `layout`."] # [doc = ""] # [doc = " The returned block of storage may or may not have its contents"] # [doc = " initialized. (Extension subtraits might restrict this"] # [doc = " behavior, e.g. to ensure initialization to particular sets of"] # [doc = " bit patterns.)"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe because undefined behavior can result"] # [doc = " if the caller does not ensure that `layout` has non-zero size."] # [doc = ""] # [doc = " (Extension subtraits might provide more specific bounds on"] # [doc = " behavior, e.g. guarantee a sentinel address or a null pointer"] # [doc = " in response to a zero-size allocation request.)"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returning `Err` indicates that either memory is exhausted or"] # [doc = " `layout` does not meet allocator's size or alignment"] # [doc = " constraints."] # [doc = ""] # [doc = " Implementations are encouraged to return `Err` on memory"] # [doc = " exhaustion rather than panicking or aborting, but this is not"] # [doc = " a strict requirement. (Specifically: it is *legal* to"] # [doc = " implement this trait atop an underlying native allocation"] # [doc = " library that aborts on memory exhaustion.)"] # [doc = ""] # [doc = " Clients wishing to abort computation in response to an"] # [doc = " allocation error are encouraged to call the [`handle_alloc_error`] function,"] # [doc = " rather than directly invoking `panic!` or similar."] # [doc = ""] # [doc = " [`handle_alloc_error`]: ../../alloc/alloc/fn.handle_alloc_error.html"] unsafe fn alloc (& mut self , layout : Layout) -> Result < NonNull < u8 > , AllocErr > ; # [doc = " Deallocate the memory referenced by `ptr`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe because undefined behavior can result"] # [doc = " if the caller does not ensure all of the following:"] # [doc = ""] # [doc = " * `ptr` must denote a block of memory currently allocated via"] # [doc = "   this allocator,"] # [doc = ""] # [doc = " * `layout` must *fit* that block of memory,"] # [doc = ""] # [doc = " * In addition to fitting the block of memory `layout`, the"] # [doc = "   alignment of the `layout` must match the alignment used"] # [doc = "   to allocate that block of memory."] unsafe fn dealloc (& mut self , ptr : NonNull < u8 > , layout : Layout) ; # [doc = " Returns bounds on the guaranteed usable size of a successful"] # [doc = " allocation created with the specified `layout`."] # [doc = ""] # [doc = " In particular, if one has a memory block allocated via a given"] # [doc = " allocator `a` and layout `k` where `a.usable_size(k)` returns"] # [doc = " `(l, u)`, then one can pass that block to `a.dealloc()` with a"] # [doc = " layout in the size range [l, u]."] # [doc = ""] # [doc = " (All implementors of `usable_size` must ensure that"] # [doc = " `l <= k.size() <= u`)"] # [doc = ""] # [doc = " Both the lower- and upper-bounds (`l` and `u` respectively)"] # [doc = " are provided, because an allocator based on size classes could"] # [doc = " misbehave if one attempts to deallocate a block without"] # [doc = " providing a correct value for its size (i.e., one within the"] # [doc = " range `[l, u]`)."] # [doc = ""] # [doc = " Clients who wish to make use of excess capacity are encouraged"] # [doc = " to use the `alloc_excess` and `realloc_excess` instead, as"] # [doc = " this method is constrained to report conservative values that"] # [doc = " serve as valid bounds for *all possible* allocation method"] # [doc = " calls."] # [doc = ""] # [doc = " However, for clients that do not wish to track the capacity"] # [doc = " returned by `alloc_excess` locally, this method is likely to"] # [doc = " produce useful results."] # [inline] fn usable_size (& self , layout : & Layout) -> (usize , usize) { (layout . size () , layout . size ()) } # [doc = " Returns a pointer suitable for holding data described by"] # [doc = " a new layout with `layout`’s alignment and a size given"] # [doc = " by `new_size`. To"] # [doc = " accomplish this, this may extend or shrink the allocation"] # [doc = " referenced by `ptr` to fit the new layout."] # [doc = ""] # [doc = " If this returns `Ok`, then ownership of the memory block"] # [doc = " referenced by `ptr` has been transferred to this"] # [doc = " allocator. The memory may or may not have been freed, and"] # [doc = " should be considered unusable (unless of course it was"] # [doc = " transferred back to the caller again via the return value of"] # [doc = " this method)."] # [doc = ""] # [doc = " If this method returns `Err`, then ownership of the memory"] # [doc = " block has not been transferred to this allocator, and the"] # [doc = " contents of the memory block are unaltered."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe because undefined behavior can result"] # [doc = " if the caller does not ensure all of the following:"] # [doc = ""] # [doc = " * `ptr` must be currently allocated via this allocator,"] # [doc = ""] # [doc = " * `layout` must *fit* the `ptr` (see above). (The `new_size`"] # [doc = "   argument need not fit it.)"] # [doc = ""] # [doc = " * `new_size` must be greater than zero."] # [doc = ""] # [doc = " * `new_size`, when rounded up to the nearest multiple of `layout.align()`,"] # [doc = "   must not overflow (i.e. the rounded value must be less than `usize::MAX`)."] # [doc = ""] # [doc = " (Extension subtraits might provide more specific bounds on"] # [doc = " behavior, e.g. guarantee a sentinel address or a null pointer"] # [doc = " in response to a zero-size allocation request.)"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `Err` only if the new layout"] # [doc = " does not meet the allocator's size"] # [doc = " and alignment constraints of the allocator, or if reallocation"] # [doc = " otherwise fails."] # [doc = ""] # [doc = " Implementations are encouraged to return `Err` on memory"] # [doc = " exhaustion rather than panicking or aborting, but this is not"] # [doc = " a strict requirement. (Specifically: it is *legal* to"] # [doc = " implement this trait atop an underlying native allocation"] # [doc = " library that aborts on memory exhaustion.)"] # [doc = ""] # [doc = " Clients wishing to abort computation in response to a"] # [doc = " reallocation error are encouraged to call the [`handle_alloc_error`] function,"] # [doc = " rather than directly invoking `panic!` or similar."] # [doc = ""] # [doc = " [`handle_alloc_error`]: ../../alloc/alloc/fn.handle_alloc_error.html"] unsafe fn realloc (& mut self , ptr : NonNull < u8 > , layout : Layout , new_size : usize ,) -> Result < NonNull < u8 > , AllocErr > { let old_size = layout . size () ; if new_size >= old_size { if let Ok (()) = self . grow_in_place (ptr , layout , new_size) { return Ok (ptr) ; } } else if new_size < old_size { if let Ok (()) = self . shrink_in_place (ptr , layout , new_size) { return Ok (ptr) ; } } let new_layout = Layout :: from_size_align_unchecked (new_size , layout . align ()) ; let result = self . alloc (new_layout) ; if let Ok (new_ptr) = result { ptr :: copy_nonoverlapping (ptr . as_ptr () , new_ptr . as_ptr () , cmp :: min (old_size , new_size)) ; self . dealloc (ptr , layout) ; } result } # [doc = " Behaves like `alloc`, but also ensures that the contents"] # [doc = " are set to zero before being returned."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe for the same reasons that `alloc` is."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returning `Err` indicates that either memory is exhausted or"] # [doc = " `layout` does not meet allocator's size or alignment"] # [doc = " constraints, just as in `alloc`."] # [doc = ""] # [doc = " Clients wishing to abort computation in response to an"] # [doc = " allocation error are encouraged to call the [`handle_alloc_error`] function,"] # [doc = " rather than directly invoking `panic!` or similar."] # [doc = ""] # [doc = " [`handle_alloc_error`]: ../../alloc/alloc/fn.handle_alloc_error.html"] unsafe fn alloc_zeroed (& mut self , layout : Layout) -> Result < NonNull < u8 > , AllocErr > { let size = layout . size () ; let p = self . alloc (layout) ; if let Ok (p) = p { ptr :: write_bytes (p . as_ptr () , 0 , size) ; } p } # [doc = " Behaves like `alloc`, but also returns the whole size of"] # [doc = " the returned block. For some `layout` inputs, like arrays, this"] # [doc = " may include extra storage usable for additional data."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe for the same reasons that `alloc` is."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returning `Err` indicates that either memory is exhausted or"] # [doc = " `layout` does not meet allocator's size or alignment"] # [doc = " constraints, just as in `alloc`."] # [doc = ""] # [doc = " Clients wishing to abort computation in response to an"] # [doc = " allocation error are encouraged to call the [`handle_alloc_error`] function,"] # [doc = " rather than directly invoking `panic!` or similar."] # [doc = ""] # [doc = " [`handle_alloc_error`]: ../../alloc/alloc/fn.handle_alloc_error.html"] unsafe fn alloc_excess (& mut self , layout : Layout) -> Result < Excess , AllocErr > { let usable_size = self . usable_size (& layout) ; self . alloc (layout) . map (| p | Excess (p , usable_size . 1)) } # [doc = " Behaves like `realloc`, but also returns the whole size of"] # [doc = " the returned block. For some `layout` inputs, like arrays, this"] # [doc = " may include extra storage usable for additional data."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe for the same reasons that `realloc` is."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returning `Err` indicates that either memory is exhausted or"] # [doc = " `layout` does not meet allocator's size or alignment"] # [doc = " constraints, just as in `realloc`."] # [doc = ""] # [doc = " Clients wishing to abort computation in response to a"] # [doc = " reallocation error are encouraged to call the [`handle_alloc_error`] function,"] # [doc = " rather than directly invoking `panic!` or similar."] # [doc = ""] # [doc = " [`handle_alloc_error`]: ../../alloc/alloc/fn.handle_alloc_error.html"] unsafe fn realloc_excess (& mut self , ptr : NonNull < u8 > , layout : Layout , new_size : usize ,) -> Result < Excess , AllocErr > { let new_layout = Layout :: from_size_align_unchecked (new_size , layout . align ()) ; let usable_size = self . usable_size (& new_layout) ; self . realloc (ptr , layout , new_size) . map (| p | Excess (p , usable_size . 1)) } # [doc = " Attempts to extend the allocation referenced by `ptr` to fit `new_size`."] # [doc = ""] # [doc = " If this returns `Ok`, then the allocator has asserted that the"] # [doc = " memory block referenced by `ptr` now fits `new_size`, and thus can"] # [doc = " be used to carry data of a layout of that size and same alignment as"] # [doc = " `layout`. (The allocator is allowed to"] # [doc = " expend effort to accomplish this, such as extending the memory block to"] # [doc = " include successor blocks, or virtual memory tricks.)"] # [doc = ""] # [doc = " Regardless of what this method returns, ownership of the"] # [doc = " memory block referenced by `ptr` has not been transferred, and"] # [doc = " the contents of the memory block are unaltered."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe because undefined behavior can result"] # [doc = " if the caller does not ensure all of the following:"] # [doc = ""] # [doc = " * `ptr` must be currently allocated via this allocator,"] # [doc = ""] # [doc = " * `layout` must *fit* the `ptr` (see above); note the"] # [doc = "   `new_size` argument need not fit it,"] # [doc = ""] # [doc = " * `new_size` must not be less than `layout.size()`,"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `Err(CannotReallocInPlace)` when the allocator is"] # [doc = " unable to assert that the memory block referenced by `ptr`"] # [doc = " could fit `layout`."] # [doc = ""] # [doc = " Note that one cannot pass `CannotReallocInPlace` to the `handle_alloc_error`"] # [doc = " function; clients are expected either to be able to recover from"] # [doc = " `grow_in_place` failures without aborting, or to fall back on"] # [doc = " another reallocation method before resorting to an abort."] unsafe fn grow_in_place (& mut self , ptr : NonNull < u8 > , layout : Layout , new_size : usize ,) -> Result < () , CannotReallocInPlace > { let _ = ptr ; debug_assert ! (new_size >= layout . size ()) ; let (_l , u) = self . usable_size (& layout) ; if new_size <= u { Ok (()) } else { Err (CannotReallocInPlace) } } # [doc = " Attempts to shrink the allocation referenced by `ptr` to fit `new_size`."] # [doc = ""] # [doc = " If this returns `Ok`, then the allocator has asserted that the"] # [doc = " memory block referenced by `ptr` now fits `new_size`, and"] # [doc = " thus can only be used to carry data of that smaller"] # [doc = " layout. (The allocator is allowed to take advantage of this,"] # [doc = " carving off portions of the block for reuse elsewhere.) The"] # [doc = " truncated contents of the block within the smaller layout are"] # [doc = " unaltered, and ownership of block has not been transferred."] # [doc = ""] # [doc = " If this returns `Err`, then the memory block is considered to"] # [doc = " still represent the original (larger) `layout`. None of the"] # [doc = " block has been carved off for reuse elsewhere, ownership of"] # [doc = " the memory block has not been transferred, and the contents of"] # [doc = " the memory block are unaltered."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe because undefined behavior can result"] # [doc = " if the caller does not ensure all of the following:"] # [doc = ""] # [doc = " * `ptr` must be currently allocated via this allocator,"] # [doc = ""] # [doc = " * `layout` must *fit* the `ptr` (see above); note the"] # [doc = "   `new_size` argument need not fit it,"] # [doc = ""] # [doc = " * `new_size` must not be greater than `layout.size()`"] # [doc = "   (and must be greater than zero),"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `Err(CannotReallocInPlace)` when the allocator is"] # [doc = " unable to assert that the memory block referenced by `ptr`"] # [doc = " could fit `layout`."] # [doc = ""] # [doc = " Note that one cannot pass `CannotReallocInPlace` to the `handle_alloc_error`"] # [doc = " function; clients are expected either to be able to recover from"] # [doc = " `shrink_in_place` failures without aborting, or to fall back"] # [doc = " on another reallocation method before resorting to an abort."] unsafe fn shrink_in_place (& mut self , ptr : NonNull < u8 > , layout : Layout , new_size : usize ,) -> Result < () , CannotReallocInPlace > { let _ = ptr ; debug_assert ! (new_size <= layout . size ()) ; let (l , _u) = self . usable_size (& layout) ; if l <= new_size { Ok (()) } else { Err (CannotReallocInPlace) } } # [doc = " Allocates a block suitable for holding an instance of `T`."] # [doc = ""] # [doc = " Captures a common usage pattern for allocators."] # [doc = ""] # [doc = " The returned block is suitable for passing to the"] # [doc = " `alloc`/`realloc` methods of this allocator."] # [doc = ""] # [doc = " Note to implementors: If this returns `Ok(ptr)`, then `ptr`"] # [doc = " must be considered \"currently allocated\" and must be"] # [doc = " acceptable input to methods such as `realloc` or `dealloc`,"] # [doc = " *even if* `T` is a zero-sized type. In other words, if your"] # [doc = " `Alloc` implementation overrides this method in a manner"] # [doc = " that can return a zero-sized `ptr`, then all reallocation and"] # [doc = " deallocation methods need to be similarly overridden to accept"] # [doc = " such values as input."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returning `Err` indicates that either memory is exhausted or"] # [doc = " `T` does not meet allocator's size or alignment constraints."] # [doc = ""] # [doc = " For zero-sized `T`, may return either of `Ok` or `Err`, but"] # [doc = " will *not* yield undefined behavior."] # [doc = ""] # [doc = " Clients wishing to abort computation in response to an"] # [doc = " allocation error are encouraged to call the [`handle_alloc_error`] function,"] # [doc = " rather than directly invoking `panic!` or similar."] # [doc = ""] # [doc = " [`handle_alloc_error`]: ../../alloc/alloc/fn.handle_alloc_error.html"] fn alloc_one < T > (& mut self) -> Result < NonNull < T > , AllocErr > where Self : Sized , { let k = Layout :: new :: < T > () ; if k . size () > 0 { unsafe { self . alloc (k) . map (| p | p . cast ()) } } else { Err (AllocErr) } } # [doc = " Deallocates a block suitable for holding an instance of `T`."] # [doc = ""] # [doc = " The given block must have been produced by this allocator,"] # [doc = " and must be suitable for storing a `T` (in terms of alignment"] # [doc = " as well as minimum and maximum size); otherwise yields"] # [doc = " undefined behavior."] # [doc = ""] # [doc = " Captures a common usage pattern for allocators."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe because undefined behavior can result"] # [doc = " if the caller does not ensure both:"] # [doc = ""] # [doc = " * `ptr` must denote a block of memory currently allocated via this allocator"] # [doc = ""] # [doc = " * the layout of `T` must *fit* that block of memory."] unsafe fn dealloc_one < T > (& mut self , ptr : NonNull < T >) where Self : Sized , { let k = Layout :: new :: < T > () ; if k . size () > 0 { self . dealloc (ptr . cast () , k) ; } } # [doc = " Allocates a block suitable for holding `n` instances of `T`."] # [doc = ""] # [doc = " Captures a common usage pattern for allocators."] # [doc = ""] # [doc = " The returned block is suitable for passing to the"] # [doc = " `alloc`/`realloc` methods of this allocator."] # [doc = ""] # [doc = " Note to implementors: If this returns `Ok(ptr)`, then `ptr`"] # [doc = " must be considered \"currently allocated\" and must be"] # [doc = " acceptable input to methods such as `realloc` or `dealloc`,"] # [doc = " *even if* `T` is a zero-sized type. In other words, if your"] # [doc = " `Alloc` implementation overrides this method in a manner"] # [doc = " that can return a zero-sized `ptr`, then all reallocation and"] # [doc = " deallocation methods need to be similarly overridden to accept"] # [doc = " such values as input."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returning `Err` indicates that either memory is exhausted or"] # [doc = " `[T; n]` does not meet allocator's size or alignment"] # [doc = " constraints."] # [doc = ""] # [doc = " For zero-sized `T` or `n == 0`, may return either of `Ok` or"] # [doc = " `Err`, but will *not* yield undefined behavior."] # [doc = ""] # [doc = " Always returns `Err` on arithmetic overflow."] # [doc = ""] # [doc = " Clients wishing to abort computation in response to an"] # [doc = " allocation error are encouraged to call the [`handle_alloc_error`] function,"] # [doc = " rather than directly invoking `panic!` or similar."] # [doc = ""] # [doc = " [`handle_alloc_error`]: ../../alloc/alloc/fn.handle_alloc_error.html"] fn alloc_array < T > (& mut self , n : usize) -> Result < NonNull < T > , AllocErr > where Self : Sized , { match Layout :: array :: < T > (n) { Ok (layout) if layout . size () > 0 => unsafe { self . alloc (layout) . map (| p | p . cast ()) } , _ => Err (AllocErr) , } } # [doc = " Reallocates a block previously suitable for holding `n_old`"] # [doc = " instances of `T`, returning a block suitable for holding"] # [doc = " `n_new` instances of `T`."] # [doc = ""] # [doc = " Captures a common usage pattern for allocators."] # [doc = ""] # [doc = " The returned block is suitable for passing to the"] # [doc = " `alloc`/`realloc` methods of this allocator."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe because undefined behavior can result"] # [doc = " if the caller does not ensure all of the following:"] # [doc = ""] # [doc = " * `ptr` must be currently allocated via this allocator,"] # [doc = ""] # [doc = " * the layout of `[T; n_old]` must *fit* that block of memory."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returning `Err` indicates that either memory is exhausted or"] # [doc = " `[T; n_new]` does not meet allocator's size or alignment"] # [doc = " constraints."] # [doc = ""] # [doc = " For zero-sized `T` or `n_new == 0`, may return either of `Ok` or"] # [doc = " `Err`, but will *not* yield undefined behavior."] # [doc = ""] # [doc = " Always returns `Err` on arithmetic overflow."] # [doc = ""] # [doc = " Clients wishing to abort computation in response to a"] # [doc = " reallocation error are encouraged to call the [`handle_alloc_error`] function,"] # [doc = " rather than directly invoking `panic!` or similar."] # [doc = ""] # [doc = " [`handle_alloc_error`]: ../../alloc/alloc/fn.handle_alloc_error.html"] unsafe fn realloc_array < T > (& mut self , ptr : NonNull < T > , n_old : usize , n_new : usize ,) -> Result < NonNull < T > , AllocErr > where Self : Sized , { match (Layout :: array :: < T > (n_old) , Layout :: array :: < T > (n_new)) { (Ok (ref k_old) , Ok (ref k_new)) if k_old . size () > 0 && k_new . size () > 0 => { debug_assert ! (k_old . align () == k_new . align ()) ; self . realloc (ptr . cast () , * k_old , k_new . size ()) . map (NonNull :: cast) } _ => Err (AllocErr) , } } # [doc = " Deallocates a block suitable for holding `n` instances of `T`."] # [doc = ""] # [doc = " Captures a common usage pattern for allocators."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe because undefined behavior can result"] # [doc = " if the caller does not ensure both:"] # [doc = ""] # [doc = " * `ptr` must denote a block of memory currently allocated via this allocator"] # [doc = ""] # [doc = " * the layout of `[T; n]` must *fit* that block of memory."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returning `Err` indicates that either `[T; n]` or the given"] # [doc = " memory block does not meet allocator's size or alignment"] # [doc = " constraints."] # [doc = ""] # [doc = " Always returns `Err` on arithmetic overflow."] unsafe fn dealloc_array < T > (& mut self , ptr : NonNull < T > , n : usize) -> Result < () , AllocErr > where Self : Sized , { match Layout :: array :: < T > (n) { Ok (k) if k . size () > 0 => { self . dealloc (ptr . cast () , k) ; Ok (()) } _ => Err (AllocErr) , } } }
};
}
