// Generated macro for impl_107 (impl)
macro_rules! Depcrate_collections_vecimpl_107 {
() => {
// Module: crate::collections::vec
// Provides: {"impl_107"}
// Dependencies: {}
impl < 'bump , T : 'bump > Vec < 'bump , T > { # [doc = " Creates a splicing iterator that replaces the specified range in the vector"] # [doc = " with the given `replace_with` iterator and yields the removed items."] # [doc = " `replace_with` does not need to be the same length as `range`."] # [doc = ""] # [doc = " Note 1: The element range is removed even if the iterator is not"] # [doc = " consumed until the end."] # [doc = ""] # [doc = " Note 2: It is unspecified how many elements are removed from the vector,"] # [doc = " if the `Splice` value is leaked."] # [doc = ""] # [doc = " Note 3: The input iterator `replace_with` is only consumed"] # [doc = " when the `Splice` value is dropped."] # [doc = ""] # [doc = " Note 4: This is optimal if:"] # [doc = ""] # [doc = " * The tail (elements in the vector after `range`) is empty,"] # [doc = " * or `replace_with` yields fewer elements than `range`’s length"] # [doc = " * or the lower bound of its `size_hint()` is exact."] # [doc = ""] # [doc = " Otherwise, a temporary vector is allocated and the tail is moved twice."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the starting point is greater than the end point or if"] # [doc = " the end point is greater than the length of the vector."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use bumpalo::{Bump, collections::Vec};"] # [doc = ""] # [doc = " let b = Bump::new();"] # [doc = ""] # [doc = " let mut v = bumpalo::vec![in &b; 1, 2, 3];"] # [doc = " let new = [7, 8];"] # [doc = " let u: Vec<_> = Vec::from_iter_in(v.splice(..2, new.iter().cloned()), &b);"] # [doc = " assert_eq!(v, &[7, 8, 3]);"] # [doc = " assert_eq!(u, &[1, 2]);"] # [doc = " ```"] # [inline] pub fn splice < 'a , R , I > (& 'a mut self , range : R , replace_with : I ,) -> Splice < 'a , 'bump , I :: IntoIter > where R : RangeBounds < usize > , I : IntoIterator < Item = T > , { Splice { drain : self . drain (range) , replace_with : replace_with . into_iter () , } } }
};
}
