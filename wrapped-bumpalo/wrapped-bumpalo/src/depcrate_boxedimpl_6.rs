// Generated macro for impl_6 (impl)
macro_rules! Depcrate_boxedimpl_6 {
() => {
// Module: crate::boxed
// Provides: {"impl_6"}
// Dependencies: {}
impl < 'a , T : ? Sized > Box < 'a , T > { # [doc = " Constructs a box from a raw pointer."] # [doc = ""] # [doc = " After calling this function, the raw pointer is owned by the"] # [doc = " resulting `Box`. Specifically, the `Box` destructor will call"] # [doc = " the destructor of `T` and free the allocated memory. For this"] # [doc = " to be safe, the memory must have been allocated in accordance"] # [doc = " with the memory layout used by `Box` ."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe because improper use may lead to"] # [doc = " memory problems. For example, a double-free may occur if the"] # [doc = " function is called twice on the same raw pointer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Recreate a `Box` which was previously converted to a raw pointer"] # [doc = " using [`Box::into_raw`]:"] # [doc = " ```"] # [doc = " use bumpalo::{Bump, boxed::Box};"] # [doc = ""] # [doc = " let b = Bump::new();"] # [doc = ""] # [doc = " let x = Box::new_in(5, &b);"] # [doc = " let ptr = Box::into_raw(x);"] # [doc = " let x = unsafe { Box::from_raw(ptr) }; // Note that new `x`'s lifetime is unbound. It must be bound to the `b` immutable borrow before `b` is reset."] # [doc = " ```"] # [doc = " Manually create a `Box` from scratch by using the bump allocator:"] # [doc = " ```"] # [doc = " use std::alloc::{alloc, Layout};"] # [doc = " use bumpalo::{Bump, boxed::Box};"] # [doc = ""] # [doc = " let b = Bump::new();"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     let ptr = b.alloc_layout(Layout::new::<i32>()).as_ptr() as *mut i32;"] # [doc = "     *ptr = 5;"] # [doc = "     let x = Box::from_raw(ptr); // Note that `x`'s lifetime is unbound. It must be bound to the `b` immutable borrow before `b` is reset."] # [doc = " }"] # [doc = " ```"] # [inline] pub unsafe fn from_raw (raw : * mut T) -> Self { Box (& mut * raw) } # [doc = " Consumes the `Box`, returning a wrapped raw pointer."] # [doc = ""] # [doc = " The pointer will be properly aligned and non-null."] # [doc = ""] # [doc = " After calling this function, the caller is responsible for the"] # [doc = " value previously managed by the `Box`. In particular, the"] # [doc = " caller should properly destroy `T`. The easiest way to"] # [doc = " do this is to convert the raw pointer back into a `Box` with the"] # [doc = " [`Box::from_raw`] function, allowing the `Box` destructor to perform"] # [doc = " the cleanup."] # [doc = ""] # [doc = " Note: this is an associated function, which means that you have"] # [doc = " to call it as `Box::into_raw(b)` instead of `b.into_raw()`. This"] # [doc = " is so that there is no conflict with a method on the inner type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Converting the raw pointer back into a `Box` with [`Box::from_raw`]"] # [doc = " for automatic cleanup:"] # [doc = " ```"] # [doc = " use bumpalo::{Bump, boxed::Box};"] # [doc = ""] # [doc = " let b = Bump::new();"] # [doc = ""] # [doc = " let x = Box::new_in(String::from(\"Hello\"), &b);"] # [doc = " let ptr = Box::into_raw(x);"] # [doc = " let x = unsafe { Box::from_raw(ptr) }; // Note that new `x`'s lifetime is unbound. It must be bound to the `b` immutable borrow before `b` is reset."] # [doc = " ```"] # [doc = " Manual cleanup by explicitly running the destructor:"] # [doc = " ```"] # [doc = " use std::ptr;"] # [doc = " use bumpalo::{Bump, boxed::Box};"] # [doc = ""] # [doc = " let b = Bump::new();"] # [doc = ""] # [doc = " let mut x = Box::new_in(String::from(\"Hello\"), &b);"] # [doc = " let p = Box::into_raw(x);"] # [doc = " unsafe {"] # [doc = "     ptr::drop_in_place(p);"] # [doc = " }"] # [doc = " ```"] # [inline] pub fn into_raw (b : Box < 'a , T >) -> * mut T { let mut b = ManuallyDrop :: new (b) ; b . deref_mut () . 0 as * mut T } # [doc = " Consumes and leaks the `Box`, returning a mutable reference,"] # [doc = " `&'a mut T`. Note that the type `T` must outlive the chosen lifetime"] # [doc = " `'a`. If the type has only static references, or none at all, then this"] # [doc = " may be chosen to be `'static`."] # [doc = ""] # [doc = " This function is mainly useful for data that lives for the remainder of"] # [doc = " the program's life. Dropping the returned reference will cause a memory"] # [doc = " leak. If this is not acceptable, the reference should first be wrapped"] # [doc = " with the [`Box::from_raw`] function producing a `Box`. This `Box` can"] # [doc = " then be dropped which will properly destroy `T` and release the"] # [doc = " allocated memory."] # [doc = ""] # [doc = " Note: this is an associated function, which means that you have"] # [doc = " to call it as `Box::leak(b)` instead of `b.leak()`. This"] # [doc = " is so that there is no conflict with a method on the inner type."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Simple usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use bumpalo::{Bump, boxed::Box};"] # [doc = ""] # [doc = " let b = Bump::new();"] # [doc = ""] # [doc = " let x = Box::new_in(41, &b);"] # [doc = " let reference: &mut usize = Box::leak(x);"] # [doc = " *reference += 1;"] # [doc = " assert_eq!(*reference, 42);"] # [doc = " ```"] # [doc = ""] # [doc = "```"] # [doc = " # #[cfg(feature = \"collections\")]"] # [doc = " # {"] # [doc = " use bumpalo::{Bump, boxed::Box, vec};"] # [doc = ""] # [doc = " let b = Bump::new();"] # [doc = ""] # [doc = " let x = vec![in &b; 1, 2, 3].into_boxed_slice();"] # [doc = " let reference = Box::leak(x);"] # [doc = " reference[0] = 4;"] # [doc = " assert_eq!(*reference, [4, 2, 3]);"] # [doc = " # }"] # [doc = "```"] # [inline] pub fn leak (b : Box < 'a , T >) -> & 'a mut T { unsafe { & mut * Box :: into_raw (b) } } }
};
}
