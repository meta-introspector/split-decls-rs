// Generated macro for RawVec (struct)
macro_rules! Depcrate_collections_raw_vecRawVec {
() => {
// Module: crate::collections::raw_vec
// Provides: {"RawVec"}
// Dependencies: {}
# [doc = " A low-level utility for more ergonomically allocating, reallocating, and deallocating"] # [doc = " a buffer of memory on the heap without having to worry about all the corner cases"] # [doc = " involved. This type is excellent for building your own data structures like Vec and VecDeque."] # [doc = " In particular:"] # [doc = ""] # [doc = " * Produces Unique::empty() on zero-sized types"] # [doc = " * Produces Unique::empty() on zero-length allocations"] # [doc = " * Catches all overflows in capacity computations (promotes them to \"capacity overflow\" panics)"] # [doc = " * Guards against 32-bit systems allocating more than isize::MAX bytes"] # [doc = " * Guards against overflowing your length"] # [doc = " * Aborts on OOM"] # [doc = " * Avoids freeing Unique::empty()"] # [doc = " * Contains a ptr::Unique and thus endows the user with all related benefits"] # [doc = ""] # [doc = " This type does not in anyway inspect the memory that it manages. When dropped it *will*"] # [doc = " free its memory, but it *won't* try to Drop its contents. It is up to the user of RawVec"] # [doc = " to handle the actual things *stored* inside of a RawVec."] # [doc = ""] # [doc = " Note that a RawVec always forces its capacity to be usize::MAX for zero-sized types."] # [doc = " This enables you to use capacity growing logic catch the overflows in your length"] # [doc = " that might occur with zero-sized types."] # [doc = ""] # [doc = " However this means that you need to be careful when round-tripping this type"] # [doc = " with a `Box<[T]>`: `cap()` won't yield the len. However `with_capacity`,"] # [doc = " `shrink_to_fit`, and `from_box` will actually set RawVec's private capacity"] # [doc = " field. This allows zero-sized types to not be special-cased by consumers of"] # [doc = " this type."] # [allow (missing_debug_implementations)] pub struct RawVec < 'a , T > { ptr : NonNull < T > , cap : usize , a : & 'a Bump , }
};
}
