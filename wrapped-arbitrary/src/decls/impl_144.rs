macro_rules! deps {
    () => {
        Error!();
        ArbitraryTakeRestIter!();
        ArbitraryIter!();
        Arbitrary!();
        Int!();
        Unstructured!();
        Result!();
    };
}

macro_rules! impl_144 {
    () => {
        deps!();
        impl < 'a > Unstructured < 'a > { # [doc = " Create a new `Unstructured` from the given raw data."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::Unstructured;"] # [doc = ""] # [doc = " let u = Unstructured::new(&[1, 2, 3, 4]);"] # [doc = " ```"] pub fn new (data : & 'a [u8]) -> Self { Unstructured { data } } # [doc = " Get the number of remaining bytes of underlying data that are still"] # [doc = " available."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::{Arbitrary, Unstructured};"] # [doc = ""] # [doc = " let mut u = Unstructured::new(&[1, 2, 3]);"] # [doc = ""] # [doc = " // Initially have three bytes of data."] # [doc = " assert_eq!(u.len(), 3);"] # [doc = ""] # [doc = " // Generating a `bool` consumes one byte from the underlying data, so"] # [doc = " // we are left with two bytes afterwards."] # [doc = " let _ = bool::arbitrary(&mut u);"] # [doc = " assert_eq!(u.len(), 2);"] # [doc = " ```"] # [inline] pub fn len (& self) -> usize { self . data . len () } # [doc = " Is the underlying unstructured data exhausted?"] # [doc = ""] # [doc = " `unstructured.is_empty()` is the same as `unstructured.len() == 0`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::{Arbitrary, Unstructured};"] # [doc = ""] # [doc = " let mut u = Unstructured::new(&[1, 2, 3, 4]);"] # [doc = ""] # [doc = " // Initially, we are not empty."] # [doc = " assert!(!u.is_empty());"] # [doc = ""] # [doc = " // Generating a `u32` consumes all four bytes of the underlying data, so"] # [doc = " // we become empty afterwards."] # [doc = " let _ = u32::arbitrary(&mut u);"] # [doc = " assert!(u.is_empty());"] # [doc = " ```"] # [inline] pub fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Generate an arbitrary instance of `A`."] # [doc = ""] # [doc = " This is simply a helper method that is equivalent to `<A as"] # [doc = " Arbitrary>::arbitrary(self)`. This helper is a little bit more concise,"] # [doc = " and can be used in situations where Rust's type inference will figure"] # [doc = " out what `A` should be."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # #[cfg(feature=\"derive\")] fn foo() -> arbitrary::Result<()> {"] # [doc = " use arbitrary::{Arbitrary, Unstructured};"] # [doc = ""] # [doc = " #[derive(Arbitrary)]"] # [doc = " struct MyType {"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " fn do_stuff(value: MyType) {"] # [doc = " #   let _ = value;"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " let mut u = Unstructured::new(&[1, 2, 3, 4]);"] # [doc = ""] # [doc = " // Rust's type inference can figure out that `value` should be of type"] # [doc = " // `MyType` here:"] # [doc = " let value = u.arbitrary()?;"] # [doc = " do_stuff(value);"] # [doc = " # Ok(()) }"] # [doc = " ```"] pub fn arbitrary < A > (& mut self) -> Result < A > where A : Arbitrary < 'a > , { < A as Arbitrary < 'a > > :: arbitrary (self) } # [doc = " Get the number of elements to insert when building up a collection of"] # [doc = " arbitrary `ElementType`s."] # [doc = ""] # [doc = " This uses the [`<ElementType as"] # [doc = " Arbitrary>::size_hint`][crate::Arbitrary::size_hint] method to smartly"] # [doc = " choose a length such that we most likely have enough underlying bytes to"] # [doc = " construct that many arbitrary `ElementType`s."] # [doc = ""] # [doc = " This should only be called within an `Arbitrary` implementation."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::{Arbitrary, Result, Unstructured};"] # [doc = " # pub struct MyCollection<T> { _t: std::marker::PhantomData<T> }"] # [doc = " # impl<T> MyCollection<T> {"] # [doc = " #     pub fn with_capacity(capacity: usize) -> Self { MyCollection { _t: std::marker::PhantomData } }"] # [doc = " #     pub fn insert(&mut self, element: T) {}"] # [doc = " # }"] # [doc = ""] # [doc = " impl<'a, T> Arbitrary<'a> for MyCollection<T>"] # [doc = " where"] # [doc = "     T: Arbitrary<'a>,"] # [doc = " {"] # [doc = "     fn arbitrary(u: &mut Unstructured<'a>) -> Result<Self> {"] # [doc = "         // Get the number of `T`s we should insert into our collection."] # [doc = "         let len = u.arbitrary_len::<T>()?;"] # [doc = ""] # [doc = "         // And then create a collection of that length!"] # [doc = "         let mut my_collection = MyCollection::with_capacity(len);"] # [doc = "         for _ in 0..len {"] # [doc = "             let element = T::arbitrary(u)?;"] # [doc = "             my_collection.insert(element);"] # [doc = "         }"] # [doc = ""] # [doc = "         Ok(my_collection)"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn arbitrary_len < ElementType > (& mut self) -> Result < usize > where ElementType : Arbitrary < 'a > , { let byte_size = self . arbitrary_byte_size () ? ; let (lower , upper) = < ElementType as Arbitrary > :: size_hint (0) ; let elem_size = upper . unwrap_or (lower * 2) ; let elem_size = std :: cmp :: max (1 , elem_size) ; Ok (byte_size / elem_size) } fn arbitrary_byte_size (& mut self) -> Result < usize > { if self . data . is_empty () { Ok (0) } else if self . data . len () == 1 { self . data = & [] ; Ok (0) } else { let len = if self . data . len () as u64 <= u8 :: MAX as u64 + 1 { let bytes = 1 ; let max_size = self . data . len () - bytes ; let (rest , for_size) = self . data . split_at (max_size) ; self . data = rest ; Self :: int_in_range_impl (0 ..= max_size as u8 , for_size . iter () . copied ()) ? . 0 as usize } else if self . data . len () as u64 <= u16 :: MAX as u64 + 2 { let bytes = 2 ; let max_size = self . data . len () - bytes ; let (rest , for_size) = self . data . split_at (max_size) ; self . data = rest ; Self :: int_in_range_impl (0 ..= max_size as u16 , for_size . iter () . copied ()) ? . 0 as usize } else if self . data . len () as u64 <= u32 :: MAX as u64 + 4 { let bytes = 4 ; let max_size = self . data . len () - bytes ; let (rest , for_size) = self . data . split_at (max_size) ; self . data = rest ; Self :: int_in_range_impl (0 ..= max_size as u32 , for_size . iter () . copied ()) ? . 0 as usize } else { let bytes = 8 ; let max_size = self . data . len () - bytes ; let (rest , for_size) = self . data . split_at (max_size) ; self . data = rest ; Self :: int_in_range_impl (0 ..= max_size as u64 , for_size . iter () . copied ()) ? . 0 as usize } ; Ok (len) } } # [doc = " Generate an integer within the given range."] # [doc = ""] # [doc = " Do not use this to generate the size of a collection. Use"] # [doc = " `arbitrary_len` instead."] # [doc = ""] # [doc = " The probability distribution of the return value is not necessarily uniform."] # [doc = ""] # [doc = " Returns `range.start()`, not an error,"] # [doc = " if this `Unstructured` [is empty][Unstructured::is_empty]."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `range.start > range.end`. That is, the given range must be"] # [doc = " non-empty."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # fn foo() -> arbitrary::Result<()> {"] # [doc = " use arbitrary::{Arbitrary, Unstructured};"] # [doc = ""] # [doc = " let mut u = Unstructured::new(&[1, 2, 3, 4]);"] # [doc = ""] # [doc = " let x: i32 = u.int_in_range(-5_000..=-1_000)?;"] # [doc = ""] # [doc = " assert!(-5_000 <= x);"] # [doc = " assert!(x <= -1_000);"] # [doc = " # Ok(()) }"] # [doc = " ```"] pub fn int_in_range < T > (& mut self , range : ops :: RangeInclusive < T >) -> Result < T > where T : Int , { let (result , bytes_consumed) = Self :: int_in_range_impl (range , self . data . iter () . cloned ()) ? ; self . data = & self . data [bytes_consumed ..] ; Ok (result) } fn int_in_range_impl < T > (range : ops :: RangeInclusive < T > , mut bytes : impl Iterator < Item = u8 > ,) -> Result < (T , usize) > where T : Int , { let start = * range . start () ; let end = * range . end () ; assert ! (start <= end , "`arbitrary::Unstructured::int_in_range` requires a non-empty range") ; if start == end { return Ok ((start , 0)) ; } let start = start . to_unsigned () ; let end = end . to_unsigned () ; let delta = end . wrapping_sub (start) ; debug_assert_ne ! (delta , T :: Unsigned :: ZERO) ; let mut arbitrary_int = T :: Unsigned :: ZERO ; let mut bytes_consumed : usize = 0 ; while (bytes_consumed < mem :: size_of :: < T > ()) && (delta >> T :: Unsigned :: from_usize (bytes_consumed * 8)) > T :: Unsigned :: ZERO { let byte = match bytes . next () { None => break , Some (b) => b , } ; bytes_consumed += 1 ; arbitrary_int = if mem :: size_of :: < T > () == 1 { T :: Unsigned :: from_u8 (byte) } else { (arbitrary_int << 8) | T :: Unsigned :: from_u8 (byte) } ; } let offset = if delta == T :: Unsigned :: MAX { arbitrary_int } else { arbitrary_int % (delta . checked_add (T :: Unsigned :: ONE) . unwrap ()) } ; let result = start . wrapping_add (offset) ; let result = T :: from_unsigned (result) ; debug_assert ! (* range . start () <= result) ; debug_assert ! (result <= * range . end ()) ; Ok ((result , bytes_consumed)) } # [doc = " Choose one of the given choices."] # [doc = ""] # [doc = " This should only be used inside of `Arbitrary` implementations."] # [doc = ""] # [doc = " The probability distribution of choices is not necessarily uniform."] # [doc = ""] # [doc = " Returns the first choice, not an error,"] # [doc = " if this `Unstructured` [is empty][Unstructured::is_empty]."] # [doc = ""] # [doc = " Returns an error if no choices are provided."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Selecting from an array of choices:"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::Unstructured;"] # [doc = ""] # [doc = " let mut u = Unstructured::new(&[1, 2, 3, 4, 5, 6, 7, 8, 9, 0]);"] # [doc = " let choices = ['a', 'b', 'c', 'd', 'e', 'f', 'g'];"] # [doc = ""] # [doc = " let choice = u.choose(&choices).unwrap();"] # [doc = ""] # [doc = " println!(\"chose {}\", choice);"] # [doc = " ```"] # [doc = ""] # [doc = " An error is returned if no choices are provided:"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::Unstructured;"] # [doc = ""] # [doc = " let mut u = Unstructured::new(&[1, 2, 3, 4, 5, 6, 7, 8, 9, 0]);"] # [doc = " let choices: [char; 0] = [];"] # [doc = ""] # [doc = " let result = u.choose(&choices);"] # [doc = ""] # [doc = " assert!(result.is_err());"] # [doc = " ```"] pub fn choose < 'b , T > (& mut self , choices : & 'b [T]) -> Result < & 'b T > { let idx = self . choose_index (choices . len ()) ? ; Ok (& choices [idx]) } # [doc = " Choose one of the given iterator choices."] # [doc = ""] # [doc = " This should only be used inside of `Arbitrary` implementations."] # [doc = ""] # [doc = " The probability distribution of choices is not necessarily uniform."] # [doc = ""] # [doc = " Returns the first choice, not an error,"] # [doc = " if this `Unstructured` [is empty][Unstructured::is_empty]."] # [doc = ""] # [doc = " Returns an error if no choices are provided."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Selecting a random item from a set:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::collections::BTreeSet;"] # [doc = " use arbitrary::Unstructured;"] # [doc = ""] # [doc = " let mut u = Unstructured::new(&[1, 2, 3, 4, 5, 6, 7, 8, 9, 0]);"] # [doc = " let set = BTreeSet::from(['a', 'b', 'c']);"] # [doc = ""] # [doc = " let choice = u.choose_iter(set.iter()).unwrap();"] # [doc = ""] # [doc = " println!(\"chose {}\", choice);"] # [doc = " ```"] pub fn choose_iter < T , I > (& mut self , choices : I) -> Result < T > where I : IntoIterator < Item = T > , I :: IntoIter : ExactSizeIterator , { let mut choices = choices . into_iter () ; let idx = self . choose_index (choices . len ()) ? ; let choice = choices . nth (idx) . expect ("ExactSizeIterator should have correct len") ; Ok (choice) } # [doc = " Choose a value in `0..len`."] # [doc = ""] # [doc = " The probability distribution of return values is not necessarily uniform."] # [doc = ""] # [doc = " Returns zero, not an error, if this `Unstructured` [is empty][Unstructured::is_empty]."] # [doc = ""] # [doc = " Returns an error if the `len` is zero."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Using Fisher–Yates shuffle shuffle to generate an arbitrary permutation."] # [doc = ""] # [doc = " [Fisher–Yates shuffle]: https://en.wikipedia.org/wiki/Fisher–Yates_shuffle"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::Unstructured;"] # [doc = ""] # [doc = " let mut u = Unstructured::new(&[1, 2, 3, 4, 5, 6, 7, 8, 9, 0]);"] # [doc = " let mut permutation = ['a', 'b', 'c', 'd', 'e', 'f', 'g'];"] # [doc = " let mut to_permute = &mut permutation[..];"] # [doc = " while to_permute.len() > 1 {"] # [doc = "     let idx = u.choose_index(to_permute.len()).unwrap();"] # [doc = "     to_permute.swap(0, idx);"] # [doc = "     to_permute = &mut to_permute[1..];"] # [doc = " }"] # [doc = ""] # [doc = " println!(\"permutation: {:?}\", permutation);"] # [doc = " ```"] # [doc = ""] # [doc = " An error is returned if the length is zero:"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::Unstructured;"] # [doc = ""] # [doc = " let mut u = Unstructured::new(&[1, 2, 3, 4, 5, 6, 7, 8, 9, 0]);"] # [doc = " let array: [i32; 0] = [];"] # [doc = ""] # [doc = " let result = u.choose_index(array.len());"] # [doc = ""] # [doc = " assert!(result.is_err());"] # [doc = " ```"] pub fn choose_index (& mut self , len : usize) -> Result < usize > { if len == 0 { return Err (Error :: EmptyChoose) ; } let idx = self . int_in_range (0 ..= len - 1) ? ; Ok (idx) } # [doc = " Generate a boolean which is true with probability approximately the given ratio."] # [doc = ""] # [doc = " Returns true, not an error, if this `Unstructured` [is empty][Unstructured::is_empty]."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics when the numerator and denominator do not meet these constraints:"] # [doc = ""] # [doc = " * `0 < numerator <= denominator`"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Generate a boolean that is `true` five sevenths of the time:"] # [doc = ""] # [doc = " ```"] # [doc = " # fn foo() -> arbitrary::Result<()> {"] # [doc = " use arbitrary::Unstructured;"] # [doc = ""] # [doc = " # let my_data = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0];"] # [doc = " let mut u = Unstructured::new(&my_data);"] # [doc = ""] # [doc = " if u.ratio(5, 7)? {"] # [doc = "     // Take this branch approximately 5/7 of the time."] # [doc = " }"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] pub fn ratio < T > (& mut self , numerator : T , denominator : T) -> Result < bool > where T : Int , { assert ! (T :: ZERO < numerator) ; assert ! (numerator <= denominator) ; let x = self . int_in_range (T :: ONE ..= denominator) ? ; Ok (x <= numerator) } # [doc = " Fill a `buffer` with bytes from the underlying raw data."] # [doc = ""] # [doc = " This should only be called within an `Arbitrary` implementation. This is"] # [doc = " a very low-level operation. You should generally prefer calling nested"] # [doc = " `Arbitrary` implementations like `<Vec<u8>>::arbitrary` and"] # [doc = " `String::arbitrary` over using this method directly."] # [doc = ""] # [doc = " If this `Unstructured` does not have enough underlying data to fill the"] # [doc = " whole `buffer`, it pads the buffer out with zeros."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::Unstructured;"] # [doc = ""] # [doc = " let mut u = Unstructured::new(&[1, 2, 3, 4]);"] # [doc = ""] # [doc = " let mut buf = [0; 2];"] # [doc = ""] # [doc = " assert!(u.fill_buffer(&mut buf).is_ok());"] # [doc = " assert_eq!(buf, [1, 2]);"] # [doc = ""] # [doc = " assert!(u.fill_buffer(&mut buf).is_ok());"] # [doc = " assert_eq!(buf, [3, 4]);"] # [doc = ""] # [doc = " assert!(u.fill_buffer(&mut buf).is_ok());"] # [doc = " assert_eq!(buf, [0, 0]);"] # [doc = " ```"] pub fn fill_buffer (& mut self , buffer : & mut [u8]) -> Result < () > { let n = std :: cmp :: min (buffer . len () , self . data . len ()) ; buffer [.. n] . copy_from_slice (& self . data [.. n]) ; for byte in buffer [n ..] . iter_mut () { * byte = 0 ; } self . data = & self . data [n ..] ; Ok (()) } # [doc = " Provide `size` bytes from the underlying raw data."] # [doc = ""] # [doc = " This should only be called within an `Arbitrary` implementation. This is"] # [doc = " a very low-level operation. You should generally prefer calling nested"] # [doc = " `Arbitrary` implementations like `<Vec<u8>>::arbitrary` and"] # [doc = " `String::arbitrary` over using this method directly."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::Unstructured;"] # [doc = ""] # [doc = " let mut u = Unstructured::new(&[1, 2, 3, 4]);"] # [doc = ""] # [doc = " assert!(u.bytes(2).unwrap() == &[1, 2]);"] # [doc = " assert!(u.bytes(2).unwrap() == &[3, 4]);"] # [doc = " ```"] pub fn bytes (& mut self , size : usize) -> Result < & 'a [u8] > { if self . data . len () < size { return Err (Error :: NotEnoughData) ; } let (for_buf , rest) = self . data . split_at (size) ; self . data = rest ; Ok (for_buf) } # [doc = " Peek at `size` number of bytes of the underlying raw input."] # [doc = ""] # [doc = " Does not consume the bytes, only peeks at them."] # [doc = ""] # [doc = " Returns `None` if there are not `size` bytes left in the underlying raw"] # [doc = " input."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::Unstructured;"] # [doc = ""] # [doc = " let u = Unstructured::new(&[1, 2, 3]);"] # [doc = ""] # [doc = " assert_eq!(u.peek_bytes(0).unwrap(), []);"] # [doc = " assert_eq!(u.peek_bytes(1).unwrap(), [1]);"] # [doc = " assert_eq!(u.peek_bytes(2).unwrap(), [1, 2]);"] # [doc = " assert_eq!(u.peek_bytes(3).unwrap(), [1, 2, 3]);"] # [doc = ""] # [doc = " assert!(u.peek_bytes(4).is_none());"] # [doc = " ```"] pub fn peek_bytes (& self , size : usize) -> Option < & 'a [u8] > { self . data . get (.. size) } # [doc = " Consume all of the rest of the remaining underlying bytes."] # [doc = ""] # [doc = " Returns a slice of all the remaining, unconsumed bytes."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::Unstructured;"] # [doc = ""] # [doc = " let mut u = Unstructured::new(&[1, 2, 3]);"] # [doc = ""] # [doc = " let mut remaining = u.take_rest();"] # [doc = ""] # [doc = " assert_eq!(remaining, [1, 2, 3]);"] # [doc = " ```"] pub fn take_rest (mut self) -> & 'a [u8] { mem :: take (& mut self . data) } # [doc = " Provide an iterator over elements for constructing a collection"] # [doc = ""] # [doc = " This is useful for implementing [`Arbitrary::arbitrary`] on collections"] # [doc = " since the implementation is simply `u.arbitrary_iter()?.collect()`"] pub fn arbitrary_iter < 'b , ElementType : Arbitrary < 'a > > (& 'b mut self ,) -> Result < ArbitraryIter < 'a , 'b , ElementType > > { Ok (ArbitraryIter { u : & mut * self , _marker : PhantomData , }) } # [doc = " Provide an iterator over elements for constructing a collection from"] # [doc = " all the remaining bytes."] # [doc = ""] # [doc = " This is useful for implementing [`Arbitrary::arbitrary_take_rest`] on collections"] # [doc = " since the implementation is simply `u.arbitrary_take_rest_iter()?.collect()`"] pub fn arbitrary_take_rest_iter < ElementType : Arbitrary < 'a > > (self ,) -> Result < ArbitraryTakeRestIter < 'a , ElementType > > { Ok (ArbitraryTakeRestIter { u : self , _marker : PhantomData , }) } # [doc = " Call the given function an arbitrary number of times."] # [doc = ""] # [doc = " The function is given this `Unstructured` so that it can continue to"] # [doc = " generate arbitrary data and structures."] # [doc = ""] # [doc = " You may optionaly specify minimum and maximum bounds on the number of"] # [doc = " times the function is called."] # [doc = ""] # [doc = " You may break out of the loop early by returning"] # [doc = " `Ok(std::ops::ControlFlow::Break)`. To continue the loop, return"] # [doc = " `Ok(std::ops::ControlFlow::Continue)`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `min > max`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Call a closure that generates an arbitrary type inside a context an"] # [doc = " arbitrary number of times:"] # [doc = ""] # [doc = " ```"] # [doc = " use arbitrary::{Result, Unstructured};"] # [doc = " use std::ops::ControlFlow;"] # [doc = ""] # [doc = " enum Type {"] # [doc = "     /// A boolean type."] # [doc = "     Bool,"] # [doc = ""] # [doc = "     /// An integer type."] # [doc = "     Int,"] # [doc = ""] # [doc = "     /// A list of the `i`th type in this type's context."] # [doc = "     List(usize),"] # [doc = " }"] # [doc = ""] # [doc = " fn arbitrary_types_context(u: &mut Unstructured) -> Result<Vec<Type>> {"] # [doc = "     let mut context = vec![];"] # [doc = ""] # [doc = "     u.arbitrary_loop(Some(10), Some(20), |u| {"] # [doc = "         let num_choices = if context.is_empty() {"] # [doc = "             2"] # [doc = "         } else {"] # [doc = "             3"] # [doc = "         };"] # [doc = "         let ty = match u.int_in_range::<u8>(1..=num_choices)? {"] # [doc = "             1 => Type::Bool,"] # [doc = "             2 => Type::Int,"] # [doc = "             3 => Type::List(u.int_in_range(0..=context.len() - 1)?),"] # [doc = "             _ => unreachable!(),"] # [doc = "         };"] # [doc = "         context.push(ty);"] # [doc = "         Ok(ControlFlow::Continue(()))"] # [doc = "     })?;"] # [doc = ""] # [doc = "     // The number of loop iterations are constrained by the min/max"] # [doc = "     // bounds that we provided."] # [doc = "     assert!(context.len() >= 10);"] # [doc = "     assert!(context.len() <= 20);"] # [doc = ""] # [doc = "     Ok(context)"] # [doc = " }"] # [doc = " ```"] pub fn arbitrary_loop (& mut self , min : Option < u32 > , max : Option < u32 > , mut f : impl FnMut (& mut Self) -> Result < ControlFlow < () , () > > ,) -> Result < () > { let min = min . unwrap_or (0) ; let max = max . unwrap_or (u32 :: MAX) ; for _ in 0 .. self . int_in_range (min ..= max) ? { match f (self) ? { ControlFlow :: Continue (_) => continue , ControlFlow :: Break (_) => break , } } Ok (()) } }
    };
}

impl_144!()