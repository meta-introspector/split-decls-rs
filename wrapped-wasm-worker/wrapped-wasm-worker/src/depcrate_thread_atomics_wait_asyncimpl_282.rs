// Generated macro for impl_282 (impl)
macro_rules! Depcrate_thread_atomics_wait_asyncimpl_282 {
() => {
// Module: crate::thread::atomics::wait_async
// Provides: {"impl_282"}
// Dependencies: {}
impl WaitAsync { # [doc = " Mimics the interface we need from [`Atomics::wait_async`]."] pub (super) fn wait (value : & AtomicI32 , check : i32) -> Self { thread_local ! { static HAS_WAIT_ASYNC : bool = ! js :: HAS_WAIT_ASYNC . with (JsValue :: is_undefined) ; } if value . load (Ordering :: Relaxed) != check { return Self (Some (State :: Ready)) ; } let index = super :: i32_to_buffer_index (value . as_ptr ()) ; if HAS_WAIT_ASYNC . with (bool :: clone) { let result : WaitAsyncResult = MEMORY_ARRAY . with (| array | Atomics :: wait_async (array , index , check)) . expect ("`Atomics.waitAsync` is not expected to fail") . unchecked_into () ; Self (Some (if result . async_ () { State :: WaitAsync (JsFuture :: from (result . value ())) } else { State :: Ready })) } else { Self :: wait_polyfill (index , check) } } # [doc = " Polyfills [`Atomics::wait_async`] if not available."] fn wait_polyfill (index : u32 , check : i32) -> Self { thread_local ! { # [doc = " Object URL to the worker script."] static URL : String = wasm_bindgen :: link_to ! (module = "/src/thread/atomics/script/wait_async.min.js") ; # [doc = " Holds cached workers."] static WORKERS : RefCell < Vec < Worker >> = const { RefCell :: new (Vec :: new ()) } ; } let worker = WORKERS . with (| workers | { if let Some (worker) = workers . borrow_mut () . pop () { return worker ; } URL . with (| url | Worker :: new (url)) . expect ("`new Worker()` is not expected to fail with a local script") }) ; let shared = Rc :: new (Shared { finished : Cell :: new (false) , waker : RefCell :: new (None) , }) ; let onmessage_callback = Closure :: once_into_js ({ let shared = Rc :: clone (& shared) ; let worker = worker . clone () ; move | | { WORKERS . with (move | workers | { let mut workers = workers . borrow_mut () ; workers . push (worker) ; workers . truncate (POLYFILL_WORKER_CACHE) ; }) ; shared . finished . set (true) ; if let Some (waker) = shared . waker . borrow_mut () . take () { waker . wake () ; } } }) ; worker . set_onmessage (Some (onmessage_callback . unchecked_ref ())) ; let message = MEMORY . with (| memory | Array :: of3 (memory , & JsValue :: from (index) , & JsValue :: from (check))) ; worker . post_message (& message) . expect ("`Worker.postMessage` is not expected to fail without a `transfer` object") ; Self (Some (State :: Polyfill (shared))) } }
};
}
