// Generated macro for StdFloat (trait)
macro_rules! DepcrateStdFloat {
() => {
// Module: crate
// Provides: {"StdFloat"}
// Dependencies: {}
# [doc = " This trait provides a possibly-temporary implementation of float functions"] # [doc = " that may, in the absence of hardware support, canonicalize to calling an"] # [doc = " operating system's `math.h` dynamically-loaded library (also known as a"] # [doc = " shared object). As these conditionally require runtime support, they"] # [doc = " should only appear in binaries built assuming OS support: `std`."] # [doc = ""] # [doc = " However, there is no reason SIMD types, in general, need OS support,"] # [doc = " as for many architectures an embedded binary may simply configure that"] # [doc = " support itself. This means these types must be visible in `core`"] # [doc = " but have these functions available in `std`."] # [doc = ""] # [doc = " [`f32`] and [`f64`] achieve a similar trick by using \"lang items\", but"] # [doc = " due to compiler limitations, it is harder to implement this approach for"] # [doc = " abstract data types like [`Simd`]. From that need, this trait is born."] # [doc = ""] # [doc = " It is possible this trait will be replaced in some manner in the future,"] # [doc = " when either the compiler or its supporting runtime functions are improved."] # [doc = " For now this trait is available to permit experimentation with SIMD float"] # [doc = " operations that may lack hardware support, such as `mul_add`."] pub trait StdFloat : Sealed + Sized { # [doc = " Elementwise fused multiply-add. Computes `(self * a) + b` with only one rounding error,"] # [doc = " yielding a more accurate result than an unfused multiply-add."] # [doc = ""] # [doc = " Using `mul_add` *may* be more performant than an unfused multiply-add if the target"] # [doc = " architecture has a dedicated `fma` CPU instruction.  However, this is not always"] # [doc = " true, and will be heavily dependent on designing algorithms with specific target"] # [doc = " hardware in mind."] # [inline] # [must_use = "method returns a new vector and does not mutate the original value"] fn mul_add (self , a : Self , b : Self) -> Self { unsafe { intrinsics :: simd_fma (self , a , b) } } # [doc = " Produces a vector where every element has the square root value"] # [doc = " of the equivalently-indexed element in `self`"] # [inline] # [must_use = "method returns a new vector and does not mutate the original value"] fn sqrt (self) -> Self { unsafe { intrinsics :: simd_fsqrt (self) } } # [doc = " Produces a vector where every element has the sine of the value"] # [doc = " in the equivalently-indexed element in `self`."] # [must_use = "method returns a new vector and does not mutate the original value"] fn sin (self) -> Self ; # [doc = " Produces a vector where every element has the cosine of the value"] # [doc = " in the equivalently-indexed element in `self`."] # [must_use = "method returns a new vector and does not mutate the original value"] fn cos (self) -> Self ; # [doc = " Produces a vector where every element has the exponential (base e) of the value"] # [doc = " in the equivalently-indexed element in `self`."] # [must_use = "method returns a new vector and does not mutate the original value"] fn exp (self) -> Self ; # [doc = " Produces a vector where every element has the exponential (base 2) of the value"] # [doc = " in the equivalently-indexed element in `self`."] # [must_use = "method returns a new vector and does not mutate the original value"] fn exp2 (self) -> Self ; # [doc = " Produces a vector where every element has the natural logarithm of the value"] # [doc = " in the equivalently-indexed element in `self`."] # [must_use = "method returns a new vector and does not mutate the original value"] fn ln (self) -> Self ; # [doc = " Produces a vector where every element has the logarithm with respect to an arbitrary"] # [doc = " in the equivalently-indexed elements in `self` and `base`."] # [inline] # [must_use = "method returns a new vector and does not mutate the original value"] fn log (self , base : Self) -> Self { unsafe { intrinsics :: simd_div (self . ln () , base . ln ()) } } # [doc = " Produces a vector where every element has the base-2 logarithm of the value"] # [doc = " in the equivalently-indexed element in `self`."] # [must_use = "method returns a new vector and does not mutate the original value"] fn log2 (self) -> Self ; # [doc = " Produces a vector where every element has the base-10 logarithm of the value"] # [doc = " in the equivalently-indexed element in `self`."] # [must_use = "method returns a new vector and does not mutate the original value"] fn log10 (self) -> Self ; # [doc = " Returns the smallest integer greater than or equal to each element."] # [must_use = "method returns a new vector and does not mutate the original value"] # [inline] fn ceil (self) -> Self { unsafe { intrinsics :: simd_ceil (self) } } # [doc = " Returns the largest integer value less than or equal to each element."] # [must_use = "method returns a new vector and does not mutate the original value"] # [inline] fn floor (self) -> Self { unsafe { intrinsics :: simd_floor (self) } } # [doc = " Rounds to the nearest integer value. Ties round toward zero."] # [must_use = "method returns a new vector and does not mutate the original value"] # [inline] fn round (self) -> Self { unsafe { intrinsics :: simd_round (self) } } # [doc = " Returns the floating point's integer value, with its fractional part removed."] # [must_use = "method returns a new vector and does not mutate the original value"] # [inline] fn trunc (self) -> Self { unsafe { intrinsics :: simd_trunc (self) } } # [doc = " Returns the floating point's fractional value, with its integer part removed."] # [must_use = "method returns a new vector and does not mutate the original value"] fn fract (self) -> Self ; }
};
}
