// Generated macro for impl_66 (impl)
macro_rules! Depcrate_componentsimpl_66 {
() => {
// Module: crate::components
// Provides: {"impl_66"}
// Dependencies: {}
impl Splitter { # [doc = " Creates a new splitter."] # [inline] # [must_use] pub (crate) fn new (scheme_end : Option < NonZeroUsize > , authority_end : Option < NonZeroUsize > , query_start : Option < NonZeroUsize > , fragment_start : Option < NonZeroUsize > ,) -> Self { Self { scheme_end , authority_end , query_start , fragment_start , } } # [doc = " Decomposes an IRI into five major components: scheme, authority, path, query, and fragment."] # [must_use] fn split_into_major (self , s : & str ,) -> (Option < & str > , Option < & str > , & str , Option < & str > , Option < & str >) { let (scheme , next_of_scheme) = match self . scheme_end { Some (end) => (Some (& s [.. end . get ()]) , end . get () + 1) , None => (None , 0) , } ; let (authority , next_of_authority) = match self . authority_end { Some (end) => (Some (& s [(next_of_scheme + 2) .. end . get ()]) , end . get ()) , None => (None , next_of_scheme) , } ; let (fragment , end_of_prev_of_fragment) = match self . fragment_start { Some (start) => (Some (& s [start . get () ..]) , start . get () - 1) , None => (None , s . len ()) , } ; let (query , end_of_path) = match self . query_start { Some (start) => (Some (& s [start . get () .. end_of_prev_of_fragment]) , start . get () - 1 ,) , None => (None , end_of_prev_of_fragment) , } ; let path = & s [next_of_authority .. end_of_path] ; (scheme , authority , path , query , fragment) } # [doc = " Returns the range for the scheme part."] # [inline] # [must_use] fn scheme_range (self) -> Option < RangeTo < usize > > { self . scheme_end . map (| end | .. end . get ()) } # [doc = " Returns the scheme as a string."] # [inline] # [must_use] pub (crate) fn scheme_str < 'a > (& self , s : & 'a str) -> Option < & 'a str > { self . scheme_range () . map (| range | & s [range]) } # [doc = " Returns true if the IRI has a scheme part, false otherwise."] # [inline] # [must_use] pub (crate) fn has_scheme (& self) -> bool { self . scheme_end . is_some () } # [doc = " Returns the range for the authority part."] # [inline] # [must_use] fn authority_range (self) -> Option < Range < usize > > { let end = self . authority_end ? . get () ; let start = self . scheme_end . map_or (2 , | v | v . get () + 3) ; Some (start .. end) } # [doc = " Returns the authority as a string."] # [inline] # [must_use] pub (crate) fn authority_str < 'a > (& self , s : & 'a str) -> Option < & 'a str > { self . authority_range () . map (| range | & s [range]) } # [doc = " Returns true if the IRI has an authority part, false otherwise."] # [inline] # [must_use] pub (crate) fn has_authority (& self) -> bool { self . authority_end . is_some () } # [doc = " Returns the range for the path part."] # [inline] # [must_use] fn path_range (self , full_len : usize) -> Range < usize > { let end = self . query_start . or (self . fragment_start) . map_or (full_len , | v | v . get () - 1) ; let start = self . authority_end . map_or_else (| | self . scheme_end . map_or (0 , | v | v . get () + 1) , NonZeroUsize :: get ,) ; start .. end } # [doc = " Returns the path as a string."] # [inline] # [must_use] pub (crate) fn path_str < 'a > (& self , s : & 'a str) -> & 'a str { & s [self . path_range (s . len ())] } # [doc = " Returns true if the path part of the IRI is empty."] # [inline] # [must_use] pub (crate) fn is_path_empty (& self , full_len : usize) -> bool { self . path_range (full_len) . is_empty () } # [doc = " Returns the range for the query part excluding a prefix `?`."] # [inline] # [must_use] fn query_range (self , full_len : usize) -> Option < Range < usize > > { let start = self . query_start ? . get () ; let end = self . fragment_start . map_or (full_len , | v | v . get () - 1) ; Some (start .. end) } # [doc = " Returns the query as a string."] # [inline] # [must_use] pub (crate) fn query_str < 'a > (& self , s : & 'a str) -> Option < & 'a str > { self . query_range (s . len ()) . map (| range | & s [range]) } # [doc = " Returns true if the IRI has a query part, false otherwise."] # [inline] # [must_use] pub (crate) fn has_query (& self) -> bool { self . query_start . is_some () } # [doc = " Returns the range for the fragment part excluding a prefix `#`."] # [inline] # [must_use] pub (crate) fn fragment_range (self) -> Option < RangeFrom < usize > > { self . fragment_start . map (| v | v . get () ..) } # [doc = " Returns the fragment as a string."] # [inline] # [must_use] pub (crate) fn fragment_str < 'a > (& self , s : & 'a str) -> Option < & 'a str > { self . fragment_range () . map (| range | & s [range]) } }
};
}
