// Generated macro for impl_222 (impl)
macro_rules! Depcrate_hir_translateimpl_222 {
() => {
// Module: crate::hir::translate
// Provides: {"impl_222"}
// Dependencies: {}
impl TranslatorBuilder { # [doc = " Create a new translator builder with a default configuration."] pub fn new () -> TranslatorBuilder { TranslatorBuilder { utf8 : true , line_terminator : b'\n' , flags : Flags :: default () , } } # [doc = " Build a translator using the current configuration."] pub fn build (& self) -> Translator { Translator { stack : RefCell :: new (vec ! []) , flags : Cell :: new (self . flags) , utf8 : self . utf8 , line_terminator : self . line_terminator , } } # [doc = " When disabled, translation will permit the construction of a regular"] # [doc = " expression that may match invalid UTF-8."] # [doc = ""] # [doc = " When enabled (the default), the translator is guaranteed to produce an"] # [doc = " expression that, for non-empty matches, will only ever produce spans"] # [doc = " that are entirely valid UTF-8 (otherwise, the translator will return an"] # [doc = " error)."] # [doc = ""] # [doc = " Perhaps surprisingly, when UTF-8 is enabled, an empty regex or even"] # [doc = " a negated ASCII word boundary (uttered as `(?-u:\\B)` in the concrete"] # [doc = " syntax) will be allowed even though they can produce matches that split"] # [doc = " a UTF-8 encoded codepoint. This only applies to zero-width or \"empty\""] # [doc = " matches, and it is expected that the regex engine itself must handle"] # [doc = " these cases if necessary (perhaps by suppressing any zero-width matches"] # [doc = " that split a codepoint)."] pub fn utf8 (& mut self , yes : bool) -> & mut TranslatorBuilder { self . utf8 = yes ; self } # [doc = " Sets the line terminator for use with `(?u-s:.)` and `(?-us:.)`."] # [doc = ""] # [doc = " Namely, instead of `.` (by default) matching everything except for `\\n`,"] # [doc = " this will cause `.` to match everything except for the byte given."] # [doc = ""] # [doc = " If `.` is used in a context where Unicode mode is enabled and this byte"] # [doc = " isn't ASCII, then an error will be returned. When Unicode mode is"] # [doc = " disabled, then any byte is permitted, but will return an error if UTF-8"] # [doc = " mode is enabled and it is a non-ASCII byte."] # [doc = ""] # [doc = " In short, any ASCII value for a line terminator is always okay. But a"] # [doc = " non-ASCII byte might result in an error depending on whether Unicode"] # [doc = " mode or UTF-8 mode are enabled."] # [doc = ""] # [doc = " Note that if `R` mode is enabled then it always takes precedence and"] # [doc = " the line terminator will be treated as `\\r` and `\\n` simultaneously."] # [doc = ""] # [doc = " Note also that this *doesn't* impact the look-around assertions"] # [doc = " `(?m:^)` and `(?m:$)`. That's usually controlled by additional"] # [doc = " configuration in the regex engine itself."] pub fn line_terminator (& mut self , byte : u8) -> & mut TranslatorBuilder { self . line_terminator = byte ; self } # [doc = " Enable or disable the case insensitive flag (`i`) by default."] pub fn case_insensitive (& mut self , yes : bool) -> & mut TranslatorBuilder { self . flags . case_insensitive = if yes { Some (true) } else { None } ; self } # [doc = " Enable or disable the multi-line matching flag (`m`) by default."] pub fn multi_line (& mut self , yes : bool) -> & mut TranslatorBuilder { self . flags . multi_line = if yes { Some (true) } else { None } ; self } # [doc = " Enable or disable the \"dot matches any character\" flag (`s`) by"] # [doc = " default."] pub fn dot_matches_new_line (& mut self , yes : bool ,) -> & mut TranslatorBuilder { self . flags . dot_matches_new_line = if yes { Some (true) } else { None } ; self } # [doc = " Enable or disable the CRLF mode flag (`R`) by default."] pub fn crlf (& mut self , yes : bool) -> & mut TranslatorBuilder { self . flags . crlf = if yes { Some (true) } else { None } ; self } # [doc = " Enable or disable the \"swap greed\" flag (`U`) by default."] pub fn swap_greed (& mut self , yes : bool) -> & mut TranslatorBuilder { self . flags . swap_greed = if yes { Some (true) } else { None } ; self } # [doc = " Enable or disable the Unicode flag (`u`) by default."] pub fn unicode (& mut self , yes : bool) -> & mut TranslatorBuilder { self . flags . unicode = if yes { None } else { Some (false) } ; self } }
};
}
