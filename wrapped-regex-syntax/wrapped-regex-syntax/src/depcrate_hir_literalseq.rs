// Generated macro for Seq (struct)
macro_rules! Depcrate_hir_literalSeq {
() => {
// Module: crate::hir::literal
// Provides: {"Seq"}
// Dependencies: {}
# [doc = " A sequence of literals."] # [doc = ""] # [doc = " A `Seq` is very much like a set in that it represents a union of its"] # [doc = " members. That is, it corresponds to a set of literals where at least one"] # [doc = " must match in order for a particular [`Hir`] expression to match. (Whether"] # [doc = " this corresponds to the entire `Hir` expression, a prefix of it or a suffix"] # [doc = " of it depends on how the `Seq` was extracted from the `Hir`.)"] # [doc = ""] # [doc = " It is also unlike a set in that multiple identical literals may appear,"] # [doc = " and that the order of the literals in the `Seq` matters. For example, if"] # [doc = " the sequence is `[sam, samwise]` and leftmost-first matching is used, then"] # [doc = " `samwise` can never match and the sequence is equivalent to `[sam]`."] # [doc = ""] # [doc = " # States of a sequence"] # [doc = ""] # [doc = " A `Seq` has a few different logical states to consider:"] # [doc = ""] # [doc = " * The sequence can represent \"any\" literal. When this happens, the set does"] # [doc = " not have a finite size. The purpose of this state is to inhibit callers"] # [doc = " from making assumptions about what literals are required in order to match"] # [doc = " a particular [`Hir`] expression. Generally speaking, when a set is in this"] # [doc = " state, literal optimizations are inhibited. A good example of a regex that"] # [doc = " will cause this sort of set to appear is `[A-Za-z]`. The character class"] # [doc = " is just too big (and also too narrow) to be usefully expanded into 52"] # [doc = " different literals. (Note that the decision for when a seq should become"] # [doc = " infinite is determined by the caller. A seq itself has no hard-coded"] # [doc = " limits.)"] # [doc = " * The sequence can be empty, in which case, it is an affirmative statement"] # [doc = " that there are no literals that can match the corresponding `Hir`."] # [doc = " Consequently, the `Hir` never matches any input. For example, `[a&&b]`."] # [doc = " * The sequence can be non-empty, in which case, at least one of the"] # [doc = " literals must match in order for the corresponding `Hir` to match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how literal sequences can be simplified by stripping"] # [doc = " suffixes and minimizing while maintaining preference order."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " let mut seq = Seq::new(&["] # [doc = "     \"farm\","] # [doc = "     \"appliance\","] # [doc = "     \"faraway\","] # [doc = "     \"apple\","] # [doc = "     \"fare\","] # [doc = "     \"gap\","] # [doc = "     \"applicant\","] # [doc = "     \"applaud\","] # [doc = " ]);"] # [doc = " seq.keep_first_bytes(3);"] # [doc = " seq.minimize_by_preference();"] # [doc = " // Notice that 'far' comes before 'app', which matches the order in the"] # [doc = " // original sequence. This guarantees that leftmost-first semantics are"] # [doc = " // not altered by simplifying the set."] # [doc = " let expected = Seq::from_iter(["] # [doc = "     Literal::inexact(\"far\"),"] # [doc = "     Literal::inexact(\"app\"),"] # [doc = "     Literal::exact(\"gap\"),"] # [doc = " ]);"] # [doc = " assert_eq!(expected, seq);"] # [doc = " ```"] # [derive (Clone , Eq , PartialEq)] pub struct Seq { # [doc = " The members of this seq."] # [doc = ""] # [doc = " When `None`, the seq represents all possible literals. That is, it"] # [doc = " prevents one from making assumptions about specific literals in the"] # [doc = " seq, and forces one to treat it as if any literal might be in the seq."] # [doc = ""] # [doc = " Note that `Some(vec![])` is valid and corresponds to the empty seq of"] # [doc = " literals, i.e., a regex that can never match. For example, `[a&&b]`."] # [doc = " It is distinct from `Some(vec![\"\"])`, which corresponds to the seq"] # [doc = " containing an empty string, which matches at every position."] literals : Option < Vec < Literal > > , }
};
}
