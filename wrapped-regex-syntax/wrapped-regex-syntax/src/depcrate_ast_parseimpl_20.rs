// Generated macro for impl_20 (impl)
macro_rules! Depcrate_ast_parseimpl_20 {
() => {
// Module: crate::ast::parse
// Provides: {"impl_20"}
// Dependencies: {}
impl ParserBuilder { # [doc = " Create a new parser builder with a default configuration."] pub fn new () -> ParserBuilder { ParserBuilder { ignore_whitespace : false , nest_limit : 250 , octal : false , empty_min_range : false , } } # [doc = " Build a parser from this configuration with the given pattern."] pub fn build (& self) -> Parser { Parser { pos : Cell :: new (Position { offset : 0 , line : 1 , column : 1 }) , capture_index : Cell :: new (0) , nest_limit : self . nest_limit , octal : self . octal , empty_min_range : self . empty_min_range , initial_ignore_whitespace : self . ignore_whitespace , ignore_whitespace : Cell :: new (self . ignore_whitespace) , comments : RefCell :: new (vec ! []) , stack_group : RefCell :: new (vec ! []) , stack_class : RefCell :: new (vec ! []) , capture_names : RefCell :: new (vec ! []) , scratch : RefCell :: new (String :: new ()) , } } # [doc = " Set the nesting limit for this parser."] # [doc = ""] # [doc = " The nesting limit controls how deep the abstract syntax tree is allowed"] # [doc = " to be. If the AST exceeds the given limit (e.g., with too many nested"] # [doc = " groups), then an error is returned by the parser."] # [doc = ""] # [doc = " The purpose of this limit is to act as a heuristic to prevent stack"] # [doc = " overflow for consumers that do structural induction on an `Ast` using"] # [doc = " explicit recursion. While this crate never does this (instead using"] # [doc = " constant stack space and moving the call stack to the heap), other"] # [doc = " crates may."] # [doc = ""] # [doc = " This limit is not checked until the entire AST is parsed. Therefore,"] # [doc = " if callers want to put a limit on the amount of heap space used, then"] # [doc = " they should impose a limit on the length, in bytes, of the concrete"] # [doc = " pattern string. In particular, this is viable since this parser"] # [doc = " implementation will limit itself to heap space proportional to the"] # [doc = " length of the pattern string."] # [doc = ""] # [doc = " Note that a nest limit of `0` will return a nest limit error for most"] # [doc = " patterns but not all. For example, a nest limit of `0` permits `a` but"] # [doc = " not `ab`, since `ab` requires a concatenation, which results in a nest"] # [doc = " depth of `1`. In general, a nest limit is not something that manifests"] # [doc = " in an obvious way in the concrete syntax, therefore, it should not be"] # [doc = " used in a granular way."] pub fn nest_limit (& mut self , limit : u32) -> & mut ParserBuilder { self . nest_limit = limit ; self } # [doc = " Whether to support octal syntax or not."] # [doc = ""] # [doc = " Octal syntax is a little-known way of uttering Unicode codepoints in"] # [doc = " a regular expression. For example, `a`, `\\x61`, `\\u0061` and"] # [doc = " `\\141` are all equivalent regular expressions, where the last example"] # [doc = " shows octal syntax."] # [doc = ""] # [doc = " While supporting octal syntax isn't in and of itself a problem, it does"] # [doc = " make good error messages harder. That is, in PCRE based regex engines,"] # [doc = " syntax like `\\0` invokes a backreference, which is explicitly"] # [doc = " unsupported in Rust's regex engine. However, many users expect it to"] # [doc = " be supported. Therefore, when octal support is disabled, the error"] # [doc = " message will explicitly mention that backreferences aren't supported."] # [doc = ""] # [doc = " Octal syntax is disabled by default."] pub fn octal (& mut self , yes : bool) -> & mut ParserBuilder { self . octal = yes ; self } # [doc = " Enable verbose mode in the regular expression."] # [doc = ""] # [doc = " When enabled, verbose mode permits insignificant whitespace in many"] # [doc = " places in the regular expression, as well as comments. Comments are"] # [doc = " started using `#` and continue until the end of the line."] # [doc = ""] # [doc = " By default, this is disabled. It may be selectively enabled in the"] # [doc = " regular expression by using the `x` flag regardless of this setting."] pub fn ignore_whitespace (& mut self , yes : bool) -> & mut ParserBuilder { self . ignore_whitespace = yes ; self } # [doc = " Allow using `{,n}` as an equivalent to `{0,n}`."] # [doc = ""] # [doc = " When enabled, the parser accepts `{,n}` as valid syntax for `{0,n}`."] # [doc = " Most regular expression engines don't support the `{,n}` syntax, but"] # [doc = " some others do it, namely Python's `re` library."] # [doc = ""] # [doc = " This is disabled by default."] pub fn empty_min_range (& mut self , yes : bool) -> & mut ParserBuilder { self . empty_min_range = yes ; self } }
};
}
