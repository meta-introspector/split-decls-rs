// Generated macro for Hir (struct)
macro_rules! Depcrate_hirHir {
() => {
// Module: crate::hir
// Provides: {"Hir"}
// Dependencies: {}
# [doc = " A high-level intermediate representation (HIR) for a regular expression."] # [doc = ""] # [doc = " An HIR value is a combination of a [`HirKind`] and a set of [`Properties`]."] # [doc = " An `HirKind` indicates what kind of regular expression it is (a literal,"] # [doc = " a repetition, a look-around assertion, etc.), where as a `Properties`"] # [doc = " describes various facts about the regular expression. For example, whether"] # [doc = " it matches UTF-8 or if it matches the empty string."] # [doc = ""] # [doc = " The HIR of a regular expression represents an intermediate step between"] # [doc = " its abstract syntax (a structured description of the concrete syntax) and"] # [doc = " an actual regex matcher. The purpose of HIR is to make regular expressions"] # [doc = " easier to analyze. In particular, the AST is much more complex than the"] # [doc = " HIR. For example, while an AST supports arbitrarily nested character"] # [doc = " classes, the HIR will flatten all nested classes into a single set. The HIR"] # [doc = " will also \"compile away\" every flag present in the concrete syntax. For"] # [doc = " example, users of HIR expressions never need to worry about case folding;"] # [doc = " it is handled automatically by the translator (e.g., by translating"] # [doc = " `(?i:A)` to `[aA]`)."] # [doc = ""] # [doc = " The specific type of an HIR expression can be accessed via its `kind`"] # [doc = " or `into_kind` methods. This extra level of indirection exists for two"] # [doc = " reasons:"] # [doc = ""] # [doc = " 1. Construction of an HIR expression *must* use the constructor methods on"] # [doc = " this `Hir` type instead of building the `HirKind` values directly. This"] # [doc = " permits construction to enforce invariants like \"concatenations always"] # [doc = " consist of two or more sub-expressions.\""] # [doc = " 2. Every HIR expression contains attributes that are defined inductively,"] # [doc = " and can be computed cheaply during the construction process. For example,"] # [doc = " one such attribute is whether the expression must match at the beginning of"] # [doc = " the haystack."] # [doc = ""] # [doc = " In particular, if you have an `HirKind` value, then there is intentionally"] # [doc = " no way to build an `Hir` value from it. You instead need to do case"] # [doc = " analysis on the `HirKind` value and build the `Hir` value using its smart"] # [doc = " constructors."] # [doc = ""] # [doc = " # UTF-8"] # [doc = ""] # [doc = " If the HIR was produced by a translator with"] # [doc = " [`TranslatorBuilder::utf8`](translate::TranslatorBuilder::utf8) enabled,"] # [doc = " then the HIR is guaranteed to match UTF-8 exclusively for all non-empty"] # [doc = " matches."] # [doc = ""] # [doc = " For empty matches, those can occur at any position. It is the"] # [doc = " responsibility of the regex engine to determine whether empty matches are"] # [doc = " permitted between the code units of a single codepoint."] # [doc = ""] # [doc = " # Stack space"] # [doc = ""] # [doc = " This type defines its own destructor that uses constant stack space and"] # [doc = " heap space proportional to the size of the HIR."] # [doc = ""] # [doc = " Also, an `Hir`'s `fmt::Display` implementation prints an HIR as a regular"] # [doc = " expression pattern string, and uses constant stack space and heap space"] # [doc = " proportional to the size of the `Hir`. The regex it prints is guaranteed to"] # [doc = " be _semantically_ equivalent to the original concrete syntax, but it may"] # [doc = " look very different. (And potentially not practically readable by a human.)"] # [doc = ""] # [doc = " An `Hir`'s `fmt::Debug` implementation currently does not use constant"] # [doc = " stack space. The implementation will also suppress some details (such as"] # [doc = " the `Properties` inlined into every `Hir` value to make it less noisy)."] # [derive (Clone , Eq , PartialEq)] pub struct Hir { # [doc = " The underlying HIR kind."] kind : HirKind , # [doc = " Analysis info about this HIR, computed during construction."] props : Properties , }
};
}
