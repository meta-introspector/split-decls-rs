// Generated macro for impl_122 (impl)
macro_rules! Depcrateimpl_122 {
() => {
// Module: crate
// Provides: {"impl_122"}
// Dependencies: {}
impl ByteRange { # [doc = " Create a new class range."] # [doc = ""] # [doc = " If `end < start`, then the two values are swapped so that"] # [doc = " the invariant `start <= end` is preserved."] fn new (start : u8 , end : u8) -> ByteRange { if start <= end { ByteRange { start : start , end : end } } else { ByteRange { start : end , end : start } } } # [doc = " Returns true if and only if the two ranges are overlapping. Note that"] # [doc = " since ranges are inclusive, `a-c` and `d-f` are overlapping!"] fn overlapping (self , other : ByteRange) -> bool { max (self . start , other . start) <= min (self . end , other . end) . saturating_add (1) } # [doc = " Returns true if and only if the intersection of self and other is non"] # [doc = " empty."] fn is_intersect_empty (self , other : ByteRange) -> bool { max (self . start , other . start) > min (self . end , other . end) } # [doc = " Creates a new range representing the union of `self` and `other."] fn merge (self , other : ByteRange) -> ByteRange { ByteRange { start : min (self . start , other . start) , end : max (self . end , other . end) , } } # [doc = " Apply case folding to this range."] # [doc = ""] # [doc = " Since case folding might add bytes such that the range is no"] # [doc = " longer contiguous, this returns multiple byte ranges."] # [doc = ""] # [doc = " This assumes that the bytes in this range are ASCII compatible."] fn case_fold (self) -> Vec < ByteRange > { let mut ranges = vec ! [self] ; if ! ByteRange :: new (b'a' , b'z') . is_intersect_empty (self) { let lower = max (self . start , b'a') ; let upper = min (self . end , b'z') ; ranges . push (ByteRange :: new (lower - 32 , upper - 32)) ; } if ! ByteRange :: new (b'A' , b'Z') . is_intersect_empty (self) { let lower = max (self . start , b'A') ; let upper = min (self . end , b'Z') ; ranges . push (ByteRange :: new (lower + 32 , upper + 32)) ; } ranges } }
};
}
