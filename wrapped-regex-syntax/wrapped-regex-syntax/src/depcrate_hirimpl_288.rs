// Generated macro for impl_288 (impl)
macro_rules! Depcrate_hirimpl_288 {
() => {
// Module: crate::hir
// Provides: {"impl_288"}
// Dependencies: {}
impl Properties { # [doc = " Returns the length (in bytes) of the smallest string matched by this"] # [doc = " HIR."] # [doc = ""] # [doc = " A return value of `0` is possible and occurs when the HIR can match an"] # [doc = " empty string."] # [doc = ""] # [doc = " `None` is returned when there is no minimum length. This occurs in"] # [doc = " precisely the cases where the HIR matches nothing. i.e., The language"] # [doc = " the regex matches is empty. An example of such a regex is `\\P{any}`."] # [inline] pub fn minimum_len (& self) -> Option < usize > { self . 0 . minimum_len } # [doc = " Returns the length (in bytes) of the longest string matched by this"] # [doc = " HIR."] # [doc = ""] # [doc = " A return value of `0` is possible and occurs when nothing longer than"] # [doc = " the empty string is in the language described by this HIR."] # [doc = ""] # [doc = " `None` is returned when there is no longest matching string. This"] # [doc = " occurs when the HIR matches nothing or when there is no upper bound on"] # [doc = " the length of matching strings. Example of such regexes are `\\P{any}`"] # [doc = " (matches nothing) and `a+` (has no upper bound)."] # [inline] pub fn maximum_len (& self) -> Option < usize > { self . 0 . maximum_len } # [doc = " Returns a set of all look-around assertions that appear at least once"] # [doc = " in this HIR value."] # [inline] pub fn look_set (& self) -> LookSet { self . 0 . look_set } # [doc = " Returns a set of all look-around assertions that appear as a prefix for"] # [doc = " this HIR value. That is, the set returned corresponds to the set of"] # [doc = " assertions that must be passed before matching any bytes in a haystack."] # [doc = ""] # [doc = " For example, `hir.look_set_prefix().contains(Look::Start)` returns true"] # [doc = " if and only if the HIR is fully anchored at the start."] # [inline] pub fn look_set_prefix (& self) -> LookSet { self . 0 . look_set_prefix } # [doc = " Returns a set of all look-around assertions that appear as a _possible_"] # [doc = " prefix for this HIR value. That is, the set returned corresponds to the"] # [doc = " set of assertions that _may_ be passed before matching any bytes in a"] # [doc = " haystack."] # [doc = ""] # [doc = " For example, `hir.look_set_prefix_any().contains(Look::Start)` returns"] # [doc = " true if and only if it's possible for the regex to match through a"] # [doc = " anchored assertion before consuming any input."] # [inline] pub fn look_set_prefix_any (& self) -> LookSet { self . 0 . look_set_prefix_any } # [doc = " Returns a set of all look-around assertions that appear as a suffix for"] # [doc = " this HIR value. That is, the set returned corresponds to the set of"] # [doc = " assertions that must be passed in order to be considered a match after"] # [doc = " all other consuming HIR expressions."] # [doc = ""] # [doc = " For example, `hir.look_set_suffix().contains(Look::End)` returns true"] # [doc = " if and only if the HIR is fully anchored at the end."] # [inline] pub fn look_set_suffix (& self) -> LookSet { self . 0 . look_set_suffix } # [doc = " Returns a set of all look-around assertions that appear as a _possible_"] # [doc = " suffix for this HIR value. That is, the set returned corresponds to the"] # [doc = " set of assertions that _may_ be passed before matching any bytes in a"] # [doc = " haystack."] # [doc = ""] # [doc = " For example, `hir.look_set_suffix_any().contains(Look::End)` returns"] # [doc = " true if and only if it's possible for the regex to match through a"] # [doc = " anchored assertion at the end of a match without consuming any input."] # [inline] pub fn look_set_suffix_any (& self) -> LookSet { self . 0 . look_set_suffix_any } # [doc = " Return true if and only if the corresponding HIR will always match"] # [doc = " valid UTF-8."] # [doc = ""] # [doc = " When this returns false, then it is possible for this HIR expression to"] # [doc = " match invalid UTF-8, including by matching between the code units of"] # [doc = " a single UTF-8 encoded codepoint."] # [doc = ""] # [doc = " Note that this returns true even when the corresponding HIR can match"] # [doc = " the empty string. Since an empty string can technically appear between"] # [doc = " UTF-8 code units, it is possible for a match to be reported that splits"] # [doc = " a codepoint which could in turn be considered matching invalid UTF-8."] # [doc = " However, it is generally assumed that such empty matches are handled"] # [doc = " specially by the search routine if it is absolutely required that"] # [doc = " matches not split a codepoint."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This code example shows the UTF-8 property of a variety of patterns."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::{ParserBuilder, parse};"] # [doc = ""] # [doc = " // Examples of 'is_utf8() == true'."] # [doc = " assert!(parse(r\"a\")?.properties().is_utf8());"] # [doc = " assert!(parse(r\"[^a]\")?.properties().is_utf8());"] # [doc = " assert!(parse(r\".\")?.properties().is_utf8());"] # [doc = " assert!(parse(r\"\\W\")?.properties().is_utf8());"] # [doc = " assert!(parse(r\"\\b\")?.properties().is_utf8());"] # [doc = " assert!(parse(r\"\\B\")?.properties().is_utf8());"] # [doc = " assert!(parse(r\"(?-u)\\b\")?.properties().is_utf8());"] # [doc = " assert!(parse(r\"(?-u)\\B\")?.properties().is_utf8());"] # [doc = " // Unicode mode is enabled by default, and in"] # [doc = " // that mode, all \\x hex escapes are treated as"] # [doc = " // codepoints. So this actually matches the UTF-8"] # [doc = " // encoding of U+00FF."] # [doc = " assert!(parse(r\"\\xFF\")?.properties().is_utf8());"] # [doc = ""] # [doc = " // Now we show examples of 'is_utf8() == false'."] # [doc = " // The only way to do this is to force the parser"] # [doc = " // to permit invalid UTF-8, otherwise all of these"] # [doc = " // would fail to parse!"] # [doc = " let parse = |pattern| {"] # [doc = "     ParserBuilder::new().utf8(false).build().parse(pattern)"] # [doc = " };"] # [doc = " assert!(!parse(r\"(?-u)[^a]\")?.properties().is_utf8());"] # [doc = " assert!(!parse(r\"(?-u).\")?.properties().is_utf8());"] # [doc = " assert!(!parse(r\"(?-u)\\W\")?.properties().is_utf8());"] # [doc = " // Conversely to the equivalent example above,"] # [doc = " // when Unicode mode is disabled, \\x hex escapes"] # [doc = " // are treated as their raw byte values."] # [doc = " assert!(!parse(r\"(?-u)\\xFF\")?.properties().is_utf8());"] # [doc = " // Note that just because we disabled UTF-8 in the"] # [doc = " // parser doesn't mean we still can't use Unicode."] # [doc = " // It is enabled by default, so \\xFF is still"] # [doc = " // equivalent to matching the UTF-8 encoding of"] # [doc = " // U+00FF by default."] # [doc = " assert!(parse(r\"\\xFF\")?.properties().is_utf8());"] # [doc = " // Even though we use raw bytes that individually"] # [doc = " // are not valid UTF-8, when combined together, the"] # [doc = " // overall expression *does* match valid UTF-8!"] # [doc = " assert!(parse(r\"(?-u)\\xE2\\x98\\x83\")?.properties().is_utf8());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn is_utf8 (& self) -> bool { self . 0 . utf8 } # [doc = " Returns the total number of explicit capturing groups in the"] # [doc = " corresponding HIR."] # [doc = ""] # [doc = " Note that this does not include the implicit capturing group"] # [doc = " corresponding to the entire match that is typically included by regex"] # [doc = " engines."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This method will return `0` for `a` and `1` for `(a)`:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::parse;"] # [doc = ""] # [doc = " assert_eq!(0, parse(\"a\")?.properties().explicit_captures_len());"] # [doc = " assert_eq!(1, parse(\"(a)\")?.properties().explicit_captures_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn explicit_captures_len (& self) -> usize { self . 0 . explicit_captures_len } # [doc = " Returns the total number of explicit capturing groups that appear in"] # [doc = " every possible match."] # [doc = ""] # [doc = " If the number of capture groups can vary depending on the match, then"] # [doc = " this returns `None`. That is, a value is only returned when the number"] # [doc = " of matching groups is invariant or \"static.\""] # [doc = ""] # [doc = " Note that this does not include the implicit capturing group"] # [doc = " corresponding to the entire match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows a few cases where a static number of capture groups is"] # [doc = " available and a few cases where it is not."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::parse;"] # [doc = ""] # [doc = " let len = |pattern| {"] # [doc = "     parse(pattern).map(|h| {"] # [doc = "         h.properties().static_explicit_captures_len()"] # [doc = "     })"] # [doc = " };"] # [doc = ""] # [doc = " assert_eq!(Some(0), len(\"a\")?);"] # [doc = " assert_eq!(Some(1), len(\"(a)\")?);"] # [doc = " assert_eq!(Some(1), len(\"(a)|(b)\")?);"] # [doc = " assert_eq!(Some(2), len(\"(a)(b)|(c)(d)\")?);"] # [doc = " assert_eq!(None, len(\"(a)|b\")?);"] # [doc = " assert_eq!(None, len(\"a|(b)\")?);"] # [doc = " assert_eq!(None, len(\"(b)*\")?);"] # [doc = " assert_eq!(Some(1), len(\"(b)+\")?);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn static_explicit_captures_len (& self) -> Option < usize > { self . 0 . static_explicit_captures_len } # [doc = " Return true if and only if this HIR is a simple literal. This is"] # [doc = " only true when this HIR expression is either itself a `Literal` or a"] # [doc = " concatenation of only `Literal`s."] # [doc = ""] # [doc = " For example, `f` and `foo` are literals, but `f+`, `(foo)`, `foo()` and"] # [doc = " the empty string are not (even though they contain sub-expressions that"] # [doc = " are literals)."] # [inline] pub fn is_literal (& self) -> bool { self . 0 . literal } # [doc = " Return true if and only if this HIR is either a simple literal or an"] # [doc = " alternation of simple literals. This is only"] # [doc = " true when this HIR expression is either itself a `Literal` or a"] # [doc = " concatenation of only `Literal`s or an alternation of only `Literal`s."] # [doc = ""] # [doc = " For example, `f`, `foo`, `a|b|c`, and `foo|bar|baz` are alternation"] # [doc = " literals, but `f+`, `(foo)`, `foo()`, and the empty pattern are not"] # [doc = " (even though that contain sub-expressions that are literals)."] # [inline] pub fn is_alternation_literal (& self) -> bool { self . 0 . alternation_literal } # [doc = " Returns the total amount of heap memory usage, in bytes, used by this"] # [doc = " `Properties` value."] # [inline] pub fn memory_usage (& self) -> usize { core :: mem :: size_of :: < PropertiesI > () } # [doc = " Returns a new set of properties that corresponds to the union of the"] # [doc = " iterator of properties given."] # [doc = ""] # [doc = " This is useful when one has multiple `Hir` expressions and wants"] # [doc = " to combine them into a single alternation without constructing the"] # [doc = " corresponding `Hir`. This routine provides a way of combining the"] # [doc = " properties of each `Hir` expression into one set of properties"] # [doc = " representing the union of those expressions."] # [doc = ""] # [doc = " # Example: union with HIRs that never match"] # [doc = ""] # [doc = " This example shows that unioning properties together with one that"] # [doc = " represents a regex that never matches will \"poison\" certain attributes,"] # [doc = " like the minimum and maximum lengths."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::{hir::Properties, parse};"] # [doc = ""] # [doc = " let hir1 = parse(\"ab?c?\")?;"] # [doc = " assert_eq!(Some(1), hir1.properties().minimum_len());"] # [doc = " assert_eq!(Some(3), hir1.properties().maximum_len());"] # [doc = ""] # [doc = " let hir2 = parse(r\"[a&&b]\")?;"] # [doc = " assert_eq!(None, hir2.properties().minimum_len());"] # [doc = " assert_eq!(None, hir2.properties().maximum_len());"] # [doc = ""] # [doc = " let hir3 = parse(r\"wxy?z?\")?;"] # [doc = " assert_eq!(Some(2), hir3.properties().minimum_len());"] # [doc = " assert_eq!(Some(4), hir3.properties().maximum_len());"] # [doc = ""] # [doc = " let unioned = Properties::union(["] # [doc = "\t\thir1.properties(),"] # [doc = "\t\thir2.properties(),"] # [doc = "\t\thir3.properties(),"] # [doc = "\t]);"] # [doc = " assert_eq!(None, unioned.minimum_len());"] # [doc = " assert_eq!(None, unioned.maximum_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " The maximum length can also be \"poisoned\" by a pattern that has no"] # [doc = " upper bound on the length of a match. The minimum length remains"] # [doc = " unaffected:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::{hir::Properties, parse};"] # [doc = ""] # [doc = " let hir1 = parse(\"ab?c?\")?;"] # [doc = " assert_eq!(Some(1), hir1.properties().minimum_len());"] # [doc = " assert_eq!(Some(3), hir1.properties().maximum_len());"] # [doc = ""] # [doc = " let hir2 = parse(r\"a+\")?;"] # [doc = " assert_eq!(Some(1), hir2.properties().minimum_len());"] # [doc = " assert_eq!(None, hir2.properties().maximum_len());"] # [doc = ""] # [doc = " let hir3 = parse(r\"wxy?z?\")?;"] # [doc = " assert_eq!(Some(2), hir3.properties().minimum_len());"] # [doc = " assert_eq!(Some(4), hir3.properties().maximum_len());"] # [doc = ""] # [doc = " let unioned = Properties::union(["] # [doc = "\t\thir1.properties(),"] # [doc = "\t\thir2.properties(),"] # [doc = "\t\thir3.properties(),"] # [doc = "\t]);"] # [doc = " assert_eq!(Some(1), unioned.minimum_len());"] # [doc = " assert_eq!(None, unioned.maximum_len());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn union < I , P > (props : I) -> Properties where I : IntoIterator < Item = P > , P : core :: borrow :: Borrow < Properties > , { let mut it = props . into_iter () . peekable () ; let fix = if it . peek () . is_none () { LookSet :: empty () } else { LookSet :: full () } ; let static_explicit_captures_len = it . peek () . and_then (| p | p . borrow () . static_explicit_captures_len ()) ; let mut props = PropertiesI { minimum_len : None , maximum_len : None , look_set : LookSet :: empty () , look_set_prefix : fix , look_set_suffix : fix , look_set_prefix_any : LookSet :: empty () , look_set_suffix_any : LookSet :: empty () , utf8 : true , explicit_captures_len : 0 , static_explicit_captures_len , literal : false , alternation_literal : true , } ; let (mut min_poisoned , mut max_poisoned) = (false , false) ; for prop in it { let p = prop . borrow () ; props . look_set . set_union (p . look_set ()) ; props . look_set_prefix . set_intersect (p . look_set_prefix ()) ; props . look_set_suffix . set_intersect (p . look_set_suffix ()) ; props . look_set_prefix_any . set_union (p . look_set_prefix_any ()) ; props . look_set_suffix_any . set_union (p . look_set_suffix_any ()) ; props . utf8 = props . utf8 && p . is_utf8 () ; props . explicit_captures_len = props . explicit_captures_len . saturating_add (p . explicit_captures_len ()) ; if props . static_explicit_captures_len != p . static_explicit_captures_len () { props . static_explicit_captures_len = None ; } props . alternation_literal = props . alternation_literal && p . is_literal () ; if ! min_poisoned { if let Some (xmin) = p . minimum_len () { if props . minimum_len . map_or (true , | pmin | xmin < pmin) { props . minimum_len = Some (xmin) ; } } else { props . minimum_len = None ; min_poisoned = true ; } } if ! max_poisoned { if let Some (xmax) = p . maximum_len () { if props . maximum_len . map_or (true , | pmax | xmax > pmax) { props . maximum_len = Some (xmax) ; } } else { props . maximum_len = None ; max_poisoned = true ; } } } Properties (Box :: new (props)) } }
};
}
