// Generated macro for HirFrame (enum)
macro_rules! Depcrate_hir_translateHirFrame {
() => {
// Module: crate::hir::translate
// Provides: {"HirFrame"}
// Dependencies: {}
# [doc = " An HirFrame is a single stack frame, represented explicitly, which is"] # [doc = " created for each item in the Ast that we traverse."] # [doc = ""] # [doc = " Note that technically, this type doesn't represent our entire stack"] # [doc = " frame. In particular, the Ast visitor represents any state associated with"] # [doc = " traversing the Ast itself."] # [derive (Clone , Debug)] enum HirFrame { # [doc = " An arbitrary HIR expression. These get pushed whenever we hit a base"] # [doc = " case in the Ast. They get popped after an inductive (i.e., recursive)"] # [doc = " step is complete."] Expr (Hir) , # [doc = " A literal that is being constructed, character by character, from the"] # [doc = " AST. We need this because the AST gives each individual character its"] # [doc = " own node. So as we see characters, we peek at the top-most HirFrame."] # [doc = " If it's a literal, then we add to it. Otherwise, we push a new literal."] # [doc = " When it comes time to pop it, we convert it to an Hir via Hir::literal."] Literal (Vec < u8 >) , # [doc = " A Unicode character class. This frame is mutated as we descend into"] # [doc = " the Ast of a character class (which is itself its own mini recursive"] # [doc = " structure)."] ClassUnicode (hir :: ClassUnicode) , # [doc = " A byte-oriented character class. This frame is mutated as we descend"] # [doc = " into the Ast of a character class (which is itself its own mini"] # [doc = " recursive structure)."] # [doc = ""] # [doc = " Byte character classes are created when Unicode mode (`u`) is disabled."] # [doc = " If `utf8` is enabled (the default), then a byte character is only"] # [doc = " permitted to match ASCII text."] ClassBytes (hir :: ClassBytes) , # [doc = " This is pushed whenever a repetition is observed. After visiting every"] # [doc = " sub-expression in the repetition, the translator's stack is expected to"] # [doc = " have this sentinel at the top."] # [doc = ""] # [doc = " This sentinel only exists to stop other things (like flattening"] # [doc = " literals) from reaching across repetition operators."] Repetition , # [doc = " This is pushed on to the stack upon first seeing any kind of capture,"] # [doc = " indicated by parentheses (including non-capturing groups). It is popped"] # [doc = " upon leaving a group."] Group { # [doc = " The old active flags when this group was opened."] # [doc = ""] # [doc = " If this group sets flags, then the new active flags are set to the"] # [doc = " result of merging the old flags with the flags introduced by this"] # [doc = " group. If the group doesn't set any flags, then this is simply"] # [doc = " equivalent to whatever flags were set when the group was opened."] # [doc = ""] # [doc = " When this group is popped, the active flags should be restored to"] # [doc = " the flags set here."] # [doc = ""] # [doc = " The \"active\" flags correspond to whatever flags are set in the"] # [doc = " Translator."] old_flags : Flags , } , # [doc = " This is pushed whenever a concatenation is observed. After visiting"] # [doc = " every sub-expression in the concatenation, the translator's stack is"] # [doc = " popped until it sees a Concat frame."] Concat , # [doc = " This is pushed whenever an alternation is observed. After visiting"] # [doc = " every sub-expression in the alternation, the translator's stack is"] # [doc = " popped until it sees an Alternation frame."] Alternation , # [doc = " This is pushed immediately before each sub-expression in an"] # [doc = " alternation. This separates the branches of an alternation on the"] # [doc = " stack and prevents literal flattening from reaching across alternation"] # [doc = " branches."] # [doc = ""] # [doc = " It is popped after each expression in a branch until an 'Alternation'"] # [doc = " frame is observed when doing a post visit on an alternation."] AlternationBranch , }
};
}
