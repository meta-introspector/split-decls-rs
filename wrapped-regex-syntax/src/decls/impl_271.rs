macro_rules! deps {
    () => {
        ParserBuilder!();
        Parser!();
        Ast!();
    };
}

macro_rules! impl_271 {
    () => {
        deps!();
        impl ParserBuilder { # [doc = " Create a new parser builder with a default configuration."] pub fn new () -> ParserBuilder { ParserBuilder :: default () } # [doc = " Build a parser from this configuration with the given pattern."] pub fn build (& self) -> Parser { Parser { ast : self . ast . build () , hir : self . hir . build () } } # [doc = " Set the nesting limit for this parser."] # [doc = ""] # [doc = " The nesting limit controls how deep the abstract syntax tree is allowed"] # [doc = " to be. If the AST exceeds the given limit (e.g., with too many nested"] # [doc = " groups), then an error is returned by the parser."] # [doc = ""] # [doc = " The purpose of this limit is to act as a heuristic to prevent stack"] # [doc = " overflow for consumers that do structural induction on an `Ast` using"] # [doc = " explicit recursion. While this crate never does this (instead using"] # [doc = " constant stack space and moving the call stack to the heap), other"] # [doc = " crates may."] # [doc = ""] # [doc = " This limit is not checked until the entire Ast is parsed. Therefore,"] # [doc = " if callers want to put a limit on the amount of heap space used, then"] # [doc = " they should impose a limit on the length, in bytes, of the concrete"] # [doc = " pattern string. In particular, this is viable since this parser"] # [doc = " implementation will limit itself to heap space proportional to the"] # [doc = " length of the pattern string."] # [doc = ""] # [doc = " Note that a nest limit of `0` will return a nest limit error for most"] # [doc = " patterns but not all. For example, a nest limit of `0` permits `a` but"] # [doc = " not `ab`, since `ab` requires a concatenation, which results in a nest"] # [doc = " depth of `1`. In general, a nest limit is not something that manifests"] # [doc = " in an obvious way in the concrete syntax, therefore, it should not be"] # [doc = " used in a granular way."] pub fn nest_limit (& mut self , limit : u32) -> & mut ParserBuilder { self . ast . nest_limit (limit) ; self } # [doc = " Whether to support octal syntax or not."] # [doc = ""] # [doc = " Octal syntax is a little-known way of uttering Unicode codepoints in"] # [doc = " a regular expression. For example, `a`, `\\x61`, `\\u0061` and"] # [doc = " `\\141` are all equivalent regular expressions, where the last example"] # [doc = " shows octal syntax."] # [doc = ""] # [doc = " While supporting octal syntax isn't in and of itself a problem, it does"] # [doc = " make good error messages harder. That is, in PCRE based regex engines,"] # [doc = " syntax like `\\0` invokes a backreference, which is explicitly"] # [doc = " unsupported in Rust's regex engine. However, many users expect it to"] # [doc = " be supported. Therefore, when octal support is disabled, the error"] # [doc = " message will explicitly mention that backreferences aren't supported."] # [doc = ""] # [doc = " Octal syntax is disabled by default."] pub fn octal (& mut self , yes : bool) -> & mut ParserBuilder { self . ast . octal (yes) ; self } # [doc = " When disabled, translation will permit the construction of a regular"] # [doc = " expression that may match invalid UTF-8."] # [doc = ""] # [doc = " When enabled (the default), the translator is guaranteed to produce an"] # [doc = " expression that, for non-empty matches, will only ever produce spans"] # [doc = " that are entirely valid UTF-8 (otherwise, the translator will return an"] # [doc = " error)."] # [doc = ""] # [doc = " Perhaps surprisingly, when UTF-8 is enabled, an empty regex or even"] # [doc = " a negated ASCII word boundary (uttered as `(?-u:\\B)` in the concrete"] # [doc = " syntax) will be allowed even though they can produce matches that split"] # [doc = " a UTF-8 encoded codepoint. This only applies to zero-width or \"empty\""] # [doc = " matches, and it is expected that the regex engine itself must handle"] # [doc = " these cases if necessary (perhaps by suppressing any zero-width matches"] # [doc = " that split a codepoint)."] pub fn utf8 (& mut self , yes : bool) -> & mut ParserBuilder { self . hir . utf8 (yes) ; self } # [doc = " Enable verbose mode in the regular expression."] # [doc = ""] # [doc = " When enabled, verbose mode permits insignificant whitespace in many"] # [doc = " places in the regular expression, as well as comments. Comments are"] # [doc = " started using `#` and continue until the end of the line."] # [doc = ""] # [doc = " By default, this is disabled. It may be selectively enabled in the"] # [doc = " regular expression by using the `x` flag regardless of this setting."] pub fn ignore_whitespace (& mut self , yes : bool) -> & mut ParserBuilder { self . ast . ignore_whitespace (yes) ; self } # [doc = " Enable or disable the case insensitive flag by default."] # [doc = ""] # [doc = " By default this is disabled. It may alternatively be selectively"] # [doc = " enabled in the regular expression itself via the `i` flag."] pub fn case_insensitive (& mut self , yes : bool) -> & mut ParserBuilder { self . hir . case_insensitive (yes) ; self } # [doc = " Enable or disable the multi-line matching flag by default."] # [doc = ""] # [doc = " By default this is disabled. It may alternatively be selectively"] # [doc = " enabled in the regular expression itself via the `m` flag."] pub fn multi_line (& mut self , yes : bool) -> & mut ParserBuilder { self . hir . multi_line (yes) ; self } # [doc = " Enable or disable the \"dot matches any character\" flag by default."] # [doc = ""] # [doc = " By default this is disabled. It may alternatively be selectively"] # [doc = " enabled in the regular expression itself via the `s` flag."] pub fn dot_matches_new_line (& mut self , yes : bool) -> & mut ParserBuilder { self . hir . dot_matches_new_line (yes) ; self } # [doc = " Enable or disable the CRLF mode flag by default."] # [doc = ""] # [doc = " By default this is disabled. It may alternatively be selectively"] # [doc = " enabled in the regular expression itself via the `R` flag."] # [doc = ""] # [doc = " When CRLF mode is enabled, the following happens:"] # [doc = ""] # [doc = " * Unless `dot_matches_new_line` is enabled, `.` will match any character"] # [doc = " except for `\\r` and `\\n`."] # [doc = " * When `multi_line` mode is enabled, `^` and `$` will treat `\\r\\n`,"] # [doc = " `\\r` and `\\n` as line terminators. And in particular, neither will"] # [doc = " match between a `\\r` and a `\\n`."] pub fn crlf (& mut self , yes : bool) -> & mut ParserBuilder { self . hir . crlf (yes) ; self } # [doc = " Sets the line terminator for use with `(?u-s:.)` and `(?-us:.)`."] # [doc = ""] # [doc = " Namely, instead of `.` (by default) matching everything except for `\\n`,"] # [doc = " this will cause `.` to match everything except for the byte given."] # [doc = ""] # [doc = " If `.` is used in a context where Unicode mode is enabled and this byte"] # [doc = " isn't ASCII, then an error will be returned. When Unicode mode is"] # [doc = " disabled, then any byte is permitted, but will return an error if UTF-8"] # [doc = " mode is enabled and it is a non-ASCII byte."] # [doc = ""] # [doc = " In short, any ASCII value for a line terminator is always okay. But a"] # [doc = " non-ASCII byte might result in an error depending on whether Unicode"] # [doc = " mode or UTF-8 mode are enabled."] # [doc = ""] # [doc = " Note that if `R` mode is enabled then it always takes precedence and"] # [doc = " the line terminator will be treated as `\\r` and `\\n` simultaneously."] # [doc = ""] # [doc = " Note also that this *doesn't* impact the look-around assertions"] # [doc = " `(?m:^)` and `(?m:$)`. That's usually controlled by additional"] # [doc = " configuration in the regex engine itself."] pub fn line_terminator (& mut self , byte : u8) -> & mut ParserBuilder { self . hir . line_terminator (byte) ; self } # [doc = " Enable or disable the \"swap greed\" flag by default."] # [doc = ""] # [doc = " By default this is disabled. It may alternatively be selectively"] # [doc = " enabled in the regular expression itself via the `U` flag."] pub fn swap_greed (& mut self , yes : bool) -> & mut ParserBuilder { self . hir . swap_greed (yes) ; self } # [doc = " Enable or disable the Unicode flag (`u`) by default."] # [doc = ""] # [doc = " By default this is **enabled**. It may alternatively be selectively"] # [doc = " disabled in the regular expression itself via the `u` flag."] # [doc = ""] # [doc = " Note that unless `utf8` is disabled (it's enabled by default), a"] # [doc = " regular expression will fail to parse if Unicode mode is disabled and a"] # [doc = " sub-expression could possibly match invalid UTF-8."] pub fn unicode (& mut self , yes : bool) -> & mut ParserBuilder { self . hir . unicode (yes) ; self } }
    };
}

impl_271!();