macro_rules! deps {
    () => {
        Seq!();
        Literal!();
        PreferenceTrie!();
    };
}

macro_rules! impl_162 {
    () => {
        deps!();
        impl Seq { # [doc = " Returns an empty sequence."] # [doc = ""] # [doc = " An empty sequence matches zero literals, and thus corresponds to a"] # [doc = " regex that itself can never match."] # [inline] pub fn empty () -> Seq { Seq { literals : Some (vec ! []) } } # [doc = " Returns a sequence of literals without a finite size and may contain"] # [doc = " any literal."] # [doc = ""] # [doc = " A sequence without finite size does not reveal anything about the"] # [doc = " characteristics of the literals in its set. There are no fixed prefixes"] # [doc = " or suffixes, nor are lower or upper bounds on the length of the literals"] # [doc = " in the set known."] # [doc = ""] # [doc = " This is useful to represent constructs in a regex that are \"too big\""] # [doc = " to useful represent as a sequence of literals. For example, `[A-Za-z]`."] # [doc = " When sequences get too big, they lose their discriminating nature and"] # [doc = " are more likely to produce false positives, which in turn makes them"] # [doc = " less likely to speed up searches."] # [doc = ""] # [doc = " More pragmatically, for many regexes, enumerating all possible literals"] # [doc = " is itself not possible or might otherwise use too many resources. So"] # [doc = " constraining the size of sets during extraction is a practical trade"] # [doc = " off to make."] # [inline] pub fn infinite () -> Seq { Seq { literals : None } } # [doc = " Returns a sequence containing a single literal."] # [inline] pub fn singleton (lit : Literal) -> Seq { Seq { literals : Some (vec ! [lit]) } } # [doc = " Returns a sequence of exact literals from the given byte strings."] # [inline] pub fn new < I , B > (it : I) -> Seq where I : IntoIterator < Item = B > , B : AsRef < [u8] > , { it . into_iter () . map (| b | Literal :: exact (b . as_ref ())) . collect () } # [doc = " If this is a finite sequence, return its members as a slice of"] # [doc = " literals."] # [doc = ""] # [doc = " The slice returned may be empty, in which case, there are no literals"] # [doc = " that can match this sequence."] # [inline] pub fn literals (& self) -> Option < & [Literal] > { self . literals . as_deref () } # [doc = " Push a literal to the end of this sequence."] # [doc = ""] # [doc = " If this sequence is not finite, then this is a no-op."] # [doc = ""] # [doc = " Similarly, if the most recently added item of this sequence is"] # [doc = " equivalent to the literal given, then it is not added. This reflects"] # [doc = " a `Seq`'s \"set like\" behavior, and represents a practical trade off."] # [doc = " Namely, there is never any need to have two adjacent and equivalent"] # [doc = " literals in the same sequence, _and_ it is easy to detect in some"] # [doc = " cases."] # [inline] pub fn push (& mut self , lit : Literal) { let lits = match self . literals { None => return , Some (ref mut lits) => lits , } ; if lits . last () . map_or (false , | m | m == & lit) { return ; } lits . push (lit) ; } # [doc = " Make all of the literals in this sequence inexact."] # [doc = ""] # [doc = " This is a no-op if this sequence is not finite."] # [inline] pub fn make_inexact (& mut self) { let lits = match self . literals { None => return , Some (ref mut lits) => lits , } ; for lit in lits . iter_mut () { lit . make_inexact () ; } } # [doc = " Converts this sequence to an infinite sequence."] # [doc = ""] # [doc = " This is a no-op if the sequence is already infinite."] # [inline] pub fn make_infinite (& mut self) { self . literals = None ; } # [doc = " Modify this sequence to contain the cross product between it and the"] # [doc = " sequence given."] # [doc = ""] # [doc = " The cross product only considers literals in this sequence that are"] # [doc = " exact. That is, inexact literals are not extended."] # [doc = ""] # [doc = " The literals are always drained from `other`, even if none are used."] # [doc = " This permits callers to reuse the sequence allocation elsewhere."] # [doc = ""] # [doc = " If this sequence is infinite, then this is a no-op, regardless of what"] # [doc = " `other` contains (and in this case, the literals are still drained from"] # [doc = " `other`). If `other` is infinite and this sequence is finite, then this"] # [doc = " is a no-op, unless this sequence contains a zero-length literal. In"] # [doc = " which case, the infiniteness of `other` infects this sequence, and this"] # [doc = " sequence is itself made infinite."] # [doc = ""] # [doc = " Like [`Seq::union`], this may attempt to deduplicate literals. See"] # [doc = " [`Seq::dedup`] for how deduplication deals with exact and inexact"] # [doc = " literals."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows basic usage and how exact and inexact literals"] # [doc = " interact."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " let mut seq1 = Seq::from_iter(["] # [doc = "     Literal::exact(\"foo\"),"] # [doc = "     Literal::inexact(\"bar\"),"] # [doc = " ]);"] # [doc = " let mut seq2 = Seq::from_iter(["] # [doc = "     Literal::inexact(\"quux\"),"] # [doc = "     Literal::exact(\"baz\"),"] # [doc = " ]);"] # [doc = " seq1.cross_forward(&mut seq2);"] # [doc = ""] # [doc = " // The literals are pulled out of seq2."] # [doc = " assert_eq!(Some(0), seq2.len());"] # [doc = ""] # [doc = " let expected = Seq::from_iter(["] # [doc = "     Literal::inexact(\"fooquux\"),"] # [doc = "     Literal::exact(\"foobaz\"),"] # [doc = "     Literal::inexact(\"bar\"),"] # [doc = " ]);"] # [doc = " assert_eq!(expected, seq1);"] # [doc = " ```"] # [doc = ""] # [doc = " This example shows the behavior of when `other` is an infinite"] # [doc = " sequence."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " let mut seq1 = Seq::from_iter(["] # [doc = "     Literal::exact(\"foo\"),"] # [doc = "     Literal::inexact(\"bar\"),"] # [doc = " ]);"] # [doc = " let mut seq2 = Seq::infinite();"] # [doc = " seq1.cross_forward(&mut seq2);"] # [doc = ""] # [doc = " // When seq2 is infinite, cross product doesn't add anything, but"] # [doc = " // ensures all members of seq1 are inexact."] # [doc = " let expected = Seq::from_iter(["] # [doc = "     Literal::inexact(\"foo\"),"] # [doc = "     Literal::inexact(\"bar\"),"] # [doc = " ]);"] # [doc = " assert_eq!(expected, seq1);"] # [doc = " ```"] # [doc = ""] # [doc = " This example is like the one above, but shows what happens when this"] # [doc = " sequence contains an empty string. In this case, an infinite `other`"] # [doc = " sequence infects this sequence (because the empty string means that"] # [doc = " there are no finite prefixes):"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " let mut seq1 = Seq::from_iter(["] # [doc = "     Literal::exact(\"foo\"),"] # [doc = "     Literal::exact(\"\"), // inexact provokes same behavior"] # [doc = "     Literal::inexact(\"bar\"),"] # [doc = " ]);"] # [doc = " let mut seq2 = Seq::infinite();"] # [doc = " seq1.cross_forward(&mut seq2);"] # [doc = ""] # [doc = " // seq1 is now infinite!"] # [doc = " assert!(!seq1.is_finite());"] # [doc = " ```"] # [doc = ""] # [doc = " This example shows the behavior of this sequence is infinite."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " let mut seq1 = Seq::infinite();"] # [doc = " let mut seq2 = Seq::from_iter(["] # [doc = "     Literal::exact(\"foo\"),"] # [doc = "     Literal::inexact(\"bar\"),"] # [doc = " ]);"] # [doc = " seq1.cross_forward(&mut seq2);"] # [doc = ""] # [doc = " // seq1 remains unchanged."] # [doc = " assert!(!seq1.is_finite());"] # [doc = " // Even though the literals in seq2 weren't used, it was still drained."] # [doc = " assert_eq!(Some(0), seq2.len());"] # [doc = " ```"] # [inline] pub fn cross_forward (& mut self , other : & mut Seq) { let (lits1 , lits2) = match self . cross_preamble (other) { None => return , Some ((lits1 , lits2)) => (lits1 , lits2) , } ; let newcap = lits1 . len () . saturating_mul (lits2 . len ()) ; for selflit in mem :: replace (lits1 , Vec :: with_capacity (newcap)) { if ! selflit . is_exact () { lits1 . push (selflit) ; continue ; } for otherlit in lits2 . iter () { let mut newlit = Literal :: exact (Vec :: with_capacity (selflit . len () + otherlit . len () ,)) ; newlit . extend (& selflit) ; newlit . extend (& otherlit) ; if ! otherlit . is_exact () { newlit . make_inexact () ; } lits1 . push (newlit) ; } } lits2 . drain (..) ; self . dedup () ; } # [doc = " Modify this sequence to contain the cross product between it and"] # [doc = " the sequence given, where the sequences are treated as suffixes"] # [doc = " instead of prefixes. Namely, the sequence `other` is *prepended*"] # [doc = " to `self` (as opposed to `other` being *appended* to `self` in"] # [doc = " [`Seq::cross_forward`])."] # [doc = ""] # [doc = " The cross product only considers literals in this sequence that are"] # [doc = " exact. That is, inexact literals are not extended."] # [doc = ""] # [doc = " The literals are always drained from `other`, even if none are used."] # [doc = " This permits callers to reuse the sequence allocation elsewhere."] # [doc = ""] # [doc = " If this sequence is infinite, then this is a no-op, regardless of what"] # [doc = " `other` contains (and in this case, the literals are still drained from"] # [doc = " `other`). If `other` is infinite and this sequence is finite, then this"] # [doc = " is a no-op, unless this sequence contains a zero-length literal. In"] # [doc = " which case, the infiniteness of `other` infects this sequence, and this"] # [doc = " sequence is itself made infinite."] # [doc = ""] # [doc = " Like [`Seq::union`], this may attempt to deduplicate literals. See"] # [doc = " [`Seq::dedup`] for how deduplication deals with exact and inexact"] # [doc = " literals."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows basic usage and how exact and inexact literals"] # [doc = " interact."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " let mut seq1 = Seq::from_iter(["] # [doc = "     Literal::exact(\"foo\"),"] # [doc = "     Literal::inexact(\"bar\"),"] # [doc = " ]);"] # [doc = " let mut seq2 = Seq::from_iter(["] # [doc = "     Literal::inexact(\"quux\"),"] # [doc = "     Literal::exact(\"baz\"),"] # [doc = " ]);"] # [doc = " seq1.cross_reverse(&mut seq2);"] # [doc = ""] # [doc = " // The literals are pulled out of seq2."] # [doc = " assert_eq!(Some(0), seq2.len());"] # [doc = ""] # [doc = " let expected = Seq::from_iter(["] # [doc = "     Literal::inexact(\"quuxfoo\"),"] # [doc = "     Literal::inexact(\"bar\"),"] # [doc = "     Literal::exact(\"bazfoo\"),"] # [doc = " ]);"] # [doc = " assert_eq!(expected, seq1);"] # [doc = " ```"] # [doc = ""] # [doc = " This example shows the behavior of when `other` is an infinite"] # [doc = " sequence."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " let mut seq1 = Seq::from_iter(["] # [doc = "     Literal::exact(\"foo\"),"] # [doc = "     Literal::inexact(\"bar\"),"] # [doc = " ]);"] # [doc = " let mut seq2 = Seq::infinite();"] # [doc = " seq1.cross_reverse(&mut seq2);"] # [doc = ""] # [doc = " // When seq2 is infinite, cross product doesn't add anything, but"] # [doc = " // ensures all members of seq1 are inexact."] # [doc = " let expected = Seq::from_iter(["] # [doc = "     Literal::inexact(\"foo\"),"] # [doc = "     Literal::inexact(\"bar\"),"] # [doc = " ]);"] # [doc = " assert_eq!(expected, seq1);"] # [doc = " ```"] # [doc = ""] # [doc = " This example is like the one above, but shows what happens when this"] # [doc = " sequence contains an empty string. In this case, an infinite `other`"] # [doc = " sequence infects this sequence (because the empty string means that"] # [doc = " there are no finite suffixes):"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " let mut seq1 = Seq::from_iter(["] # [doc = "     Literal::exact(\"foo\"),"] # [doc = "     Literal::exact(\"\"), // inexact provokes same behavior"] # [doc = "     Literal::inexact(\"bar\"),"] # [doc = " ]);"] # [doc = " let mut seq2 = Seq::infinite();"] # [doc = " seq1.cross_reverse(&mut seq2);"] # [doc = ""] # [doc = " // seq1 is now infinite!"] # [doc = " assert!(!seq1.is_finite());"] # [doc = " ```"] # [doc = ""] # [doc = " This example shows the behavior when this sequence is infinite."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " let mut seq1 = Seq::infinite();"] # [doc = " let mut seq2 = Seq::from_iter(["] # [doc = "     Literal::exact(\"foo\"),"] # [doc = "     Literal::inexact(\"bar\"),"] # [doc = " ]);"] # [doc = " seq1.cross_reverse(&mut seq2);"] # [doc = ""] # [doc = " // seq1 remains unchanged."] # [doc = " assert!(!seq1.is_finite());"] # [doc = " // Even though the literals in seq2 weren't used, it was still drained."] # [doc = " assert_eq!(Some(0), seq2.len());"] # [doc = " ```"] # [inline] pub fn cross_reverse (& mut self , other : & mut Seq) { let (lits1 , lits2) = match self . cross_preamble (other) { None => return , Some ((lits1 , lits2)) => (lits1 , lits2) , } ; let newcap = lits1 . len () . saturating_mul (lits2 . len ()) ; let selflits = mem :: replace (lits1 , Vec :: with_capacity (newcap)) ; for (i , otherlit) in lits2 . drain (..) . enumerate () { for selflit in selflits . iter () { if ! selflit . is_exact () { if i == 0 { lits1 . push (selflit . clone ()) ; } continue ; } let mut newlit = Literal :: exact (Vec :: with_capacity (otherlit . len () + selflit . len () ,)) ; newlit . extend (& otherlit) ; newlit . extend (& selflit) ; if ! otherlit . is_exact () { newlit . make_inexact () ; } lits1 . push (newlit) ; } } self . dedup () ; } # [doc = " A helper function the corresponds to the subtle preamble for both"] # [doc = " `cross_forward` and `cross_reverse`. In effect, it handles the cases"] # [doc = " of infinite sequences for both `self` and `other`, as well as ensuring"] # [doc = " that literals from `other` are drained even if they aren't used."] fn cross_preamble < 'a > (& 'a mut self , other : & 'a mut Seq ,) -> Option < (& 'a mut Vec < Literal > , & 'a mut Vec < Literal >) > { let lits2 = match other . literals { None => { if self . min_literal_len () == Some (0) { * self = Seq :: infinite () ; } else { self . make_inexact () ; } return None ; } Some (ref mut lits) => lits , } ; let lits1 = match self . literals { None => { lits2 . drain (..) ; return None ; } Some (ref mut lits) => lits , } ; Some ((lits1 , lits2)) } # [doc = " Unions the `other` sequence into this one."] # [doc = ""] # [doc = " The literals are always drained out of the given `other` sequence,"] # [doc = " even if they are being unioned into an infinite sequence. This permits"] # [doc = " the caller to reuse the `other` sequence in another context."] # [doc = ""] # [doc = " Some literal deduping may be performed. If any deduping happens,"] # [doc = " any leftmost-first or \"preference\" order match semantics will be"] # [doc = " preserved."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows basic usage."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::Seq;"] # [doc = ""] # [doc = " let mut seq1 = Seq::new(&[\"foo\", \"bar\"]);"] # [doc = " let mut seq2 = Seq::new(&[\"bar\", \"quux\", \"foo\"]);"] # [doc = " seq1.union(&mut seq2);"] # [doc = ""] # [doc = " // The literals are pulled out of seq2."] # [doc = " assert_eq!(Some(0), seq2.len());"] # [doc = ""] # [doc = " // Adjacent literals are deduped, but non-adjacent literals may not be."] # [doc = " assert_eq!(Seq::new(&[\"foo\", \"bar\", \"quux\", \"foo\"]), seq1);"] # [doc = " ```"] # [doc = ""] # [doc = " This example shows that literals are drained from `other` even when"] # [doc = " they aren't necessarily used."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::Seq;"] # [doc = ""] # [doc = " let mut seq1 = Seq::infinite();"] # [doc = " // Infinite sequences have no finite length."] # [doc = " assert_eq!(None, seq1.len());"] # [doc = ""] # [doc = " let mut seq2 = Seq::new(&[\"bar\", \"quux\", \"foo\"]);"] # [doc = " seq1.union(&mut seq2);"] # [doc = ""] # [doc = " // seq1 is still infinite and seq2 has been drained."] # [doc = " assert_eq!(None, seq1.len());"] # [doc = " assert_eq!(Some(0), seq2.len());"] # [doc = " ```"] # [inline] pub fn union (& mut self , other : & mut Seq) { let lits2 = match other . literals { None => { self . make_infinite () ; return ; } Some (ref mut lits) => lits . drain (..) , } ; let lits1 = match self . literals { None => return , Some (ref mut lits) => lits , } ; lits1 . extend (lits2) ; self . dedup () ; } # [doc = " Unions the `other` sequence into this one by splice the `other`"] # [doc = " sequence at the position of the first zero-length literal."] # [doc = ""] # [doc = " This is useful for preserving preference order semantics when combining"] # [doc = " two literal sequences. For example, in the regex `(a||f)+foo`, the"] # [doc = " correct preference order prefix sequence is `[a, foo, f]`."] # [doc = ""] # [doc = " The literals are always drained out of the given `other` sequence,"] # [doc = " even if they are being unioned into an infinite sequence. This permits"] # [doc = " the caller to reuse the `other` sequence in another context. Note that"] # [doc = " the literals are drained even if no union is performed as well, i.e.,"] # [doc = " when this sequence does not contain a zero-length literal."] # [doc = ""] # [doc = " Some literal deduping may be performed. If any deduping happens,"] # [doc = " any leftmost-first or \"preference\" order match semantics will be"] # [doc = " preserved."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows basic usage."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::Seq;"] # [doc = ""] # [doc = " let mut seq1 = Seq::new(&[\"a\", \"\", \"f\", \"\"]);"] # [doc = " let mut seq2 = Seq::new(&[\"foo\"]);"] # [doc = " seq1.union_into_empty(&mut seq2);"] # [doc = ""] # [doc = " // The literals are pulled out of seq2."] # [doc = " assert_eq!(Some(0), seq2.len());"] # [doc = " // 'foo' gets spliced into seq1 where the first empty string occurs."] # [doc = " assert_eq!(Seq::new(&[\"a\", \"foo\", \"f\"]), seq1);"] # [doc = " ```"] # [doc = ""] # [doc = " This example shows that literals are drained from `other` even when"] # [doc = " they aren't necessarily used."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::Seq;"] # [doc = ""] # [doc = " let mut seq1 = Seq::new(&[\"foo\", \"bar\"]);"] # [doc = " let mut seq2 = Seq::new(&[\"bar\", \"quux\", \"foo\"]);"] # [doc = " seq1.union_into_empty(&mut seq2);"] # [doc = ""] # [doc = " // seq1 has no zero length literals, so no splicing happens."] # [doc = " assert_eq!(Seq::new(&[\"foo\", \"bar\"]), seq1);"] # [doc = " // Even though no splicing happens, seq2 is still drained."] # [doc = " assert_eq!(Some(0), seq2.len());"] # [doc = " ```"] # [inline] pub fn union_into_empty (& mut self , other : & mut Seq) { let lits2 = other . literals . as_mut () . map (| lits | lits . drain (..)) ; let lits1 = match self . literals { None => return , Some (ref mut lits) => lits , } ; let first_empty = match lits1 . iter () . position (| m | m . is_empty ()) { None => return , Some (i) => i , } ; let lits2 = match lits2 { None => { self . literals = None ; return ; } Some (lits) => lits , } ; lits1 . retain (| m | ! m . is_empty ()) ; lits1 . splice (first_empty .. first_empty , lits2) ; self . dedup () ; } # [doc = " Deduplicate adjacent equivalent literals in this sequence."] # [doc = ""] # [doc = " If adjacent literals are equivalent strings but one is exact and the"] # [doc = " other inexact, the inexact literal is kept and the exact one is"] # [doc = " removed."] # [doc = ""] # [doc = " Deduping an infinite sequence is a no-op."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how literals that are duplicate byte strings but"] # [doc = " are not equivalent with respect to exactness are resolved."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " let mut seq = Seq::from_iter(["] # [doc = "     Literal::exact(\"foo\"),"] # [doc = "     Literal::inexact(\"foo\"),"] # [doc = " ]);"] # [doc = " seq.dedup();"] # [doc = ""] # [doc = " assert_eq!(Seq::from_iter([Literal::inexact(\"foo\")]), seq);"] # [doc = " ```"] # [inline] pub fn dedup (& mut self) { if let Some (ref mut lits) = self . literals { lits . dedup_by (| lit1 , lit2 | { if lit1 . as_bytes () != lit2 . as_bytes () { return false ; } if lit1 . is_exact () != lit2 . is_exact () { lit1 . make_inexact () ; lit2 . make_inexact () ; } true }) ; } } # [doc = " Sorts this sequence of literals lexicographically."] # [doc = ""] # [doc = " Note that if, before sorting, if a literal that is a prefix of another"] # [doc = " literal appears after it, then after sorting, the sequence will not"] # [doc = " represent the same preference order match semantics. For example,"] # [doc = " sorting the sequence `[samwise, sam]` yields the sequence `[sam,"] # [doc = " samwise]`. Under preference order semantics, the latter sequence will"] # [doc = " never match `samwise` where as the first sequence can."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows basic usage."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::Seq;"] # [doc = ""] # [doc = " let mut seq = Seq::new(&[\"foo\", \"quux\", \"bar\"]);"] # [doc = " seq.sort();"] # [doc = ""] # [doc = " assert_eq!(Seq::new(&[\"bar\", \"foo\", \"quux\"]), seq);"] # [doc = " ```"] # [inline] pub fn sort (& mut self) { if let Some (ref mut lits) = self . literals { lits . sort () ; } } # [doc = " Reverses all of the literals in this sequence."] # [doc = ""] # [doc = " The order of the sequence itself is preserved."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows basic usage."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::Seq;"] # [doc = ""] # [doc = " let mut seq = Seq::new(&[\"oof\", \"rab\"]);"] # [doc = " seq.reverse_literals();"] # [doc = " assert_eq!(Seq::new(&[\"foo\", \"bar\"]), seq);"] # [doc = " ```"] # [inline] pub fn reverse_literals (& mut self) { if let Some (ref mut lits) = self . literals { for lit in lits . iter_mut () { lit . reverse () ; } } } # [doc = " Shrinks this seq to its minimal size while respecting the preference"] # [doc = " order of its literals."] # [doc = ""] # [doc = " While this routine will remove duplicate literals from this seq, it"] # [doc = " will also remove literals that can never match in a leftmost-first or"] # [doc = " \"preference order\" search. Similar to [`Seq::dedup`], if a literal is"] # [doc = " deduped, then the one that remains is made inexact."] # [doc = ""] # [doc = " This is a no-op on seqs that are empty or not finite."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows the difference between `{sam, samwise}` and"] # [doc = " `{samwise, sam}`."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " // If 'sam' comes before 'samwise' and a preference order search is"] # [doc = " // executed, then 'samwise' can never match."] # [doc = " let mut seq = Seq::new(&[\"sam\", \"samwise\"]);"] # [doc = " seq.minimize_by_preference();"] # [doc = " assert_eq!(Seq::from_iter([Literal::inexact(\"sam\")]), seq);"] # [doc = ""] # [doc = " // But if they are reversed, then it's possible for 'samwise' to match"] # [doc = " // since it is given higher preference."] # [doc = " let mut seq = Seq::new(&[\"samwise\", \"sam\"]);"] # [doc = " seq.minimize_by_preference();"] # [doc = " assert_eq!(Seq::new(&[\"samwise\", \"sam\"]), seq);"] # [doc = " ```"] # [doc = ""] # [doc = " This example shows that if an empty string is in this seq, then"] # [doc = " anything that comes after it can never match."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " // An empty string is a prefix of all strings, so it automatically"] # [doc = " // inhibits any subsequent strings from matching."] # [doc = " let mut seq = Seq::new(&[\"foo\", \"bar\", \"\", \"quux\", \"fox\"]);"] # [doc = " seq.minimize_by_preference();"] # [doc = " let expected = Seq::from_iter(["] # [doc = "     Literal::exact(\"foo\"),"] # [doc = "     Literal::exact(\"bar\"),"] # [doc = "     Literal::inexact(\"\"),"] # [doc = " ]);"] # [doc = " assert_eq!(expected, seq);"] # [doc = ""] # [doc = " // And of course, if it's at the beginning, then it makes it impossible"] # [doc = " // for anything else to match."] # [doc = " let mut seq = Seq::new(&[\"\", \"foo\", \"quux\", \"fox\"]);"] # [doc = " seq.minimize_by_preference();"] # [doc = " assert_eq!(Seq::from_iter([Literal::inexact(\"\")]), seq);"] # [doc = " ```"] # [inline] pub fn minimize_by_preference (& mut self) { if let Some (ref mut lits) = self . literals { PreferenceTrie :: minimize (lits , false) ; } } # [doc = " Trims all literals in this seq such that only the first `len` bytes"] # [doc = " remain. If a literal has less than or equal to `len` bytes, then it"] # [doc = " remains unchanged. Otherwise, it is trimmed and made inexact."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " let mut seq = Seq::new(&[\"a\", \"foo\", \"quux\"]);"] # [doc = " seq.keep_first_bytes(2);"] # [doc = ""] # [doc = " let expected = Seq::from_iter(["] # [doc = "     Literal::exact(\"a\"),"] # [doc = "     Literal::inexact(\"fo\"),"] # [doc = "     Literal::inexact(\"qu\"),"] # [doc = " ]);"] # [doc = " assert_eq!(expected, seq);"] # [doc = " ```"] # [inline] pub fn keep_first_bytes (& mut self , len : usize) { if let Some (ref mut lits) = self . literals { for m in lits . iter_mut () { m . keep_first_bytes (len) ; } } } # [doc = " Trims all literals in this seq such that only the last `len` bytes"] # [doc = " remain. If a literal has less than or equal to `len` bytes, then it"] # [doc = " remains unchanged. Otherwise, it is trimmed and made inexact."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Literal, Seq};"] # [doc = ""] # [doc = " let mut seq = Seq::new(&[\"a\", \"foo\", \"quux\"]);"] # [doc = " seq.keep_last_bytes(2);"] # [doc = ""] # [doc = " let expected = Seq::from_iter(["] # [doc = "     Literal::exact(\"a\"),"] # [doc = "     Literal::inexact(\"oo\"),"] # [doc = "     Literal::inexact(\"ux\"),"] # [doc = " ]);"] # [doc = " assert_eq!(expected, seq);"] # [doc = " ```"] # [inline] pub fn keep_last_bytes (& mut self , len : usize) { if let Some (ref mut lits) = self . literals { for m in lits . iter_mut () { m . keep_last_bytes (len) ; } } } # [doc = " Returns true if this sequence is finite."] # [doc = ""] # [doc = " When false, this sequence is infinite and must be treated as if it"] # [doc = " contains every possible literal."] # [inline] pub fn is_finite (& self) -> bool { self . literals . is_some () } # [doc = " Returns true if and only if this sequence is finite and empty."] # [doc = ""] # [doc = " An empty sequence never matches anything. It can only be produced by"] # [doc = " literal extraction when the corresponding regex itself cannot match."] # [inline] pub fn is_empty (& self) -> bool { self . len () == Some (0) } # [doc = " Returns the number of literals in this sequence if the sequence is"] # [doc = " finite. If the sequence is infinite, then `None` is returned."] # [inline] pub fn len (& self) -> Option < usize > { self . literals . as_ref () . map (| lits | lits . len ()) } # [doc = " Returns true if and only if all literals in this sequence are exact."] # [doc = ""] # [doc = " This returns false if the sequence is infinite."] # [inline] pub fn is_exact (& self) -> bool { self . literals () . map_or (false , | lits | lits . iter () . all (| x | x . is_exact ())) } # [doc = " Returns true if and only if all literals in this sequence are inexact."] # [doc = ""] # [doc = " This returns true if the sequence is infinite."] # [inline] pub fn is_inexact (& self) -> bool { self . literals () . map_or (true , | lits | lits . iter () . all (| x | ! x . is_exact ())) } # [doc = " Return the maximum length of the sequence that would result from"] # [doc = " unioning `self` with `other`. If either set is infinite, then this"] # [doc = " returns `None`."] # [inline] pub fn max_union_len (& self , other : & Seq) -> Option < usize > { let len1 = self . len () ? ; let len2 = other . len () ? ; Some (len1 . saturating_add (len2)) } # [doc = " Return the maximum length of the sequence that would result from the"] # [doc = " cross product of `self` with `other`. If either set is infinite, then"] # [doc = " this returns `None`."] # [inline] pub fn max_cross_len (& self , other : & Seq) -> Option < usize > { let len1 = self . len () ? ; let len2 = other . len () ? ; Some (len1 . saturating_mul (len2)) } # [doc = " Returns the length of the shortest literal in this sequence."] # [doc = ""] # [doc = " If the sequence is infinite or empty, then this returns `None`."] # [inline] pub fn min_literal_len (& self) -> Option < usize > { self . literals . as_ref () ? . iter () . map (| x | x . len ()) . min () } # [doc = " Returns the length of the longest literal in this sequence."] # [doc = ""] # [doc = " If the sequence is infinite or empty, then this returns `None`."] # [inline] pub fn max_literal_len (& self) -> Option < usize > { self . literals . as_ref () ? . iter () . map (| x | x . len ()) . max () } # [doc = " Returns the longest common prefix from this seq."] # [doc = ""] # [doc = " If the seq matches any literal or other contains no literals, then"] # [doc = " there is no meaningful prefix and this returns `None`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows some example seqs and their longest common prefix."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::Seq;"] # [doc = ""] # [doc = " let seq = Seq::new(&[\"foo\", \"foobar\", \"fo\"]);"] # [doc = " assert_eq!(Some(&b\"fo\"[..]), seq.longest_common_prefix());"] # [doc = " let seq = Seq::new(&[\"foo\", \"foo\"]);"] # [doc = " assert_eq!(Some(&b\"foo\"[..]), seq.longest_common_prefix());"] # [doc = " let seq = Seq::new(&[\"foo\", \"bar\"]);"] # [doc = " assert_eq!(Some(&b\"\"[..]), seq.longest_common_prefix());"] # [doc = " let seq = Seq::new(&[\"\"]);"] # [doc = " assert_eq!(Some(&b\"\"[..]), seq.longest_common_prefix());"] # [doc = ""] # [doc = " let seq = Seq::infinite();"] # [doc = " assert_eq!(None, seq.longest_common_prefix());"] # [doc = " let seq = Seq::empty();"] # [doc = " assert_eq!(None, seq.longest_common_prefix());"] # [doc = " ```"] # [inline] pub fn longest_common_prefix (& self) -> Option < & [u8] > { let lits = match self . literals { None => return None , Some (ref lits) => lits , } ; if lits . len () == 0 { return None ; } let base = lits [0] . as_bytes () ; let mut len = base . len () ; for m in lits . iter () . skip (1) { len = m . as_bytes () . iter () . zip (base [.. len] . iter ()) . take_while (| & (a , b) | a == b) . count () ; if len == 0 { return Some (& []) ; } } Some (& base [.. len]) } # [doc = " Returns the longest common suffix from this seq."] # [doc = ""] # [doc = " If the seq matches any literal or other contains no literals, then"] # [doc = " there is no meaningful suffix and this returns `None`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows some example seqs and their longest common suffix."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::Seq;"] # [doc = ""] # [doc = " let seq = Seq::new(&[\"oof\", \"raboof\", \"of\"]);"] # [doc = " assert_eq!(Some(&b\"of\"[..]), seq.longest_common_suffix());"] # [doc = " let seq = Seq::new(&[\"foo\", \"foo\"]);"] # [doc = " assert_eq!(Some(&b\"foo\"[..]), seq.longest_common_suffix());"] # [doc = " let seq = Seq::new(&[\"foo\", \"bar\"]);"] # [doc = " assert_eq!(Some(&b\"\"[..]), seq.longest_common_suffix());"] # [doc = " let seq = Seq::new(&[\"\"]);"] # [doc = " assert_eq!(Some(&b\"\"[..]), seq.longest_common_suffix());"] # [doc = ""] # [doc = " let seq = Seq::infinite();"] # [doc = " assert_eq!(None, seq.longest_common_suffix());"] # [doc = " let seq = Seq::empty();"] # [doc = " assert_eq!(None, seq.longest_common_suffix());"] # [doc = " ```"] # [inline] pub fn longest_common_suffix (& self) -> Option < & [u8] > { let lits = match self . literals { None => return None , Some (ref lits) => lits , } ; if lits . len () == 0 { return None ; } let base = lits [0] . as_bytes () ; let mut len = base . len () ; for m in lits . iter () . skip (1) { len = m . as_bytes () . iter () . rev () . zip (base [base . len () - len ..] . iter () . rev ()) . take_while (| & (a , b) | a == b) . count () ; if len == 0 { return Some (& []) ; } } Some (& base [base . len () - len ..]) } # [doc = " Optimizes this seq while treating its literals as prefixes and"] # [doc = " respecting the preference order of its literals."] # [doc = ""] # [doc = " The specific way \"optimization\" works is meant to be an implementation"] # [doc = " detail, as it essentially represents a set of heuristics. The goal"] # [doc = " that optimization tries to accomplish is to make the literals in this"] # [doc = " set reflect inputs that will result in a more effective prefilter."] # [doc = " Principally by reducing the false positive rate of candidates found by"] # [doc = " the literals in this sequence. That is, when a match of a literal is"] # [doc = " found, we would like it to be a strong predictor of the overall match"] # [doc = " of the regex. If it isn't, then much time will be spent starting and"] # [doc = " stopping the prefilter search and attempting to confirm the match only"] # [doc = " to have it fail."] # [doc = ""] # [doc = " Some of those heuristics might be:"] # [doc = ""] # [doc = " * Identifying a common prefix from a larger sequence of literals, and"] # [doc = " shrinking the sequence down to that single common prefix."] # [doc = " * Rejecting the sequence entirely if it is believed to result in very"] # [doc = " high false positive rate. When this happens, the sequence is made"] # [doc = " infinite."] # [doc = " * Shrinking the sequence to a smaller number of literals representing"] # [doc = " prefixes, but not shrinking it so much as to make literals too short."] # [doc = " (A sequence with very short literals, of 1 or 2 bytes, will typically"] # [doc = " result in a higher false positive rate.)"] # [doc = ""] # [doc = " Optimization should only be run once extraction is complete. Namely,"] # [doc = " optimization may make assumptions that do not compose with other"] # [doc = " operations in the middle of extraction. For example, optimization will"] # [doc = " reduce `[E(sam), E(samwise)]` to `[E(sam)]`, but such a transformation"] # [doc = " is only valid if no other extraction will occur. If other extraction"] # [doc = " may occur, then the correct transformation would be to `[I(sam)]`."] # [doc = ""] # [doc = " The [`Seq::optimize_for_suffix_by_preference`] does the same thing, but"] # [doc = " for suffixes."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how optimization might transform a sequence. Note that"] # [doc = " the specific behavior is not a documented guarantee. The heuristics"] # [doc = " used are an implementation detail and may change over time in semver"] # [doc = " compatible releases."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Seq, Literal};"] # [doc = ""] # [doc = " let mut seq = Seq::new(&["] # [doc = "     \"samantha\","] # [doc = "     \"sam\","] # [doc = "     \"samwise\","] # [doc = "     \"frodo\","] # [doc = " ]);"] # [doc = " seq.optimize_for_prefix_by_preference();"] # [doc = " assert_eq!(Seq::from_iter(["] # [doc = "     Literal::exact(\"samantha\"),"] # [doc = "     // Kept exact even though 'samwise' got pruned"] # [doc = "     // because optimization assumes literal extraction"] # [doc = "     // has finished."] # [doc = "     Literal::exact(\"sam\"),"] # [doc = "     Literal::exact(\"frodo\"),"] # [doc = " ]), seq);"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: optimization may make the sequence infinite"] # [doc = ""] # [doc = " If the heuristics deem that the sequence could cause a very high false"] # [doc = " positive rate, then it may make the sequence infinite, effectively"] # [doc = " disabling its use as a prefilter."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Seq, Literal};"] # [doc = ""] # [doc = " let mut seq = Seq::new(&["] # [doc = "     \"samantha\","] # [doc = "     // An empty string matches at every position,"] # [doc = "     // thus rendering the prefilter completely"] # [doc = "     // ineffective."] # [doc = "     \"\","] # [doc = "     \"sam\","] # [doc = "     \"samwise\","] # [doc = "     \"frodo\","] # [doc = " ]);"] # [doc = " seq.optimize_for_prefix_by_preference();"] # [doc = " assert!(!seq.is_finite());"] # [doc = " ```"] # [doc = ""] # [doc = " Do note that just because there is a `\" \"` in the sequence, that"] # [doc = " doesn't mean the sequence will always be made infinite after it is"] # [doc = " optimized. Namely, if the sequence is considered exact (any match"] # [doc = " corresponds to an overall match of the original regex), then any match"] # [doc = " is an overall match, and so the false positive rate is always `0`."] # [doc = ""] # [doc = " To demonstrate this, we remove `samwise` from our sequence. This"] # [doc = " results in no optimization happening and all literals remain exact."] # [doc = " Thus the entire sequence is exact, and it is kept as-is, even though"] # [doc = " one is an ASCII space:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_syntax::hir::literal::{Seq, Literal};"] # [doc = ""] # [doc = " let mut seq = Seq::new(&["] # [doc = "     \"samantha\","] # [doc = "     \" \","] # [doc = "     \"sam\","] # [doc = "     \"frodo\","] # [doc = " ]);"] # [doc = " seq.optimize_for_prefix_by_preference();"] # [doc = " assert!(seq.is_finite());"] # [doc = " ```"] # [inline] pub fn optimize_for_prefix_by_preference (& mut self) { self . optimize_by_preference (true) ; } # [doc = " Optimizes this seq while treating its literals as suffixes and"] # [doc = " respecting the preference order of its literals."] # [doc = ""] # [doc = " Optimization should only be run once extraction is complete."] # [doc = ""] # [doc = " The [`Seq::optimize_for_prefix_by_preference`] does the same thing, but"] # [doc = " for prefixes. See its documentation for more explanation."] # [inline] pub fn optimize_for_suffix_by_preference (& mut self) { self . optimize_by_preference (false) ; } fn optimize_by_preference (& mut self , prefix : bool) { let origlen = match self . len () { None => return , Some (len) => len , } ; if self . min_literal_len () . map_or (false , | len | len == 0) { self . make_infinite () ; return ; } if prefix { if let Some (ref mut lits) = self . literals { PreferenceTrie :: minimize (lits , true) ; } } let fix = if prefix { self . longest_common_prefix () } else { self . longest_common_suffix () } ; if let Some (fix) = fix { if prefix && origlen > 1 && fix . len () >= 1 && fix . len () <= 3 && rank (fix [0]) < 200 { self . keep_first_bytes (1) ; self . dedup () ; return ; } let isfast = self . is_exact () && self . len () . map_or (false , | len | len <= 16) ; let usefix = fix . len () > 4 || (fix . len () > 1 && ! isfast) ; if usefix { if prefix { self . keep_first_bytes (fix . len ()) ; } else { self . keep_last_bytes (fix . len ()) ; } self . dedup () ; assert_eq ! (Some (1) , self . len ()) ; } } let exact : Option < Seq > = if self . is_exact () { Some (self . clone ()) } else { None } ; const ATTEMPTS : [(usize , usize) ; 5] = [(5 , 10) , (4 , 10) , (3 , 64) , (2 , 64) , (1 , 10)] ; for (keep , limit) in ATTEMPTS { let len = match self . len () { None => break , Some (len) => len , } ; if len <= limit { break ; } if prefix { self . keep_first_bytes (keep) ; } else { self . keep_last_bytes (keep) ; } if prefix { if let Some (ref mut lits) = self . literals { PreferenceTrie :: minimize (lits , true) ; } } } if let Some (lits) = self . literals () { if lits . iter () . any (| lit | lit . is_poisonous ()) { self . make_infinite () ; } } if let Some (exact) = exact { if ! self . is_finite () { * self = exact ; return ; } if self . min_literal_len () . map_or (true , | len | len <= 2) { * self = exact ; return ; } if self . len () . map_or (true , | len | len > 64) { * self = exact ; return ; } } } }
    };
}

impl_162!();