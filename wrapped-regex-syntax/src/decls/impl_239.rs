macro_rules! deps {
    () => {
        ClassUnicodeRange!();
        ClassBytes!();
        IntervalSet!();
        ClassBytesRange!();
        ClassBytesIter!();
        ClassUnicode!();
    };
}

macro_rules! impl_239 {
    () => {
        deps!();
        impl ClassBytes { # [doc = " Create a new class from a sequence of ranges."] # [doc = ""] # [doc = " The given ranges do not need to be in any specific order, and ranges"] # [doc = " may overlap. Ranges will automatically be sorted into a canonical"] # [doc = " non-overlapping order."] pub fn new < I > (ranges : I) -> ClassBytes where I : IntoIterator < Item = ClassBytesRange > , { ClassBytes { set : IntervalSet :: new (ranges) } } # [doc = " Create a new class with no ranges."] # [doc = ""] # [doc = " An empty class matches nothing. That is, it is equivalent to"] # [doc = " [`Hir::fail`]."] pub fn empty () -> ClassBytes { ClassBytes :: new (vec ! []) } # [doc = " Add a new range to this set."] pub fn push (& mut self , range : ClassBytesRange) { self . set . push (range) ; } # [doc = " Return an iterator over all ranges in this class."] # [doc = ""] # [doc = " The iterator yields ranges in ascending order."] pub fn iter (& self) -> ClassBytesIter < '_ > { ClassBytesIter (self . set . iter ()) } # [doc = " Return the underlying ranges as a slice."] pub fn ranges (& self) -> & [ClassBytesRange] { self . set . intervals () } # [doc = " Expand this character class such that it contains all case folded"] # [doc = " characters. For example, if this class consists of the range `a-z`,"] # [doc = " then applying case folding will result in the class containing both the"] # [doc = " ranges `a-z` and `A-Z`."] # [doc = ""] # [doc = " Note that this only applies ASCII case folding, which is limited to the"] # [doc = " characters `a-z` and `A-Z`."] pub fn case_fold_simple (& mut self) { self . set . case_fold_simple () . expect ("ASCII case folding never fails") ; } # [doc = " Negate this byte class."] # [doc = ""] # [doc = " For all `b` where `b` is a any byte, if `b` was in this set, then it"] # [doc = " will not be in this set after negation."] pub fn negate (& mut self) { self . set . negate () ; } # [doc = " Union this byte class with the given byte class, in place."] pub fn union (& mut self , other : & ClassBytes) { self . set . union (& other . set) ; } # [doc = " Intersect this byte class with the given byte class, in place."] pub fn intersect (& mut self , other : & ClassBytes) { self . set . intersect (& other . set) ; } # [doc = " Subtract the given byte class from this byte class, in place."] pub fn difference (& mut self , other : & ClassBytes) { self . set . difference (& other . set) ; } # [doc = " Compute the symmetric difference of the given byte classes, in place."] # [doc = ""] # [doc = " This computes the symmetric difference of two byte classes. This"] # [doc = " removes all elements in this class that are also in the given class,"] # [doc = " but all adds all elements from the given class that aren't in this"] # [doc = " class. That is, the class will contain all elements in either class,"] # [doc = " but will not contain any elements that are in both classes."] pub fn symmetric_difference (& mut self , other : & ClassBytes) { self . set . symmetric_difference (& other . set) ; } # [doc = " Returns true if and only if this character class will either match"] # [doc = " nothing or only ASCII bytes. Stated differently, this returns false"] # [doc = " if and only if this class contains a non-ASCII byte."] pub fn is_ascii (& self) -> bool { self . set . intervals () . last () . map_or (true , | r | r . end <= 0x7F) } # [doc = " Returns the length, in bytes, of the smallest string matched by this"] # [doc = " character class."] # [doc = ""] # [doc = " Returns `None` when the class is empty."] pub fn minimum_len (& self) -> Option < usize > { if self . ranges () . is_empty () { None } else { Some (1) } } # [doc = " Returns the length, in bytes, of the longest string matched by this"] # [doc = " character class."] # [doc = ""] # [doc = " Returns `None` when the class is empty."] pub fn maximum_len (& self) -> Option < usize > { if self . ranges () . is_empty () { None } else { Some (1) } } # [doc = " If this class consists of exactly one byte, then return it as"] # [doc = " a literal byte string."] # [doc = ""] # [doc = " If this class is empty or contains more than one byte, then `None`"] # [doc = " is returned."] pub fn literal (& self) -> Option < Vec < u8 > > { let rs = self . ranges () ; if rs . len () == 1 && rs [0] . start == rs [0] . end { Some (vec ! [rs [0] . start]) } else { None } } # [doc = " If this class consists of only ASCII ranges, then return its"] # [doc = " corresponding and equivalent Unicode class."] pub fn to_unicode_class (& self) -> Option < ClassUnicode > { if ! self . is_ascii () { return None ; } Some (ClassUnicode :: new (self . ranges () . iter () . map (| r | { ClassUnicodeRange { start : char :: from (r . start) , end : char :: from (r . end) , } }))) } }
    };
}

impl_239!()