// Generated macro for impl_ranged (macro)
macro_rules! Depcrateimpl_ranged {
() => {
// Module: crate
// Provides: {"impl_ranged"}
// Dependencies: {}
# [doc = " Implement a ranged integer type."] macro_rules ! impl_ranged { ($ ($ type : ident { mod_name : $ mod_name : ident internal : $ internal : ident signed : $ is_signed : ident unsigned : $ unsigned_type : ident optional : $ optional_type : ident from : [$ ($ from : ident ($ from_internal : ident)) +] $ (manual : [$ ($ skips : ident) +]) ? }) *) => { $ (# [doc = concat ! (article ! ($ is_signed) , " `" , stringify ! ($ internal) , "` that is known to be in the range `MIN..=MAX`." ,)] # [repr (transparent)] # [derive (Clone , Copy , Eq , Ord , Hash)] pub struct $ type < const MIN : $ internal , const MAX : $ internal > (Unsafe <$ internal >,) ; # [doc = concat ! ("An optional `" , stringify ! ($ type) , "`; similar to `Option<" , stringify ! ($ type) , ">` with better optimization." ,)] # [doc = ""] # [doc = concat ! ("If `MIN` is [`" , stringify ! ($ internal) , "::MIN`] _and_ `MAX` is [`" , stringify ! ($ internal) , "::MAX`] then compilation will fail. This is because there is no way to represent \
            the niche value." ,)] # [doc = ""] # [doc = " This type is useful when you need to store an optional ranged value in a struct, but"] # [doc = " do not want the overhead of an `Option` type. This reduces the size of the struct"] # [doc = " overall, and is particularly useful when you have a large number of optional fields."] # [doc = " Note that most operations must still be performed on the [`Option`] type, which is"] # [doc = concat ! ("obtained with [`" , stringify ! ($ optional_type) , "::get`].")] # [repr (transparent)] # [derive (Clone , Copy , Eq , Hash)] pub struct $ optional_type < const MIN : $ internal , const MAX : $ internal > ($ internal ,) ; impl $ type < 0 , 0 > { # [doc = concat ! ("A " , stringify ! ($ type) , " that is always `VALUE`.")] # [inline (always)] pub const fn exact < const VALUE : $ internal > () -> $ type < VALUE , VALUE > { unsafe { $ type :: new_unchecked (VALUE) } } } if_unsigned ! { $ is_signed impl $ type < 1 , { $ internal :: MAX } > { # [doc = " Creates a ranged integer from a non-zero value."] # [inline (always)] pub const fn from_nonzero (value : NonZero <$ internal >) -> Self { unsafe { Self :: new_unchecked (value . get ()) } } # [doc = " Creates a non-zero value from a ranged integer."] # [inline (always)] pub const fn to_nonzero (self) -> NonZero <$ internal > { unsafe { NonZero :: new_unchecked (self . get ()) } } } } impl < const MIN : $ internal , const MAX : $ internal > $ type < MIN , MAX > { # [doc = " The smallest value that can be represented by this type."] pub const MIN : Self = Self :: new_static ::< MIN > () ; # [doc = " The largest value that can be represented by this type."] pub const MAX : Self = Self :: new_static ::< MAX > () ; # [doc = " Creates a ranged integer without checking the value."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The value must be within the range `MIN..=MAX`."] # [track_caller] # [inline (always)] pub const unsafe fn new_unchecked (value : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { assert_unchecked (MIN <= value && value <= MAX) ; Self (Unsafe :: new (value)) } } # [doc = " Returns the value as a primitive type."] # [doc = ""] # [doc = " A call to this function will output a hint to the compiler that the value is in"] # [doc = " range. In general this will help the optimizer to generate better code, but in edge"] # [doc = " cases this may lead to worse code generation. To avoid outputting the hint, you can"] # [doc = concat ! ("use [`" , stringify ! ($ type) , "::get_without_hint`].")] # [track_caller] # [inline (always)] pub const fn get (self) -> $ internal { const { assert ! (MIN <= MAX) ; } unsafe { assert_unchecked (MIN <= * self . 0 . get () && * self . 0 . get () <= MAX) } ; * self . 0 . get () } # [doc = " Returns the value as a primitive type."] # [doc = ""] # [doc = concat ! ("The returned value is identical to [`" , stringify ! ($ type) , "::get`].")] # [doc = " Unlike `get`, no hints are output to the compiler indicating the range that the"] # [doc = " value is in. Depending on the scenario, this may with be helpful or harmful too"] # [doc = " optimization."] # [inline (always)] pub const fn get_without_hint (self) -> $ internal { const { assert ! (MIN <= MAX) ; } * self . 0 . get () } # [track_caller] # [inline (always)] pub (crate) const fn get_ref (& self) -> &$ internal { const { assert ! (MIN <= MAX) ; } let value = self . 0 . get () ; unsafe { assert_unchecked (MIN <= * value && * value <= MAX) } ; value } # [doc = " Creates a ranged integer if the given value is in the range `MIN..=MAX`."] # [inline (always)] pub const fn new (value : $ internal) -> Option < Self > { const { assert ! (MIN <= MAX) ; } if value < MIN || value > MAX { None } else { Some (unsafe { Self :: new_unchecked (value) }) } } # [doc = " Creates a ranged integer with a statically known value. **Fails to compile** if the"] # [doc = " value is not in range."] # [inline (always)] pub const fn new_static < const VALUE : $ internal > () -> Self { const { assert ! (MIN <= VALUE) ; assert ! (VALUE <= MAX) ; } unsafe { Self :: new_unchecked (VALUE) } } # [doc = " Creates a ranged integer with the given value, saturating if it is out of range."] # [inline] pub const fn new_saturating (value : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } if value < MIN { Self :: MIN } else if value > MAX { Self :: MAX } else { unsafe { Self :: new_unchecked (value) } } } # [doc = " Emit a hint to the compiler that the value is in range."] # [doc = ""] # [doc = " In some situations, this can help the optimizer to generate better code. In edge"] # [doc = " cases this may lead to **worse** code generation. If you are unsure whether this is"] # [doc = " helpful, harmful, or neutral, you should use [`cargo-show-asm`] to compare the"] # [doc = " generated assembly."] # [doc = ""] # [doc = " Aside from potentially affecting optimization, this function is a no-op."] # [doc = ""] # [doc = " [`cargo-show-asm`]: https://crates.io/crates/cargo-show-asm"] # [inline (always)] pub const fn emit_range_hint (self) { const { assert ! (MIN <= MAX) ; } let value = self . 0 . get () ; unsafe { assert_unchecked (MIN <= * value && * value <= MAX) } ; } # [doc = " Expand the range that the value may be in. **Fails to compile** if the new range is"] # [doc = " not a superset of the current range."] # [inline (always)] pub const fn expand < const NEW_MIN : $ internal , const NEW_MAX : $ internal > (self ,) -> $ type < NEW_MIN , NEW_MAX > { const { assert ! (MIN <= MAX) ; assert ! (NEW_MIN <= NEW_MAX) ; assert ! (NEW_MIN <= MIN) ; assert ! (NEW_MAX >= MAX) ; } unsafe { $ type :: new_unchecked (self . get ()) } } # [doc = " Attempt to narrow the range that the value may be in. Returns `None` if the value"] # [doc = " is outside the new range. **Fails to compile** if the new range is not a subset of"] # [doc = " the current range."] # [inline (always)] pub const fn narrow < const NEW_MIN : $ internal , const NEW_MAX : $ internal , > (self) -> Option <$ type < NEW_MIN , NEW_MAX >> { const { assert ! (MIN <= MAX) ; assert ! (NEW_MIN <= NEW_MAX) ; assert ! (NEW_MIN >= MIN) ; assert ! (NEW_MAX <= MAX) ; } $ type ::< NEW_MIN , NEW_MAX >:: new (self . get ()) } # [doc = " Converts a string slice in a given base to an integer."] # [doc = ""] # [doc = " The string is expected to be an optional `+` or `-` sign followed by digits. Leading"] # [doc = " and trailing whitespace represent an error. Digits are a subset of these characters,"] # [doc = " depending on `radix`:"] # [doc = ""] # [doc = " - `0-9`"] # [doc = " - `a-z`"] # [doc = " - `A-Z`"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `radix` is not in the range `2..=36`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```rust"] # [doc = concat ! ("# use deranged::" , stringify ! ($ type) , ";")] # [doc = concat ! ("assert_eq!(" , stringify ! ($ type) , "::<5, 10>::from_str_radix(\"A\", 16), Ok(" , stringify ! ($ type) , "::new_static::<10>()));" ,)] # [doc = " ```"] # [inline] pub fn from_str_radix (src : & str , radix : u32) -> Result < Self , ParseIntError > { const { assert ! (MIN <= MAX) ; } match $ internal :: from_str_radix (src , radix) { Ok (value) if value > MAX => { Err (ParseIntError { kind : IntErrorKind :: PosOverflow }) } Ok (value) if value < MIN => { Err (ParseIntError { kind : IntErrorKind :: NegOverflow }) } Ok (value) => Ok (unsafe { Self :: new_unchecked (value) }) , Err (e) => Err (ParseIntError { kind : e . kind () . clone () }) , } } # [doc = " Checked integer addition. Computes `self + rhs`, returning `None` if the resulting"] # [doc = " value is out of range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn checked_add (self , rhs : $ internal) -> Option < Self > { const { assert ! (MIN <= MAX) ; } Self :: new (const_try_opt ! (self . get () . checked_add (rhs))) } # [doc = " Unchecked integer addition. Computes `self + rhs`, assuming that the result is in"] # [doc = " range."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The result of `self + rhs` must be in the range `MIN..=MAX`."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline (always)] pub const unsafe fn unchecked_add (self , rhs : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked (unsafe_unwrap_unchecked ! (self . get () . checked_add (rhs))) } } # [doc = " Checked integer addition. Computes `self - rhs`, returning `None` if the resulting"] # [doc = " value is out of range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn checked_sub (self , rhs : $ internal) -> Option < Self > { const { assert ! (MIN <= MAX) ; } Self :: new (const_try_opt ! (self . get () . checked_sub (rhs))) } # [doc = " Unchecked integer subtraction. Computes `self - rhs`, assuming that the result is in"] # [doc = " range."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The result of `self - rhs` must be in the range `MIN..=MAX`."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline (always)] pub const unsafe fn unchecked_sub (self , rhs : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked (unsafe_unwrap_unchecked ! (self . get () . checked_sub (rhs))) } } # [doc = " Checked integer addition. Computes `self * rhs`, returning `None` if the resulting"] # [doc = " value is out of range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn checked_mul (self , rhs : $ internal) -> Option < Self > { const { assert ! (MIN <= MAX) ; } Self :: new (const_try_opt ! (self . get () . checked_mul (rhs))) } # [doc = " Unchecked integer multiplication. Computes `self * rhs`, assuming that the result is"] # [doc = " in range."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The result of `self * rhs` must be in the range `MIN..=MAX`."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline (always)] pub const unsafe fn unchecked_mul (self , rhs : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked (unsafe_unwrap_unchecked ! (self . get () . checked_mul (rhs))) } } # [doc = " Checked integer addition. Computes `self / rhs`, returning `None` if `rhs == 0` or"] # [doc = " if the resulting value is out of range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn checked_div (self , rhs : $ internal) -> Option < Self > { const { assert ! (MIN <= MAX) ; } Self :: new (const_try_opt ! (self . get () . checked_div (rhs))) } # [doc = " Unchecked integer division. Computes `self / rhs`, assuming that `rhs != 0` and that"] # [doc = " the result is in range."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `self` must not be zero and the result of `self / rhs` must be in the range"] # [doc = " `MIN..=MAX`."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline (always)] pub const unsafe fn unchecked_div (self , rhs : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked (unsafe_unwrap_unchecked ! (self . get () . checked_div (rhs))) } } # [doc = " Checked Euclidean division. Computes `self.div_euclid(rhs)`, returning `None` if"] # [doc = " `rhs == 0` or if the resulting value is out of range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn checked_div_euclid (self , rhs : $ internal) -> Option < Self > { const { assert ! (MIN <= MAX) ; } Self :: new (const_try_opt ! (self . get () . checked_div_euclid (rhs))) } # [doc = " Unchecked Euclidean division. Computes `self.div_euclid(rhs)`, assuming that"] # [doc = " `rhs != 0` and that the result is in range."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `self` must not be zero and the result of `self.div_euclid(rhs)` must be in the"] # [doc = " range `MIN..=MAX`."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline (always)] pub const unsafe fn unchecked_div_euclid (self , rhs : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked (unsafe_unwrap_unchecked ! (self . get () . checked_div_euclid (rhs))) } } if_unsigned ! ($ is_signed # [doc = " Remainder. Computes `self % rhs`, statically guaranteeing that the returned value"] # [doc = " is in range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline] pub const fn rem < const RHS_VALUE : $ internal > (self , rhs : $ type < RHS_VALUE , RHS_VALUE >,) -> $ type < 0 , RHS_VALUE > { const { assert ! (MIN <= MAX) ; } unsafe { $ type :: new_unchecked (self . get () % rhs . get ()) } }) ; # [doc = " Checked integer remainder. Computes `self % rhs`, returning `None` if `rhs == 0` or"] # [doc = " if the resulting value is out of range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn checked_rem (self , rhs : $ internal) -> Option < Self > { const { assert ! (MIN <= MAX) ; } Self :: new (const_try_opt ! (self . get () . checked_rem (rhs))) } # [doc = " Unchecked remainder. Computes `self % rhs`, assuming that `rhs != 0` and that the"] # [doc = " result is in range."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `self` must not be zero and the result of `self % rhs` must be in the range"] # [doc = " `MIN..=MAX`."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline (always)] pub const unsafe fn unchecked_rem (self , rhs : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked (unsafe_unwrap_unchecked ! (self . get () . checked_rem (rhs))) } } # [doc = " Checked Euclidean remainder. Computes `self.rem_euclid(rhs)`, returning `None` if"] # [doc = " `rhs == 0` or if the resulting value is out of range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn checked_rem_euclid (self , rhs : $ internal) -> Option < Self > { const { assert ! (MIN <= MAX) ; } Self :: new (const_try_opt ! (self . get () . checked_rem_euclid (rhs))) } # [doc = " Unchecked Euclidean remainder. Computes `self.rem_euclid(rhs)`, assuming that"] # [doc = " `rhs != 0` and that the result is in range."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `self` must not be zero and the result of `self.rem_euclid(rhs)` must be in the"] # [doc = " range `MIN..=MAX`."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline (always)] pub const unsafe fn unchecked_rem_euclid (self , rhs : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked (unsafe_unwrap_unchecked ! (self . get () . checked_rem_euclid (rhs))) } } # [doc = " Checked negation. Computes `-self`, returning `None` if the resulting value is out"] # [doc = " of range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn checked_neg (self) -> Option < Self > { const { assert ! (MIN <= MAX) ; } Self :: new (const_try_opt ! (self . get () . checked_neg ())) } # [doc = " Unchecked negation. Computes `-self`, assuming that `-self` is in range."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The result of `-self` must be in the range `MIN..=MAX`."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline (always)] pub const unsafe fn unchecked_neg (self) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked (unsafe_unwrap_unchecked ! (self . get () . checked_neg ())) } } # [doc = " Negation. Computes `self.neg()`, **failing to compile** if the result is not"] # [doc = " guaranteed to be in range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline (always)] pub const fn neg (self) -> Self { const { assert ! (MIN <= MAX) ; if_signed ! { $ is_signed assert ! (MIN != $ internal :: MIN) ; assert ! (- MIN <= MAX) ; assert ! (- MAX >= MIN) ; } if_unsigned ! { $ is_signed assert ! (MAX == 0) ; } } unsafe { self . unchecked_neg () } } # [doc = " Checked shift left. Computes `self << rhs`, returning `None` if the resulting value"] # [doc = " is out of range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn checked_shl (self , rhs : u32) -> Option < Self > { const { assert ! (MIN <= MAX) ; } Self :: new (const_try_opt ! (self . get () . checked_shl (rhs))) } # [doc = " Unchecked shift left. Computes `self << rhs`, assuming that the result is in range."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The result of `self << rhs` must be in the range `MIN..=MAX`."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline (always)] pub const unsafe fn unchecked_shl (self , rhs : u32) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked (unsafe_unwrap_unchecked ! (self . get () . checked_shl (rhs))) } } # [doc = " Checked shift right. Computes `self >> rhs`, returning `None` if"] # [doc = " the resulting value is out of range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn checked_shr (self , rhs : u32) -> Option < Self > { const { assert ! (MIN <= MAX) ; } Self :: new (const_try_opt ! (self . get () . checked_shr (rhs))) } # [doc = " Unchecked shift right. Computes `self >> rhs`, assuming that the result is in range."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The result of `self >> rhs` must be in the range `MIN..=MAX`."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline (always)] pub const unsafe fn unchecked_shr (self , rhs : u32) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked (unsafe_unwrap_unchecked ! (self . get () . checked_shr (rhs))) } } if_signed ! ($ is_signed # [doc = " Checked absolute value. Computes `self.abs()`, returning `None` if the resulting"] # [doc = " value is out of range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn checked_abs (self) -> Option < Self > { const { assert ! (MIN <= MAX) ; } Self :: new (const_try_opt ! (self . get () . checked_abs ())) } # [doc = " Unchecked absolute value. Computes `self.abs()`, assuming that the result is in"] # [doc = " range."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The result of `self.abs()` must be in the range `MIN..=MAX`."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline (always)] pub const unsafe fn unchecked_abs (self) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked (unsafe_unwrap_unchecked ! (self . get () . checked_abs ())) } } # [doc = " Absolute value. Computes `self.abs()`, **failing to compile** if the result is not"] # [doc = " guaranteed to be in range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline (always)] pub const fn abs (self) -> Self { const { assert ! (MIN <= MAX) ; assert ! (MIN != $ internal :: MIN) ; assert ! (- MIN <= MAX) ; } unsafe { self . unchecked_abs () } }) ; # [doc = " Checked exponentiation. Computes `self.pow(exp)`, returning `None` if the resulting"] # [doc = " value is out of range."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn checked_pow (self , exp : u32) -> Option < Self > { const { assert ! (MIN <= MAX) ; } Self :: new (const_try_opt ! (self . get () . checked_pow (exp))) } # [doc = " Unchecked exponentiation. Computes `self.pow(exp)`, assuming that the result is in"] # [doc = " range."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The result of `self.pow(exp)` must be in the range `MIN..=MAX`."] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline (always)] pub const unsafe fn unchecked_pow (self , exp : u32) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked (unsafe_unwrap_unchecked ! (self . get () . checked_pow (exp))) } } # [doc = " Saturating integer addition. Computes `self + rhs`, saturating at the numeric"] # [doc = " bounds."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn saturating_add (self , rhs : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } Self :: new_saturating (self . get () . saturating_add (rhs)) } # [doc = " Saturating integer subtraction. Computes `self - rhs`, saturating at the numeric"] # [doc = " bounds."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn saturating_sub (self , rhs : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } Self :: new_saturating (self . get () . saturating_sub (rhs)) } if_signed ! ($ is_signed # [doc = " Saturating integer negation. Computes `self - rhs`, saturating at the numeric"] # [doc = " bounds."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn saturating_neg (self) -> Self { const { assert ! (MIN <= MAX) ; } Self :: new_saturating (self . get () . saturating_neg ()) }) ; if_signed ! ($ is_signed # [doc = " Saturating absolute value. Computes `self.abs()`, saturating at the numeric bounds."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn saturating_abs (self) -> Self { const { assert ! (MIN <= MAX) ; } Self :: new_saturating (self . get () . saturating_abs ()) }) ; # [doc = " Saturating integer multiplication. Computes `self * rhs`, saturating at the numeric"] # [doc = " bounds."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn saturating_mul (self , rhs : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } Self :: new_saturating (self . get () . saturating_mul (rhs)) } # [doc = " Saturating integer exponentiation. Computes `self.pow(exp)`, saturating at the"] # [doc = " numeric bounds."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] pub const fn saturating_pow (self , exp : u32) -> Self { const { assert ! (MIN <= MAX) ; } Self :: new_saturating (self . get () . saturating_pow (exp)) } if_signed ! { $ is_signed # [doc = " Returns `true` if the number is positive and `false` if the number is zero or"] # [doc = " negative."] # [inline] pub const fn is_positive (self) -> bool { const { assert ! (MIN <= MAX) ; } self . get () . is_positive () } # [doc = " Returns `true` if the number is negative and `false` if the number is zero or"] # [doc = " positive."] # [inline] pub const fn is_negative (self) -> bool { const { assert ! (MIN <= MAX) ; } self . get () . is_negative () } } # [doc = " Compute the `rem_euclid` of this type with its unsigned type equivalent"] # [must_use = "this returns the result of the operation, without modifying the original"] # [track_caller] # [inline] # [allow (trivial_numeric_casts)] const fn rem_euclid_unsigned (rhs : $ internal , range_len : $ unsigned_type) -> $ unsigned_type { # [allow (unused_comparisons)] if rhs >= 0 { (rhs as $ unsigned_type) % range_len } else { let rhs_abs = ($ internal :: wrapping_sub (0 , rhs)) as $ unsigned_type ; ((($ unsigned_type :: MAX / range_len) * range_len) - (rhs_abs)) % range_len } } # [doc = " Wrapping integer addition. Computes `self + rhs`, wrapping around the numeric"] # [doc = " bounds."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] # [allow (trivial_numeric_casts)] pub const fn wrapping_add (self , rhs : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } if MIN == $ internal :: MIN && MAX == $ internal :: MAX { return unsafe { Self :: new_unchecked (self . get () . wrapping_add (rhs)) } } let inner = self . get () ; let range_len = MAX . abs_diff (MIN) + 1 ; let offset = Self :: rem_euclid_unsigned (rhs , range_len) ; let greater_vals = MAX . abs_diff (inner) ; if offset <= greater_vals { unsafe { Self :: new_unchecked (((inner as $ unsigned_type) . wrapping_add (offset)) as $ internal) } } else { unsafe { Self :: new_unchecked (((MIN as $ unsigned_type) . wrapping_add (offset - (greater_vals + 1))) as $ internal) } } } # [doc = " Wrapping integer subtraction. Computes `self - rhs`, wrapping around the numeric"] # [doc = " bounds."] # [must_use = "this returns the result of the operation, without modifying the original"] # [inline] # [allow (trivial_numeric_casts)] pub const fn wrapping_sub (self , rhs : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } if MIN == $ internal :: MIN && MAX == $ internal :: MAX { return unsafe { Self :: new_unchecked (self . get () . wrapping_sub (rhs)) } } let inner = self . get () ; let range_len = MAX . abs_diff (MIN) + 1 ; let offset = Self :: rem_euclid_unsigned (rhs , range_len) ; let lesser_vals = MIN . abs_diff (inner) ; if offset <= lesser_vals { unsafe { Self :: new_unchecked (((inner as $ unsigned_type) . wrapping_sub (offset)) as $ internal) } } else { unsafe { Self :: new_unchecked (((MAX as $ unsigned_type) . wrapping_sub (offset - (lesser_vals + 1))) as $ internal) } } } } impl < const MIN : $ internal , const MAX : $ internal > $ optional_type < MIN , MAX > { # [doc = " The value used as the niche. Must not be in the range `MIN..=MAX`."] const NICHE : $ internal = match (MIN , MAX) { ($ internal :: MIN , $ internal :: MAX) => panic ! ("type has no niche") , ($ internal :: MIN , _) => $ internal :: MAX , (_ , _) => $ internal :: MIN , } ; # [doc = " An optional ranged value that is not present."] # [allow (non_upper_case_globals)] pub const None : Self = Self (Self :: NICHE) ; # [doc = " Creates an optional ranged value that is present."] # [allow (non_snake_case)] # [inline (always)] pub const fn Some (value : $ type < MIN , MAX >) -> Self { const { assert ! (MIN <= MAX) ; } Self (value . get ()) } # [doc = " Returns the value as the standard library's [`Option`] type."] # [inline (always)] pub const fn get (self) -> Option <$ type < MIN , MAX >> { const { assert ! (MIN <= MAX) ; } if self . 0 == Self :: NICHE { None } else { Some (unsafe { $ type :: new_unchecked (self . 0) }) } } # [doc = " Creates an optional ranged integer without checking the value."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The value must be within the range `MIN..=MAX`. As the value used for niche"] # [doc = " value optimization is unspecified, the provided value must not be the niche"] # [doc = " value."] # [inline (always)] # [track_caller] pub const unsafe fn some_unchecked (value : $ internal) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { assert_unchecked (MIN <= value && value <= MAX) } ; Self (value) } # [doc = " Obtain the inner value of the struct. This is useful for comparisons."] # [inline (always)] pub (crate) const fn inner (self) -> $ internal { const { assert ! (MIN <= MAX) ; } self . 0 } # [doc = " Obtain the value of the struct as an `Option` of the primitive type."] # [doc = ""] # [doc = " A call to this function will output a hint to the compiler that the value is in"] # [doc = " range. In general this will help the optimizer to generate better code, but in edge"] # [doc = " cases this may lead to worse code generation. To avoid outputting the hint, you can"] # [doc = concat ! ("use [`" , stringify ! ($ optional_type) , "::get_primitive_without_hint`].")] # [inline (always)] pub const fn get_primitive (self) -> Option <$ internal > { const { assert ! (MIN <= MAX) ; } Some (const_try_opt ! (self . get ()) . get ()) } # [doc = " Obtain the value of the struct as an `Option` of the primitive type."] # [doc = ""] # [doc = concat ! ("The returned value is identical to [`" , stringify ! ($ optional_type) , "::" , "get_primitive`].")] # [doc = " Unlike `get_primitive`, no hints are output to the compiler indicating the range"] # [doc = " that the value is in. Depending on the scenario, this may with be helpful or harmful"] # [doc = " too optimization."] # [inline (always)] pub const fn get_primitive_without_hint (self) -> Option <$ internal > { const { assert ! (MIN <= MAX) ; } Some (const_try_opt ! (self . get ()) . get_without_hint ()) } # [doc = " Returns `true` if the value is the niche value."] # [inline (always)] pub const fn is_none (& self) -> bool { const { assert ! (MIN <= MAX) ; } self . get () . is_none () } # [doc = " Returns `true` if the value is not the niche value."] # [inline (always)] pub const fn is_some (& self) -> bool { const { assert ! (MIN <= MAX) ; } self . get () . is_some () } } impl < const MIN : $ internal , const MAX : $ internal > fmt :: Debug for $ type < MIN , MAX > { # [inline (always)] fn fmt (& self , f : & mut fmt :: Formatter <'_ >) -> fmt :: Result { const { assert ! (MIN <= MAX) ; } self . get () . fmt (f) } } impl < const MIN : $ internal , const MAX : $ internal > fmt :: Debug for $ optional_type < MIN , MAX > { # [inline (always)] fn fmt (& self , f : & mut fmt :: Formatter <'_ >) -> fmt :: Result { const { assert ! (MIN <= MAX) ; } self . get () . fmt (f) } } impl < const MIN : $ internal , const MAX : $ internal > fmt :: Display for $ type < MIN , MAX > { # [inline (always)] fn fmt (& self , f : & mut fmt :: Formatter <'_ >) -> fmt :: Result { const { assert ! (MIN <= MAX) ; } self . get () . fmt (f) } } # [cfg (feature = "powerfmt")] impl < const MIN : $ internal , const MAX : $ internal , > smart_display :: SmartDisplay for $ type < MIN , MAX > { type Metadata = <$ internal as smart_display :: SmartDisplay >:: Metadata ; # [inline (always)] fn metadata (& self , f : smart_display :: FormatterOptions ,) -> smart_display :: Metadata <'_ , Self > { const { assert ! (MIN <= MAX) ; } self . get_ref () . metadata (f) . reuse () } # [inline (always)] fn fmt_with_metadata (& self , f : & mut fmt :: Formatter <'_ >, metadata : smart_display :: Metadata <'_ , Self >,) -> fmt :: Result { const { assert ! (MIN <= MAX) ; } self . get () . fmt_with_metadata (f , metadata . reuse ()) } } impl < const MIN : $ internal , const MAX : $ internal > Default for $ optional_type < MIN , MAX > { # [inline (always)] fn default () -> Self { const { assert ! (MIN <= MAX) ; } Self :: None } } impl < const MIN : $ internal , const MAX : $ internal > AsRef <$ internal > for $ type < MIN , MAX > { # [inline (always)] fn as_ref (& self) -> &$ internal { const { assert ! (MIN <= MAX) ; } & self . get_ref () } } impl < const MIN : $ internal , const MAX : $ internal > Borrow <$ internal > for $ type < MIN , MAX > { # [inline (always)] fn borrow (& self) -> &$ internal { const { assert ! (MIN <= MAX) ; } & self . get_ref () } } impl < const MIN_A : $ internal , const MAX_A : $ internal , const MIN_B : $ internal , const MAX_B : $ internal , > PartialEq <$ type < MIN_B , MAX_B >> for $ type < MIN_A , MAX_A > { # [inline (always)] fn eq (& self , other : &$ type < MIN_B , MAX_B >) -> bool { const { assert ! (MIN_A <= MAX_A) ; assert ! (MIN_B <= MAX_B) ; } self . get () == other . get () } } impl < const MIN_A : $ internal , const MAX_A : $ internal , const MIN_B : $ internal , const MAX_B : $ internal , > PartialEq <$ optional_type < MIN_B , MAX_B >> for $ optional_type < MIN_A , MAX_A > { # [inline (always)] fn eq (& self , other : &$ optional_type < MIN_B , MAX_B >) -> bool { const { assert ! (MIN_A <= MAX_A) ; assert ! (MIN_B <= MAX_B) ; } self . inner () == other . inner () } } impl < const MIN_A : $ internal , const MAX_A : $ internal , const MIN_B : $ internal , const MAX_B : $ internal , > PartialOrd <$ type < MIN_B , MAX_B >> for $ type < MIN_A , MAX_A > { # [inline (always)] fn partial_cmp (& self , other : &$ type < MIN_B , MAX_B >) -> Option < Ordering > { const { assert ! (MIN_A <= MAX_A) ; assert ! (MIN_B <= MAX_B) ; } self . get () . partial_cmp (& other . get ()) } } impl < const MIN_A : $ internal , const MAX_A : $ internal , const MIN_B : $ internal , const MAX_B : $ internal , > PartialOrd <$ optional_type < MIN_B , MAX_B >> for $ optional_type < MIN_A , MAX_A > { # [inline] fn partial_cmp (& self , other : &$ optional_type < MIN_B , MAX_B >) -> Option < Ordering > { const { assert ! (MIN_A <= MAX_A) ; assert ! (MIN_B <= MAX_B) ; } if self . is_none () && other . is_none () { Some (Ordering :: Equal) } else if self . is_none () { Some (Ordering :: Less) } else if other . is_none () { Some (Ordering :: Greater) } else { self . inner () . partial_cmp (& other . inner ()) } } } impl < const MIN : $ internal , const MAX : $ internal , > Ord for $ optional_type < MIN , MAX > { # [inline] fn cmp (& self , other : & Self) -> Ordering { const { assert ! (MIN <= MAX) ; } if self . is_none () && other . is_none () { Ordering :: Equal } else if self . is_none () { Ordering :: Less } else if other . is_none () { Ordering :: Greater } else { self . inner () . cmp (& other . inner ()) } } } impl < const MIN : $ internal , const MAX : $ internal > fmt :: Binary for $ type < MIN , MAX > { # [inline (always)] fn fmt (& self , f : & mut fmt :: Formatter <'_ >) -> fmt :: Result { const { assert ! (MIN <= MAX) ; } self . get () . fmt (f) } } impl < const MIN : $ internal , const MAX : $ internal > fmt :: LowerHex for $ type < MIN , MAX > { # [inline (always)] fn fmt (& self , f : & mut fmt :: Formatter <'_ >) -> fmt :: Result { const { assert ! (MIN <= MAX) ; } self . get () . fmt (f) } } impl < const MIN : $ internal , const MAX : $ internal > fmt :: UpperHex for $ type < MIN , MAX > { # [inline (always)] fn fmt (& self , f : & mut fmt :: Formatter <'_ >) -> fmt :: Result { const { assert ! (MIN <= MAX) ; } self . get () . fmt (f) } } impl < const MIN : $ internal , const MAX : $ internal > fmt :: LowerExp for $ type < MIN , MAX > { # [inline (always)] fn fmt (& self , f : & mut fmt :: Formatter <'_ >) -> fmt :: Result { const { assert ! (MIN <= MAX) ; } self . get () . fmt (f) } } impl < const MIN : $ internal , const MAX : $ internal > fmt :: UpperExp for $ type < MIN , MAX > { # [inline (always)] fn fmt (& self , f : & mut fmt :: Formatter <'_ >) -> fmt :: Result { const { assert ! (MIN <= MAX) ; } self . get () . fmt (f) } } impl < const MIN : $ internal , const MAX : $ internal > fmt :: Octal for $ type < MIN , MAX > { # [inline (always)] fn fmt (& self , f : & mut fmt :: Formatter <'_ >) -> fmt :: Result { const { assert ! (MIN <= MAX) ; } self . get () . fmt (f) } } if_unsigned ! { $ is_signed impl From < NonZero <$ internal >> for $ type < 1 , { $ internal :: MAX } > { # [inline (always)] fn from (value : NonZero <$ internal >) -> Self { Self :: from_nonzero (value) } } impl From <$ type < 1 , { $ internal :: MAX } >> for NonZero <$ internal > { # [inline (always)] fn from (value : $ type < 1 , { $ internal :: MAX } >) -> Self { value . to_nonzero () } } } impl < const MIN : $ internal , const MAX : $ internal > From <$ type < MIN , MAX >> for $ internal { # [inline (always)] fn from (value : $ type < MIN , MAX >) -> Self { const { assert ! (MIN <= MAX) ; } value . get () } } impl < const MIN : $ internal , const MAX : $ internal , > From <$ type < MIN , MAX >> for $ optional_type < MIN , MAX > { # [inline (always)] fn from (value : $ type < MIN , MAX >) -> Self { const { assert ! (MIN <= MAX) ; } Self :: Some (value) } } impl < const MIN : $ internal , const MAX : $ internal , > From < Option <$ type < MIN , MAX >>> for $ optional_type < MIN , MAX > { # [inline (always)] fn from (value : Option <$ type < MIN , MAX >>) -> Self { const { assert ! (MIN <= MAX) ; } match value { Some (value) => Self :: Some (value) , None => Self :: None , } } } impl < const MIN : $ internal , const MAX : $ internal , > From <$ optional_type < MIN , MAX >> for Option <$ type < MIN , MAX >> { # [inline (always)] fn from (value : $ optional_type < MIN , MAX >) -> Self { const { assert ! (MIN <= MAX) ; } value . get () } } impl < const MIN : $ internal , const MAX : $ internal > TryFrom <$ internal > for $ type < MIN , MAX > { type Error = TryFromIntError ; # [inline] fn try_from (value : $ internal) -> Result < Self , Self :: Error > { const { assert ! (MIN <= MAX) ; } Self :: new (value) . ok_or (TryFromIntError) } } impl < const MIN : $ internal , const MAX : $ internal > FromStr for $ type < MIN , MAX > { type Err = ParseIntError ; # [inline] fn from_str (s : & str) -> Result < Self , Self :: Err > { const { assert ! (MIN <= MAX) ; } let value = s . parse ::<$ internal > () . map_err (| e | ParseIntError { kind : e . kind () . clone () }) ?; if value < MIN { Err (ParseIntError { kind : IntErrorKind :: NegOverflow }) } else if value > MAX { Err (ParseIntError { kind : IntErrorKind :: PosOverflow }) } else { Ok (unsafe { Self :: new_unchecked (value) }) } } } $ (impl < const MIN_SRC : $ from_internal , const MAX_SRC : $ from_internal , const MIN_DST : $ internal , const MAX_DST : $ internal , > From <$ from < MIN_SRC , MAX_SRC >> for $ type < MIN_DST , MAX_DST > { # [inline (always)] # [allow (trivial_numeric_casts , unused_comparisons)] fn from (value : $ from < MIN_SRC , MAX_SRC >) -> Self { const { assert ! (MIN_SRC <= MAX_SRC , "source range is invalid") ; assert ! (MIN_DST <= MAX_DST , "target range is invalid") ; match ($ from_internal :: MIN == 0 , $ internal :: MIN == 0) { (true , true) => { assert ! (MIN_SRC as u128 >= MIN_DST as u128 , "minimum value cannot be represented in the target range") ; assert ! (MAX_SRC as u128 <= MAX_DST as u128 , "maximum value cannot be represented in the target range") ; } (false , false) => { assert ! (MIN_SRC as i128 >= MIN_DST as i128 , "minimum value cannot be represented in the target range") ; assert ! (MAX_SRC as i128 <= MAX_DST as i128 , "maximum value cannot be represented in the target range") ; } (true , false) => { assert ! (MIN_DST < 0 || MIN_SRC as u128 >= MIN_DST as u128 , "minimum value cannot be represented in the target range") ; assert ! (MAX_DST >= 0 && MAX_SRC as u128 <= i128 :: MAX as u128 && MAX_SRC as i128 <= MAX_DST as i128 , "maximum value cannot be represented in the target range") ; } (false , true) => { assert ! (MIN_SRC >= 0 && MIN_SRC as u128 >= MIN_DST as u128 , "minimum value cannot be represented in the target range") ; assert ! (MAX_SRC >= 0 && MAX_SRC as u128 <= MAX_DST as u128 , "maximum value cannot be represented in the target range") ; } } } unsafe { $ type :: new_unchecked (value . get () as $ internal) } } }) + # [cfg (feature = "serde")] impl < const MIN : $ internal , const MAX : $ internal > serde_core :: Serialize for $ type < MIN , MAX > { # [inline (always)] fn serialize < S : serde_core :: Serializer > (& self , serializer : S) -> Result < S :: Ok , S :: Error > { const { assert ! (MIN <= MAX) ; } self . get () . serialize (serializer) } } # [cfg (feature = "serde")] impl < const MIN : $ internal , const MAX : $ internal , > serde_core :: Serialize for $ optional_type < MIN , MAX > { # [inline (always)] fn serialize < S : serde_core :: Serializer > (& self , serializer : S) -> Result < S :: Ok , S :: Error > { const { assert ! (MIN <= MAX) ; } self . get () . serialize (serializer) } } # [cfg (feature = "serde")] impl < 'de , const MIN : $ internal , const MAX : $ internal , > serde_core :: Deserialize <'de > for $ type < MIN , MAX > { # [inline] fn deserialize < D : serde_core :: Deserializer <'de >> (deserializer : D) -> Result < Self , D :: Error > { const { assert ! (MIN <= MAX) ; } let internal = <$ internal >:: deserialize (deserializer) ?; Self :: new (internal) . ok_or_else (|| < D :: Error as serde_core :: de :: Error >:: invalid_value (serde_core :: de :: Unexpected :: Other ("integer") , # [cfg (feature = "alloc")] { & alloc :: format ! ("an integer in the range {}..={}" , MIN , MAX) . as_ref () } , # [cfg (not (feature = "alloc"))] { & "an integer in the valid range" })) } } # [cfg (feature = "serde")] impl < 'de , const MIN : $ internal , const MAX : $ internal , > serde_core :: Deserialize <'de > for $ optional_type < MIN , MAX > { # [inline] fn deserialize < D : serde_core :: Deserializer <'de >> (deserializer : D) -> Result < Self , D :: Error > { const { assert ! (MIN <= MAX) ; } Ok (Self :: Some ($ type ::< MIN , MAX >:: deserialize (deserializer) ?)) } } # [cfg (feature = "rand08")] impl < const MIN : $ internal , const MAX : $ internal , > rand08 :: distributions :: Distribution <$ type < MIN , MAX >> for rand08 :: distributions :: Standard { # [inline] fn sample < R : rand08 :: Rng + ? Sized > (& self , rng : & mut R) -> $ type < MIN , MAX > { const { assert ! (MIN <= MAX) ; } $ type :: new (rng . gen_range (MIN ..= MAX)) . expect ("rand failed to generate a valid value") } } if_not_manual_rand_09 ! { [$ ($ ($ skips) +) ?] # [cfg (feature = "rand09")] impl < const MIN : $ internal , const MAX : $ internal , > rand09 :: distr :: Distribution <$ type < MIN , MAX >> for rand09 :: distr :: StandardUniform { # [inline] fn sample < R : rand09 :: Rng + ? Sized > (& self , rng : & mut R) -> $ type < MIN , MAX > { const { assert ! (MIN <= MAX) ; } $ type :: new (rng . random_range (MIN ..= MAX)) . expect ("rand failed to generate a valid value") } } } # [cfg (feature = "rand08")] impl < const MIN : $ internal , const MAX : $ internal , > rand08 :: distributions :: Distribution <$ optional_type < MIN , MAX >> for rand08 :: distributions :: Standard { # [inline] fn sample < R : rand08 :: Rng + ? Sized > (& self , rng : & mut R) -> $ optional_type < MIN , MAX > { const { assert ! (MIN <= MAX) ; } rng . r#gen ::< Option <$ type < MIN , MAX >>> () . into () } } # [cfg (feature = "rand09")] impl < const MIN : $ internal , const MAX : $ internal , > rand09 :: distr :: Distribution <$ optional_type < MIN , MAX >> for rand09 :: distr :: StandardUniform { # [inline] fn sample < R : rand09 :: Rng + ? Sized > (& self , rng : & mut R) -> $ optional_type < MIN , MAX > { const { assert ! (MIN <= MAX) ; } if rng . random () { $ optional_type :: None } else { $ optional_type :: Some (rng . random ::<$ type < MIN , MAX >> ()) } } } # [cfg (feature = "num")] impl < const MIN : $ internal , const MAX : $ internal > num_traits :: Bounded for $ type < MIN , MAX > { # [inline (always)] fn min_value () -> Self { const { assert ! (MIN <= MAX) ; } Self :: MIN } # [inline (always)] fn max_value () -> Self { const { assert ! (MIN <= MAX) ; } Self :: MAX } } # [cfg (feature = "quickcheck")] impl < const MIN : $ internal , const MAX : $ internal > quickcheck :: Arbitrary for $ type < MIN , MAX > { # [inline] fn arbitrary (g : & mut quickcheck :: Gen) -> Self { const { assert ! (MIN <= MAX) ; } unsafe { Self :: new_unchecked ($ internal :: arbitrary (g) . rem_euclid (MAX - MIN + 1) + MIN) } } # [inline] fn shrink (& self) -> :: alloc :: boxed :: Box < dyn Iterator < Item = Self >> { :: alloc :: boxed :: Box :: new (self . get () . shrink () . filter_map (Self :: new)) } } # [cfg (feature = "quickcheck")] impl < const MIN : $ internal , const MAX : $ internal , > quickcheck :: Arbitrary for $ optional_type < MIN , MAX > { # [inline] fn arbitrary (g : & mut quickcheck :: Gen) -> Self { const { assert ! (MIN <= MAX) ; } Option ::<$ type < MIN , MAX >>:: arbitrary (g) . into () } # [inline] fn shrink (& self) -> :: alloc :: boxed :: Box < dyn Iterator < Item = Self >> { :: alloc :: boxed :: Box :: new (self . get () . shrink () . map (Self :: from)) } }) * } ; }
};
}
