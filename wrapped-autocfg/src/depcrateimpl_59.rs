// Generated macro for impl_59 (impl)
macro_rules! Depcrateimpl_59 {
() => {
// Module: crate
// Provides: {"impl_59"}
// Dependencies: {}
impl AutoCfg { # [doc = " Creates a new `AutoCfg` instance."] # [doc = ""] # [doc = " # Common errors"] # [doc = ""] # [doc = " - `rustc` can't be executed, from `RUSTC` or in the `PATH`."] # [doc = " - The version output from `rustc` can't be parsed."] # [doc = " - `OUT_DIR` is not set in the environment, or is not a writable directory."] # [doc = ""] pub fn new () -> Result < Self , Error > { match env :: var_os ("OUT_DIR") { Some (d) => Self :: with_dir (d) , None => Err (error :: from_str ("no OUT_DIR specified!")) , } } # [doc = " Creates a new `AutoCfg` instance with the specified output directory."] # [doc = ""] # [doc = " # Common errors"] # [doc = ""] # [doc = " - `rustc` can't be executed, from `RUSTC` or in the `PATH`."] # [doc = " - The version output from `rustc` can't be parsed."] # [doc = " - `dir` is not a writable directory."] # [doc = ""] pub fn with_dir < T : Into < PathBuf > > (dir : T) -> Result < Self , Error > { let rustc = Rustc :: new () ; let rustc_version = try ! (rustc . version ()) ; let target = env :: var_os ("TARGET") ; let dir = dir . into () ; let meta = try ! (fs :: metadata (& dir) . map_err (error :: from_io)) ; if ! meta . is_dir () || meta . permissions () . readonly () { return Err (error :: from_str ("output path is not a writable directory")) ; } let mut ac = AutoCfg { rustflags : rustflags (& target , & dir) , out_dir : dir , rustc : rustc , rustc_version : rustc_version , target : target , no_std : false , edition : None , uuid : new_uuid () , } ; if ac . probe_raw ("") . is_err () { if ac . probe_raw ("#![no_std]") . is_ok () { ac . no_std = true ; } else { let warning = b"warning: autocfg could not probe for `std`\n" ; stderr () . write_all (warning) . ok () ; } } Ok (ac) } # [doc = " Returns whether `AutoCfg` is using `#![no_std]` in its probes."] # [doc = ""] # [doc = " This is automatically detected during construction -- if an empty probe"] # [doc = " fails while one with `#![no_std]` succeeds, then the attribute will be"] # [doc = " used for all further probes. This is usually only necessary when the"] # [doc = " `TARGET` lacks `std` altogether. If neither succeeds, `no_std` is not"] # [doc = " set, but that `AutoCfg` will probably only work for version checks."] # [doc = ""] # [doc = " This attribute changes the implicit [prelude] from `std` to `core`,"] # [doc = " which may affect the paths you need to use in other probes. It also"] # [doc = " restricts some types that otherwise get additional methods in `std`,"] # [doc = " like floating-point trigonometry and slice sorting."] # [doc = ""] # [doc = " See also [`set_no_std`](#method.set_no_std)."] # [doc = ""] # [doc = " [prelude]: https://doc.rust-lang.org/reference/names/preludes.html#the-no_std-attribute"] pub fn no_std (& self) -> bool { self . no_std } # [doc = " Sets whether `AutoCfg` should use `#![no_std]` in its probes."] # [doc = ""] # [doc = " See also [`no_std`](#method.no_std)."] pub fn set_no_std (& mut self , no_std : bool) { self . no_std = no_std ; } # [doc = " Returns the `--edition` string that is currently being passed to `rustc`, if any,"] # [doc = " as configured by the [`set_edition`][Self::set_edition] method."] pub fn edition (& self) -> Option < & str > { match self . edition { Some (ref edition) => Some (& * * edition) , None => None , } } # [doc = " Sets the `--edition` string that will be passed to `rustc`,"] # [doc = " or `None` to leave the compiler at its default edition."] # [doc = ""] # [doc = " See also [The Rust Edition Guide](https://doc.rust-lang.org/edition-guide/)."] # [doc = ""] # [doc = " **Warning:** Setting an unsupported edition will likely cause **all** subsequent probes to"] # [doc = " fail! As of this writing, the known editions and their minimum Rust versions are:"] # [doc = ""] # [doc = " | Edition | Version    |"] # [doc = " | ------- | ---------- |"] # [doc = " | 2015    | 1.27.0[^1] |"] # [doc = " | 2018    | 1.31.0     |"] # [doc = " | 2021    | 1.56.0     |"] # [doc = " | 2024    | 1.85.0     |"] # [doc = ""] # [doc = " [^1]: Prior to 1.27.0, Rust was effectively 2015 Edition by default, but the concept hadn't"] # [doc = " been established yet, so the explicit `--edition` flag wasn't supported either."] pub fn set_edition (& mut self , edition : Option < String >) { self . edition = edition ; } # [doc = " Tests whether the current `rustc` reports a version greater than"] # [doc = " or equal to \"`major`.`minor`\"."] pub fn probe_rustc_version (& self , major : usize , minor : usize) -> bool { self . rustc_version >= Version :: new (major , minor , 0) } # [doc = " Sets a `cfg` value of the form `rustc_major_minor`, like `rustc_1_29`,"] # [doc = " if the current `rustc` is at least that version."] pub fn emit_rustc_version (& self , major : usize , minor : usize) { let cfg_flag = format ! ("rustc_{}_{}" , major , minor) ; emit_possibility (& cfg_flag) ; if self . probe_rustc_version (major , minor) { emit (& cfg_flag) ; } } # [doc = " Returns a new (hopefully unique) crate name for probes."] fn new_crate_name (& self) -> String { # [allow (deprecated)] static ID : AtomicUsize = ATOMIC_USIZE_INIT ; let id = ID . fetch_add (1 , Ordering :: Relaxed) ; format ! ("autocfg_{:016x}_{}" , self . uuid , id) } fn probe_fmt < 'a > (& self , source : Arguments < 'a >) -> Result < () , Error > { let crate_name = self . new_crate_name () ; let mut command = self . rustc . command () ; command . arg ("--crate-name") . arg (& crate_name) . arg ("--crate-type=lib") . arg ("--out-dir") . arg (& self . out_dir) . arg ("--emit=llvm-ir") ; if let Some (edition) = self . edition . as_ref () { command . arg ("--edition") . arg (edition) ; } if let Some (target) = self . target . as_ref () { command . arg ("--target") . arg (target) ; } command . args (& self . rustflags) ; command . arg ("-") . stdin (Stdio :: piped ()) ; let mut child = try ! (command . spawn () . map_err (error :: from_io)) ; let mut stdin = child . stdin . take () . expect ("rustc stdin") ; try ! (stdin . write_fmt (source) . map_err (error :: from_io)) ; drop (stdin) ; match child . wait () { Ok (status) if status . success () => { let mut file = self . out_dir . join (crate_name) ; file . set_extension ("ll") ; let _ = fs :: remove_file (file) ; Ok (()) } Ok (status) => Err (error :: from_exit (status)) , Err (error) => Err (error :: from_io (error)) , } } fn probe < 'a > (& self , code : Arguments < 'a >) -> bool { let result = if self . no_std { self . probe_fmt (format_args ! ("#![no_std]\n{}" , code)) } else { self . probe_fmt (code) } ; result . is_ok () } # [doc = " Tests whether the given code can be compiled as a Rust library."] # [doc = ""] # [doc = " This will only return `Ok` if the compiler ran and exited successfully,"] # [doc = " per `ExitStatus::success()`."] # [doc = " The code is passed to the compiler exactly as-is, notably not even"] # [doc = " adding the [`#![no_std]`][Self::no_std] attribute like other probes."] # [doc = ""] # [doc = " Raw probes are useful for testing functionality that's not yet covered"] # [doc = " by the rest of the `AutoCfg` API. For example, the following attribute"] # [doc = " **must** be used at the crate level, so it wouldn't work within the code"] # [doc = " templates used by other `probe_*` methods."] # [doc = ""] # [doc = " ```"] # [doc = " # extern crate autocfg;"] # [doc = " # // Normally, cargo will set `OUT_DIR` for build scripts."] # [doc = " # let exe = std::env::current_exe().unwrap();"] # [doc = " # std::env::set_var(\"OUT_DIR\", exe.parent().unwrap());"] # [doc = " let ac = autocfg::new();"] # [doc = " assert!(ac.probe_raw(\"#![no_builtins]\").is_ok());"] # [doc = " ```"] # [doc = ""] # [doc = " Rust nightly features could be tested as well -- ideally including a"] # [doc = " code sample to ensure the unstable feature still works as expected."] # [doc = " For example, `slice::group_by` was renamed to `chunk_by` when it was"] # [doc = " stabilized, even though the feature name was unchanged, so testing the"] # [doc = " `#![feature(..)]` alone wouldn't reveal that. For larger snippets,"] # [doc = " [`include_str!`] may be useful to load them from separate files."] # [doc = ""] # [doc = " ```"] # [doc = " # extern crate autocfg;"] # [doc = " # // Normally, cargo will set `OUT_DIR` for build scripts."] # [doc = " # let exe = std::env::current_exe().unwrap();"] # [doc = " # std::env::set_var(\"OUT_DIR\", exe.parent().unwrap());"] # [doc = " let ac = autocfg::new();"] # [doc = " let code = r#\""] # [doc = "     #![feature(slice_group_by)]"] # [doc = "     pub fn probe(slice: &[i32]) -> impl Iterator<Item = &[i32]> {"] # [doc = "         slice.group_by(|a, b| a == b)"] # [doc = "     }"] # [doc = " \"#;"] # [doc = " if ac.probe_raw(code).is_ok() {"] # [doc = "     autocfg::emit(\"has_slice_group_by\");"] # [doc = " }"] # [doc = " ```"] pub fn probe_raw (& self , code : & str) -> Result < () , Error > { self . probe_fmt (format_args ! ("{}" , code)) } # [doc = " Tests whether the given sysroot crate can be used."] # [doc = ""] # [doc = " The test code is subject to change, but currently looks like:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " extern crate CRATE as probe;"] # [doc = " ```"] pub fn probe_sysroot_crate (& self , name : & str) -> bool { self . probe (format_args ! ("extern crate {} as probe;" , name)) } # [doc = " Emits a config value `has_CRATE` if `probe_sysroot_crate` returns true."] pub fn emit_sysroot_crate (& self , name : & str) { let cfg_flag = format ! ("has_{}" , mangle (name)) ; emit_possibility (& cfg_flag) ; if self . probe_sysroot_crate (name) { emit (& cfg_flag) ; } } # [doc = " Tests whether the given path can be used."] # [doc = ""] # [doc = " The test code is subject to change, but currently looks like:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " pub use PATH;"] # [doc = " ```"] pub fn probe_path (& self , path : & str) -> bool { self . probe (format_args ! ("pub use {};" , path)) } # [doc = " Emits a config value `has_PATH` if `probe_path` returns true."] # [doc = ""] # [doc = " Any non-identifier characters in the `path` will be replaced with"] # [doc = " `_` in the generated config value."] pub fn emit_has_path (& self , path : & str) { self . emit_path_cfg (path , & format ! ("has_{}" , mangle (path))) ; } # [doc = " Emits the given `cfg` value if `probe_path` returns true."] pub fn emit_path_cfg (& self , path : & str , cfg : & str) { emit_possibility (cfg) ; if self . probe_path (path) { emit (cfg) ; } } # [doc = " Tests whether the given trait can be used."] # [doc = ""] # [doc = " The test code is subject to change, but currently looks like:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " pub trait Probe: TRAIT + Sized {}"] # [doc = " ```"] pub fn probe_trait (& self , name : & str) -> bool { self . probe (format_args ! ("pub trait Probe: {} + Sized {{}}" , name)) } # [doc = " Emits a config value `has_TRAIT` if `probe_trait` returns true."] # [doc = ""] # [doc = " Any non-identifier characters in the trait `name` will be replaced with"] # [doc = " `_` in the generated config value."] pub fn emit_has_trait (& self , name : & str) { self . emit_trait_cfg (name , & format ! ("has_{}" , mangle (name))) ; } # [doc = " Emits the given `cfg` value if `probe_trait` returns true."] pub fn emit_trait_cfg (& self , name : & str , cfg : & str) { emit_possibility (cfg) ; if self . probe_trait (name) { emit (cfg) ; } } # [doc = " Tests whether the given type can be used."] # [doc = ""] # [doc = " The test code is subject to change, but currently looks like:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " pub type Probe = TYPE;"] # [doc = " ```"] pub fn probe_type (& self , name : & str) -> bool { self . probe (format_args ! ("pub type Probe = {};" , name)) } # [doc = " Emits a config value `has_TYPE` if `probe_type` returns true."] # [doc = ""] # [doc = " Any non-identifier characters in the type `name` will be replaced with"] # [doc = " `_` in the generated config value."] pub fn emit_has_type (& self , name : & str) { self . emit_type_cfg (name , & format ! ("has_{}" , mangle (name))) ; } # [doc = " Emits the given `cfg` value if `probe_type` returns true."] pub fn emit_type_cfg (& self , name : & str , cfg : & str) { emit_possibility (cfg) ; if self . probe_type (name) { emit (cfg) ; } } # [doc = " Tests whether the given expression can be used."] # [doc = ""] # [doc = " The test code is subject to change, but currently looks like:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " pub fn probe() { let _ = EXPR; }"] # [doc = " ```"] pub fn probe_expression (& self , expr : & str) -> bool { self . probe (format_args ! ("pub fn probe() {{ let _ = {}; }}" , expr)) } # [doc = " Emits the given `cfg` value if `probe_expression` returns true."] pub fn emit_expression_cfg (& self , expr : & str , cfg : & str) { emit_possibility (cfg) ; if self . probe_expression (expr) { emit (cfg) ; } } # [doc = " Tests whether the given constant expression can be used."] # [doc = ""] # [doc = " The test code is subject to change, but currently looks like:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " pub const PROBE: () = ((), EXPR).0;"] # [doc = " ```"] pub fn probe_constant (& self , expr : & str) -> bool { self . probe (format_args ! ("pub const PROBE: () = ((), {}).0;" , expr)) } # [doc = " Emits the given `cfg` value if `probe_constant` returns true."] pub fn emit_constant_cfg (& self , expr : & str , cfg : & str) { emit_possibility (cfg) ; if self . probe_constant (expr) { emit (cfg) ; } } }
};
}
