// Generated macro for UnicodeTruncateStr (trait)
macro_rules! DepcrateUnicodeTruncateStr {
() => {
// Module: crate
// Provides: {"UnicodeTruncateStr"}
// Dependencies: {}
# [doc = " Methods for padding or truncating using displayed width of Unicode strings."] pub trait UnicodeTruncateStr { # [doc = " Truncates a string to be at most `width` in terms of display width by removing the end"] # [doc = " characters."] # [doc = ""] # [doc = " For wide characters, it may not always be possible to truncate at exact width. In this case,"] # [doc = " the longest possible string is returned. To help the caller determine the situation, the"] # [doc = " display width of the returned string slice is also returned."] # [doc = ""] # [doc = " Zero-width characters decided by [`unicode_width`] are always included when deciding the"] # [doc = " truncation point."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `max_width` - the maximum display width"] fn unicode_truncate (& self , max_width : usize) -> (& str , usize) ; # [doc = " Truncates a string to be at most `width` in terms of display width by removing the start"] # [doc = " characters."] # [doc = ""] # [doc = " For wide characters, it may not always be possible to truncate at exact width. In this case,"] # [doc = " the longest possible string is returned. To help the caller determine the situation, the"] # [doc = " display width of the returned string slice is also returned."] # [doc = ""] # [doc = " Zero-width characters decided by [`unicode_width`] are always removed when deciding the"] # [doc = " truncation point."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `max_width` - the maximum display width"] fn unicode_truncate_start (& self , max_width : usize) -> (& str , usize) ; # [doc = " Truncates a string to be at most `width` in terms of display width by removing"] # [doc = " characters at both start and end."] # [doc = ""] # [doc = " For wide characters, it may not always be possible to truncate at exact width. In this case,"] # [doc = " the longest possible string is returned. To help the caller determine the situation, the"] # [doc = " display width of the returned string slice is also returned."] # [doc = ""] # [doc = " Zero-width characters decided by [`unicode_width`] are included if they are at end, or"] # [doc = " removed if they are at the beginning when deciding the truncation point."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `max_width` - the maximum display width"] fn unicode_truncate_centered (& self , max_width : usize) -> (& str , usize) ; # [doc = " Truncates a string to be at most `width` in terms of display width by removing"] # [doc = " characters."] # [doc = ""] # [doc = " Depending on the alignment characters are removed. When left aligned characters from the end"] # [doc = " are removed. When right aligned characters from the start are removed. When centered"] # [doc = " characters from both sides are removed."] # [doc = ""] # [doc = " For wide characters, it may not always be possible to truncate at exact width. In this case,"] # [doc = " the longest possible string is returned. To help the caller determine the situation, the"] # [doc = " display width of the returned string slice is also returned."] # [doc = ""] # [doc = " Zero-width characters decided by [`unicode_width`] are included if they are at end, or"] # [doc = " removed if they are at the beginning when deciding the truncation point."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `max_width` - the maximum display width"] # [doc = " * `align` - alignment for truncation"] # [inline] fn unicode_truncate_aligned (& self , max_width : usize , align : Alignment) -> (& str , usize) { match align { Alignment :: Left => self . unicode_truncate (max_width) , Alignment :: Center => self . unicode_truncate_centered (max_width) , Alignment :: Right => self . unicode_truncate_start (max_width) , } } # [doc = " Pads a string to be `width` in terms of display width. Only available when the `std` feature"] # [doc = " of this library is activated, and it is activated by default."] # [doc = ""] # [doc = " When `truncate` is true, the string is truncated to `width` if necessary. In case of wide"] # [doc = " characters and truncation point not at character boundary, the longest possible string is"] # [doc = " used, and padded to exact `width` according to `align`."] # [doc = " See [`unicode_truncate`](crate::UnicodeTruncateStr::unicode_truncate) for the behavior of"] # [doc = " truncation."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `target_width` - the display width to pad to"] # [doc = " * `align` - alignment for truncation and padding"] # [doc = " * `truncate` - whether to truncate string if necessary"] # [cfg (feature = "std")] fn unicode_pad (& self , target_width : usize , align : Alignment , truncate : bool ,) -> std :: borrow :: Cow < '_ , str > ; }
};
}
