// Generated macro for BuildFn (struct)
macro_rules! Depcrate_macro_optionsBuildFn {
() => {
// Module: crate::macro_options
// Provides: {"BuildFn"}
// Dependencies: {}
# [doc = " Options for the `build_fn` property in struct-level builder options."] # [derive (Debug , Clone , FromMeta)] # [darling (default , and_then = Self :: validation_needs_error)] pub struct BuildFn { # [doc = " If `true`, then no build method will be generated."] # [doc = ""] # [doc = " Using this is rare - it's more common to use `#[builder(build_fn(private))]`"] # [doc = " to hide the generated method and to then manually add a public build method"] # [doc = " that calls the private one."] skip : bool , # [doc = " The name of the build method. Defaults to `build`."] name : Ident , # [doc = " The path of a function that will be called with a reference to the builder for"] # [doc = " validation before constructing the target type."] # [doc = ""] # [doc = " For a type `ExampleBuilder`, the signature of this function must be"] # [doc = " `(&ExampleBuilder) -> Result<(), impl Into<ExampleBuilderError>>`."] # [doc = ""] # [doc = " Generated builder errors include a `From<String>` impl, so returning a `Result<(), String>`"] # [doc = " will work without declaring any additional types or `impl` blocks."] validate : Option < Path > , # [darling (flatten)] visibility : VisibilityAttr , # [doc = " Either the path to an existing error type that the build method should return or a meta"] # [doc = " list of options to modify the generated error."] # [doc = ""] # [doc = " Setting this to a path will prevent `derive_builder` from generating an error type for the"] # [doc = " build method."] # [doc = ""] # [doc = " This options supports to formats: path `error = \"path::to::Error\"` and meta list"] # [doc = " `error(<options>)`. Supported mata list options are the following:"] # [doc = ""] # [doc = " * `validation_error = bool` - Whether to generate `ValidationError(String)` as a variant"] # [doc = "   of the build error type. Setting this to `false` will prevent `derive_builder` from"] # [doc = "   using the `validate` function but this also means it does not generate any usage of the"] # [doc = "   `alloc` crate (useful when disabling the `alloc` feature in `no_std`)."] # [doc = ""] # [doc = " # Type Bounds for Custom Error"] # [doc = " This type's bounds depend on other settings of the builder."] # [doc = ""] # [doc = " * If uninitialized fields cause `build()` to fail, then this type"] # [doc = "   must `impl From<UninitializedFieldError>`. Uninitialized fields do not cause errors"] # [doc = "   when default values are provided for every field or at the struct level."] # [doc = " * If `validate` is specified, then this type must provide a conversion from the specified"] # [doc = "   function's error type."] error : Option < BuildFnError > , }
};
}
