// Generated macro for from_tree (function)
macro_rules! Depcrate_from_treefrom_tree {
() => {
// Module: crate::from_tree
// Provides: {"from_tree"}
// Dependencies: {}
# [doc = " Use `objects` to traverse `tree` and fetch the contained blobs to return as [`Stream`], which makes them queryable"] # [doc = " on demand with support for streaming each entry."] # [doc = ""] # [doc = " `pipeline` is used to convert blobs to their worktree representation, and `attributes` is used to read"] # [doc = " the `export-ignore` attribute. If set on a directory or blob, it won't be added to the archive."] # [doc = ""] # [doc = " ### Types of entries in stream"] # [doc = ""] # [doc = " We only return blobs (with or without executable), which may be symlinks in which case their content will"] # [doc = " be target of the symlink."] # [doc = " Directories are never returned, but maybe added by the caller via [Stream::add_entry()]."] # [doc = ""] # [doc = " ### Progress and interruptions"] # [doc = ""] # [doc = " For per-file progress, integrate progress handling into the calls of [`Stream::next_entry()`] as that"] # [doc = " correlates blobs."] # [doc = " Additional interrupt handling can be wrapped around the `Read` implementation of each [`Entry`][crate::Entry]."] # [doc = " For progress on bytes-written, integrate progress reporting when consuming the stream."] # [doc = " Further it's possible to drop the returned [`Stream`] to halt all operation."] # [doc = ""] # [doc = " ### Threaded Operation"] # [doc = ""] # [doc = " This function spawns a thread that will access the tree data in the background, synchronized through"] # [doc = " `Stream` so that it will not be faster than the consumer, with at most one file in flight at any time."] # [doc = ""] # [doc = " ### Limitations"] # [doc = ""] # [doc = " * `export-subst` is not support, as it requires the entire formatting engine of `git log`."] pub fn from_tree < Find , E > (tree : gix_hash :: ObjectId , objects : Find , pipeline : gix_filter :: Pipeline , attributes : impl FnMut (& BStr , gix_object :: tree :: EntryMode , & mut gix_attributes :: search :: Outcome) -> Result < () , E > + Send + 'static ,) -> Stream where Find : gix_object :: Find + Clone + Send + 'static , E : std :: error :: Error + Send + Sync + 'static , { let (stream , mut write , additional_entries) = Stream :: new () ; std :: thread :: spawn ({ let slot = stream . err . clone () ; move | | { if let Err (err) = run (tree , objects , pipeline , attributes , & mut write , slot . clone () , additional_entries ,) { { let mut slot = slot . lock () ; if slot . is_none () { * slot = Some (err) ; } else { drop (slot) ; write . channel . send (Err (std :: io :: Error :: other (err))) . ok () ; } } } } }) ; stream }
};
}
