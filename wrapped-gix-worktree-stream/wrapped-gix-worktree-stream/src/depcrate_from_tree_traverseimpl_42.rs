// Generated macro for impl_42 (impl)
macro_rules! Depcrate_from_tree_traverseimpl_42 {
() => {
// Module: crate::from_tree::traverse
// Provides: {"impl_42"}
// Dependencies: {}
impl < AttributesFn , Find > Delegate < '_ , AttributesFn , Find > where Find : gix_object :: Find , AttributesFn : FnMut (& BStr , gix_object :: tree :: EntryMode , & mut gix_attributes :: search :: Outcome) -> Result < () , Error > + 'static , { fn pop_element (& mut self) { if let Some (pos) = self . path . rfind_byte (b'/') { self . path . resize (pos , 0) ; } else { self . path . clear () ; } } fn push_element (& mut self , name : & BStr) { if name . is_empty () { return ; } if ! self . path . is_empty () { self . path . push (b'/') ; } self . path . push_str (name) ; } # [doc = " Return the state of the `export-ignore` attribute."] fn ignore_state (& self) -> gix_attributes :: StateRef < '_ > { self . attrs . iter_selected () . next () . expect ("initialized with one attr") . assignment . state } fn handle_entry (& mut self , entry : & tree :: EntryRef < '_ >) -> Result < Action , Error > { if ! entry . mode . is_blob_or_symlink () { return Ok (Action :: Continue) ; } (self . fetch_attributes) (self . path . as_ref () , entry . mode , & mut self . attrs) ? ; if self . ignore_state () . is_set () { return Ok (Action :: Continue) ; } self . objects . find (entry . oid , & mut self . buf) ? ; self . pipeline . driver_context_mut () . blob = Some (entry . oid . into ()) ; let converted = self . pipeline . convert_to_worktree (& self . buf , self . path . as_ref () , & mut | a , b | { (self . fetch_attributes) (a , entry . mode , b) . ok () ; } , gix_filter :: driver :: apply :: Delay :: Forbid ,) ? ; # [allow (clippy :: unused_io_amount)] match converted { ToWorktreeOutcome :: Unchanged (buf) | ToWorktreeOutcome :: Buffer (buf) => { protocol :: write_entry_header_and_path (self . path . as_ref () , entry . oid , entry . mode , Some (buf . len ()) , self . out ,) ? ; self . out . write (buf) ? ; } ToWorktreeOutcome :: Process (MaybeDelayed :: Immediate (read)) => { protocol :: write_entry_header_and_path (self . path . as_ref () , entry . oid , entry . mode , None , self . out) ? ; protocol :: write_stream (& mut self . buf , read , self . out) ? ; } ToWorktreeOutcome :: Process (MaybeDelayed :: Delayed (_)) => { unreachable ! ("we forbade it") } } Ok (Action :: Continue) } }
};
}
