// Generated macro for Worker (struct)
macro_rules! Depcrate_walkWorker {
() => {
// Module: crate::walk
// Provides: {"Worker"}
// Dependencies: {}
# [doc = " A worker is responsible for descending into directories, updating the"] # [doc = " ignore matchers, producing new work and invoking the caller's callback."] # [doc = ""] # [doc = " Note that a worker is *both* a producer and a consumer."] struct Worker < 's > { # [doc = " The caller's callback."] visitor : Box < dyn ParallelVisitor + 's > , # [doc = " A work-stealing stack of work to do."] # [doc = ""] # [doc = " We use a stack instead of a channel because a stack lets us visit"] # [doc = " directories in depth first order. This can substantially reduce peak"] # [doc = " memory usage by keeping both the number of file paths and gitignore"] # [doc = " matchers in memory lower."] stack : Stack , # [doc = " Whether all workers should terminate at the next opportunity. Note"] # [doc = " that we need this because we don't want other `Work` to be done after"] # [doc = " we quit. We wouldn't need this if have a priority channel."] quit_now : Arc < AtomicBool > , # [doc = " The number of currently active workers."] active_workers : Arc < AtomicUsize > , # [doc = " The maximum depth of directories to descend. A value of `0` means no"] # [doc = " descension at all."] max_depth : Option < usize > , # [doc = " The minimum depth of directories to descend."] min_depth : Option < usize > , # [doc = " The maximum size a searched file can be (in bytes). If a file exceeds"] # [doc = " this size it will be skipped."] max_filesize : Option < u64 > , # [doc = " Whether to follow symbolic links or not. When this is enabled, loop"] # [doc = " detection is performed."] follow_links : bool , # [doc = " A file handle to skip, currently is either `None` or stdout, if it's"] # [doc = " a file and it has been requested to skip files identical to stdout."] skip : Option < Arc < Handle > > , # [doc = " A predicate applied to dir entries. If true, the entry and all"] # [doc = " children will be skipped."] filter : Option < Filter > , }
};
}
