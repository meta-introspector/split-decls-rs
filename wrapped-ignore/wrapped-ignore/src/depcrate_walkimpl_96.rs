// Generated macro for impl_96 (impl)
macro_rules! Depcrate_walkimpl_96 {
() => {
// Module: crate::walk
// Provides: {"impl_96"}
// Dependencies: {}
impl WalkBuilder { # [doc = " Create a new builder for a recursive directory iterator for the"] # [doc = " directory given."] # [doc = ""] # [doc = " Note that if you want to traverse multiple different directories, it"] # [doc = " is better to call `add` on this builder than to create multiple"] # [doc = " `Walk` values."] pub fn new < P : AsRef < Path > > (path : P) -> WalkBuilder { WalkBuilder { paths : vec ! [path . as_ref () . to_path_buf ()] , ig_builder : IgnoreBuilder :: new () , max_depth : None , min_depth : None , max_filesize : None , follow_links : false , same_file_system : false , sorter : None , threads : 0 , skip : None , filter : None , global_gitignores_relative_to : OnceLock :: new () , } } # [doc = " Build a new `Walk` iterator."] pub fn build (& self) -> Walk { let follow_links = self . follow_links ; let max_depth = self . max_depth ; let min_depth = self . min_depth ; let sorter = self . sorter . clone () ; let its = self . paths . iter () . map (move | p | { if p == Path :: new ("-") { (p . to_path_buf () , None) } else { let mut wd = WalkDir :: new (p) ; wd = wd . follow_links (follow_links || p . is_file ()) ; wd = wd . same_file_system (self . same_file_system) ; if let Some (max_depth) = max_depth { wd = wd . max_depth (max_depth) ; } if let Some (min_depth) = min_depth { wd = wd . min_depth (min_depth) ; } if let Some (ref sorter) = sorter { match sorter . clone () { Sorter :: ByName (cmp) => { wd = wd . sort_by (move | a , b | { cmp (a . file_name () , b . file_name ()) }) ; } Sorter :: ByPath (cmp) => { wd = wd . sort_by (move | a , b | { cmp (a . path () , b . path ()) }) ; } } } (p . to_path_buf () , Some (WalkEventIter :: from (wd))) } }) . collect :: < Vec < _ > > () . into_iter () ; let ig_root = self . get_or_set_current_dir () . map (| cwd | self . ig_builder . build_with_cwd (Some (cwd . to_path_buf ()))) . unwrap_or_else (| | self . ig_builder . build ()) ; Walk { its , it : None , ig_root : ig_root . clone () , ig : ig_root . clone () , max_filesize : self . max_filesize , skip : self . skip . clone () , filter : self . filter . clone () , } } # [doc = " Build a new `WalkParallel` iterator."] # [doc = ""] # [doc = " Note that this *doesn't* return something that implements `Iterator`."] # [doc = " Instead, the returned value must be run with a closure. e.g.,"] # [doc = " `builder.build_parallel().run(|| |path| { println!(\"{path:?}\"); WalkState::Continue })`."] pub fn build_parallel (& self) -> WalkParallel { let ig_root = self . get_or_set_current_dir () . map (| cwd | self . ig_builder . build_with_cwd (Some (cwd . to_path_buf ()))) . unwrap_or_else (| | self . ig_builder . build ()) ; WalkParallel { paths : self . paths . clone () . into_iter () , ig_root , max_depth : self . max_depth , min_depth : self . min_depth , max_filesize : self . max_filesize , follow_links : self . follow_links , same_file_system : self . same_file_system , threads : self . threads , skip : self . skip . clone () , filter : self . filter . clone () , } } # [doc = " Add a file path to the iterator."] # [doc = ""] # [doc = " Each additional file path added is traversed recursively. This should"] # [doc = " be preferred over building multiple `Walk` iterators since this"] # [doc = " enables reusing resources across iteration."] pub fn add < P : AsRef < Path > > (& mut self , path : P) -> & mut WalkBuilder { self . paths . push (path . as_ref () . to_path_buf ()) ; self } # [doc = " The maximum depth to recurse."] # [doc = ""] # [doc = " The default, `None`, imposes no depth restriction."] pub fn max_depth (& mut self , depth : Option < usize >) -> & mut WalkBuilder { self . max_depth = depth ; if self . min_depth . is_some () && self . max_depth . is_some () && self . max_depth < self . min_depth { self . max_depth = self . min_depth ; } self } # [doc = " The minimum depth to recurse."] # [doc = ""] # [doc = " The default, `None`, imposes no minimum depth restriction."] pub fn min_depth (& mut self , depth : Option < usize >) -> & mut WalkBuilder { self . min_depth = depth ; if self . max_depth . is_some () && self . min_depth . is_some () && self . min_depth > self . max_depth { self . min_depth = self . max_depth ; } self } # [doc = " Whether to follow symbolic links or not."] pub fn follow_links (& mut self , yes : bool) -> & mut WalkBuilder { self . follow_links = yes ; self } # [doc = " Whether to ignore files above the specified limit."] pub fn max_filesize (& mut self , filesize : Option < u64 >) -> & mut WalkBuilder { self . max_filesize = filesize ; self } # [doc = " The number of threads to use for traversal."] # [doc = ""] # [doc = " Note that this only has an effect when using `build_parallel`."] # [doc = ""] # [doc = " The default setting is `0`, which chooses the number of threads"] # [doc = " automatically using heuristics."] pub fn threads (& mut self , n : usize) -> & mut WalkBuilder { self . threads = n ; self } # [doc = " Add a global ignore file to the matcher."] # [doc = ""] # [doc = " This has lower precedence than all other sources of ignore rules."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If there was a problem adding the ignore file, then an error is"] # [doc = " returned. Note that the error may indicate *partial* failure. For"] # [doc = " example, if an ignore file contains an invalid glob, all other globs"] # [doc = " are still applied."] # [doc = ""] # [doc = " An error will also occur if this walker could not get the current"] # [doc = " working directory (and `WalkBuilder::current_dir` isn't set)."] pub fn add_ignore < P : AsRef < Path > > (& mut self , path : P) -> Option < Error > { let path = path . as_ref () ; let Some (cwd) = self . get_or_set_current_dir () else { let err = std :: io :: Error :: other (format ! ("CWD is not known, ignoring global gitignore {}" , path . display ())) ; return Some (err . into ()) ; } ; let mut builder = GitignoreBuilder :: new (cwd) ; let mut errs = PartialErrorBuilder :: default () ; errs . maybe_push (builder . add (path)) ; match builder . build () { Ok (gi) => { self . ig_builder . add_ignore (gi) ; } Err (err) => { errs . push (err) ; } } errs . into_error_option () } # [doc = " Add a custom ignore file name"] # [doc = ""] # [doc = " These ignore files have higher precedence than all other ignore files."] # [doc = ""] # [doc = " When specifying multiple names, earlier names have lower precedence than"] # [doc = " later names."] pub fn add_custom_ignore_filename < S : AsRef < OsStr > > (& mut self , file_name : S ,) -> & mut WalkBuilder { self . ig_builder . add_custom_ignore_filename (file_name) ; self } # [doc = " Add an override matcher."] # [doc = ""] # [doc = " By default, no override matcher is used."] # [doc = ""] # [doc = " This overrides any previous setting."] pub fn overrides (& mut self , overrides : Override) -> & mut WalkBuilder { self . ig_builder . overrides (overrides) ; self } # [doc = " Add a file type matcher."] # [doc = ""] # [doc = " By default, no file type matcher is used."] # [doc = ""] # [doc = " This overrides any previous setting."] pub fn types (& mut self , types : Types) -> & mut WalkBuilder { self . ig_builder . types (types) ; self } # [doc = " Enables all the standard ignore filters."] # [doc = ""] # [doc = " This toggles, as a group, all the filters that are enabled by default:"] # [doc = ""] # [doc = " - [hidden()](#method.hidden)"] # [doc = " - [parents()](#method.parents)"] # [doc = " - [ignore()](#method.ignore)"] # [doc = " - [git_ignore()](#method.git_ignore)"] # [doc = " - [git_global()](#method.git_global)"] # [doc = " - [git_exclude()](#method.git_exclude)"] # [doc = ""] # [doc = " They may still be toggled individually after calling this function."] # [doc = ""] # [doc = " This is (by definition) enabled by default."] pub fn standard_filters (& mut self , yes : bool) -> & mut WalkBuilder { self . hidden (yes) . parents (yes) . ignore (yes) . git_ignore (yes) . git_global (yes) . git_exclude (yes) } # [doc = " Enables ignoring hidden files."] # [doc = ""] # [doc = " This is enabled by default."] pub fn hidden (& mut self , yes : bool) -> & mut WalkBuilder { self . ig_builder . hidden (yes) ; self } # [doc = " Enables reading ignore files from parent directories."] # [doc = ""] # [doc = " If this is enabled, then .gitignore files in parent directories of each"] # [doc = " file path given are respected. Otherwise, they are ignored."] # [doc = ""] # [doc = " This is enabled by default."] pub fn parents (& mut self , yes : bool) -> & mut WalkBuilder { self . ig_builder . parents (yes) ; self } # [doc = " Enables reading `.ignore` files."] # [doc = ""] # [doc = " `.ignore` files have the same semantics as `gitignore` files and are"] # [doc = " supported by search tools such as ripgrep and The Silver Searcher."] # [doc = ""] # [doc = " This is enabled by default."] pub fn ignore (& mut self , yes : bool) -> & mut WalkBuilder { self . ig_builder . ignore (yes) ; self } # [doc = " Enables reading a global gitignore file, whose path is specified in"] # [doc = " git's `core.excludesFile` config option."] # [doc = ""] # [doc = " Git's config file location is `$HOME/.gitconfig`. If `$HOME/.gitconfig`"] # [doc = " does not exist or does not specify `core.excludesFile`, then"] # [doc = " `$XDG_CONFIG_HOME/git/ignore` is read. If `$XDG_CONFIG_HOME` is not"] # [doc = " set or is empty, then `$HOME/.config/git/ignore` is used instead."] # [doc = ""] # [doc = " This is enabled by default."] pub fn git_global (& mut self , yes : bool) -> & mut WalkBuilder { self . ig_builder . git_global (yes) ; self } # [doc = " Enables reading `.gitignore` files."] # [doc = ""] # [doc = " `.gitignore` files have match semantics as described in the `gitignore`"] # [doc = " man page."] # [doc = ""] # [doc = " This is enabled by default."] pub fn git_ignore (& mut self , yes : bool) -> & mut WalkBuilder { self . ig_builder . git_ignore (yes) ; self } # [doc = " Enables reading `.git/info/exclude` files."] # [doc = ""] # [doc = " `.git/info/exclude` files have match semantics as described in the"] # [doc = " `gitignore` man page."] # [doc = ""] # [doc = " This is enabled by default."] pub fn git_exclude (& mut self , yes : bool) -> & mut WalkBuilder { self . ig_builder . git_exclude (yes) ; self } # [doc = " Whether a git repository is required to apply git-related ignore"] # [doc = " rules (global rules, .gitignore and local exclude rules)."] # [doc = ""] # [doc = " When disabled, git-related ignore rules are applied even when searching"] # [doc = " outside a git repository."] # [doc = ""] # [doc = " In particular, if this is `false` then `.gitignore` files will be read"] # [doc = " from parent directories above the git root directory containing `.git`,"] # [doc = " which is different from the git behavior."] pub fn require_git (& mut self , yes : bool) -> & mut WalkBuilder { self . ig_builder . require_git (yes) ; self } # [doc = " Process ignore files case insensitively"] # [doc = ""] # [doc = " This is disabled by default."] pub fn ignore_case_insensitive (& mut self , yes : bool) -> & mut WalkBuilder { self . ig_builder . ignore_case_insensitive (yes) ; self } # [doc = " Set a function for sorting directory entries by their path."] # [doc = ""] # [doc = " If a compare function is set, the resulting iterator will return all"] # [doc = " paths in sorted order. The compare function will be called to compare"] # [doc = " entries from the same directory."] # [doc = ""] # [doc = " This is like `sort_by_file_name`, except the comparator accepts"] # [doc = " a `&Path` instead of the base file name, which permits it to sort by"] # [doc = " more criteria."] # [doc = ""] # [doc = " This method will override any previous sorter set by this method or"] # [doc = " by `sort_by_file_name`."] # [doc = ""] # [doc = " Note that this is not used in the parallel iterator."] pub fn sort_by_file_path < F > (& mut self , cmp : F) -> & mut WalkBuilder where F : Fn (& Path , & Path) -> Ordering + Send + Sync + 'static , { self . sorter = Some (Sorter :: ByPath (Arc :: new (cmp))) ; self } # [doc = " Set a function for sorting directory entries by file name."] # [doc = ""] # [doc = " If a compare function is set, the resulting iterator will return all"] # [doc = " paths in sorted order. The compare function will be called to compare"] # [doc = " names from entries from the same directory using only the name of the"] # [doc = " entry."] # [doc = ""] # [doc = " This method will override any previous sorter set by this method or"] # [doc = " by `sort_by_file_path`."] # [doc = ""] # [doc = " Note that this is not used in the parallel iterator."] pub fn sort_by_file_name < F > (& mut self , cmp : F) -> & mut WalkBuilder where F : Fn (& OsStr , & OsStr) -> Ordering + Send + Sync + 'static , { self . sorter = Some (Sorter :: ByName (Arc :: new (cmp))) ; self } # [doc = " Do not cross file system boundaries."] # [doc = ""] # [doc = " When this option is enabled, directory traversal will not descend into"] # [doc = " directories that are on a different file system from the root path."] # [doc = ""] # [doc = " Currently, this option is only supported on Unix and Windows. If this"] # [doc = " option is used on an unsupported platform, then directory traversal"] # [doc = " will immediately return an error and will not yield any entries."] pub fn same_file_system (& mut self , yes : bool) -> & mut WalkBuilder { self . same_file_system = yes ; self } # [doc = " Do not yield directory entries that are believed to correspond to"] # [doc = " stdout."] # [doc = ""] # [doc = " This is useful when a command is invoked via shell redirection to a"] # [doc = " file that is also being read. For example, `grep -r foo ./ > results`"] # [doc = " might end up trying to search `results` even though it is also writing"] # [doc = " to it, which could cause an unbounded feedback loop. Setting this"] # [doc = " option prevents this from happening by skipping over the `results`"] # [doc = " file."] # [doc = ""] # [doc = " This is disabled by default."] pub fn skip_stdout (& mut self , yes : bool) -> & mut WalkBuilder { if yes { self . skip = stdout_handle () . map (Arc :: new) ; } else { self . skip = None ; } self } # [doc = " Yields only entries which satisfy the given predicate and skips"] # [doc = " descending into directories that do not satisfy the given predicate."] # [doc = ""] # [doc = " The predicate is applied to all entries. If the predicate is"] # [doc = " true, iteration carries on as normal. If the predicate is false, the"] # [doc = " entry is ignored and if it is a directory, it is not descended into."] # [doc = ""] # [doc = " Note that the errors for reading entries that may not satisfy the"] # [doc = " predicate will still be yielded."] # [doc = ""] # [doc = " Note also that only one filter predicate can be applied to a"] # [doc = " `WalkBuilder`. Calling this subsequent times overrides previous filter"] # [doc = " predicates."] pub fn filter_entry < P > (& mut self , filter : P) -> & mut WalkBuilder where P : Fn (& DirEntry) -> bool + Send + Sync + 'static , { self . filter = Some (Filter (Arc :: new (filter))) ; self } # [doc = " Set the current working directory used for matching global gitignores."] # [doc = ""] # [doc = " If this is not set, then this walker will attempt to discover the"] # [doc = " correct path from the environment's current working directory. If"] # [doc = " that fails, then global gitignore files will be ignored."] # [doc = ""] # [doc = " Global gitignore files come from things like a user's git configuration"] # [doc = " or from gitignore files added via [`WalkBuilder::add_ignore`]."] pub fn current_dir (& mut self , cwd : impl Into < PathBuf > ,) -> & mut WalkBuilder { let cwd = cwd . into () ; self . ig_builder . current_dir (cwd . clone ()) ; if let Err (cwd) = self . global_gitignores_relative_to . set (Ok (cwd)) { * self . global_gitignores_relative_to . get_mut () . unwrap () = cwd ; } self } # [doc = " Gets the currently configured CWD on this walk builder."] # [doc = ""] # [doc = " This is \"lazy.\" That is, we only ask for the CWD from the environment"] # [doc = " if `WalkBuilder::current_dir` hasn't been called yet. And we ensure"] # [doc = " that we only do it once."] fn get_or_set_current_dir (& self) -> Option < & Path > { let result = self . global_gitignores_relative_to . get_or_init (| | { let result = std :: env :: current_dir () . map_err (Arc :: new) ; match result { Ok (ref path) => { log :: trace ! ("automatically discovered CWD: {}" , path . display ()) ; } Err (ref err) => { log :: debug ! ("failed to find CWD \
                         (global gitignores will be ignored): \
                         {err}") ; } } result }) ; result . as_ref () . ok () . map (| path | & * * path) } }
};
}
