// Generated macro for WalkBuilder (struct)
macro_rules! Depcrate_walkWalkBuilder {
() => {
// Module: crate::walk
// Provides: {"WalkBuilder"}
// Dependencies: {}
# [doc = " WalkBuilder builds a recursive directory iterator."] # [doc = ""] # [doc = " The builder supports a large number of configurable options. This includes"] # [doc = " specific glob overrides, file type matching, toggling whether hidden"] # [doc = " files are ignored or not, and of course, support for respecting gitignore"] # [doc = " files."] # [doc = ""] # [doc = " By default, all ignore files found are respected. This includes `.ignore`,"] # [doc = " `.gitignore`, `.git/info/exclude` and even your global gitignore"] # [doc = " globs, usually found in `$XDG_CONFIG_HOME/git/ignore`."] # [doc = ""] # [doc = " Some standard recursive directory options are also supported, such as"] # [doc = " limiting the recursive depth or whether to follow symbolic links (disabled"] # [doc = " by default)."] # [doc = ""] # [doc = " # Ignore rules"] # [doc = ""] # [doc = " There are many rules that influence whether a particular file or directory"] # [doc = " is skipped by this iterator. Those rules are documented here. Note that"] # [doc = " the rules assume a default configuration."] # [doc = ""] # [doc = " * First, glob overrides are checked. If a path matches a glob override,"] # [doc = " then matching stops. The path is then only skipped if the glob that matched"] # [doc = " the path is an ignore glob. (An override glob is a whitelist glob unless it"] # [doc = " starts with a `!`, in which case it is an ignore glob.)"] # [doc = " * Second, ignore files are checked. Ignore files currently only come from"] # [doc = " git ignore files (`.gitignore`, `.git/info/exclude` and the configured"] # [doc = " global gitignore file), plain `.ignore` files, which have the same format"] # [doc = " as gitignore files, or explicitly added ignore files. The precedence order"] # [doc = " is: `.ignore`, `.gitignore`, `.git/info/exclude`, global gitignore and"] # [doc = " finally explicitly added ignore files. Note that precedence between"] # [doc = " different types of ignore files is not impacted by the directory hierarchy;"] # [doc = " any `.ignore` file overrides all `.gitignore` files. Within each precedence"] # [doc = " level, more nested ignore files have a higher precedence than less nested"] # [doc = " ignore files."] # [doc = " * Third, if the previous step yields an ignore match, then all matching"] # [doc = " is stopped and the path is skipped. If it yields a whitelist match, then"] # [doc = " matching continues. A whitelist match can be overridden by a later matcher."] # [doc = " * Fourth, unless the path is a directory, the file type matcher is run on"] # [doc = " the path. As above, if it yields an ignore match, then all matching is"] # [doc = " stopped and the path is skipped. If it yields a whitelist match, then"] # [doc = " matching continues."] # [doc = " * Fifth, if the path hasn't been whitelisted and it is hidden, then the"] # [doc = " path is skipped."] # [doc = " * Sixth, unless the path is a directory, the size of the file is compared"] # [doc = " against the max filesize limit. If it exceeds the limit, it is skipped."] # [doc = " * Seventh, if the path has made it this far then it is yielded in the"] # [doc = " iterator."] # [derive (Clone)] pub struct WalkBuilder { paths : Vec < PathBuf > , ig_builder : IgnoreBuilder , max_depth : Option < usize > , min_depth : Option < usize > , max_filesize : Option < u64 > , follow_links : bool , same_file_system : bool , sorter : Option < Sorter > , threads : usize , skip : Option < Arc < Handle > > , filter : Option < Filter > , # [doc = " The directory that gitignores should be interpreted relative to."] # [doc = ""] # [doc = " Usually this is the directory containing the gitignore file. But in"] # [doc = " some cases, like for global gitignores or for gitignores specified"] # [doc = " explicitly, this should generally be set to the current working"] # [doc = " directory. This is only used for global gitignores or \"explicit\""] # [doc = " gitignores."] # [doc = ""] # [doc = " When `None`, the CWD is fetched from `std::env::current_dir()`. If"] # [doc = " that fails, then global gitignores are ignored (an error is logged)."] global_gitignores_relative_to : OnceLock < Result < PathBuf , Arc < std :: io :: Error > > > , }
};
}
