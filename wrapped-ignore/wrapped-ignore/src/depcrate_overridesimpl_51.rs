// Generated macro for impl_51 (impl)
macro_rules! Depcrate_overridesimpl_51 {
() => {
// Module: crate::overrides
// Provides: {"impl_51"}
// Dependencies: {}
impl OverrideBuilder { # [doc = " Create a new override builder."] # [doc = ""] # [doc = " Matching is done relative to the directory path provided."] pub fn new < P : AsRef < Path > > (path : P) -> OverrideBuilder { let mut builder = GitignoreBuilder :: new (path) ; builder . allow_unclosed_class (false) ; OverrideBuilder { builder } } # [doc = " Builds a new override matcher from the globs added so far."] # [doc = ""] # [doc = " Once a matcher is built, no new globs can be added to it."] pub fn build (& self) -> Result < Override , Error > { Ok (Override (self . builder . build () ?)) } # [doc = " Add a glob to the set of overrides."] # [doc = ""] # [doc = " Globs provided here have precisely the same semantics as a single"] # [doc = " line in a `gitignore` file, where the meaning of `!` is inverted:"] # [doc = " namely, `!` at the beginning of a glob will ignore a file. Without `!`,"] # [doc = " all matches of the glob provided are treated as whitelist matches."] pub fn add (& mut self , glob : & str) -> Result < & mut OverrideBuilder , Error > { self . builder . add_line (None , glob) ? ; Ok (self) } # [doc = " Toggle whether the globs should be matched case insensitively or not."] # [doc = ""] # [doc = " When this option is changed, only globs added after the change will be"] # [doc = " affected."] # [doc = ""] # [doc = " This is disabled by default."] pub fn case_insensitive (& mut self , yes : bool ,) -> Result < & mut OverrideBuilder , Error > { self . builder . case_insensitive (yes) ? ; Ok (self) } # [doc = " Toggle whether unclosed character classes are allowed. When allowed,"] # [doc = " a `[` without a matching `]` is treated literally instead of resulting"] # [doc = " in a parse error."] # [doc = ""] # [doc = " For example, if this is set then the glob `[abc` will be treated as the"] # [doc = " literal string `[abc` instead of returning an error."] # [doc = ""] # [doc = " By default, this is false. Generally speaking, enabling this leads to"] # [doc = " worse failure modes since the glob parser becomes more permissive. You"] # [doc = " might want to enable this when compatibility (e.g., with POSIX glob"] # [doc = " implementations) is more important than good error messages."] # [doc = ""] # [doc = " This default is different from the default for [`Gitignore`]. Namely,"] # [doc = " [`Gitignore`] is intended to match git's behavior as-is. But this"] # [doc = " abstraction for \"override\" globs does not necessarily conform to any"] # [doc = " other known specification and instead prioritizes better error"] # [doc = " messages."] pub fn allow_unclosed_class (& mut self , yes : bool) -> & mut OverrideBuilder { self . builder . allow_unclosed_class (yes) ; self } }
};
}
