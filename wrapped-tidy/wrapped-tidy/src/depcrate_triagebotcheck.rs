// Generated macro for check (function)
macro_rules! Depcrate_triagebotcheck {
() => {
// Module: crate::triagebot
// Provides: {"check"}
// Dependencies: {}
pub fn check (path : & Path , bad : & mut bool) { let triagebot_path = path . join ("triagebot.toml") ; if ! triagebot_path . exists () { return ; } let contents = std :: fs :: read_to_string (& triagebot_path) . unwrap () ; let config : Value = toml :: from_str (& contents) . unwrap () ; if let Some (Value :: Table (mentions)) = config . get ("mentions") { for (entry_key , entry_val) in mentions . iter () { if entry_val . get ("type") . is_some_and (| t | t . as_str () . unwrap_or_default () != "filename") { continue ; } let path_str = entry_key ; let clean_path = path_str . trim_matches ('"') ; let full_path = path . join (clean_path) ; if ! full_path . exists () { tidy_error ! (bad , "triagebot.toml [mentions.*] contains path '{}' which doesn't exist" , clean_path) ; } } } else { tidy_error ! (bad , "triagebot.toml missing [mentions.*] section, this wrong for rust-lang/rust repo.") ; } if let Some (Value :: Table (assign)) = config . get ("assign") { if let Some (Value :: Table (owners)) = assign . get ("owners") { for path_str in owners . keys () { let clean_path = path_str . trim_matches ('"') . trim_start_matches ('/') ; let full_path = path . join (clean_path) ; if ! full_path . exists () { tidy_error ! (bad , "triagebot.toml [assign.owners] contains path '{}' which doesn't exist" , clean_path) ; } } } else { tidy_error ! (bad , "triagebot.toml missing [assign.owners] section, this wrong for rust-lang/rust repo.") ; } } if let Some (Value :: Table (autolabels)) = config . get ("autolabel") { for (label , content) in autolabels { if let Some (trigger_files) = content . get ("trigger_files") . and_then (| v | v . as_array ()) { for file in trigger_files { if let Some (file_str) = file . as_str () { let full_path = path . join (file_str) ; if ! full_path . exists () { tidy_error ! (bad , "triagebot.toml [autolabel.{}] contains trigger_files path '{}' which doesn't exist" , label , file_str) ; } } } } } } }
};
}
