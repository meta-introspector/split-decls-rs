// Generated macro for Async (struct)
macro_rules! DepcrateAsync {
() => {
// Module: crate
// Provides: {"Async"}
// Dependencies: {}
# [doc = " Async adapter for I/O types."] # [doc = ""] # [doc = " This type puts an I/O handle into non-blocking mode, registers it in"] # [doc = " [epoll]/[kqueue]/[event ports]/[IOCP], and then provides an async interface for it."] # [doc = ""] # [doc = " [epoll]: https://en.wikipedia.org/wiki/Epoll"] # [doc = " [kqueue]: https://en.wikipedia.org/wiki/Kqueue"] # [doc = " [event ports]: https://illumos.org/man/port_create"] # [doc = " [IOCP]: https://learn.microsoft.com/en-us/windows/win32/fileio/i-o-completion-ports"] # [doc = ""] # [doc = " # Caveats"] # [doc = ""] # [doc = " [`Async`] is a low-level primitive, and as such it comes with some caveats."] # [doc = ""] # [doc = " For higher-level primitives built on top of [`Async`], look into [`async-net`] or"] # [doc = " [`async-process`] (on Unix)."] # [doc = ""] # [doc = " The most notable caveat is that it is unsafe to access the inner I/O source mutably"] # [doc = " using this primitive. Traits likes [`AsyncRead`] and [`AsyncWrite`] are not implemented by"] # [doc = " default unless it is guaranteed that the resource won't be invalidated by reading or writing."] # [doc = " See the [`IoSafe`] trait for more information."] # [doc = ""] # [doc = " [`async-net`]: https://github.com/smol-rs/async-net"] # [doc = " [`async-process`]: https://github.com/smol-rs/async-process"] # [doc = " [`AsyncRead`]: https://docs.rs/futures-io/latest/futures_io/trait.AsyncRead.html"] # [doc = " [`AsyncWrite`]: https://docs.rs/futures-io/latest/futures_io/trait.AsyncWrite.html"] # [doc = ""] # [doc = " ### Supported types"] # [doc = ""] # [doc = " [`Async`] supports all networking types, as well as some OS-specific file descriptors like"] # [doc = " [timerfd] and [inotify]."] # [doc = ""] # [doc = " However, do not use [`Async`] with types like [`File`][`std::fs::File`],"] # [doc = " [`Stdin`][`std::io::Stdin`], [`Stdout`][`std::io::Stdout`], or [`Stderr`][`std::io::Stderr`]"] # [doc = " because all operating systems have issues with them when put in non-blocking mode."] # [doc = ""] # [doc = " [timerfd]: https://github.com/smol-rs/async-io/blob/master/examples/linux-timerfd.rs"] # [doc = " [inotify]: https://github.com/smol-rs/async-io/blob/master/examples/linux-inotify.rs"] # [doc = ""] # [doc = " ### Concurrent I/O"] # [doc = ""] # [doc = " Note that [`&Async<T>`][`Async`] implements [`AsyncRead`] and [`AsyncWrite`] if `&T`"] # [doc = " implements those traits, which means tasks can concurrently read and write using shared"] # [doc = " references."] # [doc = ""] # [doc = " But there is a catch: only one task can read a time, and only one task can write at a time. It"] # [doc = " is okay to have two tasks where one is reading and the other is writing at the same time, but"] # [doc = " it is not okay to have two tasks reading at the same time or writing at the same time. If you"] # [doc = " try to do that, conflicting tasks will just keep waking each other in turn, thus wasting CPU"] # [doc = " time."] # [doc = ""] # [doc = " Besides [`AsyncRead`] and [`AsyncWrite`], this caveat also applies to"] # [doc = " [`poll_readable()`][`Async::poll_readable()`] and"] # [doc = " [`poll_writable()`][`Async::poll_writable()`]."] # [doc = ""] # [doc = " However, any number of tasks can be concurrently calling other methods like"] # [doc = " [`readable()`][`Async::readable()`] or [`read_with()`][`Async::read_with()`]."] # [doc = ""] # [doc = " ### Closing"] # [doc = ""] # [doc = " Closing the write side of [`Async`] with [`close()`][`futures_lite::AsyncWriteExt::close()`]"] # [doc = " simply flushes. If you want to shutdown a TCP or Unix socket, use"] # [doc = " [`Shutdown`][`std::net::Shutdown`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Connect to a server and echo incoming messages back to the server:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use futures_lite::io;"] # [doc = " use std::net::TcpStream;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " // Connect to a local server."] # [doc = " let stream = Async::<TcpStream>::connect(([127, 0, 0, 1], 8000)).await?;"] # [doc = ""] # [doc = " // Echo all messages from the read side of the stream into the write side."] # [doc = " io::copy(&stream, &stream).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] # [doc = ""] # [doc = " You can use either predefined async methods or wrap blocking I/O operations in"] # [doc = " [`Async::read_with()`], [`Async::read_with_mut()`], [`Async::write_with()`], and"] # [doc = " [`Async::write_with_mut()`]:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::TcpListener;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let listener = Async::<TcpListener>::bind(([127, 0, 0, 1], 0))?;"] # [doc = ""] # [doc = " // These two lines are equivalent:"] # [doc = " let (stream, addr) = listener.accept().await?;"] # [doc = " let (stream, addr) = listener.read_with(|inner| inner.accept()).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] # [derive (Debug)] pub struct Async < T > { # [doc = " A source registered in the reactor."] source : Arc < Source > , # [doc = " The inner I/O handle."] io : Option < T > , }
};
}
