// Generated macro for impl_139 (impl)
macro_rules! Depcrateimpl_139 {
() => {
// Module: crate
// Provides: {"impl_139"}
// Dependencies: {}
# [cfg (unix)] impl Async < UnixListener > { # [doc = " Creates a UDS listener bound to the specified path."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::os::unix::net::UnixListener;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let listener = Async::<UnixListener>::bind(\"/tmp/socket\")?;"] # [doc = " println!(\"Listening on {:?}\", listener.get_ref().local_addr()?);"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn bind < P : AsRef < Path > > (path : P) -> io :: Result < Async < UnixListener > > { let path = path . as_ref () . to_owned () ; Async :: new (UnixListener :: bind (path) ?) } # [doc = " Accepts a new incoming UDS stream connection."] # [doc = ""] # [doc = " When a connection is established, it will be returned as a stream together with its remote"] # [doc = " address."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::os::unix::net::UnixListener;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let listener = Async::<UnixListener>::bind(\"/tmp/socket\")?;"] # [doc = " let (stream, addr) = listener.accept().await?;"] # [doc = " println!(\"Accepted client: {:?}\", addr);"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn accept (& self) -> io :: Result < (Async < UnixStream > , UnixSocketAddr) > { let (stream , addr) = self . read_with (| io | io . accept ()) . await ? ; Ok ((Async :: new (stream) ? , addr)) } # [doc = " Returns a stream of incoming UDS connections."] # [doc = ""] # [doc = " The stream is infinite, i.e. it never stops with a [`None`] item."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use futures_lite::stream::StreamExt;"] # [doc = " use std::os::unix::net::UnixListener;"] # [doc = " use std::pin::pin;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let listener = Async::<UnixListener>::bind(\"/tmp/socket\")?;"] # [doc = " let incoming = listener.incoming();"] # [doc = " let mut incoming = pin!(incoming);"] # [doc = ""] # [doc = " while let Some(stream) = incoming.next().await {"] # [doc = "     let stream = stream?;"] # [doc = "     println!(\"Accepted client: {:?}\", stream.get_ref().peer_addr()?);"] # [doc = " }"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn incoming (& self) -> impl Stream < Item = io :: Result < Async < UnixStream > > > + Send + '_ { stream :: unfold (self , | listener | async move { let res = listener . accept () . await . map (| (stream , _) | stream) ; Some ((res , listener)) }) } }
};
}
