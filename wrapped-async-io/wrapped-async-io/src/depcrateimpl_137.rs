// Generated macro for impl_137 (impl)
macro_rules! Depcrateimpl_137 {
() => {
// Module: crate
// Provides: {"impl_137"}
// Dependencies: {}
impl Async < UdpSocket > { # [doc = " Creates a UDP socket bound to the specified address."] # [doc = ""] # [doc = " Binding with port number 0 will request an available port from the OS."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Async;"] # [doc = " use std::net::UdpSocket;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let socket = Async::<UdpSocket>::bind(([127, 0, 0, 1], 0))?;"] # [doc = " println!(\"Bound to {}\", socket.get_ref().local_addr()?);"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn bind < A : Into < SocketAddr > > (addr : A) -> io :: Result < Async < UdpSocket > > { let addr = addr . into () ; Async :: new (UdpSocket :: bind (addr) ?) } # [doc = " Receives a single datagram message."] # [doc = ""] # [doc = " Returns the number of bytes read and the address the message came from."] # [doc = ""] # [doc = " This method must be called with a valid byte slice of sufficient size to hold the message."] # [doc = " If the message is too long to fit, excess bytes may get discarded."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::UdpSocket;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let socket = Async::<UdpSocket>::bind(([127, 0, 0, 1], 8000))?;"] # [doc = ""] # [doc = " let mut buf = [0u8; 1024];"] # [doc = " let (len, addr) = socket.recv_from(&mut buf).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn recv_from (& self , buf : & mut [u8]) -> io :: Result < (usize , SocketAddr) > { self . read_with (| io | io . recv_from (buf)) . await } # [doc = " Receives a single datagram message without removing it from the queue."] # [doc = ""] # [doc = " Returns the number of bytes read and the address the message came from."] # [doc = ""] # [doc = " This method must be called with a valid byte slice of sufficient size to hold the message."] # [doc = " If the message is too long to fit, excess bytes may get discarded."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::UdpSocket;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let socket = Async::<UdpSocket>::bind(([127, 0, 0, 1], 8000))?;"] # [doc = ""] # [doc = " let mut buf = [0u8; 1024];"] # [doc = " let (len, addr) = socket.peek_from(&mut buf).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn peek_from (& self , buf : & mut [u8]) -> io :: Result < (usize , SocketAddr) > { self . read_with (| io | io . peek_from (buf)) . await } # [doc = " Sends data to the specified address."] # [doc = ""] # [doc = " Returns the number of bytes written."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::UdpSocket;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let socket = Async::<UdpSocket>::bind(([127, 0, 0, 1], 0))?;"] # [doc = " let addr = socket.get_ref().local_addr()?;"] # [doc = ""] # [doc = " let msg = b\"hello\";"] # [doc = " let len = socket.send_to(msg, addr).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn send_to < A : Into < SocketAddr > > (& self , buf : & [u8] , addr : A) -> io :: Result < usize > { let addr = addr . into () ; self . write_with (| io | io . send_to (buf , addr)) . await } # [doc = " Receives a single datagram message from the connected peer."] # [doc = ""] # [doc = " Returns the number of bytes read."] # [doc = ""] # [doc = " This method must be called with a valid byte slice of sufficient size to hold the message."] # [doc = " If the message is too long to fit, excess bytes may get discarded."] # [doc = ""] # [doc = " The [`connect`][`UdpSocket::connect()`] method connects this socket to a remote address."] # [doc = " This method will fail if the socket is not connected."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::UdpSocket;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let socket = Async::<UdpSocket>::bind(([127, 0, 0, 1], 8000))?;"] # [doc = " socket.get_ref().connect(\"127.0.0.1:9000\")?;"] # [doc = ""] # [doc = " let mut buf = [0u8; 1024];"] # [doc = " let len = socket.recv(&mut buf).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn recv (& self , buf : & mut [u8]) -> io :: Result < usize > { self . read_with (| io | io . recv (buf)) . await } # [doc = " Receives a single datagram message from the connected peer without removing it from the"] # [doc = " queue."] # [doc = ""] # [doc = " Returns the number of bytes read and the address the message came from."] # [doc = ""] # [doc = " This method must be called with a valid byte slice of sufficient size to hold the message."] # [doc = " If the message is too long to fit, excess bytes may get discarded."] # [doc = ""] # [doc = " The [`connect`][`UdpSocket::connect()`] method connects this socket to a remote address."] # [doc = " This method will fail if the socket is not connected."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::UdpSocket;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let socket = Async::<UdpSocket>::bind(([127, 0, 0, 1], 8000))?;"] # [doc = " socket.get_ref().connect(\"127.0.0.1:9000\")?;"] # [doc = ""] # [doc = " let mut buf = [0u8; 1024];"] # [doc = " let len = socket.peek(&mut buf).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn peek (& self , buf : & mut [u8]) -> io :: Result < usize > { self . read_with (| io | io . peek (buf)) . await } # [doc = " Sends data to the connected peer."] # [doc = ""] # [doc = " Returns the number of bytes written."] # [doc = ""] # [doc = " The [`connect`][`UdpSocket::connect()`] method connects this socket to a remote address."] # [doc = " This method will fail if the socket is not connected."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::UdpSocket;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let socket = Async::<UdpSocket>::bind(([127, 0, 0, 1], 8000))?;"] # [doc = " socket.get_ref().connect(\"127.0.0.1:9000\")?;"] # [doc = ""] # [doc = " let msg = b\"hello\";"] # [doc = " let len = socket.send(msg).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn send (& self , buf : & [u8]) -> io :: Result < usize > { self . write_with (| io | io . send (buf)) . await } }
};
}
