// Generated macro for impl_94 (impl)
macro_rules! Depcrateimpl_94 {
() => {
// Module: crate
// Provides: {"impl_94"}
// Dependencies: {}
# [cfg (unix)] impl < T : AsFd > Async < T > { # [doc = " Creates an async I/O handle."] # [doc = ""] # [doc = " This method will put the handle in non-blocking mode and register it in"] # [doc = " [epoll]/[kqueue]/[event ports]/[IOCP]."] # [doc = ""] # [doc = " On Unix systems, the handle must implement `AsFd`, while on Windows it must implement"] # [doc = " `AsSocket`."] # [doc = ""] # [doc = " [epoll]: https://en.wikipedia.org/wiki/Epoll"] # [doc = " [kqueue]: https://en.wikipedia.org/wiki/Kqueue"] # [doc = " [event ports]: https://illumos.org/man/port_create"] # [doc = " [IOCP]: https://learn.microsoft.com/en-us/windows/win32/fileio/i-o-completion-ports"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Async;"] # [doc = " use std::net::{SocketAddr, TcpListener};"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let listener = TcpListener::bind(SocketAddr::from(([127, 0, 0, 1], 0)))?;"] # [doc = " let listener = Async::new(listener)?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn new (io : T) -> io :: Result < Async < T > > { set_nonblocking (io . as_fd ()) ? ; Self :: new_nonblocking (io) } # [doc = " Creates an async I/O handle without setting it to non-blocking mode."] # [doc = ""] # [doc = " This method will register the handle in [epoll]/[kqueue]/[event ports]/[IOCP]."] # [doc = ""] # [doc = " On Unix systems, the handle must implement `AsFd`, while on Windows it must implement"] # [doc = " `AsSocket`."] # [doc = ""] # [doc = " [epoll]: https://en.wikipedia.org/wiki/Epoll"] # [doc = " [kqueue]: https://en.wikipedia.org/wiki/Kqueue"] # [doc = " [event ports]: https://illumos.org/man/port_create"] # [doc = " [IOCP]: https://learn.microsoft.com/en-us/windows/win32/fileio/i-o-completion-ports"] # [doc = ""] # [doc = " # Caveats"] # [doc = ""] # [doc = " The caller should ensure that the handle is set to non-blocking mode or that it is okay if"] # [doc = " it is not set. If not set to non-blocking mode, I/O operations may block the current thread"] # [doc = " and cause a deadlock in an asynchronous context."] pub fn new_nonblocking (io : T) -> io :: Result < Async < T > > { let registration = unsafe { Registration :: new (io . as_fd ()) } ; Ok (Async { source : Reactor :: get () . insert_io (registration) ? , io : Some (io) , }) } }
};
}
