// Generated macro for impl_88 (impl)
macro_rules! Depcrateimpl_88 {
() => {
// Module: crate
// Provides: {"impl_88"}
// Dependencies: {}
impl Timer { # [doc = " Creates a timer that will never fire."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " This function may also be useful for creating a function with an optional timeout."] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_io::Timer;"] # [doc = " use futures_lite::prelude::*;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " async fn run_with_timeout(timeout: Option<Duration>) {"] # [doc = "     let timer = timeout"] # [doc = "         .map(|timeout| Timer::after(timeout))"] # [doc = "         .unwrap_or_else(Timer::never);"] # [doc = ""] # [doc = "     run_lengthy_operation().or(timer).await;"] # [doc = " }"] # [doc = " # // Note that since a Timer as a Future returns an Instant,"] # [doc = " # // this function needs to return an Instant to be used"] # [doc = " # // in \"or\"."] # [doc = " # async fn run_lengthy_operation() -> std::time::Instant {"] # [doc = " #    std::time::Instant::now()"] # [doc = " # }"] # [doc = ""] # [doc = " // Times out after 5 seconds."] # [doc = " run_with_timeout(Some(Duration::from_secs(5))).await;"] # [doc = " // Does not time out."] # [doc = " run_with_timeout(None).await;"] # [doc = " # });"] # [doc = " ```"] pub fn never () -> Timer { Timer { id_and_waker : None , when : None , period : Duration :: MAX , } } # [doc = " Creates a timer that emits an event once after the given duration of time."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Timer;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " Timer::after(Duration::from_secs(1)).await;"] # [doc = " # });"] # [doc = " ```"] pub fn after (duration : Duration) -> Timer { Instant :: now () . checked_add (duration) . map_or_else (Timer :: never , Timer :: at) } # [doc = " Creates a timer that emits an event once at the given time instant."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Timer;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let now = Instant::now();"] # [doc = " let when = now + Duration::from_secs(1);"] # [doc = " Timer::at(when).await;"] # [doc = " # });"] # [doc = " ```"] pub fn at (instant : Instant) -> Timer { Timer :: interval_at (instant , Duration :: MAX) } # [doc = " Creates a timer that emits events periodically."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Timer;"] # [doc = " use futures_lite::StreamExt;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let period = Duration::from_secs(1);"] # [doc = " Timer::interval(period).next().await;"] # [doc = " # });"] # [doc = " ```"] pub fn interval (period : Duration) -> Timer { Instant :: now () . checked_add (period) . map_or_else (Timer :: never , | at | Timer :: interval_at (at , period)) } # [doc = " Creates a timer that emits events periodically, starting at `start`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Timer;"] # [doc = " use futures_lite::StreamExt;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let start = Instant::now();"] # [doc = " let period = Duration::from_secs(1);"] # [doc = " Timer::interval_at(start, period).next().await;"] # [doc = " # });"] # [doc = " ```"] pub fn interval_at (start : Instant , period : Duration) -> Timer { Timer { id_and_waker : None , when : Some (start) , period , } } # [doc = " Indicates whether or not this timer will ever fire."] # [doc = ""] # [doc = " [`never()`] will never fire, and timers created with [`after()`] or [`at()`] will fire"] # [doc = " if the duration is not too large."] # [doc = ""] # [doc = " [`never()`]: Timer::never()"] # [doc = " [`after()`]: Timer::after()"] # [doc = " [`at()`]: Timer::at()"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " use async_io::Timer;"] # [doc = " use futures_lite::prelude::*;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " // `never` will never fire."] # [doc = " assert!(!Timer::never().will_fire());"] # [doc = ""] # [doc = " // `after` will fire if the duration is not too large."] # [doc = " assert!(Timer::after(Duration::from_secs(1)).will_fire());"] # [doc = " assert!(!Timer::after(Duration::MAX).will_fire());"] # [doc = ""] # [doc = " // However, once an `after` timer has fired, it will never fire again."] # [doc = " let mut t = Timer::after(Duration::from_secs(1));"] # [doc = " assert!(t.will_fire());"] # [doc = " (&mut t).await;"] # [doc = " assert!(!t.will_fire());"] # [doc = ""] # [doc = " // Interval timers will fire periodically."] # [doc = " let mut t = Timer::interval(Duration::from_secs(1));"] # [doc = " assert!(t.will_fire());"] # [doc = " t.next().await;"] # [doc = " assert!(t.will_fire());"] # [doc = " # });"] # [doc = " ```"] # [inline] pub fn will_fire (& self) -> bool { self . when . is_some () } # [doc = " Sets the timer to emit an event once after the given duration of time."] # [doc = ""] # [doc = " Note that resetting a timer is different from creating a new timer because"] # [doc = " [`set_after()`][`Timer::set_after()`] does not remove the waker associated with the task"] # [doc = " that is polling the timer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Timer;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut t = Timer::after(Duration::from_secs(1));"] # [doc = " t.set_after(Duration::from_millis(100));"] # [doc = " # });"] # [doc = " ```"] pub fn set_after (& mut self , duration : Duration) { match Instant :: now () . checked_add (duration) { Some (instant) => self . set_at (instant) , None => { self . clear () ; } } } # [doc = " Sets the timer to emit an event once at the given time instant."] # [doc = ""] # [doc = " Note that resetting a timer is different from creating a new timer because"] # [doc = " [`set_at()`][`Timer::set_at()`] does not remove the waker associated with the task"] # [doc = " that is polling the timer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Timer;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut t = Timer::after(Duration::from_secs(1));"] # [doc = ""] # [doc = " let now = Instant::now();"] # [doc = " let when = now + Duration::from_secs(1);"] # [doc = " t.set_at(when);"] # [doc = " # });"] # [doc = " ```"] pub fn set_at (& mut self , instant : Instant) { self . clear () ; self . when = Some (instant) ; if let Some ((id , waker)) = self . id_and_waker . as_mut () { * id = Reactor :: get () . insert_timer (instant , waker) ; } } # [doc = " Sets the timer to emit events periodically."] # [doc = ""] # [doc = " Note that resetting a timer is different from creating a new timer because"] # [doc = " [`set_interval()`][`Timer::set_interval()`] does not remove the waker associated with the"] # [doc = " task that is polling the timer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Timer;"] # [doc = " use futures_lite::StreamExt;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut t = Timer::after(Duration::from_secs(1));"] # [doc = ""] # [doc = " let period = Duration::from_secs(2);"] # [doc = " t.set_interval(period);"] # [doc = " # });"] # [doc = " ```"] pub fn set_interval (& mut self , period : Duration) { match Instant :: now () . checked_add (period) { Some (instant) => self . set_interval_at (instant , period) , None => { self . clear () ; } } } # [doc = " Sets the timer to emit events periodically, starting at `start`."] # [doc = ""] # [doc = " Note that resetting a timer is different from creating a new timer because"] # [doc = " [`set_interval_at()`][`Timer::set_interval_at()`] does not remove the waker associated with"] # [doc = " the task that is polling the timer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Timer;"] # [doc = " use futures_lite::StreamExt;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut t = Timer::after(Duration::from_secs(1));"] # [doc = ""] # [doc = " let start = Instant::now();"] # [doc = " let period = Duration::from_secs(2);"] # [doc = " t.set_interval_at(start, period);"] # [doc = " # });"] # [doc = " ```"] pub fn set_interval_at (& mut self , start : Instant , period : Duration) { self . clear () ; self . when = Some (start) ; self . period = period ; if let Some ((id , waker)) = self . id_and_waker . as_mut () { * id = Reactor :: get () . insert_timer (start , waker) ; } } # [doc = " Clear any timeouts set on this timer. It will never fire again until a new interval or instant is set."] pub fn clear (& mut self) { if let (Some (when) , Some ((id , _))) = (self . when , self . id_and_waker . as_ref ()) { Reactor :: get () . remove_timer (when , * id) ; } self . when = None ; } }
};
}
