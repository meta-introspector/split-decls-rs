// Generated macro for impl_133 (impl)
macro_rules! Depcrateimpl_133 {
() => {
// Module: crate
// Provides: {"impl_133"}
// Dependencies: {}
impl Async < TcpListener > { # [doc = " Creates a TCP listener bound to the specified address."] # [doc = ""] # [doc = " Binding with port number 0 will request an available port from the OS."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Async;"] # [doc = " use std::net::TcpListener;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let listener = Async::<TcpListener>::bind(([127, 0, 0, 1], 0))?;"] # [doc = " println!(\"Listening on {}\", listener.get_ref().local_addr()?);"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn bind < A : Into < SocketAddr > > (addr : A) -> io :: Result < Async < TcpListener > > { let addr = addr . into () ; Async :: new (TcpListener :: bind (addr) ?) } # [doc = " Accepts a new incoming TCP connection."] # [doc = ""] # [doc = " When a connection is established, it will be returned as a TCP stream together with its"] # [doc = " remote address."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::TcpListener;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let listener = Async::<TcpListener>::bind(([127, 0, 0, 1], 8000))?;"] # [doc = " let (stream, addr) = listener.accept().await?;"] # [doc = " println!(\"Accepted client: {}\", addr);"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn accept (& self) -> io :: Result < (Async < TcpStream > , SocketAddr) > { let (stream , addr) = self . read_with (| io | io . accept ()) . await ? ; Ok ((Async :: new (stream) ? , addr)) } # [doc = " Returns a stream of incoming TCP connections."] # [doc = ""] # [doc = " The stream is infinite, i.e. it never stops with a [`None`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use futures_lite::{stream::StreamExt};"] # [doc = " use std::net::TcpListener;"] # [doc = " use std::pin::pin;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let listener = Async::<TcpListener>::bind(([127, 0, 0, 1], 8000))?;"] # [doc = " let incoming = listener.incoming();"] # [doc = " let mut incoming = pin!(incoming);"] # [doc = ""] # [doc = " while let Some(stream) = incoming.next().await {"] # [doc = "     let stream = stream?;"] # [doc = "     println!(\"Accepted client: {}\", stream.get_ref().peer_addr()?);"] # [doc = " }"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn incoming (& self) -> impl Stream < Item = io :: Result < Async < TcpStream > > > + Send + '_ { stream :: unfold (self , | listener | async move { let res = listener . accept () . await . map (| (stream , _) | stream) ; Some ((res , listener)) }) } }
};
}
