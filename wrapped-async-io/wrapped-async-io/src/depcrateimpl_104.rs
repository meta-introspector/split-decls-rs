// Generated macro for impl_104 (impl)
macro_rules! Depcrateimpl_104 {
() => {
// Module: crate
// Provides: {"impl_104"}
// Dependencies: {}
impl < T > Async < T > { # [doc = " Gets a reference to the inner I/O handle."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Async;"] # [doc = " use std::net::TcpListener;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let listener = Async::<TcpListener>::bind(([127, 0, 0, 1], 0))?;"] # [doc = " let inner = listener.get_ref();"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn get_ref (& self) -> & T { self . io . as_ref () . unwrap () } # [doc = " Gets a mutable reference to the inner I/O handle."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The underlying I/O source must not be dropped using this function."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Async;"] # [doc = " use std::net::TcpListener;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut listener = Async::<TcpListener>::bind(([127, 0, 0, 1], 0))?;"] # [doc = " let inner = unsafe { listener.get_mut() };"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub unsafe fn get_mut (& mut self) -> & mut T { self . io . as_mut () . unwrap () } # [doc = " Unwraps the inner I/O handle."] # [doc = ""] # [doc = " This method will **not** put the I/O handle back into blocking mode."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Async;"] # [doc = " use std::net::TcpListener;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let listener = Async::<TcpListener>::bind(([127, 0, 0, 1], 0))?;"] # [doc = " let inner = listener.into_inner()?;"] # [doc = ""] # [doc = " // Put the listener back into blocking mode."] # [doc = " inner.set_nonblocking(false)?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn into_inner (mut self) -> io :: Result < T > { let io = self . io . take () . unwrap () ; Reactor :: get () . remove_io (& self . source) ? ; Ok (io) } # [doc = " Waits until the I/O handle is readable."] # [doc = ""] # [doc = " This method completes when a read operation on this I/O handle wouldn't block."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::TcpListener;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut listener = Async::<TcpListener>::bind(([127, 0, 0, 1], 0))?;"] # [doc = ""] # [doc = " // Wait until a client can be accepted."] # [doc = " listener.readable().await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn readable (& self) -> Readable < '_ , T > { Source :: readable (self) } # [doc = " Waits until the I/O handle is readable."] # [doc = ""] # [doc = " This method completes when a read operation on this I/O handle wouldn't block."] pub fn readable_owned (self : Arc < Self >) -> ReadableOwned < T > { Source :: readable_owned (self) } # [doc = " Waits until the I/O handle is writable."] # [doc = ""] # [doc = " This method completes when a write operation on this I/O handle wouldn't block."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Async;"] # [doc = " use std::net::{TcpStream, ToSocketAddrs};"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let addr = \"example.com:80\".to_socket_addrs()?.next().unwrap();"] # [doc = " let stream = Async::<TcpStream>::connect(addr).await?;"] # [doc = ""] # [doc = " // Wait until the stream is writable."] # [doc = " stream.writable().await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn writable (& self) -> Writable < '_ , T > { Source :: writable (self) } # [doc = " Waits until the I/O handle is writable."] # [doc = ""] # [doc = " This method completes when a write operation on this I/O handle wouldn't block."] pub fn writable_owned (self : Arc < Self >) -> WritableOwned < T > { Source :: writable_owned (self) } # [doc = " Polls the I/O handle for readability."] # [doc = ""] # [doc = " When this method returns [`Poll::Ready`], that means the OS has delivered an event"] # [doc = " indicating readability since the last time this task has called the method and received"] # [doc = " [`Poll::Pending`]."] # [doc = ""] # [doc = " # Caveats"] # [doc = ""] # [doc = " Two different tasks should not call this method concurrently. Otherwise, conflicting tasks"] # [doc = " will just keep waking each other in turn, thus wasting CPU time."] # [doc = ""] # [doc = " Note that the [`AsyncRead`] implementation for [`Async`] also uses this method."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::future::poll_fn;"] # [doc = " use std::net::TcpListener;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut listener = Async::<TcpListener>::bind(([127, 0, 0, 1], 0))?;"] # [doc = ""] # [doc = " // Wait until a client can be accepted."] # [doc = " poll_fn(|cx| listener.poll_readable(cx)).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn poll_readable (& self , cx : & mut Context < '_ >) -> Poll < io :: Result < () > > { self . source . poll_readable (cx) } # [doc = " Polls the I/O handle for writability."] # [doc = ""] # [doc = " When this method returns [`Poll::Ready`], that means the OS has delivered an event"] # [doc = " indicating writability since the last time this task has called the method and received"] # [doc = " [`Poll::Pending`]."] # [doc = ""] # [doc = " # Caveats"] # [doc = ""] # [doc = " Two different tasks should not call this method concurrently. Otherwise, conflicting tasks"] # [doc = " will just keep waking each other in turn, thus wasting CPU time."] # [doc = ""] # [doc = " Note that the [`AsyncWrite`] implementation for [`Async`] also uses this method."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use async_io::Async;"] # [doc = " use std::future::poll_fn;"] # [doc = " use std::net::{TcpStream, ToSocketAddrs};"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let addr = \"example.com:80\".to_socket_addrs()?.next().unwrap();"] # [doc = " let stream = Async::<TcpStream>::connect(addr).await?;"] # [doc = ""] # [doc = " // Wait until the stream is writable."] # [doc = " poll_fn(|cx| stream.poll_writable(cx)).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn poll_writable (& self , cx : & mut Context < '_ >) -> Poll < io :: Result < () > > { self . source . poll_writable (cx) } # [doc = " Performs a read operation asynchronously."] # [doc = ""] # [doc = " The I/O handle is registered in the reactor and put in non-blocking mode. This method"] # [doc = " invokes the `op` closure in a loop until it succeeds or returns an error other than"] # [doc = " [`io::ErrorKind::WouldBlock`]. In between iterations of the loop, it waits until the OS"] # [doc = " sends a notification that the I/O handle is readable."] # [doc = ""] # [doc = " The closure receives a shared reference to the I/O handle."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::TcpListener;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let listener = Async::<TcpListener>::bind(([127, 0, 0, 1], 0))?;"] # [doc = ""] # [doc = " // Accept a new client asynchronously."] # [doc = " let (stream, addr) = listener.read_with(|l| l.accept()).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn read_with < R > (& self , op : impl FnMut (& T) -> io :: Result < R >) -> io :: Result < R > { let mut op = op ; loop { match op (self . get_ref ()) { Err (err) if err . kind () == io :: ErrorKind :: WouldBlock => { } res => return res , } optimistic (self . readable ()) . await ? ; } } # [doc = " Performs a read operation asynchronously."] # [doc = ""] # [doc = " The I/O handle is registered in the reactor and put in non-blocking mode. This method"] # [doc = " invokes the `op` closure in a loop until it succeeds or returns an error other than"] # [doc = " [`io::ErrorKind::WouldBlock`]. In between iterations of the loop, it waits until the OS"] # [doc = " sends a notification that the I/O handle is readable."] # [doc = ""] # [doc = " The closure receives a mutable reference to the I/O handle."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " In the closure, the underlying I/O source must not be dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::TcpListener;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut listener = Async::<TcpListener>::bind(([127, 0, 0, 1], 0))?;"] # [doc = ""] # [doc = " // Accept a new client asynchronously."] # [doc = " let (stream, addr) = unsafe { listener.read_with_mut(|l| l.accept()).await? };"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async unsafe fn read_with_mut < R > (& mut self , op : impl FnMut (& mut T) -> io :: Result < R > ,) -> io :: Result < R > { let mut op = op ; loop { match op (self . get_mut ()) { Err (err) if err . kind () == io :: ErrorKind :: WouldBlock => { } res => return res , } optimistic (self . readable ()) . await ? ; } } # [doc = " Performs a write operation asynchronously."] # [doc = ""] # [doc = " The I/O handle is registered in the reactor and put in non-blocking mode. This method"] # [doc = " invokes the `op` closure in a loop until it succeeds or returns an error other than"] # [doc = " [`io::ErrorKind::WouldBlock`]. In between iterations of the loop, it waits until the OS"] # [doc = " sends a notification that the I/O handle is writable."] # [doc = ""] # [doc = " The closure receives a shared reference to the I/O handle."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::UdpSocket;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let socket = Async::<UdpSocket>::bind(([127, 0, 0, 1], 8000))?;"] # [doc = " socket.get_ref().connect(\"127.0.0.1:9000\")?;"] # [doc = ""] # [doc = " let msg = b\"hello\";"] # [doc = " let len = socket.write_with(|s| s.send(msg)).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async fn write_with < R > (& self , op : impl FnMut (& T) -> io :: Result < R >) -> io :: Result < R > { let mut op = op ; loop { match op (self . get_ref ()) { Err (err) if err . kind () == io :: ErrorKind :: WouldBlock => { } res => return res , } optimistic (self . writable ()) . await ? ; } } # [doc = " Performs a write operation asynchronously."] # [doc = ""] # [doc = " The I/O handle is registered in the reactor and put in non-blocking mode. This method"] # [doc = " invokes the `op` closure in a loop until it succeeds or returns an error other than"] # [doc = " [`io::ErrorKind::WouldBlock`]. In between iterations of the loop, it waits until the OS"] # [doc = " sends a notification that the I/O handle is writable."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The closure receives a mutable reference to the I/O handle. In the closure, the underlying"] # [doc = " I/O source must not be dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_io::Async;"] # [doc = " use std::net::UdpSocket;"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " let mut socket = Async::<UdpSocket>::bind(([127, 0, 0, 1], 8000))?;"] # [doc = " socket.get_ref().connect(\"127.0.0.1:9000\")?;"] # [doc = ""] # [doc = " let msg = b\"hello\";"] # [doc = " let len = unsafe { socket.write_with_mut(|s| s.send(msg)).await? };"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub async unsafe fn write_with_mut < R > (& mut self , op : impl FnMut (& mut T) -> io :: Result < R > ,) -> io :: Result < R > { let mut op = op ; loop { match op (self . get_mut ()) { Err (err) if err . kind () == io :: ErrorKind :: WouldBlock => { } res => return res , } optimistic (self . writable ()) . await ? ; } } }
};
}
