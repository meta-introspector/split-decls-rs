// Generated macro for IoSafe (trait)
macro_rules! DepcrateIoSafe {
() => {
// Module: crate
// Provides: {"IoSafe"}
// Dependencies: {}
# [doc = " Types whose I/O trait implementations do not drop the underlying I/O source."] # [doc = ""] # [doc = " The resource contained inside of the [`Async`] cannot be invalidated. This invalidation can"] # [doc = " happen if the inner resource (the [`TcpStream`], [`UnixListener`] or other `T`) is moved out"] # [doc = " and dropped before the [`Async`]. Because of this, functions that grant mutable access to"] # [doc = " the inner type are unsafe, as there is no way to guarantee that the source won't be dropped"] # [doc = " and a dangling handle won't be left behind."] # [doc = ""] # [doc = " Unfortunately this extends to implementations of [`Read`] and [`Write`]. Since methods on those"] # [doc = " traits take `&mut`, there is no guarantee that the implementor of those traits won't move the"] # [doc = " source out while the method is being run."] # [doc = ""] # [doc = " This trait is an antidote to this predicament. By implementing this trait, the user pledges"] # [doc = " that using any I/O traits won't destroy the source. This way, [`Async`] can implement the"] # [doc = " `async` version of these I/O traits, like [`AsyncRead`] and [`AsyncWrite`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Any I/O trait implementations for this type must not drop the underlying I/O source. Traits"] # [doc = " affected by this trait include [`Read`], [`Write`], [`Seek`] and [`BufRead`]."] # [doc = ""] # [doc = " This trait is implemented by default on top of `libstd` types. In addition, it is implemented"] # [doc = " for immutable reference types, as it is impossible to invalidate any outstanding references"] # [doc = " while holding an immutable reference, even with interior mutability. As Rust's current pinning"] # [doc = " system relies on similar guarantees, I believe that this approach is robust."] # [doc = ""] # [doc = " [`BufRead`]: https://doc.rust-lang.org/std/io/trait.BufRead.html"] # [doc = " [`Read`]: https://doc.rust-lang.org/std/io/trait.Read.html"] # [doc = " [`Seek`]: https://doc.rust-lang.org/std/io/trait.Seek.html"] # [doc = " [`Write`]: https://doc.rust-lang.org/std/io/trait.Write.html"] # [doc = ""] # [doc = " [`AsyncRead`]: https://docs.rs/futures-io/latest/futures_io/trait.AsyncRead.html"] # [doc = " [`AsyncWrite`]: https://docs.rs/futures-io/latest/futures_io/trait.AsyncWrite.html"] pub unsafe trait IoSafe { }
};
}
