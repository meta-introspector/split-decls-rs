// Generated macro for detect_showincludes_prefix (function)
macro_rules! Depcrate_compiler_msvcdetect_showincludes_prefix {
() => {
// Module: crate::compiler::msvc
// Provides: {"detect_showincludes_prefix"}
// Dependencies: {}
# [doc = " Detect the prefix included in the output of MSVC's -showIncludes output."] pub async fn detect_showincludes_prefix < T > (creator : & T , exe : & OsStr , is_clang : bool , env : Vec < (OsString , OsString) > , pool : & tokio :: runtime :: Handle ,) -> Result < String > where T : CommandCreatorSync , { let (tempdir , input) = write_temp_file (pool , "test.c" . as_ref () , b"#include \"test.h\"\n" . to_vec ()) . await ? ; let exe = exe . to_os_string () ; let mut creator = creator . clone () ; let pool = pool . clone () ; let header = tempdir . path () . join ("test.h") ; let tempdir = pool . spawn_blocking (move | | { let mut file = File :: create (& header) ? ; file . write_all (b"/* empty */\n") ? ; Ok :: < _ , std :: io :: Error > (tempdir) }) . await ? . context ("Failed to write temporary file") ? ; let mut cmd = creator . new_command_sync (& exe) ; if is_clang { cmd . arg ("--driver-mode=cl") ; } cmd . args (& ["-nologo" , "-showIncludes" , "-c" , "-Fonul" , "-I."]) . arg (& input) . current_dir (tempdir . path ()) . stdout (Stdio :: piped ()) . stderr (Stdio :: null ()) ; for (k , v) in env { cmd . env (k , v) ; } trace ! ("detect_showincludes_prefix: {:?}" , cmd) ; let output = run_input_output (cmd , None) . await ? ; if ! output . status . success () { bail ! ("Failed to detect showIncludes prefix") } let process :: Output { stdout : stdout_bytes , .. } = output ; let stdout = from_local_codepage (& stdout_bytes) . context ("Failed to convert compiler stdout while detecting showIncludes prefix") ? ; for line in stdout . lines () { if ! line . ends_with ("test.h") { continue ; } for (i , c) in line . char_indices () . rev () { if c != ' ' { continue ; } let path = tempdir . path () . join (& line [i + 1 ..]) ; if path . exists () { return Ok (line [..= i] . to_owned ()) ; } } } drop (tempdir) ; debug ! ("failed to detect showIncludes prefix with output: {}" , stdout) ; bail ! ("Failed to detect showIncludes prefix") }
};
}
