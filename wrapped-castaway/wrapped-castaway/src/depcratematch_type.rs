// Generated macro for match_type (macro)
macro_rules! Depcratematch_type {
() => {
// Module: crate
// Provides: {"match_type"}
// Dependencies: {}
# [doc = " Match the result of an expression against multiple concrete types."] # [doc = ""] # [doc = " You can write multiple match arms in the following syntax:"] # [doc = ""] # [doc = " ```no_compile"] # [doc = " TYPE as name => { /* expression */ }"] # [doc = " ```"] # [doc = ""] # [doc = " If the concrete type matches the given type, then the value will be cast to"] # [doc = " that type and bound to the given variable name. The expression on the"] # [doc = " right-hand side of the match is then executed and returned as the result of"] # [doc = " the entire match expression."] # [doc = ""] # [doc = " The name following the `as` keyword can be any [irrefutable"] # [doc = " pattern](https://doc.rust-lang.org/stable/reference/patterns.html#refutability)."] # [doc = " Like `match` or `let` expressions, you can use an underscore to prevent"] # [doc = " warnings if you don't use the casted value, such as `_value` or just `_`."] # [doc = ""] # [doc = " Since it would be impossible to exhaustively list all possible types of an"] # [doc = " expression, you **must** include a final default match arm. The default"] # [doc = " match arm does not specify a type:"] # [doc = ""] # [doc = " ```no_compile"] # [doc = " name => { /* expression */ }"] # [doc = " ```"] # [doc = ""] # [doc = " The original expression will be bound to the given variable name without"] # [doc = " being casted. If you don't care about the original value, the default arm"] # [doc = " can be:"] # [doc = ""] # [doc = " ```no_compile"] # [doc = " _ => { /* expression */ }"] # [doc = " ```"] # [doc = ""] # [doc = " This macro has all the same rules and restrictions around type casting as"] # [doc = " [`cast`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::fmt::Display;"] # [doc = " use castaway::match_type;"] # [doc = ""] # [doc = " fn to_string<T: Display + 'static>(value: T) -> String {"] # [doc = "     match_type!(value, {"] # [doc = "         String as s => s,"] # [doc = "         &str as s => s.to_string(),"] # [doc = "         s => s.to_string(),"] # [doc = "     })"] # [doc = " }"] # [doc = ""] # [doc = " println!(\"{}\", to_string(\"foo\"));"] # [doc = " ```"] # [macro_export] macro_rules ! match_type { ($ value : expr , { $ T : ty as $ pat : pat => $ branch : expr , $ ($ tail : tt) + }) => { match $ crate :: cast ! ($ value , $ T) { Ok (value) => { let $ pat = value ; $ branch } , Err (value) => $ crate :: match_type ! (value , { $ ($ tail) * }) } } ; ($ value : expr , { $ pat : pat => $ branch : expr $ (,) ? }) => { { let $ pat = $ value ; $ branch } } ; }
};
}
