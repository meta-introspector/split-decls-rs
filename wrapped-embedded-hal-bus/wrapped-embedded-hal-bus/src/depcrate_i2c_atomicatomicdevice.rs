// Generated macro for AtomicDevice (struct)
macro_rules! Depcrate_i2c_atomicAtomicDevice {
() => {
// Module: crate::i2c::atomic
// Provides: {"AtomicDevice"}
// Dependencies: {}
# [doc = " Atomics-based shared bus [`I2c`] implementation."] # [doc = ""] # [doc = " Sharing is implemented with a [`AtomicDevice`], which consists of an `UnsafeCell` and an `AtomicBool` \"locked\" flag."] # [doc = " This means it has low overhead, like [`RefCellDevice`](crate::i2c::RefCellDevice). Additionally, it is `Send`,"] # [doc = " which allows sharing a single bus across multiple threads (interrupt priority level), like [`CriticalSectionDevice`](crate::i2c::CriticalSectionDevice),"] # [doc = " while not using critical sections and therefore impacting real-time performance less."] # [doc = ""] # [doc = " The downside is using a simple `AtomicBool` for locking doesn't prevent two threads from trying to lock it at once."] # [doc = " For example, the main thread can be doing an I2C transaction, and an interrupt fires and tries to do another. In this"] # [doc = " case, a `Busy` error is returned that must be handled somehow, usually dropping the data or trying again later."] # [doc = ""] # [doc = " Note that retrying in a loop on `Busy` errors usually leads to deadlocks. In the above example, it'll prevent the"] # [doc = " interrupt handler from returning, which will starve the main thread and prevent it from releasing the lock. If"] # [doc = " this is an issue for your use case, you most likely should use [`CriticalSectionDevice`](crate::i2c::CriticalSectionDevice) instead."] # [doc = ""] # [doc = " This primitive is particularly well-suited for applications that have external arbitration"] # [doc = " rules that prevent `Busy` errors in the first place, such as the RTIC framework."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Assuming there is a pressure sensor with address `0x42` on the same bus as a temperature sensor"] # [doc = " with address `0x20`; [`AtomicDevice`] can be used to give access to both of these sensors"] # [doc = " from a single `i2c` instance."] # [doc = ""] # [doc = " ```"] # [doc = " use embedded_hal_bus::i2c;"] # [doc = " use embedded_hal_bus::util::AtomicCell;"] # [doc = " # use embedded_hal::i2c::{self as hali2c, SevenBitAddress, TenBitAddress, I2c, Operation, ErrorKind};"] # [doc = " # pub struct Sensor<I2C> {"] # [doc = " #     i2c: I2C,"] # [doc = " #     address: u8,"] # [doc = " # }"] # [doc = " # impl<I2C: I2c> Sensor<I2C> {"] # [doc = " #     pub fn new(i2c: I2C, address: u8) -> Self {"] # [doc = " #         Self { i2c, address }"] # [doc = " #     }"] # [doc = " # }"] # [doc = " # type PressureSensor<I2C> = Sensor<I2C>;"] # [doc = " # type TemperatureSensor<I2C> = Sensor<I2C>;"] # [doc = " # pub struct I2c0;"] # [doc = " # #[derive(Debug, Copy, Clone, Eq, PartialEq)]"] # [doc = " # pub enum Error { }"] # [doc = " # impl hali2c::Error for Error {"] # [doc = " #     fn kind(&self) -> hali2c::ErrorKind {"] # [doc = " #         ErrorKind::Other"] # [doc = " #     }"] # [doc = " # }"] # [doc = " # impl hali2c::ErrorType for I2c0 {"] # [doc = " #     type Error = Error;"] # [doc = " # }"] # [doc = " # impl I2c<SevenBitAddress> for I2c0 {"] # [doc = " #     fn transaction(&mut self, address: u8, operations: &mut [Operation<'_>]) -> Result<(), Self::Error> {"] # [doc = " #       Ok(())"] # [doc = " #     }"] # [doc = " # }"] # [doc = " # struct Hal;"] # [doc = " # impl Hal {"] # [doc = " #   fn i2c(&self) -> I2c0 {"] # [doc = " #     I2c0"] # [doc = " #   }"] # [doc = " # }"] # [doc = " # let hal = Hal;"] # [doc = ""] # [doc = " let i2c = hal.i2c();"] # [doc = " let i2c_cell = AtomicCell::new(i2c);"] # [doc = " let mut temperature_sensor = TemperatureSensor::new("] # [doc = "   i2c::AtomicDevice::new(&i2c_cell),"] # [doc = "   0x20,"] # [doc = " );"] # [doc = " let mut pressure_sensor = PressureSensor::new("] # [doc = "   i2c::AtomicDevice::new(&i2c_cell),"] # [doc = "   0x42,"] # [doc = " );"] # [doc = " ```"] pub struct AtomicDevice < 'a , T > { bus : & 'a AtomicCell < T > , }
};
}
