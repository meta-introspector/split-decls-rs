// Generated macro for AtomicDevice (struct)
macro_rules! Depcrate_spi_atomicAtomicDevice {
() => {
// Module: crate::spi::atomic
// Provides: {"AtomicDevice"}
// Dependencies: {}
# [doc = " Atomics-based shared bus [`SpiDevice`] implementation."] # [doc = ""] # [doc = " This allows for sharing an [`SpiBus`], obtaining multiple [`SpiDevice`] instances,"] # [doc = " each with its own `CS` pin."] # [doc = ""] # [doc = " Sharing is implemented with a [`AtomicDevice`], which consists of an `UnsafeCell` and an `AtomicBool` \"locked\" flag."] # [doc = " This means it has low overhead, like [`RefCellDevice`](crate::spi::RefCellDevice). Additionally, it is `Send`,"] # [doc = " which allows sharing a single bus across multiple threads (interrupt priority level), like [`CriticalSectionDevice`](crate::spi::CriticalSectionDevice),"] # [doc = " while not using critical sections and therefore impacting real-time performance less."] # [doc = ""] # [doc = " The downside is using a simple `AtomicBool` for locking doesn't prevent two threads from trying to lock it at once."] # [doc = " For example, the main thread can be doing a SPI transaction, and an interrupt fires and tries to do another. In this"] # [doc = " case, a `Busy` error is returned that must be handled somehow, usually dropping the data or trying again later."] # [doc = ""] # [doc = " Note that retrying in a loop on `Busy` errors usually leads to deadlocks. In the above example, it'll prevent the"] # [doc = " interrupt handler from returning, which will starve the main thread and prevent it from releasing the lock. If"] # [doc = " this is an issue for your use case, you most likely should use [`CriticalSectionDevice`](crate::spi::CriticalSectionDevice) instead."] # [doc = ""] # [doc = " This primitive is particularly well-suited for applications that have external arbitration"] # [doc = " rules that prevent `Busy` errors in the first place, such as the RTIC framework."] # [cfg_attr (docsrs , doc (cfg (any (feature = "portable-atomic" , target_has_atomic = "8"))))] pub struct AtomicDevice < 'a , BUS , CS , D > { bus : & 'a AtomicCell < BUS > , cs : CS , delay : D , }
};
}
