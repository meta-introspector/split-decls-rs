// Generated macro for wrap_single_line_slow_path (function)
macro_rules! Depcrate_wrapwrap_single_line_slow_path {
() => {
// Module: crate::wrap
// Provides: {"wrap_single_line_slow_path"}
// Dependencies: {}
# [doc = " Wrap a single line of text."] # [doc = ""] # [doc = " This is taken when `line` is longer than `options.width`."] pub (crate) fn wrap_single_line_slow_path < 'a > (line : & 'a str , options : & Options < '_ > , lines : & mut Vec < Cow < 'a , str > > ,) { let initial_width = options . width . saturating_sub (display_width (options . initial_indent)) ; let subsequent_width = options . width . saturating_sub (display_width (options . subsequent_indent)) ; let line_widths = [initial_width , subsequent_width] ; let words = options . word_separator . find_words (line) ; let split_words = split_words (words , & options . word_splitter) ; let broken_words = if options . break_words { let mut broken_words = break_words (split_words , line_widths [1]) ; if ! options . initial_indent . is_empty () { broken_words . insert (0 , Word :: from ("")) ; } broken_words } else { split_words . collect :: < Vec < _ > > () } ; let wrapped_words = options . wrap_algorithm . wrap (& broken_words , & line_widths) ; let mut idx = 0 ; for words in wrapped_words { let last_word = match words . last () { None => { lines . push (Cow :: from ("")) ; continue ; } Some (word) => word , } ; let len = words . iter () . map (| word | word . len () + word . whitespace . len ()) . sum :: < usize > () ; let mut result = if lines . is_empty () && ! options . initial_indent . is_empty () { Cow :: Owned (options . initial_indent . to_owned ()) } else if ! lines . is_empty () && ! options . subsequent_indent . is_empty () { Cow :: Owned (options . subsequent_indent . to_owned ()) } else { Cow :: from ("") } ; if last_word . penalty . is_empty () && options . preserve_trailing_space { result += & line [idx .. idx + len] ; } else { result += & line [idx .. idx + len - last_word . whitespace . len ()] ; } if ! last_word . penalty . is_empty () { result . to_mut () . push_str (last_word . penalty) ; } lines . push (result) ; idx += len ; } }
};
}
