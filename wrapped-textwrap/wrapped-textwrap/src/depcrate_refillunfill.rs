// Generated macro for unfill (function)
macro_rules! Depcrate_refillunfill {
() => {
// Module: crate::refill
// Provides: {"unfill"}
// Dependencies: {}
# [doc = " Unpack a paragraph of already-wrapped text."] # [doc = ""] # [doc = " This function attempts to recover the original text from a single"] # [doc = " paragraph of wrapped text, such as what [`fill()`] would produce."] # [doc = " This means that it turns"] # [doc = ""] # [doc = " ```text"] # [doc = " textwrap: a small"] # [doc = " library for"] # [doc = " wrapping text."] # [doc = " ```"] # [doc = ""] # [doc = " back into"] # [doc = ""] # [doc = " ```text"] # [doc = " textwrap: a small library for wrapping text."] # [doc = " ```"] # [doc = ""] # [doc = " In addition, it will recognize a common prefix and a common line"] # [doc = " ending among the lines."] # [doc = ""] # [doc = " The prefix of the first line is returned in"] # [doc = " [`Options::initial_indent`] and the prefix (if any) of the the"] # [doc = " other lines is returned in [`Options::subsequent_indent`]."] # [doc = ""] # [doc = " Line ending is returned in [`Options::line_ending`]. If line ending"] # [doc = " can not be confidently detected (mixed or no line endings in the"] # [doc = " input), [`LineEnding::LF`] will be returned."] # [doc = ""] # [doc = " In addition to `' '`, the prefixes can consist of characters used"] # [doc = " for unordered lists (`'-'`, `'+'`, and `'*'`) and block quotes"] # [doc = " (`'>'`) in Markdown as well as characters often used for inline"] # [doc = " comments (`'#'` and `'/'`)."] # [doc = ""] # [doc = " The text must come from a single wrapped paragraph. This means"] # [doc = " that there can be no empty lines (`\"\\n\\n\"` or `\"\\r\\n\\r\\n\"`) within"] # [doc = " the text. It is unspecified what happens if `unfill` is called on"] # [doc = " more than one paragraph of text."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use textwrap::{LineEnding, unfill};"] # [doc = ""] # [doc = " let (text, options) = unfill(\"\\"] # [doc = " * This is an"] # [doc = "   example of"] # [doc = "   a list item."] # [doc = " \");"] # [doc = ""] # [doc = " assert_eq!(text, \"This is an example of a list item.\\n\");"] # [doc = " assert_eq!(options.initial_indent, \"* \");"] # [doc = " assert_eq!(options.subsequent_indent, \"  \");"] # [doc = " assert_eq!(options.line_ending, LineEnding::LF);"] # [doc = " ```"] pub fn unfill (text : & str) -> (String , Options < '_ >) { let prefix_chars : & [_] = & [' ' , '-' , '+' , '*' , '>' , '#' , '/'] ; let mut options = Options :: new (0) ; for (idx , line) in text . lines () . enumerate () { options . width = std :: cmp :: max (options . width , display_width (line)) ; let without_prefix = line . trim_start_matches (prefix_chars) ; let prefix = & line [.. line . len () - without_prefix . len ()] ; if idx == 0 { options . initial_indent = prefix ; } else if idx == 1 { options . subsequent_indent = prefix ; } else if idx > 1 { for ((idx , x) , y) in prefix . char_indices () . zip (options . subsequent_indent . chars ()) { if x != y { options . subsequent_indent = & prefix [.. idx] ; break ; } } if prefix . len () < options . subsequent_indent . len () { options . subsequent_indent = prefix ; } } } let mut unfilled = String :: with_capacity (text . len ()) ; let mut detected_line_ending = None ; for (idx , (line , ending)) in NonEmptyLines (text) . enumerate () { if idx == 0 { unfilled . push_str (& line [options . initial_indent . len () ..]) ; } else { unfilled . push (' ') ; unfilled . push_str (& line [options . subsequent_indent . len () ..]) ; } match (detected_line_ending , ending) { (None , Some (_)) => detected_line_ending = ending , (Some (LineEnding :: CRLF) , Some (LineEnding :: LF)) => detected_line_ending = ending , _ => () , } } if let Some (line_ending) = detected_line_ending { if text . ends_with (line_ending . as_str ()) { unfilled . push_str (line_ending . as_str ()) ; } } options . line_ending = detected_line_ending . unwrap_or (LineEnding :: LF) ; (unfilled , options) }
};
}
