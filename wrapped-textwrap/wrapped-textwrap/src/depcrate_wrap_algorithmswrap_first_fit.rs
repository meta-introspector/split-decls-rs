// Generated macro for wrap_first_fit (function)
macro_rules! Depcrate_wrap_algorithmswrap_first_fit {
() => {
// Module: crate::wrap_algorithms
// Provides: {"wrap_first_fit"}
// Dependencies: {}
# [doc = " Wrap abstract fragments into lines with a first-fit algorithm."] # [doc = ""] # [doc = " The `line_widths` slice gives the target line width for each line"] # [doc = " (the last slice element is repeated as necessary). This can be"] # [doc = " used to implement hanging indentation."] # [doc = ""] # [doc = " The fragments must already have been split into the desired"] # [doc = " widths, this function will not (and cannot) attempt to split them"] # [doc = " further when arranging them into lines."] # [doc = ""] # [doc = " # First-Fit Algorithm"] # [doc = ""] # [doc = " This implements a simple “greedy” algorithm: accumulate fragments"] # [doc = " one by one and when a fragment no longer fits, start a new line."] # [doc = " There is no look-ahead, we simply take first fit of the fragments"] # [doc = " we find."] # [doc = ""] # [doc = " While fast and predictable, this algorithm can produce poor line"] # [doc = " breaks when a long fragment is moved to a new line, leaving behind"] # [doc = " a large gap:"] # [doc = ""] # [doc = " ```"] # [doc = " use textwrap::core::Word;"] # [doc = " use textwrap::wrap_algorithms::wrap_first_fit;"] # [doc = " use textwrap::WordSeparator;"] # [doc = ""] # [doc = " // Helper to convert wrapped lines to a Vec<String>."] # [doc = " fn lines_to_strings(lines: Vec<&[Word<'_>]>) -> Vec<String> {"] # [doc = "     lines.iter().map(|line| {"] # [doc = "         line.iter().map(|word| &**word).collect::<Vec<_>>().join(\" \")"] # [doc = "     }).collect::<Vec<_>>()"] # [doc = " }"] # [doc = ""] # [doc = " let text = \"These few words will unfortunately not wrap nicely.\";"] # [doc = " let words = WordSeparator::AsciiSpace.find_words(text).collect::<Vec<_>>();"] # [doc = " assert_eq!(lines_to_strings(wrap_first_fit(&words, &[15.0])),"] # [doc = "            vec![\"These few words\","] # [doc = "                 \"will\",  // <-- short line"] # [doc = "                 \"unfortunately\","] # [doc = "                 \"not wrap\","] # [doc = "                 \"nicely.\"]);"] # [doc = ""] # [doc = " // We can avoid the short line if we look ahead:"] # [doc = " #[cfg(feature = \"smawk\")]"] # [doc = " use textwrap::wrap_algorithms::{wrap_optimal_fit, Penalties};"] # [doc = " #[cfg(feature = \"smawk\")]"] # [doc = " assert_eq!(lines_to_strings(wrap_optimal_fit(&words, &[15.0], &Penalties::new()).unwrap()),"] # [doc = "            vec![\"These few\","] # [doc = "                 \"words will\","] # [doc = "                 \"unfortunately\","] # [doc = "                 \"not wrap\","] # [doc = "                 \"nicely.\"]);"] # [doc = " ```"] # [doc = ""] # [doc = " The [`wrap_optimal_fit()`] function was used above to get better"] # [doc = " line breaks. It uses an advanced algorithm which tries to avoid"] # [doc = " short lines. This function is about 4 times faster than"] # [doc = " [`wrap_optimal_fit()`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Imagine you're building a house site and you have a number of"] # [doc = " tasks you need to execute. Things like pour foundation, complete"] # [doc = " framing, install plumbing, electric cabling, install insulation."] # [doc = ""] # [doc = " The construction workers can only work during daytime, so they"] # [doc = " need to pack up everything at night. Because they need to secure"] # [doc = " their tools and move machines back to the garage, this process"] # [doc = " takes much more time than the time it would take them to simply"] # [doc = " switch to another task."] # [doc = ""] # [doc = " You would like to make a list of tasks to execute every day based"] # [doc = " on your estimates. You can model this with a program like this:"] # [doc = ""] # [doc = " ```"] # [doc = " use textwrap::core::{Fragment, Word};"] # [doc = " use textwrap::wrap_algorithms::wrap_first_fit;"] # [doc = ""] # [doc = " #[derive(Debug)]"] # [doc = " struct Task<'a> {"] # [doc = "     name: &'a str,"] # [doc = "     hours: f64,   // Time needed to complete task."] # [doc = "     sweep: f64,   // Time needed for a quick sweep after task during the day."] # [doc = "     cleanup: f64, // Time needed for full cleanup if day ends with this task."] # [doc = " }"] # [doc = ""] # [doc = " impl Fragment for Task<'_> {"] # [doc = "     fn width(&self) -> f64 { self.hours }"] # [doc = "     fn whitespace_width(&self) -> f64 { self.sweep }"] # [doc = "     fn penalty_width(&self) -> f64 { self.cleanup }"] # [doc = " }"] # [doc = ""] # [doc = " // The morning tasks"] # [doc = " let tasks = vec!["] # [doc = "     Task { name: \"Foundation\",  hours: 4.0, sweep: 2.0, cleanup: 3.0 },"] # [doc = "     Task { name: \"Framing\",     hours: 3.0, sweep: 1.0, cleanup: 2.0 },"] # [doc = "     Task { name: \"Plumbing\",    hours: 2.0, sweep: 2.0, cleanup: 2.0 },"] # [doc = "     Task { name: \"Electrical\",  hours: 2.0, sweep: 1.0, cleanup: 2.0 },"] # [doc = "     Task { name: \"Insulation\",  hours: 2.0, sweep: 1.0, cleanup: 2.0 },"] # [doc = "     Task { name: \"Drywall\",     hours: 3.0, sweep: 1.0, cleanup: 2.0 },"] # [doc = "     Task { name: \"Floors\",      hours: 3.0, sweep: 1.0, cleanup: 2.0 },"] # [doc = "     Task { name: \"Countertops\", hours: 1.0, sweep: 1.0, cleanup: 2.0 },"] # [doc = "     Task { name: \"Bathrooms\",   hours: 2.0, sweep: 1.0, cleanup: 2.0 },"] # [doc = " ];"] # [doc = ""] # [doc = " // Fill tasks into days, taking `day_length` into account. The"] # [doc = " // output shows the hours worked per day along with the names of"] # [doc = " // the tasks for that day."] # [doc = " fn assign_days<'a>(tasks: &[Task<'a>], day_length: f64) -> Vec<(f64, Vec<&'a str>)> {"] # [doc = "     let mut days = Vec::new();"] # [doc = "     // Assign tasks to days. The assignment is a vector of slices,"] # [doc = "     // with a slice per day."] # [doc = "     let assigned_days: Vec<&[Task<'a>]> = wrap_first_fit(&tasks, &[day_length]);"] # [doc = "     for day in assigned_days.iter() {"] # [doc = "         let last = day.last().unwrap();"] # [doc = "         let work_hours: f64 = day.iter().map(|t| t.hours + t.sweep).sum();"] # [doc = "         let names = day.iter().map(|t| t.name).collect::<Vec<_>>();"] # [doc = "         days.push((work_hours - last.sweep + last.cleanup, names));"] # [doc = "     }"] # [doc = "     days"] # [doc = " }"] # [doc = ""] # [doc = " // With a single crew working 8 hours a day:"] # [doc = " assert_eq!("] # [doc = "     assign_days(&tasks, 8.0),"] # [doc = "     ["] # [doc = "         (7.0, vec![\"Foundation\"]),"] # [doc = "         (8.0, vec![\"Framing\", \"Plumbing\"]),"] # [doc = "         (7.0, vec![\"Electrical\", \"Insulation\"]),"] # [doc = "         (5.0, vec![\"Drywall\"]),"] # [doc = "         (7.0, vec![\"Floors\", \"Countertops\"]),"] # [doc = "         (4.0, vec![\"Bathrooms\"]),"] # [doc = "     ]"] # [doc = " );"] # [doc = ""] # [doc = " // With two crews working in shifts, 16 hours a day:"] # [doc = " assert_eq!("] # [doc = "     assign_days(&tasks, 16.0),"] # [doc = "     ["] # [doc = "         (14.0, vec![\"Foundation\", \"Framing\", \"Plumbing\"]),"] # [doc = "         (15.0, vec![\"Electrical\", \"Insulation\", \"Drywall\", \"Floors\"]),"] # [doc = "         (6.0, vec![\"Countertops\", \"Bathrooms\"]),"] # [doc = "     ]"] # [doc = " );"] # [doc = " ```"] # [doc = ""] # [doc = " Apologies to anyone who actually knows how to build a house and"] # [doc = " knows how long each step takes :-)"] pub fn wrap_first_fit < 'a , T : Fragment > (fragments : & 'a [T] , line_widths : & [f64]) -> Vec < & 'a [T] > { let default_line_width = line_widths . last () . copied () . unwrap_or (0.0) ; let mut lines = Vec :: new () ; let mut start = 0 ; let mut width = 0.0 ; for (idx , fragment) in fragments . iter () . enumerate () { let line_width = line_widths . get (lines . len ()) . copied () . unwrap_or (default_line_width) ; if width + fragment . width () + fragment . penalty_width () > line_width && idx > start { lines . push (& fragments [start .. idx]) ; start = idx ; width = 0.0 ; } width += fragment . width () + fragment . whitespace_width () ; } lines . push (& fragments [start ..]) ; lines }
};
}
