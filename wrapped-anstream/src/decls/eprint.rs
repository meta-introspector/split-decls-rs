macro_rules! eprint {
    () => {
        # [doc = " Prints to [`stderr`][crate::stderr]."] # [doc = ""] # [doc = " Equivalent to the [`print!`] macro, except that output goes to"] # [doc = " `stderr` instead of `stdout`. See [`print!`] for"] # [doc = " example usage."] # [doc = ""] # [doc = " Use `eprint!` only for error and progress messages. Use `print!`"] # [doc = " instead for the primary output of your program."] # [doc = ""] # [doc = " **NOTE:** Not all `eprint!` calls will be captured in tests like [`std::eprint!`]"] # [doc = " - Capturing will automatically be activated in test binaries"] # [doc = " - Otherwise, only when the `test` feature is enabled"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if writing to `stderr` fails for any reason **except** broken pipe."] # [doc = ""] # [doc = " Writing to non-blocking stdout can cause an error, which will lead"] # [doc = " this macro to panic."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #  #[cfg(feature = \"auto\")] {"] # [doc = " use anstream::eprint;"] # [doc = ""] # [doc = " eprint!(\"Error: Could not complete task\");"] # [doc = " # }"] # [doc = " ```"] # [cfg (feature = "auto")] # [macro_export] macro_rules ! eprint { ($ ($ arg : tt) *) => { { if cfg ! (test) || $ crate :: _macros :: FEATURE_TEST_ACTIVATED { let target_stream = std :: io :: stderr () ; let buffer = $ crate :: _macros :: to_adapted_string (& format_args ! ($ ($ arg) *) , & target_stream) ; :: std :: eprint ! ("{}" , buffer) } else { use std :: io :: Write as _ ; let mut stream = $ crate :: stderr () ; match :: std :: write ! (& mut stream , $ ($ arg) *) { Err (e) if e . kind () != :: std :: io :: ErrorKind :: BrokenPipe => { :: std :: panic ! ("failed printing to stderr: {e}") ; } Err (_) | Ok (_) => { } } } } } ; }
    };
}

eprint!()