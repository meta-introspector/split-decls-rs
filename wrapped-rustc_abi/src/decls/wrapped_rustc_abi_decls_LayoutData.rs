// Generated by add_generated_rust_header! macro at submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs:72 from submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs
// Do not edit this file by hand. Your changes will be overwritten.

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
#[derive(PartialEq, Eq, Hash, Clone)]
#[cfg_attr(feature = "nightly", derive(HashStable_Generic))]
pub struct LayoutData<FieldIdx: Idx, VariantIdx: Idx> {
    #[doc = " Says where the fields are located within the layout."]
    pub fields: FieldsShape<FieldIdx>,
    #[doc = " Encodes information about multi-variant layouts."]
    #[doc = " Even with `Multiple` variants, a layout still has its own fields! Those are then"]
    #[doc = " shared between all variants. One of them will be the discriminant,"]
    #[doc = " but e.g. coroutines can have more."]
    #[doc = ""]
    #[doc = " To access all fields of this layout, both `fields` and the fields of the active variant"]
    #[doc = " must be taken into account."]
    pub variants: Variants<FieldIdx, VariantIdx>,
    #[doc = " The `backend_repr` defines how this data will be represented to the codegen backend,"]
    #[doc = " and encodes value restrictions via `valid_range`."]
    #[doc = ""]
    #[doc = " Note that this is entirely orthogonal to the recursive structure defined by"]
    #[doc = " `variants` and `fields`; for example, `ManuallyDrop<Result<isize, isize>>` has"]
    #[doc = " `IrForm::ScalarPair`! So, even with non-`Memory` `backend_repr`, `fields` and `variants`"]
    #[doc = " have to be taken into account to find all fields of this layout."]
    pub backend_repr: BackendRepr,
    #[doc = " The leaf scalar with the largest number of invalid values"]
    #[doc = " (i.e. outside of its `valid_range`), if it exists."]
    pub largest_niche: Option<Niche>,
    #[doc = " Is this type known to be uninhabted?"]
    #[doc = ""]
    #[doc = " This is separate from BackendRepr because uninhabited return types can affect ABI,"]
    #[doc = " especially in the case of by-pointer struct returns, which allocate stack even when unused."]
    pub uninhabited: bool,
    pub align: AbiAlign,
    pub size: Size,
    #[doc = " The largest alignment explicitly requested with `repr(align)` on this type or any field."]
    #[doc = " Only used on i686-windows, where the argument passing ABI is different when alignment is"]
    #[doc = " requested, even if the requested alignment is equal to the natural alignment."]
    pub max_repr_align: Option<Align>,
    #[doc = " The alignment the type would have, ignoring any `repr(align)` but including `repr(packed)`."]
    #[doc = " Only used on aarch64-linux, where the argument passing ABI ignores the requested alignment"]
    #[doc = " in some cases."]
    pub unadjusted_abi_align: Align,
    #[doc = " The randomization seed based on this type's own repr and its fields."]
    #[doc = ""]
    #[doc = " Since randomization is toggled on a per-crate basis even crates that do not have randomization"]
    #[doc = " enabled should still calculate a seed so that downstream uses can use it to distinguish different"]
    #[doc = " types."]
    #[doc = ""]
    #[doc = " For every T and U for which we do not guarantee that a repr(Rust) `Foo<T>` can be coerced or"]
    #[doc = " transmuted to `Foo<U>` we aim to create probalistically distinct seeds so that Foo can choose"]
    #[doc = " to reorder its fields based on that information. The current implementation is a conservative"]
    #[doc = " approximation of this goal."]
    pub randomization_seed: Hash64,
}
