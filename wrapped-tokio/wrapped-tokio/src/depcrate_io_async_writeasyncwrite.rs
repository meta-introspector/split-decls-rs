// Generated macro for AsyncWrite (trait)
macro_rules! Depcrate_io_async_writeAsyncWrite {
() => {
// Module: crate::io::async_write
// Provides: {"AsyncWrite"}
// Dependencies: {}
# [doc = " Writes bytes asynchronously."] # [doc = ""] # [doc = " This trait is analogous to the [`std::io::Write`] trait, but integrates with"] # [doc = " the asynchronous task system. In particular, the [`poll_write`] method,"] # [doc = " unlike [`Write::write`], will automatically queue the current task for wakeup"] # [doc = " and return if data is not yet available, rather than blocking the calling"] # [doc = " thread."] # [doc = ""] # [doc = " Specifically, this means that the [`poll_write`] function will return one of"] # [doc = " the following:"] # [doc = ""] # [doc = " * `Poll::Ready(Ok(n))` means that `n` bytes of data was immediately"] # [doc = "   written."] # [doc = ""] # [doc = " * `Poll::Pending` means that no data was written from the buffer"] # [doc = "   provided. The I/O object is not currently writable but may become writable"] # [doc = "   in the future. Most importantly, **the current future's task is scheduled"] # [doc = "   to get unparked when the object is writable**. This means that like"] # [doc = "   `Future::poll` you'll receive a notification when the I/O object is"] # [doc = "   writable again."] # [doc = ""] # [doc = " * `Poll::Ready(Err(e))` for other errors are standard I/O errors coming from the"] # [doc = "   underlying object."] # [doc = ""] # [doc = " This trait importantly means that the `write` method only works in the"] # [doc = " context of a future's task. The object may panic if used outside of a task."] # [doc = ""] # [doc = " Utilities for working with `AsyncWrite` values are provided by"] # [doc = " [`AsyncWriteExt`]."] # [doc = ""] # [doc = " [`std::io::Write`]: std::io::Write"] # [doc = " [`Write::write`]: std::io::Write::write()"] # [doc = " [`poll_write`]: AsyncWrite::poll_write()"] # [doc = " [`AsyncWriteExt`]: crate::io::AsyncWriteExt"] pub trait AsyncWrite { # [doc = " Attempt to write bytes from `buf` into the object."] # [doc = ""] # [doc = " On success, returns `Poll::Ready(Ok(num_bytes_written))`. If successful,"] # [doc = " then it must be guaranteed that `n <= buf.len()`. A return value of `0`"] # [doc = " typically means that the underlying object is no longer able to accept"] # [doc = " bytes and will likely not be able to in the future as well, or that the"] # [doc = " buffer provided is empty."] # [doc = ""] # [doc = " If the object is not ready for writing, the method returns"] # [doc = " `Poll::Pending` and arranges for the current task (via"] # [doc = " `cx.waker()`) to receive a notification when the object becomes"] # [doc = " writable or is closed."] fn poll_write (self : Pin < & mut Self > , cx : & mut Context < '_ > , buf : & [u8] ,) -> Poll < Result < usize , io :: Error > > ; # [doc = " Attempts to flush the object, ensuring that any buffered data reach"] # [doc = " their destination."] # [doc = ""] # [doc = " On success, returns `Poll::Ready(Ok(()))`."] # [doc = ""] # [doc = " If flushing cannot immediately complete, this method returns"] # [doc = " `Poll::Pending` and arranges for the current task (via"] # [doc = " `cx.waker()`) to receive a notification when the object can make"] # [doc = " progress towards flushing."] fn poll_flush (self : Pin < & mut Self > , cx : & mut Context < '_ >) -> Poll < Result < () , io :: Error > > ; # [doc = " Initiates or attempts to shut down this writer, returning success when"] # [doc = " the I/O connection has completely shut down."] # [doc = ""] # [doc = " This method is intended to be used for asynchronous shutdown of I/O"] # [doc = " connections. For example this is suitable for implementing shutdown of a"] # [doc = " TLS connection or calling `TcpStream::shutdown` on a proxied connection."] # [doc = " Protocols sometimes need to flush out final pieces of data or otherwise"] # [doc = " perform a graceful shutdown handshake, reading/writing more data as"] # [doc = " appropriate. This method is the hook for such protocols to implement the"] # [doc = " graceful shutdown logic."] # [doc = ""] # [doc = " This `shutdown` method is required by implementers of the"] # [doc = " `AsyncWrite` trait. Wrappers typically just want to proxy this call"] # [doc = " through to the wrapped type, and base types will typically implement"] # [doc = " shutdown logic here or just return `Ok(().into())`. Note that if you're"] # [doc = " wrapping an underlying `AsyncWrite` a call to `shutdown` implies that"] # [doc = " transitively the entire stream has been shut down. After your wrapper's"] # [doc = " shutdown logic has been executed you should shut down the underlying"] # [doc = " stream."] # [doc = ""] # [doc = " Invocation of a `shutdown` implies an invocation of `flush`. Once this"] # [doc = " method returns `Ready` it implies that a flush successfully happened"] # [doc = " before the shutdown happened. That is, callers don't need to call"] # [doc = " `flush` before calling `shutdown`. They can rely that by calling"] # [doc = " `shutdown` any pending buffered data will be written out."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " This function returns a `Poll<io::Result<()>>` classified as such:"] # [doc = ""] # [doc = " * `Poll::Ready(Ok(()))` - indicates that the connection was"] # [doc = "   successfully shut down and is now safe to deallocate/drop/close"] # [doc = "   resources associated with it. This method means that the current task"] # [doc = "   will no longer receive any notifications due to this method and the"] # [doc = "   I/O object itself is likely no longer usable."] # [doc = ""] # [doc = " * `Poll::Pending` - indicates that shutdown is initiated but could"] # [doc = "   not complete just yet. This may mean that more I/O needs to happen to"] # [doc = "   continue this shutdown operation. The current task is scheduled to"] # [doc = "   receive a notification when it's otherwise ready to continue the"] # [doc = "   shutdown operation. When woken up this method should be called again."] # [doc = ""] # [doc = " * `Poll::Ready(Err(e))` - indicates a fatal error has happened with shutdown,"] # [doc = "   indicating that the shutdown operation did not complete successfully."] # [doc = "   This typically means that the I/O object is no longer usable."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function can return normal I/O errors through `Err`, described"] # [doc = " above. Additionally this method may also render the underlying"] # [doc = " `Write::write` method no longer usable (e.g. will return errors in the"] # [doc = " future). It's recommended that once `shutdown` is called the"] # [doc = " `write` method is no longer called."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if not called within the context of a future's"] # [doc = " task."] fn poll_shutdown (self : Pin < & mut Self > , cx : & mut Context < '_ >) -> Poll < Result < () , io :: Error > > ; # [doc = " Like [`poll_write`], except that it writes from a slice of buffers."] # [doc = ""] # [doc = " Data is copied from each buffer in order, with the final buffer"] # [doc = " read from possibly being only partially consumed. This method must"] # [doc = " behave as a call to [`write`] with the buffers concatenated would."] # [doc = ""] # [doc = " The default implementation calls [`poll_write`] with either the first nonempty"] # [doc = " buffer provided, or an empty one if none exists."] # [doc = ""] # [doc = " On success, returns `Poll::Ready(Ok(num_bytes_written))`."] # [doc = ""] # [doc = " If the object is not ready for writing, the method returns"] # [doc = " `Poll::Pending` and arranges for the current task (via"] # [doc = " `cx.waker()`) to receive a notification when the object becomes"] # [doc = " writable or is closed."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " This should be implemented as a single \"atomic\" write action. If any"] # [doc = " data has been partially written, it is wrong to return an error or"] # [doc = " pending."] # [doc = ""] # [doc = " [`poll_write`]: AsyncWrite::poll_write"] fn poll_write_vectored (self : Pin < & mut Self > , cx : & mut Context < '_ > , bufs : & [IoSlice < '_ >] ,) -> Poll < Result < usize , io :: Error > > { let buf = bufs . iter () . find (| b | ! b . is_empty ()) . map_or (& [] [..] , | b | & * * b) ; self . poll_write (cx , buf) } # [doc = " Determines if this writer has an efficient [`poll_write_vectored`]"] # [doc = " implementation."] # [doc = ""] # [doc = " If a writer does not override the default [`poll_write_vectored`]"] # [doc = " implementation, code using it may want to avoid the method all together"] # [doc = " and coalesce writes into a single buffer for higher performance."] # [doc = ""] # [doc = " The default implementation returns `false`."] # [doc = ""] # [doc = " [`poll_write_vectored`]: AsyncWrite::poll_write_vectored"] fn is_write_vectored (& self) -> bool { false } }
};
}
