// Generated macro for quote (function)
macro_rules! Depcrate_writerquote {
() => {
// Module: crate::writer
// Provides: {"quote"}
// Dependencies: {}
# [doc = " Escape quotes `input` and writes the result to `output`."] # [doc = ""] # [doc = " If `input` does not have a `quote`, then the contents of `input` are"] # [doc = " copied verbatim to `output`."] # [doc = ""] # [doc = " If `output` is not big enough to store the fully quoted contents of"] # [doc = " `input`, then `WriteResult::OutputFull` is returned. The `output` buffer"] # [doc = " will require a maximum of storage of `2 * input.len()` in the worst case"] # [doc = " (where every byte is a quote)."] # [doc = ""] # [doc = " In streaming contexts, `quote` should be called in a loop until"] # [doc = " `WriteResult::InputEmpty` is returned. It is possible to write an infinite"] # [doc = " loop if your output buffer is less than 2 bytes in length (the minimum"] # [doc = " storage space required to store an escaped quote)."] # [doc = ""] # [doc = " In addition to the `WriteResult`, the number of consumed bytes from `input`"] # [doc = " and the number of bytes written to `output` are also returned."] # [doc = ""] # [doc = " `quote` is the quote byte and `escape` is the escape byte. If"] # [doc = " `double_quote` is true, then quotes are escaped by doubling them,"] # [doc = " otherwise, quotes are escaped with the `escape` byte."] # [doc = ""] # [doc = " N.B. This function is provided for low level usage. It is called"] # [doc = " automatically if you're using a `Writer`."] pub fn quote (mut input : & [u8] , mut output : & mut [u8] , quote : u8 , escape : u8 , double_quote : bool ,) -> (WriteResult , usize , usize) { let (mut nin , mut nout) = (0 , 0) ; loop { match memchr (quote , input) { None => { let (res , i , o) = write_optimistic (input , output) ; nin += i ; nout += o ; return (res , nin , nout) ; } Some (next_quote) => { let (res , i , o) = write_optimistic (& input [.. next_quote] , output) ; input = & input [i ..] ; output = & mut moving (output) [o ..] ; nin += i ; nout += o ; if let WriteResult :: OutputFull = res { return (res , nin , nout) ; } if double_quote { let (res , o) = write_pessimistic (& [quote , quote] , output) ; if let WriteResult :: OutputFull = res { return (res , nin , nout) ; } nout += o ; output = & mut moving (output) [o ..] ; } else { let (res , o) = write_pessimistic (& [escape , quote] , output) ; if let WriteResult :: OutputFull = res { return (res , nin , nout) ; } nout += o ; output = & mut moving (output) [o ..] ; } nin += 1 ; input = & input [1 ..] ; } } } }
};
}
