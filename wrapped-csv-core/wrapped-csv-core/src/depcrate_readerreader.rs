// Generated macro for Reader (struct)
macro_rules! Depcrate_readerReader {
() => {
// Module: crate::reader
// Provides: {"Reader"}
// Dependencies: {}
# [doc = " A pull based CSV reader."] # [doc = ""] # [doc = " This reader parses CSV data using a finite state machine. Callers can"] # [doc = " extract parsed data incrementally using one of the `read` methods."] # [doc = ""] # [doc = " Note that this CSV reader is somewhat encoding agnostic. The source data"] # [doc = " needs to be at least ASCII compatible. There is no support for specifying"] # [doc = " the full gamut of Unicode delimiters/terminators/quotes/escapes. Instead,"] # [doc = " any byte can be used, although callers probably want to stick to the ASCII"] # [doc = " subset (`<= 0x7F`)."] # [doc = ""] # [doc = " # Usage"] # [doc = ""] # [doc = " A reader has two different ways to read CSV data, each with their own"] # [doc = " trade offs."] # [doc = ""] # [doc = " * `read_field` - Copies a single CSV field into an output buffer while"] # [doc = "   unescaping quotes. This is simple to use and doesn't require storing an"] # [doc = "   entire record contiguously in memory, but it is slower."] # [doc = " * `read_record` - Copies an entire CSV record into an output buffer while"] # [doc = "   unescaping quotes. The ending positions of each field are copied into"] # [doc = "   an additional buffer. This is harder to use and requires larger output"] # [doc = "   buffers, but it is faster than `read_field` since it amortizes more"] # [doc = "   costs."] # [doc = ""] # [doc = " # RFC 4180"] # [doc = ""] # [doc = " [RFC 4180](https://tools.ietf.org/html/rfc4180)"] # [doc = " is the closest thing to a specification for CSV data. Unfortunately,"] # [doc = " CSV data that is seen in the wild can vary significantly. Often, the CSV"] # [doc = " data is outright invalid. Instead of fixing the producers of bad CSV data,"] # [doc = " we have seen fit to make consumers much more flexible in what they accept."] # [doc = " This reader continues that tradition, and therefore, isn't technically"] # [doc = " compliant with RFC 4180. In particular, this reader will never return an"] # [doc = " error and will always find *a* parse."] # [doc = ""] # [doc = " Here are some detailed differences from RFC 4180:"] # [doc = ""] # [doc = " * CRLF, LF and CR are each treated as a single record terminator by"] # [doc = "   default."] # [doc = " * Records are permitted to be of varying length."] # [doc = " * Empty lines (that do not include other whitespace) are ignored."] # [derive (Clone , Debug)] pub struct Reader { # [doc = " A table-based DFA for parsing CSV."] dfa : Dfa , # [doc = " The current DFA state, if the DFA is used."] dfa_state : DfaState , # [doc = " The current NFA state, if the NFA is used."] nfa_state : NfaState , # [doc = " The delimiter that separates fields."] delimiter : u8 , # [doc = " The terminator that separates records."] term : Terminator , # [doc = " The quotation byte."] quote : u8 , # [doc = " Whether to recognize escaped quotes."] escape : Option < u8 > , # [doc = " Whether to recognized doubled quotes."] double_quote : bool , # [doc = " If enabled, lines beginning with this byte are ignored."] comment : Option < u8 > , # [doc = " If enabled (the default), then quotes are respected. When disabled,"] # [doc = " quotes are not treated specially."] quoting : bool , # [doc = " Whether to use the NFA for parsing."] # [doc = ""] # [doc = " Generally this is for debugging. There's otherwise no good reason"] # [doc = " to avoid the DFA."] use_nfa : bool , # [doc = " The current line number."] line : u64 , # [doc = " Whether this parser has ever read anything."] has_read : bool , # [doc = " The current position in the output buffer when reading a record."] output_pos : usize , }
};
}
