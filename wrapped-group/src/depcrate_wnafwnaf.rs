// Generated macro for Wnaf (struct)
macro_rules! Depcrate_wnafWnaf {
() => {
// Module: crate::wnaf
// Provides: {"Wnaf"}
// Dependencies: {}
# [doc = " A \"w-ary non-adjacent form\" scalar multiplication (also known as exponentiation)"] # [doc = " context."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " This struct can be used to implement several patterns:"] # [doc = ""] # [doc = " ## One base, one scalar"] # [doc = ""] # [doc = " For this pattern, you can use a transient `Wnaf` context:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " use group::Wnaf;"] # [doc = ""] # [doc = " let result = Wnaf::new().scalar(&scalar).base(base);"] # [doc = " ```"] # [doc = ""] # [doc = " ## Many bases, one scalar"] # [doc = ""] # [doc = " For this pattern, you create a `Wnaf` context, load the scalar into it, and then"] # [doc = " process each base in turn:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " use group::Wnaf;"] # [doc = ""] # [doc = " let mut wnaf = Wnaf::new();"] # [doc = " let mut wnaf_scalar = wnaf.scalar(&scalar);"] # [doc = " let results: Vec<_> = bases"] # [doc = "     .into_iter()"] # [doc = "     .map(|base| wnaf_scalar.base(base))"] # [doc = "     .collect();"] # [doc = " ```"] # [doc = ""] # [doc = " ## One base, many scalars"] # [doc = ""] # [doc = " For this pattern, you create a `Wnaf` context, load the base into it, and then process"] # [doc = " each scalar in turn:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " use group::Wnaf;"] # [doc = ""] # [doc = " let mut wnaf = Wnaf::new();"] # [doc = " let mut wnaf_base = wnaf.base(base, scalars.len());"] # [doc = " let results: Vec<_> = scalars"] # [doc = "     .iter()"] # [doc = "     .map(|scalar| wnaf_base.scalar(scalar))"] # [doc = "     .collect();"] # [doc = " ```"] # [doc = ""] # [doc = " ## Many bases, many scalars"] # [doc = ""] # [doc = " Say you have `n` bases and `m` scalars, and want to produce `n * m` results. For this"] # [doc = " pattern, you need to cache the w-NAF tables for the bases and then compute the w-NAF"] # [doc = " form of the scalars on the fly for every base, or vice versa:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " use group::Wnaf;"] # [doc = ""] # [doc = " let mut wnaf_contexts: Vec<_> = (0..bases.len()).map(|_| Wnaf::new()).collect();"] # [doc = " let mut wnaf_bases: Vec<_> = wnaf_contexts"] # [doc = "     .iter_mut()"] # [doc = "     .zip(bases)"] # [doc = "     .map(|(wnaf, base)| wnaf.base(base, scalars.len()))"] # [doc = "     .collect();"] # [doc = " let results: Vec<_> = wnaf_bases"] # [doc = "     .iter()"] # [doc = "     .flat_map(|wnaf_base| scalars.iter().map(|scalar| wnaf_base.scalar(scalar)))"] # [doc = "     .collect();"] # [doc = " ```"] # [doc = ""] # [doc = " Alternatively, use the [`WnafBase`] and [`WnafScalar`] types, which enable the various"] # [doc = " tables and w-NAF forms to be cached individually per base and scalar. These types can"] # [doc = " then be directly multiplied without any additional runtime work, at the cost of fixing"] # [doc = " a specific window size (rather than choosing the window size dynamically)."] # [derive (Debug)] pub struct Wnaf < W , B , S > { base : B , scalar : S , window_size : W , }
};
}
