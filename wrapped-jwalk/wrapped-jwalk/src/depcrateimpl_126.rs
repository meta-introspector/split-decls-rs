// Generated macro for impl_126 (impl)
macro_rules! Depcrateimpl_126 {
() => {
// Module: crate
// Provides: {"impl_126"}
// Dependencies: {}
impl < C : ClientState > WalkDirGeneric < C > { # [doc = " Create a builder for a recursive directory iterator starting at the file"] # [doc = " path root. If root is a directory, then it is the first item yielded by"] # [doc = " the iterator. If root is a file, then it is the first and only item"] # [doc = " yielded by the iterator."] # [doc = ""] # [doc = " Note that his iterator can fail on the first element if `into_iter()` is used as it"] # [doc = " has to be infallible. Use [`try_into_iter()`][WalkDirGeneric::try_into_iter()]"] # [doc = " instead for error handling."] pub fn new < P : AsRef < Path > > (root : P) -> Self { WalkDirGeneric { root : root . as_ref () . to_path_buf () , options : WalkDirOptions { sort : false , min_depth : 0 , max_depth : :: std :: usize :: MAX , skip_hidden : true , follow_links : false , parallelism : Parallelism :: RayonDefaultPool { busy_timeout : std :: time :: Duration :: from_secs (1) , } , root_read_dir_state : C :: ReadDirState :: default () , process_read_dir : None , } , } } # [doc = " Try to create an iterator or fail if the rayon threadpool (in any configuration) is busy."] pub fn try_into_iter (self) -> Result < DirEntryIter < C > > { let iter = self . into_iter () ; if iter . read_dir_iter . is_none () { Err (Error :: busy ()) } else { Ok (iter) } } # [doc = " Root path of the walk."] pub fn root (& self) -> & Path { & self . root } # [doc = " Sort entries by `file_name` per directory. Defaults to `false`. Use"] # [doc = " [`process_read_dir`](struct.WalkDirGeneric.html#method.process_read_dir) for custom"] # [doc = " sorting or filtering."] pub fn sort (mut self , sort : bool) -> Self { self . options . sort = sort ; self } # [doc = " Skip hidden entries. Enabled by default."] pub fn skip_hidden (mut self , skip_hidden : bool) -> Self { self . options . skip_hidden = skip_hidden ; self } # [doc = " Follow symbolic links. By default, this is disabled."] # [doc = ""] # [doc = " When `yes` is `true`, symbolic links are followed as if they were normal"] # [doc = " directories and files. If a symbolic link is broken or is involved in a"] # [doc = " loop, an error is yielded."] # [doc = ""] # [doc = " When enabled, the yielded [`DirEntry`] values represent the target of"] # [doc = " the link while the path corresponds to the link. See the [`DirEntry`]"] # [doc = " type for more details."] # [doc = ""] # [doc = " [`DirEntry`]: struct.DirEntry.html"] pub fn follow_links (mut self , follow_links : bool) -> Self { self . options . follow_links = follow_links ; self } # [doc = " Set the minimum depth of entries yielded by the iterator."] # [doc = ""] # [doc = " The smallest depth is `0` and always corresponds to the path given"] # [doc = " to the `new` function on this type. Its direct descendents have depth"] # [doc = " `1`, and their descendents have depth `2`, and so on."] pub fn min_depth (mut self , depth : usize) -> Self { self . options . min_depth = depth ; if self . options . min_depth > self . options . max_depth { self . options . min_depth = self . options . max_depth ; } self } # [doc = " Set the maximum depth of entries yield by the iterator."] # [doc = ""] # [doc = " The smallest depth is `0` and always corresponds to the path given"] # [doc = " to the `new` function on this type. Its direct descendents have depth"] # [doc = " `1`, and their descendents have depth `2`, and so on."] # [doc = ""] # [doc = " A depth < 2 will automatically change `parallelism` to"] # [doc = " `Parallelism::Serial`. Parrallelism happens at the `fs::read_dir` level."] # [doc = " It only makes sense to use multiple threads when reading more then one"] # [doc = " directory."] # [doc = ""] # [doc = " Note that this will not simply filter the entries of the iterator, but"] # [doc = " it will actually avoid descending into directories when the depth is"] # [doc = " exceeded."] pub fn max_depth (mut self , depth : usize) -> Self { self . options . max_depth = depth ; if self . options . max_depth < self . options . min_depth { self . options . max_depth = self . options . min_depth ; } if self . options . max_depth < 2 { self . options . parallelism = Parallelism :: Serial ; } self } # [doc = " Degree of parallelism to use when performing walk. Defaults to"] # [doc = " [`Parallelism::RayonDefaultPool`](enum.Parallelism.html#variant.RayonDefaultPool)."] pub fn parallelism (mut self , parallelism : Parallelism) -> Self { self . options . parallelism = parallelism ; self } # [doc = " Initial ClientState::ReadDirState that is passed to"] # [doc = " [`process_read_dir`](struct.WalkDirGeneric.html#method.process_read_dir)"] # [doc = " when processing root. Defaults to ClientState::ReadDirState::default()."] pub fn root_read_dir_state (mut self , read_dir_state : C :: ReadDirState) -> Self { self . options . root_read_dir_state = read_dir_state ; self } # [doc = " A callback function to process (sort/filter/skip/state) each directory"] # [doc = " of entries before they are yielded. Modify the given array to"] # [doc = " sort/filter entries. Use [`entry.read_children_path ="] # [doc = " None`](struct.DirEntry.html#field.read_children_path) to yield a"] # [doc = " directory entry but skip reading its contents. Use"] # [doc = " [`entry.client_state`](struct.DirEntry.html#field.client_state)"] # [doc = " to store custom state with an entry."] pub fn process_read_dir < F > (mut self , process_by : F) -> Self where F : Fn (Option < usize > , & Path , & mut C :: ReadDirState , & mut Vec < Result < DirEntry < C > > >) + Send + Sync + 'static , { self . options . process_read_dir = Some (Arc :: new (process_by)) ; self } }
};
}
