// Generated macro for Ref (enum)
macro_rules! Depcrate_handshakeRef {
() => {
// Module: crate::handshake
// Provides: {"Ref"}
// Dependencies: {}
# [doc = " A git reference, commonly referred to as 'ref', as returned by a git server before sending a pack."] # [derive (PartialEq , Eq , Debug , Hash , Ord , PartialOrd , Clone)] # [cfg_attr (feature = "serde" , derive (serde :: Serialize , serde :: Deserialize))] pub enum Ref { # [doc = " A ref pointing to a `tag` object, which in turns points to an `object`, usually a commit"] Peeled { # [doc = " The name at which the ref is located, like `refs/tags/1.0`."] full_ref_name : BString , # [doc = " The hash of the tag the ref points to."] tag : gix_hash :: ObjectId , # [doc = " The hash of the object the `tag` points to."] object : gix_hash :: ObjectId , } , # [doc = " A ref pointing to a commit object"] Direct { # [doc = " The name at which the ref is located, like `refs/heads/main` or `refs/tags/v1.0` for lightweight tags."] full_ref_name : BString , # [doc = " The hash of the object the ref points to."] object : gix_hash :: ObjectId , } , # [doc = " A symbolic ref pointing to `target` ref, which in turn, ultimately after possibly following `tag`, points to an `object`"] Symbolic { # [doc = " The name at which the symbolic ref is located, like `HEAD`."] full_ref_name : BString , # [doc = " The path of the ref the symbolic ref points to, like `refs/heads/main`."] # [doc = ""] # [doc = " See issue [#205] for details"] # [doc = ""] # [doc = " [#205]: https://github.com/GitoxideLabs/gitoxide/issues/205"] target : BString , # [doc = " The hash of the annotated tag the ref points to, if present."] # [doc = ""] # [doc = " Note that this field is also `None` if `full_ref_name` is a lightweight tag."] tag : Option < gix_hash :: ObjectId > , # [doc = " The hash of the object the `target` ref ultimately points to."] object : gix_hash :: ObjectId , } , # [doc = " A ref is unborn on the remote and just points to the initial, unborn branch, as is the case in a newly initialized repository"] # [doc = " or dangling symbolic refs."] Unborn { # [doc = " The name at which the ref is located, typically `HEAD`."] full_ref_name : BString , # [doc = " The path of the ref the symbolic ref points to, like `refs/heads/main`, even though the `target` does not yet exist."] target : BString , } , }
};
}
