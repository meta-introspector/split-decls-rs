[package]
name = "runbuild"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1.0"
syn = { version = "2.0", features = ["full", "extra-traits", "visit"] }
quote = "1.0"
flate2 = "1.0"
split-decls-genesis = { path = ".." }
