macro_rules! assert_stream_next {
    () => {
        # [doc = " Assert that the next poll to the provided stream will return"] # [doc = " [`Poll::Ready`](futures_core::task::Poll::Ready) with the provided item."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use core::pin::pin;"] # [doc = ""] # [doc = " use futures::stream;"] # [doc = " use futures_test::future::FutureTestExt;"] # [doc = " use futures_test::assert_stream_pending;"] # [doc = " use futures_test::assert_stream_next;"] # [doc = " use futures_test::assert_stream_done;"] # [doc = ""] # [doc = " let stream = stream::once((async { 5 }).pending_once());"] # [doc = " let mut stream = pin!(stream);"] # [doc = ""] # [doc = " assert_stream_pending!(stream);"] # [doc = " assert_stream_next!(stream, 5);"] # [doc = " assert_stream_done!(stream);"] # [doc = " ```"] # [macro_export] macro_rules ! assert_stream_next { ($ stream : expr , $ item : expr) => { { let mut stream = & mut $ stream ; $ crate :: __private :: assert :: assert_is_unpin_stream (stream) ; let stream = $ crate :: __private :: Pin :: new (stream) ; let mut cx = $ crate :: task :: noop_context () ; match $ crate :: __private :: stream :: Stream :: poll_next (stream , & mut cx) { $ crate :: __private :: task :: Poll :: Ready ($ crate :: __private :: Some (x)) => { assert_eq ! (x , $ item) ; } $ crate :: __private :: task :: Poll :: Ready ($ crate :: __private :: None) => { panic ! ("assertion failed: expected stream to provide item but stream is at its end") ; } $ crate :: __private :: task :: Poll :: Pending => { panic ! ("assertion failed: expected stream to provide item but stream wasn't ready") ; } } } } ; }
    };
}

assert_stream_next!();