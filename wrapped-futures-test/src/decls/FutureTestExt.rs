macro_rules! deps {
    () => {
        InterleavePending!();
        PendingOnce!();
        AssertUnmoved!();
    };
}

macro_rules! FutureTestExt {
    () => {
        deps!();
        # [doc = " Additional combinators for testing futures."] pub trait FutureTestExt : Future { # [doc = " Asserts that the given is not moved after being polled."] # [doc = ""] # [doc = " A check for movement is performed each time the future is polled"] # [doc = " and when `Drop` is called."] # [doc = ""] # [doc = " Aside from keeping track of the location at which the future was first"] # [doc = " polled and providing assertions, this future adds no runtime behavior"] # [doc = " and simply delegates to the child future."] fn assert_unmoved (self) -> AssertUnmoved < Self > where Self : Sized , { AssertUnmoved :: new (self) } # [doc = " Introduces one [`Poll::Pending`](futures_core::task::Poll::Pending)"] # [doc = " before polling the given future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use core::pin::pin;"] # [doc = ""] # [doc = " use futures::task::Poll;"] # [doc = " use futures::future::FutureExt;"] # [doc = " use futures_test::task::noop_context;"] # [doc = " use futures_test::future::FutureTestExt;"] # [doc = ""] # [doc = " let future = (async { 5 }).pending_once();"] # [doc = " let mut future = pin!(future);"] # [doc = ""] # [doc = " let mut cx = noop_context();"] # [doc = ""] # [doc = " assert_eq!(future.poll_unpin(&mut cx), Poll::Pending);"] # [doc = " assert_eq!(future.poll_unpin(&mut cx), Poll::Ready(5));"] # [doc = " ```"] fn pending_once (self) -> PendingOnce < Self > where Self : Sized , { PendingOnce :: new (self) } # [doc = " Runs this future on a dedicated executor running in a background thread."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # futures::executor::block_on(async {"] # [doc = " use futures::channel::oneshot;"] # [doc = " use futures_test::future::FutureTestExt;"] # [doc = ""] # [doc = " let (tx, rx) = oneshot::channel::<i32>();"] # [doc = ""] # [doc = " (async { tx.send(5).unwrap() }).run_in_background();"] # [doc = ""] # [doc = " assert_eq!(rx.await, Ok(5));"] # [doc = " # });"] # [doc = " # std::thread::sleep(std::time::Duration::from_millis(500)); // wait for background threads closed: https://github.com/rust-lang/miri/issues/1371"] # [doc = " ```"] fn run_in_background (self) where Self : Sized + Send + 'static , Self :: Output : Send , { thread :: spawn (| | futures_executor :: block_on (self)) ; } # [doc = " Introduces an extra [`Poll::Pending`](futures_core::task::Poll::Pending)"] # [doc = " in between each call to poll."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use core::pin::pin;"] # [doc = ""] # [doc = " use futures::task::Poll;"] # [doc = " use futures::future::{self, Future};"] # [doc = " use futures_test::task::noop_context;"] # [doc = " use futures_test::future::FutureTestExt;"] # [doc = ""] # [doc = " let future = future::ready(1).interleave_pending();"] # [doc = " let mut future = pin!(future);"] # [doc = ""] # [doc = " let mut cx = noop_context();"] # [doc = ""] # [doc = " assert_eq!(future.as_mut().poll(&mut cx), Poll::Pending);"] # [doc = " assert_eq!(future.as_mut().poll(&mut cx), Poll::Ready(1));"] # [doc = " ```"] fn interleave_pending (self) -> InterleavePending < Self > where Self : Sized , { InterleavePending :: new (self) } }
    };
}

FutureTestExt!()