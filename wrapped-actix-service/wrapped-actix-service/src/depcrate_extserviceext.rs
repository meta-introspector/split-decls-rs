// Generated macro for ServiceExt (trait)
macro_rules! Depcrate_extServiceExt {
() => {
// Module: crate::ext
// Provides: {"ServiceExt"}
// Dependencies: {}
# [doc = " An extension trait for [`Service`]s that provides a variety of convenient adapters."] pub trait ServiceExt < Req > : Service < Req > { # [doc = " Map this service's output to a different type, returning a new service"] # [doc = " of the resulting type."] # [doc = ""] # [doc = " This function is similar to the `Option::map` or `Iterator::map` where"] # [doc = " it will change the type of the underlying service."] # [doc = ""] # [doc = " Note that this function consumes the receiving service and returns a"] # [doc = " wrapped version of it, similar to the existing `map` methods in the"] # [doc = " standard library."] fn map < F , R > (self , f : F) -> Map < Self , F , Req , R > where Self : Sized , F : FnMut (Self :: Response) -> R , { Map :: new (self , f) } # [doc = " Map this service's error to a different error, returning a new service."] # [doc = ""] # [doc = " This function is similar to the `Result::map_err` where it will change"] # [doc = " the error type of the underlying service. For example, this can be useful to"] # [doc = " ensure that services have the same error type."] # [doc = ""] # [doc = " Note that this function consumes the receiving service and returns a"] # [doc = " wrapped version of it."] fn map_err < F , E > (self , f : F) -> MapErr < Self , Req , F , E > where Self : Sized , F : Fn (Self :: Error) -> E , { MapErr :: new (self , f) } # [doc = " Call another service after call to this one has resolved successfully."] # [doc = ""] # [doc = " This function can be used to chain two services together and ensure that the second service"] # [doc = " isn't called until call to the first service have finished. Result of the call to the first"] # [doc = " service is used as an input parameter for the second service's call."] # [doc = ""] # [doc = " Note that this function consumes the receiving service and returns a wrapped version of it."] fn and_then < I , S1 > (self , service : I) -> AndThenService < Self , S1 , Req > where Self : Sized , I : IntoService < S1 , Self :: Response > , S1 : Service < Self :: Response , Error = Self :: Error > , { AndThenService :: new (self , service . into_service ()) } }
};
}
