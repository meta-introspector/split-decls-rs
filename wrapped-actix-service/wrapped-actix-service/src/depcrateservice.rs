// Generated macro for Service (trait)
macro_rules! DepcrateService {
() => {
// Module: crate
// Provides: {"Service"}
// Dependencies: {}
# [doc = " An asynchronous operation from `Request` to a `Response`."] # [doc = ""] # [doc = " The `Service` trait models a request/response interaction, receiving requests and returning"] # [doc = " replies. You can think about a service as a function with one argument that returns some result"] # [doc = " asynchronously. Conceptually, the operation looks like this:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " async fn(Request) -> Result<Response, Err>"] # [doc = " ```"] # [doc = ""] # [doc = " The `Service` trait just generalizes this form. Requests are defined as a generic type parameter"] # [doc = " and responses and other details are defined as associated types on the trait impl. Notice that"] # [doc = " this design means that services can receive many request types and converge them to a single"] # [doc = " response type."] # [doc = ""] # [doc = " Services can also have mutable state that influence computation by using a `Cell`, `RefCell`"] # [doc = " or `Mutex`. Services intentionally do not take `&mut self` to reduce overhead in the"] # [doc = " common cases."] # [doc = ""] # [doc = " `Service` provides a symmetric and uniform API; the same abstractions can be used to represent"] # [doc = " both clients and servers. Services describe only _transformation_ operations which encourage"] # [doc = " simple API surfaces. This leads to simpler design of each service, improves test-ability and"] # [doc = " makes composition easier."] # [doc = ""] # [doc = " ```ignore"] # [doc = " struct MyService;"] # [doc = ""] # [doc = " impl Service<u8> for MyService {"] # [doc = "      type Response = u64;"] # [doc = "      type Error = MyError;"] # [doc = "      type Future = Pin<Box<dyn Future<Output = Result<Self::Response, Self::Error>>>>;"] # [doc = ""] # [doc = "      fn poll_ready(&self, cx: &mut Context<'_>) -> Poll<Result<(), Self::Error>> { ... }"] # [doc = ""] # [doc = "      fn call(&self, req: u8) -> Self::Future { ... }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Sometimes it is not necessary to implement the Service trait. For example, the above service"] # [doc = " could be rewritten as a simple function and passed to [`fn_service`](fn_service())."] # [doc = ""] # [doc = " ```ignore"] # [doc = " async fn my_service(req: u8) -> Result<u64, MyError>;"] # [doc = ""] # [doc = " let svc = fn_service(my_service)"] # [doc = " svc.call(123)"] # [doc = " ```"] pub trait Service < Req > { # [doc = " Responses given by the service."] type Response ; # [doc = " Errors produced by the service when polling readiness or executing call."] type Error ; # [doc = " The future response value."] type Future : Future < Output = Result < Self :: Response , Self :: Error > > ; # [doc = " Returns `Ready` when the service is able to process requests."] # [doc = ""] # [doc = " If the service is at capacity, then `Pending` is returned and the task is notified when the"] # [doc = " service becomes ready again. This function is expected to be called while on a task."] # [doc = ""] # [doc = " This is a best effort implementation. False positives are permitted. It is permitted for"] # [doc = " the service to return `Ready` from a `poll_ready` call and the next invocation of `call`"] # [doc = " results in an error."] # [doc = ""] # [doc = " # Notes"] # [doc = " 1. `poll_ready` might be called on a different task to `call`."] # [doc = " 1. In cases of chained services, `.poll_ready()` is called for all services at once."] fn poll_ready (& self , ctx : & mut task :: Context < '_ >) -> Poll < Result < () , Self :: Error > > ; # [doc = " Process the request and return the response asynchronously."] # [doc = ""] # [doc = " This function is expected to be callable off-task. As such, implementations of `call` should"] # [doc = " take care to not call `poll_ready`. If the service is at capacity and the request is unable"] # [doc = " to be handled, the returned `Future` should resolve to an error."] # [doc = ""] # [doc = " Invoking `call` without first invoking `poll_ready` is permitted. Implementations must be"] # [doc = " resilient to this fact."] fn call (& self , req : Req) -> Self :: Future ; }
};
}
