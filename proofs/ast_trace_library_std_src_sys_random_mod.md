# AST Trace: library/std/src/sys/random/mod.rs

Generated 2 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=41 | LINES=168

```rust
// SRC: library/std/src/sys/random/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/random/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/random/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/random/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/random/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/random/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/random/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/std/src/sys/random/mod.rs
cfg_select! {
    // Tier 1
    any(target_os = "linux", target_os = "android") => {
// MOD DECLARATION: linux (from library/std/src/sys/random/mod.rs)
// REPLACED: mod linux; -> use crate::linux::*;
use crate::linux::*;
        pub use linux::{fill_bytes, hashmap_random_keys};
    }
    target_os = "windows" => {
// MOD DECLARATION: windows (from library/std/src/sys/random/mod.rs)
// REPLACED: mod windows; -> use crate::windows::*;
use crate::windows::*;
        pub use windows::fill_bytes;
    }
    target_vendor = "apple" => {
// MOD DECLARATION: apple (from library/std/src/sys/random/mod.rs)
// REPLACED: mod apple; -> use crate::apple::*;
use crate::apple::*;
        pub use apple::fill_bytes;
    // Others, in alphabetical ordering.
    }
    any(
        target_os = "dragonfly",
        target_os = "freebsd",
        target_os = "haiku",
        target_os = "illumos",
        target_os = "netbsd",
        target_os = "openbsd",
        target_os = "rtems",
        target_os = "solaris",
        target_os = "vita",
        target_os = "nuttx",
    ) => {
// MOD DECLARATION: arc4random (from library/std/src/sys/random/mod.rs)
// REPLACED: mod arc4random; -> use crate::arc4random::*;
use crate::arc4random::*;
        pub use arc4random::fill_bytes;
    }
    target_os = "emscripten" => {
// MOD DECLARATION: getentropy (from library/std/src/sys/random/mod.rs)
// REPLACED: mod getentropy; -> use crate::getentropy::*;
use crate::getentropy::*;
        pub use getentropy::fill_bytes;
    }
    target_os = "espidf" => {
// MOD DECLARATION: espidf (from library/std/src/sys/random/mod.rs)
// REPLACED: mod espidf; -> use crate::espidf::*;
use crate::espidf::*;
        pub use espidf::fill_bytes;
    }
    target_os = "fuchsia" => {
// MOD DECLARATION: fuchsia (from library/std/src/sys/random/mod.rs)
// REPLACED: mod fuchsia; -> use crate::fuchsia::*;
use crate::fuchsia::*;
        pub use fuchsia::fill_bytes;
    }
    target_os = "hermit" => {
// MOD DECLARATION: hermit (from library/std/src/sys/random/mod.rs)
// REPLACED: mod hermit; -> use crate::hermit::*;
use crate::hermit::*;
        pub use hermit::fill_bytes;
    }
    any(target_os = "horizon", target_os = "cygwin") => {
        // FIXME(horizon): add arc4random_buf to shim-3ds
// MOD DECLARATION: getrandom (from library/std/src/sys/random/mod.rs)
// REPLACED: mod getrandom; -> use crate::getrandom::*;
use crate::getrandom::*;
        pub use getrandom::fill_bytes;
    }
    any(
        target_os = "aix",
        target_os = "hurd",
        target_os = "l4re",
        target_os = "nto",
    ) => {
// MOD DECLARATION: unix_legacy (from library/std/src/sys/random/mod.rs)
// REPLACED: mod unix_legacy; -> use crate::unix_legacy::*;
use crate::unix_legacy::*;
        pub use unix_legacy::fill_bytes;
    }
    target_os = "redox" => {
// MOD DECLARATION: redox (from library/std/src/sys/random/mod.rs)
// REPLACED: mod redox; -> use crate::redox::*;
use crate::redox::*;
        pub use redox::fill_bytes;
    }
    all(target_vendor = "fortanix", target_env = "sgx") => {
// MOD DECLARATION: sgx (from library/std/src/sys/random/mod.rs)
// REPLACED: mod sgx; -> use crate::sgx::*;
use crate::sgx::*;
        pub use sgx::fill_bytes;
    }
    target_os = "solid_asp3" => {
// MOD DECLARATION: solid (from library/std/src/sys/random/mod.rs)
// REPLACED: mod solid; -> use crate::solid::*;
use crate::solid::*;
        pub use solid::fill_bytes;
    }
    target_os = "teeos" => {
// MOD DECLARATION: teeos (from library/std/src/sys/random/mod.rs)
// REPLACED: mod teeos; -> use crate::teeos::*;
use crate::teeos::*;
        pub use teeos::fill_bytes;
    }
    target_os = "trusty" => {
// MOD DECLARATION: trusty (from library/std/src/sys/random/mod.rs)
// REPLACED: mod trusty; -> use crate::trusty::*;
use crate::trusty::*;
        pub use trusty::fill_bytes;
    }
    target_os = "uefi" => {
// MOD DECLARATION: uefi (from library/std/src/sys/random/mod.rs)
// REPLACED: mod uefi; -> use crate::uefi::*;
use crate::uefi::*;
        pub use uefi::fill_bytes;
    }
    target_os = "vxworks" => {
// MOD DECLARATION: vxworks (from library/std/src/sys/random/mod.rs)
// REPLACED: mod vxworks; -> use crate::vxworks::*;
use crate::vxworks::*;
        pub use vxworks::fill_bytes;
    }
    all(target_os = "wasi", target_env = "p1") => {
// MOD DECLARATION: wasip1 (from library/std/src/sys/random/mod.rs)
// REPLACED: mod wasip1; -> use crate::wasip1::*;
use crate::wasip1::*;
        pub use wasip1::fill_bytes;
    }
    all(target_os = "wasi", target_env = "p2") => {
// MOD DECLARATION: wasip2 (from library/std/src/sys/random/mod.rs)
// REPLACED: mod wasip2; -> use crate::wasip2::*;
use crate::wasip2::*;
        pub use wasip2::{fill_bytes, hashmap_random_keys};
    }
    target_os = "zkvm" => {
// MOD DECLARATION: zkvm (from library/std/src/sys/random/mod.rs)
// REPLACED: mod zkvm; -> use crate::zkvm::*;
use crate::zkvm::*;
        pub use zkvm::fill_bytes;
    }
    any(
        all(target_family = "wasm", target_os = "unknown"),
        target_os = "xous",
    ) => {
        // FIXME: finally remove std support for wasm32-unknown-unknown
        // FIXME: add random data generation to xous
// MOD DECLARATION: unsupported (from library/std/src/sys/random/mod.rs)
// REPLACED: mod unsupported; -> use crate::unsupported::*;
use crate::unsupported::*;
        pub use unsupported::{fill_bytes, hashmap_random_keys};
    }
    _ => {}
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=hashmap_random_keys | COMPLEXITY=2 | LINES=15

```rust
#[cfg(not(any(
    target_os = "linux",
    target_os = "android",
    all(target_family = "wasm", target_os = "unknown"),
    all(target_os = "wasi", target_env = "p2"),
    target_os = "xous",
)))]
pub fn hashmap_random_keys() -> (u64, u64) {
    let mut buf = [0; 16];
    fill_bytes(&mut buf);
    let k1 = u64::from_ne_bytes(buf[..8].try_into().unwrap());
    let k2 = u64::from_ne_bytes(buf[8..].try_into().unwrap());
    (k1, k2)
}
```

---
*Generated by AST tracing system*
