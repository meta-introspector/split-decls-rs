# AST Trace: ../rust/compiler/rustc_errors/src/codes.rs

Generated 4 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=STRUCT | NAME=ErrCode | COMPLEXITY=6 | LINES=15

```rust
// This module defines the following.
// - The `ErrCode` type.
// - A constant for every error code, with a name like `E0123`.
// - A static table `DIAGNOSTICS` pairing every error code constant with its
//   long description text.

use std::fmt;

crate::rustc_index::newtype_index! {
    #[max = 9999] // Because all error codes have four digits.
    #[orderable]
    #[encodable]
    #[debug_format = "ErrCode({})"]
    pub struct ErrCode {}
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=fmt | COMPLEXITY=6 | LINES=6

```rust
impl fmt::Display for ErrCode {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "E{:04}", self.as_u32())
    }
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=8 | LINES=18

```rust
crate::rustc_error_messages::into_diag_arg_using_display!(ErrCode);

macro_rules! define_error_code_constants_and_diagnostics_table {
    ($($name:ident: $num:literal,)*) => (
        $(
            pub const $name: $crate::ErrCode = $crate::ErrCode::from_u32($num);
        )*
        pub static DIAGNOSTICS: &[($crate::ErrCode, &str)] = &[
            $( (
                $name,
                include_str!(
                    concat!("../../rustc_error_codes/src/error_codes/", stringify!($name), ".md")
                )
            ), )*
        ];
    )
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=1 | LINES=2

```rust
rustc_error_codes::error_codes!(define_error_code_constants_and_diagnostics_table);
```

---
*Generated by AST tracing system*
