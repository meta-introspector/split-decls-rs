# AST Trace: ../rust/library/std/src/sys/thread/unsupported.rs

Generated 6 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=Thread(!); | COMPLEXITY=10 | LINES=23

```rust
use crate::ffi::CStr;
use crate::io;
use crate::num::NonZero;
use crate::time::Duration;

pub struct Thread(!);

pub const DEFAULT_MIN_STACK_SIZE: usize = 64 * 1024;

impl Thread {
    // unsafe: see thread::Builder::spawn_unchecked for safety requirements
    pub unsafe fn new(
        _stack: usize,
        _name: Option<&str>,
        _p: Box<dyn FnOnce()>,
    ) -> io::Result<Thread> {
        Err(io::Error::UNSUPPORTED_PLATFORM)
    }

    pub fn join(self) {
        self.0
    }
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=available_parallelism | COMPLEXITY=2 | LINES=4

```rust
pub fn available_parallelism() -> io::Result<NonZero<usize>> {
    Err(io::Error::UNKNOWN_THREAD_COUNT)
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=current_os_id | COMPLEXITY=2 | LINES=4

```rust
pub fn current_os_id() -> Option<u64> {
    None
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=yield_now | COMPLEXITY=2 | LINES=4

```rust
pub fn yield_now() {
    // do nothing
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=set_name | COMPLEXITY=2 | LINES=4

```rust
pub fn set_name(_name: &CStr) {
    // nope
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=sleep | COMPLEXITY=2 | LINES=4

```rust
pub fn sleep(_dur: Duration) {
    panic!("can't sleep");
}
```

---
*Generated by AST tracing system*
