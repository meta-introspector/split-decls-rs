# AST Trace: library/std/tests/sync/mpsc_sync.rs

Generated 55 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=3 | LINES=20

```rust
// SRC: library/std/tests/sync/mpsc_sync.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/mpsc_sync.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/mpsc_sync.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/mpsc_sync.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/mpsc_sync.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/mpsc_sync.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/mpsc_sync.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/std/tests/sync/mpsc_sync.rs
use std::rc::Rc;
use std::sync::mpmc::SendTimeoutError;
use std::sync::mpsc::*;
use std::time::Duration;
use std::{env, thread};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=stress_factor | COMPLEXITY=6 | LINES=7

```rust
pub fn stress_factor() -> usize {
    match env::var("RUST_TEST_STRESS") {
        Ok(val) => val.parse().unwrap(),
        Err(..) => 1,
    }
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=smoke | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn smoke() {
    let (tx, rx) = sync_channel::<i32>(1);
    tx.send(1).unwrap();
    assert_eq!(rx.recv().unwrap(), 1);
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=drop_full | COMPLEXITY=2 | LINES=6

```rust
#[test]
fn drop_full() {
    let (tx, _rx) = sync_channel::<Box<isize>>(1);
    tx.send(Box::new(1)).unwrap();
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=smoke_shared | COMPLEXITY=2 | LINES=10

```rust
#[test]
fn smoke_shared() {
    let (tx, rx) = sync_channel::<i32>(1);
    tx.send(1).unwrap();
    assert_eq!(rx.recv().unwrap(), 1);
    let tx = tx.clone();
    tx.send(1).unwrap();
    assert_eq!(rx.recv().unwrap(), 1);
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=recv_timeout | COMPLEXITY=2 | LINES=8

```rust
#[test]
fn recv_timeout() {
    let (tx, rx) = sync_channel::<i32>(1);
    assert_eq!(rx.recv_timeout(Duration::from_millis(1)), Err(RecvTimeoutError::Timeout));
    tx.send(1).unwrap();
    assert_eq!(rx.recv_timeout(Duration::from_millis(1)), Ok(1));
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=send_timeout | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn send_timeout() {
    let (tx, _rx) = sync_channel::<i32>(1);
    assert_eq!(tx.send_timeout(1, Duration::from_millis(1)), Ok(()));
    assert_eq!(tx.send_timeout(1, Duration::from_millis(1)), Err(SendTimeoutError::Timeout(1)));
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=smoke_threads | COMPLEXITY=3 | LINES=9

```rust
#[test]
fn smoke_threads() {
    let (tx, rx) = sync_channel::<i32>(0);
    let _t = thread::spawn(move || {
        tx.send(1).unwrap();
    });
    assert_eq!(rx.recv().unwrap(), 1);
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=smoke_port_gone | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn smoke_port_gone() {
    let (tx, rx) = sync_channel::<i32>(0);
    drop(rx);
    assert!(tx.send(1).is_err());
}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=FUNCTION | NAME=smoke_shared_port_gone2 | COMPLEXITY=2 | LINES=9

```rust
#[test]
fn smoke_shared_port_gone2() {
    let (tx, rx) = sync_channel::<i32>(0);
    drop(rx);
    let tx2 = tx.clone();
    drop(tx);
    assert!(tx2.send(1).is_err());
}
```

## Block 11
**Metadata**: AST_ID=11 | TYPE=FUNCTION | NAME=port_gone_concurrent | COMPLEXITY=6 | LINES=9

```rust
#[test]
fn port_gone_concurrent() {
    let (tx, rx) = sync_channel::<i32>(0);
    let _t = thread::spawn(move || {
        rx.recv().unwrap();
    });
    while tx.send(1).is_ok() {}
}
```

## Block 12
**Metadata**: AST_ID=12 | TYPE=FUNCTION | NAME=port_gone_concurrent_shared | COMPLEXITY=6 | LINES=10

```rust
#[test]
fn port_gone_concurrent_shared() {
    let (tx, rx) = sync_channel::<i32>(0);
    let tx2 = tx.clone();
    let _t = thread::spawn(move || {
        rx.recv().unwrap();
    });
    while tx.send(1).is_ok() && tx2.send(1).is_ok() {}
}
```

## Block 13
**Metadata**: AST_ID=13 | TYPE=FUNCTION | NAME=smoke_chan_gone | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn smoke_chan_gone() {
    let (tx, rx) = sync_channel::<i32>(0);
    drop(tx);
    assert!(rx.recv().is_err());
}
```

## Block 14
**Metadata**: AST_ID=14 | TYPE=FUNCTION | NAME=smoke_chan_gone_shared | COMPLEXITY=2 | LINES=9

```rust
#[test]
fn smoke_chan_gone_shared() {
    let (tx, rx) = sync_channel::<()>(0);
    let tx2 = tx.clone();
    drop(tx);
    drop(tx2);
    assert!(rx.recv().is_err());
}
```

## Block 15
**Metadata**: AST_ID=15 | TYPE=FUNCTION | NAME=chan_gone_concurrent | COMPLEXITY=6 | LINES=10

```rust
#[test]
fn chan_gone_concurrent() {
    let (tx, rx) = sync_channel::<i32>(0);
    thread::spawn(move || {
        tx.send(1).unwrap();
        tx.send(1).unwrap();
    });
    while rx.recv().is_ok() {}
}
```

## Block 16
**Metadata**: AST_ID=16 | TYPE=FUNCTION | NAME=stress | COMPLEXITY=13 | LINES=14

```rust
#[test]
fn stress() {
    let count = if cfg!(miri) { 100 } else { 10000 };
    let (tx, rx) = sync_channel::<i32>(0);
    thread::spawn(move || {
        for _ in 0..count {
            tx.send(1).unwrap();
        }
    });
    for _ in 0..count {
        assert_eq!(rx.recv().unwrap(), 1);
    }
}
```

## Block 17
**Metadata**: AST_ID=17 | TYPE=FUNCTION | NAME=stress_recv_timeout_two_threads | COMPLEXITY=19 | LINES=26

```rust
#[test]
fn stress_recv_timeout_two_threads() {
    let count = if cfg!(miri) { 100 } else { 10000 };
    let (tx, rx) = sync_channel::<i32>(0);

    thread::spawn(move || {
        for _ in 0..count {
            tx.send(1).unwrap();
        }
    });

    let mut recv_count = 0;
    loop {
        match rx.recv_timeout(Duration::from_millis(1)) {
            Ok(v) => {
                assert_eq!(v, 1);
                recv_count += 1;
            }
            Err(RecvTimeoutError::Timeout) => continue,
            Err(RecvTimeoutError::Disconnected) => break,
        }
    }

    assert_eq!(recv_count, count);
}
```

## Block 18
**Metadata**: AST_ID=18 | TYPE=FUNCTION | NAME=stress_recv_timeout_shared | COMPLEXITY=23 | LINES=40

```rust
#[test]
fn stress_recv_timeout_shared() {
    const AMT: u32 = if cfg!(miri) { 100 } else { 1000 };
    const NTHREADS: u32 = 8;
    let (tx, rx) = sync_channel::<i32>(0);
    let (dtx, drx) = sync_channel::<()>(0);

    thread::spawn(move || {
        let mut recv_count = 0;
        loop {
            match rx.recv_timeout(Duration::from_millis(10)) {
                Ok(v) => {
                    assert_eq!(v, 1);
                    recv_count += 1;
                }
                Err(RecvTimeoutError::Timeout) => continue,
                Err(RecvTimeoutError::Disconnected) => break,
            }
        }

        assert_eq!(recv_count, AMT * NTHREADS);
        assert!(rx.try_recv().is_err());

        dtx.send(()).unwrap();
    });

    for _ in 0..NTHREADS {
        let tx = tx.clone();
        thread::spawn(move || {
            for _ in 0..AMT {
                tx.send(1).unwrap();
            }
        });
    }

    drop(tx);

    drx.recv().unwrap();
}
```

## Block 19
**Metadata**: AST_ID=19 | TYPE=FUNCTION | NAME=stress_shared | COMPLEXITY=23 | LINES=30

```rust
#[test]
fn stress_shared() {
    const AMT: u32 = if cfg!(miri) { 100 } else { 1000 };
    const NTHREADS: u32 = 8;
    let (tx, rx) = sync_channel::<i32>(0);
    let (dtx, drx) = sync_channel::<()>(0);

    thread::spawn(move || {
        for _ in 0..AMT * NTHREADS {
            assert_eq!(rx.recv().unwrap(), 1);
        }
        match rx.try_recv() {
            Ok(..) => panic!(),
            _ => {}
        }
        dtx.send(()).unwrap();
    });

    for _ in 0..NTHREADS {
        let tx = tx.clone();
        thread::spawn(move || {
            for _ in 0..AMT {
                tx.send(1).unwrap();
            }
        });
    }
    drop(tx);
    drx.recv().unwrap();
}
```

## Block 20
**Metadata**: AST_ID=20 | TYPE=FUNCTION | NAME=oneshot_single_thread_close_port_first | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_close_port_first() {
    // Simple test of closing without sending
    let (_tx, rx) = sync_channel::<i32>(0);
    drop(rx);
}
```

## Block 21
**Metadata**: AST_ID=21 | TYPE=FUNCTION | NAME=oneshot_single_thread_close_chan_first | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_close_chan_first() {
    // Simple test of closing without sending
    let (tx, _rx) = sync_channel::<i32>(0);
    drop(tx);
}
```

## Block 22
**Metadata**: AST_ID=22 | TYPE=FUNCTION | NAME=oneshot_single_thread_send_port_close | COMPLEXITY=4 | LINES=8

```rust
#[test]
fn oneshot_single_thread_send_port_close() {
    // Testing that the sender cleans up the payload if receiver is closed
    let (tx, rx) = sync_channel::<Box<i32>>(0);
    drop(rx);
    assert!(tx.send(Box::new(0)).is_err());
}
```

## Block 23
**Metadata**: AST_ID=23 | TYPE=FUNCTION | NAME=oneshot_single_thread_recv_chan_close | COMPLEXITY=3 | LINES=13

```rust
#[test]
fn oneshot_single_thread_recv_chan_close() {
    // Receiving on a closed chan will panic
    let res = thread::spawn(move || {
        let (tx, rx) = sync_channel::<i32>(0);
        drop(tx);
        rx.recv().unwrap();
    })
    .join();
    // What is our res?
    assert!(res.is_err());
}
```

## Block 24
**Metadata**: AST_ID=24 | TYPE=FUNCTION | NAME=oneshot_single_thread_send_then_recv | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_send_then_recv() {
    let (tx, rx) = sync_channel::<Box<i32>>(1);
    tx.send(Box::new(10)).unwrap();
    assert!(*rx.recv().unwrap() == 10);
}
```

## Block 25
**Metadata**: AST_ID=25 | TYPE=FUNCTION | NAME=oneshot_single_thread_try_send_open | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_try_send_open() {
    let (tx, rx) = sync_channel::<i32>(1);
    assert_eq!(tx.try_send(10), Ok(()));
    assert!(rx.recv().unwrap() == 10);
}
```

## Block 26
**Metadata**: AST_ID=26 | TYPE=FUNCTION | NAME=oneshot_single_thread_try_send_closed | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_try_send_closed() {
    let (tx, rx) = sync_channel::<i32>(0);
    drop(rx);
    assert_eq!(tx.try_send(10), Err(TrySendError::Disconnected(10)));
}
```

## Block 27
**Metadata**: AST_ID=27 | TYPE=FUNCTION | NAME=oneshot_single_thread_try_send_closed2 | COMPLEXITY=2 | LINES=6

```rust
#[test]
fn oneshot_single_thread_try_send_closed2() {
    let (tx, _rx) = sync_channel::<i32>(0);
    assert_eq!(tx.try_send(10), Err(TrySendError::Full(10)));
}
```

## Block 28
**Metadata**: AST_ID=28 | TYPE=FUNCTION | NAME=oneshot_single_thread_try_recv_open | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_try_recv_open() {
    let (tx, rx) = sync_channel::<i32>(1);
    tx.send(10).unwrap();
    assert!(rx.recv() == Ok(10));
}
```

## Block 29
**Metadata**: AST_ID=29 | TYPE=FUNCTION | NAME=oneshot_single_thread_try_recv_closed | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_try_recv_closed() {
    let (tx, rx) = sync_channel::<i32>(0);
    drop(tx);
    assert!(rx.recv().is_err());
}
```

## Block 30
**Metadata**: AST_ID=30 | TYPE=FUNCTION | NAME=oneshot_single_thread_try_recv_closed_with_data | COMPLEXITY=2 | LINES=9

```rust
#[test]
fn oneshot_single_thread_try_recv_closed_with_data() {
    let (tx, rx) = sync_channel::<i32>(1);
    tx.send(10).unwrap();
    drop(tx);
    assert_eq!(rx.try_recv(), Ok(10));
    assert_eq!(rx.try_recv(), Err(TryRecvError::Disconnected));
}
```

## Block 31
**Metadata**: AST_ID=31 | TYPE=FUNCTION | NAME=oneshot_single_thread_peek_data | COMPLEXITY=2 | LINES=8

```rust
#[test]
fn oneshot_single_thread_peek_data() {
    let (tx, rx) = sync_channel::<i32>(1);
    assert_eq!(rx.try_recv(), Err(TryRecvError::Empty));
    tx.send(10).unwrap();
    assert_eq!(rx.try_recv(), Ok(10));
}
```

## Block 32
**Metadata**: AST_ID=32 | TYPE=FUNCTION | NAME=oneshot_single_thread_peek_close | COMPLEXITY=2 | LINES=8

```rust
#[test]
fn oneshot_single_thread_peek_close() {
    let (tx, rx) = sync_channel::<i32>(0);
    drop(tx);
    assert_eq!(rx.try_recv(), Err(TryRecvError::Disconnected));
    assert_eq!(rx.try_recv(), Err(TryRecvError::Disconnected));
}
```

## Block 33
**Metadata**: AST_ID=33 | TYPE=FUNCTION | NAME=oneshot_single_thread_peek_open | COMPLEXITY=2 | LINES=6

```rust
#[test]
fn oneshot_single_thread_peek_open() {
    let (_tx, rx) = sync_channel::<i32>(0);
    assert_eq!(rx.try_recv(), Err(TryRecvError::Empty));
}
```

## Block 34
**Metadata**: AST_ID=34 | TYPE=FUNCTION | NAME=oneshot_multi_task_recv_then_send | COMPLEXITY=3 | LINES=10

```rust
#[test]
fn oneshot_multi_task_recv_then_send() {
    let (tx, rx) = sync_channel::<Box<i32>>(0);
    let _t = thread::spawn(move || {
        assert!(*rx.recv().unwrap() == 10);
    });

    tx.send(Box::new(10)).unwrap();
}
```

## Block 35
**Metadata**: AST_ID=35 | TYPE=FUNCTION | NAME=oneshot_multi_task_recv_then_close | COMPLEXITY=4 | LINES=13

```rust
#[test]
fn oneshot_multi_task_recv_then_close() {
    let (tx, rx) = sync_channel::<Box<i32>>(0);
    let _t = thread::spawn(move || {
        drop(tx);
    });
    let res = thread::spawn(move || {
        assert!(*rx.recv().unwrap() == 10);
    })
    .join();
    assert!(res.is_err());
}
```

## Block 36
**Metadata**: AST_ID=36 | TYPE=FUNCTION | NAME=oneshot_multi_thread_close_stress | COMPLEXITY=6 | LINES=11

```rust
#[test]
fn oneshot_multi_thread_close_stress() {
    for _ in 0..stress_factor() {
        let (tx, rx) = sync_channel::<i32>(0);
        let _t = thread::spawn(move || {
            drop(rx);
        });
        drop(tx);
    }
}
```

## Block 37
**Metadata**: AST_ID=37 | TYPE=FUNCTION | NAME=oneshot_multi_thread_send_close_stress | COMPLEXITY=7 | LINES=14

```rust
#[test]
fn oneshot_multi_thread_send_close_stress() {
    for _ in 0..stress_factor() {
        let (tx, rx) = sync_channel::<i32>(0);
        let _t = thread::spawn(move || {
            drop(rx);
        });
        let _ = thread::spawn(move || {
            tx.send(1).unwrap();
        })
        .join();
    }
}
```

## Block 38
**Metadata**: AST_ID=38 | TYPE=FUNCTION | NAME=oneshot_multi_thread_recv_close_stress | COMPLEXITY=9 | LINES=19

```rust
#[test]
fn oneshot_multi_thread_recv_close_stress() {
    for _ in 0..stress_factor() {
        let (tx, rx) = sync_channel::<i32>(0);
        let _t = thread::spawn(move || {
            let res = thread::spawn(move || {
                rx.recv().unwrap();
            })
            .join();
            assert!(res.is_err());
        });
        let _t = thread::spawn(move || {
            thread::spawn(move || {
                drop(tx);
            });
        });
    }
}
```

## Block 39
**Metadata**: AST_ID=39 | TYPE=FUNCTION | NAME=oneshot_multi_thread_send_recv_stress | COMPLEXITY=6 | LINES=11

```rust
#[test]
fn oneshot_multi_thread_send_recv_stress() {
    for _ in 0..stress_factor() {
        let (tx, rx) = sync_channel::<Box<i32>>(0);
        let _t = thread::spawn(move || {
            tx.send(Box::new(10)).unwrap();
        });
        assert!(*rx.recv().unwrap() == 10);
    }
}
```

## Block 40
**Metadata**: AST_ID=40 | TYPE=FUNCTION | NAME=stream_send_recv_stress | COMPLEXITY=16 | LINES=32

```rust
#[test]
fn stream_send_recv_stress() {
    for _ in 0..stress_factor() {
        let (tx, rx) = sync_channel::<Box<i32>>(0);

        send(tx, 0);
        recv(rx, 0);

        fn send(tx: SyncSender<Box<i32>>, i: i32) {
            if i == 10 {
                return;
            }

            thread::spawn(move || {
                tx.send(Box::new(i)).unwrap();
                send(tx, i + 1);
            });
        }

        fn recv(rx: Receiver<Box<i32>>, i: i32) {
            if i == 10 {
                return;
            }

            thread::spawn(move || {
                assert!(*rx.recv().unwrap() == i);
                recv(rx, i + 1);
            });
        }
    }
}
```

## Block 41
**Metadata**: AST_ID=41 | TYPE=FUNCTION | NAME=recv_a_lot | COMPLEXITY=12 | LINES=13

```rust
#[test]
fn recv_a_lot() {
    let count = if cfg!(miri) { 1000 } else { 10000 };
    // Regression test that we don't run out of stack in scheduler context
    let (tx, rx) = sync_channel(count);
    for _ in 0..count {
        tx.send(()).unwrap();
    }
    for _ in 0..count {
        rx.recv().unwrap();
    }
}
```

## Block 42
**Metadata**: AST_ID=42 | TYPE=FUNCTION | NAME=shared_chan_stress | COMPLEXITY=9 | LINES=16

```rust
#[test]
fn shared_chan_stress() {
    let (tx, rx) = sync_channel(0);
    let total = stress_factor() + 100;
    for _ in 0..total {
        let tx = tx.clone();
        thread::spawn(move || {
            tx.send(()).unwrap();
        });
    }

    for _ in 0..total {
        rx.recv().unwrap();
    }
}
```

## Block 43
**Metadata**: AST_ID=43 | TYPE=FUNCTION | NAME=test_nested_recv_iter | COMPLEXITY=6 | LINES=20

```rust
#[test]
fn test_nested_recv_iter() {
    let (tx, rx) = sync_channel::<i32>(0);
    let (total_tx, total_rx) = sync_channel::<i32>(0);

    let _t = thread::spawn(move || {
        let mut acc = 0;
        for x in rx.iter() {
            acc += x;
        }
        total_tx.send(acc).unwrap();
    });

    tx.send(3).unwrap();
    tx.send(1).unwrap();
    tx.send(2).unwrap();
    drop(tx);
    assert_eq!(total_rx.recv().unwrap(), 6);
}
```

## Block 44
**Metadata**: AST_ID=44 | TYPE=FUNCTION | NAME=test_recv_iter_break | COMPLEXITY=11 | LINES=25

```rust
#[test]
fn test_recv_iter_break() {
    let (tx, rx) = sync_channel::<i32>(0);
    let (count_tx, count_rx) = sync_channel(0);

    let _t = thread::spawn(move || {
        let mut count = 0;
        for x in rx.iter() {
            if count >= 3 {
                break;
            } else {
                count += x;
            }
        }
        count_tx.send(count).unwrap();
    });

    tx.send(2).unwrap();
    tx.send(2).unwrap();
    tx.send(2).unwrap();
    let _ = tx.try_send(2);
    drop(tx);
    assert_eq!(count_rx.recv().unwrap(), 4);
}
```

## Block 45
**Metadata**: AST_ID=45 | TYPE=FUNCTION | NAME=try_recv_states | COMPLEXITY=4 | LINES=24

```rust
#[test]
fn try_recv_states() {
    let (tx1, rx1) = sync_channel::<i32>(1);
    let (tx2, rx2) = sync_channel::<()>(1);
    let (tx3, rx3) = sync_channel::<()>(1);
    let _t = thread::spawn(move || {
        rx2.recv().unwrap();
        tx1.send(1).unwrap();
        tx3.send(()).unwrap();
        rx2.recv().unwrap();
        drop(tx1);
        tx3.send(()).unwrap();
    });

    assert_eq!(rx1.try_recv(), Err(TryRecvError::Empty));
    tx2.send(()).unwrap();
    rx3.recv().unwrap();
    assert_eq!(rx1.try_recv(), Ok(1));
    assert_eq!(rx1.try_recv(), Err(TryRecvError::Empty));
    tx2.send(()).unwrap();
    rx3.recv().unwrap();
    assert_eq!(rx1.try_recv(), Err(TryRecvError::Disconnected));
}
```

## Block 46
**Metadata**: AST_ID=46 | TYPE=FUNCTION | NAME=destroy_upgraded_shared_port_when_sender_still_active | COMPLEXITY=9 | LINES=25

```rust
// This bug used to end up in a livelock inside of the Receiver destructor
// because the internal state of the Shared packet was corrupted
#[test]
fn destroy_upgraded_shared_port_when_sender_still_active() {
    let (tx, rx) = sync_channel::<()>(0);
    let (tx2, rx2) = sync_channel::<()>(0);
    let _t = thread::spawn(move || {
        rx.recv().unwrap(); // wait on a oneshot
        drop(rx); // destroy a shared
        tx2.send(()).unwrap();
    });
    // make sure the other thread has gone to sleep
    for _ in 0..5000 {
        thread::yield_now();
    }

    // upgrade to a shared chan and send a message
    let t = tx.clone();
    drop(tx);
    t.send(()).unwrap();

    // wait for the child thread to exit before we exit
    rx2.recv().unwrap();
}
```

## Block 47
**Metadata**: AST_ID=47 | TYPE=FUNCTION | NAME=send1 | COMPLEXITY=3 | LINES=9

```rust
#[test]
fn send1() {
    let (tx, rx) = sync_channel::<i32>(0);
    let _t = thread::spawn(move || {
        rx.recv().unwrap();
    });
    assert_eq!(tx.send(1), Ok(()));
}
```

## Block 48
**Metadata**: AST_ID=48 | TYPE=FUNCTION | NAME=send2 | COMPLEXITY=3 | LINES=9

```rust
#[test]
fn send2() {
    let (tx, rx) = sync_channel::<i32>(0);
    let _t = thread::spawn(move || {
        drop(rx);
    });
    assert!(tx.send(1).is_err());
}
```

## Block 49
**Metadata**: AST_ID=49 | TYPE=FUNCTION | NAME=send3 | COMPLEXITY=3 | LINES=10

```rust
#[test]
fn send3() {
    let (tx, rx) = sync_channel::<i32>(1);
    assert_eq!(tx.send(1), Ok(()));
    let _t = thread::spawn(move || {
        drop(rx);
    });
    assert!(tx.send(1).is_err());
}
```

## Block 50
**Metadata**: AST_ID=50 | TYPE=FUNCTION | NAME=send4 | COMPLEXITY=4 | LINES=19

```rust
#[test]
fn send4() {
    let (tx, rx) = sync_channel::<i32>(0);
    let tx2 = tx.clone();
    let (done, donerx) = channel();
    let done2 = done.clone();
    let _t = thread::spawn(move || {
        assert!(tx.send(1).is_err());
        done.send(()).unwrap();
    });
    let _t = thread::spawn(move || {
        assert!(tx2.send(2).is_err());
        done2.send(()).unwrap();
    });
    drop(rx);
    donerx.recv().unwrap();
    donerx.recv().unwrap();
}
```

## Block 51
**Metadata**: AST_ID=51 | TYPE=FUNCTION | NAME=try_send1 | COMPLEXITY=2 | LINES=6

```rust
#[test]
fn try_send1() {
    let (tx, _rx) = sync_channel::<i32>(0);
    assert_eq!(tx.try_send(1), Err(TrySendError::Full(1)));
}
```

## Block 52
**Metadata**: AST_ID=52 | TYPE=FUNCTION | NAME=try_send2 | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn try_send2() {
    let (tx, _rx) = sync_channel::<i32>(1);
    assert_eq!(tx.try_send(1), Ok(()));
    assert_eq!(tx.try_send(1), Err(TrySendError::Full(1)));
}
```

## Block 53
**Metadata**: AST_ID=53 | TYPE=FUNCTION | NAME=try_send3 | COMPLEXITY=2 | LINES=8

```rust
#[test]
fn try_send3() {
    let (tx, rx) = sync_channel::<i32>(1);
    assert_eq!(tx.try_send(1), Ok(()));
    drop(rx);
    assert_eq!(tx.try_send(1), Err(TrySendError::Disconnected(1)));
}
```

## Block 54
**Metadata**: AST_ID=54 | TYPE=FUNCTION | NAME=issue_15761 | COMPLEXITY=7 | LINES=20

```rust
#[test]
fn issue_15761() {
    fn repro() {
        let (tx1, rx1) = sync_channel::<()>(3);
        let (tx2, rx2) = sync_channel::<()>(3);

        let _t = thread::spawn(move || {
            rx1.recv().unwrap();
            tx2.try_send(()).unwrap();
        });

        tx1.try_send(()).unwrap();
        rx2.recv().unwrap();
    }

    for _ in 0..100 {
        repro()
    }
}
```

## Block 55
**Metadata**: AST_ID=55 | TYPE=FUNCTION | NAME=drop_unreceived | COMPLEXITY=2 | LINES=12

```rust
#[test]
fn drop_unreceived() {
    let (tx, rx) = sync_channel::<Rc<()>>(1);
    let msg = Rc::new(());
    let weak = Rc::downgrade(&msg);
    assert!(tx.send(msg).is_ok());
    drop(rx);
    // Messages should be dropped immediately when the last receiver is destroyed.
    assert!(weak.upgrade().is_none());
    drop(tx);
}
```

---
*Generated by AST tracing system*
