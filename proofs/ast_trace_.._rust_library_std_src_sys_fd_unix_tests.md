# AST Trace: ../rust/library/std/src/sys/fd/unix/tests.rs

Generated 1 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=limit_vector_count | COMPLEXITY=7 | LINES=12

```rust
use core::mem::ManuallyDrop;

use super::FileDesc;
use crate::io::IoSlice;
use crate::os::unix::io::FromRawFd;

#[test]
fn limit_vector_count() {
    let stdout = ManuallyDrop::new(unsafe { FileDesc::from_raw_fd(1) });
    let bufs = (0..1500).map(|_| IoSlice::new(&[])).collect::<Vec<_>>();
    assert!(stdout.write_vectored(&bufs).is_ok());
}
```

---
*Generated by AST tracing system*
