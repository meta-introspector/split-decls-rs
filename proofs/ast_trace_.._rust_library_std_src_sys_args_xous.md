# AST Trace: ../rust/library/std/src/sys/args/xous.rs

Generated 1 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=args | COMPLEXITY=13 | LINES=20

```rust
pub use super::common::Args;
use crate::sys::pal::os::get_application_parameters;
use crate::sys::pal::os::params::ArgumentList;

pub fn args() -> Args {
    let Some(params) = get_application_parameters() else {
        return Args::new(vec![]);
    };

    for param in params {
        if let Ok(args) = ArgumentList::try_from(&param) {
            let mut parsed_args = vec![];
            for arg in args {
                parsed_args.push(arg.into());
            }
            return Args::new(parsed_args);
        }
    }
    Args::new(vec![])
}
```

---
*Generated by AST tracing system*
