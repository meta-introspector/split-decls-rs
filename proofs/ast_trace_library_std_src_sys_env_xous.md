# AST Trace: library/std/src/sys/env/xous.rs

Generated 9 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=3 | LINES=20

```rust
// SRC: library/std/src/sys/env/xous.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/env/xous.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/env/xous.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/env/xous.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/env/xous.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/env/xous.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/env/xous.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/env/xous.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/std/src/sys/env/xous.rs
pub use super::common::Env;
use crate::collections::HashMap;
use crate::ffi::{OsStr, OsString};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
use crate::io;
use crate::sync::atomic::{Atomic, AtomicUsize, Ordering};
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
use crate::sync::{Mutex, Once};
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
use crate::sys::pal::os::{get_application_parameters, params};
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=get_env_store | COMPLEXITY=21 | LINES=23

```rust
static ENV: Atomic<usize> = AtomicUsize::new(0);
static ENV_INIT: Once = Once::new();
type EnvStore = Mutex<HashMap<OsString, OsString>>;

fn get_env_store() -> &'static EnvStore {
    ENV_INIT.call_once(|| {
        let env_store = EnvStore::default();
        if let Some(params) = get_application_parameters() {
            for param in params {
                if let Ok(envs) = params::EnvironmentBlock::try_from(&param) {
                    let mut env_store = env_store.lock().unwrap();
                    for env in envs {
                        env_store.insert(env.key.into(), env.value.into());
                    }
                    break;
                }
            }
        }
        ENV.store(Box::into_raw(Box::new(env_store)) as _, Ordering::Relaxed)
    });
    unsafe { &*core::ptr::with_exposed_provenance::<EnvStore>(ENV.load(Ordering::Relaxed)) }
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=env | COMPLEXITY=3 | LINES=9

```rust
pub fn env() -> Env {
    let clone_to_vec = |map: &HashMap<OsString, OsString>| -> Vec<_> {
        map.iter().map(|(k, v)| (k.clone(), v.clone())).collect()
    };

    let env = clone_to_vec(&*get_env_store().lock().unwrap());
    Env::new(env)
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=getenv | COMPLEXITY=2 | LINES=4

```rust
pub fn getenv(k: &OsStr) -> Option<OsString> {
    get_env_store().lock().unwrap().get(k).cloned()
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=6 | LINES=6

```rust
pub unsafe fn setenv(k: &OsStr, v: &OsStr) -> io::Result<()> {
    let (k, v) = (k.to_owned(), v.to_owned());
    get_env_store().lock().unwrap().insert(k, v);
    Ok(())
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=6 | LINES=5

```rust
pub unsafe fn unsetenv(k: &OsStr) -> io::Result<()> {
    get_env_store().lock().unwrap().remove(k);
    Ok(())
}
```

---
*Generated by AST tracing system*
