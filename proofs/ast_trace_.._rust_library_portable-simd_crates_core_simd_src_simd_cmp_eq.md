# AST Trace: ../rust/library/portable-simd/crates/core_simd/src/simd/cmp/eq.rs

Generated 8 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=3 | LINES=4

```rust
use crate::simd::{
    LaneCount, Mask, Simd, SimdElement, SupportedLaneCount,
    ptr::{SimdConstPtr, SimdMutPtr},
};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=simd_eq | COMPLEXITY=7 | LINES=14

```rust
/// Parallel `PartialEq`.
pub trait SimdPartialEq {
    /// The mask type returned by each comparison.
    type Mask;

    /// Test if each element is equal to the corresponding element in `other`.
    #[must_use = "method returns a new mask and does not mutate the original value"]
    fn simd_eq(self, other: Self) -> Self::Mask;

    /// Test if each element is not equal to the corresponding element in `other`.
    #[must_use = "method returns a new mask and does not mutate the original value"]
    fn simd_ne(self, other: Self) -> Self::Mask;
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=simd_eq | COMPLEXITY=25 | LINES=27

```rust
macro_rules! impl_number {
    { $($number:ty),* } => {
        $(
        impl<const N: usize> SimdPartialEq for Simd<$number, N>
        where
            LaneCount<N>: SupportedLaneCount,
        {
            type Mask = Mask<<$number as SimdElement>::Mask, N>;

            #[inline]
            fn simd_eq(self, other: Self) -> Self::Mask {
                // Safety: `self` is a vector, and the result of the comparison
                // is always a valid mask.
                unsafe { Mask::from_int_unchecked(core::intrinsics::simd::simd_eq(self, other)) }
            }

            #[inline]
            fn simd_ne(self, other: Self) -> Self::Mask {
                // Safety: `self` is a vector, and the result of the comparison
                // is always a valid mask.
                unsafe { Mask::from_int_unchecked(core::intrinsics::simd::simd_ne(self, other)) }
            }
        }
        )*
    }
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
impl_number! { f32, f64, u8, u16, u32, u64, usize, i8, i16, i32, i64, isize }
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=simd_eq | COMPLEXITY=25 | LINES=27

```rust
macro_rules! impl_mask {
    { $($integer:ty),* } => {
        $(
        impl<const N: usize> SimdPartialEq for Mask<$integer, N>
        where
            LaneCount<N>: SupportedLaneCount,
        {
            type Mask = Self;

            #[inline]
            fn simd_eq(self, other: Self) -> Self::Mask {
                // Safety: `self` is a vector, and the result of the comparison
                // is always a valid mask.
                unsafe { Self::from_int_unchecked(core::intrinsics::simd::simd_eq(self.to_int(), other.to_int())) }
            }

            #[inline]
            fn simd_ne(self, other: Self) -> Self::Mask {
                // Safety: `self` is a vector, and the result of the comparison
                // is always a valid mask.
                unsafe { Self::from_int_unchecked(core::intrinsics::simd::simd_ne(self.to_int(), other.to_int())) }
            }
        }
        )*
    }
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
impl_mask! { i8, i16, i32, i64, isize }
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=simd_eq | COMPLEXITY=6 | LINES=17

```rust
impl<T, const N: usize> SimdPartialEq for Simd<*const T, N>
where
    LaneCount<N>: SupportedLaneCount,
{
    type Mask = Mask<isize, N>;

    #[inline]
    fn simd_eq(self, other: Self) -> Self::Mask {
        self.addr().simd_eq(other.addr())
    }

    #[inline]
    fn simd_ne(self, other: Self) -> Self::Mask {
        self.addr().simd_ne(other.addr())
    }
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=simd_eq | COMPLEXITY=6 | LINES=17

```rust
impl<T, const N: usize> SimdPartialEq for Simd<*mut T, N>
where
    LaneCount<N>: SupportedLaneCount,
{
    type Mask = Mask<isize, N>;

    #[inline]
    fn simd_eq(self, other: Self) -> Self::Mask {
        self.addr().simd_eq(other.addr())
    }

    #[inline]
    fn simd_ne(self, other: Self) -> Self::Mask {
        self.addr().simd_ne(other.addr())
    }
}
```

---
*Generated by AST tracing system*
