# AST Trace: library/alloctests/benches/btree/map.rs

Generated 73 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=3 | LINES=21

```rust
// SRC: library/alloctests/benches/btree/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/benches/btree/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/benches/btree/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/benches/btree/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/benches/btree/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/benches/btree/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/benches/btree/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/alloctests/benches/btree/map.rs
use std::collections::BTreeMap;
use std::ops::RangeBounds;

use rand::Rng;
use rand::seq::SliceRandom;
use test::{Bencher, black_box};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=$name | COMPLEXITY=14 | LINES=25

```rust
macro_rules! map_insert_rand_bench {
    ($name: ident, $n: expr, $map: ident) => {
        #[bench]
        pub fn $name(b: &mut Bencher) {
            let n: u32 = $n;
            let mut map = $map::new();
            // setup
            let mut rng = crate::bench_rng();

            for _ in 0..n {
                let i = rng.random::<u32>() % n;
                map.insert(i, i);
            }

            // measure
            b.iter(|| {
                let k = rng.random::<u32>() % n;
                map.insert(k, k);
                map.remove(&k);
            });
            black_box(map);
        }
    };
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=$name | COMPLEXITY=14 | LINES=23

```rust
macro_rules! map_insert_seq_bench {
    ($name: ident, $n: expr, $map: ident) => {
        #[bench]
        pub fn $name(b: &mut Bencher) {
            let mut map = $map::new();
            let n: usize = $n;
            // setup
            for i in 0..n {
                map.insert(i * 2, i * 2);
            }

            // measure
            let mut i = 1;
            b.iter(|| {
                map.insert(i, i);
                map.remove(&i);
                i = (i + 2) % n;
            });
            black_box(map);
        }
    };
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=$name | COMPLEXITY=14 | LINES=23

```rust
macro_rules! map_from_iter_rand_bench {
    ($name: ident, $n: expr, $map: ident) => {
        #[bench]
        pub fn $name(b: &mut Bencher) {
            let n: u32 = $n;
            // setup
            let mut rng = crate::bench_rng();
            let mut vec = Vec::with_capacity(n as usize);

            for _ in 0..n {
                let i = rng.random::<u32>() % n;
                vec.push((i, i));
            }

            // measure
            b.iter(|| {
                let map: $map<_, _> = vec.iter().copied().collect();
                black_box(map);
            });
        }
    };
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=$name | COMPLEXITY=14 | LINES=21

```rust
macro_rules! map_from_iter_seq_bench {
    ($name: ident, $n: expr, $map: ident) => {
        #[bench]
        pub fn $name(b: &mut Bencher) {
            let n: usize = $n;
            // setup
            let mut vec = Vec::with_capacity(n);

            for i in 0..n {
                vec.push((i, i));
            }

            // measure
            b.iter(|| {
                let map: $map<_, _> = vec.iter().copied().collect();
                black_box(map);
            });
        }
    };
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=$name | COMPLEXITY=14 | LINES=28

```rust
macro_rules! map_find_rand_bench {
    ($name: ident, $n: expr, $map: ident) => {
        #[bench]
        pub fn $name(b: &mut Bencher) {
            let mut map = $map::new();
            let n: u32 = $n;

            // setup
            let mut rng = crate::bench_rng();
            let mut keys: Vec<_> = (0..n).map(|_| rng.random::<u32>() % n).collect();

            for &k in &keys {
                map.insert(k, k);
            }

            keys.shuffle(&mut rng);

            // measure
            let mut i = 0u32;
            b.iter(|| {
                let t = map.get(&keys[i as usize]);
                i = (i + 1) % n;
                black_box(t);
            })
        }
    };
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=$name | COMPLEXITY=14 | LINES=23

```rust
macro_rules! map_find_seq_bench {
    ($name: ident, $n: expr, $map: ident) => {
        #[bench]
        pub fn $name(b: &mut Bencher) {
            let mut map = $map::new();
            let n: usize = $n;

            // setup
            for i in 0..n {
                map.insert(i, i);
            }

            // measure
            let mut i = 0;
            b.iter(|| {
                let x = map.get(&i);
                i = (i + 1) % n;
                black_box(x);
            })
        }
    };
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
map_insert_rand_bench! {insert_rand_100,    100,    BTreeMap}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
map_insert_rand_bench! {insert_rand_10_000, 10_000, BTreeMap}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
map_insert_seq_bench! {insert_seq_100,    100,    BTreeMap}
```

## Block 11
**Metadata**: AST_ID=11 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
map_insert_seq_bench! {insert_seq_10_000, 10_000, BTreeMap}
```

## Block 12
**Metadata**: AST_ID=12 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
map_from_iter_rand_bench! {from_iter_rand_100,    100,    BTreeMap}
```

## Block 13
**Metadata**: AST_ID=13 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
map_from_iter_rand_bench! {from_iter_rand_10_000, 10_000, BTreeMap}
```

## Block 14
**Metadata**: AST_ID=14 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
map_from_iter_seq_bench! {from_iter_seq_100,    100,    BTreeMap}
```

## Block 15
**Metadata**: AST_ID=15 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
map_from_iter_seq_bench! {from_iter_seq_10_000, 10_000, BTreeMap}
```

## Block 16
**Metadata**: AST_ID=16 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
map_find_rand_bench! {find_rand_100,    100,    BTreeMap}
```

## Block 17
**Metadata**: AST_ID=17 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
map_find_rand_bench! {find_rand_10_000, 10_000, BTreeMap}
```

## Block 18
**Metadata**: AST_ID=18 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
map_find_seq_bench! {find_seq_100,    100,    BTreeMap}
```

## Block 19
**Metadata**: AST_ID=19 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
map_find_seq_bench! {find_seq_10_000, 10_000, BTreeMap}
```

## Block 20
**Metadata**: AST_ID=20 | TYPE=FUNCTION | NAME=bench_iteration | COMPLEXITY=9 | LINES=15

```rust
fn bench_iteration(b: &mut Bencher, size: i32) {
    let mut map = BTreeMap::<i32, i32>::new();
    let mut rng = crate::bench_rng();

    for _ in 0..size {
        map.insert(rng.random(), rng.random());
    }

    b.iter(|| {
        for entry in &map {
            black_box(entry);
        }
    });
}
```

## Block 21
**Metadata**: AST_ID=21 | TYPE=FUNCTION | NAME=iteration_20 | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn iteration_20(b: &mut Bencher) {
    bench_iteration(b, 20);
}
```

## Block 22
**Metadata**: AST_ID=22 | TYPE=FUNCTION | NAME=iteration_1000 | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn iteration_1000(b: &mut Bencher) {
    bench_iteration(b, 1000);
}
```

## Block 23
**Metadata**: AST_ID=23 | TYPE=FUNCTION | NAME=iteration_100000 | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn iteration_100000(b: &mut Bencher) {
    bench_iteration(b, 100000);
}
```

## Block 24
**Metadata**: AST_ID=24 | TYPE=FUNCTION | NAME=bench_iteration_mut | COMPLEXITY=9 | LINES=15

```rust
fn bench_iteration_mut(b: &mut Bencher, size: i32) {
    let mut map = BTreeMap::<i32, i32>::new();
    let mut rng = crate::bench_rng();

    for _ in 0..size {
        map.insert(rng.random(), rng.random());
    }

    b.iter(|| {
        for kv in map.iter_mut() {
            black_box(kv);
        }
    });
}
```

## Block 25
**Metadata**: AST_ID=25 | TYPE=FUNCTION | NAME=iteration_mut_20 | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn iteration_mut_20(b: &mut Bencher) {
    bench_iteration_mut(b, 20);
}
```

## Block 26
**Metadata**: AST_ID=26 | TYPE=FUNCTION | NAME=iteration_mut_1000 | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn iteration_mut_1000(b: &mut Bencher) {
    bench_iteration_mut(b, 1000);
}
```

## Block 27
**Metadata**: AST_ID=27 | TYPE=FUNCTION | NAME=iteration_mut_100000 | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn iteration_mut_100000(b: &mut Bencher) {
    bench_iteration_mut(b, 100000);
}
```

## Block 28
**Metadata**: AST_ID=28 | TYPE=FUNCTION | NAME=bench_first_and_last_nightly | COMPLEXITY=6 | LINES=10

```rust
fn bench_first_and_last_nightly(b: &mut Bencher, size: i32) {
    let map: BTreeMap<_, _> = (0..size).map(|i| (i, i)).collect();
    b.iter(|| {
        for _ in 0..10 {
            black_box(map.first_key_value());
            black_box(map.last_key_value());
        }
    });
}
```

## Block 29
**Metadata**: AST_ID=29 | TYPE=FUNCTION | NAME=bench_first_and_last_stable | COMPLEXITY=6 | LINES=10

```rust
fn bench_first_and_last_stable(b: &mut Bencher, size: i32) {
    let map: BTreeMap<_, _> = (0..size).map(|i| (i, i)).collect();
    b.iter(|| {
        for _ in 0..10 {
            black_box(map.iter().next());
            black_box(map.iter().next_back());
        }
    });
}
```

## Block 30
**Metadata**: AST_ID=30 | TYPE=FUNCTION | NAME=first_and_last_0_nightly | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn first_and_last_0_nightly(b: &mut Bencher) {
    bench_first_and_last_nightly(b, 0);
}
```

## Block 31
**Metadata**: AST_ID=31 | TYPE=FUNCTION | NAME=first_and_last_0_stable | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn first_and_last_0_stable(b: &mut Bencher) {
    bench_first_and_last_stable(b, 0);
}
```

## Block 32
**Metadata**: AST_ID=32 | TYPE=FUNCTION | NAME=first_and_last_100_nightly | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn first_and_last_100_nightly(b: &mut Bencher) {
    bench_first_and_last_nightly(b, 100);
}
```

## Block 33
**Metadata**: AST_ID=33 | TYPE=FUNCTION | NAME=first_and_last_100_stable | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn first_and_last_100_stable(b: &mut Bencher) {
    bench_first_and_last_stable(b, 100);
}
```

## Block 34
**Metadata**: AST_ID=34 | TYPE=FUNCTION | NAME=first_and_last_10k_nightly | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn first_and_last_10k_nightly(b: &mut Bencher) {
    bench_first_and_last_nightly(b, 10_000);
}
```

## Block 35
**Metadata**: AST_ID=35 | TYPE=FUNCTION | NAME=first_and_last_10k_stable | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn first_and_last_10k_stable(b: &mut Bencher) {
    bench_first_and_last_stable(b, 10_000);
}
```

## Block 36
**Metadata**: AST_ID=36 | TYPE=FUNCTION | NAME=bench_range | COMPLEXITY=10 | LINES=21

```rust
const BENCH_RANGE_SIZE: i32 = 145;
const BENCH_RANGE_COUNT: i32 = BENCH_RANGE_SIZE * (BENCH_RANGE_SIZE - 1) / 2;

fn bench_range<F, R>(b: &mut Bencher, f: F)
where
    F: Fn(i32, i32) -> R,
    R: RangeBounds<i32>,
{
    let map: BTreeMap<_, _> = (0..BENCH_RANGE_SIZE).map(|i| (i, i)).collect();
    b.iter(|| {
        let mut c = 0;
        for i in 0..BENCH_RANGE_SIZE {
            for j in i + 1..BENCH_RANGE_SIZE {
                let _ = black_box(map.range(f(i, j)));
                c += 1;
            }
        }
        debug_assert_eq!(c, BENCH_RANGE_COUNT);
    });
}
```

## Block 37
**Metadata**: AST_ID=37 | TYPE=FUNCTION | NAME=range_included_excluded | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn range_included_excluded(b: &mut Bencher) {
    bench_range(b, |i, j| i..j);
}
```

## Block 38
**Metadata**: AST_ID=38 | TYPE=FUNCTION | NAME=range_included_included | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn range_included_included(b: &mut Bencher) {
    bench_range(b, |i, j| i..=j);
}
```

## Block 39
**Metadata**: AST_ID=39 | TYPE=FUNCTION | NAME=range_included_unbounded | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn range_included_unbounded(b: &mut Bencher) {
    bench_range(b, |i, _| i..);
}
```

## Block 40
**Metadata**: AST_ID=40 | TYPE=FUNCTION | NAME=range_unbounded_unbounded | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn range_unbounded_unbounded(b: &mut Bencher) {
    bench_range(b, |_, _| ..);
}
```

## Block 41
**Metadata**: AST_ID=41 | TYPE=FUNCTION | NAME=bench_iter | COMPLEXITY=6 | LINES=9

```rust
fn bench_iter(b: &mut Bencher, repeats: i32, size: i32) {
    let map: BTreeMap<_, _> = (0..size).map(|i| (i, i)).collect();
    b.iter(|| {
        for _ in 0..repeats {
            let _ = black_box(map.iter());
        }
    });
}
```

## Block 42
**Metadata**: AST_ID=42 | TYPE=FUNCTION | NAME=range_unbounded_vs_iter | COMPLEXITY=2 | LINES=6

```rust
/// Contrast range_unbounded_unbounded with `iter()`.
#[bench]
pub fn range_unbounded_vs_iter(b: &mut Bencher) {
    bench_iter(b, BENCH_RANGE_COUNT, BENCH_RANGE_SIZE);
}
```

## Block 43
**Metadata**: AST_ID=43 | TYPE=FUNCTION | NAME=iter_0 | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn iter_0(b: &mut Bencher) {
    bench_iter(b, 1_000, 0);
}
```

## Block 44
**Metadata**: AST_ID=44 | TYPE=FUNCTION | NAME=iter_1 | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn iter_1(b: &mut Bencher) {
    bench_iter(b, 1_000, 1);
}
```

## Block 45
**Metadata**: AST_ID=45 | TYPE=FUNCTION | NAME=iter_100 | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn iter_100(b: &mut Bencher) {
    bench_iter(b, 1_000, 100);
}
```

## Block 46
**Metadata**: AST_ID=46 | TYPE=FUNCTION | NAME=iter_10k | COMPLEXITY=2 | LINES=5

```rust
#[bench]
pub fn iter_10k(b: &mut Bencher) {
    bench_iter(b, 1_000, 10_000);
}
```

## Block 47
**Metadata**: AST_ID=47 | TYPE=FUNCTION | NAME=iter_1m | COMPLEXITY=2 | LINES=6

```rust
#[bench]
#[cfg_attr(target_os = "emscripten", ignore)] // hits an OOM
pub fn iter_1m(b: &mut Bencher) {
    bench_iter(b, 1_000, 1_000_000);
}
```

## Block 48
**Metadata**: AST_ID=48 | TYPE=FUNCTION | NAME=slim_map | COMPLEXITY=2 | LINES=8

```rust
const FAT: usize = 256;

// The returned map has small keys and values.
// Benchmarks on it have a counterpart in set.rs with the same keys and no values at all.
fn slim_map(n: usize) -> BTreeMap<usize, usize> {
    (0..n).map(|i| (i, i)).collect::<BTreeMap<_, _>>()
}
```

## Block 49
**Metadata**: AST_ID=49 | TYPE=FUNCTION | NAME=fat_val_map | COMPLEXITY=2 | LINES=5

```rust
// The returned map has small keys and large values.
fn fat_val_map(n: usize) -> BTreeMap<usize, [usize; FAT]> {
    (0..n).map(|i| (i, [i; FAT])).collect::<BTreeMap<_, _>>()
}
```

## Block 50
**Metadata**: AST_ID=50 | TYPE=FUNCTION | NAME=clone_slim_100 | COMPLEXITY=2 | LINES=6

```rust
#[bench]
pub fn clone_slim_100(b: &mut Bencher) {
    let src = slim_map(100);
    b.iter(|| src.clone())
}
```

## Block 51
**Metadata**: AST_ID=51 | TYPE=FUNCTION | NAME=clone_slim_100_and_clear | COMPLEXITY=2 | LINES=6

```rust
#[bench]
pub fn clone_slim_100_and_clear(b: &mut Bencher) {
    let src = slim_map(100);
    b.iter(|| src.clone().clear())
}
```

## Block 52
**Metadata**: AST_ID=52 | TYPE=FUNCTION | NAME=clone_slim_100_and_drain_all | COMPLEXITY=2 | LINES=6

```rust
#[bench]
pub fn clone_slim_100_and_drain_all(b: &mut Bencher) {
    let src = slim_map(100);
    b.iter(|| src.clone().extract_if(.., |_, _| true).count())
}
```

## Block 53
**Metadata**: AST_ID=53 | TYPE=FUNCTION | NAME=clone_slim_100_and_drain_half | COMPLEXITY=3 | LINES=10

```rust
#[bench]
pub fn clone_slim_100_and_drain_half(b: &mut Bencher) {
    let src = slim_map(100);
    b.iter(|| {
        let mut map = src.clone();
        assert_eq!(map.extract_if(.., |i, _| i % 2 == 0).count(), 100 / 2);
        assert_eq!(map.len(), 100 / 2);
    })
}
```

## Block 54
**Metadata**: AST_ID=54 | TYPE=FUNCTION | NAME=clone_slim_100_and_into_iter | COMPLEXITY=2 | LINES=6

```rust
#[bench]
pub fn clone_slim_100_and_into_iter(b: &mut Bencher) {
    let src = slim_map(100);
    b.iter(|| src.clone().into_iter().count())
}
```

## Block 55
**Metadata**: AST_ID=55 | TYPE=FUNCTION | NAME=clone_slim_100_and_pop_all | COMPLEXITY=6 | LINES=10

```rust
#[bench]
pub fn clone_slim_100_and_pop_all(b: &mut Bencher) {
    let src = slim_map(100);
    b.iter(|| {
        let mut map = src.clone();
        while map.pop_first().is_some() {}
        map
    });
}
```

## Block 56
**Metadata**: AST_ID=56 | TYPE=FUNCTION | NAME=clone_slim_100_and_remove_all | COMPLEXITY=6 | LINES=13

```rust
#[bench]
pub fn clone_slim_100_and_remove_all(b: &mut Bencher) {
    let src = slim_map(100);
    b.iter(|| {
        let mut map = src.clone();
        while let Some(elt) = map.iter().map(|(&i, _)| i).next() {
            let v = map.remove(&elt);
            debug_assert!(v.is_some());
        }
        map
    });
}
```

## Block 57
**Metadata**: AST_ID=57 | TYPE=FUNCTION | NAME=clone_slim_100_and_remove_half | COMPLEXITY=6 | LINES=14

```rust
#[bench]
pub fn clone_slim_100_and_remove_half(b: &mut Bencher) {
    let src = slim_map(100);
    b.iter(|| {
        let mut map = src.clone();
        for i in (0..100).step_by(2) {
            let v = map.remove(&i);
            debug_assert!(v.is_some());
        }
        assert_eq!(map.len(), 100 / 2);
        map
    })
}
```

## Block 58
**Metadata**: AST_ID=58 | TYPE=FUNCTION | NAME=clone_slim_10k | COMPLEXITY=2 | LINES=6

```rust
#[bench]
pub fn clone_slim_10k(b: &mut Bencher) {
    let src = slim_map(10_000);
    b.iter(|| src.clone())
}
```

## Block 59
**Metadata**: AST_ID=59 | TYPE=FUNCTION | NAME=clone_slim_10k_and_clear | COMPLEXITY=2 | LINES=6

```rust
#[bench]
pub fn clone_slim_10k_and_clear(b: &mut Bencher) {
    let src = slim_map(10_000);
    b.iter(|| src.clone().clear())
}
```

## Block 60
**Metadata**: AST_ID=60 | TYPE=FUNCTION | NAME=clone_slim_10k_and_drain_all | COMPLEXITY=2 | LINES=6

```rust
#[bench]
pub fn clone_slim_10k_and_drain_all(b: &mut Bencher) {
    let src = slim_map(10_000);
    b.iter(|| src.clone().extract_if(.., |_, _| true).count())
}
```

## Block 61
**Metadata**: AST_ID=61 | TYPE=FUNCTION | NAME=clone_slim_10k_and_drain_half | COMPLEXITY=3 | LINES=10

```rust
#[bench]
pub fn clone_slim_10k_and_drain_half(b: &mut Bencher) {
    let src = slim_map(10_000);
    b.iter(|| {
        let mut map = src.clone();
        assert_eq!(map.extract_if(.., |i, _| i % 2 == 0).count(), 10_000 / 2);
        assert_eq!(map.len(), 10_000 / 2);
    })
}
```

## Block 62
**Metadata**: AST_ID=62 | TYPE=FUNCTION | NAME=clone_slim_10k_and_into_iter | COMPLEXITY=2 | LINES=6

```rust
#[bench]
pub fn clone_slim_10k_and_into_iter(b: &mut Bencher) {
    let src = slim_map(10_000);
    b.iter(|| src.clone().into_iter().count())
}
```

## Block 63
**Metadata**: AST_ID=63 | TYPE=FUNCTION | NAME=clone_slim_10k_and_pop_all | COMPLEXITY=6 | LINES=10

```rust
#[bench]
pub fn clone_slim_10k_and_pop_all(b: &mut Bencher) {
    let src = slim_map(10_000);
    b.iter(|| {
        let mut map = src.clone();
        while map.pop_first().is_some() {}
        map
    });
}
```

## Block 64
**Metadata**: AST_ID=64 | TYPE=FUNCTION | NAME=clone_slim_10k_and_remove_all | COMPLEXITY=6 | LINES=13

```rust
#[bench]
pub fn clone_slim_10k_and_remove_all(b: &mut Bencher) {
    let src = slim_map(10_000);
    b.iter(|| {
        let mut map = src.clone();
        while let Some(elt) = map.iter().map(|(&i, _)| i).next() {
            let v = map.remove(&elt);
            debug_assert!(v.is_some());
        }
        map
    });
}
```

## Block 65
**Metadata**: AST_ID=65 | TYPE=FUNCTION | NAME=clone_slim_10k_and_remove_half | COMPLEXITY=6 | LINES=14

```rust
#[bench]
pub fn clone_slim_10k_and_remove_half(b: &mut Bencher) {
    let src = slim_map(10_000);
    b.iter(|| {
        let mut map = src.clone();
        for i in (0..10_000).step_by(2) {
            let v = map.remove(&i);
            debug_assert!(v.is_some());
        }
        assert_eq!(map.len(), 10_000 / 2);
        map
    })
}
```

## Block 66
**Metadata**: AST_ID=66 | TYPE=FUNCTION | NAME=clone_fat_val_100 | COMPLEXITY=2 | LINES=6

```rust
#[bench]
pub fn clone_fat_val_100(b: &mut Bencher) {
    let src = fat_val_map(100);
    b.iter(|| src.clone())
}
```

## Block 67
**Metadata**: AST_ID=67 | TYPE=FUNCTION | NAME=clone_fat_val_100_and_clear | COMPLEXITY=2 | LINES=6

```rust
#[bench]
pub fn clone_fat_val_100_and_clear(b: &mut Bencher) {
    let src = fat_val_map(100);
    b.iter(|| src.clone().clear())
}
```

## Block 68
**Metadata**: AST_ID=68 | TYPE=FUNCTION | NAME=clone_fat_val_100_and_drain_all | COMPLEXITY=2 | LINES=6

```rust
#[bench]
pub fn clone_fat_val_100_and_drain_all(b: &mut Bencher) {
    let src = fat_val_map(100);
    b.iter(|| src.clone().extract_if(.., |_, _| true).count())
}
```

## Block 69
**Metadata**: AST_ID=69 | TYPE=FUNCTION | NAME=clone_fat_val_100_and_drain_half | COMPLEXITY=3 | LINES=10

```rust
#[bench]
pub fn clone_fat_val_100_and_drain_half(b: &mut Bencher) {
    let src = fat_val_map(100);
    b.iter(|| {
        let mut map = src.clone();
        assert_eq!(map.extract_if(.., |i, _| i % 2 == 0).count(), 100 / 2);
        assert_eq!(map.len(), 100 / 2);
    })
}
```

## Block 70
**Metadata**: AST_ID=70 | TYPE=FUNCTION | NAME=clone_fat_val_100_and_into_iter | COMPLEXITY=2 | LINES=6

```rust
#[bench]
pub fn clone_fat_val_100_and_into_iter(b: &mut Bencher) {
    let src = fat_val_map(100);
    b.iter(|| src.clone().into_iter().count())
}
```

## Block 71
**Metadata**: AST_ID=71 | TYPE=FUNCTION | NAME=clone_fat_val_100_and_pop_all | COMPLEXITY=6 | LINES=10

```rust
#[bench]
pub fn clone_fat_val_100_and_pop_all(b: &mut Bencher) {
    let src = fat_val_map(100);
    b.iter(|| {
        let mut map = src.clone();
        while map.pop_first().is_some() {}
        map
    });
}
```

## Block 72
**Metadata**: AST_ID=72 | TYPE=FUNCTION | NAME=clone_fat_val_100_and_remove_all | COMPLEXITY=6 | LINES=13

```rust
#[bench]
pub fn clone_fat_val_100_and_remove_all(b: &mut Bencher) {
    let src = fat_val_map(100);
    b.iter(|| {
        let mut map = src.clone();
        while let Some(elt) = map.iter().map(|(&i, _)| i).next() {
            let v = map.remove(&elt);
            debug_assert!(v.is_some());
        }
        map
    });
}
```

## Block 73
**Metadata**: AST_ID=73 | TYPE=FUNCTION | NAME=clone_fat_val_100_and_remove_half | COMPLEXITY=6 | LINES=14

```rust
#[bench]
pub fn clone_fat_val_100_and_remove_half(b: &mut Bencher) {
    let src = fat_val_map(100);
    b.iter(|| {
        let mut map = src.clone();
        for i in (0..100).step_by(2) {
            let v = map.remove(&i);
            debug_assert!(v.is_some());
        }
        assert_eq!(map.len(), 100 / 2);
        map
    })
}
```

---
*Generated by AST tracing system*
