# AST Trace: library/std/benches/hash/map.rs

Generated 7 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=new_drop | COMPLEXITY=4 | LINES=30

```rust
// SRC: library/std/benches/hash/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/hash/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/hash/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/hash/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/hash/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/hash/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/hash/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/hash/map.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/std/benches/hash/map.rs
#![cfg(test)]

use std::collections::HashMap;

use test::Bencher;

#[bench]
fn new_drop(b: &mut Bencher) {
    b.iter(|| {
        let m: HashMap<i32, i32> = HashMap::new();
        assert_eq!(m.len(), 0);
    })
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=new_insert_drop | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn new_insert_drop(b: &mut Bencher) {
    b.iter(|| {
        let mut m = HashMap::new();
        m.insert(0, 0);
        assert_eq!(m.len(), 1);
    })
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=grow_by_insertion | COMPLEXITY=6 | LINES=16

```rust
#[bench]
fn grow_by_insertion(b: &mut Bencher) {
    let mut m = HashMap::new();

    for i in 1..1001 {
        m.insert(i, i);
    }

    let mut k = 1001;

    b.iter(|| {
        m.insert(k, k);
        k += 1;
    });
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=find_existing | COMPLEXITY=9 | LINES=15

```rust
#[bench]
fn find_existing(b: &mut Bencher) {
    let mut m = HashMap::new();

    for i in 1..1001 {
        m.insert(i, i);
    }

    b.iter(|| {
        for i in 1..1001 {
            m.contains_key(&i);
        }
    });
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=find_nonexisting | COMPLEXITY=9 | LINES=15

```rust
#[bench]
fn find_nonexisting(b: &mut Bencher) {
    let mut m = HashMap::new();

    for i in 1..1001 {
        m.insert(i, i);
    }

    b.iter(|| {
        for i in 1001..2001 {
            m.contains_key(&i);
        }
    });
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=hashmap_as_queue | COMPLEXITY=6 | LINES=17

```rust
#[bench]
fn hashmap_as_queue(b: &mut Bencher) {
    let mut m = HashMap::new();

    for i in 1..1001 {
        m.insert(i, i);
    }

    let mut k = 1;

    b.iter(|| {
        m.remove(&k);
        m.insert(k + 1000, k + 1000);
        k += 1;
    });
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=get_remove_insert | COMPLEXITY=6 | LINES=19

```rust
#[bench]
fn get_remove_insert(b: &mut Bencher) {
    let mut m = HashMap::new();

    for i in 1..1001 {
        m.insert(i, i);
    }

    let mut k = 1;

    b.iter(|| {
        m.get(&(k + 400));
        m.get(&(k + 2000));
        m.remove(&k);
        m.insert(k + 1000, k + 1000);
        k += 1;
    })
}
```

---
*Generated by AST tracing system*
