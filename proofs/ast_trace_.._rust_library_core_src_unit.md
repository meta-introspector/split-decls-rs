# AST Trace: ../rust/library/core/src/unit.rs

Generated 4 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=from_iter | COMPLEXITY=8 | LINES=19

```rust
/// Collapses all unit items from an iterator into one.
///
/// This is more useful when combined with higher-level abstractions, like
/// collecting to a `Result<(), E>` where you only care about errors:
///
/// ```
/// use std::io::*;
/// let data = vec![1, 2, 3, 4, 5];
/// let res: Result<()> = data.iter()
///     .map(|x| writeln!(stdout(), "{x}"))
///     .collect();
/// assert!(res.is_ok());
/// ```
#[stable(feature = "unit_from_iter", since = "1.23.0")]
impl FromIterator<()> for () {
    fn from_iter<I: IntoIterator<Item = ()>>(iter: I) -> Self {
        iter.into_iter().for_each(|()| {})
    }
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=is_unit | COMPLEXITY=2 | LINES=4

```rust
pub(crate) trait IsUnit {
    fn is_unit() -> bool;
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=5 | LINES=6

```rust
impl<T: ?Sized> IsUnit for T {
    default fn is_unit() -> bool {
        false
    }
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=is_unit | COMPLEXITY=5 | LINES=6

```rust
impl IsUnit for () {
    fn is_unit() -> bool {
        true
    }
}
```

---
*Generated by AST tracing system*
