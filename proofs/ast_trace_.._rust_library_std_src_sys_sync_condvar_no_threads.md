# AST Trace: ../rust/library/std/src/sys/sync/condvar/no_threads.rs

Generated 2 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=STRUCT | NAME=Condvar | COMPLEXITY=2 | LINES=5

```rust
use crate::sys::sync::Mutex;
use crate::thread::sleep;
use crate::time::Duration;

pub struct Condvar {}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=notify_one | COMPLEXITY=16 | LINES=22

```rust
impl Condvar {
    #[inline]
    pub const fn new() -> Condvar {
        Condvar {}
    }

    #[inline]
    pub fn notify_one(&self) {}

    #[inline]
    pub fn notify_all(&self) {}

    pub unsafe fn wait(&self, _mutex: &Mutex) {
        panic!("condvar wait not supported")
    }

    pub unsafe fn wait_timeout(&self, _mutex: &Mutex, dur: Duration) -> bool {
        sleep(dur);
        false
    }
}
```

---
*Generated by AST tracing system*
