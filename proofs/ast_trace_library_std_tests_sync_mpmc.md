# AST Trace: library/std/tests/sync/mpmc.rs

Generated 60 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=3 | LINES=17

```rust
// SRC: library/std/tests/sync/mpmc.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/mpmc.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/mpmc.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/mpmc.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/mpmc.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/mpmc.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/mpmc.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/std/tests/sync/mpmc.rs
use std::sync::mpmc::*;
use std::time::{Duration, Instant};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
use std::{env, thread};
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=stress_factor | COMPLEXITY=6 | LINES=7

```rust
pub fn stress_factor() -> usize {
    match env::var("RUST_TEST_STRESS") {
        Ok(val) => val.parse().unwrap(),
        Err(..) => 1,
    }
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=smoke | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn smoke() {
    let (tx, rx) = channel::<i32>();
    tx.send(1).unwrap();
    assert_eq!(rx.recv().unwrap(), 1);
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=drop_full | COMPLEXITY=2 | LINES=6

```rust
#[test]
fn drop_full() {
    let (tx, _rx) = channel::<Box<isize>>();
    tx.send(Box::new(1)).unwrap();
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=drop_full_shared | COMPLEXITY=2 | LINES=8

```rust
#[test]
fn drop_full_shared() {
    let (tx, _rx) = channel::<Box<isize>>();
    drop(tx.clone());
    drop(tx.clone());
    tx.send(Box::new(1)).unwrap();
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=smoke_shared | COMPLEXITY=2 | LINES=10

```rust
#[test]
fn smoke_shared() {
    let (tx, rx) = channel::<i32>();
    tx.send(1).unwrap();
    assert_eq!(rx.recv().unwrap(), 1);
    let tx = tx.clone();
    tx.send(1).unwrap();
    assert_eq!(rx.recv().unwrap(), 1);
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=smoke_threads | COMPLEXITY=7 | LINES=16

```rust
#[test]
fn smoke_threads() {
    let (tx, rx) = channel::<i32>();
    let t1 = thread::spawn(move || {
        for i in 0..2 {
            tx.send(i).unwrap();
        }
    });
    let t2 = thread::spawn(move || {
        assert_eq!(rx.recv().unwrap(), 0);
        assert_eq!(rx.recv().unwrap(), 1);
    });
    t1.join().unwrap();
    t2.join().unwrap();
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=smoke_port_gone | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn smoke_port_gone() {
    let (tx, rx) = channel::<i32>();
    drop(rx);
    assert!(tx.send(1).is_err());
}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=FUNCTION | NAME=smoke_receiver_clone | COMPLEXITY=2 | LINES=9

```rust
#[test]
fn smoke_receiver_clone() {
    let (tx, rx) = channel::<i32>();
    let rx2 = rx.clone();
    drop(rx);
    tx.send(1).unwrap();
    assert_eq!(rx2.recv().unwrap(), 1);
}
```

## Block 11
**Metadata**: AST_ID=11 | TYPE=FUNCTION | NAME=smoke_receiver_clone_port_gone | COMPLEXITY=2 | LINES=9

```rust
#[test]
fn smoke_receiver_clone_port_gone() {
    let (tx, rx) = channel::<i32>();
    let rx2 = rx.clone();
    drop(rx);
    drop(rx2);
    assert!(tx.send(1).is_err());
}
```

## Block 12
**Metadata**: AST_ID=12 | TYPE=FUNCTION | NAME=smoke_shared_port_gone | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn smoke_shared_port_gone() {
    let (tx, rx) = channel::<i32>();
    drop(rx);
    assert!(tx.send(1).is_err())
}
```

## Block 13
**Metadata**: AST_ID=13 | TYPE=FUNCTION | NAME=smoke_shared_port_gone2 | COMPLEXITY=2 | LINES=9

```rust
#[test]
fn smoke_shared_port_gone2() {
    let (tx, rx) = channel::<i32>();
    drop(rx);
    let tx2 = tx.clone();
    drop(tx);
    assert!(tx2.send(1).is_err());
}
```

## Block 14
**Metadata**: AST_ID=14 | TYPE=FUNCTION | NAME=port_gone_concurrent | COMPLEXITY=6 | LINES=9

```rust
#[test]
fn port_gone_concurrent() {
    let (tx, rx) = channel::<i32>();
    let _t = thread::spawn(move || {
        rx.recv().unwrap();
    });
    while tx.send(1).is_ok() {}
}
```

## Block 15
**Metadata**: AST_ID=15 | TYPE=FUNCTION | NAME=port_gone_concurrent_shared | COMPLEXITY=6 | LINES=10

```rust
#[test]
fn port_gone_concurrent_shared() {
    let (tx, rx) = channel::<i32>();
    let tx2 = tx.clone();
    let _t = thread::spawn(move || {
        rx.recv().unwrap();
    });
    while tx.send(1).is_ok() && tx2.send(1).is_ok() {}
}
```

## Block 16
**Metadata**: AST_ID=16 | TYPE=FUNCTION | NAME=smoke_chan_gone | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn smoke_chan_gone() {
    let (tx, rx) = channel::<i32>();
    drop(tx);
    assert!(rx.recv().is_err());
}
```

## Block 17
**Metadata**: AST_ID=17 | TYPE=FUNCTION | NAME=smoke_chan_gone_shared | COMPLEXITY=2 | LINES=9

```rust
#[test]
fn smoke_chan_gone_shared() {
    let (tx, rx) = channel::<()>();
    let tx2 = tx.clone();
    drop(tx);
    drop(tx2);
    assert!(rx.recv().is_err());
}
```

## Block 18
**Metadata**: AST_ID=18 | TYPE=FUNCTION | NAME=chan_gone_concurrent | COMPLEXITY=6 | LINES=10

```rust
#[test]
fn chan_gone_concurrent() {
    let (tx, rx) = channel::<i32>();
    let _t = thread::spawn(move || {
        tx.send(1).unwrap();
        tx.send(1).unwrap();
    });
    while rx.recv().is_ok() {}
}
```

## Block 19
**Metadata**: AST_ID=19 | TYPE=FUNCTION | NAME=receiver_cloning | COMPLEXITY=2 | LINES=12

```rust
#[test]
fn receiver_cloning() {
    let (tx, rx) = channel::<i32>();
    let rx2 = rx.clone();

    tx.send(1).unwrap();
    tx.send(2).unwrap();

    assert_eq!(rx2.recv(), Ok(1));
    assert_eq!(rx.recv(), Ok(2));
}
```

## Block 20
**Metadata**: AST_ID=20 | TYPE=FUNCTION | NAME=stress | COMPLEXITY=13 | LINES=15

```rust
#[test]
fn stress() {
    let count = if cfg!(miri) { 100 } else { 10000 };
    let (tx, rx) = channel::<i32>();
    let t = thread::spawn(move || {
        for _ in 0..count {
            tx.send(1).unwrap();
        }
    });
    for _ in 0..count {
        assert_eq!(rx.recv().unwrap(), 1);
    }
    t.join().ok().expect("thread panicked");
}
```

## Block 21
**Metadata**: AST_ID=21 | TYPE=FUNCTION | NAME=stress_shared | COMPLEXITY=23 | LINES=28

```rust
#[test]
fn stress_shared() {
    const AMT: u32 = if cfg!(miri) { 100 } else { 10000 };
    const NTHREADS: u32 = 8;
    let (tx, rx) = channel::<i32>();

    let t = thread::spawn(move || {
        for _ in 0..AMT * NTHREADS {
            assert_eq!(rx.recv().unwrap(), 1);
        }
        match rx.try_recv() {
            Ok(..) => panic!(),
            _ => {}
        }
    });

    for _ in 0..NTHREADS {
        let tx = tx.clone();
        thread::spawn(move || {
            for _ in 0..AMT {
                tx.send(1).unwrap();
            }
        });
    }
    drop(tx);
    t.join().ok().expect("thread panicked");
}
```

## Block 22
**Metadata**: AST_ID=22 | TYPE=FUNCTION | NAME=send_from_outside_runtime | COMPLEXITY=11 | LINES=20

```rust
#[test]
fn send_from_outside_runtime() {
    let (tx1, rx1) = channel::<()>();
    let (tx2, rx2) = channel::<i32>();
    let t1 = thread::spawn(move || {
        tx1.send(()).unwrap();
        for _ in 0..40 {
            assert_eq!(rx2.recv().unwrap(), 1);
        }
    });
    rx1.recv().unwrap();
    let t2 = thread::spawn(move || {
        for _ in 0..40 {
            tx2.send(1).unwrap();
        }
    });
    t1.join().ok().expect("thread panicked");
    t2.join().ok().expect("thread panicked");
}
```

## Block 23
**Metadata**: AST_ID=23 | TYPE=FUNCTION | NAME=recv_from_outside_runtime | COMPLEXITY=9 | LINES=14

```rust
#[test]
fn recv_from_outside_runtime() {
    let (tx, rx) = channel::<i32>();
    let t = thread::spawn(move || {
        for _ in 0..40 {
            assert_eq!(rx.recv().unwrap(), 1);
        }
    });
    for _ in 0..40 {
        tx.send(1).unwrap();
    }
    t.join().ok().expect("thread panicked");
}
```

## Block 24
**Metadata**: AST_ID=24 | TYPE=FUNCTION | NAME=no_runtime | COMPLEXITY=4 | LINES=16

```rust
#[test]
fn no_runtime() {
    let (tx1, rx1) = channel::<i32>();
    let (tx2, rx2) = channel::<i32>();
    let t1 = thread::spawn(move || {
        assert_eq!(rx1.recv().unwrap(), 1);
        tx2.send(2).unwrap();
    });
    let t2 = thread::spawn(move || {
        tx1.send(1).unwrap();
        assert_eq!(rx2.recv().unwrap(), 2);
    });
    t1.join().ok().expect("thread panicked");
    t2.join().ok().expect("thread panicked");
}
```

## Block 25
**Metadata**: AST_ID=25 | TYPE=FUNCTION | NAME=oneshot_single_thread_close_port_first | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_close_port_first() {
    // Simple test of closing without sending
    let (_tx, rx) = channel::<i32>();
    drop(rx);
}
```

## Block 26
**Metadata**: AST_ID=26 | TYPE=FUNCTION | NAME=oneshot_single_thread_close_chan_first | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_close_chan_first() {
    // Simple test of closing without sending
    let (tx, _rx) = channel::<i32>();
    drop(tx);
}
```

## Block 27
**Metadata**: AST_ID=27 | TYPE=FUNCTION | NAME=oneshot_single_thread_send_port_close | COMPLEXITY=4 | LINES=8

```rust
#[test]
fn oneshot_single_thread_send_port_close() {
    // Testing that the sender cleans up the payload if receiver is closed
    let (tx, rx) = channel::<Box<i32>>();
    drop(rx);
    assert!(tx.send(Box::new(0)).is_err());
}
```

## Block 28
**Metadata**: AST_ID=28 | TYPE=FUNCTION | NAME=oneshot_single_thread_recv_chan_close | COMPLEXITY=3 | LINES=13

```rust
#[test]
fn oneshot_single_thread_recv_chan_close() {
    // Receiving on a closed chan will panic
    let res = thread::spawn(move || {
        let (tx, rx) = channel::<i32>();
        drop(tx);
        rx.recv().unwrap();
    })
    .join();
    // What is our res?
    assert!(res.is_err());
}
```

## Block 29
**Metadata**: AST_ID=29 | TYPE=FUNCTION | NAME=oneshot_single_thread_send_then_recv | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_send_then_recv() {
    let (tx, rx) = channel::<Box<i32>>();
    tx.send(Box::new(10)).unwrap();
    assert!(*rx.recv().unwrap() == 10);
}
```

## Block 30
**Metadata**: AST_ID=30 | TYPE=FUNCTION | NAME=oneshot_single_thread_try_send_open | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_try_send_open() {
    let (tx, rx) = channel::<i32>();
    assert!(tx.send(10).is_ok());
    assert!(rx.recv().unwrap() == 10);
}
```

## Block 31
**Metadata**: AST_ID=31 | TYPE=FUNCTION | NAME=oneshot_single_thread_try_send_closed | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_try_send_closed() {
    let (tx, rx) = channel::<i32>();
    drop(rx);
    assert!(tx.send(10).is_err());
}
```

## Block 32
**Metadata**: AST_ID=32 | TYPE=FUNCTION | NAME=oneshot_single_thread_try_recv_open | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_try_recv_open() {
    let (tx, rx) = channel::<i32>();
    tx.send(10).unwrap();
    assert!(rx.recv() == Ok(10));
}
```

## Block 33
**Metadata**: AST_ID=33 | TYPE=FUNCTION | NAME=oneshot_single_thread_try_recv_closed | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn oneshot_single_thread_try_recv_closed() {
    let (tx, rx) = channel::<i32>();
    drop(tx);
    assert!(rx.recv().is_err());
}
```

## Block 34
**Metadata**: AST_ID=34 | TYPE=FUNCTION | NAME=oneshot_single_thread_peek_data | COMPLEXITY=2 | LINES=8

```rust
#[test]
fn oneshot_single_thread_peek_data() {
    let (tx, rx) = channel::<i32>();
    assert_eq!(rx.try_recv(), Err(TryRecvError::Empty));
    tx.send(10).unwrap();
    assert_eq!(rx.try_recv(), Ok(10));
}
```

## Block 35
**Metadata**: AST_ID=35 | TYPE=FUNCTION | NAME=oneshot_single_thread_peek_close | COMPLEXITY=2 | LINES=8

```rust
#[test]
fn oneshot_single_thread_peek_close() {
    let (tx, rx) = channel::<i32>();
    drop(tx);
    assert_eq!(rx.try_recv(), Err(TryRecvError::Disconnected));
    assert_eq!(rx.try_recv(), Err(TryRecvError::Disconnected));
}
```

## Block 36
**Metadata**: AST_ID=36 | TYPE=FUNCTION | NAME=oneshot_single_thread_peek_open | COMPLEXITY=2 | LINES=6

```rust
#[test]
fn oneshot_single_thread_peek_open() {
    let (_tx, rx) = channel::<i32>();
    assert_eq!(rx.try_recv(), Err(TryRecvError::Empty));
}
```

## Block 37
**Metadata**: AST_ID=37 | TYPE=FUNCTION | NAME=oneshot_multi_task_recv_then_send | COMPLEXITY=3 | LINES=10

```rust
#[test]
fn oneshot_multi_task_recv_then_send() {
    let (tx, rx) = channel::<Box<i32>>();
    let _t = thread::spawn(move || {
        assert!(*rx.recv().unwrap() == 10);
    });

    tx.send(Box::new(10)).unwrap();
}
```

## Block 38
**Metadata**: AST_ID=38 | TYPE=FUNCTION | NAME=oneshot_multi_task_recv_then_close | COMPLEXITY=4 | LINES=13

```rust
#[test]
fn oneshot_multi_task_recv_then_close() {
    let (tx, rx) = channel::<Box<i32>>();
    let _t = thread::spawn(move || {
        drop(tx);
    });
    let res = thread::spawn(move || {
        assert!(*rx.recv().unwrap() == 10);
    })
    .join();
    assert!(res.is_err());
}
```

## Block 39
**Metadata**: AST_ID=39 | TYPE=FUNCTION | NAME=oneshot_multi_thread_close_stress | COMPLEXITY=6 | LINES=11

```rust
#[test]
fn oneshot_multi_thread_close_stress() {
    for _ in 0..stress_factor() {
        let (tx, rx) = channel::<i32>();
        let _t = thread::spawn(move || {
            drop(rx);
        });
        drop(tx);
    }
}
```

## Block 40
**Metadata**: AST_ID=40 | TYPE=FUNCTION | NAME=oneshot_multi_thread_send_close_stress | COMPLEXITY=7 | LINES=14

```rust
#[test]
fn oneshot_multi_thread_send_close_stress() {
    for _ in 0..stress_factor() {
        let (tx, rx) = channel::<i32>();
        let _t = thread::spawn(move || {
            drop(rx);
        });
        let _ = thread::spawn(move || {
            tx.send(1).unwrap();
        })
        .join();
    }
}
```

## Block 41
**Metadata**: AST_ID=41 | TYPE=FUNCTION | NAME=oneshot_multi_thread_recv_close_stress | COMPLEXITY=9 | LINES=19

```rust
#[test]
fn oneshot_multi_thread_recv_close_stress() {
    for _ in 0..stress_factor() {
        let (tx, rx) = channel::<i32>();
        thread::spawn(move || {
            let res = thread::spawn(move || {
                rx.recv().unwrap();
            })
            .join();
            assert!(res.is_err());
        });
        let _t = thread::spawn(move || {
            thread::spawn(move || {
                drop(tx);
            });
        });
    }
}
```

## Block 42
**Metadata**: AST_ID=42 | TYPE=FUNCTION | NAME=oneshot_multi_thread_send_recv_stress | COMPLEXITY=6 | LINES=11

```rust
#[test]
fn oneshot_multi_thread_send_recv_stress() {
    for _ in 0..stress_factor() {
        let (tx, rx) = channel::<Box<isize>>();
        let _t = thread::spawn(move || {
            tx.send(Box::new(10)).unwrap();
        });
        assert!(*rx.recv().unwrap() == 10);
    }
}
```

## Block 43
**Metadata**: AST_ID=43 | TYPE=FUNCTION | NAME=stream_send_recv_stress | COMPLEXITY=16 | LINES=32

```rust
#[test]
fn stream_send_recv_stress() {
    for _ in 0..stress_factor() {
        let (tx, rx) = channel();

        send(tx, 0);
        recv(rx, 0);

        fn send(tx: Sender<Box<i32>>, i: i32) {
            if i == 10 {
                return;
            }

            thread::spawn(move || {
                tx.send(Box::new(i)).unwrap();
                send(tx, i + 1);
            });
        }

        fn recv(rx: Receiver<Box<i32>>, i: i32) {
            if i == 10 {
                return;
            }

            thread::spawn(move || {
                assert!(*rx.recv().unwrap() == i);
                recv(rx, i + 1);
            });
        }
    }
}
```

## Block 44
**Metadata**: AST_ID=44 | TYPE=FUNCTION | NAME=oneshot_single_thread_recv_timeout | COMPLEXITY=2 | LINES=10

```rust
#[test]
fn oneshot_single_thread_recv_timeout() {
    let (tx, rx) = channel();
    tx.send(()).unwrap();
    assert_eq!(rx.recv_timeout(Duration::from_millis(1)), Ok(()));
    assert_eq!(rx.recv_timeout(Duration::from_millis(1)), Err(RecvTimeoutError::Timeout));
    tx.send(()).unwrap();
    assert_eq!(rx.recv_timeout(Duration::from_millis(1)), Ok(()));
}
```

## Block 45
**Metadata**: AST_ID=45 | TYPE=FUNCTION | NAME=stress_recv_timeout_two_threads | COMPLEXITY=19 | LINES=35

```rust
#[test]
fn stress_recv_timeout_two_threads() {
    let (tx, rx) = channel();
    let stress = stress_factor() + 50;
    let timeout = Duration::from_millis(10);

    thread::spawn(move || {
        for i in 0..stress {
            if i % 2 == 0 {
                thread::sleep(timeout * 4);
            }
            tx.send(1usize).unwrap();
        }
    });

    let mut recv_count = 0;
    let mut got_timeout = false;
    loop {
        match rx.recv_timeout(timeout) {
            Ok(n) => {
                assert_eq!(n, 1usize);
                recv_count += 1;
            }
            Err(RecvTimeoutError::Timeout) => {
                got_timeout = true;
                continue;
            }
            Err(RecvTimeoutError::Disconnected) => break,
        }
    }

    assert_eq!(recv_count, stress);
    assert!(got_timeout);
}
```

## Block 46
**Metadata**: AST_ID=46 | TYPE=FUNCTION | NAME=recv_timeout_upgrade | COMPLEXITY=2 | LINES=11

```rust
#[test]
fn recv_timeout_upgrade() {
    let (tx, rx) = channel::<()>();
    let timeout = Duration::from_millis(1);
    let _tx_clone = tx.clone();

    let start = Instant::now();
    assert_eq!(rx.recv_timeout(timeout), Err(RecvTimeoutError::Timeout));
    assert!(Instant::now() >= start + timeout);
}
```

## Block 47
**Metadata**: AST_ID=47 | TYPE=FUNCTION | NAME=stress_recv_timeout_shared | COMPLEXITY=15 | LINES=30

```rust
#[test]
fn stress_recv_timeout_shared() {
    let (tx, rx) = channel();
    let stress = stress_factor() + 100;

    for i in 0..stress {
        let tx = tx.clone();
        thread::spawn(move || {
            thread::sleep(Duration::from_millis(i as u64 * 10));
            tx.send(1usize).unwrap();
        });
    }

    drop(tx);

    let mut recv_count = 0;
    loop {
        match rx.recv_timeout(Duration::from_millis(10)) {
            Ok(n) => {
                assert_eq!(n, 1usize);
                recv_count += 1;
            }
            Err(RecvTimeoutError::Timeout) => continue,
            Err(RecvTimeoutError::Disconnected) => break,
        }
    }

    assert_eq!(recv_count, stress);
}
```

## Block 48
**Metadata**: AST_ID=48 | TYPE=FUNCTION | NAME=very_long_recv_timeout_wont_panic | COMPLEXITY=2 | LINES=9

```rust
#[test]
fn very_long_recv_timeout_wont_panic() {
    let (tx, rx) = channel::<()>();
    let join_handle = thread::spawn(move || rx.recv_timeout(Duration::from_secs(u64::MAX)));
    thread::sleep(Duration::from_secs(1));
    assert!(tx.send(()).is_ok());
    assert_eq!(join_handle.join().unwrap(), Ok(()));
}
```

## Block 49
**Metadata**: AST_ID=49 | TYPE=FUNCTION | NAME=recv_a_lot | COMPLEXITY=12 | LINES=13

```rust
#[test]
fn recv_a_lot() {
    let count = if cfg!(miri) { 1000 } else { 10000 };
    // Regression test that we don't run out of stack in scheduler context
    let (tx, rx) = channel();
    for _ in 0..count {
        tx.send(()).unwrap();
    }
    for _ in 0..count {
        rx.recv().unwrap();
    }
}
```

## Block 50
**Metadata**: AST_ID=50 | TYPE=FUNCTION | NAME=shared_recv_timeout | COMPLEXITY=9 | LINES=20

```rust
#[test]
fn shared_recv_timeout() {
    let (tx, rx) = channel();
    let total = 5;
    for _ in 0..total {
        let tx = tx.clone();
        thread::spawn(move || {
            tx.send(()).unwrap();
        });
    }

    for _ in 0..total {
        rx.recv().unwrap();
    }

    assert_eq!(rx.recv_timeout(Duration::from_millis(1)), Err(RecvTimeoutError::Timeout));
    tx.send(()).unwrap();
    assert_eq!(rx.recv_timeout(Duration::from_millis(1)), Ok(()));
}
```

## Block 51
**Metadata**: AST_ID=51 | TYPE=FUNCTION | NAME=shared_chan_stress | COMPLEXITY=9 | LINES=16

```rust
#[test]
fn shared_chan_stress() {
    let (tx, rx) = channel();
    let total = stress_factor() + 100;
    for _ in 0..total {
        let tx = tx.clone();
        thread::spawn(move || {
            tx.send(()).unwrap();
        });
    }

    for _ in 0..total {
        rx.recv().unwrap();
    }
}
```

## Block 52
**Metadata**: AST_ID=52 | TYPE=FUNCTION | NAME=test_nested_recv_iter | COMPLEXITY=6 | LINES=20

```rust
#[test]
fn test_nested_recv_iter() {
    let (tx, rx) = channel::<i32>();
    let (total_tx, total_rx) = channel::<i32>();

    let _t = thread::spawn(move || {
        let mut acc = 0;
        for x in rx.iter() {
            acc += x;
        }
        total_tx.send(acc).unwrap();
    });

    tx.send(3).unwrap();
    tx.send(1).unwrap();
    tx.send(2).unwrap();
    drop(tx);
    assert_eq!(total_rx.recv().unwrap(), 6);
}
```

## Block 53
**Metadata**: AST_ID=53 | TYPE=FUNCTION | NAME=test_recv_iter_break | COMPLEXITY=11 | LINES=25

```rust
#[test]
fn test_recv_iter_break() {
    let (tx, rx) = channel::<i32>();
    let (count_tx, count_rx) = channel();

    let _t = thread::spawn(move || {
        let mut count = 0;
        for x in rx.iter() {
            if count >= 3 {
                break;
            } else {
                count += x;
            }
        }
        count_tx.send(count).unwrap();
    });

    tx.send(2).unwrap();
    tx.send(2).unwrap();
    tx.send(2).unwrap();
    let _ = tx.send(2);
    drop(tx);
    assert_eq!(count_rx.recv().unwrap(), 4);
}
```

## Block 54
**Metadata**: AST_ID=54 | TYPE=FUNCTION | NAME=test_recv_try_iter | COMPLEXITY=19 | LINES=28

```rust
#[test]
fn test_recv_try_iter() {
    let (request_tx, request_rx) = channel();
    let (response_tx, response_rx) = channel();

    // Request `x`s until we have `6`.
    let t = thread::spawn(move || {
        let mut count = 0;
        loop {
            for x in response_rx.try_iter() {
                count += x;
                if count == 6 {
                    return count;
                }
            }
            request_tx.send(()).unwrap();
        }
    });

    for _ in request_rx.iter() {
        if response_tx.send(2).is_err() {
            break;
        }
    }

    assert_eq!(t.join().unwrap(), 6);
}
```

## Block 55
**Metadata**: AST_ID=55 | TYPE=FUNCTION | NAME=test_recv_into_iter_owned | COMPLEXITY=3 | LINES=14

```rust
#[test]
fn test_recv_into_iter_owned() {
    let mut iter = {
        let (tx, rx) = channel::<i32>();
        tx.send(1).unwrap();
        tx.send(2).unwrap();

        rx.into_iter()
    };
    assert_eq!(iter.next().unwrap(), 1);
    assert_eq!(iter.next().unwrap(), 2);
    assert_eq!(iter.next().is_none(), true);
}
```

## Block 56
**Metadata**: AST_ID=56 | TYPE=FUNCTION | NAME=test_recv_into_iter_borrowed | COMPLEXITY=2 | LINES=12

```rust
#[test]
fn test_recv_into_iter_borrowed() {
    let (tx, rx) = channel::<i32>();
    tx.send(1).unwrap();
    tx.send(2).unwrap();
    drop(tx);
    let mut iter = (&rx).into_iter();
    assert_eq!(iter.next().unwrap(), 1);
    assert_eq!(iter.next().unwrap(), 2);
    assert_eq!(iter.next().is_none(), true);
}
```

## Block 57
**Metadata**: AST_ID=57 | TYPE=FUNCTION | NAME=try_recv_states | COMPLEXITY=4 | LINES=24

```rust
#[test]
fn try_recv_states() {
    let (tx1, rx1) = channel::<i32>();
    let (tx2, rx2) = channel::<()>();
    let (tx3, rx3) = channel::<()>();
    let _t = thread::spawn(move || {
        rx2.recv().unwrap();
        tx1.send(1).unwrap();
        tx3.send(()).unwrap();
        rx2.recv().unwrap();
        drop(tx1);
        tx3.send(()).unwrap();
    });

    assert_eq!(rx1.try_recv(), Err(TryRecvError::Empty));
    tx2.send(()).unwrap();
    rx3.recv().unwrap();
    assert_eq!(rx1.try_recv(), Ok(1));
    assert_eq!(rx1.try_recv(), Err(TryRecvError::Empty));
    tx2.send(()).unwrap();
    rx3.recv().unwrap();
    assert_eq!(rx1.try_recv(), Err(TryRecvError::Disconnected));
}
```

## Block 58
**Metadata**: AST_ID=58 | TYPE=FUNCTION | NAME=destroy_upgraded_shared_port_when_sender_still_active | COMPLEXITY=9 | LINES=25

```rust
// This bug used to end up in a livelock inside of the Receiver destructor
// because the internal state of the Shared packet was corrupted
#[test]
fn destroy_upgraded_shared_port_when_sender_still_active() {
    let (tx, rx) = channel();
    let (tx2, rx2) = channel();
    let _t = thread::spawn(move || {
        rx.recv().unwrap(); // wait on a oneshot
        drop(rx); // destroy a shared
        tx2.send(()).unwrap();
    });
    // make sure the other thread has gone to sleep
    for _ in 0..5000 {
        thread::yield_now();
    }

    // upgrade to a shared chan and send a message
    let t = tx.clone();
    drop(tx);
    t.send(()).unwrap();

    // wait for the child thread to exit before we exit
    rx2.recv().unwrap();
}
```

## Block 59
**Metadata**: AST_ID=59 | TYPE=FUNCTION | NAME=issue_32114 | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn issue_32114() {
    let (tx, _) = channel();
    let _ = tx.send(123);
    assert_eq!(tx.send(123), Err(SendError(123)));
}
```

## Block 60
**Metadata**: AST_ID=60 | TYPE=FUNCTION | NAME=issue_39364 | COMPLEXITY=3 | LINES=15

```rust
#[test]
fn issue_39364() {
    let (tx, rx) = channel::<()>();
    let t = thread::spawn(move || {
        thread::sleep(Duration::from_millis(300));
        let _ = tx.clone();
        // Don't drop; hand back to caller.
        tx
    });

    let _ = rx.recv_timeout(Duration::from_millis(500));
    let _tx = t.join().unwrap(); // delay dropping until end of test
    let _ = rx.recv_timeout(Duration::from_millis(500));
}
```

---
*Generated by AST tracing system*
