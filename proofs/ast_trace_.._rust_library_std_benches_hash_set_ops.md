# AST Trace: ../rust/library/std/benches/hash/set_ops.rs

Generated 5 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=set_difference | COMPLEXITY=2 | LINES=11

```rust
use std::collections::HashSet;

use test::Bencher;

#[bench]
fn set_difference(b: &mut Bencher) {
    let small: HashSet<_> = (0..10).collect();
    let large: HashSet<_> = (0..100).collect();

    b.iter(|| small.difference(&large).count());
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=set_is_subset | COMPLEXITY=2 | LINES=8

```rust
#[bench]
fn set_is_subset(b: &mut Bencher) {
    let small: HashSet<_> = (0..10).collect();
    let large: HashSet<_> = (0..100).collect();

    b.iter(|| small.is_subset(&large));
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=set_intersection | COMPLEXITY=2 | LINES=8

```rust
#[bench]
fn set_intersection(b: &mut Bencher) {
    let small: HashSet<_> = (0..10).collect();
    let large: HashSet<_> = (0..100).collect();

    b.iter(|| small.intersection(&large).count());
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=set_symmetric_difference | COMPLEXITY=2 | LINES=8

```rust
#[bench]
fn set_symmetric_difference(b: &mut Bencher) {
    let small: HashSet<_> = (0..10).collect();
    let large: HashSet<_> = (0..100).collect();

    b.iter(|| small.symmetric_difference(&large).count());
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=set_union | COMPLEXITY=2 | LINES=8

```rust
#[bench]
fn set_union(b: &mut Bencher) {
    let small: HashSet<_> = (0..10).collect();
    let large: HashSet<_> = (0..100).collect();

    b.iter(|| small.union(&large).count());
}
```

---
*Generated by AST tracing system*
