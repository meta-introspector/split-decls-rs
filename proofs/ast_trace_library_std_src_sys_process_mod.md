# AST Trace: library/std/src/sys/process/mod.rs

Generated 4 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=9 | LINES=41

```rust
// SRC: library/std/src/sys/process/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/process/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/process/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/process/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/process/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/process/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/process/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/std/src/sys/process/mod.rs
cfg_select! {
    target_family = "unix" => {
// MOD DECLARATION: unix (from library/std/src/sys/process/mod.rs)
// REPLACED: mod unix; -> use crate::unix::*;
use crate::unix::*;
        use unix as imp;
    }
    target_os = "windows" => {
// MOD DECLARATION: windows (from library/std/src/sys/process/mod.rs)
// REPLACED: mod windows; -> use crate::windows::*;
use crate::windows::*;
        use windows as imp;
    }
    target_os = "uefi" => {
// MOD DECLARATION: uefi (from library/std/src/sys/process/mod.rs)
// REPLACED: mod uefi; -> use crate::uefi::*;
use crate::uefi::*;
        use uefi as imp;
    }
    _ => {
// MOD DECLARATION: unsupported (from library/std/src/sys/process/mod.rs)
// REPLACED: mod unsupported; -> use crate::unsupported::*;
use crate::unsupported::*;
        use unsupported as imp;
    }
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=12

```rust
// This module is shared by all platforms, but nearly all platforms except for
// the "normal" UNIX ones leave some of this code unused.
#[cfg_attr(not(target_os = "linux"), allow(dead_code))]
// MOD DECLARATION: env (from library/std/src/sys/process/mod.rs)
// REPLACED: mod env; -> use crate::env::*;
use crate::env::*;

pub use env::CommandEnvs;
pub use imp::{
    Command, CommandArgs, EnvKey, ExitCode, ExitStatus, ExitStatusError, Process, Stdio, StdioPipes,
};
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=output | COMPLEXITY=12 | LINES=39

```rust
#[cfg(any(
    all(
        target_family = "unix",
        not(any(
            target_os = "espidf",
            target_os = "horizon",
            target_os = "vita",
            target_os = "nuttx"
        ))
    ),
    target_os = "windows",
))]
pub fn output(cmd: &mut Command) -> crate::io::Result<(ExitStatus, Vec<u8>, Vec<u8>)> {
    use crate::sys::pipe::read2;

    let (mut process, mut pipes) = cmd.spawn(Stdio::MakePipe, false)?;

    drop(pipes.stdin.take());
    let (mut stdout, mut stderr) = (Vec::new(), Vec::new());
    match (pipes.stdout.take(), pipes.stderr.take()) {
        (None, None) => {}
        (Some(out), None) => {
            let res = out.read_to_end(&mut stdout);
            res.unwrap();
        }
        (None, Some(err)) => {
            let res = err.read_to_end(&mut stderr);
            res.unwrap();
        }
        (Some(out), Some(err)) => {
            let res = read2(out, &mut stdout, err, &mut stderr);
            res.unwrap();
        }
    }

    let status = process.wait()?;
    Ok((status, stdout, stderr))
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=1 | LINES=14

```rust
#[cfg(not(any(
    all(
        target_family = "unix",
        not(any(
            target_os = "espidf",
            target_os = "horizon",
            target_os = "vita",
            target_os = "nuttx"
        ))
    ),
    target_os = "windows",
)))]
pub use imp::output;
```

---
*Generated by AST tracing system*
