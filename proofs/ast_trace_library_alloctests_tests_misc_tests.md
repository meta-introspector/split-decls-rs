# AST Trace: library/alloctests/tests/misc_tests.rs

Generated 11 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=test_owned_clone | COMPLEXITY=6 | LINES=29

```rust
// SRC: library/alloctests/tests/misc_tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/tests/misc_tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/tests/misc_tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/tests/misc_tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/tests/misc_tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/tests/misc_tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/tests/misc_tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/tests/misc_tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/alloctests/tests/misc_tests.rs
//! Test for `boxed` mod.

use core::any::Any;
use core::ops::Deref;
use std::boxed::Box;

#[test]
fn test_owned_clone() {
    let a = Box::new(5);
    let b: Box<i32> = a.clone();
    assert!(a == b);
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=Test; | COMPLEXITY=2 | LINES=21

```rust
#[derive(Debug, PartialEq, Eq)]
struct Test;

#[test]
fn any_move() {
    let a = Box::new(8) as Box<dyn Any>;
    let b = Box::new(Test) as Box<dyn Any>;

    let a: Box<i32> = a.downcast::<i32>().unwrap();
    assert_eq!(*a, 8);

    let b: Box<Test> = b.downcast::<Test>().unwrap();
    assert_eq!(*b, Test);

    let a = Box::new(8) as Box<dyn Any>;
    let b = Box::new(Test) as Box<dyn Any>;

    assert!(a.downcast::<Box<i32>>().is_err());
    assert!(b.downcast::<Box<Test>>().is_err());
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=test_show | COMPLEXITY=11 | LINES=19

```rust
#[test]
fn test_show() {
    let a = Box::new(8) as Box<dyn Any>;
    let b = Box::new(Test) as Box<dyn Any>;
    let a_str = format!("{a:?}");
    let b_str = format!("{b:?}");
    assert_eq!(a_str, "Any { .. }");
    assert_eq!(b_str, "Any { .. }");

    static EIGHT: usize = 8;
    static TEST: Test = Test;
    let a = &EIGHT as &dyn Any;
    let b = &TEST as &dyn Any;
    let s = format!("{a:?}");
    assert_eq!(s, "Any { .. }");
    let s = format!("{b:?}");
    assert_eq!(s, "Any { .. }");
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=deref | COMPLEXITY=3 | LINES=6

```rust
#[test]
fn deref() {
    fn homura<T: Deref<Target = i32>>(_: T) {}
    homura(Box::new(765));
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=raw_sized | COMPLEXITY=7 | LINES=12

```rust
#[test]
fn raw_sized() {
    let x = Box::new(17);
    let p = Box::into_raw(x);
    unsafe {
        assert_eq!(17, *p);
        *p = 19;
        let y = Box::from_raw(p);
        assert_eq!(19, *y);
    }
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=raw_trait | COMPLEXITY=14 | LINES=29

```rust
#[test]
fn raw_trait() {
    trait Foo {
        fn get(&self) -> u32;
        fn set(&mut self, value: u32);
    }

    struct Bar(u32);

    impl Foo for Bar {
        fn get(&self) -> u32 {
            self.0
        }

        fn set(&mut self, value: u32) {
            self.0 = value;
        }
    }

    let x: Box<dyn Foo> = Box::new(Bar(17));
    let p = Box::into_raw(x);
    unsafe {
        assert_eq!(17, (*p).get());
        (*p).set(19);
        let y: Box<dyn Foo> = Box::from_raw(p);
        assert_eq!(19, y.get());
    }
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=f64_slice | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn f64_slice() {
    let slice: &[f64] = &[-1.0, 0.0, 1.0, f64::INFINITY];
    let boxed: Box<[f64]> = Box::from(slice);
    assert_eq!(&*boxed, slice)
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=i64_slice | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn i64_slice() {
    let slice: &[i64] = &[i64::MIN, -2, -1, 0, 1, 2, i64::MAX];
    let boxed: Box<[i64]> = Box::from(slice);
    assert_eq!(&*boxed, slice)
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=str_slice | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn str_slice() {
    let s = "Hello, world!";
    let boxed: Box<str> = Box::from(s);
    assert_eq!(&*boxed, s)
}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=FUNCTION | NAME=boxed_slice_from_iter | COMPLEXITY=2 | LINES=8

```rust
#[test]
fn boxed_slice_from_iter() {
    let iter = 0..100;
    let boxed: Box<[u32]> = iter.collect();
    assert_eq!(boxed.len(), 100);
    assert_eq!(boxed[7], 7);
}
```

## Block 11
**Metadata**: AST_ID=11 | TYPE=FUNCTION | NAME=test_array_from_slice | COMPLEXITY=2 | LINES=12

```rust
#[test]
fn test_array_from_slice() {
    let v = vec![1, 2, 3];
    let r: Box<[u32]> = v.into_boxed_slice();

    let a: Result<Box<[u32; 3]>, _> = r.clone().try_into();
    assert!(a.is_ok());

    let a: Result<Box<[u32; 2]>, _> = r.clone().try_into();
    assert!(a.is_err());
}
```

---
*Generated by AST tracing system*
