# AST Trace: library/std/src/sys/pal/common/tests.rs

Generated 6 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=stack_allocation_works | COMPLEXITY=5 | LINES=33

```rust
// SRC: library/std/src/sys/pal/common/tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/pal/common/tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/pal/common/tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/pal/common/tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/pal/common/tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/pal/common/tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/pal/common/tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/pal/common/tests.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/std/src/sys/pal/common/tests.rs
use core::iter::repeat;

use crate::ffi::CString;
use crate::hint::black_box;
use crate::path::Path;
use crate::sys::common::small_c_string::run_path_with_cstr;

#[test]
fn stack_allocation_works() {
    let path = Path::new("abc");
    let result = run_path_with_cstr(path, &|p| {
        assert_eq!(p, &*CString::new(path.as_os_str().as_encoded_bytes()).unwrap());
        Ok(42)
    });
    assert_eq!(result.unwrap(), 42);
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=stack_allocation_fails | COMPLEXITY=2 | LINES=6

```rust
#[test]
fn stack_allocation_fails() {
    let path = Path::new("ab\0");
    assert!(run_path_with_cstr::<()>(path, &|_| unreachable!()).is_err());
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=heap_allocation_works | COMPLEXITY=3 | LINES=11

```rust
#[test]
fn heap_allocation_works() {
    let path = repeat("a").take(384).collect::<String>();
    let path = Path::new(&path);
    let result = run_path_with_cstr(path, &|p| {
        assert_eq!(p, &*CString::new(path.as_os_str().as_encoded_bytes()).unwrap());
        Ok(42)
    });
    assert_eq!(result.unwrap(), 42);
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=heap_allocation_fails | COMPLEXITY=2 | LINES=8

```rust
#[test]
fn heap_allocation_fails() {
    let mut path = repeat("a").take(384).collect::<String>();
    path.push('\0');
    let path = Path::new(&path);
    assert!(run_path_with_cstr::<()>(path, &|_| unreachable!()).is_err());
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=bench_stack_path_alloc | COMPLEXITY=4 | LINES=13

```rust
#[bench]
fn bench_stack_path_alloc(b: &mut test::Bencher) {
    let path = repeat("a").take(383).collect::<String>();
    let p = Path::new(&path);
    b.iter(|| {
        run_path_with_cstr(p, &|cstr| {
            black_box(cstr);
            Ok(())
        })
        .unwrap();
    });
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=bench_heap_path_alloc | COMPLEXITY=4 | LINES=13

```rust
#[bench]
fn bench_heap_path_alloc(b: &mut test::Bencher) {
    let path = repeat("a").take(384).collect::<String>();
    let p = Path::new(&path);
    b.iter(|| {
        run_path_with_cstr(p, &|cstr| {
            black_box(cstr);
            Ok(())
        })
        .unwrap();
    });
}
```

---
*Generated by AST tracing system*
