# AST Trace: ../rust/library/coretests/tests/tuple.rs

Generated 6 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
use std::cmp::Ordering::{Equal, Greater, Less};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=test_clone | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn test_clone() {
    let a = (1, "2");
    let b = a.clone();
    assert_eq!(a, b);
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=test_partial_eq | COMPLEXITY=2 | LINES=9

```rust
#[test]
fn test_partial_eq() {
    let (small, big) = ((1, 2, 3), (3, 2, 1));
    assert_eq!(small, small);
    assert_eq!(big, big);
    assert_ne!(small, big);
    assert_ne!(big, small);
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=test_partial_ord | COMPLEXITY=3 | LINES=25

```rust
#[test]
fn test_partial_ord() {
    let (small, big) = ((1, 2, 3), (3, 2, 1));

    assert!(small < big);
    assert!(!(small < small));
    assert!(!(big < small));
    assert!(!(big < big));

    assert!(small <= small);
    assert!(big <= big);

    assert!(big > small);
    assert!(small >= small);
    assert!(big >= small);
    assert!(big >= big);

    assert!(!((1.0f64, 2.0f64) < (f64::NAN, 3.0)));
    assert!(!((1.0f64, 2.0f64) <= (f64::NAN, 3.0)));
    assert!(!((1.0f64, 2.0f64) > (f64::NAN, 3.0)));
    assert!(!((1.0f64, 2.0f64) >= (f64::NAN, 3.0)));
    assert!((1.0f64, 2.0f64) < (2.0, f64::NAN));
    assert!(!((2.0f64, 2.0f64) < (2.0, f64::NAN)));
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=test_ord | COMPLEXITY=2 | LINES=9

```rust
#[test]
fn test_ord() {
    let (small, big) = ((1, 2, 3), (3, 2, 1));
    assert_eq!(small.cmp(&small), Equal);
    assert_eq!(big.cmp(&big), Equal);
    assert_eq!(small.cmp(&big), Less);
    assert_eq!(big.cmp(&small), Greater);
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=test_show | COMPLEXITY=5 | LINES=10

```rust
#[test]
fn test_show() {
    let s = format!("{:?}", (1,));
    assert_eq!(s, "(1,)");
    let s = format!("{:?}", (1, true));
    assert_eq!(s, "(1, true)");
    let s = format!("{:?}", (1, "hi", true));
    assert_eq!(s, "(1, \"hi\", true)");
}
```

---
*Generated by AST tracing system*
