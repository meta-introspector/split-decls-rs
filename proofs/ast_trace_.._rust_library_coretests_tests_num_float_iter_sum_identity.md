# AST Trace: ../rust/library/coretests/tests/num/float_iter_sum_identity.rs

Generated 4 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=f32_ref | COMPLEXITY=2 | LINES=6

```rust
#[test]
fn f32_ref() {
    let x: f32 = -0.0;
    let still_x: f32 = [x].iter().sum();
    assert_eq!(1. / x, 1. / still_x)
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=f32_own | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn f32_own() {
    let x: f32 = -0.0;
    let still_x: f32 = [x].into_iter().sum();
    assert_eq!(1. / x, 1. / still_x)
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=f64_ref | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn f64_ref() {
    let x: f64 = -0.0;
    let still_x: f64 = [x].iter().sum();
    assert_eq!(1. / x, 1. / still_x)
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=f64_own | COMPLEXITY=2 | LINES=7

```rust
#[test]
fn f64_own() {
    let x: f64 = -0.0;
    let still_x: f64 = [x].into_iter().sum();
    assert_eq!(1. / x, 1. / still_x)
}
```

---
*Generated by AST tracing system*
