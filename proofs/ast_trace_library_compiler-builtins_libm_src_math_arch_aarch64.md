# AST Trace: library/compiler-builtins/libm/src/math/arch/aarch64.rs

Generated 8 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=fma | COMPLEXITY=15 | LINES=32

```rust
// SRC: library/compiler-builtins/libm/src/math/arch/aarch64.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/compiler-builtins/libm/src/math/arch/aarch64.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/compiler-builtins/libm/src/math/arch/aarch64.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/compiler-builtins/libm/src/math/arch/aarch64.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/compiler-builtins/libm/src/math/arch/aarch64.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/compiler-builtins/libm/src/math/arch/aarch64.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/compiler-builtins/libm/src/math/arch/aarch64.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/compiler-builtins/libm/src/math/arch/aarch64.rs
//! Architecture-specific support for aarch64 with neon.

use core::arch::asm;

pub fn fma(mut x: f64, y: f64, z: f64) -> f64 {
    // SAFETY: `fmadd` is available with neon and has no side effects.
    unsafe {
        asm!(
            "fmadd {x:d}, {x:d}, {y:d}, {z:d}",
            x = inout(vreg) x,
            y = in(vreg) y,
            z = in(vreg) z,
            options(nomem, nostack, pure)
        );
    }
    x
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=fmaf | COMPLEXITY=11 | LINES=14

```rust
pub fn fmaf(mut x: f32, y: f32, z: f32) -> f32 {
    // SAFETY: `fmadd` is available with neon and has no side effects.
    unsafe {
        asm!(
            "fmadd {x:s}, {x:s}, {y:s}, {z:s}",
            x = inout(vreg) x,
            y = in(vreg) y,
            z = in(vreg) z,
            options(nomem, nostack, pure)
        );
    }
    x
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=rint | COMPLEXITY=15 | LINES=21

```rust
// NB: `frintx` is technically the correct instruction for C's `rint`. However, in Rust (and LLVM
// by default), `rint` is identical to `roundeven` (no fpenv interaction) so we use the
// side-effect-free `frintn`.
//
// In general, C code that calls Rust's libm should assume that fpenv is ignored.

pub fn rint(mut x: f64) -> f64 {
    // SAFETY: `frintn` is available with neon and has no side effects.
    //
    // `frintn` is always round-to-nearest which does not match the C specification, but Rust does
    // not support rounding modes.
    unsafe {
        asm!(
            "frintn {x:d}, {x:d}",
            x = inout(vreg) x,
            options(nomem, nostack, pure)
        );
    }
    x
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=rintf | COMPLEXITY=12 | LINES=15

```rust
pub fn rintf(mut x: f32) -> f32 {
    // SAFETY: `frintn` is available with neon and has no side effects.
    //
    // `frintn` is always round-to-nearest which does not match the C specification, but Rust does
    // not support rounding modes.
    unsafe {
        asm!(
            "frintn {x:s}, {x:s}",
            x = inout(vreg) x,
            options(nomem, nostack, pure)
        );
    }
    x
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=rintf16 | COMPLEXITY=14 | LINES=16

```rust
#[cfg(all(f16_enabled, target_feature = "fp16"))]
pub fn rintf16(mut x: f16) -> f16 {
    // SAFETY: `frintn` is available for `f16` with `fp16` (implies `neon`) and has no side effects.
    //
    // `frintn` is always round-to-nearest which does not match the C specification, but Rust does
    // not support rounding modes.
    unsafe {
        asm!(
            "frintn {x:h}, {x:h}",
            x = inout(vreg) x,
            options(nomem, nostack, pure)
        );
    }
    x
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=sqrt | COMPLEXITY=9 | LINES=12

```rust
pub fn sqrt(mut x: f64) -> f64 {
    // SAFETY: `fsqrt` is available with neon and has no side effects.
    unsafe {
        asm!(
            "fsqrt {x:d}, {x:d}",
            x = inout(vreg) x,
            options(nomem, nostack, pure)
        );
    }
    x
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=sqrtf | COMPLEXITY=9 | LINES=12

```rust
pub fn sqrtf(mut x: f32) -> f32 {
    // SAFETY: `fsqrt` is available with neon and has no side effects.
    unsafe {
        asm!(
            "fsqrt {x:s}, {x:s}",
            x = inout(vreg) x,
            options(nomem, nostack, pure)
        );
    }
    x
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=sqrtf16 | COMPLEXITY=11 | LINES=14

```rust
#[cfg(all(f16_enabled, target_feature = "fp16"))]
pub fn sqrtf16(mut x: f16) -> f16 {
    // SAFETY: `fsqrt` is available for `f16` with `fp16` (implies `neon`) and has no
    // side effects.
    unsafe {
        asm!(
            "fsqrt {x:h}, {x:h}",
            x = inout(vreg) x,
            options(nomem, nostack, pure)
        );
    }
    x
}
```

---
*Generated by AST tracing system*
