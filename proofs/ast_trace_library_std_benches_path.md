# AST Trace: library/std/benches/path.rs

Generated 9 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=3 | LINES=19

```rust
// SRC: library/std/benches/path.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/path.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/path.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/path.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/path.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/path.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/path.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/benches/path.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/std/benches/path.rs
use core::hint::black_box;
use std::collections::{BTreeSet, HashSet};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
use std::hash::{DefaultHasher, Hash, Hasher};
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=bench_path_cmp_fast_path_buf_sort | COMPLEXITY=4 | LINES=15

```rust
use std::path::*;

#[bench]
#[cfg_attr(miri, ignore)] // Miri isn't fast...
fn bench_path_cmp_fast_path_buf_sort(b: &mut test::Bencher) {
    let prefix = "my/home";
    let mut paths: Vec<_> =
        (0..1000).map(|num| PathBuf::from(prefix).join(format!("file {num}.rs"))).collect();

    paths.sort();

    b.iter(|| {
        black_box(paths.as_mut_slice()).sort_unstable();
    });
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=bench_path_cmp_fast_path_long | COMPLEXITY=6 | LINES=19

```rust
#[bench]
#[cfg_attr(miri, ignore)] // Miri isn't fast...
fn bench_path_cmp_fast_path_long(b: &mut test::Bencher) {
    let prefix = "/my/home/is/my/castle/and/my/castle/has/a/rusty/workbench/";
    let paths: Vec<_> =
        (0..1000).map(|num| PathBuf::from(prefix).join(format!("file {num}.rs"))).collect();

    let mut set = BTreeSet::new();

    paths.iter().for_each(|p| {
        set.insert(p.as_path());
    });

    b.iter(|| {
        set.remove(paths[500].as_path());
        set.insert(paths[500].as_path());
    });
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=bench_path_cmp_fast_path_short | COMPLEXITY=5 | LINES=19

```rust
#[bench]
#[cfg_attr(miri, ignore)] // Miri isn't fast...
fn bench_path_cmp_fast_path_short(b: &mut test::Bencher) {
    let prefix = "my/home";
    let paths: Vec<_> =
        (0..1000).map(|num| PathBuf::from(prefix).join(format!("file {num}.rs"))).collect();

    let mut set = BTreeSet::new();

    paths.iter().for_each(|p| {
        set.insert(p.as_path());
    });

    b.iter(|| {
        set.remove(paths[500].as_path());
        set.insert(paths[500].as_path());
    });
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=bench_path_hashset | COMPLEXITY=6 | LINES=19

```rust
#[bench]
#[cfg_attr(miri, ignore)] // Miri isn't fast...
fn bench_path_hashset(b: &mut test::Bencher) {
    let prefix = "/my/home/is/my/castle/and/my/castle/has/a/rusty/workbench/";
    let paths: Vec<_> =
        (0..1000).map(|num| PathBuf::from(prefix).join(format!("file {num}.rs"))).collect();

    let mut set = HashSet::new();

    paths.iter().for_each(|p| {
        set.insert(p.as_path());
    });

    b.iter(|| {
        set.remove(paths[500].as_path());
        set.insert(black_box(paths[500].as_path()))
    });
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=bench_path_hashset_miss | COMPLEXITY=5 | LINES=18

```rust
#[bench]
#[cfg_attr(miri, ignore)] // Miri isn't fast...
fn bench_path_hashset_miss(b: &mut test::Bencher) {
    let prefix = "/my/home/is/my/castle/and/my/castle/has/a/rusty/workbench/";
    let paths: Vec<_> =
        (0..1000).map(|num| PathBuf::from(prefix).join(format!("file {num}.rs"))).collect();

    let mut set = HashSet::new();

    paths.iter().for_each(|p| {
        set.insert(p.as_path());
    });

    let probe = PathBuf::from(prefix).join("other");

    b.iter(|| set.remove(black_box(probe.as_path())));
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=bench_hash_path_short | COMPLEXITY=2 | LINES=10

```rust
#[bench]
fn bench_hash_path_short(b: &mut test::Bencher) {
    let mut hasher = DefaultHasher::new();
    let path = Path::new("explorer.exe");

    b.iter(|| black_box(path).hash(&mut hasher));

    black_box(hasher.finish());
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=bench_hash_path_long | COMPLEXITY=2 | LINES=11

```rust
#[bench]
fn bench_hash_path_long(b: &mut test::Bencher) {
    let mut hasher = DefaultHasher::new();
    let path =
        Path::new("/aaaaa/aaaaaa/./../aaaaaaaa/bbbbbbbbbbbbb/ccccccccccc/ddddddddd/eeeeeee.fff");

    b.iter(|| black_box(path).hash(&mut hasher));

    black_box(hasher.finish());
}
```

---
*Generated by AST tracing system*
