# AST Trace: ../rust/library/backtrace/src/symbolize/gimli/mmap_fake.rs

Generated 4 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
use super::File;
use super::mystd::io::{Read, Seek, SeekFrom};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=STRUCT | NAME=Mmap | COMPLEXITY=2 | LINES=6

```rust
use alloc::vec::Vec;
use core::ops::Deref;

pub struct Mmap {
    vec: Vec<u8>,
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=8 | LINES=11

```rust
impl Mmap {
    pub unsafe fn map(mut file: &File, len: usize, offset: u64) -> Option<Mmap> {
        let mut mmap = Mmap {
            vec: Vec::with_capacity(len),
        };
        file.seek(SeekFrom::Start(offset));
        file.read_to_end(&mut mmap.vec).ok()?;
        Some(mmap)
    }
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=deref | COMPLEXITY=5 | LINES=8

```rust
impl Deref for Mmap {
    type Target = [u8];

    fn deref(&self) -> &[u8] {
        &self.vec[..]
    }
}
```

---
*Generated by AST tracing system*
