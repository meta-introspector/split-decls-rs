# AST Trace: library/std/src/sys/fs/common.rs

Generated 5 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=3 | LINES=19

```rust
// SRC: library/std/src/sys/fs/common.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/fs/common.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/fs/common.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/fs/common.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/fs/common.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/fs/common.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/fs/common.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/std/src/sys/fs/common.rs
#![allow(dead_code)] // not used on all platforms

use crate::fs;
use crate::io::{self, Error, ErrorKind};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=copy | COMPLEXITY=6 | LINES=23

```rust
use crate::path::Path;
use crate::sys_common::ignore_notfound;

pub(crate) const NOT_FILE_ERROR: Error = io::const_error!(
    ErrorKind::InvalidInput,
    "the source path is neither a regular file nor a symlink to a regular file",
);

pub fn copy(from: &Path, to: &Path) -> io::Result<u64> {
    let mut reader = fs::File::open(from)?;
    let metadata = reader.metadata()?;

    if !metadata.is_file() {
        return Err(NOT_FILE_ERROR);
    }

    let mut writer = fs::File::create(to)?;
    let perm = metadata.permissions();

    let ret = io::copy(&mut reader, &mut writer)?;
    writer.set_permissions(perm)?;
    Ok(ret)
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=remove_dir_all | COMPLEXITY=6 | LINES=5

```rust
pub fn remove_dir_all(path: &Path) -> io::Result<()> {
    let filetype = fs::symlink_metadata(path)?.file_type();
    if filetype.is_symlink() { fs::remove_file(path) } else { remove_dir_all_recursive(path) }
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=remove_dir_all_recursive | COMPLEXITY=14 | LINES=20

```rust
fn remove_dir_all_recursive(path: &Path) -> io::Result<()> {
    for child in fs::read_dir(path)? {
        let result: io::Result<()> = try {
            let child = child?;
            if child.file_type()?.is_dir() {
                remove_dir_all_recursive(&child.path())?;
            } else {
                fs::remove_file(&child.path())?;
            }
        };
        // ignore internal NotFound errors to prevent race conditions
        if let Err(err) = &result
            && err.kind() != io::ErrorKind::NotFound
        {
            return result;
        }
    }
    ignore_notfound(fs::remove_dir(path))
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=exists | COMPLEXITY=8 | LINES=8

```rust
pub fn exists(path: &Path) -> io::Result<bool> {
    match fs::metadata(path) {
        Ok(_) => Ok(true),
        Err(error) if error.kind() == io::ErrorKind::NotFound => Ok(false),
        Err(error) => Err(error),
    }
}
```

---
*Generated by AST tracing system*
