# AST Trace: library/coretests/benches/num/flt2dec/strategy/grisu.rs

Generated 12 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=decode_finite | COMPLEXITY=9 | LINES=28

```rust
// SRC: library/coretests/benches/num/flt2dec/strategy/grisu.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/num/flt2dec/strategy/grisu.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/num/flt2dec/strategy/grisu.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/num/flt2dec/strategy/grisu.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/num/flt2dec/strategy/grisu.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/num/flt2dec/strategy/grisu.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/num/flt2dec/strategy/grisu.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/num/flt2dec/strategy/grisu.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/coretests/benches/num/flt2dec/strategy/grisu.rs
use core::num::flt2dec::strategy::grisu::*;
use std::mem::MaybeUninit;

use super::super::*;

pub fn decode_finite<T: DecodableFloat>(v: T) -> Decoded {
    match decode(v).1 {
        FullDecoded::Finite(decoded) => decoded,
        full_decoded => panic!("expected finite, got {full_decoded:?} instead"),
    }
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=bench_small_shortest | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_small_shortest(b: &mut Bencher) {
    let decoded = decode_finite(3.141592f64);
    let mut buf = [MaybeUninit::new(0); MAX_SIG_DIGITS];
    b.iter(|| {
        format_shortest(black_box(&decoded), &mut buf);
    });
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=bench_big_shortest | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_big_shortest(b: &mut Bencher) {
    let decoded = decode_finite(f64::MAX);
    let mut buf = [MaybeUninit::new(0); MAX_SIG_DIGITS];
    b.iter(|| {
        format_shortest(black_box(&decoded), &mut buf);
    });
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=bench_small_exact_3 | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_small_exact_3(b: &mut Bencher) {
    let decoded = decode_finite(3.141592f64);
    let mut buf = [MaybeUninit::new(0); 3];
    b.iter(|| {
        format_exact(black_box(&decoded), &mut buf, i16::MIN);
    });
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=bench_big_exact_3 | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_big_exact_3(b: &mut Bencher) {
    let decoded = decode_finite(f64::MAX);
    let mut buf = [MaybeUninit::new(0); 3];
    b.iter(|| {
        format_exact(black_box(&decoded), &mut buf, i16::MIN);
    });
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=bench_small_exact_12 | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_small_exact_12(b: &mut Bencher) {
    let decoded = decode_finite(3.141592f64);
    let mut buf = [MaybeUninit::new(0); 12];
    b.iter(|| {
        format_exact(black_box(&decoded), &mut buf, i16::MIN);
    });
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=bench_big_exact_12 | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_big_exact_12(b: &mut Bencher) {
    let decoded = decode_finite(f64::MAX);
    let mut buf = [MaybeUninit::new(0); 12];
    b.iter(|| {
        format_exact(black_box(&decoded), &mut buf, i16::MIN);
    });
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=bench_small_exact_inf | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_small_exact_inf(b: &mut Bencher) {
    let decoded = decode_finite(3.141592f64);
    let mut buf = [MaybeUninit::new(0); 1024];
    b.iter(|| {
        format_exact(black_box(&decoded), &mut buf, i16::MIN);
    });
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=bench_big_exact_inf | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_big_exact_inf(b: &mut Bencher) {
    let decoded = decode_finite(f64::MAX);
    let mut buf = [MaybeUninit::new(0); 1024];
    b.iter(|| {
        format_exact(black_box(&decoded), &mut buf, i16::MIN);
    });
}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=FUNCTION | NAME=bench_one_exact_inf | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_one_exact_inf(b: &mut Bencher) {
    let decoded = decode_finite(1.0);
    let mut buf = [MaybeUninit::new(0); 1024];
    b.iter(|| {
        format_exact(black_box(&decoded), &mut buf, i16::MIN);
    });
}
```

## Block 11
**Metadata**: AST_ID=11 | TYPE=FUNCTION | NAME=bench_trailing_zero_exact_inf | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_trailing_zero_exact_inf(b: &mut Bencher) {
    let decoded = decode_finite(250.000000000000000000000000);
    let mut buf = [MaybeUninit::new(0); 1024];
    b.iter(|| {
        format_exact(black_box(&decoded), &mut buf, i16::MIN);
    });
}
```

## Block 12
**Metadata**: AST_ID=12 | TYPE=FUNCTION | NAME=bench_halfway_point_exact_inf | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_halfway_point_exact_inf(b: &mut Bencher) {
    let decoded = decode_finite(1.00000000000000011102230246251565404236316680908203125);
    let mut buf = [MaybeUninit::new(0); 1024];
    b.iter(|| {
        format_exact(black_box(&decoded), &mut buf, i16::MIN);
    });
}
```

---
*Generated by AST tracing system*
