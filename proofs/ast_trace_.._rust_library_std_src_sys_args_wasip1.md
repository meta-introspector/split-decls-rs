# AST Trace: ../rust/library/std/src/sys/args/wasip1.rs

Generated 3 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=4

```rust
#![forbid(unsafe_op_in_unsafe_fn)]

pub use super::common::Args;
use crate::ffi::{CStr, OsStr, OsString};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=args | COMPLEXITY=2 | LINES=6

```rust
use crate::os::wasi::ffi::OsStrExt;

/// Returns the command line arguments
pub fn args() -> Args {
    Args::new(maybe_args().unwrap_or(Vec::new()))
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=maybe_args | COMPLEXITY=11 | LINES=16

```rust
fn maybe_args() -> Option<Vec<OsString>> {
    unsafe {
        let (argc, buf_size) = wasi::args_sizes_get().ok()?;
        let mut argv = Vec::with_capacity(argc);
        let mut buf = Vec::with_capacity(buf_size);
        wasi::args_get(argv.as_mut_ptr(), buf.as_mut_ptr()).ok()?;
        argv.set_len(argc);
        let mut ret = Vec::with_capacity(argc);
        for ptr in argv {
            let s = CStr::from_ptr(ptr.cast());
            ret.push(OsStr::from_bytes(s.to_bytes()).to_owned());
        }
        Some(ret)
    }
}
```

---
*Generated by AST tracing system*
