# AST Trace: ../rust/library/std/src/sys/pal/teeos/os.rs

Generated 17 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=4 | LINES=6

```rust
//! Implementation of `std::os` functionality for teeos

use core::marker::PhantomData;

use super::unsupported;
use crate::ffi::{OsStr, OsString};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
use crate::path::PathBuf;
use crate::{fmt, io, path};
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=errno | COMPLEXITY=7 | LINES=4

```rust
pub fn errno() -> i32 {
    unsafe { (*libc::__errno_location()) as i32 }
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=page_size | COMPLEXITY=7 | LINES=6

```rust
// Hardcoded to return 4096, since `sysconf` is only implemented as a stub.
pub fn page_size() -> usize {
    // unsafe { libc::sysconf(libc::_SC_PAGESIZE) as usize };
    4096
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=error_string | COMPLEXITY=2 | LINES=6

```rust
// Everything below are stubs and copied from unsupported.rs

pub fn error_string(_errno: i32) -> String {
    "error string unimplemented".to_string()
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=getcwd | COMPLEXITY=2 | LINES=4

```rust
pub fn getcwd() -> io::Result<PathBuf> {
    unsupported()
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=chdir | COMPLEXITY=2 | LINES=4

```rust
pub fn chdir(_: &path::Path) -> io::Result<()> {
    unsupported()
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=SplitPaths | COMPLEXITY=2 | LINES=6

```rust
pub struct SplitPaths<'a>(!, PhantomData<&'a ()>);

pub fn split_paths(_unparsed: &OsStr) -> SplitPaths<'_> {
    panic!("unsupported")
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=next | COMPLEXITY=5 | LINES=7

```rust
impl<'a> Iterator for SplitPaths<'a> {
    type Item = PathBuf;
    fn next(&mut self) -> Option<PathBuf> {
        self.0
    }
}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=FUNCTION | NAME=JoinPathsError; | COMPLEXITY=2 | LINES=11

```rust
#[derive(Debug)]
pub struct JoinPathsError;

pub fn join_paths<I, T>(_paths: I) -> Result<OsString, JoinPathsError>
where
    I: Iterator<Item = T>,
    T: AsRef<OsStr>,
{
    Err(JoinPathsError)
}
```

## Block 11
**Metadata**: AST_ID=11 | TYPE=FUNCTION | NAME=fmt | COMPLEXITY=5 | LINES=6

```rust
impl fmt::Display for JoinPathsError {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        "not supported on this platform yet".fmt(f)
    }
}
```

## Block 12
**Metadata**: AST_ID=12 | TYPE=IMPL | NAME=UNNAMED | COMPLEXITY=4 | LINES=2

```rust
impl crate::error::Error for JoinPathsError {}
```

## Block 13
**Metadata**: AST_ID=13 | TYPE=FUNCTION | NAME=current_exe | COMPLEXITY=2 | LINES=4

```rust
pub fn current_exe() -> io::Result<PathBuf> {
    unsupported()
}
```

## Block 14
**Metadata**: AST_ID=14 | TYPE=FUNCTION | NAME=temp_dir | COMPLEXITY=2 | LINES=4

```rust
pub fn temp_dir() -> PathBuf {
    panic!("no filesystem on this platform")
}
```

## Block 15
**Metadata**: AST_ID=15 | TYPE=FUNCTION | NAME=home_dir | COMPLEXITY=2 | LINES=4

```rust
pub fn home_dir() -> Option<PathBuf> {
    None
}
```

## Block 16
**Metadata**: AST_ID=16 | TYPE=FUNCTION | NAME=exit | COMPLEXITY=2 | LINES=4

```rust
pub fn exit(_code: i32) -> ! {
    panic!("TA should not call `exit`")
}
```

## Block 17
**Metadata**: AST_ID=17 | TYPE=FUNCTION | NAME=getpid | COMPLEXITY=2 | LINES=4

```rust
pub fn getpid() -> u32 {
    panic!("no pids on this platform")
}
```

---
*Generated by AST tracing system*
