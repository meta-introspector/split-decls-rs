# AST Trace: ../rust/library/std/src/sys/sync/mutex/no_threads.rs

Generated 4 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=STRUCT | NAME=Mutex | COMPLEXITY=2 | LINES=6

```rust
use crate::cell::Cell;

pub struct Mutex {
    // This platform has no threads, so we can use a Cell here.
    locked: Cell<bool>,
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=IMPL | NAME=UNNAMED | COMPLEXITY=8 | LINES=2

```rust
unsafe impl Send for Mutex {}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=IMPL | NAME=UNNAMED | COMPLEXITY=8 | LINES=1

```rust
unsafe impl Sync for Mutex {} // no threads on this platform
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=lock | COMPLEXITY=11 | LINES=22

```rust
impl Mutex {
    #[inline]
    pub const fn new() -> Mutex {
        Mutex { locked: Cell::new(false) }
    }

    #[inline]
    pub fn lock(&self) {
        assert_eq!(self.locked.replace(true), false, "cannot recursively acquire mutex");
    }

    #[inline]
    pub unsafe fn unlock(&self) {
        self.locked.set(false);
    }

    #[inline]
    pub fn try_lock(&self) -> bool {
        self.locked.replace(true) == false
    }
}
```

---
*Generated by AST tracing system*
