# AST Trace: library/portable-simd/crates/test_helpers/src/array.rs

Generated 7 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=4 | LINES=23

```rust
// SRC: library/portable-simd/crates/test_helpers/src/array.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/array.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/array.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/array.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/array.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/array.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/array.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/array.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/portable-simd/crates/test_helpers/src/array.rs
//! Generic-length array strategy.

// Adapted from proptest's array code
// Copyright 2017 Jason Lingle

use core::{marker::PhantomData, mem::MaybeUninit};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=3 | LINES=4

```rust
use proptest::{
    strategy::{NewTree, Strategy, ValueTree},
    test_runner::TestRunner,
};
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=STRUCT | NAME=UniformArrayStrategy | COMPLEXITY=2 | LINES=7

```rust
#[must_use = "strategies do nothing unless used"]
#[derive(Clone, Copy, Debug)]
pub struct UniformArrayStrategy<S, T> {
    strategy: S,
    _marker: PhantomData<T>,
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=4 | LINES=9

```rust
impl<S, T> UniformArrayStrategy<S, T> {
    pub const fn new(strategy: S) -> Self {
        Self {
            strategy,
            _marker: PhantomData,
        }
    }
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=STRUCT | NAME=ArrayValueTree | COMPLEXITY=2 | LINES=6

```rust
pub struct ArrayValueTree<T> {
    tree: T,
    shrinker: usize,
    last_shrinker: Option<usize>,
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=new_tree | COMPLEXITY=15 | LINES=25

```rust
impl<T, S, const LANES: usize> Strategy for UniformArrayStrategy<S, [T; LANES]>
where
    T: core::fmt::Debug,
    S: Strategy<Value = T>,
{
    type Tree = ArrayValueTree<[S::Tree; LANES]>;
    type Value = [T; LANES];

    fn new_tree(&self, runner: &mut TestRunner) -> NewTree<Self> {
        let tree: [S::Tree; LANES] = unsafe {
            #[allow(clippy::uninit_assumed_init)]
            let mut tree: [MaybeUninit<S::Tree>; LANES] = MaybeUninit::uninit().assume_init();
            for t in tree.iter_mut() {
                *t = MaybeUninit::new(self.strategy.new_tree(runner)?)
            }
            core::mem::transmute_copy(&tree)
        };
        Ok(ArrayValueTree {
            tree,
            shrinker: 0,
            last_shrinker: None,
        })
    }
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=current | COMPLEXITY=32 | LINES=42

```rust
impl<T: ValueTree, const LANES: usize> ValueTree for ArrayValueTree<[T; LANES]> {
    type Value = [T::Value; LANES];

    fn current(&self) -> Self::Value {
        unsafe {
            #[allow(clippy::uninit_assumed_init)]
            let mut value: [MaybeUninit<T::Value>; LANES] = MaybeUninit::uninit().assume_init();
            for (tree_elem, value_elem) in self.tree.iter().zip(value.iter_mut()) {
                *value_elem = MaybeUninit::new(tree_elem.current());
            }
            core::mem::transmute_copy(&value)
        }
    }

    fn simplify(&mut self) -> bool {
        while self.shrinker < LANES {
            if self.tree[self.shrinker].simplify() {
                self.last_shrinker = Some(self.shrinker);
                return true;
            } else {
                self.shrinker += 1;
            }
        }

        false
    }

    fn complicate(&mut self) -> bool {
        if let Some(shrinker) = self.last_shrinker {
            self.shrinker = shrinker;
            if self.tree[shrinker].complicate() {
                true
            } else {
                self.last_shrinker = None;
                false
            }
        } else {
            false
        }
    }
}
```

---
*Generated by AST tracing system*
