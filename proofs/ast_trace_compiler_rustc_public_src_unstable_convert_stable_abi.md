# AST Trace: compiler/rustc_public/src/unstable/convert/stable/abi.rs

Generated 28 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=4 | LINES=22

```rust
// SRC: compiler/rustc_public/src/unstable/convert/stable/abi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: compiler/rustc_public/src/unstable/convert/stable/abi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: compiler/rustc_public/src/unstable/convert/stable/abi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: compiler/rustc_public/src/unstable/convert/stable/abi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: compiler/rustc_public/src/unstable/convert/stable/abi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: compiler/rustc_public/src/unstable/convert/stable/abi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: compiler/rustc_public/src/unstable/convert/stable/abi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: compiler/rustc_public/src/unstable/convert/stable/abi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/compiler/rustc_public/src/unstable/convert/stable/abi.rs
// Conversion of internal Rust compiler `rustc_target` and `rustc_abi` items to stable ones.

#[allow(rustc::usage_of_qualified_ty)]

use crate::rustc_abi::{ArmCall, CanonAbi, InterruptKind, X86Call};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=10

```rust
use crate::rustc_complete::ty;
use crate::rustc_public_bridge::Tables;
use crate::rustc_public_bridge::context::CompilerCtxt;
use crate::rustc_target::callconv;

use crate::abi::{
    AddressSpace, ArgAbi, CallConvention, FieldsShape, FloatLength, FnAbi, IntegerLength,
    IntegerType, Layout, LayoutShape, PassMode, Primitive, ReprFlags, ReprOptions, Scalar,
    TagEncoding, TyAndLayout, ValueAbi, VariantsShape, WrappingRange,
};
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=3

```rust
use crate::compiler_interface::BridgeTys;
use crate::target::MachineSize as Size;
use crate::ty::{Align, VariantIdx};
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
use crate::unstable::Stable;
use crate::{IndexedVal, opaque};
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=5 | LINES=7

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::VariantIdx {
    type T = VariantIdx;
    fn stable(&self, _: &mut Tables<'_, BridgeTys>, _: &CompilerCtxt<'_, BridgeTys>) -> Self::T {
        VariantIdx::to_val(self.as_usize())
    }
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=9 | LINES=11

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::Endian {
    type T = crate::target::Endian;

    fn stable(&self, _: &mut Tables<'_, BridgeTys>, _: &CompilerCtxt<'_, BridgeTys>) -> Self::T {
        match self {
            crate::rustc_abi::Endian::Little => crate::target::Endian::Little,
            crate::rustc_abi::Endian::Big => crate::target::Endian::Big,
        }
    }
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=6 | LINES=12

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::TyAndLayout<'tcx, ty::Ty<'tcx>> {
    type T = TyAndLayout;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        TyAndLayout { ty: self.ty.stable(tables, cx), layout: self.layout.stable(tables, cx) }
    }
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=5 | LINES=12

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::Layout<'tcx> {
    type T = Layout;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        tables.layout_id(cx.lift(*self).unwrap())
    }
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=7 | LINES=18

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::LayoutData<crate::rustc_abi::FieldIdx, crate::rustc_abi::VariantIdx> {
    type T = LayoutShape;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        LayoutShape {
            fields: self.fields.stable(tables, cx),
            variants: self.variants.stable(tables, cx),
            abi: self.backend_repr.stable(tables, cx),
            abi_align: self.align.abi.stable(tables, cx),
            size: self.size.stable(tables, cx),
        }
    }
}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=7 | LINES=20

```rust
impl<'tcx> Stable<'tcx> for callconv::FnAbi<'tcx, ty::Ty<'tcx>> {
    type T = FnAbi;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        assert!(self.args.len() >= self.fixed_count as usize);
        assert!(!self.c_variadic || matches!(self.conv, CanonAbi::C));
        FnAbi {
            args: self.args.as_ref().stable(tables, cx),
            ret: self.ret.stable(tables, cx),
            fixed_count: self.fixed_count,
            conv: self.conv.stable(tables, cx),
            c_variadic: self.c_variadic,
        }
    }
}
```

## Block 11
**Metadata**: AST_ID=11 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=6 | LINES=16

```rust
impl<'tcx> Stable<'tcx> for callconv::ArgAbi<'tcx, ty::Ty<'tcx>> {
    type T = ArgAbi;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        ArgAbi {
            ty: self.layout.ty.stable(tables, cx),
            layout: self.layout.layout.stable(tables, cx),
            mode: self.mode.stable(tables, cx),
        }
    }
}
```

## Block 12
**Metadata**: AST_ID=12 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=25 | LINES=36

```rust
impl<'tcx> Stable<'tcx> for CanonAbi {
    type T = CallConvention;

    fn stable(&self, _: &mut Tables<'_, BridgeTys>, _: &CompilerCtxt<'_, BridgeTys>) -> Self::T {
        match self {
            CanonAbi::C => CallConvention::C,
            CanonAbi::Rust => CallConvention::Rust,
            CanonAbi::RustCold => CallConvention::Cold,
            CanonAbi::Custom => CallConvention::Custom,
            CanonAbi::Arm(arm_call) => match arm_call {
                ArmCall::Aapcs => CallConvention::ArmAapcs,
                ArmCall::CCmseNonSecureCall => CallConvention::CCmseNonSecureCall,
                ArmCall::CCmseNonSecureEntry => CallConvention::CCmseNonSecureEntry,
            },
            CanonAbi::GpuKernel => CallConvention::GpuKernel,
            CanonAbi::Interrupt(interrupt_kind) => match interrupt_kind {
                InterruptKind::Avr => CallConvention::AvrInterrupt,
                InterruptKind::AvrNonBlocking => CallConvention::AvrNonBlockingInterrupt,
                InterruptKind::Msp430 => CallConvention::Msp430Intr,
                InterruptKind::RiscvMachine | InterruptKind::RiscvSupervisor => {
                    CallConvention::RiscvInterrupt
                }
                InterruptKind::X86 => CallConvention::X86Intr,
            },
            CanonAbi::X86(x86_call) => match x86_call {
                X86Call::Fastcall => CallConvention::X86Fastcall,
                X86Call::Stdcall => CallConvention::X86Stdcall,
                X86Call::SysV64 => CallConvention::X86_64SysV,
                X86Call::Thiscall => CallConvention::X86ThisCall,
                X86Call::Vectorcall => CallConvention::X86VectorCall,
                X86Call::Win64 => CallConvention::X86_64Win64,
            },
        }
    }
}
```

## Block 13
**Metadata**: AST_ID=13 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=16 | LINES=22

```rust
impl<'tcx> Stable<'tcx> for callconv::PassMode {
    type T = PassMode;

    fn stable(&self, _: &mut Tables<'_, BridgeTys>, _: &CompilerCtxt<'_, BridgeTys>) -> Self::T {
        match self {
            callconv::PassMode::Ignore => PassMode::Ignore,
            callconv::PassMode::Direct(attr) => PassMode::Direct(opaque(attr)),
            callconv::PassMode::Pair(first, second) => {
                PassMode::Pair(opaque(first), opaque(second))
            }
            callconv::PassMode::Cast { pad_i32, cast } => {
                PassMode::Cast { pad_i32: *pad_i32, cast: opaque(cast) }
            }
            callconv::PassMode::Indirect { attrs, meta_attrs, on_stack } => PassMode::Indirect {
                attrs: opaque(attrs),
                meta_attrs: opaque(meta_attrs),
                on_stack: *on_stack,
            },
        }
    }
}
```

## Block 14
**Metadata**: AST_ID=14 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=16 | LINES=21

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::FieldsShape<crate::rustc_abi::FieldIdx> {
    type T = FieldsShape;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        match self {
            crate::rustc_abi::FieldsShape::Primitive => FieldsShape::Primitive,
            crate::rustc_abi::FieldsShape::Union(count) => FieldsShape::Union(*count),
            crate::rustc_abi::FieldsShape::Array { stride, count } => {
                FieldsShape::Array { stride: stride.stable(tables, cx), count: *count }
            }
            crate::rustc_abi::FieldsShape::Arbitrary { offsets, .. } => {
                FieldsShape::Arbitrary { offsets: offsets.iter().as_slice().stable(tables, cx) }
            }
        }
    }
}
```

## Block 15
**Metadata**: AST_ID=15 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=16 | LINES=25

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::Variants<crate::rustc_abi::FieldIdx, crate::rustc_abi::VariantIdx> {
    type T = VariantsShape;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        match self {
            crate::rustc_abi::Variants::Single { index } => {
                VariantsShape::Single { index: index.stable(tables, cx) }
            }
            crate::rustc_abi::Variants::Empty => VariantsShape::Empty,
            crate::rustc_abi::Variants::Multiple { tag, tag_encoding, tag_field, variants } => {
                VariantsShape::Multiple {
                    tag: tag.stable(tables, cx),
                    tag_encoding: tag_encoding.stable(tables, cx),
                    tag_field: tag_field.stable(tables, cx),
                    variants: variants.iter().as_slice().stable(tables, cx),
                }
            }
        }
    }
}
```

## Block 16
**Metadata**: AST_ID=16 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=13 | LINES=21

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::TagEncoding<crate::rustc_abi::VariantIdx> {
    type T = TagEncoding;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        match self {
            crate::rustc_abi::TagEncoding::Direct => TagEncoding::Direct,
            crate::rustc_abi::TagEncoding::Niche { untagged_variant, niche_variants, niche_start } => {
                TagEncoding::Niche {
                    untagged_variant: untagged_variant.stable(tables, cx),
                    niche_variants: niche_variants.stable(tables, cx),
                    niche_start: *niche_start,
                }
            }
        }
    }
}
```

## Block 17
**Metadata**: AST_ID=17 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=16 | LINES=21

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::BackendRepr {
    type T = ValueAbi;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        match *self {
            crate::rustc_abi::BackendRepr::Scalar(scalar) => ValueAbi::Scalar(scalar.stable(tables, cx)),
            crate::rustc_abi::BackendRepr::ScalarPair(first, second) => {
                ValueAbi::ScalarPair(first.stable(tables, cx), second.stable(tables, cx))
            }
            crate::rustc_abi::BackendRepr::SimdVector { element, count } => {
                ValueAbi::Vector { element: element.stable(tables, cx), count }
            }
            crate::rustc_abi::BackendRepr::Memory { sized } => ValueAbi::Aggregate { sized },
        }
    }
}
```

## Block 18
**Metadata**: AST_ID=18 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=5 | LINES=8

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::Size {
    type T = Size;

    fn stable(&self, _: &mut Tables<'_, BridgeTys>, _: &CompilerCtxt<'_, BridgeTys>) -> Self::T {
        Size::from_bits(self.bits_usize())
    }
}
```

## Block 19
**Metadata**: AST_ID=19 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=5 | LINES=8

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::Align {
    type T = Align;

    fn stable(&self, _: &mut Tables<'_, BridgeTys>, _: &CompilerCtxt<'_, BridgeTys>) -> Self::T {
        self.bytes()
    }
}
```

## Block 20
**Metadata**: AST_ID=20 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=14 | LINES=18

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::Scalar {
    type T = Scalar;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        match self {
            crate::rustc_abi::Scalar::Initialized { value, valid_range } => Scalar::Initialized {
                value: value.stable(tables, cx),
                valid_range: valid_range.stable(tables, cx),
            },
            crate::rustc_abi::Scalar::Union { value } => Scalar::Union { value: value.stable(tables, cx) },
        }
    }
}
```

## Block 21
**Metadata**: AST_ID=21 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=14 | LINES=20

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::Primitive {
    type T = Primitive;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        match self {
            crate::rustc_abi::Primitive::Int(length, signed) => {
                Primitive::Int { length: length.stable(tables, cx), signed: *signed }
            }
            crate::rustc_abi::Primitive::Float(length) => {
                Primitive::Float { length: length.stable(tables, cx) }
            }
            crate::rustc_abi::Primitive::Pointer(space) => Primitive::Pointer(space.stable(tables, cx)),
        }
    }
}
```

## Block 22
**Metadata**: AST_ID=22 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=5 | LINES=8

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::AddressSpace {
    type T = AddressSpace;

    fn stable(&self, _: &mut Tables<'_, BridgeTys>, _: &CompilerCtxt<'_, BridgeTys>) -> Self::T {
        AddressSpace(self.0)
    }
}
```

## Block 23
**Metadata**: AST_ID=23 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=10 | LINES=14

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::Integer {
    type T = IntegerLength;

    fn stable(&self, _: &mut Tables<'_, BridgeTys>, _: &CompilerCtxt<'_, BridgeTys>) -> Self::T {
        match self {
            crate::rustc_abi::Integer::I8 => IntegerLength::I8,
            crate::rustc_abi::Integer::I16 => IntegerLength::I16,
            crate::rustc_abi::Integer::I32 => IntegerLength::I32,
            crate::rustc_abi::Integer::I64 => IntegerLength::I64,
            crate::rustc_abi::Integer::I128 => IntegerLength::I128,
        }
    }
}
```

## Block 24
**Metadata**: AST_ID=24 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=9 | LINES=13

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::Float {
    type T = FloatLength;

    fn stable(&self, _: &mut Tables<'_, BridgeTys>, _: &CompilerCtxt<'_, BridgeTys>) -> Self::T {
        match self {
            crate::rustc_abi::Float::F16 => FloatLength::F16,
            crate::rustc_abi::Float::F32 => FloatLength::F32,
            crate::rustc_abi::Float::F64 => FloatLength::F64,
            crate::rustc_abi::Float::F128 => FloatLength::F128,
        }
    }
}
```

## Block 25
**Metadata**: AST_ID=25 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=6 | LINES=8

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::WrappingRange {
    type T = WrappingRange;

    fn stable(&self, _: &mut Tables<'_, BridgeTys>, _: &CompilerCtxt<'_, BridgeTys>) -> Self::T {
        WrappingRange { start: self.start, end: self.end }
    }
}
```

## Block 26
**Metadata**: AST_ID=26 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=6 | LINES=17

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::ReprFlags {
    type T = ReprFlags;

    fn stable<'cx>(
        &self,
        _tables: &mut Tables<'cx, BridgeTys>,
        _cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        ReprFlags {
            is_simd: self.intersects(Self::IS_SIMD),
            is_c: self.intersects(Self::IS_C),
            is_transparent: self.intersects(Self::IS_TRANSPARENT),
            is_linear: self.intersects(Self::IS_LINEAR),
        }
    }
}
```

## Block 27
**Metadata**: AST_ID=27 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=13 | LINES=17

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::IntegerType {
    type T = IntegerType;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        match self {
            crate::rustc_abi::IntegerType::Pointer(signed) => IntegerType::Pointer { is_signed: *signed },
            crate::rustc_abi::IntegerType::Fixed(integer, signed) => {
                IntegerType::Fixed { length: integer.stable(tables, cx), is_signed: *signed }
            }
        }
    }
}
```

## Block 28
**Metadata**: AST_ID=28 | TYPE=FUNCTION | NAME=stable | COMPLEXITY=7 | LINES=17

```rust
impl<'tcx> Stable<'tcx> for crate::rustc_abi::ReprOptions {
    type T = ReprOptions;

    fn stable<'cx>(
        &self,
        tables: &mut Tables<'cx, BridgeTys>,
        cx: &CompilerCtxt<'cx, BridgeTys>,
    ) -> Self::T {
        ReprOptions {
            int: self.int.map(|int| int.stable(tables, cx)),
            align: self.align.map(|align| align.stable(tables, cx)),
            pack: self.pack.map(|pack| pack.stable(tables, cx)),
            flags: self.flags.stable(tables, cx),
        }
    }
}
```

---
*Generated by AST tracing system*
