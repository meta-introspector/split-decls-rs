# AST Trace: ../rust/library/std/tests/log-knows-the-names-of-variants-in-std.rs

Generated 3 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=ENUM | NAME=UNNAMED | COMPLEXITY=2 | LINES=8

```rust
#![allow(non_camel_case_types)]
#![allow(dead_code)]

#[derive(Clone, Debug)]
enum foo {
    a(usize),
    b(String),
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=check_log | COMPLEXITY=3 | LINES=4

```rust
fn check_log<T: std::fmt::Debug>(exp: String, v: T) {
    assert_eq!(exp, format!("{:?}", v));
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=log_knows_the_names_of_variants_in_std | COMPLEXITY=4 | LINES=15

```rust
#[test]
fn log_knows_the_names_of_variants_in_std() {
    let mut x = Some(foo::a(22));
    let exp = "Some(a(22))".to_string();
    let act = format!("{:?}", x);
    assert_eq!(act, exp);
    check_log(exp, x);

    x = None;
    let exp = "None".to_string();
    let act = format!("{:?}", x);
    assert_eq!(act, exp);
    check_log(exp, x);
}
```

---
*Generated by AST tracing system*
