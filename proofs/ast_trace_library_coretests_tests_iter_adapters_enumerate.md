# AST Trace: library/coretests/tests/iter/adapters/enumerate.rs

Generated 9 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=test_iterator_enumerate | COMPLEXITY=7 | LINES=28

```rust
// SRC: library/coretests/tests/iter/adapters/enumerate.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/tests/iter/adapters/enumerate.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/tests/iter/adapters/enumerate.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/tests/iter/adapters/enumerate.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/tests/iter/adapters/enumerate.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/tests/iter/adapters/enumerate.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/tests/iter/adapters/enumerate.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/tests/iter/adapters/enumerate.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/coretests/tests/iter/adapters/enumerate.rs
use core::iter::*;
use core::num::NonZero;

#[test]
fn test_iterator_enumerate() {
    let xs = [0, 1, 2, 3, 4, 5];
    let it = xs.iter().enumerate();
    for (i, &x) in it {
        assert_eq!(i, x);
    }
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=test_iterator_enumerate_nth | COMPLEXITY=11 | LINES=22

```rust
#[test]
fn test_iterator_enumerate_nth() {
    let xs = [0, 1, 2, 3, 4, 5];
    for (i, &x) in xs.iter().enumerate() {
        assert_eq!(i, x);
    }

    let mut it = xs.iter().enumerate();
    while let Some((i, &x)) = it.nth(0) {
        assert_eq!(i, x);
    }

    let mut it = xs.iter().enumerate();
    while let Some((i, &x)) = it.nth(1) {
        assert_eq!(i, x);
    }

    let (i, &x) = xs.iter().enumerate().nth(3).unwrap();
    assert_eq!(i, x);
    assert_eq!(i, 3);
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=test_iterator_enumerate_nth_back | COMPLEXITY=8 | LINES=18

```rust
#[test]
fn test_iterator_enumerate_nth_back() {
    let xs = [0, 1, 2, 3, 4, 5];
    let mut it = xs.iter().enumerate();
    while let Some((i, &x)) = it.nth_back(0) {
        assert_eq!(i, x);
    }

    let mut it = xs.iter().enumerate();
    while let Some((i, &x)) = it.nth_back(1) {
        assert_eq!(i, x);
    }

    let (i, &x) = xs.iter().enumerate().nth_back(3).unwrap();
    assert_eq!(i, x);
    assert_eq!(i, 2);
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=test_iterator_enumerate_count | COMPLEXITY=2 | LINES=6

```rust
#[test]
fn test_iterator_enumerate_count() {
    let xs = [0, 1, 2, 3, 4, 5];
    assert_eq!(xs.iter().enumerate().count(), 6);
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=test_iterator_enumerate_advance_by | COMPLEXITY=2 | LINES=14

```rust
#[test]
fn test_iterator_enumerate_advance_by() {
    let xs = [0, 1, 2, 3, 4, 5];
    let mut it = xs.iter().enumerate();
    assert_eq!(it.advance_by(0), Ok(()));
    assert_eq!(it.next(), Some((0, &0)));
    assert_eq!(it.advance_by(1), Ok(()));
    assert_eq!(it.next(), Some((2, &2)));
    assert_eq!(it.advance_by(2), Ok(()));
    assert_eq!(it.next(), Some((5, &5)));
    assert_eq!(it.advance_by(1), Err(NonZero::new(1).unwrap()));
    assert_eq!(it.next(), None);
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=test_iterator_enumerate_fold | COMPLEXITY=5 | LINES=24

```rust
#[test]
fn test_iterator_enumerate_fold() {
    let xs = [0, 1, 2, 3, 4, 5];
    let mut it = xs.iter().enumerate();
    // steal a couple to get an interesting offset
    assert_eq!(it.next(), Some((0, &0)));
    assert_eq!(it.next(), Some((1, &1)));
    let i = it.fold(2, |i, (j, &x)| {
        assert_eq!(i, j);
        assert_eq!(x, xs[j]);
        i + 1
    });
    assert_eq!(i, xs.len());

    let mut it = xs.iter().enumerate();
    assert_eq!(it.next(), Some((0, &0)));
    let i = it.rfold(xs.len() - 1, |i, (j, &x)| {
        assert_eq!(i, j);
        assert_eq!(x, xs[j]);
        i - 1
    });
    assert_eq!(i, 0);
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=test_enumerate_try_folds | COMPLEXITY=3 | LINES=14

```rust
#[test]
fn test_enumerate_try_folds() {
    let f = &|acc, (i, x)| usize::checked_add(2 * acc, x / (i + 1) + i);
    assert_eq!((9..18).enumerate().try_fold(7, f), (0..9).map(|i| (i, i + 9)).try_fold(7, f));
    assert_eq!((9..18).enumerate().try_rfold(7, f), (0..9).map(|i| (i, i + 9)).try_rfold(7, f));

    let mut iter = (100..200).enumerate();
    let f = &|acc, (i, x)| u8::checked_add(acc, u8::checked_div(x, i as u8 + 1)?);
    assert_eq!(iter.try_fold(0, f), None);
    assert_eq!(iter.next(), Some((7, 107)));
    assert_eq!(iter.try_rfold(0, f), None);
    assert_eq!(iter.next_back(), Some((11, 111)));
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=test_double_ended_enumerate | COMPLEXITY=2 | LINES=13

```rust
#[test]
fn test_double_ended_enumerate() {
    let xs = [1, 2, 3, 4, 5, 6];
    let mut it = xs.iter().cloned().enumerate();
    assert_eq!(it.next(), Some((0, 1)));
    assert_eq!(it.next(), Some((1, 2)));
    assert_eq!(it.next_back(), Some((5, 6)));
    assert_eq!(it.next_back(), Some((4, 5)));
    assert_eq!(it.next_back(), Some((3, 4)));
    assert_eq!(it.next_back(), Some((2, 3)));
    assert_eq!(it.next(), None);
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=test_empty_iterator_enumerate_next_index | COMPLEXITY=2 | LINES=10

```rust
#[test]
fn test_empty_iterator_enumerate_next_index() {
    let mut it = empty::<i32>().enumerate();
    assert_eq!(it.next_index(), 0);
    assert_eq!(it.next_index(), 0);
    assert_eq!(it.next(), None);
    assert_eq!(it.next_index(), 0);
    assert_eq!(it.next_index(), 0);
}
```

---
*Generated by AST tracing system*
