# AST Trace: ../rust/library/stdarch/crates/stdarch-verify/build.rs

Generated 2 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=main | COMPLEXITY=3 | LINES=11

```rust
use std::path::Path;

fn main() {
    let dir = Path::new(env!("CARGO_MANIFEST_DIR"));
    let root = dir.parent().unwrap();
    eprintln!("root: {}", root.display());
    walk(&root.join("core_arch/src/x86"));
    walk(&root.join("core_arch/src/x86_64"));
    walk(&root.join("core_arch/src/arm"));
    walk(&root.join("core_arch/src/aarch64"));
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=walk | COMPLEXITY=13 | LINES=17

```rust
fn walk(root: &Path) {
    for file in root.read_dir().unwrap() {
        eprintln!("root: {}", root.display());
        let file = file.unwrap();
        if file.file_type().unwrap().is_dir() {
            walk(&file.path());
            continue;
        }
        let path = file.path();
        if path.extension().and_then(|s| s.to_str()) != Some("rs") {
            continue;
        }

        println!("cargo:rerun-if-changed={}", path.display());
    }
}
```

---
*Generated by AST tracing system*
