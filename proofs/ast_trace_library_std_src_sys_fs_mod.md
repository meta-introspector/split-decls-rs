# AST Trace: library/std/src/sys/fs/mod.rs

Generated 20 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=3 | LINES=19

```rust
// SRC: library/std/src/sys/fs/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/fs/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/fs/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/fs/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/fs/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/fs/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/fs/mod.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/std/src/sys/fs/mod.rs
#![deny(unsafe_op_in_unsafe_fn)]

use crate::io;
use crate::path::{Path, PathBuf};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=14 | LINES=56

```rust
pub mod common;

cfg_select! {
    target_family = "unix" => {
// MOD DECLARATION: unix (from library/std/src/sys/fs/mod.rs)
// REPLACED: mod unix; -> use crate::unix::*;
use crate::unix::*;
        use unix as imp;
        pub use unix::{chown, fchown, lchown, mkfifo};
        #[cfg(not(target_os = "fuchsia"))]
        pub use unix::chroot;
        pub(crate) use unix::debug_assert_fd_is_open;
        #[cfg(any(target_os = "linux", target_os = "android"))]
        pub(crate) use unix::CachedFileMetadata;
        use crate::sys::common::small_c_string::run_path_with_cstr as with_native_path;
    }
    target_os = "windows" => {
// MOD DECLARATION: windows (from library/std/src/sys/fs/mod.rs)
// REPLACED: mod windows; -> use crate::windows::*;
use crate::windows::*;
        use windows as imp;
        pub use windows::{symlink_inner, junction_point};
        use crate::sys::path::with_native_path;
    }
    target_os = "hermit" => {
// MOD DECLARATION: hermit (from library/std/src/sys/fs/mod.rs)
// REPLACED: mod hermit; -> use crate::hermit::*;
use crate::hermit::*;
        use hermit as imp;
    }
    target_os = "solid_asp3" => {
// MOD DECLARATION: solid (from library/std/src/sys/fs/mod.rs)
// REPLACED: mod solid; -> use crate::solid::*;
use crate::solid::*;
        use solid as imp;
    }
    target_os = "uefi" => {
// MOD DECLARATION: uefi (from library/std/src/sys/fs/mod.rs)
// REPLACED: mod uefi; -> use crate::uefi::*;
use crate::uefi::*;
        use uefi as imp;
    }
    target_os = "wasi" => {
// MOD DECLARATION: wasi (from library/std/src/sys/fs/mod.rs)
// REPLACED: mod wasi; -> use crate::wasi::*;
use crate::wasi::*;
        use wasi as imp;
    }
    _ => {
// MOD DECLARATION: unsupported (from library/std/src/sys/fs/mod.rs)
// REPLACED: mod unsupported; -> use crate::unsupported::*;
use crate::unsupported::*;
        use unsupported as imp;
    }
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=with_native_path | COMPLEXITY=2 | LINES=7

```rust
// FIXME: Replace this with platform-specific path conversion functions.
#[cfg(not(any(target_family = "unix", target_os = "windows")))]
#[inline]
pub fn with_native_path<T>(path: &Path, f: &dyn Fn(&Path) -> io::Result<T>) -> io::Result<T> {
    f(path)
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=5

```rust
pub use imp::{
    DirBuilder, DirEntry, File, FileAttr, FilePermissions, FileTimes, FileType, OpenOptions,
    ReadDir,
};
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=read_dir | COMPLEXITY=2 | LINES=5

```rust
pub fn read_dir(path: &Path) -> io::Result<ReadDir> {
    // FIXME: use with_native_path on all platforms
    imp::readdir(path)
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=remove_file | COMPLEXITY=2 | LINES=4

```rust
pub fn remove_file(path: &Path) -> io::Result<()> {
    with_native_path(path, &imp::unlink)
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=rename | COMPLEXITY=2 | LINES=4

```rust
pub fn rename(old: &Path, new: &Path) -> io::Result<()> {
    with_native_path(old, &|old| with_native_path(new, &|new| imp::rename(old, new)))
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=remove_dir | COMPLEXITY=2 | LINES=4

```rust
pub fn remove_dir(path: &Path) -> io::Result<()> {
    with_native_path(path, &imp::rmdir)
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=remove_dir_all | COMPLEXITY=2 | LINES=8

```rust
pub fn remove_dir_all(path: &Path) -> io::Result<()> {
    // FIXME: use with_native_path on all platforms
    #[cfg(not(windows))]
    return imp::remove_dir_all(path);
    #[cfg(windows)]
    with_native_path(path, &imp::remove_dir_all)
}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=FUNCTION | NAME=read_link | COMPLEXITY=2 | LINES=4

```rust
pub fn read_link(path: &Path) -> io::Result<PathBuf> {
    with_native_path(path, &imp::readlink)
}
```

## Block 11
**Metadata**: AST_ID=11 | TYPE=FUNCTION | NAME=symlink | COMPLEXITY=3 | LINES=10

```rust
pub fn symlink(original: &Path, link: &Path) -> io::Result<()> {
    // FIXME: use with_native_path on all platforms
    #[cfg(windows)]
    return imp::symlink(original, link);
    #[cfg(not(windows))]
    with_native_path(original, &|original| {
        with_native_path(link, &|link| imp::symlink(original, link))
    })
}
```

## Block 12
**Metadata**: AST_ID=12 | TYPE=FUNCTION | NAME=hard_link | COMPLEXITY=3 | LINES=6

```rust
pub fn hard_link(original: &Path, link: &Path) -> io::Result<()> {
    with_native_path(original, &|original| {
        with_native_path(link, &|link| imp::link(original, link))
    })
}
```

## Block 13
**Metadata**: AST_ID=13 | TYPE=FUNCTION | NAME=metadata | COMPLEXITY=2 | LINES=4

```rust
pub fn metadata(path: &Path) -> io::Result<FileAttr> {
    with_native_path(path, &imp::stat)
}
```

## Block 14
**Metadata**: AST_ID=14 | TYPE=FUNCTION | NAME=symlink_metadata | COMPLEXITY=2 | LINES=4

```rust
pub fn symlink_metadata(path: &Path) -> io::Result<FileAttr> {
    with_native_path(path, &imp::lstat)
}
```

## Block 15
**Metadata**: AST_ID=15 | TYPE=FUNCTION | NAME=set_permissions | COMPLEXITY=2 | LINES=4

```rust
pub fn set_permissions(path: &Path, perm: FilePermissions) -> io::Result<()> {
    with_native_path(path, &|path| imp::set_perm(path, perm.clone()))
}
```

## Block 16
**Metadata**: AST_ID=16 | TYPE=FUNCTION | NAME=set_permissions_nofollow | COMPLEXITY=4 | LINES=17

```rust
#[cfg(unix)]
pub fn set_permissions_nofollow(path: &Path, perm: crate::fs::Permissions) -> io::Result<()> {
    use crate::fs::OpenOptions;

    let mut options = OpenOptions::new();

    // ESP-IDF and Horizon do not support O_NOFOLLOW, so we skip setting it.
    // Their filesystems do not have symbolic links, so no special handling is required.
    #[cfg(not(any(target_os = "espidf", target_os = "horizon")))]
    {
        use crate::os::unix::fs::OpenOptionsExt;
        options.custom_flags(libc::O_NOFOLLOW);
    }

    options.open(path)?.set_permissions(perm)
}
```

## Block 17
**Metadata**: AST_ID=17 | TYPE=FUNCTION | NAME=set_permissions_nofollow | COMPLEXITY=2 | LINES=7

```rust
#[cfg(not(unix))]
pub fn set_permissions_nofollow(_path: &Path, _perm: crate::fs::Permissions) -> io::Result<()> {
    crate::unimplemented!(
        "`set_permissions_nofollow` is currently only implemented on Unix platforms"
    )
}
```

## Block 18
**Metadata**: AST_ID=18 | TYPE=FUNCTION | NAME=canonicalize | COMPLEXITY=2 | LINES=4

```rust
pub fn canonicalize(path: &Path) -> io::Result<PathBuf> {
    with_native_path(path, &imp::canonicalize)
}
```

## Block 19
**Metadata**: AST_ID=19 | TYPE=FUNCTION | NAME=copy | COMPLEXITY=2 | LINES=8

```rust
pub fn copy(from: &Path, to: &Path) -> io::Result<u64> {
    // FIXME: use with_native_path on all platforms
    #[cfg(not(windows))]
    return imp::copy(from, to);
    #[cfg(windows)]
    with_native_path(from, &|from| with_native_path(to, &|to| imp::copy(from, to)))
}
```

## Block 20
**Metadata**: AST_ID=20 | TYPE=FUNCTION | NAME=exists | COMPLEXITY=2 | LINES=8

```rust
pub fn exists(path: &Path) -> io::Result<bool> {
    // FIXME: use with_native_path on all platforms
    #[cfg(not(windows))]
    return imp::exists(path);
    #[cfg(windows)]
    with_native_path(path, &imp::exists)
}
```

---
*Generated by AST tracing system*
