# AST Trace: ../rust/library/std/src/sys/pal/windows/api/tests.rs

Generated 3 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
use crate::sys::pal::windows::api::{utf16, wide_str};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=9 | LINES=7

```rust
macro_rules! check_utf16 {
    ($str:literal) => {{
        assert!(wide_str!($str).iter().copied().eq($str.encode_utf16().chain([0])));
        assert!(utf16!($str).iter().copied().eq($str.encode_utf16()));
    }};
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=test_utf16_macros | COMPLEXITY=2 | LINES=8

```rust
#[test]
fn test_utf16_macros() {
    check_utf16!("hello world");
    check_utf16!("€4.50");
    check_utf16!("𨉟呐㗂越");
    check_utf16!("Pchnąć w tę łódź jeża lub ośm skrzyń fig");
}
```

---
*Generated by AST tracing system*
