# AST Trace: ../rust/library/alloctests/tests/btree_set_hash.rs

Generated 2 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=test_hash | COMPLEXITY=2 | LINES=19

```rust
use std::collections::BTreeSet;

use crate::hash;

#[test]
fn test_hash() {
    let mut x = BTreeSet::new();
    let mut y = BTreeSet::new();

    x.insert(1);
    x.insert(2);
    x.insert(3);

    y.insert(3);
    y.insert(2);
    y.insert(1);

    assert_eq!(hash(&x), hash(&y));
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=test_prefix_free | COMPLEXITY=2 | LINES=11

```rust
#[test]
fn test_prefix_free() {
    let x = BTreeSet::from([1, 2, 3]);
    let y = BTreeSet::<i32>::new();

    // If hashed by iteration alone, `(x, y)` and `(y, x)` would visit the same
    // order of elements, resulting in the same hash. But now that we also hash
    // the length, they get distinct sequences of hashed data.
    assert_ne!(hash(&(&x, &y)), hash(&(&y, &x)));
}
```

---
*Generated by AST tracing system*
