# AST Trace: ../rust/library/std/src/sys/stdio/sgx.rs

Generated 10 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=3

```rust
use fortanix_sgx_abi as abi;

use crate::io::{self, BorrowedCursor, IoSlice, IoSliceMut};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=Stdin; | COMPLEXITY=2 | LINES=12

```rust
use crate::sys::fd::FileDesc;

pub struct Stdin;
pub struct Stdout;
pub struct Stderr;

fn with_std_fd<F: FnOnce(&FileDesc) -> R, R>(fd: abi::Fd, f: F) -> R {
    let fd = FileDesc::new(fd);
    let ret = f(&fd);
    fd.into_raw();
    ret
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=3 | LINES=6

```rust
impl Stdin {
    pub const fn new() -> Stdin {
        Stdin
    }
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=read | COMPLEXITY=9 | LINES=19

```rust
impl io::Read for Stdin {
    fn read(&mut self, buf: &mut [u8]) -> io::Result<usize> {
        with_std_fd(abi::FD_STDIN, |fd| fd.read(buf))
    }

    fn read_buf(&mut self, buf: BorrowedCursor<'_>) -> io::Result<()> {
        with_std_fd(abi::FD_STDIN, |fd| fd.read_buf(buf))
    }

    fn read_vectored(&mut self, bufs: &mut [IoSliceMut<'_>]) -> io::Result<usize> {
        with_std_fd(abi::FD_STDIN, |fd| fd.read_vectored(bufs))
    }

    #[inline]
    fn is_read_vectored(&self) -> bool {
        true
    }
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=3 | LINES=6

```rust
impl Stdout {
    pub const fn new() -> Stdout {
        Stdout
    }
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=write | COMPLEXITY=8 | LINES=19

```rust
impl io::Write for Stdout {
    fn write(&mut self, buf: &[u8]) -> io::Result<usize> {
        with_std_fd(abi::FD_STDOUT, |fd| fd.write(buf))
    }

    fn flush(&mut self) -> io::Result<()> {
        with_std_fd(abi::FD_STDOUT, |fd| fd.flush())
    }

    fn write_vectored(&mut self, bufs: &[IoSlice<'_>]) -> io::Result<usize> {
        with_std_fd(abi::FD_STDOUT, |fd| fd.write_vectored(bufs))
    }

    #[inline]
    fn is_write_vectored(&self) -> bool {
        true
    }
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=3 | LINES=6

```rust
impl Stderr {
    pub const fn new() -> Stderr {
        Stderr
    }
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=write | COMPLEXITY=8 | LINES=19

```rust
impl io::Write for Stderr {
    fn write(&mut self, buf: &[u8]) -> io::Result<usize> {
        with_std_fd(abi::FD_STDERR, |fd| fd.write(buf))
    }

    fn flush(&mut self) -> io::Result<()> {
        with_std_fd(abi::FD_STDERR, |fd| fd.flush())
    }

    fn write_vectored(&mut self, bufs: &[IoSlice<'_>]) -> io::Result<usize> {
        with_std_fd(abi::FD_STDERR, |fd| fd.write_vectored(bufs))
    }

    #[inline]
    fn is_write_vectored(&self) -> bool {
        true
    }
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=is_ebadf | COMPLEXITY=2 | LINES=7

```rust
pub const STDIN_BUF_SIZE: usize = crate::sys::io::DEFAULT_BUF_SIZE;

pub fn is_ebadf(err: &io::Error) -> bool {
    // FIXME: Rust normally maps Unix EBADF to `Uncategorized`
    err.raw_os_error() == Some(abi::Error::BrokenPipe as _)
}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=FUNCTION | NAME=panic_output | COMPLEXITY=2 | LINES=4

```rust
pub fn panic_output() -> Option<impl io::Write> {
    crate::sys::pal::abi::panic::SgxPanicOutput::new()
}
```

---
*Generated by AST tracing system*
