# AST Trace: ../rust/compiler/rustc_type_ir/src/upcast.rs

Generated 4 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=upcast | COMPLEXITY=2 | LINES=4

```rust
/// An `Into`-like trait that takes `TyCtxt` to perform interner-specific transformations.
pub trait Upcast<I, T> {
    fn upcast(self, interner: I) -> T;
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=upcast | COMPLEXITY=5 | LINES=9

```rust
impl<I, T, U> Upcast<I, U> for T
where
    U: UpcastFrom<I, T>,
{
    fn upcast(self, interner: I) -> U {
        U::upcast_from(self, interner)
    }
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=upcast_from | COMPLEXITY=2 | LINES=5

```rust
/// A `From`-like trait that takes `TyCtxt` to perform interner-specific transformations.
pub trait UpcastFrom<I, T> {
    fn upcast_from(from: T, interner: I) -> Self;
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=upcast_from | COMPLEXITY=5 | LINES=6

```rust
impl<I, T> UpcastFrom<I, T> for T {
    fn upcast_from(from: T, _tcx: I) -> Self {
        from
    }
}
```

---
*Generated by AST tracing system*
