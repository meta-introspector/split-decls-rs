# AST Trace: ../rust/library/backtrace/src/backtrace/miri.rs

Generated 10 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=miri_backtrace_size | COMPLEXITY=6 | LINES=10

```rust
use alloc::boxed::Box;
use alloc::vec::Vec;
use core::ffi::c_void;

unsafe extern "Rust" {
    fn miri_backtrace_size(flags: u64) -> usize;
    fn miri_get_backtrace(flags: u64, buf: *mut *mut ());
    fn miri_resolve_frame(ptr: *mut (), flags: u64) -> MiriFrame;
    fn miri_resolve_frame_names(ptr: *mut (), flags: u64, name_buf: *mut u8, filename_buf: *mut u8);
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=STRUCT | NAME=MiriFrame | COMPLEXITY=2 | LINES=9

```rust
#[repr(C)]
pub struct MiriFrame {
    pub name_len: usize,
    pub filename_len: usize,
    pub lineno: u32,
    pub colno: u32,
    pub fn_ptr: *mut c_void,
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=STRUCT | NAME=FullMiriFrame | COMPLEXITY=2 | LINES=9

```rust
#[derive(Clone, Debug)]
pub struct FullMiriFrame {
    pub name: Box<[u8]>,
    pub filename: Box<[u8]>,
    pub lineno: u32,
    pub colno: u32,
    pub fn_ptr: *mut c_void,
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=STRUCT | NAME=Frame | COMPLEXITY=2 | LINES=6

```rust
#[derive(Debug, Clone)]
pub struct Frame {
    pub addr: *mut c_void,
    pub inner: FullMiriFrame,
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=IMPL | NAME=UNNAMED | COMPLEXITY=8 | LINES=4

```rust
// SAFETY: Miri guarantees that the returned pointer
// can be used from any thread.
unsafe impl Send for Frame {}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=IMPL | NAME=UNNAMED | COMPLEXITY=8 | LINES=1

```rust
unsafe impl Sync for Frame {}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=ip | COMPLEXITY=6 | LINES=18

```rust
impl Frame {
    pub fn ip(&self) -> *mut c_void {
        self.addr
    }

    pub fn sp(&self) -> *mut c_void {
        core::ptr::null_mut()
    }

    pub fn symbol_address(&self) -> *mut c_void {
        self.inner.fn_ptr
    }

    pub fn module_base_address(&self) -> Option<*mut c_void> {
        None
    }
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=11 | LINES=9

```rust
// SAFETY: This function is safe to call. It is only marked as `unsafe` to
// avoid having to allow `unused_unsafe` since other implementations are
// unsafe.
pub unsafe fn trace<F: FnMut(&super::Frame) -> bool>(cb: F) {
    // SAFETY: Miri guarantees that the backtrace API functions
    // can be called from any thread.
    unsafe { trace_unsynchronized(cb) };
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=resolve_addr | COMPLEXITY=17 | LINES=34

```rust
pub fn resolve_addr(ptr: *mut c_void) -> Frame {
    // SAFETY: Miri will stop execution with an error if this pointer
    // is invalid.
    let frame = unsafe { miri_resolve_frame(ptr.cast::<()>(), 1) };

    let mut name = Vec::with_capacity(frame.name_len);
    let mut filename = Vec::with_capacity(frame.filename_len);

    // SAFETY: name and filename have been allocated with the amount
    // of memory miri has asked for, and miri guarantees it will initialize it
    unsafe {
        miri_resolve_frame_names(
            ptr.cast::<()>(),
            0,
            name.as_mut_ptr(),
            filename.as_mut_ptr(),
        );

        name.set_len(frame.name_len);
        filename.set_len(frame.filename_len);
    }

    Frame {
        addr: ptr,
        inner: FullMiriFrame {
            name: name.into(),
            filename: filename.into(),
            lineno: frame.lineno,
            colno: frame.colno,
            fn_ptr: frame.fn_ptr,
        },
    }
}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=23 | LINES=19

```rust
unsafe fn trace_unsynchronized<F: FnMut(&super::Frame) -> bool>(mut cb: F) {
    let len = unsafe { miri_backtrace_size(0) };

    let mut frames = Vec::with_capacity(len);

    unsafe {
        miri_get_backtrace(1, frames.as_mut_ptr());

        frames.set_len(len);
    }

    for ptr in frames.iter() {
        let frame = resolve_addr((*ptr).cast::<c_void>());
        if !cb(&super::Frame { inner: frame }) {
            return;
        }
    }
}
```

---
*Generated by AST tracing system*
