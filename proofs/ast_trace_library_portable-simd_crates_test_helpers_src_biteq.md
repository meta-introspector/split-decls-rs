# AST Trace: library/portable-simd/crates/test_helpers/src/biteq.rs

Generated 14 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=biteq | COMPLEXITY=4 | LINES=21

```rust
// SRC: library/portable-simd/crates/test_helpers/src/biteq.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/biteq.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/biteq.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/biteq.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/biteq.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/biteq.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/portable-simd/crates/test_helpers/src/biteq.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/portable-simd/crates/test_helpers/src/biteq.rs
//! Compare numeric types by exact bit value.

pub trait BitEq {
    fn biteq(&self, other: &Self) -> bool;
    fn fmt(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result;
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=biteq | COMPLEXITY=7 | LINES=10

```rust
impl BitEq for bool {
    fn biteq(&self, other: &Self) -> bool {
        self == other
    }

    fn fmt(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result {
        write!(f, "{:?}", self)
    }
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=biteq | COMPLEXITY=16 | LINES=16

```rust
macro_rules! impl_integer_biteq {
    { $($type:ty),* } => {
        $(
        impl BitEq for $type {
            fn biteq(&self, other: &Self) -> bool {
                self == other
            }

            fn fmt(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result {
                write!(f, "{:?} ({:x})", self, self)
            }
        }
        )*
    };
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
impl_integer_biteq! { u8, u16, u32, u64, u128, usize, i8, i16, i32, i64, i128, isize }
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=biteq | COMPLEXITY=21 | LINES=20

```rust
macro_rules! impl_float_biteq {
    { $($type:ty),* } => {
        $(
        impl BitEq for $type {
            fn biteq(&self, other: &Self) -> bool {
                if self.is_nan() && other.is_nan() {
                    true // exact nan bits don't matter
                } else {
                    self.to_bits() == other.to_bits()
                }
            }

            fn fmt(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result {
                write!(f, "{:?} ({:x})", self, self.to_bits())
            }
        }
        )*
    };
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=BLOCK | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
impl_float_biteq! { f32, f64 }
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=biteq | COMPLEXITY=7 | LINES=10

```rust
impl<T> BitEq for *const T {
    fn biteq(&self, other: &Self) -> bool {
        self == other
    }

    fn fmt(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result {
        write!(f, "{:?}", self)
    }
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=biteq | COMPLEXITY=7 | LINES=10

```rust
impl<T> BitEq for *mut T {
    fn biteq(&self, other: &Self) -> bool {
        self == other
    }

    fn fmt(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result {
        write!(f, "{:?}", self)
    }
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=biteq | COMPLEXITY=11 | LINES=23

```rust
impl<T: BitEq, const N: usize> BitEq for [T; N] {
    fn biteq(&self, other: &Self) -> bool {
        self.iter()
            .zip(other.iter())
            .fold(true, |value, (left, right)| value && left.biteq(right))
    }

    fn fmt(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result {
        #[repr(transparent)]
        struct Wrapper<'a, T: BitEq>(&'a T);

        impl<T: BitEq> core::fmt::Debug for Wrapper<'_, T> {
            fn fmt(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result {
                self.0.fmt(f)
            }
        }

        f.debug_list()
            .entries(self.iter().map(|x| Wrapper(x)))
            .finish()
    }
}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=FUNCTION | NAME=BitEqWrapper | COMPLEXITY=5 | LINES=9

```rust
#[doc(hidden)]
pub struct BitEqWrapper<'a, T>(pub &'a T);

impl<T: BitEq> PartialEq for BitEqWrapper<'_, T> {
    fn eq(&self, other: &Self) -> bool {
        self.0.biteq(other.0)
    }
}
```

## Block 11
**Metadata**: AST_ID=11 | TYPE=FUNCTION | NAME=fmt | COMPLEXITY=5 | LINES=6

```rust
impl<T: BitEq> core::fmt::Debug for BitEqWrapper<'_, T> {
    fn fmt(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result {
        self.0.fmt(f)
    }
}
```

## Block 12
**Metadata**: AST_ID=12 | TYPE=FUNCTION | NAME=BitEqEitherWrapper | COMPLEXITY=5 | LINES=9

```rust
#[doc(hidden)]
pub struct BitEqEitherWrapper<'a, T>(pub &'a T, pub &'a T);

impl<T: BitEq> PartialEq<BitEqEitherWrapper<'_, T>> for BitEqWrapper<'_, T> {
    fn eq(&self, other: &BitEqEitherWrapper<'_, T>) -> bool {
        self.0.biteq(other.0) || self.0.biteq(other.1)
    }
}
```

## Block 13
**Metadata**: AST_ID=13 | TYPE=FUNCTION | NAME=fmt | COMPLEXITY=9 | LINES=12

```rust
impl<T: BitEq> core::fmt::Debug for BitEqEitherWrapper<'_, T> {
    fn fmt(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result {
        if self.0.biteq(self.1) {
            self.0.fmt(f)
        } else {
            self.0.fmt(f)?;
            write!(f, " or ")?;
            self.1.fmt(f)
        }
    }
}
```

## Block 14
**Metadata**: AST_ID=14 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=15 | LINES=21

```rust
#[macro_export]
macro_rules! prop_assert_biteq {
    { $a:expr, $b:expr $(,)? } => {
        {
            use $crate::biteq::BitEqWrapper;
            let a = $a;
            let b = $b;
            proptest::prop_assert_eq!(BitEqWrapper(&a), BitEqWrapper(&b));
        }
    };
    { $a:expr, $b:expr, $c:expr $(,)? } => {
        {
            use $crate::biteq::{BitEqWrapper, BitEqEitherWrapper};
            let a = $a;
            let b = $b;
            let c = $c;
            proptest::prop_assert_eq!(BitEqWrapper(&a), BitEqEitherWrapper(&b, &c));
        }
    };
}
```

---
*Generated by AST tracing system*
