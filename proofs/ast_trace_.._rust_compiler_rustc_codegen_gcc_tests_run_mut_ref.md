# AST Trace: ../rust/compiler/rustc_codegen_gcc/tests/run/mut_ref.rs

Generated 4 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=STRUCT | NAME=Test | COMPLEXITY=2 | LINES=19

```rust
// Compiler:
//
// Run-time:
//   stdout: 2
//     7
//     6
//     11

#![feature(no_core)]
#![no_std]
#![no_core]
#![no_main]

extern crate mini_core;
use mini_core::*;

struct Test {
    field: isize,
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=test | COMPLEXITY=3 | LINES=4

```rust
fn test(num: isize) -> Test {
    Test { field: num + 1 }
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=update_num | COMPLEXITY=2 | LINES=4

```rust
fn update_num(num: &mut isize) {
    *num = *num + 5;
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=23 | LINES=25

```rust
#[no_mangle]
extern "C" fn main(mut argc: isize, _argv: *const *const u8) -> i32 {
    let mut test = test(argc);
    unsafe {
        libc::printf(b"%ld\n\0" as *const u8 as *const i8, test.field);
    }
    update_num(&mut test.field);
    unsafe {
        libc::printf(b"%ld\n\0" as *const u8 as *const i8, test.field);
    }

    update_num(&mut argc);
    unsafe {
        libc::printf(b"%ld\n\0" as *const u8 as *const i8, argc);
    }

    let refe = &mut argc;
    *refe = *refe + 5;
    unsafe {
        libc::printf(b"%ld\n\0" as *const u8 as *const i8, argc);
    }

    0
}
```

---
*Generated by AST tracing system*
