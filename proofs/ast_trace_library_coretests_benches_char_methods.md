# AST Trace: library/coretests/benches/char/methods.rs

Generated 14 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=3 | LINES=18

```rust
// SRC: library/coretests/benches/char/methods.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/char/methods.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/char/methods.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/char/methods.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/char/methods.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/char/methods.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/char/methods.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/coretests/benches/char/methods.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/coretests/benches/char/methods.rs
use test::{Bencher, black_box};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=bench_to_digit_radix_2 | COMPLEXITY=2 | LINES=8

```rust
const CHARS: [char; 9] = ['0', 'x', '2', '5', 'A', 'f', '7', '8', '9'];
const RADIX: [u32; 5] = [2, 8, 10, 16, 32];

#[bench]
fn bench_to_digit_radix_2(b: &mut Bencher) {
    b.iter(|| CHARS.iter().cycle().take(10_000).map(|c| black_box(c).to_digit(2)).min())
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=bench_to_digit_radix_10 | COMPLEXITY=2 | LINES=5

```rust
#[bench]
fn bench_to_digit_radix_10(b: &mut Bencher) {
    b.iter(|| CHARS.iter().cycle().take(10_000).map(|c| black_box(c).to_digit(10)).min())
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=bench_to_digit_radix_16 | COMPLEXITY=2 | LINES=5

```rust
#[bench]
fn bench_to_digit_radix_16(b: &mut Bencher) {
    b.iter(|| CHARS.iter().cycle().take(10_000).map(|c| black_box(c).to_digit(16)).min())
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=bench_to_digit_radix_36 | COMPLEXITY=2 | LINES=5

```rust
#[bench]
fn bench_to_digit_radix_36(b: &mut Bencher) {
    b.iter(|| CHARS.iter().cycle().take(10_000).map(|c| black_box(c).to_digit(36)).min())
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=bench_to_digit_radix_var | COMPLEXITY=3 | LINES=13

```rust
#[bench]
fn bench_to_digit_radix_var(b: &mut Bencher) {
    b.iter(|| {
        CHARS
            .iter()
            .cycle()
            .zip(RADIX.iter().cycle())
            .take(10_000)
            .map(|(c, radix)| black_box(c).to_digit(*radix))
            .min()
    })
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=bench_to_ascii_uppercase | COMPLEXITY=2 | LINES=5

```rust
#[bench]
fn bench_to_ascii_uppercase(b: &mut Bencher) {
    b.iter(|| CHARS.iter().cycle().take(10_000).map(|c| black_box(c).to_ascii_uppercase()).min())
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=bench_to_ascii_lowercase | COMPLEXITY=2 | LINES=5

```rust
#[bench]
fn bench_to_ascii_lowercase(b: &mut Bencher) {
    b.iter(|| CHARS.iter().cycle().take(10_000).map(|c| black_box(c).to_ascii_lowercase()).min())
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=bench_ascii_mix_to_uppercase | COMPLEXITY=3 | LINES=7

```rust
#[bench]
fn bench_ascii_mix_to_uppercase(b: &mut Bencher) {
    b.iter(|| {
        (0..=255).cycle().take(10_000).map(|b| black_box(char::from(b)).to_uppercase()).count()
    })
}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=FUNCTION | NAME=bench_ascii_mix_to_lowercase | COMPLEXITY=3 | LINES=7

```rust
#[bench]
fn bench_ascii_mix_to_lowercase(b: &mut Bencher) {
    b.iter(|| {
        (0..=255).cycle().take(10_000).map(|b| black_box(char::from(b)).to_lowercase()).count()
    })
}
```

## Block 11
**Metadata**: AST_ID=11 | TYPE=FUNCTION | NAME=bench_ascii_char_to_uppercase | COMPLEXITY=3 | LINES=7

```rust
#[bench]
fn bench_ascii_char_to_uppercase(b: &mut Bencher) {
    b.iter(|| {
        (0..=127).cycle().take(10_000).map(|b| black_box(char::from(b)).to_uppercase()).count()
    })
}
```

## Block 12
**Metadata**: AST_ID=12 | TYPE=FUNCTION | NAME=bench_ascii_char_to_lowercase | COMPLEXITY=3 | LINES=7

```rust
#[bench]
fn bench_ascii_char_to_lowercase(b: &mut Bencher) {
    b.iter(|| {
        (0..=127).cycle().take(10_000).map(|b| black_box(char::from(b)).to_lowercase()).count()
    })
}
```

## Block 13
**Metadata**: AST_ID=13 | TYPE=FUNCTION | NAME=bench_non_ascii_char_to_uppercase | COMPLEXITY=3 | LINES=7

```rust
#[bench]
fn bench_non_ascii_char_to_uppercase(b: &mut Bencher) {
    b.iter(|| {
        (128..=255).cycle().take(10_000).map(|b| black_box(char::from(b)).to_uppercase()).count()
    })
}
```

## Block 14
**Metadata**: AST_ID=14 | TYPE=FUNCTION | NAME=bench_non_ascii_char_to_lowercase | COMPLEXITY=3 | LINES=7

```rust
#[bench]
fn bench_non_ascii_char_to_lowercase(b: &mut Bencher) {
    b.iter(|| {
        (128..=255).cycle().take(10_000).map(|b| black_box(char::from(b)).to_lowercase()).count()
    })
}
```

---
*Generated by AST tracing system*
