# AST Trace: ../rust/library/std/src/sys/pal/solid/mod.rs

Generated 8 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=MODULE | NAME=UNNAMED | COMPLEXITY=2 | LINES=15

```rust
#![allow(dead_code)]
#![allow(missing_docs, nonstandard_style)]
#![forbid(unsafe_op_in_unsafe_fn)]

pub mod abi;

#[path = "../itron"]
pub mod itron {
    pub mod abi;
    pub mod error;
    pub mod spin;
    pub mod task;
    pub mod thread_parking;
    pub mod time;
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=8 | LINES=13

```rust
// `error` is `pub(crate)` so that it can be accessed by `itron/error.rs` as
// `crate::sys::error`
pub(crate) mod error;
pub mod os;
#[path = "../unsupported/pipe.rs"]
pub mod pipe;
pub use self::itron::thread_parking;
pub mod time;

// SAFETY: must be called only once during runtime initialization.
// NOTE: this is not guaranteed to run, for example when Rust code is called externally.
pub unsafe fn init(_argc: isize, _argv: *const *const u8, _sigpipe: u8) {}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=6 | LINES=3

```rust
// SAFETY: must be called only once during runtime cleanup.
pub unsafe fn cleanup() {}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=unsupported | COMPLEXITY=2 | LINES=4

```rust
pub fn unsupported<T>() -> crate::io::Result<T> {
    Err(unsupported_err())
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=unsupported_err | COMPLEXITY=2 | LINES=4

```rust
pub fn unsupported_err() -> crate::io::Error {
    crate::io::Error::UNSUPPORTED_PLATFORM
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=is_interrupted | COMPLEXITY=2 | LINES=5

```rust
#[inline]
pub fn is_interrupted(code: i32) -> bool {
    crate::sys::net::is_interrupted(code)
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=decode_error_kind | COMPLEXITY=2 | LINES=4

```rust
pub fn decode_error_kind(code: i32) -> crate::io::ErrorKind {
    error::decode_error_kind(code)
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=abort_internal | COMPLEXITY=7 | LINES=5

```rust
#[inline]
pub fn abort_internal() -> ! {
    unsafe { libc::abort() }
}
```

---
*Generated by AST tracing system*
