# AST Trace: library/std/src/sys/io/io_slice/uefi.rs

Generated 4 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=STRUCT | NAME=IoSlice | COMPLEXITY=6 | LINES=30

```rust
// SRC: library/std/src/sys/io/io_slice/uefi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/io/io_slice/uefi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/io/io_slice/uefi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/io/io_slice/uefi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/io/io_slice/uefi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/io/io_slice/uefi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/io/io_slice/uefi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/src/sys/io/io_slice/uefi.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/std/src/sys/io/io_slice/uefi.rs
//! A buffer type used with `Write::write_vectored` for UEFI Networking APIs. Vectored writing to
//! File is not supported as of UEFI Spec 2.11.

use crate::marker::PhantomData;
use crate::slice;

#[derive(Copy, Clone)]
#[repr(C)]
pub struct IoSlice<'a> {
    len: u32,
    data: *const u8,
    _p: PhantomData<&'a [u8]>,
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=new | COMPLEXITY=17 | LINES=22

```rust
impl<'a> IoSlice<'a> {
    #[inline]
    pub fn new(buf: &'a [u8]) -> IoSlice<'a> {
        let len = buf.len().try_into().unwrap();
        Self { len, data: buf.as_ptr(), _p: PhantomData }
    }

    #[inline]
    pub fn advance(&mut self, n: usize) {
        self.len = u32::try_from(n)
            .ok()
            .and_then(|n| self.len.checked_sub(n))
            .expect("advancing IoSlice beyond its length");
        unsafe { self.data = self.data.add(n) };
    }

    #[inline]
    pub const fn as_slice(&self) -> &'a [u8] {
        unsafe { slice::from_raw_parts(self.data, self.len as usize) }
    }
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=STRUCT | NAME=IoSliceMut | COMPLEXITY=2 | LINES=7

```rust
#[repr(C)]
pub struct IoSliceMut<'a> {
    len: u32,
    data: *mut u8,
    _p: PhantomData<&'a mut [u8]>,
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=new | COMPLEXITY=29 | LINES=32

```rust
impl<'a> IoSliceMut<'a> {
    #[inline]
    pub fn new(buf: &'a mut [u8]) -> IoSliceMut<'a> {
        let len = buf.len().try_into().unwrap();
        Self { len, data: buf.as_mut_ptr(), _p: PhantomData }
    }

    #[inline]
    pub fn advance(&mut self, n: usize) {
        self.len = u32::try_from(n)
            .ok()
            .and_then(|n| self.len.checked_sub(n))
            .expect("advancing IoSlice beyond its length");
        unsafe { self.data = self.data.add(n) };
    }

    #[inline]
    pub fn as_slice(&self) -> &[u8] {
        unsafe { slice::from_raw_parts(self.data, self.len as usize) }
    }

    #[inline]
    pub const fn into_slice(self) -> &'a mut [u8] {
        unsafe { slice::from_raw_parts_mut(self.data, self.len as usize) }
    }

    #[inline]
    pub fn as_mut_slice(&mut self) -> &mut [u8] {
        unsafe { slice::from_raw_parts_mut(self.data, self.len as usize) }
    }
}
```

---
*Generated by AST tracing system*
