# AST Trace: ../rust/library/std/src/sys/stdio/zkvm.rs

Generated 10 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
use crate::io::{self, BorrowedCursor};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
use crate::sys::pal::abi::{self, fileno};
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=Stdin; | COMPLEXITY=3 | LINES=10

```rust
pub struct Stdin;
pub struct Stdout;
pub struct Stderr;

impl Stdin {
    pub const fn new() -> Stdin {
        Stdin
    }
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=read | COMPLEXITY=16 | LINES=14

```rust
impl io::Read for Stdin {
    fn read(&mut self, buf: &mut [u8]) -> io::Result<usize> {
        Ok(unsafe { abi::sys_read(fileno::STDIN, buf.as_mut_ptr(), buf.len()) })
    }

    fn read_buf(&mut self, mut buf: BorrowedCursor<'_>) -> io::Result<()> {
        unsafe {
            let n = abi::sys_read(fileno::STDIN, buf.as_mut().as_mut_ptr().cast(), buf.capacity());
            buf.advance_unchecked(n);
        }
        Ok(())
    }
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=3 | LINES=6

```rust
impl Stdout {
    pub const fn new() -> Stdout {
        Stdout
    }
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=write | COMPLEXITY=11 | LINES=12

```rust
impl io::Write for Stdout {
    fn write(&mut self, buf: &[u8]) -> io::Result<usize> {
        unsafe { abi::sys_write(fileno::STDOUT, buf.as_ptr(), buf.len()) }

        Ok(buf.len())
    }

    fn flush(&mut self) -> io::Result<()> {
        Ok(())
    }
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=3 | LINES=6

```rust
impl Stderr {
    pub const fn new() -> Stderr {
        Stderr
    }
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=write | COMPLEXITY=11 | LINES=12

```rust
impl io::Write for Stderr {
    fn write(&mut self, buf: &[u8]) -> io::Result<usize> {
        unsafe { abi::sys_write(fileno::STDERR, buf.as_ptr(), buf.len()) }

        Ok(buf.len())
    }

    fn flush(&mut self) -> io::Result<()> {
        Ok(())
    }
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=is_ebadf | COMPLEXITY=2 | LINES=6

```rust
pub const STDIN_BUF_SIZE: usize = crate::sys::io::DEFAULT_BUF_SIZE;

pub fn is_ebadf(_err: &io::Error) -> bool {
    true
}
```

## Block 10
**Metadata**: AST_ID=10 | TYPE=FUNCTION | NAME=panic_output | COMPLEXITY=2 | LINES=4

```rust
pub fn panic_output() -> Option<impl io::Write> {
    Some(Stderr::new())
}
```

---
*Generated by AST tracing system*
