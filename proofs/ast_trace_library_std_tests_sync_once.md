# AST Trace: library/std/tests/sync/once.rs

Generated 8 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=3 | LINES=21

```rust
// SRC: library/std/tests/sync/once.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/once.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/once.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/once.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/once.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/once.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/std/tests/sync/once.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/std/tests/sync/once.rs
use std::sync::Once;
use std::sync::atomic::AtomicBool;
use std::sync::atomic::Ordering::Relaxed;
use std::sync::mpsc::channel;
use std::time::Duration;
use std::{panic, thread};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=smoke_once | COMPLEXITY=2 | LINES=10

```rust
#[test]
fn smoke_once() {
    static O: Once = Once::new();
    let mut a = 0;
    O.call_once(|| a += 1);
    assert_eq!(a, 1);
    O.call_once(|| a += 1);
    assert_eq!(a, 1);
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=stampede_once | COMPLEXITY=25 | LINES=36

```rust
#[test]
fn stampede_once() {
    static O: Once = Once::new();
    static mut RUN: bool = false;

    let (tx, rx) = channel();
    for _ in 0..10 {
        let tx = tx.clone();
        thread::spawn(move || {
            for _ in 0..4 {
                thread::yield_now()
            }
            unsafe {
                O.call_once(|| {
                    assert!(!RUN);
                    RUN = true;
                });
                assert!(RUN);
            }
            tx.send(()).unwrap();
        });
    }

    unsafe {
        O.call_once(|| {
            assert!(!RUN);
            RUN = true;
        });
        assert!(RUN);
    }

    for _ in 0..10 {
        rx.recv().unwrap();
    }
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=poison_bad | COMPLEXITY=8 | LINES=29

```rust
#[test]
#[cfg_attr(not(panic = "unwind"), ignore = "test requires unwinding support")]
fn poison_bad() {
    static O: Once = Once::new();

    // poison the once
    let t = panic::catch_unwind(|| {
        O.call_once(|| panic!());
    });
    assert!(t.is_err());

    // poisoning propagates
    let t = panic::catch_unwind(|| {
        O.call_once(|| {});
    });
    assert!(t.is_err());

    // we can subvert poisoning, however
    let mut called = false;
    O.call_once_force(|p| {
        called = true;
        assert!(p.is_poisoned())
    });
    assert!(called);

    // once any success happens, we stop propagating the poison
    O.call_once(|| {});
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=wait_for_force_to_finish | COMPLEXITY=8 | LINES=39

```rust
#[test]
#[cfg_attr(not(panic = "unwind"), ignore = "test requires unwinding support")]
fn wait_for_force_to_finish() {
    static O: Once = Once::new();

    // poison the once
    let t = panic::catch_unwind(|| {
        O.call_once(|| panic!());
    });
    assert!(t.is_err());

    // make sure someone's waiting inside the once via a force
    let (tx1, rx1) = channel();
    let (tx2, rx2) = channel();
    let t1 = thread::spawn(move || {
        O.call_once_force(|p| {
            assert!(p.is_poisoned());
            tx1.send(()).unwrap();
            rx2.recv().unwrap();
        });
    });

    rx1.recv().unwrap();

    // put another waiter on the once
    let t2 = thread::spawn(|| {
        let mut called = false;
        O.call_once(|| {
            called = true;
        });
        assert!(!called);
    });

    tx2.send(()).unwrap();

    assert!(t1.join().is_ok());
    assert!(t2.join().is_ok());
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=wait | COMPLEXITY=10 | LINES=19

```rust
#[test]
fn wait() {
    for _ in 0..50 {
        let val = AtomicBool::new(false);
        let once = Once::new();

        thread::scope(|s| {
            for _ in 0..4 {
                s.spawn(|| {
                    once.wait();
                    assert!(val.load(Relaxed));
                });
            }

            once.call_once(|| val.store(true, Relaxed));
        });
    }
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=wait_on_poisoned | COMPLEXITY=2 | LINES=9

```rust
#[test]
#[cfg_attr(not(panic = "unwind"), ignore = "test requires unwinding support")]
fn wait_on_poisoned() {
    let once = Once::new();

    panic::catch_unwind(|| once.call_once(|| panic!())).unwrap_err();
    panic::catch_unwind(|| once.wait()).unwrap_err();
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=wait_force_on_poisoned | COMPLEXITY=5 | LINES=18

```rust
#[test]
#[cfg_attr(not(panic = "unwind"), ignore = "test requires unwinding support")]
fn wait_force_on_poisoned() {
    let once = Once::new();

    thread::scope(|s| {
        panic::catch_unwind(|| once.call_once(|| panic!())).unwrap_err();

        s.spawn(|| {
            thread::sleep(Duration::from_millis(100));

            once.call_once_force(|_| {});
        });

        once.wait_force();
    })
}
```

---
*Generated by AST tracing system*
