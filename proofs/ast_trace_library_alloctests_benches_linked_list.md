# AST Trace: library/alloctests/benches/linked_list.rs

Generated 9 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=FUNCTION | NAME=bench_collect_into | COMPLEXITY=4 | LINES=26

```rust
// SRC: library/alloctests/benches/linked_list.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/benches/linked_list.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/benches/linked_list.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/benches/linked_list.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/benches/linked_list.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/benches/linked_list.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/alloctests/benches/linked_list.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/alloctests/benches/linked_list.rs
use std::collections::LinkedList;

use test::Bencher;

#[bench]
fn bench_collect_into(b: &mut Bencher) {
    let v = &[0; 64];
    b.iter(|| {
        let _: LinkedList<_> = v.iter().cloned().collect();
    })
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=FUNCTION | NAME=bench_push_front | COMPLEXITY=3 | LINES=8

```rust
#[bench]
fn bench_push_front(b: &mut Bencher) {
    let mut m: LinkedList<_> = LinkedList::new();
    b.iter(|| {
        m.push_front(0);
    })
}
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=FUNCTION | NAME=bench_push_back | COMPLEXITY=3 | LINES=8

```rust
#[bench]
fn bench_push_back(b: &mut Bencher) {
    let mut m: LinkedList<_> = LinkedList::new();
    b.iter(|| {
        m.push_back(0);
    })
}
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=bench_push_back_pop_back | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_push_back_pop_back(b: &mut Bencher) {
    let mut m: LinkedList<_> = LinkedList::new();
    b.iter(|| {
        m.push_back(0);
        m.pop_back();
    })
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=bench_push_front_pop_front | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_push_front_pop_front(b: &mut Bencher) {
    let mut m: LinkedList<_> = LinkedList::new();
    b.iter(|| {
        m.push_front(0);
        m.pop_front();
    })
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=bench_iter | COMPLEXITY=3 | LINES=9

```rust
#[bench]
fn bench_iter(b: &mut Bencher) {
    let v = &[0; 128];
    let m: LinkedList<_> = v.iter().cloned().collect();
    b.iter(|| {
        assert!(m.iter().count() == 128);
    })
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=FUNCTION | NAME=bench_iter_mut | COMPLEXITY=3 | LINES=8

```rust
#[bench]
fn bench_iter_mut(b: &mut Bencher) {
    let v = &[0; 128];
    let mut m: LinkedList<_> = v.iter().cloned().collect();
    b.iter(|| {
        assert!(m.iter_mut().count() == 128);
    })
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=bench_iter_rev | COMPLEXITY=3 | LINES=8

```rust
#[bench]
fn bench_iter_rev(b: &mut Bencher) {
    let v = &[0; 128];
    let m: LinkedList<_> = v.iter().cloned().collect();
    b.iter(|| {
        assert!(m.iter().rev().count() == 128);
    })
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=bench_iter_mut_rev | COMPLEXITY=3 | LINES=8

```rust
#[bench]
fn bench_iter_mut_rev(b: &mut Bencher) {
    let v = &[0; 128];
    let mut m: LinkedList<_> = v.iter().cloned().collect();
    b.iter(|| {
        assert!(m.iter_mut().rev().count() == 128);
    })
}
```

---
*Generated by AST tracing system*
