# AST Trace: library/test/src/helpers/shuffle.rs

Generated 9 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=3 | LINES=16

```rust
// SRC: library/test/src/helpers/shuffle.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/test/src/helpers/shuffle.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/test/src/helpers/shuffle.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/test/src/helpers/shuffle.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/test/src/helpers/shuffle.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/test/src/helpers/shuffle.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: library/test/src/helpers/shuffle.rs
// GENERATED BY: build.rs (split-decls-genesis)
// SRC: ../rust/library/test/src/helpers/shuffle.rs
use std::hash::{DefaultHasher, Hasher};
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=1

```rust
use std::time::{SystemTime, UNIX_EPOCH};
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=3

```rust
use crate::cli::TestOpts;
use crate::types::{TestDescAndFn, TestId, TestName};
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=7 | LINES=15

```rust
pub(crate) fn get_shuffle_seed(opts: &TestOpts) -> Option<u64> {
    opts.shuffle_seed.or_else(|| {
        if opts.shuffle {
            Some(
                SystemTime::now()
                    .duration_since(UNIX_EPOCH)
                    .expect("Failed to get system time")
                    .as_nanos() as u64,
            )
        } else {
            None
        }
    })
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=UNNAMED | COMPLEXITY=2 | LINES=7

```rust
pub(crate) fn shuffle_tests(shuffle_seed: u64, tests: &mut [(TestId, TestDescAndFn)]) {
    let test_names: Vec<&TestName> = tests.iter().map(|test| &test.1.desc.name).collect();
    let test_names_hash = calculate_hash(&test_names);
    let mut rng = Rng::new(shuffle_seed, test_names_hash);
    shuffle(&mut rng, tests);
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=shuffle | COMPLEXITY=6 | LINES=13

```rust
// `shuffle` is from `rust-analyzer/src/cli/analysis_stats.rs`.
fn shuffle<T>(rng: &mut Rng, slice: &mut [T]) {
    for i in 0..slice.len() {
        randomize_first(rng, &mut slice[i..]);
    }

    fn randomize_first<T>(rng: &mut Rng, slice: &mut [T]) {
        assert!(!slice.is_empty());
        let idx = rng.rand_range(0..slice.len() as u64) as usize;
        slice.swap(0, idx);
    }
}
```

## Block 7
**Metadata**: AST_ID=7 | TYPE=STRUCT | NAME=Rng | COMPLEXITY=2 | LINES=5

```rust
struct Rng {
    state: u64,
    extra: u64,
}
```

## Block 8
**Metadata**: AST_ID=8 | TYPE=FUNCTION | NAME=new | COMPLEXITY=6 | LINES=15

```rust
impl Rng {
    fn new(seed: u64, extra: u64) -> Self {
        Self { state: seed, extra }
    }

    fn rand_range(&mut self, range: core::ops::Range<u64>) -> u64 {
        self.rand_u64() % (range.end - range.start) + range.start
    }

    fn rand_u64(&mut self) -> u64 {
        self.state = calculate_hash(&(self.state, self.extra));
        self.state
    }
}
```

## Block 9
**Metadata**: AST_ID=9 | TYPE=FUNCTION | NAME=calculate_hash | COMPLEXITY=2 | LINES=7

```rust
// `calculate_hash` is from `core/src/hash/mod.rs`.
fn calculate_hash<T: core::hash::Hash>(t: &T) -> u64 {
    let mut s = DefaultHasher::new();
    t.hash(&mut s);
    s.finish()
}
```

---
*Generated by AST tracing system*
