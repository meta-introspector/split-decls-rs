# AST Trace: ../rust/library/coretests/benches/num/flt2dec/mod.rs

Generated 6 AST blocks from source file

## Block 1
**Metadata**: AST_ID=1 | TYPE=MODULE | NAME=UNNAMED | COMPLEXITY=2 | LINES=4

```rust
mod strategy {
    mod dragon;
    mod grisu;
}
```

## Block 2
**Metadata**: AST_ID=2 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=2

```rust
use core::num::flt2dec::{DecodableFloat, Decoded, FullDecoded, MAX_SIG_DIGITS, decode};
```

## Block 3
**Metadata**: AST_ID=3 | TYPE=USE | NAME=UNNAMED | COMPLEXITY=2 | LINES=3

```rust
use std::io::Write;

use test::{Bencher, black_box};
```

## Block 4
**Metadata**: AST_ID=4 | TYPE=FUNCTION | NAME=decode_finite | COMPLEXITY=7 | LINES=7

```rust
pub fn decode_finite<T: DecodableFloat>(v: T) -> Decoded {
    match decode(v).1 {
        FullDecoded::Finite(decoded) => decoded,
        full_decoded => panic!("expected finite, got {full_decoded:?} instead"),
    }
}
```

## Block 5
**Metadata**: AST_ID=5 | TYPE=FUNCTION | NAME=bench_small_shortest | COMPLEXITY=4 | LINES=10

```rust
#[bench]
fn bench_small_shortest(b: &mut Bencher) {
    let mut buf = Vec::with_capacity(20);

    b.iter(|| {
        buf.clear();
        write!(black_box(&mut buf), "{}", black_box(3.1415926f64)).unwrap()
    });
}
```

## Block 6
**Metadata**: AST_ID=6 | TYPE=FUNCTION | NAME=bench_big_shortest | COMPLEXITY=4 | LINES=10

```rust
#[bench]
fn bench_big_shortest(b: &mut Bencher) {
    let mut buf = Vec::with_capacity(300);

    b.iter(|| {
        buf.clear();
        write!(black_box(&mut buf), "{}", black_box(f64::MAX)).unwrap()
    });
}
```

---
*Generated by AST tracing system*
