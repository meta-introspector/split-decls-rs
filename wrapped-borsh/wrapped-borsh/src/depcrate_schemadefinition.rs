// Generated macro for Definition (enum)
macro_rules! Depcrate_schemaDefinition {
() => {
// Module: crate::schema
// Provides: {"Definition"}
// Dependencies: {}
# [doc = " The type that we use to represent the definition of the Borsh type."] # [doc = ""] # [doc = " Description of data encoding on the wire."] # [derive (Clone , PartialEq , Eq , Debug , BorshSerialize , BorshDeserialize , BorshSchemaMacro)] pub enum Definition { # [doc = " A fixed-size type, which is considered undivisible"] Primitive (u8) , # [doc = " A sequence of homogeneous elements."] # [doc = ""] # [doc = " If `length_width` is non-zero, the sequence is tagged, i.e. prefixed by"] # [doc = " the number of elements in the sequence.  In that case, the length is"] # [doc = " encoded as a `length_width`-byte wide little-endian unsigned integer."] # [doc = ""] # [doc = " If `length_width` is zero, the sequence is untagged.  In that case, if"] # [doc = " `length_range` contains a single number, the sequence is fixed-sized"] # [doc = " with the range determining number of elements.  Otherwise, knowledge of"] # [doc = " the type is necessary to be able to decode the number of elements."] # [doc = ""] # [doc = " Prototypical examples of the use of this definitions are:"] # [doc = " * `[T; N]` → `length_width: 0, length_range: N..=N, elements: \"T\"` and"] # [doc = " * `Vec<T>` → `length_width: 4, length_range: 0..=u32::MAX,"] # [doc = "   elements: \"T\"`."] # [doc = ""] # [doc = " With `length_width` and `length_range` other custom encoding formats can"] # [doc = " also be expressed.  For example:"] # [doc = " * `BoundedVec<LO, HI, T>` → `length_width: 4, length_range: LO..=HI`;"] # [doc = " * `PascalString` → `length_width: 1, length_range: 0..=255`;"] # [doc = " * `Ipv4Packet` → `length_width: 0, length_range: 20..=65536` or"] # [doc = " * `VarInt<u32>` → `length_width: 0, length_range: 1..=5`."] Sequence { # [doc = " How many bytes does the length tag occupy."] # [doc = ""] # [doc = " Zero if this is fixed-length array or the length must be determined"] # [doc = " by means not specified in the schema.  The schema is invalid if the"] # [doc = " value is greater than eight."] length_width : u8 , # [doc = " Bounds on the possible lengths of the sequence."] # [doc = ""] # [doc = " Note: The schema is invalid if the range is empty or `length_width`"] # [doc = " is non-zero and either bound of the range cannot be represented as"] # [doc = " `length_width`-byte-wide unsigned integer."] length_range : core :: ops :: RangeInclusive < u64 > , # [doc = " Type of each element of the sequence."] elements : Declaration , } , # [doc = " A fixed-size tuple with the length known at the compile time and the elements of different"] # [doc = " types."] Tuple { elements : Vec < Declaration > } , # [doc = " A possibly tagged union, a.k.a enum."] # [doc = ""] # [doc = " Tagged unions are prefixed by a tag identifying encoded variant followed"] # [doc = " by encoding of that variant.  The tag is `tag_width`-byte wide"] # [doc = " little-endian number."] # [doc = ""] # [doc = " Untagged unions don’t have a separate tag which means that knowledge of"] # [doc = " the type is necessary to fully analyse the binary.  Variants may still"] # [doc = " be used to list possible values or determine the longest possible"] # [doc = " encoding."] Enum { # [doc = " Width in bytes of the discriminant tag."] # [doc = ""] # [doc = " Zero indicates this is an untagged union.  In standard borsh"] # [doc = " encoding this is one.  Custom encoding formats may use larger width"] # [doc = " if they need to encode more than 256 variants.  The schema is"] # [doc = " invalid if the value is greater than eight."] tag_width : u8 , # [doc = " Possible variants of the enumeration."] # [doc = " `VariantName` is metadata, not present in a type's serialized representation."] variants : Vec < (DiscriminantValue , VariantName , Declaration) > , } , # [doc = " A structure, structurally similar to a tuple."] Struct { fields : Fields } , }
};
}
