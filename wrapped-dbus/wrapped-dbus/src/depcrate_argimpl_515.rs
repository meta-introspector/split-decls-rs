// Generated macro for impl_515 (impl)
macro_rules! Depcrate_argimpl_515 {
() => {
// Module: crate::arg
// Provides: {"impl_515"}
// Dependencies: {}
impl < 'a > IterAppend < 'a > { # [doc = " Creates a new IterAppend struct."] pub fn new (m : & 'a mut Message) -> IterAppend < 'a > { let mut i = ffi_iter () ; unsafe { ffi :: dbus_message_iter_init_append (m . ptr () , & mut i) } ; IterAppend (i , m) } # [doc = " Appends the argument."] pub fn append < T : Append > (& mut self , a : T) { a . append (self) } fn append_container < F : FnOnce (& mut IterAppend < 'a >) > (& mut self , arg_type : ArgType , sig : Option < & CStr > , f : F) { let mut s = IterAppend (ffi_iter () , self . 1) ; let p = sig . map (| s | s . as_ptr ()) . unwrap_or (ptr :: null ()) ; check ("dbus_message_iter_open_container" , unsafe { ffi :: dbus_message_iter_open_container (& mut self . 0 , arg_type as c_int , p , & mut s . 0) }) ; f (& mut s) ; check ("dbus_message_iter_close_container" , unsafe { ffi :: dbus_message_iter_close_container (& mut self . 0 , & mut s . 0) }) ; } # [doc = " Low-level function to append a variant."] # [doc = ""] # [doc = " Use in case the `Variant` struct is not flexible enough -"] # [doc = " the easier way is to just call e g \"append1\" on a message and supply a `Variant` parameter."] # [doc = ""] # [doc = " In order not to get D-Bus errors: during the call to \"f\" you need to call \"append\" on"] # [doc = " the supplied `IterAppend` exactly once,"] # [doc = " and with a value which has the same signature as inner_sig."] pub fn append_variant < F : FnOnce (& mut IterAppend < 'a >) > (& mut self , inner_sig : & Signature , f : F) { self . append_container (ArgType :: Variant , Some (inner_sig . as_cstr ()) , f) } # [doc = " Low-level function to append an array."] # [doc = ""] # [doc = " Use in case the `Array` struct is not flexible enough -"] # [doc = " the easier way is to just call e g \"append1\" on a message and supply an `Array` parameter."] # [doc = ""] # [doc = " In order not to get D-Bus errors: during the call to \"f\", you should only call \"append\" on"] # [doc = " the supplied `IterAppend` with values which has the same signature as inner_sig."] pub fn append_array < F : FnOnce (& mut IterAppend < 'a >) > (& mut self , inner_sig : & Signature , f : F) { self . append_container (ArgType :: Array , Some (inner_sig . as_cstr ()) , f) } # [doc = " Low-level function to append a struct."] # [doc = ""] # [doc = " Use in case tuples are not flexible enough -"] # [doc = " the easier way is to just call e g \"append1\" on a message and supply a tuple parameter."] pub fn append_struct < F : FnOnce (& mut IterAppend < 'a >) > (& mut self , f : F) { self . append_container (ArgType :: Struct , None , f) } # [doc = " Low-level function to append a dict entry."] # [doc = ""] # [doc = " Use in case the `Dict` struct is not flexible enough -"] # [doc = " the easier way is to just call e g \"append1\" on a message and supply a `Dict` parameter."] # [doc = ""] # [doc = " In order not to get D-Bus errors: during the call to \"f\", you should call \"append\" once"] # [doc = " for the key, then once for the value. You should only call this function for a subiterator"] # [doc = " you got from calling \"append_dict\", and signatures need to match what you specified in \"append_dict\"."] pub fn append_dict_entry < F : FnOnce (& mut IterAppend < 'a >) > (& mut self , f : F) { self . append_container (ArgType :: DictEntry , None , f) } # [doc = " Low-level function to append a dict."] # [doc = ""] # [doc = " Use in case the `Dict` struct is not flexible enough -"] # [doc = " the easier way is to just call e g \"append1\" on a message and supply a `Dict` parameter."] # [doc = ""] # [doc = " In order not to get D-Bus errors: during the call to \"f\", you should only call \"append_dict_entry\""] # [doc = " for the subiterator - do this as many times as the number of dict entries."] pub fn append_dict < F : FnOnce (& mut IterAppend < 'a >) > (& mut self , key_sig : & Signature , value_sig : & Signature , f : F) { let sig = format ! ("{{{}{}}}" , key_sig , value_sig) ; self . append_container (Array :: < bool , () > :: ARG_TYPE , Some (& CString :: new (sig) . unwrap ()) , f) ; } }
};
}
