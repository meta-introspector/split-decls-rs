// Generated macro for PatternBackend (trait)
macro_rules! Depcrate_commonPatternBackend {
() => {
// Module: crate::common
// Provides: {"PatternBackend"}
// Dependencies: {}
# [doc = " Types that implement backing data models for [`Pattern`] implement this trait."] # [doc = ""] # [doc = " The trait has no public methods and is not implementable outside of this crate."] # [doc = ""] # [doc = " <div class=\"stab unstable\">"] # [doc = " 🚫 This trait is sealed; it cannot be implemented by user code. If an API requests an item that implements this"] # [doc = " trait, please consider using a type from the implementors listed below."] # [doc = " </div>"] # [doc = ""] # [doc = " [`Pattern`]: crate::Pattern"] pub trait PatternBackend : crate :: private :: Sealed + 'static + core :: fmt :: Debug { # [doc = " The type to be used as the placeholder key in code."] type PlaceholderKey < 'a > ; # [doc = " Cowable version of the type to be used as the placeholder key in code."] # [cfg (feature = "alloc")] type PlaceholderKeyCow < 'a > ; # [doc = " The type of error that the [`TryWriteable`] for this backend can return."] type Error < 'a > ; # [doc = " The unsized type of the store required for this backend, usually `str` or `[u8]`."] # [doc (hidden)] type Store : ? Sized + PartialEq + core :: fmt :: Debug ; # [doc = " The iterator type returned by [`Self::try_from_items`]."] # [doc (hidden)] type Iter < 'a > : Iterator < Item = PatternItem < 'a , Self :: PlaceholderKey < 'a > > > ; # [doc = " Checks a store for validity, returning an error if invalid."] # [doc (hidden)] fn validate_store (store : & Self :: Store) -> Result < () , Error > ; # [doc = " Constructs a store from pattern items."] # [doc (hidden)] # [cfg (feature = "alloc")] fn try_from_items < 'cow , 'ph , I : Iterator < Item = Result < PatternItemCow < 'cow , Self :: PlaceholderKeyCow < 'ph > > , Error > > , > (items : I ,) -> Result < Box < Self :: Store > , Error > ; # [doc = " Iterates over the pattern items in a store."] # [doc (hidden)] fn iter_items (store : & Self :: Store) -> Self :: Iter < '_ > ; # [doc = " The store for the empty pattern, used to implement `Default`"] # [doc (hidden)] fn empty () -> & 'static Self :: Store ; }
};
}
