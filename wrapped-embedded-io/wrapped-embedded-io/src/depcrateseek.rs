// Generated macro for Seek (trait)
macro_rules! DepcrateSeek {
() => {
// Module: crate
// Provides: {"Seek"}
// Dependencies: {}
# [doc = " Blocking seek within streams.\\"] # [doc = ""] # [doc = " The `Seek` trait provides a cursor which can be moved within a stream of"] # [doc = " bytes."] # [doc = ""] # [doc = " The stream typically has a fixed size, allowing seeking relative to either"] # [doc = " end or the current offset."] # [doc = ""] # [doc = " This trait is the `embedded-io` equivalent of [`std::io::Seek`]."] pub trait Seek : ErrorType { # [doc = " Seek to an offset, in bytes, in a stream."] # [doc = " A seek beyond the end of a stream is allowed, but behavior is defined"] # [doc = " by the implementation."] # [doc = ""] # [doc = " If the seek operation completed successfully,"] # [doc = " this method returns the new position from the start of the stream."] # [doc = " That position can be used later with [`SeekFrom::Start`]."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Seeking can fail, for example because it might involve flushing a buffer."] # [doc = ""] # [doc = " Seeking to a negative offset is considered an error."] fn seek (& mut self , pos : SeekFrom) -> Result < u64 , Self :: Error > ; # [doc = " Rewind to the beginning of a stream."] # [doc = ""] # [doc = " This is a convenience method, equivalent to `seek(SeekFrom::Start(0))`."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Rewinding can fail, for example because it might involve flushing a buffer."] fn rewind (& mut self) -> Result < () , Self :: Error > { self . seek (SeekFrom :: Start (0)) ? ; Ok (()) } # [doc = " Returns the current seek position from the start of the stream."] # [doc = ""] # [doc = " This is equivalent to `self.seek(SeekFrom::Current(0))`."] fn stream_position (& mut self) -> Result < u64 , Self :: Error > { self . seek (SeekFrom :: Current (0)) } # [doc = " Seeks relative to the current position."] # [doc = ""] # [doc = " This is equivalent to `self.seek(SeekFrom::Current(offset))` but"] # [doc = " doesn't return the new position which can allow some implementations"] # [doc = " to perform more efficient seeks."] fn seek_relative (& mut self , offset : i64) -> Result < () , Self :: Error > { self . seek (SeekFrom :: Current (offset)) ? ; Ok (()) } }
};
}
