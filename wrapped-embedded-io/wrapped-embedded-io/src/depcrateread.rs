// Generated macro for Read (trait)
macro_rules! DepcrateRead {
() => {
// Module: crate
// Provides: {"Read"}
// Dependencies: {}
# [doc = " Blocking reader."] # [doc = ""] # [doc = " This trait is the `embedded-io` equivalent of [`std::io::Read`]."] pub trait Read : ErrorType { # [doc = " Read some bytes from this source into the specified buffer, returning how many bytes were read."] # [doc = ""] # [doc = " If no bytes are currently available to read:"] # [doc = " - The method blocks until at least one byte becomes available;"] # [doc = " - Once at least one (or more) bytes become available, a non-zero amount of those is copied to the"] # [doc = "   beginning of `buf`, and the amount is returned, *without waiting or blocking any further for"] # [doc = "   more bytes to become available*."] # [doc = ""] # [doc = " If bytes are available to read:"] # [doc = " - A non-zero amount of bytes is read to the beginning of `buf`, and the amount is returned immediately,"] # [doc = "   *without blocking and waiting for more bytes to become available*;"] # [doc = ""] # [doc = " Note that once some bytes are available to read, it is *not* guaranteed that all available bytes are returned."] # [doc = " It is possible for the implementation to read an amount of bytes less than `buf.len()` while there are more"] # [doc = " bytes immediately available."] # [doc = ""] # [doc = " This blocking behavior is important for the cases where `Read` represents the \"read\" leg of a pipe-like"] # [doc = " protocol (a socket, a pipe, a serial line etc.). The semantics is that the caller - by passing a non-empty"] # [doc = " buffer - does expect _some_ data (one or more bytes) - but _not necessarily `buf.len()` or more bytes_ -"] # [doc = " to become available, before the peer represented by `Read` would stop sending bytes due to"] # [doc = " application-specific reasons (as in the peer waiting for a response to the data it had sent so far)."] # [doc = ""] # [doc = " If the reader is at end-of-file (EOF), `Ok(0)` is returned. There is no guarantee that a reader at EOF"] # [doc = " will always be so in the future, for example a reader can stop being at EOF if another process appends"] # [doc = " more bytes to the underlying file."] # [doc = ""] # [doc = " If `buf.len() == 0`, `read` returns without blocking, with either `Ok(0)` or an error."] # [doc = " The `Ok(0)` doesn't indicate EOF, unlike when called with a non-empty buffer."] fn read (& mut self , buf : & mut [u8]) -> Result < usize , Self :: Error > ; # [doc = " Read the exact number of bytes required to fill `buf`."] # [doc = ""] # [doc = " This function calls `read()` in a loop until exactly `buf.len()` bytes have"] # [doc = " been read, blocking if needed."] # [doc = ""] # [doc = " If you are using [`ReadReady`] to avoid blocking, you should not use this function."] # [doc = " `ReadReady::read_ready()` returning true only guarantees the first call to `read()` will"] # [doc = " not block, so this function may still block in subsequent calls."] fn read_exact (& mut self , mut buf : & mut [u8]) -> Result < () , ReadExactError < Self :: Error > > { while ! buf . is_empty () { match self . read (buf) { Ok (0) => break , Ok (n) => buf = & mut buf [n ..] , Err (e) => return Err (ReadExactError :: Other (e)) , } } if buf . is_empty () { Ok (()) } else { Err (ReadExactError :: UnexpectedEof) } } }
};
}
