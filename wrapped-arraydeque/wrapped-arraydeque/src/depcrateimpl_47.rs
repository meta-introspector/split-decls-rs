// Generated macro for impl_47 (impl)
macro_rules! Depcrateimpl_47 {
() => {
// Module: crate
// Provides: {"impl_47"}
// Dependencies: {}
impl < T , const CAP : usize , B : Behavior > ArrayDeque < T , CAP , B > { # [doc = " Creates an empty `ArrayDeque`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let buf: ArrayDeque<usize, 2> = ArrayDeque::new();"] # [doc = " ```"] # [inline] pub const fn new () -> ArrayDeque < T , CAP , B > { ArrayDeque { xs : MaybeUninit :: uninit () , tail : 0 , len : 0 , marker : marker :: PhantomData , } } # [doc = " Return the capacity of the `ArrayDeque`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let buf: ArrayDeque<usize, 2> = ArrayDeque::new();"] # [doc = ""] # [doc = " assert_eq!(buf.capacity(), 2);"] # [doc = " ```"] # [inline] pub const fn capacity (& self) -> usize { CAP } # [doc = " Returns the number of elements in the `ArrayDeque`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 1> = ArrayDeque::new();"] # [doc = ""] # [doc = " assert_eq!(buf.len(), 0);"] # [doc = ""] # [doc = " buf.push_back(1);"] # [doc = ""] # [doc = " assert_eq!(buf.len(), 1);"] # [doc = " ```"] # [inline] pub fn len (& self) -> usize { self . len } # [doc = " Returns true if the buffer contains no elements"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 1> = ArrayDeque::new();"] # [doc = ""] # [doc = " assert!(buf.is_empty());"] # [doc = ""] # [doc = " buf.push_back(1);"] # [doc = ""] # [doc = " assert!(!buf.is_empty());"] # [doc = " ```"] # [inline] pub fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Entire capacity of the underlying storage"] pub fn as_uninit_slice (& self) -> & [MaybeUninit < T >] { unsafe { std :: slice :: from_raw_parts (self . xs . as_ptr () . cast () , CAP) } } # [doc = " Entire capacity of the underlying storage"] pub fn as_uninit_slice_mut (& mut self) -> & mut [MaybeUninit < T >] { unsafe { std :: slice :: from_raw_parts_mut (self . xs . as_mut_ptr () . cast () , CAP) } } # [doc = " Returns true if the buffer is full."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 1> = ArrayDeque::new();"] # [doc = ""] # [doc = " assert!(!buf.is_full());"] # [doc = ""] # [doc = " buf.push_back(1);"] # [doc = ""] # [doc = " assert!(buf.is_full());"] # [doc = " ```"] # [inline] pub fn is_full (& self) -> bool { self . len () == self . capacity () } # [doc = " Returns `true` if the `ArrayDeque` contains an element equal to the"] # [doc = " given value."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 2> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " assert_eq!(buf.contains(&1), true);"] # [doc = " assert_eq!(buf.contains(&3), false);"] # [doc = " ```"] pub fn contains (& self , x : & T) -> bool where T : PartialEq < T > , { let (a , b) = self . as_slices () ; a . contains (x) || b . contains (x) } # [doc = " Provides a reference to the front element, or `None` if the sequence is"] # [doc = " empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 2> = ArrayDeque::new();"] # [doc = " assert_eq!(buf.front(), None);"] # [doc = ""] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " assert_eq!(buf.front(), Some(&1));"] # [doc = " ```"] pub fn front (& self) -> Option < & T > { if ! self . is_empty () { Some (& self [0]) } else { None } } # [doc = " Provides a mutable reference to the front element, or `None` if the"] # [doc = " sequence is empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 2> = ArrayDeque::new();"] # [doc = " assert_eq!(buf.front_mut(), None);"] # [doc = ""] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " assert_eq!(buf.front_mut(), Some(&mut 1));"] # [doc = " ```"] pub fn front_mut (& mut self) -> Option < & mut T > { if ! self . is_empty () { Some (& mut self [0]) } else { None } } # [doc = " Provides a reference to the back element, or `None` if the sequence is"] # [doc = " empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 2> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " assert_eq!(buf.back(), Some(&2));"] # [doc = " ```"] pub fn back (& self) -> Option < & T > { if ! self . is_empty () { Some (& self [self . len () - 1]) } else { None } } # [doc = " Provides a mutable reference to the back element, or `None` if the"] # [doc = " sequence is empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 2> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " assert_eq!(buf.back_mut(), Some(&mut 2));"] # [doc = " ```"] pub fn back_mut (& mut self) -> Option < & mut T > { let len = self . len () ; if ! self . is_empty () { Some (& mut self [len - 1]) } else { None } } # [doc = " Retrieves an element in the `ArrayDeque` by index."] # [doc = ""] # [doc = " Element at index 0 is the front of the queue."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 3> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(0);"] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " assert_eq!(buf.get(1), Some(&1));"] # [doc = " ```"] # [inline] pub fn get (& self , index : usize) -> Option < & T > { if index < self . len () { let idx = Self :: wrap_add (self . tail () , index) ; unsafe { Some (& * self . ptr () . add (idx)) } } else { None } } # [doc = " Retrieves an element in the `ArrayDeque` mutably by index."] # [doc = ""] # [doc = " Element at index 0 is the front of the queue."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 3> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(0);"] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " assert_eq!(buf.get_mut(1), Some(&mut 1));"] # [doc = " ```"] # [inline] pub fn get_mut (& mut self , index : usize) -> Option < & mut T > { if index < self . len () { let idx = Self :: wrap_add (self . tail () , index) ; unsafe { Some (& mut * self . ptr_mut () . add (idx)) } } else { None } } # [doc = " Returns a front-to-back iterator."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 3> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(0);"] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " let expected = [0, 1, 2];"] # [doc = ""] # [doc = " assert!(buf.iter().eq(expected.iter()));"] # [doc = " ```"] # [inline] pub fn iter (& self) -> Iter < T > { Iter { tail : self . tail () , len : self . len () , ring : self . as_uninit_slice () , } } # [doc = " Returns a front-to-back iterator that returns mutable references."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<usize, 3> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(0);"] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " let mut expected = [0, 1, 2];"] # [doc = ""] # [doc = " assert!(buf.iter_mut().eq(expected.iter_mut()));"] # [doc = " ```"] # [inline] pub fn iter_mut (& mut self) -> IterMut < T > { IterMut { tail : self . tail () , len : self . len () , ring : self . as_uninit_slice_mut () , } } # [doc = " Make the buffer contiguous"] # [doc = ""] # [doc = " The linearization may be required when interacting with external"] # [doc = " interfaces requiring contiguous slices."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<isize, 10> = ArrayDeque::new();"] # [doc = " buf.extend_back([1, 2, 3]);"] # [doc = " buf.extend_front([-1, -2, -3]);"] # [doc = ""] # [doc = " buf.linearize();"] # [doc = ""] # [doc = " assert_eq!(buf.as_slices().1.len(), 0);"] # [doc = " ```"] # [doc = ""] # [doc = " # Complexity"] # [doc = ""] # [doc = " Takes `O(len())` time and no extra space."] pub fn linearize (& mut self) { if self . is_contiguous () { return ; } let tail = self . tail () ; let len = self . len () ; let mut new_tail = tail ; let mut dst : usize = 0 ; while dst < len { let mut src = new_tail ; while src < CAP && dst < len { if dst == new_tail { new_tail = src ; } let xs = self . ptr_mut () ; unsafe { ptr :: swap (xs . add (dst) , xs . add (src)) ; } dst += 1 ; src += 1 ; } } unsafe { self . set_tail (0) ; } } # [doc = " Removes the first element and returns it, or `None` if the sequence is"] # [doc = " empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 2> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " assert_eq!(buf.pop_front(), Some(1));"] # [doc = " assert_eq!(buf.pop_front(), Some(2));"] # [doc = " assert_eq!(buf.pop_front(), None);"] # [doc = " ```"] pub fn pop_front (& mut self) -> Option < T > { if self . is_empty () { return None ; } unsafe { let tail = self . tail () ; self . set_tail_forward () ; Some (self . buffer_read (tail)) } } # [doc = " Removes the last element from a buffer and returns it, or `None` if"] # [doc = " it is empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 2> = ArrayDeque::new();"] # [doc = " assert_eq!(buf.pop_back(), None);"] # [doc = ""] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " assert_eq!(buf.pop_back(), Some(2));"] # [doc = " assert_eq!(buf.pop_back(), Some(1));"] # [doc = " ```"] pub fn pop_back (& mut self) -> Option < T > { if self . is_empty () { return None ; } unsafe { self . set_head_backward () ; let head = self . head () ; Some (self . buffer_read (head)) } } # [doc = " Clears the buffer, removing all values."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 1> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(1);"] # [doc = " buf.clear();"] # [doc = ""] # [doc = " assert!(buf.is_empty());"] # [doc = " ```"] # [inline] pub fn clear (& mut self) { self . drain (..) ; } # [doc = " Create a draining iterator that removes the specified range in the"] # [doc = " `ArrayDeque` and yields the removed items."] # [doc = ""] # [doc = " Note 1: The element range is removed even if the iterator is not"] # [doc = " consumed until the end."] # [doc = ""] # [doc = " Note 2: It is unspecified how many elements are removed from the deque,"] # [doc = " if the `Drain` value is not dropped, but the borrow it holds expires"] # [doc = " (eg. due to mem::forget)."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the starting point is greater than the end point or if"] # [doc = " the end point is greater than the length of the deque."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 3> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(0);"] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " {"] # [doc = "     let drain = buf.drain(2..);"] # [doc = "     assert!([2].into_iter().eq(drain));"] # [doc = " }"] # [doc = ""] # [doc = " {"] # [doc = "     let iter = buf.iter();"] # [doc = "     assert!([0, 1].iter().eq(iter));"] # [doc = " }"] # [doc = ""] # [doc = " // A full range clears all contents"] # [doc = " buf.drain(..);"] # [doc = " assert!(buf.is_empty());"] # [doc = " ```"] # [track_caller] # [inline] pub fn drain < R > (& mut self , range : R) -> Drain < T , CAP , B > where R : RangeArgument < usize > , { let len = self . len () ; let start = range . start () . unwrap_or (0) ; let end = range . end () . unwrap_or (len) ; assert ! (start <= end , "drain lower bound was too large") ; assert ! (end <= len , "drain upper bound was too large") ; let drain_tail = Self :: wrap_add (self . tail () , start) ; let drain_head = Self :: wrap_add (self . tail () , end) ; let drain_len = end - start ; unsafe { self . set_len (start) } Drain { deque : self as * mut _ , after_tail : drain_head , after_len : len - end , iter : Iter { tail : drain_tail , len : drain_len , ring : self . as_uninit_slice_mut () , } , } } # [doc = " Swaps elements at indices `i` and `j`."] # [doc = ""] # [doc = " `i` and `j` may be equal."] # [doc = ""] # [doc = " Fails if there is no element with either index."] # [doc = ""] # [doc = " Element at index 0 is the front of the queue."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 3> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(0);"] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " buf.swap(0, 2);"] # [doc = ""] # [doc = " assert_eq!(buf, [2, 1, 0].into());"] # [doc = " ```"] # [track_caller] # [inline] pub fn swap (& mut self , i : usize , j : usize) { assert ! (i < self . len ()) ; assert ! (j < self . len ()) ; let ri = Self :: wrap_add (self . tail () , i) ; let rj = Self :: wrap_add (self . tail () , j) ; let xs = self . ptr_mut () ; unsafe { ptr :: swap (xs . add (ri) , xs . add (rj)) } } # [doc = " Removes an element from anywhere in the `ArrayDeque` and returns it, replacing it with the"] # [doc = " last element."] # [doc = ""] # [doc = " This does not preserve ordering, but is O(1)."] # [doc = ""] # [doc = " Returns `None` if `index` is out of bounds."] # [doc = ""] # [doc = " Element at index 0 is the front of the queue."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 3> = ArrayDeque::new();"] # [doc = " assert_eq!(buf.swap_remove_back(0), None);"] # [doc = ""] # [doc = " buf.push_back(0);"] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " assert_eq!(buf.swap_remove_back(0), Some(0));"] # [doc = " assert_eq!(buf, [2, 1].into());"] # [doc = " ```"] pub fn swap_remove_back (& mut self , index : usize) -> Option < T > { let length = self . len () ; if length > 0 && index < length - 1 { self . swap (index , length - 1) ; } else if index >= length { return None ; } self . pop_back () } # [doc = " Removes an element from anywhere in the `ArrayDeque` and returns it,"] # [doc = " replacing it with the first element."] # [doc = ""] # [doc = " This does not preserve ordering, but is O(1)."] # [doc = ""] # [doc = " Returns `None` if `index` is out of bounds."] # [doc = ""] # [doc = " Element at index 0 is the front of the queue."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 3> = ArrayDeque::new();"] # [doc = " assert_eq!(buf.swap_remove_back(0), None);"] # [doc = ""] # [doc = " buf.push_back(0);"] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " assert_eq!(buf.swap_remove_front(2), Some(2));"] # [doc = " assert_eq!(buf, [1, 0].into());"] # [doc = " ```"] pub fn swap_remove_front (& mut self , index : usize) -> Option < T > { let length = self . len () ; if length > 0 && index < length && index != 0 { self . swap (index , 0) ; } else if index >= length { return None ; } self . pop_front () } # [doc = " Removes and returns the element at `index` from the `ArrayDeque`."] # [doc = " Whichever end is closer to the removal point will be moved to make"] # [doc = " room, and all the affected elements will be moved to new positions."] # [doc = " Returns `None` if `index` is out of bounds."] # [doc = ""] # [doc = " Element at index 0 is the front of the queue."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 3> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(0);"] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " assert_eq!(buf.remove(1), Some(1));"] # [doc = " assert_eq!(buf, [0, 2].into());"] # [doc = " ```"] pub fn remove (& mut self , index : usize) -> Option < T > { if self . is_empty () || self . len () <= index { return None ; } let idx = Self :: wrap_add (self . tail () , index) ; let elem = unsafe { Some (self . buffer_read (idx)) } ; let distance_to_tail = index ; let distance_to_head = self . len () - index ; let contiguous = self . is_contiguous () ; match (contiguous , distance_to_tail <= distance_to_head , idx >= self . tail () ,) { (true , true , _) => { unsafe { let tail = self . tail () ; self . copy (tail + 1 , tail , index) ; self . set_tail_forward () ; } } (true , false , _) => { unsafe { let head = self . head () ; self . copy (idx , idx + 1 , head - idx - 1) ; self . set_head_backward () ; } } (false , true , true) => { unsafe { let tail = self . tail () ; self . copy (tail + 1 , tail , index) ; self . set_tail_forward () ; } } (false , false , false) => { unsafe { let head = self . head () ; self . copy (idx , idx + 1 , head - idx - 1) ; self . set_head_backward () ; } } (false , false , true) => { unsafe { self . copy (idx , idx + 1 , CAP - idx - 1) ; if self . head () != 0 { self . copy (CAP - 1 , 0 , 1) ; let head = self . head () ; self . copy (0 , 1 , head - 1) ; } self . set_head_backward () ; } } (false , true , false) => { unsafe { let tail = self . tail () ; self . copy (1 , 0 , idx) ; self . copy (0 , CAP - 1 , 1) ; self . copy (tail + 1 , tail , CAP - tail - 1) ; self . set_tail_forward () ; } } } elem } # [doc = " Splits the collection into two at the given index."] # [doc = ""] # [doc = " Returns a newly allocated `Self`. `self` contains elements `[0, at)`,"] # [doc = " and the returned `Self` contains elements `[at, len)`."] # [doc = ""] # [doc = " Element at index 0 is the front of the queue."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `at > len`"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 3> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(0);"] # [doc = " buf.push_back(1);"] # [doc = " buf.push_back(2);"] # [doc = ""] # [doc = " // buf = [0], buf2 = [1, 2]"] # [doc = " let buf2 = buf.split_off(1);"] # [doc = ""] # [doc = " assert_eq!(buf.len(), 1);"] # [doc = " assert_eq!(buf2.len(), 2);"] # [doc = " ```"] # [track_caller] # [inline] pub fn split_off (& mut self , at : usize) -> Self { let len = self . len () ; assert ! (at <= len , "`at` out of bounds") ; let other_len = len - at ; let mut other = Self :: new () ; unsafe { let (first_half , second_half) = self . as_slices () ; let first_len = first_half . len () ; let second_len = second_half . len () ; if at < first_len { let amount_in_first = first_len - at ; ptr :: copy_nonoverlapping (first_half . as_ptr () . add (at) , other . ptr_mut () , amount_in_first ,) ; ptr :: copy_nonoverlapping (second_half . as_ptr () , other . ptr_mut () . add (amount_in_first) , second_len ,) ; } else { let offset = at - first_len ; let amount_in_second = second_len - offset ; ptr :: copy_nonoverlapping (second_half . as_ptr () . add (offset) , other . ptr_mut () , amount_in_second ,) ; } } unsafe { self . set_len (at) ; other . set_len (other_len) ; } other } # [doc = " Retains only the elements specified by the predicate."] # [doc = ""] # [doc = " In other words, remove all elements `e` such that `f(&e)` returns false."] # [doc = " This method operates in place and preserves the order of the retained"] # [doc = " elements."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 4> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.extend_back(0..4);"] # [doc = " buf.retain(|&x| x % 2 == 0);"] # [doc = ""] # [doc = " assert_eq!(buf, [0, 2].into());"] # [doc = " ```"] pub fn retain < F > (& mut self , mut f : F) where F : FnMut (& T) -> bool , { let len = self . len () ; let mut del = 0 ; for i in 0 .. len { if ! f (& self [i]) { del += 1 ; } else if del > 0 { self . swap (i - del , i) ; } } if del > 0 { for _ in (len - del) .. self . len () { self . pop_back () ; } } } # [doc = " Returns a pair of slices which contain, in order, the contents of the"] # [doc = " `ArrayDeque`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 7> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(0);"] # [doc = " buf.push_back(1);"] # [doc = ""] # [doc = " assert_eq!(buf.as_slices(), (&[0, 1][..], &[][..]));"] # [doc = ""] # [doc = " buf.push_front(2);"] # [doc = ""] # [doc = " assert_eq!(buf.as_slices(), (&[2][..], &[0, 1][..]));"] # [doc = " ```"] # [inline] pub fn as_slices (& self) -> (& [T] , & [T]) { let contiguous = self . is_contiguous () ; let head = self . head () ; let tail = self . tail () ; let buf = self . as_uninit_slice () ; if contiguous { let (empty , buf) = buf . split_at (0) ; unsafe { (slice_assume_init_ref (& buf [tail .. head]) , slice_assume_init_ref (empty) ,) } } else { let (mid , right) = buf . split_at (tail) ; let (left , _) = mid . split_at (head) ; unsafe { (slice_assume_init_ref (right) , slice_assume_init_ref (left)) } } } # [doc = " Returns a pair of slices which contain, in order, the contents of the"] # [doc = " `ArrayDeque`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use arraydeque::ArrayDeque;"] # [doc = ""] # [doc = " let mut buf: ArrayDeque<_, 7> = ArrayDeque::new();"] # [doc = ""] # [doc = " buf.push_back(0);"] # [doc = " buf.push_back(1);"] # [doc = ""] # [doc = " assert_eq!(buf.as_mut_slices(), (&mut [0, 1][..], &mut[][..]));"] # [doc = ""] # [doc = " buf.push_front(2);"] # [doc = ""] # [doc = " assert_eq!(buf.as_mut_slices(), (&mut[2][..], &mut[0, 1][..]));"] # [doc = " ```"] # [inline] pub fn as_mut_slices (& mut self) -> (& mut [T] , & mut [T]) { let contiguous = self . is_contiguous () ; let head = self . head () ; let tail = self . tail () ; let buf = self . as_uninit_slice_mut () ; if contiguous { let (empty , buf) = buf . split_at_mut (0) ; unsafe { (slice_assume_init_mut (& mut buf [tail .. head]) , slice_assume_init_mut (empty) ,) } } else { let (mid , right) = buf . split_at_mut (tail) ; let (left , _) = mid . split_at_mut (head) ; unsafe { (slice_assume_init_mut (right) , slice_assume_init_mut (left)) } } } }
};
}
