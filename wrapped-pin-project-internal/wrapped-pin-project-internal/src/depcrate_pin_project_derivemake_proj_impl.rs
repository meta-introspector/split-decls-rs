// Generated macro for make_proj_impl (function)
macro_rules! Depcrate_pin_project_derivemake_proj_impl {
() => {
// Module: crate::pin_project::derive
// Provides: {"make_proj_impl"}
// Dependencies: {}
# [doc = " Creates an implementation of the projection methods."] # [doc = ""] # [doc = " On structs, both the `project` and `project_ref` methods are always generated,"] # [doc = " and the `project_replace` method is only generated if `ProjReplace::span` is `Some`."] # [doc = ""] # [doc = " On enums, only methods that the returned projected type is named will be generated."] fn make_proj_impl (cx : & Context < '_ > , proj_body : & TokenStream , proj_ref_body : & TokenStream , proj_own_body : & TokenStream ,) -> TokenStream { let vis = & cx . proj . vis ; let lifetime = & cx . proj . lifetime ; let orig_ident = cx . orig . ident ; let proj_ident = & cx . proj . mut_ident ; let proj_ref_ident = & cx . proj . ref_ident ; let proj_own_ident = & cx . proj . own_ident ; let orig_ty_generics = cx . orig . generics . split_for_impl () . 1 ; let proj_ty_generics = cx . proj . generics . split_for_impl () . 1 ; let (impl_generics , ty_generics , where_clause) = cx . orig . generics . split_for_impl () ; let allow_dead_code = quote ! { # [allow (dead_code)] } ; let mut project = Some (quote ! { # allow_dead_code # [inline] # vis fn project <# lifetime > (self : _pin_project :: __private :: Pin <&# lifetime mut Self >,) -> # proj_ident # proj_ty_generics { unsafe { # proj_body } } }) ; let mut project_ref = Some (quote ! { # allow_dead_code # [inline] # vis fn project_ref <# lifetime > (self : _pin_project :: __private :: Pin <&# lifetime Self >,) -> # proj_ref_ident # proj_ty_generics { unsafe { # proj_ref_body } } }) ; let mut project_replace = cx . project_replace . span () . map (| span | { let sig = quote_spanned ! { span => # vis fn project_replace (self : _pin_project :: __private :: Pin <& mut Self >, __replacement : Self ,) -> # proj_own_ident # orig_ty_generics } ; quote ! { # allow_dead_code # [inline] # sig { unsafe { let __self_ptr : * mut Self = self . get_unchecked_mut () ; let __guard = _pin_project :: __private :: UnsafeOverwriteGuard :: new (__self_ptr , __replacement ,) ; # proj_own_body } } } }) ; if cx . kind == Enum { if ! cx . project { project = None ; } if ! cx . project_ref { project_ref = None ; } if cx . project_replace . ident () . is_none () { project_replace = None ; } } quote ! { impl # impl_generics # orig_ident # ty_generics # where_clause { # project # project_ref # project_replace } } }
};
}
