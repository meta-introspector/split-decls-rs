// Generated macro for Closure (struct)
macro_rules! Depcrate_closureClosure {
() => {
// Module: crate::closure
// Provides: {"Closure"}
// Dependencies: {}
# [doc = " A handle to both a closure in Rust as well as JS closure which will invoke"] # [doc = " the Rust closure."] # [doc = ""] # [doc = " A `Closure` is the primary way that a `'static` lifetime closure is"] # [doc = " transferred from Rust to JS. `Closure` currently requires that the closures"] # [doc = " it's created with have the `'static` lifetime in Rust for soundness reasons."] # [doc = ""] # [doc = " This type is a \"handle\" in the sense that whenever it is dropped it will"] # [doc = " invalidate the JS closure that it refers to. Any usage of the closure in JS"] # [doc = " after the `Closure` has been dropped will raise an exception. It's then up"] # [doc = " to you to arrange for `Closure` to be properly deallocate at an appropriate"] # [doc = " location in your program."] # [doc = ""] # [doc = " The type parameter on `Closure` is the type of closure that this represents."] # [doc = " Currently this can only be the `Fn` and `FnMut` traits with up to 7"] # [doc = " arguments (and an optional return value)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Here are a number of examples of using `Closure`."] # [doc = ""] # [doc = " ## Using the `setInterval` API"] # [doc = ""] # [doc = " Sample usage of `Closure` to invoke the `setInterval` API."] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use wasm_bindgen::prelude::*;"] # [doc = ""] # [doc = " #[wasm_bindgen]"] # [doc = " extern \"C\" {"] # [doc = "     fn setInterval(closure: &Closure<dyn FnMut()>, time: u32) -> i32;"] # [doc = "     fn clearInterval(id: i32);"] # [doc = ""] # [doc = "     #[wasm_bindgen(js_namespace = console)]"] # [doc = "     fn log(s: &str);"] # [doc = " }"] # [doc = ""] # [doc = " #[wasm_bindgen]"] # [doc = " pub struct IntervalHandle {"] # [doc = "     interval_id: i32,"] # [doc = "     _closure: Closure<dyn FnMut()>,"] # [doc = " }"] # [doc = ""] # [doc = " impl Drop for IntervalHandle {"] # [doc = "     fn drop(&mut self) {"] # [doc = "         clearInterval(self.interval_id);"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " #[wasm_bindgen]"] # [doc = " pub fn run() -> IntervalHandle {"] # [doc = "     // First up we use `Closure::new` to wrap up a Rust closure and create"] # [doc = "     // a JS closure."] # [doc = "     let cb = Closure::new(|| {"] # [doc = "         log(\"interval elapsed!\");"] # [doc = "     });"] # [doc = ""] # [doc = "     // Next we pass this via reference to the `setInterval` function, and"] # [doc = "     // `setInterval` gets a handle to the corresponding JS closure."] # [doc = "     let interval_id = setInterval(&cb, 1_000);"] # [doc = ""] # [doc = "     // If we were to drop `cb` here it would cause an exception to be raised"] # [doc = "     // whenever the interval elapses. Instead we *return* our handle back to JS"] # [doc = "     // so JS can decide when to cancel the interval and deallocate the closure."] # [doc = "     IntervalHandle {"] # [doc = "         interval_id,"] # [doc = "         _closure: cb,"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " ## Casting a `Closure` to a `js_sys::Function`"] # [doc = ""] # [doc = " This is the same `setInterval` example as above, except it is using"] # [doc = " `web_sys` (which uses `js_sys::Function` for callbacks) instead of manually"] # [doc = " writing bindings to `setInterval` and other Web APIs."] # [doc = ""] # [doc = " ```rust,ignore"] # [doc = " use wasm_bindgen::JsCast;"] # [doc = ""] # [doc = " #[wasm_bindgen]"] # [doc = " pub struct IntervalHandle {"] # [doc = "     interval_id: i32,"] # [doc = "     _closure: Closure<dyn FnMut()>,"] # [doc = " }"] # [doc = ""] # [doc = " impl Drop for IntervalHandle {"] # [doc = "     fn drop(&mut self) {"] # [doc = "         let window = web_sys::window().unwrap();"] # [doc = "         window.clear_interval_with_handle(self.interval_id);"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " #[wasm_bindgen]"] # [doc = " pub fn run() -> Result<IntervalHandle, JsValue> {"] # [doc = "     let cb = Closure::new(|| {"] # [doc = "         web_sys::console::log_1(&\"interval elapsed!\".into());"] # [doc = "     });"] # [doc = ""] # [doc = "     let window = web_sys::window().unwrap();"] # [doc = "     let interval_id = window.set_interval_with_callback_and_timeout_and_arguments_0("] # [doc = "         // Note this method call, which uses `as_ref()` to get a `JsValue`"] # [doc = "         // from our `Closure` which is then converted to a `&Function`"] # [doc = "         // using the `JsCast::unchecked_ref` function."] # [doc = "         cb.as_ref().unchecked_ref(),"] # [doc = "         1_000,"] # [doc = "     )?;"] # [doc = ""] # [doc = "     // Same as above."] # [doc = "     Ok(IntervalHandle {"] # [doc = "         interval_id,"] # [doc = "         _closure: cb,"] # [doc = "     })"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " ## Using `FnOnce` and `Closure::once` with `requestAnimationFrame`"] # [doc = ""] # [doc = " Because `requestAnimationFrame` only calls its callback once, we can use"] # [doc = " `FnOnce` and `Closure::once` with it."] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use wasm_bindgen::prelude::*;"] # [doc = ""] # [doc = " #[wasm_bindgen]"] # [doc = " extern \"C\" {"] # [doc = "     fn requestAnimationFrame(closure: &Closure<dyn FnMut()>) -> u32;"] # [doc = "     fn cancelAnimationFrame(id: u32);"] # [doc = ""] # [doc = "     #[wasm_bindgen(js_namespace = console)]"] # [doc = "     fn log(s: &str);"] # [doc = " }"] # [doc = ""] # [doc = " #[wasm_bindgen]"] # [doc = " pub struct AnimationFrameHandle {"] # [doc = "     animation_id: u32,"] # [doc = "     _closure: Closure<dyn FnMut()>,"] # [doc = " }"] # [doc = ""] # [doc = " impl Drop for AnimationFrameHandle {"] # [doc = "     fn drop(&mut self) {"] # [doc = "         cancelAnimationFrame(self.animation_id);"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // A type that will log a message when it is dropped."] # [doc = " struct LogOnDrop(&'static str);"] # [doc = " impl Drop for LogOnDrop {"] # [doc = "     fn drop(&mut self) {"] # [doc = "         log(self.0);"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " #[wasm_bindgen]"] # [doc = " pub fn run() -> AnimationFrameHandle {"] # [doc = "     // We are using `Closure::once` which takes a `FnOnce`, so the function"] # [doc = "     // can drop and/or move things that it closes over."] # [doc = "     let fired = LogOnDrop(\"animation frame fired or canceled\");"] # [doc = "     let cb = Closure::once(move || drop(fired));"] # [doc = ""] # [doc = "     // Schedule the animation frame!"] # [doc = "     let animation_id = requestAnimationFrame(&cb);"] # [doc = ""] # [doc = "     // Again, return a handle to JS, so that the closure is not dropped"] # [doc = "     // immediately and JS can decide whether to cancel the animation frame."] # [doc = "     AnimationFrameHandle {"] # [doc = "         animation_id,"] # [doc = "         _closure: cb,"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " ## Converting `FnOnce`s directly into JavaScript Functions with `Closure::once_into_js`"] # [doc = ""] # [doc = " If we don't want to allow a `FnOnce` to be eagerly dropped (maybe because we"] # [doc = " just want it to drop after it is called and don't care about cancellation)"] # [doc = " then we can use the `Closure::once_into_js` function."] # [doc = ""] # [doc = " This is the same `requestAnimationFrame` example as above, but without"] # [doc = " supporting early cancellation."] # [doc = ""] # [doc = " ```"] # [doc = " use wasm_bindgen::prelude::*;"] # [doc = ""] # [doc = " #[wasm_bindgen]"] # [doc = " extern \"C\" {"] # [doc = "     // We modify the binding to take an untyped `JsValue` since that is what"] # [doc = "     // is returned by `Closure::once_into_js`."] # [doc = "     //"] # [doc = "     // If we were using the `web_sys` binding for `requestAnimationFrame`,"] # [doc = "     // then the call sites would cast the `JsValue` into a `&js_sys::Function`"] # [doc = "     // using `f.unchecked_ref::<js_sys::Function>()`. See the `web_sys`"] # [doc = "     // example above for details."] # [doc = "     fn requestAnimationFrame(callback: JsValue);"] # [doc = ""] # [doc = "     #[wasm_bindgen(js_namespace = console)]"] # [doc = "     fn log(s: &str);"] # [doc = " }"] # [doc = ""] # [doc = " // A type that will log a message when it is dropped."] # [doc = " struct LogOnDrop(&'static str);"] # [doc = " impl Drop for LogOnDrop {"] # [doc = "     fn drop(&mut self) {"] # [doc = "         log(self.0);"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " #[wasm_bindgen]"] # [doc = " pub fn run() {"] # [doc = "     // We are using `Closure::once_into_js` which takes a `FnOnce` and"] # [doc = "     // converts it into a JavaScript function, which is returned as a"] # [doc = "     // `JsValue`."] # [doc = "     let fired = LogOnDrop(\"animation frame fired\");"] # [doc = "     let cb = Closure::once_into_js(move || drop(fired));"] # [doc = ""] # [doc = "     // Schedule the animation frame!"] # [doc = "     requestAnimationFrame(cb);"] # [doc = ""] # [doc = "     // No need to worry about whether or not we drop a `Closure`"] # [doc = "     // here or return some sort of handle to JS!"] # [doc = " }"] # [doc = " ```"] pub struct Closure < T : ? Sized > { js : JsClosure , _marker : PhantomData < Box < T > > , }
};
}
