// Generated macro for JsCast (trait)
macro_rules! Depcrate_castJsCast {
() => {
// Module: crate::cast
// Provides: {"JsCast"}
// Dependencies: {}
# [doc = " A trait for checked and unchecked casting between JS types."] # [doc = ""] # [doc = " Specified [in an RFC][rfc] this trait is intended to provide support for"] # [doc = " casting JS values between different types of one another. In JS there aren't"] # [doc = " many static types but we've ascribed JS values with static types in Rust,"] # [doc = " yet they often need to be switched to other types temporarily! This trait"] # [doc = " provides both checked and unchecked casting into various kinds of values."] # [doc = ""] # [doc = " This trait is automatically implemented for any type imported in a"] # [doc = " `#[wasm_bindgen]` `extern` block."] # [doc = ""] # [doc = " [rfc]: https://github.com/rustwasm/rfcs/blob/master/text/002-wasm-bindgen-inheritance-casting.md"] pub trait JsCast where Self : AsRef < JsValue > + Into < JsValue > , { # [doc = " Test whether this JS value has a type `T`."] # [doc = ""] # [doc = " This method will dynamically check to see if this JS object can be"] # [doc = " casted to the JS object of type `T`. Usually this uses the `instanceof`"] # [doc = " operator. This also works with primitive types like"] # [doc = " booleans/strings/numbers as well as cross-realm object like `Array`"] # [doc = " which can originate from other iframes."] # [doc = ""] # [doc = " In general this is intended to be a more robust version of"] # [doc = " `is_instance_of`, but if you want strictly the `instanceof` operator"] # [doc = " it's recommended to use that instead."] fn has_type < T > (& self) -> bool where T : JsCast , { T :: is_type_of (self . as_ref ()) } # [doc = " Performs a dynamic cast (checked at runtime) of this value into the"] # [doc = " target type `T`."] # [doc = ""] # [doc = " This method will return `Err(self)` if `self.has_type::<T>()`"] # [doc = " returns `false`, and otherwise it will return `Ok(T)` manufactured with"] # [doc = " an unchecked cast (verified correct via the `has_type` operation)."] fn dyn_into < T > (self) -> Result < T , Self > where T : JsCast , { if self . has_type :: < T > () { Ok (self . unchecked_into ()) } else { Err (self) } } # [doc = " Performs a dynamic cast (checked at runtime) of this value into the"] # [doc = " target type `T`."] # [doc = ""] # [doc = " This method will return `None` if `self.has_type::<T>()`"] # [doc = " returns `false`, and otherwise it will return `Some(&T)` manufactured"] # [doc = " with an unchecked cast (verified correct via the `has_type` operation)."] fn dyn_ref < T > (& self) -> Option < & T > where T : JsCast , { if self . has_type :: < T > () { Some (self . unchecked_ref ()) } else { None } } # [doc = " Performs a zero-cost unchecked cast into the specified type."] # [doc = ""] # [doc = " This method will convert the `self` value to the type `T`, where both"] # [doc = " `self` and `T` are simple wrappers around `JsValue`. This method **does"] # [doc = " not check whether `self` is an instance of `T`**. If used incorrectly"] # [doc = " then this method may cause runtime exceptions in both Rust and JS, this"] # [doc = " should be used with caution."] fn unchecked_into < T > (self) -> T where T : JsCast , { T :: unchecked_from_js (self . into ()) } # [doc = " Performs a zero-cost unchecked cast into a reference to the specified"] # [doc = " type."] # [doc = ""] # [doc = " This method will convert the `self` value to the type `T`, where both"] # [doc = " `self` and `T` are simple wrappers around `JsValue`. This method **does"] # [doc = " not check whether `self` is an instance of `T`**. If used incorrectly"] # [doc = " then this method may cause runtime exceptions in both Rust and JS, this"] # [doc = " should be used with caution."] # [doc = ""] # [doc = " This method, unlike `unchecked_into`, does not consume ownership of"] # [doc = " `self` and instead works over a shared reference."] fn unchecked_ref < T > (& self) -> & T where T : JsCast , { T :: unchecked_from_js_ref (self . as_ref ()) } # [doc = " Test whether this JS value is an instance of the type `T`."] # [doc = ""] # [doc = " This method performs a dynamic check (at runtime) using the JS"] # [doc = " `instanceof` operator. This method returns `self instanceof T`."] # [doc = ""] # [doc = " Note that `instanceof` does not always work with primitive values or"] # [doc = " across different realms (e.g. iframes). If you're not sure whether you"] # [doc = " specifically need only `instanceof` it's recommended to use `has_type`"] # [doc = " instead."] fn is_instance_of < T > (& self) -> bool where T : JsCast , { T :: instanceof (self . as_ref ()) } # [doc = " Performs a dynamic `instanceof` check to see whether the `JsValue`"] # [doc = " provided is an instance of this type."] # [doc = ""] # [doc = " This is intended to be an internal implementation detail, you likely"] # [doc = " won't need to call this. It's generally called through the"] # [doc = " `is_instance_of` method instead."] fn instanceof (val : & JsValue) -> bool ; # [doc = " Performs a dynamic check to see whether the `JsValue` provided"] # [doc = " is a value of this type."] # [doc = ""] # [doc = " Unlike `instanceof`, this can be specialised to use a custom check by"] # [doc = " adding a `#[wasm_bindgen(is_type_of = callback)]` attribute to the"] # [doc = " type import declaration."] # [doc = ""] # [doc = " Other than that, this is intended to be an internal implementation"] # [doc = " detail of `has_type` and you likely won't need to call this."] fn is_type_of (val : & JsValue) -> bool { Self :: instanceof (val) } # [doc = " Performs a zero-cost unchecked conversion from a `JsValue` into an"] # [doc = " instance of `Self`"] # [doc = ""] # [doc = " This is intended to be an internal implementation detail, you likely"] # [doc = " won't need to call this."] fn unchecked_from_js (val : JsValue) -> Self ; # [doc = " Performs a zero-cost unchecked conversion from a `&JsValue` into an"] # [doc = " instance of `&Self`."] # [doc = ""] # [doc = " Note the safety of this method, which basically means that `Self` must"] # [doc = " be a newtype wrapper around `JsValue`."] # [doc = ""] # [doc = " This is intended to be an internal implementation detail, you likely"] # [doc = " won't need to call this."] fn unchecked_from_js_ref (val : & JsValue) -> & Self ; }
};
}
