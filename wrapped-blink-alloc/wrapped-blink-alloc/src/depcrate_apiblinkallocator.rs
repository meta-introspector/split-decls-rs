// Generated macro for BlinkAllocator (trait)
macro_rules! Depcrate_apiBlinkAllocator {
() => {
// Module: crate::api
// Provides: {"BlinkAllocator"}
// Dependencies: {}
# [doc = " Extension trait for [`Allocator`] that defines blink allocator API."] # [doc = " Blink-allocators are allocators with cheap allocation"] # [doc = " and potentially no-op deallocation."] # [doc = " Blink-allocator *can* reuse deallocated memory, but not required to."] # [doc = " Typically deallocation is either no-op or processed only if deallocating"] # [doc = " the very last allocated memory block."] # [doc = " The [`reset`][BlinkAllocator::reset]"] # [doc = " method deallocates all memory allocated from this instance at once."] # [doc = ""] # [doc = " Additional guarantees are provided that"] # [doc = ""] # [doc = " * [`Allocator::shrink`] will always succeed and never move memory"] # [doc = " when `ptr` is already aligned to `new_layout.align()`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Draws most requirements from [`Allocator`] super-trait."] # [doc = " The [`reset`][BlinkAllocator::reset] method"] # [doc = " may invalidate currently allocated memory if allocator is not cloneable."] pub unsafe trait BlinkAllocator : Allocator { # [doc = " Resets allocator potentially invalidating all allocations"] # [doc = " made from this instance."] # [doc = " This is no-op if allocator is [`Clone`][core::clone::Clone]"] # [doc = " (typically shared reference to blink-allocator)."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Caller must guarantee that all allocations made from this instance"] # [doc = " won't be used after this call."] # [doc = " The potential invalid memory access will happen through"] # [doc = " raw pointer and requires `unsafe`."] # [doc = ""] # [doc = " This method requires mutable borrow of the blink-allocator"] # [doc = " and so cannot be called while collection types reference it."] # [doc = " So it is safe to use reference to blink allocator as allocator type"] # [doc = " for, say, [`Vec`] and later then call `reset` safely."] # [doc = " The `Vec` would have to be dropped (or forgotten) before calling `reset`"] # [doc = " due to mutable borrow."] # [doc = ""] # [doc = " [`Vec`]: alloc::vec::Vec"] fn reset (& mut self) ; }
};
}
