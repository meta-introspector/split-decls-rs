// Generated macro for impl_143 (impl)
macro_rules! Depcrate_syncimpl_143 {
() => {
// Module: crate::sync
// Provides: {"impl_143"}
// Dependencies: {}
impl < A > LocalBlinkAlloc < '_ , A > where A : Allocator , { # [doc = " Allocates memory with specified layout from this allocator."] # [doc = " If needed it will allocate new chunk using underlying allocator."] # [doc = " If chunk allocation fails, it will return `Err`."] # [inline (always)] pub fn allocate (& self , layout : Layout) -> Result < NonNull < [u8] > , AllocError > { if let Some (ptr) = unsafe { self . arena . alloc_fast (layout) } { return Ok (ptr) ; } unsafe { self . arena . alloc_slow (layout , self . shared) } } # [doc = " Resizes memory allocation."] # [doc = " Potentially happens in-place."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `ptr` must be a pointer previously returned by [`allocate`](LocalBlinkAlloc::allocate)."] # [doc = " `old_size` must be in range `layout.size()..=slice.len()`"] # [doc = " where `layout` is the layout used in the call to [`allocate`](LocalBlinkAlloc::allocate)."] # [doc = " and `slice` is the slice pointer returned by [`allocate`](LocalBlinkAlloc::allocate)."] # [doc = ""] # [doc = " On success, the old pointer is invalidated and the new pointer is returned."] # [doc = " On error old allocation is still valid."] # [inline (always)] pub unsafe fn resize (& self , ptr : NonNull < u8 > , old_layout : Layout , new_layout : Layout ,) -> Result < NonNull < [u8] > , AllocError > { if let Some (ptr) = unsafe { self . arena . resize_fast (ptr , old_layout , new_layout) } { return Ok (ptr) ; } unsafe { self . arena . resize_slow (ptr , old_layout , new_layout , self . shared) } } # [doc = " Deallocates memory previously allocated from this allocator."] # [doc = ""] # [doc = " This call may not actually free memory."] # [doc = " All memory is guaranteed to be freed on [`reset`](LocalBlinkAlloc::reset) call."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `ptr` must be a pointer previously returned by [`allocate`](LocalBlinkAlloc::allocate)."] # [doc = " `size` must be in range `layout.size()..=slice.len()`"] # [doc = " where `layout` is the layout used in the call to [`allocate`](LocalBlinkAlloc::allocate)."] # [doc = " and `slice` is the slice pointer returned by [`allocate`](LocalBlinkAlloc::allocate)."] # [inline (always)] pub unsafe fn deallocate (& self , ptr : NonNull < u8 > , size : usize) { unsafe { self . arena . dealloc (ptr , size) ; } } # [doc = " Resets this allocator, deallocating all chunks except the last one."] # [doc = " Last chunk will be reused."] # [doc = " With steady memory usage after few iterations"] # [doc = " one chunk should be sufficient for all allocations between resets."] # [inline (always)] pub fn reset (& mut self) { self . shared . update_max_local_alloc (self . arena . last_chunk_size ()) ; self . arena . reset_leak (true) ; } # [doc = " Resets this allocator, deallocating all chunks except the last one."] # [doc = " Last chunk will be reused."] # [doc = " With steady memory usage after few iterations"] # [doc = " one chunk should be sufficient for all allocations between resets."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Blink-allocators guarantee that memory can be used while shared"] # [doc = " borrow to the allocator is held, preventing safe `fn reset` call."] # [doc = ""] # [doc = " With this method it becomes caller responsibility to ensure"] # [doc = " that allocated memory won't be used after reset."] # [inline (always)] pub unsafe fn reset_unchecked (& self) { unsafe { self . arena . reset_unchecked (true , self . shared) ; } } }
};
}
