// Generated macro for with_cursor (macro)
macro_rules! Depcrate_arenawith_cursor {
() => {
// Module: crate::arena
// Provides: {"with_cursor"}
// Dependencies: {}
macro_rules ! with_cursor { ($ cursor : ty) => { # [repr (C)] pub struct ChunkHeader { cursor : $ cursor , end : * mut u8 , prev : Option < NonNull < Self >>, cumulative_size : usize , } impl ChunkHeader { # [inline] unsafe fn alloc_chunk (size : usize , allocator : impl Allocator , prev : Option < NonNull < Self >>,) -> Result < NonNull < Self >, AllocError > { let Some (size) = align_up (size , align_of ::< Self > ()) else { return Err (AllocError) ; } ; let layout = unsafe { Layout :: from_size_align_unchecked (size , align_of ::< Self > ()) } ; let slice = allocator . allocate (layout) ?; Ok (Self :: init_chunk (slice , prev)) } # [inline] unsafe fn dealloc_chunk (chunk : NonNull < Self >, allocator : impl Allocator ,) -> Option < NonNull < Self >> { let me = unsafe { chunk . as_ref () } ; let prev = me . prev ; let size = unsafe { me . end . offset_from (chunk . as_ptr () . cast ()) } as usize ; let layout = unsafe { Layout :: from_size_align_unchecked (size , align_of ::< Self > ()) } ; allocator . deallocate (chunk . cast () , layout) ; prev } # [doc = " # Safety"] # [doc = ""] # [doc = " `ptr` must be a pointer to the valid chunk allocation."] # [doc = " `ptr` must be aligned for `ChunkHeader` structure."] # [doc = " `size` must be the size of the allocation."] # [doc = " `size` must be large enough to fit `Chunk` structure."] # [inline] unsafe fn init_chunk (slice : NonNull < [u8] >, prev : Option < NonNull < Self >>,) -> NonNull < Self > { let len = slice . len () ; let ptr = slice . as_ptr () . cast ::< u8 > () ; debug_assert ! (is_aligned_to (ptr as usize , align_of ::< Self > ())) ; debug_assert ! (len > size_of ::< Self > ()) ; let end = ptr . add (len) ; let header_ptr = ptr . cast ::< Self > () ; let base = header_ptr . add (1) . cast ::< u8 > () ; let cumulative_size = match prev { None => 0 , Some (prev) => { let prev = unsafe { prev . as_ref () } ; prev . cap () + prev . cumulative_size } } ; ptr :: write (header_ptr , ChunkHeader { cursor : <$ cursor >:: new (base) , end , prev , cumulative_size , } ,) ; NonNull :: new_unchecked (header_ptr) } # [inline (always)] fn base (& self) -> * const u8 { unsafe { <* const Self >:: add (self , 1) . cast () } } # [doc = " # Safety"] # [doc = ""] # [doc = " `ptr` must be a pointer withing the usable memory of the chunk."] # [doc = " e.g. it must be between `base` and `self`."] # [inline (always)] unsafe fn offset_from_end (& self , ptr : * const u8) -> usize { let offset = unsafe { self . end . offset_from (ptr) } ; offset as usize } # [inline (always)] fn cap (& self) -> usize { unsafe { self . offset_from_end (self . base ()) } } # [inline (always)] unsafe fn alloc (chunk : NonNull < Self >, layout : Layout) -> Option < NonNull < [u8] >> { let me = unsafe { chunk . as_ref () } ; let mut cursor = me . cursor . load (Ordering :: Relaxed) ; loop { let cursor_addr = cursor as usize ; let layout_sum = layout_sum (& layout) ; let unaligned = cursor_addr . checked_add (layout_sum) ?; let aligned_addr = align_down (unaligned - layout . size () , layout . align ()) ; debug_assert ! (aligned_addr >= cursor_addr , "aligned_addr addr must not be less than cursor") ; debug_assert ! ((aligned_addr - cursor_addr) < layout . align () , "Cannot waste space more than alignment size") ; let next_addr = aligned_addr + layout . size () ; let end_addr = me . end as usize ; if next_addr > end_addr { return None ; } let aligned = unsafe { cursor . add (aligned_addr - cursor_addr) } ; let next = unsafe { aligned . add (layout . size ()) } ; if let Err (updated) = me . cursor . compare_exchange_weak (cursor , next , Ordering :: Acquire , Ordering :: Relaxed ,) { cursor = updated ; continue ; } ; let len = next_addr - aligned_addr ; debug_assert ! (len >= layout . size ()) ; let slice = unsafe { debug_assert_eq ! (aligned_addr % layout . align () , 0) ; let slice = core :: ptr :: slice_from_raw_parts_mut (aligned , len) ; NonNull :: new_unchecked (slice) } ; return Some (slice) ; } } # [doc = " Optimistic resize for arena-allocated memory."] # [doc = " Handles grows, shrinks if new alignment requirement is not met - shifts."] # [doc = " When alignment requirement is already met (checked for pointer itself)"] # [doc = " shifts do not happen for both shrinks and grows."] # [doc = " Even more, cheap shrinks are always successful if alignment is met by `ptr`."] # [doc = " Cheap grows are successful if this is the last allocation in the chunk"] # [doc = " and there is enough space for the new allocation."] # [doc = " If cheap shrink or grow is not possible - reallocates."] # [doc = ""] # [doc = " Safety: `chunk` must be a pointer to the valid chunk allocation."] # [doc = " `ptr` must be a pointer to the allocated memory of at least `old_size` bytes."] # [doc = " `ptr` may be allocated from different chunk."] # [inline] unsafe fn resize (chunk : NonNull < Self >, ptr : NonNull < u8 >, old_layout : Layout , new_layout : Layout ,) -> Option < NonNull < [u8] >> { let me = unsafe { chunk . as_ref () } ; let addr = ptr . as_ptr () as usize ; if old_layout . align () >= new_layout . align () { if new_layout . size () <= old_layout . size () { let slice = core :: ptr :: slice_from_raw_parts_mut (ptr . as_ptr () , old_layout . size ()) ; return Some (NonNull :: new_unchecked (slice)) ; } else { let old_end = unsafe { ptr . as_ptr () . add (old_layout . size ()) } ; let cursor = me . cursor . load (Ordering :: Relaxed) ; if cursor == old_end { let next_addr = addr . checked_add (new_layout . size ()) ?; let end_addr = me . end as usize ; if next_addr > end_addr { return None ; } let next = unsafe { ptr . as_ptr () . add (new_layout . size ()) } ; let result = CasPtr :: compare_exchange (& me . cursor , cursor , next , Ordering :: Acquire , Ordering :: Relaxed ,) ; if let Ok (()) = result { let len = next_addr - addr ; debug_assert ! (len >= new_layout . size ()) ; let slice = core :: ptr :: slice_from_raw_parts_mut (ptr . as_ptr () , len) ; return Some (NonNull :: new_unchecked (slice)) ; } cold () ; } } } else { cold () ; } let new_ptr = ChunkHeader :: alloc (chunk , new_layout) ?; core :: ptr :: copy_nonoverlapping (ptr . as_ptr () , new_ptr . as_ptr () . cast () , new_layout . size () . min (old_layout . size ()) ,) ; Some (new_ptr) } # [inline (always)] unsafe fn reset (mut chunk : NonNull < Self >) -> Option < NonNull < Self >> { let me = chunk . as_mut () ; let base = me . end . sub (me . cap ()) ; me . cursor . set (base) ; me . cumulative_size = 0 ; me . prev . take () } # [inline (always)] unsafe fn dealloc (chunk : NonNull < Self >, ptr : NonNull < u8 >, size : usize) { let me = unsafe { chunk . as_ref () } ; let new = unsafe { ptr . as_ptr () . add (size) } ; let _ = me . cursor . compare_exchange (ptr . as_ptr () , new , Ordering :: Release , Ordering :: Relaxed ,) ; } } # [cold] pub unsafe fn alloc_slow (root : & Cell < Option < NonNull < ChunkHeader >>>, mut chunk_size : usize , layout : Layout , allocator : impl Allocator ,) -> Result < NonNull < [u8] >, AllocError > { if let Some (root) = root . get () { chunk_size = chunk_size . max (root . as_ref () . cumulative_size) ; chunk_size = chunk_size . checked_add (layout . size () . max (CHUNK_MIN_GROW_STEP)) . ok_or (AllocError) ?; } else { chunk_size = chunk_size . max (layout . size ()) ; } if layout . align () > align_of ::< ChunkHeader > () { chunk_size = chunk_size . checked_add (layout . align ()) . ok_or (AllocError) ?; } let Some (mut chunk_size) = chunk_size . checked_add (size_of ::< ChunkHeader > ()) else { return Err (AllocError) ; } ; if chunk_size < CHUNK_POWER_OF_TWO_THRESHOLD { chunk_size = chunk_size . next_power_of_two () ; } else { chunk_size = align_up (chunk_size , CHUNK_POWER_OF_TWO_THRESHOLD) . unwrap_or (chunk_size) ; } debug_assert_eq ! (chunk_size % align_of ::< ChunkHeader > () , 0) ; let new_chunk = ChunkHeader :: alloc_chunk (chunk_size , allocator , root . get ()) ?; let ptr = unsafe { ChunkHeader :: alloc (new_chunk , layout) . unwrap_unchecked () } ; root . set (Some (new_chunk)) ; Ok (ptr) } # [cold] pub unsafe fn resize_slow (root : & Cell < Option < NonNull < ChunkHeader >>>, chunk_size : usize , ptr : NonNull < u8 >, old_layout : Layout , new_layout : Layout , allocator : impl Allocator ,) -> Result < NonNull < [u8] >, AllocError > { let new_ptr = alloc_slow (root , chunk_size , new_layout , allocator) ?; core :: ptr :: copy_nonoverlapping (ptr . as_ptr () , new_ptr . as_ptr () . cast () , new_layout . size () . min (old_layout . size ()) ,) ; Ok (new_ptr) } # [inline (always)] pub unsafe fn dealloc (root : Option < NonNull < ChunkHeader >>, ptr : NonNull < u8 >, size : usize) { if let Some (root) = root { unsafe { ChunkHeader :: dealloc (root , ptr , size) ; } } } # [doc = " Safety:"] # [doc = " `allocator` must be the same allocator that was used in `alloc`."] # [inline (always)] pub unsafe fn reset < A > (root : & Cell < Option < NonNull < ChunkHeader >>>, keep_last : bool , allocator : A ,) where A : Allocator , { let mut prev = if keep_last { let Some (root) = root . get () else { return ; } ; unsafe { ChunkHeader :: reset (root) } } else { root . take () } ; while let Some (chunk) = prev { prev = unsafe { ChunkHeader :: dealloc_chunk (chunk , & allocator) } ; } } # [allow (dead_code)] # [inline (always)] pub fn reset_leak (root : & Cell < Option < NonNull < ChunkHeader >>>, keep_last : bool) { if keep_last { let Some (chunk) = root . get () else { return ; } ; unsafe { ChunkHeader :: reset (chunk) ; } } else { root . set (None) ; } ; } } ; }
};
}
