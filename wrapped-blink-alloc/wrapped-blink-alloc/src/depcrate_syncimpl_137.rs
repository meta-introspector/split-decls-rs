// Generated macro for impl_137 (impl)
macro_rules! Depcrate_syncimpl_137 {
() => {
// Module: crate::sync
// Provides: {"impl_137"}
// Dependencies: {}
impl < A > SyncBlinkAlloc < A > where A : Allocator , { # [doc = " Creates new blink allocator that uses provided allocator"] # [doc = " to allocate memory chunks."] # [doc = ""] # [doc = " See [`SyncBlinkAlloc::new`] for using global allocator."] # [inline (always)] pub const fn new_in (allocator : A) -> Self { SyncBlinkAlloc { arena : ArenaSync :: new () , allocator , max_local_alloc : AtomicUsize :: new (0) , } } # [doc = " Returns reference to the underlying allocator used by this blink allocator."] # [inline (always)] pub const fn inner (& self) -> & A { & self . allocator } # [doc = " Creates new blink allocator that uses global allocator"] # [doc = " to allocate memory chunks."] # [doc = " With this method you can specify initial chunk size."] # [doc = ""] # [doc = " See [`SyncBlinkAlloc::new_in`] for using custom allocator."] # [inline (always)] pub const fn with_chunk_size_in (chunk_size : usize , allocator : A) -> Self { SyncBlinkAlloc { arena : ArenaSync :: with_chunk_size (chunk_size) , allocator , max_local_alloc : AtomicUsize :: new (0) , } } # [doc = " Creates a new thread-local blink allocator proxy"] # [doc = " that borrows from this multi-threaded allocator."] # [doc = ""] # [doc = " The local proxy allocator works faster and"] # [doc = " allows more consistent memory reuse."] # [doc = " It can be recreated without resetting the multi-threaded allocator,"] # [doc = " allowing [`SyncBlinkAlloc`] to be warm-up and serve all allocations"] # [doc = " from a single chunk without ever blocking."] # [doc = ""] # [doc = " Best works for fork-join style of parallelism."] # [doc = " Create a local allocator for each thread/task."] # [doc = " Reset after all threads/tasks are finished."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #![cfg_attr(feature = \"nightly\", feature(allocator_api))]"] # [doc = " # use blink_alloc::SyncBlinkAlloc;"] # [doc = " # use allocator_api2::vec::Vec;"] # [doc = " # #[cfg(feature = \"alloc\")] fn main() {"] # [doc = " let mut blink = SyncBlinkAlloc::new();"] # [doc = " for _ in 0..3 {"] # [doc = "     for i in 0..16 {"] # [doc = "         std::thread::scope(|_| {"] # [doc = "             let blink = blink.local();"] # [doc = "             let mut vec = Vec::new_in(&blink);"] # [doc = "             vec.push(i);"] # [doc = "             for j in i*2..i*30 {"] # [doc = "                 vec.push(j); // Proxy will allocate enough memory to grow vec without reallocating on 2nd iteration and later."] # [doc = "             }"] # [doc = "         });"] # [doc = "     }"] # [doc = "     blink.reset();"] # [doc = " }"] # [doc = " # }"] # [doc = " # #[cfg(not(feature = \"alloc\"))] fn main() {}"] # [doc = " ```"] # [inline (always)] pub fn local (& self) -> LocalBlinkAlloc < A > { LocalBlinkAlloc { arena : ArenaLocal :: with_chunk_size (self . max_local_alloc . load (Ordering :: Relaxed)) , shared : self , } } # [doc = " Allocates memory with specified layout from this allocator."] # [doc = " If needed it will allocate new chunk using underlying allocator."] # [doc = " If chunk allocation fails, it will return `Err`."] # [inline (always)] pub fn allocate (& self , layout : Layout) -> Result < NonNull < [u8] > , AllocError > { if let Some (ptr) = unsafe { self . arena . alloc_fast (layout) } { return Ok (ptr) ; } unsafe { self . arena . alloc_slow (layout , & self . allocator) } } # [doc = " Resizes memory allocation."] # [doc = " Potentially happens in-place."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `ptr` must be a pointer previously returned by [`allocate`](SyncBlinkAlloc::allocate)."] # [doc = " `old_size` must be in range `layout.size()..=slice.len()`"] # [doc = " where `layout` is the layout used in the call to [`allocate`](SyncBlinkAlloc::allocate)."] # [doc = " and `slice` is the slice pointer returned by [`allocate`](SyncBlinkAlloc::allocate)."] # [doc = ""] # [doc = " On success, the old pointer is invalidated and the new pointer is returned."] # [doc = " On error old allocation is still valid."] # [inline (always)] pub unsafe fn resize (& self , ptr : NonNull < u8 > , old_layout : Layout , new_layout : Layout ,) -> Result < NonNull < [u8] > , AllocError > { if let Some (ptr) = unsafe { self . arena . resize_fast (ptr , old_layout , new_layout) } { return Ok (ptr) ; } unsafe { self . arena . resize_slow (ptr , old_layout , new_layout , & self . allocator) } } # [doc = " Deallocates memory previously allocated from this allocator."] # [doc = ""] # [doc = " This call may not actually free memory."] # [doc = " All memory is guaranteed to be freed on [`reset`](SyncBlinkAlloc::reset) call."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `ptr` must be a pointer previously returned by [`allocate`](SyncBlinkAlloc::allocate)."] # [doc = " `size` must be in range `layout.size()..=slice.len()`"] # [doc = " where `layout` is the layout used in the call to [`allocate`](SyncBlinkAlloc::allocate)."] # [doc = " and `slice` is the slice pointer returned by [`allocate`](SyncBlinkAlloc::allocate)."] # [inline (always)] pub unsafe fn deallocate (& self , ptr : NonNull < u8 > , size : usize) { unsafe { self . arena . dealloc (ptr , size) ; } } # [doc = " Resets this allocator, deallocating all chunks except the last one."] # [doc = " Last chunk will be reused."] # [doc = " With steady memory usage after few iterations"] # [doc = " one chunk should be sufficient for all allocations between resets."] # [inline (always)] pub fn reset (& mut self) { unsafe { self . arena . reset (true , & self . allocator) ; } } # [doc = " Resets this allocator, deallocating all chunks."] # [inline (always)] pub fn reset_final (& mut self) { unsafe { self . arena . reset (false , & self . allocator) ; } } # [doc = " Resets this allocator, deallocating all chunks except the last one."] # [doc = " Last chunk will be reused."] # [doc = " With steady memory usage after few iterations"] # [doc = " one chunk should be sufficient for all allocations between resets."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Blink-allocators guarantee that memory can be used while shared"] # [doc = " borrow to the allocator is held, preventing safe `fn reset` call."] # [doc = ""] # [doc = " With this method it becomes caller responsibility to ensure"] # [doc = " that allocated memory won't be used after reset."] # [inline (always)] pub unsafe fn reset_unchecked (& self) { unsafe { self . arena . reset_unchecked (true , & self . allocator) ; } } # [doc = " Unwrap this allocator, returning the underlying allocator."] # [doc = " Leaks allocated chunks."] # [doc = ""] # [doc = " To deallocate all chunks call [`reset_final`](BlinkAlloc::reset_final) first."] # [doc = ""] # [doc = " The second returned value will use global allocator, so"] # [doc = " use with care if this method is used inside global allocator."] pub fn into_inner (self) -> A { let me = ManuallyDrop :: new (self) ; unsafe { core :: ptr :: read (& me . allocator) } } # [doc = " Update maximum local allocation size."] # [doc = " Can be used by thread-local blink-allocators that use"] # [doc = " this shared blink-allocator."] # [inline (always)] pub fn update_max_local_alloc (& self , max_local_alloc : usize) { self . max_local_alloc . fetch_max (max_local_alloc , Ordering :: Relaxed) ; } }
};
}
