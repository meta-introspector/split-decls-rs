// Generated macro for impl_123 (impl)
macro_rules! Depcrate_localimpl_123 {
() => {
// Module: crate::local
// Provides: {"impl_123"}
// Dependencies: {}
impl < A > BlinkAlloc < A > where A : Allocator , { # [doc = " Creates new blink allocator that uses provided allocator"] # [doc = " to allocate memory chunks."] # [doc = ""] # [doc = " See [`BlinkAlloc::new`] for using global allocator."] # [inline] pub const fn new_in (allocator : A) -> Self { BlinkAlloc { arena : ArenaLocal :: new () , allocator , } } # [doc = " Returns reference to the underlying allocator used by this blink allocator."] # [inline (always)] pub const fn inner (& self) -> & A { & self . allocator } # [doc = " Creates new blink allocator that uses global allocator"] # [doc = " to allocate memory chunks."] # [doc = " With this method you can specify initial chunk size."] # [doc = ""] # [doc = " See [`BlinkAlloc::new_in`] for using custom allocator."] # [inline] pub const fn with_chunk_size_in (chunk_size : usize , allocator : A) -> Self { BlinkAlloc { arena : ArenaLocal :: with_chunk_size (chunk_size) , allocator , } } # [doc = " Allocates memory with specified layout from this allocator."] # [doc = " If needed it will allocate new chunk using underlying allocator."] # [doc = " If chunk allocation fails, it will return `Err`."] # [inline (always)] pub fn allocate (& self , layout : Layout) -> Result < NonNull < [u8] > , AllocError > { if let Some (ptr) = unsafe { self . arena . alloc_fast (layout) } { return Ok (ptr) ; } unsafe { self . arena . alloc_slow (layout , & self . allocator) } } # [doc = " Resizes memory allocation."] # [doc = " Potentially happens in-place."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `ptr` must be a pointer previously returned by [`allocate`](BlinkAlloc::allocate)."] # [doc = " `old_size` must be in range `layout.size()..=slice.len()`"] # [doc = " where `layout` is the layout used in the call to [`allocate`](BlinkAlloc::allocate)."] # [doc = " and `slice` is the slice pointer returned by [`allocate`](BlinkAlloc::allocate)."] # [doc = ""] # [doc = " On success, the old pointer is invalidated and the new pointer is returned."] # [doc = " On error old allocation is still valid."] # [inline (always)] pub unsafe fn resize (& self , ptr : NonNull < u8 > , old_layout : Layout , new_layout : Layout ,) -> Result < NonNull < [u8] > , AllocError > { if let Some (ptr) = unsafe { self . arena . resize_fast (ptr , old_layout , new_layout) } { return Ok (ptr) ; } unsafe { self . arena . resize_slow (ptr , old_layout , new_layout , & self . allocator) } } # [doc = " Deallocates memory previously allocated from this allocator."] # [doc = ""] # [doc = " This call may not actually free memory."] # [doc = " All memory is guaranteed to be freed on [`reset`](BlinkAlloc::reset) call."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `ptr` must be a pointer previously returned by [`allocate`](BlinkAlloc::allocate)."] # [doc = " `size` must be in range `layout.size()..=slice.len()`"] # [doc = " where `layout` is the layout used in the call to [`allocate`](BlinkAlloc::allocate)."] # [doc = " and `slice` is the slice pointer returned by [`allocate`](BlinkAlloc::allocate)."] # [inline (always)] pub unsafe fn deallocate (& self , ptr : NonNull < u8 > , size : usize) { unsafe { self . arena . dealloc (ptr , size) ; } } # [doc = " Resets this allocator, deallocating all chunks except the last one."] # [doc = " Last chunk will be reused."] # [doc = " With steady memory usage after few iterations"] # [doc = " one chunk should be sufficient for all allocations between resets."] # [inline (always)] pub fn reset (& mut self) { unsafe { self . arena . reset (true , & self . allocator) ; } } # [doc = " Resets this allocator, deallocating all chunks."] # [inline (always)] pub fn reset_final (& mut self) { unsafe { self . arena . reset (false , & self . allocator) ; } } # [doc = " Resets this allocator, deallocating all chunks except the last one."] # [doc = " Last chunk will be reused."] # [doc = " With steady memory usage after few iterations"] # [doc = " one chunk should be sufficient for all allocations between resets."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Blink-allocators guarantee that memory can be used while shared"] # [doc = " borrow to the allocator is held, preventing safe `fn reset` call."] # [doc = ""] # [doc = " With this method it becomes caller responsibility to ensure"] # [doc = " that allocated memory won't be used after reset."] # [inline (always)] pub unsafe fn reset_unchecked (& self) { unsafe { self . arena . reset_unchecked (true , & self . allocator) ; } } # [doc = " Unwrap this allocator, returning the underlying allocator."] # [doc = " Leaks allocated chunks."] # [doc = ""] # [doc = " To deallocate all chunks call [`reset_final`](BlinkAlloc::reset_final) first."] pub fn into_inner (self) -> A { let me = ManuallyDrop :: new (self) ; unsafe { core :: ptr :: read (& me . allocator) } } }
};
}
