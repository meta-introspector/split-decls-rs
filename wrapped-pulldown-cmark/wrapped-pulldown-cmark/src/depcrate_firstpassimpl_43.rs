// Generated macro for impl_43 (impl)
macro_rules! Depcrate_firstpassimpl_43 {
() => {
// Module: crate::firstpass
// Provides: {"impl_43"}
// Dependencies: {}
impl < 'a , 'b > FirstPass < 'a , 'b > { fn run (mut self) -> (Tree < Item > , Allocations < 'a >) { let mut ix = 0 ; while ix < self . text . len () { ix = self . parse_block (ix) ; } while self . tree . spine_len () > 0 { self . pop (ix) ; } (self . tree , self . allocs) } # [doc = " Returns offset after block."] fn parse_block (& mut self , mut start_ix : usize) -> usize { let bytes = self . text . as_bytes () ; let mut line_start = LineStart :: new (& bytes [start_ix ..]) ; self . brace_context_stack . clear () ; self . brace_context_next = 0 ; let i = scan_containers (& self . tree , & mut line_start , self . options) ; for _ in i .. self . tree . spine_len () { self . pop (start_ix) ; } if self . options . contains (Options :: ENABLE_OLD_FOOTNOTES) { if let Some (node_ix) = self . tree . peek_up () { if let ItemBody :: FootnoteDefinition (..) = self . tree [node_ix] . item . body { if self . last_line_blank { self . pop (start_ix) ; } } } } loop { let save = line_start . clone () ; let outer_indent = line_start . scan_space_upto (4) ; if outer_indent >= 4 { line_start = save ; break ; } if self . options . contains (Options :: ENABLE_FOOTNOTES) { let container_start = start_ix + line_start . bytes_scanned () ; if let Some (bytecount) = self . parse_footnote (container_start) { start_ix = container_start + bytecount ; if self . options . contains (Options :: ENABLE_OLD_FOOTNOTES) { start_ix += scan_blank_line (& bytes [start_ix ..]) . unwrap_or (0) ; } line_start = LineStart :: new (& bytes [start_ix ..]) ; continue ; } } let container_start = start_ix + line_start . bytes_scanned () ; if let Some ((ch , index , indent)) = line_start . scan_list_marker_with_indent (outer_indent) { let after_marker_index = start_ix + line_start . bytes_scanned () ; self . continue_list (container_start - outer_indent , ch , index) ; self . tree . append (Item { start : container_start - outer_indent , end : after_marker_index , body : ItemBody :: ListItem (indent) , }) ; self . tree . push () ; if let Some (n) = scan_blank_line (& bytes [after_marker_index ..]) { self . begin_list_item = Some (after_marker_index + n) ; return after_marker_index + n ; } if self . options . contains (Options :: ENABLE_TASKLISTS) { let task_list_marker = line_start . scan_task_list_marker () . map (| is_checked | Item { start : after_marker_index , end : start_ix + line_start . bytes_scanned () , body : ItemBody :: TaskListMarker (is_checked) , }) ; if let Some (task_list_marker) = task_list_marker { if let Some (n) = scan_blank_line (& bytes [task_list_marker . end ..]) { self . tree . append (task_list_marker) ; self . begin_list_item = Some (task_list_marker . end + n) ; return task_list_marker . end + n ; } else { line_start . scan_all_space () ; let ix = start_ix + line_start . bytes_scanned () ; return self . parse_paragraph (ix , Some (task_list_marker)) ; } } } } else if let Some ((indent , child , item)) = self . options . contains (Options :: ENABLE_DEFINITION_LIST) . then (| | { self . tree . cur () . map (| cur | (self . tree [cur] . child , & mut self . tree [cur] . item)) }) . flatten () . filter (| (_ , item) | { matches ! (item , Item { body : ItemBody :: Paragraph | ItemBody :: TightParagraph | ItemBody :: MaybeDefinitionListTitle | ItemBody :: DefinitionListDefinition (_) , .. }) }) . and_then (| item | { Some ((line_start . scan_definition_list_definition_marker_with_indent (outer_indent) ? , item . 0 , item . 1 ,)) }) { match item . body { ItemBody :: Paragraph | ItemBody :: TightParagraph => { item . body = ItemBody :: DefinitionList (true) ; let Item { start , end , .. } = * item ; let list_idx = self . tree . cur () . unwrap () ; let title_idx = self . tree . create_node (Item { start , end , body : ItemBody :: DefinitionListTitle , }) ; self . tree [title_idx] . child = child ; self . tree [list_idx] . child = Some (title_idx) ; self . tree . push () ; } ItemBody :: MaybeDefinitionListTitle => { item . body = ItemBody :: DefinitionListTitle ; } ItemBody :: DefinitionListDefinition (_) => { } _ => unreachable ! () , } let after_marker_index = start_ix + line_start . bytes_scanned () ; self . tree . append (Item { start : container_start - outer_indent , end : after_marker_index , body : ItemBody :: DefinitionListDefinition (indent) , }) ; if let Some (ItemBody :: DefinitionList (ref mut is_tight)) = self . tree . peek_up () . map (| cur | & mut self . tree [cur] . item . body) { if self . last_line_blank { * is_tight = false ; self . last_line_blank = false ; } } self . tree . push () ; if let Some (n) = scan_blank_line (& bytes [after_marker_index ..]) { self . begin_list_item = Some (after_marker_index + n) ; return after_marker_index + n ; } } else if line_start . scan_blockquote_marker () { let kind = if self . options . contains (Options :: ENABLE_GFM) { line_start . scan_blockquote_tag () } else { None } ; self . finish_list (start_ix) ; self . tree . append (Item { start : container_start , end : 0 , body : ItemBody :: BlockQuote (kind) , }) ; self . tree . push () ; if kind . is_some () { let ix = start_ix + line_start . bytes_scanned () ; let mut lazy_line_start = LineStart :: new (& bytes [ix ..]) ; let tree_position = scan_containers (& self . tree , & mut lazy_line_start , self . options) ; let current_container = tree_position == self . tree . spine_len () ; if ! lazy_line_start . scan_space (4) && self . scan_paragraph_interrupt (& bytes [ix + lazy_line_start . bytes_scanned () ..] , current_container , tree_position ,) { return ix ; } else { line_start = lazy_line_start ; line_start . scan_all_space () ; start_ix = ix ; break ; } } } else if self . options . contains (Options :: ENABLE_CONTAINER_EXTENSIONS) && scan_ch_repeat (& bytes [(start_ix + line_start . bytes_scanned ()) ..] , b':') > 2 { let fence_length = scan_while_max (& bytes [(start_ix + line_start . bytes_scanned ()) ..] , | c | c == b':' , u8 :: MAX as usize ,) ; if fence_length > u8 :: MAX as usize || self . tree . spine_len () > u8 :: MAX as usize { break ; } else { let excess_colons = scan_while (& bytes [(start_ix + line_start . bytes_scanned () + fence_length) ..] , | c | c == b':' ,) ; let mut kind_start = start_ix + line_start . bytes_scanned () + fence_length + excess_colons ; kind_start += scan_whitespace_no_nl (& bytes [kind_start ..]) ; let kind_length = scan_while (& bytes [kind_start ..] , | c | { is_ascii_alphanumeric (c) || c == b'_' || c == b'-' || c == b':' || c == b'.' }) ; if kind_length == 0 { break ; } else { let kind = unescape (& self . text [kind_start .. (kind_start + kind_length)] , self . tree . is_in_table () ,) ; let mut summary_start = kind_start + kind_length ; summary_start += scan_whitespace_no_nl (& bytes [summary_start ..]) ; let line_end = summary_start + scan_nextline (& bytes [summary_start ..]) ; let summary_end = line_end - scan_rev_while (& bytes [summary_start .. line_end] , is_ascii_whitespace) ; let summary = unescape (& self . text [summary_start .. summary_end] , self . tree . is_in_table () ,) ; let summary_cow_ix = self . allocs . allocate_cow (summary) ; if kind . eq_ignore_ascii_case ("spoiler") { self . tree . append (Item { start : container_start , end : 0 , body : ItemBody :: Container (fence_length as u8 , ContainerKind :: Spoiler , summary_cow_ix ,) , }) ; } else { let kind_cow_ix = self . allocs . allocate_cow (kind) ; self . tree . append (Item { start : container_start , end : 0 , body : ItemBody :: Container (fence_length as u8 , ContainerKind :: Default , kind_cow_ix ,) , }) ; } self . tree . push () ; return summary_end + 1 ; } } } else { line_start = save ; break ; } } if self . options . contains (Options :: ENABLE_CONTAINER_EXTENSIONS) { let mut pop_count = None ; for (i , & node_ix) in self . tree . walk_spine () . rev () . enumerate () { match self . tree [node_ix] . item . body { ItemBody :: Container (length , ..) => { if line_start . scan_closing_container_extensions_fence (length) { pop_count = Some (i + 1) ; break ; } } _ => { break ; } } } if let Some (c) = pop_count { for _ in 0 .. c { self . pop (start_ix) ; } } } let ix = start_ix + line_start . bytes_scanned () ; if let Some (n) = scan_blank_line (& bytes [ix ..]) { if let Some (node_ix) = self . tree . peek_up () { match & mut self . tree [node_ix] . item . body { ItemBody :: Container (..) => () , ItemBody :: BlockQuote (..) => () , ItemBody :: ListItem (indent) | ItemBody :: DefinitionListDefinition (indent) if self . begin_list_item . is_some () => { self . last_line_blank = true ; * indent = 0 ; } _ => { self . last_line_blank = true ; } } } else { self . last_line_blank = true ; } return ix + n ; } let remaining_space = line_start . remaining_space () ; let indent = line_start . scan_space_upto (4) ; if indent == 4 { self . finish_list (start_ix) ; let ix = start_ix + line_start . bytes_scanned () ; let remaining_space = line_start . remaining_space () ; return self . parse_indented_code_block (ix , remaining_space) ; } let ix = start_ix + line_start . bytes_scanned () ; if indent == 0 { if let Some ((_n , metadata_block_ch)) = scan_metadata_block (& bytes [ix ..] , self . options . contains (Options :: ENABLE_YAML_STYLE_METADATA_BLOCKS) , self . options . contains (Options :: ENABLE_PLUSES_DELIMITED_METADATA_BLOCKS) ,) { self . finish_list (start_ix) ; return self . parse_metadata_block (ix , metadata_block_ch) ; } } if bytes [ix] == b'<' { if let Some (html_end_tag) = get_html_end_tag (& bytes [(ix + 1) ..]) { self . finish_list (start_ix) ; return self . parse_html_block_type_1_to_5 (ix , html_end_tag , remaining_space , indent ,) ; } if starts_html_block_type_6 (& bytes [(ix + 1) ..]) { self . finish_list (start_ix) ; return self . parse_html_block_type_6_or_7 (ix , remaining_space , indent) ; } if let Some (_html_bytes) = scan_html_type_7 (& bytes [ix ..]) { self . finish_list (start_ix) ; return self . parse_html_block_type_6_or_7 (ix , remaining_space , indent) ; } } if let Ok (n) = scan_hrule (& bytes [ix ..]) { self . finish_list (start_ix) ; return self . parse_hrule (n , ix) ; } if let Some (atx_size) = scan_atx_heading (& bytes [ix ..]) { self . finish_list (start_ix) ; return self . parse_atx_heading (ix , atx_size) ; } if let Some ((n , fence_ch)) = scan_code_fence (& bytes [ix ..]) { self . finish_list (start_ix) ; return self . parse_fenced_code_block (ix , indent , fence_ch , n) ; } while let Some ((bytecount , label , link_def)) = self . parse_refdef_total (start_ix + line_start . bytes_scanned ()) { self . allocs . refdefs . 0 . entry (label) . or_insert (link_def) ; let container_start = start_ix + line_start . bytes_scanned () ; let mut ix = container_start + bytecount ; if let Some (nl) = scan_blank_line (& bytes [ix ..]) { ix += nl ; } else { self . finish_list (start_ix) ; return ix ; } if let Some (lazy_line_start) = self . scan_next_line_or_lazy_continuation (& bytes [ix ..]) { line_start = lazy_line_start ; start_ix = ix ; } else { self . finish_list (start_ix) ; return ix ; } } let ix = start_ix + line_start . bytes_scanned () ; self . parse_paragraph (ix , None) } # [doc = " footnote definitions and GFM quote markers can be \"interrupted\""] # [doc = " like paragraphs, but otherwise can't have other blocks after them."] # [doc = ""] # [doc = " Call this at the end of the line to parse that. If it succeeeds,"] # [doc = " this returns the LineStart for the new line."] fn scan_next_line_or_lazy_continuation < 'input > (& mut self , bytes : & 'input [u8] ,) -> Option < LineStart < 'input > > { let mut line_start = LineStart :: new (bytes) ; let tree_position = scan_containers (& self . tree , & mut line_start , self . options) ; let current_container = tree_position == self . tree . spine_len () ; if (! line_start . scan_space (4) && self . scan_paragraph_interrupt (& bytes [line_start . bytes_scanned () ..] , current_container , tree_position ,)) || scan_blank_line (& bytes [line_start . bytes_scanned () ..]) . is_some () { None } else { line_start . scan_all_space () ; Some (line_start) } } # [doc = " Returns the offset of the first line after the table."] # [doc = " Assumptions: current focus is a table element and the table header"] # [doc = " matches the separator line (same number of columns)."] fn parse_table (& mut self , table_cols : usize , head_start : usize , body_start : usize ,) -> Option < usize > { let mut missing_empty_cells = 0 ; let (_sep_start , thead_ix) = self . parse_table_row_inner (head_start , table_cols , & mut missing_empty_cells) ? ; self . tree [thead_ix] . item . body = ItemBody :: TableHead ; let mut ix = body_start ; while let Some ((next_ix , _row_ix)) = self . parse_table_row (ix , table_cols , & mut missing_empty_cells) { ix = next_ix ; } self . pop (ix) ; Some (ix) } # [doc = " Call this when containers are taken care of."] # [doc = " Returns bytes scanned, row_ix"] fn parse_table_row_inner (& mut self , mut ix : usize , row_cells : usize , missing_empty_cells : & mut usize ,) -> Option < (usize , TreeIndex) > { const MAX_AUTOCOMPLETED_CELLS : usize = 1 << 18 ; let bytes = self . text . as_bytes () ; let mut cells = 0 ; let mut final_cell_ix = None ; let old_cur = self . tree . cur () ; let row_ix = self . tree . append (Item { start : ix , end : 0 , body : ItemBody :: TableRow , }) ; self . tree . push () ; loop { ix += scan_ch (& bytes [ix ..] , b'|') ; let start_ix = ix ; ix += scan_whitespace_no_nl (& bytes [ix ..]) ; if let Some (eol_bytes) = scan_eol (& bytes [ix ..]) { ix += eol_bytes ; break ; } let cell_ix = self . tree . append (Item { start : start_ix , end : ix , body : ItemBody :: TableCell , }) ; self . tree . push () ; let (next_ix , _brk) = self . parse_line (ix , None , TableParseMode :: Active) ; self . tree [cell_ix] . item . end = next_ix ; self . tree . pop () ; ix = next_ix ; cells += 1 ; if cells == row_cells { final_cell_ix = Some (cell_ix) ; } } if let (Some (cur) , 0) = (old_cur , cells) { self . pop (ix) ; self . tree [cur] . next = None ; return None ; } for _ in cells .. row_cells { if * missing_empty_cells >= MAX_AUTOCOMPLETED_CELLS { return None ; } * missing_empty_cells += 1 ; self . tree . append (Item { start : ix , end : ix , body : ItemBody :: TableCell , }) ; } if let Some (cell_ix) = final_cell_ix { self . tree [cell_ix] . next = None ; } self . pop (ix) ; Some ((ix , row_ix)) } # [doc = " Returns first offset after the row and the tree index of the row."] fn parse_table_row (& mut self , mut ix : usize , row_cells : usize , missing_empty_cells : & mut usize ,) -> Option < (usize , TreeIndex) > { let bytes = self . text . as_bytes () ; let mut line_start = LineStart :: new (& bytes [ix ..]) ; let tree_position = scan_containers (& self . tree , & mut line_start , self . options) ; let current_container = tree_position == self . tree . spine_len () ; if ! current_container { return None ; } line_start . scan_all_space () ; ix += line_start . bytes_scanned () ; if scan_paragraph_interrupt_no_table (& bytes [ix ..] , current_container , self . options . contains (Options :: ENABLE_FOOTNOTES) , self . options . contains (Options :: ENABLE_DEFINITION_LIST) , & self . tree , tree_position ,) { return None ; } let (ix , row_ix) = self . parse_table_row_inner (ix , row_cells , missing_empty_cells) ? ; Some ((ix , row_ix)) } # [doc = " Returns offset of line start after paragraph."] fn parse_paragraph (& mut self , start_ix : usize , tasklist_marker : Option < Item >) -> usize { let body = if let Some (ItemBody :: DefinitionList (_)) = self . tree . peek_up () . map (| idx | self . tree [idx] . item . body) { if self . tree . cur () . map_or (true , | idx | { matches ! (& self . tree [idx] . item . body , ItemBody :: DefinitionListDefinition (..)) }) { self . last_line_blank = false ; ItemBody :: MaybeDefinitionListTitle } else { self . finish_list (start_ix) ; ItemBody :: Paragraph } } else { self . finish_list (start_ix) ; ItemBody :: Paragraph } ; let node_ix = self . tree . append (Item { start : start_ix , end : 0 , body , }) ; self . tree . push () ; if let Some (item) = tasklist_marker { self . tree . append (item) ; } let bytes = self . text . as_bytes () ; let mut ix = start_ix ; loop { let scan_mode = if self . options . contains (Options :: ENABLE_TABLES) && ix == start_ix { TableParseMode :: Scan } else { TableParseMode :: Disabled } ; let (next_ix , brk) = self . parse_line (ix , None , scan_mode) ; if let Some (Item { body : ItemBody :: Table (alignment_ix) , .. }) = brk { let table_cols = self . allocs [alignment_ix] . len () ; self . tree [node_ix] . item . body = ItemBody :: Table (alignment_ix) ; self . tree [node_ix] . child = None ; self . tree . pop () ; if body == ItemBody :: MaybeDefinitionListTitle { self . finish_list (ix) ; } self . tree . push () ; if let Some (ix) = self . parse_table (table_cols , ix , next_ix) { return ix ; } } ix = next_ix ; let mut line_start = LineStart :: new (& bytes [ix ..]) ; let tree_position = scan_containers (& self . tree , & mut line_start , self . options) ; let current_container = tree_position == self . tree . spine_len () ; let trailing_backslash_pos = match brk { Some (Item { start , body : ItemBody :: HardBreak (true) , .. }) if bytes [start] == b'\\' => Some (start) , _ => None , } ; if ! line_start . scan_space (4) { let ix_new = ix + line_start . bytes_scanned () ; if current_container { if let Some (ix_setext) = self . parse_setext_heading (ix_new , node_ix , trailing_backslash_pos . is_some ()) { if let Some (pos) = trailing_backslash_pos { self . tree . append_text (pos , pos + 1 , false) ; } self . pop (ix_setext) ; if body == ItemBody :: MaybeDefinitionListTitle { self . finish_list (ix) ; } return ix_setext ; } } let suffix = & bytes [ix_new ..] ; if self . scan_paragraph_interrupt (suffix , current_container , tree_position) { if let Some (pos) = trailing_backslash_pos { self . tree . append_text (pos , pos + 1 , false) ; } break ; } if self . options . contains (Options :: ENABLE_CONTAINER_EXTENSIONS) && ! current_container { if line_start . scan_closing_container_extensions_fence (3) { break ; } } } line_start . scan_all_space () ; if line_start . is_at_eol () { if let Some (pos) = trailing_backslash_pos { self . tree . append_text (pos , pos + 1 , false) ; } break ; } if self . options . contains (Options :: ENABLE_CONTAINER_EXTENSIONS) { let mut closes = false ; for & node_ix in self . tree . walk_spine () . rev () . skip (1) { match self . tree [node_ix] . item . body { ItemBody :: Container (length , ..) => { if line_start . scan_closing_container_extensions_fence (length) { closes = true ; break ; } } _ => { break ; } } } if closes { break ; } } ix = next_ix + line_start . bytes_scanned () ; if let Some (item) = brk { self . tree . append (item) ; } } self . pop (ix) ; ix } # [doc = " Returns end ix of setext_heading on success."] fn parse_setext_heading (& mut self , ix : usize , node_ix : TreeIndex , has_trailing_content : bool ,) -> Option < usize > { let bytes = self . text . as_bytes () ; let (n , level) = scan_setext_heading (& bytes [ix ..]) ? ; let mut attrs = None ; if let Some (cur_ix) = self . tree . cur () { let parent_ix = self . tree . peek_up () . unwrap () ; let header_start = self . tree [parent_ix] . item . start ; let header_end = self . tree [cur_ix] . item . end ; let (content_end , attrs_) = self . extract_and_parse_heading_attribute_block (header_start , header_end) ; attrs = attrs_ ; let new_end = if has_trailing_content { content_end } else { let mut last_line_start = header_start ; if attrs . is_some () { loop { let next_line_start = last_line_start + scan_nextline (& bytes [last_line_start .. content_end]) ; if next_line_start >= content_end { break ; } let mut line_start = LineStart :: new (& bytes [next_line_start .. content_end]) ; if scan_containers (& self . tree , & mut line_start , self . options) != self . tree . spine_len () { break ; } last_line_start = next_line_start + line_start . bytes_scanned () ; } } let trailing_ws = scan_rev_while (& bytes [last_line_start .. content_end] , is_ascii_whitespace_no_nl ,) ; content_end - trailing_ws } ; if attrs . is_some () { self . tree . truncate_siblings (new_end) ; } if let Some (cur_ix) = self . tree . cur () { self . tree [cur_ix] . item . end = new_end ; } } self . tree [node_ix] . item . body = ItemBody :: Heading (level , attrs . map (| attrs | self . allocs . allocate_heading (attrs)) ,) ; Some (ix + n) } # [doc = " Parse a line of input, appending text and items to tree."] # [doc = ""] # [doc = " Returns: index after line and an item representing the break."] fn parse_line (& mut self , start : usize , end : Option < usize > , mode : TableParseMode ,) -> (usize , Option < Item >) { let bytes = self . text . as_bytes () ; let bytes = match end { Some (end) => & bytes [.. end] , None => bytes , } ; let bytes_len = bytes . len () ; let mut pipes = 0 ; let mut last_pipe_ix = start ; let mut begin_text = start ; let mut backslash_escaped = false ; let (final_ix , brk) = iterate_special_bytes (self . lookup_table , bytes , start , | ix , byte | { match byte { b'\n' | b'\r' => { if let TableParseMode :: Active = mode { return LoopInstruction :: BreakAtWith (ix , None) ; } let mut i = ix ; let eol_bytes = scan_eol (& bytes [ix ..]) . unwrap () ; let end_ix = ix + eol_bytes ; let trailing_backslashes = scan_rev_while (& bytes [.. ix] , | b | b == b'\\') ; if trailing_backslashes % 2 == 1 && end_ix < bytes_len { i -= 1 ; self . tree . append_text (begin_text , i , backslash_escaped) ; backslash_escaped = false ; return LoopInstruction :: BreakAtWith (end_ix , Some (Item { start : i , end : end_ix , body : ItemBody :: HardBreak (true) , }) ,) ; } if mode == TableParseMode :: Scan && pipes > 0 { let next_line_ix = ix + eol_bytes ; let mut line_start = LineStart :: new (& bytes [next_line_ix ..]) ; if scan_containers (& self . tree , & mut line_start , self . options) == self . tree . spine_len () { let table_head_ix = next_line_ix + line_start . bytes_scanned () ; let (table_head_bytes , alignment) = scan_table_head (& bytes [table_head_ix ..]) ; if table_head_bytes > 0 { let header_count = count_header_cols (bytes , pipes , start , last_pipe_ix) ; if alignment . len () == header_count { let alignment_ix = self . allocs . allocate_alignment (alignment) ; let end_ix = table_head_ix + table_head_bytes ; return LoopInstruction :: BreakAtWith (end_ix , Some (Item { start : i , end : end_ix , body : ItemBody :: Table (alignment_ix) , }) ,) ; } } } } let trailing_whitespace = scan_rev_while (& bytes [.. ix] , is_ascii_whitespace_no_nl) ; if trailing_whitespace >= 2 { i -= trailing_whitespace ; self . tree . append_text (begin_text , i , backslash_escaped) ; backslash_escaped = false ; return LoopInstruction :: BreakAtWith (end_ix , Some (Item { start : i , end : end_ix , body : ItemBody :: HardBreak (false) , }) ,) ; } self . tree . append_text (begin_text , ix - trailing_whitespace , backslash_escaped) ; backslash_escaped = false ; LoopInstruction :: BreakAtWith (end_ix , Some (Item { start : i , end : end_ix , body : ItemBody :: SoftBreak , }) ,) } b'\\' if bytes . get (ix + 1) . copied () . map_or (false , is_ascii_punctuation) => { self . tree . append_text (begin_text , ix , backslash_escaped) ; if bytes [ix + 1] == b'`' { let count = 1 + scan_ch_repeat (& bytes [(ix + 2) ..] , b'`') ; self . tree . append (Item { start : ix + 1 , end : ix + count + 1 , body : ItemBody :: MaybeCode (count , true) , }) ; begin_text = ix + 1 + count ; backslash_escaped = false ; LoopInstruction :: ContinueAndSkip (count) } else if bytes [ix + 1] == b'|' && TableParseMode :: Active == mode { begin_text = ix + 1 ; backslash_escaped = false ; LoopInstruction :: ContinueAndSkip (1) } else if ix + 2 < bytes_len && bytes [ix + 1] == b'\\' && bytes [ix + 2] == b'|' && TableParseMode :: Active == mode { begin_text = ix + 2 ; backslash_escaped = true ; LoopInstruction :: ContinueAndSkip (2) } else { begin_text = ix + 1 ; backslash_escaped = true ; LoopInstruction :: ContinueAndSkip (1) } } c @ b'*' | c @ b'_' | c @ b'~' | c @ b'^' => { let string_suffix = & self . text [ix ..] ; let count = 1 + scan_ch_repeat (& string_suffix . as_bytes () [1 ..] , c) ; let can_open = delim_run_can_open (& self . text [start ..] , string_suffix , count , ix - start , mode , self . options ,) ; let can_close = delim_run_can_close (& self . text [start ..] , string_suffix , count , ix - start , mode , self . options ,) ; let is_valid_seq = (c != b'~' || count <= 2) || (c == b'~' && count == 2) ; if (can_open || can_close) && is_valid_seq { self . tree . append_text (begin_text , ix , backslash_escaped) ; backslash_escaped = false ; for i in 0 .. count { self . tree . append (Item { start : ix + i , end : ix + i + 1 , body : ItemBody :: MaybeEmphasis (count - i , can_open , can_close) , }) ; } begin_text = ix + count ; } LoopInstruction :: ContinueAndSkip (count - 1) } b'$' => { let byte_suffix = & bytes [ix ..] ; let can_open = ! byte_suffix [1 ..] . first () . copied () . map_or (true , is_ascii_whitespace) ; let can_close = ix > start && ! bytes [.. ix] . last () . copied () . map_or (true , is_ascii_whitespace) ; let brace_context = if self . brace_context_stack . len () > MATH_BRACE_CONTEXT_MAX_NESTING { self . brace_context_next as u8 } else { self . brace_context_stack . last () . copied () . unwrap_or_else (| | { self . brace_context_stack . push (! 0) ; ! 0 }) } ; self . tree . append_text (begin_text , ix , backslash_escaped) ; self . tree . append (Item { start : ix , end : ix + 1 , body : ItemBody :: MaybeMath (can_open , can_close , brace_context) , }) ; begin_text = ix + 1 ; LoopInstruction :: ContinueAndSkip (0) } b'{' => { if self . brace_context_stack . len () == MATH_BRACE_CONTEXT_MAX_NESTING { self . brace_context_stack . push (self . brace_context_next as u8) ; self . brace_context_next = MATH_BRACE_CONTEXT_MAX_NESTING ; } else if self . brace_context_stack . len () > MATH_BRACE_CONTEXT_MAX_NESTING { self . brace_context_next += 1 ; } else if ! self . brace_context_stack . is_empty () { self . brace_context_stack . push (self . brace_context_next as u8) ; self . brace_context_next += 1 ; } LoopInstruction :: ContinueAndSkip (0) } b'}' => { if let & mut [ref mut top_level_context] = & mut self . brace_context_stack [..] { * top_level_context = top_level_context . wrapping_sub (1) ; } else if self . brace_context_stack . len () > MATH_BRACE_CONTEXT_MAX_NESTING { if self . brace_context_next <= MATH_BRACE_CONTEXT_MAX_NESTING { self . brace_context_stack . pop () ; } else { self . brace_context_next -= 1 ; } } else { self . brace_context_stack . pop () ; } LoopInstruction :: ContinueAndSkip (0) } b'`' => { self . tree . append_text (begin_text , ix , backslash_escaped) ; backslash_escaped = false ; let count = 1 + scan_ch_repeat (& bytes [(ix + 1) ..] , b'`') ; self . tree . append (Item { start : ix , end : ix + count , body : ItemBody :: MaybeCode (count , false) , }) ; begin_text = ix + count ; LoopInstruction :: ContinueAndSkip (count - 1) } b'<' if bytes . get (ix + 1) != Some (& b'\\') => { self . tree . append_text (begin_text , ix , backslash_escaped) ; backslash_escaped = false ; self . tree . append (Item { start : ix , end : ix + 1 , body : ItemBody :: MaybeHtml , }) ; begin_text = ix + 1 ; LoopInstruction :: ContinueAndSkip (0) } b'!' if bytes . get (ix + 1) == Some (& b'[') => { self . tree . append_text (begin_text , ix , backslash_escaped) ; backslash_escaped = false ; self . tree . append (Item { start : ix , end : ix + 2 , body : ItemBody :: MaybeImage , }) ; begin_text = ix + 2 ; LoopInstruction :: ContinueAndSkip (1) } b'[' => { self . tree . append_text (begin_text , ix , backslash_escaped) ; backslash_escaped = false ; self . tree . append (Item { start : ix , end : ix + 1 , body : ItemBody :: MaybeLinkOpen , }) ; begin_text = ix + 1 ; LoopInstruction :: ContinueAndSkip (0) } b']' => { self . tree . append_text (begin_text , ix , backslash_escaped) ; backslash_escaped = false ; self . tree . append (Item { start : ix , end : ix + 1 , body : ItemBody :: MaybeLinkClose (true) , }) ; begin_text = ix + 1 ; LoopInstruction :: ContinueAndSkip (0) } b'&' => match scan_entity (& bytes [ix ..]) { (n , Some (value)) => { self . tree . append_text (begin_text , ix , backslash_escaped) ; backslash_escaped = false ; self . tree . append (Item { start : ix , end : ix + n , body : ItemBody :: SynthesizeText (self . allocs . allocate_cow (value)) , }) ; begin_text = ix + n ; LoopInstruction :: ContinueAndSkip (n - 1) } _ => LoopInstruction :: ContinueAndSkip (0) , } , b'|' => { if ix != 0 && bytes [ix - 1] == b'\\' { LoopInstruction :: ContinueAndSkip (0) } else if let TableParseMode :: Active = mode { LoopInstruction :: BreakAtWith (ix , None) } else { last_pipe_ix = ix ; pipes += 1 ; LoopInstruction :: ContinueAndSkip (0) } } b'.' if matches ! (bytes . get (ix + 1 ..) , Some (& [b'.' , b'.' , ..])) => { self . tree . append_text (begin_text , ix , backslash_escaped) ; backslash_escaped = false ; self . tree . append (Item { start : ix , end : ix + 3 , body : ItemBody :: SynthesizeChar ('…') , }) ; begin_text = ix + 3 ; LoopInstruction :: ContinueAndSkip (2) } b'-' => { let count = 1 + scan_ch_repeat (& bytes [(ix + 1) ..] , b'-') ; if count == 1 { LoopInstruction :: ContinueAndSkip (0) } else { let itembody = if count == 2 { ItemBody :: SynthesizeChar ('–') } else if count == 3 { ItemBody :: SynthesizeChar ('—') } else { let (ems , ens) = match count % 6 { 0 | 3 => (count / 3 , 0) , 2 | 4 => (0 , count / 2) , 1 => (count / 3 - 1 , 2) , _ => (count / 3 , 1) , } ; let mut buf = String :: with_capacity (3 * (ems + ens)) ; for _ in 0 .. ems { buf . push ('—') ; } for _ in 0 .. ens { buf . push ('–') ; } ItemBody :: SynthesizeText (self . allocs . allocate_cow (buf . into ())) } ; self . tree . append_text (begin_text , ix , backslash_escaped) ; backslash_escaped = false ; self . tree . append (Item { start : ix , end : ix + count , body : itembody , }) ; begin_text = ix + count ; LoopInstruction :: ContinueAndSkip (count - 1) } } c @ b'\'' | c @ b'"' => { let string_suffix = & self . text [ix ..] ; let can_open = delim_run_can_open (& self . text [start ..] , string_suffix , 1 , ix - start , mode , self . options ,) ; let can_close = delim_run_can_close (& self . text [start ..] , string_suffix , 1 , ix - start , mode , self . options ,) ; self . tree . append_text (begin_text , ix , backslash_escaped) ; backslash_escaped = false ; self . tree . append (Item { start : ix , end : ix + 1 , body : ItemBody :: MaybeSmartQuote (c , can_open , can_close) , }) ; begin_text = ix + 1 ; LoopInstruction :: ContinueAndSkip (0) } _ => LoopInstruction :: ContinueAndSkip (0) , } }) ; if brk . is_none () { let trailing_whitespace = scan_rev_while (& bytes [begin_text .. final_ix] , is_ascii_whitespace_no_nl) ; self . tree . append_text (begin_text , final_ix - trailing_whitespace , backslash_escaped ,) ; } (final_ix , brk) } # [doc = " When start_ix is at the beginning of an HTML block of type 1 to 5,"] # [doc = " this will find the end of the block, adding the block itself to the"] # [doc = " tree and also keeping track of the lines of HTML within the block."] # [doc = ""] # [doc = " The html_end_tag is the tag that must be found on a line to end the block."] fn parse_html_block_type_1_to_5 (& mut self , start_ix : usize , html_end_tag : & str , mut remaining_space : usize , mut indent : usize ,) -> usize { self . tree . append (Item { start : start_ix , end : 0 , body : ItemBody :: HtmlBlock , }) ; self . tree . push () ; let bytes = self . text . as_bytes () ; let mut ix = start_ix ; let end_ix ; loop { let line_start_ix = ix ; ix += scan_nextline (& bytes [ix ..]) ; self . append_html_line (remaining_space . max (indent) , line_start_ix , ix) ; let mut line_start = LineStart :: new (& bytes [ix ..]) ; let n_containers = scan_containers (& self . tree , & mut line_start , self . options) ; if n_containers < self . tree . spine_len () { end_ix = ix ; break ; } if self . text [line_start_ix .. ix] . contains (html_end_tag) { end_ix = ix ; break ; } let next_line_ix = ix + line_start . bytes_scanned () ; if next_line_ix == self . text . len () { end_ix = next_line_ix ; break ; } ix = next_line_ix ; remaining_space = line_start . remaining_space () ; indent = 0 ; } self . pop (end_ix) ; ix } # [doc = " When start_ix is at the beginning of an HTML block of type 6 or 7,"] # [doc = " this will consume lines until there is a blank line and keep track of"] # [doc = " the HTML within the block."] fn parse_html_block_type_6_or_7 (& mut self , start_ix : usize , mut remaining_space : usize , mut indent : usize ,) -> usize { self . tree . append (Item { start : start_ix , end : 0 , body : ItemBody :: HtmlBlock , }) ; self . tree . push () ; let bytes = self . text . as_bytes () ; let mut ix = start_ix ; let end_ix ; loop { let line_start_ix = ix ; ix += scan_nextline (& bytes [ix ..]) ; self . append_html_line (remaining_space . max (indent) , line_start_ix , ix) ; let mut line_start = LineStart :: new (& bytes [ix ..]) ; let n_containers = scan_containers (& self . tree , & mut line_start , self . options) ; if n_containers < self . tree . spine_len () || line_start . is_at_eol () { end_ix = ix ; break ; } let next_line_ix = ix + line_start . bytes_scanned () ; if next_line_ix == self . text . len () || scan_blank_line (& bytes [next_line_ix ..]) . is_some () { end_ix = next_line_ix ; break ; } ix = next_line_ix ; remaining_space = line_start . remaining_space () ; indent = 0 ; } self . pop (end_ix) ; ix } fn parse_indented_code_block (& mut self , start_ix : usize , mut remaining_space : usize) -> usize { self . tree . append (Item { start : start_ix , end : 0 , body : ItemBody :: IndentCodeBlock , }) ; self . tree . push () ; let bytes = self . text . as_bytes () ; let mut last_nonblank_child = None ; let mut last_nonblank_ix = 0 ; let mut end_ix = 0 ; self . last_line_blank = false ; let mut ix = start_ix ; loop { let line_start_ix = ix ; ix += scan_nextline (& bytes [ix ..]) ; self . append_code_text (remaining_space , line_start_ix , ix) ; if ! self . last_line_blank { last_nonblank_child = self . tree . cur () ; last_nonblank_ix = ix ; end_ix = ix ; } let mut line_start = LineStart :: new (& bytes [ix ..]) ; let n_containers = scan_containers (& self . tree , & mut line_start , self . options) ; if n_containers < self . tree . spine_len () || ! (line_start . scan_space (4) || line_start . is_at_eol ()) { break ; } let next_line_ix = ix + line_start . bytes_scanned () ; if next_line_ix == self . text . len () { break ; } ix = next_line_ix ; remaining_space = line_start . remaining_space () ; self . last_line_blank = scan_blank_line (& bytes [ix ..]) . is_some () ; } if let Some (child) = last_nonblank_child { self . tree [child] . next = None ; self . tree [child] . item . end = last_nonblank_ix ; } self . pop (end_ix) ; ix } fn parse_fenced_code_block (& mut self , start_ix : usize , indent : usize , fence_ch : u8 , n_fence_char : usize ,) -> usize { let bytes = self . text . as_bytes () ; let mut info_start = start_ix + n_fence_char ; info_start += scan_whitespace_no_nl (& bytes [info_start ..]) ; let mut ix = info_start + scan_nextline (& bytes [info_start ..]) ; let info_end = ix - scan_rev_while (& bytes [info_start .. ix] , is_ascii_whitespace) ; let info_string = unescape (& self . text [info_start .. info_end] , self . tree . is_in_table ()) ; self . tree . append (Item { start : start_ix , end : 0 , body : ItemBody :: FencedCodeBlock (self . allocs . allocate_cow (info_string)) , }) ; self . tree . push () ; loop { let mut line_start = LineStart :: new (& bytes [ix ..]) ; let n_containers = scan_containers (& self . tree , & mut line_start , self . options) ; if n_containers < self . tree . spine_len () { self . pop (ix) ; return ix ; } line_start . scan_space (indent) ; let mut close_line_start = line_start . clone () ; if ! close_line_start . scan_space (4 - indent) { let close_ix = ix + close_line_start . bytes_scanned () ; if let Some (n) = scan_closing_code_fence (& bytes [close_ix ..] , fence_ch , n_fence_char) { ix = close_ix + n ; self . pop (ix) ; return ix + scan_blank_line (& bytes [ix ..]) . unwrap_or (0) ; } } let remaining_space = line_start . remaining_space () ; ix += line_start . bytes_scanned () ; let next_ix = ix + scan_nextline (& bytes [ix ..]) ; self . append_code_text (remaining_space , ix , next_ix) ; ix = next_ix ; } } fn parse_metadata_block (& mut self , start_ix : usize , metadata_block_ch : u8) -> usize { let bytes = self . text . as_bytes () ; let metadata_block_kind = match metadata_block_ch { b'-' => MetadataBlockKind :: YamlStyle , b'+' => MetadataBlockKind :: PlusesStyle , _ => panic ! ("Erroneous metadata block character when parsing metadata block") , } ; let mut ix = start_ix + 3 + scan_nextline (& bytes [start_ix + 3 ..]) ; self . tree . append (Item { start : start_ix , end : 0 , body : ItemBody :: MetadataBlock (metadata_block_kind) , }) ; self . tree . push () ; loop { let mut line_start = LineStart :: new (& bytes [ix ..]) ; let n_containers = scan_containers (& self . tree , & mut line_start , self . options) ; if n_containers < self . tree . spine_len () { break ; } if let (_ , 0) = calc_indent (& bytes [ix ..] , 4) { if let Some (n) = scan_closing_metadata_block (& bytes [ix ..] , metadata_block_ch) { ix += n ; break ; } } let remaining_space = line_start . remaining_space () ; ix += line_start . bytes_scanned () ; let next_ix = ix + scan_nextline (& bytes [ix ..]) ; self . append_code_text (remaining_space , ix , next_ix) ; ix = next_ix ; } self . pop (ix) ; ix + scan_blank_line (& bytes [ix ..]) . unwrap_or (0) } fn append_code_text (& mut self , remaining_space : usize , start : usize , end : usize) { if remaining_space > 0 { let cow_ix = self . allocs . allocate_cow ("   " [.. remaining_space] . into ()) ; self . tree . append (Item { start , end : start , body : ItemBody :: SynthesizeText (cow_ix) , }) ; } if self . text . as_bytes () [end - 2] == b'\r' { self . tree . append_text (start , end - 2 , false) ; self . tree . append_text (end - 1 , end , false) ; } else { self . tree . append_text (start , end , false) ; } } # [doc = " Appends a line of HTML to the tree."] fn append_html_line (& mut self , remaining_space : usize , start : usize , end : usize) { if remaining_space > 0 { let cow_ix = self . allocs . allocate_cow ("   " [.. remaining_space] . into ()) ; self . tree . append (Item { start , end : start , body : ItemBody :: SynthesizeText (cow_ix) , }) ; } if self . text . as_bytes () [end - 2] == b'\r' { self . tree . append (Item { start , end : end - 2 , body : ItemBody :: Html , }) ; self . tree . append (Item { start : end - 1 , end , body : ItemBody :: Html , }) ; } else { self . tree . append (Item { start , end , body : ItemBody :: Html , }) ; } } # [doc = " Pop a container, setting its end."] fn pop (& mut self , ix : usize) { let cur_ix = self . tree . pop () . unwrap () ; self . tree [cur_ix] . item . end = ix ; if let ItemBody :: DefinitionList (_) = self . tree [cur_ix] . item . body { fixup_end_of_definition_list (& mut self . tree , cur_ix) ; self . begin_list_item = None ; } if let ItemBody :: List (true , _ , _) | ItemBody :: DefinitionList (true) = self . tree [cur_ix] . item . body { surgerize_tight_list (& mut self . tree , cur_ix) ; self . begin_list_item = None ; } } # [doc = " Close a list if it's open. Also set loose if last line was blank"] # [doc = " and end current list if it's a lone, empty item"] fn finish_list (& mut self , ix : usize) { self . finish_empty_list_item () ; if let Some (node_ix) = self . tree . peek_up () { if let ItemBody :: List (_ , _ , _) | ItemBody :: DefinitionList (_) = self . tree [node_ix] . item . body { self . pop (ix) ; } } if self . last_line_blank { if let Some (node_ix) = self . tree . peek_grandparent () { if let ItemBody :: List (ref mut is_tight , _ , _) | ItemBody :: DefinitionList (ref mut is_tight) = self . tree [node_ix] . item . body { * is_tight = false ; } } self . last_line_blank = false ; } } fn finish_empty_list_item (& mut self) { if let Some (begin_list_item) = self . begin_list_item { if self . last_line_blank { if let Some (node_ix) = self . tree . peek_up () { if let ItemBody :: ListItem (_) | ItemBody :: DefinitionListDefinition (_) = self . tree [node_ix] . item . body { self . pop (begin_list_item) ; } } } } self . begin_list_item = None ; } # [doc = " Continue an existing list or start a new one if there's not an open"] # [doc = " list that matches."] fn continue_list (& mut self , start : usize , ch : u8 , index : u64) { self . finish_empty_list_item () ; if let Some (node_ix) = self . tree . peek_up () { if let ItemBody :: List (ref mut is_tight , existing_ch , _) = self . tree [node_ix] . item . body { if existing_ch == ch { if self . last_line_blank { * is_tight = false ; self . last_line_blank = false ; } return ; } } self . finish_list (start) ; } self . tree . append (Item { start , end : 0 , body : ItemBody :: List (true , ch , index) , }) ; self . tree . push () ; self . last_line_blank = false ; } # [doc = " Parse a thematic break."] # [doc = ""] # [doc = " Returns index of start of next line."] fn parse_hrule (& mut self , hrule_size : usize , ix : usize) -> usize { self . tree . append (Item { start : ix , end : ix + hrule_size , body : ItemBody :: Rule , }) ; ix + hrule_size } # [doc = " Parse an ATX heading."] # [doc = ""] # [doc = " Returns index of start of next line."] fn parse_atx_heading (& mut self , start : usize , atx_level : HeadingLevel) -> usize { let mut ix = start ; let heading_ix = self . tree . append (Item { start , end : 0 , body : ItemBody :: default () , }) ; ix += atx_level as usize ; let bytes = self . text . as_bytes () ; if let Some (eol_bytes) = scan_eol (& bytes [ix ..]) { self . tree [heading_ix] . item . end = ix + eol_bytes ; self . tree [heading_ix] . item . body = ItemBody :: Heading (atx_level , None) ; return ix + eol_bytes ; } let skip_spaces = scan_whitespace_no_nl (& bytes [ix ..]) ; ix += skip_spaces ; let header_start = ix ; let header_node_idx = self . tree . push () ; let (end , content_end , attrs) = if self . options . contains (Options :: ENABLE_HEADING_ATTRIBUTES) { let header_end = header_start + scan_nextline (& bytes [header_start ..]) ; let (content_end , attrs) = self . extract_and_parse_heading_attribute_block (header_start , header_end) ; self . parse_line (ix , Some (content_end) , TableParseMode :: Disabled) ; (header_end , content_end , attrs) } else { let (line_ix , line_brk) = self . parse_line (ix , None , TableParseMode :: Disabled) ; ix = line_ix ; if let Some (Item { start , end , body : ItemBody :: HardBreak (true) , }) = line_brk { self . tree . append_text (start , end , false) ; } (ix , ix , None) } ; self . tree [header_node_idx] . item . end = end ; let mut empty_text_node = false ; if let Some (cur_ix) = self . tree . cur () { let header_text = & bytes [header_start .. content_end] ; let mut limit = header_text . iter () . rposition (| & b | ! (b == b'\n' || b == b'\r' || b == b' ')) . map_or (0 , | i | i + 1) ; let closer = header_text [.. limit] . iter () . rposition (| & b | b != b'#') . map_or (0 , | i | i + 1) ; if closer == 0 { limit = closer ; } else { let spaces = scan_rev_while (& header_text [.. closer] , | b | b == b' ') ; if spaces > 0 { limit = closer - spaces ; } } self . tree [cur_ix] . item . end = limit + header_start ; if limit == 0 { empty_text_node = true ; } } if empty_text_node { self . tree . remove_node () ; } else { self . tree . pop () ; } self . tree [heading_ix] . item . body = ItemBody :: Heading (atx_level , attrs . map (| attrs | self . allocs . allocate_heading (attrs)) ,) ; end } # [doc = " Returns the number of bytes scanned on success."] fn parse_footnote (& mut self , start : usize) -> Option < usize > { let bytes = & self . text . as_bytes () [start ..] ; if ! bytes . starts_with (b"[^") { return None ; } let (mut i , label) = if self . options . has_gfm_footnotes () { scan_link_label_rest (& self . text [start + 2 ..] , & | _ | None , self . tree . is_in_table ()) ? } else { self . parse_refdef_label (start + 2) ? } ; if self . options . has_gfm_footnotes () && label . bytes () . any (| b | b == b'\r' || b == b'\n') { return None ; } i += 2 ; if bytes . get (i) != Some (& b':') { return None ; } i += 1 ; self . finish_list (start) ; if let Some (node_ix) = self . tree . peek_up () { if let ItemBody :: FootnoteDefinition (..) = self . tree [node_ix] . item . body { self . pop (start) ; } } if self . options . has_gfm_footnotes () { i += scan_whitespace_no_nl (& bytes [i ..]) ; } self . allocs . footdefs . 0 . insert (UniCase :: new (label . clone ()) , FootnoteDef { use_count : 0 }) ; self . tree . append (Item { start , end : 0 , body : ItemBody :: FootnoteDefinition (self . allocs . allocate_cow (label)) , }) ; self . tree . push () ; Some (i) } # [doc = " Tries to parse a reference label, which can be interrupted by new blocks."] # [doc = " On success, returns the number of bytes of the label and the label itself."] fn parse_refdef_label (& self , start : usize) -> Option < (usize , CowStr < 'a >) > { scan_link_label_rest (& self . text [start ..] , & | bytes | { let mut line_start = LineStart :: new (bytes) ; let tree_position = scan_containers (& self . tree , & mut line_start , self . options) ; let current_container = tree_position == self . tree . spine_len () ; if line_start . scan_space (4) { return Some (line_start . bytes_scanned ()) ; } let bytes_scanned = line_start . bytes_scanned () ; let suffix = & bytes [bytes_scanned ..] ; if self . scan_paragraph_interrupt (suffix , current_container , tree_position) || (current_container && scan_setext_heading (suffix) . is_some ()) { None } else { Some (bytes_scanned) } } , self . tree . is_in_table () ,) } # [doc = " Returns number of bytes scanned, label and definition on success."] fn parse_refdef_total (& mut self , start : usize) -> Option < (usize , LinkLabel < 'a > , LinkDef < 'a >) > { let bytes = & self . text . as_bytes () [start ..] ; if bytes . get (0) != Some (& b'[') { return None ; } let (mut i , label) = self . parse_refdef_label (start + 1) ? ; i += 1 ; if bytes . get (i) != Some (& b':') { return None ; } i += 1 ; let (bytecount , link_def) = self . scan_refdef (start , start + i) ? ; Some ((bytecount + i , UniCase :: new (label) , link_def)) } # [doc = " Returns number of bytes and number of newlines"] fn scan_refdef_space (& self , bytes : & [u8] , mut i : usize) -> Option < (usize , usize) > { let mut newlines = 0 ; loop { let whitespaces = scan_whitespace_no_nl (& bytes [i ..]) ; i += whitespaces ; if let Some (eol_bytes) = scan_eol (& bytes [i ..]) { i += eol_bytes ; newlines += 1 ; if newlines > 1 { return None ; } } else { break ; } let mut line_start = LineStart :: new (& bytes [i ..]) ; let tree_position = scan_containers (& self . tree , & mut line_start , self . options) ; let current_container = tree_position == self . tree . spine_len () ; if ! line_start . scan_space (4) { let suffix = & bytes [i + line_start . bytes_scanned () ..] ; if self . scan_paragraph_interrupt (suffix , current_container , tree_position) || scan_setext_heading (suffix) . is_some () { return None ; } } i += line_start . bytes_scanned () ; } Some ((i , newlines)) } fn scan_refdef_title < 't > (& self , text : & 't str) -> Option < (usize , CowStr < 't >) > { let bytes = text . as_bytes () ; let closing_delim = match bytes . first () ? { b'\'' => b'\'' , b'"' => b'"' , b'(' => b')' , _ => return None , } ; let mut bytecount = 1 ; let mut linestart = 1 ; let mut linebuf = None ; while let Some (& c) = bytes . get (bytecount) { match c { b'(' if closing_delim == b')' => { return None ; } b'\n' | b'\r' => { let linebuf = if let Some (linebuf) = & mut linebuf { linebuf } else { linebuf = Some (String :: new ()) ; linebuf . as_mut () . unwrap () } ; linebuf . push_str (& text [linestart .. bytecount]) ; linebuf . push ('\n') ; bytecount += 1 ; if c == b'\r' && bytes . get (bytecount) == Some (& b'\n') { bytecount += 1 ; } let mut line_start = LineStart :: new (& bytes [bytecount ..]) ; let tree_position = scan_containers (& self . tree , & mut line_start , self . options) ; let current_container = tree_position == self . tree . spine_len () ; if ! line_start . scan_space (4) { let suffix = & bytes [bytecount + line_start . bytes_scanned () ..] ; if self . scan_paragraph_interrupt (suffix , current_container , tree_position) || scan_setext_heading (suffix) . is_some () { return None ; } } line_start . scan_all_space () ; bytecount += line_start . bytes_scanned () ; linestart = bytecount ; if scan_blank_line (& bytes [bytecount ..]) . is_some () { return None ; } } b'\\' => { bytecount += 1 ; if let Some (c) = bytes . get (bytecount) { if c != & b'\r' && c != & b'\n' { bytecount += 1 ; } } } c if c == closing_delim => { let cow = if let Some (mut linebuf) = linebuf { linebuf . push_str (& text [linestart .. bytecount]) ; CowStr :: from (linebuf) } else { CowStr :: from (& text [linestart .. bytecount]) } ; return Some ((bytecount + 1 , cow)) ; } _ => { bytecount += 1 ; } } } None } # [doc = " Returns # of bytes and definition."] # [doc = " Assumes the label of the reference including colon has already been scanned."] fn scan_refdef (& self , span_start : usize , start : usize) -> Option < (usize , LinkDef < 'a >) > { let bytes = self . text . as_bytes () ; let (mut i , _newlines) = self . scan_refdef_space (bytes , start) ? ; let (dest_length , dest) = scan_link_dest (self . text , i , LINK_MAX_NESTED_PARENS) ? ; if dest_length == 0 { return None ; } let dest = unescape (dest , self . tree . is_in_table ()) ; i += dest_length ; let mut backup = (i - start , LinkDef { dest , title : None , span : span_start .. i , } ,) ; let (mut i , newlines) = if let Some ((new_i , mut newlines)) = self . scan_refdef_space (bytes , i) { if i == self . text . len () { newlines += 1 ; } if new_i == i && newlines == 0 { return None ; } if newlines > 1 { return Some (backup) ; } ; (new_i , newlines) } else { return Some (backup) ; } ; if let Some ((title_length , title)) = self . scan_refdef_title (& self . text [i ..]) { i += title_length ; if scan_blank_line (& bytes [i ..]) . is_some () { backup . 0 = i - start ; backup . 1 . span = span_start .. i ; backup . 1 . title = Some (unescape (title , self . tree . is_in_table ())) ; return Some (backup) ; } } if newlines > 0 { Some (backup) } else { None } } # [doc = " Checks whether we should break a paragraph on the given input."] fn scan_paragraph_interrupt (& self , bytes : & [u8] , current_container : bool , tree_position : usize ,) -> bool { if scan_paragraph_interrupt_no_table (bytes , current_container , self . options . contains (Options :: ENABLE_FOOTNOTES) , self . options . contains (Options :: ENABLE_DEFINITION_LIST) , & self . tree , tree_position ,) { return true ; } if ! self . options . contains (Options :: ENABLE_TABLES) || ! bytes . starts_with (b"|") { return false ; } let mut pipes = 0 ; let mut next_line_ix = 0 ; let mut bsesc = false ; let mut last_pipe_ix = 0 ; for (i , & byte) in bytes . iter () . enumerate () { match byte { b'\\' => { bsesc = true ; continue ; } b'|' if ! bsesc => { pipes += 1 ; last_pipe_ix = i ; } b'\r' | b'\n' => { next_line_ix = i + scan_eol (& bytes [i ..]) . unwrap () ; break ; } _ => { } } bsesc = false ; } if next_line_ix == 0 { return false ; } let mut line_start = LineStart :: new (& bytes [next_line_ix ..]) ; if scan_containers (& self . tree , & mut line_start , self . options) != self . tree . spine_len () { return false ; } let table_head_ix = next_line_ix + line_start . bytes_scanned () ; let (table_head_bytes , alignment) = scan_table_head (& bytes [table_head_ix ..]) ; if table_head_bytes == 0 { return false ; } let header_count = count_header_cols (bytes , pipes , 0 , last_pipe_ix) ; alignment . len () == header_count } # [doc = " Extracts and parses a heading attribute block if exists."] # [doc = ""] # [doc = " Returns `(end_offset_of_heading_content, (id, classes))`."] # [doc = ""] # [doc = " If `header_end` is less than or equal to `header_start`, the given"] # [doc = " input is considered as empty."] fn extract_and_parse_heading_attribute_block (& mut self , header_start : usize , header_end : usize ,) -> (usize , Option < HeadingAttributes < 'a > >) { if ! self . options . contains (Options :: ENABLE_HEADING_ATTRIBUTES) { return (header_end , None) ; } let header_bytes = & self . text . as_bytes () [header_start .. header_end] ; let (content_len , attr_block_range_rel) = extract_attribute_block_content_from_header_text (header_bytes) ; let content_end = header_start + content_len ; let attrs = attr_block_range_rel . and_then (| r | { parse_inside_attribute_block (& self . text [(header_start + r . start) .. (header_start + r . end)] ,) }) ; (content_end , attrs) } }
};
}
