// Generated macro for DecompressionReader (struct)
macro_rules! Depcrate_decompressDecompressionReader {
() => {
// Module: crate::decompress
// Provides: {"DecompressionReader"}
// Dependencies: {}
# [doc = " A streaming reader for decompressing the contents of a file."] # [doc = ""] # [doc = " The purpose of this reader is to provide a seamless way to decompress the"] # [doc = " contents of file using existing tools in the current environment. This is"] # [doc = " meant to be an alternative to using decompression libraries in favor of the"] # [doc = " simplicity and portability of using external commands such as `gzip` and"] # [doc = " `xz`. This does impose the overhead of spawning a process, so other means"] # [doc = " for performing decompression should be sought if this overhead isn't"] # [doc = " acceptable."] # [doc = ""] # [doc = " A decompression reader comes with a default set of matching rules that are"] # [doc = " meant to associate file paths with the corresponding command to use to"] # [doc = " decompress them. For example, a glob like `*.gz` matches gzip compressed"] # [doc = " files with the command `gzip -d -c`. If a file path does not match any"] # [doc = " existing rules, or if it matches a rule whose command does not exist in the"] # [doc = " current environment, then the decompression reader passes through the"] # [doc = " contents of the underlying file without doing any decompression."] # [doc = ""] # [doc = " The default matching rules are probably good enough for most cases, and if"] # [doc = " they require revision, pull requests are welcome. In cases where they must"] # [doc = " be changed or extended, they can be customized through the use of"] # [doc = " [`DecompressionMatcherBuilder`] and [`DecompressionReaderBuilder`]."] # [doc = ""] # [doc = " By default, this reader will asynchronously read the processes' stderr."] # [doc = " This prevents subtle deadlocking bugs for noisy processes that write a lot"] # [doc = " to stderr. Currently, the entire contents of stderr is read on to the heap."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to read the decompressed contents of a file without"] # [doc = " needing to explicitly choose the decompression command to run."] # [doc = ""] # [doc = " Note that if you need to decompress multiple files, it is better to use"] # [doc = " `DecompressionReaderBuilder`, which will amortize the cost of compiling the"] # [doc = " matcher."] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::{io::Read, process::Command};"] # [doc = ""] # [doc = " use grep_cli::DecompressionReader;"] # [doc = ""] # [doc = " let mut rdr = DecompressionReader::new(\"/usr/share/man/man1/ls.1.gz\")?;"] # [doc = " let mut contents = vec![];"] # [doc = " rdr.read_to_end(&mut contents)?;"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Debug)] pub struct DecompressionReader { rdr : Result < CommandReader , File > , }
};
}
