// Generated macro for CommandReader (struct)
macro_rules! Depcrate_processCommandReader {
() => {
// Module: crate::process
// Provides: {"CommandReader"}
// Dependencies: {}
# [doc = " A streaming reader for a command's output."] # [doc = ""] # [doc = " The purpose of this reader is to provide an easy way to execute processes"] # [doc = " whose stdout is read in a streaming way while also making the processes'"] # [doc = " stderr available when the process fails with an exit code. This makes it"] # [doc = " possible to execute processes while surfacing the underlying failure mode"] # [doc = " in the case of an error."] # [doc = ""] # [doc = " Moreover, by default, this reader will asynchronously read the processes'"] # [doc = " stderr. This prevents subtle deadlocking bugs for noisy processes that"] # [doc = " write a lot to stderr. Currently, the entire contents of stderr is read"] # [doc = " on to the heap."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to invoke `gzip` to decompress the contents of a"] # [doc = " file. If the `gzip` command reports a failing exit status, then its stderr"] # [doc = " is returned as an error."] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::{io::Read, process::Command};"] # [doc = ""] # [doc = " use grep_cli::CommandReader;"] # [doc = ""] # [doc = " let mut cmd = Command::new(\"gzip\");"] # [doc = " cmd.arg(\"-d\").arg(\"-c\").arg(\"/usr/share/man/man1/ls.1.gz\");"] # [doc = ""] # [doc = " let mut rdr = CommandReader::new(&mut cmd)?;"] # [doc = " let mut contents = vec![];"] # [doc = " rdr.read_to_end(&mut contents)?;"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Debug)] pub struct CommandReader { child : process :: Child , stderr : StderrReader , # [doc = " This is set to true once 'read' returns zero bytes. When this isn't"] # [doc = " set and we close the reader, then we anticipate a pipe error when"] # [doc = " reaping the child process and silence it."] eof : bool , }
};
}
