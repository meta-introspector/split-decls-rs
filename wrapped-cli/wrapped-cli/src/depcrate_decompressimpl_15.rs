// Generated macro for impl_15 (impl)
macro_rules! Depcrate_decompressimpl_15 {
() => {
// Module: crate::decompress
// Provides: {"impl_15"}
// Dependencies: {}
impl DecompressionReader { # [doc = " Build a new streaming reader for decompressing data."] # [doc = ""] # [doc = " If decompression is done out-of-process and if there was a problem"] # [doc = " spawning the process, then its error is returned."] # [doc = ""] # [doc = " If the given file path could not be matched with a decompression"] # [doc = " strategy, then a passthru reader is returned that does no"] # [doc = " decompression."] # [doc = ""] # [doc = " This uses the default matching rules for determining how to decompress"] # [doc = " the given file. To change those matching rules, use"] # [doc = " [`DecompressionReaderBuilder`] and [`DecompressionMatcherBuilder`]."] # [doc = ""] # [doc = " When creating readers for many paths. it is better to use the builder"] # [doc = " since it will amortize the cost of constructing the matcher."] pub fn new < P : AsRef < Path > > (path : P ,) -> Result < DecompressionReader , CommandError > { DecompressionReaderBuilder :: new () . build (path) } # [doc = " Creates a new \"passthru\" decompression reader that reads from the file"] # [doc = " corresponding to the given path without doing decompression and without"] # [doc = " executing another process."] fn new_passthru (path : & Path) -> Result < DecompressionReader , CommandError > { let file = File :: open (path) ? ; Ok (DecompressionReader { rdr : Err (file) }) } # [doc = " Closes this reader, freeing any resources used by its underlying child"] # [doc = " process, if one was used. If the child process exits with a nonzero"] # [doc = " exit code, the returned Err value will include its stderr."] # [doc = ""] # [doc = " `close` is idempotent, meaning it can be safely called multiple times."] # [doc = " The first call closes the CommandReader and any subsequent calls do"] # [doc = " nothing."] # [doc = ""] # [doc = " This method should be called after partially reading a file to prevent"] # [doc = " resource leakage. However there is no need to call `close` explicitly"] # [doc = " if your code always calls `read` to EOF, as `read` takes care of"] # [doc = " calling `close` in this case."] # [doc = ""] # [doc = " `close` is also called in `drop` as a last line of defense against"] # [doc = " resource leakage. Any error from the child process is then printed as a"] # [doc = " warning to stderr. This can be avoided by explicitly calling `close`"] # [doc = " before the CommandReader is dropped."] pub fn close (& mut self) -> io :: Result < () > { match self . rdr { Ok (ref mut rdr) => rdr . close () , Err (_) => Ok (()) , } } }
};
}
