// Generated macro for impl_67 (impl)
macro_rules! Depcrate_processimpl_67 {
() => {
// Module: crate::process
// Provides: {"impl_67"}
// Dependencies: {}
impl CommandReaderBuilder { # [doc = " Create a new builder with the default configuration."] pub fn new () -> CommandReaderBuilder { CommandReaderBuilder :: default () } # [doc = " Build a new streaming reader for the given command's output."] # [doc = ""] # [doc = " The caller should set everything that's required on the given command"] # [doc = " before building a reader, such as its arguments, environment and"] # [doc = " current working directory. Settings such as the stdout and stderr (but"] # [doc = " not stdin) pipes will be overridden so that they can be controlled by"] # [doc = " the reader."] # [doc = ""] # [doc = " If there was a problem spawning the given command, then its error is"] # [doc = " returned."] pub fn build (& self , command : & mut process :: Command ,) -> Result < CommandReader , CommandError > { let mut child = command . stdout (process :: Stdio :: piped ()) . stderr (process :: Stdio :: piped ()) . spawn () ? ; let stderr = if self . async_stderr { StderrReader :: r#async (child . stderr . take () . unwrap ()) } else { StderrReader :: sync (child . stderr . take () . unwrap ()) } ; Ok (CommandReader { child , stderr , eof : false }) } # [doc = " When enabled, the reader will asynchronously read the contents of the"] # [doc = " command's stderr output. When disabled, stderr is only read after the"] # [doc = " stdout stream has been exhausted (or if the process quits with an error"] # [doc = " code)."] # [doc = ""] # [doc = " Note that when enabled, this may require launching an additional"] # [doc = " thread in order to read stderr. This is done so that the process being"] # [doc = " executed is never blocked from writing to stdout or stderr. If this is"] # [doc = " disabled, then it is possible for the process to fill up the stderr"] # [doc = " buffer and deadlock."] # [doc = ""] # [doc = " This is enabled by default."] pub fn async_stderr (& mut self , yes : bool) -> & mut CommandReaderBuilder { self . async_stderr = yes ; self } }
};
}
