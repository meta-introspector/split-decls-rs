macro_rules! IndexAttr {
    () => {
        # [doc = "\nThe `index` attribute.\n\nThis attribute specifies an `sval::Index` as a constant\nto use for the annotated item.\n"] pub (crate) struct IndexAttr ;
    };
}

IndexAttr!()