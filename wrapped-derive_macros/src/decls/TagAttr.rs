macro_rules! TagAttr {
    () => {
        # [doc = "\nThe `tag` attribute.\n\nThis attribute specifies a path to an `sval::Tag` to use\nfor the annotated item.\n"] pub (crate) struct TagAttr ;
    };
}

TagAttr!()