macro_rules! DynamicAttr {
    () => {
        # [doc = "\nThe `dynamic` attribute.\n\nThis attribute signals that an enum should be dynamic.\n"] pub (crate) struct DynamicAttr ;
    };
}

DynamicAttr!();