// Generated macro for impl_207 (impl)
macro_rules! Depcrate_stringimpl_207 {
() => {
// Module: crate::string
// Provides: {"impl_207"}
// Dependencies: {}
impl CaptureLocations { # [doc = " Returns the start and end byte offsets of the capture group at index"] # [doc = " `i`. This returns `None` if `i` is not a valid capture group or if the"] # [doc = " capture group did not match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?<first>\\w+)\\s+(?<last>\\w+)\").unwrap();"] # [doc = " let mut locs = re.capture_locations();"] # [doc = " re.captures_read(&mut locs, \"Bruce Springsteen\").unwrap();"] # [doc = " assert_eq!(Some((0, 17)), locs.get(0));"] # [doc = " assert_eq!(Some((0, 5)), locs.get(1));"] # [doc = " assert_eq!(Some((6, 17)), locs.get(2));"] # [doc = " ```"] # [inline] pub fn get (& self , i : usize) -> Option < (usize , usize) > { let slot = i . checked_mul (2) ? ; let start = self . 0 . get (slot) . copied () ? ? . get () ; let slot = slot . checked_add (1) ? ; let end = self . 0 . get (slot) . copied () ? ? . get () ; Some ((start , end)) } # [doc = " Returns the total number of capture groups (even if they didn't match)."] # [doc = " That is, the length returned is unaffected by the result of a search."] # [doc = ""] # [doc = " This is always at least `1` since every regex has at least `1`"] # [doc = " capturing group that corresponds to the entire match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?<first>\\w+)\\s+(?<last>\\w+)\").unwrap();"] # [doc = " let mut locs = re.capture_locations();"] # [doc = " assert_eq!(3, locs.len());"] # [doc = " re.captures_read(&mut locs, \"Bruce Springsteen\").unwrap();"] # [doc = " assert_eq!(3, locs.len());"] # [doc = " ```"] # [doc = ""] # [doc = " Notice that the length is always at least `1`, regardless of the regex:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\").unwrap();"] # [doc = " let locs = re.capture_locations();"] # [doc = " assert_eq!(1, locs.len());"] # [doc = ""] # [doc = " // [a&&b] is a regex that never matches anything."] # [doc = " let re = Regex::new(r\"[^\\s\\S]\").unwrap();"] # [doc = " let locs = re.capture_locations();"] # [doc = " assert_eq!(1, locs.len());"] # [doc = " ```"] # [inline] pub fn len (& self) -> usize { self . 0 . len () . checked_shr (1) . unwrap () } }
};
}
