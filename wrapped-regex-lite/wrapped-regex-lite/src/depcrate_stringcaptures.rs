// Generated macro for Captures (struct)
macro_rules! Depcrate_stringCaptures {
() => {
// Module: crate::string
// Provides: {"Captures"}
// Dependencies: {}
# [doc = " Represents the capture groups for a single match."] # [doc = ""] # [doc = " Capture groups refer to parts of a regex enclosed in parentheses. They can"] # [doc = " be optionally named. The purpose of capture groups is to be able to"] # [doc = " reference different parts of a match based on the original pattern. For"] # [doc = " example, say you want to match the individual letters in a 5-letter word:"] # [doc = ""] # [doc = " ```text"] # [doc = " (?<first>\\w)(\\w)(?:\\w)\\w(?<last>\\w)"] # [doc = " ```"] # [doc = ""] # [doc = " This regex has 4 capture groups:"] # [doc = ""] # [doc = " * The group at index `0` corresponds to the overall match. It is always"] # [doc = " present in every match and never has a name."] # [doc = " * The group at index `1` with name `first` corresponding to the first"] # [doc = " letter."] # [doc = " * The group at index `2` with no name corresponding to the second letter."] # [doc = " * The group at index `3` with name `last` corresponding to the fifth and"] # [doc = " last letter."] # [doc = ""] # [doc = " Notice that `(?:\\w)` was not listed above as a capture group despite it"] # [doc = " being enclosed in parentheses. That's because `(?:pattern)` is a special"] # [doc = " syntax that permits grouping but *without* capturing. The reason for not"] # [doc = " treating it as a capture is that tracking and reporting capture groups"] # [doc = " requires additional state that may lead to slower searches. So using as few"] # [doc = " capture groups as possible can help performance. (Although the difference"] # [doc = " in performance of a couple of capture groups is likely immaterial.)"] # [doc = ""] # [doc = " Values with this type are created by [`Regex::captures`] or"] # [doc = " [`Regex::captures_iter`]."] # [doc = ""] # [doc = " `'h` is the lifetime of the haystack that these captures were matched from."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?<first>\\w)(\\w)(?:\\w)\\w(?<last>\\w)\").unwrap();"] # [doc = " let caps = re.captures(\"toady\").unwrap();"] # [doc = " assert_eq!(\"toady\", &caps[0]);"] # [doc = " assert_eq!(\"t\", &caps[\"first\"]);"] # [doc = " assert_eq!(\"o\", &caps[2]);"] # [doc = " assert_eq!(\"y\", &caps[\"last\"]);"] # [doc = " ```"] pub struct Captures < 'h > { haystack : & 'h str , slots : CaptureLocations , pikevm : Arc < PikeVM > , }
};
}
