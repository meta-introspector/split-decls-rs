// Generated macro for impl_161 (impl)
macro_rules! Depcrate_pikevmimpl_161 {
() => {
// Module: crate::pikevm
// Provides: {"impl_161"}
// Dependencies: {}
impl SparseSet { # [doc = " Create a new sparse set with the given capacity."] # [doc = ""] # [doc = " Sparse sets have a fixed size and they cannot grow. Attempting to"] # [doc = " insert more distinct elements than the total capacity of the set will"] # [doc = " result in a panic."] # [doc = ""] # [doc = " This panics if the capacity given is bigger than `StateID::LIMIT`."] fn new (capacity : usize) -> SparseSet { let mut set = SparseSet { len : 0 , dense : vec ! [] , sparse : vec ! [] } ; set . resize (capacity) ; set } # [doc = " Resizes this sparse set to have the new capacity given."] # [doc = ""] # [doc = " This set is automatically cleared."] # [doc = ""] # [doc = " This panics if the capacity given is bigger than `StateID::LIMIT`."] fn resize (& mut self , new_capacity : usize) { assert ! (new_capacity <= u32 :: MAX . as_usize () , "sparse set capacity cannot exceed {:?}" , u32 :: MAX ,) ; self . clear () ; self . dense . resize (new_capacity , 0) ; self . sparse . resize (new_capacity , 0) ; } # [doc = " Returns the capacity of this set."] # [doc = ""] # [doc = " The capacity represents a fixed limit on the number of distinct"] # [doc = " elements that are allowed in this set. The capacity cannot be changed."] fn capacity (& self) -> usize { self . dense . len () } # [doc = " Returns the number of elements in this set."] fn len (& self) -> usize { self . len } # [doc = " Returns true if and only if this set is empty."] fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Insert the state ID value into this set and return true if the given"] # [doc = " state ID was not previously in this set."] # [doc = ""] # [doc = " This operation is idempotent. If the given value is already in this"] # [doc = " set, then this is a no-op."] # [doc = ""] # [doc = " If more than `capacity` ids are inserted, then this panics."] fn insert (& mut self , id : StateID) -> bool { if self . contains (id) { return false ; } let index = self . len () ; assert ! (index < self . capacity () , "{:?} exceeds capacity of {:?} when inserting {:?}" , index , self . capacity () , id ,) ; self . dense [index] = id ; self . sparse [id . as_usize ()] = u32 :: try_from (index) . unwrap () ; self . len += 1 ; true } # [doc = " Returns true if and only if this set contains the given value."] fn contains (& self , id : StateID) -> bool { let index = self . sparse [id . as_usize ()] ; index . as_usize () < self . len () && self . dense [index . as_usize ()] == id } # [doc = " Clear this set such that it has no members."] fn clear (& mut self) { self . len = 0 ; } # [doc = " Returns an iterator over all the state IDs in this set in the order in"] # [doc = " which they were inserted."] fn iter (& self) -> SparseSetIter < '_ > { SparseSetIter (self . dense [.. self . len ()] . iter ()) } }
};
}
