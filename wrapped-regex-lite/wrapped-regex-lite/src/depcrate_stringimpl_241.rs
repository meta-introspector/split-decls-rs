// Generated macro for impl_241 (impl)
macro_rules! Depcrate_stringimpl_241 {
() => {
// Module: crate::string
// Provides: {"impl_241"}
// Dependencies: {}
impl RegexBuilder { # [doc = " Create a new builder with a default configuration for the given"] # [doc = " pattern."] # [doc = ""] # [doc = " If the pattern is invalid or exceeds the configured size limits, then"] # [doc = " an error will be returned when [`RegexBuilder::build`] is called."] pub fn new (pattern : & str) -> RegexBuilder { RegexBuilder { pattern : pattern . to_string () , hir_config : hir :: Config :: default () , nfa_config : nfa :: Config :: default () , } } # [doc = " Compiles the pattern given to `RegexBuilder::new` with the"] # [doc = " configuration set on this builder."] # [doc = ""] # [doc = " If the pattern isn't a valid regex or if a configured size limit was"] # [doc = " exceeded, then an error is returned."] pub fn build (& self) -> Result < Regex , Error > { let hir = Hir :: parse (self . hir_config , & self . pattern) ? ; let nfa = NFA :: new (self . nfa_config , self . pattern . clone () , & hir) ? ; let pikevm = Arc :: new (PikeVM :: new (nfa)) ; let pool = { let pikevm = Arc :: clone (& pikevm) ; let create = Box :: new (move | | Cache :: new (& pikevm)) ; CachePool :: new (create) } ; Ok (Regex { pikevm , pool }) } # [doc = " This configures whether to enable ASCII case insensitive matching for"] # [doc = " the entire pattern."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `i`"] # [doc = " in the pattern. For example, `(?i:foo)` matches `foo` case"] # [doc = " insensitively while `(?-i:foo)` matches `foo` case sensitively."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"foo(?-i:bar)quux\")"] # [doc = "     .case_insensitive(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert!(re.is_match(\"FoObarQuUx\"));"] # [doc = " // Even though case insensitive matching is enabled in the builder,"] # [doc = " // it can be locally disabled within the pattern. In this case,"] # [doc = " // `bar` is matched case sensitively."] # [doc = " assert!(!re.is_match(\"fooBARquux\"));"] # [doc = " ```"] pub fn case_insensitive (& mut self , yes : bool) -> & mut RegexBuilder { self . hir_config . flags . case_insensitive = yes ; self } # [doc = " This configures multi-line mode for the entire pattern."] # [doc = ""] # [doc = " Enabling multi-line mode changes the behavior of the `^` and `$` anchor"] # [doc = " assertions. Instead of only matching at the beginning and end of a"] # [doc = " haystack, respectively, multi-line mode causes them to match at the"] # [doc = " beginning and end of a line *in addition* to the beginning and end of"] # [doc = " a haystack. More precisely, `^` will match at the position immediately"] # [doc = " following a `\\n` and `$` will match at the position immediately"] # [doc = " preceding a `\\n`."] # [doc = ""] # [doc = " The behavior of this option is impacted by the [`RegexBuilder::crlf`]"] # [doc = " setting. Namely, CRLF mode changes the line terminator to be either"] # [doc = " `\\r` or `\\n`, but never at the position between a `\\r` and `\\`n."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `m` in the"] # [doc = " pattern."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"^foo$\")"] # [doc = "     .multi_line(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert_eq!(Some(1..4), re.find(\"\\nfoo\\n\").map(|m| m.range()));"] # [doc = " ```"] pub fn multi_line (& mut self , yes : bool) -> & mut RegexBuilder { self . hir_config . flags . multi_line = yes ; self } # [doc = " This configures dot-matches-new-line mode for the entire pattern."] # [doc = ""] # [doc = " Perhaps surprisingly, the default behavior for `.` is not to match"] # [doc = " any character, but rather, to match any character except for the line"] # [doc = " terminator (which is `\\n` by default). When this mode is enabled, the"] # [doc = " behavior changes such that `.` truly matches any character."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `s` in the"] # [doc = " pattern."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"foo.bar\")"] # [doc = "     .dot_matches_new_line(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " let hay = \"foo\\nbar\";"] # [doc = " assert_eq!(Some(\"foo\\nbar\"), re.find(hay).map(|m| m.as_str()));"] # [doc = " ```"] pub fn dot_matches_new_line (& mut self , yes : bool) -> & mut RegexBuilder { self . hir_config . flags . dot_matches_new_line = yes ; self } # [doc = " This configures CRLF mode for the entire pattern."] # [doc = ""] # [doc = " When CRLF mode is enabled, both `\\r` (\"carriage return\" or CR for"] # [doc = " short) and `\\n` (\"line feed\" or LF for short) are treated as line"] # [doc = " terminators. This results in the following:"] # [doc = ""] # [doc = " * Unless dot-matches-new-line mode is enabled, `.` will now match any"] # [doc = " character except for `\\n` and `\\r`."] # [doc = " * When multi-line mode is enabled, `^` will match immediately"] # [doc = " following a `\\n` or a `\\r`. Similarly, `$` will match immediately"] # [doc = " preceding a `\\n` or a `\\r`. Neither `^` nor `$` will ever match between"] # [doc = " `\\r` and `\\n`."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `R` in"] # [doc = " the pattern."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"^foo$\")"] # [doc = "     .multi_line(true)"] # [doc = "     .crlf(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " let hay = \"\\r\\nfoo\\r\\n\";"] # [doc = " // If CRLF mode weren't enabled here, then '$' wouldn't match"] # [doc = " // immediately after 'foo', and thus no match would be found."] # [doc = " assert_eq!(Some(\"foo\"), re.find(hay).map(|m| m.as_str()));"] # [doc = " ```"] # [doc = ""] # [doc = " This example demonstrates that `^` will never match at a position"] # [doc = " between `\\r` and `\\n`. (`$` will similarly not match between a `\\r`"] # [doc = " and a `\\n`.)"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"^\")"] # [doc = "     .multi_line(true)"] # [doc = "     .crlf(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " let hay = \"\\r\\n\\r\\n\";"] # [doc = " let ranges: Vec<_> = re.find_iter(hay).map(|m| m.range()).collect();"] # [doc = " assert_eq!(ranges, vec![0..0, 2..2, 4..4]);"] # [doc = " ```"] pub fn crlf (& mut self , yes : bool) -> & mut RegexBuilder { self . hir_config . flags . crlf = yes ; self } # [doc = " This configures swap-greed mode for the entire pattern."] # [doc = ""] # [doc = " When swap-greed mode is enabled, patterns like `a+` will become"] # [doc = " non-greedy and patterns like `a+?` will become greedy. In other words,"] # [doc = " the meanings of `a+` and `a+?` are switched."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `U` in the"] # [doc = " pattern."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::RegexBuilder;"] # [doc = ""] # [doc = " let re = RegexBuilder::new(r\"a+\")"] # [doc = "     .swap_greed(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = " assert_eq!(Some(\"a\"), re.find(\"aaa\").map(|m| m.as_str()));"] # [doc = " ```"] pub fn swap_greed (& mut self , yes : bool) -> & mut RegexBuilder { self . hir_config . flags . swap_greed = yes ; self } # [doc = " This configures verbose mode for the entire pattern."] # [doc = ""] # [doc = " When enabled, whitespace will treated as insignificant in the pattern"] # [doc = " and `#` can be used to start a comment until the next new line."] # [doc = ""] # [doc = " Normally, in most places in a pattern, whitespace is treated literally."] # [doc = " For example ` +` will match one or more ASCII whitespace characters."] # [doc = ""] # [doc = " When verbose mode is enabled, `\\#` can be used to match a literal `#`"] # [doc = " and `\\ ` can be used to match a literal ASCII whitespace character."] # [doc = ""] # [doc = " Verbose mode is useful for permitting regexes to be formatted and"] # [doc = " broken up more nicely. This may make them more easily readable."] # [doc = ""] # [doc = " This setting can also be configured using the inline flag `x` in the"] # [doc = " pattern."] # [doc = ""] # [doc = " The default for this is `false`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::RegexBuilder;"] # [doc = ""] # [doc = " let pat = r\""] # [doc = "     \\b"] # [doc = "     (?<first>[A-Z]\\w*)  # always start with uppercase letter"] # [doc = "     \\s+                 # whitespace should separate names"] # [doc = "     (?: # middle name can be an initial!"] # [doc = "         (?:(?<initial>[A-Z])\\.|(?<middle>[A-Z]\\w*))"] # [doc = "         \\s+"] # [doc = "     )?"] # [doc = "     (?<last>[A-Z]\\w*)"] # [doc = "     \\b"] # [doc = " \";"] # [doc = " let re = RegexBuilder::new(pat)"] # [doc = "     .ignore_whitespace(true)"] # [doc = "     .build()"] # [doc = "     .unwrap();"] # [doc = ""] # [doc = " let caps = re.captures(\"Harry Potter\").unwrap();"] # [doc = " assert_eq!(\"Harry\", &caps[\"first\"]);"] # [doc = " assert_eq!(\"Potter\", &caps[\"last\"]);"] # [doc = ""] # [doc = " let caps = re.captures(\"Harry J. Potter\").unwrap();"] # [doc = " assert_eq!(\"Harry\", &caps[\"first\"]);"] # [doc = " // Since a middle name/initial isn't required for an overall match,"] # [doc = " // we can't assume that 'initial' or 'middle' will be populated!"] # [doc = " assert_eq!(Some(\"J\"), caps.name(\"initial\").map(|m| m.as_str()));"] # [doc = " assert_eq!(None, caps.name(\"middle\").map(|m| m.as_str()));"] # [doc = " assert_eq!(\"Potter\", &caps[\"last\"]);"] # [doc = ""] # [doc = " let caps = re.captures(\"Harry James Potter\").unwrap();"] # [doc = " assert_eq!(\"Harry\", &caps[\"first\"]);"] # [doc = " // Since a middle name/initial isn't required for an overall match,"] # [doc = " // we can't assume that 'initial' or 'middle' will be populated!"] # [doc = " assert_eq!(None, caps.name(\"initial\").map(|m| m.as_str()));"] # [doc = " assert_eq!(Some(\"James\"), caps.name(\"middle\").map(|m| m.as_str()));"] # [doc = " assert_eq!(\"Potter\", &caps[\"last\"]);"] # [doc = " ```"] pub fn ignore_whitespace (& mut self , yes : bool) -> & mut RegexBuilder { self . hir_config . flags . ignore_whitespace = yes ; self } # [doc = " Sets the approximate size limit, in bytes, of the compiled regex."] # [doc = ""] # [doc = " This roughly corresponds to the number of heap memory, in bytes,"] # [doc = " occupied by a single regex. If the regex would otherwise approximately"] # [doc = " exceed this limit, then compiling that regex will fail."] # [doc = ""] # [doc = " The main utility of a method like this is to avoid compiling regexes"] # [doc = " that use an unexpected amount of resources, such as time and memory."] # [doc = " Even if the memory usage of a large regex is acceptable, its search"] # [doc = " time may not be. Namely, worst case time complexity for search is `O(m"] # [doc = " * n)`, where `m ~ len(pattern)` and `n ~ len(haystack)`. That is,"] # [doc = " search time depends, in part, on the size of the compiled regex. This"] # [doc = " means that putting a limit on the size of the regex limits how much a"] # [doc = " regex can impact search time."] # [doc = ""] # [doc = " The default for this is some reasonable number that permits most"] # [doc = " patterns to compile successfully."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::RegexBuilder;"] # [doc = ""] # [doc = " assert!(RegexBuilder::new(r\"\\w\").size_limit(100).build().is_err());"] # [doc = " ```"] pub fn size_limit (& mut self , limit : usize) -> & mut RegexBuilder { self . nfa_config . size_limit = Some (limit) ; self } # [doc = " Set the nesting limit for this parser."] # [doc = ""] # [doc = " The nesting limit controls how deep the abstract syntax tree is allowed"] # [doc = " to be. If the AST exceeds the given limit (e.g., with too many nested"] # [doc = " groups), then an error is returned by the parser."] # [doc = ""] # [doc = " The purpose of this limit is to act as a heuristic to prevent stack"] # [doc = " overflow for consumers that do structural induction on an AST using"] # [doc = " explicit recursion. While this crate never does this (instead using"] # [doc = " constant stack space and moving the call stack to the heap), other"] # [doc = " crates may."] # [doc = ""] # [doc = " This limit is not checked until the entire AST is parsed. Therefore, if"] # [doc = " callers want to put a limit on the amount of heap space used, then they"] # [doc = " should impose a limit on the length, in bytes, of the concrete pattern"] # [doc = " string. In particular, this is viable since this parser implementation"] # [doc = " will limit itself to heap space proportional to the length of the"] # [doc = " pattern string. See also the [untrusted inputs](crate#untrusted-input)"] # [doc = " section in the top-level crate documentation for more information about"] # [doc = " this."] # [doc = ""] # [doc = " Note that a nest limit of `0` will return a nest limit error for most"] # [doc = " patterns but not all. For example, a nest limit of `0` permits `a` but"] # [doc = " not `ab`, since `ab` requires an explicit concatenation, which results"] # [doc = " in a nest depth of `1`. In general, a nest limit is not something that"] # [doc = " manifests in an obvious way in the concrete syntax, therefore, it"] # [doc = " should not be used in a granular way."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::RegexBuilder;"] # [doc = ""] # [doc = " assert!(RegexBuilder::new(r\"\").nest_limit(0).build().is_ok());"] # [doc = " assert!(RegexBuilder::new(r\"a\").nest_limit(0).build().is_ok());"] # [doc = " assert!(RegexBuilder::new(r\"(a)\").nest_limit(0).build().is_err());"] # [doc = " ```"] pub fn nest_limit (& mut self , limit : u32) -> & mut RegexBuilder { self . hir_config . nest_limit = limit ; self } }
};
}
