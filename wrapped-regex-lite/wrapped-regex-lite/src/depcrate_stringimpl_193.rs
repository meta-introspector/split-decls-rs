// Generated macro for impl_193 (impl)
macro_rules! Depcrate_stringimpl_193 {
() => {
// Module: crate::string
// Provides: {"impl_193"}
// Dependencies: {}
# [doc = " Core regular expression methods."] impl Regex { # [doc = " Compiles a regular expression. Once compiled, it can be used repeatedly"] # [doc = " to search, split or replace substrings in a haystack."] # [doc = ""] # [doc = " Note that regex compilation tends to be a somewhat expensive process,"] # [doc = " and unlike higher level environments, compilation is not automatically"] # [doc = " cached for you. One should endeavor to compile a regex once and then"] # [doc = " reuse it. For example, it's a bad idea to compile the same regex"] # [doc = " repeatedly in a loop."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If an invalid pattern is given, then an error is returned."] # [doc = " An error is also returned if the pattern is valid, but would"] # [doc = " produce a regex that is bigger than the configured size limit via"] # [doc = " [`RegexBuilder::size_limit`]. (A reasonable size limit is enabled by"] # [doc = " default.)"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " // An Invalid pattern because of an unclosed parenthesis"] # [doc = " assert!(Regex::new(r\"foo(bar\").is_err());"] # [doc = " // An invalid pattern because the regex would be too big"] # [doc = " // because Unicode tends to inflate things."] # [doc = " assert!(Regex::new(r\"\\w{1000000}\").is_err());"] # [doc = " ```"] pub fn new (pattern : & str) -> Result < Regex , Error > { RegexBuilder :: new (pattern) . build () } # [doc = " Returns true if and only if there is a match for the regex anywhere"] # [doc = " in the haystack given."] # [doc = ""] # [doc = " It is recommended to use this method if all you need to do is test"] # [doc = " whether a match exists, since the underlying matching engine may be"] # [doc = " able to do less work."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Test if some haystack contains at least one word with exactly 13"] # [doc = " word characters:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\\b\\w{13}\\b\").unwrap();"] # [doc = " let hay = \"I categorically deny having triskaidekaphobia.\";"] # [doc = " assert!(re.is_match(hay));"] # [doc = " ```"] # [inline] pub fn is_match (& self , haystack : & str) -> bool { self . is_match_at (haystack , 0) } # [doc = " This routine searches for the first match of this regex in the"] # [doc = " haystack given, and if found, returns a [`Match`]. The `Match`"] # [doc = " provides access to both the byte offsets of the match and the actual"] # [doc = " substring that matched."] # [doc = ""] # [doc = " Note that this should only be used if you want to find the entire"] # [doc = " match. If instead you just want to test the existence of a match,"] # [doc = " it's potentially faster to use `Regex::is_match(hay)` instead of"] # [doc = " `Regex::find(hay).is_some()`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Find the first word with exactly 13 word characters:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\\b\\w{13}\\b\").unwrap();"] # [doc = " let hay = \"I categorically deny having triskaidekaphobia.\";"] # [doc = " let mat = re.find(hay).unwrap();"] # [doc = " assert_eq!(2..15, mat.range());"] # [doc = " assert_eq!(\"categorically\", mat.as_str());"] # [doc = " ```"] # [inline] pub fn find < 'h > (& self , haystack : & 'h str) -> Option < Match < 'h > > { self . find_at (haystack , 0) } # [doc = " Returns an iterator that yields successive non-overlapping matches in"] # [doc = " the given haystack. The iterator yields values of type [`Match`]."] # [doc = ""] # [doc = " # Time complexity"] # [doc = ""] # [doc = " Note that since `find_iter` runs potentially many searches on the"] # [doc = " haystack and since each search has worst case `O(m * n)` time"] # [doc = " complexity, the overall worst case time complexity for iteration is"] # [doc = " `O(m * n^2)`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Find every word with exactly 13 word characters:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\\b\\w{13}\\b\").unwrap();"] # [doc = " let hay = \"Retroactively relinquishing remunerations is reprehensible.\";"] # [doc = " let matches: Vec<_> = re.find_iter(hay).map(|m| m.as_str()).collect();"] # [doc = " assert_eq!(matches, vec!["] # [doc = "     \"Retroactively\","] # [doc = "     \"relinquishing\","] # [doc = "     \"remunerations\","] # [doc = "     \"reprehensible\","] # [doc = " ]);"] # [doc = " ```"] # [inline] pub fn find_iter < 'r , 'h > (& 'r self , haystack : & 'h str) -> Matches < 'r , 'h > { Matches { haystack , it : self . pikevm . find_iter (self . pool . get () , haystack . as_bytes ()) , } } # [doc = " This routine searches for the first match of this regex in the haystack"] # [doc = " given, and if found, returns not only the overall match but also the"] # [doc = " matches of each capture group in the regex. If no match is found, then"] # [doc = " `None` is returned."] # [doc = ""] # [doc = " Capture group `0` always corresponds to an implicit unnamed group that"] # [doc = " includes the entire match. If a match is found, this group is always"] # [doc = " present. Subsequent groups may be named and are numbered, starting"] # [doc = " at 1, by the order in which the opening parenthesis appears in the"] # [doc = " pattern. For example, in the pattern `(?<a>.(?<b>.))(?<c>.)`, `a`,"] # [doc = " `b` and `c` correspond to capture group indices `1`, `2` and `3`,"] # [doc = " respectively."] # [doc = ""] # [doc = " You should only use `captures` if you need access to the capture group"] # [doc = " matches. Otherwise, [`Regex::find`] is generally faster for discovering"] # [doc = " just the overall match."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Say you have some haystack with movie names and their release years,"] # [doc = " like \"'Citizen Kane' (1941)\". It'd be nice if we could search for"] # [doc = " substrings looking like that, while also extracting the movie name and"] # [doc = " its release year separately. The example below shows how to do that."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"'([^']+)'\\s+\\((\\d{4})\\)\").unwrap();"] # [doc = " let hay = \"Not my favorite movie: 'Citizen Kane' (1941).\";"] # [doc = " let caps = re.captures(hay).unwrap();"] # [doc = " assert_eq!(caps.get(0).unwrap().as_str(), \"'Citizen Kane' (1941)\");"] # [doc = " assert_eq!(caps.get(1).unwrap().as_str(), \"Citizen Kane\");"] # [doc = " assert_eq!(caps.get(2).unwrap().as_str(), \"1941\");"] # [doc = " // You can also access the groups by index using the Index notation."] # [doc = " // Note that this will panic on an invalid index. In this case, these"] # [doc = " // accesses are always correct because the overall regex will only"] # [doc = " // match when these capture groups match."] # [doc = " assert_eq!(&caps[0], \"'Citizen Kane' (1941)\");"] # [doc = " assert_eq!(&caps[1], \"Citizen Kane\");"] # [doc = " assert_eq!(&caps[2], \"1941\");"] # [doc = " ```"] # [doc = ""] # [doc = " Note that the full match is at capture group `0`. Each subsequent"] # [doc = " capture group is indexed by the order of its opening `(`."] # [doc = ""] # [doc = " We can make this example a bit clearer by using *named* capture groups:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"'(?<title>[^']+)'\\s+\\((?<year>\\d{4})\\)\").unwrap();"] # [doc = " let hay = \"Not my favorite movie: 'Citizen Kane' (1941).\";"] # [doc = " let caps = re.captures(hay).unwrap();"] # [doc = " assert_eq!(caps.get(0).unwrap().as_str(), \"'Citizen Kane' (1941)\");"] # [doc = " assert_eq!(caps.name(\"title\").unwrap().as_str(), \"Citizen Kane\");"] # [doc = " assert_eq!(caps.name(\"year\").unwrap().as_str(), \"1941\");"] # [doc = " // You can also access the groups by name using the Index notation."] # [doc = " // Note that this will panic on an invalid group name. In this case,"] # [doc = " // these accesses are always correct because the overall regex will"] # [doc = " // only match when these capture groups match."] # [doc = " assert_eq!(&caps[0], \"'Citizen Kane' (1941)\");"] # [doc = " assert_eq!(&caps[\"title\"], \"Citizen Kane\");"] # [doc = " assert_eq!(&caps[\"year\"], \"1941\");"] # [doc = " ```"] # [doc = ""] # [doc = " Here we name the capture groups, which we can access with the `name`"] # [doc = " method or the `Index` notation with a `&str`. Note that the named"] # [doc = " capture groups are still accessible with `get` or the `Index` notation"] # [doc = " with a `usize`."] # [doc = ""] # [doc = " The `0`th capture group is always unnamed, so it must always be"] # [doc = " accessed with `get(0)` or `[0]`."] # [doc = ""] # [doc = " Finally, one other way to get the matched substrings is with the"] # [doc = " [`Captures::extract`] API:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"'([^']+)'\\s+\\((\\d{4})\\)\").unwrap();"] # [doc = " let hay = \"Not my favorite movie: 'Citizen Kane' (1941).\";"] # [doc = " let (full, [title, year]) = re.captures(hay).unwrap().extract();"] # [doc = " assert_eq!(full, \"'Citizen Kane' (1941)\");"] # [doc = " assert_eq!(title, \"Citizen Kane\");"] # [doc = " assert_eq!(year, \"1941\");"] # [doc = " ```"] # [inline] pub fn captures < 'h > (& self , haystack : & 'h str) -> Option < Captures < 'h > > { self . captures_at (haystack , 0) } # [doc = " Returns an iterator that yields successive non-overlapping matches in"] # [doc = " the given haystack. The iterator yields values of type [`Captures`]."] # [doc = ""] # [doc = " This is the same as [`Regex::find_iter`], but instead of only providing"] # [doc = " access to the overall match, each value yield includes access to the"] # [doc = " matches of all capture groups in the regex. Reporting this extra match"] # [doc = " data is potentially costly, so callers should only use `captures_iter`"] # [doc = " over `find_iter` when they actually need access to the capture group"] # [doc = " matches."] # [doc = ""] # [doc = " # Time complexity"] # [doc = ""] # [doc = " Note that since `captures_iter` runs potentially many searches on the"] # [doc = " haystack and since each search has worst case `O(m * n)` time"] # [doc = " complexity, the overall worst case time complexity for iteration is"] # [doc = " `O(m * n^2)`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " We can use this to find all movie titles and their release years in"] # [doc = " some haystack, where the movie is formatted like \"'Title' (xxxx)\":"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"'([^']+)'\\s+\\(([0-9]{4})\\)\").unwrap();"] # [doc = " let hay = \"'Citizen Kane' (1941), 'The Wizard of Oz' (1939), 'M' (1931).\";"] # [doc = " let mut movies = vec![];"] # [doc = " for (_, [title, year]) in re.captures_iter(hay).map(|c| c.extract()) {"] # [doc = "     movies.push((title, year.parse::<i64>()?));"] # [doc = " }"] # [doc = " assert_eq!(movies, vec!["] # [doc = "     (\"Citizen Kane\", 1941),"] # [doc = "     (\"The Wizard of Oz\", 1939),"] # [doc = "     (\"M\", 1931),"] # [doc = " ]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Or with named groups:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"'(?<title>[^']+)'\\s+\\((?<year>[0-9]{4})\\)\").unwrap();"] # [doc = " let hay = \"'Citizen Kane' (1941), 'The Wizard of Oz' (1939), 'M' (1931).\";"] # [doc = " let mut it = re.captures_iter(hay);"] # [doc = ""] # [doc = " let caps = it.next().unwrap();"] # [doc = " assert_eq!(&caps[\"title\"], \"Citizen Kane\");"] # [doc = " assert_eq!(&caps[\"year\"], \"1941\");"] # [doc = ""] # [doc = " let caps = it.next().unwrap();"] # [doc = " assert_eq!(&caps[\"title\"], \"The Wizard of Oz\");"] # [doc = " assert_eq!(&caps[\"year\"], \"1939\");"] # [doc = ""] # [doc = " let caps = it.next().unwrap();"] # [doc = " assert_eq!(&caps[\"title\"], \"M\");"] # [doc = " assert_eq!(&caps[\"year\"], \"1931\");"] # [doc = " ```"] # [inline] pub fn captures_iter < 'r , 'h > (& 'r self , haystack : & 'h str ,) -> CaptureMatches < 'r , 'h > { CaptureMatches { haystack , re : self , it : self . pikevm . captures_iter (self . pool . get () , haystack . as_bytes ()) , } } # [doc = " Returns an iterator of substrings of the haystack given, delimited by a"] # [doc = " match of the regex. Namely, each element of the iterator corresponds to"] # [doc = " a part of the haystack that *isn't* matched by the regular expression."] # [doc = ""] # [doc = " # Time complexity"] # [doc = ""] # [doc = " Since iterators over all matches requires running potentially many"] # [doc = " searches on the haystack, and since each search has worst case"] # [doc = " `O(m * n)` time complexity, the overall worst case time complexity for"] # [doc = " this routine is `O(m * n^2)`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " To split a string delimited by arbitrary amounts of spaces or tabs:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"[ \\t]+\").unwrap();"] # [doc = " let hay = \"a b \\t  c\\td    e\";"] # [doc = " let fields: Vec<&str> = re.split(hay).collect();"] # [doc = " assert_eq!(fields, vec![\"a\", \"b\", \"c\", \"d\", \"e\"]);"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: more cases"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\" \").unwrap();"] # [doc = " let hay = \"Mary had a little lamb\";"] # [doc = " let got: Vec<&str> = re.split(hay).collect();"] # [doc = " assert_eq!(got, vec![\"Mary\", \"had\", \"a\", \"little\", \"lamb\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\").unwrap();"] # [doc = " let hay = \"\";"] # [doc = " let got: Vec<&str> = re.split(hay).collect();"] # [doc = " assert_eq!(got, vec![\"\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\").unwrap();"] # [doc = " let hay = \"lionXXtigerXleopard\";"] # [doc = " let got: Vec<&str> = re.split(hay).collect();"] # [doc = " assert_eq!(got, vec![\"lion\", \"\", \"tiger\", \"leopard\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"::\").unwrap();"] # [doc = " let hay = \"lion::tiger::leopard\";"] # [doc = " let got: Vec<&str> = re.split(hay).collect();"] # [doc = " assert_eq!(got, vec![\"lion\", \"tiger\", \"leopard\"]);"] # [doc = " ```"] # [doc = ""] # [doc = " If a haystack contains multiple contiguous matches, you will end up"] # [doc = " with empty spans yielded by the iterator:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\").unwrap();"] # [doc = " let hay = \"XXXXaXXbXc\";"] # [doc = " let got: Vec<&str> = re.split(hay).collect();"] # [doc = " assert_eq!(got, vec![\"\", \"\", \"\", \"\", \"a\", \"\", \"b\", \"c\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"/\").unwrap();"] # [doc = " let hay = \"(///)\";"] # [doc = " let got: Vec<&str> = re.split(hay).collect();"] # [doc = " assert_eq!(got, vec![\"(\", \"\", \"\", \")\"]);"] # [doc = " ```"] # [doc = ""] # [doc = " Separators at the start or end of a haystack are neighbored by empty"] # [doc = " substring."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"0\").unwrap();"] # [doc = " let hay = \"010\";"] # [doc = " let got: Vec<&str> = re.split(hay).collect();"] # [doc = " assert_eq!(got, vec![\"\", \"1\", \"\"]);"] # [doc = " ```"] # [doc = ""] # [doc = " When the empty string is used as a regex, it splits at every valid"] # [doc = " UTF-8 boundary by default (which includes the beginning and end of the"] # [doc = " haystack):"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\").unwrap();"] # [doc = " let hay = \"rust\";"] # [doc = " let got: Vec<&str> = re.split(hay).collect();"] # [doc = " assert_eq!(got, vec![\"\", \"r\", \"u\", \"s\", \"t\", \"\"]);"] # [doc = ""] # [doc = " // Splitting by an empty string is UTF-8 aware by default!"] # [doc = " let re = Regex::new(r\"\").unwrap();"] # [doc = " let hay = \"☃\";"] # [doc = " let got: Vec<&str> = re.split(hay).collect();"] # [doc = " assert_eq!(got, vec![\"\", \"☃\", \"\"]);"] # [doc = " ```"] # [doc = ""] # [doc = " Contiguous separators (commonly shows up with whitespace), can lead to"] # [doc = " possibly surprising behavior. For example, this code is correct:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\" \").unwrap();"] # [doc = " let hay = \"    a  b c\";"] # [doc = " let got: Vec<&str> = re.split(hay).collect();"] # [doc = " assert_eq!(got, vec![\"\", \"\", \"\", \"\", \"a\", \"\", \"b\", \"c\"]);"] # [doc = " ```"] # [doc = ""] # [doc = " It does *not* give you `[\"a\", \"b\", \"c\"]`. For that behavior, you'd want"] # [doc = " to match contiguous space characters:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\" +\").unwrap();"] # [doc = " let hay = \"    a  b c\";"] # [doc = " let got: Vec<&str> = re.split(hay).collect();"] # [doc = " // N.B. This does still include a leading empty span because ' +'"] # [doc = " // matches at the beginning of the haystack."] # [doc = " assert_eq!(got, vec![\"\", \"a\", \"b\", \"c\"]);"] # [doc = " ```"] # [inline] pub fn split < 'r , 'h > (& 'r self , haystack : & 'h str) -> Split < 'r , 'h > { Split { haystack , finder : self . find_iter (haystack) , last : 0 } } # [doc = " Returns an iterator of at most `limit` substrings of the haystack"] # [doc = " given, delimited by a match of the regex. (A `limit` of `0` will return"] # [doc = " no substrings.) Namely, each element of the iterator corresponds to a"] # [doc = " part of the haystack that *isn't* matched by the regular expression."] # [doc = " The remainder of the haystack that is not split will be the last"] # [doc = " element in the iterator."] # [doc = ""] # [doc = " # Time complexity"] # [doc = ""] # [doc = " Since iterators over all matches requires running potentially many"] # [doc = " searches on the haystack, and since each search has worst case"] # [doc = " `O(m * n)` time complexity, the overall worst case time complexity for"] # [doc = " this routine is `O(m * n^2)`."] # [doc = ""] # [doc = " Although note that the worst case time here has an upper bound given"] # [doc = " by the `limit` parameter."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Get the first two words in some haystack:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"\\W+\").unwrap();"] # [doc = " let hay = \"Hey! How are you?\";"] # [doc = " let fields: Vec<&str> = re.splitn(hay, 3).collect();"] # [doc = " assert_eq!(fields, vec![\"Hey\", \"How\", \"are you?\"]);"] # [doc = " ```"] # [doc = ""] # [doc = " # Examples: more cases"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\" \").unwrap();"] # [doc = " let hay = \"Mary had a little lamb\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 3).collect();"] # [doc = " assert_eq!(got, vec![\"Mary\", \"had\", \"a little lamb\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\").unwrap();"] # [doc = " let hay = \"\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 3).collect();"] # [doc = " assert_eq!(got, vec![\"\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\").unwrap();"] # [doc = " let hay = \"lionXXtigerXleopard\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 3).collect();"] # [doc = " assert_eq!(got, vec![\"lion\", \"\", \"tigerXleopard\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"::\").unwrap();"] # [doc = " let hay = \"lion::tiger::leopard\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 2).collect();"] # [doc = " assert_eq!(got, vec![\"lion\", \"tiger::leopard\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\").unwrap();"] # [doc = " let hay = \"abcXdef\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 1).collect();"] # [doc = " assert_eq!(got, vec![\"abcXdef\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\").unwrap();"] # [doc = " let hay = \"abcdef\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 2).collect();"] # [doc = " assert_eq!(got, vec![\"abcdef\"]);"] # [doc = ""] # [doc = " let re = Regex::new(r\"X\").unwrap();"] # [doc = " let hay = \"abcXdef\";"] # [doc = " let got: Vec<&str> = re.splitn(hay, 0).collect();"] # [doc = " assert!(got.is_empty());"] # [doc = " ```"] # [inline] pub fn splitn < 'r , 'h > (& 'r self , haystack : & 'h str , limit : usize ,) -> SplitN < 'r , 'h > { SplitN { splits : self . split (haystack) , limit } } # [doc = " Replaces the leftmost-first match in the given haystack with the"] # [doc = " replacement provided. The replacement can be a regular string (where"] # [doc = " `$N` and `$name` are expanded to match capture groups) or a function"] # [doc = " that takes a [`Captures`] and returns the replaced string."] # [doc = ""] # [doc = " If no match is found, then the haystack is returned unchanged. In that"] # [doc = " case, this implementation will likely return a `Cow::Borrowed` value"] # [doc = " such that no allocation is performed."] # [doc = ""] # [doc = " # Replacement string syntax"] # [doc = ""] # [doc = " All instances of `$ref` in the replacement string are replaced with"] # [doc = " the substring corresponding to the capture group identified by `ref`."] # [doc = ""] # [doc = " `ref` may be an integer corresponding to the index of the capture group"] # [doc = " (counted by order of opening parenthesis where `0` is the entire match)"] # [doc = " or it can be a name (consisting of letters, digits or underscores)"] # [doc = " corresponding to a named capture group."] # [doc = ""] # [doc = " If `ref` isn't a valid capture group (whether the name doesn't exist or"] # [doc = " isn't a valid index), then it is replaced with the empty string."] # [doc = ""] # [doc = " The longest possible name is used. For example, `$1a` looks up the"] # [doc = " capture group named `1a` and not the capture group at index `1`. To"] # [doc = " exert more precise control over the name, use braces, e.g., `${1}a`."] # [doc = ""] # [doc = " To write a literal `$` use `$$`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Note that this function is polymorphic with respect to the replacement."] # [doc = " In typical usage, this can just be a normal string:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"[^01]+\").unwrap();"] # [doc = " assert_eq!(re.replace(\"1078910\", \"\"), \"1010\");"] # [doc = " ```"] # [doc = ""] # [doc = " But anything satisfying the [`Replacer`] trait will work. For example,"] # [doc = " a closure of type `|&Captures| -> String` provides direct access to the"] # [doc = " captures corresponding to a match. This allows one to access capturing"] # [doc = " group matches easily:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::{Captures, Regex};"] # [doc = ""] # [doc = " let re = Regex::new(r\"([^,\\s]+),\\s+(\\S+)\").unwrap();"] # [doc = " let result = re.replace(\"Springsteen, Bruce\", |caps: &Captures| {"] # [doc = "     format!(\"{} {}\", &caps[2], &caps[1])"] # [doc = " });"] # [doc = " assert_eq!(result, \"Bruce Springsteen\");"] # [doc = " ```"] # [doc = ""] # [doc = " But this is a bit cumbersome to use all the time. Instead, a simple"] # [doc = " syntax is supported (as described above) that expands `$name` into the"] # [doc = " corresponding capture group. Here's the last example, but using this"] # [doc = " expansion technique with named capture groups:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?<last>[^,\\s]+),\\s+(?<first>\\S+)\").unwrap();"] # [doc = " let result = re.replace(\"Springsteen, Bruce\", \"$first $last\");"] # [doc = " assert_eq!(result, \"Bruce Springsteen\");"] # [doc = " ```"] # [doc = ""] # [doc = " Note that using `$2` instead of `$first` or `$1` instead of `$last`"] # [doc = " would produce the same result. To write a literal `$` use `$$`."] # [doc = ""] # [doc = " Sometimes the replacement string requires use of curly braces to"] # [doc = " delineate a capture group replacement when it is adjacent to some other"] # [doc = " literal text. For example, if we wanted to join two words together with"] # [doc = " an underscore:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?<first>\\w+)\\s+(?<second>\\w+)\").unwrap();"] # [doc = " let result = re.replace(\"deep fried\", \"${first}_$second\");"] # [doc = " assert_eq!(result, \"deep_fried\");"] # [doc = " ```"] # [doc = ""] # [doc = " Without the curly braces, the capture group name `first_` would be"] # [doc = " used, and since it doesn't exist, it would be replaced with the empty"] # [doc = " string."] # [doc = ""] # [doc = " Finally, sometimes you just want to replace a literal string with no"] # [doc = " regard for capturing group expansion. This can be done by wrapping a"] # [doc = " string with [`NoExpand`]:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::{NoExpand, Regex};"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?<last>[^,\\s]+),\\s+(\\S+)\").unwrap();"] # [doc = " let result = re.replace(\"Springsteen, Bruce\", NoExpand(\"$2 $last\"));"] # [doc = " assert_eq!(result, \"$2 $last\");"] # [doc = " ```"] # [doc = ""] # [doc = " Using `NoExpand` may also be faster, since the replacement string won't"] # [doc = " need to be parsed for the `$` syntax."] # [inline] pub fn replace < 'h , R : Replacer > (& self , haystack : & 'h str , rep : R ,) -> Cow < 'h , str > { self . replacen (haystack , 1 , rep) } # [doc = " Replaces all non-overlapping matches in the haystack with the"] # [doc = " replacement provided. This is the same as calling `replacen` with"] # [doc = " `limit` set to `0`."] # [doc = ""] # [doc = " The documentation for [`Regex::replace`] goes into more detail about"] # [doc = " what kinds of replacement strings are supported."] # [doc = ""] # [doc = " # Time complexity"] # [doc = ""] # [doc = " Since iterators over all matches requires running potentially many"] # [doc = " searches on the haystack, and since each search has worst case"] # [doc = " `O(m * n)` time complexity, the overall worst case time complexity for"] # [doc = " this routine is `O(m * n^2)`."] # [doc = ""] # [doc = " # Fallibility"] # [doc = ""] # [doc = " If you need to write a replacement routine where any individual"] # [doc = " replacement might \"fail,\" doing so with this API isn't really feasible"] # [doc = " because there's no way to stop the search process if a replacement"] # [doc = " fails. Instead, if you need this functionality, you should consider"] # [doc = " implementing your own replacement routine:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::{Captures, Regex};"] # [doc = ""] # [doc = " fn replace_all<E>("] # [doc = "     re: &Regex,"] # [doc = "     haystack: &str,"] # [doc = "     replacement: impl Fn(&Captures) -> Result<String, E>,"] # [doc = " ) -> Result<String, E> {"] # [doc = "     let mut new = String::with_capacity(haystack.len());"] # [doc = "     let mut last_match = 0;"] # [doc = "     for caps in re.captures_iter(haystack) {"] # [doc = "         let m = caps.get(0).unwrap();"] # [doc = "         new.push_str(&haystack[last_match..m.start()]);"] # [doc = "         new.push_str(&replacement(&caps)?);"] # [doc = "         last_match = m.end();"] # [doc = "     }"] # [doc = "     new.push_str(&haystack[last_match..]);"] # [doc = "     Ok(new)"] # [doc = " }"] # [doc = ""] # [doc = " // Let's replace each word with the number of bytes in that word."] # [doc = " // But if we see a word that is \"too long,\" we'll give up."] # [doc = " let re = Regex::new(r\"\\w+\").unwrap();"] # [doc = " let replacement = |caps: &Captures| -> Result<String, &'static str> {"] # [doc = "     if caps[0].len() >= 5 {"] # [doc = "         return Err(\"word too long\");"] # [doc = "     }"] # [doc = "     Ok(caps[0].len().to_string())"] # [doc = " };"] # [doc = " assert_eq!("] # [doc = "     Ok(\"2 3 3 3?\".to_string()),"] # [doc = "     replace_all(&re, \"hi how are you?\", &replacement),"] # [doc = " );"] # [doc = " assert!(replace_all(&re, \"hi there\", &replacement).is_err());"] # [doc = " ```"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to flip the order of whitespace delimited"] # [doc = " fields, and normalizes the whitespace that delimits the fields:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?m)^(\\S+)\\s+(\\S+)$\").unwrap();"] # [doc = " let hay = \""] # [doc = " Greetings  1973"] # [doc = " Wild\\t1973"] # [doc = " BornToRun\\t\\t\\t\\t1975"] # [doc = " Darkness                    1978"] # [doc = " TheRiver 1980"] # [doc = " \";"] # [doc = " let new = re.replace_all(hay, \"$2 $1\");"] # [doc = " assert_eq!(new, \""] # [doc = " 1973 Greetings"] # [doc = " 1973 Wild"] # [doc = " 1975 BornToRun"] # [doc = " 1978 Darkness"] # [doc = " 1980 TheRiver"] # [doc = " \");"] # [doc = " ```"] # [inline] pub fn replace_all < 'h , R : Replacer > (& self , haystack : & 'h str , rep : R ,) -> Cow < 'h , str > { self . replacen (haystack , 0 , rep) } # [doc = " Replaces at most `limit` non-overlapping matches in the haystack with"] # [doc = " the replacement provided. If `limit` is `0`, then all non-overlapping"] # [doc = " matches are replaced. That is, `Regex::replace_all(hay, rep)` is"] # [doc = " equivalent to `Regex::replacen(hay, 0, rep)`."] # [doc = ""] # [doc = " The documentation for [`Regex::replace`] goes into more detail about"] # [doc = " what kinds of replacement strings are supported."] # [doc = ""] # [doc = " # Time complexity"] # [doc = ""] # [doc = " Since iterators over all matches requires running potentially many"] # [doc = " searches on the haystack, and since each search has worst case"] # [doc = " `O(m * n)` time complexity, the overall worst case time complexity for"] # [doc = " this routine is `O(m * n^2)`."] # [doc = ""] # [doc = " Although note that the worst case time here has an upper bound given"] # [doc = " by the `limit` parameter."] # [doc = ""] # [doc = " # Fallibility"] # [doc = ""] # [doc = " See the corresponding section in the docs for [`Regex::replace_all`]"] # [doc = " for tips on how to deal with a replacement routine that can fail."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to flip the order of whitespace delimited"] # [doc = " fields, and normalizes the whitespace that delimits the fields. But we"] # [doc = " only do it for the first two matches."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?m)^(\\S+)\\s+(\\S+)$\").unwrap();"] # [doc = " let hay = \""] # [doc = " Greetings  1973"] # [doc = " Wild\\t1973"] # [doc = " BornToRun\\t\\t\\t\\t1975"] # [doc = " Darkness                    1978"] # [doc = " TheRiver 1980"] # [doc = " \";"] # [doc = " let new = re.replacen(hay, 2, \"$2 $1\");"] # [doc = " assert_eq!(new, \""] # [doc = " 1973 Greetings"] # [doc = " 1973 Wild"] # [doc = " BornToRun\\t\\t\\t\\t1975"] # [doc = " Darkness                    1978"] # [doc = " TheRiver 1980"] # [doc = " \");"] # [doc = " ```"] # [inline] pub fn replacen < 'h , R : Replacer > (& self , haystack : & 'h str , limit : usize , mut rep : R ,) -> Cow < 'h , str > { if let Some (rep) = rep . no_expansion () { let mut it = self . find_iter (haystack) . enumerate () . peekable () ; if it . peek () . is_none () { return Cow :: Borrowed (haystack) ; } let mut new = String :: with_capacity (haystack . len ()) ; let mut last_match = 0 ; for (i , m) in it { new . push_str (& haystack [last_match .. m . start ()]) ; new . push_str (& rep) ; last_match = m . end () ; if limit > 0 && i >= limit - 1 { break ; } } new . push_str (& haystack [last_match ..]) ; return Cow :: Owned (new) ; } let mut it = self . captures_iter (haystack) . enumerate () . peekable () ; if it . peek () . is_none () { return Cow :: Borrowed (haystack) ; } let mut new = String :: with_capacity (haystack . len ()) ; let mut last_match = 0 ; for (i , cap) in it { let m = cap . get (0) . unwrap () ; new . push_str (& haystack [last_match .. m . start ()]) ; rep . replace_append (& cap , & mut new) ; last_match = m . end () ; if limit > 0 && i >= limit - 1 { break ; } } new . push_str (& haystack [last_match ..]) ; Cow :: Owned (new) } }
};
}
