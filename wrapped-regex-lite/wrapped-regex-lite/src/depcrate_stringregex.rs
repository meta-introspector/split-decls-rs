// Generated macro for Regex (struct)
macro_rules! Depcrate_stringRegex {
() => {
// Module: crate::string
// Provides: {"Regex"}
// Dependencies: {}
# [doc = " A compiled regular expression for searching Unicode haystacks."] # [doc = ""] # [doc = " A `Regex` can be used to search haystacks, split haystacks into substrings"] # [doc = " or replace substrings in a haystack with a different substring. All"] # [doc = " searching is done with an implicit `(?s:.)*?` at the beginning and end of"] # [doc = " an pattern. To force an expression to match the whole string (or a prefix"] # [doc = " or a suffix), you must use an anchor like `^` or `$` (or `\\A` and `\\z`)."] # [doc = ""] # [doc = " While this crate will handle Unicode strings (whether in the regular"] # [doc = " expression or in the haystack), all positions returned are **byte"] # [doc = " offsets**. Every byte offset is guaranteed to be at a Unicode code point"] # [doc = " boundary. That is, all offsets returned by the `Regex` API are guaranteed"] # [doc = " to be ranges that can slice a `&str` without panicking."] # [doc = ""] # [doc = " The only methods that allocate new strings are the string replacement"] # [doc = " methods. All other methods (searching and splitting) return borrowed"] # [doc = " references into the haystack given."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Find the offsets of a US phone number:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(\"[0-9]{3}-[0-9]{3}-[0-9]{4}\").unwrap();"] # [doc = " let m = re.find(\"phone: 111-222-3333\").unwrap();"] # [doc = " assert_eq!(7..19, m.range());"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: extracting capture groups"] # [doc = ""] # [doc = " A common way to use regexes is with capture groups. That is, instead of"] # [doc = " just looking for matches of an entire regex, parentheses are used to create"] # [doc = " groups that represent part of the match."] # [doc = ""] # [doc = " For example, consider a haystack with multiple lines, and each line has"] # [doc = " three whitespace delimited fields where the second field is expected to be"] # [doc = " a number and the third field a boolean. To make this convenient, we use"] # [doc = " the [`Captures::extract`] API to put the strings that match each group"] # [doc = " into a fixed size array:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_lite::Regex;"] # [doc = ""] # [doc = " let hay = \""] # [doc = " rabbit         54 true"] # [doc = " groundhog 2 true"] # [doc = " does not match"] # [doc = " fox   109    false"] # [doc = " \";"] # [doc = " let re = Regex::new(r\"(?m)^\\s*(\\S+)\\s+([0-9]+)\\s+(true|false)\\s*$\").unwrap();"] # [doc = " let mut fields: Vec<(&str, i64, bool)> = vec![];"] # [doc = " for (_, [f1, f2, f3]) in re.captures_iter(hay).map(|caps| caps.extract()) {"] # [doc = "     fields.push((f1, f2.parse()?, f3.parse()?));"] # [doc = " }"] # [doc = " assert_eq!(fields, vec!["] # [doc = "     (\"rabbit\", 54, true),"] # [doc = "     (\"groundhog\", 2, true),"] # [doc = "     (\"fox\", 109, false),"] # [doc = " ]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub struct Regex { pikevm : Arc < PikeVM > , pool : CachePool , }
};
}
