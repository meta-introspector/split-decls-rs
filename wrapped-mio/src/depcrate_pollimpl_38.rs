// Generated macro for impl_38 (impl)
macro_rules! Depcrate_pollimpl_38 {
() => {
// Module: crate::poll
// Provides: {"impl_38"}
// Dependencies: {}
impl Registry { # [doc = " Register an [`event::Source`] with the `Poll` instance."] # [doc = ""] # [doc = " Once registered, the `Poll` instance will monitor the event source for"] # [doc = " readiness state changes. When it notices a state change, it will return"] # [doc = " a readiness event for the handle the next time [`poll`] is called."] # [doc = ""] # [doc = " See [`Poll`] docs for a high level overview."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " `source: &mut S: event::Source`: This is the source of events that the"] # [doc = " `Poll` instance should monitor for readiness state changes."] # [doc = ""] # [doc = " `token: Token`: The caller picks a token to associate with the socket."] # [doc = " When [`poll`] returns an event for the handle, this token is included."] # [doc = " This allows the caller to map the event to its source. The token"] # [doc = " associated with the `event::Source` can be changed at any time by"] # [doc = " calling [`reregister`]."] # [doc = ""] # [doc = " See documentation on [`Token`] for an example showing how to pick"] # [doc = " [`Token`] values."] # [doc = ""] # [doc = " `interest: Interest`: Specifies which operations `Poll` should monitor"] # [doc = " for readiness. `Poll` will only return readiness events for operations"] # [doc = " specified by this argument."] # [doc = ""] # [doc = " If a socket is registered with readable interest and the socket becomes"] # [doc = " writable, no event will be returned from [`poll`]."] # [doc = ""] # [doc = " The readiness interest for an `event::Source` can be changed at any time"] # [doc = " by calling [`reregister`]."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " Callers must ensure that if a source being registered with a `Poll`"] # [doc = " instance was previously registered with that `Poll` instance, then a"] # [doc = " call to [`deregister`] has already occurred. Consecutive calls to"] # [doc = " `register` is unspecified behavior."] # [doc = ""] # [doc = " Unless otherwise specified, the caller should assume that once an event"] # [doc = " source is registered with a `Poll` instance, it is bound to that `Poll`"] # [doc = " instance for the lifetime of the event source. This remains true even"] # [doc = " if the event source is deregistered from the poll instance using"] # [doc = " [`deregister`]."] # [doc = ""] # [doc = " [`event::Source`]: ./event/trait.Source.html"] # [doc = " [`poll`]: struct.Poll.html#method.poll"] # [doc = " [`reregister`]: struct.Registry.html#method.reregister"] # [doc = " [`deregister`]: struct.Registry.html#method.deregister"] # [doc = " [`Token`]: struct.Token.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [cfg_attr (all (feature = "os-poll" , feature = "net") , doc = "```")] # [cfg_attr (not (all (feature = "os-poll" , feature = "net")) , doc = "```ignore")] # [doc = " # use std::error::Error;"] # [doc = " # use std::net;"] # [doc = " # fn main() -> Result<(), Box<dyn Error>> {"] # [doc = " use mio::{Events, Poll, Interest, Token};"] # [doc = " use mio::net::TcpStream;"] # [doc = " use std::net::SocketAddr;"] # [doc = " use std::time::{Duration, Instant};"] # [doc = ""] # [doc = " let mut poll = Poll::new()?;"] # [doc = ""] # [doc = " let address: SocketAddr = \"127.0.0.1:0\".parse()?;"] # [doc = " let listener = net::TcpListener::bind(address)?;"] # [doc = " let mut socket = TcpStream::connect(listener.local_addr()?)?;"] # [doc = ""] # [doc = " // Register the socket with `poll`"] # [doc = " poll.registry().register("] # [doc = "     &mut socket,"] # [doc = "     Token(0),"] # [doc = "     Interest::READABLE | Interest::WRITABLE)?;"] # [doc = ""] # [doc = " let mut events = Events::with_capacity(1024);"] # [doc = " let start = Instant::now();"] # [doc = " let timeout = Duration::from_millis(500);"] # [doc = ""] # [doc = " loop {"] # [doc = "     let elapsed = start.elapsed();"] # [doc = ""] # [doc = "     if elapsed >= timeout {"] # [doc = "         // Connection timed out"] # [doc = "         return Ok(());"] # [doc = "     }"] # [doc = ""] # [doc = "     let remaining = timeout - elapsed;"] # [doc = "     poll.poll(&mut events, Some(remaining))?;"] # [doc = ""] # [doc = "     for event in &events {"] # [doc = "         if event.token() == Token(0) {"] # [doc = "             // Something (probably) happened on the socket."] # [doc = "             return Ok(());"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " # }"] # [doc = " ```"] pub fn register < S > (& self , source : & mut S , token : Token , interests : Interest) -> io :: Result < () > where S : event :: Source + ? Sized , { trace ! ("registering event source with poller: token={:?}, interests={:?}" , token , interests) ; source . register (self , token , interests) } # [doc = " Re-register an [`event::Source`] with the `Poll` instance."] # [doc = ""] # [doc = " Re-registering an event source allows changing the details of the"] # [doc = " registration. Specifically, it allows updating the associated `token`"] # [doc = " and `interests` specified in previous `register` and `reregister` calls."] # [doc = ""] # [doc = " The `reregister` arguments fully override the previous values. In other"] # [doc = " words, if a socket is registered with [`readable`] interest and the call"] # [doc = " to `reregister` specifies [`writable`], then read interest is no longer"] # [doc = " requested for the handle."] # [doc = ""] # [doc = " The event source must have previously been registered with this instance"] # [doc = " of `Poll`, otherwise the behavior is unspecified."] # [doc = ""] # [doc = " See the [`register`] documentation for details about the function"] # [doc = " arguments and see the [`struct`] docs for a high level overview of"] # [doc = " polling."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [cfg_attr (all (feature = "os-poll" , feature = "net") , doc = "```")] # [cfg_attr (not (all (feature = "os-poll" , feature = "net")) , doc = "```ignore")] # [doc = " # use std::error::Error;"] # [doc = " # use std::net;"] # [doc = " # fn main() -> Result<(), Box<dyn Error>> {"] # [doc = " use mio::{Poll, Interest, Token};"] # [doc = " use mio::net::TcpStream;"] # [doc = " use std::net::SocketAddr;"] # [doc = ""] # [doc = " let poll = Poll::new()?;"] # [doc = ""] # [doc = " let address: SocketAddr = \"127.0.0.1:0\".parse()?;"] # [doc = " let listener = net::TcpListener::bind(address)?;"] # [doc = " let mut socket = TcpStream::connect(listener.local_addr()?)?;"] # [doc = ""] # [doc = " // Register the socket with `poll`, requesting readable"] # [doc = " poll.registry().register("] # [doc = "     &mut socket,"] # [doc = "     Token(0),"] # [doc = "     Interest::READABLE)?;"] # [doc = ""] # [doc = " // Reregister the socket specifying write interest instead. Even though"] # [doc = " // the token is the same it must be specified."] # [doc = " poll.registry().reregister("] # [doc = "     &mut socket,"] # [doc = "     Token(0),"] # [doc = "     Interest::WRITABLE)?;"] # [doc = " #     Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " [`event::Source`]: ./event/trait.Source.html"] # [doc = " [`struct`]: struct.Poll.html"] # [doc = " [`register`]: struct.Registry.html#method.register"] # [doc = " [`readable`]: ./event/struct.Event.html#is_readable"] # [doc = " [`writable`]: ./event/struct.Event.html#is_writable"] pub fn reregister < S > (& self , source : & mut S , token : Token , interests : Interest) -> io :: Result < () > where S : event :: Source + ? Sized , { trace ! ("reregistering event source with poller: token={:?}, interests={:?}" , token , interests) ; source . reregister (self , token , interests) } # [doc = " Deregister an [`event::Source`] with the `Poll` instance."] # [doc = ""] # [doc = " When an event source is deregistered, the `Poll` instance will no longer"] # [doc = " monitor it for readiness state changes. Deregistering clears up any"] # [doc = " internal resources needed to track the handle.  After an explicit call"] # [doc = " to this method completes, it is guaranteed that the token previously"] # [doc = " registered to this handle will not be returned by a future poll, so long"] # [doc = " as a happens-before relationship is established between this call and"] # [doc = " the poll."] # [doc = ""] # [doc = " The event source must have previously been registered with this instance"] # [doc = " of `Poll`, otherwise the behavior is unspecified."] # [doc = ""] # [doc = " A handle can be passed back to `register` after it has been"] # [doc = " deregistered; however, it must be passed back to the **same** `Poll`"] # [doc = " instance, otherwise the behavior is unspecified."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [cfg_attr (all (feature = "os-poll" , feature = "net") , doc = "```")] # [cfg_attr (not (all (feature = "os-poll" , feature = "net")) , doc = "```ignore")] # [doc = " # use std::error::Error;"] # [doc = " # use std::net;"] # [doc = " # fn main() -> Result<(), Box<dyn Error>> {"] # [doc = " use mio::{Events, Poll, Interest, Token};"] # [doc = " use mio::net::TcpStream;"] # [doc = " use std::net::SocketAddr;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " let mut poll = Poll::new()?;"] # [doc = ""] # [doc = " let address: SocketAddr = \"127.0.0.1:0\".parse()?;"] # [doc = " let listener = net::TcpListener::bind(address)?;"] # [doc = " let mut socket = TcpStream::connect(listener.local_addr()?)?;"] # [doc = ""] # [doc = " // Register the socket with `poll`"] # [doc = " poll.registry().register("] # [doc = "     &mut socket,"] # [doc = "     Token(0),"] # [doc = "     Interest::READABLE)?;"] # [doc = ""] # [doc = " poll.registry().deregister(&mut socket)?;"] # [doc = ""] # [doc = " let mut events = Events::with_capacity(1024);"] # [doc = ""] # [doc = " // Set a timeout because this poll should never receive any events."] # [doc = " poll.poll(&mut events, Some(Duration::from_secs(1)))?;"] # [doc = " assert!(events.is_empty());"] # [doc = " #     Ok(())"] # [doc = " # }"] # [doc = " ```"] pub fn deregister < S > (& self , source : & mut S) -> io :: Result < () > where S : event :: Source + ? Sized , { trace ! ("deregistering event source from poller") ; source . deregister (self) } # [doc = " Creates a new independently owned `Registry`."] # [doc = ""] # [doc = " Event sources registered with this `Registry` will be registered with"] # [doc = " the original `Registry` and `Poll` instance."] pub fn try_clone (& self) -> io :: Result < Registry > { self . selector . try_clone () . map (| selector | Registry { selector , # [cfg (all (debug_assertions , not (target_os = "wasi")))] has_waker : Arc :: clone (& self . has_waker) , }) } # [doc = " Internal check to ensure only a single `Waker` is active per [`Poll`]"] # [doc = " instance."] # [cfg (all (debug_assertions , not (target_os = "wasi")))] pub (crate) fn register_waker (& self) { assert ! (! self . has_waker . swap (true , Ordering :: AcqRel) , "Only a single `Waker` can be active per `Poll` instance") ; } # [doc = " Get access to the `sys::Selector`."] # [cfg (any (not (target_os = "wasi") , feature = "net"))] pub (crate) fn selector (& self) -> & sys :: Selector { & self . selector } }
};
}
