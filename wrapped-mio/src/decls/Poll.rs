macro_rules! deps {
    () => {
        Token!();
        Interest!();
        Events!();
        Registry!();
    };
}

macro_rules! Poll {
    () => {
        deps!();
        # [doc = " Polls for readiness events on all registered values."] # [doc = ""] # [doc = " `Poll` allows a program to monitor a large number of [`event::Source`]s,"] # [doc = " waiting until one or more become \"ready\" for some class of operations; e.g."] # [doc = " reading and writing. An event source is considered ready if it is possible"] # [doc = " to immediately perform a corresponding operation; e.g. [`read`] or"] # [doc = " [`write`]."] # [doc = ""] # [doc = " To use `Poll`, an `event::Source` must first be registered with the `Poll`"] # [doc = " instance using the [`register`] method on its associated `Register`,"] # [doc = " supplying readiness interest. The readiness interest tells `Poll` which"] # [doc = " specific operations on the handle to monitor for readiness. A `Token` is"] # [doc = " also passed to the [`register`] function. When `Poll` returns a readiness"] # [doc = " event, it will include this token.  This associates the event with the"] # [doc = " event source that generated the event."] # [doc = ""] # [doc = " [`event::Source`]: ./event/trait.Source.html"] # [doc = " [`read`]: ./net/struct.TcpStream.html#method.read"] # [doc = " [`write`]: ./net/struct.TcpStream.html#method.write"] # [doc = " [`register`]: struct.Registry.html#method.register"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " A basic example -- establishing a `TcpStream` connection."] # [doc = ""] # [cfg_attr (all (feature = "os-poll" , feature = "net") , doc = "```")] # [cfg_attr (not (all (feature = "os-poll" , feature = "net")) , doc = "```ignore")] # [doc = " # use std::error::Error;"] # [doc = " # fn main() -> Result<(), Box<dyn Error>> {"] # [doc = " use mio::{Events, Poll, Interest, Token};"] # [doc = " use mio::net::TcpStream;"] # [doc = ""] # [doc = " use std::net::{self, SocketAddr};"] # [doc = ""] # [doc = " // Bind a server socket to connect to."] # [doc = " let addr: SocketAddr = \"127.0.0.1:0\".parse()?;"] # [doc = " let server = net::TcpListener::bind(addr)?;"] # [doc = ""] # [doc = " // Construct a new `Poll` handle as well as the `Events` we'll store into"] # [doc = " let mut poll = Poll::new()?;"] # [doc = " let mut events = Events::with_capacity(1024);"] # [doc = ""] # [doc = " // Connect the stream"] # [doc = " let mut stream = TcpStream::connect(server.local_addr()?)?;"] # [doc = ""] # [doc = " // Register the stream with `Poll`"] # [doc = " poll.registry().register(&mut stream, Token(0), Interest::READABLE | Interest::WRITABLE)?;"] # [doc = ""] # [doc = " // Wait for the socket to become ready. This has to happens in a loop to"] # [doc = " // handle spurious wakeups."] # [doc = " loop {"] # [doc = "     poll.poll(&mut events, None)?;"] # [doc = ""] # [doc = "     for event in &events {"] # [doc = "         if event.token() == Token(0) && event.is_writable() {"] # [doc = "             // The socket connected (probably, it could still be a spurious"] # [doc = "             // wakeup)"] # [doc = "             return Ok(());"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " # Portability"] # [doc = ""] # [doc = " Using `Poll` provides a portable interface across supported platforms as"] # [doc = " long as the caller takes the following into consideration:"] # [doc = ""] # [doc = " ### Spurious events"] # [doc = ""] # [doc = " [`Poll::poll`] may return readiness events even if the associated"] # [doc = " event source is not actually ready. Given the same code, this may"] # [doc = " happen more on some platforms than others. It is important to never assume"] # [doc = " that, just because a readiness event was received, that the associated"] # [doc = " operation will succeed as well."] # [doc = ""] # [doc = " If operation fails with [`WouldBlock`], then the caller should not treat"] # [doc = " this as an error, but instead should wait until another readiness event is"] # [doc = " received."] # [doc = ""] # [doc = " ### Draining readiness"] # [doc = ""] # [doc = " Once a readiness event is received, the corresponding operation must be"] # [doc = " performed repeatedly until it returns [`WouldBlock`]. Unless this is done,"] # [doc = " there is no guarantee that another readiness event will be delivered, even"] # [doc = " if further data is received for the event source."] # [doc = ""] # [doc = " [`WouldBlock`]: std::io::ErrorKind::WouldBlock"] # [doc = ""] # [doc = " ### Readiness operations"] # [doc = ""] # [doc = " The only readiness operations that are guaranteed to be present on all"] # [doc = " supported platforms are [`readable`] and [`writable`]. All other readiness"] # [doc = " operations may have false negatives and as such should be considered"] # [doc = " **hints**. This means that if a socket is registered with [`readable`]"] # [doc = " interest and either an error or close is received, a readiness event will"] # [doc = " be generated for the socket, but it **may** only include `readable`"] # [doc = " readiness. Also note that, given the potential for spurious events,"] # [doc = " receiving a readiness event with `read_closed`, `write_closed`, or `error`"] # [doc = " doesn't actually mean that a `read` on the socket will return a result"] # [doc = " matching the readiness event."] # [doc = ""] # [doc = " In other words, portable programs that explicitly check for [`read_closed`],"] # [doc = " [`write_closed`], or [`error`] readiness should be doing so as an"] # [doc = " **optimization** and always be able to handle an error or close situation"] # [doc = " when performing the actual read operation."] # [doc = ""] # [doc = " [`readable`]: ./event/struct.Event.html#method.is_readable"] # [doc = " [`writable`]: ./event/struct.Event.html#method.is_writable"] # [doc = " [`error`]: ./event/struct.Event.html#method.is_error"] # [doc = " [`read_closed`]: ./event/struct.Event.html#method.is_read_closed"] # [doc = " [`write_closed`]: ./event/struct.Event.html#method.is_write_closed"] # [doc = ""] # [doc = " ### Registering handles"] # [doc = ""] # [doc = " Unless otherwise noted, it should be assumed that types implementing"] # [doc = " [`event::Source`] will never become ready unless they are registered with"] # [doc = " `Poll`."] # [doc = ""] # [doc = " For example:"] # [doc = ""] # [cfg_attr (all (feature = "os-poll" , feature = "net") , doc = "```")] # [cfg_attr (not (all (feature = "os-poll" , feature = "net")) , doc = "```ignore")] # [doc = " # use std::error::Error;"] # [doc = " # use std::net;"] # [doc = " # fn main() -> Result<(), Box<dyn Error>> {"] # [doc = " use mio::{Poll, Interest, Token};"] # [doc = " use mio::net::TcpStream;"] # [doc = " use std::net::SocketAddr;"] # [doc = " use std::time::Duration;"] # [doc = " use std::thread;"] # [doc = ""] # [doc = " let address: SocketAddr = \"127.0.0.1:0\".parse()?;"] # [doc = " let listener = net::TcpListener::bind(address)?;"] # [doc = " let mut sock = TcpStream::connect(listener.local_addr()?)?;"] # [doc = ""] # [doc = " thread::sleep(Duration::from_secs(1));"] # [doc = ""] # [doc = " let poll = Poll::new()?;"] # [doc = ""] # [doc = " // The connect is not guaranteed to have started until it is registered at"] # [doc = " // this point"] # [doc = " poll.registry().register(&mut sock, Token(0), Interest::READABLE | Interest::WRITABLE)?;"] # [doc = " #     Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " ### Dropping `Poll`"] # [doc = ""] # [doc = " When the `Poll` instance is dropped it may cancel in-flight operations for"] # [doc = " the registered [event sources], meaning that no further events for them may"] # [doc = " be received. It also means operations on the registered event sources may no"] # [doc = " longer work. It is up to the user to keep the `Poll` instance alive while"] # [doc = " registered event sources are being used."] # [doc = ""] # [doc = " [event sources]: ./event/trait.Source.html"] # [doc = ""] # [doc = " ### Accessing raw fd/socket/handle"] # [doc = ""] # [doc = " Mio makes it possible for many types to be converted into a raw file"] # [doc = " descriptor (fd, Unix), socket (Windows) or handle (Windows). This makes it"] # [doc = " possible to support more operations on the type than Mio supports, for"] # [doc = " example it makes [mio-aio] possible. However accessing the raw fd is not"] # [doc = " without it's pitfalls."] # [doc = ""] # [doc = " Specifically performing I/O operations outside of Mio on these types (via"] # [doc = " the raw fd) has unspecified behaviour. It could cause no more events to be"] # [doc = " generated for the type even though it returned `WouldBlock` (in an operation"] # [doc = " directly accessing the fd). The behaviour is OS specific and Mio can only"] # [doc = " guarantee cross-platform behaviour if it can control the I/O."] # [doc = ""] # [doc = " [mio-aio]: https://github.com/asomers/mio-aio"] # [doc = ""] # [doc = " *The following is **not** guaranteed, just a description of the current"] # [doc = " situation!* Mio is allowed to change the following without it being considered"] # [doc = " a breaking change, don't depend on this, it's just here to inform the user."] # [doc = " Currently the kqueue and epoll implementation support direct I/O operations"] # [doc = " on the fd without Mio's knowledge. Windows however needs **all** I/O"] # [doc = " operations to go through Mio otherwise it is not able to update it's"] # [doc = " internal state properly and won't generate events."] # [doc = ""] # [doc = " ### Polling without registering event sources"] # [doc = ""] # [doc = ""] # [doc = " *The following is **not** guaranteed, just a description of the current"] # [doc = " situation!* Mio is allowed to change the following without it being"] # [doc = " considered a breaking change, don't depend on this, it's just here to inform"] # [doc = " the user. On platforms that use epoll, kqueue or IOCP (see implementation"] # [doc = " notes below) polling without previously registering [event sources] will"] # [doc = " result in sleeping forever, only a process signal will be able to wake up"] # [doc = " the thread."] # [doc = ""] # [doc = " On WASM/WASI this is different as it doesn't support process signals,"] # [doc = " furthermore the WASI specification doesn't specify a behaviour in this"] # [doc = " situation, thus it's up to the implementation what to do here. As an"] # [doc = " example, the wasmtime runtime will return `EINVAL` in this situation, but"] # [doc = " different runtimes may return different results. If you have further"] # [doc = " insights or thoughts about this situation (and/or how Mio should handle it)"] # [doc = " please add you comment to [pull request#1580]."] # [doc = ""] # [doc = " [event sources]: crate::event::Source"] # [doc = " [pull request#1580]: https://github.com/tokio-rs/mio/pull/1580"] # [doc = ""] # [doc = " # Implementation notes"] # [doc = ""] # [doc = " `Poll` is backed by the selector provided by the operating system."] # [doc = ""] # [doc = " |      OS       |  Selector |"] # [doc = " |---------------|-----------|"] # [doc = " | Android       | [epoll]   |"] # [doc = " | DragonFly BSD | [kqueue]  |"] # [doc = " | FreeBSD       | [kqueue]  |"] # [doc = " | iOS           | [kqueue]  |"] # [doc = " | illumos       | [epoll]   |"] # [doc = " | Linux         | [epoll]   |"] # [doc = " | NetBSD        | [kqueue]  |"] # [doc = " | OpenBSD       | [kqueue]  |"] # [doc = " | Windows       | [IOCP]    |"] # [doc = " | macOS         | [kqueue]  |"] # [doc = ""] # [doc = " On all supported platforms, socket operations are handled by using the"] # [doc = " system selector. Platform specific extensions (e.g. [`SourceFd`]) allow"] # [doc = " accessing other features provided by individual system selectors. For"] # [doc = " example, Linux's [`signalfd`] feature can be used by registering the FD with"] # [doc = " `Poll` via [`SourceFd`]."] # [doc = ""] # [doc = " On all platforms except windows, a call to [`Poll::poll`] is mostly just a"] # [doc = " direct call to the system selector. However, [IOCP] uses a completion model"] # [doc = " instead of a readiness model. In this case, `Poll` must adapt the completion"] # [doc = " model Mio's API. While non-trivial, the bridge layer is still quite"] # [doc = " efficient. The most expensive part being calls to `read` and `write` require"] # [doc = " data to be copied into an intermediate buffer before it is passed to the"] # [doc = " kernel."] # [doc = ""] # [doc = " [epoll]: https://man7.org/linux/man-pages/man7/epoll.7.html"] # [doc = " [kqueue]: https://www.freebsd.org/cgi/man.cgi?query=kqueue&sektion=2"] # [doc = " [IOCP]: https://docs.microsoft.com/en-us/windows/win32/fileio/i-o-completion-ports"] # [doc = " [`signalfd`]: https://man7.org/linux/man-pages/man2/signalfd.2.html"] # [doc = " [`SourceFd`]: unix/struct.SourceFd.html"] # [doc = " [`Poll::poll`]: struct.Poll.html#method.poll"] pub struct Poll { registry : Registry , }
    };
}

Poll!();