macro_rules! find {
    () => {
        # [doc = " Attempts to find a tool within an MSVC installation using the Windows"] # [doc = " registry as a point to search from."] # [doc = ""] # [doc = " The `arch_or_target` argument is the architecture or the Rust target name"] # [doc = " that the tool should work for (e.g. compile or link for). The supported"] # [doc = " architecture names are:"] # [doc = " - `\"x64\"` or `\"x86_64\"`"] # [doc = " - `\"arm64\"` or `\"aarch64\"`"] # [doc = " - `\"arm64ec\"`"] # [doc = " - `\"x86\"`, `\"i586\"` or `\"i686\"`"] # [doc = " - `\"arm\"` or `\"thumbv7a\"`"] # [doc = ""] # [doc = " The `tool` argument is the tool to find. Supported tools include:"] # [doc = " - MSVC tools: `cl.exe`, `link.exe`, `lib.exe`, etc."] # [doc = " - `MSBuild`: `msbuild.exe`"] # [doc = " - Visual Studio IDE: `devenv.exe`"] # [doc = " - Clang/LLVM tools: `clang.exe`, `clang++.exe`, `clang-*.exe`, `llvm-*.exe`, `lld.exe`, etc."] # [doc = ""] # [doc = " This function will return `None` if the tool could not be found, or it will"] # [doc = " return `Some(cmd)` which represents a command that's ready to execute the"] # [doc = " tool with the appropriate environment variables set."] # [doc = ""] # [doc = " To find MSVC tools, this function will first attempt to detect if we are"] # [doc = " running in the context of a developer command prompt, and then use the tools"] # [doc = " as found in the current `PATH`. If that fails, it will attempt to locate"] # [doc = " the newest MSVC toolset in the newest installed version of Visual Studio."] # [doc = " To limit the search to a specific version of the MSVC toolset, set the"] # [doc = " VCToolsVersion environment variable to the desired version (e.g. \"14.44.35207\")."] # [doc = ""] # [doc = " Note that this function always returns `None` for non-MSVC targets (if a"] # [doc = " full target name was specified)."] pub fn find (arch_or_target : & str , tool : & str) -> Option < Command > { find_tool (arch_or_target , tool) . map (| c | c . to_command ()) }
    };
}

find!();