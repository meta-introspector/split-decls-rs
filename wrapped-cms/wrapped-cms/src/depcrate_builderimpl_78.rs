// Generated macro for impl_78 (impl)
macro_rules! Depcrate_builderimpl_78 {
() => {
// Module: crate::builder
// Provides: {"impl_78"}
// Dependencies: {}
impl < 's > SignedDataBuilder < 's > { # [doc = " Create a new builder for `SignedData`"] pub fn new (encapsulated_content_info : & 's EncapsulatedContentInfo) -> SignedDataBuilder < 's > { Self { digest_algorithms : Vec :: new () , encapsulated_content_info , certificates : None , crls : None , signer_infos : Vec :: new () , } } # [doc = " Add a digest algorithm to the collection of message digest algorithms."] # [doc = " RFC 5652 § 5.1: digestAlgorithms is a collection of message digest algorithm"] # [doc = " identifiers.  There MAY be any number of elements in the"] # [doc = " collection, including zero.  Each element identifies the message"] # [doc = " digest algorithm, along with any associated parameters, used by"] # [doc = " one or more signer.  The collection is intended to list the"] # [doc = " message digest algorithms employed by all of the signers, in any"] # [doc = " order, to facilitate one-pass signature verification."] pub fn add_digest_algorithm (& mut self , digest_algorithm : AlgorithmIdentifierOwned ,) -> Result < & mut Self > { self . digest_algorithms . push (digest_algorithm) ; Ok (self) } # [doc = " Add a certificate to the certificate collection."] # [doc = " RFC 5652 § 5.1:"] # [doc = " certificates is a collection of certificates.  It is intended that"] # [doc = " the set of certificates be sufficient to contain certification"] # [doc = " paths from a recognized \"root\" or \"top-level certification"] # [doc = " authority\" to all of the signers in the signerInfos field.  There"] # [doc = " may be more certificates than necessary, and there may be"] # [doc = " certificates sufficient to contain certification paths from two or"] # [doc = " more independent top-level certification authorities.  There may"] # [doc = " also be fewer certificates than necessary, if it is expected that"] # [doc = " recipients have an alternate means of obtaining necessary"] # [doc = " certificates (e.g., from a previous set of certificates).  The"] # [doc = " signer's certificate MAY be included.  The use of version 1"] # [doc = " attribute certificates is strongly discouraged."] pub fn add_certificate (& mut self , certificate : CertificateChoices) -> Result < & mut Self > { if self . certificates . is_none () { self . certificates = Some (Vec :: new ()) ; } if let Some (certificates) = & mut self . certificates { certificates . push (certificate) ; } Ok (self) } # [doc = " Add a CRL to the collection of CRLs."] # [doc = " RFC 5652 § 5.1:"] # [doc = " crls is a collection of revocation status information.  It is"] # [doc = " intended that the collection contain information sufficient to"] # [doc = " determine whether the certificates in the certificates field are"] # [doc = " valid, but such correspondence is not necessary.  Certificate"] # [doc = " revocation lists (CRLs) are the primary source of revocation"] # [doc = " status information.  There MAY be more CRLs than necessary, and"] # [doc = " there MAY also be fewer CRLs than necessary."] pub fn add_crl (& mut self , crl : RevocationInfoChoice) -> Result < & mut Self > { if self . crls . is_none () { self . crls = Some (Vec :: new ()) ; } if let Some (crls) = & mut self . crls { crls . push (crl) ; } Ok (self) } # [doc = " Add a signer info. The signature will be calculated. Note that the encapsulated content"] # [doc = " must not be changed after the first signer info was added."] pub fn add_signer_info < S , Signature > (& mut self , signer_info_builder : SignerInfoBuilder < '_ > , signer : & S ,) -> Result < & mut Self > where S : Keypair + DynSignatureAlgorithmIdentifier , S : Signer < Signature > , S :: VerifyingKey : EncodePublicKey , Signature : SignatureBitStringEncoding , { let signer_info = signer_info_builder . build :: < S , Signature > (signer) . map_err (| _ | der :: Error :: from (ErrorKind :: Failed)) ? ; self . signer_infos . push (signer_info) ; Ok (self) } # [doc = " Add a signer info. The signature will be calculated. Note that the encapsulated content"] # [doc = " must not be changed after the first signer info was added."] pub fn add_signer_info_with_rng < S , Signature , R > (& mut self , signer_info_builder : SignerInfoBuilder < '_ > , signer : & S , rng : & mut R ,) -> Result < & mut Self > where S : Keypair + DynSignatureAlgorithmIdentifier , S : RandomizedSigner < Signature > , S :: VerifyingKey : EncodePublicKey , Signature : SignatureBitStringEncoding , R : CryptoRng + ? Sized , { let signer_info = signer_info_builder . build_with_rng :: < S , Signature , R > (signer , rng) . map_err (| _ | der :: Error :: from (ErrorKind :: Failed)) ? ; self . signer_infos . push (signer_info) ; Ok (self) } # [doc = " Add a signer info. The signature will be calculated. Note that the encapsulated content"] # [doc = " must not be changed after the first signer info was added."] pub async fn add_signer_info_async < S , Signature > (& mut self , signer_info_builder : SignerInfoBuilder < '_ > , signer : & S ,) -> Result < & mut Self > where S : Keypair + DynSignatureAlgorithmIdentifier , S : AsyncSigner < Signature > , S :: VerifyingKey : EncodePublicKey , Signature : SignatureBitStringEncoding , { let signer_info = signer_info_builder . build_async :: < S , Signature > (signer) . await . map_err (| _ | der :: Error :: from (ErrorKind :: Failed)) ? ; self . signer_infos . push (signer_info) ; Ok (self) } # [doc = " Add a signer info. The signature will be calculated. Note that the encapsulated content"] # [doc = " must not be changed after the first signer info was added."] pub async fn add_signer_info_with_rng_async < S , Signature , R > (& mut self , signer_info_builder : SignerInfoBuilder < '_ > , signer : & S , rng : & mut R ,) -> Result < & mut Self > where S : Keypair + DynSignatureAlgorithmIdentifier , S : AsyncRandomizedSigner < Signature > , S :: VerifyingKey : EncodePublicKey , Signature : SignatureBitStringEncoding , R : CryptoRng + ? Sized , { let signer_info = signer_info_builder . build_with_rng_async :: < S , Signature , R > (signer , rng) . await . map_err (| _ | der :: Error :: from (ErrorKind :: Failed)) ? ; self . signer_infos . push (signer_info) ; Ok (self) } # [doc = " This method returns a `ContentInfo` of type `signedData`."] pub fn build (& mut self) -> Result < ContentInfo > { let digest_algorithms = DigestAlgorithmIdentifiers :: try_from (self . digest_algorithms . to_owned ()) . unwrap () ; let encap_content_info = self . encapsulated_content_info . clone () ; let certificates = self . certificates . as_mut () . map (| certificates | CertificateSet :: try_from (certificates . to_owned ()) . unwrap ()) ; let crls = self . crls . as_mut () . map (| crls | RevocationInfoChoices :: try_from (crls . to_owned ()) . unwrap ()) ; let signer_infos = SignerInfos :: try_from (self . signer_infos . clone ()) . unwrap () ; let signed_data = SignedData { version : self . calculate_version () , digest_algorithms , encap_content_info , certificates , crls , signer_infos , } ; let signed_data_der = signed_data . to_der () ? ; let content = AnyRef :: try_from (signed_data_der . as_slice ()) ? ; let signed_data = ContentInfo { content_type : const_oid :: db :: rfc5911 :: ID_SIGNED_DATA , content : Any :: from (content) , } ; Ok (signed_data) } fn calculate_version (& self) -> CmsVersion { let other_certificates_are_present = if let Some (certificates) = & self . certificates { certificates . iter () . any (| certificate | matches ! (certificate , CertificateChoices :: Other (_))) } else { false } ; let v2_certificates_are_present = false ; let v1_certificates_are_present = false ; let other_crls_are_present = if let Some (crls) = & self . crls { crls . iter () . any (| revocation_info_choice | { matches ! (revocation_info_choice , RevocationInfoChoice :: Other (_)) }) } else { false } ; let v3_signer_infos_present = self . signer_infos . iter () . any (| signer_info | signer_info . version == CmsVersion :: V3) ; let content_not_data = self . encapsulated_content_info . econtent_type != const_oid :: db :: rfc5911 :: ID_DATA ; if other_certificates_are_present || other_crls_are_present { CmsVersion :: V5 } else if v2_certificates_are_present { CmsVersion :: V4 } else if v1_certificates_are_present || v3_signer_infos_present || content_not_data { CmsVersion :: V3 } else { CmsVersion :: V1 } } }
};
}
