// Generated macro for http_into_wasi_response (function)
macro_rules! Depcrate_http_compat_conversionshttp_into_wasi_response {
() => {
// Module: crate::http_compat::conversions
// Provides: {"http_into_wasi_response"}
// Dependencies: {}
# [doc = " Converts a host-side HTTP response (`HttpResponse<T>`) into a WASI HTTP response (`WasiHttpResponse`)."] # [doc = ""] # [doc = " This function bridges standard Rust `http` responses with the WASI HTTP model,"] # [doc = " serializing status codes, headers, and body data into their WebAssembly-compatible"] # [doc = " representations. It supports generic response body types and streams the response"] # [doc = " asynchronously into the WASI environment."] # [doc = " "] # [doc = " # See Also"] # [doc = ""] # [doc = " - [`http_from_wasi_response`] — converts a WASI response back into a host-side HTTP response."] # [doc = " - [`BodyWriter`] — for streaming body data into WASI."] # [doc = " - [`IncomingResponseBody`] — for handling pending or unstarted response states."] pub fn http_into_wasi_response < T > (mut resp : HttpResponse < T >) -> Result < WasiHttpResponse , ErrorCode > where T : http_body :: Body + Any , T :: Data : Into < Vec < u8 > > , T :: Error : Into < Box < dyn std :: error :: Error + Send + Sync + 'static > > { if let Some (incoming_body) = (& mut resp as & mut dyn Any) . downcast_mut :: < IncomingResponseBody > () { if let Some (response) = incoming_body . take_unstarted () { return Ok (response) ; } } let headers = resp . headers () . clone () . try_into () . map_err (to_internal_error_code) ? ; let (body_writer , body_rx , body_result_rx) = BodyWriter :: new () ; let (response , _future_result) = WasiHttpResponse :: new (headers , Some (body_rx) , body_result_rx) ; _ = response . set_status_code (resp . status () . as_u16 ()) ; wit_bindgen :: spawn (async move { let mut body = std :: pin :: pin ! (resp . into_body ()) ; _ = body_writer . send_http_body (& mut body) . await ; }) ; Ok (response) }
};
}
