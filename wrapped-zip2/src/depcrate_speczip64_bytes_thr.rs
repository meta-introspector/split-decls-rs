// Generated macro for ZIP64_BYTES_THR (const)
macro_rules! Depcrate_specZIP64_BYTES_THR {
() => {
// Module: crate::spec
// Provides: {"ZIP64_BYTES_THR"}
// Dependencies: {}
# [doc = " The file size at which a ZIP64 record becomes necessary."] # [doc = ""] # [doc = " If a file larger than this threshold attempts to be written, compressed or uncompressed, and"] # [doc = " [`FileOptions::large_file()`](crate::write::FileOptions) was not true, then [`ZipWriter`] will"] # [doc = " raise an [`io::Error`] with [`io::ErrorKind::Other`]."] # [doc = ""] # [doc = " If the zip file itself is larger than this value, then a zip64 central directory record will be"] # [doc = " written to the end of the file."] # [doc = ""] # [doc = "```"] # [doc = " # fn main() -> Result<(), zip::result::ZipError> {"] # [doc = " # #[cfg(target_pointer_width = \"64\")]"] # [doc = " # {"] # [doc = " use std::io::{self, Cursor, prelude::*};"] # [doc = " use std::error::Error;"] # [doc = " use zip::{ZipWriter, write::SimpleFileOptions};"] # [doc = ""] # [doc = " let mut zip = ZipWriter::new(Cursor::new(Vec::new()));"] # [doc = " // Writing an extremely large file for this test is faster without compression."] # [doc = " let options = SimpleFileOptions::default().compression_method(zip::CompressionMethod::Stored);"] # [doc = ""] # [doc = " let big_len: usize = (zip::ZIP64_BYTES_THR as usize) + 1;"] # [doc = " let big_buf = vec![0u8; big_len];"] # [doc = " {"] # [doc = " zip.start_file(\"zero.dat\", options)?;"] # [doc = " // This is too big!"] # [doc = " let res = zip.write_all(&big_buf[..]).err().unwrap();"] # [doc = " assert_eq!(res.kind(), io::ErrorKind::Other);"] # [doc = " let description = format!(\"{}\", &res);"] # [doc = " assert_eq!(description, \"Large file option has not been set\");"] # [doc = " }"] # [doc = " {"] # [doc = " // Attempting to write anything further to the same zip will still succeed, but the previous"] # [doc = " // failing entry has been removed."] # [doc = " zip.start_file(\"one.dat\", options)?;"] # [doc = " let zip = zip.finish_into_readable()?;"] # [doc = " let names: Vec<_> = zip.file_names().collect();"] # [doc = " assert_eq!(&names, &[\"one.dat\"]);"] # [doc = " }"] # [doc = ""] # [doc = " // Create a new zip output."] # [doc = " let mut zip = ZipWriter::new(Cursor::new(Vec::new()));"] # [doc = " // This time, create a zip64 record for the file."] # [doc = " let options = SimpleFileOptions::default()"] # [doc = "      .compression_method(zip::CompressionMethod::Stored).large_file(true);"] # [doc = " zip.start_file(\"zero.dat\", options)?;"] # [doc = " // This succeeds because we specified that it could be a large file."] # [doc = " assert!(zip.write_all(&big_buf[..]).is_ok());"] # [doc = " # }"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = "```"] pub const ZIP64_BYTES_THR : u64 = u32 :: MAX as u64 ;
};
}
