macro_rules! deps {
    () => {
        ZipCentralEntryBlock!();
        Zip32CentralDirectoryEnd!();
        ZipResult!();
        Magic!();
        Zip64CDELocatorBlock!();
        Zip64CentralDirectoryEndLocator!();
        CentralDirectoryEndInfo!();
        Zip64CentralDirectoryEnd!();
    };
}

macro_rules! find_central_directory {
    () => {
        deps!();
        # [doc = " Finds the EOCD and possibly the EOCD64 block and determines the archive offset."] # [doc = ""] # [doc = " In the best case scenario (no prepended junk), this function will not backtrack"] # [doc = " in the reader."] pub (crate) fn find_central_directory < R : Read + Seek > (reader : & mut R , archive_offset : ArchiveOffset , end_exclusive : u64 , file_len : u64 ,) -> ZipResult < CentralDirectoryEndInfo > { const EOCD_SIG_BYTES : [u8 ; mem :: size_of :: < Magic > ()] = Magic :: CENTRAL_DIRECTORY_END_SIGNATURE . to_le_bytes () ; const EOCD64_SIG_BYTES : [u8 ; mem :: size_of :: < Magic > ()] = Magic :: ZIP64_CENTRAL_DIRECTORY_END_SIGNATURE . to_le_bytes () ; const CDFH_SIG_BYTES : [u8 ; mem :: size_of :: < Magic > ()] = Magic :: CENTRAL_DIRECTORY_HEADER_SIGNATURE . to_le_bytes () ; let mut eocd_finder = MagicFinder :: < Backwards < 'static > > :: new (& EOCD_SIG_BYTES , 0 , end_exclusive) ; let mut subfinder : Option < OptimisticMagicFinder < Forward < 'static > > > = None ; let mut parsing_error = None ; while let Some (eocd_offset) = eocd_finder . next (reader) ? { let eocd = match Zip32CentralDirectoryEnd :: parse (reader) { Ok (eocd) => eocd , Err (e) => { if parsing_error . is_none () { parsing_error = Some (e) ; } continue ; } } ; if eocd . zip_file_comment . len () as u64 + eocd_offset + 22 > file_len { parsing_error = Some (invalid ! ("Invalid EOCD comment length")) ; continue ; } let zip64_metadata = if eocd . may_be_zip64 () { fn try_read_eocd64_locator (reader : & mut (impl Read + Seek) , eocd_offset : u64 ,) -> ZipResult < (u64 , Zip64CentralDirectoryEndLocator) > { if eocd_offset < mem :: size_of :: < Zip64CDELocatorBlock > () as u64 { return Err (invalid ! ("EOCD64 Locator does not fit in file")) ; } let locator64_offset = eocd_offset - mem :: size_of :: < Zip64CDELocatorBlock > () as u64 ; reader . seek (io :: SeekFrom :: Start (locator64_offset)) ? ; Ok ((locator64_offset , Zip64CentralDirectoryEndLocator :: parse (reader) ? ,)) } try_read_eocd64_locator (reader , eocd_offset) . ok () } else { None } ; let Some ((locator64_offset , locator64)) = zip64_metadata else { let relative_cd_offset = eocd . central_directory_offset as u64 ; if eocd . number_of_files == 0 { return Ok (CentralDirectoryEndInfo { eocd : (eocd , eocd_offset) . into () , eocd64 : None , archive_offset : eocd_offset . saturating_sub (relative_cd_offset) , }) ; } if relative_cd_offset >= eocd_offset { parsing_error = Some (invalid ! ("Invalid CDFH offset in EOCD")) ; continue ; } let subfinder = subfinder . get_or_insert_with (OptimisticMagicFinder :: new_empty) . repurpose (& CDFH_SIG_BYTES , (relative_cd_offset , eocd_offset) , match archive_offset { ArchiveOffset :: Known (n) => { Some ((relative_cd_offset . saturating_add (n) . min (eocd_offset) , true)) } _ => Some ((relative_cd_offset , false)) , } ,) ; if let Some (cd_offset) = subfinder . next (reader) ? { let archive_offset = cd_offset - relative_cd_offset ; return Ok (CentralDirectoryEndInfo { eocd : (eocd , eocd_offset) . into () , eocd64 : None , archive_offset , }) ; } parsing_error = Some (invalid ! ("No CDFH found")) ; continue ; } ; if locator64 . end_of_central_directory_offset >= locator64_offset { parsing_error = Some (invalid ! ("Invalid EOCD64 Locator CD offset")) ; continue ; } if locator64 . number_of_disks > 1 { parsing_error = Some (invalid ! ("Multi-disk ZIP files are not supported")) ; continue ; } fn try_read_eocd64 < R : Read + Seek > (reader : & mut R , locator64 : & Zip64CentralDirectoryEndLocator , expected_length : u64 ,) -> ZipResult < Zip64CentralDirectoryEnd > { let z64 = Zip64CentralDirectoryEnd :: parse (reader , expected_length) ? ; if z64 . disk_with_central_directory != locator64 . disk_with_central_directory { return Err (invalid ! ("Invalid EOCD64: inconsistency with Locator data")) ; } if z64 . record_size + 12 != expected_length { return Err (invalid ! ("Invalid EOCD64: inconsistent length")) ; } Ok (z64) } let subfinder = subfinder . get_or_insert_with (OptimisticMagicFinder :: new_empty) . repurpose (& EOCD64_SIG_BYTES , (locator64 . end_of_central_directory_offset , locator64_offset) , match archive_offset { ArchiveOffset :: Known (n) => Some ((locator64 . end_of_central_directory_offset . saturating_add (n) . min (locator64_offset) , true ,)) , _ => Some ((locator64 . end_of_central_directory_offset , false)) , } ,) ; let mut local_error = None ; while let Some (eocd64_offset) = subfinder . next (reader) ? { let archive_offset = eocd64_offset - locator64 . end_of_central_directory_offset ; match try_read_eocd64 (reader , & locator64 , locator64_offset . saturating_sub (eocd64_offset) ,) { Ok (eocd64) => { if eocd64_offset < eocd64 . number_of_files . saturating_mul (mem :: size_of :: < crate :: types :: ZipCentralEntryBlock > () as u64) . saturating_add (eocd64 . central_directory_offset) { local_error = Some (invalid ! ("Invalid EOCD64: inconsistent number of files")) ; continue ; } return Ok (CentralDirectoryEndInfo { eocd : (eocd , eocd_offset) . into () , eocd64 : Some ((eocd64 , eocd64_offset) . into ()) , archive_offset , }) ; } Err (e) => { local_error = Some (e) ; } } } parsing_error = local_error . or (Some (invalid ! ("Could not find EOCD64"))) ; } Err (parsing_error . unwrap_or (invalid ! ("Could not find EOCD"))) }
    };
}

find_central_directory!();