macro_rules! deps {
    () => {
        DateTime!();
        ZipCryptoKeys!();
        CompressionMethod!();
        AesMode!();
        EncryptWith!();
        FileOptions!();
    };
}

macro_rules! impl_246 {
    () => {
        deps!();
        impl < T : FileOptionExtension > FileOptions < '_ , T > { pub (crate) fn normalize (& mut self) { if ! self . last_modified_time . is_valid () { self . last_modified_time = FileOptions :: < T > :: default () . last_modified_time ; } * self . permissions . get_or_insert (0o644) |= ffi :: S_IFREG ; } # [doc = " Set the compression method for the new file"] # [doc = ""] # [doc = " The default is `CompressionMethod::Deflated` if it is enabled. If not,"] # [doc = " `CompressionMethod::Bzip2` is the default if it is enabled. If neither `bzip2` nor `deflate`"] # [doc = " is enabled, `CompressionMethod::Zlib` is the default. If all else fails,"] # [doc = " `CompressionMethod::Stored` becomes the default and files are written uncompressed."] # [must_use] pub const fn compression_method (mut self , method : CompressionMethod) -> Self { self . compression_method = method ; self } # [doc = " Set the compression level for the new file"] # [doc = ""] # [doc = " `None` value specifies default compression level."] # [doc = ""] # [doc = " Range of values depends on compression method:"] # [doc = " * `Deflated`: 10 - 264 for Zopfli, 0 - 9 for other encoders. Default is 24 if Zopfli is the"] # [doc = "   only encoder, or 6 otherwise."] # [doc = " * `Bzip2`: 0 - 9. Default is 6"] # [doc = " * `Zstd`: -7 - 22, with zero being mapped to default level. Default is 3"] # [doc = " * others: only `None` is allowed"] # [must_use] pub const fn compression_level (mut self , level : Option < i64 >) -> Self { self . compression_level = level ; self } # [doc = " Set the last modified time"] # [doc = ""] # [doc = " The default is the current timestamp if the 'time' feature is enabled, and 1980-01-01"] # [doc = " otherwise"] # [must_use] pub const fn last_modified_time (mut self , mod_time : DateTime) -> Self { self . last_modified_time = mod_time ; self } # [doc = " Set the permissions for the new file."] # [doc = ""] # [doc = " The format is represented with unix-style permissions."] # [doc = " The default is `0o644`, which represents `rw-r--r--` for files,"] # [doc = " and `0o755`, which represents `rwxr-xr-x` for directories."] # [doc = ""] # [doc = " This method only preserves the file permissions bits (via a `& 0o777`) and discards"] # [doc = " higher file mode bits. So it cannot be used to denote an entry as a directory,"] # [doc = " symlink, or other special file type."] # [must_use] pub const fn unix_permissions (mut self , mode : u32) -> Self { self . permissions = Some (mode & 0o777) ; self } # [doc = " Set whether the new file's compressed and uncompressed size is less than 4 GiB."] # [doc = ""] # [doc = " If set to `false` and the file exceeds the limit, an I/O error is thrown and the file is"] # [doc = " aborted. If set to `true`, readers will require ZIP64 support and if the file does not"] # [doc = " exceed the limit, 20 B are wasted. The default is `false`."] # [must_use] pub const fn large_file (mut self , large : bool) -> Self { self . large_file = large ; self } pub (crate) fn with_deprecated_encryption (self , password : & [u8]) -> FileOptions < 'static , T > { FileOptions { encrypt_with : Some (EncryptWith :: ZipCrypto (ZipCryptoKeys :: derive (password) , PhantomData ,)) , .. self } } # [doc = " Set the AES encryption parameters."] # [cfg (feature = "aes-crypto")] pub fn with_aes_encryption (self , mode : AesMode , password : & str) -> FileOptions < '_ , T > { FileOptions { encrypt_with : Some (EncryptWith :: Aes { mode , password }) , .. self } } # [doc = " Sets the size of the buffer used to hold the next block that Zopfli will compress. The"] # [doc = " larger the buffer, the more effective the compression, but the more memory is required."] # [doc = " A value of `None` indicates no buffer, which is recommended only when all non-empty writes"] # [doc = " are larger than about 32 KiB."] # [must_use] # [cfg (feature = "deflate-zopfli")] pub const fn with_zopfli_buffer (mut self , size : Option < usize >) -> Self { self . zopfli_buffer_size = size ; self } # [doc = " Returns the compression level currently set."] pub const fn get_compression_level (& self) -> Option < i64 > { self . compression_level } # [doc = " Sets the alignment to the given number of bytes."] # [must_use] pub const fn with_alignment (mut self , alignment : u16) -> Self { self . alignment = alignment ; self } }
    };
}

impl_246!()