// Generated macro for zeroize_flat_type (function)
macro_rules! Depcratezeroize_flat_type {
() => {
// Module: crate
// Provides: {"zeroize_flat_type"}
// Dependencies: {}
# [doc = " Zeroizes a flat type/struct. Only zeroizes the values that it owns, and it does not work on"] # [doc = " dynamically sized values or trait objects. It would be inefficient to use this function on a"] # [doc = " type that already implements `ZeroizeOnDrop`."] # [doc = ""] # [doc = " # Safety"] # [doc = " - The type must not contain references to outside data or dynamically sized data, such as"] # [doc = "   `Vec<T>` or `String`."] # [doc = " - Values stored in the type must not have `Drop` impls."] # [doc = " - This function can invalidate the type if it is used after this function is called on it."] # [doc = "   It is advisable to call this function only in `impl Drop`."] # [doc = " - The bit pattern of all zeroes must be valid for the data being zeroized. This may not be"] # [doc = "   true for enums and pointers."] # [doc = ""] # [doc = " # Incompatible data types"] # [doc = " Some data types that cannot be safely zeroized using `zeroize_flat_type` include,"] # [doc = " but are not limited to:"] # [doc = " - References: `&T` and `&mut T`"] # [doc = " - Non-nullable types: `NonNull<T>`, `NonZeroU32`, etc."] # [doc = " - Enums with explicit non-zero tags."] # [doc = " - Smart pointers and collections: `Arc<T>`, `Box<T>`, `Vec<T>`, `HashMap<K, V>`, `String`, etc."] # [doc = ""] # [doc = " # Examples"] # [doc = " Safe usage for a struct containing strictly flat data:"] # [doc = " ```"] # [doc = " use zeroize::{ZeroizeOnDrop, zeroize_flat_type};"] # [doc = ""] # [doc = " struct DataToZeroize {"] # [doc = "     flat_data_1: [u8; 32],"] # [doc = "     flat_data_2: SomeMoreFlatData,"] # [doc = " }"] # [doc = ""] # [doc = " struct SomeMoreFlatData(u64);"] # [doc = ""] # [doc = " impl Drop for DataToZeroize {"] # [doc = "     fn drop(&mut self) {"] # [doc = "         unsafe { zeroize_flat_type(self as *mut Self) }"] # [doc = "     }"] # [doc = " }"] # [doc = " impl ZeroizeOnDrop for DataToZeroize {}"] # [doc = ""] # [doc = " let mut data = DataToZeroize {"] # [doc = "     flat_data_1: [3u8; 32],"] # [doc = "     flat_data_2: SomeMoreFlatData(123u64)"] # [doc = " };"] # [doc = ""] # [doc = " // data gets zeroized when dropped"] # [doc = " ```"] # [inline (always)] pub unsafe fn zeroize_flat_type < F : Sized > (data : * mut F) { let size = size_of :: < F > () ; unsafe { volatile_set (data as * mut u8 , 0 , size) ; } atomic_fence () }
};
}
