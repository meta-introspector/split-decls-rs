// Generated macro for DiffActivity (enum)
macro_rules! Depcrate_expand_autodiff_attrsDiffActivity {
() => {
// Module: crate::expand::autodiff_attrs
// Provides: {"DiffActivity"}
// Dependencies: {}
# [doc = " Dual and Duplicated (and their Only variants) are getting lowered to the same Enzyme Activity."] # [doc = " However, under forward mode we overwrite the previous shadow value, while for reverse mode"] # [doc = " we add to the previous shadow value. To not surprise users, we picked different names."] # [doc = " Dual numbers is also a quite well known name for forward mode AD types."] # [derive (Clone , Copy , Eq , PartialEq , Encodable , Decodable , Debug , HashStable_Generic)] pub enum DiffActivity { # [doc = " Implicit or Explicit () return type, so a special case of Const."] None , # [doc = " Don't compute derivatives with respect to this input/output."] Const , # [doc = " Reverse Mode, Compute derivatives for this scalar input/output."] Active , # [doc = " Reverse Mode, Compute derivatives for this scalar output, but don't compute"] # [doc = " the original return value."] ActiveOnly , # [doc = " Forward Mode, Compute derivatives for this input/output and *overwrite* the shadow argument"] # [doc = " with it."] Dual , # [doc = " Forward Mode, Compute derivatives for this input/output and *overwrite* the shadow argument"] # [doc = " with it. It expects the shadow argument to be `width` times larger than the original"] # [doc = " input/output."] Dualv , # [doc = " Forward Mode, Compute derivatives for this input/output and *overwrite* the shadow argument"] # [doc = " with it. Drop the code which updates the original input/output for maximum performance."] DualOnly , # [doc = " Forward Mode, Compute derivatives for this input/output and *overwrite* the shadow argument"] # [doc = " with it. Drop the code which updates the original input/output for maximum performance."] # [doc = " It expects the shadow argument to be `width` times larger than the original input/output."] DualvOnly , # [doc = " Reverse Mode, Compute derivatives for this &T or *T input and *add* it to the shadow argument."] Duplicated , # [doc = " Reverse Mode, Compute derivatives for this &T or *T input and *add* it to the shadow argument."] # [doc = " Drop the code which updates the original input for maximum performance."] DuplicatedOnly , # [doc = " All Integers must be Const, but these are used to mark the integer which represents the"] # [doc = " length of a slice/vec. This is used for safety checks on slices."] # [doc = " The integer (if given) specifies the size of the slice element in bytes."] FakeActivitySize (Option < u32 >) , }
};
}
