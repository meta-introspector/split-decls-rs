// Generated macro for Body (trait)
macro_rules! DepcrateBody {
() => {
// Module: crate
// Provides: {"Body"}
// Dependencies: {}
# [doc = " Trait representing a streaming body of a Request or Response."] # [doc = ""] # [doc = " Individual frames are streamed via the `poll_frame` function, which asynchronously yields"] # [doc = " instances of [`Frame<Data>`]."] # [doc = ""] # [doc = " Frames can contain a data buffer of type `Self::Data`. Frames can also contain an optional"] # [doc = " set of trailers used to finalize the request/response exchange. This is mostly used when using"] # [doc = " the HTTP/2.0 protocol."] # [doc = ""] # [doc = " The `size_hint` function provides insight into the total number of bytes that will be streamed."] pub trait Body { # [doc = " Values yielded by the `Body`."] type Data : Buf ; # [doc = " The error type this `Body` might generate."] type Error ; # [allow (clippy :: type_complexity)] # [doc = " Attempt to pull out the next frame of this stream."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " This function returns:"] # [doc = ""] # [doc = " - `Poll::Pending` if the next frame is not ready yet."] # [doc = " - `Poll::Ready(Some(Ok(frame)))` when the next frame is available."] # [doc = " - `Poll::Ready(Some(Err(error)))` when an error has been reached."] # [doc = " - `Poll::Ready(None)` means that all of the frames in this stream have been returned, and"] # [doc = "   that the end of the stream has been reached."] # [doc = ""] # [doc = " If `Poll::Ready(Some(Err(error)))` is returned, this body should be discarded."] # [doc = ""] # [doc = " Once the end of the stream is reached, implementations should continue to return"] # [doc = " `Poll::Ready(None)`."] fn poll_frame (self : Pin < & mut Self > , cx : & mut Context < '_ > ,) -> Poll < Option < Result < Frame < Self :: Data > , Self :: Error > > > ; # [doc = " A hint that may return `true` when the end of stream has been reached."] # [doc = ""] # [doc = " An end of stream means that `poll_frame` will return `None`."] # [doc = ""] # [doc = " A return value of `false` **does not** guarantee that a value will be"] # [doc = " returned from `poll_frame`. Combinators or other implementations may"] # [doc = " not be able to know the end of stream has been reached for this hint."] # [doc = ""] # [doc = " Returning `true` allows consumers of this body to optimize, such as not"] # [doc = " calling `poll_frame` again."] fn is_end_stream (& self) -> bool { false } # [doc = " A hint that returns the bounds on the remaining length of the stream."] # [doc = ""] # [doc = " When the **exact** remaining length of the stream is known, the upper bound will be set and"] # [doc = " will equal the lower bound."] fn size_hint (& self) -> SizeHint { SizeHint :: default () } }
};
}
