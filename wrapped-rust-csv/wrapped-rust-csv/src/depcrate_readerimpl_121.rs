// Generated macro for impl_121 (impl)
macro_rules! Depcrate_readerimpl_121 {
() => {
// Module: crate::reader
// Provides: {"impl_121"}
// Dependencies: {}
impl < R : io :: Read + io :: Seek > Reader < R > { # [doc = " Seeks the underlying reader to the position given."] # [doc = ""] # [doc = " This comes with a few caveats:"] # [doc = ""] # [doc = " * Any internal buffer associated with this reader is cleared."] # [doc = " * If the given position does not correspond to a position immediately"] # [doc = "   before the start of a record, then the behavior of this reader is"] # [doc = "   unspecified."] # [doc = " * Any special logic that skips the first record in the CSV reader"] # [doc = "   when reading or iterating over records is disabled."] # [doc = ""] # [doc = " If the given position has a byte offset equivalent to the current"] # [doc = " position, then no seeking is performed."] # [doc = ""] # [doc = " If the header row has not already been read, then this will attempt"] # [doc = " to read the header row before seeking. Therefore, it is possible that"] # [doc = " this returns an error associated with reading CSV data."] # [doc = ""] # [doc = " Note that seeking is performed based only on the byte offset in the"] # [doc = " given position. Namely, the record or line numbers in the position may"] # [doc = " be incorrect, but this will cause any future position generated by"] # [doc = " this CSV reader to be similarly incorrect."] # [doc = ""] # [doc = " # Example: seek to parse a record twice"] # [doc = ""] # [doc = " ```"] # [doc = " use std::{error::Error, io};"] # [doc = " use csv::{Reader, Position};"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,popcount"] # [doc = " Boston,United States,4628910"] # [doc = " Concord,United States,42695"] # [doc = " \";"] # [doc = "     let rdr = Reader::from_reader(io::Cursor::new(data));"] # [doc = "     let mut iter = rdr.into_records();"] # [doc = "     let mut pos = Position::new();"] # [doc = "     loop {"] # [doc = "         // Read the position immediately before each record."] # [doc = "         let next_pos = iter.reader().position().clone();"] # [doc = "         if iter.next().is_none() {"] # [doc = "             break;"] # [doc = "         }"] # [doc = "         pos = next_pos;"] # [doc = "     }"] # [doc = ""] # [doc = "     // Now seek the reader back to `pos`. This will let us read the"] # [doc = "     // last record again."] # [doc = "     iter.reader_mut().seek(pos)?;"] # [doc = "     let mut iter = iter.into_reader().into_records();"] # [doc = "     if let Some(result) = iter.next() {"] # [doc = "         let record = result?;"] # [doc = "         assert_eq!(record, vec![\"Concord\", \"United States\", \"42695\"]);"] # [doc = "         Ok(())"] # [doc = "     } else {"] # [doc = "         Err(From::from(\"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn seek (& mut self , pos : Position) -> Result < () > { self . byte_headers () ? ; self . state . seeked = true ; if pos . byte () == self . state . cur_pos . byte () { return Ok (()) ; } self . rdr . seek (io :: SeekFrom :: Start (pos . byte ())) ? ; self . core . reset () ; self . core . set_line (pos . line ()) ; self . state . cur_pos = pos ; self . state . eof = ReaderEofState :: NotEof ; Ok (()) } # [doc = " This is like `seek`, but provides direct control over how the seeking"] # [doc = " operation is performed via `io::SeekFrom`."] # [doc = ""] # [doc = " The `pos` position given *should* correspond the position indicated"] # [doc = " by `seek_from`, but there is no requirement. If the `pos` position"] # [doc = " given is incorrect, then the position information returned by this"] # [doc = " reader will be similarly incorrect."] # [doc = ""] # [doc = " If the header row has not already been read, then this will attempt"] # [doc = " to read the header row before seeking. Therefore, it is possible that"] # [doc = " this returns an error associated with reading CSV data."] # [doc = ""] # [doc = " Unlike `seek`, this will always cause an actual seek to be performed."] pub fn seek_raw (& mut self , seek_from : io :: SeekFrom , pos : Position ,) -> Result < () > { self . byte_headers () ? ; self . state . seeked = true ; self . rdr . seek (seek_from) ? ; self . core . reset () ; self . core . set_line (pos . line ()) ; self . state . cur_pos = pos ; self . state . eof = ReaderEofState :: NotEof ; Ok (()) } }
};
}
