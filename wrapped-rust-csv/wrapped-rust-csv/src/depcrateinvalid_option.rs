// Generated macro for invalid_option (function)
macro_rules! Depcrateinvalid_option {
() => {
// Module: crate
// Provides: {"invalid_option"}
// Dependencies: {}
# [doc = " A custom Serde deserializer for possibly invalid `Option<T>` fields."] # [doc = ""] # [doc = " When deserializing CSV data, it is sometimes desirable to simply ignore"] # [doc = " fields with invalid data. For example, there might be a field that is"] # [doc = " usually a number, but will occasionally contain garbage data that causes"] # [doc = " number parsing to fail."] # [doc = ""] # [doc = " You might be inclined to use, say, `Option<i32>` for fields such at this."] # [doc = " By default, however, `Option<i32>` will either capture *empty* fields with"] # [doc = " `None` or valid numeric fields with `Some(the_number)`. If the field is"] # [doc = " non-empty and not a valid number, then deserialization will return an error"] # [doc = " instead of using `None`."] # [doc = ""] # [doc = " This function allows you to override this default behavior. Namely, if"] # [doc = " `Option<T>` is deserialized with non-empty but invalid data, then the value"] # [doc = " will be `None` and the error will be ignored."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to parse CSV records with numerical data, even if"] # [doc = " some numerical data is absent or invalid. Without the"] # [doc = " `serde(deserialize_with = \"...\")` annotations, this example would return"] # [doc = " an error."] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = ""] # [doc = " #[derive(Debug, serde::Deserialize, Eq, PartialEq)]"] # [doc = " struct Row {"] # [doc = "     #[serde(deserialize_with = \"csv::invalid_option\")]"] # [doc = "     a: Option<i32>,"] # [doc = "     #[serde(deserialize_with = \"csv::invalid_option\")]"] # [doc = "     b: Option<i32>,"] # [doc = "     #[serde(deserialize_with = \"csv::invalid_option\")]"] # [doc = "     c: Option<i32>,"] # [doc = " }"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " a,b,c"] # [doc = " 5,\\\"\\\",xyz"] # [doc = " \";"] # [doc = "     let mut rdr = csv::Reader::from_reader(data.as_bytes());"] # [doc = "     if let Some(result) = rdr.deserialize().next() {"] # [doc = "         let record: Row = result?;"] # [doc = "         assert_eq!(record, Row { a: Some(5), b: None, c: None });"] # [doc = "         Ok(())"] # [doc = "     } else {"] # [doc = "         Err(From::from(\"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn invalid_option < 'de , D , T > (de : D) -> result :: Result < Option < T > , D :: Error > where D : Deserializer < 'de > , Option < T > : Deserialize < 'de > , { Option :: < T > :: deserialize (de) . or_else (| _ | Ok (None)) }
};
}
