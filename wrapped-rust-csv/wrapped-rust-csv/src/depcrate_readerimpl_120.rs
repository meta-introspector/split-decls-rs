// Generated macro for impl_120 (impl)
macro_rules! Depcrate_readerimpl_120 {
() => {
// Module: crate::reader
// Provides: {"impl_120"}
// Dependencies: {}
impl < R : io :: Read > Reader < R > { # [doc = " Create a new CSV reader given a builder and a source of underlying"] # [doc = " bytes."] fn new (builder : & ReaderBuilder , rdr : R) -> Reader < R > { Reader { core : Box :: new (builder . builder . build ()) , rdr : io :: BufReader :: with_capacity (builder . capacity , rdr) , state : ReaderState { headers : None , has_headers : builder . has_headers , flexible : builder . flexible , trim : builder . trim , first_field_count : None , cur_pos : Position :: new () , first : false , seeked : false , eof : ReaderEofState :: NotEof , } , } } # [doc = " Create a new CSV parser with a default configuration for the given"] # [doc = " reader."] # [doc = ""] # [doc = " To customize CSV parsing, use a `ReaderBuilder`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = " use csv::Reader;"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,pop"] # [doc = " Boston,United States,4628910"] # [doc = " Concord,United States,42695"] # [doc = " \";"] # [doc = "     let mut rdr = Reader::from_reader(data.as_bytes());"] # [doc = "     for result in rdr.records() {"] # [doc = "         let record = result?;"] # [doc = "         println!(\"{:?}\", record);"] # [doc = "     }"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] pub fn from_reader (rdr : R) -> Reader < R > { ReaderBuilder :: new () . from_reader (rdr) } # [doc = " Returns a borrowed iterator over deserialized records."] # [doc = ""] # [doc = " Each item yielded by this iterator is a `Result<D, Error>`."] # [doc = " Therefore, in order to access the record, callers must handle the"] # [doc = " possibility of error (typically with `try!` or `?`)."] # [doc = ""] # [doc = " If `has_headers` was enabled via a `ReaderBuilder` (which is the"] # [doc = " default), then this does not include the first record. Additionally,"] # [doc = " if `has_headers` is enabled, then deserializing into a struct will"] # [doc = " automatically align the values in each row to the fields of a struct"] # [doc = " based on the header row."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to deserialize CSV data into normal Rust structs. The"] # [doc = " fields of the header row are used to match up the values in each row"] # [doc = " to the fields of the struct."] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = ""] # [doc = " #[derive(Debug, serde::Deserialize, Eq, PartialEq)]"] # [doc = " struct Row {"] # [doc = "     city: String,"] # [doc = "     country: String,"] # [doc = "     #[serde(rename = \"popcount\")]"] # [doc = "     population: u64,"] # [doc = " }"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,popcount"] # [doc = " Boston,United States,4628910"] # [doc = " \";"] # [doc = "     let mut rdr = csv::Reader::from_reader(data.as_bytes());"] # [doc = "     let mut iter = rdr.deserialize();"] # [doc = ""] # [doc = "     if let Some(result) = iter.next() {"] # [doc = "         let record: Row = result?;"] # [doc = "         assert_eq!(record, Row {"] # [doc = "             city: \"Boston\".to_string(),"] # [doc = "             country: \"United States\".to_string(),"] # [doc = "             population: 4628910,"] # [doc = "         });"] # [doc = "         Ok(())"] # [doc = "     } else {"] # [doc = "         Err(From::from(\"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " # Rules"] # [doc = ""] # [doc = " For the most part, any Rust type that maps straight-forwardly to a CSV"] # [doc = " record is supported. This includes maps, structs, tuples and tuple"] # [doc = " structs. Other Rust types, such as `Vec`s, arrays, and enums have"] # [doc = " a more complicated story. In general, when working with CSV data, one"] # [doc = " should avoid *nested sequences* as much as possible."] # [doc = ""] # [doc = " Maps, structs, tuples and tuple structs map to CSV records in a simple"] # [doc = " way. Tuples and tuple structs decode their fields in the order that"] # [doc = " they are defined. Structs will do the same only if `has_headers` has"] # [doc = " been disabled using [`ReaderBuilder`](struct.ReaderBuilder.html),"] # [doc = " otherwise, structs and maps are deserialized based on the fields"] # [doc = " defined in the header row. (If there is no header row, then"] # [doc = " deserializing into a map will result in an error.)"] # [doc = ""] # [doc = " Nested sequences are supported in a limited capacity. Namely, they"] # [doc = " are flattened. As a result, it's often useful to use a `Vec` to capture"] # [doc = " a \"tail\" of fields in a record:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = ""] # [doc = " #[derive(Debug, serde::Deserialize, Eq, PartialEq)]"] # [doc = " struct Row {"] # [doc = "     label: String,"] # [doc = "     values: Vec<i32>,"] # [doc = " }"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"foo,1,2,3\";"] # [doc = "     let mut rdr = csv::ReaderBuilder::new()"] # [doc = "         .has_headers(false)"] # [doc = "         .from_reader(data.as_bytes());"] # [doc = "     let mut iter = rdr.deserialize();"] # [doc = ""] # [doc = "     if let Some(result) = iter.next() {"] # [doc = "         let record: Row = result?;"] # [doc = "         assert_eq!(record, Row {"] # [doc = "             label: \"foo\".to_string(),"] # [doc = "             values: vec![1, 2, 3],"] # [doc = "         });"] # [doc = "         Ok(())"] # [doc = "     } else {"] # [doc = "         Err(From::from(\"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " In the above example, adding another field to the `Row` struct after"] # [doc = " the `values` field will result in a deserialization error. This is"] # [doc = " because the deserializer doesn't know when to stop reading fields"] # [doc = " into the `values` vector, so it will consume the rest of the fields in"] # [doc = " the record leaving none left over for the additional field."] # [doc = ""] # [doc = " Finally, simple enums in Rust can be deserialized as well. Namely,"] # [doc = " enums must either be variants with no arguments or variants with a"] # [doc = " single argument. Variants with no arguments are deserialized based on"] # [doc = " which variant name the field matches. Variants with a single argument"] # [doc = " are deserialized based on which variant can store the data. The latter"] # [doc = " is only supported when using \"untagged\" enum deserialization. The"] # [doc = " following example shows both forms in action:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = ""] # [doc = " #[derive(Debug, serde::Deserialize, PartialEq)]"] # [doc = " struct Row {"] # [doc = "     label: Label,"] # [doc = "     value: Number,"] # [doc = " }"] # [doc = ""] # [doc = " #[derive(Debug, serde::Deserialize, PartialEq)]"] # [doc = " #[serde(rename_all = \"lowercase\")]"] # [doc = " enum Label {"] # [doc = "     Celsius,"] # [doc = "     Fahrenheit,"] # [doc = " }"] # [doc = ""] # [doc = " #[derive(Debug, serde::Deserialize, PartialEq)]"] # [doc = " #[serde(untagged)]"] # [doc = " enum Number {"] # [doc = "     Integer(i64),"] # [doc = "     Float(f64),"] # [doc = " }"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " label,value"] # [doc = " celsius,22.2222"] # [doc = " fahrenheit,72"] # [doc = " \";"] # [doc = "     let mut rdr = csv::Reader::from_reader(data.as_bytes());"] # [doc = "     let mut iter = rdr.deserialize();"] # [doc = ""] # [doc = "     // Read the first record."] # [doc = "     if let Some(result) = iter.next() {"] # [doc = "         let record: Row = result?;"] # [doc = "         assert_eq!(record, Row {"] # [doc = "             label: Label::Celsius,"] # [doc = "             value: Number::Float(22.2222),"] # [doc = "         });"] # [doc = "     } else {"] # [doc = "         return Err(From::from("] # [doc = "             \"expected at least two records but got none\"));"] # [doc = "     }"] # [doc = ""] # [doc = "     // Read the second record."] # [doc = "     if let Some(result) = iter.next() {"] # [doc = "         let record: Row = result?;"] # [doc = "         assert_eq!(record, Row {"] # [doc = "             label: Label::Fahrenheit,"] # [doc = "             value: Number::Integer(72),"] # [doc = "         });"] # [doc = "         Ok(())"] # [doc = "     } else {"] # [doc = "         Err(From::from("] # [doc = "             \"expected at least two records but got only one\"))"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn deserialize < D > (& mut self) -> DeserializeRecordsIter < '_ , R , D > where D : DeserializeOwned , { DeserializeRecordsIter :: new (self) } # [doc = " Returns an owned iterator over deserialized records."] # [doc = ""] # [doc = " Each item yielded by this iterator is a `Result<D, Error>`."] # [doc = " Therefore, in order to access the record, callers must handle the"] # [doc = " possibility of error (typically with `try!` or `?`)."] # [doc = ""] # [doc = " This is mostly useful when you want to return a CSV iterator or store"] # [doc = " it somewhere."] # [doc = ""] # [doc = " If `has_headers` was enabled via a `ReaderBuilder` (which is the"] # [doc = " default), then this does not include the first record. Additionally,"] # [doc = " if `has_headers` is enabled, then deserializing into a struct will"] # [doc = " automatically align the values in each row to the fields of a struct"] # [doc = " based on the header row."] # [doc = ""] # [doc = " For more detailed deserialization rules, see the documentation on the"] # [doc = " `deserialize` method."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = ""] # [doc = " #[derive(Debug, serde::Deserialize, Eq, PartialEq)]"] # [doc = " struct Row {"] # [doc = "     city: String,"] # [doc = "     country: String,"] # [doc = "     #[serde(rename = \"popcount\")]"] # [doc = "     population: u64,"] # [doc = " }"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,popcount"] # [doc = " Boston,United States,4628910"] # [doc = " \";"] # [doc = "     let rdr = csv::Reader::from_reader(data.as_bytes());"] # [doc = "     let mut iter = rdr.into_deserialize();"] # [doc = ""] # [doc = "     if let Some(result) = iter.next() {"] # [doc = "         let record: Row = result?;"] # [doc = "         assert_eq!(record, Row {"] # [doc = "             city: \"Boston\".to_string(),"] # [doc = "             country: \"United States\".to_string(),"] # [doc = "             population: 4628910,"] # [doc = "         });"] # [doc = "         Ok(())"] # [doc = "     } else {"] # [doc = "         Err(From::from(\"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn into_deserialize < D > (self) -> DeserializeRecordsIntoIter < R , D > where D : DeserializeOwned , { DeserializeRecordsIntoIter :: new (self) } # [doc = " Returns a borrowed iterator over all records as strings."] # [doc = ""] # [doc = " Each item yielded by this iterator is a `Result<StringRecord, Error>`."] # [doc = " Therefore, in order to access the record, callers must handle the"] # [doc = " possibility of error (typically with `try!` or `?`)."] # [doc = ""] # [doc = " If `has_headers` was enabled via a `ReaderBuilder` (which is the"] # [doc = " default), then this does not include the first record."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = " use csv::Reader;"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,pop"] # [doc = " Boston,United States,4628910"] # [doc = " \";"] # [doc = "     let mut rdr = Reader::from_reader(data.as_bytes());"] # [doc = "     let mut iter = rdr.records();"] # [doc = ""] # [doc = "     if let Some(result) = iter.next() {"] # [doc = "         let record = result?;"] # [doc = "         assert_eq!(record, vec![\"Boston\", \"United States\", \"4628910\"]);"] # [doc = "         Ok(())"] # [doc = "     } else {"] # [doc = "         Err(From::from(\"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn records (& mut self) -> StringRecordsIter < '_ , R > { StringRecordsIter :: new (self) } # [doc = " Returns an owned iterator over all records as strings."] # [doc = ""] # [doc = " Each item yielded by this iterator is a `Result<StringRecord, Error>`."] # [doc = " Therefore, in order to access the record, callers must handle the"] # [doc = " possibility of error (typically with `try!` or `?`)."] # [doc = ""] # [doc = " This is mostly useful when you want to return a CSV iterator or store"] # [doc = " it somewhere."] # [doc = ""] # [doc = " If `has_headers` was enabled via a `ReaderBuilder` (which is the"] # [doc = " default), then this does not include the first record."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = " use csv::Reader;"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,pop"] # [doc = " Boston,United States,4628910"] # [doc = " \";"] # [doc = "     let rdr = Reader::from_reader(data.as_bytes());"] # [doc = "     let mut iter = rdr.into_records();"] # [doc = ""] # [doc = "     if let Some(result) = iter.next() {"] # [doc = "         let record = result?;"] # [doc = "         assert_eq!(record, vec![\"Boston\", \"United States\", \"4628910\"]);"] # [doc = "         Ok(())"] # [doc = "     } else {"] # [doc = "         Err(From::from(\"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn into_records (self) -> StringRecordsIntoIter < R > { StringRecordsIntoIter :: new (self) } # [doc = " Returns a borrowed iterator over all records as raw bytes."] # [doc = ""] # [doc = " Each item yielded by this iterator is a `Result<ByteRecord, Error>`."] # [doc = " Therefore, in order to access the record, callers must handle the"] # [doc = " possibility of error (typically with `try!` or `?`)."] # [doc = ""] # [doc = " If `has_headers` was enabled via a `ReaderBuilder` (which is the"] # [doc = " default), then this does not include the first record."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = " use csv::Reader;"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,pop"] # [doc = " Boston,United States,4628910"] # [doc = " \";"] # [doc = "     let mut rdr = Reader::from_reader(data.as_bytes());"] # [doc = "     let mut iter = rdr.byte_records();"] # [doc = ""] # [doc = "     if let Some(result) = iter.next() {"] # [doc = "         let record = result?;"] # [doc = "         assert_eq!(record, vec![\"Boston\", \"United States\", \"4628910\"]);"] # [doc = "         Ok(())"] # [doc = "     } else {"] # [doc = "         Err(From::from(\"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn byte_records (& mut self) -> ByteRecordsIter < '_ , R > { ByteRecordsIter :: new (self) } # [doc = " Returns an owned iterator over all records as raw bytes."] # [doc = ""] # [doc = " Each item yielded by this iterator is a `Result<ByteRecord, Error>`."] # [doc = " Therefore, in order to access the record, callers must handle the"] # [doc = " possibility of error (typically with `try!` or `?`)."] # [doc = ""] # [doc = " This is mostly useful when you want to return a CSV iterator or store"] # [doc = " it somewhere."] # [doc = ""] # [doc = " If `has_headers` was enabled via a `ReaderBuilder` (which is the"] # [doc = " default), then this does not include the first record."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = " use csv::Reader;"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,pop"] # [doc = " Boston,United States,4628910"] # [doc = " \";"] # [doc = "     let rdr = Reader::from_reader(data.as_bytes());"] # [doc = "     let mut iter = rdr.into_byte_records();"] # [doc = ""] # [doc = "     if let Some(result) = iter.next() {"] # [doc = "         let record = result?;"] # [doc = "         assert_eq!(record, vec![\"Boston\", \"United States\", \"4628910\"]);"] # [doc = "         Ok(())"] # [doc = "     } else {"] # [doc = "         Err(From::from(\"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn into_byte_records (self) -> ByteRecordsIntoIter < R > { ByteRecordsIntoIter :: new (self) } # [doc = " Returns a reference to the first row read by this parser."] # [doc = ""] # [doc = " If no row has been read yet, then this will force parsing of the first"] # [doc = " row."] # [doc = ""] # [doc = " If there was a problem parsing the row or if it wasn't valid UTF-8,"] # [doc = " then this returns an error."] # [doc = ""] # [doc = " If the underlying reader emits EOF before any data, then this returns"] # [doc = " an empty record."] # [doc = ""] # [doc = " Note that this method may be used regardless of whether `has_headers`"] # [doc = " was enabled (but it is enabled by default)."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to get the header row of CSV data. Notice that"] # [doc = " the header row does not appear as a record in the iterator!"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = " use csv::Reader;"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,pop"] # [doc = " Boston,United States,4628910"] # [doc = " \";"] # [doc = "     let mut rdr = Reader::from_reader(data.as_bytes());"] # [doc = ""] # [doc = "     // We can read the headers before iterating."] # [doc = "     {"] # [doc = "         // `headers` borrows from the reader, so we put this in its"] # [doc = "         // own scope. That way, the borrow ends before we try iterating"] # [doc = "         // below. Alternatively, we could clone the headers."] # [doc = "         let headers = rdr.headers()?;"] # [doc = "         assert_eq!(headers, vec![\"city\", \"country\", \"pop\"]);"] # [doc = "     }"] # [doc = ""] # [doc = "     if let Some(result) = rdr.records().next() {"] # [doc = "         let record = result?;"] # [doc = "         assert_eq!(record, vec![\"Boston\", \"United States\", \"4628910\"]);"] # [doc = "     } else {"] # [doc = "         return Err(From::from("] # [doc = "             \"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = ""] # [doc = "     // We can also read the headers after iterating."] # [doc = "     let headers = rdr.headers()?;"] # [doc = "     assert_eq!(headers, vec![\"city\", \"country\", \"pop\"]);"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] pub fn headers (& mut self) -> Result < & StringRecord > { if self . state . headers . is_none () { let mut record = ByteRecord :: new () ; self . read_byte_record_impl (& mut record) ? ; self . set_headers_impl (Err (record)) ; } let headers = self . state . headers . as_ref () . unwrap () ; match headers . string_record { Ok (ref record) => Ok (record) , Err (ref err) => Err (Error :: new (ErrorKind :: Utf8 { pos : headers . byte_record . position () . cloned () , err : err . clone () , })) , } } # [doc = " Returns a reference to the first row read by this parser as raw bytes."] # [doc = ""] # [doc = " If no row has been read yet, then this will force parsing of the first"] # [doc = " row."] # [doc = ""] # [doc = " If there was a problem parsing the row then this returns an error."] # [doc = ""] # [doc = " If the underlying reader emits EOF before any data, then this returns"] # [doc = " an empty record."] # [doc = ""] # [doc = " Note that this method may be used regardless of whether `has_headers`"] # [doc = " was enabled (but it is enabled by default)."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to get the header row of CSV data. Notice that"] # [doc = " the header row does not appear as a record in the iterator!"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = " use csv::Reader;"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,pop"] # [doc = " Boston,United States,4628910"] # [doc = " \";"] # [doc = "     let mut rdr = Reader::from_reader(data.as_bytes());"] # [doc = ""] # [doc = "     // We can read the headers before iterating."] # [doc = "     {"] # [doc = "         // `headers` borrows from the reader, so we put this in its"] # [doc = "         // own scope. That way, the borrow ends before we try iterating"] # [doc = "         // below. Alternatively, we could clone the headers."] # [doc = "         let headers = rdr.byte_headers()?;"] # [doc = "         assert_eq!(headers, vec![\"city\", \"country\", \"pop\"]);"] # [doc = "     }"] # [doc = ""] # [doc = "     if let Some(result) = rdr.byte_records().next() {"] # [doc = "         let record = result?;"] # [doc = "         assert_eq!(record, vec![\"Boston\", \"United States\", \"4628910\"]);"] # [doc = "     } else {"] # [doc = "         return Err(From::from("] # [doc = "             \"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = ""] # [doc = "     // We can also read the headers after iterating."] # [doc = "     let headers = rdr.byte_headers()?;"] # [doc = "     assert_eq!(headers, vec![\"city\", \"country\", \"pop\"]);"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] pub fn byte_headers (& mut self) -> Result < & ByteRecord > { if self . state . headers . is_none () { let mut record = ByteRecord :: new () ; self . read_byte_record_impl (& mut record) ? ; self . set_headers_impl (Err (record)) ; } Ok (& self . state . headers . as_ref () . unwrap () . byte_record) } # [doc = " Set the headers of this CSV parser manually."] # [doc = ""] # [doc = " This overrides any other setting (including `set_byte_headers`). Any"] # [doc = " automatic detection of headers is disabled. This may be called at any"] # [doc = " time."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = " use csv::{Reader, StringRecord};"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,pop"] # [doc = " Boston,United States,4628910"] # [doc = " \";"] # [doc = "     let mut rdr = Reader::from_reader(data.as_bytes());"] # [doc = ""] # [doc = "     assert_eq!(rdr.headers()?, vec![\"city\", \"country\", \"pop\"]);"] # [doc = "     rdr.set_headers(StringRecord::from(vec![\"a\", \"b\", \"c\"]));"] # [doc = "     assert_eq!(rdr.headers()?, vec![\"a\", \"b\", \"c\"]);"] # [doc = ""] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] pub fn set_headers (& mut self , headers : StringRecord) { self . set_headers_impl (Ok (headers)) ; } # [doc = " Set the headers of this CSV parser manually as raw bytes."] # [doc = ""] # [doc = " This overrides any other setting (including `set_headers`). Any"] # [doc = " automatic detection of headers is disabled. This may be called at any"] # [doc = " time."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = " use csv::{Reader, ByteRecord};"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,pop"] # [doc = " Boston,United States,4628910"] # [doc = " \";"] # [doc = "     let mut rdr = Reader::from_reader(data.as_bytes());"] # [doc = ""] # [doc = "     assert_eq!(rdr.byte_headers()?, vec![\"city\", \"country\", \"pop\"]);"] # [doc = "     rdr.set_byte_headers(ByteRecord::from(vec![\"a\", \"b\", \"c\"]));"] # [doc = "     assert_eq!(rdr.byte_headers()?, vec![\"a\", \"b\", \"c\"]);"] # [doc = ""] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] pub fn set_byte_headers (& mut self , headers : ByteRecord) { self . set_headers_impl (Err (headers)) ; } fn set_headers_impl (& mut self , headers : result :: Result < StringRecord , ByteRecord > ,) { let (mut str_headers , mut byte_headers) = match headers { Ok (string) => { let bytes = string . clone () . into_byte_record () ; (Ok (string) , bytes) } Err (bytes) => { match StringRecord :: from_byte_record (bytes . clone ()) { Ok (str_headers) => (Ok (str_headers) , bytes) , Err (err) => (Err (err . utf8_error () . clone ()) , bytes) , } } } ; if self . state . trim . should_trim_headers () { if let Ok (ref mut str_headers) = str_headers . as_mut () { str_headers . trim () ; } byte_headers . trim () ; } self . state . headers = Some (Headers { byte_record : byte_headers , string_record : str_headers , }) ; } # [doc = " Read a single row into the given record. Returns false when no more"] # [doc = " records could be read."] # [doc = ""] # [doc = " If `has_headers` was enabled via a `ReaderBuilder` (which is the"] # [doc = " default), then this will never read the first record."] # [doc = ""] # [doc = " This method is useful when you want to read records as fast as"] # [doc = " as possible. It's less ergonomic than an iterator, but it permits the"] # [doc = " caller to reuse the `StringRecord` allocation, which usually results"] # [doc = " in higher throughput."] # [doc = ""] # [doc = " Records read via this method are guaranteed to have a position set"] # [doc = " on them, even if the reader is at EOF or if an error is returned."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = " use csv::{Reader, StringRecord};"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,pop"] # [doc = " Boston,United States,4628910"] # [doc = " \";"] # [doc = "     let mut rdr = Reader::from_reader(data.as_bytes());"] # [doc = "     let mut record = StringRecord::new();"] # [doc = ""] # [doc = "     if rdr.read_record(&mut record)? {"] # [doc = "         assert_eq!(record, vec![\"Boston\", \"United States\", \"4628910\"]);"] # [doc = "         Ok(())"] # [doc = "     } else {"] # [doc = "         Err(From::from(\"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn read_record (& mut self , record : & mut StringRecord) -> Result < bool > { let result = record . read (self) ; if self . state . trim . should_trim_fields () { record . trim () ; } result } # [doc = " Read a single row into the given byte record. Returns false when no"] # [doc = " more records could be read."] # [doc = ""] # [doc = " If `has_headers` was enabled via a `ReaderBuilder` (which is the"] # [doc = " default), then this will never read the first record."] # [doc = ""] # [doc = " This method is useful when you want to read records as fast as"] # [doc = " as possible. It's less ergonomic than an iterator, but it permits the"] # [doc = " caller to reuse the `ByteRecord` allocation, which usually results"] # [doc = " in higher throughput."] # [doc = ""] # [doc = " Records read via this method are guaranteed to have a position set"] # [doc = " on them, even if the reader is at EOF or if an error is returned."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::error::Error;"] # [doc = " use csv::{ByteRecord, Reader};"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,pop"] # [doc = " Boston,United States,4628910"] # [doc = " \";"] # [doc = "     let mut rdr = Reader::from_reader(data.as_bytes());"] # [doc = "     let mut record = ByteRecord::new();"] # [doc = ""] # [doc = "     if rdr.read_byte_record(&mut record)? {"] # [doc = "         assert_eq!(record, vec![\"Boston\", \"United States\", \"4628910\"]);"] # [doc = "         Ok(())"] # [doc = "     } else {"] # [doc = "         Err(From::from(\"expected at least one record but got none\"))"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn read_byte_record (& mut self , record : & mut ByteRecord ,) -> Result < bool > { if ! self . state . seeked && ! self . state . has_headers && ! self . state . first { if let Some (ref headers) = self . state . headers { self . state . first = true ; record . clone_from (& headers . byte_record) ; if self . state . trim . should_trim_fields () { record . trim () ; } return Ok (! record . is_empty ()) ; } } let ok = self . read_byte_record_impl (record) ? ; self . state . first = true ; if ! self . state . seeked && self . state . headers . is_none () { self . set_headers_impl (Err (record . clone ())) ; if self . state . has_headers { let result = self . read_byte_record_impl (record) ; if self . state . trim . should_trim_fields () { record . trim () ; } return result ; } } if self . state . trim . should_trim_fields () { record . trim () ; } Ok (ok) } # [doc = " Read a byte record from the underlying CSV reader, without accounting"] # [doc = " for headers."] # [inline (always)] fn read_byte_record_impl (& mut self , record : & mut ByteRecord ,) -> Result < bool > { use csv_core :: ReadRecordResult :: * ; record . clear () ; record . set_position (Some (self . state . cur_pos . clone ())) ; if self . state . eof != ReaderEofState :: NotEof { return Ok (false) ; } let (mut outlen , mut endlen) = (0 , 0) ; loop { let (res , nin , nout , nend) = { let input_res = self . rdr . fill_buf () ; if input_res . is_err () { self . state . eof = ReaderEofState :: IOError ; } let input = input_res ? ; let (fields , ends) = record . as_parts () ; self . core . read_record (input , & mut fields [outlen ..] , & mut ends [endlen ..] ,) } ; self . rdr . consume (nin) ; let byte = self . state . cur_pos . byte () ; self . state . cur_pos . set_byte (byte + nin as u64) . set_line (self . core . line ()) ; outlen += nout ; endlen += nend ; match res { InputEmpty => continue , OutputFull => { record . expand_fields () ; continue ; } OutputEndsFull => { record . expand_ends () ; continue ; } Record => { record . set_len (endlen) ; self . state . add_record (record) ? ; return Ok (true) ; } End => { self . state . eof = ReaderEofState :: Eof ; return Ok (false) ; } } } } # [doc = " Return the current position of this CSV reader."] # [doc = ""] # [doc = " The byte offset in the position returned can be used to `seek` this"] # [doc = " reader. In particular, seeking to a position returned here on the same"] # [doc = " data will result in parsing the same subsequent record."] # [doc = ""] # [doc = " # Example: reading the position"] # [doc = ""] # [doc = " ```"] # [doc = " use std::{error::Error, io};"] # [doc = " use csv::{Reader, Position};"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,popcount"] # [doc = " Boston,United States,4628910"] # [doc = " Concord,United States,42695"] # [doc = " \";"] # [doc = "     let rdr = Reader::from_reader(io::Cursor::new(data));"] # [doc = "     let mut iter = rdr.into_records();"] # [doc = "     let mut pos = Position::new();"] # [doc = "     loop {"] # [doc = "         // Read the position immediately before each record."] # [doc = "         let next_pos = iter.reader().position().clone();"] # [doc = "         if iter.next().is_none() {"] # [doc = "             break;"] # [doc = "         }"] # [doc = "         pos = next_pos;"] # [doc = "     }"] # [doc = ""] # [doc = "     // `pos` should now be the position immediately before the last"] # [doc = "     // record."] # [doc = "     assert_eq!(pos.byte(), 51);"] # [doc = "     assert_eq!(pos.line(), 3);"] # [doc = "     assert_eq!(pos.record(), 2);"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] pub fn position (& self) -> & Position { & self . state . cur_pos } # [doc = " Returns true if and only if this reader has been exhausted."] # [doc = ""] # [doc = " When this returns true, no more records can be read from this reader"] # [doc = " (unless it has been seeked to another position)."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::{error::Error, io};"] # [doc = " use csv::{Reader, Position};"] # [doc = ""] # [doc = " # fn main() { example().unwrap(); }"] # [doc = " fn example() -> Result<(), Box<dyn Error>> {"] # [doc = "     let data = \"\\"] # [doc = " city,country,popcount"] # [doc = " Boston,United States,4628910"] # [doc = " Concord,United States,42695"] # [doc = " \";"] # [doc = "     let mut rdr = Reader::from_reader(io::Cursor::new(data));"] # [doc = "     assert!(!rdr.is_done());"] # [doc = "     for result in rdr.records() {"] # [doc = "         let _ = result?;"] # [doc = "     }"] # [doc = "     assert!(rdr.is_done());"] # [doc = "     Ok(())"] # [doc = " }"] # [doc = " ```"] pub fn is_done (& self) -> bool { self . state . eof != ReaderEofState :: NotEof } # [doc = " Returns true if and only if this reader has been configured to"] # [doc = " interpret the first record as a header record."] pub fn has_headers (& self) -> bool { self . state . has_headers } # [doc = " Returns a reference to the underlying reader."] pub fn get_ref (& self) -> & R { self . rdr . get_ref () } # [doc = " Returns a mutable reference to the underlying reader."] pub fn get_mut (& mut self) -> & mut R { self . rdr . get_mut () } # [doc = " Unwraps this CSV reader, returning the underlying reader."] # [doc = ""] # [doc = " Note that any leftover data inside this reader's internal buffer is"] # [doc = " lost."] pub fn into_inner (self) -> R { self . rdr . into_inner () } }
};
}
