// Generated macro for large_offsets (module)
macro_rules! Depcrate_multi_index_chunklarge_offsets {
() => {
// Module: crate::multi_index::chunk
// Provides: {"large_offsets"}
// Dependencies: {}
# [doc = " Information about the large offsets table."] pub mod large_offsets { use std :: ops :: Range ; use crate :: { index :: encode :: LARGE_OFFSET_THRESHOLD , multi_index } ; # [doc = " The id uniquely identifying the large offsets table (with 64 bit offsets)"] pub const ID : gix_chunk :: Id = * b"LOFF" ; # [doc = " Returns Some(num-large-offset) if there are offsets larger than u32."] pub (crate) fn num_large_offsets (entries : & [multi_index :: write :: Entry]) -> Option < usize > { let mut num_large_offsets = 0 ; let mut needs_large_offsets = false ; for entry in entries { if entry . pack_offset > LARGE_OFFSET_THRESHOLD { num_large_offsets += 1 ; } if entry . pack_offset > crate :: data :: Offset :: from (u32 :: MAX) { needs_large_offsets = true ; } } needs_large_offsets . then_some (num_large_offsets) } # [doc = " Returns true if the `offsets` range seems to be properly aligned for the data we expect."] pub fn is_valid (offset : & Range < usize >) -> bool { (offset . end - offset . start) % 8 == 0 } pub (crate) fn write (sorted_entries : & [multi_index :: write :: Entry] , mut num_large_offsets : usize , out : & mut dyn std :: io :: Write ,) -> std :: io :: Result < () > { for offset in sorted_entries . iter () . filter_map (| e | (e . pack_offset > LARGE_OFFSET_THRESHOLD) . then_some (e . pack_offset)) { out . write_all (& offset . to_be_bytes ()) ? ; num_large_offsets = num_large_offsets . checked_sub (1) . expect ("BUG: wrote more offsets the previously found") ; } assert_eq ! (num_large_offsets , 0 , "BUG: wrote less offsets than initially counted") ; Ok (()) } # [doc = " Return the number of bytes needed to store the given amount of `large_offsets`"] pub (crate) fn storage_size (large_offsets : usize) -> u64 { 8 * large_offsets as u64 } }
};
}
