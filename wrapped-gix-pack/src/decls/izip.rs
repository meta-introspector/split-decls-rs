macro_rules! izip {
    () => {
        # [doc = " From itertools"] # [doc = " Create an iterator running multiple iterators in lockstep."] # [doc = ""] # [doc = " The `izip!` iterator yields elements until any subiterator"] # [doc = " returns `None`."] # [doc = ""] # [doc = " This is a version of the standard ``.zip()`` that's supporting more than"] # [doc = " two iterators. The iterator element type is a tuple with one element"] # [doc = " from each of the input iterators. Just like ``.zip()``, the iteration stops"] # [doc = " when the shortest of the inputs reaches its end."] # [doc = ""] # [doc = " **Note:** The result of this macro is in the general case an iterator"] # [doc = " composed of repeated `.zip()` and a `.map()`; it has an anonymous type."] # [doc = " The special cases of one and two arguments produce the equivalent of"] # [doc = " `$a.into_iter()` and `$a.into_iter().zip($b)` respectively."] # [doc = ""] # [doc = " Prefer this macro `izip!()` over [`multizip`] for the performance benefits"] # [doc = " of using the standard library `.zip()`."] # [doc = ""] # [doc = " [`multizip`]: fn.multizip.html"] # [doc = ""] # [doc = " ```ignore"] # [doc = " # use itertools::izip;"] # [doc = " #"] # [doc = " # fn main() {"] # [doc = ""] # [doc = " // iterate over three sequences side-by-side"] # [doc = " let mut results = [0, 0, 0, 0];"] # [doc = " let inputs = [3, 7, 9, 6];"] # [doc = ""] # [doc = " for (r, index, input) in izip!(&mut results, 0..10, &inputs) {"] # [doc = "     *r = index * 10 + input;"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(results, [0 + 3, 10 + 7, 29, 36]);"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " (The above is vendored from [itertools](https://github.com/rust-itertools/itertools),"] # [doc = " including the original doctest, though it has been marked `ignore` here.)"] macro_rules ! izip { (@ closure $ p : pat => $ tup : expr) => { |$ p | $ tup } ; (@ closure $ p : pat => ($ ($ tup : tt) *) , $ _iter : expr $ (, $ tail : expr) *) => { izip ! (@ closure ($ p , b) => ($ ($ tup) *, b) $ (, $ tail) *) } ; ($ first : expr $ (,) *) => { std :: iter :: IntoIterator :: into_iter ($ first) } ; ($ first : expr , $ second : expr $ (,) *) => { izip ! ($ first) . zip ($ second) } ; ($ first : expr $ (, $ rest : expr) * $ (,) *) => { izip ! ($ first) $ (. zip ($ rest)) * . map (izip ! (@ closure a => (a) $ (, $ rest) *)) } ; }
    };
}

izip!()