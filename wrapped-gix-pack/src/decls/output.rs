macro_rules! output {
    () => {
        # [doc = " Utilities to encode pack data entries and write them to a `Write` implementation to resemble a pack data file."] # [cfg (feature = "generate")] pub mod output ;
    };
}

output!()