// Generated macro for impl_134 (impl)
macro_rules! Depcrateimpl_134 {
() => {
// Module: crate
// Provides: {"impl_134"}
// Dependencies: {}
impl < 'a > Checker < 'a > { # [doc = " Create a new checker."] pub fn new () -> Checker < 'a > { Checker :: default () } # [doc = " Adds a clause to the checker."] pub fn add_clause (& mut self , clause : & [Lit]) -> Result < () , CheckerError > { let mut ctx = self . ctx . into_partial_ref_mut () ; add_clause (ctx . borrow () , clause) } # [doc = " Add a formula to the checker."] pub fn add_formula (& mut self , formula : & CnfFormula) -> Result < () , CheckerError > { for clause in formula . iter () { self . add_clause (clause) ? ; } Ok (()) } # [doc = " Reads and adds a formula in DIMACS CNF format."] # [doc = ""] # [doc = " Using this avoids creating a temporary [`CnfFormula`](varisat_formula::CnfFormula)."] pub fn add_dimacs_cnf (& mut self , input : impl io :: Read) -> Result < () , Error > { let parser = DimacsParser :: parse_incremental (input , | parser | { Ok (self . add_formula (& parser . take_formula ()) ?) }) ? ; log :: info ! ("Parsed formula with {} variables and {} clauses" , parser . var_count () , parser . clause_count ()) ; Ok (()) } # [doc = " Add a [`ProofProcessor`]."] # [doc = ""] # [doc = " This has to be called before loading any clauses or checking any proofs."] pub fn add_processor (& mut self , processor : & 'a mut dyn ProofProcessor) { self . ctx . processing . processors . push (processor) ; } # [doc = " Add a [`ProofTranscriptProcessor`]."] # [doc = ""] # [doc = " This has to be called before loading any clauses or checking any proofs."] pub fn add_transcript (& mut self , processor : & 'a mut dyn ProofTranscriptProcessor) { self . ctx . processing . transcript_processors . push (processor) ; } # [doc = " Checks a proof in the native Varisat format."] pub fn check_proof (& mut self , input : impl io :: Read) -> Result < () , CheckerError > { let mut ctx = self . ctx . into_partial_ref_mut () ; check_proof (ctx . borrow () , input) } }
};
}
