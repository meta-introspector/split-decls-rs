// Generated macro for into_iter_bound (function)
macro_rules! Depcrate_useless_conversioninto_iter_bound {
() => {
// Module: crate::useless_conversion
// Provides: {"into_iter_bound"}
// Dependencies: {}
# [doc = " Returns the span of the `IntoIterator` trait bound in the function pointed to by `fn_did`,"] # [doc = " iff all of the bounds also hold for the type of the `.into_iter()` receiver."] # [doc = " ```ignore"] # [doc = " pub fn foo<I>(i: I)"] # [doc = " where I: IntoIterator<Item=i32> + ExactSizeIterator"] # [doc = "                                   ^^^^^^^^^^^^^^^^^ this extra bound stops us from suggesting to remove `.into_iter()` ..."] # [doc = " {"] # [doc = "     assert_eq!(i.len(), 3);"] # [doc = " }"] # [doc = ""] # [doc = " pub fn bar() {"] # [doc = "     foo([1, 2, 3].into_iter());"] # [doc = "                  ^^^^^^^^^^^^ ... here, because `[i32; 3]` is not `ExactSizeIterator`"] # [doc = " }"] # [doc = " ```"] fn into_iter_bound < 'tcx > (cx : & LateContext < 'tcx > , fn_did : DefId , into_iter_did : DefId , into_iter_receiver : Ty < 'tcx > , param_index : u32 , node_args : GenericArgsRef < 'tcx > ,) -> Option < Span > { let mut into_iter_span = None ; for (pred , span) in cx . tcx . explicit_predicates_of (fn_did) . predicates { if let ty :: ClauseKind :: Trait (tr) = pred . kind () . skip_binder () && tr . self_ty () . is_param (param_index) { if tr . def_id () == into_iter_did { into_iter_span = Some (* span) ; } else { let tr = cx . tcx . erase_and_anonymize_regions (tr) ; if tr . has_escaping_bound_vars () { return None ; } let args = cx . tcx . mk_args_from_iter (node_args . iter () . enumerate () . map (| (i , arg) | { if i == param_index as usize { GenericArg :: from (into_iter_receiver) } else { arg } })) ; let predicate = EarlyBinder :: bind (tr) . instantiate (cx . tcx , args) ; let obligation = Obligation :: new (cx . tcx , ObligationCause :: dummy () , cx . param_env , predicate) ; if ! cx . tcx . infer_ctxt () . build (cx . typing_mode ()) . predicate_must_hold_modulo_regions (& obligation) { return None ; } } } } into_iter_span }
};
}
