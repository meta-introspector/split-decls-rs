// Generated macro for macro_9516 (macro)
macro_rules! Depcrate_repeat_vec_with_capacitymacro_9516 {
() => {
// Module: crate::repeat_vec_with_capacity
// Provides: {"macro_9516"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " Looks for patterns such as `vec![Vec::with_capacity(x); n]` or `iter::repeat(Vec::with_capacity(x))`."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = " These constructs work by cloning the element, but cloning a `Vec<_>` does not"] # [doc = " respect the old vector's capacity and effectively discards it."] # [doc = ""] # [doc = " This makes `iter::repeat(Vec::with_capacity(x))` especially suspicious because the user most certainly"] # [doc = " expected that the yielded `Vec<_>` will have the requested capacity, otherwise one can simply write"] # [doc = " `iter::repeat(Vec::new())` instead and it will have the same effect."] # [doc = ""] # [doc = " Similarly for `vec![x; n]`, the element `x` is cloned to fill the vec."] # [doc = " Unlike `iter::repeat` however, the vec repeat macro does not have to clone the value `n` times"] # [doc = " but just `n - 1` times, because it can reuse the passed value for the last slot."] # [doc = " That means that the last `Vec<_>` gets the requested capacity but all other ones do not."] # [doc = ""] # [doc = " ### Example"] # [doc = " ```rust"] # [doc = " # use std::iter;"] # [doc = ""] # [doc = " let _: Vec<Vec<u8>> = vec![Vec::with_capacity(42); 123];"] # [doc = " let _: Vec<Vec<u8>> = iter::repeat(Vec::with_capacity(42)).take(123).collect();"] # [doc = " ```"] # [doc = " Use instead:"] # [doc = " ```rust"] # [doc = " # use std::iter;"] # [doc = ""] # [doc = " let _: Vec<Vec<u8>> = iter::repeat_with(|| Vec::with_capacity(42)).take(123).collect();"] # [doc = " //                                      ^^^ this closure executes 123 times"] # [doc = " //                                          and the vecs will have the expected capacity"] # [doc = " ```"] # [clippy :: version = "1.76.0"] pub REPEAT_VEC_WITH_CAPACITY , suspicious , "repeating a `Vec::with_capacity` expression which does not retain capacity" }
};
}
