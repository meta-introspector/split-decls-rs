// Generated macro for check_closure (function)
macro_rules! Depcrate_eta_reductioncheck_closure {
() => {
// Module: crate::eta_reduction
// Provides: {"check_closure"}
// Dependencies: {}
# [expect (clippy :: too_many_lines)] fn check_closure < 'tcx > (cx : & LateContext < 'tcx > , outer_receiver : Option < & Expr < 'tcx > > , expr : & Expr < 'tcx >) { let body = if let ExprKind :: Closure (c) = expr . kind && c . fn_decl . inputs . iter () . all (| ty | matches ! (ty . kind , TyKind :: Infer (()))) && matches ! (c . fn_decl . output , FnRetTy :: DefaultReturn (_)) && ! expr . span . from_expansion () { cx . tcx . hir_body (c . body) } else { return ; } ; if body . value . span . from_expansion () { if body . params . is_empty () && let Some (VecArgs :: Vec (& [])) = VecArgs :: hir (cx , body . value) { let vec_crate = if is_no_std_crate (cx) { "alloc" } else { "std" } ; span_lint_hir_and_then (cx , REDUNDANT_CLOSURE , expr . hir_id , expr . span , "redundant closure" , | diag | { diag . span_suggestion (expr . span , "replace the closure with `Vec::new`" , format ! ("{vec_crate}::vec::Vec::new") , Applicability :: MachineApplicable ,) ; } ,) ; } return ; } if is_adjusted (cx , body . value) { return ; } let typeck = cx . typeck_results () ; let closure = if let ty :: Closure (_ , closure_subs) = typeck . expr_ty (expr) . kind () { closure_subs . as_closure () } else { return ; } ; let closure_sig = cx . tcx . signature_unclosure (closure . sig () , Safety :: Safe) . skip_binder () ; match body . value . kind { ExprKind :: Call (callee , args) if matches ! (callee . kind , ExprKind :: Path (QPath :: Resolved (..) | QPath :: TypeRelative (..))) => { let callee_ty_raw = typeck . expr_ty (callee) ; let callee_ty = callee_ty_raw . peel_refs () ; if matches ! (callee_ty . opt_diag_name (cx) , Some (sym :: Arc | sym :: Rc)) || ! check_inputs (typeck , body . params , None , args) { return ; } let callee_ty_adjustments = typeck . expr_adjustments (callee) ; let callee_ty_adjusted = callee_ty_adjustments . last () . map_or (callee_ty , | a | a . target) ; let sig = match callee_ty_adjusted . kind () { ty :: FnDef (def , _) => { if find_attr ! (cx . tcx . get_all_attrs (* def) , AttributeKind :: TrackCaller (..)) { return ; } cx . tcx . fn_sig (def) . skip_binder () . skip_binder () } , ty :: FnPtr (sig_tys , hdr) => sig_tys . with (* hdr) . skip_binder () , ty :: Closure (_ , subs) => cx . tcx . signature_unclosure (subs . as_closure () . sig () , Safety :: Safe) . skip_binder () , _ => { if typeck . type_dependent_def_id (body . value . hir_id) . is_some () && let subs = typeck . node_args (body . value . hir_id) && let output = typeck . expr_ty (body . value) && let ty :: Tuple (tys) = * subs . type_at (1) . kind () { cx . tcx . mk_fn_sig (tys , output , false , Safety :: Safe , ExternAbi :: Rust) } else { return ; } } , } ; if let Some (outer) = outer_receiver && ty_has_static (sig . output ()) && let generic_args = typeck . node_args (outer . hir_id) && ! generic_args . is_empty () { return ; } if check_sig (closure_sig , sig) && let generic_args = typeck . node_args (callee . hir_id) && ! generic_args . types () . any (| t | matches ! (t . kind () , ty :: Param (_))) && let Ok ((closure_kind , _)) = cx . tcx . infer_ctxt () . build (cx . typing_mode ()) . err_ctxt () . type_implements_fn_trait (cx . param_env , Binder :: bind_with_vars (callee_ty_adjusted , List :: empty ()) , ty :: PredicatePolarity :: Positive ,) { span_lint_hir_and_then (cx , REDUNDANT_CLOSURE , expr . hir_id , expr . span , "redundant closure" , | diag | { if let Some (mut snippet) = snippet_opt (cx , callee . span) { if callee . res_local_id () . is_some_and (| l | { local_used_in (cx , l , args) || local_used_after_expr (cx , l , expr) }) { match closure_kind { ClosureKind :: FnMut => snippet = format ! ("&mut {snippet}") , ClosureKind :: Fn if ! callee_ty_raw . is_ref () => { snippet = format ! ("&{snippet}") ; } , _ => () , } } else if let n_refs = callee_ty_adjustments . iter () . rev () . fold (0 , | acc , adjustment | match adjustment . kind { Adjust :: Deref (Some (_)) => acc + 1 , Adjust :: Deref (_) if acc > 0 => acc + 1 , _ => acc , }) && n_refs > 0 { snippet = format ! ("{}{snippet}" , "*" . repeat (n_refs)) ; } let replace_with = match callee_ty_adjusted . kind () { ty :: FnDef (def , _) => cx . tcx . def_descr (* def) , _ => "function" , } ; diag . span_suggestion (expr . span , format ! ("replace the closure with the {replace_with} itself") , snippet , Applicability :: MachineApplicable ,) ; } } ,) ; } } , ExprKind :: MethodCall (path , self_ , args , _) if check_inputs (typeck , body . params , Some (self_) , args) => { if let Some (method_def_id) = typeck . type_dependent_def_id (body . value . hir_id) && ! find_attr ! (cx . tcx . get_all_attrs (method_def_id) , AttributeKind :: TrackCaller (..)) && check_sig (closure_sig , cx . tcx . fn_sig (method_def_id) . skip_binder () . skip_binder ()) { let mut app = Applicability :: MachineApplicable ; let generic_args = match path . args . and_then (GenericArgs :: span_ext) { Some (span) => format ! ("::{}" , snippet_with_applicability (cx , span , "<..>" , & mut app)) , None => String :: new () , } ; span_lint_hir_and_then (cx , REDUNDANT_CLOSURE_FOR_METHOD_CALLS , expr . hir_id , expr . span , "redundant closure" , | diag | { let args = typeck . node_args (body . value . hir_id) ; let caller = self_ . hir_id . owner . def_id ; let type_name = get_path_from_caller_to_method_type (cx . tcx , caller , method_def_id , args) ; diag . span_suggestion (expr . span , "replace the closure with the method itself" , format ! ("{}::{}{}" , type_name , path . ident . name , generic_args) , app ,) ; } ,) ; } } , _ => () , } }
};
}
