// Generated macro for macro_8230 (macro)
macro_rules! Depcrate_non_send_fields_in_send_tymacro_8230 {
() => {
// Module: crate::non_send_fields_in_send_ty
// Provides: {"macro_8230"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " This lint warns about a `Send` implementation for a type that"] # [doc = " contains fields that are not safe to be sent across threads."] # [doc = " It tries to detect fields that can cause a soundness issue"] # [doc = " when sent to another thread (e.g., `Rc`) while allowing `!Send` fields"] # [doc = " that are expected to exist in a `Send` type, such as raw pointers."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = " Sending the struct to another thread effectively sends all of its fields,"] # [doc = " and the fields that do not implement `Send` can lead to soundness bugs"] # [doc = " such as data races when accessed in a thread"] # [doc = " that is different from the thread that created it."] # [doc = ""] # [doc = " See:"] # [doc = " * [*The Rustonomicon* about *Send and Sync*](https://doc.rust-lang.org/nomicon/send-and-sync.html)"] # [doc = " * [The documentation of `Send`](https://doc.rust-lang.org/std/marker/trait.Send.html)"] # [doc = ""] # [doc = " ### Known Problems"] # [doc = " This lint relies on heuristics to distinguish types that are actually"] # [doc = " unsafe to be sent across threads and `!Send` types that are expected to"] # [doc = " exist in  `Send` type. Its rule can filter out basic cases such as"] # [doc = " `Vec<*const T>`, but it's not perfect. Feel free to create an issue if"] # [doc = " you have a suggestion on how this heuristic can be improved."] # [doc = ""] # [doc = " ### Example"] # [doc = " ```rust,ignore"] # [doc = " struct ExampleStruct<T> {"] # [doc = "     rc_is_not_send: Rc<String>,"] # [doc = "     unbounded_generic_field: T,"] # [doc = " }"] # [doc = ""] # [doc = " // This impl is unsound because it allows sending `!Send` types through `ExampleStruct`"] # [doc = " unsafe impl<T> Send for ExampleStruct<T> {}"] # [doc = " ```"] # [doc = " Use thread-safe types like [`std::sync::Arc`](https://doc.rust-lang.org/std/sync/struct.Arc.html)"] # [doc = " or specify correct bounds on generic type parameters (`T: Send`)."] # [clippy :: version = "1.57.0"] pub NON_SEND_FIELDS_IN_SEND_TY , nursery , "there is a field that is not safe to be sent to another thread in a `Send` struct" }
};
}
