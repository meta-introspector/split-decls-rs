// Generated macro for macro_7441 (macro)
macro_rules! Depcrate_missing_asserts_for_indexingmacro_7441 {
() => {
// Module: crate::missing_asserts_for_indexing
// Provides: {"macro_7441"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " Checks for repeated slice indexing without asserting beforehand that the length"] # [doc = " is greater than the largest index used to index into the slice."] # [doc = ""] # [doc = " ### Why restrict this?"] # [doc = " In the general case where the compiler does not have a lot of information"] # [doc = " about the length of a slice, indexing it repeatedly will generate a bounds check"] # [doc = " for every single index."] # [doc = ""] # [doc = " Asserting that the length of the slice is at least as large as the largest value"] # [doc = " to index beforehand gives the compiler enough information to elide the bounds checks,"] # [doc = " effectively reducing the number of bounds checks from however many times"] # [doc = " the slice was indexed to just one (the assert)."] # [doc = ""] # [doc = " ### Drawbacks"] # [doc = " False positives. It is, in general, very difficult to predict how well"] # [doc = " the optimizer will be able to elide bounds checks and it very much depends on"] # [doc = " the surrounding code. For example, indexing into the slice yielded by the"] # [doc = " [`slice::chunks_exact`](https://doc.rust-lang.org/stable/std/primitive.slice.html#method.chunks_exact)"] # [doc = " iterator will likely have all of the bounds checks elided even without an assert"] # [doc = " if the `chunk_size` is a constant."] # [doc = ""] # [doc = " Asserts are not tracked across function calls. Asserting the length of a slice"] # [doc = " in a different function likely gives the optimizer enough information"] # [doc = " about the length of a slice, but this lint will not detect that."] # [doc = ""] # [doc = " ### Example"] # [doc = " ```no_run"] # [doc = " fn sum(v: &[u8]) -> u8 {"] # [doc = "     // 4 bounds checks"] # [doc = "     v[0] + v[1] + v[2] + v[3]"] # [doc = " }"] # [doc = " ```"] # [doc = " Use instead:"] # [doc = " ```no_run"] # [doc = " fn sum(v: &[u8]) -> u8 {"] # [doc = "     assert!(v.len() > 3);"] # [doc = "     // no bounds checks"] # [doc = "     v[0] + v[1] + v[2] + v[3]"] # [doc = " }"] # [doc = " ```"] # [clippy :: version = "1.74.0"] pub MISSING_ASSERTS_FOR_INDEXING , restriction , "indexing into a slice multiple times without an `assert`" }
};
}
