// Generated macro for macro_3929 (macro)
macro_rules! Depcrate_macro_metavars_in_unsafemacro_3929 {
() => {
// Module: crate::macro_metavars_in_unsafe
// Provides: {"macro_3929"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " Looks for macros that expand metavariables in an unsafe block."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = " This hides an unsafe block and allows the user of the macro to write unsafe code without an explicit"] # [doc = " unsafe block at callsite, making it possible to perform unsafe operations in seemingly safe code."] # [doc = ""] # [doc = " The macro should be restructured so that these metavariables are referenced outside of unsafe blocks"] # [doc = " and that the usual unsafety checks apply to the macro argument."] # [doc = ""] # [doc = " This is usually done by binding it to a variable outside of the unsafe block"] # [doc = " and then using that variable inside of the block as shown in the example, or by referencing it a second time"] # [doc = " in a safe context, e.g. `if false { $expr }`."] # [doc = ""] # [doc = " ### Known limitations"] # [doc = " Due to how macros are represented in the compiler at the time Clippy runs its lints,"] # [doc = " it's not possible to look for metavariables in macro definitions directly."] # [doc = ""] # [doc = " Instead, this lint looks at expansions of macros."] # [doc = " This leads to false negatives for macros that are never actually invoked."] # [doc = ""] # [doc = " By default, this lint is rather conservative and will only emit warnings on publicly-exported"] # [doc = " macros from the same crate, because oftentimes private internal macros are one-off macros where"] # [doc = " this lint would just be noise (e.g. macros that generate `impl` blocks)."] # [doc = " The default behavior should help with preventing a high number of such false positives,"] # [doc = " however it can be configured to also emit warnings in private macros if desired."] # [doc = ""] # [doc = " ### Example"] # [doc = " ```no_run"] # [doc = " /// Gets the first element of a slice"] # [doc = " macro_rules! first {"] # [doc = "     ($slice:expr) => {"] # [doc = "         unsafe {"] # [doc = "             let slice = $slice; // ⚠\u{fe0f} expansion inside of `unsafe {}`"] # [doc = ""] # [doc = "             assert!(!slice.is_empty());"] # [doc = "             // SAFETY: slice is checked to have at least one element"] # [doc = "             slice.first().unwrap_unchecked()"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(*first!(&[1i32]), 1);"] # [doc = ""] # [doc = " // This will compile as a consequence (note the lack of `unsafe {}`)"] # [doc = " assert_eq!(*first!(std::hint::unreachable_unchecked() as &[i32]), 1);"] # [doc = " ```"] # [doc = " Use instead:"] # [doc = " ```compile_fail"] # [doc = " macro_rules! first {"] # [doc = "     ($slice:expr) => {{"] # [doc = "         let slice = $slice; // ✅ outside of `unsafe {}`"] # [doc = "         unsafe {"] # [doc = "             assert!(!slice.is_empty());"] # [doc = "             // SAFETY: slice is checked to have at least one element"] # [doc = "             slice.first().unwrap_unchecked()"] # [doc = "         }"] # [doc = "     }}"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(*first!(&[1]), 1);"] # [doc = ""] # [doc = " // This won't compile:"] # [doc = " assert_eq!(*first!(std::hint::unreachable_unchecked() as &[i32]), 1);"] # [doc = " ```"] # [clippy :: version = "1.80.0"] pub MACRO_METAVARS_IN_UNSAFE , suspicious , "expanding macro metavariables in an unsafe block" }
};
}
