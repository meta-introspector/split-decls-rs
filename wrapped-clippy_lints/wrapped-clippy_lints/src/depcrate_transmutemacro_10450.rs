// Generated macro for macro_10450 (macro)
macro_rules! Depcrate_transmutemacro_10450 {
() => {
// Module: crate::transmute
// Provides: {"macro_10450"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " Checks for integer validity checks, followed by a transmute that is (incorrectly) evaluated"] # [doc = " eagerly (e.g. using `bool::then_some`)."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = " Eager evaluation means that the `transmute` call is executed regardless of whether the condition is true or false."] # [doc = " This can introduce unsoundness and other subtle bugs."] # [doc = ""] # [doc = " ### Example"] # [doc = " Consider the following function which is meant to convert an unsigned integer to its enum equivalent via transmute."] # [doc = ""] # [doc = " ```no_run"] # [doc = " #[repr(u8)]"] # [doc = " enum Opcode {"] # [doc = "     Add = 0,"] # [doc = "     Sub = 1,"] # [doc = "     Mul = 2,"] # [doc = "     Div = 3"] # [doc = " }"] # [doc = ""] # [doc = " fn int_to_opcode(op: u8) -> Option<Opcode> {"] # [doc = "     (op < 4).then_some(unsafe { std::mem::transmute(op) })"] # [doc = " }"] # [doc = " ```"] # [doc = " This may appear fine at first given that it checks that the `u8` is within the validity range of the enum,"] # [doc = " *however* the transmute is evaluated eagerly, meaning that it executes even if `op >= 4`!"] # [doc = ""] # [doc = " This makes the function unsound, because it is possible for the caller to cause undefined behavior"] # [doc = " (creating an enum with an invalid bitpattern) entirely in safe code only by passing an incorrect value,"] # [doc = " which is normally only a bug that is possible in unsafe code."] # [doc = ""] # [doc = " One possible way in which this can go wrong practically is that the compiler sees it as:"] # [doc = " ```rust,ignore (illustrative)"] # [doc = " let temp: Foo = unsafe { std::mem::transmute(op) };"] # [doc = " (0 < 4).then_some(temp)"] # [doc = " ```"] # [doc = " and optimizes away the `(0 < 4)` check based on the assumption that since a `Foo` was created from `op` with the validity range `0..3`,"] # [doc = " it is **impossible** for this condition to be false."] # [doc = ""] # [doc = " In short, it is possible for this function to be optimized in a way that makes it [never return `None`](https://godbolt.org/z/ocrcenevq),"] # [doc = " even if passed the value `4`."] # [doc = ""] # [doc = " This can be avoided by instead using lazy evaluation. For the example above, this should be written:"] # [doc = " ```rust,ignore (illustrative)"] # [doc = " fn int_to_opcode(op: u8) -> Option<Opcode> {"] # [doc = "     (op < 4).then(|| unsafe { std::mem::transmute(op) })"] # [doc = "              ^^^^ ^^ `bool::then` only executes the closure if the condition is true!"] # [doc = " }"] # [doc = " ```"] # [clippy :: version = "1.77.0"] pub EAGER_TRANSMUTE , correctness , "eager evaluation of `transmute`" }
};
}
