// Generated macro for impl_8002 (impl)
macro_rules! Depcrate_needless_pass_by_valueimpl_8002 {
() => {
// Module: crate::needless_pass_by_value
// Provides: {"impl_8002"}
// Dependencies: {}
impl < 'tcx > LateLintPass < 'tcx > for NeedlessPassByValue { # [expect (clippy :: too_many_lines)] fn check_fn (& mut self , cx : & LateContext < 'tcx > , kind : FnKind < 'tcx > , decl : & 'tcx FnDecl < '_ > , body : & 'tcx Body < '_ > , span : Span , fn_def_id : LocalDefId ,) { if span . from_expansion () { return ; } let hir_id = cx . tcx . local_def_id_to_hir_id (fn_def_id) ; match kind { FnKind :: ItemFn (.. , header) => { let attrs = cx . tcx . hir_attrs (hir_id) ; if header . abi != ExternAbi :: Rust || requires_exact_signature (attrs) { return ; } } , FnKind :: Method (..) => () , FnKind :: Closure => return , } if let Node :: Item (item) = cx . tcx . parent_hir_node (hir_id) && matches ! (item . kind , ItemKind :: Impl (Impl { of_trait : Some (_) , .. }) | ItemKind :: Trait (..)) { return ; } let allowed_traits = [need ! (cx . tcx . lang_items () . fn_trait ()) , need ! (cx . tcx . lang_items () . fn_once_trait ()) , need ! (cx . tcx . lang_items () . fn_mut_trait ()) , need ! (cx . tcx . get_diagnostic_item (sym :: RangeBounds)) ,] ; let sized_trait = need ! (cx . tcx . lang_items () . sized_trait ()) ; let meta_sized_trait = need ! (cx . tcx . lang_items () . meta_sized_trait ()) ; let preds = traits :: elaborate (cx . tcx , cx . param_env . caller_bounds () . iter ()) . filter (| p | ! p . is_global ()) . filter_map (| pred | { match pred . kind () . no_bound_vars () { Some (ty :: ClauseKind :: Trait (pred)) if pred . def_id () != sized_trait && pred . def_id () != meta_sized_trait => { Some (pred) } , _ => None , } }) . collect :: < Vec < _ > > () ; let MovedVariablesCtxt { moved_vars } = { let mut ctx = MovedVariablesCtxt :: default () ; euv :: ExprUseVisitor :: for_clippy (cx , fn_def_id , & mut ctx) . consume_body (body) . into_ok () ; ctx } ; let fn_sig = cx . tcx . fn_sig (fn_def_id) . instantiate_identity () ; let fn_sig = cx . tcx . liberate_late_bound_regions (fn_def_id . to_def_id () , fn_sig) ; for (idx , ((input , & ty) , arg)) in decl . inputs . iter () . zip (fn_sig . inputs ()) . zip (body . params) . enumerate () { if span == input . span { return ; } if let PatKind :: Binding (.. , ident , _) = arg . pat . kind { if idx == 0 && ident . name == kw :: SelfLower { continue ; } if ident . name . as_str () . starts_with ('_') { continue ; } } let (implements_borrow_trait , all_borrowable_trait) = { let preds = preds . iter () . filter (| t | t . self_ty () == ty) . collect :: < Vec < _ > > () ; (preds . iter () . any (| t | cx . tcx . is_diagnostic_item (sym :: Borrow , t . def_id ())) , ! preds . is_empty () && { let ty_empty_region = Ty :: new_imm_ref (cx . tcx , cx . tcx . lifetimes . re_erased , ty) ; preds . iter () . all (| t | { let ty_params = t . trait_ref . args . iter () . skip (1) . collect :: < Vec < _ > > () ; implements_trait (cx , ty_empty_region , t . def_id () , & ty_params) }) } ,) } ; if ! is_self (arg) && ! ty . is_mutable_ptr () && ! is_copy (cx , ty) && ty . is_sized (cx . tcx , cx . typing_env ()) && ! allowed_traits . iter () . any (| & t | { implements_trait_with_env_from_iter (cx . tcx , cx . typing_env () , ty , t , None , [None :: < ty :: GenericArg < 'tcx > >] ,) }) && ! implements_borrow_trait && ! all_borrowable_trait && let PatKind :: Binding (BindingMode (_ , Mutability :: Not) , canonical_id , ..) = arg . pat . kind && ! moved_vars . contains (& canonical_id) { let sugg = | diag : & mut Diag < '_ , () > | { if let ty :: Adt (def , ..) = ty . kind () && let Some (span) = cx . tcx . hir_span_if_local (def . did ()) && type_allowed_to_implement_copy (cx . tcx , cx . param_env , ty , traits :: ObligationCause :: dummy_with_span (span) , rustc_hir :: Safety :: Safe ,) . is_ok () { diag . span_help (span , "or consider marking this type as `Copy`") ; } if ty . is_diag_item (cx , sym :: Vec) && let Some (clone_spans) = get_spans (cx , body , idx , & [(sym :: clone , ".to_owned()")]) && let TyKind :: Path (QPath :: Resolved (_ , path)) = input . kind && let Some (elem_ty) = path . segments . iter () . find (| seg | seg . ident . name == sym :: Vec) . and_then (| ps | ps . args . as_ref ()) . map (| params | { params . args . iter () . find_map (| arg | match arg { GenericArg :: Type (ty) => Some (ty) , _ => None , }) . unwrap () }) { let slice_ty = format ! ("&[{}]" , snippet (cx , elem_ty . span , "_")) ; diag . span_suggestion (input . span , "consider changing the type to" , slice_ty , Applicability :: Unspecified ,) ; for (span , suggestion) in clone_spans { diag . span_suggestion (span , span . get_source_text (cx) . map_or_else (| | "change the call to" . to_owned () , | src | format ! ("change `{src}` to") ,) , suggestion , Applicability :: Unspecified ,) ; } return ; } if ty . is_lang_item (cx , LangItem :: String) && let Some (clone_spans) = get_spans (cx , body , idx , & [(sym :: clone , ".to_string()") , (sym :: as_str , "")]) { diag . span_suggestion (input . span , "consider changing the type to" , "&str" , Applicability :: Unspecified ,) ; for (span , suggestion) in clone_spans { diag . span_suggestion (span , span . get_source_text (cx) . map_or_else (| | "change the call to" . to_owned () , | src | format ! ("change `{src}` to") ,) , suggestion , Applicability :: Unspecified ,) ; } return ; } diag . span_suggestion_verbose (peel_hir_ty_options (cx , input) . span . shrink_to_lo () , "consider taking a reference instead" , '&' , Applicability :: MaybeIncorrect ,) ; } ; span_lint_and_then (cx , NEEDLESS_PASS_BY_VALUE , input . span , "this argument is passed by value, but not consumed in the function body" , sugg ,) ; } } } }
};
}
