// Generated macro for check (function)
macro_rules! Depcrate_methods_filter_map_bool_thencheck {
() => {
// Module: crate::methods::filter_map_bool_then
// Provides: {"check"}
// Dependencies: {}
pub (super) fn check < 'tcx > (cx : & LateContext < 'tcx > , expr : & 'tcx Expr < 'tcx > , arg : & Expr < '_ > , call_span : Span) { if ! expr . span . in_external_macro (cx . sess () . source_map ()) && cx . ty_based_def (expr) . opt_parent (cx) . is_diag_item (cx , sym :: Iterator) && let ExprKind :: Closure (closure) = arg . kind && let body = cx . tcx . hir_body (closure . body) && let value = peel_blocks (body . value) && let param_ty = closure . fn_decl . inputs [0] && let param = body . params [0] && let param_ty = cx . tcx . liberate_late_bound_regions (closure . def_id . to_def_id () , Binder :: bind_with_vars (cx . typeck_results () . node_type (param_ty . hir_id) , cx . tcx . late_bound_vars (cx . tcx . local_def_id_to_hir_id (closure . def_id)) ,) ,) && is_copy (cx , param_ty) && let ExprKind :: MethodCall (_ , recv , [then_arg] , _) = value . kind && let ExprKind :: Closure (then_closure) = then_arg . kind && let then_body = peel_blocks (cx . tcx . hir_body (then_closure . body) . value) && let Some (def_id) = cx . typeck_results () . type_dependent_def_id (value . hir_id) && cx . tcx . is_diagnostic_item (sym :: bool_then , def_id) && ! is_from_proc_macro (cx , expr) && let needed_derefs = cx . typeck_results () . expr_adjustments (recv) . iter () . filter (| adj | matches ! (adj . kind , Adjust :: Deref (_))) . count () && let Some (param_snippet) = param . span . get_source_text (cx) { let mut applicability = Applicability :: MachineApplicable ; let (filter , _) = snippet_with_context (cx , recv . span , expr . span . ctxt () , ".." , & mut applicability) ; let (map , _) = snippet_with_context (cx , then_body . span , expr . span . ctxt () , ".." , & mut applicability) ; span_lint_and_then (cx , FILTER_MAP_BOOL_THEN , call_span , "usage of `bool::then` in `filter_map`" , | diag | { if can_filter_and_then_move_to_closure (cx , & param , recv , then_body) { diag . span_suggestion (call_span , "use `filter` then `map` instead" , format ! ("filter(|&{param_snippet}| {derefs}{filter}).map(|{param_snippet}| {map})" , derefs = "*" . repeat (needed_derefs)) , applicability ,) ; } else { diag . help ("consider using `filter` then `map` instead") ; } } ,) ; } }
};
}
