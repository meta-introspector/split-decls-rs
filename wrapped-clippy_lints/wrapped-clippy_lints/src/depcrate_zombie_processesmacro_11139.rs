// Generated macro for macro_11139 (macro)
macro_rules! Depcrate_zombie_processesmacro_11139 {
() => {
// Module: crate::zombie_processes
// Provides: {"macro_11139"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " Looks for code that spawns a process but never calls `wait()` on the child."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = " As explained in the [standard library documentation](https://doc.rust-lang.org/stable/std/process/struct.Child.html#warning),"] # [doc = " calling `wait()` is necessary on Unix platforms to properly release all OS resources associated with the process."] # [doc = " Not doing so will effectively leak process IDs and/or other limited global resources,"] # [doc = " which can eventually lead to resource exhaustion, so it's recommended to call `wait()` in long-running applications."] # [doc = " Such processes are called \"zombie processes\"."] # [doc = ""] # [doc = " To reduce the rate of false positives, if the spawned process is assigned to a binding, the lint actually works the other way around; it"] # [doc = " conservatively checks that all uses of a variable definitely don't call `wait()` and only then emits a warning."] # [doc = " For that reason, a seemingly unrelated use can get called out as calling `wait()` in help messages."] # [doc = ""] # [doc = " ### Control flow"] # [doc = " If a `wait()` call exists in an if/then block but not in the else block (or there is no else block),"] # [doc = " then this still gets linted as not calling `wait()` in all code paths."] # [doc = " Likewise, when early-returning from the function, `wait()` calls that appear after the return expression"] # [doc = " are also not accepted."] # [doc = " In other words, the `wait()` call must be unconditionally reachable after the spawn expression."] # [doc = ""] # [doc = " ### Example"] # [doc = " ```rust"] # [doc = " use std::process::Command;"] # [doc = ""] # [doc = " let _child = Command::new(\"ls\").spawn().expect(\"failed to execute child\");"] # [doc = " ```"] # [doc = " Use instead:"] # [doc = " ```rust"] # [doc = " use std::process::Command;"] # [doc = ""] # [doc = " let mut child = Command::new(\"ls\").spawn().expect(\"failed to execute child\");"] # [doc = " child.wait().expect(\"failed to wait on child\");"] # [doc = " ```"] # [clippy :: version = "1.83.0"] pub ZOMBIE_PROCESSES , suspicious , "not waiting on a spawned child process" }
};
}
