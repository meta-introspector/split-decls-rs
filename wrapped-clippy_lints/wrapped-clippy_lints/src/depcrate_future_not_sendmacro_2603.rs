// Generated macro for macro_2603 (macro)
macro_rules! Depcrate_future_not_sendmacro_2603 {
() => {
// Module: crate::future_not_send
// Provides: {"macro_2603"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " This lint requires Future implementations returned from"] # [doc = " functions and methods to implement the `Send` marker trait,"] # [doc = " ignoring type parameters."] # [doc = ""] # [doc = " If a function is generic and its Future conditionally implements `Send`"] # [doc = " based on a generic parameter then it is considered `Send` and no warning is emitted."] # [doc = ""] # [doc = " This can be used by library authors (public and internal) to ensure"] # [doc = " their functions are compatible with both multi-threaded runtimes that require `Send` futures,"] # [doc = " as well as single-threaded runtimes where callers may choose `!Send` types"] # [doc = " for generic parameters."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = " A Future implementation captures some state that it"] # [doc = " needs to eventually produce its final value. When targeting a multithreaded"] # [doc = " executor (which is the norm on non-embedded devices) this means that this"] # [doc = " state may need to be transported to other threads, in other words the"] # [doc = " whole Future needs to implement the `Send` marker trait. If it does not,"] # [doc = " then the resulting Future cannot be submitted to a thread pool in the"] # [doc = " end user’s code."] # [doc = ""] # [doc = " Especially for generic functions it can be confusing to leave the"] # [doc = " discovery of this problem to the end user: the reported error location"] # [doc = " will be far from its cause and can in many cases not even be fixed without"] # [doc = " modifying the library where the offending Future implementation is"] # [doc = " produced."] # [doc = ""] # [doc = " ### Example"] # [doc = " ```no_run"] # [doc = " async fn not_send(bytes: std::rc::Rc<[u8]>) {}"] # [doc = " ```"] # [doc = " Use instead:"] # [doc = " ```no_run"] # [doc = " async fn is_send(bytes: std::sync::Arc<[u8]>) {}"] # [doc = " ```"] # [clippy :: version = "1.44.0"] pub FUTURE_NOT_SEND , nursery , "public Futures must be Send" }
};
}
