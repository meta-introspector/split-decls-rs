// Generated macro for impl_1330 (impl)
macro_rules! Depcrate_defaultimpl_1330 {
() => {
// Module: crate::default
// Provides: {"impl_1330"}
// Dependencies: {}
impl < 'tcx > LateLintPass < 'tcx > for Default { fn check_expr (& mut self , cx : & LateContext < 'tcx > , expr : & 'tcx Expr < '_ >) { if ! expr . span . from_expansion () && ! self . reassigned_linted . contains (& expr . span) && let ExprKind :: Call (path , []) = expr . kind && ! in_automatically_derived (cx . tcx , expr . hir_id) && let ExprKind :: Path (ref qpath) = path . kind && let Some (def_id) = cx . qpath_res (qpath , path . hir_id) . opt_def_id () && cx . tcx . is_diagnostic_item (sym :: default_fn , def_id) && ! is_update_syntax_base (cx , expr) && let QPath :: Resolved (None , _path) = qpath && let expr_ty = cx . typeck_results () . expr_ty (expr) && let ty :: Adt (def , ..) = expr_ty . kind () && ! is_from_proc_macro (cx , expr) { let replacement = with_forced_trimmed_paths ! (format ! ("{}::default()" , cx . tcx . def_path_str (def . did ()))) ; span_lint_and_sugg (cx , DEFAULT_TRAIT_ACCESS , expr . span , format ! ("calling `{replacement}` is more clear than this expression") , "try" , replacement , Applicability :: Unspecified ,) ; } } # [expect (clippy :: too_many_lines)] fn check_block (& mut self , cx : & LateContext < 'tcx > , block : & Block < 'tcx >) { let stmts_head = match block . stmts { [] | [_] => return , [head @ .. , _] => head , } ; for (stmt_idx , stmt) in stmts_head . iter () . enumerate () { let (local , variant , binding_name , binding_type , span) = if let StmtKind :: Let (local) = stmt . kind && let Some (expr) = local . init && ! in_automatically_derived (cx . tcx , expr . hir_id) && ! expr . span . from_expansion () && let PatKind :: Binding (_ , binding_id , ident , _) = local . pat . kind && is_expr_default (cx , expr) && let binding_type = cx . typeck_results () . node_type (binding_id) && let ty :: Adt (adt , args) = * binding_type . kind () && adt . is_struct () && let variant = adt . non_enum_variant () && ! variant . field_list_has_applicable_non_exhaustive () && let module_did = cx . tcx . parent_module (stmt . hir_id) && variant . fields . iter () . all (| field | field . vis . is_accessible_from (module_did , cx . tcx)) && let all_fields_are_copy = variant . fields . iter () . all (| field | { is_copy (cx , cx . tcx . type_of (field . did) . instantiate (cx . tcx , args)) }) && (! has_drop (cx , binding_type) || all_fields_are_copy) { (local , variant , ident . name , binding_type , expr . span) } else { continue ; } ; let init_ctxt = local . span . ctxt () ; let mut first_assign = None ; let mut assigned_fields = Vec :: new () ; let mut cancel_lint = false ; for consecutive_statement in & block . stmts [stmt_idx + 1 ..] { if let Some ((field_ident , assign_rhs)) = field_reassigned_by_stmt (consecutive_statement , binding_name) { if contains_name (binding_name , assign_rhs , cx) || init_ctxt != consecutive_statement . span . ctxt () { cancel_lint = true ; break ; } if let Some (prev) = assigned_fields . iter_mut () . find (| (field_name , _) | field_name == & field_ident . name) { * prev = (field_ident . name , assign_rhs) ; } else { assigned_fields . push ((field_ident . name , assign_rhs)) ; } if first_assign . is_none () { first_assign = Some (consecutive_statement) ; } } else { break ; } } if ! assigned_fields . is_empty () && ! cancel_lint { let ext_with_default = ! variant . fields . iter () . all (| field | assigned_fields . iter () . any (| (a , _) | a == & field . name)) ; let mut app = Applicability :: Unspecified ; let field_list = assigned_fields . into_iter () . map (| (field , rhs) | { let value_snippet = snippet_with_context (cx , rhs . span , init_ctxt , ".." , & mut app) . 0 ; format ! ("{field}: {value_snippet}") }) . collect :: < Vec < String > > () . join (", ") ; let binding_type = if let ty :: Adt (adt_def , args) = binding_type . kind () && ! args . is_empty () { let adt_def_ty_name = cx . tcx . item_name (adt_def . did ()) ; let generic_args = args . iter () . collect :: < Vec < _ > > () ; let tys_str = generic_args . iter () . map (ToString :: to_string) . collect :: < Vec < _ > > () . join (", ") ; format ! ("{adt_def_ty_name}::<{tys_str}>") } else { binding_type . to_string () } ; let sugg = if ext_with_default { if field_list . is_empty () { format ! ("{binding_type}::default()") } else { format ! ("{binding_type} {{ {field_list}, ..Default::default() }}") } } else { format ! ("{binding_type} {{ {field_list} }}") } ; span_lint_and_note (cx , FIELD_REASSIGN_WITH_DEFAULT , first_assign . unwrap () . span , "field assignment outside of initializer for an instance created with Default::default()" , Some (local . span) , format ! ("consider initializing the variable with `{sugg}` and removing relevant reassignments") ,) ; self . reassigned_linted . insert (span) ; } } } }
};
}
