// Generated macro for impl_1438 (impl)
macro_rules! Depcrate_dereferenceimpl_1438 {
() => {
// Module: crate::dereference
// Provides: {"impl_1438"}
// Dependencies: {}
impl < 'tcx > Dereferencing < 'tcx > { fn in_deref_impl (& self) -> bool { self . outermost_deref_impl . is_some () } fn check_local_usage (& mut self , cx : & LateContext < 'tcx > , e : & Expr < 'tcx > , local : HirId) { if let Some (outer_pat) = self . ref_locals . get_mut (& local) && let Some (pat) = outer_pat && ! matches ! (cx . typeck_results () . expr_adjustments (e) , [Adjustment { kind : Adjust :: Deref (_) , .. } , Adjustment { kind : Adjust :: Deref (_) , .. } , ..]) { match get_parent_expr (cx , e) { Some (Expr { kind : ExprKind :: Field (..) , .. }) => () , Some (& Expr { span , kind : ExprKind :: Unary (UnOp :: Deref , _) , .. }) if ! span . from_expansion () => { let snip = snippet_with_context (cx , e . span , span . ctxt () , ".." , & mut pat . app) . 0 ; pat . replacements . push ((span , snip . into ())) ; } , Some (parent) if ! parent . span . from_expansion () => { if cx . precedence (parent) == ExprPrecedence :: Unambiguous { * outer_pat = None ; } else { pat . always_deref = false ; let snip = snippet_with_context (cx , e . span , parent . span . ctxt () , ".." , & mut pat . app) . 0 ; pat . replacements . push ((e . span , format ! ("&{snip}"))) ; } } , _ if ! e . span . from_expansion () => { pat . always_deref = false ; let snip = snippet_with_applicability (cx , e . span , ".." , & mut pat . app) ; pat . replacements . push ((e . span , format ! ("&{snip}"))) ; } , _ => * outer_pat = None , } } } # [expect (clippy :: needless_pass_by_value , clippy :: too_many_lines)] fn report (& self , cx : & LateContext < 'tcx > , expr : & 'tcx Expr < '_ > , state : State , data : StateData < 'tcx > , typeck : & 'tcx TypeckResults < 'tcx > ,) { match state { State :: DerefMethod { ty_changed_count , is_ufcs , mutbl , } => { if self . in_deref_impl () { return ; } let mut app = Applicability :: MachineApplicable ; let (expr_str , expr_is_macro_call) = snippet_with_context (cx , expr . span , data . first_expr . span . ctxt () , ".." , & mut app) ; let ty = typeck . expr_ty (expr) ; let (_ , ref_count , _) = peel_and_count_ty_refs (ty) ; let deref_str = if ty_changed_count >= ref_count && ref_count != 0 { "*" . repeat (ty_changed_count + 1) } else { "*" . repeat (ty_changed_count) } ; let addr_of_str = if ty_changed_count < ref_count { if mutbl == Mutability :: Not && matches ! (ty . kind () , ty :: Ref (_ , _ , Mutability :: Mut)) { "&*" } else { "" } } else if mutbl == Mutability :: Mut { "&mut " } else { "&" } ; let expr_str = if ! expr_is_macro_call && is_ufcs && cx . precedence (expr) < ExprPrecedence :: Prefix { Cow :: Owned (format ! ("({expr_str})")) } else { expr_str } ; span_lint_and_sugg (cx , EXPLICIT_DEREF_METHODS , data . first_expr . span , match mutbl { Mutability :: Not => "explicit `deref` method call" , Mutability :: Mut => "explicit `deref_mut` method call" , } , "try" , format ! ("{addr_of_str}{deref_str}{expr_str}") , app ,) ; } , State :: DerefedBorrow (state) => { if let ExprKind :: AddrOf (BorrowKind :: Ref , _ , subexpr) = data . first_expr . kind && let ExprKind :: Unary (UnOp :: Deref , subsubexpr) = subexpr . kind && cx . typeck_results () . expr_ty_adjusted (subsubexpr) . is_raw_ptr () { return ; } let mut app = Applicability :: MachineApplicable ; let (snip , snip_is_macro) = snippet_with_context (cx , expr . span , data . first_expr . span . ctxt () , ".." , & mut app) ; span_lint_hir_and_then (cx , NEEDLESS_BORROW , data . first_expr . hir_id , data . first_expr . span , state . msg , | diag | { let needs_paren = match cx . tcx . parent_hir_node (data . first_expr . hir_id) { Node :: Expr (e) => match e . kind { ExprKind :: Call (callee , _) if callee . hir_id != data . first_expr . hir_id => false , ExprKind :: Call (..) => { cx . precedence (expr) < ExprPrecedence :: Unambiguous || matches ! (expr . kind , ExprKind :: Field (..)) } , _ => cx . precedence (expr) < cx . precedence (e) , } , _ => false , } ; let is_in_tuple = matches ! (get_parent_expr (cx , data . first_expr) , Some (Expr { kind : ExprKind :: Tup (..) , .. })) ; let sugg = if ! snip_is_macro && needs_paren && ! has_enclosing_paren (& snip) && ! is_in_tuple { format ! ("({snip})") } else { snip . into () } ; diag . span_suggestion (data . first_expr . span , "change this to" , sugg , app) ; } ,) ; } , State :: ExplicitDeref { mutability } => { if is_block_like (expr) && let ty :: Ref (_ , ty , _) = data . adjusted_ty . kind () && ty . is_sized (cx . tcx , cx . typing_env ()) { return ; } let ty = typeck . expr_ty (expr) ; if let ty :: Ref (_ , dst , _) = data . adjusted_ty . kind () && dst . is_slice () { let (src , n_src_refs , _) = peel_and_count_ty_refs (ty) ; if n_src_refs >= 2 && src . is_array () { return ; } } let (prefix , needs_paren) = match mutability { Some (mutability) if ! ty . is_ref () => { let prefix = match mutability { Mutability :: Not => "&" , Mutability :: Mut => "&mut " , } ; (prefix , cx . precedence (expr) < ExprPrecedence :: Prefix) } , None if ! ty . is_ref () && data . adjusted_ty . is_ref () => ("&" , false) , _ => ("" , false) , } ; span_lint_hir_and_then (cx , EXPLICIT_AUTO_DEREF , data . first_expr . hir_id , data . first_expr . span , "deref which would be done by auto-deref" , | diag | { let mut app = Applicability :: MachineApplicable ; let (snip , snip_is_macro) = snippet_with_context (cx , expr . span , data . first_expr . span . ctxt () , ".." , & mut app) ; let sugg = if ! snip_is_macro && needs_paren && ! has_enclosing_paren (& snip) { format ! ("{prefix}({snip})") } else { format ! ("{prefix}{snip}") } ; diag . span_suggestion (data . first_expr . span , "try" , sugg , app) ; } ,) ; } , State :: ExplicitDerefField { derefs_manually_drop , .. } => { let (snip_span , needs_parens) = if matches ! (expr . kind , ExprKind :: Field (..)) && (derefs_manually_drop || adjust_derefs_manually_drop (typeck . expr_adjustments (data . first_expr) , typeck . expr_ty (data . first_expr) ,)) { let parent_id = cx . tcx . parent_hir_id (expr . hir_id) ; if parent_id == data . first_expr . hir_id { return ; } (cx . tcx . hir_node (parent_id) . expect_expr () . span , true) } else { (expr . span , false) } ; span_lint_hir_and_then (cx , EXPLICIT_AUTO_DEREF , data . first_expr . hir_id , data . first_expr . span , "deref which would be done by auto-deref" , | diag | { let mut app = Applicability :: MachineApplicable ; let snip = snippet_with_context (cx , snip_span , data . first_expr . span . ctxt () , ".." , & mut app) . 0 ; let sugg = if needs_parens { format ! ("({snip})") } else { snip . into_owned () } ; diag . span_suggestion (data . first_expr . span , "try" , sugg , app) ; } ,) ; } , State :: Borrow { .. } | State :: Reborrow { .. } => () , } } }
};
}
