// Generated macro for macro_3909 (macro)
macro_rules! Depcrate_loopsmacro_3909 {
() => {
// Module: crate::loops
// Provides: {"macro_3909"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " Checks for usage of a character position yielded by `.chars().enumerate()` in a context where a **byte index** is expected,"] # [doc = " such as an argument to a specific `str` method or indexing into a `str` or `String`."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = " A character (more specifically, a Unicode scalar value) that is yielded by `str::chars` can take up multiple bytes,"] # [doc = " so a character position does not necessarily have the same byte index at which the character is stored."] # [doc = " Thus, using the character position where a byte index is expected can unexpectedly return wrong values"] # [doc = " or panic when the string consists of multibyte characters."] # [doc = ""] # [doc = " For example, the character `a` in `äa` is stored at byte index 2 but has the character position 1."] # [doc = " Using the character position 1 to index into the string will lead to a panic as it is in the middle of the first character."] # [doc = ""] # [doc = " Instead of `.chars().enumerate()`, the correct iterator to use is `.char_indices()`, which yields byte indices."] # [doc = ""] # [doc = " This pattern is technically fine if the strings are known to only use the ASCII subset,"] # [doc = " though in those cases it would be better to use `bytes()` directly to make the intent clearer,"] # [doc = " but there is also no downside to just using `.char_indices()` directly and supporting non-ASCII strings."] # [doc = ""] # [doc = " You may also want to read the [chapter on strings in the Rust Book](https://doc.rust-lang.org/book/ch08-02-strings.html)"] # [doc = " which goes into this in more detail."] # [doc = ""] # [doc = " ### Example"] # [doc = " ```no_run"] # [doc = " # let s = \"...\";"] # [doc = " for (idx, c) in s.chars().enumerate() {"] # [doc = "     let _ = s[idx..]; // ⚠\u{fe0f} Panics for strings consisting of multibyte characters"] # [doc = " }"] # [doc = " ```"] # [doc = " Use instead:"] # [doc = " ```no_run"] # [doc = " # let s = \"...\";"] # [doc = " for (idx, c) in s.char_indices() {"] # [doc = "     let _ = s[idx..];"] # [doc = " }"] # [doc = " ```"] # [clippy :: version = "1.88.0"] pub CHAR_INDICES_AS_BYTE_INDICES , correctness , "using the character position yielded by `.chars().enumerate()` in a context where a byte index is expected" }
};
}
