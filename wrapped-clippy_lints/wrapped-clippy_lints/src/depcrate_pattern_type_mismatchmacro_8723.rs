// Generated macro for macro_8723 (macro)
macro_rules! Depcrate_pattern_type_mismatchmacro_8723 {
() => {
// Module: crate::pattern_type_mismatch
// Provides: {"macro_8723"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " Checks for patterns that aren't exact representations of the types"] # [doc = " they are applied to."] # [doc = ""] # [doc = " To satisfy this lint, you will have to adjust either the expression that is matched"] # [doc = " against or the pattern itself, as well as the bindings that are introduced by the"] # [doc = " adjusted patterns. For matching you will have to either dereference the expression"] # [doc = " with the `*` operator, or amend the patterns to explicitly match against `&<pattern>`"] # [doc = " or `&mut <pattern>` depending on the reference mutability. For the bindings you need"] # [doc = " to use the inverse. You can leave them as plain bindings if you wish for the value"] # [doc = " to be copied, but you must use `ref mut <variable>` or `ref <variable>` to construct"] # [doc = " a reference into the matched structure."] # [doc = ""] # [doc = " If you are looking for a way to learn about ownership semantics in more detail, it"] # [doc = " is recommended to look at IDE options available to you to highlight types, lifetimes"] # [doc = " and reference semantics in your code. The available tooling would expose these things"] # [doc = " in a general way even outside of the various pattern matching mechanics. Of course"] # [doc = " this lint can still be used to highlight areas of interest and ensure a good understanding"] # [doc = " of ownership semantics."] # [doc = ""] # [doc = " ### Why restrict this?"] # [doc = " It increases ownership hints in the code, and will guard against some changes"] # [doc = " in ownership."] # [doc = ""] # [doc = " ### Example"] # [doc = " This example shows the basic adjustments necessary to satisfy the lint. Note how"] # [doc = " the matched expression is explicitly dereferenced with `*` and the `inner` variable"] # [doc = " is bound to a shared borrow via `ref inner`."] # [doc = ""] # [doc = " ```rust,ignore"] # [doc = " // Bad"] # [doc = " let value = &Some(Box::new(23));"] # [doc = " match value {"] # [doc = "     Some(inner) => println!(\"{}\", inner),"] # [doc = "     None => println!(\"none\"),"] # [doc = " }"] # [doc = ""] # [doc = " // Good"] # [doc = " let value = &Some(Box::new(23));"] # [doc = " match *value {"] # [doc = "     Some(ref inner) => println!(\"{}\", inner),"] # [doc = "     None => println!(\"none\"),"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " The following example demonstrates one of the advantages of the more verbose style."] # [doc = " Note how the second version uses `ref mut a` to explicitly declare `a` a shared mutable"] # [doc = " borrow, while `b` is simply taken by value. This ensures that the loop body cannot"] # [doc = " accidentally modify the wrong part of the structure."] # [doc = ""] # [doc = " ```rust,ignore"] # [doc = " // Bad"] # [doc = " let mut values = vec![(2, 3), (3, 4)];"] # [doc = " for (a, b) in &mut values {"] # [doc = "     *a += *b;"] # [doc = " }"] # [doc = ""] # [doc = " // Good"] # [doc = " let mut values = vec![(2, 3), (3, 4)];"] # [doc = " for &mut (ref mut a, b) in &mut values {"] # [doc = "     *a += b;"] # [doc = " }"] # [doc = " ```"] # [clippy :: version = "1.47.0"] pub PATTERN_TYPE_MISMATCH , restriction , "type of pattern does not match the expression type" }
};
}
