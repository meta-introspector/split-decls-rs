// Generated macro for macro_10451 (macro)
macro_rules! Depcrate_transmutemacro_10451 {
() => {
// Module: crate::transmute
// Provides: {"macro_10451"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " Checks if transmute calls have all generics specified."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = " If not, one or more unexpected types could be used during `transmute()`, potentially leading"] # [doc = " to Undefined Behavior or other problems."] # [doc = ""] # [doc = " This is particularly dangerous in case a seemingly innocent/unrelated change causes type"] # [doc = " inference to result in a different type. For example, if `transmute()` is the tail"] # [doc = " expression of an `if`-branch, and the `else`-branch type changes, the compiler may silently"] # [doc = " infer a different type to be returned by `transmute()`. That is because the compiler is"] # [doc = " free to change the inference of a type as long as that inference is technically correct,"] # [doc = " regardless of the programmer's unknown expectation."] # [doc = ""] # [doc = " Both type-parameters, the input- and the output-type, to any `transmute()` should"] # [doc = " be given explicitly: Setting the input-type explicitly avoids confusion about what the"] # [doc = " argument's type actually is. Setting the output-type explicitly avoids type-inference"] # [doc = " to infer a technically correct yet unexpected type."] # [doc = ""] # [doc = " ### Example"] # [doc = " ```"] # [doc = " # unsafe {"] # [doc = " let mut x: i32 = 0;"] # [doc = " // Avoid \"naked\" calls to `transmute()`!"] # [doc = " x = std::mem::transmute([1u16, 2u16]);"] # [doc = ""] # [doc = " // `first_answers` is intended to transmute a slice of bool to a slice of u8."] # [doc = " // But the programmer forgot to index the first element of the outer slice,"] # [doc = " // so we are actually transmuting from \"pointers to slices\" instead of"] # [doc = " // transmuting from \"a slice of bool\", causing a nonsensical result."] # [doc = " let the_answers: &[&[bool]] = &[&[true, false, true]];"] # [doc = " let first_answers: &[u8] = std::mem::transmute(the_answers);"] # [doc = " # }"] # [doc = " ```"] # [doc = " Use instead:"] # [doc = " ```"] # [doc = " # unsafe {"] # [doc = " let x = std::mem::transmute::<[u16; 2], i32>([1u16, 2u16]);"] # [doc = ""] # [doc = " // The explicit type parameters on `transmute()` makes the intention clear,"] # [doc = " // and cause a type-error if the actual types don't match our expectation."] # [doc = " let the_answers: &[&[bool]] = &[&[true, false, true]];"] # [doc = " let first_answers: &[u8] = std::mem::transmute::<&[bool], &[u8]>(the_answers[0]);"] # [doc = " # }"] # [doc = " ```"] # [clippy :: version = "1.79.0"] pub MISSING_TRANSMUTE_ANNOTATIONS , suspicious , "warns if a transmute call doesn't have all generics specified" }
};
}
