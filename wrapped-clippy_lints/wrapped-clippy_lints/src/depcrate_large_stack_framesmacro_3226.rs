// Generated macro for macro_3226 (macro)
macro_rules! Depcrate_large_stack_framesmacro_3226 {
() => {
// Module: crate::large_stack_frames
// Provides: {"macro_3226"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " Checks for functions that use a lot of stack space."] # [doc = ""] # [doc = " This often happens when constructing a large type, such as an array with a lot of elements,"] # [doc = " or constructing *many* smaller-but-still-large structs, or copying around a lot of large types."] # [doc = ""] # [doc = " This lint is a more general version of [`large_stack_arrays`](https://rust-lang.github.io/rust-clippy/master/#large_stack_arrays)"] # [doc = " that is intended to look at functions as a whole instead of only individual array expressions inside of a function."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = " The stack region of memory is very limited in size (usually *much* smaller than the heap) and attempting to"] # [doc = " use too much will result in a stack overflow and crash the program."] # [doc = " To avoid this, you should consider allocating large types on the heap instead (e.g. by boxing them)."] # [doc = ""] # [doc = " Keep in mind that the code path to construction of large types does not even need to be reachable;"] # [doc = " it purely needs to *exist* inside of the function to contribute to the stack size."] # [doc = " For example, this causes a stack overflow even though the branch is unreachable:"] # [doc = " ```rust,ignore"] # [doc = " fn main() {"] # [doc = "     if false {"] # [doc = "         let x = [0u8; 10000000]; // 10 MB stack array"] # [doc = "         black_box(&x);"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " ### Known issues"] # [doc = " False positives. The stack size that clippy sees is an estimated value and can be vastly different"] # [doc = " from the actual stack usage after optimizations passes have run (especially true in release mode)."] # [doc = " Modern compilers are very smart and are able to optimize away a lot of unnecessary stack allocations."] # [doc = " In debug mode however, it is usually more accurate."] # [doc = ""] # [doc = " This lint works by summing up the size of all variables that the user typed, variables that were"] # [doc = " implicitly introduced by the compiler for temporaries, function arguments and the return value,"] # [doc = " and comparing them against a (configurable, but high-by-default)."] # [doc = ""] # [doc = " ### Example"] # [doc = " This function creates four 500 KB arrays on the stack. Quite big but just small enough to not trigger `large_stack_arrays`."] # [doc = " However, looking at the function as a whole, it's clear that this uses a lot of stack space."] # [doc = " ```no_run"] # [doc = " struct QuiteLargeType([u8; 500_000]);"] # [doc = " fn foo() {"] # [doc = "     // ... some function that uses a lot of stack space ..."] # [doc = "     let _x1 = QuiteLargeType([0; 500_000]);"] # [doc = "     let _x2 = QuiteLargeType([0; 500_000]);"] # [doc = "     let _x3 = QuiteLargeType([0; 500_000]);"] # [doc = "     let _x4 = QuiteLargeType([0; 500_000]);"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Instead of doing this, allocate the arrays on the heap."] # [doc = " This currently requires going through a `Vec` first and then converting it to a `Box`:"] # [doc = " ```no_run"] # [doc = " struct NotSoLargeType(Box<[u8]>);"] # [doc = ""] # [doc = " fn foo() {"] # [doc = "     let _x1 = NotSoLargeType(vec![0; 500_000].into_boxed_slice());"] # [doc = " //                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^  Now heap allocated."] # [doc = " //                                                                The size of `NotSoLargeType` is 16 bytes."] # [doc = " //  ..."] # [doc = " }"] # [doc = " ```"] # [clippy :: version = "1.72.0"] pub LARGE_STACK_FRAMES , nursery , "checks for functions that allocate a lot of stack space" }
};
}
