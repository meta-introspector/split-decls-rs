// Generated macro for check (function)
macro_rules! Depcrate_methods_map_identitycheck {
() => {
// Module: crate::methods::map_identity
// Provides: {"check"}
// Dependencies: {}
pub (super) fn check (cx : & LateContext < '_ > , expr : & hir :: Expr < '_ > , caller : & hir :: Expr < '_ > , map_arg : & hir :: Expr < '_ > , name : Symbol , _map_span : Span ,) { let caller_ty = cx . typeck_results () . expr_ty (caller) ; if (cx . ty_based_def (expr) . opt_parent (cx) . is_diag_item (cx , sym :: Iterator) || caller_ty . is_diag_item (cx , sym :: Result) || caller_ty . is_diag_item (cx , sym :: Option)) && is_expr_untyped_identity_function (cx , map_arg) && let Some (call_span) = expr . span . trim_start (caller . span) { span_lint_and_then (cx , MAP_IDENTITY , call_span , MSG , | diag | { let main_sugg = (call_span , String :: new ()) ; let mut app = if is_copy (cx , caller_ty) { Applicability :: Unspecified } else { Applicability :: MachineApplicable } ; let needs_to_be_mutable = cx . typeck_results () . expr_ty_adjusted (expr) . is_mutable_ptr () ; if needs_to_be_mutable && ! is_mutable (cx , caller) { if let Some (hir_id) = path_to_local_with_projections (caller) && let Node :: Pat (pat) = cx . tcx . hir_node (hir_id) && let PatKind :: Binding (_ , _ , ident , _) = pat . kind { let suggs = vec ! [main_sugg , (ident . span . shrink_to_lo () , String :: from ("mut "))] ; let ident = snippet_with_applicability (cx . sess () , ident . span , "_" , & mut app) ; diag . multipart_suggestion (format ! ("remove the call to `{name}`, and make `{ident}` mutable") , suggs , app ,) ; } else { app = Applicability :: Unspecified ; let method_requiring_mut = if let Node :: Expr (expr) = cx . tcx . parent_hir_node (expr . hir_id) && let ExprKind :: MethodCall (method , ..) = expr . kind { Some (method . ident) } else { None } ; diag . span_suggestion (main_sugg . 0 , format ! ("remove the call to `{name}`") , main_sugg . 1 , app) ; let note = if let Some (method_requiring_mut) = method_requiring_mut { format ! ("this must be made mutable to use `{method_requiring_mut}`") } else { "this must be made mutable" . to_string () } ; diag . span_note (caller . span , note) ; } } else { diag . span_suggestion (main_sugg . 0 , format ! ("remove the call to `{name}`") , main_sugg . 1 , app) ; } }) ; } }
};
}
