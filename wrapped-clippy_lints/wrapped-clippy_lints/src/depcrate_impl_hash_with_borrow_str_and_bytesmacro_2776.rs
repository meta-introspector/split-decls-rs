// Generated macro for macro_2776 (macro)
macro_rules! Depcrate_impl_hash_with_borrow_str_and_bytesmacro_2776 {
() => {
// Module: crate::impl_hash_with_borrow_str_and_bytes
// Provides: {"macro_2776"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = ""] # [doc = " This lint is concerned with the semantics of `Borrow` and `Hash` for a"] # [doc = " type that implements all three of `Hash`, `Borrow<str>` and `Borrow<[u8]>`"] # [doc = " as it is impossible to satisfy the semantics of Borrow and `Hash` for"] # [doc = " both `Borrow<str>` and `Borrow<[u8]>`."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = ""] # [doc = " When providing implementations for `Borrow<T>`, one should consider whether the different"] # [doc = " implementations should act as facets or representations of the underlying type. Generic code"] # [doc = " typically uses `Borrow<T>` when it relies on the identical behavior of these additional trait"] # [doc = " implementations. These traits will likely appear as additional trait bounds."] # [doc = ""] # [doc = " In particular `Eq`, `Ord` and `Hash` must be equivalent for borrowed and owned values:"] # [doc = " `x.borrow() == y.borrow()` should give the same result as `x == y`."] # [doc = " It follows then that the following equivalence must hold:"] # [doc = " `hash(x) == hash((x as Borrow<[u8]>).borrow()) == hash((x as Borrow<str>).borrow())`"] # [doc = ""] # [doc = " Unfortunately it doesn't hold as `hash(\"abc\") != hash(\"abc\".as_bytes())`."] # [doc = " This happens because the `Hash` impl for str passes an additional `0xFF` byte to"] # [doc = " the hasher to avoid collisions. For example, given the tuples `(\"a\", \"bc\")`, and `(\"ab\", \"c\")`,"] # [doc = " the two tuples would have the same hash value if the `0xFF` byte was not added."] # [doc = ""] # [doc = " ### Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::borrow::Borrow;"] # [doc = " use std::hash::{Hash, Hasher};"] # [doc = ""] # [doc = " struct ExampleType {"] # [doc = "     data: String"] # [doc = " }"] # [doc = ""] # [doc = " impl Hash for ExampleType {"] # [doc = "     fn hash<H: Hasher>(&self, state: &mut H) {"] # [doc = "         self.data.hash(state);"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " impl Borrow<str> for ExampleType {"] # [doc = "     fn borrow(&self) -> &str {"] # [doc = "         &self.data"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " impl Borrow<[u8]> for ExampleType {"] # [doc = "     fn borrow(&self) -> &[u8] {"] # [doc = "         self.data.as_bytes()"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = " As a consequence, hashing a `&ExampleType` and hashing the result of the two"] # [doc = " borrows will result in different values."] # [doc = ""] # [clippy :: version = "1.76.0"] pub IMPL_HASH_BORROW_WITH_STR_AND_BYTES , correctness , "ensures that the semantics of `Borrow` for `Hash` are satisfied when `Borrow<str>` and `Borrow<[u8]>` are implemented" }
};
}
