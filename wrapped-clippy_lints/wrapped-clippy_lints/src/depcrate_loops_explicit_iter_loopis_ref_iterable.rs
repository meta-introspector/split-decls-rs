// Generated macro for is_ref_iterable (function)
macro_rules! Depcrate_loops_explicit_iter_loopis_ref_iterable {
() => {
// Module: crate::loops::explicit_iter_loop
// Provides: {"is_ref_iterable"}
// Dependencies: {}
# [doc = " Checks if an `iter` or `iter_mut` call returns `IntoIterator::IntoIter`. Returns how the"] # [doc = " argument needs to be adjusted."] # [expect (clippy :: too_many_lines)] fn is_ref_iterable < 'tcx > (cx : & LateContext < 'tcx > , self_arg : & Expr < '_ > , call_expr : & Expr < '_ > , enforce_iter_loop_reborrow : bool , msrv : Msrv ,) -> Option < (AdjustKind , Ty < 'tcx >) > { let typeck = cx . typeck_results () ; if let Some (trait_id) = cx . tcx . get_diagnostic_item (sym :: IntoIterator) && let Some (fn_id) = typeck . type_dependent_def_id (call_expr . hir_id) && let sig = cx . tcx . liberate_late_bound_regions (fn_id , cx . tcx . fn_sig (fn_id) . skip_binder ()) && let & [req_self_ty , req_res_ty] = & * * sig . inputs_and_output && let typing_env = ty :: TypingEnv :: non_body_analysis (cx . tcx , fn_id) && implements_trait_with_env (cx . tcx , typing_env , req_self_ty , trait_id , Some (fn_id) , & []) && let Some (into_iter_ty) = make_normalized_projection_with_regions (cx . tcx , typing_env , trait_id , sym :: IntoIter , [req_self_ty]) && let req_res_ty = normalize_with_regions (cx . tcx , typing_env , req_res_ty) && into_iter_ty == req_res_ty { let adjustments = typeck . expr_adjustments (self_arg) ; let self_ty = typeck . expr_ty (self_arg) ; let self_is_copy = is_copy (cx , self_ty) ; if self_ty . peel_refs () . is_lang_item (cx , rustc_hir :: LangItem :: OwnedBox) && ! msrv . meets (cx , msrvs :: BOX_INTO_ITER) { return None ; } if adjustments . is_empty () && self_is_copy { return Some ((AdjustKind :: None , self_ty)) ; } let res_ty = cx . tcx . erase_and_anonymize_regions (EarlyBinder :: bind (req_res_ty) . instantiate (cx . tcx , typeck . node_args (call_expr . hir_id)) ,) ; let mutbl = if let ty :: Ref (_ , _ , mutbl) = * req_self_ty . kind () { Some (mutbl) } else { None } ; if ! adjustments . is_empty () { if self_is_copy { if implements_trait (cx , self_ty , trait_id , & []) && let Some (ty) = make_normalized_projection (cx . tcx , cx . typing_env () , trait_id , sym :: IntoIter , [self_ty]) && ty == res_ty { return Some ((AdjustKind :: None , self_ty)) ; } } else if enforce_iter_loop_reborrow && let ty :: Ref (region , ty , Mutability :: Mut) = * self_ty . kind () && let Some (mutbl) = mutbl { let self_ty = if mutbl . is_mut () { self_ty } else { Ty :: new_ref (cx . tcx , region , ty , mutbl) } ; if implements_trait (cx , self_ty , trait_id , & []) && let Some (ty) = make_normalized_projection (cx . tcx , cx . typing_env () , trait_id , sym :: IntoIter , [self_ty]) && ty == res_ty { return Some ((AdjustKind :: reborrow (mutbl) , self_ty)) ; } } } if let Some (mutbl) = mutbl && ! self_ty . is_ref () { let self_ty = Ty :: new_ref (cx . tcx , cx . tcx . lifetimes . re_erased , self_ty , mutbl) ; if implements_trait (cx , self_ty , trait_id , & []) && let Some (ty) = make_normalized_projection (cx . tcx , cx . typing_env () , trait_id , sym :: IntoIter , [self_ty]) && ty == res_ty { return Some ((AdjustKind :: borrow (mutbl) , self_ty)) ; } } match adjustments { [] => Some ((AdjustKind :: None , self_ty)) , & [Adjustment { kind : Adjust :: Deref (_) , .. } , Adjustment { kind : Adjust :: Borrow (AutoBorrow :: Ref (mutbl)) , target , } , .. ,] => { if enforce_iter_loop_reborrow && target != self_ty && implements_trait (cx , target , trait_id , & []) && let Some (ty) = make_normalized_projection (cx . tcx , cx . typing_env () , trait_id , sym :: IntoIter , [target]) && ty == res_ty { Some ((AdjustKind :: auto_reborrow (mutbl) , target)) } else { None } } , & [Adjustment { kind : Adjust :: Deref (_) , target , } , .. ,] => { if is_copy (cx , target) && implements_trait (cx , target , trait_id , & []) && let Some (ty) = make_normalized_projection (cx . tcx , cx . typing_env () , trait_id , sym :: IntoIter , [target]) && ty == res_ty { Some ((AdjustKind :: Deref , target)) } else { None } } , & [Adjustment { kind : Adjust :: Borrow (AutoBorrow :: Ref (mutbl)) , target , } , .. ,] => { if self_ty . is_ref () && implements_trait (cx , target , trait_id , & []) && let Some (ty) = make_normalized_projection (cx . tcx , cx . typing_env () , trait_id , sym :: IntoIter , [target]) && ty == res_ty { Some ((AdjustKind :: auto_borrow (mutbl) , target)) } else { None } } , _ => None , } } else { None } }
};
}
