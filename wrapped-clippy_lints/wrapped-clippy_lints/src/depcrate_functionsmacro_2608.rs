// Generated macro for macro_2608 (macro)
macro_rules! Depcrate_functionsmacro_2608 {
() => {
// Module: crate::functions
// Provides: {"macro_2608"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " Checks for public functions that dereference raw pointer"] # [doc = " arguments but are not marked `unsafe`."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = " The function should almost definitely be marked `unsafe`, since for an"] # [doc = " arbitrary raw pointer, there is no way of telling for sure if it is valid."] # [doc = ""] # [doc = " In general, this lint should **never be disabled** unless it is definitely a"] # [doc = " false positive (please submit an issue if so) since it breaks Rust's"] # [doc = " soundness guarantees, directly exposing API users to potentially dangerous"] # [doc = " program behavior. This is also true for internal APIs, as it is easy to leak"] # [doc = " unsoundness."] # [doc = ""] # [doc = " ### Context"] # [doc = " In Rust, an `unsafe {...}` block is used to indicate that the code in that"] # [doc = " section has been verified in some way that the compiler can not. For a"] # [doc = " function that accepts a raw pointer then accesses the pointer's data, this is"] # [doc = " generally impossible as the incoming pointer could point anywhere, valid or"] # [doc = " not. So, the signature should be marked `unsafe fn`: this indicates that the"] # [doc = " function's caller must provide some verification that the arguments it sends"] # [doc = " are valid (and then call the function within an `unsafe` block)."] # [doc = ""] # [doc = " ### Known problems"] # [doc = " * It does not check functions recursively so if the pointer is passed to a"] # [doc = " private non-`unsafe` function which does the dereferencing, the lint won't"] # [doc = " trigger (false negative)."] # [doc = " * It only checks for arguments whose type are raw pointers, not raw pointers"] # [doc = " got from an argument in some other way (`fn foo(bar: &[*const u8])` or"] # [doc = " `some_argument.get_raw_ptr()`) (false negative)."] # [doc = ""] # [doc = " ### Example"] # [doc = " ```rust,ignore"] # [doc = " pub fn foo(x: *const u8) {"] # [doc = "     println!(\"{}\", unsafe { *x });"] # [doc = " }"] # [doc = ""] # [doc = " // this call \"looks\" safe but will segfault or worse!"] # [doc = " // foo(invalid_ptr);"] # [doc = " ```"] # [doc = ""] # [doc = " Use instead:"] # [doc = " ```rust,ignore"] # [doc = " pub unsafe fn foo(x: *const u8) {"] # [doc = "     println!(\"{}\", unsafe { *x });"] # [doc = " }"] # [doc = ""] # [doc = " // this would cause a compiler error for calling without `unsafe`"] # [doc = " // foo(invalid_ptr);"] # [doc = ""] # [doc = " // sound call if the caller knows the pointer is valid"] # [doc = " unsafe { foo(valid_ptr); }"] # [doc = " ```"] # [clippy :: version = "pre 1.29.0"] pub NOT_UNSAFE_PTR_ARG_DEREF , correctness , "public functions dereferencing raw pointer arguments but not marked `unsafe`" }
};
}
