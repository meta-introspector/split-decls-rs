// Generated macro for macro_7051 (macro)
macro_rules! Depcrate_methodsmacro_7051 {
() => {
// Module: crate::methods
// Provides: {"macro_7051"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " Looks for calls to `.type_id()` on a `Box<dyn _>`."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = " This almost certainly does not do what the user expects and can lead to subtle bugs."] # [doc = " Calling `.type_id()` on a `Box<dyn Trait>` returns a fixed `TypeId` of the `Box` itself,"] # [doc = " rather than returning the `TypeId` of the underlying type behind the trait object."] # [doc = ""] # [doc = " For `Box<dyn Any>` specifically (and trait objects that have `Any` as its supertrait),"] # [doc = " this lint will provide a suggestion, which is to dereference the receiver explicitly"] # [doc = " to go from `Box<dyn Any>` to `dyn Any`."] # [doc = " This makes sure that `.type_id()` resolves to a dynamic call on the trait object"] # [doc = " and not on the box."] # [doc = ""] # [doc = " If the fixed `TypeId` of the `Box` is the intended behavior, it's better to be explicit about it"] # [doc = " and write `TypeId::of::<Box<dyn Trait>>()`:"] # [doc = " this makes it clear that a fixed `TypeId` is returned and not the `TypeId` of the implementor."] # [doc = ""] # [doc = " ### Example"] # [doc = " ```rust,ignore"] # [doc = " use std::any::{Any, TypeId};"] # [doc = ""] # [doc = " let any_box: Box<dyn Any> = Box::new(42_i32);"] # [doc = " assert_eq!(any_box.type_id(), TypeId::of::<i32>()); // ⚠\u{fe0f} this fails!"] # [doc = " ```"] # [doc = " Use instead:"] # [doc = " ```no_run"] # [doc = " use std::any::{Any, TypeId};"] # [doc = ""] # [doc = " let any_box: Box<dyn Any> = Box::new(42_i32);"] # [doc = " assert_eq!((*any_box).type_id(), TypeId::of::<i32>());"] # [doc = " //          ^ dereference first, to call `type_id` on `dyn Any`"] # [doc = " ```"] # [clippy :: version = "1.73.0"] pub TYPE_ID_ON_BOX , suspicious , "calling `.type_id()` on a boxed trait object" }
};
}
