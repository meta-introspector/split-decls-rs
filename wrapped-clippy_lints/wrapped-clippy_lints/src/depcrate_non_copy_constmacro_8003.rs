// Generated macro for macro_8003 (macro)
macro_rules! Depcrate_non_copy_constmacro_8003 {
() => {
// Module: crate::non_copy_const
// Provides: {"macro_8003"}
// Dependencies: {}
declare_clippy_lint ! { # [doc = " ### What it does"] # [doc = " Checks for the declaration of named constant which contain interior mutability."] # [doc = ""] # [doc = " ### Why is this bad?"] # [doc = " Named constants are copied at every use site which means any change to their value"] # [doc = " will be lost after the newly created value is dropped. e.g."] # [doc = ""] # [doc = " ```rust"] # [doc = " use core::sync::atomic::{AtomicUsize, Ordering};"] # [doc = " const ATOMIC: AtomicUsize = AtomicUsize::new(0);"] # [doc = " fn add_one() -> usize {"] # [doc = "     // This will always return `0` since `ATOMIC` is copied before it's used."] # [doc = "     ATOMIC.fetch_add(1, Ordering::AcqRel)"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " If shared modification of the value is desired, a `static` item is needed instead."] # [doc = " If that is not desired, a `const fn` constructor should be used to make it obvious"] # [doc = " at the use site that a new value is created."] # [doc = ""] # [doc = " ### Known problems"] # [doc = " Prior to `const fn` stabilization this was the only way to provide a value which"] # [doc = " could initialize a `static` item (e.g. the `std::sync::ONCE_INIT` constant). In"] # [doc = " this case the use of `const` is required and this lint should be suppressed."] # [doc = ""] # [doc = " There also exists types which contain private fields with interior mutability, but"] # [doc = " no way to both create a value as a constant and modify any mutable field using the"] # [doc = " type's public interface (e.g. `bytes::Bytes`). As there is no reasonable way to"] # [doc = " scan a crate's interface to see if this is the case, all such types will be linted."] # [doc = " If this happens use the `ignore-interior-mutability` configuration option to allow"] # [doc = " the type."] # [doc = ""] # [doc = " ### Example"] # [doc = " ```no_run"] # [doc = " use std::sync::atomic::{AtomicUsize, Ordering::SeqCst};"] # [doc = ""] # [doc = " const CONST_ATOM: AtomicUsize = AtomicUsize::new(12);"] # [doc = " CONST_ATOM.store(6, SeqCst); // the content of the atomic is unchanged"] # [doc = " assert_eq!(CONST_ATOM.load(SeqCst), 12); // because the CONST_ATOM in these lines are distinct"] # [doc = " ```"] # [doc = ""] # [doc = " Use instead:"] # [doc = " ```no_run"] # [doc = " # use std::sync::atomic::{AtomicUsize, Ordering::SeqCst};"] # [doc = " static STATIC_ATOM: AtomicUsize = AtomicUsize::new(15);"] # [doc = " STATIC_ATOM.store(9, SeqCst);"] # [doc = " assert_eq!(STATIC_ATOM.load(SeqCst), 9); // use a `static` item to refer to the same instance"] # [doc = " ```"] # [clippy :: version = "pre 1.29.0"] pub DECLARE_INTERIOR_MUTABLE_CONST , suspicious , "declaring `const` with interior mutability" }
};
}
