// Generated macro for check (function)
macro_rules! Depcrate_types_vec_boxcheck {
() => {
// Module: crate::types::vec_box
// Provides: {"check"}
// Dependencies: {}
pub (super) fn check < 'tcx > (cx : & LateContext < 'tcx > , hir_ty : & hir :: Ty < '_ > , qpath : & QPath < 'tcx > , def_id : DefId , box_size_threshold : u64 ,) -> bool { if cx . tcx . is_diagnostic_item (sym :: Vec , def_id) && let Some (last) = last_path_segment (qpath) . args && let Some (GenericArg :: Type (ty)) = last . args . first () && let vec_alloc_ty = last . args . get (1) && let TyKind :: Path (ref ty_qpath) = ty . kind && let res = cx . qpath_res (ty_qpath , ty . hir_id) && let Some (def_id) = res . opt_def_id () && Some (def_id) == cx . tcx . lang_items () . owned_box () && let Some (last) = last_path_segment (ty_qpath) . args && let Some (GenericArg :: Type (boxed_ty)) = last . args . first () && let boxed_alloc_ty = last . args . get (1) && let ty_ty = lower_ty (cx . tcx , boxed_ty . as_unambig_ty ()) && ! ty_ty . has_escaping_bound_vars () && ty_ty . is_sized (cx . tcx , cx . typing_env ()) && let Ok (ty_ty_size) = cx . layout_of (ty_ty) . map (| l | l . size . bytes ()) && ty_ty_size < box_size_threshold && match (vec_alloc_ty , boxed_alloc_ty) { (None , None) => true , (None , Some (GenericArg :: Type (inner))) | (Some (GenericArg :: Type (inner)) , None) => { if let TyKind :: Path (path) = inner . kind && let Some (did) = cx . qpath_res (& path , inner . hir_id) . opt_def_id () { cx . tcx . lang_items () . get (LangItem :: GlobalAlloc) == Some (did) } else { false } } , (Some (GenericArg :: Type (l)) , Some (GenericArg :: Type (r))) => lower_ty (cx . tcx , l . as_unambig_ty ()) == lower_ty (cx . tcx , r . as_unambig_ty ()) , _ => false } { span_lint_and_sugg (cx , VEC_BOX , hir_ty . span , "`Vec<T>` is already on the heap, the boxing is unnecessary" , "try" , format ! ("Vec<{}>" , snippet (cx , boxed_ty . span , "..")) , Applicability :: Unspecified ,) ; true } else { false } }
};
}
