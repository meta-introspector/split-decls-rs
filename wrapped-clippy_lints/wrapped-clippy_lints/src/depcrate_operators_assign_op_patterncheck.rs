// Generated macro for check (function)
macro_rules! Depcrate_operators_assign_op_patterncheck {
() => {
// Module: crate::operators::assign_op_pattern
// Provides: {"check"}
// Dependencies: {}
pub (super) fn check < 'tcx > (cx : & LateContext < 'tcx > , expr : & 'tcx hir :: Expr < '_ > , assignee : & 'tcx hir :: Expr < '_ > , e : & 'tcx hir :: Expr < '_ > , msrv : Msrv ,) { if let hir :: ExprKind :: Binary (op , l , r) = & e . kind { let lint = | assignee : & hir :: Expr < '_ > , rhs : & hir :: Expr < '_ > | { let ty = cx . typeck_results () . expr_ty (assignee) ; let rty = cx . typeck_results () . expr_ty (rhs) ; if let Some ((_ , lang_item)) = binop_traits (op . node) && let Some (trait_id) = cx . tcx . lang_items () . get (lang_item) && let parent_fn = cx . tcx . hir_get_parent_item (e . hir_id) && trait_ref_of_method (cx , parent_fn) . is_none_or (| t | t . path . res . def_id () != trait_id) && implements_trait (cx , ty , trait_id , & [rty . into ()]) { if ! (ty . is_primitive () && rty . is_primitive ()) { let mut_borrows = mut_borrows_in_expr (cx , assignee) ; let imm_borrows = imm_borrows_in_expr (cx , rhs) ; if mut_borrows . iter () . any (| id | imm_borrows . contains (id)) { return ; } } if is_in_const_context (cx) { if cx . tcx . associated_item_def_ids (trait_id) . first () . is_none_or (| binop_id | ! is_stable_const_fn (cx , * binop_id , msrv)) { return ; } let impls = cx . tcx . non_blanket_impls_for_ty (trait_id , rty) . collect :: < Vec < _ > > () ; if impls . is_empty () || impls . into_iter () . any (| impl_id | { cx . tcx . associated_item_def_ids (impl_id) . first () . is_none_or (| fn_id | ! is_stable_const_fn (cx , * fn_id , msrv)) }) { return ; } } span_lint_and_then (cx , ASSIGN_OP_PATTERN , expr . span , "manual implementation of an assign operation" , | diag | { if let Some (snip_a) = assignee . span . get_source_text (cx) && let Some (snip_r) = rhs . span . get_source_text (cx) { diag . span_suggestion (expr . span , "replace it with" , format ! ("{snip_a} {}= {snip_r}" , op . node . as_str ()) , Applicability :: MachineApplicable ,) ; } } ,) ; } } ; let mut found = false ; let found_multiple = for_each_expr_without_closures (e , | e | { if eq_expr_value (cx , assignee , e) { if found { return ControlFlow :: Break (()) ; } found = true ; } ControlFlow :: Continue (()) }) . is_some () ; if found && ! found_multiple { if eq_expr_value (cx , assignee , l) { lint (assignee , r) ; } if eq_expr_value (cx , assignee , r) && cx . typeck_results () . expr_ty (assignee) . is_primitive_ty () { match op . node { hir :: BinOpKind :: Add | hir :: BinOpKind :: Mul | hir :: BinOpKind :: And | hir :: BinOpKind :: Or | hir :: BinOpKind :: BitXor | hir :: BinOpKind :: BitAnd | hir :: BinOpKind :: BitOr => { lint (assignee , l) ; } , _ => { } , } } } } }
};
}
