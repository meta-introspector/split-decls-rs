// Generated macro for impl_21 (impl)
macro_rules! Depcrateimpl_21 {
() => {
// Module: crate
// Provides: {"impl_21"}
// Dependencies: {}
impl < 'a , T , Data > Heap < 'a , Data , T > where Data : ? Sized + AsMut < [T] > , { # [doc = " Create a new `Heap`."] # [doc = ""] # [doc = " # Panics"] # [doc = " If the number of elements is too large (see `MAXHEAP`)."] # [doc = ""] # [doc = " The `heap_recursive` function has no length limit, but"] # [doc = " note that for *n* elements there are *n!* (*n* factorial) permutations,"] # [doc = " which gets impractical for surprisingingly small values of *n* anyway."] pub fn new (data : & 'a mut Data) -> Self { assert ! (data . as_mut () . len () <= MAXHEAP) ; Heap { data , c : [0 ; MAXHEAP - 1] , n : ! 0 , _element : PhantomData , } } # [doc = " Return a reference to the inner data"] # [must_use] pub fn get (& self) -> & Data { self . data } # [doc = " Return a mutable reference to the inner data"] pub fn get_mut (& mut self) -> & mut Data { self . data } # [doc = " Reset the permutations walker, without changing the data. It allows"] # [doc = " generating permutations again with the current state as starting"] # [doc = " point."] pub fn reset (& mut self) { self . n = ! 0 ; for c in & mut self . c [..] { * c = 0 ; } } # [doc = " Step the internal data into the next permutation and return"] # [doc = " a reference to it. Return `None` when all permutations"] # [doc = " have been visited."] # [doc = ""] # [doc = " Note that for *n* elements there are *n!* (*n* factorial) permutations."] pub fn next_permutation (& mut self) -> Option < & mut Data > { if self . n == ! 0 { self . n = 0 ; Some (self . data) } else { while 1 + (self . n as usize) < self . data . as_mut () . len () { # [allow (clippy :: cast_possible_truncation)] let n = self . n as u8 ; let nu = self . n as usize ; let c = & mut self . c ; if c [nu] <= n { let j = if nu % 2 == 0 { c [nu] as usize } else { 0 } ; self . data . as_mut () . swap (j , nu + 1) ; c [nu] += 1 ; self . n = 0 ; return Some (self . data) ; } c [nu] = 0 ; self . n += 1 ; } None } } }
};
}
