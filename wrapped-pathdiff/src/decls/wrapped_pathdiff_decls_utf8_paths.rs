// Generated by add_generated_rust_header! macro at submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs:72 from submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs
// Do not edit this file by hand. Your changes will be overwritten.

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
#[cfg(feature = "camino")]
mod utf8_paths {
    use camino::{Utf8Component, Utf8Path, Utf8PathBuf};
    #[doc = " Construct a relative UTF-8 path from a provided base directory path to the provided path."]
    #[doc = ""]
    #[doc = " Requires the `camino` feature."]
    #[doc = ""]
    #[doc = " ```rust"]
    #[doc = " # extern crate camino;"]
    #[doc = " use camino::*;"]
    #[doc = " use pathdiff::diff_utf8_paths;"]
    #[doc = ""]
    #[doc = " assert_eq!(diff_utf8_paths(\"/foo/bar\",      \"/foo/bar/baz\"),  Some(\"../\".into()));"]
    #[doc = " assert_eq!(diff_utf8_paths(\"/foo/bar/baz\",  \"/foo/bar\"),      Some(\"baz\".into()));"]
    #[doc = " assert_eq!(diff_utf8_paths(\"/foo/bar/quux\", \"/foo/bar/baz\"),  Some(\"../quux\".into()));"]
    #[doc = " assert_eq!(diff_utf8_paths(\"/foo/bar/baz\",  \"/foo/bar/quux\"), Some(\"../baz\".into()));"]
    #[doc = " assert_eq!(diff_utf8_paths(\"/foo/bar\",      \"/foo/bar/quux\"), Some(\"../\".into()));"]
    #[doc = ""]
    #[doc = " assert_eq!(diff_utf8_paths(\"/foo/bar\",      \"baz\"),           Some(\"/foo/bar\".into()));"]
    #[doc = " assert_eq!(diff_utf8_paths(\"/foo/bar\",      \"/baz\"),          Some(\"../foo/bar\".into()));"]
    #[doc = " assert_eq!(diff_utf8_paths(\"foo\",           \"bar\"),           Some(\"../foo\".into()));"]
    #[doc = ""]
    #[doc = " assert_eq!("]
    #[doc = "     diff_utf8_paths(&\"/foo/bar/baz\", \"/foo/bar\".to_string()),"]
    #[doc = "     Some(\"baz\".into())"]
    #[doc = " );"]
    #[doc = " assert_eq!("]
    #[doc = "     diff_utf8_paths(Utf8Path::new(\"/foo/bar/baz\"), Utf8Path::new(\"/foo/bar\").to_path_buf()),"]
    #[doc = "     Some(\"baz\".into())"]
    #[doc = " );"]
    #[doc = " ```"]
    #[cfg_attr(docsrs, doc(cfg(feature = "camino")))]
    pub fn diff_utf8_paths<P, B>(path: P, base: B) -> Option<Utf8PathBuf>
    where
        P: AsRef<Utf8Path>,
        B: AsRef<Utf8Path>,
    {
        let path = path.as_ref();
        let base = base.as_ref();
        if path.is_absolute() != base.is_absolute() {
            if path.is_absolute() {
                Some(Utf8PathBuf::from(path))
            } else {
                None
            }
        } else {
            let mut ita = path.components();
            let mut itb = base.components();
            let mut comps: Vec<Utf8Component> = vec![];
            if let Some(Utf8Component::CurDir) = ita.clone().next() {
                ita.next();
            }
            if let Some(Utf8Component::CurDir) = itb.clone().next() {
                itb.next();
            }
            loop {
                match (ita.next(), itb.next()) {
                    (None, None) => break,
                    (Some(a), None) => {
                        comps.push(a);
                        comps.extend(ita.by_ref());
                        break;
                    }
                    (None, _) => comps.push(Utf8Component::ParentDir),
                    (Some(a), Some(b)) if comps.is_empty() && a == b => (),
                    (Some(a), Some(b)) if b == Utf8Component::CurDir => comps.push(a),
                    (Some(_), Some(b)) if b == Utf8Component::ParentDir => return None,
                    (Some(a), Some(_)) => {
                        comps.push(Utf8Component::ParentDir);
                        for _ in itb {
                            comps.push(Utf8Component::ParentDir);
                        }
                        comps.push(a);
                        comps.extend(ita.by_ref());
                        break;
                    }
                }
            }
            Some(comps.iter().map(|c| c.as_str()).collect())
        }
    }
}
