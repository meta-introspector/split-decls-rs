macro_rules! deps {
    () => {
        Result!();
        Scalar!();
        Field!();
        TypeRef!();
        Union!();
        SchemaInner!();
        Upload!();
        Object!();
        SchemaError!();
        Interface!();
        InputObject!();
        Subscription!();
    };
}

macro_rules! impl_410 {
    () => {
        deps!();
        impl SchemaInner { pub (crate) fn check (& self) -> Result < () , SchemaError > { self . check_types_exists () ? ; self . check_root_types () ? ; self . check_objects () ? ; self . check_input_objects () ? ; self . check_interfaces () ? ; self . check_unions () ? ; Ok (()) } fn check_root_types (& self) -> Result < () , SchemaError > { if let Some (ty) = self . types . get (& self . env . registry . query_type) { if ! matches ! (ty , Type :: Object (_)) { return Err ("The query root must be an object" . into ()) ; } } if let Some (mutation_type) = & self . env . registry . mutation_type { if let Some (ty) = self . types . get (mutation_type) { if ! matches ! (ty , Type :: Object (_)) { return Err ("The mutation root must be an object" . into ()) ; } } } if let Some (subscription_type) = & self . env . registry . subscription_type { if let Some (ty) = self . types . get (subscription_type) { if ! matches ! (ty , Type :: Subscription (_)) { return Err ("The subscription root must be a subscription object" . into ()) ; } } } Ok (()) } fn check_types_exists (& self) -> Result < () , SchemaError > { fn check < I : IntoIterator < Item = T > , T : AsRef < str > > (types : & IndexMap < String , Type > , type_names : I ,) -> Result < () , SchemaError > { for name in type_names { if ! types . contains_key (name . as_ref ()) { return Err (format ! ("Type \"{0}\" not found" , name . as_ref ()) . into ()) ; } } Ok (()) } check (& self . types , std :: iter :: once (self . env . registry . query_type . as_str ()) . chain (self . env . registry . mutation_type . as_deref ()) ,) ? ; for ty in self . types . values () { match ty { Type :: Object (obj) => check (& self . types , obj . fields . values () . map (| field | { std :: iter :: once (field . ty . type_name ()) . chain (field . arguments . values () . map (| arg | arg . ty . type_name ())) }) . flatten () . chain (obj . implements . iter () . map (AsRef :: as_ref)) ,) ? , Type :: InputObject (obj) => { check (& self . types , obj . fields . values () . map (| field | field . ty . type_name ()) ,) ? ; } Type :: Interface (interface) => check (& self . types , interface . fields . values () . map (| field | { std :: iter :: once (field . ty . type_name ()) . chain (field . arguments . values () . map (| arg | arg . ty . type_name ())) }) . flatten () ,) ? , Type :: Union (union) => check (& self . types , & union . possible_types) ? , Type :: Subscription (subscription) => check (& self . types , subscription . fields . values () . map (| field | { std :: iter :: once (field . ty . type_name ()) . chain (field . arguments . values () . map (| arg | arg . ty . type_name ())) }) . flatten () ,) ? , Type :: Scalar (_) | Type :: Enum (_) | Type :: Upload => { } } } Ok (()) } fn check_objects (& self) -> Result < () , SchemaError > { let has_entities = self . types . iter () . filter_map (| (_ , ty) | ty . as_object ()) . any (Object :: is_entity) ; for ty in self . types . values () { if let Type :: Object (obj) = ty { if obj . fields . is_empty () && ! (obj . type_name () == self . env . registry . query_type && has_entities) { return Err (format ! ("Object \"{}\" must define one or more fields" , obj . name) . into () ,) ; } for field in obj . fields . values () { if field . name . starts_with ("__") { return Err (format ! ("Field \"{}.{}\" must not have a name which begins with the characters \"__\" (two underscores)" , obj . name , field . name) . into ()) ; } if let Some (ty) = self . types . get (field . ty . type_name ()) { if ! ty . is_output_type () { return Err (format ! ("Field \"{}.{}\" must return a output type" , obj . name , field . name) . into ()) ; } } for arg in field . arguments . values () { if arg . name . starts_with ("__") { return Err (format ! ("Argument \"{}.{}.{}\" must not have a name which begins with the characters \"__\" (two underscores)" , obj . name , field . name , arg . name) . into ()) ; } if let Some (ty) = self . types . get (arg . ty . type_name ()) { if ! ty . is_input_type () { return Err (format ! ("Argument \"{}.{}.{}\" must accept a input type" , obj . name , field . name , arg . name) . into ()) ; } } } } for interface_name in & obj . implements { if let Some (ty) = self . types . get (interface_name) { let interface = ty . as_interface () . ok_or_else (| | { format ! ("Type \"{}\" is not interface" , interface_name) }) ? ; check_is_valid_implementation (obj , interface) ? ; } } } } Ok (()) } fn check_input_objects (& self) -> Result < () , SchemaError > { for ty in self . types . values () { if let Type :: InputObject (obj) = ty { for field in obj . fields . values () { if field . name . starts_with ("__") { return Err (format ! ("Field \"{}.{}\" must not have a name which begins with the characters \"__\" (two underscores)" , obj . name , field . name) . into ()) ; } if let Some (ty) = self . types . get (field . ty . type_name ()) { if ! ty . is_input_type () { return Err (format ! ("Field \"{}.{}\" must accept a input type" , obj . name , field . name) . into ()) ; } } if obj . oneof { if ! field . ty . is_nullable () { return Err (format ! ("Field \"{}.{}\" must be nullable" , obj . name , field . name) . into ()) ; } if field . default_value . is_some () { return Err (format ! ("Field \"{}.{}\" must not have a default value" , obj . name , field . name) . into ()) ; } } } self . check_input_object_reference (& obj . name , & obj , & mut HashSet :: new ()) ? ; } } Ok (()) } fn check_input_object_reference < 'a > (& 'a self , current : & str , obj : & 'a InputObject , ref_chain : & mut HashSet < & 'a str > ,) -> Result < () , SchemaError > { fn typeref_nonnullable_name (ty : & TypeRef) -> Option < & str > { match ty { TypeRef :: NonNull (inner) => match inner . as_ref () { TypeRef :: Named (name) => Some (name) , _ => None , } , _ => None , } } for field in obj . fields . values () { if let Some (this_name) = typeref_nonnullable_name (& field . ty) { if this_name == current { return Err (format ! ("\"{}\" references itself either directly or through referenced Input Objects, at least one of the fields in the chain of references must be either a nullable or a List type." , current) . into ()) ; } else if let Some (obj) = self . types . get (field . ty . type_name ()) . and_then (Type :: as_input_object) { if ref_chain . insert (this_name) { self . check_input_object_reference (current , obj , ref_chain) ? ; ref_chain . remove (this_name) ; } } } } Ok (()) } fn check_interfaces (& self) -> Result < () , SchemaError > { for ty in self . types . values () { if let Type :: Interface (interface) = ty { for field in interface . fields . values () { if field . name . starts_with ("__") { return Err (format ! ("Field \"{}.{}\" must not have a name which begins with the characters \"__\" (two underscores)" , interface . name , field . name) . into ()) ; } if let Some (ty) = self . types . get (field . ty . type_name ()) { if ! ty . is_output_type () { return Err (format ! ("Field \"{}.{}\" must return a output type" , interface . name , field . name) . into ()) ; } } for arg in field . arguments . values () { if arg . name . starts_with ("__") { return Err (format ! ("Argument \"{}.{}.{}\" must not have a name which begins with the characters \"__\" (two underscores)" , interface . name , field . name , arg . name) . into ()) ; } if let Some (ty) = self . types . get (arg . ty . type_name ()) { if ! ty . is_input_type () { return Err (format ! ("Argument \"{}.{}.{}\" must accept a input type" , interface . name , field . name , arg . name) . into ()) ; } } } if interface . implements . contains (& interface . name) { return Err (format ! ("Interface \"{}\" may not implement itself" , interface . name) . into ()) ; } for interface_name in & interface . implements { if let Some (ty) = self . types . get (interface_name) { let implemenented_type = ty . as_interface () . ok_or_else (| | { format ! ("Type \"{}\" is not interface" , interface_name) }) ? ; check_is_valid_implementation (interface , implemenented_type) ? ; } } } } } Ok (()) } fn check_unions (& self) -> Result < () , SchemaError > { for ty in self . types . values () { if let Type :: Union (union) = ty { for type_name in & union . possible_types { if let Some (ty) = self . types . get (type_name) { if ty . as_object () . is_none () { return Err (format ! ("Member \"{}\" of union \"{}\" is not an object" , type_name , union . name) . into ()) ; } } } } } Ok (()) } }
    };
}

impl_410!()