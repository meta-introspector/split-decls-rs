macro_rules! deps {
    () => {
        BitReaderReversed!();
        Read!();
    };
}

macro_rules! impl_11 {
    () => {
        deps!();
        impl < 's > BitReaderReversed < 's > { # [doc = " How many bits are left to read by the reader."] pub fn bits_remaining (& self) -> isize { self . index as isize * 8 + (64 - self . bits_consumed as isize) - self . extra_bits as isize } pub fn new (source : & 's [u8]) -> BitReaderReversed < 's > { BitReaderReversed { index : source . len () , bits_consumed : 64 , source , bit_container : 0 , extra_bits : 0 , } } # [doc = " We refill the container in full bytes, shifting the still unread portion to the left, and filling the lower bits with new data"] # [cold] fn refill (& mut self) { let bytes_consumed = self . bits_consumed as usize / 8 ; if bytes_consumed == 0 { return ; } if self . index >= bytes_consumed { self . index -= bytes_consumed ; self . bits_consumed &= 7 ; self . bit_container = u64 :: from_le_bytes ((& self . source [self . index ..] [.. 8]) . try_into () . unwrap ()) ; } else if self . index > 0 { if self . source . len () >= 8 { self . bit_container = u64 :: from_le_bytes ((& self . source [.. 8]) . try_into () . unwrap ()) ; } else { let mut value = [0 ; 8] ; value [.. self . source . len ()] . copy_from_slice (self . source) ; self . bit_container = u64 :: from_le_bytes (value) ; } self . bits_consumed -= 8 * self . index as u8 ; self . index = 0 ; self . bit_container <<= self . bits_consumed ; self . extra_bits += self . bits_consumed as usize ; self . bits_consumed = 0 ; } else if self . bits_consumed < 64 { self . bit_container <<= self . bits_consumed ; self . extra_bits += self . bits_consumed as usize ; self . bits_consumed = 0 ; } else { self . extra_bits += self . bits_consumed as usize ; self . bits_consumed = 0 ; self . bit_container = 0 ; } debug_assert ! (self . bits_consumed < 8) ; } # [doc = " Read `n` number of bits from the source. Will read at most 56 bits."] # [doc = " If there are no more bits to be read from the source zero bits will be returned instead."] # [inline (always)] pub fn get_bits (& mut self , n : u8) -> u64 { if self . bits_consumed + n > 64 { self . refill () ; } let value = self . peek_bits (n) ; self . consume (n) ; value } # [doc = " Get the next `n` bits from the source without consuming them."] # [doc = " Caller is responsible for making sure that `n` many bits have been refilled."] # [inline (always)] pub fn peek_bits (& mut self , n : u8) -> u64 { if n == 0 { return 0 ; } let mask = (1u64 << n) - 1u64 ; let shift_by = 64 - self . bits_consumed - n ; (self . bit_container >> shift_by) & mask } # [doc = " Get the next `n1` `n2` and `n3` bits from the source without consuming them."] # [doc = " Caller is responsible for making sure that `sum` many bits have been refilled."] # [inline (always)] pub fn peek_bits_triple (& mut self , sum : u8 , n1 : u8 , n2 : u8 , n3 : u8) -> (u64 , u64 , u64) { if sum == 0 { return (0 , 0 , 0) ; } let all_three = self . bit_container >> (64 - self . bits_consumed - sum) ; let mask1 = (1u64 << n1) - 1u64 ; let shift_by1 = n3 + n2 ; let val1 = (all_three >> shift_by1) & mask1 ; let mask2 = (1u64 << n2) - 1u64 ; let shift_by2 = n3 ; let val2 = (all_three >> shift_by2) & mask2 ; let mask3 = (1u64 << n3) - 1u64 ; let val3 = all_three & mask3 ; (val1 , val2 , val3) } # [doc = " Consume `n` bits from the source."] # [inline (always)] pub fn consume (& mut self , n : u8) { self . bits_consumed += n ; debug_assert ! (self . bits_consumed <= 64) ; } # [doc = " Same as calling get_bits three times but slightly more performant"] # [inline (always)] pub fn get_bits_triple (& mut self , n1 : u8 , n2 : u8 , n3 : u8) -> (u64 , u64 , u64) { let sum = n1 + n2 + n3 ; if sum <= 56 { self . refill () ; let triple = self . peek_bits_triple (sum , n1 , n2 , n3) ; self . consume (sum) ; return triple ; } (self . get_bits (n1) , self . get_bits (n2) , self . get_bits (n3)) } }
    };
}

impl_11!()