macro_rules! huff0_decoder {
    () => {
        # [doc = " Huffman coding is a method of encoding where symbols are assigned a code,"] # [doc = " and more commonly used symbols get shorter codes, and less commonly"] # [doc = " used symbols get longer codes. Codes are prefix free, meaning no two codes"] # [doc = " will start with the same sequence of bits."] mod huff0_decoder ;
    };
}

huff0_decoder!();