macro_rules! ACC_LOG_OFFSET {
    () => {
        # [doc = " This value is added to the first 4 bits of the stream to determine the"] # [doc = " `Accuracy_Log`"] const ACC_LOG_OFFSET : u8 = 5 ;
    };
}

ACC_LOG_OFFSET!();