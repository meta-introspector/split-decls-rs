// Generated macro for create_raw_dict_from_dir (function)
macro_rules! Depcrate_dictionarycreate_raw_dict_from_dir {
() => {
// Module: crate::dictionary
// Provides: {"create_raw_dict_from_dir"}
// Dependencies: {}
# [doc = " Creates a \"raw content\" dictionary, training off of every file in this directory and all"] # [doc = " sub-directories."] # [doc = ""] # [doc = " The resulting dictionary will be approxamitely `dict_size` or less, and written to `output`."] # [doc = ""] # [doc = " # Errors"] # [doc = " This function returns `Ok(())` if the dictionary was created successfully, and an"] # [doc = " `Err(io::Error)` if an error was encountered reading the input directory."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```no_run"] # [doc = " use std::fs::File;"] # [doc = " // Create a roughly 1mb dictionary, training off of file in `sample_files`"] # [doc = " let input_folder = \"sample_files/\";"] # [doc = " let mut output = File::create(\"output.dict\").unwrap();"] # [doc = " ruzstd::dictionary::create_raw_dict_from_dir(input_folder, &mut output, 1_000_000);"] # [doc = " ```"] pub fn create_raw_dict_from_dir < P : AsRef < Path > , W : io :: Write > (path : P , output : & mut W , dict_size : usize ,) -> Result < () , io :: Error > { let mut file_paths : Vec < PathBuf > = Vec :: new () ; let dir : fs :: ReadDir = fs :: read_dir (path) ? ; fn recurse_read (dir : fs :: ReadDir , file_paths : & mut Vec < PathBuf >) -> Result < () , io :: Error > { for entry in dir { let entry = entry ? ; if entry . file_type () ? . is_dir () { recurse_read (fs :: read_dir (entry . path ()) ? , file_paths) ? ; } else { file_paths . push (entry . path ()) ; } } Ok (()) } recurse_read (dir , & mut file_paths) ? ; let mut total_file_len : u64 = 0 ; let mut file_handles : Vec < fs :: File > = Vec :: new () ; for path in file_paths { let handle = File :: open (path) ? ; total_file_len += handle . metadata () ? . len () ; file_handles . push (handle) ; } let empty_reader : Box < dyn Read > = Box :: new (io :: empty ()) ; let chained_files = file_handles . iter () . fold (empty_reader , | acc , reader | Box :: new (acc . chain (reader))) ; create_raw_dict_from_source (chained_files , total_file_len as usize , output , dict_size) ; Ok (()) }
};
}
