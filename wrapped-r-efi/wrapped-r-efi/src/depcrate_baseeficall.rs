// Generated macro for eficall (macro)
macro_rules! Depcrate_baseeficall {
() => {
// Module: crate::base
// Provides: {"eficall"}
// Dependencies: {}
# [doc = " Annotate function with UEFI calling convention"] # [doc = ""] # [doc = " Since rust-1.68 you can use `extern \"efiapi\"` as calling-convention to achieve the same"] # [doc = " behavior as this macro. This macro is kept for backwards-compatibility only, but will nowadays"] # [doc = " map to `extern \"efiapi\"`."] # [doc = ""] # [doc = " This macro takes a function-declaration as argument and produces the same function-declaration"] # [doc = " but annotated with the correct calling convention. Since the default `extern \"C\"` annotation"] # [doc = " depends on your compiler defaults, we cannot use it. Instead, this macro selects the default"] # [doc = " for your target platform."] # [doc = ""] # [doc = " Ideally, the macro would expand to `extern \"<abi>\"` so you would be able to write:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " // THIS DOES NOT WORK!"] # [doc = " pub fn eficall!{} foobar() {"] # [doc = "     // ..."] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " However, macros are evaluated too late for this to work. Instead, the entire construct must be"] # [doc = " wrapped in a macro, which then expands to the same construct but with `extern \"<abi>\"`"] # [doc = " inserted at the correct place:"] # [doc = ""] # [doc = " ```"] # [doc = " use r_efi::{eficall, eficall_abi};"] # [doc = ""] # [doc = " eficall!{pub fn foobar() {"] # [doc = "     // ..."] # [doc = " }}"] # [doc = ""] # [doc = " type FooBar = eficall!{fn(u8) -> (u8)};"] # [doc = " ```"] # [doc = ""] # [doc = " The `eficall!{}` macro takes either a function-type or function-definition as argument. It"] # [doc = " inserts `extern \"<abi>\"` after the function qualifiers, but before the `fn` keyword."] # [doc = ""] # [doc = " # Internals"] # [doc = ""] # [doc = " The `eficall!{}` macro tries to parse the function header so it can insert `extern \"<abi>\"` at"] # [doc = " the right place. If, for whatever reason, this does not work with a particular syntax, you can"] # [doc = " use the internal `eficall_abi!{}` macro. This macro takes two token-streams as input and"] # [doc = " evaluates to the concatenation of both token-streams, but separated by the selected ABI."] # [doc = ""] # [doc = " For instance, the following 3 type definitions are equivalent, assuming the selected ABI"] # [doc = " is \"C\":"] # [doc = ""] # [doc = " ```"] # [doc = " use r_efi::{eficall, eficall_abi};"] # [doc = ""] # [doc = " type FooBar1 = unsafe extern \"C\" fn(u8) -> (u8);"] # [doc = " type FooBar2 = eficall!{unsafe fn(u8) -> (u8)};"] # [doc = " type FooBar3 = eficall_abi!{(unsafe), (fn(u8) -> (u8))};"] # [doc = " ```"] # [doc = ""] # [doc = " # Calling Conventions"] # [doc = ""] # [doc = " The UEFI specification defines the calling convention for each platform individually. It"] # [doc = " usually refers to other standards for details, but adds some restrictions on top. As of this"] # [doc = " writing, it mentions:"] # [doc = ""] # [doc = "  * aarch32 / arm: The `aapcs` calling-convention is used. It is native to aarch32 and described"] # [doc = "                   in a document called"] # [doc = "                   \"Procedure Call Standard for the ARM Architecture\". It is openly distributed"] # [doc = "                   by ARM and widely known under the keyword `aapcs`."] # [doc = "  * aarch64: The `aapcs64` calling-convention is used. It is native to aarch64 and described in"] # [doc = "             a document called"] # [doc = "             \"Procedure Call Standard for the ARM 64-bit Architecture (AArch64)\". It is openly"] # [doc = "             distributed by ARM and widely known under the keyword `aapcs64`."] # [doc = "  * ia-64: The \"P64 C Calling Convention\" as described in the"] # [doc = "           \"Itanium Software Conventions and Runtime Architecture Guide\". It is also"] # [doc = "           standardized in the \"Intel Itanium SAL Specification\"."] # [doc = "  * RISC-V: The \"Standard RISC-V C Calling Convention\" is used. The UEFI specification"] # [doc = "            describes it in detail, but also refers to the official RISC-V resources for"] # [doc = "            detailed information."] # [doc = "  * x86 / ia-32: The `cdecl` C calling convention is used. Originated in the C Language and"] # [doc = "                 originally tightly coupled to C specifics. Unclear whether a formal"] # [doc = "                 specification exists (does anyone know?). Most compilers support it under the"] # [doc = "                 `cdecl` keyword, and in nearly all situations it is the default on x86."] # [doc = "  * x86_64 / amd64 / x64: The `win64` calling-convention is used. It is similar to the `sysv64`"] # [doc = "                          convention that is used on most non-windows x86_64 systems, but not"] # [doc = "                          exactly the same. Microsoft provides open documentation on it. See"] # [doc = "                          MSDN \"x64 Software Conventions -> Calling Conventions\"."] # [doc = "                          The UEFI Specification does not directly refer to `win64`, but"] # [doc = "                          contains a full specification of the calling convention itself."] # [doc = ""] # [doc = " Note that in most cases the UEFI Specification adds several more restrictions on top of the"] # [doc = " common calling-conventions. These restrictions usually do not affect how the compiler will lay"] # [doc = " out the function calls. Instead, it usually only restricts the set of APIs that are allowed in"] # [doc = " UEFI. Therefore, most compilers already support the calling conventions used on UEFI."] # [doc = ""] # [doc = " # Variadics"] # [doc = ""] # [doc = " For some reason, the rust compiler allows variadics only in combination with the `\"C\"` calling"] # [doc = " convention, even if the selected calling-convention matches what `\"C\"` would select on the"] # [doc = " target platform. Hence, you will very likely be unable to use variadics with this macro."] # [doc = " Luckily, all of the UEFI functions that use variadics are wrappers around more low-level"] # [doc = " accessors, so they are not necessarily required."] # [macro_export] macro_rules ! eficall { (@ munch (($ ($ prefix : tt) *) , (pub $ ($ suffix : tt) *))) => { eficall ! { @ munch (($ ($ prefix) * pub) , ($ ($ suffix) *)) } } ; (@ munch (($ ($ prefix : tt) *) , (unsafe $ ($ suffix : tt) *))) => { eficall ! { @ munch (($ ($ prefix) * unsafe) , ($ ($ suffix) *)) } } ; (@ munch (($ ($ prefix : tt) *) , ($ ($ suffix : tt) *))) => { eficall_abi ! { ($ ($ prefix) *) , ($ ($ suffix) *) } } ; ($ ($ arg : tt) *) => { eficall ! { @ munch (() , ($ ($ arg) *)) } } ; }
};
}
