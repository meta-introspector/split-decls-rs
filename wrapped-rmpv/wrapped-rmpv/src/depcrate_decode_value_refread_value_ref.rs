// Generated macro for read_value_ref (function)
macro_rules! Depcrate_decode_value_refread_value_ref {
() => {
// Module: crate::decode::value_ref
// Provides: {"read_value_ref"}
// Dependencies: {}
# [doc = " Attempts to read the data from the given reader until either a complete MessagePack value"] # [doc = " decoded or an error detected."] # [doc = ""] # [doc = " Returns either a non-owning `ValueRef`, which borrows the buffer from the given reader or an"] # [doc = " error."] # [doc = ""] # [doc = " The reader should meet the requirement of a special `BorrowRead` trait, which allows to mutate"] # [doc = " itself but permits to mutate the buffer it contains. It allows to perform a completely"] # [doc = " zero-copy reading without a data loss fear in case of an error."] # [doc = ""] # [doc = " Currently only two types fit in this requirement: `&[u8]` and `Cursor<&[u8]>`. Using Cursor is"] # [doc = " helpful, when you need to know how exactly many bytes the decoded `ValueRef` consumes. A `Vec<u8>`"] # [doc = " type doesn't fit in the `BorrowRead` requirement, because its mut reference can mutate the"] # [doc = " underlying buffer - use `Vec::as_slice()` if you need to decode a value from the vector."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns an `Error` value if unable to continue the decoding operation either because of read"] # [doc = " failure or any other circumstances. See `Error` documentation for more information."] # [doc = ""] # [doc = " This function enforces a maximum recursion depth of [`MAX_DEPTH`](super::MAX_DEPTH) and returns"] # [doc = " [`Error::DepthLimitExceeded`] if the maximum is hit. If you run into stack overflows despite"] # [doc = " this, use [`read_value_ref_with_max_depth`] with a custom maximum depth."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```"] # [doc = " use rmpv::ValueRef;"] # [doc = " use rmpv::decode::read_value_ref;"] # [doc = ""] # [doc = " let buf = [0xaa, 0x6c, 0x65, 0x20, 0x6d, 0x65, 0x73, 0x73, 0x61, 0x67, 0x65];"] # [doc = " let mut rd = &buf[..];"] # [doc = ""] # [doc = " assert_eq!(ValueRef::from(\"le message\"), read_value_ref(&mut rd).unwrap());"] # [doc = " ```"] # [inline (never)] pub fn read_value_ref < 'a , R > (rd : & mut R) -> Result < ValueRef < 'a > , Error > where R : BorrowRead < 'a > { read_value_ref_inner (rd , super :: MAX_DEPTH as _) }
};
}
