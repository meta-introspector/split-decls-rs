// Generated macro for compute_member_data (function)
macro_rules! Depcrate_archive_writercompute_member_data {
() => {
// Module: crate::archive_writer
// Provides: {"compute_member_data"}
// Dependencies: {}
fn compute_member_data < 'a , S : Write + Seek > (string_table : & mut S , sym_names : & mut Cursor < Vec < u8 > > , kind : ArchiveKind , thin : bool , sym_map : & mut Option < & mut SymMap > , new_members : & 'a [NewArchiveMember < 'a >] , is_ec : Option < bool > ,) -> io :: Result < Vec < MemberData < 'a > > > { const PADDING_DATA : & [u8 ; 8] = & [b'\n' ; 8] ; let mut mem_head_pad_size = 0 ; let mut pos = if is_aix_big_archive (kind) { u64 :: try_from (std :: mem :: size_of :: < big_archive :: FixLenHdr > ()) . unwrap () } else { 0 } ; let mut ret = vec ! [] ; let mut has_object = false ; let mut member_names = HashMap :: < & str , u64 > :: new () ; let unique_timestamps = is_darwin (kind) ; let mut filename_count = HashMap :: new () ; if unique_timestamps { for m in new_members { * filename_count . entry (& * m . member_name) . or_insert (0) += 1 ; } for (_name , count) in filename_count . iter_mut () { * count = if * count > 1 { 1 } else { 0 } ; } } if let Some (sym_map) = sym_map { if let Some (is_ec) = is_ec { sym_map . use_ec_map = is_ec ; } else { let mut have_arm64 = false ; let mut have_ec = false ; for m in new_members { if ! have_arm64 { have_arm64 = (m . object_reader . is_any_arm64_coff) (m . buf . as_ref () . as_ref ()) ; } if ! have_ec { have_ec = (m . object_reader . is_ec_object_file) (m . buf . as_ref () . as_ref ()) ; } if have_arm64 && have_ec { sym_map . use_ec_map = true ; break ; } } } } let mut prev_offset = 0 ; let mut next_mem_head_pad_size = 0 ; let mut index = 0 ; for m in new_members { let mut header = Vec :: new () ; let buf = m . buf . as_ref () . as_ref () ; let data = if thin { & [] [..] } else { buf } ; index += 1 ; let member_padding = if is_darwin (kind) { offset_to_alignment (u64 :: try_from (data . len ()) . unwrap () , 8) } else { 0 } ; let tail_padding = offset_to_alignment (u64 :: try_from (data . len ()) . unwrap () + member_padding , 2) ; let padding = & PADDING_DATA [.. usize :: try_from (member_padding + tail_padding) . unwrap ()] ; let mtime = if unique_timestamps { * filename_count . get_mut (& * m . member_name) . unwrap () += 1 ; filename_count [& * m . member_name] - 1 } else { m . mtime } ; let size = u64 :: try_from (buf . len ()) . unwrap () + member_padding ; if size > MAX_MEMBER_SIZE { return Err (io :: Error :: other (format ! ("Archive member {} is too big" , m . member_name))) ; } if is_aix_big_archive (kind) { let offset_to_mem_data = pos + BIG_AR_MEM_HDR_SIZE + align_to (m . member_name . len () . try_into () . unwrap () , 2) ; if index == 1 { next_mem_head_pad_size = align_to_power_of2 (offset_to_mem_data , (m . object_reader . get_xcoff_member_alignment) (buf) . into () ,) - offset_to_mem_data ; } mem_head_pad_size = next_mem_head_pad_size ; pos += mem_head_pad_size ; let mut next_offset = pos + BIG_AR_MEM_HDR_SIZE + align_to (u64 :: try_from (m . member_name . len ()) . unwrap () , 2) + align_to (size , 2) ; if index != new_members . len () { let offset_to_next_mem_data = next_offset + BIG_AR_MEM_HDR_SIZE + align_to (new_members [index] . member_name . len () . try_into () . unwrap () , 2) ; next_mem_head_pad_size = align_to_power_of2 (offset_to_next_mem_data , (m . object_reader . get_xcoff_member_alignment) (new_members [index] . buf . as_ref () . as_ref () ,) . into () ,) - offset_to_next_mem_data ; next_offset += next_mem_head_pad_size ; } print_big_archive_member_header (& mut header , & m . member_name , mtime , m . uid , m . gid , m . perms , size , prev_offset , next_offset ,) ? ; prev_offset = pos ; } else { print_member_header (& mut header , pos , string_table , & mut member_names , kind , thin , m , mtime , size ,) ? ; } let symbols = write_symbols (buf , index . try_into () . unwrap () , sym_names , sym_map , m . object_reader ,) ? ; has_object = true ; pos += u64 :: try_from (header . len () + data . len () + padding . len ()) . unwrap () ; ret . push (MemberData { symbols , header , data , padding , pre_head_pad_size : mem_head_pad_size , object_reader : m . object_reader , }) } if has_object && sym_names . stream_position () ? == 0 && ! is_coff_archive (kind) { write ! (sym_names , "\0\0\0") ? ; } Ok (ret) }
};
}
