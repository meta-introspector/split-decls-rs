// Generated macro for impl_9 (impl)
macro_rules! Depcrateimpl_9 {
() => {
// Module: crate
// Provides: {"impl_9"}
// Dependencies: {}
impl Pattern { # [doc = " Returns a new `Pattern` for the given regex, or an error if the regex"] # [doc = " was invalid."] # [doc = ""] # [doc = " The returned `Pattern` will match occurances of the pattern which start"] # [doc = " at *any* in a byte or character stream — the pattern may be preceded by"] # [doc = " any number of non-matching characters. Essentially, it will behave as"] # [doc = " though the regular expression started with a `.*?`, which enables a"] # [doc = " match to appear anywhere. If this is not the desired behavior, use"] # [doc = " [`Pattern::new_anchored`] instead."] # [doc = ""] # [doc = " For example:"] # [doc = " ```"] # [doc = " use matchers::Pattern;"] # [doc = ""] # [doc = " // This pattern matches any number of `a`s followed by a `b`."] # [doc = " let pattern = Pattern::new(\"a+b\").expect(\"regex is not invalid\");"] # [doc = ""] # [doc = " // Of course, the pattern matches an input where the entire sequence of"] # [doc = " // characters matches the pattern:"] # [doc = " assert!(pattern.display_matches(&\"aaaaab\"));"] # [doc = ""] # [doc = " // And, since the pattern is unanchored, it will also match the"] # [doc = " // sequence when it's followed by non-matching characters:"] # [doc = " assert!(pattern.display_matches(&\"hello world! aaaaab\"));"] # [doc = " ```"] pub fn new (pattern : & str) -> Result < Self , BuildError > { let automaton = DFA :: new (pattern) ? ; Ok (Pattern { automaton , anchored : Anchored :: No , }) } # [doc = " Returns a new `Pattern` anchored at the beginning of the input stream,"] # [doc = " or an error if the regex was invalid."] # [doc = ""] # [doc = " The returned `Pattern` will *only* match an occurence of the pattern in"] # [doc = " an input sequence if the first character or byte in the input matches"] # [doc = " the pattern. If this is not the desired behavior, use [`Pattern::new`]"] # [doc = " instead."] # [doc = ""] # [doc = " For example:"] # [doc = " ```"] # [doc = " use matchers::Pattern;"] # [doc = ""] # [doc = " // This pattern matches any number of `a`s followed by a `b`."] # [doc = " let pattern = Pattern::new_anchored(\"a+b\")"] # [doc = "     .expect(\"regex is not invalid\");"] # [doc = ""] # [doc = " // The pattern matches an input where the entire sequence of"] # [doc = " // characters matches the pattern:"] # [doc = " assert!(pattern.display_matches(&\"aaaaab\"));"] # [doc = ""] # [doc = " // Since the pattern is anchored, it will *not* match an input that"] # [doc = " // begins with non-matching characters:"] # [doc = " assert!(!pattern.display_matches(&\"hello world! aaaaab\"));"] # [doc = ""] # [doc = " // ...however, if we create a pattern beginning with `.*?`, it will:"] # [doc = " let pattern2 = Pattern::new_anchored(\".*?a+b\")"] # [doc = "     .expect(\"regex is not invalid\");"] # [doc = " assert!(pattern2.display_matches(&\"hello world! aaaaab\"));"] # [doc = " ```"] pub fn new_anchored (pattern : & str) -> Result < Self , BuildError > { let automaton = DFA :: new (pattern) ? ; Ok (Pattern { automaton , anchored : Anchored :: Yes , }) } }
};
}
