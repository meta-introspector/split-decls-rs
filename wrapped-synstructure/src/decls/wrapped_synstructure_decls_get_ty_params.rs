// Generated by add_generated_rust_header! macro at submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs:72 from submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs
// Do not edit this file by hand. Your changes will be overwritten.

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
#[doc = " Helper function used by the `VariantInfo` constructor. Walks all of the types"]
#[doc = " in `field` and returns a list of the type parameters from `ty_params` which"]
#[doc = " are referenced in the field."]
fn get_ty_params(field: &Field, generics: &Generics) -> Vec<bool> {
    struct BoundTypeLocator<'a> {
        result: Vec<bool>,
        generics: &'a Generics,
    }
    impl<'a> Visit<'a> for BoundTypeLocator<'a> {
        fn visit_ident(&mut self, id: &Ident) {
            for (idx, i) in self.generics.params.iter().enumerate() {
                if let GenericParam::Type(tparam) = i {
                    if tparam.ident == *id {
                        self.result[idx] = true;
                    }
                }
            }
        }
        fn visit_type_macro(&mut self, x: &'a TypeMacro) {
            for r in &mut self.result {
                *r = true;
            }
            visit::visit_type_macro(self, x);
        }
    }
    let mut btl = BoundTypeLocator {
        result: vec![false; generics.params.len()],
        generics,
    };
    btl.visit_type(&field.ty);
    btl.result
}
