// Generated macro for float_ty (macro)
macro_rules! Depcrate_value_numberfloat_ty {
() => {
// Module: crate::value::number
// Provides: {"float_ty"}
// Dependencies: {}
macro_rules ! float_ty { ($ ty : ident ($ float : ty)) => { # [doc = concat ! ("A wrapper for [`" , stringify ! ($ float) , "`], which implements [`Eq`], " , "[`Hash`] and [`Ord`] using [`" , stringify ! ($ float) , "::total_cmp`] " , "for a total order comparison" ,)] # [derive (Copy , Clone , Debug)] pub struct $ ty (pub $ float) ; impl $ ty { # [doc = concat ! ("Construct a new [`" , stringify ! ($ ty) , "`].")] # [must_use] pub fn new (v : $ float) -> Self { Self (v) } # [doc = concat ! ("Returns the wrapped [`" , stringify ! ($ float) , "`].")] # [must_use] pub fn get (self) -> $ float { self . 0 } } impl From <$ float > for $ ty { fn from (v : $ float) -> Self { Self :: new (v) } } # [doc = " Partial equality comparison"] # [doc = ""] # [doc = concat ! ("In order to be able to use [`" , stringify ! ($ ty) , "`] as a mapping key, " , "floating values use [`" , stringify ! ($ float) , "::total_cmp`] for a total " , "order comparison." ,)] # [doc = ""] # [doc = " See the [`Ord`] implementation."] impl PartialEq for $ ty { fn eq (& self , other : & Self) -> bool { self . cmp (other) . is_eq () } } # [doc = " Equality comparison"] # [doc = ""] # [doc = concat ! ("In order to be able to use [`" , stringify ! ($ ty) , "`] as a mapping key, " , "floating values use [`" , stringify ! ($ float) , "::total_cmp`] for a total " , "order comparison." ,)] # [doc = ""] # [doc = " See the [`Ord`] implementation."] impl Eq for $ ty { } impl Hash for $ ty { fn hash < H : Hasher > (& self , state : & mut H) { self . 0 . to_bits () . hash (state) ; } } # [doc = " Partial ordering comparison"] # [doc = ""] # [doc = concat ! ("In order to be able to use [`" , stringify ! ($ ty) , "`] as a mapping key, " , "floating values use [`" , stringify ! ($ float) , "::total_cmp`] for a total " , "order comparison." ,)] # [doc = ""] # [doc = " See the [`Ord`] implementation."] impl PartialOrd for $ ty { fn partial_cmp (& self , other : & Self) -> Option < Ordering > { Some (self . cmp (other)) } } # [doc = " Ordering comparison"] # [doc = ""] # [doc = concat ! ("In order to be able to use [`" , stringify ! ($ ty) , "`] as a mapping key, " , "floating values use [`" , stringify ! ($ float) , "::total_cmp`] for a total " , "order comparison." ,)] # [doc = ""] # [doc = " ```"] # [doc = concat ! ("use ron::value::" , stringify ! ($ ty) , ";")] # [doc = concat ! ("assert!(" , stringify ! ($ ty) , "::new(" , stringify ! ($ float) , "::NAN) > " , stringify ! ($ ty) , "::new(" , stringify ! ($ float) , "::INFINITY));" ,)] # [doc = concat ! ("assert!(" , stringify ! ($ ty) , "::new(-" , stringify ! ($ float) , "::NAN) < " , stringify ! ($ ty) , "::new(" , stringify ! ($ float) , "::NEG_INFINITY));" ,)] # [doc = concat ! ("assert!(" , stringify ! ($ ty) , "::new(" , stringify ! ($ float) , "::NAN) == " , stringify ! ($ ty) , "::new(" , stringify ! ($ float) , "::NAN));" ,)] # [doc = " ```"] impl Ord for $ ty { fn cmp (& self , other : & Self) -> Ordering { self . 0 . total_cmp (& other . 0) } } } ; }
};
}
