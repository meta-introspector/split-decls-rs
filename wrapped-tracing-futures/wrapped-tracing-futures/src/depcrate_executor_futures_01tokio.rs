// Generated macro for tokio (module)
macro_rules! Depcrate_executor_futures_01tokio {
() => {
// Module: crate::executor::futures_01
// Provides: {"tokio"}
// Dependencies: {}
# [cfg (feature = "tokio")] mod tokio { use crate :: { Instrument , Instrumented , WithDispatch } ; use futures_01 :: Future ; use tokio_01 :: { executor :: { Executor , SpawnError , TypedExecutor } , runtime :: { current_thread , Runtime , TaskExecutor } , } ; impl < T > Executor for Instrumented < T > where T : Executor , { fn spawn (& mut self , future : Box < dyn Future < Error = () , Item = () > + 'static + Send > ,) -> Result < () , SpawnError > { let future = Box :: new (future . instrument (self . span . clone ())) ; self . inner . spawn (future) } } impl < T , F > TypedExecutor < F > for Instrumented < T > where T : TypedExecutor < Instrumented < F > > , { fn spawn (& mut self , future : F) -> Result < () , SpawnError > { self . inner . spawn (future . instrument (self . span . clone ())) } fn status (& self) -> Result < () , SpawnError > { self . inner . status () } } impl Instrumented < Runtime > { # [doc = " Spawn an instrumented future onto the Tokio runtime."] # [doc = ""] # [doc = " This spawns the given future onto the runtime's executor, usually a"] # [doc = " thread pool. The thread pool is then responsible for polling the"] # [doc = " future until it completes."] # [doc = ""] # [doc = " This method simply wraps a call to `tokio::runtime::Runtime::spawn`,"] # [doc = " instrumenting the spawned future beforehand."] pub fn spawn < F > (& mut self , future : F) -> & mut Self where F : Future < Item = () , Error = () > + Send + 'static , { let future = future . instrument (self . span . clone ()) ; self . inner . spawn (future) ; self } # [doc = " Run an instrumented future to completion on the Tokio runtime."] # [doc = ""] # [doc = " This runs the given future on the runtime, blocking until it is"] # [doc = " complete, and yielding its resolved result. Any tasks or timers which"] # [doc = " the future spawns internally will be executed on the runtime."] # [doc = ""] # [doc = " This method should not be called from an asynchronous context."] # [doc = ""] # [doc = " This method simply wraps a call to `tokio::runtime::Runtime::block_on`,"] # [doc = " instrumenting the spawned future beforehand."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if the executor is at capacity, if the provided"] # [doc = " future panics, or if called within an asynchronous execution context."] pub fn block_on < F , R , E > (& mut self , future : F) -> Result < R , E > where F : Send + 'static + Future < Item = R , Error = E > , R : Send + 'static , E : Send + 'static , { let future = future . instrument (self . span . clone ()) ; self . inner . block_on (future) } # [doc = " Return an instrumented handle to the runtime's executor."] # [doc = ""] # [doc = " The returned handle can be used to spawn tasks that run on this runtime."] # [doc = ""] # [doc = " The instrumented handle functions identically to a"] # [doc = " `tokio::runtime::TaskExecutor`, but instruments the spawned"] # [doc = " futures prior to spawning them."] pub fn executor (& self) -> Instrumented < TaskExecutor > { self . inner . executor () . instrument (self . span . clone ()) } } impl Instrumented < current_thread :: Runtime > { # [doc = " Spawn an instrumented future onto the single-threaded Tokio runtime."] # [doc = ""] # [doc = " This method simply wraps a call to `current_thread::Runtime::spawn`,"] # [doc = " instrumenting the spawned future beforehand."] pub fn spawn < F > (& mut self , future : F) -> & mut Self where F : Future < Item = () , Error = () > + 'static , { let future = future . instrument (self . span . clone ()) ; self . inner . spawn (future) ; self } # [doc = " Instruments and runs the provided future, blocking the current thread"] # [doc = " until the future completes."] # [doc = ""] # [doc = " This function can be used to synchronously block the current thread"] # [doc = " until the provided `future` has resolved either successfully or with an"] # [doc = " error. The result of the future is then returned from this function"] # [doc = " call."] # [doc = ""] # [doc = " Note that this function will **also** execute any spawned futures on the"] # [doc = " current thread, but will **not** block until these other spawned futures"] # [doc = " have completed. Once the function returns, any uncompleted futures"] # [doc = " remain pending in the `Runtime` instance. These futures will not run"] # [doc = " until `block_on` or `run` is called again."] # [doc = ""] # [doc = " The caller is responsible for ensuring that other spawned futures"] # [doc = " complete execution by calling `block_on` or `run`."] # [doc = ""] # [doc = " This method simply wraps a call to `current_thread::Runtime::block_on`,"] # [doc = " instrumenting the spawned future beforehand."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if the executor is at capacity, if the provided"] # [doc = " future panics, or if called within an asynchronous execution context."] pub fn block_on < F , R , E > (& mut self , future : F) -> Result < R , E > where F : 'static + Future < Item = R , Error = E > , R : 'static , E : 'static , { let future = future . instrument (self . span . clone ()) ; self . inner . block_on (future) } # [doc = " Get a new instrumented handle to spawn futures on the single-threaded"] # [doc = " Tokio runtime"] # [doc = ""] # [doc = " Different to the runtime itself, the handle can be sent to different"] # [doc = " threads."] # [doc = ""] # [doc = " The instrumented handle functions identically to a"] # [doc = " `tokio::runtime::current_thread::Handle`, but instruments the spawned"] # [doc = " futures prior to spawning them."] pub fn handle (& self) -> Instrumented < current_thread :: Handle > { self . inner . handle () . instrument (self . span . clone ()) } } impl < T > Executor for WithDispatch < T > where T : Executor , { fn spawn (& mut self , future : Box < dyn Future < Error = () , Item = () > + 'static + Send > ,) -> Result < () , SpawnError > { let future = Box :: new (self . with_dispatch (future)) ; self . inner . spawn (future) } } impl < T , F > TypedExecutor < F > for WithDispatch < T > where T : TypedExecutor < WithDispatch < F > > , { fn spawn (& mut self , future : F) -> Result < () , SpawnError > { self . inner . spawn (self . with_dispatch (future)) } fn status (& self) -> Result < () , SpawnError > { self . inner . status () } } impl WithDispatch < Runtime > { # [doc = " Spawn a future onto the Tokio runtime, in the context of this"] # [doc = " `WithDispatch`'s trace dispatcher."] # [doc = ""] # [doc = " This spawns the given future onto the runtime's executor, usually a"] # [doc = " thread pool. The thread pool is then responsible for polling the"] # [doc = " future until it completes."] # [doc = ""] # [doc = " This method simply wraps a call to `tokio::runtime::Runtime::spawn`,"] # [doc = " instrumenting the spawned future beforehand."] pub fn spawn < F > (& mut self , future : F) -> & mut Self where F : Future < Item = () , Error = () > + Send + 'static , { let future = self . with_dispatch (future) ; self . inner . spawn (future) ; self } # [doc = " Run a future to completion on the Tokio runtime, in the context of this"] # [doc = " `WithDispatch`'s trace dispatcher."] # [doc = ""] # [doc = " This runs the given future on the runtime, blocking until it is"] # [doc = " complete, and yielding its resolved result. Any tasks or timers which"] # [doc = " the future spawns internally will be executed on the runtime."] # [doc = ""] # [doc = " This method should not be called from an asynchronous context."] # [doc = ""] # [doc = " This method simply wraps a call to `tokio::runtime::Runtime::block_on`,"] # [doc = " instrumenting the spawned future beforehand."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if the executor is at capacity, if the provided"] # [doc = " future panics, or if called within an asynchronous execution context."] pub fn block_on < F , R , E > (& mut self , future : F) -> Result < R , E > where F : Send + 'static + Future < Item = R , Error = E > , R : Send + 'static , E : Send + 'static , { let future = self . with_dispatch (future) ; self . inner . block_on (future) } # [doc = " Return a handle to the runtime's executor, in the context of this"] # [doc = " `WithDispatch`'s trace dispatcher."] # [doc = ""] # [doc = " The returned handle can be used to spawn tasks that run on this runtime."] # [doc = ""] # [doc = " The instrumented handle functions identically to a"] # [doc = " `tokio::runtime::TaskExecutor`, but instruments the spawned"] # [doc = " futures prior to spawning them."] pub fn executor (& self) -> WithDispatch < TaskExecutor > { self . with_dispatch (self . inner . executor ()) } } impl WithDispatch < current_thread :: Runtime > { # [doc = " Spawn a future onto the single-threaded Tokio runtime, in the context"] # [doc = " of this `WithDispatch`'s trace dispatcher."] # [doc = ""] # [doc = " This method simply wraps a call to `current_thread::Runtime::spawn`,"] # [doc = " instrumenting the spawned future beforehand."] pub fn spawn < F > (& mut self , future : F) -> & mut Self where F : Future < Item = () , Error = () > + 'static , { let future = self . with_dispatch (future) ; self . inner . spawn (future) ; self } # [doc = " Runs the provided future in the context of this `WithDispatch`'s trace"] # [doc = " dispatcher, blocking the current thread until the future completes."] # [doc = ""] # [doc = " This function can be used to synchronously block the current thread"] # [doc = " until the provided `future` has resolved either successfully or with an"] # [doc = " error. The result of the future is then returned from this function"] # [doc = " call."] # [doc = ""] # [doc = " Note that this function will **also** execute any spawned futures on the"] # [doc = " current thread, but will **not** block until these other spawned futures"] # [doc = " have completed. Once the function returns, any uncompleted futures"] # [doc = " remain pending in the `Runtime` instance. These futures will not run"] # [doc = " until `block_on` or `run` is called again."] # [doc = ""] # [doc = " The caller is responsible for ensuring that other spawned futures"] # [doc = " complete execution by calling `block_on` or `run`."] # [doc = ""] # [doc = " This method simply wraps a call to `current_thread::Runtime::block_on`,"] # [doc = " instrumenting the spawned future beforehand."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if the executor is at capacity, if the provided"] # [doc = " future panics, or if called within an asynchronous execution context."] pub fn block_on < F , R , E > (& mut self , future : F) -> Result < R , E > where F : 'static + Future < Item = R , Error = E > , R : 'static , E : 'static , { let future = self . with_dispatch (future) ; self . inner . block_on (future) } # [doc = " Get a new handle to spawn futures on the single-threaded Tokio runtime,"] # [doc = " in the context of this `WithDispatch`'s trace dispatcher.\\"] # [doc = ""] # [doc = " Different to the runtime itself, the handle can be sent to different"] # [doc = " threads."] # [doc = ""] # [doc = " The instrumented handle functions identically to a"] # [doc = " `tokio::runtime::current_thread::Handle`, but the spawned"] # [doc = " futures are run in the context of the trace dispatcher."] pub fn handle (& self) -> WithDispatch < current_thread :: Handle > { self . with_dispatch (self . inner . handle ()) } } }
};
}
