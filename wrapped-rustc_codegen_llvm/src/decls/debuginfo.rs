macro_rules! deps {
    () => {
        InvariantOpaque!();
    };
}

macro_rules! debuginfo {
    () => {
        deps!();
        pub (crate) mod debuginfo { use std :: ptr ; use bitflags :: bitflags ; use super :: { InvariantOpaque , Metadata } ; use crate :: llvm :: { self , Module } ; # [doc = " Opaque target type for references to an LLVM debuginfo builder."] # [doc = ""] # [doc = " `&'_ DIBuilder<'ll>` corresponds to `LLVMDIBuilderRef`, which is the"] # [doc = " LLVM-C wrapper for `DIBuilder *`."] # [doc = ""] # [doc = " Debuginfo builders are created and destroyed during codegen, so the"] # [doc = " builder reference typically has a shorter lifetime than the LLVM"] # [doc = " session (`'ll`) that it participates in."] # [repr (C)] pub (crate) struct DIBuilder < 'll > (InvariantOpaque < 'll >) ; # [doc = " Owning pointer to a `DIBuilder<'ll>` that will dispose of the builder"] # [doc = " when dropped. Use `.as_ref()` to get the underlying `&DIBuilder`"] # [doc = " needed for debuginfo FFI calls."] pub (crate) struct DIBuilderBox < 'll > { raw : ptr :: NonNull < DIBuilder < 'll > > , } impl < 'll > DIBuilderBox < 'll > { pub (crate) fn new (llmod : & 'll Module) -> Self { let raw = unsafe { llvm :: LLVMCreateDIBuilder (llmod) } ; let raw = ptr :: NonNull :: new (raw) . unwrap () ; Self { raw } } pub (crate) fn as_ref (& self) -> & DIBuilder < 'll > { unsafe { self . raw . as_ref () } } } impl < 'll > Drop for DIBuilderBox < 'll > { fn drop (& mut self) { unsafe { llvm :: LLVMDisposeDIBuilder (self . raw) } ; } } pub (crate) type DIDescriptor = Metadata ; pub (crate) type DILocation = Metadata ; pub (crate) type DIScope = DIDescriptor ; pub (crate) type DIFile = DIScope ; pub (crate) type DILexicalBlock = DIScope ; pub (crate) type DISubprogram = DIScope ; pub (crate) type DIType = DIDescriptor ; pub (crate) type DIBasicType = DIType ; pub (crate) type DIDerivedType = DIType ; pub (crate) type DICompositeType = DIDerivedType ; pub (crate) type DIVariable = DIDescriptor ; pub (crate) type DIGlobalVariableExpression = DIDescriptor ; pub (crate) type DIArray = DIDescriptor ; pub (crate) type DISubrange = DIDescriptor ; pub (crate) type DIEnumerator = DIDescriptor ; pub (crate) type DITemplateTypeParameter = DIDescriptor ; bitflags ! { # [doc = " Must match the layout of `LLVMDIFlags` in the LLVM-C API."] # [doc = ""] # [doc = " Each value declared here must also be covered by the static"] # [doc = " assertions in `RustWrapper.cpp` used by `fromRust(LLVMDIFlags)`."] # [repr (transparent)] # [derive (Clone , Copy , Default)] pub (crate) struct DIFlags : u32 { const FlagZero = 0 ; const FlagPrivate = 1 ; const FlagProtected = 2 ; const FlagPublic = 3 ; const FlagFwdDecl = (1 << 2) ; const FlagAppleBlock = (1 << 3) ; const FlagReservedBit4 = (1 << 4) ; const FlagVirtual = (1 << 5) ; const FlagArtificial = (1 << 6) ; const FlagExplicit = (1 << 7) ; const FlagPrototyped = (1 << 8) ; const FlagObjcClassComplete = (1 << 9) ; const FlagObjectPointer = (1 << 10) ; const FlagVector = (1 << 11) ; const FlagStaticMember = (1 << 12) ; const FlagLValueReference = (1 << 13) ; const FlagRValueReference = (1 << 14) ; const FlagReserved = (1 << 15) ; const FlagSingleInheritance = (1 << 16) ; const FlagMultipleInheritance = (2 << 16) ; const FlagVirtualInheritance = (3 << 16) ; const FlagIntroducedVirtual = (1 << 18) ; const FlagBitField = (1 << 19) ; const FlagNoReturn = (1 << 20) ; const FlagTypePassByValue = (1 << 22) ; const FlagTypePassByReference = (1 << 23) ; const FlagEnumClass = (1 << 24) ; const FlagThunk = (1 << 25) ; const FlagNonTrivial = (1 << 26) ; const FlagBigEndian = (1 << 27) ; const FlagLittleEndian = (1 << 28) ; } } bitflags ! { # [repr (transparent)] # [derive (Clone , Copy , Default)] pub (crate) struct DISPFlags : u32 { const SPFlagZero = 0 ; const SPFlagVirtual = 1 ; const SPFlagPureVirtual = 2 ; const SPFlagLocalToUnit = (1 << 2) ; const SPFlagDefinition = (1 << 3) ; const SPFlagOptimized = (1 << 4) ; const SPFlagMainSubprogram = (1 << 5) ; } } # [doc = " LLVMRustDebugEmissionKind"] # [derive (Copy , Clone)] # [repr (C)] pub (crate) enum DebugEmissionKind { NoDebug , FullDebug , LineTablesOnly , DebugDirectivesOnly , } impl DebugEmissionKind { pub (crate) fn from_generic (kind : rustc_session :: config :: DebugInfo) -> Self { use rustc_session :: config :: DebugInfo ; match kind { DebugInfo :: None => DebugEmissionKind :: NoDebug , DebugInfo :: LineDirectivesOnly => DebugEmissionKind :: DebugDirectivesOnly , DebugInfo :: LineTablesOnly => DebugEmissionKind :: LineTablesOnly , DebugInfo :: Limited | DebugInfo :: Full => DebugEmissionKind :: FullDebug , } } } # [doc = " LLVMRustDebugNameTableKind"] # [derive (Clone , Copy)] # [repr (C)] pub (crate) enum DebugNameTableKind { Default , # [expect (dead_code)] Gnu , None , } }
    };
}

debuginfo!()