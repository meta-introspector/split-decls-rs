macro_rules! macro_263 {
    () => {
        rustc_index :: newtype_index ! { # [doc = " An index into a function's list of global file IDs. That underlying list"] # [doc = " of local-to-global mappings will be embedded in the function's record in"] # [doc = " the `__llvm_covfun` linker section."] struct LocalFileId { } }
    };
}

macro_263!()