// Generated macro for Worker (trait)
macro_rules! Depcrate_actor_traitsWorker {
() => {
// Module: crate::actor::traits
// Provides: {"Worker"}
// Dependencies: {}
# [doc = " Declares the behaviour of a worker."] pub trait Worker : Sized { # [doc = " Update message type."] type Message ; # [doc = " Incoming message type."] type Input ; # [doc = " Outgoing message type."] type Output ; # [doc = " Creates an instance of a worker."] fn create (scope : & WorkerScope < Self >) -> Self ; # [doc = " Receives an update."] # [doc = ""] # [doc = " This method is called when the worker send messages to itself via [`WorkerScope::send_message`]."] fn update (& mut self , scope : & WorkerScope < Self > , msg : Self :: Message) ; # [doc = " New bridge created."] # [doc = ""] # [doc = " When a new bridge is created by [`WorkerSpawner::spawn`](crate::WorkerSpawner)"] # [doc = " or [`WorkerBridge::fork`](crate::WorkerBridge::fork),"] # [doc = " the worker will be notified the [`HandlerId`] of the created bridge via this method."] fn connected (& mut self , scope : & WorkerScope < Self > , id : HandlerId) { let _scope = scope ; let _id = id ; } # [doc = " Receives an input from a connected bridge."] # [doc = ""] # [doc = " When a bridge sends an input via [`WorkerBridge::send`](crate::WorkerBridge::send), the worker will receive the"] # [doc = " input via this method."] fn received (& mut self , scope : & WorkerScope < Self > , msg : Self :: Input , id : HandlerId) ; # [doc = " Existing bridge destroyed."] # [doc = ""] # [doc = " When a bridge is dropped, the worker will be notified with this method."] fn disconnected (& mut self , scope : & WorkerScope < Self > , id : HandlerId) { let _scope = scope ; let _id = id ; } # [doc = " Destroys the current worker."] # [doc = ""] # [doc = " When all bridges are dropped, the method will be invoked."] # [doc = ""] # [doc = " This method is provided a destroy handle where when it is dropped, the worker is closed."] # [doc = " If the worker is closed immediately, then it can ignore the destroy handle."] # [doc = " Otherwise hold the destroy handle until the clean up task is finished."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " This method will only be called after all bridges are disconnected."] # [doc = " Attempting to send messages after this method is called will have no effect."] fn destroy (& mut self , scope : & WorkerScope < Self > , destruct : WorkerDestroyHandle < Self >) { let _scope = scope ; let _destruct = destruct ; } }
};
}
