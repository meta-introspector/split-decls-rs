// Generated macro for test_bake (macro)
macro_rules! Depcratetest_bake {
() => {
// Module: crate
// Provides: {"test_bake"}
// Dependencies: {}
# [doc = " This macro tests that an expression evaluates to a value that bakes to the same expression."] # [doc = ""] # [doc = " Its mandatory arguments are a type and an expression (of that type)."] # [doc = ""] # [doc = " ```"] # [doc = " # use databake::test_bake;"] # [doc = " test_bake!(usize, 18usize);"] # [doc = " ```"] # [doc = ""] # [doc = " ## `Const`"] # [doc = ""] # [doc = " We usually want baked output to be const constructible. To test this, add the `const:` prefix to"] # [doc = " the expression:"] # [doc = ""] # [doc = " ```"] # [doc = " # use databake::test_bake;"] # [doc = " test_bake!(usize, const, 18usize);"] # [doc = " ```"] # [doc = ""] # [doc = " ## Crates and imports"] # [doc = ""] # [doc = " As most output will need to reference its crate, and its not possible to name a crate from"] # [doc = " within it, a third parameter can be used to specify the crate name. The `crate` identifier"] # [doc = " in the original expression will be replaced by this in the expected output."] # [doc = ""] # [doc = " ```no_run"] # [doc = " # use databake::*;"] # [doc = " # struct MyStruct(usize);"] # [doc = " # impl Bake for MyStruct {"] # [doc = " #   fn bake(&self, _: &CrateEnv) -> TokenStream { unimplemented!() }"] # [doc = " # }"] # [doc = " # // We need an explicit main to put the struct at the crate root"] # [doc = " # fn main() {"] # [doc = " test_bake!("] # [doc = "     MyStruct,"] # [doc = "     crate::MyStruct(42usize), // matches `::my_crate::MyStruct(42usize)`"] # [doc = "     my_crate,"] # [doc = " );"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " A fourth, optional, parameter is a list of crate names that are expected to be added to the"] # [doc = " `CrateEnv`. The `crate`-replacement crate will always be checked."] # [macro_export] macro_rules ! test_bake { ($ type : ty , const , $ expr : expr $ (, $ krate : ident) ? $ (, [$ ($ env_crate : ident) ,+]) ? $ (,) ?) => { const _ : &$ type = &$ expr ; $ crate :: test_bake ! ($ type , $ expr $ (, $ krate) ? $ (, [$ ($ env_crate) ,+]) ?) ; } ; ($ type : ty , $ expr : expr $ (, $ krate : ident) ? $ (, [$ ($ env_crate : ident) ,+]) ? $ (,) ?) => { let env = Default :: default () ; let expr : &$ type = &$ expr ; let bake = $ crate :: Bake :: bake (expr , & env) . to_string () ; let expected_bake = $ crate :: quote ! ($ expr) . to_string () . replace ("::<" , ":: <") . replace (">::" , "> ::") ; let bake = bake . replace (" ,)" , ")") . replace (" ,]" , "]") . replace (" , }" , " }") . replace (" , >" , " >") ; let expected_bake = expected_bake . replace (" ,)" , ")") . replace (" ,]" , "]") . replace (" , }" , " }") . replace (" , >" , " >") ; $ (let expected_bake = expected_bake . replace ("crate" , stringify ! ($ krate)) ;) ? assert_eq ! (bake , expected_bake) ; # [allow (unused_variables)] let _env = env . into_iter () . collect ::< std :: collections :: HashSet < _ >> () ; $ (assert ! (_env . contains (stringify ! ($ krate)) , "Crate {:?} was not added to the CrateEnv" , stringify ! ($ krate)) ;) ? $ ($ (assert ! (_env . contains (stringify ! ($ env_crate)) , "Crate {:?} was not added to the CrateEnv" , stringify ! ($ env_crate)) ;) +) ? } ; }
};
}
