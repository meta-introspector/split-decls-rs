// Generated macro for conditions (macro)
macro_rules! Depcrate_conditionconditions {
() => {
// Module: crate::condition
// Provides: {"conditions"}
// Dependencies: {}
macro_rules ! conditions { ($ feat : meta $ ($ f : expr , $ CACHED : ident : $ cached : ident , $ LIVE : ident : $ live : ident) ,* $ (,) ?) => (# [cfg ($ feat)] # [cfg_attr (feature = "_nightly" , doc (cfg ($ feat)))] # [doc = " Feature dependent conditions."] # [doc = ""] # [doc = " Available when compiled with"] # [doc = concat ! ('`' , stringify ! ($ feat) , "`.")] impl Condition { $ (# [doc = " Evaluates to `true` if"] # [doc = concat ! ('`' , stringify ! ($ f) , "`.")] # [doc = ""] # [doc = " The result of the first check is cached for subsequent"] # [doc = " checks. Internally uses"] # [doc = concat ! ("[`" , stringify ! ($ cached) , "`](Condition::" , stringify ! ($ cached) , ").")] pub const $ CACHED : Condition = Condition (Condition ::$ cached) ;) * $ (# [doc = " Evaluates to `true` if"] # [doc = concat ! ('`' , stringify ! ($ f) , "`.")] # [doc = ""] # [doc = " A call is dispatched each time the condition is checked."] # [doc = " This is expensive, so prefer to use"] # [doc = concat ! ("[`" , stringify ! ($ CACHED) , "`](Condition::" , stringify ! ($ CACHED) , ")")] # [doc = " instead."] # [doc = ""] # [doc = " Internally uses"] # [doc = concat ! ("[`" , stringify ! ($ live) , "`](Condition::" , stringify ! ($ live) , ").")] pub const $ LIVE : Condition = Condition (Condition ::$ live) ;) * $ (# [doc = " Returns `true` if"] # [doc = concat ! ('`' , stringify ! ($ f) , "`.")] # [doc = ""] # [doc = " The result of the first check is cached for subsequent"] # [doc = " checks. This is the backing function for"] # [doc = concat ! ("[`" , stringify ! ($ CACHED) , "`](Condition::" , stringify ! ($ CACHED) , ").")] pub fn $ cached () -> bool { static IS_TTY : CachedBool = CachedBool :: new () ; IS_TTY . get_or_init (Condition ::$ live) }) * $ (# [doc = " Returns `true` if"] # [doc = concat ! ('`' , stringify ! ($ f) , "`.")] # [doc = ""] # [doc = " This is the backing function for"] # [doc = concat ! ("[`" , stringify ! ($ LIVE) , "`](Condition::" , stringify ! ($ LIVE) , ").")] pub fn $ live () -> bool { $ f }) * }) }
};
}
