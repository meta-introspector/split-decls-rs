// Generated macro for impl_79 (impl)
macro_rules! Depcrate_conditionimpl_79 {
() => {
// Module: crate::condition
// Provides: {"impl_79"}
// Dependencies: {}
impl Condition { # [doc = " A condition that evaluates to `true` if the OS supports coloring."] # [doc = ""] # [doc = " Uses [`Condition::os_support()`]. On Windows, this condition tries to"] # [doc = " enable coloring support on the first call and caches the result for"] # [doc = " subsequent calls. Outside of Windows, this always evaluates to `true`."] pub const DEFAULT : Condition = Condition (Condition :: os_support) ; # [doc = " A condition that always evaluates to `true`."] pub const ALWAYS : Condition = Condition (Condition :: always) ; # [doc = " A condition that always evaluated to `false`."] pub const NEVER : Condition = Condition (Condition :: never) ; # [doc = " Creates a dynamically checked condition from a function `f`."] # [doc = ""] # [doc = " The function `f` is called anytime the condition is checked, including"] # [doc = " every time a style with the condition is used."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use yansi::Condition;"] # [doc = ""] # [doc = " fn some_function() -> bool {"] # [doc = "     /* checking arbitrary conditions */"] # [doc = "     todo!()"] # [doc = " }"] # [doc = ""] # [doc = " // Create a custom static condition from a function."] # [doc = " static MY_CONDITION: Condition = Condition::from(some_function);"] # [doc = ""] # [doc = " // Create a condition on the stack from a function."] # [doc = " let my_condition = Condition::from(some_function);"] # [doc = ""] # [doc = " // Create a static condition from a closure that becomes a `fn`."] # [doc = " static MY_CONDITION_2: Condition = Condition::from(|| false);"] # [doc = ""] # [doc = " // Create a condition on the stack from a closure that becomes a `fn`."] # [doc = " let my_condition = Condition::from(|| some_function());"] # [doc = " ```"] pub const fn from (f : fn () -> bool) -> Self { Condition (f) } # [doc = " Creates a condition that is [`ALWAYS`](Self::ALWAYS) when `value` is"] # [doc = " `true` and [`NEVER`](Self::NEVER) otherwise."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use yansi::Condition;"] # [doc = ""] # [doc = " fn some_function() -> bool {"] # [doc = "     /* checking arbitrary conditions */"] # [doc = "     todo!()"] # [doc = " }"] # [doc = ""] # [doc = " // Cache the result of `some_function()` so it doesn't get called each"] # [doc = " // time the condition needs to be checked."] # [doc = " let my_condition = Condition::cached(some_function());"] # [doc = " ```"] pub const fn cached (value : bool) -> Self { match value { true => Condition :: ALWAYS , false => Condition :: NEVER , } } # [doc = " The backing function for [`Condition::ALWAYS`]. Returns `true` always."] pub const fn always () -> bool { true } # [doc = " The backing function for [`Condition::NEVER`]. Returns `false` always."] pub const fn never () -> bool { false } # [doc = " The backing function for [`Condition::DEFAULT`]."] # [doc = ""] # [doc = " Returns `true` if the current OS supports ANSI escape sequences for"] # [doc = " coloring. Outside of Windows, this always returns `true`. On Windows,"] # [doc = " the first call to this function attempts to enable support and returns"] # [doc = " whether it was successful every time thereafter."] pub fn os_support () -> bool { crate :: windows :: cache_enable () } }
};
}
