// Generated macro for _config_data (macro)
macro_rules! Depcrate_config_config_data {
() => {
// Module: crate::config
// Provides: {"_config_data"}
// Dependencies: {}
macro_rules ! _config_data { ($ (# [doc =$ dox : literal]) * $ modname : ident : struct $ name : ident <- $ input : ident -> { $ ($ (# [doc =$ doc : literal]) * $ vis : vis $ field : ident $ (| $ alias : ident) *: $ ty : ty = $ default : expr ,) * }) => { # [doc = " Default config values for this grouping."] # [allow (non_snake_case)] # [derive (Debug , Clone)] struct $ name { $ ($ field : $ ty ,) * } impl_for_config_data ! { $ modname , $ ($ vis $ field : $ ty = $ default ,) * } # [doc = " All fields `Option<T>`, `None` representing fields not set in a particular JSON/TOML blob."] # [allow (non_snake_case)] # [derive (Clone , Default)] struct $ input { $ ($ field : Option <$ ty >,) * } impl std :: fmt :: Debug for $ input { fn fmt (& self , f : & mut std :: fmt :: Formatter <'_ >) -> std :: fmt :: Result { let mut s = f . debug_struct (stringify ! ($ input)) ; $ (if let Some (val) = self .$ field . as_ref () { s . field (stringify ! ($ field) , val) ; }) * s . finish () } } impl Default for $ name { fn default () -> Self { $ name { $ ($ field : default_val ! ($ default , $ ty) ,) * } } } # [allow (unused , clippy :: ptr_arg)] impl $ input { const FIELDS : &'static [&'static str] = & [$ (stringify ! ($ field)) ,*] ; fn from_json (json : & mut serde_json :: Value , error_sink : & mut Vec < (String , serde_json :: Error) >) -> Self { Self { $ ($ field : get_field_json (json , error_sink , stringify ! ($ field) , None $ (. or (Some (stringify ! ($ alias)))) *,) ,) * } } fn from_toml (toml : & toml :: Table , error_sink : & mut Vec < (String , toml :: de :: Error) >) -> Self { Self { $ ($ field : get_field_toml ::<$ ty > (toml , error_sink , stringify ! ($ field) , None $ (. or (Some (stringify ! ($ alias)))) *,) ,) * } } fn schema_fields (sink : & mut Vec < SchemaField >) { sink . extend_from_slice (& [$ ({ let field = stringify ! ($ field) ; let ty = stringify ! ($ ty) ; let default = default_str ! ($ default , $ ty) ; (field , ty , & [$ ($ doc) ,*] , default) } ,) *]) } } mod $ modname { # [test] fn fields_are_sorted () { super ::$ input :: FIELDS . windows (2) . for_each (| w | assert ! (w [0] <= w [1] , "{} <= {} does not hold" , w [0] , w [1])) ; } } } ; }
};
}
