// Generated macro for GlobalState (struct)
macro_rules! Depcrate_global_stateGlobalState {
() => {
// Module: crate::global_state
// Provides: {"GlobalState"}
// Dependencies: {}
# [doc = " `GlobalState` is the primary mutable state of the language server"] # [doc = ""] # [doc = " The most interesting components are `vfs`, which stores a consistent"] # [doc = " snapshot of the file systems, and `analysis_host`, which stores our"] # [doc = " incremental salsa database."] # [doc = ""] # [doc = " Note that this struct has more than one impl in various modules!"] # [doc (alias = "GlobalMess")] pub (crate) struct GlobalState { sender : Sender < lsp_server :: Message > , req_queue : ReqQueue , pub (crate) task_pool : Handle < TaskPool < Task > , Receiver < Task > > , pub (crate) fmt_pool : Handle < TaskPool < Task > , Receiver < Task > > , pub (crate) cancellation_pool : thread :: Pool , pub (crate) config : Arc < Config > , pub (crate) config_errors : Option < ConfigErrors > , pub (crate) analysis_host : AnalysisHost , pub (crate) diagnostics : DiagnosticCollection , pub (crate) mem_docs : MemDocs , pub (crate) source_root_config : SourceRootConfig , # [doc = " A mapping that maps a local source root's `SourceRootId` to it parent's `SourceRootId`, if it has one."] pub (crate) local_roots_parent_map : Arc < FxHashMap < SourceRootId , SourceRootId > > , pub (crate) semantic_tokens_cache : Arc < Mutex < FxHashMap < Url , SemanticTokens > > > , pub (crate) shutdown_requested : bool , pub (crate) last_reported_status : lsp_ext :: ServerStatusParams , pub (crate) proc_macro_clients : Arc < [Option < anyhow :: Result < ProcMacroClient > >] > , pub (crate) build_deps_changed : bool , pub (crate) flycheck : Arc < [FlycheckHandle] > , pub (crate) flycheck_sender : Sender < FlycheckMessage > , pub (crate) flycheck_receiver : Receiver < FlycheckMessage > , pub (crate) last_flycheck_error : Option < String > , pub (crate) test_run_session : Option < Vec < CargoTestHandle > > , pub (crate) test_run_sender : Sender < CargoTestMessage > , pub (crate) test_run_receiver : Receiver < CargoTestMessage > , pub (crate) test_run_remaining_jobs : usize , pub (crate) discover_handle : Option < discover :: DiscoverHandle > , pub (crate) discover_sender : Sender < discover :: DiscoverProjectMessage > , pub (crate) discover_receiver : Receiver < discover :: DiscoverProjectMessage > , pub (crate) fetch_ws_receiver : Option < (Receiver < Instant > , FetchWorkspaceRequest) > , pub (crate) loader : Handle < Box < dyn vfs :: loader :: Handle > , Receiver < vfs :: loader :: Message > > , pub (crate) vfs : Arc < RwLock < (vfs :: Vfs , FxHashMap < FileId , LineEndings >) > > , pub (crate) vfs_config_version : u32 , pub (crate) vfs_progress_config_version : u32 , pub (crate) vfs_done : bool , pub (crate) vfs_span : Option < tracing :: span :: EnteredSpan > , pub (crate) wants_to_switch : Option < Cause > , # [doc = " `workspaces` field stores the data we actually use, while the `OpQueue`"] # [doc = " stores the result of the last fetch."] # [doc = ""] # [doc = " If the fetch (partially) fails, we do not update the current value."] # [doc = ""] # [doc = " The handling of build data is subtle. We fetch workspace in two phases:"] # [doc = ""] # [doc = " *First*, we run `cargo metadata`, which gives us fast results for"] # [doc = " initial analysis."] # [doc = ""] # [doc = " *Second*, we run `cargo check` which runs build scripts and compiles"] # [doc = " proc macros."] # [doc = ""] # [doc = " We need both for the precise analysis, but we want rust-analyzer to be"] # [doc = " at least partially available just after the first phase. That's because"] # [doc = " first phase is much faster, and is much less likely to fail."] # [doc = ""] # [doc = " This creates a complication -- by the time the second phase completes,"] # [doc = " the results of the first phase could be invalid. That is, while we run"] # [doc = " `cargo check`, the user edits `Cargo.toml`, we notice this, and the new"] # [doc = " `cargo metadata` completes before `cargo check`."] # [doc = ""] # [doc = " An additional complication is that we want to avoid needless work. When"] # [doc = " the user just adds comments or whitespace to Cargo.toml, we do not want"] # [doc = " to invalidate any salsa caches."] pub (crate) workspaces : Arc < Vec < ProjectWorkspace > > , pub (crate) crate_graph_file_dependencies : FxHashSet < vfs :: VfsPath > , pub (crate) detached_files : FxHashSet < ManifestPath > , pub (crate) fetch_workspaces_queue : OpQueue < FetchWorkspaceRequest , FetchWorkspaceResponse > , pub (crate) fetch_build_data_queue : OpQueue < () , FetchBuildDataResponse > , pub (crate) fetch_proc_macros_queue : OpQueue < (ChangeWithProcMacros , Vec < ProcMacroPaths >) , bool > , pub (crate) prime_caches_queue : OpQueue , pub (crate) discover_workspace_queue : OpQueue , # [doc = " A deferred task queue."] # [doc = ""] # [doc = " This queue is used for doing database-dependent work inside of sync"] # [doc = " handlers, as accessing the database may block latency-sensitive"] # [doc = " interactions and should be moved away from the main thread."] # [doc = ""] # [doc = " For certain features, such as [`GlobalState::handle_discover_msg`],"] # [doc = " this queue should run only *after* [`GlobalState::process_changes`] has"] # [doc = " been called."] pub (crate) deferred_task_queue : TaskQueue , # [doc = " HACK: Workaround for https://github.com/rust-lang/rust-analyzer/issues/19709"] # [doc = " This is marked true if we failed to load a crate root file at crate graph creation,"] # [doc = " which will usually end up causing a bunch of incorrect diagnostics on startup."] pub (crate) incomplete_crate_graph : bool , pub (crate) minicore : MiniCoreRustAnalyzerInternalOnly , }
};
}
