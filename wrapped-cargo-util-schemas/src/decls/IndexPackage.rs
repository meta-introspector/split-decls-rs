macro_rules! deps {
    () => {
        RegistryDependency!();
        RustVersion!();
    };
}

macro_rules! IndexPackage {
    () => {
        deps!();
        # [doc = " A single line in the index representing a single version of a package."] # [derive (Deserialize , Serialize)] # [cfg_attr (feature = "unstable-schema" , derive (schemars :: JsonSchema))] pub struct IndexPackage < 'a > { # [doc = " Name of the package."] # [serde (borrow)] pub name : Cow < 'a , str > , # [doc = " The version of this dependency."] pub vers : Version , # [doc = " All kinds of direct dependencies of the package, including dev and"] # [doc = " build dependencies."] # [serde (borrow)] pub deps : Vec < RegistryDependency < 'a > > , # [doc = " Set of features defined for the package, i.e., `[features]` table."] # [serde (default)] pub features : BTreeMap < Cow < 'a , str > , Vec < Cow < 'a , str > > > , # [doc = " This field contains features with new, extended syntax. Specifically,"] # [doc = " namespaced features (`dep:`) and weak dependencies (`pkg?/feat`)."] # [doc = ""] # [doc = " This is separated from `features` because versions older than 1.19"] # [doc = " will fail to load due to not being able to parse the new syntax, even"] # [doc = " with a `Cargo.lock` file."] pub features2 : Option < BTreeMap < Cow < 'a , str > , Vec < Cow < 'a , str > > > > , # [doc = " Checksum for verifying the integrity of the corresponding downloaded package."] pub cksum : String , # [doc = " If `true`, Cargo will skip this version when resolving."] # [doc = ""] # [doc = " This was added in 2014. Everything in the crates.io index has this set"] # [doc = " now, so this probably doesn't need to be an option anymore."] pub yanked : Option < bool > , # [doc = " Native library name this package links to."] # [doc = ""] # [doc = " Added early 2018 (see <https://github.com/rust-lang/cargo/pull/4978>),"] # [doc = " can be `None` if published before then."] pub links : Option < Cow < 'a , str > > , # [doc = " Required version of rust"] # [doc = ""] # [doc = " Corresponds to `package.rust-version`."] # [doc = ""] # [doc = " Added in 2023 (see <https://github.com/rust-lang/crates.io/pull/6267>),"] # [doc = " can be `None` if published before then or if not set in the manifest."] # [cfg_attr (feature = "unstable-schema" , schemars (with = "Option<String>"))] pub rust_version : Option < RustVersion > , # [doc = " The publish time for the package.  Unstable."] # [doc = ""] # [doc = " In ISO8601 with UTC timezone (e.g. 2025-11-12T19:30:12Z)"] pub pubtime : Option < String > , # [doc = " The schema version for this entry."] # [doc = ""] # [doc = " If this is None, it defaults to version `1`. Entries with unknown"] # [doc = " versions are ignored."] # [doc = ""] # [doc = " Version `2` schema adds the `features2` field."] # [doc = ""] # [doc = " Version `3` schema adds `artifact`, `bindep_targes`, and `lib` for"] # [doc = " artifact dependencies support."] # [doc = ""] # [doc = " This provides a method to safely introduce changes to index entries"] # [doc = " and allow older versions of cargo to ignore newer entries it doesn't"] # [doc = " understand. This is honored as of 1.51, so unfortunately older"] # [doc = " versions will ignore it, and potentially misinterpret version 2 and"] # [doc = " newer entries."] # [doc = ""] # [doc = " The intent is that versions older than 1.51 will work with a"] # [doc = " pre-existing `Cargo.lock`, but they may not correctly process `cargo"] # [doc = " update` or build a lock from scratch. In that case, cargo may"] # [doc = " incorrectly select a new package that uses a new index schema. A"] # [doc = " workaround is to downgrade any packages that are incompatible with the"] # [doc = " `--precise` flag of `cargo update`."] pub v : Option < u32 > , }
    };
}

IndexPackage!();