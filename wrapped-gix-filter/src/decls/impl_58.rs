macro_rules! deps {
    () => {
        Operation!();
        Key!();
        Error!();
        State!();
    };
}

macro_rules! impl_58 {
    () => {
        deps!();
        # [doc = " Operations related to delayed filtering."] impl State { # [doc = " Return a list of delayed paths for `process` that can then be obtained with [`fetch_delayed()`][Self::fetch_delayed()]."] # [doc = ""] # [doc = " A process abiding the protocol will eventually list all previously delayed paths for any invoked command, or"] # [doc = " signals that it is done with all delayed paths by returning an empty list."] # [doc = " It's up to the caller to validate these assumptions."] # [doc = ""] # [doc = " ### Error Handling"] # [doc = ""] # [doc = " Usually if the process sends the \"abort\" status, we will not use a certain capability again. Here it's unclear what capability"] # [doc = " that is and what to do, so we leave the process running and do nothing else (just like `git`)."] pub fn list_delayed_paths (& mut self , process : & driver :: Key) -> Result < Vec < BString > , list :: Error > { let client = self . running . get_mut (& process . 0) . ok_or_else (| | list :: Error :: ProcessMissing { wanted : process . clone () , }) ? ; let mut out = Vec :: new () ; let result = client . invoke_without_content ("list_available_blobs" , & mut None . into_iter () , & mut | line | { if let Some (path) = line . strip_prefix (b"pathname=") { out . push (path . into ()) ; } }) ; let status = match result { Ok (res) => res , Err (err) => { if let driver :: process :: client :: invoke :: without_content :: Error :: Io (err) = & err { handle_io_err (err , & mut self . running , process . 0 . as_ref ()) ; } return Err (err . into ()) ; } } ; if status . is_success () { Ok (out) } else { let message = status . message () . unwrap_or_default () ; match message { "error" | "abort" => { } _strange => { let client = self . running . remove (& process . 0) . expect ("we definitely have it") ; client . into_child () . kill () . ok () ; } } Err (list :: Error :: ProcessStatus { status }) } } # [doc = " Given a `process` and a `path`  (as previously returned by [list_delayed_paths()][Self::list_delayed_paths()]), return"] # [doc = " a reader to stream the filtered result. Note that `operation` must match the original operation that produced the delayed result"] # [doc = " or the long-running process might not know the path, depending on its implementation."] pub fn fetch_delayed (& mut self , process : & driver :: Key , path : & BStr , operation : Operation ,) -> Result < impl std :: io :: Read + '_ , fetch :: Error > { let client = self . running . get_mut (& process . 0) . ok_or_else (| | fetch :: Error :: ProcessMissing { wanted : process . clone () , }) ? ; let result = client . invoke (operation . as_str () , & mut [("pathname" , path . to_owned ())] . into_iter () , & mut & b"" [..] ,) ; let status = match result { Ok (status) => status , Err (err) => { let driver :: process :: client :: invoke :: Error :: Io (io_err) = & err ; handle_io_err (io_err , & mut self . running , process . 0 . as_ref ()) ; return Err (fetch :: Error :: ProcessInvoke { command : operation . as_str () . into () , source : err , }) ; } } ; if status . is_success () { let client = self . running . remove (& process . 0) . expect ("present for borrowcheck dance") ; self . running . insert (process . 0 . clone () , client) ; let client = self . running . get_mut (& process . 0) . expect ("just inserted") ; Ok (client . as_read ()) } else { let message = status . message () . unwrap_or_default () ; match message { "abort" => { client . capabilities_mut () . remove (operation . as_str ()) ; } "error" => { } _strange => { let client = self . running . remove (& process . 0) . expect ("we definitely have it") ; client . into_child () . kill () . ok () ; } } Err (fetch :: Error :: ProcessStatus { command : operation . as_str () . into () , status , }) } } }
    };
}

impl_58!();