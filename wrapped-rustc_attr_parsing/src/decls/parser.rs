macro_rules! parser {
    () => {
        # [doc = " Despite this entire module called attribute parsing and the term being a little overloaded,"] # [doc = " in this module the code lives that actually breaks up tokenstreams into semantic pieces of attributes,"] # [doc = " like lists or name-value pairs."] pub mod parser ;
    };
}

parser!();