macro_rules! deps {
    () => {
        ObjectId!();
        Error!();
        Prefix!();
        Kind!();
    };
}

macro_rules! impl_26 {
    () => {
        deps!();
        impl Prefix { # [doc = " The smallest allowed prefix length below which chances for collisions are too high even in small repositories."] pub const MIN_HEX_LEN : usize = 4 ; # [doc = " Create a new instance by taking a full `id` as input and truncating it to `hex_len`."] # [doc = ""] # [doc = " For instance, with `hex_len` of 7 the resulting prefix is 3.5 bytes, or 3 bytes and 4 bits"] # [doc = " wide, with all other bytes and bits set to zero."] pub fn new (id : & oid , hex_len : usize) -> Result < Self , Error > { if hex_len > id . kind () . len_in_hex () { Err (Error :: TooLong { object_kind : id . kind () , hex_len , }) } else if hex_len < Self :: MIN_HEX_LEN { Err (Error :: TooShort { hex_len }) } else { let mut prefix = ObjectId :: null (id . kind ()) ; let b = prefix . as_mut_slice () ; let copy_len = hex_len . div_ceil (2) ; b [.. copy_len] . copy_from_slice (& id . as_bytes () [.. copy_len]) ; if hex_len % 2 == 1 { b [hex_len / 2] &= 0xf0 ; } Ok (Prefix { bytes : prefix , hex_len }) } } # [doc = " Returns the prefix as object id."] # [doc = ""] # [doc = " Note that it may be deceptive to use given that it looks like a full"] # [doc = " object id, even though its post-prefix bytes/bits are set to zero."] pub fn as_oid (& self) -> & oid { & self . bytes } # [doc = " Return the amount of hexadecimal characters that are set in the prefix."] # [doc = ""] # [doc = " This gives the prefix a granularity of 4 bits."] pub fn hex_len (& self) -> usize { self . hex_len } # [doc = " Provided with candidate id which is a full hash, determine how this prefix compares to it,"] # [doc = " only looking at the prefix bytes, ignoring everything behind that."] pub fn cmp_oid (& self , candidate : & oid) -> Ordering { let common_len = self . hex_len / 2 ; self . bytes . as_bytes () [.. common_len] . cmp (& candidate . as_bytes () [.. common_len]) . then (if self . hex_len % 2 == 1 { let half_byte_idx = self . hex_len / 2 ; self . bytes . as_bytes () [half_byte_idx] . cmp (& (candidate . as_bytes () [half_byte_idx] & 0xf0)) } else { Ordering :: Equal }) } # [doc = " Create an instance from the given hexadecimal prefix `value`, e.g. `35e77c16` would yield a `Prefix` with `hex_len()` = 8."] pub fn from_hex (value : & str) -> Result < Self , from_hex :: Error > { let hex_len = value . len () ; if hex_len > crate :: Kind :: longest () . len_in_hex () { return Err (from_hex :: Error :: TooLong { hex_len }) ; } else if hex_len < Self :: MIN_HEX_LEN { return Err (from_hex :: Error :: TooShort { hex_len }) ; } let src = if value . len () % 2 == 0 { let mut out = Vec :: from_iter (std :: iter :: repeat_n (0 , value . len () / 2)) ; faster_hex :: hex_decode (value . as_bytes () , & mut out) . map (move | _ | out) } else { let mut buf = [0u8 ; crate :: Kind :: longest () . len_in_hex ()] ; buf [.. value . len ()] . copy_from_slice (value . as_bytes ()) ; buf [value . len ()] = b'0' ; let src = & buf [..= value . len ()] ; let mut out = Vec :: from_iter (std :: iter :: repeat_n (0 , src . len () / 2)) ; faster_hex :: hex_decode (src , & mut out) . map (move | _ | out) } . map_err (| e | match e { faster_hex :: Error :: InvalidChar | faster_hex :: Error :: Overflow => from_hex :: Error :: Invalid , faster_hex :: Error :: InvalidLength (_) => panic ! ("This is already checked") , }) ? ; let mut bytes = ObjectId :: null (crate :: Kind :: from_hex_len (value . len ()) . expect ("hex-len is already checked")) ; let dst = bytes . as_mut_slice () ; let copy_len = src . len () ; dst [.. copy_len] . copy_from_slice (& src) ; Ok (Prefix { bytes , hex_len }) } }
    };
}

impl_26!();