// Generated macro for impl_10 (impl)
macro_rules! Depcrateimpl_10 {
() => {
// Module: crate
// Provides: {"impl_10"}
// Dependencies: {}
impl Args { # [doc = " Determines whether the given `Expr` is a path referring to one of the"] # [doc = " variables we intend to print. Expressions are used as the left-hand side"] # [doc = " of the assignment operator."] fn should_print_expr (& self , e : & Expr) -> bool { match * e { Expr :: Path (ref e) => { if e . path . leading_colon . is_some () { false } else if e . path . segments . len () != 1 { false } else { let first = e . path . segments . first () . unwrap () ; self . vars . contains (& first . ident) && first . arguments . is_empty () } } _ => false , } } # [doc = " Determines whether the given `Pat` is an identifier equal to one of the"] # [doc = " variables we intend to print. Patterns are used as the left-hand side of"] # [doc = " a `let` binding."] fn should_print_pat (& self , p : & Pat) -> bool { match p { Pat :: Ident (ref p) => self . vars . contains (& p . ident) , _ => false , } } # [doc = " Produces an expression that assigns the right-hand side to the left-hand"] # [doc = " side and then prints the value."] # [doc = ""] # [doc = "     // Before"] # [doc = "     VAR = INIT"] # [doc = ""] # [doc = "     // After"] # [doc = "     { VAR = INIT; println!(\"VAR = {:?}\", VAR); }"] fn assign_and_print (& mut self , left : Expr , op : & dyn ToTokens , right : Expr) -> Expr { let right = fold :: fold_expr (self , right) ; parse_quote ! ({ # left # op # right ; println ! (concat ! (stringify ! (# left) , " = {:?}") , # left) ; }) } # [doc = " Produces a let-binding that assigns the right-hand side to the left-hand"] # [doc = " side and then prints the value."] # [doc = ""] # [doc = "     // Before"] # [doc = "     let VAR = INIT;"] # [doc = ""] # [doc = "     // After"] # [doc = "     let VAR = { let VAR = INIT; println!(\"VAR = {:?}\", VAR); VAR };"] fn let_and_print (& mut self , local : Local) -> Stmt { let Local { pat , init , .. } = local ; let init = self . fold_expr (* init . unwrap () . expr) ; let ident = match pat { Pat :: Ident (ref p) => & p . ident , _ => unreachable ! () , } ; parse_quote ! { let # pat = { # [allow (unused_mut)] let # pat = # init ; println ! (concat ! (stringify ! (# ident) , " = {:?}") , # ident) ; # ident } ; } } }
};
}
