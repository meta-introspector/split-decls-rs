// Generated macro for HalfBitsSliceExt (trait)
macro_rules! Depcrate_sliceHalfBitsSliceExt {
() => {
// Module: crate::slice
// Provides: {"HalfBitsSliceExt"}
// Dependencies: {}
# [doc = " Extensions to `[u16]` slices to support reinterpret operations."] # [doc = ""] # [doc = " This trait is sealed and cannot be implemented outside of this crate."] pub trait HalfBitsSliceExt : private :: SealedHalfBitsSlice { # [doc = " Reinterprets a slice of [`u16`] bits as a slice of [`struct@f16`] or [`struct@bf16`] numbers."] # [doc = ""] # [doc = " `H` is the type to cast to, and must be either the [`struct@f16`] or [`struct@bf16`] type."] # [doc = ""] # [doc = " This is a zero-copy operation. The reinterpreted slice has the same lifetime and memory"] # [doc = " location as `self`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use half::prelude::*;"] # [doc = " let int_buffer = [f16::from_f32(1.).to_bits(), f16::from_f32(2.).to_bits(), f16::from_f32(3.).to_bits()];"] # [doc = " let float_buffer: &[f16] = int_buffer.reinterpret_cast();"] # [doc = ""] # [doc = " assert_eq!(float_buffer, [f16::from_f32(1.), f16::from_f32(2.), f16::from_f32(3.)]);"] # [doc = ""] # [doc = " // You may have to specify the cast type directly if the compiler can't infer the type."] # [doc = " // The following is also valid in Rust."] # [doc = " let typed_buffer = int_buffer.reinterpret_cast::<f16>();"] # [doc = " ```"] # [must_use] fn reinterpret_cast < H > (& self) -> & [H] where H : crate :: private :: SealedHalf ; # [doc = " Reinterprets a mutable slice of [`u16`] bits as a mutable slice of [`struct@f16`] or [`struct@bf16`]"] # [doc = " numbers."] # [doc = ""] # [doc = " `H` is the type to cast to, and must be either the [`struct@f16`] or [`struct@bf16`] type."] # [doc = ""] # [doc = " This is a zero-copy operation. The transmuted slice has the same lifetime as the original,"] # [doc = " which prevents mutating `self` as long as the returned `&mut [f16]` is borrowed."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use half::prelude::*;"] # [doc = " let mut int_buffer = [f16::from_f32(1.).to_bits(), f16::from_f32(2.).to_bits(), f16::from_f32(3.).to_bits()];"] # [doc = ""] # [doc = " {"] # [doc = "     let float_buffer: &mut [f16] = int_buffer.reinterpret_cast_mut();"] # [doc = ""] # [doc = "     assert_eq!(float_buffer, [f16::from_f32(1.), f16::from_f32(2.), f16::from_f32(3.)]);"] # [doc = ""] # [doc = "     // Mutating the f16 slice will mutating the original"] # [doc = "     float_buffer[0] = f16::from_f32(0.);"] # [doc = " }"] # [doc = ""] # [doc = " // Note that we need to drop float_buffer before using int_buffer again or we will get a borrow error."] # [doc = " assert_eq!(int_buffer, [f16::from_f32(0.).to_bits(), f16::from_f32(2.).to_bits(), f16::from_f32(3.).to_bits()]);"] # [doc = ""] # [doc = " // You may have to specify the cast type directly if the compiler can't infer the type."] # [doc = " // The following is also valid in Rust."] # [doc = " let typed_buffer = int_buffer.reinterpret_cast_mut::<f16>();"] # [doc = " ```"] # [must_use] fn reinterpret_cast_mut < H > (& mut self) -> & mut [H] where H : crate :: private :: SealedHalf ; }
};
}
