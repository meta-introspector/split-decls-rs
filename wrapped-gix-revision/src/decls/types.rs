macro_rules! types {
    () => {
        pub (crate) mod types { # [doc = " A revision specification without any bindings to a repository, useful for serialization or movement over thread boundaries."] # [doc = ""] # [doc = " Note that all [object ids][gix_hash::ObjectId] should be a committish, but don't have to be."] # [doc = " Unless the field name contains `_exclusive`, the respective objects are included in the set."] # [derive (Copy , Clone , Debug , Ord , PartialOrd , Eq , PartialEq , Hash)] # [cfg_attr (feature = "serde" , derive (serde :: Serialize , serde :: Deserialize))] pub enum Spec { # [doc = " Include commits reachable from this revision, i.e. `a` and its ancestors."] # [doc = ""] # [doc = " The equivalent to [crate::spec::Kind::IncludeReachable], but with data."] Include (gix_hash :: ObjectId) , # [doc = " Exclude commits reachable from this revision, i.e. `a` and its ancestors. Example: `^a`."] # [doc = ""] # [doc = " The equivalent to [crate::spec::Kind::ExcludeReachable], but with data."] Exclude (gix_hash :: ObjectId) , # [doc = " Every commit that is reachable from `from` to `to`, but not any ancestors of `from`. Example: `from..to`."] # [doc = ""] # [doc = " The equivalent to [crate::spec::Kind::RangeBetween], but with data."] Range { # [doc = " The starting point of the range, which is included in the set."] from : gix_hash :: ObjectId , # [doc = " The end point of the range, which is included in the set."] to : gix_hash :: ObjectId , } , # [doc = " Every commit reachable through either `theirs` or `ours`, but no commit that is reachable by both. Example: `theirs...ours`."] # [doc = ""] # [doc = " The equivalent to [crate::spec::Kind::ReachableToMergeBase], but with data."] Merge { # [doc = " Their side of the merge, which is included in the set."] theirs : gix_hash :: ObjectId , # [doc = " Our side of the merge, which is included in the set."] ours : gix_hash :: ObjectId , } , # [doc = " Include every commit of all parents of `a`, but not `a` itself. Example: `a^@`."] # [doc = ""] # [doc = " The equivalent to [crate::spec::Kind::IncludeReachableFromParents], but with data."] IncludeOnlyParents (# [doc = " Include only the parents of this object, but not the object itself."] gix_hash :: ObjectId ,) , # [doc = " Exclude every commit of all parents of `a`, but not `a` itself. Example: `a^!`."] # [doc = ""] # [doc = " The equivalent to [crate::spec::Kind::ExcludeReachableFromParents], but with data."] ExcludeParents (# [doc = " Exclude the parents of this object, but not the object itself."] gix_hash :: ObjectId ,) , } }
    };
}

types!();