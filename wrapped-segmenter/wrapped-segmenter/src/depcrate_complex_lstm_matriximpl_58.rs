// Generated macro for impl_58 (impl)
macro_rules! Depcrate_complex_lstm_matriximpl_58 {
() => {
// Module: crate::complex::lstm::matrix
// Provides: {"impl_58"}
// Dependencies: {}
impl < const D : usize > MatrixBorrowedMut < '_ , D > { pub (super) fn as_borrowed (& self) -> MatrixBorrowed < '_ , D > { MatrixBorrowed { data : self . data , dims : self . dims , } } pub (super) fn as_mut_slice (& mut self) -> & mut [f32] { self . data } pub (super) fn copy_submatrix < const M : usize > (& mut self , from : usize , to : usize) { let (range_from , _) = self . as_borrowed () . submatrix_range :: < M > (from) ; let (range_to , _) = self . as_borrowed () . submatrix_range :: < M > (to) ; if let (Some (_) , Some (_)) = (self . data . get (range_from . clone ()) , self . data . get (range_to . clone ()) ,) { self . data . copy_within (range_from , range_to . start) ; } } # [must_use] pub (super) fn add (& mut self , other : MatrixZero < '_ , D >) -> Option < () > { debug_assert_eq ! (self . dims , other . dims) ; for i in 0 .. self . data . len () { * self . data . get_mut (i) ? += other . data . get (i) ? ; } Some (()) } # [allow (dead_code)] # [doc = " Mutates this matrix by applying a softmax transformation."] pub (super) fn softmax_transform (& mut self) { for v in self . data . iter_mut () { * v = v . exp () ; } let sm = 1.0 / self . data . iter () . sum :: < f32 > () ; for v in self . data . iter_mut () { * v *= sm ; } } pub (super) fn sigmoid_transform (& mut self) { for x in & mut self . data . iter_mut () { * x = 1.0 / (1.0 + (- * x) . exp ()) ; } } pub (super) fn tanh_transform (& mut self) { for x in & mut self . data . iter_mut () { * x = x . tanh () ; } } pub (super) fn convolve (& mut self , i : MatrixBorrowed < '_ , D > , c : MatrixBorrowed < '_ , D > , f : MatrixBorrowed < '_ , D > ,) { let i = i . as_slice () ; let c = c . as_slice () ; let f = f . as_slice () ; let len = self . data . len () ; if len != i . len () || len != c . len () || len != f . len () { debug_assert ! (false , "LSTM matrices not the correct dimensions") ; return ; } for idx in 0 .. len { unsafe { * self . data . get_unchecked_mut (idx) = i . get_unchecked (idx) * c . get_unchecked (idx) + self . data . get_unchecked (idx) * f . get_unchecked (idx) } } } pub (super) fn mul_tanh (& mut self , o : MatrixBorrowed < '_ , D > , c : MatrixBorrowed < '_ , D >) { let o = o . as_slice () ; let c = c . as_slice () ; let len = self . data . len () ; if len != o . len () || len != c . len () { debug_assert ! (false , "LSTM matrices not the correct dimensions") ; return ; } for idx in 0 .. len { unsafe { * self . data . get_unchecked_mut (idx) = o . get_unchecked (idx) * c . get_unchecked (idx) . tanh () ; } } } }
};
}
