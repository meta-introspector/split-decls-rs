macro_rules! deps {
    () => {
        DeInteger!();
        DeFloat!();
        DeTable!();
        Error!();
        TomlSink!();
        Array!();
        DeValue!();
        Table!();
        Index!();
        DeArray!();
    };
}

macro_rules! impl_222 {
    () => {
        deps!();
        impl < 'i > DeValue < 'i > { # [doc = " Parse a TOML value"] pub fn parse (input : & 'i str) -> Result < Spanned < Self > , crate :: de :: Error > { let source = toml_parser :: Source :: new (input) ; let mut errors = crate :: de :: error :: TomlSink :: < Option < _ > > :: new (source) ; let value = crate :: de :: parser :: parse_value (source , & mut errors) ; if let Some (err) = errors . into_inner () { Err (err) } else { Ok (value) } } # [doc = " Parse a TOML value, with best effort recovery on error"] pub fn parse_recoverable (input : & 'i str) -> (Spanned < Self > , Vec < crate :: de :: Error >) { let source = toml_parser :: Source :: new (input) ; let mut errors = crate :: de :: error :: TomlSink :: < Vec < _ > > :: new (source) ; let value = crate :: de :: parser :: parse_value (source , & mut errors) ; (value , errors . into_inner ()) } # [doc = " Ensure no data is borrowed"] pub fn make_owned (& mut self) { match self { DeValue :: String (v) => { let owned = core :: mem :: take (v) ; * v = Cow :: Owned (owned . into_owned ()) ; } DeValue :: Integer (..) | DeValue :: Float (..) | DeValue :: Boolean (..) | DeValue :: Datetime (..) => { } DeValue :: Array (v) => { for e in v . iter_mut () { e . get_mut () . make_owned () ; } } DeValue :: Table (v) => v . make_owned () , } } # [doc = " Index into a TOML array or map. A string index can be used to access a"] # [doc = " value in a map, and a usize index can be used to access an element of an"] # [doc = " array."] # [doc = ""] # [doc = " Returns `None` if the type of `self` does not match the type of the"] # [doc = " index, for example if the index is a string and `self` is an array or a"] # [doc = " number. Also returns `None` if the given key does not exist in the map"] # [doc = " or the given index is not within the bounds of the array."] pub fn get < I : Index > (& self , index : I) -> Option < & Spanned < Self > > { index . index (self) } # [doc = " Extracts the integer value if it is an integer."] pub fn as_integer (& self) -> Option < & DeInteger < 'i > > { match self { DeValue :: Integer (i) => Some (i) , _ => None , } } # [doc = " Tests whether this value is an integer."] pub fn is_integer (& self) -> bool { self . as_integer () . is_some () } # [doc = " Extracts the float value if it is a float."] pub fn as_float (& self) -> Option < & DeFloat < 'i > > { match self { DeValue :: Float (f) => Some (f) , _ => None , } } # [doc = " Tests whether this value is a float."] pub fn is_float (& self) -> bool { self . as_float () . is_some () } # [doc = " Extracts the boolean value if it is a boolean."] pub fn as_bool (& self) -> Option < bool > { match * self { DeValue :: Boolean (b) => Some (b) , _ => None , } } # [doc = " Tests whether this value is a boolean."] pub fn is_bool (& self) -> bool { self . as_bool () . is_some () } # [doc = " Extracts the string of this value if it is a string."] pub fn as_str (& self) -> Option < & str > { match * self { DeValue :: String (ref s) => Some (& * * s) , _ => None , } } # [doc = " Tests if this value is a string."] pub fn is_str (& self) -> bool { self . as_str () . is_some () } # [doc = " Extracts the datetime value if it is a datetime."] # [doc = ""] # [doc = " Note that a parsed TOML value will only contain ISO 8601 dates. An"] # [doc = " example date is:"] # [doc = ""] # [doc = " ```notrust"] # [doc = " 1979-05-27T07:32:00Z"] # [doc = " ```"] pub fn as_datetime (& self) -> Option < & Datetime > { match * self { DeValue :: Datetime (ref s) => Some (s) , _ => None , } } # [doc = " Tests whether this value is a datetime."] pub fn is_datetime (& self) -> bool { self . as_datetime () . is_some () } # [doc = " Extracts the array value if it is an array."] pub fn as_array (& self) -> Option < & DeArray < 'i > > { match * self { DeValue :: Array (ref s) => Some (s) , _ => None , } } pub (crate) fn as_array_mut (& mut self) -> Option < & mut DeArray < 'i > > { match self { DeValue :: Array (s) => Some (s) , _ => None , } } # [doc = " Tests whether this value is an array."] pub fn is_array (& self) -> bool { self . as_array () . is_some () } # [doc = " Extracts the table value if it is a table."] pub fn as_table (& self) -> Option < & DeTable < 'i > > { match * self { DeValue :: Table (ref s) => Some (s) , _ => None , } } pub (crate) fn as_table_mut (& mut self) -> Option < & mut DeTable < 'i > > { match self { DeValue :: Table (s) => Some (s) , _ => None , } } # [doc = " Tests whether this value is a table."] pub fn is_table (& self) -> bool { self . as_table () . is_some () } # [doc = " Tests whether this and another value have the same type."] pub fn same_type (& self , other : & DeValue < '_ >) -> bool { discriminant (self) == discriminant (other) } # [doc = " Returns a human-readable representation of the type of this value."] pub fn type_str (& self) -> & 'static str { match * self { DeValue :: String (..) => "string" , DeValue :: Integer (..) => "integer" , DeValue :: Float (..) => "float" , DeValue :: Boolean (..) => "boolean" , DeValue :: Datetime (..) => "datetime" , DeValue :: Array (..) => "array" , DeValue :: Table (..) => "table" , } } }
    };
}

impl_222!()