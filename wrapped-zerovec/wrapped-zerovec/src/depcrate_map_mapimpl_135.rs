// Generated macro for impl_135 (impl)
macro_rules! Depcrate_map_mapimpl_135 {
() => {
// Module: crate::map::map
// Provides: {"impl_135"}
// Dependencies: {}
impl < 'a , K , V > ZeroMap < 'a , K , V > where K : ZeroMapKV < 'a > + ? Sized + Ord , V : Copy + ZeroMapKV < 'a > , { # [doc = " For cases when `V` is fixed-size, obtain a direct copy of `V` instead of `V::ULE`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap;"] # [doc = ""] # [doc = " let mut map = ZeroMap::new();"] # [doc = " map.insert(&1, &'a');"] # [doc = " map.insert(&2, &'b');"] # [doc = " assert_eq!(map.get_copied(&1), Some('a'));"] # [doc = " assert_eq!(map.get_copied(&3), None);"] # [inline] pub fn get_copied (& self , key : & K) -> Option < V > { let index = self . keys . zvl_binary_search (key) . ok () ? ; self . get_copied_at (index) } # [doc = " Binary search the map with `predicate` to find a key, returning the value."] # [doc = ""] # [doc = " For cases when `V` is fixed-size, use this method to obtain a direct copy of `V`"] # [doc = " instead of `V::ULE`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap;"] # [doc = ""] # [doc = " let mut map = ZeroMap::new();"] # [doc = " map.insert(&1, &'a');"] # [doc = " map.insert(&2, &'b');"] # [doc = " assert_eq!(map.get_copied_by(|probe| probe.cmp(&1)), Some('a'));"] # [doc = " assert_eq!(map.get_copied_by(|probe| probe.cmp(&3)), None);"] # [doc = " ```"] # [inline] pub fn get_copied_by (& self , predicate : impl FnMut (& K) -> Ordering) -> Option < V > { let index = self . keys . zvl_binary_search_by (predicate) . ok () ? ; self . get_copied_at (index) } fn get_copied_at (& self , index : usize) -> Option < V > { let ule = self . values . zvl_get (index) ? ; let mut result = Option :: < V > :: None ; V :: Container :: zvl_get_as_t (ule , | v | result . replace (* v)) ; # [expect (clippy :: unwrap_used)] Some (result . unwrap ()) } }
};
}
