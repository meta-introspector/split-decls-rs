// Generated macro for impl_562 (impl)
macro_rules! Depcrate_zerovecimpl_562 {
() => {
// Module: crate::zerovec
// Provides: {"impl_562"}
// Dependencies: {}
impl < 'a , T > ZeroVec < 'a , T > where T : AsULE , { # [doc = " Mutates each element according to a given function, meant to be"] # [doc = " a more convenient version of calling `.iter_mut()` with"] # [doc = " [`ZeroVec::with_mut()`] which serves fewer use cases."] # [doc = ""] # [doc = " This will convert the ZeroVec into an owned ZeroVec if not already the case."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use zerovec::ZeroVec;"] # [doc = ""] # [doc = " let bytes: &[u8] = &[0xD3, 0x00, 0x19, 0x01, 0xA5, 0x01, 0xCD, 0x01];"] # [doc = " let mut zerovec: ZeroVec<u16> ="] # [doc = "     ZeroVec::parse_bytes(bytes).expect(\"infallible\");"] # [doc = ""] # [doc = " zerovec.for_each_mut(|item| *item += 1);"] # [doc = ""] # [doc = " assert_eq!(zerovec.to_vec(), &[212, 282, 422, 462]);"] # [doc = " assert!(zerovec.is_owned());"] # [doc = " ```"] # [inline] # [cfg (feature = "alloc")] pub fn for_each_mut (& mut self , mut f : impl FnMut (& mut T)) { self . to_mut_slice () . iter_mut () . for_each (| item | { let mut aligned = T :: from_unaligned (* item) ; f (& mut aligned) ; * item = aligned . to_unaligned () }) } # [doc = " Same as [`ZeroVec::for_each_mut()`], but bubbles up errors."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use zerovec::ZeroVec;"] # [doc = ""] # [doc = " let bytes: &[u8] = &[0xD3, 0x00, 0x19, 0x01, 0xA5, 0x01, 0xCD, 0x01];"] # [doc = " let mut zerovec: ZeroVec<u16> ="] # [doc = "     ZeroVec::parse_bytes(bytes).expect(\"infallible\");"] # [doc = ""] # [doc = " zerovec.try_for_each_mut(|item| {"] # [doc = "     *item = item.checked_add(1).ok_or(())?;"] # [doc = "     Ok(())"] # [doc = " })?;"] # [doc = ""] # [doc = " assert_eq!(zerovec.to_vec(), &[212, 282, 422, 462]);"] # [doc = " assert!(zerovec.is_owned());"] # [doc = " # Ok::<(), ()>(())"] # [doc = " ```"] # [inline] # [cfg (feature = "alloc")] pub fn try_for_each_mut < E > (& mut self , mut f : impl FnMut (& mut T) -> Result < () , E > ,) -> Result < () , E > { self . to_mut_slice () . iter_mut () . try_for_each (| item | { let mut aligned = T :: from_unaligned (* item) ; f (& mut aligned) ? ; * item = aligned . to_unaligned () ; Ok (()) }) } # [doc = " Converts a borrowed ZeroVec to an owned ZeroVec. No-op if already owned."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use zerovec::ZeroVec;"] # [doc = ""] # [doc = " let bytes: &[u8] = &[0xD3, 0x00, 0x19, 0x01, 0xA5, 0x01, 0xCD, 0x01];"] # [doc = " let zerovec: ZeroVec<u16> ="] # [doc = "     ZeroVec::parse_bytes(bytes).expect(\"infallible\");"] # [doc = " assert!(!zerovec.is_owned());"] # [doc = ""] # [doc = " let owned = zerovec.into_owned();"] # [doc = " assert!(owned.is_owned());"] # [doc = " ```"] # [cfg (feature = "alloc")] pub fn into_owned (self) -> ZeroVec < 'static , T > { use alloc :: borrow :: Cow ; match self . into_cow () { Cow :: Owned (vec) => ZeroVec :: new_owned (vec) , Cow :: Borrowed (b) => ZeroVec :: new_owned (b . into ()) , } } # [doc = " Allows the ZeroVec to be mutated by converting it to an owned variant, and producing"] # [doc = " a mutable vector of ULEs. If you only need a mutable slice, consider using [`Self::to_mut_slice()`]"] # [doc = " instead."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use crate::zerovec::ule::AsULE;"] # [doc = " use zerovec::ZeroVec;"] # [doc = ""] # [doc = " let bytes: &[u8] = &[0xD3, 0x00, 0x19, 0x01, 0xA5, 0x01, 0xCD, 0x01];"] # [doc = " let mut zerovec: ZeroVec<u16> ="] # [doc = "     ZeroVec::parse_bytes(bytes).expect(\"infallible\");"] # [doc = " assert!(!zerovec.is_owned());"] # [doc = ""] # [doc = " zerovec.with_mut(|v| v.push(12_u16.to_unaligned()));"] # [doc = " assert!(zerovec.is_owned());"] # [doc = " ```"] # [cfg (feature = "alloc")] pub fn with_mut < R > (& mut self , f : impl FnOnce (& mut alloc :: vec :: Vec < T :: ULE >) -> R) -> R { use alloc :: borrow :: Cow ; let this = core :: mem :: take (self) ; let mut vec = match this . into_cow () { Cow :: Owned (v) => v , Cow :: Borrowed (s) => s . into () , } ; let ret = f (& mut vec) ; * self = Self :: new_owned (vec) ; ret } # [doc = " Allows the ZeroVec to be mutated by converting it to an owned variant (if necessary)"] # [doc = " and returning a slice to its backing buffer. [`Self::with_mut()`] allows for mutation"] # [doc = " of the vector itself."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use crate::zerovec::ule::AsULE;"] # [doc = " use zerovec::ZeroVec;"] # [doc = ""] # [doc = " let bytes: &[u8] = &[0xD3, 0x00, 0x19, 0x01, 0xA5, 0x01, 0xCD, 0x01];"] # [doc = " let mut zerovec: ZeroVec<u16> ="] # [doc = "     ZeroVec::parse_bytes(bytes).expect(\"infallible\");"] # [doc = " assert!(!zerovec.is_owned());"] # [doc = ""] # [doc = " zerovec.to_mut_slice()[1] = 5u16.to_unaligned();"] # [doc = " assert!(zerovec.is_owned());"] # [doc = " ```"] # [cfg (feature = "alloc")] pub fn to_mut_slice (& mut self) -> & mut [T :: ULE] { if ! self . is_owned () { let slice = self . vector . as_slice () ; * self = ZeroVec :: new_owned (slice . into ()) ; } unsafe { self . vector . buf . as_mut () } } # [doc = " Remove all elements from this ZeroVec and reset it to an empty borrowed state."] pub fn clear (& mut self) { * self = Self :: new_borrowed (& []) } # [doc = " Removes the first element of the ZeroVec. The ZeroVec remains in the same"] # [doc = " borrowed or owned state."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use crate::zerovec::ule::AsULE;"] # [doc = " use zerovec::ZeroVec;"] # [doc = ""] # [doc = " let bytes: &[u8] = &[0xD3, 0x00, 0x19, 0x01, 0xA5, 0x01, 0xCD, 0x01];"] # [doc = " let mut zerovec: ZeroVec<u16> ="] # [doc = "     ZeroVec::parse_bytes(bytes).expect(\"infallible\");"] # [doc = " assert!(!zerovec.is_owned());"] # [doc = ""] # [doc = " let first = zerovec.take_first().unwrap();"] # [doc = " assert_eq!(first, 0x00D3);"] # [doc = " assert!(!zerovec.is_owned());"] # [doc = ""] # [doc = " let mut zerovec = zerovec.into_owned();"] # [doc = " assert!(zerovec.is_owned());"] # [doc = " let first = zerovec.take_first().unwrap();"] # [doc = " assert_eq!(first, 0x0119);"] # [doc = " assert!(zerovec.is_owned());"] # [doc = " ```"] # [cfg (feature = "alloc")] pub fn take_first (& mut self) -> Option < T > { match core :: mem :: take (self) . into_cow () { Cow :: Owned (mut vec) => { if vec . is_empty () { return None ; } let ule = vec . remove (0) ; let rv = T :: from_unaligned (ule) ; * self = ZeroVec :: new_owned (vec) ; Some (rv) } Cow :: Borrowed (b) => { let (ule , remainder) = b . split_first () ? ; let rv = T :: from_unaligned (* ule) ; * self = ZeroVec :: new_borrowed (remainder) ; Some (rv) } } } # [doc = " Removes the last element of the ZeroVec. The ZeroVec remains in the same"] # [doc = " borrowed or owned state."] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use crate::zerovec::ule::AsULE;"] # [doc = " use zerovec::ZeroVec;"] # [doc = ""] # [doc = " let bytes: &[u8] = &[0xD3, 0x00, 0x19, 0x01, 0xA5, 0x01, 0xCD, 0x01];"] # [doc = " let mut zerovec: ZeroVec<u16> ="] # [doc = "     ZeroVec::parse_bytes(bytes).expect(\"infallible\");"] # [doc = " assert!(!zerovec.is_owned());"] # [doc = ""] # [doc = " let last = zerovec.take_last().unwrap();"] # [doc = " assert_eq!(last, 0x01CD);"] # [doc = " assert!(!zerovec.is_owned());"] # [doc = ""] # [doc = " let mut zerovec = zerovec.into_owned();"] # [doc = " assert!(zerovec.is_owned());"] # [doc = " let last = zerovec.take_last().unwrap();"] # [doc = " assert_eq!(last, 0x01A5);"] # [doc = " assert!(zerovec.is_owned());"] # [doc = " ```"] # [cfg (feature = "alloc")] pub fn take_last (& mut self) -> Option < T > { match core :: mem :: take (self) . into_cow () { Cow :: Owned (mut vec) => { let ule = vec . pop () ? ; let rv = T :: from_unaligned (ule) ; * self = ZeroVec :: new_owned (vec) ; Some (rv) } Cow :: Borrowed (b) => { let (ule , remainder) = b . split_last () ? ; let rv = T :: from_unaligned (* ule) ; * self = ZeroVec :: new_borrowed (remainder) ; Some (rv) } } } # [doc = " Converts the type into a `Cow<'a, [T::ULE]>`, which is"] # [doc = " the logical equivalent of this type's internal representation"] # [doc = ""] # [doc = " ✨ *Enabled with the `alloc` Cargo feature.*"] # [inline] # [cfg (feature = "alloc")] pub fn into_cow (self) -> Cow < 'a , [T :: ULE] > { let this = core :: mem :: ManuallyDrop :: new (self) ; if this . is_owned () { let vec = unsafe { { this } . vector . get_vec () } ; Cow :: Owned (vec) } else { let slice = unsafe { { this } . vector . as_arbitrary_slice () } ; Cow :: Borrowed (slice) } } # [doc = " Truncates this vector to `min(self.len(), max)`."] # [inline] pub fn truncated (mut self , max : usize) -> Self { self . vector . truncate (max) ; self } }
};
}
