// Generated macro for VarZeroVec (struct)
macro_rules! Depcrate_varzerovec_vecVarZeroVec {
() => {
// Module: crate::varzerovec::vec
// Provides: {"VarZeroVec"}
// Dependencies: {}
# [doc = " A zero-copy, byte-aligned vector for variable-width types."] # [doc = ""] # [doc = " `VarZeroVec<T>` is designed as a drop-in replacement for `Vec<T>` in situations where it is"] # [doc = " desirable to borrow data from an unaligned byte slice, such as zero-copy deserialization, and"] # [doc = " where `T`'s data is variable-length (e.g. `String`)"] # [doc = ""] # [doc = " `T` must implement [`VarULE`], which is already implemented for [`str`] and `[u8]`. For storing more"] # [doc = " complicated series of elements, it is implemented on `ZeroSlice<T>` as well as `VarZeroSlice<T>`"] # [doc = " for nesting. [`zerovec::make_varule`](crate::make_varule) may be used to generate"] # [doc = " a dynamically-sized [`VarULE`] type and conversions to and from a custom type."] # [doc = ""] # [doc = " For example, here are some owned types and their zero-copy equivalents:"] # [doc = ""] # [doc = " - `Vec<String>`: `VarZeroVec<'a, str>`"] # [doc = " - `Vec<Vec<u8>>>`: `VarZeroVec<'a, [u8]>`"] # [doc = " - `Vec<Vec<u32>>`: `VarZeroVec<'a, ZeroSlice<u32>>`"] # [doc = " - `Vec<Vec<String>>`: `VarZeroVec<'a, VarZeroSlice<str>>`"] # [doc = ""] # [doc = " Most of the methods on `VarZeroVec<'a, T>` come from its [`Deref`] implementation to [`VarZeroSlice<T>`](VarZeroSlice)."] # [doc = ""] # [doc = " For creating zero-copy vectors of fixed-size types, see [`ZeroVec`](crate::ZeroVec)."] # [doc = ""] # [doc = " `VarZeroVec<T>` behaves much like [`Cow`](alloc::borrow::Cow), where it can be constructed from"] # [doc = " owned data (and then mutated!) but can also borrow from some buffer."] # [doc = ""] # [doc = " The `F` type parameter is a [`VarZeroVecFormat`] (see its docs for more details), which can be used to select the"] # [doc = " precise format of the backing buffer with various size and performance tradeoffs. It defaults to [`Index16`]."] # [doc = ""] # [doc = " # Bytes and Equality"] # [doc = ""] # [doc = " Two [`VarZeroVec`]s are equal if and only if their bytes are equal, as described in the trait"] # [doc = " [`VarULE`]. However, we do not guarantee stability of byte equality or serialization format"] # [doc = " across major SemVer releases."] # [doc = ""] # [doc = " To compare a [`Vec<T>`] to a [`VarZeroVec<T>`], it is generally recommended to use"] # [doc = " [`Iterator::eq`], since it is somewhat expensive at runtime to convert from a [`Vec<T>`] to a"] # [doc = " [`VarZeroVec<T>`] or vice-versa."] # [doc = ""] # [doc = " Prior to zerovec reaching 1.0, the precise byte representation of [`VarZeroVec`] is still"] # [doc = " under consideration, with different options along the space-time spectrum. See"] # [doc = " [#1410](https://github.com/unicode-org/icu4x/issues/1410)."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::VarZeroVec;"] # [doc = ""] # [doc = " // The little-endian bytes correspond to the list of strings."] # [doc = " let strings = vec![\"w\", \"ω\", \"文\", \"𑄃\"];"] # [doc = ""] # [doc = " #[derive(serde::Serialize, serde::Deserialize)]"] # [doc = " struct Data<'a> {"] # [doc = "     #[serde(borrow)]"] # [doc = "     strings: VarZeroVec<'a, str>,"] # [doc = " }"] # [doc = ""] # [doc = " let data = Data {"] # [doc = "     strings: VarZeroVec::from(&strings),"] # [doc = " };"] # [doc = ""] # [doc = " let bincode_bytes ="] # [doc = "     bincode::serialize(&data).expect(\"Serialization should be successful\");"] # [doc = ""] # [doc = " // Will deserialize without allocations"] # [doc = " let deserialized: Data = bincode::deserialize(&bincode_bytes)"] # [doc = "     .expect(\"Deserialization should be successful\");"] # [doc = ""] # [doc = " assert_eq!(deserialized.strings.get(2), Some(\"文\"));"] # [doc = " assert_eq!(deserialized.strings, &*strings);"] # [doc = " ```"] # [doc = ""] # [doc = " Here's another example with `ZeroSlice<T>` (similar to `[T]`):"] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::VarZeroVec;"] # [doc = " use zerovec::ZeroSlice;"] # [doc = ""] # [doc = " // The structured list correspond to the list of integers."] # [doc = " let numbers: &[&[u32]] = &["] # [doc = "     &[12, 25, 38],"] # [doc = "     &[39179, 100],"] # [doc = "     &[42, 55555],"] # [doc = "     &[12345, 54321, 9],"] # [doc = " ];"] # [doc = ""] # [doc = " #[derive(serde::Serialize, serde::Deserialize)]"] # [doc = " struct Data<'a> {"] # [doc = "     #[serde(borrow)]"] # [doc = "     vecs: VarZeroVec<'a, ZeroSlice<u32>>,"] # [doc = " }"] # [doc = ""] # [doc = " let data = Data {"] # [doc = "     vecs: VarZeroVec::from(numbers),"] # [doc = " };"] # [doc = ""] # [doc = " let bincode_bytes ="] # [doc = "     bincode::serialize(&data).expect(\"Serialization should be successful\");"] # [doc = ""] # [doc = " let deserialized: Data = bincode::deserialize(&bincode_bytes)"] # [doc = "     .expect(\"Deserialization should be successful\");"] # [doc = ""] # [doc = " assert_eq!(deserialized.vecs[0].get(1).unwrap(), 25);"] # [doc = " assert_eq!(deserialized.vecs[1], *numbers[1]);"] # [doc = " ```"] # [doc = ""] # [doc = " [`VarZeroVec`]s can be nested infinitely via a similar mechanism, see the docs of [`VarZeroSlice`]"] # [doc = " for more information."] # [doc = ""] # [doc = " # How it Works"] # [doc = ""] # [doc = " `VarZeroVec<T>`, when used with non-human-readable serializers (like `bincode`), will"] # [doc = " serialize to a specially formatted list of bytes. The format is:"] # [doc = ""] # [doc = " -  2 bytes for `length` (interpreted as a little-endian u16)"] # [doc = " - `2 * (length - 1)` bytes of `indices` (interpreted as little-endian u16s)"] # [doc = " - Remaining bytes for actual `data`"] # [doc = ""] # [doc = " The format is tweakable by setting the `F` parameter, by default it uses u16 indices and lengths but other"] # [doc = " `VarZeroVecFormat` types can set other sizes."] # [doc = ""] # [doc = " Each element in the `indices` array points to the ending index of its corresponding"] # [doc = " data part in the `data` list. The starting index can be calculated from the ending index"] # [doc = " of the next element (or 0 for the first element). The last ending index, not stored in the array, is"] # [doc = " the length of the `data` segment."] # [doc = ""] # [doc = " See [the design doc](https://github.com/unicode-org/icu4x/blob/main/utils/zerovec/design_doc.md) for more details."] # [doc = ""] # [doc = " [`ule`]: crate::ule"] pub struct VarZeroVec < 'a , T : ? Sized , F = Index16 > (pub (crate) VarZeroVecInner < 'a , T , F >) ;
};
}
