// Generated macro for compute_displacements (function)
macro_rules! Depcrate_hashmap_algorithmscompute_displacements {
() => {
// Module: crate::hashmap::algorithms
// Provides: {"compute_displacements"}
// Dependencies: {}
# [doc = " Compute displacements for the given `key_hashes`, which split the keys into distinct slots by a"] # [doc = " two-level hashing schema."] # [doc = ""] # [doc = " Returns a tuple of where the first item is the displacement array and the second item is the"] # [doc = " reverse mapping used to permute keys, values into their slots."] # [doc = ""] # [doc = " 1. Split the hashes into (g, f0, f1)."] # [doc = " 2. Bucket and sort the split hash on g in descending order."] # [doc = " 3. In decreasing order of bucket size, try until a (d0, d1) is found that splits the keys"] # [doc = "    in the bucket into distinct slots."] # [doc = " 4. Mark the slots for current bucket as occupied and store the reverse mapping."] # [doc = " 5. Repeat untill all the keys have been assigned distinct slots."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `key_hashes` - [`ExactSizeIterator`] over the hashed key values"] # [expect (clippy :: indexing_slicing , clippy :: unwrap_used)] pub fn compute_displacements (key_hashes : impl ExactSizeIterator < Item = u64 > ,) -> (Vec < (u32 , u32) > , Vec < usize >) { let len = key_hashes . len () ; let mut bucket_sizes = vec ! [0 ; len] ; let mut bucket_flatten = Vec :: with_capacity (len) ; key_hashes . into_iter () . enumerate () . for_each (| (i , kh) | { let h = split_hash64 (kh , len) ; bucket_sizes [h . 0] += 1 ; bucket_flatten . push ((h , i)) }) ; bucket_flatten . sort_by (| & (ha , _) , & (hb , _) | { (bucket_sizes [hb . 0] , hb) . cmp (& (bucket_sizes [ha . 0] , ha)) }) ; let mut generation = 0 ; let mut occupied = vec ! [false ; len] ; let mut assignments = vec ! [0 ; len] ; let mut current_displacements = Vec :: with_capacity (16) ; let mut displacements = vec ! [(0 , 0) ; len] ; let mut reverse_mapping = vec ! [0 ; len] ; let mut start = 0 ; while start < len { let g = bucket_flatten [start] . 0 . 0 ; let end = start + bucket_sizes [g] ; let buckets = & bucket_flatten [start .. end] ; 'd0 : for d0 in 0 .. len as u32 { 'd1 : for d1 in 0 .. len as u32 { if (d0 , d1) == (0 , 0) { continue ; } current_displacements . clear () ; generation += 1 ; for ((_ , f0 , f1) , _) in buckets { let displacement_idx = compute_index ((* f0 , * f1) , (d0 , d1) , len as u32) . unwrap () ; if occupied [displacement_idx] || assignments [displacement_idx] == generation { continue 'd1 ; } assignments [displacement_idx] = generation ; current_displacements . push (displacement_idx) ; } displacements [g] = (d0 , d1) ; for (i , displacement_idx) in current_displacements . iter () . enumerate () { let (_ , idx) = & buckets [i] ; occupied [* displacement_idx] = true ; reverse_mapping [* displacement_idx] = * idx ; } break 'd0 ; } } start = end ; } (displacements , reverse_mapping) }
};
}
