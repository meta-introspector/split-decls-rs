// Generated macro for impl_81 (impl)
macro_rules! Depcrate_map_borrowedimpl_81 {
() => {
// Module: crate::map::borrowed
// Provides: {"impl_81"}
// Dependencies: {}
impl < 'a , K , V > ZeroMapBorrowed < 'a , K , V > where K : ZeroMapKV < 'a > + Ord , V : ZeroMapKV < 'a > , K : ? Sized , V : ? Sized , { # [doc = " Get the value associated with `key`, if it exists."] # [doc = ""] # [doc = " This is able to return values that live longer than the map itself"] # [doc = " since they borrow directly from the backing buffer. This is the"] # [doc = " primary advantage of using [`ZeroMapBorrowed`](super::ZeroMapBorrowed) over [`ZeroMap`](super::ZeroMap)."] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap;"] # [doc = ""] # [doc = " let mut map = ZeroMap::new();"] # [doc = " map.insert(&1, \"one\");"] # [doc = " map.insert(&2, \"two\");"] # [doc = " let borrowed = map.as_borrowed();"] # [doc = " assert_eq!(borrowed.get(&1), Some(\"one\"));"] # [doc = " assert_eq!(borrowed.get(&3), None);"] # [doc = " ```"] pub fn get (self , key : & K) -> Option < & 'a V :: GetType > { let index = self . keys . zvl_binary_search (key) . ok () ? ; self . values . zvl_get (index) } # [doc = " Binary search the map with `predicate` to find a key, returning the value."] # [doc = ""] # [doc = " This is able to return values that live longer than the map itself"] # [doc = " since they borrow directly from the backing buffer. This is the"] # [doc = " primary advantage of using [`ZeroMapBorrowed`](super::ZeroMapBorrowed) over [`ZeroMap`](super::ZeroMap)."] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap;"] # [doc = ""] # [doc = " let mut map = ZeroMap::new();"] # [doc = " map.insert(&1, \"one\");"] # [doc = " map.insert(&2, \"two\");"] # [doc = " let borrowed = map.as_borrowed();"] # [doc = " assert_eq!(borrowed.get_by(|probe| probe.cmp(&1)), Some(\"one\"));"] # [doc = " assert_eq!(borrowed.get_by(|probe| probe.cmp(&3)), None);"] # [doc = " ```"] pub fn get_by (self , predicate : impl FnMut (& K) -> Ordering) -> Option < & 'a V :: GetType > { let index = self . keys . zvl_binary_search_by (predicate) . ok () ? ; self . values . zvl_get (index) } # [doc = " Returns whether `key` is contained in this map"] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap;"] # [doc = ""] # [doc = " let mut map = ZeroMap::new();"] # [doc = " map.insert(&1, \"one\");"] # [doc = " map.insert(&2, \"two\");"] # [doc = " let borrowed = map.as_borrowed();"] # [doc = " assert!(borrowed.contains_key(&1));"] # [doc = " assert!(!borrowed.contains_key(&3));"] # [doc = " ```"] pub fn contains_key (self , key : & K) -> bool { self . keys . zvl_binary_search (key) . is_ok () } }
};
}
