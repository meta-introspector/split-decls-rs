// Generated macro for EncodeAsVarULE (trait)
macro_rules! Depcrate_ule_encodeEncodeAsVarULE {
() => {
// Module: crate::ule::encode
// Provides: {"EncodeAsVarULE"}
// Dependencies: {}
# [doc = " Allows types to be encoded as VarULEs. This is highly useful for implementing VarULE on"] # [doc = " custom DSTs where the type cannot be obtained as a reference to some other type."] # [doc = ""] # [doc = " [`Self::encode_var_ule_as_slices()`] should be implemented by providing an encoded slice for each field"] # [doc = " of the VarULE type to the callback, in order. For an implementation to be safe, the slices"] # [doc = " to the callback must, when concatenated, be a valid instance of the VarULE type."] # [doc = ""] # [doc = " See the [custom VarULEdocumentation](crate::ule::custom) for examples."] # [doc = ""] # [doc = " [`Self::encode_var_ule_as_slices()`] is only used to provide default implementations for [`Self::encode_var_ule_write()`]"] # [doc = " and [`Self::encode_var_ule_len()`]. If you override the default implementations it is totally valid to"] # [doc = " replace [`Self::encode_var_ule_as_slices()`]'s body with `unreachable!()`. This can be done for cases where"] # [doc = " it is not possible to implement [`Self::encode_var_ule_as_slices()`] but the other methods still work."] # [doc = ""] # [doc = " A typical implementation will take each field in the order found in the [`VarULE`] type,"] # [doc = " convert it to ULE, call [`ULE::slice_as_bytes()`] on them, and pass the slices to `cb` in order."] # [doc = " A trailing [`ZeroVec`](crate::ZeroVec) or [`VarZeroVec`](crate::VarZeroVec) can have their underlying"] # [doc = " byte representation passed through."] # [doc = ""] # [doc = " In case the compiler is not optimizing [`Self::encode_var_ule_len()`], it can be overridden. A typical"] # [doc = " implementation will add up the sizes of each field on the [`VarULE`] type and then add in the byte length of the"] # [doc = " dynamically-sized part."] # [doc = ""] # [doc = " # Reverse-encoding VarULE"] # [doc = ""] # [doc = " This trait maps a struct to its bytes representation (\"serialization\"), and"] # [doc = " [`ZeroFrom`](zerofrom::ZeroFrom) performs the opposite operation, taking those bytes and"] # [doc = " creating a struct from them (\"deserialization\")."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The safety invariants of [`Self::encode_var_ule_as_slices()`] are:"] # [doc = " - It must call `cb` (only once)"] # [doc = " - The slices passed to `cb`, if concatenated, should be a valid instance of the `T` [`VarULE`] type"] # [doc = "   (i.e. if fed to [`VarULE::validate_bytes()`] they must produce a successful result)"] # [doc = " - It must return the return value of `cb` to the caller"] # [doc = ""] # [doc = " One or more of [`Self::encode_var_ule_len()`] and [`Self::encode_var_ule_write()`] may be provided."] # [doc = " If both are, then `zerovec` code is guaranteed to not call [`Self::encode_var_ule_as_slices()`], and it may be replaced"] # [doc = " with `unreachable!()`."] # [doc = ""] # [doc = " The safety invariants of [`Self::encode_var_ule_len()`] are:"] # [doc = " - It must return the length of the corresponding VarULE type"] # [doc = ""] # [doc = " The safety invariants of [`Self::encode_var_ule_write()`] are:"] # [doc = " - The slice written to `dst` must be a valid instance of the `T` [`VarULE`] type"] pub unsafe trait EncodeAsVarULE < T : VarULE + ? Sized > { # [doc = " Calls `cb` with a piecewise list of byte slices that when concatenated"] # [doc = " produce the memory pattern of the corresponding instance of `T`."] # [doc = ""] # [doc = " Do not call this function directly; instead use the other two. Some implementors"] # [doc = " may define this function to panic."] fn encode_var_ule_as_slices < R > (& self , cb : impl FnOnce (& [& [u8]]) -> R) -> R ; # [doc = " Return the length, in bytes, of the corresponding [`VarULE`] type"] fn encode_var_ule_len (& self) -> usize { self . encode_var_ule_as_slices (| slices | slices . iter () . map (| s | s . len ()) . sum ()) } # [doc = " Write the corresponding [`VarULE`] type to the `dst` buffer. `dst` should"] # [doc = " be the size of [`Self::encode_var_ule_len()`]"] fn encode_var_ule_write (& self , mut dst : & mut [u8]) { debug_assert_eq ! (self . encode_var_ule_len () , dst . len ()) ; self . encode_var_ule_as_slices (move | slices | { # [expect (clippy :: indexing_slicing)] for slice in slices { dst [.. slice . len ()] . copy_from_slice (slice) ; dst = & mut dst [slice . len () ..] ; } }) ; } }
};
}
