// Generated macro for impl_130 (impl)
macro_rules! Depcrate_map_mapimpl_130 {
() => {
// Module: crate::map::map
// Provides: {"impl_130"}
// Dependencies: {}
impl < 'a , K , V > ZeroMap < 'a , K , V > where K : ZeroMapKV < 'a > + ? Sized + Ord , V : ZeroMapKV < 'a > + ? Sized , { # [doc = " Get the value associated with `key`, if it exists."] # [doc = ""] # [doc = " For fixed-size ([`AsULE`]) `V` types, this _will_ return"] # [doc = " their corresponding [`AsULE::ULE`] type. If you wish to work with the `V`"] # [doc = " type directly, [`Self::get_copied()`] exists for convenience."] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap;"] # [doc = ""] # [doc = " let mut map = ZeroMap::new();"] # [doc = " map.insert(&1, \"one\");"] # [doc = " map.insert(&2, \"two\");"] # [doc = " assert_eq!(map.get(&1), Some(\"one\"));"] # [doc = " assert_eq!(map.get(&3), None);"] # [doc = " ```"] pub fn get (& self , key : & K) -> Option < & V :: GetType > { let index = self . keys . zvl_binary_search (key) . ok () ? ; self . values . zvl_get (index) } # [doc = " Binary search the map with `predicate` to find a key, returning the value."] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap;"] # [doc = ""] # [doc = " let mut map = ZeroMap::new();"] # [doc = " map.insert(&1, \"one\");"] # [doc = " map.insert(&2, \"two\");"] # [doc = " assert_eq!(map.get_by(|probe| probe.cmp(&1)), Some(\"one\"));"] # [doc = " assert_eq!(map.get_by(|probe| probe.cmp(&3)), None);"] # [doc = " ```"] pub fn get_by (& self , predicate : impl FnMut (& K) -> Ordering) -> Option < & V :: GetType > { let index = self . keys . zvl_binary_search_by (predicate) . ok () ? ; self . values . zvl_get (index) } # [doc = " Returns whether `key` is contained in this map"] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap;"] # [doc = ""] # [doc = " let mut map = ZeroMap::new();"] # [doc = " map.insert(&1, \"one\");"] # [doc = " map.insert(&2, \"two\");"] # [doc = " assert!(map.contains_key(&1));"] # [doc = " assert!(!map.contains_key(&3));"] # [doc = " ```"] pub fn contains_key (& self , key : & K) -> bool { self . keys . zvl_binary_search (key) . is_ok () } # [doc = " Insert `value` with `key`, returning the existing value if it exists."] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap;"] # [doc = ""] # [doc = " let mut map = ZeroMap::new();"] # [doc = " map.insert(&1, \"one\");"] # [doc = " map.insert(&2, \"two\");"] # [doc = " assert_eq!(map.get(&1), Some(\"one\"));"] # [doc = " assert_eq!(map.get(&3), None);"] # [doc = " ```"] pub fn insert (& mut self , key : & K , value : & V) -> Option < V :: OwnedType > { match self . keys . zvl_binary_search (key) { Ok (index) => Some (self . values . zvl_replace (index , value)) , Err (index) => { self . keys . zvl_insert (index , key) ; self . values . zvl_insert (index , value) ; None } } } # [doc = " Remove the value at `key`, returning it if it exists."] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap;"] # [doc = ""] # [doc = " let mut map = ZeroMap::new();"] # [doc = " map.insert(&1, \"one\");"] # [doc = " map.insert(&2, \"two\");"] # [doc = " assert_eq!(map.remove(&1), Some(\"one\".to_owned().into_boxed_str()));"] # [doc = " assert_eq!(map.get(&1), None);"] # [doc = " ```"] pub fn remove (& mut self , key : & K) -> Option < V :: OwnedType > { let idx = self . keys . zvl_binary_search (key) . ok () ? ; self . keys . zvl_remove (idx) ; Some (self . values . zvl_remove (idx)) } # [doc = " Appends `value` with `key` to the end of the underlying vector, returning"] # [doc = " `key` and `value` _if it failed_. Useful for extending with an existing"] # [doc = " sorted list."] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap;"] # [doc = ""] # [doc = " let mut map = ZeroMap::new();"] # [doc = " assert!(map.try_append(&1, \"uno\").is_none());"] # [doc = " assert!(map.try_append(&3, \"tres\").is_none());"] # [doc = ""] # [doc = " let unsuccessful = map.try_append(&3, \"tres-updated\");"] # [doc = " assert!(unsuccessful.is_some(), \"append duplicate of last key\");"] # [doc = ""] # [doc = " let unsuccessful = map.try_append(&2, \"dos\");"] # [doc = " assert!(unsuccessful.is_some(), \"append out of order\");"] # [doc = ""] # [doc = " assert_eq!(map.get(&1), Some(\"uno\"));"] # [doc = ""] # [doc = " // contains the original value for the key: 3"] # [doc = " assert_eq!(map.get(&3), Some(\"tres\"));"] # [doc = ""] # [doc = " // not appended since it wasn't in order"] # [doc = " assert_eq!(map.get(&2), None);"] # [doc = " ```"] # [must_use] pub fn try_append < 'b > (& mut self , key : & 'b K , value : & 'b V) -> Option < (& 'b K , & 'b V) > { if self . keys . zvl_len () != 0 { if let Some (last) = self . keys . zvl_get (self . keys . zvl_len () - 1) { if K :: Container :: t_cmp_get (key , last) != Ordering :: Greater { return Some ((key , value)) ; } } } self . keys . zvl_push (key) ; self . values . zvl_push (value) ; None } }
};
}
