// Generated macro for ULE (trait)
macro_rules! Depcrate_uleULE {
() => {
// Module: crate::ule
// Provides: {"ULE"}
// Dependencies: {}
# [doc = " Fixed-width, byte-aligned data that can be cast to and from a little-endian byte slice."] # [doc = ""] # [doc = " If you need to implement this trait, consider using [`#[make_ule]`](crate::make_ule) or"] # [doc = "  [`#[derive(ULE)]`](macro@ULE) instead."] # [doc = ""] # [doc = " Types that are not fixed-width can implement [`VarULE`] instead."] # [doc = ""] # [doc = " \"ULE\" stands for \"Unaligned little-endian\""] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Safety checklist for `ULE`:"] # [doc = ""] # [doc = " 1. The type *must not* include any uninitialized or padding bytes."] # [doc = " 2. The type must have an alignment of 1 byte, or it is a ZST that is safe to construct."] # [doc = " 3. The impl of [`ULE::validate_bytes()`] *must* return an error if the given byte slice"] # [doc = "    would not represent a valid slice of this type."] # [doc = " 4. The impl of [`ULE::validate_bytes()`] *must* return an error if the given byte slice"] # [doc = "    cannot be used in its entirety (if its length is not a multiple of `size_of::<Self>()`)."] # [doc = " 5. All other methods *must* be left with their default impl, or else implemented according to"] # [doc = "    their respective safety guidelines."] # [doc = " 6. Acknowledge the following note about the equality invariant."] # [doc = ""] # [doc = " If the ULE type is a struct only containing other ULE types (or other types which satisfy invariants 1 and 2,"] # [doc = " like `[u8; N]`), invariants 1 and 2 can be achieved via `#[repr(C, packed)]` or `#[repr(transparent)]`."] # [doc = ""] # [doc = " # Equality invariant"] # [doc = ""] # [doc = " A non-safety invariant is that if `Self` implements `PartialEq`, the it *must* be logically"] # [doc = " equivalent to byte equality on [`Self::slice_as_bytes()`]."] # [doc = ""] # [doc = " It may be necessary to introduce a \"canonical form\" of the ULE if logical equality does not"] # [doc = " equal byte equality. In such a case, [`Self::validate_bytes()`] should return an error"] # [doc = " for any values that are not in canonical form. For example, the decimal strings \"1.23e4\" and"] # [doc = " \"12.3e3\" are logically equal, but not byte-for-byte equal, so we could define a canonical form"] # [doc = " where only a single digit is allowed before `.`."] # [doc = ""] # [doc = " Failure to follow this invariant will cause surprising behavior in `PartialEq`, which may"] # [doc = " result in unpredictable operations on `ZeroVec`, `VarZeroVec`, and `ZeroMap`."] pub unsafe trait ULE where Self : Sized , Self : Copy + 'static , { # [doc = " Validates a byte slice, `&[u8]`."] # [doc = ""] # [doc = " If `Self` is not well-defined for all possible bit values, the bytes should be validated."] # [doc = " If the bytes can be transmuted, *in their entirety*, to a valid slice of `Self`, then `Ok`"] # [doc = " should be returned; otherwise, `Err` should be returned."] fn validate_bytes (bytes : & [u8]) -> Result < () , UleError > ; # [doc = " Parses a byte slice, `&[u8]`, and return it as `&[Self]` with the same lifetime."] # [doc = ""] # [doc = " If `Self` is not well-defined for all possible bit values, the bytes should be validated,"] # [doc = " and an error should be returned in the same cases as [`Self::validate_bytes()`]."] # [doc = ""] # [doc = " The default implementation executes [`Self::validate_bytes()`] followed by"] # [doc = " [`Self::slice_from_bytes_unchecked`]."] # [doc = ""] # [doc = " Note: The following equality should hold: `bytes.len() % size_of::<Self>() == 0`. This"] # [doc = " means that the returned slice can span the entire byte slice."] fn parse_bytes_to_slice (bytes : & [u8]) -> Result < & [Self] , UleError > { Self :: validate_bytes (bytes) ? ; debug_assert_eq ! (bytes . len () % mem :: size_of ::< Self > () , 0) ; Ok (unsafe { Self :: slice_from_bytes_unchecked (bytes) }) } # [doc = " Takes a byte slice, `&[u8]`, and return it as `&[Self]` with the same lifetime, assuming"] # [doc = " that this byte slice has previously been run through [`Self::parse_bytes_to_slice()`] with"] # [doc = " success."] # [doc = ""] # [doc = " The default implementation performs a pointer cast to the same region of memory."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " ## Callers"] # [doc = ""] # [doc = " Callers of this method must take care to ensure that `bytes` was previously passed through"] # [doc = " [`Self::validate_bytes()`] with success (and was not changed since then)."] # [doc = ""] # [doc = " ## Implementors"] # [doc = ""] # [doc = " Implementations of this method may call unsafe functions to cast the pointer to the correct"] # [doc = " type, assuming the \"Callers\" invariant above."] # [doc = ""] # [doc = " Keep in mind that `&[Self]` and `&[u8]` may have different lengths."] # [doc = ""] # [doc = " Safety checklist:"] # [doc = ""] # [doc = " 1. This method *must* return the same result as [`Self::parse_bytes_to_slice()`]."] # [doc = " 2. This method *must* return a slice to the same region of memory as the argument."] # [inline] unsafe fn slice_from_bytes_unchecked (bytes : & [u8]) -> & [Self] { let data = bytes . as_ptr () ; let len = bytes . len () / mem :: size_of :: < Self > () ; debug_assert_eq ! (bytes . len () % mem :: size_of ::< Self > () , 0) ; core :: slice :: from_raw_parts (data as * const Self , len) } # [doc = " Given `&[Self]`, returns a `&[u8]` with the same lifetime."] # [doc = ""] # [doc = " The default implementation performs a pointer cast to the same region of memory."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Implementations of this method should call potentially unsafe functions to cast the"] # [doc = " pointer to the correct type."] # [doc = ""] # [doc = " Keep in mind that `&[Self]` and `&[u8]` may have different lengths."] # [inline] fn slice_as_bytes (slice : & [Self]) -> & [u8] { unsafe { slice :: from_raw_parts (slice as * const [Self] as * const u8 , mem :: size_of_val (slice)) } } }
};
}
