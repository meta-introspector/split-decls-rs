// Generated macro for impl_209 (impl)
macro_rules! Depcrate_map2d_borrowedimpl_209 {
() => {
// Module: crate::map2d::borrowed
// Provides: {"impl_209"}
// Dependencies: {}
impl < 'a , K0 , K1 , V > ZeroMap2dBorrowed < 'a , K0 , K1 , V > where K0 : ZeroMapKV < 'a > + Ord , K1 : ZeroMapKV < 'a > , V : ZeroMapKV < 'a > , K0 : ? Sized , K1 : ? Sized , V : ? Sized , { # [doc = " Gets a cursor for `key0`. If `None`, then `key0` is not in the map. If `Some`,"] # [doc = " then `key0` is in the map, and `key1` can be queried."] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap2d;"] # [doc = ""] # [doc = " let mut map = ZeroMap2d::new();"] # [doc = " map.insert(&1, \"one\", \"foo\");"] # [doc = " map.insert(&2, \"two\", \"bar\");"] # [doc = " let borrowed = map.as_borrowed();"] # [doc = " assert!(borrowed.get0(&1).is_some());"] # [doc = " assert!(borrowed.get0(&3).is_none());"] # [doc = " ```"] # [inline] pub fn get0 < 'l > (& 'l self , key0 : & K0) -> Option < ZeroMap2dCursor < 'a , 'a , K0 , K1 , V > > { let key0_index = self . keys0 . zvl_binary_search (key0) . ok () ? ; Some (ZeroMap2dCursor :: from_borrowed (self , key0_index)) } # [doc = " Binary search the map for `key0`, returning a cursor."] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap2d;"] # [doc = ""] # [doc = " let mut map = ZeroMap2d::new();"] # [doc = " map.insert(&1, \"one\", \"foo\");"] # [doc = " map.insert(&2, \"two\", \"bar\");"] # [doc = " let borrowed = map.as_borrowed();"] # [doc = " assert!(borrowed.get0_by(|probe| probe.cmp(&1)).is_some());"] # [doc = " assert!(borrowed.get0_by(|probe| probe.cmp(&3)).is_none());"] # [doc = " ```"] pub fn get0_by < 'l > (& 'l self , predicate : impl FnMut (& K0) -> Ordering ,) -> Option < ZeroMap2dCursor < 'a , 'a , K0 , K1 , V > > { let key0_index = self . keys0 . zvl_binary_search_by (predicate) . ok () ? ; Some (ZeroMap2dCursor :: from_borrowed (self , key0_index)) } # [doc = " Returns whether `key0` is contained in this map"] # [doc = ""] # [doc = " ```rust"] # [doc = " use zerovec::ZeroMap2d;"] # [doc = ""] # [doc = " let mut map = ZeroMap2d::new();"] # [doc = " map.insert(&1, \"one\", \"foo\");"] # [doc = " map.insert(&2, \"two\", \"bar\");"] # [doc = " let borrowed = map.as_borrowed();"] # [doc = " assert!(borrowed.contains_key0(&1));"] # [doc = " assert!(!borrowed.contains_key0(&3));"] # [doc = " ```"] pub fn contains_key0 (& self , key0 : & K0) -> bool { self . keys0 . zvl_binary_search (key0) . is_ok () } }
};
}
