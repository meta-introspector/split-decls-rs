// Generated macro for ZeroMap (struct)
macro_rules! Depcrate_map_mapZeroMap {
() => {
// Module: crate::map::map
// Provides: {"ZeroMap"}
// Dependencies: {}
# [doc = " A zero-copy map datastructure, built on sorted binary-searchable [`ZeroVec`]"] # [doc = " and [`VarZeroVec`]."] # [doc = ""] # [doc = " This type, like [`ZeroVec`] and [`VarZeroVec`], is able to zero-copy"] # [doc = " deserialize from appropriately formatted byte buffers. It is internally copy-on-write, so it can be mutated"] # [doc = " afterwards as necessary."] # [doc = ""] # [doc = " Internally, a `ZeroMap` is a zero-copy vector for keys paired with a zero-copy vector for"] # [doc = " values, sorted by the keys. Therefore, all types used in `ZeroMap` need to work with either"] # [doc = " [`ZeroVec`] or [`VarZeroVec`]."] # [doc = ""] # [doc = " This does mean that for fixed-size data, one must use the regular type (`u32`, `u8`, `char`, etc),"] # [doc = " whereas for variable-size data, `ZeroMap` will use the dynamically sized version (`str` not `String`,"] # [doc = " `ZeroSlice` not `ZeroVec`, `FooULE` not `Foo` for custom types)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use zerovec::ZeroMap;"] # [doc = ""] # [doc = " #[derive(serde::Serialize, serde::Deserialize)]"] # [doc = " struct Data<'a> {"] # [doc = "     #[serde(borrow)]"] # [doc = "     map: ZeroMap<'a, u32, str>,"] # [doc = " }"] # [doc = ""] # [doc = " let mut map = ZeroMap::new();"] # [doc = " map.insert(&1, \"one\");"] # [doc = " map.insert(&2, \"two\");"] # [doc = " map.insert(&4, \"four\");"] # [doc = ""] # [doc = " let data = Data { map };"] # [doc = ""] # [doc = " let bincode_bytes ="] # [doc = "     bincode::serialize(&data).expect(\"Serialization should be successful\");"] # [doc = ""] # [doc = " // Will deserialize without any allocations"] # [doc = " let deserialized: Data = bincode::deserialize(&bincode_bytes)"] # [doc = "     .expect(\"Deserialization should be successful\");"] # [doc = ""] # [doc = " assert_eq!(data.map.get(&1), Some(\"one\"));"] # [doc = " assert_eq!(data.map.get(&2), Some(\"two\"));"] # [doc = " ```"] # [doc = ""] # [doc = " [`VarZeroVec`]: crate::VarZeroVec"] pub struct ZeroMap < 'a , K , V > where K : ZeroMapKV < 'a > + ? Sized , V : ZeroMapKV < 'a > + ? Sized , { pub (crate) keys : K :: Container , pub (crate) values : V :: Container , }
};
}
