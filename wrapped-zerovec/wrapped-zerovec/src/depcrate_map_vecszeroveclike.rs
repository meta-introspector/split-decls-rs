// Generated macro for ZeroVecLike (trait)
macro_rules! Depcrate_map_vecsZeroVecLike {
() => {
// Module: crate::map::vecs
// Provides: {"ZeroVecLike"}
// Dependencies: {}
# [doc = " Trait abstracting over [`ZeroVec`] and [`VarZeroVec`], for use in [`ZeroMap`](super::ZeroMap). **You"] # [doc = " should not be implementing or calling this trait directly.**"] # [doc = ""] # [doc = " The T type is the type received by [`Self::zvl_binary_search()`], as well as the one used"] # [doc = " for human-readable serialization."] # [doc = ""] # [doc = " Methods are prefixed with `zvl_*` to avoid clashes with methods on the types themselves"] pub trait ZeroVecLike < T : ? Sized > { # [doc = " The type returned by `Self::get()`"] type GetType : ? Sized + 'static ; # [doc = " A fully borrowed version of this"] type SliceVariant : ZeroVecLike < T , GetType = Self :: GetType > + ? Sized ; # [doc = " Create a new, empty borrowed variant"] fn zvl_new_borrowed () -> & 'static Self :: SliceVariant ; # [doc = " Search for a key in a sorted vector, returns `Ok(index)` if found,"] # [doc = " returns `Err(insert_index)` if not found, where `insert_index` is the"] # [doc = " index where it should be inserted to maintain sort order."] fn zvl_binary_search (& self , k : & T) -> Result < usize , usize > where T : Ord ; # [doc = " Search for a key within a certain range in a sorted vector."] # [doc = " Returns `None` if the range is out of bounds, and"] # [doc = " `Ok` or `Err` in the same way as `zvl_binary_search`."] # [doc = " Indices are returned relative to the start of the range."] fn zvl_binary_search_in_range (& self , k : & T , range : Range < usize > ,) -> Option < Result < usize , usize > > where T : Ord ; # [doc = " Search for a key in a sorted vector by a predicate, returns `Ok(index)` if found,"] # [doc = " returns `Err(insert_index)` if not found, where `insert_index` is the"] # [doc = " index where it should be inserted to maintain sort order."] fn zvl_binary_search_by (& self , predicate : impl FnMut (& T) -> Ordering) -> Result < usize , usize > ; # [doc = " Search for a key within a certain range in a sorted vector by a predicate."] # [doc = " Returns `None` if the range is out of bounds, and"] # [doc = " `Ok` or `Err` in the same way as `zvl_binary_search`."] # [doc = " Indices are returned relative to the start of the range."] fn zvl_binary_search_in_range_by (& self , predicate : impl FnMut (& T) -> Ordering , range : Range < usize > ,) -> Option < Result < usize , usize > > ; # [doc = " Get element at `index`"] fn zvl_get (& self , index : usize) -> Option < & Self :: GetType > ; # [doc = " The length of this vector"] fn zvl_len (& self) -> usize ; # [doc = " Check if this vector is in ascending order according to `T`s `Ord` impl"] fn zvl_is_ascending (& self) -> bool where T : Ord , { if let Some (first) = self . zvl_get (0) { let mut prev = first ; for i in 1 .. self . zvl_len () { # [expect (clippy :: unwrap_used)] let curr = self . zvl_get (i) . unwrap () ; if Self :: get_cmp_get (prev , curr) != Ordering :: Less { return false ; } prev = curr ; } } true } # [doc = " Check if this vector is empty"] fn zvl_is_empty (& self) -> bool { self . zvl_len () == 0 } # [doc = " Construct a borrowed variant by borrowing from `&self`."] # [doc = ""] # [doc = " This function behaves like `&'b self -> Self::SliceVariant<'b>`,"] # [doc = " where `'b` is the lifetime of the reference to this object."] # [doc = ""] # [doc = " Note: We rely on the compiler recognizing `'a` and `'b` as covariant and"] # [doc = " casting `&'b Self<'a>` to `&'b Self<'b>` when this gets called, which works"] # [doc = " out for `ZeroVec` and `VarZeroVec` containers just fine."] fn zvl_as_borrowed (& self) -> & Self :: SliceVariant ; # [doc = " Compare this type with a `Self::GetType`. This must produce the same result as"] # [doc = " if `g` were converted to `Self`"] # [inline] fn t_cmp_get (t : & T , g : & Self :: GetType) -> Ordering where T : Ord , { Self :: zvl_get_as_t (g , | g | t . cmp (g)) } # [doc = " Compare two values of `Self::GetType`. This must produce the same result as"] # [doc = " if both `a` and `b` were converted to `Self`"] # [inline] fn get_cmp_get (a : & Self :: GetType , b : & Self :: GetType) -> Ordering where T : Ord , { Self :: zvl_get_as_t (a , | a | Self :: zvl_get_as_t (b , | b | a . cmp (b))) } # [doc = " Obtain a reference to T, passed to a closure"] # [doc = ""] # [doc = " This uses a callback because it's not possible to return owned-or-borrowed"] # [doc = " types without GATs"] # [doc = ""] # [doc = " Impls should guarantee that the callback function is be called exactly once."] fn zvl_get_as_t < R > (g : & Self :: GetType , f : impl FnOnce (& T) -> R) -> R ; }
};
}
