// Generated macro for impl_1030 (impl)
macro_rules! Depcrate_base_normimpl_1030 {
() => {
// Module: crate::base::norm
// Provides: {"impl_1030"}
// Dependencies: {}
# [doc = " # Basis and orthogonalization"] impl < T : ComplexField , D : DimName > OVector < T , D > where DefaultAllocator : Allocator < D > , { # [doc = " The i-the canonical basis element."] # [inline] fn canonical_basis_element (i : usize) -> Self { let mut res = Self :: zero () ; res [i] = T :: one () ; res } # [doc = " Orthonormalizes the given family of vectors. The largest free family of vectors is moved at"] # [doc = " the beginning of the array and its size is returned. Vectors at an indices larger or equal to"] # [doc = " this length can be modified to an arbitrary value."] # [inline] pub fn orthonormalize (vs : & mut [Self]) -> usize { let mut nbasis_elements = 0 ; for i in 0 .. vs . len () { { let (elt , basis) = vs [.. i + 1] . split_last_mut () . unwrap () ; for basis_element in & basis [.. nbasis_elements] { * elt -= basis_element * elt . dot (basis_element) } } if vs [i] . try_normalize_mut (T :: RealField :: zero ()) . is_some () { vs . swap (nbasis_elements , i) ; nbasis_elements += 1 ; if nbasis_elements == D :: dim () { break ; } } } nbasis_elements } # [doc = " Applies the given closure to each element of the orthonormal basis of the subspace"] # [doc = " orthogonal to free family of vectors `vs`. If `vs` is not a free family, the result is"] # [doc = " unspecified."] # [inline] pub fn orthonormal_subspace_basis < F > (vs : & [Self] , mut f : F) where F : FnMut (& Self) -> bool , { assert ! (vs . len () <= D :: dim () , "The given set of vectors has no chance of being a free family.") ; match D :: dim () { 1 => { if vs . is_empty () { let _ = f (& Self :: canonical_basis_element (0)) ; } } 2 => { if vs . is_empty () { let _ = f (& Self :: canonical_basis_element (0)) && f (& Self :: canonical_basis_element (1)) ; } else if vs . len () == 1 { let v = & vs [0] ; let res = Self :: from_column_slice (& [- v [1] . clone () , v [0] . clone ()]) ; let _ = f (& res . normalize ()) ; } } 3 => { if vs . is_empty () { let _ = f (& Self :: canonical_basis_element (0)) && f (& Self :: canonical_basis_element (1)) && f (& Self :: canonical_basis_element (2)) ; } else if vs . len () == 1 { let v = & vs [0] ; let mut a ; if v [0] . clone () . norm1 () > v [1] . clone () . norm1 () { a = Self :: from_column_slice (& [v [2] . clone () , T :: zero () , - v [0] . clone ()]) ; } else { a = Self :: from_column_slice (& [T :: zero () , - v [2] . clone () , v [1] . clone ()]) ; } ; let _ = a . normalize_mut () ; if f (& a . cross (v)) { let _ = f (& a) ; } } else if vs . len () == 2 { let _ = f (& vs [0] . cross (& vs [1]) . normalize ()) ; } } _ => { # [cfg (any (feature = "std" , feature = "alloc"))] { let mut known_basis = Vec :: new () ; for v in vs . iter () { known_basis . push (v . normalize ()) } for i in 0 .. D :: dim () - vs . len () { let mut elt = Self :: canonical_basis_element (i) ; for v in & known_basis { elt -= v * elt . dot (v) } if let Some (subsp_elt) = elt . try_normalize (T :: RealField :: zero ()) { if ! f (& subsp_elt) { return ; } ; known_basis . push (subsp_elt) ; } } } # [cfg (all (not (feature = "std") , not (feature = "alloc")))] { panic ! ("Cannot compute the orthogonal subspace basis of a vector with a dimension greater than 3 \
                            if #![no_std] is enabled and the 'alloc' feature is not enabled.") } } } } }
};
}
