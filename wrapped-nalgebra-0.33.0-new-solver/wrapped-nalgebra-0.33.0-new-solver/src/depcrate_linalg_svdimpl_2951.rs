// Generated macro for impl_2951 (impl)
macro_rules! Depcrate_linalg_svdimpl_2951 {
() => {
// Module: crate::linalg::svd
// Provides: {"impl_2951"}
// Dependencies: {}
impl < T : ComplexField , R : DimMin < C > , C : Dim > SVD < T , R , C > where DimMinimum < R , C > : DimSub < U1 > , DefaultAllocator : Allocator < R , C > + Allocator < C > + Allocator < R > + Allocator < DimDiff < DimMinimum < R , C > , U1 > > + Allocator < DimMinimum < R , C > , C > + Allocator < R , DimMinimum < R , C > > + Allocator < DimMinimum < R , C > > , { # [doc = " Computes the Singular Value Decomposition of `matrix` using implicit shift."] # [doc = " The singular values are guaranteed to be sorted in descending order."] # [doc = " If this order is not required consider using `new_unordered`."] pub fn new (matrix : OMatrix < T , R , C > , compute_u : bool , compute_v : bool) -> Self { let mut svd = Self :: new_unordered (matrix , compute_u , compute_v) ; if ! Self :: use_special_always_ordered_svd3 () && ! Self :: use_special_always_ordered_svd2 () { svd . sort_by_singular_values () ; } svd } # [doc = " Attempts to compute the Singular Value Decomposition of `matrix` using implicit shift."] # [doc = " The singular values are guaranteed to be sorted in descending order."] # [doc = " If this order is not required consider using `try_new_unordered`."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `compute_u` − set this to `true` to enable the computation of left-singular vectors."] # [doc = " * `compute_v` − set this to `true` to enable the computation of right-singular vectors."] # [doc = " * `eps`       − tolerance used to determine when a value converged to 0."] # [doc = " * `max_niter` − maximum total number of iterations performed by the algorithm. If this"] # [doc = " number of iteration is exceeded, `None` is returned. If `niter == 0`, then the algorithm"] # [doc = " continues indefinitely until convergence."] pub fn try_new (matrix : OMatrix < T , R , C > , compute_u : bool , compute_v : bool , eps : T :: RealField , max_niter : usize ,) -> Option < Self > { Self :: try_new_unordered (matrix , compute_u , compute_v , eps , max_niter) . map (| mut svd | { if ! Self :: use_special_always_ordered_svd3 () && ! Self :: use_special_always_ordered_svd2 () { svd . sort_by_singular_values () ; } svd }) } # [doc = " Sort the estimated components of the SVD by its singular values in descending order."] # [doc = " Such an ordering is often implicitly required when the decompositions are used for estimation or fitting purposes."] # [doc = " Using this function is only required if `new_unordered` or `try_new_unordered` were used and the specific sorting is required afterward."] pub fn sort_by_singular_values (& mut self) { const VALUE_PROCESSED : usize = usize :: MAX ; let mut singular_values = self . singular_values . map_with_location (| r , _ , e | (e , r)) ; assert_ne ! (singular_values . data . shape () . 0 . value () , VALUE_PROCESSED , "Too many singular values") ; singular_values . as_mut_slice () . sort_unstable_by (| (a , _) , (b , _) | b . partial_cmp (a) . expect ("Singular value was NaN")) ; self . singular_values . zip_apply (& singular_values , | value , (new_value , _) | { value . clone_from (& new_value) }) ; let mut permutations = crate :: PermutationSequence :: identity_generic (singular_values . data . shape () . 0) ; for i in 0 .. singular_values . len () { let mut index_1 = i ; let mut index_2 = singular_values [i] . 1 ; while index_2 != VALUE_PROCESSED && singular_values [index_2] . 1 != VALUE_PROCESSED { permutations . append_permutation (index_1 , index_2) ; singular_values [index_1] . 1 = VALUE_PROCESSED ; index_1 = index_2 ; index_2 = singular_values [index_1] . 1 ; } } if let Some (u) = self . u . as_mut () { permutations . permute_columns (u) ; } if let Some (v_t) = self . v_t . as_mut () { permutations . permute_rows (v_t) ; } } }
};
}
