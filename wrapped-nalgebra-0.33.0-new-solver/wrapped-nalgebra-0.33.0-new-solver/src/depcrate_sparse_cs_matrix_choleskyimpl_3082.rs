// Generated macro for impl_3082 (impl)
macro_rules! Depcrate_sparse_cs_matrix_choleskyimpl_3082 {
() => {
// Module: crate::sparse::cs_matrix_cholesky
// Provides: {"impl_3082"}
// Dependencies: {}
impl < T : RealField , D : Dim > CsCholesky < T , D > where DefaultAllocator : Allocator < D > + Allocator < D > , { # [doc = " Computes the cholesky decomposition of the sparse matrix `m`."] pub fn new (m : & CsMatrix < T , D , D >) -> Self { let mut me = Self :: new_symbolic (m) ; let _ = me . decompose_left_looking (& m . data . vals) ; me } # [doc = " Perform symbolic analysis for the given matrix."] # [doc = ""] # [doc = " This does not access the numerical values of `m`."] pub fn new_symbolic (m : & CsMatrix < T , D , D >) -> Self { assert ! (m . is_square () , "The matrix `m` must be square to compute its elimination tree.") ; let (l , u) = Self :: nonzero_pattern (m) ; let work_x = Matrix :: zeros_generic (m . data . shape () . 0 , Const :: < 1 >) ; let work_c = Matrix :: zeros_generic (m . data . shape () . 1 , Const :: < 1 >) ; let mut original_p = m . data . p . as_slice () . to_vec () ; original_p . push (m . data . i . len ()) ; CsCholesky { original_p , original_i : m . data . i . clone () , l , u , ok : false , work_x , work_c , } } # [doc = " The lower-triangular matrix of the cholesky decomposition."] # [must_use] pub fn l (& self) -> Option < & CsMatrix < T , D , D > > { if self . ok { Some (& self . l) } else { None } } # [doc = " Extracts the lower-triangular matrix of the cholesky decomposition."] pub fn unwrap_l (self) -> Option < CsMatrix < T , D , D > > { if self . ok { Some (self . l) } else { None } } # [doc = " Perform a numerical left-looking cholesky decomposition of a matrix with the same structure as the"] # [doc = " one used to initialize `self`, but with different non-zero values provided by `values`."] pub fn decompose_left_looking (& mut self , values : & [T]) -> bool { assert ! (values . len () >= self . original_i . len () , "The set of values is too small.") ; let n = self . l . nrows () ; self . work_c . copy_from (& self . l . data . p) ; unsafe { for k in 0 .. n { let range_k = * self . original_p . get_unchecked (k) .. * self . original_p . get_unchecked (k + 1) ; * self . work_x . vget_unchecked_mut (k) = T :: zero () ; for p in range_k . clone () { let irow = * self . original_i . get_unchecked (p) ; if irow >= k { * self . work_x . vget_unchecked_mut (irow) = values . get_unchecked (p) . clone () ; } } for j in self . u . data . column_row_indices (k) { let factor = - self . l . data . vals . get_unchecked (* self . work_c . vget_unchecked (j)) . clone () ; * self . work_c . vget_unchecked_mut (j) += 1 ; if j < k { for (z , val) in self . l . data . column_entries (j) { if z >= k { * self . work_x . vget_unchecked_mut (z) += val * factor . clone () ; } } } } let diag = self . work_x . vget_unchecked (k) . clone () ; if diag > T :: zero () { let denom = diag . sqrt () ; * self . l . data . vals . get_unchecked_mut (* self . l . data . p . vget_unchecked (k)) = denom . clone () ; for (p , val) in self . l . data . column_entries_mut (k) { * val = self . work_x . vget_unchecked (p) . clone () / denom . clone () ; * self . work_x . vget_unchecked_mut (p) = T :: zero () ; } } else { self . ok = false ; return false ; } } } self . ok = true ; true } # [doc = " Perform a numerical up-looking cholesky decomposition of a matrix with the same structure as the"] # [doc = " one used to initialize `self`, but with different non-zero values provided by `values`."] pub fn decompose_up_looking (& mut self , values : & [T]) -> bool { assert ! (values . len () >= self . original_i . len () , "The set of values is too small.") ; self . work_c . copy_from (& self . l . data . p) ; for k in 0 .. self . l . nrows () { unsafe { let column_range = * self . original_p . get_unchecked (k) .. * self . original_p . get_unchecked (k + 1) ; * self . work_x . vget_unchecked_mut (k) = T :: zero () ; for p in column_range . clone () { let irow = * self . original_i . get_unchecked (p) ; if irow <= k { * self . work_x . vget_unchecked_mut (irow) = values . get_unchecked (p) . clone () ; } } let mut diag = self . work_x . vget_unchecked (k) . clone () ; * self . work_x . vget_unchecked_mut (k) = T :: zero () ; for irow in self . u . data . column_row_indices (k) { if irow >= k { continue ; } let lki = self . work_x . vget_unchecked (irow) . clone () / self . l . data . vals . get_unchecked (* self . l . data . p . vget_unchecked (irow)) . clone () ; * self . work_x . vget_unchecked_mut (irow) = T :: zero () ; for p in * self . l . data . p . vget_unchecked (irow) + 1 .. * self . work_c . vget_unchecked (irow) { * self . work_x . vget_unchecked_mut (* self . l . data . i . get_unchecked (p)) -= self . l . data . vals . get_unchecked (p) . clone () * lki . clone () ; } diag -= lki . clone () * lki . clone () ; let p = * self . work_c . vget_unchecked (irow) ; * self . work_c . vget_unchecked_mut (irow) += 1 ; * self . l . data . i . get_unchecked_mut (p) = k ; * self . l . data . vals . get_unchecked_mut (p) = lki ; } if diag <= T :: zero () { self . ok = false ; return false ; } let p = * self . work_c . vget_unchecked (k) ; * self . work_c . vget_unchecked_mut (k) += 1 ; * self . l . data . i . get_unchecked_mut (p) = k ; * self . l . data . vals . get_unchecked_mut (p) = diag . sqrt () ; } } self . ok = true ; true } fn elimination_tree < S : CsStorage < T , D , D > > (m : & CsMatrix < T , D , D , S >) -> Vec < usize > { let nrows = m . nrows () ; let mut forest : Vec < _ > = iter :: repeat (usize :: max_value ()) . take (nrows) . collect () ; let mut ancestor : Vec < _ > = iter :: repeat (usize :: max_value ()) . take (nrows) . collect () ; for k in 0 .. nrows { for irow in m . data . column_row_indices (k) { let mut i = irow ; while i < k { let i_ancestor = ancestor [i] ; ancestor [i] = k ; if i_ancestor == usize :: max_value () { forest [i] = k ; break ; } i = i_ancestor ; } } } forest } fn reach < S : CsStorage < T , D , D > > (m : & CsMatrix < T , D , D , S > , j : usize , max_j : usize , tree : & [usize] , marks : & mut Vec < bool > , out : & mut Vec < usize > ,) { marks . clear () ; marks . resize (tree . len () , false) ; let mut tmp = Vec :: new () ; let mut res = Vec :: new () ; for irow in m . data . column_row_indices (j) { let mut curr = irow ; while curr != usize :: max_value () && curr <= max_j && ! marks [curr] { marks [curr] = true ; tmp . push (curr) ; curr = tree [curr] ; } tmp . append (& mut res) ; mem :: swap (& mut tmp , & mut res) ; } out . append (& mut res) ; } fn nonzero_pattern < S : CsStorage < T , D , D > > (m : & CsMatrix < T , D , D , S > ,) -> (CsMatrix < T , D , D > , CsMatrix < T , D , D >) { let etree = Self :: elimination_tree (m) ; let (nrows , ncols) = m . data . shape () ; let mut rows = Vec :: with_capacity (m . len ()) ; let mut cols = Matrix :: zeros_generic (m . data . shape () . 0 , Const :: < 1 >) ; let mut marks = Vec :: new () ; for i in 0 .. nrows . value () { cols [i] = rows . len () ; Self :: reach (m , i , i , & etree , & mut marks , & mut rows) ; } let mut vals = Vec :: with_capacity (rows . len ()) ; unsafe { vals . set_len (rows . len ()) ; } vals . shrink_to_fit () ; let data = CsVecStorage { shape : (nrows , ncols) , p : cols , i : rows , vals , } ; let u = CsMatrix :: from_data (data) ; let l = u . transpose () ; (l , u) } }
};
}
