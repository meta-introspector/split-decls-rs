// Generated macro for RawStorageMut (trait)
macro_rules! Depcrate_base_storageRawStorageMut {
() => {
// Module: crate::base::storage
// Provides: {"RawStorageMut"}
// Dependencies: {}
# [doc = " Trait implemented by matrix data storage that can provide a mutable access to its elements."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " In generic code, it is recommended use the `StorageMut` trait bound instead. The"] # [doc = " `RawStorageMut` trait bound is generally used by code that needs to work with storages that"] # [doc = " contains `MaybeUninit<T>` elements."] # [doc = ""] # [doc = " Note that a mutable access does not mean that the matrix owns its data. For example, a mutable"] # [doc = " matrix view can provide mutable access to its elements even if it does not own its data (it"] # [doc = " contains only an internal reference to them)."] pub unsafe trait RawStorageMut < T , R : Dim , C : Dim = U1 > : RawStorage < T , R , C > { # [doc = " The matrix mutable data pointer."] fn ptr_mut (& mut self) -> * mut T ; # [doc = " Gets the mutable address of the i-th matrix component without performing bound-checking."] # [doc = ""] # [doc = " # Safety"] # [doc = " If the index is out of bounds, dereferencing the result will cause undefined behavior."] # [inline] fn get_address_unchecked_linear_mut (& mut self , i : usize) -> * mut T { self . ptr_mut () . wrapping_add (i) } # [doc = " Gets the mutable address of the i-th matrix component without performing bound-checking."] # [doc = ""] # [doc = " # Safety"] # [doc = " If the index is out of bounds, dereferencing the result will cause undefined behavior."] # [inline] fn get_address_unchecked_mut (& mut self , irow : usize , icol : usize) -> * mut T { let lid = self . linear_index (irow , icol) ; self . get_address_unchecked_linear_mut (lid) } # [doc = " Retrieves a mutable reference to the i-th element without bound-checking."] # [doc = ""] # [doc = " # Safety"] # [doc = " If the index is out of bounds, the method will cause undefined behavior."] unsafe fn get_unchecked_linear_mut (& mut self , i : usize) -> & mut T { & mut * self . get_address_unchecked_linear_mut (i) } # [doc = " Retrieves a mutable reference to the element at `(irow, icol)` without bound-checking."] # [doc = ""] # [doc = " # Safety"] # [doc = " If the index is out of bounds, the method will cause undefined behavior."] # [inline] unsafe fn get_unchecked_mut (& mut self , irow : usize , icol : usize) -> & mut T { & mut * self . get_address_unchecked_mut (irow , icol) } # [doc = " Swaps two elements using their linear index without bound-checking."] # [doc = ""] # [doc = " # Safety"] # [doc = " If the indices are out of bounds, the method will cause undefined behavior."] # [doc = ""] # [doc = " # Validity"] # [doc = " The default implementation of this trait function is only guaranteed to be"] # [doc = " sound if invocations of `self.ptr_mut()` and `self.get_address_unchecked_linear_mut()`"] # [doc = " result in stable references. If any of the data pointed to by these trait methods"] # [doc = " moves as a consequence of invoking either of these methods then this default"] # [doc = " trait implementation may be invalid or unsound and should be overridden."] # [inline] unsafe fn swap_unchecked_linear (& mut self , i1 : usize , i2 : usize) { let base = self . ptr_mut () ; let offset1 = self . get_address_unchecked_linear_mut (i1) . offset_from (base) ; let offset2 = self . get_address_unchecked_linear_mut (i2) . offset_from (base) ; let base = self . ptr_mut () ; let a = base . offset (offset1) ; let b = base . offset (offset2) ; ptr :: swap (a , b) ; } # [doc = " Swaps two elements without bound-checking."] # [doc = ""] # [doc = " # Safety"] # [doc = " If the indices are out of bounds, the method will cause undefined behavior."] # [inline] unsafe fn swap_unchecked (& mut self , row_col1 : (usize , usize) , row_col2 : (usize , usize)) { let lid1 = self . linear_index (row_col1 . 0 , row_col1 . 1) ; let lid2 = self . linear_index (row_col2 . 0 , row_col2 . 1) ; self . swap_unchecked_linear (lid1 , lid2) } # [doc = " Retrieves the mutable data buffer as a contiguous slice."] # [doc = ""] # [doc = " Matrix components may not be contiguous, depending on its strides.    "] # [doc = ""] # [doc = " # Safety"] # [doc = " The matrix components may not be stored in a contiguous way, depending on the strides."] # [doc = " This method is unsafe because this can yield to invalid aliasing when called on some pairs"] # [doc = " of matrix slices originating from the same matrix with strides."] unsafe fn as_mut_slice_unchecked (& mut self) -> & mut [T] ; }
};
}
