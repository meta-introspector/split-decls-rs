// Generated macro for impl_1026 (impl)
macro_rules! Depcrate_base_normimpl_1026 {
() => {
// Module: crate::base::norm
// Provides: {"impl_1026"}
// Dependencies: {}
# [doc = " # Magnitude and norms"] impl < T : Scalar , R : Dim , C : Dim , S : Storage < T , R , C > > Matrix < T , R , C , S > { # [doc = " The squared L2 norm of this vector."] # [inline] # [must_use] pub fn norm_squared (& self) -> T :: SimdRealField where T : SimdComplexField , { let mut res = T :: SimdRealField :: zero () ; for i in 0 .. self . ncols () { let col = self . column (i) ; res += col . dotc (& col) . simd_real () } res } # [doc = " The L2 norm of this matrix."] # [doc = ""] # [doc = " Use `.apply_norm` to apply a custom norm."] # [inline] # [must_use] pub fn norm (& self) -> T :: SimdRealField where T : SimdComplexField , { self . norm_squared () . simd_sqrt () } # [doc = " Compute the distance between `self` and `rhs` using the metric induced by the euclidean norm."] # [doc = ""] # [doc = " Use `.apply_metric_distance` to apply a custom norm."] # [inline] # [must_use] pub fn metric_distance < R2 , C2 , S2 > (& self , rhs : & Matrix < T , R2 , C2 , S2 >) -> T :: SimdRealField where T : SimdComplexField , R2 : Dim , C2 : Dim , S2 : Storage < T , R2 , C2 > , ShapeConstraint : SameNumberOfRows < R , R2 > + SameNumberOfColumns < C , C2 > , { self . apply_metric_distance (rhs , & EuclideanNorm) } # [doc = " Uses the given `norm` to compute the norm of `self`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use nalgebra::{Vector3, UniformNorm, LpNorm, EuclideanNorm};"] # [doc = ""] # [doc = " let v = Vector3::new(1.0, 2.0, 3.0);"] # [doc = " assert_eq!(v.apply_norm(&UniformNorm), 3.0);"] # [doc = " assert_eq!(v.apply_norm(&LpNorm(1)), 6.0);"] # [doc = " assert_eq!(v.apply_norm(&EuclideanNorm), v.norm());"] # [doc = " ```"] # [inline] # [must_use] pub fn apply_norm (& self , norm : & impl Norm < T >) -> T :: SimdRealField where T : SimdComplexField , { norm . norm (self) } # [doc = " Uses the metric induced by the given `norm` to compute the metric distance between `self` and `rhs`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use nalgebra::{Vector3, UniformNorm, LpNorm, EuclideanNorm};"] # [doc = ""] # [doc = " let v1 = Vector3::new(1.0, 2.0, 3.0);"] # [doc = " let v2 = Vector3::new(10.0, 20.0, 30.0);"] # [doc = ""] # [doc = " assert_eq!(v1.apply_metric_distance(&v2, &UniformNorm), 27.0);"] # [doc = " assert_eq!(v1.apply_metric_distance(&v2, &LpNorm(1)), 27.0 + 18.0 + 9.0);"] # [doc = " assert_eq!(v1.apply_metric_distance(&v2, &EuclideanNorm), (v1 - v2).norm());"] # [doc = " ```"] # [inline] # [must_use] pub fn apply_metric_distance < R2 , C2 , S2 > (& self , rhs : & Matrix < T , R2 , C2 , S2 > , norm : & impl Norm < T > ,) -> T :: SimdRealField where T : SimdComplexField , R2 : Dim , C2 : Dim , S2 : Storage < T , R2 , C2 > , ShapeConstraint : SameNumberOfRows < R , R2 > + SameNumberOfColumns < C , C2 > , { norm . metric_distance (self , rhs) } # [doc = " A synonym for the norm of this matrix."] # [doc = ""] # [doc = " Aka the length."] # [doc = ""] # [doc = " This function is simply implemented as a call to `norm()`"] # [inline] # [must_use] pub fn magnitude (& self) -> T :: SimdRealField where T : SimdComplexField , { self . norm () } # [doc = " A synonym for the squared norm of this matrix."] # [doc = ""] # [doc = " Aka the squared length."] # [doc = ""] # [doc = " This function is simply implemented as a call to `norm_squared()`"] # [inline] # [must_use] pub fn magnitude_squared (& self) -> T :: SimdRealField where T : SimdComplexField , { self . norm_squared () } # [doc = " Sets the magnitude of this vector."] # [inline] pub fn set_magnitude (& mut self , magnitude : T :: SimdRealField) where T : SimdComplexField , S : StorageMut < T , R , C > , { let n = self . norm () ; self . scale_mut (magnitude / n) } # [doc = " Returns a normalized version of this matrix."] # [inline] # [must_use = "Did you mean to use normalize_mut()?"] pub fn normalize (& self) -> OMatrix < T , R , C > where T : SimdComplexField , DefaultAllocator : Allocator < R , C > , { self . unscale (self . norm ()) } # [doc = " The Lp norm of this matrix."] # [inline] # [must_use] pub fn lp_norm (& self , p : i32) -> T :: SimdRealField where T : SimdComplexField , { self . apply_norm (& LpNorm (p)) } # [doc = " Attempts to normalize `self`."] # [doc = ""] # [doc = " The components of this matrix can be SIMD types."] # [inline] # [must_use = "Did you mean to use simd_try_normalize_mut()?"] pub fn simd_try_normalize (& self , min_norm : T :: SimdRealField) -> SimdOption < OMatrix < T , R , C > > where T : SimdComplexField , T :: Element : Scalar , DefaultAllocator : Allocator < R , C > , { let n = self . norm () ; let le = n . clone () . simd_le (min_norm) ; let val = self . unscale (n) ; SimdOption :: new (val , le) } # [doc = " Sets the magnitude of this vector unless it is smaller than `min_magnitude`."] # [doc = ""] # [doc = " If `self.magnitude()` is smaller than `min_magnitude`, it will be left unchanged."] # [doc = " Otherwise this is equivalent to: `*self = self.normalize() * magnitude`."] # [inline] pub fn try_set_magnitude (& mut self , magnitude : T :: RealField , min_magnitude : T :: RealField) where T : ComplexField , S : StorageMut < T , R , C > , { let n = self . norm () ; if n > min_magnitude { self . scale_mut (magnitude / n) } } # [doc = " Returns a new vector with the same magnitude as `self` clamped between `0.0` and `max`."] # [inline] # [must_use] pub fn cap_magnitude (& self , max : T :: RealField) -> OMatrix < T , R , C > where T : ComplexField , DefaultAllocator : Allocator < R , C > , { let n = self . norm () ; if n > max { self . scale (max / n) } else { self . clone_owned () } } # [doc = " Returns a new vector with the same magnitude as `self` clamped between `0.0` and `max`."] # [inline] # [must_use] pub fn simd_cap_magnitude (& self , max : T :: SimdRealField) -> OMatrix < T , R , C > where T : SimdComplexField , T :: Element : Scalar , DefaultAllocator : Allocator < R , C > , { let n = self . norm () ; let scaled = self . scale (max . clone () / n . clone ()) ; let use_scaled = n . simd_gt (max) ; scaled . select (use_scaled , self . clone_owned ()) } # [doc = " Returns a normalized version of this matrix unless its norm as smaller or equal to `eps`."] # [doc = ""] # [doc = " The components of this matrix cannot be SIMD types (see `simd_try_normalize`) instead."] # [inline] # [must_use = "Did you mean to use try_normalize_mut()?"] pub fn try_normalize (& self , min_norm : T :: RealField) -> Option < OMatrix < T , R , C > > where T : ComplexField , DefaultAllocator : Allocator < R , C > , { let n = self . norm () ; if n <= min_norm { None } else { Some (self . unscale (n)) } } }
};
}
