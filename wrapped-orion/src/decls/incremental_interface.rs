macro_rules! incremental_interface {
    () => {
        # [doc = " Tests for a streaming context that offers incremental processing."] pub mod incremental_interface ;
    };
}

incremental_interface!();