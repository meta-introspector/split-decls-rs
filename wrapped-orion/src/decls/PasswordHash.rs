macro_rules! PasswordHash {
    () => {
        # [doc = " A type to represent the `PasswordHash` that Argon2i returns when used for password hashing."] # [doc = ""] # [doc = ""] # [doc = " # Errors:"] # [doc = " An error will be returned if:"] # [doc = " - The encoded password hash contains whitespace."] # [doc = " - The encoded password hash has a parallelism count other than 1."] # [doc = " - The encoded password contains any other fields than: The algorithm name,"] # [doc = "   version, m, t, p and the salt and password hash."] # [doc = " - The encoded password hash contains invalid Base64 encoding."] # [doc = " - Any decimal parameter value, such as m, contains leading zeroes and is longer"] # [doc = "   than a single character."] # [doc = " - `iterations` is less than 3."] # [doc = " - `memory` is less than 8."] # [doc = " - `password` is not 32 bytes."] # [doc = " - `salt` is not 16 bytes."] # [doc = " - The encoded password hash contains numerical values that cannot"] # [doc = "   be represented as a `u32`."] # [doc = " - The encoded password hash length is less than [`PasswordHash::MIN_ENCODED_LEN`] or greater than [`PasswordHash::MAX_ENCODED_LEN`]."] # [doc = " - The parameters in the encoded password hash are not correctly ordered. The ordering must be:"] # [doc = "   `$argon2i$v=19$m=<value>,t=<value>,p=<value>$<salt>$<hash>`"] # [doc = " # Panics:"] # [doc = " A panic will occur if:"] # [doc = " - Overflowing calculations happen on `usize` when decoding the password and salt from Base64."] # [doc = ""] # [doc = " # Security:"] # [doc = " - __**Avoid using**__ `unprotected_as_bytes()` whenever possible, as it breaks all protections"] # [doc = "   that the type implements."] # [doc = " - Never use `unprotected_as_bytes()` or `unprotected_as_encoded()` to compare password hashes,"] # [doc = "   as that will not run in constant-time. Compare `PasswordHash`es directly using `==` instead."] # [doc = " - The trait `PartialEq<&'_ [u8]>` is implemented for this type so that users are not tempted"] # [doc = "   to call `unprotected_as_bytes` to compare this sensitive value to a byte slice. The trait"] # [doc = "   is implemented in such a way that the comparison happens in constant time. Thus, users should"] # [doc = "   prefer `SecretType == &[u8]` over `SecretType.unprotected_as_bytes() == &[u8]`."] # [doc = ""] # [doc = " Examples are shown below. The examples apply to any type that implements `PartialEq<&'_ [u8]>`."] # [doc = " ```rust"] # [doc = " use orion::hazardous::mac::hmac::sha512::Tag;"] # [doc = " # use orion::errors::UnknownCryptoError;"] # [doc = ""] # [doc = " # fn main() -> Result<(), Box<UnknownCryptoError>> {"] # [doc = " // Initialize an arbitrary, 64-byte tag."] # [doc = " let tag = Tag::from_slice(&[1; 64])?;"] # [doc = ""] # [doc = " // Secure, constant-time comparison with a byte slice"] # [doc = " assert_eq!(tag, &[1; 64][..]);"] # [doc = ""] # [doc = " // Secure, constant-time comparison with another Tag"] # [doc = " assert_eq!(tag, Tag::from_slice(&[1; 64])?);"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] pub struct PasswordHash { encoded_password_hash : String , password_hash : Vec < u8 > , salt : Salt , iterations : u32 , memory : u32 , }
    };
}

PasswordHash!();