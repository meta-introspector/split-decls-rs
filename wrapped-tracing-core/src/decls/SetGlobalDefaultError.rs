macro_rules! SetGlobalDefaultError {
    () => {
        # [doc = " Returned if setting the global dispatcher fails."] pub struct SetGlobalDefaultError { _no_construct : () , }
    };
}

SetGlobalDefaultError!();