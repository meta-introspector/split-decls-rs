// Generated macro for impl_174 (impl)
macro_rules! Depcrateimpl_174 {
() => {
// Module: crate
// Provides: {"impl_174"}
// Dependencies: {}
impl JsString { # [doc = " Returns the `JsString` value of this JS value if it's an instance of a"] # [doc = " string."] # [doc = ""] # [doc = " If this JS value is not an instance of a string then this returns"] # [doc = " `None`."] # [deprecated (note = "recommended to use dyn_ref instead which is now equivalent")] pub fn try_from (val : & JsValue) -> Option < & JsString > { val . dyn_ref () } # [doc = " Returns whether this string is a valid UTF-16 string."] # [doc = ""] # [doc = " This is useful for learning whether `String::from(..)` will return a"] # [doc = " lossless representation of the JS string. If this string contains"] # [doc = " unpaired surrogates then `String::from` will succeed but it will be a"] # [doc = " lossy representation of the JS string because unpaired surrogates will"] # [doc = " become replacement characters."] # [doc = ""] # [doc = " If this function returns `false` then to get a lossless representation"] # [doc = " of the string you'll need to manually use the `iter` method (or the"] # [doc = " `char_code_at` accessor) to view the raw character codes."] # [doc = ""] # [doc = " For more information, see the documentation on [JS strings vs Rust"] # [doc = " strings][docs]"] # [doc = ""] # [doc = " [docs]: https://wasm-bindgen.github.io/wasm-bindgen/reference/types/str.html"] pub fn is_valid_utf16 (& self) -> bool { core :: char :: decode_utf16 (self . iter ()) . all (| i | i . is_ok ()) } # [doc = " Returns an iterator over the `u16` character codes that make up this JS"] # [doc = " string."] # [doc = ""] # [doc = " This method will call `char_code_at` for each code in this JS string,"] # [doc = " returning an iterator of the codes in sequence."] pub fn iter (& self ,) -> impl ExactSizeIterator < Item = u16 > + DoubleEndedIterator < Item = u16 > + '_ { (0 .. self . length ()) . map (move | i | self . char_code_at (i) as u16) } # [doc = " If this string consists of a single Unicode code point, then this method"] # [doc = " converts it into a Rust `char` without doing any allocations."] # [doc = ""] # [doc = " If this JS value is not a valid UTF-8 or consists of more than a single"] # [doc = " codepoint, then this returns `None`."] # [doc = ""] # [doc = " Note that a single Unicode code point might be represented as more than"] # [doc = " one code unit on the JavaScript side. For example, a JavaScript string"] # [doc = " `\"\\uD801\\uDC37\"` is actually a single Unicode code point U+10437 which"] # [doc = " corresponds to a character '𐐷'."] pub fn as_char (& self) -> Option < char > { let len = self . length () ; if len == 0 || len > 2 { return None ; } let cp = self . code_point_at (0) . as_f64 () . unwrap_throw () as u32 ; let c = core :: char :: from_u32 (cp) ? ; if c . len_utf16 () as u32 == len { Some (c) } else { None } } }
};
}
