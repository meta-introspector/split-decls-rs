// Generated macro for impl_391 (impl)
macro_rules! Depcrate_serverimpl_391 {
() => {
// Module: crate::server
// Provides: {"impl_391"}
// Dependencies: {}
impl rustls_server_config_builder { # [doc = " Create a rustls_server_config_builder using the process default crypto provider."] # [doc = ""] # [doc = " Caller owns the memory and must eventually call rustls_server_config_builder_build,"] # [doc = " then free the resulting rustls_server_config."] # [doc = ""] # [doc = " Alternatively, if an error occurs or, you don't wish to build a config, call"] # [doc = " `rustls_server_config_builder_free` to free the builder directly."] # [doc = ""] # [doc = " This uses the process default provider's values for the cipher suites and key exchange"] # [doc = " groups, as well as safe defaults for protocol versions."] # [no_mangle] pub extern "C" fn rustls_server_config_builder_new () -> * mut rustls_server_config_builder { ffi_panic_boundary ! { let builder = ServerConfigBuilder { provider : crypto_provider :: get_default_or_install_from_crate_features () , versions : rustls :: DEFAULT_VERSIONS . to_vec () , verifier : WebPkiClientVerifier :: no_client_auth () , cert_resolver : None , session_storage : None , alpn_protocols : vec ! [] , ignore_client_order : None , key_log : None , } ; to_boxed_mut_ptr (builder) } } # [doc = " Create a rustls_server_config_builder using the specified crypto provider."] # [doc = ""] # [doc = " Caller owns the memory and must eventually call rustls_server_config_builder_build,"] # [doc = " then free the resulting rustls_server_config."] # [doc = ""] # [doc = " Alternatively, if an error occurs or, you don't wish to build a config, call"] # [doc = " `rustls_server_config_builder_free` to free the builder directly."] # [doc = ""] # [doc = " `tls_versions` set the TLS protocol versions to use when negotiating a TLS session."] # [doc = ""] # [doc = " `tls_versions` is the version of the protocol, as defined in rfc8446,"] # [doc = " ch. 4.2.1 and end of ch. 5.1. Some values are defined in"] # [doc = " `rustls_tls_version` for convenience."] # [doc = ""] # [doc = " `tls_versions` will only be used during the call and the application retains"] # [doc = " ownership. `tls_versions_len` is the number of consecutive `uint16_t` pointed"] # [doc = " to by `tls_versions`."] # [doc = ""] # [doc = " Ciphersuites are configured separately via the crypto provider. See"] # [doc = " `rustls_crypto_provider_builder_set_cipher_suites` for more information."] # [no_mangle] pub extern "C" fn rustls_server_config_builder_new_custom (provider : * const rustls_crypto_provider , tls_versions : * const u16 , tls_versions_len : size_t , builder_out : * mut * mut rustls_server_config_builder ,) -> rustls_result { ffi_panic_boundary ! { let provider = try_clone_arc ! (provider) ; let tls_versions = try_slice ! (tls_versions , tls_versions_len) ; let mut versions = vec ! [] ; for version_number in tls_versions { let proto = ProtocolVersion :: from (* version_number) ; if proto == rustls :: version :: TLS12 . version { versions . push (& rustls :: version :: TLS12) ; } else if proto == rustls :: version :: TLS13 . version { versions . push (& rustls :: version :: TLS13) ; } } let builder_out = try_mut_from_ptr_ptr ! (builder_out) ; let builder = ServerConfigBuilder { provider : Some (provider) , versions , verifier : WebPkiClientVerifier :: no_client_auth () , cert_resolver : None , session_storage : None , alpn_protocols : vec ! [] , ignore_client_order : None , key_log : None , } ; set_boxed_mut_ptr (builder_out , builder) ; rustls_result :: Ok } } # [doc = " Create a rustls_server_config_builder for TLS sessions that may verify client"] # [doc = " certificates."] # [doc = ""] # [doc = " This increases the refcount of `verifier` and doesn't take ownership."] # [no_mangle] pub extern "C" fn rustls_server_config_builder_set_client_verifier (builder : * mut rustls_server_config_builder , verifier : * const rustls_client_cert_verifier ,) { ffi_panic_boundary ! { let builder = try_mut_from_ptr ! (builder) ; let verifier = try_ref_from_ptr ! (verifier) ; builder . verifier = verifier . clone () ; } } # [doc = " Log key material to the file specified by the `SSLKEYLOGFILE` environment variable."] # [doc = ""] # [doc = " The key material will be logged in the NSS key log format,"] # [doc = " <https://developer.mozilla.org/en-US/docs/Mozilla/Projects/NSS/Key_Log_Format> and is"] # [doc = " compatible with tools like Wireshark."] # [doc = ""] # [doc = " Secrets logged in this manner are **extremely sensitive** and can break the security"] # [doc = " of past, present and future sessions."] # [doc = ""] # [doc = " For more control over which secrets are logged, or to customize the format, prefer"] # [doc = " `rustls_server_config_builder_set_key_log`."] # [no_mangle] pub extern "C" fn rustls_server_config_builder_set_key_log_file (builder : * mut rustls_server_config_builder ,) -> rustls_result { ffi_panic_boundary ! { let builder = try_mut_from_ptr ! (builder) ; builder . key_log = Some (Arc :: new (KeyLogFile :: new ())) ; rustls_result :: Ok } } # [doc = " Provide callbacks to manage logging key material."] # [doc = ""] # [doc = " The `log_cb` argument is mandatory and must not be `NULL` or a `NullParameter` error is"] # [doc = " returned. The `log_cb` will be invoked with a `client_random` to identify the relevant session,"] # [doc = " a `label` to identify the purpose of the `secret`, and the `secret` itself. See the"] # [doc = " Rustls documentation of the `KeyLog` trait for more information on possible labels:"] # [doc = " <https://docs.rs/rustls/latest/rustls/trait.KeyLog.html#tymethod.log>"] # [doc = ""] # [doc = " The `will_log_cb` may be `NULL`, in which case all key material will be provided to"] # [doc = " the `log_cb`. By providing a custom `will_log_cb` you may return `0` for labels you don't"] # [doc = " wish to log, and non-zero for labels you _do_ wish to log as a performance optimization."] # [doc = ""] # [doc = " Both callbacks **must** be thread-safe. Arguments provided to the callback live only for as"] # [doc = " long as the callback is executing and are not valid after the callback returns. The"] # [doc = " callbacks must not retain references to the provided data."] # [doc = ""] # [doc = " Secrets provided to the `log_cb` are **extremely sensitive** and can break the security"] # [doc = " of past, present and future sessions."] # [doc = ""] # [doc = " See also `rustls_server_config_builder_set_key_log_file` for a simpler way to log"] # [doc = " to a file specified by the `SSLKEYLOGFILE` environment variable."] # [no_mangle] pub extern "C" fn rustls_server_config_builder_set_key_log (builder : * mut rustls_server_config_builder , log_cb : rustls_keylog_log_callback , will_log_cb : rustls_keylog_will_log_callback ,) -> rustls_result { ffi_panic_boundary ! { let builder = try_mut_from_ptr ! (builder) ; let log_cb = match log_cb { Some (cb) => cb , None => return rustls_result :: NullParameter , } ; builder . key_log = Some (Arc :: new (CallbackKeyLog { log_cb , will_log_cb , })) ; rustls_result :: Ok } } # [doc = " \"Free\" a server_config_builder without building it into a rustls_server_config."] # [doc = ""] # [doc = " Normally builders are built into rustls_server_configs via `rustls_server_config_builder_build`"] # [doc = " and may not be free'd or otherwise used afterwards."] # [doc = ""] # [doc = " Use free only when the building of a config has to be aborted before a config"] # [doc = " was created."] # [no_mangle] pub extern "C" fn rustls_server_config_builder_free (config : * mut rustls_server_config_builder) { ffi_panic_boundary ! { free_box (config) ; } } # [doc = " With `ignore` != 0, the server will ignore the client ordering of cipher"] # [doc = " suites, aka preference, during handshake and respect its own ordering"] # [doc = " as configured."] # [doc = " <https://docs.rs/rustls/latest/rustls/struct.ServerConfig.html#structfield.ignore_client_order>"] # [no_mangle] pub extern "C" fn rustls_server_config_builder_set_ignore_client_order (builder : * mut rustls_server_config_builder , ignore : bool ,) -> rustls_result { ffi_panic_boundary ! { let config = try_mut_from_ptr ! (builder) ; config . ignore_client_order = Some (ignore) ; rustls_result :: Ok } } # [doc = " Set the ALPN protocol list to the given protocols."] # [doc = ""] # [doc = " `protocols` must point to a buffer of `rustls_slice_bytes` (built by the caller)"] # [doc = " with `len` elements. Each element of the buffer must point to a slice of bytes that"] # [doc = " contains a single ALPN protocol from"] # [doc = " <https://www.iana.org/assignments/tls-extensiontype-values/tls-extensiontype-values.xhtml#alpn-protocol-ids>."] # [doc = ""] # [doc = " This function makes a copy of the data in `protocols` and does not retain"] # [doc = " any pointers, so the caller can free the pointed-to memory after calling."] # [doc = ""] # [doc = " <https://docs.rs/rustls/latest/rustls/server/struct.ServerConfig.html#structfield.alpn_protocols>"] # [no_mangle] pub extern "C" fn rustls_server_config_builder_set_alpn_protocols (builder : * mut rustls_server_config_builder , protocols : * const rustls_slice_bytes , len : size_t ,) -> rustls_result { ffi_panic_boundary ! { let config = try_mut_from_ptr ! (builder) ; let protocols = try_slice ! (protocols , len) ; let mut vv = Vec :: new () ; for p in protocols { let v = try_slice ! (p . data , p . len) ; vv . push (v . to_vec ()) ; } config . alpn_protocols = vv ; rustls_result :: Ok } } # [doc = " Provide the configuration a list of certificates where the connection"] # [doc = " will select the first one that is compatible with the client's signature"] # [doc = " verification capabilities."] # [doc = ""] # [doc = " Servers that want to support both ECDSA and RSA certificates will want"] # [doc = " the ECSDA to go first in the list."] # [doc = ""] # [doc = " The built configuration will keep a reference to all certified keys"] # [doc = " provided. The client may `rustls_certified_key_free()` afterwards"] # [doc = " without the configuration losing them. The same certified key may also"] # [doc = " be used in multiple configs."] # [doc = ""] # [doc = " EXPERIMENTAL: installing a client_hello callback will replace any"] # [doc = " configured certified keys and vice versa."] # [no_mangle] pub extern "C" fn rustls_server_config_builder_set_certified_keys (builder : * mut rustls_server_config_builder , certified_keys : * const * const rustls_certified_key , certified_keys_len : size_t ,) -> rustls_result { ffi_panic_boundary ! { let builder = try_mut_from_ptr ! (builder) ; let keys_ptrs = try_slice ! (certified_keys , certified_keys_len) ; let mut keys = Vec :: new () ; for & key_ptr in keys_ptrs { let certified_key = try_clone_arc ! (key_ptr) ; keys . push (certified_key) ; } builder . cert_resolver = Some (Arc :: new (ResolvesServerCertFromChoices :: new (& keys))) ; rustls_result :: Ok } } # [doc = " Turn a *rustls_server_config_builder (mutable) into a const *rustls_server_config"] # [doc = " (read-only). The constructed `rustls_server_config` will be written to the `config_out`"] # [doc = " pointer when this function returns `rustls_result::Ok`."] # [doc = ""] # [doc = " This function may return an error if no process default crypto provider has been set"] # [doc = " and the builder was constructed using `rustls_server_config_builder_new`, or if no"] # [doc = " certificate resolver was set."] # [no_mangle] pub extern "C" fn rustls_server_config_builder_build (builder : * mut rustls_server_config_builder , config_out : * mut * const rustls_server_config ,) -> rustls_result { ffi_panic_boundary ! { let builder = try_box_from_ptr ! (builder) ; let config_out = try_ref_from_ptr_ptr ! (config_out) ; let provider = match builder . provider { Some (provider) => provider , None => return rustls_result :: NoDefaultCryptoProvider , } ; let base = match ServerConfig :: builder_with_provider (provider) . with_protocol_versions (& builder . versions) { Ok (base) => base , Err (err) => return map_error (err) , } . with_client_cert_verifier (builder . verifier) ; let mut config = if let Some (r) = builder . cert_resolver { base . with_cert_resolver (r) } else { return rustls_result :: NoCertResolver ; } ; if let Some (ss) = builder . session_storage { config . session_storage = ss ; } config . alpn_protocols = builder . alpn_protocols ; if let Some (ignore_client_order) = builder . ignore_client_order { config . ignore_client_order = ignore_client_order ; } if let Some (key_log) = builder . key_log { config . key_log = key_log ; } set_arc_mut_ptr (config_out , config) ; rustls_result :: Ok } } }
};
}
