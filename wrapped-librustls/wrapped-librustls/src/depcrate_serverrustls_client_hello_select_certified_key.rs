// Generated macro for rustls_client_hello_select_certified_key (function)
macro_rules! Depcrate_serverrustls_client_hello_select_certified_key {
() => {
// Module: crate::server
// Provides: {"rustls_client_hello_select_certified_key"}
// Dependencies: {}
# [doc = " Select a `rustls_certified_key` from the list that matches the cryptographic"] # [doc = " parameters of a TLS client hello."] # [doc = ""] # [doc = " Note that this does not do any SNI matching. The input certificates should"] # [doc = " already have been filtered to ones matching the SNI from the client hello."] # [doc = ""] # [doc = " This is intended for servers that are configured with several keys for the"] # [doc = " same domain name(s), for example ECDSA and RSA types. The presented keys are"] # [doc = " inspected in the order given and keys first in the list are given preference,"] # [doc = " all else being equal. However rustls is free to choose whichever it considers"] # [doc = " to be the best key with its knowledge about security issues and possible future"] # [doc = " extensions of the protocol."] # [doc = ""] # [doc = " Return RUSTLS_RESULT_OK if a key was selected and RUSTLS_RESULT_NOT_FOUND"] # [doc = " if none was suitable."] # [no_mangle] pub extern "C" fn rustls_client_hello_select_certified_key (hello : * const rustls_client_hello , certified_keys : * const * const rustls_certified_key , certified_keys_len : size_t , out_key : * mut * const rustls_certified_key ,) -> rustls_result { ffi_panic_boundary ! { let hello = try_ref_from_ptr ! (hello) ; let schemes = sigschemes (try_slice ! (hello . signature_schemes . data , hello . signature_schemes . len)) ; if out_key . is_null () { return rustls_result :: NullParameter ; } let keys_ptrs = try_slice ! (certified_keys , certified_keys_len) ; for & key_ptr in keys_ptrs { let key_ref = try_ref_from_ptr ! (key_ptr) ; if key_ref . key . choose_scheme (& schemes) . is_some () { unsafe { * out_key = key_ptr ; } return rustls_result :: Ok ; } } rustls_result :: NotFound } }
};
}
