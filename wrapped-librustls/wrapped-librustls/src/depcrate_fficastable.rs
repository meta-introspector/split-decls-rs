// Generated macro for Castable (trait)
macro_rules! Depcrate_ffiCastable {
() => {
// Module: crate::ffi
// Provides: {"Castable"}
// Dependencies: {}
# [doc = " `Castable` represents the relationship between a snake case type (like [`client::rustls_client_config`])"] # [doc = " and the corresponding Rust type (like [`rustls::ClientConfig`]), specified as the associated type"] # [doc = " `RustType`. Each `Castable` also has an associated type `Ownership` specifying one of the"] # [doc = " [`OwnershipMarker`] types, [`OwnershipBox`], [`OwnershipArc`] or [`OwnershipRef`]."] # [doc = ""] # [doc = " An implementation of `Castable` that uses [`OwnershipBox`] indicates that when we give C code"] # [doc = " a pointer to the relevant `RustType` `T`, that it is actually a `Box<T>`. An"] # [doc = " implementation of `Castable` that uses [`OwnershipArc`] means that when we give C code a"] # [doc = " pointer to the relevant type, that it is actually an `Arc<T>`. Lastly an implementation of"] # [doc = " `Castable` that uses [`OwnershipRef`] means that when we give C code a pointer to the relevant"] # [doc = " type, that it is actually a `&T`."] # [doc = ""] # [doc = " By using an associated type on `Castable` to communicate this we can use the type system to"] # [doc = " guarantee that a single type can't implement `Castable` for more than one [`OwnershipMarker`],"] # [doc = " since this would be a conflicting trait implementation and rejected by the compiler."] # [doc = ""] # [doc = " This trait allows us to avoid using `as` in most places, and ensures that when we cast, we're"] # [doc = " preserving const-ness, and casting between the correct types. Implementing this is required in"] # [doc = " order to use `try_ref_from_ptr!` or `try_mut_from_ptr!` and several other helpful cast-related"] # [doc = " conversion helpers."] # [doc = ""] # [doc = " You can define a new `Castable` type using the `box_castable!`, `arc_castable!` or"] # [doc = " `ref_castable!` macros depending on the ownership marker you desire. See each macro's"] # [doc = " documentation for more information."] pub (crate) trait Castable { # [doc = " Indicates whether to use `Box` or `Arc` when giving a pointer to C code for the underlying"] # [doc = " `RustType`."] type Ownership : OwnershipMarker ; # [doc = " The underlying Rust type that we are casting to and from."] type RustType ; }
};
}
