// Generated macro for impl_392 (impl)
macro_rules! Depcrate_serverimpl_392 {
() => {
// Module: crate::server
// Provides: {"impl_392"}
// Dependencies: {}
impl rustls_server_config { # [doc = " Returns true if a `rustls_connection` created from the `rustls_server_config` will"] # [doc = " operate in FIPS mode."] # [doc = ""] # [doc = " This is different from `rustls_crypto_provider_fips` which is concerned"] # [doc = " only with cryptography, whereas this also covers TLS-level configuration that NIST"] # [doc = " recommends, as well as ECH HPKE suites if applicable."] # [no_mangle] pub extern "C" fn rustls_server_config_fips (config : * const rustls_server_config) -> bool { ffi_panic_boundary ! { try_ref_from_ptr ! (config) . fips () } } # [doc = " \"Free\" a rustls_server_config previously returned from"] # [doc = " rustls_server_config_builder_build."] # [doc = ""] # [doc = " Since rustls_server_config is actually an"] # [doc = " atomically reference-counted pointer, extant server connections may still"] # [doc = " hold an internal reference to the Rust object. However, C code must"] # [doc = " consider this pointer unusable after \"free\"ing it."] # [doc = " Calling with NULL is fine. Must not be called twice with the same value."] # [no_mangle] pub extern "C" fn rustls_server_config_free (config : * const rustls_server_config) { ffi_panic_boundary ! { free_arc (config) ; } } # [doc = " Create a new rustls_connection containing a server connection, and return it."] # [doc = ""] # [doc = " It is returned in the output parameter `conn_out`."] # [doc = ""] # [doc = " If this returns an error code, the memory pointed to by `conn_out` remains unchanged."] # [doc = ""] # [doc = " If this returns a non-error, the memory pointed to by `conn_out` is modified to point"] # [doc = " at a valid rustls_connection"] # [doc = ""] # [doc = " The caller now owns the rustls_connection and must call `rustls_connection_free` when"] # [doc = " done with it."] # [no_mangle] pub extern "C" fn rustls_server_connection_new (config : * const rustls_server_config , conn_out : * mut * mut rustls_connection ,) -> rustls_result { ffi_panic_boundary ! { if conn_out . is_null () { return rustls_result :: NullParameter ; } let config = try_clone_arc ! (config) ; let conn_out = try_mut_from_ptr_ptr ! (conn_out) ; let server_connection = match ServerConnection :: new (config) { Ok (sc) => sc , Err (e) => return map_error (e) , } ; let c = Connection :: from_server (server_connection) ; set_boxed_mut_ptr (conn_out , c) ; rustls_result :: Ok } } }
};
}
