// Generated macro for impl_41 (impl)
macro_rules! Depcrate_certificateimpl_41 {
() => {
// Module: crate::certificate
// Provides: {"impl_41"}
// Dependencies: {}
impl rustls_certified_key { # [doc = " Build a `rustls_certified_key` from a certificate chain and a private key"] # [doc = " and the default process-wide crypto provider."] # [doc = ""] # [doc = " `cert_chain` must point to a buffer of `cert_chain_len` bytes, containing"] # [doc = " a series of PEM-encoded certificates, with the end-entity (leaf)"] # [doc = " certificate first."] # [doc = ""] # [doc = " `private_key` must point to a buffer of `private_key_len` bytes, containing"] # [doc = " a PEM-encoded private key in either PKCS#1, PKCS#8 or SEC#1 format when"] # [doc = " using `aws-lc-rs` as the crypto provider. Supported formats may vary by"] # [doc = " provider."] # [doc = ""] # [doc = " On success, this writes a pointer to the newly created"] # [doc = " `rustls_certified_key` in `certified_key_out`. That pointer must later"] # [doc = " be freed with `rustls_certified_key_free` to avoid memory leaks. Note that"] # [doc = " internally, this is an atomically reference-counted pointer, so even after"] # [doc = " the original caller has called `rustls_certified_key_free`, other objects"] # [doc = " may retain a pointer to the object. The memory will be freed when all"] # [doc = " references are gone."] # [doc = ""] # [doc = " This function does not take ownership of any of its input pointers. It"] # [doc = " parses the pointed-to data and makes a copy of the result. You may"] # [doc = " free the cert_chain and private_key pointers after calling it."] # [doc = ""] # [doc = " Typically, you will build a `rustls_certified_key`, use it to create a"] # [doc = " `rustls_server_config` (which increments the reference count), and then"] # [doc = " immediately call `rustls_certified_key_free`. That leaves the"] # [doc = " `rustls_server_config` in possession of the sole reference, so the"] # [doc = " `rustls_certified_key`'s memory will automatically be released when"] # [doc = " the `rustls_server_config` is freed."] # [no_mangle] pub extern "C" fn rustls_certified_key_build (cert_chain : * const u8 , cert_chain_len : size_t , private_key : * const u8 , private_key_len : size_t , certified_key_out : * mut * const rustls_certified_key ,) -> rustls_result { ffi_panic_boundary ! { let default_provider = match crypto_provider :: get_default_or_install_from_crate_features () { Some (default_provider) => default_provider , None => return rustls_result :: NoDefaultCryptoProvider , } ; let private_key_der = match PrivateKeyDer :: from_pem_slice (try_slice ! (private_key , private_key_len)) { Ok (der) => der , Err (_) => return rustls_result :: PrivateKeyParseError , } ; let private_key = match default_provider . key_provider . load_private_key (private_key_der) { Ok (key) => key , Err (e) => return map_error (e) , } ; Self :: rustls_certified_key_build_with_signing_key (cert_chain , cert_chain_len , to_boxed_mut_ptr (private_key) , certified_key_out ,) } } # [doc = " Build a `rustls_certified_key` from a certificate chain and a"] # [doc = " `rustls_signing_key`."] # [doc = ""] # [doc = " `cert_chain` must point to a buffer of `cert_chain_len` bytes, containing"] # [doc = " a series of PEM-encoded certificates, with the end-entity (leaf)"] # [doc = " certificate first."] # [doc = ""] # [doc = " `signing_key` must point to a `rustls_signing_key` loaded using a"] # [doc = " `rustls_crypto_provider` and `rustls_crypto_provider_load_key()`."] # [doc = ""] # [doc = " On success, this writes a pointer to the newly created"] # [doc = " `rustls_certified_key` in `certified_key_out`. That pointer must later"] # [doc = " be freed with `rustls_certified_key_free` to avoid memory leaks. Note that"] # [doc = " internally, this is an atomically reference-counted pointer, so even after"] # [doc = " the original caller has called `rustls_certified_key_free`, other objects"] # [doc = " may retain a pointer to the object. The memory will be freed when all"] # [doc = " references are gone."] # [doc = ""] # [doc = " This function does not take ownership of any of its input pointers. It"] # [doc = " parses the pointed-to data and makes a copy of the result. You may"] # [doc = " free the cert_chain and private_key pointers after calling it."] # [doc = ""] # [doc = " Typically, you will build a `rustls_certified_key`, use it to create a"] # [doc = " `rustls_server_config` (which increments the reference count), and then"] # [doc = " immediately call `rustls_certified_key_free`. That leaves the"] # [doc = " `rustls_server_config` in possession of the sole reference, so the"] # [doc = " `rustls_certified_key`'s memory will automatically be released when"] # [doc = " the `rustls_server_config` is freed."] # [no_mangle] pub extern "C" fn rustls_certified_key_build_with_signing_key (cert_chain : * const u8 , cert_chain_len : size_t , signing_key : * mut rustls_signing_key , certified_key_out : * mut * const rustls_certified_key ,) -> rustls_result { ffi_panic_boundary ! { let cert_chain = try_slice ! (cert_chain , cert_chain_len) ; let signing_key = try_box_from_ptr ! (signing_key) ; let certified_key_out = try_ref_from_ptr_ptr ! (certified_key_out) ; let parsed_chain = match CertificateDer :: pem_slice_iter (cert_chain) . collect ::< Result < Vec < _ >, _ >> () { Ok (parsed_chain) => parsed_chain , Err (_) => return rustls_result :: CertificateParseError , } ; set_arc_mut_ptr (certified_key_out , CertifiedKey :: new (parsed_chain , * signing_key) ,) ; rustls_result :: Ok } } # [doc = " Return the i-th rustls_certificate in the rustls_certified_key."] # [doc = ""] # [doc = " 0 gives the end-entity certificate. 1 and higher give certificates from the chain."] # [doc = ""] # [doc = " Indexes higher than the last available certificate return NULL."] # [doc = ""] # [doc = " The returned certificate is valid until the rustls_certified_key is freed."] # [no_mangle] pub extern "C" fn rustls_certified_key_get_certificate < 'a > (certified_key : * const rustls_certified_key , i : size_t ,) -> * const rustls_certificate < 'a > { ffi_panic_boundary ! { let certified_key = try_ref_from_ptr ! (certified_key) ; match certified_key . cert . get (i) { Some (cert) => cert as * const CertificateDer as * const _ , None => null () , } } } # [doc = " Create a copy of the rustls_certified_key with the given OCSP response data"] # [doc = " as DER encoded bytes."] # [doc = ""] # [doc = " The OCSP response may be given as NULL to clear any possibly present OCSP"] # [doc = " data from the cloned key."] # [doc = ""] # [doc = " The cloned key is independent from its original and needs to be freed"] # [doc = " by the application."] # [no_mangle] pub extern "C" fn rustls_certified_key_clone_with_ocsp (certified_key : * const rustls_certified_key , ocsp_response : * const rustls_slice_bytes , cloned_key_out : * mut * const rustls_certified_key ,) -> rustls_result { ffi_panic_boundary ! { let cloned_key_out = unsafe { match cloned_key_out . as_mut () { Some (c) => c , None => return rustls_result :: NullParameter , } } ; let certified_key = try_ref_from_ptr ! (certified_key) ; let mut new_key = certified_key . clone () ; if ! ocsp_response . is_null () { let ocsp_slice = unsafe { &* ocsp_response } ; new_key . ocsp = Some (Vec :: from (try_slice ! (ocsp_slice . data , ocsp_slice . len))) ; } else { new_key . ocsp = None ; } * cloned_key_out = to_arc_const_ptr (new_key) ; rustls_result :: Ok } } # [doc = " Verify the consistency of this `rustls_certified_key`'s public and private keys."] # [doc = ""] # [doc = " This is done by performing a comparison of subject public key information (SPKI) bytes"] # [doc = " between the certificate and private key."] # [doc = ""] # [doc = " If the private key matches the certificate this function returns `RUSTLS_RESULT_OK`,"] # [doc = " otherwise an error `rustls_result` is returned."] # [no_mangle] pub extern "C" fn rustls_certified_key_keys_match (key : * const rustls_certified_key ,) -> rustls_result { ffi_panic_boundary ! { match try_ref_from_ptr ! (key) . keys_match () { Ok (_) => rustls_result :: Ok , Err (e) => map_error (e) , } } } # [doc = " \"Free\" a certified_key previously returned from `rustls_certified_key_build`."] # [doc = ""] # [doc = " Since certified_key is actually an atomically reference-counted pointer,"] # [doc = " extant certified_key may still hold an internal reference to the Rust object."] # [doc = ""] # [doc = " However, C code must consider this pointer unusable after \"free\"ing it."] # [doc = ""] # [doc = " Calling with NULL is fine. Must not be called twice with the same value."] # [no_mangle] pub extern "C" fn rustls_certified_key_free (key : * const rustls_certified_key) { ffi_panic_boundary ! { free_arc (key) ; } } }
};
}
