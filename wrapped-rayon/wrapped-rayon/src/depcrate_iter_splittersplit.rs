// Generated macro for split (function)
macro_rules! Depcrate_iter_splittersplit {
() => {
// Module: crate::iter::splitter
// Provides: {"split"}
// Dependencies: {}
# [doc = " The `split` function takes arbitrary data and a closure that knows how to"] # [doc = " split it, and turns this into a `ParallelIterator`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " As a simple example, Rayon can recursively split ranges of indices"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::iter;"] # [doc = " use rayon::prelude::*;"] # [doc = " use std::ops::Range;"] # [doc = ""] # [doc = ""] # [doc = " // We define a range of indices as follows"] # [doc = " type Range1D = Range<usize>;"] # [doc = ""] # [doc = " // Splitting it in two can be done like this"] # [doc = " fn split_range1(r: Range1D) -> (Range1D, Option<Range1D>) {"] # [doc = "     // We are mathematically unable to split the range if there is only"] # [doc = "     // one point inside of it, but we could stop splitting before that."] # [doc = "     if r.end - r.start <= 1 { return (r, None); }"] # [doc = ""] # [doc = "     // Here, our range is considered large enough to be splittable"] # [doc = "     let midpoint = r.start + (r.end - r.start) / 2;"] # [doc = "     (r.start..midpoint, Some(midpoint..r.end))"] # [doc = " }"] # [doc = ""] # [doc = " // By using iter::split, Rayon will split the range until it has enough work"] # [doc = " // to feed the CPU cores, then give us the resulting sub-ranges"] # [doc = " iter::split(0..4096, split_range1).for_each(|sub_range| {"] # [doc = "     // As our initial range had a power-of-two size, the final sub-ranges"] # [doc = "     // should have power-of-two sizes too"] # [doc = "     assert!((sub_range.end - sub_range.start).is_power_of_two());"] # [doc = " });"] # [doc = " ```"] # [doc = ""] # [doc = " This recursive splitting can be extended to two or three dimensions,"] # [doc = " to reproduce a classic \"block-wise\" parallelization scheme of graphics and"] # [doc = " numerical simulations:"] # [doc = ""] # [doc = " ```"] # [doc = " # use rayon::iter;"] # [doc = " # use rayon::prelude::*;"] # [doc = " # use std::ops::Range;"] # [doc = " # type Range1D = Range<usize>;"] # [doc = " # fn split_range1(r: Range1D) -> (Range1D, Option<Range1D>) {"] # [doc = " #     if r.end - r.start <= 1 { return (r, None); }"] # [doc = " #     let midpoint = r.start + (r.end - r.start) / 2;"] # [doc = " #     (r.start..midpoint, Some(midpoint..r.end))"] # [doc = " # }"] # [doc = " #"] # [doc = " // A two-dimensional range of indices can be built out of two 1D ones"] # [doc = " struct Range2D {"] # [doc = "     // Range of horizontal indices"] # [doc = "     pub rx: Range1D,"] # [doc = ""] # [doc = "     // Range of vertical indices"] # [doc = "     pub ry: Range1D,"] # [doc = " }"] # [doc = ""] # [doc = " // We want to recursively split them by the largest dimension until we have"] # [doc = " // enough sub-ranges to feed our mighty multi-core CPU. This function"] # [doc = " // carries out one such split."] # [doc = " fn split_range2(r2: Range2D) -> (Range2D, Option<Range2D>) {"] # [doc = "     // Decide on which axis (horizontal/vertical) the range should be split"] # [doc = "     let width = r2.rx.end - r2.rx.start;"] # [doc = "     let height = r2.ry.end - r2.ry.start;"] # [doc = "     if width >= height {"] # [doc = "         // This is a wide range, split it on the horizontal axis"] # [doc = "         let (split_rx, ry) = (split_range1(r2.rx), r2.ry);"] # [doc = "         let out1 = Range2D {"] # [doc = "             rx: split_rx.0,"] # [doc = "             ry: ry.clone(),"] # [doc = "         };"] # [doc = "         let out2 = split_rx.1.map(|rx| Range2D { rx, ry });"] # [doc = "         (out1, out2)"] # [doc = "     } else {"] # [doc = "         // This is a tall range, split it on the vertical axis"] # [doc = "         let (rx, split_ry) = (r2.rx, split_range1(r2.ry));"] # [doc = "         let out1 = Range2D {"] # [doc = "             rx: rx.clone(),"] # [doc = "             ry: split_ry.0,"] # [doc = "         };"] # [doc = "         let out2 = split_ry.1.map(|ry| Range2D { rx, ry, });"] # [doc = "         (out1, out2)"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // Again, rayon can handle the recursive splitting for us"] # [doc = " let range = Range2D { rx: 0..800, ry: 0..600 };"] # [doc = " iter::split(range, split_range2).for_each(|sub_range| {"] # [doc = "     // If the sub-ranges were indeed split by the largest dimension, then"] # [doc = "     // if no dimension was twice larger than the other initially, this"] # [doc = "     // property will remain true in the final sub-ranges."] # [doc = "     let width = sub_range.rx.end - sub_range.rx.start;"] # [doc = "     let height = sub_range.ry.end - sub_range.ry.start;"] # [doc = "     assert!((width / 2 <= height) && (height / 2 <= width));"] # [doc = " });"] # [doc = " ```"] # [doc = ""] pub fn split < D , S > (data : D , splitter : S) -> Split < D , S > where D : Send , S : Fn (D) -> (D , Option < D >) + Sync , { Split { data , splitter } }
};
}
