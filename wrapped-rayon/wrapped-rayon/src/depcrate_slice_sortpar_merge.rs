// Generated macro for par_merge (function)
macro_rules! Depcrate_slice_sortpar_merge {
() => {
// Module: crate::slice::sort
// Provides: {"par_merge"}
// Dependencies: {}
# [doc = " Merges slices `left` and `right` in parallel and stores the result into `dest`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The `dest` pointer must have enough space to store the result."] # [doc = ""] # [doc = " Even if `is_less` panics at any point during the merge process, this function will fully copy"] # [doc = " all elements from `left` and `right` into `dest` (not necessarily in sorted order)."] unsafe fn par_merge < T , F > (left : & mut [T] , right : & mut [T] , dest : * mut T , is_less : & F) where T : Send , F : Fn (& T , & T) -> bool + Sync , { unsafe { const MAX_SEQUENTIAL : usize = 5000 ; let left_len = left . len () ; let right_len = right . len () ; let mut s = State { left_start : left . as_mut_ptr () , left_end : left . as_mut_ptr () . add (left_len) , right_start : right . as_mut_ptr () , right_end : right . as_mut_ptr () . add (right_len) , dest , } ; if left_len == 0 || right_len == 0 || left_len + right_len < MAX_SEQUENTIAL { while s . left_start < s . left_end && s . right_start < s . right_end { let is_l = is_less (& * s . right_start , & * s . left_start) ; let to_copy = if is_l { s . right_start } else { s . left_start } ; ptr :: copy_nonoverlapping (to_copy , s . dest , 1) ; s . dest = s . dest . add (1) ; s . right_start = s . right_start . add (is_l as usize) ; s . left_start = s . left_start . add (! is_l as usize) ; } } else { let (left_mid , right_mid) = split_for_merge (left , right , is_less) ; let (left_l , left_r) = left . split_at_mut (left_mid) ; let (right_l , right_r) = right . split_at_mut (right_mid) ; mem :: forget (s) ; let dest_l = SendPtr (dest) ; let dest_r = SendPtr (dest . add (left_l . len () + right_l . len ())) ; rayon_core :: join (move | | par_merge (left_l , right_l , dest_l . get () , is_less) , move | | par_merge (left_r , right_r , dest_r . get () , is_less) ,) ; } struct State < T > { left_start : * mut T , left_end : * mut T , right_start : * mut T , right_end : * mut T , dest : * mut T , } impl < T > Drop for State < T > { fn drop (& mut self) { unsafe { let left_len = self . left_end . offset_from (self . left_start) as usize ; ptr :: copy_nonoverlapping (self . left_start , self . dest , left_len) ; self . dest = self . dest . add (left_len) ; let right_len = self . right_end . offset_from (self . right_start) as usize ; ptr :: copy_nonoverlapping (self . right_start , self . dest , right_len) ; } } } } }
};
}
