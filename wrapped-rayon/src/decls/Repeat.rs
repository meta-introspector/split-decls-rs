macro_rules! Repeat {
    () => {
        # [doc = " Iterator adaptor for [the `repeat()` function]."] # [doc = ""] # [doc = " [the `repeat()` function]: repeat()"] # [derive (Debug , Clone)] pub struct Repeat < T > { element : T , }
    };
}

Repeat!();