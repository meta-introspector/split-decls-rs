macro_rules! EncodeUtf16 {
    () => {
        # [doc = " Parallel iterator over a string encoded as UTF-16"] # [derive (Debug , Clone)] pub struct EncodeUtf16 < 'ch > { chars : & 'ch str , }
    };
}

EncodeUtf16!()