macro_rules! SplitAsciiWhitespace {
    () => {
        # [doc = " Parallel iterator over substrings separated by ASCII whitespace"] # [derive (Debug , Clone)] pub struct SplitAsciiWhitespace < 'ch > (& 'ch str) ;
    };
}

SplitAsciiWhitespace!()