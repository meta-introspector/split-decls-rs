macro_rules! deps {
    () => {
        MapWith!();
        SkipAnyWhile!();
        FlattenIter!();
        FoldWith!();
        Consumer!();
        Chain!();
        Fold!();
        Map!();
        TryFold!();
        Iter!();
        FlatMapIter!();
        Update!();
        WhileSome!();
        IndexedParallelIterator!();
        FilterMap!();
        Flatten!();
        Inspect!();
        UnindexedConsumer!();
        FlatMap!();
        Copied!();
        TakeAnyWhile!();
        TryFoldWith!();
        PanicFuse!();
        Filter!();
        TakeAny!();
        ParallelExtend!();
        MapInit!();
        FromParallelIterator!();
        Intersperse!();
        SkipAny!();
        Once!();
        Cloned!();
        IntoParallelIterator!();
    };
}

macro_rules! ParallelIterator {
    () => {
        deps!();
        # [doc = " Parallel version of the standard iterator trait."] # [doc = ""] # [doc = " The combinators on this trait are available on **all** parallel"] # [doc = " iterators.  Additional methods can be found on the"] # [doc = " [`IndexedParallelIterator`] trait: those methods are only"] # [doc = " available for parallel iterators where the number of items is"] # [doc = " known in advance (so, e.g., after invoking `filter`, those methods"] # [doc = " become unavailable)."] # [doc = ""] # [doc = " For examples of using parallel iterators, see [the docs on the"] # [doc = " `iter` module][iter]."] # [doc = ""] # [doc = " [iter]: self"] pub trait ParallelIterator : Sized + Send { # [doc = " The type of item that this parallel iterator produces."] # [doc = " For example, if you use the [`for_each`] method, this is the type of"] # [doc = " item that your closure will be invoked with."] # [doc = ""] # [doc = " [`for_each`]: #method.for_each"] type Item : Send ; # [doc = " Executes `OP` on each item produced by the iterator, in parallel."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " (0..100).into_par_iter().for_each(|x| println!(\"{:?}\", x));"] # [doc = " ```"] fn for_each < OP > (self , op : OP) where OP : Fn (Self :: Item) + Sync + Send , { for_each :: for_each (self , & op) } # [doc = " Executes `OP` on the given `init` value with each item produced by"] # [doc = " the iterator, in parallel."] # [doc = ""] # [doc = " The `init` value will be cloned only as needed to be paired with"] # [doc = " the group of items in each rayon job.  It does not require the type"] # [doc = " to be `Sync`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::sync::mpsc::channel;"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let (sender, receiver) = channel();"] # [doc = ""] # [doc = " (0..5).into_par_iter().for_each_with(sender, |s, x| s.send(x).unwrap());"] # [doc = ""] # [doc = " let mut res: Vec<_> = receiver.iter().collect();"] # [doc = ""] # [doc = " res.sort();"] # [doc = ""] # [doc = " assert_eq!(&res[..], &[0, 1, 2, 3, 4])"] # [doc = " ```"] fn for_each_with < OP , T > (self , init : T , op : OP) where OP : Fn (& mut T , Self :: Item) + Sync + Send , T : Send + Clone , { self . map_with (init , op) . collect () } # [doc = " Executes `OP` on a value returned by `init` with each item produced by"] # [doc = " the iterator, in parallel."] # [doc = ""] # [doc = " The `init` function will be called only as needed for a value to be"] # [doc = " paired with the group of items in each rayon job.  There is no"] # [doc = " constraint on that returned type at all!"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::Rng;"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let mut v = vec![0u8; 1_000_000];"] # [doc = ""] # [doc = " v.par_chunks_mut(1000)"] # [doc = "     .for_each_init("] # [doc = "         || rand::rng(),"] # [doc = "         |rng, chunk| rng.fill(chunk),"] # [doc = "     );"] # [doc = ""] # [doc = " // There's a remote chance that this will fail..."] # [doc = " for i in 0u8..=255 {"] # [doc = "     assert!(v.contains(&i));"] # [doc = " }"] # [doc = " ```"] fn for_each_init < OP , INIT , T > (self , init : INIT , op : OP) where OP : Fn (& mut T , Self :: Item) + Sync + Send , INIT : Fn () -> T + Sync + Send , { self . map_init (init , op) . collect () } # [doc = " Executes a fallible `OP` on each item produced by the iterator, in parallel."] # [doc = ""] # [doc = " If the `OP` returns `Result::Err` or `Option::None`, we will attempt to"] # [doc = " stop processing the rest of the items in the iterator as soon as"] # [doc = " possible, and we will return that terminating value.  Otherwise, we will"] # [doc = " return an empty `Result::Ok(())` or `Option::Some(())`.  If there are"] # [doc = " multiple errors in parallel, it is not specified which will be returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = " use std::io::{self, Write};"] # [doc = ""] # [doc = " // This will stop iteration early if there's any write error, like"] # [doc = " // having piped output get closed on the other end."] # [doc = " (0..100).into_par_iter()"] # [doc = "     .try_for_each(|x| writeln!(io::stdout(), \"{:?}\", x))"] # [doc = "     .expect(\"expected no write errors\");"] # [doc = " ```"] fn try_for_each < OP , R > (self , op : OP) -> R where OP : Fn (Self :: Item) -> R + Sync + Send , R : Try < Output = () > + Send , { fn ok < R : Try < Output = () > > (_ : () , _ : ()) -> R { R :: from_output (()) } self . map (op) . try_reduce (< () > :: default , ok) } # [doc = " Executes a fallible `OP` on the given `init` value with each item"] # [doc = " produced by the iterator, in parallel."] # [doc = ""] # [doc = " This combines the `init` semantics of [`for_each_with()`] and the"] # [doc = " failure semantics of [`try_for_each()`]."] # [doc = ""] # [doc = " [`for_each_with()`]: #method.for_each_with"] # [doc = " [`try_for_each()`]: #method.try_for_each"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::sync::mpsc::channel;"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let (sender, receiver) = channel();"] # [doc = ""] # [doc = " (0..5).into_par_iter()"] # [doc = "     .try_for_each_with(sender, |s, x| s.send(x))"] # [doc = "     .expect(\"expected no send errors\");"] # [doc = ""] # [doc = " let mut res: Vec<_> = receiver.iter().collect();"] # [doc = ""] # [doc = " res.sort();"] # [doc = ""] # [doc = " assert_eq!(&res[..], &[0, 1, 2, 3, 4])"] # [doc = " ```"] fn try_for_each_with < OP , T , R > (self , init : T , op : OP) -> R where OP : Fn (& mut T , Self :: Item) -> R + Sync + Send , T : Send + Clone , R : Try < Output = () > + Send , { fn ok < R : Try < Output = () > > (_ : () , _ : ()) -> R { R :: from_output (()) } self . map_with (init , op) . try_reduce (< () > :: default , ok) } # [doc = " Executes a fallible `OP` on a value returned by `init` with each item"] # [doc = " produced by the iterator, in parallel."] # [doc = ""] # [doc = " This combines the `init` semantics of [`for_each_init()`] and the"] # [doc = " failure semantics of [`try_for_each()`]."] # [doc = ""] # [doc = " [`for_each_init()`]: #method.for_each_init"] # [doc = " [`try_for_each()`]: #method.try_for_each"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::{Rng, TryRngCore};"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let mut v = vec![0u8; 1_000_000];"] # [doc = ""] # [doc = " v.par_chunks_mut(1000)"] # [doc = "     .try_for_each_init("] # [doc = "         || rand::rng(),"] # [doc = "         |rng, chunk| rng.try_fill_bytes(chunk),"] # [doc = "     )"] # [doc = "     .expect(\"expected no rand errors\");"] # [doc = ""] # [doc = " // There's a remote chance that this will fail..."] # [doc = " for i in 0u8..=255 {"] # [doc = "     assert!(v.contains(&i));"] # [doc = " }"] # [doc = " ```"] fn try_for_each_init < OP , INIT , T , R > (self , init : INIT , op : OP) -> R where OP : Fn (& mut T , Self :: Item) -> R + Sync + Send , INIT : Fn () -> T + Sync + Send , R : Try < Output = () > + Send , { fn ok < R : Try < Output = () > > (_ : () , _ : ()) -> R { R :: from_output (()) } self . map_init (init , op) . try_reduce (< () > :: default , ok) } # [doc = " Counts the number of items in this parallel iterator."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let count = (0..100).into_par_iter().count();"] # [doc = ""] # [doc = " assert_eq!(count, 100);"] # [doc = " ```"] fn count (self) -> usize { fn one < T > (_ : T) -> usize { 1 } self . map (one) . sum () } # [doc = " Applies `map_op` to each item of this iterator, producing a new"] # [doc = " iterator with the results."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let mut par_iter = (0..5).into_par_iter().map(|x| x * 2);"] # [doc = ""] # [doc = " let doubles: Vec<_> = par_iter.collect();"] # [doc = ""] # [doc = " assert_eq!(&doubles[..], &[0, 2, 4, 6, 8]);"] # [doc = " ```"] fn map < F , R > (self , map_op : F) -> Map < Self , F > where F : Fn (Self :: Item) -> R + Sync + Send , R : Send , { Map :: new (self , map_op) } # [doc = " Applies `map_op` to the given `init` value with each item of this"] # [doc = " iterator, producing a new iterator with the results."] # [doc = ""] # [doc = " The `init` value will be cloned only as needed to be paired with"] # [doc = " the group of items in each rayon job.  It does not require the type"] # [doc = " to be `Sync`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::sync::mpsc::channel;"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let (sender, receiver) = channel();"] # [doc = ""] # [doc = " let a: Vec<_> = (0..5)"] # [doc = "                 .into_par_iter()            // iterating over i32"] # [doc = "                 .map_with(sender, |s, x| {"] # [doc = "                     s.send(x).unwrap();     // sending i32 values through the channel"] # [doc = "                     x                       // returning i32"] # [doc = "                 })"] # [doc = "                 .collect();                 // collecting the returned values into a vector"] # [doc = ""] # [doc = " let mut b: Vec<_> = receiver.iter()         // iterating over the values in the channel"] # [doc = "                             .collect();     // and collecting them"] # [doc = " b.sort();"] # [doc = ""] # [doc = " assert_eq!(a, b);"] # [doc = " ```"] fn map_with < F , T , R > (self , init : T , map_op : F) -> MapWith < Self , T , F > where F : Fn (& mut T , Self :: Item) -> R + Sync + Send , T : Send + Clone , R : Send , { MapWith :: new (self , init , map_op) } # [doc = " Applies `map_op` to a value returned by `init` with each item of this"] # [doc = " iterator, producing a new iterator with the results."] # [doc = ""] # [doc = " The `init` function will be called only as needed for a value to be"] # [doc = " paired with the group of items in each rayon job.  There is no"] # [doc = " constraint on that returned type at all!"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::Rng;"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a: Vec<_> = (1i32..1_000_000)"] # [doc = "     .into_par_iter()"] # [doc = "     .map_init("] # [doc = "         || rand::rng(),  // get the thread-local RNG"] # [doc = "         |rng, x| if rng.random() { // randomly negate items"] # [doc = "             -x"] # [doc = "         } else {"] # [doc = "             x"] # [doc = "         },"] # [doc = "     ).collect();"] # [doc = ""] # [doc = " // There's a remote chance that this will fail..."] # [doc = " assert!(a.iter().any(|&x| x < 0));"] # [doc = " assert!(a.iter().any(|&x| x > 0));"] # [doc = " ```"] fn map_init < F , INIT , T , R > (self , init : INIT , map_op : F) -> MapInit < Self , INIT , F > where F : Fn (& mut T , Self :: Item) -> R + Sync + Send , INIT : Fn () -> T + Sync + Send , R : Send , { MapInit :: new (self , init , map_op) } # [doc = " Creates an iterator which clones all of its elements.  This may be"] # [doc = " useful when you have an iterator over `&T`, but you need `T`, and"] # [doc = " that type implements `Clone`. See also [`copied()`]."] # [doc = ""] # [doc = " [`copied()`]: #method.copied"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [1, 2, 3];"] # [doc = ""] # [doc = " let v_cloned: Vec<_> = a.par_iter().cloned().collect();"] # [doc = ""] # [doc = " // cloned is the same as .map(|&x| x), for integers"] # [doc = " let v_map: Vec<_> = a.par_iter().map(|&x| x).collect();"] # [doc = ""] # [doc = " assert_eq!(v_cloned, vec![1, 2, 3]);"] # [doc = " assert_eq!(v_map, vec![1, 2, 3]);"] # [doc = " ```"] fn cloned < 'a , T > (self) -> Cloned < Self > where T : 'a + Clone + Send , Self : ParallelIterator < Item = & 'a T > , { Cloned :: new (self) } # [doc = " Creates an iterator which copies all of its elements.  This may be"] # [doc = " useful when you have an iterator over `&T`, but you need `T`, and"] # [doc = " that type implements `Copy`. See also [`cloned()`]."] # [doc = ""] # [doc = " [`cloned()`]: #method.cloned"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [1, 2, 3];"] # [doc = ""] # [doc = " let v_copied: Vec<_> = a.par_iter().copied().collect();"] # [doc = ""] # [doc = " // copied is the same as .map(|&x| x), for integers"] # [doc = " let v_map: Vec<_> = a.par_iter().map(|&x| x).collect();"] # [doc = ""] # [doc = " assert_eq!(v_copied, vec![1, 2, 3]);"] # [doc = " assert_eq!(v_map, vec![1, 2, 3]);"] # [doc = " ```"] fn copied < 'a , T > (self) -> Copied < Self > where T : 'a + Copy + Send , Self : ParallelIterator < Item = & 'a T > , { Copied :: new (self) } # [doc = " Applies `inspect_op` to a reference to each item of this iterator,"] # [doc = " producing a new iterator passing through the original items.  This is"] # [doc = " often useful for debugging to see what's happening in iterator stages."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [1, 4, 2, 3];"] # [doc = ""] # [doc = " // this iterator sequence is complex."] # [doc = " let sum = a.par_iter()"] # [doc = "             .cloned()"] # [doc = "             .filter(|&x| x % 2 == 0)"] # [doc = "             .reduce(|| 0, |sum, i| sum + i);"] # [doc = ""] # [doc = " println!(\"{}\", sum);"] # [doc = ""] # [doc = " // let's add some inspect() calls to investigate what's happening"] # [doc = " let sum = a.par_iter()"] # [doc = "             .cloned()"] # [doc = "             .inspect(|x| println!(\"about to filter: {}\", x))"] # [doc = "             .filter(|&x| x % 2 == 0)"] # [doc = "             .inspect(|x| println!(\"made it through filter: {}\", x))"] # [doc = "             .reduce(|| 0, |sum, i| sum + i);"] # [doc = ""] # [doc = " println!(\"{}\", sum);"] # [doc = " ```"] fn inspect < OP > (self , inspect_op : OP) -> Inspect < Self , OP > where OP : Fn (& Self :: Item) + Sync + Send , { Inspect :: new (self , inspect_op) } # [doc = " Mutates each item of this iterator before yielding it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let par_iter = (0..5).into_par_iter().update(|x| {*x *= 2;});"] # [doc = ""] # [doc = " let doubles: Vec<_> = par_iter.collect();"] # [doc = ""] # [doc = " assert_eq!(&doubles[..], &[0, 2, 4, 6, 8]);"] # [doc = " ```"] fn update < F > (self , update_op : F) -> Update < Self , F > where F : Fn (& mut Self :: Item) + Sync + Send , { Update :: new (self , update_op) } # [doc = " Applies `filter_op` to each item of this iterator, producing a new"] # [doc = " iterator with only the items that gave `true` results."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let mut par_iter = (0..10).into_par_iter().filter(|x| x % 2 == 0);"] # [doc = ""] # [doc = " let even_numbers: Vec<_> = par_iter.collect();"] # [doc = ""] # [doc = " assert_eq!(&even_numbers[..], &[0, 2, 4, 6, 8]);"] # [doc = " ```"] fn filter < P > (self , filter_op : P) -> Filter < Self , P > where P : Fn (& Self :: Item) -> bool + Sync + Send , { Filter :: new (self , filter_op) } # [doc = " Applies `filter_op` to each item of this iterator to get an `Option`,"] # [doc = " producing a new iterator with only the items from `Some` results."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let mut par_iter = (0..10).into_par_iter()"] # [doc = "                         .filter_map(|x| {"] # [doc = "                             if x % 2 == 0 { Some(x * 3) }"] # [doc = "                             else { None }"] # [doc = "                         });"] # [doc = ""] # [doc = " let even_numbers: Vec<_> = par_iter.collect();"] # [doc = ""] # [doc = " assert_eq!(&even_numbers[..], &[0, 6, 12, 18, 24]);"] # [doc = " ```"] fn filter_map < P , R > (self , filter_op : P) -> FilterMap < Self , P > where P : Fn (Self :: Item) -> Option < R > + Sync + Send , R : Send , { FilterMap :: new (self , filter_op) } # [doc = " Applies `map_op` to each item of this iterator to get nested parallel iterators,"] # [doc = " producing a new parallel iterator that flattens these back into one."] # [doc = ""] # [doc = " See also [`flat_map_iter`](#method.flat_map_iter)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [[1, 2], [3, 4], [5, 6], [7, 8]];"] # [doc = ""] # [doc = " let par_iter = a.par_iter().cloned().flat_map(|a| a.to_vec());"] # [doc = ""] # [doc = " let vec: Vec<_> = par_iter.collect();"] # [doc = ""] # [doc = " assert_eq!(&vec[..], &[1, 2, 3, 4, 5, 6, 7, 8]);"] # [doc = " ```"] fn flat_map < F , PI > (self , map_op : F) -> FlatMap < Self , F > where F : Fn (Self :: Item) -> PI + Sync + Send , PI : IntoParallelIterator , { FlatMap :: new (self , map_op) } # [doc = " Applies `map_op` to each item of this iterator to get nested serial iterators,"] # [doc = " producing a new parallel iterator that flattens these back into one."] # [doc = ""] # [doc = " # `flat_map_iter` versus `flat_map`"] # [doc = ""] # [doc = " These two methods are similar but behave slightly differently. With [`flat_map`],"] # [doc = " each of the nested iterators must be a parallel iterator, and they will be further"] # [doc = " split up with nested parallelism. With `flat_map_iter`, each nested iterator is a"] # [doc = " sequential `Iterator`, and we only parallelize _between_ them, while the items"] # [doc = " produced by each nested iterator are processed sequentially."] # [doc = ""] # [doc = " When choosing between these methods, consider whether nested parallelism suits the"] # [doc = " potential iterators at hand. If there's little computation involved, or its length"] # [doc = " is much less than the outer parallel iterator, then it may perform better to avoid"] # [doc = " the overhead of parallelism, just flattening sequentially with `flat_map_iter`."] # [doc = " If there is a lot of computation, potentially outweighing the outer parallel"] # [doc = " iterator, then the nested parallelism of `flat_map` may be worthwhile."] # [doc = ""] # [doc = " [`flat_map`]: #method.flat_map"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = " use std::cell::RefCell;"] # [doc = ""] # [doc = " let a = [[1, 2], [3, 4], [5, 6], [7, 8]];"] # [doc = ""] # [doc = " let par_iter = a.par_iter().flat_map_iter(|a| {"] # [doc = "     // The serial iterator doesn't have to be thread-safe, just its items."] # [doc = "     let cell_iter = RefCell::new(a.iter().cloned());"] # [doc = "     std::iter::from_fn(move || cell_iter.borrow_mut().next())"] # [doc = " });"] # [doc = ""] # [doc = " let vec: Vec<_> = par_iter.collect();"] # [doc = ""] # [doc = " assert_eq!(&vec[..], &[1, 2, 3, 4, 5, 6, 7, 8]);"] # [doc = " ```"] fn flat_map_iter < F , SI > (self , map_op : F) -> FlatMapIter < Self , F > where F : Fn (Self :: Item) -> SI + Sync + Send , SI : IntoIterator < Item : Send > , { FlatMapIter :: new (self , map_op) } # [doc = " An adaptor that flattens parallel-iterable `Item`s into one large iterator."] # [doc = ""] # [doc = " See also [`flatten_iter`](#method.flatten_iter)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let x: Vec<Vec<_>> = vec![vec![1, 2], vec![3, 4]];"] # [doc = " let y: Vec<_> = x.into_par_iter().flatten().collect();"] # [doc = ""] # [doc = " assert_eq!(y, vec![1, 2, 3, 4]);"] # [doc = " ```"] fn flatten (self) -> Flatten < Self > where Self :: Item : IntoParallelIterator , { Flatten :: new (self) } # [doc = " An adaptor that flattens serial-iterable `Item`s into one large iterator."] # [doc = ""] # [doc = " See also [`flatten`](#method.flatten) and the analogous comparison of"] # [doc = " [`flat_map_iter` versus `flat_map`](#flat_map_iter-versus-flat_map)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let x: Vec<Vec<_>> = vec![vec![1, 2], vec![3, 4]];"] # [doc = " let iters: Vec<_> = x.into_iter().map(Vec::into_iter).collect();"] # [doc = " let y: Vec<_> = iters.into_par_iter().flatten_iter().collect();"] # [doc = ""] # [doc = " assert_eq!(y, vec![1, 2, 3, 4]);"] # [doc = " ```"] fn flatten_iter (self) -> FlattenIter < Self > where Self :: Item : IntoIterator < Item : Send > , { FlattenIter :: new (self) } # [doc = " Reduces the items in the iterator into one item using `op`."] # [doc = " The argument `identity` should be a closure that can produce"] # [doc = " \"identity\" value which may be inserted into the sequence as"] # [doc = " needed to create opportunities for parallel execution. So, for"] # [doc = " example, if you are doing a summation, then `identity()` ought"] # [doc = " to produce something that represents the zero for your type"] # [doc = " (but consider just calling `sum()` in that case)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " // Iterate over a sequence of pairs `(x0, y0), ..., (xN, yN)`"] # [doc = " // and use reduce to compute one pair `(x0 + ... + xN, y0 + ... + yN)`"] # [doc = " // where the first/second elements are summed separately."] # [doc = " use rayon::prelude::*;"] # [doc = " let sums = [(0, 1), (5, 6), (16, 2), (8, 9)]"] # [doc = "            .par_iter()        // iterating over &(i32, i32)"] # [doc = "            .cloned()          // iterating over (i32, i32)"] # [doc = "            .reduce(|| (0, 0), // the \"identity\" is 0 in both columns"] # [doc = "                    |a, b| (a.0 + b.0, a.1 + b.1));"] # [doc = " assert_eq!(sums, (0 + 5 + 16 + 8, 1 + 6 + 2 + 9));"] # [doc = " ```"] # [doc = ""] # [doc = " **Note:** unlike a sequential `fold` operation, the order in"] # [doc = " which `op` will be applied to reduce the result is not fully"] # [doc = " specified. So `op` should be [associative] or else the results"] # [doc = " will be non-deterministic. And of course `identity()` should"] # [doc = " produce a true identity."] # [doc = ""] # [doc = " [associative]: https://en.wikipedia.org/wiki/Associative_property"] fn reduce < OP , ID > (self , identity : ID , op : OP) -> Self :: Item where OP : Fn (Self :: Item , Self :: Item) -> Self :: Item + Sync + Send , ID : Fn () -> Self :: Item + Sync + Send , { reduce :: reduce (self , identity , op) } # [doc = " Reduces the items in the iterator into one item using `op`."] # [doc = " If the iterator is empty, `None` is returned; otherwise,"] # [doc = " `Some` is returned."] # [doc = ""] # [doc = " This version of `reduce` is simple but somewhat less"] # [doc = " efficient. If possible, it is better to call `reduce()`, which"] # [doc = " requires an identity element."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = " let sums = [(0, 1), (5, 6), (16, 2), (8, 9)]"] # [doc = "            .par_iter()        // iterating over &(i32, i32)"] # [doc = "            .cloned()          // iterating over (i32, i32)"] # [doc = "            .reduce_with(|a, b| (a.0 + b.0, a.1 + b.1))"] # [doc = "            .unwrap();"] # [doc = " assert_eq!(sums, (0 + 5 + 16 + 8, 1 + 6 + 2 + 9));"] # [doc = " ```"] # [doc = ""] # [doc = " **Note:** unlike a sequential `fold` operation, the order in"] # [doc = " which `op` will be applied to reduce the result is not fully"] # [doc = " specified. So `op` should be [associative] or else the results"] # [doc = " will be non-deterministic."] # [doc = ""] # [doc = " [associative]: https://en.wikipedia.org/wiki/Associative_property"] fn reduce_with < OP > (self , op : OP) -> Option < Self :: Item > where OP : Fn (Self :: Item , Self :: Item) -> Self :: Item + Sync + Send , { fn opt_fold < T > (op : impl Fn (T , T) -> T) -> impl Fn (Option < T > , T) -> Option < T > { move | opt_a , b | match opt_a { Some (a) => Some (op (a , b)) , None => Some (b) , } } fn opt_reduce < T > (op : impl Fn (T , T) -> T) -> impl Fn (Option < T > , Option < T >) -> Option < T > { move | opt_a , opt_b | match (opt_a , opt_b) { (Some (a) , Some (b)) => Some (op (a , b)) , (Some (v) , None) | (None , Some (v)) => Some (v) , (None , None) => None , } } self . fold (< _ > :: default , opt_fold (& op)) . reduce (< _ > :: default , opt_reduce (& op)) } # [doc = " Reduces the items in the iterator into one item using a fallible `op`."] # [doc = " The `identity` argument is used the same way as in [`reduce()`]."] # [doc = ""] # [doc = " [`reduce()`]: #method.reduce"] # [doc = ""] # [doc = " If a `Result::Err` or `Option::None` item is found, or if `op` reduces"] # [doc = " to one, we will attempt to stop processing the rest of the items in the"] # [doc = " iterator as soon as possible, and we will return that terminating value."] # [doc = " Otherwise, we will return the final reduced `Result::Ok(T)` or"] # [doc = " `Option::Some(T)`.  If there are multiple errors in parallel, it is not"] # [doc = " specified which will be returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " // Compute the sum of squares, being careful about overflow."] # [doc = " fn sum_squares<I: IntoParallelIterator<Item = i32>>(iter: I) -> Option<i32> {"] # [doc = "     iter.into_par_iter()"] # [doc = "         .map(|i| i.checked_mul(i))            // square each item,"] # [doc = "         .try_reduce(|| 0, i32::checked_add)   // and add them up!"] # [doc = " }"] # [doc = " assert_eq!(sum_squares(0..5), Some(0 + 1 + 4 + 9 + 16));"] # [doc = ""] # [doc = " // The sum might overflow"] # [doc = " assert_eq!(sum_squares(0..10_000), None);"] # [doc = ""] # [doc = " // Or the squares might overflow before it even reaches `try_reduce`"] # [doc = " assert_eq!(sum_squares(1_000_000..1_000_001), None);"] # [doc = " ```"] fn try_reduce < T , OP , ID > (self , identity : ID , op : OP) -> Self :: Item where OP : Fn (T , T) -> Self :: Item + Sync + Send , ID : Fn () -> T + Sync + Send , Self :: Item : Try < Output = T > , { try_reduce :: try_reduce (self , identity , op) } # [doc = " Reduces the items in the iterator into one item using a fallible `op`."] # [doc = ""] # [doc = " Like [`reduce_with()`], if the iterator is empty, `None` is returned;"] # [doc = " otherwise, `Some` is returned.  Beyond that, it behaves like"] # [doc = " [`try_reduce()`] for handling `Err`/`None`."] # [doc = ""] # [doc = " [`reduce_with()`]: #method.reduce_with"] # [doc = " [`try_reduce()`]: #method.try_reduce"] # [doc = ""] # [doc = " For instance, with `Option` items, the return value may be:"] # [doc = " - `None`, the iterator was empty"] # [doc = " - `Some(None)`, we stopped after encountering `None`."] # [doc = " - `Some(Some(x))`, the entire iterator reduced to `x`."] # [doc = ""] # [doc = " With `Result` items, the nesting is more obvious:"] # [doc = " - `None`, the iterator was empty"] # [doc = " - `Some(Err(e))`, we stopped after encountering an error `e`."] # [doc = " - `Some(Ok(x))`, the entire iterator reduced to `x`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let files = [\"/dev/null\", \"/does/not/exist\"];"] # [doc = ""] # [doc = " // Find the biggest file"] # [doc = " files.into_par_iter()"] # [doc = "     .map(|path| std::fs::metadata(path).map(|m| (path, m.len())))"] # [doc = "     .try_reduce_with(|a, b| {"] # [doc = "         Ok(if a.1 >= b.1 { a } else { b })"] # [doc = "     })"] # [doc = "     .expect(\"Some value, since the iterator is not empty\")"] # [doc = "     .expect_err(\"not found\");"] # [doc = " ```"] fn try_reduce_with < T , OP > (self , op : OP) -> Option < Self :: Item > where OP : Fn (T , T) -> Self :: Item + Sync + Send , Self :: Item : Try < Output = T > , { try_reduce_with :: try_reduce_with (self , op) } # [doc = " Parallel fold is similar to sequential fold except that the"] # [doc = " sequence of items may be subdivided before it is"] # [doc = " folded. Consider a list of numbers like `22 3 77 89 46`. If"] # [doc = " you used sequential fold to add them (`fold(0, |a,b| a+b)`,"] # [doc = " you would wind up first adding 0 + 22, then 22 + 3, then 25 +"] # [doc = " 77, and so forth. The **parallel fold** works similarly except"] # [doc = " that it first breaks up your list into sublists, and hence"] # [doc = " instead of yielding up a single sum at the end, it yields up"] # [doc = " multiple sums. The number of results is nondeterministic, as"] # [doc = " is the point where the breaks occur."] # [doc = ""] # [doc = " So if we did the same parallel fold (`fold(0, |a,b| a+b)`) on"] # [doc = " our example list, we might wind up with a sequence of two numbers,"] # [doc = " like so:"] # [doc = ""] # [doc = " ```notrust"] # [doc = " 22 3 77 89 46"] # [doc = "       |     |"] # [doc = "     102   135"] # [doc = " ```"] # [doc = ""] # [doc = " Or perhaps these three numbers:"] # [doc = ""] # [doc = " ```notrust"] # [doc = " 22 3 77 89 46"] # [doc = "       |  |  |"] # [doc = "     102 89 46"] # [doc = " ```"] # [doc = ""] # [doc = " In general, Rayon will attempt to find good breaking points"] # [doc = " that keep all of your cores busy."] # [doc = ""] # [doc = " ### Fold versus reduce"] # [doc = ""] # [doc = " The `fold()` and `reduce()` methods each take an identity element"] # [doc = " and a combining function, but they operate rather differently."] # [doc = ""] # [doc = " `reduce()` requires that the identity function has the same"] # [doc = " type as the things you are iterating over, and it fully"] # [doc = " reduces the list of items into a single item. So, for example,"] # [doc = " imagine we are iterating over a list of bytes `bytes: [128_u8,"] # [doc = " 64_u8, 64_u8]`. If we used `bytes.reduce(|| 0_u8, |a: u8, b:"] # [doc = " u8| a + b)`, we would get an overflow. This is because `0`,"] # [doc = " `a`, and `b` here are all bytes, just like the numbers in the"] # [doc = " list (I wrote the types explicitly above, but those are the"] # [doc = " only types you can use). To avoid the overflow, we would need"] # [doc = " to do something like `bytes.map(|b| b as u32).reduce(|| 0, |a,"] # [doc = " b| a + b)`, in which case our result would be `256`."] # [doc = ""] # [doc = " In contrast, with `fold()`, the identity function does not"] # [doc = " have to have the same type as the things you are iterating"] # [doc = " over, and you potentially get back many results. So, if we"] # [doc = " continue with the `bytes` example from the previous paragraph,"] # [doc = " we could do `bytes.fold(|| 0_u32, |a, b| a + (b as u32))` to"] # [doc = " convert our bytes into `u32`. And of course we might not get"] # [doc = " back a single sum."] # [doc = ""] # [doc = " There is a more subtle distinction as well, though it's"] # [doc = " actually implied by the above points. When you use `reduce()`,"] # [doc = " your reduction function is sometimes called with values that"] # [doc = " were never part of your original parallel iterator (for"] # [doc = " example, both the left and right might be a partial sum). With"] # [doc = " `fold()`, in contrast, the left value in the fold function is"] # [doc = " always the accumulator, and the right value is always from"] # [doc = " your original sequence."] # [doc = ""] # [doc = " ### Fold vs Map/Reduce"] # [doc = ""] # [doc = " Fold makes sense if you have some operation where it is"] # [doc = " cheaper to create groups of elements at a time. For example,"] # [doc = " imagine collecting characters into a string. If you were going"] # [doc = " to use map/reduce, you might try this:"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let s ="] # [doc = "     ['a', 'b', 'c', 'd', 'e']"] # [doc = "     .par_iter()"] # [doc = "     .map(|c: &char| format!(\"{}\", c))"] # [doc = "     .reduce(|| String::new(),"] # [doc = "             |mut a: String, b: String| { a.push_str(&b); a });"] # [doc = ""] # [doc = " assert_eq!(s, \"abcde\");"] # [doc = " ```"] # [doc = ""] # [doc = " Because reduce produces the same type of element as its input,"] # [doc = " you have to first map each character into a string, and then"] # [doc = " you can reduce them. This means we create one string per"] # [doc = " element in our iterator -- not so great. Using `fold`, we can"] # [doc = " do this instead:"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let s ="] # [doc = "     ['a', 'b', 'c', 'd', 'e']"] # [doc = "     .par_iter()"] # [doc = "     .fold(|| String::new(),"] # [doc = "             |mut s: String, c: &char| { s.push(*c); s })"] # [doc = "     .reduce(|| String::new(),"] # [doc = "             |mut a: String, b: String| { a.push_str(&b); a });"] # [doc = ""] # [doc = " assert_eq!(s, \"abcde\");"] # [doc = " ```"] # [doc = ""] # [doc = " Now `fold` will process groups of our characters at a time,"] # [doc = " and we only make one string per group. We should wind up with"] # [doc = " some small-ish number of strings roughly proportional to the"] # [doc = " number of CPUs you have (it will ultimately depend on how busy"] # [doc = " your processors are). Note that we still need to do a reduce"] # [doc = " afterwards to combine those groups of strings into a single"] # [doc = " string."] # [doc = ""] # [doc = " You could use a similar trick to save partial results (e.g., a"] # [doc = " cache) or something similar."] # [doc = ""] # [doc = " ### Combining fold with other operations"] # [doc = ""] # [doc = " You can combine `fold` with `reduce` if you want to produce a"] # [doc = " single value. This is then roughly equivalent to a map/reduce"] # [doc = " combination in effect:"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let bytes = 0..22_u8;"] # [doc = " let sum = bytes.into_par_iter()"] # [doc = "                .fold(|| 0_u32, |a: u32, b: u8| a + (b as u32))"] # [doc = "                .sum::<u32>();"] # [doc = ""] # [doc = " assert_eq!(sum, (0..22).sum()); // compare to sequential"] # [doc = " ```"] fn fold < T , ID , F > (self , identity : ID , fold_op : F) -> Fold < Self , ID , F > where F : Fn (T , Self :: Item) -> T + Sync + Send , ID : Fn () -> T + Sync + Send , T : Send , { Fold :: new (self , identity , fold_op) } # [doc = " Applies `fold_op` to the given `init` value with each item of this"] # [doc = " iterator, finally producing the value for further use."] # [doc = ""] # [doc = " This works essentially like `fold(|| init.clone(), fold_op)`, except"] # [doc = " it doesn't require the `init` type to be `Sync`, nor any other form"] # [doc = " of added synchronization."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let bytes = 0..22_u8;"] # [doc = " let sum = bytes.into_par_iter()"] # [doc = "                .fold_with(0_u32, |a: u32, b: u8| a + (b as u32))"] # [doc = "                .sum::<u32>();"] # [doc = ""] # [doc = " assert_eq!(sum, (0..22).sum()); // compare to sequential"] # [doc = " ```"] fn fold_with < F , T > (self , init : T , fold_op : F) -> FoldWith < Self , T , F > where F : Fn (T , Self :: Item) -> T + Sync + Send , T : Send + Clone , { FoldWith :: new (self , init , fold_op) } # [doc = " Performs a fallible parallel fold."] # [doc = ""] # [doc = " This is a variation of [`fold()`] for operations which can fail with"] # [doc = " `Option::None` or `Result::Err`.  The first such failure stops"] # [doc = " processing the local set of items, without affecting other folds in the"] # [doc = " iterator's subdivisions."] # [doc = ""] # [doc = " Often, `try_fold()` will be followed by [`try_reduce()`]"] # [doc = " for a final reduction and global short-circuiting effect."] # [doc = ""] # [doc = " [`fold()`]: #method.fold"] # [doc = " [`try_reduce()`]: #method.try_reduce"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let bytes = 0..22_u8;"] # [doc = " let sum = bytes.into_par_iter()"] # [doc = "                .try_fold(|| 0_u32, |a: u32, b: u8| a.checked_add(b as u32))"] # [doc = "                .try_reduce(|| 0, u32::checked_add);"] # [doc = ""] # [doc = " assert_eq!(sum, Some((0..22).sum())); // compare to sequential"] # [doc = " ```"] fn try_fold < T , R , ID , F > (self , identity : ID , fold_op : F) -> TryFold < Self , R , ID , F > where F : Fn (T , Self :: Item) -> R + Sync + Send , ID : Fn () -> T + Sync + Send , R : Try < Output = T > + Send , { TryFold :: new (self , identity , fold_op) } # [doc = " Performs a fallible parallel fold with a cloneable `init` value."] # [doc = ""] # [doc = " This combines the `init` semantics of [`fold_with()`] and the failure"] # [doc = " semantics of [`try_fold()`]."] # [doc = ""] # [doc = " [`fold_with()`]: #method.fold_with"] # [doc = " [`try_fold()`]: #method.try_fold"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let bytes = 0..22_u8;"] # [doc = " let sum = bytes.into_par_iter()"] # [doc = "                .try_fold_with(0_u32, |a: u32, b: u8| a.checked_add(b as u32))"] # [doc = "                .try_reduce(|| 0, u32::checked_add);"] # [doc = ""] # [doc = " assert_eq!(sum, Some((0..22).sum())); // compare to sequential"] # [doc = " ```"] fn try_fold_with < F , T , R > (self , init : T , fold_op : F) -> TryFoldWith < Self , R , F > where F : Fn (T , Self :: Item) -> R + Sync + Send , R : Try < Output = T > + Send , T : Clone + Send , { TryFoldWith :: new (self , init , fold_op) } # [doc = " Sums up the items in the iterator."] # [doc = ""] # [doc = " Note that the order in items will be reduced is not specified,"] # [doc = " so if the `+` operator is not truly [associative] \\(as is the"] # [doc = " case for floating point numbers), then the results are not"] # [doc = " fully deterministic."] # [doc = ""] # [doc = " [associative]: https://en.wikipedia.org/wiki/Associative_property"] # [doc = ""] # [doc = " Basically equivalent to `self.reduce(|| 0, |a, b| a + b)`,"] # [doc = " except that the type of `0` and the `+` operation may vary"] # [doc = " depending on the type of value being produced."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [1, 5, 7];"] # [doc = ""] # [doc = " let sum: i32 = a.par_iter().sum();"] # [doc = ""] # [doc = " assert_eq!(sum, 13);"] # [doc = " ```"] fn sum < S > (self) -> S where S : Send + Sum < Self :: Item > + Sum < S > , { sum :: sum (self) } # [doc = " Multiplies all the items in the iterator."] # [doc = ""] # [doc = " Note that the order in items will be reduced is not specified,"] # [doc = " so if the `*` operator is not truly [associative] \\(as is the"] # [doc = " case for floating point numbers), then the results are not"] # [doc = " fully deterministic."] # [doc = ""] # [doc = " [associative]: https://en.wikipedia.org/wiki/Associative_property"] # [doc = ""] # [doc = " Basically equivalent to `self.reduce(|| 1, |a, b| a * b)`,"] # [doc = " except that the type of `1` and the `*` operation may vary"] # [doc = " depending on the type of value being produced."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " fn factorial(n: u32) -> u32 {"] # [doc = "    (1..n+1).into_par_iter().product()"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(factorial(0), 1);"] # [doc = " assert_eq!(factorial(1), 1);"] # [doc = " assert_eq!(factorial(5), 120);"] # [doc = " ```"] fn product < P > (self) -> P where P : Send + Product < Self :: Item > + Product < P > , { product :: product (self) } # [doc = " Computes the minimum of all the items in the iterator. If the"] # [doc = " iterator is empty, `None` is returned; otherwise, `Some(min)`"] # [doc = " is returned."] # [doc = ""] # [doc = " Note that the order in which the items will be reduced is not"] # [doc = " specified, so if the `Ord` impl is not truly associative, then"] # [doc = " the results are not deterministic."] # [doc = ""] # [doc = " Basically equivalent to `self.reduce_with(|a, b| Ord::min(a, b))`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [45, 74, 32];"] # [doc = ""] # [doc = " assert_eq!(a.par_iter().min(), Some(&32));"] # [doc = ""] # [doc = " let b: [i32; 0] = [];"] # [doc = ""] # [doc = " assert_eq!(b.par_iter().min(), None);"] # [doc = " ```"] fn min (self) -> Option < Self :: Item > where Self :: Item : Ord , { self . reduce_with (Ord :: min) } # [doc = " Computes the minimum of all the items in the iterator with respect to"] # [doc = " the given comparison function. If the iterator is empty, `None` is"] # [doc = " returned; otherwise, `Some(min)` is returned."] # [doc = ""] # [doc = " Note that the order in which the items will be reduced is not"] # [doc = " specified, so if the comparison function is not associative, then"] # [doc = " the results are not deterministic."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [-3_i32, 77, 53, 240, -1];"] # [doc = ""] # [doc = " assert_eq!(a.par_iter().min_by(|x, y| x.cmp(y)), Some(&-3));"] # [doc = " ```"] fn min_by < F > (self , f : F) -> Option < Self :: Item > where F : Sync + Send + Fn (& Self :: Item , & Self :: Item) -> Ordering , { fn min < T > (f : impl Fn (& T , & T) -> Ordering) -> impl Fn (T , T) -> T { move | a , b | match f (& a , & b) { Ordering :: Greater => b , _ => a , } } self . reduce_with (min (f)) } # [doc = " Computes the item that yields the minimum value for the given"] # [doc = " function. If the iterator is empty, `None` is returned;"] # [doc = " otherwise, `Some(item)` is returned."] # [doc = ""] # [doc = " Note that the order in which the items will be reduced is not"] # [doc = " specified, so if the `Ord` impl is not truly associative, then"] # [doc = " the results are not deterministic."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [-3_i32, 34, 2, 5, -10, -3, -23];"] # [doc = ""] # [doc = " assert_eq!(a.par_iter().min_by_key(|x| x.abs()), Some(&2));"] # [doc = " ```"] fn min_by_key < K , F > (self , f : F) -> Option < Self :: Item > where K : Ord + Send , F : Sync + Send + Fn (& Self :: Item) -> K , { fn key < T , K > (f : impl Fn (& T) -> K) -> impl Fn (T) -> (K , T) { move | x | (f (& x) , x) } fn min_key < T , K : Ord > (a : (K , T) , b : (K , T)) -> (K , T) { match (a . 0) . cmp (& b . 0) { Ordering :: Greater => b , _ => a , } } let (_ , x) = self . map (key (f)) . reduce_with (min_key) ? ; Some (x) } # [doc = " Computes the maximum of all the items in the iterator. If the"] # [doc = " iterator is empty, `None` is returned; otherwise, `Some(max)`"] # [doc = " is returned."] # [doc = ""] # [doc = " Note that the order in which the items will be reduced is not"] # [doc = " specified, so if the `Ord` impl is not truly associative, then"] # [doc = " the results are not deterministic."] # [doc = ""] # [doc = " Basically equivalent to `self.reduce_with(|a, b| Ord::max(a, b))`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [45, 74, 32];"] # [doc = ""] # [doc = " assert_eq!(a.par_iter().max(), Some(&74));"] # [doc = ""] # [doc = " let b: [i32; 0] = [];"] # [doc = ""] # [doc = " assert_eq!(b.par_iter().max(), None);"] # [doc = " ```"] fn max (self) -> Option < Self :: Item > where Self :: Item : Ord , { self . reduce_with (Ord :: max) } # [doc = " Computes the maximum of all the items in the iterator with respect to"] # [doc = " the given comparison function. If the iterator is empty, `None` is"] # [doc = " returned; otherwise, `Some(max)` is returned."] # [doc = ""] # [doc = " Note that the order in which the items will be reduced is not"] # [doc = " specified, so if the comparison function is not associative, then"] # [doc = " the results are not deterministic."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [-3_i32, 77, 53, 240, -1];"] # [doc = ""] # [doc = " assert_eq!(a.par_iter().max_by(|x, y| x.abs().cmp(&y.abs())), Some(&240));"] # [doc = " ```"] fn max_by < F > (self , f : F) -> Option < Self :: Item > where F : Sync + Send + Fn (& Self :: Item , & Self :: Item) -> Ordering , { fn max < T > (f : impl Fn (& T , & T) -> Ordering) -> impl Fn (T , T) -> T { move | a , b | match f (& a , & b) { Ordering :: Greater => a , _ => b , } } self . reduce_with (max (f)) } # [doc = " Computes the item that yields the maximum value for the given"] # [doc = " function. If the iterator is empty, `None` is returned;"] # [doc = " otherwise, `Some(item)` is returned."] # [doc = ""] # [doc = " Note that the order in which the items will be reduced is not"] # [doc = " specified, so if the `Ord` impl is not truly associative, then"] # [doc = " the results are not deterministic."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [-3_i32, 34, 2, 5, -10, -3, -23];"] # [doc = ""] # [doc = " assert_eq!(a.par_iter().max_by_key(|x| x.abs()), Some(&34));"] # [doc = " ```"] fn max_by_key < K , F > (self , f : F) -> Option < Self :: Item > where K : Ord + Send , F : Sync + Send + Fn (& Self :: Item) -> K , { fn key < T , K > (f : impl Fn (& T) -> K) -> impl Fn (T) -> (K , T) { move | x | (f (& x) , x) } fn max_key < T , K : Ord > (a : (K , T) , b : (K , T)) -> (K , T) { match (a . 0) . cmp (& b . 0) { Ordering :: Greater => a , _ => b , } } let (_ , x) = self . map (key (f)) . reduce_with (max_key) ? ; Some (x) } # [doc = " Takes two iterators and creates a new iterator over both."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [0, 1, 2];"] # [doc = " let b = [9, 8, 7];"] # [doc = ""] # [doc = " let par_iter = a.par_iter().chain(b.par_iter());"] # [doc = ""] # [doc = " let chained: Vec<_> = par_iter.cloned().collect();"] # [doc = ""] # [doc = " assert_eq!(&chained[..], &[0, 1, 2, 9, 8, 7]);"] # [doc = " ```"] fn chain < C > (self , chain : C) -> Chain < Self , C :: Iter > where C : IntoParallelIterator < Item = Self :: Item > , { Chain :: new (self , chain . into_par_iter ()) } # [doc = " Searches for **some** item in the parallel iterator that"] # [doc = " matches the given predicate and returns it. This operation"] # [doc = " is similar to [`find` on sequential iterators][find] but"] # [doc = " the item returned may not be the **first** one in the parallel"] # [doc = " sequence which matches, since we search the entire sequence in parallel."] # [doc = ""] # [doc = " Once a match is found, we will attempt to stop processing"] # [doc = " the rest of the items in the iterator as soon as possible"] # [doc = " (just as `find` stops iterating once a match is found)."] # [doc = ""] # [doc = " [find]: Iterator::find()"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [1, 2, 3, 3];"] # [doc = ""] # [doc = " assert_eq!(a.par_iter().find_any(|&&x| x == 3), Some(&3));"] # [doc = ""] # [doc = " assert_eq!(a.par_iter().find_any(|&&x| x == 100), None);"] # [doc = " ```"] fn find_any < P > (self , predicate : P) -> Option < Self :: Item > where P : Fn (& Self :: Item) -> bool + Sync + Send , { find :: find (self , predicate) } # [doc = " Searches for the sequentially **first** item in the parallel iterator"] # [doc = " that matches the given predicate and returns it."] # [doc = ""] # [doc = " Once a match is found, all attempts to the right of the match"] # [doc = " will be stopped, while attempts to the left must continue in case"] # [doc = " an earlier match is found."] # [doc = ""] # [doc = " For added performance, you might consider using `find_first` in conjunction with"] # [doc = " [`by_exponential_blocks()`][IndexedParallelIterator::by_exponential_blocks]."] # [doc = ""] # [doc = " Note that not all parallel iterators have a useful order, much like"] # [doc = " sequential `HashMap` iteration, so \"first\" may be nebulous.  If you"] # [doc = " just want the first match that discovered anywhere in the iterator,"] # [doc = " `find_any` is a better choice."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [1, 2, 3, 3];"] # [doc = ""] # [doc = " assert_eq!(a.par_iter().find_first(|&&x| x == 3), Some(&3));"] # [doc = ""] # [doc = " assert_eq!(a.par_iter().find_first(|&&x| x == 100), None);"] # [doc = " ```"] fn find_first < P > (self , predicate : P) -> Option < Self :: Item > where P : Fn (& Self :: Item) -> bool + Sync + Send , { find_first_last :: find_first (self , predicate) } # [doc = " Searches for the sequentially **last** item in the parallel iterator"] # [doc = " that matches the given predicate and returns it."] # [doc = ""] # [doc = " Once a match is found, all attempts to the left of the match"] # [doc = " will be stopped, while attempts to the right must continue in case"] # [doc = " a later match is found."] # [doc = ""] # [doc = " Note that not all parallel iterators have a useful order, much like"] # [doc = " sequential `HashMap` iteration, so \"last\" may be nebulous.  When the"] # [doc = " order doesn't actually matter to you, `find_any` is a better choice."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [1, 2, 3, 3];"] # [doc = ""] # [doc = " assert_eq!(a.par_iter().find_last(|&&x| x == 3), Some(&3));"] # [doc = ""] # [doc = " assert_eq!(a.par_iter().find_last(|&&x| x == 100), None);"] # [doc = " ```"] fn find_last < P > (self , predicate : P) -> Option < Self :: Item > where P : Fn (& Self :: Item) -> bool + Sync + Send , { find_first_last :: find_last (self , predicate) } # [doc = " Applies the given predicate to the items in the parallel iterator"] # [doc = " and returns **any** non-None result of the map operation."] # [doc = ""] # [doc = " Once a non-None value is produced from the map operation, we will"] # [doc = " attempt to stop processing the rest of the items in the iterator"] # [doc = " as soon as possible."] # [doc = ""] # [doc = " Note that this method only returns **some** item in the parallel"] # [doc = " iterator that is not None from the map predicate. The item returned"] # [doc = " may not be the **first** non-None value produced in the parallel"] # [doc = " sequence, since the entire sequence is mapped over in parallel."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let c = [\"lol\", \"NaN\", \"5\", \"5\"];"] # [doc = ""] # [doc = " let found_number = c.par_iter().find_map_any(|s| s.parse().ok());"] # [doc = ""] # [doc = " assert_eq!(found_number, Some(5));"] # [doc = " ```"] fn find_map_any < P , R > (self , predicate : P) -> Option < R > where P : Fn (Self :: Item) -> Option < R > + Sync + Send , R : Send , { fn yes < T > (_ : & T) -> bool { true } self . filter_map (predicate) . find_any (yes) } # [doc = " Applies the given predicate to the items in the parallel iterator and"] # [doc = " returns the sequentially **first** non-None result of the map operation."] # [doc = ""] # [doc = " Once a non-None value is produced from the map operation, all attempts"] # [doc = " to the right of the match will be stopped, while attempts to the left"] # [doc = " must continue in case an earlier match is found."] # [doc = ""] # [doc = " Note that not all parallel iterators have a useful order, much like"] # [doc = " sequential `HashMap` iteration, so \"first\" may be nebulous. If you"] # [doc = " just want the first non-None value discovered anywhere in the iterator,"] # [doc = " `find_map_any` is a better choice."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let c = [\"lol\", \"NaN\", \"2\", \"5\"];"] # [doc = ""] # [doc = " let first_number = c.par_iter().find_map_first(|s| s.parse().ok());"] # [doc = ""] # [doc = " assert_eq!(first_number, Some(2));"] # [doc = " ```"] fn find_map_first < P , R > (self , predicate : P) -> Option < R > where P : Fn (Self :: Item) -> Option < R > + Sync + Send , R : Send , { fn yes < T > (_ : & T) -> bool { true } self . filter_map (predicate) . find_first (yes) } # [doc = " Applies the given predicate to the items in the parallel iterator and"] # [doc = " returns the sequentially **last** non-None result of the map operation."] # [doc = ""] # [doc = " Once a non-None value is produced from the map operation, all attempts"] # [doc = " to the left of the match will be stopped, while attempts to the right"] # [doc = " must continue in case a later match is found."] # [doc = ""] # [doc = " Note that not all parallel iterators have a useful order, much like"] # [doc = " sequential `HashMap` iteration, so \"first\" may be nebulous. If you"] # [doc = " just want the first non-None value discovered anywhere in the iterator,"] # [doc = " `find_map_any` is a better choice."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let c = [\"lol\", \"NaN\", \"2\", \"5\"];"] # [doc = ""] # [doc = " let last_number = c.par_iter().find_map_last(|s| s.parse().ok());"] # [doc = ""] # [doc = " assert_eq!(last_number, Some(5));"] # [doc = " ```"] fn find_map_last < P , R > (self , predicate : P) -> Option < R > where P : Fn (Self :: Item) -> Option < R > + Sync + Send , R : Send , { fn yes < T > (_ : & T) -> bool { true } self . filter_map (predicate) . find_last (yes) } # [doc (hidden)] # [deprecated (note = "parallel `find` does not search in order -- use `find_any`, \\
                         `find_first`, or `find_last`")] fn find < P > (self , predicate : P) -> Option < Self :: Item > where P : Fn (& Self :: Item) -> bool + Sync + Send , { self . find_any (predicate) } # [doc = " Searches for **some** item in the parallel iterator that"] # [doc = " matches the given predicate, and if so returns true.  Once"] # [doc = " a match is found, we'll attempt to stop process the rest"] # [doc = " of the items.  Proving that there's no match, returning false,"] # [doc = " does require visiting every item."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [0, 12, 3, 4, 0, 23, 0];"] # [doc = ""] # [doc = " let is_valid = a.par_iter().any(|&x| x > 10);"] # [doc = ""] # [doc = " assert!(is_valid);"] # [doc = " ```"] fn any < P > (self , predicate : P) -> bool where P : Fn (Self :: Item) -> bool + Sync + Send , { self . map (predicate) . find_any (bool :: clone) . is_some () } # [doc = " Tests that every item in the parallel iterator matches the given"] # [doc = " predicate, and if so returns true.  If a counter-example is found,"] # [doc = " we'll attempt to stop processing more items, then return false."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [0, 12, 3, 4, 0, 23, 0];"] # [doc = ""] # [doc = " let is_valid = a.par_iter().all(|&x| x > 10);"] # [doc = ""] # [doc = " assert!(!is_valid);"] # [doc = " ```"] fn all < P > (self , predicate : P) -> bool where P : Fn (Self :: Item) -> bool + Sync + Send , { # [inline] fn is_false (x : & bool) -> bool { ! x } self . map (predicate) . find_any (is_false) . is_none () } # [doc = " Creates an iterator over the `Some` items of this iterator, halting"] # [doc = " as soon as any `None` is found."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = " use std::sync::atomic::{AtomicUsize, Ordering};"] # [doc = ""] # [doc = " let counter = AtomicUsize::new(0);"] # [doc = " let value = (0_i32..2048)"] # [doc = "     .into_par_iter()"] # [doc = "     .map(|x| {"] # [doc = "              counter.fetch_add(1, Ordering::SeqCst);"] # [doc = "              if x < 1024 { Some(x) } else { None }"] # [doc = "          })"] # [doc = "     .while_some()"] # [doc = "     .max();"] # [doc = ""] # [doc = " assert!(value < Some(1024));"] # [doc = " assert!(counter.load(Ordering::SeqCst) < 2048); // should not have visited every single one"] # [doc = " ```"] fn while_some < T > (self) -> WhileSome < Self > where Self : ParallelIterator < Item = Option < T > > , T : Send , { WhileSome :: new (self) } # [doc = " Wraps an iterator with a fuse in case of panics, to halt all threads"] # [doc = " as soon as possible."] # [doc = ""] # [doc = " Panics within parallel iterators are always propagated to the caller,"] # [doc = " but they don't always halt the rest of the iterator right away, due to"] # [doc = " the internal semantics of [`join`]. This adaptor makes a greater effort"] # [doc = " to stop processing other items sooner, with the cost of additional"] # [doc = " synchronization overhead, which may also inhibit some optimizations."] # [doc = ""] # [doc = " [`join`]: crate::join()#panics"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " If this code didn't use `panic_fuse()`, it would continue processing"] # [doc = " many more items in other threads (with long sleep delays) before the"] # [doc = " panic is finally propagated."] # [doc = ""] # [doc = " ```should_panic"] # [doc = " use rayon::prelude::*;"] # [doc = " use std::{thread, time};"] # [doc = ""] # [doc = " (0..1_000_000)"] # [doc = "     .into_par_iter()"] # [doc = "     .panic_fuse()"] # [doc = "     .for_each(|i| {"] # [doc = "         // simulate some work"] # [doc = "         thread::sleep(time::Duration::from_secs(1));"] # [doc = "         assert!(i > 0); // oops!"] # [doc = "     });"] # [doc = " ```"] fn panic_fuse (self) -> PanicFuse < Self > { PanicFuse :: new (self) } # [doc = " Creates a fresh collection containing all the elements produced"] # [doc = " by this parallel iterator."] # [doc = ""] # [doc = " You may prefer [`collect_into_vec()`] implemented on"] # [doc = " [`IndexedParallelIterator`], if your underlying iterator also implements"] # [doc = " it. [`collect_into_vec()`] allocates efficiently with precise knowledge"] # [doc = " of how many elements the iterator contains, and even allows you to reuse"] # [doc = " an existing vector's backing store rather than allocating a fresh vector."] # [doc = ""] # [doc = " See also [`collect_vec_list()`] for collecting into a"] # [doc = " `LinkedList<Vec<T>>`."] # [doc = ""] # [doc = " [`collect_into_vec()`]: IndexedParallelIterator::collect_into_vec()"] # [doc = " [`collect_vec_list()`]: Self::collect_vec_list()"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let sync_vec: Vec<_> = (0..100).into_iter().collect();"] # [doc = ""] # [doc = " let async_vec: Vec<_> = (0..100).into_par_iter().collect();"] # [doc = ""] # [doc = " assert_eq!(sync_vec, async_vec);"] # [doc = " ```"] # [doc = ""] # [doc = " You can collect a pair of collections like [`unzip`](#method.unzip)"] # [doc = " for paired items:"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [(0, 1), (1, 2), (2, 3), (3, 4)];"] # [doc = " let (first, second): (Vec<_>, Vec<_>) = a.into_par_iter().collect();"] # [doc = ""] # [doc = " assert_eq!(first, [0, 1, 2, 3]);"] # [doc = " assert_eq!(second, [1, 2, 3, 4]);"] # [doc = " ```"] # [doc = ""] # [doc = " Or like [`partition_map`](#method.partition_map) for `Either` items:"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = " use rayon::iter::Either;"] # [doc = ""] # [doc = " let (left, right): (Vec<_>, Vec<_>) = (0..8).into_par_iter().map(|x| {"] # [doc = "     if x % 2 == 0 {"] # [doc = "         Either::Left(x * 4)"] # [doc = "     } else {"] # [doc = "         Either::Right(x * 3)"] # [doc = "     }"] # [doc = " }).collect();"] # [doc = ""] # [doc = " assert_eq!(left, [0, 8, 16, 24]);"] # [doc = " assert_eq!(right, [3, 9, 15, 21]);"] # [doc = " ```"] # [doc = ""] # [doc = " You can even collect an arbitrarily-nested combination of pairs and `Either`:"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = " use rayon::iter::Either;"] # [doc = ""] # [doc = " let (first, (left, right)): (Vec<_>, (Vec<_>, Vec<_>))"] # [doc = "     = (0..8).into_par_iter().map(|x| {"] # [doc = "         if x % 2 == 0 {"] # [doc = "             (x, Either::Left(x * 4))"] # [doc = "         } else {"] # [doc = "             (-x, Either::Right(x * 3))"] # [doc = "         }"] # [doc = "     }).collect();"] # [doc = ""] # [doc = " assert_eq!(first, [0, -1, 2, -3, 4, -5, 6, -7]);"] # [doc = " assert_eq!(left, [0, 8, 16, 24]);"] # [doc = " assert_eq!(right, [3, 9, 15, 21]);"] # [doc = " ```"] # [doc = ""] # [doc = " All of that can _also_ be combined with short-circuiting collection of"] # [doc = " `Result` or `Option` types:"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = " use rayon::iter::Either;"] # [doc = ""] # [doc = " let result: Result<(Vec<_>, (Vec<_>, Vec<_>)), _>"] # [doc = "     = (0..8).into_par_iter().map(|x| {"] # [doc = "         if x > 5 {"] # [doc = "             Err(x)"] # [doc = "         } else if x % 2 == 0 {"] # [doc = "             Ok((x, Either::Left(x * 4)))"] # [doc = "         } else {"] # [doc = "             Ok((-x, Either::Right(x * 3)))"] # [doc = "         }"] # [doc = "     }).collect();"] # [doc = ""] # [doc = " let error = result.unwrap_err();"] # [doc = " assert!(error == 6 || error == 7);"] # [doc = " ```"] fn collect < C > (self) -> C where C : FromParallelIterator < Self :: Item > , { C :: from_par_iter (self) } # [doc = " Unzips the items of a parallel iterator into a pair of arbitrary"] # [doc = " `ParallelExtend` containers."] # [doc = ""] # [doc = " You may prefer to use `unzip_into_vecs()`, which allocates more"] # [doc = " efficiently with precise knowledge of how many elements the"] # [doc = " iterator contains, and even allows you to reuse existing"] # [doc = " vectors' backing stores rather than allocating fresh vectors."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let a = [(0, 1), (1, 2), (2, 3), (3, 4)];"] # [doc = ""] # [doc = " let (left, right): (Vec<_>, Vec<_>) = a.par_iter().cloned().unzip();"] # [doc = ""] # [doc = " assert_eq!(left, [0, 1, 2, 3]);"] # [doc = " assert_eq!(right, [1, 2, 3, 4]);"] # [doc = " ```"] # [doc = ""] # [doc = " Nested pairs can be unzipped too."] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let (values, (squares, cubes)): (Vec<_>, (Vec<_>, Vec<_>)) = (0..4).into_par_iter()"] # [doc = "     .map(|i| (i, (i * i, i * i * i)))"] # [doc = "     .unzip();"] # [doc = ""] # [doc = " assert_eq!(values, [0, 1, 2, 3]);"] # [doc = " assert_eq!(squares, [0, 1, 4, 9]);"] # [doc = " assert_eq!(cubes, [0, 1, 8, 27]);"] # [doc = " ```"] fn unzip < A , B , FromA , FromB > (self) -> (FromA , FromB) where Self : ParallelIterator < Item = (A , B) > , FromA : Default + Send + ParallelExtend < A > , FromB : Default + Send + ParallelExtend < B > , A : Send , B : Send , { unzip :: unzip (self) } # [doc = " Partitions the items of a parallel iterator into a pair of arbitrary"] # [doc = " `ParallelExtend` containers.  Items for which the `predicate` returns"] # [doc = " true go into the first container, and the rest go into the second."] # [doc = ""] # [doc = " Note: unlike the standard `Iterator::partition`, this allows distinct"] # [doc = " collection types for the left and right items.  This is more flexible,"] # [doc = " but may require new type annotations when converting sequential code"] # [doc = " that used type inference assuming the two were the same."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let (left, right): (Vec<_>, Vec<_>) = (0..8).into_par_iter().partition(|x| x % 2 == 0);"] # [doc = ""] # [doc = " assert_eq!(left, [0, 2, 4, 6]);"] # [doc = " assert_eq!(right, [1, 3, 5, 7]);"] # [doc = " ```"] fn partition < A , B , P > (self , predicate : P) -> (A , B) where A : Default + Send + ParallelExtend < Self :: Item > , B : Default + Send + ParallelExtend < Self :: Item > , P : Fn (& Self :: Item) -> bool + Sync + Send , { unzip :: partition (self , predicate) } # [doc = " Partitions and maps the items of a parallel iterator into a pair of"] # [doc = " arbitrary `ParallelExtend` containers.  `Either::Left` items go into"] # [doc = " the first container, and `Either::Right` items go into the second."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = " use rayon::iter::Either;"] # [doc = ""] # [doc = " let (left, right): (Vec<_>, Vec<_>) = (0..8).into_par_iter()"] # [doc = "     .partition_map(|x| {"] # [doc = "         if x % 2 == 0 {"] # [doc = "             Either::Left(x * 4)"] # [doc = "         } else {"] # [doc = "             Either::Right(x * 3)"] # [doc = "         }"] # [doc = "     });"] # [doc = ""] # [doc = " assert_eq!(left, [0, 8, 16, 24]);"] # [doc = " assert_eq!(right, [3, 9, 15, 21]);"] # [doc = " ```"] # [doc = ""] # [doc = " Nested `Either` enums can be split as well."] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = " use rayon::iter::Either::*;"] # [doc = ""] # [doc = " let ((fizzbuzz, fizz), (buzz, other)): ((Vec<_>, Vec<_>), (Vec<_>, Vec<_>)) = (1..20)"] # [doc = "     .into_par_iter()"] # [doc = "     .partition_map(|x| match (x % 3, x % 5) {"] # [doc = "         (0, 0) => Left(Left(x)),"] # [doc = "         (0, _) => Left(Right(x)),"] # [doc = "         (_, 0) => Right(Left(x)),"] # [doc = "         (_, _) => Right(Right(x)),"] # [doc = "     });"] # [doc = ""] # [doc = " assert_eq!(fizzbuzz, [15]);"] # [doc = " assert_eq!(fizz, [3, 6, 9, 12, 18]);"] # [doc = " assert_eq!(buzz, [5, 10]);"] # [doc = " assert_eq!(other, [1, 2, 4, 7, 8, 11, 13, 14, 16, 17, 19]);"] # [doc = " ```"] fn partition_map < A , B , P , L , R > (self , predicate : P) -> (A , B) where A : Default + Send + ParallelExtend < L > , B : Default + Send + ParallelExtend < R > , P : Fn (Self :: Item) -> Either < L , R > + Sync + Send , L : Send , R : Send , { unzip :: partition_map (self , predicate) } # [doc = " Intersperses clones of an element between items of this iterator."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let x = vec![1, 2, 3];"] # [doc = " let r: Vec<_> = x.into_par_iter().intersperse(-1).collect();"] # [doc = ""] # [doc = " assert_eq!(r, vec![1, -1, 2, -1, 3]);"] # [doc = " ```"] fn intersperse (self , element : Self :: Item) -> Intersperse < Self > where Self :: Item : Clone , { Intersperse :: new (self , element) } # [doc = " Creates an iterator that yields `n` elements from *anywhere* in the original iterator."] # [doc = ""] # [doc = " This is similar to [`IndexedParallelIterator::take`] without being"] # [doc = " constrained to the \"first\" `n` of the original iterator order. The"] # [doc = " taken items will still maintain their relative order where that is"] # [doc = " visible in `collect`, `reduce`, and similar outputs."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let result: Vec<_> = (0..100)"] # [doc = "     .into_par_iter()"] # [doc = "     .filter(|&x| x % 2 == 0)"] # [doc = "     .take_any(5)"] # [doc = "     .collect();"] # [doc = ""] # [doc = " assert_eq!(result.len(), 5);"] # [doc = " assert!(result.windows(2).all(|w| w[0] < w[1]));"] # [doc = " ```"] fn take_any (self , n : usize) -> TakeAny < Self > { TakeAny :: new (self , n) } # [doc = " Creates an iterator that skips `n` elements from *anywhere* in the original iterator."] # [doc = ""] # [doc = " This is similar to [`IndexedParallelIterator::skip`] without being"] # [doc = " constrained to the \"first\" `n` of the original iterator order. The"] # [doc = " remaining items will still maintain their relative order where that is"] # [doc = " visible in `collect`, `reduce`, and similar outputs."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let result: Vec<_> = (0..100)"] # [doc = "     .into_par_iter()"] # [doc = "     .filter(|&x| x % 2 == 0)"] # [doc = "     .skip_any(5)"] # [doc = "     .collect();"] # [doc = ""] # [doc = " assert_eq!(result.len(), 45);"] # [doc = " assert!(result.windows(2).all(|w| w[0] < w[1]));"] # [doc = " ```"] fn skip_any (self , n : usize) -> SkipAny < Self > { SkipAny :: new (self , n) } # [doc = " Creates an iterator that takes elements from *anywhere* in the original iterator"] # [doc = " until the given `predicate` returns `false`."] # [doc = ""] # [doc = " The `predicate` may be anything -- e.g. it could be checking a fact about the item, a"] # [doc = " global condition unrelated to the item itself, or some combination thereof."] # [doc = ""] # [doc = " If parallel calls to the `predicate` race and give different results, then the"] # [doc = " `true` results will still take those particular items, while respecting the `false`"] # [doc = " result from elsewhere to skip any further items."] # [doc = ""] # [doc = " This is similar to [`Iterator::take_while`] without being constrained to the original"] # [doc = " iterator order. The taken items will still maintain their relative order where that is"] # [doc = " visible in `collect`, `reduce`, and similar outputs."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let result: Vec<_> = (0..100)"] # [doc = "     .into_par_iter()"] # [doc = "     .take_any_while(|x| *x < 50)"] # [doc = "     .collect();"] # [doc = ""] # [doc = " assert!(result.len() <= 50);"] # [doc = " assert!(result.windows(2).all(|w| w[0] < w[1]));"] # [doc = " ```"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = " use std::sync::atomic::AtomicUsize;"] # [doc = " use std::sync::atomic::Ordering::Relaxed;"] # [doc = ""] # [doc = " // Collect any group of items that sum <= 1000"] # [doc = " let quota = AtomicUsize::new(1000);"] # [doc = " let result: Vec<_> = (0_usize..100)"] # [doc = "     .into_par_iter()"] # [doc = "     .take_any_while(|&x| {"] # [doc = "         quota.fetch_update(Relaxed, Relaxed, |q| q.checked_sub(x))"] # [doc = "             .is_ok()"] # [doc = "     })"] # [doc = "     .collect();"] # [doc = ""] # [doc = " let sum = result.iter().sum::<usize>();"] # [doc = " assert!(matches!(sum, 902..=1000));"] # [doc = " ```"] fn take_any_while < P > (self , predicate : P) -> TakeAnyWhile < Self , P > where P : Fn (& Self :: Item) -> bool + Sync + Send , { TakeAnyWhile :: new (self , predicate) } # [doc = " Creates an iterator that skips elements from *anywhere* in the original iterator"] # [doc = " until the given `predicate` returns `false`."] # [doc = ""] # [doc = " The `predicate` may be anything -- e.g. it could be checking a fact about the item, a"] # [doc = " global condition unrelated to the item itself, or some combination thereof."] # [doc = ""] # [doc = " If parallel calls to the `predicate` race and give different results, then the"] # [doc = " `true` results will still skip those particular items, while respecting the `false`"] # [doc = " result from elsewhere to skip any further items."] # [doc = ""] # [doc = " This is similar to [`Iterator::skip_while`] without being constrained to the original"] # [doc = " iterator order. The remaining items will still maintain their relative order where that is"] # [doc = " visible in `collect`, `reduce`, and similar outputs."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let result: Vec<_> = (0..100)"] # [doc = "     .into_par_iter()"] # [doc = "     .skip_any_while(|x| *x < 50)"] # [doc = "     .collect();"] # [doc = ""] # [doc = " assert!(result.len() >= 50);"] # [doc = " assert!(result.windows(2).all(|w| w[0] < w[1]));"] # [doc = " ```"] fn skip_any_while < P > (self , predicate : P) -> SkipAnyWhile < Self , P > where P : Fn (& Self :: Item) -> bool + Sync + Send , { SkipAnyWhile :: new (self , predicate) } # [doc = " Collects this iterator into a linked list of vectors."] # [doc = ""] # [doc = " This is useful when you need to condense a parallel iterator into a collection,"] # [doc = " but have no specific requirements for what that collection should be. If you"] # [doc = " plan to store the collection longer-term, `Vec<T>` is, as always, likely the"] # [doc = " best default choice, despite the overhead that comes from concatenating each"] # [doc = " vector. Or, if this is an `IndexedParallelIterator`, you should also prefer to"] # [doc = " just collect to a `Vec<T>`."] # [doc = ""] # [doc = " Internally, most [`FromParallelIterator`]/[`ParallelExtend`] implementations"] # [doc = " use this strategy; each job collecting their chunk of the iterator to a `Vec<T>`"] # [doc = " and those chunks getting merged into a `LinkedList`, before then extending the"] # [doc = " collection with each vector. This is a very efficient way to collect an"] # [doc = " unindexed parallel iterator, without much intermediate data movement."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use std::collections::LinkedList;"] # [doc = " use rayon::prelude::*;"] # [doc = ""] # [doc = " let result: LinkedList<Vec<_>> = (0..=100)"] # [doc = "     .into_par_iter()"] # [doc = "     .filter(|x| x % 2 == 0)"] # [doc = "     .flat_map(|x| 0..x)"] # [doc = "     .collect_vec_list();"] # [doc = ""] # [doc = " // `par_iter.collect_vec_list().into_iter().flatten()` turns"] # [doc = " // a parallel iterator into a serial one"] # [doc = " let total_len = result.into_iter().flatten().count();"] # [doc = " assert_eq!(total_len, 2550);"] # [doc = " ```"] fn collect_vec_list (self) -> LinkedList < Vec < Self :: Item > > { match extend :: fast_collect (self) { Either :: Left (vec) => { let mut list = LinkedList :: new () ; if ! vec . is_empty () { list . push_back (vec) ; } list } Either :: Right (list) => list , } } # [doc = " Internal method used to define the behavior of this parallel"] # [doc = " iterator. You should not need to call this directly."] # [doc = ""] # [doc = " This method causes the iterator `self` to start producing"] # [doc = " items and to feed them to the consumer `consumer` one by one."] # [doc = " It may split the consumer before doing so to create the"] # [doc = " opportunity to produce in parallel."] # [doc = ""] # [doc = " See the [README] for more details on the internals of parallel"] # [doc = " iterators."] # [doc = ""] # [doc = " [README]: https://github.com/rayon-rs/rayon/blob/main/src/iter/plumbing/README.md"] fn drive_unindexed < C > (self , consumer : C) -> C :: Result where C : UnindexedConsumer < Self :: Item > ; # [doc = " Internal method used to define the behavior of this parallel"] # [doc = " iterator. You should not need to call this directly."] # [doc = ""] # [doc = " Returns the number of items produced by this iterator, if known"] # [doc = " statically. This can be used by consumers to trigger special fast"] # [doc = " paths. Therefore, if `Some(_)` is returned, this iterator must only"] # [doc = " use the (indexed) `Consumer` methods when driving a consumer, such"] # [doc = " as `split_at()`. Calling `UnindexedConsumer::split_off_left()` or"] # [doc = " other `UnindexedConsumer` methods -- or returning an inaccurate"] # [doc = " value -- may result in panics."] # [doc = ""] # [doc = " This method is currently used to optimize `collect` for want"] # [doc = " of true Rust specialization; it may be removed when"] # [doc = " specialization is stable."] fn opt_len (& self) -> Option < usize > { None } }
    };
}

ParallelIterator!();