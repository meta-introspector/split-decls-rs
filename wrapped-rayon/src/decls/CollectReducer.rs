macro_rules! CollectReducer {
    () => {
        # [doc = " CollectReducer combines adjacent chunks; the result must always"] # [doc = " be contiguous so that it is one combined slice."] pub (super) struct CollectReducer ;
    };
}

CollectReducer!()