macro_rules! SplitWhitespace {
    () => {
        # [doc = " Parallel iterator over substrings separated by whitespace"] # [derive (Debug , Clone)] pub struct SplitWhitespace < 'ch > (& 'ch str) ;
    };
}

SplitWhitespace!();