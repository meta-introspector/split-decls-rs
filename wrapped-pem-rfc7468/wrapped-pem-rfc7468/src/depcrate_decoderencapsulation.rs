// Generated macro for Encapsulation (struct)
macro_rules! Depcrate_decoderEncapsulation {
() => {
// Module: crate::decoder
// Provides: {"Encapsulation"}
// Dependencies: {}
# [doc = " PEM encapsulation parser."] # [doc = ""] # [doc = " This parser performs an initial pass over the data, locating the"] # [doc = " pre-encapsulation (`---BEGIN [...]---`) and post-encapsulation"] # [doc = " (`---END [...]`) boundaries while attempting to avoid branching"] # [doc = " on the potentially secret Base64-encoded data encapsulated between"] # [doc = " the two boundaries."] # [doc = ""] # [doc = " It only supports a single encapsulated message at present. Future work"] # [doc = " could potentially include extending it provide an iterator over a series"] # [doc = " of encapsulated messages."] # [derive (Copy , Clone , Debug)] struct Encapsulation < 'a > { # [doc = " Type label extracted from the pre/post-encapsulation boundaries."] # [doc = ""] # [doc = " From RFC 7468 Section 2:"] # [doc = ""] # [doc = " > The type of data encoded is labeled depending on the type label in"] # [doc = " > the \"-----BEGIN \" line (pre-encapsulation boundary).  For example,"] # [doc = " > the line may be \"-----BEGIN CERTIFICATE-----\" to indicate that the"] # [doc = " > content is a PKIX certificate (see further below).  Generators MUST"] # [doc = " > put the same label on the \"-----END \" line (post-encapsulation"] # [doc = " > boundary) as the corresponding \"-----BEGIN \" line.  Labels are"] # [doc = " > formally case-sensitive, uppercase, and comprised of zero or more"] # [doc = " > characters; they do not contain consecutive spaces or hyphen-minuses,"] # [doc = " > nor do they contain spaces or hyphen-minuses at either end.  Parsers"] # [doc = " > MAY disregard the label in the post-encapsulation boundary instead of"] # [doc = " > signaling an error if there is a label mismatch: some extant"] # [doc = " > implementations require the labels to match; others do not."] label : & 'a str , # [doc = " Encapsulated text portion contained between the boundaries."] # [doc = ""] # [doc = " This data should be encoded as Base64, however this type performs no"] # [doc = " validation of it so it can be handled in constant-time."] encapsulated_text : & 'a [u8] , }
};
}
