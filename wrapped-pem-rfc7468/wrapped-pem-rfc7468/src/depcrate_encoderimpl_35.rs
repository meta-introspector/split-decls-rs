// Generated macro for impl_35 (impl)
macro_rules! Depcrate_encoderimpl_35 {
() => {
// Module: crate::encoder
// Provides: {"impl_35"}
// Dependencies: {}
impl < 'l , 'o > Encoder < 'l , 'o > { # [doc = " Create a new PEM [`Encoder`] with the default options which"] # [doc = " writes output into the provided buffer."] # [doc = ""] # [doc = " Uses the default 64-character line wrapping."] pub fn new (type_label : & 'l str , line_ending : LineEnding , out : & 'o mut [u8]) -> Result < Self > { Self :: new_wrapped (type_label , BASE64_WRAP_WIDTH , line_ending , out) } # [doc = " Create a new PEM [`Encoder`] which wraps at the given line width."] # [doc = ""] # [doc = " Note that per [RFC7468 § 2] encoding PEM with any other wrap width besides"] # [doc = " 64 is technically non-compliant:"] # [doc = ""] # [doc = " > Generators MUST wrap the base64-encoded lines so that each line"] # [doc = " > consists of exactly 64 characters except for the final line, which"] # [doc = " > will encode the remainder of the data (within the 64-character line"] # [doc = " > boundary)"] # [doc = ""] # [doc = " This method is provided with the intended purpose of implementing the"] # [doc = " OpenSSH private key format, which uses a non-standard wrap width of 70."] # [doc = ""] # [doc = " [RFC7468 § 2]: https://datatracker.ietf.org/doc/html/rfc7468#section-2"] pub fn new_wrapped (type_label : & 'l str , line_width : usize , line_ending : LineEnding , mut out : & 'o mut [u8] ,) -> Result < Self > { grammar :: validate_label (type_label . as_bytes ()) ? ; for boundary_part in [PRE_ENCAPSULATION_BOUNDARY , type_label . as_bytes () , ENCAPSULATION_BOUNDARY_DELIMITER , line_ending . as_bytes () ,] { if out . len () < boundary_part . len () { return Err (Error :: Length) ; } let (part , rest) = out . split_at_mut (boundary_part . len ()) ; out = rest ; part . copy_from_slice (boundary_part) ; } let base64 = Base64Encoder :: new_wrapped (out , line_width , line_ending) ? ; Ok (Self { type_label , line_ending , base64 , }) } # [doc = " Get the PEM type label used for this document."] pub fn type_label (& self) -> & 'l str { self . type_label } # [doc = " Encode the provided input data."] # [doc = ""] # [doc = " This method can be called as many times as needed with any sized input"] # [doc = " to write data encoded data into the output buffer, so long as there is"] # [doc = " sufficient space in the buffer to handle the resulting Base64 encoded"] # [doc = " data."] pub fn encode (& mut self , input : & [u8]) -> Result < () > { self . base64 . encode (input) ? ; Ok (()) } # [doc = " Borrow the inner [`Base64Encoder`]."] pub fn base64_encoder (& mut self) -> & mut Base64Encoder < 'o > { & mut self . base64 } # [doc = " Finish encoding PEM, writing the post-encapsulation boundary."] # [doc = ""] # [doc = " On success, returns the total number of bytes written to the output"] # [doc = " buffer."] pub fn finish (self) -> Result < usize > { let (base64 , mut out) = self . base64 . finish_with_remaining () ? ; for boundary_part in [self . line_ending . as_bytes () , POST_ENCAPSULATION_BOUNDARY , self . type_label . as_bytes () , ENCAPSULATION_BOUNDARY_DELIMITER , self . line_ending . as_bytes () ,] { if out . len () < boundary_part . len () { return Err (Error :: Length) ; } let (part , rest) = out . split_at_mut (boundary_part . len ()) ; out = rest ; part . copy_from_slice (boundary_part) ; } encapsulated_len_inner (self . type_label , self . line_ending , base64 . len ()) } }
};
}
