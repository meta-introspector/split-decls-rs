// Generated macro for RawStorage (trait)
macro_rules! Depcrate_base_storageRawStorage {
() => {
// Module: crate::base::storage
// Provides: {"RawStorage"}
// Dependencies: {}
# [doc = " The trait shared by all matrix data storage."] # [doc = ""] # [doc = " TODO: doc"] # [doc = " # Safety"] # [doc = ""] # [doc = " In generic code, it is recommended use the `Storage` trait bound instead. The `RawStorage`"] # [doc = " trait bound is generally used by code that needs to work with storages that contains"] # [doc = " `MaybeUninit<T>` elements."] # [doc = ""] # [doc = " Note that `Self` must always have a number of elements compatible with the matrix length (given"] # [doc = " by `R` and `C` if they are known at compile-time). For example, implementors of this trait"] # [doc = " should **not** allow the user to modify the size of the underlying buffer with safe methods"] # [doc = " (for example the `VecStorage::data_mut` method is unsafe because the user could change the"] # [doc = " vector's size so that it no longer contains enough elements: this will lead to UB."] pub unsafe trait RawStorage < T , R : Dim , C : Dim = U1 > : Sized { # [doc = " The static stride of this storage's rows."] type RStride : Dim ; # [doc = " The static stride of this storage's columns."] type CStride : Dim ; # [doc = " The matrix data pointer."] fn ptr (& self) -> * const T ; # [doc = " The dimension of the matrix at run-time. Arr length of zero indicates the additive identity"] # [doc = " element of any dimension. Must be equal to `Self::dimension()` if it is not `None`."] fn shape (& self) -> (R , C) ; # [doc = " The spacing between consecutive row elements and consecutive column elements."] # [doc = ""] # [doc = " For example this returns `(1, 5)` for a column-major matrix with 5 columns."] fn strides (& self) -> (Self :: RStride , Self :: CStride) ; # [doc = " Compute the index corresponding to the irow-th row and icol-th column of this matrix. The"] # [doc = " index must be such that the following holds:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " let lindex = self.linear_index(irow, icol);"] # [doc = " assert!(*self.get_unchecked(irow, icol) == *self.get_unchecked_linear(lindex))"] # [doc = " ```"] # [inline] fn linear_index (& self , irow : usize , icol : usize) -> usize { let (rstride , cstride) = self . strides () ; irow * rstride . value () + icol * cstride . value () } # [doc = " Gets the address of the i-th matrix component without performing bound-checking."] # [doc = ""] # [doc = " # Safety"] # [doc = " If the index is out of bounds, dereferencing the result will cause undefined behavior."] # [inline] fn get_address_unchecked_linear (& self , i : usize) -> * const T { self . ptr () . wrapping_add (i) } # [doc = " Gets the address of the i-th matrix component without performing bound-checking."] # [doc = ""] # [doc = " # Safety"] # [doc = " If the index is out of bounds, dereferencing the result will cause undefined behavior."] # [inline] fn get_address_unchecked (& self , irow : usize , icol : usize) -> * const T { self . get_address_unchecked_linear (self . linear_index (irow , icol)) } # [doc = " Retrieves a reference to the i-th element without bound-checking."] # [doc = ""] # [doc = " # Safety"] # [doc = " If the index is out of bounds, the method will cause undefined behavior."] # [inline] unsafe fn get_unchecked_linear (& self , i : usize) -> & T { & * self . get_address_unchecked_linear (i) } # [doc = " Retrieves a reference to the i-th element without bound-checking."] # [doc = ""] # [doc = " # Safety"] # [doc = " If the index is out of bounds, the method will cause undefined behavior."] # [inline] unsafe fn get_unchecked (& self , irow : usize , icol : usize) -> & T { self . get_unchecked_linear (self . linear_index (irow , icol)) } # [doc = " Indicates whether this data buffer stores its elements contiguously."] # [doc = ""] # [doc = " # Safety"] # [doc = " This function must not return `true` if the underlying storage is not contiguous,"] # [doc = " or undefined behaviour will occur."] fn is_contiguous (& self) -> bool ; # [doc = " Retrieves the data buffer as a contiguous slice."] # [doc = ""] # [doc = " # Safety"] # [doc = " The matrix components may not be stored in a contiguous way, depending on the strides."] # [doc = " This method is unsafe because this can yield to invalid aliasing when called on some pairs"] # [doc = " of matrix views originating from the same matrix with strides."] # [doc = ""] # [doc = " Call the safe alternative `matrix.as_slice()` instead."] unsafe fn as_slice_unchecked (& self) -> & [T] ; }
};
}
