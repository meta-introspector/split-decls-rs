// Generated macro for impl_2776 (impl)
macro_rules! Depcrate_linalg_decompositionimpl_2776 {
() => {
// Module: crate::linalg::decomposition
// Provides: {"impl_2776"}
// Dependencies: {}
# [doc = " # Square matrix decomposition"] # [doc = ""] # [doc = " This section contains the methods for computing some common decompositions of square"] # [doc = " matrices with real or complex components. The following are currently supported:"] # [doc = ""] # [doc = " | Decomposition            | Factors                   | Details |"] # [doc = " | -------------------------|---------------------------|--------------|"] # [doc = " | Hessenberg               | `Q * H * Qᵀ`             | `Q` is a unitary matrix and `H` an upper-Hessenberg matrix. |"] # [doc = " | Cholesky                 | `L * Lᵀ`                 | `L` is a lower-triangular matrix. |"] # [doc = " | UDU                      | `U * D * Uᵀ`             | `U` is a upper-triangular matrix, and `D` a diagonal matrix. |"] # [doc = " | Schur decomposition      | `Q * T * Qᵀ`             | `Q` is an unitary matrix and `T` a quasi-upper-triangular matrix. |"] # [doc = " | Symmetric eigendecomposition | `Q ~ Λ ~ Qᵀ`   | `Q` is an unitary matrix, and `Λ` is a real diagonal matrix. |"] # [doc = " | Symmetric tridiagonalization | `Q ~ T ~ Qᵀ`   | `Q` is an unitary matrix, and `T` is a tridiagonal matrix. |"] impl < T : ComplexField , D : Dim , S : Storage < T , D , D > > Matrix < T , D , D , S > { # [doc = " Attempts to compute the Cholesky decomposition of this matrix."] # [doc = ""] # [doc = " Returns `None` if the input matrix is not definite-positive. The input matrix is assumed"] # [doc = " to be symmetric and only the lower-triangular part is read."] pub fn cholesky (self) -> Option < Cholesky < T , D > > where DefaultAllocator : Allocator < D , D > , { Cholesky :: new (self . into_owned ()) } # [doc = " Attempts to compute the UDU decomposition of this matrix."] # [doc = ""] # [doc = " The input matrix `self` is assumed to be symmetric and this decomposition will only read"] # [doc = " the upper-triangular part of `self`."] pub fn udu (self) -> Option < UDU < T , D > > where T : RealField , DefaultAllocator : Allocator < D > + Allocator < D , D > , { UDU :: new (self . into_owned ()) } # [doc = " Computes the Hessenberg decomposition of this matrix using householder reflections."] pub fn hessenberg (self) -> Hessenberg < T , D > where D : DimSub < U1 > , DefaultAllocator : Allocator < D , D > + Allocator < D > + Allocator < DimDiff < D , U1 > > , { Hessenberg :: new (self . into_owned ()) } # [doc = " Computes the Schur decomposition of a square matrix."] pub fn schur (self) -> Schur < T , D > where D : DimSub < U1 > , DefaultAllocator : Allocator < D , DimDiff < D , U1 > > + Allocator < DimDiff < D , U1 > > + Allocator < D , D > + Allocator < D > , { Schur :: new (self . into_owned ()) } # [doc = " Attempts to compute the Schur decomposition of a square matrix."] # [doc = ""] # [doc = " If only eigenvalues are needed, it is more efficient to call the matrix method"] # [doc = " `.eigenvalues()` instead."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `eps`       − tolerance used to determine when a value converged to 0."] # [doc = " * `max_niter` − maximum total number of iterations performed by the algorithm. If this"] # [doc = " number of iteration is exceeded, `None` is returned. If `niter == 0`, then the algorithm"] # [doc = " continues indefinitely until convergence."] pub fn try_schur (self , eps : T :: RealField , max_niter : usize) -> Option < Schur < T , D > > where D : DimSub < U1 > , DefaultAllocator : Allocator < D , DimDiff < D , U1 > > + Allocator < DimDiff < D , U1 > > + Allocator < D , D > + Allocator < D > , { Schur :: try_new (self . into_owned () , eps , max_niter) } # [doc = " Computes the eigendecomposition of this symmetric matrix."] # [doc = ""] # [doc = " Only the lower-triangular part (including the diagonal) of `m` is read."] pub fn symmetric_eigen (self) -> SymmetricEigen < T , D > where D : DimSub < U1 > , DefaultAllocator : Allocator < D , D > + Allocator < DimDiff < D , U1 > > + Allocator < D > + Allocator < DimDiff < D , U1 > > , { SymmetricEigen :: new (self . into_owned ()) } # [doc = " Computes the eigendecomposition of the given symmetric matrix with user-specified"] # [doc = " convergence parameters."] # [doc = ""] # [doc = " Only the lower-triangular part (including the diagonal) of `m` is read."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `eps`       − tolerance used to determine when a value converged to 0."] # [doc = " * `max_niter` − maximum total number of iterations performed by the algorithm. If this"] # [doc = " number of iteration is exceeded, `None` is returned. If `niter == 0`, then the algorithm"] # [doc = " continues indefinitely until convergence."] pub fn try_symmetric_eigen (self , eps : T :: RealField , max_niter : usize ,) -> Option < SymmetricEigen < T , D > > where D : DimSub < U1 > , DefaultAllocator : Allocator < D , D > + Allocator < DimDiff < D , U1 > > + Allocator < D > + Allocator < DimDiff < D , U1 > > , { SymmetricEigen :: try_new (self . into_owned () , eps , max_niter) } # [doc = " Computes the tridiagonalization of this symmetric matrix."] # [doc = ""] # [doc = " Only the lower-triangular part (including the diagonal) of `m` is read."] pub fn symmetric_tridiagonalize (self) -> SymmetricTridiagonal < T , D > where D : DimSub < U1 > , DefaultAllocator : Allocator < D , D > + Allocator < DimDiff < D , U1 > > , { SymmetricTridiagonal :: new (self . into_owned ()) } }
};
}
