// Generated macro for impl_2775 (impl)
macro_rules! Depcrate_linalg_decompositionimpl_2775 {
() => {
// Module: crate::linalg::decomposition
// Provides: {"impl_2775"}
// Dependencies: {}
# [doc = " # Rectangular matrix decomposition"] # [doc = ""] # [doc = " This section contains the methods for computing some common decompositions of rectangular"] # [doc = " matrices with real or complex components. The following are currently supported:"] # [doc = ""] # [doc = " | Decomposition            | Factors             | Details |"] # [doc = " | -------------------------|---------------------|--------------|"] # [doc = " | QR                       | `Q * R`             | `Q` is an unitary matrix, and `R` is upper-triangular. |"] # [doc = " | QR with column pivoting  | `Q * R * P⁻¹`       | `Q` is an unitary matrix, and `R` is upper-triangular. `P` is a permutation matrix. |"] # [doc = " | LU with partial pivoting | `P⁻¹ * L * U`       | `L` is lower-triangular with a diagonal filled with `1` and `U` is upper-triangular. `P` is a permutation matrix. |"] # [doc = " | LU with full pivoting    | `P⁻¹ * L * U * Q⁻¹` | `L` is lower-triangular with a diagonal filled with `1` and `U` is upper-triangular. `P` and `Q` are permutation matrices. |"] # [doc = " | SVD                      | `U * Σ * Vᵀ`        | `U` and `V` are two orthogonal matrices and `Σ` is a diagonal matrix containing the singular values. |"] # [doc = " | Polar (Left Polar)       | `P' * U`            | `U` is semi-unitary/unitary and `P'` is a positive semi-definite Hermitian Matrix"] impl < T : ComplexField , R : Dim , C : Dim , S : Storage < T , R , C > > Matrix < T , R , C , S > { # [doc = " Computes the bidiagonalization using householder reflections."] pub fn bidiagonalize (self) -> Bidiagonal < T , R , C > where R : DimMin < C > , DimMinimum < R , C > : DimSub < U1 > , DefaultAllocator : Allocator < R , C > + Allocator < C > + Allocator < R > + Allocator < DimMinimum < R , C > > + Allocator < DimDiff < DimMinimum < R , C > , U1 > > , { Bidiagonal :: new (self . into_owned ()) } # [doc = " Computes the LU decomposition with full pivoting of `matrix`."] # [doc = ""] # [doc = " This effectively computes `P, L, U, Q` such that `P * matrix * Q = LU`."] pub fn full_piv_lu (self) -> FullPivLU < T , R , C > where R : DimMin < C > , DefaultAllocator : Allocator < R , C > + Allocator < DimMinimum < R , C > > , { FullPivLU :: new (self . into_owned ()) } # [doc = " Computes the LU decomposition with partial (row) pivoting of `matrix`."] pub fn lu (self) -> LU < T , R , C > where R : DimMin < C > , DefaultAllocator : Allocator < R , C > + Allocator < DimMinimum < R , C > > , { LU :: new (self . into_owned ()) } # [doc = " Computes the QR decomposition of this matrix."] pub fn qr (self) -> QR < T , R , C > where R : DimMin < C > , DefaultAllocator : Allocator < R , C > + Allocator < R > + Allocator < DimMinimum < R , C > > , { QR :: new (self . into_owned ()) } # [doc = " Computes the QR decomposition (with column pivoting) of this matrix."] pub fn col_piv_qr (self) -> ColPivQR < T , R , C > where R : DimMin < C > , DefaultAllocator : Allocator < R , C > + Allocator < R > + Allocator < DimMinimum < R , C > > + Allocator < DimMinimum < R , C > > , { ColPivQR :: new (self . into_owned ()) } # [doc = " Computes the Singular Value Decomposition using implicit shift."] # [doc = " The singular values are guaranteed to be sorted in descending order."] # [doc = " If this order is not required consider using `svd_unordered`."] pub fn svd (self , compute_u : bool , compute_v : bool) -> SVD < T , R , C > where R : DimMin < C > , DimMinimum < R , C > : DimSub < U1 > , DefaultAllocator : Allocator < R , C > + Allocator < C > + Allocator < R > + Allocator < DimDiff < DimMinimum < R , C > , U1 > > + Allocator < DimMinimum < R , C > , C > + Allocator < R , DimMinimum < R , C > > + Allocator < DimMinimum < R , C > > , { SVD :: new (self . into_owned () , compute_u , compute_v) } # [doc = " Computes the Singular Value Decomposition using implicit shift."] # [doc = " The singular values are not guaranteed to be sorted in any particular order."] # [doc = " If a descending order is required, consider using `svd` instead."] pub fn svd_unordered (self , compute_u : bool , compute_v : bool) -> SVD < T , R , C > where R : DimMin < C > , DimMinimum < R , C > : DimSub < U1 > , DefaultAllocator : Allocator < R , C > + Allocator < C > + Allocator < R > + Allocator < DimDiff < DimMinimum < R , C > , U1 > > + Allocator < DimMinimum < R , C > , C > + Allocator < R , DimMinimum < R , C > > + Allocator < DimMinimum < R , C > > , { SVD :: new_unordered (self . into_owned () , compute_u , compute_v) } # [doc = " Attempts to compute the Singular Value Decomposition of `matrix` using implicit shift."] # [doc = " The singular values are guaranteed to be sorted in descending order."] # [doc = " If this order is not required consider using `try_svd_unordered`."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `compute_u` − set this to `true` to enable the computation of left-singular vectors."] # [doc = " * `compute_v` − set this to `true` to enable the computation of right-singular vectors."] # [doc = " * `eps`       − tolerance used to determine when a value converged to 0."] # [doc = " * `max_niter` − maximum total number of iterations performed by the algorithm. If this"] # [doc = " number of iteration is exceeded, `None` is returned. If `niter == 0`, then the algorithm"] # [doc = " continues indefinitely until convergence."] pub fn try_svd (self , compute_u : bool , compute_v : bool , eps : T :: RealField , max_niter : usize ,) -> Option < SVD < T , R , C > > where R : DimMin < C > , DimMinimum < R , C > : DimSub < U1 > , DefaultAllocator : Allocator < R , C > + Allocator < C > + Allocator < R > + Allocator < DimDiff < DimMinimum < R , C > , U1 > > + Allocator < DimMinimum < R , C > , C > + Allocator < R , DimMinimum < R , C > > + Allocator < DimMinimum < R , C > > , { SVD :: try_new (self . into_owned () , compute_u , compute_v , eps , max_niter) } # [doc = " Attempts to compute the Singular Value Decomposition of `matrix` using implicit shift."] # [doc = " The singular values are not guaranteed to be sorted in any particular order."] # [doc = " If a descending order is required, consider using `try_svd` instead."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `compute_u` − set this to `true` to enable the computation of left-singular vectors."] # [doc = " * `compute_v` − set this to `true` to enable the computation of right-singular vectors."] # [doc = " * `eps`       − tolerance used to determine when a value converged to 0."] # [doc = " * `max_niter` − maximum total number of iterations performed by the algorithm. If this"] # [doc = " number of iteration is exceeded, `None` is returned. If `niter == 0`, then the algorithm"] # [doc = " continues indefinitely until convergence."] pub fn try_svd_unordered (self , compute_u : bool , compute_v : bool , eps : T :: RealField , max_niter : usize ,) -> Option < SVD < T , R , C > > where R : DimMin < C > , DimMinimum < R , C > : DimSub < U1 > , DefaultAllocator : Allocator < R , C > + Allocator < C > + Allocator < R > + Allocator < DimDiff < DimMinimum < R , C > , U1 > > + Allocator < DimMinimum < R , C > , C > + Allocator < R , DimMinimum < R , C > > + Allocator < DimMinimum < R , C > > + Allocator < DimMinimum < R , C > > + Allocator < DimDiff < DimMinimum < R , C > , U1 > > , { SVD :: try_new_unordered (self . into_owned () , compute_u , compute_v , eps , max_niter) } # [doc = " Computes the Polar Decomposition of  a `matrix` (indirectly uses SVD)."] pub fn polar (self) -> (OMatrix < T , R , R > , OMatrix < T , R , C >) where R : DimMin < C > , DimMinimum < R , C > : DimSub < U1 > , DefaultAllocator : Allocator < R , C > + Allocator < DimMinimum < R , C > , R > + Allocator < DimMinimum < R , C > > + Allocator < R , R > + Allocator < DimMinimum < R , C > , DimMinimum < R , C > > + Allocator < C > + Allocator < R > + Allocator < DimDiff < DimMinimum < R , C > , U1 > > + Allocator < DimMinimum < R , C > , C > + Allocator < R , DimMinimum < R , C > > + Allocator < DimMinimum < R , C > > + Allocator < DimMinimum < R , C > > + Allocator < DimDiff < DimMinimum < R , C > , U1 > > , { SVD :: new_unordered (self . into_owned () , true , true) . to_polar () . unwrap () } # [doc = " Attempts to compute the Polar Decomposition of  a `matrix` (indirectly uses SVD)."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `eps`       − tolerance used to determine when a value converged to 0 when computing the SVD."] # [doc = " * `max_niter` − maximum total number of iterations performed by the SVD computation algorithm."] pub fn try_polar (self , eps : T :: RealField , max_niter : usize ,) -> Option < (OMatrix < T , R , R > , OMatrix < T , R , C >) > where R : DimMin < C > , DimMinimum < R , C > : DimSub < U1 > , DefaultAllocator : Allocator < R , C > + Allocator < DimMinimum < R , C > , R > + Allocator < DimMinimum < R , C > > + Allocator < R , R > + Allocator < DimMinimum < R , C > , DimMinimum < R , C > > + Allocator < C > + Allocator < R > + Allocator < DimDiff < DimMinimum < R , C > , U1 > > + Allocator < DimMinimum < R , C > , C > + Allocator < R , DimMinimum < R , C > > + Allocator < DimMinimum < R , C > > + Allocator < DimMinimum < R , C > > + Allocator < DimDiff < DimMinimum < R , C > , U1 > > , { SVD :: try_new_unordered (self . into_owned () , true , true , eps , max_niter) . and_then (| svd | svd . to_polar ()) } }
};
}
