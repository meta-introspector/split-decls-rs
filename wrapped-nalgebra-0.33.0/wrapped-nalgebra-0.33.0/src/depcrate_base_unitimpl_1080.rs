// Generated macro for impl_1080 (impl)
macro_rules! Depcrate_base_unitimpl_1080 {
() => {
// Module: crate::base::unit
// Provides: {"impl_1080"}
// Dependencies: {}
# [doc = " # Construction with normalization"] impl < T : Normed > Unit < T > { # [doc = " Normalize the given vector and return it wrapped on a `Unit` structure."] # [inline] pub fn new_normalize (value : T) -> Self { Self :: new_and_get (value) . 0 } # [doc = " Attempts to normalize the given vector and return it wrapped on a `Unit` structure."] # [doc = ""] # [doc = " Returns `None` if the norm was smaller or equal to `min_norm`."] # [inline] pub fn try_new (value : T , min_norm : T :: Norm) -> Option < Self > where T :: Norm : RealField , { Self :: try_new_and_get (value , min_norm) . map (| res | res . 0) } # [doc = " Normalize the given vector and return it wrapped on a `Unit` structure and its norm."] # [inline] pub fn new_and_get (mut value : T) -> (Self , T :: Norm) { let n = value . norm () ; value . unscale_mut (n . clone ()) ; (Unit { value } , n) } # [doc = " Normalize the given vector and return it wrapped on a `Unit` structure and its norm."] # [doc = ""] # [doc = " Returns `None` if the norm was smaller or equal to `min_norm`."] # [inline] pub fn try_new_and_get (mut value : T , min_norm : T :: Norm) -> Option < (Self , T :: Norm) > where T :: Norm : RealField , { let sq_norm = value . norm_squared () ; if sq_norm > min_norm . clone () * min_norm { let n = sq_norm . simd_sqrt () ; value . unscale_mut (n . clone ()) ; Some ((Unit { value } , n)) } else { None } } # [doc = " Normalizes this vector again. This is useful when repeated computations"] # [doc = " might cause a drift in the norm because of float inaccuracies."] # [doc = ""] # [doc = " Returns the norm before re-normalization. See `.renormalize_fast` for a faster alternative"] # [doc = " that may be slightly less accurate if `self` drifted significantly from having a unit length."] # [inline] pub fn renormalize (& mut self) -> T :: Norm { let n = self . norm () ; self . value . unscale_mut (n . clone ()) ; n } # [doc = " Normalizes this vector again using a first-order Taylor approximation."] # [doc = " This is useful when repeated computations might cause a drift in the norm"] # [doc = " because of float inaccuracies."] # [inline] pub fn renormalize_fast (& mut self) { let sq_norm = self . value . norm_squared () ; let three : T :: Norm = crate :: convert (3.0) ; let half : T :: Norm = crate :: convert (0.5) ; self . value . scale_mut (half * (three - sq_norm)) ; } }
};
}
