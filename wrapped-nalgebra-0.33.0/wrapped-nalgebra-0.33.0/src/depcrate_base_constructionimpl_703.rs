// Generated macro for impl_703 (impl)
macro_rules! Depcrate_base_constructionimpl_703 {
() => {
// Module: crate::base::construction
// Provides: {"impl_703"}
// Dependencies: {}
# [doc = " # Generic constructors"] # [doc = " This set of matrix and vector construction functions are all generic"] # [doc = " with-regard to the matrix dimensions. They all expect to be given"] # [doc = " the dimension as inputs."] # [doc = ""] # [doc = " These functions should only be used when working on dimension-generic code."] impl < T : Scalar , R : Dim , C : Dim > OMatrix < T , R , C > where DefaultAllocator : Allocator < R , C > , { # [doc = " Creates a matrix with all its elements set to `elem`."] # [inline] pub fn from_element_generic (nrows : R , ncols : C , elem : T) -> Self { let len = nrows . value () * ncols . value () ; Self :: from_iterator_generic (nrows , ncols , iter :: repeat (elem) . take (len)) } # [doc = " Creates a matrix with all its elements set to `elem`."] # [doc = ""] # [doc = " Same as `from_element_generic`."] # [inline] pub fn repeat_generic (nrows : R , ncols : C , elem : T) -> Self { let len = nrows . value () * ncols . value () ; Self :: from_iterator_generic (nrows , ncols , iter :: repeat (elem) . take (len)) } # [doc = " Creates a matrix with all its elements set to 0."] # [inline] pub fn zeros_generic (nrows : R , ncols : C) -> Self where T : Zero , { Self :: from_element_generic (nrows , ncols , T :: zero ()) } # [doc = " Creates a matrix with all its elements filled by an iterator."] # [inline] pub fn from_iterator_generic < I > (nrows : R , ncols : C , iter : I) -> Self where I : IntoIterator < Item = T > , { Self :: from_data (DefaultAllocator :: allocate_from_iterator (nrows , ncols , iter)) } # [doc = " Creates a matrix with all its elements filled by an row-major order iterator."] # [inline] pub fn from_row_iterator_generic < I > (nrows : R , ncols : C , iter : I) -> Self where I : IntoIterator < Item = T > , { Self :: from_data (DefaultAllocator :: allocate_from_row_iterator (nrows , ncols , iter ,)) } # [doc = " Creates a matrix with its elements filled with the components provided by a slice in"] # [doc = " row-major order."] # [doc = ""] # [doc = " The order of elements in the slice must follow the usual mathematic writing, i.e.,"] # [doc = " row-by-row."] # [inline] pub fn from_row_slice_generic (nrows : R , ncols : C , slice : & [T]) -> Self { assert ! (slice . len () == nrows . value () * ncols . value () , "Matrix init. error: the slice did not contain the right number of elements.") ; let mut res = Matrix :: uninit (nrows , ncols) ; let mut iter = slice . iter () ; unsafe { for i in 0 .. nrows . value () { for j in 0 .. ncols . value () { * res . get_unchecked_mut ((i , j)) = MaybeUninit :: new (iter . next () . unwrap () . clone ()) } } res . assume_init () } } # [doc = " Creates a matrix with its elements filled with the components provided by a slice. The"] # [doc = " components must have the same layout as the matrix data storage (i.e. column-major)."] # [inline] pub fn from_column_slice_generic (nrows : R , ncols : C , slice : & [T]) -> Self { Self :: from_iterator_generic (nrows , ncols , slice . iter () . cloned ()) } # [doc = " Creates a matrix filled with the results of a function applied to each of its component"] # [doc = " coordinates."] # [inline] pub fn from_fn_generic < F > (nrows : R , ncols : C , mut f : F) -> Self where F : FnMut (usize , usize) -> T , { let mut res = Matrix :: uninit (nrows , ncols) ; unsafe { for j in 0 .. ncols . value () { for i in 0 .. nrows . value () { * res . get_unchecked_mut ((i , j)) = MaybeUninit :: new (f (i , j)) ; } } res . assume_init () } } # [doc = " Creates a new identity matrix."] # [doc = ""] # [doc = " If the matrix is not square, the largest square submatrix starting at index `(0, 0)` is set"] # [doc = " to the identity matrix. All other entries are set to zero."] # [inline] pub fn identity_generic (nrows : R , ncols : C) -> Self where T : Zero + One , { Self :: from_diagonal_element_generic (nrows , ncols , T :: one ()) } # [doc = " Creates a new matrix with its diagonal filled with copies of `elt`."] # [doc = ""] # [doc = " If the matrix is not square, the largest square submatrix starting at index `(0, 0)` is set"] # [doc = " to the identity matrix. All other entries are set to zero."] # [inline] pub fn from_diagonal_element_generic (nrows : R , ncols : C , elt : T) -> Self where T : Zero + One , { let mut res = Self :: zeros_generic (nrows , ncols) ; for i in 0 .. crate :: min (nrows . value () , ncols . value ()) { unsafe { * res . get_unchecked_mut ((i , i)) = elt . clone () } } res } # [doc = " Creates a new matrix that may be rectangular. The first `elts.len()` diagonal elements are"] # [doc = " filled with the content of `elts`. Others are set to 0."] # [doc = ""] # [doc = " Panics if `elts.len()` is larger than the minimum among `nrows` and `ncols`."] # [inline] pub fn from_partial_diagonal_generic (nrows : R , ncols : C , elts : & [T]) -> Self where T : Zero , { let mut res = Self :: zeros_generic (nrows , ncols) ; assert ! (elts . len () <= crate :: min (nrows . value () , ncols . value ()) , "Too many diagonal elements provided.") ; for (i , elt) in elts . iter () . enumerate () { unsafe { * res . get_unchecked_mut ((i , i)) = elt . clone () } } res } # [doc = " Builds a new matrix from its rows."] # [doc = ""] # [doc = " Panics if not enough rows are provided (for statically-sized matrices), or if all rows do"] # [doc = " not have the same dimensions."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " # use nalgebra::{RowVector3, Matrix3};"] # [doc = " # use std::iter;"] # [doc = ""] # [doc = " let m = Matrix3::from_rows(&[ RowVector3::new(1.0, 2.0, 3.0),  RowVector3::new(4.0, 5.0, 6.0),  RowVector3::new(7.0, 8.0, 9.0) ]);"] # [doc = ""] # [doc = " assert!(m.m11 == 1.0 && m.m12 == 2.0 && m.m13 == 3.0 &&"] # [doc = "         m.m21 == 4.0 && m.m22 == 5.0 && m.m23 == 6.0 &&"] # [doc = "         m.m31 == 7.0 && m.m32 == 8.0 && m.m33 == 9.0);"] # [doc = " ```"] # [inline] pub fn from_rows < SB > (rows : & [Matrix < T , Const < 1 > , C , SB >]) -> Self where SB : RawStorage < T , Const < 1 > , C > , { assert ! (! rows . is_empty () , "At least one row must be given.") ; let nrows = R :: try_to_usize () . unwrap_or (rows . len ()) ; let ncols = rows [0] . len () ; assert ! (rows . len () == nrows , "Invalid number of rows provided to build this matrix.") ; if C :: try_to_usize () . is_none () { assert ! (rows . iter () . all (| r | r . len () == ncols) , "The provided rows must all have the same dimension.") ; } Self :: from_fn_generic (R :: from_usize (nrows) , C :: from_usize (ncols) , | i , j | { rows [i] [(0 , j)] . clone () }) } # [doc = " Builds a new matrix from its columns."] # [doc = ""] # [doc = " Panics if not enough columns are provided (for statically-sized matrices), or if all"] # [doc = " columns do not have the same dimensions."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " # use nalgebra::{Vector3, Matrix3};"] # [doc = " # use std::iter;"] # [doc = ""] # [doc = " let m = Matrix3::from_columns(&[ Vector3::new(1.0, 2.0, 3.0),  Vector3::new(4.0, 5.0, 6.0),  Vector3::new(7.0, 8.0, 9.0) ]);"] # [doc = ""] # [doc = " assert!(m.m11 == 1.0 && m.m12 == 4.0 && m.m13 == 7.0 &&"] # [doc = "         m.m21 == 2.0 && m.m22 == 5.0 && m.m23 == 8.0 &&"] # [doc = "         m.m31 == 3.0 && m.m32 == 6.0 && m.m33 == 9.0);"] # [doc = " ```"] # [inline] pub fn from_columns < SB > (columns : & [Vector < T , R , SB >]) -> Self where SB : RawStorage < T , R > , { assert ! (! columns . is_empty () , "At least one column must be given.") ; let ncols = C :: try_to_usize () . unwrap_or (columns . len ()) ; let nrows = columns [0] . len () ; assert ! (columns . len () == ncols , "Invalid number of columns provided to build this matrix.") ; if R :: try_to_usize () . is_none () { assert ! (columns . iter () . all (| r | r . len () == nrows) , "The columns provided must all have the same dimension.") ; } Self :: from_fn_generic (R :: from_usize (nrows) , C :: from_usize (ncols) , | i , j | { columns [j] [i] . clone () }) } # [doc = " Creates a matrix filled with random values."] # [inline] # [cfg (feature = "rand")] pub fn new_random_generic (nrows : R , ncols : C) -> Self where Standard : Distribution < T > , { let mut rng = rand :: thread_rng () ; Self :: from_fn_generic (nrows , ncols , | _ , _ | rng . gen ()) } # [doc = " Creates a matrix filled with random values from the given distribution."] # [inline] # [cfg (feature = "rand-no-std")] pub fn from_distribution_generic < Distr : Distribution < T > + ? Sized , G : Rng + ? Sized > (nrows : R , ncols : C , distribution : & Distr , rng : & mut G ,) -> Self { Self :: from_fn_generic (nrows , ncols , | _ , _ | distribution . sample (rng)) } # [doc = " Creates a matrix backed by a given `Vec`."] # [doc = ""] # [doc = " The output matrix is filled column-by-column."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " # use nalgebra::{Dyn, DMatrix, Matrix, Const};"] # [doc = ""] # [doc = " let vec = vec![0, 1, 2, 3, 4, 5];"] # [doc = " let vec_ptr = vec.as_ptr();"] # [doc = ""] # [doc = " let matrix = Matrix::from_vec_generic(Dyn(vec.len()), Const::<1>, vec);"] # [doc = " let matrix_storage_ptr = matrix.data.as_vec().as_ptr();"] # [doc = ""] # [doc = " // `matrix` is backed by exactly the same `Vec` as it was constructed from."] # [doc = " assert_eq!(matrix_storage_ptr, vec_ptr);"] # [doc = " ```"] # [inline] # [cfg (any (feature = "std" , feature = "alloc"))] pub fn from_vec_generic (nrows : R , ncols : C , data : Vec < T >) -> Self { Self :: from_iterator_generic (nrows , ncols , data) } }
};
}
