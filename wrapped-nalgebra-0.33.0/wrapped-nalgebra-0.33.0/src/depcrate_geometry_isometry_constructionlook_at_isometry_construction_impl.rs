// Generated macro for look_at_isometry_construction_impl (macro)
macro_rules! Depcrate_geometry_isometry_constructionlook_at_isometry_construction_impl {
() => {
// Module: crate::geometry::isometry_construction
// Provides: {"look_at_isometry_construction_impl"}
// Dependencies: {}
macro_rules ! look_at_isometry_construction_impl (($ RotId : ident < $ ($ RotParams : ident) ,*>) => { # [doc = " Creates an isometry that corresponds to the local frame of an observer standing at the"] # [doc = " point `eye` and looking toward `target`."] # [doc = ""] # [doc = " It maps the `z` axis to the view direction `target - eye`and the origin to the `eye`."] # [doc = ""] # [doc = " # Arguments"] # [doc = "   * eye - The observer position."] # [doc = "   * target - The target position."] # [doc = "   * up - Vertical direction. The only requirement of this parameter is to not be collinear"] # [doc = "   to `eye - at`. Non-collinearity is not checked."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # #[macro_use] extern crate approx;"] # [doc = " # use std::f32;"] # [doc = " # use nalgebra::{Isometry3, IsometryMatrix3, Point3, Vector3};"] # [doc = " let eye = Point3::new(1.0, 2.0, 3.0);"] # [doc = " let target = Point3::new(2.0, 2.0, 3.0);"] # [doc = " let up = Vector3::y();"] # [doc = ""] # [doc = " // Isometry with its rotation part represented as a UnitQuaternion"] # [doc = " let iso = Isometry3::face_towards(&eye, &target, &up);"] # [doc = " assert_eq!(iso * Point3::origin(), eye);"] # [doc = " assert_relative_eq!(iso * Vector3::z(), Vector3::x());"] # [doc = ""] # [doc = " // Isometry with its rotation part represented as Rotation3 (a 3x3 rotation matrix)."] # [doc = " let iso = IsometryMatrix3::face_towards(&eye, &target, &up);"] # [doc = " assert_eq!(iso * Point3::origin(), eye);"] # [doc = " assert_relative_eq!(iso * Vector3::z(), Vector3::x());"] # [doc = " ```"] # [inline] pub fn face_towards (eye : & Point3 < T >, target : & Point3 < T >, up : & Vector3 < T >) -> Self { Self :: from_parts (Translation :: from (eye . coords . clone ()) , $ RotId :: face_towards (& (target - eye) , up)) } # [doc = " Deprecated: Use [`Isometry::face_towards`] instead."] # [deprecated (note = "renamed to `face_towards`")] pub fn new_observer_frame (eye : & Point3 < T >, target : & Point3 < T >, up : & Vector3 < T >) -> Self { Self :: face_towards (eye , target , up) } # [doc = " Builds a right-handed look-at view matrix."] # [doc = ""] # [doc = " It maps the view direction `target - eye` to the **negative** `z` axis to and the `eye` to the origin."] # [doc = " This conforms to the common notion of right handed camera look-at **view matrix** from"] # [doc = " the computer graphics community, i.e. the camera is assumed to look toward its local `-z` axis."] # [doc = ""] # [doc = " # Arguments"] # [doc = "   * eye - The eye position."] # [doc = "   * target - The target position."] # [doc = "   * up - A vector approximately aligned with required the vertical axis. The only"] # [doc = "   requirement of this parameter is to not be collinear to `target - eye`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # #[macro_use] extern crate approx;"] # [doc = " # use std::f32;"] # [doc = " # use nalgebra::{Isometry3, IsometryMatrix3, Point3, Vector3};"] # [doc = " let eye = Point3::new(1.0, 2.0, 3.0);"] # [doc = " let target = Point3::new(2.0, 2.0, 3.0);"] # [doc = " let up = Vector3::y();"] # [doc = ""] # [doc = " // Isometry with its rotation part represented as a UnitQuaternion"] # [doc = " let iso = Isometry3::look_at_rh(&eye, &target, &up);"] # [doc = " assert_eq!(iso * eye, Point3::origin());"] # [doc = " assert_relative_eq!(iso * Vector3::x(), -Vector3::z());"] # [doc = ""] # [doc = " // Isometry with its rotation part represented as Rotation3 (a 3x3 rotation matrix)."] # [doc = " let iso = IsometryMatrix3::look_at_rh(&eye, &target, &up);"] # [doc = " assert_eq!(iso * eye, Point3::origin());"] # [doc = " assert_relative_eq!(iso * Vector3::x(), -Vector3::z());"] # [doc = " ```"] # [inline] pub fn look_at_rh (eye : & Point3 < T >, target : & Point3 < T >, up : & Vector3 < T >) -> Self { let rotation = $ RotId :: look_at_rh (& (target - eye) , up) ; let trans = & rotation * (- eye) ; Self :: from_parts (Translation :: from (trans . coords) , rotation) } # [doc = " Builds a left-handed look-at view matrix."] # [doc = ""] # [doc = " It maps the view direction `target - eye` to the **positive** `z` axis and the `eye` to the origin."] # [doc = " This conforms to the common notion of left handed camera look-at **view matrix** from"] # [doc = " the computer graphics community, i.e. the camera is assumed to look toward its local `z` axis."] # [doc = ""] # [doc = " # Arguments"] # [doc = "   * eye - The eye position."] # [doc = "   * target - The target position."] # [doc = "   * up - A vector approximately aligned with required the vertical axis. The only"] # [doc = "   requirement of this parameter is to not be collinear to `target - eye`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # #[macro_use] extern crate approx;"] # [doc = " # use std::f32;"] # [doc = " # use nalgebra::{Isometry3, IsometryMatrix3, Point3, Vector3};"] # [doc = " let eye = Point3::new(1.0, 2.0, 3.0);"] # [doc = " let target = Point3::new(2.0, 2.0, 3.0);"] # [doc = " let up = Vector3::y();"] # [doc = ""] # [doc = " // Isometry with its rotation part represented as a UnitQuaternion"] # [doc = " let iso = Isometry3::look_at_lh(&eye, &target, &up);"] # [doc = " assert_eq!(iso * eye, Point3::origin());"] # [doc = " assert_relative_eq!(iso * Vector3::x(), Vector3::z());"] # [doc = ""] # [doc = " // Isometry with its rotation part represented as Rotation3 (a 3x3 rotation matrix)."] # [doc = " let iso = IsometryMatrix3::look_at_lh(&eye, &target, &up);"] # [doc = " assert_eq!(iso * eye, Point3::origin());"] # [doc = " assert_relative_eq!(iso * Vector3::x(), Vector3::z());"] # [doc = " ```"] # [inline] pub fn look_at_lh (eye : & Point3 < T >, target : & Point3 < T >, up : & Vector3 < T >) -> Self { let rotation = $ RotId :: look_at_lh (& (target - eye) , up) ; let trans = & rotation * (- eye) ; Self :: from_parts (Translation :: from (trans . coords) , rotation) } }) ;
};
}
