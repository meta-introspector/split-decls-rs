// Generated macro for Writeable (trait)
macro_rules! DepcrateWriteable {
() => {
// Module: crate
// Provides: {"Writeable"}
// Dependencies: {}
# [doc = " `Writeable` is an alternative to `std::fmt::Display` with the addition of a length function."] pub trait Writeable { # [doc = " Writes a string to the given sink. Errors from the sink are bubbled up."] # [doc = " The default implementation delegates to `write_to_parts`, and discards any"] # [doc = " `Part` annotations."] fn write_to < W : fmt :: Write + ? Sized > (& self , sink : & mut W) -> fmt :: Result { self . write_to_parts (& mut parts_write_adapter :: CoreWriteAsPartsWrite (sink)) } # [doc = " Write bytes and `Part` annotations to the given sink. Errors from the"] # [doc = " sink are bubbled up. The default implementation delegates to `write_to`,"] # [doc = " and doesn't produce any `Part` annotations."] fn write_to_parts < S : PartsWrite + ? Sized > (& self , sink : & mut S) -> fmt :: Result { self . write_to (sink) } # [doc = " Returns a hint for the number of UTF-8 bytes that will be written to the sink."] # [doc = ""] # [doc = " Override this method if it can be computed quickly."] fn writeable_length_hint (& self) -> LengthHint { LengthHint :: undefined () } # [doc = " Returns a `&str` that matches the output of `write_to`, if possible."] # [doc = ""] # [doc = " This method is used to avoid materializing a [`String`] in `write_to_string`."] fn writeable_borrow (& self) -> Option < & str > { None } # [doc = " Creates a new string with the data from this `Writeable`."] # [doc = ""] # [doc = " Unlike [`to_string`](ToString::to_string), this does not pull in `core::fmt`"] # [doc = " code, and borrows the string if possible."] # [doc = ""] # [doc = " To remove the `Cow` wrapper, call `.into_owned()` or `.as_str()` as appropriate."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Inspect a [`Writeable`] before writing it to the sink:"] # [doc = ""] # [doc = " ```"] # [doc = " use core::fmt::{Result, Write};"] # [doc = " use writeable::Writeable;"] # [doc = ""] # [doc = " fn write_if_ascii<W, S>(w: &W, sink: &mut S) -> Result"] # [doc = " where"] # [doc = "     W: Writeable + ?Sized,"] # [doc = "     S: Write + ?Sized,"] # [doc = " {"] # [doc = "     let s = w.write_to_string();"] # [doc = "     if s.is_ascii() {"] # [doc = "         sink.write_str(&s)"] # [doc = "     } else {"] # [doc = "         Ok(())"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Convert the `Writeable` into a fully owned `String`:"] # [doc = ""] # [doc = " ```"] # [doc = " use writeable::Writeable;"] # [doc = ""] # [doc = " fn make_string(w: &impl Writeable) -> String {"] # [doc = "     w.write_to_string().into_owned()"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " # Note to implementors"] # [doc = ""] # [doc = " This method has a default implementation in terms of `writeable_borrow`,"] # [doc = " `writeable_length_hint`, and `write_to`. The only case"] # [doc = " where this should be implemented is if the computation of `writeable_borrow`"] # [doc = " requires a full invocation of `write_to`. In this case, implement this"] # [doc = " using [`to_string_or_borrow`]."] # [doc = ""] # [doc = " # `alloc` Cargo feature"] # [doc = ""] # [doc = " Calling or implementing this method requires the `alloc` Cargo feature."] # [doc = " However, as all the methods required by the default implementation do"] # [doc = " not require the `alloc` Cargo feature, a caller that uses the feature"] # [doc = " can still call this on types from crates that don't use the `alloc`"] # [doc = " Cargo feature."] # [cfg (feature = "alloc")] fn write_to_string (& self) -> Cow < '_ , str > { if let Some (borrow) = self . writeable_borrow () { return Cow :: Borrowed (borrow) ; } let hint = self . writeable_length_hint () ; if hint . is_zero () { return Cow :: Borrowed ("") ; } let mut output = String :: with_capacity (hint . capacity ()) ; let _ = self . write_to (& mut output) ; Cow :: Owned (output) } }
};
}
