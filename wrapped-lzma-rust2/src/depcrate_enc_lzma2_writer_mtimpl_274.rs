// Generated macro for impl_274 (impl)
macro_rules! Depcrate_enc_lzma2_writer_mtimpl_274 {
() => {
// Module: crate::enc::lzma2_writer_mt
// Provides: {"impl_274"}
// Dependencies: {}
impl < W : Write > Lzma2WriterMt < W > { # [doc = " Creates a new multi-threaded LZMA2 writer."] # [doc = ""] # [doc = " - `inner`: The writer to write compressed data to."] # [doc = " - `options`: The LZMA2 options used for compressing. Chunk size must be set when using the"] # [doc = "   multi-threaded encoder. If you need just one chunk, then use the single-threaded encoder."] # [doc = " - `num_workers`: The maximum number of worker threads for compression."] # [doc = "   Currently capped at 256 Threads."] pub fn new (inner : W , options : Lzma2Options , num_workers : u32) -> crate :: Result < Self > { let chunk_size = match options . chunk_size { None => return Err (error_invalid_input ("chunk size must be set")) , Some (chunk_size) => chunk_size . get () . max (options . lzma_options . dict_size as u64) , } ; let chunk_size = usize :: try_from (chunk_size) . map_err (| _ | error_invalid_input ("chunk size bigger than usize")) ? ; let num_work = u64 :: MAX ; Ok (Self { inner , options , chunk_size , current_work_unit : Vec :: with_capacity (chunk_size) , work_pool : WorkPool :: new (WorkPoolConfig :: new (num_workers , num_work) , worker_thread_logic ,) , }) } # [doc = " Sends the current work unit to the workers."] fn send_work_unit (& mut self) -> io :: Result < () > { if self . current_work_unit . is_empty () { return Ok (()) ; } self . drain_available_results () ? ; let work_data = core :: mem :: take (& mut self . current_work_unit) ; let mut single_chunk_options = self . options . clone () ; single_chunk_options . chunk_size = None ; single_chunk_options . lzma_options . preset_dict = None ; let mut work_data_opt = Some (work_data) ; self . work_pool . dispatch_next_work (& mut | _seq | { let data = work_data_opt . take () . ok_or_else (| | { io :: Error :: new (io :: ErrorKind :: InvalidInput , "work already provided") }) ? ; Ok (WorkUnit { data , options : single_chunk_options . clone () , }) }) ? ; self . drain_available_results () ? ; Ok (()) } # [doc = " Drains all currently available results from the work pool and writes them."] fn drain_available_results (& mut self) -> io :: Result < () > { while let Some (compressed_data) = self . work_pool . try_get_result () ? { self . inner . write_all (& compressed_data) ? ; } Ok (()) } # [doc = " Returns a wrapper around `self` that will finish the stream on drop."] pub fn auto_finish (self) -> AutoFinisher < Self > { AutoFinisher (Some (self)) } # [doc = " Consume the Lzma2WriterMt and return the inner writer."] pub fn into_inner (self) -> W { self . inner } # [doc = " Finishes the compression and returns the underlying writer."] pub fn finish (mut self) -> io :: Result < W > { if ! self . current_work_unit . is_empty () { self . send_work_unit () ? ; } if self . work_pool . next_index_to_dispatch () == 0 { self . inner . write_u8 (0x00) ? ; self . inner . flush () ? ; return Ok (self . inner) ; } self . work_pool . finish () ; while let Some (compressed_data) = self . work_pool . get_result (| _ | { Err (io :: Error :: new (io :: ErrorKind :: InvalidInput , "no more work to dispatch" ,)) }) ? { self . inner . write_all (& compressed_data) ? ; } self . inner . write_u8 (0x00) ? ; self . inner . flush () ? ; Ok (self . inner) } }
};
}
