macro_rules! deps {
    () => {
        LengthEncoder!();
        LzmaEncData!();
        LiteralEncoder!();
        LzmaEncoderModes!();
        MfType!();
        RangeEncoder!();
        LzmaEncoder!();
        Result!();
        FastEncoderMode!();
        Bt4!();
        EncodeMode!();
        Hc4!();
        NormalEncoderMode!();
        LzEncoder!();
        LzmaCoder!();
        LzmaEncoderTrait!();
        Write!();
        State!();
    };
}

macro_rules! impl_185 {
    () => {
        deps!();
        impl LzmaEncoder { # [allow (clippy :: too_many_arguments)] pub (crate) fn new (mode : EncodeMode , lc : u32 , lp : u32 , pb : u32 , mf : MfType , depth_limit : i32 , dict_size : u32 , nice_len : usize ,) -> (Self , LzmaEncoderModes) { let fast_mode = mode == EncodeMode :: Fast ; let mut mode : LzmaEncoderModes = if fast_mode { LzmaEncoderModes :: Fast (FastEncoderMode :: default ()) } else { LzmaEncoderModes :: Normal (NormalEncoderMode :: new ()) } ; let (extra_size_before , extra_size_after) = if fast_mode { (FastEncoderMode :: EXTRA_SIZE_BEFORE , FastEncoderMode :: EXTRA_SIZE_AFTER ,) } else { (NormalEncoderMode :: EXTRA_SIZE_BEFORE , NormalEncoderMode :: EXTRA_SIZE_AFTER ,) } ; let lz = match mf { MfType :: Hc4 => LzEncoder :: new_hc4 (dict_size , extra_size_before , extra_size_after , nice_len as _ , MATCH_LEN_MAX as _ , depth_limit ,) , MfType :: Bt4 => LzEncoder :: new_bt4 (dict_size , extra_size_before , extra_size_after , nice_len as _ , MATCH_LEN_MAX as _ , depth_limit ,) , } ; let literal_encoder = LiteralEncoder :: new (lc , lp) ; let match_len_encoder = LengthEncoder :: new (pb , nice_len) ; let rep_len_encoder = LengthEncoder :: new (pb , nice_len) ; let dist_slot_price_size = LzmaEncoder :: get_dist_slot (dict_size - 1) + 1 ; let mut e = Self { coder : LzmaCoder :: new (pb as usize) , lz , literal_encoder , match_len_encoder , rep_len_encoder , data : LzmaEncData { nice_len , dist_price_count : 0 , align_price_count : 0 , dist_slot_prices_size : dist_slot_price_size , dist_slot_prices : vec ! [vec ! [0 ; dist_slot_price_size as usize] ; DIST_STATES] , full_dist_prices : [[0 ; FULL_DISTANCES] ; DIST_STATES] , align_prices : Default :: default () , back : 0 , read_ahead : - 1 , uncompressed_size : 0 , } , } ; e . reset (& mut mode) ; (e , mode) } pub (crate) fn reset (& mut self , mode : & mut dyn LzmaEncoderTrait) { self . coder . reset () ; self . literal_encoder . reset () ; self . match_len_encoder . reset () ; self . rep_len_encoder . reset () ; self . data . dist_price_count = 0 ; self . data . align_price_count = 0 ; self . data . uncompressed_size += (self . data . read_ahead + 1) as u32 ; self . data . read_ahead = - 1 ; mode . reset () ; } # [inline (always)] pub (crate) fn reset_uncompressed_size (& mut self) { self . data . uncompressed_size = 0 ; } # [allow (unused)] pub (crate) fn encode_for_lzma1 < W : Write > (& mut self , rc : & mut RangeEncoder < W > , mode : & mut dyn LzmaEncoderTrait ,) -> crate :: Result < () > { if ! self . lz . is_started () && ! self . encode_init (rc) ? { return Ok (()) ; } while self . encode_symbol (rc , mode) ? { } Ok (()) } # [allow (unused)] pub (crate) fn encode_lzma1_end_marker < W : Write > (& mut self , rc : & mut RangeEncoder < W > ,) -> crate :: Result < () > { let pos_state = (self . lz . get_pos () - self . data . read_ahead) as u32 & self . coder . pos_mask ; rc . encode_bit (& mut self . coder . is_match [self . coder . state . get () as usize] , pos_state as usize , 1 ,) ? ; rc . encode_bit (& mut self . coder . is_rep , self . coder . state . get () as usize , 0) ? ; self . encode_match (u32 :: MAX , MATCH_LEN_MIN as u32 , pos_state , rc) ? ; Ok (()) } fn encode_init < W : Write > (& mut self , rc : & mut RangeEncoder < W >) -> crate :: Result < bool > { debug_assert_eq ! (self . data . read_ahead , - 1) ; if ! self . lz . has_enough_data (0) { return Ok (false) ; } self . skip (1) ; let state = self . coder . state . get () as usize ; rc . encode_bit (& mut self . coder . is_match [state] , 0 , 0) ? ; self . literal_encoder . encode_init (& self . lz , & self . data , & mut self . coder , rc) ? ; self . data . read_ahead -= 1 ; debug_assert_eq ! (self . data . read_ahead , - 1) ; self . data . uncompressed_size += 1 ; debug_assert_eq ! (self . data . uncompressed_size , 1) ; Ok (true) } fn encode_symbol < W : Write > (& mut self , rc : & mut RangeEncoder < W > , mode : & mut dyn LzmaEncoderTrait ,) -> crate :: Result < bool > { if ! self . lz . has_enough_data (self . data . read_ahead + 1) { return Ok (false) ; } let len = mode . get_next_symbol (self) ; debug_assert ! (self . data . read_ahead >= 0) ; let pos_state = (self . lz . get_pos () - self . data . read_ahead) as u32 & self . coder . pos_mask ; if self . data . back == - 1 { debug_assert_eq ! (len , 1) ; let state = self . coder . state . get () as usize ; rc . encode_bit (& mut self . coder . is_match [state] , pos_state as _ , 0) ? ; self . literal_encoder . encode (& self . lz , & self . data , & mut self . coder , rc) ? ; } else { let state = self . coder . state . get () as usize ; rc . encode_bit (& mut self . coder . is_match [state] , pos_state as usize , 1) ? ; if self . data . back < REPS as i32 { let state = self . coder . state . get () as usize ; rc . encode_bit (& mut self . coder . is_rep , state , 1) ? ; self . encode_rep_match (self . data . back as u32 , len , pos_state , rc) ? ; } else { let state = self . coder . state . get () as usize ; rc . encode_bit (& mut self . coder . is_rep , state , 0) ? ; self . encode_match ((self . data . back - REPS as i32) as u32 , len , pos_state , rc) ? ; } } self . data . read_ahead -= len as i32 ; self . data . uncompressed_size += len ; Ok (true) } fn encode_match < W : Write > (& mut self , dist : u32 , len : u32 , pos_state : u32 , rc : & mut RangeEncoder < W > ,) -> crate :: Result < () > { self . coder . state . update_match () ; self . match_len_encoder . encode (len , pos_state , rc) ? ; let dist_slot = LzmaEncoder :: get_dist_slot (dist) ; rc . encode_bit_tree (& mut self . coder . dist_slots [get_dist_state (len) as usize] , dist_slot ,) ? ; if dist_slot as usize >= DIST_MODEL_START { let footer_bits = (dist_slot >> 1) . wrapping_sub (1) ; let base = (2 | (dist_slot & 1)) << footer_bits ; let dist_reduced = dist - base ; if dist_slot < DIST_MODEL_END as u32 { rc . encode_reverse_bit_tree (self . coder . get_dist_special (dist_slot as usize - DIST_MODEL_START) , dist_reduced ,) ? ; } else { rc . encode_direct_bits (dist_reduced >> ALIGN_BITS , footer_bits - ALIGN_BITS as u32) ? ; rc . encode_reverse_bit_tree (& mut self . coder . dist_align , dist_reduced & ALIGN_MASK as u32 ,) ? ; self . data . align_price_count -= 1 ; } } self . coder . reps [3] = self . coder . reps [2] ; self . coder . reps [2] = self . coder . reps [1] ; self . coder . reps [1] = self . coder . reps [0] ; self . coder . reps [0] = dist as i32 ; self . data . dist_price_count -= 1 ; Ok (()) } fn encode_rep_match < W : Write > (& mut self , rep : u32 , len : u32 , pos_state : u32 , rc : & mut RangeEncoder < W > ,) -> crate :: Result < () > { if rep == 0 { let state = self . coder . state . get () as usize ; rc . encode_bit (& mut self . coder . is_rep0 , state , 0) ? ; let state = self . coder . state . get () as usize ; rc . encode_bit (& mut self . coder . is_rep0_long [state] , pos_state as usize , if len == 1 { 0 } else { 1 } ,) ? ; } else { let dist = self . coder . reps [rep as usize] ; let state = self . coder . state . get () as usize ; rc . encode_bit (& mut self . coder . is_rep0 , state , 1) ? ; if rep == 1 { let state = self . coder . state . get () as usize ; rc . encode_bit (& mut self . coder . is_rep1 , state , 0) ? ; } else { let state = self . coder . state . get () as usize ; rc . encode_bit (& mut self . coder . is_rep1 , state , 1) ? ; let state = self . coder . state . get () as usize ; rc . encode_bit (& mut self . coder . is_rep2 , state , rep - 2) ? ; if rep == 3 { self . coder . reps [3] = self . coder . reps [2] ; } self . coder . reps [2] = self . coder . reps [1] ; } self . coder . reps [1] = self . coder . reps [0] ; self . coder . reps [0] = dist ; } if len == 1 { self . coder . state . update_short_rep () ; } else { self . rep_len_encoder . encode (len , pos_state , rc) ? ; self . coder . state . update_long_rep () ; } Ok (()) } pub (crate) fn find_matches (& mut self) { self . data . read_ahead += 1 ; self . lz . find_matches () ; debug_assert ! (self . lz . verify_matches ()) ; } pub (crate) fn skip (& mut self , len : usize) { self . data . read_ahead += len as i32 ; self . lz . skip (len) } pub (crate) fn get_any_match_price (& self , state : & State , pos_state : u32) -> u32 { RangeEncoder :: get_bit_price (self . coder . is_match [state . get () as usize] [pos_state as usize] as _ , 1 ,) } pub (crate) fn get_normal_match_price (& self , any_match_price : u32 , state : & State) -> u32 { any_match_price + RangeEncoder :: get_bit_price (self . coder . is_rep [state . get () as usize] as _ , 0) } pub (crate) fn get_any_rep_price (& self , any_match_price : u32 , state : & State) -> u32 { any_match_price + RangeEncoder :: get_bit_price (self . coder . is_rep [state . get () as usize] as _ , 1) } pub (crate) fn get_short_rep_price (& self , any_rep_price : u32 , state : & State , pos_state : u32 ,) -> u32 { any_rep_price + RangeEncoder :: get_bit_price (self . coder . is_rep0 [state . get () as usize] as _ , 0) + RangeEncoder :: get_bit_price (self . coder . is_rep0_long [state . get () as usize] [pos_state as usize] as _ , 0 ,) } pub (crate) fn get_long_rep_price (& self , any_rep_price : u32 , rep : u32 , state : & State , pos_state : u32 ,) -> u32 { let mut price = any_rep_price ; if rep == 0 { price += RangeEncoder :: get_bit_price (self . coder . is_rep0 [state . get () as usize] as _ , 0) + RangeEncoder :: get_bit_price (self . coder . is_rep0_long [state . get () as usize] [pos_state as usize] as _ , 1 ,) ; } else { price += RangeEncoder :: get_bit_price (self . coder . is_rep0 [state . get () as usize] as _ , 1) ; if rep == 1 { price += RangeEncoder :: get_bit_price (self . coder . is_rep1 [state . get () as usize] as _ , 0) } else { price += RangeEncoder :: get_bit_price (self . coder . is_rep1 [state . get () as usize] as _ , 1) + RangeEncoder :: get_bit_price (self . coder . is_rep2 [state . get () as usize] as _ , rep as i32 - 2 ,) ; } } price } pub (crate) fn get_long_rep_and_len_price (& self , rep : u32 , len : u32 , state : & State , pos_state : u32 ,) -> u32 { let any_match_price = self . get_any_match_price (state , pos_state) ; let any_rep_price = self . get_any_rep_price (any_match_price , state) ; let long_rep_price = self . get_long_rep_price (any_rep_price , rep , state , pos_state) ; long_rep_price + self . rep_len_encoder . get_price (len as _ , pos_state as _) } pub (crate) fn get_match_and_len_price (& self , normal_match_price : u32 , dist : u32 , len : u32 , pos_state : u32 ,) -> u32 { let mut price = normal_match_price + self . match_len_encoder . get_price (len as _ , pos_state as _) ; let dist_state = get_dist_state (len) ; if dist < FULL_DISTANCES as u32 { price += self . data . full_dist_prices [dist_state as usize] [dist as usize] ; } else { let dist_slot = LzmaEncoder :: get_dist_slot (dist) ; price += self . data . dist_slot_prices [dist_state as usize] [dist_slot as usize] + self . data . align_prices [(dist & ALIGN_MASK as u32) as usize] ; } price } pub (crate) fn update_dist_prices (& mut self) { self . data . dist_price_count = DIST_PRICE_UPDATE_INTERVAL as _ ; for dist_state in 0 .. DIST_STATES { for dist_slot in 0 .. self . data . dist_slot_prices_size as usize { self . data . dist_slot_prices [dist_state] [dist_slot] = RangeEncoder :: get_bit_tree_price (& mut self . coder . dist_slots [dist_state] , dist_slot as u32 ,) ; } for dist_slot in DIST_MODEL_END as u32 .. self . data . dist_slot_prices_size { let count = (dist_slot >> 1) - 1 - ALIGN_BITS as u32 ; self . data . dist_slot_prices [dist_state] [dist_slot as usize] += RangeEncoder :: get_direct_bits_price (count) ; } for dist in 0 .. DIST_MODEL_START { self . data . full_dist_prices [dist_state] [dist] = self . data . dist_slot_prices [dist_state] [dist] ; } } let mut dist = DIST_MODEL_START ; for dist_slot in DIST_MODEL_START .. DIST_MODEL_END { let footer_bits = (dist_slot >> 1) - 1 ; let base = (2 | (dist_slot & 1)) << footer_bits ; let limit = self . coder . get_dist_special (dist_slot - DIST_MODEL_START) . len () ; for _i in 0 .. limit { let dist_reduced = dist - base ; let price = RangeEncoder :: get_reverse_bit_tree_price (self . coder . get_dist_special (dist_slot - DIST_MODEL_START) , dist_reduced as u32 ,) ; for dist_state in 0 .. DIST_STATES { self . data . full_dist_prices [dist_state] [dist] = self . data . dist_slot_prices [dist_state] [dist_slot] + price ; } dist += 1 ; } } debug_assert_eq ! (dist , FULL_DISTANCES) ; } fn update_align_prices (& mut self) { self . data . align_price_count = ALIGN_PRICE_UPDATE_INTERVAL as i32 ; for i in 0 .. ALIGN_SIZE { self . data . align_prices [i] = RangeEncoder :: get_reverse_bit_tree_price (& mut self . coder . dist_align , i as u32) ; } } pub (crate) fn update_prices (& mut self) { if self . data . dist_price_count <= 0 { self . update_dist_prices () ; } if self . data . align_price_count <= 0 { self . update_align_prices () ; } self . match_len_encoder . update_prices () ; self . rep_len_encoder . update_prices () ; } }
    };
}

impl_185!();