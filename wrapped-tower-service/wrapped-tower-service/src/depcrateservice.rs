// Generated macro for Service (trait)
macro_rules! DepcrateService {
() => {
// Module: crate
// Provides: {"Service"}
// Dependencies: {}
# [doc = " An asynchronous function from a `Request` to a `Response`."] # [doc = ""] # [doc = " The `Service` trait is a simplified interface making it easy to write"] # [doc = " network applications in a modular and reusable way, decoupled from the"] # [doc = " underlying protocol. It is one of Tower's fundamental abstractions."] # [doc = ""] # [doc = " # Functional"] # [doc = ""] # [doc = " A `Service` is a function of a `Request`. It immediately returns a"] # [doc = " `Future` representing the eventual completion of processing the"] # [doc = " request. The actual request processing may happen at any time in the"] # [doc = " future, on any thread or executor. The processing may depend on calling"] # [doc = " other services. At some point in the future, the processing will complete,"] # [doc = " and the `Future` will resolve to a response or error."] # [doc = ""] # [doc = " At a high level, the `Service::call` function represents an RPC request. The"] # [doc = " `Service` value can be a server or a client."] # [doc = ""] # [doc = " # Server"] # [doc = ""] # [doc = " An RPC server *implements* the `Service` trait. Requests received by the"] # [doc = " server over the network are deserialized and then passed as an argument to the"] # [doc = " server value. The returned response is sent back over the network."] # [doc = ""] # [doc = " As an example, here is how an HTTP request is processed by a server:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use std::pin::Pin;"] # [doc = " # use std::task::{Poll, Context};"] # [doc = " # use std::future::Future;"] # [doc = " # use tower_service::Service;"] # [doc = " use http::{Request, Response, StatusCode};"] # [doc = ""] # [doc = " struct HelloWorld;"] # [doc = ""] # [doc = " impl Service<Request<Vec<u8>>> for HelloWorld {"] # [doc = "     type Response = Response<Vec<u8>>;"] # [doc = "     type Error = http::Error;"] # [doc = "     type Future = Pin<Box<dyn Future<Output = Result<Self::Response, Self::Error>>>>;"] # [doc = ""] # [doc = "     fn poll_ready(&mut self, cx: &mut Context<'_>) -> Poll<Result<(), Self::Error>> {"] # [doc = "         Poll::Ready(Ok(()))"] # [doc = "     }"] # [doc = ""] # [doc = "     fn call(&mut self, req: Request<Vec<u8>>) -> Self::Future {"] # [doc = "         // create the body"] # [doc = "         let body: Vec<u8> = \"hello, world!\\n\""] # [doc = "             .as_bytes()"] # [doc = "             .to_owned();"] # [doc = "         // Create the HTTP response"] # [doc = "         let resp = Response::builder()"] # [doc = "             .status(StatusCode::OK)"] # [doc = "             .body(body)"] # [doc = "             .expect(\"Unable to create `http::Response`\");"] # [doc = ""] # [doc = "         // create a response in a future."] # [doc = "         let fut = async {"] # [doc = "             Ok(resp)"] # [doc = "         };"] # [doc = ""] # [doc = "         // Return the response as an immediate future"] # [doc = "         Box::pin(fut)"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " # Client"] # [doc = ""] # [doc = " A client consumes a service by using a `Service` value. The client may"] # [doc = " issue requests by invoking `call` and passing the request as an argument."] # [doc = " It then receives the response by waiting for the returned future."] # [doc = ""] # [doc = " As an example, here is how a Redis request would be issued:"] # [doc = ""] # [doc = " ```rust,ignore"] # [doc = " let mut client = redis::Client::new()"] # [doc = "     .connect(\"127.0.0.1:6379\".parse().unwrap())"] # [doc = "     .unwrap();"] # [doc = ""] # [doc = " ServiceExt::<Cmd>::ready(&mut client).await?;"] # [doc = ""] # [doc = " let resp = client.call(Cmd::set(\"foo\", \"this is the value of foo\")).await?;"] # [doc = ""] # [doc = " println!(\"Redis response: {:?}\", resp);"] # [doc = " ```"] # [doc = ""] # [doc = " # Middleware / Layer"] # [doc = ""] # [doc = " More often than not, all the pieces needed for writing robust, scalable"] # [doc = " network applications are the same no matter the underlying protocol. By"] # [doc = " unifying the API for both clients and servers in a protocol agnostic way,"] # [doc = " it is possible to write middleware that provide these pieces in a"] # [doc = " reusable way."] # [doc = ""] # [doc = " Take timeouts as an example:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use tower_service::Service;"] # [doc = " use tower_layer::Layer;"] # [doc = " use futures::FutureExt;"] # [doc = " use std::future::Future;"] # [doc = " use std::task::{Context, Poll};"] # [doc = " use std::time::Duration;"] # [doc = " use std::pin::Pin;"] # [doc = " use std::fmt;"] # [doc = " use std::error::Error;"] # [doc = ""] # [doc = " // Our timeout service, which wraps another service and"] # [doc = " // adds a timeout to its response future."] # [doc = " pub struct Timeout<T> {"] # [doc = "     inner: T,"] # [doc = "     timeout: Duration,"] # [doc = " }"] # [doc = ""] # [doc = " impl<T> Timeout<T> {"] # [doc = "     pub const fn new(inner: T, timeout: Duration) -> Timeout<T> {"] # [doc = "         Timeout {"] # [doc = "             inner,"] # [doc = "             timeout"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // The error returned if processing a request timed out"] # [doc = " #[derive(Debug)]"] # [doc = " pub struct Expired;"] # [doc = ""] # [doc = " impl fmt::Display for Expired {"] # [doc = "     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {"] # [doc = "         write!(f, \"expired\")"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " impl Error for Expired {}"] # [doc = ""] # [doc = " // We can implement `Service` for `Timeout<T>` if `T` is a `Service`"] # [doc = " impl<T, Request> Service<Request> for Timeout<T>"] # [doc = " where"] # [doc = "     T: Service<Request>,"] # [doc = "     T::Future: 'static,"] # [doc = "     T::Error: Into<Box<dyn Error + Send + Sync>> + 'static,"] # [doc = "     T::Response: 'static,"] # [doc = " {"] # [doc = "     // `Timeout` doesn't modify the response type, so we use `T`'s response type"] # [doc = "     type Response = T::Response;"] # [doc = "     // Errors may be either `Expired` if the timeout expired, or the inner service's"] # [doc = "     // `Error` type. Therefore, we return a boxed `dyn Error + Send + Sync` trait object to erase"] # [doc = "     // the error's type."] # [doc = "     type Error = Box<dyn Error + Send + Sync>;"] # [doc = "     type Future = Pin<Box<dyn Future<Output = Result<Self::Response, Self::Error>>>>;"] # [doc = ""] # [doc = "     fn poll_ready(&mut self, cx: &mut Context<'_>) -> Poll<Result<(), Self::Error>> {"] # [doc = "         // Our timeout service is ready if the inner service is ready."] # [doc = "         // This is how backpressure can be propagated through a tree of nested services."] # [doc = "        self.inner.poll_ready(cx).map_err(Into::into)"] # [doc = "     }"] # [doc = ""] # [doc = "     fn call(&mut self, req: Request) -> Self::Future {"] # [doc = "         // Create a future that completes after `self.timeout`"] # [doc = "         let timeout = tokio::time::sleep(self.timeout);"] # [doc = ""] # [doc = "         // Call the inner service and get a future that resolves to the response"] # [doc = "         let fut = self.inner.call(req);"] # [doc = ""] # [doc = "         // Wrap those two futures in another future that completes when either one completes"] # [doc = "         //"] # [doc = "         // If the inner service is too slow the `sleep` future will complete first"] # [doc = "         // And an error will be returned and `fut` will be dropped and not polled again"] # [doc = "         //"] # [doc = "         // We have to box the errors so the types match"] # [doc = "         let f = async move {"] # [doc = "             tokio::select! {"] # [doc = "                 res = fut => {"] # [doc = "                     res.map_err(|err| err.into())"] # [doc = "                 },"] # [doc = "                 _ = timeout => {"] # [doc = "                     Err(Box::new(Expired) as Box<dyn Error + Send + Sync>)"] # [doc = "                 },"] # [doc = "             }"] # [doc = "         };"] # [doc = ""] # [doc = "         Box::pin(f)"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // A layer for wrapping services in `Timeout`"] # [doc = " pub struct TimeoutLayer(Duration);"] # [doc = ""] # [doc = " impl TimeoutLayer {"] # [doc = "     pub const fn new(delay: Duration) -> Self {"] # [doc = "         TimeoutLayer(delay)"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " impl<S> Layer<S> for TimeoutLayer {"] # [doc = "     type Service = Timeout<S>;"] # [doc = ""] # [doc = "     fn layer(&self, service: S) -> Timeout<S> {"] # [doc = "         Timeout::new(service, self.0)"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " The above timeout implementation is decoupled from the underlying protocol"] # [doc = " and is also decoupled from client or server concerns. In other words, the"] # [doc = " same timeout middleware could be used in either a client or a server."] # [doc = ""] # [doc = " # Backpressure"] # [doc = ""] # [doc = " Calling a `Service` which is at capacity (i.e., it is temporarily unable to process a"] # [doc = " request) should result in an error. The caller is responsible for ensuring"] # [doc = " that the service is ready to receive the request before calling it."] # [doc = ""] # [doc = " `Service` provides a mechanism by which the caller is able to coordinate"] # [doc = " readiness. `Service::poll_ready` returns `Ready` if the service expects that"] # [doc = " it is able to process a request."] # [doc = ""] # [doc = " # Be careful when cloning inner services"] # [doc = ""] # [doc = " Services are permitted to panic if `call` is invoked without obtaining `Poll::Ready(Ok(()))`"] # [doc = " from `poll_ready`. You should therefore be careful when cloning services for example to move"] # [doc = " them into boxed futures. Even though the original service is ready, the clone might not be."] # [doc = ""] # [doc = " Therefore this kind of code is wrong and might panic:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use std::pin::Pin;"] # [doc = " # use std::task::{Poll, Context};"] # [doc = " # use std::future::Future;"] # [doc = " # use tower_service::Service;"] # [doc = " #"] # [doc = " struct Wrapper<S> {"] # [doc = "     inner: S,"] # [doc = " }"] # [doc = ""] # [doc = " impl<R, S> Service<R> for Wrapper<S>"] # [doc = " where"] # [doc = "     S: Service<R> + Clone + 'static,"] # [doc = "     R: 'static,"] # [doc = " {"] # [doc = "     type Response = S::Response;"] # [doc = "     type Error = S::Error;"] # [doc = "     type Future = Pin<Box<dyn Future<Output = Result<Self::Response, Self::Error>>>>;"] # [doc = ""] # [doc = "     fn poll_ready(&mut self, cx: &mut Context<'_>) -> Poll<Result<(), Self::Error>> {"] # [doc = "         self.inner.poll_ready(cx)"] # [doc = "     }"] # [doc = ""] # [doc = "     fn call(&mut self, req: R) -> Self::Future {"] # [doc = "         let mut inner = self.inner.clone();"] # [doc = "         Box::pin(async move {"] # [doc = "             // `inner` might not be ready since its a clone"] # [doc = "             inner.call(req).await"] # [doc = "         })"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " You should instead use [`core::mem::replace`] to take the service that was ready:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use std::pin::Pin;"] # [doc = " # use std::task::{Poll, Context};"] # [doc = " # use std::future::Future;"] # [doc = " # use tower_service::Service;"] # [doc = " #"] # [doc = " struct Wrapper<S> {"] # [doc = "     inner: S,"] # [doc = " }"] # [doc = ""] # [doc = " impl<R, S> Service<R> for Wrapper<S>"] # [doc = " where"] # [doc = "     S: Service<R> + Clone + 'static,"] # [doc = "     R: 'static,"] # [doc = " {"] # [doc = "     type Response = S::Response;"] # [doc = "     type Error = S::Error;"] # [doc = "     type Future = Pin<Box<dyn Future<Output = Result<Self::Response, Self::Error>>>>;"] # [doc = ""] # [doc = "     fn poll_ready(&mut self, cx: &mut Context<'_>) -> Poll<Result<(), Self::Error>> {"] # [doc = "         self.inner.poll_ready(cx)"] # [doc = "     }"] # [doc = ""] # [doc = "     fn call(&mut self, req: R) -> Self::Future {"] # [doc = "         let clone = self.inner.clone();"] # [doc = "         // take the service that was ready"] # [doc = "         let mut inner = std::mem::replace(&mut self.inner, clone);"] # [doc = "         Box::pin(async move {"] # [doc = "             inner.call(req).await"] # [doc = "         })"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub trait Service < Request > { # [doc = " Responses given by the service."] type Response ; # [doc = " Errors produced by the service."] type Error ; # [doc = " The future response value."] type Future : Future < Output = Result < Self :: Response , Self :: Error > > ; # [doc = " Returns `Poll::Ready(Ok(()))` when the service is able to process requests."] # [doc = ""] # [doc = " If the service is at capacity, then `Poll::Pending` is returned and the task"] # [doc = " is notified when the service becomes ready again. This function is"] # [doc = " expected to be called while on a task. Generally, this can be done with"] # [doc = " a simple `futures::future::poll_fn` call."] # [doc = ""] # [doc = " If `Poll::Ready(Err(_))` is returned, the service is no longer able to service requests"] # [doc = " and the caller should discard the service instance."] # [doc = ""] # [doc = " Once `poll_ready` returns `Poll::Ready(Ok(()))`, a request may be dispatched to the"] # [doc = " service using `call`. Until a request is dispatched, repeated calls to"] # [doc = " `poll_ready` must return either `Poll::Ready(Ok(()))` or `Poll::Ready(Err(_))`."] # [doc = ""] # [doc = " Note that `poll_ready` may reserve shared resources that are consumed in a subsequent"] # [doc = " invocation of `call`. Thus, it is critical for implementations to not assume that `call`"] # [doc = " will always be invoked and to ensure that such resources are released if the service is"] # [doc = " dropped before `call` is invoked or the future returned by `call` is dropped before it"] # [doc = " is polled."] fn poll_ready (& mut self , cx : & mut Context < '_ >) -> Poll < Result < () , Self :: Error > > ; # [doc = " Process the request and return the response asynchronously."] # [doc = ""] # [doc = " This function is expected to be callable off task. As such,"] # [doc = " implementations should take care to not call `poll_ready`."] # [doc = ""] # [doc = " Before dispatching a request, `poll_ready` must be called and return"] # [doc = " `Poll::Ready(Ok(()))`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Implementations are permitted to panic if `call` is invoked without"] # [doc = " obtaining `Poll::Ready(Ok(()))` from `poll_ready`."] # [must_use = "futures do nothing unless you `.await` or poll them"] fn call (& mut self , req : Request) -> Self :: Future ; }
};
}
