// Generated macro for blake2_mac_impl (macro)
macro_rules! Depcrate_macrosblake2_mac_impl {
() => {
// Module: crate::macros
// Provides: {"blake2_mac_impl"}
// Dependencies: {}
macro_rules ! blake2_mac_impl { ($ name : ident , $ hash : ty , $ max_size : ty , $ doc : expr) => { # [derive (Clone)] # [doc =$ doc] pub struct $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True >, { core : $ hash , buffer : LazyBuffer <<$ hash as BlockSizeUser >:: BlockSize >, # [cfg (feature = "reset")] key_block : Option < Key < Self >>, _out : PhantomData < OutSize >, } impl < OutSize > $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True >, { # [doc = " Create new instance using provided key, salt, and persona."] # [doc = ""] # [doc = " Setting key to `None` indicates unkeyed usage."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If key is `Some`, then its length should not be zero or bigger"] # [doc = " than the block size. The salt and persona length should not be"] # [doc = " bigger than quarter of block size. If any of those conditions is"] # [doc = " false the method will return an error."] # [inline] pub fn new_with_salt_and_personal (key : Option <& [u8] >, salt : & [u8] , persona : & [u8] ,) -> Result < Self , InvalidLength > { let kl = key . map_or (0 , | k | k . len ()) ; let bs = <$ hash as BlockSizeUser >:: BlockSize :: USIZE ; let qbs = bs / 4 ; if key . is_some () && kl == 0 || kl > bs || salt . len () > qbs || persona . len () > qbs { return Err (InvalidLength) ; } let buffer = if let Some (k) = key { let mut padded_key = Block ::<$ hash >:: default () ; padded_key [.. kl] . copy_from_slice (k) ; LazyBuffer :: new (& padded_key) } else { LazyBuffer :: default () } ; Ok (Self { core : <$ hash >:: new_with_params (salt , persona , kl , OutSize :: USIZE) , buffer , # [cfg (feature = "reset")] key_block : key . map (| k | { let mut t = Key ::< Self >:: default () ; t [.. kl] . copy_from_slice (k) ; t }) , _out : PhantomData , }) } } impl < OutSize > KeySizeUser for $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True >, { type KeySize = $ max_size ; } impl < OutSize > KeyInit for $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True >, { # [inline] fn new (key : & Key < Self >) -> Self { Self :: new_from_slice (key) . expect ("Key has correct length") } # [inline] fn new_from_slice (key : & [u8]) -> Result < Self , InvalidLength > { let kl = key . len () ; if kl > < Self as KeySizeUser >:: KeySize :: USIZE { return Err (InvalidLength) ; } let mut padded_key = Block ::<$ hash >:: default () ; padded_key [.. kl] . copy_from_slice (key) ; Ok (Self { core : <$ hash >:: new_with_params (& [] , & [] , key . len () , OutSize :: USIZE) , buffer : LazyBuffer :: new (& padded_key) , # [cfg (feature = "reset")] key_block : { let mut t = Key ::< Self >:: default () ; t [.. kl] . copy_from_slice (key) ; Some (t) } , _out : PhantomData , }) } } impl < OutSize > Update for $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True >, { # [inline] fn update (& mut self , input : & [u8]) { let Self { core , buffer , .. } = self ; buffer . digest_blocks (input , | blocks | core . update_blocks (blocks)) ; } } impl < OutSize > OutputSizeUser for $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True >, { type OutputSize = OutSize ; } impl < OutSize > FixedOutput for $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True >, { # [inline] fn finalize_into (mut self , out : & mut Output < Self >) { let Self { core , buffer , .. } = & mut self ; let mut full_res = Default :: default () ; core . finalize_variable_core (buffer , & mut full_res) ; out . copy_from_slice (& full_res [.. OutSize :: USIZE]) ; } } # [cfg (feature = "reset")] impl < OutSize > Reset for $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True >, { fn reset (& mut self) { self . core . reset () ; self . buffer = if let Some (k) = self . key_block { let kl = k . len () ; let mut padded_key = Block ::<$ hash >:: default () ; padded_key [.. kl] . copy_from_slice (& k) ; LazyBuffer :: new (& padded_key) } else { LazyBuffer :: default () } } } # [cfg (feature = "reset")] impl < OutSize > FixedOutputReset for $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True >, { # [inline] fn finalize_into_reset (& mut self , out : & mut Output < Self >) { let Self { core , buffer , .. } = self ; let mut full_res = Default :: default () ; core . finalize_variable_core (buffer , & mut full_res) ; out . copy_from_slice (& full_res [.. OutSize :: USIZE]) ; self . reset () ; } } impl < OutSize > MacMarker for $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True > { } impl < OutSize > fmt :: Debug for $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True >, { fn fmt (& self , f : & mut fmt :: Formatter <'_ >) -> fmt :: Result { write ! (f , "{}{} {{ ... }}" , stringify ! ($ name) , OutSize :: USIZE) } } impl < OutSize > Drop for $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True >, { fn drop (& mut self) { # [cfg (feature = "zeroize")] { self . buffer . zeroize () ; # [cfg (feature = "reset")] if let Some (mut key_block) = self . key_block { key_block . zeroize () ; } } } } # [cfg (feature = "zeroize")] impl < OutSize > ZeroizeOnDrop for $ name < OutSize > where OutSize : ArraySize + IsLessOrEqual <$ max_size , Output = True > { } } ; }
};
}
