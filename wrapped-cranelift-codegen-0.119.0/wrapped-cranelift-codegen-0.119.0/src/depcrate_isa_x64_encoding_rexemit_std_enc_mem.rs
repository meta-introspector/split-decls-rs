// Generated macro for emit_std_enc_mem (function)
macro_rules! Depcrate_isa_x64_encoding_rexemit_std_enc_mem {
() => {
// Module: crate::isa::x64::encoding::rex
// Provides: {"emit_std_enc_mem"}
// Dependencies: {}
# [doc = " This is the core 'emit' function for instructions that reference memory."] # [doc = ""] # [doc = " For an instruction that has as operands a reg encoding `enc_g` and a memory address `mem_e`,"] # [doc = " create and emit:"] # [doc = " - first the legacy prefixes, if any"] # [doc = " - then the REX prefix, if needed"] # [doc = " - then caller-supplied opcode byte(s) (`opcodes` and `num_opcodes`),"] # [doc = " - then the MOD/RM byte,"] # [doc = " - then optionally, a SIB byte,"] # [doc = " - and finally optionally an immediate that will be derived from the `mem_e` operand."] # [doc = ""] # [doc = " For most instructions up to and including SSE4.2, that will be the whole instruction: this is"] # [doc = " what we call \"standard\" instructions, and abbreviate \"std\" in the name here. VEX-prefixed"] # [doc = " instructions will require their own emitter functions."] # [doc = ""] # [doc = " This will also work for 32-bits x86 instructions, assuming no REX prefix is provided."] # [doc = ""] # [doc = " The opcodes are written bigendianly for the convenience of callers.  For example, if the opcode"] # [doc = " bytes to be emitted are, in this order, F3 0F 27, then the caller should pass `opcodes` =="] # [doc = " 0xF3_0F_27 and `num_opcodes` == 3."] # [doc = ""] # [doc = " The register operand is represented here not as a `Reg` but as its hardware encoding, `enc_g`."] # [doc = " `rex` can specify special handling for the REX prefix.  By default, the REX prefix will"] # [doc = " indicate a 64-bit operation and will be deleted if it is redundant (0x40).  Note that for a"] # [doc = " 64-bit operation, the REX prefix will normally never be redundant, since REX.W must be 1 to"] # [doc = " indicate a 64-bit operation."] pub (crate) fn emit_std_enc_mem (sink : & mut MachBuffer < Inst > , prefixes : LegacyPrefixes , opcodes : u32 , mut num_opcodes : usize , enc_g : u8 , mem_e : & Amode , rex : RexFlags , bytes_at_end : u8 ,) { if let Some (trap_code) = mem_e . get_flags () . trap_code () { sink . add_trap (trap_code) ; } prefixes . emit (sink) ; match * mem_e { Amode :: ImmReg { base , .. } => { let enc_e = int_reg_enc (base) ; rex . emit_two_op (sink , enc_g , enc_e) ; } Amode :: ImmRegRegShift { base : reg_base , index : reg_index , .. } => { let enc_base = int_reg_enc (* reg_base) ; let enc_index = int_reg_enc (* reg_index) ; rex . emit_three_op (sink , enc_g , enc_index , enc_base) ; } Amode :: RipRelative { .. } => { rex . emit_two_op (sink , enc_g , 0) ; } } while num_opcodes > 0 { num_opcodes -= 1 ; sink . put1 (((opcodes >> (num_opcodes << 3)) & 0xFF) as u8) ; } emit_modrm_sib_disp (sink , enc_g , mem_e , bytes_at_end , None) }
};
}
