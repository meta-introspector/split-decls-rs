// Generated macro for VecInstOverlapInfo (trait)
macro_rules! Depcrate_isa_riscv64_inst_vectorVecInstOverlapInfo {
() => {
// Module: crate::isa::riscv64::inst::vector
// Provides: {"VecInstOverlapInfo"}
// Dependencies: {}
pub trait VecInstOverlapInfo { # [doc = " § 5.2 Vector Operands states:"] # [doc = ""] # [doc = " A destination vector register group can overlap a source vector register group"] # [doc = " only if one of the following holds:"] # [doc = ""] # [doc = "  * The destination EEW equals the source EEW."] # [doc = ""] # [doc = "  * The destination EEW is smaller than the source EEW and the overlap is"] # [doc = "    in the lowest-numbered part of the source register group (e.g., when LMUL=1,"] # [doc = "    vnsrl.wi v0, v0, 3 is legal, but a destination of v1 is not)."] # [doc = ""] # [doc = "  * The destination EEW is greater than the source EEW, the source EMUL is at"] # [doc = "    least 1, and the overlap is in the highest-numbered part of the destination register"] # [doc = "    group (e.g., when LMUL=8, vzext.vf4 v0, v6 is legal, but a source of v0, v2, or v4 is not)."] # [doc = ""] # [doc = " For the purpose of determining register group overlap constraints, mask elements have EEW=1."] fn forbids_src_dst_overlaps (& self) -> bool ; # [doc = " § 5.3 Vector Masking states:"] # [doc = ""] # [doc = " > The destination vector register group for a masked vector instruction"] # [doc = " > cannot overlap the source mask register (v0), unless the destination"] # [doc = " > vector register is being written with a mask value (e.g., compares) or"] # [doc = " > the scalar result of a reduction. These instruction encodings are reserved."] # [doc = ""] # [doc = " In almost all instructions we should not allow the mask to be re-used as"] # [doc = " a destination register."] fn forbids_mask_dst_overlaps (& self) -> bool { true } # [doc = " There are two broad categories of overlaps (see above). But we can't represent such"] # [doc = " fine grained overlaps to regalloc. So if any of the two come into play we forbid"] # [doc = " all source and destination overlaps (including masks)."] fn forbids_overlaps (& self , mask : & VecOpMasking) -> bool { self . forbids_src_dst_overlaps () || (mask . is_enabled () && self . forbids_mask_dst_overlaps ()) } }
};
}
