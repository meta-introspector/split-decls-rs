// Generated macro for MachInstLabelUse (trait)
macro_rules! Depcrate_machinstMachInstLabelUse {
() => {
// Module: crate::machinst
// Provides: {"MachInstLabelUse"}
// Dependencies: {}
# [doc = " A descriptor of a label reference (use) in an instruction set."] pub trait MachInstLabelUse : Clone + Copy + Debug + Eq { # [doc = " Required alignment for any veneer. Usually the required instruction"] # [doc = " alignment (e.g., 4 for a RISC with 32-bit instructions, or 1 for x86)."] const ALIGN : CodeOffset ; # [doc = " What is the maximum PC-relative range (positive)? E.g., if `1024`, a"] # [doc = " label-reference fixup at offset `x` is valid if the label resolves to `x"] # [doc = " + 1024`."] fn max_pos_range (self) -> CodeOffset ; # [doc = " What is the maximum PC-relative range (negative)? This is the absolute"] # [doc = " value; i.e., if `1024`, then a label-reference fixup at offset `x` is"] # [doc = " valid if the label resolves to `x - 1024`."] fn max_neg_range (self) -> CodeOffset ; # [doc = " What is the size of code-buffer slice this label-use needs to patch in"] # [doc = " the label's value?"] fn patch_size (self) -> CodeOffset ; # [doc = " Perform a code-patch, given the offset into the buffer of this label use"] # [doc = " and the offset into the buffer of the label's definition."] # [doc = " It is guaranteed that, given `delta = offset - label_offset`, we will"] # [doc = " have `offset >= -self.max_neg_range()` and `offset <="] # [doc = " self.max_pos_range()`."] fn patch (self , buffer : & mut [u8] , use_offset : CodeOffset , label_offset : CodeOffset) ; # [doc = " Can the label-use be patched to a veneer that supports a longer range?"] # [doc = " Usually valid for jumps (a short-range jump can jump to a longer-range"] # [doc = " jump), but not for e.g. constant pool references, because the constant"] # [doc = " load would require different code (one more level of indirection)."] fn supports_veneer (self) -> bool ; # [doc = " How many bytes are needed for a veneer?"] fn veneer_size (self) -> CodeOffset ; # [doc = " What's the largest possible veneer that may be generated?"] fn worst_case_veneer_size () -> CodeOffset ; # [doc = " Generate a veneer. The given code-buffer slice is `self.veneer_size()`"] # [doc = " bytes long at offset `veneer_offset` in the buffer. The original"] # [doc = " label-use will be patched to refer to this veneer's offset.  A new"] # [doc = " (offset, LabelUse) is returned that allows the veneer to use the actual"] # [doc = " label. For veneers to work properly, it is expected that the new veneer"] # [doc = " has a larger range; on most platforms this probably means either a"] # [doc = " \"long-range jump\" (e.g., on ARM, the 26-bit form), or if already at that"] # [doc = " stage, a jump that supports a full 32-bit range, for example."] fn generate_veneer (self , buffer : & mut [u8] , veneer_offset : CodeOffset) -> (CodeOffset , Self) ; # [doc = " Returns the corresponding label-use for the relocation specified."] # [doc = ""] # [doc = " This returns `None` if the relocation doesn't have a corresponding"] # [doc = " representation for the target architecture."] fn from_reloc (reloc : Reloc , addend : Addend) -> Option < Self > ; }
};
}
