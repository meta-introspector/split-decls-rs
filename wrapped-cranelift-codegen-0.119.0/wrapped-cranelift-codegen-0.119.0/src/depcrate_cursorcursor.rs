// Generated macro for Cursor (trait)
macro_rules! Depcrate_cursorCursor {
() => {
// Module: crate::cursor
// Provides: {"Cursor"}
// Dependencies: {}
# [doc = " All cursor types implement the `Cursor` which provides common navigation operations."] pub trait Cursor { # [doc = " Get the current cursor position."] fn position (& self) -> CursorPosition ; # [doc = " Set the current position."] fn set_position (& mut self , pos : CursorPosition) ; # [doc = " Get the source location that should be assigned to new instructions."] fn srcloc (& self) -> ir :: SourceLoc ; # [doc = " Set the source location that should be assigned to new instructions."] fn set_srcloc (& mut self , srcloc : ir :: SourceLoc) ; # [doc = " Borrow a reference to the function layout that this cursor is navigating."] fn layout (& self) -> & ir :: Layout ; # [doc = " Borrow a mutable reference to the function layout that this cursor is navigating."] fn layout_mut (& mut self) -> & mut ir :: Layout ; # [doc = " Exchange this cursor for one with a set source location."] # [doc = ""] # [doc = " This is intended to be used as a builder method:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block, SourceLoc};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_func(func: &mut Function, srcloc: SourceLoc) {"] # [doc = "     let mut pos = FuncCursor::new(func).with_srcloc(srcloc);"] # [doc = ""] # [doc = "     // Use `pos`..."] # [doc = " }"] # [doc = " ```"] fn with_srcloc (mut self , srcloc : ir :: SourceLoc) -> Self where Self : Sized , { self . set_srcloc (srcloc) ; self } # [doc = " Rebuild this cursor positioned at `pos`."] fn at_position (mut self , pos : CursorPosition) -> Self where Self : Sized , { self . set_position (pos) ; self } # [doc = " Rebuild this cursor positioned at `inst`."] # [doc = ""] # [doc = " This is intended to be used as a builder method:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block, Inst};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_func(func: &mut Function, inst: Inst) {"] # [doc = "     let mut pos = FuncCursor::new(func).at_inst(inst);"] # [doc = ""] # [doc = "     // Use `pos`..."] # [doc = " }"] # [doc = " ```"] fn at_inst (mut self , inst : ir :: Inst) -> Self where Self : Sized , { self . goto_inst (inst) ; self } # [doc = " Rebuild this cursor positioned at the first insertion point for `block`."] # [doc = " This differs from `at_first_inst` in that it doesn't assume that any"] # [doc = " instructions have been inserted into `block` yet."] # [doc = ""] # [doc = " This is intended to be used as a builder method:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block, Inst};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_func(func: &mut Function, block: Block) {"] # [doc = "     let mut pos = FuncCursor::new(func).at_first_insertion_point(block);"] # [doc = ""] # [doc = "     // Use `pos`..."] # [doc = " }"] # [doc = " ```"] fn at_first_insertion_point (mut self , block : ir :: Block) -> Self where Self : Sized , { self . goto_first_insertion_point (block) ; self } # [doc = " Rebuild this cursor positioned at the first instruction in `block`."] # [doc = ""] # [doc = " This is intended to be used as a builder method:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block, Inst};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_func(func: &mut Function, block: Block) {"] # [doc = "     let mut pos = FuncCursor::new(func).at_first_inst(block);"] # [doc = ""] # [doc = "     // Use `pos`..."] # [doc = " }"] # [doc = " ```"] fn at_first_inst (mut self , block : ir :: Block) -> Self where Self : Sized , { self . goto_first_inst (block) ; self } # [doc = " Rebuild this cursor positioned at the last instruction in `block`."] # [doc = ""] # [doc = " This is intended to be used as a builder method:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block, Inst};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_func(func: &mut Function, block: Block) {"] # [doc = "     let mut pos = FuncCursor::new(func).at_last_inst(block);"] # [doc = ""] # [doc = "     // Use `pos`..."] # [doc = " }"] # [doc = " ```"] fn at_last_inst (mut self , block : ir :: Block) -> Self where Self : Sized , { self . goto_last_inst (block) ; self } # [doc = " Rebuild this cursor positioned after `inst`."] # [doc = ""] # [doc = " This is intended to be used as a builder method:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block, Inst};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_func(func: &mut Function, inst: Inst) {"] # [doc = "     let mut pos = FuncCursor::new(func).after_inst(inst);"] # [doc = ""] # [doc = "     // Use `pos`..."] # [doc = " }"] # [doc = " ```"] fn after_inst (mut self , inst : ir :: Inst) -> Self where Self : Sized , { self . goto_after_inst (inst) ; self } # [doc = " Rebuild this cursor positioned at the top of `block`."] # [doc = ""] # [doc = " This is intended to be used as a builder method:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block, Inst};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_func(func: &mut Function, block: Block) {"] # [doc = "     let mut pos = FuncCursor::new(func).at_top(block);"] # [doc = ""] # [doc = "     // Use `pos`..."] # [doc = " }"] # [doc = " ```"] fn at_top (mut self , block : ir :: Block) -> Self where Self : Sized , { self . goto_top (block) ; self } # [doc = " Rebuild this cursor positioned at the bottom of `block`."] # [doc = ""] # [doc = " This is intended to be used as a builder method:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block, Inst};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_func(func: &mut Function, block: Block) {"] # [doc = "     let mut pos = FuncCursor::new(func).at_bottom(block);"] # [doc = ""] # [doc = "     // Use `pos`..."] # [doc = " }"] # [doc = " ```"] fn at_bottom (mut self , block : ir :: Block) -> Self where Self : Sized , { self . goto_bottom (block) ; self } # [doc = " Get the block corresponding to the current position."] fn current_block (& self) -> Option < ir :: Block > { use self :: CursorPosition :: * ; match self . position () { Nowhere => None , At (inst) => self . layout () . inst_block (inst) , Before (block) | After (block) => Some (block) , } } # [doc = " Get the instruction corresponding to the current position, if any."] fn current_inst (& self) -> Option < ir :: Inst > { use self :: CursorPosition :: * ; match self . position () { At (inst) => Some (inst) , _ => None , } } # [doc = " Go to the position after a specific instruction, which must be inserted"] # [doc = " in the layout. New instructions will be inserted after `inst`."] fn goto_after_inst (& mut self , inst : ir :: Inst) { debug_assert ! (self . layout () . inst_block (inst) . is_some ()) ; let new_pos = if let Some (next) = self . layout () . next_inst (inst) { CursorPosition :: At (next) } else { CursorPosition :: After (self . layout () . inst_block (inst) . expect ("current instruction removed?") ,) } ; self . set_position (new_pos) ; } # [doc = " Go to a specific instruction which must be inserted in the layout."] # [doc = " New instructions will be inserted before `inst`."] fn goto_inst (& mut self , inst : ir :: Inst) { debug_assert ! (self . layout () . inst_block (inst) . is_some ()) ; self . set_position (CursorPosition :: At (inst)) ; } # [doc = " Go to the position for inserting instructions at the beginning of `block`,"] # [doc = " which unlike `goto_first_inst` doesn't assume that any instructions have"] # [doc = " been inserted into `block` yet."] fn goto_first_insertion_point (& mut self , block : ir :: Block) { if let Some (inst) = self . layout () . first_inst (block) { self . goto_inst (inst) ; } else { self . goto_bottom (block) ; } } # [doc = " Go to the first instruction in `block`."] fn goto_first_inst (& mut self , block : ir :: Block) { let inst = self . layout () . first_inst (block) . expect ("Empty block") ; self . goto_inst (inst) ; } # [doc = " Go to the last instruction in `block`."] fn goto_last_inst (& mut self , block : ir :: Block) { let inst = self . layout () . last_inst (block) . expect ("Empty block") ; self . goto_inst (inst) ; } # [doc = " Go to the top of `block` which must be inserted into the layout."] # [doc = " At this position, instructions cannot be inserted, but `next_inst()` will move to the first"] # [doc = " instruction in `block`."] fn goto_top (& mut self , block : ir :: Block) { debug_assert ! (self . layout () . is_block_inserted (block)) ; self . set_position (CursorPosition :: Before (block)) ; } # [doc = " Go to the bottom of `block` which must be inserted into the layout."] # [doc = " At this position, inserted instructions will be appended to `block`."] fn goto_bottom (& mut self , block : ir :: Block) { debug_assert ! (self . layout () . is_block_inserted (block)) ; self . set_position (CursorPosition :: After (block)) ; } # [doc = " Go to the top of the next block in layout order and return it."] # [doc = ""] # [doc = " - If the cursor wasn't pointing at anything, go to the top of the first block in the"] # [doc = "   function."] # [doc = " - If there are no more blocks, leave the cursor pointing at nothing and return `None`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " The `next_block()` method is intended for iterating over the blocks in layout order:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_func(func: &mut Function) {"] # [doc = "     let mut cursor = FuncCursor::new(func);"] # [doc = "     while let Some(block) = cursor.next_block() {"] # [doc = "         // Edit block."] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] fn next_block (& mut self) -> Option < ir :: Block > { let next = if let Some (block) = self . current_block () { self . layout () . next_block (block) } else { self . layout () . entry_block () } ; self . set_position (match next { Some (block) => CursorPosition :: Before (block) , None => CursorPosition :: Nowhere , }) ; next } # [doc = " Go to the bottom of the previous block in layout order and return it."] # [doc = ""] # [doc = " - If the cursor wasn't pointing at anything, go to the bottom of the last block in the"] # [doc = "   function."] # [doc = " - If there are no more blocks, leave the cursor pointing at nothing and return `None`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " The `prev_block()` method is intended for iterating over the blocks in backwards layout order:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_func(func: &mut Function) {"] # [doc = "     let mut cursor = FuncCursor::new(func);"] # [doc = "     while let Some(block) = cursor.prev_block() {"] # [doc = "         // Edit block."] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] fn prev_block (& mut self) -> Option < ir :: Block > { let prev = if let Some (block) = self . current_block () { self . layout () . prev_block (block) } else { self . layout () . last_block () } ; self . set_position (match prev { Some (block) => CursorPosition :: After (block) , None => CursorPosition :: Nowhere , }) ; prev } # [doc = " Move to the next instruction in the same block and return it."] # [doc = ""] # [doc = " - If the cursor was positioned before a block, go to the first instruction in that block."] # [doc = " - If there are no more instructions in the block, go to the `After(block)` position and return"] # [doc = "   `None`."] # [doc = " - If the cursor wasn't pointing anywhere, keep doing that."] # [doc = ""] # [doc = " This method will never move the cursor to a different block."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " The `next_inst()` method is intended for iterating over the instructions in a block like"] # [doc = " this:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_block(func: &mut Function, block: Block) {"] # [doc = "     let mut cursor = FuncCursor::new(func).at_top(block);"] # [doc = "     while let Some(inst) = cursor.next_inst() {"] # [doc = "         // Edit instructions..."] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = " The loop body can insert and remove instructions via the cursor."] # [doc = ""] # [doc = " Iterating over all the instructions in a function looks like this:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_func(func: &mut Function) {"] # [doc = "     let mut cursor = FuncCursor::new(func);"] # [doc = "     while let Some(block) = cursor.next_block() {"] # [doc = "         while let Some(inst) = cursor.next_inst() {"] # [doc = "             // Edit instructions..."] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] fn next_inst (& mut self) -> Option < ir :: Inst > { use self :: CursorPosition :: * ; match self . position () { Nowhere | After (..) => None , At (inst) => { if let Some (next) = self . layout () . next_inst (inst) { self . set_position (At (next)) ; Some (next) } else { let pos = After (self . layout () . inst_block (inst) . expect ("current instruction removed?") ,) ; self . set_position (pos) ; None } } Before (block) => { if let Some (next) = self . layout () . first_inst (block) { self . set_position (At (next)) ; Some (next) } else { self . set_position (After (block)) ; None } } } } # [doc = " Move to the previous instruction in the same block and return it."] # [doc = ""] # [doc = " - If the cursor was positioned after a block, go to the last instruction in that block."] # [doc = " - If there are no more instructions in the block, go to the `Before(block)` position and return"] # [doc = "   `None`."] # [doc = " - If the cursor wasn't pointing anywhere, keep doing that."] # [doc = ""] # [doc = " This method will never move the cursor to a different block."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " The `prev_inst()` method is intended for iterating backwards over the instructions in an"] # [doc = " block like this:"] # [doc = ""] # [doc = " ```"] # [doc = " # use cranelift_codegen::ir::{Function, Block};"] # [doc = " # use cranelift_codegen::cursor::{Cursor, FuncCursor};"] # [doc = " fn edit_block(func: &mut Function, block: Block) {"] # [doc = "     let mut cursor = FuncCursor::new(func).at_bottom(block);"] # [doc = "     while let Some(inst) = cursor.prev_inst() {"] # [doc = "         // Edit instructions..."] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] fn prev_inst (& mut self) -> Option < ir :: Inst > { use self :: CursorPosition :: * ; match self . position () { Nowhere | Before (..) => None , At (inst) => { if let Some (prev) = self . layout () . prev_inst (inst) { self . set_position (At (prev)) ; Some (prev) } else { let pos = Before (self . layout () . inst_block (inst) . expect ("current instruction removed?") ,) ; self . set_position (pos) ; None } } After (block) => { if let Some (prev) = self . layout () . last_inst (block) { self . set_position (At (prev)) ; Some (prev) } else { self . set_position (Before (block)) ; None } } } } # [doc = " Insert an instruction at the current position."] # [doc = ""] # [doc = " - If pointing at an instruction, the new instruction is inserted before the current"] # [doc = "   instruction."] # [doc = " - If pointing at the bottom of a block, the new instruction is appended to the block."] # [doc = " - Otherwise panic."] # [doc = ""] # [doc = " In either case, the cursor is not moved, such that repeated calls to `insert_inst()` causes"] # [doc = " instructions to appear in insertion order in the block."] fn insert_inst (& mut self , inst : ir :: Inst) { use self :: CursorPosition :: * ; match self . position () { Nowhere | Before (..) => panic ! ("Invalid insert_inst position") , At (cur) => self . layout_mut () . insert_inst (inst , cur) , After (block) => self . layout_mut () . append_inst (inst , block) , } } # [doc = " Remove the instruction under the cursor."] # [doc = ""] # [doc = " The cursor is left pointing at the position following the current instruction."] # [doc = ""] # [doc = " Return the instruction that was removed."] fn remove_inst (& mut self) -> ir :: Inst { let inst = self . current_inst () . expect ("No instruction to remove") ; self . next_inst () ; self . layout_mut () . remove_inst (inst) ; inst } # [doc = " Remove the instruction under the cursor."] # [doc = ""] # [doc = " The cursor is left pointing at the position preceding the current instruction."] # [doc = ""] # [doc = " Return the instruction that was removed."] fn remove_inst_and_step_back (& mut self) -> ir :: Inst { let inst = self . current_inst () . expect ("No instruction to remove") ; self . prev_inst () ; self . layout_mut () . remove_inst (inst) ; inst } # [doc = " Insert a block at the current position and switch to it."] # [doc = ""] # [doc = " As far as possible, this method behaves as if the block header were an instruction inserted"] # [doc = " at the current position."] # [doc = ""] # [doc = " - If the cursor is pointing at an existing instruction, *the current block is split in two*"] # [doc = "   and the current instruction becomes the first instruction in the inserted block."] # [doc = " - If the cursor points at the bottom of a block, the new block is inserted after the current"] # [doc = "   one, and moved to the bottom of the new block where instructions can be appended."] # [doc = " - If the cursor points to the top of a block, the new block is inserted above the current one."] # [doc = " - If the cursor is not pointing at anything, the new block is placed last in the layout."] # [doc = ""] # [doc = " This means that it is always valid to call this method, and it always leaves the cursor in"] # [doc = " a state that will insert instructions into the new block."] fn insert_block (& mut self , new_block : ir :: Block) { use self :: CursorPosition :: * ; match self . position () { At (inst) => { self . layout_mut () . split_block (new_block , inst) ; return ; } Nowhere => self . layout_mut () . append_block (new_block) , Before (block) => self . layout_mut () . insert_block (new_block , block) , After (block) => self . layout_mut () . insert_block_after (new_block , block) , } self . set_position (After (new_block)) ; } }
};
}
