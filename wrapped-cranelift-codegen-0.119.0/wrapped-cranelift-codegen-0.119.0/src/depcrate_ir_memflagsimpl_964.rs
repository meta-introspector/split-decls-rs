// Generated macro for impl_964 (impl)
macro_rules! Depcrate_ir_memflagsimpl_964 {
() => {
// Module: crate::ir::memflags
// Provides: {"impl_964"}
// Dependencies: {}
impl MemFlags { # [doc = " Create a new empty set of flags."] pub const fn new () -> Self { Self { bits : 0 } . with_trap_code (Some (TrapCode :: HEAP_OUT_OF_BOUNDS)) } # [doc = " Create a set of flags representing an access from a \"trusted\" address, meaning it's"] # [doc = " known to be aligned and non-trapping."] pub const fn trusted () -> Self { Self :: new () . with_notrap () . with_aligned () } # [doc = " Read a flag bit."] const fn read_bit (self , bit : u16) -> bool { self . bits & bit != 0 } # [doc = " Return a new `MemFlags` with this flag bit set."] const fn with_bit (mut self , bit : u16) -> Self { self . bits |= bit ; self } # [doc = " Reads the alias region that this memory operation works with."] pub const fn alias_region (self) -> Option < AliasRegion > { AliasRegion :: from_bits (((self . bits & MASK_ALIAS_REGION) >> ALIAS_REGION_OFFSET) as u8) } # [doc = " Sets the alias region that this works on to the specified `region`."] pub const fn with_alias_region (mut self , region : Option < AliasRegion >) -> Self { let bits = AliasRegion :: to_bits (region) ; self . bits &= ! MASK_ALIAS_REGION ; self . bits |= (bits as u16) << ALIAS_REGION_OFFSET ; self } # [doc = " Sets the alias region that this works on to the specified `region`."] pub fn set_alias_region (& mut self , region : Option < AliasRegion >) { * self = self . with_alias_region (region) ; } # [doc = " Set a flag bit by name."] # [doc = ""] # [doc = " Returns true if the flag was found and set, false for an unknown flag"] # [doc = " name."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns an error message if the `name` is known but couldn't be applied"] # [doc = " due to it being a semantic error."] pub fn set_by_name (& mut self , name : & str) -> Result < bool , & 'static str > { * self = match name { "notrap" => self . with_trap_code (None) , "aligned" => self . with_aligned () , "readonly" => self . with_readonly () , "little" => { if self . read_bit (BIT_BIG_ENDIAN) { return Err ("cannot set both big and little endian bits") ; } self . with_endianness (Endianness :: Little) } "big" => { if self . read_bit (BIT_LITTLE_ENDIAN) { return Err ("cannot set both big and little endian bits") ; } self . with_endianness (Endianness :: Big) } "heap" => { if self . alias_region () . is_some () { return Err ("cannot set more than one alias region") ; } self . with_alias_region (Some (AliasRegion :: Heap)) } "table" => { if self . alias_region () . is_some () { return Err ("cannot set more than one alias region") ; } self . with_alias_region (Some (AliasRegion :: Table)) } "vmctx" => { if self . alias_region () . is_some () { return Err ("cannot set more than one alias region") ; } self . with_alias_region (Some (AliasRegion :: Vmctx)) } "checked" => self . with_checked () , "can_move" => self . with_can_move () , other => match TrapCode :: from_str (other) { Ok (code) => self . with_trap_code (Some (code)) , Err (()) => return Ok (false) , } , } ; Ok (true) } # [doc = " Return endianness of the memory access.  This will return the endianness"] # [doc = " explicitly specified by the flags if any, and will default to the native"] # [doc = " endianness otherwise.  The native endianness has to be provided by the"] # [doc = " caller since it is not explicitly encoded in CLIF IR -- this allows a"] # [doc = " front end to create IR without having to know the target endianness."] pub const fn endianness (self , native_endianness : Endianness) -> Endianness { if self . read_bit (BIT_LITTLE_ENDIAN) { Endianness :: Little } else if self . read_bit (BIT_BIG_ENDIAN) { Endianness :: Big } else { native_endianness } } # [doc = " Return endianness of the memory access, if explicitly specified."] # [doc = ""] # [doc = " If the endianness is not explicitly specified, this will return `None`,"] # [doc = " which means \"native endianness\"."] pub const fn explicit_endianness (self) -> Option < Endianness > { if self . read_bit (BIT_LITTLE_ENDIAN) { Some (Endianness :: Little) } else if self . read_bit (BIT_BIG_ENDIAN) { Some (Endianness :: Big) } else { None } } # [doc = " Set endianness of the memory access."] pub fn set_endianness (& mut self , endianness : Endianness) { * self = self . with_endianness (endianness) ; } # [doc = " Set endianness of the memory access, returning new flags."] pub const fn with_endianness (self , endianness : Endianness) -> Self { let res = match endianness { Endianness :: Little => self . with_bit (BIT_LITTLE_ENDIAN) , Endianness :: Big => self . with_bit (BIT_BIG_ENDIAN) , } ; assert ! (! (res . read_bit (BIT_LITTLE_ENDIAN) && res . read_bit (BIT_BIG_ENDIAN))) ; res } # [doc = " Test if this memory operation cannot trap."] # [doc = ""] # [doc = " By default `MemFlags` will assume that any load/store can trap and is"] # [doc = " associated with a `TrapCode::HeapOutOfBounds` code. If the trap code is"] # [doc = " configured to `None` though then this method will return `true` and"] # [doc = " indicates that the memory operation will not trap."] # [doc = ""] # [doc = " If this returns `true` then the memory is *accessible*, which means"] # [doc = " that accesses will not trap. This makes it possible to delete an unused"] # [doc = " load or a dead store instruction."] # [doc = ""] # [doc = " This flag does *not* mean that the associated instruction can be"] # [doc = " code-motioned to arbitrary places in the function so long as its data"] # [doc = " dependencies are met. This only means that, given its current location"] # [doc = " in the function, it will never trap. See the `can_move` method for more"] # [doc = " details."] pub const fn notrap (self) -> bool { self . trap_code () . is_none () } # [doc = " Sets the trap code for this `MemFlags` to `None`."] pub fn set_notrap (& mut self) { * self = self . with_notrap () ; } # [doc = " Sets the trap code for this `MemFlags` to `None`, returning the new"] # [doc = " flags."] pub const fn with_notrap (self) -> Self { self . with_trap_code (None) } # [doc = " Is this memory operation safe to move so long as its data dependencies"] # [doc = " remain satisfied?"] # [doc = ""] # [doc = " If this is `true`, then it is okay to code motion this instruction to"] # [doc = " arbitrary locations, in the function, including across blocks and"] # [doc = " conditional branches, so long as data dependencies (and trap ordering,"] # [doc = " if any) are upheld."] # [doc = ""] # [doc = " If this is `false`, then this memory operation's safety potentially"] # [doc = " relies upon invariants that are not reflected in its data dependencies,"] # [doc = " and therefore it is not safe to code motion this operation. For example,"] # [doc = " this operation could be in a block that is dominated by a control-flow"] # [doc = " bounds check, which is not reflected in its operands, and it would be"] # [doc = " unsafe to code motion it above the bounds check, even if its data"] # [doc = " dependencies would still be satisfied."] pub const fn can_move (self) -> bool { self . read_bit (BIT_CAN_MOVE) } # [doc = " Set the `can_move` flag."] pub const fn set_can_move (& mut self) { * self = self . with_can_move () ; } # [doc = " Set the `can_move` flag, returning new flags."] pub const fn with_can_move (self) -> Self { self . with_bit (BIT_CAN_MOVE) } # [doc = " Test if the `aligned` flag is set."] # [doc = ""] # [doc = " By default, Cranelift memory instructions work with any unaligned effective address. If the"] # [doc = " `aligned` flag is set, the instruction is permitted to trap or return a wrong result if the"] # [doc = " effective address is misaligned."] pub const fn aligned (self) -> bool { self . read_bit (BIT_ALIGNED) } # [doc = " Set the `aligned` flag."] pub fn set_aligned (& mut self) { * self = self . with_aligned () ; } # [doc = " Set the `aligned` flag, returning new flags."] pub const fn with_aligned (self) -> Self { self . with_bit (BIT_ALIGNED) } # [doc = " Test if the `readonly` flag is set."] # [doc = ""] # [doc = " Loads with this flag have no memory dependencies."] # [doc = " This results in undefined behavior if the dereferenced memory is mutated at any time"] # [doc = " between when the function is called and when it is exited."] pub const fn readonly (self) -> bool { self . read_bit (BIT_READONLY) } # [doc = " Set the `readonly` flag."] pub fn set_readonly (& mut self) { * self = self . with_readonly () ; } # [doc = " Set the `readonly` flag, returning new flags."] pub const fn with_readonly (self) -> Self { self . with_bit (BIT_READONLY) } # [doc = " Test if the `checked` bit is set."] # [doc = ""] # [doc = " Loads and stores with this flag are verified to access"] # [doc = " pointers only with a validated `PointsTo` fact attached, and"] # [doc = " with that fact validated, when using the proof-carrying-code"] # [doc = " framework. If initial facts on program inputs are correct"] # [doc = " (i.e., correctly denote the shape and types of data structures"] # [doc = " in memory), and if PCC validates the compiled output, then all"] # [doc = " `checked`-marked memory accesses are guaranteed (up to the"] # [doc = " checker's correctness) to access valid memory. This can be"] # [doc = " used to ensure memory safety and sandboxing."] pub const fn checked (self) -> bool { self . read_bit (BIT_CHECKED) } # [doc = " Set the `checked` bit."] pub fn set_checked (& mut self) { * self = self . with_checked () ; } # [doc = " Set the `checked` bit, returning new flags."] pub const fn with_checked (self) -> Self { self . with_bit (BIT_CHECKED) } # [doc = " Get the trap code to report if this memory access traps."] # [doc = ""] # [doc = " A `None` trap code indicates that this memory access does not trap."] pub const fn trap_code (self) -> Option < TrapCode > { let byte = ((self . bits & MASK_TRAP_CODE) >> TRAP_CODE_OFFSET) as u8 ; match NonZeroU8 :: new (byte) { Some (code) => Some (TrapCode :: from_raw (code)) , None => None , } } # [doc = " Configures these flags with the specified trap code `code`."] # [doc = ""] # [doc = " A trap code indicates that this memory operation cannot be optimized"] # [doc = " away and it must \"stay where it is\" in the programs. Traps are"] # [doc = " considered side effects, for example, and have meaning through the trap"] # [doc = " code that is communicated and which instruction trapped."] pub const fn with_trap_code (mut self , code : Option < TrapCode >) -> Self { let bits = match code { Some (code) => code . as_raw () . get () as u16 , None => 0 , } ; self . bits &= ! MASK_TRAP_CODE ; self . bits |= bits << TRAP_CODE_OFFSET ; self } }
};
}
