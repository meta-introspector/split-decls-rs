// Generated macro for Lower (struct)
macro_rules! Depcrate_machinst_lowerLower {
() => {
// Module: crate::machinst::lower
// Provides: {"Lower"}
// Dependencies: {}
# [doc = " Machine-independent lowering driver / machine-instruction container. Maintains a correspondence"] # [doc = " from original Inst to MachInsts."] pub struct Lower < 'func , I : VCodeInst > { # [doc = " The function to lower."] f : & 'func Function , # [doc = " Lowered machine instructions."] vcode : VCodeBuilder < I > , # [doc = " VReg allocation context, given to the vcode field at build time to finalize the vcode."] vregs : VRegAllocator < I > , # [doc = " Mapping from `Value` (SSA value in IR) to virtual register."] value_regs : SecondaryMap < Value , ValueRegs < Reg > > , # [doc = " sret registers, if needed."] sret_reg : Option < ValueRegs < Reg > > , # [doc = " Instruction colors at block exits. From this map, we can recover all"] # [doc = " instruction colors by scanning backward from the block end and"] # [doc = " decrementing on any color-changing (side-effecting) instruction."] block_end_colors : SecondaryMap < Block , InstColor > , # [doc = " Instruction colors at side-effecting ops. This is the *entry* color,"] # [doc = " i.e., the version of global state that exists before an instruction"] # [doc = " executes.  For each side-effecting instruction, the *exit* color is its"] # [doc = " entry color plus one."] side_effect_inst_entry_colors : FxHashMap < Inst , InstColor > , # [doc = " Current color as we scan during lowering. While we are lowering an"] # [doc = " instruction, this is equal to the color *at entry to* the instruction."] cur_scan_entry_color : Option < InstColor > , # [doc = " Current instruction as we scan during lowering."] cur_inst : Option < Inst > , # [doc = " Instruction constant values, if known."] inst_constants : FxHashMap < Inst , u64 > , # [doc = " Use-counts per SSA value, as counted in the input IR. These"] # [doc = " are \"coarsened\", in the abstract-interpretation sense: we only"] # [doc = " care about \"0, 1, many\" states, as this is all we need and"] # [doc = " this lets us do an efficient fixpoint analysis."] # [doc = ""] # [doc = " See doc comment on `ValueUseState` for more details."] value_ir_uses : SecondaryMap < Value , ValueUseState > , # [doc = " Actual uses of each SSA value so far, incremented while lowering."] value_lowered_uses : SecondaryMap < Value , u32 > , # [doc = " Effectful instructions that have been sunk; they are not codegen'd at"] # [doc = " their original locations."] inst_sunk : FxHashSet < Inst > , # [doc = " Instructions collected for the CLIF inst in progress, in forward order."] ir_insts : Vec < I > , # [doc = " The register to use for GetPinnedReg, if any, on this architecture."] pinned_reg : Option < Reg > , # [doc = " Compilation flags."] flags : Flags , }
};
}
