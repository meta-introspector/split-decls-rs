// Generated macro for ValueUseState (enum)
macro_rules! Depcrate_machinst_lowerValueUseState {
() => {
// Module: crate::machinst::lower
// Provides: {"ValueUseState"}
// Dependencies: {}
# [doc = " How is a value used in the IR?"] # [doc = ""] # [doc = " This can be seen as a coarsening of an integer count. We only need"] # [doc = " distinct states for zero, one, or many."] # [doc = ""] # [doc = " This analysis deserves further explanation. The basic idea is that"] # [doc = " we want to allow instruction lowering to know whether a value that"] # [doc = " an instruction references is *only* referenced by that one use, or"] # [doc = " by others as well. This is necessary to know when we might want to"] # [doc = " move a side-effect: we cannot, for example, duplicate a load, so"] # [doc = " we cannot let instruction lowering match a load as part of a"] # [doc = " subpattern and potentially incorporate it."] # [doc = ""] # [doc = " Note that a lot of subtlety comes into play once we have"] # [doc = " *indirect* uses. The classical example of this in our development"] # [doc = " history was the x86 compare instruction, which is incorporated"] # [doc = " into flags users (e.g. `selectif`, `trueif`, branches) and can"] # [doc = " subsequently incorporate loads, or at least we would like it"] # [doc = " to. However, danger awaits: the compare might be the only user of"] # [doc = " a load, so we might think we can just move the load (and nothing"] # [doc = " is duplicated -- success!), except that the compare itself is"] # [doc = " codegen'd in multiple places, where it is incorporated as a"] # [doc = " subpattern itself."] # [doc = ""] # [doc = " So we really want a notion of \"unique all the way along the"] # [doc = " matching path\". Rust's `&T` and `&mut T` offer a partial analogy"] # [doc = " to the semantics that we want here: we want to know when we've"] # [doc = " matched a unique use of an instruction, and that instruction's"] # [doc = " unique use of another instruction, etc, just as `&mut T` can only"] # [doc = " be obtained by going through a chain of `&mut T`. If one has a"] # [doc = " `&T` to a struct containing `&mut T` (one of several uses of an"] # [doc = " instruction that itself has a unique use of an instruction), one"] # [doc = " can only get a `&T` (one can only get a \"I am one of several users"] # [doc = " of this instruction\" result)."] # [doc = ""] # [doc = " We could track these paths, either dynamically as one \"looks up the operand"] # [doc = " tree\" or precomputed. But the former requires state and means that the"] # [doc = " `Lower` API carries that state implicitly, which we'd like to avoid if we"] # [doc = " can. And the latter implies O(n^2) storage: it is an all-pairs property (is"] # [doc = " inst `i` unique from the point of view of `j`)."] # [doc = ""] # [doc = " To make matters even a little more complex still, a value that is"] # [doc = " not uniquely used when initially viewing the IR can *become*"] # [doc = " uniquely used, at least as a root allowing further unique uses of"] # [doc = " e.g. loads to merge, if no other instruction actually merges"] # [doc = " it. To be more concrete, if we have `v1 := load; v2 := op v1; v3"] # [doc = " := op v2; v4 := op v2` then `v2` is non-uniquely used, so from the"] # [doc = " point of view of lowering `v4` or `v3`, we cannot merge the load"] # [doc = " at `v1`. But if we decide just to use the assigned register for"] # [doc = " `v2` at both `v3` and `v4`, then we only actually codegen `v2`"] # [doc = " once, so it *is* a unique root at that point and we *can* merge"] # [doc = " the load."] # [doc = ""] # [doc = " Note also that the color scheme is not sufficient to give us this"] # [doc = " information, for various reasons: reasoning about side-effects"] # [doc = " does not tell us about potential duplication of uses through pure"] # [doc = " ops."] # [doc = ""] # [doc = " To keep things simple and avoid error-prone lowering APIs that"] # [doc = " would extract more information about whether instruction merging"] # [doc = " happens or not (we don't have that info now, and it would be"] # [doc = " difficult to refactor to get it and make that refactor 100%"] # [doc = " correct), we give up on the above \"can become unique if not"] # [doc = " actually merged\" point. Instead, we compute a"] # [doc = " transitive-uniqueness. That is what this enum represents."] # [doc = ""] # [doc = " There is one final caveat as well to the result of this analysis.  Notably,"] # [doc = " we define some instructions to be \"root\" instructions, which means that we"] # [doc = " assume they will always be codegen'd at the root of a matching tree, and not"] # [doc = " matched. (This comes with the caveat that we actually enforce this property"] # [doc = " by making them \"opaque\" to subtree matching in"] # [doc = " `get_value_as_source_or_const`). Because they will always be codegen'd once,"] # [doc = " they in some sense \"reset\" multiplicity: these root instructions can be used"] # [doc = " many times, but because their result(s) are only computed once, they only"] # [doc = " use their inputs once."] # [doc = ""] # [doc = " We currently define all multi-result instructions to be \"root\" instructions,"] # [doc = " because it is too complex to reason about matching through them, and they"] # [doc = " cause too-coarse-grained approximation of multiplicity otherwise: the"] # [doc = " analysis would have to assume (as it used to!) that they are always"] # [doc = " multiply-used, simply because they have multiple outputs even if those"] # [doc = " outputs are used only once."] # [doc = ""] # [doc = " In the future we could define other instructions to be \"root\" instructions"] # [doc = " as well, if we make the corresponding change to get_value_as_source_or_const"] # [doc = " as well."] # [doc = ""] # [doc = " To define `ValueUseState` more plainly: a value is `Unused` if no references"] # [doc = " exist to it; `Once` if only one other op refers to it, *and* that other op"] # [doc = " is `Unused` or `Once`; and `Multiple` otherwise. In other words, `Multiple`"] # [doc = " is contagious (except through root instructions): even if an op's result"] # [doc = " value is directly used only once in the CLIF, that value is `Multiple` if"] # [doc = " the op that uses it is itself used multiple times (hence could be codegen'd"] # [doc = " multiple times). In brief, this analysis tells us whether, if every op"] # [doc = " merged all of its operand tree, a given op could be codegen'd in more than"] # [doc = " one place."] # [doc = ""] # [doc = " To compute this, we first consider direct uses. At this point"] # [doc = " `Unused` answers are correct, `Multiple` answers are correct, but"] # [doc = " some `Once`s may change to `Multiple`s. Then we propagate"] # [doc = " `Multiple` transitively using a workqueue/fixpoint algorithm."] # [derive (Clone , Copy , Debug , PartialEq , Eq)] enum ValueUseState { # [doc = " Not used at all."] Unused , # [doc = " Used exactly once."] Once , # [doc = " Used multiple times."] Multiple , }
};
}
