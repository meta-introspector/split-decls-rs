// Generated macro for do_remove_constant_phis (function)
macro_rules! Depcrate_remove_constant_phisdo_remove_constant_phis {
() => {
// Module: crate::remove_constant_phis
// Provides: {"do_remove_constant_phis"}
// Dependencies: {}
# [doc = " Detect phis in `func` that will only ever produce one value, using a"] # [doc = " classic forward dataflow analysis.  Then remove them."] # [inline (never)] pub fn do_remove_constant_phis (func : & mut Function , domtree : & mut DominatorTree) { let _tt = timing :: remove_constant_phis () ; debug_assert ! (domtree . is_valid ()) ; let bump = Bump :: with_capacity (domtree . cfg_postorder () . len () * 4 * std :: mem :: size_of :: < Value > ()) ; let mut summaries = SecondaryMap :: < Block , BlockSummary > :: with_capacity (domtree . cfg_postorder () . len ()) ; for b in domtree . cfg_rpo () . copied () { let formals = func . dfg . block_params (b) ; let mut summary = BlockSummary :: new (& bump , formals) ; for inst in func . layout . block_insts (b) { for (ix , dest) in func . dfg . insts [inst] . branch_destination (& func . dfg . jump_tables) . iter () . enumerate () { if let Some (edge) = OutEdge :: new (& bump , & func . dfg , inst , ix , * dest) { summary . dests . push (edge) ; } } } if formals . len () > 0 || summary . dests . len () > 0 { summaries [b] = summary ; } } let entry_block = func . layout . entry_block () . expect ("remove_constant_phis: entry block unknown") ; let mut state = SolverState :: new () ; for b in domtree . cfg_rpo () . copied () { if b == entry_block { continue ; } let formals = func . dfg . block_params (b) ; for formal in formals { let mb_old_absval = state . absvals . insert (* formal , AbstractValue :: None) ; assert ! (mb_old_absval . is_none ()) ; } } let mut iter_no = 0 ; loop { iter_no += 1 ; let mut changed = false ; for src in domtree . cfg_rpo () . copied () { let src_summary = & summaries [src] ; for edge in & src_summary . dests { assert ! (edge . block != entry_block) ; let dst_summary = & summaries [edge . block] ; let dst_formals = & dst_summary . formals ; assert_eq ! (edge . args . len () , dst_formals . len ()) ; for (formal , actual) in dst_formals . iter () . zip (edge . args) { let actual_absval = match state . maybe_get (* actual) { Some (pt) => * pt , None => AbstractValue :: One (* actual) , } ; let formal_absval_old = state . get (* formal) ; let formal_absval_new = formal_absval_old . join (actual_absval) ; if formal_absval_new != formal_absval_old { changed = true ; state . set (* formal , formal_absval_new) ; } } } } if ! changed { break ; } } let mut n_consts = 0 ; for absval in state . absvals . values () { if absval . is_one () { n_consts += 1 ; } } let mut need_editing = FxHashSet :: < Block > :: default () ; for (block , summary) in summaries . iter () { if block == entry_block { continue ; } for formal in summary . formals { let formal_absval = state . get (* formal) ; if formal_absval . is_one () { need_editing . insert (block) ; break ; } } } for b in & need_editing { let mut del_these = SmallVec :: < [(Value , Value) ; 32] > :: new () ; let formals : & [Value] = func . dfg . block_params (* b) ; for formal in formals { if let AbstractValue :: One (replacement_val) = state . get (* formal) { del_these . push ((* formal , replacement_val)) ; } } for (redundant_formal , replacement_val) in del_these . into_iter () . rev () { func . dfg . remove_block_param (redundant_formal) ; func . dfg . change_to_alias (redundant_formal , replacement_val) ; } } let mut old_actuals = alloc :: vec :: Vec :: new () ; for summary in summaries . values () { for edge in & summary . dests { if ! need_editing . contains (& edge . block) { continue ; } let dfg = & mut func . dfg ; let dests = dfg . insts [edge . inst] . branch_destination_mut (& mut dfg . jump_tables) ; let block = & mut dests [edge . branch_index as usize] ; old_actuals . extend (block . args_slice (& dfg . value_lists)) ; let formals = & summaries [edge . block] . formals ; assert_eq ! (formals . len () , old_actuals . len ()) ; let mut formals = formals . iter () ; old_actuals . retain (| _ | { let formal_i = formals . next () . unwrap () ; ! state . get (* formal_i) . is_one () }) ; let destination = block . block (& dfg . value_lists) ; * block = BlockCall :: new (destination , & old_actuals , & mut dfg . value_lists) ; old_actuals . clear () ; } } log :: debug ! ("do_remove_constant_phis: done, {} iters.   {} formals, of which {} const." , iter_no , state . absvals . len () , n_consts) ; }
};
}
