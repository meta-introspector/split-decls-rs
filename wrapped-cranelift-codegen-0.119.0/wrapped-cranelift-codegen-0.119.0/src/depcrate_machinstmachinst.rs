// Generated macro for MachInst (trait)
macro_rules! Depcrate_machinstMachInst {
() => {
// Module: crate::machinst
// Provides: {"MachInst"}
// Dependencies: {}
# [doc = " A machine instruction."] pub trait MachInst : Clone + Debug { # [doc = " The ABI machine spec for this `MachInst`."] type ABIMachineSpec : ABIMachineSpec < I = Self > ; # [doc = " Return the registers referenced by this machine instruction along with"] # [doc = " the modes of reference (use, def, modify)."] fn get_operands (& mut self , collector : & mut impl OperandVisitor) ; # [doc = " If this is a simple move, return the (source, destination) tuple of registers."] fn is_move (& self) -> Option < (Writable < Reg > , Reg) > ; # [doc = " Is this a terminator (branch or ret)? If so, return its type"] # [doc = " (ret/uncond/cond) and target if applicable."] fn is_term (& self) -> MachTerminator ; # [doc = " Is this an unconditional trap?"] fn is_trap (& self) -> bool ; # [doc = " Is this an \"args\" pseudoinst?"] fn is_args (& self) -> bool ; # [doc = " Should this instruction be included in the clobber-set?"] fn is_included_in_clobbers (& self) -> bool ; # [doc = " Does this instruction access memory?"] fn is_mem_access (& self) -> bool ; # [doc = " Generate a move."] fn gen_move (to_reg : Writable < Reg > , from_reg : Reg , ty : Type) -> Self ; # [doc = " Generate a dummy instruction that will keep a value alive but"] # [doc = " has no other purpose."] fn gen_dummy_use (reg : Reg) -> Self ; # [doc = " Determine register class(es) to store the given Cranelift type, and the"] # [doc = " Cranelift type actually stored in the underlying register(s).  May return"] # [doc = " an error if the type isn't supported by this backend."] # [doc = ""] # [doc = " If the type requires multiple registers, then the list of registers is"] # [doc = " returned in little-endian order."] # [doc = ""] # [doc = " Note that the type actually stored in the register(s) may differ in the"] # [doc = " case that a value is split across registers: for example, on a 32-bit"] # [doc = " target, an I64 may be stored in two registers, each of which holds an"] # [doc = " I32. The actually-stored types are used only to inform the backend when"] # [doc = " generating spills and reloads for individual registers."] fn rc_for_type (ty : Type) -> CodegenResult < (& 'static [RegClass] , & 'static [Type]) > ; # [doc = " Get an appropriate type that can fully hold a value in a given"] # [doc = " register class. This may not be the only type that maps to"] # [doc = " that class, but when used with `gen_move()` or the ABI trait's"] # [doc = " load/spill constructors, it should produce instruction(s) that"] # [doc = " move the entire register contents."] fn canonical_type_for_rc (rc : RegClass) -> Type ; # [doc = " Generate a jump to another target. Used during lowering of"] # [doc = " control flow."] fn gen_jump (target : MachLabel) -> Self ; # [doc = " Generate a store of an immediate 64-bit integer to a register. Used by"] # [doc = " the control plane to generate random instructions."] fn gen_imm_u64 (_value : u64 , _dst : Writable < Reg >) -> Option < Self > { None } # [doc = " Generate a store of an immediate 64-bit integer to a register. Used by"] # [doc = " the control plane to generate random instructions. The tmp register may"] # [doc = " be used by architectures which don't support writing immediate values to"] # [doc = " floating point registers directly."] fn gen_imm_f64 (_value : f64 , _tmp : Writable < Reg > , _dst : Writable < Reg >) -> SmallVec < [Self ; 2] > { SmallVec :: new () } # [doc = " Generate a NOP. The `preferred_size` parameter allows the caller to"] # [doc = " request a NOP of that size, or as close to it as possible. The machine"] # [doc = " backend may return a NOP whose binary encoding is smaller than the"] # [doc = " preferred size, but must not return a NOP that is larger. However,"] # [doc = " the instruction must have a nonzero size if preferred_size is nonzero."] fn gen_nop (preferred_size : usize) -> Self ; # [doc = " Align a basic block offset (from start of function).  By default, no"] # [doc = " alignment occurs."] fn align_basic_block (offset : CodeOffset) -> CodeOffset { offset } # [doc = " What is the worst-case instruction size emitted by this instruction type?"] fn worst_case_size () -> CodeOffset ; # [doc = " What is the register class used for reference types (GC-observable pointers)? Can"] # [doc = " be dependent on compilation flags."] fn ref_type_regclass (_flags : & Flags) -> RegClass ; # [doc = " Is this a safepoint?"] fn is_safepoint (& self) -> bool ; # [doc = " Generate an instruction that must appear at the beginning of a basic"] # [doc = " block, if any. Note that the return value must not be subject to"] # [doc = " register allocation."] fn gen_block_start (_is_indirect_branch_target : bool , _is_forward_edge_cfi_enabled : bool ,) -> Option < Self > { None } # [doc = " Returns a description of the alignment required for functions for this"] # [doc = " architecture."] fn function_alignment () -> FunctionAlignment ; # [doc = " Is this a low-level, one-way branch, not meant for use in a"] # [doc = " VCode body? These instructions are meant to be used only when"] # [doc = " directly emitted, i.e. when `MachInst` is used as an assembler"] # [doc = " library."] fn is_low_level_branch (& self) -> bool { false } # [doc = " A label-use kind: a type that describes the types of label references that"] # [doc = " can occur in an instruction."] type LabelUse : MachInstLabelUse ; # [doc = " Byte representation of a trap opcode which is inserted by `MachBuffer`"] # [doc = " during its `defer_trap` method."] const TRAP_OPCODE : & 'static [u8] ; }
};
}
