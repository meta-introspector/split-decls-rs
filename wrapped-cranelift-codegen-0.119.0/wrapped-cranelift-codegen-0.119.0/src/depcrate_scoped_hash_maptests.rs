// Generated macro for tests (module)
macro_rules! Depcrate_scoped_hash_maptests {
() => {
// Module: crate::scoped_hash_map
// Provides: {"tests"}
// Dependencies: {}
# [cfg (test)] mod tests { use super :: * ; use crate :: ctxhash :: NullCtx ; # [test] fn basic () { let mut map : ScopedHashMap < i32 , i32 > = ScopedHashMap :: new () ; match map . entry (& NullCtx , 0) { Entry :: Occupied (_entry) => panic ! () , Entry :: Vacant (entry) => entry . insert (1) , } match map . entry (& NullCtx , 2) { Entry :: Occupied (_entry) => panic ! () , Entry :: Vacant (entry) => entry . insert (8) , } match map . entry (& NullCtx , 2) { Entry :: Occupied (entry) => assert ! (* entry . get () == 8) , Entry :: Vacant (_entry) => panic ! () , } map . increment_depth () ; match map . entry (& NullCtx , 2) { Entry :: Occupied (entry) => assert ! (* entry . get () == 8) , Entry :: Vacant (_entry) => panic ! () , } match map . entry (& NullCtx , 1) { Entry :: Occupied (_entry) => panic ! () , Entry :: Vacant (entry) => entry . insert (3) , } match map . entry (& NullCtx , 1) { Entry :: Occupied (entry) => assert ! (* entry . get () == 3) , Entry :: Vacant (_entry) => panic ! () , } match map . entry (& NullCtx , 0) { Entry :: Occupied (entry) => assert ! (* entry . get () == 1) , Entry :: Vacant (_entry) => panic ! () , } match map . entry (& NullCtx , 2) { Entry :: Occupied (entry) => assert ! (* entry . get () == 8) , Entry :: Vacant (_entry) => panic ! () , } map . decrement_depth () ; match map . entry (& NullCtx , 0) { Entry :: Occupied (entry) => assert ! (* entry . get () == 1) , Entry :: Vacant (_entry) => panic ! () , } match map . entry (& NullCtx , 2) { Entry :: Occupied (entry) => assert ! (* entry . get () == 8) , Entry :: Vacant (_entry) => panic ! () , } map . increment_depth () ; match map . entry (& NullCtx , 2) { Entry :: Occupied (entry) => assert ! (* entry . get () == 8) , Entry :: Vacant (_entry) => panic ! () , } match map . entry (& NullCtx , 1) { Entry :: Occupied (_entry) => panic ! () , Entry :: Vacant (entry) => entry . insert (4) , } match map . entry (& NullCtx , 1) { Entry :: Occupied (entry) => assert ! (* entry . get () == 4) , Entry :: Vacant (_entry) => panic ! () , } match map . entry (& NullCtx , 2) { Entry :: Occupied (entry) => assert ! (* entry . get () == 8) , Entry :: Vacant (_entry) => panic ! () , } map . decrement_depth () ; map . increment_depth () ; map . increment_depth () ; map . increment_depth () ; match map . entry (& NullCtx , 2) { Entry :: Occupied (entry) => assert ! (* entry . get () == 8) , Entry :: Vacant (_entry) => panic ! () , } match map . entry (& NullCtx , 1) { Entry :: Occupied (_entry) => panic ! () , Entry :: Vacant (entry) => entry . insert (5) , } match map . entry (& NullCtx , 1) { Entry :: Occupied (entry) => assert ! (* entry . get () == 5) , Entry :: Vacant (_entry) => panic ! () , } match map . entry (& NullCtx , 2) { Entry :: Occupied (entry) => assert ! (* entry . get () == 8) , Entry :: Vacant (_entry) => panic ! () , } map . decrement_depth () ; map . decrement_depth () ; map . decrement_depth () ; match map . entry (& NullCtx , 2) { Entry :: Occupied (entry) => assert ! (* entry . get () == 8) , Entry :: Vacant (_entry) => panic ! () , } match map . entry (& NullCtx , 1) { Entry :: Occupied (_entry) => panic ! () , Entry :: Vacant (entry) => entry . insert (3) , } } # [test] fn insert_arbitrary_depth () { let mut map : ScopedHashMap < i32 , i32 > = ScopedHashMap :: new () ; map . insert_if_absent (& NullCtx , 1 , 2) ; assert_eq ! (map . get (& NullCtx , & 1) , Some (& 2)) ; map . increment_depth () ; assert_eq ! (map . get (& NullCtx , & 1) , Some (& 2)) ; map . insert_if_absent (& NullCtx , 3 , 4) ; assert_eq ! (map . get (& NullCtx , & 3) , Some (& 4)) ; map . decrement_depth () ; assert_eq ! (map . get (& NullCtx , & 3) , None) ; map . increment_depth () ; map . insert_if_absent_with_depth (& NullCtx , 3 , 4 , 0) ; assert_eq ! (map . get (& NullCtx , & 3) , Some (& 4)) ; map . decrement_depth () ; assert_eq ! (map . get (& NullCtx , & 3) , Some (& 4)) ; } }
};
}
