// Generated macro for UnwindInst (enum)
macro_rules! Depcrate_isa_unwindUnwindInst {
() => {
// Module: crate::isa::unwind
// Provides: {"UnwindInst"}
// Dependencies: {}
# [doc = " Unwind pseudoinstruction used in VCode backends: represents that"] # [doc = " at the present location, an action has just been taken."] # [doc = ""] # [doc = " VCode backends always emit unwind info that is relative to a frame"] # [doc = " pointer, because we are planning to allow for dynamic frame allocation,"] # [doc = " and because it makes the design quite a lot simpler in general: we don't"] # [doc = " have to be precise about SP adjustments throughout the body of the function."] # [doc = ""] # [doc = " We include only unwind info for prologues at this time. Note that unwind"] # [doc = " info for epilogues is only necessary if one expects to unwind while within"] # [doc = " the last few instructions of the function (after FP has been restored) or"] # [doc = " if one wishes to instruction-step through the epilogue and see a backtrace"] # [doc = " at every point. This is not necessary for correct operation otherwise and so"] # [doc = " we simplify the world a bit by omitting epilogue information. (Note that"] # [doc = " some platforms also don't require or have a way to describe unwind"] # [doc = " information for epilogues at all: for example, on Windows, the `UNWIND_INFO`"] # [doc = " format only stores information for the function prologue.)"] # [doc = ""] # [doc = " Because we are defining an abstraction over multiple unwind formats (at"] # [doc = " least Windows/fastcall and System V) and multiple architectures (at least"] # [doc = " x86-64 and aarch64), we have to be a little bit flexible in how we describe"] # [doc = " the frame. However, it turns out that a least-common-denominator prologue"] # [doc = " works for all of the cases we have to worry about today!"] # [doc = ""] # [doc = " We assume the stack looks something like this:"] # [doc = ""] # [doc = ""] # [doc = " ```plain"] # [doc = "                  +----------------------------------------------+"] # [doc = "                  | stack arg area, etc (according to ABI)       |"] # [doc = "                  | ...                                          |"] # [doc = "   SP at call --> +----------------------------------------------+"] # [doc = "                  | return address (pushed by HW or SW)          |"] # [doc = "                  +----------------------------------------------+"] # [doc = "                  | old frame pointer (FP)                       |"] # [doc = "   FP in this --> +----------------------------------------------+"] # [doc = "   function       | clobbered callee-save registers              |"] # [doc = "                  | ...                                          |"] # [doc = "   start of   --> +----------------------------------------------+"] # [doc = "   clobbers       | (rest of function's frame, irrelevant here)  |"] # [doc = "                  | ...                                          |"] # [doc = "   SP in this --> +----------------------------------------------+"] # [doc = "   function"] # [doc = " ```"] # [doc = ""] # [doc = " We assume that the prologue consists of:"] # [doc = ""] # [doc = " * `PushFrameRegs`: A push operation that adds the old FP to the stack (and"] # [doc = "    maybe the link register, on architectures that do not push return addresses"] # [doc = "    in hardware)"] # [doc = " * `DefineFrame`: An update that sets FP to SP to establish a new frame"] # [doc = " * `SaveReg`: A number of stores or pushes to the stack to save clobbered registers"] # [doc = ""] # [doc = " Each of these steps has a corresponding pseudo-instruction. At each step,"] # [doc = " we need some information to determine where the current stack frame is"] # [doc = " relative to SP or FP. When the `PushFrameRegs` occurs, we need to know how"] # [doc = " much SP was decremented by, so we can allow the unwinder to continue to find"] # [doc = " the caller's frame. When we define the new frame, we need to know where FP"] # [doc = " is in relation to \"SP at call\" and also \"start of clobbers\", because"] # [doc = " different unwind formats define one or the other of those as the anchor by"] # [doc = " which we define the frame. Finally, when registers are saved, we need to"] # [doc = " know which ones, and where."] # [doc = ""] # [doc = " Different unwind formats work differently; here is a whirlwind tour of how"] # [doc = " they define frames to help understanding:"] # [doc = ""] # [doc = " - Windows unwind information defines a frame that must start below the"] # [doc = "   clobber area, because all clobber-save offsets are non-negative. We set it"] # [doc = "   at the \"start of clobbers\" in the figure above. The `UNWIND_INFO` contains"] # [doc = "   a \"frame pointer offset\" field; when we define the new frame, the frame is"] # [doc = "   understood to be the value of FP (`RBP`) *minus* this offset. In other"] # [doc = "   words, the FP is *at the frame pointer offset* relative to the"] # [doc = "   start-of-clobber-frame. We use the \"FP offset down to clobber area\" offset"] # [doc = "   to generate this info."] # [doc = ""] # [doc = " - System V unwind information defines a frame in terms of the CFA"] # [doc = "   (call-frame address), which is equal to the \"SP at call\" above. SysV"] # [doc = "   allows negative offsets, so there is no issue defining clobber-save"] # [doc = "   locations in terms of CFA. The format allows us to define CFA flexibly in"] # [doc = "   terms of any register plus an offset; we define it in terms of FP plus"] # [doc = "   the clobber-to-caller-SP offset once FP is established."] # [doc = ""] # [doc = " Note that certain architectures impose limits on offsets: for example, on"] # [doc = " Windows, the base of the clobber area must not be more than 240 bytes below"] # [doc = " FP."] # [doc = ""] # [doc = " Unwind pseudoinstructions are emitted inline by ABI code as it generates"] # [doc = " a prologue. Thus, for the usual case, a prologue might look like (using x64"] # [doc = " as an example):"] # [doc = ""] # [doc = " ```plain"] # [doc = " push rbp"] # [doc = " unwind UnwindInst::PushFrameRegs { offset_upward_to_caller_sp: 16 }"] # [doc = " mov rbp, rsp"] # [doc = " unwind UnwindInst::DefineNewFrame { offset_upward_to_caller_sp: 16,"] # [doc = "                                     offset_downward_to_clobbers: 16 }"] # [doc = " sub rsp, 32"] # [doc = " mov [rsp+16], r12"] # [doc = " unwind UnwindInst::SaveReg { reg: R12, clobber_offset: 0 }"] # [doc = " mov [rsp+24], r13"] # [doc = " unwind UnwindInst::SaveReg { reg: R13, clobber_offset: 8 }"] # [doc = " ..."] # [doc = " ```"] # [derive (Clone , Debug , PartialEq , Eq)] # [cfg_attr (feature = "enable-serde" , derive (Serialize , Deserialize))] pub enum UnwindInst { # [doc = " The frame-pointer register for this architecture has just been pushed to"] # [doc = " the stack (and on architectures where return-addresses are not pushed by"] # [doc = " hardware, the link register as well). The FP has not been set to this"] # [doc = " frame yet. The current location of SP is such that"] # [doc = " `offset_upward_to_caller_sp` is the distance to SP-at-callsite (our"] # [doc = " caller's frame)."] PushFrameRegs { # [doc = " The offset from the current SP (after push) to the SP at"] # [doc = " caller's callsite."] offset_upward_to_caller_sp : u32 , } , # [doc = " The frame-pointer register for this architecture has just been"] # [doc = " set to the current stack location. We wish to define a new"] # [doc = " frame that is anchored on this new FP value. Offsets are provided"] # [doc = " upward to the caller's stack frame and downward toward the clobber"] # [doc = " area. We expect this pseudo-op to come after `PushFrameRegs`."] DefineNewFrame { # [doc = " The offset from the current SP and FP value upward to the value of"] # [doc = " SP at the callsite that invoked us."] offset_upward_to_caller_sp : u32 , # [doc = " The offset from the current SP and FP value downward to the start of"] # [doc = " the clobber area."] offset_downward_to_clobbers : u32 , } , # [doc = " The stack pointer was adjusted to allocate the stack."] StackAlloc { # [doc = " Size to allocate."] size : u32 , } , # [doc = " The stack slot at the given offset from the clobber-area base has been"] # [doc = " used to save the given register."] # [doc = ""] # [doc = " Given that `CreateFrame` has occurred first with some"] # [doc = " `offset_downward_to_clobbers`, `SaveReg` with `clobber_offset` indicates"] # [doc = " that the value of `reg` is saved on the stack at address `FP -"] # [doc = " offset_downward_to_clobbers + clobber_offset`."] SaveReg { # [doc = " The offset from the start of the clobber area to this register's"] # [doc = " stack location."] clobber_offset : u32 , # [doc = " The saved register."] reg : RealReg , } , # [doc = " Computes the value of the given register in the caller as stack offset."] # [doc = " Typically used to unwind the stack pointer if the default rule does not apply."] # [doc = " The `clobber_offset` is computed the same way as for the `SaveReg` rule."] RegStackOffset { # [doc = " The offset from the start of the clobber area to this register's value."] clobber_offset : u32 , # [doc = " The register whose value is to be set."] reg : RealReg , } , # [doc = " Defines if the aarch64-specific pointer authentication available for ARM v8.3+ devices"] # [doc = " is enabled for certain pointers or not."] Aarch64SetPointerAuth { # [doc = " Whether return addresses (hold in LR) contain a pointer-authentication code."] return_addresses : bool , } , }
};
}
