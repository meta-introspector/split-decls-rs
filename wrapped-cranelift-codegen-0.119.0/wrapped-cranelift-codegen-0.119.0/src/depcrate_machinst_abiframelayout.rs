// Generated macro for FrameLayout (struct)
macro_rules! Depcrate_machinst_abiFrameLayout {
() => {
// Module: crate::machinst::abi
// Provides: {"FrameLayout"}
// Dependencies: {}
# [doc = " Structure describing the layout of a function's stack frame."] # [derive (Clone , Debug , Default)] pub struct FrameLayout { # [doc = " N.B. The areas whose sizes are given in this structure fully"] # [doc = " cover the current function's stack frame, from high to low"] # [doc = " stack addresses in the sequence below.  Each size contains"] # [doc = " any alignment padding that may be required by the ABI."] # [doc = " Size of incoming arguments on the stack.  This is not technically"] # [doc = " part of this function's frame, but code in the function will still"] # [doc = " need to access it.  Depending on the ABI, we may need to set up a"] # [doc = " frame pointer to do so; we also may need to pop this area from the"] # [doc = " stack upon return."] pub incoming_args_size : u32 , # [doc = " The size of the incoming argument area, taking into account any"] # [doc = " potential increase in size required for tail calls present in the"] # [doc = " function. In the case that no tail calls are present, this value"] # [doc = " will be the same as [`Self::incoming_args_size`]."] pub tail_args_size : u32 , # [doc = " Size of the \"setup area\", typically holding the return address"] # [doc = " and/or the saved frame pointer.  This may be written either during"] # [doc = " the call itself (e.g. a pushed return address) or by code emitted"] # [doc = " from gen_prologue_frame_setup.  In any case, after that code has"] # [doc = " completed execution, the stack pointer is expected to point to the"] # [doc = " bottom of this area.  The same holds at the start of code emitted"] # [doc = " by gen_epilogue_frame_restore."] pub setup_area_size : u32 , # [doc = " Size of the area used to save callee-saved clobbered registers."] # [doc = " This area is accessed by code emitted from gen_clobber_save and"] # [doc = " gen_clobber_restore."] pub clobber_size : u32 , # [doc = " Storage allocated for the fixed part of the stack frame."] # [doc = " This contains stack slots and spill slots."] pub fixed_frame_storage_size : u32 , # [doc = " Stack size to be reserved for outgoing arguments, if used by"] # [doc = " the current ABI, or 0 otherwise.  After gen_clobber_save and"] # [doc = " before gen_clobber_restore, the stack pointer points to the"] # [doc = " bottom of this area."] pub outgoing_args_size : u32 , # [doc = " Sorted list of callee-saved registers that are clobbered"] # [doc = " according to the ABI.  These registers will be saved and"] # [doc = " restored by gen_clobber_save and gen_clobber_restore."] pub clobbered_callee_saves : Vec < Writable < RealReg > > , }
};
}
