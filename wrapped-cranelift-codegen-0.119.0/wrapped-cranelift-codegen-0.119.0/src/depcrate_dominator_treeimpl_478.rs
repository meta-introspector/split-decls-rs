// Generated macro for impl_478 (impl)
macro_rules! Depcrate_dominator_treeimpl_478 {
() => {
// Module: crate::dominator_tree
// Provides: {"impl_478"}
// Dependencies: {}
# [doc = " Methods for querying the dominator tree."] impl DominatorTree { # [doc = " Is `block` reachable from the entry block?"] pub fn is_reachable (& self , block : Block) -> bool { self . nodes [block] . pre_number != NOT_VISITED } # [doc = " Get the CFG post-order of blocks that was used to compute the dominator tree."] # [doc = ""] # [doc = " Note that this post-order is not updated automatically when the CFG is modified. It is"] # [doc = " computed from scratch and cached by `compute()`."] pub fn cfg_postorder (& self) -> & [Block] { debug_assert ! (self . is_valid ()) ; & self . postorder } # [doc = " Get an iterator over CFG reverse post-order of blocks used to compute the dominator tree."] # [doc = ""] # [doc = " Note that the post-order is not updated automatically when the CFG is modified. It is"] # [doc = " computed from scratch and cached by `compute()`."] pub fn cfg_rpo (& self) -> impl Iterator < Item = & Block > { debug_assert ! (self . is_valid ()) ; self . postorder . iter () . rev () } # [doc = " Returns the immediate dominator of `block`."] # [doc = ""] # [doc = " `block_a` is said to *dominate* `block_b` if all control flow paths from the function"] # [doc = " entry to `block_b` must go through `block_a`."] # [doc = ""] # [doc = " The *immediate dominator* is the dominator that is closest to `block`. All other dominators"] # [doc = " also dominate the immediate dominator."] # [doc = ""] # [doc = " This returns `None` if `block` is not reachable from the entry block, or if it is the entry block"] # [doc = " which has no dominators."] pub fn idom (& self , block : Block) -> Option < Block > { self . nodes [block] . idom . into () } # [doc = " Returns `true` if `a` dominates `b`."] # [doc = ""] # [doc = " This means that every control-flow path from the function entry to `b` must go through `a`."] # [doc = ""] # [doc = " Dominance is ill defined for unreachable blocks. This function can always determine"] # [doc = " dominance for instructions in the same block, but otherwise returns `false` if either block"] # [doc = " is unreachable."] # [doc = ""] # [doc = " An instruction is considered to dominate itself."] # [doc = " A block is also considered to dominate itself."] pub fn dominates < A , B > (& self , a : A , b : B , layout : & Layout) -> bool where A : Into < ProgramPoint > , B : Into < ProgramPoint > , { let a = a . into () ; let b = b . into () ; match a { ProgramPoint :: Block (block_a) => match b { ProgramPoint :: Block (block_b) => self . block_dominates (block_a , block_b) , ProgramPoint :: Inst (inst_b) => { let block_b = layout . inst_block (inst_b) . expect ("Instruction not in layout.") ; self . block_dominates (block_a , block_b) } } , ProgramPoint :: Inst (inst_a) => { let block_a : Block = layout . inst_block (inst_a) . expect ("Instruction not in layout.") ; match b { ProgramPoint :: Block (block_b) => { block_a != block_b && self . block_dominates (block_a , block_b) } ProgramPoint :: Inst (inst_b) => { let block_b = layout . inst_block (inst_b) . expect ("Instruction not in layout.") ; if block_a == block_b { layout . pp_cmp (a , b) != Ordering :: Greater } else { self . block_dominates (block_a , block_b) } } } } } } # [doc = " Returns `true` if `block_a` dominates `block_b`."] # [doc = ""] # [doc = " A block is considered to dominate itself."] fn block_dominates (& self , block_a : Block , mut block_b : Block) -> bool { let pre_a = self . nodes [block_a] . pre_number ; while pre_a < self . nodes [block_b] . pre_number { let idom = match self . idom (block_b) { Some (idom) => idom , None => return false , } ; block_b = idom ; } block_a == block_b } }
};
}
