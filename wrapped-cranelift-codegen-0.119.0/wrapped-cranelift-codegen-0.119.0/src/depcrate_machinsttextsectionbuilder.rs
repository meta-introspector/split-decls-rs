// Generated macro for TextSectionBuilder (trait)
macro_rules! Depcrate_machinstTextSectionBuilder {
() => {
// Module: crate::machinst
// Provides: {"TextSectionBuilder"}
// Dependencies: {}
# [doc = " An object that can be used to create the text section of an executable."] # [doc = ""] # [doc = " This primarily handles resolving relative relocations at"] # [doc = " text-section-assembly time rather than at load/link time. This"] # [doc = " architecture-specific logic is sort of like a linker, but only for one"] # [doc = " object file at a time."] pub trait TextSectionBuilder { # [doc = " Appends `data` to the text section with the `align` specified."] # [doc = ""] # [doc = " If `labeled` is `true` then this also binds the appended data to the"] # [doc = " `n`th label for how many times this has been called with `labeled:"] # [doc = " true`. The label target can be passed as the `target` argument to"] # [doc = " `resolve_reloc`."] # [doc = ""] # [doc = " This function returns the offset at which the data was placed in the"] # [doc = " text section."] fn append (& mut self , labeled : bool , data : & [u8] , align : u32 , ctrl_plane : & mut ControlPlane ,) -> u64 ; # [doc = " Attempts to resolve a relocation for this function."] # [doc = ""] # [doc = " The `offset` is the offset of the relocation, within the text section."] # [doc = " The `reloc` is the kind of relocation."] # [doc = " The `addend` is the value to add to the relocation."] # [doc = " The `target` is the labeled function that is the target of this"] # [doc = " relocation."] # [doc = ""] # [doc = " Labeled functions are created with the `append` function above by"] # [doc = " setting the `labeled` parameter to `true`."] # [doc = ""] # [doc = " If this builder does not know how to handle `reloc` then this function"] # [doc = " will return `false`. Otherwise this function will return `true` and this"] # [doc = " relocation will be resolved in the final bytes returned by `finish`."] fn resolve_reloc (& mut self , offset : u64 , reloc : Reloc , addend : Addend , target : usize) -> bool ; # [doc = " A debug-only option which is used to for"] fn force_veneers (& mut self) ; # [doc = " Write the `data` provided at `offset`, for example when resolving a"] # [doc = " relocation."] fn write (& mut self , offset : u64 , data : & [u8]) ; # [doc = " Completes this text section, filling out any final details, and returns"] # [doc = " the bytes of the text section."] fn finish (& mut self , ctrl_plane : & mut ControlPlane) -> Vec < u8 > ; }
};
}
