// Generated macro for MachBuffer (struct)
macro_rules! Depcrate_machinst_bufferMachBuffer {
() => {
// Module: crate::machinst::buffer
// Provides: {"MachBuffer"}
// Dependencies: {}
# [doc = " A buffer of output to be produced, fixed up, and then emitted to a CodeSink"] # [doc = " in bulk."] # [doc = ""] # [doc = " This struct uses `SmallVec`s to support small-ish function bodies without"] # [doc = " any heap allocation. As such, it will be several kilobytes large. This is"] # [doc = " likely fine as long as it is stack-allocated for function emission then"] # [doc = " thrown away; but beware if many buffer objects are retained persistently."] pub struct MachBuffer < I : VCodeInst > { # [doc = " The buffer contents, as raw bytes."] data : SmallVec < [u8 ; 1024] > , # [doc = " The required alignment of this buffer."] min_alignment : u32 , # [doc = " Any relocations referring to this code. Note that only *external*"] # [doc = " relocations are tracked here; references to labels within the buffer are"] # [doc = " resolved before emission."] relocs : SmallVec < [MachReloc ; 16] > , # [doc = " Any trap records referring to this code."] traps : SmallVec < [MachTrap ; 16] > , # [doc = " Any call site records referring to this code."] call_sites : SmallVec < [MachCallSite ; 16] > , # [doc = " Any source location mappings referring to this code."] srclocs : SmallVec < [MachSrcLoc < Stencil > ; 64] > , # [doc = " Any user stack maps for this code."] # [doc = ""] # [doc = " Each entry is an `(offset, span, stack_map)` triple. Entries are sorted"] # [doc = " by code offset, and each stack map covers `span` bytes on the stack."] user_stack_maps : SmallVec < [(CodeOffset , u32 , ir :: UserStackMap) ; 8] > , # [doc = " Any unwind info at a given location."] unwind_info : SmallVec < [(CodeOffset , UnwindInst) ; 8] > , # [doc = " The current source location in progress (after `start_srcloc()` and"] # [doc = " before `end_srcloc()`).  This is a (start_offset, src_loc) tuple."] cur_srcloc : Option < (CodeOffset , RelSourceLoc) > , # [doc = " Known label offsets; `UNKNOWN_LABEL_OFFSET` if unknown."] label_offsets : SmallVec < [CodeOffset ; 16] > , # [doc = " Label aliases: when one label points to an unconditional jump, and that"] # [doc = " jump points to another label, we can redirect references to the first"] # [doc = " label immediately to the second."] # [doc = ""] # [doc = " Invariant: we don't have label-alias cycles. We ensure this by,"] # [doc = " before setting label A to alias label B, resolving B's alias"] # [doc = " target (iteratively until a non-aliased label); if B is already"] # [doc = " aliased to A, then we cannot alias A back to B."] label_aliases : SmallVec < [MachLabel ; 16] > , # [doc = " Constants that must be emitted at some point."] pending_constants : SmallVec < [VCodeConstant ; 16] > , # [doc = " Byte size of all constants in `pending_constants`."] pending_constants_size : CodeOffset , # [doc = " Traps that must be emitted at some point."] pending_traps : SmallVec < [MachLabelTrap ; 16] > , # [doc = " Fixups that haven't yet been flushed into `fixup_records` below and may"] # [doc = " be related to branches that are chomped. These all get added to"] # [doc = " `fixup_records` during island emission."] pending_fixup_records : SmallVec < [MachLabelFixup < I > ; 16] > , # [doc = " The nearest upcoming deadline for entries in `pending_fixup_records`."] pending_fixup_deadline : CodeOffset , # [doc = " Fixups that must be performed after all code is emitted."] fixup_records : BinaryHeap < MachLabelFixup < I > > , # [doc = " Latest branches, to facilitate in-place editing for better fallthrough"] # [doc = " behavior and empty-block removal."] latest_branches : SmallVec < [MachBranch ; 4] > , # [doc = " All labels at the current offset (emission tail). This is lazily"] # [doc = " cleared: it is actually accurate as long as the current offset is"] # [doc = " `labels_at_tail_off`, but if `cur_offset()` has grown larger, it should"] # [doc = " be considered as empty."] # [doc = ""] # [doc = " For correctness, this *must* be complete (i.e., the vector must contain"] # [doc = " all labels whose offsets are resolved to the current tail), because we"] # [doc = " rely on it to update labels when we truncate branches."] labels_at_tail : SmallVec < [MachLabel ; 4] > , # [doc = " The last offset at which `labels_at_tail` is valid. It is conceptually"] # [doc = " always describing the tail of the buffer, but we do not clear"] # [doc = " `labels_at_tail` eagerly when the tail grows, rather we lazily clear it"] # [doc = " when the offset has grown past this (`labels_at_tail_off`) point."] # [doc = " Always <= `cur_offset()`."] labels_at_tail_off : CodeOffset , # [doc = " Metadata about all constants that this function has access to."] # [doc = ""] # [doc = " This records the size/alignment of all constants (not the actual data)"] # [doc = " along with the last available label generated for the constant. This map"] # [doc = " is consulted when constants are referred to and the label assigned to a"] # [doc = " constant may change over time as well."] constants : PrimaryMap < VCodeConstant , MachBufferConstant > , # [doc = " All recorded usages of constants as pairs of the constant and where the"] # [doc = " constant needs to be placed within `self.data`. Note that the same"] # [doc = " constant may appear in this array multiple times if it was emitted"] # [doc = " multiple times."] used_constants : SmallVec < [(VCodeConstant , CodeOffset) ; 4] > , # [doc = " Indicates when a patchable region is currently open, to guard that it's"] # [doc = " not possible to nest patchable regions."] open_patchable : bool , }
};
}
