// Generated macro for EgraphPass (struct)
macro_rules! Depcrate_egraphEgraphPass {
() => {
// Module: crate::egraph
// Provides: {"EgraphPass"}
// Dependencies: {}
# [doc = " Pass over a Function that does the whole aegraph thing."] # [doc = ""] # [doc = " - Removes non-skeleton nodes from the Layout."] # [doc = " - Performs a GVN-and-rule-application pass over all Values"] # [doc = "   reachable from the skeleton, potentially creating new Union"] # [doc = "   nodes (i.e., an aegraph) so that some values have multiple"] # [doc = "   representations."] # [doc = " - Does \"extraction\" on the aegraph: selects the best value out of"] # [doc = "   the tree-of-Union nodes for each used value."] # [doc = " - Does \"scoped elaboration\" on the aegraph: chooses one or more"] # [doc = "   locations for pure nodes to become instructions again in the"] # [doc = "   layout, as forced by the skeleton."] # [doc = ""] # [doc = " At the beginning and end of this pass, the CLIF should be in a"] # [doc = " state that passes the verifier and, additionally, has no Union"] # [doc = " nodes. During the pass, Union nodes may exist, and instructions in"] # [doc = " the layout may refer to results of instructions that are not"] # [doc = " placed in the layout."] pub struct EgraphPass < 'a > { # [doc = " The function we're operating on."] func : & 'a mut Function , # [doc = " Dominator tree for the CFG, used to visit blocks in pre-order"] # [doc = " so we see value definitions before their uses, and also used for"] # [doc = " O(1) dominance checks."] domtree : DominatorTreePreorder , # [doc = " Alias analysis, used during optimization."] alias_analysis : & 'a mut AliasAnalysis < 'a > , # [doc = " Loop analysis results, used for built-in LICM during"] # [doc = " elaboration."] loop_analysis : & 'a LoopAnalysis , # [doc = " Compiler flags."] flags : & 'a Flags , # [doc = " Chaos-mode control-plane so we can test that we still get"] # [doc = " correct results when our heuristics make bad decisions."] ctrl_plane : & 'a mut ControlPlane , # [doc = " Which Values do we want to rematerialize in each block where"] # [doc = " they're used?"] remat_values : FxHashSet < Value > , # [doc = " Stats collected while we run this pass."] pub (crate) stats : Stats , }
};
}
