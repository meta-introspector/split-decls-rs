// Generated macro for impl_92 (impl)
macro_rules! Depcrate_mapimpl_92 {
() => {
// Module: crate::map
// Provides: {"impl_92"}
// Dependencies: {}
impl < K , V , S > IndexMap < K , V , S > where S : BuildHasher , { pub (crate) fn hash < Q : ? Sized + Hash > (& self , key : & Q) -> HashValue { let h = self . hash_builder . hash_one (key) ; HashValue (h as usize) } # [doc = " Return `true` if an equivalent to `key` exists in the map."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn contains_key < Q > (& self , key : & Q) -> bool where Q : ? Sized + Hash + Equivalent < K > , { self . get_index_of (key) . is_some () } # [doc = " Return a reference to the stored value for `key`, if it is present,"] # [doc = " else `None`."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn get < Q > (& self , key : & Q) -> Option < & V > where Q : ? Sized + Hash + Equivalent < K > , { if let Some (i) = self . get_index_of (key) { let entry = & self . as_entries () [i] ; Some (& entry . value) } else { None } } # [doc = " Return references to the stored key-value pair for the lookup `key`,"] # [doc = " if it is present, else `None`."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn get_key_value < Q > (& self , key : & Q) -> Option < (& K , & V) > where Q : ? Sized + Hash + Equivalent < K > , { if let Some (i) = self . get_index_of (key) { let entry = & self . as_entries () [i] ; Some ((& entry . key , & entry . value)) } else { None } } # [doc = " Return the index with references to the stored key-value pair for the"] # [doc = " lookup `key`, if it is present, else `None`."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn get_full < Q > (& self , key : & Q) -> Option < (usize , & K , & V) > where Q : ? Sized + Hash + Equivalent < K > , { if let Some (i) = self . get_index_of (key) { let entry = & self . as_entries () [i] ; Some ((i , & entry . key , & entry . value)) } else { None } } # [doc = " Return the item index for `key`, if it is present, else `None`."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn get_index_of < Q > (& self , key : & Q) -> Option < usize > where Q : ? Sized + Hash + Equivalent < K > , { match self . as_entries () { [] => None , [x] => key . equivalent (& x . key) . then_some (0) , _ => { let hash = self . hash (key) ; self . core . get_index_of (hash , key) } } } # [doc = " Return a mutable reference to the stored value for `key`,"] # [doc = " if it is present, else `None`."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn get_mut < Q > (& mut self , key : & Q) -> Option < & mut V > where Q : ? Sized + Hash + Equivalent < K > , { if let Some (i) = self . get_index_of (key) { let entry = & mut self . as_entries_mut () [i] ; Some (& mut entry . value) } else { None } } # [doc = " Return a reference and mutable references to the stored key-value pair"] # [doc = " for the lookup `key`, if it is present, else `None`."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn get_key_value_mut < Q > (& mut self , key : & Q) -> Option < (& K , & mut V) > where Q : ? Sized + Hash + Equivalent < K > , { if let Some (i) = self . get_index_of (key) { let entry = & mut self . as_entries_mut () [i] ; Some ((& entry . key , & mut entry . value)) } else { None } } # [doc = " Return the index with a reference and mutable reference to the stored"] # [doc = " key-value pair for the lookup `key`, if it is present, else `None`."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn get_full_mut < Q > (& mut self , key : & Q) -> Option < (usize , & K , & mut V) > where Q : ? Sized + Hash + Equivalent < K > , { if let Some (i) = self . get_index_of (key) { let entry = & mut self . as_entries_mut () [i] ; Some ((i , & entry . key , & mut entry . value)) } else { None } } # [doc = " Return the values for `N` keys. If any key is duplicated, this function will panic."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut map = indexmap::IndexMap::from([(1, 'a'), (3, 'b'), (2, 'c')]);"] # [doc = " assert_eq!(map.get_disjoint_mut([&2, &1]), [Some(&mut 'c'), Some(&mut 'a')]);"] # [doc = " ```"] pub fn get_disjoint_mut < Q , const N : usize > (& mut self , keys : [& Q ; N]) -> [Option < & mut V > ; N] where Q : ? Sized + Hash + Equivalent < K > , { let indices = keys . map (| key | self . get_index_of (key)) ; match self . as_mut_slice () . get_disjoint_opt_mut (indices) { Err (GetDisjointMutError :: IndexOutOfBounds) => { unreachable ! ("Internal error: indices should never be OOB as we got them from get_index_of") ; } Err (GetDisjointMutError :: OverlappingIndices) => { panic ! ("duplicate keys found") ; } Ok (key_values) => key_values . map (| kv_opt | kv_opt . map (| kv | kv . 1)) , } } # [doc = " Remove the key-value pair equivalent to `key` and return"] # [doc = " its value."] # [doc = ""] # [doc = " **NOTE:** This is equivalent to [`.swap_remove(key)`][Self::swap_remove], replacing this"] # [doc = " entry's position with the last element, and it is deprecated in favor of calling that"] # [doc = " explicitly. If you need to preserve the relative order of the keys in the map, use"] # [doc = " [`.shift_remove(key)`][Self::shift_remove] instead."] # [deprecated (note = "`remove` disrupts the map order -- \
        use `swap_remove` or `shift_remove` for explicit behavior.")] pub fn remove < Q > (& mut self , key : & Q) -> Option < V > where Q : ? Sized + Hash + Equivalent < K > , { self . swap_remove (key) } # [doc = " Remove and return the key-value pair equivalent to `key`."] # [doc = ""] # [doc = " **NOTE:** This is equivalent to [`.swap_remove_entry(key)`][Self::swap_remove_entry],"] # [doc = " replacing this entry's position with the last element, and it is deprecated in favor of"] # [doc = " calling that explicitly. If you need to preserve the relative order of the keys in the map,"] # [doc = " use [`.shift_remove_entry(key)`][Self::shift_remove_entry] instead."] # [deprecated (note = "`remove_entry` disrupts the map order -- \
        use `swap_remove_entry` or `shift_remove_entry` for explicit behavior.")] pub fn remove_entry < Q > (& mut self , key : & Q) -> Option < (K , V) > where Q : ? Sized + Hash + Equivalent < K > , { self . swap_remove_entry (key) } # [doc = " Remove the key-value pair equivalent to `key` and return"] # [doc = " its value."] # [doc = ""] # [doc = " Like [`Vec::swap_remove`], the pair is removed by swapping it with the"] # [doc = " last element of the map and popping it off. **This perturbs"] # [doc = " the position of what used to be the last element!**"] # [doc = ""] # [doc = " Return `None` if `key` is not in map."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn swap_remove < Q > (& mut self , key : & Q) -> Option < V > where Q : ? Sized + Hash + Equivalent < K > , { self . swap_remove_full (key) . map (third) } # [doc = " Remove and return the key-value pair equivalent to `key`."] # [doc = ""] # [doc = " Like [`Vec::swap_remove`], the pair is removed by swapping it with the"] # [doc = " last element of the map and popping it off. **This perturbs"] # [doc = " the position of what used to be the last element!**"] # [doc = ""] # [doc = " Return `None` if `key` is not in map."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn swap_remove_entry < Q > (& mut self , key : & Q) -> Option < (K , V) > where Q : ? Sized + Hash + Equivalent < K > , { match self . swap_remove_full (key) { Some ((_ , key , value)) => Some ((key , value)) , None => None , } } # [doc = " Remove the key-value pair equivalent to `key` and return it and"] # [doc = " the index it had."] # [doc = ""] # [doc = " Like [`Vec::swap_remove`], the pair is removed by swapping it with the"] # [doc = " last element of the map and popping it off. **This perturbs"] # [doc = " the position of what used to be the last element!**"] # [doc = ""] # [doc = " Return `None` if `key` is not in map."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn swap_remove_full < Q > (& mut self , key : & Q) -> Option < (usize , K , V) > where Q : ? Sized + Hash + Equivalent < K > , { match self . as_entries () { [x] if key . equivalent (& x . key) => { let (k , v) = self . core . pop () ? ; Some ((0 , k , v)) } [_] | [] => None , _ => { let hash = self . hash (key) ; self . core . swap_remove_full (hash , key) } } } # [doc = " Remove the key-value pair equivalent to `key` and return"] # [doc = " its value."] # [doc = ""] # [doc = " Like [`Vec::remove`], the pair is removed by shifting all of the"] # [doc = " elements that follow it, preserving their relative order."] # [doc = " **This perturbs the index of all of those elements!**"] # [doc = ""] # [doc = " Return `None` if `key` is not in map."] # [doc = ""] # [doc = " Computes in **O(n)** time (average)."] pub fn shift_remove < Q > (& mut self , key : & Q) -> Option < V > where Q : ? Sized + Hash + Equivalent < K > , { self . shift_remove_full (key) . map (third) } # [doc = " Remove and return the key-value pair equivalent to `key`."] # [doc = ""] # [doc = " Like [`Vec::remove`], the pair is removed by shifting all of the"] # [doc = " elements that follow it, preserving their relative order."] # [doc = " **This perturbs the index of all of those elements!**"] # [doc = ""] # [doc = " Return `None` if `key` is not in map."] # [doc = ""] # [doc = " Computes in **O(n)** time (average)."] pub fn shift_remove_entry < Q > (& mut self , key : & Q) -> Option < (K , V) > where Q : ? Sized + Hash + Equivalent < K > , { match self . shift_remove_full (key) { Some ((_ , key , value)) => Some ((key , value)) , None => None , } } # [doc = " Remove the key-value pair equivalent to `key` and return it and"] # [doc = " the index it had."] # [doc = ""] # [doc = " Like [`Vec::remove`], the pair is removed by shifting all of the"] # [doc = " elements that follow it, preserving their relative order."] # [doc = " **This perturbs the index of all of those elements!**"] # [doc = ""] # [doc = " Return `None` if `key` is not in map."] # [doc = ""] # [doc = " Computes in **O(n)** time (average)."] pub fn shift_remove_full < Q > (& mut self , key : & Q) -> Option < (usize , K , V) > where Q : ? Sized + Hash + Equivalent < K > , { match self . as_entries () { [x] if key . equivalent (& x . key) => { let (k , v) = self . core . pop () ? ; Some ((0 , k , v)) } [_] | [] => None , _ => { let hash = self . hash (key) ; self . core . shift_remove_full (hash , key) } } } }
};
}
