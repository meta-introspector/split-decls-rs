macro_rules! deps {
    () => {
        TryReserveError!();
        Bucket!();
        IndexMap!();
    };
}

macro_rules! impl_52 {
    () => {
        deps!();
        impl < K , V , S > IndexMap < K , V , S > { # [doc = " Create a new map with capacity for `n` key-value pairs. (Does not"] # [doc = " allocate if `n` is zero.)"] # [doc = ""] # [doc = " Computes in **O(n)** time."] # [inline] pub fn with_capacity_and_hasher (n : usize , hash_builder : S) -> Self { if n == 0 { Self :: with_hasher (hash_builder) } else { IndexMap { core : IndexMapCore :: with_capacity (n) , hash_builder , } } } # [doc = " Create a new map with `hash_builder`."] # [doc = ""] # [doc = " This function is `const`, so it"] # [doc = " can be called in `static` contexts."] pub const fn with_hasher (hash_builder : S) -> Self { IndexMap { core : IndexMapCore :: new () , hash_builder , } } # [inline] pub (crate) fn into_entries (self) -> Vec < Bucket < K , V > > { self . core . into_entries () } # [inline] pub (crate) fn as_entries (& self) -> & [Bucket < K , V >] { self . core . as_entries () } # [inline] pub (crate) fn as_entries_mut (& mut self) -> & mut [Bucket < K , V >] { self . core . as_entries_mut () } pub (crate) fn with_entries < F > (& mut self , f : F) where F : FnOnce (& mut [Bucket < K , V >]) , { self . core . with_entries (f) ; } # [doc = " Return the number of elements the map can hold without reallocating."] # [doc = ""] # [doc = " This number is a lower bound; the map might be able to hold more,"] # [doc = " but is guaranteed to be able to hold at least this many."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn capacity (& self) -> usize { self . core . capacity () } # [doc = " Return a reference to the map's `BuildHasher`."] pub fn hasher (& self) -> & S { & self . hash_builder } # [doc = " Return the number of key-value pairs in the map."] # [doc = ""] # [doc = " Computes in **O(1)** time."] # [inline] pub fn len (& self) -> usize { self . core . len () } # [doc = " Returns true if the map contains no elements."] # [doc = ""] # [doc = " Computes in **O(1)** time."] # [inline] pub fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Return an iterator over the key-value pairs of the map, in their order"] pub fn iter (& self) -> Iter < '_ , K , V > { Iter :: new (self . as_entries ()) } # [doc = " Return an iterator over the key-value pairs of the map, in their order"] pub fn iter_mut (& mut self) -> IterMut < '_ , K , V > { IterMut :: new (self . as_entries_mut ()) } # [doc = " Return an iterator over the keys of the map, in their order"] pub fn keys (& self) -> Keys < '_ , K , V > { Keys :: new (self . as_entries ()) } # [doc = " Return an owning iterator over the keys of the map, in their order"] pub fn into_keys (self) -> IntoKeys < K , V > { IntoKeys :: new (self . into_entries ()) } # [doc = " Return an iterator over the values of the map, in their order"] pub fn values (& self) -> Values < '_ , K , V > { Values :: new (self . as_entries ()) } # [doc = " Return an iterator over mutable references to the values of the map,"] # [doc = " in their order"] pub fn values_mut (& mut self) -> ValuesMut < '_ , K , V > { ValuesMut :: new (self . as_entries_mut ()) } # [doc = " Return an owning iterator over the values of the map, in their order"] pub fn into_values (self) -> IntoValues < K , V > { IntoValues :: new (self . into_entries ()) } # [doc = " Remove all key-value pairs in the map, while preserving its capacity."] # [doc = ""] # [doc = " Computes in **O(n)** time."] pub fn clear (& mut self) { self . core . clear () ; } # [doc = " Shortens the map, keeping the first `len` elements and dropping the rest."] # [doc = ""] # [doc = " If `len` is greater than the map's current length, this has no effect."] pub fn truncate (& mut self , len : usize) { self . core . truncate (len) ; } # [doc = " Clears the `IndexMap` in the given index range, returning those"] # [doc = " key-value pairs as a drain iterator."] # [doc = ""] # [doc = " The range may be any type that implements [`RangeBounds<usize>`],"] # [doc = " including all of the `std::ops::Range*` types, or even a tuple pair of"] # [doc = " `Bound` start and end values. To drain the map entirely, use `RangeFull`"] # [doc = " like `map.drain(..)`."] # [doc = ""] # [doc = " This shifts down all entries following the drained range to fill the"] # [doc = " gap, and keeps the allocated memory for reuse."] # [doc = ""] # [doc = " ***Panics*** if the starting point is greater than the end point or if"] # [doc = " the end point is greater than the length of the map."] # [track_caller] pub fn drain < R > (& mut self , range : R) -> Drain < '_ , K , V > where R : RangeBounds < usize > , { Drain :: new (self . core . drain (range)) } # [doc = " Creates an iterator which uses a closure to determine if an element should be removed,"] # [doc = " for all elements in the given range."] # [doc = ""] # [doc = " If the closure returns true, the element is removed from the map and yielded."] # [doc = " If the closure returns false, or panics, the element remains in the map and will not be"] # [doc = " yielded."] # [doc = ""] # [doc = " Note that `extract_if` lets you mutate every value in the filter closure, regardless of"] # [doc = " whether you choose to keep or remove it."] # [doc = ""] # [doc = " The range may be any type that implements [`RangeBounds<usize>`],"] # [doc = " including all of the `std::ops::Range*` types, or even a tuple pair of"] # [doc = " `Bound` start and end values. To check the entire map, use `RangeFull`"] # [doc = " like `map.extract_if(.., predicate)`."] # [doc = ""] # [doc = " If the returned `ExtractIf` is not exhausted, e.g. because it is dropped without iterating"] # [doc = " or the iteration short-circuits, then the remaining elements will be retained."] # [doc = " Use [`retain`] with a negated predicate if you do not need the returned iterator."] # [doc = ""] # [doc = " [`retain`]: IndexMap::retain"] # [doc = ""] # [doc = " ***Panics*** if the starting point is greater than the end point or if"] # [doc = " the end point is greater than the length of the map."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Splitting a map into even and odd keys, reusing the original map:"] # [doc = ""] # [doc = " ```"] # [doc = " use indexmap::IndexMap;"] # [doc = ""] # [doc = " let mut map: IndexMap<i32, i32> = (0..8).map(|x| (x, x)).collect();"] # [doc = " let extracted: IndexMap<i32, i32> = map.extract_if(.., |k, _v| k % 2 == 0).collect();"] # [doc = ""] # [doc = " let evens = extracted.keys().copied().collect::<Vec<_>>();"] # [doc = " let odds = map.keys().copied().collect::<Vec<_>>();"] # [doc = ""] # [doc = " assert_eq!(evens, vec![0, 2, 4, 6]);"] # [doc = " assert_eq!(odds, vec![1, 3, 5, 7]);"] # [doc = " ```"] # [track_caller] pub fn extract_if < F , R > (& mut self , range : R , pred : F) -> ExtractIf < '_ , K , V , F > where F : FnMut (& K , & mut V) -> bool , R : RangeBounds < usize > , { ExtractIf :: new (& mut self . core , range , pred) } # [doc = " Splits the collection into two at the given index."] # [doc = ""] # [doc = " Returns a newly allocated map containing the elements in the range"] # [doc = " `[at, len)`. After the call, the original map will be left containing"] # [doc = " the elements `[0, at)` with its previous capacity unchanged."] # [doc = ""] # [doc = " ***Panics*** if `at > len`."] # [track_caller] pub fn split_off (& mut self , at : usize) -> Self where S : Clone , { Self { core : self . core . split_off (at) , hash_builder : self . hash_builder . clone () , } } # [doc = " Reserve capacity for `additional` more key-value pairs."] # [doc = ""] # [doc = " Computes in **O(n)** time."] pub fn reserve (& mut self , additional : usize) { self . core . reserve (additional) ; } # [doc = " Reserve capacity for `additional` more key-value pairs, without over-allocating."] # [doc = ""] # [doc = " Unlike `reserve`, this does not deliberately over-allocate the entry capacity to avoid"] # [doc = " frequent re-allocations. However, the underlying data structures may still have internal"] # [doc = " capacity requirements, and the allocator itself may give more space than requested, so this"] # [doc = " cannot be relied upon to be precisely minimal."] # [doc = ""] # [doc = " Computes in **O(n)** time."] pub fn reserve_exact (& mut self , additional : usize) { self . core . reserve_exact (additional) ; } # [doc = " Try to reserve capacity for `additional` more key-value pairs."] # [doc = ""] # [doc = " Computes in **O(n)** time."] pub fn try_reserve (& mut self , additional : usize) -> Result < () , TryReserveError > { self . core . try_reserve (additional) } # [doc = " Try to reserve capacity for `additional` more key-value pairs, without over-allocating."] # [doc = ""] # [doc = " Unlike `try_reserve`, this does not deliberately over-allocate the entry capacity to avoid"] # [doc = " frequent re-allocations. However, the underlying data structures may still have internal"] # [doc = " capacity requirements, and the allocator itself may give more space than requested, so this"] # [doc = " cannot be relied upon to be precisely minimal."] # [doc = ""] # [doc = " Computes in **O(n)** time."] pub fn try_reserve_exact (& mut self , additional : usize) -> Result < () , TryReserveError > { self . core . try_reserve_exact (additional) } # [doc = " Shrink the capacity of the map as much as possible."] # [doc = ""] # [doc = " Computes in **O(n)** time."] pub fn shrink_to_fit (& mut self) { self . core . shrink_to (0) ; } # [doc = " Shrink the capacity of the map with a lower limit."] # [doc = ""] # [doc = " Computes in **O(n)** time."] pub fn shrink_to (& mut self , min_capacity : usize) { self . core . shrink_to (min_capacity) ; } }
    };
}

impl_52!();