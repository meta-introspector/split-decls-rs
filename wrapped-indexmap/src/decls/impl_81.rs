macro_rules! deps {
    () => {
        Bucket!();
        IndexSet!();
    };
}

macro_rules! impl_81 {
    () => {
        deps!();
        impl < T , S > IndexSet < T , S > { # [doc = " Remove the last value"] # [doc = ""] # [doc = " This preserves the order of the remaining elements."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] # [doc (alias = "pop_last")] pub fn pop (& mut self) -> Option < T > { self . map . pop () . map (| (x , ()) | x) } # [doc = " Removes and returns the last value from a set if the predicate"] # [doc = " returns `true`, or [`None`] if the predicate returns false or the set"] # [doc = " is empty (the predicate will not be called in that case)."] # [doc = ""] # [doc = " This preserves the order of the remaining elements."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use indexmap::IndexSet;"] # [doc = ""] # [doc = " let mut set = IndexSet::from([1, 2, 3, 4]);"] # [doc = " let pred = |x: &i32| *x % 2 == 0;"] # [doc = ""] # [doc = " assert_eq!(set.pop_if(pred), Some(4));"] # [doc = " assert_eq!(set.as_slice(), &[1, 2, 3]);"] # [doc = " assert_eq!(set.pop_if(pred), None);"] # [doc = " ```"] pub fn pop_if (& mut self , predicate : impl FnOnce (& T) -> bool) -> Option < T > { let last = self . last () ? ; if predicate (last) { self . pop () } else { None } } # [doc = " Scan through each value in the set and keep those where the"] # [doc = " closure `keep` returns `true`."] # [doc = ""] # [doc = " The elements are visited in order, and remaining elements keep their"] # [doc = " order."] # [doc = ""] # [doc = " Computes in **O(n)** time (average)."] pub fn retain < F > (& mut self , mut keep : F) where F : FnMut (& T) -> bool , { self . map . retain (move | x , & mut () | keep (x)) } # [doc = " Sort the set's values by their default ordering."] # [doc = ""] # [doc = " This is a stable sort -- but equivalent values should not normally coexist in"] # [doc = " a set at all, so [`sort_unstable`][Self::sort_unstable] is preferred"] # [doc = " because it is generally faster and doesn't allocate auxiliary memory."] # [doc = ""] # [doc = " See [`sort_by`](Self::sort_by) for details."] pub fn sort (& mut self) where T : Ord , { self . map . sort_keys () } # [doc = " Sort the set's values in place using the comparison function `cmp`."] # [doc = ""] # [doc = " Computes in **O(n log n)** time and **O(n)** space. The sort is stable."] pub fn sort_by < F > (& mut self , mut cmp : F) where F : FnMut (& T , & T) -> Ordering , { self . map . sort_by (move | a , () , b , () | cmp (a , b)) ; } # [doc = " Sort the values of the set and return a by-value iterator of"] # [doc = " the values with the result."] # [doc = ""] # [doc = " The sort is stable."] pub fn sorted_by < F > (self , mut cmp : F) -> IntoIter < T > where F : FnMut (& T , & T) -> Ordering , { let mut entries = self . into_entries () ; entries . sort_by (move | a , b | cmp (& a . key , & b . key)) ; IntoIter :: new (entries) } # [doc = " Sort the set's values in place using a key extraction function."] # [doc = ""] # [doc = " Computes in **O(n log n)** time and **O(n)** space. The sort is stable."] pub fn sort_by_key < K , F > (& mut self , mut sort_key : F) where K : Ord , F : FnMut (& T) -> K , { self . with_entries (move | entries | { entries . sort_by_key (move | a | sort_key (& a . key)) ; }) ; } # [doc = " Sort the set's values by their default ordering."] # [doc = ""] # [doc = " See [`sort_unstable_by`](Self::sort_unstable_by) for details."] pub fn sort_unstable (& mut self) where T : Ord , { self . map . sort_unstable_keys () } # [doc = " Sort the set's values in place using the comparison function `cmp`."] # [doc = ""] # [doc = " Computes in **O(n log n)** time. The sort is unstable."] pub fn sort_unstable_by < F > (& mut self , mut cmp : F) where F : FnMut (& T , & T) -> Ordering , { self . map . sort_unstable_by (move | a , _ , b , _ | cmp (a , b)) } # [doc = " Sort the values of the set and return a by-value iterator of"] # [doc = " the values with the result."] pub fn sorted_unstable_by < F > (self , mut cmp : F) -> IntoIter < T > where F : FnMut (& T , & T) -> Ordering , { let mut entries = self . into_entries () ; entries . sort_unstable_by (move | a , b | cmp (& a . key , & b . key)) ; IntoIter :: new (entries) } # [doc = " Sort the set's values in place using a key extraction function."] # [doc = ""] # [doc = " Computes in **O(n log n)** time. The sort is unstable."] pub fn sort_unstable_by_key < K , F > (& mut self , mut sort_key : F) where K : Ord , F : FnMut (& T) -> K , { self . with_entries (move | entries | { entries . sort_unstable_by_key (move | a | sort_key (& a . key)) ; }) ; } # [doc = " Sort the set's values in place using a key extraction function."] # [doc = ""] # [doc = " During sorting, the function is called at most once per entry, by using temporary storage"] # [doc = " to remember the results of its evaluation. The order of calls to the function is"] # [doc = " unspecified and may change between versions of `indexmap` or the standard library."] # [doc = ""] # [doc = " Computes in **O(m n + n log n + c)** time () and **O(n)** space, where the function is"] # [doc = " **O(m)**, *n* is the length of the map, and *c* the capacity. The sort is stable."] pub fn sort_by_cached_key < K , F > (& mut self , mut sort_key : F) where K : Ord , F : FnMut (& T) -> K , { self . with_entries (move | entries | { entries . sort_by_cached_key (move | a | sort_key (& a . key)) ; }) ; } # [doc = " Search over a sorted set for a value."] # [doc = ""] # [doc = " Returns the position where that value is present, or the position where it can be inserted"] # [doc = " to maintain the sort. See [`slice::binary_search`] for more details."] # [doc = ""] # [doc = " Computes in **O(log(n))** time, which is notably less scalable than looking the value up"] # [doc = " using [`get_index_of`][IndexSet::get_index_of], but this can also position missing values."] pub fn binary_search (& self , x : & T) -> Result < usize , usize > where T : Ord , { self . as_slice () . binary_search (x) } # [doc = " Search over a sorted set with a comparator function."] # [doc = ""] # [doc = " Returns the position where that value is present, or the position where it can be inserted"] # [doc = " to maintain the sort. See [`slice::binary_search_by`] for more details."] # [doc = ""] # [doc = " Computes in **O(log(n))** time."] # [inline] pub fn binary_search_by < 'a , F > (& 'a self , f : F) -> Result < usize , usize > where F : FnMut (& 'a T) -> Ordering , { self . as_slice () . binary_search_by (f) } # [doc = " Search over a sorted set with an extraction function."] # [doc = ""] # [doc = " Returns the position where that value is present, or the position where it can be inserted"] # [doc = " to maintain the sort. See [`slice::binary_search_by_key`] for more details."] # [doc = ""] # [doc = " Computes in **O(log(n))** time."] # [inline] pub fn binary_search_by_key < 'a , B , F > (& 'a self , b : & B , f : F) -> Result < usize , usize > where F : FnMut (& 'a T) -> B , B : Ord , { self . as_slice () . binary_search_by_key (b , f) } # [doc = " Checks if the values of this set are sorted."] # [inline] pub fn is_sorted (& self) -> bool where T : PartialOrd , { self . as_slice () . is_sorted () } # [doc = " Checks if this set is sorted using the given comparator function."] # [inline] pub fn is_sorted_by < 'a , F > (& 'a self , cmp : F) -> bool where F : FnMut (& 'a T , & 'a T) -> bool , { self . as_slice () . is_sorted_by (cmp) } # [doc = " Checks if this set is sorted using the given sort-key function."] # [inline] pub fn is_sorted_by_key < 'a , F , K > (& 'a self , sort_key : F) -> bool where F : FnMut (& 'a T) -> K , K : PartialOrd , { self . as_slice () . is_sorted_by_key (sort_key) } # [doc = " Returns the index of the partition point of a sorted set according to the given predicate"] # [doc = " (the index of the first element of the second partition)."] # [doc = ""] # [doc = " See [`slice::partition_point`] for more details."] # [doc = ""] # [doc = " Computes in **O(log(n))** time."] # [must_use] pub fn partition_point < P > (& self , pred : P) -> usize where P : FnMut (& T) -> bool , { self . as_slice () . partition_point (pred) } # [doc = " Reverses the order of the set's values in place."] # [doc = ""] # [doc = " Computes in **O(n)** time and **O(1)** space."] pub fn reverse (& mut self) { self . map . reverse () } # [doc = " Returns a slice of all the values in the set."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn as_slice (& self) -> & Slice < T > { Slice :: from_slice (self . as_entries ()) } # [doc = " Converts into a boxed slice of all the values in the set."] # [doc = ""] # [doc = " Note that this will drop the inner hash table and any excess capacity."] pub fn into_boxed_slice (self) -> Box < Slice < T > > { Slice :: from_boxed (self . into_entries () . into_boxed_slice ()) } # [doc = " Get a value by index"] # [doc = ""] # [doc = " Valid indices are `0 <= index < self.len()`."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn get_index (& self , index : usize) -> Option < & T > { self . as_entries () . get (index) . map (Bucket :: key_ref) } # [doc = " Returns a slice of values in the given range of indices."] # [doc = ""] # [doc = " Valid indices are `0 <= index < self.len()`."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn get_range < R : RangeBounds < usize > > (& self , range : R) -> Option < & Slice < T > > { let entries = self . as_entries () ; let range = try_simplify_range (range , entries . len ()) ? ; entries . get (range) . map (Slice :: from_slice) } # [doc = " Get the first value"] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn first (& self) -> Option < & T > { self . as_entries () . first () . map (Bucket :: key_ref) } # [doc = " Get the last value"] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn last (& self) -> Option < & T > { self . as_entries () . last () . map (Bucket :: key_ref) } # [doc = " Remove the value by index"] # [doc = ""] # [doc = " Valid indices are `0 <= index < self.len()`."] # [doc = ""] # [doc = " Like [`Vec::swap_remove`], the value is removed by swapping it with the"] # [doc = " last element of the set and popping it off. **This perturbs"] # [doc = " the position of what used to be the last element!**"] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn swap_remove_index (& mut self , index : usize) -> Option < T > { self . map . swap_remove_index (index) . map (| (x , ()) | x) } # [doc = " Remove the value by index"] # [doc = ""] # [doc = " Valid indices are `0 <= index < self.len()`."] # [doc = ""] # [doc = " Like [`Vec::remove`], the value is removed by shifting all of the"] # [doc = " elements that follow it, preserving their relative order."] # [doc = " **This perturbs the index of all of those elements!**"] # [doc = ""] # [doc = " Computes in **O(n)** time (average)."] pub fn shift_remove_index (& mut self , index : usize) -> Option < T > { self . map . shift_remove_index (index) . map (| (x , ()) | x) } # [doc = " Moves the position of a value from one index to another"] # [doc = " by shifting all other values in-between."] # [doc = ""] # [doc = " * If `from < to`, the other values will shift down while the targeted value moves up."] # [doc = " * If `from > to`, the other values will shift up while the targeted value moves down."] # [doc = ""] # [doc = " ***Panics*** if `from` or `to` are out of bounds."] # [doc = ""] # [doc = " Computes in **O(n)** time (average)."] # [track_caller] pub fn move_index (& mut self , from : usize , to : usize) { self . map . move_index (from , to) } # [doc = " Swaps the position of two values in the set."] # [doc = ""] # [doc = " ***Panics*** if `a` or `b` are out of bounds."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] # [track_caller] pub fn swap_indices (& mut self , a : usize , b : usize) { self . map . swap_indices (a , b) } }
    };
}

impl_81!()