macro_rules! deps {
    () => {
        IndexSet!();
    };
}

macro_rules! impl_80 {
    () => {
        deps!();
        impl < T , S > IndexSet < T , S > where S : BuildHasher , { # [doc = " Return `true` if an equivalent to `value` exists in the set."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn contains < Q > (& self , value : & Q) -> bool where Q : ? Sized + Hash + Equivalent < T > , { self . map . contains_key (value) } # [doc = " Return a reference to the value stored in the set, if it is present,"] # [doc = " else `None`."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn get < Q > (& self , value : & Q) -> Option < & T > where Q : ? Sized + Hash + Equivalent < T > , { self . map . get_key_value (value) . map (| (x , & ()) | x) } # [doc = " Return item index and value"] pub fn get_full < Q > (& self , value : & Q) -> Option < (usize , & T) > where Q : ? Sized + Hash + Equivalent < T > , { self . map . get_full (value) . map (| (i , x , & ()) | (i , x)) } # [doc = " Return item index, if it exists in the set"] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn get_index_of < Q > (& self , value : & Q) -> Option < usize > where Q : ? Sized + Hash + Equivalent < T > , { self . map . get_index_of (value) } # [doc = " Remove the value from the set, and return `true` if it was present."] # [doc = ""] # [doc = " **NOTE:** This is equivalent to [`.swap_remove(value)`][Self::swap_remove], replacing this"] # [doc = " value's position with the last element, and it is deprecated in favor of calling that"] # [doc = " explicitly. If you need to preserve the relative order of the values in the set, use"] # [doc = " [`.shift_remove(value)`][Self::shift_remove] instead."] # [deprecated (note = "`remove` disrupts the set order -- \
        use `swap_remove` or `shift_remove` for explicit behavior.")] pub fn remove < Q > (& mut self , value : & Q) -> bool where Q : ? Sized + Hash + Equivalent < T > , { self . swap_remove (value) } # [doc = " Remove the value from the set, and return `true` if it was present."] # [doc = ""] # [doc = " Like [`Vec::swap_remove`], the value is removed by swapping it with the"] # [doc = " last element of the set and popping it off. **This perturbs"] # [doc = " the position of what used to be the last element!**"] # [doc = ""] # [doc = " Return `false` if `value` was not in the set."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn swap_remove < Q > (& mut self , value : & Q) -> bool where Q : ? Sized + Hash + Equivalent < T > , { self . map . swap_remove (value) . is_some () } # [doc = " Remove the value from the set, and return `true` if it was present."] # [doc = ""] # [doc = " Like [`Vec::remove`], the value is removed by shifting all of the"] # [doc = " elements that follow it, preserving their relative order."] # [doc = " **This perturbs the index of all of those elements!**"] # [doc = ""] # [doc = " Return `false` if `value` was not in the set."] # [doc = ""] # [doc = " Computes in **O(n)** time (average)."] pub fn shift_remove < Q > (& mut self , value : & Q) -> bool where Q : ? Sized + Hash + Equivalent < T > , { self . map . shift_remove (value) . is_some () } # [doc = " Removes and returns the value in the set, if any, that is equal to the"] # [doc = " given one."] # [doc = ""] # [doc = " **NOTE:** This is equivalent to [`.swap_take(value)`][Self::swap_take], replacing this"] # [doc = " value's position with the last element, and it is deprecated in favor of calling that"] # [doc = " explicitly. If you need to preserve the relative order of the values in the set, use"] # [doc = " [`.shift_take(value)`][Self::shift_take] instead."] # [deprecated (note = "`take` disrupts the set order -- \
        use `swap_take` or `shift_take` for explicit behavior.")] pub fn take < Q > (& mut self , value : & Q) -> Option < T > where Q : ? Sized + Hash + Equivalent < T > , { self . swap_take (value) } # [doc = " Removes and returns the value in the set, if any, that is equal to the"] # [doc = " given one."] # [doc = ""] # [doc = " Like [`Vec::swap_remove`], the value is removed by swapping it with the"] # [doc = " last element of the set and popping it off. **This perturbs"] # [doc = " the position of what used to be the last element!**"] # [doc = ""] # [doc = " Return `None` if `value` was not in the set."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn swap_take < Q > (& mut self , value : & Q) -> Option < T > where Q : ? Sized + Hash + Equivalent < T > , { self . map . swap_remove_entry (value) . map (| (x , ()) | x) } # [doc = " Removes and returns the value in the set, if any, that is equal to the"] # [doc = " given one."] # [doc = ""] # [doc = " Like [`Vec::remove`], the value is removed by shifting all of the"] # [doc = " elements that follow it, preserving their relative order."] # [doc = " **This perturbs the index of all of those elements!**"] # [doc = ""] # [doc = " Return `None` if `value` was not in the set."] # [doc = ""] # [doc = " Computes in **O(n)** time (average)."] pub fn shift_take < Q > (& mut self , value : & Q) -> Option < T > where Q : ? Sized + Hash + Equivalent < T > , { self . map . shift_remove_entry (value) . map (| (x , ()) | x) } # [doc = " Remove the value from the set return it and the index it had."] # [doc = ""] # [doc = " Like [`Vec::swap_remove`], the value is removed by swapping it with the"] # [doc = " last element of the set and popping it off. **This perturbs"] # [doc = " the position of what used to be the last element!**"] # [doc = ""] # [doc = " Return `None` if `value` was not in the set."] pub fn swap_remove_full < Q > (& mut self , value : & Q) -> Option < (usize , T) > where Q : ? Sized + Hash + Equivalent < T > , { self . map . swap_remove_full (value) . map (| (i , x , ()) | (i , x)) } # [doc = " Remove the value from the set return it and the index it had."] # [doc = ""] # [doc = " Like [`Vec::remove`], the value is removed by shifting all of the"] # [doc = " elements that follow it, preserving their relative order."] # [doc = " **This perturbs the index of all of those elements!**"] # [doc = ""] # [doc = " Return `None` if `value` was not in the set."] pub fn shift_remove_full < Q > (& mut self , value : & Q) -> Option < (usize , T) > where Q : ? Sized + Hash + Equivalent < T > , { self . map . shift_remove_full (value) . map (| (i , x , ()) | (i , x)) } }
    };
}

impl_80!()