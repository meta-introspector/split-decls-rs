macro_rules! deps {
    () => {
        Bucket!();
        GetDisjointMutError!();
        IndexMap!();
    };
}

macro_rules! impl_55 {
    () => {
        deps!();
        impl < K , V , S > IndexMap < K , V , S > { # [doc = " Remove the last key-value pair"] # [doc = ""] # [doc = " This preserves the order of the remaining elements."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] # [doc (alias = "pop_last")] pub fn pop (& mut self) -> Option < (K , V) > { self . core . pop () } # [doc = " Removes and returns the last key-value pair from a map if the predicate"] # [doc = " returns `true`, or [`None`] if the predicate returns false or the map"] # [doc = " is empty (the predicate will not be called in that case)."] # [doc = ""] # [doc = " This preserves the order of the remaining elements."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use indexmap::IndexMap;"] # [doc = ""] # [doc = " let init = [(1, 'a'), (2, 'b'), (3, 'c'), (4, 'd')];"] # [doc = " let mut map = IndexMap::from(init);"] # [doc = " let pred = |key: &i32, _value: &mut char| *key % 2 == 0;"] # [doc = ""] # [doc = " assert_eq!(map.pop_if(pred), Some((4, 'd')));"] # [doc = " assert_eq!(map.as_slice(), &init[..3]);"] # [doc = " assert_eq!(map.pop_if(pred), None);"] # [doc = " ```"] pub fn pop_if (& mut self , predicate : impl FnOnce (& K , & mut V) -> bool) -> Option < (K , V) > { let (last_key , last_value) = self . last_mut () ? ; if predicate (last_key , last_value) { self . core . pop () } else { None } } # [doc = " Scan through each key-value pair in the map and keep those where the"] # [doc = " closure `keep` returns `true`."] # [doc = ""] # [doc = " The elements are visited in order, and remaining elements keep their"] # [doc = " order."] # [doc = ""] # [doc = " Computes in **O(n)** time (average)."] pub fn retain < F > (& mut self , mut keep : F) where F : FnMut (& K , & mut V) -> bool , { self . core . retain_in_order (move | k , v | keep (k , v)) ; } # [doc = " Sort the map's key-value pairs by the default ordering of the keys."] # [doc = ""] # [doc = " This is a stable sort -- but equivalent keys should not normally coexist in"] # [doc = " a map at all, so [`sort_unstable_keys`][Self::sort_unstable_keys] is preferred"] # [doc = " because it is generally faster and doesn't allocate auxiliary memory."] # [doc = ""] # [doc = " See [`sort_by`](Self::sort_by) for details."] pub fn sort_keys (& mut self) where K : Ord , { self . with_entries (move | entries | { entries . sort_by (move | a , b | K :: cmp (& a . key , & b . key)) ; }) ; } # [doc = " Sort the map's key-value pairs in place using the comparison"] # [doc = " function `cmp`."] # [doc = ""] # [doc = " The comparison function receives two key and value pairs to compare (you"] # [doc = " can sort by keys or values or their combination as needed)."] # [doc = ""] # [doc = " Computes in **O(n log n + c)** time and **O(n)** space where *n* is"] # [doc = " the length of the map and *c* the capacity. The sort is stable."] pub fn sort_by < F > (& mut self , mut cmp : F) where F : FnMut (& K , & V , & K , & V) -> Ordering , { self . with_entries (move | entries | { entries . sort_by (move | a , b | cmp (& a . key , & a . value , & b . key , & b . value)) ; }) ; } # [doc = " Sort the key-value pairs of the map and return a by-value iterator of"] # [doc = " the key-value pairs with the result."] # [doc = ""] # [doc = " The sort is stable."] pub fn sorted_by < F > (self , mut cmp : F) -> IntoIter < K , V > where F : FnMut (& K , & V , & K , & V) -> Ordering , { let mut entries = self . into_entries () ; entries . sort_by (move | a , b | cmp (& a . key , & a . value , & b . key , & b . value)) ; IntoIter :: new (entries) } # [doc = " Sort the map's key-value pairs in place using a sort-key extraction function."] # [doc = ""] # [doc = " Computes in **O(n log n + c)** time and **O(n)** space where *n* is"] # [doc = " the length of the map and *c* the capacity. The sort is stable."] pub fn sort_by_key < T , F > (& mut self , mut sort_key : F) where T : Ord , F : FnMut (& K , & V) -> T , { self . with_entries (move | entries | { entries . sort_by_key (move | a | sort_key (& a . key , & a . value)) ; }) ; } # [doc = " Sort the map's key-value pairs by the default ordering of the keys, but"] # [doc = " may not preserve the order of equal elements."] # [doc = ""] # [doc = " See [`sort_unstable_by`](Self::sort_unstable_by) for details."] pub fn sort_unstable_keys (& mut self) where K : Ord , { self . with_entries (move | entries | { entries . sort_unstable_by (move | a , b | K :: cmp (& a . key , & b . key)) ; }) ; } # [doc = " Sort the map's key-value pairs in place using the comparison function `cmp`, but"] # [doc = " may not preserve the order of equal elements."] # [doc = ""] # [doc = " The comparison function receives two key and value pairs to compare (you"] # [doc = " can sort by keys or values or their combination as needed)."] # [doc = ""] # [doc = " Computes in **O(n log n + c)** time where *n* is"] # [doc = " the length of the map and *c* is the capacity. The sort is unstable."] pub fn sort_unstable_by < F > (& mut self , mut cmp : F) where F : FnMut (& K , & V , & K , & V) -> Ordering , { self . with_entries (move | entries | { entries . sort_unstable_by (move | a , b | cmp (& a . key , & a . value , & b . key , & b . value)) ; }) ; } # [doc = " Sort the key-value pairs of the map and return a by-value iterator of"] # [doc = " the key-value pairs with the result."] # [doc = ""] # [doc = " The sort is unstable."] # [inline] pub fn sorted_unstable_by < F > (self , mut cmp : F) -> IntoIter < K , V > where F : FnMut (& K , & V , & K , & V) -> Ordering , { let mut entries = self . into_entries () ; entries . sort_unstable_by (move | a , b | cmp (& a . key , & a . value , & b . key , & b . value)) ; IntoIter :: new (entries) } # [doc = " Sort the map's key-value pairs in place using a sort-key extraction function."] # [doc = ""] # [doc = " Computes in **O(n log n + c)** time where *n* is"] # [doc = " the length of the map and *c* is the capacity. The sort is unstable."] pub fn sort_unstable_by_key < T , F > (& mut self , mut sort_key : F) where T : Ord , F : FnMut (& K , & V) -> T , { self . with_entries (move | entries | { entries . sort_unstable_by_key (move | a | sort_key (& a . key , & a . value)) ; }) ; } # [doc = " Sort the map's key-value pairs in place using a sort-key extraction function."] # [doc = ""] # [doc = " During sorting, the function is called at most once per entry, by using temporary storage"] # [doc = " to remember the results of its evaluation. The order of calls to the function is"] # [doc = " unspecified and may change between versions of `indexmap` or the standard library."] # [doc = ""] # [doc = " Computes in **O(m n + n log n + c)** time () and **O(n)** space, where the function is"] # [doc = " **O(m)**, *n* is the length of the map, and *c* the capacity. The sort is stable."] pub fn sort_by_cached_key < T , F > (& mut self , mut sort_key : F) where T : Ord , F : FnMut (& K , & V) -> T , { self . with_entries (move | entries | { entries . sort_by_cached_key (move | a | sort_key (& a . key , & a . value)) ; }) ; } # [doc = " Search over a sorted map for a key."] # [doc = ""] # [doc = " Returns the position where that key is present, or the position where it can be inserted to"] # [doc = " maintain the sort. See [`slice::binary_search`] for more details."] # [doc = ""] # [doc = " Computes in **O(log(n))** time, which is notably less scalable than looking the key up"] # [doc = " using [`get_index_of`][IndexMap::get_index_of], but this can also position missing keys."] pub fn binary_search_keys (& self , x : & K) -> Result < usize , usize > where K : Ord , { self . as_slice () . binary_search_keys (x) } # [doc = " Search over a sorted map with a comparator function."] # [doc = ""] # [doc = " Returns the position where that value is present, or the position where it can be inserted"] # [doc = " to maintain the sort. See [`slice::binary_search_by`] for more details."] # [doc = ""] # [doc = " Computes in **O(log(n))** time."] # [inline] pub fn binary_search_by < 'a , F > (& 'a self , f : F) -> Result < usize , usize > where F : FnMut (& 'a K , & 'a V) -> Ordering , { self . as_slice () . binary_search_by (f) } # [doc = " Search over a sorted map with an extraction function."] # [doc = ""] # [doc = " Returns the position where that value is present, or the position where it can be inserted"] # [doc = " to maintain the sort. See [`slice::binary_search_by_key`] for more details."] # [doc = ""] # [doc = " Computes in **O(log(n))** time."] # [inline] pub fn binary_search_by_key < 'a , B , F > (& 'a self , b : & B , f : F) -> Result < usize , usize > where F : FnMut (& 'a K , & 'a V) -> B , B : Ord , { self . as_slice () . binary_search_by_key (b , f) } # [doc = " Checks if the keys of this map are sorted."] # [inline] pub fn is_sorted (& self) -> bool where K : PartialOrd , { self . as_slice () . is_sorted () } # [doc = " Checks if this map is sorted using the given comparator function."] # [inline] pub fn is_sorted_by < 'a , F > (& 'a self , cmp : F) -> bool where F : FnMut (& 'a K , & 'a V , & 'a K , & 'a V) -> bool , { self . as_slice () . is_sorted_by (cmp) } # [doc = " Checks if this map is sorted using the given sort-key function."] # [inline] pub fn is_sorted_by_key < 'a , F , T > (& 'a self , sort_key : F) -> bool where F : FnMut (& 'a K , & 'a V) -> T , T : PartialOrd , { self . as_slice () . is_sorted_by_key (sort_key) } # [doc = " Returns the index of the partition point of a sorted map according to the given predicate"] # [doc = " (the index of the first element of the second partition)."] # [doc = ""] # [doc = " See [`slice::partition_point`] for more details."] # [doc = ""] # [doc = " Computes in **O(log(n))** time."] # [must_use] pub fn partition_point < P > (& self , pred : P) -> usize where P : FnMut (& K , & V) -> bool , { self . as_slice () . partition_point (pred) } # [doc = " Reverses the order of the map's key-value pairs in place."] # [doc = ""] # [doc = " Computes in **O(n)** time and **O(1)** space."] pub fn reverse (& mut self) { self . core . reverse () } # [doc = " Returns a slice of all the key-value pairs in the map."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn as_slice (& self) -> & Slice < K , V > { Slice :: from_slice (self . as_entries ()) } # [doc = " Returns a mutable slice of all the key-value pairs in the map."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn as_mut_slice (& mut self) -> & mut Slice < K , V > { Slice :: from_mut_slice (self . as_entries_mut ()) } # [doc = " Converts into a boxed slice of all the key-value pairs in the map."] # [doc = ""] # [doc = " Note that this will drop the inner hash table and any excess capacity."] pub fn into_boxed_slice (self) -> Box < Slice < K , V > > { Slice :: from_boxed (self . into_entries () . into_boxed_slice ()) } # [doc = " Get a key-value pair by index"] # [doc = ""] # [doc = " Valid indices are `0 <= index < self.len()`."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn get_index (& self , index : usize) -> Option < (& K , & V) > { self . as_entries () . get (index) . map (Bucket :: refs) } # [doc = " Get a key-value pair by index"] # [doc = ""] # [doc = " Valid indices are `0 <= index < self.len()`."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn get_index_mut (& mut self , index : usize) -> Option < (& K , & mut V) > { self . as_entries_mut () . get_mut (index) . map (Bucket :: ref_mut) } # [doc = " Get an entry in the map by index for in-place manipulation."] # [doc = ""] # [doc = " Valid indices are `0 <= index < self.len()`."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn get_index_entry (& mut self , index : usize) -> Option < IndexedEntry < '_ , K , V > > { if index >= self . len () { return None ; } Some (IndexedEntry :: new (& mut self . core , index)) } # [doc = " Get an array of `N` key-value pairs by `N` indices"] # [doc = ""] # [doc = " Valid indices are *0 <= index < self.len()* and each index needs to be unique."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut map = indexmap::IndexMap::from([(1, 'a'), (3, 'b'), (2, 'c')]);"] # [doc = " assert_eq!(map.get_disjoint_indices_mut([2, 0]), Ok([(&2, &mut 'c'), (&1, &mut 'a')]));"] # [doc = " ```"] pub fn get_disjoint_indices_mut < const N : usize > (& mut self , indices : [usize ; N] ,) -> Result < [(& K , & mut V) ; N] , GetDisjointMutError > { self . as_mut_slice () . get_disjoint_mut (indices) } # [doc = " Returns a slice of key-value pairs in the given range of indices."] # [doc = ""] # [doc = " Valid indices are `0 <= index < self.len()`."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn get_range < R : RangeBounds < usize > > (& self , range : R) -> Option < & Slice < K , V > > { let entries = self . as_entries () ; let range = try_simplify_range (range , entries . len ()) ? ; entries . get (range) . map (Slice :: from_slice) } # [doc = " Returns a mutable slice of key-value pairs in the given range of indices."] # [doc = ""] # [doc = " Valid indices are `0 <= index < self.len()`."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn get_range_mut < R : RangeBounds < usize > > (& mut self , range : R) -> Option < & mut Slice < K , V > > { let entries = self . as_entries_mut () ; let range = try_simplify_range (range , entries . len ()) ? ; entries . get_mut (range) . map (Slice :: from_mut_slice) } # [doc = " Get the first key-value pair"] # [doc = ""] # [doc = " Computes in **O(1)** time."] # [doc (alias = "first_key_value")] pub fn first (& self) -> Option < (& K , & V) > { self . as_entries () . first () . map (Bucket :: refs) } # [doc = " Get the first key-value pair, with mutable access to the value"] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn first_mut (& mut self) -> Option < (& K , & mut V) > { self . as_entries_mut () . first_mut () . map (Bucket :: ref_mut) } # [doc = " Get the first entry in the map for in-place manipulation."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn first_entry (& mut self) -> Option < IndexedEntry < '_ , K , V > > { self . get_index_entry (0) } # [doc = " Get the last key-value pair"] # [doc = ""] # [doc = " Computes in **O(1)** time."] # [doc (alias = "last_key_value")] pub fn last (& self) -> Option < (& K , & V) > { self . as_entries () . last () . map (Bucket :: refs) } # [doc = " Get the last key-value pair, with mutable access to the value"] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn last_mut (& mut self) -> Option < (& K , & mut V) > { self . as_entries_mut () . last_mut () . map (Bucket :: ref_mut) } # [doc = " Get the last entry in the map for in-place manipulation."] # [doc = ""] # [doc = " Computes in **O(1)** time."] pub fn last_entry (& mut self) -> Option < IndexedEntry < '_ , K , V > > { self . get_index_entry (self . len () . checked_sub (1) ?) } # [doc = " Remove the key-value pair by index"] # [doc = ""] # [doc = " Valid indices are `0 <= index < self.len()`."] # [doc = ""] # [doc = " Like [`Vec::swap_remove`], the pair is removed by swapping it with the"] # [doc = " last element of the map and popping it off. **This perturbs"] # [doc = " the position of what used to be the last element!**"] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] pub fn swap_remove_index (& mut self , index : usize) -> Option < (K , V) > { self . core . swap_remove_index (index) } # [doc = " Remove the key-value pair by index"] # [doc = ""] # [doc = " Valid indices are `0 <= index < self.len()`."] # [doc = ""] # [doc = " Like [`Vec::remove`], the pair is removed by shifting all of the"] # [doc = " elements that follow it, preserving their relative order."] # [doc = " **This perturbs the index of all of those elements!**"] # [doc = ""] # [doc = " Computes in **O(n)** time (average)."] pub fn shift_remove_index (& mut self , index : usize) -> Option < (K , V) > { self . core . shift_remove_index (index) } # [doc = " Moves the position of a key-value pair from one index to another"] # [doc = " by shifting all other pairs in-between."] # [doc = ""] # [doc = " * If `from < to`, the other pairs will shift down while the targeted pair moves up."] # [doc = " * If `from > to`, the other pairs will shift up while the targeted pair moves down."] # [doc = ""] # [doc = " ***Panics*** if `from` or `to` are out of bounds."] # [doc = ""] # [doc = " Computes in **O(n)** time (average)."] # [track_caller] pub fn move_index (& mut self , from : usize , to : usize) { self . core . move_index (from , to) } # [doc = " Swaps the position of two key-value pairs in the map."] # [doc = ""] # [doc = " ***Panics*** if `a` or `b` are out of bounds."] # [doc = ""] # [doc = " Computes in **O(1)** time (average)."] # [track_caller] pub fn swap_indices (& mut self , a : usize , b : usize) { self . core . swap_indices (a , b) } }
    };
}

impl_55!();