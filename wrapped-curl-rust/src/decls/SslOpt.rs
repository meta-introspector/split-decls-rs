macro_rules! SslOpt {
    () => {
        # [doc = " Structure which stores possible ssl options to pass to `ssl_options`."] # [derive (Clone)] pub struct SslOpt { bits : c_long , }
    };
}

SslOpt!()