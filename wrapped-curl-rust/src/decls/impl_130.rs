macro_rules! deps {
    () => {
        EasyHandle!();
        MultiData!();
        MultiWaker!();
        RawMulti!();
        DetachGuard!();
        Message!();
        Easy2Handle!();
        WaitFd!();
        MultiError!();
        Events!();
        Multi!();
        Socket!();
        Error!();
        SocketEvents!();
        Easy2!();
        Easy!();
    };
}

macro_rules! impl_130 {
    () => {
        deps!();
        impl Multi { # [doc = " Creates a new multi session through which multiple HTTP transfers can be"] # [doc = " initiated."] pub fn new () -> Multi { unsafe { crate :: init () ; let ptr = curl_sys :: curl_multi_init () ; assert ! (! ptr . is_null ()) ; Multi { raw : Arc :: new (RawMulti { handle : ptr }) , data : Box :: new (MultiData { socket : Box :: new (| _ , _ , _ | ()) , timer : Box :: new (| _ | true) , }) , } } } # [doc = " Set the callback informed about what to wait for"] # [doc = ""] # [doc = " When the `action` function runs, it informs the application about"] # [doc = " updates in the socket (file descriptor) status by doing none, one, or"] # [doc = " multiple calls to the socket callback. The callback gets status updates"] # [doc = " with changes since the previous time the callback was called. See"] # [doc = " `action` for more details on how the callback is used and should work."] # [doc = ""] # [doc = " The `SocketEvents` parameter informs the callback on the status of the"] # [doc = " given socket, and the methods on that type can be used to learn about"] # [doc = " what's going on with the socket."] # [doc = ""] # [doc = " The third `usize` parameter is a custom value set by the `assign` method"] # [doc = " below."] pub fn socket_function < F > (& mut self , f : F) -> Result < () , MultiError > where F : FnMut (Socket , SocketEvents , usize) + Send + 'static , { self . _socket_function (Box :: new (f)) } fn _socket_function (& mut self , f : Box < dyn FnMut (Socket , SocketEvents , usize) + Send > ,) -> Result < () , MultiError > { self . data . socket = f ; let cb : curl_sys :: curl_socket_callback = cb ; self . setopt_ptr (curl_sys :: CURLMOPT_SOCKETFUNCTION , cb as usize as * const c_char ,) ? ; let ptr = & * self . data as * const _ ; self . setopt_ptr (curl_sys :: CURLMOPT_SOCKETDATA , ptr as * const c_char) ? ; return Ok (()) ; extern "C" fn cb (_easy : * mut curl_sys :: CURL , socket : curl_sys :: curl_socket_t , what : c_int , userptr : * mut c_void , socketp : * mut c_void ,) -> c_int { panic :: catch (| | unsafe { let f = & mut (* (userptr as * mut MultiData)) . socket ; f (socket , SocketEvents { bits : what } , socketp as usize) }) ; 0 } } # [doc = " Set data to associate with an internal socket"] # [doc = ""] # [doc = " This function creates an association in the multi handle between the"] # [doc = " given socket and a private token of the application. This is designed"] # [doc = " for `action` uses."] # [doc = ""] # [doc = " When set, the token will be passed to all future socket callbacks for"] # [doc = " the specified socket."] # [doc = ""] # [doc = " If the given socket isn't already in use by libcurl, this function will"] # [doc = " return an error."] # [doc = ""] # [doc = " libcurl only keeps one single token associated with a socket, so"] # [doc = " calling this function several times for the same socket will make the"] # [doc = " last set token get used."] # [doc = ""] # [doc = " The idea here being that this association (socket to token) is something"] # [doc = " that just about every application that uses this API will need and then"] # [doc = " libcurl can just as well do it since it already has an internal hash"] # [doc = " table lookup for this."] # [doc = ""] # [doc = " # Typical Usage"] # [doc = ""] # [doc = " In a typical application you allocate a struct or at least use some kind"] # [doc = " of semi-dynamic data for each socket that we must wait for action on"] # [doc = " when using the `action` approach."] # [doc = ""] # [doc = " When our socket-callback gets called by libcurl and we get to know about"] # [doc = " yet another socket to wait for, we can use `assign` to point out the"] # [doc = " particular data so that when we get updates about this same socket"] # [doc = " again, we don't have to find the struct associated with this socket by"] # [doc = " ourselves."] pub fn assign (& self , socket : Socket , token : usize) -> Result < () , MultiError > { unsafe { cvt (curl_sys :: curl_multi_assign (self . raw . handle , socket , token as * mut _ ,)) ? ; Ok (()) } } # [doc = " Set callback to receive timeout values"] # [doc = ""] # [doc = " Certain features, such as timeouts and retries, require you to call"] # [doc = " libcurl even when there is no activity on the file descriptors."] # [doc = ""] # [doc = " Your callback function should install a non-repeating timer with the"] # [doc = " interval specified. Each time that timer fires, call either `action` or"] # [doc = " `perform`, depending on which interface you use."] # [doc = ""] # [doc = " A timeout value of `None` means you should delete your timer."] # [doc = ""] # [doc = " A timeout value of 0 means you should call `action` or `perform` (once)"] # [doc = " as soon as possible."] # [doc = ""] # [doc = " This callback will only be called when the timeout changes."] # [doc = ""] # [doc = " The timer callback should return `true` on success, and `false` on"] # [doc = " error. This callback can be used instead of, or in addition to,"] # [doc = " `get_timeout`."] pub fn timer_function < F > (& mut self , f : F) -> Result < () , MultiError > where F : FnMut (Option < Duration >) -> bool + Send + 'static , { self . _timer_function (Box :: new (f)) } fn _timer_function (& mut self , f : Box < dyn FnMut (Option < Duration >) -> bool + Send > ,) -> Result < () , MultiError > { self . data . timer = f ; let cb : curl_sys :: curl_multi_timer_callback = cb ; self . setopt_ptr (curl_sys :: CURLMOPT_TIMERFUNCTION , cb as usize as * const c_char ,) ? ; let ptr = & * self . data as * const _ ; self . setopt_ptr (curl_sys :: CURLMOPT_TIMERDATA , ptr as * const c_char) ? ; return Ok (()) ; extern "C" fn cb (_multi : * mut curl_sys :: CURLM , timeout_ms : c_long , user : * mut c_void ,) -> c_int { let keep_going = panic :: catch (| | unsafe { let f = & mut (* (user as * mut MultiData)) . timer ; if timeout_ms == - 1 { f (None) } else { f (Some (Duration :: from_millis (timeout_ms as u64))) } }) . unwrap_or (false) ; if keep_going { 0 } else { - 1 } } } # [doc = " Enable or disable HTTP pipelining and multiplexing."] # [doc = ""] # [doc = " When http_1 is true, enable HTTP/1.1 pipelining, which means that if"] # [doc = " you add a second request that can use an already existing connection,"] # [doc = " the second request will be \"piped\" on the same connection rather than"] # [doc = " being executed in parallel."] # [doc = ""] # [doc = " When multiplex is true, enable HTTP/2 multiplexing, which means that"] # [doc = " follow-up requests can re-use an existing connection and send the new"] # [doc = " request multiplexed over that at the same time as other transfers are"] # [doc = " already using that single connection."] pub fn pipelining (& mut self , http_1 : bool , multiplex : bool) -> Result < () , MultiError > { let bitmask = if http_1 { curl_sys :: CURLPIPE_HTTP1 } else { 0 } | if multiplex { curl_sys :: CURLPIPE_MULTIPLEX } else { 0 } ; self . setopt_long (curl_sys :: CURLMOPT_PIPELINING , bitmask) } # [doc = " Sets the max number of connections to a single host."] # [doc = ""] # [doc = " Pass a long to indicate the max number of simultaneously open connections"] # [doc = " to a single host (a host being the same as a host name + port number pair)."] # [doc = " For each new session to a host, libcurl will open up a new connection up to the"] # [doc = " limit set by the provided value. When the limit is reached, the sessions will"] # [doc = " be pending until a connection becomes available. If pipelining is enabled,"] # [doc = " libcurl will try to pipeline if the host is capable of it."] pub fn set_max_host_connections (& mut self , val : usize) -> Result < () , MultiError > { self . setopt_long (curl_sys :: CURLMOPT_MAX_HOST_CONNECTIONS , val as c_long) } # [doc = " Sets the max simultaneously open connections."] # [doc = ""] # [doc = " The set number will be used as the maximum number of simultaneously open"] # [doc = " connections in total using this multi handle. For each new session,"] # [doc = " libcurl will open a new connection up to the limit set by the provided"] # [doc = " value. When the limit is reached, the sessions will be pending until"] # [doc = " there are available connections. If pipelining is enabled, libcurl will"] # [doc = " try to pipeline or use multiplexing if the host is capable of it."] pub fn set_max_total_connections (& mut self , val : usize) -> Result < () , MultiError > { self . setopt_long (curl_sys :: CURLMOPT_MAX_TOTAL_CONNECTIONS , val as c_long) } # [doc = " Set size of connection cache."] # [doc = ""] # [doc = " The set number will be used as the maximum amount of simultaneously open"] # [doc = " connections that libcurl may keep in its connection cache after"] # [doc = " completed use. By default libcurl will enlarge the size for each added"] # [doc = " easy handle to make it fit 4 times the number of added easy handles."] # [doc = ""] # [doc = " By setting this option, you can prevent the cache size from growing"] # [doc = " beyond the limit set by you."] # [doc = ""] # [doc = " When the cache is full, curl closes the oldest one in the cache to"] # [doc = " prevent the number of open connections from increasing."] # [doc = ""] # [doc = " See [`set_max_total_connections`](#method.set_max_total_connections) for"] # [doc = " limiting the number of active connections."] pub fn set_max_connects (& mut self , val : usize) -> Result < () , MultiError > { self . setopt_long (curl_sys :: CURLMOPT_MAXCONNECTS , val as c_long) } # [doc = " Sets the pipeline length."] # [doc = ""] # [doc = " This sets the max number that will be used as the maximum amount of"] # [doc = " outstanding requests in an HTTP/1.1 pipelined connection. This option"] # [doc = " is only used for HTTP/1.1 pipelining, and not HTTP/2 multiplexing."] pub fn set_pipeline_length (& mut self , val : usize) -> Result < () , MultiError > { self . setopt_long (curl_sys :: CURLMOPT_MAX_PIPELINE_LENGTH , val as c_long) } # [doc = " Sets the number of max concurrent streams for http2."] # [doc = ""] # [doc = " This sets the max number will be used as the maximum number of"] # [doc = " concurrent streams for a connections that libcurl should support on"] # [doc = " connections done using HTTP/2. Defaults to 100."] pub fn set_max_concurrent_streams (& mut self , val : usize) -> Result < () , MultiError > { self . setopt_long (curl_sys :: CURLMOPT_MAX_CONCURRENT_STREAMS , val as c_long) } fn setopt_long (& mut self , opt : curl_sys :: CURLMoption , val : c_long) -> Result < () , MultiError > { unsafe { cvt (curl_sys :: curl_multi_setopt (self . raw . handle , opt , val)) } } fn setopt_ptr (& mut self , opt : curl_sys :: CURLMoption , val : * const c_char ,) -> Result < () , MultiError > { unsafe { cvt (curl_sys :: curl_multi_setopt (self . raw . handle , opt , val)) } } # [doc = " Add an easy handle to a multi session"] # [doc = ""] # [doc = " Adds a standard easy handle to the multi stack. This function call will"] # [doc = " make this multi handle control the specified easy handle."] # [doc = ""] # [doc = " When an easy interface is added to a multi handle, it will use a shared"] # [doc = " connection cache owned by the multi handle. Removing and adding new easy"] # [doc = " handles will not affect the pool of connections or the ability to do"] # [doc = " connection re-use."] # [doc = ""] # [doc = " If you have `timer_function` set in the multi handle (and you really"] # [doc = " should if you're working event-based with `action` and friends), that"] # [doc = " callback will be called from within this function to ask for an updated"] # [doc = " timer so that your main event loop will get the activity on this handle"] # [doc = " to get started."] # [doc = ""] # [doc = " The easy handle will remain added to the multi handle until you remove"] # [doc = " it again with `remove` on the returned handle - even when a transfer"] # [doc = " with that specific easy handle is completed."] pub fn add (& self , mut easy : Easy) -> Result < EasyHandle , MultiError > { easy . transfer () ; unsafe { cvt (curl_sys :: curl_multi_add_handle (self . raw . handle , easy . raw ())) ? ; } Ok (EasyHandle { guard : DetachGuard { multi : self . raw . clone () , easy : easy . raw () , } , easy , _marker : marker :: PhantomData , }) } # [doc = " Same as `add`, but works with the `Easy2` type."] pub fn add2 < H > (& self , easy : Easy2 < H >) -> Result < Easy2Handle < H > , MultiError > { unsafe { cvt (curl_sys :: curl_multi_add_handle (self . raw . handle , easy . raw ())) ? ; } Ok (Easy2Handle { guard : DetachGuard { multi : self . raw . clone () , easy : easy . raw () , } , easy , _marker : marker :: PhantomData , }) } # [doc = " Remove an easy handle from this multi session"] # [doc = ""] # [doc = " Removes the easy handle from this multi handle. This will make the"] # [doc = " returned easy handle be removed from this multi handle's control."] # [doc = ""] # [doc = " When the easy handle has been removed from a multi stack, it is again"] # [doc = " perfectly legal to invoke `perform` on it."] # [doc = ""] # [doc = " Removing an easy handle while being used is perfectly legal and will"] # [doc = " effectively halt the transfer in progress involving that easy handle."] # [doc = " All other easy handles and transfers will remain unaffected."] pub fn remove (& self , mut easy : EasyHandle) -> Result < Easy , MultiError > { easy . guard . detach () ? ; Ok (easy . easy) } # [doc = " Same as `remove`, but for `Easy2Handle`."] pub fn remove2 < H > (& self , mut easy : Easy2Handle < H >) -> Result < Easy2 < H > , MultiError > { easy . guard . detach () ? ; Ok (easy . easy) } # [doc = " Read multi stack informationals"] # [doc = ""] # [doc = " Ask the multi handle if there are any messages/informationals from the"] # [doc = " individual transfers. Messages may include informationals such as an"] # [doc = " error code from the transfer or just the fact that a transfer is"] # [doc = " completed. More details on these should be written down as well."] pub fn messages < F > (& self , mut f : F) where F : FnMut (Message) , { self . _messages (& mut f) } fn _messages (& self , f : & mut dyn FnMut (Message)) { let mut queue = 0 ; unsafe { loop { let ptr = curl_sys :: curl_multi_info_read (self . raw . handle , & mut queue) ; if ptr . is_null () { break ; } f (Message { ptr , _multi : self }) } } } # [doc = " Inform of reads/writes available data given an action"] # [doc = ""] # [doc = " When the application has detected action on a socket handled by libcurl,"] # [doc = " it should call this function with the sockfd argument set to"] # [doc = " the socket with the action. When the events on a socket are known, they"] # [doc = " can be passed `events`. When the events on a socket are unknown, pass"] # [doc = " `Events::new()` instead, and libcurl will test the descriptor"] # [doc = " internally."] # [doc = ""] # [doc = " The returned integer will contain the number of running easy handles"] # [doc = " within the multi handle. When this number reaches zero, all transfers"] # [doc = " are complete/done. When you call `action` on a specific socket and the"] # [doc = " counter decreases by one, it DOES NOT necessarily mean that this exact"] # [doc = " socket/transfer is the one that completed. Use `messages` to figure out"] # [doc = " which easy handle that completed."] # [doc = ""] # [doc = " The `action` function informs the application about updates in the"] # [doc = " socket (file descriptor) status by doing none, one, or multiple calls to"] # [doc = " the socket callback function set with the `socket_function` method. They"] # [doc = " update the status with changes since the previous time the callback was"] # [doc = " called."] pub fn action (& self , socket : Socket , events : & Events) -> Result < u32 , MultiError > { let mut remaining = 0 ; unsafe { cvt (curl_sys :: curl_multi_socket_action (self . raw . handle , socket , events . bits , & mut remaining ,)) ? ; Ok (remaining as u32) } } # [doc = " Inform libcurl that a timeout has expired and sockets should be tested."] # [doc = ""] # [doc = " The returned integer will contain the number of running easy handles"] # [doc = " within the multi handle. When this number reaches zero, all transfers"] # [doc = " are complete/done. When you call `action` on a specific socket and the"] # [doc = " counter decreases by one, it DOES NOT necessarily mean that this exact"] # [doc = " socket/transfer is the one that completed. Use `messages` to figure out"] # [doc = " which easy handle that completed."] # [doc = ""] # [doc = " Get the timeout time by calling the `timer_function` method. Your"] # [doc = " application will then get called with information on how long to wait"] # [doc = " for socket actions at most before doing the timeout action: call the"] # [doc = " `timeout` method. You can also use the `get_timeout` function to"] # [doc = " poll the value at any given time, but for an event-based system using"] # [doc = " the callback is far better than relying on polling the timeout value."] pub fn timeout (& self) -> Result < u32 , MultiError > { let mut remaining = 0 ; unsafe { cvt (curl_sys :: curl_multi_socket_action (self . raw . handle , curl_sys :: CURL_SOCKET_BAD , 0 , & mut remaining ,)) ? ; Ok (remaining as u32) } } # [doc = " Get how long to wait for action before proceeding"] # [doc = ""] # [doc = " An application using the libcurl multi interface should call"] # [doc = " `get_timeout` to figure out how long it should wait for socket actions -"] # [doc = " at most - before proceeding."] # [doc = ""] # [doc = " Proceeding means either doing the socket-style timeout action: call the"] # [doc = " `timeout` function, or call `perform` if you're using the simpler and"] # [doc = " older multi interface approach."] # [doc = ""] # [doc = " The timeout value returned is the duration at this very moment. If 0, it"] # [doc = " means you should proceed immediately without waiting for anything. If it"] # [doc = " returns `None`, there's no timeout at all set."] # [doc = ""] # [doc = " Note: if libcurl returns a `None` timeout here, it just means that"] # [doc = " libcurl currently has no stored timeout value. You must not wait too"] # [doc = " long (more than a few seconds perhaps) before you call `perform` again."] pub fn get_timeout (& self) -> Result < Option < Duration > , MultiError > { let mut ms = 0 ; unsafe { cvt (curl_sys :: curl_multi_timeout (self . raw . handle , & mut ms)) ? ; if ms == - 1 { Ok (None) } else { Ok (Some (Duration :: from_millis (ms as u64))) } } } # [doc = " Block until activity is detected or a timeout passes."] # [doc = ""] # [doc = " The timeout is used in millisecond-precision. Large durations are"] # [doc = " clamped at the maximum value curl accepts."] # [doc = ""] # [doc = " The returned integer will contain the number of internal file"] # [doc = " descriptors on which interesting events occured."] # [doc = ""] # [doc = " This function is a simpler alternative to using `fdset()` and `select()`"] # [doc = " and does not suffer from file descriptor limits."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use curl::multi::Multi;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " let m = Multi::new();"] # [doc = ""] # [doc = " // Add some Easy handles..."] # [doc = ""] # [doc = " while m.perform().unwrap() > 0 {"] # [doc = "     m.wait(&mut [], Duration::from_secs(1)).unwrap();"] # [doc = " }"] # [doc = " ```"] pub fn wait (& self , waitfds : & mut [WaitFd] , timeout : Duration) -> Result < u32 , MultiError > { let timeout_ms = Multi :: timeout_i32 (timeout) ; unsafe { let mut ret = 0 ; cvt (curl_sys :: curl_multi_wait (self . raw . handle , waitfds . as_mut_ptr () as * mut _ , waitfds . len () as u32 , timeout_ms , & mut ret ,)) ? ; Ok (ret as u32) } } fn timeout_i32 (timeout : Duration) -> i32 { let secs = timeout . as_secs () ; if secs > (i32 :: MAX / 1000) as u64 { i32 :: MAX } else { secs as i32 * 1000 + timeout . subsec_nanos () as i32 / 1_000_000 } } # [doc = " Block until activity is detected or a timeout passes."] # [doc = ""] # [doc = " The timeout is used in millisecond-precision. Large durations are"] # [doc = " clamped at the maximum value curl accepts."] # [doc = ""] # [doc = " The returned integer will contain the number of internal file"] # [doc = " descriptors on which interesting events occurred."] # [doc = ""] # [doc = " This function is a simpler alternative to using `fdset()` and `select()`"] # [doc = " and does not suffer from file descriptor limits."] # [doc = ""] # [doc = " While this method is similar to [Multi::wait], with the following"] # [doc = " distinctions:"] # [doc = " * If there are no handles added to the multi, poll will honor the"] # [doc = " provided timeout, while [Multi::wait] returns immediately."] # [doc = " * If poll has blocked due to there being no activity on the handles in"] # [doc = " the Multi, it can be woken up from any thread and at any time before"] # [doc = " the timeout expires."] # [doc = ""] # [doc = " Requires libcurl 7.66.0 or later."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use curl::multi::Multi;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " let m = Multi::new();"] # [doc = ""] # [doc = " // Add some Easy handles..."] # [doc = ""] # [doc = " while m.perform().unwrap() > 0 {"] # [doc = "     m.poll(&mut [], Duration::from_secs(1)).unwrap();"] # [doc = " }"] # [doc = " ```"] # [cfg (feature = "poll_7_68_0")] pub fn poll (& self , waitfds : & mut [WaitFd] , timeout : Duration) -> Result < u32 , MultiError > { let timeout_ms = Multi :: timeout_i32 (timeout) ; unsafe { let mut ret = 0 ; cvt (curl_sys :: curl_multi_poll (self . raw . handle , waitfds . as_mut_ptr () as * mut _ , waitfds . len () as u32 , timeout_ms , & mut ret ,)) ? ; Ok (ret as u32) } } # [doc = " Returns a new [MultiWaker] that can be used to wake up a thread that's"] # [doc = " currently blocked in [Multi::poll]."] # [cfg (feature = "poll_7_68_0")] pub fn waker (& self) -> MultiWaker { MultiWaker :: new (Arc :: downgrade (& self . raw)) } # [doc = " Reads/writes available data from each easy handle."] # [doc = ""] # [doc = " This function handles transfers on all the added handles that need"] # [doc = " attention in an non-blocking fashion."] # [doc = ""] # [doc = " When an application has found out there's data available for this handle"] # [doc = " or a timeout has elapsed, the application should call this function to"] # [doc = " read/write whatever there is to read or write right now etc.  This"] # [doc = " method returns as soon as the reads/writes are done. This function does"] # [doc = " not require that there actually is any data available for reading or"] # [doc = " that data can be written, it can be called just in case. It will return"] # [doc = " the number of handles that still transfer data."] # [doc = ""] # [doc = " If the amount of running handles is changed from the previous call (or"] # [doc = " is less than the amount of easy handles you've added to the multi"] # [doc = " handle), you know that there is one or more transfers less \"running\"."] # [doc = " You can then call `info` to get information about each individual"] # [doc = " completed transfer, and that returned info includes `Error` and more."] # [doc = " If an added handle fails very quickly, it may never be counted as a"] # [doc = " running handle."] # [doc = ""] # [doc = " When running_handles is set to zero (0) on the return of this function,"] # [doc = " there is no longer any transfers in progress."] # [doc = ""] # [doc = " # Return"] # [doc = ""] # [doc = " Before libcurl version 7.20.0: If you receive `is_call_perform`, this"] # [doc = " basically means that you should call `perform` again, before you select"] # [doc = " on more actions. You don't have to do it immediately, but the return"] # [doc = " code means that libcurl may have more data available to return or that"] # [doc = " there may be more data to send off before it is \"satisfied\". Do note"] # [doc = " that `perform` will return `is_call_perform` only when it wants to be"] # [doc = " called again immediately. When things are fine and there is nothing"] # [doc = " immediate it wants done, it'll return `Ok` and you need to wait for"] # [doc = " \"action\" and then call this function again."] # [doc = ""] # [doc = " This function only returns errors etc regarding the whole multi stack."] # [doc = " Problems still might have occurred on individual transfers even when"] # [doc = " this function returns `Ok`. Use `info` to figure out how individual"] # [doc = " transfers did."] pub fn perform (& self) -> Result < u32 , MultiError > { unsafe { let mut ret = 0 ; cvt (curl_sys :: curl_multi_perform (self . raw . handle , & mut ret)) ? ; Ok (ret as u32) } } # [doc = " Extracts file descriptor information from a multi handle"] # [doc = ""] # [doc = " This function extracts file descriptor information from a given"] # [doc = " handle, and libcurl returns its `fd_set` sets. The application can use"] # [doc = " these to `select()` on, but be sure to `FD_ZERO` them before calling"] # [doc = " this function as curl_multi_fdset only adds its own descriptors, it"] # [doc = " doesn't zero or otherwise remove any others. The curl_multi_perform"] # [doc = " function should be called as soon as one of them is ready to be read"] # [doc = " from or written to."] # [doc = ""] # [doc = " If no file descriptors are set by libcurl, this function will return"] # [doc = " `Ok(None)`. Otherwise `Ok(Some(n))` will be returned where `n` the"] # [doc = " highest descriptor number libcurl set. When `Ok(None)` is returned it"] # [doc = " is because libcurl currently does something that isn't possible for"] # [doc = " your application to monitor with a socket and unfortunately you can"] # [doc = " then not know exactly when the current action is completed using"] # [doc = " `select()`. You then need to wait a while before you proceed and call"] # [doc = " `perform` anyway."] # [doc = ""] # [doc = " When doing `select()`, you should use `get_timeout` to figure out"] # [doc = " how long to wait for action. Call `perform` even if no activity has"] # [doc = " been seen on the `fd_set`s after the timeout expires as otherwise"] # [doc = " internal retries and timeouts may not work as you'd think and want."] # [doc = ""] # [doc = " If one of the sockets used by libcurl happens to be larger than what"] # [doc = " can be set in an `fd_set`, which on POSIX systems means that the file"] # [doc = " descriptor is larger than `FD_SETSIZE`, then libcurl will try to not"] # [doc = " set it. Setting a too large file descriptor in an `fd_set` implies an out"] # [doc = " of bounds write which can cause crashes, or worse. The effect of NOT"] # [doc = " storing it will possibly save you from the crash, but will make your"] # [doc = " program NOT wait for sockets it should wait for..."] pub fn fdset2 (& self , read : Option < & mut curl_sys :: fd_set > , write : Option < & mut curl_sys :: fd_set > , except : Option < & mut curl_sys :: fd_set > ,) -> Result < Option < i32 > , MultiError > { unsafe { let mut ret = 0 ; let read = read . map (| r | r as * mut _) . unwrap_or (ptr :: null_mut ()) ; let write = write . map (| r | r as * mut _) . unwrap_or (ptr :: null_mut ()) ; let except = except . map (| r | r as * mut _) . unwrap_or (ptr :: null_mut ()) ; cvt (curl_sys :: curl_multi_fdset (self . raw . handle , read , write , except , & mut ret ,)) ? ; if ret == - 1 { Ok (None) } else { Ok (Some (ret)) } } } # [doc = " Does nothing and returns `Ok(())`. This method remains for backwards"] # [doc = " compatibility."] # [doc = ""] # [doc = " This method will be changed to take `self` in a future release."] # [doc (hidden)] # [deprecated (since = "0.4.30" , note = "cannot close safely without consuming self; \
                will be changed or removed in a future release")] pub fn close (& self) -> Result < () , MultiError > { Ok (()) } # [doc = " Get a pointer to the raw underlying CURLM handle."] pub fn raw (& self) -> * mut curl_sys :: CURLM { self . raw . handle } }
    };
}

impl_130!();