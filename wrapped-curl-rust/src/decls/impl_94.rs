macro_rules! deps {
    () => {
        SslOpt!();
    };
}

macro_rules! impl_94 {
    () => {
        deps!();
        impl SslOpt { # [doc = " Creates a new set of SSL options."] pub fn new () -> SslOpt { SslOpt { bits : 0 } } # [doc = " Tell libcurl to automatically locate and use a client certificate for authentication,"] # [doc = " when requested by the server."] # [doc = ""] # [doc = " This option is only supported for Schannel (the native Windows SSL library)."] # [doc = " Prior to 7.77.0 this was the default behavior in libcurl with Schannel."] # [doc = ""] # [doc = " Since the server can request any certificate that supports client authentication in"] # [doc = " the OS certificate store it could be a privacy violation and unexpected. (Added in 7.77.0)"] pub fn auto_client_cert (& mut self , on : bool) -> & mut SslOpt { self . flag (curl_sys :: CURLSSLOPT_AUTO_CLIENT_CERT , on) } # [doc = " Tell libcurl to use the operating system's native CA store for certificate verification."] # [doc = ""] # [doc = " Works only on Windows when built to use OpenSSL."] # [doc = ""] # [doc = " This option is experimental and behavior is subject to change. (Added in 7.71.0)"] pub fn native_ca (& mut self , on : bool) -> & mut SslOpt { self . flag (curl_sys :: CURLSSLOPT_NATIVE_CA , on) } # [doc = " Tells libcurl to ignore certificate revocation checks in case of missing or"] # [doc = " offline distribution points for those SSL backends where such behavior is present."] # [doc = ""] # [doc = " This option is only supported for Schannel (the native Windows SSL library)."] # [doc = ""] # [doc = " If combined with CURLSSLOPT_NO_REVOKE, the latter takes precedence. (Added in 7.70.0)"] pub fn revoke_best_effort (& mut self , on : bool) -> & mut SslOpt { self . flag (curl_sys :: CURLSSLOPT_REVOKE_BEST_EFFORT , on) } # [doc = " Tells libcurl to not accept \"partial\" certificate chains, which it otherwise does by default."] # [doc = ""] # [doc = " This option is only supported for OpenSSL and will fail the certificate verification"] # [doc = " if the chain ends with an intermediate certificate and not with a root cert."] # [doc = " (Added in 7.68.0)"] pub fn no_partial_chain (& mut self , on : bool) -> & mut SslOpt { self . flag (curl_sys :: CURLSSLOPT_NO_PARTIALCHAIN , on) } # [doc = " Tells libcurl to disable certificate revocation checks for those SSL"] # [doc = " backends where such behavior is present."] # [doc = ""] # [doc = " Currently this option is only supported for WinSSL (the native Windows"] # [doc = " SSL library), with an exception in the case of Windows' Untrusted"] # [doc = " Publishers blacklist which it seems can't be bypassed. This option may"] # [doc = " have broader support to accommodate other SSL backends in the future."] # [doc = " <https://curl.haxx.se/docs/ssl-compared.html>"] pub fn no_revoke (& mut self , on : bool) -> & mut SslOpt { self . flag (curl_sys :: CURLSSLOPT_NO_REVOKE , on) } # [doc = " Tells libcurl to not attempt to use any workarounds for a security flaw"] # [doc = " in the SSL3 and TLS1.0 protocols."] # [doc = ""] # [doc = " If this option isn't used or this bit is set to 0, the SSL layer libcurl"] # [doc = " uses may use a work-around for this flaw although it might cause"] # [doc = " interoperability problems with some (older) SSL implementations."] # [doc = ""] # [doc = " > WARNING: avoiding this work-around lessens the security, and by"] # [doc = " > setting this option to 1 you ask for exactly that. This option is only"] # [doc = " > supported for DarwinSSL, NSS and OpenSSL."] pub fn allow_beast (& mut self , on : bool) -> & mut SslOpt { self . flag (curl_sys :: CURLSSLOPT_ALLOW_BEAST , on) } fn flag (& mut self , bit : c_long , on : bool) -> & mut SslOpt { if on { self . bits |= bit as c_long ; } else { self . bits &= ! bit as c_long ; } self } }
    };
}

impl_94!();