macro_rules! PostRedirections {
    () => {
        # [doc = " Structure which stores possible post redirection options to pass to `post_redirections`."] pub struct PostRedirections { bits : c_ulong , }
    };
}

PostRedirections!();