macro_rules! deps {
    () => {
        Case!();
        Pattern!();
    };
}

macro_rules! impl_4 {
    () => {
        deps!();
        # [doc = " Access"] impl Pattern { # [doc = " Return true if a match is negated."] pub fn is_negative (& self) -> bool { self . mode . contains (Mode :: NEGATIVE) } # [doc = " Match the given `path` which takes slashes (and only slashes) literally, and is relative to the repository root."] # [doc = " Note that `path` is assumed to be relative to the repository."] # [doc = ""] # [doc = " We may take various shortcuts which is when `basename_start_pos` and `is_dir` come into play."] # [doc = " `basename_start_pos` is the index at which the `path`'s basename starts."] # [doc = ""] # [doc = " `case` folding can be configured as well."] # [doc = " `mode` is used to control how [`crate::wildmatch()`] should operate."] pub fn matches_repo_relative_path (& self , path : & BStr , basename_start_pos : Option < usize > , is_dir : Option < bool > , case : Case , mode : wildmatch :: Mode ,) -> bool { let is_dir = is_dir . unwrap_or (false) ; if ! is_dir && self . mode . contains (pattern :: Mode :: MUST_BE_DIR) { return false ; } let flags = mode | match case { Case :: Fold => wildmatch :: Mode :: IGNORE_CASE , Case :: Sensitive => wildmatch :: Mode :: empty () , } ; # [cfg (debug_assertions)] { if basename_start_pos . is_some () { debug_assert_eq ! (basename_start_pos , path . rfind_byte (b'/') . map (| p | p + 1) , "BUG: invalid cached basename_start_pos provided") ; } } debug_assert ! (! path . starts_with (b"/") , "input path must be relative") ; if self . mode . contains (pattern :: Mode :: NO_SUB_DIR) && ! self . mode . contains (pattern :: Mode :: ABSOLUTE) { let basename = & path [basename_start_pos . unwrap_or_default () ..] ; self . matches (basename , flags) } else { self . matches (path , flags) } } # [doc = " See if `value` matches this pattern in the given `mode`."] # [doc = ""] # [doc = " `mode` can identify `value` as path which won't match the slash character, and can match"] # [doc = " strings with cases ignored as well. Note that the case folding performed here is ASCII only."] # [doc = ""] # [doc = " Note that this method uses some shortcuts to accelerate simple patterns, but falls back to"] # [doc = " [wildmatch()][crate::wildmatch()] if these fail."] pub fn matches (& self , value : & BStr , mode : wildmatch :: Mode) -> bool { match self . first_wildcard_pos { Some (pos) if self . mode . contains (pattern :: Mode :: ENDS_WITH) && (! mode . contains (wildmatch :: Mode :: NO_MATCH_SLASH_LITERAL) || ! value . contains (& b'/')) => { let text = & self . text [pos + 1 ..] ; if mode . contains (wildmatch :: Mode :: IGNORE_CASE) { value . len () . checked_sub (text . len ()) . is_some_and (| start | text . eq_ignore_ascii_case (& value [start ..])) } else { value . ends_with (text . as_ref ()) } } Some (pos) => { if mode . contains (wildmatch :: Mode :: IGNORE_CASE) { if ! value . get (.. pos) . is_some_and (| value | value . eq_ignore_ascii_case (& self . text [.. pos])) { return false ; } } else if ! value . starts_with (& self . text [.. pos]) { return false ; } crate :: wildmatch (self . text . as_bstr () , value , mode) } None => { if mode . contains (wildmatch :: Mode :: IGNORE_CASE) { self . text . eq_ignore_ascii_case (value) } else { self . text == value } } } } }
    };
}

impl_4!()