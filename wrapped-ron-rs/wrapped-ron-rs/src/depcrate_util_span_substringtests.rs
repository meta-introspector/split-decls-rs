// Generated macro for tests (module)
macro_rules! Depcrate_util_span_substringtests {
() => {
// Module: crate::util::span_substring
// Provides: {"tests"}
// Dependencies: {}
# [cfg (test)] mod tests { use crate :: de :: { Position , Span } ; fn span (start : Position , end : Position) -> Span { Span { start , end } } fn pos (line : usize , col : usize) -> Position { Position { line , col } } # [test] fn ascii_basics () { let text = "hello\nworld" ; assert_eq ! (pos (1 , 1) . grapheme_index (text) , Some (0)) ; assert_eq ! (pos (1 , 5) . grapheme_index (text) , Some (4)) ; assert_eq ! (pos (2 , 1) . grapheme_index (text) , Some (6)) ; assert_eq ! (span (pos (1 , 4) , pos (2 , 2)) . substring_exclusive (text) . unwrap () , "lo\nw") ; } # [test] fn multibyte_greek () { let text = "αβγ\ndeux\n三四五\r\nend" ; assert_eq ! (pos (1 , 2) . grapheme_index (text) , Some (1)) ; assert_eq ! (pos (3 , 1) . grapheme_index (text) , Some (9)) ; assert_eq ! (pos (4 , 1) . grapheme_index (text) , Some (13)) ; assert_eq ! (span (pos (1 , 1) , pos (2 , 1)) . substring_exclusive (text) . unwrap () , "αβγ\n") ; } # [test] fn combining_mark_cluster () { let text = "e\u{0301}x\n" ; assert_eq ! (pos (1 , 1) . grapheme_index (text) , Some (0)) ; assert_eq ! (pos (1 , 2) . grapheme_index (text) , Some (1)) ; assert_eq ! (pos (1 , 4) . grapheme_index (text) , None) ; assert_eq ! (span (pos (1 , 1) , pos (1 , 2)) . substring_exclusive (text) . unwrap () , "e\u{0301}") ; } # [test] fn zwj_emoji_cluster () { let text = "👩‍👩‍👧‍👧 and 👨‍👩‍👦" ; assert_eq ! (pos (1 , 1) . grapheme_index (text) , Some (0)) ; assert_eq ! (pos (1 , 2) . grapheme_index (text) , Some (1)) ; assert_eq ! (span (pos (1 , 1) , pos (1 , 2)) . substring_exclusive (text) . unwrap () , "👩‍👩‍👧‍👧") ; assert_eq ! (span (pos (1 , 7) , pos (1 , 8)) . substring_exclusive (text) . unwrap () , "👨‍👩‍👦") ; } # [test] fn mixed_newlines () { let text = "one\r\ntwo\nthree\r\n" ; assert_eq ! (pos (2 , 1) . grapheme_index (text) , Some (4)) ; assert_eq ! (pos (3 , 1) . grapheme_index (text) , Some (8)) ; assert_eq ! (span (pos (2 , 1) , pos (3 , 1)) . substring_exclusive (text) . unwrap () , "two\n") ; assert_eq ! (span (pos (2 , 1) , pos (3 , 6)) . substring_exclusive (text) . unwrap () , "two\nthree") ; } # [test] fn oob_and_error_paths () { let text = "short" ; assert_eq ! (pos (2 , 1) . grapheme_index (text) , None) ; assert_eq ! (pos (1 , 10) . grapheme_index (text) , None) ; assert_eq ! (span (pos (1 , 1) , pos (2 , 1)) . substring_exclusive (text) , None) ; } # [test] fn whole_text_span () { let text = "αβγ\nδεζ" ; let all = span (pos (1 , 1) , pos (2 , 4)) ; assert_eq ! (& all . substring_exclusive (text) . unwrap () , text) ; } # [test] fn span_substring_helper () { assert_eq ! (Span { start : Position { line : 1 , col : 1 } , end : Position { line : 2 , col : 1 } , } . substring_exclusive ("In the first place, there are two sorts of bets, or toh.11 There is the
single axial bet in the center between the principals (toh ketengah), and
there is the cloud of peripheral ones around the ring between members
of the audience (toh kesasi). " ,) . unwrap () , "In the first place, there are two sorts of bets, or toh.11 There is the\n") ; assert_eq ! (Span { start : Position { line : 2 , col : 1 } , end : Position { line : 3 , col : 1 } , } . substring_exclusive ("In the first place, there are two sorts of bets, or toh.11 There is the
single axial bet in the center between the principals (toh ketengah), and
there is the cloud of peripheral ones around the ring between members
of the audience (toh kesasi). " ,) . unwrap () , "single axial bet in the center between the principals (toh ketengah), and\n") ; } }
};
}
