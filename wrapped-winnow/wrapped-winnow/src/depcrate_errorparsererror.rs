// Generated macro for ParserError (trait)
macro_rules! Depcrate_errorParserError {
() => {
// Module: crate::error
// Provides: {"ParserError"}
// Dependencies: {}
# [doc = " The basic [`Parser`] trait for errors"] # [doc = ""] # [doc = " It provides methods to create an error from some combinators,"] # [doc = " and combine existing errors in combinators like `alt`."] pub trait ParserError < I : Stream > : Sized { # [doc = " Generally, `Self`"] # [doc = ""] # [doc = " Mostly used for [`ErrMode`]"] type Inner ; # [doc = " Creates an error from the input position"] fn from_input (input : & I) -> Self ; # [doc = " Process a parser assertion"] # [inline (always)] fn assert (input : & I , _message : & 'static str) -> Self where I : core :: fmt :: Debug , { # [cfg (debug_assertions)] panic ! ("assert `{_message}` failed at {input:#?}") ; # [cfg (not (debug_assertions))] Self :: from_input (input) } # [doc = " There was not enough data to determine the appropriate action"] # [doc = ""] # [doc = " More data needs to be buffered before retrying the parse."] # [doc = ""] # [doc = " This must only be set when the [`Stream`] is [partial][`crate::stream::StreamIsPartial`], like with"] # [doc = " [`Partial`][crate::Partial]"] # [doc = ""] # [doc = " Convert this into an `Backtrack` with [`Parser::complete_err`]"] # [inline (always)] fn incomplete (input : & I , _needed : Needed) -> Self { Self :: from_input (input) } # [doc = " Like [`ParserError::from_input`] but merges it with the existing error."] # [doc = ""] # [doc = " This is useful when backtracking through a parse tree, accumulating error context on the"] # [doc = " way."] # [inline] fn append (self , _input : & I , _token_start : & < I as Stream > :: Checkpoint) -> Self { self } # [doc = " Combines errors from two different parse branches."] # [doc = ""] # [doc = " For example, this would be used by [`alt`][crate::combinator::alt] to report the error from"] # [doc = " each case."] # [inline] fn or (self , other : Self) -> Self { other } # [doc = " Is backtracking and trying new parse branches allowed?"] # [inline (always)] fn is_backtrack (& self) -> bool { true } # [doc = " Unwrap the mode, returning the underlying error, if present"] fn into_inner (self) -> Result < Self :: Inner , Self > ; # [doc = " Is more data [`Needed`]"] # [doc = ""] # [doc = " This must be the same as [`err.needed().is_some()`][ParserError::needed]"] # [inline (always)] fn is_incomplete (& self) -> bool { false } # [doc = " Extract the [`Needed`] data, if present"] # [doc = ""] # [doc = " `Self::needed().is_some()` must be the same as"] # [doc = " [`err.is_incomplete()`][ParserError::is_incomplete]"] # [inline (always)] fn needed (& self) -> Option < Needed > { None } }
};
}
