// Generated macro for Partial (struct)
macro_rules! Depcrate_stream_partialPartial {
() => {
// Module: crate::stream::partial
// Provides: {"Partial"}
// Dependencies: {}
# [doc = " Mark the input as a partial buffer for streaming input."] # [doc = ""] # [doc = " Complete input means that we already have all of the data. This will be the common case with"] # [doc = " small files that can be read entirely to memory."] # [doc = ""] # [doc = " In contrast, streaming input assumes that we might not have all of the data."] # [doc = " This can happen with some network protocol or large file parsers, where the"] # [doc = " input buffer can be full and need to be resized or refilled."] # [doc = " - [`ErrMode::Incomplete`][crate::error::ErrMode::Incomplete] will report how much more data is needed."] # [doc = " - [`Parser::complete_err`][crate::Parser::complete_err] transform"] # [doc = "   [`ErrMode::Incomplete`][crate::error::ErrMode::Incomplete] to"] # [doc = "   [`ErrMode::Backtrack`][crate::error::ErrMode::Backtrack]"] # [doc = ""] # [doc = " See also [`StreamIsPartial`] to tell whether the input supports complete or partial parsing."] # [doc = ""] # [doc = " See also [Special Topics: Parsing Partial Input][crate::_topic::partial]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Here is how it works in practice:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use winnow::{Result, error::ErrMode, error::Needed, error::ContextError, token, ascii, stream::Partial};"] # [doc = " # use winnow::prelude::*;"] # [doc = ""] # [doc = " fn take_partial<'s>(i: &mut Partial<&'s [u8]>) -> ModalResult<&'s [u8], ContextError> {"] # [doc = "   token::take(4u8).parse_next(i)"] # [doc = " }"] # [doc = ""] # [doc = " fn take_complete<'s>(i: &mut &'s [u8]) -> ModalResult<&'s [u8], ContextError> {"] # [doc = "   token::take(4u8).parse_next(i)"] # [doc = " }"] # [doc = ""] # [doc = " // both parsers will take 4 bytes as expected"] # [doc = " assert_eq!(take_partial.parse_peek(Partial::new(&b\"abcde\"[..])), Ok((Partial::new(&b\"e\"[..]), &b\"abcd\"[..])));"] # [doc = " assert_eq!(take_complete.parse_peek(&b\"abcde\"[..]), Ok((&b\"e\"[..], &b\"abcd\"[..])));"] # [doc = ""] # [doc = " // if the input is smaller than 4 bytes, the partial parser"] # [doc = " // will return `Incomplete` to indicate that we need more data"] # [doc = " assert_eq!(take_partial.parse_peek(Partial::new(&b\"abc\"[..])), Err(ErrMode::Incomplete(Needed::new(1))));"] # [doc = ""] # [doc = " // but the complete parser will return an error"] # [doc = " assert!(take_complete.parse_peek(&b\"abc\"[..]).is_err());"] # [doc = ""] # [doc = " // the alpha0 function takes 0 or more alphabetic characters"] # [doc = " fn alpha0_partial<'s>(i: &mut Partial<&'s str>) -> ModalResult<&'s str, ContextError> {"] # [doc = "   ascii::alpha0.parse_next(i)"] # [doc = " }"] # [doc = ""] # [doc = " fn alpha0_complete<'s>(i: &mut &'s str) -> ModalResult<&'s str, ContextError> {"] # [doc = "   ascii::alpha0.parse_next(i)"] # [doc = " }"] # [doc = ""] # [doc = " // if there's a clear limit to the taken characters, both parsers work the same way"] # [doc = " assert_eq!(alpha0_partial.parse_peek(Partial::new(\"abcd;\")), Ok((Partial::new(\";\"), \"abcd\")));"] # [doc = " assert_eq!(alpha0_complete.parse_peek(\"abcd;\"), Ok((\";\", \"abcd\")));"] # [doc = ""] # [doc = " // but when there's no limit, the partial version returns `Incomplete`, because it cannot"] # [doc = " // know if more input data should be taken. The whole input could be \"abcd;\", or"] # [doc = " // \"abcde;\""] # [doc = " assert_eq!(alpha0_partial.parse_peek(Partial::new(\"abcd\")), Err(ErrMode::Incomplete(Needed::new(1))));"] # [doc = ""] # [doc = " // while the complete version knows that all of the data is there"] # [doc = " assert_eq!(alpha0_complete.parse_peek(\"abcd\"), Ok((\"\", \"abcd\")));"] # [doc = " ```"] # [derive (Copy , Clone , Debug , PartialEq , Eq , PartialOrd , Ord)] pub struct Partial < I > { input : I , partial : bool , }
};
}
