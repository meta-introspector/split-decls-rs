// Generated macro for Stream (trait)
macro_rules! Depcrate_streamStream {
() => {
// Module: crate::stream
// Provides: {"Stream"}
// Dependencies: {}
# [doc = " Core definition for parser input state"] pub trait Stream : Offset < < Self as Stream > :: Checkpoint > + core :: fmt :: Debug { # [doc = " The smallest unit being parsed"] # [doc = ""] # [doc = " Example: `u8` for `&[u8]` or `char` for `&str`"] type Token : core :: fmt :: Debug ; # [doc = " Sequence of `Token`s"] # [doc = ""] # [doc = " Example: `&[u8]` for `LocatingSlice<&[u8]>` or `&str` for `LocatingSlice<&str>`"] type Slice : core :: fmt :: Debug ; # [doc = " Iterate with the offset from the current location"] type IterOffsets : Iterator < Item = (usize , Self :: Token) > ; # [doc = " A parse location within the stream"] type Checkpoint : Offset + Clone + core :: fmt :: Debug ; # [doc = " Iterate with the offset from the current location"] fn iter_offsets (& self) -> Self :: IterOffsets ; # [doc = " Returns the offset to the end of the input"] fn eof_offset (& self) -> usize ; # [doc = " Split off the next token from the input"] fn next_token (& mut self) -> Option < Self :: Token > ; # [doc = " Split off the next token from the input"] fn peek_token (& self) -> Option < Self :: Token > ; # [doc = " Finds the offset of the next matching token"] fn offset_for < P > (& self , predicate : P) -> Option < usize > where P : Fn (Self :: Token) -> bool ; # [doc = " Get the offset for the number of `tokens` into the stream"] # [doc = ""] # [doc = " This means \"0 tokens\" will return `0` offset"] fn offset_at (& self , tokens : usize) -> Result < usize , Needed > ; # [doc = " Split off a slice of tokens from the input"] # [doc = ""] # [doc = " <div class=\"warning\">"] # [doc = ""] # [doc = " **Note:** For inputs with variable width tokens, like `&str`'s `char`, `offset` might not correspond"] # [doc = " with the number of tokens. To get a valid offset, use:"] # [doc = " - [`Stream::eof_offset`]"] # [doc = " - [`Stream::iter_offsets`]"] # [doc = " - [`Stream::offset_for`]"] # [doc = " - [`Stream::offset_at`]"] # [doc = ""] # [doc = " </div>"] # [doc = ""] # [doc = " # Panic"] # [doc = ""] # [doc = " This will panic if"] # [doc = ""] # [doc = " * Indexes must be within bounds of the original input;"] # [doc = " * Indexes must uphold invariants of the stream, like for `str` they must lie on UTF-8"] # [doc = "   sequence boundaries."] # [doc = ""] fn next_slice (& mut self , offset : usize) -> Self :: Slice ; # [doc = " Split off a slice of tokens from the input"] # [doc = ""] # [doc = " <div class=\"warning\">"] # [doc = ""] # [doc = " **Note:** For inputs with variable width tokens, like `&str`'s `char`, `offset` might not correspond"] # [doc = " with the number of tokens. To get a valid offset, use:"] # [doc = " - [`Stream::eof_offset`]"] # [doc = " - [`Stream::iter_offsets`]"] # [doc = " - [`Stream::offset_for`]"] # [doc = " - [`Stream::offset_at`]"] # [doc = ""] # [doc = " </div>"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Callers of this function are responsible that these preconditions are satisfied:"] # [doc = ""] # [doc = " * Indexes must be within bounds of the original input;"] # [doc = " * Indexes must uphold invariants of the stream, like for `str` they must lie on UTF-8"] # [doc = "   sequence boundaries."] # [doc = ""] unsafe fn next_slice_unchecked (& mut self , offset : usize) -> Self :: Slice { self . next_slice (offset) } # [doc = " Split off a slice of tokens from the input"] fn peek_slice (& self , offset : usize) -> Self :: Slice ; # [doc = " Split off a slice of tokens from the input"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Callers of this function are responsible that these preconditions are satisfied:"] # [doc = ""] # [doc = " * Indexes must be within bounds of the original input;"] # [doc = " * Indexes must uphold invariants of the stream, like for `str` they must lie on UTF-8"] # [doc = "   sequence boundaries."] unsafe fn peek_slice_unchecked (& self , offset : usize) -> Self :: Slice { self . peek_slice (offset) } # [doc = " Advance to the end of the stream"] # [inline (always)] fn finish (& mut self) -> Self :: Slice { self . next_slice (self . eof_offset ()) } # [doc = " Advance to the end of the stream"] # [inline (always)] fn peek_finish (& self) -> Self :: Slice where Self : Clone , { self . peek_slice (self . eof_offset ()) } # [doc = " Save the current parse location within the stream"] fn checkpoint (& self) -> Self :: Checkpoint ; # [doc = " Revert the stream to a prior [`Self::Checkpoint`]"] # [doc = ""] # [doc = " # Panic"] # [doc = ""] # [doc = " May panic if an invalid [`Self::Checkpoint`] is provided"] fn reset (& mut self , checkpoint : & Self :: Checkpoint) ; # [doc = " Deprecated for callers as of 0.7.10, instead call [`Stream::trace`]"] # [deprecated (since = "0.7.10" , note = "Replaced with `Stream::trace`")] fn raw (& self) -> & dyn core :: fmt :: Debug ; # [doc = " Write out a single-line summary of the current parse location"] fn trace (& self , f : & mut core :: fmt :: Formatter < '_ >) -> core :: fmt :: Result { # ! [allow (deprecated)] write ! (f , "{:#?}" , self . raw ()) } }
};
}
