// Generated macro for take (function)
macro_rules! Depcrate_tokentake {
() => {
// Module: crate::token
// Provides: {"take"}
// Dependencies: {}
# [doc = " Recognize an input slice containing the first N input elements (I[..N])."] # [doc = ""] # [doc = " *Complete version*: It will return `Err(ErrMode::Backtrack(_))` if the input is shorter than the argument."] # [doc = ""] # [doc = " *[Partial version][crate::_topic::partial]*: if the input has less than N elements, `take` will"] # [doc = " return a `ErrMode::Incomplete(Needed::new(M))` where M is the number of"] # [doc = " additional bytes the parser would need to succeed."] # [doc = " It is well defined for `&[u8]` as the number of elements is the byte size,"] # [doc = " but for types like `&str`, we cannot know how many bytes correspond for"] # [doc = " the next few chars, so the result will be `ErrMode::Incomplete(Needed::Unknown)`"] # [doc = ""] # [doc = " # Effective Signature"] # [doc = ""] # [doc = " Assuming you are parsing a `&str` [Stream] with `0..` or `1..` ranges:"] # [doc = " ```rust"] # [doc = " # use std::ops::RangeFrom;"] # [doc = " # use winnow::prelude::*;"] # [doc = " # use winnow::stream::ContainsToken;"] # [doc = " # use winnow::error::ContextError;"] # [doc = " pub fn take<'i>(token_count: usize) -> impl Parser<&'i str, &'i str, ContextError>"] # [doc = " # {"] # [doc = " #     winnow::token::take(token_count)"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use winnow::{error::ErrMode, error::ContextError, error::Needed};"] # [doc = " # use winnow::prelude::*;"] # [doc = " use winnow::token::take;"] # [doc = ""] # [doc = " fn take6<'i>(s: &mut &'i str) -> ModalResult<&'i str> {"] # [doc = "   take(6usize).parse_next(s)"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(take6.parse_peek(\"1234567\"), Ok((\"7\", \"123456\")));"] # [doc = " assert_eq!(take6.parse_peek(\"things\"), Ok((\"\", \"things\")));"] # [doc = " assert!(take6.parse_peek(\"short\").is_err());"] # [doc = " assert!(take6.parse_peek(\"\").is_err());"] # [doc = " ```"] # [doc = ""] # [doc = " The units that are taken will depend on the input type. For example, for a"] # [doc = " `&str` it will take a number of `char`'s, whereas for a `&[u8]` it will"] # [doc = " take that many `u8`'s:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use winnow::prelude::*;"] # [doc = " use winnow::error::ContextError;"] # [doc = " use winnow::token::take;"] # [doc = ""] # [doc = " assert_eq!(take::<_, _, ContextError>(1usize).parse_peek(\"💙\"), Ok((\"\", \"💙\")));"] # [doc = " assert_eq!(take::<_, _, ContextError>(1usize).parse_peek(\"💙\".as_bytes()), Ok((b\"\\x9F\\x92\\x99\".as_ref(), b\"\\xF0\".as_ref())));"] # [doc = " ```"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use winnow::prelude::*;"] # [doc = " # use winnow::error::{ErrMode, ContextError, Needed};"] # [doc = " # use winnow::Partial;"] # [doc = " use winnow::token::take;"] # [doc = ""] # [doc = " fn take6<'i>(s: &mut Partial<&'i str>) -> ModalResult<&'i str> {"] # [doc = "   take(6usize).parse_next(s)"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(take6.parse_peek(Partial::new(\"1234567\")), Ok((Partial::new(\"7\"), \"123456\")));"] # [doc = " assert_eq!(take6.parse_peek(Partial::new(\"things\")), Ok((Partial::new(\"\"), \"things\")));"] # [doc = " // `Unknown` as we don't know the number of bytes that `count` corresponds to"] # [doc = " assert_eq!(take6.parse_peek(Partial::new(\"short\")), Err(ErrMode::Incomplete(Needed::Unknown)));"] # [doc = " ```"] # [inline (always)] pub fn take < UsizeLike , Input , Error > (token_count : UsizeLike ,) -> impl Parser < Input , < Input as Stream > :: Slice , Error > where Input : StreamIsPartial + Stream , UsizeLike : ToUsize , Error : ParserError < Input > , { let c = token_count . to_usize () ; trace ("take" , move | i : & mut Input | { if < Input as StreamIsPartial > :: is_partial_supported () { take_ :: < _ , _ , true > (i , c) } else { take_ :: < _ , _ , false > (i , c) } }) }
};
}
