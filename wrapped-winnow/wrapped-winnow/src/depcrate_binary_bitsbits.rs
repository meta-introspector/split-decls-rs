// Generated macro for bits (function)
macro_rules! Depcrate_binary_bitsbits {
() => {
// Module: crate::binary::bits
// Provides: {"bits"}
// Dependencies: {}
# [doc = " Converts a byte-level input to a bit-level input"] # [doc = ""] # [doc = " See [`bytes`] to convert it back."] # [doc = ""] # [doc = " # Example"] # [doc = " ```rust"] # [doc = " # use winnow::prelude::*;"] # [doc = " # use winnow::Bytes;"] # [doc = " # use winnow::binary::bits::{bits, take};"] # [doc = " # use winnow::error::ContextError;"] # [doc = " # use winnow::error::ErrMode;"] # [doc = " type Stream<'i> = &'i Bytes;"] # [doc = ""] # [doc = " fn stream(b: &[u8]) -> Stream<'_> {"] # [doc = "     Bytes::new(b)"] # [doc = " }"] # [doc = ""] # [doc = " fn parse(input: &mut Stream<'_>) -> ModalResult<(u8, u8)> {"] # [doc = "     bits::<_, _, ErrMode<ContextError>, _, _>((take(4usize), take(8usize))).parse_next(input)"] # [doc = " }"] # [doc = ""] # [doc = " let input = stream(&[0x12, 0x34, 0xff, 0xff]);"] # [doc = ""] # [doc = " let output = parse.parse_peek(input).expect(\"We take 1.5 bytes and the input is longer than 2 bytes\");"] # [doc = ""] # [doc = " // The first byte is consumed, the second byte is partially consumed and dropped."] # [doc = " let remaining = output.0;"] # [doc = " assert_eq!(remaining, stream(&[0xff, 0xff]));"] # [doc = ""] # [doc = " let parsed = output.1;"] # [doc = " assert_eq!(parsed.0, 0x01);"] # [doc = " assert_eq!(parsed.1, 0x23);"] # [doc = " ```"] pub fn bits < Input , Output , BitError , ByteError , ParseNext > (mut parser : ParseNext ,) -> impl Parser < Input , Output , ByteError > where BitError : ParserError < (Input , usize) > + ErrorConvert < ByteError > , ByteError : ParserError < Input > , (Input , usize) : Stream , Input : Stream + Clone , ParseNext : Parser < (Input , usize) , Output , BitError > , { trace ("bits" , move | input : & mut Input | { let mut bit_input = (input . clone () , 0) ; match parser . parse_next (& mut bit_input) { Ok (result) => { let (mut rest , offset) = bit_input ; let remaining_bytes_index = offset / BYTE + if offset % BYTE == 0 { 0 } else { 1 } ; let _ = rest . next_slice (remaining_bytes_index) ; * input = rest ; Ok (result) } Err (e) => match e . needed () { Some (n) => Err (ParserError :: incomplete (input , n . map (| u | u . get () / BYTE + 1) ,)) , None => Err (ErrorConvert :: convert (e)) , } , } }) }
};
}
