// Generated macro for impl_91 (impl)
macro_rules! Depcrate_provider_exceptionsimpl_91 {
() => {
// Module: crate::provider::exceptions
// Provides: {"impl_91"}
// Dependencies: {}
impl ExceptionULE { # [inline] fn empty_exception () -> & 'static Self { static EMPTY_BYTES : & [u8] = & [0 , 0] ; unsafe { let slice : * const [u8] = ptr :: slice_from_raw_parts (EMPTY_BYTES . as_ptr () , 0) ; & * (slice as * const Self) } } pub (crate) fn has_slot (& self , slot : ExceptionSlot) -> bool { self . slot_presence . has_slot (slot) } # [doc = " Obtain a `char` slot, if occupied. If `slot` represents a string slot,"] # [doc = " will return `None`"] pub (crate) fn get_char_slot (& self , slot : ExceptionSlot) -> Option < char > { if slot >= ExceptionSlot :: STRING_SLOTS_START { return None ; } let bit = 1 << (slot as u8) ; if self . slot_presence . 0 & bit == 0 { return None ; } let previous_slot_mask = bit - 1 ; let previous_slots = self . slot_presence . 0 & previous_slot_mask ; let slot_num = previous_slots . count_ones () as usize ; self . data . chars () . nth (slot_num) } # [doc = " Get the `simple_case` delta (i.e. the `delta` slot), given the character"] # [doc = " this data belongs to."] # [doc = ""] # [doc = " Normalizes the delta from char-format to u32 format"] # [doc = ""] # [doc = " Does *not* handle the sign of the delta; see self.bits.negative_delta"] fn get_simple_case_delta (& self) -> Option < u32 > { let delta_ch = self . get_char_slot (ExceptionSlot :: Delta) ? ; let mut delta = u32 :: from (delta_ch) ; if delta >= SURROGATES_START { delta -= SURROGATES_LEN ; } Some (delta) } # [doc = " Get the `simple_case` value (i.e. the `delta` slot), given the character"] # [doc = " this data belongs to."] # [doc = ""] # [doc = " The data is stored as a delta so the character must be provided."] # [doc = ""] # [doc = " The data cannot be stored directly as a character because the trie is more"] # [doc = " compact with adjacent characters sharing deltas."] pub (crate) fn get_simple_case_slot_for (& self , ch : char) -> Option < char > { let delta = self . get_simple_case_delta () ? ; let mut delta = i32 :: try_from (delta) . ok () ? ; if self . bits . negative_delta () { delta = - delta ; } let new_ch = i32 :: try_from (u32 :: from (ch)) . ok () ? + delta ; char :: try_from (u32 :: try_from (new_ch) . ok () ?) . ok () } # [doc = " Returns *all* the data in the closure/full slots, including length metadata"] fn get_stringy_data (& self) -> Option < & str > { const CHAR_MASK : u8 = (1 << ExceptionSlot :: STRING_SLOTS_START as u8) - 1 ; let char_slot_count = (self . slot_presence . 0 & CHAR_MASK) . count_ones () as usize ; let mut chars = self . data . chars () ; for _ in 0 .. char_slot_count { let res = chars . next () ; res ? ; } Some (chars . as_str ()) } # [doc = " Returns a single stringy slot, either ExceptionSlot::Closure"] # [doc = " or ExceptionSlot::FullMappings."] fn get_stringy_slot (& self , slot : ExceptionSlot) -> Option < & str > { debug_assert ! (slot == ExceptionSlot :: Closure || slot == ExceptionSlot :: FullMappings) ; let other_slot = if slot == ExceptionSlot :: Closure { ExceptionSlot :: FullMappings } else { ExceptionSlot :: Closure } ; if ! self . slot_presence . has_slot (slot) { return None ; } let stringy_data = self . get_stringy_data () ? ; if self . slot_presence . has_slot (other_slot) { let mut chars = stringy_data . chars () ; let length_char = chars . next () ? ; let length = usize :: try_from (u32 :: from (length_char)) . unwrap_or (0) ; let remaining_slice = chars . as_str () ; if slot == ExceptionSlot :: Closure { remaining_slice . get (0 .. length) } else { remaining_slice . get (length ..) } } else { Some (stringy_data) } } # [doc = " Get the data behind the `closure` slot"] pub (crate) fn get_closure_slot (& self) -> Option < & str > { self . get_stringy_slot (ExceptionSlot :: Closure) } # [doc = " Get all the slot data for the FullMappings slot"] # [doc = ""] # [doc = " This needs to be further segmented into four based on length metadata"] fn get_fullmappings_slot_data (& self) -> Option < & str > { self . get_stringy_slot (ExceptionSlot :: FullMappings) } # [doc = " Get a specific FullMappings slot value"] pub (crate) fn get_fullmappings_slot_for_kind (& self , kind : MappingKind) -> Option < & str > { let data = self . get_fullmappings_slot_data () ? ; let mut chars = data . chars () ; let i1 = usize :: try_from (u32 :: from (chars . next () ?)) . ok () ? ; let i2 = usize :: try_from (u32 :: from (chars . next () ?)) . ok () ? ; let i3 = usize :: try_from (u32 :: from (chars . next () ?)) . ok () ? ; let remaining_slice = chars . as_str () ; match kind { MappingKind :: Lower => remaining_slice . get (.. i1) , MappingKind :: Fold => remaining_slice . get (i1 .. i2) , MappingKind :: Upper => remaining_slice . get (i2 .. i3) , MappingKind :: Title => remaining_slice . get (i3 ..) , } } fn get_all_fullmapping_slots (& self) -> Option < [Cow < '_ , str > ; 4] > { Some ([self . get_fullmappings_slot_for_kind (MappingKind :: Lower) ? . into () , self . get_fullmappings_slot_for_kind (MappingKind :: Fold) ? . into () , self . get_fullmappings_slot_for_kind (MappingKind :: Upper) ? . into () , self . get_fullmappings_slot_for_kind (MappingKind :: Title) ? . into () ,]) } # [inline] pub (crate) fn slot_char_for_kind (& self , kind : MappingKind) -> Option < char > { match kind { MappingKind :: Lower | MappingKind :: Upper => self . get_char_slot (kind . into ()) , MappingKind :: Fold => self . get_char_slot (ExceptionSlot :: Fold) . or_else (| | self . get_char_slot (ExceptionSlot :: Lower)) , MappingKind :: Title => self . get_char_slot (ExceptionSlot :: Title) . or_else (| | self . get_char_slot (ExceptionSlot :: Upper)) , } } pub (crate) fn add_full_and_closure_mappings < S : ClosureSink > (& self , set : & mut S) { if let Some (full) = self . get_fullmappings_slot_for_kind (MappingKind :: Fold) { if ! full . is_empty () { set . add_string (full) ; } } ; if let Some (closure) = self . get_closure_slot () { for c in closure . chars () { set . add_char (c) ; } } ; } # [doc = " Extract all the data out into a structured form"] # [doc = ""] # [doc = " Useful for serialization and debugging"] pub fn decode (& self) -> DecodedException < '_ > { let bits = self . bits ; let lowercase = self . get_char_slot (ExceptionSlot :: Lower) ; let casefold = self . get_char_slot (ExceptionSlot :: Fold) ; let uppercase = self . get_char_slot (ExceptionSlot :: Upper) ; let titlecase = self . get_char_slot (ExceptionSlot :: Title) ; let simple_case_delta = self . get_simple_case_delta () ; let closure = self . get_closure_slot () . map (Into :: into) ; let full = self . get_all_fullmapping_slots () ; DecodedException { bits : ExceptionBits :: from_unaligned (bits) , lowercase , casefold , uppercase , titlecase , simple_case_delta , closure , full , } } # [cfg (any (feature = "serde" , feature = "datagen"))] pub (crate) fn validate (& self) -> Result < () , & 'static str > { if self . bits . double_width_slots () { return Err ("double-width-slots should not be used in ICU4C") ; } let decoded = self . decode () ; for (slot , decoded_slot) in [(ExceptionSlot :: Lower , & decoded . lowercase) , (ExceptionSlot :: Fold , & decoded . casefold) , (ExceptionSlot :: Upper , & decoded . uppercase) , (ExceptionSlot :: Title , & decoded . titlecase) ,] { if self . has_slot (slot) && decoded_slot . is_none () { return Err ("Slot decoding failed") ; } } if self . has_slot (ExceptionSlot :: Delta) && decoded . simple_case_delta . is_none () { return Err ("Slot decoding failed") ; } if self . has_slot (ExceptionSlot :: Closure) && decoded . closure . is_none () { return Err ("Slot decoding failed") ; } if self . has_slot (ExceptionSlot :: FullMappings) { if decoded . full . is_some () { let data = self . get_fullmappings_slot_data () . ok_or ("fullmappings slot doesn't parse") ? ; let mut chars = data . chars () ; let i1 = u32 :: from (chars . next () . ok_or ("fullmappings string too small") ?) ; let i2 = u32 :: from (chars . next () . ok_or ("fullmappings string too small") ?) ; let i3 = u32 :: from (chars . next () . ok_or ("fullmappings string too small") ?) ; if i2 < i1 || i3 < i2 { return Err ("fullmappings string contains non-sequential indices") ; } let rest = chars . as_str () ; let len = u32 :: try_from (rest . len ()) . map_err (| _ | "len too large for u32") ? ; if i1 > len || i2 > len || i3 > len { return Err ("fullmappings string contains out-of-bounds indices") ; } } else { return Err ("Slot decoding failed") ; } } Ok (()) } }
};
}
