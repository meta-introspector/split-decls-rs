// Generated macro for impl_23 (impl)
macro_rules! Depcrateimpl_23 {
() => {
// Module: crate
// Provides: {"impl_23"}
// Dependencies: {}
impl < T > VecList < T > { # [doc = " Returns an immutable reference to the value at the back of the list, if it exists."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " assert_eq!(list.back(), None);"] # [doc = ""] # [doc = " list.push_back(0);"] # [doc = " list.push_back(5);"] # [doc = " assert_eq!(list.back(), Some(&5));"] # [doc = " ```"] # [must_use] pub fn back (& self) -> Option < & T > { let index = self . tail ? . get () ; match & self . entries [index] { Entry :: Occupied (entry) => Some (& entry . value) , _ => None , } } # [doc = " Returns the index of the value at the back of the list, if it exists."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " assert_eq!(list.back_index(), None);"] # [doc = ""] # [doc = " list.push_back(0);"] # [doc = " let index = list.push_back(5);"] # [doc = " assert_eq!(list.back_index(), Some(index));"] # [doc = " ```"] # [must_use] pub fn back_index (& self) -> Option < Index < T > > { let index = self . tail ? ; let entry = self . entries [index . get ()] . occupied_ref () ; let index = Index :: new (index , entry . generation) ; Some (index) } # [doc = " Returns a mutable reference to the value at the back of the list, if it exists."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " assert_eq!(list.back_mut(), None);"] # [doc = ""] # [doc = " list.push_back(0);"] # [doc = " list.push_back(5);"] # [doc = ""] # [doc = " let mut back = list.back_mut().unwrap();"] # [doc = " assert_eq!(back, &mut 5);"] # [doc = " *back *= 2;"] # [doc = ""] # [doc = " assert_eq!(list.back(), Some(&10));"] # [doc = " ```"] # [must_use] pub fn back_mut (& mut self) -> Option < & mut T > { let index = self . tail ? . get () ; match & mut self . entries [index] { Entry :: Occupied (entry) => Some (& mut entry . value) , _ => None , } } # [doc = " Returns the capacity of the list."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let list: VecList<u32> = VecList::new();"] # [doc = " assert_eq!(list.capacity(), 0);"] # [doc = ""] # [doc = " let list: VecList<u32> = VecList::with_capacity(10);"] # [doc = " assert_eq!(list.capacity(), 10);"] # [doc = " ```"] # [must_use] pub fn capacity (& self) -> usize { self . entries . capacity () } # [doc = " Removes all values from the list and invalidates all existing indices."] # [doc = ""] # [doc = " Complexity: O(n)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = ""] # [doc = " list.push_back(5);"] # [doc = " assert!(!list.is_empty());"] # [doc = ""] # [doc = " list.clear();"] # [doc = " assert!(list.is_empty());"] # [doc = " ```"] pub fn clear (& mut self) { self . entries . clear () ; self . generation = self . generation . wrapping_add (1) ; self . head = None ; self . length = 0 ; self . tail = None ; self . vacant_head = None ; } # [doc = " Returns whether or not the list contains the given value."] # [doc = ""] # [doc = " Complexity: O(n)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " assert!(!list.contains(&0));"] # [doc = ""] # [doc = " list.push_back(0);"] # [doc = " assert!(list.contains(&0));"] # [doc = " ```"] # [must_use] pub fn contains (& self , value : & T) -> bool where T : PartialEq , { self . iter () . any (| entry | entry == value) } # [doc = " Creates a draining iterator that removes all values from the list and yields them in order."] # [doc = ""] # [doc = " All values are removed even if the iterator is only partially consumed or not consumed at all."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " list.push_back(0);"] # [doc = " list.push_back(5);"] # [doc = ""] # [doc = " {"] # [doc = "     let mut iter = list.drain();"] # [doc = "     assert_eq!(iter.next(), Some(0));"] # [doc = "     assert_eq!(iter.next(), Some(5));"] # [doc = "     assert_eq!(iter.next(), None);"] # [doc = " }"] # [doc = ""] # [doc = " println!(\"{}\", list.len());"] # [doc = " assert!(list.is_empty());"] # [doc = " ```"] pub fn drain (& mut self) -> Drain < '_ , T > { Drain { head : self . head , remaining : self . length , tail : self . tail , list : self , } } # [doc = " Returns an immutable reference to the value at the front of the list, if it exists."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " assert_eq!(list.front(), None);"] # [doc = ""] # [doc = " list.push_front(0);"] # [doc = " list.push_front(5);"] # [doc = " assert_eq!(list.front(), Some(&5));"] # [doc = " ```"] # [must_use] pub fn front (& self) -> Option < & T > { let index = self . head ? . get () ; match & self . entries [index] { Entry :: Occupied (entry) => Some (& entry . value) , _ => None , } } # [doc = " Returns the index of the value at the front of the list, if it exists."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " assert_eq!(list.front_index(), None);"] # [doc = ""] # [doc = " list.push_front(0);"] # [doc = " let index = list.push_front(5);"] # [doc = " assert_eq!(list.front_index(), Some(index));"] # [doc = " ```"] # [must_use] pub fn front_index (& self) -> Option < Index < T > > { let index = self . head ? ; let entry = self . entries [index . get ()] . occupied_ref () ; let index = Index :: new (index , entry . generation) ; Some (index) } # [doc = " Returns a mutable reference to the value at the front of the list, if it exists."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " assert_eq!(list.front_mut(), None);"] # [doc = ""] # [doc = " list.push_front(0);"] # [doc = " list.push_front(5);"] # [doc = ""] # [doc = " let mut front = list.front_mut().unwrap();"] # [doc = " assert_eq!(front, &mut 5);"] # [doc = " *front *= 2;"] # [doc = ""] # [doc = " assert_eq!(list.front(), Some(&10));"] # [doc = " ```"] # [must_use] pub fn front_mut (& mut self) -> Option < & mut T > { let index = self . head ? . get () ; match & mut self . entries [index] { Entry :: Occupied (entry) => Some (& mut entry . value) , _ => None , } } # [doc = " Returns an immutable reference to the value at the given index."] # [doc = ""] # [doc = " If the index refers to an index not in the list anymore or if the index has been invalidated, then [`None`] will"] # [doc = " be returned."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " let index = list.push_front(0);"] # [doc = " assert_eq!(list.get(index), Some(&0));"] # [doc = ""] # [doc = " let index = list.push_front(5);"] # [doc = " assert_eq!(list.get(index), Some(&5));"] # [doc = " ```"] # [must_use] pub fn get (& self , index : Index < T >) -> Option < & T > { match self . entries . get (index . index ()) ? { Entry :: Occupied (entry) if entry . generation == index . generation => Some (& entry . value) , _ => None , } } # [doc = " Returns an immutable reference to the value at the given index."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Caller needs to guarantee that the index is in bound, and has never been removed from the"] # [doc = " list. This function does not perform generation checks. So if an element is removed then a"] # [doc = " new element is added at the same index, then the returned reference will be to the new"] # [doc = " element."] # [must_use] pub unsafe fn get_unchecked (& self , index : Index < T >) -> & T { match unsafe { self . entries . get_unchecked (index . index ()) } { Entry :: Occupied (entry) => & entry . value , _ => unsafe { unreachable_unchecked () } , } } # [doc = " Returns a mutable reference to the value at the given index."] # [doc = ""] # [doc = " If the index refers to an index not in the list anymore or if the index has been invalidated, then [`None`] will"] # [doc = " be returned."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " let index = list.push_front(0);"] # [doc = " let value = list.get_mut(index).unwrap();"] # [doc = " *value = 100;"] # [doc = " assert_eq!(list.get(index), Some(&100));"] # [doc = " ```"] # [must_use] pub fn get_mut (& mut self , index : Index < T >) -> Option < & mut T > { match self . entries . get_mut (index . index ()) ? { Entry :: Occupied (entry) if entry . generation == index . generation => Some (& mut entry . value) , _ => None , } } # [doc = " Returns an mutable reference to the value at the given index."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Caller needs to guarantee that the index is in bound, and has never been removed from the list."] # [doc = " See also: [`VecList::get_unchecked`]."] # [doc = ""] # [doc = " Complexity: O(1)"] # [must_use] pub unsafe fn get_unchecked_mut (& mut self , index : Index < T >) -> & mut T { match unsafe { self . entries . get_unchecked_mut (index . index ()) } { Entry :: Occupied (entry) => & mut entry . value , _ => unsafe { unreachable_unchecked () } , } } # [doc = " Returns the index of the value next to the value at the given index."] # [doc = ""] # [doc = " If the index refers to an index not in the list anymore or if the index has been invalidated, then [`None`] will"] # [doc = " be returned."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = ""] # [doc = " let index_1 = list.push_back(0);"] # [doc = " assert_eq!(list.get_next_index(index_1), None);"] # [doc = ""] # [doc = " let index_2 = list.push_back(5);"] # [doc = " assert_eq!(list.get_next_index(index_1), Some(index_2));"] # [doc = " ```"] # [must_use] pub fn get_next_index (& self , index : Index < T >) -> Option < Index < T > > { match self . entries . get (index . index ()) ? { Entry :: Occupied (entry) if entry . generation == index . generation => { let next_index = entry . next ? ; let next_entry = self . entries [next_index . get ()] . occupied_ref () ; Some (Index :: new (next_index , next_entry . generation)) } _ => None , } } # [doc = " Returns the index of the value previous to the value at the given index."] # [doc = ""] # [doc = " If the index refers to an index not in the list anymore or if the index has been invalidated, then [`None`] will"] # [doc = " be returned."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = ""] # [doc = " let index_1 = list.push_front(0);"] # [doc = " assert_eq!(list.get_previous_index(index_1), None);"] # [doc = ""] # [doc = " let index_2 = list.push_front(5);"] # [doc = " assert_eq!(list.get_previous_index(index_1), Some(index_2));"] # [doc = " ```"] # [must_use] pub fn get_previous_index (& self , index : Index < T >) -> Option < Index < T > > { match self . entries . get (index . index ()) ? { Entry :: Occupied (entry) if entry . generation == index . generation => { let previous_index = entry . previous ? ; let previous_entry = self . entries [previous_index . get ()] . occupied_ref () ; Some (Index :: new (previous_index , previous_entry . generation)) } _ => None , } } # [doc = " Connect the node at `index` to the node at `next`. If `index` is `None`, then the head will be"] # [doc = " set to `next`; if `next` is `None`, then the tail will be set to `index`."] # [inline] fn update_link (& mut self , index : Option < NonMaxUsize > , next : Option < NonMaxUsize >) { if let Some (index) = index { let entry = self . entries [index . get ()] . occupied_mut () ; entry . next = next ; } else { self . head = next } if let Some (next) = next { let entry = self . entries [next . get ()] . occupied_mut () ; entry . previous = index ; } else { self . tail = index ; } } # [doc = " Move the node at `index` to after the node at `target`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if either `index` or `target` is invalidated. Also panics if `index` is the same as `target`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " let index_1 = list.push_back(0);"] # [doc = " let index_2 = list.push_back(1);"] # [doc = " let index_3 = list.push_back(2);"] # [doc = " let index_4 = list.push_back(3);"] # [doc = ""] # [doc = " list.move_after(index_1, index_3);"] # [doc = " assert_eq!(list.iter().copied().collect::<Vec<_>>(), vec![1, 2, 0, 3]);"] # [doc = " assert_eq!(list.iter().rev().copied().collect::<Vec<_>>(), vec![3, 0, 2, 1]);"] # [doc = " ```"] pub fn move_after (& mut self , index : Index < T > , target : Index < T >) { let (previous_index , next_index) = match & self . entries [index . index ()] { Entry :: Occupied (entry) if entry . generation == index . generation => { (entry . previous , entry . next) } _ => panic ! ("expected occupied entry with correct generation at `index`") , } ; let target_next_index = match & self . entries [target . index ()] { Entry :: Occupied (entry) if entry . generation == target . generation => entry . next , _ => panic ! ("expected occupied entry with correct generation at `target`") , } ; if target . index == index . index { panic ! ("cannot move after `index` itself") ; } if previous_index == Some (target . index) { return ; } self . update_link (previous_index , next_index) ; self . update_link (Some (target . index) , Some (index . index)) ; self . update_link (Some (index . index) , target_next_index) ; } # [doc = " Move the node at `index` to before the node at `target`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if either `index` or `target` is invalidated. Also panics if `index` is the same as `target`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " let index_1 = list.push_back(0);"] # [doc = " let index_2 = list.push_back(1);"] # [doc = " let index_3 = list.push_back(2);"] # [doc = " let index_4 = list.push_back(3);"] # [doc = ""] # [doc = " list.move_before(index_1, index_3);"] # [doc = " assert_eq!(list.iter().copied().collect::<Vec<_>>(), vec![1, 0, 2, 3]);"] # [doc = " assert_eq!(list.iter().rev().copied().collect::<Vec<_>>(), vec![3, 2, 0, 1]);"] # [doc = " ```"] pub fn move_before (& mut self , index : Index < T > , target : Index < T >) { let (previous_index , next_index) = match & self . entries [index . index ()] { Entry :: Occupied (entry) if entry . generation == index . generation => { (entry . previous , entry . next) } _ => panic ! ("expected occupied entry with correct generation at `index`") , } ; let target_previous_index = match & self . entries [target . index ()] { Entry :: Occupied (entry) if entry . generation == target . generation => entry . previous , _ => panic ! ("expected occupied entry with correct generation at `target`") , } ; if target . index == index . index { panic ! ("cannot move before `index` itself") ; } if next_index == Some (target . index) { return ; } self . update_link (previous_index , next_index) ; self . update_link (Some (index . index) , Some (target . index)) ; self . update_link (target_previous_index , Some (index . index)) ; } # [doc = " Creates an indices iterator which will yield all indices of the list in order."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " list.push_front(0);"] # [doc = " list.push_front(5);"] # [doc = ""] # [doc = " let mut indices = list.indices();"] # [doc = " let index = indices.next().unwrap();"] # [doc = " assert_eq!(list.get(index), Some(&5));"] # [doc = ""] # [doc = " let index = indices.next().unwrap();"] # [doc = " assert_eq!(list.get(index), Some(&0));"] # [doc = ""] # [doc = " assert_eq!(indices.next(), None);"] # [doc = " ```"] # [must_use] pub fn indices (& self) -> Indices < '_ , T > { Indices { entries : & self . entries , head : self . head , remaining : self . length , tail : self . tail , } } # [doc = " Inserts the given value after the value at the given index."] # [doc = ""] # [doc = " The index of the newly inserted value will be returned."] # [doc = ""] # [doc = " Complexity: amortized O(1)"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the index refers to an index not in the list anymore or if the index has been invalidated. This is"] # [doc = " enforced because this function will consume the value to be inserted, and if it cannot be inserted (due to the"] # [doc = " index not being valid), then it will be lost."] # [doc = ""] # [doc = " Also panics if the new capacity overflows `usize`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " list.push_front(0);"] # [doc = " let index_1 = list.push_front(5);"] # [doc = " list.push_front(10);"] # [doc = ""] # [doc = " let index_2 = list.insert_after(index_1, 1000);"] # [doc = " assert_eq!(list.get_next_index(index_1), Some(index_2));"] # [doc = " ```"] pub fn insert_after (& mut self , index : Index < T > , value : T) -> Index < T > { let next_index = match & mut self . entries [index . index ()] { Entry :: Occupied (entry) if entry . generation == index . generation => entry . next , _ => panic ! ("expected occupied entry with correct generation") , } ; let new_index = self . insert_new (value , Some (index . index) , next_index) ; let entry = self . entries [index . index ()] . occupied_mut () ; entry . next = Some (new_index) ; if Some (index . index) == self . tail { self . tail = Some (new_index) ; } if let Some (next_index) = next_index { self . entries [next_index . get ()] . occupied_mut () . previous = Some (new_index) ; } Index :: new (new_index , self . generation) } # [doc = " Inserts the given value before the value at the given index."] # [doc = ""] # [doc = " The index of the newly inserted value will be returned."] # [doc = ""] # [doc = " Complexity: amortized O(1)"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the index refers to an index not in the list anymore or if the index has been invalidated. This is"] # [doc = " enforced because this function will consume the value to be inserted, and if it cannot be inserted (due to the"] # [doc = " index not being valid), then it will be lost."] # [doc = ""] # [doc = " Also panics if the new capacity overflows `usize`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " list.push_front(0);"] # [doc = " let index_1 = list.push_front(5);"] # [doc = " list.push_front(10);"] # [doc = ""] # [doc = " let index_2 = list.insert_before(index_1, 1000);"] # [doc = " assert_eq!(list.get_previous_index(index_1), Some(index_2));"] # [doc = " ```"] pub fn insert_before (& mut self , index : Index < T > , value : T) -> Index < T > { let previous_index = match & mut self . entries [index . index ()] { Entry :: Occupied (entry) if entry . generation == index . generation => entry . previous , _ => panic ! ("expected occupied entry with correct generation") , } ; let new_index = self . insert_new (value , previous_index , Some (index . index)) ; let entry = self . entries [index . index ()] . occupied_mut () ; entry . previous = Some (new_index) ; if Some (index . index) == self . head { self . head = Some (new_index) ; } if let Some (previous_index) = previous_index { self . entries [previous_index . get ()] . occupied_mut () . next = Some (new_index) ; } Index :: new (new_index , self . generation) } # [doc = " Inserts the given value into the list with the assumption that it is currently empty."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity overflows `usize`."] fn insert_empty (& mut self , value : T) -> Index < T > { let generation = self . generation ; let index = self . insert_new (value , None , None) ; self . head = Some (index) ; self . tail = Some (index) ; Index :: new (index , generation) } # [doc = " Inserts the given value into the list with its expected previous and next value indices."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity overflows `usize`."] fn insert_new (& mut self , value : T , previous : Option < NonMaxUsize > , next : Option < NonMaxUsize > ,) -> NonMaxUsize { self . length += 1 ; if self . length == usize :: MAX { panic ! ("reached maximum possible length") ; } match self . vacant_head { Some (index) => { self . vacant_head = self . entries [index . get ()] . vacant_ref () . next ; self . entries [index . get ()] = Entry :: Occupied (OccupiedEntry :: new (self . generation , previous , next , value)) ; index } None => { self . entries . push (Entry :: Occupied (OccupiedEntry :: new (self . generation , previous , next , value ,))) ; NonMaxUsize :: new (self . entries . len () - 1) . unwrap () } } } # [doc = " Returns whether or not the list is empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " assert!(list.is_empty());"] # [doc = ""] # [doc = " list.push_back(0);"] # [doc = " assert!(!list.is_empty());"] # [doc = " ```"] # [must_use] pub fn is_empty (& self) -> bool { self . length == 0 } # [doc = " Creates an iterator that yields immutable references to values in the list in order."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " list.push_back(0);"] # [doc = " list.push_back(10);"] # [doc = " list.push_back(200);"] # [doc = " list.push_back(-10);"] # [doc = ""] # [doc = " let mut iter = list.iter();"] # [doc = " assert_eq!(iter.next(), Some(&0));"] # [doc = " assert_eq!(iter.next(), Some(&10));"] # [doc = " assert_eq!(iter.next(), Some(&200));"] # [doc = " assert_eq!(iter.next(), Some(&-10));"] # [doc = " assert_eq!(iter.next(), None);"] # [doc = " ```"] # [must_use] pub fn iter (& self) -> Iter < '_ , T > { Iter { entries : & self . entries , head : self . head , remaining : self . length , tail : self . tail , } } # [doc = " Creates an iterator that yields mutable references to values in the list in order."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " list.push_back(0);"] # [doc = " list.push_back(10);"] # [doc = " list.push_back(200);"] # [doc = " list.push_back(-10);"] # [doc = ""] # [doc = " let mut iter = list.iter_mut();"] # [doc = " assert_eq!(iter.next(), Some(&mut 0));"] # [doc = " assert_eq!(iter.next(), Some(&mut 10));"] # [doc = " assert_eq!(iter.next(), Some(&mut 200));"] # [doc = " assert_eq!(iter.next(), Some(&mut -10));"] # [doc = " assert_eq!(iter.next(), None);"] # [doc = " ```"] # [must_use] pub fn iter_mut (& mut self) -> IterMut < '_ , T > { IterMut { entries : & mut self . entries , head : self . head , phantom : PhantomData , remaining : self . length , tail : self . tail , } } # [doc = " Returns the number of values in the list."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " assert_eq!(list.len(), 0);"] # [doc = ""] # [doc = " list.push_back(0);"] # [doc = " list.push_back(1);"] # [doc = " list.push_back(2);"] # [doc = " assert_eq!(list.len(), 3);"] # [doc = " ```"] # [must_use] pub fn len (& self) -> usize { self . length } # [doc = " Creates a new list with no initial capacity."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " let index = list.push_back(0);"] # [doc = " assert_eq!(list.get(index), Some(&0));"] # [doc = " ```"] # [must_use] pub fn new () -> Self { VecList :: default () } # [doc = " Reorganizes the existing values to ensure maximum cache locality and shrinks the list such that the capacity is"] # [doc = " exactly [`minimum_capacity`]."] # [doc = ""] # [doc = " This function can be used to actually increase the capacity of the list."] # [doc = ""] # [doc = " Complexity: O(n)"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the given minimum capacity is less than the current length of the list."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " let index_1 = list.push_back(5);"] # [doc = " let index_2 = list.push_back(10);"] # [doc = " let index_3 = list.push_front(100);"] # [doc = " list.remove(index_1);"] # [doc = ""] # [doc = " assert!(list.capacity() >= 3);"] # [doc = ""] # [doc = " let mut map = list.pack_to(list.len() + 5);"] # [doc = " assert_eq!(list.capacity(), 7);"] # [doc = " assert_eq!(map.len(), 2);"] # [doc = ""] # [doc = " let index_2 = map.remove(&index_2).unwrap();"] # [doc = " let index_3 = map.remove(&index_3).unwrap();"] # [doc = ""] # [doc = " assert_eq!(list.get(index_2), Some(&10));"] # [doc = " assert_eq!(list.get(index_3), Some(&100));"] # [doc = ""] # [doc = " let mut iter = list.iter();"] # [doc = " assert_eq!(iter.next(), Some(&100));"] # [doc = " assert_eq!(iter.next(), Some(&10));"] # [doc = " assert_eq!(iter.next(), None);"] # [doc = " ```"] # [cfg (feature = "std")] pub fn pack_to (& mut self , minimum_capacity : usize) -> HashMap < Index < T > , Index < T > > { assert ! (minimum_capacity >= self . length , "cannot shrink to capacity lower than current length") ; let mut count = NonMaxUsize :: zero () ; let mut entries = Vec :: with_capacity (minimum_capacity) ; let generation = create_initial_generation () ; let length = self . length ; let mut map = HashMap :: with_capacity (length) ; let mut next_index = self . head ; while let Some (index) = next_index { let mut entry = self . remove_entry (index) . expect ("expected occupied entry") ; next_index = entry . next ; let _ = map . insert (Index :: new (index , entry . generation) , Index :: new (count , generation) ,) ; entry . generation = generation ; entry . previous = if count > 0 { Some (count . checked_sub (1) . unwrap ()) } else { None } ; entry . next = if count < length - 1 { Some (count . checked_add (1) . expect ("overflow")) } else { None } ; entries . push (Entry :: Occupied (entry)) ; count = count . checked_add (1) . expect ("overflow") ; } self . entries = entries ; self . generation = generation ; self . length = length ; self . vacant_head = None ; if self . length > 0 { self . head = Some (NonMaxUsize :: zero ()) ; self . tail = Some (unsafe { NonMaxUsize :: new_unchecked (length - 1) }) ; } else { self . head = None ; self . tail = None ; } map } # [doc = " Reorganizes the existing values to ensure maximum cache locality and shrinks the list such that no additional"] # [doc = " capacity exists."] # [doc = ""] # [doc = " This is equivalent to calling [`VecList::pack_to`] with the current length."] # [doc = ""] # [doc = " Complexity: O(n)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " let index_1 = list.push_back(5);"] # [doc = " let index_2 = list.push_back(10);"] # [doc = " let index_3 = list.push_front(100);"] # [doc = " list.remove(index_1);"] # [doc = ""] # [doc = " assert!(list.capacity() >= 3);"] # [doc = ""] # [doc = " let mut map = list.pack_to_fit();"] # [doc = " assert_eq!(list.capacity(), 2);"] # [doc = " assert_eq!(map.len(), 2);"] # [doc = ""] # [doc = " let index_2 = map.remove(&index_2).unwrap();"] # [doc = " let index_3 = map.remove(&index_3).unwrap();"] # [doc = ""] # [doc = " assert_eq!(list.get(index_2), Some(&10));"] # [doc = " assert_eq!(list.get(index_3), Some(&100));"] # [doc = ""] # [doc = " let mut iter = list.iter();"] # [doc = " assert_eq!(iter.next(), Some(&100));"] # [doc = " assert_eq!(iter.next(), Some(&10));"] # [doc = " assert_eq!(iter.next(), None);"] # [doc = " ```"] # [cfg (feature = "std")] pub fn pack_to_fit (& mut self) -> HashMap < Index < T > , Index < T > > { self . pack_to (self . length) } # [doc = " Removes and returns the value at the back of the list, if it exists."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " assert_eq!(list.pop_back(), None);"] # [doc = ""] # [doc = " list.push_back(0);"] # [doc = " list.push_back(1);"] # [doc = " list.push_back(2);"] # [doc = " assert_eq!(list.len(), 3);"] # [doc = ""] # [doc = " assert_eq!(list.pop_back(), Some(2));"] # [doc = " assert_eq!(list.len(), 2);"] # [doc = " ```"] pub fn pop_back (& mut self) -> Option < T > { self . remove_entry (self . tail ?) . map (| entry | entry . value) } # [doc = " Removes and returns the value at the front of the list, if it exists."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " assert_eq!(list.pop_front(), None);"] # [doc = ""] # [doc = " list.push_front(0);"] # [doc = " list.push_front(1);"] # [doc = " list.push_front(2);"] # [doc = " assert_eq!(list.len(), 3);"] # [doc = ""] # [doc = " assert_eq!(list.pop_front(), Some(2));"] # [doc = " assert_eq!(list.len(), 2);"] # [doc = " ```"] pub fn pop_front (& mut self) -> Option < T > { self . remove_entry (self . head ?) . map (| entry | entry . value) } # [doc = " Inserts the given value to the back of the list."] # [doc = ""] # [doc = " The index of the newly inserted value will be returned."] # [doc = ""] # [doc = " Complexity: amortized O(1)"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity overflows `usize`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " let index = list.push_back(0);"] # [doc = " assert_eq!(list.get(index), Some(&0));"] # [doc = " ```"] pub fn push_back (& mut self , value : T) -> Index < T > { let tail_index = match self . tail { Some (index) => index , None => return self . insert_empty (value) , } ; let index = self . insert_new (value , Some (tail_index) , None) ; self . entries [tail_index . get ()] . occupied_mut () . next = Some (index) ; self . tail = Some (index) ; Index :: new (index , self . generation) } # [doc = " Inserts the given value to the front of the list."] # [doc = ""] # [doc = " The index of the newly inserted value will be returned."] # [doc = ""] # [doc = " Complexity: amortized O(1)"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity overflows `usize`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " let index = list.push_front(0);"] # [doc = " assert_eq!(list.get(index), Some(&0));"] # [doc = " ```"] pub fn push_front (& mut self , value : T) -> Index < T > { let head_index = match self . head { Some (index) => index , None => return self . insert_empty (value) , } ; let index = self . insert_new (value , None , Some (head_index)) ; self . entries [head_index . get ()] . occupied_mut () . previous = Some (index) ; self . head = Some (index) ; Index :: new (index , self . generation) } # [doc = " Removes and returns the value at the given index, if it exists."] # [doc = ""] # [doc = " If the index refers to an index not in the list anymore or if the index has been invalidated, then [`None`] will"] # [doc = " be returned and the list will be unaffected."] # [doc = ""] # [doc = " Complexity: O(1)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " let index = list.push_back(0);"] # [doc = " assert_eq!(list.remove(index), Some(0));"] # [doc = " assert_eq!(list.remove(index), None);"] # [doc = " ```"] pub fn remove (& mut self , index : Index < T >) -> Option < T > { let (previous_index , next_index) = match & self . entries [index . index ()] { Entry :: Occupied (entry) if entry . generation == index . generation => { (entry . previous , entry . next) } _ => return None , } ; Some (self . remove_helper (previous_index , index . index , next_index) . value ,) } # [doc = " Removes and returns the entry at the given index, if it exists."] # [doc = ""] # [doc = " If the index refers to an index not in the list anymore or if the index has been invalidated, then [`None`] will"] # [doc = " be returned and the list will be unaffected."] fn remove_entry (& mut self , index : NonMaxUsize) -> Option < OccupiedEntry < T > > { let (previous_index , next_index) = match & self . entries [index . get ()] { Entry :: Occupied (entry) => (entry . previous , entry . next) , Entry :: Vacant (_) => return None , } ; Some (self . remove_helper (previous_index , index , next_index)) } # [doc = " Removes and returns the entry at the given index with the entries previous and next index"] # [doc = " values."] # [doc = ""] # [doc = " It is assumed that there is an entry at the given index."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if called when the list is empty. Behavior is undefined if provided indices do not follow the expected"] # [doc = " constraints."] fn remove_helper (& mut self , previous_index : Option < NonMaxUsize > , index : NonMaxUsize , next_index : Option < NonMaxUsize > ,) -> OccupiedEntry < T > { let head_index = self . head . expect ("expected head index") ; let tail_index = self . tail . expect ("expected tail index") ; let vacant_head = self . vacant_head ; let removed_entry = mem :: replace (& mut self . entries [index . get ()] , Entry :: Vacant (VacantEntry :: new (vacant_head)) ,) ; self . generation = self . generation . wrapping_add (1) ; self . length -= 1 ; self . vacant_head = Some (index) ; if index == head_index && index == tail_index { self . head = None ; self . tail = None ; } else if index == head_index { self . entries [next_index . expect ("expected next entry to exist") . get ()] . occupied_mut () . previous = None ; self . head = next_index ; } else if index == tail_index { self . entries [previous_index . expect ("expected previous entry to exist") . get ()] . occupied_mut () . next = None ; self . tail = previous_index ; } else { self . entries [next_index . expect ("expected next entry to exist") . get ()] . occupied_mut () . previous = previous_index ; self . entries [previous_index . expect ("expected previous entry to exist") . get ()] . occupied_mut () . next = next_index ; } removed_entry . occupied () } # [doc = " Reserves capacity for the given expected size increase."] # [doc = ""] # [doc = " The collection may reserve more space to avoid frequent reallocations. After calling this function, capacity will"] # [doc = " be greater than or equal to `self.len() + additional_capacity`. Does nothing if the current capacity is already"] # [doc = " sufficient."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity overflows `usize`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list: VecList<u32> = VecList::new();"] # [doc = " assert_eq!(list.capacity(), 0);"] # [doc = ""] # [doc = " list.reserve(10);"] # [doc = " assert!(list.capacity() >= 10);"] # [doc = " ```"] pub fn reserve (& mut self , additional_capacity : usize) { self . entries . reserve (additional_capacity) ; } # [doc = " Removes all elements from the list not satisfying the given predicate."] # [doc = ""] # [doc = " Complexity: O(n)"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list = VecList::new();"] # [doc = " list.push_back(0);"] # [doc = " list.push_back(-1);"] # [doc = " list.push_back(1);"] # [doc = " list.push_back(-2);"] # [doc = " list.retain(|&mut value| value >= 0);"] # [doc = ""] # [doc = " let mut iter = list.iter();"] # [doc = " assert_eq!(iter.next(), Some(&0));"] # [doc = " assert_eq!(iter.next(), Some(&1));"] # [doc = " assert_eq!(iter.next(), None);"] # [doc = " ```"] pub fn retain < Predicate > (& mut self , mut predicate : Predicate) where Predicate : FnMut (& mut T) -> bool , { let mut next_index = self . head ; while let Some (index) = next_index { let entry = self . entries [index . get ()] . occupied_mut () ; next_index = entry . next ; if ! predicate (& mut entry . value) { let _ = self . remove_entry (index) ; } } } # [doc = " Creates a new list with the given capacity."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use dlv_list::VecList;"] # [doc = ""] # [doc = " let mut list: VecList<u32> = VecList::new();"] # [doc = " assert_eq!(list.capacity(), 0);"] # [doc = ""] # [doc = " let mut list: VecList<u32> = VecList::with_capacity(10);"] # [doc = " assert_eq!(list.capacity(), 10);"] # [doc = " ```"] # [must_use] pub fn with_capacity (capacity : usize) -> Self { VecList { entries : Vec :: with_capacity (capacity) , generation : create_initial_generation () , head : None , length : 0 , tail : None , vacant_head : None , } } }
};
}
