macro_rules! Decomposition {
    () => {
        # [derive (Debug)] pub (crate) struct Decomposition { pub (crate) leading_nonstarters : usize , pub (crate) trailing_nonstarters : usize , pub (crate) decomposition_len : usize , }
    };
}

Decomposition!();