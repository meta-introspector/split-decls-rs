// Generated macro for Rng (trait)
macro_rules! Depcrate_rngRng {
() => {
// Module: crate::rng
// Provides: {"Rng"}
// Dependencies: {}
# [doc = " User-level interface for RNGs"] # [doc = ""] # [doc = " [`RngCore`] is the `dyn`-safe implementation-level interface for Random"] # [doc = " (Number) Generators. This trait, `Rng`, provides a user-level interface on"] # [doc = " RNGs. It is implemented automatically for any `R: RngCore`."] # [doc = ""] # [doc = " This trait must usually be brought into scope via `use rand::Rng;` or"] # [doc = " `use rand::prelude::*;`."] # [doc = ""] # [doc = " # Generic usage"] # [doc = ""] # [doc = " The basic pattern is `fn foo<R: Rng + ?Sized>(rng: &mut R)`. Some"] # [doc = " things are worth noting here:"] # [doc = ""] # [doc = " - Since `Rng: RngCore` and every `RngCore` implements `Rng`, it makes no"] # [doc = "   difference whether we use `R: Rng` or `R: RngCore`."] # [doc = " - The `+ ?Sized` un-bounding allows functions to be called directly on"] # [doc = "   type-erased references; i.e. `foo(r)` where `r: &mut dyn RngCore`. Without"] # [doc = "   this it would be necessary to write `foo(&mut r)`."] # [doc = ""] # [doc = " An alternative pattern is possible: `fn foo<R: Rng>(rng: R)`. This has some"] # [doc = " trade-offs. It allows the argument to be consumed directly without a `&mut`"] # [doc = " (which is how `from_rng(rand::rng())` works); also it still works directly"] # [doc = " on references (including type-erased references). Unfortunately within the"] # [doc = " function `foo` it is not known whether `rng` is a reference type or not,"] # [doc = " hence many uses of `rng` require an extra reference, either explicitly"] # [doc = " (`distr.sample(&mut rng)`) or implicitly (`rng.random()`); one may hope the"] # [doc = " optimiser can remove redundant references later."] # [doc = ""] # [doc = " Example:"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::Rng;"] # [doc = ""] # [doc = " fn foo<R: Rng + ?Sized>(rng: &mut R) -> f32 {"] # [doc = "     rng.random()"] # [doc = " }"] # [doc = ""] # [doc = " # let v = foo(&mut rand::rng());"] # [doc = " ```"] pub trait Rng : RngCore { # [doc = " Return a random value via the [`StandardUniform`] distribution."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::Rng;"] # [doc = ""] # [doc = " let mut rng = rand::rng();"] # [doc = " let x: u32 = rng.random();"] # [doc = " println!(\"{}\", x);"] # [doc = " println!(\"{:?}\", rng.random::<(f64, bool)>());"] # [doc = " ```"] # [doc = ""] # [doc = " # Arrays and tuples"] # [doc = ""] # [doc = " The `rng.random()` method is able to generate arrays"] # [doc = " and tuples (up to 12 elements), so long as all element types can be"] # [doc = " generated."] # [doc = ""] # [doc = " For arrays of integers, especially for those with small element types"] # [doc = " (< 64 bit), it will likely be faster to instead use [`Rng::fill`],"] # [doc = " though note that generated values will differ."] # [doc = ""] # [doc = " ```"] # [doc = " use rand::Rng;"] # [doc = ""] # [doc = " let mut rng = rand::rng();"] # [doc = " let tuple: (u8, i32, char) = rng.random(); // arbitrary tuple support"] # [doc = ""] # [doc = " let arr1: [f32; 32] = rng.random();        // array construction"] # [doc = " let mut arr2 = [0u8; 128];"] # [doc = " rng.fill(&mut arr2);                    // array fill"] # [doc = " ```"] # [doc = ""] # [doc = " [`StandardUniform`]: distr::StandardUniform"] # [inline] fn random < T > (& mut self) -> T where StandardUniform : Distribution < T > , { StandardUniform . sample (self) } # [doc = " Return an iterator over [`random`](Self::random) variates"] # [doc = ""] # [doc = " This is a just a wrapper over [`Rng::sample_iter`] using"] # [doc = " [`distr::StandardUniform`]."] # [doc = ""] # [doc = " Note: this method consumes its argument. Use"] # [doc = " `(&mut rng).random_iter()` to avoid consuming the RNG."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::{rngs::SmallRng, Rng, SeedableRng};"] # [doc = ""] # [doc = " let rng = SmallRng::seed_from_u64(0);"] # [doc = " let v: Vec<i32> = rng.random_iter().take(5).collect();"] # [doc = " assert_eq!(v.len(), 5);"] # [doc = " ```"] # [inline] fn random_iter < T > (self) -> distr :: Iter < StandardUniform , Self , T > where Self : Sized , StandardUniform : Distribution < T > , { StandardUniform . sample_iter (self) } # [doc = " Generate a random value in the given range."] # [doc = ""] # [doc = " This function is optimised for the case that only a single sample is"] # [doc = " made from the given range. See also the [`Uniform`] distribution"] # [doc = " type which may be faster if sampling from the same range repeatedly."] # [doc = ""] # [doc = " All types support `low..high_exclusive` and `low..=high` range syntax."] # [doc = " Unsigned integer types also support `..high_exclusive` and `..=high` syntax."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the range is empty, or if `high - low` overflows for floats."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::Rng;"] # [doc = ""] # [doc = " let mut rng = rand::rng();"] # [doc = ""] # [doc = " // Exclusive range"] # [doc = " let n: u32 = rng.random_range(..10);"] # [doc = " println!(\"{}\", n);"] # [doc = " let m: f64 = rng.random_range(-40.0..1.3e5);"] # [doc = " println!(\"{}\", m);"] # [doc = ""] # [doc = " // Inclusive range"] # [doc = " let n: u32 = rng.random_range(..=10);"] # [doc = " println!(\"{}\", n);"] # [doc = " ```"] # [doc = ""] # [doc = " [`Uniform`]: distr::uniform::Uniform"] # [track_caller] fn random_range < T , R > (& mut self , range : R) -> T where T : SampleUniform , R : SampleRange < T > , { assert ! (! range . is_empty () , "cannot sample empty range") ; range . sample_single (self) . unwrap () } # [doc = " Return a bool with a probability `p` of being true."] # [doc = ""] # [doc = " See also the [`Bernoulli`] distribution, which may be faster if"] # [doc = " sampling from the same probability repeatedly."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::Rng;"] # [doc = ""] # [doc = " let mut rng = rand::rng();"] # [doc = " println!(\"{}\", rng.random_bool(1.0 / 3.0));"] # [doc = " ```"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If `p < 0` or `p > 1`."] # [doc = ""] # [doc = " [`Bernoulli`]: distr::Bernoulli"] # [inline] # [track_caller] fn random_bool (& mut self , p : f64) -> bool { match distr :: Bernoulli :: new (p) { Ok (d) => self . sample (d) , Err (_) => panic ! ("p={:?} is outside range [0.0, 1.0]" , p) , } } # [doc = " Return a bool with a probability of `numerator/denominator` of being"] # [doc = " true."] # [doc = ""] # [doc = " That is, `random_ratio(2, 3)` has chance of 2 in 3, or about 67%, of"] # [doc = " returning true. If `numerator == denominator`, then the returned value"] # [doc = " is guaranteed to be `true`. If `numerator == 0`, then the returned"] # [doc = " value is guaranteed to be `false`."] # [doc = ""] # [doc = " See also the [`Bernoulli`] distribution, which may be faster if"] # [doc = " sampling from the same `numerator` and `denominator` repeatedly."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If `denominator == 0` or `numerator > denominator`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::Rng;"] # [doc = ""] # [doc = " let mut rng = rand::rng();"] # [doc = " println!(\"{}\", rng.random_ratio(2, 3));"] # [doc = " ```"] # [doc = ""] # [doc = " [`Bernoulli`]: distr::Bernoulli"] # [inline] # [track_caller] fn random_ratio (& mut self , numerator : u32 , denominator : u32) -> bool { match distr :: Bernoulli :: from_ratio (numerator , denominator) { Ok (d) => self . sample (d) , Err (_) => panic ! ("p={}/{} is outside range [0.0, 1.0]" , numerator , denominator) , } } # [doc = " Sample a new value, using the given distribution."] # [doc = ""] # [doc = " ### Example"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::Rng;"] # [doc = " use rand::distr::Uniform;"] # [doc = ""] # [doc = " let mut rng = rand::rng();"] # [doc = " let x = rng.sample(Uniform::new(10u32, 15).unwrap());"] # [doc = " // Type annotation requires two types, the type and distribution; the"] # [doc = " // distribution can be inferred."] # [doc = " let y = rng.sample::<u16, _>(Uniform::new(10, 15).unwrap());"] # [doc = " ```"] fn sample < T , D : Distribution < T > > (& mut self , distr : D) -> T { distr . sample (self) } # [doc = " Create an iterator that generates values using the given distribution."] # [doc = ""] # [doc = " Note: this method consumes its arguments. Use"] # [doc = " `(&mut rng).sample_iter(..)` to avoid consuming the RNG."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::Rng;"] # [doc = " use rand::distr::{Alphanumeric, Uniform, StandardUniform};"] # [doc = ""] # [doc = " let mut rng = rand::rng();"] # [doc = ""] # [doc = " // Vec of 16 x f32:"] # [doc = " let v: Vec<f32> = (&mut rng).sample_iter(StandardUniform).take(16).collect();"] # [doc = ""] # [doc = " // String:"] # [doc = " let s: String = (&mut rng).sample_iter(Alphanumeric)"] # [doc = "     .take(7)"] # [doc = "     .map(char::from)"] # [doc = "     .collect();"] # [doc = ""] # [doc = " // Combined values"] # [doc = " println!(\"{:?}\", (&mut rng).sample_iter(StandardUniform).take(5)"] # [doc = "                              .collect::<Vec<(f64, bool)>>());"] # [doc = ""] # [doc = " // Dice-rolling:"] # [doc = " let die_range = Uniform::new_inclusive(1, 6).unwrap();"] # [doc = " let mut roll_die = (&mut rng).sample_iter(die_range);"] # [doc = " while roll_die.next().unwrap() != 6 {"] # [doc = "     println!(\"Not a 6; rolling again!\");"] # [doc = " }"] # [doc = " ```"] fn sample_iter < T , D > (self , distr : D) -> distr :: Iter < D , Self , T > where D : Distribution < T > , Self : Sized , { distr . sample_iter (self) } # [doc = " Fill any type implementing [`Fill`] with random data"] # [doc = ""] # [doc = " This method is implemented for types which may be safely reinterpreted"] # [doc = " as an (aligned) `[u8]` slice then filled with random data. It is often"] # [doc = " faster than using [`Rng::random`] but not value-equivalent."] # [doc = ""] # [doc = " The distribution is expected to be uniform with portable results, but"] # [doc = " this cannot be guaranteed for third-party implementations."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use rand::Rng;"] # [doc = ""] # [doc = " let mut arr = [0i8; 20];"] # [doc = " rand::rng().fill(&mut arr[..]);"] # [doc = " ```"] # [doc = ""] # [doc = " [`fill_bytes`]: RngCore::fill_bytes"] # [track_caller] fn fill < T : Fill > (& mut self , dest : & mut [T]) { Fill :: fill_slice (dest , self) } # [doc = " Alias for [`Rng::random`]."] # [inline] # [deprecated (since = "0.9.0" , note = "Renamed to `random` to avoid conflict with the new `gen` keyword in Rust 2024.")] fn r#gen < T > (& mut self) -> T where StandardUniform : Distribution < T > , { self . random () } # [doc = " Alias for [`Rng::random_range`]."] # [inline] # [deprecated (since = "0.9.0" , note = "Renamed to `random_range`")] fn gen_range < T , R > (& mut self , range : R) -> T where T : SampleUniform , R : SampleRange < T > , { self . random_range (range) } # [doc = " Alias for [`Rng::random_bool`]."] # [inline] # [deprecated (since = "0.9.0" , note = "Renamed to `random_bool`")] fn gen_bool (& mut self , p : f64) -> bool { self . random_bool (p) } # [doc = " Alias for [`Rng::random_ratio`]."] # [inline] # [deprecated (since = "0.9.0" , note = "Renamed to `random_ratio`")] fn gen_ratio (& mut self , numerator : u32 , denominator : u32) -> bool { self . random_ratio (numerator , denominator) } }
};
}
