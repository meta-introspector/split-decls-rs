macro_rules! deps {
    () => {
        MsgHdrMut!();
        Protocol!();
        Socket!();
        SockAddr!();
        Domain!();
        MaybeUninitSlice!();
        RecvFlags!();
        Type!();
        MsgHdr!();
    };
}

macro_rules! impl_20 {
    () => {
        deps!();
        impl Socket { # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure `raw` is a valid file descriptor/socket. NOTE:"] # [doc = " this should really be marked `unsafe`, but this being an internal"] # [doc = " function, often passed as mapping function, it's makes it very"] # [doc = " inconvenient to mark it as `unsafe`."] pub (crate) fn from_raw (raw : sys :: RawSocket) -> Socket { Socket { inner : unsafe { sys :: socket_from_raw (raw) } , } } pub (crate) fn as_raw (& self) -> sys :: RawSocket { sys :: socket_as_raw (& self . inner) } pub (crate) fn into_raw (self) -> sys :: RawSocket { sys :: socket_into_raw (self . inner) } # [doc = " Creates a new socket and sets common flags."] # [doc = ""] # [doc = " This function corresponds to `socket(2)` on Unix and `WSASocketW` on"] # [doc = " Windows."] # [doc = ""] # [doc = " On Unix-like systems, the close-on-exec flag is set on the new socket."] # [doc = " Additionally, on Apple platforms `SOCK_NOSIGPIPE` is set. On Windows,"] # [doc = " the socket is made non-inheritable."] # [doc = ""] # [doc = " [`Socket::new_raw`] can be used if you don't want these flags to be set."] # [doc = man_links ! (socket (2))] pub fn new (domain : Domain , ty : Type , protocol : Option < Protocol >) -> io :: Result < Socket > { let ty = set_common_type (ty) ; Socket :: new_raw (domain , ty , protocol) . and_then (set_common_flags) } # [doc = " Creates a new socket ready to be configured."] # [doc = ""] # [doc = " This function corresponds to `socket(2)` on Unix and `WSASocketW` on"] # [doc = " Windows and simply creates a new socket, no other configuration is done."] pub fn new_raw (domain : Domain , ty : Type , protocol : Option < Protocol >) -> io :: Result < Socket > { let protocol = protocol . map_or (0 , | p | p . 0) ; sys :: socket (domain . 0 , ty . 0 , protocol) . map (Socket :: from_raw) } # [doc = " Creates a pair of sockets which are connected to each other."] # [doc = ""] # [doc = " This function corresponds to `socketpair(2)`."] # [doc = ""] # [doc = " This function sets the same flags as in done for [`Socket::new`],"] # [doc = " [`Socket::pair_raw`] can be used if you don't want to set those flags."] # [doc = man_links ! (unix : socketpair (2))] # [cfg (all (feature = "all" , unix))] pub fn pair (domain : Domain , ty : Type , protocol : Option < Protocol > ,) -> io :: Result < (Socket , Socket) > { let ty = set_common_type (ty) ; let (a , b) = Socket :: pair_raw (domain , ty , protocol) ? ; let a = set_common_flags (a) ? ; let b = set_common_flags (b) ? ; Ok ((a , b)) } # [doc = " Creates a pair of sockets which are connected to each other."] # [doc = ""] # [doc = " This function corresponds to `socketpair(2)`."] # [cfg (all (feature = "all" , unix))] pub fn pair_raw (domain : Domain , ty : Type , protocol : Option < Protocol > ,) -> io :: Result < (Socket , Socket) > { let protocol = protocol . map_or (0 , | p | p . 0) ; sys :: socketpair (domain . 0 , ty . 0 , protocol) . map (| [a , b] | (Socket :: from_raw (a) , Socket :: from_raw (b))) } # [doc = " Binds this socket to the specified address."] # [doc = ""] # [doc = " This function directly corresponds to the `bind(2)` function on Windows"] # [doc = " and Unix."] # [doc = man_links ! (bind (2))] pub fn bind (& self , address : & SockAddr) -> io :: Result < () > { sys :: bind (self . as_raw () , address) } # [doc = " Initiate a connection on this socket to the specified address."] # [doc = ""] # [doc = " This function directly corresponds to the `connect(2)` function on"] # [doc = " Windows and Unix."] # [doc = ""] # [doc = " An error will be returned if `listen` or `connect` has already been"] # [doc = " called on this builder."] # [doc = man_links ! (connect (2))] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " When using a non-blocking connect (by setting the socket into"] # [doc = " non-blocking mode before calling this function), socket option can't be"] # [doc = " set *while connecting*. This will cause errors on Windows. Socket"] # [doc = " options can be safely set before and after connecting the socket."] # [doc = ""] # [doc = " On Cygwin, a Unix domain socket connect blocks until the server accepts"] # [doc = " it. If the behavior is not expected, try [`Socket::set_no_peercred`]"] # [doc = " (Cygwin only)."] # [allow (rustdoc :: broken_intra_doc_links)] pub fn connect (& self , address : & SockAddr) -> io :: Result < () > { sys :: connect (self . as_raw () , address) } # [doc = " Initiate a connection on this socket to the specified address, only"] # [doc = " only waiting for a certain period of time for the connection to be"] # [doc = " established."] # [doc = ""] # [doc = " Unlike many other methods on `Socket`, this does *not* correspond to a"] # [doc = " single C function. It sets the socket to nonblocking mode, connects via"] # [doc = " connect(2), and then waits for the connection to complete with poll(2)"] # [doc = " on Unix and select on Windows. When the connection is complete, the"] # [doc = " socket is set back to blocking mode. On Unix, this will loop over"] # [doc = " `EINTR` errors."] # [doc = ""] # [doc = " # Warnings"] # [doc = ""] # [doc = " The non-blocking state of the socket is overridden by this function -"] # [doc = " it will be returned in blocking mode on success, and in an indeterminate"] # [doc = " state on failure."] # [doc = ""] # [doc = " If the connection request times out, it may still be processing in the"] # [doc = " background - a second call to `connect` or `connect_timeout` may fail."] pub fn connect_timeout (& self , addr : & SockAddr , timeout : Duration) -> io :: Result < () > { self . set_nonblocking (true) ? ; let res = self . connect (addr) ; self . set_nonblocking (false) ? ; match res { Ok (()) => return Ok (()) , Err (ref e) if e . kind () == io :: ErrorKind :: WouldBlock => { } # [cfg (unix)] Err (ref e) if e . raw_os_error () == Some (libc :: EINPROGRESS) => { } Err (e) => return Err (e) , } sys :: poll_connect (self , timeout) } # [doc = " Mark a socket as ready to accept incoming connection requests using"] # [doc = " [`Socket::accept()`]."] # [doc = ""] # [doc = " This function directly corresponds to the `listen(2)` function on"] # [doc = " Windows and Unix."] # [doc = ""] # [doc = " An error will be returned if `listen` or `connect` has already been"] # [doc = " called on this builder."] # [doc = man_links ! (listen (2))] pub fn listen (& self , backlog : c_int) -> io :: Result < () > { sys :: listen (self . as_raw () , backlog) } # [doc = " Accept a new incoming connection from this listener."] # [doc = ""] # [doc = " This function uses `accept4(2)` on platforms that support it and"] # [doc = " `accept(2)` platforms that do not."] # [doc = ""] # [doc = " This function sets the same flags as in done for [`Socket::new`],"] # [doc = " [`Socket::accept_raw`] can be used if you don't want to set those flags."] # [doc = man_links ! (accept (2))] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " On Cygwin, a Unix domain socket connect blocks until the server accepts"] # [doc = " it. If the behavior is not expected, try [`Socket::set_no_peercred`]"] # [doc = " (Cygwin only)."] # [allow (rustdoc :: broken_intra_doc_links)] pub fn accept (& self) -> io :: Result < (Socket , SockAddr) > { # [cfg (any (target_os = "android" , target_os = "dragonfly" , target_os = "freebsd" , target_os = "fuchsia" , target_os = "illumos" , target_os = "linux" , target_os = "netbsd" , target_os = "openbsd" , target_os = "cygwin" ,))] return self . _accept4 (libc :: SOCK_CLOEXEC) ; # [cfg (not (any (target_os = "android" , target_os = "dragonfly" , target_os = "freebsd" , target_os = "fuchsia" , target_os = "illumos" , target_os = "linux" , target_os = "netbsd" , target_os = "openbsd" , target_os = "cygwin" ,)))] { let (socket , addr) = self . accept_raw () ? ; let socket = set_common_flags (socket) ? ; # [cfg (windows)] socket . _set_no_inherit (true) ? ; Ok ((socket , addr)) } } # [doc = " Accept a new incoming connection from this listener."] # [doc = ""] # [doc = " This function directly corresponds to the `accept(2)` function on"] # [doc = " Windows and Unix."] pub fn accept_raw (& self) -> io :: Result < (Socket , SockAddr) > { sys :: accept (self . as_raw ()) . map (| (inner , addr) | (Socket :: from_raw (inner) , addr)) } # [doc = " Returns the socket address of the local half of this socket."] # [doc = ""] # [doc = " This function directly corresponds to the `getsockname(2)` function on"] # [doc = " Windows and Unix."] # [doc = man_links ! (getsockname (2))] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " Depending on the OS this may return an error if the socket is not"] # [doc = " [bound]."] # [doc = ""] # [doc = " [bound]: Socket::bind"] pub fn local_addr (& self) -> io :: Result < SockAddr > { sys :: getsockname (self . as_raw ()) } # [doc = " Returns the socket address of the remote peer of this socket."] # [doc = ""] # [doc = " This function directly corresponds to the `getpeername(2)` function on"] # [doc = " Windows and Unix."] # [doc = man_links ! (getpeername (2))] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " This returns an error if the socket is not [`connect`ed]."] # [doc = ""] # [doc = " [`connect`ed]: Socket::connect"] pub fn peer_addr (& self) -> io :: Result < SockAddr > { sys :: getpeername (self . as_raw ()) } # [doc = " Returns the [`Type`] of this socket by checking the `SO_TYPE` option on"] # [doc = " this socket."] pub fn r#type (& self) -> io :: Result < Type > { unsafe { getsockopt :: < c_int > (self . as_raw () , sys :: SOL_SOCKET , sys :: SO_TYPE) . map (Type) } } # [doc = " Creates a new independently owned handle to the underlying socket."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " On Unix this uses `F_DUPFD_CLOEXEC` and thus sets the `FD_CLOEXEC` on"] # [doc = " the returned socket."] # [doc = ""] # [doc = " On Windows this uses `WSA_FLAG_NO_HANDLE_INHERIT` setting inheriting to"] # [doc = " false."] # [doc = ""] # [doc = " On Windows this can **not** be used function cannot be used on a"] # [doc = " QOS-enabled socket, see"] # [doc = " <https://docs.microsoft.com/en-us/windows/win32/api/winsock2/nf-winsock2-wsaduplicatesocketw>."] pub fn try_clone (& self) -> io :: Result < Socket > { sys :: try_clone (self . as_raw ()) . map (Socket :: from_raw) } # [doc = " Returns true if this socket is set to nonblocking mode, false otherwise."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " On Unix this corresponds to calling `fcntl` returning the value of"] # [doc = " `O_NONBLOCK`."] # [doc = ""] # [doc = " On Windows it is not possible retrieve the nonblocking mode status."] # [cfg (all (feature = "all" , unix))] pub fn nonblocking (& self) -> io :: Result < bool > { sys :: nonblocking (self . as_raw ()) } # [doc = " Moves this socket into or out of nonblocking mode."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " On Unix this corresponds to calling `fcntl` (un)setting `O_NONBLOCK`."] # [doc = ""] # [doc = " On Windows this corresponds to calling `ioctlsocket` (un)setting"] # [doc = " `FIONBIO`."] pub fn set_nonblocking (& self , nonblocking : bool) -> io :: Result < () > { sys :: set_nonblocking (self . as_raw () , nonblocking) } # [doc = " Shuts down the read, write, or both halves of this connection."] # [doc = ""] # [doc = " This function will cause all pending and future I/O on the specified"] # [doc = " portions to return immediately with an appropriate value."] # [doc = man_links ! (shutdown (2))] pub fn shutdown (& self , how : Shutdown) -> io :: Result < () > { sys :: shutdown (self . as_raw () , how) } # [doc = " Receives data on the socket from the remote address to which it is"] # [doc = " connected."] # [doc = ""] # [doc = " The [`connect`] method will connect this socket to a remote address."] # [doc = " This method might fail if the socket is not connected."] # [doc = man_links ! (recv (2))] # [doc = ""] # [doc = " [`connect`]: Socket::connect"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Normally casting a `&mut [u8]` to `&mut [MaybeUninit<u8>]` would be"] # [doc = " unsound, as that allows us to write uninitialised bytes to the buffer."] # [doc = " However this implementation promises to not write uninitialised bytes to"] # [doc = " the `buf`fer and passes it directly to `recv(2)` system call. This"] # [doc = " promise ensures that this function can be called using a `buf`fer of"] # [doc = " type `&mut [u8]`."] # [doc = ""] # [doc = " Note that the [`io::Read::read`] implementation calls this function with"] # [doc = " a `buf`fer of type `&mut [u8]`, allowing initialised buffers to be used"] # [doc = " without using `unsafe`."] pub fn recv (& self , buf : & mut [MaybeUninit < u8 >]) -> io :: Result < usize > { self . recv_with_flags (buf , 0) } # [doc = " Receives out-of-band (OOB) data on the socket from the remote address to"] # [doc = " which it is connected by setting the `MSG_OOB` flag for this call."] # [doc = ""] # [doc = " For more information, see [`recv`], [`out_of_band_inline`]."] # [doc = ""] # [doc = " [`recv`]: Socket::recv"] # [doc = " [`out_of_band_inline`]: Socket::out_of_band_inline"] # [cfg_attr (target_os = "redox" , allow (rustdoc :: broken_intra_doc_links))] pub fn recv_out_of_band (& self , buf : & mut [MaybeUninit < u8 >]) -> io :: Result < usize > { self . recv_with_flags (buf , sys :: MSG_OOB) } # [doc = " Identical to [`recv`] but allows for specification of arbitrary flags to"] # [doc = " the underlying `recv` call."] # [doc = ""] # [doc = " [`recv`]: Socket::recv"] pub fn recv_with_flags (& self , buf : & mut [MaybeUninit < u8 >] , flags : sys :: c_int ,) -> io :: Result < usize > { sys :: recv (self . as_raw () , buf , flags) } # [doc = " Receives data on the socket from the remote address to which it is"] # [doc = " connected. Unlike [`recv`] this allows passing multiple buffers."] # [doc = ""] # [doc = " The [`connect`] method will connect this socket to a remote address."] # [doc = " This method might fail if the socket is not connected."] # [doc = ""] # [doc = " In addition to the number of bytes read, this function returns the flags"] # [doc = " for the received message. See [`RecvFlags`] for more information about"] # [doc = " the returned flags."] # [doc = man_links ! (recvmsg (2))] # [doc = ""] # [doc = " [`recv`]: Socket::recv"] # [doc = " [`connect`]: Socket::connect"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Normally casting a `IoSliceMut` to `MaybeUninitSlice` would be unsound,"] # [doc = " as that allows us to write uninitialised bytes to the buffer. However"] # [doc = " this implementation promises to not write uninitialised bytes to the"] # [doc = " `bufs` and passes it directly to `recvmsg(2)` system call. This promise"] # [doc = " ensures that this function can be called using `bufs` of type `&mut"] # [doc = " [IoSliceMut]`."] # [doc = ""] # [doc = " Note that the [`io::Read::read_vectored`] implementation calls this"] # [doc = " function with `buf`s of type `&mut [IoSliceMut]`, allowing initialised"] # [doc = " buffers to be used without using `unsafe`."] # [cfg (not (target_os = "redox"))] pub fn recv_vectored (& self , bufs : & mut [MaybeUninitSlice < '_ >] ,) -> io :: Result < (usize , RecvFlags) > { self . recv_vectored_with_flags (bufs , 0) } # [doc = " Identical to [`recv_vectored`] but allows for specification of arbitrary"] # [doc = " flags to the underlying `recvmsg`/`WSARecv` call."] # [doc = ""] # [doc = " [`recv_vectored`]: Socket::recv_vectored"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `recv_from_vectored` makes the same safety guarantees regarding `bufs`"] # [doc = " as [`recv_vectored`]."] # [doc = ""] # [doc = " [`recv_vectored`]: Socket::recv_vectored"] # [cfg (not (target_os = "redox"))] pub fn recv_vectored_with_flags (& self , bufs : & mut [MaybeUninitSlice < '_ >] , flags : c_int ,) -> io :: Result < (usize , RecvFlags) > { sys :: recv_vectored (self . as_raw () , bufs , flags) } # [doc = " Receives data on the socket from the remote adress to which it is"] # [doc = " connected, without removing that data from the queue. On success,"] # [doc = " returns the number of bytes peeked."] # [doc = ""] # [doc = " Successive calls return the same data. This is accomplished by passing"] # [doc = " `MSG_PEEK` as a flag to the underlying `recv` system call."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `peek` makes the same safety guarantees regarding the `buf`fer as"] # [doc = " [`recv`]."] # [doc = ""] # [doc = " [`recv`]: Socket::recv"] pub fn peek (& self , buf : & mut [MaybeUninit < u8 >]) -> io :: Result < usize > { self . recv_with_flags (buf , sys :: MSG_PEEK) } # [doc = " Receives data from the socket. On success, returns the number of bytes"] # [doc = " read and the address from whence the data came."] # [doc = man_links ! (recvfrom (2))] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `recv_from` makes the same safety guarantees regarding the `buf`fer as"] # [doc = " [`recv`]."] # [doc = ""] # [doc = " [`recv`]: Socket::recv"] pub fn recv_from (& self , buf : & mut [MaybeUninit < u8 >]) -> io :: Result < (usize , SockAddr) > { self . recv_from_with_flags (buf , 0) } # [doc = " Identical to [`recv_from`] but allows for specification of arbitrary"] # [doc = " flags to the underlying `recvfrom` call."] # [doc = ""] # [doc = " [`recv_from`]: Socket::recv_from"] pub fn recv_from_with_flags (& self , buf : & mut [MaybeUninit < u8 >] , flags : c_int ,) -> io :: Result < (usize , SockAddr) > { sys :: recv_from (self . as_raw () , buf , flags) } # [doc = " Receives data from the socket. Returns the amount of bytes read, the"] # [doc = " [`RecvFlags`] and the remote address from the data is coming. Unlike"] # [doc = " [`recv_from`] this allows passing multiple buffers."] # [doc = man_links ! (recvmsg (2))] # [doc = ""] # [doc = " [`recv_from`]: Socket::recv_from"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `recv_from_vectored` makes the same safety guarantees regarding `bufs`"] # [doc = " as [`recv_vectored`]."] # [doc = ""] # [doc = " [`recv_vectored`]: Socket::recv_vectored"] # [cfg (not (target_os = "redox"))] pub fn recv_from_vectored (& self , bufs : & mut [MaybeUninitSlice < '_ >] ,) -> io :: Result < (usize , RecvFlags , SockAddr) > { self . recv_from_vectored_with_flags (bufs , 0) } # [doc = " Identical to [`recv_from_vectored`] but allows for specification of"] # [doc = " arbitrary flags to the underlying `recvmsg`/`WSARecvFrom` call."] # [doc = ""] # [doc = " [`recv_from_vectored`]: Socket::recv_from_vectored"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `recv_from_vectored` makes the same safety guarantees regarding `bufs`"] # [doc = " as [`recv_vectored`]."] # [doc = ""] # [doc = " [`recv_vectored`]: Socket::recv_vectored"] # [cfg (not (target_os = "redox"))] pub fn recv_from_vectored_with_flags (& self , bufs : & mut [MaybeUninitSlice < '_ >] , flags : c_int ,) -> io :: Result < (usize , RecvFlags , SockAddr) > { sys :: recv_from_vectored (self . as_raw () , bufs , flags) } # [doc = " Receives data from the socket, without removing it from the queue."] # [doc = ""] # [doc = " Successive calls return the same data. This is accomplished by passing"] # [doc = " `MSG_PEEK` as a flag to the underlying `recvfrom` system call."] # [doc = ""] # [doc = " On success, returns the number of bytes peeked and the address from"] # [doc = " whence the data came."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `peek_from` makes the same safety guarantees regarding the `buf`fer as"] # [doc = " [`recv`]."] # [doc = ""] # [doc = " # Note: Datagram Sockets"] # [doc = " For datagram sockets, the behavior of this method when `buf` is smaller than"] # [doc = " the datagram at the head of the receive queue differs between Windows and"] # [doc = " Unix-like platforms (Linux, macOS, BSDs, etc: colloquially termed \"*nix\")."] # [doc = ""] # [doc = " On *nix platforms, the datagram is truncated to the length of `buf`."] # [doc = ""] # [doc = " On Windows, an error corresponding to `WSAEMSGSIZE` will be returned."] # [doc = ""] # [doc = " For consistency between platforms, be sure to provide a sufficiently large buffer to avoid"] # [doc = " truncation; the exact size required depends on the underlying protocol."] # [doc = ""] # [doc = " If you just want to know the sender of the data, try [`peek_sender`]."] # [doc = ""] # [doc = " [`recv`]: Socket::recv"] # [doc = " [`peek_sender`]: Socket::peek_sender"] pub fn peek_from (& self , buf : & mut [MaybeUninit < u8 >]) -> io :: Result < (usize , SockAddr) > { self . recv_from_with_flags (buf , sys :: MSG_PEEK) } # [doc = " Retrieve the sender for the data at the head of the receive queue."] # [doc = ""] # [doc = " This is equivalent to calling [`peek_from`] with a zero-sized buffer,"] # [doc = " but suppresses the `WSAEMSGSIZE` error on Windows."] # [doc = ""] # [doc = " [`peek_from`]: Socket::peek_from"] pub fn peek_sender (& self) -> io :: Result < SockAddr > { sys :: peek_sender (self . as_raw ()) } # [doc = " Receive a message from a socket using a message structure."] # [doc = ""] # [doc = " This is not supported on Windows as calling `WSARecvMsg` (the `recvmsg`"] # [doc = " equivalent) is not straight forward on Windows. See"] # [doc = " <https://github.com/microsoft/Windows-classic-samples/blob/7cbd99ac1d2b4a0beffbaba29ea63d024ceff700/Samples/Win7Samples/netds/winsock/recvmsg/rmmc.cpp>"] # [doc = " for an example (in C++)."] # [doc = man_links ! (recvmsg (2))] # [cfg (all (unix , not (target_os = "redox")))] pub fn recvmsg (& self , msg : & mut MsgHdrMut < '_ , '_ , '_ > , flags : sys :: c_int) -> io :: Result < usize > { sys :: recvmsg (self . as_raw () , msg , flags) } # [doc = " Sends data on the socket to a connected peer."] # [doc = ""] # [doc = " This is typically used on TCP sockets or datagram sockets which have"] # [doc = " been connected."] # [doc = ""] # [doc = " On success returns the number of bytes that were sent."] # [doc = man_links ! (send (2))] pub fn send (& self , buf : & [u8]) -> io :: Result < usize > { self . send_with_flags (buf , 0) } # [doc = " Identical to [`send`] but allows for specification of arbitrary flags to the underlying"] # [doc = " `send` call."] # [doc = ""] # [doc = " [`send`]: Socket::send"] pub fn send_with_flags (& self , buf : & [u8] , flags : c_int) -> io :: Result < usize > { sys :: send (self . as_raw () , buf , flags) } # [doc = " Send data to the connected peer. Returns the amount of bytes written."] # [cfg (not (target_os = "redox"))] pub fn send_vectored (& self , bufs : & [IoSlice < '_ >]) -> io :: Result < usize > { self . send_vectored_with_flags (bufs , 0) } # [doc = " Identical to [`send_vectored`] but allows for specification of arbitrary"] # [doc = " flags to the underlying `sendmsg`/`WSASend` call."] # [doc = man_links ! (sendmsg (2))] # [doc = ""] # [doc = " [`send_vectored`]: Socket::send_vectored"] # [cfg (not (target_os = "redox"))] pub fn send_vectored_with_flags (& self , bufs : & [IoSlice < '_ >] , flags : c_int ,) -> io :: Result < usize > { sys :: send_vectored (self . as_raw () , bufs , flags) } # [doc = " Sends out-of-band (OOB) data on the socket to connected peer"] # [doc = " by setting the `MSG_OOB` flag for this call."] # [doc = ""] # [doc = " For more information, see [`send`], [`out_of_band_inline`]."] # [doc = ""] # [doc = " [`send`]: Socket::send"] # [doc = " [`out_of_band_inline`]: Socket::out_of_band_inline"] # [cfg_attr (target_os = "redox" , allow (rustdoc :: broken_intra_doc_links))] pub fn send_out_of_band (& self , buf : & [u8]) -> io :: Result < usize > { self . send_with_flags (buf , sys :: MSG_OOB) } # [doc = " Sends data on the socket to the given address. On success, returns the"] # [doc = " number of bytes written."] # [doc = ""] # [doc = " This is typically used on UDP or datagram-oriented sockets."] # [doc = man_links ! (sendto (2))] pub fn send_to (& self , buf : & [u8] , addr : & SockAddr) -> io :: Result < usize > { self . send_to_with_flags (buf , addr , 0) } # [doc = " Identical to [`send_to`] but allows for specification of arbitrary flags"] # [doc = " to the underlying `sendto` call."] # [doc = ""] # [doc = " [`send_to`]: Socket::send_to"] pub fn send_to_with_flags (& self , buf : & [u8] , addr : & SockAddr , flags : c_int ,) -> io :: Result < usize > { sys :: send_to (self . as_raw () , buf , addr , flags) } # [doc = " Send data to a peer listening on `addr`. Returns the amount of bytes"] # [doc = " written."] # [doc = man_links ! (sendmsg (2))] # [cfg (not (target_os = "redox"))] pub fn send_to_vectored (& self , bufs : & [IoSlice < '_ >] , addr : & SockAddr) -> io :: Result < usize > { self . send_to_vectored_with_flags (bufs , addr , 0) } # [doc = " Identical to [`send_to_vectored`] but allows for specification of"] # [doc = " arbitrary flags to the underlying `sendmsg`/`WSASendTo` call."] # [doc = ""] # [doc = " [`send_to_vectored`]: Socket::send_to_vectored"] # [cfg (not (target_os = "redox"))] pub fn send_to_vectored_with_flags (& self , bufs : & [IoSlice < '_ >] , addr : & SockAddr , flags : c_int ,) -> io :: Result < usize > { sys :: send_to_vectored (self . as_raw () , bufs , addr , flags) } # [doc = " Send a message on a socket using a message structure."] # [doc = man_links ! (sendmsg (2))] # [cfg (not (target_os = "redox"))] pub fn sendmsg (& self , msg : & MsgHdr < '_ , '_ , '_ > , flags : sys :: c_int) -> io :: Result < usize > { sys :: sendmsg (self . as_raw () , msg , flags) } }
    };
}

impl_20!();