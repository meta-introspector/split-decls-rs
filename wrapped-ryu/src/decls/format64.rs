macro_rules! format64 {
    () => {
        # [doc = " Print f64 to the given buffer and return number of bytes written."] # [doc = ""] # [doc = " At most 24 bytes will be written."] # [doc = ""] # [doc = " ## Special cases"] # [doc = ""] # [doc = " This function **does not** check for NaN or infinity. If the input"] # [doc = " number is not a finite float, the printed representation will be some"] # [doc = " correctly formatted but unspecified numerical value."] # [doc = ""] # [doc = " Please check [`is_finite`] yourself before calling this function, or"] # [doc = " check [`is_nan`] and [`is_infinite`] and handle those cases yourself."] # [doc = ""] # [doc = " [`is_finite`]: f64::is_finite"] # [doc = " [`is_nan`]: f64::is_nan"] # [doc = " [`is_infinite`]: f64::is_infinite"] # [doc = ""] # [doc = " ## Safety"] # [doc = ""] # [doc = " The `result` pointer argument must point to sufficiently many writable bytes"] # [doc = " to hold Ryū's representation of `f`."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::{mem::MaybeUninit, slice, str};"] # [doc = ""] # [doc = " let f = 1.234f64;"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     let mut buffer = [MaybeUninit::<u8>::uninit(); 24];"] # [doc = "     let len = ryu::raw::format64(f, buffer.as_mut_ptr() as *mut u8);"] # [doc = "     let slice = slice::from_raw_parts(buffer.as_ptr() as *const u8, len);"] # [doc = "     let print = str::from_utf8_unchecked(slice);"] # [doc = "     assert_eq!(print, \"1.234\");"] # [doc = " }"] # [doc = " ```"] # [must_use] # [cfg_attr (feature = "no-panic" , no_panic)] pub unsafe fn format64 (f : f64 , result : * mut u8) -> usize { let bits = f . to_bits () ; let sign = ((bits >> (DOUBLE_MANTISSA_BITS + DOUBLE_EXPONENT_BITS)) & 1) != 0 ; let ieee_mantissa = bits & ((1u64 << DOUBLE_MANTISSA_BITS) - 1) ; let ieee_exponent = (bits >> DOUBLE_MANTISSA_BITS) as u32 & ((1u32 << DOUBLE_EXPONENT_BITS) - 1) ; let mut index = 0isize ; if sign { * result = b'-' ; index += 1 ; } if ieee_exponent == 0 && ieee_mantissa == 0 { ptr :: copy_nonoverlapping (b"0.0" . as_ptr () , result . offset (index) , 3) ; return sign as usize + 3 ; } let v = d2d (ieee_mantissa , ieee_exponent) ; let length = d2s :: decimal_length17 (v . mantissa) as isize ; let k = v . exponent as isize ; let kk = length + k ; debug_assert ! (k >= - 324) ; if 0 <= k && kk <= 16 { write_mantissa_long (v . mantissa , result . offset (index + length)) ; for i in length .. kk { * result . offset (index + i) = b'0' ; } * result . offset (index + kk) = b'.' ; * result . offset (index + kk + 1) = b'0' ; index as usize + kk as usize + 2 } else if 0 < kk && kk <= 16 { write_mantissa_long (v . mantissa , result . offset (index + length + 1)) ; ptr :: copy (result . offset (index + 1) , result . offset (index) , kk as usize) ; * result . offset (index + kk) = b'.' ; index as usize + length as usize + 1 } else if - 5 < kk && kk <= 0 { * result . offset (index) = b'0' ; * result . offset (index + 1) = b'.' ; let offset = 2 - kk ; for i in 2 .. offset { * result . offset (index + i) = b'0' ; } write_mantissa_long (v . mantissa , result . offset (index + length + offset)) ; index as usize + length as usize + offset as usize } else if length == 1 { * result . offset (index) = b'0' + v . mantissa as u8 ; * result . offset (index + 1) = b'e' ; index as usize + 2 + write_exponent3 (kk - 1 , result . offset (index + 2)) } else { write_mantissa_long (v . mantissa , result . offset (index + length + 1)) ; * result . offset (index) = * result . offset (index + 1) ; * result . offset (index + 1) = b'.' ; * result . offset (index + length + 1) = b'e' ; index as usize + length as usize + 2 + write_exponent3 (kk - 1 , result . offset (index + length + 2)) } }
    };
}

format64!();