// Generated macro for sealed (module)
macro_rules! Depcrate_rejectsealed {
() => {
// Module: crate::reject
// Provides: {"sealed"}
// Dependencies: {}
mod sealed { use super :: { Reason , Rejection , Rejections } ; use http :: StatusCode ; use std :: convert :: Infallible ; use std :: fmt ; pub trait IsReject : fmt :: Debug + Send + Sync { fn status (& self) -> StatusCode ; fn into_response (& self) -> crate :: reply :: Response ; } fn _assert_object_safe () { fn _assert (_ : & dyn IsReject) { } } pub trait CombineRejection < E > : Send + Sized { # [doc = " The type that should be returned when only 1 of the two"] # [doc = " \"rejections\" occurs."] # [doc = ""] # [doc = " # For example:"] # [doc = ""] # [doc = " `warp::any().and(warp::path(\"foo\"))` has the following steps:"] # [doc = ""] # [doc = " 1. Since this is `and`, only **one** of the rejections will occur,"] # [doc = "    and as soon as it does, it will be returned."] # [doc = " 2. `warp::any()` rejects with `Never`. So, it will never return `Never`."] # [doc = " 3. `warp::path()` rejects with `Rejection`. It may return `Rejection`."] # [doc = ""] # [doc = " Thus, if the above filter rejects, it will definitely be `Rejection`."] type One : IsReject + From < Self > + From < E > + Into < Rejection > ; # [doc = " The type that should be returned when both rejections occur,"] # [doc = " and need to be combined."] type Combined : IsReject ; fn combine (self , other : E) -> Self :: Combined ; } impl CombineRejection < Rejection > for Rejection { type One = Rejection ; type Combined = Rejection ; fn combine (self , other : Rejection) -> Self :: Combined { let reason = match (self . reason , other . reason) { (Reason :: Other (left) , Reason :: Other (right)) => { Reason :: Other (Box :: new (Rejections :: Combined (left , right))) } (Reason :: Other (other) , Reason :: NotFound) | (Reason :: NotFound , Reason :: Other (other)) => { Reason :: Other (other) } (Reason :: NotFound , Reason :: NotFound) => Reason :: NotFound , } ; Rejection { reason } } } impl CombineRejection < Infallible > for Rejection { type One = Rejection ; type Combined = Infallible ; fn combine (self , other : Infallible) -> Self :: Combined { match other { } } } impl CombineRejection < Rejection > for Infallible { type One = Rejection ; type Combined = Infallible ; fn combine (self , _ : Rejection) -> Self :: Combined { match self { } } } impl CombineRejection < Infallible > for Infallible { type One = Infallible ; type Combined = Infallible ; fn combine (self , _ : Infallible) -> Self :: Combined { match self { } } } }
};
}
