// Generated macro for impl_335 (impl)
macro_rules! Depcrate_filters_corsimpl_335 {
() => {
// Module: crate::filters::cors
// Provides: {"impl_335"}
// Dependencies: {}
impl Builder { # [doc = " Sets whether to add the `Access-Control-Allow-Credentials` header."] pub fn allow_credentials (mut self , allow : bool) -> Self { self . credentials = allow ; self } # [doc = " Adds a method to the existing list of allowed request methods."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the provided argument is not a valid `http::Method`."] pub fn allow_method < M > (mut self , method : M) -> Self where http :: Method : TryFrom < M > , { let method = match TryFrom :: try_from (method) { Ok (m) => m , Err (_) => panic ! ("illegal Method") , } ; self . methods . insert (method) ; self } # [doc = " Adds multiple methods to the existing list of allowed request methods."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the provided argument is not a valid `http::Method`."] pub fn allow_methods < I > (mut self , methods : I) -> Self where I : IntoIterator , http :: Method : TryFrom < I :: Item > , { let iter = methods . into_iter () . map (| m | match TryFrom :: try_from (m) { Ok (m) => m , Err (_) => panic ! ("illegal Method") , }) ; self . methods . extend (iter) ; self } # [doc = " Adds a header to the list of allowed request headers."] # [doc = ""] # [doc = " **Note**: These should match the values the browser sends via `Access-Control-Request-Headers`, e.g. `content-type`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the provided argument is not a valid `http::header::HeaderName`."] pub fn allow_header < H > (mut self , header : H) -> Self where HeaderName : TryFrom < H > , { let header = match TryFrom :: try_from (header) { Ok (m) => m , Err (_) => panic ! ("illegal Header") , } ; self . allowed_headers . insert (header) ; self } # [doc = " Adds multiple headers to the list of allowed request headers."] # [doc = ""] # [doc = " **Note**: These should match the values the browser sends via `Access-Control-Request-Headers`, e.g.`content-type`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if any of the headers are not a valid `http::header::HeaderName`."] pub fn allow_headers < I > (mut self , headers : I) -> Self where I : IntoIterator , HeaderName : TryFrom < I :: Item > , { let iter = headers . into_iter () . map (| h | match TryFrom :: try_from (h) { Ok (h) => h , Err (_) => panic ! ("illegal Header") , }) ; self . allowed_headers . extend (iter) ; self } # [doc = " Adds a header to the list of exposed headers."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the provided argument is not a valid `http::header::HeaderName`."] pub fn expose_header < H > (mut self , header : H) -> Self where HeaderName : TryFrom < H > , { let header = match TryFrom :: try_from (header) { Ok (m) => m , Err (_) => panic ! ("illegal Header") , } ; self . exposed_headers . insert (header) ; self } # [doc = " Adds multiple headers to the list of exposed headers."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if any of the headers are not a valid `http::header::HeaderName`."] pub fn expose_headers < I > (mut self , headers : I) -> Self where I : IntoIterator , HeaderName : TryFrom < I :: Item > , { let iter = headers . into_iter () . map (| h | match TryFrom :: try_from (h) { Ok (h) => h , Err (_) => panic ! ("illegal Header") , }) ; self . exposed_headers . extend (iter) ; self } # [doc = " Sets that *any* `Origin` header is allowed."] # [doc = ""] # [doc = " # Warning"] # [doc = ""] # [doc = " This can allow websites you didn't intend to access this resource,"] # [doc = " it is usually better to set an explicit list."] pub fn allow_any_origin (mut self) -> Self { self . origins = None ; self } # [doc = " Add an origin to the existing list of allowed `Origin`s."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the provided argument is not a valid `Origin`."] pub fn allow_origin (self , origin : impl IntoOrigin) -> Self { self . allow_origins (Some (origin)) } # [doc = " Add multiple origins to the existing list of allowed `Origin`s."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the provided argument is not a valid `Origin`."] pub fn allow_origins < I > (mut self , origins : I) -> Self where I : IntoIterator , I :: Item : IntoOrigin , { let iter = origins . into_iter () . map (IntoOrigin :: into_origin) . map (| origin | { origin . to_string () . parse () . expect ("Origin is always a valid HeaderValue") }) ; self . origins . get_or_insert_with (HashSet :: new) . extend (iter) ; self } # [doc = " Sets the `Access-Control-Max-Age` header."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = ""] # [doc = " ```"] # [doc = " use std::time::Duration;"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " let cors = warp::cors()"] # [doc = "     .max_age(30) // 30u32 seconds"] # [doc = "     .max_age(Duration::from_secs(30)); // or a Duration"] # [doc = " ```"] pub fn max_age (mut self , seconds : impl Seconds) -> Self { self . max_age = Some (seconds . seconds ()) ; self } # [doc = " Builds the `Cors` wrapper from the configured settings."] # [doc = ""] # [doc = " This step isn't *required*, as the `Builder` itself can be passed"] # [doc = " to `Filter::with`. This just allows constructing once, thus not needing"] # [doc = " to pay the cost of \"building\" every time."] pub fn build (self) -> Cors { let expose_headers_header = if self . exposed_headers . is_empty () { None } else { Some (self . exposed_headers . iter () . cloned () . collect ()) } ; let allowed_headers_header = self . allowed_headers . iter () . cloned () . collect () ; let methods_header = self . methods . iter () . cloned () . collect () ; let config = Arc :: new (Configured { cors : self , allowed_headers_header , expose_headers_header , methods_header , }) ; Cors { config } } }
};
}
