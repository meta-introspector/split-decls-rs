// Generated macro for Filter (trait)
macro_rules! Depcrate_filterFilter {
() => {
// Module: crate::filter
// Provides: {"Filter"}
// Dependencies: {}
# [doc = " Composable request filters."] # [doc = ""] # [doc = " A `Filter` can optionally extract some data from a request, combine"] # [doc = " it with others, mutate it, and return back some value as a reply. The"] # [doc = " power of `Filter`s come from being able to isolate small subsets, and then"] # [doc = " chain and reuse them in various parts of your app."] # [doc = ""] # [doc = " # Extracting Tuples"] # [doc = ""] # [doc = " You may notice that several of these filters extract some tuple, often"] # [doc = " times a tuple of just 1 item! Why?"] # [doc = ""] # [doc = " If a filter extracts a `(String,)`, that simply means that it"] # [doc = " extracts a `String`. If you were to `map` the filter, the argument type"] # [doc = " would be exactly that, just a `String`."] # [doc = ""] # [doc = " What is it? It's just some type magic that allows for automatic combining"] # [doc = " and flattening of tuples. Without it, combining two filters together with"] # [doc = " `and`, where one extracted `()`, and another `String`, would mean the"] # [doc = " `map` would be given a single argument of `((), String,)`, which is just"] # [doc = " no fun."] pub trait Filter : FilterBase { # [doc = " Composes a new `Filter` that requires both this and the other to filter a request."] # [doc = ""] # [doc = " Additionally, this will join together the extracted values of both"] # [doc = " filters, so that `map` and `and_then` receive them as separate arguments."] # [doc = ""] # [doc = " If a `Filter` extracts nothing (so, `()`), combining with any other"] # [doc = " filter will simply discard the `()`. If a `Filter` extracts one or"] # [doc = " more items, combining will mean it extracts the values of itself"] # [doc = " combined with the other."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " // Match `/hello/:name`..."] # [doc = " warp::path(\"hello\")"] # [doc = "     .and(warp::path::param::<String>());"] # [doc = " ```"] fn and < F > (self , other : F) -> And < Self , F > where Self : Sized , < Self :: Extract as Tuple > :: HList : Combine < < F :: Extract as Tuple > :: HList > , F : Filter + Clone , F :: Error : CombineRejection < Self :: Error > , { And { first : self , second : other , } } # [doc = " Composes a new `Filter` of either this or the other filter."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use std::net::SocketAddr;"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " // Match either `/:u32` or `/:socketaddr`"] # [doc = " warp::path::param::<u32>()"] # [doc = "     .or(warp::path::param::<SocketAddr>());"] # [doc = " ```"] fn or < F > (self , other : F) -> Or < Self , F > where Self : Filter < Error = Rejection > + Sized , F : Filter , F :: Error : CombineRejection < Self :: Error > , { Or { first : self , second : other , } } # [doc = " Composes this `Filter` with a function receiving the extracted value."] # [doc = ""] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " // Map `/:id`"] # [doc = " warp::path::param().map(|id: u64| {"] # [doc = "   format!(\"Hello #{}\", id)"] # [doc = " });"] # [doc = " ```"] # [doc = ""] # [doc = " # `Func`"] # [doc = ""] # [doc = " The generic `Func` trait is implemented for any function that receives"] # [doc = " the same arguments as this `Filter` extracts. In practice, this"] # [doc = " shouldn't ever bother you, and simply makes things feel more natural."] # [doc = ""] # [doc = " For example, if three `Filter`s were combined together, suppose one"] # [doc = " extracts nothing (so `()`), and the other two extract two integers,"] # [doc = " a function that accepts exactly two integer arguments is allowed."] # [doc = " Specifically, any `Fn(u32, u32)`."] # [doc = ""] # [doc = " Without `Product` and `Func`, this would be a lot messier. First of"] # [doc = " all, the `()`s couldn't be discarded, and the tuples would be nested."] # [doc = " So, instead, you'd need to pass an `Fn(((), (u32, u32)))`. That's just"] # [doc = " a single argument. Bleck!"] # [doc = ""] # [doc = " Even worse, the tuples would shuffle the types around depending on"] # [doc = " the exact invocation of `and`s. So, `unit.and(int).and(int)` would"] # [doc = " result in a different extracted type from `unit.and(int.and(int))`,"] # [doc = " or from `int.and(unit).and(int)`. If you changed around the order"] # [doc = " of filters, while still having them be semantically equivalent, you'd"] # [doc = " need to update all your `map`s as well."] # [doc = ""] # [doc = " `Product`, `HList`, and `Func` do all the heavy work so that none of"] # [doc = " this is a bother to you. What's more, the types are enforced at"] # [doc = " compile-time, and tuple flattening is optimized away to nothing by"] # [doc = " LLVM."] fn map < F > (self , fun : F) -> Map < Self , F > where Self : Sized , F : Func < Self :: Extract > + Clone , { Map { filter : self , callback : fun , } } # [doc = " Composes this `Filter` with an async function receiving"] # [doc = " the extracted value."] # [doc = ""] # [doc = " The function should return some `Future` type."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " // Map `/:id`"] # [doc = " warp::path::param().then(|id: u64| async move {"] # [doc = "   format!(\"Hello #{}\", id)"] # [doc = " });"] # [doc = " ```"] fn then < F > (self , fun : F) -> Then < Self , F > where Self : Sized , F : Func < Self :: Extract > + Clone , F :: Output : Future + Send , { Then { filter : self , callback : fun , } } # [doc = " Composes this `Filter` with a fallible async function receiving"] # [doc = " the extracted value."] # [doc = ""] # [doc = " The function should return some `TryFuture` type."] # [doc = ""] # [doc = " The `Error` type of the return `Future` needs be a `Rejection`, which"] # [doc = " means most futures will need to have their error mapped into one."] # [doc = ""] # [doc = " Rejections are meant to say \"this filter didn't accept the request,"] # [doc = " maybe another can\". So for application-level errors, consider using"] # [doc = " [`Filter::then`] instead."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " // Validate after `/:id`"] # [doc = " warp::path::param().and_then(|id: u64| async move {"] # [doc = "     if id != 0 {"] # [doc = "         Ok(format!(\"Hello #{}\", id))"] # [doc = "     } else {"] # [doc = "         Err(warp::reject::not_found())"] # [doc = "     }"] # [doc = " });"] # [doc = " ```"] fn and_then < F > (self , fun : F) -> AndThen < Self , F > where Self : Sized , F : Func < Self :: Extract > + Clone , F :: Output : TryFuture + Send , < F :: Output as TryFuture > :: Error : CombineRejection < Self :: Error > , { AndThen { filter : self , callback : fun , } } # [doc = " Compose this `Filter` with a function receiving an error."] # [doc = ""] # [doc = " The function should return some `TryFuture` type yielding the"] # [doc = " same item and error types."] fn or_else < F > (self , fun : F) -> OrElse < Self , F > where Self : Filter < Error = Rejection > + Sized , F : Func < Rejection > , F :: Output : TryFuture < Ok = Self :: Extract > + Send , < F :: Output as TryFuture > :: Error : IsReject , { OrElse { filter : self , callback : fun , } } # [doc = " Compose this `Filter` with a function receiving an error and"] # [doc = " returning a *new* type, instead of the *same* type."] # [doc = ""] # [doc = " This is useful for \"customizing\" rejections into new response types."] # [doc = " See also the [rejections example][ex]."] # [doc = ""] # [doc = " [ex]: https://github.com/seanmonstar/warp/blob/master/examples/rejections.rs"] fn recover < F > (self , fun : F) -> Recover < Self , F > where Self : Filter < Error = Rejection > + Sized , F : Func < Rejection > , F :: Output : TryFuture + Send , < F :: Output as TryFuture > :: Error : IsReject , { Recover { filter : self , callback : fun , } } # [doc = " Unifies the extracted value of `Filter`s composed with `or`."] # [doc = ""] # [doc = " When a `Filter` extracts some `Either<T, T>`, where both sides"] # [doc = " are the same type, this combinator can be used to grab the"] # [doc = " inner value, regardless of which side of `Either` it was. This"] # [doc = " is useful for values that could be extracted from multiple parts"] # [doc = " of a request, and the exact place isn't important."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::net::SocketAddr;"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " let client_ip = warp::header(\"x-real-ip\")"] # [doc = "     .or(warp::header(\"x-forwarded-for\"))"] # [doc = "     .unify()"] # [doc = "     .map(|ip: SocketAddr| {"] # [doc = "         // Get the IP from either header,"] # [doc = "         // and unify into the inner type."] # [doc = "     });"] # [doc = " ```"] fn unify < T > (self) -> Unify < Self > where Self : Filter < Extract = (Either < T , T > ,) > + Sized , T : Tuple , { Unify { filter : self } } # [doc = " Convenience method to remove one layer of tupling."] # [doc = ""] # [doc = " This is useful for when things like `map` don't return a new value,"] # [doc = " but just `()`, since warp will wrap it up into a `((),)`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " let route = warp::path::param()"] # [doc = "     .map(|num: u64| {"] # [doc = "         println!(\"just logging: {}\", num);"] # [doc = "         // returning \"nothing\""] # [doc = "     })"] # [doc = "     .untuple_one()"] # [doc = "     .map(|| {"] # [doc = "         println!(\"the ((),) was removed\");"] # [doc = "         warp::reply()"] # [doc = "     });"] # [doc = " ```"] # [doc = ""] # [doc = " ```"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " let route = warp::any()"] # [doc = "     .map(|| {"] # [doc = "         // wanting to return a tuple"] # [doc = "         (true, 33)"] # [doc = "     })"] # [doc = "     .untuple_one()"] # [doc = "     .map(|is_enabled: bool, count: i32| {"] # [doc = "         println!(\"untupled: ({}, {})\", is_enabled, count);"] # [doc = "     });"] # [doc = " ```"] fn untuple_one < T > (self) -> UntupleOne < Self > where Self : Filter < Extract = (T ,) > + Sized , T : Tuple , { UntupleOne { filter : self } } # [doc = " Wraps the current filter with some wrapper."] # [doc = ""] # [doc = " The wrapper may do some preparation work before starting this filter,"] # [doc = " and may do post-processing after the filter completes."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " let route = warp::any()"] # [doc = "     .map(warp::reply);"] # [doc = ""] # [doc = " // Wrap the route with a log wrapper."] # [doc = " let route = route.with(warp::log(\"example\"));"] # [doc = " ```"] fn with < W > (self , wrapper : W) -> W :: Wrapped where Self : Sized , W : Wrap < Self > , { wrapper . wrap (self) } # [doc = " Boxes this filter into a trait object, making it easier to name the type."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " fn impl_reply() -> warp::filters::BoxedFilter<(impl warp::Reply,)> {"] # [doc = "     warp::any()"] # [doc = "         .map(warp::reply)"] # [doc = "         .boxed()"] # [doc = " }"] # [doc = ""] # [doc = " fn named_i32() -> warp::filters::BoxedFilter<(i32,)> {"] # [doc = "     warp::path::param::<i32>()"] # [doc = "         .boxed()"] # [doc = " }"] # [doc = ""] # [doc = " fn named_and() -> warp::filters::BoxedFilter<(i32, String)> {"] # [doc = "     warp::path::param::<i32>()"] # [doc = "         .and(warp::header::<String>(\"host\"))"] # [doc = "         .boxed()"] # [doc = " }"] # [doc = " ```"] fn boxed (self) -> BoxedFilter < Self :: Extract > where Self : Sized + Send + Sync + 'static , Self :: Extract : Send , Self :: Error : Into < Rejection > , { BoxedFilter :: new (self) } }
};
}
