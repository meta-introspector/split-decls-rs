// Generated macro for path (macro)
macro_rules! Depcrate_filters_pathpath {
() => {
// Module: crate::filters::path
// Provides: {"path"}
// Dependencies: {}
# [doc = " Convenient way to chain multiple path filters together."] # [doc = ""] # [doc = " Any number of either type identifiers or string expressions can be passed,"] # [doc = " each separated by a forward slash (`/`). Strings will be used to match"] # [doc = " path segments exactly, and type identifiers are used just like"] # [doc = " [`param`](crate::path::param) filters."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " // Match `/sum/:a/:b`"] # [doc = " let route = warp::path!(\"sum\" / u32 / u32)"] # [doc = "     .map(|a, b| {"] # [doc = "         format!(\"{} + {} = {}\", a, b, a + b)"] # [doc = "     });"] # [doc = " ```"] # [doc = ""] # [doc = " The equivalent filter chain without using the `path!` macro looks this:"] # [doc = ""] # [doc = " ```"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " let route = warp::path(\"sum\")"] # [doc = "     .and(warp::path::param::<u32>())"] # [doc = "     .and(warp::path::param::<u32>())"] # [doc = "     .and(warp::path::end())"] # [doc = "     .map(|a, b| {"] # [doc = "         format!(\"{} + {} = {}\", a, b, a + b)"] # [doc = "     });"] # [doc = " ```"] # [doc = ""] # [doc = " # Path Prefixes"] # [doc = ""] # [doc = " The `path!` macro automatically assumes the path should include an `end()`"] # [doc = " filter. To build up a path filter *prefix*, such that the `end()` isn't"] # [doc = " included, use the `/ ..` syntax."] # [doc = ""] # [doc = ""] # [doc = " ```"] # [doc = " use warp::Filter;"] # [doc = ""] # [doc = " let prefix = warp::path!(\"math\" / \"sum\" / ..);"] # [doc = ""] # [doc = " let sum = warp::path!(u32 / u32)"] # [doc = "     .map(|a, b| {"] # [doc = "         format!(\"{} + {} = {}\", a, b, a + b)"] # [doc = "     });"] # [doc = ""] # [doc = " let help = warp::path::end()"] # [doc = "     .map(|| \"This API returns the sum of two u32's\");"] # [doc = ""] # [doc = " let api = prefix.and(sum.or(help));"] # [doc = " ```"] # [macro_export] macro_rules ! path { ($ ($ pieces : tt) *) => ({ $ crate :: __internal_path ! (@ start $ ($ pieces) *) }) ; }
};
}
