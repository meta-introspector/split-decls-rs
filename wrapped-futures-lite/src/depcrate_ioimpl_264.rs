// Generated macro for impl_264 (impl)
macro_rules! Depcrate_ioimpl_264 {
() => {
// Module: crate::io
// Provides: {"impl_264"}
// Dependencies: {}
impl < R : AsyncSeek > AsyncSeek for BufReader < R > { # [doc = " Seeks to an offset, in bytes, in the underlying reader."] # [doc = ""] # [doc = " The position used for seeking with [`SeekFrom::Current`] is the position the underlying"] # [doc = " reader would be at if the [`BufReader`] had no internal buffer."] # [doc = ""] # [doc = " Seeking always discards the internal buffer, even if the seek position would otherwise fall"] # [doc = " within it. This guarantees that calling [`into_inner()`][`BufReader::into_inner()`]"] # [doc = " immediately after a seek yields the underlying reader at the same position."] # [doc = ""] # [doc = " See [`AsyncSeek`] for more details."] # [doc = ""] # [doc = " Note: In the edge case where you're seeking with `SeekFrom::Current(n)` where `n` minus the"] # [doc = " internal buffer length overflows an `i64`, two seeks will be performed instead of one. If"] # [doc = " the second seek returns `Err`, the underlying reader will be left at the same position it"] # [doc = " would have if you called [`seek()`][`AsyncSeekExt::seek()`] with `SeekFrom::Current(0)`."] fn poll_seek (mut self : Pin < & mut Self > , cx : & mut Context < '_ > , pos : SeekFrom ,) -> Poll < Result < u64 > > { let result : u64 ; if let SeekFrom :: Current (n) = pos { let remainder = (self . cap - self . pos) as i64 ; if let Some (offset) = n . checked_sub (remainder) { result = ready ! (self . as_mut () . get_pin_mut () . poll_seek (cx , SeekFrom :: Current (offset))) ? ; } else { ready ! (self . as_mut () . get_pin_mut () . poll_seek (cx , SeekFrom :: Current (- remainder))) ? ; self . as_mut () . discard_buffer () ; result = ready ! (self . as_mut () . get_pin_mut () . poll_seek (cx , SeekFrom :: Current (n))) ? ; } } else { result = ready ! (self . as_mut () . get_pin_mut () . poll_seek (cx , pos)) ? ; } self . discard_buffer () ; Poll :: Ready (Ok (result)) } }
};
}
