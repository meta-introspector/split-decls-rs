macro_rules! deps {
    () => {
        ReadUntilFuture!();
        ReadLineFuture!();
        FillBuf!();
        Cursor!();
    };
}

macro_rules! AsyncBufReadExt {
    () => {
        deps!();
        # [doc = " Extension trait for [`AsyncBufRead`]."] pub trait AsyncBufReadExt : AsyncBufRead { # [doc = " Returns the contents of the internal buffer, filling it with more data if empty."] # [doc = ""] # [doc = " If the stream has reached EOF, an empty buffer will be returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::{AsyncBufReadExt, BufReader};"] # [doc = " use std::pin::Pin;"] # [doc = ""] # [doc = " # spin_on::spin_on(async {"] # [doc = " let input: &[u8] = b\"hello world\";"] # [doc = " let mut reader = BufReader::with_capacity(5, input);"] # [doc = ""] # [doc = " assert_eq!(reader.fill_buf().await?, b\"hello\");"] # [doc = " reader.consume(2);"] # [doc = " assert_eq!(reader.fill_buf().await?, b\"llo\");"] # [doc = " reader.consume(3);"] # [doc = " assert_eq!(reader.fill_buf().await?, b\" worl\");"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] fn fill_buf (& mut self) -> FillBuf < '_ , Self > where Self : Unpin , { FillBuf { reader : Some (self) } } # [doc = " Consumes `amt` buffered bytes."] # [doc = ""] # [doc = " This method does not perform any I/O, it simply consumes some amount of bytes from the"] # [doc = " internal buffer."] # [doc = ""] # [doc = " The `amt` must be <= the number of bytes in the buffer returned by"] # [doc = " [`fill_buf()`][`AsyncBufReadExt::fill_buf()`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::{AsyncBufReadExt, BufReader};"] # [doc = " use std::pin::Pin;"] # [doc = ""] # [doc = " # spin_on::spin_on(async {"] # [doc = " let input: &[u8] = b\"hello\";"] # [doc = " let mut reader = BufReader::with_capacity(4, input);"] # [doc = ""] # [doc = " assert_eq!(reader.fill_buf().await?, b\"hell\");"] # [doc = " reader.consume(2);"] # [doc = " assert_eq!(reader.fill_buf().await?, b\"ll\");"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] fn consume (& mut self , amt : usize) where Self : Unpin , { AsyncBufRead :: consume (Pin :: new (self) , amt) ; } # [doc = " Reads all bytes and appends them into `buf` until the delimiter `byte` or EOF is found."] # [doc = ""] # [doc = " This method will read bytes from the underlying stream until the delimiter or EOF is"] # [doc = " found. All bytes up to and including the delimiter (if found) will be appended to `buf`."] # [doc = ""] # [doc = " If successful, returns the total number of bytes read."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::{AsyncBufReadExt, BufReader};"] # [doc = ""] # [doc = " # spin_on::spin_on(async {"] # [doc = " let input: &[u8] = b\"hello\";"] # [doc = " let mut reader = BufReader::new(input);"] # [doc = ""] # [doc = " let mut buf = Vec::new();"] # [doc = " let n = reader.read_until(b'\\n', &mut buf).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] fn read_until < 'a > (& 'a mut self , byte : u8 , buf : & 'a mut Vec < u8 >) -> ReadUntilFuture < 'a , Self > where Self : Unpin , { ReadUntilFuture { reader : self , byte , buf , read : 0 , } } # [doc = " Reads all bytes and appends them into `buf` until a newline (the 0xA byte) or EOF is found."] # [doc = ""] # [doc = " This method will read bytes from the underlying stream until the newline delimiter (the"] # [doc = " 0xA byte) or EOF is found. All bytes up to, and including, the newline delimiter (if found)"] # [doc = " will be appended to `buf`."] # [doc = ""] # [doc = " If successful, returns the total number of bytes read."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::{AsyncBufReadExt, BufReader};"] # [doc = ""] # [doc = " # spin_on::spin_on(async {"] # [doc = " let input: &[u8] = b\"hello\";"] # [doc = " let mut reader = BufReader::new(input);"] # [doc = ""] # [doc = " let mut line = String::new();"] # [doc = " let n = reader.read_line(&mut line).await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] fn read_line < 'a > (& 'a mut self , buf : & 'a mut String) -> ReadLineFuture < 'a , Self > where Self : Unpin , { ReadLineFuture { reader : self , buf , bytes : Vec :: new () , read : 0 , } } # [doc = " Returns a stream over the lines of this byte stream."] # [doc = ""] # [doc = " The stream returned from this method yields items of type"] # [doc = " [`io::Result`][`super::io::Result`]`<`[`String`]`>`."] # [doc = " Each string returned will *not* have a newline byte (the 0xA byte) or CRLF (0xD, 0xA bytes)"] # [doc = " at the end."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::{AsyncBufReadExt, BufReader};"] # [doc = " use futures_lite::stream::StreamExt;"] # [doc = ""] # [doc = " # spin_on::spin_on(async {"] # [doc = " let input: &[u8] = b\"hello\\nworld\\n\";"] # [doc = " let mut reader = BufReader::new(input);"] # [doc = " let mut lines = reader.lines();"] # [doc = ""] # [doc = " while let Some(line) = lines.next().await {"] # [doc = "     println!(\"{}\", line?);"] # [doc = " }"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] fn lines (self) -> Lines < Self > where Self : Sized , { Lines { reader : self , buf : String :: new () , bytes : Vec :: new () , read : 0 , } } # [doc = " Returns a stream over the contents of this reader split on the specified `byte`."] # [doc = ""] # [doc = " The stream returned from this method yields items of type"] # [doc = " [`io::Result`][`super::io::Result`]`<`[`Vec<u8>`][`Vec`]`>`."] # [doc = " Each vector returned will *not* have the delimiter byte at the end."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::{AsyncBufReadExt, Cursor};"] # [doc = " use futures_lite::stream::StreamExt;"] # [doc = ""] # [doc = " # spin_on::spin_on(async {"] # [doc = " let cursor = Cursor::new(b\"lorem-ipsum-dolor\");"] # [doc = " let items: Vec<Vec<u8>> = cursor.split(b'-').try_collect().await?;"] # [doc = ""] # [doc = " assert_eq!(items[0], b\"lorem\");"] # [doc = " assert_eq!(items[1], b\"ipsum\");"] # [doc = " assert_eq!(items[2], b\"dolor\");"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] fn split (self , byte : u8) -> Split < Self > where Self : Sized , { Split { reader : self , buf : Vec :: new () , delim : byte , read : 0 , } } }
    };
}

AsyncBufReadExt!()