macro_rules! macro_286 {
    () => {
        pin_project ! { # [doc = " Reader for the [`AsyncReadExt::bytes()`] method."] # [derive (Debug)] pub struct Bytes < R > { # [pin] inner : R , } }
    };
}

macro_286!();