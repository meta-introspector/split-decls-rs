macro_rules! macro_281 {
    () => {
        pin_project ! { # [doc = " Reader for the [`AsyncReadExt::take()`] method."] # [derive (Debug)] pub struct Take < R > { # [pin] inner : R , limit : u64 , } }
    };
}

macro_281!()