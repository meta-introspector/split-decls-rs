macro_rules! deps {
    () => {
        WriteFuture!();
        WriteVectoredFuture!();
        WriteAllFuture!();
        FlushFuture!();
        CloseFuture!();
    };
}

macro_rules! AsyncWriteExt {
    () => {
        deps!();
        # [doc = " Extension trait for [`AsyncWrite`]."] pub trait AsyncWriteExt : AsyncWrite { # [doc = " Writes some bytes into the byte stream."] # [doc = ""] # [doc = " Returns the number of bytes written from the start of the buffer."] # [doc = ""] # [doc = " If the return value is `Ok(n)` then it must be guaranteed that"] # [doc = " `0 <= n <= buf.len()`. A return value of `0` typically means that the underlying"] # [doc = " object is no longer able to accept bytes and will likely not be able to in the"] # [doc = " future as well, or that the provided buffer is empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::{AsyncWriteExt, BufWriter};"] # [doc = ""] # [doc = " # spin_on::spin_on(async {"] # [doc = " let mut output = Vec::new();"] # [doc = " let mut writer = BufWriter::new(&mut output);"] # [doc = ""] # [doc = " let n = writer.write(b\"hello\").await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] fn write < 'a > (& 'a mut self , buf : & 'a [u8]) -> WriteFuture < 'a , Self > where Self : Unpin , { WriteFuture { writer : self , buf } } # [doc = " Like [`write()`][`AsyncWriteExt::write()`], except that it writes a slice of buffers."] # [doc = ""] # [doc = " Data is copied from each buffer in order, with the final buffer possibly being only"] # [doc = " partially consumed. This method must behave same as a call to"] # [doc = " [`write()`][`AsyncWriteExt::write()`] with the buffers concatenated would."] fn write_vectored < 'a > (& 'a mut self , bufs : & 'a [IoSlice < 'a >]) -> WriteVectoredFuture < 'a , Self > where Self : Unpin , { WriteVectoredFuture { writer : self , bufs } } # [doc = " Writes an entire buffer into the byte stream."] # [doc = ""] # [doc = " This method will keep calling [`write()`][`AsyncWriteExt::write()`] until there is no more"] # [doc = " data to be written or an error occurs. It will not return before the entire buffer is"] # [doc = " successfully written or an error occurs."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::{AsyncWriteExt, BufWriter};"] # [doc = ""] # [doc = " # spin_on::spin_on(async {"] # [doc = " let mut output = Vec::new();"] # [doc = " let mut writer = BufWriter::new(&mut output);"] # [doc = ""] # [doc = " let n = writer.write_all(b\"hello\").await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] fn write_all < 'a > (& 'a mut self , buf : & 'a [u8]) -> WriteAllFuture < 'a , Self > where Self : Unpin , { WriteAllFuture { writer : self , buf } } # [doc = " Flushes the stream to ensure that all buffered contents reach their destination."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::{AsyncWriteExt, BufWriter};"] # [doc = ""] # [doc = " # spin_on::spin_on(async {"] # [doc = " let mut output = Vec::new();"] # [doc = " let mut writer = BufWriter::new(&mut output);"] # [doc = ""] # [doc = " writer.write_all(b\"hello\").await?;"] # [doc = " writer.flush().await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] fn flush (& mut self) -> FlushFuture < '_ , Self > where Self : Unpin , { FlushFuture { writer : self } } # [doc = " Closes the writer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::{AsyncWriteExt, BufWriter};"] # [doc = ""] # [doc = " # spin_on::spin_on(async {"] # [doc = " let mut output = Vec::new();"] # [doc = " let mut writer = BufWriter::new(&mut output);"] # [doc = ""] # [doc = " writer.close().await?;"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] fn close (& mut self) -> CloseFuture < '_ , Self > where Self : Unpin , { CloseFuture { writer : self } } # [doc = " Boxes the writer and changes its type to `dyn AsyncWrite + Send + 'a`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::AsyncWriteExt;"] # [doc = ""] # [doc = " let writer = Vec::<u8>::new().boxed_writer();"] # [doc = " ```"] # [cfg (feature = "alloc")] fn boxed_writer < 'a > (self) -> Pin < Box < dyn AsyncWrite + Send + 'a > > where Self : Sized + Send + 'a , { Box :: pin (self) } }
    };
}

AsyncWriteExt!();