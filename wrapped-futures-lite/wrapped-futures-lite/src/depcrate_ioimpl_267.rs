// Generated macro for impl_267 (impl)
macro_rules! Depcrate_ioimpl_267 {
() => {
// Module: crate::io
// Provides: {"impl_267"}
// Dependencies: {}
impl < W : AsyncWrite > BufWriter < W > { # [doc = " Creates a buffered writer with the default buffer capacity."] # [doc = ""] # [doc = " The default capacity is currently 8 KB, but that may change in the future."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::BufWriter;"] # [doc = ""] # [doc = " let mut output = Vec::new();"] # [doc = " let writer = BufWriter::new(&mut output);"] # [doc = " ```"] pub fn new (inner : W) -> BufWriter < W > { BufWriter :: with_capacity (DEFAULT_BUF_SIZE , inner) } # [doc = " Creates a buffered writer with the specified buffer capacity."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::BufWriter;"] # [doc = ""] # [doc = " let mut output = Vec::new();"] # [doc = " let writer = BufWriter::with_capacity(100, &mut output);"] # [doc = " ```"] pub fn with_capacity (capacity : usize , inner : W) -> BufWriter < W > { BufWriter { inner , buf : Vec :: with_capacity (capacity) , written : 0 , } } # [doc = " Gets a reference to the underlying writer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::BufWriter;"] # [doc = ""] # [doc = " let mut output = Vec::new();"] # [doc = " let writer = BufWriter::new(&mut output);"] # [doc = ""] # [doc = " let r = writer.get_ref();"] # [doc = " ```"] pub fn get_ref (& self) -> & W { & self . inner } # [doc = " Gets a mutable reference to the underlying writer."] # [doc = ""] # [doc = " It is not advisable to directly write to the underlying writer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::BufWriter;"] # [doc = ""] # [doc = " let mut output = Vec::new();"] # [doc = " let mut writer = BufWriter::new(&mut output);"] # [doc = ""] # [doc = " let r = writer.get_mut();"] # [doc = " ```"] pub fn get_mut (& mut self) -> & mut W { & mut self . inner } # [doc = " Gets a pinned mutable reference to the underlying writer."] # [doc = ""] # [doc = " It is not not advisable to directly write to the underlying writer."] fn get_pin_mut (self : Pin < & mut Self >) -> Pin < & mut W > { self . project () . inner } # [doc = " Unwraps the buffered writer, returning the underlying writer."] # [doc = ""] # [doc = " Note that any leftover data in the internal buffer will be lost. If you don't want to lose"] # [doc = " that data, flush the buffered writer before unwrapping it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::{AsyncWriteExt, BufWriter};"] # [doc = ""] # [doc = " # spin_on::spin_on(async {"] # [doc = " let mut output = vec![1, 2, 3];"] # [doc = " let mut writer = BufWriter::new(&mut output);"] # [doc = ""] # [doc = " writer.write_all(&[4]).await?;"] # [doc = " writer.flush().await?;"] # [doc = " assert_eq!(writer.into_inner(), &[1, 2, 3, 4]);"] # [doc = " # std::io::Result::Ok(()) });"] # [doc = " ```"] pub fn into_inner (self) -> W { self . inner } # [doc = " Returns a reference to the internal buffer."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use futures_lite::io::BufWriter;"] # [doc = ""] # [doc = " let mut output = Vec::new();"] # [doc = " let writer = BufWriter::new(&mut output);"] # [doc = ""] # [doc = " // The internal buffer is empty until the first write request."] # [doc = " assert_eq!(writer.buffer(), &[]);"] # [doc = " ```"] pub fn buffer (& self) -> & [u8] { & self . buf } # [doc = " Flush the buffer."] fn poll_flush_buf (self : Pin < & mut Self > , cx : & mut Context < '_ >) -> Poll < Result < () > > { let mut this = self . project () ; let len = this . buf . len () ; let mut ret = Ok (()) ; while * this . written < len { match this . inner . as_mut () . poll_write (cx , & this . buf [* this . written ..]) { Poll :: Ready (Ok (0)) => { ret = Err (Error :: new (ErrorKind :: WriteZero , "Failed to write buffered data" ,)) ; break ; } Poll :: Ready (Ok (n)) => * this . written += n , Poll :: Ready (Err (ref e)) if e . kind () == ErrorKind :: Interrupted => { } Poll :: Ready (Err (e)) => { ret = Err (e) ; break ; } Poll :: Pending => return Poll :: Pending , } } if * this . written > 0 { this . buf . drain (.. * this . written) ; } * this . written = 0 ; Poll :: Ready (ret) } }
};
}
