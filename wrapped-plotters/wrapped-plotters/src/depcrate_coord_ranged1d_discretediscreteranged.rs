// Generated macro for DiscreteRanged (trait)
macro_rules! Depcrate_coord_ranged1d_discreteDiscreteRanged {
() => {
// Module: crate::coord::ranged1d::discrete
// Provides: {"DiscreteRanged"}
// Dependencies: {}
# [doc = " The trait indicates the coordinate is discrete"] # [doc = " This means we can bidirectionally map the range value to 0 to N"] # [doc = " in which N is the number of distinct values of the range."] # [doc = ""] # [doc = " This is useful since for a histgoram, this is an abstraction of bucket."] pub trait DiscreteRanged where Self : Ranged , { # [doc = " Get the number of element in the range"] # [doc = " Note: we assume that all the ranged discrete coordinate has finite value"] # [doc = ""] # [doc = " - **returns** The number of values in the range"] fn size (& self) -> usize ; # [doc = " Map a value to the index"] # [doc = ""] # [doc = " Note: This function doesn't guarantee return None when the value is out of range."] # [doc = " The only way to confirm the value is in the range is to examining the return value isn't"] # [doc = " larger than self.size."] # [doc = ""] # [doc = " - `value`: The value to map"] # [doc = " - **returns** The index of the value"] fn index_of (& self , value : & Self :: ValueType) -> Option < usize > ; # [doc = " Reverse map the index to the value"] # [doc = ""] # [doc = " Note: This function doesn't guarantee returning None when the index is out of range."] # [doc = ""] # [doc = " - `value`: The index to map"] # [doc = " - **returns** The value"] # [allow (clippy :: wrong_self_convention)] fn from_index (& self , index : usize) -> Option < Self :: ValueType > ; # [doc = " Return a iterator that iterates over the all possible values"] # [doc = ""] # [doc = " - **returns** The value iterator"] fn values (& self) -> DiscreteValueIter < '_ , Self > where Self : Sized , { DiscreteValueIter (self , 0 , self . size ()) } # [doc = " Returns the previous value in this range"] # [doc = ""] # [doc = " Normally, it's based on the `from_index` and `index_of` function. But for"] # [doc = " some of the coord spec, it's possible that we value faster implementation."] # [doc = " If this is the case, we can impelemnet the type specific impl for the `previous`"] # [doc = " and `next`."] # [doc = ""] # [doc = " - `value`: The current value"] # [doc = " - **returns**: The value piror to current value"] fn previous (& self , value : & Self :: ValueType) -> Option < Self :: ValueType > { if let Some (idx) = self . index_of (value) { if idx > 0 { return self . from_index (idx - 1) ; } } None } # [doc = " Returns the next value in this range"] # [doc = ""] # [doc = " Normally, it's based on the `from_index` and `index_of` function. But for"] # [doc = " some of the coord spec, it's possible that we value faster implementation."] # [doc = " If this is the case, we can impelemnet the type specific impl for the `previous`"] # [doc = " and `next`."] # [doc = ""] # [doc = " - `value`: The current value"] # [doc = " - **returns**: The value next to current value"] fn next (& self , value : & Self :: ValueType) -> Option < Self :: ValueType > { if let Some (idx) = self . index_of (value) { if idx + 1 < self . size () { return self . from_index (idx + 1) ; } } None } }
};
}
