// Generated macro for PointCollection (trait)
macro_rules! Depcrate_elementPointCollection {
() => {
// Module: crate::element
// Provides: {"PointCollection"}
// Dependencies: {}
# [doc = " A type which is logically a collection of points, under any given coordinate system."] # [doc = " Note: Ideally, a point collection trait should be any type of which coordinate elements can be"] # [doc = " iterated. This is similar to `iter` method of many collection types in std."] # [doc = ""] # [doc = " ```ignore"] # [doc = " trait PointCollection<Coord> {"] # [doc = "     type PointIter<'a> : Iterator<Item = &'a Coord>;"] # [doc = "     fn iter(&self) -> PointIter<'a>;"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " However,"] # [doc = " [Generic Associated Types](https://github.com/rust-lang/rfcs/blob/master/text/1598-generic_associated_types.md)"] # [doc = " is far away from stabilize."] # [doc = " So currently we have the following workaround:"] # [doc = ""] # [doc = " Instead of implement the PointCollection trait on the element type itself, it implements on the"] # [doc = " reference to the element. By doing so, we now have a well-defined lifetime for the iterator."] # [doc = ""] # [doc = " In addition, for some element, the coordinate is computed on the fly, thus we can't hard-code"] # [doc = " the iterator's return type is `&'a Coord`."] # [doc = " `Borrow` trait seems to strict in this case, since we don't need the order and hash"] # [doc = " preservation properties at this point. However, `AsRef` doesn't work with `Coord`"] # [doc = ""] # [doc = " This workaround also leads overly strict lifetime bound on `ChartContext::draw_series`."] # [doc = ""] # [doc = " TODO: Once GAT is ready on stable Rust, we should simplify the design."] # [doc = ""] pub trait PointCollection < 'a , Coord , CM = BackendCoordOnly > { # [doc = " The item in point iterator"] type Point : Borrow < Coord > + 'a ; # [doc = " The point iterator"] type IntoIter : IntoIterator < Item = Self :: Point > ; # [doc = " framework to do the coordinate mapping"] fn point_iter (self) -> Self :: IntoIter ; }
};
}
