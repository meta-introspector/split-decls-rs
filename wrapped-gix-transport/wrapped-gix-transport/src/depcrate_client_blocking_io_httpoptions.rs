// Generated macro for Options (struct)
macro_rules! Depcrate_client_blocking_io_httpOptions {
() => {
// Module: crate::client::blocking_io::http
// Provides: {"Options"}
// Dependencies: {}
# [doc = " Options to configure http requests."] # [derive (Clone)] pub struct Options { # [doc = " Headers to be added to every request."] # [doc = " They are applied unconditionally and are expected to be valid as they occur in an HTTP request, like `header: value`, without newlines."] # [doc = ""] # [doc = " Refers to `http.extraHeader` multi-var."] pub extra_headers : Vec < String > , # [doc = " How to handle redirects."] # [doc = ""] # [doc = " Refers to `http.followRedirects`."] pub follow_redirects : options :: FollowRedirects , # [doc = " Used in conjunction with `low_speed_time_seconds`, any non-0 value signals the amount of bytes per second at least to avoid"] # [doc = " aborting the connection."] # [doc = ""] # [doc = " Refers to `http.lowSpeedLimit`."] pub low_speed_limit_bytes_per_second : u32 , # [doc = " Used in conjunction with `low_speed_bytes_per_second`, any non-0 value signals the amount seconds the minimal amount"] # [doc = " of bytes per second isn't reached."] # [doc = ""] # [doc = " Refers to `http.lowSpeedTime`."] pub low_speed_time_seconds : u64 , # [doc = " A curl-style proxy declaration of the form `[protocol://][user[:password]@]proxyhost[:port]`."] # [doc = ""] # [doc = " Note that an empty string means the proxy is disabled entirely."] # [doc = " Refers to `http.proxy`."] pub proxy : Option < String > , # [doc = " The comma-separated list of hosts to not send through the `proxy`, or `*` to entirely disable all proxying."] pub no_proxy : Option < String > , # [doc = " The way to authenticate against the proxy if the `proxy` field contains a username."] # [doc = ""] # [doc = " Refers to `http.proxyAuthMethod`."] pub proxy_auth_method : options :: ProxyAuthMethod , # [doc = " If authentication is needed for the proxy as its URL contains a username, this method must be set to provide a password"] # [doc = " for it before making the request, and to store it if the connection succeeds."] pub proxy_authenticate : Option < (gix_credentials :: helper :: Action , Arc < std :: sync :: Mutex < options :: AuthenticateFn > > ,) > , # [doc = " The `HTTP` `USER_AGENT` string presented to an `HTTP` server, notably not the user agent present to the `git` server."] # [doc = ""] # [doc = " If not overridden, it defaults to the user agent provided by `curl`, which is a deviation from how `git` handles this."] # [doc = " Thus it's expected from the callers to set it to their application, or use higher-level crates which make it easy to do this"] # [doc = " more correctly."] # [doc = ""] # [doc = " Using the correct user-agent might affect how the server treats the request."] # [doc = ""] # [doc = " Refers to `http.userAgent`."] pub user_agent : Option < String > , # [doc = " The amount of time we wait until aborting a connection attempt."] # [doc = ""] # [doc = " If `None`, this typically defaults to 2 minutes to 5 minutes."] # [doc = " Refers to `gitoxide.http.connectTimeout`."] pub connect_timeout : Option < std :: time :: Duration > , # [doc = " If enabled, emit additional information about connections and possibly the data received or written."] pub verbose : bool , # [doc = " If set, use this path to point to a file with CA certificates to verify peers."] pub ssl_ca_info : Option < PathBuf > , # [doc = " The SSL version or version range to use, or `None` to let the TLS backend determine which versions are acceptable."] pub ssl_version : Option < SslVersionRangeInclusive > , # [doc = " Controls whether to perform SSL identity verification or not. Turning this off is not recommended and can lead to"] # [doc = " various security risks. An example where this may be needed is when an internal git server uses a self-signed"] # [doc = " certificate and the user accepts the associated security risks."] pub ssl_verify : bool , # [doc = " The HTTP version to enforce. If unset, it is implementation defined."] pub http_version : Option < HttpVersion > , # [doc = " Backend specific options, if available."] pub backend : Option < Arc < Mutex < dyn Any + Send + Sync + 'static > > > , }
};
}
