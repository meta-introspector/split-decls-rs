// Generated macro for Options (struct)
macro_rules! Depcrate_index_as_worktree_with_renames_typesOptions {
() => {
// Module: crate::index_as_worktree_with_renames::types
// Provides: {"Options"}
// Dependencies: {}
# [doc = " Options for use in [index_as_worktree_with_renames()](crate::index_as_worktree_with_renames())."] # [derive (Clone , Default)] pub struct Options < 'a > { # [doc = " The way all output should be sorted."] # [doc = ""] # [doc = " If `None`, and depending on the `rewrites` field, output will be immediate but the output order"] # [doc = " isn't determined, and may differ between two runs. `rewrites` also depend on the order of entries that"] # [doc = " are presented to it, hence for deterministic results, sorting needs to be enabled."] # [doc = ""] # [doc = " If `Some(_)`, all entries are collected beforehand, so they can be sorted before outputting any of them"] # [doc = " to the user."] # [doc = ""] # [doc = " If immediate output of entries in any order is desired, this should be `None`,"] # [doc = " along with `rewrites` being `None` as well."] pub sorting : Option < Sorting > , # [doc = " The kind of hash to create when hashing worktree entries."] pub object_hash : gix_hash :: Kind , # [doc = " Options to configure how modifications to tracked files should be obtained."] pub tracked_file_modifications : crate :: index_as_worktree :: Options , # [doc = " Options to control the directory walk that informs about untracked files."] # [doc = ""] # [doc = " Note that we forcefully disable emission of tracked files to avoid any overlap"] # [doc = " between emissions to indicate modifications, and those that are obtained by"] # [doc = " the directory walk."] # [doc = ""] # [doc = " If `None`, the directory walk portion will not run at all, yielding data similar"] # [doc = " to a bare [index_as_worktree()](crate::index_as_worktree()) call."] pub dirwalk : Option < gix_dir :: walk :: Options < 'a > > , # [doc = " The configuration for the rewrite tracking. Note that if set, the [`dirwalk`](Self::dirwalk) should be configured"] # [doc = " to *not* collapse untracked and ignored entries, as rewrite tracking is on a file-by-file basis."] # [doc = " Also note that when `Some(_)`, it will collect certain changes depending on the exact configuration, which typically increases"] # [doc = " the latency until the first entries are received. Note that some entries are never candidates for renames, which means"] # [doc = " they are forwarded to the caller right away."] # [doc = ""] # [doc = " If `None`, no tracking will occur, which means that all output becomes visible to the delegate immediately."] pub rewrites : Option < gix_diff :: Rewrites > , }
};
}
