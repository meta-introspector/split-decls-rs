// Generated macro for Entry (enum)
macro_rules! Depcrate_index_as_worktree_with_renames_typesEntry {
() => {
// Module: crate::index_as_worktree_with_renames::types
// Provides: {"Entry"}
// Dependencies: {}
# [doc = " An 'entry' in the sense of a merge of modified tracked files and results from a directory walk."] # [derive (Clone , Debug)] pub enum Entry < 'index , ContentChange , SubmoduleStatus > { # [doc = " A tracked file was modified, and index-specific information is passed."] Modification { # [doc = " All entries in the index."] entries : & 'index [gix_index :: Entry] , # [doc = " The entry with modifications."] entry : & 'index gix_index :: Entry , # [doc = " The index of the `entry` for lookup in `entries` - useful to look at neighbors."] entry_index : usize , # [doc = " The repository-relative path of the entry."] rela_path : & 'index BStr , # [doc = " The computed status of the entry."] status : EntryStatus < ContentChange , SubmoduleStatus > , } , # [doc = " An entry returned by the directory walk, without any relation to the index."] # [doc = ""] # [doc = " This can happen if ignored files are returned as well, or if rename-tracking is disabled."] DirectoryContents { # [doc = " The entry found during the disk traversal."] entry : gix_dir :: Entry , # [doc = " `collapsed_directory_status` is `Some(dir_status)` if this `entry` was part of a directory with the given"] # [doc = " `dir_status` that wasn't the same as the one of `entry` and if [gix_dir::walk::Options::emit_collapsed] was"] # [doc = " [CollapsedEntriesEmissionMode::OnStatusMismatch](gix_dir::walk::CollapsedEntriesEmissionMode::OnStatusMismatch)."] # [doc = " It will also be `Some(dir_status)` if that option was [CollapsedEntriesEmissionMode::All](gix_dir::walk::CollapsedEntriesEmissionMode::All)."] collapsed_directory_status : Option < gix_dir :: entry :: Status > , } , # [doc = " The rewrite tracking discovered a match between a deleted and added file, and considers them equal enough,"] # [doc = " depending on the tracker settings."] # [doc = ""] # [doc = " Note that the source of the rewrite is always the index as it detects the absence of entries, something that"] # [doc = " can't be done during a directory walk."] Rewrite { # [doc = " The source of the rewrite operation."] source : RewriteSource < 'index , ContentChange , SubmoduleStatus > , # [doc = " The untracked entry found during the disk traversal, the destination of the rewrite."] # [doc = ""] # [doc = " Note that its [`rela_path`](gix_dir::EntryRef::rela_path) is the destination of the rewrite, and the current"] # [doc = " location of the entry."] dirwalk_entry : gix_dir :: Entry , # [doc = " `collapsed_directory_status` is `Some(dir_status)` if this `dirwalk_entry` was part of a directory with the given"] # [doc = " `dir_status` that wasn't the same as the one of `dirwalk_entry` and if [gix_dir::walk::Options::emit_collapsed] was"] # [doc = " [CollapsedEntriesEmissionMode::OnStatusMismatch](gix_dir::walk::CollapsedEntriesEmissionMode::OnStatusMismatch)."] # [doc = " It will also be `Some(dir_status)` if that option was [CollapsedEntriesEmissionMode::All](gix_dir::walk::CollapsedEntriesEmissionMode::All)."] dirwalk_entry_collapsed_directory_status : Option < gix_dir :: entry :: Status > , # [doc = " The object id as it would appear if the entry was written to the object database, specifically hashed in order to determine equality."] # [doc = " Note that it doesn't (necessarily) exist in the object database, and may be [null](gix_hash::ObjectId::null) if no hashing"] # [doc = " was performed."] dirwalk_entry_id : gix_hash :: ObjectId , # [doc = " It's `None` if the 'source.id' is equal to `dirwalk_entry_id`, as identity made an actual diff computation unnecessary."] # [doc = " Otherwise, and if enabled, it's `Some(stats)` to indicate how similar both entries were."] diff : Option < gix_diff :: blob :: DiffLineStats > , # [doc = " If true, this rewrite is created by copy, and 'source.id' is pointing to its source."] # [doc = " Otherwise, it's a rename, and 'source.id' points to a deleted object,"] # [doc = " as renames are tracked as deletions and additions of the same or similar content."] copy : bool , } , }
};
}
