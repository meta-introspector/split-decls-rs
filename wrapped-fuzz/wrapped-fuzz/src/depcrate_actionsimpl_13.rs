// Generated macro for impl_13 (impl)
macro_rules! Depcrate_actionsimpl_13 {
() => {
// Module: crate::actions
// Provides: {"impl_13"}
// Dependencies: {}
impl Action < '_ > { pub fn perform (self , control : & mut String , compact : & mut CompactString) { use Action :: * ; match self { Push (c) => { control . push (c) ; compact . push (c) ; let og_capacity = compact . capacity () ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; if og_capacity != compact . capacity () { super :: assert_properly_allocated (compact , control) ; } } Pop (count) => { (0 .. count) . for_each (| _ | { let a = control . pop () ; let b = compact . pop () ; assert_eq ! (a , b) ; }) ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; assert_eq ! (control . is_empty () , compact . is_empty ()) ; } PushStr (s) => { control . push_str (s) ; compact . push_str (s) ; let og_capacity = compact . capacity () ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; if og_capacity != compact . capacity () { super :: assert_properly_allocated (compact , control) ; } } ExtendChars (chs) => { control . extend (chs . iter ()) ; compact . extend (chs . iter ()) ; let og_capacity = compact . capacity () ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; if og_capacity != compact . capacity () { super :: assert_properly_allocated (compact , control) ; } } ExtendStr (strs) => { control . extend (strs . iter () . copied ()) ; compact . extend (strs . iter () . copied ()) ; let og_capacity = compact . capacity () ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; if og_capacity != compact . capacity () { super :: assert_properly_allocated (compact , control) ; } } CheckSubslice (a , b) => { assert_eq ! (control . len () , compact . len ()) ; let c = a as f32 / (u8 :: MAX as f32) ; let d = b as f32 / (u8 :: MAX as f32) ; let e = (c * compact . len () as f32) as usize ; let f = (d * compact . len () as f32) as usize ; let lower = core :: cmp :: min (e , f) ; let upper = core :: cmp :: max (e , f) ; let lower = core :: cmp :: min (lower . wrapping_sub (1) , lower) ; let upper = core :: cmp :: min (upper + 1 , compact . len ()) ; let control_slice = & control . as_bytes () [lower .. upper] ; let compact_slice = & compact . as_bytes () [lower .. upper] ; assert_eq ! (control_slice , compact_slice) ; } MakeUppercase => { control . as_mut_str () . make_ascii_uppercase () ; compact . as_mut_str () . make_ascii_uppercase () ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; } ReplaceRange (start , end , replace_with) => { let start = to_index (control , start) ; let end = to_index (control , end) ; let (start , end) = (start . min (end) , start . max (end)) ; control . replace_range (start .. end , replace_with) ; compact . replace_range (start .. end , replace_with) ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; } Reserve (num_bytes) => { if (compact . capacity () + num_bytes as usize) > super :: TWENTY_FOUR_MIB_AS_BYTES { return ; } let og_capacity = compact . capacity () ; compact . reserve (num_bytes as usize) ; control . reserve (num_bytes as usize) ; assert_eq ! (compact , control) ; assert_eq ! (compact . len () , control . len ()) ; if og_capacity != compact . capacity () && og_capacity + num_bytes as usize <= super :: MAX_INLINE_LENGTH { assert ! (! compact . is_heap_allocated ()) ; } } Truncate (new_len) => { let new_len = to_index (control , new_len) ; control . truncate (new_len) ; compact . truncate (new_len) ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; } InsertStr (idx , s) => { let idx = to_index (control , idx) ; let og_capacity = compact . capacity () ; control . insert_str (idx , s) ; compact . insert_str (idx , s) ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; if og_capacity != compact . capacity () { super :: assert_properly_allocated (compact , control) ; } } Insert (idx , ch) => { let idx = to_index (control , idx) ; let og_capacity = compact . capacity () ; control . insert (idx , ch) ; compact . insert (idx , ch) ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; if og_capacity != compact . capacity () { super :: assert_properly_allocated (compact , control) ; } } Clear => { control . clear () ; compact . clear () ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; } SplitOff (at) => { let at = to_index (control , at) ; let compact_capacity = compact . capacity () ; assert_eq ! (compact . split_off (at) , control . split_off (at)) ; if compact . as_static_str () . is_some () { assert_eq ! (compact . capacity () , at) ; } else { assert_eq ! (compact . capacity () , compact_capacity) ; } assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; } Drain (start , end) => { let start = to_index (control , start) ; let end = to_index (control , end) ; let (start , end) = (start . min (end) , start . max (end)) ; let compact_capacity = compact . capacity () ; let is_static = compact . as_static_str () . is_some () ; let control_drain = control . drain (start .. end) ; let compact_drain = compact . drain (start .. end) ; assert_eq ! (control_drain . as_str () , compact_drain . as_str ()) ; drop (control_drain) ; drop (compact_drain) ; assert_eq ! (control . as_str () , compact . as_str ()) ; if ! is_static { assert_eq ! (compact . capacity () , compact_capacity) ; } } Remove (val) => { let idx = to_index (control , val) ; let idx = if idx == control . len () { 0 } else { idx } ; if control . is_empty () && compact . is_empty () { assert_eq ! (idx , 0) ; return ; } assert_eq ! (control . remove (idx) , compact . remove (idx)) ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; } ShrinkTo (a , b) => { let a = (a % 5000) as usize ; let b = (b % 5000) as usize ; let (reserve , shrink) = (a . max (b) , a . min (b)) ; control . reserve (reserve) ; compact . reserve (reserve) ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; control . shrink_to (shrink) ; compact . shrink_to (shrink) ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; control . shrink_to_fit () ; compact . shrink_to_fit () ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; super :: assert_properly_allocated (compact , control) ; } Retain (nth , codepoint) => { let nth = nth % 8 ; let new_predicate = | | { let mut index = 0 ; move | c : char | { if index == nth || c > codepoint { index = 0 ; false } else { index += 1 ; true } } } ; control . retain (new_predicate ()) ; compact . retain (new_predicate ()) ; assert_eq ! (control , compact) ; assert_eq ! (control . len () , compact . len ()) ; } CloneAndDrop => { let control_clone = control . clone () ; let og = std :: mem :: replace (control , control_clone) ; drop (og) ; let compact_clone = compact . clone () ; if compact . len () <= super :: MAX_INLINE_LENGTH { assert ! (! compact_clone . is_heap_allocated ()) } let og = std :: mem :: replace (compact , compact_clone) ; drop (og) ; } RoundTripIntoBytes => { let control_clone = control . clone () ; let compact_clone = compact . clone () ; let og_control = std :: mem :: take (control) ; let og_compact = std :: mem :: take (compact) ; let control_bytes = og_control . into_bytes () ; let comapct_bytes = og_compact . into_bytes () ; assert_eq ! (control_bytes . len () , comapct_bytes . len ()) ; assert_eq ! (& control_bytes [..] , & comapct_bytes [..]) ; let new_control = String :: from_utf8 (control_bytes) . expect ("valid UTF-8") ; let new_compact = CompactString :: from_utf8 (comapct_bytes) . expect ("valid UTF-8") ; assert_eq ! (new_control , control_clone) ; assert_eq ! (new_compact , compact_clone) ; * control = new_control ; * compact = new_compact ; } Repeat (times) => { let mut times = times % 3 ; if times * compact . len () > super :: TWENTY_FOUR_MIB_AS_BYTES { times = 0 ; } let new_compact = compact . repeat (times) ; let new_control = control . repeat (times) ; assert_eq ! (new_compact , new_control) ; super :: assert_properly_allocated (& new_compact , & new_control) ; * compact = new_compact ; * control = new_control ; } Zeroize => { use zeroize :: Zeroize ; control . zeroize () ; compact . zeroize () ; } } } }
};
}
