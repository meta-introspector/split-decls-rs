macro_rules! deps {
    () => {
        TraitDef!();
        StaticFields!();
        Path!();
        IsTuple!();
        FieldInfo!();
    };
}

macro_rules! impl_127 {
    () => {
        deps!();
        impl < 'a > TraitDef < 'a > { fn summarise_struct (& self , cx : & ExtCtxt < '_ > , struct_def : & VariantData) -> StaticFields { let mut named_idents = Vec :: new () ; let mut just_spans = Vec :: new () ; for field in struct_def . fields () { let sp = field . span . with_ctxt (self . span . ctxt ()) ; match field . ident { Some (ident) => named_idents . push ((ident , sp , field . default . clone ())) , _ => just_spans . push (sp) , } } let is_tuple = match struct_def { ast :: VariantData :: Tuple (..) => IsTuple :: Yes , _ => IsTuple :: No , } ; match (just_spans . is_empty () , named_idents . is_empty ()) { (false , false) => cx . dcx () . span_bug (self . span , "a struct with named and unnamed fields in generic `derive`") , (_ , false) => Named (named_idents) , (false , _) => Unnamed (just_spans , is_tuple) , _ => Named (Vec :: new ()) , } } fn create_struct_patterns (& self , cx : & ExtCtxt < '_ > , struct_path : ast :: Path , struct_def : & 'a VariantData , prefixes : & [String] , by_ref : ByRef ,) -> ThinVec < Box < ast :: Pat > > { prefixes . iter () . map (| prefix | { let pieces_iter = struct_def . fields () . iter () . enumerate () . map (| (i , struct_field) | { let sp = struct_field . span . with_ctxt (self . span . ctxt ()) ; let ident = self . mk_pattern_ident (prefix , i) ; let path = ident . with_span_pos (sp) ; (sp , struct_field . ident , cx . pat (path . span , PatKind :: Ident (BindingMode (by_ref , Mutability :: Not) , path , None) ,) ,) }) ; let struct_path = struct_path . clone () ; match * struct_def { VariantData :: Struct { .. } => { let field_pats = pieces_iter . map (| (sp , ident , pat) | { if ident . is_none () { cx . dcx () . span_bug (sp , "a braced struct with unnamed fields in `derive`" ,) ; } ast :: PatField { ident : ident . unwrap () , is_shorthand : false , attrs : ast :: AttrVec :: new () , id : ast :: DUMMY_NODE_ID , span : pat . span . with_ctxt (self . span . ctxt ()) , pat , is_placeholder : false , } }) . collect () ; cx . pat_struct (self . span , struct_path , field_pats) } VariantData :: Tuple (..) => { let subpats = pieces_iter . map (| (_ , _ , subpat) | subpat) . collect () ; cx . pat_tuple_struct (self . span , struct_path , subpats) } VariantData :: Unit (..) => cx . pat_path (self . span , struct_path) , } }) . collect () } fn create_fields < F > (& self , struct_def : & 'a VariantData , mk_exprs : F) -> Vec < FieldInfo > where F : Fn (usize , & ast :: FieldDef , Span) -> Vec < Box < ast :: Expr > > , { struct_def . fields () . iter () . enumerate () . map (| (i , struct_field) | { let sp = struct_field . span . with_ctxt (self . span . ctxt ()) ; let mut exprs : Vec < _ > = mk_exprs (i , struct_field , sp) ; let self_expr = exprs . remove (0) ; let other_selflike_exprs = exprs ; FieldInfo { span : sp . with_ctxt (self . span . ctxt ()) , name : struct_field . ident , self_expr , other_selflike_exprs , maybe_scalar : struct_field . ty . peel_refs () . kind . maybe_scalar () , } }) . collect () } fn mk_pattern_ident (& self , prefix : & str , i : usize) -> Ident { Ident :: from_str_and_span (& format ! ("{prefix}_{i}") , self . span) } fn create_struct_pattern_fields (& self , cx : & ExtCtxt < '_ > , struct_def : & 'a VariantData , prefixes : & [String] ,) -> Vec < FieldInfo > { self . create_fields (struct_def , | i , _struct_field , sp | { prefixes . iter () . map (| prefix | { let ident = self . mk_pattern_ident (prefix , i) ; cx . expr_path (cx . path_ident (sp , ident)) }) . collect () }) } fn create_struct_field_access_fields (& self , cx : & ExtCtxt < '_ > , selflike_args : & [Box < Expr >] , struct_def : & 'a VariantData , is_packed : bool ,) -> Vec < FieldInfo > { self . create_fields (struct_def , | i , struct_field , sp | { selflike_args . iter () . map (| selflike_arg | { let mut field_expr = cx . expr (sp , ast :: ExprKind :: Field (selflike_arg . clone () , struct_field . ident . unwrap_or_else (| | { Ident :: from_str_and_span (& i . to_string () , struct_field . span) }) ,) ,) ; if is_packed { field_expr = cx . expr_block (cx . block (struct_field . span , thin_vec ! [cx . stmt_expr (field_expr)]) ,) ; } cx . expr_addr_of (sp , field_expr) }) . collect () }) } }
    };
}

impl_127!();