// Generated macro for HybridKeyExchange (trait)
macro_rules! Depcrate_cryptoHybridKeyExchange {
() => {
// Module: crate::crypto
// Provides: {"HybridKeyExchange"}
// Dependencies: {}
# [doc = " An in-progress hybrid key exchange originating from a [`SupportedKxGroup`]."] # [doc = ""] # [doc = " \"Hybrid\" means a key exchange algorithm which is constructed from two"] # [doc = " (or more) independent component algorithms. Usually one is post-quantum-secure,"] # [doc = " and the other is \"classical\".  See"] # [doc = " <https://datatracker.ietf.org/doc/draft-ietf-tls-hybrid-design/11/>"] # [doc = ""] # [doc = " There is no requirement for a hybrid scheme (or any other!) to implement"] # [doc = " `HybridKeyExchange` if it is not desirable for it to be \"split\" like this."] # [doc = " It only enables an optimization; described below."] # [doc = ""] # [doc = " # Background"] # [doc = " Rustls always sends a presumptive key share in its `ClientHello`, using"] # [doc = " (absent any other information) the first item in [`CryptoProvider::kx_groups`]."] # [doc = " If the server accepts the client's selection, it can complete the handshake"] # [doc = " using that key share.  If not, the server sends a `HelloRetryRequest` instructing"] # [doc = " the client to send a different key share instead."] # [doc = ""] # [doc = " This request costs an extra round trip, and wastes the key exchange computation"] # [doc = " (in [`SupportedKxGroup::start()`]) the client already did.  We would"] # [doc = " like to avoid those wastes if possible."] # [doc = ""] # [doc = " It is early days for post-quantum-secure hybrid key exchange deployment."] # [doc = " This means (commonly) continuing to offer both the hybrid and classical"] # [doc = " key exchanges, so the handshake can be completed without a `HelloRetryRequest`"] # [doc = " for servers that support the offered hybrid or classical schemes."] # [doc = ""] # [doc = " Implementing `HybridKeyExchange` enables two optimizations:"] # [doc = ""] # [doc = " 1. Sending both the hybrid and classical key shares in the `ClientHello`."] # [doc = ""] # [doc = " 2. Performing the classical key exchange setup only once.  This is important"] # [doc = "    because the classical key exchange setup is relatively expensive."] # [doc = "    This optimization is permitted and described in"] # [doc = "    <https://www.ietf.org/archive/id/draft-ietf-tls-hybrid-design-11.html#section-3.2>"] # [doc = ""] # [doc = " Both of these only happen if the classical algorithm appears separately in"] # [doc = " the client's [`CryptoProvider::kx_groups`], and if the hybrid algorithm appears"] # [doc = " first in that list."] # [doc = ""] # [doc = " # How it works"] # [doc = " This function is only called by rustls for clients.  It is called when"] # [doc = " constructing the initial `ClientHello`.  rustls follows these steps:"] # [doc = ""] # [doc = " 1. If the return value is `None`, nothing further happens."] # [doc = " 2. If the given [`NamedGroup`] does not appear in"] # [doc = "    [`CryptoProvider::kx_groups`], nothing further happens."] # [doc = " 3. The given key share is added to the `ClientHello`, after the hybrid entry."] # [doc = ""] # [doc = " Then, one of three things may happen when the server replies to the `ClientHello`:"] # [doc = ""] # [doc = " 1. The server sends a `HelloRetryRequest`.  Everything is thrown away and"] # [doc = "    we start again."] # [doc = " 2. The server agrees to our hybrid key exchange: rustls calls"] # [doc = "    [`ActiveKeyExchange::complete()`] consuming `self`."] # [doc = " 3. The server agrees to our classical key exchange: rustls calls"] # [doc = "    [`HybridKeyExchange::complete_component()`] which"] # [doc = "    discards the hybrid key data, and completes just the classical key exchange."] pub trait HybridKeyExchange : ActiveKeyExchange { # [doc = " Returns the [`NamedGroup`] and public key \"share\" for the component."] fn component (& self) -> (NamedGroup , & [u8]) ; # [doc = " Completes the classical component of the key exchange, given the peer's public key."] # [doc = ""] # [doc = " This method must return an error if `peer_pub_key` is invalid: either"] # [doc = " misencoded, or an invalid public key (such as, but not limited to, being"] # [doc = " in a small order subgroup)."] # [doc = ""] # [doc = " The shared secret is returned as a [`SharedSecret`] which can be constructed"] # [doc = " from a `&[u8]`."] # [doc = ""] # [doc = " See the documentation on [`HybridKeyExchange`] for explanation."] fn complete_component (self : Box < Self > , peer_pub_key : & [u8]) -> Result < SharedSecret , Error > ; # [doc = " Obtain the value as a `dyn ActiveKeyExchange`"] fn as_key_exchange (& self) -> & (dyn ActiveKeyExchange + 'static) ; # [doc = " Remove the ability to do hybrid key exchange on this object."] fn into_key_exchange (self : Box < Self >) -> Box < dyn ActiveKeyExchange > ; }
};
}
