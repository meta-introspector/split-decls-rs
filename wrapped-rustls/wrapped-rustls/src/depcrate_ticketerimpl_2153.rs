// Generated macro for impl_2153 (impl)
macro_rules! Depcrate_ticketerimpl_2153 {
() => {
// Module: crate::ticketer
// Provides: {"impl_2153"}
// Dependencies: {}
# [cfg (feature = "std")] impl TicketRotator { # [doc = " Creates a new `TicketRotator`, which rotates through sub-ticketers"] # [doc = " based on the passage of time."] # [doc = ""] # [doc = " `lifetime` is in seconds, and is how long the current ticketer"] # [doc = " is used to generate new tickets.  Tickets are accepted for no"] # [doc = " longer than twice this duration.  This means a given ticket will"] # [doc = " be usable for at least one `lifetime`, and at most two `lifetime`s"] # [doc = " (depending on when its creation falls in the replacement cycle.)"] # [doc = ""] # [doc = " `generator` produces a new [`TicketProducer`] implementation."] pub fn new (lifetime : Duration , generator : fn () -> Result < Box < dyn TicketProducer > , Error > ,) -> Result < Self , Error > { Ok (Self { generator , lifetime , state : RwLock :: new (TicketRotatorState { current : generator () ? , previous : None , next_switch_time : UnixTime :: now () . as_secs () . saturating_add (lifetime . as_secs ()) , }) , }) } # [doc = " If it's time, demote the `current` ticketer to `previous` (so it"] # [doc = " does no new encryptions but can do decryption) and replace it"] # [doc = " with a new one."] # [doc = ""] # [doc = " Calling this regularly will ensure timely key erasure.  Otherwise,"] # [doc = " key erasure will be delayed until the next encrypt/decrypt call."] # [doc = ""] # [doc = " For efficiency, this is also responsible for locking the state rwlock"] # [doc = " and returning it for read."] pub (crate) fn maybe_roll (& self , now : UnixTime ,) -> Option < RwLockReadGuard < '_ , TicketRotatorState > > { let now = now . as_secs () ; { let read = self . state . read () . ok () ? ; if now <= read . next_switch_time { return Some (read) ; } } let next = (self . generator) () . ok () ? ; let mut write = self . state . write () . ok () ? ; if now <= write . next_switch_time { drop (write) ; return self . state . read () . ok () ; } write . previous = Some (mem :: replace (& mut write . current , next)) ; write . next_switch_time = now . saturating_add (self . lifetime . as_secs ()) ; drop (write) ; self . state . read () . ok () } # [cfg (any (feature = "aws-lc-rs" , feature = "ring"))] pub (crate) const SIX_HOURS : Duration = Duration :: from_secs (6 * 60 * 60) ; }
};
}
