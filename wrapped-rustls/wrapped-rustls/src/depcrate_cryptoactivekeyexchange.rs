// Generated macro for ActiveKeyExchange (trait)
macro_rules! Depcrate_cryptoActiveKeyExchange {
() => {
// Module: crate::crypto
// Provides: {"ActiveKeyExchange"}
// Dependencies: {}
# [doc = " An in-progress key exchange originating from a [`SupportedKxGroup`]."] pub trait ActiveKeyExchange : Send + Sync { # [doc = " Completes the key exchange, given the peer's public key."] # [doc = ""] # [doc = " This method must return an error if `peer_pub_key` is invalid: either"] # [doc = " misencoded, or an invalid public key (such as, but not limited to, being"] # [doc = " in a small order subgroup)."] # [doc = ""] # [doc = " If the key exchange algorithm is FFDHE, the result must be left-padded with zeros,"] # [doc = " as required by [RFC 8446](https://www.rfc-editor.org/rfc/rfc8446#section-7.4.1)"] # [doc = " (see [`complete_for_tls_version()`](Self::complete_for_tls_version) for more details)."] # [doc = ""] # [doc = " The shared secret is returned as a [`SharedSecret`] which can be constructed"] # [doc = " from a `&[u8]`."] # [doc = ""] # [doc = " This consumes and so terminates the [`ActiveKeyExchange`]."] fn complete (self : Box < Self > , peer_pub_key : & [u8]) -> Result < SharedSecret , Error > ; # [doc = " Completes the key exchange for the given TLS version, given the peer's public key."] # [doc = ""] # [doc = " Note that finite-field Diffie–Hellman key exchange has different requirements for the derived"] # [doc = " shared secret in TLS 1.2 and TLS 1.3 (ECDHE key exchange is the same in TLS 1.2 and TLS 1.3):"] # [doc = ""] # [doc = " In TLS 1.2, the calculated secret is required to be stripped of leading zeros"] # [doc = " [(RFC 5246)](https://www.rfc-editor.org/rfc/rfc5246#section-8.1.2)."] # [doc = ""] # [doc = " In TLS 1.3, the calculated secret is required to be padded with leading zeros to be the same"] # [doc = " byte-length as the group modulus [(RFC 8446)](https://www.rfc-editor.org/rfc/rfc8446#section-7.4.1)."] # [doc = ""] # [doc = " The default implementation of this method delegates to [`complete()`](Self::complete) assuming it is"] # [doc = " implemented for TLS 1.3 (i.e., for FFDHE KX, removes padding as needed). Implementers of this trait"] # [doc = " are encouraged to just implement [`complete()`](Self::complete) assuming TLS 1.3, and let the default"] # [doc = " implementation of this method handle TLS 1.2-specific requirements."] # [doc = ""] # [doc = " This method must return an error if `peer_pub_key` is invalid: either"] # [doc = " misencoded, or an invalid public key (such as, but not limited to, being"] # [doc = " in a small order subgroup)."] # [doc = ""] # [doc = " The shared secret is returned as a [`SharedSecret`] which can be constructed"] # [doc = " from a `&[u8]`."] # [doc = ""] # [doc = " This consumes and so terminates the [`ActiveKeyExchange`]."] fn complete_for_tls_version (self : Box < Self > , peer_pub_key : & [u8] , tls_version : ProtocolVersion ,) -> Result < SharedSecret , Error > { if tls_version == ProtocolVersion :: TLSv1_3 { return self . complete (peer_pub_key) ; } let group = self . group () ; let mut complete_res = self . complete (peer_pub_key) ? ; if group . key_exchange_algorithm () == KeyExchangeAlgorithm :: DHE { complete_res . strip_leading_zeros () ; } Ok (complete_res) } # [doc = " Return the public key being used."] # [doc = ""] # [doc = " For ECDHE, the encoding required is defined in"] # [doc = " [RFC8446 section 4.2.8.2](https://www.rfc-editor.org/rfc/rfc8446#section-4.2.8.2)."] # [doc = ""] # [doc = " For FFDHE, the encoding required is defined in"] # [doc = " [RFC8446 section 4.2.8.1](https://www.rfc-editor.org/rfc/rfc8446#section-4.2.8.1)."] fn pub_key (& self) -> & [u8] ; # [doc = " FFDHE group the `ActiveKeyExchange` is operating in."] # [doc = ""] # [doc = " The default implementation returns `None`, so non-FFDHE groups (the"] # [doc = " most common) do not need to do anything."] # [doc = ""] # [doc = " FFDHE groups must implement this. `rustls::ffdhe_groups` contains"] # [doc = " suitable values to return, for example"] # [doc = " [`rustls::ffdhe_groups::FFDHE2048`][crate::ffdhe_groups::FFDHE2048]."] fn ffdhe_group (& self) -> Option < FfdheGroup < 'static > > { None } # [doc = " Return the group being used."] fn group (& self) -> NamedGroup ; }
};
}
