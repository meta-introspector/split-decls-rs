// Generated macro for Tls12AeadAlgorithm (trait)
macro_rules! Depcrate_crypto_cipherTls12AeadAlgorithm {
() => {
// Module: crate::crypto::cipher
// Provides: {"Tls12AeadAlgorithm"}
// Dependencies: {}
# [doc = " Factory trait for building `MessageEncrypter` and `MessageDecrypter` for a TLS1.2 cipher suite."] pub trait Tls12AeadAlgorithm : Send + Sync + 'static { # [doc = " Build a `MessageEncrypter` for the given key/iv and extra key block (which can be used for"] # [doc = " improving explicit nonce size security, if needed)."] # [doc = ""] # [doc = " The length of `key` is set by [`KeyBlockShape::enc_key_len`]."] # [doc = ""] # [doc = " The length of `iv` is set by [`KeyBlockShape::fixed_iv_len`]."] # [doc = ""] # [doc = " The length of `extra` is set by [`KeyBlockShape::explicit_nonce_len`]."] fn encrypter (& self , key : AeadKey , iv : & [u8] , extra : & [u8]) -> Box < dyn MessageEncrypter > ; # [doc = " Build a `MessageDecrypter` for the given key/iv."] # [doc = ""] # [doc = " The length of `key` is set by [`KeyBlockShape::enc_key_len`]."] # [doc = ""] # [doc = " The length of `iv` is set by [`KeyBlockShape::fixed_iv_len`]."] fn decrypter (& self , key : AeadKey , iv : & [u8]) -> Box < dyn MessageDecrypter > ; # [doc = " Return a `KeyBlockShape` that defines how large the `key_block` is and how it"] # [doc = " is split up prior to calling `encrypter()`, `decrypter()` and/or `extract_keys()`."] fn key_block_shape (& self) -> KeyBlockShape ; # [doc = " Convert the key material from `key`/`iv`, into a `ConnectionTrafficSecrets` item."] # [doc = ""] # [doc = " The length of `key` is set by [`KeyBlockShape::enc_key_len`]."] # [doc = ""] # [doc = " The length of `iv` is set by [`KeyBlockShape::fixed_iv_len`]."] # [doc = ""] # [doc = " The length of `extra` is set by [`KeyBlockShape::explicit_nonce_len`]."] # [doc = ""] # [doc = " May return [`UnsupportedOperationError`] if the AEAD algorithm is not a supported"] # [doc = " variant of `ConnectionTrafficSecrets`."] fn extract_keys (& self , key : AeadKey , iv : & [u8] , explicit : & [u8] ,) -> Result < ConnectionTrafficSecrets , UnsupportedOperationError > ; # [doc = " Return `true` if this is backed by a FIPS-approved implementation."] fn fips (& self) -> bool { false } }
};
}
