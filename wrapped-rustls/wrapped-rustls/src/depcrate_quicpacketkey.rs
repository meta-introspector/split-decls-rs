// Generated macro for PacketKey (trait)
macro_rules! Depcrate_quicPacketKey {
() => {
// Module: crate::quic
// Provides: {"PacketKey"}
// Dependencies: {}
# [doc = " Keys to encrypt or decrypt the payload of a packet"] pub trait PacketKey : Send + Sync { # [doc = " Encrypt a QUIC packet"] # [doc = ""] # [doc = " Takes a `packet_number` and optional `path_id`, used to derive the nonce; the packet"] # [doc = " `header`, which is used as the additional authenticated data; and the `payload`. The"] # [doc = " authentication tag is returned if encryption succeeds."] # [doc = ""] # [doc = " Fails if and only if the payload is longer than allowed by the cipher suite's AEAD algorithm."] # [doc = ""] # [doc = " When provided, the `path_id` is used for multipath encryption as described in"] # [doc = " <https://www.ietf.org/archive/id/draft-ietf-quic-multipath-15.html#section-2.4>."] fn encrypt_in_place (& self , packet_number : u64 , header : & [u8] , payload : & mut [u8] , path_id : Option < u32 > ,) -> Result < Tag , Error > ; # [doc = " Decrypt a QUIC packet"] # [doc = ""] # [doc = " Takes a `packet_number` and optional `path_id`, used to derive the nonce; the packet"] # [doc = " `header`, which is used as the additional authenticated data, and the `payload`, which"] # [doc = " includes the authentication tag."] # [doc = ""] # [doc = " On success, returns the slice of `payload` containing the decrypted data."] # [doc = ""] # [doc = " When provided, the `path_id` is used for multipath encryption as described in"] # [doc = " <https://www.ietf.org/archive/id/draft-ietf-quic-multipath-15.html#section-2.4>."] fn decrypt_in_place < 'a > (& self , packet_number : u64 , header : & [u8] , payload : & 'a mut [u8] , path_id : Option < u32 > ,) -> Result < & 'a [u8] , Error > ; # [doc = " Tag length for the underlying AEAD algorithm"] fn tag_len (& self) -> usize ; # [doc = " Number of QUIC messages that can be safely encrypted with a single key of this type."] # [doc = ""] # [doc = " Once a `MessageEncrypter` produced for this suite has encrypted more than"] # [doc = " `confidentiality_limit` messages, an attacker gains an advantage in distinguishing it"] # [doc = " from an ideal pseudorandom permutation (PRP)."] # [doc = ""] # [doc = " This is to be set on the assumption that messages are maximally sized --"] # [doc = " 2 ** 16. For non-QUIC TCP connections see [`CipherSuiteCommon::confidentiality_limit`][csc-limit]."] # [doc = ""] # [doc = " [csc-limit]: crate::crypto::CipherSuiteCommon::confidentiality_limit"] fn confidentiality_limit (& self) -> u64 ; # [doc = " Number of QUIC messages that can be safely decrypted with a single key of this type"] # [doc = ""] # [doc = " Once a `MessageDecrypter` produced for this suite has failed to decrypt `integrity_limit`"] # [doc = " messages, an attacker gains an advantage in forging messages."] # [doc = ""] # [doc = " This is not relevant for TLS over TCP (which is also implemented in this crate)"] # [doc = " because a single failed decryption is fatal to the connection."] # [doc = " However, this quantity is used by QUIC."] fn integrity_limit (& self) -> u64 ; }
};
}
