// Generated macro for Hpke (trait)
macro_rules! Depcrate_crypto_hpkeHpke {
() => {
// Module: crate::crypto::hpke
// Provides: {"Hpke"}
// Dependencies: {}
# [doc = " An HPKE instance that can be used for base-mode single-shot encryption and decryption."] pub trait Hpke : Debug + Send + Sync { # [doc = " Seal the provided `plaintext` to the recipient public key `pub_key` with application supplied"] # [doc = " `info`, and additional data `aad`."] # [doc = ""] # [doc = " Returns ciphertext that can be used with [Self::open] by the recipient to recover plaintext"] # [doc = " using the same `info` and `aad` and the private key corresponding to `pub_key`. RFC 9180"] # [doc = " refers to `pub_key` as `pkR`."] fn seal (& self , info : & [u8] , aad : & [u8] , plaintext : & [u8] , pub_key : & HpkePublicKey ,) -> Result < (EncapsulatedSecret , Vec < u8 >) , Error > ; # [doc = " Set up a sealer context for the receiver public key `pub_key` with application supplied `info`."] # [doc = ""] # [doc = " Returns both an encapsulated ciphertext and a sealer context that can be used to seal"] # [doc = " messages to the recipient. RFC 9180 refers to `pub_key` as `pkR`."] fn setup_sealer (& self , info : & [u8] , pub_key : & HpkePublicKey ,) -> Result < (EncapsulatedSecret , Box < dyn HpkeSealer + 'static >) , Error > ; # [doc = " Open the provided `ciphertext` using the encapsulated secret `enc`, with application"] # [doc = " supplied `info`, and additional data `aad`."] # [doc = ""] # [doc = " Returns plaintext if  the `info` and `aad` match those used with [Self::seal], and"] # [doc = " decryption with `secret_key` succeeds. RFC 9180 refers to `secret_key` as `skR`."] fn open (& self , enc : & EncapsulatedSecret , info : & [u8] , aad : & [u8] , ciphertext : & [u8] , secret_key : & HpkePrivateKey ,) -> Result < Vec < u8 > , Error > ; # [doc = " Set up an opener context for the secret key `secret_key` with application supplied `info`."] # [doc = ""] # [doc = " Returns an opener context that can be used to open sealed messages encrypted to the"] # [doc = " public key corresponding to `secret_key`. RFC 9180 refers to `secret_key` as `skR`."] fn setup_opener (& self , enc : & EncapsulatedSecret , info : & [u8] , secret_key : & HpkePrivateKey ,) -> Result < Box < dyn HpkeOpener + 'static > , Error > ; # [doc = " Generate a new public key and private key pair compatible with this HPKE instance."] # [doc = ""] # [doc = " Key pairs should be encoded as raw big endian fixed length integers sized based"] # [doc = " on the suite's DH KEM algorithm."] fn generate_key_pair (& self) -> Result < (HpkePublicKey , HpkePrivateKey) , Error > ; # [doc = " Return whether the HPKE instance is FIPS compatible."] fn fips (& self) -> bool { false } # [doc = " Return the [HpkeSuite] that this HPKE instance supports."] fn suite (& self) -> HpkeSuite ; }
};
}
