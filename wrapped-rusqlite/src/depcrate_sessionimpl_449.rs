// Generated macro for impl_449 (impl)
macro_rules! Depcrate_sessionimpl_449 {
() => {
// Module: crate::session
// Provides: {"impl_449"}
// Dependencies: {}
impl ChangesetItem { # [doc = " Obtain conflicting row values"] # [doc = ""] # [doc = " May only be called with an `SQLITE_CHANGESET_DATA` or"] # [doc = " `SQLITE_CHANGESET_CONFLICT` conflict handler callback."] # [inline] pub fn conflict (& self , col : usize) -> Result < ValueRef < '_ > > { unsafe { let mut p_value : * mut ffi :: sqlite3_value = ptr :: null_mut () ; check (ffi :: sqlite3changeset_conflict (self . it , col as i32 , & mut p_value ,)) ? ; if p_value . is_null () { Err (Error :: InvalidColumnIndex (col)) } else { Ok (ValueRef :: from_value (p_value)) } } } # [doc = " Determine the number of foreign key constraint violations"] # [doc = ""] # [doc = " May only be called with an `SQLITE_CHANGESET_FOREIGN_KEY` conflict"] # [doc = " handler callback."] # [inline] pub fn fk_conflicts (& self) -> Result < i32 > { unsafe { let mut p_out = 0 ; check (ffi :: sqlite3changeset_fk_conflicts (self . it , & mut p_out)) ? ; Ok (p_out) } } # [doc = " Obtain new.* Values"] # [doc = ""] # [doc = " May only be called if the type of change is either `SQLITE_UPDATE` or"] # [doc = " `SQLITE_INSERT`."] # [inline] pub fn new_value (& self , col : usize) -> Result < ValueRef < '_ > > { unsafe { let mut p_value : * mut ffi :: sqlite3_value = ptr :: null_mut () ; check (ffi :: sqlite3changeset_new (self . it , col as i32 , & mut p_value)) ? ; if p_value . is_null () { Err (Error :: InvalidColumnIndex (col)) } else { Ok (ValueRef :: from_value (p_value)) } } } # [doc = " Obtain old.* Values"] # [doc = ""] # [doc = " May only be called if the type of change is either `SQLITE_DELETE` or"] # [doc = " `SQLITE_UPDATE`."] # [inline] pub fn old_value (& self , col : usize) -> Result < ValueRef < '_ > > { unsafe { let mut p_value : * mut ffi :: sqlite3_value = ptr :: null_mut () ; check (ffi :: sqlite3changeset_old (self . it , col as i32 , & mut p_value)) ? ; if p_value . is_null () { Err (Error :: InvalidColumnIndex (col)) } else { Ok (ValueRef :: from_value (p_value)) } } } # [doc = " Obtain the current operation"] # [inline] pub fn op (& self) -> Result < Operation < '_ > > { let mut number_of_columns = 0 ; let mut code = 0 ; let mut indirect = 0 ; let tab = unsafe { let mut pz_tab : * const c_char = ptr :: null () ; check (ffi :: sqlite3changeset_op (self . it , & mut pz_tab , & mut number_of_columns , & mut code , & mut indirect ,)) ? ; CStr :: from_ptr (pz_tab) } ; let table_name = tab . to_str () ? ; Ok (Operation { table_name , number_of_columns , code : Action :: from (code) , indirect : indirect != 0 , }) } # [doc = " Obtain the primary key definition of a table"] # [inline] pub fn pk (& self) -> Result < & [u8] > { let mut number_of_columns = 0 ; unsafe { let mut pks : * mut c_uchar = ptr :: null_mut () ; check (ffi :: sqlite3changeset_pk (self . it , & mut pks , & mut number_of_columns ,)) ? ; Ok (from_raw_parts (pks , number_of_columns as usize)) } } }
};
}
