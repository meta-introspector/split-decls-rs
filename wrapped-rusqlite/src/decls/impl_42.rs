macro_rules! deps {
    () => {
        Blob!();
        Error!();
        Result!();
    };
}

macro_rules! impl_42 {
    () => {
        deps!();
        impl Blob < '_ > { # [doc = " Write `buf` to `self` starting at `write_start`, returning an error if"] # [doc = " `write_start + buf.len()` is past the end of the blob."] # [doc = ""] # [doc = " If an error is returned, no data is written."] # [doc = ""] # [doc = " Note: the blob cannot be resized using this function -- that must be"] # [doc = " done using SQL (for example, an `UPDATE` statement)."] # [doc = ""] # [doc = " Note: This is part of the positional I/O API, and thus takes an absolute"] # [doc = " position write to, instead of using the internal position that can be"] # [doc = " manipulated by the `std::io` traits."] # [doc = ""] # [doc = " Unlike the similarly named [`FileExt::write_at`][fext_write_at] function"] # [doc = " (from `std::os::unix`), it's always an error to perform a \"short write\"."] # [doc = ""] # [doc = " [fext_write_at]: https://doc.rust-lang.org/std/os/unix/fs/trait.FileExt.html#tymethod.write_at"] # [inline] pub fn write_at (& mut self , buf : & [u8] , write_start : usize) -> Result < () > { let len = self . len () ; if buf . len () . saturating_add (write_start) > len { return Err (Error :: BlobSizeError) ; } debug_assert ! (i32 :: try_from (write_start) . is_ok () && i32 :: try_from (buf . len ()) . is_ok ()) ; self . conn . decode_result (unsafe { ffi :: sqlite3_blob_write (self . blob , buf . as_ptr () . cast () , buf . len () as i32 , write_start as i32 ,) }) } # [doc = " An alias for `write_at` provided for compatibility with the conceptually"] # [doc = " equivalent [`std::os::unix::FileExt::write_all_at`][write_all_at]"] # [doc = " function from libstd:"] # [doc = ""] # [doc = " [write_all_at]: https://doc.rust-lang.org/std/os/unix/fs/trait.FileExt.html#method.write_all_at"] # [inline] pub fn write_all_at (& mut self , buf : & [u8] , write_start : usize) -> Result < () > { self . write_at (buf , write_start) } # [doc = " Read as much as possible from `offset` to `offset + buf.len()` out of"] # [doc = " `self`, writing into `buf`. On success, returns the number of bytes"] # [doc = " written."] # [doc = ""] # [doc = " If there's insufficient data in `self`, then the returned value will be"] # [doc = " less than `buf.len()`."] # [doc = ""] # [doc = " See also [`Blob::raw_read_at`], which can take an uninitialized buffer,"] # [doc = " or [`Blob::read_at_exact`] which returns an error if the entire `buf` is"] # [doc = " not read."] # [doc = ""] # [doc = " Note: This is part of the positional I/O API, and thus takes an absolute"] # [doc = " position to read from, instead of using the internal position that can"] # [doc = " be manipulated by the `std::io` traits. Consequently, it does not change"] # [doc = " that value either."] # [inline] pub fn read_at (& self , buf : & mut [u8] , read_start : usize) -> Result < usize > { let as_uninit : & mut [MaybeUninit < u8 >] = unsafe { from_raw_parts_mut (buf . as_mut_ptr () . cast () , buf . len ()) } ; self . raw_read_at (as_uninit , read_start) . map (| s | s . len ()) } # [doc = " Read as much as possible from `offset` to `offset + buf.len()` out of"] # [doc = " `self`, writing into `buf`. On success, returns the portion of `buf`"] # [doc = " which was initialized by this call."] # [doc = ""] # [doc = " If there's insufficient data in `self`, then the returned value will be"] # [doc = " shorter than `buf`."] # [doc = ""] # [doc = " See also [`Blob::read_at`], which takes a `&mut [u8]` buffer instead of"] # [doc = " a slice of `MaybeUninit<u8>`."] # [doc = ""] # [doc = " Note: This is part of the positional I/O API, and thus takes an absolute"] # [doc = " position to read from, instead of using the internal position that can"] # [doc = " be manipulated by the `std::io` traits. Consequently, it does not change"] # [doc = " that value either."] # [inline] pub fn raw_read_at < 'a > (& self , buf : & 'a mut [MaybeUninit < u8 >] , read_start : usize ,) -> Result < & 'a mut [u8] > { let len = self . len () ; let read_len = match len . checked_sub (read_start) { None | Some (0) => 0 , Some (v) => v . min (buf . len ()) , } ; if read_len == 0 { let empty = unsafe { from_raw_parts_mut (buf . as_mut_ptr () . cast :: < u8 > () , 0) } ; return Ok (empty) ; } debug_assert ! (i32 :: try_from (read_start) . is_ok ()) ; debug_assert ! (read_start + read_len <= len) ; debug_assert ! (buf . len () >= read_len) ; debug_assert ! (i32 :: try_from (buf . len ()) . is_ok ()) ; debug_assert ! (i32 :: try_from (read_len) . is_ok ()) ; unsafe { self . conn . decode_result (ffi :: sqlite3_blob_read (self . blob , buf . as_mut_ptr () . cast () , read_len as i32 , read_start as i32 ,)) ? ; Ok (from_raw_parts_mut (buf . as_mut_ptr () . cast :: < u8 > () , read_len)) } } # [doc = " Equivalent to [`Blob::read_at`], but returns a `BlobSizeError` if `buf`"] # [doc = " is not fully initialized."] # [inline] pub fn read_at_exact (& self , buf : & mut [u8] , read_start : usize) -> Result < () > { let n = self . read_at (buf , read_start) ? ; if n != buf . len () { Err (Error :: BlobSizeError) } else { Ok (()) } } # [doc = " Equivalent to [`Blob::raw_read_at`], but returns a `BlobSizeError` if"] # [doc = " `buf` is not fully initialized."] # [inline] pub fn raw_read_at_exact < 'a > (& self , buf : & 'a mut [MaybeUninit < u8 >] , read_start : usize ,) -> Result < & 'a mut [u8] > { let buflen = buf . len () ; let initted = self . raw_read_at (buf , read_start) ? ; if initted . len () != buflen { Err (Error :: BlobSizeError) } else { Ok (initted) } } }
    };
}

impl_42!()