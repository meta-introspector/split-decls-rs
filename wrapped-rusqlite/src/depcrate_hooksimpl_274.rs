// Generated macro for impl_274 (impl)
macro_rules! Depcrate_hooksimpl_274 {
() => {
// Module: crate::hooks
// Provides: {"impl_274"}
// Dependencies: {}
impl Connection { # [doc = " Register a callback function to be invoked whenever"] # [doc = " a transaction is committed."] # [doc = ""] # [doc = " The callback returns `true` to rollback."] # [inline] pub fn commit_hook < F > (& self , hook : Option < F >) where F : FnMut () -> bool + Send + 'static , { self . db . borrow_mut () . commit_hook (hook) ; } # [doc = " Register a callback function to be invoked whenever"] # [doc = " a transaction is committed."] # [inline] pub fn rollback_hook < F > (& self , hook : Option < F >) where F : FnMut () + Send + 'static , { self . db . borrow_mut () . rollback_hook (hook) ; } # [doc = " Register a callback function to be invoked whenever"] # [doc = " a row is updated, inserted or deleted in a rowid table."] # [doc = ""] # [doc = " The callback parameters are:"] # [doc = ""] # [doc = " - the type of database update (`SQLITE_INSERT`, `SQLITE_UPDATE` or"] # [doc = "   `SQLITE_DELETE`),"] # [doc = " - the name of the database (\"main\", \"temp\", ...),"] # [doc = " - the name of the table that is updated,"] # [doc = " - the ROWID of the row that is updated."] # [inline] pub fn update_hook < F > (& self , hook : Option < F >) where F : FnMut (Action , & str , & str , i64) + Send + 'static , { self . db . borrow_mut () . update_hook (hook) ; } # [doc = " Register a callback that is invoked each time data is committed to a database in wal mode."] # [doc = ""] # [doc = " A single database handle may have at most a single write-ahead log callback registered at one time."] # [doc = " Calling `wal_hook` replaces any previously registered write-ahead log callback."] # [doc = " Note that the `sqlite3_wal_autocheckpoint()` interface and the `wal_autocheckpoint` pragma"] # [doc = " both invoke `sqlite3_wal_hook()` and will overwrite any prior `sqlite3_wal_hook()` settings."] pub fn wal_hook (& self , hook : Option < fn (& Wal , c_int) -> Result < () > >) { unsafe extern "C" fn wal_hook_callback (client_data : * mut c_void , db : * mut ffi :: sqlite3 , db_name : * const c_char , pages : c_int ,) -> c_int { let hook_fn : fn (& Wal , c_int) -> Result < () > = std :: mem :: transmute (client_data) ; let wal = Wal { db , db_name } ; catch_unwind (| | match hook_fn (& wal , pages) { Ok (_) => ffi :: SQLITE_OK , Err (e) => e . sqlite_error () . map_or (ffi :: SQLITE_ERROR , | x | x . extended_code) , }) . unwrap_or_default () } let c = self . db . borrow_mut () ; match hook { Some (f) => unsafe { ffi :: sqlite3_wal_hook (c . db () , Some (wal_hook_callback) , f as * mut c_void) } , None => unsafe { ffi :: sqlite3_wal_hook (c . db () , None , ptr :: null_mut ()) } , } ; } # [doc = " Register a query progress callback."] # [doc = ""] # [doc = " The parameter `num_ops` is the approximate number of virtual machine"] # [doc = " instructions that are evaluated between successive invocations of the"] # [doc = " `handler`. If `num_ops` is less than one then the progress handler"] # [doc = " is disabled."] # [doc = ""] # [doc = " If the progress callback returns `true`, the operation is interrupted."] pub fn progress_handler < F > (& self , num_ops : c_int , handler : Option < F >) where F : FnMut () -> bool + Send + 'static , { self . db . borrow_mut () . progress_handler (num_ops , handler) ; } # [doc = " Register an authorizer callback that's invoked"] # [doc = " as a statement is being prepared."] # [inline] pub fn authorizer < 'c , F > (& self , hook : Option < F >) where F : for < 'r > FnMut (AuthContext < 'r >) -> Authorization + Send + 'static , { self . db . borrow_mut () . authorizer (hook) ; } }
};
}
