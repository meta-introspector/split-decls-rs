// Generated macro for impl_979 (impl)
macro_rules! Depcrate_util_sqlite_stringimpl_979 {
() => {
// Module: crate::util::sqlite_string
// Provides: {"impl_979"}
// Dependencies: {}
impl SqliteMallocString { # [doc = " SAFETY: Caller must be certain that `m` a nul-terminated c string"] # [doc = " allocated by `sqlite3_malloc64`, and that SQLite expects us to free it!"] # [inline] pub (crate) unsafe fn from_raw_nonnull (ptr : NonNull < c_char >) -> Self { Self { ptr , _boo : PhantomData , } } # [doc = " SAFETY: Caller must be certain that `m` a nul-terminated c string"] # [doc = " allocated by `sqlite3_malloc64`, and that SQLite expects us to free it!"] # [inline] pub (crate) unsafe fn from_raw (ptr : * mut c_char) -> Option < Self > { NonNull :: new (ptr) . map (| p | Self :: from_raw_nonnull (p)) } # [doc = " Get the pointer behind `self`. After this is called, we no longer manage"] # [doc = " it."] # [inline] pub (crate) fn into_inner (self) -> NonNull < c_char > { let p = self . ptr ; std :: mem :: forget (self) ; p } # [doc = " Get the pointer behind `self`. After this is called, we no longer manage"] # [doc = " it."] # [inline] pub (crate) fn into_raw (self) -> * mut c_char { self . into_inner () . as_ptr () } # [doc = " Borrow the pointer behind `self`. We still manage it when this function"] # [doc = " returns. If you want to relinquish ownership, use `into_raw`."] # [inline] pub (crate) fn as_ptr (& self) -> * const c_char { self . ptr . as_ptr () } # [inline] pub (crate) fn as_cstr (& self) -> & CStr { unsafe { CStr :: from_ptr (self . as_ptr ()) } } # [inline] pub (crate) fn to_string_lossy (& self) -> std :: borrow :: Cow < '_ , str > { self . as_cstr () . to_string_lossy () } # [doc = " Convert `s` into a SQLite string."] # [doc = ""] # [doc = " This should almost never be done except for cases like error messages or"] # [doc = " other strings that SQLite frees."] # [doc = ""] # [doc = " If `s` contains internal NULs, we'll replace them with"] # [doc = " `NUL_REPLACE_CHAR`."] # [doc = ""] # [doc = " Except for `debug_assert`s which may trigger during testing, this"] # [doc = " function never panics. If we hit integer overflow or the allocation"] # [doc = " fails, we call `handle_alloc_error` which aborts the program after"] # [doc = " calling a global hook."] # [doc = ""] # [doc = " This means it's safe to use in extern \"C\" functions even outside"] # [doc = " `catch_unwind`."] pub (crate) fn from_str (s : & str) -> Self { let s = if s . as_bytes () . contains (& 0) { std :: borrow :: Cow :: Owned (make_nonnull (s)) } else { std :: borrow :: Cow :: Borrowed (s) } ; debug_assert ! (! s . as_bytes () . contains (& 0)) ; let bytes : & [u8] = s . as_ref () . as_bytes () ; let src_ptr : * const c_char = bytes . as_ptr () . cast () ; let src_len = bytes . len () ; let maybe_len_plus_1 = s . len () . checked_add (1) . and_then (| v | v . try_into () . ok ()) ; unsafe { let res_ptr = maybe_len_plus_1 . and_then (| len_to_alloc | { debug_assert ! (len_to_alloc > 0) ; debug_assert_eq ! ((len_to_alloc - 1) as usize , src_len) ; NonNull :: new (ffi :: sqlite3_malloc64 (len_to_alloc) . cast :: < c_char > ()) }) . unwrap_or_else (| | { use std :: alloc :: { handle_alloc_error , Layout } ; let len = s . len () . saturating_add (1) . min (isize :: MAX as usize) ; let layout = Layout :: from_size_align_unchecked (len , 1) ; handle_alloc_error (layout) ; }) ; let buf : * mut c_char = res_ptr . as_ptr () . cast :: < c_char > () ; src_ptr . copy_to_nonoverlapping (buf , src_len) ; buf . add (src_len) . write (0) ; debug_assert_eq ! (std :: ffi :: CStr :: from_ptr (res_ptr . as_ptr ()) . to_bytes () , bytes) ; Self :: from_raw_nonnull (res_ptr) } } }
};
}
