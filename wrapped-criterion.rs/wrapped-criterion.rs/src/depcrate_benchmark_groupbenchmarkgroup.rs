// Generated macro for BenchmarkGroup (struct)
macro_rules! Depcrate_benchmark_groupBenchmarkGroup {
() => {
// Module: crate::benchmark_group
// Provides: {"BenchmarkGroup"}
// Dependencies: {}
# [doc = " Structure used to group together a set of related benchmarks, along with custom configuration"] # [doc = " settings for groups of benchmarks. All benchmarks performed using a benchmark group will be"] # [doc = " grouped together in the final report."] # [doc = ""] # [doc = " # Examples:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use criterion::{criterion_group, criterion_main, BenchmarkId, Criterion, Throughput};"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " fn bench_simple(c: &mut Criterion) {"] # [doc = "     let mut group = c.benchmark_group(\"My Group\");"] # [doc = ""] # [doc = "     // Now we can perform benchmarks with this group"] # [doc = "     group.bench_function(\"Bench 1\", |b| b.iter(|| 1 ));"] # [doc = "     group.bench_function(\"Bench 2\", |b| b.iter(|| 2 ));"] # [doc = "    "] # [doc = "     // It's recommended to call group.finish() explicitly at the end, but if you don't it will"] # [doc = "     // be called automatically when the group is dropped."] # [doc = "     group.finish();"] # [doc = " }"] # [doc = ""] # [doc = " fn bench_nested(c: &mut Criterion) {"] # [doc = "     let mut group = c.benchmark_group(\"My Second Group\");"] # [doc = "     // We can override the configuration on a per-group level"] # [doc = "     group.measurement_time(Duration::from_secs(1));"] # [doc = ""] # [doc = "     // We can also use loops to define multiple benchmarks, even over multiple dimensions."] # [doc = "     for x in 0..3 {"] # [doc = "         for y in 0..3 {"] # [doc = "             let point = (x, y);"] # [doc = "             let parameter_string = format!(\"{} * {}\", x, y);"] # [doc = "             group.bench_with_input(BenchmarkId::new(\"Multiply\", parameter_string), &point,"] # [doc = "                 |b, (p_x, p_y)| b.iter(|| p_x * p_y));"] # [doc = "         }"] # [doc = "     }"] # [doc = "    "] # [doc = "     group.finish();"] # [doc = " }"] # [doc = ""] # [doc = " fn bench_throughput(c: &mut Criterion) {"] # [doc = "     let mut group = c.benchmark_group(\"Summation\");"] # [doc = "     "] # [doc = "     for size in [1024, 2048, 4096].iter() {"] # [doc = "         // Generate input of an appropriate size..."] # [doc = "         let input = vec![1u64, *size];"] # [doc = ""] # [doc = "         // We can use the throughput function to tell Criterion.rs how large the input is"] # [doc = "         // so it can calculate the overall throughput of the function. If we wanted, we could"] # [doc = "         // even change the benchmark configuration for different inputs (eg. to reduce the"] # [doc = "         // number of samples for extremely large and slow inputs) or even different functions."] # [doc = "         group.throughput(Throughput::Elements(*size as u64));"] # [doc = ""] # [doc = "         group.bench_with_input(BenchmarkId::new(\"sum\", *size), &input,"] # [doc = "             |b, i| b.iter(|| i.iter().sum::<u64>()));"] # [doc = "         group.bench_with_input(BenchmarkId::new(\"fold\", *size), &input,"] # [doc = "             |b, i| b.iter(|| i.iter().fold(0u64, |a, b| a + b)));"] # [doc = "     }"] # [doc = ""] # [doc = "     group.finish();"] # [doc = " }"] # [doc = ""] # [doc = " criterion_group!(benches, bench_simple, bench_nested, bench_throughput);"] # [doc = " criterion_main!(benches);"] # [doc = " ```"] pub struct BenchmarkGroup < 'a , M : Measurement > { criterion : & 'a mut Criterion < M > , group_name : String , all_ids : Vec < InternalBenchmarkId > , any_matched : bool , partial_config : PartialBenchmarkConfig , throughput : Option < Throughput > , }
};
}
