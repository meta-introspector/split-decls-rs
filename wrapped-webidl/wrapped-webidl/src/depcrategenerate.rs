// Generated macro for generate (function)
macro_rules! Depcrategenerate {
() => {
// Module: crate
// Provides: {"generate"}
// Dependencies: {}
# [doc = " Generates Rust source code with #[wasm_bindgen] annotations."] # [doc = ""] # [doc = " * Reads WebIDL files in `from`"] # [doc = " * Generates Rust source code in the directory `to`"] # [doc = " * `options.features` indicates whether everything is gated by features or"] # [doc = "   not"] # [doc = ""] # [doc = " If features are enabled, returns a string that should be appended to"] # [doc = " `Cargo.toml` which lists all the known features."] pub fn generate (from : & Path , to : & Path , options : Options) -> Result < String > { let generate_features = options . features ; let source = read_source_from_path (& from . join ("enabled")) ? ; let unstable_source = read_source_from_path (& from . join ("unstable")) ? ; let features = parse_webidl (generate_features , source , unstable_source) ? ; if to . exists () { fs :: remove_dir_all (to) . context ("Removing features directory") ? ; } fs :: create_dir_all (to) . context ("Creating features directory") ? ; for (name , feature) in features . iter () { let out_file_path = to . join (format ! ("gen_{name}.rs")) ; fs :: write (& out_file_path , & feature . code) ? ; } let binding_file = features . keys () . map (| name | { if generate_features { format ! ("#[cfg(feature = \"{name}\")] #[allow(non_snake_case)] mod gen_{name};\n#[cfg(feature = \"{name}\")] #[allow(unused_imports)] pub use gen_{name}::*;") } else { format ! ("#[allow(non_snake_case)] mod gen_{name};\n#[allow(unused_imports)] pub use gen_{name}::*;") } }) . collect :: < Vec < _ > > () . join ("\n\n") ; fs :: write (to . join ("mod.rs") , binding_file) ? ; let to_format = features . keys () . map (| name | to . join (format ! ("gen_{name}.rs"))) . chain ([to . join ("mod.rs")]) ; rustfmt (to_format) ? ; return if generate_features { let features = features . iter () . map (| (name , feature) | { let features = feature . required_features . iter () . map (| x | format ! ("\"{x}\"")) . collect :: < Vec < _ > > () . join (", ") ; format ! ("{name} = [{features}]") }) . collect :: < Vec < _ > > () . join ("\n") ; Ok (features) } else { Ok (String :: new ()) } ; # [doc = " Read all WebIDL files in a directory into a single `SourceFile`"] fn read_source_from_path (dir : & Path) -> Result < SourceFile > { let entries = read_dir (dir) . context ("reading webidls directory") ? ; let mut source = SourceFile :: default () ; for path in entries { if path . extension () != Some (OsStr :: new ("webidl")) { continue ; } source . add_file (& path) . with_context (| | format ! ("reading contents of file \"{}\"" , path . display ())) ? ; } Ok (source) } fn rustfmt (paths : impl IntoIterator < Item = PathBuf >) -> Result < () > { let paths : Vec < _ > = paths . into_iter () . collect () ; for chunk in paths . chunks (400) { let result = Command :: new ("rustfmt") . arg ("--edition") . arg ("2021") . args (chunk) . status () . context ("rustfmt failed") ? ; assert ! (result . success () , "rustfmt failed") ; } Ok (()) } fn parse_webidl (generate_features : bool , enabled : SourceFile , unstable : SourceFile ,) -> Result < BTreeMap < String , Feature > > { let options = Options { features : generate_features , } ; match compile (& enabled . contents , & unstable . contents , options) { Ok (features) => Ok (features) , Err (e) => { if let Some (err) = e . downcast_ref :: < WebIDLParseError > () { if let Some (pos) = enabled . resolve_offset (err . 0) { let ctx = format ! ("compiling WebIDL into wasm-bindgen bindings in file \
                             \"{}\", line {} column {}" , pos . filename , pos . line + 1 , pos . col + 1) ; return Err (e . context (ctx)) ; } else { return Err (e . context ("compiling WebIDL into wasm-bindgen bindings")) ; } } Err (e . context ("compiling WebIDL into wasm-bindgen bindings")) } } } }
};
}
