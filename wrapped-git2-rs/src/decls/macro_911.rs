macro_rules! macro_911 {
    () => {
        bitflags ! { # [doc = " Options for [`Reference::normalize_name`]."] # [derive (Clone , Copy , Debug , Eq , PartialEq , PartialOrd , Ord , Hash)] pub struct ReferenceFormat : u32 { # [doc = " No particular normalization."] const NORMAL = raw :: GIT_REFERENCE_FORMAT_NORMAL as u32 ; # [doc = " Control whether one-level refname are accepted (i.e., refnames that"] # [doc = " do not contain multiple `/`-separated components). Those are"] # [doc = " expected to be written only using uppercase letters and underscore"] # [doc = " (e.g. `HEAD`, `FETCH_HEAD`)."] const ALLOW_ONELEVEL = raw :: GIT_REFERENCE_FORMAT_ALLOW_ONELEVEL as u32 ; # [doc = " Interpret the provided name as a reference pattern for a refspec (as"] # [doc = " used with remote repositories). If this option is enabled, the name"] # [doc = " is allowed to contain a single `*` in place of a full pathname"] # [doc = " components (e.g., `foo/*/bar` but not `foo/bar*`)."] const REFSPEC_PATTERN = raw :: GIT_REFERENCE_FORMAT_REFSPEC_PATTERN as u32 ; # [doc = " Interpret the name as part of a refspec in shorthand form so the"] # [doc = " `ALLOW_ONELEVEL` naming rules aren't enforced and `main` becomes a"] # [doc = " valid name."] const REFSPEC_SHORTHAND = raw :: GIT_REFERENCE_FORMAT_REFSPEC_SHORTHAND as u32 ; } }
    };
}

macro_911!()