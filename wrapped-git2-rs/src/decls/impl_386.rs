macro_rules! deps {
    () => {
        IndexMatchedPath!();
        Tree!();
        Oid!();
        IndexConflict!();
        IndexEntry!();
        IndexEntries!();
        IntoCString!();
        Repository!();
        Binding!();
        Error!();
        Index!();
        IndexConflicts!();
    };
}

macro_rules! impl_386 {
    () => {
        deps!();
        impl Index { # [doc = " Creates a new in-memory index."] # [doc = ""] # [doc = " This index object cannot be read/written to the filesystem, but may be"] # [doc = " used to perform in-memory index operations."] pub fn new () -> Result < Index , Error > { crate :: init () ; let mut raw = ptr :: null_mut () ; unsafe { try_call ! (raw :: git_index_new (& mut raw)) ; Ok (Binding :: from_raw (raw)) } } # [doc = " Create a new bare Git index object as a memory representation of the Git"] # [doc = " index file in 'index_path', without a repository to back it."] # [doc = ""] # [doc = " Since there is no ODB or working directory behind this index, any Index"] # [doc = " methods which rely on these (e.g. add_path) will fail."] # [doc = ""] # [doc = " If you need an index attached to a repository, use the `index()` method"] # [doc = " on `Repository`."] pub fn open (index_path : & Path) -> Result < Index , Error > { crate :: init () ; let mut raw = ptr :: null_mut () ; let index_path = index_path . into_c_string () ? ; unsafe { try_call ! (raw :: git_index_open (& mut raw , index_path)) ; Ok (Binding :: from_raw (raw)) } } # [doc = " Get index on-disk version."] # [doc = ""] # [doc = " Valid return values are 2, 3, or 4.  If 3 is returned, an index"] # [doc = " with version 2 may be written instead, if the extension data in"] # [doc = " version 3 is not necessary."] pub fn version (& self) -> u32 { unsafe { raw :: git_index_version (self . raw) } } # [doc = " Set index on-disk version."] # [doc = ""] # [doc = " Valid values are 2, 3, or 4.  If 2 is given, git_index_write may"] # [doc = " write an index with version 3 instead, if necessary to accurately"] # [doc = " represent the index."] pub fn set_version (& mut self , version : u32) -> Result < () , Error > { unsafe { try_call ! (raw :: git_index_set_version (self . raw , version)) ; } Ok (()) } # [doc = " Add or update an index entry from an in-memory struct"] # [doc = ""] # [doc = " If a previous index entry exists that has the same path and stage as the"] # [doc = " given 'source_entry', it will be replaced. Otherwise, the 'source_entry'"] # [doc = " will be added."] pub fn add (& mut self , entry : & IndexEntry) -> Result < () , Error > { let path = CString :: new (& entry . path [..]) ? ; let mut flags = entry . flags & ! raw :: GIT_INDEX_ENTRY_NAMEMASK ; if entry . path . len () < raw :: GIT_INDEX_ENTRY_NAMEMASK as usize { flags |= entry . path . len () as u16 ; } else { flags |= raw :: GIT_INDEX_ENTRY_NAMEMASK ; } unsafe { let raw = raw :: git_index_entry { dev : entry . dev , ino : entry . ino , mode : entry . mode , uid : entry . uid , gid : entry . gid , file_size : entry . file_size , id : * entry . id . raw () , flags , flags_extended : entry . flags_extended , path : path . as_ptr () , mtime : raw :: git_index_time { seconds : entry . mtime . seconds () , nanoseconds : entry . mtime . nanoseconds () , } , ctime : raw :: git_index_time { seconds : entry . ctime . seconds () , nanoseconds : entry . ctime . nanoseconds () , } , } ; try_call ! (raw :: git_index_add (self . raw , & raw)) ; Ok (()) } } # [doc = " Add or update an index entry from a buffer in memory"] # [doc = ""] # [doc = " This method will create a blob in the repository that owns the index and"] # [doc = " then add the index entry to the index. The path of the entry represents"] # [doc = " the position of the blob relative to the repository's root folder."] # [doc = ""] # [doc = " If a previous index entry exists that has the same path as the given"] # [doc = " 'entry', it will be replaced. Otherwise, the 'entry' will be added."] # [doc = " The id and the file_size of the 'entry' are updated with the real value"] # [doc = " of the blob."] # [doc = ""] # [doc = " This forces the file to be added to the index, not looking at gitignore"] # [doc = " rules."] # [doc = ""] # [doc = " If this file currently is the result of a merge conflict, this file will"] # [doc = " no longer be marked as conflicting. The data about the conflict will be"] # [doc = " moved to the \"resolve undo\" (REUC) section."] pub fn add_frombuffer (& mut self , entry : & IndexEntry , data : & [u8]) -> Result < () , Error > { let path = CString :: new (& entry . path [..]) ? ; let mut flags = entry . flags & ! raw :: GIT_INDEX_ENTRY_NAMEMASK ; if entry . path . len () < raw :: GIT_INDEX_ENTRY_NAMEMASK as usize { flags |= entry . path . len () as u16 ; } else { flags |= raw :: GIT_INDEX_ENTRY_NAMEMASK ; } unsafe { let raw = raw :: git_index_entry { dev : entry . dev , ino : entry . ino , mode : entry . mode , uid : entry . uid , gid : entry . gid , file_size : entry . file_size , id : * entry . id . raw () , flags , flags_extended : entry . flags_extended , path : path . as_ptr () , mtime : raw :: git_index_time { seconds : entry . mtime . seconds () , nanoseconds : entry . mtime . nanoseconds () , } , ctime : raw :: git_index_time { seconds : entry . ctime . seconds () , nanoseconds : entry . ctime . nanoseconds () , } , } ; let ptr = data . as_ptr () as * const c_void ; let len = data . len () as size_t ; try_call ! (raw :: git_index_add_frombuffer (self . raw , & raw , ptr , len)) ; Ok (()) } } # [doc = " Add or update an index entry from a file on disk"] # [doc = ""] # [doc = " The file path must be relative to the repository's working folder and"] # [doc = " must be readable."] # [doc = ""] # [doc = " This method will fail in bare index instances."] # [doc = ""] # [doc = " This forces the file to be added to the index, not looking at gitignore"] # [doc = " rules."] # [doc = ""] # [doc = " If this file currently is the result of a merge conflict, this file will"] # [doc = " no longer be marked as conflicting. The data about the conflict will be"] # [doc = " moved to the \"resolve undo\" (REUC) section."] pub fn add_path (& mut self , path : & Path) -> Result < () , Error > { let posix_path = path_to_repo_path (path) ? ; unsafe { try_call ! (raw :: git_index_add_bypath (self . raw , posix_path)) ; Ok (()) } } # [doc = " Add or update index entries matching files in the working directory."] # [doc = ""] # [doc = " This method will fail in bare index instances."] # [doc = ""] # [doc = " The `pathspecs` are a list of file names or shell glob patterns that"] # [doc = " will matched against files in the repository's working directory. Each"] # [doc = " file that matches will be added to the index (either updating an"] # [doc = " existing entry or adding a new entry). You can disable glob expansion"] # [doc = " and force exact matching with the `AddDisablePathspecMatch` flag."] # [doc = ""] # [doc = " Files that are ignored will be skipped (unlike `add_path`). If a file is"] # [doc = " already tracked in the index, then it will be updated even if it is"] # [doc = " ignored. Pass the `AddForce` flag to skip the checking of ignore rules."] # [doc = ""] # [doc = " To emulate `git add -A` and generate an error if the pathspec contains"] # [doc = " the exact path of an ignored file (when not using `AddForce`), add the"] # [doc = " `AddCheckPathspec` flag. This checks that each entry in `pathspecs`"] # [doc = " that is an exact match to a filename on disk is either not ignored or"] # [doc = " already in the index. If this check fails, the function will return"] # [doc = " an error."] # [doc = ""] # [doc = " To emulate `git add -A` with the \"dry-run\" option, just use a callback"] # [doc = " function that always returns a positive value. See below for details."] # [doc = ""] # [doc = " If any files are currently the result of a merge conflict, those files"] # [doc = " will no longer be marked as conflicting. The data about the conflicts"] # [doc = " will be moved to the \"resolve undo\" (REUC) section."] # [doc = ""] # [doc = " If you provide a callback function, it will be invoked on each matching"] # [doc = " item in the working directory immediately before it is added to /"] # [doc = " updated in the index. Returning zero will add the item to the index,"] # [doc = " greater than zero will skip the item, and less than zero will abort the"] # [doc = " scan an return an error to the caller."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Emulate `git add *`:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use git2::{Index, IndexAddOption, Repository};"] # [doc = ""] # [doc = " let repo = Repository::open(\"/path/to/a/repo\").expect(\"failed to open\");"] # [doc = " let mut index = repo.index().expect(\"cannot get the Index file\");"] # [doc = " index.add_all([\"*\"].iter(), IndexAddOption::DEFAULT, None);"] # [doc = " index.write();"] # [doc = " ```"] pub fn add_all < T , I > (& mut self , pathspecs : I , flag : IndexAddOption , mut cb : Option < & mut IndexMatchedPath < '_ > > ,) -> Result < () , Error > where T : IntoCString , I : IntoIterator < Item = T > , { let (_a , _b , raw_strarray) = crate :: util :: iter2cstrs_paths (pathspecs) ? ; let ptr = cb . as_mut () ; let callback = ptr . as_ref () . map (| _ | index_matched_path_cb as extern "C" fn (_ , _ , _) -> _) ; unsafe { try_call ! (raw :: git_index_add_all (self . raw , & raw_strarray , flag . bits () as c_uint , callback , ptr . map (| p | p as * mut _) . unwrap_or (ptr :: null_mut ()) as * mut c_void)) ; } Ok (()) } # [doc = " Clear the contents (all the entries) of an index object."] # [doc = ""] # [doc = " This clears the index object in memory; changes must be explicitly"] # [doc = " written to disk for them to take effect persistently via `write_*`."] pub fn clear (& mut self) -> Result < () , Error > { unsafe { try_call ! (raw :: git_index_clear (self . raw)) ; } Ok (()) } # [doc = " Get the count of entries currently in the index"] pub fn len (& self) -> usize { unsafe { raw :: git_index_entrycount (& * self . raw) as usize } } # [doc = " Return `true` is there is no entry in the index"] pub fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Get one of the entries in the index by its position."] pub fn get (& self , n : usize) -> Option < IndexEntry > { unsafe { let ptr = raw :: git_index_get_byindex (self . raw , n as size_t) ; if ptr . is_null () { None } else { Some (Binding :: from_raw (* ptr)) } } } # [doc = " Get an iterator over the entries in this index."] pub fn iter (& self) -> IndexEntries < '_ > { IndexEntries { range : 0 .. self . len () , index : self , } } # [doc = " Get an iterator over the index entries that have conflicts"] pub fn conflicts (& self) -> Result < IndexConflicts < '_ > , Error > { crate :: init () ; let mut conflict_iter = ptr :: null_mut () ; unsafe { try_call ! (raw :: git_index_conflict_iterator_new (& mut conflict_iter , self . raw)) ; Ok (Binding :: from_raw (conflict_iter)) } } # [doc = " Get one of the entries in the index by its path."] pub fn get_path (& self , path : & Path , stage : i32) -> Option < IndexEntry > { let path = path_to_repo_path (path) . unwrap () ; unsafe { let ptr = call ! (raw :: git_index_get_bypath (self . raw , path , stage as c_int)) ; if ptr . is_null () { None } else { Some (Binding :: from_raw (* ptr)) } } } # [doc = " Does this index have conflicts?"] # [doc = ""] # [doc = " Returns `true` if the index contains conflicts, `false` if it does not."] pub fn has_conflicts (& self) -> bool { unsafe { raw :: git_index_has_conflicts (self . raw) == 1 } } # [doc = " Get the index entries that represent a conflict of a single file."] pub fn conflict_get (& self , path : & Path) -> Result < IndexConflict , Error > { let path = path_to_repo_path (path) ? ; let mut ancestor = ptr :: null () ; let mut our = ptr :: null () ; let mut their = ptr :: null () ; unsafe { try_call ! (raw :: git_index_conflict_get (& mut ancestor , & mut our , & mut their , self . raw , path)) ; Ok (IndexConflict { ancestor : match ancestor . is_null () { false => Some (IndexEntry :: from_raw (* ancestor)) , true => None , } , our : match our . is_null () { false => Some (IndexEntry :: from_raw (* our)) , true => None , } , their : match their . is_null () { false => Some (IndexEntry :: from_raw (* their)) , true => None , } , }) } } # [doc = " Get the full path to the index file on disk."] # [doc = ""] # [doc = " Returns `None` if this is an in-memory index."] pub fn path (& self) -> Option < & Path > { unsafe { crate :: opt_bytes (self , raw :: git_index_path (& * self . raw)) . map (util :: bytes2path) } } # [doc = " Update the contents of an existing index object in memory by reading"] # [doc = " from the hard disk."] # [doc = ""] # [doc = " If force is true, this performs a \"hard\" read that discards in-memory"] # [doc = " changes and always reloads the on-disk index data. If there is no"] # [doc = " on-disk version, the index will be cleared."] # [doc = ""] # [doc = " If force is false, this does a \"soft\" read that reloads the index data"] # [doc = " from disk only if it has changed since the last time it was loaded."] # [doc = " Purely in-memory index data will be untouched. Be aware: if there are"] # [doc = " changes on disk, unwritten in-memory changes are discarded."] pub fn read (& mut self , force : bool) -> Result < () , Error > { unsafe { try_call ! (raw :: git_index_read (self . raw , force)) ; } Ok (()) } # [doc = " Read a tree into the index file with stats"] # [doc = ""] # [doc = " The current index contents will be replaced by the specified tree."] pub fn read_tree (& mut self , tree : & Tree < '_ >) -> Result < () , Error > { unsafe { try_call ! (raw :: git_index_read_tree (self . raw , &* tree . raw ())) ; } Ok (()) } # [doc = " Remove an entry from the index"] pub fn remove (& mut self , path : & Path , stage : i32) -> Result < () , Error > { let path = path_to_repo_path (path) ? ; unsafe { try_call ! (raw :: git_index_remove (self . raw , path , stage as c_int)) ; } Ok (()) } # [doc = " Remove an index entry corresponding to a file on disk."] # [doc = ""] # [doc = " The file path must be relative to the repository's working folder. It"] # [doc = " may exist."] # [doc = ""] # [doc = " If this file currently is the result of a merge conflict, this file will"] # [doc = " no longer be marked as conflicting. The data about the conflict will be"] # [doc = " moved to the \"resolve undo\" (REUC) section."] pub fn remove_path (& mut self , path : & Path) -> Result < () , Error > { let path = path_to_repo_path (path) ? ; unsafe { try_call ! (raw :: git_index_remove_bypath (self . raw , path)) ; } Ok (()) } # [doc = " Remove all entries from the index under a given directory."] pub fn remove_dir (& mut self , path : & Path , stage : i32) -> Result < () , Error > { let path = path_to_repo_path (path) ? ; unsafe { try_call ! (raw :: git_index_remove_directory (self . raw , path , stage as c_int)) ; } Ok (()) } # [doc = " Removes the index entries that represent a conflict of a single file."] pub fn conflict_remove (& mut self , path : & Path) -> Result < () , Error > { let path = path_to_repo_path (path) ? ; unsafe { try_call ! (raw :: git_index_conflict_remove (self . raw , path)) ; } Ok (()) } # [doc = " Remove all matching index entries."] # [doc = ""] # [doc = " If you provide a callback function, it will be invoked on each matching"] # [doc = " item in the index immediately before it is removed. Return 0 to remove"] # [doc = " the item, > 0 to skip the item, and < 0 to abort the scan."] pub fn remove_all < T , I > (& mut self , pathspecs : I , mut cb : Option < & mut IndexMatchedPath < '_ > > ,) -> Result < () , Error > where T : IntoCString , I : IntoIterator < Item = T > , { let (_a , _b , raw_strarray) = crate :: util :: iter2cstrs_paths (pathspecs) ? ; let ptr = cb . as_mut () ; let callback = ptr . as_ref () . map (| _ | index_matched_path_cb as extern "C" fn (_ , _ , _) -> _) ; unsafe { try_call ! (raw :: git_index_remove_all (self . raw , & raw_strarray , callback , ptr . map (| p | p as * mut _) . unwrap_or (ptr :: null_mut ()) as * mut c_void)) ; } Ok (()) } # [doc = " Update all index entries to match the working directory"] # [doc = ""] # [doc = " This method will fail in bare index instances."] # [doc = ""] # [doc = " This scans the existing index entries and synchronizes them with the"] # [doc = " working directory, deleting them if the corresponding working directory"] # [doc = " file no longer exists otherwise updating the information (including"] # [doc = " adding the latest version of file to the ODB if needed)."] # [doc = ""] # [doc = " If you provide a callback function, it will be invoked on each matching"] # [doc = " item in the index immediately before it is updated (either refreshed or"] # [doc = " removed depending on working directory state). Return 0 to proceed with"] # [doc = " updating the item, > 0 to skip the item, and < 0 to abort the scan."] pub fn update_all < T , I > (& mut self , pathspecs : I , mut cb : Option < & mut IndexMatchedPath < '_ > > ,) -> Result < () , Error > where T : IntoCString , I : IntoIterator < Item = T > , { let (_a , _b , raw_strarray) = crate :: util :: iter2cstrs_paths (pathspecs) ? ; let ptr = cb . as_mut () ; let callback = ptr . as_ref () . map (| _ | index_matched_path_cb as extern "C" fn (_ , _ , _) -> _) ; unsafe { try_call ! (raw :: git_index_update_all (self . raw , & raw_strarray , callback , ptr . map (| p | p as * mut _) . unwrap_or (ptr :: null_mut ()) as * mut c_void)) ; } Ok (()) } # [doc = " Write an existing index object from memory back to disk using an atomic"] # [doc = " file lock."] pub fn write (& mut self) -> Result < () , Error > { unsafe { try_call ! (raw :: git_index_write (self . raw)) ; } Ok (()) } # [doc = " Write the index as a tree."] # [doc = ""] # [doc = " This method will scan the index and write a representation of its"] # [doc = " current state back to disk; it recursively creates tree objects for each"] # [doc = " of the subtrees stored in the index, but only returns the OID of the"] # [doc = " root tree. This is the OID that can be used e.g. to create a commit."] # [doc = ""] # [doc = " The index instance cannot be bare, and needs to be associated to an"] # [doc = " existing repository."] # [doc = ""] # [doc = " The index must not contain any file in conflict."] pub fn write_tree (& mut self) -> Result < Oid , Error > { let mut raw = raw :: git_oid { id : [0 ; raw :: GIT_OID_RAWSZ] , } ; unsafe { try_call ! (raw :: git_index_write_tree (& mut raw , self . raw)) ; Ok (Binding :: from_raw (& raw as * const _)) } } # [doc = " Write the index as a tree to the given repository"] # [doc = ""] # [doc = " This is the same as `write_tree` except that the destination repository"] # [doc = " can be chosen."] pub fn write_tree_to (& mut self , repo : & Repository) -> Result < Oid , Error > { let mut raw = raw :: git_oid { id : [0 ; raw :: GIT_OID_RAWSZ] , } ; unsafe { try_call ! (raw :: git_index_write_tree_to (& mut raw , self . raw , repo . raw ())) ; Ok (Binding :: from_raw (& raw as * const _)) } } # [doc = " Find the first position of any entries matching a prefix."] # [doc = ""] # [doc = " To find the first position of a path inside a given folder, suffix the prefix with a '/'."] pub fn find_prefix < T : IntoCString > (& self , prefix : T) -> Result < usize , Error > { let mut at_pos : size_t = 0 ; let entry_path = prefix . into_c_string () ? ; unsafe { try_call ! (raw :: git_index_find_prefix (& mut at_pos , self . raw , entry_path)) ; Ok (at_pos) } } }
    };
}

impl_386!();