macro_rules! deps {
    () => {
        Parents!();
        Tree!();
        Commit!();
        Object!();
        ParentIds!();
        Mailmap!();
        Time!();
        Signature!();
        Oid!();
        Binding!();
        IntoCString!();
        Buf!();
        Error!();
    };
}

macro_rules! impl_250 {
    () => {
        deps!();
        impl < 'repo > Commit < 'repo > { # [doc = " Get the id (SHA1) of a repository commit"] pub fn id (& self) -> Oid { unsafe { Binding :: from_raw (raw :: git_commit_id (& * self . raw)) } } # [doc = " Get the id of the tree pointed to by this commit."] # [doc = ""] # [doc = " No attempts are made to fetch an object from the ODB."] pub fn tree_id (& self) -> Oid { unsafe { Binding :: from_raw (raw :: git_commit_tree_id (& * self . raw)) } } # [doc = " Get the tree pointed to by a commit."] pub fn tree (& self) -> Result < Tree < 'repo > , Error > { let mut ret = ptr :: null_mut () ; unsafe { try_call ! (raw :: git_commit_tree (& mut ret , &* self . raw)) ; Ok (Binding :: from_raw (ret)) } } # [doc = " Get access to the underlying raw pointer."] pub fn raw (& self) -> * mut raw :: git_commit { self . raw } # [doc = " Get the full message of a commit."] # [doc = ""] # [doc = " The returned message will be slightly prettified by removing any"] # [doc = " potential leading newlines."] # [doc = ""] # [doc = " `None` will be returned if the message is not valid utf-8"] pub fn message (& self) -> Option < & str > { str :: from_utf8 (self . message_bytes ()) . ok () } # [doc = " Get the full message of a commit as a byte slice."] # [doc = ""] # [doc = " The returned message will be slightly prettified by removing any"] # [doc = " potential leading newlines."] pub fn message_bytes (& self) -> & [u8] { unsafe { crate :: opt_bytes (self , raw :: git_commit_message (& * self . raw)) . unwrap () } } # [doc = " Get the encoding for the message of a commit, as a string representing a"] # [doc = " standard encoding name."] # [doc = ""] # [doc = " `None` will be returned if the encoding is not known"] pub fn message_encoding (& self) -> Option < & str > { let bytes = unsafe { crate :: opt_bytes (self , raw :: git_commit_message_encoding (& * self . raw)) } ; bytes . and_then (| b | str :: from_utf8 (b) . ok ()) } # [doc = " Get the full raw message of a commit."] # [doc = ""] # [doc = " `None` will be returned if the message is not valid utf-8"] pub fn message_raw (& self) -> Option < & str > { str :: from_utf8 (self . message_raw_bytes ()) . ok () } # [doc = " Get the full raw message of a commit."] pub fn message_raw_bytes (& self) -> & [u8] { unsafe { crate :: opt_bytes (self , raw :: git_commit_message_raw (& * self . raw)) . unwrap () } } # [doc = " Get the full raw text of the commit header."] # [doc = ""] # [doc = " `None` will be returned if the message is not valid utf-8"] pub fn raw_header (& self) -> Option < & str > { str :: from_utf8 (self . raw_header_bytes ()) . ok () } # [doc = " Get an arbitrary header field."] pub fn header_field_bytes < T : IntoCString > (& self , field : T) -> Result < Buf , Error > { let buf = Buf :: new () ; let raw_field = field . into_c_string () ? ; unsafe { try_call ! (raw :: git_commit_header_field (buf . raw () , &* self . raw , raw_field)) ; } Ok (buf) } # [doc = " Get the full raw text of the commit header."] pub fn raw_header_bytes (& self) -> & [u8] { unsafe { crate :: opt_bytes (self , raw :: git_commit_raw_header (& * self . raw)) . unwrap () } } # [doc = " Get the short \"summary\" of the git commit message."] # [doc = ""] # [doc = " The returned message is the summary of the commit, comprising the first"] # [doc = " paragraph of the message with whitespace trimmed and squashed."] # [doc = ""] # [doc = " `None` may be returned if an error occurs or if the summary is not valid"] # [doc = " utf-8."] pub fn summary (& self) -> Option < & str > { self . summary_bytes () . and_then (| s | str :: from_utf8 (s) . ok ()) } # [doc = " Get the short \"summary\" of the git commit message."] # [doc = ""] # [doc = " The returned message is the summary of the commit, comprising the first"] # [doc = " paragraph of the message with whitespace trimmed and squashed."] # [doc = ""] # [doc = " `None` may be returned if an error occurs"] pub fn summary_bytes (& self) -> Option < & [u8] > { unsafe { crate :: opt_bytes (self , raw :: git_commit_summary (self . raw)) } } # [doc = " Get the long \"body\" of the git commit message."] # [doc = ""] # [doc = " The returned message is the body of the commit, comprising everything"] # [doc = " but the first paragraph of the message. Leading and trailing whitespaces"] # [doc = " are trimmed."] # [doc = ""] # [doc = " `None` may be returned if an error occurs or if the summary is not valid"] # [doc = " utf-8."] pub fn body (& self) -> Option < & str > { self . body_bytes () . and_then (| s | str :: from_utf8 (s) . ok ()) } # [doc = " Get the long \"body\" of the git commit message."] # [doc = ""] # [doc = " The returned message is the body of the commit, comprising everything"] # [doc = " but the first paragraph of the message. Leading and trailing whitespaces"] # [doc = " are trimmed."] # [doc = ""] # [doc = " `None` may be returned if an error occurs."] pub fn body_bytes (& self) -> Option < & [u8] > { unsafe { crate :: opt_bytes (self , raw :: git_commit_body (self . raw)) } } # [doc = " Get the commit time (i.e. committer time) of a commit."] # [doc = ""] # [doc = " The first element of the tuple is the time, in seconds, since the epoch."] # [doc = " The second element is the offset, in minutes, of the time zone of the"] # [doc = " committer's preferred time zone."] pub fn time (& self) -> Time { unsafe { Time :: new (raw :: git_commit_time (& * self . raw) as i64 , raw :: git_commit_time_offset (& * self . raw) as i32 ,) } } # [doc = " Creates a new iterator over the parents of this commit."] pub fn parents < 'a > (& 'a self) -> Parents < 'a , 'repo > { Parents { range : 0 .. self . parent_count () , commit : self , } } # [doc = " Creates a new iterator over the parents of this commit."] pub fn parent_ids (& self) -> ParentIds < '_ > { ParentIds { range : 0 .. self . parent_count () , commit : self , } } # [doc = " Get the author of this commit."] pub fn author (& self) -> Signature < '_ > { unsafe { let ptr = raw :: git_commit_author (& * self . raw) ; signature :: from_raw_const (self , ptr) } } # [doc = " Get the author of this commit, using the mailmap to map names and email"] # [doc = " addresses to canonical real names and email addresses."] pub fn author_with_mailmap (& self , mailmap : & Mailmap) -> Result < Signature < 'static > , Error > { let mut ret = ptr :: null_mut () ; unsafe { try_call ! (raw :: git_commit_author_with_mailmap (& mut ret , &* self . raw , &* mailmap . raw ())) ; Ok (Binding :: from_raw (ret)) } } # [doc = " Get the committer of this commit."] pub fn committer (& self) -> Signature < '_ > { unsafe { let ptr = raw :: git_commit_committer (& * self . raw) ; signature :: from_raw_const (self , ptr) } } # [doc = " Get the committer of this commit, using the mailmap to map names and email"] # [doc = " addresses to canonical real names and email addresses."] pub fn committer_with_mailmap (& self , mailmap : & Mailmap) -> Result < Signature < 'static > , Error > { let mut ret = ptr :: null_mut () ; unsafe { try_call ! (raw :: git_commit_committer_with_mailmap (& mut ret , &* self . raw , &* mailmap . raw ())) ; Ok (Binding :: from_raw (ret)) } } # [doc = " Amend this existing commit with all non-`None` values"] # [doc = ""] # [doc = " This creates a new commit that is exactly the same as the old commit,"] # [doc = " except that any non-`None` values will be updated. The new commit has"] # [doc = " the same parents as the old commit."] # [doc = ""] # [doc = " For information about `update_ref`, see [`Repository::commit`]."] # [doc = ""] # [doc = " [`Repository::commit`]: struct.Repository.html#method.commit"] pub fn amend (& self , update_ref : Option < & str > , author : Option < & Signature < '_ > > , committer : Option < & Signature < '_ > > , message_encoding : Option < & str > , message : Option < & str > , tree : Option < & Tree < 'repo > > ,) -> Result < Oid , Error > { let mut raw = raw :: git_oid { id : [0 ; raw :: GIT_OID_RAWSZ] , } ; let update_ref = crate :: opt_cstr (update_ref) ? ; let encoding = crate :: opt_cstr (message_encoding) ? ; let message = crate :: opt_cstr (message) ? ; unsafe { try_call ! (raw :: git_commit_amend (& mut raw , self . raw () , update_ref , author . map (| s | s . raw ()) , committer . map (| s | s . raw ()) , encoding , message , tree . map (| t | t . raw ()))) ; Ok (Binding :: from_raw (& raw as * const _)) } } # [doc = " Get the number of parents of this commit."] # [doc = ""] # [doc = " Use the `parents` iterator to return an iterator over all parents."] pub fn parent_count (& self) -> usize { unsafe { raw :: git_commit_parentcount (& * self . raw) as usize } } # [doc = " Get the specified parent of the commit."] # [doc = ""] # [doc = " Use the `parents` iterator to return an iterator over all parents."] pub fn parent (& self , i : usize) -> Result < Commit < 'repo > , Error > { unsafe { let mut raw = ptr :: null_mut () ; try_call ! (raw :: git_commit_parent (& mut raw , &* self . raw , i as libc :: c_uint)) ; Ok (Binding :: from_raw (raw)) } } # [doc = " Get the specified parent id of the commit."] # [doc = ""] # [doc = " This is different from `parent`, which will attempt to load the"] # [doc = " parent commit from the ODB."] # [doc = ""] # [doc = " Use the `parent_ids` iterator to return an iterator over all parents."] pub fn parent_id (& self , i : usize) -> Result < Oid , Error > { unsafe { let id = raw :: git_commit_parent_id (self . raw , i as libc :: c_uint) ; if id . is_null () { Err (Error :: from_str ("parent index out of bounds")) } else { Ok (Binding :: from_raw (id)) } } } # [doc = " Casts this Commit to be usable as an `Object`"] pub fn as_object (& self) -> & Object < 'repo > { unsafe { & * (self as * const _ as * const Object < 'repo >) } } # [doc = " Consumes Commit to be returned as an `Object`"] pub fn into_object (self) -> Object < 'repo > { assert_eq ! (mem :: size_of_val (& self) , mem :: size_of ::< Object <'_ >> ()) ; unsafe { mem :: transmute (self) } } }
    };
}

impl_250!()