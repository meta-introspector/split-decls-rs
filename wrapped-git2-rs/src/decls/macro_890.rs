macro_rules! macro_890 {
    () => {
        bitflags ! { # [doc = " ..."] # [derive (Clone , Copy , Debug , Eq , PartialEq , PartialOrd , Ord , Hash)] pub struct PathspecFlags : u32 { # [doc = " Use the default pathspec matching configuration."] const DEFAULT = raw :: GIT_PATHSPEC_DEFAULT as u32 ; # [doc = " Force matching to ignore case, otherwise matching will use native"] # [doc = " case sensitivity of the platform filesystem."] const IGNORE_CASE = raw :: GIT_PATHSPEC_IGNORE_CASE as u32 ; # [doc = " Force case sensitive matches, otherwise match will use the native"] # [doc = " case sensitivity of the platform filesystem."] const USE_CASE = raw :: GIT_PATHSPEC_USE_CASE as u32 ; # [doc = " Disable glob patterns and just use simple string comparison for"] # [doc = " matching."] const NO_GLOB = raw :: GIT_PATHSPEC_NO_GLOB as u32 ; # [doc = " Means that match functions return the error code `NotFound` if no"] # [doc = " matches are found. By default no matches is a success."] const NO_MATCH_ERROR = raw :: GIT_PATHSPEC_NO_MATCH_ERROR as u32 ; # [doc = " Means that the list returned should track which patterns matched"] # [doc = " which files so that at the end of the match we can identify patterns"] # [doc = " that did not match any files."] const FIND_FAILURES = raw :: GIT_PATHSPEC_FIND_FAILURES as u32 ; # [doc = " Means that the list returned does not need to keep the actual"] # [doc = " matching filenames. Use this to just test if there were any matches"] # [doc = " at all or in combination with `PATHSPEC_FAILURES` to validate a"] # [doc = " pathspec."] const FAILURES_ONLY = raw :: GIT_PATHSPEC_FAILURES_ONLY as u32 ; } }
    };
}

macro_890!()