// Generated macro for tests (module)
macro_rules! Depcrate_packbuildertests {
() => {
// Module: crate::packbuilder
// Provides: {"tests"}
// Dependencies: {}
# [cfg (test)] mod tests { use crate :: { Buf , Oid } ; const EMPTY_PACKFILE_OID : & str = "029d08823bd8a8eab510ad6ac75c823cfd3ed31e" ; fn pack_header (len : u8) -> Vec < u8 > { [] . iter () . chain (b"PACK") . chain (& [0 , 0 , 0 , 2]) . chain (& [0 , 0 , 0 , len]) . cloned () . collect :: < Vec < u8 > > () } fn empty_pack_header () -> Vec < u8 > { pack_header (0) . iter () . chain (& [0x02 , 0x9d , 0x08 , 0x82 , 0x3b , 0xd8 , 0xa8 , 0xea , 0xb5 , 0x10 , 0xad , 0x6a , 0xc7 , 0x5c , 0x82 , 0x3c , 0xfd , 0x3e , 0xd3 , 0x1e ,]) . cloned () . collect :: < Vec < u8 > > () } # [test] fn smoke () { let (_td , repo) = crate :: test :: repo_init () ; let _builder = t ! (repo . packbuilder ()) ; } # [test] fn smoke_write_buf () { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; let mut buf = Buf :: new () ; t ! (builder . write_buf (& mut buf)) ; # [allow (deprecated)] { assert ! (builder . hash () . unwrap () . is_zero ()) ; } assert ! (builder . name () . is_none ()) ; assert_eq ! (&* buf , &* empty_pack_header ()) ; } # [test] fn smoke_write () { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; t ! (builder . write (repo . path () , 0)) ; # [allow (deprecated)] { assert ! (builder . hash () . unwrap () == Oid :: from_str (EMPTY_PACKFILE_OID) . unwrap ()) ; } assert ! (builder . name () . unwrap () == EMPTY_PACKFILE_OID) ; } # [test] fn smoke_foreach () { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; let mut buf = Vec :: < u8 > :: new () ; t ! (builder . foreach (| bytes | { buf . extend (bytes) ; true })) ; assert_eq ! (&* buf , &* empty_pack_header ()) ; } # [test] fn insert_write_buf () { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; let mut buf = Buf :: new () ; let (commit , _tree) = crate :: test :: commit (& repo) ; t ! (builder . insert_object (commit , None)) ; assert_eq ! (builder . object_count () , 1) ; t ! (builder . write_buf (& mut buf)) ; assert_eq ! (& buf [0 .. 12] , &* pack_header (1)) ; } # [test] fn insert_tree_write_buf () { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; let mut buf = Buf :: new () ; let (_commit , tree) = crate :: test :: commit (& repo) ; t ! (builder . insert_tree (tree)) ; assert_eq ! (builder . object_count () , 2) ; t ! (builder . write_buf (& mut buf)) ; assert_eq ! (& buf [0 .. 12] , &* pack_header (2)) ; } # [test] fn insert_commit_write_buf () { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; let mut buf = Buf :: new () ; let (commit , _tree) = crate :: test :: commit (& repo) ; t ! (builder . insert_commit (commit)) ; assert_eq ! (builder . object_count () , 3) ; t ! (builder . write_buf (& mut buf)) ; assert_eq ! (& buf [0 .. 12] , &* pack_header (3)) ; } # [test] fn insert_write () { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; let (commit , _tree) = crate :: test :: commit (& repo) ; t ! (builder . insert_object (commit , None)) ; assert_eq ! (builder . object_count () , 1) ; t ! (builder . write (repo . path () , 0)) ; t ! (repo . find_commit (commit)) ; } # [test] fn insert_tree_write () { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; let (_commit , tree) = crate :: test :: commit (& repo) ; t ! (builder . insert_tree (tree)) ; assert_eq ! (builder . object_count () , 2) ; t ! (builder . write (repo . path () , 0)) ; t ! (repo . find_tree (tree)) ; } # [test] fn insert_commit_write () { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; let (commit , _tree) = crate :: test :: commit (& repo) ; t ! (builder . insert_commit (commit)) ; assert_eq ! (builder . object_count () , 3) ; t ! (builder . write (repo . path () , 0)) ; t ! (repo . find_commit (commit)) ; } # [test] fn progress_callback () { let mut progress_called = false ; { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; let (commit , _tree) = crate :: test :: commit (& repo) ; t ! (builder . set_progress_callback (| _ , _ , _ | { progress_called = true ; true })) ; t ! (builder . insert_commit (commit)) ; t ! (builder . write_buf (& mut Buf :: new ())) ; } assert_eq ! (progress_called , true) ; } # [test] fn clear_progress_callback () { let mut progress_called = false ; { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; let (commit , _tree) = crate :: test :: commit (& repo) ; t ! (builder . set_progress_callback (| _ , _ , _ | { progress_called = true ; true })) ; t ! (builder . unset_progress_callback ()) ; t ! (builder . insert_commit (commit)) ; t ! (builder . write_buf (& mut Buf :: new ())) ; } assert_eq ! (progress_called , false) ; } # [test] fn progress_callback_with_write () { let mut progress_called = false ; { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; let (commit , _tree) = crate :: test :: commit (& repo) ; t ! (builder . set_progress_callback (| _ , _ , _ | { progress_called = true ; true })) ; t ! (builder . insert_commit (commit)) ; t ! (builder . write (repo . path () , 0)) ; } assert_eq ! (progress_called , true) ; } # [test] fn set_threads () { let (_td , repo) = crate :: test :: repo_init () ; let mut builder = t ! (repo . packbuilder ()) ; let used = builder . set_threads (4) ; assert ! (used == 1 || used == 4) ; } }
};
}
