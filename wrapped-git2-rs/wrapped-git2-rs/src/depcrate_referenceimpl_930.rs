// Generated macro for impl_930 (impl)
macro_rules! Depcrate_referenceimpl_930 {
() => {
// Module: crate::reference
// Provides: {"impl_930"}
// Dependencies: {}
impl < 'repo > Reference < 'repo > { # [doc = " Ensure the reference name is well-formed."] # [doc = ""] # [doc = " Validation is performed as if [`ReferenceFormat::ALLOW_ONELEVEL`]"] # [doc = " was given to [`Reference::normalize_name`]. No normalization is"] # [doc = " performed, however."] # [doc = ""] # [doc = " ```rust"] # [doc = " use git2::Reference;"] # [doc = ""] # [doc = " assert!(Reference::is_valid_name(\"HEAD\"));"] # [doc = " assert!(Reference::is_valid_name(\"refs/heads/main\"));"] # [doc = ""] # [doc = " // But:"] # [doc = " assert!(!Reference::is_valid_name(\"main\"));"] # [doc = " assert!(!Reference::is_valid_name(\"refs/heads/*\"));"] # [doc = " assert!(!Reference::is_valid_name(\"foo//bar\"));"] # [doc = " ```"] # [doc = ""] # [doc = " [`ReferenceFormat::ALLOW_ONELEVEL`]:"] # [doc = "     struct.ReferenceFormat#associatedconstant.ALLOW_ONELEVEL"] # [doc = " [`Reference::normalize_name`]: struct.Reference#method.normalize_name"] pub fn is_valid_name (refname : & str) -> bool { crate :: init () ; let refname = CString :: new (refname) . unwrap () ; let mut valid : libc :: c_int = 0 ; unsafe { call :: c_try (raw :: git_reference_name_is_valid (& mut valid , refname . as_ptr () ,)) . unwrap () ; } valid == 1 } # [doc = " Normalize reference name and check validity."] # [doc = ""] # [doc = " This will normalize the reference name by collapsing runs of adjacent"] # [doc = " slashes between name components into a single slash. It also validates"] # [doc = " the name according to the following rules:"] # [doc = ""] # [doc = " 1. If [`ReferenceFormat::ALLOW_ONELEVEL`] is given, the name may"] # [doc = "    contain only capital letters and underscores, and must begin and end"] # [doc = "    with a letter. (e.g. \"HEAD\", \"ORIG_HEAD\")."] # [doc = " 2. The flag [`ReferenceFormat::REFSPEC_SHORTHAND`] has an effect"] # [doc = "    only when combined with [`ReferenceFormat::ALLOW_ONELEVEL`]. If"] # [doc = "    it is given, \"shorthand\" branch names (i.e. those not prefixed by"] # [doc = "    `refs/`, but consisting of a single word without `/` separators)"] # [doc = "    become valid. For example, \"main\" would be accepted."] # [doc = " 3. If [`ReferenceFormat::REFSPEC_PATTERN`] is given, the name may"] # [doc = "    contain a single `*` in place of a full pathname component (e.g."] # [doc = "    `foo/*/bar`, `foo/bar*`)."] # [doc = " 4. Names prefixed with \"refs/\" can be almost anything. You must avoid"] # [doc = "    the characters '~', '^', ':', '\\\\', '?', '[', and '*', and the"] # [doc = "    sequences \"..\" and \"@{\" which have special meaning to revparse."] # [doc = ""] # [doc = " If the reference passes validation, it is returned in normalized form,"] # [doc = " otherwise an [`Error`] with [`ErrorCode::InvalidSpec`] is returned."] # [doc = ""] # [doc = " ```rust"] # [doc = " use git2::{Reference, ReferenceFormat};"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     Reference::normalize_name("] # [doc = "         \"foo//bar\","] # [doc = "         ReferenceFormat::NORMAL"] # [doc = "     )"] # [doc = "     .unwrap(),"] # [doc = "     \"foo/bar\".to_owned()"] # [doc = " );"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     Reference::normalize_name("] # [doc = "         \"HEAD\","] # [doc = "         ReferenceFormat::ALLOW_ONELEVEL"] # [doc = "     )"] # [doc = "     .unwrap(),"] # [doc = "     \"HEAD\".to_owned()"] # [doc = " );"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     Reference::normalize_name("] # [doc = "         \"refs/heads/*\","] # [doc = "         ReferenceFormat::REFSPEC_PATTERN"] # [doc = "     )"] # [doc = "     .unwrap(),"] # [doc = "     \"refs/heads/*\".to_owned()"] # [doc = " );"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     Reference::normalize_name("] # [doc = "         \"main\","] # [doc = "         ReferenceFormat::ALLOW_ONELEVEL | ReferenceFormat::REFSPEC_SHORTHAND"] # [doc = "     )"] # [doc = "     .unwrap(),"] # [doc = "     \"main\".to_owned()"] # [doc = " );"] # [doc = " ```"] # [doc = ""] # [doc = " [`ReferenceFormat::ALLOW_ONELEVEL`]:"] # [doc = "     struct.ReferenceFormat#associatedconstant.ALLOW_ONELEVEL"] # [doc = " [`ReferenceFormat::REFSPEC_SHORTHAND`]:"] # [doc = "     struct.ReferenceFormat#associatedconstant.REFSPEC_SHORTHAND"] # [doc = " [`ReferenceFormat::REFSPEC_PATTERN`]:"] # [doc = "     struct.ReferenceFormat#associatedconstant.REFSPEC_PATTERN"] # [doc = " [`Error`]: struct.Error"] # [doc = " [`ErrorCode::InvalidSpec`]: enum.ErrorCode#variant.InvalidSpec"] pub fn normalize_name (refname : & str , flags : ReferenceFormat) -> Result < String , Error > { crate :: init () ; let mut dst = [0u8 ; GIT_REFNAME_MAX] ; let refname = CString :: new (refname) ? ; unsafe { try_call ! (raw :: git_reference_normalize_name (dst . as_mut_ptr () as * mut libc :: c_char , dst . len () as libc :: size_t , refname , flags . bits ())) ; let s = & dst [.. dst . iter () . position (| & a | a == 0) . unwrap ()] ; Ok (str :: from_utf8 (s) . unwrap () . to_owned ()) } } # [doc = " Get access to the underlying raw pointer."] pub fn raw (& self) -> * mut raw :: git_reference { self . raw } # [doc = " Delete an existing reference."] # [doc = ""] # [doc = " This method works for both direct and symbolic references. The reference"] # [doc = " will be immediately removed on disk."] # [doc = ""] # [doc = " This function will return an error if the reference has changed from the"] # [doc = " time it was looked up."] pub fn delete (& mut self) -> Result < () , Error > { unsafe { try_call ! (raw :: git_reference_delete (self . raw)) ; } Ok (()) } # [doc = " Check if a reference is a local branch."] pub fn is_branch (& self) -> bool { unsafe { raw :: git_reference_is_branch (& * self . raw) == 1 } } # [doc = " Check if a reference is a note."] pub fn is_note (& self) -> bool { unsafe { raw :: git_reference_is_note (& * self . raw) == 1 } } # [doc = " Check if a reference is a remote tracking branch"] pub fn is_remote (& self) -> bool { unsafe { raw :: git_reference_is_remote (& * self . raw) == 1 } } # [doc = " Check if a reference is a tag"] pub fn is_tag (& self) -> bool { unsafe { raw :: git_reference_is_tag (& * self . raw) == 1 } } # [doc = " Get the reference type of a reference."] # [doc = ""] # [doc = " If the type is unknown, then `None` is returned."] pub fn kind (& self) -> Option < ReferenceType > { ReferenceType :: from_raw (unsafe { raw :: git_reference_type (& * self . raw) }) } # [doc = " Get the full name of a reference."] # [doc = ""] # [doc = " Returns `None` if the name is not valid utf-8."] pub fn name (& self) -> Option < & str > { str :: from_utf8 (self . name_bytes ()) . ok () } # [doc = " Get the full name of a reference."] pub fn name_bytes (& self) -> & [u8] { unsafe { crate :: opt_bytes (self , raw :: git_reference_name (& * self . raw)) . unwrap () } } # [doc = " Get the full shorthand of a reference."] # [doc = ""] # [doc = " This will transform the reference name into a name \"human-readable\""] # [doc = " version. If no shortname is appropriate, it will return the full name."] # [doc = ""] # [doc = " Returns `None` if the shorthand is not valid utf-8."] pub fn shorthand (& self) -> Option < & str > { str :: from_utf8 (self . shorthand_bytes ()) . ok () } # [doc = " Get the full shorthand of a reference."] pub fn shorthand_bytes (& self) -> & [u8] { unsafe { crate :: opt_bytes (self , raw :: git_reference_shorthand (& * self . raw)) . unwrap () } } # [doc = " Get the OID pointed to by a direct reference."] # [doc = ""] # [doc = " Only available if the reference is direct (i.e. an object id reference,"] # [doc = " not a symbolic one)."] pub fn target (& self) -> Option < Oid > { unsafe { Binding :: from_raw_opt (raw :: git_reference_target (& * self . raw)) } } # [doc = " Return the peeled OID target of this reference."] # [doc = ""] # [doc = " This peeled OID only applies to direct references that point to a hard"] # [doc = " Tag object: it is the result of peeling such Tag."] pub fn target_peel (& self) -> Option < Oid > { unsafe { Binding :: from_raw_opt (raw :: git_reference_target_peel (& * self . raw)) } } # [doc = " Get full name to the reference pointed to by a symbolic reference."] # [doc = ""] # [doc = " May return `None` if the reference is either not symbolic or not a"] # [doc = " valid utf-8 string."] pub fn symbolic_target (& self) -> Option < & str > { self . symbolic_target_bytes () . and_then (| s | str :: from_utf8 (s) . ok ()) } # [doc = " Get full name to the reference pointed to by a symbolic reference."] # [doc = ""] # [doc = " Only available if the reference is symbolic."] pub fn symbolic_target_bytes (& self) -> Option < & [u8] > { unsafe { crate :: opt_bytes (self , raw :: git_reference_symbolic_target (& * self . raw)) } } # [doc = " Resolve a symbolic reference to a direct reference."] # [doc = ""] # [doc = " This method iteratively peels a symbolic reference until it resolves to"] # [doc = " a direct reference to an OID."] # [doc = ""] # [doc = " If a direct reference is passed as an argument, a copy of that"] # [doc = " reference is returned."] pub fn resolve (& self) -> Result < Reference < 'repo > , Error > { let mut raw = ptr :: null_mut () ; unsafe { try_call ! (raw :: git_reference_resolve (& mut raw , &* self . raw)) ; Ok (Binding :: from_raw (raw)) } } # [doc = " Peel a reference to an object"] # [doc = ""] # [doc = " This method recursively peels the reference until it reaches"] # [doc = " an object of the specified type."] pub fn peel (& self , kind : ObjectType) -> Result < Object < 'repo > , Error > { let mut raw = ptr :: null_mut () ; unsafe { try_call ! (raw :: git_reference_peel (& mut raw , self . raw , kind)) ; Ok (Binding :: from_raw (raw)) } } # [doc = " Peel a reference to a blob"] # [doc = ""] # [doc = " This method recursively peels the reference until it reaches"] # [doc = " a blob."] pub fn peel_to_blob (& self) -> Result < Blob < 'repo > , Error > { Ok (self . peel (ObjectType :: Blob) ? . cast_or_panic (ObjectType :: Blob)) } # [doc = " Peel a reference to a commit"] # [doc = ""] # [doc = " This method recursively peels the reference until it reaches"] # [doc = " a commit."] pub fn peel_to_commit (& self) -> Result < Commit < 'repo > , Error > { Ok (self . peel (ObjectType :: Commit) ? . cast_or_panic (ObjectType :: Commit)) } # [doc = " Peel a reference to a tree"] # [doc = ""] # [doc = " This method recursively peels the reference until it reaches"] # [doc = " a tree."] pub fn peel_to_tree (& self) -> Result < Tree < 'repo > , Error > { Ok (self . peel (ObjectType :: Tree) ? . cast_or_panic (ObjectType :: Tree)) } # [doc = " Peel a reference to a tag"] # [doc = ""] # [doc = " This method recursively peels the reference until it reaches"] # [doc = " a tag."] pub fn peel_to_tag (& self) -> Result < Tag < 'repo > , Error > { Ok (self . peel (ObjectType :: Tag) ? . cast_or_panic (ObjectType :: Tag)) } # [doc = " Rename an existing reference."] # [doc = ""] # [doc = " This method works for both direct and symbolic references."] # [doc = ""] # [doc = " If the force flag is not enabled, and there's already a reference with"] # [doc = " the given name, the renaming will fail."] pub fn rename (& mut self , new_name : & str , force : bool , msg : & str ,) -> Result < Reference < 'repo > , Error > { let mut raw = ptr :: null_mut () ; let new_name = CString :: new (new_name) ? ; let msg = CString :: new (msg) ? ; unsafe { try_call ! (raw :: git_reference_rename (& mut raw , self . raw , new_name , force , msg)) ; Ok (Binding :: from_raw (raw)) } } # [doc = " Conditionally create a new reference with the same name as the given"] # [doc = " reference but a different OID target. The reference must be a direct"] # [doc = " reference, otherwise this will fail."] # [doc = ""] # [doc = " The new reference will be written to disk, overwriting the given"] # [doc = " reference."] pub fn set_target (& mut self , id : Oid , reflog_msg : & str) -> Result < Reference < 'repo > , Error > { let mut raw = ptr :: null_mut () ; let msg = CString :: new (reflog_msg) ? ; unsafe { try_call ! (raw :: git_reference_set_target (& mut raw , self . raw , id . raw () , msg)) ; Ok (Binding :: from_raw (raw)) } } # [doc = " Create a new reference with the same name as the given reference but a"] # [doc = " different symbolic target. The reference must be a symbolic reference,"] # [doc = " otherwise this will fail."] # [doc = ""] # [doc = " The new reference will be written to disk, overwriting the given"] # [doc = " reference."] # [doc = ""] # [doc = " The target name will be checked for validity. See"] # [doc = " [`Repository::reference_symbolic`] for rules about valid names."] # [doc = ""] # [doc = " The message for the reflog will be ignored if the reference does not"] # [doc = " belong in the standard set (HEAD, branches and remote-tracking"] # [doc = " branches) and it does not have a reflog."] pub fn symbolic_set_target (& mut self , target : & str , reflog_msg : & str ,) -> Result < Reference < 'repo > , Error > { let mut raw = ptr :: null_mut () ; let target = CString :: new (target) ? ; let msg = CString :: new (reflog_msg) ? ; unsafe { try_call ! (raw :: git_reference_symbolic_set_target (& mut raw , self . raw , target , msg)) ; Ok (Binding :: from_raw (raw)) } } }
};
}
