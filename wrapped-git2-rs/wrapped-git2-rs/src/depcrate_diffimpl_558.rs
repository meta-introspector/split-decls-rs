// Generated macro for impl_558 (impl)
macro_rules! Depcrate_diffimpl_558 {
() => {
// Module: crate::diff
// Provides: {"impl_558"}
// Dependencies: {}
impl DiffFindOptions { # [doc = " Creates a new set of empty diff find options."] # [doc = ""] # [doc = " All flags and other options are defaulted to false or their otherwise"] # [doc = " zero equivalents."] pub fn new () -> DiffFindOptions { let mut opts = DiffFindOptions { raw : unsafe { mem :: zeroed () } , } ; assert_eq ! (unsafe { raw :: git_diff_find_init_options (& mut opts . raw , 1) } , 0) ; opts } fn flag (& mut self , opt : u32 , val : bool) -> & mut DiffFindOptions { if val { self . raw . flags |= opt ; } else { self . raw . flags &= ! opt ; } self } # [doc = " Reset all flags back to their unset state, indicating that"] # [doc = " `diff.renames` should be used instead. This is overridden once any flag"] # [doc = " is set."] pub fn by_config (& mut self) -> & mut DiffFindOptions { self . flag (0xffffffff , false) } # [doc = " Look for renames?"] pub fn renames (& mut self , find : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_FIND_RENAMES , find) } # [doc = " Consider old side of modified for renames?"] pub fn renames_from_rewrites (& mut self , find : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_FIND_RENAMES_FROM_REWRITES , find) } # [doc = " Look for copies?"] pub fn copies (& mut self , find : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_FIND_COPIES , find) } # [doc = " Consider unmodified as copy sources?"] # [doc = ""] # [doc = " For this to work correctly, use `include_unmodified` when the initial"] # [doc = " diff is being generated."] pub fn copies_from_unmodified (& mut self , find : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_FIND_COPIES_FROM_UNMODIFIED , find) } # [doc = " Mark significant rewrites for split."] pub fn rewrites (& mut self , find : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_FIND_REWRITES , find) } # [doc = " Actually split large rewrites into delete/add pairs"] pub fn break_rewrites (& mut self , find : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_BREAK_REWRITES , find) } # [doc (hidden)] pub fn break_rewries (& mut self , find : bool) -> & mut DiffFindOptions { self . break_rewrites (find) } # [doc = " Find renames/copies for untracked items in working directory."] # [doc = ""] # [doc = " For this to work correctly use the `include_untracked` option when the"] # [doc = " initial diff is being generated."] pub fn for_untracked (& mut self , find : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_FIND_FOR_UNTRACKED , find) } # [doc = " Turn on all finding features."] pub fn all (& mut self , find : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_FIND_ALL , find) } # [doc = " Measure similarity ignoring leading whitespace (default)"] pub fn ignore_leading_whitespace (& mut self , ignore : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_FIND_IGNORE_LEADING_WHITESPACE , ignore) } # [doc = " Measure similarity ignoring all whitespace"] pub fn ignore_whitespace (& mut self , ignore : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_FIND_IGNORE_WHITESPACE , ignore) } # [doc = " Measure similarity including all data"] pub fn dont_ignore_whitespace (& mut self , dont : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_FIND_DONT_IGNORE_WHITESPACE , dont) } # [doc = " Measure similarity only by comparing SHAs (fast and cheap)"] pub fn exact_match_only (& mut self , exact : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_FIND_EXACT_MATCH_ONLY , exact) } # [doc = " Do not break rewrites unless they contribute to a rename."] # [doc = ""] # [doc = " Normally, `break_rewrites` and `rewrites` will measure the"] # [doc = " self-similarity of modified files and split the ones that have changed a"] # [doc = " lot into a delete/add pair.  Then the sides of that pair will be"] # [doc = " considered candidates for rename and copy detection"] # [doc = ""] # [doc = " If you add this flag in and the split pair is not used for an actual"] # [doc = " rename or copy, then the modified record will be restored to a regular"] # [doc = " modified record instead of being split."] pub fn break_rewrites_for_renames_only (& mut self , b : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_BREAK_REWRITES_FOR_RENAMES_ONLY , b) } # [doc = " Remove any unmodified deltas after find_similar is done."] # [doc = ""] # [doc = " Using `copies_from_unmodified` to emulate the `--find-copies-harder`"] # [doc = " behavior requires building a diff with the `include_unmodified` flag. If"] # [doc = " you do not want unmodified records in the final result, pas this flag to"] # [doc = " have them removed."] pub fn remove_unmodified (& mut self , remove : bool) -> & mut DiffFindOptions { self . flag (raw :: GIT_DIFF_FIND_REMOVE_UNMODIFIED , remove) } # [doc = " Similarity to consider a file renamed (default 50)"] pub fn rename_threshold (& mut self , thresh : u16) -> & mut DiffFindOptions { self . raw . rename_threshold = thresh ; self } # [doc = " Similarity of modified to be eligible rename source (default 50)"] pub fn rename_from_rewrite_threshold (& mut self , thresh : u16) -> & mut DiffFindOptions { self . raw . rename_from_rewrite_threshold = thresh ; self } # [doc = " Similarity to consider a file copy (default 50)"] pub fn copy_threshold (& mut self , thresh : u16) -> & mut DiffFindOptions { self . raw . copy_threshold = thresh ; self } # [doc = " Similarity to split modify into delete/add pair (default 60)"] pub fn break_rewrite_threshold (& mut self , thresh : u16) -> & mut DiffFindOptions { self . raw . break_rewrite_threshold = thresh ; self } # [doc = " Maximum similarity sources to examine for a file (somewhat like"] # [doc = " git-diff's `-l` option or `diff.renameLimit` config)"] # [doc = ""] # [doc = " Defaults to 200"] pub fn rename_limit (& mut self , limit : usize) -> & mut DiffFindOptions { self . raw . rename_limit = limit as size_t ; self } # [doc = " Acquire a pointer to the underlying raw options."] pub unsafe fn raw (& mut self) -> * const raw :: git_diff_find_options { & self . raw } }
};
}
