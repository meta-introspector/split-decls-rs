// Generated macro for net (module)
macro_rules! Depcratenet {
() => {
// Module: crate
// Provides: {"net"}
// Dependencies: {}
pub mod net { # ! [doc = " TCP/UDP/Unix bindings (mostly Tokio re-exports)."] use std :: { future :: Future , io , pin :: pin , task :: { Context , Poll } , } ; use tokio :: io :: { AsyncRead , AsyncWrite , BufReader , Interest } ; # [cfg (unix)] pub use tokio :: net :: { UnixDatagram , UnixListener , UnixStream } ; pub use tokio :: { io :: Ready , net :: { TcpListener , TcpSocket , TcpStream , UdpSocket } , } ; # [doc = " Extension trait over async read+write types that can also signal readiness."] # [doc (hidden)] pub trait ActixStream : AsyncRead + AsyncWrite + Unpin { # [doc = " Poll stream and check read readiness of Self."] # [doc = ""] # [doc = " See [tokio::net::TcpStream::poll_read_ready] for detail on intended use."] fn poll_read_ready (& self , cx : & mut Context < '_ >) -> Poll < io :: Result < Ready > > ; # [doc = " Poll stream and check write readiness of Self."] # [doc = ""] # [doc = " See [tokio::net::TcpStream::poll_write_ready] for detail on intended use."] fn poll_write_ready (& self , cx : & mut Context < '_ >) -> Poll < io :: Result < Ready > > ; } impl ActixStream for TcpStream { fn poll_read_ready (& self , cx : & mut Context < '_ >) -> Poll < io :: Result < Ready > > { let ready = self . ready (Interest :: READABLE) ; pin ! (ready) . poll (cx) } fn poll_write_ready (& self , cx : & mut Context < '_ >) -> Poll < io :: Result < Ready > > { let ready = self . ready (Interest :: WRITABLE) ; pin ! (ready) . poll (cx) } } # [cfg (unix)] impl ActixStream for UnixStream { fn poll_read_ready (& self , cx : & mut Context < '_ >) -> Poll < io :: Result < Ready > > { let ready = self . ready (Interest :: READABLE) ; pin ! (ready) . poll (cx) } fn poll_write_ready (& self , cx : & mut Context < '_ >) -> Poll < io :: Result < Ready > > { let ready = self . ready (Interest :: WRITABLE) ; pin ! (ready) . poll (cx) } } impl < Io : ActixStream + ? Sized > ActixStream for Box < Io > { fn poll_read_ready (& self , cx : & mut Context < '_ >) -> Poll < io :: Result < Ready > > { (* * self) . poll_read_ready (cx) } fn poll_write_ready (& self , cx : & mut Context < '_ >) -> Poll < io :: Result < Ready > > { (* * self) . poll_write_ready (cx) } } impl < Io : ActixStream > ActixStream for BufReader < Io > { fn poll_read_ready (& self , cx : & mut Context < '_ >) -> Poll < io :: Result < Ready > > { self . get_ref () . poll_read_ready (cx) } fn poll_write_ready (& self , cx : & mut Context < '_ >) -> Poll < io :: Result < Ready > > { self . get_ref () . poll_write_ready (cx) } } }
};
}
