// Generated macro for template (function)
macro_rules! Depcratetemplate {
() => {
// Module: crate
// Provides: {"template"}
// Dependencies: {}
# [doc = " Define a template where the name is given from the function name. This attribute register all"] # [doc = " attributes. The function signature don't really mater but to make it clear is better that you"] # [doc = " use a signature like if you're wrinting a standard `rstest`."] # [doc = ""] # [doc = " If you need to export the template at the root of your crate or use it from another crate you"] # [doc = " should annotate it with `#[export]` attribute. This attribute add `#[macro_export]` attribute to"] # [doc = " the template macro and make possible to use it from another crate."] # [doc = ""] # [doc = " When define a template you can also set the arguments attributes like `#[case]`, `#[values]`"] # [doc = " and `#[with]`: when you apply it attributes will be copied to the matched by name arguments."] # [doc = ""] # [proc_macro_attribute] pub fn template (_args : proc_macro :: TokenStream , input : proc_macro :: TokenStream) -> TokenStream { let mut template : ItemFn = parse (input) . unwrap () ; let rstest_index = template . attrs . iter () . position (| attr | attr . path () . is_ident (& format_ident ! ("rstest"))) ; let mut attributes = template . attrs ; template . attrs = match rstest_index { Some (idx) => attributes . split_off (idx) , None => std :: mem :: take (& mut attributes) , } ; let (macro_attribute , visibility) = match get_export (& attributes) { Some (_) => (quote ! { # [macro_export] } , quote ! { pub } ,) , None => (quote ! { } , quote ! { pub (crate) } ,) , } ; let macro_name = template . sig . ident . clone () ; let macro_name_rand = format_ident ! ("{}_{}" , macro_name , rand :: random ::< u64 > ()) ; let tokens = quote ! { # [doc = " Apply #macro_name template to given body"] # macro_attribute macro_rules ! # macro_name_rand { ($ test : item) => { :: rstest_reuse :: merge_attrs ! { # template , $ test } } } # [allow (unused_imports)] # visibility use # macro_name_rand as # macro_name ; } ; tokens . into () }
};
}
