macro_rules! Barrier {
    () => {
        # [derive (Debug)] # [doc = " `std::sync::Barrier` is not supported yet in Loom. This stub is provided just"] # [doc = " to make the code to compile."] pub struct Barrier { }
    };
}

Barrier!();