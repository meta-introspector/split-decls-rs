// Generated macro for impl_44 (impl)
macro_rules! Depcrate_rt_arcimpl_44 {
() => {
// Module: crate::rt::arc
// Provides: {"impl_44"}
// Dependencies: {}
impl Arc { pub (crate) fn new (location : Location) -> Arc { rt :: execution (| execution | { let state = execution . objects . insert (State { ref_cnt : 1 , allocated : location , synchronize : Synchronize :: new () , last_ref_inc : None , last_ref_dec : None , last_ref_inspect : None , last_ref_modification : None , }) ; trace ! (? state , % location , "Arc::new") ; Arc { state } }) } pub (crate) fn ref_inc (& self , location : Location) { self . branch (Action :: RefInc , location) ; rt :: execution (| execution | { let state = self . state . get_mut (& mut execution . objects) ; state . ref_cnt = state . ref_cnt . checked_add (1) . expect ("overflow") ; trace ! (state = ? self . state , ref_cnt = ? state . ref_cnt , % location , "Arc::ref_inc") ; }) } # [doc = " Validate a `get_mut` call"] pub (crate) fn get_mut (& self , location : Location) -> bool { self . branch (Action :: RefDec , location) ; rt :: execution (| execution | { let state = self . state . get_mut (& mut execution . objects) ; assert ! (state . ref_cnt >= 1 , "Arc is released") ; state . synchronize . sync_load (& mut execution . threads , Acquire) ; let is_only_ref = state . ref_cnt == 1 ; trace ! (state = ? self . state , ? is_only_ref , % location , "Arc::get_mut") ; is_only_ref }) } # [doc = " Returns true if the memory should be dropped."] pub (crate) fn ref_dec (& self , location : Location) -> bool { self . branch (Action :: RefDec , location) ; rt :: execution (| execution | { let state = self . state . get_mut (& mut execution . objects) ; assert ! (state . ref_cnt >= 1 , "Arc is already released") ; state . ref_cnt -= 1 ; trace ! (state = ? self . state , ref_cnt = ? state . ref_cnt , % location , "Arc::ref_dec") ; state . synchronize . sync_store (& mut execution . threads , Release) ; if state . ref_cnt == 0 { state . synchronize . sync_load (& mut execution . threads , Acquire) ; true } else { false } }) } # [track_caller] pub (crate) fn strong_count (& self) -> usize { self . branch (Action :: Inspect , location ! ()) ; rt :: execution (| execution | { let state = self . state . get_mut (& mut execution . objects) ; assert ! (state . ref_cnt > 0 , "Arc is already released") ; state . synchronize . sync_load (& mut execution . threads , SeqCst) ; state . ref_cnt }) } fn branch (& self , action : Action , location : Location) { let r = self . state ; r . branch_action (action , location) ; assert ! (r . ref_eq (self . state) , "Internal state mutated during branch. This is \
                usually due to a bug in the algorithm being tested writing in \
                an invalid memory location.") ; } }
};
}
