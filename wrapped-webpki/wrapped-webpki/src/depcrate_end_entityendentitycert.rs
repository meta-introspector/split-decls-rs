// Generated macro for EndEntityCert (struct)
macro_rules! Depcrate_end_entityEndEntityCert {
() => {
// Module: crate::end_entity
// Provides: {"EndEntityCert"}
// Dependencies: {}
# [doc = " An end-entity certificate."] # [doc = ""] # [doc = " Server certificate processing in a TLS connection consists of several"] # [doc = " steps. All of these steps are necessary:"] # [doc = ""] # [doc = " * [`EndEntityCert::verify_for_usage()`]: Verify that the peer's certificate"] # [doc = "   is valid for the current usage scenario. For server authentication, use"] # [doc = "   [`crate::ExtendedKeyUsage::server_auth()`]."] # [doc = " * [`EndEntityCert::verify_is_valid_for_subject_name()`]: Verify that the server's"] # [doc = "   certificate is valid for the host or IP address that is being connected to."] # [doc = " * [`EndEntityCert::verify_signature()`]: Verify that the signature of server's"] # [doc = "   `ServerKeyExchange` message is valid for the server's certificate."] # [doc = ""] # [doc = " Client certificate processing in a TLS connection consists of analogous"] # [doc = " steps. All of these steps are necessary:"] # [doc = ""] # [doc = " * [`EndEntityCert::verify_for_usage()`]: Verify that the peer's certificate"] # [doc = "   is valid for the current usage scenario. For client authentication, use"] # [doc = "   [`crate::ExtendedKeyUsage::client_auth()`]."] # [doc = " * [`EndEntityCert::verify_signature()`]: Verify that the signature of client's"] # [doc = "   `CertificateVerify` message is valid using the public key from the"] # [doc = "   client's certificate."] # [doc = ""] # [doc = " Although it would be less error-prone to combine all these steps into a"] # [doc = " single function call, some significant optimizations are possible if the"] # [doc = " three steps are processed separately (in parallel). It does not matter much"] # [doc = " which order the steps are done in, but **all of these steps must completed"] # [doc = " before application data is sent and before received application data is"] # [doc = " processed**. The [`TryFrom`] conversion from `&CertificateDer<'_>` is an"] # [doc = " inexpensive operation and is deterministic, so if these tasks are done in"] # [doc = " multiple threads, it is probably best to just create multiple [`EndEntityCert`]"] # [doc = " instances for the same DER-encoded ASN.1 certificate bytes."] pub struct EndEntityCert < 'a > { inner : cert :: Cert < 'a > , }
};
}
