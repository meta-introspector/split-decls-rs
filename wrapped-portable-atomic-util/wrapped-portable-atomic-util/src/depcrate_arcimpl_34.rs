// Generated macro for impl_34 (impl)
macro_rules! Depcrate_arcimpl_34 {
() => {
// Module: crate::arc
// Provides: {"impl_34"}
// Dependencies: {}
impl < T : ? Sized > Arc < T > { # [doc = " Consumes the `Arc`, returning the wrapped pointer."] # [doc = ""] # [doc = " To avoid a memory leak the pointer must be converted back to an `Arc` using"] # [doc = " [`Arc::from_raw`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = ""] # [doc = " let x = Arc::new(\"hello\".to_owned());"] # [doc = " let x_ptr = Arc::into_raw(x);"] # [doc = " assert_eq!(unsafe { &*x_ptr }, \"hello\");"] # [doc = " # // Prevent leaks for Miri."] # [doc = " # drop(unsafe { Arc::from_raw(x_ptr) });"] # [doc = " ```"] # [must_use = "losing the pointer will leak memory"] pub fn into_raw (this : Self) -> * const T { let this = ManuallyDrop :: new (this) ; Self :: as_ptr (& * this) } # [doc = " Provides a raw pointer to the data."] # [doc = ""] # [doc = " The counts are not affected in any way and the `Arc` is not consumed. The pointer is valid for"] # [doc = " as long as there are strong counts in the `Arc`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = ""] # [doc = " let x = Arc::new(\"hello\".to_owned());"] # [doc = " let y = Arc::clone(&x);"] # [doc = " let x_ptr = Arc::as_ptr(&x);"] # [doc = " assert_eq!(x_ptr, Arc::as_ptr(&y));"] # [doc = " assert_eq!(unsafe { &*x_ptr }, \"hello\");"] # [doc = " ```"] # [must_use] pub fn as_ptr (this : & Self) -> * const T { let ptr : * mut ArcInner < T > = this . ptr . as_ptr () ; unsafe { data_ptr :: < T > (ptr , & * * this) } } # [doc = " Creates a new [`Weak`] pointer to this allocation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = ""] # [doc = " let five = Arc::new(5);"] # [doc = ""] # [doc = " let weak_five = Arc::downgrade(&five);"] # [doc = " ```"] # [must_use = "this returns a new `Weak` pointer, \
                  without modifying the original `Arc`"] # [allow (clippy :: missing_panics_doc)] pub fn downgrade (this : & Self) -> Weak < T > { let mut cur = this . inner () . weak . load (Relaxed) ; loop { if cur == usize :: MAX { hint :: spin_loop () ; cur = this . inner () . weak . load (Relaxed) ; continue ; } assert ! (cur <= MAX_REFCOUNT , "{}" , INTERNAL_OVERFLOW_ERROR) ; match this . inner () . weak . compare_exchange_weak (cur , cur + 1 , Acquire , Relaxed) { Ok (_) => { debug_assert ! (! is_dangling (this . ptr . as_ptr ())) ; return Weak { ptr : this . ptr } ; } Err (old) => cur = old , } } } # [doc = " Gets the number of [`Weak`] pointers to this allocation."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method by itself is safe, but using it correctly requires extra care."] # [doc = " Another thread can change the weak count at any time,"] # [doc = " including potentially between calling this method and acting on the result."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = ""] # [doc = " let five = Arc::new(5);"] # [doc = " let _weak_five = Arc::downgrade(&five);"] # [doc = ""] # [doc = " // This assertion is deterministic because we haven't shared"] # [doc = " // the `Arc` or `Weak` between threads."] # [doc = " assert_eq!(1, Arc::weak_count(&five));"] # [doc = " ```"] # [inline] # [must_use] pub fn weak_count (this : & Self) -> usize { let cnt = this . inner () . weak . load (Relaxed) ; if cnt == usize :: MAX { 0 } else { cnt - 1 } } # [doc = " Gets the number of strong (`Arc`) pointers to this allocation."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method by itself is safe, but using it correctly requires extra care."] # [doc = " Another thread can change the strong count at any time,"] # [doc = " including potentially between calling this method and acting on the result."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = ""] # [doc = " let five = Arc::new(5);"] # [doc = " let _also_five = Arc::clone(&five);"] # [doc = ""] # [doc = " // This assertion is deterministic because we haven't shared"] # [doc = " // the `Arc` between threads."] # [doc = " assert_eq!(2, Arc::strong_count(&five));"] # [doc = " ```"] # [inline] # [must_use] pub fn strong_count (this : & Self) -> usize { this . inner () . strong . load (Relaxed) } # [inline] fn inner (& self) -> & ArcInner < T > { unsafe { self . ptr . as_ref () } } # [inline (never)] unsafe fn drop_slow (& mut self) { unsafe { ptr :: drop_in_place (Self :: get_mut_unchecked (self)) } drop (Weak { ptr : self . ptr }) ; } # [doc = " Returns `true` if the two `Arc`s point to the same allocation in a vein similar to"] # [doc = " [`ptr::eq`]. This function ignores the metadata of  `dyn Trait` pointers."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = ""] # [doc = " let five = Arc::new(5);"] # [doc = " let same_five = Arc::clone(&five);"] # [doc = " let other_five = Arc::new(5);"] # [doc = ""] # [doc = " assert!(Arc::ptr_eq(&five, &same_five));"] # [doc = " assert!(!Arc::ptr_eq(&five, &other_five));"] # [doc = " ```"] # [doc = ""] # [doc = " [`ptr::eq`]: core::ptr::eq \"ptr::eq\""] # [inline] # [must_use] pub fn ptr_eq (this : & Self , other : & Self) -> bool { ptr :: eq (this . ptr . as_ptr () as * const () , other . ptr . as_ptr () as * const ()) } }
};
}
