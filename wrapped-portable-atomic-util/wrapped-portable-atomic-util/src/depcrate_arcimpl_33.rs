// Generated macro for impl_33 (impl)
macro_rules! Depcrate_arcimpl_33 {
() => {
// Module: crate::arc
// Provides: {"impl_33"}
// Dependencies: {}
impl < T : ? Sized > Arc < T > { # [doc = " Constructs an `Arc<T>` from a raw pointer."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The raw pointer must have been previously returned by a call to"] # [doc = " [`Arc<U>::into_raw`][into_raw] with the following requirements:"] # [doc = ""] # [doc = " * If `U` is sized, it must have the same size and alignment as `T`. This"] # [doc = "   is trivially true if `U` is `T`."] # [doc = " * If `U` is unsized, its data pointer must have the same size and"] # [doc = "   alignment as `T`. This is trivially true if `Arc<U>` was constructed"] # [doc = "   through `Arc<T>` and then converted to `Arc<U>` through an [unsized"] # [doc = "   coercion]."] # [doc = ""] # [doc = " Note that if `U` or `U`'s data pointer is not `T` but has the same size"] # [doc = " and alignment, this is basically like transmuting references of"] # [doc = " different types. See [`mem::transmute`] for more information"] # [doc = " on what restrictions apply in this case."] # [doc = ""] # [doc = " The user of `from_raw` has to make sure a specific value of `T` is only"] # [doc = " dropped once."] # [doc = ""] # [doc = " This function is unsafe because improper use may lead to memory unsafety,"] # [doc = " even if the returned `Arc<T>` is never accessed."] # [doc = ""] # [doc = " [into_raw]: Arc::into_raw"] # [doc = " [unsized coercion]: https://doc.rust-lang.org/reference/type-coercions.html#unsized-coercions"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = ""] # [doc = " let x = Arc::new(\"hello\".to_owned());"] # [doc = " let x_ptr = Arc::into_raw(x);"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     // Convert back to an `Arc` to prevent leak."] # [doc = "     let x = Arc::from_raw(x_ptr);"] # [doc = "     assert_eq!(&*x, \"hello\");"] # [doc = ""] # [doc = "     // Further calls to `Arc::from_raw(x_ptr)` would be memory-unsafe."] # [doc = " }"] # [doc = ""] # [doc = " // The memory was freed when `x` went out of scope above, so `x_ptr` is now dangling!"] # [doc = " ```"] # [doc = ""] # [doc = " Convert a slice back into its original array:"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = ""] # [doc = " let x: Arc<[u32]> = Arc::from([1, 2, 3]);"] # [doc = " let x_ptr: *const [u32] = Arc::into_raw(x);"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     let x: Arc<[u32; 3]> = Arc::from_raw(x_ptr.cast::<[u32; 3]>());"] # [doc = "     assert_eq!(&*x, &[1, 2, 3]);"] # [doc = " }"] # [doc = " ```"] # [inline] pub unsafe fn from_raw (ptr : * const T) -> Self { unsafe { let offset = data_offset :: < T > (& * ptr) ; let arc_ptr = strict :: byte_sub (ptr as * mut T , offset) as * mut ArcInner < T > ; Self :: from_ptr (arc_ptr) } } # [doc = " Increments the strong reference count on the `Arc<T>` associated with the"] # [doc = " provided pointer by one."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The pointer must have been obtained through `Arc::into_raw`, and the"] # [doc = " associated `Arc` instance must be valid (i.e. the strong count must be at"] # [doc = " least 1) for the duration of this method."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = ""] # [doc = " let five = Arc::new(5);"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     let ptr = Arc::into_raw(five);"] # [doc = "     Arc::increment_strong_count(ptr);"] # [doc = ""] # [doc = "     // This assertion is deterministic because we haven't shared"] # [doc = "     // the `Arc` between threads."] # [doc = "     let five = Arc::from_raw(ptr);"] # [doc = "     assert_eq!(2, Arc::strong_count(&five));"] # [doc = " #   // Prevent leaks for Miri."] # [doc = " #   Arc::decrement_strong_count(ptr);"] # [doc = " }"] # [doc = " ```"] # [inline] pub unsafe fn increment_strong_count (ptr : * const T) { let arc = unsafe { mem :: ManuallyDrop :: new (Self :: from_raw (ptr)) } ; let _arc_clone : mem :: ManuallyDrop < _ > = arc . clone () ; } # [doc = " Decrements the strong reference count on the `Arc<T>` associated with the"] # [doc = " provided pointer by one."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The pointer must have been obtained through `Arc::into_raw`, and the"] # [doc = " associated `Arc` instance must be valid (i.e. the strong count must be at"] # [doc = " least 1) when invoking this method. This method can be used to release the final"] # [doc = " `Arc` and backing storage, but **should not** be called after the final `Arc` has been"] # [doc = " released."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = ""] # [doc = " let five = Arc::new(5);"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     let ptr = Arc::into_raw(five);"] # [doc = "     Arc::increment_strong_count(ptr);"] # [doc = ""] # [doc = "     // Those assertions are deterministic because we haven't shared"] # [doc = "     // the `Arc` between threads."] # [doc = "     let five = Arc::from_raw(ptr);"] # [doc = "     assert_eq!(2, Arc::strong_count(&five));"] # [doc = "     Arc::decrement_strong_count(ptr);"] # [doc = "     assert_eq!(1, Arc::strong_count(&five));"] # [doc = " }"] # [doc = " ```"] # [inline] pub unsafe fn decrement_strong_count (ptr : * const T) { unsafe { drop (Self :: from_raw (ptr)) } } }
};
}
