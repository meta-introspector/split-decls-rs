// Generated macro for impl_47 (impl)
macro_rules! Depcrate_arcimpl_47 {
() => {
// Module: crate::arc
// Provides: {"impl_47"}
// Dependencies: {}
impl < T > Weak < T > { # [doc = " Converts a raw pointer previously created by [`into_raw`] back into `Weak<T>`."] # [doc = ""] # [doc = " This can be used to safely get a strong reference (by calling [`upgrade`]"] # [doc = " later) or to deallocate the weak count by dropping the `Weak<T>`."] # [doc = ""] # [doc = " It takes ownership of one weak reference (with the exception of pointers created by [`new`],"] # [doc = " as these don't own anything; the method still works on them)."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The pointer must have originated from the [`into_raw`] and must still own its potential"] # [doc = " weak reference."] # [doc = ""] # [doc = " It is allowed for the strong count to be 0 at the time of calling this. Nevertheless, this"] # [doc = " takes ownership of one weak reference currently represented as a raw pointer (the weak"] # [doc = " count is not modified by this operation) and therefore it must be paired with a previous"] # [doc = " call to [`into_raw`]."] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::{Arc, Weak};"] # [doc = ""] # [doc = " let strong = Arc::new(\"hello\".to_owned());"] # [doc = ""] # [doc = " let raw_1 = Arc::downgrade(&strong).into_raw();"] # [doc = " let raw_2 = Arc::downgrade(&strong).into_raw();"] # [doc = ""] # [doc = " assert_eq!(2, Arc::weak_count(&strong));"] # [doc = ""] # [doc = " assert_eq!(\"hello\", &*unsafe { Weak::from_raw(raw_1) }.upgrade().unwrap());"] # [doc = " assert_eq!(1, Arc::weak_count(&strong));"] # [doc = ""] # [doc = " drop(strong);"] # [doc = ""] # [doc = " // Decrement the last weak count."] # [doc = " assert!(unsafe { Weak::from_raw(raw_2) }.upgrade().is_none());"] # [doc = " ```"] # [doc = ""] # [doc = " [`new`]: Weak::new"] # [doc = " [`into_raw`]: Weak::into_raw"] # [doc = " [`upgrade`]: Weak::upgrade"] # [inline] pub unsafe fn from_raw (ptr : * const T) -> Self { let ptr = if is_dangling (ptr) { ptr as * mut ArcInner < T > } else { let offset = data_offset_align (mem :: align_of :: < T > ()) ; unsafe { strict :: byte_sub (ptr as * mut T , offset) as * mut ArcInner < T > } } ; Weak { ptr : unsafe { NonNull :: new_unchecked (ptr) } } } }
};
}
