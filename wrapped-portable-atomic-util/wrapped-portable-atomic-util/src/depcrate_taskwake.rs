// Generated macro for Wake (trait)
macro_rules! Depcrate_taskWake {
() => {
// Module: crate::task
// Provides: {"Wake"}
// Dependencies: {}
# [doc = " The implementation of waking a task on an executor."] # [doc = ""] # [doc = " This is an equivalent to [`std::task::Wake`], but using [`portable_atomic_util::Arc`](crate::Arc)"] # [doc = " as a reference-counted pointer. See the documentation for [`std::task::Wake`] for more details."] # [doc = ""] # [doc = " **Note:** Unlike `std::task::Wake`, all methods take `this:` instead of `self:`."] # [doc = " This is because using `portable_atomic_util::Arc` as a receiver requires the"] # [doc = " [unstable `arbitrary_self_types` feature](https://github.com/rust-lang/rust/issues/44874)."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " A basic `block_on` function that takes a future and runs it to completion on"] # [doc = " the current thread."] # [doc = ""] # [doc = " **Note:** This example trades correctness for simplicity. In order to prevent"] # [doc = " deadlocks, production-grade implementations will also need to handle"] # [doc = " intermediate calls to `thread::unpark` as well as nested invocations."] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::{Arc, task::Wake};"] # [doc = " use std::{"] # [doc = "     future::Future,"] # [doc = "     task::{Context, Poll},"] # [doc = "     thread::{self, Thread},"] # [doc = " };"] # [doc = ""] # [doc = " /// A waker that wakes up the current thread when called."] # [doc = " struct ThreadWaker(Thread);"] # [doc = ""] # [doc = " impl Wake for ThreadWaker {"] # [doc = "     fn wake(this: Arc<Self>) {"] # [doc = "         this.0.unpark();"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " /// Run a future to completion on the current thread."] # [doc = " fn block_on<T>(fut: impl Future<Output = T>) -> T {"] # [doc = "     // Pin the future so it can be polled."] # [doc = "     let mut fut = Box::pin(fut);"] # [doc = ""] # [doc = "     // Create a new context to be passed to the future."] # [doc = "     let t = thread::current();"] # [doc = "     let waker = Arc::new(ThreadWaker(t)).into();"] # [doc = "     let mut cx = Context::from_waker(&waker);"] # [doc = ""] # [doc = "     // Run the future to completion."] # [doc = "     loop {"] # [doc = "         match fut.as_mut().poll(&mut cx) {"] # [doc = "             Poll::Ready(res) => return res,"] # [doc = "             Poll::Pending => thread::park(),"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " block_on(async {"] # [doc = "     println!(\"Hi from inside a future!\");"] # [doc = " });"] # [doc = " ```"] pub trait Wake { # [doc = " Wake this task."] fn wake (this : Arc < Self >) ; # [doc = " Wake this task without consuming the waker."] # [doc = ""] # [doc = " If an executor supports a cheaper way to wake without consuming the"] # [doc = " waker, it should override this method. By default, it clones the"] # [doc = " [`Arc`] and calls [`wake`] on the clone."] # [doc = ""] # [doc = " [`wake`]: Wake::wake"] fn wake_by_ref (this : & Arc < Self >) { Self :: wake (this . clone ()) ; } }
};
}
