// Generated macro for impl_76 (impl)
macro_rules! Depcrate_arcimpl_76 {
() => {
// Module: crate::arc
// Provides: {"impl_76"}
// Dependencies: {}
# [cfg (not (portable_atomic_no_alloc_layout_extras))] impl < T > core :: iter :: FromIterator < T > for Arc < [T] > { # [doc = " Takes each element in the `Iterator` and collects it into an `Arc<[T]>`."] # [doc = ""] # [doc = " # Performance characteristics"] # [doc = ""] # [doc = " ## The general case"] # [doc = ""] # [doc = " In the general case, collecting into `Arc<[T]>` is done by first"] # [doc = " collecting into a `Vec<T>`. That is, when writing the following:"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = " let evens: Arc<[u8]> = (0..10).filter(|&x| x % 2 == 0).collect();"] # [doc = " # assert_eq!(&*evens, &[0, 2, 4, 6, 8]);"] # [doc = " ```"] # [doc = ""] # [doc = " this behaves as if we wrote:"] # [doc = ""] # [doc = " ```"] # [doc = " # #![cfg_attr(rustfmt, rustfmt::skip)] // rustfmt bug that inserts new line before \"# ...\" when the previous line ends with a comment."] # [doc = " use portable_atomic_util::Arc;"] # [doc = " let evens: Arc<[u8]> = (0..10).filter(|&x| x % 2 == 0)"] # [doc = "     .collect::<Vec<_>>() // The first set of allocations happens here."] # [doc = "     .into(); // A second allocation for `Arc<[T]>` happens here."] # [doc = " # assert_eq!(&*evens, &[0, 2, 4, 6, 8]);"] # [doc = " ```"] # [doc = ""] # [doc = " This will allocate as many times as needed for constructing the `Vec<T>`"] # [doc = " and then it will allocate once for turning the `Vec<T>` into the `Arc<[T]>`."] # [doc = ""] # [doc = " ## Iterators of known length"] # [doc = ""] # [doc = " When your `Iterator` implements `TrustedLen` and is of an exact size,"] # [doc = " a single allocation will be made for the `Arc<[T]>`. For example:"] # [doc = ""] # [doc = " ```"] # [doc = " # #![cfg_attr(rustfmt, rustfmt::skip)] // rustfmt bug that inserts new line before \"# ...\" when the previous line ends with a comment."] # [doc = " use portable_atomic_util::Arc;"] # [doc = " let evens: Arc<[u8]> = (0..10).collect(); // Just a single allocation happens here."] # [doc = " # assert_eq!(&*evens, &*(0..10).collect::<Vec<_>>());"] # [doc = " ```"] fn from_iter < I : IntoIterator < Item = T > > (iter : I) -> Self { iter . into_iter () . collect :: < Vec < T > > () . into () } }
};
}
