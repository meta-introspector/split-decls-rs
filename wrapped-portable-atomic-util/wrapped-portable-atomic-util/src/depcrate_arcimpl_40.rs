// Generated macro for impl_40 (impl)
macro_rules! Depcrate_arcimpl_40 {
() => {
// Module: crate::arc
// Provides: {"impl_40"}
// Dependencies: {}
impl < T : ? Sized + CloneToUninit > Arc < T > { # [doc = " Makes a mutable reference into the given `Arc`."] # [doc = ""] # [doc = " If there are other `Arc` pointers to the same allocation, then `make_mut` will"] # [doc = " [`clone`] the inner value to a new allocation to ensure unique ownership.  This is also"] # [doc = " referred to as clone-on-write."] # [doc = ""] # [doc = " However, if there are no other `Arc` pointers to this allocation, but some [`Weak`]"] # [doc = " pointers, then the [`Weak`] pointers will be dissociated and the inner value will not"] # [doc = " be cloned."] # [doc = ""] # [doc = " See also [`get_mut`], which will fail rather than cloning the inner value"] # [doc = " or dissociating [`Weak`] pointers."] # [doc = ""] # [doc = " [`clone`]: Clone::clone"] # [doc = " [`get_mut`]: Arc::get_mut"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = ""] # [doc = " let mut data = Arc::new(5);"] # [doc = ""] # [doc = " *Arc::make_mut(&mut data) += 1; // Won't clone anything"] # [doc = " let mut other_data = Arc::clone(&data); // Won't clone inner data"] # [doc = " *Arc::make_mut(&mut data) += 1; // Clones inner data"] # [doc = " *Arc::make_mut(&mut data) += 1; // Won't clone anything"] # [doc = " *Arc::make_mut(&mut other_data) *= 2; // Won't clone anything"] # [doc = ""] # [doc = " // Now `data` and `other_data` point to different allocations."] # [doc = " assert_eq!(*data, 8);"] # [doc = " assert_eq!(*other_data, 12);"] # [doc = " ```"] # [doc = ""] # [doc = " [`Weak`] pointers will be dissociated:"] # [doc = ""] # [doc = " ```"] # [doc = " use portable_atomic_util::Arc;"] # [doc = ""] # [doc = " let mut data = Arc::new(75);"] # [doc = " let weak = Arc::downgrade(&data);"] # [doc = ""] # [doc = " assert!(75 == *data);"] # [doc = " assert!(75 == *weak.upgrade().unwrap());"] # [doc = ""] # [doc = " *Arc::make_mut(&mut data) += 1;"] # [doc = ""] # [doc = " assert!(76 == *data);"] # [doc = " assert!(weak.upgrade().is_none());"] # [doc = " ```"] # [inline] pub fn make_mut (this : & mut Self) -> & mut T { let size_of_val = mem :: size_of_val :: < T > (& * * this) ; if this . inner () . strong . compare_exchange (1 , 0 , Acquire , Relaxed) . is_err () { let this_data_ref : & T = this ; let mut in_progress : UniqueArcUninit < T > = UniqueArcUninit :: new (this_data_ref) ; let initialized_clone = unsafe { this_data_ref . clone_to_uninit (in_progress . data_ptr ()) ; in_progress . into_arc () } ; * this = initialized_clone ; } else if this . inner () . weak . load (Relaxed) != 1 { let _weak = Weak { ptr : this . ptr } ; let mut in_progress : UniqueArcUninit < T > = UniqueArcUninit :: new (& * * this) ; unsafe { ptr :: copy_nonoverlapping (& * * this as * const T as * const u8 , in_progress . data_ptr () as * mut u8 , size_of_val ,) ; ptr :: write (this , in_progress . into_arc ()) ; } } else { this . inner () . strong . store (1 , Release) ; } unsafe { Self :: get_mut_unchecked (this) } } }
};
}
