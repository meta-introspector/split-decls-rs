macro_rules! R_386_TLS_DESC {
    () => {
        # [doc = " TLS descriptor containing pointer to code and to argument, returning the TLS offset for the symbol."] pub const R_386_TLS_DESC : u32 = 41 ;
    };
}

R_386_TLS_DESC!();