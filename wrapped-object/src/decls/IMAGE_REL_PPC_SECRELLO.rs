macro_rules! IMAGE_REL_PPC_SECRELLO {
    () => {
        # [doc = " Low 16-bit section relative reference (used for >32k TLS)"] pub const IMAGE_REL_PPC_SECRELLO : u16 = 0x0013 ;
    };
}

IMAGE_REL_PPC_SECRELLO!();