// Generated macro for ProgramHeader (trait)
macro_rules! Depcrate_read_elf_segmentProgramHeader {
() => {
// Module: crate::read::elf::segment
// Provides: {"ProgramHeader"}
// Dependencies: {}
# [doc = " A trait for generic access to [`elf::ProgramHeader32`] and [`elf::ProgramHeader64`]."] # [allow (missing_docs)] pub trait ProgramHeader : Debug + Pod { type Elf : FileHeader < ProgramHeader = Self , Endian = Self :: Endian , Word = Self :: Word > ; type Word : Into < u64 > ; type Endian : endian :: Endian ; fn p_type (& self , endian : Self :: Endian) -> u32 ; fn p_flags (& self , endian : Self :: Endian) -> u32 ; fn p_offset (& self , endian : Self :: Endian) -> Self :: Word ; fn p_vaddr (& self , endian : Self :: Endian) -> Self :: Word ; fn p_paddr (& self , endian : Self :: Endian) -> Self :: Word ; fn p_filesz (& self , endian : Self :: Endian) -> Self :: Word ; fn p_memsz (& self , endian : Self :: Endian) -> Self :: Word ; fn p_align (& self , endian : Self :: Endian) -> Self :: Word ; # [doc = " Return the offset and size of the segment in the file."] fn file_range (& self , endian : Self :: Endian) -> (u64 , u64) { (self . p_offset (endian) . into () , self . p_filesz (endian) . into ()) } # [doc = " Return the segment data."] # [doc = ""] # [doc = " Returns `Err` for invalid values."] fn data < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> Result < & 'data [u8] , () > { let (offset , size) = self . file_range (endian) ; data . read_bytes_at (offset , size) } # [doc = " Return the segment data as a slice of the given type."] # [doc = ""] # [doc = " Allows padding at the end of the data."] # [doc = " Returns `Ok(&[])` if the segment has no data."] # [doc = " Returns `Err` for invalid values, including bad alignment."] fn data_as_array < 'data , T : Pod , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> Result < & 'data [T] , () > { pod :: slice_from_all_bytes (self . data (endian , data) ?) } # [doc = " Return the segment data in the given virtual address range"] # [doc = ""] # [doc = " Returns `Ok(None)` if the segment does not contain the address."] # [doc = " Returns `Err` for invalid values."] fn data_range < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R , address : u64 , size : u64 ,) -> Result < Option < & 'data [u8] > , () > { Ok (read :: util :: data_range (self . data (endian , data) ? , self . p_vaddr (endian) . into () , address , size ,)) } # [doc = " Return entries in a dynamic segment."] # [doc = ""] # [doc = " Returns `Ok(None)` if the segment is not `PT_DYNAMIC`."] # [doc = " Returns `Err` for invalid values."] fn dynamic < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < & 'data [< Self :: Elf as FileHeader > :: Dyn] > > { if self . p_type (endian) != elf :: PT_DYNAMIC { return Ok (None) ; } let dynamic = self . data_as_array (endian , data) . read_error ("Invalid ELF dynamic segment offset or size") ? ; Ok (Some (dynamic)) } # [doc = " Return the data in an interpreter segment."] # [doc = ""] # [doc = " Returns `Ok(None)` if the segment is not `PT_INTERP`."] # [doc = " Returns `Err` for invalid values."] fn interpreter < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < & 'data [u8] > > { if self . p_type (endian) != elf :: PT_INTERP { return Ok (None) ; } let data = self . data (endian , data) . read_error ("Invalid ELF interpreter segment offset or size") ? ; let len = data . iter () . position (| & b | b == 0) . read_error ("Invalid ELF interpreter segment data") ? ; Ok (Some (& data [.. len])) } # [doc = " Return a note iterator for the segment data."] # [doc = ""] # [doc = " Returns `Ok(None)` if the segment does not contain notes."] # [doc = " Returns `Err` for invalid values."] fn notes < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < NoteIterator < 'data , Self :: Elf > > > { if self . p_type (endian) != elf :: PT_NOTE { return Ok (None) ; } let data = self . data (endian , data) . read_error ("Invalid ELF note segment offset or size") ? ; let notes = NoteIterator :: new (endian , self . p_align (endian) , data) ? ; Ok (Some (notes)) } }
};
}
