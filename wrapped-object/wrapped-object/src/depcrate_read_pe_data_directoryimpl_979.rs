// Generated macro for impl_979 (impl)
macro_rules! Depcrate_read_pe_data_directoryimpl_979 {
() => {
// Module: crate::read::pe::data_directory
// Provides: {"impl_979"}
// Dependencies: {}
impl < 'data > DataDirectories < 'data > { # [doc = " Parse the data directory table."] # [doc = ""] # [doc = " `data` must be the remaining optional data following the"] # [doc = " [optional header](pe::ImageOptionalHeader64).  `number` must be from the"] # [doc = " [`number_of_rva_and_sizes`](pe::ImageOptionalHeader64::number_of_rva_and_sizes)"] # [doc = " field of the optional header."] pub fn parse (data : & 'data [u8] , number : u32) -> Result < Self > { let entries = data . read_slice_at (0 , number as usize) . read_error ("Invalid PE number of RVA and sizes") ? ; Ok (DataDirectories { entries }) } # [doc = " The number of data directories."] # [allow (clippy :: len_without_is_empty)] pub fn len (& self) -> usize { self . entries . len () } # [doc = " Iterator over the data directories."] pub fn iter (& self) -> slice :: Iter < 'data , pe :: ImageDataDirectory > { self . entries . iter () } # [doc = " Iterator which gives the directories as well as their index (one of the IMAGE_DIRECTORY_ENTRY_* constants)."] pub fn enumerate (& self) -> core :: iter :: Enumerate < slice :: Iter < 'data , pe :: ImageDataDirectory > > { self . entries . iter () . enumerate () } # [doc = " Returns the data directory at the given index."] # [doc = ""] # [doc = " Index should be one of the `IMAGE_DIRECTORY_ENTRY_*` constants."] # [doc = ""] # [doc = " Returns `None` if the index is larger than the table size,"] # [doc = " or if the entry at the index has a zero virtual address."] pub fn get (& self , index : usize) -> Option < & 'data pe :: ImageDataDirectory > { self . entries . get (index) . filter (| d | d . virtual_address . get (LE) != 0) } # [doc = " Returns the unparsed export directory."] # [doc = ""] # [doc = " `data` must be the entire file data."] pub fn export_directory < R : ReadRef < 'data > > (& self , data : R , sections : & SectionTable < 'data > ,) -> Result < Option < & 'data pe :: ImageExportDirectory > > { let data_dir = match self . get (pe :: IMAGE_DIRECTORY_ENTRY_EXPORT) { Some (data_dir) => data_dir , None => return Ok (None) , } ; let export_data = data_dir . data (data , sections) ? ; ExportTable :: parse_directory (export_data) . map (Some) } # [doc = " Returns the partially parsed export directory."] # [doc = ""] # [doc = " `data` must be the entire file data."] pub fn export_table < R : ReadRef < 'data > > (& self , data : R , sections : & SectionTable < 'data > ,) -> Result < Option < ExportTable < 'data > > > { let data_dir = match self . get (pe :: IMAGE_DIRECTORY_ENTRY_EXPORT) { Some (data_dir) => data_dir , None => return Ok (None) , } ; let export_va = data_dir . virtual_address . get (LE) ; let export_data = data_dir . data (data , sections) ? ; ExportTable :: parse (export_data , export_va) . map (Some) } # [doc = " Returns the partially parsed import directory."] # [doc = ""] # [doc = " `data` must be the entire file data."] pub fn import_table < R : ReadRef < 'data > > (& self , data : R , sections : & SectionTable < 'data > ,) -> Result < Option < ImportTable < 'data > > > { let data_dir = match self . get (pe :: IMAGE_DIRECTORY_ENTRY_IMPORT) { Some (data_dir) => data_dir , None => return Ok (None) , } ; let import_va = data_dir . virtual_address . get (LE) ; let (section_data , section_va) = sections . pe_data_containing (data , import_va) . read_error ("Invalid import data dir virtual address") ? ; Ok (Some (ImportTable :: new (section_data , section_va , import_va))) } # [doc = " Returns the partially parsed delay-load import directory."] # [doc = ""] # [doc = " `data` must be the entire file data."] pub fn delay_load_import_table < R : ReadRef < 'data > > (& self , data : R , sections : & SectionTable < 'data > ,) -> Result < Option < DelayLoadImportTable < 'data > > > { let data_dir = match self . get (pe :: IMAGE_DIRECTORY_ENTRY_DELAY_IMPORT) { Some (data_dir) => data_dir , None => return Ok (None) , } ; let import_va = data_dir . virtual_address . get (LE) ; let (section_data , section_va) = sections . pe_data_containing (data , import_va) . read_error ("Invalid import data dir virtual address") ? ; Ok (Some (DelayLoadImportTable :: new (section_data , section_va , import_va ,))) } # [doc = " Returns the blocks in the base relocation directory."] # [doc = ""] # [doc = " `data` must be the entire file data."] pub fn relocation_blocks < R : ReadRef < 'data > > (& self , data : R , sections : & SectionTable < 'data > ,) -> Result < Option < RelocationBlockIterator < 'data > > > { let data_dir = match self . get (pe :: IMAGE_DIRECTORY_ENTRY_BASERELOC) { Some (data_dir) => data_dir , None => return Ok (None) , } ; let reloc_data = data_dir . data (data , sections) ? ; Ok (Some (RelocationBlockIterator :: new (reloc_data))) } # [doc = " Returns the resource directory."] # [doc = ""] # [doc = " `data` must be the entire file data."] pub fn resource_directory < R : ReadRef < 'data > > (& self , data : R , sections : & SectionTable < 'data > ,) -> Result < Option < ResourceDirectory < 'data > > > { let data_dir = match self . get (pe :: IMAGE_DIRECTORY_ENTRY_RESOURCE) { Some (data_dir) => data_dir , None => return Ok (None) , } ; let rsrc_data = data_dir . data (data , sections) ? ; Ok (Some (ResourceDirectory :: new (rsrc_data))) } }
};
}
