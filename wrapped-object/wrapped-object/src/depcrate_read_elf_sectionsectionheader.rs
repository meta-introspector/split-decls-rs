// Generated macro for SectionHeader (trait)
macro_rules! Depcrate_read_elf_sectionSectionHeader {
() => {
// Module: crate::read::elf::section
// Provides: {"SectionHeader"}
// Dependencies: {}
# [doc = " A trait for generic access to [`elf::SectionHeader32`] and [`elf::SectionHeader64`]."] # [allow (missing_docs)] pub trait SectionHeader : Debug + Pod { type Elf : FileHeader < SectionHeader = Self , Endian = Self :: Endian , Word = Self :: Word > ; type Word : Into < u64 > ; type Endian : endian :: Endian ; fn sh_name (& self , endian : Self :: Endian) -> u32 ; fn sh_type (& self , endian : Self :: Endian) -> u32 ; fn sh_flags (& self , endian : Self :: Endian) -> Self :: Word ; fn sh_addr (& self , endian : Self :: Endian) -> Self :: Word ; fn sh_offset (& self , endian : Self :: Endian) -> Self :: Word ; fn sh_size (& self , endian : Self :: Endian) -> Self :: Word ; fn sh_link (& self , endian : Self :: Endian) -> u32 ; fn sh_info (& self , endian : Self :: Endian) -> u32 ; fn sh_addralign (& self , endian : Self :: Endian) -> Self :: Word ; fn sh_entsize (& self , endian : Self :: Endian) -> Self :: Word ; # [doc = " Parse the section name from the string table."] fn name < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , strings : StringTable < 'data , R > ,) -> read :: Result < & 'data [u8] > { strings . get (self . sh_name (endian)) . read_error ("Invalid ELF section name offset") } # [doc = " Get the `sh_link` field as a section index."] # [doc = ""] # [doc = " This may return a null section index, and does not check for validity."] fn link (& self , endian : Self :: Endian) -> SectionIndex { SectionIndex (self . sh_link (endian) as usize) } # [doc = " Return true if the `SHF_INFO_LINK` flag is set."] fn has_info_link (& self , endian : Self :: Endian) -> bool { self . sh_flags (endian) . into () & u64 :: from (elf :: SHF_INFO_LINK) != 0 } # [doc = " Get the `sh_info` field as a section index."] # [doc = ""] # [doc = " This does not check the `SHF_INFO_LINK` flag."] # [doc = " This may return a null section index, and does not check for validity."] fn info_link (& self , endian : Self :: Endian) -> SectionIndex { SectionIndex (self . sh_info (endian) as usize) } # [doc = " Return the offset and size of the section in the file."] # [doc = ""] # [doc = " Returns `None` for sections that have no data in the file."] fn file_range (& self , endian : Self :: Endian) -> Option < (u64 , u64) > { if self . sh_type (endian) == elf :: SHT_NOBITS { None } else { Some ((self . sh_offset (endian) . into () , self . sh_size (endian) . into ())) } } # [doc = " Return the section data."] # [doc = ""] # [doc = " Returns `Ok(&[])` if the section has no data."] # [doc = " Returns `Err` for invalid values."] fn data < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < & 'data [u8] > { if let Some ((offset , size)) = self . file_range (endian) { data . read_bytes_at (offset , size) . read_error ("Invalid ELF section size or offset") } else { Ok (& []) } } # [doc = " Return the section data as a slice of the given type."] # [doc = ""] # [doc = " Allows padding at the end of the data."] # [doc = " Returns `Ok(&[])` if the section has no data."] # [doc = " Returns `Err` for invalid values, including bad alignment."] fn data_as_array < 'data , T : Pod , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < & 'data [T] > { pod :: slice_from_all_bytes (self . data (endian , data) ?) . read_error ("Invalid ELF section size or offset") } # [doc = " Return the strings in the section."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section does not contain strings."] # [doc = " Returns `Err` for invalid values."] fn strings < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < StringTable < 'data , R > > > { if self . sh_type (endian) != elf :: SHT_STRTAB { return Ok (None) ; } let str_offset = self . sh_offset (endian) . into () ; let str_size = self . sh_size (endian) . into () ; let str_end = str_offset . checked_add (str_size) . read_error ("Invalid ELF string section offset or size") ? ; Ok (Some (StringTable :: new (data , str_offset , str_end))) } # [doc = " Return the symbols in the section."] # [doc = ""] # [doc = " Also finds the linked string table in `sections`."] # [doc = ""] # [doc = " `section_index` must be the 0-based index of this section, and is used"] # [doc = " to find the corresponding extended section index table in `sections`."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section does not contain symbols."] # [doc = " Returns `Err` for invalid values."] fn symbols < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R , sections : & SectionTable < 'data , Self :: Elf , R > , section_index : SectionIndex ,) -> read :: Result < Option < SymbolTable < 'data , Self :: Elf , R > > > { let sh_type = self . sh_type (endian) ; if sh_type != elf :: SHT_SYMTAB && sh_type != elf :: SHT_DYNSYM { return Ok (None) ; } SymbolTable :: parse (endian , data , sections , section_index , self) . map (Some) } # [doc = " Return the `Elf::Rel` entries in the section."] # [doc = ""] # [doc = " Also returns the linked symbol table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section does not contain relocations."] # [doc = " Returns `Err` for invalid values."] fn rel < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < (& 'data [< Self :: Elf as FileHeader > :: Rel] , SectionIndex) > > { if self . sh_type (endian) != elf :: SHT_REL { return Ok (None) ; } let rel = self . data_as_array (endian , data) . read_error ("Invalid ELF relocation section offset or size") ? ; Ok (Some ((rel , self . link (endian)))) } # [doc = " Return the `Elf::Rela` entries in the section."] # [doc = ""] # [doc = " Also returns the linked symbol table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section does not contain relocations."] # [doc = " Returns `Err` for invalid values."] fn rela < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < (& 'data [< Self :: Elf as FileHeader > :: Rela] , SectionIndex) > > { if self . sh_type (endian) != elf :: SHT_RELA { return Ok (None) ; } let rela = self . data_as_array (endian , data) . read_error ("Invalid ELF relocation section offset or size") ? ; Ok (Some ((rela , self . link (endian)))) } # [doc = " Return the `Elf::Relr` entries in the section."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section does not contain relative relocations."] # [doc = " Returns `Err` for invalid values."] fn relr < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < RelrIterator < 'data , Self :: Elf > > > { if self . sh_type (endian) != elf :: SHT_RELR { return Ok (None) ; } let data = self . data_as_array (endian , data) . read_error ("Invalid ELF relocation section offset or size") ? ; let relrs = RelrIterator :: new (endian , data) ; Ok (Some (relrs)) } # [doc = " Return the `Crel` entries in the section."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section does not contain compact relocations."] # [doc = " Returns `Err` for invalid values."] fn crel < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < (CrelIterator < 'data > , SectionIndex) > > { if self . sh_type (endian) != elf :: SHT_CREL { return Ok (None) ; } let data = self . data (endian , data) . read_error ("Invalid ELF relocation section offset or size") ? ; let relrs = CrelIterator :: new (data) ; Ok (Some ((relrs ? , self . link (endian)))) } # [doc = " Return entries in a dynamic section."] # [doc = ""] # [doc = " Also returns the linked string table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section type is not `SHT_DYNAMIC`."] # [doc = " Returns `Err` for invalid values."] fn dynamic < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < (& 'data [< Self :: Elf as FileHeader > :: Dyn] , SectionIndex) > > { if self . sh_type (endian) != elf :: SHT_DYNAMIC { return Ok (None) ; } let dynamic = self . data_as_array (endian , data) . read_error ("Invalid ELF dynamic section offset or size") ? ; Ok (Some ((dynamic , self . link (endian)))) } # [doc = " Return a note iterator for the section data."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section does not contain notes."] # [doc = " Returns `Err` for invalid values."] fn notes < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < NoteIterator < 'data , Self :: Elf > > > { if self . sh_type (endian) != elf :: SHT_NOTE { return Ok (None) ; } let data = self . data (endian , data) . read_error ("Invalid ELF note section offset or size") ? ; let notes = NoteIterator :: new (endian , self . sh_addralign (endian) , data) ? ; Ok (Some (notes)) } # [doc = " Return the contents of a group section."] # [doc = ""] # [doc = " The first value is a `GRP_*` value, and the remaining values"] # [doc = " are section indices."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section does not define a group."] # [doc = " Returns `Err` for invalid values."] fn group < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < (u32 , & 'data [U32Bytes < Self :: Endian >]) > > { if self . sh_type (endian) != elf :: SHT_GROUP { return Ok (None) ; } let msg = "Invalid ELF group section offset or size" ; let data = self . data (endian , data) . read_error (msg) ? ; let (flag , data) = pod :: from_bytes :: < U32Bytes < _ > > (data) . read_error (msg) ? ; let sections = pod :: slice_from_all_bytes (data) . read_error (msg) ? ; Ok (Some ((flag . get (endian) , sections))) } # [doc = " Return the header of a SysV hash section."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section does not contain a SysV hash."] # [doc = " Returns `Err` for invalid values."] fn hash_header < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < & 'data elf :: HashHeader < Self :: Endian > > > { if self . sh_type (endian) != elf :: SHT_HASH { return Ok (None) ; } let data = self . data (endian , data) . read_error ("Invalid ELF hash section offset or size") ? ; let header = data . read_at :: < elf :: HashHeader < Self :: Endian > > (0) . read_error ("Invalid hash header") ? ; Ok (Some (header)) } # [doc = " Return the contents of a SysV hash section."] # [doc = ""] # [doc = " Also returns the linked symbol table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section does not contain a SysV hash."] # [doc = " Returns `Err` for invalid values."] fn hash < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < (HashTable < 'data , Self :: Elf > , SectionIndex) > > { if self . sh_type (endian) != elf :: SHT_HASH { return Ok (None) ; } let data = self . data (endian , data) . read_error ("Invalid ELF hash section offset or size") ? ; let hash = HashTable :: parse (endian , data) ? ; Ok (Some ((hash , self . link (endian)))) } # [doc = " Return the header of a GNU hash section."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section does not contain a GNU hash."] # [doc = " Returns `Err` for invalid values."] fn gnu_hash_header < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < & 'data elf :: GnuHashHeader < Self :: Endian > > > { if self . sh_type (endian) != elf :: SHT_GNU_HASH { return Ok (None) ; } let data = self . data (endian , data) . read_error ("Invalid ELF GNU hash section offset or size") ? ; let header = data . read_at :: < elf :: GnuHashHeader < Self :: Endian > > (0) . read_error ("Invalid GNU hash header") ? ; Ok (Some (header)) } # [doc = " Return the contents of a GNU hash section."] # [doc = ""] # [doc = " Also returns the linked symbol table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section does not contain a GNU hash."] # [doc = " Returns `Err` for invalid values."] fn gnu_hash < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < (GnuHashTable < 'data , Self :: Elf > , SectionIndex) > > { if self . sh_type (endian) != elf :: SHT_GNU_HASH { return Ok (None) ; } let data = self . data (endian , data) . read_error ("Invalid ELF GNU hash section offset or size") ? ; let hash = GnuHashTable :: parse (endian , data) ? ; Ok (Some ((hash , self . link (endian)))) } # [doc = " Return the contents of a `SHT_GNU_VERSYM` section."] # [doc = ""] # [doc = " Also returns the linked symbol table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section type is not `SHT_GNU_VERSYM`."] # [doc = " Returns `Err` for invalid values."] fn gnu_versym < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < (& 'data [elf :: Versym < Self :: Endian >] , SectionIndex) > > { if self . sh_type (endian) != elf :: SHT_GNU_VERSYM { return Ok (None) ; } let versym = self . data_as_array (endian , data) . read_error ("Invalid ELF GNU versym section offset or size") ? ; Ok (Some ((versym , self . link (endian)))) } # [doc = " Return an iterator for the entries of a `SHT_GNU_VERDEF` section."] # [doc = ""] # [doc = " Also returns the linked string table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section type is not `SHT_GNU_VERDEF`."] # [doc = " Returns `Err` for invalid values."] fn gnu_verdef < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < (VerdefIterator < 'data , Self :: Elf > , SectionIndex) > > { if self . sh_type (endian) != elf :: SHT_GNU_VERDEF { return Ok (None) ; } let verdef = self . data (endian , data) . read_error ("Invalid ELF GNU verdef section offset or size") ? ; Ok (Some ((VerdefIterator :: new (endian , verdef) , self . link (endian) ,))) } # [doc = " Return an iterator for the entries of a `SHT_GNU_VERNEED` section."] # [doc = ""] # [doc = " Also returns the linked string table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section type is not `SHT_GNU_VERNEED`."] # [doc = " Returns `Err` for invalid values."] fn gnu_verneed < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < (VerneedIterator < 'data , Self :: Elf > , SectionIndex) > > { if self . sh_type (endian) != elf :: SHT_GNU_VERNEED { return Ok (None) ; } let verneed = self . data (endian , data) . read_error ("Invalid ELF GNU verneed section offset or size") ? ; Ok (Some ((VerneedIterator :: new (endian , verneed) , self . link (endian) ,))) } # [doc = " Return the contents of a `SHT_GNU_ATTRIBUTES` section."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section type is not `SHT_GNU_ATTRIBUTES`."] # [doc = " Returns `Err` for invalid values."] fn gnu_attributes < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < AttributesSection < 'data , Self :: Elf > > > { if self . sh_type (endian) != elf :: SHT_GNU_ATTRIBUTES { return Ok (None) ; } self . attributes (endian , data) . map (Some) } # [doc = " Parse the contents of the section as attributes."] # [doc = ""] # [doc = " This function does not check whether section type corresponds"] # [doc = " to a section that contains attributes."] # [doc = ""] # [doc = " Returns `Err` for invalid values."] fn attributes < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < AttributesSection < 'data , Self :: Elf > > { let data = self . data (endian , data) ? ; AttributesSection :: new (endian , data) } # [doc = " Parse the compression header if present."] # [doc = ""] # [doc = " Returns the header, and the offset and size of the compressed section data"] # [doc = " in the file."] # [doc = ""] # [doc = " Returns `Ok(None)` if the section flags do not have `SHF_COMPRESSED`."] # [doc = " Returns `Err` for invalid values."] fn compression < 'data , R : ReadRef < 'data > > (& self , endian : Self :: Endian , data : R ,) -> read :: Result < Option < (& 'data < Self :: Elf as FileHeader > :: CompressionHeader , u64 , u64 ,) > , > { if (self . sh_flags (endian) . into () & u64 :: from (elf :: SHF_COMPRESSED)) == 0 { return Ok (None) ; } let (section_offset , section_size) = self . file_range (endian) . read_error ("Invalid ELF compressed section type") ? ; let mut offset = section_offset ; let header = data . read :: < < Self :: Elf as FileHeader > :: CompressionHeader > (& mut offset) . read_error ("Invalid ELF compressed section offset") ? ; let compressed_size = section_size . checked_sub (offset - section_offset) . read_error ("Invalid ELF compressed section size") ? ; Ok (Some ((header , offset , compressed_size))) } }
};
}
