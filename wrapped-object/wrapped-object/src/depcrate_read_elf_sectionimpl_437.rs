// Generated macro for impl_437 (impl)
macro_rules! Depcrate_read_elf_sectionimpl_437 {
() => {
// Module: crate::read::elf::section
// Provides: {"impl_437"}
// Dependencies: {}
impl < 'data , Elf : FileHeader , R : ReadRef < 'data > > SectionTable < 'data , Elf , R > { # [doc = " Create a new section table."] # [inline] pub fn new (sections : & 'data [Elf :: SectionHeader] , strings : StringTable < 'data , R >) -> Self { SectionTable { sections , strings } } # [doc = " Iterate over the section headers."] # [doc = ""] # [doc = " This includes the null section at index 0, which you will usually need to skip."] # [inline] pub fn iter (& self) -> slice :: Iter < 'data , Elf :: SectionHeader > { self . sections . iter () } # [doc = " Iterate over the section headers and their indices."] # [doc = ""] # [doc = " This includes the null section at index 0, which you will usually need to skip."] # [inline] pub fn enumerate (& self) -> impl Iterator < Item = (SectionIndex , & 'data Elf :: SectionHeader) > { self . sections . iter () . enumerate () . map (| (i , section) | (SectionIndex (i) , section)) } # [doc = " Return true if the section table is empty."] # [inline] pub fn is_empty (& self) -> bool { self . sections . is_empty () } # [doc = " The number of section headers."] # [inline] pub fn len (& self) -> usize { self . sections . len () } # [doc = " Get the section header at the given index."] # [doc = ""] # [doc = " Returns an error for the null section at index 0."] pub fn section (& self , index : SectionIndex) -> read :: Result < & 'data Elf :: SectionHeader > { if index == SectionIndex (0) { return Err (read :: Error ("Invalid ELF section index")) ; } self . sections . get (index . 0) . read_error ("Invalid ELF section index") } # [doc = " Return the section header with the given name."] # [doc = ""] # [doc = " Ignores sections with invalid names."] pub fn section_by_name (& self , endian : Elf :: Endian , name : & [u8] ,) -> Option < (SectionIndex , & 'data Elf :: SectionHeader) > { self . enumerate () . find (| (_ , section) | self . section_name (endian , section) == Ok (name)) } # [doc = " Return the section name for the given section header."] pub fn section_name (& self , endian : Elf :: Endian , section : & Elf :: SectionHeader ,) -> read :: Result < & 'data [u8] > { section . name (endian , self . strings) } # [doc = " Return the string table at the given section index."] # [doc = ""] # [doc = " Returns an empty string table if the index is 0."] # [doc = " Returns an error if the section is not a string table."] # [inline] pub fn strings (& self , endian : Elf :: Endian , data : R , index : SectionIndex ,) -> read :: Result < StringTable < 'data , R > > { if index == SectionIndex (0) { return Ok (StringTable :: default ()) ; } self . section (index) ? . strings (endian , data) ? . read_error ("Invalid ELF string section type") } # [doc = " Return the symbol table of the given section type."] # [doc = ""] # [doc = " Returns an empty symbol table if the symbol table does not exist."] # [inline] pub fn symbols (& self , endian : Elf :: Endian , data : R , sh_type : u32 ,) -> read :: Result < SymbolTable < 'data , Elf , R > > { debug_assert ! (sh_type == elf :: SHT_DYNSYM || sh_type == elf :: SHT_SYMTAB) ; let (index , section) = match self . enumerate () . find (| s | s . 1 . sh_type (endian) == sh_type) { Some (s) => s , None => return Ok (SymbolTable :: default ()) , } ; SymbolTable :: parse (endian , data , self , index , section) } # [doc = " Return the symbol table at the given section index."] # [doc = ""] # [doc = " Returns an error if the section is not a symbol table."] # [inline] pub fn symbol_table_by_index (& self , endian : Elf :: Endian , data : R , index : SectionIndex ,) -> read :: Result < SymbolTable < 'data , Elf , R > > { let section = self . section (index) ? ; match section . sh_type (endian) { elf :: SHT_DYNSYM | elf :: SHT_SYMTAB => { } _ => return Err (Error ("Invalid ELF symbol table section type")) , } SymbolTable :: parse (endian , data , self , index , section) } # [doc = " Create a mapping from section index to associated relocation sections."] # [inline] pub fn relocation_sections (& self , endian : Elf :: Endian , symbol_section : SectionIndex ,) -> read :: Result < RelocationSections > { RelocationSections :: parse (endian , self , symbol_section) } # [doc = " Return the contents of a dynamic section."] # [doc = ""] # [doc = " Also returns the linked string table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if there is no `SHT_DYNAMIC` section."] # [doc = " Returns `Err` for invalid values."] pub fn dynamic (& self , endian : Elf :: Endian , data : R ,) -> read :: Result < Option < (& 'data [Elf :: Dyn] , SectionIndex) > > { for section in self . sections { if let Some (dynamic) = section . dynamic (endian , data) ? { return Ok (Some (dynamic)) ; } } Ok (None) } # [doc = " Return the header of a SysV hash section."] # [doc = ""] # [doc = " Returns `Ok(None)` if there is no SysV GNU hash section."] # [doc = " Returns `Err` for invalid values."] pub fn hash_header (& self , endian : Elf :: Endian , data : R ,) -> read :: Result < Option < & 'data elf :: HashHeader < Elf :: Endian > > > { for section in self . sections { if let Some (hash) = section . hash_header (endian , data) ? { return Ok (Some (hash)) ; } } Ok (None) } # [doc = " Return the contents of a SysV hash section."] # [doc = ""] # [doc = " Also returns the linked symbol table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if there is no SysV hash section."] # [doc = " Returns `Err` for invalid values."] pub fn hash (& self , endian : Elf :: Endian , data : R ,) -> read :: Result < Option < (HashTable < 'data , Elf > , SectionIndex) > > { for section in self . sections { if let Some (hash) = section . hash (endian , data) ? { return Ok (Some (hash)) ; } } Ok (None) } # [doc = " Return the header of a GNU hash section."] # [doc = ""] # [doc = " Returns `Ok(None)` if there is no GNU hash section."] # [doc = " Returns `Err` for invalid values."] pub fn gnu_hash_header (& self , endian : Elf :: Endian , data : R ,) -> read :: Result < Option < & 'data elf :: GnuHashHeader < Elf :: Endian > > > { for section in self . sections { if let Some (hash) = section . gnu_hash_header (endian , data) ? { return Ok (Some (hash)) ; } } Ok (None) } # [doc = " Return the contents of a GNU hash section."] # [doc = ""] # [doc = " Also returns the linked symbol table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if there is no GNU hash section."] # [doc = " Returns `Err` for invalid values."] pub fn gnu_hash (& self , endian : Elf :: Endian , data : R ,) -> read :: Result < Option < (GnuHashTable < 'data , Elf > , SectionIndex) > > { for section in self . sections { if let Some (hash) = section . gnu_hash (endian , data) ? { return Ok (Some (hash)) ; } } Ok (None) } # [doc = " Return the contents of a `SHT_GNU_VERSYM` section."] # [doc = ""] # [doc = " Also returns the linked symbol table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if there is no `SHT_GNU_VERSYM` section."] # [doc = " Returns `Err` for invalid values."] pub fn gnu_versym (& self , endian : Elf :: Endian , data : R ,) -> read :: Result < Option < (& 'data [elf :: Versym < Elf :: Endian >] , SectionIndex) > > { for section in self . sections { if let Some (syms) = section . gnu_versym (endian , data) ? { return Ok (Some (syms)) ; } } Ok (None) } # [doc = " Return the contents of a `SHT_GNU_VERDEF` section."] # [doc = ""] # [doc = " Also returns the linked string table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if there is no `SHT_GNU_VERDEF` section."] # [doc = " Returns `Err` for invalid values."] pub fn gnu_verdef (& self , endian : Elf :: Endian , data : R ,) -> read :: Result < Option < (VerdefIterator < 'data , Elf > , SectionIndex) > > { for section in self . sections { if let Some (defs) = section . gnu_verdef (endian , data) ? { return Ok (Some (defs)) ; } } Ok (None) } # [doc = " Return the contents of a `SHT_GNU_VERNEED` section."] # [doc = ""] # [doc = " Also returns the linked string table index."] # [doc = ""] # [doc = " Returns `Ok(None)` if there is no `SHT_GNU_VERNEED` section."] # [doc = " Returns `Err` for invalid values."] pub fn gnu_verneed (& self , endian : Elf :: Endian , data : R ,) -> read :: Result < Option < (VerneedIterator < 'data , Elf > , SectionIndex) > > { for section in self . sections { if let Some (needs) = section . gnu_verneed (endian , data) ? { return Ok (Some (needs)) ; } } Ok (None) } # [doc = " Returns the symbol version table."] # [doc = ""] # [doc = " Returns `Ok(None)` if there is no `SHT_GNU_VERSYM` section."] # [doc = " Returns `Err` for invalid values."] pub fn versions (& self , endian : Elf :: Endian , data : R ,) -> read :: Result < Option < VersionTable < 'data , Elf > > > { let (versyms , link) = match self . gnu_versym (endian , data) ? { Some (val) => val , None => return Ok (None) , } ; let strings = self . symbol_table_by_index (endian , data , link) ? . strings () ; let verdefs = self . gnu_verdef (endian , data) ? . map (| x | x . 0) ; let verneeds = self . gnu_verneed (endian , data) ? . map (| x | x . 0) ; VersionTable :: parse (endian , versyms , verdefs , verneeds , strings) . map (Some) } }
};
}
