// Generated macro for TypeMode (struct)
macro_rules! Depcrate_interfaceTypeMode {
() => {
// Module: crate::interface
// Provides: {"TypeMode"}
// Dependencies: {}
# [doc = " A description of the \"mode\" in which a type is printed."] # [doc = ""] # [doc = " Rust types can either be \"borrowed\" or \"owned\". This primarily has to do"] # [doc = " with lists and imports where arguments to imports can be borrowed lists in"] # [doc = " theory as ownership is not taken from the caller. This structure is used to"] # [doc = " help with this fact in addition to the various codegen options of this"] # [doc = " generator. Namely types in WIT can be reflected into Rust as two separate"] # [doc = " types, one \"owned\" and one \"borrowed\" (aka one with `Vec` and one with"] # [doc = " `&[T]`)."] # [doc = ""] # [doc = " This structure is used in conjunction with `modes_of` and `type_mode_for*`"] # [doc = " primarily. That enables creating a programmatic description of what a type"] # [doc = " is rendered as along with various options."] # [doc = ""] # [doc = " Note that a `TypeMode` is a description of a single \"level\" of a type. This"] # [doc = " means that there's one mode for `Vec<T>` and one mode for `T` internally."] # [doc = " This is mostly used for things like records where some fields have lifetime"] # [doc = " parameters, for example, and others don't."] # [doc = ""] # [doc = " This type is intended to simplify generation of types and encapsulate all"] # [doc = " the knowledge about whether lifetime parameters are used and how lists are"] # [doc = " rendered."] # [doc = ""] # [doc = " There are currently two users of lifetime parameters:"] # [doc = ""] # [doc = " * Lists - when borrowed these are rendered as either `&[T]` or `&str`."] # [doc = " * Borrowed resources - for resources owned by the current module they're"] # [doc = "   represented as `&T` and for borrows of imported resources they're"] # [doc = "   represented, more-or-less, as `&Resource<T>`."] # [doc = ""] # [doc = " Lists have a choice of being rendered as borrowed or not but resources are"] # [doc = " required to be borrowed."] # [derive (Debug , Copy , Clone , PartialEq)] struct TypeMode { # [doc = " The lifetime parameter, if any, for this type. If present this type is"] # [doc = " required to have a lifetime parameter."] lifetime : Option < & 'static str > , # [doc = " Whether or not lists are borrowed in this type."] # [doc = ""] # [doc = " If this field is `true` then lists are rendered as `&[T]` and `&str`"] # [doc = " rather than their owned equivalent. If this field is `false` than the"] # [doc = " owned equivalents are used instead."] lists_borrowed : bool , # [doc = " The \"style\" of ownership that this mode was created with."] # [doc = ""] # [doc = " This information is used to determine what mode the next layer deep int"] # [doc = " he type tree is rendered with. For example if this layer is owned so is"] # [doc = " the next layer. This is primarily used for the \"OnlyTopBorrowed\""] # [doc = " ownership style where all further layers beneath that are `Owned`."] style : TypeOwnershipStyle , }
};
}
