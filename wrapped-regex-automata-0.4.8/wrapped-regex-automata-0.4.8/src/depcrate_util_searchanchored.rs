// Generated macro for Anchored (enum)
macro_rules! Depcrate_util_searchAnchored {
() => {
// Module: crate::util::search
// Provides: {"Anchored"}
// Dependencies: {}
# [doc = " The type of anchored search to perform."] # [doc = ""] # [doc = " This is *almost* a boolean option. That is, you can either do an unanchored"] # [doc = " search for any pattern in a regex, or you can do an anchored search for any"] # [doc = " pattern in a regex."] # [doc = ""] # [doc = " A third option exists that, assuming the regex engine supports it, permits"] # [doc = " you to do an anchored search for a specific pattern."] # [doc = ""] # [doc = " Note that there is no way to run an unanchored search for a specific"] # [doc = " pattern. If you need that, you'll need to build separate regexes for each"] # [doc = " pattern."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If a regex engine does not support the anchored mode selected, then the"] # [doc = " regex engine will return an error. While any non-trivial regex engine"] # [doc = " should support at least one of the available anchored modes, there is no"] # [doc = " singular mode that is guaranteed to be universally supported. Some regex"] # [doc = " engines might only support unanchored searches (DFAs compiled without"] # [doc = " anchored starting states) and some regex engines might only support"] # [doc = " anchored searches (like the one-pass DFA)."] # [doc = ""] # [doc = " The specific error returned is a [`MatchError`] with a"] # [doc = " [`MatchErrorKind::UnsupportedAnchored`] kind. The kind includes the"] # [doc = " `Anchored` value given that is unsupported."] # [doc = ""] # [doc = " Note that regex engines should report \"no match\" if, for example, an"] # [doc = " `Anchored::Pattern` is provided with an invalid pattern ID _but_ where"] # [doc = " anchored searches for a specific pattern are supported. This is smooths out"] # [doc = " behavior such that it's possible to guarantee that an error never occurs"] # [doc = " based on how the regex engine is configured. All regex engines in this"] # [doc = " crate report \"no match\" when searching for an invalid pattern ID, but where"] # [doc = " searching for a valid pattern ID is otherwise supported."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to use the various `Anchored` modes to run a"] # [doc = " search. We use the [`PikeVM`](crate::nfa::thompson::pikevm::PikeVM)"] # [doc = " because it supports all modes unconditionally. Some regex engines, like"] # [doc = " the [`onepass::DFA`](crate::dfa::onepass::DFA) cannot support unanchored"] # [doc = " searches."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     Anchored, Input, Match, PatternID,"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::new_many(&["] # [doc = "     r\"Mrs. \\w+\","] # [doc = "     r\"Miss \\w+\","] # [doc = "     r\"Mr. \\w+\","] # [doc = "     r\"Ms. \\w+\","] # [doc = " ])?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let hay = \"Hello Mr. Springsteen!\";"] # [doc = ""] # [doc = " // The default is to do an unanchored search."] # [doc = " assert_eq!(Some(Match::must(2, 6..21)), re.find(&mut cache, hay));"] # [doc = " // Explicitly ask for an unanchored search. Same as above."] # [doc = " let input = Input::new(hay).anchored(Anchored::No);"] # [doc = " assert_eq!(Some(Match::must(2, 6..21)), re.find(&mut cache, hay));"] # [doc = ""] # [doc = " // Now try an anchored search. Since the match doesn't start at the"] # [doc = " // beginning of the haystack, no match is found!"] # [doc = " let input = Input::new(hay).anchored(Anchored::Yes);"] # [doc = " assert_eq!(None, re.find(&mut cache, input));"] # [doc = ""] # [doc = " // We can try an anchored search again, but move the location of where"] # [doc = " // we start the search. Note that the offsets reported are still in"] # [doc = " // terms of the overall haystack and not relative to where we started"] # [doc = " // the search."] # [doc = " let input = Input::new(hay).anchored(Anchored::Yes).range(6..);"] # [doc = " assert_eq!(Some(Match::must(2, 6..21)), re.find(&mut cache, input));"] # [doc = ""] # [doc = " // Now try an anchored search for a specific pattern. We specifically"] # [doc = " // choose a pattern that we know doesn't match to prove that the search"] # [doc = " // only looks for the pattern we provide."] # [doc = " let input = Input::new(hay)"] # [doc = "     .anchored(Anchored::Pattern(PatternID::must(1)))"] # [doc = "     .range(6..);"] # [doc = " assert_eq!(None, re.find(&mut cache, input));"] # [doc = ""] # [doc = " // But if we switch it to the pattern that we know matches, then we find"] # [doc = " // the match."] # [doc = " let input = Input::new(hay)"] # [doc = "     .anchored(Anchored::Pattern(PatternID::must(2)))"] # [doc = "     .range(6..);"] # [doc = " assert_eq!(Some(Match::must(2, 6..21)), re.find(&mut cache, input));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Copy , Debug , Eq , PartialEq)] pub enum Anchored { # [doc = " Run an unanchored search. This means a match may occur anywhere at or"] # [doc = " after the start position of the search."] # [doc = ""] # [doc = " This search can return a match for any pattern in the regex."] No , # [doc = " Run an anchored search. This means that a match must begin at the"] # [doc = " start position of the search."] # [doc = ""] # [doc = " This search can return a match for any pattern in the regex."] Yes , # [doc = " Run an anchored search for a specific pattern. This means that a match"] # [doc = " must be for the given pattern and must begin at the start position of"] # [doc = " the search."] Pattern (PatternID) , }
};
}
