// Generated macro for Builder (struct)
macro_rules! Depcrate_nfa_thompson_backtrackBuilder {
() => {
// Module: crate::nfa::thompson::backtrack
// Provides: {"Builder"}
// Dependencies: {}
# [doc = " A builder for a bounded backtracker."] # [doc = ""] # [doc = " This builder permits configuring options for the syntax of a pattern, the"] # [doc = " NFA construction and the `BoundedBacktracker` construction. This builder"] # [doc = " is different from a general purpose regex builder in that it permits fine"] # [doc = " grain configuration of the construction process. The trade off for this is"] # [doc = " complexity, and the possibility of setting a configuration that might not"] # [doc = " make sense. For example, there are two different UTF-8 modes:"] # [doc = ""] # [doc = " * [`syntax::Config::utf8`](crate::util::syntax::Config::utf8) controls"] # [doc = " whether the pattern itself can contain sub-expressions that match invalid"] # [doc = " UTF-8."] # [doc = " * [`thompson::Config::utf8`] controls how the regex iterators themselves"] # [doc = " advance the starting position of the next search when a match with zero"] # [doc = " length is found."] # [doc = ""] # [doc = " Generally speaking, callers will want to either enable all of these or"] # [doc = " disable all of these."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to disable UTF-8 mode in the syntax and the regex"] # [doc = " itself. This is generally what you want for matching on arbitrary bytes."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::{self, backtrack::BoundedBacktracker},"] # [doc = "     util::syntax,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = BoundedBacktracker::builder()"] # [doc = "     .syntax(syntax::Config::new().utf8(false))"] # [doc = "     .thompson(thompson::Config::new().utf8(false))"] # [doc = "     .build(r\"foo(?-u:[^b])ar.*\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let haystack = b\"\\xFEfoo\\xFFarzz\\xE2\\x98\\xFF\\n\";"] # [doc = " let expected = Some(Ok(Match::must(0, 1..9)));"] # [doc = " let got = re.try_find_iter(&mut cache, haystack).next();"] # [doc = " assert_eq!(expected, got);"] # [doc = " // Notice that `(?-u:[^b])` matches invalid UTF-8,"] # [doc = " // but the subsequent `.*` does not! Disabling UTF-8"] # [doc = " // on the syntax permits this."] # [doc = " //"] # [doc = " // N.B. This example does not show the impact of"] # [doc = " // disabling UTF-8 mode on a BoundedBacktracker Config, since that"] # [doc = " // only impacts regexes that can produce matches of"] # [doc = " // length 0."] # [doc = " assert_eq!(b\"foo\\xFFarzz\", &haystack[got.unwrap()?.range()]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Debug)] pub struct Builder { config : Config , # [cfg (feature = "syntax")] thompson : thompson :: Compiler , }
};
}
