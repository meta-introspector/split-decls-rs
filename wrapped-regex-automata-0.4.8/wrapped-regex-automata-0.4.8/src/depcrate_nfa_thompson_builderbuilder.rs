// Generated macro for Builder (struct)
macro_rules! Depcrate_nfa_thompson_builderBuilder {
() => {
// Module: crate::nfa::thompson::builder
// Provides: {"Builder"}
// Dependencies: {}
# [doc = " An abstraction for building Thompson NFAs by hand."] # [doc = ""] # [doc = " A builder is what a [`thompson::Compiler`](crate::nfa::thompson::Compiler)"] # [doc = " uses internally to translate a regex's high-level intermediate"] # [doc = " representation into an [`NFA`]."] # [doc = ""] # [doc = " The primary function of this builder is to abstract away the internal"] # [doc = " representation of an NFA and make it difficult to produce NFAs are that"] # [doc = " internally invalid or inconsistent. This builder also provides a way to"] # [doc = " add \"empty\" states (which can be thought of as unconditional epsilon"] # [doc = " transitions), despite the fact that [`thompson::State`](nfa::State) does"] # [doc = " not have any \"empty\" representation. The advantage of \"empty\" states is"] # [doc = " that they make the code for constructing a Thompson NFA logically simpler."] # [doc = ""] # [doc = " Many of the routines on this builder may panic or return errors. Generally"] # [doc = " speaking, panics occur when an invalid sequence of method calls were made,"] # [doc = " where as an error occurs if things get too big. (Where \"too big\" might mean"] # [doc = " exhausting identifier space or using up too much heap memory in accordance"] # [doc = " with the configured [`size_limit`](Builder::set_size_limit).)"] # [doc = ""] # [doc = " # Overview"] # [doc = ""] # [doc = " ## Adding multiple patterns"] # [doc = ""] # [doc = " Each pattern you add to an NFA should correspond to a pair of"] # [doc = " [`Builder::start_pattern`] and [`Builder::finish_pattern`] calls, with"] # [doc = " calls inbetween that add NFA states for that pattern. NFA states may be"] # [doc = " added without first calling `start_pattern`, with the exception of adding"] # [doc = " capturing states."] # [doc = ""] # [doc = " ## Adding NFA states"] # [doc = ""] # [doc = " Here is a very brief overview of each of the methods that add NFA states."] # [doc = " Every method adds a single state."] # [doc = ""] # [doc = " * [`add_empty`](Builder::add_empty): Add a state with a single"] # [doc = " unconditional epsilon transition to another state."] # [doc = " * [`add_union`](Builder::add_union): Adds a state with unconditional"] # [doc = " epsilon transitions to two or more states, with earlier transitions"] # [doc = " preferred over later ones."] # [doc = " * [`add_union_reverse`](Builder::add_union_reverse): Adds a state with"] # [doc = " unconditional epsilon transitions to two or more states, with later"] # [doc = " transitions preferred over earlier ones."] # [doc = " * [`add_range`](Builder::add_range): Adds a state with a single transition"] # [doc = " to another state that can only be followed if the current input byte is"] # [doc = " within the range given."] # [doc = " * [`add_sparse`](Builder::add_sparse): Adds a state with two or more"] # [doc = " range transitions to other states, where a transition is only followed"] # [doc = " if the current input byte is within one of the ranges. All transitions"] # [doc = " in this state have equal priority, and the corresponding ranges must be"] # [doc = " non-overlapping."] # [doc = " * [`add_look`](Builder::add_look): Adds a state with a single *conditional*"] # [doc = " epsilon transition to another state, where the condition depends on a"] # [doc = " limited look-around property."] # [doc = " * [`add_capture_start`](Builder::add_capture_start): Adds a state with"] # [doc = " a single unconditional epsilon transition that also instructs an NFA"] # [doc = " simulation to record the current input position to a specific location in"] # [doc = " memory. This is intended to represent the starting location of a capturing"] # [doc = " group."] # [doc = " * [`add_capture_end`](Builder::add_capture_end): Adds a state with"] # [doc = " a single unconditional epsilon transition that also instructs an NFA"] # [doc = " simulation to record the current input position to a specific location in"] # [doc = " memory. This is intended to represent the ending location of a capturing"] # [doc = " group."] # [doc = " * [`add_fail`](Builder::add_fail): Adds a state that never transitions to"] # [doc = " another state."] # [doc = " * [`add_match`](Builder::add_match): Add a state that indicates a match has"] # [doc = " been found for a particular pattern. A match state is a final state with"] # [doc = " no outgoing transitions."] # [doc = ""] # [doc = " ## Setting transitions between NFA states"] # [doc = ""] # [doc = " The [`Builder::patch`] method creates a transition from one state to the"] # [doc = " next. If the `from` state corresponds to a state that supports multiple"] # [doc = " outgoing transitions (such as \"union\"), then this adds the corresponding"] # [doc = " transition. Otherwise, it sets the single transition. (This routine panics"] # [doc = " if `from` corresponds to a state added by `add_sparse`, since sparse states"] # [doc = " need more specialized handling.)"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This annotated example shows how to hand construct the regex `[a-z]+`"] # [doc = " (without an unanchored prefix)."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::{pikevm::PikeVM, Builder, Transition},"] # [doc = "     util::primitives::StateID,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let mut builder = Builder::new();"] # [doc = " // Before adding NFA states for our pattern, we need to tell the builder"] # [doc = " // that we are starting the pattern."] # [doc = " builder.start_pattern()?;"] # [doc = " // Since we use the Pike VM below for searching, we need to add capturing"] # [doc = " // states. If you're just going to build a DFA from the NFA, then capturing"] # [doc = " // states do not need to be added."] # [doc = " let start = builder.add_capture_start(StateID::ZERO, 0, None)?;"] # [doc = " let range = builder.add_range(Transition {"] # [doc = "     // We don't know the state ID of the 'next' state yet, so we just fill"] # [doc = "     // in a dummy 'ZERO' value."] # [doc = "     start: b'a', end: b'z', next: StateID::ZERO,"] # [doc = " })?;"] # [doc = " // This state will point back to 'range', but also enable us to move ahead."] # [doc = " // That is, this implements the '+' repetition operator. We add 'range' and"] # [doc = " // then 'end' below to this alternation."] # [doc = " let alt = builder.add_union(vec![])?;"] # [doc = " // The final state before the match state, which serves to capture the"] # [doc = " // end location of the match."] # [doc = " let end = builder.add_capture_end(StateID::ZERO, 0)?;"] # [doc = " // The match state for our pattern."] # [doc = " let mat = builder.add_match()?;"] # [doc = " // Now we fill in the transitions between states."] # [doc = " builder.patch(start, range)?;"] # [doc = " builder.patch(range, alt)?;"] # [doc = " // If we added 'end' before 'range', then we'd implement non-greedy"] # [doc = " // matching, i.e., '+?'."] # [doc = " builder.patch(alt, range)?;"] # [doc = " builder.patch(alt, end)?;"] # [doc = " builder.patch(end, mat)?;"] # [doc = " // We must explicitly finish pattern and provide the starting state ID for"] # [doc = " // this particular pattern."] # [doc = " builder.finish_pattern(start)?;"] # [doc = " // Finally, when we build the NFA, we provide the anchored and unanchored"] # [doc = " // starting state IDs. Since we didn't bother with an unanchored prefix"] # [doc = " // here, we only support anchored searching. Thus, both starting states are"] # [doc = " // the same."] # [doc = " let nfa = builder.build(start, start)?;"] # [doc = ""] # [doc = " // Now build a Pike VM from our NFA, and use it for searching. This shows"] # [doc = " // how we can use a regex engine without ever worrying about syntax!"] # [doc = " let re = PikeVM::new_from_nfa(nfa)?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut caps = re.create_captures();"] # [doc = " let expected = Some(Match::must(0, 0..3));"] # [doc = " re.captures(&mut cache, \"foo0\", &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Debug , Default)] pub struct Builder { # [doc = " The ID of the pattern that we're currently building."] # [doc = ""] # [doc = " Callers are required to set (and unset) this by calling"] # [doc = " {start,finish}_pattern. Otherwise, most methods will panic."] pattern_id : Option < PatternID > , # [doc = " A sequence of intermediate NFA states. Once a state is added to this"] # [doc = " sequence, it is assigned a state ID equivalent to its index. Once a"] # [doc = " state is added, it is still expected to be mutated, e.g., to set its"] # [doc = " transition to a state that didn't exist at the time it was added."] states : Vec < State > , # [doc = " The starting states for each individual pattern. Starting at any"] # [doc = " of these states will result in only an anchored search for the"] # [doc = " corresponding pattern. The vec is indexed by pattern ID. When the NFA"] # [doc = " contains a single regex, then `start_pattern[0]` and `start_anchored`"] # [doc = " are always equivalent."] start_pattern : Vec < StateID > , # [doc = " A map from pattern ID to capture group index to name. (If no name"] # [doc = " exists, then a None entry is present. Thus, all capturing groups are"] # [doc = " present in this mapping.)"] # [doc = ""] # [doc = " The outer vec is indexed by pattern ID, while the inner vec is indexed"] # [doc = " by capture index offset for the corresponding pattern."] # [doc = ""] # [doc = " The first capture group for each pattern is always unnamed and is thus"] # [doc = " always None."] captures : Vec < Vec < Option < Arc < str > > > > , # [doc = " The combined memory used by each of the 'State's in 'states'. This"] # [doc = " only includes heap usage by each state, and not the size of the state"] # [doc = " itself. In other words, this tracks heap memory used that isn't"] # [doc = " captured via `size_of::<State>() * states.len()`."] memory_states : usize , # [doc = " Whether this NFA only matches UTF-8 and whether regex engines using"] # [doc = " this NFA for searching should report empty matches that split a"] # [doc = " codepoint."] utf8 : bool , # [doc = " Whether this NFA should be matched in reverse or not."] reverse : bool , # [doc = " The matcher to use for look-around assertions."] look_matcher : LookMatcher , # [doc = " A size limit to respect when building an NFA. If the total heap memory"] # [doc = " of the intermediate NFA states exceeds (or would exceed) this amount,"] # [doc = " then an error is returned."] size_limit : Option < usize > , }
};
}
