// Generated macro for Builder (struct)
macro_rules! Depcrate_dfa_denseBuilder {
() => {
// Module: crate::dfa::dense
// Provides: {"Builder"}
// Dependencies: {}
# [doc = " A builder for constructing a deterministic finite automaton from regular"] # [doc = " expressions."] # [doc = ""] # [doc = " This builder provides two main things:"] # [doc = ""] # [doc = " 1. It provides a few different `build` routines for actually constructing"] # [doc = " a DFA from different kinds of inputs. The most convenient is"] # [doc = " [`Builder::build`], which builds a DFA directly from a pattern string. The"] # [doc = " most flexible is [`Builder::build_from_nfa`], which builds a DFA straight"] # [doc = " from an NFA."] # [doc = " 2. The builder permits configuring a number of things."] # [doc = " [`Builder::configure`] is used with [`Config`] to configure aspects of"] # [doc = " the DFA and the construction process itself. [`Builder::syntax`] and"] # [doc = " [`Builder::thompson`] permit configuring the regex parser and Thompson NFA"] # [doc = " construction, respectively. The syntax and thompson configurations only"] # [doc = " apply when building from a pattern string."] # [doc = ""] # [doc = " This builder always constructs a *single* DFA. As such, this builder"] # [doc = " can only be used to construct regexes that either detect the presence"] # [doc = " of a match or find the end location of a match. A single DFA cannot"] # [doc = " produce both the start and end of a match. For that information, use a"] # [doc = " [`Regex`](crate::dfa::regex::Regex), which can be similarly configured"] # [doc = " using [`regex::Builder`](crate::dfa::regex::Builder). The main reason to"] # [doc = " use a DFA directly is if the end location of a match is enough for your use"] # [doc = " case. Namely, a `Regex` will construct two DFAs instead of one, since a"] # [doc = " second reverse DFA is needed to find the start of a match."] # [doc = ""] # [doc = " Note that if one wants to build a sparse DFA, you must first build a dense"] # [doc = " DFA and convert that to a sparse DFA. There is no way to build a sparse"] # [doc = " DFA without first building a dense DFA."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to build a minimized DFA that completely disables"] # [doc = " Unicode. That is:"] # [doc = ""] # [doc = " * Things such as `\\w`, `.` and `\\b` are no longer Unicode-aware. `\\w`"] # [doc = "   and `\\b` are ASCII-only while `.` matches any byte except for `\\n`"] # [doc = "   (instead of any UTF-8 encoding of a Unicode scalar value except for"] # [doc = "   `\\n`). Things that are Unicode only, such as `\\pL`, are not allowed."] # [doc = " * The pattern itself is permitted to match invalid UTF-8. For example,"] # [doc = "   things like `[^a]` that match any byte except for `a` are permitted."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     dfa::{Automaton, dense},"] # [doc = "     util::syntax,"] # [doc = "     HalfMatch, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let dfa = dense::Builder::new()"] # [doc = "     .configure(dense::Config::new().minimize(false))"] # [doc = "     .syntax(syntax::Config::new().unicode(false).utf8(false))"] # [doc = "     .build(r\"foo[^b]ar.*\")?;"] # [doc = ""] # [doc = " let haystack = b\"\\xFEfoo\\xFFar\\xE2\\x98\\xFF\\n\";"] # [doc = " let expected = Some(HalfMatch::must(0, 10));"] # [doc = " let got = dfa.try_search_fwd(&Input::new(haystack))?;"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "dfa-build")] # [derive (Clone , Debug)] pub struct Builder { config : Config , # [cfg (feature = "syntax")] thompson : thompson :: Compiler , }
};
}
