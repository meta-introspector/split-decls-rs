// Generated macro for impl_121 (impl)
macro_rules! Depcrate_dfa_regeximpl_121 {
() => {
// Module: crate::dfa::regex
// Provides: {"impl_121"}
// Dependencies: {}
# [doc = " Standard search routines for finding and iterating over matches."] impl < A : Automaton > Regex < A > { # [doc = " Returns true if and only if this regex matches the given haystack."] # [doc = ""] # [doc = " This routine may short circuit if it knows that scanning future input"] # [doc = " will never lead to a different result. In particular, if the underlying"] # [doc = " DFA enters a match state or a dead state, then this routine will return"] # [doc = " `true` or `false`, respectively, without inspecting any future input."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This routine panics if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the DFA quitting."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search panics, callers cannot know whether a match exists or"] # [doc = " not."] # [doc = ""] # [doc = " Use [`Regex::try_search`] if you want to handle these error conditions."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::dfa::regex::Regex;"] # [doc = ""] # [doc = " let re = Regex::new(\"foo[0-9]+bar\")?;"] # [doc = " assert_eq!(true, re.is_match(\"foo12345bar\"));"] # [doc = " assert_eq!(false, re.is_match(\"foobar\"));"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn is_match < 'h , I : Into < Input < 'h > > > (& self , input : I) -> bool { let input = input . into () . earliest (true) ; self . forward () . try_search_fwd (& input) . map (| x | x . is_some ()) . unwrap () } # [doc = " Returns the start and end offset of the leftmost match. If no match"] # [doc = " exists, then `None` is returned."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This routine panics if the search could not complete. This can occur"] # [doc = " in a number of circumstances:"] # [doc = ""] # [doc = " * The configuration of the DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the DFA quitting."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search panics, callers cannot know whether a match exists or"] # [doc = " not."] # [doc = ""] # [doc = " Use [`Regex::try_search`] if you want to handle these error conditions."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{Match, dfa::regex::Regex};"] # [doc = ""] # [doc = " // Greediness is applied appropriately."] # [doc = " let re = Regex::new(\"foo[0-9]+\")?;"] # [doc = " assert_eq!(Some(Match::must(0, 3..11)), re.find(\"zzzfoo12345zzz\"));"] # [doc = ""] # [doc = " // Even though a match is found after reading the first byte (`a`),"] # [doc = " // the default leftmost-first match semantics demand that we find the"] # [doc = " // earliest match that prefers earlier parts of the pattern over latter"] # [doc = " // parts."] # [doc = " let re = Regex::new(\"abc|a\")?;"] # [doc = " assert_eq!(Some(Match::must(0, 0..3)), re.find(\"abc\"));"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn find < 'h , I : Into < Input < 'h > > > (& self , input : I) -> Option < Match > { self . try_search (& input . into ()) . unwrap () } # [doc = " Returns an iterator over all non-overlapping leftmost matches in the"] # [doc = " given bytes. If no match exists, then the iterator yields no elements."] # [doc = ""] # [doc = " This corresponds to the \"standard\" regex search iterator."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the search returns an error during iteration, then iteration"] # [doc = " panics. See [`Regex::find`] for the panic conditions."] # [doc = ""] # [doc = " Use [`Regex::try_search`] with"] # [doc = " [`util::iter::Searcher`](crate::util::iter::Searcher) if you want to"] # [doc = " handle these error conditions."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{Match, dfa::regex::Regex};"] # [doc = ""] # [doc = " let re = Regex::new(\"foo[0-9]+\")?;"] # [doc = " let text = \"foo1 foo12 foo123\";"] # [doc = " let matches: Vec<Match> = re.find_iter(text).collect();"] # [doc = " assert_eq!(matches, vec!["] # [doc = "     Match::must(0, 0..4),"] # [doc = "     Match::must(0, 5..10),"] # [doc = "     Match::must(0, 11..17),"] # [doc = " ]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn find_iter < 'r , 'h , I : Into < Input < 'h > > > (& 'r self , input : I ,) -> FindMatches < 'r , 'h , A > { let it = iter :: Searcher :: new (input . into ()) ; FindMatches { re : self , it } } }
};
}
