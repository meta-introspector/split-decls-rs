// Generated macro for impl_36 (impl)
macro_rules! Depcrate_dfa_denseimpl_36 {
() => {
// Module: crate::dfa::dense
// Provides: {"impl_36"}
// Dependencies: {}
impl < T : AsRef < [u32] > > DFA < T > { # [doc = " Return the info about special states."] pub (crate) fn special (& self) -> & Special { & self . special } # [doc = " Return the info about special states as a mutable borrow."] # [cfg (feature = "dfa-build")] pub (crate) fn special_mut (& mut self) -> & mut Special { & mut self . special } # [doc = " Returns the quit set (may be empty) used by this DFA."] pub (crate) fn quitset (& self) -> & ByteSet { & self . quitset } # [doc = " Returns the flags for this DFA."] pub (crate) fn flags (& self) -> & Flags { & self . flags } # [doc = " Returns an iterator over all states in this DFA."] # [doc = ""] # [doc = " This iterator yields a tuple for each state. The first element of the"] # [doc = " tuple corresponds to a state's identifier, and the second element"] # [doc = " corresponds to the state itself (comprised of its transitions)."] pub (crate) fn states (& self) -> StateIter < '_ , T > { self . tt . states () } # [doc = " Return the total number of states in this DFA. Every DFA has at least"] # [doc = " 1 state, even the empty DFA."] pub (crate) fn state_len (& self) -> usize { self . tt . len () } # [doc = " Return an iterator over all pattern IDs for the given match state."] # [doc = ""] # [doc = " If the given state is not a match state, then this panics."] # [cfg (feature = "dfa-build")] pub (crate) fn pattern_id_slice (& self , id : StateID) -> & [PatternID] { assert ! (self . is_match_state (id)) ; self . ms . pattern_id_slice (self . match_state_index (id)) } # [doc = " Return the total number of pattern IDs for the given match state."] # [doc = ""] # [doc = " If the given state is not a match state, then this panics."] pub (crate) fn match_pattern_len (& self , id : StateID) -> usize { assert ! (self . is_match_state (id)) ; self . ms . pattern_len (self . match_state_index (id)) } # [doc = " Returns the total number of patterns matched by this DFA."] pub (crate) fn pattern_len (& self) -> usize { self . ms . pattern_len } # [doc = " Returns a map from match state ID to a list of pattern IDs that match"] # [doc = " in that state."] # [cfg (feature = "dfa-build")] pub (crate) fn pattern_map (& self) -> BTreeMap < StateID , Vec < PatternID > > { self . ms . to_map (self) } # [doc = " Returns the ID of the quit state for this DFA."] # [cfg (feature = "dfa-build")] pub (crate) fn quit_id (& self) -> StateID { self . to_state_id (1) } # [doc = " Convert the given state identifier to the state's index. The state's"] # [doc = " index corresponds to the position in which it appears in the transition"] # [doc = " table. When a DFA is NOT premultiplied, then a state's identifier is"] # [doc = " also its index. When a DFA is premultiplied, then a state's identifier"] # [doc = " is equal to `index * alphabet_len`. This routine reverses that."] pub (crate) fn to_index (& self , id : StateID) -> usize { self . tt . to_index (id) } # [doc = " Convert an index to a state (in the range 0..self.state_len()) to an"] # [doc = " actual state identifier."] # [doc = ""] # [doc = " This is useful when using a `Vec<T>` as an efficient map keyed by state"] # [doc = " to some other information (such as a remapped state ID)."] # [cfg (feature = "dfa-build")] pub (crate) fn to_state_id (& self , index : usize) -> StateID { self . tt . to_state_id (index) } # [doc = " Return the table of state IDs for this DFA's start states."] pub (crate) fn starts (& self) -> StartStateIter < '_ > { self . st . iter () } # [doc = " Returns the index of the match state for the given ID. If the"] # [doc = " given ID does not correspond to a match state, then this may"] # [doc = " panic or produce an incorrect result."] # [cfg_attr (feature = "perf-inline" , inline (always))] fn match_state_index (& self , id : StateID) -> usize { debug_assert ! (self . is_match_state (id)) ; let min = self . special () . min_match . as_usize () ; self . to_index (StateID :: new_unchecked (id . as_usize () - min)) } # [doc = " Returns the index of the accelerator state for the given ID. If the"] # [doc = " given ID does not correspond to an accelerator state, then this may"] # [doc = " panic or produce an incorrect result."] fn accelerator_index (& self , id : StateID) -> usize { let min = self . special () . min_accel . as_usize () ; self . to_index (StateID :: new_unchecked (id . as_usize () - min)) } # [doc = " Return the accelerators for this DFA."] fn accels (& self) -> Accels < & [u32] > { self . accels . as_ref () } # [doc = " Return this DFA's transition table as a slice."] fn trans (& self) -> & [StateID] { self . tt . table () } }
};
}
