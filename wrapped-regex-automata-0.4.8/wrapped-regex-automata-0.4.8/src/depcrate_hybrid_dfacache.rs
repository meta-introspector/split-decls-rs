// Generated macro for Cache (struct)
macro_rules! Depcrate_hybrid_dfaCache {
() => {
// Module: crate::hybrid::dfa
// Provides: {"Cache"}
// Dependencies: {}
# [doc = " A cache represents a partially computed DFA."] # [doc = ""] # [doc = " A cache is the key component that differentiates a classical DFA and a"] # [doc = " hybrid NFA/DFA (also called a \"lazy DFA\"). Where a classical DFA builds a"] # [doc = " complete transition table that can handle all possible inputs, a hybrid"] # [doc = " NFA/DFA starts with an empty transition table and builds only the parts"] # [doc = " required during search. The parts that are built are stored in a cache. For"] # [doc = " this reason, a cache is a required parameter for nearly every operation on"] # [doc = " a [`DFA`]."] # [doc = ""] # [doc = " Caches can be created from their corresponding DFA via"] # [doc = " [`DFA::create_cache`]. A cache can only be used with either the DFA that"] # [doc = " created it, or the DFA that was most recently used to reset it with"] # [doc = " [`Cache::reset`]. Using a cache with any other DFA may result in panics"] # [doc = " or incorrect results."] # [derive (Clone , Debug)] pub struct Cache { # [doc = " The transition table."] # [doc = ""] # [doc = " Given a `current` LazyStateID and an `input` byte, the next state can"] # [doc = " be computed via `trans[untagged(current) + equiv_class(input)]`. Notice"] # [doc = " that no multiplication is used. That's because state identifiers are"] # [doc = " \"premultiplied.\""] # [doc = ""] # [doc = " Note that the next state may be the \"unknown\" state. In this case, the"] # [doc = " next state is not known and determinization for `current` on `input`"] # [doc = " must be performed."] trans : Vec < LazyStateID > , # [doc = " The starting states for this DFA."] # [doc = ""] # [doc = " These are computed lazily. Initially, these are all set to \"unknown\""] # [doc = " lazy state IDs."] # [doc = ""] # [doc = " When 'starts_for_each_pattern' is disabled (the default), then the size"] # [doc = " of this is constrained to the possible starting configurations based"] # [doc = " on the search parameters. (At time of writing, that's 4.) However,"] # [doc = " when starting states for each pattern is enabled, then there are N"] # [doc = " additional groups of starting states, where each group reflects the"] # [doc = " different possible configurations and N is the number of patterns."] starts : Vec < LazyStateID > , # [doc = " A sequence of NFA/DFA powerset states that have been computed for this"] # [doc = " lazy DFA. This sequence is indexable by untagged LazyStateIDs. (Every"] # [doc = " tagged LazyStateID can be used to index this sequence by converting it"] # [doc = " to its untagged form.)"] states : Vec < State > , # [doc = " A map from states to their corresponding IDs. This map may be accessed"] # [doc = " via the raw byte representation of a state, which means that a `State`"] # [doc = " does not need to be allocated to determine whether it already exists"] # [doc = " in this map. Indeed, the existence of such a state is what determines"] # [doc = " whether we allocate a new `State` or not."] # [doc = ""] # [doc = " The higher level idea here is that we do just enough determinization"] # [doc = " for a state to check whether we've already computed it. If we have,"] # [doc = " then we can save a little (albeit not much) work. The real savings is"] # [doc = " in memory usage. If we never checked for trivially duplicate states,"] # [doc = " then our memory usage would explode to unreasonable levels."] states_to_id : StateMap , # [doc = " Sparse sets used to track which NFA states have been visited during"] # [doc = " various traversals."] sparses : SparseSets , # [doc = " Scratch space for traversing the NFA graph. (We use space on the heap"] # [doc = " instead of the call stack.)"] stack : Vec < NFAStateID > , # [doc = " Scratch space for building a NFA/DFA powerset state. This is used to"] # [doc = " help amortize allocation since not every powerset state generated is"] # [doc = " added to the cache. In particular, if it already exists in the cache,"] # [doc = " then there is no need to allocate a new `State` for it."] scratch_state_builder : StateBuilderEmpty , # [doc = " A simple abstraction for handling the saving of at most a single state"] # [doc = " across a cache clearing. This is required for correctness. Namely, if"] # [doc = " adding a new state after clearing the cache fails, then the caller"] # [doc = " must retain the ability to continue using the state ID given. The"] # [doc = " state corresponding to the state ID is what we preserve across cache"] # [doc = " clearings."] state_saver : StateSaver , # [doc = " The memory usage, in bytes, used by 'states' and 'states_to_id'. We"] # [doc = " track this as new states are added since states use a variable amount"] # [doc = " of heap. Tracking this as we add states makes it possible to compute"] # [doc = " the total amount of memory used by the determinizer in constant time."] memory_usage_state : usize , # [doc = " The number of times the cache has been cleared. When a minimum cache"] # [doc = " clear count is set, then the cache will return an error instead of"] # [doc = " clearing the cache if the count has been exceeded."] clear_count : usize , # [doc = " The total number of bytes searched since the last time this cache was"] # [doc = " cleared, not including the current search."] # [doc = ""] # [doc = " This can be added to the length of the current search to get the true"] # [doc = " total number of bytes searched."] # [doc = ""] # [doc = " This is generally only non-zero when the"] # [doc = " `Cache::search_{start,update,finish}` APIs are used to track search"] # [doc = " progress."] bytes_searched : usize , # [doc = " The progress of the current search."] # [doc = ""] # [doc = " This is only non-`None` when callers utlize the `Cache::search_start`,"] # [doc = " `Cache::search_update` and `Cache::search_finish` APIs."] # [doc = ""] # [doc = " The purpose of recording search progress is to be able to make a"] # [doc = " determination about the efficiency of the cache. Namely, by keeping"] # [doc = " track of the"] progress : Option < SearchProgress > , }
};
}
