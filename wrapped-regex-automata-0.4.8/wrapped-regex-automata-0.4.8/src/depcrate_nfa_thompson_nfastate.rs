// Generated macro for State (enum)
macro_rules! Depcrate_nfa_thompson_nfaState {
() => {
// Module: crate::nfa::thompson::nfa
// Provides: {"State"}
// Dependencies: {}
# [doc = " A state in an NFA."] # [doc = ""] # [doc = " In theory, it can help to conceptualize an `NFA` as a graph consisting of"] # [doc = " `State`s. Each `State` contains its complete set of outgoing transitions."] # [doc = ""] # [doc = " In practice, it can help to conceptualize an `NFA` as a sequence of"] # [doc = " instructions for a virtual machine. Each `State` says what to do and where"] # [doc = " to go next."] # [doc = ""] # [doc = " Strictly speaking, the practical interpretation is the most correct one,"] # [doc = " because of the [`Capture`](State::Capture) state. Namely, a `Capture`"] # [doc = " state always forwards execution to another state unconditionally. Its only"] # [doc = " purpose is to cause a side effect: the recording of the current input"] # [doc = " position at a particular location in memory. In this sense, an `NFA`"] # [doc = " has more power than a theoretical non-deterministic finite automaton."] # [doc = ""] # [doc = " For most uses of this crate, it is likely that one may never even need to"] # [doc = " be aware of this type at all. The main use cases for looking at `State`s"] # [doc = " directly are if you need to write your own search implementation or if you"] # [doc = " need to do some kind of analysis on the NFA."] # [derive (Clone , Eq , PartialEq)] pub enum State { # [doc = " A state with a single transition that can only be taken if the current"] # [doc = " input symbol is in a particular range of bytes."] ByteRange { # [doc = " The transition from this state to the next."] trans : Transition , } , # [doc = " A state with possibly many transitions represented in a sparse fashion."] # [doc = " Transitions are non-overlapping and ordered lexicographically by input"] # [doc = " range."] # [doc = ""] # [doc = " In practice, this is used for encoding UTF-8 automata. Its presence is"] # [doc = " primarily an optimization that avoids many additional unconditional"] # [doc = " epsilon transitions (via [`Union`](State::Union) states), and thus"] # [doc = " decreases the overhead of traversing the NFA. This can improve both"] # [doc = " matching time and DFA construction time."] Sparse (SparseTransitions) , # [doc = " A dense representation of a state with multiple transitions."] Dense (DenseTransitions) , # [doc = " A conditional epsilon transition satisfied via some sort of"] # [doc = " look-around. Look-around is limited to anchor and word boundary"] # [doc = " assertions."] # [doc = ""] # [doc = " Look-around states are meant to be evaluated while performing epsilon"] # [doc = " closure (computing the set of states reachable from a particular state"] # [doc = " via only epsilon transitions). If the current position in the haystack"] # [doc = " satisfies the look-around assertion, then you're permitted to follow"] # [doc = " that epsilon transition."] Look { # [doc = " The look-around assertion that must be satisfied before moving"] # [doc = " to `next`."] look : Look , # [doc = " The state to transition to if the look-around assertion is"] # [doc = " satisfied."] next : StateID , } , # [doc = " An alternation such that there exists an epsilon transition to all"] # [doc = " states in `alternates`, where matches found via earlier transitions"] # [doc = " are preferred over later transitions."] Union { # [doc = " An ordered sequence of unconditional epsilon transitions to other"] # [doc = " states. Transitions earlier in the sequence are preferred over"] # [doc = " transitions later in the sequence."] alternates : Box < [StateID] > , } , # [doc = " An alternation such that there exists precisely two unconditional"] # [doc = " epsilon transitions, where matches found via `alt1` are preferred over"] # [doc = " matches found via `alt2`."] # [doc = ""] # [doc = " This state exists as a common special case of Union where there are"] # [doc = " only two alternates. In this case, we don't need any allocations to"] # [doc = " represent the state. This saves a bit of memory and also saves an"] # [doc = " additional memory access when traversing the NFA."] BinaryUnion { # [doc = " An unconditional epsilon transition to another NFA state. This"] # [doc = " is preferred over `alt2`."] alt1 : StateID , # [doc = " An unconditional epsilon transition to another NFA state. Matches"] # [doc = " reported via this transition should only be reported if no matches"] # [doc = " were found by following `alt1`."] alt2 : StateID , } , # [doc = " An empty state that records a capture location."] # [doc = ""] # [doc = " From the perspective of finite automata, this is precisely equivalent"] # [doc = " to an unconditional epsilon transition, but serves the purpose of"] # [doc = " instructing NFA simulations to record additional state when the finite"] # [doc = " state machine passes through this epsilon transition."] # [doc = ""] # [doc = " `slot` in this context refers to the specific capture group slot"] # [doc = " offset that is being recorded. Each capturing group has two slots"] # [doc = " corresponding to the start and end of the matching portion of that"] # [doc = " group."] # [doc = ""] # [doc = " The pattern ID and capture group index are also included in this state"] # [doc = " in case they are useful. But mostly, all you'll need is `next` and"] # [doc = " `slot`."] Capture { # [doc = " The state to transition to, unconditionally."] next : StateID , # [doc = " The pattern ID that this capture belongs to."] pattern_id : PatternID , # [doc = " The capture group index that this capture belongs to. Capture group"] # [doc = " indices are local to each pattern. For example, when capturing"] # [doc = " groups are enabled, every pattern has a capture group at index"] # [doc = " `0`."] group_index : SmallIndex , # [doc = " The slot index for this capture. Every capturing group has two"] # [doc = " slots: one for the start haystack offset and one for the end"] # [doc = " haystack offset. Unlike capture group indices, slot indices are"] # [doc = " global across all patterns in this NFA. That is, each slot belongs"] # [doc = " to a single pattern, but there is only one slot at index `i`."] slot : SmallIndex , } , # [doc = " A state that cannot be transitioned out of. This is useful for cases"] # [doc = " where you want to prevent matching from occurring. For example, if your"] # [doc = " regex parser permits empty character classes, then one could choose"] # [doc = " a `Fail` state to represent them. (An empty character class can be"] # [doc = " thought of as an empty set. Since nothing is in an empty set, they can"] # [doc = " never match anything.)"] Fail , # [doc = " A match state. There is at least one such occurrence of this state for"] # [doc = " each regex that can match that is in this NFA."] Match { # [doc = " The matching pattern ID."] pattern_id : PatternID , } , }
};
}
