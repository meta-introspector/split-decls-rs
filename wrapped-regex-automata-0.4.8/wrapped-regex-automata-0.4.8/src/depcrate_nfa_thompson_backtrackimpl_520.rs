// Generated macro for impl_520 (impl)
macro_rules! Depcrate_nfa_thompson_backtrackimpl_520 {
() => {
// Module: crate::nfa::thompson::backtrack
// Provides: {"impl_520"}
// Dependencies: {}
impl Cache { # [doc = " Create a new [`BoundedBacktracker`] cache."] # [doc = ""] # [doc = " A potentially more convenient routine to create a cache is"] # [doc = " [`BoundedBacktracker::create_cache`], as it does not require also"] # [doc = " importing the `Cache` type."] # [doc = ""] # [doc = " If you want to reuse the returned `Cache` with some other"] # [doc = " `BoundedBacktracker`, then you must call [`Cache::reset`] with the"] # [doc = " desired `BoundedBacktracker`."] pub fn new (re : & BoundedBacktracker) -> Cache { Cache { stack : vec ! [] , visited : Visited :: new (re) } } # [doc = " Reset this cache such that it can be used for searching with different"] # [doc = " [`BoundedBacktracker`]."] # [doc = ""] # [doc = " A cache reset permits reusing memory already allocated in this cache"] # [doc = " with a different `BoundedBacktracker`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to re-purpose a cache for use with a different"] # [doc = " `BoundedBacktracker`."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::backtrack::BoundedBacktracker,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re1 = BoundedBacktracker::new(r\"\\w\")?;"] # [doc = " let re2 = BoundedBacktracker::new(r\"\\W\")?;"] # [doc = ""] # [doc = " let mut cache = re1.create_cache();"] # [doc = " assert_eq!("] # [doc = "     Some(Ok(Match::must(0, 0..2))),"] # [doc = "     re1.try_find_iter(&mut cache, \"Δ\").next(),"] # [doc = " );"] # [doc = ""] # [doc = " // Using 'cache' with re2 is not allowed. It may result in panics or"] # [doc = " // incorrect results. In order to re-purpose the cache, we must reset"] # [doc = " // it with the BoundedBacktracker we'd like to use it with."] # [doc = " //"] # [doc = " // Similarly, after this reset, using the cache with 're1' is also not"] # [doc = " // allowed."] # [doc = " cache.reset(&re2);"] # [doc = " assert_eq!("] # [doc = "     Some(Ok(Match::must(0, 0..3))),"] # [doc = "     re2.try_find_iter(&mut cache, \"☃\").next(),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn reset (& mut self , re : & BoundedBacktracker) { self . visited . reset (re) ; } # [doc = " Returns the heap memory usage, in bytes, of this cache."] # [doc = ""] # [doc = " This does **not** include the stack size used up by this cache. To"] # [doc = " compute that, use `std::mem::size_of::<Cache>()`."] pub fn memory_usage (& self) -> usize { self . stack . len () * core :: mem :: size_of :: < Frame > () + self . visited . memory_usage () } # [doc = " Clears this cache. This should be called at the start of every search"] # [doc = " to ensure we start with a clean slate."] # [doc = ""] # [doc = " This also sets the length of the capturing groups used in the current"] # [doc = " search. This permits an optimization where by 'SlotTable::for_state'"] # [doc = " only returns the number of slots equivalent to the number of slots"] # [doc = " given in the 'Captures' value. This may be less than the total number"] # [doc = " of possible slots, e.g., when one only wants to track overall match"] # [doc = " offsets. This in turn permits less copying of capturing group spans"] # [doc = " in the BoundedBacktracker."] fn setup_search (& mut self , re : & BoundedBacktracker , input : & Input < '_ > ,) -> Result < () , MatchError > { self . stack . clear () ; self . visited . setup_search (re , input) ? ; Ok (()) } }
};
}
