// Generated macro for impl_90 (impl)
macro_rules! Depcrate_dfa_onepassimpl_90 {
() => {
// Module: crate::dfa::onepass
// Provides: {"impl_90"}
// Dependencies: {}
impl Cache { # [doc = " Create a new [`onepass::DFA`](DFA) cache."] # [doc = ""] # [doc = " A potentially more convenient routine to create a cache is"] # [doc = " [`DFA::create_cache`], as it does not require also importing the"] # [doc = " `Cache` type."] # [doc = ""] # [doc = " If you want to reuse the returned `Cache` with some other one-pass DFA,"] # [doc = " then you must call [`Cache::reset`] with the desired one-pass DFA."] pub fn new (re : & DFA) -> Cache { let mut cache = Cache { explicit_slots : vec ! [] , explicit_slot_len : 0 } ; cache . reset (re) ; cache } # [doc = " Reset this cache such that it can be used for searching with a"] # [doc = " different [`onepass::DFA`](DFA)."] # [doc = ""] # [doc = " A cache reset permits reusing memory already allocated in this cache"] # [doc = " with a different one-pass DFA."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This shows how to re-purpose a cache for use with a different one-pass"] # [doc = " DFA."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{dfa::onepass::DFA, Match};"] # [doc = ""] # [doc = " let re1 = DFA::new(r\"\\w\")?;"] # [doc = " let re2 = DFA::new(r\"\\W\")?;"] # [doc = " let mut caps1 = re1.create_captures();"] # [doc = " let mut caps2 = re2.create_captures();"] # [doc = ""] # [doc = " let mut cache = re1.create_cache();"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 0..2)),"] # [doc = "     { re1.captures(&mut cache, \"Δ\", &mut caps1); caps1.get_match() },"] # [doc = " );"] # [doc = ""] # [doc = " // Using 'cache' with re2 is not allowed. It may result in panics or"] # [doc = " // incorrect results. In order to re-purpose the cache, we must reset"] # [doc = " // it with the one-pass DFA we'd like to use it with."] # [doc = " //"] # [doc = " // Similarly, after this reset, using the cache with 're1' is also not"] # [doc = " // allowed."] # [doc = " re2.reset_cache(&mut cache);"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 0..3)),"] # [doc = "     { re2.captures(&mut cache, \"☃\", &mut caps2); caps2.get_match() },"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn reset (& mut self , re : & DFA) { let explicit_slot_len = re . get_nfa () . group_info () . explicit_slot_len () ; self . explicit_slots . resize (explicit_slot_len , None) ; self . explicit_slot_len = explicit_slot_len ; } # [doc = " Returns the heap memory usage, in bytes, of this cache."] # [doc = ""] # [doc = " This does **not** include the stack size used up by this cache. To"] # [doc = " compute that, use `std::mem::size_of::<Cache>()`."] pub fn memory_usage (& self) -> usize { self . explicit_slots . len () * core :: mem :: size_of :: < Option < NonMaxUsize > > () } fn explicit_slots (& mut self) -> & mut [Option < NonMaxUsize >] { & mut self . explicit_slots [.. self . explicit_slot_len] } fn setup_search (& mut self , explicit_slot_len : usize) { self . explicit_slot_len = explicit_slot_len ; } }
};
}
