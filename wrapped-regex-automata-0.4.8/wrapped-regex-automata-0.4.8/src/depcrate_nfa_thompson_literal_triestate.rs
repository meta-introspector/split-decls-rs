// Generated macro for State (struct)
macro_rules! Depcrate_nfa_thompson_literal_trieState {
() => {
// Module: crate::nfa::thompson::literal_trie
// Provides: {"State"}
// Dependencies: {}
# [doc = " A state in a trie."] # [doc = ""] # [doc = " This uses a sparse representation. Since we don't use literal tries"] # [doc = " for searching, and ultimately (and compilation requires visiting every"] # [doc = " transition anyway), we use a sparse representation for transitions. This"] # [doc = " means we save on memory, at the expense of 'LiteralTrie::add' being perhaps"] # [doc = " a bit slower."] # [doc = ""] # [doc = " While 'transitions' is pretty standard as far as tries goes, the 'chunks'"] # [doc = " piece here is more unusual. In effect, 'chunks' defines a partitioning"] # [doc = " of 'transitions', where each chunk corresponds to a distinct set of"] # [doc = " transitions. The key invariant is that a transition in one chunk cannot"] # [doc = " be moved to another chunk. This is the secret sauce that preserve"] # [doc = " leftmost-first match semantics."] # [doc = ""] # [doc = " A new chunk is added whenever we mark a state as a match state. Once a"] # [doc = " new chunk is added, the old active chunk is frozen and is never mutated"] # [doc = " again. The new chunk becomes the active chunk, which is defined as"] # [doc = " '&transitions[chunks.last().map_or(0, |c| c.1)..]'. Thus, a state where"] # [doc = " 'chunks' is empty actually contains one chunk. Thus, every state contains"] # [doc = " at least one (possibly empty) chunk."] # [doc = ""] # [doc = " A \"leaf\" state is a state that has no outgoing transitions (so"] # [doc = " 'transitions' is empty). Note that there is no way for a leaf state to be a"] # [doc = " non-matching state. (Although while building the trie, within 'add', a leaf"] # [doc = " state may exist while not containing any matches. But this invariant is"] # [doc = " only broken within 'add'. Once 'add' returns, the invariant is upheld.)"] # [derive (Clone , Default)] struct State { transitions : Vec < Transition > , chunks : Vec < (usize , usize) > , }
};
}
