// Generated macro for Regex (struct)
macro_rules! Depcrate_hybrid_regexRegex {
() => {
// Module: crate::hybrid::regex
// Provides: {"Regex"}
// Dependencies: {}
# [doc = " A regular expression that uses hybrid NFA/DFAs (also called \"lazy DFAs\")"] # [doc = " for searching."] # [doc = ""] # [doc = " A regular expression is comprised of two lazy DFAs, a \"forward\" DFA and a"] # [doc = " \"reverse\" DFA. The forward DFA is responsible for detecting the end of"] # [doc = " a match while the reverse DFA is responsible for detecting the start"] # [doc = " of a match. Thus, in order to find the bounds of any given match, a"] # [doc = " forward search must first be run followed by a reverse search. A match"] # [doc = " found by the forward DFA guarantees that the reverse DFA will also find"] # [doc = " a match."] # [doc = ""] # [doc = " # Fallibility"] # [doc = ""] # [doc = " Most of the search routines defined on this type will _panic_ when the"] # [doc = " underlying search fails. This might be because the DFA gave up because it"] # [doc = " saw a quit byte, whether configured explicitly or via heuristic Unicode"] # [doc = " word boundary support, although neither are enabled by default. It might"] # [doc = " also fail if the underlying DFA determines it isn't making effective use of"] # [doc = " the cache (which also never happens by default). Or it might fail because"] # [doc = " an invalid `Input` configuration is given, for example, with an unsupported"] # [doc = " [`Anchored`] mode."] # [doc = ""] # [doc = " If you need to handle these error cases instead of allowing them to trigger"] # [doc = " a panic, then the lower level [`Regex::try_search`] provides a fallible API"] # [doc = " that never panics."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to cause a search to terminate if it sees a"] # [doc = " `\\n` byte, and handle the error returned. This could be useful if, for"] # [doc = " example, you wanted to prevent a user supplied pattern from matching"] # [doc = " across a line boundary."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{hybrid::{dfa, regex::Regex}, Input, MatchError};"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .dfa(dfa::Config::new().quit(b'\\n', true))"] # [doc = "     .build(r\"foo\\p{any}+bar\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let input = Input::new(\"foo\\nbar\");"] # [doc = " // Normally this would produce a match, since \\p{any} contains '\\n'."] # [doc = " // But since we instructed the automaton to enter a quit state if a"] # [doc = " // '\\n' is observed, this produces a match error instead."] # [doc = " let expected = MatchError::quit(b'\\n', 3);"] # [doc = " let got = re.try_search(&mut cache, &input).unwrap_err();"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Debug)] pub struct Regex { # [doc = " The forward lazy DFA. This can only find the end of a match."] forward : DFA , # [doc = " The reverse lazy DFA. This can only find the start of a match."] # [doc = ""] # [doc = " This is built with 'all' match semantics (instead of leftmost-first)"] # [doc = " so that it always finds the longest possible match (which corresponds"] # [doc = " to the leftmost starting position). It is also compiled as an anchored"] # [doc = " matcher and has 'starts_for_each_pattern' enabled. Including starting"] # [doc = " states for each pattern is necessary to ensure that we only look for"] # [doc = " matches of a pattern that matched in the forward direction. Otherwise,"] # [doc = " we might wind up finding the \"leftmost\" starting position of a totally"] # [doc = " different pattern!"] reverse : DFA , }
};
}
