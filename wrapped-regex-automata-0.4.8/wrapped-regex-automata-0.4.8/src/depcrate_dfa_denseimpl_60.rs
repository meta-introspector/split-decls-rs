// Generated macro for impl_60 (impl)
macro_rules! Depcrate_dfa_denseimpl_60 {
() => {
// Module: crate::dfa::dense
// Provides: {"impl_60"}
// Dependencies: {}
impl < 'a > State < 'a > { # [doc = " Return an iterator over all transitions in this state. This yields"] # [doc = " a number of transitions equivalent to the alphabet length of the"] # [doc = " corresponding DFA."] # [doc = ""] # [doc = " Each transition is represented by a tuple. The first element is"] # [doc = " the input byte for that transition and the second element is the"] # [doc = " transitions itself."] pub (crate) fn transitions (& self) -> StateTransitionIter < '_ > { StateTransitionIter { len : self . transitions . len () , it : self . transitions . iter () . enumerate () , } } # [doc = " Return an iterator over a sparse representation of the transitions in"] # [doc = " this state. Only non-dead transitions are returned."] # [doc = ""] # [doc = " The \"sparse\" representation in this case corresponds to a sequence of"] # [doc = " triples. The first two elements of the triple comprise an inclusive"] # [doc = " byte range while the last element corresponds to the transition taken"] # [doc = " for all bytes in the range."] # [doc = ""] # [doc = " This is somewhat more condensed than the classical sparse"] # [doc = " representation (where you have an element for every non-dead"] # [doc = " transition), but in practice, checking if a byte is in a range is very"] # [doc = " cheap and using ranges tends to conserve quite a bit more space."] pub (crate) fn sparse_transitions (& self) -> StateSparseTransitionIter < '_ > { StateSparseTransitionIter { dense : self . transitions () , cur : None } } # [doc = " Returns the identifier for this state."] pub (crate) fn id (& self) -> StateID { self . id } # [doc = " Analyzes this state to determine whether it can be accelerated. If so,"] # [doc = " it returns an accelerator that contains at least one byte."] # [cfg (feature = "dfa-build")] fn accelerate (& self , classes : & ByteClasses) -> Option < Accel > { let mut accel = Accel :: new () ; for (class , id) in self . transitions () { if id == self . id () { continue ; } for unit in classes . elements (class) { if let Some (byte) = unit . as_u8 () { if ! accel . add (byte) { return None ; } } } } if accel . is_empty () { None } else { Some (accel) } } }
};
}
