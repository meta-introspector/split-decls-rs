// Generated macro for impl_389 (impl)
macro_rules! Depcrate_meta_regeximpl_389 {
() => {
// Module: crate::meta::regex
// Provides: {"impl_389"}
// Dependencies: {}
# [doc = " Lower level search routines that give more control, and require the caller"] # [doc = " to provide an explicit [`Cache`] parameter."] impl Regex { # [doc = " This is like [`Regex::search`], but requires the caller to"] # [doc = " explicitly pass a [`Cache`]."] # [doc = ""] # [doc = " # Why pass a `Cache` explicitly?"] # [doc = ""] # [doc = " Passing a `Cache` explicitly will bypass the use of an internal memory"] # [doc = " pool used by `Regex` to get a `Cache` for a search. The use of this"] # [doc = " pool can be slower in some cases when a `Regex` is used from multiple"] # [doc = " threads simultaneously. Typically, performance only becomes an issue"] # [doc = " when there is heavy contention, which in turn usually only occurs"] # [doc = " when each thread's primary unit of work is a regex search on a small"] # [doc = " haystack."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Input, Match};"] # [doc = ""] # [doc = " let re = Regex::new(r\"Samwise|Sam\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let input = Input::new("] # [doc = "     \"one of the chief characters, Samwise the Brave\","] # [doc = " );"] # [doc = " assert_eq!("] # [doc = "     Some(Match::must(0, 29..36)),"] # [doc = "     re.search_with(&mut cache, &input),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn search_with (& self , cache : & mut Cache , input : & Input < '_ > ,) -> Option < Match > { if self . imp . info . is_impossible (input) { return None ; } self . imp . strat . search (cache , input) } # [doc = " This is like [`Regex::search_half`], but requires the caller to"] # [doc = " explicitly pass a [`Cache`]."] # [doc = ""] # [doc = " # Why pass a `Cache` explicitly?"] # [doc = ""] # [doc = " Passing a `Cache` explicitly will bypass the use of an internal memory"] # [doc = " pool used by `Regex` to get a `Cache` for a search. The use of this"] # [doc = " pool can be slower in some cases when a `Regex` is used from multiple"] # [doc = " threads simultaneously. Typically, performance only becomes an issue"] # [doc = " when there is heavy contention, which in turn usually only occurs"] # [doc = " when each thread's primary unit of work is a regex search on a small"] # [doc = " haystack."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Input, HalfMatch};"] # [doc = ""] # [doc = " let re = Regex::new(r\"Samwise|Sam\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let input = Input::new("] # [doc = "     \"one of the chief characters, Samwise the Brave\","] # [doc = " );"] # [doc = " assert_eq!("] # [doc = "     Some(HalfMatch::must(0, 36)),"] # [doc = "     re.search_half_with(&mut cache, &input),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn search_half_with (& self , cache : & mut Cache , input : & Input < '_ > ,) -> Option < HalfMatch > { if self . imp . info . is_impossible (input) { return None ; } self . imp . strat . search_half (cache , input) } # [doc = " This is like [`Regex::search_captures`], but requires the caller to"] # [doc = " explicitly pass a [`Cache`]."] # [doc = ""] # [doc = " # Why pass a `Cache` explicitly?"] # [doc = ""] # [doc = " Passing a `Cache` explicitly will bypass the use of an internal memory"] # [doc = " pool used by `Regex` to get a `Cache` for a search. The use of this"] # [doc = " pool can be slower in some cases when a `Regex` is used from multiple"] # [doc = " threads simultaneously. Typically, performance only becomes an issue"] # [doc = " when there is heavy contention, which in turn usually only occurs"] # [doc = " when each thread's primary unit of work is a regex search on a small"] # [doc = " haystack."] # [doc = ""] # [doc = " # Example: specific pattern search"] # [doc = ""] # [doc = " This example shows how to build a multi-pattern `Regex` that permits"] # [doc = " searching for specific patterns."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     meta::Regex,"] # [doc = "     Anchored, Match, PatternID, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let re = Regex::new_many(&[\"[a-z0-9]{6}\", \"[a-z][a-z0-9]{5}\"])?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = " let haystack = \"foo123\";"] # [doc = ""] # [doc = " // Since we are using the default leftmost-first match and both"] # [doc = " // patterns match at the same starting position, only the first pattern"] # [doc = " // will be returned in this case when doing a search for any of the"] # [doc = " // patterns."] # [doc = " let expected = Some(Match::must(0, 0..6));"] # [doc = " re.search_captures_with(&mut cache, &Input::new(haystack), &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " // But if we want to check whether some other pattern matches, then we"] # [doc = " // can provide its pattern ID."] # [doc = " let expected = Some(Match::must(1, 0..6));"] # [doc = " let input = Input::new(haystack)"] # [doc = "     .anchored(Anchored::Pattern(PatternID::must(1)));"] # [doc = " re.search_captures_with(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: specifying the bounds of a search"] # [doc = ""] # [doc = " This example shows how providing the bounds of a search can produce"] # [doc = " different results than simply sub-slicing the haystack."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{meta::Regex, Match, Input};"] # [doc = ""] # [doc = " let re = Regex::new(r\"\\b[0-9]{3}\\b\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = " let haystack = \"foo123bar\";"] # [doc = ""] # [doc = " // Since we sub-slice the haystack, the search doesn't know about"] # [doc = " // the larger context and assumes that `123` is surrounded by word"] # [doc = " // boundaries. And of course, the match position is reported relative"] # [doc = " // to the sub-slice as well, which means we get `0..3` instead of"] # [doc = " // `3..6`."] # [doc = " let expected = Some(Match::must(0, 0..3));"] # [doc = " let input = Input::new(&haystack[3..6]);"] # [doc = " re.search_captures_with(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " // But if we provide the bounds of the search within the context of the"] # [doc = " // entire haystack, then the search can take the surrounding context"] # [doc = " // into account. (And if we did find a match, it would be reported"] # [doc = " // as a valid offset into `haystack` instead of its sub-slice.)"] # [doc = " let expected = None;"] # [doc = " let input = Input::new(haystack).range(3..6);"] # [doc = " re.search_captures_with(&mut cache, &input, &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn search_captures_with (& self , cache : & mut Cache , input : & Input < '_ > , caps : & mut Captures ,) { caps . set_pattern (None) ; let pid = self . search_slots_with (cache , input , caps . slots_mut ()) ; caps . set_pattern (pid) ; } # [doc = " This is like [`Regex::search_slots`], but requires the caller to"] # [doc = " explicitly pass a [`Cache`]."] # [doc = ""] # [doc = " # Why pass a `Cache` explicitly?"] # [doc = ""] # [doc = " Passing a `Cache` explicitly will bypass the use of an internal memory"] # [doc = " pool used by `Regex` to get a `Cache` for a search. The use of this"] # [doc = " pool can be slower in some cases when a `Regex` is used from multiple"] # [doc = " threads simultaneously. Typically, performance only becomes an issue"] # [doc = " when there is heavy contention, which in turn usually only occurs"] # [doc = " when each thread's primary unit of work is a regex search on a small"] # [doc = " haystack."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to find the overall match offsets in a"] # [doc = " multi-pattern search without allocating a `Captures` value. Indeed, we"] # [doc = " can put our slots right on the stack."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{meta::Regex, PatternID, Input};"] # [doc = ""] # [doc = " let re = Regex::new_many(&["] # [doc = "     r\"\\pL+\","] # [doc = "     r\"\\d+\","] # [doc = " ])?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let input = Input::new(\"!@#123\");"] # [doc = ""] # [doc = " // We only care about the overall match offsets here, so we just"] # [doc = " // allocate two slots for each pattern. Each slot records the start"] # [doc = " // and end of the match."] # [doc = " let mut slots = [None; 4];"] # [doc = " let pid = re.search_slots_with(&mut cache, &input, &mut slots);"] # [doc = " assert_eq!(Some(PatternID::must(1)), pid);"] # [doc = ""] # [doc = " // The overall match offsets are always at 'pid * 2' and 'pid * 2 + 1'."] # [doc = " // See 'GroupInfo' for more details on the mapping between groups and"] # [doc = " // slot indices."] # [doc = " let slot_start = pid.unwrap().as_usize() * 2;"] # [doc = " let slot_end = slot_start + 1;"] # [doc = " assert_eq!(Some(3), slots[slot_start].map(|s| s.get()));"] # [doc = " assert_eq!(Some(6), slots[slot_end].map(|s| s.get()));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn search_slots_with (& self , cache : & mut Cache , input : & Input < '_ > , slots : & mut [Option < NonMaxUsize >] ,) -> Option < PatternID > { if self . imp . info . is_impossible (input) { return None ; } self . imp . strat . search_slots (cache , input , slots) } # [doc = " This is like [`Regex::which_overlapping_matches`], but requires the"] # [doc = " caller to explicitly pass a [`Cache`]."] # [doc = ""] # [doc = " Passing a `Cache` explicitly will bypass the use of an internal memory"] # [doc = " pool used by `Regex` to get a `Cache` for a search. The use of this"] # [doc = " pool can be slower in some cases when a `Regex` is used from multiple"] # [doc = " threads simultaneously. Typically, performance only becomes an issue"] # [doc = " when there is heavy contention, which in turn usually only occurs"] # [doc = " when each thread's primary unit of work is a regex search on a small"] # [doc = " haystack."] # [doc = ""] # [doc = " # Why pass a `Cache` explicitly?"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{meta::Regex, Input, MatchKind, PatternSet};"] # [doc = ""] # [doc = " let patterns = &["] # [doc = "     r\"\\w+\", r\"\\d+\", r\"\\pL+\", r\"foo\", r\"bar\", r\"barfoo\", r\"foobar\","] # [doc = " ];"] # [doc = " let re = Regex::builder()"] # [doc = "     .configure(Regex::config().match_kind(MatchKind::All))"] # [doc = "     .build_many(patterns)?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\");"] # [doc = " let mut patset = PatternSet::new(re.pattern_len());"] # [doc = " re.which_overlapping_matches_with(&mut cache, &input, &mut patset);"] # [doc = " let expected = vec![0, 2, 3, 4, 6];"] # [doc = " let got: Vec<usize> = patset.iter().map(|p| p.as_usize()).collect();"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn which_overlapping_matches_with (& self , cache : & mut Cache , input : & Input < '_ > , patset : & mut PatternSet ,) { if self . imp . info . is_impossible (input) { return ; } self . imp . strat . which_overlapping_matches (cache , input , patset) } }
};
}
