// Generated macro for impl_858 (impl)
macro_rules! Depcrate_util_prefilterimpl_858 {
() => {
// Module: crate::util::prefilter
// Provides: {"impl_858"}
// Dependencies: {}
impl Prefilter { # [doc = " Create a new prefilter from a sequence of needles and a corresponding"] # [doc = " match semantics."] # [doc = ""] # [doc = " This may return `None` for a variety of reasons, for example, if"] # [doc = " a suitable prefilter could not be constructed. That might occur"] # [doc = " if they are unavailable (e.g., the `perf-literal-substring` and"] # [doc = " `perf-literal-multisubstring` features aren't enabled), or it might"] # [doc = " occur because of heuristics or other artifacts of how the prefilter"] # [doc = " works."] # [doc = ""] # [doc = " Note that if you have an [`Hir`] expression, it may be more convenient"] # [doc = " to use [`Prefilter::from_hir_prefix`]. It will automatically handle the"] # [doc = " task of extracting prefix literals for you."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how match semantics can impact the matching"] # [doc = " algorithm used by the prefilter. For this reason, it is important to"] # [doc = " ensure that the match semantics given here are consistent with the"] # [doc = " match semantics intended for the regular expression that the literals"] # [doc = " were extracted from."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     util::{prefilter::Prefilter, syntax},"] # [doc = "     MatchKind, Span,"] # [doc = " };"] # [doc = ""] # [doc = " let hay = \"Hello samwise\";"] # [doc = ""] # [doc = " // With leftmost-first, we find 'samwise' here because it comes"] # [doc = " // before 'sam' in the sequence we give it.."] # [doc = " let pre = Prefilter::new(MatchKind::LeftmostFirst, &[\"samwise\", \"sam\"])"] # [doc = "     .expect(\"a prefilter\");"] # [doc = " assert_eq!("] # [doc = "     Some(Span::from(6..13)),"] # [doc = "     pre.find(hay.as_bytes(), Span::from(0..hay.len())),"] # [doc = " );"] # [doc = " // Still with leftmost-first but with the literals reverse, now 'sam'"] # [doc = " // will match instead!"] # [doc = " let pre = Prefilter::new(MatchKind::LeftmostFirst, &[\"sam\", \"samwise\"])"] # [doc = "     .expect(\"a prefilter\");"] # [doc = " assert_eq!("] # [doc = "     Some(Span::from(6..9)),"] # [doc = "     pre.find(hay.as_bytes(), Span::from(0..hay.len())),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn new < B : AsRef < [u8] > > (kind : MatchKind , needles : & [B] ,) -> Option < Prefilter > { Choice :: new (kind , needles) . and_then (| choice | { let max_needle_len = needles . iter () . map (| b | b . as_ref () . len ()) . max () . unwrap_or (0) ; Prefilter :: from_choice (choice , max_needle_len) }) } # [doc = " This turns a prefilter selection into a `Prefilter`. That is, in turns"] # [doc = " the enum given into a trait object."] fn from_choice (choice : Choice , max_needle_len : usize ,) -> Option < Prefilter > { # [cfg (not (feature = "alloc"))] { None } # [cfg (feature = "alloc")] { let pre : Arc < dyn PrefilterI > = match choice { Choice :: Memchr (p) => Arc :: new (p) , Choice :: Memchr2 (p) => Arc :: new (p) , Choice :: Memchr3 (p) => Arc :: new (p) , Choice :: Memmem (p) => Arc :: new (p) , Choice :: Teddy (p) => Arc :: new (p) , Choice :: ByteSet (p) => Arc :: new (p) , Choice :: AhoCorasick (p) => Arc :: new (p) , } ; let is_fast = pre . is_fast () ; Some (Prefilter { pre , is_fast , max_needle_len }) } } # [doc = " This attempts to extract prefixes from the given `Hir` expression for"] # [doc = " the given match semantics, and if possible, builds a prefilter for"] # [doc = " them."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to build a prefilter directly from an [`Hir`]"] # [doc = " expression, and use to find an occurrence of a prefix from the regex"] # [doc = " pattern."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     util::{prefilter::Prefilter, syntax},"] # [doc = "     MatchKind, Span,"] # [doc = " };"] # [doc = ""] # [doc = " let hir = syntax::parse(r\"(Bruce|Patti) \\w+\")?;"] # [doc = " let pre = Prefilter::from_hir_prefix(MatchKind::LeftmostFirst, &hir)"] # [doc = "     .expect(\"a prefilter\");"] # [doc = " let hay = \"Hello Patti Scialfa!\";"] # [doc = " assert_eq!("] # [doc = "     Some(Span::from(6..12)),"] # [doc = "     pre.find(hay.as_bytes(), Span::from(0..hay.len())),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] pub fn from_hir_prefix (kind : MatchKind , hir : & Hir) -> Option < Prefilter > { Prefilter :: from_hirs_prefix (kind , & [hir]) } # [doc = " This attempts to extract prefixes from the given `Hir` expressions for"] # [doc = " the given match semantics, and if possible, builds a prefilter for"] # [doc = " them."] # [doc = ""] # [doc = " Note that as of now, prefilters throw away information about which"] # [doc = " pattern each literal comes from. In other words, when a prefilter finds"] # [doc = " a match, there's no way to know which pattern (or patterns) it came"] # [doc = " from. Therefore, in order to confirm a match, you'll have to check all"] # [doc = " of the patterns by running the full regex engine."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to build a prefilter directly from multiple"] # [doc = " `Hir` expressions expression, and use it to find an occurrence of a"] # [doc = " prefix from the regex patterns."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     util::{prefilter::Prefilter, syntax},"] # [doc = "     MatchKind, Span,"] # [doc = " };"] # [doc = ""] # [doc = " let hirs = syntax::parse_many(&["] # [doc = "     r\"(Bruce|Patti) \\w+\","] # [doc = "     r\"Mrs?\\. Doubtfire\","] # [doc = " ])?;"] # [doc = " let pre = Prefilter::from_hirs_prefix(MatchKind::LeftmostFirst, &hirs)"] # [doc = "     .expect(\"a prefilter\");"] # [doc = " let hay = \"Hello Mrs. Doubtfire\";"] # [doc = " assert_eq!("] # [doc = "     Some(Span::from(6..20)),"] # [doc = "     pre.find(hay.as_bytes(), Span::from(0..hay.len())),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [cfg (feature = "syntax")] pub fn from_hirs_prefix < H : Borrow < Hir > > (kind : MatchKind , hirs : & [H] ,) -> Option < Prefilter > { prefixes (kind , hirs) . literals () . and_then (| lits | Prefilter :: new (kind , lits)) } # [doc = " Run this prefilter on `haystack[span.start..end]` and return a matching"] # [doc = " span if one exists."] # [doc = ""] # [doc = " The span returned is guaranteed to have a start position greater than"] # [doc = " or equal to the one given, and an end position less than or equal to"] # [doc = " the one given."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to build a prefilter directly from an [`Hir`]"] # [doc = " expression, and use it to find an occurrence of a prefix from the regex"] # [doc = " pattern."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     util::{prefilter::Prefilter, syntax},"] # [doc = "     MatchKind, Span,"] # [doc = " };"] # [doc = ""] # [doc = " let hir = syntax::parse(r\"Bruce \\w+\")?;"] # [doc = " let pre = Prefilter::from_hir_prefix(MatchKind::LeftmostFirst, &hir)"] # [doc = "     .expect(\"a prefilter\");"] # [doc = " let hay = \"Hello Bruce Springsteen!\";"] # [doc = " assert_eq!("] # [doc = "     Some(Span::from(6..12)),"] # [doc = "     pre.find(hay.as_bytes(), Span::from(0..hay.len())),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn find (& self , haystack : & [u8] , span : Span) -> Option < Span > { # [cfg (not (feature = "alloc"))] { unreachable ! () } # [cfg (feature = "alloc")] { self . pre . find (haystack , span) } } # [doc = " Returns the span of a prefix of `haystack[span.start..span.end]` if"] # [doc = " the prefilter matches."] # [doc = ""] # [doc = " The span returned is guaranteed to have a start position equivalent to"] # [doc = " the one given, and an end position less than or equal to the one given."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to build a prefilter directly from an [`Hir`]"] # [doc = " expression, and use it to find an occurrence of a prefix from the regex"] # [doc = " pattern that begins at the start of a haystack only."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     util::{prefilter::Prefilter, syntax},"] # [doc = "     MatchKind, Span,"] # [doc = " };"] # [doc = ""] # [doc = " let hir = syntax::parse(r\"Bruce \\w+\")?;"] # [doc = " let pre = Prefilter::from_hir_prefix(MatchKind::LeftmostFirst, &hir)"] # [doc = "     .expect(\"a prefilter\");"] # [doc = " let hay = \"Hello Bruce Springsteen!\";"] # [doc = " // Nothing is found here because 'Bruce' does"] # [doc = " // not occur at the beginning of our search."] # [doc = " assert_eq!("] # [doc = "     None,"] # [doc = "     pre.prefix(hay.as_bytes(), Span::from(0..hay.len())),"] # [doc = " );"] # [doc = " // But if we change where we start the search"] # [doc = " // to begin where 'Bruce ' begins, then a"] # [doc = " // match will be found."] # [doc = " assert_eq!("] # [doc = "     Some(Span::from(6..12)),"] # [doc = "     pre.prefix(hay.as_bytes(), Span::from(6..hay.len())),"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn prefix (& self , haystack : & [u8] , span : Span) -> Option < Span > { # [cfg (not (feature = "alloc"))] { unreachable ! () } # [cfg (feature = "alloc")] { self . pre . prefix (haystack , span) } } # [doc = " Returns the heap memory, in bytes, used by the underlying prefilter."] # [inline] pub fn memory_usage (& self) -> usize { # [cfg (not (feature = "alloc"))] { unreachable ! () } # [cfg (feature = "alloc")] { self . pre . memory_usage () } } # [doc = " Return the length of the longest needle"] # [doc = " in this Prefilter"] # [inline] pub fn max_needle_len (& self) -> usize { # [cfg (not (feature = "alloc"))] { unreachable ! () } # [cfg (feature = "alloc")] { self . max_needle_len } } # [doc = " Implementations might return true here if they believe themselves to"] # [doc = " be \"fast.\" The concept of \"fast\" is deliberately left vague, but in"] # [doc = " practice this usually corresponds to whether it's believed that SIMD"] # [doc = " will be used."] # [doc = ""] # [doc = " Why do we care about this? Well, some prefilter tricks tend to come"] # [doc = " with their own bits of overhead, and so might only make sense if we"] # [doc = " know that a scan will be *much* faster than the regex engine itself."] # [doc = " Otherwise, the trick may not be worth doing. Whether something is"] # [doc = " \"much\" faster than the regex engine generally boils down to whether"] # [doc = " SIMD is used. (But not always. Even a SIMD matcher with a high false"] # [doc = " positive rate can become quite slow.)"] # [doc = ""] # [doc = " Even if this returns true, it is still possible for the prefilter to"] # [doc = " be \"slow.\" Remember, prefilters are just heuristics. We can't really"] # [doc = " *know* a prefilter will be fast without actually trying the prefilter."] # [doc = " (Which of course we cannot afford to do.)"] # [inline] pub fn is_fast (& self) -> bool { # [cfg (not (feature = "alloc"))] { unreachable ! () } # [cfg (feature = "alloc")] { self . is_fast } } }
};
}
