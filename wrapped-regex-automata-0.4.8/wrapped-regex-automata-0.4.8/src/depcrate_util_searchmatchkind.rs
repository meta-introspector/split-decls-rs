// Generated macro for MatchKind (enum)
macro_rules! Depcrate_util_searchMatchKind {
() => {
// Module: crate::util::search
// Provides: {"MatchKind"}
// Dependencies: {}
# [doc = " The kind of match semantics to use for a regex pattern."] # [doc = ""] # [doc = " The default match kind is `LeftmostFirst`, and this corresponds to the"] # [doc = " match semantics used by most backtracking engines, such as Perl."] # [doc = ""] # [doc = " # Leftmost first or \"preference order\" match semantics"] # [doc = ""] # [doc = " Leftmost-first semantics determine which match to report when there are"] # [doc = " multiple paths through a regex that match at the same position. The tie is"] # [doc = " essentially broken by how a backtracker would behave. For example, consider"] # [doc = " running the regex `foofoofoo|foofoo|foo` on the haystack `foofoo`. In this"] # [doc = " case, both the `foofoo` and `foo` branches match at position `0`. So should"] # [doc = " the end of the match be `3` or `6`?"] # [doc = ""] # [doc = " A backtracker will conceptually work by trying `foofoofoo` and failing."] # [doc = " Then it will try `foofoo`, find the match and stop there. Thus, the"] # [doc = " leftmost-first match position is `6`. This is called \"leftmost-first\" or"] # [doc = " \"preference order\" because the order of the branches as written in the"] # [doc = " regex pattern is what determines how to break the tie."] # [doc = ""] # [doc = " (Note that leftmost-longest match semantics, which break ties by always"] # [doc = " taking the longest matching string, are not currently supported by this"] # [doc = " crate. These match semantics tend to be found in POSIX regex engines.)"] # [doc = ""] # [doc = " This example shows how leftmost-first semantics work, and how it even"] # [doc = " applies to multi-pattern regexes:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     Match,"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::new_many(&["] # [doc = "     r\"foofoofoo\","] # [doc = "     r\"foofoo\","] # [doc = "     r\"foo\","] # [doc = " ])?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let got: Vec<Match> = re.find_iter(&mut cache, \"foofoo\").collect();"] # [doc = " let expected = vec![Match::must(1, 0..6)];"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # All matches"] # [doc = ""] # [doc = " The `All` match semantics report any and all matches, and generally will"] # [doc = " attempt to match as much as possible. It doesn't respect any sort of match"] # [doc = " priority at all, so things like non-greedy matching don't work in this"] # [doc = " mode."] # [doc = ""] # [doc = " The fact that non-greedy matching doesn't work generally makes most forms"] # [doc = " of unanchored non-overlapping searches have unintuitive behavior. Namely,"] # [doc = " unanchored searches behave as if there is a `(?s-u:.)*?` prefix at the"] # [doc = " beginning of the pattern, which is specifically non-greedy. Since it will"] # [doc = " be treated as greedy in `All` match semantics, this generally means that"] # [doc = " it will first attempt to consume all of the haystack and is likely to wind"] # [doc = " up skipping matches."] # [doc = ""] # [doc = " Generally speaking, `All` should only be used in two circumstances:"] # [doc = ""] # [doc = " * When running an anchored search and there is a desire to match as much as"] # [doc = " possible. For example, when building a reverse regex matcher to find the"] # [doc = " start of a match after finding the end. In this case, the reverse search"] # [doc = " is anchored to the end of the match found by the forward search."] # [doc = " * When running overlapping searches. Since `All` encodes all possible"] # [doc = " matches, this is generally what you want for an overlapping search. If you"] # [doc = " try to use leftmost-first in an overlapping search, it is likely to produce"] # [doc = " counter-intuitive results since leftmost-first specifically excludes some"] # [doc = " matches from its underlying finite state machine."] # [doc = ""] # [doc = " This example demonstrates the counter-intuitive behavior of `All` semantics"] # [doc = " when using a standard leftmost unanchored search:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     Match, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::builder()"] # [doc = "     .configure(PikeVM::config().match_kind(MatchKind::All))"] # [doc = "     .build(\"foo\")?;"] # [doc = " let hay = \"first foo second foo wat\";"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let got: Vec<Match> = re.find_iter(&mut cache, hay).collect();"] # [doc = " // Notice that it completely skips the first 'foo'!"] # [doc = " let expected = vec![Match::must(0, 17..20)];"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " This second example shows how `All` semantics are useful for an overlapping"] # [doc = " search. Note that we use lower level lazy DFA APIs here since the NFA"] # [doc = " engines only currently support a very limited form of overlapping search."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     hybrid::dfa::{DFA, OverlappingState},"] # [doc = "     HalfMatch, Input, MatchKind,"] # [doc = " };"] # [doc = ""] # [doc = " let re = DFA::builder()"] # [doc = "     // If we didn't set 'All' semantics here, then the regex would only"] # [doc = "     // match 'foo' at offset 3 and nothing else. Why? Because the state"] # [doc = "     // machine implements preference order and knows that the 'foofoo' and"] # [doc = "     // 'foofoofoo' branches can never match since 'foo' will always match"] # [doc = "     // when they match and take priority."] # [doc = "     .configure(DFA::config().match_kind(MatchKind::All))"] # [doc = "     .build(r\"foo|foofoo|foofoofoo\")?;"] # [doc = " let mut cache = re.create_cache();"] # [doc = " let mut state = OverlappingState::start();"] # [doc = " let input = Input::new(\"foofoofoo\");"] # [doc = " let mut got = vec![];"] # [doc = " loop {"] # [doc = "     re.try_search_overlapping_fwd(&mut cache, &input, &mut state)?;"] # [doc = "     let m = match state.get_match() {"] # [doc = "         None => break,"] # [doc = "         Some(m) => m,"] # [doc = "     };"] # [doc = "     got.push(m);"] # [doc = " }"] # [doc = " let expected = vec!["] # [doc = "     HalfMatch::must(0, 3),"] # [doc = "     HalfMatch::must(0, 6),"] # [doc = "     HalfMatch::must(0, 9),"] # [doc = " ];"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [non_exhaustive] # [derive (Clone , Copy , Debug , Eq , PartialEq)] pub enum MatchKind { # [doc = " Report all possible matches."] All , # [doc = " Report only the leftmost matches. When multiple leftmost matches exist,"] # [doc = " report the match corresponding to the part of the regex that appears"] # [doc = " first in the syntax."] LeftmostFirst , }
};
}
