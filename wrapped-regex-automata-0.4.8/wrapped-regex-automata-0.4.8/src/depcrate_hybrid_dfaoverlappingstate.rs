// Generated macro for OverlappingState (struct)
macro_rules! Depcrate_hybrid_dfaOverlappingState {
() => {
// Module: crate::hybrid::dfa
// Provides: {"OverlappingState"}
// Dependencies: {}
# [doc = " Represents the current state of an overlapping search."] # [doc = ""] # [doc = " This is used for overlapping searches since they need to know something"] # [doc = " about the previous search. For example, when multiple patterns match at the"] # [doc = " same position, this state tracks the last reported pattern so that the next"] # [doc = " search knows whether to report another matching pattern or continue with"] # [doc = " the search at the next position. Additionally, it also tracks which state"] # [doc = " the last search call terminated in."] # [doc = ""] # [doc = " This type provides little introspection capabilities. The only thing a"] # [doc = " caller can do is construct it and pass it around to permit search routines"] # [doc = " to use it to track state, and also ask whether a match has been found."] # [doc = ""] # [doc = " Callers should always provide a fresh state constructed via"] # [doc = " [`OverlappingState::start`] when starting a new search. Reusing state from"] # [doc = " a previous search may result in incorrect results."] # [derive (Clone , Debug , Eq , PartialEq)] pub struct OverlappingState { # [doc = " The match reported by the most recent overlapping search to use this"] # [doc = " state."] # [doc = ""] # [doc = " If a search does not find any matches, then it is expected to clear"] # [doc = " this value."] pub (crate) mat : Option < HalfMatch > , # [doc = " The state ID of the state at which the search was in when the call"] # [doc = " terminated. When this is a match state, `last_match` must be set to a"] # [doc = " non-None value."] # [doc = ""] # [doc = " A `None` value indicates the start state of the corresponding"] # [doc = " automaton. We cannot use the actual ID, since any one automaton may"] # [doc = " have many start states, and which one is in use depends on several"] # [doc = " search-time factors."] pub (crate) id : Option < LazyStateID > , # [doc = " The position of the search."] # [doc = ""] # [doc = " When `id` is None (i.e., we are starting a search), this is set to"] # [doc = " the beginning of the search as given by the caller regardless of its"] # [doc = " current value. Subsequent calls to an overlapping search pick up at"] # [doc = " this offset."] pub (crate) at : usize , # [doc = " The index into the matching patterns of the next match to report if the"] # [doc = " current state is a match state. Note that this may be 1 greater than"] # [doc = " the total number of matches to report for the current match state. (In"] # [doc = " which case, no more matches should be reported at the current position"] # [doc = " and the search should advance to the next position.)"] pub (crate) next_match_index : Option < usize > , # [doc = " This is set to true when a reverse overlapping search has entered its"] # [doc = " EOI transitions."] # [doc = ""] # [doc = " This isn't used in a forward search because it knows to stop once the"] # [doc = " position exceeds the end of the search range. In a reverse search,"] # [doc = " since we use unsigned offsets, we don't \"know\" once we've gone past"] # [doc = " `0`. So the only way to detect it is with this extra flag. The reverse"] # [doc = " overlapping search knows to terminate specifically after it has"] # [doc = " reported all matches after following the EOI transition."] pub (crate) rev_eoi : bool , }
};
}
