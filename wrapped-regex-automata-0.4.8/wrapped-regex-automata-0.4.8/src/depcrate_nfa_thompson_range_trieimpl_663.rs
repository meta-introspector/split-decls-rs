// Generated macro for impl_663 (impl)
macro_rules! Depcrate_nfa_thompson_range_trieimpl_663 {
() => {
// Module: crate::nfa::thompson::range_trie
// Provides: {"impl_663"}
// Dependencies: {}
impl Split { # [doc = " Create a partitioning of the given ranges."] # [doc = ""] # [doc = " If the given ranges have an empty intersection, then None is returned."] fn new (o : Utf8Range , n : Utf8Range) -> Option < Split > { let range = | r : RangeInclusive < u8 > | Utf8Range { start : * r . start () , end : * r . end () , } ; let old = | r | SplitRange :: Old (range (r)) ; let new = | r | SplitRange :: New (range (r)) ; let both = | r | SplitRange :: Both (range (r)) ; let (a , b , x , y) = (o . start , o . end , n . start , n . end) ; if b < x || y < a { None } else if a == x && b == y { Some (Split :: parts1 (both (a ..= b))) } else if a == x && b < y { Some (Split :: parts2 (both (a ..= b) , new (b + 1 ..= y))) } else if b == y && a > x { Some (Split :: parts2 (new (x ..= a - 1) , both (a ..= b))) } else if x == a && y < b { Some (Split :: parts2 (both (x ..= y) , old (y + 1 ..= b))) } else if y == b && x > a { Some (Split :: parts2 (old (a ..= x - 1) , both (x ..= y))) } else if a > x && b < y { Some (Split :: parts3 (new (x ..= a - 1) , both (a ..= b) , new (b + 1 ..= y))) } else if x > a && y < b { Some (Split :: parts3 (old (a ..= x - 1) , both (x ..= y) , old (y + 1 ..= b))) } else if b == x && a < y { Some (Split :: parts3 (old (a ..= b - 1) , both (b ..= b) , new (b + 1 ..= y))) } else if y == a && x < b { Some (Split :: parts3 (new (x ..= y - 1) , both (y ..= y) , old (y + 1 ..= b))) } else if b > x && b < y { Some (Split :: parts3 (old (a ..= x - 1) , both (x ..= b) , new (b + 1 ..= y))) } else if y > a && y < b { Some (Split :: parts3 (new (x ..= a - 1) , both (a ..= y) , old (y + 1 ..= b))) } else { unreachable ! () } } # [doc = " Create a new split with a single partition. This only occurs when two"] # [doc = " ranges are equivalent."] fn parts1 (r1 : SplitRange) -> Split { let nada = SplitRange :: Old (Utf8Range { start : 0 , end : 0 }) ; Split { partitions : [r1 , nada , nada] , len : 1 } } # [doc = " Create a new split with two partitions."] fn parts2 (r1 : SplitRange , r2 : SplitRange) -> Split { let nada = SplitRange :: Old (Utf8Range { start : 0 , end : 0 }) ; Split { partitions : [r1 , r2 , nada] , len : 2 } } # [doc = " Create a new split with three partitions."] fn parts3 (r1 : SplitRange , r2 : SplitRange , r3 : SplitRange) -> Split { Split { partitions : [r1 , r2 , r3] , len : 3 } } # [doc = " Return the partitions in this split as a slice."] fn as_slice (& self) -> & [SplitRange] { & self . partitions [.. self . len] } }
};
}
