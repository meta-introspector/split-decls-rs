// Generated macro for impl_122 (impl)
macro_rules! Depcrate_dfa_regeximpl_122 {
() => {
// Module: crate::dfa::regex
// Provides: {"impl_122"}
// Dependencies: {}
# [doc = " Lower level fallible search routines that permit controlling where the"] # [doc = " search starts and ends in a particular sequence."] impl < A : Automaton > Regex < A > { # [doc = " Returns the start and end offset of the leftmost match. If no match"] # [doc = " exists, then `None` is returned."] # [doc = ""] # [doc = " This is like [`Regex::find`] but with two differences:"] # [doc = ""] # [doc = " 1. It is not generic over `Into<Input>` and instead accepts a"] # [doc = " `&Input`. This permits reusing the same `Input` for multiple searches"] # [doc = " without needing to create a new one. This _may_ help with latency."] # [doc = " 2. It returns an error if the search could not complete where as"] # [doc = " [`Regex::find`] will panic."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This routine errors if the search could not complete. This can occur"] # [doc = " in the following circumstances:"] # [doc = ""] # [doc = " * The configuration of the DFA may permit it to \"quit\" the search."] # [doc = " For example, setting quit bytes or enabling heuristic support for"] # [doc = " Unicode word boundaries. The default configuration does not enable any"] # [doc = " option that could result in the DFA quitting."] # [doc = " * When the provided `Input` configuration is not supported. For"] # [doc = " example, by providing an unsupported anchor mode."] # [doc = ""] # [doc = " When a search returns an error, callers cannot know whether a match"] # [doc = " exists or not."] # [inline] pub fn try_search (& self , input : & Input < '_ > ,) -> Result < Option < Match > , MatchError > { let (fwd , rev) = (self . forward () , self . reverse ()) ; let end = match fwd . try_search_fwd (input) ? { None => return Ok (None) , Some (end) => end , } ; if input . start () == end . offset () { return Ok (Some (Match :: new (end . pattern () , end . offset () .. end . offset () ,))) ; } if self . is_anchored (input) { return Ok (Some (Match :: new (end . pattern () , input . start () .. end . offset () ,))) ; } let revsearch = input . clone () . span (input . start () .. end . offset ()) . anchored (Anchored :: Yes) . earliest (false) ; let start = rev . try_search_rev (& revsearch) ? . expect ("reverse search must match if forward search does") ; assert_eq ! (start . pattern () , end . pattern () , "forward and reverse search must match same pattern" ,) ; assert ! (start . offset () <= end . offset ()) ; Ok (Some (Match :: new (end . pattern () , start . offset () .. end . offset ()))) } # [doc = " Returns true if either the given input specifies an anchored search"] # [doc = " or if the underlying DFA is always anchored."] fn is_anchored (& self , input : & Input < '_ >) -> bool { match input . get_anchored () { Anchored :: No => self . forward () . is_always_start_anchored () , Anchored :: Yes | Anchored :: Pattern (_) => true , } } }
};
}
