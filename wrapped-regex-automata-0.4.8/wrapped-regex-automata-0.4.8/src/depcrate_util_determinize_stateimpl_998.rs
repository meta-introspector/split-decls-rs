// Generated macro for impl_998 (impl)
macro_rules! Depcrate_util_determinize_stateimpl_998 {
() => {
// Module: crate::util::determinize::state
// Provides: {"impl_998"}
// Dependencies: {}
impl < 'a > ReprVec < 'a > { # [doc = " Set this state as a match state."] # [doc = ""] # [doc = " This should not be exposed explicitly outside of this module. It is"] # [doc = " set automatically when a pattern ID is added."] fn set_is_match (& mut self) { self . 0 [0] |= 1 << 0 ; } # [doc = " Set that this state has pattern IDs explicitly written to it."] # [doc = ""] # [doc = " This should not be exposed explicitly outside of this module. This is"] # [doc = " used internally as a space saving optimization. Namely, if the state"] # [doc = " is a match state but does not have any pattern IDs written to it,"] # [doc = " then it is automatically inferred to have a pattern ID of ZERO."] fn set_has_pattern_ids (& mut self) { self . 0 [0] |= 1 << 1 ; } # [doc = " Set this state as being built from a transition over a word byte."] # [doc = ""] # [doc = " Setting this is only necessary when one needs to deal with word"] # [doc = " boundary assertions. Therefore, if the underlying NFA has no word"] # [doc = " boundary assertions, callers should not set this."] fn set_is_from_word (& mut self) { self . 0 [0] |= 1 << 2 ; } # [doc = " Set this state as having seen half of a CRLF terminator."] # [doc = ""] # [doc = " In the forward direction, this should be set when a `\\r` has been seen."] # [doc = " In the reverse direction, this should be set when a `\\n` has been seen."] fn set_is_half_crlf (& mut self) { self . 0 [0] |= 1 << 3 ; } # [doc = " The set of look-behind assertions that were true in the transition that"] # [doc = " created this state."] fn look_have (& self) -> LookSet { self . repr () . look_have () } # [doc = " The set of look-around (both behind and ahead) assertions that appear"] # [doc = " at least once in this state's set of NFA states."] fn look_need (& self) -> LookSet { self . repr () . look_need () } # [doc = " Mutate the set of look-behind assertions that were true in the"] # [doc = " transition that created this state."] fn set_look_have (& mut self , mut set : impl FnMut (LookSet) -> LookSet) { set (self . look_have ()) . write_repr (& mut self . 0 [1 ..]) ; } # [doc = " Mutate the set of look-around (both behind and ahead) assertions that"] # [doc = " appear at least once in this state's set of NFA states."] fn set_look_need (& mut self , mut set : impl FnMut (LookSet) -> LookSet) { set (self . look_need ()) . write_repr (& mut self . 0 [5 ..]) ; } # [doc = " Add a pattern ID to this state. All match states must have at least"] # [doc = " one pattern ID associated with it."] # [doc = ""] # [doc = " Callers must never add duplicative pattern IDs."] # [doc = ""] # [doc = " The order in which patterns are added must correspond to the order"] # [doc = " in which patterns are reported as matches."] fn add_match_pattern_id (& mut self , pid : PatternID) { if ! self . repr () . has_pattern_ids () { if pid == PatternID :: ZERO { self . set_is_match () ; return ; } self . 0 . extend (core :: iter :: repeat (0) . take (PatternID :: SIZE)) ; self . set_has_pattern_ids () ; if self . repr () . is_match () { write_u32 (self . 0 , 0) } else { self . set_is_match () ; } } write_u32 (self . 0 , pid . as_u32 ()) ; } # [doc = " Indicate that no more pattern IDs will be added to this state."] # [doc = ""] # [doc = " Once this is called, callers must not call it or 'add_match_pattern_id'"] # [doc = " again."] # [doc = ""] # [doc = " This should not be exposed explicitly outside of this module. It"] # [doc = " should be called only when converting a StateBuilderMatches into a"] # [doc = " StateBuilderNFA."] fn close_match_pattern_ids (& mut self) { if ! self . repr () . has_pattern_ids () { return ; } let patsize = PatternID :: SIZE ; let pattern_bytes = self . 0 . len () - 13 ; assert_eq ! (pattern_bytes % patsize , 0) ; let count32 = u32 :: try_from (pattern_bytes / patsize) . unwrap () ; wire :: NE :: write_u32 (count32 , & mut self . 0 [9 .. 13]) ; } # [doc = " Add an NFA state ID to this state. The order in which NFA states are"] # [doc = " added matters. It is the caller's responsibility to ensure that"] # [doc = " duplicate NFA state IDs are not added."] fn add_nfa_state_id (& mut self , prev : & mut StateID , sid : StateID) { let delta = sid . as_i32 () - prev . as_i32 () ; write_vari32 (self . 0 , delta) ; * prev = sid ; } # [doc = " Return a read-only view of this state's representation."] fn repr (& self) -> Repr < '_ > { Repr (self . 0 . as_slice ()) } }
};
}
