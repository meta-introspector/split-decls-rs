// Generated macro for Builder (struct)
macro_rules! Depcrate_meta_regexBuilder {
() => {
// Module: crate::meta::regex
// Provides: {"Builder"}
// Dependencies: {}
# [doc = " A builder for configuring and constructing a `Regex`."] # [doc = ""] # [doc = " The builder permits configuring two different aspects of a `Regex`:"] # [doc = ""] # [doc = " * [`Builder::configure`] will set high-level configuration options as"] # [doc = " described by a [`Config`]."] # [doc = " * [`Builder::syntax`] will set the syntax level configuration options"] # [doc = " as described by a [`util::syntax::Config`](crate::util::syntax::Config)."] # [doc = " This only applies when building a `Regex` from pattern strings."] # [doc = ""] # [doc = " Once configured, the builder can then be used to construct a `Regex` from"] # [doc = " one of 4 different inputs:"] # [doc = ""] # [doc = " * [`Builder::build`] creates a regex from a single pattern string."] # [doc = " * [`Builder::build_many`] creates a regex from many pattern strings."] # [doc = " * [`Builder::build_from_hir`] creates a regex from a"] # [doc = " [`regex-syntax::Hir`](Hir) expression."] # [doc = " * [`Builder::build_many_from_hir`] creates a regex from many"] # [doc = " [`regex-syntax::Hir`](Hir) expressions."] # [doc = ""] # [doc = " The latter two methods in particular provide a way to construct a fully"] # [doc = " feature regular expression matcher directly from an `Hir` expression"] # [doc = " without having to first convert it to a string. (This is in contrast to the"] # [doc = " top-level `regex` crate which intentionally provides no such API in order"] # [doc = " to avoid making `regex-syntax` a public dependency.)"] # [doc = ""] # [doc = " As a convenience, this builder may be created via [`Regex::builder`], which"] # [doc = " may help avoid an extra import."] # [doc = ""] # [doc = " # Example: change the line terminator"] # [doc = ""] # [doc = " This example shows how to enable multi-line mode by default and change the"] # [doc = " line terminator to the NUL byte:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, util::syntax, Match};"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .syntax(syntax::Config::new().multi_line(true))"] # [doc = "     .configure(Regex::config().line_terminator(b'\\x00'))"] # [doc = "     .build(r\"^foo$\")?;"] # [doc = " let hay = \"\\x00foo\\x00\";"] # [doc = " assert_eq!(Some(Match::must(0, 1..4)), re.find(hay));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: disable UTF-8 requirement"] # [doc = ""] # [doc = " By default, regex patterns are required to match UTF-8. This includes"] # [doc = " regex patterns that can produce matches of length zero. In the case of an"] # [doc = " empty match, by default, matches will not appear between the code units of"] # [doc = " a UTF-8 encoded codepoint."] # [doc = ""] # [doc = " However, it can be useful to disable this requirement, particularly if"] # [doc = " you're searching things like `&[u8]` that are not known to be valid UTF-8."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, util::syntax, Match};"] # [doc = ""] # [doc = " let mut builder = Regex::builder();"] # [doc = " // Disables the requirement that non-empty matches match UTF-8."] # [doc = " builder.syntax(syntax::Config::new().utf8(false));"] # [doc = " // Disables the requirement that empty matches match UTF-8 boundaries."] # [doc = " builder.configure(Regex::config().utf8_empty(false));"] # [doc = ""] # [doc = " // We can match raw bytes via \\xZZ syntax, but we need to disable"] # [doc = " // Unicode mode to do that. We could disable it everywhere, or just"] # [doc = " // selectively, as shown here."] # [doc = " let re = builder.build(r\"(?-u:\\xFF)foo(?-u:\\xFF)\")?;"] # [doc = " let hay = b\"\\xFFfoo\\xFF\";"] # [doc = " assert_eq!(Some(Match::must(0, 0..5)), re.find(hay));"] # [doc = ""] # [doc = " // We can also match between code units."] # [doc = " let re = builder.build(r\"\")?;"] # [doc = " let hay = \"☃\";"] # [doc = " assert_eq!(re.find_iter(hay).collect::<Vec<Match>>(), vec!["] # [doc = "     Match::must(0, 0..0),"] # [doc = "     Match::must(0, 1..1),"] # [doc = "     Match::must(0, 2..2),"] # [doc = "     Match::must(0, 3..3),"] # [doc = " ]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Debug)] pub struct Builder { config : Config , ast : ast :: parse :: ParserBuilder , hir : hir :: translate :: TranslatorBuilder , }
};
}
