// Generated macro for impl_386 (impl)
macro_rules! Depcrate_meta_regeximpl_386 {
() => {
// Module: crate::meta::regex
// Provides: {"impl_386"}
// Dependencies: {}
# [doc = " Convenience constructors for a `Regex` using the default configuration."] impl Regex { # [doc = " Builds a `Regex` from a single pattern string using the default"] # [doc = " configuration."] # [doc = ""] # [doc = " If there was a problem parsing the pattern or a problem turning it into"] # [doc = " a regex matcher, then an error is returned."] # [doc = ""] # [doc = " If you want to change the configuration of a `Regex`, use a [`Builder`]"] # [doc = " with a [`Config`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Match};"] # [doc = ""] # [doc = " let re = Regex::new(r\"(?Rm)^foo$\")?;"] # [doc = " let hay = \"\\r\\nfoo\\r\\n\";"] # [doc = " assert_eq!(Some(Match::must(0, 2..5)), re.find(hay));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn new (pattern : & str) -> Result < Regex , BuildError > { Regex :: builder () . build (pattern) } # [doc = " Builds a `Regex` from many pattern strings using the default"] # [doc = " configuration."] # [doc = ""] # [doc = " If there was a problem parsing any of the patterns or a problem turning"] # [doc = " them into a regex matcher, then an error is returned."] # [doc = ""] # [doc = " If you want to change the configuration of a `Regex`, use a [`Builder`]"] # [doc = " with a [`Config`]."] # [doc = ""] # [doc = " # Example: simple lexer"] # [doc = ""] # [doc = " This simplistic example leverages the multi-pattern support to build a"] # [doc = " simple little lexer. The pattern ID in the match tells you which regex"] # [doc = " matched, which in turn might be used to map back to the \"type\" of the"] # [doc = " token returned by the lexer."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Match};"] # [doc = ""] # [doc = " let re = Regex::new_many(&["] # [doc = "     r\"[[:space:]]\","] # [doc = "     r\"[A-Za-z0-9][A-Za-z0-9_]+\","] # [doc = "     r\"->\","] # [doc = "     r\".\","] # [doc = " ])?;"] # [doc = " let haystack = \"fn is_boss(bruce: i32, springsteen: String) -> bool;\";"] # [doc = " let matches: Vec<Match> = re.find_iter(haystack).collect();"] # [doc = " assert_eq!(matches, vec!["] # [doc = "     Match::must(1, 0..2),   // 'fn'"] # [doc = "     Match::must(0, 2..3),   // ' '"] # [doc = "     Match::must(1, 3..10),  // 'is_boss'"] # [doc = "     Match::must(3, 10..11), // '('"] # [doc = "     Match::must(1, 11..16), // 'bruce'"] # [doc = "     Match::must(3, 16..17), // ':'"] # [doc = "     Match::must(0, 17..18), // ' '"] # [doc = "     Match::must(1, 18..21), // 'i32'"] # [doc = "     Match::must(3, 21..22), // ','"] # [doc = "     Match::must(0, 22..23), // ' '"] # [doc = "     Match::must(1, 23..34), // 'springsteen'"] # [doc = "     Match::must(3, 34..35), // ':'"] # [doc = "     Match::must(0, 35..36), // ' '"] # [doc = "     Match::must(1, 36..42), // 'String'"] # [doc = "     Match::must(3, 42..43), // ')'"] # [doc = "     Match::must(0, 43..44), // ' '"] # [doc = "     Match::must(2, 44..46), // '->'"] # [doc = "     Match::must(0, 46..47), // ' '"] # [doc = "     Match::must(1, 47..51), // 'bool'"] # [doc = "     Match::must(3, 51..52), // ';'"] # [doc = " ]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " One can write a lexer like the above using a regex like"] # [doc = " `(?P<space>[[:space:]])|(?P<ident>[A-Za-z0-9][A-Za-z0-9_]+)|...`,"] # [doc = " but then you need to ask whether capture group matched to determine"] # [doc = " which branch in the regex matched, and thus, which token the match"] # [doc = " corresponds to. In contrast, the above example includes the pattern ID"] # [doc = " in the match. There's no need to use capture groups at all."] # [doc = ""] # [doc = " # Example: finding the pattern that caused an error"] # [doc = ""] # [doc = " When a syntax error occurs, it is possible to ask which pattern"] # [doc = " caused the syntax error."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, PatternID};"] # [doc = ""] # [doc = " let err = Regex::new_many(&[\"a\", \"b\", r\"\\p{Foo}\", \"c\"]).unwrap_err();"] # [doc = " assert_eq!(Some(PatternID::must(2)), err.pattern());"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: zero patterns is valid"] # [doc = ""] # [doc = " Building a regex with zero patterns results in a regex that never"] # [doc = " matches anything. Because this routine is generic, passing an empty"] # [doc = " slice usually requires a turbo-fish (or something else to help type"] # [doc = " inference)."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, util::syntax, Match};"] # [doc = ""] # [doc = " let re = Regex::new_many::<&str>(&[])?;"] # [doc = " assert_eq!(None, re.find(\"\"));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn new_many < P : AsRef < str > > (patterns : & [P] ,) -> Result < Regex , BuildError > { Regex :: builder () . build_many (patterns) } # [doc = " Return a default configuration for a `Regex`."] # [doc = ""] # [doc = " This is a convenience routine to avoid needing to import the [`Config`]"] # [doc = " type when customizing the construction of a `Regex`."] # [doc = ""] # [doc = " # Example: lower the NFA size limit"] # [doc = ""] # [doc = " In some cases, the default size limit might be too big. The size limit"] # [doc = " can be lowered, which will prevent large regex patterns from compiling."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::meta::Regex;"] # [doc = ""] # [doc = " let result = Regex::builder()"] # [doc = "     .configure(Regex::config().nfa_size_limit(Some(20 * (1<<10))))"] # [doc = "     // Not even 20KB is enough to build a single large Unicode class!"] # [doc = "     .build(r\"\\pL\");"] # [doc = " assert!(result.is_err());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn config () -> Config { Config :: new () } # [doc = " Return a builder for configuring the construction of a `Regex`."] # [doc = ""] # [doc = " This is a convenience routine to avoid needing to import the"] # [doc = " [`Builder`] type in common cases."] # [doc = ""] # [doc = " # Example: change the line terminator"] # [doc = ""] # [doc = " This example shows how to enable multi-line mode by default and change"] # [doc = " the line terminator to the NUL byte:"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, util::syntax, Match};"] # [doc = ""] # [doc = " let re = Regex::builder()"] # [doc = "     .syntax(syntax::Config::new().multi_line(true))"] # [doc = "     .configure(Regex::config().line_terminator(b'\\x00'))"] # [doc = "     .build(r\"^foo$\")?;"] # [doc = " let hay = \"\\x00foo\\x00\";"] # [doc = " assert_eq!(Some(Match::must(0, 1..4)), re.find(hay));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn builder () -> Builder { Builder :: new () } }
};
}
