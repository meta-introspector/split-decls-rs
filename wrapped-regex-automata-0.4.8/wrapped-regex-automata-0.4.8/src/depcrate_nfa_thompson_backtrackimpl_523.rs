// Generated macro for impl_523 (impl)
macro_rules! Depcrate_nfa_thompson_backtrackimpl_523 {
() => {
// Module: crate::nfa::thompson::backtrack
// Provides: {"impl_523"}
// Dependencies: {}
impl Visited { # [doc = " The size of each block, in bits."] const BLOCK_SIZE : usize = 8 * core :: mem :: size_of :: < usize > () ; # [doc = " Create a new visited set for the given backtracker."] # [doc = ""] # [doc = " The set is ready to use, but must be setup at the beginning of each"] # [doc = " search by calling `setup_search`."] fn new (re : & BoundedBacktracker) -> Visited { let mut visited = Visited { bitset : vec ! [] , stride : 0 } ; visited . reset (re) ; visited } # [doc = " Insert the given (StateID, offset) pair into this set. If it already"] # [doc = " exists, then this is a no-op and it returns false. Otherwise this"] # [doc = " returns true."] fn insert (& mut self , sid : StateID , at : usize) -> bool { let table_index = sid . as_usize () * self . stride + at ; let block_index = table_index / Visited :: BLOCK_SIZE ; let bit = table_index % Visited :: BLOCK_SIZE ; let block_with_bit = 1 << bit ; if self . bitset [block_index] & block_with_bit != 0 { return false ; } self . bitset [block_index] |= block_with_bit ; true } # [doc = " Reset this visited set to work with the given bounded backtracker."] fn reset (& mut self , _ : & BoundedBacktracker) { self . bitset . truncate (0) ; } # [doc = " Setup this visited set to work for a search using the given NFA"] # [doc = " and input configuration. The NFA must be the same NFA used by the"] # [doc = " BoundedBacktracker given to Visited::reset. Failing to call this might"] # [doc = " result in panics or silently incorrect search behavior."] fn setup_search (& mut self , re : & BoundedBacktracker , input : & Input < '_ > ,) -> Result < () , MatchError > { let haylen = input . get_span () . len () ; let err = | | MatchError :: haystack_too_long (haylen) ; self . stride = haylen + 1 ; let needed_capacity = match re . get_nfa () . states () . len () . checked_mul (self . stride) { None => return Err (err ()) , Some (capacity) => capacity , } ; let max_capacity = 8 * re . get_config () . get_visited_capacity () ; if needed_capacity > max_capacity { return Err (err ()) ; } let needed_blocks = div_ceil (needed_capacity , Visited :: BLOCK_SIZE) ; self . bitset . truncate (needed_blocks) ; for block in self . bitset . iter_mut () { * block = 0 ; } if needed_blocks > self . bitset . len () { self . bitset . resize (needed_blocks , 0) ; } Ok (()) } # [doc = " Return the heap memory usage, in bytes, of this visited set."] fn memory_usage (& self) -> usize { self . bitset . len () * core :: mem :: size_of :: < usize > () } }
};
}
