// Generated macro for impl_592 (impl)
macro_rules! Depcrate_nfa_thompson_mapimpl_592 {
() => {
// Module: crate::nfa::thompson::map
// Provides: {"impl_592"}
// Dependencies: {}
impl Utf8SuffixMap { # [doc = " Create a new bounded map with the given capacity. The map will never"] # [doc = " grow beyond the given size."] # [doc = ""] # [doc = " Note that this does not allocate. Instead, callers must call `clear`"] # [doc = " before using this map. `clear` will allocate space if necessary."] # [doc = ""] # [doc = " This avoids the need to pay for the allocation of this map when"] # [doc = " compiling regexes that lack large Unicode character classes."] pub fn new (capacity : usize) -> Utf8SuffixMap { assert ! (capacity > 0) ; Utf8SuffixMap { version : 0 , capacity , map : vec ! [] } } # [doc = " Clear this map of all entries, but permit the reuse of allocation"] # [doc = " if possible."] # [doc = ""] # [doc = " This must be called before the map can be used."] pub fn clear (& mut self) { if self . map . is_empty () { self . map = vec ! [Utf8SuffixEntry :: default () ; self . capacity] ; } else { self . version = self . version . wrapping_add (1) ; if self . version == 0 { self . map = vec ! [Utf8SuffixEntry :: default () ; self . capacity] ; } } } # [doc = " Return a hash of the given transition."] pub fn hash (& self , key : & Utf8SuffixKey) -> usize { const PRIME : u64 = 1099511628211 ; const INIT : u64 = 14695981039346656037 ; let mut h = INIT ; h = (h ^ key . from . as_u64 ()) . wrapping_mul (PRIME) ; h = (h ^ u64 :: from (key . start)) . wrapping_mul (PRIME) ; h = (h ^ u64 :: from (key . end)) . wrapping_mul (PRIME) ; (h % self . map . len () . as_u64 ()) . as_usize () } # [doc = " Retrieve the cached state ID corresponding to the given key. The hash"] # [doc = " given must have been computed with `hash` using the same key value."] # [doc = ""] # [doc = " If there is no cached state with the given key, then None is returned."] pub fn get (& mut self , key : & Utf8SuffixKey , hash : usize ,) -> Option < StateID > { let entry = & self . map [hash] ; if entry . version != self . version { return None ; } if key != & entry . key { return None ; } Some (entry . val) } # [doc = " Add a cached state to this map with the given key. Callers should"] # [doc = " ensure that `state_id` points to a state that contains precisely the"] # [doc = " NFA transition given."] # [doc = ""] # [doc = " `hash` must have been computed using the `hash` method with the same"] # [doc = " key."] pub fn set (& mut self , key : Utf8SuffixKey , hash : usize , state_id : StateID) { self . map [hash] = Utf8SuffixEntry { version : self . version , key , val : state_id } ; } }
};
}
