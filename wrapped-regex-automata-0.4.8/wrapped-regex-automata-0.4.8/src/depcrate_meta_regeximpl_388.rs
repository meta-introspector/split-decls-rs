// Generated macro for impl_388 (impl)
macro_rules! Depcrate_meta_regeximpl_388 {
() => {
// Module: crate::meta::regex
// Provides: {"impl_388"}
// Dependencies: {}
# [doc = " Lower level search routines that give more control."] impl Regex { # [doc = " Returns the start and end offset of the leftmost match. If no match"] # [doc = " exists, then `None` is returned."] # [doc = ""] # [doc = " This is like [`Regex::find`] but, but it accepts a concrete `&Input`"] # [doc = " instead of an `Into<Input>`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Input, Match};"] # [doc = ""] # [doc = " let re = Regex::new(r\"Samwise|Sam\")?;"] # [doc = " let input = Input::new("] # [doc = "     \"one of the chief characters, Samwise the Brave\","] # [doc = " );"] # [doc = " assert_eq!(Some(Match::must(0, 29..36)), re.search(&input));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn search (& self , input : & Input < '_ >) -> Option < Match > { if self . imp . info . is_impossible (input) { return None ; } let mut guard = self . pool . get () ; let result = self . imp . strat . search (& mut guard , input) ; PoolGuard :: put (guard) ; result } # [doc = " Returns the end offset of the leftmost match. If no match exists, then"] # [doc = " `None` is returned."] # [doc = ""] # [doc = " This is distinct from [`Regex::search`] in that it only returns the end"] # [doc = " of a match and not the start of the match. Depending on a variety of"] # [doc = " implementation details, this _may_ permit the regex engine to do less"] # [doc = " overall work. For example, if a DFA is being used to execute a search,"] # [doc = " then the start of a match usually requires running a separate DFA in"] # [doc = " reverse to the find the start of a match. If one only needs the end of"] # [doc = " a match, then the separate reverse scan to find the start of a match"] # [doc = " can be skipped. (Note that the reverse scan is avoided even when using"] # [doc = " `Regex::search` when possible, for example, in the case of an anchored"] # [doc = " search.)"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{meta::Regex, Input, HalfMatch};"] # [doc = ""] # [doc = " let re = Regex::new(r\"Samwise|Sam\")?;"] # [doc = " let input = Input::new("] # [doc = "     \"one of the chief characters, Samwise the Brave\","] # [doc = " );"] # [doc = " assert_eq!(Some(HalfMatch::must(0, 36)), re.search_half(&input));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn search_half (& self , input : & Input < '_ >) -> Option < HalfMatch > { if self . imp . info . is_impossible (input) { return None ; } let mut guard = self . pool . get () ; let result = self . imp . strat . search_half (& mut guard , input) ; PoolGuard :: put (guard) ; result } # [doc = " Executes a leftmost forward search and writes the spans of capturing"] # [doc = " groups that participated in a match into the provided [`Captures`]"] # [doc = " value. If no match was found, then [`Captures::is_match`] is guaranteed"] # [doc = " to return `false`."] # [doc = ""] # [doc = " This is like [`Regex::captures`], but it accepts a concrete `&Input`"] # [doc = " instead of an `Into<Input>`."] # [doc = ""] # [doc = " # Example: specific pattern search"] # [doc = ""] # [doc = " This example shows how to build a multi-pattern `Regex` that permits"] # [doc = " searching for specific patterns."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     meta::Regex,"] # [doc = "     Anchored, Match, PatternID, Input,"] # [doc = " };"] # [doc = ""] # [doc = " let re = Regex::new_many(&[\"[a-z0-9]{6}\", \"[a-z][a-z0-9]{5}\"])?;"] # [doc = " let mut caps = re.create_captures();"] # [doc = " let haystack = \"foo123\";"] # [doc = ""] # [doc = " // Since we are using the default leftmost-first match and both"] # [doc = " // patterns match at the same starting position, only the first pattern"] # [doc = " // will be returned in this case when doing a search for any of the"] # [doc = " // patterns."] # [doc = " let expected = Some(Match::must(0, 0..6));"] # [doc = " re.search_captures(&Input::new(haystack), &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " // But if we want to check whether some other pattern matches, then we"] # [doc = " // can provide its pattern ID."] # [doc = " let expected = Some(Match::must(1, 0..6));"] # [doc = " let input = Input::new(haystack)"] # [doc = "     .anchored(Anchored::Pattern(PatternID::must(1)));"] # [doc = " re.search_captures(&input, &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: specifying the bounds of a search"] # [doc = ""] # [doc = " This example shows how providing the bounds of a search can produce"] # [doc = " different results than simply sub-slicing the haystack."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{meta::Regex, Match, Input};"] # [doc = ""] # [doc = " let re = Regex::new(r\"\\b[0-9]{3}\\b\")?;"] # [doc = " let mut caps = re.create_captures();"] # [doc = " let haystack = \"foo123bar\";"] # [doc = ""] # [doc = " // Since we sub-slice the haystack, the search doesn't know about"] # [doc = " // the larger context and assumes that `123` is surrounded by word"] # [doc = " // boundaries. And of course, the match position is reported relative"] # [doc = " // to the sub-slice as well, which means we get `0..3` instead of"] # [doc = " // `3..6`."] # [doc = " let expected = Some(Match::must(0, 0..3));"] # [doc = " let input = Input::new(&haystack[3..6]);"] # [doc = " re.search_captures(&input, &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " // But if we provide the bounds of the search within the context of the"] # [doc = " // entire haystack, then the search can take the surrounding context"] # [doc = " // into account. (And if we did find a match, it would be reported"] # [doc = " // as a valid offset into `haystack` instead of its sub-slice.)"] # [doc = " let expected = None;"] # [doc = " let input = Input::new(haystack).range(3..6);"] # [doc = " re.search_captures(&input, &mut caps);"] # [doc = " assert_eq!(expected, caps.get_match());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn search_captures (& self , input : & Input < '_ > , caps : & mut Captures) { caps . set_pattern (None) ; let pid = self . search_slots (input , caps . slots_mut ()) ; caps . set_pattern (pid) ; } # [doc = " Executes a leftmost forward search and writes the spans of capturing"] # [doc = " groups that participated in a match into the provided `slots`, and"] # [doc = " returns the matching pattern ID. The contents of the slots for patterns"] # [doc = " other than the matching pattern are unspecified. If no match was found,"] # [doc = " then `None` is returned and the contents of `slots` is unspecified."] # [doc = ""] # [doc = " This is like [`Regex::search`], but it accepts a raw slots slice"] # [doc = " instead of a `Captures` value. This is useful in contexts where you"] # [doc = " don't want or need to allocate a `Captures`."] # [doc = ""] # [doc = " It is legal to pass _any_ number of slots to this routine. If the regex"] # [doc = " engine would otherwise write a slot offset that doesn't fit in the"] # [doc = " provided slice, then it is simply skipped. In general though, there are"] # [doc = " usually three slice lengths you might want to use:"] # [doc = ""] # [doc = " * An empty slice, if you only care about which pattern matched."] # [doc = " * A slice with [`pattern_len() * 2`](Regex::pattern_len) slots, if you"] # [doc = " only care about the overall match spans for each matching pattern."] # [doc = " * A slice with"] # [doc = " [`slot_len()`](crate::util::captures::GroupInfo::slot_len) slots, which"] # [doc = " permits recording match offsets for every capturing group in every"] # [doc = " pattern."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to find the overall match offsets in a"] # [doc = " multi-pattern search without allocating a `Captures` value. Indeed, we"] # [doc = " can put our slots right on the stack."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{meta::Regex, PatternID, Input};"] # [doc = ""] # [doc = " let re = Regex::new_many(&["] # [doc = "     r\"\\pL+\","] # [doc = "     r\"\\d+\","] # [doc = " ])?;"] # [doc = " let input = Input::new(\"!@#123\");"] # [doc = ""] # [doc = " // We only care about the overall match offsets here, so we just"] # [doc = " // allocate two slots for each pattern. Each slot records the start"] # [doc = " // and end of the match."] # [doc = " let mut slots = [None; 4];"] # [doc = " let pid = re.search_slots(&input, &mut slots);"] # [doc = " assert_eq!(Some(PatternID::must(1)), pid);"] # [doc = ""] # [doc = " // The overall match offsets are always at 'pid * 2' and 'pid * 2 + 1'."] # [doc = " // See 'GroupInfo' for more details on the mapping between groups and"] # [doc = " // slot indices."] # [doc = " let slot_start = pid.unwrap().as_usize() * 2;"] # [doc = " let slot_end = slot_start + 1;"] # [doc = " assert_eq!(Some(3), slots[slot_start].map(|s| s.get()));"] # [doc = " assert_eq!(Some(6), slots[slot_end].map(|s| s.get()));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn search_slots (& self , input : & Input < '_ > , slots : & mut [Option < NonMaxUsize >] ,) -> Option < PatternID > { if self . imp . info . is_impossible (input) { return None ; } let mut guard = self . pool . get () ; let result = self . imp . strat . search_slots (& mut guard , input , slots) ; PoolGuard :: put (guard) ; result } # [doc = " Writes the set of patterns that match anywhere in the given search"] # [doc = " configuration to `patset`. If multiple patterns match at the same"] # [doc = " position and this `Regex` was configured with [`MatchKind::All`]"] # [doc = " semantics, then all matching patterns are written to the given set."] # [doc = ""] # [doc = " Unless all of the patterns in this `Regex` are anchored, then generally"] # [doc = " speaking, this will scan the entire haystack."] # [doc = ""] # [doc = " This search routine *does not* clear the pattern set. This gives some"] # [doc = " flexibility to the caller (e.g., running multiple searches with the"] # [doc = " same pattern set), but does make the API bug-prone if you're reusing"] # [doc = " the same pattern set for multiple searches but intended them to be"] # [doc = " independent."] # [doc = ""] # [doc = " If a pattern ID matched but the given `PatternSet` does not have"] # [doc = " sufficient capacity to store it, then it is not inserted and silently"] # [doc = " dropped."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to find all matching patterns in a haystack,"] # [doc = " even when some patterns match at the same position as other patterns."] # [doc = " It is important that we configure the `Regex` with [`MatchKind::All`]"] # [doc = " semantics here, or else overlapping matches will not be reported."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::{meta::Regex, Input, MatchKind, PatternSet};"] # [doc = ""] # [doc = " let patterns = &["] # [doc = "     r\"\\w+\", r\"\\d+\", r\"\\pL+\", r\"foo\", r\"bar\", r\"barfoo\", r\"foobar\","] # [doc = " ];"] # [doc = " let re = Regex::builder()"] # [doc = "     .configure(Regex::config().match_kind(MatchKind::All))"] # [doc = "     .build_many(patterns)?;"] # [doc = ""] # [doc = " let input = Input::new(\"foobar\");"] # [doc = " let mut patset = PatternSet::new(re.pattern_len());"] # [doc = " re.which_overlapping_matches(&input, &mut patset);"] # [doc = " let expected = vec![0, 2, 3, 4, 6];"] # [doc = " let got: Vec<usize> = patset.iter().map(|p| p.as_usize()).collect();"] # [doc = " assert_eq!(expected, got);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn which_overlapping_matches (& self , input : & Input < '_ > , patset : & mut PatternSet ,) { if self . imp . info . is_impossible (input) { return ; } let mut guard = self . pool . get () ; let result = self . imp . strat . which_overlapping_matches (& mut guard , input , patset) ; PoolGuard :: put (guard) ; result } }
};
}
