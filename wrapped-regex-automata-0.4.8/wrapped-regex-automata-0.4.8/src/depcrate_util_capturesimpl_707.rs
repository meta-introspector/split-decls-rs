// Generated macro for impl_707 (impl)
macro_rules! Depcrate_util_capturesimpl_707 {
() => {
// Module: crate::util::captures
// Provides: {"impl_707"}
// Dependencies: {}
# [doc = " Lower level \"slot\" oriented APIs. One does not typically need to use these"] # [doc = " when executing a search. They are instead mostly intended for folks that"] # [doc = " are writing their own regex engine while reusing this `Captures` type."] impl Captures { # [doc = " Clear this `Captures` value."] # [doc = ""] # [doc = " After clearing, all slots inside this `Captures` value will be set to"] # [doc = " `None`. Similarly, any pattern ID that it was previously associated"] # [doc = " with (for a match) is erased."] # [doc = ""] # [doc = " It is not usually necessary to call this routine. Namely, a `Captures`"] # [doc = " value only provides high level access to the capturing groups of the"] # [doc = " pattern that matched, and only low level access to individual slots."] # [doc = " Thus, even if slots corresponding to groups that aren't associated"] # [doc = " with the matching pattern are set, then it won't impact the higher"] # [doc = " level APIs. Namely, higher level APIs like [`Captures::get_group`] will"] # [doc = " return `None` if no pattern ID is present, even if there are spans set"] # [doc = " in the underlying slots."] # [doc = ""] # [doc = " Thus, to \"clear\" a `Captures` value of a match, it is usually only"] # [doc = " necessary to call [`Captures::set_pattern`] with `None`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows what happens when a `Captures` value is cleared."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::nfa::thompson::pikevm::PikeVM;"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"^(?P<first>\\pL+)\\s+(?P<last>\\pL+)$\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"Bruce Springsteen\", &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " let slots: Vec<Option<usize>> ="] # [doc = "     caps.slots().iter().map(|s| s.map(|x| x.get())).collect();"] # [doc = " // Note that the following ordering is considered an API guarantee."] # [doc = " assert_eq!(slots, vec!["] # [doc = "     Some(0),"] # [doc = "     Some(17),"] # [doc = "     Some(0),"] # [doc = "     Some(5),"] # [doc = "     Some(6),"] # [doc = "     Some(17),"] # [doc = " ]);"] # [doc = ""] # [doc = " // Now clear the slots. Everything is gone and it is no longer a match."] # [doc = " caps.clear();"] # [doc = " assert!(!caps.is_match());"] # [doc = " let slots: Vec<Option<usize>> ="] # [doc = "     caps.slots().iter().map(|s| s.map(|x| x.get())).collect();"] # [doc = " assert_eq!(slots, vec!["] # [doc = "     None,"] # [doc = "     None,"] # [doc = "     None,"] # [doc = "     None,"] # [doc = "     None,"] # [doc = "     None,"] # [doc = " ]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn clear (& mut self) { self . pid = None ; for slot in self . slots . iter_mut () { * slot = None ; } } # [doc = " Set the pattern on this `Captures` value."] # [doc = ""] # [doc = " When the pattern ID is `None`, then this `Captures` value does not"] # [doc = " correspond to a match (`is_match` will return `false`). Otherwise, it"] # [doc = " corresponds to a match."] # [doc = ""] # [doc = " This is useful in search implementations where you might want to"] # [doc = " initially call `set_pattern(None)` in order to avoid the cost of"] # [doc = " calling `clear()` if it turns out to not be necessary."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows that `set_pattern` merely overwrites the pattern ID."] # [doc = " It does not actually change the underlying slot values."] # [doc = ""] # [doc = " ```"] # [doc = " # if cfg!(miri) { return Ok(()); } // miri takes too long"] # [doc = " use regex_automata::nfa::thompson::pikevm::PikeVM;"] # [doc = ""] # [doc = " let re = PikeVM::new(r\"^(?P<first>\\pL+)\\s+(?P<last>\\pL+)$\")?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"Bruce Springsteen\", &mut caps);"] # [doc = " assert!(caps.is_match());"] # [doc = " assert!(caps.pattern().is_some());"] # [doc = " let slots: Vec<Option<usize>> ="] # [doc = "     caps.slots().iter().map(|s| s.map(|x| x.get())).collect();"] # [doc = " // Note that the following ordering is considered an API guarantee."] # [doc = " assert_eq!(slots, vec!["] # [doc = "     Some(0),"] # [doc = "     Some(17),"] # [doc = "     Some(0),"] # [doc = "     Some(5),"] # [doc = "     Some(6),"] # [doc = "     Some(17),"] # [doc = " ]);"] # [doc = ""] # [doc = " // Now set the pattern to None. Note that the slot values remain."] # [doc = " caps.set_pattern(None);"] # [doc = " assert!(!caps.is_match());"] # [doc = " assert!(!caps.pattern().is_some());"] # [doc = " let slots: Vec<Option<usize>> ="] # [doc = "     caps.slots().iter().map(|s| s.map(|x| x.get())).collect();"] # [doc = " // Note that the following ordering is considered an API guarantee."] # [doc = " assert_eq!(slots, vec!["] # [doc = "     Some(0),"] # [doc = "     Some(17),"] # [doc = "     Some(0),"] # [doc = "     Some(5),"] # [doc = "     Some(6),"] # [doc = "     Some(17),"] # [doc = " ]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn set_pattern (& mut self , pid : Option < PatternID >) { self . pid = pid ; } # [doc = " Returns the underlying slots, where each slot stores a single offset."] # [doc = ""] # [doc = " Every matching capturing group generally corresponds to two slots: one"] # [doc = " slot for the starting position and another for the ending position."] # [doc = " Typically, either both are present or neither are. (The weasel word"] # [doc = " \"typically\" is used here because it really depends on the regex engine"] # [doc = " implementation. Every sensible regex engine likely adheres to this"] # [doc = " invariant, and every regex engine in this crate is sensible.)"] # [doc = ""] # [doc = " Generally speaking, callers should prefer to use higher level routines"] # [doc = " like [`Captures::get_match`] or [`Captures::get_group`]."] # [doc = ""] # [doc = " An important note here is that a regex engine may not reset all of the"] # [doc = " slots to `None` values when no match occurs, or even when a match of"] # [doc = " a different pattern occurs. But this depends on how the regex engine"] # [doc = " implementation deals with slots."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to get the underlying slots from a regex match."] # [doc = ""] # [doc = " ```"] # [doc = " use regex_automata::{"] # [doc = "     nfa::thompson::pikevm::PikeVM,"] # [doc = "     util::primitives::{PatternID, NonMaxUsize},"] # [doc = " };"] # [doc = ""] # [doc = " let re = PikeVM::new_many(&["] # [doc = "     r\"[a-z]+\","] # [doc = "     r\"[0-9]+\","] # [doc = " ])?;"] # [doc = " let (mut cache, mut caps) = (re.create_cache(), re.create_captures());"] # [doc = ""] # [doc = " re.captures(&mut cache, \"123\", &mut caps);"] # [doc = " assert_eq!(Some(PatternID::must(1)), caps.pattern());"] # [doc = " // Note that the only guarantee we have here is that slots 2 and 3"] # [doc = " // are set to correct values. The contents of the first two slots are"] # [doc = " // unspecified since the 0th pattern did not match."] # [doc = " let expected = &["] # [doc = "     None,"] # [doc = "     None,"] # [doc = "     NonMaxUsize::new(0),"] # [doc = "     NonMaxUsize::new(3),"] # [doc = " ];"] # [doc = " assert_eq!(expected, caps.slots());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [inline] pub fn slots (& self) -> & [Option < NonMaxUsize >] { & self . slots } # [doc = " Returns the underlying slots as a mutable slice, where each slot stores"] # [doc = " a single offset."] # [doc = ""] # [doc = " This tends to be most useful for regex engine implementations for"] # [doc = " writing offsets for matching capturing groups to slots."] # [doc = ""] # [doc = " See [`Captures::slots`] for more information about slots."] # [inline] pub fn slots_mut (& mut self) -> & mut [Option < NonMaxUsize >] { & mut self . slots } }
};
}
