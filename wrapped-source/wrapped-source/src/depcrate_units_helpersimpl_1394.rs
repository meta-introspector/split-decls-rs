// Generated macro for impl_1394 (impl)
macro_rules! Depcrate_units_helpersimpl_1394 {
() => {
// Module: crate::units::helpers
// Provides: {"impl_1394"}
// Dependencies: {}
impl Supplemental { # [doc = " Helper function to check if a target unit matches any unit in the set,"] # [doc = " including as a subunit in compound units (e.g., \"foot\" matches \"foot-and-inch\")"] fn unit_matches_any (target_unit : & str , units : & HashSet < String >) -> bool { units . iter () . any (| u | u . contains (target_unit)) } # [doc = " Returns a map from each category (e.g., \"length\", \"mass\", \"duration\") to a map from each region to a set of units."] # [doc = " This excludes usages; it only checks if the unit is present for a given country in a specific category and region."] # [doc = " NOTE:"] # [doc = "   The units can be present as compound units (e.g., \"square-meter\") or mixed units (e.g., \"foot-and-inch\")."] pub (crate) fn categorized_units_list (& self) -> CategorizedUnitsList { let mut result = BTreeMap :: new () ; for (category , usage_prefs) in & self . unit_preference_data { let mut region_units_map = BTreeMap :: new () ; for region_prefs in usage_prefs . values () { for (region_str , unit_prefs) in region_prefs { let units_set = region_units_map . entry (region_str . clone ()) . or_insert_with (HashSet :: new) ; for unit_pref in unit_prefs { units_set . insert (unit_pref . unit . clone ()) ; } } } result . insert (category . clone () , region_units_map) ; } result } # [doc = " Determines the type of unit based on the region and category."] # [doc = " If the unit is present in the unit preferences and the region is present in the unit preferences, it is a core unit."] # [doc = " If the unit is present in the unit preferences but not in the region preferences, it is an extended unit."] # [doc = " If the unit is not present in the unit preferences, it is an outlier unit."] # [doc = ""] # [doc = " If the specific region is not found, falls back to region \"001\" (world/default region)."] # [doc = ""] # [doc = " The matching includes subunit matching: a unit like \"foot\" will match compound units like \"foot-and-inch\"."] pub (crate) fn unit_type (category : & str , unit : & str , region : & str , categorized_units_list : & CategorizedUnitsList ,) -> UnitType { let region_units = match categorized_units_list . get (category) { Some (units) => units , None => return UnitType :: Outlier , } ; let found_in_region = region_units . get (region) . or_else (| | region_units . get ("001")) . is_some_and (| units | Self :: unit_matches_any (unit , units)) ; let found_anywhere = region_units . values () . any (| units | Self :: unit_matches_any (unit , units)) ; if found_in_region { UnitType :: Core } else if found_anywhere { UnitType :: Extended } else { UnitType :: Outlier } } }
};
}
