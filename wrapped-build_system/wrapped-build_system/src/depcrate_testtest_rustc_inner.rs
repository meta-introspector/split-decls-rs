// Generated macro for test_rustc_inner (function)
macro_rules! Depcrate_testtest_rustc_inner {
() => {
// Module: crate::test
// Provides: {"test_rustc_inner"}
// Dependencies: {}
fn test_rustc_inner < F > (env : & Env , args : & TestArg , prepare_files_callback : F , run_error_pattern_test : bool , test_type : & str ,) -> Result < () , String > where F : Fn (& Path) -> Result < bool , String > , { println ! ("[TEST] rust-lang/rust") ; let mut env = env . clone () ; let rust_path = setup_rustc (& mut env , args) ? ; if ! prepare_files_callback (& rust_path) ? { println ! ("Keeping all {test_type} tests") ; } if test_type == "ui" { if run_error_pattern_test { walk_dir (rust_path . join ("tests/ui") , & mut | _dir | Ok (()) , & mut | file_path | { if contains_ui_error_patterns (file_path , args . keep_lto_tests) ? { Ok (()) } else { remove_file (file_path) . map_err (| e | e . to_string ()) } } , true ,) ? ; } else { walk_dir (rust_path . join ("tests/ui") , & mut | dir | { let dir_name = dir . file_name () . and_then (| name | name . to_str ()) . unwrap_or ("") ; if ["abi" , "extern" , "unsized-locals" , "proc-macro" , "threads-sendsync" , "borrowck" , "test-attrs" ,] . contains (& dir_name) { remove_dir_all (dir) . map_err (| error | { format ! ("Failed to remove folder `{}`: {:?}" , dir . display () , error) }) ? ; } Ok (()) } , & mut | _ | Ok (()) , false ,) ? ; fn dir_handling (keep_lto_tests : bool) -> impl Fn (& Path) -> Result < () , String > { move | dir | { if dir . file_name () . map (| name | name == "auxiliary") . unwrap_or (true) { return Ok (()) ; } walk_dir (dir , & mut dir_handling (keep_lto_tests) , & mut file_handling (keep_lto_tests) , false ,) } } fn file_handling (keep_lto_tests : bool) -> impl Fn (& Path) -> Result < () , String > { move | file_path | { if ! file_path . extension () . map (| extension | extension == "rs") . unwrap_or (false) { return Ok (()) ; } let path_str = file_path . display () . to_string () . replace ("\\" , "/") ; if valid_ui_error_pattern_test (& path_str) { return Ok (()) ; } else if contains_ui_error_patterns (file_path , keep_lto_tests) ? { return remove_file (& file_path) ; } Ok (()) } } walk_dir (rust_path . join ("tests/ui") , & mut dir_handling (args . keep_lto_tests) , & mut file_handling (args . keep_lto_tests) , false ,) ? ; } let nb_parts = args . nb_parts . unwrap_or (0) ; if nb_parts > 0 { let current_part = args . current_part . unwrap () ; println ! ("Splitting ui_test into {nb_parts} parts (and running part {current_part})") ; let out = String :: from_utf8 (run_command (& [& "find" , & "tests/ui" , & "-type" , & "f" , & "-name" , & "*.rs" , & "-not" , & "-path" , & "*/auxiliary/*" ,] , Some (& rust_path) ,) ? . stdout ,) . map_err (| error | format ! ("Failed to retrieve output of find command: {error:?}")) ? ; let mut files = out . split ('\n') . map (| line | line . trim ()) . filter (| line | ! line . is_empty ()) . collect :: < Vec < _ > > () ; files . sort () ; let count = files . len () / nb_parts + 1 ; let start = current_part * count ; for path in files . iter () . skip (start) . take (count) { remove_file (& rust_path . join (path)) ? ; } } } println ! ("[TEST] rustc {test_type} test suite") ; let extra = if args . is_using_gcc_master_branch () { "" } else { " -Csymbol-mangling-version=v0" } ; let rustc_args = format ! ("{test_flags} -Zcodegen-backend={backend} --sysroot {sysroot}{extra}" , test_flags = env . get ("TEST_FLAGS") . unwrap_or (& String :: new ()) , backend = args . config_info . cg_backend_path , sysroot = args . config_info . sysroot_path , extra = extra ,) ; env . get_mut ("RUSTFLAGS") . unwrap () . clear () ; run_command_with_output_and_env (& [& "./x.py" , & "test" , & "--run" , & "always" , & "--stage" , & "0" , & "--set" , & "build.compiletest-allow-stage0=true" , & format ! ("tests/{test_type}") , & "--compiletest-rustc-args" , & rustc_args ,] , Some (& rust_path) , Some (& env) ,) ? ; Ok (()) }
};
}
