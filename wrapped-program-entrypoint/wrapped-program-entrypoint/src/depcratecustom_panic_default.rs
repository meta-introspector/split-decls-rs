// Generated macro for custom_panic_default (macro)
macro_rules! Depcratecustom_panic_default {
() => {
// Module: crate
// Provides: {"custom_panic_default"}
// Dependencies: {}
# [doc = " Define the default global panic handler."] # [doc = ""] # [doc = " This must be used if the [`entrypoint`] macro is not used, and no other"] # [doc = " panic handler has been defined; otherwise a program will crash without an"] # [doc = " explicit panic message."] # [doc = ""] # [doc = " The default global allocator is enabled only if the calling crate has not"] # [doc = " disabled it using [Cargo features] as described below. It is only defined"] # [doc = " for [BPF] targets."] # [doc = ""] # [doc = " [Cargo features]: https://doc.rust-lang.org/cargo/reference/features.html"] # [doc = " [BPF]: https://solana.com/docs/programs/faq#berkeley-packet-filter-bpf"] # [doc = ""] # [doc = " # Cargo features"] # [doc = ""] # [doc = " A crate that calls this macro can provide its own custom panic handler, or"] # [doc = " allow others to provide their own custom panic handler, by adding a"] # [doc = " `custom-panic` feature to its `Cargo.toml`. After enabling the feature, one"] # [doc = " may define their own panic handler."] # [doc = ""] # [doc = " A good way to reduce the final size of the program is to provide a"] # [doc = " `custom_panic` implementation that does nothing. Doing so will cut ~25kb"] # [doc = " from a noop program. That number goes down the more the programs pulls in"] # [doc = " Rust's standard library for other purposes."] # [doc = ""] # [doc = " # Defining a panic handler for Solana"] # [doc = ""] # [doc = " _The mechanism for defining a Solana panic handler is different [from most"] # [doc = " Rust programs][rpanic]._"] # [doc = ""] # [doc = " [rpanic]: https://doc.rust-lang.org/nomicon/panic-handler.html"] # [doc = ""] # [doc = " To define a panic handler one must define a `custom_panic` function"] # [doc = " with the `#[no_mangle]` attribute, as below:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " #[cfg(all(feature = \"custom-panic\", target_os = \"solana\"))]"] # [doc = " #[no_mangle]"] # [doc = " fn custom_panic(info: &core::panic::PanicInfo<'_>) {"] # [doc = "     $crate::msg!(\"{}\", info);"] # [doc = " }"] # [doc = " ```"] # [macro_export] macro_rules ! custom_panic_default { () => { # [cfg (all (not (feature = "custom-panic") , target_os = "solana"))] # [no_mangle] fn custom_panic (info : & core :: panic :: PanicInfo <'_ >) { if let Some (mm) = info . message () . as_str () { unsafe { $ crate :: __log (mm . as_ptr () , mm . len () as u64) ; } } if let Some (loc) = info . location () { unsafe { $ crate :: __panic (loc . file () . as_ptr () , loc . file () . len () as u64 , loc . line () as u64 , loc . column () as u64 ,) } } } } ; }
};
}
