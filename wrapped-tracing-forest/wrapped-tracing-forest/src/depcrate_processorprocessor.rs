// Generated macro for Processor (trait)
macro_rules! Depcrate_processorProcessor {
() => {
// Module: crate::processor
// Provides: {"Processor"}
// Dependencies: {}
# [doc = " A trait for processing completed [`Tree`]s."] # [doc = ""] # [doc = " `Processor`s are responsible for both formatting and writing logs to their"] # [doc = " intended destinations. This is typically implemented using"] # [doc = " [`Formatter`], [`MakeWriter`], and [`io::Write`]."] # [doc = ""] # [doc = " While this trait may be implemented on downstream types, [`from_fn`]"] # [doc = " provides a convenient interface for creating `Processor`s without having to"] # [doc = " explicitly define new types."] # [doc = ""] # [doc = " [trace trees]: crate::tree::Tree"] # [doc = " [`Formatter`]: crate::printer::Formatter"] # [doc = " [`MakeWriter`]: tracing_subscriber::fmt::MakeWriter"] # [doc = " [`io::Write`]: std::io::Write"] pub trait Processor : 'static + Sized { # [doc = " Process a [`Tree`]. This can mean many things, such as writing to"] # [doc = " stdout or a file, sending over a network, storing in memory, ignoring,"] # [doc = " or anything else."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the `Tree` cannot be processed, then it is returned along with a"] # [doc = " `Box<dyn Error + Send + Sync>`. If the processor is configured with a"] # [doc = " fallback processor from [`Processor::or`], then the `Tree` is deferred"] # [doc = " to that processor."] # [allow (clippy :: result_large_err)] fn process (& self , tree : Tree) -> Result ; # [doc = " Returns a `Processor` that first attempts processing with `self`, and"] # [doc = " resorts to processing with `fallback` on failure."] # [doc = ""] # [doc = " Note that [`or_stdout`], [`or_stderr`], and [`or_none`] can be used as"] # [doc = " shortcuts for pretty printing or dropping the `Tree` entirely."] # [doc = ""] # [doc = " [`or_stdout`]: Processor::or_stdout"] # [doc = " [`or_stderr`]: Processor::or_stderr"] # [doc = " [`or_none`]: Processor::or_none"] fn or < P : Processor > (self , processor : P) -> WithFallback < Self , P > { WithFallback { primary : self , fallback : processor , } } # [doc = " Returns a `Processor` that first attempts processing with `self`, and"] # [doc = " resorts to pretty-printing to stdout on failure."] fn or_stdout (self) -> WithFallback < Self , Printer < Pretty , MakeStdout > > { self . or (Printer :: new () . writer (MakeStdout)) } # [doc = " Returns a `Processor` that first attempts processing with `self`, and"] # [doc = " resorts to pretty-printing to stderr on failure."] fn or_stderr (self) -> WithFallback < Self , Printer < Pretty , MakeStderr > > { self . or (Printer :: new () . writer (MakeStderr)) } # [doc = " Returns a `Processor` that first attempts processing with `self`, otherwise"] # [doc = " silently fails."] fn or_none (self) -> WithFallback < Self , Sink > { self . or (Sink) } }
};
}
