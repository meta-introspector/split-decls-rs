// Generated macro for TreeIndex (struct)
macro_rules! Depcrate_tree_indexTreeIndex {
() => {
// Module: crate::tree_index
// Provides: {"TreeIndex"}
// Dependencies: {}
# [doc = " Scalable concurrent B-plus tree."] # [doc = ""] # [doc = " [`TreeIndex`] is a concurrent B-plus tree variant optimized for read operations. Read"] # [doc = " operations, such as read iteration over entries, are neither blocked nor interrupted by other"] # [doc = " threads or tasks. Write operations, such as insert and remove, do not block if structural"] # [doc = " changes are not required."] # [doc = ""] # [doc = " ## Note"] # [doc = ""] # [doc = " [`TreeIndex`] methods are linearizable. However, its iterator methods are not; [`Iter`] and"] # [doc = " [`Range`] are only guaranteed to observe events that happened before the first call to"] # [doc = " [`Iterator::next`]."] # [doc = ""] # [doc = " ## The key features of [`TreeIndex`]"] # [doc = ""] # [doc = " * Lock-free read: read and scan operations are never blocked and do not modify shared data."] # [doc = " * Near lock-free write: write operations do not block unless a structural change is needed."] # [doc = " * No busy waiting: each node has a wait queue to avoid spinning."] # [doc = " * Immutability: the data in the container is immutable until it becomes unreachable."] # [doc = ""] # [doc = " ## The key statistics for [`TreeIndex`]"] # [doc = ""] # [doc = " * The maximum number of entries that a leaf can contain: 14."] # [doc = " * The maximum number of leaves or child nodes a node can contain: 15."] # [doc = ""] # [doc = " ## Locking behavior"] # [doc = ""] # [doc = " Read access is always lock-free and non-blocking. Write access to an entry is also lock-free and"] # [doc = " non-blocking as long as no structural changes are required. However, when nodes are split or"] # [doc = " merged by a write operation, other write operations on keys in the affected range are blocked."] # [doc = ""] # [doc = " ### Synchronous methods in an asynchronous code block"] # [doc = ""] # [doc = " It is generally not recommended to use blocking methods, such as [`TreeIndex::insert_sync`], in"] # [doc = " an asynchronous code block or [`poll`](std::future::Future::poll), since it may lead to"] # [doc = " deadlocks or performance degradation."] # [doc = ""] # [doc = " ### Unwind safety"] # [doc = ""] # [doc = " [`TreeIndex`] is impervious to out-of-memory errors and panics in user-specified code under one"] # [doc = " condition; `K::drop` and `V::drop` must not panic."] pub struct TreeIndex < K , V > { root : AtomicShared < Node < K , V > > , }
};
}
