// Generated macro for impl_145 (impl)
macro_rules! Depcrate_hash_mapimpl_145 {
() => {
// Module: crate::hash_map
// Provides: {"impl_145"}
// Dependencies: {}
impl < 'h , K , V , H > OccupiedEntry < 'h , K , V , H > where K : Eq + Hash , H : BuildHasher , { # [doc = " Gets a reference to the key in the entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashMap;"] # [doc = ""] # [doc = " let hashmap: HashMap<u64, u32> = HashMap::default();"] # [doc = ""] # [doc = " assert_eq!(hashmap.entry_sync(29).or_default().key(), &29);"] # [doc = " ```"] # [inline] # [must_use] pub fn key (& self) -> & K { & self . locked_bucket . entry (& self . entry_ptr) . 0 } # [doc = " Takes ownership of the key and value from the [`HashMap`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashMap;"] # [doc = " use scc::hash_map::Entry;"] # [doc = ""] # [doc = " let hashmap: HashMap<u64, u32> = HashMap::default();"] # [doc = ""] # [doc = " hashmap.entry_sync(11).or_insert(17);"] # [doc = ""] # [doc = " if let Entry::Occupied(o) = hashmap.entry_sync(11) {"] # [doc = "     assert_eq!(o.remove_entry(), (11, 17));"] # [doc = " };"] # [doc = " ```"] # [inline] # [must_use] pub fn remove_entry (mut self) -> (K , V) { let guard = Guard :: new () ; let prolonged_guard = self . hashmap . prolonged_guard_ref (& guard) ; self . locked_bucket . remove (self . hashmap , & mut self . entry_ptr , prolonged_guard) } # [doc = " Gets a reference to the value in the entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashMap;"] # [doc = " use scc::hash_map::Entry;"] # [doc = ""] # [doc = " let hashmap: HashMap<u64, u32> = HashMap::default();"] # [doc = ""] # [doc = " hashmap.entry_sync(19).or_insert(11);"] # [doc = ""] # [doc = " if let Entry::Occupied(o) = hashmap.entry_sync(19) {"] # [doc = "     assert_eq!(o.get(), &11);"] # [doc = " };"] # [doc = " ```"] # [inline] # [must_use] pub fn get (& self) -> & V { & self . locked_bucket . entry (& self . entry_ptr) . 1 } # [doc = " Gets a mutable reference to the value in the entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashMap;"] # [doc = " use scc::hash_map::Entry;"] # [doc = ""] # [doc = " let hashmap: HashMap<u64, u32> = HashMap::default();"] # [doc = ""] # [doc = " hashmap.entry_sync(37).or_insert(11);"] # [doc = ""] # [doc = " if let Entry::Occupied(mut o) = hashmap.entry_sync(37) {"] # [doc = "     *o.get_mut() += 18;"] # [doc = "     assert_eq!(*o.get(), 29);"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(hashmap.read_sync(&37, |_, v| *v), Some(29));"] # [doc = " ```"] # [inline] pub fn get_mut (& mut self) -> & mut V { & mut self . locked_bucket . entry_mut (& mut self . entry_ptr) . 1 } # [doc = " Sets the value of the entry, and returns the old value."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashMap;"] # [doc = " use scc::hash_map::Entry;"] # [doc = ""] # [doc = " let hashmap: HashMap<u64, u32> = HashMap::default();"] # [doc = ""] # [doc = " hashmap.entry_sync(37).or_insert(11);"] # [doc = ""] # [doc = " if let Entry::Occupied(mut o) = hashmap.entry_sync(37) {"] # [doc = "     assert_eq!(o.insert(17), 11);"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(hashmap.read_sync(&37, |_, v| *v), Some(17));"] # [doc = " ```"] # [inline] pub fn insert (& mut self , val : V) -> V { replace (self . get_mut () , val) } # [doc = " Takes the value out of the entry, and returns it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashMap;"] # [doc = " use scc::hash_map::Entry;"] # [doc = ""] # [doc = " let hashmap: HashMap<u64, u32> = HashMap::default();"] # [doc = ""] # [doc = " hashmap.entry_sync(11).or_insert(17);"] # [doc = ""] # [doc = " if let Entry::Occupied(o) = hashmap.entry_sync(11) {"] # [doc = "     assert_eq!(o.remove(), 17);"] # [doc = " };"] # [doc = " ```"] # [inline] # [must_use] pub fn remove (self) -> V { self . remove_entry () . 1 } # [doc = " Removes the entry and gets the next closest occupied entry."] # [doc = ""] # [doc = " [`HashMap::begin_async`] and this method together allow the [`OccupiedEntry`] to"] # [doc = " effectively act as a mutable iterator over entries. This method never acquires more than one"] # [doc = " lock, even when it searches other buckets for the next closest occupied entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashMap;"] # [doc = " use scc::hash_map::Entry;"] # [doc = ""] # [doc = " let hashmap: HashMap<u64, u32> = HashMap::default();"] # [doc = ""] # [doc = " assert!(hashmap.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashmap.insert_sync(2, 0).is_ok());"] # [doc = ""] # [doc = " let second_entry_future = hashmap.begin_sync().unwrap().remove_and_async();"] # [doc = " ```"] # [inline] pub async fn remove_and_async (self) -> ((K , V) , Option < OccupiedEntry < 'h , K , V , H > >) { let hashmap = self . hashmap ; let mut entry_ptr = self . entry_ptr . clone () ; let mut async_guard = pin ! (AsyncGuard :: default ()) ; let entry = self . locked_bucket . writer . remove (self . locked_bucket . data_block , & mut entry_ptr , hashmap . prolonged_guard_ref (async_guard . guard ()) ,) ; if let Some (locked_bucket) = self . locked_bucket . next_async (hashmap , & mut entry_ptr , & mut async_guard) . await { return (entry , Some (OccupiedEntry { hashmap , locked_bucket , entry_ptr , }) ,) ; } (entry , None) } # [doc = " Removes the entry and gets the next closest occupied entry."] # [doc = ""] # [doc = " [`HashMap::begin_sync`] and this method together allow the [`OccupiedEntry`] to effectively"] # [doc = " act as a mutable iterator over entries. This method never acquires more than one lock, even"] # [doc = " when it searches other buckets for the next closest occupied entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashMap;"] # [doc = " use scc::hash_map::Entry;"] # [doc = ""] # [doc = " let hashmap: HashMap<u64, u32> = HashMap::default();"] # [doc = ""] # [doc = " assert!(hashmap.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashmap.insert_sync(2, 0).is_ok());"] # [doc = ""] # [doc = " let first_entry = hashmap.begin_sync().unwrap();"] # [doc = " let first_key = *first_entry.key();"] # [doc = " let (removed, second_entry) = first_entry.remove_and_sync();"] # [doc = " assert_eq!(removed.1, 0);"] # [doc = " assert_eq!(hashmap.len(), 1);"] # [doc = ""] # [doc = " let second_entry = second_entry.unwrap();"] # [doc = " let second_key = *second_entry.key();"] # [doc = ""] # [doc = " assert!(second_entry.remove_and_sync().1.is_none());"] # [doc = " assert_eq!(first_key + second_key, 3);"] # [doc = " ```"] # [inline] # [must_use] pub fn remove_and_sync (self) -> ((K , V) , Option < Self >) { let hashmap = self . hashmap ; let mut entry_ptr = self . entry_ptr . clone () ; let guard = Guard :: new () ; let prolonged_guard = hashmap . prolonged_guard_ref (& guard) ; let entry = self . locked_bucket . writer . remove (self . locked_bucket . data_block , & mut entry_ptr , prolonged_guard ,) ; if let Some (locked_bucket) = self . locked_bucket . next_sync (hashmap , & mut entry_ptr , prolonged_guard) { return (entry , Some (OccupiedEntry { hashmap , locked_bucket , entry_ptr , }) ,) ; } (entry , None) } # [doc = " Gets the next closest occupied entry."] # [doc = ""] # [doc = " [`HashMap::begin_async`] and this method together allow the [`OccupiedEntry`] to"] # [doc = " effectively act as a mutable iterator over entries. This method never acquires more than one"] # [doc = " lock, even when it searches other buckets for the next closest occupied entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashMap;"] # [doc = " use scc::hash_map::Entry;"] # [doc = ""] # [doc = " let hashmap: HashMap<u64, u32> = HashMap::default();"] # [doc = ""] # [doc = " assert!(hashmap.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashmap.insert_sync(2, 0).is_ok());"] # [doc = ""] # [doc = " let second_entry_future = hashmap.begin_sync().unwrap().next_async();"] # [doc = " ```"] # [inline] pub async fn next_async (self) -> Option < OccupiedEntry < 'h , K , V , H > > { let hashmap = self . hashmap ; let mut entry_ptr = self . entry_ptr . clone () ; let mut async_guard = pin ! (AsyncGuard :: default ()) ; if let Some (locked_bucket) = self . locked_bucket . next_async (hashmap , & mut entry_ptr , & mut async_guard) . await { return Some (OccupiedEntry { hashmap , locked_bucket , entry_ptr , }) ; } None } # [doc = " Gets the next closest occupied entry."] # [doc = ""] # [doc = " [`HashMap::begin_sync`] and this method together allow the [`OccupiedEntry`] to effectively"] # [doc = " act as a mutable iterator over entries. This method never acquires more than one lock, even"] # [doc = " when it searches other buckets for the next closest occupied entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashMap;"] # [doc = " use scc::hash_map::Entry;"] # [doc = ""] # [doc = " let hashmap: HashMap<u64, u32> = HashMap::default();"] # [doc = ""] # [doc = " assert!(hashmap.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashmap.insert_sync(2, 0).is_ok());"] # [doc = ""] # [doc = " let first_entry = hashmap.begin_sync().unwrap();"] # [doc = " let first_key = *first_entry.key();"] # [doc = " let second_entry = first_entry.next_sync().unwrap();"] # [doc = " let second_key = *second_entry.key();"] # [doc = ""] # [doc = " assert!(second_entry.next_sync().is_none());"] # [doc = " assert_eq!(first_key + second_key, 3);"] # [doc = " ```"] # [inline] # [must_use] pub fn next_sync (self) -> Option < Self > { let hashmap = self . hashmap ; let mut entry_ptr = self . entry_ptr . clone () ; let guard = Guard :: new () ; let prolonged_guard = hashmap . prolonged_guard_ref (& guard) ; if let Some (locked_bucket) = self . locked_bucket . next_sync (hashmap , & mut entry_ptr , prolonged_guard) { return Some (OccupiedEntry { hashmap , locked_bucket , entry_ptr , }) ; } None } }
};
}
