// Generated macro for impl_93 (impl)
macro_rules! Depcrate_hash_indeximpl_93 {
() => {
// Module: crate::hash_index
// Provides: {"impl_93"}
// Dependencies: {}
impl < 'h , K , V , H > OccupiedEntry < 'h , K , V , H > where K : Eq + Hash , H : BuildHasher , { # [doc = " Gets a reference to the key in the entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert_eq!(hashindex.entry_sync(29).or_default().key(), &29);"] # [doc = " ```"] # [inline] # [must_use] pub fn key (& self) -> & K { & self . locked_bucket . entry (& self . entry_ptr) . 0 } # [doc = " Marks that the entry is removed from the [`HashIndex`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = " use scc::hash_index::Entry;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " hashindex.entry_sync(11).or_insert(17);"] # [doc = ""] # [doc = " if let Entry::Occupied(o) = hashindex.entry_sync(11) {"] # [doc = "     o.remove_entry();"] # [doc = " };"] # [doc = " assert_eq!(hashindex.peek_with(&11, |_, v| *v), None);"] # [doc = " ```"] # [inline] pub fn remove_entry (mut self) { let guard = Guard :: new () ; let prolonged_guard = self . hashindex . prolonged_guard_ref (& guard) ; self . locked_bucket . mark_removed (self . hashindex , & mut self . entry_ptr , prolonged_guard) ; } # [doc = " Gets a reference to the value in the entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = " use scc::hash_index::Entry;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " hashindex.entry_sync(19).or_insert(11);"] # [doc = ""] # [doc = " if let Entry::Occupied(o) = hashindex.entry_sync(19) {"] # [doc = "     assert_eq!(o.get(), &11);"] # [doc = " };"] # [doc = " ```"] # [inline] # [must_use] pub fn get (& self) -> & V { & self . locked_bucket . entry (& self . entry_ptr) . 1 } # [doc = " Gets a mutable reference to the value in the entry."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller has to make sure that there are no readers of the entry, e.g., a reader keeping"] # [doc = " a reference to the entry via [`HashIndex::iter`], [`HashIndex::peek`], or"] # [doc = " [`HashIndex::peek_with`], unless an instance of `V` can be safely read when there is a"] # [doc = " single writer, e.g., `V = [u8; 32]`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = " use scc::hash_index::Entry;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " hashindex.entry_sync(37).or_insert(11);"] # [doc = ""] # [doc = " if let Entry::Occupied(mut o) = hashindex.entry_sync(37) {"] # [doc = "     // Safety: `u32` can be safely read while being modified."] # [doc = "     unsafe { *o.get_mut() += 18; }"] # [doc = "     assert_eq!(*o.get(), 29);"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(hashindex.peek_with(&37, |_, v| *v), Some(29));"] # [doc = " ```"] # [inline] pub unsafe fn get_mut (& mut self) -> & mut V { & mut self . locked_bucket . entry_mut (& mut self . entry_ptr) . 1 } # [doc = " Gets the next closest occupied entry after removing the entry."] # [doc = ""] # [doc = " [`HashIndex::begin_async`] and this method together enable the [`OccupiedEntry`] to"] # [doc = " effectively act as a mutable iterator over entries. The method never acquires more than one"] # [doc = " lock even when it searches other buckets for the next closest occupied entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = " use scc::hash_index::Entry;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashindex.insert_sync(2, 0).is_ok());"] # [doc = ""] # [doc = " let second_entry_future = hashindex.begin_sync().unwrap().remove_and_async();"] # [doc = " ```"] # [inline] pub async fn remove_and_async (self) -> Option < OccupiedEntry < 'h , K , V , H > > { let hashindex = self . hashindex ; let mut entry_ptr = self . entry_ptr . clone () ; let mut async_guard = pin ! (AsyncGuard :: default ()) ; self . locked_bucket . writer . mark_removed (& mut entry_ptr , hashindex . prolonged_guard_ref (async_guard . guard ()) ,) ; if let Some (locked_bucket) = self . locked_bucket . next_async (hashindex , & mut entry_ptr , & mut async_guard) . await { return Some (OccupiedEntry { hashindex , locked_bucket , entry_ptr , }) ; } None } # [doc = " Gets the next closest occupied entry after removing the entry."] # [doc = ""] # [doc = " [`HashIndex::begin_sync`] and this method together enable the [`OccupiedEntry`] to"] # [doc = " effectively act as a mutable iterator over entries. The method never acquires more than one"] # [doc = " lock even when it searches other buckets for the next closest occupied entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = " use scc::hash_index::Entry;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashindex.insert_sync(2, 0).is_ok());"] # [doc = ""] # [doc = " let first_entry = hashindex.begin_sync().unwrap();"] # [doc = " let first_key = *first_entry.key();"] # [doc = " let second_entry = first_entry.remove_and_sync().unwrap();"] # [doc = " assert_eq!(hashindex.len(),  1);"] # [doc = ""] # [doc = " let second_key = *second_entry.key();"] # [doc = ""] # [doc = " assert!(second_entry.remove_and_sync().is_none());"] # [doc = " assert_eq!(first_key + second_key, 3);"] # [doc = " assert_eq!(hashindex.len(),  0);"] # [doc = " ```"] # [inline] # [must_use] pub fn remove_and_sync (self) -> Option < Self > { let hashindex = self . hashindex ; let mut entry_ptr = self . entry_ptr . clone () ; let guard = Guard :: new () ; let prolonged_guard = hashindex . prolonged_guard_ref (& guard) ; self . locked_bucket . writer . mark_removed (& mut entry_ptr , prolonged_guard) ; if let Some (locked_bucket) = self . locked_bucket . next_sync (hashindex , & mut entry_ptr , prolonged_guard) { return Some (OccupiedEntry { hashindex , locked_bucket , entry_ptr , }) ; } None } # [doc = " Gets the next closest occupied entry."] # [doc = ""] # [doc = " [`HashIndex::begin_async`] and this method together enable the [`OccupiedEntry`] to"] # [doc = " effectively act as a mutable iterator over entries. The method never acquires more than one"] # [doc = " lock even when it searches other buckets for the next closest occupied entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = " use scc::hash_index::Entry;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashindex.insert_sync(2, 0).is_ok());"] # [doc = ""] # [doc = " let second_entry_future = hashindex.begin_sync().unwrap().next_async();"] # [doc = " ```"] # [inline] pub async fn next_async (self) -> Option < OccupiedEntry < 'h , K , V , H > > { let hashindex = self . hashindex ; let mut entry_ptr = self . entry_ptr . clone () ; let mut async_guard = pin ! (AsyncGuard :: default ()) ; if let Some (locked_bucket) = self . locked_bucket . next_async (hashindex , & mut entry_ptr , & mut async_guard) . await { return Some (OccupiedEntry { hashindex , locked_bucket , entry_ptr , }) ; } None } # [doc = " Gets the next closest occupied entry."] # [doc = ""] # [doc = " [`HashIndex::begin_sync`] and this method together enable the [`OccupiedEntry`] to"] # [doc = " effectively act as a mutable iterator over entries. The method never acquires more than one"] # [doc = " lock even when it searches other buckets for the next closest occupied entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = " use scc::hash_index::Entry;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashindex.insert_sync(2, 0).is_ok());"] # [doc = ""] # [doc = " let first_entry = hashindex.begin_sync().unwrap();"] # [doc = " let first_key = *first_entry.key();"] # [doc = " let second_entry = first_entry.next_sync().unwrap();"] # [doc = " let second_key = *second_entry.key();"] # [doc = ""] # [doc = " assert!(second_entry.next_sync().is_none());"] # [doc = " assert_eq!(first_key + second_key, 3);"] # [doc = " ```"] # [inline] # [must_use] pub fn next_sync (self) -> Option < Self > { let hashindex = self . hashindex ; let mut entry_ptr = self . entry_ptr . clone () ; let guard = Guard :: new () ; let prolonged_guard = hashindex . prolonged_guard_ref (& guard) ; if let Some (locked_bucket) = self . locked_bucket . next_sync (hashindex , & mut entry_ptr , prolonged_guard) { return Some (OccupiedEntry { hashindex , locked_bucket , entry_ptr , }) ; } None } }
};
}
