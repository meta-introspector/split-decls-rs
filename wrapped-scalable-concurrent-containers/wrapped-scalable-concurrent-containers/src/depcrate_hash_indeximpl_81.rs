// Generated macro for impl_81 (impl)
macro_rules! Depcrate_hash_indeximpl_81 {
() => {
// Module: crate::hash_index
// Provides: {"impl_81"}
// Dependencies: {}
impl < K , V , H > HashIndex < K , V , H > where K : Eq + Hash , H : BuildHasher , { # [doc = " Temporarily increases the minimum capacity of the [`HashIndex`]."] # [doc = ""] # [doc = " A [`Reserve`] is returned if the [`HashIndex`] could increase the minimum capacity while"] # [doc = " the increased capacity is not exclusively owned by the returned [`Reserve`], allowing"] # [doc = " others to benefit from it. The memory for the additional space may not be immediately"] # [doc = " allocated if the [`HashIndex`] is empty or currently being resized, however once the memory"] # [doc = " is reserved eventually, the capacity will not shrink below the additional capacity until"] # [doc = " the returned [`Reserve`] is dropped."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns `None` if a too large number is given."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<usize, usize> = HashIndex::with_capacity(1000);"] # [doc = " assert_eq!(hashindex.capacity(), 1024);"] # [doc = ""] # [doc = " let reserved = hashindex.reserve(10000);"] # [doc = " assert!(reserved.is_some());"] # [doc = " assert_eq!(hashindex.capacity(), 16384);"] # [doc = ""] # [doc = " assert!(hashindex.reserve(usize::MAX).is_none());"] # [doc = " assert_eq!(hashindex.capacity(), 16384);"] # [doc = ""] # [doc = " for i in 0..16 {"] # [doc = "     assert!(hashindex.insert_sync(i, i).is_ok());"] # [doc = " }"] # [doc = " drop(reserved);"] # [doc = ""] # [doc = " assert_eq!(hashindex.capacity(), 1024);"] # [doc = " ```"] # [inline] pub fn reserve (& self , additional_capacity : usize) -> Option < Reserve < '_ , K , V , H > > { let additional = self . reserve_capacity (additional_capacity) ; if additional == 0 { None } else { Some (Reserve { hashindex : self , additional , }) } } # [doc = " Gets the entry associated with the given key in the map for in-place manipulation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<char, u32> = HashIndex::default();"] # [doc = ""] # [doc = " let future_entry = hashindex.entry_async('b');"] # [doc = " ```"] # [inline] pub async fn entry_async (& self , key : K) -> Entry < '_ , K , V , H > { let hash = self . hash (& key) ; let async_guard = pin ! (AsyncGuard :: default ()) ; self . reclaim_memory (async_guard . guard ()) ; let locked_bucket = self . writer_async (hash , & async_guard) . await ; let prolonged_guard = self . prolonged_guard_ref (async_guard . guard ()) ; let entry_ptr = locked_bucket . search (& key , hash , prolonged_guard) ; if entry_ptr . is_valid () { Entry :: Occupied (OccupiedEntry { hashindex : self , locked_bucket , entry_ptr , }) } else { let vacant_entry = VacantEntry { hashindex : self , key , hash , locked_bucket , } ; Entry :: Vacant (vacant_entry) } } # [doc = " Gets the entry associated with the given key in the map for in-place manipulation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<char, u32> = HashIndex::default();"] # [doc = ""] # [doc = " for ch in \"a short treatise on fungi\".chars() {"] # [doc = "     unsafe {"] # [doc = "         hashindex.entry_sync(ch).and_modify(|counter| *counter += 1).or_insert(1);"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(hashindex.peek_with(&'s', |_, v| *v), Some(2));"] # [doc = " assert_eq!(hashindex.peek_with(&'t', |_, v| *v), Some(3));"] # [doc = " assert!(hashindex.peek_with(&'y', |_, v| *v).is_none());"] # [doc = " ```"] # [inline] pub fn entry_sync (& self , key : K) -> Entry < '_ , K , V , H > { let hash = self . hash (& key) ; let guard = Guard :: new () ; let prolonged_guard = self . prolonged_guard_ref (& guard) ; let locked_bucket = self . writer_sync (hash , prolonged_guard) ; let entry_ptr = locked_bucket . search (& key , hash , prolonged_guard) ; if entry_ptr . is_valid () { Entry :: Occupied (OccupiedEntry { hashindex : self , locked_bucket , entry_ptr , }) } else { let vacant_entry = VacantEntry { hashindex : self , key , hash , locked_bucket , } ; Entry :: Vacant (vacant_entry) } } # [doc = " Tries to get the entry associated with the given key in the map for in-place manipulation."] # [doc = ""] # [doc = " Returns `None` if the entry could not be locked."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<usize, usize> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(0, 1).is_ok());"] # [doc = " assert!(hashindex.try_entry(0).is_some());"] # [doc = " ```"] # [inline] pub fn try_entry (& self , key : K) -> Option < Entry < '_ , K , V , H > > { let hash = self . hash (& key) ; let guard = Guard :: new () ; self . reclaim_memory (& guard) ; let prolonged_guard = self . prolonged_guard_ref (& guard) ; let locked_bucket = self . try_reserve_bucket (hash , prolonged_guard) ? ; let entry_ptr = locked_bucket . search (& key , hash , prolonged_guard) ; if entry_ptr . is_valid () { Some (Entry :: Occupied (OccupiedEntry { hashindex : self , locked_bucket , entry_ptr , })) } else { Some (Entry :: Vacant (VacantEntry { hashindex : self , key , hash , locked_bucket , })) } } # [doc = " Begins iterating over entries by getting the first occupied entry."] # [doc = ""] # [doc = " The returned [`OccupiedEntry`] in combination with [`OccupiedEntry::next_async`] can act as"] # [doc = " a mutable iterator over entries."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<char, u32> = HashIndex::default();"] # [doc = ""] # [doc = " let future_entry = hashindex.begin_async();"] # [doc = " ```"] # [inline] pub async fn begin_async (& self) -> Option < OccupiedEntry < '_ , K , V , H > > { self . any_async (| _ , _ | true) . await } # [doc = " Begins iterating over entries by getting the first occupied entry."] # [doc = ""] # [doc = " The returned [`OccupiedEntry`] in combination with [`OccupiedEntry::next_sync`] can act as a"] # [doc = " mutable iterator over entries."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = ""] # [doc = " let mut first_entry = hashindex.begin_sync().unwrap();"] # [doc = " unsafe {"] # [doc = "     *first_entry.get_mut() = 2;"] # [doc = " }"] # [doc = ""] # [doc = " assert!(first_entry.next_sync().is_none());"] # [doc = " assert_eq!(hashindex.peek_with(&1, |_, v| *v), Some(2));"] # [doc = " ```"] # [inline] pub fn begin_sync (& self) -> Option < OccupiedEntry < '_ , K , V , H > > { self . any_sync (| _ , _ | true) } # [doc = " Finds any entry satisfying the supplied predicate for in-place manipulation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " let future_entry = hashindex.any_async(|k, _| *k == 2);"] # [doc = " ```"] # [inline] pub async fn any_async < P : FnMut (& K , & V) -> bool > (& self , mut pred : P ,) -> Option < OccupiedEntry < '_ , K , V , H > > { let async_guard = pin ! (AsyncGuard :: default ()) ; self . reclaim_memory (async_guard . guard ()) ; let mut entry = None ; self . for_each_writer_async (0 , 0 , & async_guard , | locked_bucket , _ | { let guard = self . prolonged_guard_ref (async_guard . guard ()) ; let mut entry_ptr = EntryPtr :: new (guard) ; while entry_ptr . move_to_next (& locked_bucket . writer , guard) { let (k , v) = locked_bucket . entry (& entry_ptr) ; if pred (k , v) { entry = Some (OccupiedEntry { hashindex : self , locked_bucket , entry_ptr , }) ; return true ; } } false }) . await ; entry } # [doc = " Finds any entry satisfying the supplied predicate for in-place manipulation."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashindex.insert_sync(2, 3).is_ok());"] # [doc = ""] # [doc = " let mut entry = hashindex.any_sync(|k, _| *k == 2).unwrap();"] # [doc = " assert_eq!(*entry.get(), 3);"] # [doc = " ```"] # [inline] pub fn any_sync < P : FnMut (& K , & V) -> bool > (& self , mut pred : P ,) -> Option < OccupiedEntry < '_ , K , V , H > > { let mut entry = None ; let guard = Guard :: new () ; self . reclaim_memory (& guard) ; let prolonged_guard = self . prolonged_guard_ref (& guard) ; self . for_each_writer_sync (0 , 0 , prolonged_guard , | locked_bucket , _ | { let mut entry_ptr = EntryPtr :: new (prolonged_guard) ; while entry_ptr . move_to_next (& locked_bucket . writer , prolonged_guard) { let (k , v) = locked_bucket . entry (& entry_ptr) ; if pred (k , v) { entry = Some (OccupiedEntry { hashindex : self , locked_bucket , entry_ptr , }) ; return true ; } } false }) ; entry } # [doc = " Inserts a key-value pair into the [`HashIndex`]."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " If the key exists, the value is *not* updated."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns an error containing the supplied key-value pair if the key exists."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = " let future_insert = hashindex.insert_async(11, 17);"] # [doc = " ```"] # [inline] pub async fn insert_async (& self , key : K , val : V) -> Result < () , (K , V) > { let hash = self . hash (& key) ; let async_guard = pin ! (AsyncGuard :: default ()) ; self . reclaim_memory (async_guard . guard ()) ; let locked_bucket = self . writer_async (hash , & async_guard) . await ; let guard = async_guard . guard () ; let partial_hash = hash ; if locked_bucket . search (& key , hash , guard) . is_valid () { Err ((key , val)) } else { locked_bucket . insert (partial_hash , (key , val) , guard) ; Ok (()) } } # [doc = " Inserts a key-value pair into the [`HashIndex`]."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " If the key exists, the value is *not* updated."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns an error along with the supplied key-value pair if the key exists."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert_eq!(hashindex.insert_sync(1, 1).unwrap_err(), (1, 1));"] # [doc = " ```"] # [inline] pub fn insert_sync (& self , key : K , val : V) -> Result < () , (K , V) > { let hash = self . hash (& key) ; let guard = Guard :: new () ; self . reclaim_memory (& guard) ; let locked_bucket = self . writer_sync (hash , & guard) ; if locked_bucket . search (& key , hash , & guard) . is_valid () { Err ((key , val)) } else { locked_bucket . insert (hash , (key , val) , & guard) ; Ok (()) } } # [doc = " Removes a key-value pair if the key exists."] # [doc = ""] # [doc = " Returns `false` if the key does not exist."] # [doc = ""] # [doc = " Returns `true` if the key existed and the condition was met after marking the entry"] # [doc = " unreachable; the memory will be reclaimed later."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = " let future_insert = hashindex.insert_async(11, 17);"] # [doc = " let future_remove = hashindex.remove_async(&11);"] # [doc = " ```"] # [inline] pub async fn remove_async < Q > (& self , key : & Q) -> bool where Q : Equivalent < K > + Hash + ? Sized , { self . remove_if_async (key , | _ | true) . await } # [doc = " Removes a key-value pair if the key exists."] # [doc = ""] # [doc = " Returns `false` if the key does not exist."] # [doc = ""] # [doc = " Returns `true` if the key existed and the condition was met after marking the entry"] # [doc = " unreachable; the memory will be reclaimed later."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(!hashindex.remove_sync(&1));"] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashindex.remove_sync(&1));"] # [doc = " ```"] # [inline] pub fn remove_sync < Q > (& self , key : & Q) -> bool where Q : Equivalent < K > + Hash + ? Sized , { self . remove_if_sync (key , | _ | true) } # [doc = " Removes a key-value pair if the key exists and the given condition is met."] # [doc = ""] # [doc = " Returns `false` if the key does not exist or the condition was not met."] # [doc = ""] # [doc = " Returns `true` if the key existed and the condition was met after marking the entry"] # [doc = " unreachable; the memory will be reclaimed later."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = " let future_insert = hashindex.insert_async(11, 17);"] # [doc = " let future_remove = hashindex.remove_if_async(&11, |_| true);"] # [doc = " ```"] # [inline] pub async fn remove_if_async < Q , F : FnOnce (& V) -> bool > (& self , key : & Q , condition : F) -> bool where Q : Equivalent < K > + Hash + ? Sized , { let hash = self . hash (key) ; let async_guard = pin ! (AsyncGuard :: default ()) ; self . reclaim_memory (async_guard . guard ()) ; let Some (mut locked_bucket) = self . optional_writer_async (hash , & async_guard) . await else { return false ; } ; let mut entry_ptr = locked_bucket . search (key , hash , async_guard . guard ()) ; if entry_ptr . is_valid () && condition (& mut locked_bucket . entry_mut (& mut entry_ptr) . 1) { locked_bucket . mark_removed (self , & mut entry_ptr , async_guard . guard ()) ; true } else { false } } # [doc = " Removes a key-value pair if the key exists and the given condition is met."] # [doc = ""] # [doc = " Returns `false` if the key does not exist or the condition was not met."] # [doc = ""] # [doc = " Returns `true` if the key existed and the condition was met after marking the entry"] # [doc = " unreachable; the memory will be reclaimed later."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert!(!hashindex.remove_if_sync(&1, |v| *v == 1));"] # [doc = " assert!(hashindex.remove_if_sync(&1, |v| *v == 0));"] # [doc = " ```"] # [inline] pub fn remove_if_sync < Q , F : FnOnce (& V) -> bool > (& self , key : & Q , condition : F) -> bool where Q : Equivalent < K > + Hash + ? Sized , { let hash = self . hash (key) ; let guard = Guard :: default () ; self . reclaim_memory (& guard) ; let Some (mut locked_bucket) = self . optional_writer_sync (hash , & guard) else { return false ; } ; let mut entry_ptr = locked_bucket . search (key , hash , & guard) ; if entry_ptr . is_valid () && condition (& mut locked_bucket . entry_mut (& mut entry_ptr) . 1) { locked_bucket . mark_removed (self , & mut entry_ptr , & guard) ; true } else { false } } # [doc = " Gets an [`OccupiedEntry`] corresponding to the key for in-place modification."] # [doc = ""] # [doc = " [`OccupiedEntry`] exclusively owns the entry, preventing others from gaining access to it:"] # [doc = " use [`peek`](Self::peek) if read-only access is sufficient."] # [doc = ""] # [doc = " Returns `None` if the key does not exist."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = " let future_insert = hashindex.insert_async(11, 17);"] # [doc = " let future_get = hashindex.get_async(&11);"] # [doc = " ```"] # [inline] pub async fn get_async < Q > (& self , key : & Q) -> Option < OccupiedEntry < '_ , K , V , H > > where Q : Equivalent < K > + Hash + ? Sized , { let hash = self . hash (key) ; let async_guard = pin ! (AsyncGuard :: default ()) ; self . reclaim_memory (async_guard . guard ()) ; let locked_bucket = self . optional_writer_async (hash , & async_guard) . await ? ; let guard = self . prolonged_guard_ref (async_guard . guard ()) ; let entry_ptr = locked_bucket . search (key , hash , guard) ; if entry_ptr . is_valid () { return Some (OccupiedEntry { hashindex : self , locked_bucket , entry_ptr , }) ; } None } # [doc = " Gets an [`OccupiedEntry`] corresponding to the key for in-place modification."] # [doc = ""] # [doc = " [`OccupiedEntry`] exclusively owns the entry, preventing others from gaining access to it:"] # [doc = " use [`peek`](Self::peek) if read-only access is sufficient."] # [doc = ""] # [doc = " Returns `None` if the key does not exist."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.get_sync(&1).is_none());"] # [doc = " assert!(hashindex.insert_sync(1, 10).is_ok());"] # [doc = " assert_eq!(*hashindex.get_sync(&1).unwrap().get(), 10);"] # [doc = " assert_eq!(*hashindex.get_sync(&1).unwrap(), 10);"] # [doc = " ```"] # [inline] pub fn get_sync < Q > (& self , key : & Q) -> Option < OccupiedEntry < '_ , K , V , H > > where Q : Equivalent < K > + Hash + ? Sized , { let hash = self . hash (key) ; let guard = Guard :: new () ; self . reclaim_memory (& guard) ; let prolonged_guard = self . prolonged_guard_ref (& guard) ; let locked_bucket = self . optional_writer_sync (hash , prolonged_guard) ? ; let entry_ptr = locked_bucket . search (key , hash , prolonged_guard) ; if entry_ptr . is_valid () { return Some (OccupiedEntry { hashindex : self , locked_bucket , entry_ptr , }) ; } None } # [doc = " Returns a guarded reference to the value for the specified key without acquiring locks."] # [doc = ""] # [doc = " Returns `None` if the key does not exist."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " The closure may see an old snapshot of the value if the entry has recently been relocated"] # [doc = " due to resizing. This means that the effects of interior mutability, e.g., `Mutex<T>` or"] # [doc = " `UnsafeCell<T>`, may not be observable in the closure."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method is safe to use if the value is not modified or if `V` is a pointer type such as"] # [doc = " `Box<T>` or `Arc<T>`. However, it is unsafe if `V` contains a pointer that can be modified"] # [doc = " through interior mutability."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::{Guard, HashIndex};"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 10).is_ok());"] # [doc = ""] # [doc = " let guard = Guard::new();"] # [doc = " let value_ref = hashindex.peek(&1, &guard).unwrap();"] # [doc = " assert_eq!(*value_ref, 10);"] # [doc = " ```"] # [inline] pub fn peek < 'h , Q > (& 'h self , key : & Q , guard : & 'h Guard) -> Option < & 'h V > where Q : Equivalent < K > + Hash + ? Sized , { self . reclaim_memory (guard) ; self . peek_entry (key , self . hash (key) , guard) . map (| (_ , v) | v) } # [doc = " Peeks a key-value pair without acquiring locks."] # [doc = ""] # [doc = " Returns `None` if the key does not exist."] # [doc = ""] # [doc = " # Note"] # [doc = ""] # [doc = " The closure may see an old snapshot of the value if the entry has recently been relocated"] # [doc = " due to resizing. This means that the effects of interior mutability, e.g., `Mutex<T>` or"] # [doc = " `UnsafeCell<T>`, may not be observable in the closure."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This method is safe to use if the value is not modified or if `V` is a pointer type such as"] # [doc = " `Box<T>` or `Arc<T>`. However, it is unsafe if `V` contains a pointer that can be modified"] # [doc = " through interior mutability."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.peek_with(&1, |_, v| *v).is_none());"] # [doc = " assert!(hashindex.insert_sync(1, 10).is_ok());"] # [doc = " assert_eq!(hashindex.peek_with(&1, |_, v| *v).unwrap(), 10);"] # [doc = " ```"] # [inline] pub fn peek_with < Q , R , F : FnOnce (& K , & V) -> R > (& self , key : & Q , reader : F) -> Option < R > where Q : Equivalent < K > + Hash + ? Sized , { let guard = Guard :: new () ; self . reclaim_memory (& guard) ; self . peek_entry (key , self . hash (key) , & guard) . map (| (k , v) | reader (k , v)) } # [doc = " Returns `true` if the [`HashIndex`] contains a value for the specified key."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(!hashindex.contains(&1));"] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashindex.contains(&1));"] # [doc = " ```"] # [inline] pub fn contains < Q > (& self , key : & Q) -> bool where Q : Equivalent < K > + Hash + ? Sized , { self . peek_with (key , | _ , _ | ()) . is_some () } # [doc = " Iterates over entries asynchronously for reading."] # [doc = ""] # [doc = " Stops iterating when the closure returns `false`, and this method also returns `false`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u64> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = ""] # [doc = " async {"] # [doc = "     let result = hashindex.iter_async(|k, v| {"] # [doc = "         false"] # [doc = "     }).await;"] # [doc = "     assert!(!result);"] # [doc = " };"] # [doc = " ```"] # [inline] pub async fn iter_async < F : FnMut (& K , & V) -> bool > (& self , mut f : F) -> bool { let async_guard = pin ! (AsyncGuard :: default ()) ; self . reclaim_memory (async_guard . guard ()) ; let mut result = true ; self . for_each_reader_async (& async_guard , | reader , data_block | { let guard = async_guard . guard () ; let mut entry_ptr = EntryPtr :: new (guard) ; while entry_ptr . move_to_next (& reader , guard) { let (k , v) = entry_ptr . get (data_block) ; if ! f (k , v) { result = false ; return false ; } } true }) . await ; result } # [doc = " Iterates over entries synchronously for reading."] # [doc = ""] # [doc = " Stops iterating when the closure returns `false`, and this method also returns `false`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u64> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashindex.insert_sync(2, 1).is_ok());"] # [doc = ""] # [doc = " let mut acc = 0_u64;"] # [doc = " let result = hashindex.iter_sync(|k, v| {"] # [doc = "     acc += *k;"] # [doc = "     acc += *v;"] # [doc = "     true"] # [doc = " });"] # [doc = ""] # [doc = " assert!(result);"] # [doc = " assert_eq!(acc, 4);"] # [doc = " ```"] # [inline] pub fn iter_sync < F : FnMut (& K , & V) -> bool > (& self , mut f : F) -> bool { let mut result = true ; let guard = Guard :: new () ; self . reclaim_memory (& guard) ; self . for_each_reader_sync (& guard , | reader , data_block | { let mut entry_ptr = EntryPtr :: new (& guard) ; while entry_ptr . move_to_next (& reader , & guard) { let (k , v) = entry_ptr . get (data_block) ; if ! f (k , v) { result = false ; return false ; } } true }) ; result } # [doc = " Retains the entries specified by the predicate."] # [doc = ""] # [doc = " Entries that have existed since the invocation of the method are guaranteed to be visited"] # [doc = " if they are not removed, however the same entry can be visited more than once if the"] # [doc = " [`HashIndex`] gets resized by another thread."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " let future_insert = hashindex.insert_async(1, 0);"] # [doc = " let future_retain = hashindex.retain_async(|k, v| *k == 1);"] # [doc = " ```"] # [inline] pub async fn retain_async < F : FnMut (& K , & V) -> bool > (& self , mut pred : F) { let async_guard = pin ! (AsyncGuard :: default ()) ; self . reclaim_memory (async_guard . guard ()) ; self . for_each_writer_async (0 , 0 , & async_guard , | mut locked_bucket , removed | { let guard = async_guard . guard () ; let mut entry_ptr = EntryPtr :: new (guard) ; while entry_ptr . move_to_next (& locked_bucket . writer , guard) { let (k , v) = locked_bucket . entry_mut (& mut entry_ptr) ; if ! pred (k , v) { locked_bucket . writer . mark_removed (& mut entry_ptr , guard) ; * removed = true ; } } false }) . await ; } # [doc = " Retains the entries specified by the predicate."] # [doc = ""] # [doc = " Entries that have existed since the invocation of the method are guaranteed to be visited"] # [doc = " if they are not removed, however the same entry can be visited more than once if the"] # [doc = " [`HashIndex`] gets resized by another thread."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert!(hashindex.insert_sync(2, 1).is_ok());"] # [doc = " assert!(hashindex.insert_sync(3, 2).is_ok());"] # [doc = ""] # [doc = " hashindex.retain_sync(|k, v| *k == 1 && *v == 0);"] # [doc = ""] # [doc = " assert!(hashindex.contains(&1));"] # [doc = " assert!(!hashindex.contains(&2));"] # [doc = " assert!(!hashindex.contains(&3));"] # [doc = " ```"] # [inline] pub fn retain_sync < F : FnMut (& K , & V) -> bool > (& self , mut pred : F) { let guard = Guard :: new () ; self . reclaim_memory (& guard) ; self . for_each_writer_sync (0 , 0 , & guard , | mut locked_bucket , removed | { let mut entry_ptr = EntryPtr :: new (& guard) ; while entry_ptr . move_to_next (& locked_bucket . writer , & guard) { let (k , v) = locked_bucket . entry_mut (& mut entry_ptr) ; if ! pred (k , v) { locked_bucket . writer . mark_removed (& mut entry_ptr , & guard) ; * removed = true ; } } false }) ; } # [doc = " Clears the [`HashIndex`] by removing all key-value pairs."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " let future_insert = hashindex.insert_async(1, 0);"] # [doc = " let future_retain = hashindex.clear_async();"] # [doc = " ```"] pub async fn clear_async (& self) { self . retain_async (| _ , _ | false) . await ; } # [doc = " Clears the [`HashIndex`] by removing all key-value pairs."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " hashindex.clear_sync();"] # [doc = ""] # [doc = " assert!(!hashindex.contains(&1));"] # [doc = " ```"] pub fn clear_sync (& self) { self . retain_sync (| _ , _ | false) ; } # [doc = " Returns the number of entries in the [`HashIndex`]."] # [doc = ""] # [doc = " It reads the entire metadata area of the bucket array to calculate the number of valid"] # [doc = " entries, making its time complexity `O(N)`. Furthermore, it may overcount entries if an old"] # [doc = " bucket array has yet to be dropped."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert_eq!(hashindex.len(), 1);"] # [doc = " ```"] # [inline] pub fn len (& self) -> usize { self . num_entries (& Guard :: new ()) } # [doc = " Returns `true` if the [`HashIndex`] is empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.is_empty());"] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = " assert!(!hashindex.is_empty());"] # [doc = " ```"] # [inline] pub fn is_empty (& self) -> bool { ! self . has_entry (& Guard :: new ()) } # [doc = " Returns the capacity of the [`HashIndex`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex_default: HashIndex<u64, u32> = HashIndex::default();"] # [doc = " assert_eq!(hashindex_default.capacity(), 0);"] # [doc = ""] # [doc = " assert!(hashindex_default.insert_sync(1, 0).is_ok());"] # [doc = " assert_eq!(hashindex_default.capacity(), 64);"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::with_capacity(1000);"] # [doc = " assert_eq!(hashindex.capacity(), 1024);"] # [doc = " ```"] # [inline] pub fn capacity (& self) -> usize { self . num_slots (& Guard :: new ()) } # [doc = " Returns the current capacity range of the [`HashIndex`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert_eq!(hashindex.capacity_range(), 0..=(1_usize << (usize::BITS - 1)));"] # [doc = ""] # [doc = " let reserved = hashindex.reserve(1000);"] # [doc = " assert_eq!(hashindex.capacity_range(), 1000..=(1_usize << (usize::BITS - 1)));"] # [doc = " ```"] # [inline] pub fn capacity_range (& self) -> RangeInclusive < usize > { self . minimum_capacity . load (Relaxed) ..= self . maximum_capacity () } # [doc = " Returns the index of the bucket that may contain the key."] # [doc = ""] # [doc = " The method returns the index of the bucket associated with the key. The number of buckets"] # [doc = " can be calculated by dividing the capacity by `32`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::HashIndex;"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::with_capacity(1024);"] # [doc = ""] # [doc = " let bucket_index = hashindex.bucket_index(&11);"] # [doc = " assert!(bucket_index < hashindex.capacity() / 32);"] # [doc = " ```"] # [inline] pub fn bucket_index < Q > (& self , key : & Q) -> usize where Q : Equivalent < K > + Hash + ? Sized , { self . calculate_bucket_index (key) } # [doc = " Returns an [`Iter`]."] # [doc = ""] # [doc = " It is guaranteed to go through all the key-value pairs pertaining in the [`HashIndex`]"] # [doc = " at the moment, however the same key-value pair can be visited more than once if the"] # [doc = " [`HashIndex`] is being resized."] # [doc = ""] # [doc = " It requires the user to supply a reference to a [`Guard`]."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use scc::{Guard, HashIndex};"] # [doc = ""] # [doc = " let hashindex: HashIndex<u64, u32> = HashIndex::default();"] # [doc = ""] # [doc = " assert!(hashindex.insert_sync(1, 0).is_ok());"] # [doc = ""] # [doc = " let guard = Guard::new();"] # [doc = ""] # [doc = " let mut iter = hashindex.iter(&guard);"] # [doc = " let entry_ref = iter.next().unwrap();"] # [doc = " assert_eq!(iter.next(), None);"] # [doc = ""] # [doc = " for iter in hashindex.iter(&guard) {"] # [doc = "     assert_eq!(iter, (&1, &0));"] # [doc = " }"] # [doc = " ```"] # [inline] pub fn iter < 'h > (& 'h self , guard : & 'h Guard) -> Iter < 'h , K , V , H > { Iter { hashindex : self , bucket_array : None , index : 0 , bucket : None , entry_ptr : EntryPtr :: new (guard) , guard , } } # [doc = " Reclaims memory by dropping all garbage bucket arrays if they are unreachable."] # [inline] fn reclaim_memory (& self , guard : & Guard) { let head_ptr = self . garbage_chain . load (Acquire , guard) ; if ! head_ptr . is_null () { self . dealloc_garbage (head_ptr , guard) ; } } # [doc = " Deallocates the supplied bucket array."] fn dealloc_garbage (& self , ptr : Ptr < BucketArray < K , V , () , INDEX > > , guard : & Guard) { let garbage_epoch = self . garbage_epoch . load (Acquire) ; if Epoch :: try_from (garbage_epoch) . is_ok_and (| e | ! e . in_same_generation (guard . epoch ())) { if let Ok ((mut garbage_head , _)) = self . garbage_chain . compare_exchange (ptr , (None , Tag :: None) , Acquire , Relaxed , guard) { while let Some (garbage_bucket_array) = garbage_head { garbage_head = garbage_bucket_array . bucket_link () . swap ((None , Tag :: None) , Acquire) . 0 ; let dropped = unsafe { garbage_bucket_array . drop_in_place () } ; debug_assert ! (dropped) ; } } } else { guard . accelerate () ; } } }
};
}
