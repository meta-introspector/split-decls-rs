// Generated macro for HashMap (struct)
macro_rules! Depcrate_hash_mapHashMap {
() => {
// Module: crate::hash_map
// Provides: {"HashMap"}
// Dependencies: {}
# [doc = " Scalable concurrent hash map."] # [doc = ""] # [doc = " [`HashMap`] is a concurrent hash map data structure optimized for highly concurrent workloads."] # [doc = " [`HashMap`] has a dynamically sized array of buckets where a bucket is a fixed-size hash table"] # [doc = " with linear probing that can be expanded by allocating a linked list of smaller buckets when it"] # [doc = " is full."] # [doc = ""] # [doc = " ## The key features of [`HashMap`]"] # [doc = ""] # [doc = " * Non-sharded: data is stored in a single array of entry buckets."] # [doc = " * Non-blocking resizing: resizing does not block other threads or tasks."] # [doc = " * Automatic resizing: grows or shrinks as needed."] # [doc = " * Incremental resizing: entries in the old bucket array are incrementally relocated."] # [doc = " * No busy waiting: no spin locks or hot loops to wait for desired resources."] # [doc = " * Linearizability: [`HashMap`] manipulation methods are linearizable."] # [doc = ""] # [doc = " ## The key statistics for [`HashMap`]"] # [doc = ""] # [doc = " * The expected size of metadata for a single entry: 2 bytes."] # [doc = " * The expected number of atomic write operations required for an operation on a single key: 2."] # [doc = " * The expected number of atomic variables accessed during a single key operation: 2."] # [doc = " * The number of entries managed by a single bucket without a linked list: 32."] # [doc = " * The expected maximum linked list length when a resize is triggered: log(capacity) / 8."] # [doc = ""] # [doc = " ## Locking behavior"] # [doc = ""] # [doc = " ### Bucket access"] # [doc = ""] # [doc = " Bucket arrays are protected by [`sdd`], thus allowing lock-free access to them."] # [doc = ""] # [doc = " ### Entry access"] # [doc = ""] # [doc = " Each read/write access to an entry is serialized by the read-write lock in the bucket containing"] # [doc = " the entry. There are no container-level locks, therefore, the larger the [`HashMap`] gets, the"] # [doc = " lower the chance that the bucket-level lock will be contended."] # [doc = ""] # [doc = " ### Resize"] # [doc = ""] # [doc = " Resizing of the [`HashMap`] is non-blocking and lock-free; resizing does not block any other"] # [doc = " read/write access to the [`HashMap`] or resizing attempts. Resizing is analogous to pushing a"] # [doc = " new bucket array into a lock-free stack. Each entry in the old bucket array will be"] # [doc = " incrementally relocated to the new bucket array upon future access to the [`HashMap`], and the old"] # [doc = " bucket array is dropped when it becomes empty and unreachable."] # [doc = ""] # [doc = " ### Synchronous methods in an asynchronous code block"] # [doc = ""] # [doc = " It is generally not recommended to use blocking methods, such as [`HashMap::insert_sync`], in an"] # [doc = " asynchronous code block or [`poll`](std::future::Future::poll), since it may lead to deadlocks"] # [doc = " or performance degradation."] # [doc = ""] # [doc = " ## Unwind safety"] # [doc = ""] # [doc = " [`HashMap`] is impervious to out-of-memory errors and panics in user-specified code under one"] # [doc = " condition: `H::Hasher::hash`, `K::drop` and `V::drop` must not panic."] pub struct HashMap < K , V , H = RandomState > where H : BuildHasher , { bucket_array : AtomicShared < BucketArray < K , V , () , MAP > > , minimum_capacity : AtomicUsize , build_hasher : H , }
};
}
