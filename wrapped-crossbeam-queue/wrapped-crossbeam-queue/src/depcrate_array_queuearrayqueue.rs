// Generated macro for ArrayQueue (struct)
macro_rules! Depcrate_array_queueArrayQueue {
() => {
// Module: crate::array_queue
// Provides: {"ArrayQueue"}
// Dependencies: {}
# [doc = " A bounded multi-producer multi-consumer queue."] # [doc = ""] # [doc = " This queue allocates a fixed-capacity buffer on construction, which is used to store pushed"] # [doc = " elements. The queue cannot hold more elements than the buffer allows. Attempting to push an"] # [doc = " element into a full queue will fail. Alternatively, [`force_push`] makes it possible for"] # [doc = " this queue to be used as a ring-buffer. Having a buffer allocated upfront makes this queue"] # [doc = " a bit faster than [`SegQueue`]."] # [doc = ""] # [doc = " [`force_push`]: ArrayQueue::force_push"] # [doc = " [`SegQueue`]: super::SegQueue"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_queue::ArrayQueue;"] # [doc = ""] # [doc = " let q = ArrayQueue::new(2);"] # [doc = ""] # [doc = " assert_eq!(q.push('a'), Ok(()));"] # [doc = " assert_eq!(q.push('b'), Ok(()));"] # [doc = " assert_eq!(q.push('c'), Err('c'));"] # [doc = " assert_eq!(q.pop(), Some('a'));"] # [doc = " ```"] pub struct ArrayQueue < T > { # [doc = " The head of the queue."] # [doc = ""] # [doc = " This value is a \"stamp\" consisting of an index into the buffer and a lap, but packed into a"] # [doc = " single `usize`. The lower bits represent the index, while the upper bits represent the lap."] # [doc = ""] # [doc = " Elements are popped from the head of the queue."] head : CachePadded < AtomicUsize > , # [doc = " The tail of the queue."] # [doc = ""] # [doc = " This value is a \"stamp\" consisting of an index into the buffer and a lap, but packed into a"] # [doc = " single `usize`. The lower bits represent the index, while the upper bits represent the lap."] # [doc = ""] # [doc = " Elements are pushed into the tail of the queue."] tail : CachePadded < AtomicUsize > , # [doc = " The buffer holding slots."] buffer : Box < [Slot < T >] > , # [doc = " A stamp with the value of `{ lap: 1, index: 0 }`."] one_lap : usize , }
};
}
