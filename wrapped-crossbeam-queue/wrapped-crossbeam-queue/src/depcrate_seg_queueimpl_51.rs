// Generated macro for impl_51 (impl)
macro_rules! Depcrate_seg_queueimpl_51 {
() => {
// Module: crate::seg_queue
// Provides: {"impl_51"}
// Dependencies: {}
impl < T > SegQueue < T > { # [doc = " Creates a new unbounded queue."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_queue::SegQueue;"] # [doc = ""] # [doc = " let q = SegQueue::<i32>::new();"] # [doc = " ```"] pub const fn new () -> Self { Self { head : CachePadded :: new (Position { block : AtomicPtr :: new (ptr :: null_mut ()) , index : AtomicUsize :: new (0) , }) , tail : CachePadded :: new (Position { block : AtomicPtr :: new (ptr :: null_mut ()) , index : AtomicUsize :: new (0) , }) , _marker : PhantomData , } } # [doc = " Pushes back an element to the tail."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_queue::SegQueue;"] # [doc = ""] # [doc = " let q = SegQueue::new();"] # [doc = ""] # [doc = " q.push(10);"] # [doc = " q.push(20);"] # [doc = " ```"] pub fn push (& self , value : T) { let backoff = Backoff :: new () ; let mut tail = self . tail . index . load (Ordering :: Acquire) ; let mut block = self . tail . block . load (Ordering :: Acquire) ; let mut next_block = None ; loop { let offset = (tail >> SHIFT) % LAP ; if offset == BLOCK_CAP { backoff . snooze () ; tail = self . tail . index . load (Ordering :: Acquire) ; block = self . tail . block . load (Ordering :: Acquire) ; continue ; } if offset + 1 == BLOCK_CAP && next_block . is_none () { next_block = Some (Block :: < T > :: new ()) ; } if block . is_null () { let new = Box :: into_raw (Block :: < T > :: new ()) ; if self . tail . block . compare_exchange (block , new , Ordering :: Release , Ordering :: Relaxed) . is_ok () { self . head . block . store (new , Ordering :: Release) ; block = new ; } else { next_block = unsafe { Some (Box :: from_raw (new)) } ; tail = self . tail . index . load (Ordering :: Acquire) ; block = self . tail . block . load (Ordering :: Acquire) ; continue ; } } let new_tail = tail + (1 << SHIFT) ; match self . tail . index . compare_exchange_weak (tail , new_tail , Ordering :: SeqCst , Ordering :: Acquire ,) { Ok (_) => unsafe { if offset + 1 == BLOCK_CAP { let next_block = Box :: into_raw (next_block . unwrap ()) ; let next_index = new_tail . wrapping_add (1 << SHIFT) ; self . tail . block . store (next_block , Ordering :: Release) ; self . tail . index . store (next_index , Ordering :: Release) ; (* block) . next . store (next_block , Ordering :: Release) ; } let slot = (* block) . slots . get_unchecked (offset) ; slot . value . get () . write (MaybeUninit :: new (value)) ; slot . state . fetch_or (WRITE , Ordering :: Release) ; return ; } , Err (t) => { tail = t ; block = self . tail . block . load (Ordering :: Acquire) ; backoff . spin () ; } } } } # [doc = " Pops the head element from the queue."] # [doc = ""] # [doc = " If the queue is empty, `None` is returned."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_queue::SegQueue;"] # [doc = ""] # [doc = " let q = SegQueue::new();"] # [doc = ""] # [doc = " q.push(10);"] # [doc = " q.push(20);"] # [doc = " assert_eq!(q.pop(), Some(10));"] # [doc = " assert_eq!(q.pop(), Some(20));"] # [doc = " assert!(q.pop().is_none());"] # [doc = " ```"] pub fn pop (& self) -> Option < T > { let backoff = Backoff :: new () ; let mut head = self . head . index . load (Ordering :: Acquire) ; let mut block = self . head . block . load (Ordering :: Acquire) ; loop { let offset = (head >> SHIFT) % LAP ; if offset == BLOCK_CAP { backoff . snooze () ; head = self . head . index . load (Ordering :: Acquire) ; block = self . head . block . load (Ordering :: Acquire) ; continue ; } let mut new_head = head + (1 << SHIFT) ; if new_head & HAS_NEXT == 0 { atomic :: fence (Ordering :: SeqCst) ; let tail = self . tail . index . load (Ordering :: Relaxed) ; if head >> SHIFT == tail >> SHIFT { return None ; } if (head >> SHIFT) / LAP != (tail >> SHIFT) / LAP { new_head |= HAS_NEXT ; } } if block . is_null () { backoff . snooze () ; head = self . head . index . load (Ordering :: Acquire) ; block = self . head . block . load (Ordering :: Acquire) ; continue ; } match self . head . index . compare_exchange_weak (head , new_head , Ordering :: SeqCst , Ordering :: Acquire ,) { Ok (_) => unsafe { if offset + 1 == BLOCK_CAP { let next = (* block) . wait_next () ; let mut next_index = (new_head & ! HAS_NEXT) . wrapping_add (1 << SHIFT) ; if ! (* next) . next . load (Ordering :: Relaxed) . is_null () { next_index |= HAS_NEXT ; } self . head . block . store (next , Ordering :: Release) ; self . head . index . store (next_index , Ordering :: Release) ; } let slot = (* block) . slots . get_unchecked (offset) ; slot . wait_write () ; let value = slot . value . get () . read () . assume_init () ; if offset + 1 == BLOCK_CAP { Block :: destroy (block , 0) ; } else if slot . state . fetch_or (READ , Ordering :: AcqRel) & DESTROY != 0 { Block :: destroy (block , offset + 1) ; } return Some (value) ; } , Err (h) => { head = h ; block = self . head . block . load (Ordering :: Acquire) ; backoff . spin () ; } } } } # [doc = " Returns `true` if the queue is empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_queue::SegQueue;"] # [doc = ""] # [doc = " let q = SegQueue::new();"] # [doc = ""] # [doc = " assert!(q.is_empty());"] # [doc = " q.push(1);"] # [doc = " assert!(!q.is_empty());"] # [doc = " ```"] pub fn is_empty (& self) -> bool { let head = self . head . index . load (Ordering :: SeqCst) ; let tail = self . tail . index . load (Ordering :: SeqCst) ; head >> SHIFT == tail >> SHIFT } # [doc = " Returns the number of elements in the queue."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use crossbeam_queue::SegQueue;"] # [doc = ""] # [doc = " let q = SegQueue::new();"] # [doc = " assert_eq!(q.len(), 0);"] # [doc = ""] # [doc = " q.push(10);"] # [doc = " assert_eq!(q.len(), 1);"] # [doc = ""] # [doc = " q.push(20);"] # [doc = " assert_eq!(q.len(), 2);"] # [doc = " ```"] pub fn len (& self) -> usize { loop { let mut tail = self . tail . index . load (Ordering :: SeqCst) ; let mut head = self . head . index . load (Ordering :: SeqCst) ; if self . tail . index . load (Ordering :: SeqCst) == tail { tail &= ! ((1 << SHIFT) - 1) ; head &= ! ((1 << SHIFT) - 1) ; if (tail >> SHIFT) & (LAP - 1) == LAP - 1 { tail = tail . wrapping_add (1 << SHIFT) ; } if (head >> SHIFT) & (LAP - 1) == LAP - 1 { head = head . wrapping_add (1 << SHIFT) ; } let lap = (head >> SHIFT) / LAP ; tail = tail . wrapping_sub ((lap * LAP) << SHIFT) ; head = head . wrapping_sub ((lap * LAP) << SHIFT) ; tail >>= SHIFT ; head >>= SHIFT ; return tail - head - tail / LAP ; } } } }
};
}
