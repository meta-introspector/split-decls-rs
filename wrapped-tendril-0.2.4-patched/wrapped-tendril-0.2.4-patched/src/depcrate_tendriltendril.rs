// Generated macro for Tendril (struct)
macro_rules! Depcrate_tendrilTendril {
() => {
// Module: crate::tendril
// Provides: {"Tendril"}
// Dependencies: {}
# [doc = " Compact string type for zero-copy parsing."] # [doc = ""] # [doc = " `Tendril`s have the semantics of owned strings, but are sometimes views"] # [doc = " into shared buffers. When you mutate a `Tendril`, an owned copy is made"] # [doc = " if necessary. Further mutations occur in-place until the string becomes"] # [doc = " shared, e.g. with `clone()` or `subtendril()`."] # [doc = ""] # [doc = " Buffer sharing is accomplished through thread-local (non-atomic) reference"] # [doc = " counting, which has very low overhead. The Rust type system will prevent"] # [doc = " you at compile time from sending a `Tendril` between threads. We plan to"] # [doc = " relax this restriction in the future; see `README.md`."] # [doc = ""] # [doc = " Whereas `String` allocates in the heap for any non-empty string, `Tendril`"] # [doc = " can store small strings (up to 8 bytes) in-line, without a heap allocation."] # [doc = " `Tendril` is also smaller than `String` on 64-bit platforms — 16 bytes"] # [doc = " versus 24."] # [doc = ""] # [doc = " The type parameter `F` specifies the format of the tendril, for example"] # [doc = " UTF-8 text or uninterpreted bytes. The parameter will be instantiated"] # [doc = " with one of the marker types from `tendril::fmt`. See the `StrTendril`"] # [doc = " and `ByteTendril` type aliases for two examples."] # [doc = ""] # [doc = " The type parameter `A` indicates the atomicity of the tendril; it is by"] # [doc = " default `NonAtomic`, but can be specified as `Atomic` to get a tendril"] # [doc = " which implements `Send` and `Sync` (viz. a thread-safe tendril)."] # [doc = ""] # [doc = " The maximum length of a `Tendril` is 4 GB. The library will panic if"] # [doc = " you attempt to go over the limit."] pub struct Tendril < F , A = NonAtomic > where F : fmt :: Format , A : Atomicity , { ptr : Cell < NonZero < usize > > , len : u32 , aux : Cell < u32 > , marker : PhantomData < * mut F > , refcount_marker : PhantomData < A > , }
};
}
