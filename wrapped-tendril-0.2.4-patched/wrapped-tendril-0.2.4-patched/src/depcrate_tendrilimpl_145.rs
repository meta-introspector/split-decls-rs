// Generated macro for impl_145 (impl)
macro_rules! Depcrate_tendrilimpl_145 {
() => {
// Module: crate::tendril
// Provides: {"impl_145"}
// Dependencies: {}
impl < F , A > Tendril < F , A > where F : fmt :: Format , A : Atomicity , { # [doc = " Create a new, empty `Tendril` in any format."] # [inline (always)] pub fn new () -> Tendril < F , A > { unsafe { Tendril :: inline (& []) } } # [doc = " Create a new, empty `Tendril` with a specified capacity."] # [inline] pub fn with_capacity (capacity : u32) -> Tendril < F , A > { let mut t : Tendril < F , A > = Tendril :: new () ; if capacity > MAX_INLINE_LEN as u32 { unsafe { t . make_owned_with_capacity (capacity) ; } } t } # [doc = " Reserve space for additional bytes."] # [doc = ""] # [doc = " This is only a suggestion. There are cases where `Tendril` will"] # [doc = " decline to allocate until the buffer is actually modified."] # [inline] pub fn reserve (& mut self , additional : u32) { if ! self . is_shared () { self . force_reserve (additional) ; } } # [doc = " Reserve space for additional bytes, even for shared buffers."] # [inline] fn force_reserve (& mut self , additional : u32) { let new_len = self . len32 () . checked_add (additional) . expect (OFLOW) ; if new_len > MAX_INLINE_LEN as u32 { unsafe { self . make_owned_with_capacity (new_len) ; } } } # [doc = " Get the length of the `Tendril`."] # [doc = ""] # [doc = " This is named not to conflict with `len()` on the underlying"] # [doc = " slice, if any."] # [inline (always)] pub fn len32 (& self) -> u32 { match self . ptr . get () . get () { EMPTY_TAG => 0 , n if n <= MAX_INLINE_LEN => n as u32 , _ => self . len , } } # [doc = " Is the backing buffer shared?"] # [inline] pub fn is_shared (& self) -> bool { let n = self . ptr . get () . get () ; (n > MAX_INLINE_TAG) && ((n & 1) == 1) } # [doc = " Is the backing buffer shared with this other `Tendril`?"] # [inline] pub fn is_shared_with (& self , other : & Tendril < F , A >) -> bool { let n = self . ptr . get () . get () ; (n > MAX_INLINE_TAG) && (n == other . ptr . get () . get ()) } # [doc = " Truncate to length 0 without discarding any owned storage."] # [inline] pub fn clear (& mut self) { if self . ptr . get () . get () <= MAX_INLINE_TAG { self . ptr . set (unsafe { NonZero :: new (EMPTY_TAG) }) ; } else { let (_ , shared , _) = unsafe { self . assume_buf () } ; if shared { * self = Tendril :: new () ; } else { self . len = 0 ; } } } # [doc = " Build a `Tendril` by copying a byte slice, if it conforms to the format."] # [inline] pub fn try_from_byte_slice (x : & [u8]) -> Result < Tendril < F , A > , () > { match F :: validate (x) { true => Ok (unsafe { Tendril :: from_byte_slice_without_validating (x) }) , false => Err (()) , } } # [doc = " View as uninterpreted bytes."] # [inline (always)] pub fn as_bytes (& self) -> & Tendril < fmt :: Bytes , A > { unsafe { mem :: transmute (self) } } # [doc = " Convert into uninterpreted bytes."] # [inline (always)] pub fn into_bytes (self) -> Tendril < fmt :: Bytes , A > { unsafe { mem :: transmute (self) } } # [doc = " Convert `self` into a type which is `Send`."] # [doc = ""] # [doc = " If the tendril is owned or inline, this is free,"] # [doc = " but if it's shared this will entail a copy of the contents."] # [inline] pub fn into_send (mut self) -> SendTendril < F > { self . make_owned () ; SendTendril { tendril : unsafe { mem :: transmute (self) } , } } # [doc = " View as a superset format, for free."] # [inline (always)] pub fn as_superset < Super > (& self) -> & Tendril < Super , A > where F : fmt :: SubsetOf < Super > , Super : fmt :: Format , { unsafe { mem :: transmute (self) } } # [doc = " Convert into a superset format, for free."] # [inline (always)] pub fn into_superset < Super > (self) -> Tendril < Super , A > where F : fmt :: SubsetOf < Super > , Super : fmt :: Format , { unsafe { mem :: transmute (self) } } # [doc = " View as a subset format, if the `Tendril` conforms to that subset."] # [inline] pub fn try_as_subset < Sub > (& self) -> Result < & Tendril < Sub , A > , () > where Sub : fmt :: SubsetOf < F > , { match Sub :: revalidate_subset (self . as_byte_slice ()) { true => Ok (unsafe { mem :: transmute (self) }) , false => Err (()) , } } # [doc = " Convert into a subset format, if the `Tendril` conforms to that subset."] # [inline] pub fn try_into_subset < Sub > (self) -> Result < Tendril < Sub , A > , Self > where Sub : fmt :: SubsetOf < F > , { match Sub :: revalidate_subset (self . as_byte_slice ()) { true => Ok (unsafe { mem :: transmute (self) }) , false => Err (self) , } } # [doc = " View as another format, if the bytes of the `Tendril` are valid for"] # [doc = " that format."] # [inline] pub fn try_reinterpret_view < Other > (& self) -> Result < & Tendril < Other , A > , () > where Other : fmt :: Format , { match Other :: validate (self . as_byte_slice ()) { true => Ok (unsafe { mem :: transmute (self) }) , false => Err (()) , } } # [doc = " Convert into another format, if the `Tendril` conforms to that format."] # [doc = ""] # [doc = " This only re-validates the existing bytes under the new format. It"] # [doc = " will *not* change the byte content of the tendril!"] # [doc = ""] # [doc = " See the `encode` and `decode` methods for character encoding conversion."] # [inline] pub fn try_reinterpret < Other > (self) -> Result < Tendril < Other , A > , Self > where Other : fmt :: Format , { match Other :: validate (self . as_byte_slice ()) { true => Ok (unsafe { mem :: transmute (self) }) , false => Err (self) , } } # [doc = " Push some bytes onto the end of the `Tendril`, if they conform to the"] # [doc = " format."] # [inline] pub fn try_push_bytes (& mut self , buf : & [u8]) -> Result < () , () > { match F :: validate (buf) { true => unsafe { self . push_bytes_without_validating (buf) ; Ok (()) } , false => Err (()) , } } # [doc = " Push another `Tendril` onto the end of this one."] # [inline] pub fn push_tendril (& mut self , other : & Tendril < F , A >) { let new_len = self . len32 () . checked_add (other . len32 ()) . expect (OFLOW) ; unsafe { if (self . ptr . get () . get () > MAX_INLINE_TAG) && (other . ptr . get () . get () > MAX_INLINE_TAG) { let (self_buf , self_shared , _) = self . assume_buf () ; let (other_buf , other_shared , _) = other . assume_buf () ; if self_shared && other_shared && (self_buf . data_ptr () == other_buf . data_ptr ()) && (other . aux . get () == self . aux . get () + self . len) { self . len = new_len ; return ; } } self . push_bytes_without_validating (other . as_byte_slice ()) } } # [doc = " Attempt to slice this `Tendril` as a new `Tendril`."] # [doc = ""] # [doc = " This will share the buffer when possible. Mutating a shared buffer"] # [doc = " will copy the contents."] # [doc = ""] # [doc = " The offset and length are in bytes. The function will return"] # [doc = " `Err` if these are out of bounds, or if the resulting slice"] # [doc = " does not conform to the format."] # [inline] pub fn try_subtendril (& self , offset : u32 , length : u32) -> Result < Tendril < F , A > , SubtendrilError > { let self_len = self . len32 () ; if offset > self_len || length > (self_len - offset) { return Err (SubtendrilError :: OutOfBounds) ; } unsafe { let byte_slice = unsafe_slice (self . as_byte_slice () , offset as usize , length as usize) ; if ! F :: validate_subseq (byte_slice) { return Err (SubtendrilError :: ValidationFailed) ; } Ok (self . unsafe_subtendril (offset , length)) } } # [doc = " Slice this `Tendril` as a new `Tendril`."] # [doc = ""] # [doc = " Panics on bounds or validity check failure."] # [inline] pub fn subtendril (& self , offset : u32 , length : u32) -> Tendril < F , A > { self . try_subtendril (offset , length) . unwrap () } # [doc = " Try to drop `n` bytes from the front."] # [doc = ""] # [doc = " Returns `Err` if the bytes are not available, or the suffix fails"] # [doc = " validation."] # [inline] pub fn try_pop_front (& mut self , n : u32) -> Result < () , SubtendrilError > { if n == 0 { return Ok (()) ; } let old_len = self . len32 () ; if n > old_len { return Err (SubtendrilError :: OutOfBounds) ; } let new_len = old_len - n ; unsafe { if ! F :: validate_suffix (unsafe_slice (self . as_byte_slice () , n as usize , new_len as usize)) { return Err (SubtendrilError :: ValidationFailed) ; } self . unsafe_pop_front (n) ; Ok (()) } } # [doc = " Drop `n` bytes from the front."] # [doc = ""] # [doc = " Panics if the bytes are not available, or the suffix fails"] # [doc = " validation."] # [inline] pub fn pop_front (& mut self , n : u32) { self . try_pop_front (n) . unwrap () } # [doc = " Drop `n` bytes from the back."] # [doc = ""] # [doc = " Returns `Err` if the bytes are not available, or the prefix fails"] # [doc = " validation."] # [inline] pub fn try_pop_back (& mut self , n : u32) -> Result < () , SubtendrilError > { if n == 0 { return Ok (()) ; } let old_len = self . len32 () ; if n > old_len { return Err (SubtendrilError :: OutOfBounds) ; } let new_len = old_len - n ; unsafe { if ! F :: validate_prefix (unsafe_slice (self . as_byte_slice () , 0 , new_len as usize)) { return Err (SubtendrilError :: ValidationFailed) ; } self . unsafe_pop_back (n) ; Ok (()) } } # [doc = " Drop `n` bytes from the back."] # [doc = ""] # [doc = " Panics if the bytes are not available, or the prefix fails"] # [doc = " validation."] # [inline] pub fn pop_back (& mut self , n : u32) { self . try_pop_back (n) . unwrap () } # [doc = " View as another format, without validating."] # [inline (always)] pub unsafe fn reinterpret_view_without_validating < Other > (& self) -> & Tendril < Other , A > where Other : fmt :: Format , { mem :: transmute (self) } # [doc = " Convert into another format, without validating."] # [inline (always)] pub unsafe fn reinterpret_without_validating < Other > (self) -> Tendril < Other , A > where Other : fmt :: Format , { mem :: transmute (self) } # [doc = " Build a `Tendril` by copying a byte slice, without validating."] # [inline] pub unsafe fn from_byte_slice_without_validating (x : & [u8]) -> Tendril < F , A > { assert ! (x . len () <= buf32 :: MAX_LEN) ; if x . len () <= MAX_INLINE_LEN { Tendril :: inline (x) } else { Tendril :: owned_copy (x) } } # [doc = " Push some bytes onto the end of the `Tendril`, without validating."] # [inline] pub unsafe fn push_bytes_without_validating (& mut self , buf : & [u8]) { assert ! (buf . len () <= buf32 :: MAX_LEN) ; let Fixup { drop_left , drop_right , insert_len , insert_bytes } = F :: fixup (self . as_byte_slice () , buf) ; let adj_len = self . len32 () + insert_len - drop_left ; let new_len = adj_len . checked_add (buf . len () as u32) . expect (OFLOW) - drop_right ; let drop_left = drop_left as usize ; let drop_right = drop_right as usize ; if new_len <= MAX_INLINE_LEN as u32 { let mut tmp : [u8 ; MAX_INLINE_LEN] = mem :: uninitialized () ; { let old = self . as_byte_slice () ; let mut dest = tmp . as_mut_ptr () ; copy_and_advance (& mut dest , unsafe_slice (old , 0 , old . len () - drop_left)) ; copy_and_advance (& mut dest , unsafe_slice (& insert_bytes , 0 , insert_len as usize)) ; copy_and_advance (& mut dest , unsafe_slice (buf , drop_right , buf . len () - drop_right)) ; } * self = Tendril :: inline (& tmp [.. new_len as usize]) ; } else { self . make_owned_with_capacity (new_len) ; let (owned , _ , _) = self . assume_buf () ; let mut dest = owned . data_ptr () . offset ((owned . len as usize - drop_left) as isize) ; copy_and_advance (& mut dest , unsafe_slice (& insert_bytes , 0 , insert_len as usize)) ; copy_and_advance (& mut dest , unsafe_slice (buf , drop_right , buf . len () - drop_right)) ; self . len = new_len ; } } # [doc = " Slice this `Tendril` as a new `Tendril`."] # [doc = ""] # [doc = " Does not check validity or bounds!"] # [inline] pub unsafe fn unsafe_subtendril (& self , offset : u32 , length : u32) -> Tendril < F , A > { if length <= MAX_INLINE_LEN as u32 { Tendril :: inline (unsafe_slice (self . as_byte_slice () , offset as usize , length as usize)) } else { self . make_buf_shared () ; self . incref () ; let (buf , _ , _) = self . assume_buf () ; Tendril :: shared (buf , self . aux . get () + offset , length) } } # [doc = " Drop `n` bytes from the front."] # [doc = ""] # [doc = " Does not check validity or bounds!"] # [inline] pub unsafe fn unsafe_pop_front (& mut self , n : u32) { let new_len = self . len32 () - n ; if new_len <= MAX_INLINE_LEN as u32 { * self = Tendril :: inline (unsafe_slice (self . as_byte_slice () , n as usize , new_len as usize)) ; } else { self . make_buf_shared () ; self . aux . set (self . aux . get () + n) ; self . len -= n ; } } # [doc = " Drop `n` bytes from the back."] # [doc = ""] # [doc = " Does not check validity or bounds!"] # [inline] pub unsafe fn unsafe_pop_back (& mut self , n : u32) { let new_len = self . len32 () - n ; if new_len <= MAX_INLINE_LEN as u32 { * self = Tendril :: inline (unsafe_slice (self . as_byte_slice () , 0 , new_len as usize)) ; } else { self . make_buf_shared () ; self . len -= n ; } } # [inline] unsafe fn incref (& self) { (* self . header ()) . refcount . increment () ; } # [inline] unsafe fn make_buf_shared (& self) { let p = self . ptr . get () . get () ; if p & 1 == 0 { let header = p as * mut Header < A > ; (* header) . cap = self . aux . get () ; self . ptr . set (NonZero :: new (p | 1)) ; self . aux . set (0) ; } } # [inline] fn make_owned (& mut self) { unsafe { let ptr = self . ptr . get () . get () ; if ptr <= MAX_INLINE_TAG || (ptr & 1) == 1 { * self = Tendril :: owned_copy (self . as_byte_slice ()) ; } } } # [inline] unsafe fn make_owned_with_capacity (& mut self , cap : u32) { self . make_owned () ; let mut buf = self . assume_buf () . 0 ; buf . grow (cap) ; self . ptr . set (NonZero :: new (buf . ptr as usize)) ; self . aux . set (buf . cap) ; } # [inline (always)] unsafe fn header (& self) -> * mut Header < A > { (self . ptr . get () . get () & ! 1) as * mut Header < A > } # [inline] unsafe fn assume_buf (& self) -> (Buf32 < Header < A > > , bool , u32) { let ptr = self . ptr . get () . get () ; let header = self . header () ; let shared = (ptr & 1) == 1 ; let (cap , offset) = match shared { true => ((* header) . cap , self . aux . get ()) , false => (self . aux . get () , 0) , } ; (Buf32 { ptr : header , len : offset + self . len32 () , cap : cap , } , shared , offset) } # [inline] unsafe fn inline (x : & [u8]) -> Tendril < F , A > { let len = x . len () ; let mut t = Tendril { ptr : Cell :: new (inline_tag (len as u32)) , len : mem :: uninitialized () , aux : mem :: uninitialized () , marker : PhantomData , refcount_marker : PhantomData , } ; ptr :: copy_nonoverlapping (x . as_ptr () , & mut t . len as * mut u32 as * mut u8 , len) ; t } # [inline] unsafe fn owned (x : Buf32 < Header < A > >) -> Tendril < F , A > { Tendril { ptr : Cell :: new (NonZero :: new (x . ptr as usize)) , len : x . len , aux : Cell :: new (x . cap) , marker : PhantomData , refcount_marker : PhantomData , } } # [inline] unsafe fn owned_copy (x : & [u8]) -> Tendril < F , A > { let len32 = x . len () as u32 ; let mut b = Buf32 :: with_capacity (len32 , Header :: new ()) ; ptr :: copy_nonoverlapping (x . as_ptr () , b . data_ptr () , x . len ()) ; b . len = len32 ; Tendril :: owned (b) } # [inline] unsafe fn shared (buf : Buf32 < Header < A > > , off : u32 , len : u32) -> Tendril < F , A > { Tendril { ptr : Cell :: new (NonZero :: new ((buf . ptr as usize) | 1)) , len : len , aux : Cell :: new (off) , marker : PhantomData , refcount_marker : PhantomData , } } # [inline] fn as_byte_slice < 'a > (& 'a self) -> & 'a [u8] { unsafe { match self . ptr . get () . get () { EMPTY_TAG => & [] , n if n <= MAX_INLINE_LEN => { slice :: from_raw_parts (& self . len as * const u32 as * const u8 , n) } _ => { let (buf , _ , offset) = self . assume_buf () ; copy_lifetime (self , unsafe_slice (buf . data () , offset as usize , self . len32 () as usize)) } } } } # [inline] fn as_mut_byte_slice < 'a > (& 'a mut self) -> & 'a mut [u8] { unsafe { match self . ptr . get () . get () { EMPTY_TAG => & mut [] , n if n <= MAX_INLINE_LEN => { slice :: from_raw_parts_mut (& mut self . len as * mut u32 as * mut u8 , n) } _ => { self . make_owned () ; let (mut buf , _ , offset) = self . assume_buf () ; let len = self . len32 () as usize ; copy_lifetime_mut (self , unsafe_slice_mut (buf . data_mut () , offset as usize , len)) } } } } }
};
}
