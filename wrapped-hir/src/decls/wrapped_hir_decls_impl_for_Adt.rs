// Generated by add_generated_rust_header! macro at submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs:72 from submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs
// Do not edit this file by hand. Your changes will be overwritten.

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
impl Adt {
    pub fn has_non_default_type_params(self, db: &dyn HirDatabase) -> bool {
        has_non_default_type_params(db, self.into())
    }
    pub fn layout(self, db: &dyn HirDatabase) -> Result<Layout, LayoutError> {
        let env = db.trait_environment(self.into());
        let interner = DbInterner::new_with(db, Some(env.krate), env.block);
        let adt_id = AdtId::from(self);
        let args = GenericArgs::for_item_with_defaults(interner, adt_id.into(), |_, id, _| {
            GenericArg::error_from_id(interner, id)
        });
        db.layout_of_adt(adt_id, args, env)
            .map(|layout| Layout(layout, db.target_data_layout(self.krate(db).id).unwrap()))
    }
    #[doc = " Turns this ADT into a type. Any type parameters of the ADT will be"]
    #[doc = " turned into unknown types, which is good for e.g. finding the most"]
    #[doc = " general set of completions, but will not look very nice when printed."]
    pub fn ty(self, db: &dyn HirDatabase) -> Type<'_> {
        let id = AdtId::from(self);
        Type::from_def(db, id)
    }
    #[doc = " Turns this ADT into a type with the given type parameters. This isn't"]
    #[doc = " the greatest API, FIXME find a better one."]
    pub fn ty_with_args<'db>(
        self,
        db: &'db dyn HirDatabase,
        args: impl IntoIterator<Item = Type<'db>>,
    ) -> Type<'db> {
        let id = AdtId::from(self);
        let interner = DbInterner::new_with(db, None, None);
        let ty = Ty::new_adt(
            interner,
            id,
            generic_args_from_tys(interner, id.into(), args.into_iter().map(|ty| ty.ty)),
        );
        Type::new(db, id, ty)
    }
    pub fn module(self, db: &dyn HirDatabase) -> Module {
        match self {
            Adt::Struct(s) => s.module(db),
            Adt::Union(s) => s.module(db),
            Adt::Enum(e) => e.module(db),
        }
    }
    pub fn name(self, db: &dyn HirDatabase) -> Name {
        match self {
            Adt::Struct(s) => s.name(db),
            Adt::Union(u) => u.name(db),
            Adt::Enum(e) => e.name(db),
        }
    }
    #[doc = " Returns the lifetime of the DataType"]
    pub fn lifetime(&self, db: &dyn HirDatabase) -> Option<LifetimeParamData> {
        let resolver = match self {
            Adt::Struct(s) => s.id.resolver(db),
            Adt::Union(u) => u.id.resolver(db),
            Adt::Enum(e) => e.id.resolver(db),
        };
        resolver
            .generic_params()
            .and_then(|gp| gp.iter_lt().nth(0))
            .map(|arena| arena.1.clone())
    }
    pub fn as_struct(&self) -> Option<Struct> {
        if let Self::Struct(v) = self {
            Some(*v)
        } else {
            None
        }
    }
    pub fn as_enum(&self) -> Option<Enum> {
        if let Self::Enum(v) = self {
            Some(*v)
        } else {
            None
        }
    }
}
