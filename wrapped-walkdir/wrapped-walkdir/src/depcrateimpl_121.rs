// Generated macro for impl_121 (impl)
macro_rules! Depcrateimpl_121 {
() => {
// Module: crate
// Provides: {"impl_121"}
// Dependencies: {}
impl WalkDir { # [doc = " Create a builder for a recursive directory iterator starting at the"] # [doc = " file path `root`. If `root` is a directory, then it is the first item"] # [doc = " yielded by the iterator. If `root` is a file, then it is the first"] # [doc = " and only item yielded by the iterator. If `root` is a symlink, then it"] # [doc = " is always followed for the purposes of directory traversal. (A root"] # [doc = " `DirEntry` still obeys its documentation with respect to symlinks and"] # [doc = " the `follow_links` setting.)"] pub fn new < P : AsRef < Path > > (root : P) -> Self { WalkDir { opts : WalkDirOptions { follow_links : false , follow_root_links : true , max_open : 10 , min_depth : 0 , max_depth : :: std :: usize :: MAX , sorter : None , contents_first : false , same_file_system : false , } , root : root . as_ref () . to_path_buf () , } } # [doc = " Set the minimum depth of entries yielded by the iterator."] # [doc = ""] # [doc = " The smallest depth is `0` and always corresponds to the path given"] # [doc = " to the `new` function on this type. Its direct descendents have depth"] # [doc = " `1`, and their descendents have depth `2`, and so on."] pub fn min_depth (mut self , depth : usize) -> Self { self . opts . min_depth = depth ; if self . opts . min_depth > self . opts . max_depth { self . opts . min_depth = self . opts . max_depth ; } self } # [doc = " Set the maximum depth of entries yield by the iterator."] # [doc = ""] # [doc = " The smallest depth is `0` and always corresponds to the path given"] # [doc = " to the `new` function on this type. Its direct descendents have depth"] # [doc = " `1`, and their descendents have depth `2`, and so on."] # [doc = ""] # [doc = " Note that this will not simply filter the entries of the iterator, but"] # [doc = " it will actually avoid descending into directories when the depth is"] # [doc = " exceeded."] pub fn max_depth (mut self , depth : usize) -> Self { self . opts . max_depth = depth ; if self . opts . max_depth < self . opts . min_depth { self . opts . max_depth = self . opts . min_depth ; } self } # [doc = " Follow symbolic links. By default, this is disabled."] # [doc = ""] # [doc = " When `yes` is `true`, symbolic links are followed as if they were"] # [doc = " normal directories and files. If a symbolic link is broken or is"] # [doc = " involved in a loop, an error is yielded."] # [doc = ""] # [doc = " When enabled, the yielded [`DirEntry`] values represent the target of"] # [doc = " the link while the path corresponds to the link. See the [`DirEntry`]"] # [doc = " type for more details."] # [doc = ""] # [doc = " [`DirEntry`]: struct.DirEntry.html"] pub fn follow_links (mut self , yes : bool) -> Self { self . opts . follow_links = yes ; self } # [doc = " Follow symbolic links if these are the root of the traversal."] # [doc = " By default, this is enabled."] # [doc = ""] # [doc = " When `yes` is `true`, symbolic links on root paths are followed"] # [doc = " which is effective if the symbolic link points to a directory."] # [doc = " If a symbolic link is broken or is involved in a loop, an error is yielded"] # [doc = " as the first entry of the traversal."] # [doc = ""] # [doc = " When enabled, the yielded [`DirEntry`] values represent the target of"] # [doc = " the link while the path corresponds to the link. See the [`DirEntry`]"] # [doc = " type for more details, and all future entries will be contained within"] # [doc = " the resolved directory behind the symbolic link of the root path."] # [doc = ""] # [doc = " [`DirEntry`]: struct.DirEntry.html"] pub fn follow_root_links (mut self , yes : bool) -> Self { self . opts . follow_root_links = yes ; self } # [doc = " Set the maximum number of simultaneously open file descriptors used"] # [doc = " by the iterator."] # [doc = ""] # [doc = " `n` must be greater than or equal to `1`. If `n` is `0`, then it is set"] # [doc = " to `1` automatically. If this is not set, then it defaults to some"] # [doc = " reasonably low number."] # [doc = ""] # [doc = " This setting has no impact on the results yielded by the iterator"] # [doc = " (even when `n` is `1`). Instead, this setting represents a trade off"] # [doc = " between scarce resources (file descriptors) and memory. Namely, when"] # [doc = " the maximum number of file descriptors is reached and a new directory"] # [doc = " needs to be opened to continue iteration, then a previous directory"] # [doc = " handle is closed and has its unyielded entries stored in memory. In"] # [doc = " practice, this is a satisfying trade off because it scales with respect"] # [doc = " to the *depth* of your file tree. Therefore, low values (even `1`) are"] # [doc = " acceptable."] # [doc = ""] # [doc = " Note that this value does not impact the number of system calls made by"] # [doc = " an exhausted iterator."] # [doc = ""] # [doc = " # Platform behavior"] # [doc = ""] # [doc = " On Windows, if `follow_links` is enabled, then this limit is not"] # [doc = " respected. In particular, the maximum number of file descriptors opened"] # [doc = " is proportional to the depth of the directory tree traversed."] pub fn max_open (mut self , mut n : usize) -> Self { if n == 0 { n = 1 ; } self . opts . max_open = n ; self } # [doc = " Set a function for sorting directory entries with a comparator"] # [doc = " function."] # [doc = ""] # [doc = " If a compare function is set, the resulting iterator will return all"] # [doc = " paths in sorted order. The compare function will be called to compare"] # [doc = " entries from the same directory."] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::cmp;"] # [doc = " use std::ffi::OsString;"] # [doc = " use walkdir::WalkDir;"] # [doc = ""] # [doc = " WalkDir::new(\"foo\").sort_by(|a,b| a.file_name().cmp(b.file_name()));"] # [doc = " ```"] pub fn sort_by < F > (mut self , cmp : F) -> Self where F : FnMut (& DirEntry , & DirEntry) -> Ordering + Send + Sync + 'static , { self . opts . sorter = Some (Box :: new (cmp)) ; self } # [doc = " Set a function for sorting directory entries with a key extraction"] # [doc = " function."] # [doc = ""] # [doc = " If a compare function is set, the resulting iterator will return all"] # [doc = " paths in sorted order. The compare function will be called to compare"] # [doc = " entries from the same directory."] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use std::cmp;"] # [doc = " use std::ffi::OsString;"] # [doc = " use walkdir::WalkDir;"] # [doc = ""] # [doc = " WalkDir::new(\"foo\").sort_by_key(|a| a.file_name().to_owned());"] # [doc = " ```"] pub fn sort_by_key < K , F > (self , mut cmp : F) -> Self where F : FnMut (& DirEntry) -> K + Send + Sync + 'static , K : Ord , { self . sort_by (move | a , b | cmp (a) . cmp (& cmp (b))) } # [doc = " Sort directory entries by file name, to ensure a deterministic order."] # [doc = ""] # [doc = " This is a convenience function for calling `Self::sort_by()`."] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " use walkdir::WalkDir;"] # [doc = ""] # [doc = " WalkDir::new(\"foo\").sort_by_file_name();"] # [doc = " ```"] pub fn sort_by_file_name (self) -> Self { self . sort_by (| a , b | a . file_name () . cmp (b . file_name ())) } # [doc = " Yield a directory's contents before the directory itself. By default,"] # [doc = " this is disabled."] # [doc = ""] # [doc = " When `yes` is `false` (as is the default), the directory is yielded"] # [doc = " before its contents are read. This is useful when, e.g. you want to"] # [doc = " skip processing of some directories."] # [doc = ""] # [doc = " When `yes` is `true`, the iterator yields the contents of a directory"] # [doc = " before yielding the directory itself. This is useful when, e.g. you"] # [doc = " want to recursively delete a directory."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Assume the following directory tree:"] # [doc = ""] # [doc = " ```text"] # [doc = " foo/"] # [doc = "   abc/"] # [doc = "     qrs"] # [doc = "     tuv"] # [doc = "   def/"] # [doc = " ```"] # [doc = ""] # [doc = " With contents_first disabled (the default), the following code visits"] # [doc = " the directory tree in depth-first order:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use walkdir::WalkDir;"] # [doc = ""] # [doc = " for entry in WalkDir::new(\"foo\") {"] # [doc = "     let entry = entry.unwrap();"] # [doc = "     println!(\"{}\", entry.path().display());"] # [doc = " }"] # [doc = ""] # [doc = " // foo"] # [doc = " // foo/abc"] # [doc = " // foo/abc/qrs"] # [doc = " // foo/abc/tuv"] # [doc = " // foo/def"] # [doc = " ```"] # [doc = ""] # [doc = " With contents_first enabled:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use walkdir::WalkDir;"] # [doc = ""] # [doc = " for entry in WalkDir::new(\"foo\").contents_first(true) {"] # [doc = "     let entry = entry.unwrap();"] # [doc = "     println!(\"{}\", entry.path().display());"] # [doc = " }"] # [doc = ""] # [doc = " // foo/abc/qrs"] # [doc = " // foo/abc/tuv"] # [doc = " // foo/abc"] # [doc = " // foo/def"] # [doc = " // foo"] # [doc = " ```"] pub fn contents_first (mut self , yes : bool) -> Self { self . opts . contents_first = yes ; self } # [doc = " Do not cross file system boundaries."] # [doc = ""] # [doc = " When this option is enabled, directory traversal will not descend into"] # [doc = " directories that are on a different file system from the root path."] # [doc = ""] # [doc = " Currently, this option is only supported on Unix and Windows. If this"] # [doc = " option is used on an unsupported platform, then directory traversal"] # [doc = " will immediately return an error and will not yield any entries."] pub fn same_file_system (mut self , yes : bool) -> Self { self . opts . same_file_system = yes ; self } }
};
}
