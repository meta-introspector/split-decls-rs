// Generated macro for IntoIter (struct)
macro_rules! DepcrateIntoIter {
() => {
// Module: crate
// Provides: {"IntoIter"}
// Dependencies: {}
# [doc = " An iterator for recursively descending into a directory."] # [doc = ""] # [doc = " A value with this type must be constructed with the [`WalkDir`] type, which"] # [doc = " uses a builder pattern to set options such as min/max depth, max open file"] # [doc = " descriptors and whether the iterator should follow symbolic links. After"] # [doc = " constructing a `WalkDir`, call [`.into_iter()`] at the end of the chain."] # [doc = ""] # [doc = " The order of elements yielded by this iterator is unspecified."] # [doc = ""] # [doc = " [`WalkDir`]: struct.WalkDir.html"] # [doc = " [`.into_iter()`]: struct.WalkDir.html#into_iter.v"] # [derive (Debug)] pub struct IntoIter { # [doc = " Options specified in the builder. Depths, max fds, etc."] opts : WalkDirOptions , # [doc = " The start path."] # [doc = ""] # [doc = " This is only `Some(...)` at the beginning. After the first iteration,"] # [doc = " this is always `None`."] start : Option < PathBuf > , # [doc = " A stack of open (up to max fd) or closed handles to directories."] # [doc = " An open handle is a plain [`fs::ReadDir`] while a closed handle is"] # [doc = " a `Vec<fs::DirEntry>` corresponding to the as-of-yet consumed entries."] # [doc = ""] # [doc = " [`fs::ReadDir`]: https://doc.rust-lang.org/stable/std/fs/struct.ReadDir.html"] stack_list : Vec < DirList > , # [doc = " A stack of file paths."] # [doc = ""] # [doc = " This is *only* used when [`follow_links`] is enabled. In all other"] # [doc = " cases this stack is empty."] # [doc = ""] # [doc = " [`follow_links`]: struct.WalkDir.html#method.follow_links"] stack_path : Vec < Ancestor > , # [doc = " An index into `stack_list` that points to the oldest open directory"] # [doc = " handle. If the maximum fd limit is reached and a new directory needs to"] # [doc = " be read, the handle at this index is closed before the new directory is"] # [doc = " opened."] oldest_opened : usize , # [doc = " The current depth of iteration (the length of the stack at the"] # [doc = " beginning of each iteration)."] depth : usize , # [doc = " A list of DirEntries corresponding to directories, that are"] # [doc = " yielded after their contents has been fully yielded. This is only"] # [doc = " used when `contents_first` is enabled."] deferred_dirs : Vec < DirEntry > , # [doc = " The device of the root file path when the first call to `next` was"] # [doc = " made."] # [doc = ""] # [doc = " If the `same_file_system` option isn't enabled, then this is always"] # [doc = " `None`. Conversely, if it is enabled, this is always `Some(...)` after"] # [doc = " handling the root path."] root_device : Option < u64 > , }
};
}
