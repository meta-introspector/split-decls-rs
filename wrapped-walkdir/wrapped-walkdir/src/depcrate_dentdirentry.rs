// Generated macro for DirEntry (struct)
macro_rules! Depcrate_dentDirEntry {
() => {
// Module: crate::dent
// Provides: {"DirEntry"}
// Dependencies: {}
# [doc = " A directory entry."] # [doc = ""] # [doc = " This is the type of value that is yielded from the iterators defined in"] # [doc = " this crate."] # [doc = ""] # [doc = " On Unix systems, this type implements the [`DirEntryExt`] trait, which"] # [doc = " provides efficient access to the inode number of the directory entry."] # [doc = ""] # [doc = " # Differences with `std::fs::DirEntry`"] # [doc = ""] # [doc = " This type mostly mirrors the type by the same name in [`std::fs`]. There"] # [doc = " are some differences however:"] # [doc = ""] # [doc = " * All recursive directory iterators must inspect the entry's type."] # [doc = " Therefore, the value is stored and its access is guaranteed to be cheap and"] # [doc = " successful."] # [doc = " * [`path`] and [`file_name`] return borrowed variants."] # [doc = " * If [`follow_links`] was enabled on the originating iterator, then all"] # [doc = " operations except for [`path`] operate on the link target. Otherwise, all"] # [doc = " operations operate on the symbolic link."] # [doc = ""] # [doc = " [`std::fs`]: https://doc.rust-lang.org/stable/std/fs/index.html"] # [doc = " [`path`]: #method.path"] # [doc = " [`file_name`]: #method.file_name"] # [doc = " [`follow_links`]: struct.WalkDir.html#method.follow_links"] # [doc = " [`DirEntryExt`]: trait.DirEntryExt.html"] pub struct DirEntry { # [doc = " The path as reported by the [`fs::ReadDir`] iterator (even if it's a"] # [doc = " symbolic link)."] # [doc = ""] # [doc = " [`fs::ReadDir`]: https://doc.rust-lang.org/stable/std/fs/struct.ReadDir.html"] path : PathBuf , # [doc = " The file type. Necessary for recursive iteration, so store it."] ty : FileType , # [doc = " Is set when this entry was created from a symbolic link and the user"] # [doc = " expects the iterator to follow symbolic links."] follow_link : bool , # [doc = " The depth at which this entry was generated relative to the root."] depth : usize , # [doc = " The underlying inode number (Unix only)."] # [cfg (unix)] ino : u64 , # [doc = " The underlying metadata (Windows only). We store this on Windows"] # [doc = " because this comes for free while reading a directory."] # [doc = ""] # [doc = " We use this to determine whether an entry is a directory or not, which"] # [doc = " works around a bug in Rust's standard library:"] # [doc = " https://github.com/rust-lang/rust/issues/46484"] # [cfg (windows)] metadata : fs :: Metadata , }
};
}
