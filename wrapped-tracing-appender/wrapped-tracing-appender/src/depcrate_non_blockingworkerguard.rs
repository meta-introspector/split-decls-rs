// Generated macro for WorkerGuard (struct)
macro_rules! Depcrate_non_blockingWorkerGuard {
() => {
// Module: crate::non_blocking
// Provides: {"WorkerGuard"}
// Dependencies: {}
# [doc = " A guard that flushes spans/events associated to a [`NonBlocking`] on a drop"] # [doc = ""] # [doc = " Writing to a [`NonBlocking`] writer will **not** immediately write a span or event to the underlying"] # [doc = " output. Instead, the span or event will be written by a dedicated logging thread at some later point."] # [doc = " To increase throughput, the non-blocking writer will flush to the underlying output on"] # [doc = " a periodic basis rather than every time a span or event is written. This means that if the program"] # [doc = " terminates abruptly (such as through an uncaught `panic` or a `std::process::exit`), some spans"] # [doc = " or events may not be written."] # [doc = ""] # [doc = " Since spans/events and events recorded near a crash are often necessary for diagnosing the failure,"] # [doc = " `WorkerGuard` provides a mechanism to ensure that _all_ buffered logs are flushed to their output."] # [doc = " `WorkerGuard` should be assigned in the `main` function or whatever the entrypoint of the program is."] # [doc = " This will ensure that the guard will be dropped during an unwinding or when `main` exits"] # [doc = " successfully."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ``` rust"] # [doc = " # #[clippy::allow(needless_doctest_main)]"] # [doc = " fn main () {"] # [doc = " # fn doc() {"] # [doc = "     let (non_blocking, _guard) = tracing_appender::non_blocking(std::io::stdout());"] # [doc = "     let subscriber = tracing_subscriber::fmt().with_writer(non_blocking);"] # [doc = "     tracing::subscriber::with_default(subscriber.finish(), || {"] # [doc = "         // Emit some tracing events within context of the non_blocking `_guard` and tracing subscriber"] # [doc = "         tracing::event!(tracing::Level::INFO, \"Hello\");"] # [doc = "     });"] # [doc = "     // Exiting the context of `main` will drop the `_guard` and any remaining logs should get flushed"] # [doc = " # }"] # [doc = " }"] # [doc = " ```"] # [must_use] # [derive (Debug)] pub struct WorkerGuard { _guard : Option < JoinHandle < () > > , sender : Sender < Msg > , shutdown : Sender < () > , }
};
}
