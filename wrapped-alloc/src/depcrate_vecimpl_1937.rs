// Generated macro for impl_1937 (impl)
macro_rules! Depcrate_vecimpl_1937 {
() => {
// Module: crate::vec
// Provides: {"impl_1937"}
// Dependencies: {}
impl < T : Clone , A : Allocator > Vec < T , A > { # [doc = " Resizes the `Vec` in-place so that `len` is equal to `new_len`."] # [doc = ""] # [doc = " If `new_len` is greater than `len`, the `Vec` is extended by the"] # [doc = " difference, with each additional slot filled with `value`."] # [doc = " If `new_len` is less than `len`, the `Vec` is simply truncated."] # [doc = ""] # [doc = " This method requires `T` to implement [`Clone`],"] # [doc = " in order to be able to clone the passed value."] # [doc = " If you need more flexibility (or want to rely on [`Default`] instead of"] # [doc = " [`Clone`]), use [`Vec::resize_with`]."] # [doc = " If you only need to resize to a smaller size, use [`Vec::truncate`]."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` _bytes_."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut vec = vec![\"hello\"];"] # [doc = " vec.resize(3, \"world\");"] # [doc = " assert_eq!(vec, [\"hello\", \"world\", \"world\"]);"] # [doc = ""] # [doc = " let mut vec = vec!['a', 'b', 'c', 'd'];"] # [doc = " vec.resize(2, '_');"] # [doc = " assert_eq!(vec, ['a', 'b']);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [stable (feature = "vec_resize" , since = "1.5.0")] # [track_caller] pub fn resize (& mut self , new_len : usize , value : T) { let len = self . len () ; if new_len > len { self . extend_with (new_len - len , value) } else { self . truncate (new_len) ; } } # [doc = " Clones and appends all elements in a slice to the `Vec`."] # [doc = ""] # [doc = " Iterates over the slice `other`, clones each element, and then appends"] # [doc = " it to this `Vec`. The `other` slice is traversed in-order."] # [doc = ""] # [doc = " Note that this function is the same as [`extend`],"] # [doc = " except that it also works with slice elements that are Clone but not Copy."] # [doc = " If Rust gets specialization this function may be deprecated."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut vec = vec![1];"] # [doc = " vec.extend_from_slice(&[2, 3, 4]);"] # [doc = " assert_eq!(vec, [1, 2, 3, 4]);"] # [doc = " ```"] # [doc = ""] # [doc = " [`extend`]: Vec::extend"] # [cfg (not (no_global_oom_handling))] # [stable (feature = "vec_extend_from_slice" , since = "1.6.0")] # [track_caller] pub fn extend_from_slice (& mut self , other : & [T]) { self . spec_extend (other . iter ()) } # [doc = " Given a range `src`, clones a slice of elements in that range and appends it to the end."] # [doc = ""] # [doc = " `src` must be a range that can form a valid subslice of the `Vec`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if starting index is greater than the end index"] # [doc = " or if the index is greater than the length of the vector."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut characters = vec!['a', 'b', 'c', 'd', 'e'];"] # [doc = " characters.extend_from_within(2..);"] # [doc = " assert_eq!(characters, ['a', 'b', 'c', 'd', 'e', 'c', 'd', 'e']);"] # [doc = ""] # [doc = " let mut numbers = vec![0, 1, 2, 3, 4];"] # [doc = " numbers.extend_from_within(..2);"] # [doc = " assert_eq!(numbers, [0, 1, 2, 3, 4, 0, 1]);"] # [doc = ""] # [doc = " let mut strings = vec![String::from(\"hello\"), String::from(\"world\"), String::from(\"!\")];"] # [doc = " strings.extend_from_within(1..=2);"] # [doc = " assert_eq!(strings, [\"hello\", \"world\", \"!\", \"world\", \"!\"]);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [stable (feature = "vec_extend_from_within" , since = "1.53.0")] # [track_caller] pub fn extend_from_within < R > (& mut self , src : R) where R : RangeBounds < usize > , { let range = slice :: range (src , .. self . len ()) ; self . reserve (range . len ()) ; unsafe { self . spec_extend_from_within (range) ; } } }
};
}
