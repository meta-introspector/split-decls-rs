// Generated macro for impl_2058 (impl)
macro_rules! Depcrate_wtf8impl_2058 {
() => {
// Module: crate::wtf8
// Provides: {"impl_2058"}
// Dependencies: {}
# [cfg_attr (test , allow (dead_code))] impl Wtf8Buf { # [doc = " Creates a new, empty WTF-8 string."] # [inline] pub fn new () -> Wtf8Buf { Wtf8Buf { bytes : Vec :: new () , is_known_utf8 : true } } # [doc = " Creates a new, empty WTF-8 string with pre-allocated capacity for `capacity` bytes."] # [inline] pub fn with_capacity (capacity : usize) -> Wtf8Buf { Wtf8Buf { bytes : Vec :: with_capacity (capacity) , is_known_utf8 : true } } # [doc = " Creates a WTF-8 string from a WTF-8 byte vec."] # [doc = ""] # [doc = " Since the byte vec is not checked for valid WTF-8, this function is"] # [doc = " marked unsafe."] # [inline] pub unsafe fn from_bytes_unchecked (value : Vec < u8 >) -> Wtf8Buf { Wtf8Buf { bytes : value , is_known_utf8 : false } } # [doc = " Creates a WTF-8 string from a UTF-8 `String`."] # [doc = ""] # [doc = " This takes ownership of the `String` and does not copy."] # [doc = ""] # [doc = " Since WTF-8 is a superset of UTF-8, this always succeeds."] # [inline] pub const fn from_string (string : String) -> Wtf8Buf { Wtf8Buf { bytes : string . into_bytes () , is_known_utf8 : true } } # [doc = " Creates a WTF-8 string from a UTF-8 `&str` slice."] # [doc = ""] # [doc = " This copies the content of the slice."] # [doc = ""] # [doc = " Since WTF-8 is a superset of UTF-8, this always succeeds."] # [inline] pub fn from_str (s : & str) -> Wtf8Buf { Wtf8Buf { bytes : s . as_bytes () . to_vec () , is_known_utf8 : true } } pub fn clear (& mut self) { self . bytes . clear () ; self . is_known_utf8 = true ; } # [doc = " Creates a WTF-8 string from a potentially ill-formed UTF-16 slice of 16-bit code units."] # [doc = ""] # [doc = " This is lossless: calling `.encode_wide()` on the resulting string"] # [doc = " will always return the original code units."] pub fn from_wide (v : & [u16]) -> Wtf8Buf { let mut string = Wtf8Buf :: with_capacity (v . len ()) ; for item in char :: decode_utf16 (v . iter () . cloned ()) { match item { Ok (ch) => string . push_char (ch) , Err (surrogate) => { let surrogate = surrogate . unpaired_surrogate () ; let code_point = unsafe { CodePoint :: from_u32_unchecked (surrogate as u32) } ; string . is_known_utf8 = false ; unsafe { string . push_code_point_unchecked (code_point) ; } } } } string } # [doc = " Appends the given `char` to the end of this string."] # [doc = " This does **not** include the WTF-8 concatenation check or `is_known_utf8` check."] # [doc = " Copied from String::push."] unsafe fn push_code_point_unchecked (& mut self , code_point : CodePoint) { let mut bytes = [0 ; MAX_LEN_UTF8] ; let bytes = encode_utf8_raw (code_point . to_u32 () , & mut bytes) ; self . bytes . extend_from_slice (bytes) } # [inline] pub fn as_slice (& self) -> & Wtf8 { unsafe { Wtf8 :: from_bytes_unchecked (& self . bytes) } } # [inline] pub fn as_mut_slice (& mut self) -> & mut Wtf8 { unsafe { Wtf8 :: from_mut_bytes_unchecked (& mut self . bytes) } } # [doc = " Converts the string to UTF-8 without validation, if it was created from"] # [doc = " valid UTF-8."] # [inline] fn as_known_utf8 (& self) -> Option < & str > { if self . is_known_utf8 { Some (unsafe { str :: from_utf8_unchecked (self . as_bytes ()) }) } else { None } } # [doc = " Reserves capacity for at least `additional` more bytes to be inserted"] # [doc = " in the given `Wtf8Buf`."] # [doc = " The collection may reserve more space to avoid frequent reallocations."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` bytes."] # [inline] pub fn reserve (& mut self , additional : usize) { self . bytes . reserve (additional) } # [doc = " Tries to reserve capacity for at least `additional` more bytes to be"] # [doc = " inserted in the given `Wtf8Buf`. The `Wtf8Buf` may reserve more space to"] # [doc = " avoid frequent reallocations. After calling `try_reserve`, capacity will"] # [doc = " be greater than or equal to `self.len() + additional`. Does nothing if"] # [doc = " capacity is already sufficient. This method preserves the contents even"] # [doc = " if an error occurs."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the capacity overflows, or the allocator reports a failure, then an error"] # [doc = " is returned."] # [inline] pub fn try_reserve (& mut self , additional : usize) -> Result < () , TryReserveError > { self . bytes . try_reserve (additional) } # [inline] pub fn reserve_exact (& mut self , additional : usize) { self . bytes . reserve_exact (additional) } # [doc = " Tries to reserve the minimum capacity for exactly `additional` more"] # [doc = " bytes to be inserted in the given `Wtf8Buf`. After calling"] # [doc = " `try_reserve_exact`, capacity will be greater than or equal to"] # [doc = " `self.len() + additional` if it returns `Ok(())`."] # [doc = " Does nothing if the capacity is already sufficient."] # [doc = ""] # [doc = " Note that the allocator may give the `Wtf8Buf` more space than it"] # [doc = " requests. Therefore, capacity can not be relied upon to be precisely"] # [doc = " minimal. Prefer [`try_reserve`] if future insertions are expected."] # [doc = ""] # [doc = " [`try_reserve`]: Wtf8Buf::try_reserve"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the capacity overflows, or the allocator reports a failure, then an error"] # [doc = " is returned."] # [inline] pub fn try_reserve_exact (& mut self , additional : usize) -> Result < () , TryReserveError > { self . bytes . try_reserve_exact (additional) } # [inline] pub fn shrink_to_fit (& mut self) { self . bytes . shrink_to_fit () } # [inline] pub fn shrink_to (& mut self , min_capacity : usize) { self . bytes . shrink_to (min_capacity) } # [inline] pub fn leak < 'a > (self) -> & 'a mut Wtf8 { unsafe { Wtf8 :: from_mut_bytes_unchecked (self . bytes . leak ()) } } # [doc = " Returns the number of bytes that this string buffer can hold without reallocating."] # [inline] pub fn capacity (& self) -> usize { self . bytes . capacity () } # [doc = " Append a UTF-8 slice at the end of the string."] # [inline] pub fn push_str (& mut self , other : & str) { self . bytes . extend_from_slice (other . as_bytes ()) } # [doc = " Append a WTF-8 slice at the end of the string."] # [doc = ""] # [doc = " This replaces newly paired surrogates at the boundary"] # [doc = " with a supplementary code point,"] # [doc = " like concatenating ill-formed UTF-16 strings effectively would."] # [inline] pub fn push_wtf8 (& mut self , other : & Wtf8) { match ((& * self) . final_lead_surrogate () , other . initial_trail_surrogate ()) { (Some (lead) , Some (trail)) => { let len_without_lead_surrogate = self . len () - 3 ; self . bytes . truncate (len_without_lead_surrogate) ; let other_without_trail_surrogate = & other . as_bytes () [3 ..] ; self . bytes . reserve (4 + other_without_trail_surrogate . len ()) ; self . push_char (decode_surrogate_pair (lead , trail)) ; self . bytes . extend_from_slice (other_without_trail_surrogate) ; } _ => { if self . is_known_utf8 && other . next_surrogate (0) . is_some () { self . is_known_utf8 = false ; } self . bytes . extend_from_slice (other . as_bytes ()) ; } } } # [doc = " Append a Unicode scalar value at the end of the string."] # [inline] pub fn push_char (& mut self , c : char) { unsafe { self . push_code_point_unchecked (CodePoint :: from_char (c)) } } # [doc = " Append a code point at the end of the string."] # [doc = ""] # [doc = " This replaces newly paired surrogates at the boundary"] # [doc = " with a supplementary code point,"] # [doc = " like concatenating ill-formed UTF-16 strings effectively would."] # [inline] pub fn push (& mut self , code_point : CodePoint) { if let Some (trail) = code_point . to_trail_surrogate () { if let Some (lead) = (& * self) . final_lead_surrogate () { let len_without_lead_surrogate = self . len () - 3 ; self . bytes . truncate (len_without_lead_surrogate) ; self . push_char (decode_surrogate_pair (lead , trail)) ; return ; } self . is_known_utf8 = false ; } else if code_point . to_lead_surrogate () . is_some () { self . is_known_utf8 = false ; } unsafe { self . push_code_point_unchecked (code_point) } } # [doc = " Shortens a string to the specified length."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `new_len` > current length,"] # [doc = " or if `new_len` is not a code point boundary."] # [inline] pub fn truncate (& mut self , new_len : usize) { assert ! (self . is_code_point_boundary (new_len)) ; self . bytes . truncate (new_len) } # [doc = " Consumes the WTF-8 string and tries to convert it to a vec of bytes."] # [inline] pub fn into_bytes (self) -> Vec < u8 > { self . bytes } # [doc = " Consumes the WTF-8 string and tries to convert it to UTF-8."] # [doc = ""] # [doc = " This does not copy the data."] # [doc = ""] # [doc = " If the contents are not well-formed UTF-8"] # [doc = " (that is, if the string contains surrogates),"] # [doc = " the original WTF-8 string is returned instead."] pub fn into_string (self) -> Result < String , Wtf8Buf > { if self . is_known_utf8 || self . next_surrogate (0) . is_none () { Ok (unsafe { String :: from_utf8_unchecked (self . bytes) }) } else { Err (self) } } # [doc = " Consumes the WTF-8 string and converts it lossily to UTF-8."] # [doc = ""] # [doc = " This does not copy the data (but may overwrite parts of it in place)."] # [doc = ""] # [doc = " Surrogates are replaced with `\"\\u{FFFD}\"` (the replacement character “�”)"] pub fn into_string_lossy (mut self) -> String { if ! self . is_known_utf8 { let mut pos = 0 ; while let Some ((surrogate_pos , _)) = self . next_surrogate (pos) { pos = surrogate_pos + 3 ; self . bytes [surrogate_pos .. pos] . copy_from_slice ("\u{FFFD}" . as_bytes ()) ; } } unsafe { String :: from_utf8_unchecked (self . bytes) } } # [doc = " Converts this `Wtf8Buf` into a boxed `Wtf8`."] # [inline] pub fn into_box (self) -> Box < Wtf8 > { unsafe { mem :: transmute (self . bytes . into_boxed_slice ()) } } # [doc = " Converts a `Box<Wtf8>` into a `Wtf8Buf`."] pub fn from_box (boxed : Box < Wtf8 >) -> Wtf8Buf { let bytes : Box < [u8] > = unsafe { mem :: transmute (boxed) } ; Wtf8Buf { bytes : bytes . into_vec () , is_known_utf8 : false } } # [doc = " Provides plumbing to core `Vec::extend_from_slice`."] # [doc = " More well behaving alternative to allowing outer types"] # [doc = " full mutable access to the core `Vec`."] # [inline] pub unsafe fn extend_from_slice_unchecked (& mut self , other : & [u8]) { self . bytes . extend_from_slice (other) ; self . is_known_utf8 = false ; } }
};
}
