// Generated macro for impl_521 (impl)
macro_rules! Depcrate_collections_btree_merge_iterimpl_521 {
() => {
// Module: crate::collections::btree::merge_iter
// Provides: {"impl_521"}
// Dependencies: {}
impl < I : Iterator > MergeIterInner < I > { # [doc = " Creates a new core for an iterator merging a pair of sources."] pub (super) fn new (a : I , b : I) -> Self { MergeIterInner { a , b , peeked : None } } # [doc = " Returns the next pair of items stemming from the pair of sources"] # [doc = " being merged. If both returned options contain a value, that value"] # [doc = " is equal and occurs in both sources. If one of the returned options"] # [doc = " contains a value, that value doesn't occur in the other source (or"] # [doc = " the sources are not strictly ascending). If neither returned option"] # [doc = " contains a value, iteration has finished and subsequent calls will"] # [doc = " return the same empty pair."] pub (super) fn nexts < Cmp : Fn (& I :: Item , & I :: Item) -> Ordering > (& mut self , cmp : Cmp ,) -> (Option < I :: Item > , Option < I :: Item >) where I : FusedIterator , { let mut a_next ; let mut b_next ; match self . peeked . take () { Some (Peeked :: A (next)) => { a_next = Some (next) ; b_next = self . b . next () ; } Some (Peeked :: B (next)) => { b_next = Some (next) ; a_next = self . a . next () ; } None => { a_next = self . a . next () ; b_next = self . b . next () ; } } if let (Some (a1) , Some (b1)) = (& a_next , & b_next) { match cmp (a1 , b1) { Ordering :: Less => self . peeked = b_next . take () . map (Peeked :: B) , Ordering :: Greater => self . peeked = a_next . take () . map (Peeked :: A) , Ordering :: Equal => () , } } (a_next , b_next) } # [doc = " Returns a pair of upper bounds for the `size_hint` of the final iterator."] pub (super) fn lens (& self) -> (usize , usize) where I : ExactSizeIterator , { match self . peeked { Some (Peeked :: A (_)) => (1 + self . a . len () , self . b . len ()) , Some (Peeked :: B (_)) => (self . a . len () , 1 + self . b . len ()) , _ => (self . a . len () , self . b . len ()) , } } }
};
}
