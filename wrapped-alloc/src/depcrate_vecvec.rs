// Generated macro for Vec (struct)
macro_rules! Depcrate_vecVec {
() => {
// Module: crate::vec
// Provides: {"Vec"}
// Dependencies: {}
# [doc = " A contiguous growable array type, written as `Vec<T>`, short for 'vector'."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut vec = Vec::new();"] # [doc = " vec.push(1);"] # [doc = " vec.push(2);"] # [doc = ""] # [doc = " assert_eq!(vec.len(), 2);"] # [doc = " assert_eq!(vec[0], 1);"] # [doc = ""] # [doc = " assert_eq!(vec.pop(), Some(2));"] # [doc = " assert_eq!(vec.len(), 1);"] # [doc = ""] # [doc = " vec[0] = 7;"] # [doc = " assert_eq!(vec[0], 7);"] # [doc = ""] # [doc = " vec.extend([1, 2, 3]);"] # [doc = ""] # [doc = " for x in &vec {"] # [doc = "     println!(\"{x}\");"] # [doc = " }"] # [doc = " assert_eq!(vec, [7, 1, 2, 3]);"] # [doc = " ```"] # [doc = ""] # [doc = " The [`vec!`] macro is provided for convenient initialization:"] # [doc = ""] # [doc = " ```"] # [doc = " let mut vec1 = vec![1, 2, 3];"] # [doc = " vec1.push(4);"] # [doc = " let vec2 = Vec::from([1, 2, 3, 4]);"] # [doc = " assert_eq!(vec1, vec2);"] # [doc = " ```"] # [doc = ""] # [doc = " It can also initialize each element of a `Vec<T>` with a given value."] # [doc = " This may be more efficient than performing allocation and initialization"] # [doc = " in separate steps, especially when initializing a vector of zeros:"] # [doc = ""] # [doc = " ```"] # [doc = " let vec = vec![0; 5];"] # [doc = " assert_eq!(vec, [0, 0, 0, 0, 0]);"] # [doc = ""] # [doc = " // The following is equivalent, but potentially slower:"] # [doc = " let mut vec = Vec::with_capacity(5);"] # [doc = " vec.resize(5, 0);"] # [doc = " assert_eq!(vec, [0, 0, 0, 0, 0]);"] # [doc = " ```"] # [doc = ""] # [doc = " For more information, see"] # [doc = " [Capacity and Reallocation](#capacity-and-reallocation)."] # [doc = ""] # [doc = " Use a `Vec<T>` as an efficient stack:"] # [doc = ""] # [doc = " ```"] # [doc = " let mut stack = Vec::new();"] # [doc = ""] # [doc = " stack.push(1);"] # [doc = " stack.push(2);"] # [doc = " stack.push(3);"] # [doc = ""] # [doc = " while let Some(top) = stack.pop() {"] # [doc = "     // Prints 3, 2, 1"] # [doc = "     println!(\"{top}\");"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " # Indexing"] # [doc = ""] # [doc = " The `Vec` type allows access to values by index, because it implements the"] # [doc = " [`Index`] trait. An example will be more explicit:"] # [doc = ""] # [doc = " ```"] # [doc = " let v = vec![0, 2, 4, 6];"] # [doc = " println!(\"{}\", v[1]); // it will display '2'"] # [doc = " ```"] # [doc = ""] # [doc = " However be careful: if you try to access an index which isn't in the `Vec`,"] # [doc = " your software will panic! You cannot do this:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = " let v = vec![0, 2, 4, 6];"] # [doc = " println!(\"{}\", v[6]); // it will panic!"] # [doc = " ```"] # [doc = ""] # [doc = " Use [`get`] and [`get_mut`] if you want to check whether the index is in"] # [doc = " the `Vec`."] # [doc = ""] # [doc = " # Slicing"] # [doc = ""] # [doc = " A `Vec` can be mutable. On the other hand, slices are read-only objects."] # [doc = " To get a [slice][prim@slice], use [`&`]. Example:"] # [doc = ""] # [doc = " ```"] # [doc = " fn read_slice(slice: &[usize]) {"] # [doc = "     // ..."] # [doc = " }"] # [doc = ""] # [doc = " let v = vec![0, 1];"] # [doc = " read_slice(&v);"] # [doc = ""] # [doc = " // ... and that's all!"] # [doc = " // you can also do it like this:"] # [doc = " let u: &[usize] = &v;"] # [doc = " // or like this:"] # [doc = " let u: &[_] = &v;"] # [doc = " ```"] # [doc = ""] # [doc = " In Rust, it's more common to pass slices as arguments rather than vectors"] # [doc = " when you just want to provide read access. The same goes for [`String`] and"] # [doc = " [`&str`]."] # [doc = ""] # [doc = " # Capacity and reallocation"] # [doc = ""] # [doc = " The capacity of a vector is the amount of space allocated for any future"] # [doc = " elements that will be added onto the vector. This is not to be confused with"] # [doc = " the *length* of a vector, which specifies the number of actual elements"] # [doc = " within the vector. If a vector's length exceeds its capacity, its capacity"] # [doc = " will automatically be increased, but its elements will have to be"] # [doc = " reallocated."] # [doc = ""] # [doc = " For example, a vector with capacity 10 and length 0 would be an empty vector"] # [doc = " with space for 10 more elements. Pushing 10 or fewer elements onto the"] # [doc = " vector will not change its capacity or cause reallocation to occur. However,"] # [doc = " if the vector's length is increased to 11, it will have to reallocate, which"] # [doc = " can be slow. For this reason, it is recommended to use [`Vec::with_capacity`]"] # [doc = " whenever possible to specify how big the vector is expected to get."] # [doc = ""] # [doc = " # Guarantees"] # [doc = ""] # [doc = " Due to its incredibly fundamental nature, `Vec` makes a lot of guarantees"] # [doc = " about its design. This ensures that it's as low-overhead as possible in"] # [doc = " the general case, and can be correctly manipulated in primitive ways"] # [doc = " by unsafe code. Note that these guarantees refer to an unqualified `Vec<T>`."] # [doc = " If additional type parameters are added (e.g., to support custom allocators),"] # [doc = " overriding their defaults may change the behavior."] # [doc = ""] # [doc = " Most fundamentally, `Vec` is and always will be a (pointer, capacity, length)"] # [doc = " triplet. No more, no less. The order of these fields is completely"] # [doc = " unspecified, and you should use the appropriate methods to modify these."] # [doc = " The pointer will never be null, so this type is null-pointer-optimized."] # [doc = ""] # [doc = " However, the pointer might not actually point to allocated memory. In particular,"] # [doc = " if you construct a `Vec` with capacity 0 via [`Vec::new`], [`vec![]`][`vec!`],"] # [doc = " [`Vec::with_capacity(0)`][`Vec::with_capacity`], or by calling [`shrink_to_fit`]"] # [doc = " on an empty Vec, it will not allocate memory. Similarly, if you store zero-sized"] # [doc = " types inside a `Vec`, it will not allocate space for them. *Note that in this case"] # [doc = " the `Vec` might not report a [`capacity`] of 0*. `Vec` will allocate if and only"] # [doc = " if <code>[size_of::\\<T>]\\() * [capacity]\\() > 0</code>. In general, `Vec`'s allocation"] # [doc = " details are very subtle --- if you intend to allocate memory using a `Vec`"] # [doc = " and use it for something else (either to pass to unsafe code, or to build your"] # [doc = " own memory-backed collection), be sure to deallocate this memory by using"] # [doc = " `from_raw_parts` to recover the `Vec` and then dropping it."] # [doc = ""] # [doc = " If a `Vec` *has* allocated memory, then the memory it points to is on the heap"] # [doc = " (as defined by the allocator Rust is configured to use by default), and its"] # [doc = " pointer points to [`len`] initialized, contiguous elements in order (what"] # [doc = " you would see if you coerced it to a slice), followed by <code>[capacity] - [len]</code>"] # [doc = " logically uninitialized, contiguous elements."] # [doc = ""] # [doc = " A vector containing the elements `'a'` and `'b'` with capacity 4 can be"] # [doc = " visualized as below. The top part is the `Vec` struct, it contains a"] # [doc = " pointer to the head of the allocation in the heap, length and capacity."] # [doc = " The bottom part is the allocation on the heap, a contiguous memory block."] # [doc = ""] # [doc = " ```text"] # [doc = "             ptr      len  capacity"] # [doc = "        +--------+--------+--------+"] # [doc = "        | 0x0123 |      2 |      4 |"] # [doc = "        +--------+--------+--------+"] # [doc = "             |"] # [doc = "             v"] # [doc = " Heap   +--------+--------+--------+--------+"] # [doc = "        |    'a' |    'b' | uninit | uninit |"] # [doc = "        +--------+--------+--------+--------+"] # [doc = " ```"] # [doc = ""] # [doc = " - **uninit** represents memory that is not initialized, see [`MaybeUninit`]."] # [doc = " - Note: the ABI is not stable and `Vec` makes no guarantees about its memory"] # [doc = "   layout (including the order of fields)."] # [doc = ""] # [doc = " `Vec` will never perform a \"small optimization\" where elements are actually"] # [doc = " stored on the stack for two reasons:"] # [doc = ""] # [doc = " * It would make it more difficult for unsafe code to correctly manipulate"] # [doc = "   a `Vec`. The contents of a `Vec` wouldn't have a stable address if it were"] # [doc = "   only moved, and it would be more difficult to determine if a `Vec` had"] # [doc = "   actually allocated memory."] # [doc = ""] # [doc = " * It would penalize the general case, incurring an additional branch"] # [doc = "   on every access."] # [doc = ""] # [doc = " `Vec` will never automatically shrink itself, even if completely empty. This"] # [doc = " ensures no unnecessary allocations or deallocations occur. Emptying a `Vec`"] # [doc = " and then filling it back up to the same [`len`] should incur no calls to"] # [doc = " the allocator. If you wish to free up unused memory, use"] # [doc = " [`shrink_to_fit`] or [`shrink_to`]."] # [doc = ""] # [doc = " [`push`] and [`insert`] will never (re)allocate if the reported capacity is"] # [doc = " sufficient. [`push`] and [`insert`] *will* (re)allocate if"] # [doc = " <code>[len] == [capacity]</code>. That is, the reported capacity is completely"] # [doc = " accurate, and can be relied on. It can even be used to manually free the memory"] # [doc = " allocated by a `Vec` if desired. Bulk insertion methods *may* reallocate, even"] # [doc = " when not necessary."] # [doc = ""] # [doc = " `Vec` does not guarantee any particular growth strategy when reallocating"] # [doc = " when full, nor when [`reserve`] is called. The current strategy is basic"] # [doc = " and it may prove desirable to use a non-constant growth factor. Whatever"] # [doc = " strategy is used will of course guarantee *O*(1) amortized [`push`]."] # [doc = ""] # [doc = " It is guaranteed, in order to respect the intentions of the programmer, that"] # [doc = " all of `vec![e_1, e_2, ..., e_n]`, `vec![x; n]`, and [`Vec::with_capacity(n)`] produce a `Vec`"] # [doc = " that requests an allocation of the exact size needed for precisely `n` elements from the allocator,"] # [doc = " and no other size (such as, for example: a size rounded up to the nearest power of 2)."] # [doc = " The allocator will return an allocation that is at least as large as requested, but it may be larger."] # [doc = ""] # [doc = " It is guaranteed that the [`Vec::capacity`] method returns a value that is at least the requested capacity"] # [doc = " and not more than the allocated capacity."] # [doc = ""] # [doc = " The method [`Vec::shrink_to_fit`] will attempt to discard excess capacity an allocator has given to a `Vec`."] # [doc = " If <code>[len] == [capacity]</code>, then a `Vec<T>` can be converted"] # [doc = " to and from a [`Box<[T]>`][owned slice] without reallocating or moving the elements."] # [doc = " `Vec` exploits this fact as much as reasonable when implementing common conversions"] # [doc = " such as [`into_boxed_slice`]."] # [doc = ""] # [doc = " `Vec` will not specifically overwrite any data that is removed from it,"] # [doc = " but also won't specifically preserve it. Its uninitialized memory is"] # [doc = " scratch space that it may use however it wants. It will generally just do"] # [doc = " whatever is most efficient or otherwise easy to implement. Do not rely on"] # [doc = " removed data to be erased for security purposes. Even if you drop a `Vec`, its"] # [doc = " buffer may simply be reused by another allocation. Even if you zero a `Vec`'s memory"] # [doc = " first, that might not actually happen because the optimizer does not consider"] # [doc = " this a side-effect that must be preserved. There is one case which we will"] # [doc = " not break, however: using `unsafe` code to write to the excess capacity,"] # [doc = " and then increasing the length to match, is always valid."] # [doc = ""] # [doc = " Currently, `Vec` does not guarantee the order in which elements are dropped."] # [doc = " The order has changed in the past and may change again."] # [doc = ""] # [doc = " [`get`]: slice::get"] # [doc = " [`get_mut`]: slice::get_mut"] # [doc = " [`String`]: crate::string::String"] # [doc = " [`&str`]: type@str"] # [doc = " [`shrink_to_fit`]: Vec::shrink_to_fit"] # [doc = " [`shrink_to`]: Vec::shrink_to"] # [doc = " [capacity]: Vec::capacity"] # [doc = " [`capacity`]: Vec::capacity"] # [doc = " [`Vec::capacity`]: Vec::capacity"] # [doc = " [size_of::\\<T>]: size_of"] # [doc = " [len]: Vec::len"] # [doc = " [`len`]: Vec::len"] # [doc = " [`push`]: Vec::push"] # [doc = " [`insert`]: Vec::insert"] # [doc = " [`reserve`]: Vec::reserve"] # [doc = " [`Vec::with_capacity(n)`]: Vec::with_capacity"] # [doc = " [`MaybeUninit`]: core::mem::MaybeUninit"] # [doc = " [owned slice]: Box"] # [doc = " [`into_boxed_slice`]: Vec::into_boxed_slice"] # [stable (feature = "rust1" , since = "1.0.0")] # [rustc_diagnostic_item = "Vec"] # [rustc_insignificant_dtor] pub struct Vec < T , # [unstable (feature = "allocator_api" , issue = "32838")] A : Allocator = Global > { buf : RawVec < T , A > , len : usize , }
};
}
