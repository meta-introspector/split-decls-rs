// Generated macro for impl_1352 (impl)
macro_rules! Depcrate_sliceimpl_1352 {
() => {
// Module: crate::slice
// Provides: {"impl_1352"}
// Dependencies: {}
impl < T > [T] { # [doc = " Sorts the slice in ascending order, preserving initial order of equal elements."] # [doc = ""] # [doc = " This sort is stable (i.e., does not reorder equal elements) and *O*(*n* \\* log(*n*))"] # [doc = " worst-case."] # [doc = ""] # [doc = " If the implementation of [`Ord`] for `T` does not implement a [total order], the function"] # [doc = " may panic; even if the function exits normally, the resulting order of elements in the slice"] # [doc = " is unspecified. See also the note on panicking below."] # [doc = ""] # [doc = " When applicable, unstable sorting is preferred because it is generally faster than stable"] # [doc = " sorting and it doesn't allocate auxiliary memory. See"] # [doc = " [`sort_unstable`](slice::sort_unstable). The exception are partially sorted slices, which"] # [doc = " may be better served with `slice::sort`."] # [doc = ""] # [doc = " Sorting types that only implement [`PartialOrd`] such as [`f32`] and [`f64`] require"] # [doc = " additional precautions. For example, `f32::NAN != f32::NAN`, which doesn't fulfill the"] # [doc = " reflexivity requirement of [`Ord`]. By using an alternative comparison function with"] # [doc = " `slice::sort_by` such as [`f32::total_cmp`] or [`f64::total_cmp`] that defines a [total"] # [doc = " order] users can sort slices containing floating-point values. Alternatively, if all values"] # [doc = " in the slice are guaranteed to be in a subset for which [`PartialOrd::partial_cmp`] forms a"] # [doc = " [total order], it's possible to sort the slice with `sort_by(|a, b|"] # [doc = " a.partial_cmp(b).unwrap())`."] # [doc = ""] # [doc = " # Current implementation"] # [doc = ""] # [doc = " The current implementation is based on [driftsort] by Orson Peters and Lukas Bergdoll, which"] # [doc = " combines the fast average case of quicksort with the fast worst case and partial run"] # [doc = " detection of mergesort, achieving linear time on fully sorted and reversed inputs. On inputs"] # [doc = " with k distinct elements, the expected time to sort the data is *O*(*n* \\* log(*k*))."] # [doc = ""] # [doc = " The auxiliary memory allocation behavior depends on the input length. Short slices are"] # [doc = " handled without allocation, medium sized slices allocate `self.len()` and beyond that it"] # [doc = " clamps at `self.len() / 2`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " May panic if the implementation of [`Ord`] for `T` does not implement a [total order], or if"] # [doc = " the [`Ord`] implementation itself panics."] # [doc = ""] # [doc = " All safe functions on slices preserve the invariant that even if the function panics, all"] # [doc = " original elements will remain in the slice and any possible modifications via interior"] # [doc = " mutability are observed in the input. This ensures that recovery code (for instance inside"] # [doc = " of a `Drop` or following a `catch_unwind`) will still have access to all the original"] # [doc = " elements. For instance, if the slice belongs to a `Vec`, the `Vec::drop` method will be able"] # [doc = " to dispose of all contained elements."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut v = [4, -5, 1, -3, 2];"] # [doc = ""] # [doc = " v.sort();"] # [doc = " assert_eq!(v, [-5, -3, 1, 2, 4]);"] # [doc = " ```"] # [doc = ""] # [doc = " [driftsort]: https://github.com/Voultapher/driftsort"] # [doc = " [total order]: https://en.wikipedia.org/wiki/Total_order"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn sort (& mut self) where T : Ord , { stable_sort (self , T :: lt) ; } # [doc = " Sorts the slice in ascending order with a comparison function, preserving initial order of"] # [doc = " equal elements."] # [doc = ""] # [doc = " This sort is stable (i.e., does not reorder equal elements) and *O*(*n* \\* log(*n*))"] # [doc = " worst-case."] # [doc = ""] # [doc = " If the comparison function `compare` does not implement a [total order], the function may"] # [doc = " panic; even if the function exits normally, the resulting order of elements in the slice is"] # [doc = " unspecified. See also the note on panicking below."] # [doc = ""] # [doc = " For example `|a, b| (a - b).cmp(a)` is a comparison function that is neither transitive nor"] # [doc = " reflexive nor total, `a < b < c < a` with `a = 1, b = 2, c = 3`. For more information and"] # [doc = " examples see the [`Ord`] documentation."] # [doc = ""] # [doc = " # Current implementation"] # [doc = ""] # [doc = " The current implementation is based on [driftsort] by Orson Peters and Lukas Bergdoll, which"] # [doc = " combines the fast average case of quicksort with the fast worst case and partial run"] # [doc = " detection of mergesort, achieving linear time on fully sorted and reversed inputs. On inputs"] # [doc = " with k distinct elements, the expected time to sort the data is *O*(*n* \\* log(*k*))."] # [doc = ""] # [doc = " The auxiliary memory allocation behavior depends on the input length. Short slices are"] # [doc = " handled without allocation, medium sized slices allocate `self.len()` and beyond that it"] # [doc = " clamps at `self.len() / 2`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " May panic if `compare` does not implement a [total order], or if `compare` itself panics."] # [doc = ""] # [doc = " All safe functions on slices preserve the invariant that even if the function panics, all"] # [doc = " original elements will remain in the slice and any possible modifications via interior"] # [doc = " mutability are observed in the input. This ensures that recovery code (for instance inside"] # [doc = " of a `Drop` or following a `catch_unwind`) will still have access to all the original"] # [doc = " elements. For instance, if the slice belongs to a `Vec`, the `Vec::drop` method will be able"] # [doc = " to dispose of all contained elements."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut v = [4, -5, 1, -3, 2];"] # [doc = " v.sort_by(|a, b| a.cmp(b));"] # [doc = " assert_eq!(v, [-5, -3, 1, 2, 4]);"] # [doc = ""] # [doc = " // reverse sorting"] # [doc = " v.sort_by(|a, b| b.cmp(a));"] # [doc = " assert_eq!(v, [4, 2, 1, -3, -5]);"] # [doc = " ```"] # [doc = ""] # [doc = " [driftsort]: https://github.com/Voultapher/driftsort"] # [doc = " [total order]: https://en.wikipedia.org/wiki/Total_order"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn sort_by < F > (& mut self , mut compare : F) where F : FnMut (& T , & T) -> Ordering , { stable_sort (self , | a , b | compare (a , b) == Less) ; } # [doc = " Sorts the slice in ascending order with a key extraction function, preserving initial order"] # [doc = " of equal elements."] # [doc = ""] # [doc = " This sort is stable (i.e., does not reorder equal elements) and *O*(*m* \\* *n* \\* log(*n*))"] # [doc = " worst-case, where the key function is *O*(*m*)."] # [doc = ""] # [doc = " If the implementation of [`Ord`] for `K` does not implement a [total order], the function"] # [doc = " may panic; even if the function exits normally, the resulting order of elements in the slice"] # [doc = " is unspecified. See also the note on panicking below."] # [doc = ""] # [doc = " # Current implementation"] # [doc = ""] # [doc = " The current implementation is based on [driftsort] by Orson Peters and Lukas Bergdoll, which"] # [doc = " combines the fast average case of quicksort with the fast worst case and partial run"] # [doc = " detection of mergesort, achieving linear time on fully sorted and reversed inputs. On inputs"] # [doc = " with k distinct elements, the expected time to sort the data is *O*(*n* \\* log(*k*))."] # [doc = ""] # [doc = " The auxiliary memory allocation behavior depends on the input length. Short slices are"] # [doc = " handled without allocation, medium sized slices allocate `self.len()` and beyond that it"] # [doc = " clamps at `self.len() / 2`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " May panic if the implementation of [`Ord`] for `K` does not implement a [total order], or if"] # [doc = " the [`Ord`] implementation or the key-function `f` panics."] # [doc = ""] # [doc = " All safe functions on slices preserve the invariant that even if the function panics, all"] # [doc = " original elements will remain in the slice and any possible modifications via interior"] # [doc = " mutability are observed in the input. This ensures that recovery code (for instance inside"] # [doc = " of a `Drop` or following a `catch_unwind`) will still have access to all the original"] # [doc = " elements. For instance, if the slice belongs to a `Vec`, the `Vec::drop` method will be able"] # [doc = " to dispose of all contained elements."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut v = [4i32, -5, 1, -3, 2];"] # [doc = ""] # [doc = " v.sort_by_key(|k| k.abs());"] # [doc = " assert_eq!(v, [1, 2, -3, 4, -5]);"] # [doc = " ```"] # [doc = ""] # [doc = " [driftsort]: https://github.com/Voultapher/driftsort"] # [doc = " [total order]: https://en.wikipedia.org/wiki/Total_order"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [stable (feature = "slice_sort_by_key" , since = "1.7.0")] # [inline] pub fn sort_by_key < K , F > (& mut self , mut f : F) where F : FnMut (& T) -> K , K : Ord , { stable_sort (self , | a , b | f (a) . lt (& f (b))) ; } # [doc = " Sorts the slice in ascending order with a key extraction function, preserving initial order"] # [doc = " of equal elements."] # [doc = ""] # [doc = " This sort is stable (i.e., does not reorder equal elements) and *O*(*m* \\* *n* + *n* \\*"] # [doc = " log(*n*)) worst-case, where the key function is *O*(*m*)."] # [doc = ""] # [doc = " During sorting, the key function is called at most once per element, by using temporary"] # [doc = " storage to remember the results of key evaluation. The order of calls to the key function is"] # [doc = " unspecified and may change in future versions of the standard library."] # [doc = ""] # [doc = " If the implementation of [`Ord`] for `K` does not implement a [total order], the function"] # [doc = " may panic; even if the function exits normally, the resulting order of elements in the slice"] # [doc = " is unspecified. See also the note on panicking below."] # [doc = ""] # [doc = " For simple key functions (e.g., functions that are property accesses or basic operations),"] # [doc = " [`sort_by_key`](slice::sort_by_key) is likely to be faster."] # [doc = ""] # [doc = " # Current implementation"] # [doc = ""] # [doc = " The current implementation is based on [instruction-parallel-network sort][ipnsort] by Lukas"] # [doc = " Bergdoll, which combines the fast average case of randomized quicksort with the fast worst"] # [doc = " case of heapsort, while achieving linear time on fully sorted and reversed inputs. And"] # [doc = " *O*(*k* \\* log(*n*)) where *k* is the number of distinct elements in the input. It leverages"] # [doc = " superscalar out-of-order execution capabilities commonly found in CPUs, to efficiently"] # [doc = " perform the operation."] # [doc = ""] # [doc = " In the worst case, the algorithm allocates temporary storage in a `Vec<(K, usize)>` the"] # [doc = " length of the slice."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " May panic if the implementation of [`Ord`] for `K` does not implement a [total order], or if"] # [doc = " the [`Ord`] implementation panics."] # [doc = ""] # [doc = " All safe functions on slices preserve the invariant that even if the function panics, all"] # [doc = " original elements will remain in the slice and any possible modifications via interior"] # [doc = " mutability are observed in the input. This ensures that recovery code (for instance inside"] # [doc = " of a `Drop` or following a `catch_unwind`) will still have access to all the original"] # [doc = " elements. For instance, if the slice belongs to a `Vec`, the `Vec::drop` method will be able"] # [doc = " to dispose of all contained elements."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut v = [4i32, -5, 1, -3, 2, 10];"] # [doc = ""] # [doc = " // Strings are sorted by lexicographical order."] # [doc = " v.sort_by_cached_key(|k| k.to_string());"] # [doc = " assert_eq!(v, [-3, -5, 1, 10, 2, 4]);"] # [doc = " ```"] # [doc = ""] # [doc = " [ipnsort]: https://github.com/Voultapher/sort-research-rs/tree/main/ipnsort"] # [doc = " [total order]: https://en.wikipedia.org/wiki/Total_order"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [stable (feature = "slice_sort_by_cached_key" , since = "1.34.0")] # [inline] pub fn sort_by_cached_key < K , F > (& mut self , f : F) where F : FnMut (& T) -> K , K : Ord , { macro_rules ! sort_by_key { ($ t : ty , $ slice : ident , $ f : ident) => { { let mut indices : Vec < _ > = $ slice . iter () . map ($ f) . enumerate () . map (| (i , k) | (k , i as $ t)) . collect () ; indices . sort_unstable () ; for i in 0 ..$ slice . len () { let mut index = indices [i] . 1 ; while (index as usize) < i { index = indices [index as usize] . 1 ; } indices [i] . 1 = index ; $ slice . swap (i , index as usize) ; } } } ; } let len = self . len () ; if len < 2 { return ; } let is_using_u32_as_idx_type_helpful = const { size_of :: < (K , u32) > () < size_of :: < (K , usize) > () } ; if is_using_u32_as_idx_type_helpful && len <= (u32 :: MAX as usize) { return sort_by_key ! (u32 , self , f) ; } sort_by_key ! (usize , self , f) } # [doc = " Copies `self` into a new `Vec`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let s = [10, 40, 30];"] # [doc = " let x = s.to_vec();"] # [doc = " // Here, `s` and `x` can be modified independently."] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [rustc_conversion_suggestion] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] pub fn to_vec (& self) -> Vec < T > where T : Clone , { self . to_vec_in (Global) } # [doc = " Copies `self` into a new `Vec` with an allocator."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(allocator_api)]"] # [doc = ""] # [doc = " use std::alloc::System;"] # [doc = ""] # [doc = " let s = [10, 40, 30];"] # [doc = " let x = s.to_vec_in(System);"] # [doc = " // Here, `s` and `x` can be modified independently."] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [rustc_allow_incoherent_impl] # [inline] # [unstable (feature = "allocator_api" , issue = "32838")] pub fn to_vec_in < A : Allocator > (& self , alloc : A) -> Vec < T , A > where T : Clone , { return T :: to_vec (self , alloc) ; trait ConvertVec { fn to_vec < A : Allocator > (s : & [Self] , alloc : A) -> Vec < Self , A > where Self : Sized ; } impl < T : Clone > ConvertVec for T { # [inline] default fn to_vec < A : Allocator > (s : & [Self] , alloc : A) -> Vec < Self , A > { struct DropGuard < 'a , T , A : Allocator > { vec : & 'a mut Vec < T , A > , num_init : usize , } impl < 'a , T , A : Allocator > Drop for DropGuard < 'a , T , A > { # [inline] fn drop (& mut self) { unsafe { self . vec . set_len (self . num_init) ; } } } let mut vec = Vec :: with_capacity_in (s . len () , alloc) ; let mut guard = DropGuard { vec : & mut vec , num_init : 0 } ; let slots = guard . vec . spare_capacity_mut () ; for (i , b) in s . iter () . enumerate () . take (slots . len ()) { guard . num_init = i ; slots [i] . write (b . clone ()) ; } core :: mem :: forget (guard) ; unsafe { vec . set_len (s . len ()) ; } vec } } impl < T : Copy > ConvertVec for T { # [inline] fn to_vec < A : Allocator > (s : & [Self] , alloc : A) -> Vec < Self , A > { let mut v = Vec :: with_capacity_in (s . len () , alloc) ; unsafe { s . as_ptr () . copy_to_nonoverlapping (v . as_mut_ptr () , s . len ()) ; v . set_len (s . len ()) ; } v } } } # [doc = " Converts `self` into a vector without clones or allocation."] # [doc = ""] # [doc = " The resulting vector can be converted back into a box via"] # [doc = " `Vec<T>`'s `into_boxed_slice` method."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let s: Box<[i32]> = Box::new([10, 40, 30]);"] # [doc = " let x = s.into_vec();"] # [doc = " // `s` cannot be used anymore because it has been converted into `x`."] # [doc = ""] # [doc = " assert_eq!(x, vec![10, 40, 30]);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [stable (feature = "rust1" , since = "1.0.0")] # [inline] # [rustc_diagnostic_item = "slice_into_vec"] pub fn into_vec < A : Allocator > (self : Box < Self , A >) -> Vec < T , A > { unsafe { let len = self . len () ; let (b , alloc) = Box :: into_raw_with_allocator (self) ; Vec :: from_raw_parts_in (b as * mut T , len , len , alloc) } } # [doc = " Creates a vector by copying a slice `n` times."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function will panic if the capacity would overflow."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " assert_eq!([1, 2].repeat(3), vec![1, 2, 1, 2, 1, 2]);"] # [doc = " ```"] # [doc = ""] # [doc = " A panic upon overflow:"] # [doc = ""] # [doc = " ```should_panic"] # [doc = " // this will panic at runtime"] # [doc = " b\"0123456789abcdef\".repeat(usize::MAX);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [cfg (not (no_global_oom_handling))] # [stable (feature = "repeat_generic_slice" , since = "1.40.0")] pub fn repeat (& self , n : usize) -> Vec < T > where T : Copy , { if n == 0 { return Vec :: new () ; } let capacity = self . len () . checked_mul (n) . expect ("capacity overflow") ; let mut buf = Vec :: with_capacity (capacity) ; buf . extend (self) ; { let mut m = n >> 1 ; while m > 0 { unsafe { ptr :: copy_nonoverlapping :: < T > (buf . as_ptr () , (buf . as_mut_ptr ()) . add (buf . len ()) , buf . len () ,) ; let buf_len = buf . len () ; buf . set_len (buf_len * 2) ; } m >>= 1 ; } } let rem_len = capacity - buf . len () ; if rem_len > 0 { unsafe { ptr :: copy_nonoverlapping :: < T > (buf . as_ptr () , (buf . as_mut_ptr ()) . add (buf . len ()) , rem_len ,) ; buf . set_len (capacity) ; } } buf } # [doc = " Flattens a slice of `T` into a single value `Self::Output`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " assert_eq!([\"hello\", \"world\"].concat(), \"helloworld\");"] # [doc = " assert_eq!([[1, 2], [3, 4]].concat(), [1, 2, 3, 4]);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [stable (feature = "rust1" , since = "1.0.0")] pub fn concat < Item : ? Sized > (& self) -> < Self as Concat < Item > > :: Output where Self : Concat < Item > , { Concat :: concat (self) } # [doc = " Flattens a slice of `T` into a single value `Self::Output`, placing a"] # [doc = " given separator between each."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " assert_eq!([\"hello\", \"world\"].join(\" \"), \"hello world\");"] # [doc = " assert_eq!([[1, 2], [3, 4]].join(&0), [1, 2, 0, 3, 4]);"] # [doc = " assert_eq!([[1, 2], [3, 4]].join(&[0, 0][..]), [1, 2, 0, 0, 3, 4]);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [stable (feature = "rename_connect_to_join" , since = "1.3.0")] pub fn join < Separator > (& self , sep : Separator) -> < Self as Join < Separator > > :: Output where Self : Join < Separator > , { Join :: join (self , sep) } # [doc = " Flattens a slice of `T` into a single value `Self::Output`, placing a"] # [doc = " given separator between each."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #![allow(deprecated)]"] # [doc = " assert_eq!([\"hello\", \"world\"].connect(\" \"), \"hello world\");"] # [doc = " assert_eq!([[1, 2], [3, 4]].connect(&0), [1, 2, 0, 3, 4]);"] # [doc = " ```"] # [rustc_allow_incoherent_impl] # [stable (feature = "rust1" , since = "1.0.0")] # [deprecated (since = "1.3.0" , note = "renamed to join" , suggestion = "join")] pub fn connect < Separator > (& self , sep : Separator) -> < Self as Join < Separator > > :: Output where Self : Join < Separator > , { Join :: join (self , sep) } }
};
}
