// Generated macro for impl_648 (impl)
macro_rules! Depcrate_collections_btree_nodeimpl_648 {
() => {
// Module: crate::collections::btree::node
// Provides: {"impl_648"}
// Dependencies: {}
impl < 'a , K , V > NodeRef < marker :: Mut < 'a > , K , V , marker :: LeafOrInternal > { # [doc = " Chooses a balancing context involving the node as a child, thus between"] # [doc = " the KV immediately to the left or to the right in the parent node."] # [doc = " Returns an `Err` if there is no parent."] # [doc = " Panics if the parent is empty."] # [doc = ""] # [doc = " Prefers the left side, to be optimal if the given node is somehow"] # [doc = " underfull, meaning here only that it has fewer elements than its left"] # [doc = " sibling and than its right sibling, if they exist. In that case,"] # [doc = " merging with the left sibling is faster, since we only need to move"] # [doc = " the node's N elements, instead of shifting them to the right and moving"] # [doc = " more than N elements in front. Stealing from the left sibling is also"] # [doc = " typically faster, since we only need to shift the node's N elements to"] # [doc = " the right, instead of shifting at least N of the sibling's elements to"] # [doc = " the left."] pub (super) fn choose_parent_kv (self) -> Result < LeftOrRight < BalancingContext < 'a , K , V > > , Self > { match unsafe { ptr :: read (& self) } . ascend () { Ok (parent_edge) => match parent_edge . left_kv () { Ok (left_parent_kv) => Ok (LeftOrRight :: Left (BalancingContext { parent : unsafe { ptr :: read (& left_parent_kv) } , left_child : left_parent_kv . left_edge () . descend () , right_child : self , })) , Err (parent_edge) => match parent_edge . right_kv () { Ok (right_parent_kv) => Ok (LeftOrRight :: Right (BalancingContext { parent : unsafe { ptr :: read (& right_parent_kv) } , left_child : self , right_child : right_parent_kv . right_edge () . descend () , })) , Err (_) => unreachable ! ("empty internal node") , } , } , Err (root) => Err (root) , } } }
};
}
