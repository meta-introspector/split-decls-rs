// Generated macro for impl_359 (impl)
macro_rules! Depcrate_collections_btree_fiximpl_359 {
() => {
// Module: crate::collections::btree::fix
// Provides: {"impl_359"}
// Dependencies: {}
impl < K , V > Root < K , V > { # [doc = " Removes empty levels on the top, but keeps an empty leaf if the entire tree is empty."] pub (super) fn fix_top < A : Allocator + Clone > (& mut self , alloc : A) { while self . height () > 0 && self . len () == 0 { self . pop_internal_level (alloc . clone ()) ; } } # [doc = " Stocks up or merge away any underfull nodes on the right border of the"] # [doc = " tree. The other nodes, those that are not the root nor a rightmost edge,"] # [doc = " must already have at least MIN_LEN elements."] pub (super) fn fix_right_border < A : Allocator + Clone > (& mut self , alloc : A) { self . fix_top (alloc . clone ()) ; if self . len () > 0 { self . borrow_mut () . last_kv () . fix_right_border_of_right_edge (alloc . clone ()) ; self . fix_top (alloc) ; } } # [doc = " The symmetric clone of `fix_right_border`."] pub (super) fn fix_left_border < A : Allocator + Clone > (& mut self , alloc : A) { self . fix_top (alloc . clone ()) ; if self . len () > 0 { self . borrow_mut () . first_kv () . fix_left_border_of_left_edge (alloc . clone ()) ; self . fix_top (alloc) ; } } # [doc = " Stocks up any underfull nodes on the right border of the tree."] # [doc = " The other nodes, those that are neither the root nor a rightmost edge,"] # [doc = " must be prepared to have up to MIN_LEN elements stolen."] pub (super) fn fix_right_border_of_plentiful (& mut self) { let mut cur_node = self . borrow_mut () ; while let Internal (internal) = cur_node . force () { let mut last_kv = internal . last_kv () . consider_for_balancing () ; debug_assert ! (last_kv . left_child_len () >= MIN_LEN * 2) ; let right_child_len = last_kv . right_child_len () ; if right_child_len < MIN_LEN { last_kv . bulk_steal_left (MIN_LEN - right_child_len) ; } cur_node = last_kv . into_right_child () ; } } }
};
}
