// Generated macro for impl_1935 (impl)
macro_rules! Depcrate_vecimpl_1935 {
() => {
// Module: crate::vec
// Provides: {"impl_1935"}
// Dependencies: {}
impl < T > Vec < T > { # [doc = " Constructs a new, empty `Vec<T>`."] # [doc = ""] # [doc = " The vector will not allocate until elements are pushed onto it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # #![allow(unused_mut)]"] # [doc = " let mut vec: Vec<i32> = Vec::new();"] # [doc = " ```"] # [inline] # [rustc_const_stable (feature = "const_vec_new" , since = "1.39.0")] # [rustc_diagnostic_item = "vec_new"] # [stable (feature = "rust1" , since = "1.0.0")] # [must_use] pub const fn new () -> Self { Vec { buf : RawVec :: new () , len : 0 } } # [doc = " Constructs a new, empty `Vec<T>` with at least the specified capacity."] # [doc = ""] # [doc = " The vector will be able to hold at least `capacity` elements without"] # [doc = " reallocating. This method is allowed to allocate for more elements than"] # [doc = " `capacity`. If `capacity` is zero, the vector will not allocate."] # [doc = ""] # [doc = " It is important to note that although the returned vector has the"] # [doc = " minimum *capacity* specified, the vector will have a zero *length*. For"] # [doc = " an explanation of the difference between length and capacity, see"] # [doc = " *[Capacity and reallocation]*."] # [doc = ""] # [doc = " If it is important to know the exact allocated capacity of a `Vec`,"] # [doc = " always use the [`capacity`] method after construction."] # [doc = ""] # [doc = " For `Vec<T>` where `T` is a zero-sized type, there will be no allocation"] # [doc = " and the capacity will always be `usize::MAX`."] # [doc = ""] # [doc = " [Capacity and reallocation]: #capacity-and-reallocation"] # [doc = " [`capacity`]: Vec::capacity"] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the new capacity exceeds `isize::MAX` _bytes_."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " let mut vec = Vec::with_capacity(10);"] # [doc = ""] # [doc = " // The vector contains no items, even though it has capacity for more"] # [doc = " assert_eq!(vec.len(), 0);"] # [doc = " assert!(vec.capacity() >= 10);"] # [doc = ""] # [doc = " // These are all done without reallocating..."] # [doc = " for i in 0..10 {"] # [doc = "     vec.push(i);"] # [doc = " }"] # [doc = " assert_eq!(vec.len(), 10);"] # [doc = " assert!(vec.capacity() >= 10);"] # [doc = ""] # [doc = " // ...but this may make the vector reallocate"] # [doc = " vec.push(11);"] # [doc = " assert_eq!(vec.len(), 11);"] # [doc = " assert!(vec.capacity() >= 11);"] # [doc = ""] # [doc = " // A vector of a zero-sized type will always over-allocate, since no"] # [doc = " // allocation is necessary"] # [doc = " let vec_units = Vec::<()>::with_capacity(10);"] # [doc = " assert_eq!(vec_units.capacity(), usize::MAX);"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [inline] # [stable (feature = "rust1" , since = "1.0.0")] # [must_use] # [rustc_diagnostic_item = "vec_with_capacity"] # [track_caller] pub fn with_capacity (capacity : usize) -> Self { Self :: with_capacity_in (capacity , Global) } # [doc = " Constructs a new, empty `Vec<T>` with at least the specified capacity."] # [doc = ""] # [doc = " The vector will be able to hold at least `capacity` elements without"] # [doc = " reallocating. This method is allowed to allocate for more elements than"] # [doc = " `capacity`. If `capacity` is zero, the vector will not allocate."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns an error if the capacity exceeds `isize::MAX` _bytes_,"] # [doc = " or if the allocator reports allocation failure."] # [inline] # [unstable (feature = "try_with_capacity" , issue = "91913")] pub fn try_with_capacity (capacity : usize) -> Result < Self , TryReserveError > { Self :: try_with_capacity_in (capacity , Global) } # [doc = " Creates a `Vec<T>` directly from a pointer, a length, and a capacity."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This is highly unsafe, due to the number of invariants that aren't"] # [doc = " checked:"] # [doc = ""] # [doc = " * If `T` is not a zero-sized type and the capacity is nonzero, `ptr` must have"] # [doc = "   been allocated using the global allocator, such as via the [`alloc::alloc`]"] # [doc = "   function. If `T` is a zero-sized type or the capacity is zero, `ptr` need"] # [doc = "   only be non-null and aligned."] # [doc = " * `T` needs to have the same alignment as what `ptr` was allocated with,"] # [doc = "   if the pointer is required to be allocated."] # [doc = "   (`T` having a less strict alignment is not sufficient, the alignment really"] # [doc = "   needs to be equal to satisfy the [`dealloc`] requirement that memory must be"] # [doc = "   allocated and deallocated with the same layout.)"] # [doc = " * The size of `T` times the `capacity` (ie. the allocated size in bytes), if"] # [doc = "   nonzero, needs to be the same size as the pointer was allocated with."] # [doc = "   (Because similar to alignment, [`dealloc`] must be called with the same"] # [doc = "   layout `size`.)"] # [doc = " * `length` needs to be less than or equal to `capacity`."] # [doc = " * The first `length` values must be properly initialized values of type `T`."] # [doc = " * `capacity` needs to be the capacity that the pointer was allocated with,"] # [doc = "   if the pointer is required to be allocated."] # [doc = " * The allocated size in bytes must be no larger than `isize::MAX`."] # [doc = "   See the safety documentation of [`pointer::offset`]."] # [doc = ""] # [doc = " These requirements are always upheld by any `ptr` that has been allocated"] # [doc = " via `Vec<T>`. Other allocation sources are allowed if the invariants are"] # [doc = " upheld."] # [doc = ""] # [doc = " Violating these may cause problems like corrupting the allocator's"] # [doc = " internal data structures. For example it is normally **not** safe"] # [doc = " to build a `Vec<u8>` from a pointer to a C `char` array with length"] # [doc = " `size_t`, doing so is only safe if the array was initially allocated by"] # [doc = " a `Vec` or `String`."] # [doc = " It's also not safe to build one from a `Vec<u16>` and its length, because"] # [doc = " the allocator cares about the alignment, and these two types have different"] # [doc = " alignments. The buffer was allocated with alignment 2 (for `u16`), but after"] # [doc = " turning it into a `Vec<u8>` it'll be deallocated with alignment 1. To avoid"] # [doc = " these issues, it is often preferable to do casting/transmuting using"] # [doc = " [`slice::from_raw_parts`] instead."] # [doc = ""] # [doc = " The ownership of `ptr` is effectively transferred to the"] # [doc = " `Vec<T>` which may then deallocate, reallocate or change the"] # [doc = " contents of memory pointed to by the pointer at will. Ensure"] # [doc = " that nothing else uses the pointer after calling this"] # [doc = " function."] # [doc = ""] # [doc = " [`String`]: crate::string::String"] # [doc = " [`alloc::alloc`]: crate::alloc::alloc"] # [doc = " [`dealloc`]: crate::alloc::GlobalAlloc::dealloc"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::ptr;"] # [doc = " use std::mem;"] # [doc = ""] # [doc = " let v = vec![1, 2, 3];"] # [doc = ""] # [doc = " // Prevent running `v`'s destructor so we are in complete control"] # [doc = " // of the allocation."] # [doc = " let mut v = mem::ManuallyDrop::new(v);"] # [doc = ""] # [doc = " // Pull out the various important pieces of information about `v`"] # [doc = " let p = v.as_mut_ptr();"] # [doc = " let len = v.len();"] # [doc = " let cap = v.capacity();"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     // Overwrite memory with 4, 5, 6"] # [doc = "     for i in 0..len {"] # [doc = "         ptr::write(p.add(i), 4 + i);"] # [doc = "     }"] # [doc = ""] # [doc = "     // Put everything back together into a Vec"] # [doc = "     let rebuilt = Vec::from_raw_parts(p, len, cap);"] # [doc = "     assert_eq!(rebuilt, [4, 5, 6]);"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Using memory that was allocated elsewhere:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::alloc::{alloc, Layout};"] # [doc = ""] # [doc = " fn main() {"] # [doc = "     let layout = Layout::array::<u32>(16).expect(\"overflow cannot happen\");"] # [doc = ""] # [doc = "     let vec = unsafe {"] # [doc = "         let mem = alloc(layout).cast::<u32>();"] # [doc = "         if mem.is_null() {"] # [doc = "             return;"] # [doc = "         }"] # [doc = ""] # [doc = "         mem.write(1_000_000);"] # [doc = ""] # [doc = "         Vec::from_raw_parts(mem, 1, 16)"] # [doc = "     };"] # [doc = ""] # [doc = "     assert_eq!(vec, &[1_000_000]);"] # [doc = "     assert_eq!(vec.capacity(), 16);"] # [doc = " }"] # [doc = " ```"] # [inline] # [stable (feature = "rust1" , since = "1.0.0")] pub unsafe fn from_raw_parts (ptr : * mut T , length : usize , capacity : usize) -> Self { unsafe { Self :: from_raw_parts_in (ptr , length , capacity , Global) } } # [doc (alias = "from_non_null_parts")] # [doc = " Creates a `Vec<T>` directly from a `NonNull` pointer, a length, and a capacity."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This is highly unsafe, due to the number of invariants that aren't"] # [doc = " checked:"] # [doc = ""] # [doc = " * `ptr` must have been allocated using the global allocator, such as via"] # [doc = "   the [`alloc::alloc`] function."] # [doc = " * `T` needs to have the same alignment as what `ptr` was allocated with."] # [doc = "   (`T` having a less strict alignment is not sufficient, the alignment really"] # [doc = "   needs to be equal to satisfy the [`dealloc`] requirement that memory must be"] # [doc = "   allocated and deallocated with the same layout.)"] # [doc = " * The size of `T` times the `capacity` (ie. the allocated size in bytes) needs"] # [doc = "   to be the same size as the pointer was allocated with. (Because similar to"] # [doc = "   alignment, [`dealloc`] must be called with the same layout `size`.)"] # [doc = " * `length` needs to be less than or equal to `capacity`."] # [doc = " * The first `length` values must be properly initialized values of type `T`."] # [doc = " * `capacity` needs to be the capacity that the pointer was allocated with."] # [doc = " * The allocated size in bytes must be no larger than `isize::MAX`."] # [doc = "   See the safety documentation of [`pointer::offset`]."] # [doc = ""] # [doc = " These requirements are always upheld by any `ptr` that has been allocated"] # [doc = " via `Vec<T>`. Other allocation sources are allowed if the invariants are"] # [doc = " upheld."] # [doc = ""] # [doc = " Violating these may cause problems like corrupting the allocator's"] # [doc = " internal data structures. For example it is normally **not** safe"] # [doc = " to build a `Vec<u8>` from a pointer to a C `char` array with length"] # [doc = " `size_t`, doing so is only safe if the array was initially allocated by"] # [doc = " a `Vec` or `String`."] # [doc = " It's also not safe to build one from a `Vec<u16>` and its length, because"] # [doc = " the allocator cares about the alignment, and these two types have different"] # [doc = " alignments. The buffer was allocated with alignment 2 (for `u16`), but after"] # [doc = " turning it into a `Vec<u8>` it'll be deallocated with alignment 1. To avoid"] # [doc = " these issues, it is often preferable to do casting/transmuting using"] # [doc = " [`NonNull::slice_from_raw_parts`] instead."] # [doc = ""] # [doc = " The ownership of `ptr` is effectively transferred to the"] # [doc = " `Vec<T>` which may then deallocate, reallocate or change the"] # [doc = " contents of memory pointed to by the pointer at will. Ensure"] # [doc = " that nothing else uses the pointer after calling this"] # [doc = " function."] # [doc = ""] # [doc = " [`String`]: crate::string::String"] # [doc = " [`alloc::alloc`]: crate::alloc::alloc"] # [doc = " [`dealloc`]: crate::alloc::GlobalAlloc::dealloc"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(box_vec_non_null)]"] # [doc = ""] # [doc = " use std::ptr::NonNull;"] # [doc = " use std::mem;"] # [doc = ""] # [doc = " let v = vec![1, 2, 3];"] # [doc = ""] # [doc = " // Prevent running `v`'s destructor so we are in complete control"] # [doc = " // of the allocation."] # [doc = " let mut v = mem::ManuallyDrop::new(v);"] # [doc = ""] # [doc = " // Pull out the various important pieces of information about `v`"] # [doc = " let p = unsafe { NonNull::new_unchecked(v.as_mut_ptr()) };"] # [doc = " let len = v.len();"] # [doc = " let cap = v.capacity();"] # [doc = ""] # [doc = " unsafe {"] # [doc = "     // Overwrite memory with 4, 5, 6"] # [doc = "     for i in 0..len {"] # [doc = "         p.add(i).write(4 + i);"] # [doc = "     }"] # [doc = ""] # [doc = "     // Put everything back together into a Vec"] # [doc = "     let rebuilt = Vec::from_parts(p, len, cap);"] # [doc = "     assert_eq!(rebuilt, [4, 5, 6]);"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Using memory that was allocated elsewhere:"] # [doc = ""] # [doc = " ```rust"] # [doc = " #![feature(box_vec_non_null)]"] # [doc = ""] # [doc = " use std::alloc::{alloc, Layout};"] # [doc = " use std::ptr::NonNull;"] # [doc = ""] # [doc = " fn main() {"] # [doc = "     let layout = Layout::array::<u32>(16).expect(\"overflow cannot happen\");"] # [doc = ""] # [doc = "     let vec = unsafe {"] # [doc = "         let Some(mem) = NonNull::new(alloc(layout).cast::<u32>()) else {"] # [doc = "             return;"] # [doc = "         };"] # [doc = ""] # [doc = "         mem.write(1_000_000);"] # [doc = ""] # [doc = "         Vec::from_parts(mem, 1, 16)"] # [doc = "     };"] # [doc = ""] # [doc = "     assert_eq!(vec, &[1_000_000]);"] # [doc = "     assert_eq!(vec.capacity(), 16);"] # [doc = " }"] # [doc = " ```"] # [inline] # [unstable (feature = "box_vec_non_null" , reason = "new API" , issue = "130364")] pub unsafe fn from_parts (ptr : NonNull < T > , length : usize , capacity : usize) -> Self { unsafe { Self :: from_parts_in (ptr , length , capacity , Global) } } # [doc = " Returns a mutable reference to the last item in the vector, or"] # [doc = " `None` if it is empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(vec_peek_mut)]"] # [doc = " let mut vec = Vec::new();"] # [doc = " assert!(vec.peek_mut().is_none());"] # [doc = ""] # [doc = " vec.push(1);"] # [doc = " vec.push(5);"] # [doc = " vec.push(2);"] # [doc = " assert_eq!(vec.last(), Some(&2));"] # [doc = " if let Some(mut val) = vec.peek_mut() {"] # [doc = "     *val = 0;"] # [doc = " }"] # [doc = " assert_eq!(vec.last(), Some(&0));"] # [doc = " ```"] # [inline] # [unstable (feature = "vec_peek_mut" , issue = "122742")] pub fn peek_mut (& mut self) -> Option < PeekMut < '_ , T > > { PeekMut :: new (self) } # [doc = " Decomposes a `Vec<T>` into its raw components: `(pointer, length, capacity)`."] # [doc = ""] # [doc = " Returns the raw pointer to the underlying data, the length of"] # [doc = " the vector (in elements), and the allocated capacity of the"] # [doc = " data (in elements). These are the same arguments in the same"] # [doc = " order as the arguments to [`from_raw_parts`]."] # [doc = ""] # [doc = " After calling this function, the caller is responsible for the"] # [doc = " memory previously managed by the `Vec`. Most often, one does"] # [doc = " this by converting the raw pointer, length, and capacity back"] # [doc = " into a `Vec` with the [`from_raw_parts`] function; more generally,"] # [doc = " if `T` is non-zero-sized and the capacity is nonzero, one may use"] # [doc = " any method that calls [`dealloc`] with a layout of"] # [doc = " `Layout::array::<T>(capacity)`; if `T` is zero-sized or the"] # [doc = " capacity is zero, nothing needs to be done."] # [doc = ""] # [doc = " [`from_raw_parts`]: Vec::from_raw_parts"] # [doc = " [`dealloc`]: crate::alloc::GlobalAlloc::dealloc"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(vec_into_raw_parts)]"] # [doc = " let v: Vec<i32> = vec![-1, 0, 1];"] # [doc = ""] # [doc = " let (ptr, len, cap) = v.into_raw_parts();"] # [doc = ""] # [doc = " let rebuilt = unsafe {"] # [doc = "     // We can now make changes to the components, such as"] # [doc = "     // transmuting the raw pointer to a compatible type."] # [doc = "     let ptr = ptr as *mut u32;"] # [doc = ""] # [doc = "     Vec::from_raw_parts(ptr, len, cap)"] # [doc = " };"] # [doc = " assert_eq!(rebuilt, [4294967295, 0, 1]);"] # [doc = " ```"] # [must_use = "losing the pointer will leak memory"] # [unstable (feature = "vec_into_raw_parts" , reason = "new API" , issue = "65816")] pub fn into_raw_parts (self) -> (* mut T , usize , usize) { let mut me = ManuallyDrop :: new (self) ; (me . as_mut_ptr () , me . len () , me . capacity ()) } # [doc (alias = "into_non_null_parts")] # [doc = " Decomposes a `Vec<T>` into its raw components: `(NonNull pointer, length, capacity)`."] # [doc = ""] # [doc = " Returns the `NonNull` pointer to the underlying data, the length of"] # [doc = " the vector (in elements), and the allocated capacity of the"] # [doc = " data (in elements). These are the same arguments in the same"] # [doc = " order as the arguments to [`from_parts`]."] # [doc = ""] # [doc = " After calling this function, the caller is responsible for the"] # [doc = " memory previously managed by the `Vec`. The only way to do"] # [doc = " this is to convert the `NonNull` pointer, length, and capacity back"] # [doc = " into a `Vec` with the [`from_parts`] function, allowing"] # [doc = " the destructor to perform the cleanup."] # [doc = ""] # [doc = " [`from_parts`]: Vec::from_parts"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #![feature(vec_into_raw_parts, box_vec_non_null)]"] # [doc = ""] # [doc = " let v: Vec<i32> = vec![-1, 0, 1];"] # [doc = ""] # [doc = " let (ptr, len, cap) = v.into_parts();"] # [doc = ""] # [doc = " let rebuilt = unsafe {"] # [doc = "     // We can now make changes to the components, such as"] # [doc = "     // transmuting the raw pointer to a compatible type."] # [doc = "     let ptr = ptr.cast::<u32>();"] # [doc = ""] # [doc = "     Vec::from_parts(ptr, len, cap)"] # [doc = " };"] # [doc = " assert_eq!(rebuilt, [4294967295, 0, 1]);"] # [doc = " ```"] # [must_use = "losing the pointer will leak memory"] # [unstable (feature = "box_vec_non_null" , reason = "new API" , issue = "130364")] pub fn into_parts (self) -> (NonNull < T > , usize , usize) { let (ptr , len , capacity) = self . into_raw_parts () ; (unsafe { NonNull :: new_unchecked (ptr) } , len , capacity) } }
};
}
