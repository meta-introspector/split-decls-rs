// Generated macro for impl_1953 (impl)
macro_rules! Depcrate_vecimpl_1953 {
() => {
// Module: crate::vec
// Provides: {"impl_1953"}
// Dependencies: {}
# [doc = " Collects an iterator into a Vec, commonly called via [`Iterator::collect()`]"] # [doc = ""] # [doc = " # Allocation behavior"] # [doc = ""] # [doc = " In general `Vec` does not guarantee any particular growth or allocation strategy."] # [doc = " That also applies to this trait impl."] # [doc = ""] # [doc = " **Note:** This section covers implementation details and is therefore exempt from"] # [doc = " stability guarantees."] # [doc = ""] # [doc = " Vec may use any or none of the following strategies,"] # [doc = " depending on the supplied iterator:"] # [doc = ""] # [doc = " * preallocate based on [`Iterator::size_hint()`]"] # [doc = "   * and panic if the number of items is outside the provided lower/upper bounds"] # [doc = " * use an amortized growth strategy similar to `pushing` one item at a time"] # [doc = " * perform the iteration in-place on the original allocation backing the iterator"] # [doc = ""] # [doc = " The last case warrants some attention. It is an optimization that in many cases reduces peak memory"] # [doc = " consumption and improves cache locality. But when big, short-lived allocations are created,"] # [doc = " only a small fraction of their items get collected, no further use is made of the spare capacity"] # [doc = " and the resulting `Vec` is moved into a longer-lived structure, then this can lead to the large"] # [doc = " allocations having their lifetimes unnecessarily extended which can result in increased memory"] # [doc = " footprint."] # [doc = ""] # [doc = " In cases where this is an issue, the excess capacity can be discarded with [`Vec::shrink_to()`],"] # [doc = " [`Vec::shrink_to_fit()`] or by collecting into [`Box<[T]>`][owned slice] instead, which additionally reduces"] # [doc = " the size of the long-lived struct."] # [doc = ""] # [doc = " [owned slice]: Box"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use std::sync::Mutex;"] # [doc = " static LONG_LIVED: Mutex<Vec<Vec<u16>>> = Mutex::new(Vec::new());"] # [doc = ""] # [doc = " for i in 0..10 {"] # [doc = "     let big_temporary: Vec<u16> = (0..1024).collect();"] # [doc = "     // discard most items"] # [doc = "     let mut result: Vec<_> = big_temporary.into_iter().filter(|i| i % 100 == 0).collect();"] # [doc = "     // without this a lot of unused capacity might be moved into the global"] # [doc = "     result.shrink_to_fit();"] # [doc = "     LONG_LIVED.lock().unwrap().push(result);"] # [doc = " }"] # [doc = " ```"] # [cfg (not (no_global_oom_handling))] # [stable (feature = "rust1" , since = "1.0.0")] impl < T > FromIterator < T > for Vec < T > { # [inline] # [track_caller] fn from_iter < I : IntoIterator < Item = T > > (iter : I) -> Vec < T > { < Self as SpecFromIter < T , I :: IntoIter > > :: from_iter (iter . into_iter ()) } }
};
}
