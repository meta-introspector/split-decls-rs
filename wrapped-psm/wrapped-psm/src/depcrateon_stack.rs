// Generated macro for on_stack (function)
macro_rules! Depcrateon_stack {
() => {
// Module: crate
// Provides: {"on_stack"}
// Dependencies: {}
# [doc = " Run the closure on the provided stack."] # [doc = ""] # [doc = " Once the closure completes its execution, the original stack pointer is restored and execution"] # [doc = " returns to the caller."] # [doc = ""] # [doc = " `base` address must be the low address of the stack memory region, regardless of the stack"] # [doc = " growth direction. It is not necessary for the whole region `[base; base + size]` to be usable"] # [doc = " at the time this function called, however it is required that at least the following hold:"] # [doc = ""] # [doc = " * Both `base` and `base + size` are aligned up to the target-specific requirements;"] # [doc = " * Depending on `StackDirection` value for the platform, the end of the stack memory region,"] # [doc = "   which would end up containing the first frame(s), must have sufficient number of pages"] # [doc = "   allocated to execute code until more pages are commited. The other end should contain a guard"] # [doc = "   page (not writable, readable or executable) to ensure Rust’s soundness guarantees."] # [doc = ""] # [doc = " Note, that some or all of these considerations are irrelevant to some applications. For"] # [doc = " example, Rust’s soundness story relies on all stacks having a guard-page, however if the user"] # [doc = " is able to guarantee that the memory region used for stack cannot be exceeded, a guard page may"] # [doc = " end up being an expensive unnecessity."] # [doc = ""] # [doc = " The previous stack may not be deallocated. If an ability to deallocate the old stack is desired"] # [doc = " consider `replace_stack` instead."] # [doc = ""] # [doc = " # Guidelines"] # [doc = ""] # [doc = " Memory regions that are aligned to a single page (usually 4kB) are an extremely portable choice"] # [doc = " for stacks."] # [doc = ""] # [doc = " Allocate at least 4kB of stack. Some architectures (such as SPARC) consume stack memory"] # [doc = " significantly faster compared to the more usual architectures such as x86 or ARM. Allocating"] # [doc = " less than 4kB of memory may make it impossible to commit more pages without overflowing the"] # [doc = " stack later on."] # [doc = ""] # [doc = " # Unsafety"] # [doc = ""] # [doc = " The stack `base` address must be aligned as appropriate for the target."] # [doc = ""] # [doc = " The stack `size` must be a multiple of stack alignment required by target."] # [doc = ""] # [doc = " The `size` must not overflow `isize`."] # [doc = ""] # [doc = " `callback` must not unwind or return control flow by any other means than directly returning."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::alloc;"] # [doc = " const STACK_ALIGN: usize = 4096;"] # [doc = " const STACK_SIZE: usize = 4096;"] # [doc = " unsafe {"] # [doc = "     let layout = alloc::Layout::from_size_align(STACK_SIZE, STACK_ALIGN).unwrap();"] # [doc = "     let new_stack = alloc::alloc(layout);"] # [doc = "     assert!(!new_stack.is_null(), \"allocations must succeed!\");"] # [doc = "     let (stack, result) = psm::on_stack(new_stack, STACK_SIZE, || {"] # [doc = "         (psm::stack_pointer(), 4 + 4)"] # [doc = "     });"] # [doc = "     println!(\"4 + 4 = {} has been calculated on stack {:p}\", result, stack);"] # [doc = " }"] # [doc = " ```"] # [cfg (switchable_stack)] pub unsafe fn on_stack < R , F : FnOnce () -> R > (base : * mut u8 , size : usize , callback : F) -> R { use core :: mem :: MaybeUninit ; extern_item ! { unsafe fn with_on_stack < R , F : FnOnce () -> R > (callback_ptr : usize , return_ptr : usize) { let return_ptr = (* (return_ptr as * mut MaybeUninit < R >)) . as_mut_ptr () ; let callback = (* (callback_ptr as * mut MaybeUninit < F >)) . as_ptr () ; return_ptr . write ((callback . read ()) ()) ; } } let sp = match StackDirection :: new () { StackDirection :: Ascending => base , StackDirection :: Descending => base . offset (size as isize) , } ; let mut callback : MaybeUninit < F > = MaybeUninit :: new (callback) ; let mut return_value : MaybeUninit < R > = MaybeUninit :: uninit () ; rust_psm_on_stack (& mut callback as * mut MaybeUninit < F > as usize , & mut return_value as * mut MaybeUninit < R > as usize , with_on_stack :: < R , F > , sp , base ,) ; return return_value . assume_init () ; }
};
}
