// Generated macro for replace_stack (function)
macro_rules! Depcratereplace_stack {
() => {
// Module: crate
// Provides: {"replace_stack"}
// Dependencies: {}
# [doc = " Run the provided non-terminating computation on an entirely new stack."] # [doc = ""] # [doc = " `base` address must be the low address of the stack memory region, regardless of the stack"] # [doc = " growth direction. It is not necessary for the whole region `[base; base + size]` to be usable"] # [doc = " at the time this function called, however it is required that at least the following hold:"] # [doc = ""] # [doc = " * Both `base` and `base + size` are aligned up to the target-specific requirements;"] # [doc = " * Depending on `StackDirection` value for the platform, the end of the stack memory region,"] # [doc = "   which would end up containing the first frame(s), must have sufficient number of pages"] # [doc = "   allocated to execute code until more pages are commited. The other end should contain a guard"] # [doc = "   page (not writable, readable or executable) to ensure Rust’s soundness guarantees."] # [doc = ""] # [doc = " Note, that some or all of these considerations are irrelevant to some applications. For"] # [doc = " example, Rust’s soundness story relies on all stacks having a guard-page, however if the user"] # [doc = " is able to guarantee that the memory region used for stack cannot be exceeded, a guard page may"] # [doc = " end up being an expensive unnecessity."] # [doc = ""] # [doc = " The previous stack is not deallocated and may not be deallocated unless the data on the old"] # [doc = " stack is not referenced in any way (by e.g. the `callback` closure)."] # [doc = ""] # [doc = " On platforms where multiple stack pointers are available, the “current” stack pointer is"] # [doc = " replaced."] # [doc = ""] # [doc = " # Guidelines"] # [doc = ""] # [doc = " Memory regions that are aligned to a single page (usually 4kB) are an extremely portable choice"] # [doc = " for stacks."] # [doc = ""] # [doc = " Allocate at least 4kB of stack. Some architectures (such as SPARC) consume stack memory"] # [doc = " significantly faster compared to the more usual architectures such as x86 or ARM. Allocating"] # [doc = " less than 4kB of memory may make it impossible to commit more pages without overflowing the"] # [doc = " stack later on."] # [doc = ""] # [doc = " # Unsafety"] # [doc = ""] # [doc = " The stack `base` address must be aligned as appropriate for the target."] # [doc = ""] # [doc = " The stack `size` must be a multiple of stack alignment required by target."] # [doc = ""] # [doc = " The `size` must not overflow `isize`."] # [doc = ""] # [doc = " `callback` must not return (not enforced by typesystem currently because `!` is unstable),"] # [doc = " unwind or otherwise return control flow to any of the previous frames."] # [cfg (switchable_stack)] pub unsafe fn replace_stack < F : FnOnce () > (base : * mut u8 , size : usize , callback : F) -> ! { extern_item ! { unsafe fn with_replaced_stack < F : FnOnce () > (d : usize) -> ! { :: core :: ptr :: read (d as * const F) () ; :: core :: hint :: unreachable_unchecked () ; } } let sp = match StackDirection :: new () { StackDirection :: Ascending => base , StackDirection :: Descending => base . offset (size as isize) , } ; rust_psm_replace_stack (& callback as * const F as usize , with_replaced_stack :: < F > , sp , base ,) ; }
};
}
