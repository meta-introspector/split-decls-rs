// Generated macro for BufReadExt (trait)
macro_rules! Depcrate_ioBufReadExt {
() => {
// Module: crate::io
// Provides: {"BufReadExt"}
// Dependencies: {}
# [doc = " An extension trait for"] # [doc = " [`std::io::BufRead`](https://doc.rust-lang.org/std/io/trait.BufRead.html)"] # [doc = " which provides convenience APIs for dealing with byte strings."] pub trait BufReadExt : io :: BufRead { # [doc = " Returns an iterator over the lines of this reader, where each line"] # [doc = " is represented as a byte string."] # [doc = ""] # [doc = " Each item yielded by this iterator is a `io::Result<Vec<u8>>`, where"] # [doc = " an error is yielded if there was a problem reading from the underlying"] # [doc = " reader."] # [doc = ""] # [doc = " On success, the next line in the iterator is returned. The line does"] # [doc = " *not* contain a trailing `\\n` or `\\r\\n`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io;"] # [doc = ""] # [doc = " use bstr::io::BufReadExt;"] # [doc = ""] # [doc = " # fn example() -> Result<(), io::Error> {"] # [doc = " let mut cursor = io::Cursor::new(b\"lorem\\nipsum\\r\\ndolor\");"] # [doc = ""] # [doc = " let mut lines = vec![];"] # [doc = " for result in cursor.byte_lines() {"] # [doc = "     let line = result?;"] # [doc = "     lines.push(line);"] # [doc = " }"] # [doc = " assert_eq!(lines.len(), 3);"] # [doc = " assert_eq!(lines[0], \"lorem\".as_bytes());"] # [doc = " assert_eq!(lines[1], \"ipsum\".as_bytes());"] # [doc = " assert_eq!(lines[2], \"dolor\".as_bytes());"] # [doc = " # Ok(()) }; example().unwrap()"] # [doc = " ```"] fn byte_lines (self) -> ByteLines < Self > where Self : Sized , { ByteLines { buf : self } } # [doc = " Returns an iterator over byte-terminated records of this reader, where"] # [doc = " each record is represented as a byte string."] # [doc = ""] # [doc = " Each item yielded by this iterator is a `io::Result<Vec<u8>>`, where"] # [doc = " an error is yielded if there was a problem reading from the underlying"] # [doc = " reader."] # [doc = ""] # [doc = " On success, the next record in the iterator is returned. The record"] # [doc = " does *not* contain its trailing terminator."] # [doc = ""] # [doc = " Note that calling `byte_records(b'\\n')` differs from `byte_lines()` in"] # [doc = " that it has no special handling for `\\r`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io;"] # [doc = ""] # [doc = " use bstr::io::BufReadExt;"] # [doc = ""] # [doc = " # fn example() -> Result<(), io::Error> {"] # [doc = " let mut cursor = io::Cursor::new(b\"lorem\\x00ipsum\\x00dolor\");"] # [doc = ""] # [doc = " let mut records = vec![];"] # [doc = " for result in cursor.byte_records(b'\\x00') {"] # [doc = "     let record = result?;"] # [doc = "     records.push(record);"] # [doc = " }"] # [doc = " assert_eq!(records.len(), 3);"] # [doc = " assert_eq!(records[0], \"lorem\".as_bytes());"] # [doc = " assert_eq!(records[1], \"ipsum\".as_bytes());"] # [doc = " assert_eq!(records[2], \"dolor\".as_bytes());"] # [doc = " # Ok(()) }; example().unwrap()"] # [doc = " ```"] fn byte_records (self , terminator : u8) -> ByteRecords < Self > where Self : Sized , { ByteRecords { terminator , buf : self } } # [doc = " Executes the given closure on each line in the underlying reader."] # [doc = ""] # [doc = " If the closure returns an error (or if the underlying reader returns an"] # [doc = " error), then iteration is stopped and the error is returned. If false"] # [doc = " is returned, then iteration is stopped and no error is returned."] # [doc = ""] # [doc = " The closure given is called on exactly the same values as yielded by"] # [doc = " the [`byte_lines`](trait.BufReadExt.html#method.byte_lines)"] # [doc = " iterator. Namely, lines do _not_ contain trailing `\\n` or `\\r\\n` bytes."] # [doc = ""] # [doc = " This routine is useful for iterating over lines as quickly as"] # [doc = " possible. Namely, a single allocation is reused for each line."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io;"] # [doc = ""] # [doc = " use bstr::io::BufReadExt;"] # [doc = ""] # [doc = " # fn example() -> Result<(), io::Error> {"] # [doc = " let mut cursor = io::Cursor::new(b\"lorem\\nipsum\\r\\ndolor\");"] # [doc = ""] # [doc = " let mut lines = vec![];"] # [doc = " cursor.for_byte_line(|line| {"] # [doc = "     lines.push(line.to_vec());"] # [doc = "     Ok(true)"] # [doc = " })?;"] # [doc = " assert_eq!(lines.len(), 3);"] # [doc = " assert_eq!(lines[0], \"lorem\".as_bytes());"] # [doc = " assert_eq!(lines[1], \"ipsum\".as_bytes());"] # [doc = " assert_eq!(lines[2], \"dolor\".as_bytes());"] # [doc = " # Ok(()) }; example().unwrap()"] # [doc = " ```"] fn for_byte_line < F > (& mut self , mut for_each_line : F) -> io :: Result < () > where Self : Sized , F : FnMut (& [u8]) -> io :: Result < bool > , { self . for_byte_line_with_terminator (| line | { for_each_line (trim_line_slice (line)) }) } # [doc = " Executes the given closure on each byte-terminated record in the"] # [doc = " underlying reader."] # [doc = ""] # [doc = " If the closure returns an error (or if the underlying reader returns an"] # [doc = " error), then iteration is stopped and the error is returned. If false"] # [doc = " is returned, then iteration is stopped and no error is returned."] # [doc = ""] # [doc = " The closure given is called on exactly the same values as yielded by"] # [doc = " the [`byte_records`](trait.BufReadExt.html#method.byte_records)"] # [doc = " iterator. Namely, records do _not_ contain a trailing terminator byte."] # [doc = ""] # [doc = " This routine is useful for iterating over records as quickly as"] # [doc = " possible. Namely, a single allocation is reused for each record."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io;"] # [doc = ""] # [doc = " use bstr::io::BufReadExt;"] # [doc = ""] # [doc = " # fn example() -> Result<(), io::Error> {"] # [doc = " let mut cursor = io::Cursor::new(b\"lorem\\x00ipsum\\x00dolor\");"] # [doc = ""] # [doc = " let mut records = vec![];"] # [doc = " cursor.for_byte_record(b'\\x00', |record| {"] # [doc = "     records.push(record.to_vec());"] # [doc = "     Ok(true)"] # [doc = " })?;"] # [doc = " assert_eq!(records.len(), 3);"] # [doc = " assert_eq!(records[0], \"lorem\".as_bytes());"] # [doc = " assert_eq!(records[1], \"ipsum\".as_bytes());"] # [doc = " assert_eq!(records[2], \"dolor\".as_bytes());"] # [doc = " # Ok(()) }; example().unwrap()"] # [doc = " ```"] fn for_byte_record < F > (& mut self , terminator : u8 , mut for_each_record : F ,) -> io :: Result < () > where Self : Sized , F : FnMut (& [u8]) -> io :: Result < bool > , { self . for_byte_record_with_terminator (terminator , | chunk | { for_each_record (trim_record_slice (chunk , terminator)) }) } # [doc = " Executes the given closure on each line in the underlying reader."] # [doc = ""] # [doc = " If the closure returns an error (or if the underlying reader returns an"] # [doc = " error), then iteration is stopped and the error is returned. If false"] # [doc = " is returned, then iteration is stopped and no error is returned."] # [doc = ""] # [doc = " Unlike"] # [doc = " [`for_byte_line`](trait.BufReadExt.html#method.for_byte_line),"] # [doc = " the lines given to the closure *do* include the line terminator, if one"] # [doc = " exists."] # [doc = ""] # [doc = " This routine is useful for iterating over lines as quickly as"] # [doc = " possible. Namely, a single allocation is reused for each line."] # [doc = ""] # [doc = " This is identical to `for_byte_record_with_terminator` with a"] # [doc = " terminator of `\\n`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io;"] # [doc = ""] # [doc = " use bstr::io::BufReadExt;"] # [doc = ""] # [doc = " # fn example() -> Result<(), io::Error> {"] # [doc = " let mut cursor = io::Cursor::new(b\"lorem\\nipsum\\r\\ndolor\");"] # [doc = ""] # [doc = " let mut lines = vec![];"] # [doc = " cursor.for_byte_line_with_terminator(|line| {"] # [doc = "     lines.push(line.to_vec());"] # [doc = "     Ok(true)"] # [doc = " })?;"] # [doc = " assert_eq!(lines.len(), 3);"] # [doc = " assert_eq!(lines[0], \"lorem\\n\".as_bytes());"] # [doc = " assert_eq!(lines[1], \"ipsum\\r\\n\".as_bytes());"] # [doc = " assert_eq!(lines[2], \"dolor\".as_bytes());"] # [doc = " # Ok(()) }; example().unwrap()"] # [doc = " ```"] fn for_byte_line_with_terminator < F > (& mut self , for_each_line : F ,) -> io :: Result < () > where Self : Sized , F : FnMut (& [u8]) -> io :: Result < bool > , { self . for_byte_record_with_terminator (b'\n' , for_each_line) } # [doc = " Executes the given closure on each byte-terminated record in the"] # [doc = " underlying reader."] # [doc = ""] # [doc = " If the closure returns an error (or if the underlying reader returns an"] # [doc = " error), then iteration is stopped and the error is returned. If false"] # [doc = " is returned, then iteration is stopped and no error is returned."] # [doc = ""] # [doc = " Unlike"] # [doc = " [`for_byte_record`](trait.BufReadExt.html#method.for_byte_record),"] # [doc = " the lines given to the closure *do* include the record terminator, if"] # [doc = " one exists."] # [doc = ""] # [doc = " This routine is useful for iterating over records as quickly as"] # [doc = " possible. Namely, a single allocation is reused for each record."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io;"] # [doc = ""] # [doc = " use bstr::{io::BufReadExt, B};"] # [doc = ""] # [doc = " # fn example() -> Result<(), io::Error> {"] # [doc = " let mut cursor = io::Cursor::new(b\"lorem\\x00ipsum\\x00dolor\");"] # [doc = ""] # [doc = " let mut records = vec![];"] # [doc = " cursor.for_byte_record_with_terminator(b'\\x00', |record| {"] # [doc = "     records.push(record.to_vec());"] # [doc = "     Ok(true)"] # [doc = " })?;"] # [doc = " assert_eq!(records.len(), 3);"] # [doc = " assert_eq!(records[0], B(b\"lorem\\x00\"));"] # [doc = " assert_eq!(records[1], B(\"ipsum\\x00\"));"] # [doc = " assert_eq!(records[2], B(\"dolor\"));"] # [doc = " # Ok(()) }; example().unwrap()"] # [doc = " ```"] fn for_byte_record_with_terminator < F > (& mut self , terminator : u8 , mut for_each_record : F ,) -> io :: Result < () > where Self : Sized , F : FnMut (& [u8]) -> io :: Result < bool > , { let mut bytes = vec ! [] ; let mut res = Ok (()) ; let mut consumed = 0 ; 'outer : loop { { let mut buf = self . fill_buf () ? ; if buf . is_empty () { break ; } while let Some (index) = buf . find_byte (terminator) { let (record , rest) = buf . split_at (index + 1) ; buf = rest ; consumed += record . len () ; match for_each_record (record) { Ok (false) => break 'outer , Err (err) => { res = Err (err) ; break 'outer ; } _ => () , } } bytes . extend_from_slice (buf) ; consumed += buf . len () ; } self . consume (consumed) ; consumed = 0 ; self . read_until (terminator , & mut bytes) ? ; if bytes . is_empty () || ! for_each_record (& bytes) ? { break ; } bytes . clear () ; } self . consume (consumed) ; res } }
};
}
