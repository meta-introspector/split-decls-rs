// Generated macro for GraphemeIndices (struct)
macro_rules! Depcrate_unicode_graphemeGraphemeIndices {
() => {
// Module: crate::unicode::grapheme
// Provides: {"GraphemeIndices"}
// Dependencies: {}
# [doc = " An iterator over grapheme clusters in a byte string and their byte index"] # [doc = " positions."] # [doc = ""] # [doc = " This iterator is typically constructed by"] # [doc = " [`ByteSlice::grapheme_indices`](trait.ByteSlice.html#method.grapheme_indices)."] # [doc = ""] # [doc = " Unicode defines a grapheme cluster as an *approximation* to a single user"] # [doc = " visible character. A grapheme cluster, or just \"grapheme,\" is made up of"] # [doc = " one or more codepoints. For end user oriented tasks, one should generally"] # [doc = " prefer using graphemes instead of [`Chars`](struct.Chars.html), which"] # [doc = " always yields one codepoint at a time."] # [doc = ""] # [doc = " Since graphemes are made up of one or more codepoints, this iterator"] # [doc = " yields `&str` elements (along with their start and end byte offsets)."] # [doc = " When invalid UTF-8 is encountered, replacement codepoints are"] # [doc = " [substituted](index.html#handling-of-invalid-utf-8). Because of this, the"] # [doc = " indices yielded by this iterator may not correspond to the length of the"] # [doc = " grapheme cluster yielded with those indices. For example, when this"] # [doc = " iterator encounters `\\xFF` in the byte string, then it will yield a pair"] # [doc = " of indices ranging over a single byte, but will provide an `&str`"] # [doc = " equivalent to `\"\\u{FFFD}\"`, which is three bytes in length. However, when"] # [doc = " given only valid UTF-8, then all indices are in exact correspondence with"] # [doc = " their paired grapheme cluster."] # [doc = ""] # [doc = " This iterator can be used in reverse. When reversed, exactly the same"] # [doc = " set of grapheme clusters are yielded, but in reverse order."] # [doc = ""] # [doc = " This iterator only yields *extended* grapheme clusters, in accordance with"] # [doc = " [UAX #29](https://www.unicode.org/reports/tr29/tr29-33.html#Grapheme_Cluster_Boundaries)."] # [derive (Clone , Debug)] pub struct GraphemeIndices < 'a > { bs : & 'a [u8] , forward_index : usize , reverse_index : usize , }
};
}
