// Generated macro for impl_8 (impl)
macro_rules! Depcrate_rangeimpl_8 {
() => {
// Module: crate::range
// Provides: {"impl_8"}
// Dependencies: {}
# [doc = " Manipulation methods."] impl TextRange { # [doc = " Check if this range contains an offset."] # [doc = ""] # [doc = " The end index is considered excluded."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use text_size::*;"] # [doc = " let (start, end): (TextSize, TextSize);"] # [doc = " # start = 10.into(); end = 20.into();"] # [doc = " let range = TextRange::new(start, end);"] # [doc = " assert!(range.contains(start));"] # [doc = " assert!(!range.contains(end));"] # [doc = " ```"] # [inline] pub fn contains (self , offset : TextSize) -> bool { self . start () <= offset && offset < self . end () } # [doc = " Check if this range contains an offset."] # [doc = ""] # [doc = " The end index is considered included."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use text_size::*;"] # [doc = " let (start, end): (TextSize, TextSize);"] # [doc = " # start = 10.into(); end = 20.into();"] # [doc = " let range = TextRange::new(start, end);"] # [doc = " assert!(range.contains_inclusive(start));"] # [doc = " assert!(range.contains_inclusive(end));"] # [doc = " ```"] # [inline] pub fn contains_inclusive (self , offset : TextSize) -> bool { self . start () <= offset && offset <= self . end () } # [doc = " Check if this range completely contains another range."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use text_size::*;"] # [doc = " let larger = TextRange::new(0.into(), 20.into());"] # [doc = " let smaller = TextRange::new(5.into(), 15.into());"] # [doc = " assert!(larger.contains_range(smaller));"] # [doc = " assert!(!smaller.contains_range(larger));"] # [doc = ""] # [doc = " // a range always contains itself"] # [doc = " assert!(larger.contains_range(larger));"] # [doc = " assert!(smaller.contains_range(smaller));"] # [doc = " ```"] # [inline] pub fn contains_range (self , other : TextRange) -> bool { self . start () <= other . start () && other . end () <= self . end () } # [doc = " The range covered by both ranges, if it exists."] # [doc = " If the ranges touch but do not overlap, the output range is empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use text_size::*;"] # [doc = " assert_eq!("] # [doc = "     TextRange::intersect("] # [doc = "         TextRange::new(0.into(), 10.into()),"] # [doc = "         TextRange::new(5.into(), 15.into()),"] # [doc = "     ),"] # [doc = "     Some(TextRange::new(5.into(), 10.into())),"] # [doc = " );"] # [doc = " ```"] # [inline] pub fn intersect (self , other : TextRange) -> Option < TextRange > { let start = cmp :: max (self . start () , other . start ()) ; let end = cmp :: min (self . end () , other . end ()) ; if end < start { return None ; } Some (TextRange :: new (start , end)) } # [doc = " Extends the range to cover `other` as well."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use text_size::*;"] # [doc = " assert_eq!("] # [doc = "     TextRange::cover("] # [doc = "         TextRange::new(0.into(), 5.into()),"] # [doc = "         TextRange::new(15.into(), 20.into()),"] # [doc = "     ),"] # [doc = "     TextRange::new(0.into(), 20.into()),"] # [doc = " );"] # [doc = " ```"] # [inline] pub fn cover (self , other : TextRange) -> TextRange { let start = cmp :: min (self . start () , other . start ()) ; let end = cmp :: max (self . end () , other . end ()) ; TextRange :: new (start , end) } # [doc = " Extends the range to cover `other` offsets as well."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use text_size::*;"] # [doc = " assert_eq!("] # [doc = "     TextRange::empty(0.into()).cover_offset(20.into()),"] # [doc = "     TextRange::new(0.into(), 20.into()),"] # [doc = " )"] # [doc = " ```"] # [inline] pub fn cover_offset (self , offset : TextSize) -> TextRange { self . cover (TextRange :: empty (offset)) } # [doc = " Add an offset to this range."] # [doc = ""] # [doc = " Note that this is not appropriate for changing where a `TextRange` is"] # [doc = " within some string; rather, it is for changing the reference anchor"] # [doc = " that the `TextRange` is measured against."] # [doc = ""] # [doc = " The unchecked version (`Add::add`) will _always_ panic on overflow,"] # [doc = " in contrast to primitive integers, which check in debug mode only."] # [inline] pub fn checked_add (self , offset : TextSize) -> Option < TextRange > { Some (TextRange { start : self . start . checked_add (offset) ? , end : self . end . checked_add (offset) ? , }) } # [doc = " Subtract an offset from this range."] # [doc = ""] # [doc = " Note that this is not appropriate for changing where a `TextRange` is"] # [doc = " within some string; rather, it is for changing the reference anchor"] # [doc = " that the `TextRange` is measured against."] # [doc = ""] # [doc = " The unchecked version (`Sub::sub`) will _always_ panic on overflow,"] # [doc = " in contrast to primitive integers, which check in debug mode only."] # [inline] pub fn checked_sub (self , offset : TextSize) -> Option < TextRange > { Some (TextRange { start : self . start . checked_sub (offset) ? , end : self . end . checked_sub (offset) ? , }) } # [doc = " Relative order of the two ranges (overlapping ranges are considered"] # [doc = " equal)."] # [doc = ""] # [doc = ""] # [doc = " This is useful when, for example, binary searching an array of disjoint"] # [doc = " ranges."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use text_size::*;"] # [doc = " # use std::cmp::Ordering;"] # [doc = ""] # [doc = " let a = TextRange::new(0.into(), 3.into());"] # [doc = " let b = TextRange::new(4.into(), 5.into());"] # [doc = " assert_eq!(a.ordering(b), Ordering::Less);"] # [doc = ""] # [doc = " let a = TextRange::new(0.into(), 3.into());"] # [doc = " let b = TextRange::new(3.into(), 5.into());"] # [doc = " assert_eq!(a.ordering(b), Ordering::Less);"] # [doc = ""] # [doc = " let a = TextRange::new(0.into(), 3.into());"] # [doc = " let b = TextRange::new(2.into(), 5.into());"] # [doc = " assert_eq!(a.ordering(b), Ordering::Equal);"] # [doc = ""] # [doc = " let a = TextRange::new(0.into(), 3.into());"] # [doc = " let b = TextRange::new(2.into(), 2.into());"] # [doc = " assert_eq!(a.ordering(b), Ordering::Equal);"] # [doc = ""] # [doc = " let a = TextRange::new(2.into(), 3.into());"] # [doc = " let b = TextRange::new(2.into(), 2.into());"] # [doc = " assert_eq!(a.ordering(b), Ordering::Greater);"] # [doc = " ```"] # [inline] pub fn ordering (self , other : TextRange) -> Ordering { if self . end () <= other . start () { Ordering :: Less } else if other . end () <= self . start () { Ordering :: Greater } else { Ordering :: Equal } } }
};
}
