// Generated macro for impl_99 (impl)
macro_rules! Depcrate_entryimpl_99 {
() => {
// Module: crate::entry
// Provides: {"impl_99"}
// Dependencies: {}
impl < 'a , R : Read > Entry < 'a , R > { # [doc = " Returns the path name for this entry."] # [doc = ""] # [doc = " This method may fail if the pathname is not valid Unicode and this is"] # [doc = " called on a Windows platform."] # [doc = ""] # [doc = " Note that this function will convert any `\\` characters to directory"] # [doc = " separators, and it will not always return the same value as"] # [doc = " `self.header().path()` as some archive formats have support for longer"] # [doc = " path names described in separate entries."] # [doc = ""] # [doc = " It is recommended to use this method instead of inspecting the `header`"] # [doc = " directly to ensure that various archive formats are handled correctly."] pub fn path (& self) -> io :: Result < Cow < '_ , Path > > { self . fields . path () } # [doc = " Returns the raw bytes listed for this entry."] # [doc = ""] # [doc = " Note that this function will convert any `\\` characters to directory"] # [doc = " separators, and it will not always return the same value as"] # [doc = " `self.header().path_bytes()` as some archive formats have support for"] # [doc = " longer path names described in separate entries."] pub fn path_bytes (& self) -> Cow < '_ , [u8] > { self . fields . path_bytes () } # [doc = " Returns the link name for this entry, if any is found."] # [doc = ""] # [doc = " This method may fail if the pathname is not valid Unicode and this is"] # [doc = " called on a Windows platform. `Ok(None)` being returned, however,"] # [doc = " indicates that the link name was not present."] # [doc = ""] # [doc = " Note that this function will convert any `\\` characters to directory"] # [doc = " separators, and it will not always return the same value as"] # [doc = " `self.header().link_name()` as some archive formats have support for"] # [doc = " longer path names described in separate entries."] # [doc = ""] # [doc = " It is recommended to use this method instead of inspecting the `header`"] # [doc = " directly to ensure that various archive formats are handled correctly."] pub fn link_name (& self) -> io :: Result < Option < Cow < '_ , Path > > > { self . fields . link_name () } # [doc = " Returns the link name for this entry, in bytes, if listed."] # [doc = ""] # [doc = " Note that this will not always return the same value as"] # [doc = " `self.header().link_name_bytes()` as some archive formats have support for"] # [doc = " longer path names described in separate entries."] pub fn link_name_bytes (& self) -> Option < Cow < '_ , [u8] > > { self . fields . link_name_bytes () } # [doc = " Returns an iterator over the pax extensions contained in this entry."] # [doc = ""] # [doc = " Pax extensions are a form of archive where extra metadata is stored in"] # [doc = " key/value pairs in entries before the entry they're intended to"] # [doc = " describe. For example this can be used to describe long file name or"] # [doc = " other metadata like atime/ctime/mtime in more precision."] # [doc = ""] # [doc = " The returned iterator will yield key/value pairs for each extension."] # [doc = ""] # [doc = " `None` will be returned if this entry does not indicate that it itself"] # [doc = " contains extensions, or if there were no previous extensions describing"] # [doc = " it."] # [doc = ""] # [doc = " Note that global pax extensions are intended to be applied to all"] # [doc = " archive entries."] # [doc = ""] # [doc = " Also note that this function will read the entire entry if the entry"] # [doc = " itself is a list of extensions."] pub fn pax_extensions (& mut self) -> io :: Result < Option < PaxExtensions < '_ > > > { self . fields . pax_extensions () } # [doc = " Returns access to the header of this entry in the archive."] # [doc = ""] # [doc = " This provides access to the metadata for this entry in the archive."] pub fn header (& self) -> & Header { & self . fields . header } # [doc = " Returns access to the size of this entry in the archive."] # [doc = ""] # [doc = " In the event the size is stored in a pax extension, that size value"] # [doc = " will be referenced. Otherwise, the entry size will be stored in the header."] pub fn size (& self) -> u64 { self . fields . size } # [doc = " Returns the starting position, in bytes, of the header of this entry in"] # [doc = " the archive."] # [doc = ""] # [doc = " The header is always a contiguous section of 512 bytes, so if the"] # [doc = " underlying reader implements `Seek`, then the slice from `header_pos` to"] # [doc = " `header_pos + 512` contains the raw header bytes."] pub fn raw_header_position (& self) -> u64 { self . fields . header_pos } # [doc = " Returns the starting position, in bytes, of the file of this entry in"] # [doc = " the archive."] # [doc = ""] # [doc = " If the file of this entry is continuous (e.g. not a sparse file), and"] # [doc = " if the underlying reader implements `Seek`, then the slice from"] # [doc = " `file_pos` to `file_pos + entry_size` contains the raw file bytes."] pub fn raw_file_position (& self) -> u64 { self . fields . file_pos } # [doc = " Writes this file to the specified location."] # [doc = ""] # [doc = " This function will write the entire contents of this file into the"] # [doc = " location specified by `dst`. Metadata will also be propagated to the"] # [doc = " path `dst`."] # [doc = ""] # [doc = " This function will create a file at the path `dst`, and it is required"] # [doc = " that the intermediate directories are created. Any existing file at the"] # [doc = " location `dst` will be overwritten."] # [doc = ""] # [doc = " > **Note**: This function does not have as many sanity checks as"] # [doc = " > `Archive::unpack` or `Entry::unpack_in`. As a result if you're"] # [doc = " > thinking of unpacking untrusted tarballs you may want to review the"] # [doc = " > implementations of the previous two functions and perhaps implement"] # [doc = " > similar logic yourself."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::fs::File;"] # [doc = " use tar::Archive;"] # [doc = ""] # [doc = " let mut ar = Archive::new(File::open(\"foo.tar\").unwrap());"] # [doc = ""] # [doc = " for (i, file) in ar.entries().unwrap().enumerate() {"] # [doc = "     let mut file = file.unwrap();"] # [doc = "     file.unpack(format!(\"file-{}\", i)).unwrap();"] # [doc = " }"] # [doc = " ```"] pub fn unpack < P : AsRef < Path > > (& mut self , dst : P) -> io :: Result < Unpacked > { self . fields . unpack (None , dst . as_ref ()) } # [doc = " Extracts this file under the specified path, avoiding security issues."] # [doc = ""] # [doc = " This function will write the entire contents of this file into the"] # [doc = " location obtained by appending the path of this file in the archive to"] # [doc = " `dst`, creating any intermediate directories if needed. Metadata will"] # [doc = " also be propagated to the path `dst`. Any existing file at the location"] # [doc = " `dst` will be overwritten."] # [doc = ""] # [doc = " This function carefully avoids writing outside of `dst`. If the file has"] # [doc = " a '..' in its path, this function will skip it and return false."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::fs::File;"] # [doc = " use tar::Archive;"] # [doc = ""] # [doc = " let mut ar = Archive::new(File::open(\"foo.tar\").unwrap());"] # [doc = ""] # [doc = " for (i, file) in ar.entries().unwrap().enumerate() {"] # [doc = "     let mut file = file.unwrap();"] # [doc = "     file.unpack_in(\"target\").unwrap();"] # [doc = " }"] # [doc = " ```"] pub fn unpack_in < P : AsRef < Path > > (& mut self , dst : P) -> io :: Result < bool > { self . fields . unpack_in (dst . as_ref ()) } # [doc = " Set the mask of the permission bits when unpacking this entry."] # [doc = ""] # [doc = " The mask will be inverted when applying against a mode, similar to how"] # [doc = " `umask` works on Unix. In logical notation it looks like:"] # [doc = ""] # [doc = " ```text"] # [doc = " new_mode = old_mode & (~mask)"] # [doc = " ```"] # [doc = ""] # [doc = " The mask is 0 by default and is currently only implemented on Unix."] pub fn set_mask (& mut self , mask : u32) { self . fields . mask = mask ; } # [doc = " Indicate whether extended file attributes (xattrs on Unix) are preserved"] # [doc = " when unpacking this entry."] # [doc = ""] # [doc = " This flag is disabled by default and is currently only implemented on"] # [doc = " Unix using xattr support. This may eventually be implemented for"] # [doc = " Windows, however, if other archive implementations are found which do"] # [doc = " this as well."] pub fn set_unpack_xattrs (& mut self , unpack_xattrs : bool) { self . fields . unpack_xattrs = unpack_xattrs ; } # [doc = " Indicate whether extended permissions (like suid on Unix) are preserved"] # [doc = " when unpacking this entry."] # [doc = ""] # [doc = " This flag is disabled by default and is currently only implemented on"] # [doc = " Unix."] pub fn set_preserve_permissions (& mut self , preserve : bool) { self . fields . preserve_permissions = preserve ; } # [doc = " Indicate whether access time information is preserved when unpacking"] # [doc = " this entry."] # [doc = ""] # [doc = " This flag is enabled by default."] pub fn set_preserve_mtime (& mut self , preserve : bool) { self . fields . preserve_mtime = preserve ; } }
};
}
