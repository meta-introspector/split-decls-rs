// Generated macro for impl_53 (impl)
macro_rules! Depcrate_builderimpl_53 {
() => {
// Module: crate::builder
// Provides: {"impl_53"}
// Dependencies: {}
impl < W : Write > Builder < W > { # [doc = " Create a new archive builder with the underlying object as the"] # [doc = " destination of all data written. The builder will use"] # [doc = " `HeaderMode::Complete` by default."] pub fn new (obj : W) -> Builder < W > { Builder { options : BuilderOptions { mode : HeaderMode :: Complete , follow : true , sparse : true , } , finished : false , obj : Some (obj) , } } # [doc = " Changes the HeaderMode that will be used when reading fs Metadata for"] # [doc = " methods that implicitly read metadata for an input Path. Notably, this"] # [doc = " does _not_ apply to `append(Header)`."] pub fn mode (& mut self , mode : HeaderMode) { self . options . mode = mode ; } # [doc = " Follow symlinks, archiving the contents of the file they point to rather"] # [doc = " than adding a symlink to the archive. Defaults to true."] # [doc = ""] # [doc = " When true, it exhibits the same behavior as GNU `tar` command's"] # [doc = " `--dereference` or `-h` options <https://man7.org/linux/man-pages/man1/tar.1.html>."] pub fn follow_symlinks (& mut self , follow : bool) { self . options . follow = follow ; } # [doc = " Handle sparse files efficiently, if supported by the underlying"] # [doc = " filesystem. When true, sparse file information is read from disk and"] # [doc = " empty segments are omitted from the archive. Defaults to true."] pub fn sparse (& mut self , sparse : bool) { self . options . sparse = sparse ; } # [doc = " Gets shared reference to the underlying object."] pub fn get_ref (& self) -> & W { self . obj . as_ref () . unwrap () } # [doc = " Gets mutable reference to the underlying object."] # [doc = ""] # [doc = " Note that care must be taken while writing to the underlying"] # [doc = " object. But, e.g. `get_mut().flush()` is claimed to be safe and"] # [doc = " useful in the situations when one needs to be ensured that"] # [doc = " tar entry was flushed to the disk."] pub fn get_mut (& mut self) -> & mut W { self . obj . as_mut () . unwrap () } # [doc = " Unwrap this archive, returning the underlying object."] # [doc = ""] # [doc = " This function will finish writing the archive if the `finish` function"] # [doc = " hasn't yet been called, returning any I/O error which happens during"] # [doc = " that operation."] pub fn into_inner (mut self) -> io :: Result < W > { if ! self . finished { self . finish () ? ; } Ok (self . obj . take () . unwrap ()) } # [doc = " Adds a new entry to this archive."] # [doc = ""] # [doc = " This function will append the header specified, followed by contents of"] # [doc = " the stream specified by `data`. To produce a valid archive the `size`"] # [doc = " field of `header` must be the same as the length of the stream that's"] # [doc = " being written. Additionally the checksum for the header should have been"] # [doc = " set via the `set_cksum` method."] # [doc = ""] # [doc = " Note that this will not attempt to seek the archive to a valid position,"] # [doc = " so if the archive is in the middle of a read or some other similar"] # [doc = " operation then this may corrupt the archive."] # [doc = ""] # [doc = " Also note that after all entries have been written to an archive the"] # [doc = " `finish` function needs to be called to finish writing the archive."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will return an error for any intermittent I/O error which"] # [doc = " occurs when either reading or writing."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use tar::{Builder, Header};"] # [doc = ""] # [doc = " let mut header = Header::new_gnu();"] # [doc = " header.set_path(\"foo\").unwrap();"] # [doc = " header.set_size(4);"] # [doc = " header.set_cksum();"] # [doc = ""] # [doc = " let mut data: &[u8] = &[1, 2, 3, 4];"] # [doc = ""] # [doc = " let mut ar = Builder::new(Vec::new());"] # [doc = " ar.append(&header, data).unwrap();"] # [doc = " let data = ar.into_inner().unwrap();"] # [doc = " ```"] pub fn append < R : Read > (& mut self , header : & Header , mut data : R) -> io :: Result < () > { append (self . get_mut () , header , & mut data) } # [doc = " Adds a new entry to this archive with the specified path."] # [doc = ""] # [doc = " This function will set the specified path in the given header, which may"] # [doc = " require appending a GNU long-name extension entry to the archive first."] # [doc = " The checksum for the header will be automatically updated via the"] # [doc = " `set_cksum` method after setting the path. No other metadata in the"] # [doc = " header will be modified."] # [doc = ""] # [doc = " Then it will append the header, followed by contents of the stream"] # [doc = " specified by `data`. To produce a valid archive the `size` field of"] # [doc = " `header` must be the same as the length of the stream that's being"] # [doc = " written."] # [doc = ""] # [doc = " Note that this will not attempt to seek the archive to a valid position,"] # [doc = " so if the archive is in the middle of a read or some other similar"] # [doc = " operation then this may corrupt the archive."] # [doc = ""] # [doc = " Also note that after all entries have been written to an archive the"] # [doc = " `finish` function needs to be called to finish writing the archive."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will return an error for any intermittent I/O error which"] # [doc = " occurs when either reading or writing."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use tar::{Builder, Header};"] # [doc = ""] # [doc = " let mut header = Header::new_gnu();"] # [doc = " header.set_size(4);"] # [doc = " header.set_cksum();"] # [doc = ""] # [doc = " let mut data: &[u8] = &[1, 2, 3, 4];"] # [doc = ""] # [doc = " let mut ar = Builder::new(Vec::new());"] # [doc = " ar.append_data(&mut header, \"really/long/path/to/foo\", data).unwrap();"] # [doc = " let data = ar.into_inner().unwrap();"] # [doc = " ```"] pub fn append_data < P : AsRef < Path > , R : Read > (& mut self , header : & mut Header , path : P , data : R ,) -> io :: Result < () > { prepare_header_path (self . get_mut () , header , path . as_ref ()) ? ; header . set_cksum () ; self . append (header , data) } # [doc = " Adds a new entry to this archive and returns an [`EntryWriter`] for"] # [doc = " adding its contents."] # [doc = ""] # [doc = " This function is similar to [`Self::append_data`] but returns a"] # [doc = " [`io::Write`] implementation instead of taking data as a parameter."] # [doc = ""] # [doc = " Similar constraints around the position of the archive and completion"] # [doc = " apply as with [`Self::append_data`]. It requires the underlying writer"] # [doc = " to implement [`Seek`] to update the header after writing the data."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will return an error for any intermittent I/O error which"] # [doc = " occurs when either reading or writing."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::io::Cursor;"] # [doc = " use std::io::Write as _;"] # [doc = " use tar::{Builder, Header};"] # [doc = ""] # [doc = " let mut header = Header::new_gnu();"] # [doc = ""] # [doc = " let mut ar = Builder::new(Cursor::new(Vec::new()));"] # [doc = " let mut entry = ar.append_writer(&mut header, \"hi.txt\").unwrap();"] # [doc = " entry.write_all(b\"Hello, \").unwrap();"] # [doc = " entry.write_all(b\"world!\\n\").unwrap();"] # [doc = " entry.finish().unwrap();"] # [doc = " ```"] pub fn append_writer < 'a , P : AsRef < Path > > (& 'a mut self , header : & 'a mut Header , path : P ,) -> io :: Result < EntryWriter < 'a > > where W : Seek , { EntryWriter :: start (self . get_mut () , header , path . as_ref ()) } # [doc = " Adds a new link (symbolic or hard) entry to this archive with the specified path and target."] # [doc = ""] # [doc = " This function is similar to [`Self::append_data`] which supports long filenames,"] # [doc = " but also supports long link targets using GNU extensions if necessary."] # [doc = " You must set the entry type to either [`EntryType::Link`] or [`EntryType::Symlink`]."] # [doc = " The `set_cksum` method will be invoked after setting the path. No other metadata in the"] # [doc = " header will be modified."] # [doc = ""] # [doc = " If you are intending to use GNU extensions, you must use this method over calling"] # [doc = " [`Header::set_link_name`] because that function will fail on long links."] # [doc = ""] # [doc = " Similar constraints around the position of the archive and completion"] # [doc = " apply as with [`Self::append_data`]."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will return an error for any intermittent I/O error which"] # [doc = " occurs when either reading or writing."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use tar::{Builder, Header, EntryType};"] # [doc = ""] # [doc = " let mut ar = Builder::new(Vec::new());"] # [doc = " let mut header = Header::new_gnu();"] # [doc = " header.set_username(\"foo\");"] # [doc = " header.set_entry_type(EntryType::Symlink);"] # [doc = " header.set_size(0);"] # [doc = " ar.append_link(&mut header, \"really/long/path/to/foo\", \"other/really/long/target\").unwrap();"] # [doc = " let data = ar.into_inner().unwrap();"] # [doc = " ```"] pub fn append_link < P : AsRef < Path > , T : AsRef < Path > > (& mut self , header : & mut Header , path : P , target : T ,) -> io :: Result < () > { self . _append_link (header , path . as_ref () , target . as_ref ()) } fn _append_link (& mut self , header : & mut Header , path : & Path , target : & Path) -> io :: Result < () > { prepare_header_path (self . get_mut () , header , path) ? ; prepare_header_link (self . get_mut () , header , target) ? ; header . set_cksum () ; self . append (header , std :: io :: empty ()) } # [doc = " Adds a file on the local filesystem to this archive."] # [doc = ""] # [doc = " This function will open the file specified by `path` and insert the file"] # [doc = " into the archive with the appropriate metadata set, returning any I/O"] # [doc = " error which occurs while writing. The path name for the file inside of"] # [doc = " this archive will be the same as `path`, and it is required that the"] # [doc = " path is a relative path."] # [doc = ""] # [doc = " Note that this will not attempt to seek the archive to a valid position,"] # [doc = " so if the archive is in the middle of a read or some other similar"] # [doc = " operation then this may corrupt the archive."] # [doc = ""] # [doc = " Also note that after all files have been written to an archive the"] # [doc = " `finish` function needs to be called to finish writing the archive."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use tar::Builder;"] # [doc = ""] # [doc = " let mut ar = Builder::new(Vec::new());"] # [doc = ""] # [doc = " ar.append_path(\"foo/bar.txt\").unwrap();"] # [doc = " ```"] pub fn append_path < P : AsRef < Path > > (& mut self , path : P) -> io :: Result < () > { let options = self . options ; append_path_with_name (self . get_mut () , path . as_ref () , None , options) } # [doc = " Adds a file on the local filesystem to this archive under another name."] # [doc = ""] # [doc = " This function will open the file specified by `path` and insert the file"] # [doc = " into the archive as `name` with appropriate metadata set, returning any"] # [doc = " I/O error which occurs while writing. The path name for the file inside"] # [doc = " of this archive will be `name` is required to be a relative path."] # [doc = ""] # [doc = " Note that this will not attempt to seek the archive to a valid position,"] # [doc = " so if the archive is in the middle of a read or some other similar"] # [doc = " operation then this may corrupt the archive."] # [doc = ""] # [doc = " Note if the `path` is a directory. This will just add an entry to the archive,"] # [doc = " rather than contents of the directory."] # [doc = ""] # [doc = " Also note that after all files have been written to an archive the"] # [doc = " `finish` function needs to be called to finish writing the archive."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use tar::Builder;"] # [doc = ""] # [doc = " let mut ar = Builder::new(Vec::new());"] # [doc = ""] # [doc = " // Insert the local file \"foo/bar.txt\" in the archive but with the name"] # [doc = " // \"bar/foo.txt\"."] # [doc = " ar.append_path_with_name(\"foo/bar.txt\", \"bar/foo.txt\").unwrap();"] # [doc = " ```"] pub fn append_path_with_name < P : AsRef < Path > , N : AsRef < Path > > (& mut self , path : P , name : N ,) -> io :: Result < () > { let options = self . options ; append_path_with_name (self . get_mut () , path . as_ref () , Some (name . as_ref ()) , options) } # [doc = " Adds a file to this archive with the given path as the name of the file"] # [doc = " in the archive."] # [doc = ""] # [doc = " This will use the metadata of `file` to populate a `Header`, and it will"] # [doc = " then append the file to the archive with the name `path`."] # [doc = ""] # [doc = " Note that this will not attempt to seek the archive to a valid position,"] # [doc = " so if the archive is in the middle of a read or some other similar"] # [doc = " operation then this may corrupt the archive."] # [doc = ""] # [doc = " Also note that after all files have been written to an archive the"] # [doc = " `finish` function needs to be called to finish writing the archive."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use std::fs::File;"] # [doc = " use tar::Builder;"] # [doc = ""] # [doc = " let mut ar = Builder::new(Vec::new());"] # [doc = ""] # [doc = " // Open the file at one location, but insert it into the archive with a"] # [doc = " // different name."] # [doc = " let mut f = File::open(\"foo/bar/baz.txt\").unwrap();"] # [doc = " ar.append_file(\"bar/baz.txt\", &mut f).unwrap();"] # [doc = " ```"] pub fn append_file < P : AsRef < Path > > (& mut self , path : P , file : & mut fs :: File) -> io :: Result < () > { let options = self . options ; append_file (self . get_mut () , path . as_ref () , file , options) } # [doc = " Adds a directory to this archive with the given path as the name of the"] # [doc = " directory in the archive."] # [doc = ""] # [doc = " This will use `stat` to populate a `Header`, and it will then append the"] # [doc = " directory to the archive with the name `path`."] # [doc = ""] # [doc = " Note that this will not attempt to seek the archive to a valid position,"] # [doc = " so if the archive is in the middle of a read or some other similar"] # [doc = " operation then this may corrupt the archive."] # [doc = ""] # [doc = " Note this will not add the contents of the directory to the archive."] # [doc = " See `append_dir_all` for recursively adding the contents of the directory."] # [doc = ""] # [doc = " Also note that after all files have been written to an archive the"] # [doc = " `finish` function needs to be called to finish writing the archive."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::fs;"] # [doc = " use tar::Builder;"] # [doc = ""] # [doc = " let mut ar = Builder::new(Vec::new());"] # [doc = ""] # [doc = " // Use the directory at one location, but insert it into the archive"] # [doc = " // with a different name."] # [doc = " ar.append_dir(\"bardir\", \".\").unwrap();"] # [doc = " ```"] pub fn append_dir < P , Q > (& mut self , path : P , src_path : Q) -> io :: Result < () > where P : AsRef < Path > , Q : AsRef < Path > , { let options = self . options ; append_dir (self . get_mut () , path . as_ref () , src_path . as_ref () , options) } # [doc = " Adds a directory and all of its contents (recursively) to this archive"] # [doc = " with the given path as the name of the directory in the archive."] # [doc = ""] # [doc = " Note that this will not attempt to seek the archive to a valid position,"] # [doc = " so if the archive is in the middle of a read or some other similar"] # [doc = " operation then this may corrupt the archive."] # [doc = ""] # [doc = " Also note that after all files have been written to an archive the"] # [doc = " `finish` or `into_inner` function needs to be called to finish"] # [doc = " writing the archive."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use std::fs;"] # [doc = " use tar::Builder;"] # [doc = ""] # [doc = " let mut ar = Builder::new(Vec::new());"] # [doc = ""] # [doc = " // Use the directory at one location (\".\"), but insert it into the archive"] # [doc = " // with a different name (\"bardir\")."] # [doc = " ar.append_dir_all(\"bardir\", \".\").unwrap();"] # [doc = " ar.finish().unwrap();"] # [doc = " ```"] # [doc = ""] # [doc = " Use `append_dir_all` with an empty string as the first path argument to"] # [doc = " create an archive from all files in a directory without renaming."] # [doc = ""] # [doc = " ```"] # [doc = " use std::fs;"] # [doc = " use std::path::PathBuf;"] # [doc = " use tar::{Archive, Builder};"] # [doc = ""] # [doc = " let tmpdir = tempfile::tempdir().unwrap();"] # [doc = " let path = tmpdir.path();"] # [doc = " fs::write(path.join(\"a.txt\"), b\"hello\").unwrap();"] # [doc = " fs::write(path.join(\"b.txt\"), b\"world\").unwrap();"] # [doc = ""] # [doc = " // Create a tarball from the files in the directory"] # [doc = " let mut ar = Builder::new(Vec::new());"] # [doc = " ar.append_dir_all(\"\", path).unwrap();"] # [doc = ""] # [doc = " // List files in the archive"] # [doc = " let archive = ar.into_inner().unwrap();"] # [doc = " let archived_files = Archive::new(archive.as_slice())"] # [doc = "     .entries()"] # [doc = "     .unwrap()"] # [doc = "     .map(|entry| entry.unwrap().path().unwrap().into_owned())"] # [doc = "     .collect::<Vec<_>>();"] # [doc = ""] # [doc = " assert!(archived_files.contains(&PathBuf::from(\"a.txt\")));"] # [doc = " assert!(archived_files.contains(&PathBuf::from(\"b.txt\")));"] # [doc = " ```"] pub fn append_dir_all < P , Q > (& mut self , path : P , src_path : Q) -> io :: Result < () > where P : AsRef < Path > , Q : AsRef < Path > , { let options = self . options ; append_dir_all (self . get_mut () , path . as_ref () , src_path . as_ref () , options) } # [doc = " Finish writing this archive, emitting the termination sections."] # [doc = ""] # [doc = " This function should only be called when the archive has been written"] # [doc = " entirely and if an I/O error happens the underlying object still needs"] # [doc = " to be acquired."] # [doc = ""] # [doc = " In most situations the `into_inner` method should be preferred."] pub fn finish (& mut self) -> io :: Result < () > { if self . finished { return Ok (()) ; } self . finished = true ; self . get_mut () . write_all (& [0 ; 1024]) } }
};
}
