macro_rules! deps {
    () => {
        DecodePaddingMode!();
        GeneralPurposeConfig!();
    };
}

macro_rules! impl_110 {
    () => {
        deps!();
        impl GeneralPurposeConfig { # [doc = " Create a new config with `padding` = `true`, `decode_allow_trailing_bits` = `false`, and"] # [doc = " `decode_padding_mode = DecodePaddingMode::RequireCanonicalPadding`."] # [doc = ""] # [doc = " This probably matches most people's expectations, but consider disabling padding to save"] # [doc = " a few bytes unless you specifically need it for compatibility with some legacy system."] # [must_use] pub const fn new () -> Self { Self { encode_padding : true , decode_allow_trailing_bits : false , decode_padding_mode : DecodePaddingMode :: RequireCanonical , } } # [doc = " Create a new config based on `self` with an updated `padding` setting."] # [doc = ""] # [doc = " If `padding` is `true`, encoding will append either 1 or 2 `=` padding characters as needed"] # [doc = " to produce an output whose length is a multiple of 4."] # [doc = ""] # [doc = " Padding is not needed for correct decoding and only serves to waste bytes, but it's in the"] # [doc = " [spec](https://datatracker.ietf.org/doc/html/rfc4648#section-3.2)."] # [doc = ""] # [doc = " For new applications, consider not using padding if the decoders you're using don't require"] # [doc = " padding to be present."] # [must_use] pub const fn with_encode_padding (self , padding : bool) -> Self { Self { encode_padding : padding , .. self } } # [doc = " Create a new config based on `self` with an updated `decode_allow_trailing_bits` setting."] # [doc = ""] # [doc = " Most users will not need to configure this. It's useful if you need to decode base64"] # [doc = " produced by a buggy encoder that has bits set in the unused space on the last base64"] # [doc = " character as per [forgiving-base64 decode](https://infra.spec.whatwg.org/#forgiving-base64-decode)."] # [doc = " If invalid trailing bits are present and this is `true`, those bits will"] # [doc = " be silently ignored, else `DecodeError::InvalidLastSymbol` will be emitted."] # [must_use] pub const fn with_decode_allow_trailing_bits (self , allow : bool) -> Self { Self { decode_allow_trailing_bits : allow , .. self } } # [doc = " Create a new config based on `self` with an updated `decode_padding_mode` setting."] # [doc = ""] # [doc = " Padding is not useful in terms of representing encoded data -- it makes no difference to"] # [doc = " the decoder if padding is present or not, so if you have some un-padded input to decode, it"] # [doc = " is perfectly fine to use `DecodePaddingMode::Indifferent` to prevent errors from being"] # [doc = " emitted."] # [doc = ""] # [doc = " However, since in practice"] # [doc = " [people who learned nothing from BER vs DER seem to expect base64 to have one canonical encoding](https://eprint.iacr.org/2022/361),"] # [doc = " the default setting is the stricter `DecodePaddingMode::RequireCanonicalPadding`."] # [doc = ""] # [doc = " Or, if \"canonical\" in your circumstance means _no_ padding rather than padding to the"] # [doc = " next multiple of four, there's `DecodePaddingMode::RequireNoPadding`."] # [must_use] pub const fn with_decode_padding_mode (self , mode : DecodePaddingMode) -> Self { Self { decode_padding_mode : mode , .. self } } }
    };
}

impl_110!()