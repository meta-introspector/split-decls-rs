// Generated macro for impl_65 (impl)
macro_rules! Depcrate_write_encoderimpl_65 {
() => {
// Module: crate::write::encoder
// Provides: {"impl_65"}
// Dependencies: {}
impl < 'e , E : Engine , W : io :: Write > EncoderWriter < 'e , E , W > { # [doc = " Create a new encoder that will write to the provided delegate writer."] pub fn new (delegate : W , engine : & 'e E) -> EncoderWriter < 'e , E , W > { EncoderWriter { engine , delegate : Some (delegate) , extra_input : [0u8 ; MIN_ENCODE_CHUNK_SIZE] , extra_input_occupied_len : 0 , output : [0u8 ; BUF_SIZE] , output_occupied_len : 0 , panicked : false , } } # [doc = " Encode all remaining buffered data and write it, including any trailing incomplete input"] # [doc = " triples and associated padding."] # [doc = ""] # [doc = " Once this succeeds, no further writes or calls to this method are allowed."] # [doc = ""] # [doc = " This may write to the delegate writer multiple times if the delegate writer does not accept"] # [doc = " all input provided to its `write` each invocation."] # [doc = ""] # [doc = " If you don't care about error handling, it is not necessary to call this function, as the"] # [doc = " equivalent finalization is done by the Drop impl."] # [doc = ""] # [doc = " Returns the writer that this was constructed around."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " The first error that is not of `ErrorKind::Interrupted` will be returned."] pub fn finish (& mut self) -> Result < W > { assert ! (self . delegate . is_some () , "Encoder has already had finish() called") ; self . write_final_leftovers () ? ; let writer = self . delegate . take () . expect ("Writer must be present") ; Ok (writer) } # [doc = " Write any remaining buffered data to the delegate writer."] fn write_final_leftovers (& mut self) -> Result < () > { if self . delegate . is_none () { return Ok (()) ; } self . write_all_encoded_output () ? ; if self . extra_input_occupied_len > 0 { let encoded_len = self . engine . encode_slice (& self . extra_input [.. self . extra_input_occupied_len] , & mut self . output [..] ,) . expect ("buffer is large enough") ; self . output_occupied_len = encoded_len ; self . write_all_encoded_output () ? ; self . extra_input_occupied_len = 0 ; } Ok (()) } # [doc = " Write as much of the encoded output to the delegate writer as it will accept, and store the"] # [doc = " leftovers to be attempted at the next `write()` call. Updates `self.output_occupied_len`."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Errors from the delegate writer are returned. In the case of an error,"] # [doc = " `self.output_occupied_len` will not be updated, as errors from `write` are specified to mean"] # [doc = " that no write took place."] fn write_to_delegate (& mut self , current_output_len : usize) -> Result < () > { self . panicked = true ; let res = self . delegate . as_mut () . expect ("Writer must be present") . write (& self . output [.. current_output_len]) ; self . panicked = false ; res . map (| consumed | { debug_assert ! (consumed <= current_output_len) ; if consumed < current_output_len { self . output_occupied_len = current_output_len . checked_sub (consumed) . unwrap () ; self . output . rotate_left (consumed) ; } else { self . output_occupied_len = 0 ; } }) } # [doc = " Write all buffered encoded output. If this returns `Ok`, `self.output_occupied_len` is `0`."] # [doc = ""] # [doc = " This is basically `write_all` for the remaining buffered data but without the undesirable"] # [doc = " abort-on-`Ok(0)` behavior."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Any error emitted by the delegate writer abort the write loop and is returned, unless it's"] # [doc = " `Interrupted`, in which case the error is ignored and writes will continue."] fn write_all_encoded_output (& mut self) -> Result < () > { while self . output_occupied_len > 0 { let remaining_len = self . output_occupied_len ; match self . write_to_delegate (remaining_len) { Err (ref e) if e . kind () == ErrorKind :: Interrupted => { } Err (e) => return Err (e) , Ok (()) => { } } ; } debug_assert_eq ! (0 , self . output_occupied_len) ; Ok (()) } # [doc = " Unwraps this `EncoderWriter`, returning the base writer it writes base64 encoded output"] # [doc = " to."] # [doc = ""] # [doc = " Normally this method should not be needed, since `finish()` returns the inner writer if"] # [doc = " it completes successfully. That will also ensure all data has been flushed, which the"] # [doc = " `into_inner()` function does *not* do."] # [doc = ""] # [doc = " Calling this method after `finish()` has completed successfully will panic, since the"] # [doc = " writer has already been returned."] # [doc = ""] # [doc = " This method may be useful if the writer implements additional APIs beyond the `Write`"] # [doc = " trait. Note that the inner writer might be in an error state or have an incomplete"] # [doc = " base64 string written to it."] pub fn into_inner (mut self) -> W { self . delegate . take () . expect ("Encoder has already had finish() called") } }
};
}
