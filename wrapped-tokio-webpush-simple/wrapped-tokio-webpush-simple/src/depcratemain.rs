// Generated macro for main (function)
macro_rules! Depcratemain {
() => {
// Module: crate
// Provides: {"main"}
// Dependencies: {}
fn main () { let mut core = Core :: new () . unwrap () ; let handle = core . handle () ; let ws_listener = TcpListener :: bind (& "127.0.0.1:8080" . parse () . unwrap () , & handle) . unwrap () ; let cx = Rc :: new (Context { channels : RefCell :: new (HashMap :: new ()) , uaids : RefCell :: new (HashMap :: new ()) , }) ; let ws_srv = ws_listener . incoming () . for_each (| (socket , addr) | { let ws = accept_async (socket) . from_err () ; let client = ws . and_then (move | ws | { let ws = ws . from_err () . and_then (parse) . with (serialize) ; Client :: handshake (ws) }) ; let handshake_timeout = Duration :: new (30 , 0) ; let client = timeout (client , handshake_timeout , & handle) ; let handle2 = handle . clone () ; let cx2 = cx . clone () ; let work = client . and_then (move | (ws , client , rx) | { let uaid = client . uaid ; client . process (ws , cx2 . clone () , rx , & handle2) . then (move | res | { let mut uaids = cx2 . uaids . borrow_mut () ; let mut channels = cx2 . channels . borrow_mut () ; for id in uaids . remove (& uaid) . expect ("uaid not registered") . channel_ids { channels . remove (& id) . expect ("uaid pointed to missing channel") ; } return res }) }) ; handle . spawn (work . then (move | result | { match result { Ok (()) => println ! ("peer {} gone" , addr) , Err (e) => { println ! ("{} error: {}" , addr , e) ; for e in e . iter () . skip (1) { println ! ("\t{}" , e) ; } } } Ok (()) })) ; Ok (()) }) ; let push_listener = TcpListener :: bind (& "127.0.0.1:8081" . parse () . unwrap () , & handle) . unwrap () ; let proto = Http :: new () ; let cx = cx . clone () ; let push_srv = push_listener . incoming () . for_each (| (socket , addr) | { proto . bind_connection (& handle , socket , addr , Push (cx . clone ())) ; Ok (()) }) ; core . run (ws_srv . join (push_srv)) . ok () . unwrap () ; }
};
}
