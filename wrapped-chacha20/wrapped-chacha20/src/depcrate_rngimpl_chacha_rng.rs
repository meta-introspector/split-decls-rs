// Generated macro for impl_chacha_rng (macro)
macro_rules! Depcrate_rngimpl_chacha_rng {
() => {
// Module: crate::rng
// Provides: {"impl_chacha_rng"}
// Dependencies: {}
macro_rules ! impl_chacha_rng { ($ ChaChaXRng : ident , $ ChaChaXCore : ident , $ rounds : ident , $ abst : ident) => { # [doc = " A cryptographically secure random number generator that uses the ChaCha algorithm."] # [doc = ""] # [doc = " ChaCha is a stream cipher designed by Daniel J. Bernstein[^1], that we use as an RNG. It is"] # [doc = " an improved variant of the Salsa20 cipher family, which was selected as one of the \"stream"] # [doc = " ciphers suitable for widespread adoption\" by eSTREAM[^2]."] # [doc = ""] # [doc = " ChaCha uses add-rotate-xor (ARX) operations as its basis. These are safe against timing"] # [doc = " attacks, although that is mostly a concern for ciphers and not for RNGs. We provide a SIMD"] # [doc = " implementation to support high throughput on a variety of common hardware platforms."] # [doc = ""] # [doc = " With the ChaCha algorithm it is possible to choose the number of rounds the core algorithm"] # [doc = " should run. The number of rounds is a tradeoff between performance and security, where 8"] # [doc = " rounds is the minimum potentially secure configuration, and 20 rounds is widely used as a"] # [doc = " conservative choice."] # [doc = ""] # [doc = " We use a 64-bit counter and 64-bit stream identifier as in Bernstein's implementation"] # [doc = " except that we use a stream identifier in place of a nonce. A 64-bit counter over 64-byte"] # [doc = " (16 word) blocks allows 1 ZiB of output before cycling, and the stream identifier allows"] # [doc = " 2<sup>64</sup> unique streams of output per seed. Both counter and stream are initialized"] # [doc = " to zero but may be set via the `set_word_pos` and `set_stream` methods."] # [doc = ""] # [doc = " The word layout is:"] # [doc = ""] # [doc = " ```text"] # [doc = " constant  constant  constant  constant"] # [doc = " seed      seed      seed      seed"] # [doc = " seed      seed      seed      seed"] # [doc = " counter   counter   stream_id stream_id"] # [doc = " ```"] # [doc = " This implementation uses an output buffer of sixteen `u32` words, and uses"] # [doc = " [`BlockRng`] to implement the [`RngCore`] methods."] # [doc = ""] # [doc = " # Example for `ChaCha20Rng`"] # [doc = ""] # [doc = " ```rust"] # [doc = " use chacha20::ChaCha20Rng;"] # [doc = " // use rand_core traits"] # [doc = " use rand_core::{SeedableRng, RngCore};"] # [doc = ""] # [doc = " // the following inputs are examples and are neither"] # [doc = " // recommended nor suggested values"] # [doc = ""] # [doc = " let seed = [42u8; 32];"] # [doc = " let mut rng = ChaCha20Rng::from_seed(seed);"] # [doc = " rng.set_stream(100);"] # [doc = ""] # [doc = " // you can also use a [u8; 8] in `.set_stream()`"] # [doc = " rng.set_stream([3u8; 8]);"] # [doc = " // or a [u32; 2]"] # [doc = " rng.set_stream([4u32; 2]);"] # [doc = ""] # [doc = " rng.set_word_pos(5);"] # [doc = ""] # [doc = " let x = rng.next_u32();"] # [doc = " let mut array = [0u8; 32];"] # [doc = " rng.fill_bytes(&mut array);"] # [doc = ""] # [doc = " // If you need to zeroize the RNG's buffer, ensure that \"zeroize\""] # [doc = " // feature is enabled in Cargo.toml, and then it will zeroize on"] # [doc = " // drop automatically"] # [doc = " # #[cfg(feature = \"zeroize\")]"] # [doc = " use zeroize::Zeroize;"] # [doc = " ```"] # [doc = ""] # [doc = " The other Rngs from this crate are initialized similarly."] # [doc = ""] # [doc = " [^1]: D. J. Bernstein, [*ChaCha, a variant of Salsa20*](https://cr.yp.to/chacha.html)"] # [doc = ""] # [doc = " [^2]: [eSTREAM: the ECRYPT Stream Cipher Project](http://www.ecrypt.eu.org/stream/)"] pub struct $ ChaChaXRng { # [doc = " The ChaChaCore struct"] pub core : BlockRng <$ ChaChaXCore >, } # [doc = " The ChaCha core random number generator"] pub struct $ ChaChaXCore (ChaChaCore <$ rounds , Legacy >) ; impl SeedableRng for $ ChaChaXCore { type Seed = Seed ; # [inline] fn from_seed (seed : Self :: Seed) -> Self { Self (ChaChaCore ::<$ rounds , Legacy >:: new (seed . as_ref () , & [0u8 ; 8])) } } impl SeedableRng for $ ChaChaXRng { type Seed = [u8 ; 32] ; # [inline] fn from_seed (seed : Self :: Seed) -> Self { Self { core : BlockRng :: new ($ ChaChaXCore :: from_seed (seed . into ())) , } } } impl RngCore for $ ChaChaXRng { # [inline] fn next_u32 (& mut self) -> u32 { self . core . next_u32 () } # [inline] fn next_u64 (& mut self) -> u64 { self . core . next_u64 () } # [inline] fn fill_bytes (& mut self , dest : & mut [u8]) { self . core . fill_bytes (dest) } } impl CryptoBlockRng for $ ChaChaXCore { } impl CryptoRng for $ ChaChaXRng { } # [cfg (feature = "zeroize")] impl ZeroizeOnDrop for $ ChaChaXCore { } # [cfg (feature = "zeroize")] impl ZeroizeOnDrop for $ ChaChaXRng { } impl Debug for $ ChaChaXRng { fn fmt (& self , f : & mut core :: fmt :: Formatter <'_ >) -> core :: fmt :: Result { write ! (f , "ChaChaXCore {{}}") } } impl $ ChaChaXRng { # [doc = " Get the offset from the start of the stream, in 32-bit words."] # [doc = ""] # [doc = " Since the generated blocks are 64 words (2<sup>6</sup>) long and the"] # [doc = " counter is 64-bits, the offset is a 68-bit number. Sub-word offsets are"] # [doc = " not supported, hence the result can simply be multiplied by 4 to get a"] # [doc = " byte-offset."] # [inline] pub fn get_word_pos (& self) -> u128 { let mut block_counter = (u64 :: from (self . core . core . 0 . state [13]) << 32) | u64 :: from (self . core . core . 0 . state [12]) ; block_counter = block_counter . wrapping_sub (BUF_BLOCKS as u64) ; let word_pos = block_counter as u128 * BLOCK_WORDS as u128 + self . core . index () as u128 ; word_pos & ((1 << 68) - 1) } # [doc = " Set the offset from the start of the stream, in 32-bit words."] # [doc = ""] # [doc = " As with `get_word_pos`, we use a 68-bit number. Since the generator"] # [doc = " simply cycles at the end of its period (1 ZiB), we ignore the upper"] # [doc = " 60 bits."] # [inline] pub fn set_word_pos (& mut self , word_offset : u128) { let index = (word_offset & 0b1111) as usize ; let counter = word_offset >> 4 ; self . core . core . 0 . state [12] = counter as u32 ; self . core . core . 0 . state [13] = (counter >> 32) as u32 ; self . core . generate_and_set (index) ; } # [doc = " Set the block pos and reset the RNG's index."] # [doc = ""] # [doc = " The word pos will be equal to `block_pos * 16 words per block`."] # [doc = ""] # [doc = " This method takes any of the following:"] # [doc = " * `u64`"] # [doc = " * `[u32; 2]`"] # [doc = " * `[u8; 8]`"] # [doc = ""] # [doc = " Note: the arrays should be in little endian order."] # [inline] # [allow (unused)] pub fn set_block_pos < B : Into < BlockPos >> (& mut self , block_pos : B) { self . core . reset () ; let block_pos = block_pos . into () . 0 ; self . core . core . 0 . state [12] = block_pos [0] ; self . core . core . 0 . state [13] = block_pos [1] } # [doc = " Get the block pos."] # [inline] # [allow (unused)] pub fn get_block_pos (& self) -> u64 { self . core . core . 0 . state [12] as u64 | ((self . core . core . 0 . state [13] as u64) << 32) } # [doc = " Set the stream number. The lower 64 bits are used and the rest are"] # [doc = " discarded. This method takes any of the following:"] # [doc = " * `u64`"] # [doc = " * `[u32; 2]`"] # [doc = " * `[u8; 8]`"] # [doc = ""] # [doc = " Note: the arrays should be in little endian order."] # [doc = ""] # [doc = " This is initialized to zero; 2<sup>64</sup> unique streams of output"] # [doc = " are available per seed/key. In theory a 96-bit nonce can be used by"] # [doc = " passing the last 64-bits to this function and using the first 32-bits as"] # [doc = " the most significant half of the 64-bit counter, which may be set"] # [doc = " directly via `set_block_pos` like so:"] # [doc = ""] # [doc = " ```"] # [doc = " use chacha20::ChaCha20Rng;"] # [doc = " use rand_core::{SeedableRng, RngCore};"] # [doc = ""] # [doc = " let seed = [2u8; 32];"] # [doc = " let mut rng = ChaCha20Rng::from_seed(seed);"] # [doc = ""] # [doc = " // set state[12] to 0, state[13] to 1, state[14] to 2, state[15] to 3"] # [doc = " rng.set_block_pos([0u32, 1u32]);"] # [doc = " rng.set_stream([2u32, 3u32]);"] # [doc = ""] # [doc = " // confirm that state is set correctly"] # [doc = " assert_eq!(rng.get_block_pos(), 1 << 32);"] # [doc = " assert_eq!(rng.get_stream(), (3 << 32) + 2);"] # [doc = " ```"] # [inline] pub fn set_stream < S : Into < StreamId >> (& mut self , stream : S) { let stream : StreamId = stream . into () ; self . core . core . 0 . state [14 ..] . copy_from_slice (& stream . 0) ; if self . core . index () != BUFFER_SIZE { self . core . generate_and_set (self . core . index ()) ; } } # [doc = " Get the stream number."] # [inline] pub fn get_stream (& self) -> u64 { let mut result = [0u8 ; 8] ; for (i , & big) in self . core . core . 0 . state [14 .. BLOCK_WORDS as usize] . iter () . enumerate () { let index = i * 4 ; result [index + 0] = big as u8 ; result [index + 1] = (big >> 8) as u8 ; result [index + 2] = (big >> 16) as u8 ; result [index + 3] = (big >> 24) as u8 ; } u64 :: from_le_bytes (result) } # [doc = " Get the seed."] # [inline] pub fn get_seed (& self) -> [u8 ; 32] { let mut result = [0u8 ; 32] ; for (i , & big) in self . core . core . 0 . state [4 .. 12] . iter () . enumerate () { let index = i * 4 ; result [index + 0] = big as u8 ; result [index + 1] = (big >> 8) as u8 ; result [index + 2] = (big >> 16) as u8 ; result [index + 3] = (big >> 24) as u8 ; } result } } impl PartialEq <$ ChaChaXRng > for $ ChaChaXRng { fn eq (& self , rhs : &$ ChaChaXRng) -> bool { let a : $ abst ::$ ChaChaXRng = self . into () ; let b : $ abst ::$ ChaChaXRng = rhs . into () ; a == b } } impl Eq for $ ChaChaXRng { } impl From <$ ChaChaXCore > for $ ChaChaXRng { fn from (core : $ ChaChaXCore) -> Self { $ ChaChaXRng { core : BlockRng :: new (core) , } } } mod $ abst { # [derive (Debug , PartialEq , Eq)] pub (crate) struct $ ChaChaXRng { seed : crate :: rng :: Seed , stream : u64 , word_pos : u128 , } impl From <& super ::$ ChaChaXRng > for $ ChaChaXRng { fn from (r : & super ::$ ChaChaXRng) -> Self { Self { seed : r . get_seed () . into () , stream : r . get_stream () , word_pos : r . get_word_pos () , } } } impl From <&$ ChaChaXRng > for super ::$ ChaChaXRng { fn from (a : &$ ChaChaXRng) -> Self { use rand_core :: SeedableRng ; let mut r = Self :: from_seed (a . seed . 0 . into ()) ; r . set_stream (a . stream) ; r . set_word_pos (a . word_pos) ; r } } } impl BlockRngCore for $ ChaChaXCore { type Item = u32 ; type Results = BlockRngResults ; # [inline] fn generate (& mut self , r : & mut Self :: Results) { self . 0 . generate (& mut r . 0) ; } } } ; }
};
}
