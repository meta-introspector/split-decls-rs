// Generated macro for AeadCore (trait)
macro_rules! DepcrateAeadCore {
() => {
// Module: crate
// Provides: {"AeadCore"}
// Dependencies: {}
# [doc = " Authenticated Encryption with Associated Data (AEAD) algorithm."] pub trait AeadCore { # [doc = " The length of a nonce."] type NonceSize : ArraySize ; # [doc = " The maximum length of the tag."] type TagSize : ArraySize ; # [doc = " The AEAD tag position."] const TAG_POSITION : TagPosition ; # [doc = " Generate a random nonce for this AEAD algorithm."] # [doc = ""] # [doc = " AEAD algorithms accept a parameter to encryption/decryption called"] # [doc = " a \"nonce\" which must be unique every time encryption is performed and"] # [doc = " never repeated for the same key. The nonce is often prepended to the"] # [doc = " ciphertext. The nonce used to produce a given ciphertext must be passed"] # [doc = " to the decryption function in order for it to decrypt correctly."] # [doc = ""] # [doc = " Nonces don't necessarily have to be random, but it is one strategy"] # [doc = " which is implemented by this function."] # [doc = ""] # [doc = " # ⚠\u{fe0f}Security Warning"] # [doc = ""] # [doc = " AEAD algorithms often fail catastrophically if nonces are ever repeated"] # [doc = " (with SIV modes being an exception)."] # [doc = ""] # [doc = " Using random nonces runs the risk of repeating them unless the nonce"] # [doc = " size is particularly large (e.g. 192-bit extended nonces used by the"] # [doc = " `XChaCha20Poly1305` and `XSalsa20Poly1305` constructions."] # [doc = ""] # [doc = " [NIST SP 800-38D] recommends the following:"] # [doc = ""] # [doc = " > The total number of invocations of the authenticated encryption"] # [doc = " > function shall not exceed 2^32, including all IV lengths and all"] # [doc = " > instances of the authenticated encryption function with the given key."] # [doc = ""] # [doc = " Following this guideline, only 4,294,967,296 messages with random"] # [doc = " nonces can be encrypted under a given key. While this bound is high,"] # [doc = " it's possible to encounter in practice, and systems which might"] # [doc = " reach it should consider alternatives to purely random nonces, like"] # [doc = " a counter or a combination of a random nonce + counter."] # [doc = ""] # [doc = " See the [`aead-stream`] crate for a ready-made implementation of the latter."] # [doc = ""] # [doc = " [NIST SP 800-38D]: https://csrc.nist.gov/publications/detail/sp/800-38d/final"] # [doc = " [`aead-stream`]: https://docs.rs/aead-stream"] # [cfg (feature = "getrandom")] fn generate_nonce () -> core :: result :: Result < Nonce < Self > , getrandom :: Error > { let mut nonce = Nonce :: < Self > :: default () ; getrandom :: fill (& mut nonce) ? ; Ok (nonce) } # [doc = " Generate a random nonce for this AEAD algorithm using the specified [`CryptoRng`]."] # [doc = ""] # [doc = " See [`AeadCore::generate_nonce`] documentation for requirements for"] # [doc = " random nonces."] # [cfg (feature = "rand_core")] fn generate_nonce_with_rng < R : CryptoRng + ? Sized > (rng : & mut R) -> Nonce < Self > { let mut nonce = Nonce :: < Self > :: default () ; rng . fill_bytes (& mut nonce) ; nonce } # [doc = " Generate a random nonce for this AEAD algorithm using the specified [`TryCryptoRng`]."] # [doc = ""] # [doc = " See [`AeadCore::generate_nonce`] documentation for requirements for"] # [doc = " random nonces."] # [cfg (feature = "rand_core")] fn try_generate_nonce_with_rng < R : TryCryptoRng + ? Sized > (rng : & mut R ,) -> core :: result :: Result < Nonce < Self > , R :: Error > { let mut nonce = Nonce :: < Self > :: default () ; rng . try_fill_bytes (& mut nonce) ? ; Ok (nonce) } }
};
}
