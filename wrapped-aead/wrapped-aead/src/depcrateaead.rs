// Generated macro for Aead (trait)
macro_rules! DepcrateAead {
() => {
// Module: crate
// Provides: {"Aead"}
// Dependencies: {}
# [doc = " Authenticated Encryption with Associated Data (AEAD) algorithm."] # [cfg (feature = "alloc")] pub trait Aead : AeadCore { # [doc = " Encrypt the given plaintext payload, and return the resulting"] # [doc = " ciphertext as a vector of bytes."] # [doc = ""] # [doc = " The [`Payload`] type can be used to provide Additional Associated Data"] # [doc = " (AAD) along with the message: this is an optional bytestring which is"] # [doc = " not encrypted, but *is* authenticated along with the message. Failure"] # [doc = " to pass the same AAD that was used during encryption will cause"] # [doc = " decryption to fail, which is useful if you would like to \"bind\" the"] # [doc = " ciphertext to some other identifier, like a digital signature key"] # [doc = " or other identifier."] # [doc = ""] # [doc = " If you don't care about AAD and just want to encrypt a plaintext"] # [doc = " message, `&[u8]` will automatically be coerced into a `Payload`:"] # [doc = ""] # [doc = " ```nobuild"] # [doc = " let plaintext = b\"Top secret message, handle with care\";"] # [doc = " let ciphertext = cipher.encrypt(nonce, plaintext);"] # [doc = " ```"] # [doc = ""] # [doc = " The default implementation assumes a postfix tag (ala AES-GCM,"] # [doc = " AES-GCM-SIV, ChaCha20Poly1305). [`Aead`] implementations which do not"] # [doc = " use a postfix tag will need to override this to correctly assemble the"] # [doc = " ciphertext message."] fn encrypt < 'msg , 'aad > (& self , nonce : & Nonce < Self > , plaintext : impl Into < Payload < 'msg , 'aad > > ,) -> Result < Vec < u8 > > ; # [doc = " Decrypt the given ciphertext slice, and return the resulting plaintext"] # [doc = " as a vector of bytes."] # [doc = ""] # [doc = " See notes on [`Aead::encrypt()`] about allowable message payloads and"] # [doc = " Associated Additional Data (AAD)."] # [doc = ""] # [doc = " If you have no AAD, you can call this as follows:"] # [doc = ""] # [doc = " ```nobuild"] # [doc = " let ciphertext = b\"...\";"] # [doc = " let plaintext = cipher.decrypt(nonce, ciphertext)?;"] # [doc = " ```"] # [doc = ""] # [doc = " The default implementation assumes a postfix tag (ala AES-GCM,"] # [doc = " AES-GCM-SIV, ChaCha20Poly1305). [`Aead`] implementations which do not"] # [doc = " use a postfix tag will need to override this to correctly parse the"] # [doc = " ciphertext message."] fn decrypt < 'msg , 'aad > (& self , nonce : & Nonce < Self > , ciphertext : impl Into < Payload < 'msg , 'aad > > ,) -> Result < Vec < u8 > > ; }
};
}
