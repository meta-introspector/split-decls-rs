// Generated macro for Flavor (trait)
macro_rules! Depcrate_de_flavorsFlavor {
() => {
// Module: crate::de::flavors
// Provides: {"Flavor"}
// Dependencies: {}
# [doc = " The deserialization Flavor trait"] # [doc = ""] # [doc = " This is used as the primary way to decode serialized data from some kind of buffer,"] # [doc = " or modify that data in a middleware style pattern."] # [doc = ""] # [doc = " See the module level docs for an example of how flavors are used."] pub trait Flavor < 'de > : 'de { # [doc = " The remaining data of this flavor after deserializing has completed."] # [doc = ""] # [doc = " Typically, this includes the remaining buffer that was not used for"] # [doc = " deserialization, and in cases of more complex flavors, any additional"] # [doc = " information that was decoded or otherwise calculated during"] # [doc = " the deserialization process."] type Remainder : 'de ; # [doc = " The source of data retrieved for deserialization."] # [doc = ""] # [doc = " This is typically some sort of data buffer, or another Flavor, when"] # [doc = " chained behavior is desired"] type Source : 'de ; # [doc = " Obtain the next byte for deserialization"] fn pop (& mut self) -> Result < u8 > ; # [doc = " Returns the number of bytes remaining in the message, if known."] # [doc = ""] # [doc = " # Implementation notes"] # [doc = ""] # [doc = " It is not enforced that this number is exactly correct."] # [doc = " A flavor may yield less or more bytes than the what is hinted at by"] # [doc = " this function."] # [doc = ""] # [doc = " `size_hint()` is primarily intended to be used for optimizations such as"] # [doc = " reserving space for deserialized items, but must not be trusted to"] # [doc = " e.g., omit bounds checks in unsafe code. An incorrect implementation of"] # [doc = " `size_hint()` should not lead to memory safety violations."] # [doc = ""] # [doc = " That said, the implementation should provide a correct estimation,"] # [doc = " because otherwise it would be a violation of the trait’s protocol."] # [doc = ""] # [doc = " The default implementation returns `None` which is correct for any flavor."] fn size_hint (& self) -> Option < usize > { None } # [doc = " Attempt to take the next `ct` bytes from the serialized message."] # [doc = ""] # [doc = " This variant borrows the data from the input for zero-copy deserialization. If zero-copy"] # [doc = " deserialization is not necessary, prefer to use `try_take_n_temp` instead."] fn try_take_n (& mut self , ct : usize) -> Result < & 'de [u8] > ; # [doc = " Attempt to take the next `ct` bytes from the serialized message."] # [doc = ""] # [doc = " This variant does not guarantee that the returned value is borrowed from the input, so it"] # [doc = " cannot be used for zero-copy deserialization, but it also avoids needing to potentially"] # [doc = " allocate a data in a temporary buffer."] # [doc = ""] # [doc = " This variant should be used instead of `try_take_n`"] # [doc = " if zero-copy deserialization is not necessary."] # [doc = ""] # [doc = " It is only necessary to implement this method if the flavor requires storing data in a"] # [doc = " temporary buffer in order to implement the borrow semantics, e.g. the `std::io::Read`"] # [doc = " flavor."] fn try_take_n_temp < 'a > (& 'a mut self , ct : usize) -> Result < & 'a [u8] > where 'de : 'a , { self . try_take_n (ct) } # [doc = " Complete the deserialization process."] # [doc = ""] # [doc = " This is typically called separately, after the `serde` deserialization"] # [doc = " has completed."] fn finalize (self) -> Result < Self :: Remainder > ; }
};
}
