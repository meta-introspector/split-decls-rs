// Generated macro for impl_943 (impl)
macro_rules! Depcrate_tz_concatenatedimpl_943 {
() => {
// Module: crate::tz::concatenated
// Provides: {"impl_943"}
// Dependencies: {}
impl < 'a > IndexEntry < 'a > { # [doc = " The length of an index entry. It's fixed size. 40 bytes for the IANA"] # [doc = " time zone identifier. 4 bytes for each of 3 big-endian integers. The"] # [doc = " first is the start of the corresponding TZif data within the data"] # [doc = " block. The second is the length of said TZif data. And the third is"] # [doc = " the \"raw UTC offset\" of the time zone. (I'm unclear on the semantics"] # [doc = " of this third, since some time zones have more than one because of"] # [doc = " DST. And of course, it can change over time. Since I don't know what"] # [doc = " Android uses this for, I'm not sure how I'm supposed to interpret it.)"] const LEN : usize = 40 + 3 * 4 ; # [doc = " Creates a new view into an entry in the concatenated TZif index."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " When `slice` does not have the expected length (`IndexEntry::LEN`)."] fn new (slice : & 'a [u8]) -> IndexEntry < 'a > { assert_eq ! (slice . len () , IndexEntry :: LEN , "invalid index entry length") ; IndexEntry (slice) } # [doc = " Like `name_bytes`, but as a `&str`."] # [doc = ""] # [doc = " This returns an error if the name isn't valid UTF-8."] fn name (& self) -> Result < & str , Error > { core :: str :: from_utf8 (self . name_bytes ()) . map_err (| _ | { err ! ("IANA time zone identifier `{name}` is not valid UTF-8" , name = escape :: Bytes (self . name_bytes ()) ,) }) } # [doc = " Returns the IANA time zone identifier as a byte slice."] # [doc = ""] # [doc = " In theory, an empty slice could be returned. But if that happens,"] # [doc = " then there is probably a bug in this code somewhere, the format"] # [doc = " changed or the source data is corrupt somehow."] fn name_bytes (& self) -> & 'a [u8] { let mut block = & self . 0 [.. 40] ; while block . last () . copied () == Some (0) { block = & block [.. block . len () - 1] ; } block } # [doc = " Returns the starting offset (relative to the beginning of the TZif"] # [doc = " data block) of the corresponding TZif data."] fn start (& self) -> u64 { u64 :: from (read_be32 (& self . 0 [40 .. 44])) } # [doc = " Returns the length of the TZif data block."] # [doc = ""] # [doc = " Beware of using this to create allocations. In theory, this should be"] # [doc = " trusted data, but the length can be any 32-bit integer. If it's used to"] # [doc = " create an allocation, it could potentially be up to 4GB."] fn len (& self) -> usize { usize :: try_from (read_be32 (& self . 0 [44 .. 48])) . unwrap_or (usize :: MAX) } }
};
}
