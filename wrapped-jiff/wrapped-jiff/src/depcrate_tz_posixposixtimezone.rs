// Generated macro for PosixTimeZone (struct)
macro_rules! Depcrate_tz_posixPosixTimeZone {
() => {
// Module: crate::tz::posix
// Provides: {"PosixTimeZone"}
// Dependencies: {}
# [doc = " A POSIX time zone."] # [doc = ""] # [doc = " # On \"reasonable\" POSIX time zones"] # [doc = ""] # [doc = " Jiff only supports \"reasonable\" POSIX time zones. A \"reasonable\" POSIX time"] # [doc = " zone is a POSIX time zone that has a DST transition rule _when_ it has a"] # [doc = " DST time zone abbreviation. Without the transition rule, it isn't possible"] # [doc = " to know when DST starts and stops."] # [doc = ""] # [doc = " POSIX technically allows a DST time zone abbreviation *without* a"] # [doc = " transition rule, but the behavior is literally unspecified. So Jiff just"] # [doc = " rejects them."] # [doc = ""] # [doc = " Note that if you're confused as to why Jiff accepts `TZ=EST5EDT` (where"] # [doc = " `EST5EDT` is an example of an _unreasonable_ POSIX time zone), that's"] # [doc = " because Jiff rejects `EST5EDT` and instead attempts to use it as an IANA"] # [doc = " time zone identifier. And indeed, the IANA Time Zone Database contains an"] # [doc = " entry for `EST5EDT` (presumably for legacy reasons)."] # [doc = ""] # [doc = " Also, we expect `TZ` strings parsed from IANA v2+ formatted `tzfile`s to"] # [doc = " also be reasonable or parsing fails. This also seems to be consistent with"] # [doc = " the [GNU C Library]'s treatment of the `TZ` variable: it only documents"] # [doc = " support for reasonable POSIX time zone strings."] # [doc = ""] # [doc = " Note that a V2 `TZ` string is precisely identical to a POSIX `TZ`"] # [doc = " environment variable string. A V3 `TZ` string however supports signed DST"] # [doc = " transition times, and hours in the range `0..=167`. The V2 and V3 here"] # [doc = " reference how `TZ` strings are defined in the TZif format specified by"] # [doc = " [RFC 9636]. V2 is the original version of it straight from POSIX, where as"] # [doc = " V3+ corresponds to an extension added to V3 (and newer versions) of the"] # [doc = " TZif format. V3 is a superset of V2, so in practice, Jiff just permits"] # [doc = " V3 everywhere."] # [doc = ""] # [doc = " [GNU C Library]: https://www.gnu.org/software/libc/manual/2.25/html_node/TZ-Variable.html"] # [doc = " [RFC 9636]: https://datatracker.ietf.org/doc/rfc9636/"] # [derive (Clone , Debug , Eq , PartialEq)] # [doc (hidden)] # [repr (align (8))] pub struct PosixTimeZone < ABBREV > { inner : shared :: PosixTimeZone < ABBREV > , }
};
}
