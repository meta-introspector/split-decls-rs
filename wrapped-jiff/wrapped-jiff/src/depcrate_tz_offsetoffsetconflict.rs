// Generated macro for OffsetConflict (enum)
macro_rules! Depcrate_tz_offsetOffsetConflict {
() => {
// Module: crate::tz::offset
// Provides: {"OffsetConflict"}
// Dependencies: {}
# [doc = " Configuration for resolving disparities between an offset and a time zone."] # [doc = ""] # [doc = " A conflict between an offset and a time zone most commonly appears in a"] # [doc = " datetime string. For example, `2024-06-14T17:30-05[America/New_York]`"] # [doc = " has a definitive inconsistency between the reported offset (`-05`) and"] # [doc = " the time zone (`America/New_York`), because at this time in New York,"] # [doc = " daylight saving time (DST) was in effect. In New York in the year 2024,"] # [doc = " DST corresponded to the UTC offset `-04`."] # [doc = ""] # [doc = " Other conflict variations exist. For example, in 2019, Brazil abolished"] # [doc = " DST completely. But if one were to create a datetime for 2020 in 2018, that"] # [doc = " datetime in 2020 would reflect the DST rules as they exist in 2018. That"] # [doc = " could in turn result in a datetime with an offset that is incorrect with"] # [doc = " respect to the rules in 2019."] # [doc = ""] # [doc = " For this reason, this crate exposes a few ways of resolving these"] # [doc = " conflicts. It is most commonly used as configuration for parsing"] # [doc = " [`Zoned`](crate::Zoned) values via"] # [doc = " [`fmt::temporal::DateTimeParser::offset_conflict`](crate::fmt::temporal::DateTimeParser::offset_conflict). But this configuration can also be used directly via"] # [doc = " [`OffsetConflict::resolve`]."] # [doc = ""] # [doc = " The default value is `OffsetConflict::Reject`, which results in an"] # [doc = " error being returned if the offset and a time zone are not in agreement."] # [doc = " This is the default so that Jiff does not automatically make silent choices"] # [doc = " about whether to prefer the time zone or the offset. The"] # [doc = " [`fmt::temporal::DateTimeParser::parse_zoned_with`](crate::fmt::temporal::DateTimeParser::parse_zoned_with)"] # [doc = " documentation shows an example demonstrating its utility in the face"] # [doc = " of changes in the law, such as the abolition of daylight saving time."] # [doc = " By rejecting such things, one can ensure that the original timestamp is"] # [doc = " preserved or else an error occurs."] # [doc = ""] # [doc = " This enum is non-exhaustive so that other forms of offset conflicts may be"] # [doc = " added in semver compatible releases."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to always use the time zone even if the offset is"] # [doc = " wrong."] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{civil::date, tz};"] # [doc = ""] # [doc = " let dt = date(2024, 6, 14).at(17, 30, 0, 0);"] # [doc = " let offset = tz::offset(-5); // wrong! should be -4"] # [doc = " let newyork = tz::db().get(\"America/New_York\")?;"] # [doc = ""] # [doc = " // The default conflict resolution, 'Reject', will error."] # [doc = " let result = tz::OffsetConflict::Reject"] # [doc = "     .resolve(dt, offset, newyork.clone());"] # [doc = " assert!(result.is_err());"] # [doc = ""] # [doc = " // But we can change it to always prefer the time zone."] # [doc = " let zdt = tz::OffsetConflict::AlwaysTimeZone"] # [doc = "     .resolve(dt, offset, newyork.clone())?"] # [doc = "     .unambiguous()?;"] # [doc = " assert_eq!(zdt.datetime(), date(2024, 6, 14).at(17, 30, 0, 0));"] # [doc = " // The offset has been corrected automatically."] # [doc = " assert_eq!(zdt.offset(), tz::offset(-4));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " # Example: parsing"] # [doc = ""] # [doc = " This example shows how to set the offset conflict resolution configuration"] # [doc = " while parsing a [`Zoned`](crate::Zoned) datetime. In this example, we"] # [doc = " always prefer the offset, even if it conflicts with the time zone."] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{civil::date, fmt::temporal::DateTimeParser, tz};"] # [doc = ""] # [doc = " static PARSER: DateTimeParser = DateTimeParser::new()"] # [doc = "     .offset_conflict(tz::OffsetConflict::AlwaysOffset);"] # [doc = ""] # [doc = " let zdt = PARSER.parse_zoned(\"2024-06-14T17:30-05[America/New_York]\")?;"] # [doc = " // The time *and* offset have been corrected. The offset given was invalid,"] # [doc = " // so it cannot be kept, but the timestamp returned is equivalent to"] # [doc = " // `2024-06-14T17:30-05`. It is just adjusted automatically to be correct"] # [doc = " // in the `America/New_York` time zone."] # [doc = " assert_eq!(zdt.datetime(), date(2024, 6, 14).at(18, 30, 0, 0));"] # [doc = " assert_eq!(zdt.offset(), tz::offset(-4));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Copy , Debug , Default)] # [non_exhaustive] pub enum OffsetConflict { # [doc = " When the offset and time zone are in conflict, this will always use"] # [doc = " the offset to interpret the date time."] # [doc = ""] # [doc = " When resolving to a [`AmbiguousZoned`], the time zone attached"] # [doc = " to the timestamp will still be the same as the time zone given. The"] # [doc = " difference here is that the offset will be adjusted such that it is"] # [doc = " correct for the given time zone. However, the timestamp itself will"] # [doc = " always match the datetime and offset given (and which is always"] # [doc = " unambiguous)."] # [doc = ""] # [doc = " Basically, you should use this option when you want to keep the exact"] # [doc = " time unchanged (as indicated by the datetime and offset), even if it"] # [doc = " means a change to civil time."] AlwaysOffset , # [doc = " When the offset and time zone are in conflict, this will always use"] # [doc = " the time zone to interpret the date time."] # [doc = ""] # [doc = " When resolving to an [`AmbiguousZoned`], the offset attached to the"] # [doc = " timestamp will always be determined by only looking at the time zone."] # [doc = " This in turn implies that the timestamp returned could be ambiguous,"] # [doc = " since this conflict resolution strategy specifically ignores the"] # [doc = " offset. (And, we're only at this point because the offset is not"] # [doc = " possible for the given time zone, so it can't be used in concert with"] # [doc = " the time zone anyway.) This is unlike the `AlwaysOffset` strategy where"] # [doc = " the timestamp returned is guaranteed to be unambiguous."] # [doc = ""] # [doc = " You should use this option when you want to keep the civil time"] # [doc = " unchanged even if it means a change to the exact time."] AlwaysTimeZone , # [doc = " Always attempt to use the offset to resolve a datetime to a timestamp,"] # [doc = " unless the offset is invalid for the provided time zone. In that case,"] # [doc = " use the time zone. When the time zone is used, it's possible for an"] # [doc = " ambiguous datetime to be returned."] # [doc = ""] # [doc = " See [`ZonedWith::offset_conflict`](crate::ZonedWith::offset_conflict)"] # [doc = " for an example of when this strategy is useful."] PreferOffset , # [doc = " When the offset and time zone are in conflict, this strategy always"] # [doc = " results in conflict resolution returning an error."] # [doc = ""] # [doc = " This is the default since a conflict between the offset and the time"] # [doc = " zone usually implies an invalid datetime in some way."] # [default] Reject , }
};
}
