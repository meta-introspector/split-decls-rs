// Generated macro for ZonedInner (struct)
macro_rules! Depcrate_zonedZonedInner {
() => {
// Module: crate::zoned
// Provides: {"ZonedInner"}
// Dependencies: {}
# [doc = " The representation of a `Zoned`."] # [doc = ""] # [doc = " This uses 4 different things: a timestamp, a datetime, an offset and a"] # [doc = " time zone. This in turn makes `Zoned` a bit beefy (40 bytes on x86-64),"] # [doc = " but I think this is probably the right trade off. (At time of writing,"] # [doc = " 2024-07-04.)"] # [doc = ""] # [doc = " Technically speaking, the only essential fields here are timestamp and time"] # [doc = " zone. The datetime and offset can both be unambiguously _computed_ from the"] # [doc = " combination of a timestamp and a time zone. Indeed, just the timestamp and"] # [doc = " the time zone was my initial representation. But as I developed the API of"] # [doc = " this type, it became clearer that we should probably store the datetime and"] # [doc = " offset as well."] # [doc = ""] # [doc = " The main issue here is that in order to compute the datetime from a"] # [doc = " timestamp and a time zone, you need to do two things:"] # [doc = ""] # [doc = " 1. First, compute the offset. This means doing a binary search on the TZif"] # [doc = " data for the transition (or closest transition) matching the timestamp."] # [doc = " 2. Second, use the offset (from UTC) to convert the timestamp into a civil"] # [doc = " datetime. This involves a \"Unix time to Unix epoch days\" conversion that"] # [doc = " requires some heavy arithmetic."] # [doc = ""] # [doc = " So if we don't store the datetime or offset, then we need to compute them"] # [doc = " any time we need them. And the Temporal design really pushes heavily in"] # [doc = " favor of treating the \"instant in time\" and \"civil datetime\" as two sides"] # [doc = " to the same coin. That means users are very encouraged to just use whatever"] # [doc = " they need. So if we are always computing the offset and datetime whenever"] # [doc = " we need them, we're potentially punishing users for working with civil"] # [doc = " datetimes. It just doesn't feel like the right trade-off."] # [doc = ""] # [doc = " Instead, my idea here is that, ultimately, `Zoned` is meant to provide"] # [doc = " a one-stop shop for \"doing the right thing.\" Presenting that unified"] # [doc = " abstraction comes with costs. And that if we want to expose cheaper ways"] # [doc = " of performing at least some of the operations on `Zoned` by making fewer"] # [doc = " assumptions, then we should probably endeavor to do that by exposing a"] # [doc = " lower level API. I'm not sure what that would look like, so I think it"] # [doc = " should be driven by use cases."] # [doc = ""] # [doc = " Some other things I considered:"] # [doc = ""] # [doc = " * Use `Zoned(Arc<ZonedInner>)` to make `Zoned` pointer-sized. But I didn't"] # [doc = " like this because it implies creating any new `Zoned` value requires an"] # [doc = " allocation. Since a `TimeZone` internally uses an `Arc`, all it requires"] # [doc = " today is a chunky memcpy and an atomic ref count increment."] # [doc = " * Use `OnceLock` shenanigans for the datetime and offset fields. This would"] # [doc = " make `Zoned` even beefier and I wasn't totally clear how much this would"] # [doc = " save us. And it would impose some (probably small) cost on every datetime"] # [doc = " or offset access."] # [doc = " * Use a radically different design that permits a `Zoned` to be `Copy`."] # [doc = " I personally find it deeply annoying that `Zoned` is both the \"main\""] # [doc = " datetime type in Jiff and also the only one that doesn't implement `Copy`."] # [doc = " I explored some designs, but I couldn't figure out how to make it work in"] # [doc = " a satisfying way. The main issue here is `TimeZone`. A `TimeZone` is a huge"] # [doc = " chunk of data and the ergonomics of the `Zoned` API require being able to"] # [doc = " access a `TimeZone` without the caller providing it explicitly. So to me,"] # [doc = " the only real alternative here is to use some kind of integer handle into"] # [doc = " a global time zone database. But now you all of a sudden need to worry"] # [doc = " about synchronization for every time zone access and plausibly also garbage"] # [doc = " collection. And this also complicates matters for using custom time zone"] # [doc = " databases. So I ultimately came down on \"Zoned is not Copy\" as the least"] # [doc = " awful choice. *heavy sigh*"] # [derive (Clone)] struct ZonedInner { timestamp : Timestamp , datetime : DateTime , offset : Offset , time_zone : TimeZone , }
};
}
