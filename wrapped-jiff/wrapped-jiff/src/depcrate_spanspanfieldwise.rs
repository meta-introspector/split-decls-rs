// Generated macro for SpanFieldwise (struct)
macro_rules! Depcrate_spanSpanFieldwise {
() => {
// Module: crate::span
// Provides: {"SpanFieldwise"}
// Dependencies: {}
# [doc = " A wrapper for [`Span`] that implements the `Hash`, `Eq` and `PartialEq`"] # [doc = " traits."] # [doc = ""] # [doc = " A `SpanFieldwise` is meant to make it easy to compare two spans in a \"dumb\""] # [doc = " way based purely on its unit values, while still providing a speed bump"] # [doc = " to avoid accidentally doing this comparison on `Span` directly. This is"] # [doc = " distinct from something like [`Span::compare`] that performs a comparison"] # [doc = " on the actual elapsed time of two spans."] # [doc = ""] # [doc = " It is generally discouraged to use `SpanFieldwise` since spans that"] # [doc = " represent an equivalent elapsed amount of time may compare unequal."] # [doc = " However, in some cases, it is useful to be able to assert precise field"] # [doc = " values. For example, Jiff itself makes heavy use of fieldwise comparisons"] # [doc = " for tests."] # [doc = ""] # [doc = " # Construction"] # [doc = ""] # [doc = " While callers may use `SpanFieldwise(span)` (where `span` has type [`Span`])"] # [doc = " to construct a value of this type, callers may find [`Span::fieldwise`]"] # [doc = " more convenient. Namely, `Span::fieldwise` may avoid the need to explicitly"] # [doc = " import `SpanFieldwise`."] # [doc = ""] # [doc = " # Trait implementations"] # [doc = ""] # [doc = " In addition to implementing the `Hash`, `Eq` and `PartialEq` traits, this"] # [doc = " type also provides `PartialEq` impls for comparing a `Span` with a"] # [doc = " `SpanFieldwise`. This simplifies comparisons somewhat while still requiring"] # [doc = " that at least one of the values has an explicit fieldwise comparison type."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This type is guaranteed to have the same layout in memory as [`Span`]."] # [doc = ""] # [doc = " # Example: the difference between `SpanFieldwise` and [`Span::compare`]"] # [doc = ""] # [doc = " In short, `SpanFieldwise` considers `2 hours` and `120 minutes` to be"] # [doc = " distinct values, but `Span::compare` considers them to be equivalent:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::cmp::Ordering;"] # [doc = " use jiff::ToSpan;"] # [doc = ""] # [doc = " assert_ne!(120.minutes().fieldwise(), 2.hours().fieldwise());"] # [doc = " assert_eq!(120.minutes().compare(2.hours())?, Ordering::Equal);"] # [doc = ""] # [doc = " // These comparisons are allowed between a `Span` and a `SpanFieldwise`."] # [doc = " // Namely, as long as one value is \"fieldwise,\" then the comparison is OK."] # [doc = " assert_ne!(120.minutes().fieldwise(), 2.hours());"] # [doc = " assert_ne!(120.minutes(), 2.hours().fieldwise());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Copy , Debug , Default)] # [repr (transparent)] pub struct SpanFieldwise (pub Span) ;
};
}
