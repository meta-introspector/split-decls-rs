// Generated macro for Designator (enum)
macro_rules! Depcrate_fmt_friendly_printerDesignator {
() => {
// Module: crate::fmt::friendly::printer
// Provides: {"Designator"}
// Dependencies: {}
# [doc = " Configuration for [`SpanPrinter::designator`]."] # [doc = ""] # [doc = " This controls which kinds of designators to use when formatting a"] # [doc = " \"friendly\" duration. Generally, this only provides one axis of control:"] # [doc = " the length of each designator."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{fmt::friendly::{Designator, SpanPrinter}, ToSpan};"] # [doc = ""] # [doc = " let span = 1.year().months(2);"] # [doc = ""] # [doc = " let printer = SpanPrinter::new();"] # [doc = " assert_eq!(printer.span_to_string(&span), \"1y 2mo\");"] # [doc = ""] # [doc = " let printer = SpanPrinter::new().designator(Designator::Short);"] # [doc = " assert_eq!(printer.span_to_string(&span), \"1yr 2mos\");"] # [doc = ""] # [doc = " let printer = SpanPrinter::new().designator(Designator::Verbose);"] # [doc = " assert_eq!(printer.span_to_string(&span), \"1year 2months\");"] # [doc = ""] # [doc = " let printer = SpanPrinter::new().designator(Designator::HumanTime);"] # [doc = " assert_eq!(printer.span_to_string(&span), \"1y 2months\");"] # [doc = " ```"] # [derive (Clone , Copy , Debug)] # [non_exhaustive] pub enum Designator { # [doc = " This writes out the full word of each unit designation. For example,"] # [doc = " `year`."] Verbose , # [doc = " This writes out a short but not minimal label for each unit. For"] # [doc = " example, `yr` for `year` and `yrs` for `years`."] Short , # [doc = " This writes out the shortest possible label for each unit that is still"] # [doc = " generally recognizable. For example, `y`. Note that in the compact"] # [doc = " representation, and unlike the verbose and short representations, there"] # [doc = " is no distinction between singular or plural."] Compact , # [doc = " A special mode that uses designator labels that are known to be"] # [doc = " compatible with the `humantime` crate."] # [doc = ""] # [doc = " None of `Verbose`, `Short` or `Compact` are compatible with"] # [doc = " `humantime`."] # [doc = ""] # [doc = " `Compact` is, on its own, nearly compatible. When using `Compact`, all"] # [doc = " designator labels are parsable by `humantime` except for months and"] # [doc = " microseconds. For months, Jiff uses `mo` and `mos`, but `humantime`"] # [doc = " only parses `months`, `month` and `M`. Jiff specifically doesn't"] # [doc = " support `M` for months because of the confusability with minutes."] # [doc = " For microseconds, Jiff uses `µs` which `humantime` does not support"] # [doc = " parsing."] # [doc = ""] # [doc = " Most of the designator labels Jiff uses for `Short` aren't supported"] # [doc = " by `humantime`. And even when they are, `humantime` is inconsistent."] # [doc = " For example, `humantime` supports `sec` and `secs`, but only `nsec`"] # [doc = " and not `nsecs`."] # [doc = ""] # [doc = " Finally, for `Verbose`, humantime supports spelling out some units"] # [doc = " in their entirety (e.g., `seconds`) but not others (e.g., `nanoseconds`"] # [doc = " is not supported by `humantime`)."] # [doc = ""] # [doc = " Therefore, this custom variant is provided so that designator labels"] # [doc = " that are compatible with both Jiff and `humantime`, even when there"] # [doc = " isn't a coherent concept otherwise connecting their style."] HumanTime , }
};
}
