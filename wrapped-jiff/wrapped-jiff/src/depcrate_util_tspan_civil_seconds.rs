// Generated macro for SPAN_CIVIL_SECONDS (const)
macro_rules! Depcrate_util_tSPAN_CIVIL_SECONDS {
() => {
// Module: crate::util::t
// Provides: {"SPAN_CIVIL_SECONDS"}
// Dependencies: {}
# [doc = " The maximum number of seconds that can be expressed with a span."] # [doc = ""] # [doc = " All of our span types (except for years and months, since they have"] # [doc = " variable length even in civil datetimes) are defined in terms of this"] # [doc = " constant. The way it's defined is a little odd, so let's break it down."] # [doc = ""] # [doc = " Firstly, a span of seconds should be able to represent at least"] # [doc = " the complete span supported by `Timestamp`. Thus, it's based off of"] # [doc = " `UnixSeconds::LEN`. That is, a span should be able to represent the value"] # [doc = " `UnixSeconds::MAX - UnixSeconds::MIN`."] # [doc = ""] # [doc = " Secondly, a span should also be able to account for any amount of possible"] # [doc = " time that a time zone offset might add or subtract to an `Timestamp`. This"] # [doc = " also means it can account for any difference between two `civil::DateTime`"] # [doc = " values."] # [doc = ""] # [doc = " Thirdly, we would like our span to be divisible by `SECONDS_PER_CIVIL_DAY`."] # [doc = " This isn't strictly required, but it makes defining boundaries a little"] # [doc = " smoother. If it weren't divisible, then the lower bounds on some types"] # [doc = " would need to be adjusted by one."] # [doc = ""] # [doc = " Note that neither the existence of this constant nor defining our spans"] # [doc = " based on it impacts the correctness of doing arithmetic on zoned instants."] # [doc = " Artihemetic on zoned instants still uses \"civil\" spans, but the length"] # [doc = " of time for some units (like a day) might vary. The arithmetic for zoned"] # [doc = " instants accounts for this explicitly. But it still must obey the limits"] # [doc = " set here."] const SPAN_CIVIL_SECONDS : i128 = next_multiple_of (UnixSeconds :: LEN + SpanZoneOffset :: MAX + SECONDS_PER_CIVIL_DAY . bound () , SECONDS_PER_CIVIL_DAY . bound () ,) ;
};
}
