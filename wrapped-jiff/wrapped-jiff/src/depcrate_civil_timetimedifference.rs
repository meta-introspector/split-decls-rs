// Generated macro for TimeDifference (struct)
macro_rules! Depcrate_civil_timeTimeDifference {
() => {
// Module: crate::civil::time
// Provides: {"TimeDifference"}
// Dependencies: {}
# [doc = " Options for [`Time::since`] and [`Time::until`]."] # [doc = ""] # [doc = " This type provides a way to configure the calculation of spans between two"] # [doc = " [`Time`] values. In particular, both `Time::since` and `Time::until` accept"] # [doc = " anything that implements `Into<TimeDifference>`. There are a few key trait"] # [doc = " implementations that make this convenient:"] # [doc = ""] # [doc = " * `From<Time> for TimeDifference` will construct a configuration consisting"] # [doc = " of just the time. So for example, `time1.until(time2)` will return the span"] # [doc = " from `time1` to `time2`."] # [doc = " * `From<DateTime> for TimeDifference` will construct a configuration"] # [doc = " consisting of just the time from the given datetime. So for example,"] # [doc = " `time.since(datetime)` returns the span from `datetime.time()` to `time`."] # [doc = " * `From<(Unit, Time)>` is a convenient way to specify the largest units"] # [doc = " that should be present on the span returned. By default, the largest units"] # [doc = " are hours. Using this trait implementation is equivalent to"] # [doc = " `TimeDifference::new(time).largest(unit)`."] # [doc = " * `From<(Unit, DateTime)>` is like the one above, but with the time from"] # [doc = " the given datetime."] # [doc = ""] # [doc = " One can also provide a `TimeDifference` value directly. Doing so"] # [doc = " is necessary to use the rounding features of calculating a span. For"] # [doc = " example, setting the smallest unit (defaults to [`Unit::Nanosecond`]), the"] # [doc = " rounding mode (defaults to [`RoundMode::Trunc`]) and the rounding increment"] # [doc = " (defaults to `1`). The defaults are selected such that no rounding occurs."] # [doc = ""] # [doc = " Rounding a span as part of calculating it is provided as a convenience."] # [doc = " Callers may choose to round the span as a distinct step via"] # [doc = " [`Span::round`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to round a span between two datetimes to the nearest"] # [doc = " half-hour, with ties breaking away from zero."] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{civil::{Time, TimeDifference}, RoundMode, ToSpan, Unit};"] # [doc = ""] # [doc = " let t1 = \"08:14:00.123456789\".parse::<Time>()?;"] # [doc = " let t2 = \"15:00\".parse::<Time>()?;"] # [doc = " let span = t1.until("] # [doc = "     TimeDifference::new(t2)"] # [doc = "         .smallest(Unit::Minute)"] # [doc = "         .mode(RoundMode::HalfExpand)"] # [doc = "         .increment(30),"] # [doc = " )?;"] # [doc = " assert_eq!(span, 7.hours().fieldwise());"] # [doc = ""] # [doc = " // One less minute, and because of the HalfExpand mode, the span would"] # [doc = " // get rounded down."] # [doc = " let t2 = \"14:59\".parse::<Time>()?;"] # [doc = " let span = t1.until("] # [doc = "     TimeDifference::new(t2)"] # [doc = "         .smallest(Unit::Minute)"] # [doc = "         .mode(RoundMode::HalfExpand)"] # [doc = "         .increment(30),"] # [doc = " )?;"] # [doc = " assert_eq!(span, 6.hours().minutes(30).fieldwise());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Copy , Debug)] pub struct TimeDifference { time : Time , round : SpanRound < 'static > , }
};
}
