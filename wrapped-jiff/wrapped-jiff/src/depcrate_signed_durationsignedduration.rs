// Generated macro for SignedDuration (struct)
macro_rules! Depcrate_signed_durationSignedDuration {
() => {
// Module: crate::signed_duration
// Provides: {"SignedDuration"}
// Dependencies: {}
# [doc = " A signed duration of time represented as a 96-bit integer of nanoseconds."] # [doc = ""] # [doc = " Each duration is made up of a 64-bit integer of whole seconds and a"] # [doc = " 32-bit integer of fractional nanoseconds less than 1 whole second. Unlike"] # [doc = " [`std::time::Duration`], this duration is signed. The sign applies"] # [doc = " to the entire duration. That is, either _both_ the seconds and the"] # [doc = " fractional nanoseconds are negative or _neither_ are. Stated differently,"] # [doc = " it is guaranteed that the signs of [`SignedDuration::as_secs`] and"] # [doc = " [`SignedDuration::subsec_nanos`] are always the same, or one component is"] # [doc = " zero. (For example, `-1 seconds` and `0 nanoseconds`, or `0 seconds` and"] # [doc = " `-1 nanoseconds`.)"] # [doc = ""] # [doc = " # Parsing and printing"] # [doc = ""] # [doc = " Like the [`Span`](crate::Span) type, the `SignedDuration` type"] # [doc = " provides convenient trait implementations of [`std::str::FromStr`] and"] # [doc = " [`std::fmt::Display`]:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::SignedDuration;"] # [doc = ""] # [doc = " let duration: SignedDuration = \"PT2h30m\".parse()?;"] # [doc = " assert_eq!(duration.to_string(), \"PT2H30M\");"] # [doc = ""] # [doc = " // Or use the \"friendly\" format by invoking the alternate:"] # [doc = " assert_eq!(format!(\"{duration:#}\"), \"2h 30m\");"] # [doc = ""] # [doc = " // Parsing automatically supports both the ISO 8601 and \"friendly\" formats:"] # [doc = " let duration: SignedDuration = \"2h 30m\".parse()?;"] # [doc = " assert_eq!(duration, SignedDuration::new(2 * 60 * 60 + 30 * 60, 0));"] # [doc = " let duration: SignedDuration = \"2 hours, 30 minutes\".parse()?;"] # [doc = " assert_eq!(duration, SignedDuration::new(2 * 60 * 60 + 30 * 60, 0));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Unlike the `Span` type, though, only uniform units are supported. This"] # [doc = " means that ISO 8601 durations with non-zero units of days or greater cannot"] # [doc = " be parsed directly into a `SignedDuration`:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::SignedDuration;"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     \"P1d\".parse::<SignedDuration>().unwrap_err().to_string(),"] # [doc = "     \"failed to parse \\\"P1d\\\" as an ISO 8601 duration string: \\"] # [doc = "      parsing ISO 8601 duration into a `SignedDuration` requires that \\"] # [doc = "      the duration contain a time component and no components of days or \\"] # [doc = "      greater\","] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " To parse such durations, one should first parse them into a `Span` and"] # [doc = " then convert them to a `SignedDuration` by providing a relative date:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{civil::date, Span};"] # [doc = ""] # [doc = " let span: Span = \"P1d\".parse()?;"] # [doc = " let relative = date(2024, 11, 3).in_tz(\"US/Eastern\")?;"] # [doc = " let duration = span.to_duration(&relative)?;"] # [doc = " // This example also motivates *why* a relative date"] # [doc = " // is required. Not all days are the same length!"] # [doc = " assert_eq!(duration.to_string(), \"PT25H\");"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " The format supported is a variation (nearly a subset) of the duration"] # [doc = " format specified in [ISO 8601] _and_ a Jiff-specific \"friendly\" format."] # [doc = " Here are more examples:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::SignedDuration;"] # [doc = ""] # [doc = " let durations = ["] # [doc = "     // ISO 8601"] # [doc = "     (\"PT2H30M\", SignedDuration::from_secs(2 * 60 * 60 + 30 * 60)),"] # [doc = "     (\"PT2.5h\", SignedDuration::from_secs(2 * 60 * 60 + 30 * 60)),"] # [doc = "     (\"PT1m\", SignedDuration::from_mins(1)),"] # [doc = "     (\"PT1.5m\", SignedDuration::from_secs(90)),"] # [doc = "     (\"PT0.0021s\", SignedDuration::new(0, 2_100_000)),"] # [doc = "     (\"PT0s\", SignedDuration::ZERO),"] # [doc = "     (\"PT0.000000001s\", SignedDuration::from_nanos(1)),"] # [doc = "     // Jiff's \"friendly\" format"] # [doc = "     (\"2h30m\", SignedDuration::from_secs(2 * 60 * 60 + 30 * 60)),"] # [doc = "     (\"2 hrs 30 mins\", SignedDuration::from_secs(2 * 60 * 60 + 30 * 60)),"] # [doc = "     (\"2 hours 30 minutes\", SignedDuration::from_secs(2 * 60 * 60 + 30 * 60)),"] # [doc = "     (\"2.5h\", SignedDuration::from_secs(2 * 60 * 60 + 30 * 60)),"] # [doc = "     (\"1m\", SignedDuration::from_mins(1)),"] # [doc = "     (\"1.5m\", SignedDuration::from_secs(90)),"] # [doc = "     (\"0.0021s\", SignedDuration::new(0, 2_100_000)),"] # [doc = "     (\"0s\", SignedDuration::ZERO),"] # [doc = "     (\"0.000000001s\", SignedDuration::from_nanos(1)),"] # [doc = " ];"] # [doc = " for (string, duration) in durations {"] # [doc = "     let parsed: SignedDuration = string.parse()?;"] # [doc = "     assert_eq!(duration, parsed, \"result of parsing {string:?}\");"] # [doc = " }"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " For more details, see the [`fmt::temporal`](temporal) and"] # [doc = " [`fmt::friendly`](friendly) modules."] # [doc = ""] # [doc = " [ISO 8601]: https://www.iso.org/iso-8601-date-and-time-format.html"] # [doc = ""] # [doc = " # API design"] # [doc = ""] # [doc = " A `SignedDuration` is, as much as is possible, a replica of the"] # [doc = " `std::time::Duration` API. While there are probably some quirks in the API"] # [doc = " of `std::time::Duration` that could have been fixed here, it is probably"] # [doc = " more important that it behave \"exactly like a `std::time::Duration` but"] # [doc = " with a sign.\" That is, this type mirrors the parallels between signed and"] # [doc = " unsigned integer types."] # [doc = ""] # [doc = " While the goal was to match the `std::time::Duration` API as much as"] # [doc = " possible, there are some differences worth highlighting:"] # [doc = ""] # [doc = " * As stated, a `SignedDuration` has a sign. Therefore, it uses `i64` and"] # [doc = " `i32` instead of `u64` and `u32` to represent its 96-bit integer."] # [doc = " * Because it's signed, the range of possible values is different. For"] # [doc = " example, a `SignedDuration::MAX` has a whole number of seconds equivalent"] # [doc = " to `i64::MAX`, which is less than `u64::MAX`."] # [doc = " * There are some additional APIs that don't make sense on an unsigned"] # [doc = " duration, like [`SignedDuration::abs`] and [`SignedDuration::checked_neg`]."] # [doc = " * A [`SignedDuration::system_until`] routine is provided as a replacement"] # [doc = " for [`std::time::SystemTime::duration_since`], but with signed durations."] # [doc = " * Constructors and getters for units of hours and minutes are provided,"] # [doc = " where as these routines are unstable in the standard library."] # [doc = " * Unlike the standard library, this type implements the `std::fmt::Display`"] # [doc = " and `std::str::FromStr` traits via the ISO 8601 duration format, just"] # [doc = " like the [`Span`](crate::Span) type does. Also like `Span`, the ISO"] # [doc = " 8601 duration format is used to implement the serde `Serialize` and"] # [doc = " `Deserialize` traits when the `serde` crate feature is enabled."] # [doc = " * The `std::fmt::Debug` trait implementation is a bit different. If you"] # [doc = " have a problem with it, please file an issue."] # [doc = " * At present, there is no `SignedDuration::abs_diff` since there are some"] # [doc = " API design questions. If you want it, please file an issue."] # [doc = ""] # [doc = " # When should I use `SignedDuration` versus [`Span`](crate::Span)?"] # [doc = ""] # [doc = " Jiff's primary duration type is `Span`. The key differences between it and"] # [doc = " `SignedDuration` are:"] # [doc = ""] # [doc = " * A `Span` keeps track of each individual unit separately. That is, even"] # [doc = " though `1 hour 60 minutes` and `2 hours` are equivalent durations"] # [doc = " of time, representing each as a `Span` corresponds to two distinct values"] # [doc = " in memory. And serializing them to the ISO 8601 duration format will also"] # [doc = " preserve the units, for example, `PT1h60m` and `PT2h`."] # [doc = " * A `Span` supports non-uniform units like days, weeks, months and years."] # [doc = " Since not all days, weeks, months and years have the same length, they"] # [doc = " cannot be represented by a `SignedDuration`. In some cases, it may be"] # [doc = " appropriate, for example, to assume that all days are 24 hours long. But"] # [doc = " since Jiff sometimes assumes all days are 24 hours (for civil time) and"] # [doc = " sometimes doesn't (like for `Zoned` when respecting time zones), it would"] # [doc = " be inappropriate to bake one of those assumptions into a `SignedDuration`."] # [doc = " * A `SignedDuration` is a much smaller type than a `Span`. Specifically,"] # [doc = " it's a 96-bit integer. In contrast, a `Span` is much larger since it needs"] # [doc = " to track each individual unit separately."] # [doc = ""] # [doc = " Those differences in turn motivate some approximate reasoning for when to"] # [doc = " use `Span` and when to use `SignedDuration`:"] # [doc = ""] # [doc = " * If you don't care about keeping track of individual units separately or"] # [doc = " don't need the sophisticated rounding options available on a `Span`, it"] # [doc = " might be simpler and faster to use a `SignedDuration`."] # [doc = " * If you specifically need performance on arithmetic operations involving"] # [doc = " datetimes and durations, even if it's not as convenient or correct, then it"] # [doc = " might make sense to use a `SignedDuration`."] # [doc = " * If you need to perform arithmetic using a `std::time::Duration` and"] # [doc = " otherwise don't need the functionality of a `Span`, it might make sense"] # [doc = " to first convert the `std::time::Duration` to a `SignedDuration`, and then"] # [doc = " use one of the corresponding operations defined for `SignedDuration` on"] # [doc = " the datetime types. (They all support it.)"] # [doc = ""] # [doc = " In general, a `Span` provides more functionality and is overall more"] # [doc = " flexible. A `Span` can also deserialize all forms of ISO 8601 durations"] # [doc = " (as long as they're within Jiff's limits), including durations with units"] # [doc = " of years, months, weeks and days. A `SignedDuration`, by contrast, only"] # [doc = " supports units up to and including hours."] # [doc = ""] # [doc = " # Integration with datetime types"] # [doc = ""] # [doc = " All datetime types that support arithmetic using [`Span`](crate::Span) also"] # [doc = " support arithmetic using `SignedDuration` (and [`std::time::Duration`])."] # [doc = " For example, here's how to add an absolute duration to a [`Timestamp`]:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{SignedDuration, Timestamp};"] # [doc = ""] # [doc = " let ts1 = Timestamp::from_second(1_123_456_789)?;"] # [doc = " assert_eq!(ts1.to_string(), \"2005-08-07T23:19:49Z\");"] # [doc = ""] # [doc = " let duration = SignedDuration::new(59, 999_999_999);"] # [doc = " // Timestamp::checked_add is polymorphic! It can accept a"] # [doc = " // span or a duration."] # [doc = " let ts2 = ts1.checked_add(duration)?;"] # [doc = " assert_eq!(ts2.to_string(), \"2005-08-07T23:20:48.999999999Z\");"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " The same API pattern works with [`Zoned`], [`DateTime`], [`Date`] and"] # [doc = " [`Time`]."] # [doc = ""] # [doc = " # Interaction with daylight saving time and time zone transitions"] # [doc = ""] # [doc = " A `SignedDuration` always corresponds to a specific number of nanoseconds."] # [doc = " Since a [`Zoned`] is always a precise instant in time, adding a `SignedDuration`"] # [doc = " to a `Zoned` always behaves by adding the nanoseconds from the duration to"] # [doc = " the timestamp inside of `Zoned`. Consider `2024-03-10` in `US/Eastern`."] # [doc = " At `02:00:00`, daylight saving time came into effect, switching the UTC"] # [doc = " offset for the region from `-05` to `-04`. This has the effect of skipping"] # [doc = " an hour on the clocks:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{civil::date, SignedDuration};"] # [doc = ""] # [doc = " let zdt = date(2024, 3, 10).at(1, 59, 0, 0).in_tz(\"US/Eastern\")?;"] # [doc = " assert_eq!("] # [doc = "     zdt.checked_add(SignedDuration::from_hours(1))?,"] # [doc = "     // Time on the clock skipped an hour, but in this time"] # [doc = "     // zone, 03:59 is actually precisely 1 hour later than"] # [doc = "     // 01:59."] # [doc = "     date(2024, 3, 10).at(3, 59, 0, 0).in_tz(\"US/Eastern\")?,"] # [doc = " );"] # [doc = " // The same would apply if you used a `Span`:"] # [doc = " assert_eq!("] # [doc = "     zdt.checked_add(jiff::Span::new().hours(1))?,"] # [doc = "     // Time on the clock skipped an hour, but in this time"] # [doc = "     // zone, 03:59 is actually precisely 1 hour later than"] # [doc = "     // 01:59."] # [doc = "     date(2024, 3, 10).at(3, 59, 0, 0).in_tz(\"US/Eastern\")?,"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Where time zones might have a more interesting effect is in the definition"] # [doc = " of the \"day\" itself. If, for example, you encode the notion that a day is"] # [doc = " always 24 hours into your arithmetic, you might get unexpected results."] # [doc = " For example, let's say you want to find the datetime precisely one week"] # [doc = " after `2024-03-08T17:00` in the `US/Eastern` time zone. You might be"] # [doc = " tempted to just ask for the time that is `7 * 24` hours later:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{civil::date, SignedDuration};"] # [doc = ""] # [doc = " let zdt = date(2024, 3, 8).at(17, 0, 0, 0).in_tz(\"US/Eastern\")?;"] # [doc = " assert_eq!("] # [doc = "     zdt.checked_add(SignedDuration::from_hours(7 * 24))?,"] # [doc = "     date(2024, 3, 15).at(18, 0, 0, 0).in_tz(\"US/Eastern\")?,"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Notice that you get `18:00` and not `17:00`! That's because, as shown"] # [doc = " in the previous example, `2024-03-10` was only 23 hours long. That in turn"] # [doc = " implies that the week starting from `2024-03-08` is only `7 * 24 - 1` hours"] # [doc = " long. This can be tricky to get correct with absolute durations like"] # [doc = " `SignedDuration`, but a `Span` will handle this for you automatically:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{civil::date, ToSpan};"] # [doc = ""] # [doc = " let zdt = date(2024, 3, 8).at(17, 0, 0, 0).in_tz(\"US/Eastern\")?;"] # [doc = " assert_eq!("] # [doc = "     zdt.checked_add(1.week())?,"] # [doc = "     // The expected time!"] # [doc = "     date(2024, 3, 15).at(17, 0, 0, 0).in_tz(\"US/Eastern\")?,"] # [doc = " );"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " A `Span` achieves this by keeping track of individual units. Unlike a"] # [doc = " `SignedDuration`, it is not just a simple count of nanoseconds. It is a"] # [doc = " \"bag\" of individual units, and the arithmetic operations defined on a"] # [doc = " `Span` for `Zoned` know how to interpret \"day\" in a particular time zone"] # [doc = " at a particular instant in time."] # [doc = ""] # [doc = " With that said, the above does not mean that using a `SignedDuration` is"] # [doc = " always wrong. For example, if you're dealing with units of hours or lower,"] # [doc = " then all such units are uniform and so you'll always get the same results"] # [doc = " as with a `Span`. And using a `SignedDuration` can sometimes be simpler"] # [doc = " or faster."] # [derive (Clone , Copy , PartialEq , Eq , PartialOrd , Ord , Hash , Default)] pub struct SignedDuration { secs : i64 , nanos : i32 , }
};
}
