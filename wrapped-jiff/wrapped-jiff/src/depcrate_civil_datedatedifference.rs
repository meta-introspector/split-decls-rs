// Generated macro for DateDifference (struct)
macro_rules! Depcrate_civil_dateDateDifference {
() => {
// Module: crate::civil::date
// Provides: {"DateDifference"}
// Dependencies: {}
# [doc = " Options for [`Date::since`] and [`Date::until`]."] # [doc = ""] # [doc = " This type provides a way to configure the calculation of spans between two"] # [doc = " [`Date`] values. In particular, both `Date::since` and `Date::until` accept"] # [doc = " anything that implements `Into<DateDifference>`. There are a few key trait"] # [doc = " implementations that make this convenient:"] # [doc = ""] # [doc = " * `From<Date> for DateDifference` will construct a configuration consisting"] # [doc = " of just the date. So for example, `date1.until(date2)` will return the span"] # [doc = " from `date1` to `date2`."] # [doc = " * `From<DateTime> for DateDifference` will construct a configuration"] # [doc = " consisting of just the date from the given datetime. So for example,"] # [doc = " `date.since(datetime)` returns the span from `datetime.date()` to `date`."] # [doc = " * `From<(Unit, Date)>` is a convenient way to specify the largest units"] # [doc = " that should be present on the span returned. By default, the largest units"] # [doc = " are days. Using this trait implementation is equivalent to"] # [doc = " `DateDifference::new(date).largest(unit)`."] # [doc = " * `From<(Unit, DateTime)>` is like the one above, but with the date from"] # [doc = " the given datetime."] # [doc = ""] # [doc = " One can also provide a `DateDifference` value directly. Doing so is"] # [doc = " necessary to use the rounding features of calculating a span. For example,"] # [doc = " setting the smallest unit (defaults to [`Unit::Day`]), the rounding mode"] # [doc = " (defaults to [`RoundMode::Trunc`]) and the rounding increment (defaults to"] # [doc = " `1`). The defaults are selected such that no rounding occurs."] # [doc = ""] # [doc = " Rounding a span as part of calculating it is provided as a convenience."] # [doc = " Callers may choose to round the span as a distinct step via"] # [doc = " [`Span::round`], but callers may need to provide a reference date"] # [doc = " for rounding larger units. By coupling rounding with routines like"] # [doc = " [`Date::since`], the reference date can be set automatically based on"] # [doc = " the input to `Date::since`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to round a span between two date to the nearest"] # [doc = " year, with ties breaking away from zero."] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{civil::{Date, DateDifference}, RoundMode, ToSpan, Unit};"] # [doc = ""] # [doc = " let d1 = \"2024-03-15\".parse::<Date>()?;"] # [doc = " let d2 = \"2030-09-13\".parse::<Date>()?;"] # [doc = " let span = d1.until("] # [doc = "     DateDifference::new(d2)"] # [doc = "         .smallest(Unit::Year)"] # [doc = "         .mode(RoundMode::HalfExpand),"] # [doc = " )?;"] # [doc = " assert_eq!(span, 6.years().fieldwise());"] # [doc = ""] # [doc = " // If the span were one day longer, it would round up to 7 years."] # [doc = " let d2 = \"2030-09-14\".parse::<Date>()?;"] # [doc = " let span = d1.until("] # [doc = "     DateDifference::new(d2)"] # [doc = "         .smallest(Unit::Year)"] # [doc = "         .mode(RoundMode::HalfExpand),"] # [doc = " )?;"] # [doc = " assert_eq!(span, 7.years().fieldwise());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Copy , Debug)] pub struct DateDifference { date : Date , round : SpanRound < 'static > , }
};
}
