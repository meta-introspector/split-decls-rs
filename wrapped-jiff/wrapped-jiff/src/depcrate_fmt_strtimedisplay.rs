// Generated macro for Display (struct)
macro_rules! Depcrate_fmt_strtimeDisplay {
() => {
// Module: crate::fmt::strtime
// Provides: {"Display"}
// Dependencies: {}
# [doc = " A \"lazy\" implementation of `std::fmt::Display` for `strftime`."] # [doc = ""] # [doc = " Values of this type are created by the `strftime` methods on the various"] # [doc = " datetime types in this crate. For example, [`Zoned::strftime`]."] # [doc = ""] # [doc = " A `Display` captures the information needed from the datetime and waits to"] # [doc = " do the actual formatting when this type's `std::fmt::Display` trait"] # [doc = " implementation is actually used."] # [doc = ""] # [doc = " # Errors and panics"] # [doc = ""] # [doc = " This trait implementation returns an error when the underlying formatting"] # [doc = " can fail. Formatting can fail either because of an invalid format string,"] # [doc = " or if formatting requires a field in `BrokenDownTime` to be set that isn't."] # [doc = " For example, trying to format a [`DateTime`] with the `%z` specifier will"] # [doc = " fail because a `DateTime` has no time zone or offset information associated"] # [doc = " with it."] # [doc = ""] # [doc = " Note though that the `std::fmt::Display` API doesn't support surfacing"] # [doc = " arbitrary errors. All errors collapse into the unit `std::fmt::Error`"] # [doc = " struct. To see the actual error, use [`BrokenDownTime::format`],"] # [doc = " [`BrokenDownTime::to_string`] or [`strtime::format`](format())."] # [doc = " Unfortunately, the `std::fmt::Display` trait is used in many places where"] # [doc = " there is no way to report errors other than panicking."] # [doc = ""] # [doc = " Therefore, only use this type if you know your formatting string is valid"] # [doc = " and that the datetime type being formatted has all of the information"] # [doc = " required by the format string. Moreover, the `strftime` implementation in"] # [doc = " this crate is specifically designed to never error based on the specific"] # [doc = " values. For example, even though `%y` can only _parse_ years in the"] # [doc = " `1969-2068` range, it can format any valid year supported by Jiff."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to format a zoned datetime using"] # [doc = " [`Zoned::strftime`]:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::civil::date;"] # [doc = ""] # [doc = " let zdt = date(2024, 7, 15).at(16, 24, 59, 0).in_tz(\"America/New_York\")?;"] # [doc = " let string = zdt.strftime(\"%a, %-d %b %Y %T %z\").to_string();"] # [doc = " assert_eq!(string, \"Mon, 15 Jul 2024 16:24:59 -0400\");"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " Or use it directly when writing to something:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{civil::date, fmt::strtime};"] # [doc = ""] # [doc = " let zdt = date(2024, 7, 15).at(16, 24, 59, 0).in_tz(\"America/New_York\")?;"] # [doc = ""] # [doc = " let string = format!(\"the date is: {}\", zdt.strftime(\"%-m/%-d/%-Y\"));"] # [doc = " assert_eq!(string, \"the date is: 7/15/2024\");"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub struct Display < 'f > { pub (crate) fmt : & 'f [u8] , pub (crate) tm : BrokenDownTime , }
};
}
