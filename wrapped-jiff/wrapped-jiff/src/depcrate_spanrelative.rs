// Generated macro for Relative (enum)
macro_rules! Depcrate_spanRelative {
() => {
// Module: crate::span
// Provides: {"Relative"}
// Dependencies: {}
# [doc = " An internal abstraction for managing a relative datetime for use in some"] # [doc = " `Span` APIs."] # [doc = ""] # [doc = " This is effectively the same as a `SpanRelativeTo`, but uses a `Cow<Zoned>`"] # [doc = " instead of a `&Zoned`. This makes it non-`Copy`, but allows us to craft a"] # [doc = " more uniform API. (i.e., `relative + span = relative` instead of `relative"] # [doc = " + span = owned_relative` or whatever.) Note that the `Copy` impl on"] # [doc = " `SpanRelativeTo` means it has to accept a `&Zoned`. It can't ever take a"] # [doc = " `Zoned` since it is non-Copy."] # [doc = ""] # [doc = " NOTE: Separately from above, I think it's plausible that this type could be"] # [doc = " designed a bit differently. Namely, something like this:"] # [doc = ""] # [doc = " ```text"] # [doc = " struct Relative<'a> {"] # [doc = "     tz: Option<&'a TimeZone>,"] # [doc = "     dt: DateTime,"] # [doc = "     ts: Timestamp,"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " That is, we do zone aware stuff but without an actual `Zoned` type. But I"] # [doc = " think in order to make that work, we would need to expose most of the"] # [doc = " `Zoned` API as functions on its component types (DateTime, Timestamp and"] # [doc = " TimeZone). I think we are likely to want to do that for public API reasons,"] # [doc = " but I'd like to resist it since I think it will add a lot of complexity."] # [doc = " Or maybe we need a `Unzoned` type that is `DateTime` and `Timestamp`, but"] # [doc = " requires passing the time zone in to each of its methods. That might work"] # [doc = " quite well, even if it was just an internal type."] # [doc = ""] # [doc = " Anyway, I'm not 100% sure the above would work, but I think it would. It"] # [doc = " would be nicer because everything would be `Copy` all the time. We'd never"] # [doc = " need a `Cow<TimeZone>` for example, because we never need to change or"] # [doc = " create a new time zone."] # [derive (Clone , Debug)] enum Relative < 'a > { Civil (RelativeCivil) , Zoned (RelativeZoned < 'a >) , }
};
}
