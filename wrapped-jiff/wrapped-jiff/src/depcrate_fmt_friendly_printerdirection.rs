// Generated macro for Direction (enum)
macro_rules! Depcrate_fmt_friendly_printerDirection {
() => {
// Module: crate::fmt::friendly::printer
// Provides: {"Direction"}
// Dependencies: {}
# [doc = " Configuration for [`SpanPrinter::direction`]."] # [doc = ""] # [doc = " This controls how the sign, if at all, is included in the formatted"] # [doc = " duration."] # [doc = ""] # [doc = " When using the \"hours-minutes-seconds\" format, `Auto` and `Suffix` are"] # [doc = " both treated as equivalent to `Sign` when all calendar units (days and"] # [doc = " greater) are zero."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{fmt::friendly::{Direction, SpanPrinter}, SignedDuration};"] # [doc = ""] # [doc = " let duration = SignedDuration::from_secs(-1);"] # [doc = ""] # [doc = " let printer = SpanPrinter::new();"] # [doc = " assert_eq!(printer.duration_to_string(&duration), \"1s ago\");"] # [doc = ""] # [doc = " let printer = SpanPrinter::new().direction(Direction::Sign);"] # [doc = " assert_eq!(printer.duration_to_string(&duration), \"-1s\");"] # [doc = " ```"] # [derive (Clone , Copy , Debug)] # [non_exhaustive] pub enum Direction { # [doc = " Sets the sign format based on other configuration options."] # [doc = ""] # [doc = " When [`SpanPrinter::spacing`] is set to [`Spacing::None`], then"] # [doc = " `Auto` is equivalent to `Sign`."] # [doc = ""] # [doc = " When using the \"hours-minutes-seconds\" format, `Auto` is equivalent to"] # [doc = " `Sign` when all calendar units (days and greater) are zero."] # [doc = ""] # [doc = " Otherwise, `Auto` is equivalent to `Suffix`."] # [doc = ""] # [doc = " This is the default used by [`SpanPrinter`]."] Auto , # [doc = " When set, a sign is only written when the span or duration is negative."] # [doc = " And when it is written, it is written as a prefix of the formatted"] # [doc = " duration."] Sign , # [doc = " A sign is always written, with `-` for negative spans and `+` for all"] # [doc = " non-negative spans. The sign is always written as a prefix of the"] # [doc = " formatted duration."] ForceSign , # [doc = " When set, a sign is only written when the span or duration is negative."] # [doc = " And when it is written, it is written as a suffix via a trailing ` ago`"] # [doc = " string."] Suffix , }
};
}
