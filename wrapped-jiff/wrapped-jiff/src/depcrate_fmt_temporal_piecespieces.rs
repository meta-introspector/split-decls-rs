// Generated macro for Pieces (struct)
macro_rules! Depcrate_fmt_temporal_piecesPieces {
() => {
// Module: crate::fmt::temporal::pieces
// Provides: {"Pieces"}
// Dependencies: {}
# [doc = " A low level representation of a parsed Temporal ISO 8601 datetime string."] # [doc = ""] # [doc = " Most users should not need to use or care about this type. Its purpose is"] # [doc = " to represent the individual components of a datetime string for more"] # [doc = " flexible parsing when use cases call for it."] # [doc = ""] # [doc = " One can parse into `Pieces` via [`Pieces::parse`]. Its date, time"] # [doc = " (optional), offset (optional) and time zone annotation (optional) can be"] # [doc = " queried independently. Each component corresponds to the following in a"] # [doc = " datetime string:"] # [doc = ""] # [doc = " ```text"] # [doc = " {date}T{time}{offset}[{time-zone-annotation}]"] # [doc = " ```"] # [doc = ""] # [doc = " For example:"] # [doc = ""] # [doc = " ```text"] # [doc = " 2025-01-03T19:54-05[America/New_York]"] # [doc = " ```"] # [doc = ""] # [doc = " A date is the only required component."] # [doc = ""] # [doc = " A `Pieces` can also be constructed from structured values via its `From`"] # [doc = " trait implementations. The `From` trait has the following implementations"] # [doc = " available:"] # [doc = ""] # [doc = " * `From<Date>` creates a `Pieces` with just a civil [`Date`]. All other"] # [doc = " components are left empty."] # [doc = " * `From<DateTime>` creates a `Pieces` with a civil [`Date`] and [`Time`]."] # [doc = " The offset and time zone annotation are left empty."] # [doc = " * `From<Timestamp>` creates a `Pieces` from a [`Timestamp`] using"] # [doc = " a Zulu offset. This signifies that the precise instant is known, but the"] # [doc = " local time's offset from UTC is unknown. The [`Date`] and [`Time`] are"] # [doc = " determined via `Offset::UTC.to_datetime(timestamp)`. The time zone"] # [doc = " annotation is left empty."] # [doc = " * `From<(Timestamp, Offset)>` creates a `Pieces` from a [`Timestamp`] and"] # [doc = " an [`Offset`]. The [`Date`] and [`Time`] are determined via"] # [doc = " `offset.to_datetime(timestamp)`. The time zone annotation is left empty."] # [doc = " * `From<&Zoned>` creates a `Pieces` from a [`Zoned`]. This populates all"] # [doc = " fields of a `Pieces`."] # [doc = ""] # [doc = " A `Pieces` can be converted to a Temporal ISO 8601 string via its `Display`"] # [doc = " trait implementation."] # [doc = ""] # [doc = " # Example: distinguishing between `Z`, `+00:00` and `-00:00`"] # [doc = ""] # [doc = " With `Pieces`, it's possible to parse a datetime string and inspect the"] # [doc = " \"type\" of its offset when it is zero. This makes use of the"] # [doc = " [`PiecesOffset`] and [`PiecesNumericOffset`] auxiliary types."] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{"] # [doc = "     fmt::temporal::{Pieces, PiecesNumericOffset, PiecesOffset},"] # [doc = "     tz::Offset,"] # [doc = " };"] # [doc = ""] # [doc = " let pieces = Pieces::parse(\"1970-01-01T00:00:00Z\")?;"] # [doc = " let off = pieces.offset().unwrap();"] # [doc = " // Parsed as Zulu."] # [doc = " assert_eq!(off, PiecesOffset::Zulu);"] # [doc = " // Gets converted from Zulu to UTC, i.e., just zero."] # [doc = " assert_eq!(off.to_numeric_offset(), Offset::UTC);"] # [doc = ""] # [doc = " let pieces = Pieces::parse(\"1970-01-01T00:00:00-00:00\")?;"] # [doc = " let off = pieces.offset().unwrap();"] # [doc = " // Parsed as a negative zero."] # [doc = " assert_eq!(off, PiecesOffset::from("] # [doc = "     PiecesNumericOffset::from(Offset::UTC).with_negative_zero(),"] # [doc = " ));"] # [doc = " // Gets converted from -00:00 to UTC, i.e., just zero."] # [doc = " assert_eq!(off.to_numeric_offset(), Offset::UTC);"] # [doc = ""] # [doc = " let pieces = Pieces::parse(\"1970-01-01T00:00:00+00:00\")?;"] # [doc = " let off = pieces.offset().unwrap();"] # [doc = " // Parsed as a positive zero."] # [doc = " assert_eq!(off, PiecesOffset::from("] # [doc = "     PiecesNumericOffset::from(Offset::UTC),"] # [doc = " ));"] # [doc = " // Gets converted from -00:00 to UTC, i.e., just zero."] # [doc = " assert_eq!(off.to_numeric_offset(), Offset::UTC);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " It's rare to need to care about these differences, but the above example"] # [doc = " demonstrates that `Pieces` doesn't try to do any automatic translation for"] # [doc = " you."] # [doc = ""] # [doc = " # Example: it is very easy to misuse `Pieces`"] # [doc = ""] # [doc = " This example shows how easily you can shoot yourself in the foot with"] # [doc = " `Pieces`:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{fmt::temporal::{Pieces, TimeZoneAnnotation}, tz};"] # [doc = ""] # [doc = " let mut pieces = Pieces::parse(\"2025-01-03T07:55+02[Africa/Cairo]\")?;"] # [doc = " pieces = pieces.with_offset(tz::offset(-10));"] # [doc = " // This is nonsense because the offset isn't compatible with the time zone!"] # [doc = " // Moreover, the actual instant that this timestamp represents has changed."] # [doc = " assert_eq!(pieces.to_string(), \"2025-01-03T07:55:00-10:00[Africa/Cairo]\");"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " In the above example, we take a parsed `Pieces`, change its offset and"] # [doc = " then format it back into a string. There are no speed bumps or errors."] # [doc = " A `Pieces` will just blindly follow your instruction, even if it produces"] # [doc = " a nonsense result. Nonsense results are still parsable back into `Pieces`:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{civil, fmt::temporal::Pieces, tz::{TimeZone, offset}};"] # [doc = ""] # [doc = " let pieces = Pieces::parse(\"2025-01-03T07:55:00-10:00[Africa/Cairo]\")?;"] # [doc = " assert_eq!(pieces.date(), civil::date(2025, 1, 3));"] # [doc = " assert_eq!(pieces.time(), Some(civil::time(7, 55, 0, 0)));"] # [doc = " assert_eq!(pieces.to_numeric_offset(), Some(offset(-10)));"] # [doc = " assert_eq!(pieces.to_time_zone()?, Some(TimeZone::get(\"Africa/Cairo\")?));"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " This exemplifies that `Pieces` is a mostly \"dumb\" type that passes"] # [doc = " through the data it contains, even if it doesn't make sense."] # [doc = ""] # [doc = " # Case study: how to parse `2025-01-03T17:28-05` into `Zoned`"] # [doc = ""] # [doc = " One thing in particular that `Pieces` enables callers to do is side-step"] # [doc = " some of the stricter requirements placed on the higher level parsing"] # [doc = " functions (such as `Zoned`'s `FromStr` trait implementation). For example,"] # [doc = " parsing a datetime string into a `Zoned` _requires_ that the string contain"] # [doc = " a time zone annotation. Namely, parsing `2025-01-03T17:28-05` into a"] # [doc = " `Zoned` will fail:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::Zoned;"] # [doc = ""] # [doc = " assert_eq!("] # [doc = "     \"2025-01-03T17:28-05\".parse::<Zoned>().unwrap_err().to_string(),"] # [doc = "     \"failed to find time zone in square brackets in \\"] # [doc = "      \\\"2025-01-03T17:28-05\\\", which is required for \\"] # [doc = "      parsing a zoned instant\","] # [doc = " );"] # [doc = " ```"] # [doc = ""] # [doc = " The above fails because an RFC 3339 timestamp only contains an offset,"] # [doc = " not a time zone, and thus the resulting `Zoned` could never do time zone"] # [doc = " aware arithmetic."] # [doc = ""] # [doc = " However, in some cases, you might want to bypass these protections and"] # [doc = " creat a `Zoned` value with a fixed offset time zone anyway. For example,"] # [doc = " perhaps your use cases don't need time zone aware arithmetic, but want to"] # [doc = " preserve the offset anyway. This can be accomplished with `Pieces`:"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{fmt::temporal::Pieces, tz::TimeZone};"] # [doc = ""] # [doc = " let pieces = Pieces::parse(\"2025-01-03T17:28-05\")?;"] # [doc = " let time = pieces.time().unwrap_or_else(jiff::civil::Time::midnight);"] # [doc = " let dt = pieces.date().to_datetime(time);"] # [doc = " let Some(offset) = pieces.to_numeric_offset() else {"] # [doc = "     let msg = format!("] # [doc = "         \"datetime string has no offset, \\"] # [doc = "          and thus cannot be parsed into an instant\","] # [doc = "     );"] # [doc = "     return Err(msg.into());"] # [doc = " };"] # [doc = " let zdt = TimeZone::fixed(offset).to_zoned(dt)?;"] # [doc = " assert_eq!(zdt.to_string(), \"2025-01-03T17:28:00-05:00[-05:00]\");"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " One problem with the above code snippet is that it completely ignores if"] # [doc = " a time zone annotation is present. If it is, it probably makes sense to use"] # [doc = " it, but \"fall back\" to a fixed offset time zone if it isn't (which the"] # [doc = " higher level `Zoned` parsing function won't do for you):"] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{fmt::temporal::Pieces, tz::TimeZone};"] # [doc = ""] # [doc = " let timestamp = \"2025-01-02T15:13-05\";"] # [doc = ""] # [doc = " let pieces = Pieces::parse(timestamp)?;"] # [doc = " let time = pieces.time().unwrap_or_else(jiff::civil::Time::midnight);"] # [doc = " let dt = pieces.date().to_datetime(time);"] # [doc = " let tz = match pieces.to_time_zone()? {"] # [doc = "     Some(tz) => tz,"] # [doc = "     None => {"] # [doc = "         let Some(offset) = pieces.to_numeric_offset() else {"] # [doc = "             let msg = format!("] # [doc = "                 \"timestamp `{timestamp}` has no time zone \\"] # [doc = "                  or offset, and thus cannot be parsed into \\"] # [doc = "                  an instant\","] # [doc = "             );"] # [doc = "             return Err(msg.into());"] # [doc = "         };"] # [doc = "         TimeZone::fixed(offset)"] # [doc = "     }"] # [doc = " };"] # [doc = " // We don't bother with offset conflict resolution. And note that"] # [doc = " // this uses automatic \"compatible\" disambiguation in the case of"] # [doc = " // discontinuities. Of course, this is all moot if `TimeZone` is"] # [doc = " // fixed. The above code handles the case where it isn't!"] # [doc = " let zdt = tz.to_zoned(dt)?;"] # [doc = " assert_eq!(zdt.to_string(), \"2025-01-02T15:13:00-05:00[-05:00]\");"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " This is mostly the same as above, but if an annotation is present, we use"] # [doc = " a `TimeZone` derived from that over the offset present."] # [doc = ""] # [doc = " However, this still doesn't quite capture what happens when parsing into a"] # [doc = " `Zoned` value. In particular, parsing into a `Zoned` is _also_ doing offset"] # [doc = " conflict resolution for you. An offset conflict occurs when there is a"] # [doc = " mismatch between the offset in an RFC 3339 timestamp and the time zone in"] # [doc = " an RFC 9557 time zone annotation."] # [doc = ""] # [doc = " For example, `2024-06-14T17:30-05[America/New_York]` has a mismatch"] # [doc = " since the date is in daylight saving time, but the offset, `-05`, is the"] # [doc = " offset for standard time in `America/New_York`. If this datetime were"] # [doc = " fed to the above code, then the `-05` offset would be completely ignored"] # [doc = " and `America/New_York` would resolve the datetime based on its rules. In"] # [doc = " this case, you'd get `2024-06-14T17:30-04`, which is a different instant"] # [doc = " than the original datetime!"] # [doc = ""] # [doc = " You can either implement your own conflict resolution or use"] # [doc = " [`tz::OffsetConflict`](crate::tz::OffsetConflict) to do it for you."] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{fmt::temporal::Pieces, tz::{OffsetConflict, TimeZone}};"] # [doc = ""] # [doc = " let timestamp = \"2024-06-14T17:30-05[America/New_York]\";"] # [doc = " // The default for conflict resolution when parsing into a `Zoned` is"] # [doc = " // actually `Reject`, but we use `AlwaysOffset` here to show a different"] # [doc = " // strategy. You'll want to pick the conflict resolution that suits your"] # [doc = " // needs. The `Reject` strategy is what you should pick if you aren't"] # [doc = " // sure."] # [doc = " let conflict_resolution = OffsetConflict::AlwaysOffset;"] # [doc = ""] # [doc = " let pieces = Pieces::parse(timestamp)?;"] # [doc = " let time = pieces.time().unwrap_or_else(jiff::civil::Time::midnight);"] # [doc = " let dt = pieces.date().to_datetime(time);"] # [doc = " let ambiguous_zdt = match pieces.to_time_zone()? {"] # [doc = "     Some(tz) => {"] # [doc = "         match pieces.to_numeric_offset() {"] # [doc = "             None => tz.into_ambiguous_zoned(dt),"] # [doc = "             Some(offset) => {"] # [doc = "                 conflict_resolution.resolve(dt, offset, tz)?"] # [doc = "             }"] # [doc = "         }"] # [doc = "     }"] # [doc = "     None => {"] # [doc = "         let Some(offset) = pieces.to_numeric_offset() else {"] # [doc = "             let msg = format!("] # [doc = "                 \"timestamp `{timestamp}` has no time zone \\"] # [doc = "                  or offset, and thus cannot be parsed into \\"] # [doc = "                  an instant\","] # [doc = "             );"] # [doc = "             return Err(msg.into());"] # [doc = "         };"] # [doc = "         // Won't even be ambiguous, but gets us the same"] # [doc = "         // type as the branch above."] # [doc = "         TimeZone::fixed(offset).into_ambiguous_zoned(dt)"] # [doc = "     }"] # [doc = " };"] # [doc = " // We do compatible disambiguation here like we do in the previous"] # [doc = " // examples, but you could choose any strategy. As with offset conflict"] # [doc = " // resolution, if you aren't sure what to pick, a safe choice here would"] # [doc = " // be `ambiguous_zdt.unambiguous()`, which will return an error if the"] # [doc = " // datetime is ambiguous in any way. Then, if you ever hit an error, you"] # [doc = " // can examine the case to see if it should be handled in a different way."] # [doc = " let zdt = ambiguous_zdt.compatible()?;"] # [doc = " // Notice that we now have a different civil time and offset, but the"] # [doc = " // instant it corresponds to is the same as the one we started with."] # [doc = " assert_eq!(zdt.to_string(), \"2024-06-14T18:30:00-04:00[America/New_York]\");"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " The above has effectively completely rebuilt the higher level `Zoned`"] # [doc = " parsing routine, but with a fallback to a fixed time zone when a time zone"] # [doc = " annotation is not present."] # [doc = ""] # [doc = " # Case study: inferring the time zone of RFC 3339 timestamps"] # [doc = ""] # [doc = " As [one real world use case details][infer-time-zone], it might be"] # [doc = " desirable to try and infer the time zone of RFC 3339 timestamps with"] # [doc = " varying offsets. This might be applicable when:"] # [doc = ""] # [doc = " * You have out-of-band information, possibly contextual, that indicates"] # [doc = " the timestamps have to come from a fixed set of time zones."] # [doc = " * The time zones have different standard offsets."] # [doc = " * You have a specific desire or need to use a [`Zoned`] value for its"] # [doc = " ergonomics and time zone aware handling. After all, in this case, you"] # [doc = " believe the timestamps to actually be generated from a specific time zone,"] # [doc = " but the interchange format doesn't support carrying that information. Or"] # [doc = " the source data simply omits it."] # [doc = ""] # [doc = " In other words, you might be trying to make the best of a bad situation."] # [doc = ""] # [doc = " A `Pieces` can help you accomplish this because it gives you access to each"] # [doc = " component of a parsed datetime, and thus lets you implement arbitrary logic"] # [doc = " for how to translate that into a `Zoned`. In this case, there is"] # [doc = " contextual information that Jiff can't possibly know about."] # [doc = ""] # [doc = " The general approach we take here is to make use of"] # [doc = " [`tz::OffsetConflict`](crate::tz::OffsetConflict) to query whether a"] # [doc = " timestamp has a fixed offset compatible with a particular time zone. And if"] # [doc = " so, we can _probably_ assume it comes from that time zone. One hitch is"] # [doc = " that it's possible for the timestamp to be valid for multiple time zones,"] # [doc = " so we check that as well."] # [doc = ""] # [doc = " In the use case linked above, we have fixed offset timestamps from"] # [doc = " `America/Chicago` and `America/New_York`. So let's try implementing the"] # [doc = " above strategy. Note that we assume our inputs are RFC 3339 fixed offset"] # [doc = " timestamps and error otherwise. This is just to keep things simple. To"] # [doc = " handle data that is more varied, see the previous case study where we"] # [doc = " respect a time zone annotation if it's present, and fall back to a fixed"] # [doc = " offset time zone if it isn't."] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{fmt::temporal::Pieces, tz::{OffsetConflict, TimeZone}, Zoned};"] # [doc = ""] # [doc = " // The time zones we're allowed to choose from."] # [doc = " let tzs = &["] # [doc = "     TimeZone::get(\"America/New_York\")?,"] # [doc = "     TimeZone::get(\"America/Chicago\")?,"] # [doc = " ];"] # [doc = ""] # [doc = " // Here's our data that lacks time zones. The task is to assign a time zone"] # [doc = " // from `tzs` to each below and convert it to a `Zoned`. If we fail on any"] # [doc = " // one, then we substitute `None`."] # [doc = " let data = &["] # [doc = "     \"2024-01-13T10:33-05\","] # [doc = "     \"2024-01-25T12:15-06\","] # [doc = "     \"2024-03-10T02:30-05\","] # [doc = "     \"2024-06-08T14:01-05\","] # [doc = "     \"2024-06-12T11:46-04\","] # [doc = "     \"2024-11-03T01:30-05\","] # [doc = " ];"] # [doc = " // Our answers."] # [doc = " let mut zdts: Vec<Option<Zoned>> = vec![];"] # [doc = " for string in data {"] # [doc = "     // Parse and gather up the data that we can from the input."] # [doc = "     // In this case, that's a civil datetime and an offset from UTC."] # [doc = "     let pieces = Pieces::parse(string)?;"] # [doc = "     let time = pieces.time().unwrap_or_else(jiff::civil::Time::midnight);"] # [doc = "     let dt = pieces.date().to_datetime(time);"] # [doc = "     let Some(offset) = pieces.to_numeric_offset() else {"] # [doc = "         // A robust implementation should use a TZ annotation if present."] # [doc = "         return Err(\"missing offset\".into());"] # [doc = "     };"] # [doc = "     // Now collect all time zones that are valid for this timestamp."] # [doc = "     let mut candidates = vec![];"] # [doc = "     for tz in tzs {"] # [doc = "         let result = OffsetConflict::Reject.resolve(dt, offset, tz.clone());"] # [doc = "         // The parsed offset isn't valid for this time zone, so reject it."] # [doc = "         let Ok(ambiguous_zdt) = result else { continue };"] # [doc = "         // This can never fail because we used the \"reject\" conflict"] # [doc = "         // resolution strategy. It will never return an ambiguous"] # [doc = "         // `Zoned` since we always have a valid offset that does"] # [doc = "         // disambiguation for us."] # [doc = "         let zdt = ambiguous_zdt.unambiguous().unwrap();"] # [doc = "         candidates.push(zdt);"] # [doc = "     }"] # [doc = "     if candidates.len() == 1 {"] # [doc = "         zdts.push(Some(candidates.pop().unwrap()));"] # [doc = "     } else {"] # [doc = "         zdts.push(None);"] # [doc = "     }"] # [doc = " }"] # [doc = " assert_eq!(zdts, vec!["] # [doc = "     Some(\"2024-01-13T10:33-05[America/New_York]\".parse()?),"] # [doc = "     Some(\"2024-01-25T12:15-06[America/Chicago]\".parse()?),"] # [doc = "     // Failed because the clock time falls in a gap in the"] # [doc = "     // transition to daylight saving time, and it could be"] # [doc = "     // valid for either America/New_York or America/Chicago."] # [doc = "     None,"] # [doc = "     Some(\"2024-06-08T14:01-05[America/Chicago]\".parse()?),"] # [doc = "     Some(\"2024-06-12T11:46-04[America/New_York]\".parse()?),"] # [doc = "     // Failed because the clock time falls in a fold in the"] # [doc = "     // transition out of daylight saving time, and it could be"] # [doc = "     // valid for either America/New_York or America/Chicago."] # [doc = "     None,"] # [doc = " ]);"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " The one hitch here is that if the time zones are close to each"] # [doc = " geographically and both have daylight saving time, then there are some"] # [doc = " RFC 3339 timestamps that are truly ambiguous. For example,"] # [doc = " `2024-11-03T01:30-05` is perfectly valid for both `America/New_York` and"] # [doc = " `America/Chicago`. In this case, there is no way to tell which time zone"] # [doc = " the timestamp belongs to. It might be reasonable to return an error in"] # [doc = " this case or omit the timestamp. It depends on what you need to do."] # [doc = ""] # [doc = " With more effort, it would also be possible to optimize the above routine"] # [doc = " by utilizing [`TimeZone::preceding`] and [`TimeZone::following`] to get"] # [doc = " the exact boundaries of each time zone transition. Then you could use an"] # [doc = " offset lookup table for each range to determine the appropriate time zone."] # [doc = ""] # [doc = " [infer-time-zone]: https://github.com/BurntSushi/jiff/discussions/181#discussioncomment-11729435"] # [derive (Clone , Debug , Eq , Hash , PartialEq)] pub struct Pieces < 'n > { date : Date , time : Option < Time > , offset : Option < PiecesOffset > , time_zone_annotation : Option < TimeZoneAnnotation < 'n > > , }
};
}
