// Generated macro for ZonedDifference (struct)
macro_rules! Depcrate_zonedZonedDifference {
() => {
// Module: crate::zoned
// Provides: {"ZonedDifference"}
// Dependencies: {}
# [doc = " Options for [`Zoned::since`] and [`Zoned::until`]."] # [doc = ""] # [doc = " This type provides a way to configure the calculation of spans between two"] # [doc = " [`Zoned`] values. In particular, both `Zoned::since` and `Zoned::until`"] # [doc = " accept anything that implements `Into<ZonedDifference>`. There are a few"] # [doc = " key trait implementations that make this convenient:"] # [doc = ""] # [doc = " * `From<&Zoned> for ZonedDifference` will construct a configuration"] # [doc = " consisting of just the zoned datetime. So for example, `zdt1.since(zdt2)`"] # [doc = " returns the span from `zdt2` to `zdt1`."] # [doc = " * `From<(Unit, &Zoned)>` is a convenient way to specify the largest units"] # [doc = " that should be present on the span returned. By default, the largest units"] # [doc = " are days. Using this trait implementation is equivalent to"] # [doc = " `ZonedDifference::new(&zdt).largest(unit)`."] # [doc = ""] # [doc = " One can also provide a `ZonedDifference` value directly. Doing so"] # [doc = " is necessary to use the rounding features of calculating a span. For"] # [doc = " example, setting the smallest unit (defaults to [`Unit::Nanosecond`]), the"] # [doc = " rounding mode (defaults to [`RoundMode::Trunc`]) and the rounding increment"] # [doc = " (defaults to `1`). The defaults are selected such that no rounding occurs."] # [doc = ""] # [doc = " Rounding a span as part of calculating it is provided as a convenience."] # [doc = " Callers may choose to round the span as a distinct step via"] # [doc = " [`Span::round`], but callers may need to provide a reference date"] # [doc = " for rounding larger units. By coupling rounding with routines like"] # [doc = " [`Zoned::since`], the reference date can be set automatically based on"] # [doc = " the input to `Zoned::since`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to round a span between two zoned datetimes to the"] # [doc = " nearest half-hour, with ties breaking away from zero."] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{RoundMode, ToSpan, Unit, Zoned, ZonedDifference};"] # [doc = ""] # [doc = " let zdt1 = \"2024-03-15 08:14:00.123456789[America/New_York]\".parse::<Zoned>()?;"] # [doc = " let zdt2 = \"2030-03-22 15:00[America/New_York]\".parse::<Zoned>()?;"] # [doc = " let span = zdt1.until("] # [doc = "     ZonedDifference::new(&zdt2)"] # [doc = "         .smallest(Unit::Minute)"] # [doc = "         .largest(Unit::Year)"] # [doc = "         .mode(RoundMode::HalfExpand)"] # [doc = "         .increment(30),"] # [doc = " )?;"] # [doc = " assert_eq!(span, 6.years().days(7).hours(7).fieldwise());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Copy , Debug)] pub struct ZonedDifference < 'a > { zoned : & 'a Zoned , round : SpanRound < 'static > , }
};
}
