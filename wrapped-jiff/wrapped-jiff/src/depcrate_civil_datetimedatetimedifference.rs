// Generated macro for DateTimeDifference (struct)
macro_rules! Depcrate_civil_datetimeDateTimeDifference {
() => {
// Module: crate::civil::datetime
// Provides: {"DateTimeDifference"}
// Dependencies: {}
# [doc = " Options for [`DateTime::since`] and [`DateTime::until`]."] # [doc = ""] # [doc = " This type provides a way to configure the calculation of"] # [doc = " spans between two [`DateTime`] values. In particular, both"] # [doc = " `DateTime::since` and `DateTime::until` accept anything that implements"] # [doc = " `Into<DateTimeDifference>`. There are a few key trait implementations that"] # [doc = " make this convenient:"] # [doc = ""] # [doc = " * `From<DateTime> for DateTimeDifference` will construct a configuration"] # [doc = " consisting of just the datetime. So for example, `dt1.since(dt2)` returns"] # [doc = " the span from `dt2` to `dt1`."] # [doc = " * `From<Date> for DateTimeDifference` will construct a configuration"] # [doc = " consisting of just the datetime built from the date given at midnight on"] # [doc = " that day."] # [doc = " * `From<(Unit, DateTime)>` is a convenient way to specify the largest units"] # [doc = " that should be present on the span returned. By default, the largest units"] # [doc = " are days. Using this trait implementation is equivalent to"] # [doc = " `DateTimeDifference::new(datetime).largest(unit)`."] # [doc = " * `From<(Unit, Date)>` is like the one above, but with the time component"] # [doc = " fixed to midnight."] # [doc = ""] # [doc = " One can also provide a `DateTimeDifference` value directly. Doing so"] # [doc = " is necessary to use the rounding features of calculating a span. For"] # [doc = " example, setting the smallest unit (defaults to [`Unit::Nanosecond`]), the"] # [doc = " rounding mode (defaults to [`RoundMode::Trunc`]) and the rounding increment"] # [doc = " (defaults to `1`). The defaults are selected such that no rounding occurs."] # [doc = ""] # [doc = " Rounding a span as part of calculating it is provided as a convenience."] # [doc = " Callers may choose to round the span as a distinct step via"] # [doc = " [`Span::round`], but callers may need to provide a reference date"] # [doc = " for rounding larger units. By coupling rounding with routines like"] # [doc = " [`DateTime::since`], the reference date can be set automatically based on"] # [doc = " the input to `DateTime::since`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " This example shows how to round a span between two datetimes to the nearest"] # [doc = " half-hour, with ties breaking away from zero."] # [doc = ""] # [doc = " ```"] # [doc = " use jiff::{civil::{DateTime, DateTimeDifference}, RoundMode, ToSpan, Unit};"] # [doc = ""] # [doc = " let dt1 = \"2024-03-15 08:14:00.123456789\".parse::<DateTime>()?;"] # [doc = " let dt2 = \"2030-03-22 15:00\".parse::<DateTime>()?;"] # [doc = " let span = dt1.until("] # [doc = "     DateTimeDifference::new(dt2)"] # [doc = "         .smallest(Unit::Minute)"] # [doc = "         .largest(Unit::Year)"] # [doc = "         .mode(RoundMode::HalfExpand)"] # [doc = "         .increment(30),"] # [doc = " )?;"] # [doc = " assert_eq!(span, 6.years().days(7).hours(7).fieldwise());"] # [doc = ""] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [derive (Clone , Copy , Debug)] pub struct DateTimeDifference { datetime : DateTime , round : SpanRound < 'static > , }
};
}
