// Generated macro for DeflateFlush (enum)
macro_rules! DepcrateDeflateFlush {
() => {
// Module: crate
// Provides: {"DeflateFlush"}
// Dependencies: {}
# [derive (Debug , Clone , Copy , PartialEq , Eq , Default)] # [cfg_attr (feature = "__internal-fuzz" , derive (arbitrary :: Arbitrary))] pub enum DeflateFlush { # [default] # [doc = " if flush is set to `NoFlush`, that allows deflate to decide how much data"] # [doc = " to accumulate before producing output, in order to maximize compression."] NoFlush = 0 , # [doc = " If flush is set to `PartialFlush`, all pending output is flushed to the"] # [doc = " output buffer, but the output is not aligned to a byte boundary.  All of the"] # [doc = " input data so far will be available to the decompressor, as for Z_SYNC_FLUSH."] # [doc = " This completes the current deflate block and follows it with an empty fixed"] # [doc = " codes block that is 10 bits long.  This assures that enough bytes are output"] # [doc = " in order for the decompressor to finish the block before the empty fixed"] # [doc = " codes block."] PartialFlush = 1 , # [doc = " If the parameter flush is set to `SyncFlush`, all pending output is"] # [doc = " flushed to the output buffer and the output is aligned on a byte boundary, so"] # [doc = " that the decompressor can get all input data available so far.  (In"] # [doc = " particular avail_in is zero after the call if enough output space has been"] # [doc = " provided before the call.) Flushing may degrade compression for some"] # [doc = " compression algorithms and so it should be used only when necessary.  This"] # [doc = " completes the current deflate block and follows it with an empty stored block"] # [doc = " that is three bits plus filler bits to the next byte, followed by four bytes"] # [doc = " (00 00 ff ff)."] SyncFlush = 2 , # [doc = " If flush is set to `FullFlush`, all output is flushed as with"] # [doc = " Z_SYNC_FLUSH, and the compression state is reset so that decompression can"] # [doc = " restart from this point if previous compressed data has been damaged or if"] # [doc = " random access is desired.  Using `FullFlush` too often can seriously degrade"] # [doc = " compression."] FullFlush = 3 , # [doc = " If the parameter flush is set to `Finish`, pending input is processed,"] # [doc = " pending output is flushed and deflate returns with `StreamEnd` if there was"] # [doc = " enough output space.  If deflate returns with `Ok` or `BufError`, this"] # [doc = " function must be called again with `Finish` and more output space (updated"] # [doc = " avail_out) but no more input data, until it returns with `StreamEnd` or an"] # [doc = " error.  After deflate has returned `StreamEnd`, the only possible operations"] # [doc = " on the stream are deflateReset or deflateEnd."] # [doc = ""] # [doc = " `Finish` can be used in the first deflate call after deflateInit if all the"] # [doc = " compression is to be done in a single step.  In order to complete in one"] # [doc = " call, avail_out must be at least the value returned by deflateBound (see"] # [doc = " below).  Then deflate is guaranteed to return `StreamEnd`.  If not enough"] # [doc = " output space is provided, deflate will not return `StreamEnd`, and it must"] # [doc = " be called again as described above."] Finish = 4 , # [doc = " If flush is set to `Block`, a deflate block is completed and emitted, as"] # [doc = " for `SyncFlush`, but the output is not aligned on a byte boundary, and up to"] # [doc = " seven bits of the current block are held to be written as the next byte after"] # [doc = " the next deflate block is completed.  In this case, the decompressor may not"] # [doc = " be provided enough bits at this point in order to complete decompression of"] # [doc = " the data provided so far to the compressor.  It may need to wait for the next"] # [doc = " block to be emitted.  This is for advanced applications that need to control"] # [doc = " the emission of deflate blocks."] Block = 5 , }
};
}
