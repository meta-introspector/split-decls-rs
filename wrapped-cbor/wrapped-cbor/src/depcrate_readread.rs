// Generated macro for Read (trait)
macro_rules! Depcrate_readRead {
() => {
// Module: crate::read
// Provides: {"Read"}
// Dependencies: {}
# [cfg (feature = "unsealed_read_write")] # [doc = " Trait used by the deserializer for iterating over input."] pub trait Read < 'de > { # [doc = " Read n bytes from the input."] # [doc = ""] # [doc = " Implementations that can are asked to return a slice with a Long lifetime that outlives the"] # [doc = " decoder, but others (eg. ones that need to allocate the data into a temporary buffer) can"] # [doc = " return it with a Short lifetime that just lives for the time of read's mutable borrow of"] # [doc = " the reader."] # [doc = ""] # [doc = " This may, as a side effect, clear the reader's scratch buffer (as the provided"] # [doc = " implementation does)."] fn read < 'a > (& 'a mut self , n : usize) -> Result < EitherLifetime < 'a , 'de > > { self . clear_buffer () ; self . read_to_buffer (n) ? ; Ok (self . take_buffer ()) } # [doc = " Read the next byte from the input, if any."] fn next (& mut self) -> Result < Option < u8 > > ; # [doc = " Peek at the next byte of the input, if any. This does not advance the reader, so the result"] # [doc = " of this function will remain the same until a read or clear occurs."] fn peek (& mut self) -> Result < Option < u8 > > ; # [doc = " Clear the underlying scratch buffer"] fn clear_buffer (& mut self) ; # [doc = " Append n bytes from the reader to the reader's scratch buffer (without clearing it)"] fn read_to_buffer (& mut self , n : usize) -> Result < () > ; # [doc = " Read out everything accumulated in the reader's scratch buffer. This may, as a side effect,"] # [doc = " clear it."] fn take_buffer < 'a > (& 'a mut self) -> EitherLifetime < 'a , 'de > ; # [doc = " Read from the input until `buf` is full or end of input is encountered."] fn read_into (& mut self , buf : & mut [u8]) -> Result < () > ; # [doc = " Discard any data read by `peek`."] fn discard (& mut self) ; # [doc = " Returns the offset from the start of the reader."] fn offset (& self) -> u64 ; }
};
}
