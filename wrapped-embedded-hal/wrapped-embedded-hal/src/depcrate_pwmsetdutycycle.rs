// Generated macro for SetDutyCycle (trait)
macro_rules! Depcrate_pwmSetDutyCycle {
() => {
// Module: crate::pwm
// Provides: {"SetDutyCycle"}
// Dependencies: {}
# [doc = " Single PWM channel / pin."] pub trait SetDutyCycle : ErrorType { # [doc = " Get the maximum duty cycle value."] # [doc = ""] # [doc = " This value corresponds to a 100% duty cycle."] fn max_duty_cycle (& self) -> u16 ; # [doc = " Set the duty cycle to `duty / max_duty`."] # [doc = ""] # [doc = " The caller is responsible for ensuring that the duty cycle value is less than or equal to the maximum duty cycle value,"] # [doc = " as reported by [`max_duty_cycle`]."] # [doc = ""] # [doc = " [`max_duty_cycle`]: SetDutyCycle::max_duty_cycle"] fn set_duty_cycle (& mut self , duty : u16) -> Result < () , Self :: Error > ; # [doc = " Set the duty cycle to 0%, or always inactive."] # [inline] fn set_duty_cycle_fully_off (& mut self) -> Result < () , Self :: Error > { self . set_duty_cycle (0) } # [doc = " Set the duty cycle to 100%, or always active."] # [inline] fn set_duty_cycle_fully_on (& mut self) -> Result < () , Self :: Error > { self . set_duty_cycle (self . max_duty_cycle ()) } # [doc = " Set the duty cycle to `num / denom`."] # [doc = ""] # [doc = " The caller is responsible for ensuring that `num` is less than or equal to `denom`,"] # [doc = " and that `denom` is not zero."] # [inline] fn set_duty_cycle_fraction (& mut self , num : u16 , denom : u16) -> Result < () , Self :: Error > { debug_assert ! (denom != 0) ; debug_assert ! (num <= denom) ; let duty = u32 :: from (num) * u32 :: from (self . max_duty_cycle ()) / u32 :: from (denom) ; # [allow (clippy :: cast_possible_truncation)] { self . set_duty_cycle (duty as u16) } } # [doc = " Set the duty cycle to `percent / 100`"] # [doc = ""] # [doc = " The caller is responsible for ensuring that `percent` is less than or equal to 100."] # [inline] fn set_duty_cycle_percent (& mut self , percent : u8) -> Result < () , Self :: Error > { self . set_duty_cycle_fraction (u16 :: from (percent) , 100) } }
};
}
