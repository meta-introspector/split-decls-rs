use serde::{Deserialize, Serialize};
use std::collections::HashMap;

# [doc = " Moves a list of directories and files to another place recursively. This function will"] # [doc = " also copy the permission bits of the original files to destination files (not for"] # [doc = " directories)."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function will return an error in the following situations, but is not limited to just"] # [doc = " these case:"] # [doc = ""] # [doc = " * List `from` contains  file or directory does not exist."] # [doc = ""] # [doc = " * List `from` contains  file or directory with invalid name."] # [doc = ""] # [doc = " * The current process does not have the permission to access to file from `lists from` or"] # [doc = " `to`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust,ignore"] # [doc = "  extern crate fs_extra;"] # [doc = "  use fs_extra::dir::copy;"] # [doc = ""] # [doc = "  let options = dir::CopyOptions::new(); //Initialize default values for CopyOptions"] # [doc = ""] # [doc = "  // move dir1 and file1.txt to target/dir1 and target/file1.txt"] # [doc = "  let mut from_paths = Vec::new();"] # [doc = "  from_paths.push(\"source/dir1\");"] # [doc = "  from_paths.push(\"source/file.txt\");"] # [doc = "  move_items(&from_paths, \"target\", &options)?;"] # [doc = " ```"] # [doc = ""] pub fn move_items < P , Q > (from_items : & [P] , to : Q , options : & dir :: CopyOptions) -> Result < u64 > where P : AsRef < Path > , Q : AsRef < Path > , { if options . content_only { err ! ("Options 'content_only' not access for move_items function" , ErrorKind :: Other) ; } let mut total_size = 0 ; let mut list_paths = Vec :: new () ; for item in from_items { let item = item . as_ref () ; total_size += dir :: get_size (item) ? ; list_paths . push (item) ; } let mut result = 0 ; let mut info_process = TransitProcess { copied_bytes : 0 , total_bytes : total_size , file_bytes_copied : 0 , file_total_bytes : 0 , file_name : String :: new () , dir_name : String :: new () , state : dir :: TransitState :: Normal , } ; for item in list_paths { if item . is_dir () { if let Some (dir_name) = item . components () . last () { if let Ok (dir_name) = dir_name . as_os_str () . to_os_string () . into_string () { info_process . dir_name = dir_name ; } else { err ! ("Invalid folder from" , ErrorKind :: InvalidFolder) ; } } else { err ! ("Invalid folder from" , ErrorKind :: InvalidFolder) ; } result += dir :: move_dir (item , & to , options) ? ; } else { let file_options = file :: CopyOptions { overwrite : options . overwrite , skip_exist : options . skip_exist , buffer_size : options . buffer_size , } ; if let Some (file_name) = item . file_name () { if let Some (file_name) = file_name . to_str () { info_process . file_name = file_name . to_string () ; } else { err ! ("Invalid file name" , ErrorKind :: InvalidFileName) ; } } else { err ! ("Invalid file name" , ErrorKind :: InvalidFileName) ; } info_process . file_bytes_copied = 0 ; info_process . file_total_bytes = item . metadata () ? . len () ; let file_name = to . as_ref () . join (info_process . file_name . clone ()) ; result += file :: move_file (item , & file_name , & file_options) ? ; } } Ok (result) }