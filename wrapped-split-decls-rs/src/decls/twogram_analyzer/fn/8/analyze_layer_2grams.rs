use serde::{Deserialize, Serialize};
use std::collections::HashMap;

mkdeclfn! {
println!("🔧 Calling function: analyze_layer_2grams");
fn analyze_layer_2grams (translation : & serde_json :: Value , layer : u8) -> Result < LayerAnalysis > { let compressions = translation ["layer_compressions"] . as_array () . unwrap () ; let layer_data = & compressions [layer as usize] ; let input_patterns = layer_data ["input_patterns"] . as_array () . unwrap () ; let output_emojis = layer_data ["output_emojis"] . as_array () . unwrap () ; let input_2grams = extract_2grams_from_patterns (input_patterns) ; let output_2grams = extract_2grams_from_patterns (output_emojis) ; let mut sorted_2grams : Vec < _ > = input_2grams . iter () . collect () ; sorted_2grams . sort_by (| a , b | b . 1 . cmp (a . 1)) ; let top_2grams : Vec < TwoGram > = sorted_2grams . iter () . take (10) . map (| (tokens , count) | { let preserved = check_preservation (tokens , & input_2grams , & output_2grams) ; TwoGram { tokens : (* tokens) . clone () , count : * * count , relationship_type : classify_relationship (tokens) , preserved_in_next_layer : preserved , } }) . collect () ; let total_2grams = input_2grams . len () ; let preserved_count = top_2grams . iter () . filter (| g | g . preserved_in_next_layer) . count () ; let preservation_ratio = if total_2grams > 0 { preserved_count as f64 / top_2grams . len () as f64 } else { 0.0 } ; let compression_mapping = create_compression_mapping (input_patterns , output_emojis) ; Ok (LayerAnalysis { layer , input_size : input_patterns . len () , output_size : output_emojis . len () , top_2grams , preservation_proof : PreservationProof { total_2grams , preserved_count , preservation_ratio , compression_mapping , } , }) }
}