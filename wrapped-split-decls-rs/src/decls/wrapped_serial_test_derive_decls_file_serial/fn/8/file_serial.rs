use serde::{Deserialize, Serialize};
use std::collections::HashMap;

# [doc = " Allows for the creation of file-serialised Rust tests"] # [doc = " ````no_run"] # [doc = " #[test]"] # [doc = " #[file_serial]"] # [doc = " fn test_serial_one() {"] # [doc = "   // Do things"] # [doc = " }"] # [doc = ""] # [doc = " #[test]"] # [doc = " #[file_serial]"] # [doc = " fn test_serial_another() {"] # [doc = "   // Do things"] # [doc = " }"] # [doc = " ````"] # [doc = ""] # [doc = " Multiple tests with the [file_serial](macro@file_serial) attribute are guaranteed to run in serial, as per the [serial](macro@serial)"] # [doc = " attribute. Note that there are no guarantees about one test with [serial](macro@serial) and another with [file_serial](macro@file_serial)"] # [doc = " as they lock using different methods, and [file_serial](macro@file_serial) does not support nested serialised tests, but otherwise acts"] # [doc = " like [serial](macro@serial). If you have other tests that can be run in parallel, but would clash"] # [doc = " if run at the same time as the [file_serial](macro@file_serial) tests, you can use the [file_parallel](macro@file_parallel) attribute."] # [doc = ""] # [doc = " It also supports an optional `path` arg as well as key(s) as per [serial](macro@serial), which is the path to the file used for"] # [doc = " locking purposes. This file is managed by `serial_test` and no assumptions about it's format should be made. The `path` defaults to"] # [doc = " a file under a reasonable temp directory for the OS if not specified. If the `path` is specified, you can only use one key, as we"] # [doc = " can't generate per-key paths if you've done that."] # [doc = " ````no_run"] # [doc = " #[test]"] # [doc = " #[file_serial(key)]"] # [doc = " fn test_serial_one() {"] # [doc = "   // Do things"] # [doc = " }"] # [doc = ""] # [doc = " #[test]"] # [doc = " #[file_serial(key, path => \"/tmp/foo\")]"] # [doc = " fn test_serial_another() {"] # [doc = "   // Do things"] # [doc = " }"] # [doc = " ````"] # [proc_macro_attribute] # [cfg_attr (docsrs , doc (cfg (feature = "file_locks")))] pub fn file_serial (attr : TokenStream , input : TokenStream) -> TokenStream { fs_serial_core (attr . into () , input . into ()) . into () }