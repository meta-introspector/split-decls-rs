use serde::{Deserialize, Serialize};
use std::collections::HashMap;

mkdeclfn! {
# [doc = " The [domain to ASCII](https://url.spec.whatwg.org/#concept-domain-to-ascii) algorithm;"] # [doc = " version returning a `Cow`."] # [doc = ""] # [doc = " Most applications should be using this function or `domain_to_ascii_from_cow` rather"] # [doc = " than the sibling functions, and most applications should pass [`AsciiDenyList::URL`] as"] # [doc = " the second argument. Passing [`AsciiDenyList::URL`] as the second argument makes this function also"] # [doc = " perform the [forbidden domain code point](https://url.spec.whatwg.org/#forbidden-domain-code-point)"] # [doc = " check in addition to the [domain to ASCII](https://url.spec.whatwg.org/#concept-domain-to-ascii)"] # [doc = " algorithm."] # [doc = ""] # [doc = " Returns the ASCII representation a domain name,"] # [doc = " normalizing characters (upper-case to lower-case and other kinds of equivalence)"] # [doc = " and using Punycode as necessary."] # [doc = ""] # [doc = " This process may fail."] # [doc = ""] # [doc = " If you have a `&str` instead of `&[u8]`, just call `.as_bytes()` on it before"] # [doc = " passing it to this function. It's still preferable to use this function over"] # [doc = " the sibling functions that take `&str`."] pub fn domain_to_ascii_cow (domain : & [u8] , ascii_deny_list : AsciiDenyList ,) -> Result < Cow < '_ , str > , Errors > { Uts46 :: new () . to_ascii (domain , ascii_deny_list , uts46 :: Hyphens :: Allow , uts46 :: DnsLength :: Ignore ,) }
}