use serde::{Deserialize, Serialize};
use std::collections::HashMap;

mkdeclstruct! {
# [doc = " A wrapper around google_cpu_profiler."] # [doc = ""] # [doc = " Usage:"] # [doc = " 1. Install gperf_tools (<https://github.com/gperftools/gperftools>), probably packaged with your Linux distro."] # [doc = " 2. Build with `cpu_profiler` feature."] # [doc = " 3. Run the code, the *raw* output would be in the `./out.profile` file."] # [doc = " 4. Install pprof for visualization (<https://github.com/google/pprof>)."] # [doc = " 5. Bump sampling frequency to once per ms: `export CPUPROFILE_FREQUENCY=1000`"] # [doc = " 6. Use something like `pprof -svg target/release/rust-analyzer ./out.profile` to see the results."] # [doc = ""] # [doc = " For example, here's how I run profiling on NixOS:"] # [doc = ""] # [doc = " ```bash"] # [doc = " $ bat -p shell.nix"] # [doc = " with import <nixpkgs> {};"] # [doc = " mkShell {"] # [doc = "   buildInputs = [ gperftools ];"] # [doc = "   shellHook = ''"] # [doc = "     export LD_LIBRARY_PATH=\"${gperftools}/lib:\""] # [doc = "   '';"] # [doc = " }"] # [doc = " $ set -x CPUPROFILE_FREQUENCY 1000"] # [doc = " $ nix-shell --run 'cargo test --release --package rust-analyzer --lib -- benchmarks::benchmark_integrated_highlighting --exact --nocapture'"] # [doc = " $ pprof -svg target/release/deps/rust_analyzer-8739592dc93d63cb crates/rust-analyzer/out.profile > profile.svg"] # [doc = " ```"] # [doc = ""] # [doc = " See this diff for how to profile completions:"] # [doc = ""] # [doc = " <https://github.com/rust-lang/rust-analyzer/pull/5306>"] # [derive (Debug)] pub struct CpuSpan { _private : () , }
}