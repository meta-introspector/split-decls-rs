use serde::{Deserialize, Serialize};
use std::collections::HashMap;

# [doc = " [`str`] methods producing [`SmolStr`]s."] pub trait StrExt : private :: Sealed { # [doc = " Returns the lowercase equivalent of this string slice as a new [`SmolStr`],"] # [doc = " potentially without allocating."] # [doc = ""] # [doc = " See [`str::to_lowercase`]."] # [must_use = "this returns a new SmolStr without modifying the original"] fn to_lowercase_smolstr (& self) -> SmolStr ; # [doc = " Returns the uppercase equivalent of this string slice as a new [`SmolStr`],"] # [doc = " potentially without allocating."] # [doc = ""] # [doc = " See [`str::to_uppercase`]."] # [must_use = "this returns a new SmolStr without modifying the original"] fn to_uppercase_smolstr (& self) -> SmolStr ; # [doc = " Returns the ASCII lowercase equivalent of this string slice as a new [`SmolStr`],"] # [doc = " potentially without allocating."] # [doc = ""] # [doc = " See [`str::to_ascii_lowercase`]."] # [must_use = "this returns a new SmolStr without modifying the original"] fn to_ascii_lowercase_smolstr (& self) -> SmolStr ; # [doc = " Returns the ASCII uppercase equivalent of this string slice as a new [`SmolStr`],"] # [doc = " potentially without allocating."] # [doc = ""] # [doc = " See [`str::to_ascii_uppercase`]."] # [must_use = "this returns a new SmolStr without modifying the original"] fn to_ascii_uppercase_smolstr (& self) -> SmolStr ; # [doc = " Replaces all matches of a &str with another &str returning a new [`SmolStr`],"] # [doc = " potentially without allocating."] # [doc = ""] # [doc = " See [`str::replace`]."] # [must_use = "this returns a new SmolStr without modifying the original"] fn replace_smolstr (& self , from : & str , to : & str) -> SmolStr ; # [doc = " Replaces first N matches of a &str with another &str returning a new [`SmolStr`],"] # [doc = " potentially without allocating."] # [doc = ""] # [doc = " See [`str::replacen`]."] # [must_use = "this returns a new SmolStr without modifying the original"] fn replacen_smolstr (& self , from : & str , to : & str , count : usize) -> SmolStr ; }