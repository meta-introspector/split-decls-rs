use serde::{Deserialize, Serialize};
use std::collections::HashMap;

# [doc = " Installs a panic hook that will print the ICE message on unexpected panics."] # [doc = ""] # [doc = " The hook is intended to be useable even by external tools. You can pass a custom"] # [doc = " `bug_report_url`, or report arbitrary info in `extra_info`. Note that `extra_info` is called in"] # [doc = " a context where *the thread is currently panicking*, so it must not panic or the process will"] # [doc = " abort."] # [doc = ""] # [doc = " If you have no extra info to report, pass the empty closure `|_| ()` as the argument to"] # [doc = " extra_info."] # [doc = ""] # [doc = " A custom rustc driver can skip calling this to set up a custom ICE hook."] pub fn install_ice_hook (bug_report_url : & 'static str , extra_info : fn (& DiagCtxt)) { if env :: var_os ("RUST_BACKTRACE") . is_none () { let ui_testing = std :: env :: args () . any (| arg | arg == "-Zui-testing") ; if env ! ("CFG_RELEASE_CHANNEL") == "dev" && ! ui_testing { panic :: set_backtrace_style (panic :: BacktraceStyle :: Short) ; } else { panic :: set_backtrace_style (panic :: BacktraceStyle :: Full) ; } } panic :: update_hook (Box :: new (move | default_hook : & (dyn Fn (& PanicHookInfo < '_ >) + Send + Sync + 'static) , info : & PanicHookInfo < '_ > | { let _guard = io :: stderr () . lock () ; # [cfg (windows)] if let Some (msg) = info . payload () . downcast_ref :: < String > () { if msg . starts_with ("failed printing to stdout: ") && msg . ends_with ("(os error 232)") { let early_dcx = EarlyDiagCtxt :: new (ErrorOutputType :: default ()) ; let _ = early_dcx . early_err (msg . clone ()) ; return ; } } if ! info . payload () . is :: < rustc_errors :: DelayedBugPanic > () { default_hook (info) ; eprintln ! () ; if let Some (ice_path) = ice_path () && let Ok (mut out) = File :: options () . create (true) . append (true) . open (ice_path) { let location = info . location () . unwrap () ; let msg = match info . payload () . downcast_ref :: < & 'static str > () { Some (s) => * s , None => { match info . payload () . downcast_ref :: < String > () { Some (s) => & s [..] , None => "Box<dyn Any>" , } } } ; let thread = std :: thread :: current () ; let name = thread . name () . unwrap_or ("<unnamed>") ; let _ = write ! (& mut out , "thread '{name}' panicked at {location}:\n\
                        {msg}\n\
                        stack backtrace:\n\
                        {:#}" , std :: backtrace :: Backtrace :: force_capture ()) ; } } report_ice (info , bug_report_url , extra_info , & USING_INTERNAL_FEATURES) ; }) ,) ; }