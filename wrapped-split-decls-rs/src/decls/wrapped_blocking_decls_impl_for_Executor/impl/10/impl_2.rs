use serde::{Deserialize, Serialize};
use std::collections::HashMap;

mkdeclimpl! {
impl Executor { # [cfg (not (target_family = "wasm"))] fn max_threads () -> NonZeroUsize { match env :: var (MAX_THREADS_ENV) { Ok (v) => v . parse :: < usize > () . ok () . and_then (| v | NonZeroUsize :: new (v . clamp (MIN_MAX_THREADS , MAX_MAX_THREADS))) . unwrap_or (DEFAULT_MAX_THREADS) , Err (_) => DEFAULT_MAX_THREADS , } } # [cfg (target_family = "wasm")] fn max_threads () -> NonZeroUsize { NonZeroUsize :: new (1) . unwrap () } # [doc = " Get a reference to the global executor."] # [inline] fn get () -> & 'static Self { # [cfg (not (target_family = "wasm"))] { static EXECUTOR : Executor = Executor { inner : Mutex :: new (Inner { idle_count : 0 , thread_count : 0 , queue : VecDeque :: new () , thread_limit : None , }) , cvar : Condvar :: new () , } ; & EXECUTOR } # [cfg (target_family = "wasm")] panic ! ("cannot spawn a blocking task on WASM") } # [doc = " Spawns a future onto this executor."] # [doc = ""] # [doc = " Returns a [`Task`] handle for the spawned task."] fn spawn < T : Send + 'static > (future : impl Future < Output = T > + Send + 'static) -> Task < T > { let (runnable , task) = async_task :: Builder :: new () . propagate_panic (true) . spawn (move | () | future , | r | { let executor = Self :: get () ; executor . schedule (r) } ,) ; runnable . schedule () ; task } # [doc = " Runs the main loop on the current thread."] # [doc = ""] # [doc = " This function runs blocking tasks until it becomes idle and times out."] fn main_loop (& 'static self) { # [cfg (feature = "tracing")] let _span = tracing :: trace_span ! ("blocking::main_loop") . entered () ; let mut inner = self . inner . lock () . unwrap_or_else (PoisonError :: into_inner) ; loop { inner . idle_count -= 1 ; while let Some (runnable) = inner . queue . pop_front () { self . grow_pool (inner) ; panic :: catch_unwind (| | runnable . run ()) . ok () ; inner = self . inner . lock () . unwrap_or_else (PoisonError :: into_inner) ; } inner . idle_count += 1 ; let timeout = Duration :: from_millis (500) ; # [cfg (feature = "tracing")] tracing :: trace ! (? timeout , "going to sleep") ; let (lock , res) = self . cvar . wait_timeout (inner , timeout) . unwrap () ; inner = lock ; if (Some (inner . thread_count) > inner . thread_limit . map (NonZeroUsize :: get)) && (res . timed_out () && inner . queue . is_empty ()) { inner . idle_count -= 1 ; inner . thread_count -= 1 ; break ; } # [cfg (feature = "tracing")] tracing :: trace ! ("notified") ; } # [cfg (feature = "tracing")] tracing :: trace ! ("shutting down due to lack of tasks") ; } # [doc = " Schedules a runnable task for execution."] fn schedule (& 'static self , runnable : Runnable) { let mut inner = self . inner . lock () . unwrap_or_else (PoisonError :: into_inner) ; inner . queue . push_back (runnable) ; self . cvar . notify_one () ; self . grow_pool (inner) ; } # [doc = " Spawns more blocking threads if the pool is overloaded with work."] fn grow_pool (& 'static self , mut inner : MutexGuard < 'static , Inner >) { # [cfg (feature = "tracing")] let _span = tracing :: trace_span ! ("grow_pool" , queue_len = inner . queue . len () , idle_count = inner . idle_count , thread_count = inner . thread_count ,) . entered () ; let thread_limit = inner . thread_limit . get_or_insert_with (Self :: max_threads) . get () ; while inner . queue . len () > inner . idle_count * 5 && inner . thread_count < thread_limit { # [cfg (feature = "tracing")] tracing :: trace ! ("spawning a new thread to handle blocking tasks") ; inner . idle_count += 1 ; inner . thread_count += 1 ; self . cvar . notify_all () ; static ID : AtomicUsize = AtomicUsize :: new (1) ; let id = ID . fetch_add (1 , Ordering :: Relaxed) ; if let Err (_e) = thread :: Builder :: new () . name (format ! ("blocking-{id}")) . spawn (move | | self . main_loop ()) { # [cfg (feature = "tracing")] tracing :: error ! ("failed to spawn a blocking thread: {}" , _e) ; inner . idle_count -= 1 ; inner . thread_count -= 1 ; inner . thread_limit = { let new_limit = inner . thread_count ; Some (NonZeroUsize :: new (new_limit) . unwrap_or_else (| | { # [cfg (feature = "tracing")] tracing :: warn ! ("attempted to lower thread_limit to zero; setting to one instead") ; NonZeroUsize :: new (1) . unwrap () })) } ; } } } }
}