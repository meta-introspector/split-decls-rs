use serde::{Deserialize, Serialize};
use std::collections::HashMap;

mkdeclimpl! {
impl Encoding { # [doc = " Implements the"] # [doc = " [_get an encoding_](https://encoding.spec.whatwg.org/#concept-encoding-get)"] # [doc = " algorithm."] # [doc = ""] # [doc = " If, after ASCII-lowercasing and removing leading and trailing"] # [doc = " whitespace, the argument matches a label defined in the Encoding"] # [doc = " Standard, `Some(&'static Encoding)` representing the corresponding"] # [doc = " encoding is returned. If there is no match, `None` is returned."] # [doc = ""] # [doc = " This is the right method to use if the action upon the method returning"] # [doc = " `None` is to use a fallback encoding (e.g. `WINDOWS_1252`) instead."] # [doc = " When the action upon the method returning `None` is not to proceed with"] # [doc = " a fallback but to refuse processing, `for_label_no_replacement()` is more"] # [doc = " appropriate."] # [doc = ""] # [doc = " The argument is of type `&[u8]` instead of `&str` to save callers"] # [doc = " that are extracting the label from a non-UTF-8 protocol the trouble"] # [doc = " of conversion to UTF-8. (If you have a `&str`, just call `.as_bytes()`"] # [doc = " on it.)"] # [doc = ""] # [doc = " Available via the C wrapper."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " use encoding_rs::Encoding;"] # [doc = ""] # [doc = " assert_eq!(Some(encoding_rs::UTF_8), Encoding::for_label(b\"utf-8\"));"] # [doc = " assert_eq!(Some(encoding_rs::UTF_8), Encoding::for_label(b\"unicode11utf8\"));"] # [doc = ""] # [doc = " assert_eq!(Some(encoding_rs::ISO_8859_2), Encoding::for_label(b\"latin2\"));"] # [doc = ""] # [doc = " assert_eq!(Some(encoding_rs::UTF_16BE), Encoding::for_label(b\"utf-16be\"));"] # [doc = ""] # [doc = " assert_eq!(None, Encoding::for_label(b\"unrecognized label\"));"] # [doc = " ```"] pub fn for_label (label : & [u8]) -> Option < & 'static Encoding > { let mut trimmed = [0u8 ; LONGEST_LABEL_LENGTH] ; let mut trimmed_pos = 0usize ; let mut iter = label . into_iter () ; loop { match iter . next () { None => { return None ; } Some (byte) => match * byte { 0x09u8 | 0x0Au8 | 0x0Cu8 | 0x0Du8 | 0x20u8 => { continue ; } b'A' ..= b'Z' => { trimmed [trimmed_pos] = * byte + 0x20u8 ; trimmed_pos = 1usize ; break ; } b'a' ..= b'z' | b'0' ..= b'9' | b'-' | b'_' | b':' | b'.' => { trimmed [trimmed_pos] = * byte ; trimmed_pos = 1usize ; break ; } _ => { return None ; } } , } } loop { match iter . next () { None => { break ; } Some (byte) => match * byte { 0x09u8 | 0x0Au8 | 0x0Cu8 | 0x0Du8 | 0x20u8 => { break ; } b'A' ..= b'Z' => { if trimmed_pos == LONGEST_LABEL_LENGTH { return None ; } trimmed [trimmed_pos] = * byte + 0x20u8 ; trimmed_pos += 1usize ; continue ; } b'a' ..= b'z' | b'0' ..= b'9' | b'-' | b'_' | b':' | b'.' => { if trimmed_pos == LONGEST_LABEL_LENGTH { return None ; } trimmed [trimmed_pos] = * byte ; trimmed_pos += 1usize ; continue ; } _ => { return None ; } } , } } loop { match iter . next () { None => { break ; } Some (byte) => match * byte { 0x09u8 | 0x0Au8 | 0x0Cu8 | 0x0Du8 | 0x20u8 => { continue ; } _ => { return None ; } } , } } let candidate = & trimmed [.. trimmed_pos] ; match LABELS_SORTED . binary_search_by (| probe | { let bytes = probe . as_bytes () ; let c = bytes . len () . cmp (& candidate . len ()) ; if c != Ordering :: Equal { return c ; } let probe_iter = bytes . iter () . rev () ; let candidate_iter = candidate . iter () . rev () ; probe_iter . cmp (candidate_iter) }) { Ok (i) => Some (ENCODINGS_IN_LABEL_SORT [i]) , Err (_) => None , } } # [doc = " This method behaves the same as `for_label()`, except when `for_label()`"] # [doc = " would return `Some(REPLACEMENT)`, this method returns `None` instead."] # [doc = ""] # [doc = " This method is useful in scenarios where a fatal error is required"] # [doc = " upon invalid label, because in those cases the caller typically wishes"] # [doc = " to treat the labels that map to the replacement encoding as fatal"] # [doc = " errors, too."] # [doc = ""] # [doc = " It is not OK to use this method when the action upon the method returning"] # [doc = " `None` is to use a fallback encoding (e.g. `WINDOWS_1252`). In such a"] # [doc = " case, the `for_label()` method should be used instead in order to avoid"] # [doc = " unsafe fallback for labels that `for_label()` maps to `Some(REPLACEMENT)`."] # [doc = ""] # [doc = " Available via the C wrapper."] # [inline] pub fn for_label_no_replacement (label : & [u8]) -> Option < & 'static Encoding > { match Encoding :: for_label (label) { None => None , Some (encoding) => { if encoding == REPLACEMENT { None } else { Some (encoding) } } } } # [doc = " Performs non-incremental BOM sniffing."] # [doc = ""] # [doc = " The argument must either be a buffer representing the entire input"] # [doc = " stream (non-streaming case) or a buffer representing at least the first"] # [doc = " three bytes of the input stream (streaming case)."] # [doc = ""] # [doc = " Returns `Some((UTF_8, 3))`, `Some((UTF_16LE, 2))` or"] # [doc = " `Some((UTF_16BE, 2))` if the argument starts with the UTF-8, UTF-16LE"] # [doc = " or UTF-16BE BOM or `None` otherwise."] # [doc = ""] # [doc = " Available via the C wrapper."] # [inline] pub fn for_bom (buffer : & [u8]) -> Option < (& 'static Encoding , usize) > { if buffer . starts_with (b"\xEF\xBB\xBF") { Some ((UTF_8 , 3)) } else if buffer . starts_with (b"\xFF\xFE") { Some ((UTF_16LE , 2)) } else if buffer . starts_with (b"\xFE\xFF") { Some ((UTF_16BE , 2)) } else { None } } # [doc = " Returns the name of this encoding."] # [doc = ""] # [doc = " This name is appropriate to return as-is from the DOM"] # [doc = " `document.characterSet` property."] # [doc = ""] # [doc = " Available via the C wrapper."] # [inline] pub fn name (& 'static self) -> & 'static str { self . name } # [doc = " Checks whether the _output encoding_ of this encoding can encode every"] # [doc = " `char`. (Only true if the output encoding is UTF-8.)"] # [doc = ""] # [doc = " Available via the C wrapper."] # [inline] pub fn can_encode_everything (& 'static self) -> bool { self . output_encoding () == UTF_8 } # [doc = " Checks whether the bytes 0x00...0x7F map exclusively to the characters"] # [doc = " U+0000...U+007F and vice versa."] # [doc = ""] # [doc = " Available via the C wrapper."] # [inline] pub fn is_ascii_compatible (& 'static self) -> bool { ! (self == REPLACEMENT || self == UTF_16BE || self == UTF_16LE || self == ISO_2022_JP) } # [doc = " Checks whether this encoding maps one byte to one Basic Multilingual"] # [doc = " Plane code point (i.e. byte length equals decoded UTF-16 length) and"] # [doc = " vice versa (for mappable characters)."] # [doc = ""] # [doc = " `true` iff this encoding is on the list of [Legacy single-byte"] # [doc = " encodings](https://encoding.spec.whatwg.org/#legacy-single-byte-encodings)"] # [doc = " in the spec or x-user-defined."] # [doc = ""] # [doc = " Available via the C wrapper."] # [inline] pub fn is_single_byte (& 'static self) -> bool { self . variant . is_single_byte () } # [doc = " Checks whether the bytes 0x00...0x7F map mostly to the characters"] # [doc = " U+0000...U+007F and vice versa."] # [cfg (feature = "alloc")] # [inline] fn is_potentially_borrowable (& 'static self) -> bool { ! (self == REPLACEMENT || self == UTF_16BE || self == UTF_16LE) } # [doc = " Returns the _output encoding_ of this encoding. This is UTF-8 for"] # [doc = " UTF-16BE, UTF-16LE, and replacement and the encoding itself otherwise."] # [doc = ""] # [doc = " _Note:_ The _output encoding_ concept is needed for form submission and"] # [doc = " error handling in the query strings of URLs in the Web Platform."] # [doc = ""] # [doc = " Available via the C wrapper."] # [inline] pub fn output_encoding (& 'static self) -> & 'static Encoding { if self == REPLACEMENT || self == UTF_16BE || self == UTF_16LE { UTF_8 } else { self } } # [doc = " Decode complete input to `Cow<'a, str>` _with BOM sniffing_ and with"] # [doc = " malformed sequences replaced with the REPLACEMENT CHARACTER when the"] # [doc = " entire input is available as a single buffer (i.e. the end of the"] # [doc = " buffer marks the end of the stream)."] # [doc = ""] # [doc = " The BOM, if any, does not appear in the output."] # [doc = ""] # [doc = " This method implements the (non-streaming version of) the"] # [doc = " [_decode_](https://encoding.spec.whatwg.org/#decode) spec concept."] # [doc = ""] # [doc = " The second item in the returned tuple is the encoding that was actually"] # [doc = " used (which may differ from this encoding thanks to BOM sniffing)."] # [doc = ""] # [doc = " The third item in the returned tuple indicates whether there were"] # [doc = " malformed sequences (that were replaced with the REPLACEMENT CHARACTER)."] # [doc = ""] # [doc = " _Note:_ It is wrong to use this when the input buffer represents only"] # [doc = " a segment of the input instead of the whole input. Use `new_decoder()`"] # [doc = " when decoding segmented input."] # [doc = ""] # [doc = " This method performs a one or two heap allocations for the backing"] # [doc = " buffer of the `String` when unable to borrow. (One allocation if not"] # [doc = " errors and potentially another one in the presence of errors.) The"] # [doc = " first allocation assumes jemalloc and may not be optimal with"] # [doc = " allocators that do not use power-of-two buckets. A borrow is performed"] # [doc = " if decoding UTF-8 and the input is valid UTF-8, if decoding an"] # [doc = " ASCII-compatible encoding and the input is ASCII-only, or when decoding"] # [doc = " ISO-2022-JP and the input is entirely in the ASCII state without state"] # [doc = " transitions."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the size calculation for a heap-allocated backing buffer overflows"] # [doc = " `usize`."] # [doc = ""] # [doc = " Available to Rust only and only with the `alloc` feature enabled (enabled"] # [doc = " by default)."] # [cfg (feature = "alloc")] # [inline] pub fn decode < 'a > (& 'static self , bytes : & 'a [u8]) -> (Cow < 'a , str > , & 'static Encoding , bool) { let (encoding , without_bom) = match Encoding :: for_bom (bytes) { Some ((encoding , bom_length)) => (encoding , & bytes [bom_length ..]) , None => (self , bytes) , } ; let (cow , had_errors) = encoding . decode_without_bom_handling (without_bom) ; (cow , encoding , had_errors) } # [doc = " Decode complete input to `Cow<'a, str>` _with BOM removal_ and with"] # [doc = " malformed sequences replaced with the REPLACEMENT CHARACTER when the"] # [doc = " entire input is available as a single buffer (i.e. the end of the"] # [doc = " buffer marks the end of the stream)."] # [doc = ""] # [doc = " Only an initial byte sequence that is a BOM for this encoding is removed."] # [doc = ""] # [doc = " When invoked on `UTF_8`, this method implements the (non-streaming"] # [doc = " version of) the"] # [doc = " [_UTF-8 decode_](https://encoding.spec.whatwg.org/#utf-8-decode) spec"] # [doc = " concept."] # [doc = ""] # [doc = " The second item in the returned pair indicates whether there were"] # [doc = " malformed sequences (that were replaced with the REPLACEMENT CHARACTER)."] # [doc = ""] # [doc = " _Note:_ It is wrong to use this when the input buffer represents only"] # [doc = " a segment of the input instead of the whole input. Use"] # [doc = " `new_decoder_with_bom_removal()` when decoding segmented input."] # [doc = ""] # [doc = " This method performs a one or two heap allocations for the backing"] # [doc = " buffer of the `String` when unable to borrow. (One allocation if not"] # [doc = " errors and potentially another one in the presence of errors.) The"] # [doc = " first allocation assumes jemalloc and may not be optimal with"] # [doc = " allocators that do not use power-of-two buckets. A borrow is performed"] # [doc = " if decoding UTF-8 and the input is valid UTF-8, if decoding an"] # [doc = " ASCII-compatible encoding and the input is ASCII-only, or when decoding"] # [doc = " ISO-2022-JP and the input is entirely in the ASCII state without state"] # [doc = " transitions."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the size calculation for a heap-allocated backing buffer overflows"] # [doc = " `usize`."] # [doc = ""] # [doc = " Available to Rust only and only with the `alloc` feature enabled (enabled"] # [doc = " by default)."] # [cfg (feature = "alloc")] # [inline] pub fn decode_with_bom_removal < 'a > (& 'static self , bytes : & 'a [u8]) -> (Cow < 'a , str > , bool) { let without_bom = if self == UTF_8 && bytes . starts_with (b"\xEF\xBB\xBF") { & bytes [3 ..] } else if (self == UTF_16LE && bytes . starts_with (b"\xFF\xFE")) || (self == UTF_16BE && bytes . starts_with (b"\xFE\xFF")) { & bytes [2 ..] } else { bytes } ; self . decode_without_bom_handling (without_bom) } # [doc = " Decode complete input to `Cow<'a, str>` _without BOM handling_ and"] # [doc = " with malformed sequences replaced with the REPLACEMENT CHARACTER when"] # [doc = " the entire input is available as a single buffer (i.e. the end of the"] # [doc = " buffer marks the end of the stream)."] # [doc = ""] # [doc = " When invoked on `UTF_8`, this method implements the (non-streaming"] # [doc = " version of) the"] # [doc = " [_UTF-8 decode without BOM_](https://encoding.spec.whatwg.org/#utf-8-decode-without-bom)"] # [doc = " spec concept."] # [doc = ""] # [doc = " The second item in the returned pair indicates whether there were"] # [doc = " malformed sequences (that were replaced with the REPLACEMENT CHARACTER)."] # [doc = ""] # [doc = " _Note:_ It is wrong to use this when the input buffer represents only"] # [doc = " a segment of the input instead of the whole input. Use"] # [doc = " `new_decoder_without_bom_handling()` when decoding segmented input."] # [doc = ""] # [doc = " This method performs a one or two heap allocations for the backing"] # [doc = " buffer of the `String` when unable to borrow. (One allocation if not"] # [doc = " errors and potentially another one in the presence of errors.) The"] # [doc = " first allocation assumes jemalloc and may not be optimal with"] # [doc = " allocators that do not use power-of-two buckets. A borrow is performed"] # [doc = " if decoding UTF-8 and the input is valid UTF-8, if decoding an"] # [doc = " ASCII-compatible encoding and the input is ASCII-only, or when decoding"] # [doc = " ISO-2022-JP and the input is entirely in the ASCII state without state"] # [doc = " transitions."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the size calculation for a heap-allocated backing buffer overflows"] # [doc = " `usize`."] # [doc = ""] # [doc = " Available to Rust only and only with the `alloc` feature enabled (enabled"] # [doc = " by default)."] # [cfg (feature = "alloc")] pub fn decode_without_bom_handling < 'a > (& 'static self , bytes : & 'a [u8]) -> (Cow < 'a , str > , bool) { let (mut decoder , mut string , mut total_read) = if self . is_potentially_borrowable () { let valid_up_to = if self == UTF_8 { utf8_valid_up_to (bytes) } else if self == ISO_2022_JP { iso_2022_jp_ascii_valid_up_to (bytes) } else { ascii_valid_up_to (bytes) } ; if valid_up_to == bytes . len () { let str : & str = unsafe { core :: str :: from_utf8_unchecked (bytes) } ; return (Cow :: Borrowed (str) , false) ; } let decoder = self . new_decoder_without_bom_handling () ; let rounded_without_replacement = checked_next_power_of_two (checked_add (valid_up_to , decoder . max_utf8_buffer_length_without_replacement (bytes . len () - valid_up_to) ,)) ; let with_replacement = checked_add (valid_up_to , decoder . max_utf8_buffer_length (bytes . len () - valid_up_to) ,) ; let mut string = String :: with_capacity (checked_min (rounded_without_replacement , with_replacement) . unwrap () ,) ; unsafe { let vec = string . as_mut_vec () ; vec . set_len (valid_up_to) ; core :: ptr :: copy_nonoverlapping (bytes . as_ptr () , vec . as_mut_ptr () , valid_up_to) ; } (decoder , string , valid_up_to) } else { let decoder = self . new_decoder_without_bom_handling () ; let rounded_without_replacement = checked_next_power_of_two (decoder . max_utf8_buffer_length_without_replacement (bytes . len ()) ,) ; let with_replacement = decoder . max_utf8_buffer_length (bytes . len ()) ; let string = String :: with_capacity (checked_min (rounded_without_replacement , with_replacement) . unwrap () ,) ; (decoder , string , 0) } ; let mut total_had_errors = false ; loop { let (result , read , had_errors) = decoder . decode_to_string (& bytes [total_read ..] , & mut string , true) ; total_read += read ; total_had_errors |= had_errors ; match result { CoderResult :: InputEmpty => { debug_assert_eq ! (total_read , bytes . len ()) ; return (Cow :: Owned (string) , total_had_errors) ; } CoderResult :: OutputFull => { let needed = decoder . max_utf8_buffer_length (bytes . len () - total_read) ; string . reserve (needed . unwrap ()) ; } } } } # [doc = " Decode complete input to `Cow<'a, str>` _without BOM handling_ and"] # [doc = " _with malformed sequences treated as fatal_ when the entire input is"] # [doc = " available as a single buffer (i.e. the end of the buffer marks the end"] # [doc = " of the stream)."] # [doc = ""] # [doc = " When invoked on `UTF_8`, this method implements the (non-streaming"] # [doc = " version of) the"] # [doc = " [_UTF-8 decode without BOM or fail_](https://encoding.spec.whatwg.org/#utf-8-decode-without-bom-or-fail)"] # [doc = " spec concept."] # [doc = ""] # [doc = " Returns `None` if a malformed sequence was encountered and the result"] # [doc = " of the decode as `Some(String)` otherwise."] # [doc = ""] # [doc = " _Note:_ It is wrong to use this when the input buffer represents only"] # [doc = " a segment of the input instead of the whole input. Use"] # [doc = " `new_decoder_without_bom_handling()` when decoding segmented input."] # [doc = ""] # [doc = " This method performs a single heap allocation for the backing"] # [doc = " buffer of the `String` when unable to borrow. A borrow is performed if"] # [doc = " decoding UTF-8 and the input is valid UTF-8, if decoding an"] # [doc = " ASCII-compatible encoding and the input is ASCII-only, or when decoding"] # [doc = " ISO-2022-JP and the input is entirely in the ASCII state without state"] # [doc = " transitions."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the size calculation for a heap-allocated backing buffer overflows"] # [doc = " `usize`."] # [doc = ""] # [doc = " Available to Rust only and only with the `alloc` feature enabled (enabled"] # [doc = " by default)."] # [cfg (feature = "alloc")] pub fn decode_without_bom_handling_and_without_replacement < 'a > (& 'static self , bytes : & 'a [u8] ,) -> Option < Cow < 'a , str > > { if self == UTF_8 { let valid_up_to = utf8_valid_up_to (bytes) ; if valid_up_to == bytes . len () { let str : & str = unsafe { core :: str :: from_utf8_unchecked (bytes) } ; return Some (Cow :: Borrowed (str)) ; } return None ; } let (mut decoder , mut string , input) = if self . is_potentially_borrowable () { let valid_up_to = if self == ISO_2022_JP { iso_2022_jp_ascii_valid_up_to (bytes) } else { ascii_valid_up_to (bytes) } ; if valid_up_to == bytes . len () { let str : & str = unsafe { core :: str :: from_utf8_unchecked (bytes) } ; return Some (Cow :: Borrowed (str)) ; } let decoder = self . new_decoder_without_bom_handling () ; let mut string = String :: with_capacity (checked_add (valid_up_to , decoder . max_utf8_buffer_length_without_replacement (bytes . len () - valid_up_to) ,) . unwrap () ,) ; unsafe { let vec = string . as_mut_vec () ; vec . set_len (valid_up_to) ; core :: ptr :: copy_nonoverlapping (bytes . as_ptr () , vec . as_mut_ptr () , valid_up_to) ; } (decoder , string , & bytes [valid_up_to ..]) } else { let decoder = self . new_decoder_without_bom_handling () ; let string = String :: with_capacity (decoder . max_utf8_buffer_length_without_replacement (bytes . len ()) . unwrap () ,) ; (decoder , string , bytes) } ; let (result , read) = decoder . decode_to_string_without_replacement (input , & mut string , true) ; match result { DecoderResult :: InputEmpty => { debug_assert_eq ! (read , input . len ()) ; Some (Cow :: Owned (string)) } DecoderResult :: Malformed (_ , _) => None , DecoderResult :: OutputFull => unreachable ! () , } } # [doc = " Encode complete input to `Cow<'a, [u8]>` using the"] # [doc = " [_output encoding_](Encoding::output_encoding) of this encoding with"] # [doc = " unmappable characters replaced with decimal numeric character references"] # [doc = " when the entire input is available as a single buffer (i.e. the end of"] # [doc = " the buffer marks the end of the stream)."] # [doc = ""] # [doc = " This method implements the (non-streaming version of) the"] # [doc = " [_encode_](https://encoding.spec.whatwg.org/#encode) spec concept. For"] # [doc = " the [_UTF-8 encode_](https://encoding.spec.whatwg.org/#utf-8-encode)"] # [doc = " spec concept, it is slightly more efficient to use"] # [doc = " <code><var>string</var>.as_bytes()</code> instead of invoking this"] # [doc = " method on `UTF_8`."] # [doc = ""] # [doc = " The second item in the returned tuple is the encoding that was actually"] # [doc = " used (*which may differ from this encoding thanks to some encodings"] # [doc = " having UTF-8 as their output encoding*)."] # [doc = ""] # [doc = " The third item in the returned tuple indicates whether there were"] # [doc = " unmappable characters (that were replaced with HTML numeric character"] # [doc = " references)."] # [doc = ""] # [doc = " _Note:_ It is wrong to use this when the input buffer represents only"] # [doc = " a segment of the input instead of the whole input. Use `new_encoder()`"] # [doc = " when encoding segmented output."] # [doc = ""] # [doc = " When encoding to UTF-8 or when encoding an ASCII-only input to a"] # [doc = " ASCII-compatible encoding, this method returns a borrow of the input"] # [doc = " without a heap allocation. Otherwise, this method performs a single"] # [doc = " heap allocation for the backing buffer of the `Vec<u8>` if there are no"] # [doc = " unmappable characters and potentially multiple heap allocations if"] # [doc = " there are. These allocations are tuned for jemalloc and may not be"] # [doc = " optimal when using a different allocator that doesn't use power-of-two"] # [doc = " buckets."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the size calculation for a heap-allocated backing buffer overflows"] # [doc = " `usize`."] # [doc = ""] # [doc = " Available to Rust only and only with the `alloc` feature enabled (enabled"] # [doc = " by default)."] # [cfg (feature = "alloc")] pub fn encode < 'a > (& 'static self , string : & 'a str) -> (Cow < 'a , [u8] > , & 'static Encoding , bool) { let output_encoding = self . output_encoding () ; if output_encoding == UTF_8 { return (Cow :: Borrowed (string . as_bytes ()) , output_encoding , false) ; } debug_assert ! (output_encoding . is_potentially_borrowable ()) ; let bytes = string . as_bytes () ; let valid_up_to = if output_encoding == ISO_2022_JP { iso_2022_jp_ascii_valid_up_to (bytes) } else { ascii_valid_up_to (bytes) } ; if valid_up_to == bytes . len () { return (Cow :: Borrowed (bytes) , output_encoding , false) ; } let mut encoder = output_encoding . new_encoder () ; let mut vec : Vec < u8 > = Vec :: with_capacity ((checked_add (valid_up_to , encoder . max_buffer_length_from_utf8_if_no_unmappables (string . len () - valid_up_to) ,)) . unwrap () . next_power_of_two () ,) ; unsafe { vec . set_len (valid_up_to) ; core :: ptr :: copy_nonoverlapping (bytes . as_ptr () , vec . as_mut_ptr () , valid_up_to) ; } let mut total_read = valid_up_to ; let mut total_had_errors = false ; loop { let (result , read , had_errors) = encoder . encode_from_utf8_to_vec (& string [total_read ..] , & mut vec , true) ; total_read += read ; total_had_errors |= had_errors ; match result { CoderResult :: InputEmpty => { debug_assert_eq ! (total_read , string . len ()) ; return (Cow :: Owned (vec) , output_encoding , total_had_errors) ; } CoderResult :: OutputFull => { let needed = encoder . max_buffer_length_from_utf8_if_no_unmappables (string . len () - total_read) ; let rounded = (checked_add (vec . capacity () , needed)) . unwrap () . next_power_of_two () ; let additional = rounded - vec . len () ; vec . reserve_exact (additional) ; } } } } fn new_variant_decoder (& 'static self) -> VariantDecoder { self . variant . new_variant_decoder () } # [doc = " Instantiates a new decoder for this encoding with BOM sniffing enabled."] # [doc = ""] # [doc = " BOM sniffing may cause the returned decoder to morph into a decoder"] # [doc = " for UTF-8, UTF-16LE or UTF-16BE instead of this encoding. The BOM"] # [doc = " does not appear in the output."] # [doc = ""] # [doc = " Available via the C wrapper."] # [inline] pub fn new_decoder (& 'static self) -> Decoder { Decoder :: new (self , self . new_variant_decoder () , BomHandling :: Sniff) } # [doc = " Instantiates a new decoder for this encoding with BOM removal."] # [doc = ""] # [doc = " If the input starts with bytes that are the BOM for this encoding,"] # [doc = " those bytes are removed. However, the decoder never morphs into a"] # [doc = " decoder for another encoding: A BOM for another encoding is treated as"] # [doc = " (potentially malformed) input to the decoding algorithm for this"] # [doc = " encoding."] # [doc = ""] # [doc = " Available via the C wrapper."] # [inline] pub fn new_decoder_with_bom_removal (& 'static self) -> Decoder { Decoder :: new (self , self . new_variant_decoder () , BomHandling :: Remove) } # [doc = " Instantiates a new decoder for this encoding with BOM handling disabled."] # [doc = ""] # [doc = " If the input starts with bytes that look like a BOM, those bytes are"] # [doc = " not treated as a BOM. (Hence, the decoder never morphs into a decoder"] # [doc = " for another encoding.)"] # [doc = ""] # [doc = " _Note:_ If the caller has performed BOM sniffing on its own but has not"] # [doc = " removed the BOM, the caller should use `new_decoder_with_bom_removal()`"] # [doc = " instead of this method to cause the BOM to be removed."] # [doc = ""] # [doc = " Available via the C wrapper."] # [inline] pub fn new_decoder_without_bom_handling (& 'static self) -> Decoder { Decoder :: new (self , self . new_variant_decoder () , BomHandling :: Off) } # [doc = " Instantiates a new encoder for the [_output encoding_](Encoding::output_encoding)"] # [doc = " of this encoding."] # [doc = ""] # [doc = " _Note:_ The output encoding of UTF-16BE, UTF-16LE, and replacement is UTF-8. There"] # [doc = " is no encoder for UTF-16BE, UTF-16LE, and replacement themselves."] # [doc = ""] # [doc = " Available via the C wrapper."] # [inline] pub fn new_encoder (& 'static self) -> Encoder { let enc = self . output_encoding () ; enc . variant . new_encoder (enc) } # [doc = " Validates UTF-8."] # [doc = ""] # [doc = " Returns the index of the first byte that makes the input malformed as"] # [doc = " UTF-8 or the length of the slice if the slice is entirely valid."] # [doc = ""] # [doc = " This is currently faster than the corresponding standard library"] # [doc = " functionality. If this implementation gets upstreamed to the standard"] # [doc = " library, this method may be removed in the future."] # [doc = ""] # [doc = " Available via the C wrapper."] pub fn utf8_valid_up_to (bytes : & [u8]) -> usize { utf8_valid_up_to (bytes) } # [doc = " Validates ASCII."] # [doc = ""] # [doc = " Returns the index of the first byte that makes the input malformed as"] # [doc = " ASCII or the length of the slice if the slice is entirely valid."] # [doc = ""] # [doc = " Available via the C wrapper."] pub fn ascii_valid_up_to (bytes : & [u8]) -> usize { ascii_valid_up_to (bytes) } # [doc = " Validates ISO-2022-JP ASCII-state data."] # [doc = ""] # [doc = " Returns the index of the first byte that makes the input not"] # [doc = " representable in the ASCII state of ISO-2022-JP or the length of the"] # [doc = " slice if the slice is entirely representable in the ASCII state of"] # [doc = " ISO-2022-JP."] # [doc = ""] # [doc = " Available via the C wrapper."] pub fn iso_2022_jp_ascii_valid_up_to (bytes : & [u8]) -> usize { iso_2022_jp_ascii_valid_up_to (bytes) } }
}