use serde::{Deserialize, Serialize};
use std::collections::HashMap;

mkdeclfn! {
# [doc = " Set the maximum number of threads used by the backing thread pool."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use blocking::unblock;"] # [doc = " use std::fs::{read_dir, File};"] # [doc = " use std::io::prelude::*;"] # [doc = " # use std::num::NonZeroUsize;"] # [doc = ""] # [doc = " blocking::set_max_blocking_threads(NonZeroUsize::new(100).unwrap());"] # [doc = ""] # [doc = " # fn test() -> std::io::Result<()> {"] # [doc = " let mut files = Vec::new();"] # [doc = " for entry in read_dir(\"/path/to/large/directory\").unwrap() {"] # [doc = "     files.push(unblock(move || -> std::io::Result<String> {"] # [doc = "         let mut contents = String::new();"] # [doc = "         let mut file = File::open(entry?.path())?;"] # [doc = "         file.read_to_string(&mut contents)?;"] # [doc = "         Ok(contents)"] # [doc = "     }));"] # [doc = " }"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] pub fn set_max_blocking_threads (threads : NonZeroUsize) { let executor = Executor :: get () ; let mut inner = executor . inner . lock () . unwrap_or_else (PoisonError :: into_inner) ; let old_limit = inner . thread_limit ; inner . thread_limit = Some (threads) ; if let Some (old_limit) = old_limit { if old_limit > threads { executor . cvar . notify_all () ; } } }
}