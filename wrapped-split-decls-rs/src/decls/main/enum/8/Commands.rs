use serde::{Deserialize, Serialize};
use std::collections::HashMap;

# [derive (Subcommand , Debug)] enum Commands { # [doc = " Generates a wrapped workspace for given crates"] # [command (name = "wrapped-workspace")] WrappedWorkspace { # [doc = " Optional: Directory to output the wrapped workspace"] # [arg (short , long , value_name = "DIR")] output_dir : Option < PathBuf > , # [doc = " Run in dry-run mode, no files will be modified"] # [arg (short , long)] dry_run : bool , } , # [doc = " Scans an ecosystem for crates and processes them"] # [command (name = "ecosystem-scan")] EcosystemScan { # [doc = " Base path to scan for crates"] # [arg (value_name = "PATH")] base_path : PathBuf , # [doc = " Run in dry-run mode, no files will be modified"] # [arg (short , long)] recursive : bool , # [doc = " Run in dry-run mode, no files will be modified"] # [arg (short , long)] dry_run : bool , } , # [doc = " Executes a workflow defined in a goal.toml file"] # [command (name = "execute-goal-workflow")] ExecuteGoalWorkflow { # [doc = " Path to the goal.toml file"] # [arg (value_name = "FILE")] goal_file : PathBuf , # [doc = " Run in dry-run mode, no files will be modified"] # [arg (short , long)] dry_run : bool , } , # [doc = " Bootstrap command: Scans the project itself into output2, builds the generated code, and reports errors."] # [command (name = "bootstrap")] Bootstrap { # [doc = " Optional: Directory to output the wrapped workspace"] # [arg (short , long , value_name = "DIR")] output_dir : Option < PathBuf > , # [doc = " Run in dry-run mode, no files will be modified"] # [arg (short , long)] dry_run : bool , # [doc = " Only update Cargo.toml files, skip Rust parsing"] # [arg (long)] cargo_only : bool , # [doc = " Plan mode: Show what will be processed without making changes"] # [arg (long)] plan : bool , } , }