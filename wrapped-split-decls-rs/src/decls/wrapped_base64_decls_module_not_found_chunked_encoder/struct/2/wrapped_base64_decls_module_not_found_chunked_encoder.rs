use serde::{Deserialize, Serialize};
use std::collections::HashMap;

mkdeclstruct! {
# [llm_error_message (message = "Module file not found for: chunked_encoder")] # [llm_context (crate_name = "wrapped_base64" , module_name = "chunked_encoder")] pub struct wrapped_base64_decls_module_not_found_chunked_encoder ;
}