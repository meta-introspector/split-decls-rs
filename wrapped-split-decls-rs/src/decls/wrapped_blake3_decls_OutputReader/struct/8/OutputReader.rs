use serde::{Deserialize, Serialize};
use std::collections::HashMap;

mkdeclstruct! {
# [doc = " An incremental reader for extended output, returned by"] # [doc = " [`Hasher::finalize_xof`](struct.Hasher.html#method.finalize_xof)."] # [doc = ""] # [doc = " Shorter BLAKE3 outputs are prefixes of longer ones, and explicitly requesting a short output is"] # [doc = " equivalent to truncating the default-length output. Note that this is a difference between"] # [doc = " BLAKE2 and BLAKE3."] # [doc = ""] # [doc = " # Security notes"] # [doc = ""] # [doc = " Outputs shorter than the default length of 32 bytes (256 bits) provide less security. An N-bit"] # [doc = " BLAKE3 output is intended to provide N bits of first and second preimage resistance and N/2"] # [doc = " bits of collision resistance, for any N up to 256. Longer outputs don't provide any additional"] # [doc = " security."] # [doc = ""] # [doc = " Avoid relying on the secrecy of the output offset, that is, the number of output bytes read or"] # [doc = " the arguments to [`seek`](struct.OutputReader.html#method.seek) or"] # [doc = " [`set_position`](struct.OutputReader.html#method.set_position). [_Block-Cipher-Based Tree"] # [doc = " Hashing_ by Aldo Gunsing](https://eprint.iacr.org/2022/283) shows that an attacker who knows"] # [doc = " both the message and the key (if any) can easily determine the offset of an extended output."] # [doc = " For comparison, AES-CTR has a similar property: if you know the key, you can decrypt a block"] # [doc = " from an unknown position in the output stream to recover its block index. Callers with strong"] # [doc = " secret keys aren't affected in practice, but secret offsets are a [design"] # [doc = " smell](https://en.wikipedia.org/wiki/Design_smell) in any case."] # [derive (Clone)] pub struct OutputReader { inner : Output , position_within_block : u8 , }
}