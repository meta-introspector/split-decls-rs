// Generated macro for impl_180 (impl)
macro_rules! Depcrate_output2_macro_systemimpl_180 {
() => {
// Module: crate::output2_macro_system
// Provides: {"impl_180"}
// Dependencies: {}
impl Output2MacroSystem { # [doc = " Scan output2 and import all declarations as macros"] pub fn import_from_output2 () -> Result < Self > { let mut macros = HashMap :: new () ; let output2_path = Path :: new ("output2") ; for entry in ignore_syscall ! (std :: fs :: read_dir (output2_path)) ? { let entry = entry ? ; if entry . file_type () ? . is_dir () { let crate_name = entry . file_name () . to_string_lossy () . to_string () ; if crate_name . starts_with ("wrapped-") { Self :: import_crate_declarations (& mut macros , & entry . path ()) ? ; } } } println ! ("📦 Imported {} macro declarations from output2" , macros . len ()) ; Ok (Output2MacroSystem { macros , interpreter : LispInterpreter :: new () , }) } # [doc = " Import all declarations from a single wrapped crate"] fn import_crate_declarations (macros : & mut HashMap < String , MacroDeclaration > , crate_path : & Path) -> Result < () > { let decls_path = crate_path . join ("src/decls") ; if ! decls_path . exists () { return Ok (()) ; } for entry in ignore_syscall ! (std :: fs :: read_dir (decls_path)) ? { let entry = entry ? ; if entry . path () . extension () . map_or (false , | ext | ext == "rs") { let file_name = entry . file_name () . to_string_lossy () . to_string () ; let content = ignore_syscall ! (std :: fs :: read_to_string (entry . path ())) ? ; let (decl_name , decl_type) = Self :: parse_declaration_info (& file_name , & content) ; let macro_decl = MacroDeclaration { name : decl_name . clone () , source_path : entry . path () . to_string_lossy () . to_string () , declaration_type : decl_type , content , wrapper : None , } ; macros . insert (decl_name , macro_decl) ; } } Ok (()) } # [doc = " Parse declaration name and type from filename and content"] fn parse_declaration_info (filename : & str , content : & str) -> (String , String) { let parts : Vec < & str > = filename . trim_end_matches (".rs") . split ('_') . collect () ; let decl_name = parts . last () . map_or ("unknown" , | v | v) . to_string () ; let decl_type = if content . contains ("pub fn ") { "function" } else if content . contains ("pub struct ") { "struct" } else if content . contains ("pub enum ") { "enum" } else if content . contains ("macro_rules!") { "macro" } else { "unknown" } ; (decl_name , decl_type . to_string ()) } # [doc = " Overlay wrapper macros on imported declarations"] pub fn overlay_wrappers (& mut self) { for (name , decl) in & mut self . macros { let wrapper = match decl . declaration_type . as_str () { "function" => Some (format ! ("
                    macro_rules! call_{} {{
                        ($($args:expr),*) => {{
                            // Runtime wrapper for {}
                            println!(\"🔧 Calling function: {}\");
                            {}($($args),*)
                        }};
                    }}" , name , name , name , name)) , "struct" => Some (format ! ("
                    macro_rules! create_{} {{
                        ($($field:ident: $value:expr),*) => {{
                            println!(\"🏗️ Creating struct: {}\");
                            {} {{ $($field: $value),* }}
                        }};
                    }}" , name , name , name)) , "macro" => Some (format ! ("
                    macro_rules! invoke_{} {{
                        ($($args:tt)*) => {{
                            println!(\"🎭 Invoking macro: {}\");
                            {}!($($args)*)
                        }};
                    }}" , name , name , name)) , _ => None , } ; decl . wrapper = wrapper ; } println ! ("🎭 Overlaid wrappers on {} declarations" , self . macros . len ()) ; } # [doc = " Generate wrapped rustdoc with all callable macros"] pub fn generate_rustdoc (& self) -> Result < String > { let mut doc = String :: new () ; doc . push_str ("//! # Output2 Macro System - Lisp-like Runtime Code Generation\n") ; doc . push_str ("//! \n") ; doc . push_str ("//! All declarations from output2 wrapped crates, callable as macros.\n") ; doc . push_str ("//! Runtime interpretation system for dynamic code generation.\n\n") ; let mut functions = Vec :: new () ; let mut structs = Vec :: new () ; let mut macros = Vec :: new () ; for (name , decl) in & self . macros { match decl . declaration_type . as_str () { "function" => functions . push ((name , decl)) , "struct" => structs . push ((name , decl)) , "macro" => macros . push ((name , decl)) , _ => { } } } if ! functions . is_empty () { doc . push_str ("## 🔧 Callable Functions\n\n") ; for (name , decl) in functions { if let Some (wrapper) = & decl . wrapper { doc . push_str (& format ! ("### `call_{}!()`\n" , name)) ; doc . push_str (& format ! ("Source: `{}`\n\n" , decl . source_path)) ; doc . push_str ("```rust\n") ; doc . push_str (wrapper) ; doc . push_str ("\n```\n\n") ; } } } if ! structs . is_empty () { doc . push_str ("## 🏗️ Creatable Structs\n\n") ; for (name , decl) in structs { if let Some (wrapper) = & decl . wrapper { doc . push_str (& format ! ("### `create_{}!()`\n" , name)) ; doc . push_str (& format ! ("Source: `{}`\n\n" , decl . source_path)) ; doc . push_str ("```rust\n") ; doc . push_str (wrapper) ; doc . push_str ("\n```\n\n") ; } } } if ! macros . is_empty () { doc . push_str ("## 🎭 Invokable Macros\n\n") ; for (name , decl) in macros { if let Some (wrapper) = & decl . wrapper { doc . push_str (& format ! ("### `invoke_{}!()`\n" , name)) ; doc . push_str (& format ! ("Source: `{}`\n\n" , decl . source_path)) ; doc . push_str ("```rust\n") ; doc . push_str (wrapper) ; doc . push_str ("\n```\n\n") ; } } } Ok (doc) } }
};
}
