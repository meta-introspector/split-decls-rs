// Generated macro for copy_dir_recursive (function)
macro_rules! Depcrate_copy_dir_recursivecopy_dir_recursive {
() => {
// Module: crate::copy_dir_recursive
// Provides: {"copy_dir_recursive"}
// Dependencies: {}
# [doc = " Recursively copies contents from `src` to `dst`, with optional `ignore` patterns."] # [doc = ""] # [doc = " `ignore`: A list of file or directory names (relative to `src`) to ignore during the copy."] # [doc = " `overwrite`: If true, existing files in `dst` will be overwritten."] # [doc = " `current_depth`: The current recursion depth."] # [doc = " `max_depth`: The maximum allowed recursion depth."] pub fn copy_dir_recursive (src : & Path , dst : & Path , ignore : & [String] , overwrite : bool , current_depth : usize , max_depth : usize ,) -> Result < () > { if current_depth > max_depth { anyhow :: bail ! ("Maximum recursion depth ({}) exceeded while copying directory from {} to {}" , max_depth , src . display () , dst . display ()) ; } let lock_file_path = dst . join (".copy_lock") ; if lock_file_path . exists () { anyhow :: bail ! ("Detected potential infinite recursion: lock file exists at {}. Aborting copy from {} to {}" , lock_file_path . display () , src . display () , dst . display ()) ; } fs :: File :: create (& lock_file_path) . context (format ! ("Failed to create lock file at {}" , lock_file_path . display ())) ? ; let _lock_guard = LockFileGuard { path : lock_file_path } ; fs :: create_dir_all (dst) . context (format ! ("Failed to create destination directory {}" , dst . display ())) ? ; for entry in fs :: read_dir (src) . context (format ! ("Failed to read source directory {}" , src . display ())) ? { let entry = entry ? ; let entry_path = entry . path () ; let file_name = entry_path . file_name () . ok_or_else (| | anyhow :: anyhow ! ("Invalid file name")) ? ; let dest_path = dst . join (file_name) ; if ignore . contains (& file_name . to_string_lossy () . to_string ()) { continue ; } let ty = entry . file_type () ? ; if ty . is_dir () { copy_dir_recursive (& entry_path , & dest_path , ignore , overwrite , current_depth + 1 , max_depth) ? ; } else { if dest_path . exists () && ! overwrite { continue ; } fs :: copy (& entry_path , & dest_path) . context (format ! ("Failed to copy file from {} to {}" , entry_path . display () , dest_path . display ())) ? ; } } Ok (()) }
};
}
