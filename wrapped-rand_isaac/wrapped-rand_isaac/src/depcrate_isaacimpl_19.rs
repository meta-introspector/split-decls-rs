// Generated macro for impl_19 (impl)
macro_rules! Depcrate_isaacimpl_19 {
() => {
// Module: crate::isaac
// Provides: {"impl_19"}
// Dependencies: {}
impl IsaacCore { # [doc = " Create a new ISAAC random number generator."] # [doc = ""] # [doc = " The author Bob Jenkins describes how to best initialize ISAAC here:"] # [doc = " <https://rt.cpan.org/Public/Bug/Display.html?id=64324>"] # [doc = " The answer is included here just in case:"] # [doc = ""] # [doc = " \"No, you don't need a full 8192 bits of seed data. Normal key sizes will"] # [doc = " do fine, and they should have their expected strength (eg a 40-bit key"] # [doc = " will take as much time to brute force as 40-bit keys\u{a0}usually will). You"] # [doc = " could fill the remainder with 0, but set the last array element to the"] # [doc = " length of the key provided (to distinguish keys that differ only by"] # [doc = " different amounts of 0 padding). You do still need to call `randinit()`"] # [doc = " to make sure the initial state isn't uniform-looking.\""] # [doc = " \"After publishing ISAAC, I wanted to limit the key to half the size of"] # [doc = " `r[]`, and repeat it twice. That would have made it hard to provide a"] # [doc = " key that sets the whole internal state to anything convenient. But I'd"] # [doc = " already published it.\""] # [doc = ""] # [doc = " And his answer to the question \"For my code, would repeating the key"] # [doc = " over and over to fill 256 integers be a better solution than"] # [doc = " zero-filling, or would they essentially be the same?\":"] # [doc = " \"If the seed is under 32 bytes, they're essentially the same, otherwise"] # [doc = " repeating the seed would be stronger. randinit() takes a chunk of 32"] # [doc = " bytes, mixes it, and combines that with the next 32 bytes, et cetera."] # [doc = " Then loops over all the elements the same way a second time.\""] # [inline] fn init (mut mem : [w32 ; RAND_SIZE] , rounds : u32) -> Self { # [rustfmt :: skip] fn mix (a : & mut w32 , b : & mut w32 , c : & mut w32 , d : & mut w32 , e : & mut w32 , f : & mut w32 , g : & mut w32 , h : & mut w32) { * a ^= * b << 11 ; * d += * a ; * b += * c ; * b ^= * c >> 2 ; * e += * b ; * c += * d ; * c ^= * d << 8 ; * f += * c ; * d += * e ; * d ^= * e >> 16 ; * g += * d ; * e += * f ; * e ^= * f << 10 ; * h += * e ; * f += * g ; * f ^= * g >> 4 ; * a += * f ; * g += * h ; * g ^= * h << 8 ; * b += * g ; * h += * a ; * h ^= * a >> 9 ; * c += * h ; * a += * b ; } let mut a = w (0x1367df5a) ; let mut b = w (0x95d90059) ; let mut c = w (0xc3163e4b) ; let mut d = w (0x0f421ad8) ; let mut e = w (0xd92a4a78) ; let mut f = w (0xa51a3c49) ; let mut g = w (0xc4efea1b) ; let mut h = w (0x30609119) ; for _ in 0 .. rounds { for i in (0 .. RAND_SIZE / 8) . map (| i | i * 8) { a += mem [i] ; b += mem [i + 1] ; c += mem [i + 2] ; d += mem [i + 3] ; e += mem [i + 4] ; f += mem [i + 5] ; g += mem [i + 6] ; h += mem [i + 7] ; mix (& mut a , & mut b , & mut c , & mut d , & mut e , & mut f , & mut g , & mut h ,) ; mem [i] = a ; mem [i + 1] = b ; mem [i + 2] = c ; mem [i + 3] = d ; mem [i + 4] = e ; mem [i + 5] = f ; mem [i + 6] = g ; mem [i + 7] = h ; } } Self { mem , a : w (0) , b : w (0) , c : w (0) , } } }
};
}
