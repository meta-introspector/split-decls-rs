// Generated by add_generated_rust_header! macro at submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs:71 from submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs at 2025-12-23T20:00:20.906906855+00:00
// Do not edit this file by hand. Your changes will be overwritten.

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
fn process_recursive(
    path: &PathBuf,
    config: &SplitDeclsConfig,
    processed: &mut i32,
    errors: &mut i32,
    start_time: &Instant,
    total_crates: Option<usize>,
) -> Result<()> {
    if path.join("Cargo.toml").exists() && path.join("src/lib.rs").exists() {
        let crate_start = Instant::now();
        println!("🔧 Processing: {}", path.display());
        match process_crate(path, config, false) {
            Ok(_) => {
                *processed += 1;
                let elapsed = start_time.elapsed().as_secs_f64();
                let crate_time = crate_start.elapsed().as_secs_f64();
                if let Some(total) = total_crates {
                    let progress = (*processed as f64 / total as f64) * 100.0;
                    let rate = *processed as f64 / elapsed;
                    let remaining = total - *processed as usize;
                    let eta_seconds = remaining as f64 / rate;
                    println!(
                        "✅ Completed {} ({:.1}% done, {:.1}s, ETA: {:.0}m {:.0}s)",
                        path.display(),
                        progress,
                        crate_time,
                        eta_seconds / 60.0,
                        eta_seconds % 60.0
                    );
                } else {
                    println!(
                        "✅ Completed {} ({:.1}s, rate: {:.1}/min)",
                        path.display(),
                        crate_time,
                        (*processed as f64 / elapsed) * 60.0
                    );
                }
                if *processed % 50 == 0 {
                    println!(
                        "📈 Milestone: {} crates processed in {:.1}m",
                        processed,
                        elapsed / 60.0
                    );
                }
            }
            Err(e) => {
                println!("❌ Error in {}: {}", path.display(), e);
                *errors += 1;
            }
        }
    }
    if let Ok(entries) = fs::read_dir(path) {
        for entry in entries {
            if let Ok(entry) = entry {
                let entry_path = entry.path();
                if entry_path.is_dir() && !entry_path.is_symlink() {
                    let name = entry_path.file_name().unwrap().to_string_lossy();
                    if !name.starts_with('.') && name != "target" && name != "node_modules" {
                        process_recursive(
                            &entry_path,
                            config,
                            processed,
                            errors,
                            start_time,
                            total_crates,
                        )?;
                    }
                }
            }
        }
    }
    Ok(())
}
