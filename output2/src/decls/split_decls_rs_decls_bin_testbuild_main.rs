 use serde :: { Deserialize , Serialize } ; use std :: collections :: HashMap ; fn main () -> anyhow :: Result < () > { let generated_cargo_toml : CargoToml = define_root_cargo_toml ! { [package] { mkbuildrs ! () } } ; let toml_string = toml :: to_string_pretty (& generated_cargo_toml) . expect ("Failed to serialize CargoToml to TOML string") ; println ! ("{}" , toml_string) ; Ok (()) }