// Generated by add_generated_rust_header! macro at submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs:71 from submodules/split-decls-rs/src/eager_splitter/declaration_writer.rs at 2025-12-23T19:48:56.301838152+00:00
// Do not edit this file by hand. Your changes will be overwritten.

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
#[cfg(test)]
mod tests {
    use super::*;
    use quote::ToTokens;
    use std::path::PathBuf;
    #[test]
    fn test_generate_build_rs_token_stream_basic() {
        let decls_output_dir = PathBuf::from("/tmp/test_crate/src/decls");
        let crate_name_sanitized = "test_crate_name";
        let result = generate_build_rs_token_stream(
            &old_lib_rs_path,
            &old_build_rs_path,
            &decls_output_dir,
            crate_name_sanitized,
        );
        assert!(result.is_ok());
        let token_stream = result.unwrap();
        let code = token_stream.to_string();
        println!("Generated code for mod.rs test:\n{}", code);
    }
    #[test]
    fn test_generate_build_rs_token_stream_empty_lib_rs() {
        let temp_dir = tempfile::tempdir().expect("Failed to create temporary directory");
        let decls_output_dir = temp_dir.path().join("decls");
        let crate_name_sanitized = "empty_crate";
        let result = generate_build_rs_token_stream(
            &old_lib_rs_path,
            &old_build_rs_path,
            &decls_output_dir,
            crate_name_sanitized,
        );
        assert!(result.is_ok());
        let token_stream = result.unwrap();
        let code = token_stream.to_string();
        println!("Generated code for empty_lib_rs test:\n{}", code);
        let parsed_file: syn::File =
            syn::parse2(token_stream.clone()).expect("Failed to parse generated TokenStream");
        let main_fn_item = parsed_file.items.iter().find_map(|item| {
            if let syn::Item::Fn(item_fn) = item {
                if item_fn.sig.ident == "main" {
                    Some(item_fn)
                } else {
                    None
                }
            } else {
                None
            }
        });
        assert!(
            main_fn_item.is_some(),
            "Generated code should contain a main function."
        );
        let main_fn = main_fn_item.unwrap();
        assert_eq!(
            main_fn.sig.output.to_token_stream().to_string(),
            "-> Result < () >",
            "Main function should return `Result<()>`"
        );
        let decl_module_call_exists = parsed_file.items.iter().any(|item| {
            if let syn::Item::Macro(item_macro) = item {
                item_macro
                    .mac
                    .path
                    .segments
                    .last()
                    .map(|s| s.ident.to_string())
                    == Some("decl_module".to_string())
            } else {
                false
            }
        });
        assert!(
            !decl_module_call_exists,
            "Generated code should not contain `decl_module!` for an empty lib.rs."
        );
        assert!(
            code.contains("cargo:rerun-if-changed=build.rs"),
            "Generated code should contain rerun-if-changed for build.rs"
        );
        assert!(
            code.contains("cargo:rerun-if-changed=.split-decls-config.toml"),
            "Generated code should contain rerun-if-changed for config file"
        );
    }
}
