use proc_macro :: TokenStream ; prelude ! { } # [decl_ cargo_toml_generator_macros_decls_macros_impl_for_Parse] impl Parse for DepTableInput { fn parse (input : ParseStream) -> Result < Self > { let name : LitStr = input . parse () ? ; let comma_token : Token ! [,] = input . parse () ? ; let table_content : proc_macro2 :: TokenStream = input . parse () ? ; Ok (DepTableInput { name , _comma_token : comma_token , table_content , }) } }