// Generated macro for detect_from_fdt (function)
macro_rules! Depcrate_mm_physicalmemdetect_from_fdt {
() => {
// Module: crate::mm::physicalmem
// Provides: {"detect_from_fdt"}
// Dependencies: {}
unsafe fn detect_from_fdt () -> Result < () , () > { let fdt = env :: fdt () . ok_or (()) ? ; let all_regions = fdt . find_all_nodes ("/memory") . map (| m | m . reg () . unwrap () . next () . unwrap ()) ; if all_regions . count () == 0 { return Err (()) ; } let all_regions = fdt . find_all_nodes ("/memory") . map (| m | m . reg () . unwrap () . next () . unwrap ()) ; for m in all_regions { let start_address = m . starting_address as u64 ; let size = m . size . unwrap () as u64 ; let end_address = start_address + size ; if end_address <= super :: kernel_end_address () . as_u64 () && ! env :: is_uefi () { continue ; } let start_address = if start_address <= super :: kernel_start_address () . as_u64 () && ! env :: is_uefi () { super :: kernel_end_address () } else { VirtAddr :: new (start_address) } ; let range = PageRange :: new (start_address . as_usize () , end_address as usize) . unwrap () ; unsafe { FrameAlloc :: deallocate (range) ; map_frame_range (range) ; } TOTAL_MEMORY . fetch_add (range . len () . get () , Ordering :: Relaxed) ; debug ! ("Claimed physical memory: {range:#x?}") ; } let reserve = | reservation : PageRange | { debug ! ("Memory reservation: {reservation:#x?}") ; while let Ok (reserved) = PHYSICAL_FREE_LIST . lock () . allocate_with (| range | reservation . and (range)) { debug ! ("Reserved {reserved:#x?}") ; } } ; for reservation in fdt . memory_reservations () { let start = reservation . address () . addr () ; let end = start + reservation . size () ; let reservation = PageRange :: new (start , end) . unwrap () ; reserve (reservation) ; } let kernel_start = if env :: is_uefi () { super :: kernel_start_address () . as_usize () } else { 0 } ; let kernel_end = super :: kernel_end_address () . as_usize () ; let kernel_region = PageRange :: new (kernel_start , kernel_end) . unwrap () ; reserve (kernel_region) ; let fdt_start = env :: boot_info () . hardware_info . device_tree . unwrap () . get () ; let fdt_start = usize :: try_from (fdt_start) . unwrap () ; let fdt_end = fdt_start + fdt . total_size () ; let fdt_region = PageRange :: containing (fdt_start , fdt_end) . unwrap () ; reserve (fdt_region) ; Ok (()) }
};
}
