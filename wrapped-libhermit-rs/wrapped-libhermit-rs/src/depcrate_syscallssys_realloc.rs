// Generated macro for sys_realloc (function)
macro_rules! Depcrate_syscallssys_realloc {
() => {
// Module: crate::syscalls
// Provides: {"sys_realloc"}
// Dependencies: {}
# [doc = " Shrink or grow a block of memory to the given `new_size`. The block is described by the given"] # [doc = " ptr pointer and layout. If this returns a non-null pointer, then ownership of the memory block"] # [doc = " referenced by ptr has been transferred to this allocator. The memory may or may not have been"] # [doc = " deallocated, and should be considered unusable (unless of course it was transferred back to the"] # [doc = " caller again via the return value of this method). The new memory block is allocated with"] # [doc = " layout, but with the size updated to new_size."] # [doc = " If this method returns null, then ownership of the memory block has not been transferred to this"] # [doc = " allocator, and the contents of the memory block are unaltered."] # [doc = ""] # [doc = " # Safety"] # [doc = " This function is unsafe because undefined behavior can result if the caller does not ensure all"] # [doc = " of the following:"] # [doc = " - `ptr` must be currently allocated via this allocator,"] # [doc = " - `size` and `align` must be the same layout that was used to allocate that block of memory."] # [doc = " ToDO: verify if the same values for size and align always lead to the same layout"] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns null if the new layout does not meet the size and alignment constraints of the"] # [doc = " allocator, or if reallocation otherwise fails."] # [cfg (all (target_os = "none" , not (feature = "common-os")))] # [hermit_macro :: system] # [unsafe (no_mangle)] pub unsafe extern "C" fn sys_realloc (ptr : * mut u8 , size : usize , align : usize , new_size : usize ,) -> * mut u8 { unsafe { let layout_res = Layout :: from_size_align (size , align) ; if layout_res . is_err () || size == 0 || new_size == 0 { warn ! ("__sys_realloc called with ptr {ptr:p}, size {size:#x}, align {align:#x}, new_size {new_size:#x} is an invalid layout!") ; return core :: ptr :: null_mut () ; } let layout = layout_res . unwrap () ; let new_ptr = ALLOCATOR . realloc (ptr , layout , new_size) ; if new_ptr . is_null () { debug ! ("__sys_realloc failed to resize ptr {ptr:p} with size {size:#x}, align {align:#x}, new_size {new_size:#x} !") ; } else { trace ! ("__sys_realloc: resized memory at {ptr:p}, new address {new_ptr:p}") ; } new_ptr } }
};
}
