// Generated macro for impl_542 (impl)
macro_rules! Depcrate_drivers_virtio_transport_pciimpl_542 {
() => {
// Module: crate::drivers::virtio::transport::pci
// Provides: {"impl_542"}
// Dependencies: {}
impl ComCfg { # [doc = " Select a queue via an index. If queue does NOT exist returns `None`, else"] # [doc = " returns `Some(VqCfgHandler)`."] # [doc = ""] # [doc = " INFO: The queue size is automatically bounded by constant `src::config:VIRTIO_MAX_QUEUE_SIZE`."] pub fn select_vq (& mut self , index : u16) -> Option < VqCfgHandler < '_ > > { self . com_cfg . as_mut_ptr () . queue_select () . write (index . into ()) ; if self . com_cfg . as_mut_ptr () . queue_size () . read () . to_ne () == 0 { None } else { Some (VqCfgHandler { vq_index : index , raw : self . com_cfg . borrow_mut () , }) } } pub fn device_config_space (& self) -> VolatilePtr < '_ , CommonCfg , ReadOnly > { self . com_cfg . as_ptr () } # [doc = " Returns the device status field."] pub fn dev_status (& self) -> u8 { self . com_cfg . as_ptr () . device_status () . read () . bits () } # [doc = " Resets the device status field to zero."] pub fn reset_dev (& mut self) { memory_barrier () ; self . com_cfg . as_mut_ptr () . device_status () . write (DeviceStatus :: empty ()) ; } # [doc = " Sets the device status field to FAILED."] # [doc = " A driver MUST NOT initialize and use the device any further after this."] # [doc = " A driver MAY use the device again after a proper reset of the device."] pub fn set_failed (& mut self) { memory_barrier () ; self . com_cfg . as_mut_ptr () . device_status () . write (DeviceStatus :: FAILED) ; } # [doc = " Sets the ACKNOWLEDGE bit in the device status field. This indicates, the"] # [doc = " OS has notived the device"] pub fn ack_dev (& mut self) { memory_barrier () ; self . com_cfg . as_mut_ptr () . device_status () . update (| s | s | DeviceStatus :: ACKNOWLEDGE) ; } # [doc = " Sets the DRIVER bit in the device status field. This indicates, the OS"] # [doc = " know how to run this device."] pub fn set_drv (& mut self) { memory_barrier () ; self . com_cfg . as_mut_ptr () . device_status () . update (| s | s | DeviceStatus :: DRIVER) ; } # [doc = " Sets the FEATURES_OK bit in the device status field."] # [doc = ""] # [doc = " Drivers MUST NOT accept new features after this step."] pub fn features_ok (& mut self) { memory_barrier () ; self . com_cfg . as_mut_ptr () . device_status () . update (| s | s | DeviceStatus :: FEATURES_OK) ; } # [doc = " In order to correctly check feature negotiaten, this function"] # [doc = " MUST be called after [self.features_ok()](ComCfg::features_ok()) in order to check"] # [doc = " if features have been accepted by the device after negotiation."] # [doc = ""] # [doc = " Re-reads device status to ensure the FEATURES_OK bit is still set:"] # [doc = " otherwise, the device does not support our subset of features and the device is unusable."] pub fn check_features (& self) -> bool { memory_barrier () ; self . com_cfg . as_ptr () . device_status () . read () . contains (DeviceStatus :: FEATURES_OK) } # [doc = " Sets the DRIVER_OK bit in the device status field."] # [doc = ""] # [doc = " After this call, the device is \"live\"!"] pub fn drv_ok (& mut self) { memory_barrier () ; self . com_cfg . as_mut_ptr () . device_status () . update (| s | s | DeviceStatus :: DRIVER_OK) ; } # [doc = " Returns the features offered by the device."] pub fn dev_features (& mut self) -> virtio :: F { let com_cfg = self . com_cfg . as_mut_ptr () ; let device_feature_select = com_cfg . device_feature_select () ; let device_feature = com_cfg . device_feature () ; memory_barrier () ; device_feature_select . write (1 . into ()) ; memory_barrier () ; let mut device_features = u64 :: from (device_feature . read () . to_ne ()) << 32 ; device_feature_select . write (0 . into ()) ; memory_barrier () ; device_features |= u64 :: from (device_feature . read () . to_ne ()) ; virtio :: F :: from_bits_retain (u128 :: from (device_features) . into ()) } # [doc = " Write selected features into driver_select field."] pub fn set_drv_features (& mut self , features : virtio :: F) { let features = features . bits () . to_ne () as u64 ; let com_cfg = self . com_cfg . as_mut_ptr () ; let driver_feature_select = com_cfg . driver_feature_select () ; let driver_feature = com_cfg . driver_feature () ; let high : u32 = (features >> 32) as u32 ; let low : u32 = features as u32 ; memory_barrier () ; driver_feature_select . write (0 . into ()) ; memory_barrier () ; driver_feature . write (low . into ()) ; driver_feature_select . write (1 . into ()) ; memory_barrier () ; driver_feature . write (high . into ()) ; } }
};
}
