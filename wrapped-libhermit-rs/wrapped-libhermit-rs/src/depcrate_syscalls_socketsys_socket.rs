// Generated macro for sys_socket (function)
macro_rules! Depcrate_syscalls_socketsys_socket {
() => {
// Module: crate::syscalls::socket
// Provides: {"sys_socket"}
// Dependencies: {}
# [hermit_macro :: system (errno)] # [unsafe (no_mangle)] pub extern "C" fn sys_socket (domain : i32 , type_ : i32 , protocol : i32) -> i32 { debug ! ("sys_socket: domain {domain}, type {type_:?}, protocol {protocol}") ; let Ok (Ok (domain)) = u8 :: try_from (domain) . map (Af :: try_from) else { return - i32 :: from (Errno :: Inval) ; } ; let Some ((sock , sock_flags)) = Sock :: from_bits (type_) else { return - i32 :: from (Errno :: Inval) ; } ; let Ok (Ok (proto)) = u8 :: try_from (protocol) . map (Ipproto :: try_from) else { return - i32 :: from (Errno :: Inval) ; } ; match (sock , proto) { (_ , Ipproto :: Ip | Ipproto :: Ipv6) | (Sock :: Stream , Ipproto :: Tcp) | (Sock :: Dgram , Ipproto :: Udp) => { } (_ , _) => return - i32 :: from (Errno :: Inval) , } # [cfg (feature = "vsock")] if domain == Af :: Vsock && sock == Sock :: Stream { let mut socket = vsock :: Socket :: new () ; if sock_flags . contains (SockFlags :: SOCK_NONBLOCK) { block_on (socket . set_status_flags (fd :: StatusFlags :: O_NONBLOCK) , None) . unwrap () ; } let socket = Arc :: new (async_lock :: RwLock :: new (socket)) ; let fd = insert_object (socket) . expect ("FD is already used") ; return fd ; } # [cfg (feature = "net")] if (domain == Af :: Inet || domain == Af :: Inet6) && (sock == Sock :: Stream || sock == Sock :: Dgram) { let mut guard = NIC . lock () ; if let NetworkState :: Initialized (nic) = & mut * guard { # [cfg (feature = "udp")] if sock == Sock :: Dgram { let handle = nic . create_udp_handle () . unwrap () ; drop (guard) ; let mut socket = udp :: Socket :: new (handle , domain) ; if sock_flags . contains (SockFlags :: SOCK_NONBLOCK) { block_on (socket . set_status_flags (fd :: StatusFlags :: O_NONBLOCK) , None) . unwrap () ; } let socket = Arc :: new (async_lock :: RwLock :: new (socket)) ; let fd = insert_object (socket) . expect ("FD is already used") ; return fd ; } # [cfg (feature = "tcp")] if sock == Sock :: Stream { let handle = nic . create_tcp_handle () . unwrap () ; drop (guard) ; let mut socket = tcp :: Socket :: new (handle , domain) ; if sock_flags . contains (SockFlags :: SOCK_NONBLOCK) { block_on (socket . set_status_flags (fd :: StatusFlags :: O_NONBLOCK) , None) . unwrap () ; } let socket = Arc :: new (async_lock :: RwLock :: new (socket)) ; let fd = insert_object (socket) . expect ("FD is already used") ; return fd ; } } } - i32 :: from (Errno :: Inval) }
};
}
