// Generated macro for impl_469 (impl)
macro_rules! Depcrate_drivers_pciimpl_469 {
() => {
// Module: crate::drivers::pci
// Provides: {"impl_469"}
// Dependencies: {}
impl < T : ConfigRegionAccess > PciDevice < T > { pub const fn new (address : PciAddress , access : T) -> Self { Self { address , access } } pub fn access (& self) -> & T { & self . access } pub fn header (& self) -> PciHeader { PciHeader :: new (self . address) } # [doc = " Set flag to the command register"] pub fn set_command (& self , cmd : CommandRegister) { self . header () . update_command (& self . access , | command | command | cmd) ; } # [doc = " Returns the bar at bar-register `slot`."] pub fn get_bar (& self , slot : u8) -> Option < Bar > { let header = self . header () ; if let Some (endpoint) = EndpointHeader :: from_header (header , & self . access) { return endpoint . bar (slot , & self . access) ; } None } # [doc = " Configure the bar at register `slot`"] pub fn set_bar (& self , slot : u8 , bar : Bar) { let value = match bar { Bar :: Io { port } => (port | 1) as usize , Bar :: Memory32 { address , size : _ , prefetchable , } => { if prefetchable { (address | (1 << 3)) as usize } else { address as usize } } Bar :: Memory64 { address , size : _ , prefetchable , } => { if prefetchable { (address | (2 << 1) | (1 << 3)) as usize } else { (address | (2 << 1)) as usize } } } ; let mut header = EndpointHeader :: from_header (self . header () , & self . access) . unwrap () ; unsafe { header . write_bar (slot , & self . access , value) . unwrap () ; } } # [doc = " Memory maps pci bar with specified index to identical location in virtual memory."] # [doc = " no_cache determines if we set the `Cache Disable` flag in the page-table-entry."] # [doc = " Returns (virtual-pointer, size) if successful, else None (if bar non-existent or IOSpace)"] pub fn memory_map_bar (& self , index : u8 , no_cache : bool) -> Option < (VirtAddr , usize) > { let (address , size , prefetchable , _width) = match self . get_bar (index) { Some (Bar :: Io { .. }) => { warn ! ("Cannot map IOBar!") ; return None ; } Some (Bar :: Memory32 { address , size , prefetchable , }) => (u64 :: from (address) , usize :: try_from (size) . unwrap () , prefetchable , 32 ,) , Some (Bar :: Memory64 { address , size , prefetchable , }) => (address , usize :: try_from (size) . unwrap () , prefetchable , 64) , _ => { return None ; } } ; if address == 0 { return None ; } debug ! ("Mapping bar {index} at {address:#x} with length {size:#x}") ; if ! prefetchable { warn ! ("Currently only mapping of prefetchable bars is supported!") ; } let physical_address = address ; let virtual_address = crate :: mm :: map (PhysAddr :: new (physical_address) , size , true , true , no_cache) ; Some ((virtual_address , size)) } pub fn get_irq (& self) -> Option < InterruptLine > { let header = self . header () ; let endpoint = EndpointHeader :: from_header (header , & self . access) ? ; let (pin , line) = endpoint . interrupt (& self . access) ; match pin { 0 => { warn ! ("The function uses no legacy interrupt message(s).") ; None } 1 ..= 4 => { # [cfg (target_arch = "x86_64")] if matches ! (line , 16 .. 254) { error ! ("Reserved IRQ number") ; return None ; } else if line == 255 { error ! ("Unknown IRQ line or no connection to the interrupt controller") ; return None ; } Some (line) } 5 .. => { error ! ("Reserved interrupt pin value returned.") ; None } } } pub fn set_irq (& self , pin : InterruptPin , line : InterruptLine) { let mut header = EndpointHeader :: from_header (self . header () , & self . access) . unwrap () ; header . update_interrupt (& self . access , | (_pin , _line) | (pin , line)) ; } pub fn device_id (& self) -> DeviceId { let (_vendor_id , device_id) = self . header () . id (& self . access) ; device_id } pub fn id (& self) -> (VendorId , DeviceId) { self . header () . id (& self . access) } pub fn status (& self) -> StatusRegister { self . header () . status (& self . access) } pub fn capabilities (& self) -> Option < CapabilityIterator < & T > > { EndpointHeader :: from_header (self . header () , & self . access) . map (| header | header . capabilities (& self . access)) } }
};
}
