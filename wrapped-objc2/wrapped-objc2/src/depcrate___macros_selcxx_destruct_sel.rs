// Generated macro for cxx_destruct_sel (function)
macro_rules! Depcrate___macros_selcxx_destruct_sel {
() => {
// Module: crate::__macros::sel
// Provides: {"cxx_destruct_sel"}
// Dependencies: {}
# [doc = " Objective-C runtimes call `.cxx_destruct` as part of the final `dealloc`"] # [doc = " call inside `NSObject` (and has done so since macOS 10.4)."] # [doc = ""] # [doc = " While [GCC does document it somewhat][gcc-docs], this is still severely"] # [doc = " undocumented in clang - but since the selector is emitted into the final"] # [doc = " binary, it is fine to rely on it being used."] # [doc = ""] # [doc = " Unfortunately though, this only works if the class has been defined"] # [doc = " statically, since in that case a flag is set to inform the runtime that it"] # [doc = " needs to run destructors. So unfortunately we can't use this on"] # [doc = " dynamically defined classes."] # [doc = ""] # [doc = ""] # [doc = " # ABI"] # [doc = ""] # [doc = " The ABI of `.cxx_destruct` in Apple's runtime is actually that it does NOT"] # [doc = " take a selector, unlike every other Objective-C method, see:"] # [doc = " <https://github.com/apple-oss-distributions/objc4/blob/objc4-906/runtime/objc-class.mm#L457>"] # [doc = ""] # [doc = " So the signature is `extern \"C-unwind\" fn(*mut AnyObject)`."] # [doc = ""] # [doc = " This is likely because it's not a real Objective-C method that can be"] # [doc = " called from userspace / objc_msgSend, and it's more efficient to not pass"] # [doc = " the selector."] # [doc = ""] # [doc = " Note that even if Apple decides to suddenly add the selector one day,"] # [doc = " ignoring it will still be sound, since the function uses the C calling"] # [doc = " convention, where such an ignored parameter would be allowed on all"] # [doc = " relevant architectures."] # [doc = ""] # [doc = " [gcc-docs]: https://gcc.gnu.org/onlinedocs/gcc/Objective-C-and-Objective-C_002b_002b-Dialect-Options.html#index-fobjc-call-cxx-cdtors"] # [inline] # [allow (dead_code)] fn cxx_destruct_sel () -> Sel { __sel_inner ! (".cxx_destruct" , ".cxx_destruct") }
};
}
