// Generated macro for Retained (struct)
macro_rules! Depcrate_rc_retainedRetained {
() => {
// Module: crate::rc::retained
// Provides: {"Retained"}
// Dependencies: {}
# [doc = " A reference counted pointer type for Objective-C objects."] # [doc = ""] # [doc = " [`Retained`] strongly references or \"retains\" the given object `T`, and"] # [doc = " decrements the retain count or \"releases\" it again when dropped, thereby"] # [doc = " ensuring it will be deallocated at the right time."] # [doc = ""] # [doc = " The type `T` inside `Retained<T>` can be anything that implements"] # [doc = " [`Message`]."] # [doc = ""] # [doc = " This can usually be gotten from one of the methods in [the framework"] # [doc = " crates], but can also be created manually with the [`msg_send!`] macro, or"] # [doc = " even more manually with the [`Retained::retain`], [`Retained::from_raw`]"] # [doc = " and [`Retained::retain_autoreleased`] methods."] # [doc = ""] # [doc = " [the framework crates]: crate::topics::frameworks_list"] # [doc = " [`msg_send!`]: crate::msg_send"] # [doc = ""] # [doc = ""] # [doc = " # Comparison to `std` types"] # [doc = ""] # [doc = " `Retained<T>` is the Objective-C equivalent of [`Arc`], that is, it is a"] # [doc = " thread-safe reference-counting pointer, and allows cloning by bumping the"] # [doc = " reference count, and weak references using [`rc::Weak`]."] # [doc = ""] # [doc = " Unlike `Arc`, objects can be retained directly from a `&T` using"] # [doc = " [`Message::retain`] (for `Arc` you need `&Arc<T>`)."] # [doc = ""] # [doc = " Even though most Objective-C types aren't thread safe, Objective-C has no"] # [doc = " concept of [`Rc`]. Retain/release operations are always atomic."] # [doc = ""] # [doc = " [`Arc`]: alloc::sync::Arc"] # [doc = " [`rc::Weak`]: crate::rc::Weak"] # [doc = " [`Rc`]: std::rc::Rc"] # [doc = ""] # [doc = ""] # [doc = " # Forwarding implementations"] # [doc = ""] # [doc = " Since `Retained<T>` is a smart pointer, it [`Deref`]s to `T`."] # [doc = ""] # [doc = " It also forwards the implementation of a bunch of standard library traits"] # [doc = " such as [`PartialEq`], [`AsRef`], and so on, so that it becomes possible"] # [doc = " to use e.g. `Retained<NSString>` as if it was `NSString`. Note that having"] # [doc = " `NSString` directly is not possible since Objective-C objects cannot live"] # [doc = " on the stack, but instead must reside on the heap, and as such must be"] # [doc = " accessed behind a pointer or a reference (i.e. `&NSString`)."] # [doc = ""] # [doc = " Note that because of current limitations in the Rust trait system, some"] # [doc = " traits like [`Default`], [`IntoIterator`], [`FromIterator`], [`From`] and"] # [doc = " [`Into`] are not directly implementable on `NSString`; for that use-case,"] # [doc = " we instead provide the [`DefaultRetained`], [`RetainedIntoIterator`] and"] # [doc = " [`RetainedFromIterator`] traits, which make some of the the aforementioned"] # [doc = " traits implementable on `Retained`."] # [doc = ""] # [doc = " [`DefaultRetained`]: crate::rc::DefaultRetained"] # [doc = " [`RetainedIntoIterator`]: crate::rc::RetainedIntoIterator"] # [doc = " [`RetainedFromIterator`]: crate::rc::RetainedFromIterator"] # [doc = ""] # [doc = ""] # [doc = " # Memory layout"] # [doc = ""] # [doc = " This is guaranteed to have the same size and alignment as a pointer to the"] # [doc = " object, `*const T`."] # [doc = ""] # [doc = " Additionally, it participates in the null-pointer optimization, that is,"] # [doc = " `Option<Retained<T>>` is guaranteed to have the same size as"] # [doc = " `Retained<T>`."] # [doc = ""] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " Various usage of `Retained` on an immutable object."] # [doc = ""] # [doc = " ```"] # [doc = " # use objc2::runtime::NSObject;"] # [doc = " # objc2::extern_class!("] # [doc = " #     #[unsafe(super(NSObject))]"] # [doc = " #     struct NSString;"] # [doc = " # );"] # [doc = " # #[cfg(available_in_foundation)]"] # [doc = " use objc2_foundation::{NSObject, NSString};"] # [doc = " use objc2::rc::Retained;"] # [doc = " use objc2::{ClassType, msg_send};"] # [doc = ""] # [doc = " // Use `msg_send!` to create an `Retained` with correct memory management"] # [doc = " //"] # [doc = " // SAFETY: The types are correct, and it is safe to call the `new`"] # [doc = " // selector on `NSString`."] # [doc = " let string: Retained<NSString> = unsafe { msg_send![NSString::class(), new] };"] # [doc = " // Or:"] # [doc = " // let string = NSString::new();"] # [doc = ""] # [doc = " // Methods on `NSString` is usable via `Deref`"] # [doc = " # #[cfg(available_in_foundation)]"] # [doc = " assert_eq!(string.length(), 0);"] # [doc = ""] # [doc = " // Bump the reference count of the object."] # [doc = " let another_ref: Retained<NSString> = string.clone();"] # [doc = ""] # [doc = " // Convert one of the references to a reference to `NSObject` instead"] # [doc = " let obj: Retained<NSObject> = string.into_super();"] # [doc = ""] # [doc = " // And use the `Debug` impl from that"] # [doc = " assert_eq!(format!(\"{obj:?}\"), \"\");"] # [doc = ""] # [doc = " // Finally, the `Retained`s go out of scope, the reference counts are"] # [doc = " // decreased, and the string will deallocate"] # [doc = " ```"] # [repr (transparent)] # [doc (alias = "id")] # [doc (alias = "Id")] # [doc (alias = "StrongPtr")] # [cfg_attr (feature = "unstable-coerce-pointee" , derive (std :: marker :: CoercePointee))] pub struct Retained < T : ? Sized > { # [doc = " A pointer to the contained object. The pointer is always retained."] # [doc = ""] # [doc = " It is important that this is `NonNull`, since we want to dereference"] # [doc = " it later, and be able to use the null-pointer optimization."] # [doc = ""] # [doc = " Additionally, covariance is correct because we're either the unique"] # [doc = " owner of `T`, or `T` is immutable."] ptr : NonNull < T > , # [doc = " Necessary for dropck even though we never actually run T's destructor,"] # [doc = " because it might have a `dealloc` that assumes that contained"] # [doc = " references outlive the type."] # [doc = ""] # [doc = " See <https://doc.rust-lang.org/nightly/nomicon/phantom-data.html>"] item : PhantomData < T > , # [doc = " Marks the type as !UnwindSafe. Later on we'll re-enable this."] # [doc = ""] # [doc = " See <https://github.com/rust-lang/rust/issues/93367> for why this is"] # [doc = " required."] notunwindsafe : PhantomData < & 'static mut () > , }
};
}
