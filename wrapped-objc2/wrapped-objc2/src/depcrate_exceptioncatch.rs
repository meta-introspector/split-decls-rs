// Generated macro for catch (function)
macro_rules! Depcrate_exceptioncatch {
() => {
// Module: crate::exception
// Provides: {"catch"}
// Dependencies: {}
# [doc = " Tries to execute the given closure and catches an Objective-C exception"] # [doc = " if one is thrown."] # [doc = ""] # [doc = " This is the Objective-C equivalent of Rust's [`catch_unwind`]."] # [doc = " Accordingly, if your Rust code is compiled with `panic=abort`, or your"] # [doc = " Objective-C code with `-fno-objc-exceptions`, this cannot catch the"] # [doc = " exception."] # [doc = ""] # [doc = " [`catch_unwind`]: std::panic::catch_unwind"] # [doc = ""] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns a `Result` that is either `Ok` if the closure succeeded without an"] # [doc = " exception being thrown, or an `Err` with the exception. The exception is"] # [doc = " automatically released."] # [doc = ""] # [doc = " The exception is `None` in the extremely exceptional case that the"] # [doc = " exception object is `nil`. This should basically never happen, but is"] # [doc = " technically possible on some systems with `@throw nil`, or in OOM"] # [doc = " situations."] # [doc = ""] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This panics if the given closure panics."] # [doc = ""] # [doc = " That is, it completely ignores Rust unwinding and simply lets that pass"] # [doc = " through unchanged."] # [doc = ""] # [doc = " It may also not catch all Objective-C exceptions (such as exceptions"] # [doc = " thrown when handling the memory management of the exception). These are"] # [doc = " mostly theoretical, and should only happen in utmost exceptional cases."] # [cfg (feature = "exception")] pub fn catch < R > (closure : impl FnOnce () -> R + UnwindSafe ,) -> Result < R , Option < Retained < Exception > > > { let mut value = None ; let value_ref = & mut value ; let closure = move | | { * value_ref = Some (closure ()) ; } ; let result = try_no_ret (closure) ; result . map (| () | value . unwrap_or_else (| | unreachable ! ())) }
};
}
