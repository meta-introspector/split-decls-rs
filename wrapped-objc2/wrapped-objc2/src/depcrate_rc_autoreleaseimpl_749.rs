// Generated macro for impl_749 (impl)
macro_rules! Depcrate_rc_autoreleaseimpl_749 {
() => {
// Module: crate::rc::autorelease
// Provides: {"impl_749"}
// Dependencies: {}
impl Pool { # [doc = " Construct a new autorelease pool."] # [doc = ""] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure that when handing out `AutoreleasePool<'p>` to"] # [doc = " functions that this is the innermost pool."] # [doc = ""] # [doc = " Additionally, the pools must be dropped in the same order they were"] # [doc = " created."] # [inline] unsafe fn new () -> Self { let context = unsafe { ffi :: objc_autoreleasePoolPush () } ; # [cfg (all (debug_assertions , not (feature = "unstable-autoreleasesafe")))] POOLS . with (| c | c . borrow_mut () . push (context)) ; Self { context } } # [doc = " Drains the autoreleasepool."] # [doc = ""] # [doc = " The [clang documentation] says that `@autoreleasepool` blocks are not"] # [doc = " drained when exceptions occur because:"] # [doc = ""] # [doc = " > Not draining the pool during an unwind is apparently required by the"] # [doc = " > Objective-C exceptions implementation."] # [doc = ""] # [doc = " The above statement was true in the past, but since [revision `551.1`]"] # [doc = " of objc4 (ships with MacOS 10.9) the exception is now retained when"] # [doc = " `@throw` is encountered (on __OBJC2__, so e.g. not on macOS 32bit)."] # [doc = ""] # [doc = " Since an unwind here is probably caused by Rust, and forgetting to pop"] # [doc = " the pool will likely leak memory, we would really like to do drain in"] # [doc = " `drop` when possible, so that is what we are going to do."] # [doc = ""] # [doc = " [clang documentation]: https://clang.llvm.org/docs/AutomaticReferenceCounting.html#autoreleasepool"] # [doc = " [revision `551.1`]: https://github.com/apple-oss-distributions/objc4/blob/objc4-551.1/runtime/objc-exception.mm#L540"] # [inline] unsafe fn drain (self) { # [cfg (all (target_os = "macos" , target_arch = "x86"))] unsafe { ffi :: objc_autoreleasePoolPop (self . context) ; } } }
};
}
