// Generated macro for autoreleasepool_leaking (function)
macro_rules! Depcrate_rc_autoreleaseautoreleasepool_leaking {
() => {
// Module: crate::rc::autorelease
// Provides: {"autoreleasepool_leaking"}
// Dependencies: {}
# [doc = " Execute `f` in the context of a \"fake\" autorelease pool."] # [doc = ""] # [doc = " This is useful to create a context in which to use autoreleased objects,"] # [doc = " without the overhead of actually creating and draining the pool."] # [doc = ""] # [doc = " Any function boundary in Objective-C is an implicit autorelease pool, so"] # [doc = " there you'd do `id obj2 = [obj autorelease]` and be done with it - but we"] # [doc = " do this using a closure instead because we need some way to bind the"] # [doc = " lifetime of any objects released to the pool."] # [doc = ""] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Autorelease an object to an outer pool, from inside an inner, \"fake\" pool."] # [doc = ""] # [doc = " ```"] # [doc = " use objc2::rc::{autoreleasepool, autoreleasepool_leaking, Retained};"] # [doc = " use objc2::runtime::NSObject;"] # [doc = ""] # [doc = " autoreleasepool(|outer_pool| {"] # [doc = "     let obj = autoreleasepool_leaking(|inner_pool| {"] # [doc = "         // SAFETY: The given `outer_pool` is the actual innermost pool."] # [doc = "         unsafe { Retained::autorelease(NSObject::new(), outer_pool) }"] # [doc = "     });"] # [doc = "     // `obj` is still usable here, since the leaking pool doesn't actually"] # [doc = "     // do anything."] # [doc = "     println!(\"{obj:?}\");"] # [doc = " });"] # [doc = ""] # [doc = " // But it is not usable here, since the outer pool has been closed"] # [doc = " ```"] # [doc = ""] # [doc = " Like [`autoreleasepool`], you can't extend the lifetime of an object to"] # [doc = " outside the closure."] # [doc = ""] # [doc = " ```compile_fail"] # [doc = " use objc2::rc::{autoreleasepool_leaking, Retained};"] # [doc = " use objc2::runtime::NSObject;"] # [doc = ""] # [doc = " let obj = autoreleasepool_leaking(|pool| unsafe {"] # [doc = "     unsafe { Retained::autorelease(NSObject::new(), pool) }"] # [doc = " });"] # [doc = " ```"] # [doc = ""] # [doc = " While you can pass an outer pool into this, you still can't pass the pool"] # [doc = " from this into [`autoreleasepool`]:"] # [doc = ""] # [cfg_attr (feature = "unstable-autoreleasesafe" , doc = "```compile_fail,E0277")] # [cfg_attr (not (feature = "unstable-autoreleasesafe") , doc = "```should_panic")] # [doc = " use objc2::rc::{autoreleasepool, autoreleasepool_leaking, Retained};"] # [doc = " use objc2::runtime::NSObject;"] # [doc = ""] # [doc = " autoreleasepool_leaking(|outer_pool| {"] # [doc = "     let obj = autoreleasepool(|inner_pool| {"] # [doc = "         // SAFETY: NOT safe, the pool is _not_ the innermost pool!"] # [doc = "         unsafe { Retained::autorelease(NSObject::new(), outer_pool) }"] # [doc = "     });"] # [doc = " });"] # [doc = " #"] # [doc = " # panic!(\"Does not panic in release mode, so for testing we make it!\");"] # [doc = " ```"] # [inline] pub fn autoreleasepool_leaking < T , F > (f : F) -> T where for < 'pool > F : FnOnce (AutoreleasePool < 'pool >) -> T , { f (AutoreleasePool :: new (None)) }
};
}
