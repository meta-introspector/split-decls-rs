// Generated macro for Weak (struct)
macro_rules! Depcrate_rc_weakWeak {
() => {
// Module: crate::rc::weak
// Provides: {"Weak"}
// Dependencies: {}
# [doc = " A weak pointer to an Objective-C reference counted object."] # [doc = ""] # [doc = " The object is allowed to be deallocated while the weak pointer is alive,"] # [doc = " though the backing allocation for the object can only be released once all"] # [doc = " weak pointers are gone."] # [doc = ""] # [doc = " Useful for breaking reference cycles and safely checking whether an"] # [doc = " object has been deallocated."] # [doc = ""] # [doc = ""] # [doc = " # Comparison to `std` types"] # [doc = ""] # [doc = " This is the Objective-C equivalent of [`sync::Weak`] from the standard"] # [doc = " library, and hence is only usable on types where `Retained<T>` acts like"] # [doc = " [`sync::Arc`], a.k.a. on non-mutable types."] # [doc = ""] # [doc = " [`sync::Weak`]: std::sync::Weak"] # [doc = " [`sync::Arc`]: std::sync::Arc"] # [repr (transparent)] # [doc (alias = "WeakId")] pub struct Weak < T : ? Sized > { # [doc = " We give the runtime the address to this box, so that it can modify it"] # [doc = " even if the `Weak` is moved."] # [doc = ""] # [doc = " Loading may modify the pointer through a shared reference, so we use"] # [doc = " an UnsafeCell to get a *mut without self being mutable."] # [doc = ""] # [doc = " Remember that any thread may actually modify the inner value"] # [doc = " concurrently, but as long as we only use it through the `objc_XXXWeak`"] # [doc = " methods, all access is behind a lock."] # [doc = ""] # [doc = " TODO: Verify the need for UnsafeCell?"] # [doc = " TODO: Investigate if we can avoid some allocations using `Pin`."] # [doc = " TODO: Add derive(CoercePointee) once this doesn't Box internally."] inner : Box < UnsafeCell < * mut AnyObject > > , # [doc = " Weak inherits variance, dropck and various marker traits from"] # [doc = " `Retained<T>`."] item : PhantomData < Retained < T > > , }
};
}
