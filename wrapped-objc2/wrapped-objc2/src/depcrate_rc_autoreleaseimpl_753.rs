// Generated macro for impl_753 (impl)
macro_rules! Depcrate_rc_autoreleaseimpl_753 {
() => {
// Module: crate::rc::autorelease
// Provides: {"impl_753"}
// Dependencies: {}
impl < 'pool > AutoreleasePool < 'pool > { fn new (_inner : Option < & 'pool Pool >) -> Self { Self { # [cfg (all (debug_assertions , not (feature = "unstable-autoreleasesafe")))] inner : _inner , # [cfg (not (all (debug_assertions , not (feature = "unstable-autoreleasesafe"))))] inner : PhantomData , } } # [inline] pub (crate) fn __verify_is_inner (self) { # [cfg (all (debug_assertions , not (feature = "unstable-autoreleasesafe")))] if let Some (pool) = & self . inner { POOLS . with (| c | { assert_eq ! (c . borrow () . last () , Some (& pool . context) , "tried to use lifetime from pool that was not innermost") ; }) ; } } # [doc = " Returns a shared reference to the given autoreleased pointer object."] # [doc = ""] # [doc = " This is the preferred way to make references from autoreleased"] # [doc = " objects, since it binds the lifetime of the reference to the pool, and"] # [doc = " does some extra checks when debug assertions are enabled."] # [doc = ""] # [doc = " Note that this is helpful, but not sufficient, for ensuring that the"] # [doc = " lifetime of the reference does not exceed the lifetime of the"] # [doc = " autorelease pool. When calling this, you must also ensure that the"] # [doc = " pool is actually the current innermost pool."] # [doc = ""] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the pool is not the innermost pool, this function may panic when"] # [doc = " the `\"std\"` Cargo feature and debug assertions are enabled."] # [doc = ""] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This is equivalent to `&*ptr`, and shares the unsafety of that, except"] # [doc = " the lifetime is bound to the pool instead of being unbounded."] # [doc = ""] # [doc = " The pool must be the innermost pool for the lifetime to be correct."] # [inline] pub unsafe fn ptr_as_ref < T : ? Sized > (self , ptr : * const T) -> & 'pool T { self . __verify_is_inner () ; unsafe { ptr . as_ref () . unwrap_unchecked () } } }
};
}
