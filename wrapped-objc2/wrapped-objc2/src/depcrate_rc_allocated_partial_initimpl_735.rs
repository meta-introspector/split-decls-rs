// Generated macro for impl_735 (impl)
macro_rules! Depcrate_rc_allocated_partial_initimpl_735 {
() => {
// Module: crate::rc::allocated_partial_init
// Provides: {"impl_735"}
// Dependencies: {}
impl < T : ? Sized + Message > Allocated < T > { # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure the pointer is NULL, or that the given object"] # [doc = " has +1 retain count, and that the object behind the pointer has been"] # [doc = " allocated (but not yet initialized)."] # [inline] pub (crate) unsafe fn new (ptr : * mut T) -> Self { Self { ptr , p : PhantomData , p_auto_traits : PhantomData , } } # [doc = " Allocate the object with a fast path using `objc_alloc`."] # [doc = ""] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The object must be safe to allocate on the current thread, and the"] # [doc = " object `T` must be an instance of the class."] # [doc (alias = "objc_alloc")] # [inline] pub (crate) unsafe fn alloc (cls : & AnyClass) -> Self where T : Sized , { # [cfg (all (target_vendor = "apple" , not (all (target_os = "macos" , target_arch = "x86"))))] { let obj : * mut T = unsafe { crate :: ffi :: objc_alloc (cls) . cast () } ; unsafe { Self :: new (obj) } } # [cfg (not (all (target_vendor = "apple" , not (all (target_os = "macos" , target_arch = "x86")))))] { unsafe { crate :: msg_send ! [cls , alloc] } } } # [doc = " Returns a raw pointer to the object."] # [doc = ""] # [doc = " The pointer is valid for at least as long as the `Allocated` is held."] # [doc = ""] # [doc = " See [`Allocated::as_mut_ptr`] for the mutable equivalent."] # [doc = ""] # [doc = " This is an associated method, and must be called as"] # [doc = " `Allocated::as_ptr(obj)`."] # [inline] pub fn as_ptr (this : & Self) -> * const T { this . ptr } # [doc = " Returns a raw mutable pointer to the object."] # [doc = ""] # [doc = " The pointer is valid for at least as long as the `Allocated` is held."] # [doc = ""] # [doc = " See [`Allocated::as_ptr`] for the immutable equivalent."] # [doc = ""] # [doc = " This is an associated method, and must be called as"] # [doc = " `Allocated::as_mut_ptr(obj)`."] # [doc = ""] # [doc = ""] # [doc = " # Note about mutable references"] # [doc = ""] # [doc = " In general, you're not allowed to create a mutable reference from"] # [doc = " `Allocated`, unless you're defining the object and know that to be"] # [doc = " safe."] # [doc = ""] # [doc = " For example, `+[NSMutableString alloc]` is allowed to return a"] # [doc = " non-unique object as an optimization, and then only figure out"] # [doc = " afterwards whether it needs to allocate, or if it can store an"] # [doc = " `NSString` internally."] # [doc = ""] # [doc = " Similarly, while e.g. `+[NSData alloc]` may return a unique object,"] # [doc = " calling `-[NSData init]` afterwards could return a shared empty"] # [doc = " `NSData` instance."] # [inline] # [allow (unknown_lints)] # [allow (clippy :: needless_pass_by_ref_mut)] pub fn as_mut_ptr (this : & mut Self) -> * mut T { this . ptr as * mut T } # [inline] pub (crate) fn into_ptr (this : Self) -> * mut T { let this = ManuallyDrop :: new (this) ; this . ptr as * mut T } # [doc = " Initialize the instance variables for this object."] # [doc = ""] # [doc = " This consumes the allocated instance, and returns the now partially"] # [doc = " initialized instance instead, which can be further used in"] # [doc = " [`msg_send!`] `super` calls."] # [doc = ""] # [doc = " This works very similarly to [Swift's two-phase initialization"] # [doc = " scheme][two-phase-init], see that for details."] # [doc = ""] # [doc = " See also the [`Ivars`] type alias which you will likely need when"] # [doc = " using this."] # [doc = ""] # [doc = " [`msg_send!`]: crate::msg_send"] # [doc = " [two-phase-init]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/initialization/#Two-Phase-Initialization"] # [doc = " [`Ivars`]: crate::Ivars"] # [doc = ""] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If debug assertions are enabled, this function will panic if the"] # [doc = " allocated instance is `NULL`, which usually only happens in Out of"] # [doc = " Memory situations."] # [doc = ""] # [doc = " If debug assertions are disabled, this will return a `NULL` instance"] # [doc = " and the ivars will be dropped. The NULL instance cannot cause"] # [doc = " unsoundness and will likely lead to an initialization failure later on"] # [doc = " instead, but not panicking here is done as a code-size optimization."] # [inline] # [track_caller] pub fn set_ivars (self , ivars : T :: Ivars) -> PartialInit < T > where T : DefinedClass + Sized , { if let Some (ptr) = NonNull :: new (ManuallyDrop :: new (self) . ptr as * mut T) { unsafe { initialize_ivars :: < T > (ptr , ivars) } ; unsafe { PartialInit :: new (ptr . as_ptr ()) } } else if cfg ! (debug_assertions) { panic ! ("tried to initialize instance variables on a NULL allocated object") } else { drop (ivars) ; unsafe { PartialInit :: new (ptr :: null_mut ()) } } } }
};
}
