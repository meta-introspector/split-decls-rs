// Generated macro for Allocated (struct)
macro_rules! Depcrate_rc_allocated_partial_initAllocated {
() => {
// Module: crate::rc::allocated_partial_init
// Provides: {"Allocated"}
// Dependencies: {}
# [doc = " An Objective-C object that has been allocated, but not initialized."] # [doc = ""] # [doc = " Objective-C splits the allocation and initialization steps up into two, so"] # [doc = " we need to track it in the type-system whether something has been"] # [doc = " initialized or not."] # [doc = ""] # [doc = " Note that allocation in Objective-C can fail, e.g. in Out Of Memory"] # [doc = " situations! This is handled by `objc2` automatically, but if you really"] # [doc = " need to, you can check for this explicitly by inspecting the pointer"] # [doc = " returned from [`as_ptr`]."] # [doc = ""] # [doc = " Note also that this represents that the _current_ class's instance"] # [doc = " variables are not yet initialized; but subclass instance variables may"] # [doc = " have been so."] # [doc = ""] # [doc = " See [Apple's documentation on Object Allocation][object-allocation] for a"] # [doc = " few more details."] # [doc = ""] # [doc = " [`as_ptr`]: Self::as_ptr"] # [doc = " [object-allocation]: https://developer.apple.com/library/archive/documentation/General/Conceptual/CocoaEncyclopedia/ObjectAllocation/ObjectAllocation.html"] # [doc = ""] # [doc = ""] # [doc = " # Memory layout"] # [doc = ""] # [doc = " This is guaranteed to have the same size and alignment as a pointer to the"] # [doc = " object, `*const T`. The pointer may be NULL."] # [repr (transparent)] # [derive (Debug)] # [cfg_attr (feature = "unstable-coerce-pointee" , derive (std :: marker :: CoercePointee))] pub struct Allocated < T : ? Sized > { # [doc = " The yet-to-be initialized object."] # [doc = ""] # [doc = " We don't use `Retained` here, since that has different auto-trait"] # [doc = " impls, and requires in its safety contract that the object is"] # [doc = " initialized (which makes it difficult to ensure correctness if such"] # [doc = " things are split across different files). Additionally, we want to"] # [doc = " have fine control over NULL-ness."] # [doc = ""] # [doc = " Covariance is correct, same as `Retained`."] ptr : * const T , # [doc = " Necessary for dropck, as with `Retained`."] p : PhantomData < T > , # [doc = " Necessary for restricting auto traits."] # [doc = ""] # [doc = " We _could_ probably implement auto traits `Send` and `Sync` here, but to be"] # [doc = " safe, we won't for now."] p_auto_traits : PhantomData < AnyObject > , }
};
}
