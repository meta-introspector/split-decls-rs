// Generated macro for encode_fn_pointer_impl (macro)
macro_rules! Depcrate_encodeencode_fn_pointer_impl {
() => {
// Module: crate::encode
// Provides: {"encode_fn_pointer_impl"}
// Dependencies: {}
# [doc = " Helper for implementing [`Encode`]/[`RefEncode`] for function pointers"] # [doc = " whose arguments implement [`Encode`]."] # [doc = ""] # [doc = " Ideally we'd implement it for all function pointers, but due to coherence"] # [doc = " issues, see <https://github.com/rust-lang/rust/issues/56105>, function"] # [doc = " pointers that are higher-ranked over lifetimes don't get implemented."] # [doc = ""] # [doc = " We could fix it by adding those impls and allowing `coherence_leak_check`,"] # [doc = " but it would have to be done for _all_ references, `Option<&T>` and such"] # [doc = " as well. So trying to do it quickly requires generating a polynomial"] # [doc = " amount of implementations, which IMO is overkill for such a small issue."] # [doc = ""] # [doc = " Using `?Sized` is probably not safe here because C functions can only take"] # [doc = " and return items with a known size."] macro_rules ! encode_fn_pointer_impl { (@ $ FnTy : ty , $ ($ Arg : ident) ,*) => { unsafe impl < Ret : EncodeReturn , $ ($ Arg : EncodeArgument) ,*> Encode for $ FnTy { const ENCODING : Encoding = Encoding :: Pointer (& Encoding :: Unknown) ; } unsafe impl < Ret : EncodeReturn , $ ($ Arg : EncodeArgument) ,*> RefEncode for $ FnTy { const ENCODING_REF : Encoding = Encoding :: Pointer (& Self :: ENCODING) ; } unsafe impl < Ret : EncodeReturn , $ ($ Arg : EncodeArgument) ,*> OptionEncode for $ FnTy { } } ; (# $ abi : literal ; $ ($ Arg : ident) ,+) => { encode_fn_pointer_impl ! (@ extern $ abi fn ($ ($ Arg) ,+) -> Ret , $ ($ Arg) ,+) ; encode_fn_pointer_impl ! (@ unsafe extern $ abi fn ($ ($ Arg) ,+) -> Ret , $ ($ Arg) ,+) ; encode_fn_pointer_impl ! (@ extern $ abi fn ($ ($ Arg) ,+ , ...) -> Ret , $ ($ Arg) ,+) ; encode_fn_pointer_impl ! (@ unsafe extern $ abi fn ($ ($ Arg) ,+ , ...) -> Ret , $ ($ Arg) ,+) ; } ; (# $ abi : literal ;) => { encode_fn_pointer_impl ! (@ extern $ abi fn () -> Ret ,) ; encode_fn_pointer_impl ! (@ unsafe extern $ abi fn () -> Ret ,) ; } ; ($ ($ Arg : ident) ,*) => { encode_fn_pointer_impl ! (# "C" ; $ ($ Arg) ,*) ; encode_fn_pointer_impl ! (# "C-unwind" ; $ ($ Arg) ,*) ; } ; }
};
}
