// Generated macro for impl_1027 (impl)
macro_rules! Depcrate_runtime_defineimpl_1027 {
() => {
// Module: crate::runtime::define
// Provides: {"impl_1027"}
// Dependencies: {}
impl ClassBuilder { fn as_mut_ptr (& mut self) -> * mut AnyClass { self . cls . as_ptr () } # [allow (unused)] pub (crate) fn superclass (& self) -> Option < & AnyClass > { unsafe { AnyClass :: superclass_raw (self . cls . as_ptr ()) } } # [allow (unused)] fn name (& self) -> & CStr { unsafe { AnyClass :: name_raw (self . cls . as_ptr ()) } } # [inline] fn with_superclass (name : & CStr , superclass : Option < & AnyClass >) -> Option < Self > { let super_ptr = superclass . map_or (ptr :: null () , | c | c) . cast () ; let cls = unsafe { ffi :: objc_allocateClassPair (super_ptr , name . as_ptr () , 0) } ; NonNull :: new (cls) . map (| cls | Self { cls }) } # [doc = " Constructs a [`ClassBuilder`] with the given name and superclass."] # [doc = ""] # [doc = " Returns [`None`] if the class couldn't be allocated, or a class with"] # [doc = " that name already exist."] # [inline] pub fn new (name : & CStr , superclass : & AnyClass) -> Option < Self > { Self :: with_superclass (name , Some (superclass)) } # [doc = " Constructs a [`ClassBuilder`] that will construct a new root class"] # [doc = " with the given name."] # [doc = ""] # [doc = " Returns [`None`] if the class couldn't be allocated."] # [doc = ""] # [doc = " An implementation for `+initialize` must also be given; the runtime"] # [doc = " calls this method for all classes, so it must be defined on root"] # [doc = " classes."] # [doc = ""] # [doc = " Note that implementing a root class is not a simple endeavor!"] # [doc = " For example, your class probably cannot be passed to Cocoa code unless"] # [doc = " the entire `NSObject` protocol is implemented."] # [doc = " Functionality it expects, like implementations of `-retain` and"] # [doc = " `-release` used by ARC, will not be present otherwise."] pub fn root < F > (name : & CStr , initialize_fn : F) -> Option < Self > where F : MethodImplementation < Callee = AnyClass , Arguments = () , Return = () > , { Self :: with_superclass (name , None) . map (| mut this | { unsafe { this . add_class_method (sel ! (initialize) , initialize_fn) } ; this }) } # [doc = " Adds a method with the given name and implementation."] # [doc = ""] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the method wasn't successfully added (e.g. a method with that"] # [doc = " name already exists)."] # [doc = ""] # [doc = " May also panic if the method was detected to be invalid in some way;"] # [doc = " for example if `debug_assertions` are enabled and the method is"] # [doc = " overriding another method, we verify that their encodings are equal."] # [doc = ""] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure that the types match those that are expected"] # [doc = " when the method is invoked from Objective-C."] pub unsafe fn add_method < T , F > (& mut self , sel : Sel , func : F) where T : Message + ? Sized , F : MethodImplementation < Callee = T > , { unsafe { self . add_method_inner (sel , F :: Arguments :: ENCODINGS , & F :: Return :: ENCODING_RETURN , func . __imp () ,) ; } } unsafe fn add_method_inner (& mut self , sel : Sel , enc_args : & [Encoding] , enc_ret : & Encoding , func : Imp ,) { let sel_args = sel . number_of_arguments () ; assert_eq ! (sel_args , enc_args . len () , "selector {sel} accepts {sel_args} arguments, but function accepts {}" , enc_args . len () ,) ; # [cfg (all (debug_assertions , not (feature = "disable-encoding-assertions")))] if let Some (superclass) = self . superclass () { if let Some (method) = superclass . instance_method (sel) { if let Err (err) = super :: verify :: verify_method_signature (method , enc_args , enc_ret) { panic ! ("defined invalid method -[{} {sel}]: {err}" , self . name () . to_string_lossy ()) } } } let types = method_type_encoding (enc_ret , enc_args) ; let success = unsafe { ffi :: class_addMethod (self . as_mut_ptr () , sel , func , types . as_ptr ()) } ; assert ! (success . as_bool () , "failed to add method {sel}") ; } fn metaclass_mut (& mut self) -> * mut AnyClass { unsafe { ffi :: object_getClass (self . as_mut_ptr () . cast ()) as * mut AnyClass } } # [doc = " Adds a class method with the given name and implementation."] # [doc = ""] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics in the same cases as [`add_method`][Self::add_method]."] # [doc = ""] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure that the types match those that are expected"] # [doc = " when the method is invoked from Objective-C."] pub unsafe fn add_class_method < F > (& mut self , sel : Sel , func : F) where F : MethodImplementation < Callee = AnyClass > , { unsafe { self . add_class_method_inner (sel , F :: Arguments :: ENCODINGS , & F :: Return :: ENCODING_RETURN , func . __imp () ,) ; } } unsafe fn add_class_method_inner (& mut self , sel : Sel , enc_args : & [Encoding] , enc_ret : & Encoding , func : Imp ,) { let sel_args = sel . number_of_arguments () ; assert_eq ! (sel_args , enc_args . len () , "selector {sel} accepts {sel_args} arguments, but function accepts {}" , enc_args . len () ,) ; # [cfg (all (debug_assertions , not (feature = "disable-encoding-assertions")))] if let Some (superclass) = self . superclass () { if let Some (method) = superclass . class_method (sel) { if let Err (err) = super :: verify :: verify_method_signature (method , enc_args , enc_ret) { panic ! ("defined invalid method +[{} {sel}]: {err}" , self . name () . to_string_lossy ()) } } } let types = method_type_encoding (enc_ret , enc_args) ; let success = unsafe { ffi :: class_addMethod (self . metaclass_mut () , sel , func , types . as_ptr ()) } ; assert ! (success . as_bool () , "failed to add class method {sel}") ; } # [doc = " Adds an ivar with type `T` and the provided name."] # [doc = ""] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " If the ivar wasn't successfully added for some reason - this usually"] # [doc = " happens if there already was an ivar with that name."] pub fn add_ivar < T : Encode > (& mut self , name : & CStr) { unsafe { self . add_ivar_inner :: < T > (name , & T :: ENCODING) } } pub (crate) unsafe fn add_ivar_inner < T > (& mut self , name : & CStr , encoding : & Encoding) { unsafe { self . add_ivar_inner_mono (name , mem :: size_of :: < T > () , T :: LOG2_ALIGNMENT , encoding) } } unsafe fn add_ivar_inner_mono (& mut self , name : & CStr , size : usize , align : u8 , encoding : & Encoding ,) { let encoding = CString :: new (encoding . to_string ()) . unwrap () ; let success = unsafe { ffi :: class_addIvar (self . as_mut_ptr () , name . as_ptr () , size , align , encoding . as_ptr () ,) } ; assert ! (success . as_bool () , "failed to add ivar {name:?}") ; } # [doc = " Makes the class conform to the given protocol."] # [doc = ""] # [doc = " This will also make the class conform to any super-protocols that the"] # [doc = " given protocol may have."] # [doc = ""] # [doc = " Returns whether the class did not already conform to the protocol."] # [doc = " This may commonly return false if you first add e.g."] # [doc = " `NSProgressReporting`, and then later try to add `NSObjectProtocol`,"] # [doc = " which is a super-protocol thereof."] # [inline] pub fn add_protocol (& mut self , proto : & AnyProtocol) -> bool { let success = unsafe { ffi :: class_addProtocol (self . as_mut_ptr () , proto) } ; success . as_bool () } # [doc = " Registers the [`ClassBuilder`], consuming it, and returns a reference"] # [doc = " to the newly registered [`AnyClass`]."] # [inline] pub fn register (self) -> & 'static AnyClass { let mut this = ManuallyDrop :: new (self) ; unsafe { ffi :: objc_registerClassPair (this . as_mut_ptr ()) } ; unsafe { this . cls . as_ref () } } }
};
}
