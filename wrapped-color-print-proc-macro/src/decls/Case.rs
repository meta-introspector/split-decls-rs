macro_rules! Case {
    () => {
        # [doc = " Indicates a color has to be searched in its lowercase or uppercase version."] # [derive (Debug , Clone , Copy)] enum Case { Uppercase , Lowercase , }
    };
}

Case!();