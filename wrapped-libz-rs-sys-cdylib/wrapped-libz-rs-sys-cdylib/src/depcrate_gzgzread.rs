// Generated macro for gzread (function)
macro_rules! Depcrate_gzgzread {
() => {
// Module: crate::gz
// Provides: {"gzread"}
// Dependencies: {}
# [doc = " Read and decompress up to `len` uncompressed bytes from `file` into `buf`.  If"] # [doc = " the input file is not in gzip format, `gzread` copies up to `len` bytes into"] # [doc = " the buffer directly from the file."] # [doc = ""] # [doc = " After reaching the end of a gzip stream in the input, `gzread` will continue"] # [doc = " to read, looking for another gzip stream.  Any number of gzip streams may be"] # [doc = " concatenated in the input file, and will all be decompressed by `gzread()`."] # [doc = " If something other than a gzip stream is encountered after a gzip stream,"] # [doc = " `gzread` ignores that remaining trailing garbage (and no error is returned)."] # [doc = ""] # [doc = " `gzread` can be used to read a gzip file that is being concurrently written."] # [doc = " Upon reaching the end of the input, `gzread` will return with the available"] # [doc = " data.  If the error code returned by [`gzerror`] is `Z_OK` or `Z_BUF_ERROR`,"] # [doc = " then [`gzclearerr`] can be used to clear the end of file indicator in order"] # [doc = " to permit `gzread` to be tried again.  `Z_OK` indicates that a gzip stream"] # [doc = " was completed on the last `gzread`.  `Z_BUF_ERROR` indicates that the input"] # [doc = " file ended in the middle of a gzip stream.  Note that `gzread` does not return"] # [doc = " `-1` in the event of an incomplete gzip stream.  This error is deferred until"] # [doc = " [`gzclose`], which will return `Z_BUF_ERROR` if the last gzread ended in the"] # [doc = " middle of a gzip stream.  Alternatively, `gzerror` can be used before `gzclose`"] # [doc = " to detect this case."] # [doc = ""] # [doc = " If the unsigned value `len` is too large to fit in the signed return type"] # [doc = " `c_int`, then nothing is read, `-1` is returned, and the error state is set to"] # [doc = " `Z_STREAM_ERROR`."] # [doc = ""] # [doc = " # Returns"] # [doc = ""] # [doc = " * The number of uncompressed bytes read from the file into `buf`, which may"] # [doc = "   be smaller than `len` if there is insufficient data in the file."] # [doc = " * `-1` on error."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `file` - A gzip file handle, or null."] # [doc = " * `buf` - Buffer where the read data should be stored. The caller retains ownership of this buffer."] # [doc = " * `len` - Number of bytes to attempt to read into `buf`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " - `file`, if non-null, must be an open file handle obtained from [`gzopen`] or [`gzdopen`]."] # [doc = " - The caller must ensure that `buf` points to at least `len` writable bytes."] # [export_name = crate :: prefix ! (gzread)] pub unsafe extern "C-unwind" fn gzread (file : gzFile , buf : * mut c_void , len : c_uint) -> c_int { let Some (state) = (unsafe { file . cast :: < GzState > () . as_mut () }) else { return - 1 ; } ; if state . mode != GzMode :: GZ_READ || (state . err != Z_OK && state . err != Z_BUF_ERROR) { return - 1 ; } if c_int :: try_from (len) . is_err () { const MSG : & str = "request does not fit in an int" ; unsafe { gz_error (state , Some ((Z_STREAM_ERROR , MSG))) } ; return - 1 ; } let got = unsafe { gz_read (state , buf . cast :: < u8 > () , len as usize) } ; if got == 0 && state . err != Z_OK && state . err != Z_BUF_ERROR { - 1 } else { got as _ } }
};
}
