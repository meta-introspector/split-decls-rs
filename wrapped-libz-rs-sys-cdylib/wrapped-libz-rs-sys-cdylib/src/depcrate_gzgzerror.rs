// Generated macro for gzerror (function)
macro_rules! Depcrate_gzgzerror {
() => {
// Module: crate::gz
// Provides: {"gzerror"}
// Dependencies: {}
# [doc = " Retrieve the zlib error code and a human-readable string description of"] # [doc = " the most recent error on a gzip file stream."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `file` - A gzip file handle, or null"] # [doc = " * `errnum` - A pointer to a C integer in which the zlib error code should be"] # [doc = "   written, or null if the caller does not need the numeric error code."] # [doc = ""] # [doc = " # Returns"] # [doc = ""] # [doc = " * A pointer to a null-terminated C string describing the error, if `file` is non-null"] # [doc = "   and has an error"] # [doc = " * A pointer to an empty (zero-length), null-terminated C string, if `file` is non-null"] # [doc = "   but has no error"] # [doc = " * Null otherwise"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `file` must be one of the following:"] # [doc = " - A file handle obtained from [`gzopen`] or [`gzdopen`]."] # [doc = " - A null pointer."] # [doc = ""] # [doc = " If this function returns a non-null string, the caller must not modifiy or"] # [doc = " deallocate the string."] # [doc = ""] # [doc = " If `errnum` is non-null, it must point to an address at which a [`c_int`] may be written."] # [export_name = crate :: prefix ! (gzerror)] pub unsafe extern "C-unwind" fn gzerror (file : gzFile , errnum : * mut c_int) -> * const c_char { let Some (state) = (unsafe { file . cast :: < GzState > () . as_ref () }) else { return ptr :: null () ; } ; if state . mode != GzMode :: GZ_READ && state . mode != GzMode :: GZ_WRITE { return ptr :: null () ; } if ! errnum . is_null () { unsafe { * errnum = state . err } ; } if state . err == Z_MEM_ERROR { b"out of memory\0" . as_ptr () . cast :: < c_char > () } else if state . msg . is_null () { b"\0" . as_ptr () . cast :: < c_char > () } else { state . msg } }
};
}
