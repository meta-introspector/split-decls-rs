macro_rules! macro_251 {
    () => {
        rustc_index :: newtype_index ! { # [doc = " A (kinda) newtype of `RegionVid` so we can implement `Atom` on it."] # [orderable] # [debug_format = "'?{}"] pub struct PoloniusRegionVid { } }
    };
}

macro_251!();