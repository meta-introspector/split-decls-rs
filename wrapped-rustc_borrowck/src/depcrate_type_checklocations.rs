// Generated macro for Locations (enum)
macro_rules! Depcrate_type_checkLocations {
() => {
// Module: crate::type_check
// Provides: {"Locations"}
// Dependencies: {}
# [doc = " The `Locations` type summarizes *where* region constraints are"] # [doc = " required to hold. Normally, this is at a particular point which"] # [doc = " created the obligation, but for constraints that the user gave, we"] # [doc = " want the constraint to hold at all points."] # [derive (Copy , Clone , PartialEq , Eq , PartialOrd , Ord , Hash , Debug)] pub enum Locations { # [doc = " Indicates that a type constraint should always be true. This"] # [doc = " is particularly important in the new borrowck analysis for"] # [doc = " things like the type of the return slot. Consider this"] # [doc = " example:"] # [doc = ""] # [doc = " ```compile_fail,E0515"] # [doc = " fn foo<'a>(x: &'a u32) -> &'a u32 {"] # [doc = "     let y = 22;"] # [doc = "     return &y; // error"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Here, we wind up with the signature from the return type being"] # [doc = " something like `&'1 u32` where `'1` is a universal region. But"] # [doc = " the type of the return slot `_0` is something like `&'2 u32`"] # [doc = " where `'2` is an existential region variable. The type checker"] # [doc = " requires that `&'2 u32 = &'1 u32` -- but at what point? In the"] # [doc = " older NLL analysis, we required this only at the entry point"] # [doc = " to the function. By the nature of the constraints, this wound"] # [doc = " up propagating to all points reachable from start (because"] # [doc = " `'1` -- as a universal region -- is live everywhere). In the"] # [doc = " newer analysis, though, this doesn't work: `_0` is considered"] # [doc = " dead at the start (it has no usable value) and hence this type"] # [doc = " equality is basically a no-op. Then, later on, when we do `_0"] # [doc = " = &'3 y`, that region `'3` never winds up related to the"] # [doc = " universal region `'1` and hence no error occurs. Therefore, we"] # [doc = " use Locations::All instead, which ensures that the `'1` and"] # [doc = " `'2` are equal everything. We also use this for other"] # [doc = " user-given type annotations; e.g., if the user wrote `let mut"] # [doc = " x: &'static u32 = ...`, we would ensure that all values"] # [doc = " assigned to `x` are of `'static` lifetime."] # [doc = ""] # [doc = " The span points to the place the constraint arose. For example,"] # [doc = " it points to the type in a user-given type annotation. If"] # [doc = " there's no sensible span then it's DUMMY_SP."] All (Span) , # [doc = " An outlives constraint that only has to hold at a single location,"] # [doc = " usually it represents a point where references flow from one spot to"] # [doc = " another (e.g., `x = y`)"] Single (Location) , }
};
}
