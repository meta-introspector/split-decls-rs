// Generated macro for RegionClassification (enum)
macro_rules! Depcrate_universal_regionsRegionClassification {
() => {
// Module: crate::universal_regions
// Provides: {"RegionClassification"}
// Dependencies: {}
# [derive (Debug , PartialEq)] pub (crate) enum RegionClassification { # [doc = " A **global** region is one that can be named from"] # [doc = " anywhere. There is only one, `'static`."] Global , # [doc = " An **external** region is only relevant for"] # [doc = " closures, coroutines, and inline consts. In that"] # [doc = " case, it refers to regions that are free in the type"] # [doc = " -- basically, something bound in the surrounding context."] # [doc = ""] # [doc = " Consider this example:"] # [doc = ""] # [doc = " ```ignore (pseudo-rust)"] # [doc = " fn foo<'a, 'b>(a: &'a u32, b: &'b u32, c: &'static u32) {"] # [doc = "   let closure = for<'x> |x: &'x u32| { .. };"] # [doc = "    //           ^^^^^^^ pretend this were legal syntax"] # [doc = "    //                   for declaring a late-bound region in"] # [doc = "    //                   a closure signature"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Here, the lifetimes `'a` and `'b` would be **external** to the"] # [doc = " closure."] # [doc = ""] # [doc = " If we are not analyzing a closure/coroutine/inline-const,"] # [doc = " there are no external lifetimes."] External , # [doc = " A **local** lifetime is one about which we know the full set"] # [doc = " of relevant constraints (that is, relationships to other named"] # [doc = " regions). For a closure, this includes any region bound in"] # [doc = " the closure's signature. For a fn item, this includes all"] # [doc = " regions other than global ones."] # [doc = ""] # [doc = " Continuing with the example from `External`, if we were"] # [doc = " analyzing the closure, then `'x` would be local (and `'a` and"] # [doc = " `'b` are external). If we are analyzing the function item"] # [doc = " `foo`, then `'a` and `'b` are local (and `'x` is not in"] # [doc = " scope)."] Local , }
};
}
