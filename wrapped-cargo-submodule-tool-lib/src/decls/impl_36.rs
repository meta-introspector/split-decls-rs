macro_rules! deps {
    () => {
        RealCargoTomlProcessor!();
    };
}

macro_rules! impl_36 {
    () => {
        deps!();
        # [cfg (feature = "toml_edit_enabled")] impl CargoTomlProcessor for RealCargoTomlProcessor { fn process_cargo_toml (& self , path : & Path , discovered_repos : & mut HashSet < RepoState > , all_vendored_crate_names : & mut HashSet < String > , target_org : & str , target_branch : & str ,) -> std :: result :: Result < () , String > { let content = fs :: read_to_string (path) . map_err (| e | format ! ("Failed to read Cargo.toml at {:?}: {}" , path , e)) ? ; let doc = content . parse :: < Document < String > > () . map_err (| e | format ! ("Failed to parse Cargo.toml at {:?}: {}" , path , e)) ? ; if let Some (package_name) = doc . get ("package") . and_then (| item | item . as_table ()) . and_then (| table | table . get ("name")) . and_then (| item | item . as_str ()) { all_vendored_crate_names . insert (package_name . to_string ()) ; } if let Some (Item :: Value (toml_edit :: Value :: String (s))) = doc . get ("package") . and_then (| item | item . as_table ()) . and_then (| table | table . get ("repository")) { let repo_url = s . value () . to_string () ; if repo_url . contains ("github.com") { let cleaned_url = self . clean_repo_url (& repo_url) ; if let Some ((owner , repo_name)) = self . extract_owner_repo_name (& cleaned_url) { discovered_repos . insert (RepoState { repo_url : cleaned_url , owner , repo_name , target_org : target_org . to_string () , target_branch : target_branch . to_string () , .. Default :: default () }) ; } else { eprintln ! ("WARNING: Could not extract owner or repository name from: {}. Skipping." , cleaned_url) ; } } } if let Some (workspace_table) = doc . get ("workspace") . and_then (| item | item . as_table ()) { if let Some (members_array) = workspace_table . get ("members") . and_then (| item | item . as_array ()) { for member_item in members_array . iter () { if let Some (member_path_str) = member_item . as_str () { let member_path = path . parent () . unwrap () . join (member_path_str) . join ("Cargo.toml") ; if member_path . exists () { if let Ok (member_content) = fs :: read_to_string (& member_path) { if let Ok (member_doc) = member_content . parse :: < Document < String > > () { if let Some (member_package_name) = member_doc . get ("package") . and_then (| item | item . as_table ()) . and_then (| table | table . get ("name")) . and_then (| item | item . as_str ()) { all_vendored_crate_names . insert (member_package_name . to_string ()) ; } } } } } } } } let sections = ["dependencies" , "dev-dependencies" , "build-dependencies"] ; for section_name in sections { if let Some (deps_table) = doc . get (section_name) . and_then (| item | item . as_table ()) { for (_key , item) in deps_table . iter () { if let Some (dep_table) = item . as_table () { if let Some (path_item) = dep_table . get ("path") { if let Some (path_str) = path_item . as_str () { let dep_cargo_toml_path = path . parent () . unwrap () . join (path_str) . join ("Cargo.toml") ; if dep_cargo_toml_path . exists () { if let Ok (dep_content) = fs :: read_to_string (& dep_cargo_toml_path) { if let Ok (dep_doc) = dep_content . parse :: < Document < String > > () { if let Some (dep_package_name) = dep_doc . get ("package") . and_then (| item | item . as_table ()) . and_then (| table | table . get ("name")) . and_then (| item | item . as_str ()) { all_vendored_crate_names . insert (dep_package_name . to_string ()) ; } } } } } } } } } } Ok (()) } }
    };
}

impl_36!();