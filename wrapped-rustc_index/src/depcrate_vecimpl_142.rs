// Generated macro for impl_142 (impl)
macro_rules! Depcrate_vecimpl_142 {
() => {
// Module: crate::vec
// Provides: {"impl_142"}
// Dependencies: {}
impl < I : Idx , T > IndexVec < I , T > { # [doc = " Constructs a new, empty `IndexVec<I, T>`."] # [inline] pub const fn new () -> Self { IndexVec :: from_raw (Vec :: new ()) } # [doc = " Constructs a new `IndexVec<I, T>` from a `Vec<T>`."] # [inline] pub const fn from_raw (raw : Vec < T >) -> Self { IndexVec { raw , _marker : PhantomData } } # [inline] pub fn with_capacity (capacity : usize) -> Self { IndexVec :: from_raw (Vec :: with_capacity (capacity)) } # [doc = " Creates a new vector with a copy of `elem` for each index in `universe`."] # [doc = ""] # [doc = " Thus `IndexVec::from_elem(elem, &universe)` is equivalent to"] # [doc = " `IndexVec::<I, _>::from_elem_n(elem, universe.len())`. That can help"] # [doc = " type inference as it ensures that the resulting vector uses the same"] # [doc = " index type as `universe`, rather than something potentially surprising."] # [doc = ""] # [doc = " For example, if you want to store data for each local in a MIR body,"] # [doc = " using `let mut uses = IndexVec::from_elem(vec![], &body.local_decls);`"] # [doc = " ensures that `uses` is an `IndexVec<Local, _>`, and thus can give"] # [doc = " better error messages later if one accidentally mismatches indices."] # [inline] pub fn from_elem < S > (elem : T , universe : & IndexSlice < I , S >) -> Self where T : Clone , { IndexVec :: from_raw (vec ! [elem ; universe . len ()]) } # [doc = " Creates a new IndexVec with n copies of the `elem`."] # [inline] pub fn from_elem_n (elem : T , n : usize) -> Self where T : Clone , { IndexVec :: from_raw (vec ! [elem ; n]) } # [doc = " Create an `IndexVec` with `n` elements, where the value of each"] # [doc = " element is the result of `func(i)`. (The underlying vector will"] # [doc = " be allocated only once, with a capacity of at least `n`.)"] # [inline] pub fn from_fn_n (func : impl FnMut (I) -> T , n : usize) -> Self { let _ = I :: new (n) ; IndexVec :: from_raw ((0 .. n) . map (I :: new) . map (func) . collect ()) } # [inline] pub fn as_slice (& self) -> & IndexSlice < I , T > { IndexSlice :: from_raw (& self . raw) } # [inline] pub fn as_mut_slice (& mut self) -> & mut IndexSlice < I , T > { IndexSlice :: from_raw_mut (& mut self . raw) } # [doc = " Pushes an element to the array returning the index where it was pushed to."] # [inline] pub fn push (& mut self , d : T) -> I { let idx = self . next_index () ; self . raw . push (d) ; idx } # [inline] pub fn pop (& mut self) -> Option < T > { self . raw . pop () } # [inline] pub fn into_iter (self) -> vec :: IntoIter < T > { self . raw . into_iter () } # [inline] pub fn into_iter_enumerated (self ,) -> impl DoubleEndedIterator < Item = (I , T) > + ExactSizeIterator { let _ = I :: new (self . len ()) ; self . raw . into_iter () . enumerate () . map (| (n , t) | (I :: new (n) , t)) } # [inline] pub fn drain < R : RangeBounds < usize > > (& mut self , range : R) -> impl Iterator < Item = T > { self . raw . drain (range) } # [inline] pub fn drain_enumerated < R : RangeBounds < usize > > (& mut self , range : R ,) -> impl Iterator < Item = (I , T) > { let begin = match range . start_bound () { std :: ops :: Bound :: Included (i) => * i , std :: ops :: Bound :: Excluded (i) => i . checked_add (1) . unwrap () , std :: ops :: Bound :: Unbounded => 0 , } ; self . raw . drain (range) . enumerate () . map (move | (n , t) | (I :: new (begin + n) , t)) } # [inline] pub fn shrink_to_fit (& mut self) { self . raw . shrink_to_fit () } # [inline] pub fn truncate (& mut self , a : usize) { self . raw . truncate (a) } # [doc = " Grows the index vector so that it contains an entry for"] # [doc = " `elem`; if that is already true, then has no"] # [doc = " effect. Otherwise, inserts new values as needed by invoking"] # [doc = " `fill_value`."] # [doc = ""] # [doc = " Returns a reference to the `elem` entry."] # [inline] pub fn ensure_contains_elem (& mut self , elem : I , fill_value : impl FnMut () -> T) -> & mut T { let min_new_len = elem . index () + 1 ; if self . len () < min_new_len { self . raw . resize_with (min_new_len , fill_value) ; } & mut self [elem] } # [inline] pub fn resize (& mut self , new_len : usize , value : T) where T : Clone , { self . raw . resize (new_len , value) } # [inline] pub fn resize_to_elem (& mut self , elem : I , fill_value : impl FnMut () -> T) { let min_new_len = elem . index () + 1 ; self . raw . resize_with (min_new_len , fill_value) ; } # [inline] pub fn append (& mut self , other : & mut Self) { self . raw . append (& mut other . raw) ; } }
};
}
