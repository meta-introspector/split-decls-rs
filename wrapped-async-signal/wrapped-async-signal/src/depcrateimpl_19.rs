// Generated macro for impl_19 (impl)
macro_rules! Depcrateimpl_19 {
() => {
// Module: crate
// Provides: {"impl_19"}
// Dependencies: {}
impl Signals { # [doc = " Create a new `Signals` instance with a set of signals."] pub fn new < B > (signals : impl IntoIterator < Item = B >) -> io :: Result < Self > where B : Borrow < Signal > , { let mut this = Self { notifier : sys :: Notifier :: new () ? , signal_ids : HashMap :: new () , } ; this . add_signals (signals) ? ; Ok (this) } # [doc = " Add signals to the set of signals to wait for."] # [doc = ""] # [doc = " One signal cannot be added twice. If a signal that has already been added is passed to this"] # [doc = " method, it will be ignored."] # [doc = ""] # [doc = " Registering a signal prevents the default behavior of that signal from occurring. For"] # [doc = " example, if you register `SIGINT`, pressing `Ctrl+C` will no longer terminate the process."] # [doc = " To run the default signal handler, use [`signal_hook::low_level::emulate_default_handler`]"] # [doc = " instead."] # [doc = ""] # [doc = " [`signal_hook::low_level::emulate_default_handler`]: https://docs.rs/signal-hook/latest/signal_hook/low_level/fn.emulate_default_handler.html"] pub fn add_signals < B > (& mut self , signals : impl IntoIterator < Item = B >) -> io :: Result < () > where B : Borrow < Signal > , { for signal in signals { let signal = signal . borrow () ; if self . signal_ids . contains_key (signal) { continue ; } let closure = self . notifier . add_signal (* signal) ? ; let id = unsafe { registry :: register (signal . number () , closure) ? } ; self . signal_ids . insert (* signal , id) ; } Ok (()) } # [doc = " Remove signals from the set of signals to wait for."] # [doc = ""] # [doc = " This function can be used to opt out of listening to signals previously registered via"] # [doc = " [`add_signals`](Self::add_signals) or [`new`](Self::new). If a signal that has not been"] # [doc = " registered is passed to this method, it will be ignored."] pub fn remove_signals < B > (& mut self , signals : impl IntoIterator < Item = B >) -> io :: Result < () > where B : Borrow < Signal > , { for signal in signals { let signal = signal . borrow () ; let id = match self . signal_ids . remove (signal) { Some (id) => id , None => continue , } ; self . notifier . remove_signal (* signal) ? ; registry :: unregister (id) ; } Ok (()) } }
};
}
