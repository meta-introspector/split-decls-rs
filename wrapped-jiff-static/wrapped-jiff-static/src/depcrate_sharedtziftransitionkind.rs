// Generated macro for TzifTransitionKind (enum)
macro_rules! Depcrate_sharedTzifTransitionKind {
() => {
// Module: crate::shared
// Provides: {"TzifTransitionKind"}
// Dependencies: {}
# [doc = " The kind of a transition."] # [doc = ""] # [doc = " This is used when trying to determine the offset for a local datetime. It"] # [doc = " indicates how the corresponding civil datetimes in `civil_starts` and"] # [doc = " `civil_ends` should be interpreted. That is, there are three possible"] # [doc = " cases:"] # [doc = ""] # [doc = " 1. The offset of this transition is equivalent to the offset of the"] # [doc = " previous transition. That means there are no ambiguous civil datetimes"] # [doc = " between the transitions. This can occur, e.g., when the time zone"] # [doc = " abbreviation changes."] # [doc = " 2. The offset of the transition is greater than the offset of the previous"] # [doc = " transition. That means there is a \"gap\" in local time between the"] # [doc = " transitions. This typically corresponds to entering daylight saving time."] # [doc = " It is usually, but not always, 1 hour."] # [doc = " 3. The offset of the transition is less than the offset of the previous"] # [doc = " transition. That means there is a \"fold\" in local time where time is"] # [doc = " repeated. This typically corresponds to leaving daylight saving time. It"] # [doc = " is usually, but not always, 1 hour."] # [doc = ""] # [doc = " # More explanation"] # [doc = ""] # [doc = " This, when combined with `civil_starts` and `civil_ends` in"] # [doc = " `TzifTransitions`, explicitly represents ambiguous wall clock times that"] # [doc = " occur at the boundaries of transitions."] # [doc = ""] # [doc = " The start of the wall clock time is always the earlier possible wall clock"] # [doc = " time that could occur with this transition's corresponding offset. For a"] # [doc = " gap, it's the previous transition's offset. For a fold, it's the current"] # [doc = " transition's offset."] # [doc = ""] # [doc = " For example, DST for `America/New_York` began on `2024-03-10T07:00:00+00`."] # [doc = " The offset prior to this instant in time is `-05`, corresponding"] # [doc = " to standard time (EST). Thus, in wall clock time, DST began at"] # [doc = " `2024-03-10T02:00:00`. And since this is a DST transition that jumps ahead"] # [doc = " an hour, the start of DST also corresponds to the start of a gap. That is,"] # [doc = " the times `02:00:00` through `02:59:59` never appear on a clock for this"] # [doc = " hour. The question is thus: which offset should we apply to `02:00:00`?"] # [doc = " We could apply the offset from the earlier transition `-05` and get"] # [doc = " `2024-03-10T01:00:00-05` (that's `2024-03-10T06:00:00+00`), or we could"] # [doc = " apply the offset from the later transition `-04` and get"] # [doc = " `2024-03-10T03:00:00-04` (that's `2024-03-10T07:00:00+00`)."] # [doc = ""] # [doc = " So in the above, we would have a `Gap` variant where `start` (inclusive) is"] # [doc = " `2024-03-10T02:00:00` and `end` (exclusive) is `2024-03-10T03:00:00`."] # [doc = ""] # [doc = " The fold case is the same idea, but where the same time is repeated."] # [doc = " For example, in `America/New_York`, standard time began on"] # [doc = " `2024-11-03T06:00:00+00`. The offset prior to this instant in time"] # [doc = " is `-04`, corresponding to DST (EDT). Thus, in wall clock time, DST"] # [doc = " ended at `2024-11-03T02:00:00`. However, since this is a fold, the"] # [doc = " actual set of ambiguous times begins at `2024-11-03T01:00:00` and"] # [doc = " ends at `2024-11-03T01:59:59.999999999`. That is, the wall clock time"] # [doc = " `2024-11-03T02:00:00` is unambiguous."] # [doc = ""] # [doc = " So in the fold case above, we would have a `Fold` variant where"] # [doc = " `start` (inclusive) is `2024-11-03T01:00:00` and `end` (exclusive) is"] # [doc = " `2024-11-03T02:00:00`."] # [doc = ""] # [doc = " Since this gets bundled in with the sorted sequence of transitions, we'll"] # [doc = " use the \"start\" time in all three cases as our target of binary search."] # [doc = " Once we land on a transition, we'll know our given wall clock time is"] # [doc = " greater than or equal to its start wall clock time. At that point, to"] # [doc = " determine if there is ambiguity, we merely need to determine if the given"] # [doc = " wall clock time is less than the corresponding `end` time. If it is, then"] # [doc = " it falls in a gap or fold. Otherwise, it's unambiguous."] # [doc = ""] # [doc = " Note that we could compute these datetime values while searching for the"] # [doc = " correct transition, but there's a fair bit of math involved in going"] # [doc = " between timestamps (which is what TZif gives us) and calendar datetimes"] # [doc = " (which is what we're given as input). It is also necessary that we offset"] # [doc = " the timestamp given in TZif at some point, since it is in UTC and the"] # [doc = " datetime given is in wall clock time. So I decided it would be worth"] # [doc = " pre-computing what we need in terms of what the input is. This way, we"] # [doc = " don't need to do any conversions, or indeed, any arithmetic at all, for"] # [doc = " time zone lookups. We *could* store these as transitions, but then the"] # [doc = " input datetime would need to be converted to a timestamp before searching"] # [doc = " the transitions."] # [derive (Clone , Copy , Debug)] pub enum TzifTransitionKind { # [doc = " This transition cannot possibly lead to an unambiguous offset because"] # [doc = " its offset is equivalent to the offset of the previous transition."] # [doc = ""] # [doc = " Has an entry in `civil_starts`, but corresponding entry in `civil_ends`"] # [doc = " is always zeroes (i.e., meaningless)."] Unambiguous , # [doc = " This occurs when this transition's offset is strictly greater than the"] # [doc = " previous transition's offset. This effectively results in a \"gap\" of"] # [doc = " time equal to the difference in the offsets between the two"] # [doc = " transitions."] # [doc = ""] # [doc = " Has an entry in `civil_starts` for when the gap starts (inclusive) in"] # [doc = " local time. Also has an entry in `civil_ends` for when the fold ends"] # [doc = " (exclusive) in local time."] Gap , # [doc = " This occurs when this transition's offset is strictly less than the"] # [doc = " previous transition's offset. This results in a \"fold\" of time where"] # [doc = " the two transitions have an overlap where it is ambiguous which one"] # [doc = " applies given a wall clock time. In effect, a span of time equal to the"] # [doc = " difference in the offsets is repeated."] # [doc = ""] # [doc = " Has an entry in `civil_starts` for when the fold starts (inclusive) in"] # [doc = " local time. Also has an entry in `civil_ends` for when the fold ends"] # [doc = " (exclusive) in local time."] Fold , }
};
}
