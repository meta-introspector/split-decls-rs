// Generated macro for shared (module)
macro_rules! Depcrateshared {
() => {
// Module: crate
// Provides: {"shared"}
// Dependencies: {}
# [doc = " A bundle of code copied from `src/shared`."] # [doc = ""] # [doc = " The main thing we use in here is the parsing routine for TZif data and"] # [doc = " shared data types for representing TZif data."] # [doc = ""] # [doc = " We also squash dead code warnings. This is somewhat precarious since"] # [doc = " ideally we wouldn't compile what we don't need. But in practice, it's"] # [doc = " annoying to get rid of everything we don't need in this context, and it"] # [doc = " should be pretty small anyway."] # [allow (dead_code)] mod shared ;
};
}
