// Generated macro for impl_53 (impl)
macro_rules! Depcrate_shared_posiximpl_53 {
() => {
// Module: crate::shared::posix
// Provides: {"impl_53"}
// Dependencies: {}
impl < 's > Parser < 's > { # [doc = " Create a new parser for extracting a POSIX time zone from the given"] # [doc = " bytes."] fn new < B : ? Sized + AsRef < [u8] > > (tz : & 's B) -> Parser < 's > { Parser { tz : tz . as_ref () , pos : core :: cell :: Cell :: new (0) , ianav3plus : false , } } # [doc = " Parses a POSIX time zone from the current position of the parser and"] # [doc = " ensures that the entire TZ string corresponds to a single valid POSIX"] # [doc = " time zone."] fn parse (& self) -> Result < PosixTimeZone < Abbreviation > , Error > { let (time_zone , remaining) = self . parse_prefix () ? ; if ! remaining . is_empty () { return Err (err ! ("expected entire TZ string to be a valid POSIX \
                 time zone, but found `{}` after what would otherwise \
                 be a valid POSIX TZ string" , Bytes (remaining) ,)) ; } Ok (time_zone) } # [doc = " Parses a POSIX time zone from the current position of the parser and"] # [doc = " returns the remaining input."] fn parse_prefix (& self ,) -> Result < (PosixTimeZone < Abbreviation > , & 's [u8]) , Error > { let time_zone = self . parse_posix_time_zone () ? ; Ok ((time_zone , self . remaining ())) } # [doc = " Parse a POSIX time zone from the current position of the parser."] # [doc = ""] # [doc = " Upon success, the parser will be positioned immediately following the"] # [doc = " TZ string."] fn parse_posix_time_zone (& self ,) -> Result < PosixTimeZone < Abbreviation > , Error > { if self . is_done () { return Err (err ! ("an empty string is not a valid POSIX time zone")) ; } let std_abbrev = self . parse_abbreviation () . map_err (| e | err ! ("failed to parse standard abbreviation: {e}")) ? ; let std_offset = self . parse_posix_offset () . map_err (| e | err ! ("failed to parse standard offset: {e}")) ? ; let mut dst = None ; if ! self . is_done () && (self . byte () . is_ascii_alphabetic () || self . byte () == b'<') { dst = Some (self . parse_posix_dst (& std_offset) ?) ; } Ok (PosixTimeZone { std_abbrev , std_offset , dst }) } # [doc = " Parse a DST zone with an optional explicit transition rule."] # [doc = ""] # [doc = " This assumes the parser is positioned at the first byte of the DST"] # [doc = " abbreviation."] # [doc = ""] # [doc = " Upon success, the parser will be positioned immediately after the end"] # [doc = " of the DST transition rule (which might just be the abbreviation, but"] # [doc = " might also include explicit start/end datetime specifications)."] fn parse_posix_dst (& self , std_offset : & PosixOffset ,) -> Result < PosixDst < Abbreviation > , Error > { let abbrev = self . parse_abbreviation () . map_err (| e | err ! ("failed to parse DST abbreviation: {e}")) ? ; if self . is_done () { return Err (err ! ("found DST abbreviation `{abbrev}`, but no transition \
                 rule (this is technically allowed by POSIX, but has \
                 unspecified behavior)" ,)) ; } let mut offset = PosixOffset { second : std_offset . second + 3600 } ; if self . byte () != b',' { offset = self . parse_posix_offset () . map_err (| e | err ! ("failed to parse DST offset: {e}")) ? ; if self . is_done () { return Err (err ! ("found DST abbreviation `{abbrev}` and offset \
                     `{offset}s`, but no transition rule (this is \
                     technically allowed by POSIX, but has \
                     unspecified behavior)" , offset = offset . second ,)) ; } } if self . byte () != b',' { return Err (err ! ("after parsing DST offset in POSIX time zone string, \
                 found `{}` but expected a ','" , Byte (self . byte ()) ,)) ; } if ! self . bump () { return Err (err ! ("after parsing DST offset in POSIX time zone string, \
                 found end of string after a trailing ','" ,)) ; } let rule = self . parse_rule () ? ; Ok (PosixDst { abbrev , offset , rule }) } # [doc = " Parse a time zone abbreviation."] # [doc = ""] # [doc = " This assumes the parser is positioned at the first byte of"] # [doc = " the abbreviation. This is either the first character in the"] # [doc = " abbreviation, or the opening quote of a quoted abbreviation."] # [doc = ""] # [doc = " Upon success, the parser will be positioned immediately following"] # [doc = " the abbreviation name."] # [doc = ""] # [doc = " The string returned is guaranteed to be no more than 30 bytes."] # [doc = " (This restriction is somewhat arbitrary, but it's so we can put"] # [doc = " the abbreviation in a fixed capacity array.)"] fn parse_abbreviation (& self) -> Result < Abbreviation , Error > { if self . byte () == b'<' { if ! self . bump () { return Err (err ! ("found opening '<' quote for abbreviation in \
                         POSIX time zone string, and expected a name \
                         following it, but found the end of string instead")) ; } self . parse_quoted_abbreviation () } else { self . parse_unquoted_abbreviation () } } # [doc = " Parses an unquoted time zone abbreviation."] # [doc = ""] # [doc = " This assumes the parser is position at the first byte in the"] # [doc = " abbreviation."] # [doc = ""] # [doc = " Upon success, the parser will be positioned immediately after the"] # [doc = " last byte in the abbreviation."] # [doc = ""] # [doc = " The string returned is guaranteed to be no more than 30 bytes."] # [doc = " (This restriction is somewhat arbitrary, but it's so we can put"] # [doc = " the abbreviation in a fixed capacity array.)"] fn parse_unquoted_abbreviation (& self) -> Result < Abbreviation , Error > { let start = self . pos () ; for i in 0 .. { if ! self . byte () . is_ascii_alphabetic () { break ; } if i >= Abbreviation :: capacity () { return Err (err ! ("expected abbreviation with at most {} bytes, \
                     but found a longer abbreviation beginning with `{}`" , Abbreviation :: capacity () , Bytes (& self . tz [start ..] [.. i]) ,)) ; } if ! self . bump () { break ; } } let end = self . pos () ; let abbrev = core :: str :: from_utf8 (& self . tz [start .. end]) . map_err (| _ | { err ! ("found abbreviation `{}`, but it is not valid UTF-8" , Bytes (& self . tz [start .. end]) ,) }) ? ; if abbrev . len () < 3 { return Err (err ! ("expected abbreviation with 3 or more bytes, but found \
                 abbreviation {:?} with {} bytes" , abbrev , abbrev . len () ,)) ; } Ok (Abbreviation :: new (abbrev) . unwrap ()) } # [doc = " Parses a quoted time zone abbreviation."] # [doc = ""] # [doc = " This assumes the parser is positioned immediately after the opening"] # [doc = " `<` quote. That is, at the first byte in the abbreviation."] # [doc = ""] # [doc = " Upon success, the parser will be positioned immediately after the"] # [doc = " closing `>` quote."] # [doc = ""] # [doc = " The string returned is guaranteed to be no more than 30 bytes."] # [doc = " (This restriction is somewhat arbitrary, but it's so we can put"] # [doc = " the abbreviation in a fixed capacity array.)"] fn parse_quoted_abbreviation (& self) -> Result < Abbreviation , Error > { let start = self . pos () ; for i in 0 .. { if ! self . byte () . is_ascii_alphanumeric () && self . byte () != b'+' && self . byte () != b'-' { break ; } if i >= Abbreviation :: capacity () { return Err (err ! ("expected abbreviation with at most {} bytes, \
                     but found a longer abbreviation beginning with `{}`" , Abbreviation :: capacity () , Bytes (& self . tz [start ..] [.. i]) ,)) ; } if ! self . bump () { break ; } } let end = self . pos () ; let abbrev = core :: str :: from_utf8 (& self . tz [start .. end]) . map_err (| _ | { err ! ("found abbreviation `{}`, but it is not valid UTF-8" , Bytes (& self . tz [start .. end]) ,) }) ? ; if self . is_done () { return Err (err ! ("found non-empty quoted abbreviation {abbrev:?}, but \
                     did not find expected end-of-quoted abbreviation \
                     '>' character" ,)) ; } if self . byte () != b'>' { return Err (err ! ("found non-empty quoted abbreviation {abbrev:?}, but \
                     found `{}` instead of end-of-quoted abbreviation '>' \
                     character" , Byte (self . byte ()) ,)) ; } self . bump () ; if abbrev . len () < 3 { return Err (err ! ("expected abbreviation with 3 or more bytes, but found \
                     abbreviation {abbrev:?} with {} bytes" , abbrev . len () ,)) ; } Ok (Abbreviation :: new (abbrev) . unwrap ()) } # [doc = " Parse a POSIX time offset."] # [doc = ""] # [doc = " This assumes the parser is positioned at the first byte of the"] # [doc = " offset. This can either be a digit (for a positive offset) or the"] # [doc = " sign of the offset (which must be either `-` or `+`)."] # [doc = ""] # [doc = " Upon success, the parser will be positioned immediately after the"] # [doc = " end of the offset."] fn parse_posix_offset (& self) -> Result < PosixOffset , Error > { let sign = self . parse_optional_sign () . map_err (| e | { err ! ("failed to parse sign for time offset \
                     in POSIX time zone string: {e}" ,) }) ? . unwrap_or (1) ; let hour = self . parse_hour_posix () ? ; let (mut minute , mut second) = (0 , 0) ; if self . maybe_byte () == Some (b':') { if ! self . bump () { return Err (err ! ("incomplete time in POSIX timezone (missing minutes)" ,)) ; } minute = self . parse_minute () ? ; if self . maybe_byte () == Some (b':') { if ! self . bump () { return Err (err ! ("incomplete time in POSIX timezone (missing seconds)" ,)) ; } second = self . parse_second () ? ; } } let mut offset = PosixOffset { second : i32 :: from (hour) * 3600 } ; offset . second += i32 :: from (minute) * 60 ; offset . second += i32 :: from (second) ; offset . second *= i32 :: from (- sign) ; assert ! (- 89999 <= offset . second && offset . second <= 89999 , "POSIX offset seconds {} is out of range" , offset . second) ; Ok (offset) } # [doc = " Parses a POSIX DST transition rule."] # [doc = ""] # [doc = " This assumes the parser is positioned at the first byte in the"] # [doc = " rule. That is, it comes immediately after the DST abbreviation or"] # [doc = " its optional offset."] # [doc = ""] # [doc = " Upon success, the parser will be positioned immediately after the"] # [doc = " DST transition rule. In typical cases, this corresponds to the end"] # [doc = " of the TZ string."] fn parse_rule (& self) -> Result < PosixRule , Error > { let start = self . parse_posix_datetime () . map_err (| e | { err ! ("failed to parse start of DST transition rule: {e}") }) ? ; if self . maybe_byte () != Some (b',') || ! self . bump () { return Err (err ! ("expected end of DST rule after parsing the start \
                 of the DST rule")) ; } let end = self . parse_posix_datetime () . map_err (| e | { err ! ("failed to parse end of DST transition rule: {e}") }) ? ; Ok (PosixRule { start , end }) } # [doc = " Parses a POSIX datetime specification."] # [doc = ""] # [doc = " This assumes the parser is position at the first byte where a"] # [doc = " datetime specification is expected to occur."] # [doc = ""] # [doc = " Upon success, the parser will be positioned after the datetime"] # [doc = " specification. This will either be immediately after the date, or"] # [doc = " if it's present, the time part of the specification."] fn parse_posix_datetime (& self) -> Result < PosixDayTime , Error > { let mut daytime = PosixDayTime { date : self . parse_posix_date () ? , time : PosixTime :: DEFAULT , } ; if self . maybe_byte () != Some (b'/') { return Ok (daytime) ; } if ! self . bump () { return Err (err ! ("expected time specification after '/' following a date
                 specification in a POSIX time zone DST transition rule" ,)) ; } daytime . time = self . parse_posix_time () ? ; Ok (daytime) } # [doc = " Parses a POSIX date specification."] # [doc = ""] # [doc = " This assumes the parser is positioned at the first byte of the date"] # [doc = " specification. This can be `J` (for one based Julian day without"] # [doc = " leap days), `M` (for \"weekday of month\") or a digit starting the"] # [doc = " zero based Julian day with leap days. This routine will validate"] # [doc = " that the position points to one of these possible values. That is,"] # [doc = " the caller doesn't need to parse the `M` or the `J` or the leading"] # [doc = " digit. The caller should just call this routine when it *expect* a"] # [doc = " date specification to follow."] # [doc = ""] # [doc = " Upon success, the parser will be positioned immediately after the"] # [doc = " date specification."] fn parse_posix_date (& self) -> Result < PosixDay , Error > { match self . byte () { b'J' => { if ! self . bump () { return Err (err ! ("expected one-based Julian day after 'J' in date \
                         specification of a POSIX time zone DST \
                         transition rule, but got the end of the string \
                         instead")) ; } Ok (PosixDay :: JulianOne (self . parse_posix_julian_day_no_leap () ?)) } b'0' ..= b'9' => Ok (PosixDay :: JulianZero (self . parse_posix_julian_day_with_leap () ? ,)) , b'M' => { if ! self . bump () { return Err (err ! ("expected month-week-weekday after 'M' in date \
                         specification of a POSIX time zone DST \
                         transition rule, but got the end of the string \
                         instead")) ; } let (month , week , weekday) = self . parse_weekday_of_month () ? ; Ok (PosixDay :: WeekdayOfMonth { month , week , weekday }) } _ => Err (err ! ("expected 'J', a digit or 'M' at the beginning of a date \
                 specification of a POSIX time zone DST transition rule, \
                 but got `{}` instead" , Byte (self . byte ()) ,)) , } } # [doc = " Parses a POSIX Julian day that does not include leap days"] # [doc = " (`1 <= n <= 365`)."] # [doc = ""] # [doc = " This assumes the parser is positioned just after the `J` and at the"] # [doc = " first digit of the Julian day. Upon success, the parser will be"] # [doc = " positioned immediately following the day number."] fn parse_posix_julian_day_no_leap (& self) -> Result < i16 , Error > { let number = self . parse_number_with_upto_n_digits (3) . map_err (| e | err ! ("invalid one based Julian day: {e}")) ? ; let number = i16 :: try_from (number) . map_err (| _ | { err ! ("one based Julian day `{number}` in POSIX time zone \
                 does not fit into 16-bit integer") }) ? ; if ! (1 <= number && number <= 365) { return Err (err ! ("parsed one based Julian day `{number}`, \
                 but one based Julian day in POSIX time zone \
                 must be in range 1..=365" ,)) ; } Ok (number) } # [doc = " Parses a POSIX Julian day that includes leap days (`0 <= n <="] # [doc = " 365`)."] # [doc = ""] # [doc = " This assumes the parser is positioned at the first digit of the"] # [doc = " Julian day. Upon success, the parser will be positioned immediately"] # [doc = " following the day number."] fn parse_posix_julian_day_with_leap (& self) -> Result < i16 , Error > { let number = self . parse_number_with_upto_n_digits (3) . map_err (| e | err ! ("invalid zero based Julian day: {e}")) ? ; let number = i16 :: try_from (number) . map_err (| _ | { err ! ("zero based Julian day `{number}` in POSIX time zone \
                 does not fit into 16-bit integer") }) ? ; if ! (0 <= number && number <= 365) { return Err (err ! ("parsed zero based Julian day `{number}`, \
                 but zero based Julian day in POSIX time zone \
                 must be in range 0..=365" ,)) ; } Ok (number) } # [doc = " Parses a POSIX \"weekday of month\" specification."] # [doc = ""] # [doc = " This assumes the parser is positioned just after the `M` byte and"] # [doc = " at the first digit of the month. Upon success, the parser will be"] # [doc = " positioned immediately following the \"weekday of the month\" that"] # [doc = " was parsed."] # [doc = ""] # [doc = " The tuple returned is month (1..=12), week (1..=5) and weekday"] # [doc = " (0..=6 with 0=Sunday)."] fn parse_weekday_of_month (& self) -> Result < (i8 , i8 , i8) , Error > { let month = self . parse_month () ? ; if self . maybe_byte () != Some (b'.') { return Err (err ! ("expected '.' after month `{month}` in \
                 POSIX time zone rule")) ; } if ! self . bump () { return Err (err ! ("expected week after month `{month}` in \
                 POSIX time zone rule")) ; } let week = self . parse_week () ? ; if self . maybe_byte () != Some (b'.') { return Err (err ! ("expected '.' after week `{week}` in POSIX time zone rule")) ; } if ! self . bump () { return Err (err ! ("expected day-of-week after week `{week}` in \
                 POSIX time zone rule")) ; } let weekday = self . parse_weekday () ? ; Ok ((month , week , weekday)) } # [doc = " This parses a POSIX time specification in the format"] # [doc = " `[+/-]hh?[:mm[:ss]]`."] # [doc = ""] # [doc = " This assumes the parser is positioned at the first `h` (or the"] # [doc = " sign, if present). Upon success, the parser will be positioned"] # [doc = " immediately following the end of the time specification."] fn parse_posix_time (& self) -> Result < PosixTime , Error > { let (sign , hour) = if self . ianav3plus { let sign = self . parse_optional_sign () . map_err (| e | { err ! ("failed to parse sign for transition time \
                         in POSIX time zone string: {e}" ,) }) ? . unwrap_or (1) ; let hour = self . parse_hour_ianav3plus () ? ; (sign , hour) } else { (1 , i16 :: from (self . parse_hour_posix () ?)) } ; let (mut minute , mut second) = (0 , 0) ; if self . maybe_byte () == Some (b':') { if ! self . bump () { return Err (err ! ("incomplete transition time in \
                     POSIX time zone string (missing minutes)" ,)) ; } minute = self . parse_minute () ? ; if self . maybe_byte () == Some (b':') { if ! self . bump () { return Err (err ! ("incomplete transition time in \
                         POSIX time zone string (missing seconds)" ,)) ; } second = self . parse_second () ? ; } } let mut time = PosixTime { second : i32 :: from (hour) * 3600 } ; time . second += i32 :: from (minute) * 60 ; time . second += i32 :: from (second) ; time . second *= i32 :: from (sign) ; assert ! (- 604799 <= time . second && time . second <= 604799 , "POSIX time seconds {} is out of range" , time . second) ; Ok (time) } # [doc = " Parses a month."] # [doc = ""] # [doc = " This is expected to be positioned at the first digit. Upon success,"] # [doc = " the parser will be positioned after the month (which may contain"] # [doc = " two digits)."] fn parse_month (& self) -> Result < i8 , Error > { let number = self . parse_number_with_upto_n_digits (2) ? ; let number = i8 :: try_from (number) . map_err (| _ | { err ! ("month `{number}` in POSIX time zone \
                 does not fit into 8-bit integer") }) ? ; if ! (1 <= number && number <= 12) { return Err (err ! ("parsed month `{number}`, but month in \
                 POSIX time zone must be in range 1..=12" ,)) ; } Ok (number) } # [doc = " Parses a week-of-month number."] # [doc = ""] # [doc = " This is expected to be positioned at the first digit. Upon success,"] # [doc = " the parser will be positioned after the week digit."] fn parse_week (& self) -> Result < i8 , Error > { let number = self . parse_number_with_exactly_n_digits (1) ? ; let number = i8 :: try_from (number) . map_err (| _ | { err ! ("week `{number}` in POSIX time zone \
                 does not fit into 8-bit integer") }) ? ; if ! (1 <= number && number <= 5) { return Err (err ! ("parsed week `{number}`, but week in \
                 POSIX time zone must be in range 1..=5")) ; } Ok (number) } # [doc = " Parses a weekday number."] # [doc = ""] # [doc = " This is expected to be positioned at the first digit. Upon success,"] # [doc = " the parser will be positioned after the week digit."] # [doc = ""] # [doc = " The weekday returned is guaranteed to be in the range `0..=6`, with"] # [doc = " `0` corresponding to Sunday."] fn parse_weekday (& self) -> Result < i8 , Error > { let number = self . parse_number_with_exactly_n_digits (1) ? ; let number = i8 :: try_from (number) . map_err (| _ | { err ! ("weekday `{number}` in POSIX time zone \
                 does not fit into 8-bit integer") }) ? ; if ! (0 <= number && number <= 6) { return Err (err ! ("parsed weekday `{number}`, but weekday in \
                 POSIX time zone must be in range `0..=6` \
                 (with `0` corresponding to Sunday)" ,)) ; } Ok (number) } # [doc = " Parses an hour from a POSIX time specification with the IANA"] # [doc = " v3+ extension. That is, the hour may be in the range `0..=167`."] # [doc = " (Callers should parse an optional sign preceding the hour digits"] # [doc = " when IANA V3+ parsing is enabled.)"] # [doc = ""] # [doc = " The hour is allowed to be a single digit (unlike minutes or"] # [doc = " seconds)."] # [doc = ""] # [doc = " This assumes the parser is positioned at the position where the"] # [doc = " first hour digit should occur. Upon success, the parser will be"] # [doc = " positioned immediately after the last hour digit."] fn parse_hour_ianav3plus (& self) -> Result < i16 , Error > { assert ! (self . ianav3plus) ; let number = self . parse_number_with_upto_n_digits (3) . map_err (| e | err ! ("invalid hour digits: {e}")) ? ; let number = i16 :: try_from (number) . map_err (| _ | { err ! ("hour `{number}` in POSIX time zone \
                 does not fit into 16-bit integer") }) ? ; if ! (0 <= number && number <= 167) { return Err (err ! ("parsed hour `{number}`, but hour in IANA v3+ \
                 POSIX time zone must be in range `-167..=167`" ,)) ; } Ok (number) } # [doc = " Parses an hour from a POSIX time specification, with the allowed"] # [doc = " range being `0..=24`."] # [doc = ""] # [doc = " The hour is allowed to be a single digit (unlike minutes or"] # [doc = " seconds)."] # [doc = ""] # [doc = " This assumes the parser is positioned at the position where the"] # [doc = " first hour digit should occur. Upon success, the parser will be"] # [doc = " positioned immediately after the last hour digit."] fn parse_hour_posix (& self) -> Result < i8 , Error > { let number = self . parse_number_with_upto_n_digits (2) . map_err (| e | err ! ("invalid hour digits: {e}")) ? ; let number = i8 :: try_from (number) . map_err (| _ | { err ! ("hour `{number}` in POSIX time zone \
                 does not fit into 8-bit integer") }) ? ; if ! (0 <= number && number <= 24) { return Err (err ! ("parsed hour `{number}`, but hour in \
                 POSIX time zone must be in range `0..=24`" ,)) ; } Ok (number) } # [doc = " Parses a minute from a POSIX time specification."] # [doc = ""] # [doc = " The minute must be exactly two digits."] # [doc = ""] # [doc = " This assumes the parser is positioned at the position where the"] # [doc = " first minute digit should occur. Upon success, the parser will be"] # [doc = " positioned immediately after the second minute digit."] fn parse_minute (& self) -> Result < i8 , Error > { let number = self . parse_number_with_exactly_n_digits (2) . map_err (| e | err ! ("invalid minute digits: {e}")) ? ; let number = i8 :: try_from (number) . map_err (| _ | { err ! ("minute `{number}` in POSIX time zone \
                 does not fit into 8-bit integer") }) ? ; if ! (0 <= number && number <= 59) { return Err (err ! ("parsed minute `{number}`, but minute in \
                 POSIX time zone must be in range `0..=59`" ,)) ; } Ok (number) } # [doc = " Parses a second from a POSIX time specification."] # [doc = ""] # [doc = " The second must be exactly two digits."] # [doc = ""] # [doc = " This assumes the parser is positioned at the position where the"] # [doc = " first second digit should occur. Upon success, the parser will be"] # [doc = " positioned immediately after the second second digit."] fn parse_second (& self) -> Result < i8 , Error > { let number = self . parse_number_with_exactly_n_digits (2) . map_err (| e | err ! ("invalid second digits: {e}")) ? ; let number = i8 :: try_from (number) . map_err (| _ | { err ! ("second `{number}` in POSIX time zone \
                 does not fit into 8-bit integer") }) ? ; if ! (0 <= number && number <= 59) { return Err (err ! ("parsed second `{number}`, but second in \
                 POSIX time zone must be in range `0..=59`" ,)) ; } Ok (number) } # [doc = " Parses a signed 64-bit integer expressed in exactly `n` digits."] # [doc = ""] # [doc = " If `n` digits could not be found (or if the `TZ` string ends before"] # [doc = " `n` digits could be found), then this returns an error."] # [doc = ""] # [doc = " This assumes that `n >= 1` and that the parser is positioned at the"] # [doc = " first digit. Upon success, the parser is positioned immediately"] # [doc = " after the `n`th digit."] fn parse_number_with_exactly_n_digits (& self , n : usize ,) -> Result < i32 , Error > { assert ! (n >= 1 , "numbers must have at least 1 digit") ; let start = self . pos () ; let mut number : i32 = 0 ; for i in 0 .. n { if self . is_done () { return Err (err ! ("expected {n} digits, but found {i}")) ; } let byte = self . byte () ; let digit = match byte . checked_sub (b'0') { None => { return Err (err ! ("invalid digit, expected 0-9 but got {}" , Byte (byte) ,)) ; } Some (digit) if digit > 9 => { return Err (err ! ("invalid digit, expected 0-9 but got {}" , Byte (byte) ,)) } Some (digit) => { debug_assert ! ((0 ..= 9) . contains (& digit)) ; i32 :: from (digit) } } ; number = number . checked_mul (10) . and_then (| n | n . checked_add (digit)) . ok_or_else (| | { err ! ("number `{}` too big to parse into 64-bit integer" , Bytes (& self . tz [start ..] [.. i]) ,) }) ? ; self . bump () ; } Ok (number) } # [doc = " Parses a signed 64-bit integer expressed with up to `n` digits and"] # [doc = " at least 1 digit."] # [doc = ""] # [doc = " This assumes that `n >= 1` and that the parser is positioned at the"] # [doc = " first digit. Upon success, the parser is position immediately after"] # [doc = " the last digit (which can be at most `n`)."] fn parse_number_with_upto_n_digits (& self , n : usize) -> Result < i32 , Error > { assert ! (n >= 1 , "numbers must have at least 1 digit") ; let start = self . pos () ; let mut number : i32 = 0 ; for i in 0 .. n { if self . is_done () || ! self . byte () . is_ascii_digit () { if i == 0 { return Err (err ! ("invalid number, no digits found")) ; } break ; } let digit = i32 :: from (self . byte () - b'0') ; number = number . checked_mul (10) . and_then (| n | n . checked_add (digit)) . ok_or_else (| | { err ! ("number `{}` too big to parse into 64-bit integer" , Bytes (& self . tz [start ..] [.. i]) ,) }) ? ; self . bump () ; } Ok (number) } # [doc = " Parses an optional sign."] # [doc = ""] # [doc = " This assumes the parser is positioned at the position where a"] # [doc = " positive or negative sign is permitted. If one exists, then it"] # [doc = " is consumed and returned. Moreover, if one exists, then this"] # [doc = " guarantees that it is not the last byte in the input. That is, upon"] # [doc = " success, it is valid to call `self.byte()`."] fn parse_optional_sign (& self) -> Result < Option < i8 > , Error > { if self . is_done () { return Ok (None) ; } Ok (match self . byte () { b'-' => { if ! self . bump () { return Err (err ! ("expected digit after '-' sign, \
                         but got end of input" ,)) ; } Some (- 1) } b'+' => { if ! self . bump () { return Err (err ! ("expected digit after '+' sign, \
                         but got end of input" ,)) ; } Some (1) } _ => None , }) } }
};
}
