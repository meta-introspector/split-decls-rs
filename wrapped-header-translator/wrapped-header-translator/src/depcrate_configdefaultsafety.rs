// Generated macro for DefaultSafety (struct)
macro_rules! Depcrate_configDefaultSafety {
() => {
// Module: crate::config
// Provides: {"DefaultSafety"}
// Dependencies: {}
# [doc = " There are many different things that influence the soundness"] # [doc = " of an API. A few of these:"] # [doc = " 1. Initialization safety."] # [doc = " 2. Lifetime safety."] # [doc = " 3. Type safety."] # [doc = " 4. Bounds safety."] # [doc = " 5. Thread safety."] # [doc = " 6. Arbitrary additional restrictions."] # [doc = ""] # [doc = " Objective-C's object model upholds the initialization requirement, we can"] # [doc = " reasonably ensure that lifetime safety is upheld (object pointers are"] # [doc = " reference-counted, and we can disallow direct pointers), type safety is"] # [doc = " also doable (the subtyping model is fairly intricate, but we can be"] # [doc = " conservative), collections like NSArray etc. are internally bounds checked"] # [doc = " and thread safety is handled by `nonatomic` on properties, as well as the"] # [doc = " NS_SWIFT_SENDABLE- and NS_SWIFT_UI_ACTOR-like macros."] # [doc = ""] # [doc = " The only thing we really can't check automatically is if a function/method"] # [doc = " does something outside the usual. One example of this is the"] # [doc = " `NSAutoreleasePool` class, which may allow one to violate lifetime safety"] # [doc = " if misused."] # [doc = ""] # [doc = " As such, each framework must be explicitly marked with the kinds of safety"] # [doc = " it has been reviewed for."] # [derive (Deserialize , Debug , Clone , Copy , PartialEq , Eq , Hash)] # [serde (deny_unknown_fields)] pub struct DefaultSafety { # [doc = " All methods, functions and properties are marked as safe by default,"] # [doc = " provided that they do not take pointers or other such things as"] # [doc = " arguments."] # [serde (rename = "documentation-is-reviewed")] # [serde (default)] pub automatically_safe : bool , # [doc = " Whether functions or methods with bounds-affecting parameters are"] # [doc = " checked internally, and thus don't need to be unsafe."] # [doc = ""] # [doc = " The documentation in CoreFoundation-like frameworks often say it's"] # [doc = " \"undefined behaviour\" to have indexes or ranges out of bounds - but"] # [doc = " then the function often checks it anyhow, so whether that is something"] # [doc = " we can rely on is unclear?"] # [serde (rename = "bounds-checked-internally")] # [serde (default = "bounds_checked_internally_default")] pub bounds_checked_internally : bool , }
};
}
