// Generated macro for enum_prefix (function)
macro_rules! Depcrate_name_translationenum_prefix {
() => {
// Module: crate::name_translation
// Provides: {"enum_prefix"}
// Dependencies: {}
# [doc = " Find the common prefix of the enum."] # [doc = ""] # [doc = " According to the following algorithm:"] # [doc = " <https://github.com/swiftlang/swift/blob/swift-6.0.3-RELEASE/docs/CToSwiftNameTranslation.md#enum-style-prefix-stripping>"] # [doc = ""] # [doc = " Should be given all available, non-deprecated enum cases without custom"] # [doc = " names. If there are no such cases, then all cases without custom names,"] # [doc = " whether available or not."] pub (crate) fn enum_prefix < 'a > (enum_name : & 'a str , relevant_enum_cases : impl IntoIterator < Item = & 'a str > + Clone ,) -> & 'a str { let mut cp = common_prefix (relevant_enum_cases . clone ()) ; let mut had_k = false ; let original_cp = cp ; if let Some (cp_without_k) = cp . strip_prefix ("k") { if cp_without_k . starts_with (| c : char | c . is_ascii_uppercase ()) { cp = cp_without_k ; had_k = true ; } if cp_without_k . is_empty () && relevant_enum_cases . into_iter () . all (| case | { case . strip_prefix ("k") . unwrap () . starts_with (| c : char | ! c . is_ascii_digit ()) }) { cp = cp_without_k ; had_k = true ; } } let mut ep = common_prefix ([cp , enum_name]) ; let next_word_enum = split_words (enum_name . strip_prefix (ep) . unwrap ()) . next () . unwrap_or ("") ; let next_word_cp = split_words (cp . strip_prefix (ep) . unwrap ()) . next () . unwrap_or ("") ; if next_word_enum . strip_suffix ("s") == Some (next_word_cp) || next_word_enum . strip_suffix ("es") == Some (next_word_cp) || (next_word_enum . strip_suffix ("ies") . is_some () && next_word_enum . strip_suffix ("ies") == next_word_cp . strip_suffix ("y")) { ep = cp . split_at (ep . len () + next_word_cp . len ()) . 0 ; } if let Some ("_") = split_words (cp . strip_prefix (ep) . unwrap ()) . next () { ep = cp . split_at (ep . len () + 1) . 0 ; } if had_k { ep = original_cp . split_at (ep . len () + 1) . 0 ; } ep }
};
}
