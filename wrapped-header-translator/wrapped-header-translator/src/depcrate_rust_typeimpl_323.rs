// Generated macro for impl_323 (impl)
macro_rules! Depcrate_rust_typeimpl_323 {
() => {
// Module: crate::rust_type
// Provides: {"impl_323"}
// Dependencies: {}
impl SafetyProperty { pub fn new_unsafe (reason : impl Into < String >) -> Self { Self :: Unsafe { reasons : vec ! [reason . into ()] , } } pub fn new_unknown (reason : impl Into < String >) -> Self { Self :: Unknown { reasons : vec ! [reason . into ()] , } } # [doc = " Merge two safety properties."] # [doc = ""] # [doc = " When there are multiple reasons for something being unsafe, these"] # [doc = " are merged as well."] pub fn merge (self , other : Self) -> Self { match (self , other) { (Self :: Unsafe { reasons : reasons1 } , Self :: Unsafe { reasons : reasons2 } | Self :: Unknown { reasons : reasons2 } ,) => Self :: Unsafe { reasons : reasons1 . into_iter () . chain (reasons2) . collect () , } , (Self :: Unknown { reasons : reasons1 } , Self :: Unsafe { reasons : reasons2 }) => { Self :: Unsafe { reasons : reasons1 . into_iter () . chain (reasons2) . collect () , } } (Self :: Unsafe { reasons } , Self :: Safe) => Self :: Unsafe { reasons } , (Self :: Safe , Self :: Unsafe { reasons }) => Self :: Unsafe { reasons } , (Self :: Unknown { reasons : reasons1 } , Self :: Unknown { reasons : reasons2 }) => { Self :: Unknown { reasons : reasons1 . into_iter () . chain (reasons2) . collect () , } } (Self :: Unknown { reasons } , Self :: Safe) => Self :: Unknown { reasons } , (Self :: Safe , Self :: Unknown { reasons }) => Self :: Unknown { reasons } , (Self :: Safe , Self :: Safe) => Self :: Safe , } } pub fn is_unsafe (& self) -> bool { matches ! (self , SafetyProperty :: Unsafe { .. }) } pub fn is_safe (& self) -> bool { matches ! (self , SafetyProperty :: Safe) } fn ignore (self) -> Self { match self { Self :: Unsafe { .. } => Self :: Unsafe { reasons : vec ! [] } , Self :: Unknown { .. } => Self :: Unknown { reasons : vec ! [] } , Self :: Safe => Self :: Safe , } } fn context (self , context : impl Display) -> Self { match self { Self :: Unsafe { mut reasons } => { for reason in & mut reasons { * reason = format ! ("{context} {reason}") ; } Self :: Unsafe { reasons } } Self :: Unknown { mut reasons } => { for reason in & mut reasons { * reason = format ! ("{context} {reason}") ; } Self :: Unknown { reasons } } Self :: Safe => Self :: Safe , } } fn preface (self , preface : impl Display) -> Self { match self { Self :: Unsafe { mut reasons } => { for reason in & mut reasons { * reason = format ! ("{preface} {reason}") ; } Self :: Unsafe { reasons } } Self :: Unknown { mut reasons } => { for reason in & mut reasons { * reason = format ! ("{preface} {reason}") ; } Self :: Unknown { reasons } } Self :: Safe => Self :: Safe , } } # [doc = " This comment is incomplete, it cannot know about extra requirements"] # [doc = " that a method may have."] # [doc = ""] # [doc = " A concern could be that adding the \"# Safety\" comments might lead"] # [doc = " users to think that that is all they need to uphold, whereas in"] # [doc = " reality there might be further requirements stated elsewhere in the"] # [doc = " documentation. This is _probably_ fine, having a safety comment is"] # [doc = " better than not having it."] # [doc = ""] # [doc = " TODO: Maybe search for \"undefined behaviour\" in the docstring, and use"] # [doc = " that as a marker too?"] pub fn to_safety_comment (& self) -> Option < String > { match self { Self :: Unsafe { reasons } | Self :: Unknown { reasons } => Some (if reasons . len () == 1 { format ! ("{}." , reasons [0]) } else { format ! ("- {}." , reasons . join (".\n- ")) }) , Self :: Safe => None , } } }
};
}
