// Generated macro for DispatchRetained (struct)
macro_rules! Depcrate_retainedDispatchRetained {
() => {
// Module: crate::retained
// Provides: {"DispatchRetained"}
// Dependencies: {}
# [doc = " A reference counted pointer type for Dispatch objects."] # [doc = ""] # [doc = " [`DispatchRetained`] strongly references or \"retains\" the given object"] # [doc = " `T`, and decrements the retain count or \"releases\" it again when dropped,"] # [doc = " thereby ensuring it will be deallocated at the right time."] # [doc = ""] # [doc = " The type `T` inside `DispatchRetained<T>` can be anything that implements"] # [doc = " [`DispatchObject`], i.e. any Dispatch object."] # [doc = ""] # [doc = ""] # [doc = " # Comparison to other types"] # [doc = ""] # [doc = " `DispatchRetained<T>` is equivalent to [`objc2::rc::Retained`], and can be"] # [doc = " converted to/from that when the `\"objc2\"` feature is enabled. Note though"] # [doc = " that this type uses the underlying Dispatch primitives `dispatch_retain` /"] # [doc = " `dispatch_release` instead of `objc_retain` / `objc_release` for"] # [doc = " performance, and to avoid depending on the Objective-C runtime if not"] # [doc = " needed."] # [doc = ""] # [doc = " You can also view `DispatchRetained<T>` as the Dispatch equivalent of"] # [doc = " [`std::sync::Arc`], that is, it is a thread-safe reference-counting smart"] # [doc = " pointer that allows cloning by bumping the reference count."] # [doc = ""] # [doc = " Unlike `Arc`, objects can be retained directly from a `&T` using"] # [doc = " [`DispatchObject::retain`] (for `Arc` you need `&Arc<T>`)."] # [doc = ""] # [doc = " Weak references are not supported, for that you need to convert to"] # [doc = " `objc2::rc::Retained`."] # [doc = ""] # [cfg_attr (not (feature = "objc2") , doc = "[`objc2::rc::Retained`]: #objc2-not-available")] # [cfg_attr (not (feature = "std") , doc = "[`std::sync::Arc`]: #std-not-enabled")] # [doc = ""] # [doc = ""] # [doc = " # Forwarding implementations"] # [doc = ""] # [doc = " Since `DispatchRetained<T>` is a smart pointer, it [`Deref`]s to `T`."] # [doc = ""] # [doc = " It also forwards the implementation of a bunch of standard library traits"] # [doc = " such as [`PartialEq`], [`AsRef`], and so on, so that it becomes possible"] # [doc = " to use e.g. `DispatchRetained<DispatchQueue>` as-if it you had a"] # [doc = " `&DispatchQueue`. Note that having a `DispatchQueue` directly is not"] # [doc = " possible since dispatch objects cannot live on the stack, but instead must"] # [doc = " reside on the heap, and as such must be accessed behind a pointer or a"] # [doc = " reference."] # [doc = ""] # [doc = ""] # [doc = " # Memory layout"] # [doc = ""] # [doc = " This is guaranteed to have the same size and alignment as a pointer to the"] # [doc = " object, i.e. same as `*const T` or `dispatch_object_t`."] # [doc = ""] # [doc = " Additionally, it participates in the null-pointer optimization, that is,"] # [doc = " `Option<DispatchRetained<T>>` is guaranteed to have the same size as"] # [doc = " `DispatchRetained<T>`."] # [repr (transparent)] # [doc (alias = "Retained")] # [doc (alias = "objc2::rc::Retained")] pub struct DispatchRetained < T : ? Sized > { # [doc = " A pointer to the contained object. The pointer is always retained."] # [doc = ""] # [doc = " It is important that this is `NonNull`, since we want to dereference"] # [doc = " it later, and be able to use the null-pointer optimization."] ptr : NonNull < T > , # [doc = " Necessary for dropck even though we never actually run T's destructor,"] # [doc = " because it might have a `dealloc` that assumes that contained"] # [doc = " references outlive the type."] # [doc = ""] # [doc = " See <https://doc.rust-lang.org/nightly/nomicon/phantom-data.html>"] item : PhantomData < T > , # [doc = " Marks the type as !UnwindSafe. Later on we'll re-enable this."] # [doc = ""] # [doc = " See <https://github.com/rust-lang/rust/issues/93367> for why this is"] # [doc = " required."] notunwindsafe : PhantomData < & 'static mut () > , }
};
}
