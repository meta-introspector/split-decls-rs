// Generated macro for interact_polling (function)
macro_rules! Depcrate_interact_sessioninteract_polling {
() => {
// Module: crate::interact::session
// Provides: {"interact_polling"}
// Dependencies: {}
# [cfg (all (unix , not (feature = "async") , feature = "polling"))] fn interact_polling < S , O , I , C > (s : & mut InteractSession < S , I , O , C >) -> Result < bool , Error > where S : Healthcheck < Status = WaitStatus > + Write + Read + std :: os :: unix :: io :: AsRawFd , I : Read + std :: os :: unix :: io :: AsRawFd , O : Write , { use polling :: { Event , Poller } ; let poller = Poller :: new () ? ; poller . add (s . input . as_raw_fd () , Event :: readable (0)) ? ; poller . add (s . session . as_raw_fd () , Event :: readable (1)) ? ; let mut buf = [0 ; 512] ; let mut events = Vec :: new () ; loop { let status = get_status (& s . session) ? ; if ! matches ! (status , Some (WaitStatus :: StillAlive)) { s . status = status ; return Ok (false) ; } events . clear () ; let _ = poller . wait (& mut events , Some (std :: time :: Duration :: from_secs (5))) ? ; for ev in & events { if ev . key == 0 { match s . input . read (& mut buf) { Ok (n) => { let eof = n == 0 ; let buf = & buf [.. n] ; let buf = call_filter (s . opts . input_filter . as_mut () , buf) ? ; let exit = run_action_input (s , & buf , eof) ? ; if eof || exit { return Ok (true) ; } let escape_char_pos = buf . iter () . position (| c | * c == s . escape_character) ; match escape_char_pos { Some (pos) => { s . session . write_all (& buf [.. pos]) . map_err (Error :: IO) ? ; return Ok (true) ; } None => s . session . write_all (& buf [..]) ? , } } Err (err) if err . kind () == ErrorKind :: WouldBlock => { } Err (err) => return Err (err . into ()) , } poller . modify (s . input . as_raw_fd () , Event :: readable (0)) ? ; } if ev . key == 1 { match s . session . read (& mut buf) { Ok (n) => { let eof = n == 0 ; let buf = & buf [.. n] ; let buf = call_filter (s . opts . output_filter . as_mut () , buf) ? ; let exit = run_action_output (s , & buf , eof) ? ; if eof || exit { return Ok (true) ; } spin_write (& mut s . output , & buf) ? ; spin_flush (& mut s . output) ? ; } Err (err) if err . kind () == ErrorKind :: WouldBlock => { } Err (err) => return Err (err . into ()) , } poller . modify (s . session . as_raw_fd () , Event :: readable (1)) ? ; } } let exit = run_action_idle (s , & [] , false) ? ; if exit { return Ok (true) ; } } }
};
}
