// Generated macro for impl_313 (impl)
macro_rules! Depcrate_sessionimpl_313 {
() => {
// Module: crate::session
// Provides: {"impl_313"}
// Dependencies: {}
impl < P , S > Session < P , S > { # [doc = " Interact gives control of the child process to the interactive user (the"] # [doc = " human at the keyboard or a [`Read`]er implementator)."] # [doc = ""] # [doc = " You can set different callbacks to the session, see [`InteractSession`]."] # [doc = ""] # [doc = " Keystrokes are sent to the child process, and"] # [doc = " the `stdout` and `stderr` output of the child process is printed."] # [doc = ""] # [doc = " When the user types the `escape_character` this method will return control to a running process."] # [doc = " The escape_character will not be transmitted."] # [doc = " The default for escape_character is entered as `Ctrl-]`, the very same as BSD telnet."] # [doc = ""] # [doc = " This simply echos the child `stdout` and `stderr` to the real `stdout` and"] # [doc = " it echos the real `stdin` to the child `stdin`."] # [doc = ""] # [doc = " BEWARE that interact finishes after a process stops."] # [doc = " So after the return you may not obtain a correct status of a process."] # [doc = ""] # [doc = " In not `async` mode the default version uses a buzy loop."] # [doc = ""] # [doc = " - On `linux` you can use a `polling` version using the corresponding feature."] # [doc = " - On `windows` the feature is also present but it spawns a thread for pooling which creates a set of obsticales."] # [doc = "   Specifically if you're planning to call `interact()` multiple times it may not be safe. Because the previous threads may still be running."] # [doc = ""] # [doc = " It works via polling in `async` mode on both `unix` and `windows`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [cfg_attr (all (unix , not (feature = "async") , not (feature = "polling")) , doc = "```no_run")] # [cfg_attr (not (all (unix , not (feature = "async") , not (feature = "polling"))) , doc = "```ignore")] # [doc = " use std::io::{stdout, Cursor};"] # [doc = ""] # [doc = " let mut p = expectrl::spawn(\"cat\").unwrap();"] # [doc = ""] # [doc = " let input = Cursor::new(String::from(\"Some text right here\"));"] # [doc = ""] # [doc = " p.interact(input, stdout()).spawn().unwrap();"] # [doc = " ```"] # [doc = ""] # [doc = " [`Read`]: std::io::Read"] pub fn interact < I , O > (& mut self , input : I , output : O) -> InteractSession < & mut Self , I , O , () > { InteractSession :: new (self , input , output , ()) } }
};
}
