// Generated macro for ShutdownConnection (trait)
macro_rules! Depcrate_shutdownShutdownConnection {
() => {
// Module: crate::shutdown
// Provides: {"ShutdownConnection"}
// Dependencies: {}
pub trait ShutdownConnection { # [doc = " Initiates or attempts to shut down this writer, returning success when"] # [doc = " the I/O connection has completely shut down."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " This function returns a `Poll<io::Result<()>>` classified as such:"] # [doc = ""] # [doc = " * `Poll::Ready(Ok(()))` - indicates that the connection was successfully"] # [doc = "   shut down and is now safe to deallocate/drop/close resources"] # [doc = "   associated with it. This method means that the current task will no"] # [doc = "   longer receive any notifications due to this method and the I/O object"] # [doc = "   itself is likely no longer usable."] # [doc = ""] # [doc = " * `Poll::Pending` - indicates that shutdown is initiated but could not"] # [doc = "   complete just yet. This may mean that more I/O needs to happen to"] # [doc = "   continue this shutdown operation. The current task is scheduled to"] # [doc = "   receive a notification when it's otherwise ready to continue the"] # [doc = "   shutdown operation. When woken up this method should be called again."] # [doc = ""] # [doc = " * `Poll::Ready(Err(e))` - indicates a fatal error has happened with"] # [doc = "   shutdown, indicating that the shutdown operation did not complete"] # [doc = "   successfully. This typically means that the I/O object is no longer"] # [doc = "   usable."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function can return normal I/O errors through `Err`, described"] # [doc = " above. Additionally this method may also render the underlying"] # [doc = " `Write::write` method no longer usable (e.g. will return errors in the"] # [doc = " future). It's recommended that once `shutdown` is called the"] # [doc = " `write` method is no longer called."] fn poll_shutdown (& mut self , cx : & mut Context) -> Poll < io :: Result < () > > ; }
};
}
