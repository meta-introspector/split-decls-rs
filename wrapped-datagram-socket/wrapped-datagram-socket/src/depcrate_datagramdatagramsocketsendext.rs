// Generated macro for DatagramSocketSendExt (trait)
macro_rules! Depcrate_datagramDatagramSocketSendExt {
() => {
// Module: crate::datagram
// Provides: {"DatagramSocketSendExt"}
// Dependencies: {}
# [doc = " Writes datagrams to a socket."] # [doc = ""] # [doc = " Implemented as an extension trait, adding utility methods to all"] # [doc = " [`DatagramSocketSend`] types. Callers will tend to import this trait instead"] # [doc = " of [`DatagramSocketSend`]."] # [doc = ""] # [doc = " [`DatagramSocketSend`]: DatagramSocketSend"] pub trait DatagramSocketSendExt : DatagramSocketSend { # [doc = " Sends data on the socket to the remote address that the socket is"] # [doc = " connected to."] fn send (& self , buf : & [u8]) -> impl Future < Output = io :: Result < usize > > { poll_fn (move | cx | self . poll_send (cx , buf)) } # [doc = " Sends data on the socket to the given address. On success, returns the"] # [doc = " number of bytes written."] fn send_to (& self , buf : & [u8] , addr : SocketAddr ,) -> impl Future < Output = io :: Result < usize > > { poll_fn (move | cx | self . poll_send_to (cx , buf , addr)) } # [doc = " Sends multiple data packets on the socket to the to the remote address"] # [doc = " that the socket is connected to. On success, returns the number of"] # [doc = " packets sent."] fn send_many (& self , bufs : & [ReadBuf < '_ >] ,) -> impl Future < Output = io :: Result < usize > > { poll_fn (move | cx | self . poll_send_many (cx , bufs)) } fn try_send (& self , buf : & [u8]) -> io :: Result < usize > { match unconstrained (poll_fn (| cx | self . poll_send (cx , buf))) . now_or_never () { Some (result) => result , None => Err (io :: ErrorKind :: WouldBlock . into ()) , } } fn try_send_many (& self , bufs : & [ReadBuf < '_ >]) -> io :: Result < usize > { match unconstrained (poll_fn (| cx | self . poll_send_many (cx , bufs))) . now_or_never () { Some (result) => result , None => Err (io :: ErrorKind :: WouldBlock . into ()) , } } }
};
}
