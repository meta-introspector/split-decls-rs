// Generated macro for DatagramSocketRecv (trait)
macro_rules! Depcrate_datagramDatagramSocketRecv {
() => {
// Module: crate::datagram
// Provides: {"DatagramSocketRecv"}
// Dependencies: {}
# [doc = " Describes the receive half of a connected datagram socket."] pub trait DatagramSocketRecv : Send { # [doc = " Attempts to receive a single datagram message on the socket from the"] # [doc = " remote address to which it is `connect`ed."] # [doc = ""] # [doc = " Note that on multiple calls to a `poll_*` method in the `recv`"] # [doc = " direction, only the `Waker` from the `Context` passed to the most"] # [doc = " recent call will be scheduled to receive a wakeup."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " The function returns:"] # [doc = ""] # [doc = " * `Poll::Pending` if the socket is not ready to read"] # [doc = " * `Poll::Ready(Ok(()))` reads data `ReadBuf` if the socket is ready"] # [doc = " * `Poll::Ready(Err(e))` if an error is encountered."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function may encounter any standard I/O error except `WouldBlock`."] fn poll_recv (& mut self , cx : & mut Context < '_ > , buf : & mut ReadBuf < '_ > ,) -> Poll < io :: Result < () > > ; # [doc = " Attempts to receive a single datagram on the socket."] # [doc = ""] # [doc = " Note that on multiple calls to a `poll_*` method in the `recv`"] # [doc = " direction, only the `Waker` from the `Context` passed to the most"] # [doc = " recent call will be scheduled to receive a wakeup."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " The function returns:"] # [doc = ""] # [doc = " * `Poll::Pending` if the socket is not ready to read"] # [doc = " * `Poll::Ready(Ok(addr))` reads data from `addr` into `ReadBuf` if the"] # [doc = "   socket is ready"] # [doc = " * `Poll::Ready(Err(e))` if an error is encountered."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function may encounter any standard I/O error except `WouldBlock`."] fn poll_recv_from (& mut self , cx : & mut Context < '_ > , buf : & mut ReadBuf < '_ > ,) -> Poll < io :: Result < SocketAddr > > { self . poll_recv (cx , buf) . map_ok (| _ | { SocketAddr :: V4 (SocketAddrV4 :: new (Ipv4Addr :: UNSPECIFIED , 0)) }) } # [doc = " Attempts to receive multiple datagrams on the socket from the remote"] # [doc = " address to which it is `connect`ed."] # [doc = ""] # [doc = " Note that on multiple calls to a `poll_*` method in the `recv`"] # [doc = " direction, only the `Waker` from the `Context` passed to the most"] # [doc = " recent call will be scheduled to receive a wakeup."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " The function returns:"] # [doc = ""] # [doc = " * `Poll::Pending` if the socket is not ready to read"] # [doc = " * `Poll::Ready(Ok(n))` reads data `ReadBuf` if the socket is ready `n`"] # [doc = "   is the number of datagrams read."] # [doc = " * `Poll::Ready(Err(e))` if an error is encountered."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function may encounter any standard I/O error except `WouldBlock`."] fn poll_recv_many (& mut self , cx : & mut Context < '_ > , bufs : & mut [ReadBuf < '_ >] ,) -> Poll < io :: Result < usize > > { let mut read = 0 ; for buf in bufs { match self . poll_recv (cx , buf) { Poll :: Ready (Ok (())) => read += 1 , Poll :: Ready (Err (e)) => return Poll :: Ready (Err (e)) , Poll :: Pending if read == 0 => return Poll :: Pending , Poll :: Pending => break , } } Poll :: Ready (Ok (read)) } # [doc = " If the underlying socket is a `UdpSocket`, return the reference to it."] fn as_udp_socket (& self) -> Option < & UdpSocket > { None } }
};
}
