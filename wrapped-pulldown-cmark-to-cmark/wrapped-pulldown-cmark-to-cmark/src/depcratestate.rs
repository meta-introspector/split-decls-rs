// Generated macro for State (struct)
macro_rules! DepcrateState {
() => {
// Module: crate
// Provides: {"State"}
// Dependencies: {}
# [doc = " The state of the [`cmark_resume()`] and [`cmark_resume_with_options()`] functions."] # [doc = " This does not only allow introspection, but enables the user"] # [doc = " to halt the serialization at any time, and resume it later."] # [derive (Clone , Default , Debug , PartialEq , Eq , PartialOrd , Ord , Hash)] # [non_exhaustive] pub struct State < 'a > { # [doc = " The amount of newlines to insert after `Event::Start(...)`"] pub newlines_before_start : usize , # [doc = " The lists and their types for which we have seen a `Event::Start(List(...))` tag"] pub list_stack : Vec < Option < u64 > > , # [doc = " The computed padding and prefix to print after each newline."] # [doc = " This changes with the level of `BlockQuote` and `List` events."] pub padding : Vec < Cow < 'a , str > > , # [doc = " Keeps the current table alignments, if we are currently serializing a table."] pub table_alignments : Vec < Alignment > , # [doc = " Keeps the current table headers, if we are currently serializing a table."] pub table_headers : Vec < String > , # [doc = " The last seen text when serializing a header"] pub text_for_header : Option < String > , # [doc = " Is set while we are handling text in a code block"] pub code_block : Option < CodeBlockKind > , # [doc = " True if the last event was text and the text does not have trailing newline. Used to inject additional newlines before code block end fence."] pub last_was_text_without_trailing_newline : bool , # [doc = " True if the last event was a paragraph start. Used to escape spaces at start of line (prevent spurrious indented code)."] pub last_was_paragraph_start : bool , # [doc = " True if the next event is a link, image, or footnote."] pub next_is_link_like : bool , # [doc = " Currently open links"] pub link_stack : Vec < LinkCategory < 'a > > , # [doc = " Currently open images"] pub image_stack : Vec < ImageLink < 'a > > , # [doc = " Keeps track of the last seen heading's id, classes, and attributes"] pub current_heading : Option < Heading < 'a > > , # [doc = " True whenever between `Start(TableCell)` and `End(TableCell)`"] pub in_table_cell : bool , # [doc = " Keeps track of the last seen shortcut/link"] pub current_shortcut_text : Option < String > , # [doc = " A list of shortcuts seen so far for later emission"] pub shortcuts : Vec < (String , String , String) > , # [doc = " Index into the `source` bytes of the end of the range corresponding to the last event."] # [doc = ""] # [doc = " It's used to see if the current event didn't capture some bytes because of a"] # [doc = " skipped-over backslash."] pub last_event_end_index : usize , }
};
}
