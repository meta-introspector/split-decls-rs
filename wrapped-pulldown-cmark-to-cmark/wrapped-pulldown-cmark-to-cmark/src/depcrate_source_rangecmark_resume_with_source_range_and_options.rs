// Generated macro for cmark_resume_with_source_range_and_options (function)
macro_rules! Depcrate_source_rangecmark_resume_with_source_range_and_options {
() => {
// Module: crate::source_range
// Provides: {"cmark_resume_with_source_range_and_options"}
// Dependencies: {}
# [doc = " Serialize a stream of [pulldown-cmark-Events][Event] while preserving the escape characters in `source`."] # [doc = " Each input [Event] is accompanied by an optional [Range] that maps it back to the `source` string."] # [doc = ""] # [doc = " Different from [`cmark_resume_with_options`](super::cmark_resume_with_options), which always escape"] # [doc = " Markdown special characters like `#` or `[`, this function only escapes a special character if"] # [doc = " it is escaped in `source`."] # [doc = ""] # [doc = " 1. **source**"] # [doc = "     * Markdown source from which `event_and_ranges` are created."] # [doc = " 1. **event_and_ranges**"] # [doc = "    * An iterator over [`Event`]-range pairs, for example as returned by [`pulldown_cmark::OffsetIter`]."] # [doc = "      Must match what's provided in `source`."] # [doc = " 1. **formatter**"] # [doc = "    * A format writer, can be a `String`."] # [doc = " 1. **state**"] # [doc = "    * The optional initial state of the serialization, useful when the operation should be resumed."] # [doc = " 1. **options**"] # [doc = "    * Customize the appearance of the serialization. All otherwise magic values are contained"] # [doc = "      here."] # [doc = ""] # [doc = " *Returns* the [`State`] of the serialization on success. You can use it as initial state in the"] # [doc = " next call if you are halting event serialization."] # [doc = ""] # [doc = " *Errors* if the underlying buffer fails (which is unlikely) or if the [`Event`] stream"] # [doc = " iterated over by `event_and_ranges` cannot ever be produced by deserializing valid Markdown."] # [doc = " Each failure mode corresponds to one of [`Error`]'s variants."] pub fn cmark_resume_with_source_range_and_options < 'a , I , E , F > (event_and_ranges : I , source : & 'a str , mut formatter : F , state : Option < State < 'a > > , options : Options < '_ > ,) -> Result < State < 'a > , Error > where I : Iterator < Item = (E , Option < Range < usize > >) > , E : Borrow < Event < 'a > > , F : fmt :: Write , { let mut state = state . unwrap_or_default () ; for (event , range) in event_and_ranges { let update_event_end_index = ! matches ! (* event . borrow () , Event :: Start (_)) ; let prevent_escape_leading_special_characters = match (& range , event . borrow ()) { _ if state . current_heading . is_some () || ! state . table_alignments . is_empty () => false , (Some (range) , Event :: Text (_)) => { range . start <= state . last_event_end_index || source . as_bytes () . get (range . start . saturating_sub (1)) != Some (& b'\\') } _ => false , } && ! state . is_in_code_block () ; if prevent_escape_leading_special_characters { state . code_block = Some (crate :: CodeBlockKind :: Fenced) ; } cmark_resume_one_event (event , & mut formatter , & mut state , & options) ? ; if prevent_escape_leading_special_characters { state . code_block = None ; } if let (true , Some (range)) = (update_event_end_index , range) { state . last_event_end_index = range . end ; } } Ok (state) }
};
}
