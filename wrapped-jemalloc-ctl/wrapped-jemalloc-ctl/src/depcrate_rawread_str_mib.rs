// Generated macro for read_str_mib (function)
macro_rules! Depcrate_rawread_str_mib {
() => {
// Module: crate::raw
// Provides: {"read_str_mib"}
// Dependencies: {}
# [doc = " Uses the MIB `mib` as key to the _MALLCTL NAMESPACE_ and reads its value."] # [doc = ""] # [doc = " The [`name_to_mib`] API translates a string of the key (e.g. `arenas.nbins`)"] # [doc = " to a `mib` (Management Information Base)."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This function is unsafe because if the key does not return a pointer to a"] # [doc = " null-terminated string the behavior is undefined."] # [doc = ""] # [doc = " For example, a key for a `u64` value can be used to read a pointer on 64-bit"] # [doc = " platform, where this pointer will point to the address denoted by the `u64`s"] # [doc = " representation. Also, a key to a `*mut extent_hooks_t` will return a pointer"] # [doc = " that will not point to a null-terminated string."] # [doc = ""] # [doc = " This function needs to compute the length of the string by looking for the"] # [doc = " null-terminator: `\\0`. This requires reading the memory behind the pointer."] # [doc = ""] # [doc = " If the pointer is invalid (e.g. because it was converted from a `u64` that"] # [doc = " does not represent a valid address), reading the string to look for `\\0`"] # [doc = " will dereference a non-dereferenceable pointer, which is undefined behavior."] # [doc = ""] # [doc = " If the pointer is valid but it does not point to a null-terminated string,"] # [doc = " looking for `\\0` will read garbage and might end up reading out-of-bounds,"] # [doc = " which is undefined behavior."] pub unsafe fn read_str_mib (mib : & [usize]) -> Result < & 'static [u8] > { let ptr : * const c_char = read_mib (mib) ? ; Ok (ptr2str (ptr)) }
};
}
