// Generated macro for name_to_mib (function)
macro_rules! Depcrate_rawname_to_mib {
() => {
// Module: crate::raw
// Provides: {"name_to_mib"}
// Dependencies: {}
# [doc = " Translates `name` to a `mib` (Management Information Base)"] # [doc = ""] # [doc = " `mib`s are used to avoid repeated name lookups for applications that"] # [doc = " repeatedly query the same portion of `jemalloc`s `mallctl` namespace."] # [doc = ""] # [doc = " On success, `mib` contains an array of integers. It is possible to pass"] # [doc = " `mib` with a length smaller than the number of period-separated name"] # [doc = " components. This results in a partial MIB that can be used as the basis for"] # [doc = " constructing a complete MIB."] # [doc = ""] # [doc = " For name components that are integers (e.g. the `2` in `arenas.bin.2.size`),"] # [doc = " the corresponding MIB component will always be that integer. Therefore, it"] # [doc = " is legitimate to construct code like the following:"] # [doc = ""] # [doc = " ```"] # [doc = " #[global_allocator]"] # [doc = " static ALLOC: tikv_jemallocator::Jemalloc = tikv_jemallocator::Jemalloc;"] # [doc = ""] # [doc = " fn main() {"] # [doc = "     use tikv_jemalloc_ctl::raw;"] # [doc = "     use libc::{c_uint, c_char};"] # [doc = "     unsafe {"] # [doc = "         let mut mib = [0; 4];"] # [doc = "         let nbins: c_uint = raw::read(b\"arenas.nbins\\0\").unwrap();"] # [doc = "         raw::name_to_mib(b\"arenas.bin.0.size\\0\", &mut mib).unwrap();"] # [doc = "         for i in 0..4 {"] # [doc = "             mib[2] = i;"] # [doc = "             let bin_size: usize = raw::read_mib(&mut mib).unwrap();"] # [doc = "             println!(\"arena bin {} has size {}\", i, bin_size);"] # [doc = "         }"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] pub fn name_to_mib (name : & [u8] , mib : & mut [usize]) -> Result < () > { unsafe { validate_name (name) ; let mut len = mib . len () ; cvt (tikv_jemalloc_sys :: mallctlnametomib (name as * const _ as * const c_char , mib . as_mut_ptr () , & mut len ,)) ? ; assert_eq ! (mib . len () , len) ; Ok (()) } }
};
}
