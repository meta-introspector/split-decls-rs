// Generated macro for impl_223 (impl)
macro_rules! Depcrate_hirimpl_223 {
() => {
// Module: crate::hir
// Provides: {"impl_223"}
// Dependencies: {}
impl < 'hir > GenericArgs < 'hir > { pub const fn none () -> Self { Self { args : & [] , constraints : & [] , parenthesized : GenericArgsParentheses :: No , span_ext : DUMMY_SP , } } # [doc = " Obtain the list of input types and the output type if the generic arguments are parenthesized."] # [doc = ""] # [doc = " Returns the `Ty0, Ty1, ...` and the `RetTy` in `Trait(Ty0, Ty1, ...) -> RetTy`."] # [doc = " Panics if the parenthesized arguments have an incorrect form (this shouldn't happen)."] pub fn paren_sugar_inputs_output (& self) -> Option < (& [Ty < 'hir >] , & Ty < 'hir >) > { if self . parenthesized != GenericArgsParentheses :: ParenSugar { return None ; } let inputs = self . args . iter () . find_map (| arg | { let GenericArg :: Type (ty) = arg else { return None } ; let TyKind :: Tup (tys) = & ty . kind else { return None } ; Some (tys) }) . unwrap () ; Some ((inputs , self . paren_sugar_output_inner ())) } # [doc = " Obtain the output type if the generic arguments are parenthesized."] # [doc = ""] # [doc = " Returns the `RetTy` in `Trait(Ty0, Ty1, ...) -> RetTy`."] # [doc = " Panics if the parenthesized arguments have an incorrect form (this shouldn't happen)."] pub fn paren_sugar_output (& self) -> Option < & Ty < 'hir > > { (self . parenthesized == GenericArgsParentheses :: ParenSugar) . then (| | self . paren_sugar_output_inner ()) } fn paren_sugar_output_inner (& self) -> & Ty < 'hir > { let [constraint] = self . constraints . try_into () . unwrap () ; debug_assert_eq ! (constraint . ident . name , sym :: Output) ; constraint . ty () . unwrap () } pub fn has_err (& self) -> Option < ErrorGuaranteed > { self . args . iter () . find_map (| arg | { let GenericArg :: Type (ty) = arg else { return None } ; let TyKind :: Err (guar) = ty . kind else { return None } ; Some (guar) }) . or_else (| | { self . constraints . iter () . find_map (| constraint | { let TyKind :: Err (guar) = constraint . ty () ? . kind else { return None } ; Some (guar) }) }) } # [inline] pub fn num_lifetime_params (& self) -> usize { self . args . iter () . filter (| arg | matches ! (arg , GenericArg :: Lifetime (_))) . count () } # [inline] pub fn has_lifetime_params (& self) -> bool { self . args . iter () . any (| arg | matches ! (arg , GenericArg :: Lifetime (_))) } # [inline] # [doc = " This function returns the number of type and const generic params."] # [doc = " It should only be used for diagnostics."] pub fn num_generic_params (& self) -> usize { self . args . iter () . filter (| arg | ! matches ! (arg , GenericArg :: Lifetime (_))) . count () } # [doc = " The span encompassing the arguments and constraints[^1] inside the surrounding brackets."] # [doc = ""] # [doc = " Returns `None` if the span is empty (i.e., no brackets) or dummy."] # [doc = ""] # [doc = " [^1]: Unless of the form `-> Ty` (see [`GenericArgsParentheses`])."] pub fn span (& self) -> Option < Span > { let span_ext = self . span_ext () ? ; Some (span_ext . with_lo (span_ext . lo () + BytePos (1)) . with_hi (span_ext . hi () - BytePos (1))) } # [doc = " Returns span encompassing arguments and their surrounding `<>` or `()`"] pub fn span_ext (& self) -> Option < Span > { Some (self . span_ext) . filter (| span | ! span . is_empty ()) } pub fn is_empty (& self) -> bool { self . args . is_empty () } }
};
}
