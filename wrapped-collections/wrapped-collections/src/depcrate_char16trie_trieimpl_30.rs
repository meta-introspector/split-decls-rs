// Generated macro for impl_30 (impl)
macro_rules! Depcrate_char16trie_trieimpl_30 {
() => {
// Module: crate::char16trie::trie
// Provides: {"impl_30"}
// Dependencies: {}
impl < 'a > Char16TrieIterator < 'a > { # [doc = " Returns a new [`Char16TrieIterator`] backed by borrowed data for the `trie` array"] # [inline] pub fn new (trie : & 'a ZeroSlice < u16 >) -> Self { Self { trie , pos : Some (0) , remaining_match_length : None , } } # [doc = " Traverses the trie from the current state for this input char."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::collections::char16trie::{Char16Trie, TrieResult};"] # [doc = " use zerovec::ZeroVec;"] # [doc = ""] # [doc = " // A Char16Trie containing the ASCII characters 'a' and 'b'."] # [doc = " let trie_data = [48, 97, 176, 98, 32868];"] # [doc = " let trie = Char16Trie::new(ZeroVec::from_slice_or_alloc(&trie_data));"] # [doc = ""] # [doc = " let mut iter = trie.iter();"] # [doc = " let res = iter.next('a');"] # [doc = " assert_eq!(res, TrieResult::Intermediate(1));"] # [doc = " let res = iter.next('b');"] # [doc = " assert_eq!(res, TrieResult::FinalValue(100));"] # [doc = " let res = iter.next('c');"] # [doc = " assert_eq!(res, TrieResult::NoMatch);"] # [doc = " ```"] pub fn next (& mut self , c : char) -> TrieResult { if (c as u32) <= 0xffff { self . next16 (c as u16) } else { match self . next16 (u16_lead (c as i32)) { TrieResult :: NoValue | TrieResult :: Intermediate (_) => { self . next16 (u16_tail (c as i32)) } _ => TrieResult :: NoMatch , } } } # [doc = " Traverses the trie from the current state for this input char."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::collections::char16trie::{Char16Trie, TrieResult};"] # [doc = " use zerovec::ZeroVec;"] # [doc = ""] # [doc = " // A Char16Trie containing the ASCII characters 'a' and 'b'."] # [doc = " let trie_data = [48, 97, 176, 98, 32868];"] # [doc = " let trie = Char16Trie::new(ZeroVec::from_slice_or_alloc(&trie_data));"] # [doc = ""] # [doc = " let mut iter = trie.iter();"] # [doc = " let res = iter.next('a');"] # [doc = " assert_eq!(res, TrieResult::Intermediate(1));"] # [doc = " let res = iter.next('b');"] # [doc = " assert_eq!(res, TrieResult::FinalValue(100));"] # [doc = " let res = iter.next('c');"] # [doc = " assert_eq!(res, TrieResult::NoMatch);"] # [doc = " ```"] pub fn next32 (& mut self , c : u32) -> TrieResult { if c <= 0xffff { self . next16 (c as u16) } else { match self . next16 (u16_lead (c as i32)) { TrieResult :: NoValue | TrieResult :: Intermediate (_) => { self . next16 (u16_tail (c as i32)) } _ => TrieResult :: NoMatch , } } } # [doc = " Traverses the trie from the current state for this input char."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use icu::collections::char16trie::{Char16Trie, TrieResult};"] # [doc = " use zerovec::ZeroVec;"] # [doc = ""] # [doc = " // A Char16Trie containing the ASCII characters 'a' and 'b'."] # [doc = " let trie_data = [48, 97, 176, 98, 32868];"] # [doc = " let trie = Char16Trie::new(ZeroVec::from_slice_or_alloc(&trie_data));"] # [doc = ""] # [doc = " let mut iter = trie.iter();"] # [doc = " let res = iter.next16('a' as u16);"] # [doc = " assert_eq!(res, TrieResult::Intermediate(1));"] # [doc = " let res = iter.next16('b' as u16);"] # [doc = " assert_eq!(res, TrieResult::FinalValue(100));"] # [doc = " let res = iter.next16('c' as u16);"] # [doc = " assert_eq!(res, TrieResult::NoMatch);"] # [doc = " ```"] pub fn next16 (& mut self , c : u16) -> TrieResult { let mut pos = match self . pos { Some (p) => p , None => return TrieResult :: NoMatch , } ; if let Some (length) = self . remaining_match_length { if c == trie_unwrap ! (self . trie . get (pos)) { pos += 1 ; self . pos = Some (pos) ; if length == 0 { self . remaining_match_length = None ; let node = trie_unwrap ! (self . trie . get (pos)) ; if node >= MIN_VALUE_LEAD { return self . value_result (pos) ; } } else { self . remaining_match_length = Some (length - 1) ; } return TrieResult :: NoValue ; } self . stop () ; TrieResult :: NoMatch } else { self . next_impl (pos , c) } } fn branch_next (& mut self , pos : usize , length : usize , in_unit : u16) -> TrieResult { let mut pos = pos ; let mut length = length ; if length == 0 { length = trie_unwrap ! (self . trie . get (pos)) as usize ; pos += 1 ; } length += 1 ; while length > MAX_BRANCH_LINEAR_SUB_NODE_LENGTH { if in_unit < trie_unwrap ! (self . trie . get (pos)) { length >>= 1 ; pos = trie_unwrap ! (self . jump_by_delta (pos + 1)) ; } else { length = length - (length >> 1) ; pos = trie_unwrap ! (self . skip_delta (pos + 1)) ; } } loop { if in_unit == trie_unwrap ! (self . trie . get (pos)) { pos += 1 ; let mut node = trie_unwrap ! (self . trie . get (pos)) ; if node & VALUE_IS_FINAL != 0 { self . pos = Some (pos) ; return self . value_result (pos) ; } pos += 1 ; if node < MIN_TWO_UNIT_VALUE_LEAD { pos += node as usize ; } else if node < THREE_UNIT_VALUE_LEAD { pos += (((node - MIN_TWO_UNIT_VALUE_LEAD) as u32) << 16) as usize | trie_unwrap ! (self . trie . get (pos)) as usize ; pos += 1 ; } else { pos += ((trie_unwrap ! (self . trie . get (pos)) as usize) << 16) | trie_unwrap ! (self . trie . get (pos + 1)) as usize ; pos += 2 ; } node = trie_unwrap ! (self . trie . get (pos)) ; self . pos = Some (pos) ; if node >= MIN_VALUE_LEAD { return self . value_result (pos) ; } return TrieResult :: NoValue ; } length -= 1 ; pos = trie_unwrap ! (self . skip_value (pos + 1)) ; if length <= 1 { break ; } } if in_unit == trie_unwrap ! (self . trie . get (pos)) { pos += 1 ; self . pos = Some (pos) ; let node = trie_unwrap ! (self . trie . get (pos)) ; if node >= MIN_VALUE_LEAD { return self . value_result (pos) ; } TrieResult :: NoValue } else { self . stop () ; TrieResult :: NoMatch } } fn next_impl (& mut self , pos : usize , in_unit : u16) -> TrieResult { let mut node = trie_unwrap ! (self . trie . get (pos)) ; let mut pos = pos + 1 ; loop { if node < MIN_LINEAR_MATCH { return self . branch_next (pos , node as usize , in_unit) ; } else if node < MIN_VALUE_LEAD { let length = node - MIN_LINEAR_MATCH ; if in_unit == trie_unwrap ! (self . trie . get (pos)) { pos += 1 ; if length == 0 { self . remaining_match_length = None ; self . pos = Some (pos) ; node = trie_unwrap ! (self . trie . get (pos)) ; if node >= MIN_VALUE_LEAD { return self . value_result (pos) ; } return TrieResult :: NoValue ; } self . remaining_match_length = Some (length as usize - 1) ; self . pos = Some (pos) ; return TrieResult :: NoValue ; } break ; } else if (node & VALUE_IS_FINAL) != 0 { break ; } else { pos = skip_node_value (pos , node) ; node &= NODE_TYPE_MASK ; } } self . stop () ; TrieResult :: NoMatch } fn stop (& mut self) { self . pos = None ; } # [inline (always)] fn jump_by_delta (& self , pos : usize) -> Option < usize > { let delta = self . trie . get (pos) ? ; let v = if delta < MIN_TWO_UNIT_DELTA_LEAD { pos + 1 + delta as usize } else if delta == THREE_UNIT_DELTA_LEAD { let delta = ((self . trie . get (pos + 1) ? as usize) << 16) | (self . trie . get (pos + 2) ? as usize) ; pos + delta + 3 } else { let delta = (((delta - MIN_TWO_UNIT_DELTA_LEAD) as usize) << 16) | (self . trie . get (pos + 1) ? as usize) ; pos + delta + 2 } ; Some (v) } # [inline (always)] fn skip_value (& self , pos : usize) -> Option < usize > { let lead_unit = self . trie . get (pos) ? ; Some (skip_value (pos + 1 , lead_unit & 0x7fff)) } # [inline (always)] fn skip_delta (& self , pos : usize) -> Option < usize > { let delta = self . trie . get (pos) ? ; let v = if delta < MIN_TWO_UNIT_DELTA_LEAD { pos + 1 } else if delta == THREE_UNIT_DELTA_LEAD { pos + 3 } else { pos + 2 } ; Some (v) } fn value_result (& self , pos : usize) -> TrieResult { match self . get_value (pos) { Some (result) => result , None => { debug_assert ! (false) ; TrieResult :: NoMatch } } } # [inline (always)] fn get_value (& self , pos : usize) -> Option < TrieResult > { let lead_unit = self . trie . get (pos) ? ; if lead_unit & VALUE_IS_FINAL == VALUE_IS_FINAL { self . read_value (pos + 1 , lead_unit & 0x7fff) . map (TrieResult :: FinalValue) } else { self . read_node_value (pos + 1 , lead_unit) . map (TrieResult :: Intermediate) } } # [inline (always)] fn read_value (& self , pos : usize , lead_unit : u16) -> Option < i32 > { let v = if lead_unit < MIN_TWO_UNIT_VALUE_LEAD { lead_unit . into () } else if lead_unit < THREE_UNIT_VALUE_LEAD { (((lead_unit - MIN_TWO_UNIT_VALUE_LEAD) as i32) << 16) | self . trie . get (pos) ? as i32 } else { ((self . trie . get (pos) ? as i32) << 16) | self . trie . get (pos + 1) ? as i32 } ; Some (v) } # [inline (always)] fn read_node_value (& self , pos : usize , lead_unit : u16) -> Option < i32 > { let v = if lead_unit < (MIN_TWO_UNIT_NODE_VALUE_LEAD) { ((lead_unit >> 6) - 1) . into () } else if lead_unit < THREE_UNIT_NODE_VALUE_LEAD { ((((lead_unit & 0x7fc0) - MIN_TWO_UNIT_NODE_VALUE_LEAD) as i32) << 10) | self . trie . get (pos) ? as i32 } else { ((self . trie . get (pos) ? as i32) << 16) | self . trie . get (pos + 1) ? as i32 } ; Some (v) } }
};
}
