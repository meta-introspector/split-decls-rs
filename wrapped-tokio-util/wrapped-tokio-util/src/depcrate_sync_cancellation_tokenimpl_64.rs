// Generated macro for impl_64 (impl)
macro_rules! Depcrate_sync_cancellation_tokenimpl_64 {
() => {
// Module: crate::sync::cancellation_token
// Provides: {"impl_64"}
// Dependencies: {}
impl CancellationToken { # [doc = " Creates a new [`CancellationToken`] in the non-cancelled state."] pub fn new () -> CancellationToken { CancellationToken { inner : Arc :: new (tree_node :: TreeNode :: new ()) , } } # [doc = " Creates a [`CancellationToken`] which will get cancelled whenever the"] # [doc = " current token gets cancelled. Unlike a cloned [`CancellationToken`],"] # [doc = " cancelling a child token does not cancel the parent token."] # [doc = ""] # [doc = " If the current token is already cancelled, the child token will get"] # [doc = " returned in cancelled state."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use tokio::select;"] # [doc = " use tokio_util::sync::CancellationToken;"] # [doc = ""] # [doc = " #[tokio::main]"] # [doc = " async fn main() {"] # [doc = "     let token = CancellationToken::new();"] # [doc = "     let child_token = token.child_token();"] # [doc = ""] # [doc = "     let join_handle = tokio::spawn(async move {"] # [doc = "         // Wait for either cancellation or a very long time"] # [doc = "         select! {"] # [doc = "             _ = child_token.cancelled() => {"] # [doc = "                 // The token was cancelled"] # [doc = "                 5"] # [doc = "             }"] # [doc = "             _ = tokio::time::sleep(std::time::Duration::from_secs(9999)) => {"] # [doc = "                 99"] # [doc = "             }"] # [doc = "         }"] # [doc = "     });"] # [doc = ""] # [doc = "     tokio::spawn(async move {"] # [doc = "         tokio::time::sleep(std::time::Duration::from_millis(10)).await;"] # [doc = "         token.cancel();"] # [doc = "     });"] # [doc = ""] # [doc = "     assert_eq!(5, join_handle.await.unwrap());"] # [doc = " }"] # [doc = " ```"] pub fn child_token (& self) -> CancellationToken { CancellationToken { inner : tree_node :: child_node (& self . inner) , } } # [doc = " Cancel the [`CancellationToken`] and all child tokens which had been"] # [doc = " derived from it."] # [doc = ""] # [doc = " This will wake up all tasks which are waiting for cancellation."] # [doc = ""] # [doc = " Be aware that cancellation is not an atomic operation. It is possible"] # [doc = " for another thread running in parallel with a call to `cancel` to first"] # [doc = " receive `true` from `is_cancelled` on one child node, and then receive"] # [doc = " `false` from `is_cancelled` on another child node. However, once the"] # [doc = " call to `cancel` returns, all child nodes have been fully cancelled."] pub fn cancel (& self) { tree_node :: cancel (& self . inner) ; } # [doc = " Returns `true` if the `CancellationToken` is cancelled."] pub fn is_cancelled (& self) -> bool { tree_node :: is_cancelled (& self . inner) } # [doc = " Returns a [`Future`] that gets fulfilled when cancellation is requested."] # [doc = ""] # [doc = " Equivalent to:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " async fn cancelled(&self);"] # [doc = " ```"] # [doc = ""] # [doc = " The future will complete immediately if the token is already cancelled"] # [doc = " when this method is called."] # [doc = ""] # [doc = " # Cancellation safety"] # [doc = ""] # [doc = " This method is cancel safe."] pub fn cancelled (& self) -> WaitForCancellationFuture < '_ > { WaitForCancellationFuture { cancellation_token : self , future : self . inner . notified () , } } # [doc = " Returns a [`Future`] that gets fulfilled when cancellation is requested."] # [doc = ""] # [doc = " Equivalent to:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " async fn cancelled_owned(self);"] # [doc = " ```"] # [doc = ""] # [doc = " The future will complete immediately if the token is already cancelled"] # [doc = " when this method is called."] # [doc = ""] # [doc = " The function takes self by value and returns a future that owns the"] # [doc = " token."] # [doc = ""] # [doc = " # Cancellation safety"] # [doc = ""] # [doc = " This method is cancel safe."] pub fn cancelled_owned (self) -> WaitForCancellationFutureOwned { WaitForCancellationFutureOwned :: new (self) } # [doc = " Creates a [`DropGuard`] for this token."] # [doc = ""] # [doc = " Returned guard will cancel this token (and all its children) on drop"] # [doc = " unless disarmed."] pub fn drop_guard (self) -> DropGuard { DropGuard { inner : Some (self) } } # [doc = " Creates a [`DropGuardRef`] for this token."] # [doc = ""] # [doc = " Returned guard will cancel this token (and all its children) on drop"] # [doc = " unless disarmed."] pub fn drop_guard_ref (& self) -> DropGuardRef < '_ > { DropGuardRef { inner : Some (self) } } # [doc = " Runs a future to completion and returns its result wrapped inside of an `Option`"] # [doc = " unless the [`CancellationToken`] is cancelled. In that case the function returns"] # [doc = " `None` and the future gets dropped."] # [doc = ""] # [doc = " # Fairness"] # [doc = ""] # [doc = " Calling this on an already-cancelled token directly returns `None`."] # [doc = " For all subsequent polls, in case of concurrent completion and"] # [doc = " cancellation, this is biased towards the future completion."] # [doc = ""] # [doc = " # Cancellation safety"] # [doc = ""] # [doc = " This method is only cancel safe if `fut` is cancel safe."] pub async fn run_until_cancelled < F > (& self , fut : F) -> Option < F :: Output > where F : Future , { if self . is_cancelled () { None } else { RunUntilCancelledFuture { cancellation : self . cancelled () , future : fut , } . await } } # [doc = " Runs a future to completion and returns its result wrapped inside of an `Option`"] # [doc = " unless the [`CancellationToken`] is cancelled. In that case the function returns"] # [doc = " `None` and the future gets dropped."] # [doc = ""] # [doc = " The function takes self by value and returns a future that owns the token."] # [doc = ""] # [doc = " # Fairness"] # [doc = ""] # [doc = " Calling this on an already-cancelled token directly returns `None`."] # [doc = " For all subsequent polls, in case of concurrent completion and"] # [doc = " cancellation, this is biased towards the future completion."] # [doc = ""] # [doc = " # Cancellation safety"] # [doc = ""] # [doc = " This method is only cancel safe if `fut` is cancel safe."] pub async fn run_until_cancelled_owned < F > (self , fut : F) -> Option < F :: Output > where F : Future , { self . run_until_cancelled (fut) . await } }
};
}
