// Generated macro for Virtq (trait)
macro_rules! Depcrate_drivers_virtio_virtqueueVirtq {
() => {
// Module: crate::drivers::virtio::virtqueue
// Provides: {"Virtq"}
// Dependencies: {}
# [doc = " The Virtq trait unifies access to the two different Virtqueue types"] # [doc = " [packed::PackedVq] and [split::SplitVq]."] # [doc = ""] # [doc = " The trait provides a common interface for both types. Which in some case"] # [doc = " might not provide the complete feature set of each queue. Drivers who"] # [doc = " do need these features should refrain from providing support for both"] # [doc = " Virtqueue types and use the structs directly instead."] # [enum_dispatch] pub trait Virtq : Send { # [doc = " The `notif` parameter indicates if the driver wants to have a notification for this specific"] # [doc = " transfer. This is only for performance optimization. As it is NOT ensured, that the device sees the"] # [doc = " updated notification flags before finishing transfers!"] fn dispatch (& mut self , tkn : AvailBufferToken , notif : bool , buffer_type : BufferType ,) -> Result < () , VirtqError > ; # [doc = " Dispatches the provided TransferToken to the respective queue and does"] # [doc = " return when, the queue finished the transfer."] # [doc = ""] # [doc = " The returned [UsedBufferToken] can be copied from"] # [doc = " or return the underlying buffers."] # [doc = ""] # [doc = " **INFO:**"] # [doc = " Currently this function is constantly polling the queue while keeping the notifications disabled."] # [doc = " Upon finish notifications are enabled again."] fn dispatch_blocking (& mut self , tkn : AvailBufferToken , buffer_type : BufferType ,) -> Result < UsedBufferToken , VirtqError > { self . dispatch (tkn , false , buffer_type) ? ; self . disable_notifs () ; let result : UsedBufferToken ; loop { if let Ok (buffer_tkn) = self . try_recv () { result = buffer_tkn ; break ; } } self . enable_notifs () ; Ok (result) } # [doc = " Enables interrupts for this virtqueue upon receiving a transfer"] fn enable_notifs (& mut self) ; # [doc = " Disables interrupts for this virtqueue upon receiving a transfer"] fn disable_notifs (& mut self) ; # [doc = " Checks if new used descriptors have been written by the device."] # [doc = " This activates the queue and polls the descriptor ring of the queue."] fn try_recv (& mut self) -> Result < UsedBufferToken , VirtqError > ; # [doc = " Dispatches a batch of [AvailBufferToken]s. The buffers are provided to the queue in"] # [doc = " sequence. After the last buffer has been written, the queue marks the first buffer as available and triggers"] # [doc = " a device notification if wanted by the device."] # [doc = ""] # [doc = " The `notif` parameter indicates if the driver wants to have a notification for this specific"] # [doc = " transfer. This is only for performance optimization. As it is NOT ensured, that the device sees the"] # [doc = " updated notification flags before finishing transfers!"] fn dispatch_batch (& mut self , tkns : Vec < (AvailBufferToken , BufferType) > , notif : bool ,) -> Result < () , VirtqError > ; # [doc = " Dispatches a batch of [AvailBufferToken]s. The tokens will be placed in to the `await_queue`"] # [doc = " upon finish."] # [doc = ""] # [doc = " The `notif` parameter indicates if the driver wants to have a notification for this specific"] # [doc = " transfer. This is only for performance optimization. As it is NOT ensured, that the device sees the"] # [doc = " updated notification flags before finishing transfers!"] # [doc = ""] # [doc = " The buffers are provided to the queue in"] # [doc = " sequence. After the last buffer has been written, the queue marks the first buffer as available and triggers"] # [doc = " a device notification if wanted by the device."] # [doc = ""] # [doc = " Tokens to get a reference to the provided await_queue, where they will be placed upon finish."] fn dispatch_batch_await (& mut self , tkns : Vec < (AvailBufferToken , BufferType) > , notif : bool ,) -> Result < () , VirtqError > ; # [doc = " Returns the size of a Virtqueue. This represents the overall size and not the capacity the"] # [doc = " queue currently has for new descriptors."] fn size (& self) -> u16 ; fn index (& self) -> u16 ; fn has_used_buffers (& self) -> bool ; }
};
}
