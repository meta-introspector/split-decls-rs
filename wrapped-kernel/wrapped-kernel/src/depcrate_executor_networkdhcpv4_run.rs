// Generated macro for dhcpv4_run (function)
macro_rules! Depcrate_executor_networkdhcpv4_run {
() => {
// Module: crate::executor::network
// Provides: {"dhcpv4_run"}
// Dependencies: {}
# [cfg (feature = "dhcpv4")] async fn dhcpv4_run () { future :: poll_fn (| cx | { let Some (mut guard) = NIC . try_lock () else { cx . waker () . wake_by_ref () ; return Poll :: Pending ; } ; let nic = guard . as_nic_mut () . unwrap () ; let dhcp_handle = nic . dhcp_handle ; let socket = nic . sockets . get_mut :: < dhcpv4 :: Socket < '_ > > (dhcp_handle) ; socket . register_waker (cx . waker ()) ; match socket . poll () { None => { } Some (dhcpv4 :: Event :: Configured (config)) => { info ! ("DHCP config acquired!") ; info ! ("IP address:   {}" , config . address) ; nic . iface . update_ip_addrs (| addrs | { if let Some (dest) = addrs . iter_mut () . next () { * dest = IpCidr :: Ipv4 (config . address) ; } else if addrs . push (IpCidr :: Ipv4 (config . address)) . is_err () { info ! ("Unable to update IP address") ; } }) ; if let Some (router) = config . router { info ! ("Gateway:      {router}") ; nic . iface . routes_mut () . add_default_ipv4_route (router) . unwrap () ; } else { info ! ("Gateway:      None") ; nic . iface . routes_mut () . remove_default_ipv4_route () ; } # [cfg (feature = "dns")] let mut dns_servers : Vec < IpAddress > = Vec :: new () ; for (i , s) in config . dns_servers . iter () . enumerate () { info ! ("DNS server {i}: {s}") ; # [cfg (feature = "dns")] dns_servers . push (IpAddress :: Ipv4 (* s)) ; } # [cfg (feature = "dns")] if ! dns_servers . is_empty () { let dns_socket = dns :: Socket :: new (dns_servers . as_slice () , vec ! []) ; nic . dns_handle = Some (nic . sockets . add (dns_socket)) ; } } Some (dhcpv4 :: Event :: Deconfigured) => { info ! ("DHCP lost config!") ; let cidr = Ipv4Cidr :: new (Ipv4Address :: UNSPECIFIED , 0) ; nic . iface . update_ip_addrs (| addrs | { if let Some (dest) = addrs . iter_mut () . next () { * dest = IpCidr :: Ipv4 (cidr) ; } }) ; nic . iface . routes_mut () . remove_default_ipv4_route () ; # [cfg (feature = "dns")] { if let Some (dns_handle) = nic . dns_handle { nic . sockets . remove (dns_handle) ; } nic . dns_handle = None ; } } } ; Poll :: < () > :: Pending }) . await ; }
};
}
