// Generated macro for block_on (function)
macro_rules! Depcrate_executorblock_on {
() => {
// Module: crate::executor
// Provides: {"block_on"}
// Dependencies: {}
# [doc = " Blocks the current thread on `f`, running the executor when idling."] pub (crate) fn block_on < F , T > (future : F , timeout : Option < Duration >) -> io :: Result < T > where F : Future < Output = io :: Result < T > > , { let backoff = Backoff :: new () ; let start = crate :: arch :: kernel :: systemtime :: now_micros () ; let task_notify = Arc :: new (TaskNotify :: new ()) ; let waker = task_notify . clone () . into () ; let mut cx = Context :: from_waker (& waker) ; let mut future = pin ! (future) ; loop { let result = future . as_mut () . poll (& mut cx) ; run () ; let now = crate :: arch :: kernel :: systemtime :: now_micros () ; if let Poll :: Ready (t) = result { # [cfg (feature = "net")] { if let Some (mut guard) = crate :: executor :: network :: NIC . try_lock () { let delay = if let Ok (nic) = guard . as_nic_mut () { nic . set_polling_mode (false) ; nic . poll_delay (Instant :: from_micros_const (now . try_into () . unwrap ())) . map (| d | d . total_micros ()) } else { None } ; core_local :: core_scheduler () . add_network_timer (delay . map (| d | crate :: arch :: processor :: get_timer_ticks () + d) ,) ; } } return t ; } if let Some (duration) = timeout && Duration :: from_micros (now - start) >= duration { # [cfg (feature = "net")] { if let Some (mut guard) = crate :: executor :: network :: NIC . try_lock () { let delay = if let Ok (nic) = guard . as_nic_mut () { nic . set_polling_mode (false) ; nic . poll_delay (Instant :: from_micros_const (now . try_into () . unwrap ())) . map (| d | d . total_micros ()) } else { None } ; core_local :: core_scheduler () . add_network_timer (delay . map (| d | crate :: arch :: processor :: get_timer_ticks () + d) ,) ; } } return Err (Errno :: Time) ; } # [cfg (feature = "net")] if backoff . is_completed () { let delay = if let Some (mut guard) = crate :: executor :: network :: NIC . try_lock () { if let Ok (nic) = guard . as_nic_mut () { nic . set_polling_mode (false) ; nic . poll_delay (Instant :: from_micros_const (now . try_into () . unwrap ())) . map (| d | d . total_micros ()) } else { None } } else { None } ; if delay . unwrap_or (10_000_000) > 10_000 { core_local :: core_scheduler () . add_network_timer (delay . map (| d | crate :: arch :: processor :: get_timer_ticks () + d) ,) ; let wakeup_time = timeout . map (| duration | start + u64 :: try_from (duration . as_micros ()) . unwrap ()) ; task_notify . wait (wakeup_time) ; if let Ok (nic) = crate :: executor :: network :: NIC . lock () . as_nic_mut () { nic . set_polling_mode (true) ; } backoff . reset () ; } } else { backoff . snooze () ; } # [cfg (not (feature = "net"))] { if backoff . is_completed () { let wakeup_time = timeout . map (| duration | start + u64 :: try_from (duration . as_micros ()) . unwrap ()) ; task_notify . wait (wakeup_time) ; backoff . reset () ; } else { backoff . snooze () ; } } } }
};
}
