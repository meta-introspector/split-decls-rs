// Generated macro for impl_613 (impl)
macro_rules! Depcrate_drivers_virtio_virtqueue_splitimpl_613 {
() => {
// Module: crate::drivers::virtio::virtqueue::split
// Provides: {"impl_613"}
// Dependencies: {}
impl DescrRing { fn descr_table_mut (& mut self) -> & mut [MaybeUninit < virtq :: Desc >] { unsafe { & mut * self . descr_table_cell . get () } } fn avail_ring (& self) -> & virtq :: Avail { unsafe { & * self . avail_ring_cell . get () } } fn avail_ring_mut (& mut self) -> & mut virtq :: Avail { unsafe { & mut * self . avail_ring_cell . get () } } fn used_ring (& self) -> & virtq :: Used { unsafe { & * self . used_ring_cell . get () } } fn push (& mut self , tkn : TransferToken < virtq :: Desc >) -> Result < u16 , VirtqError > { let mut index ; if let Some (ctrl_desc) = tkn . ctrl_desc . as_ref () { let descriptor = SplitVq :: indirect_desc (ctrl_desc . as_ref ()) ; index = self . mem_pool . pool . pop () . ok_or (VirtqError :: NoDescrAvail) ? ; self . descr_table_mut () [usize :: from (index)] = MaybeUninit :: new (descriptor) ; } else { let mut rev_all_desc_iter = SplitVq :: descriptor_iter (& tkn . buff_tkn) ? . rev () ; { let descriptor = rev_all_desc_iter . next () . unwrap () ; index = self . mem_pool . pool . pop () . ok_or (VirtqError :: NoDescrAvail) ? ; self . descr_table_mut () [usize :: from (index)] = MaybeUninit :: new (descriptor) ; } for mut descriptor in rev_all_desc_iter { descriptor . next = le16 :: from (index) ; index = self . mem_pool . pool . pop () . ok_or (VirtqError :: NoDescrAvail) ? ; self . descr_table_mut () [usize :: from (index)] = MaybeUninit :: new (descriptor) ; } } self . token_ring [usize :: from (index)] = Some (Box :: new (tkn)) ; let len = self . token_ring . len () ; let idx = self . avail_ring_mut () . idx . to_ne () ; self . avail_ring_mut () . ring_mut (true) [idx as usize % len] = index . into () ; memory_barrier () ; let next_idx = idx . wrapping_add (1) ; self . avail_ring_mut () . idx = next_idx . into () ; Ok (next_idx) } fn try_recv (& mut self) -> Result < UsedBufferToken , VirtqError > { if self . read_idx == self . used_ring () . idx . to_ne () { return Err (VirtqError :: NoNewUsed) ; } let cur_ring_index = self . read_idx as usize % self . token_ring . len () ; let used_elem = self . used_ring () . ring () [cur_ring_index] ; let tkn = self . token_ring [used_elem . id . to_ne () as usize] . take () . expect ("The buff_id is incorrect or the reference to the TransferToken was misplaced." ,) ; let mut id_ret_idx = u16 :: try_from (used_elem . id . to_ne ()) . unwrap () ; loop { self . mem_pool . ret_id (id_ret_idx) ; let cur_chain_elem = unsafe { self . descr_table_mut () [usize :: from (id_ret_idx)] . assume_init () } ; if cur_chain_elem . flags . contains (virtq :: DescF :: NEXT) { id_ret_idx = cur_chain_elem . next . to_ne () ; } else { break ; } } memory_barrier () ; self . read_idx = self . read_idx . wrapping_add (1) ; Ok (UsedBufferToken :: from_avail_buffer_token (tkn . buff_tkn , used_elem . len . to_ne () ,)) } fn drv_enable_notif (& mut self) { self . avail_ring_mut () . flags . remove (virtq :: AvailF :: NO_INTERRUPT) ; } fn drv_disable_notif (& mut self) { self . avail_ring_mut () . flags . insert (virtq :: AvailF :: NO_INTERRUPT) ; } fn dev_is_notif (& self) -> bool { ! self . used_ring () . flags . contains (virtq :: UsedF :: NO_NOTIFY) } }
};
}
