// Generated macro for Environment (struct)
macro_rules! Depcrate_envEnvironment {
() => {
// Module: crate::env
// Provides: {"Environment"}
// Dependencies: {}
# [doc = " An environment source collects a dictionary of environment variables values into a hierarchical"] # [doc = " config Value type. We have to be aware how the config tree is created from the environment"] # [doc = " dictionary, therefore we are mindful about prefixes for the environment keys, level separators,"] # [doc = " encoding form (kebab, snake case) etc."] # [must_use] # [derive (Clone , Debug , Default)] pub struct Environment { # [doc = " Optional prefix that will limit access to the environment to only keys that"] # [doc = " begin with the defined prefix."] # [doc = ""] # [doc = " The prefix is tested to be present on each key before it's considered to be part of the"] # [doc = " source environment. The separator character can be set through"] # [doc = " [`prefix_separator`](Environment::prefix_separator())."] # [doc = ""] # [doc = " For example, the key `CONFIG_DEBUG` would become `DEBUG` with a prefix of `config`."] prefix : Option < String > , # [doc = " Optional character sequence that separates the prefix from the rest of the key."] # [doc = ""] # [doc = " Defaults to [`separator`](Environment::separator()) if that is set, otherwise `_`."] prefix_separator : Option < String > , # [doc = " Optional character sequence that separates each key segment in an environment key pattern."] # [doc = " Consider a nested configuration such as `redis.password`, a separator of `_` would allow"] # [doc = " an environment key of `REDIS_PASSWORD` to match."] separator : Option < String > , # [doc = " Optional directive to translate collected keys into a form that matches what serializers"] # [doc = " that the configuration would expect. For example if you have the `kebab-case` attribute"] # [doc = " for your serde config types, you may want to pass `Case::Kebab` here."] # [cfg (feature = "convert-case")] convert_case : Option < Case > , # [doc = " Optional character sequence that separates each env value into a vector. only works when `try_parsing` is set to true"] # [doc = " Once set, you cannot have type String on the same environment, unless you set `list_parse_keys`."] list_separator : Option < String > , # [doc = " A list of keys which should always be parsed as a list. If not set you can have only `Vec<String>` or `String` (not both) in one environment."] list_parse_keys : Option < Vec < String > > , # [doc = " Ignore empty env values (treat as unset)."] ignore_empty : bool , # [doc = " Parses booleans, integers and floats if they're detected (can be safely parsed)."] try_parsing : bool , keep_prefix : bool , # [doc = " Alternate source for the environment. This can be used when you want to test your own code"] # [doc = " using this source, without the need to change the actual system environment variables."] # [doc = ""] # [doc = " ## Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use config::{Environment, Config};"] # [doc = " # use serde::Deserialize;"] # [doc = " # use std::collections::HashMap;"] # [doc = " # use std::convert::TryInto;"] # [doc = " #"] # [doc = " #[test]"] # [doc = " fn test_config() -> Result<(), config::ConfigError> {"] # [doc = "   #[derive(Clone, Debug, Deserialize)]"] # [doc = "   struct MyConfig {"] # [doc = "     pub my_string: String,"] # [doc = "   }"] # [doc = ""] # [doc = "   let source = Environment::default()"] # [doc = "     .source(Some({"] # [doc = "       let mut env = HashMap::new();"] # [doc = "       env.insert(\"MY_STRING\".into(), \"my-value\".into());"] # [doc = "       env"] # [doc = "   }));"] # [doc = ""] # [doc = "   let config: MyConfig = Config::builder()"] # [doc = "     .add_source(source)"] # [doc = "     .build()?"] # [doc = "     .try_into()?;"] # [doc = "   assert_eq!(config.my_string, \"my-value\");"] # [doc = ""] # [doc = "   Ok(())"] # [doc = " }"] # [doc = " ```"] source : Option < Map < String , String > > , }
};
}
