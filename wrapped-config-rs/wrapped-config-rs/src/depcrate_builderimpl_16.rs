// Generated macro for impl_16 (impl)
macro_rules! Depcrate_builderimpl_16 {
() => {
// Module: crate::builder
// Provides: {"impl_16"}
// Dependencies: {}
# [doc = " Operations allowed in async state"] impl ConfigBuilder < AsyncState > { # [doc = " Registers new [`Source`] in this builder."] # [doc = ""] # [doc = " Calling this method does not invoke any I/O. [`Source`] is only saved in internal register for later use."] pub fn add_source < T > (mut self , source : T) -> Self where T : Source + Send + Sync + 'static , { self . state . sources . push (SourceType :: Sync (Box :: new (source))) ; self } # [doc = " Registers new [`AsyncSource`] in this builder."] # [doc = ""] # [doc = " Calling this method does not invoke any I/O. [`AsyncSource`] is only saved in internal register for later use."] # [cfg (feature = "async")] pub fn add_async_source < T > (mut self , source : T) -> Self where T : AsyncSource + Send + Sync + 'static , { self . state . sources . push (SourceType :: Async (Box :: new (source))) ; self } # [doc = " Reads all registered defaults, [`Source`]s, [`AsyncSource`]s and overrides."] # [doc = ""] # [doc = " This is the method that invokes all I/O operations."] # [doc = " For a non consuming alternative see [`build_cloned`](Self::build_cloned)"] # [doc = ""] # [doc = " # Errors"] # [doc = " If source collection fails, be it technical reasons or related to inability to read data as `Config` for different reasons,"] # [doc = " this method returns error."] pub async fn build (self) -> Result < Config > { Self :: build_internal (self . defaults , self . overrides , & self . state . sources) . await } # [doc = " Reads all registered defaults, [`Source`]s, [`AsyncSource`]s and overrides."] # [doc = ""] # [doc = " Similar to [`build`](Self::build), but it does not take ownership of `ConfigBuilder` to allow later reuse."] # [doc = " Internally it clones data to achieve it."] # [doc = ""] # [doc = " # Errors"] # [doc = " If source collection fails, be it technical reasons or related to inability to read data as `Config` for different reasons,"] # [doc = " this method returns error."] pub async fn build_cloned (& self) -> Result < Config > { Self :: build_internal (self . defaults . clone () , self . overrides . clone () , & self . state . sources ,) . await } async fn build_internal (defaults : Map < Expression , Value > , overrides : Map < Expression , Value > , sources : & [SourceType] ,) -> Result < Config > { let mut cache : Value = Map :: < String , Value > :: new () . into () ; for (key , val) in defaults { key . set (& mut cache , val) ; } for source in sources . iter () { match source { SourceType :: Sync (source) => source . collect_to (& mut cache) ? , # [cfg (feature = "async")] SourceType :: Async (source) => source . collect_to (& mut cache) . await ? , } } for (key , val) in overrides { key . set (& mut cache , val) ; } Ok (Config :: new (cache)) } }
};
}
