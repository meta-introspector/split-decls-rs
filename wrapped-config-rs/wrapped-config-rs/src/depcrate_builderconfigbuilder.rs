// Generated macro for ConfigBuilder (struct)
macro_rules! Depcrate_builderConfigBuilder {
() => {
// Module: crate::builder
// Provides: {"ConfigBuilder"}
// Dependencies: {}
# [doc = " A configuration builder"] # [doc = ""] # [doc = " It registers ordered sources of configuration to later build consistent [`Config`] from them."] # [doc = " Configuration sources it defines are defaults, [`Source`]s and overrides."] # [doc = ""] # [doc = " Defaults are always loaded first and can be overwritten by any of two other sources."] # [doc = " Overrides are always loaded last, thus cannot be overridden."] # [doc = " Both can be only set explicitly key by key in code"] # [doc = " using [`set_default`](Self::set_default) or [`set_override`](Self::set_override)."] # [doc = ""] # [doc = " An intermediate category, [`Source`], set groups of keys at once implicitly using data coming from external sources"] # [doc = " like files, environment variables or others that one implements. Defining a [`Source`] is as simple as implementing"] # [doc = " a trait for a struct."] # [doc = ""] # [doc = " Adding sources, setting defaults and overrides does not invoke any I/O nor builds a config."] # [doc = " It happens on demand when [`build`](Self::build) (or its alternative) is called."] # [doc = " Therefore all errors, related to any of the [`Source`] will only show up then."] # [doc = ""] # [doc = " # Sync and async builder"] # [doc = ""] # [doc = " [`ConfigBuilder`] uses type parameter to keep track of builder state."] # [doc = ""] # [doc = " In [`DefaultState`] builder only supports [`Source`]s"] # [doc = ""] # [doc = " In [`AsyncState`] it supports both [`Source`]s and [`AsyncSource`]s at the price of building using `async fn`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use config::*;"] # [doc = " # use std::error::Error;"] # [doc = " # fn main() -> Result<(), Box<dyn Error>> {"] # [doc = " # #[cfg(feature = \"json\")]"] # [doc = " # {"] # [doc = " let mut builder = Config::builder()"] # [doc = "     .set_default(\"default\", \"1\")?"] # [doc = "     .add_source(File::new(\"config/settings\", FileFormat::Json))"] # [doc = " //  .add_async_source(...)"] # [doc = "     .set_override(\"override\", \"1\")?;"] # [doc = ""] # [doc = " match builder.build() {"] # [doc = "     Ok(config) => {"] # [doc = "         // use your config"] # [doc = "     },"] # [doc = "     Err(e) => {"] # [doc = "         // something went wrong"] # [doc = "     }"] # [doc = " }"] # [doc = " # }"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " If any [`AsyncSource`] is used, the builder will transition to [`AsyncState`]."] # [doc = " In such case, it is required to _await_ calls to [`build`](Self::build) and its non-consuming sibling."] # [doc = ""] # [doc = " Calls can be not chained as well"] # [doc = " ```rust"] # [doc = " # use std::error::Error;"] # [doc = " # use config::*;"] # [doc = " # fn main() -> Result<(), Box<dyn Error>> {"] # [doc = " # #[cfg(feature = \"json\")]"] # [doc = " # {"] # [doc = " let mut builder = Config::builder();"] # [doc = " builder = builder.set_default(\"default\", \"1\")?;"] # [doc = " builder = builder.add_source(File::new(\"config/settings\", FileFormat::Json));"] # [doc = " builder = builder.add_source(File::new(\"config/settings.prod\", FileFormat::Json));"] # [doc = " builder = builder.set_override(\"override\", \"1\")?;"] # [doc = " # }"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " Calling [`Config::builder`](Config::builder) yields builder in the default state."] # [doc = " If having an asynchronous state as the initial state is desired, _turbofish_ notation needs to be used."] # [doc = " ```rust"] # [doc = " # use config::{*, builder::AsyncState};"] # [doc = " let mut builder = ConfigBuilder::<AsyncState>::default();"] # [doc = " ```"] # [doc = ""] # [doc = " If for some reason acquiring builder in default state is required without calling [`Config::builder`](Config::builder)"] # [doc = " it can also be achieved."] # [doc = " ```rust"] # [doc = " # use config::{*, builder::DefaultState};"] # [doc = " let mut builder = ConfigBuilder::<DefaultState>::default();"] # [doc = " ```"] # [derive (Debug , Clone , Default)] # [must_use] pub struct ConfigBuilder < St : BuilderState > { defaults : Map < Expression , Value > , overrides : Map < Expression , Value > , state : St , }
};
}
