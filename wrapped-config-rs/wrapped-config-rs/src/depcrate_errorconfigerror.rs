// Generated macro for ConfigError (enum)
macro_rules! Depcrate_errorConfigError {
() => {
// Module: crate::error
// Provides: {"ConfigError"}
// Dependencies: {}
# [doc = " Represents all possible errors that can occur when working with"] # [doc = " configuration."] # [non_exhaustive] pub enum ConfigError { # [doc = " Configuration is frozen and no further mutations can be made."] Frozen , # [doc = " Configuration property was not found"] NotFound (String) , # [doc = " Configuration path could not be parsed."] PathParse { cause : Box < dyn Error + Send + Sync > } , # [doc = " Configuration could not be parsed from file."] FileParse { # [doc = " The URI used to access the file (if not loaded from a string)."] # [doc = " Example: `/path/to/config.json`"] uri : Option < String > , # [doc = " The captured error from attempting to parse the file in its desired format."] # [doc = " This is the actual error object from the library used for the parsing."] cause : Box < dyn Error + Send + Sync > , } , # [doc = " Value could not be converted into the requested type."] Type { # [doc = " The URI that references the source that the value came from."] # [doc = " Example: `/path/to/config.json` or `Environment` or `etcd://localhost`"] origin : Option < String > , # [doc = " What we found when parsing the value"] unexpected : Unexpected , # [doc = " What was expected when parsing the value"] expected : & 'static str , # [doc = " The key in the configuration hash of this value (if available where the"] # [doc = " error is generated)."] key : Option < String > , } , # [doc = " Custom message"] At { # [doc = " Error being extended with a path"] error : Box < ConfigError > , # [doc = " The URI that references the source that the value came from."] # [doc = " Example: `/path/to/config.json` or `Environment` or `etcd://localhost`"] origin : Option < String > , # [doc = " The key in the configuration hash of this value (if available where the"] # [doc = " error is generated)."] key : Option < String > , } , # [doc = " Custom message"] Message (String) , # [doc = " Unadorned error from a foreign origin."] Foreign (Box < dyn Error + Send + Sync >) , }
};
}
