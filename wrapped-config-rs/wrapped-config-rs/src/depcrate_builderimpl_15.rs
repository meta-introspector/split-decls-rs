// Generated macro for impl_15 (impl)
macro_rules! Depcrate_builderimpl_15 {
() => {
// Module: crate::builder
// Provides: {"impl_15"}
// Dependencies: {}
# [doc = " Operations allowed in sync state"] impl ConfigBuilder < DefaultState > { # [doc = " Registers new [`Source`] in this builder."] # [doc = ""] # [doc = " Calling this method does not invoke any I/O. [`Source`] is only saved in internal register for later use."] pub fn add_source < T > (mut self , source : T) -> Self where T : Source + Send + Sync + 'static , { self . state . sources . push (Box :: new (source)) ; self } # [doc = " Registers new [`AsyncSource`] in this builder and forces transition to [`AsyncState`]."] # [doc = ""] # [doc = " Calling this method does not invoke any I/O. [`AsyncSource`] is only saved in internal register for later use."] # [cfg (feature = "async")] pub fn add_async_source < T > (self , source : T) -> ConfigBuilder < AsyncState > where T : AsyncSource + Send + Sync + 'static , { let async_state = ConfigBuilder { state : AsyncState { sources : self . state . sources . into_iter () . map (SourceType :: Sync) . collect () , } , defaults : self . defaults , overrides : self . overrides , } ; async_state . add_async_source (source) } # [doc = " Reads all registered [`Source`]s."] # [doc = ""] # [doc = " This is the method that invokes all I/O operations."] # [doc = " For a non consuming alternative see [`build_cloned`](Self::build_cloned)"] # [doc = ""] # [doc = " # Errors"] # [doc = " If source collection fails, be it technical reasons or related to inability to read data as `Config` for different reasons,"] # [doc = " this method returns error."] pub fn build (self) -> Result < Config > { Self :: build_internal (self . defaults , self . overrides , & self . state . sources) } # [doc = " Reads all registered [`Source`]s."] # [doc = ""] # [doc = " Similar to [`build`](Self::build), but it does not take ownership of `ConfigBuilder` to allow later reuse."] # [doc = " Internally it clones data to achieve it."] # [doc = ""] # [doc = " # Errors"] # [doc = " If source collection fails, be it technical reasons or related to inability to read data as `Config` for different reasons,"] # [doc = " this method returns error."] pub fn build_cloned (& self) -> Result < Config > { Self :: build_internal (self . defaults . clone () , self . overrides . clone () , & self . state . sources ,) } fn build_internal (defaults : Map < Expression , Value > , overrides : Map < Expression , Value > , sources : & [Box < dyn Source + Send + Sync >] ,) -> Result < Config > { let mut cache : Value = Map :: < String , Value > :: new () . into () ; for (key , val) in defaults { key . set (& mut cache , val) ; } sources . collect_to (& mut cache) ? ; for (key , val) in overrides { key . set (& mut cache , val) ; } Ok (Config :: new (cache)) } }
};
}
