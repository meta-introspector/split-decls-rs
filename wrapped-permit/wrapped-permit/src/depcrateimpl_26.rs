// Generated macro for impl_26 (impl)
macro_rules! Depcrateimpl_26 {
() => {
// Module: crate
// Provides: {"impl_26"}
// Dependencies: {}
impl Permit { # [doc = " Makes a new permit."] # [doc = ""] # [doc = " This permit is not subordinate to any other permit."] # [doc = " It has no superior."] # [doc = ""] # [doc = " Dropping the permit revokes it and any subordinate permits."] # [must_use] pub fn new () -> Self { Self { node : Arc :: new (Node :: new_apex ()) , } } # [doc = " Make a new permit that is subordinate to this permit."] # [doc = ""] # [doc = " Call [`revoke()`](#method.revoke) to revoke a permit"] # [doc = " and its subordinate permits, recursively."] # [doc = ""] # [doc = " Dropping the permit revokes it and any subordinate permits."] # [must_use] pub fn new_sub (& self) -> Self { Self { node : self . node . new_sub () , } } # [doc = " Returns `true` if [`revoke()`](#method.revoke) has previously been called"] # [doc = " on this permit or any of its superiors."] # [must_use] pub fn is_revoked (& self) -> bool { self . node . is_revoked () } # [doc = " Returns `Some(())` if [`revoke()`](#method.revoke) has not been called"] # [doc = " on this permit or any of its superiors."] # [must_use] pub fn ok (& self) -> Option < () > { if self . node . is_revoked () { None } else { Some (()) } } # [doc = " Revokes this permit and all subordinate permits."] # [allow (clippy :: must_use_candidate)] pub fn revoke (& self) -> & Self { self . node . revoke_and_remove_from_superior () ; self } # [doc = " Returns `true` if this permit has any subordinate permits that have not"] # [doc = " been dropped."] # [doc = ""] # [doc = " This includes direct subordinates and their subordinates, recursively."] # [allow (clippy :: missing_panics_doc)] # [must_use] pub fn has_subs (& self) -> bool { ! self . node . inner . lock () . unwrap () . subs . is_empty () } # [doc = " Waits until `duration` time passes or the permit is revoked."] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns `Err` when the permit is revoked."] # [allow (clippy :: missing_panics_doc)] pub fn sleep (& self , duration : Duration) -> Result < () , PermitRevoked > { let inner_guard = self . node . inner . lock () . unwrap () ; let (_guard , wait_result) = self . node . condvar . wait_timeout_while (inner_guard , duration , | inner_guard | ! inner_guard . revoked) . unwrap () ; if wait_result . timed_out () { Ok (()) } else { Err (PermitRevoked { }) } } # [doc = " Waits until `deadline` or the permit is revoked."] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns `Err` when the permit is revoked."] pub fn sleep_until (& self , deadline : Instant) -> Result < () , PermitRevoked > { let duration = deadline . saturating_duration_since (Instant :: now ()) ; self . sleep (duration) } # [doc = " Waits for all direct subordinate permits to drop."] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns [`DeadlineExceeded`](struct.DeadlineExceeded.html) when `duration` passes"] # [doc = " and the permit has a subordinate permit."] # [allow (clippy :: missing_panics_doc)] pub fn wait_subs_timeout (& self , duration : Duration) -> Result < () , DeadlineExceeded > { let guard = self . node . inner . lock () . unwrap () ; let (_guard , wait_result) = self . node . condvar . wait_timeout_while (guard , duration , | guard | ! guard . subs . is_empty ()) . unwrap () ; if wait_result . timed_out () { Err (DeadlineExceeded { }) } else { Ok (()) } } # [doc = " Waits for all direct subordinate permits to drop."] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns [`DeadlineExceeded`](struct.DeadlineExceeded.html) when `deadline` passes"] # [doc = " and the permit has a subordinate permit."] pub fn wait_subs_deadline (& self , deadline : Instant) -> Result < () , DeadlineExceeded > { let duration = deadline . saturating_duration_since (Instant :: now ()) ; self . wait_subs_timeout (duration) } }
};
}
