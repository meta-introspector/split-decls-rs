// Generated macro for select_once (macro)
macro_rules! Depcrate_math_support_feature_detectselect_once {
() => {
// Module: crate::math::support::feature_detect
// Provides: {"select_once"}
// Dependencies: {}
# [doc = " Call `init` once to choose an implementation, then use it for the rest of the program."] # [doc = ""] # [doc = " - `sig` is the function type."] # [doc = " - `init` is an expression called at startup that chooses an implementation and returns a"] # [doc = "   function pointer."] # [doc = " - `call` is an expression to call a function returned by `init`, encapsulating any safety"] # [doc = "   preconditions."] # [doc = ""] # [doc = " The type `Func` is available in `init` and `call`."] # [doc = ""] # [doc = " This is effectively our version of an ifunc without linker support. Note that `init` may be"] # [doc = " called more than once until one completes."] # [allow (unused_macros)] macro_rules ! select_once { (sig : fn ($ ($ arg : ident : $ ArgTy : ty) ,*) -> $ RetTy : ty , init : $ init : expr , call : $ call : expr ,) => { { use core :: mem ; use core :: sync :: atomic :: { AtomicPtr , Ordering } ; type Func = unsafe fn ($ ($ arg : $ ArgTy) ,*) -> $ RetTy ; # [doc = " Stores a pointer that is immediately jumped to. By default it is an init function"] # [doc = " that sets FUNC to something else."] static FUNC : AtomicPtr < () > = AtomicPtr :: new ((initializer as Func) as * mut ()) ; # [doc = " Run once to set the function that will be used for all subsequent calls."] fn initializer ($ ($ arg : $ ArgTy) ,*) -> $ RetTy { let fn_ptr : Func = $ init () ; FUNC . store (fn_ptr as * mut () , Ordering :: Relaxed) ; $ call (fn_ptr) } let raw : * mut () = FUNC . load (Ordering :: Relaxed) ; let fn_ptr : Func = unsafe { mem :: transmute ::<* mut () , Func > (raw) } ; $ call (fn_ptr) } } }
};
}
