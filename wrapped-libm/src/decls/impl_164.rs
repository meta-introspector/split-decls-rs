macro_rules! deps {
    () => {
        Status!();
    };
}

macro_rules! impl_164 {
    () => {
        deps!();
        impl Status { # [doc = " Default status indicating no errors."] pub const OK : Self = Self (0) ; # [doc = " No definable result."] # [doc = ""] # [doc = " Includes:"] # [doc = " - Any ops on sNaN, with a few exceptions."] # [doc = " - `0 * inf`, `inf * 0`."] # [doc = " - `fma(0, inf, c)` or `fma(inf, 0, c)`, possibly excluding `c = qNaN`."] # [doc = " - `+inf + -inf` and similar (includes subtraction and fma)."] # [doc = " - `0.0 / 0.0`, `inf / inf`"] # [doc = " - `remainder(x, y)` if `y == 0.0` or `x == inf`, and neither is NaN."] # [doc = " - `sqrt(x)` with `x < 0.0`."] pub const INVALID : Self = Self (1) ; # [doc = " Division by zero."] # [doc = ""] # [doc = " The default result for division is +/-inf based on operand sign. For `logB`, the default"] # [doc = " result is -inf."] # [doc = " `x / y` when `x != 0.0` and `y == 0.0`,"] # [cfg_attr (not (feature = "unstable-public-internals") , allow (dead_code))] pub const DIVIDE_BY_ZERO : Self = Self (1 << 2) ; # [doc = " The result exceeds the maximum finite value."] # [doc = ""] # [doc = " The default result depends on rounding mode. `Nearest*` rounds to +/- infinity, sign based"] # [doc = " on the intermediate result. `Zero` rounds to the signed maximum finite. `Positive` and"] # [doc = " `Negative` round to signed maximum finite in one direction, signed infinity in the other."] # [cfg_attr (not (feature = "unstable-public-internals") , allow (dead_code))] pub const OVERFLOW : Self = Self (1 << 3) ; # [doc = " The result is subnormal and lost precision."] pub const UNDERFLOW : Self = Self (1 << 4) ; # [doc = " The finite-precision result does not match that of infinite precision, and the reason"] # [doc = " is not represented by one of the other flags."] pub const INEXACT : Self = Self (1 << 5) ; # [doc = " True if `UNDERFLOW` is set."] # [cfg_attr (not (feature = "unstable-public-internals") , allow (dead_code))] pub const fn underflow (self) -> bool { self . 0 & Self :: UNDERFLOW . 0 != 0 } # [doc = " True if `OVERFLOW` is set."] # [cfg_attr (not (feature = "unstable-public-internals") , allow (dead_code))] pub const fn overflow (self) -> bool { self . 0 & Self :: OVERFLOW . 0 != 0 } pub fn set_underflow (& mut self , val : bool) { self . set_flag (val , Self :: UNDERFLOW) ; } # [doc = " True if `INEXACT` is set."] pub const fn inexact (self) -> bool { self . 0 & Self :: INEXACT . 0 != 0 } pub fn set_inexact (& mut self , val : bool) { self . set_flag (val , Self :: INEXACT) ; } fn set_flag (& mut self , val : bool , mask : Self) { if val { self . 0 |= mask . 0 ; } else { self . 0 &= ! mask . 0 ; } } pub (crate) const fn with (self , rhs : Self) -> Self { Self (self . 0 | rhs . 0) } }
    };
}

impl_164!()