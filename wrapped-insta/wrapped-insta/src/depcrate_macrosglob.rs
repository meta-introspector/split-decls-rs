// Generated macro for glob (macro)
macro_rules! Depcrate_macrosglob {
() => {
// Module: crate::macros
// Provides: {"glob"}
// Dependencies: {}
# [doc = " Executes a closure for all input files matching a glob."] # [doc = ""] # [doc = " The closure is passed the path to the file.  You can use [`std::fs::read_to_string`]"] # [doc = " or similar functions to load the file and process it."] # [doc = ""] # [doc = " ```"] # [doc = " # use insta::{assert_snapshot, glob, Settings};"] # [doc = " # let mut settings = Settings::clone_current();"] # [doc = " # settings.set_allow_empty_glob(true);"] # [doc = " # let _dropguard = settings.bind_to_scope();"] # [doc = " use std::fs;"] # [doc = ""] # [doc = " glob!(\"inputs/*.txt\", |path| {"] # [doc = "     let input = fs::read_to_string(path).unwrap();"] # [doc = "     assert_snapshot!(input.to_uppercase());"] # [doc = " });"] # [doc = " ```"] # [doc = ""] # [doc = " The `INSTA_GLOB_FILTER` environment variable can be set to only execute certain files."] # [doc = " The format of the filter is a semicolon separated filter.  For instance by setting"] # [doc = " `INSTA_GLOB_FILTER` to `foo-*txt;bar-*.txt` only files starting with `foo-` or `bar-`"] # [doc = " end ending in `.txt` will be executed.  When using `cargo-insta` the `--glob-filter`"] # [doc = " option can be used instead."] # [doc = ""] # [doc = " Another effect of the globbing system is that snapshot failures within the glob macro"] # [doc = " are deferred until the end of of it.  In other words this means that each snapshot"] # [doc = " assertion within the `glob!` block are reported.  It can be disabled by setting"] # [doc = " `INSTA_GLOB_FAIL_FAST` environment variable to `1`."] # [doc = ""] # [doc = " Note: Parent directory traversal patterns (e.g., \"../**/*.rs\") are not supported in the"] # [doc = " two-argument form of this macro currently. If you need to access parent"] # [doc = " directories, use the three-argument version of this macro instead."] # [doc = ""] # [doc = " A three-argument version of this macro allows specifying a base directory"] # [doc = " for the glob to start in. This allows globbing in arbitrary directories,"] # [doc = " including parent directories:"] # [doc = ""] # [doc = " ```"] # [doc = " # use insta::{assert_snapshot, glob, Settings};"] # [doc = " # let mut settings = Settings::clone_current();"] # [doc = " # settings.set_allow_empty_glob(true);"] # [doc = " # let _dropguard = settings.bind_to_scope();"] # [doc = " use std::fs;"] # [doc = ""] # [doc = " glob!(\"../test_data\", \"inputs/*.txt\", |path| {"] # [doc = "     let input = fs::read_to_string(path).unwrap();"] # [doc = "     assert_snapshot!(input.to_uppercase());"] # [doc = " });"] # [doc = " ```"] # [cfg (feature = "glob")] # [cfg_attr (docsrs , doc (cfg (feature = "glob")))] # [macro_export] macro_rules ! glob { ($ base_path : expr , $ glob : expr , $ closure : expr) => { { use $ crate :: _macro_support :: path :: Path ; let base = $ crate :: _get_workspace_root ! () . join (Path :: new (file ! ()) . parent () . unwrap ()) . join ($ base_path) . to_path_buf () ; let base = base . canonicalize () . unwrap_or_else (| _ | base) ; $ crate :: _macro_support :: glob_exec ($ crate :: _get_workspace_root ! () . as_path () , & base , $ glob , $ closure ,) ; } } ; ($ glob : expr , $ closure : expr) => { { $ crate :: glob ! ("." , $ glob , $ closure) } } ; }
};
}
