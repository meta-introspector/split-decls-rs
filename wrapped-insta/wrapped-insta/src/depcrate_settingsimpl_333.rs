// Generated macro for impl_333 (impl)
macro_rules! Depcrate_settingsimpl_333 {
() => {
// Module: crate::settings
// Provides: {"impl_333"}
// Dependencies: {}
impl Settings { # [doc = " Returns the default settings."] # [doc = ""] # [doc = " It's recommended to use [`Self::clone_current`] instead so that"] # [doc = " already applied modifications are not discarded."] pub fn new () -> Settings { Settings :: default () } # [doc = " Returns a copy of the current settings."] pub fn clone_current () -> Settings { Settings :: with (| x | x . clone ()) } # [doc = " Internal helper for macros"] # [doc (hidden)] pub fn _private_inner_mut (& mut self) -> & mut ActualSettings { Arc :: make_mut (& mut self . inner) } # [doc = " Enables forceful sorting of maps before serialization."] # [doc = ""] # [doc = " Note that this only applies to snapshots that undergo serialization"] # [doc = " (eg: does not work for [`assert_debug_snapshot!`](crate::assert_debug_snapshot!))."] # [doc = ""] # [doc = " The default value is `false`."] pub fn set_sort_maps (& mut self , value : bool) { self . _private_inner_mut () . sort_maps = value ; } # [doc = " Returns the current value for map sorting."] pub fn sort_maps (& self) -> bool { self . inner . sort_maps } # [doc = " Disables prepending of modules to the snapshot filename."] # [doc = ""] # [doc = " By default, the filename of a snapshot is `<module>__<name>.snap`."] # [doc = " Setting this flag to `false` changes the snapshot filename to just"] # [doc = " `<name>.snap`."] # [doc = ""] # [doc = " The default value is `true`."] pub fn set_prepend_module_to_snapshot (& mut self , value : bool) { self . _private_inner_mut () . prepend_module_to_snapshot (value) ; } # [doc = " Returns the current value for module name prepending."] pub fn prepend_module_to_snapshot (& self) -> bool { self . inner . prepend_module_to_snapshot } # [doc = " Allows the [`glob!`] macro to succeed if it matches no files."] # [doc = ""] # [doc = " By default, the glob macro will fail the test if it does not find"] # [doc = " any files to prevent accidental typos.  This can be disabled when"] # [doc = " fixtures should be conditional."] # [doc = ""] # [doc = " The default value is `false`."] # [cfg (feature = "glob")] pub fn set_allow_empty_glob (& mut self , value : bool) { self . _private_inner_mut () . allow_empty_glob (value) ; } # [doc = " Returns the current value for the empty glob setting."] # [cfg (feature = "glob")] pub fn allow_empty_glob (& self) -> bool { self . inner . allow_empty_glob } # [doc = " Sets the snapshot suffix."] # [doc = ""] # [doc = " The snapshot suffix is added to all snapshot names with an `@` sign"] # [doc = " between.  For instance, if the snapshot suffix is set to `\"foo\"`, and"] # [doc = " the snapshot would be named `\"snapshot\"`, it turns into `\"snapshot@foo\"`."] # [doc = " This is useful to separate snapshots if you want to use test"] # [doc = " parameterization."] pub fn set_snapshot_suffix < I : Into < String > > (& mut self , suffix : I) { self . _private_inner_mut () . snapshot_suffix (suffix) ; } # [doc = " Removes the snapshot suffix."] pub fn remove_snapshot_suffix (& mut self) { self . set_snapshot_suffix ("") ; } # [doc = " Returns the current snapshot suffix."] pub fn snapshot_suffix (& self) -> Option < & str > { if self . inner . snapshot_suffix . is_empty () { None } else { Some (& self . inner . snapshot_suffix) } } # [doc = " Sets the input file reference."] # [doc = ""] # [doc = " This value is completely unused by the snapshot testing system, but it"] # [doc = " allows storing some metadata with a snapshot that refers back to the"] # [doc = " input file.  The path stored here is made relative to the workspace root"] # [doc = " before storing with the snapshot."] pub fn set_input_file < P : AsRef < Path > > (& mut self , p : P) { self . _private_inner_mut () . input_file (p) ; } # [doc = " Removes the input file reference."] pub fn remove_input_file (& mut self) { self . _private_inner_mut () . input_file = None ; } # [doc = " Returns the current input file reference."] pub fn input_file (& self) -> Option < & Path > { self . inner . input_file . as_deref () } # [doc = " Sets the description."] # [doc = ""] # [doc = " The description is stored alongside the snapshot and will be displayed"] # [doc = " in the diff UI.  When a snapshot is captured, the Rust expression for that"] # [doc = " snapshot is always retained.  However, sometimes that information is not"] # [doc = " super useful by itself, particularly when working with loops and generated"] # [doc = " tests.  In that case the `description` can be set as extra information."] # [doc = ""] # [doc = " See also [`Self::set_info`]."] pub fn set_description < S : Into < String > > (& mut self , value : S) { self . _private_inner_mut () . description (value) ; } # [doc = " Removes the description."] pub fn remove_description (& mut self) { self . _private_inner_mut () . description = None ; } # [doc = " Returns the current description"] pub fn description (& self) -> Option < & str > { self . inner . description . as_deref () } # [doc = " Sets the info."] # [doc = ""] # [doc = " The `info` is similar to `description` but for structured data.  This is"] # [doc = " stored with the snapshot and shown in the review UI.  This for instance"] # [doc = " can be used to show extended information that can make a reviewer better"] # [doc = " understand what the snapshot is supposed to be testing."] # [doc = ""] # [doc = " As an example the input parameters to the function that creates the snapshot"] # [doc = " can be persisted here."] # [doc = ""] # [doc = " **Note:** Redactions configured via [`Self::add_redaction`] are automatically"] # [doc = " applied to the info metadata when it is set."] # [doc = ""] # [doc = " Alternatively you can use [`Self::set_raw_info`] instead."] # [cfg (feature = "serde")] # [cfg_attr (docsrs , doc (cfg (feature = "serde")))] pub fn set_info < S : Serialize > (& mut self , s : & S) { self . _private_inner_mut () . info (s) ; } # [doc = " Sets the info from a content object."] # [doc = ""] # [doc = " This works like [`Self::set_info`] but does not require [`serde`]."] # [doc = ""] # [doc = " **Note:** Unlike [`Self::set_info`], this method does NOT automatically apply"] # [doc = " redactions. If you need redactions applied to metadata, use [`Self::set_info`]"] # [doc = " instead (which requires the `serde` feature)."] pub fn set_raw_info (& mut self , content : & Content) { self . _private_inner_mut () . raw_info (content) ; } # [doc = " Removes the info."] pub fn remove_info (& mut self) { self . _private_inner_mut () . info = None ; } # [doc = " Returns the current info"] pub (crate) fn info (& self) -> Option < & Content > { self . inner . info . as_ref () } # [doc = " Returns the current info"] pub fn has_info (& self) -> bool { self . inner . info . is_some () } # [doc = " If set to true, does not retain the expression in the snapshot."] pub fn set_omit_expression (& mut self , value : bool) { self . _private_inner_mut () . omit_expression (value) ; } # [doc = " Returns true if expressions are omitted from snapshots."] pub fn omit_expression (& self) -> bool { self . inner . omit_expression } # [doc = " Registers redactions that should be applied."] # [doc = ""] # [doc = " This can be useful if redactions must be shared across multiple"] # [doc = " snapshots."] # [doc = ""] # [doc = " Note that this only applies to snapshots that undergo serialization"] # [doc = " (eg: does not work for [`assert_debug_snapshot!`](crate::assert_debug_snapshot!).)"] # [cfg (feature = "redactions")] # [cfg_attr (docsrs , doc (cfg (feature = "redactions")))] pub fn add_redaction < R : Into < Redaction > > (& mut self , selector : & str , replacement : R) { self . add_redaction_impl (selector , replacement . into ()) } # [cfg (feature = "redactions")] fn add_redaction_impl (& mut self , selector : & str , replacement : Redaction) { self . _private_inner_mut () . redactions . 0 . push ((Selector :: parse (selector) . unwrap () . make_static () , Arc :: new (replacement) ,)) ; } # [doc = " Registers a replacement callback."] # [doc = ""] # [doc = " This works similar to a redaction but instead of changing the value it"] # [doc = " asserts the value at a certain place.  This function is internally"] # [doc = " supposed to call things like [`assert_eq!`]."] # [doc = ""] # [doc = " This is a shortcut to `add_redaction(selector, dynamic_redaction(...))`;"] # [cfg (feature = "redactions")] # [cfg_attr (docsrs , doc (cfg (feature = "redactions")))] pub fn add_dynamic_redaction < I , F > (& mut self , selector : & str , func : F) where I : Into < Content > , F : Fn (Content , ContentPath < '_ >) -> I + Send + Sync + 'static , { self . add_redaction (selector , dynamic_redaction (func)) ; } # [doc = " A special redaction that sorts a sequence or map."] # [doc = ""] # [doc = " This is a shortcut to `add_redaction(selector, sorted_redaction())`."] # [cfg (feature = "redactions")] # [cfg_attr (docsrs , doc (cfg (feature = "redactions")))] pub fn sort_selector (& mut self , selector : & str) { self . add_redaction (selector , sorted_redaction ()) ; } # [doc = " Replaces the currently set redactions."] # [doc = ""] # [doc = " The default set is empty."] # [cfg (feature = "redactions")] # [cfg_attr (docsrs , doc (cfg (feature = "redactions")))] pub fn set_redactions < R : Into < Redactions > > (& mut self , redactions : R) { self . _private_inner_mut () . redactions (redactions) ; } # [doc = " Removes all redactions."] # [cfg (feature = "redactions")] # [cfg_attr (docsrs , doc (cfg (feature = "redactions")))] pub fn clear_redactions (& mut self) { self . _private_inner_mut () . redactions . 0 . clear () ; } # [doc = " Apply redactions to content."] # [cfg (feature = "redactions")] # [cfg_attr (docsrs , doc (cfg (feature = "redactions")))] pub (crate) fn apply_redactions (& self , content : Content) -> Content { self . inner . redactions . apply_to_content (content) } # [doc = " Adds a new filter."] # [doc = ""] # [doc = " Filters are similar to redactions but are applied as regex onto the final snapshot"] # [doc = " value.  This can be used to perform modifications to the snapshot string that would"] # [doc = " be impossible to do with redactions because for instance the value is just a string."] # [doc = ""] # [doc = " The first argument is the [`regex`] pattern to apply, the second is a replacement"] # [doc = " string.  The replacement string has the same functionality as the second argument"] # [doc = " to [`regex::Regex::replace`]."] # [doc = ""] # [doc = " This is useful to perform some cleanup procedures on the snapshot for unstable values."] # [doc = ""] # [doc = " ```rust"] # [doc = " # use insta::Settings;"] # [doc = " # async fn foo() {"] # [doc = " # let mut settings = Settings::new();"] # [doc = " settings.add_filter(r\"\\b[[:xdigit:]]{32}\\b\", \"[UID]\");"] # [doc = " # }"] # [doc = " ```"] # [cfg (feature = "filters")] # [cfg_attr (docsrs , doc (cfg (feature = "filters")))] pub fn add_filter < S : Into < String > > (& mut self , regex : & str , replacement : S) { self . _private_inner_mut () . filters . add (regex , replacement) ; } # [doc = " Replaces the currently set filters."] # [doc = ""] # [doc = " The default set is empty."] # [cfg (feature = "filters")] # [cfg_attr (docsrs , doc (cfg (feature = "filters")))] pub fn set_filters < F : Into < Filters > > (& mut self , filters : F) { self . _private_inner_mut () . filters (filters) ; } # [doc = " Removes all filters."] # [cfg (feature = "filters")] # [cfg_attr (docsrs , doc (cfg (feature = "filters")))] pub fn clear_filters (& mut self) { self . _private_inner_mut () . filters . clear () ; } # [doc = " Returns the current filters"] # [cfg (feature = "filters")] # [cfg_attr (docsrs , doc (cfg (feature = "filters")))] pub (crate) fn filters (& self) -> & Filters { & self . inner . filters } # [doc = " Sets the snapshot path."] # [doc = ""] # [doc = " If not absolute, it's relative to where the test is in."] # [doc = ""] # [doc = " Defaults to `snapshots`."] pub fn set_snapshot_path < P : AsRef < Path > > (& mut self , path : P) { self . _private_inner_mut () . snapshot_path (path) ; } # [doc = " Returns the snapshot path."] pub fn snapshot_path (& self) -> & Path { & self . inner . snapshot_path } # [doc = " Runs a function with the current settings bound to the thread."] # [doc = ""] # [doc = " This is an alternative to [`Self::bind_to_scope`]()"] # [doc = " which does not require holding on to a drop guard.  The return value"] # [doc = " of the closure is passed through."] # [doc = ""] # [doc = " ```"] # [doc = " # use insta::Settings;"] # [doc = " let mut settings = Settings::clone_current();"] # [doc = " settings.set_sort_maps(true);"] # [doc = " settings.bind(|| {"] # [doc = "     // do stuff here"] # [doc = " });"] # [doc = " ```"] pub fn bind < F : FnOnce () -> R , R > (& self , f : F) -> R { let _guard = self . bind_to_scope () ; f () } # [doc = " Like [`Self::bind`] but for futures."] # [doc = ""] # [doc = " This lets you bind settings for the duration of a future like this:"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use insta::Settings;"] # [doc = " # async fn foo() {"] # [doc = " let settings = Settings::new();"] # [doc = " settings.bind_async(async {"] # [doc = "     // do assertions here"] # [doc = " }).await;"] # [doc = " # }"] # [doc = " ```"] pub fn bind_async < F : Future < Output = T > , T > (& self , future : F) -> impl Future < Output = T > { struct BindingFuture < F > { settings : Arc < ActualSettings > , future : F , } impl < F : Future > Future for BindingFuture < F > { type Output = F :: Output ; fn poll (self : Pin < & mut Self > , cx : & mut Context) -> Poll < Self :: Output > { let inner = self . settings . clone () ; let future = unsafe { self . map_unchecked_mut (| s | & mut s . future) } ; CURRENT_SETTINGS . with (| x | { let old = { let mut current = x . borrow_mut () ; let old = current . inner . clone () ; current . inner = inner ; old } ; let rv = future . poll (cx) ; let mut current = x . borrow_mut () ; current . inner = old ; rv }) } } BindingFuture { settings : self . inner . clone () , future , } } # [doc = " Binds the settings to the current thread and resets when the drop"] # [doc = " guard is released."] # [doc = ""] # [doc = " This is the recommended way to temporarily bind settings. It replaces"] # [doc = " the earlier [`bind_to_scope`](Settings::bind_to_scope), and relies on"] # [doc = " drop guards.  An alternative is [`bind`](Settings::bind), which binds"] # [doc = " for the duration of the block it wraps."] # [doc = ""] # [doc = " ```"] # [doc = " # use insta::Settings;"] # [doc = " let mut settings = Settings::clone_current();"] # [doc = " settings.set_sort_maps(true);"] # [doc = " let _guard = settings.bind_to_scope();"] # [doc = " // do stuff here"] # [doc = " ```"] pub fn bind_to_scope (& self) -> SettingsBindDropGuard { CURRENT_SETTINGS . with (| x | { let mut x = x . borrow_mut () ; let old = mem :: replace (& mut x . inner , self . inner . clone ()) ; SettingsBindDropGuard (Some (old) , std :: marker :: PhantomData) }) } # [doc = " Runs a function with the current settings."] pub (crate) fn with < R , F : FnOnce (& Settings) -> R > (f : F) -> R { CURRENT_SETTINGS . with (| x | f (& x . borrow ())) } }
};
}
