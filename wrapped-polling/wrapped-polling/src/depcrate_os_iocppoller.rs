// Generated macro for Poller (struct)
macro_rules! Depcrate_os_iocpPoller {
() => {
// Module: crate::os::iocp
// Provides: {"Poller"}
// Dependencies: {}
# [doc = " Interface to I/O completion ports."] # [derive (Debug)] pub (super) struct Poller { # [doc = " The I/O completion port."] port : Arc < IoCompletionPort < Packet > > , # [doc = " List of currently active AFD instances."] # [doc = ""] # [doc = " AFD acts as the actual source of the socket events. It's essentially running `WSAPoll` on"] # [doc = " the sockets and then posting the events to the IOCP."] # [doc = ""] # [doc = " AFD instances can be keyed to an unlimited number of sockets. However, each AFD instance"] # [doc = " polls their sockets linearly. Therefore, it is best to limit the number of sockets each AFD"] # [doc = " instance is responsible for. The limit of 32 is chosen because that's what `wepoll` uses."] # [doc = ""] # [doc = " Weak references are kept here so that the AFD handle is automatically dropped when the last"] # [doc = " associated socket is dropped."] afd : Mutex < Vec < Weak < Afd < Packet > > > > , # [doc = " The state of the sources registered with this poller."] # [doc = ""] # [doc = " Each source is keyed by its raw socket ID."] sources : RwLock < HashMap < RawSocket , Packet > > , # [doc = " The state of the waitable handles registered with this poller."] waitables : RwLock < HashMap < RawHandle , Packet > > , # [doc = " Sockets with pending updates."] # [doc = ""] # [doc = " This list contains packets with sockets that need to have their AFD state adjusted by"] # [doc = " calling the `update()` function on them. It's best to queue up packets as they need to"] # [doc = " be updated and then run all of the updates before we start waiting on the IOCP, rather than"] # [doc = " updating them as we come. If we're waiting on the IOCP updates should be run immediately."] pending_updates : ConcurrentQueue < Packet > , # [doc = " Are we currently polling?"] # [doc = ""] # [doc = " This indicates whether or not we are blocking on the IOCP, and is used to determine"] # [doc = " whether pending updates should be run immediately or queued."] polling : AtomicBool , # [doc = " The packet used to notify the poller."] # [doc = ""] # [doc = " This is a special-case packet that is used to wake up the poller when it is waiting."] notifier : Packet , }
};
}
