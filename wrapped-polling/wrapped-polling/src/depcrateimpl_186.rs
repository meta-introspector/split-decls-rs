// Generated macro for impl_186 (impl)
macro_rules! Depcrateimpl_186 {
() => {
// Module: crate
// Provides: {"impl_186"}
// Dependencies: {}
impl Poller { # [doc = " Creates a new poller."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use polling::Poller;"] # [doc = ""] # [doc = " let poller = Poller::new()?;"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] pub fn new () -> io :: Result < Poller > { Ok (Poller { poller : sys :: Poller :: new () ? , lock : Mutex :: new (()) , notified : AtomicBool :: new (false) , }) } # [doc = " Tell whether or not this `Poller` supports level-triggered polling."] pub fn supports_level (& self) -> bool { self . poller . supports_level () } # [doc = " Tell whether or not this `Poller` supports edge-triggered polling."] pub fn supports_edge (& self) -> bool { self . poller . supports_edge () } # [doc = " Adds a file descriptor or socket to the poller."] # [doc = ""] # [doc = " A file descriptor or socket is considered readable or writable when a read or write"] # [doc = " operation on it would not block. This doesn't mean the read or write operation will"] # [doc = " succeed, it only means the operation will return immediately."] # [doc = ""] # [doc = " If interest is set in both readability and writability, the two kinds of events might be"] # [doc = " delivered either separately or together."] # [doc = ""] # [doc = " For example, interest in `Event { key: 7, readable: true, writable: true }` might result in"] # [doc = " a single [`Event`] of the same form, or in two separate [`Event`]s:"] # [doc = " - `Event { key: 7, readable: true, writable: false }`"] # [doc = " - `Event { key: 7, readable: false, writable: true }`"] # [doc = ""] # [doc = " Note that interest in I/O events needs to be re-enabled using"] # [doc = " [`modify()`][`Poller::modify()`] again after an event is delivered if we're interested in"] # [doc = " the next event of the same kind."] # [doc = ""] # [doc = " It is possible to register interest in the same file descriptor or socket using multiple"] # [doc = " separate [`Poller`] instances. When the event is delivered, one or more [`Poller`]s are"] # [doc = " notified with that event. The exact number of [`Poller`]s notified depends on the"] # [doc = " underlying platform. When registering multiple sources into one event, the user should"] # [doc = " be careful to accommodate for events lost to other pollers."] # [doc = ""] # [doc = " One may also register one source into other, non-`polling` event loops, like GLib's"] # [doc = " context. While the plumbing will vary from platform to platform, in general the [`Poller`]"] # [doc = " will act as if the source was registered with another [`Poller`], with the same caveats"] # [doc = " as above."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The source must be [`delete()`]d from this `Poller` before it is dropped."] # [doc = ""] # [doc = " [`delete()`]: Poller::delete"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This method returns an error in the following situations:"] # [doc = ""] # [doc = " * If `key` equals `usize::MAX` because that key is reserved for internal use."] # [doc = " * If an error is returned by the syscall."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Set interest in all events:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use polling::{Event, Poller};"] # [doc = ""] # [doc = " let source = std::net::TcpListener::bind(\"127.0.0.1:0\")?;"] # [doc = " source.set_nonblocking(true)?;"] # [doc = " let key = 7;"] # [doc = ""] # [doc = " let poller = Poller::new()?;"] # [doc = " unsafe {"] # [doc = "     poller.add(&source, Event::all(key))?;"] # [doc = " }"] # [doc = " poller.delete(&source)?;"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] pub unsafe fn add (& self , source : impl AsRawSource , interest : Event) -> io :: Result < () > { self . add_with_mode (source , interest , PollMode :: Oneshot) } # [doc = " Adds a file descriptor or socket to the poller in the specified mode."] # [doc = ""] # [doc = " This is identical to the `add()` function, but allows specifying the"] # [doc = " polling mode to use for this socket."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The source must be [`delete()`]d from this `Poller` before it is dropped."] # [doc = ""] # [doc = " [`delete()`]: Poller::delete"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the operating system does not support the specified mode, this function"] # [doc = " will return an error."] pub unsafe fn add_with_mode (& self , source : impl AsRawSource , interest : Event , mode : PollMode ,) -> io :: Result < () > { if interest . key == NOTIFY_KEY { return Err (io :: Error :: new (io :: ErrorKind :: InvalidInput , "the key is not allowed to be `usize::MAX`" ,)) ; } self . poller . add (source . raw () , interest , mode) } # [doc = " Modifies the interest in a file descriptor or socket."] # [doc = ""] # [doc = " This method has the same behavior as [`add()`][`Poller::add()`] except it modifies the"] # [doc = " interest of a previously added file descriptor or socket."] # [doc = ""] # [doc = " To use this method with a file descriptor or socket, you must first add it using"] # [doc = " [`add()`][`Poller::add()`]."] # [doc = ""] # [doc = " Note that interest in I/O events needs to be re-enabled using"] # [doc = " [`modify()`][`Poller::modify()`] again after an event is delivered if we're interested in"] # [doc = " the next event of the same kind."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This method returns an error in the following situations:"] # [doc = ""] # [doc = " * If `key` equals `usize::MAX` because that key is reserved for internal use."] # [doc = " * If an error is returned by the syscall."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " To enable interest in all events:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " # use polling::{Event, Poller};"] # [doc = " # let source = std::net::TcpListener::bind(\"127.0.0.1:0\")?;"] # [doc = " # let key = 7;"] # [doc = " # let poller = Poller::new()?;"] # [doc = " # unsafe { poller.add(&source, Event::none(key))?; }"] # [doc = " poller.modify(&source, Event::all(key))?;"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] # [doc = ""] # [doc = " To enable interest in readable events and disable interest in writable events:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " # use polling::{Event, Poller};"] # [doc = " # let source = std::net::TcpListener::bind(\"127.0.0.1:0\")?;"] # [doc = " # let key = 7;"] # [doc = " # let poller = Poller::new()?;"] # [doc = " # unsafe { poller.add(&source, Event::none(key))?; }"] # [doc = " poller.modify(&source, Event::readable(key))?;"] # [doc = " # poller.delete(&source)?;"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] # [doc = ""] # [doc = " To disable interest in readable events and enable interest in writable events:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " # use polling::{Event, Poller};"] # [doc = " # let poller = Poller::new()?;"] # [doc = " # let key = 7;"] # [doc = " # let source = std::net::TcpListener::bind(\"127.0.0.1:0\")?;"] # [doc = " # unsafe { poller.add(&source, Event::none(key))? };"] # [doc = " poller.modify(&source, Event::writable(key))?;"] # [doc = " # poller.delete(&source)?;"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] # [doc = ""] # [doc = " To disable interest in all events:"] # [doc = ""] # [doc = " ```no_run"] # [doc = " # use polling::{Event, Poller};"] # [doc = " # let source = std::net::TcpListener::bind(\"127.0.0.1:0\")?;"] # [doc = " # let key = 7;"] # [doc = " # let poller = Poller::new()?;"] # [doc = " # unsafe { poller.add(&source, Event::none(key))?; }"] # [doc = " poller.modify(&source, Event::none(key))?;"] # [doc = " # poller.delete(&source)?;"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] pub fn modify (& self , source : impl AsSource , interest : Event) -> io :: Result < () > { self . modify_with_mode (source , interest , PollMode :: Oneshot) } # [doc = " Modifies interest in a file descriptor or socket to the poller, but with the specified"] # [doc = " mode."] # [doc = ""] # [doc = " This is identical to the `modify()` function, but allows specifying the polling mode"] # [doc = " to use for this socket."] # [doc = ""] # [doc = " # Performance Notes"] # [doc = ""] # [doc = " This function can be used to change a source from one polling mode to another. However,"] # [doc = " on some platforms, this switch can cause delays in the delivery of events."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If the operating system does not support the specified mode, this function will return"] # [doc = " an error."] pub fn modify_with_mode (& self , source : impl AsSource , interest : Event , mode : PollMode ,) -> io :: Result < () > { if interest . key == NOTIFY_KEY { return Err (io :: Error :: new (io :: ErrorKind :: InvalidInput , "the key is not allowed to be `usize::MAX`" ,)) ; } self . poller . modify (source . source () , interest , mode) } # [doc = " Removes a file descriptor or socket from the poller."] # [doc = ""] # [doc = " Unlike [`add()`][`Poller::add()`], this method only removes the file descriptor or"] # [doc = " socket from the poller without putting it back into blocking mode."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use polling::{Event, Poller};"] # [doc = " use std::net::TcpListener;"] # [doc = ""] # [doc = " let socket = TcpListener::bind(\"127.0.0.1:0\")?;"] # [doc = " socket.set_nonblocking(true)?;"] # [doc = " let key = 7;"] # [doc = ""] # [doc = " let poller = Poller::new()?;"] # [doc = " unsafe { poller.add(&socket, Event::all(key))?; }"] # [doc = " poller.delete(&socket)?;"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] pub fn delete (& self , source : impl AsSource) -> io :: Result < () > { self . poller . delete (source . source ()) } # [doc = " Waits for at least one I/O event and returns the number of new events."] # [doc = ""] # [doc = " New events will be appended to `events`. If necessary, make sure to clear the"] # [doc = " [`Events`][Events::clear()] before calling [`wait()`][`Poller::wait()`]!"] # [doc = ""] # [doc = " This method will return with no new events if a notification is delivered by the"] # [doc = " [`notify()`] method, or the timeout is reached. Sometimes it may even return with no events"] # [doc = " spuriously."] # [doc = ""] # [doc = " Only one thread can wait on I/O. If another thread is already in [`wait()`], concurrent"] # [doc = " calls to this method will return immediately with no new events."] # [doc = ""] # [doc = " If the operating system is ready to deliver a large number of events at once, this method"] # [doc = " may decide to deliver them in smaller batches."] # [doc = ""] # [doc = " [`notify()`]: `Poller::notify()`"] # [doc = " [`wait()`]: `Poller::wait()`"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use polling::{Event, Events, Poller};"] # [doc = " use std::net::TcpListener;"] # [doc = " use std::time::Duration;"] # [doc = ""] # [doc = " let socket = TcpListener::bind(\"127.0.0.1:0\")?;"] # [doc = " socket.set_nonblocking(true)?;"] # [doc = " let key = 7;"] # [doc = ""] # [doc = " let poller = Poller::new()?;"] # [doc = " unsafe {"] # [doc = "     poller.add(&socket, Event::all(key))?;"] # [doc = " }"] # [doc = ""] # [doc = " let mut events = Events::new();"] # [doc = " let n = poller.wait(&mut events, Some(Duration::from_secs(1)))?;"] # [doc = " poller.delete(&socket)?;"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] pub fn wait (& self , events : & mut Events , timeout : Option < Duration >) -> io :: Result < usize > { self . wait_impl (events , timeout . and_then (| timeout | Instant :: now () . checked_add (timeout)) ,) } # [doc = " Waits for at least one I/O event and returns the number of new events, with a deadline."] # [doc = ""] # [doc = " See [`wait()`][`Poller::wait()`] for more details."] pub fn wait_deadline (& self , events : & mut Events , deadline : Instant) -> io :: Result < usize > { self . wait_impl (events , Some (deadline)) } fn wait_impl (& self , events : & mut Events , deadline : Option < Instant >) -> io :: Result < usize > { # [cfg (feature = "tracing")] let span = tracing :: trace_span ! ("Poller::wait" , ? deadline) ; # [cfg (feature = "tracing")] let _enter = span . enter () ; if let Ok (_lock) = self . lock . try_lock () { loop { if let Err (e) = self . poller . wait_deadline (& mut events . events , deadline) { if e . kind () == io :: ErrorKind :: Interrupted { events . clear () ; continue ; } else { return Err (e) ; } } self . notified . swap (false , Ordering :: SeqCst) ; return Ok (events . len ()) ; } } else { # [cfg (feature = "tracing")] tracing :: trace ! ("wait: skipping because another thread is already waiting on I/O") ; Ok (0) } } # [doc = " Wakes up the current or the following invocation of [`wait()`]."] # [doc = ""] # [doc = " If no thread is calling [`wait()`] right now, this method will cause the following call"] # [doc = " to wake up immediately."] # [doc = ""] # [doc = " [`wait()`]: `Poller::wait()`"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " use polling::{Events, Poller};"] # [doc = ""] # [doc = " let poller = Poller::new()?;"] # [doc = ""] # [doc = " // Notify the poller."] # [doc = " poller.notify()?;"] # [doc = ""] # [doc = " let mut events = Events::new();"] # [doc = " poller.wait(&mut events, None)?; // wakes up immediately"] # [doc = " assert!(events.is_empty());"] # [doc = " # std::io::Result::Ok(())"] # [doc = " ```"] pub fn notify (& self) -> io :: Result < () > { # [cfg (feature = "tracing")] let span = tracing :: trace_span ! ("Poller::notify") ; # [cfg (feature = "tracing")] let _enter = span . enter () ; if self . notified . compare_exchange (false , true , Ordering :: SeqCst , Ordering :: SeqCst) . is_ok () { self . poller . notify () ? ; } Ok (()) } }
};
}
