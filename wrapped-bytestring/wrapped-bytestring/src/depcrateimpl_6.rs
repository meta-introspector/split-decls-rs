// Generated macro for impl_6 (impl)
macro_rules! Depcrateimpl_6 {
() => {
// Module: crate
// Provides: {"impl_6"}
// Dependencies: {}
impl ByteString { # [doc = " Creates a new empty `ByteString`."] pub const fn new () -> Self { ByteString (Bytes :: new ()) } # [doc = " Get a reference to the underlying `Bytes` object."] pub fn as_bytes (& self) -> & Bytes { & self . 0 } # [doc = " Unwraps this `ByteString` into the underlying `Bytes` object."] pub fn into_bytes (self) -> Bytes { self . 0 } # [doc = " Creates a new `ByteString` from a `&'static str`."] pub const fn from_static (src : & 'static str) -> ByteString { Self (Bytes :: from_static (src . as_bytes ())) } # [doc = " Creates a new `ByteString` from a Bytes."] # [doc = ""] # [doc = " # Safety"] # [doc = " This function is unsafe because it does not check the bytes passed to it are valid UTF-8."] # [doc = " If this constraint is violated, it may cause memory unsafety issues with future users of"] # [doc = " the `ByteString`, as we assume that `ByteString`s are valid UTF-8. However, the most likely"] # [doc = " issue is that the data gets corrupted."] pub const unsafe fn from_bytes_unchecked (src : Bytes) -> ByteString { Self (src) } # [doc = " Divides one bytestring into two at an index, returning both parts."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `mid` is not on a UTF-8 code point boundary, or if it is past the end of the last"] # [doc = " code point of the bytestring."] pub fn split_at (& self , mid : usize) -> (ByteString , ByteString) { let this : & str = self . as_ref () ; let _valid_midpoint_check = this . split_at (mid) ; let mut bytes = self . 0 . clone () ; let first = bytes . split_to (mid) ; let last = bytes ; unsafe { (ByteString :: from_bytes_unchecked (first) , ByteString :: from_bytes_unchecked (last) ,) } } # [doc = " Returns a new `ByteString` that is equivalent to the given `subset`."] # [doc = ""] # [doc = " When processing a `ByteString` buffer with other tools, one often gets a `&str` which is in"] # [doc = " fact a slice of the original `ByteString`; i.e., a subset of it. This function turns that"] # [doc = " `&str` into another `ByteString`, as if one had sliced the `ByteString` with the offsets"] # [doc = " that correspond to `subset`."] # [doc = ""] # [doc = " Corresponds to [`Bytes::slice_ref`]."] # [doc = ""] # [doc = " This operation is `O(1)`."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `subset` is not a sub-slice of this byte string."] # [doc = ""] # [doc = " Note that strings which are only subsets from an equality perspective do not uphold this"] # [doc = " requirement; see examples."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use bytestring::ByteString;"] # [doc = " let string = ByteString::from_static(\" foo \");"] # [doc = " let subset = string.trim();"] # [doc = " let substring = string.slice_ref(subset);"] # [doc = " assert_eq!(substring, \"foo\");"] # [doc = " ```"] # [doc = ""] # [doc = " ```should_panic"] # [doc = " # use bytestring::ByteString;"] # [doc = " // panics because the given slice is not derived from the original byte string, despite"] # [doc = " // being a logical subset of the string"] # [doc = " ByteString::from_static(\"foo bar\").slice_ref(\"foo\");"] # [doc = " ```"] pub fn slice_ref (& self , subset : & str) -> Self { Self (self . 0 . slice_ref (subset . as_bytes ())) } }
};
}
