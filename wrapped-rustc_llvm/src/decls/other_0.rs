macro_rules! other_0 {
    () => {
        unsafe extern "C" { # [doc = " Opaque type that allows C++ code to write bytes to a Rust-side buffer,"] # [doc = " in conjunction with `RawRustStringOstream`. Use this as `&RustString`"] # [doc = " (Rust) and `RustStringRef` (C++) in FFI signatures."] pub type RustString ; }
    };
}

other_0!();