// Generated macro for impl_83 (impl)
macro_rules! Depcrate_arc_borrowimpl_83 {
() => {
// Module: crate::arc_borrow
// Provides: {"impl_83"}
// Dependencies: {}
impl < 'a , T > ArcBorrow < 'a , T > { # [doc = " Clone this as an `Arc<T>`. This bumps the refcount."] # [inline] pub fn clone_arc (& self) -> Arc < T > { let arc = unsafe { Arc :: from_raw (self . 0 . as_ptr ()) } ; mem :: forget (arc . clone ()) ; arc } # [doc = " For constructing from a pointer known to be Arc-backed,"] # [doc = " e.g. if we obtain such a pointer over FFI"] # [doc = ""] # [doc = " # Safety"] # [doc = " - The pointer to `T` must have come from a Triomphe `Arc`, `UniqueArc`, or `ArcBorrow`."] # [doc = " - The pointer to `T` must have full provenance over the `Arc`, `UniqueArc`, or `ArcBorrow`,"] # [doc = "   in particular it must not have been derived from a `&T` reference, as references immediately"] # [doc = "   loose all provenance over the adjacent reference counts. As of this writing,"] # [doc = "   of the 3 types, only Trimphe's `Arc` offers a direct API for obtaining such a pointer:"] # [doc = "   [`Arc::as_ptr`]."] # [inline] pub unsafe fn from_ptr (ptr : * const T) -> Self { unsafe { ArcBorrow (NonNull :: new_unchecked (ptr as * mut T) , PhantomData) } } # [doc = " Compare two `ArcBorrow`s via pointer equality. Will only return"] # [doc = " true if they come from the same allocation"] # [inline] pub fn ptr_eq (this : & Self , other : & Self) -> bool { this . 0 == other . 0 } # [doc = " The reference count of the underlying `Arc`."] # [doc = ""] # [doc = " The number does not include borrowed pointers,"] # [doc = " or temporary `Arc` pointers created with functions like"] # [doc = " [`ArcBorrow::with_arc`]."] # [doc = ""] # [doc = " The function is called `strong_count` to mirror `std::sync::Arc::strong_count`,"] # [doc = " however `triomphe::Arc` does not support weak references."] # [inline] pub fn strong_count (this : & Self) -> usize { Self :: with_arc (this , | arc | Arc :: strong_count (arc)) } # [doc = " Temporarily converts |self| into a bonafide Arc and exposes it to the"] # [doc = " provided callback. The refcount is not modified."] # [inline] pub fn with_arc < F , U > (& self , f : F) -> U where F : FnOnce (& Arc < T >) -> U , { let transient = unsafe { ManuallyDrop :: new (Arc :: from_raw (self . 0 . as_ptr ())) } ; f (& transient) } # [doc = " Similar to deref, but uses the lifetime |a| rather than the lifetime of"] # [doc = " self, which is incompatible with the signature of the Deref trait."] # [inline] pub fn get (& self) -> & 'a T { unsafe { & * self . 0 . as_ptr () } } }
};
}
