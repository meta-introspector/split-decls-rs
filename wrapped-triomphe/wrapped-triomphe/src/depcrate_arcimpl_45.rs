// Generated macro for impl_45 (impl)
macro_rules! Depcrate_arcimpl_45 {
() => {
// Module: crate::arc
// Provides: {"impl_45"}
// Dependencies: {}
impl < T : Clone > Arc < T > { # [doc = " Makes a mutable reference to the `Arc`, cloning if necessary"] # [doc = ""] # [doc = " This is functionally equivalent to [`Arc::make_mut`][mm] from the standard library."] # [doc = ""] # [doc = " If this `Arc` is uniquely owned, `make_mut()` will provide a mutable"] # [doc = " reference to the contents. If not, `make_mut()` will create a _new_ `Arc`"] # [doc = " with a copy of the contents, update `this` to point to it, and provide"] # [doc = " a mutable reference to its contents."] # [doc = ""] # [doc = " This is useful for implementing copy-on-write schemes where you wish to"] # [doc = " avoid copying things if your `Arc` is not shared."] # [doc = ""] # [doc = " [mm]: https://doc.rust-lang.org/stable/std/sync/struct.Arc.html#method.make_mut"] # [inline] pub fn make_mut (this : & mut Self) -> & mut T { if ! this . is_unique () { * this = Arc :: new (T :: clone (this)) ; } unsafe { & mut (* this . ptr ()) . data } } # [doc = " Makes a `UniqueArc` from an `Arc`, cloning if necessary."] # [doc = ""] # [doc = " If this `Arc` is uniquely owned, `make_unique()` will provide a `UniqueArc`"] # [doc = " containing `this`. If not, `make_unique()` will create a _new_ `Arc`"] # [doc = " with a copy of the contents, update `this` to point to it, and provide"] # [doc = " a `UniqueArc` to it."] # [doc = ""] # [doc = " This is useful for implementing copy-on-write schemes where you wish to"] # [doc = " avoid copying things if your `Arc` is not shared."] # [inline] pub fn make_unique (this : & mut Self) -> & mut UniqueArc < T > { if ! this . is_unique () { * this = Arc :: new (T :: clone (this)) ; } unsafe { UniqueArc :: from_arc_ref (this) } } # [doc = " If we have the only reference to `T` then unwrap it. Otherwise, clone `T` and return the clone."] # [doc = ""] # [doc = " Assuming `arc_t` is of type `Arc<T>`, this function is functionally equivalent to `(*arc_t).clone()`, but will avoid cloning the inner value where possible."] pub fn unwrap_or_clone (this : Arc < T >) -> T { Self :: try_unwrap (this) . unwrap_or_else (| this | T :: clone (& this)) } }
};
}
