// Generated macro for impl_180 (impl)
macro_rules! Depcrate_thin_arcimpl_180 {
() => {
// Module: crate::thin_arc
// Provides: {"impl_180"}
// Dependencies: {}
impl < H , T > ThinArc < H , T > { # [doc = " Temporarily converts |self| into a bonafide Arc and exposes it to the"] # [doc = " provided callback. The refcount is not modified."] # [inline] pub fn with_arc < F , U > (& self , f : F) -> U where F : FnOnce (& Arc < HeaderSliceWithLengthUnchecked < H , T > >) -> U , { let transient = ManuallyDrop :: new (Arc :: from_protected (unsafe { Arc :: from_raw_inner (thin_to_thick (self)) })) ; f (& transient) } # [doc = " Temporarily converts |self| into a bonafide Arc and exposes it to the"] # [doc = " provided callback. The refcount is not modified."] # [inline] fn with_protected_arc < F , U > (& self , f : F) -> U where F : FnOnce (& Arc < HeaderSliceWithLengthProtected < H , T > >) -> U , { let transient = ManuallyDrop :: new (unsafe { Arc :: from_raw_inner (thin_to_thick (self)) }) ; f (& transient) } # [doc = " Temporarily converts |self| into a bonafide Arc and exposes it to the"] # [doc = " provided callback. The refcount is not modified."] # [inline] pub fn with_arc_mut < F , U > (& mut self , f : F) -> U where F : FnOnce (& mut Arc < HeaderSliceWithLengthProtected < H , T > >) -> U , { struct DropGuard < 'a , H , T > { transient : ManuallyDrop < Arc < HeaderSliceWithLengthProtected < H , T > > > , this : & 'a mut ThinArc < H , T > , } impl < 'a , H , T > Drop for DropGuard < 'a , H , T > { fn drop (& mut self) { debug_assert_eq ! (self . transient . length () , self . transient . slice () . len () , "Length needs to be correct for ThinArc to work") ; self . this . ptr = self . transient . p . cast () ; } } let transient = ManuallyDrop :: new (unsafe { Arc :: from_raw_inner (thin_to_thick (self)) }) ; let mut guard = DropGuard { transient , this : self , } ; let ret = f (& mut guard . transient) ; debug_assert_eq ! (guard . transient . length () , guard . transient . slice () . len () , "Length needs to be correct for ThinArc to work") ; ret } # [doc = " Creates a `ThinArc` for a HeaderSlice using the given header struct and"] # [doc = " iterator to generate the slice."] pub fn from_header_and_iter < I > (header : H , items : I) -> Self where I : Iterator < Item = T > + ExactSizeIterator , { let header = HeaderWithLength :: new (header , items . len ()) ; Arc :: into_thin (Arc :: from_header_and_iter (header , items)) } # [doc = " Creates a `ThinArc` for a HeaderSlice using the given header struct and"] # [doc = " a slice to copy."] pub fn from_header_and_slice (header : H , items : & [T]) -> Self where T : Copy , { let header = HeaderWithLength :: new (header , items . len ()) ; Arc :: into_thin (Arc :: from_header_and_slice (header , items)) } # [doc = " Returns the address on the heap of the ThinArc itself -- not the T"] # [doc = " within it -- for memory reporting."] # [inline] pub fn ptr (& self) -> * const c_void { self . ptr . cast () . as_ptr () } # [doc = " Returns the address on the heap of the Arc itself -- not the T within it -- for memory"] # [doc = " reporting."] # [inline] pub fn heap_ptr (& self) -> * const c_void { self . ptr () } # [doc = " # Safety"] # [doc = ""] # [doc = " Constructs an ThinArc from a raw pointer."] # [doc = ""] # [doc = " The raw pointer must have been previously returned by a call to"] # [doc = " ThinArc::into_raw."] # [doc = ""] # [doc = " The user of from_raw has to make sure a specific value of T is only dropped once."] # [doc = ""] # [doc = " This function is unsafe because improper use may lead to memory unsafety,"] # [doc = " even if the returned ThinArc is never accessed."] # [inline] pub unsafe fn from_raw (ptr : * const c_void) -> Self { Self { ptr : ptr :: NonNull :: new_unchecked (ptr as * mut c_void) . cast () , phantom : PhantomData , } } # [doc = " Consume ThinArc and returned the wrapped pointer."] # [inline] pub fn into_raw (self) -> * const c_void { let this = ManuallyDrop :: new (self) ; this . ptr () } # [doc = " Provides a raw pointer to the data."] # [doc = " The counts are not affected in any way and the ThinArc is not consumed."] # [doc = " The pointer is valid for as long as there are strong counts in the ThinArc."] # [inline] pub fn as_ptr (& self) -> * const c_void { self . ptr () } # [doc = " The reference count of this `Arc`."] # [doc = ""] # [doc = " The number does not include borrowed pointers,"] # [doc = " or temporary `Arc` pointers created with functions like"] # [doc = " [`ArcBorrow::with_arc`](crate::ArcBorrow::with_arc)."] # [doc = ""] # [doc = " The function is called `strong_count` to mirror `std::sync::Arc::strong_count`,"] # [doc = " however `triomphe::Arc` does not support weak references."] # [inline] pub fn strong_count (this : & Self) -> usize { Self :: with_arc (this , Arc :: strong_count) } }
};
}
