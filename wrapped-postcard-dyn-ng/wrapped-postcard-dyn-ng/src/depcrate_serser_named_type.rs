// Generated macro for ser_named_type (function)
macro_rules! Depcrate_serser_named_type {
() => {
// Module: crate::ser
// Provides: {"ser_named_type"}
// Dependencies: {}
fn ser_named_type (ty : & OwnedDataModelType , value : & Value , out : & mut Vec < u8 >) -> Result < () , Error > { match ty { OwnedDataModelType :: Bool => { let val = value . as_bool () . right () ? ; out . push (if val { 0x01 } else { 0x00 }) ; } OwnedDataModelType :: I8 => { let val = value . as_i64 () . right () ? ; let val = i8 :: try_from (val) ? ; out . push (val as u8) ; } OwnedDataModelType :: U8 => { let val = value . as_u64 () . right () ? ; let val = u8 :: try_from (val) ? ; out . push (val) ; } OwnedDataModelType :: I16 => { let val = value . as_i64 () . right () ? ; let val = i16 :: try_from (val) ? ; let val = zig_zag_i16 (val) ; let mut buf = [0u8 ; varint_max :: < i16 > ()] ; let used = varint_u16 (val , & mut buf) ; out . extend_from_slice (used) ; } OwnedDataModelType :: I32 => { let val = value . as_i64 () . right () ? ; let val = i32 :: try_from (val) ? ; let val = zig_zag_i32 (val) ; let mut buf = [0u8 ; varint_max :: < i32 > ()] ; let used = varint_u32 (val , & mut buf) ; out . extend_from_slice (used) ; } OwnedDataModelType :: I64 => { let val = value . as_i64 () . right () ? ; let val = zig_zag_i64 (val) ; let mut buf = [0u8 ; varint_max :: < i64 > ()] ; let used = varint_u64 (val , & mut buf) ; out . extend_from_slice (used) ; } OwnedDataModelType :: I128 => { let val = value . as_i64 () . right () ? ; let val = i128 :: from (val) ; let val = zig_zag_i128 (val) ; let mut buf = [0u8 ; varint_max :: < i128 > ()] ; let used = varint_u128 (val , & mut buf) ; out . extend_from_slice (used) ; } OwnedDataModelType :: U16 => { let val = value . as_u64 () . right () ? ; let val = u16 :: try_from (val) ? ; let mut buf = [0u8 ; varint_max :: < u16 > ()] ; let used = varint_u16 (val , & mut buf) ; out . extend_from_slice (used) ; } OwnedDataModelType :: U32 => { let val = value . as_u64 () . right () ? ; let val = u32 :: try_from (val) ? ; let mut buf = [0u8 ; varint_max :: < u32 > ()] ; let used = varint_u32 (val , & mut buf) ; out . extend_from_slice (used) ; } OwnedDataModelType :: U64 => { let val = value . as_u64 () . right () ? ; let mut buf = [0u8 ; varint_max :: < u64 > ()] ; let used = varint_u64 (val , & mut buf) ; out . extend_from_slice (used) ; } OwnedDataModelType :: U128 => { let val = value . as_u64 () . right () ? ; let val = u128 :: from (val) ; let mut buf = [0u8 ; varint_max :: < u128 > ()] ; let used = varint_u128 (val , & mut buf) ; out . extend_from_slice (used) ; } OwnedDataModelType :: Usize => { let val = value . as_u64 () . right () ? ; let val = usize :: try_from (val) ? ; let mut buf = [0u8 ; varint_max :: < usize > ()] ; let used = varint_usize (val , & mut buf) ; out . extend_from_slice (used) ; } OwnedDataModelType :: Isize => { let val = value . as_i64 () . right () ? ; let mut buf ; let used ; # [cfg (target_pointer_width = "16")] { let val = i16 :: try_from (val) ? ; let val = zig_zag_i16 (val) ; buf = [0u8 ; varint_max :: < i16 > ()] ; used = varint_u16 (val , & mut buf) ; } # [cfg (target_pointer_width = "32")] { let val = i32 :: try_from (val) ? ; let val = zig_zag_i32 (val) ; buf = [0u8 ; varint_max :: < i32 > ()] ; used = varint_u32 (val , & mut buf) ; } # [cfg (target_pointer_width = "64")] { let val = zig_zag_i64 (val) ; buf = [0u8 ; varint_max :: < i64 > ()] ; used = varint_u64 (val , & mut buf) ; } out . extend_from_slice (used) ; } OwnedDataModelType :: F32 => { let val = value . as_f64 () . right () ? ; let val = val as f32 ; let val = val . to_le_bytes () ; out . extend_from_slice (& val) ; } OwnedDataModelType :: F64 => { let val = value . as_f64 () . right () ? ; let val = val . to_le_bytes () ; out . extend_from_slice (& val) ; } OwnedDataModelType :: String | OwnedDataModelType :: Char => { let val = value . as_str () . right () ? ; let len = val . len () ; let mut buf = [0u8 ; varint_max :: < usize > ()] ; let used = varint_usize (len , & mut buf) ; out . extend_from_slice (used) ; out . extend_from_slice (val . as_bytes ()) ; } OwnedDataModelType :: ByteArray => { let val = value . as_array () . right () ? ; let len = val . len () ; let mut buf = [0u8 ; varint_max :: < usize > ()] ; let used = varint_usize (len , & mut buf) ; out . extend_from_slice (used) ; for b in val { let val = b . as_u64 () . right () ? ; let val = u8 :: try_from (val) ? ; out . push (val) ; } } OwnedDataModelType :: Option (ty) => { if value . is_null () { out . push (0x00) ; } else { out . push (0x01) ; ser_named_type (ty , value , out) ? ; } } OwnedDataModelType :: Unit => { } OwnedDataModelType :: Struct { name : _ , data : OwnedData :: Unit , } => { } OwnedDataModelType :: Struct { name : _ , data : OwnedData :: Newtype (ty) , } => { ser_named_type (ty , value , out) ? ; } OwnedDataModelType :: Seq (ty) => { let val = value . as_array () . right () ? ; let len = val . len () ; let mut buf = [0u8 ; varint_max :: < usize > ()] ; let used = varint_usize (len , & mut buf) ; out . extend_from_slice (used) ; for b in val { ser_named_type (ty , b , out) ? ; } } OwnedDataModelType :: Tuple (tys) | OwnedDataModelType :: Struct { name : _ , data : OwnedData :: Tuple (tys) , } => { if tys . len () == 1 { return ser_named_type (& tys [0] , value , out) ; } let val = value . as_array () . right () ? ; if val . len () != tys . len () { return Err (Error :: SchemaMismatch) ; } for (ty , val) in tys . iter () . zip (val . iter ()) { ser_named_type (ty , val , out) ? ; } } OwnedDataModelType :: Map { key , val } => { if * * key != OwnedDataModelType :: String { return Err (Error :: ShouldSupportButDont) ; } let obj = value . as_object () . right () ? ; let len = obj . len () ; let mut buf = [0u8 ; varint_max :: < usize > ()] ; let used = varint_usize (len , & mut buf) ; out . extend_from_slice (used) ; for (k , v) in obj . iter () { let len = k . len () ; let used = varint_usize (len , & mut buf) ; out . extend_from_slice (used) ; out . extend_from_slice (k . as_bytes ()) ; ser_named_type (val , v , out) ? ; } } OwnedDataModelType :: Struct { name : _ , data : OwnedData :: Struct (nvs) , } => { let val = value . as_object () . right () ? ; if val . len () != nvs . len () { return Err (Error :: SchemaMismatch) ; } for field in nvs . iter () { let v = val . get (field . name . as_ref ()) . right () ? ; ser_named_type (& field . ty , v , out) ? ; } } OwnedDataModelType :: Enum { name : _ , variants : nvars , } => { if let Some (s) = value . as_str () { let (idx , evar) = nvars . iter () . enumerate () . find (| (_i , v) | * v . name == * s) . right () ? ; if evar . data != OwnedData :: Unit { return Err (Error :: SchemaMismatch) ; } let mut buf = [0u8 ; varint_max :: < usize > ()] ; let used = varint_usize (idx , & mut buf) ; out . extend_from_slice (used) ; } else if let Some (o) = value . as_object () { if o . len () != 1 { return Err (Error :: SchemaMismatch) ; } let (k , v) = o . iter () . next () . right () ? ; let (idx , evar) = nvars . iter () . enumerate () . find (| (_i , v) | * v . name == * k) . right () ? ; let mut buf = [0u8 ; varint_max :: < usize > ()] ; let used = varint_usize (idx , & mut buf) ; out . extend_from_slice (used) ; match & evar . data { OwnedData :: Unit => { } OwnedData :: Newtype (ty) => { ser_named_type (ty , v , out) ? ; } OwnedData :: Tuple (tys) => { if tys . len () == 1 { return ser_named_type (& tys [0] , v , out) ; } let val = v . as_array () . right () ? ; if val . len () != tys . len () { return Err (Error :: SchemaMismatch) ; } for (ty , val) in tys . iter () . zip (val . iter ()) { ser_named_type (ty , val , out) ? ; } } OwnedData :: Struct (nvs) => { let val = v . as_object () . right () ? ; if val . len () != nvs . len () { return Err (Error :: SchemaMismatch) ; } for field in nvs . iter () { let v = val . get (field . name . as_ref ()) . right () ? ; ser_named_type (& field . ty , v , out) ? ; } } } } else { return Err (Error :: SchemaMismatch) ; } } OwnedDataModelType :: Schema => todo ! () , } Ok (()) }
};
}
