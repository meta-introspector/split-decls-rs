// Generated macro for impl_518 (impl)
macro_rules! Depcrate_sorted_linked_listimpl_518 {
() => {
// Module: crate::sorted_linked_list
// Provides: {"impl_518"}
// Dependencies: {}
impl < T , Idx , K , S > SortedLinkedListInner < T , Idx , K , S > where T : Ord , Idx : LenType , K : Kind , S : SortedLinkedListStorage < T , Idx > + ? Sized , { # [doc = " Pushes a value onto the list without checking if the list is full."] # [doc = ""] # [doc = " Complexity is worst-case *O*(n)."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Assumes that the list is not full."] pub unsafe fn push_unchecked (& mut self , value : T) { let new = self . free . into_usize () ; self . write_data_in_node_at (new , value) ; self . free = self . node_at (new) . next ; if let Some (head) = self . head . to_non_max () { if self . read_data_in_node_at (head) . cmp (self . read_data_in_node_at (new)) == K :: ordering () { let mut current = head ; while let Some (next) = self . node_at (current) . next . to_non_max () { if self . read_data_in_node_at (next) . cmp (self . read_data_in_node_at (new)) != K :: ordering () { break ; } current = next ; } self . node_at_mut (new) . next = self . node_at (current) . next ; self . node_at_mut (current) . next = Idx :: from_usize (new) ; } else { self . node_at_mut (new) . next = self . head ; self . head = Idx :: from_usize (new) ; } } else { self . node_at_mut (new) . next = self . head ; self . head = Idx :: from_usize (new) ; } } # [doc = " Pushes an element to the linked list and sorts it into place."] # [doc = ""] # [doc = " Complexity is worst-case *O*(n)."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use heapless::sorted_linked_list::{Max, SortedLinkedList};"] # [doc = " let mut ll: SortedLinkedList<_, Max, 3, u8> = SortedLinkedList::new_u8();"] # [doc = ""] # [doc = " // The largest value will always be first"] # [doc = " ll.push(1).unwrap();"] # [doc = " assert_eq!(ll.peek(), Some(&1));"] # [doc = ""] # [doc = " ll.push(2).unwrap();"] # [doc = " assert_eq!(ll.peek(), Some(&2));"] # [doc = ""] # [doc = " ll.push(3).unwrap();"] # [doc = " assert_eq!(ll.peek(), Some(&3));"] # [doc = ""] # [doc = " // This will not fit in the queue."] # [doc = " assert_eq!(ll.push(4), Err(4));"] # [doc = " ```"] pub fn push (& mut self , value : T) -> Result < () , T > { if self . is_full () { Err (value) } else { unsafe { self . push_unchecked (value) } Ok (()) } } # [doc = " Peek at the first element."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use heapless::sorted_linked_list::{Max, Min, SortedLinkedList};"] # [doc = " let mut ll_max: SortedLinkedList<_, Max, 3, u8> = SortedLinkedList::new_u8();"] # [doc = ""] # [doc = " // The largest value will always be first"] # [doc = " ll_max.push(1).unwrap();"] # [doc = " assert_eq!(ll_max.peek(), Some(&1));"] # [doc = " ll_max.push(2).unwrap();"] # [doc = " assert_eq!(ll_max.peek(), Some(&2));"] # [doc = " ll_max.push(3).unwrap();"] # [doc = " assert_eq!(ll_max.peek(), Some(&3));"] # [doc = ""] # [doc = " let mut ll_min: SortedLinkedList<_, Min, 3, u8> = SortedLinkedList::new_u8();"] # [doc = ""] # [doc = " // The Smallest value will always be first"] # [doc = " ll_min.push(3).unwrap();"] # [doc = " assert_eq!(ll_min.peek(), Some(&3));"] # [doc = " ll_min.push(2).unwrap();"] # [doc = " assert_eq!(ll_min.peek(), Some(&2));"] # [doc = " ll_min.push(1).unwrap();"] # [doc = " assert_eq!(ll_min.peek(), Some(&1));"] # [doc = " ```"] pub fn peek (& self) -> Option < & T > { self . head . to_non_max () . map (| head | self . read_data_in_node_at (head)) } # [doc = " Pop an element from the list without checking so the list is not empty."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Assumes that the list is not empty."] pub unsafe fn pop_unchecked (& mut self) -> T { let head = self . head . into_usize () ; let current = head ; self . head = self . node_at (head) . next ; self . node_at_mut (current) . next = self . free ; self . free = Idx :: from_usize (current) ; self . extract_data_in_node_at (current) } # [doc = " Pops the first element in the list."] # [doc = ""] # [doc = " Complexity is worst-case *O*(1)."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use heapless::sorted_linked_list::{Max, SortedLinkedList};"] # [doc = " let mut ll: SortedLinkedList<_, Max, 3, u8> = SortedLinkedList::new_u8();"] # [doc = ""] # [doc = " ll.push(1).unwrap();"] # [doc = " ll.push(2).unwrap();"] # [doc = ""] # [doc = " assert_eq!(ll.pop(), Some(2));"] # [doc = " assert_eq!(ll.pop(), Some(1));"] # [doc = " assert_eq!(ll.pop(), None);"] # [doc = " ```"] pub fn pop (& mut self) -> Option < T > { if self . is_empty () { None } else { Some (unsafe { self . pop_unchecked () }) } } # [doc = " Checks if the linked list is full."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use heapless::sorted_linked_list::{Max, SortedLinkedList};"] # [doc = " let mut ll: SortedLinkedList<_, Max, 3, u8> = SortedLinkedList::new_u8();"] # [doc = ""] # [doc = " assert_eq!(ll.is_full(), false);"] # [doc = ""] # [doc = " ll.push(1).unwrap();"] # [doc = " assert_eq!(ll.is_full(), false);"] # [doc = " ll.push(2).unwrap();"] # [doc = " assert_eq!(ll.is_full(), false);"] # [doc = " ll.push(3).unwrap();"] # [doc = " assert_eq!(ll.is_full(), true);"] # [doc = " ```"] # [inline] pub fn is_full (& self) -> bool { self . free . to_non_max () . is_none () } # [doc = " Checks if the linked list is empty."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " use heapless::sorted_linked_list::{Max, SortedLinkedList};"] # [doc = " let mut ll: SortedLinkedList<_, Max, 3, u8> = SortedLinkedList::new_u8();"] # [doc = ""] # [doc = " assert_eq!(ll.is_empty(), true);"] # [doc = ""] # [doc = " ll.push(1).unwrap();"] # [doc = " assert_eq!(ll.is_empty(), false);"] # [doc = " ```"] # [inline] pub fn is_empty (& self) -> bool { self . head . to_non_max () . is_none () } }
};
}
