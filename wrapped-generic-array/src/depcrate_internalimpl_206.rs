// Generated macro for impl_206 (impl)
macro_rules! Depcrate_internalimpl_206 {
() => {
// Module: crate::internal
// Provides: {"impl_206"}
// Dependencies: {}
impl < 'a , T , N : ArrayLength > IntrusiveArrayBuilder < 'a , T , N > { # [doc = " Begin building an array"] # [doc = ""] # [doc = " This method is `const` since Rust 1.83.0, but non-`const` before."] # [rustversion :: attr (since (1.83) , const)] # [inline (always)] pub fn new (array : & 'a mut GenericArray < MaybeUninit < T > , N >) -> IntrusiveArrayBuilder < 'a , T , N > { IntrusiveArrayBuilder { array , position : 0 } } # [doc = " Begin building an array from a `MaybeUninit<GenericArray<T, N>>`, which can potentially"] # [doc = " result in better codegen."] # [doc = ""] # [doc = " This method is `const` since Rust 1.83.0, but non-`const` before."] # [rustversion :: attr (since (1.83) , const)] # [inline (always)] pub fn new_alt (array : & 'a mut MaybeUninit < GenericArray < T , N > > ,) -> IntrusiveArrayBuilder < 'a , T , N > { IntrusiveArrayBuilder :: new (unsafe { mem :: transmute :: < & mut MaybeUninit < GenericArray < T , N > > , & mut GenericArray < MaybeUninit < T > , N > , > (array) }) } # [doc = " Consume an iterator, `.zip`-ing it to fill some or all of the array. This does not check if the"] # [doc = " iterator had extra elements or too few elements."] # [doc = ""] # [doc = " This makes no attempt to continue where a previous `extend` leaves off. Therefore, it should"] # [doc = " only be used once per `ArrayBuilder`."] # [inline (always)] pub unsafe fn extend (& mut self , source : impl Iterator < Item = T >) { let (destination , position) = (self . array . iter_mut () , & mut self . position) ; destination . zip (source) . for_each (| (dst , src) | { dst . write (src) ; * position += 1 ; }) ; } # [doc = " Consume a fallible iterator, `.zip`-ing it to fill some or all of the array. This does not check if the"] # [doc = " iterator had extra elements or too few elements."] # [doc = ""] # [doc = " This makes no attempt to continue where a previous `extend` leaves off. Therefore, it should"] # [doc = " only be used once per `ArrayBuilder`."] # [inline (always)] pub unsafe fn try_extend < E > (& mut self , source : impl Iterator < Item = Result < T , E > > ,) -> Result < () , E > { let (destination , position) = (self . array . iter_mut () , & mut self . position) ; destination . zip (source) . try_for_each (| (dst , src) | match src { Ok (value) => { dst . write (value) ; * position += 1 ; Ok (()) } Err (e) => Err (e) , }) } # [doc = " Returns true if the write position equals the array size"] # [inline (always)] pub const fn is_full (& self) -> bool { self . position == N :: USIZE } # [doc = " Creates a mutable iterator for writing to the array elements."] # [doc = ""] # [doc = " You MUST increment the position value (given as a mutable reference) as you iterate"] # [doc = " to mark how many elements have been created."] # [doc = ""] # [doc = " ```"] # [doc = " #[cfg(feature = \"internals\")]"] # [doc = " # {"] # [doc = " # use generic_array::{GenericArray, internals::IntrusiveArrayBuilder, typenum::U5};"] # [doc = " # use core::mem::MaybeUninit;"] # [doc = " # struct SomeType;"] # [doc = " fn make_some_struct() -> SomeType { SomeType }"] # [doc = " unsafe {"] # [doc = "     let mut array = MaybeUninit::<GenericArray<SomeType, U5>>::uninit();"] # [doc = "     let mut builder = IntrusiveArrayBuilder::new_alt(&mut array);"] # [doc = "     let (dst_iter, position) = builder.iter_position();"] # [doc = "     for dst in dst_iter {"] # [doc = "         dst.write(make_some_struct());"] # [doc = "         // MUST be done AFTER ownership of the value has been given to `dst.write`"] # [doc = "         *position += 1;"] # [doc = "     }"] # [doc = ""] # [doc = "     let your_array = builder.finish_and_assume_init();"] # [doc = " }"] # [doc = " # }"] # [doc = " ```"] # [inline (always)] pub unsafe fn iter_position (& '_ mut self ,) -> (slice :: IterMut < '_ , MaybeUninit < T > > , & '_ mut usize) { (self . array . iter_mut () , & mut self . position) } # [doc = " When done writing (assuming all elements have been written to), mark the builder"] # [doc = " as finished to avoid dropping the initialized elements."] # [inline (always)] pub const unsafe fn finish (self) { debug_assert ! (self . is_full ()) ; mem :: forget (self) } # [doc = " When done writing (assuming all elements have been written to), mark the builder"] # [doc = " as finished to avoid dropping the initialized elements, and get the inner array."] # [doc = ""] # [doc = " This method is `const` since Rust 1.71.0, but non-`const` before."] # [rustversion :: attr (since (1.71) , const)] # [inline (always)] pub unsafe fn finish_and_assume_init (self) -> GenericArray < T , N > { debug_assert ! (self . is_full ()) ; let array = ptr :: read (self . array as * const _ as * const GenericArray < T , N >) ; mem :: forget (self) ; array } # [doc = " Similar to [`GenericArray::assume_init`] but for arrays passed to `IntrusiveArrayBuilder`."] # [deprecated (since = "1.3.4" , note = "See `iter_position` for modern usage pattern without this function")] # [inline (always)] pub unsafe fn array_assume_init (array : GenericArray < MaybeUninit < T > , N >) -> GenericArray < T , N > { ptr :: read (& array as * const _ as * const MaybeUninit < GenericArray < T , N > >) . assume_init () } }
};
}
