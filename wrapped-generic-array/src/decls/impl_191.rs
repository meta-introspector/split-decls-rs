macro_rules! deps {
    () => {
        GenericArray!();
        IntoArrayLength!();
        LengthError!();
        ArrayLength!();
    };
}

macro_rules! impl_191 {
    () => {
        deps!();
        impl < T , N : ArrayLength > GenericArray < T , N > { # [doc = " Returns the number of elements in the array."] # [doc = ""] # [doc = " Equivalent to [`<N as Unsigned>::USIZE`](typenum::Unsigned) where `N` is the array length."] # [doc = ""] # [doc = " Useful for when only a type alias is available."] pub const fn len () -> usize { N :: USIZE } # [doc = " Extracts a slice containing the entire array."] # [inline (always)] pub const fn as_slice (& self) -> & [T] { unsafe { slice :: from_raw_parts (self as * const Self as * const T , N :: USIZE) } } # [doc = " Extracts a mutable slice containing the entire array."] # [doc = ""] # [doc = " This method is `const` since Rust 1.83.0, but non-`const` before."] # [rustversion :: attr (since (1.83) , const)] # [inline (always)] pub fn as_mut_slice (& mut self) -> & mut [T] { unsafe { slice :: from_raw_parts_mut (self as * mut Self as * mut T , N :: USIZE) } } # [doc = " Converts a slice to a generic array reference with inferred length."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the slice is not equal to the length of the array."] # [doc = ""] # [doc = " Consider [`TryFrom`]/[`TryInto`] for a fallible conversion,"] # [doc = " or [`try_from_slice`](GenericArray::try_from_slice) for use in const expressions."] # [inline (always)] pub const fn from_slice (slice : & [T]) -> & GenericArray < T , N > { if slice . len () != N :: USIZE { panic ! ("slice.len() != N in GenericArray::from_slice") ; } unsafe { & * (slice . as_ptr () as * const GenericArray < T , N >) } } # [doc = " Converts a slice to a generic array reference with inferred length."] # [doc = ""] # [doc = " This is a fallible alternative to [`from_slice`](GenericArray::from_slice), and can be used in const expressions,"] # [doc = " but [`TryFrom`]/[`TryInto`] are also available to do the same thing."] # [inline (always)] pub const fn try_from_slice (slice : & [T]) -> Result < & GenericArray < T , N > , LengthError > { if slice . len () != N :: USIZE { return Err (LengthError) ; } Ok (unsafe { & * (slice . as_ptr () as * const GenericArray < T , N >) }) } # [doc = " Converts a mutable slice to a mutable generic array reference with inferred length."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the slice is not equal to the length of the array."] # [doc = ""] # [doc = " Consider [`TryFrom`]/[`TryInto`] for a fallible conversion."] # [doc = ""] # [doc = " This method is `const` since Rust 1.83.0, but non-`const` before."] # [rustversion :: attr (since (1.83) , const)] # [inline (always)] pub fn from_mut_slice (slice : & mut [T]) -> & mut GenericArray < T , N > { assert ! (slice . len () == N :: USIZE , "slice.len() != N in GenericArray::from_mut_slice") ; unsafe { & mut * (slice . as_mut_ptr () as * mut GenericArray < T , N >) } } # [doc = " Converts a mutable slice to a mutable generic array reference with inferred length."] # [doc = ""] # [doc = " This is a fallible alternative to [`from_mut_slice`](GenericArray::from_mut_slice),"] # [doc = " and is equivalent to the [`TryFrom`] implementation with the added benefit of being `const`."] # [doc = ""] # [doc = " This method is `const` since Rust 1.83.0, but non-`const` before."] # [rustversion :: attr (since (1.83) , const)] # [inline (always)] pub fn try_from_mut_slice (slice : & mut [T]) -> Result < & mut GenericArray < T , N > , LengthError > { match slice . len () == N :: USIZE { true => Ok (GenericArray :: from_mut_slice (slice)) , false => Err (LengthError) , } } # [doc = " Borrows each element and returns a `GenericArray` of references"] # [doc = " with the same length as `self`."] # [doc = ""] # [doc = " This method is const since Rust 1.83.0, but non-const before."] # [doc = ""] # [doc = " See also [`each_mut`](GenericArray::each_mut) for mutable references."] # [rustversion :: attr (since (1.83) , const)] pub fn each_ref (& self) -> GenericArray < & T , N > { let mut out : GenericArray < MaybeUninit < * const T > , N > = GenericArray :: uninit () ; { let (this , out) = (self . as_slice () , out . as_mut_slice ()) ; let mut i = 0 ; while i < N :: USIZE { out [i] . write (ptr :: addr_of ! (this [i])) ; i += 1 ; } } unsafe { const_transmute (out) } } # [doc = " Borrows each element mutably and returns a `GenericArray` of mutable references"] # [doc = " with the same length as `self`."] # [doc = ""] # [doc = " This method is const since Rust 1.83.0, but non-const before."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use generic_array::{arr, GenericArray};"] # [doc = " let mut ga = arr![1, 2, 3];"] # [doc = " let mut_refs: GenericArray<&mut i32, _> = ga.each_mut();"] # [doc = " for r in mut_refs {"] # [doc = "     *r *= 2;"] # [doc = " }"] # [doc = " assert_eq!(ga, arr![2, 4, 6]);"] # [doc = " ```"] # [rustversion :: attr (since (1.83) , const)] pub fn each_mut (& mut self) -> GenericArray < & mut T , N > { let mut out : GenericArray < MaybeUninit < * mut T > , N > = GenericArray :: uninit () ; { let (this , out) = (self . as_mut_slice () , out . as_mut_slice ()) ; let mut i = 0 ; while i < N :: USIZE { out [i] . write (ptr :: addr_of_mut ! (this [i])) ; i += 1 ; } } unsafe { const_transmute (out) } } # [doc = " Converts a slice of `T` elements into a slice of `GenericArray<T, N>` chunks."] # [doc = ""] # [doc = " Any remaining elements that do not fill the array will be returned as a second slice."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `N` is `U0` _AND_ the input slice is not empty."] pub const fn chunks_from_slice (slice : & [T]) -> (& [GenericArray < T , N >] , & [T]) { if N :: USIZE == 0 { assert ! (slice . is_empty () , "GenericArray length N must be non-zero") ; return (& [] , & []) ; } let num_chunks = slice . len () / N :: USIZE ; let num_in_chunks = num_chunks * N :: USIZE ; let num_remainder = slice . len () - num_in_chunks ; unsafe { (slice :: from_raw_parts (slice . as_ptr () as * const GenericArray < T , N > , num_chunks) , slice :: from_raw_parts (slice . as_ptr () . add (num_in_chunks) , num_remainder) ,) } } # [doc = " Converts a mutable slice of `T` elements into a mutable slice `GenericArray<T, N>` chunks."] # [doc = ""] # [doc = " Any remaining elements that do not fill the array will be returned as a second slice."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `N` is `U0` _AND_ the input slice is not empty."] # [doc = ""] # [doc = " This method is `const` since Rust 1.83.0, but non-`const` before."] # [rustversion :: attr (since (1.83) , const)] pub fn chunks_from_slice_mut (slice : & mut [T]) -> (& mut [GenericArray < T , N >] , & mut [T]) { if N :: USIZE == 0 { assert ! (slice . is_empty () , "GenericArray length N must be non-zero") ; return (& mut [] , & mut []) ; } let num_chunks = slice . len () / N :: USIZE ; let num_in_chunks = num_chunks * N :: USIZE ; let num_remainder = slice . len () - num_in_chunks ; unsafe { (slice :: from_raw_parts_mut (slice . as_mut_ptr () as * mut GenericArray < T , N > , num_chunks ,) , slice :: from_raw_parts_mut (slice . as_mut_ptr () . add (num_in_chunks) , num_remainder) ,) } } # [doc = " Convert a slice of `GenericArray<T, N>` into a slice of `T`, effectively flattening the arrays."] # [inline (always)] pub const fn slice_from_chunks (slice : & [GenericArray < T , N >]) -> & [T] { unsafe { slice :: from_raw_parts (slice . as_ptr () as * const T , slice . len () * N :: USIZE) } } # [doc = " Convert a slice of `GenericArray<T, N>` into a slice of `T`, effectively flattening the arrays."] # [doc = ""] # [doc = " This method is `const` since Rust 1.83.0, but non-`const` before."] # [rustversion :: attr (since (1.83) , const)] # [inline (always)] pub fn slice_from_chunks_mut (slice : & mut [GenericArray < T , N >]) -> & mut [T] { unsafe { slice :: from_raw_parts_mut (slice . as_mut_ptr () as * mut T , slice . len () * N :: USIZE) } } # [doc = " Convert a native array into `GenericArray` of the same length and type."] # [doc = ""] # [doc = " This is the `const` equivalent of using the standard [`From`]/[`Into`] traits methods."] # [inline (always)] pub const fn from_array < const U : usize > (value : [T ; U]) -> Self where Const < U > : IntoArrayLength < ArrayLength = N > , { unsafe { crate :: const_transmute (value) } } # [doc = " Convert the `GenericArray` into a native array of the same length and type."] # [doc = ""] # [doc = " This is the `const` equivalent of using the standard [`From`]/[`Into`] traits methods."] # [inline (always)] pub const fn into_array < const U : usize > (self) -> [T ; U] where Const < U > : IntoArrayLength < ArrayLength = N > , { unsafe { crate :: const_transmute (self) } } # [doc = " Convert a slice of native arrays into a slice of `GenericArray`s."] # [inline (always)] pub const fn from_chunks < const U : usize > (chunks : & [[T ; U]]) -> & [GenericArray < T , N >] where Const < U > : IntoArrayLength < ArrayLength = N > , { unsafe { mem :: transmute (chunks) } } # [doc = " Convert a mutable slice of native arrays into a mutable slice of `GenericArray`s."] # [doc = ""] # [doc = " This method is `const` since Rust 1.83.0, but non-`const` before."] # [rustversion :: attr (since (1.83) , const)] # [inline (always)] pub fn from_chunks_mut < const U : usize > (chunks : & mut [[T ; U]]) -> & mut [GenericArray < T , N >] where Const < U > : IntoArrayLength < ArrayLength = N > , { unsafe { mem :: transmute (chunks) } } # [doc = " Converts a slice `GenericArray<T, N>` into a slice of `[T; N]`"] # [inline (always)] pub const fn into_chunks < const U : usize > (chunks : & [GenericArray < T , N >]) -> & [[T ; U]] where Const < U > : IntoArrayLength < ArrayLength = N > , { unsafe { mem :: transmute (chunks) } } # [doc = " Converts a mutable slice `GenericArray<T, N>` into a mutable slice of `[T; N]`"] # [doc = ""] # [doc = " This method is `const` since Rust 1.83.0, but non-`const` before."] # [rustversion :: attr (since (1.83) , const)] # [inline (always)] pub fn into_chunks_mut < const U : usize > (chunks : & mut [GenericArray < T , N >]) -> & mut [[T ; U]] where Const < U > : IntoArrayLength < ArrayLength = N > , { unsafe { mem :: transmute (chunks) } } }
    };
}

impl_191!()