// Generated macro for LoadConnection (trait)
macro_rules! Depcrate_connectionLoadConnection {
() => {
// Module: crate::connection
// Provides: {"LoadConnection"}
// Dependencies: {}
# [doc = " The specific part of a [`Connection`] which actually loads data from the database"] # [doc = ""] # [doc = " This is a separate trait to allow connection implementations to specify"] # [doc = " different loading modes via the generic parameter."] pub trait LoadConnection < B = DefaultLoadingMode > : Connection { # [doc = " The cursor type returned by [`LoadConnection::load`]"] # [doc = ""] # [doc = " Users should handle this as opaque type that implements [`Iterator`]"] type Cursor < 'conn , 'query > : Iterator < Item = QueryResult < < Self as LoadConnection < B > > :: Row < 'conn , 'query > > , > where Self : 'conn ; # [doc = " The row type used as [`Iterator::Item`] for the iterator implementation"] # [doc = " of [`LoadConnection::Cursor`]"] type Row < 'conn , 'query > : crate :: row :: Row < 'conn , Self :: Backend > where Self : 'conn ; # [doc = " Executes a given query and returns any requested values"] # [doc = ""] # [doc = " This function executes a given query and returns the"] # [doc = " query result as given by the database. **Normal users"] # [doc = " should not use this function**. Use"] # [doc = " [`QueryDsl::load`](crate::QueryDsl) instead."] # [doc = ""] # [doc = " This function is useful for people trying to build an alternative"] # [doc = " dsl on top of diesel. It returns an [`impl Iterator<Item = QueryResult<&impl Row<Self::Backend>>`](Iterator)."] # [doc = " This type can be used to iterate over all rows returned by the database."] # [diesel_derives :: __diesel_public_if (feature = "i-implement-a-third-party-backend-and-opt-into-breaking-changes")] fn load < 'conn , 'query , T > (& 'conn mut self , source : T ,) -> QueryResult < Self :: Cursor < 'conn , 'query > > where T : Query + QueryFragment < Self :: Backend > + QueryId + 'query , Self :: Backend : QueryMetadata < T :: SqlType > ; }
};
}
