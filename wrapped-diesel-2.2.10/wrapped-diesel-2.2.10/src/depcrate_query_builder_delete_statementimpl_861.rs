// Generated macro for impl_861 (impl)
macro_rules! Depcrate_query_builder_delete_statementimpl_861 {
() => {
// Module: crate::query_builder::delete_statement
// Provides: {"impl_861"}
// Dependencies: {}
impl < T : QuerySource , U > DeleteStatement < T , U , NoReturningClause > { pub (crate) fn new (table : T , where_clause : U) -> Self { DeleteStatement { from_clause : FromClause :: new (table) , where_clause , returning : NoReturningClause , } } # [doc = " Adds the given predicate to the `WHERE` clause of the statement being"] # [doc = " constructed."] # [doc = ""] # [doc = " If there is already a `WHERE` clause, the predicate will be appended"] # [doc = " with `AND`. There is no difference in behavior between"] # [doc = " `delete(table.filter(x))` and `delete(table).filter(x)`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # include!(\"../../doctest_setup.rs\");"] # [doc = " #"] # [doc = " # fn main() {"] # [doc = " #     use schema::users::dsl::*;"] # [doc = " #     let connection = &mut establish_connection();"] # [doc = " let deleted_rows = diesel::delete(users)"] # [doc = "     .filter(name.eq(\"Sean\"))"] # [doc = "     .execute(connection);"] # [doc = " assert_eq!(Ok(1), deleted_rows);"] # [doc = ""] # [doc = " let expected_names = vec![\"Tess\".to_string()];"] # [doc = " let names = users.select(name).load(connection);"] # [doc = ""] # [doc = " assert_eq!(Ok(expected_names), names);"] # [doc = " # }"] # [doc = " ```"] pub fn filter < Predicate > (self , predicate : Predicate) -> Filter < Self , Predicate > where Self : FilterDsl < Predicate > , { FilterDsl :: filter (self , predicate) } # [doc = " Adds to the `WHERE` clause of a query using `OR`"] # [doc = ""] # [doc = " If there is already a `WHERE` clause, the result will be `(old OR new)`."] # [doc = " Calling `foo.filter(bar).or_filter(baz)`"] # [doc = " is identical to `foo.filter(bar.or(baz))`."] # [doc = " However, the second form is much harder to do dynamically."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # include!(\"../../doctest_setup.rs\");"] # [doc = " #"] # [doc = " # fn main() {"] # [doc = " #     use schema::users::dsl::*;"] # [doc = " #     let connection = &mut establish_connection();"] # [doc = " let deleted_rows = diesel::delete(users)"] # [doc = "     .filter(name.eq(\"Sean\"))"] # [doc = "     .or_filter(name.eq(\"Tess\"))"] # [doc = "     .execute(connection);"] # [doc = " assert_eq!(Ok(2), deleted_rows);"] # [doc = ""] # [doc = " let num_users = users.count().first(connection);"] # [doc = ""] # [doc = " assert_eq!(Ok(0), num_users);"] # [doc = " # }"] # [doc = " ```"] pub fn or_filter < Predicate > (self , predicate : Predicate) -> OrFilter < Self , Predicate > where Self : OrFilterDsl < Predicate > , { OrFilterDsl :: or_filter (self , predicate) } # [doc = " Boxes the `WHERE` clause of this delete statement."] # [doc = ""] # [doc = " This is useful for cases where you want to conditionally modify a query,"] # [doc = " but need the type to remain the same. The backend must be specified as"] # [doc = " part of this. It is not possible to box a query and have it be useable"] # [doc = " on multiple backends."] # [doc = ""] # [doc = " A boxed query will incur a minor performance penalty, as the query builder"] # [doc = " can no longer be inlined by the compiler. For most applications this cost"] # [doc = " will be minimal."] # [doc = ""] # [doc = " ### Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # include!(\"../../doctest_setup.rs\");"] # [doc = " #"] # [doc = " # fn main() {"] # [doc = " #     run_test().unwrap();"] # [doc = " # }"] # [doc = " #"] # [doc = " # fn run_test() -> QueryResult<()> {"] # [doc = " #     use std::collections::HashMap;"] # [doc = " #     use schema::users::dsl::*;"] # [doc = " #     let connection = &mut establish_connection();"] # [doc = " #     let mut params = HashMap::new();"] # [doc = " #     params.insert(\"sean_has_been_a_jerk\", true);"] # [doc = " let mut query = diesel::delete(users)"] # [doc = "     .into_boxed();"] # [doc = ""] # [doc = " if params[\"sean_has_been_a_jerk\"] {"] # [doc = "     query = query.filter(name.eq(\"Sean\"));"] # [doc = " }"] # [doc = ""] # [doc = " let deleted_rows = query.execute(connection)?;"] # [doc = " assert_eq!(1, deleted_rows);"] # [doc = ""] # [doc = " let expected_names = vec![\"Tess\"];"] # [doc = " let names = users.select(name).load::<String>(connection)?;"] # [doc = ""] # [doc = " assert_eq!(expected_names, names);"] # [doc = " #     Ok(())"] # [doc = " # }"] # [doc = " ```"] pub fn into_boxed < 'a , DB > (self) -> IntoBoxed < 'a , Self , DB > where DB : Backend , Self : BoxedDsl < 'a , DB > , { BoxedDsl :: internal_into_boxed (self) } }
};
}
