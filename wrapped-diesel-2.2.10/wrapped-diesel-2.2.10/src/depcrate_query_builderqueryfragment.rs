// Generated macro for QueryFragment (trait)
macro_rules! Depcrate_query_builderQueryFragment {
() => {
// Module: crate::query_builder
// Provides: {"QueryFragment"}
// Dependencies: {}
# [doc = " An untyped fragment of SQL."] # [doc = ""] # [doc = " This may be a complete SQL command (such as an update statement without a"] # [doc = " `RETURNING` clause), or a subsection (such as our internal types used to"] # [doc = " represent a `WHERE` clause). Implementations of [`ExecuteDsl`] and"] # [doc = " [`LoadQuery`] will generally require that this trait be implemented."] # [doc = ""] # [doc = " [`ExecuteDsl`]: crate::query_dsl::methods::ExecuteDsl"] # [doc = " [`LoadQuery`]: crate::query_dsl::methods::LoadQuery"] # [diagnostic :: on_unimplemented (message = "`{Self}` is no valid SQL fragment for the `{DB}` backend" , note = "this usually means that the `{DB}` database system does not support \n\
            this SQL syntax")] pub trait QueryFragment < DB : Backend , SP = self :: private :: NotSpecialized > { # [doc = " Walk over this `QueryFragment` for all passes."] # [doc = ""] # [doc = " This method is where the actual behavior of an AST node is implemented."] # [doc = " This method will contain the behavior required for all possible AST"] # [doc = " passes. See [`AstPass`] for more details."] # [doc = ""] fn walk_ast < 'b > (& 'b self , pass : AstPass < '_ , 'b , DB >) -> QueryResult < () > ; # [doc = " Converts this `QueryFragment` to its SQL representation."] # [doc = ""] # [doc = " This method should only be called by implementations of `Connection`."] # [diesel_derives :: __diesel_public_if (feature = "i-implement-a-third-party-backend-and-opt-into-breaking-changes")] fn to_sql (& self , out : & mut DB :: QueryBuilder , backend : & DB) -> QueryResult < () > { let mut options = AstPassToSqlOptions :: default () ; self . walk_ast (AstPass :: to_sql (out , & mut options , backend)) } # [doc = " Serializes all bind parameters in this query."] # [doc = ""] # [doc = " A bind parameter is a value which is sent separately from the query"] # [doc = " itself. It is represented in SQL with a placeholder such as `?` or `$1`."] # [doc = ""] # [doc = " This method should only be called by implementations of `Connection`."] # [diesel_derives :: __diesel_public_if (feature = "i-implement-a-third-party-backend-and-opt-into-breaking-changes")] fn collect_binds < 'b > (& 'b self , out : & mut DB :: BindCollector < 'b > , metadata_lookup : & mut DB :: MetadataLookup , backend : & 'b DB ,) -> QueryResult < () > { self . walk_ast (AstPass :: collect_binds (out , metadata_lookup , backend)) } # [doc = " Is this query safe to store in the prepared statement cache?"] # [doc = ""] # [doc = " In order to keep our prepared statement cache at a reasonable size, we"] # [doc = " avoid caching any queries which represent a potentially unbounded number"] # [doc = " of SQL queries. Generally this will only return `true` for queries for"] # [doc = " which `to_sql` will always construct exactly identical SQL."] # [doc = ""] # [doc = " Some examples of where this method will return `false` are:"] # [doc = ""] # [doc = " - `SqlLiteral` (We don't know if the SQL was constructed dynamically, so"] # [doc = "   we must assume that it was)"] # [doc = " - `In` and `NotIn` (Each value requires a separate bind param"] # [doc = "   placeholder)"] # [doc = ""] # [doc = " This method should only be called by implementations of `Connection`."] # [diesel_derives :: __diesel_public_if (feature = "i-implement-a-third-party-backend-and-opt-into-breaking-changes")] fn is_safe_to_cache_prepared (& self , backend : & DB) -> QueryResult < bool > { let mut result = true ; self . walk_ast (AstPass :: is_safe_to_cache_prepared (& mut result , backend)) ? ; Ok (result) } # [doc = " Does walking this AST have any effect?"] # [diesel_derives :: __diesel_public_if (feature = "i-implement-a-third-party-backend-and-opt-into-breaking-changes")] fn is_noop (& self , backend : & DB) -> QueryResult < bool > { let mut result = true ; self . walk_ast (AstPass :: is_noop (& mut result , backend)) ? ; Ok (result) } }
};
}
