// Generated macro for TransactionManager (trait)
macro_rules! Depcrate_connection_transaction_managerTransactionManager {
() => {
// Module: crate::connection::transaction_manager
// Provides: {"TransactionManager"}
// Dependencies: {}
# [doc = " Manages the internal transaction state for a connection."] # [doc = ""] # [doc = " You will not need to interact with this trait, unless you are writing an"] # [doc = " implementation of [`Connection`]."] pub trait TransactionManager < Conn : Connection > { # [doc = " Data stored as part of the connection implementation"] # [doc = " to track the current transaction state of a connection"] type TransactionStateData ; # [doc = " Begin a new transaction or savepoint"] # [doc = ""] # [doc = " If the transaction depth is greater than 0,"] # [doc = " this should create a savepoint instead."] # [doc = " This function is expected to increment the transaction depth by 1."] fn begin_transaction (conn : & mut Conn) -> QueryResult < () > ; # [doc = " Rollback the inner-most transaction or savepoint"] # [doc = ""] # [doc = " If the transaction depth is greater than 1,"] # [doc = " this should rollback to the most recent savepoint."] # [doc = " This function is expected to decrement the transaction depth by 1."] fn rollback_transaction (conn : & mut Conn) -> QueryResult < () > ; # [doc = " Commit the inner-most transaction or savepoint"] # [doc = ""] # [doc = " If the transaction depth is greater than 1,"] # [doc = " this should release the most recent savepoint."] # [doc = " This function is expected to decrement the transaction depth by 1."] fn commit_transaction (conn : & mut Conn) -> QueryResult < () > ; # [doc = " Fetch the current transaction status as mutable"] # [doc = ""] # [doc = " Used to ensure that `begin_test_transaction` is not called when already"] # [doc = " inside of a transaction, and that operations are not run in a `InError`"] # [doc = " transaction manager."] # [diesel_derives :: __diesel_public_if (feature = "i-implement-a-third-party-backend-and-opt-into-breaking-changes")] fn transaction_manager_status_mut (conn : & mut Conn) -> & mut TransactionManagerStatus ; # [doc = " Executes the given function inside of a database transaction"] # [doc = ""] # [doc = " Each implementation of this function needs to fulfill the documented"] # [doc = " behaviour of [`Connection::transaction`]"] fn transaction < F , R , E > (conn : & mut Conn , callback : F) -> Result < R , E > where F : FnOnce (& mut Conn) -> Result < R , E > , E : From < Error > , { Self :: begin_transaction (conn) ? ; match callback (& mut * conn) { Ok (value) => { Self :: commit_transaction (conn) ? ; Ok (value) } Err (user_error) => match Self :: rollback_transaction (conn) { Ok (()) => Err (user_error) , Err (Error :: BrokenTransactionManager) => { Err (user_error) } Err (rollback_error) => Err (rollback_error . into ()) , } , } } # [doc = " This methods checks if the connection manager is considered to be broken"] # [doc = " by connection pool implementations"] # [doc = ""] # [doc = " A connection manager is considered to be broken by default if it either"] # [doc = " contains an open transaction (because you don't want to have connections"] # [doc = " with open transactions in your pool) or when the transaction manager is"] # [doc = " in an error state."] # [diesel_derives :: __diesel_public_if (feature = "i-implement-a-third-party-backend-and-opt-into-breaking-changes")] fn is_broken_transaction_manager (conn : & mut Conn) -> bool { match Self :: transaction_manager_status_mut (conn) . transaction_state () { Ok (ValidTransactionManagerStatus { in_transaction : None , }) => false , Err (_) => true , Ok (ValidTransactionManagerStatus { in_transaction : Some (s) , }) => ! s . test_transaction , } } }
};
}
