// Generated macro for allow_columns_to_appear_in_same_group_by_clause (macro)
macro_rules! Depcrate_macrosallow_columns_to_appear_in_same_group_by_clause {
() => {
// Module: crate::macros
// Provides: {"allow_columns_to_appear_in_same_group_by_clause"}
// Dependencies: {}
# [doc = " Allow two or more columns which are otherwise unrelated to be used together"] # [doc = " in a group by clause."] # [doc = ""] # [doc = " This macro must be invoked any time two columns need to appear in the same"] # [doc = " group by clause. When this macro is invoked with more than 2 columns, every"] # [doc = " combination of those columns will be allowed to appear together."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # include!(\"../doctest_setup.rs\");"] # [doc = " # use crate::schema::{users, posts};"] # [doc = " // This would be required"] # [doc = ""] # [doc = " allow_columns_to_appear_in_same_group_by_clause!(users::name, posts::id, posts::title);"] # [doc = " # fn main() {"] # [doc = " // to do implement the following join"] # [doc = " users::table.inner_join(posts::table).group_by((users::name, posts::id, posts::title))"] # [doc = " # ;"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " When more than two columns are passed, the relevant code is generated for"] # [doc = " every combination of those columns. This code would be equivalent to the"] # [doc = " previous example."] # [doc = ""] # [doc = " ```"] # [doc = " # include!(\"../doctest_setup.rs\");"] # [doc = " # use crate::schema::{users, posts};"] # [doc = " #"] # [doc = " allow_columns_to_appear_in_same_group_by_clause!(users::name, posts::title);"] # [doc = " allow_columns_to_appear_in_same_group_by_clause!(users::name, posts::id);"] # [doc = " allow_columns_to_appear_in_same_group_by_clause!(posts::title, posts::id);"] # [doc = " # fn main() {}"] # [doc = " ```"] # [macro_export] macro_rules ! allow_columns_to_appear_in_same_group_by_clause { ($ ($ left_path : tt) ::+, $ ($ right_path : tt) ::+ $ (,) ?) => { $ crate :: __diesel_impl_allow_in_same_group_by_clause ! { left = [$ ($ left_path) ::+] , $ ($ right_path) ::+, } } ; ($ ($ left_path : tt) ::+, $ ($ right_path : tt) ::+, $ ($ other : tt) *) => { $ crate :: __diesel_impl_allow_in_same_group_by_clause ! { left = [$ ($ left_path) ::+] , $ ($ right_path) ::+, $ ($ other) * } $ crate :: allow_columns_to_appear_in_same_group_by_clause ! { $ ($ right_path) ::+, $ ($ other) * } } ; ($ last_col : ty ,) => { } ; () => { } ; }
};
}
