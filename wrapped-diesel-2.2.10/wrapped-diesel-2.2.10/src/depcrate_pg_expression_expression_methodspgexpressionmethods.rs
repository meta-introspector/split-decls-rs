// Generated macro for PgExpressionMethods (trait)
macro_rules! Depcrate_pg_expression_expression_methodsPgExpressionMethods {
() => {
// Module: crate::pg::expression::expression_methods
// Provides: {"PgExpressionMethods"}
// Dependencies: {}
# [doc = " PostgreSQL specific methods which are present on all expressions."] # [cfg (feature = "postgres_backend")] pub trait PgExpressionMethods : Expression + Sized { # [doc = " Creates a PostgreSQL `IS NOT DISTINCT FROM` expression."] # [doc = ""] # [doc = " This behaves identically to the `=` operator, except that `NULL` is"] # [doc = " treated as a normal value."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # include!(\"../../doctest_setup.rs\");"] # [doc = " #"] # [doc = " # fn main() {"] # [doc = " #     use schema::users::dsl::*;"] # [doc = " #     let connection = &mut establish_connection();"] # [doc = " let distinct = users.select(id).filter(name.is_distinct_from(\"Sean\"));"] # [doc = " let not_distinct = users.select(id).filter(name.is_not_distinct_from(\"Sean\"));"] # [doc = " assert_eq!(Ok(2), distinct.first(connection));"] # [doc = " assert_eq!(Ok(1), not_distinct.first(connection));"] # [doc = " # }"] # [doc = " ```"] # [allow (clippy :: wrong_self_convention)] fn is_not_distinct_from < T > (self , other : T) -> dsl :: IsNotDistinctFrom < Self , T > where Self :: SqlType : SqlType , T : AsExpression < Self :: SqlType > , { Grouped (IsNotDistinctFrom :: new (self , other . as_expression ())) } # [doc = " Creates a PostgreSQL `IS DISTINCT FROM` expression."] # [doc = ""] # [doc = " This behaves identically to the `!=` operator, except that `NULL` is"] # [doc = " treated as a normal value."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # include!(\"../../doctest_setup.rs\");"] # [doc = " #"] # [doc = " # fn main() {"] # [doc = " #     use schema::users::dsl::*;"] # [doc = " #     let connection = &mut establish_connection();"] # [doc = " let distinct = users.select(id).filter(name.is_distinct_from(\"Sean\"));"] # [doc = " let not_distinct = users.select(id).filter(name.is_not_distinct_from(\"Sean\"));"] # [doc = " assert_eq!(Ok(2), distinct.first(connection));"] # [doc = " assert_eq!(Ok(1), not_distinct.first(connection));"] # [doc = " # }"] # [doc = " ```"] # [allow (clippy :: wrong_self_convention)] fn is_distinct_from < T > (self , other : T) -> dsl :: IsDistinctFrom < Self , T > where Self :: SqlType : SqlType , T : AsExpression < Self :: SqlType > , { Grouped (IsDistinctFrom :: new (self , other . as_expression ())) } }
};
}
