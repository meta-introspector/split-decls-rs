// Generated macro for joinable (macro)
macro_rules! Depcrate_macrosjoinable {
() => {
// Module: crate::macros
// Provides: {"joinable"}
// Dependencies: {}
# [doc = " Allow two tables to be referenced in a join query without providing an"] # [doc = " explicit `ON` clause."] # [doc = ""] # [doc = " The generated `ON` clause will always join to the primary key of the parent"] # [doc = " table. This macro removes the need to call [`.on`] explicitly, you will"] # [doc = " still need to invoke"] # [doc = " [`allow_tables_to_appear_in_same_query!`](crate::allow_tables_to_appear_in_same_query)"] # [doc = " for these two tables to be able to use the resulting query, unless you are"] # [doc = " using `diesel print-schema` which will generate it for you."] # [doc = ""] # [doc = " If you are using `diesel print-schema`, an invocation of this macro"] # [doc = " will be generated for every foreign key in your database unless"] # [doc = " one of the following is true:"] # [doc = ""] # [doc = " - The foreign key references something other than the primary key"] # [doc = " - The foreign key is composite"] # [doc = " - There is more than one foreign key connecting two tables"] # [doc = " - The foreign key is self-referential"] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " # include!(\"../doctest_setup.rs\");"] # [doc = " use schema::*;"] # [doc = ""] # [doc = " # /*"] # [doc = " joinable!(posts -> users (user_id));"] # [doc = " allow_tables_to_appear_in_same_query!(posts, users);"] # [doc = " # */"] # [doc = ""] # [doc = " # fn main() {"] # [doc = " let implicit_on_clause = users::table.inner_join(posts::table);"] # [doc = " let implicit_on_clause_sql = diesel::debug_query::<DB, _>(&implicit_on_clause).to_string();"] # [doc = ""] # [doc = " let explicit_on_clause = users::table"] # [doc = "     .inner_join(posts::table.on(posts::user_id.eq(users::id)));"] # [doc = " let explicit_on_clause_sql = diesel::debug_query::<DB, _>(&explicit_on_clause).to_string();"] # [doc = ""] # [doc = " assert_eq!(implicit_on_clause_sql, explicit_on_clause_sql);"] # [doc = " # }"] # [doc = ""] # [doc = " ```"] # [doc = ""] # [doc = " In the example above, the line `joinable!(posts -> users (user_id));`"] # [doc = ""] # [doc = " specifies the relation of the tables and the ON clause in the following way:"] # [doc = ""] # [doc = " `child_table -> parent_table (foreign_key)`"] # [doc = ""] # [doc = " * `parent_table` is the Table with the Primary key."] # [doc = ""] # [doc = " * `child_table` is the Table with the Foreign key."] # [doc = ""] # [doc = " So given the Table declaration from [Associations docs](crate::associations)"] # [doc = ""] # [doc = " * The parent table would be `User`"] # [doc = " * The child table would be `Post`"] # [doc = " * and the Foreign key would be `Post.user_id`"] # [doc = ""] # [doc = " For joins that do not explicitly use on clauses via [`JoinOnDsl`](crate::prelude::JoinOnDsl)"] # [doc = " the following on clause is generated implicitly:"] # [doc = " ```sql"] # [doc = " post JOIN users ON posts.user_id = users.id"] # [doc = " ```"] # [macro_export] macro_rules ! joinable { ($ ($ child : ident) ::* -> $ ($ parent : ident) ::* ($ source : ident)) => { $ crate :: joinable_inner ! ($ ($ child) ::* :: table => $ ($ parent) ::* :: table : ($ ($ child) ::* ::$ source = $ ($ parent) ::* :: table)) ; $ crate :: joinable_inner ! ($ ($ parent) ::* :: table => $ ($ child) ::* :: table : ($ ($ child) ::* ::$ source = $ ($ parent) ::* :: table)) ; } }
};
}
