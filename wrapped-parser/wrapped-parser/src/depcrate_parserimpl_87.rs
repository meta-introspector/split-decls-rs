// Generated macro for impl_87 (impl)
macro_rules! Depcrate_parserimpl_87 {
() => {
// Module: crate::parser
// Provides: {"impl_87"}
// Dependencies: {}
impl CompletedMarker { fn new (start_pos : u32 , end_pos : u32 , kind : SyntaxKind) -> Self { CompletedMarker { start_pos , end_pos , kind } } # [doc = " This method allows to create a new node which starts"] # [doc = " *before* the current one. That is, parser could start"] # [doc = " node `A`, then complete it, and then after parsing the"] # [doc = " whole `A`, decide that it should have started some node"] # [doc = " `B` before starting `A`. `precede` allows to do exactly"] # [doc = " that. See also docs about"] # [doc = " [`Event::Start::forward_parent`](crate::event::Event::Start::forward_parent)."] # [doc = ""] # [doc = " Given completed events `[START, FINISH]` and its corresponding"] # [doc = " `CompletedMarker(pos: 0, _)`."] # [doc = " Append a new `START` events as `[START, FINISH, NEWSTART]`,"] # [doc = " then mark `NEWSTART` as `START`'s parent with saving its relative"] # [doc = " distance to `NEWSTART` into forward_parent(=2 in this case);"] pub (crate) fn precede (self , p : & mut Parser < '_ >) -> Marker { let new_pos = p . start () ; let idx = self . start_pos as usize ; match & mut p . events [idx] { Event :: Start { forward_parent , .. } => { * forward_parent = Some (new_pos . pos - self . start_pos) ; } _ => unreachable ! () , } new_pos } # [doc = " Extends this completed marker *to the left* up to `m`."] pub (crate) fn extend_to (self , p : & mut Parser < '_ > , mut m : Marker) -> CompletedMarker { m . bomb . defuse () ; let idx = m . pos as usize ; match & mut p . events [idx] { Event :: Start { forward_parent , .. } => { * forward_parent = Some (self . start_pos - m . pos) ; } _ => unreachable ! () , } self } pub (crate) fn kind (& self) -> SyntaxKind { self . kind } pub (crate) fn last_token (& self , p : & Parser < '_ >) -> Option < SyntaxKind > { let end_pos = self . end_pos as usize ; debug_assert_eq ! (p . events [end_pos - 1] , Event :: Finish) ; p . events [.. end_pos] . iter () . rev () . find_map (| event | match event { Event :: Token { kind , .. } => Some (* kind) , _ => None , }) } }
};
}
