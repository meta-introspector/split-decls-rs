macro_rules! deps {
    () => {
        Entry!();
        IntoValues!();
        Values!();
        VacantEntry!();
        ValuesMut!();
        Value!();
        Keys!();
        OccupiedEntry!();
        MapImpl!();
        Iter!();
        Map!();
        IterMut!();
    };
}

macro_rules! impl_79 {
    () => {
        deps!();
        impl Map < String , Value > { # [doc = " Makes a new empty Map."] # [inline] pub fn new () -> Self { Map { map : MapImpl :: new () , } } # [doc = " Makes a new empty Map with the given initial capacity."] # [inline] pub fn with_capacity (capacity : usize) -> Self { Map { # [cfg (not (feature = "preserve_order"))] map : { let _ = capacity ; BTreeMap :: new () } , # [cfg (feature = "preserve_order")] map : IndexMap :: with_capacity (capacity) , } } # [doc = " Clears the map, removing all values."] # [inline] pub fn clear (& mut self) { self . map . clear () ; } # [doc = " Returns a reference to the value corresponding to the key."] # [doc = ""] # [doc = " The key may be any borrowed form of the map's key type, but the ordering"] # [doc = " on the borrowed form *must* match the ordering on the key type."] # [inline] pub fn get < Q > (& self , key : & Q) -> Option < & Value > where String : Borrow < Q > , Q : ? Sized + Ord + Eq + Hash , { self . map . get (key) } # [doc = " Returns true if the map contains a value for the specified key."] # [doc = ""] # [doc = " The key may be any borrowed form of the map's key type, but the ordering"] # [doc = " on the borrowed form *must* match the ordering on the key type."] # [inline] pub fn contains_key < Q > (& self , key : & Q) -> bool where String : Borrow < Q > , Q : ? Sized + Ord + Eq + Hash , { self . map . contains_key (key) } # [doc = " Returns a mutable reference to the value corresponding to the key."] # [doc = ""] # [doc = " The key may be any borrowed form of the map's key type, but the ordering"] # [doc = " on the borrowed form *must* match the ordering on the key type."] # [inline] pub fn get_mut < Q > (& mut self , key : & Q) -> Option < & mut Value > where String : Borrow < Q > , Q : ? Sized + Ord + Eq + Hash , { self . map . get_mut (key) } # [doc = " Returns the key-value pair matching the given key."] # [doc = ""] # [doc = " The key may be any borrowed form of the map's key type, but the ordering"] # [doc = " on the borrowed form *must* match the ordering on the key type."] # [inline] pub fn get_key_value < Q > (& self , key : & Q) -> Option < (& String , & Value) > where String : Borrow < Q > , Q : ? Sized + Ord + Eq + Hash , { self . map . get_key_value (key) } # [doc = " Inserts a key-value pair into the map."] # [doc = ""] # [doc = " If the map did not have this key present, `None` is returned."] # [doc = ""] # [doc = " If the map did have this key present, the value is updated, and the old"] # [doc = " value is returned."] # [inline] pub fn insert (& mut self , k : String , v : Value) -> Option < Value > { self . map . insert (k , v) } # [doc = " Insert a key-value pair in the map at the given index."] # [doc = ""] # [doc = " If the map did not have this key present, `None` is returned."] # [doc = ""] # [doc = " If the map did have this key present, the key is moved to the new"] # [doc = " position, the value is updated, and the old value is returned."] # [cfg (feature = "preserve_order")] # [cfg_attr (docsrs , doc (cfg (feature = "preserve_order")))] # [inline] pub fn shift_insert (& mut self , index : usize , k : String , v : Value) -> Option < Value > { self . map . shift_insert (index , k , v) } # [doc = " Removes a key from the map, returning the value at the key if the key"] # [doc = " was previously in the map."] # [doc = ""] # [doc = " The key may be any borrowed form of the map's key type, but the ordering"] # [doc = " on the borrowed form *must* match the ordering on the key type."] # [doc = ""] # [doc = " If serde_json's \"preserve_order\" is enabled, `.remove(key)` is"] # [doc = " equivalent to [`.swap_remove(key)`][Self::swap_remove], replacing this"] # [doc = " entry's position with the last element. If you need to preserve the"] # [doc = " relative order of the keys in the map, use"] # [doc = " [`.shift_remove(key)`][Self::shift_remove] instead."] # [inline] pub fn remove < Q > (& mut self , key : & Q) -> Option < Value > where String : Borrow < Q > , Q : ? Sized + Ord + Eq + Hash , { # [cfg (feature = "preserve_order")] return self . swap_remove (key) ; # [cfg (not (feature = "preserve_order"))] return self . map . remove (key) ; } # [doc = " Removes a key from the map, returning the stored key and value if the"] # [doc = " key was previously in the map."] # [doc = ""] # [doc = " The key may be any borrowed form of the map's key type, but the ordering"] # [doc = " on the borrowed form *must* match the ordering on the key type."] # [doc = ""] # [doc = " If serde_json's \"preserve_order\" is enabled, `.remove_entry(key)` is"] # [doc = " equivalent to [`.swap_remove_entry(key)`][Self::swap_remove_entry],"] # [doc = " replacing this entry's position with the last element. If you need to"] # [doc = " preserve the relative order of the keys in the map, use"] # [doc = " [`.shift_remove_entry(key)`][Self::shift_remove_entry] instead."] # [inline] pub fn remove_entry < Q > (& mut self , key : & Q) -> Option < (String , Value) > where String : Borrow < Q > , Q : ? Sized + Ord + Eq + Hash , { # [cfg (feature = "preserve_order")] return self . swap_remove_entry (key) ; # [cfg (not (feature = "preserve_order"))] return self . map . remove_entry (key) ; } # [doc = " Removes and returns the value corresponding to the key from the map."] # [doc = ""] # [doc = " Like [`Vec::swap_remove`], the entry is removed by swapping it with the"] # [doc = " last element of the map and popping it off. This perturbs the position"] # [doc = " of what used to be the last element!"] # [doc = ""] # [doc = " [`Vec::swap_remove`]: std::vec::Vec::swap_remove"] # [cfg (feature = "preserve_order")] # [cfg_attr (docsrs , doc (cfg (feature = "preserve_order")))] # [inline] pub fn swap_remove < Q > (& mut self , key : & Q) -> Option < Value > where String : Borrow < Q > , Q : ? Sized + Ord + Eq + Hash , { self . map . swap_remove (key) } # [doc = " Remove and return the key-value pair."] # [doc = ""] # [doc = " Like [`Vec::swap_remove`], the entry is removed by swapping it with the"] # [doc = " last element of the map and popping it off. This perturbs the position"] # [doc = " of what used to be the last element!"] # [doc = ""] # [doc = " [`Vec::swap_remove`]: std::vec::Vec::swap_remove"] # [cfg (feature = "preserve_order")] # [cfg_attr (docsrs , doc (cfg (feature = "preserve_order")))] # [inline] pub fn swap_remove_entry < Q > (& mut self , key : & Q) -> Option < (String , Value) > where String : Borrow < Q > , Q : ? Sized + Ord + Eq + Hash , { self . map . swap_remove_entry (key) } # [doc = " Removes and returns the value corresponding to the key from the map."] # [doc = ""] # [doc = " Like [`Vec::remove`], the entry is removed by shifting all of the"] # [doc = " elements that follow it, preserving their relative order. This perturbs"] # [doc = " the index of all of those elements!"] # [doc = ""] # [doc = " [`Vec::remove`]: std::vec::Vec::remove"] # [cfg (feature = "preserve_order")] # [cfg_attr (docsrs , doc (cfg (feature = "preserve_order")))] # [inline] pub fn shift_remove < Q > (& mut self , key : & Q) -> Option < Value > where String : Borrow < Q > , Q : ? Sized + Ord + Eq + Hash , { self . map . shift_remove (key) } # [doc = " Remove and return the key-value pair."] # [doc = ""] # [doc = " Like [`Vec::remove`], the entry is removed by shifting all of the"] # [doc = " elements that follow it, preserving their relative order. This perturbs"] # [doc = " the index of all of those elements!"] # [doc = ""] # [doc = " [`Vec::remove`]: std::vec::Vec::remove"] # [cfg (feature = "preserve_order")] # [cfg_attr (docsrs , doc (cfg (feature = "preserve_order")))] # [inline] pub fn shift_remove_entry < Q > (& mut self , key : & Q) -> Option < (String , Value) > where String : Borrow < Q > , Q : ? Sized + Ord + Eq + Hash , { self . map . shift_remove_entry (key) } # [doc = " Moves all elements from other into self, leaving other empty."] # [inline] pub fn append (& mut self , other : & mut Self) { # [cfg (feature = "preserve_order")] self . map . extend (mem :: replace (& mut other . map , MapImpl :: default ())) ; # [cfg (not (feature = "preserve_order"))] self . map . append (& mut other . map) ; } # [doc = " Gets the given key's corresponding entry in the map for in-place"] # [doc = " manipulation."] pub fn entry < S > (& mut self , key : S) -> Entry where S : Into < String > , { # [cfg (not (feature = "preserve_order"))] use alloc :: collections :: btree_map :: Entry as EntryImpl ; # [cfg (feature = "preserve_order")] use indexmap :: map :: Entry as EntryImpl ; match self . map . entry (key . into ()) { EntryImpl :: Vacant (vacant) => Entry :: Vacant (VacantEntry { vacant }) , EntryImpl :: Occupied (occupied) => Entry :: Occupied (OccupiedEntry { occupied }) , } } # [doc = " Returns the number of elements in the map."] # [inline] pub fn len (& self) -> usize { self . map . len () } # [doc = " Returns true if the map contains no elements."] # [inline] pub fn is_empty (& self) -> bool { self . map . is_empty () } # [doc = " Gets an iterator over the entries of the map."] # [inline] pub fn iter (& self) -> Iter { Iter { iter : self . map . iter () , } } # [doc = " Gets a mutable iterator over the entries of the map."] # [inline] pub fn iter_mut (& mut self) -> IterMut { IterMut { iter : self . map . iter_mut () , } } # [doc = " Gets an iterator over the keys of the map."] # [inline] pub fn keys (& self) -> Keys { Keys { iter : self . map . keys () , } } # [doc = " Gets an iterator over the values of the map."] # [inline] pub fn values (& self) -> Values { Values { iter : self . map . values () , } } # [doc = " Gets an iterator over mutable values of the map."] # [inline] pub fn values_mut (& mut self) -> ValuesMut { ValuesMut { iter : self . map . values_mut () , } } # [doc = " Gets an iterator over the values of the map."] # [inline] pub fn into_values (self) -> IntoValues { IntoValues { iter : self . map . into_values () , } } # [doc = " Retains only the elements specified by the predicate."] # [doc = ""] # [doc = " In other words, remove all pairs `(k, v)` such that `f(&k, &mut v)`"] # [doc = " returns `false`."] # [inline] pub fn retain < F > (& mut self , f : F) where F : FnMut (& String , & mut Value) -> bool , { self . map . retain (f) ; } # [doc = " Sorts this map's entries in-place using `str`'s usual ordering."] # [doc = ""] # [doc = " If serde_json's \"preserve_order\" feature is not enabled, this method"] # [doc = " does no work because all JSON maps are always kept in a sorted state."] # [doc = ""] # [doc = " If serde_json's \"preserve_order\" feature is enabled, this method"] # [doc = " destroys the original source order or insertion order of this map in"] # [doc = " favor of an alphanumerical order that matches how a BTreeMap with the"] # [doc = " same contents would be ordered. This takes **O(n log n + c)** time where"] # [doc = " _n_ is the length of the map and _c_ is the capacity."] # [doc = ""] # [doc = " Other maps nested within the values of this map are not sorted. If you"] # [doc = " need the entire data structure to be sorted at all levels, you must also"] # [doc = " call"] # [doc = " <code>map.[values_mut]\\().for_each([Value::sort_all_objects])</code>."] # [doc = ""] # [doc = " [values_mut]: Map::values_mut"] # [inline] pub fn sort_keys (& mut self) { # [cfg (feature = "preserve_order")] self . map . sort_unstable_keys () ; } }
    };
}

impl_79!();