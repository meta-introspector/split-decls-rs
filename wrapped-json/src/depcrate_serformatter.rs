// Generated macro for Formatter (trait)
macro_rules! Depcrate_serFormatter {
() => {
// Module: crate::ser
// Provides: {"Formatter"}
// Dependencies: {}
# [doc = " This trait abstracts away serializing the JSON control characters, which allows the user to"] # [doc = " optionally pretty print the JSON output."] pub trait Formatter { # [doc = " Writes a `null` value to the specified writer."] # [inline] fn write_null < W > (& mut self , writer : & mut W) -> io :: Result < () > where W : ? Sized + io :: Write , { writer . write_all (b"null") } # [doc = " Writes a `true` or `false` value to the specified writer."] # [inline] fn write_bool < W > (& mut self , writer : & mut W , value : bool) -> io :: Result < () > where W : ? Sized + io :: Write , { let s = if value { b"true" as & [u8] } else { b"false" as & [u8] } ; writer . write_all (s) } # [doc = " Writes an integer value like `-123` to the specified writer."] # [inline] fn write_i8 < W > (& mut self , writer : & mut W , value : i8) -> io :: Result < () > where W : ? Sized + io :: Write , { let mut buffer = itoa :: Buffer :: new () ; let s = buffer . format (value) ; writer . write_all (s . as_bytes ()) } # [doc = " Writes an integer value like `-123` to the specified writer."] # [inline] fn write_i16 < W > (& mut self , writer : & mut W , value : i16) -> io :: Result < () > where W : ? Sized + io :: Write , { let mut buffer = itoa :: Buffer :: new () ; let s = buffer . format (value) ; writer . write_all (s . as_bytes ()) } # [doc = " Writes an integer value like `-123` to the specified writer."] # [inline] fn write_i32 < W > (& mut self , writer : & mut W , value : i32) -> io :: Result < () > where W : ? Sized + io :: Write , { let mut buffer = itoa :: Buffer :: new () ; let s = buffer . format (value) ; writer . write_all (s . as_bytes ()) } # [doc = " Writes an integer value like `-123` to the specified writer."] # [inline] fn write_i64 < W > (& mut self , writer : & mut W , value : i64) -> io :: Result < () > where W : ? Sized + io :: Write , { let mut buffer = itoa :: Buffer :: new () ; let s = buffer . format (value) ; writer . write_all (s . as_bytes ()) } # [doc = " Writes an integer value like `-123` to the specified writer."] # [inline] fn write_i128 < W > (& mut self , writer : & mut W , value : i128) -> io :: Result < () > where W : ? Sized + io :: Write , { let mut buffer = itoa :: Buffer :: new () ; let s = buffer . format (value) ; writer . write_all (s . as_bytes ()) } # [doc = " Writes an integer value like `123` to the specified writer."] # [inline] fn write_u8 < W > (& mut self , writer : & mut W , value : u8) -> io :: Result < () > where W : ? Sized + io :: Write , { let mut buffer = itoa :: Buffer :: new () ; let s = buffer . format (value) ; writer . write_all (s . as_bytes ()) } # [doc = " Writes an integer value like `123` to the specified writer."] # [inline] fn write_u16 < W > (& mut self , writer : & mut W , value : u16) -> io :: Result < () > where W : ? Sized + io :: Write , { let mut buffer = itoa :: Buffer :: new () ; let s = buffer . format (value) ; writer . write_all (s . as_bytes ()) } # [doc = " Writes an integer value like `123` to the specified writer."] # [inline] fn write_u32 < W > (& mut self , writer : & mut W , value : u32) -> io :: Result < () > where W : ? Sized + io :: Write , { let mut buffer = itoa :: Buffer :: new () ; let s = buffer . format (value) ; writer . write_all (s . as_bytes ()) } # [doc = " Writes an integer value like `123` to the specified writer."] # [inline] fn write_u64 < W > (& mut self , writer : & mut W , value : u64) -> io :: Result < () > where W : ? Sized + io :: Write , { let mut buffer = itoa :: Buffer :: new () ; let s = buffer . format (value) ; writer . write_all (s . as_bytes ()) } # [doc = " Writes an integer value like `123` to the specified writer."] # [inline] fn write_u128 < W > (& mut self , writer : & mut W , value : u128) -> io :: Result < () > where W : ? Sized + io :: Write , { let mut buffer = itoa :: Buffer :: new () ; let s = buffer . format (value) ; writer . write_all (s . as_bytes ()) } # [doc = " Writes a floating point value like `-31.26e+12` to the specified writer."] # [doc = ""] # [doc = " # Special cases"] # [doc = ""] # [doc = " This function **does not** check for NaN or infinity. If the input"] # [doc = " number is not a finite float, the printed representation will be some"] # [doc = " correctly formatted but unspecified numerical value."] # [doc = ""] # [doc = " Please check [`is_finite`] yourself before calling this function, or"] # [doc = " check [`is_nan`] and [`is_infinite`] and handle those cases yourself"] # [doc = " with a different `Formatter` method."] # [doc = ""] # [doc = " [`is_finite`]: f32::is_finite"] # [doc = " [`is_nan`]: f32::is_nan"] # [doc = " [`is_infinite`]: f32::is_infinite"] # [inline] fn write_f32 < W > (& mut self , writer : & mut W , value : f32) -> io :: Result < () > where W : ? Sized + io :: Write , { let mut buffer = ryu :: Buffer :: new () ; let s = buffer . format_finite (value) ; writer . write_all (s . as_bytes ()) } # [doc = " Writes a floating point value like `-31.26e+12` to the specified writer."] # [doc = ""] # [doc = " # Special cases"] # [doc = ""] # [doc = " This function **does not** check for NaN or infinity. If the input"] # [doc = " number is not a finite float, the printed representation will be some"] # [doc = " correctly formatted but unspecified numerical value."] # [doc = ""] # [doc = " Please check [`is_finite`] yourself before calling this function, or"] # [doc = " check [`is_nan`] and [`is_infinite`] and handle those cases yourself"] # [doc = " with a different `Formatter` method."] # [doc = ""] # [doc = " [`is_finite`]: f64::is_finite"] # [doc = " [`is_nan`]: f64::is_nan"] # [doc = " [`is_infinite`]: f64::is_infinite"] # [inline] fn write_f64 < W > (& mut self , writer : & mut W , value : f64) -> io :: Result < () > where W : ? Sized + io :: Write , { let mut buffer = ryu :: Buffer :: new () ; let s = buffer . format_finite (value) ; writer . write_all (s . as_bytes ()) } # [doc = " Writes a number that has already been rendered to a string."] # [inline] fn write_number_str < W > (& mut self , writer : & mut W , value : & str) -> io :: Result < () > where W : ? Sized + io :: Write , { writer . write_all (value . as_bytes ()) } # [doc = " Called before each series of `write_string_fragment` and"] # [doc = " `write_char_escape`.  Writes a `\"` to the specified writer."] # [inline] fn begin_string < W > (& mut self , writer : & mut W) -> io :: Result < () > where W : ? Sized + io :: Write , { writer . write_all (b"\"") } # [doc = " Called after each series of `write_string_fragment` and"] # [doc = " `write_char_escape`.  Writes a `\"` to the specified writer."] # [inline] fn end_string < W > (& mut self , writer : & mut W) -> io :: Result < () > where W : ? Sized + io :: Write , { writer . write_all (b"\"") } # [doc = " Writes a string fragment that doesn't need any escaping to the"] # [doc = " specified writer."] # [inline] fn write_string_fragment < W > (& mut self , writer : & mut W , fragment : & str) -> io :: Result < () > where W : ? Sized + io :: Write , { writer . write_all (fragment . as_bytes ()) } # [doc = " Writes a character escape code to the specified writer."] # [inline] fn write_char_escape < W > (& mut self , writer : & mut W , char_escape : CharEscape) -> io :: Result < () > where W : ? Sized + io :: Write , { use self :: CharEscape :: * ; let escape_char = match char_escape { Quote => b'"' , ReverseSolidus => b'\\' , Solidus => b'/' , Backspace => b'b' , FormFeed => b'f' , LineFeed => b'n' , CarriageReturn => b'r' , Tab => b't' , AsciiControl (_) => b'u' , } ; match char_escape { AsciiControl (byte) => { static HEX_DIGITS : [u8 ; 16] = * b"0123456789abcdef" ; let bytes = & [b'\\' , escape_char , b'0' , b'0' , HEX_DIGITS [(byte >> 4) as usize] , HEX_DIGITS [(byte & 0xF) as usize] ,] ; writer . write_all (bytes) } _ => writer . write_all (& [b'\\' , escape_char]) , } } # [doc = " Writes the representation of a byte array. Formatters can choose whether"] # [doc = " to represent bytes as a JSON array of integers (the default), or some"] # [doc = " JSON string encoding like hex or base64."] fn write_byte_array < W > (& mut self , writer : & mut W , value : & [u8]) -> io :: Result < () > where W : ? Sized + io :: Write , { tri ! (self . begin_array (writer)) ; let mut first = true ; for byte in value { tri ! (self . begin_array_value (writer , first)) ; tri ! (self . write_u8 (writer , * byte)) ; tri ! (self . end_array_value (writer)) ; first = false ; } self . end_array (writer) } # [doc = " Called before every array.  Writes a `[` to the specified"] # [doc = " writer."] # [inline] fn begin_array < W > (& mut self , writer : & mut W) -> io :: Result < () > where W : ? Sized + io :: Write , { writer . write_all (b"[") } # [doc = " Called after every array.  Writes a `]` to the specified"] # [doc = " writer."] # [inline] fn end_array < W > (& mut self , writer : & mut W) -> io :: Result < () > where W : ? Sized + io :: Write , { writer . write_all (b"]") } # [doc = " Called before every array value.  Writes a `,` if needed to"] # [doc = " the specified writer."] # [inline] fn begin_array_value < W > (& mut self , writer : & mut W , first : bool) -> io :: Result < () > where W : ? Sized + io :: Write , { if first { Ok (()) } else { writer . write_all (b",") } } # [doc = " Called after every array value."] # [inline] fn end_array_value < W > (& mut self , _writer : & mut W) -> io :: Result < () > where W : ? Sized + io :: Write , { Ok (()) } # [doc = " Called before every object.  Writes a `{` to the specified"] # [doc = " writer."] # [inline] fn begin_object < W > (& mut self , writer : & mut W) -> io :: Result < () > where W : ? Sized + io :: Write , { writer . write_all (b"{") } # [doc = " Called after every object.  Writes a `}` to the specified"] # [doc = " writer."] # [inline] fn end_object < W > (& mut self , writer : & mut W) -> io :: Result < () > where W : ? Sized + io :: Write , { writer . write_all (b"}") } # [doc = " Called before every object key."] # [inline] fn begin_object_key < W > (& mut self , writer : & mut W , first : bool) -> io :: Result < () > where W : ? Sized + io :: Write , { if first { Ok (()) } else { writer . write_all (b",") } } # [doc = " Called after every object key.  A `:` should be written to the"] # [doc = " specified writer by either this method or"] # [doc = " `begin_object_value`."] # [inline] fn end_object_key < W > (& mut self , _writer : & mut W) -> io :: Result < () > where W : ? Sized + io :: Write , { Ok (()) } # [doc = " Called before every object value.  A `:` should be written to"] # [doc = " the specified writer by either this method or"] # [doc = " `end_object_key`."] # [inline] fn begin_object_value < W > (& mut self , writer : & mut W) -> io :: Result < () > where W : ? Sized + io :: Write , { writer . write_all (b":") } # [doc = " Called after every object value."] # [inline] fn end_object_value < W > (& mut self , _writer : & mut W) -> io :: Result < () > where W : ? Sized + io :: Write , { Ok (()) } # [doc = " Writes a raw JSON fragment that doesn't need any escaping to the"] # [doc = " specified writer."] # [inline] fn write_raw_fragment < W > (& mut self , writer : & mut W , fragment : & str) -> io :: Result < () > where W : ? Sized + io :: Write , { writer . write_all (fragment . as_bytes ()) } }
};
}
