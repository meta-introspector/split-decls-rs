// Generated macro for mark_initialized (function)
macro_rules! Depcratemark_initialized {
() => {
// Module: crate
// Provides: {"mark_initialized"}
// Dependencies: {}
# [doc = " Convert a `[MaybeUninit<T>; N]` to a `[T; N]`"] # [doc = ""] # [doc = " ```"] # [doc = " # use unarray::*;"] # [doc = " let mut buffer = uninit_buf::<i32, 1000>();"] # [doc = ""] # [doc = " for elem in &mut buffer {"] # [doc = "   elem.write(123);"] # [doc = " }"] # [doc = ""] # [doc = " let result = unsafe { mark_initialized(buffer) };"] # [doc = " assert_eq!(result, [123; 1000])"] # [doc = " ```"] # [doc = ""] # [doc = " This largely acts as a workaround to the fact that [`core::mem::transmute`] cannot be used with"] # [doc = " const generic arrays, as it can't prove they have the same size (even when intuitively they are"] # [doc = " the same, e.g. `[i32; N]` and `[u32; N]`)."] # [doc = ""] # [doc = " This is similar to the nightly-only [`core::mem::MaybeUninit::array_assume_init`]"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Internally, this uses [`core::mem::transmute_copy`] to convert a `[MaybeUninit<T>; N]` to `[T; N]`."] # [doc = " As such, you must make sure every element has been initialized before calling this function. If"] # [doc = " there are uninitialized elements in `src`, these will be converted to `T`s, which is UB. For"] # [doc = " example:"] # [doc = " ```no_run"] # [doc = " # use unarray::*;"] # [doc = " // ⚠\u{fe0f} This example produces UB ⚠\u{fe0f}"] # [doc = " let bools = uninit_buf::<bool, 10>();"] # [doc = " let uh_oh = unsafe { mark_initialized(bools) };  // UB: creating an invalid instance"] # [doc = " if uh_oh[0] {                                    // double UB: reading from unintiailized memory"] # [doc = "   // ..."] # [doc = " }"] # [doc = " ```"] # [doc = " Even if you never use a value, it's still UB. This is especially true for types with"] # [doc = " [`core::ops::Drop`] implementations:"] # [doc = " ```no_run"] # [doc = " # use unarray::*;"] # [doc = " // ⚠\u{fe0f} This example produces UB ⚠\u{fe0f}"] # [doc = " let strings = uninit_buf::<String, 10>();"] # [doc = " let uh_oh = unsafe { mark_initialized(strings) };  // UB: creating an invalid instance"] # [doc = ""] # [doc = " // uh_oh is dropped here, freeing memory at random addresses"] # [doc = " ```"] pub unsafe fn mark_initialized < T , const N : usize > (src : [MaybeUninit < T > ; N]) -> [T ; N] { core :: mem :: transmute_copy :: < [MaybeUninit < T > ; N] , [T ; N] > (& src) }
};
}
