// Generated macro for invoke_signed (function)
macro_rules! Depcrate_cpiinvoke_signed {
() => {
// Module: crate::cpi
// Provides: {"invoke_signed"}
// Dependencies: {}
# [doc = " Invoke a cross-program instruction with signatures from an array of"] # [doc = " `AccountView`s."] # [doc = ""] # [doc = " This function performs validation of the `account_views` array to ensure that:"] # [doc = "   1. It has at least as many accounts as the number of accounts expected by"] # [doc = "      the instruction."] # [doc = "   2. The accounts match the expected accounts in the instruction, i.e., their"] # [doc = "      `Address` matches the `address` in the `AccountView`."] # [doc = "   3. The borrow state of the accounts is compatible with the mutability of the"] # [doc = "      instruction accounts."] # [doc = ""] # [doc = " This validation is done to ensure that the borrow checker rules are followed,"] # [doc = " consuming CUs in the process. The [`invoke_signed_unchecked`] is an alternative"] # [doc = " to this function that have lower CU consumption since it does not perform"] # [doc = " any validation. This should only be used when the caller is sure that the borrow"] # [doc = " checker rules are followed."] # [doc = ""] # [doc = " Note that this function is inlined to avoid the overhead of a function call,"] # [doc = " but uses stack memory allocation. When a large number of accounts is needed,"] # [doc = " it is recommended to use the [`invoke_signed_with_slice`] function instead"] # [doc = " to reduce stack memory utilization."] # [doc = ""] # [doc = " # Important"] # [doc = ""] # [doc = " The accounts on the `account_views` array must be in the same order as the"] # [doc = " `accounts` field of the `instruction`. When the instruction has duplicated"] # [doc = " accounts, it is necessary to pass a duplicated reference to the same account"] # [doc = " to maintain the 1:1 relationship between accounts and instruction accounts."] # [inline (always)] pub fn invoke_signed < const ACCOUNTS : usize > (instruction : & InstructionView , account_views : & [& AccountView ; ACCOUNTS] , signers_seeds : & [Signer] ,) -> ProgramResult { const { assert ! (ACCOUNTS <= MAX_STATIC_CPI_ACCOUNTS , "ACCOUNTS is greater than allowed MAX_STATIC_CPI_ACCOUNTS") ; } const UNINIT : MaybeUninit < CpiAccount > = MaybeUninit :: < CpiAccount > :: uninit () ; let mut accounts = [UNINIT ; ACCOUNTS] ; unsafe { inner_invoke_signed_with_slice (instruction , account_views , & mut accounts , signers_seeds) } }
};
}
