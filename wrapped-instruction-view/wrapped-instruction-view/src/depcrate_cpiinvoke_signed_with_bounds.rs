// Generated macro for invoke_signed_with_bounds (function)
macro_rules! Depcrate_cpiinvoke_signed_with_bounds {
() => {
// Module: crate::cpi
// Provides: {"invoke_signed_with_bounds"}
// Dependencies: {}
# [doc = " Invoke a cross-program instruction with signatures from a slice of"] # [doc = " `AccountView`s."] # [doc = ""] # [doc = " This function performs validation of the `account_views` slice to ensure that:"] # [doc = "   1. It has at least as many accounts as the number of accounts expected by"] # [doc = "      the instruction."] # [doc = "   2. The accounts match the expected accounts in the instruction, i.e., their"] # [doc = "      `Address` matches the `address` in the `AccountView`."] # [doc = "   3. The borrow state of the accounts is compatible with the mutability of the"] # [doc = "      instruction accounts."] # [doc = ""] # [doc = " This validation is done to ensure that the borrow checker rules are followed,"] # [doc = " consuming CUs in the process. The [`invoke_signed_unchecked`] is an alternative"] # [doc = " to this function that has lower CU consumption since it does not perform"] # [doc = " any validation. This should only be used when the caller is sure that the borrow"] # [doc = " checker rules are followed."] # [doc = ""] # [doc = " The `MAX_ACCOUNTS` constant defines the maximum number of accounts expected"] # [doc = " to be passed to the cross-program invocation. This provides an upper bound to"] # [doc = " the number of accounts that need to be statically allocated for cases where the"] # [doc = " number of instruction accounts is not known at compile time. The final number of"] # [doc = " accounts passed to the cross-program invocation will be the number of accounts"] # [doc = " required by the `instruction`, even if `MAX_ACCOUNTS` is greater than that. When"] # [doc = " `MAX_ACCOUNTS` is lower than the number of accounts expected by the instruction,"] # [doc = " this function will return a [`ProgramError::InvalidArgument`] error."] # [doc = ""] # [doc = " Note that this function is inlined to avoid the overhead of a function call,"] # [doc = " but uses stack memory allocation. When a large number of accounts is needed,"] # [doc = " it is recommended to use the [`invoke_signed_with_slice`] function instead to reduce"] # [doc = " stack memory utilization."] # [doc = ""] # [doc = " # Important"] # [doc = ""] # [doc = " The accounts on the `account_views` slice must be in the same order as the"] # [doc = " `accounts` field of the `instruction`. When the instruction has duplicated"] # [doc = " accounts, it is necessary to pass a duplicated reference to the same account"] # [doc = " to maintain the 1:1 relationship between accounts and instruction accounts."] # [inline (always)] pub fn invoke_signed_with_bounds < const MAX_ACCOUNTS : usize > (instruction : & InstructionView , account_views : & [& AccountView] , signers_seeds : & [Signer] ,) -> ProgramResult { const { assert ! (MAX_ACCOUNTS <= MAX_STATIC_CPI_ACCOUNTS , "MAX_ACCOUNTS is greater than allowed MAX_STATIC_CPI_ACCOUNTS") ; } if MAX_ACCOUNTS < instruction . accounts . len () { return Err (ProgramError :: InvalidArgument) ; } const UNINIT : MaybeUninit < CpiAccount > = MaybeUninit :: < CpiAccount > :: uninit () ; let mut accounts = [UNINIT ; MAX_ACCOUNTS] ; unsafe { inner_invoke_signed_with_slice (instruction , account_views , & mut accounts , signers_seeds) } }
};
}
