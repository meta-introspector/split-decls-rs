macro_rules! typesize_derive {
    () => {
        # [doc = " Implements `TypeSize` automatically for a `struct` or `enum`."] # [doc = ""] # [doc = " Use `#[typesize(skip)]` on a field to assume it does not manage any external memory."] # [doc = ""] # [doc = " Use `#[typesize(with = path::to::fn)]` on a field to specify a custom `extra_size` for that field."] # [doc = " The function accepts a reference to the type of the field."] # [doc = ""] # [doc = " This will avoid requiring `TypeSize` to be implemented for this field, however may lead to undercounted results if the assumption does not hold."] # [doc = ""] # [doc = " # Struct Mode"] # [doc = ""] # [doc = " `TypeSize::extra_size` will be calculated by adding up the `extra_size` of all fields."] # [doc = ""] # [doc = " # Enum Mode"] # [doc = ""] # [doc = " `TypeSize::extra_size` will be calculated by adding up the `extra_size` of all of the fields of the active enum variant."] # [doc = ""] # [doc = " # Union Mode"] # [doc = ""] # [doc = " Unions are unsupported as there is no safe way to calculate the `extra_size`, implement `typesize::TypeSize` manually."] # [proc_macro_derive (TypeSize , attributes (typesize))] pub fn typesize_derive (tokens : proc_macro :: TokenStream) -> proc_macro :: TokenStream { let DeriveInput { attrs , vis : _ , ident , generics , data , } = parse_macro_input ! (tokens as DeriveInput) ; let is_packed = check_repr_packed (& attrs) ; let (impl_generics , ty_generics , where_clause) = generics . split_for_impl () ; let bodies = match data { syn :: Data :: Struct (data) => gen_struct (& data . fields , is_packed) , syn :: Data :: Enum (data) => gen_enum (data . variants . into_iter () , is_packed) , syn :: Data :: Union (data) => Err (syn :: Error :: new (data . union_token . span , "Unions are unsupported for typesize derive." ,)) , } ; let bodies = match bodies { Ok (bodies) => bodies , Err (err) => { return err . into_compile_error () . into () ; } } ; let extra_size = bodies . extra_size ; # [cfg_attr (not (feature = "details") , allow (unused_mut))] let mut impl_body = quote ! (fn extra_size (& self) -> usize { # extra_size }) ; # [cfg (feature = "details")] if let Some (details) = bodies . details { impl_body = quote ! (# impl_body fn get_size_details (& self) -> Vec <:: typesize :: Field > { # details }) ; } let output = quote ! { # [automatically_derived] impl # impl_generics :: typesize :: TypeSize for # ident # ty_generics # where_clause { # impl_body } } ; output . into () }
    };
}

typesize_derive!()