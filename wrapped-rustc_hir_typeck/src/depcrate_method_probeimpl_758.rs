// Generated macro for impl_758 (impl)
macro_rules! Depcrate_method_probeimpl_758 {
() => {
// Module: crate::method::probe
// Provides: {"impl_758"}
// Dependencies: {}
impl < 'a , 'tcx > ProbeContext < 'a , 'tcx > { fn select_trait_candidate (& self , trait_ref : ty :: TraitRef < 'tcx > ,) -> traits :: SelectionResult < 'tcx , traits :: Selection < 'tcx > > { let obligation = traits :: Obligation :: new (self . tcx , self . misc (self . span) , self . param_env , trait_ref) ; traits :: SelectionContext :: new (self) . select (& obligation) } # [doc = " Used for ambiguous method call error reporting. Uses probing that throws away the result internally,"] # [doc = " so do not use to make a decision that may lead to a successful compilation."] fn candidate_source (& self , candidate : & Candidate < 'tcx > , self_ty : Ty < 'tcx >) -> CandidateSource { match candidate . kind { InherentImplCandidate { .. } => { CandidateSource :: Impl (candidate . item . container_id (self . tcx)) } ObjectCandidate (_) | WhereClauseCandidate (_) => { CandidateSource :: Trait (candidate . item . container_id (self . tcx)) } TraitCandidate (trait_ref) => self . probe (| _ | { let trait_ref = self . instantiate_binder_with_fresh_vars (self . span , BoundRegionConversionTime :: FnCall , trait_ref ,) ; let (xform_self_ty , _) = self . xform_self_ty (candidate . item , trait_ref . self_ty () , trait_ref . args) ; let _ = self . at (& ObligationCause :: dummy () , self . param_env) . sup (DefineOpaqueTypes :: Yes , xform_self_ty , self_ty ,) ; match self . select_trait_candidate (trait_ref) { Ok (Some (traits :: ImplSource :: UserDefined (ref impl_data))) => { CandidateSource :: Impl (impl_data . impl_def_id) } _ => CandidateSource :: Trait (candidate . item . container_id (self . tcx)) , } }) , } } fn candidate_source_from_pick (& self , pick : & Pick < 'tcx >) -> CandidateSource { match pick . kind { InherentImplPick => CandidateSource :: Impl (pick . item . container_id (self . tcx)) , ObjectPick | WhereClausePick (_) | TraitPick => { CandidateSource :: Trait (pick . item . container_id (self . tcx)) } } } # [instrument (level = "trace" , skip (self , possibly_unsatisfied_predicates) , ret)] fn consider_probe (& self , self_ty : Ty < 'tcx > , probe : & Candidate < 'tcx > , possibly_unsatisfied_predicates : & mut Vec < (ty :: Predicate < 'tcx > , Option < ty :: Predicate < 'tcx > > , Option < ObligationCause < 'tcx > > ,) > ,) -> ProbeResult { debug ! ("consider_probe: self_ty={:?} probe={:?}" , self_ty , probe) ; self . probe (| snapshot | { let outer_universe = self . universe () ; let mut result = ProbeResult :: Match ; let cause = & self . misc (self . span) ; let ocx = ObligationCtxt :: new_with_diagnostics (self) ; let mut trait_predicate = None ; let (mut xform_self_ty , mut xform_ret_ty) ; match probe . kind { InherentImplCandidate { impl_def_id , .. } => { let impl_args = self . fresh_args_for_item (self . span , impl_def_id) ; let impl_ty = self . tcx . type_of (impl_def_id) . instantiate (self . tcx , impl_args) ; (xform_self_ty , xform_ret_ty) = self . xform_self_ty (probe . item , impl_ty , impl_args) ; xform_self_ty = ocx . normalize (cause , self . param_env , xform_self_ty) ; match ocx . relate (cause , self . param_env , self . variance () , self_ty , xform_self_ty) { Ok (()) => { } Err (err) => { debug ! ("--> cannot relate self-types {:?}" , err) ; return ProbeResult :: NoMatch ; } } xform_ret_ty = ocx . normalize (cause , self . param_env , xform_ret_ty) ; let impl_def_id = probe . item . container_id (self . tcx) ; let impl_bounds = self . tcx . predicates_of (impl_def_id) . instantiate (self . tcx , impl_args) ; let impl_bounds = ocx . normalize (cause , self . param_env , impl_bounds) ; ocx . register_obligations (traits :: predicates_for_generics (| idx , span | { let code = ObligationCauseCode :: WhereClauseInExpr (impl_def_id , span , self . scope_expr_id , idx ,) ; self . cause (self . span , code) } , self . param_env , impl_bounds ,)) ; } TraitCandidate (poly_trait_ref) => { if let Some (method_name) = self . method_name { if self_ty . is_array () && ! method_name . span . at_least_rust_2021 () { let trait_def = self . tcx . trait_def (poly_trait_ref . def_id ()) ; if trait_def . skip_array_during_method_dispatch { return ProbeResult :: NoMatch ; } } if self_ty . boxed_ty () . is_some_and (Ty :: is_slice) && ! method_name . span . at_least_rust_2024 () { let trait_def = self . tcx . trait_def (poly_trait_ref . def_id ()) ; if trait_def . skip_boxed_slice_during_method_dispatch { return ProbeResult :: NoMatch ; } } } let trait_ref = self . instantiate_binder_with_fresh_vars (self . span , BoundRegionConversionTime :: FnCall , poly_trait_ref ,) ; let trait_ref = ocx . normalize (cause , self . param_env , trait_ref) ; (xform_self_ty , xform_ret_ty) = self . xform_self_ty (probe . item , trait_ref . self_ty () , trait_ref . args) ; xform_self_ty = ocx . normalize (cause , self . param_env , xform_self_ty) ; match self_ty . kind () { ty :: Alias (ty :: Opaque , alias_ty) if ! self . next_trait_solver () && self . infcx . can_define_opaque_ty (alias_ty . def_id) && ! xform_self_ty . is_ty_var () => { return ProbeResult :: NoMatch ; } _ => match ocx . relate (cause , self . param_env , self . variance () , self_ty , xform_self_ty ,) { Ok (()) => { } Err (err) => { debug ! ("--> cannot relate self-types {:?}" , err) ; return ProbeResult :: NoMatch ; } } , } let obligation = traits :: Obligation :: new (self . tcx , cause . clone () , self . param_env , ty :: Binder :: dummy (trait_ref) ,) ; if self . infcx . next_trait_solver () || self . infcx . predicate_may_hold (& obligation) { ocx . register_obligation (obligation) ; } else { result = ProbeResult :: NoMatch ; if let Ok (Some (candidate)) = self . select_trait_candidate (trait_ref) { for nested_obligation in candidate . nested_obligations () { if ! self . infcx . predicate_may_hold (& nested_obligation) { possibly_unsatisfied_predicates . push ((self . resolve_vars_if_possible (nested_obligation . predicate) , Some (self . resolve_vars_if_possible (obligation . predicate)) , Some (nested_obligation . cause) ,)) ; } } } } trait_predicate = Some (trait_ref . upcast (self . tcx)) ; } ObjectCandidate (poly_trait_ref) | WhereClauseCandidate (poly_trait_ref) => { let trait_ref = self . instantiate_binder_with_fresh_vars (self . span , BoundRegionConversionTime :: FnCall , poly_trait_ref ,) ; (xform_self_ty , xform_ret_ty) = self . xform_self_ty (probe . item , trait_ref . self_ty () , trait_ref . args) ; if matches ! (probe . kind , WhereClauseCandidate (_)) { match ocx . structurally_normalize_ty (cause , self . param_env , trait_ref . self_ty () ,) { Ok (ty) => { if ! matches ! (ty . kind () , ty :: Param (_)) { debug ! ("--> not a param ty: {xform_self_ty:?}") ; return ProbeResult :: NoMatch ; } } Err (errors) => { debug ! ("--> cannot relate self-types {:?}" , errors) ; return ProbeResult :: NoMatch ; } } } xform_self_ty = ocx . normalize (cause , self . param_env , xform_self_ty) ; match ocx . relate (cause , self . param_env , self . variance () , self_ty , xform_self_ty) { Ok (()) => { } Err (err) => { debug ! ("--> cannot relate self-types {:?}" , err) ; return ProbeResult :: NoMatch ; } } } } if let Some (xform_ret_ty) = xform_ret_ty && self . infcx . next_trait_solver () { ocx . register_obligation (traits :: Obligation :: new (self . tcx , cause . clone () , self . param_env , ty :: ClauseKind :: WellFormed (xform_ret_ty . into ()) ,)) ; } for error in ocx . select_where_possible () { result = ProbeResult :: NoMatch ; let nested_predicate = self . resolve_vars_if_possible (error . obligation . predicate) ; if let Some (trait_predicate) = trait_predicate && nested_predicate == self . resolve_vars_if_possible (trait_predicate) { } else { possibly_unsatisfied_predicates . push ((nested_predicate , Some (self . resolve_vars_if_possible (error . root_obligation . predicate)) . filter (| root_predicate | * root_predicate != nested_predicate) , Some (error . obligation . cause) ,)) ; } } if let ProbeResult :: Match = result && let Some (return_ty) = self . return_type && let Some (mut xform_ret_ty) = xform_ret_ty { if ! matches ! (probe . kind , InherentImplCandidate { .. }) { xform_ret_ty = ocx . normalize (& cause , self . param_env , xform_ret_ty) ; } debug ! ("comparing return_ty {:?} with xform ret ty {:?}" , return_ty , xform_ret_ty) ; match ocx . relate (cause , self . param_env , self . variance () , xform_ret_ty , return_ty) { Ok (()) => { } Err (_) => { result = ProbeResult :: BadReturnType ; } } for error in ocx . select_where_possible () { result = ProbeResult :: NoMatch ; possibly_unsatisfied_predicates . push ((error . obligation . predicate , Some (error . root_obligation . predicate) . filter (| predicate | * predicate != error . obligation . predicate) , Some (error . root_obligation . cause) ,)) ; } } if let Err (_) = self . leak_check (outer_universe , Some (snapshot)) { result = ProbeResult :: NoMatch ; } result }) } # [doc = " Sometimes we get in a situation where we have multiple probes that are all impls of the"] # [doc = " same trait, but we don't know which impl to use. In this case, since in all cases the"] # [doc = " external interface of the method can be determined from the trait, it's ok not to decide."] # [doc = " We can basically just collapse all of the probes for various impls into one where-clause"] # [doc = " probe. This will result in a pending obligation so when more type-info is available we can"] # [doc = " make the final decision."] # [doc = ""] # [doc = " Example (`tests/ui/methods/method-two-trait-defer-resolution-1.rs`):"] # [doc = ""] # [doc = " ```ignore (illustrative)"] # [doc = " trait Foo { ... }"] # [doc = " impl Foo for Vec<i32> { ... }"] # [doc = " impl Foo for Vec<usize> { ... }"] # [doc = " ```"] # [doc = ""] # [doc = " Now imagine the receiver is `Vec<_>`. It doesn't really matter at this time which impl we"] # [doc = " use, so it's ok to just commit to \"using the method from the trait Foo\"."] fn collapse_candidates_to_trait_pick (& self , self_ty : Ty < 'tcx > , probes : & [(& Candidate < 'tcx > , ProbeResult)] ,) -> Option < Pick < 'tcx > > { let container = probes [0] . 0 . item . trait_container (self . tcx) ? ; for (p , _) in & probes [1 ..] { let p_container = p . item . trait_container (self . tcx) ? ; if p_container != container { return None ; } } Some (Pick { item : probes [0] . 0 . item , kind : TraitPick , import_ids : probes [0] . 0 . import_ids . clone () , autoderefs : 0 , autoref_or_ptr_adjustment : None , self_ty , unstable_candidates : vec ! [] , receiver_steps : None , shadowed_candidates : vec ! [] , }) } # [doc = " Much like `collapse_candidates_to_trait_pick`, this method allows us to collapse"] # [doc = " multiple conflicting picks if there is one pick whose trait container is a subtrait"] # [doc = " of the trait containers of all of the other picks."] # [doc = ""] # [doc = " This implements RFC #3624."] fn collapse_candidates_to_subtrait_pick (& self , self_ty : Ty < 'tcx > , probes : & [(& Candidate < 'tcx > , ProbeResult)] ,) -> Option < Pick < 'tcx > > { let mut child_candidate = probes [0] . 0 ; let mut child_trait = child_candidate . item . trait_container (self . tcx) ? ; let mut supertraits : SsoHashSet < _ > = supertrait_def_ids (self . tcx , child_trait) . collect () ; let mut remaining_candidates : Vec < _ > = probes [1 ..] . iter () . map (| & (p , _) | p) . collect () ; while ! remaining_candidates . is_empty () { let mut made_progress = false ; let mut next_round = vec ! [] ; for remaining_candidate in remaining_candidates { let remaining_trait = remaining_candidate . item . trait_container (self . tcx) ? ; if supertraits . contains (& remaining_trait) { made_progress = true ; continue ; } let remaining_trait_supertraits : SsoHashSet < _ > = supertrait_def_ids (self . tcx , remaining_trait) . collect () ; if remaining_trait_supertraits . contains (& child_trait) { child_candidate = remaining_candidate ; child_trait = remaining_trait ; supertraits = remaining_trait_supertraits ; made_progress = true ; continue ; } next_round . push (remaining_candidate) ; } if made_progress { remaining_candidates = next_round ; } else { return None ; } } Some (Pick { item : child_candidate . item , kind : TraitPick , import_ids : child_candidate . import_ids . clone () , autoderefs : 0 , autoref_or_ptr_adjustment : None , self_ty , unstable_candidates : vec ! [] , shadowed_candidates : probes . iter () . map (| (c , _) | c . item) . filter (| item | item . def_id != child_candidate . item . def_id) . collect () , receiver_steps : None , }) } # [doc = " Similarly to `probe_for_return_type`, this method attempts to find the best matching"] # [doc = " candidate method where the method name may have been misspelled. Similarly to other"] # [doc = " edit distance based suggestions, we provide at most one such suggestion."] # [instrument (level = "debug" , skip (self))] pub (crate) fn probe_for_similar_candidate (& mut self ,) -> Result < Option < ty :: AssocItem > , MethodError < 'tcx > > { debug ! ("probing for method names similar to {:?}" , self . method_name) ; self . probe (| _ | { let mut pcx = ProbeContext :: new (self . fcx , self . span , self . mode , self . method_name , self . return_type , self . orig_steps_var_values , self . steps , self . scope_expr_id , IsSuggestion (true) ,) ; pcx . allow_similar_names = true ; pcx . assemble_inherent_candidates () ; pcx . assemble_extension_candidates_for_all_traits () ; let method_names = pcx . candidate_method_names (| _ | true) ; pcx . allow_similar_names = false ; let applicable_close_candidates : Vec < ty :: AssocItem > = method_names . iter () . filter_map (| & method_name | { pcx . reset () ; pcx . method_name = Some (method_name) ; pcx . assemble_inherent_candidates () ; pcx . assemble_extension_candidates_for_all_traits () ; pcx . pick_core (& mut Vec :: new ()) . and_then (| pick | pick . ok ()) . map (| pick | pick . item) }) . collect () ; if applicable_close_candidates . is_empty () { Ok (None) } else { let best_name = { let names = applicable_close_candidates . iter () . map (| cand | cand . name ()) . collect :: < Vec < Symbol > > () ; find_best_match_for_name_with_substrings (& names , self . method_name . unwrap () . name , None ,) } . or_else (| | { applicable_close_candidates . iter () . find (| cand | self . matches_by_doc_alias (cand . def_id)) . map (| cand | cand . name ()) }) ; Ok (best_name . and_then (| best_name | { applicable_close_candidates . into_iter () . find (| method | method . name () == best_name) })) } }) } fn has_applicable_self (& self , item : & ty :: AssocItem) -> bool { match self . mode { Mode :: MethodCall => item . is_method () , Mode :: Path => match item . kind { ty :: AssocKind :: Type { .. } => false , ty :: AssocKind :: Fn { .. } | ty :: AssocKind :: Const { .. } => true , } , } } fn record_static_candidate (& self , source : CandidateSource) { self . static_candidates . borrow_mut () . push (source) ; } # [instrument (level = "debug" , skip (self))] fn xform_self_ty (& self , item : ty :: AssocItem , impl_ty : Ty < 'tcx > , args : GenericArgsRef < 'tcx > ,) -> (Ty < 'tcx > , Option < Ty < 'tcx > >) { if item . is_fn () && self . mode == Mode :: MethodCall { let sig = self . xform_method_sig (item . def_id , args) ; (sig . inputs () [0] , Some (sig . output ())) } else { (impl_ty , None) } } # [instrument (level = "debug" , skip (self))] fn xform_method_sig (& self , method : DefId , args : GenericArgsRef < 'tcx >) -> ty :: FnSig < 'tcx > { let fn_sig = self . tcx . fn_sig (method) ; debug ! (? fn_sig) ; assert ! (! args . has_escaping_bound_vars ()) ; let generics = self . tcx . generics_of (method) ; assert_eq ! (args . len () , generics . parent_count) ; let xform_fn_sig = if generics . is_own_empty () { fn_sig . instantiate (self . tcx , args) } else { let args = GenericArgs :: for_item (self . tcx , method , | param , _ | { let i = param . index as usize ; if i < args . len () { args [i] } else { match param . kind { GenericParamDefKind :: Lifetime => { self . tcx . lifetimes . re_erased . into () } GenericParamDefKind :: Type { .. } | GenericParamDefKind :: Const { .. } => { self . var_for_def (self . span , param) } } } }) ; fn_sig . instantiate (self . tcx , args) } ; self . tcx . instantiate_bound_regions_with_erased (xform_fn_sig) } # [doc = " Determine if the given associated item type is relevant in the current context."] fn is_relevant_kind_for_mode (& self , kind : ty :: AssocKind) -> bool { match (self . mode , kind) { (Mode :: MethodCall , ty :: AssocKind :: Fn { .. }) => true , (Mode :: Path , ty :: AssocKind :: Const { .. } | ty :: AssocKind :: Fn { .. }) => true , _ => false , } } # [doc = " Determine if the associated item with the given DefId matches"] # [doc = " the desired name via a doc alias."] fn matches_by_doc_alias (& self , def_id : DefId) -> bool { let Some (method) = self . method_name else { return false ; } ; let Some (local_def_id) = def_id . as_local () else { return false ; } ; let hir_id = self . fcx . tcx . local_def_id_to_hir_id (local_def_id) ; let attrs = self . fcx . tcx . hir_attrs (hir_id) ; if is_doc_alias_attrs_contain_symbol (attrs . into_iter () , method . name) { return true ; } for attr in attrs { if attr . has_name (sym :: rustc_confusables) { let Some (confusables) = attr . meta_item_list () else { continue ; } ; for n in confusables { if let Some (lit) = n . lit () && method . name == lit . symbol { return true ; } } } } false } # [doc = " Finds the method with the appropriate name (or return type, as the case may be). If"] # [doc = " `allow_similar_names` is set, find methods with close-matching names."] fn impl_or_trait_item (& self , def_id : DefId) -> SmallVec < [ty :: AssocItem ; 1] > { if let Some (name) = self . method_name { if self . allow_similar_names { let max_dist = max (name . as_str () . len () , 3) / 3 ; self . tcx . associated_items (def_id) . in_definition_order () . filter (| x | { if ! self . is_relevant_kind_for_mode (x . kind) { return false ; } if let Some (d) = edit_distance_with_substrings (name . as_str () , x . name () . as_str () , max_dist ,) { return d > 0 ; } self . matches_by_doc_alias (x . def_id) }) . copied () . collect () } else { self . fcx . associated_value (def_id , name) . filter (| x | self . is_relevant_kind_for_mode (x . kind)) . map_or_else (SmallVec :: new , | x | SmallVec :: from_buf ([x])) } } else { self . tcx . associated_items (def_id) . in_definition_order () . filter (| x | self . is_relevant_kind_for_mode (x . kind)) . copied () . collect () } } }
};
}
