macro_rules! deps {
    () => {
        IsSuggestion!();
        MethodCallee!();
        Mode!();
        MethodError!();
        ProbeScope!();
        CandidateSource!();
        FnCtxt!();
        PickResult!();
    };
}

macro_rules! impl_284 {
    () => {
        deps!();
        impl < 'a , 'tcx > FnCtxt < 'a , 'tcx > { # [doc = " Determines whether the type `self_ty` supports a visible method named `method_name` or not."] # [instrument (level = "debug" , skip (self))] pub (crate) fn method_exists_for_diagnostic (& self , method_name : Ident , self_ty : Ty < 'tcx > , call_expr_id : hir :: HirId , return_type : Option < Ty < 'tcx > > ,) -> bool { match self . probe_for_name (probe :: Mode :: MethodCall , method_name , return_type , IsSuggestion (true) , self_ty , call_expr_id , ProbeScope :: TraitsInScope ,) { Ok (pick) => { pick . maybe_emit_unstable_name_collision_hint (self . tcx , method_name . span , call_expr_id ,) ; true } Err (NoMatch (..)) => false , Err (Ambiguity (..)) => true , Err (PrivateMatch (..)) => false , Err (IllegalSizedBound { .. }) => true , Err (BadReturnType) => true , Err (ErrorReported (_)) => false , } } # [doc = " Adds a suggestion to call the given method to the provided diagnostic."] # [instrument (level = "debug" , skip (self , err , call_expr))] pub (crate) fn suggest_method_call (& self , err : & mut Diag < '_ > , msg : impl Into < SubdiagMessage > + std :: fmt :: Debug , method_name : Ident , self_ty : Ty < 'tcx > , call_expr : & hir :: Expr < 'tcx > , span : Option < Span > ,) { let params = self . lookup_probe_for_diagnostic (method_name , self_ty , call_expr , ProbeScope :: TraitsInScope , None ,) . map (| pick | { let sig = self . tcx . fn_sig (pick . item . def_id) ; sig . skip_binder () . inputs () . skip_binder () . len () . saturating_sub (1) }) . unwrap_or (0) ; let sugg_span = span . unwrap_or (call_expr . span) . shrink_to_hi () ; let (suggestion , applicability) = (format ! ("({})" , (0 .. params) . map (| _ | "_") . collect ::< Vec < _ >> () . join (", ")) , if params > 0 { Applicability :: HasPlaceholders } else { Applicability :: MaybeIncorrect } ,) ; err . span_suggestion_verbose (sugg_span , msg , suggestion , applicability) ; } # [doc = " Performs method lookup. If lookup is successful, it will return the callee"] # [doc = " and store an appropriate adjustment for the self-expr. In some cases it may"] # [doc = " report an error (e.g., invoking the `drop` method)."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " Given a method call like `foo.bar::<T1,...Tn>(a, b + 1, ...)`:"] # [doc = ""] # [doc = " * `self`:                  the surrounding `FnCtxt` (!)"] # [doc = " * `self_ty`:               the (unadjusted) type of the self expression (`foo`)"] # [doc = " * `segment`:               the name and generic arguments of the method (`bar::<T1, ...Tn>`)"] # [doc = " * `span`:                  the span for the method call"] # [doc = " * `call_expr`:             the complete method call: (`foo.bar::<T1,...Tn>(...)`)"] # [doc = " * `self_expr`:             the self expression (`foo`)"] # [doc = " * `args`:                  the expressions of the arguments (`a, b + 1, ...`)"] # [instrument (level = "debug" , skip (self))] pub (crate) fn lookup_method (& self , self_ty : Ty < 'tcx > , segment : & 'tcx hir :: PathSegment < 'tcx > , span : Span , call_expr : & 'tcx hir :: Expr < 'tcx > , self_expr : & 'tcx hir :: Expr < 'tcx > , args : & 'tcx [hir :: Expr < 'tcx >] ,) -> Result < MethodCallee < 'tcx > , MethodError < 'tcx > > { let scope = if let Some (only_method) = segment . res . opt_def_id () { ProbeScope :: Single (only_method) } else { ProbeScope :: TraitsInScope } ; let pick = self . lookup_probe (segment . ident , self_ty , call_expr , scope) ? ; self . lint_edition_dependent_dot_call (self_ty , segment , span , call_expr , self_expr , & pick , args ,) ; for & import_id in & pick . import_ids { debug ! ("used_trait_import: {:?}" , import_id) ; self . typeck_results . borrow_mut () . used_trait_imports . insert (import_id) ; } self . tcx . check_stability (pick . item . def_id , Some (call_expr . hir_id) , span , None) ; let result = self . confirm_method (span , self_expr , call_expr , self_ty , & pick , segment) ; debug ! ("result = {:?}" , result) ; if let Some (span) = result . illegal_sized_bound { let mut needs_mut = false ; if let ty :: Ref (region , t_type , mutability) = self_ty . kind () { let trait_type = Ty :: new_ref (self . tcx , * region , * t_type , mutability . invert ()) ; match self . lookup_probe (segment . ident , trait_type , call_expr , ProbeScope :: TraitsInScope ,) { Ok (ref new_pick) if pick . differs_from (new_pick) => { needs_mut = new_pick . self_ty . ref_mutability () != self_ty . ref_mutability () ; } _ => { } } } let candidates = match self . lookup_probe_for_diagnostic (segment . ident , self_ty , call_expr , ProbeScope :: AllTraits , None ,) { Ok (ref new_pick) if pick . differs_from (new_pick) => { vec ! [new_pick . item . container_id (self . tcx)] } Err (Ambiguity (ref sources)) => sources . iter () . filter_map (| source | { match * source { CandidateSource :: Impl (def) => self . tcx . trait_id_of_impl (def) , CandidateSource :: Trait (_) => None , } }) . collect () , _ => Vec :: new () , } ; return Err (IllegalSizedBound { candidates , needs_mut , bound_span : span , self_expr }) ; } Ok (result . callee) } pub (crate) fn lookup_method_for_diagnostic (& self , self_ty : Ty < 'tcx > , segment : & hir :: PathSegment < 'tcx > , span : Span , call_expr : & 'tcx hir :: Expr < 'tcx > , self_expr : & 'tcx hir :: Expr < 'tcx > ,) -> Result < MethodCallee < 'tcx > , MethodError < 'tcx > > { let pick = self . lookup_probe_for_diagnostic (segment . ident , self_ty , call_expr , ProbeScope :: TraitsInScope , None ,) ? ; Ok (self . confirm_method_for_diagnostic (span , self_expr , call_expr , self_ty , & pick , segment) . callee) } # [instrument (level = "debug" , skip (self , call_expr))] pub (crate) fn lookup_probe (& self , method_name : Ident , self_ty : Ty < 'tcx > , call_expr : & hir :: Expr < '_ > , scope : ProbeScope ,) -> probe :: PickResult < 'tcx > { let pick = self . probe_for_name (probe :: Mode :: MethodCall , method_name , None , IsSuggestion (false) , self_ty , call_expr . hir_id , scope ,) ? ; pick . maybe_emit_unstable_name_collision_hint (self . tcx , method_name . span , call_expr . hir_id) ; Ok (pick) } pub (crate) fn lookup_probe_for_diagnostic (& self , method_name : Ident , self_ty : Ty < 'tcx > , call_expr : & hir :: Expr < '_ > , scope : ProbeScope , return_type : Option < Ty < 'tcx > > ,) -> probe :: PickResult < 'tcx > { let pick = self . probe_for_name (probe :: Mode :: MethodCall , method_name , return_type , IsSuggestion (true) , self_ty , call_expr . hir_id , scope ,) ? ; Ok (pick) } # [doc = " `lookup_method_in_trait` is used for overloaded operators."] # [doc = " It does a very narrow slice of what the normal probe/confirm path does."] # [doc = " In particular, it doesn't really do any probing: it simply constructs"] # [doc = " an obligation for a particular trait with the given self type and checks"] # [doc = " whether that trait is implemented."] # [instrument (level = "debug" , skip (self))] pub (super) fn lookup_method_for_operator (& self , cause : ObligationCause < 'tcx > , method_name : Symbol , trait_def_id : DefId , self_ty : Ty < 'tcx > , opt_rhs_ty : Option < Ty < 'tcx > > ,) -> Option < InferOk < 'tcx , MethodCallee < 'tcx > > > { let args = GenericArgs :: for_item (self . tcx , trait_def_id , | param , _ | match param . kind { GenericParamDefKind :: Lifetime | GenericParamDefKind :: Const { .. } => { unreachable ! ("did not expect operator trait to have lifetime/const") } GenericParamDefKind :: Type { .. } => { if param . index == 0 { self_ty . into () } else if let Some (rhs_ty) = opt_rhs_ty { assert_eq ! (param . index , 1 , "did not expect >1 param on operator trait") ; rhs_ty . into () } else { self . var_for_def (cause . span , param) } } }) ; let obligation = traits :: Obligation :: new (self . tcx , cause , self . param_env , ty :: TraitRef :: new_from_args (self . tcx , trait_def_id , args) ,) ; if ! self . predicate_may_hold (& obligation) { debug ! ("--> Cannot match obligation") ; return None ; } let tcx = self . tcx ; let Some (method_item) = self . associated_value (trait_def_id , Ident :: with_dummy_span (method_name)) else { bug ! ("expected associated item for operator trait") } ; let def_id = method_item . def_id ; if ! method_item . is_fn () { span_bug ! (tcx . def_span (def_id) , "expected `{method_name}` to be an associated function") ; } debug ! ("lookup_in_trait_adjusted: method_item={:?}" , method_item) ; let mut obligations = PredicateObligations :: new () ; let fn_sig = tcx . fn_sig (def_id) . instantiate (self . tcx , args) ; let fn_sig = self . instantiate_binder_with_fresh_vars (obligation . cause . span , BoundRegionConversionTime :: FnCall , fn_sig ,) ; let InferOk { value : fn_sig , obligations : o } = self . at (& obligation . cause , self . param_env) . normalize (fn_sig) ; obligations . extend (o) ; let bounds = self . tcx . predicates_of (def_id) . instantiate (self . tcx , args) ; let InferOk { value : bounds , obligations : o } = self . at (& obligation . cause , self . param_env) . normalize (bounds) ; obligations . extend (o) ; assert ! (! bounds . has_escaping_bound_vars ()) ; let predicates_cause = obligation . cause . clone () ; obligations . extend (traits :: predicates_for_generics (move | _ , _ | predicates_cause . clone () , self . param_env , bounds ,)) ; debug ! ("lookup_method_in_trait: matched method fn_sig={:?} obligation={:?}" , fn_sig , obligation) ; for ty in fn_sig . inputs_and_output { obligations . push (traits :: Obligation :: new (tcx , obligation . cause . clone () , self . param_env , ty :: Binder :: dummy (ty :: PredicateKind :: Clause (ty :: ClauseKind :: WellFormed (ty . into ()))) ,)) ; } let callee = MethodCallee { def_id , args , sig : fn_sig } ; debug ! ("callee = {:?}" , callee) ; Some (InferOk { obligations , value : callee }) } # [doc = " Performs a [full-qualified function call] (formerly \"universal function call\") lookup. If"] # [doc = " lookup is successful, it will return the type of definition and the [`DefId`] of the found"] # [doc = " function definition."] # [doc = ""] # [doc = " [full-qualified function call]: https://doc.rust-lang.org/reference/expressions/call-expr.html#disambiguating-function-calls"] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " Given a function call like `Foo::bar::<T1,...Tn>(...)`:"] # [doc = ""] # [doc = " * `self`:                  the surrounding `FnCtxt` (!)"] # [doc = " * `span`:                  the span of the call, excluding arguments (`Foo::bar::<T1, ...Tn>`)"] # [doc = " * `method_name`:           the identifier of the function within the container type (`bar`)"] # [doc = " * `self_ty`:               the type to search within (`Foo`)"] # [doc = " * `self_ty_span`           the span for the type being searched within (span of `Foo`)"] # [doc = " * `expr_id`:               the [`hir::HirId`] of the expression composing the entire call"] # [instrument (level = "debug" , skip (self) , ret)] pub (crate) fn resolve_fully_qualified_call (& self , span : Span , method_name : Ident , self_ty : Ty < 'tcx > , self_ty_span : Span , expr_id : hir :: HirId ,) -> Result < (DefKind , DefId) , MethodError < 'tcx > > { let tcx = self . tcx ; let mut struct_variant = None ; if let ty :: Adt (adt_def , _) = self_ty . kind () { if adt_def . is_enum () { let variant_def = adt_def . variants () . iter () . find (| vd | tcx . hygienic_eq (method_name , vd . ident (tcx) , adt_def . did ())) ; if let Some (variant_def) = variant_def { if let Some ((ctor_kind , ctor_def_id)) = variant_def . ctor { tcx . check_stability (ctor_def_id , Some (expr_id) , span , Some (method_name . span) ,) ; return Ok ((DefKind :: Ctor (CtorOf :: Variant , ctor_kind) , ctor_def_id)) ; } else { struct_variant = Some ((DefKind :: Variant , variant_def . def_id)) ; } } } } let pick = self . probe_for_name (probe :: Mode :: Path , method_name , None , IsSuggestion (false) , self_ty , expr_id , ProbeScope :: TraitsInScope ,) ; let pick = match (pick , struct_variant) { (Err (_) , Some (res)) => return Ok (res) , (pick , _) => pick ? , } ; pick . maybe_emit_unstable_name_collision_hint (self . tcx , span , expr_id) ; self . lint_fully_qualified_call_from_2018 (span , method_name , self_ty , self_ty_span , expr_id , & pick ,) ; debug ! (? pick) ; { let mut typeck_results = self . typeck_results . borrow_mut () ; for import_id in pick . import_ids { debug ! (used_trait_import =? import_id) ; typeck_results . used_trait_imports . insert (import_id) ; } } let def_kind = pick . item . as_def_kind () ; tcx . check_stability (pick . item . def_id , Some (expr_id) , span , Some (method_name . span)) ; Ok ((def_kind , pick . item . def_id)) } # [doc = " Finds item with name `item_ident` defined in impl/trait `def_id`"] # [doc = " and return it, or `None`, if no such item was defined there."] fn associated_value (& self , def_id : DefId , item_ident : Ident) -> Option < ty :: AssocItem > { self . tcx . associated_items (def_id) . find_by_ident_and_namespace (self . tcx , item_ident , Namespace :: ValueNS , def_id) . copied () } }
    };
}

impl_284!();