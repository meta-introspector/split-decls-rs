macro_rules! deps {
    () => {
        ExpectedSig!();
        Error!();
        FnCtxt!();
        Expectation!();
        CoroutineTypes!();
        ClosureSignatures!();
    };
}

macro_rules! impl_26 {
    () => {
        deps!();
        impl < 'a , 'tcx > FnCtxt < 'a , 'tcx > { # [instrument (skip (self , closure) , level = "debug")] pub (crate) fn check_expr_closure (& self , closure : & hir :: Closure < 'tcx > , expr_span : Span , expected : Expectation < 'tcx > ,) -> Ty < 'tcx > { let tcx = self . tcx ; let body = tcx . hir_body (closure . body) ; let expr_def_id = closure . def_id ; let (expected_sig , expected_kind) = match expected . to_option (self) { Some (ty) => self . deduce_closure_signature (self . try_structurally_resolve_type (expr_span , ty) , closure . kind ,) , None => (None , None) , } ; let ClosureSignatures { bound_sig , mut liberated_sig } = self . sig_of_closure (expr_def_id , closure . fn_decl , closure . kind , expected_sig) ; debug ! (? bound_sig , ? liberated_sig) ; let parent_args = GenericArgs :: identity_for_item (tcx , tcx . typeck_root_def_id (expr_def_id . to_def_id ())) ; let tupled_upvars_ty = self . next_ty_var (expr_span) ; let (closure_ty , coroutine_types) = match closure . kind { hir :: ClosureKind :: Closure => { let sig = bound_sig . map_bound (| sig | { tcx . mk_fn_sig ([Ty :: new_tup (tcx , sig . inputs ())] , sig . output () , sig . c_variadic , sig . safety , sig . abi ,) }) ; debug ! (? sig , ? expected_kind) ; let closure_kind_ty = match expected_kind { Some (kind) => Ty :: from_closure_kind (tcx , kind) , None => self . next_ty_var (expr_span) , } ; let closure_args = ty :: ClosureArgs :: new (tcx , ty :: ClosureArgsParts { parent_args , closure_kind_ty , closure_sig_as_fn_ptr_ty : Ty :: new_fn_ptr (tcx , sig) , tupled_upvars_ty , } ,) ; (Ty :: new_closure (tcx , expr_def_id . to_def_id () , closure_args . args) , None) } hir :: ClosureKind :: Coroutine (kind) => { let yield_ty = match kind { hir :: CoroutineKind :: Desugared (hir :: CoroutineDesugaring :: Gen , _) | hir :: CoroutineKind :: Coroutine (_) => { let yield_ty = self . next_ty_var (expr_span) ; self . require_type_is_sized (yield_ty , expr_span , ObligationCauseCode :: SizedYieldType ,) ; yield_ty } hir :: CoroutineKind :: Desugared (hir :: CoroutineDesugaring :: AsyncGen , _) => { let yield_ty = self . next_ty_var (expr_span) ; self . require_type_is_sized (yield_ty , expr_span , ObligationCauseCode :: SizedYieldType ,) ; Ty :: new_adt (tcx , tcx . adt_def (tcx . require_lang_item (hir :: LangItem :: Poll , expr_span)) , tcx . mk_args (& [Ty :: new_adt (tcx , tcx . adt_def (tcx . require_lang_item (hir :: LangItem :: Option , expr_span) ,) , tcx . mk_args (& [yield_ty . into ()]) ,) . into ()]) ,) } hir :: CoroutineKind :: Desugared (hir :: CoroutineDesugaring :: Async , _) => { tcx . types . unit } } ; let resume_ty = liberated_sig . inputs () . get (0) . copied () . unwrap_or (tcx . types . unit) ; let kind_ty = match kind { hir :: CoroutineKind :: Desugared (_ , hir :: CoroutineSource :: Closure) => { self . next_ty_var (expr_span) } _ => tcx . types . unit , } ; let coroutine_args = ty :: CoroutineArgs :: new (tcx , ty :: CoroutineArgsParts { parent_args , kind_ty , resume_ty , yield_ty , return_ty : liberated_sig . output () , tupled_upvars_ty , } ,) ; (Ty :: new_coroutine (tcx , expr_def_id . to_def_id () , coroutine_args . args) , Some (CoroutineTypes { resume_ty , yield_ty }) ,) } hir :: ClosureKind :: CoroutineClosure (kind) => { let (bound_return_ty , bound_yield_ty) = match kind { hir :: CoroutineDesugaring :: Gen => { (tcx . types . unit , self . infcx . next_ty_var (expr_span)) } hir :: CoroutineDesugaring :: Async => { (bound_sig . skip_binder () . output () , tcx . types . unit) } hir :: CoroutineDesugaring :: AsyncGen => { todo ! ("`async gen` closures not supported yet") } } ; let resume_ty = self . next_ty_var (expr_span) ; let closure_kind_ty = match expected_kind { Some (kind) => Ty :: from_closure_kind (tcx , kind) , None => self . next_ty_var (expr_span) , } ; let coroutine_captures_by_ref_ty = self . next_ty_var (expr_span) ; let closure_args = ty :: CoroutineClosureArgs :: new (tcx , ty :: CoroutineClosureArgsParts { parent_args , closure_kind_ty , signature_parts_ty : Ty :: new_fn_ptr (tcx , bound_sig . map_bound (| sig | { tcx . mk_fn_sig ([resume_ty , Ty :: new_tup_from_iter (tcx , sig . inputs () . iter () . copied ()) ,] , Ty :: new_tup (tcx , & [bound_yield_ty , bound_return_ty]) , sig . c_variadic , sig . safety , sig . abi ,) }) ,) , tupled_upvars_ty , coroutine_captures_by_ref_ty , } ,) ; let coroutine_kind_ty = match expected_kind { Some (kind) => Ty :: from_coroutine_closure_kind (tcx , kind) , None => self . next_ty_var (expr_span) , } ; let coroutine_upvars_ty = self . next_ty_var (expr_span) ; let coroutine_output_ty = tcx . liberate_late_bound_regions (expr_def_id . to_def_id () , closure_args . coroutine_closure_sig () . map_bound (| sig | { sig . to_coroutine (tcx , parent_args , coroutine_kind_ty , tcx . coroutine_for_closure (expr_def_id) , coroutine_upvars_ty ,) }) ,) ; liberated_sig = tcx . mk_fn_sig (liberated_sig . inputs () . iter () . copied () , coroutine_output_ty , liberated_sig . c_variadic , liberated_sig . safety , liberated_sig . abi ,) ; (Ty :: new_coroutine_closure (tcx , expr_def_id . to_def_id () , closure_args . args) , None) } } ; check_fn (& mut FnCtxt :: new (self , self . param_env , closure . def_id) , liberated_sig , coroutine_types , closure . fn_decl , expr_def_id , body , false ,) ; closure_ty } # [doc = " Given the expected type, figures out what it can about this closure we"] # [doc = " are about to type check:"] # [instrument (skip (self) , level = "debug" , ret)] fn deduce_closure_signature (& self , expected_ty : Ty < 'tcx > , closure_kind : hir :: ClosureKind ,) -> (Option < ExpectedSig < 'tcx > > , Option < ty :: ClosureKind >) { match * expected_ty . kind () { ty :: Alias (ty :: Opaque , ty :: AliasTy { def_id , args , .. }) => self . deduce_closure_signature_from_predicates (expected_ty , closure_kind , self . tcx . explicit_item_self_bounds (def_id) . iter_instantiated_copied (self . tcx , args) . map (| (c , s) | (c . as_predicate () , s)) ,) , ty :: Dynamic (object_type , ..) => { let sig = object_type . projection_bounds () . find_map (| pb | { let pb = pb . with_self_ty (self . tcx , self . tcx . types . trait_object_dummy_self) ; self . deduce_sig_from_projection (None , closure_kind , pb) }) ; let kind = object_type . principal_def_id () . and_then (| did | self . tcx . fn_trait_kind_from_def_id (did)) ; (sig , kind) } ty :: Infer (ty :: TyVar (vid)) => self . deduce_closure_signature_from_predicates (Ty :: new_var (self . tcx , self . root_var (vid)) , closure_kind , self . obligations_for_self_ty (vid) . into_iter () . map (| obl | (obl . predicate , obl . cause . span)) ,) , ty :: FnPtr (sig_tys , hdr) => match closure_kind { hir :: ClosureKind :: Closure => { let expected_sig = ExpectedSig { cause_span : None , sig : sig_tys . with (hdr) } ; (Some (expected_sig) , Some (ty :: ClosureKind :: Fn)) } hir :: ClosureKind :: Coroutine (_) | hir :: ClosureKind :: CoroutineClosure (_) => { (None , None) } } , _ => (None , None) , } } fn deduce_closure_signature_from_predicates (& self , expected_ty : Ty < 'tcx > , closure_kind : hir :: ClosureKind , predicates : impl DoubleEndedIterator < Item = (ty :: Predicate < 'tcx > , Span) > ,) -> (Option < ExpectedSig < 'tcx > > , Option < ty :: ClosureKind >) { let mut expected_sig = None ; let mut expected_kind = None ; for (pred , span) in traits :: elaborate (self . tcx , predicates . rev () ,) . filter_only_self () { debug ! (? pred) ; let bound_predicate = pred . kind () ; if expected_sig . is_none () && let ty :: PredicateKind :: Clause (ty :: ClauseKind :: Projection (proj_predicate)) = bound_predicate . skip_binder () { let inferred_sig = self . normalize (span , self . deduce_sig_from_projection (Some (span) , closure_kind , bound_predicate . rebind (proj_predicate) ,) ,) ; struct MentionsTy < 'tcx > { expected_ty : Ty < 'tcx > , } impl < 'tcx > TypeVisitor < TyCtxt < 'tcx > > for MentionsTy < 'tcx > { type Result = ControlFlow < () > ; fn visit_ty (& mut self , t : Ty < 'tcx >) -> Self :: Result { if t == self . expected_ty { ControlFlow :: Break (()) } else { t . super_visit_with (self) } } } if self . next_trait_solver () && let Some (inferred_sig) = inferred_sig { let generalized_fnptr_sig = self . next_ty_var (span) ; let inferred_fnptr_sig = Ty :: new_fn_ptr (self . tcx , inferred_sig . sig) ; self . demand_eqtype (span , inferred_fnptr_sig , generalized_fnptr_sig) ; let resolved_sig = self . resolve_vars_if_possible (generalized_fnptr_sig) ; if resolved_sig . visit_with (& mut MentionsTy { expected_ty }) . is_continue () { expected_sig = Some (ExpectedSig { cause_span : inferred_sig . cause_span , sig : resolved_sig . fn_sig (self . tcx) , }) ; } } else { if inferred_sig . visit_with (& mut MentionsTy { expected_ty }) . is_continue () { expected_sig = inferred_sig ; } } } let trait_def_id = match bound_predicate . skip_binder () { ty :: PredicateKind :: Clause (ty :: ClauseKind :: Projection (data)) => { Some (data . projection_term . trait_def_id (self . tcx)) } ty :: PredicateKind :: Clause (ty :: ClauseKind :: Trait (data)) => Some (data . def_id ()) , _ => None , } ; if let Some (trait_def_id) = trait_def_id { let found_kind = match closure_kind { hir :: ClosureKind :: Closure | hir :: ClosureKind :: CoroutineClosure (hir :: CoroutineDesugaring :: Gen) => { self . tcx . fn_trait_kind_from_def_id (trait_def_id) } hir :: ClosureKind :: CoroutineClosure (hir :: CoroutineDesugaring :: Async) => self . tcx . async_fn_trait_kind_from_def_id (trait_def_id) . or_else (| | self . tcx . fn_trait_kind_from_def_id (trait_def_id)) , _ => None , } ; if let Some (found_kind) = found_kind { match (expected_kind , found_kind) { (None , _) => expected_kind = Some (found_kind) , (Some (ClosureKind :: FnMut) , ClosureKind :: Fn) => { expected_kind = Some (ClosureKind :: Fn) } (Some (ClosureKind :: FnOnce) , ClosureKind :: Fn | ClosureKind :: FnMut) => { expected_kind = Some (found_kind) } _ => { } } } } } (expected_sig , expected_kind) } # [doc = " Given a projection like \"<F as Fn(X)>::Result == Y\", we can deduce"] # [doc = " everything we need to know about a closure or coroutine."] # [doc = ""] # [doc = " The `cause_span` should be the span that caused us to"] # [doc = " have this expected signature, or `None` if we can't readily"] # [doc = " know that."] # [instrument (level = "debug" , skip (self , cause_span) , ret)] fn deduce_sig_from_projection (& self , cause_span : Option < Span > , closure_kind : hir :: ClosureKind , projection : ty :: PolyProjectionPredicate < 'tcx > ,) -> Option < ExpectedSig < 'tcx > > { let def_id = projection . item_def_id () ; match closure_kind { hir :: ClosureKind :: Closure if self . tcx . is_lang_item (def_id , LangItem :: FnOnceOutput) => { self . extract_sig_from_projection (cause_span , projection) } hir :: ClosureKind :: CoroutineClosure (hir :: CoroutineDesugaring :: Async) if self . tcx . is_lang_item (def_id , LangItem :: AsyncFnOnceOutput) => { self . extract_sig_from_projection (cause_span , projection) } hir :: ClosureKind :: CoroutineClosure (hir :: CoroutineDesugaring :: Async) if self . tcx . is_lang_item (def_id , LangItem :: FnOnceOutput) => { self . extract_sig_from_projection_and_future_bound (cause_span , projection) } _ => None , } } # [doc = " Given an `FnOnce::Output` or `AsyncFn::Output` projection, extract the args"] # [doc = " and return type to infer a [`ty::PolyFnSig`] for the closure."] fn extract_sig_from_projection (& self , cause_span : Option < Span > , projection : ty :: PolyProjectionPredicate < 'tcx > ,) -> Option < ExpectedSig < 'tcx > > { let projection = self . resolve_vars_if_possible (projection) ; let arg_param_ty = projection . skip_binder () . projection_term . args . type_at (1) ; debug ! (? arg_param_ty) ; let ty :: Tuple (input_tys) = * arg_param_ty . kind () else { return None ; } ; let ret_param_ty = projection . skip_binder () . term . expect_type () ; debug ! (? ret_param_ty) ; let sig = projection . rebind (self . tcx . mk_fn_sig (input_tys , ret_param_ty , false , hir :: Safety :: Safe , ExternAbi :: Rust ,)) ; Some (ExpectedSig { cause_span , sig }) } # [doc = " When an async closure is passed to a function that has a \"two-part\" `Fn`"] # [doc = " and `Future` trait bound, like:"] # [doc = ""] # [doc = " ```rust"] # [doc = " use std::future::Future;"] # [doc = ""] # [doc = " fn not_exactly_an_async_closure<F, Fut>(_f: F)"] # [doc = " where"] # [doc = "     F: FnOnce(String, u32) -> Fut,"] # [doc = "     Fut: Future<Output = i32>,"] # [doc = " {}"] # [doc = " ```"] # [doc = ""] # [doc = " The we want to be able to extract the signature to guide inference in the async"] # [doc = " closure. We will have two projection predicates registered in this case. First,"] # [doc = " we identify the `FnOnce<Args, Output = ?Fut>` bound, and if the output type is"] # [doc = " an inference variable `?Fut`, we check if that is bounded by a `Future<Output = Ty>`"] # [doc = " projection."] # [doc = ""] # [doc = " This function is actually best-effort with the return type; if we don't find a"] # [doc = " `Future` projection, we still will return arguments that we extracted from the `FnOnce`"] # [doc = " projection, and the output will be an unconstrained type variable instead."] fn extract_sig_from_projection_and_future_bound (& self , cause_span : Option < Span > , projection : ty :: PolyProjectionPredicate < 'tcx > ,) -> Option < ExpectedSig < 'tcx > > { let projection = self . resolve_vars_if_possible (projection) ; let arg_param_ty = projection . skip_binder () . projection_term . args . type_at (1) ; debug ! (? arg_param_ty) ; let ty :: Tuple (input_tys) = * arg_param_ty . kind () else { return None ; } ; let ty :: Infer (ty :: TyVar (return_vid)) = * projection . skip_binder () . term . expect_type () . kind () else { return None ; } ; let mut return_ty = None ; for bound in self . obligations_for_self_ty (return_vid) { if let Some (ret_projection) = bound . predicate . as_projection_clause () && let Some (ret_projection) = ret_projection . no_bound_vars () && self . tcx . is_lang_item (ret_projection . def_id () , LangItem :: FutureOutput) { return_ty = Some (ret_projection . term . expect_type ()) ; break ; } } let return_ty = return_ty . unwrap_or_else (| | self . next_ty_var (cause_span . unwrap_or (DUMMY_SP))) ; let sig = projection . rebind (self . tcx . mk_fn_sig (input_tys , return_ty , false , hir :: Safety :: Safe , ExternAbi :: Rust ,)) ; Some (ExpectedSig { cause_span , sig }) } fn sig_of_closure (& self , expr_def_id : LocalDefId , decl : & hir :: FnDecl < 'tcx > , closure_kind : hir :: ClosureKind , expected_sig : Option < ExpectedSig < 'tcx > > ,) -> ClosureSignatures < 'tcx > { if let Some (e) = expected_sig { self . sig_of_closure_with_expectation (expr_def_id , decl , closure_kind , e) } else { self . sig_of_closure_no_expectation (expr_def_id , decl , closure_kind) } } # [doc = " If there is no expected signature, then we will convert the"] # [doc = " types that the user gave into a signature."] # [instrument (skip (self , expr_def_id , decl) , level = "debug")] fn sig_of_closure_no_expectation (& self , expr_def_id : LocalDefId , decl : & hir :: FnDecl < 'tcx > , closure_kind : hir :: ClosureKind ,) -> ClosureSignatures < 'tcx > { let bound_sig = self . supplied_sig_of_closure (expr_def_id , decl , closure_kind) ; self . closure_sigs (expr_def_id , bound_sig) } # [doc = " Invoked to compute the signature of a closure expression. This"] # [doc = " combines any user-provided type annotations (e.g., `|x: u32|"] # [doc = " -> u32 { .. }`) with the expected signature."] # [doc = ""] # [doc = " The approach is as follows:"] # [doc = ""] # [doc = " - Let `S` be the (higher-ranked) signature that we derive from the user's annotations."] # [doc = " - Let `E` be the (higher-ranked) signature that we derive from the expectations, if any."] # [doc = "   - If we have no expectation `E`, then the signature of the closure is `S`."] # [doc = "   - Otherwise, the signature of the closure is E. Moreover:"] # [doc = "     - Skolemize the late-bound regions in `E`, yielding `E'`."] # [doc = "     - Instantiate all the late-bound regions bound in the closure within `S`"] # [doc = "       with fresh (existential) variables, yielding `S'`"] # [doc = "     - Require that `E' = S'`"] # [doc = "       - We could use some kind of subtyping relationship here,"] # [doc = "         I imagine, but equality is easier and works fine for"] # [doc = "         our purposes."] # [doc = ""] # [doc = " The key intuition here is that the user's types must be valid"] # [doc = " from \"the inside\" of the closure, but the expectation"] # [doc = " ultimately drives the overall signature."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```ignore (illustrative)"] # [doc = " fn with_closure<F>(_: F)"] # [doc = "   where F: Fn(&u32) -> &u32 { .. }"] # [doc = ""] # [doc = " with_closure(|x: &u32| { ... })"] # [doc = " ```"] # [doc = ""] # [doc = " Here:"] # [doc = " - E would be `fn(&u32) -> &u32`."] # [doc = " - S would be `fn(&u32) -> ?T`"] # [doc = " - E' is `&'!0 u32 -> &'!0 u32`"] # [doc = " - S' is `&'?0 u32 -> ?T`"] # [doc = ""] # [doc = " S' can be unified with E' with `['?0 = '!0, ?T = &'!10 u32]`."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " - `expr_def_id`: the `LocalDefId` of the closure expression"] # [doc = " - `decl`: the HIR declaration of the closure"] # [doc = " - `body`: the body of the closure"] # [doc = " - `expected_sig`: the expected signature (if any). Note that"] # [doc = "   this is missing a binder: that is, there may be late-bound"] # [doc = "   regions with depth 1, which are bound then by the closure."] # [instrument (skip (self , expr_def_id , decl) , level = "debug")] fn sig_of_closure_with_expectation (& self , expr_def_id : LocalDefId , decl : & hir :: FnDecl < 'tcx > , closure_kind : hir :: ClosureKind , expected_sig : ExpectedSig < 'tcx > ,) -> ClosureSignatures < 'tcx > { if expected_sig . sig . c_variadic () != decl . c_variadic { return self . sig_of_closure_no_expectation (expr_def_id , decl , closure_kind) ; } else if expected_sig . sig . skip_binder () . inputs_and_output . len () != decl . inputs . len () + 1 { return self . sig_of_closure_with_mismatched_number_of_arguments (expr_def_id , decl , expected_sig ,) ; } assert ! (! expected_sig . sig . skip_binder () . has_vars_bound_above (ty :: INNERMOST)) ; let bound_sig = expected_sig . sig . map_bound (| sig | { self . tcx . mk_fn_sig (sig . inputs () . iter () . cloned () , sig . output () , sig . c_variadic , hir :: Safety :: Safe , ExternAbi :: RustCall ,) }) ; let bound_sig = self . tcx . anonymize_bound_vars (bound_sig) ; let closure_sigs = self . closure_sigs (expr_def_id , bound_sig) ; match self . merge_supplied_sig_with_expectation (expr_def_id , decl , closure_kind , closure_sigs ,) { Ok (infer_ok) => self . register_infer_ok_obligations (infer_ok) , Err (_) => self . sig_of_closure_no_expectation (expr_def_id , decl , closure_kind) , } } fn sig_of_closure_with_mismatched_number_of_arguments (& self , expr_def_id : LocalDefId , decl : & hir :: FnDecl < 'tcx > , expected_sig : ExpectedSig < 'tcx > ,) -> ClosureSignatures < 'tcx > { let expr_map_node = self . tcx . hir_node_by_def_id (expr_def_id) ; let expected_args : Vec < _ > = expected_sig . sig . skip_binder () . inputs () . iter () . map (| ty | ArgKind :: from_expected_ty (* ty , None)) . collect () ; let (closure_span , closure_arg_span , found_args) = match self . err_ctxt () . get_fn_like_arguments (expr_map_node) { Some ((sp , arg_sp , args)) => (Some (sp) , arg_sp , args) , None => (None , None , Vec :: new ()) , } ; let expected_span = expected_sig . cause_span . unwrap_or_else (| | self . tcx . def_span (expr_def_id)) ; let guar = self . err_ctxt () . report_arg_count_mismatch (expected_span , closure_span , expected_args , found_args , true , closure_arg_span ,) . emit () ; let error_sig = self . error_sig_of_closure (decl , guar) ; self . closure_sigs (expr_def_id , error_sig) } # [doc = " Enforce the user's types against the expectation. See"] # [doc = " `sig_of_closure_with_expectation` for details on the overall"] # [doc = " strategy."] # [instrument (level = "debug" , skip (self , expr_def_id , decl , expected_sigs))] fn merge_supplied_sig_with_expectation (& self , expr_def_id : LocalDefId , decl : & hir :: FnDecl < 'tcx > , closure_kind : hir :: ClosureKind , mut expected_sigs : ClosureSignatures < 'tcx > ,) -> InferResult < 'tcx , ClosureSignatures < 'tcx > > { let supplied_sig = self . supplied_sig_of_closure (expr_def_id , decl , closure_kind) ; debug ! (? supplied_sig) ; self . commit_if_ok (| _ | { let mut all_obligations = PredicateObligations :: new () ; let supplied_sig = self . instantiate_binder_with_fresh_vars (self . tcx . def_span (expr_def_id) , BoundRegionConversionTime :: FnCall , supplied_sig ,) ; for ((hir_ty , & supplied_ty) , expected_ty) in iter :: zip (iter :: zip (decl . inputs , supplied_sig . inputs ()) , expected_sigs . liberated_sig . inputs () ,) { let cause = self . misc (hir_ty . span) ; let InferOk { value : () , obligations } = self . at (& cause , self . param_env) . eq (DefineOpaqueTypes :: Yes , * expected_ty , supplied_ty ,) ? ; all_obligations . extend (obligations) ; } let supplied_output_ty = supplied_sig . output () ; let cause = & self . misc (decl . output . span ()) ; let InferOk { value : () , obligations } = self . at (cause , self . param_env) . eq (DefineOpaqueTypes :: Yes , expected_sigs . liberated_sig . output () , supplied_output_ty ,) ? ; all_obligations . extend (obligations) ; let inputs = supplied_sig . inputs () . into_iter () . map (| & ty | self . resolve_vars_if_possible (ty)) ; expected_sigs . liberated_sig = self . tcx . mk_fn_sig (inputs , supplied_output_ty , expected_sigs . liberated_sig . c_variadic , hir :: Safety :: Safe , ExternAbi :: RustCall ,) ; Ok (InferOk { value : expected_sigs , obligations : all_obligations }) }) } # [doc = " If there is no expected signature, then we will convert the"] # [doc = " types that the user gave into a signature."] # [doc = ""] # [doc = " Also, record this closure signature for later."] # [instrument (skip (self , decl) , level = "debug" , ret)] fn supplied_sig_of_closure (& self , expr_def_id : LocalDefId , decl : & hir :: FnDecl < 'tcx > , closure_kind : hir :: ClosureKind ,) -> ty :: PolyFnSig < 'tcx > { let lowerer = self . lowerer () ; trace ! ("decl = {:#?}" , decl) ; debug ! (? closure_kind) ; let hir_id = self . tcx . local_def_id_to_hir_id (expr_def_id) ; let bound_vars = self . tcx . late_bound_vars (hir_id) ; let supplied_arguments = decl . inputs . iter () . map (| a | lowerer . lower_ty (a)) ; let supplied_return = match decl . output { hir :: FnRetTy :: Return (ref output) => lowerer . lower_ty (output) , hir :: FnRetTy :: DefaultReturn (_) => match closure_kind { hir :: ClosureKind :: Coroutine (hir :: CoroutineKind :: Desugared (hir :: CoroutineDesugaring :: Async , hir :: CoroutineSource :: Fn ,)) => { debug ! ("closure is async fn body") ; self . deduce_future_output_from_obligations (expr_def_id) . unwrap_or_else (| | { lowerer . ty_infer (None , decl . output . span ()) }) } hir :: ClosureKind :: Coroutine (hir :: CoroutineKind :: Desugared (hir :: CoroutineDesugaring :: Gen | hir :: CoroutineDesugaring :: AsyncGen , _ ,)) => self . tcx . types . unit , hir :: ClosureKind :: Coroutine (hir :: CoroutineKind :: Desugared (hir :: CoroutineDesugaring :: Async , _ ,)) | hir :: ClosureKind :: Coroutine (hir :: CoroutineKind :: Coroutine (_)) | hir :: ClosureKind :: Closure | hir :: ClosureKind :: CoroutineClosure (_) => { lowerer . ty_infer (None , decl . output . span ()) } } , } ; let result = ty :: Binder :: bind_with_vars (self . tcx . mk_fn_sig (supplied_arguments , supplied_return , decl . c_variadic , hir :: Safety :: Safe , ExternAbi :: RustCall ,) , bound_vars ,) ; let c_result = self . infcx . canonicalize_response (result) ; self . typeck_results . borrow_mut () . user_provided_sigs . insert (expr_def_id , c_result) ; self . normalize (self . tcx . def_span (expr_def_id) , result) } # [doc = " Invoked when we are translating the coroutine that results"] # [doc = " from desugaring an `async fn`. Returns the \"sugared\" return"] # [doc = " type of the `async fn` -- that is, the return type that the"] # [doc = " user specified. The \"desugared\" return type is an `impl"] # [doc = " Future<Output = T>`, so we do this by searching through the"] # [doc = " obligations to extract the `T`."] # [instrument (skip (self) , level = "debug" , ret)] fn deduce_future_output_from_obligations (& self , body_def_id : LocalDefId) -> Option < Ty < 'tcx > > { let ret_coercion = self . ret_coercion . as_ref () . unwrap_or_else (| | { span_bug ! (self . tcx . def_span (body_def_id) , "async fn coroutine outside of a fn") }) ; let closure_span = self . tcx . def_span (body_def_id) ; let ret_ty = ret_coercion . borrow () . expected_ty () ; let ret_ty = self . try_structurally_resolve_type (closure_span , ret_ty) ; let get_future_output = | predicate : ty :: Predicate < 'tcx > , span | { let bound_predicate = predicate . kind () ; if let ty :: PredicateKind :: Clause (ty :: ClauseKind :: Projection (proj_predicate)) = bound_predicate . skip_binder () { self . deduce_future_output_from_projection (span , bound_predicate . rebind (proj_predicate) ,) } else { None } } ; let output_ty = match * ret_ty . kind () { ty :: Infer (ty :: TyVar (ret_vid)) => { self . obligations_for_self_ty (ret_vid) . into_iter () . find_map (| obligation | { get_future_output (obligation . predicate , obligation . cause . span) }) ? } ty :: Alias (ty :: Projection , _) => { return Some (Ty :: new_error_with_message (self . tcx , closure_span , "this projection should have been projected to an opaque type" ,)) ; } ty :: Alias (ty :: Opaque , ty :: AliasTy { def_id , args , .. }) => self . tcx . explicit_item_self_bounds (def_id) . iter_instantiated_copied (self . tcx , args) . find_map (| (p , s) | get_future_output (p . as_predicate () , s)) ? , ty :: Error (_) => return Some (ret_ty) , _ => { span_bug ! (closure_span , "invalid async fn coroutine return type: {ret_ty:?}") } } ; let output_ty = self . normalize (closure_span , output_ty) ; let InferOk { value : output_ty , obligations } = self . replace_opaque_types_with_inference_vars (output_ty , body_def_id , closure_span , self . param_env ,) ; self . register_predicates (obligations) ; Some (output_ty) } # [doc = " Given a projection like"] # [doc = ""] # [doc = " `<X as Future>::Output = T`"] # [doc = ""] # [doc = " where `X` is some type that has no late-bound regions, returns"] # [doc = " `Some(T)`. If the projection is for some other trait, returns"] # [doc = " `None`."] fn deduce_future_output_from_projection (& self , cause_span : Span , predicate : ty :: PolyProjectionPredicate < 'tcx > ,) -> Option < Ty < 'tcx > > { debug ! ("deduce_future_output_from_projection(predicate={:?})" , predicate) ; let Some (predicate) = predicate . no_bound_vars () else { debug ! ("deduce_future_output_from_projection: has late-bound regions") ; return None ; } ; let trait_def_id = predicate . projection_term . trait_def_id (self . tcx) ; if ! self . tcx . is_lang_item (trait_def_id , LangItem :: Future) { debug ! ("deduce_future_output_from_projection: not a future") ; return None ; } let output_assoc_item = self . tcx . associated_item_def_ids (trait_def_id) [0] ; if output_assoc_item != predicate . projection_term . def_id { span_bug ! (cause_span , "projecting associated item `{:?}` from future, which is not Output `{:?}`" , predicate . projection_term . def_id , output_assoc_item ,) ; } let output_ty = self . resolve_vars_if_possible (predicate . term) ; debug ! ("deduce_future_output_from_projection: output_ty={:?}" , output_ty) ; Some (output_ty . expect_type ()) } # [doc = " Converts the types that the user supplied, in case that doing"] # [doc = " so should yield an error, but returns back a signature where"] # [doc = " all parameters are of type `ty::Error`."] fn error_sig_of_closure (& self , decl : & hir :: FnDecl < 'tcx > , guar : ErrorGuaranteed ,) -> ty :: PolyFnSig < 'tcx > { let lowerer = self . lowerer () ; let err_ty = Ty :: new_error (self . tcx , guar) ; let supplied_arguments = decl . inputs . iter () . map (| a | { lowerer . lower_ty (a) ; err_ty }) ; if let hir :: FnRetTy :: Return (ref output) = decl . output { lowerer . lower_ty (output) ; } let result = ty :: Binder :: dummy (self . tcx . mk_fn_sig (supplied_arguments , err_ty , decl . c_variadic , hir :: Safety :: Safe , ExternAbi :: RustCall ,)) ; debug ! ("supplied_sig_of_closure: result={:?}" , result) ; result } # [instrument (level = "debug" , skip (self) , ret)] fn closure_sigs (& self , expr_def_id : LocalDefId , bound_sig : ty :: PolyFnSig < 'tcx > ,) -> ClosureSignatures < 'tcx > { let liberated_sig = self . tcx () . liberate_late_bound_regions (expr_def_id . to_def_id () , bound_sig) ; let liberated_sig = self . normalize (self . tcx . def_span (expr_def_id) , liberated_sig) ; ClosureSignatures { bound_sig , liberated_sig } } }
    };
}

impl_26!();