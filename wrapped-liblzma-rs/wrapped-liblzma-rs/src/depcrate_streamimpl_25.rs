// Generated macro for impl_25 (impl)
macro_rules! Depcrate_streamimpl_25 {
() => {
// Module: crate::stream
// Provides: {"impl_25"}
// Dependencies: {}
# [doc = " Encoder-related functions"] impl Stream { # [doc = " Initialize .xz stream encoder using a preset number"] # [doc = ""] # [doc = " This is intended to be used by most for encoding data. The `preset`"] # [doc = " argument is a number 0-9 indicating the compression level to use, and"] # [doc = " normally 6 is a reasonable default."] # [doc = ""] # [doc = " The `check` argument is the integrity check to insert at the end of the"] # [doc = " stream. The default of `Crc64` is typically appropriate."] # [inline] pub fn new_easy_encoder (preset : u32 , check : Check) -> Result < Stream , Error > { let mut init = unsafe { Stream :: zeroed () } ; cvt (unsafe { liblzma_sys :: lzma_easy_encoder (& mut init . raw , preset , check as liblzma_sys :: lzma_check) }) ? ; Ok (init) } # [doc = " Initialize .lzma encoder (legacy file format)"] # [doc = ""] # [doc = " The .lzma format is sometimes called the LZMA_Alone format, which is the"] # [doc = " reason for the name of this function. The .lzma format supports only the"] # [doc = " LZMA1 filter. There is no support for integrity checks like CRC32."] # [doc = ""] # [doc = " Use this function if and only if you need to create files readable by"] # [doc = " legacy LZMA tools such as LZMA Utils 4.32.x. Moving to the .xz format"] # [doc = " (the `new_easy_encoder` function) is strongly recommended."] # [doc = ""] # [doc = " The valid action values for [`Stream::process`] are [`Action::Run`] and [`Action::Finish`]."] # [doc = " No flushing is supported, because the file format doesn't support it."] # [inline] pub fn new_lzma_encoder (options : & LzmaOptions) -> Result < Stream , Error > { let mut init = unsafe { Stream :: zeroed () } ; cvt (unsafe { liblzma_sys :: lzma_alone_encoder (& mut init . raw , & options . raw) }) ? ; Ok (init) } # [doc = " Initialize .xz Stream encoder using a custom filter chain"] # [doc = ""] # [doc = " This function is similar to `new_easy_encoder` but a custom filter chain"] # [doc = " is specified."] # [inline] pub fn new_stream_encoder (filters : & Filters , check : Check) -> Result < Stream , Error > { let mut init = unsafe { Stream :: zeroed () } ; cvt (unsafe { liblzma_sys :: lzma_stream_encoder (& mut init . raw , filters . inner . as_ptr () , check as liblzma_sys :: lzma_check ,) }) ? ; Ok (init) } # [doc = " Initialize an encoder stream using a custom filter chain."] # [inline] pub fn new_raw_encoder (filters : & Filters) -> Result < Stream , Error > { let mut init = unsafe { Self :: zeroed () } ; cvt (unsafe { liblzma_sys :: lzma_raw_encoder (& mut init . raw , filters . inner . as_ptr ()) }) ? ; Ok (init) } }
};
}
