// Generated macro for Context (struct)
macro_rules! Depcrate_pack_createContext {
() => {
// Module: crate::pack::create
// Provides: {"Context"}
// Dependencies: {}
# [doc = " A general purpose context for many operations provided here"] pub struct Context < W > { # [doc = " The way input objects should be handled"] pub expansion : ObjectExpansion , # [doc = " If `Some(threads)`, use this amount of `threads` to accelerate the counting phase at the cost of losing"] # [doc = " determinism as the order of objects during expansion changes with multiple threads unless no expansion is performed."] # [doc = " In the latter case, this flag has no effect."] # [doc = " If `None`, counting will only use one thread and thus yield the same sequence of objects in any case."] pub nondeterministic_thread_count : Option < usize > , # [doc = " If true, delta objects may refer to their base as reference, allowing it not to be included in the created back."] # [doc = " Otherwise these have to be recompressed in order to make the pack self-contained."] pub thin : bool , # [doc = " If set, don't use more than this amount of threads."] # [doc = " Otherwise, usually use as many threads as there are logical cores."] # [doc = " A value of 0 is interpreted as no-limit"] pub thread_limit : Option < usize > , # [doc = " If set, statistics about the operation will be written to the output stream."] pub statistics : Option < OutputFormat > , # [doc = " The size of the cache storing fully decoded delta objects. This can greatly speed up pack decoding by reducing the length of delta"] # [doc = " chains. Note that caches also incur a cost and poorly used caches may reduce overall performance."] # [doc = " This is a total, shared among all threads if `thread_limit` permits."] # [doc = ""] # [doc = " If 0, the cache is disabled entirely."] pub pack_cache_size_in_bytes : usize , # [doc = " The size of the cache to store full objects by their ID, bypassing any lookup in the object database."] # [doc = " Note that caches also incur a cost and poorly used caches may reduce overall performance."] # [doc = ""] # [doc = " This is a total, shared among all threads if `thread_limit` permits."] # [doc = " Only used when known to be effective, namely when `expansion == ObjectExpansion::TreeDiff`."] pub object_cache_size_in_bytes : usize , # [doc = " The output stream for use of additional information"] pub out : W , }
};
}
