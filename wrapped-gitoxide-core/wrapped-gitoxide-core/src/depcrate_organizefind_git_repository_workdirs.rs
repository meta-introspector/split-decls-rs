// Generated macro for find_git_repository_workdirs (function)
macro_rules! Depcrate_organizefind_git_repository_workdirs {
() => {
// Module: crate::organize
// Provides: {"find_git_repository_workdirs"}
// Dependencies: {}
pub fn find_git_repository_workdirs (root : impl AsRef < Path > , mut progress : impl Progress , debug : bool , threads : Option < usize > ,) -> impl Iterator < Item = (PathBuf , gix :: repository :: Kind) > { progress . init (None , progress :: count ("filesystem items")) ; fn is_repository (path : & Path) -> Option < gix :: repository :: Kind > { if path . file_name () != Some (OsStr :: new (".git")) && path . extension () != Some (OsStr :: new ("git")) { return None ; } if path . is_dir () { if path . join ("HEAD") . is_file () && path . join ("config") . is_file () { gix :: discover :: is_git (path) . ok () . map (Into :: into) } else { None } } else { Some (gix :: repository :: Kind :: WorkTree { is_linked : true }) } } fn into_workdir (git_dir : PathBuf , kind : & gix :: repository :: Kind) -> PathBuf { if matches ! (kind , gix :: repository :: Kind :: Bare) || gix :: discover :: is_bare (& git_dir) { git_dir } else { git_dir . parent () . expect ("git is never in the root") . to_owned () } } # [derive (Debug , Default)] struct State { kind : Option < gix :: repository :: Kind > , } let walk = jwalk :: WalkDirGeneric :: < (() , State) > :: new (root) . follow_links (false) . sort (true) . skip_hidden (false) . parallelism (jwalk :: Parallelism :: RayonNewPool (threads . unwrap_or (0))) ; walk . process_read_dir (move | _depth , path , _read_dir_state , siblings | { if debug { eprintln ! ("{}" , path . display ()) ; } let mut found_any_repo = false ; let mut found_bare_repo = false ; for entry in siblings . iter_mut () . flatten () { let path = entry . path () ; if let Some (kind) = is_repository (& path) { let is_bare = kind . is_bare () ; entry . client_state = State { kind : kind . into () } ; entry . read_children_path = None ; found_any_repo = true ; found_bare_repo = is_bare ; } } if found_any_repo && ! found_bare_repo { siblings . retain (| e | e . as_ref () . map (| e | e . client_state . kind . is_some ()) . unwrap_or (false)) ; } }) . into_iter () . inspect (move | _ | progress . inc ()) . filter_map (Result :: ok) . filter_map (| mut e | { e . client_state . kind . take () . map (| kind | (into_workdir (e . path () , & kind) , kind)) }) }
};
}
