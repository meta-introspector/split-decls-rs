// Generated macro for TrackCompletion (trait)
macro_rules! Depcrate_load_completionTrackCompletion {
() => {
// Module: crate::load::completion
// Provides: {"TrackCompletion"}
// Dependencies: {}
# [doc = " Attaches `H`-typed completion tracker to `V` typed values."] # [doc = ""] # [doc = " Handles (of type `H`) are intended to be RAII guards that primarily implement [`Drop`] and update"] # [doc = " load metric state as they are dropped. This trait allows implementors to \"forward\" the handle"] # [doc = " to later parts of the request-handling pipeline, so that the handle is only dropped when the"] # [doc = " request has truly completed."] # [doc = ""] # [doc = " This utility allows load metrics to have a protocol-agnostic means to track streams past their"] # [doc = " initial response future. For example, if `V` represents an HTTP response type, an"] # [doc = " implementation could add `H`-typed handles to each response's extensions to detect when all the"] # [doc = " response's extensions have been dropped."] # [doc = ""] # [doc = " A base `impl<H, V> TrackCompletion<H, V> for CompleteOnResponse` is provided to drop the handle"] # [doc = " once the response future is resolved. This is appropriate when a response is discrete and"] # [doc = " cannot comprise multiple messages."] # [doc = ""] # [doc = " In many cases, the `Output` type is simply `V`. However, [`TrackCompletion`] may alter the type"] # [doc = " in order to instrument it appropriately. For example, an HTTP [`TrackCompletion`] may modify"] # [doc = " the body type: so a [`TrackCompletion`] that takes values of type"] # [doc = " [`http::Response<A>`][response] may output values of type [`http::Response<B>`][response]."] # [doc = ""] # [doc = " [response]: https://docs.rs/http/latest/http/response/struct.Response.html"] pub trait TrackCompletion < H , V > : Clone { # [doc = " The instrumented value type."] type Output ; # [doc = " Attaches a `H`-typed handle to a `V`-typed value."] fn track_completion (& self , handle : H , value : V) -> Self :: Output ; }
};
}
