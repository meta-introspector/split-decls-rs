// Generated macro for future_service (function)
macro_rules! Depcrate_util_future_servicefuture_service {
() => {
// Module: crate::util::future_service
// Provides: {"future_service"}
// Dependencies: {}
# [doc = " Returns a new [`FutureService`] for the given future."] # [doc = ""] # [doc = " A [`FutureService`] allows you to treat a future that resolves to a service as a service. This"] # [doc = " can be useful for services that are created asynchronously."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " use tower::{service_fn, Service, ServiceExt};"] # [doc = " use tower::util::future_service;"] # [doc = " use std::convert::Infallible;"] # [doc = ""] # [doc = " # fn main() {"] # [doc = " # async {"] # [doc = " // A future which outputs a type implementing `Service`."] # [doc = " let future_of_a_service = async {"] # [doc = "     let svc = service_fn(|_req: ()| async { Ok::<_, Infallible>(\"ok\") });"] # [doc = "     Ok::<_, Infallible>(svc)"] # [doc = " };"] # [doc = ""] # [doc = " // Wrap the future with a `FutureService`, allowing it to be used"] # [doc = " // as a service without awaiting the future's completion:"] # [doc = " let mut svc = future_service(Box::pin(future_of_a_service));"] # [doc = ""] # [doc = " // Now, when we wait for the service to become ready, it will"] # [doc = " // drive the future to completion internally."] # [doc = " let svc = svc.ready().await.unwrap();"] # [doc = " let res = svc.call(()).await.unwrap();"] # [doc = " # };"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " # Regarding the [`Unpin`] bound"] # [doc = ""] # [doc = " The [`Unpin`] bound on `F` is necessary because the future will be polled in"] # [doc = " [`Service::poll_ready`] which doesn't have a pinned receiver (it takes `&mut self` and not `self:"] # [doc = " Pin<&mut Self>`). So we cannot put the future into a `Pin` without requiring `Unpin`."] # [doc = ""] # [doc = " This will most likely come up if you're calling `future_service` with an async block. In that"] # [doc = " case you can use `Box::pin(async { ... })` as shown in the example."] pub fn future_service < F , S , R , E > (future : F) -> FutureService < F , S > where F : Future < Output = Result < S , E > > + Unpin , S : Service < R , Error = E > , { FutureService :: new (future) }
};
}
