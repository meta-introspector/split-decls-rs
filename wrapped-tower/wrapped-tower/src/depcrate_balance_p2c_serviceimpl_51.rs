// Generated macro for impl_51 (impl)
macro_rules! Depcrate_balance_p2c_serviceimpl_51 {
() => {
// Module: crate::balance::p2c::service
// Provides: {"impl_51"}
// Dependencies: {}
impl < D , Req > Balance < D , Req > where D : Discover + Unpin , D :: Key : Hash + Clone , D :: Error : Into < crate :: BoxError > , D :: Service : Service < Req > + Load , < D :: Service as Load > :: Metric : std :: fmt :: Debug , < D :: Service as Service < Req > > :: Error : Into < crate :: BoxError > , { # [doc = " Polls `discover` for updates, adding new items to `not_ready`."] # [doc = ""] # [doc = " Removals may alter the order of either `ready` or `not_ready`."] fn update_pending_from_discover (& mut self , cx : & mut Context < '_ > ,) -> Poll < Option < Result < () , error :: Discover > > > { debug ! ("updating from discover") ; loop { match ready ! (Pin :: new (& mut self . discover) . poll_discover (cx)) . transpose () . map_err (| e | error :: Discover (e . into ())) ? { None => return Poll :: Ready (None) , Some (Change :: Remove (key)) => { trace ! ("remove") ; self . services . evict (& key) ; } Some (Change :: Insert (key , svc)) => { trace ! ("insert") ; self . services . push (key , svc) ; } } } } fn promote_pending_to_ready (& mut self , cx : & mut Context < '_ >) { loop { match self . services . poll_pending (cx) { Poll :: Ready (Ok (())) => { debug_assert_eq ! (self . services . pending_len () , 0) ; break ; } Poll :: Pending => { debug_assert ! (self . services . pending_len () > 0) ; break ; } Poll :: Ready (Err (error)) => { debug ! (% error , "dropping failed endpoint") ; } } } trace ! (ready = % self . services . ready_len () , pending = % self . services . pending_len () , "poll_unready") ; } # [doc = " Performs P2C on inner services to find a suitable endpoint."] fn p2c_ready_index (& mut self) -> Option < usize > { match self . services . ready_len () { 0 => None , 1 => Some (0) , len => { let [aidx , bidx] = sample_floyd2 (& mut self . rng , len as u64) ; debug_assert_ne ! (aidx , bidx , "random indices must be distinct") ; let aload = self . ready_index_load (aidx as usize) ; let bload = self . ready_index_load (bidx as usize) ; let chosen = if aload <= bload { aidx } else { bidx } ; trace ! (a . index = aidx , a . load = ? aload , b . index = bidx , b . load = ? bload , chosen = if chosen == aidx { "a" } else { "b" } , "p2c" ,) ; Some (chosen as usize) } } } # [doc = " Accesses a ready endpoint by index and returns its current load."] fn ready_index_load (& self , index : usize) -> < D :: Service as Load > :: Metric { let (_ , svc) = self . services . get_ready_index (index) . expect ("invalid index") ; svc . load () } }
};
}
