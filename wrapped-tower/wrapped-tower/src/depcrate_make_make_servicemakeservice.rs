// Generated macro for MakeService (trait)
macro_rules! Depcrate_make_make_serviceMakeService {
() => {
// Module: crate::make::make_service
// Provides: {"MakeService"}
// Dependencies: {}
# [doc = " Creates new [`Service`] values."] # [doc = ""] # [doc = " Acts as a service factory. This is useful for cases where new [`Service`]"] # [doc = " values must be produced. One case is a TCP server listener. The listener"] # [doc = " accepts new TCP streams, obtains a new [`Service`] value using the"] # [doc = " [`MakeService`] trait, and uses that new [`Service`] value to process inbound"] # [doc = " requests on that new TCP stream."] # [doc = ""] # [doc = " This is essentially a trait alias for a [`Service`] of [`Service`]s."] pub trait MakeService < Target , Request > : Sealed < (Target , Request) > { # [doc = " Responses given by the service"] type Response ; # [doc = " Errors produced by the service"] type Error ; # [doc = " The [`Service`] value created by this factory"] type Service : Service < Request , Response = Self :: Response , Error = Self :: Error > ; # [doc = " Errors produced while building a service."] type MakeError ; # [doc = " The future of the [`Service`] instance."] type Future : Future < Output = Result < Self :: Service , Self :: MakeError > > ; # [doc = " Returns [`Poll::Ready`] when the factory is able to create more services."] # [doc = ""] # [doc = " If the service is at capacity, then [`Poll::Pending`] is returned and the task"] # [doc = " is notified when the service becomes ready again. This function is"] # [doc = " expected to be called while on a task."] # [doc = ""] # [doc = " [`Poll::Ready`]: std::task::Poll::Ready"] # [doc = " [`Poll::Pending`]: std::task::Poll::Pending"] fn poll_ready (& mut self , cx : & mut Context < '_ >) -> Poll < Result < () , Self :: MakeError > > ; # [doc = " Create and return a new service value asynchronously."] fn make_service (& mut self , target : Target) -> Self :: Future ; # [doc = " Consume this [`MakeService`] and convert it into a [`Service`]."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " use std::convert::Infallible;"] # [doc = " use tower::Service;"] # [doc = " use tower::make::MakeService;"] # [doc = " use tower::service_fn;"] # [doc = ""] # [doc = " # fn main() {"] # [doc = " # async {"] # [doc = " // A `MakeService`"] # [doc = " let make_service = service_fn(|make_req: ()| async {"] # [doc = "     Ok::<_, Infallible>(service_fn(|req: String| async {"] # [doc = "         Ok::<_, Infallible>(req)"] # [doc = "     }))"] # [doc = " });"] # [doc = ""] # [doc = " // Convert the `MakeService` into a `Service`"] # [doc = " let mut svc = make_service.into_service();"] # [doc = ""] # [doc = " // Make a new service"] # [doc = " let mut new_svc = svc.call(()).await.unwrap();"] # [doc = ""] # [doc = " // Call the service"] # [doc = " let res = new_svc.call(\"foo\".to_string()).await.unwrap();"] # [doc = " # };"] # [doc = " # }"] # [doc = " ```"] fn into_service (self) -> IntoService < Self , Request > where Self : Sized , { IntoService { make : self , _marker : PhantomData , } } # [doc = " Convert this [`MakeService`] into a [`Service`] without consuming the original [`MakeService`]."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " use std::convert::Infallible;"] # [doc = " use tower::Service;"] # [doc = " use tower::make::MakeService;"] # [doc = " use tower::service_fn;"] # [doc = ""] # [doc = " # fn main() {"] # [doc = " # async {"] # [doc = " // A `MakeService`"] # [doc = " let mut make_service = service_fn(|make_req: ()| async {"] # [doc = "     Ok::<_, Infallible>(service_fn(|req: String| async {"] # [doc = "         Ok::<_, Infallible>(req)"] # [doc = "     }))"] # [doc = " });"] # [doc = ""] # [doc = " // Convert the `MakeService` into a `Service`"] # [doc = " let mut svc = make_service.as_service();"] # [doc = ""] # [doc = " // Make a new service"] # [doc = " let mut new_svc = svc.call(()).await.unwrap();"] # [doc = ""] # [doc = " // Call the service"] # [doc = " let res = new_svc.call(\"foo\".to_string()).await.unwrap();"] # [doc = ""] # [doc = " // The original `MakeService` is still accessible"] # [doc = " let new_svc = make_service.make_service(()).await.unwrap();"] # [doc = " # };"] # [doc = " # }"] # [doc = " ```"] fn as_service (& mut self) -> AsService < '_ , Self , Request > where Self : Sized , { AsService { make : self , _marker : PhantomData , } } }
};
}
