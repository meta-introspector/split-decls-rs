// Generated macro for impl_112 (impl)
macro_rules! Depcrate_buffer_serviceimpl_112 {
() => {
// Module: crate::buffer::service
// Provides: {"impl_112"}
// Dependencies: {}
impl < Req , F > Buffer < Req , F > where F : 'static , { # [doc = " Creates a new [`Buffer`] wrapping `service`."] # [doc = ""] # [doc = " `bound` gives the maximal number of requests that can be queued for the service before"] # [doc = " backpressure is applied to callers."] # [doc = ""] # [doc = " The default Tokio executor is used to run the given service, which means that this method"] # [doc = " must be called while on the Tokio runtime."] # [doc = ""] # [doc = " # A note on choosing a `bound`"] # [doc = ""] # [doc = " When [`Buffer`]'s implementation of [`poll_ready`] returns [`Poll::Ready`], it reserves a"] # [doc = " slot in the channel for the forthcoming [`call`]. However, if this call doesn't arrive,"] # [doc = " this reserved slot may be held up for a long time. As a result, it's advisable to set"] # [doc = " `bound` to be at least the maximum number of concurrent requests the [`Buffer`] will see."] # [doc = " If you do not, all the slots in the buffer may be held up by futures that have just called"] # [doc = " [`poll_ready`] but will not issue a [`call`], which prevents other senders from issuing new"] # [doc = " requests."] # [doc = ""] # [doc = " [`Poll::Ready`]: std::task::Poll::Ready"] # [doc = " [`call`]: crate::Service::call"] # [doc = " [`poll_ready`]: crate::Service::poll_ready"] pub fn new < S > (service : S , bound : usize) -> Self where S : Service < Req , Future = F > + Send + 'static , F : Send , S :: Error : Into < crate :: BoxError > + Send + Sync , Req : Send + 'static , { let (service , worker) = Self :: pair (service , bound) ; tokio :: spawn (worker) ; service } # [doc = " Creates a new [`Buffer`] wrapping `service`, but returns the background worker."] # [doc = ""] # [doc = " This is useful if you do not want to spawn directly onto the tokio runtime"] # [doc = " but instead want to use your own executor. This will return the [`Buffer`] and"] # [doc = " the background `Worker` that you can then spawn."] pub fn pair < S > (service : S , bound : usize) -> (Self , Worker < S , Req >) where S : Service < Req , Future = F > + Send + 'static , F : Send , S :: Error : Into < crate :: BoxError > + Send + Sync , Req : Send + 'static , { let (tx , rx) = mpsc :: channel (bound) ; let (handle , worker) = Worker :: new (service , rx) ; let buffer = Self { tx : PollSender :: new (tx) , handle , } ; (buffer , worker) } fn get_worker_error (& self) -> crate :: BoxError { self . handle . get_error_on_closed () } }
};
}
