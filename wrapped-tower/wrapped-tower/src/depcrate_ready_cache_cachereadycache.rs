// Generated macro for ReadyCache (struct)
macro_rules! Depcrate_ready_cache_cacheReadyCache {
() => {
// Module: crate::ready_cache::cache
// Provides: {"ReadyCache"}
// Dependencies: {}
# [doc = " Drives readiness over a set of services."] # [doc = ""] # [doc = " The cache maintains two internal data structures:"] # [doc = ""] # [doc = " * a set of _pending_ services that have not yet become ready; and"] # [doc = " * a set of _ready_ services that have previously polled ready."] # [doc = ""] # [doc = " As each `S` typed [`Service`] is added to the cache via [`ReadyCache::push`], it"] # [doc = " is added to the _pending set_. As [`ReadyCache::poll_pending`] is invoked,"] # [doc = " pending services are polled and added to the _ready set_."] # [doc = ""] # [doc = " [`ReadyCache::call_ready`] (or [`ReadyCache::call_ready_index`]) dispatches a"] # [doc = " request to the specified service, but panics if the specified service is not"] # [doc = " in the ready set. The `ReadyCache::check_*` functions can be used to ensure"] # [doc = " that a service is ready before dispatching a request."] # [doc = ""] # [doc = " The ready set can hold services for an arbitrarily long time. During this"] # [doc = " time, the runtime may process events that invalidate that ready state (for"] # [doc = " instance, if a keepalive detects a lost connection). In such cases, callers"] # [doc = " should use [`ReadyCache::check_ready`] (or [`ReadyCache::check_ready_index`])"] # [doc = " immediately before dispatching a request to ensure that the service has not"] # [doc = " become unavailable."] # [doc = ""] # [doc = " Once `ReadyCache::call_ready*` is invoked, the service is placed back into"] # [doc = " the _pending_ set to be driven to readiness again."] # [doc = ""] # [doc = " When `ReadyCache::check_ready*` returns `false`, it indicates that the"] # [doc = " specified service is _not_ ready. If an error is returned, this indicates that"] # [doc = " the server failed and has been removed from the cache entirely."] # [doc = ""] # [doc = " [`ReadyCache::evict`] can be used to remove a service from the cache (by key),"] # [doc = " though the service may not be dropped (if it is currently pending) until"] # [doc = " [`ReadyCache::poll_pending`] is invoked."] # [doc = ""] # [doc = " Note that the by-index accessors are provided to support use cases (like"] # [doc = " power-of-two-choices load balancing) where the caller does not care to keep"] # [doc = " track of each service's key. Instead, it needs only to access _some_ ready"] # [doc = " service. In such a case, it should be noted that calls to"] # [doc = " [`ReadyCache::poll_pending`] and [`ReadyCache::evict`] may perturb the order of"] # [doc = " the ready set, so any cached indexes should be discarded after such a call."] pub struct ReadyCache < K , S , Req > where K : Eq + Hash , { # [doc = " A stream of services that are not yet ready."] pending : FuturesUnordered < Pending < K , S , Req > > , # [doc = " An index of cancelation handles for pending streams."] pending_cancel_txs : IndexMap < K , CancelTx > , # [doc = " Services that have previously become ready. Readiness can become stale,"] # [doc = " so a given service should be polled immediately before use."] # [doc = ""] # [doc = " The cancelation oneshot is preserved (though unused) while the service is"] # [doc = " ready so that it need not be reallocated each time a request is"] # [doc = " dispatched."] ready : IndexMap < K , (S , CancelPair) > , }
};
}
