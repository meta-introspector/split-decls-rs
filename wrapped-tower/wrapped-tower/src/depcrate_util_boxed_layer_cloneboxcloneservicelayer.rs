// Generated macro for BoxCloneServiceLayer (struct)
macro_rules! Depcrate_util_boxed_layer_cloneBoxCloneServiceLayer {
() => {
// Module: crate::util::boxed::layer_clone
// Provides: {"BoxCloneServiceLayer"}
// Dependencies: {}
# [doc = " A [`Clone`] + [`Send`] boxed [`Layer`]."] # [doc = ""] # [doc = " [`BoxCloneServiceLayer`] turns a layer into a trait object, allowing both the [`Layer`] itself"] # [doc = " and the output [`Service`] to be dynamic, while having consistent types."] # [doc = ""] # [doc = " This [`Layer`] produces [`BoxCloneService`] instances erasing the type of the"] # [doc = " [`Service`] produced by the wrapped [`Layer`]."] # [doc = ""] # [doc = " This is similar to [`BoxLayer`](super::BoxLayer) except the layer and resulting"] # [doc = " service implements [`Clone`]."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " `BoxCloneServiceLayer` can, for example, be useful to create layers dynamically that otherwise wouldn't have"] # [doc = " the same types, when the underlying service must be clone (for example, when building a MakeService)"] # [doc = " In this example, we include a [`Timeout`] layer only if an environment variable is set. We can use"] # [doc = " `BoxCloneService` to return a consistent type regardless of runtime configuration:"] # [doc = ""] # [doc = " ```"] # [doc = " use std::time::Duration;"] # [doc = " use tower::{Service, ServiceBuilder, BoxError};"] # [doc = " use tower::util::{BoxCloneServiceLayer, BoxCloneService};"] # [doc = ""] # [doc = " #"] # [doc = " # struct Request;"] # [doc = " # struct Response;"] # [doc = " # impl Response {"] # [doc = " #     fn new() -> Self { Self }"] # [doc = " # }"] # [doc = ""] # [doc = " fn common_layer<S, T>() -> BoxCloneServiceLayer<S, T, S::Response, BoxError>"] # [doc = " where"] # [doc = "     S: Service<T> + Clone + Send + 'static,"] # [doc = "     S::Future: Send + 'static,"] # [doc = "     S::Error: Into<BoxError> + 'static,"] # [doc = " {"] # [doc = "     let builder = ServiceBuilder::new()"] # [doc = "         .concurrency_limit(100);"] # [doc = ""] # [doc = "     if std::env::var(\"SET_TIMEOUT\").is_ok() {"] # [doc = "         let layer = builder"] # [doc = "             .timeout(Duration::from_secs(30))"] # [doc = "             .into_inner();"] # [doc = ""] # [doc = "         BoxCloneServiceLayer::new(layer)"] # [doc = "     } else {"] # [doc = "         let layer = builder"] # [doc = "             .map_err(Into::into)"] # [doc = "             .into_inner();"] # [doc = ""] # [doc = "         BoxCloneServiceLayer::new(layer)"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " // We can clone the layer (this is true of BoxLayer as well)"] # [doc = " let boxed_clone_layer = common_layer();"] # [doc = ""] # [doc = " let cloned_layer = boxed_clone_layer.clone();"] # [doc = ""] # [doc = " // Using the `BoxCloneServiceLayer` we can create a `BoxCloneService`"] # [doc = " let service: BoxCloneService<Request, Response, BoxError> = ServiceBuilder::new().layer(boxed_clone_layer)"] # [doc = "      .service_fn(|req: Request| async {"] # [doc = "         Ok::<_, BoxError>(Response::new())"] # [doc = "     });"] # [doc = ""] # [doc = " # let service = assert_service(service);"] # [doc = ""] # [doc = " // And we can still clone the service"] # [doc = " let cloned_service = service.clone();"] # [doc = " #"] # [doc = " # fn assert_service<S, R>(svc: S) -> S"] # [doc = " # where S: Service<R> { svc }"] # [doc = ""] # [doc = " ```"] # [doc = ""] # [doc = " [`Layer`]: tower_layer::Layer"] # [doc = " [`Service`]: tower_service::Service"] # [doc = " [`BoxService`]: super::BoxService"] # [doc = " [`Timeout`]: crate::timeout"] pub struct BoxCloneServiceLayer < In , T , U , E > { boxed : Arc < dyn Layer < In , Service = BoxCloneService < T , U , E > > + Send + Sync + 'static > , }
};
}
