// Generated macro for use_1 (use)
macro_rules! Depcrateuse_1 {
() => {
// Module: crate
// Provides: {"use_1"}
// Dependencies: {}
# [doc = " A Ratatui example that demonstrates how to handle panics in your application."] # [doc = ""] # [doc = " Prior to Ratatui 0.28.1, a panic hook had to be manually set up to ensure that the terminal"] # [doc = " was reset when a panic occurred. This was necessary because a panic would interrupt the"] # [doc = " normal control flow and leave the terminal in a distorted state."] # [doc = ""] # [doc = " Starting with Ratatui 0.28.1, the panic hook is automatically set up by the new"] # [doc = " `ratatui::init` function, so you no longer need to manually set up the panic hook. This"] # [doc = " example now demonstrates how the panic hook acts when it is enabled by default."] # [doc = ""] # [doc = " When exiting normally or when handling `Result::Err`, we can reset the terminal manually at"] # [doc = " the end of `main` just before we print the error."] # [doc = ""] # [doc = " Because a panic interrupts the normal control flow, manually resetting the terminal at the"] # [doc = " end of `main` won't do us any good. Instead, we need to make sure to set up a panic hook"] # [doc = " that first resets the terminal before handling the panic. This both reuses the standard"] # [doc = " panic hook to ensure a consistent panic handling UX and properly resets the terminal to not"] # [doc = " distort the output."] # [doc = ""] # [doc = " That's why this example is set up to show both situations, with and without the panic hook,"] # [doc = " to see the difference."] # [doc = ""] # [doc = " For more information on how to set this up manually, see the [Color Eyre recipe] in the"] # [doc = " Ratatui website."] # [doc = ""] # [doc = " This example runs with the Ratatui library code in the branch that you are currently"] # [doc = " reading. See the [`latest`] branch for the code which works with the most recent Ratatui"] # [doc = " release."] # [doc = ""] # [doc = " [`latest`]: https://github.com/ratatui/ratatui/tree/latest"] # [doc = " [Color Eyre recipe]: https://ratatui.rs/recipes/apps/color-eyre"] use color_eyre :: { Result , eyre :: bail } ;
};
}
