// Generated macro for Opts (struct)
macro_rules! DepcrateOpts {
() => {
// Module: crate
// Provides: {"Opts"}
// Dependencies: {}
# [doc = " Tool to run tests that exercise the `wit-bindgen` bindings generator."] # [doc = ""] # [doc = " This tool is used to (a) generate bindings for a target language, (b)"] # [doc = " compile the bindings and source code to a wasm component, (c) compose a"] # [doc = " \"runner\" and a \"test\" component together, and (d) execute this component to"] # [doc = " ensure that it passes. This process is guided by filesystem structure which"] # [doc = " must adhere to some conventions."] # [doc = ""] # [doc = " * Tests are located in any directory that contains a `test.wit` description"] # [doc = "   of the WIT being tested. The `<TEST>` argument to this command is walked"] # [doc = "   recursively to find `test.wit` files."] # [doc = ""] # [doc = " * The `test.wit` file must have a `runner` world and a `test` world. The"] # [doc = "   \"runner\" should import interfaces that are exported by \"test\"."] # [doc = ""] # [doc = " * Adjacent to `test.wit` should be a number of `runner*.*` files. There is"] # [doc = "   one runner per source language, for example `runner.rs` and `runner.c`."] # [doc = "   These are source files for the `runner` world. Source files can start with"] # [doc = "   `//@ ...` comments to deserialize into `config::RuntimeTestConfig`,"] # [doc = "   currently that supports:"] # [doc = ""] # [doc = "   ```text"] # [doc = "   //@ args = ['--arguments', 'to', '--the', 'bindings', '--generator']"] # [doc = "   ```"] # [doc = ""] # [doc = "   or"] # [doc = ""] # [doc = "   ```text"] # [doc = "   //@ args = '--arguments to --the bindings --generator'"] # [doc = "   ```"] # [doc = ""] # [doc = " * Adjacent to `test.wit` should also be a number of `test*.*` files. Like"] # [doc = "   runners there is one per source language. Note that you can have multiple"] # [doc = "   implementations of tests in the same language too, for example"] # [doc = "   `test-foo.rs` and `test-bar.rs`. All tests must export the same `test`"] # [doc = "   world from `test.wit`, however."] # [doc = ""] # [doc = " This tool will discover `test.wit` files, discover runners/tests, and then"] # [doc = " compile everything and run the combinatorial matrix of runners against"] # [doc = " tests. It's expected that each `runner.*` and `test.*` perform the same"] # [doc = " functionality and only differ in source language."] # [derive (Default , Debug , Clone , Parser)] pub struct Opts { # [doc = " Directory containing the test being run or all tests being run."] test : Vec < PathBuf > , # [doc = " Path to where binary artifacts for tests are stored."] # [clap (long , value_name = "PATH")] artifacts : PathBuf , # [doc = " Optional filter to use on test names to only run some tests."] # [doc = ""] # [doc = " This is a regular expression defined by the `regex` Rust crate."] # [clap (short , long , value_name = "REGEX")] filter : Option < regex :: Regex > , # [doc = " The executable or script used to execute a fully composed test case."] # [clap (long , default_value = "wasmtime")] runner : std :: ffi :: OsString , # [clap (flatten)] rust : rust :: RustOpts , # [clap (flatten)] c : c :: COpts , # [clap (flatten)] custom : custom :: CustomOpts , # [doc = " Whether or not the calling process's stderr is inherited into child"] # [doc = " processes."] # [doc = ""] # [doc = " This helps preserving color in compiler error messages but can also"] # [doc = " jumble up output if there are multiple errors."] # [clap (short , long)] inherit_stderr : bool , # [doc = " Configuration of which languages are tested."] # [doc = ""] # [doc = " Passing `--lang rust` will only test Rust for example."] # [clap (short , long , required = true , value_delimiter = ',')] languages : Vec < String > , }
};
}
