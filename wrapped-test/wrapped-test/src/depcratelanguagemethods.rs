// Generated macro for LanguageMethods (trait)
macro_rules! DepcrateLanguageMethods {
() => {
// Module: crate
// Provides: {"LanguageMethods"}
// Dependencies: {}
# [doc = " Helper trait for each language to implement which encapsulates"] # [doc = " language-specific logic."] trait LanguageMethods { # [doc = " Display name for this language, used in filenames."] fn display (& self) -> & str ; # [doc = " Returns the prefix that this language uses to annotate configuration in"] # [doc = " the top of source files."] # [doc = ""] # [doc = " This should be the language's line-comment syntax followed by `@`, e.g."] # [doc = " `//@` for Rust or `;;@` for WebAssembly Text."] fn comment_prefix_for_test_config (& self) -> Option < & str > ; # [doc = " Returns the extra permutations, if any, of arguments to use with codegen"] # [doc = " tests."] # [doc = ""] # [doc = " This is used to run all codegen tests with a variety of bindings"] # [doc = " generator options. The first element in the tuple is a descriptive"] # [doc = " string that should be unique (used in file names) and the second elemtn"] # [doc = " is the list of arguments for that variant to pass to the bindings"] # [doc = " generator."] fn codegen_test_variants (& self) -> & [(& str , & [& str])] { & [] } # [doc = " Performs any one-time preparation necessary for this language, such as"] # [doc = " downloading or caching dependencies."] fn prepare (& self , runner : & mut Runner < '_ >) -> Result < () > ; # [doc = " Add some files to the generated directory _before_ calling bindgen"] fn generate_bindings_prepare (& self , _runner : & Runner < '_ > , _bindgen : & Bindgen , _dir : & Path ,) -> Result < () > { Ok (()) } # [doc = " Generates bindings for `component` into `dir`."] # [doc = ""] # [doc = " Runs `wit-bindgen` in aa subprocess to catch failures such as panics."] fn generate_bindings (& self , runner : & Runner < '_ > , bindgen : & Bindgen , dir : & Path) -> Result < () > { let name = match self . bindgen_name () { Some (name) => name , None => return Ok (()) , } ; self . generate_bindings_prepare (runner , bindgen , dir) ? ; let mut cmd = Command :: new (runner . wit_bindgen) ; cmd . arg (name) . arg (& bindgen . wit_path) . arg ("--world") . arg (format ! ("%{}" , bindgen . world)) . arg ("--out-dir") . arg (dir) ; match bindgen . wit_config . default_bindgen_args { Some (true) | None => { for arg in self . default_bindgen_args () { cmd . arg (arg) ; } } Some (false) => { } } for arg in bindgen . args . iter () { cmd . arg (arg) ; } runner . run_command (& mut cmd) } # [doc = " Returns the default set of arguments that will be passed to"] # [doc = " `wit-bindgen`."] # [doc = ""] # [doc = " Defaults to empty, but each language can override it."] fn default_bindgen_args (& self) -> & [& str] { & [] } # [doc = " Same as `default_bindgen_args` but specifically applied during codegen"] # [doc = " tests, such as generating stub impls by default."] fn default_bindgen_args_for_codegen (& self) -> & [& str] { & [] } # [doc = " Returns the name of this bindings generator when passed to"] # [doc = " `wit-bindgen`."] # [doc = ""] # [doc = " By default this is `Some(self.display())`, but it can be overridden if"] # [doc = " necessary. Returning `None` here means that no bindings generator is"] # [doc = " supported."] fn bindgen_name (& self) -> Option < & str > { Some (self . display ()) } # [doc = " Performs compilation as specified by `compile`."] fn compile (& self , runner : & Runner < '_ > , compile : & Compile) -> Result < () > ; # [doc = " Returns whether this language is supposed to fail this codegen tests"] # [doc = " given the `config` and `args` for the test."] fn should_fail_verify (& self , name : & str , config : & config :: WitConfig , args : & [String]) -> bool ; # [doc = " Performs a \"check\" or a verify that the generated bindings described by"] # [doc = " `Verify` are indeed valid."] fn verify (& self , runner : & Runner < '_ > , verify : & Verify) -> Result < () > ; }
};
}
