// Generated macro for mut_array_refs (macro)
macro_rules! Depcratemut_array_refs {
() => {
// Module: crate
// Provides: {"mut_array_refs"}
// Dependencies: {}
# [doc = " You can use `mut_array_refs` to generate a series of mutable array"] # [doc = " references to an input mutable array reference.  The idea is if"] # [doc = " you want to break an array into a series of contiguous and"] # [doc = " non-overlapping mutable array references.  Like `array_refs!`,"] # [doc = " `mut_array_refs!` is a bit funny in that it insists on slicing up"] # [doc = " the *entire* array.  This is intentional, as I find it handy to"] # [doc = " make me ensure that my sub-arrays add up to the entire array."] # [doc = " This macro will *never* panic, since the sizes are all checked at"] # [doc = " compile time."] # [doc = ""] # [doc = " Note that unlike `array_mut_ref!`, `mut_array_refs` *requires*"] # [doc = " that the first argument be a mutable array reference.  The"] # [doc = " following arguments are the lengths of each subarray you wish a"] # [doc = " reference to.  The total of these arguments *must* equal the size"] # [doc = " of the array itself.  Also note that this macro allows you to take"] # [doc = " out multiple mutable references to a single object, which is both"] # [doc = " weird and powerful."] # [doc = ""] # [doc = " ```"] # [doc = " #[macro_use]"] # [doc = " extern crate arrayref;"] # [doc = ""] # [doc = " fn write_u16(bytes: &mut [u8; 2], num: u16) {"] # [doc = "      bytes[0] = num as u8;"] # [doc = "      bytes[1] = (num >> 8) as u8;"] # [doc = " }"] # [doc = " fn write_u32(bytes: &mut [u8; 4], num: u32) {"] # [doc = "      bytes[0] = num as u8;"] # [doc = "      bytes[1] = (num >> 8) as u8; // this is buggy to save space..."] # [doc = " }"] # [doc = " // ..."] # [doc = " # fn main() {"] # [doc = " let mut data = [0,1,2,3,4,0,6,7];"] # [doc = " let (a,b,c) = mut_array_refs![&mut data,2,2,4];"] # [doc = " // let's write out some nice prime numbers!"] # [doc = " write_u16(a, 37);"] # [doc = " write_u16(b, 73);"] # [doc = " write_u32(c, 137); // approximate inverse of the fine structure constant!"] # [doc = " # }"] # [doc = " ```"] # [macro_export] macro_rules ! mut_array_refs { ($ arr : expr , $ ($ pre : expr) ,* ; .. ; $ ($ post : expr) ,*) => { { { use core :: slice ; # [inline] # [allow (unused_assignments)] # [allow (clippy :: eval_order_dependence)] unsafe fn as_arrays < T > (a : & mut [T]) -> ($ (& mut [T ; $ pre] ,) * & mut [T] , $ (& mut [T ; $ post] ,) *) { const MIN_LEN : usize = 0usize $ (. saturating_add ($ pre)) * $ (. saturating_add ($ post)) *; assert ! (MIN_LEN < usize :: MAX , "Your arrays are too big, are you trying to hack yourself?!") ; let var_len = a . len () - MIN_LEN ; assert ! (a . len () >= MIN_LEN) ; let mut p = a . as_mut_ptr () ; ($ ({ let aref = & mut * (p as * mut [T ; $ pre]) ; p = p . add ($ pre) ; aref } ,) * { let sl = slice :: from_raw_parts_mut (p as * mut T , var_len) ; p = p . add (var_len) ; sl } , $ ({ let aref = & mut * (p as * mut [T ; $ post]) ; p = p . add ($ post) ; aref } ,) *) } let input = $ arr ; # [allow (unused_unsafe)] unsafe { as_arrays (input) } } } } ; ($ arr : expr , $ ($ len : expr) ,*) => { { { # [inline] # [allow (unused_assignments)] # [allow (clippy :: eval_order_dependence)] unsafe fn as_arrays < T > (a : & mut [T ; $ ($ len +) * 0]) -> ($ (& mut [T ; $ len] ,) *) { let mut p = a . as_mut_ptr () ; ($ ({ let aref = & mut * (p as * mut [T ; $ len]) ; p = p . add ($ len) ; aref } ,) *) } let input = $ arr ; # [allow (unused_unsafe)] unsafe { as_arrays (input) } } } } ; }
};
}
