macro_rules! deps {
    () => {
        IgnorableBehavior!();
        Uts46MapperBorrowed!();
    };
}

macro_rules! impl_59 {
    () => {
        deps!();
        impl Uts46MapperBorrowed < '_ > { # [doc = " Returns an iterator adaptor that turns an `Iterator` over `char`"] # [doc = " into an iterator yielding a `char` sequence that gets the following"] # [doc = " operations from the \"Map\" and \"Normalize\" steps of the \"Processing\""] # [doc = " section of UTS 46 lazily applied to it:"] # [doc = ""] # [doc = " 1. The _ignored_ characters are ignored."] # [doc = " 2. The _mapped_ characters are mapped."] # [doc = " 3. The _disallowed_ characters are replaced with U+FFFD,"] # [doc = "    which itself is a disallowed character."] # [doc = " 4. The _deviation_ characters are treated as _mapped_ or _valid_"] # [doc = "    as appropriate."] # [doc = " 5. The _disallowed_STD3_valid_ characters are treated as allowed."] # [doc = " 6. The _disallowed_STD3_mapped_ characters are treated as"] # [doc = "    _mapped_."] # [doc = " 7. The result is normalized to NFC."] # [doc = ""] # [doc = " Notably:"] # [doc = ""] # [doc = " * The STD3 or WHATWG ASCII deny list should be implemented as a"] # [doc = "   post-processing step."] # [doc = " * Transitional processing is not performed. Transitional mapping"] # [doc = "   would be a pre-processing step, but transitional processing is"] # [doc = "   deprecated, and none of Firefox, Safari, or Chrome use it."] pub fn map_normalize < 'delegate , I : Iterator < Item = char > + 'delegate > (& 'delegate self , iter : I ,) -> impl Iterator < Item = char > + 'delegate { self . normalizer . normalize_iter_private (iter , crate :: IgnorableBehavior :: Ignored) } # [doc = " Returns an iterator adaptor that turns an `Iterator` over `char`"] # [doc = " into an iterator yielding a `char` sequence that gets the following"] # [doc = " operations from the NFC check and statucs steps of the \"Validity"] # [doc = " Criteria\" section of UTS 46 lazily applied to it:"] # [doc = ""] # [doc = " 1. The _ignored_ characters are treated as _disallowed_."] # [doc = " 2. The _mapped_ characters are mapped."] # [doc = " 3. The _disallowed_ characters are replaced with U+FFFD,"] # [doc = "    which itself is a disallowed character."] # [doc = " 4. The _deviation_ characters are treated as _mapped_ or _valid_"] # [doc = "    as appropriate."] # [doc = " 5. The _disallowed_STD3_valid_ characters are treated as allowed."] # [doc = " 6. The _disallowed_STD3_mapped_ characters are treated as"] # [doc = "    _mapped_."] # [doc = " 7. The result is normalized to NFC."] # [doc = ""] # [doc = " Notably:"] # [doc = ""] # [doc = " * The STD3 or WHATWG ASCII deny list should be implemented as a"] # [doc = "   post-processing step."] # [doc = " * Transitional processing is not performed. Transitional mapping"] # [doc = "   would be a pre-processing step, but transitional processing is"] # [doc = "   deprecated, and none of Firefox, Safari, or Chrome use it."] # [doc = " * The output needs to be compared with input to see if anything"] # [doc = "   changed. This check catches failures to adhere to the normalization"] # [doc = "   and status requirements. In particular, this comparison results"] # [doc = "   in _mapped_ characters resulting in error like \"Validity Criteria\""] # [doc = "   requires."] pub fn normalize_validate < 'delegate , I : Iterator < Item = char > + 'delegate > (& 'delegate self , iter : I ,) -> impl Iterator < Item = char > + 'delegate { self . normalizer . normalize_iter_private (iter , crate :: IgnorableBehavior :: ReplacementCharacter) } }
    };
}

impl_59!()