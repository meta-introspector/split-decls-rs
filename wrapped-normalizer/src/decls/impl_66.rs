macro_rules! deps {
    () => {
        DecomposingNormalizerBorrowed!();
        DecomposingNormalizer!();
    };
}

macro_rules! impl_66 {
    () => {
        deps!();
        impl DecomposingNormalizer { # [doc = " Constructs a borrowed version of this type for more efficient querying."] pub fn as_borrowed (& self) -> DecomposingNormalizerBorrowed < '_ > { DecomposingNormalizerBorrowed { decompositions : self . decompositions . get () , tables : self . tables . get () , supplementary_tables : self . supplementary_tables . as_ref () . map (| s | s . get ()) , decomposition_passthrough_bound : self . decomposition_passthrough_bound , composition_passthrough_bound : self . composition_passthrough_bound , } } # [doc = " NFD constructor using compiled data."] # [doc = ""] # [doc = " ✨ *Enabled with the `compiled_data` Cargo feature.*"] # [doc = ""] # [doc = " [📚 Help choosing a constructor](icu_provider::constructors)"] # [cfg (feature = "compiled_data")] pub const fn new_nfd () -> DecomposingNormalizerBorrowed < 'static > { DecomposingNormalizerBorrowed :: new_nfd () } icu_provider :: gen_buffer_data_constructors ! (() -> error : DataError , functions : [new_nfd : skip , try_new_nfd_with_buffer_provider , try_new_nfd_unstable , Self ,]) ; # [doc = icu_provider :: gen_buffer_unstable_docs ! (UNSTABLE , Self :: new_nfd)] pub fn try_new_nfd_unstable < D > (provider : & D) -> Result < Self , DataError > where D : DataProvider < NormalizerNfdDataV1 > + DataProvider < NormalizerNfdTablesV1 > + ? Sized , { let decompositions : DataPayload < NormalizerNfdDataV1 > = provider . load (Default :: default ()) ? . payload ; let tables : DataPayload < NormalizerNfdTablesV1 > = provider . load (Default :: default ()) ? . payload ; if tables . get () . scalars16 . len () + tables . get () . scalars24 . len () > 0xFFF { return Err (DataError :: custom ("future extension") . with_marker (NormalizerNfdTablesV1 :: INFO)) ; } let cap = decompositions . get () . passthrough_cap ; if cap > 0x0300 { return Err (DataError :: custom ("invalid") . with_marker (NormalizerNfdDataV1 :: INFO)) ; } let decomposition_capped = cap . min (0xC0) ; let composition_capped = cap . min (0x0300) ; Ok (DecomposingNormalizer { decompositions , tables , supplementary_tables : None , decomposition_passthrough_bound : decomposition_capped as u8 , composition_passthrough_bound : composition_capped , }) } icu_provider :: gen_buffer_data_constructors ! (() -> error : DataError , functions : [new_nfkd : skip , try_new_nfkd_with_buffer_provider , try_new_nfkd_unstable , Self ,]) ; # [doc = " NFKD constructor using compiled data."] # [doc = ""] # [doc = " ✨ *Enabled with the `compiled_data` Cargo feature.*"] # [doc = ""] # [doc = " [📚 Help choosing a constructor](icu_provider::constructors)"] # [cfg (feature = "compiled_data")] pub const fn new_nfkd () -> DecomposingNormalizerBorrowed < 'static > { DecomposingNormalizerBorrowed :: new_nfkd () } # [doc = icu_provider :: gen_buffer_unstable_docs ! (UNSTABLE , Self :: new_nfkd)] pub fn try_new_nfkd_unstable < D > (provider : & D) -> Result < Self , DataError > where D : DataProvider < NormalizerNfkdDataV1 > + DataProvider < NormalizerNfdTablesV1 > + DataProvider < NormalizerNfkdTablesV1 > + ? Sized , { let decompositions : DataPayload < NormalizerNfkdDataV1 > = provider . load (Default :: default ()) ? . payload ; let tables : DataPayload < NormalizerNfdTablesV1 > = provider . load (Default :: default ()) ? . payload ; let supplementary_tables : DataPayload < NormalizerNfkdTablesV1 > = provider . load (Default :: default ()) ? . payload ; if tables . get () . scalars16 . len () + tables . get () . scalars24 . len () + supplementary_tables . get () . scalars16 . len () + supplementary_tables . get () . scalars24 . len () > 0xFFF { return Err (DataError :: custom ("future extension") . with_marker (NormalizerNfdTablesV1 :: INFO)) ; } let cap = decompositions . get () . passthrough_cap ; if cap > 0x0300 { return Err (DataError :: custom ("invalid") . with_marker (NormalizerNfkdDataV1 :: INFO)) ; } let decomposition_capped = cap . min (0xC0) ; let composition_capped = cap . min (0x0300) ; Ok (DecomposingNormalizer { decompositions : decompositions . cast () , tables , supplementary_tables : Some (supplementary_tables) , decomposition_passthrough_bound : decomposition_capped as u8 , composition_passthrough_bound : composition_capped , }) } # [doc = " UTS 46 decomposed constructor (testing only)"] # [doc = ""] # [doc = " This is a special building block normalization for IDNA. It is the decomposed counterpart of"] # [doc = " ICU4C's UTS 46 normalization with two exceptions: characters that UTS 46 disallows and"] # [doc = " ICU4C maps to U+FFFD and characters that UTS 46 maps to the empty string normalize as in"] # [doc = " NFD in this normalization. In both cases, the previous UTS 46 processing before using"] # [doc = " normalization is expected to deal with these characters. Making the disallowed characters"] # [doc = " behave like this is beneficial to data size, and this normalizer implementation cannot"] # [doc = " deal with a character normalizing to the empty string, which doesn't happen in NFD or"] # [doc = " NFKD as of Unicode 14."] # [doc = ""] # [doc = " Warning: In this normalization, U+0345 COMBINING GREEK YPOGEGRAMMENI exhibits a behavior"] # [doc = " that no character in Unicode exhibits in NFD, NFKD, NFC, or NFKC: Case folding turns"] # [doc = " U+0345 from a reordered character into a non-reordered character before reordering happens."] # [doc = " Therefore, the output of this normalization may differ for different inputs that are"] # [doc = " canonically equivalent with each other if they differ by how U+0345 is ordered relative"] # [doc = " to other reorderable characters."] pub (crate) fn try_new_uts46_decomposed_unstable < D > (provider : & D) -> Result < Self , DataError > where D : DataProvider < NormalizerUts46DataV1 > + DataProvider < NormalizerNfdTablesV1 > + DataProvider < NormalizerNfkdTablesV1 > + ? Sized , { let decompositions : DataPayload < NormalizerUts46DataV1 > = provider . load (Default :: default ()) ? . payload ; let tables : DataPayload < NormalizerNfdTablesV1 > = provider . load (Default :: default ()) ? . payload ; let supplementary_tables : DataPayload < NormalizerNfkdTablesV1 > = provider . load (Default :: default ()) ? . payload ; if tables . get () . scalars16 . len () + tables . get () . scalars24 . len () + supplementary_tables . get () . scalars16 . len () + supplementary_tables . get () . scalars24 . len () > 0xFFF { return Err (DataError :: custom ("future extension") . with_marker (NormalizerNfdTablesV1 :: INFO)) ; } let cap = decompositions . get () . passthrough_cap ; if cap > 0x0300 { return Err (DataError :: custom ("invalid") . with_marker (NormalizerUts46DataV1 :: INFO)) ; } let decomposition_capped = cap . min (0xC0) ; let composition_capped = cap . min (0x0300) ; Ok (DecomposingNormalizer { decompositions : decompositions . cast () , tables , supplementary_tables : Some (supplementary_tables) , decomposition_passthrough_bound : decomposition_capped as u8 , composition_passthrough_bound : composition_capped , }) } }
    };
}

impl_66!()