// Generated macro for Options (struct)
macro_rules! Depcrate_checkoutOptions {
() => {
// Module: crate::checkout
// Provides: {"Options"}
// Dependencies: {}
# [doc = " Options to further configure the checkout operation."] # [derive (Clone , Default)] pub struct Options { # [doc = " capabilities of the file system"] pub fs : gix_fs :: Capabilities , # [doc = " Options to configure how to validate path components."] pub validate : gix_worktree :: validate :: path :: component :: Options , # [doc = " If set, don't use more than this amount of threads."] # [doc = " Otherwise, usually use as many threads as there are logical cores."] # [doc = " A value of 0 is interpreted as no-limit"] pub thread_limit : Option < usize > , # [doc = " If true, we assume no file to exist in the target directory, and want exclusive access to it."] # [doc = " This should be enabled when cloning to avoid checks for freshness of files. This also enables"] # [doc = " detection of collisions based on whether or not exclusive file creation succeeds or fails."] pub destination_is_initially_empty : bool , # [doc = " If true, default false, worktree entries on disk will be overwritten with content from the index"] # [doc = " even if they appear to be changed. When creating directories that clash with existing worktree entries,"] # [doc = " these will try to delete the existing entry."] # [doc = " This is similar in behaviour as `git checkout --force`."] pub overwrite_existing : bool , # [doc = " If true, default false, try to checkout as much as possible and don't abort on first error which isn't"] # [doc = " due to a conflict."] # [doc = " The checkout operation will never fail, but count the encountered errors instead along with their paths."] pub keep_going : bool , # [doc = " Control how stat comparisons are made when checking if a file is fresh."] pub stat_options : stat :: Options , # [doc = " A stack of attributes to use with the filesystem cache to use as driver for filters."] pub attributes : gix_worktree :: stack :: state :: Attributes , # [doc = " The filter pipeline to use for applying mandatory filters before writing to the worktree."] pub filters : gix_filter :: Pipeline , # [doc = " Control how long-running processes may use the 'delay' capability."] pub filter_process_delay : gix_filter :: driver :: apply :: Delay , }
};
}
