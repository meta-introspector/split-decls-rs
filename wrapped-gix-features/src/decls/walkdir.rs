macro_rules! walkdir {
    () => {
        # [doc = ""] # [cfg (feature = "walkdir")] pub mod walkdir { use std :: { borrow :: Cow , ffi :: OsStr , fs :: FileType , path :: Path } ; pub use walkdir :: Error ; use walkdir :: { DirEntry as DirEntryImpl , WalkDir as WalkDirImpl } ; # [doc = " A directory entry returned by [DirEntryIter]."] pub type DirEntry = super :: walkdir_precompose :: DirEntry < DirEntryImpl > ; # [doc = " A platform to create a [DirEntryIter] from."] pub type WalkDir = super :: walkdir_precompose :: WalkDir < WalkDirImpl > ; pub use super :: shared :: Parallelism ; impl super :: walkdir_precompose :: DirEntryApi for DirEntryImpl { fn path (& self) -> Cow < '_ , Path > { self . path () . into () } fn file_name (& self) -> Cow < '_ , OsStr > { self . file_name () . into () } fn file_type (& self) -> std :: io :: Result < FileType > { Ok (self . file_type ()) } } impl IntoIterator for WalkDir { type Item = Result < DirEntry , walkdir :: Error > ; type IntoIter = DirEntryIter ; fn into_iter (self) -> Self :: IntoIter { DirEntryIter { inner : self . inner . expect ("always set (builder fix)") . into_iter () , precompose_unicode : self . precompose_unicode , } } } impl WalkDir { # [doc = " Set the minimum component depth of paths of entries."] pub fn min_depth (mut self , min : usize) -> Self { self . inner = Some (self . inner . take () . expect ("always set") . min_depth (min)) ; self } # [doc = " Set the maximum component depth of paths of entries."] pub fn max_depth (mut self , max : usize) -> Self { self . inner = Some (self . inner . take () . expect ("always set") . max_depth (max)) ; self } # [doc = " Follow symbolic links."] pub fn follow_links (mut self , toggle : bool) -> Self { self . inner = Some (self . inner . take () . expect ("always set") . follow_links (toggle)) ; self } } # [doc = " Instantiate a new directory iterator which will not skip hidden files, with the given level of `parallelism`."] # [doc = ""] # [doc = " Use `precompose_unicode` to represent the `core.precomposeUnicode` configuration option."] pub fn walkdir_new (root : & Path , _ : Parallelism , precompose_unicode : bool) -> WalkDir { WalkDir { inner : WalkDirImpl :: new (root) . into () , precompose_unicode , } } # [doc = " Instantiate a new directory iterator which will not skip hidden files and is sorted, with the given level of `parallelism`."] # [doc = ""] # [doc = " Use `precompose_unicode` to represent the `core.precomposeUnicode` configuration option."] # [doc = " Use `max_depth` to limit the depth of the recursive walk."] # [doc = "   * `0`"] # [doc = "       - Returns only the root path with no children"] # [doc = "   * `1`"] # [doc = "       - Root directory and children."] # [doc = "   * `1..n`"] # [doc = "       - Root directory, children and {n}-grandchildren"] pub fn walkdir_sorted_new (root : & Path , _ : Parallelism , max_depth : usize , precompose_unicode : bool) -> WalkDir { WalkDir { inner : WalkDirImpl :: new (root) . max_depth (max_depth) . sort_by (| a , b | { let storage_a ; let storage_b ; let a_name = match gix_path :: os_str_into_bstr (a . file_name ()) { Ok (f) => f , Err (_) => { storage_a = a . file_name () . to_string_lossy () ; storage_a . as_ref () . into () } } ; let b_name = match gix_path :: os_str_into_bstr (b . file_name ()) { Ok (f) => f , Err (_) => { storage_b = b . file_name () . to_string_lossy () ; storage_b . as_ref () . into () } } ; let common = a_name . len () . min (b_name . len ()) ; a_name [.. common] . cmp (& b_name [.. common]) . then_with (| | { let a = a_name . get (common) . or_else (| | a . file_type () . is_dir () . then_some (& b'/')) ; let b = b_name . get (common) . or_else (| | b . file_type () . is_dir () . then_some (& b'/')) ; a . cmp (& b) }) }) . into () , precompose_unicode , } } # [doc = " The Iterator yielding directory items"] pub type DirEntryIter = super :: walkdir_precompose :: WalkDirIter < walkdir :: IntoIter , DirEntryImpl , walkdir :: Error > ; }
    };
}

walkdir!()