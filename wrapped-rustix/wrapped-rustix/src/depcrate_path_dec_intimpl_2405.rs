// Generated macro for impl_2405 (impl)
macro_rules! Depcrate_path_dec_intimpl_2405 {
() => {
// Module: crate::path::dec_int
// Provides: {"impl_2405"}
// Dependencies: {}
impl DecInt { # [doc = " Construct a new path component from an integer."] pub fn new < Int : Integer > (i : Int) -> Self { use private :: Sealed as _ ; let (is_neg , mut i) = i . as_unsigned () ; let mut len = 1 ; let mut buf = [MaybeUninit :: uninit () ; BUF_LEN] ; buf [BUF_LEN - 1] = MaybeUninit :: new (b'\0') ; loop { len += 1 ; if len > BUF_LEN { unsafe { unreachable_unchecked () } ; } buf [BUF_LEN - len] = MaybeUninit :: new (b'0' + i . div_mod_10 ()) ; if i . eq_zero () { break ; } } if is_neg { len += 1 ; if len > BUF_LEN { unsafe { unreachable_unchecked () } ; } buf [BUF_LEN - len] = MaybeUninit :: new (b'-') ; } Self { buf , len : NonZeroU8 :: new (len as u8) . unwrap () , } } # [doc = " Construct a new path component from a file descriptor."] # [inline] pub fn from_fd < Fd : AsFd > (fd : Fd) -> Self { Self :: new (fd . as_fd () . as_raw_fd ()) } # [doc = " Return the raw byte buffer as a `&str`."] # [inline] pub fn as_str (& self) -> & str { unsafe { core :: str :: from_utf8_unchecked (self . as_bytes ()) } } # [doc = " Return the raw byte buffer as a `&CStr`."] # [inline] pub fn as_c_str (& self) -> & CStr { let bytes_with_nul = self . as_bytes_with_nul () ; debug_assert ! (CStr :: from_bytes_with_nul (bytes_with_nul) . is_ok ()) ; unsafe { CStr :: from_bytes_with_nul_unchecked (bytes_with_nul) } } # [doc = " Return the raw byte buffer including the NUL byte."] # [inline] pub fn as_bytes_with_nul (& self) -> & [u8] { let len = NonZeroUsize :: from (self . len) . get () ; if len > BUF_LEN { unsafe { unreachable_unchecked () } ; } let init = & self . buf [(self . buf . len () - len) ..] ; unsafe { mem :: transmute :: < & [MaybeUninit < u8 >] , & [u8] > (init) } } # [doc = " Return the raw byte buffer."] # [inline] pub fn as_bytes (& self) -> & [u8] { let bytes = self . as_bytes_with_nul () ; & bytes [.. bytes . len () - 1] } }
};
}
