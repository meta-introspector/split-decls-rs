// Generated macro for SocketAddrArg (trait)
macro_rules! Depcrate_net_addrSocketAddrArg {
() => {
// Module: crate::net::addr
// Provides: {"SocketAddrArg"}
// Dependencies: {}
# [doc = " A trait abstracting over the types that can be passed as a `sockaddr`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Implementers of this trait must ensure that `with_sockaddr` calls `f` with"] # [doc = " a pointer that is readable for the passed length, and points to data that"] # [doc = " is a valid socket address for the system calls that accept `sockaddr` as a"] # [doc = " const pointer."] pub unsafe trait SocketAddrArg { # [doc = " Call a closure with the pointer and length to the corresponding C type."] # [doc = ""] # [doc = " The memory pointed to by the pointer of size length is guaranteed to be"] # [doc = " valid only for the duration of the call."] # [doc = ""] # [doc = " The API uses a closure so that:"] # [doc = "  - The libc types are not exposed in the rustix API."] # [doc = "  - Types like `SocketAddrUnix` that contain their corresponding C type"] # [doc = "    can pass it directly without a copy."] # [doc = "  - Other socket types can construct their C-compatible struct on the"] # [doc = "    stack and call the closure with a pointer to it."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " For `f` to use its pointer argument, it'll contain an `unsafe` block."] # [doc = " The caller of `with_sockaddr` here is responsible for ensuring that the"] # [doc = " safety condition for that `unsafe` block is satisfied by the guarantee"] # [doc = " that `with_sockaddr` here provides."] unsafe fn with_sockaddr < R > (& self , f : impl FnOnce (* const SocketAddrOpaque , SocketAddrLen) -> R ,) -> R ; # [doc = " Convert to `SocketAddrAny`."] fn as_any (& self) -> SocketAddrAny { let mut storage = MaybeUninit :: < SocketAddrStorage > :: uninit () ; unsafe { let len = self . write_sockaddr (storage . as_mut_ptr ()) ; SocketAddrAny :: new (storage , len) } } # [doc = " Encode an address into a `SocketAddrStorage`."] # [doc = ""] # [doc = " Returns the number of bytes that were written."] # [doc = ""] # [doc = " For a safe interface to this functionality, use [`as_any`]."] # [doc = ""] # [doc = " [`as_any`]: Self::as_any"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `storage` must be valid to write up to `size_of<SocketAddrStorage>()`"] # [doc = " bytes to."] unsafe fn write_sockaddr (& self , storage : * mut SocketAddrStorage) -> SocketAddrLen { self . with_sockaddr (| ptr , len | { ptr :: copy_nonoverlapping (ptr . cast :: < u8 > () , storage . cast :: < u8 > () , len as usize) ; len }) } }
};
}
