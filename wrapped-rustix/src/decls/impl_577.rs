macro_rules! deps {
    () => {
        AncillaryDrain!();
        RecvAncillaryBuffer!();
    };
}

macro_rules! impl_577 {
    () => {
        deps!();
        impl < 'buf > RecvAncillaryBuffer < 'buf > { # [doc = " Create a new, empty `RecvAncillaryBuffer` from a raw byte buffer."] # [doc = ""] # [doc = " The buffer size may be computed with [`cmsg_space`], or it may be"] # [doc = " zero for an empty buffer, however in that case, consider `default()`"] # [doc = " instead, or even using [`recv`] instead of `recvmsg`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Allocate a buffer for a single file descriptor:"] # [doc = " ```"] # [doc = " # use std::mem::MaybeUninit;"] # [doc = " # use rustix::cmsg_space;"] # [doc = " # use rustix::net::RecvAncillaryBuffer;"] # [doc = " let mut space = [MaybeUninit::uninit(); rustix::cmsg_space!(ScmRights(1))];"] # [doc = " let mut cmsg_buffer = RecvAncillaryBuffer::new(&mut space);"] # [doc = " ```"] # [doc = ""] # [doc = " Allocate a buffer for credentials:"] # [doc = " ```"] # [doc = " # #[cfg(linux_kernel)]"] # [doc = " # {"] # [doc = " # use std::mem::MaybeUninit;"] # [doc = " # use rustix::cmsg_space;"] # [doc = " # use rustix::net::RecvAncillaryBuffer;"] # [doc = " let mut space = [MaybeUninit::uninit(); rustix::cmsg_space!(ScmCredentials(1))];"] # [doc = " let mut cmsg_buffer = RecvAncillaryBuffer::new(&mut space);"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " Allocate a buffer for two file descriptors and credentials:"] # [doc = " ```"] # [doc = " # #[cfg(linux_kernel)]"] # [doc = " # {"] # [doc = " # use std::mem::MaybeUninit;"] # [doc = " # use rustix::cmsg_space;"] # [doc = " # use rustix::net::RecvAncillaryBuffer;"] # [doc = " let mut space = [MaybeUninit::uninit(); rustix::cmsg_space!(ScmRights(2), ScmCredentials(1))];"] # [doc = " let mut cmsg_buffer = RecvAncillaryBuffer::new(&mut space);"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " [`recv`]: crate::net::recv"] # [inline] pub fn new (buffer : & 'buf mut [MaybeUninit < u8 >]) -> Self { Self { buffer : align_for_cmsghdr (buffer) , read : 0 , length : 0 , } } # [doc = " Returns a pointer to the message data."] pub (crate) fn as_control_ptr (& mut self) -> * mut u8 { # [cfg (not (linux_kernel))] if self . buffer . is_empty () { return core :: ptr :: null_mut () ; } self . buffer . as_mut_ptr () . cast () } # [doc = " Returns the length of the message data."] pub (crate) fn control_len (& self) -> usize { self . buffer . len () } # [doc = " Set the length of the message data."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The buffer must be filled with valid message data."] pub (crate) unsafe fn set_control_len (& mut self , len : usize) { self . length = len ; self . read = 0 ; } # [doc = " Delete all messages from the buffer."] pub (crate) fn clear (& mut self) { self . drain () . for_each (drop) ; } # [doc = " Drain all messages from the buffer."] pub fn drain (& mut self) -> AncillaryDrain < '_ > { AncillaryDrain { messages : messages :: Messages :: new (& mut self . buffer [self . read ..] [.. self . length]) , read_and_length : Some ((& mut self . read , & mut self . length)) , } } }
    };
}

impl_577!();