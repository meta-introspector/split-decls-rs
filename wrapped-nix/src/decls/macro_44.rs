macro_rules! deps {
    () => {
        Result!();
    };
}

macro_rules! macro_44 {
    () => {
        deps!();
        feature ! { #! [feature = "zerocopy"] # [doc = " Copy a range of data from one file to another"] # [doc = ""] # [doc = " The `copy_file_range` system call performs an in-kernel copy between"] # [doc = " file descriptors `fd_in` and `fd_out` without the additional cost of"] # [doc = " transferring data from the kernel to user space and back again. There may be"] # [doc = " additional optimizations for specific file systems.  It copies up to `len`"] # [doc = " bytes of data from file descriptor `fd_in` to file descriptor `fd_out`,"] # [doc = " overwriting any data that exists within the requested range of the target"] # [doc = " file."] # [doc = ""] # [doc = " If the `off_in` and/or `off_out` arguments are used, the values"] # [doc = " will be mutated to reflect the new position within the file after"] # [doc = " copying. If they are not used, the relevant file descriptors will be seeked"] # [doc = " to the new position."] # [doc = ""] # [doc = " On successful completion the number of bytes actually copied will be"] # [doc = " returned."] # [cfg (any (linux_android , target_os = "freebsd"))] pub fn copy_file_range < Fd1 : std :: os :: fd :: AsFd , Fd2 : std :: os :: fd :: AsFd > (fd_in : Fd1 , off_in : Option <& mut i64 >, fd_out : Fd2 , off_out : Option <& mut i64 >, len : usize ,) -> Result < usize > { use std :: os :: fd :: AsRawFd ; let off_in = off_in . map (| offset | offset as * mut i64) . unwrap_or (ptr :: null_mut ()) ; let off_out = off_out . map (| offset | offset as * mut i64) . unwrap_or (ptr :: null_mut ()) ; cfg_if :: cfg_if ! { if # [cfg (target_os = "freebsd")] { let ret = unsafe { libc :: copy_file_range (fd_in . as_fd () . as_raw_fd () , off_in , fd_out . as_fd () . as_raw_fd () , off_out , len , 0 ,) } ; } else { let ret = unsafe { libc :: syscall (libc :: SYS_copy_file_range , fd_in . as_fd () . as_raw_fd () , off_in , fd_out . as_fd () . as_raw_fd () , off_out , len , 0 ,) } ; } } Errno :: result (ret) . map (| r | r as usize) } # [doc = " Splice data to/from a pipe"] # [doc = ""] # [doc = " # See Also"] # [doc = " *[`splice`](https://man7.org/linux/man-pages/man2/splice.2.html)"] # [cfg (linux_android)] pub fn splice < Fd1 : std :: os :: fd :: AsFd , Fd2 : std :: os :: fd :: AsFd > (fd_in : Fd1 , off_in : Option <& mut libc :: loff_t >, fd_out : Fd2 , off_out : Option <& mut libc :: loff_t >, len : usize , flags : SpliceFFlags ,) -> Result < usize > { use std :: os :: fd :: AsRawFd ; let off_in = off_in . map (| offset | offset as * mut libc :: loff_t) . unwrap_or (ptr :: null_mut ()) ; let off_out = off_out . map (| offset | offset as * mut libc :: loff_t) . unwrap_or (ptr :: null_mut ()) ; let ret = unsafe { libc :: splice (fd_in . as_fd () . as_raw_fd () , off_in , fd_out . as_fd () . as_raw_fd () , off_out , len , flags . bits ()) } ; Errno :: result (ret) . map (| r | r as usize) } # [doc = " Duplicate pipe content"] # [doc = ""] # [doc = " # See Also"] # [doc = " *[`tee`](https://man7.org/linux/man-pages/man2/tee.2.html)"] # [cfg (linux_android)] pub fn tee < Fd1 : std :: os :: fd :: AsFd , Fd2 : std :: os :: fd :: AsFd > (fd_in : Fd1 , fd_out : Fd2 , len : usize , flags : SpliceFFlags ,) -> Result < usize > { use std :: os :: fd :: AsRawFd ; let ret = unsafe { libc :: tee (fd_in . as_fd () . as_raw_fd () , fd_out . as_fd () . as_raw_fd () , len , flags . bits ()) } ; Errno :: result (ret) . map (| r | r as usize) } # [doc = " Splice user pages to/from a pipe"] # [doc = ""] # [doc = " # See Also"] # [doc = " *[`vmsplice`](https://man7.org/linux/man-pages/man2/vmsplice.2.html)"] # [cfg (linux_android)] pub fn vmsplice < F : std :: os :: fd :: AsFd > (fd : F , iov : & [std :: io :: IoSlice <'_ >] , flags : SpliceFFlags ,) -> Result < usize > { use std :: os :: fd :: AsRawFd ; let ret = unsafe { libc :: vmsplice (fd . as_fd () . as_raw_fd () , iov . as_ptr () . cast () , iov . len () , flags . bits () ,) } ; Errno :: result (ret) . map (| r | r as usize) } }
    };
}

macro_44!();