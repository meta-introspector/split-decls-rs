macro_rules! libc_bitflags {
    () => {
        # [doc = " The `libc_bitflags!` macro helps with a common use case of defining a public bitflags type"] # [doc = " with values from the libc crate. It is used the same way as the `bitflags!` macro, except"] # [doc = " that only the name of the flag value has to be given."] # [doc = ""] # [doc = " The `libc` crate must be in scope with the name `libc`."] # [doc = ""] # [doc = " # Example"] # [doc = " ```ignore"] # [doc = " libc_bitflags!{"] # [doc = "     pub struct ProtFlags: libc::c_int {"] # [doc = "         PROT_NONE;"] # [doc = "         PROT_READ;"] # [doc = "         /// PROT_WRITE enables write protect"] # [doc = "         PROT_WRITE;"] # [doc = "         PROT_EXEC;"] # [doc = "         #[cfg(linux_android)]"] # [doc = "         PROT_GROWSDOWN;"] # [doc = "         #[cfg(linux_android)]"] # [doc = "         PROT_GROWSUP;"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] # [doc = ""] # [doc = " Example with casting, due to a mistake in libc. In this example, the"] # [doc = " various flags have different types, so we cast the broken ones to the right"] # [doc = " type."] # [doc = ""] # [doc = " ```ignore"] # [doc = " libc_bitflags!{"] # [doc = "     pub struct SaFlags: libc::c_ulong {"] # [doc = "         SA_NOCLDSTOP as libc::c_ulong;"] # [doc = "         SA_NOCLDWAIT;"] # [doc = "         SA_NODEFER as libc::c_ulong;"] # [doc = "         SA_ONSTACK;"] # [doc = "         SA_RESETHAND as libc::c_ulong;"] # [doc = "         SA_RESTART as libc::c_ulong;"] # [doc = "         SA_SIGINFO;"] # [doc = "     }"] # [doc = " }"] # [doc = " ```"] macro_rules ! libc_bitflags { ($ (# [$ outer : meta]) * pub struct $ BitFlags : ident : $ T : ty { $ ($ (# [$ inner : ident $ ($ args : tt) *]) * $ Flag : ident $ (as $ cast : ty) *;) + }) => { :: bitflags :: bitflags ! { # [derive (Copy , Clone , Debug , Eq , Hash , Ord , PartialEq , PartialOrd)] # [repr (transparent)] $ (# [$ outer]) * pub struct $ BitFlags : $ T { $ ($ (# [$ inner $ ($ args) *]) * const $ Flag = libc ::$ Flag $ (as $ cast) *;) + } } } ; }
    };
}

libc_bitflags!()