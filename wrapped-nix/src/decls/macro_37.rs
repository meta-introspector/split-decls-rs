macro_rules! deps {
    () => {
        Result!();
        NixPath!();
    };
}

macro_rules! macro_37 {
    () => {
        deps!();
        feature ! { #! [feature = "fs"] # [doc = " open or create a file for reading, writing or executing"] # [doc = ""] # [doc = " # See Also"] # [doc = " [`open`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/open.html)"] # [allow (clippy :: useless_conversion)] pub fn open < P : ? Sized + NixPath > (path : & P , oflag : OFlag , mode : Mode ,) -> Result < std :: os :: fd :: OwnedFd > { use std :: os :: fd :: FromRawFd ; let fd = path . with_nix_path (| cstr | unsafe { libc :: open (cstr . as_ptr () , oflag . bits () , mode . bits () as c_uint) }) ?; Errno :: result (fd) ?; Ok (unsafe { std :: os :: fd :: OwnedFd :: from_raw_fd (fd) }) } # [doc = " open or create a file for reading, writing or executing"] # [doc = ""] # [doc = " The `openat` function is equivalent to the [`open`] function except in the case where the path"] # [doc = " specifies a relative path.  In that case, the file to be opened is determined relative to the"] # [doc = " directory associated with the file descriptor `dirfd`."] # [doc = ""] # [doc = " # See Also"] # [doc = " [`openat`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/openat.html)"] # [allow (clippy :: useless_conversion)] # [cfg (not (target_os = "redox"))] pub fn openat < P : ? Sized + NixPath , Fd : std :: os :: fd :: AsFd > (dirfd : Fd , path : & P , oflag : OFlag , mode : Mode ,) -> Result < OwnedFd > { use std :: os :: fd :: AsRawFd ; use std :: os :: fd :: FromRawFd ; let fd = path . with_nix_path (| cstr | unsafe { libc :: openat (dirfd . as_fd () . as_raw_fd () , cstr . as_ptr () , oflag . bits () , mode . bits () as c_uint) }) ?; Errno :: result (fd) ?; Ok (unsafe { OwnedFd :: from_raw_fd (fd) }) } cfg_if :: cfg_if ! { if # [cfg (target_os = "linux")] { libc_bitflags ! { # [doc = " Path resolution flags."] # [doc = ""] # [doc = " See [path resolution(7)](https://man7.org/linux/man-pages/man7/path_resolution.7.html)"] # [doc = " for details of the resolution process."] pub struct ResolveFlag : libc :: c_ulonglong { # [doc = " Do not permit the path resolution to succeed if any component of"] # [doc = " the resolution is not a descendant of the directory indicated by"] # [doc = " dirfd.  This causes absolute symbolic links (and absolute values of"] # [doc = " pathname) to be rejected."] RESOLVE_BENEATH ; # [doc = " Treat the directory referred to by dirfd as the root directory"] # [doc = " while resolving pathname."] RESOLVE_IN_ROOT ; # [doc = " Disallow all magic-link resolution during path resolution. Magic"] # [doc = " links are symbolic link-like objects that are most notably found"] # [doc = " in proc(5);  examples include `/proc/[pid]/exe` and `/proc/[pid]/fd/*`."] # [doc = ""] # [doc = " See symlink(7) for more details."] RESOLVE_NO_MAGICLINKS ; # [doc = " Disallow resolution of symbolic links during path resolution. This"] # [doc = " option implies RESOLVE_NO_MAGICLINKS."] RESOLVE_NO_SYMLINKS ; # [doc = " Disallow traversal of mount points during path resolution (including"] # [doc = " all bind mounts)."] RESOLVE_NO_XDEV ; } } # [doc = " Specifies how [`openat2()`] should open a pathname."] # [doc = ""] # [doc = " # Reference"] # [doc = ""] # [doc = " * [Linux](https://man7.org/linux/man-pages/man2/open_how.2type.html)"] # [repr (transparent)] # [derive (Clone , Copy , Debug)] pub struct OpenHow (libc :: open_how) ; impl OpenHow { # [doc = " Create a new zero-filled `open_how`."] pub fn new () -> Self { Self (unsafe { std :: mem :: MaybeUninit :: zeroed () . assume_init () }) } # [doc = " Set the open flags used to open a file, completely overwriting any"] # [doc = " existing flags."] pub fn flags (mut self , flags : OFlag) -> Self { let flags = flags . bits () as libc :: c_ulonglong ; self . 0 . flags = flags ; self } # [doc = " Set the file mode new files will be created with, overwriting any"] # [doc = " existing flags."] pub fn mode (mut self , mode : Mode) -> Self { let mode = mode . bits () as libc :: c_ulonglong ; self . 0 . mode = mode ; self } # [doc = " Set resolve flags, completely overwriting any existing flags."] # [doc = ""] # [doc = " See [ResolveFlag] for more detail."] pub fn resolve (mut self , resolve : ResolveFlag) -> Self { let resolve = resolve . bits () ; self . 0 . resolve = resolve ; self } } impl Default for OpenHow { fn default () -> Self { Self :: new () } } # [doc = " Open or create a file for reading, writing or executing."] # [doc = ""] # [doc = " `openat2` is an extension of the [`openat`] function that allows the caller"] # [doc = " to control how path resolution happens."] # [doc = ""] # [doc = " # See also"] # [doc = ""] # [doc = " [openat2](https://man7.org/linux/man-pages/man2/openat2.2.html)"] pub fn openat2 < P : ? Sized + NixPath , Fd : std :: os :: fd :: AsFd > (dirfd : Fd , path : & P , mut how : OpenHow ,) -> Result < OwnedFd > { use std :: os :: fd :: AsRawFd ; use std :: os :: fd :: FromRawFd ; let fd = path . with_nix_path (| cstr | unsafe { libc :: syscall (libc :: SYS_openat2 , dirfd . as_fd () . as_raw_fd () , cstr . as_ptr () , & mut how as * mut OpenHow , std :: mem :: size_of ::< libc :: open_how > () ,) }) ? as RawFd ; Errno :: result (fd) ?; Ok (unsafe { OwnedFd :: from_raw_fd (fd) }) } } } # [doc = " Change the name of a file."] # [doc = ""] # [doc = " The `renameat` function is equivalent to `rename` except in the case where either `old_path`"] # [doc = " or `new_path` specifies a relative path.  In such cases, the file to be renamed (or the its new"] # [doc = " name, respectively) is located relative to `old_dirfd` or `new_dirfd`, respectively"] # [doc = ""] # [doc = " # See Also"] # [doc = " [`renameat`](https://pubs.opengroup.org/onlinepubs/9699919799/functions/rename.html)"] # [cfg (not (target_os = "redox"))] pub fn renameat < P1 : ? Sized + NixPath , P2 : ? Sized + NixPath , Fd1 : std :: os :: fd :: AsFd , Fd2 : std :: os :: fd :: AsFd > (old_dirfd : Fd1 , old_path : & P1 , new_dirfd : Fd2 , new_path : & P2 ,) -> Result < () > { use std :: os :: fd :: AsRawFd ; let res = old_path . with_nix_path (| old_cstr | { new_path . with_nix_path (| new_cstr | unsafe { libc :: renameat (old_dirfd . as_fd () . as_raw_fd () , old_cstr . as_ptr () , new_dirfd . as_fd () . as_raw_fd () , new_cstr . as_ptr () ,) }) }) ??; Errno :: result (res) . map (drop) } }
    };
}

macro_37!();