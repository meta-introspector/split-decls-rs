macro_rules! deps {
    () => {
        Metadata!();
        SectionId!();
        Event!();
        AsKey!();
        Error!();
        Section!();
        File!();
        Key!();
    };
}

macro_rules! impl_42 {
    () => {
        deps!();
        # [doc = " Read-only low-level access methods, as it requires generics for converting into"] # [doc = " custom values defined in this crate like [`Integer`](crate::Integer) and"] # [doc = " [`Color`](crate::Color)."] impl < 'event > File < 'event > { # [doc = " Returns an interpreted value given a `key`."] # [doc = ""] # [doc = " It's recommended to use one of the value types provide dby this crate"] # [doc = " as they implement the conversion, but this function is flexible and"] # [doc = " will accept any type that implements [`TryFrom<&BStr>`](TryFrom)."] # [doc = ""] # [doc = " Consider [`Self::values`] if you want to get all values of a multivar instead."] # [doc = ""] # [doc = " If a `string` is desired, use the [`string()`](Self::string()) method instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use gix_config::File;"] # [doc = " # use gix_config::{Integer, Boolean};"] # [doc = " # use std::borrow::Cow;"] # [doc = " # use std::convert::TryFrom;"] # [doc = " let config = r#\""] # [doc = "     [core]"] # [doc = "         a = 10k"] # [doc = "         c = false"] # [doc = " \"#;"] # [doc = " let git_config = gix_config::File::try_from(config)?;"] # [doc = " // You can either use the turbofish to determine the type..."] # [doc = " let a_value = git_config.value::<Integer>(\"core.a\")?;"] # [doc = " // ... or explicitly declare the type to avoid the turbofish"] # [doc = " let c_value: Boolean = git_config.value(\"core.c\")?;"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn value < 'a , T : TryFrom < Cow < 'a , BStr > > > (& 'a self , key : impl AsKey) -> Result < T , lookup :: Error < T :: Error > > { let key = key . as_key () ; self . value_by (key . section_name , key . subsection_name , key . value_name) } # [doc = " Returns an interpreted value given a section, an optional subsection and"] # [doc = " value name."] # [doc = ""] # [doc = " It's recommended to use one of the value types provide dby this crate"] # [doc = " as they implement the conversion, but this function is flexible and"] # [doc = " will accept any type that implements [`TryFrom<&BStr>`](std::convert::TryFrom)."] # [doc = ""] # [doc = " Consider [`Self::values`] if you want to get all values of a multivar instead."] # [doc = ""] # [doc = " If a `string` is desired, use the [`string()`](Self::string()) method instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use gix_config::File;"] # [doc = " # use gix_config::{Integer, Boolean};"] # [doc = " # use std::borrow::Cow;"] # [doc = " # use std::convert::TryFrom;"] # [doc = " let config = r#\""] # [doc = "     [core]"] # [doc = "         a = 10k"] # [doc = "         c = false"] # [doc = " \"#;"] # [doc = " let git_config = gix_config::File::try_from(config)?;"] # [doc = " // You can either use the turbofish to determine the type..."] # [doc = " let a_value = git_config.value_by::<Integer>(\"core\", None, \"a\")?;"] # [doc = " // ... or explicitly declare the type to avoid the turbofish"] # [doc = " let c_value: Boolean = git_config.value_by(\"core\", None, \"c\")?;"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] pub fn value_by < 'a , T : TryFrom < Cow < 'a , BStr > > > (& 'a self , section_name : & str , subsection_name : Option < & BStr > , value_name : & str ,) -> Result < T , lookup :: Error < T :: Error > > { T :: try_from (self . raw_value_by (section_name , subsection_name , value_name) ?) . map_err (lookup :: Error :: FailedConversion) } # [doc = " Like [`value()`](File::value()), but returning an `None` if the value wasn't found at `section[.subsection].value_name`"] pub fn try_value < 'a , T : TryFrom < Cow < 'a , BStr > > > (& 'a self , key : impl AsKey) -> Option < Result < T , T :: Error > > { let key = key . as_key () ; self . try_value_by (key . section_name , key . subsection_name , key . value_name) } # [doc = " Like [`value_by()`](File::value_by()), but returning an `None` if the value wasn't found at `section[.subsection].value_name`"] pub fn try_value_by < 'a , T : TryFrom < Cow < 'a , BStr > > > (& 'a self , section_name : & str , subsection_name : Option < & BStr > , value_name : & str ,) -> Option < Result < T , T :: Error > > { self . raw_value_by (section_name , subsection_name , value_name) . ok () . map (T :: try_from) } # [doc = " Returns all interpreted values given a section, an optional subsection"] # [doc = " and value name."] # [doc = ""] # [doc = " It's recommended to use one of the value types provide dby this crate"] # [doc = " as they implement the conversion, but this function is flexible and"] # [doc = " will accept any type that implements [`TryFrom<&BStr>`](TryFrom)."] # [doc = ""] # [doc = " Consider [`Self::value`] if you want to get a single value"] # [doc = " (following last-one-wins resolution) instead."] # [doc = ""] # [doc = " To access plain strings, use the [`strings()`](Self::strings()) method instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use gix_config::File;"] # [doc = " # use gix_config::{Integer, Boolean};"] # [doc = " # use std::borrow::Cow;"] # [doc = " # use std::convert::TryFrom;"] # [doc = " # use bstr::ByteSlice;"] # [doc = " let config = r#\""] # [doc = "     [core]"] # [doc = "         a = true"] # [doc = "         c"] # [doc = "     [core]"] # [doc = "         a"] # [doc = "         a = false"] # [doc = " \"#;"] # [doc = " let git_config = gix_config::File::try_from(config).unwrap();"] # [doc = " // You can either use the turbofish to determine the type..."] # [doc = " let a_value = git_config.values::<Boolean>(\"core.a\")?;"] # [doc = " assert_eq!("] # [doc = "     a_value,"] # [doc = "     vec!["] # [doc = "         Boolean(true),"] # [doc = "         Boolean(false),"] # [doc = "         Boolean(false),"] # [doc = "     ]"] # [doc = " );"] # [doc = " // ... or explicitly declare the type to avoid the turbofish"] # [doc = " let c_value: Vec<Boolean> = git_config.values(\"core.c\").unwrap();"] # [doc = " assert_eq!(c_value, vec![Boolean(false)]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " [`value`]: crate::value"] # [doc = " [`TryFrom`]: std::convert::TryFrom"] pub fn values < 'a , T : TryFrom < Cow < 'a , BStr > > > (& 'a self , key : impl AsKey) -> Result < Vec < T > , lookup :: Error < T :: Error > > { self . raw_values (key) ? . into_iter () . map (T :: try_from) . collect :: < Result < Vec < _ > , _ > > () . map_err (lookup :: Error :: FailedConversion) } # [doc = " Returns all interpreted values given a section, an optional subsection"] # [doc = " and value name."] # [doc = ""] # [doc = " It's recommended to use one of the value types provide dby this crate"] # [doc = " as they implement the conversion, but this function is flexible and"] # [doc = " will accept any type that implements [`TryFrom<&BStr>`](std::convert::TryFrom)."] # [doc = ""] # [doc = " Consider [`Self::value`] if you want to get a single value"] # [doc = " (following last-one-wins resolution) instead."] # [doc = ""] # [doc = " To access plain strings, use the [`strings()`](Self::strings()) method instead."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use gix_config::File;"] # [doc = " # use gix_config::{Integer, Boolean};"] # [doc = " # use std::borrow::Cow;"] # [doc = " # use std::convert::TryFrom;"] # [doc = " # use bstr::ByteSlice;"] # [doc = " let config = r#\""] # [doc = "     [core]"] # [doc = "         a = true"] # [doc = "         c"] # [doc = "     [core]"] # [doc = "         a"] # [doc = "         a = false"] # [doc = " \"#;"] # [doc = " let git_config = gix_config::File::try_from(config).unwrap();"] # [doc = " // You can either use the turbofish to determine the type..."] # [doc = " let a_value = git_config.values_by::<Boolean>(\"core\", None, \"a\")?;"] # [doc = " assert_eq!("] # [doc = "     a_value,"] # [doc = "     vec!["] # [doc = "         Boolean(true),"] # [doc = "         Boolean(false),"] # [doc = "         Boolean(false),"] # [doc = "     ]"] # [doc = " );"] # [doc = " // ... or explicitly declare the type to avoid the turbofish"] # [doc = " let c_value: Vec<Boolean> = git_config.values_by(\"core\", None, \"c\").unwrap();"] # [doc = " assert_eq!(c_value, vec![Boolean(false)]);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [doc = ""] # [doc = " [`value`]: crate::value"] # [doc = " [`TryFrom`]: std::convert::TryFrom"] pub fn values_by < 'a , T : TryFrom < Cow < 'a , BStr > > > (& 'a self , section_name : & str , subsection_name : Option < & BStr > , value_name : & str ,) -> Result < Vec < T > , lookup :: Error < T :: Error > > { self . raw_values_by (section_name , subsection_name , value_name) ? . into_iter () . map (T :: try_from) . collect :: < Result < Vec < _ > , _ > > () . map_err (lookup :: Error :: FailedConversion) } # [doc = " Returns the last found immutable section with a given `name` and optional `subsection_name`."] pub fn section (& self , name : & str , subsection_name : Option < & BStr > ,) -> Result < & file :: Section < 'event > , lookup :: existing :: Error > { self . section_filter (name , subsection_name , | _ | true) ? . ok_or (lookup :: existing :: Error :: SectionMissing) } # [doc = " Returns the last found immutable section with a given `section_key`, identifying the name and subsection name like `core`"] # [doc = " or `remote.origin`."] pub fn section_by_key (& self , section_key : & BStr) -> Result < & file :: Section < 'event > , lookup :: existing :: Error > { let key = crate :: parse :: section :: unvalidated :: Key :: parse (section_key) . ok_or (lookup :: existing :: Error :: KeyMissing) ? ; self . section (key . section_name , key . subsection_name) } # [doc = " Returns the last found immutable section with a given `name` and optional `subsection_name`, that matches `filter`."] # [doc = ""] # [doc = " If there are sections matching `section_name` and `subsection_name` but the `filter` rejects all of them, `Ok(None)`"] # [doc = " is returned."] pub fn section_filter < 'a > (& 'a self , name : & str , subsection_name : Option < & BStr > , mut filter : impl FnMut (& Metadata) -> bool ,) -> Result < Option < & 'a file :: Section < 'event > > , lookup :: existing :: Error > { Ok (self . section_ids_by_name_and_subname (name . as_ref () , subsection_name) ? . rev () . find_map ({ let sections = & self . sections ; move | id | { let s = & sections [& id] ; filter (s . meta ()) . then_some (s) } })) } # [doc = " Like [`section_filter()`](File::section_filter()), but identifies the section with `section_key` like `core` or `remote.origin`."] pub fn section_filter_by_key < 'a > (& 'a self , section_key : & BStr , filter : impl FnMut (& Metadata) -> bool ,) -> Result < Option < & 'a file :: Section < 'event > > , lookup :: existing :: Error > { let key = crate :: parse :: section :: unvalidated :: Key :: parse (section_key) . ok_or (lookup :: existing :: Error :: KeyMissing) ? ; self . section_filter (key . section_name , key . subsection_name , filter) } # [doc = " Gets all sections that match the provided `name`, ignoring any subsections."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Provided the following config:"] # [doc = ""] # [doc = " ```text"] # [doc = " [core]"] # [doc = "     a = b"] # [doc = " [core \"\"]"] # [doc = "     c = d"] # [doc = " [core \"apple\"]"] # [doc = "     e = f"] # [doc = " ```"] # [doc = ""] # [doc = " Calling this method will yield all sections:"] # [doc = ""] # [doc = " ```"] # [doc = " # use gix_config::File;"] # [doc = " # use gix_config::{Integer, Boolean};"] # [doc = " # use std::borrow::Cow;"] # [doc = " # use std::convert::TryFrom;"] # [doc = " let config = r#\""] # [doc = "     [core]"] # [doc = "         a = b"] # [doc = "     [core \"\"]"] # [doc = "         c = d"] # [doc = "     [core \"apple\"]"] # [doc = "         e = f"] # [doc = " \"#;"] # [doc = " let git_config = gix_config::File::try_from(config)?;"] # [doc = " assert_eq!(git_config.sections_by_name(\"core\").map_or(0, |s|s.count()), 3);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] # [must_use] pub fn sections_by_name < 'a > (& 'a self , name : & 'a str ,) -> Option < impl Iterator < Item = & 'a file :: Section < 'event > > + 'a > { self . section_ids_by_name (name) . ok () . map (move | ids | { ids . map (move | id | { self . sections . get (& id) . expect ("section doesn't have id from from lookup") }) }) } # [doc = " Similar to [`sections_by_name()`](Self::sections_by_name()), but returns an identifier for this section as well to allow"] # [doc = " referring to it unambiguously even in the light of deletions."] # [must_use] pub fn sections_and_ids_by_name < 'a > (& 'a self , name : & 'a str ,) -> Option < impl Iterator < Item = (& 'a file :: Section < 'event > , SectionId) > + 'a > { self . section_ids_by_name (name) . ok () . map (move | ids | { ids . map (move | id | { (self . sections . get (& id) . expect ("section doesn't have id from from lookup") , id ,) }) }) } # [doc = " Gets all sections that match the provided `name`, ignoring any subsections, and pass the `filter`."] # [must_use] pub fn sections_by_name_and_filter < 'a > (& 'a self , name : & 'a str , mut filter : impl FnMut (& Metadata) -> bool + 'a ,) -> Option < impl Iterator < Item = & 'a file :: Section < 'event > > + 'a > { self . section_ids_by_name (name) . ok () . map (move | ids | { ids . filter_map (move | id | { let s = self . sections . get (& id) . expect ("section doesn't have id from from lookup") ; filter (s . meta ()) . then_some (s) }) }) } # [doc = " Returns the number of values in the config, no matter in which section."] # [doc = ""] # [doc = " For example, a config with multiple empty sections will return 0."] # [doc = " This ignores any comments."] # [must_use] pub fn num_values (& self) -> usize { self . sections . values () . map (| section | section . num_values ()) . sum () } # [doc = " Returns if there are no entries in the config. This will return true"] # [doc = " if there are only empty sections, with whitespace and comments not being considered"] # [doc = " void."] # [must_use] pub fn is_void (& self) -> bool { self . sections . values () . all (| s | s . body . is_void ()) } # [doc = " Return this file's metadata, typically set when it was first created to indicate its origins."] # [doc = ""] # [doc = " It will be used in all newly created sections to identify them."] # [doc = " Change it with [`File::set_meta()`]."] pub fn meta (& self) -> & Metadata { & self . meta } # [doc = " Change the origin of this instance to be the given `meta`data."] # [doc = ""] # [doc = " This is useful to control what origin about-to-be-added sections receive."] pub fn set_meta (& mut self , meta : impl Into < OwnShared < Metadata > >) -> & mut Self { self . meta = meta . into () ; self } # [doc = " Similar to [`meta()`](File::meta()), but with shared ownership."] pub fn meta_owned (& self) -> OwnShared < Metadata > { OwnShared :: clone (& self . meta) } # [doc = " Return an iterator over all sections, in order of occurrence in the file itself."] pub fn sections (& self) -> impl Iterator < Item = & file :: Section < 'event > > + '_ { self . section_order . iter () . map (move | id | & self . sections [id]) } # [doc = " Return an iterator over all sections and their ids, in order of occurrence in the file itself."] pub fn sections_and_ids (& self) -> impl Iterator < Item = (& file :: Section < 'event > , SectionId) > + '_ { self . section_order . iter () . map (move | id | (& self . sections [id] , * id)) } # [doc = " Return an iterator over all section ids, in order of occurrence in the file itself."] pub fn section_ids (& mut self) -> impl Iterator < Item = SectionId > + '_ { self . section_order . iter () . copied () } # [doc = " Return an iterator over all sections along with non-section events that are placed right after them,"] # [doc = " in order of occurrence in the file itself."] # [doc = ""] # [doc = " This allows to reproduce the look of sections perfectly when serializing them with"] # [doc = " [`write_to()`](file::Section::write_to())."] pub fn sections_and_postmatter (& self) -> impl Iterator < Item = (& file :: Section < 'event > , Vec < & Event < 'event > >) > { self . section_order . iter () . map (move | id | { let s = & self . sections [id] ; let pm : Vec < _ > = self . frontmatter_post_section . get (id) . map (| events | events . iter () . collect ()) . unwrap_or_default () ; (s , pm) }) } # [doc = " Return all events which are in front of the first of our sections, or `None` if there are none."] pub fn frontmatter (& self) -> Option < impl Iterator < Item = & Event < 'event > > > { (! self . frontmatter_events . is_empty ()) . then (| | self . frontmatter_events . iter ()) } # [doc = " Return the newline characters that have been detected in this config file or the default ones"] # [doc = " for the current platform."] # [doc = ""] # [doc = " Note that the first found newline is the one we use in the assumption of consistency."] pub fn detect_newline_style (& self) -> & BStr { self . frontmatter_events . iter () . find_map (extract_newline) . or_else (| | { self . sections () . find_map (| s | s . body . as_ref () . iter () . find_map (extract_newline)) }) . unwrap_or_else (| | platform_newline ()) } pub (crate) fn detect_newline_style_smallvec (& self) -> SmallVec < u8 , 2 > { self . detect_newline_style () . as_bytes () . into () } }
    };
}

impl_42!()