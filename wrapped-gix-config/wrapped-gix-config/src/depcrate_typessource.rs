// Generated macro for Source (enum)
macro_rules! Depcrate_typesSource {
() => {
// Module: crate::types
// Provides: {"Source"}
// Dependencies: {}
# [doc = " A list of known sources for git configuration in order of ascending precedence."] # [doc = ""] # [doc = " This means values from the first one will be overridden by values in the second one, and so forth."] # [doc = " Note that included files via `include.path` and `includeIf.<condition>.path` inherit"] # [doc = " their source."] # [derive (Clone , Copy , Debug , Eq , PartialEq , Hash , Ord , PartialOrd)] pub enum Source { # [doc = " A special configuration file that ships with the git installation, and is thus tied to the used git binary."] GitInstallation , # [doc = " System-wide configuration path. This is defined as"] # [doc = " `$(prefix)/etc/gitconfig` (where prefix is the git-installation directory)."] System , # [doc = " A platform defined location for where a user's git application configuration should be located."] # [doc = " If `$XDG_CONFIG_HOME` is not set or empty, `$HOME/.config/git/config` will be used"] # [doc = " on unix."] Git , # [doc = " This is usually `~/.gitconfig` on unix."] User , # [doc = " The configuration of the repository itself, located in `.git/config`."] Local , # [doc = " Configuration specific to a worktree as created with `git worktree` and"] # [doc = " typically located in `$GIT_DIR/config.worktree` if `extensions.worktreeConfig`"] # [doc = " is enabled."] Worktree , # [doc = " Values parsed from the environment using `GIT_CONFIG_COUNT`,"] # [doc = " `GIT_CONFIG_KEY_N` and `GIT_CONFIG_VALUE_N` where `N` is incremented from 0 up to the"] # [doc = " value of `GIT_CONFIG_COUNT`."] Env , # [doc = " Values set from the command-line, typically controlled by the user running a program."] Cli , # [doc = " Entirely internal from a programmatic source, and can be used to have (near final) say in configuration values."] Api , # [doc = " Values obtained from specific environment variables that override values in the git configuration."] # [doc = ""] # [doc = " For example, `HTTP_PROXY` overrides `http.proxy`, no matter where it is specified, and thus"] # [doc = " controls the value similar to how it's done in `git`."] EnvOverride , }
};
}
