macro_rules! DEFAULT_FILTER_ENV {
    () => {
        # [doc = " The default name for the environment variable to read filters from."] pub const DEFAULT_FILTER_ENV : & str = "RUST_LOG" ;
    };
}

DEFAULT_FILTER_ENV!()