// Generated macro for impl_356 (impl)
macro_rules! Depcrate_connection_datagramsimpl_356 {
() => {
// Module: crate::connection::datagrams
// Provides: {"impl_356"}
// Dependencies: {}
impl Datagrams < '_ > { # [doc = " Queue an unreliable, unordered datagram for immediate transmission"] # [doc = ""] # [doc = " If `drop` is true, previously queued datagrams which are still unsent may be discarded to"] # [doc = " make space for this datagram, in order of oldest to newest. If `drop` is false, and there"] # [doc = " isn't enough space due to previously queued datagrams, this function will return"] # [doc = " `SendDatagramError::Blocked`. `Event::DatagramsUnblocked` will be emitted once datagrams"] # [doc = " have been sent."] # [doc = ""] # [doc = " Returns `Err` iff a `len`-byte datagram cannot currently be sent."] pub fn send (& mut self , data : Bytes , drop : bool) -> Result < () , SendDatagramError > { if self . conn . config . datagram_receive_buffer_size . is_none () { return Err (SendDatagramError :: Disabled) ; } let max = self . max_size () . ok_or (SendDatagramError :: UnsupportedByPeer) ? ; if data . len () > max { return Err (SendDatagramError :: TooLarge) ; } if drop { while self . conn . datagrams . outgoing_total > self . conn . config . datagram_send_buffer_size { let prev = self . conn . datagrams . outgoing . pop_front () . expect ("datagrams.outgoing_total desynchronized") ; trace ! (len = prev . data . len () , "dropping outgoing datagram") ; self . conn . datagrams . outgoing_total -= prev . data . len () ; } } else if self . conn . datagrams . outgoing_total + data . len () > self . conn . config . datagram_send_buffer_size { self . conn . datagrams . send_blocked = true ; return Err (SendDatagramError :: Blocked (data)) ; } self . conn . datagrams . outgoing_total += data . len () ; self . conn . datagrams . outgoing . push_back (Datagram { data }) ; Ok (()) } # [doc = " Compute the maximum size of datagrams that may passed to `send_datagram`"] # [doc = ""] # [doc = " Returns `None` if datagrams are unsupported by the peer or disabled locally."] # [doc = ""] # [doc = " This may change over the lifetime of a connection according to variation in the path MTU"] # [doc = " estimate. The peer can also enforce an arbitrarily small fixed limit, but if the peer's"] # [doc = " limit is large this is guaranteed to be a little over a kilobyte at minimum."] # [doc = ""] # [doc = " Not necessarily the maximum size of received datagrams."] pub fn max_size (& self) -> Option < usize > { let max_size = self . conn . path . current_mtu () as usize - self . conn . predict_1rtt_overhead (None) - Datagram :: SIZE_BOUND ; let limit = self . conn . peer_params . max_datagram_frame_size ? . into_inner () . saturating_sub (Datagram :: SIZE_BOUND as u64) ; Some (limit . min (max_size as u64) as usize) } # [doc = " Receive an unreliable, unordered datagram"] pub fn recv (& mut self) -> Option < Bytes > { self . conn . datagrams . recv () } # [doc = " Bytes available in the outgoing datagram buffer"] # [doc = ""] # [doc = " When greater than zero, [`send`](Self::send)ing a datagram of at most this size is"] # [doc = " guaranteed not to cause older datagrams to be dropped."] pub fn send_buffer_space (& self) -> usize { self . conn . config . datagram_send_buffer_size . saturating_sub (self . conn . datagrams . outgoing_total) } }
};
}
