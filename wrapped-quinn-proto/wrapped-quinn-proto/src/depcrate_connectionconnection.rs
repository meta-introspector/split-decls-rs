// Generated macro for Connection (struct)
macro_rules! Depcrate_connectionConnection {
() => {
// Module: crate::connection
// Provides: {"Connection"}
// Dependencies: {}
# [doc = " Protocol state and logic for a single QUIC connection"] # [doc = ""] # [doc = " Objects of this type receive [`ConnectionEvent`]s and emit [`EndpointEvent`]s and application"] # [doc = " [`Event`]s to make progress. To handle timeouts, a `Connection` returns timer updates and"] # [doc = " expects timeouts through various methods. A number of simple getter methods are exposed"] # [doc = " to allow callers to inspect some of the connection state."] # [doc = ""] # [doc = " `Connection` has roughly 4 types of methods:"] # [doc = ""] # [doc = " - A. Simple getters, taking `&self`"] # [doc = " - B. Handlers for incoming events from the network or system, named `handle_*`."] # [doc = " - C. State machine mutators, for incoming commands from the application. For convenience we"] # [doc = "   refer to this as \"performing I/O\" below, however as per the design of this library none of the"] # [doc = "   functions actually perform system-level I/O. For example, [`read`](RecvStream::read) and"] # [doc = "   [`write`](SendStream::write), but also things like [`reset`](SendStream::reset)."] # [doc = " - D. Polling functions for outgoing events or actions for the caller to"] # [doc = "   take, named `poll_*`."] # [doc = ""] # [doc = " The simplest way to use this API correctly is to call (B) and (C) whenever"] # [doc = " appropriate, then after each of those calls, as soon as feasible call all"] # [doc = " polling methods (D) and deal with their outputs appropriately, e.g. by"] # [doc = " passing it to the application or by making a system-level I/O call. You"] # [doc = " should call the polling functions in this order:"] # [doc = ""] # [doc = " 1. [`poll_transmit`](Self::poll_transmit)"] # [doc = " 2. [`poll_timeout`](Self::poll_timeout)"] # [doc = " 3. [`poll_endpoint_events`](Self::poll_endpoint_events)"] # [doc = " 4. [`poll`](Self::poll)"] # [doc = ""] # [doc = " Currently the only actual dependency is from (2) to (1), however additional"] # [doc = " dependencies may be added in future, so the above order is recommended."] # [doc = ""] # [doc = " (A) may be called whenever desired."] # [doc = ""] # [doc = " Care should be made to ensure that the input events represent monotonically"] # [doc = " increasing time. Specifically, calling [`handle_timeout`](Self::handle_timeout)"] # [doc = " with events of the same [`Instant`] may be interleaved in any order with a"] # [doc = " call to [`handle_event`](Self::handle_event) at that same instant; however"] # [doc = " events or timeouts with different instants must not be interleaved."] pub struct Connection { endpoint_config : Arc < EndpointConfig > , config : Arc < TransportConfig > , rng : StdRng , crypto : Box < dyn crypto :: Session > , # [doc = " The CID we initially chose, for use during the handshake"] handshake_cid : ConnectionId , # [doc = " The CID the peer initially chose, for use during the handshake"] rem_handshake_cid : ConnectionId , # [doc = " The \"real\" local IP address which was was used to receive the initial packet."] # [doc = " This is only populated for the server case, and if known"] local_ip : Option < IpAddr > , path : PathData , # [doc = " Incremented every time we see a new path"] # [doc = ""] # [doc = " Stored separately from `path.generation` to account for aborted migrations"] path_counter : u64 , # [doc = " Whether MTU detection is supported in this environment"] allow_mtud : bool , prev_path : Option < (ConnectionId , PathData) > , state : State , side : ConnectionSide , # [doc = " Whether or not 0-RTT was enabled during the handshake. Does not imply acceptance."] zero_rtt_enabled : bool , # [doc = " Set if 0-RTT is supported, then cleared when no longer needed."] zero_rtt_crypto : Option < ZeroRttCrypto > , key_phase : bool , # [doc = " How many packets are in the current key phase. Used only for `Data` space."] key_phase_size : u64 , # [doc = " Transport parameters set by the peer"] peer_params : TransportParameters , # [doc = " Source ConnectionId of the first packet received from the peer"] orig_rem_cid : ConnectionId , # [doc = " Destination ConnectionId sent by the client on the first Initial"] initial_dst_cid : ConnectionId , # [doc = " The value that the server included in the Source Connection ID field of a Retry packet, if"] # [doc = " one was received"] retry_src_cid : Option < ConnectionId > , events : VecDeque < Event > , endpoint_events : VecDeque < EndpointEventInner > , # [doc = " Whether the spin bit is in use for this connection"] spin_enabled : bool , # [doc = " Outgoing spin bit state"] spin : bool , # [doc = " Packet number spaces: initial, handshake, 1-RTT"] spaces : [PacketSpace ; 3] , # [doc = " Highest usable packet number space"] highest_space : SpaceId , # [doc = " 1-RTT keys used prior to a key update"] prev_crypto : Option < PrevCrypto > , # [doc = " 1-RTT keys to be used for the next key update"] # [doc = ""] # [doc = " These are generated in advance to prevent timing attacks and/or DoS by third-party attackers"] # [doc = " spoofing key updates."] next_crypto : Option < KeyPair < Box < dyn PacketKey > > > , accepted_0rtt : bool , # [doc = " Whether the idle timer should be reset the next time an ack-eliciting packet is transmitted."] permit_idle_reset : bool , # [doc = " Negotiated idle timeout"] idle_timeout : Option < Duration > , timers : TimerTable , # [doc = " Number of packets received which could not be authenticated"] authentication_failures : u64 , # [doc = " Why the connection was lost, if it has been"] error : Option < ConnectionError > , # [doc = " Identifies Data-space packet numbers to skip. Not used in earlier spaces."] packet_number_filter : PacketNumberFilter , # [doc = " Responses to PATH_CHALLENGE frames"] path_responses : PathResponses , close : bool , ack_frequency : AckFrequencyState , # [doc = " The number of times a PTO has been sent without receiving an ack."] pto_count : u32 , # [doc = " Whether the most recently received packet had an ECN codepoint set"] receiving_ecn : bool , # [doc = " Number of packets authenticated"] total_authed_packets : u64 , # [doc = " Whether the last `poll_transmit` call yielded no data because there was"] # [doc = " no outgoing application data."] app_limited : bool , streams : StreamsState , # [doc = " Surplus remote CIDs for future use on new paths"] rem_cids : CidQueue , local_cid_state : CidState , # [doc = " State of the unreliable datagram extension"] datagrams : DatagramState , # [doc = " Connection level statistics"] stats : ConnectionStats , # [doc = " QUIC version used for the connection."] version : u32 , }
};
}
