// Generated macro for impl_653 (impl)
macro_rules! Depcrate_configimpl_653 {
() => {
// Module: crate::config
// Provides: {"impl_653"}
// Dependencies: {}
impl EndpointConfig { # [doc = " Create a default config with a particular `reset_key`"] pub fn new (reset_key : Arc < dyn HmacKey >) -> Self { let cid_factory = | | -> Box < dyn ConnectionIdGenerator > { Box :: < HashedConnectionIdGenerator > :: default () } ; Self { reset_key , max_udp_payload_size : (1500u32 - 28) . into () , connection_id_generator_factory : Arc :: new (cid_factory) , supported_versions : DEFAULT_SUPPORTED_VERSIONS . to_vec () , grease_quic_bit : true , min_reset_interval : Duration :: from_millis (20) , rng_seed : None , } } # [doc = " Supply a custom connection ID generator factory"] # [doc = ""] # [doc = " Called once by each `Endpoint` constructed from this configuration to obtain the CID"] # [doc = " generator which will be used to generate the CIDs used for incoming packets on all"] # [doc = " connections involving that  `Endpoint`. A custom CID generator allows applications to embed"] # [doc = " information in local connection IDs, e.g. to support stateless packet-level load balancers."] # [doc = ""] # [doc = " Defaults to [`HashedConnectionIdGenerator`]."] pub fn cid_generator < F : Fn () -> Box < dyn ConnectionIdGenerator > + Send + Sync + 'static > (& mut self , factory : F ,) -> & mut Self { self . connection_id_generator_factory = Arc :: new (factory) ; self } # [doc = " Private key used to send authenticated connection resets to peers who were"] # [doc = " communicating with a previous instance of this endpoint."] pub fn reset_key (& mut self , key : Arc < dyn HmacKey >) -> & mut Self { self . reset_key = key ; self } # [doc = " Maximum UDP payload size accepted from peers (excluding UDP and IP overhead)."] # [doc = ""] # [doc = " Must be greater or equal than 1200."] # [doc = ""] # [doc = " Defaults to 1472, which is the largest UDP payload that can be transmitted in the typical"] # [doc = " 1500 byte Ethernet MTU. Deployments on links with larger MTUs (e.g. loopback or Ethernet"] # [doc = " with jumbo frames) can raise this to improve performance at the cost of a linear increase in"] # [doc = " datagram receive buffer size."] pub fn max_udp_payload_size (& mut self , value : u16) -> Result < & mut Self , ConfigError > { if ! (1200 ..= 65_527) . contains (& value) { return Err (ConfigError :: OutOfBounds) ; } self . max_udp_payload_size = value . into () ; Ok (self) } # [doc = " Get the current value of [`max_udp_payload_size`](Self::max_udp_payload_size)"] pub fn get_max_udp_payload_size (& self) -> u64 { self . max_udp_payload_size . into () } # [doc = " Override supported QUIC versions"] pub fn supported_versions (& mut self , supported_versions : Vec < u32 >) -> & mut Self { self . supported_versions = supported_versions ; self } # [doc = " Whether to accept QUIC packets containing any value for the fixed bit"] # [doc = ""] # [doc = " Enabled by default. Helps protect against protocol ossification and makes traffic less"] # [doc = " identifiable to observers. Disable if helping observers identify this traffic as QUIC is"] # [doc = " desired."] pub fn grease_quic_bit (& mut self , value : bool) -> & mut Self { self . grease_quic_bit = value ; self } # [doc = " Minimum interval between outgoing stateless reset packets"] # [doc = ""] # [doc = " Defaults to 20ms. Limits the impact of attacks which flood an endpoint with garbage packets,"] # [doc = " e.g. [ISAKMP/IKE amplification]. Larger values provide a stronger defense, but may delay"] # [doc = " detection of some error conditions by clients. Using a [`ConnectionIdGenerator`] with a low"] # [doc = " rate of false positives in [`validate`](ConnectionIdGenerator::validate) reduces the risk"] # [doc = " incurred by a small minimum reset interval."] # [doc = ""] # [doc = " [ISAKMP/IKE"] # [doc = " amplification]: https://bughunters.google.com/blog/5960150648750080/preventing-cross-service-udp-loops-in-quic#isakmp-ike-amplification-vs-quic"] pub fn min_reset_interval (& mut self , value : Duration) -> & mut Self { self . min_reset_interval = value ; self } # [doc = " Optional seed to be used internally for random number generation"] # [doc = ""] # [doc = " By default, quinn will initialize an endpoint's rng using a platform entropy source."] # [doc = " However, you can seed the rng yourself through this method (e.g. if you need to run quinn"] # [doc = " deterministically or if you are using quinn in an environment that doesn't have a source of"] # [doc = " entropy available)."] pub fn rng_seed (& mut self , seed : Option < [u8 ; 32] >) -> & mut Self { self . rng_seed = seed ; self } }
};
}
