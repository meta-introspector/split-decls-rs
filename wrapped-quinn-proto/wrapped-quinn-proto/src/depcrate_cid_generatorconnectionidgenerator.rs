// Generated macro for ConnectionIdGenerator (trait)
macro_rules! Depcrate_cid_generatorConnectionIdGenerator {
() => {
// Module: crate::cid_generator
// Provides: {"ConnectionIdGenerator"}
// Dependencies: {}
# [doc = " Generates connection IDs for incoming connections"] pub trait ConnectionIdGenerator : Send + Sync { # [doc = " Generates a new CID"] # [doc = ""] # [doc = " Connection IDs MUST NOT contain any information that can be used by"] # [doc = " an external observer (that is, one that does not cooperate with the"] # [doc = " issuer) to correlate them with other connection IDs for the same"] # [doc = " connection. They MUST have high entropy, e.g. due to encrypted data"] # [doc = " or cryptographic-grade random data."] fn generate_cid (& mut self) -> ConnectionId ; # [doc = " Quickly determine whether `cid` could have been generated by this generator"] # [doc = ""] # [doc = " False positives are permitted, but increase the cost of handling invalid packets."] fn validate (& self , _cid : ConnectionId) -> Result < () , InvalidCid > { Ok (()) } # [doc = " Returns the length of a CID for connections created by this generator"] fn cid_len (& self) -> usize ; # [doc = " Returns the lifetime of generated Connection IDs"] # [doc = ""] # [doc = " Connection IDs will be retired after the returned `Duration`, if any. Assumed to be constant."] fn cid_lifetime (& self) -> Option < Duration > ; }
};
}
