// Generated macro for impl_638 (impl)
macro_rules! Depcrate_config_transportimpl_638 {
() => {
// Module: crate::config::transport
// Provides: {"impl_638"}
// Dependencies: {}
impl AckFrequencyConfig { # [doc = " The ack-eliciting threshold we will request the peer to use"] # [doc = ""] # [doc = " This threshold represents the number of ack-eliciting packets an endpoint may receive"] # [doc = " without immediately sending an ACK."] # [doc = ""] # [doc = " The remote peer should send at least one ACK frame when more than this number of"] # [doc = " ack-eliciting packets have been received. A value of 0 results in a receiver immediately"] # [doc = " acknowledging every ack-eliciting packet."] # [doc = ""] # [doc = " Defaults to 1, which sends ACK frames for every other ack-eliciting packet."] pub fn ack_eliciting_threshold (& mut self , value : VarInt) -> & mut Self { self . ack_eliciting_threshold = value ; self } # [doc = " The `max_ack_delay` we will request the peer to use"] # [doc = ""] # [doc = " This parameter represents the maximum amount of time that an endpoint waits before sending"] # [doc = " an ACK when the ack-eliciting threshold hasn't been reached."] # [doc = ""] # [doc = " The effective `max_ack_delay` will be clamped to be at least the peer's `min_ack_delay`"] # [doc = " transport parameter, and at most the greater of the current path RTT or 25ms."] # [doc = ""] # [doc = " Defaults to `None`, in which case the peer's original `max_ack_delay` will be used, as"] # [doc = " obtained from its transport parameters."] pub fn max_ack_delay (& mut self , value : Option < Duration >) -> & mut Self { self . max_ack_delay = value ; self } # [doc = " The reordering threshold we will request the peer to use"] # [doc = ""] # [doc = " This threshold represents the amount of out-of-order packets that will trigger an endpoint"] # [doc = " to send an ACK, without waiting for `ack_eliciting_threshold` to be exceeded or for"] # [doc = " `max_ack_delay` to be elapsed."] # [doc = ""] # [doc = " A value of 0 indicates out-of-order packets do not elicit an immediate ACK. A value of 1"] # [doc = " immediately acknowledges any packets that are received out of order (this is also the"] # [doc = " behavior when the extension is disabled)."] # [doc = ""] # [doc = " It is recommended to set this value to [`TransportConfig::packet_threshold`] minus one."] # [doc = " Since the default value for [`TransportConfig::packet_threshold`] is 3, this value defaults"] # [doc = " to 2."] pub fn reordering_threshold (& mut self , value : VarInt) -> & mut Self { self . reordering_threshold = value ; self } }
};
}
