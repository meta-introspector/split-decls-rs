// Generated macro for StreamsState (struct)
macro_rules! Depcrate_connection_streams_stateStreamsState {
() => {
// Module: crate::connection::streams::state
// Provides: {"StreamsState"}
// Dependencies: {}
# [allow (unreachable_pub)] pub struct StreamsState { pub (super) side : Side , pub (super) send : FxHashMap < StreamId , Option < Box < Send > > > , pub (super) recv : FxHashMap < StreamId , Option < StreamRecv > > , pub (super) free_recv : Vec < StreamRecv > , pub (super) next : [u64 ; 2] , # [doc = " Maximum number of locally-initiated streams that may be opened over the lifetime of the"] # [doc = " connection so far, per direction"] pub (super) max : [u64 ; 2] , # [doc = " Maximum number of remotely-initiated streams that may be opened over the lifetime of the"] # [doc = " connection so far, per direction"] pub (super) max_remote : [u64 ; 2] , # [doc = " Value of `max_remote` most recently transmitted to the peer in a `MAX_STREAMS` frame"] sent_max_remote : [u64 ; 2] , # [doc = " Number of streams that we've given the peer permission to open and which aren't fully closed"] pub (super) allocated_remote_count : [u64 ; 2] , # [doc = " Size of the desired stream flow control window. May be smaller than `allocated_remote_count`"] # [doc = " due to `set_max_concurrent` calls."] max_concurrent_remote_count : [u64 ; 2] , # [doc = " Whether `max_concurrent_remote_count` has ever changed"] flow_control_adjusted : bool , # [doc = " Lowest remotely-initiated stream index that haven't actually been opened by the peer"] pub (super) next_remote : [u64 ; 2] , # [doc = " Whether the remote endpoint has opened any streams the application doesn't know about yet,"] # [doc = " per directionality"] opened : [bool ; 2] , pub (super) next_reported_remote : [u64 ; 2] , # [doc = " Number of outbound streams"] # [doc = ""] # [doc = " This differs from `self.send.len()` in that it does not include streams that the peer is"] # [doc = " permitted to open but which have not yet been opened."] pub (super) send_streams : usize , # [doc = " Streams with outgoing data queued, sorted by priority"] pub (super) pending : PendingStreamsQueue , events : VecDeque < StreamEvent > , # [doc = " Streams blocked on connection-level flow control or stream window space"] # [doc = ""] # [doc = " Streams are only added to this list when a write fails."] pub (super) connection_blocked : Vec < StreamId > , # [doc = " Connection-level flow control budget dictated by the peer"] pub (super) max_data : u64 , # [doc = " The initial receive window"] receive_window : u64 , # [doc = " Limit on incoming data, which is transmitted through `MAX_DATA` frames"] local_max_data : u64 , # [doc = " The last value of `MAX_DATA` which had been queued for transmission in"] # [doc = " an outgoing `MAX_DATA` frame"] sent_max_data : VarInt , # [doc = " Sum of current offsets of all send streams."] pub (super) data_sent : u64 , # [doc = " Sum of end offsets of all receive streams. Includes gaps, so it's an upper bound."] data_recvd : u64 , # [doc = " Total quantity of unacknowledged outgoing data"] pub (super) unacked_data : u64 , # [doc = " Configured upper bound for `unacked_data`."] # [doc = ""] # [doc = " Note this may be less than `unacked_data` if the user has set a new value."] pub (super) send_window : u64 , # [doc = " Configured upper bound for how much unacked data the peer can send us per stream"] pub (super) stream_receive_window : u64 , initial_max_stream_data_uni : VarInt , initial_max_stream_data_bidi_local : VarInt , initial_max_stream_data_bidi_remote : VarInt , # [doc = " The shrink to be applied to local_max_data when receive_window is shrunk"] receive_window_shrink_debt : u64 , }
};
}
