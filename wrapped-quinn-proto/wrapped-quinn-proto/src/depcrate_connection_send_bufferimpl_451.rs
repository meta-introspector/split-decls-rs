// Generated macro for impl_451 (impl)
macro_rules! Depcrate_connection_send_bufferimpl_451 {
() => {
// Module: crate::connection::send_buffer
// Provides: {"impl_451"}
// Dependencies: {}
impl SendBuffer { # [doc = " Construct an empty buffer at the initial offset"] pub (super) fn new () -> Self { Self :: default () } # [doc = " Append application data to the end of the stream"] pub (super) fn write (& mut self , data : Bytes) { self . unacked_len += data . len () ; self . offset += data . len () as u64 ; self . unacked_segments . push_back (data) ; } # [doc = " Discard a range of acknowledged stream data"] pub (super) fn ack (& mut self , mut range : Range < u64 >) { let base_offset = self . offset - self . unacked_len as u64 ; range . start = base_offset . max (range . start) ; range . end = base_offset . max (range . end) ; self . acks . insert (range) ; while self . acks . min () == Some (self . offset - self . unacked_len as u64) { let prefix = self . acks . pop_min () . unwrap () ; let mut to_advance = (prefix . end - prefix . start) as usize ; self . unacked_len -= to_advance ; while to_advance > 0 { let front = self . unacked_segments . front_mut () . expect ("Expected buffered data") ; if front . len () <= to_advance { to_advance -= front . len () ; self . unacked_segments . pop_front () ; if self . unacked_segments . len () * 4 < self . unacked_segments . capacity () { self . unacked_segments . shrink_to_fit () ; } } else { front . advance (to_advance) ; to_advance = 0 ; } } } } # [doc = " Compute the next range to transmit on this stream and update state to account for that"] # [doc = " transmission."] # [doc = ""] # [doc = " `max_len` here includes the space which is available to transmit the"] # [doc = " offset and length of the data to send. The caller has to guarantee that"] # [doc = " there is at least enough space available to write maximum-sized metadata"] # [doc = " (8 byte offset + 8 byte length)."] # [doc = ""] # [doc = " The method returns a tuple:"] # [doc = " - The first return value indicates the range of data to send"] # [doc = " - The second return value indicates whether the length needs to be encoded"] # [doc = "   in the STREAM frames metadata (`true`), or whether it can be omitted"] # [doc = "   since the selected range will fill the whole packet."] pub (super) fn poll_transmit (& mut self , mut max_len : usize) -> (Range < u64 > , bool) { debug_assert ! (max_len >= 8 + 8) ; let mut encode_length = false ; if let Some (range) = self . retransmits . pop_min () { if range . start != 0 { max_len -= VarInt :: size (unsafe { VarInt :: from_u64_unchecked (range . start) }) ; } if range . end - range . start < max_len as u64 { encode_length = true ; max_len -= 8 ; } let end = range . end . min ((max_len as u64) . saturating_add (range . start)) ; if end != range . end { self . retransmits . insert (end .. range . end) ; } return (range . start .. end , encode_length) ; } if self . unsent != 0 { max_len -= VarInt :: size (unsafe { VarInt :: from_u64_unchecked (self . unsent) }) ; } if self . offset - self . unsent < max_len as u64 { encode_length = true ; max_len -= 8 ; } let end = self . offset . min ((max_len as u64) . saturating_add (self . unsent)) ; let result = self . unsent .. end ; self . unsent = end ; (result , encode_length) } # [doc = " Returns data which is associated with a range"] # [doc = ""] # [doc = " This function can return a subset of the range, if the data is stored"] # [doc = " in noncontiguous fashion in the send buffer. In this case callers"] # [doc = " should call the function again with an incremented start offset to"] # [doc = " retrieve more data."] pub (super) fn get (& self , offsets : Range < u64 >) -> & [u8] { let base_offset = self . offset - self . unacked_len as u64 ; let mut segment_offset = base_offset ; for segment in self . unacked_segments . iter () { if offsets . start >= segment_offset && offsets . start < segment_offset + segment . len () as u64 { let start = (offsets . start - segment_offset) as usize ; let end = (offsets . end - segment_offset) as usize ; return & segment [start .. end . min (segment . len ())] ; } segment_offset += segment . len () as u64 ; } & [] } # [doc = " Queue a range of sent but unacknowledged data to be retransmitted"] pub (super) fn retransmit (& mut self , range : Range < u64 >) { debug_assert ! (range . end <= self . unsent , "unsent data can't be lost") ; self . retransmits . insert (range) ; } pub (super) fn retransmit_all_for_0rtt (& mut self) { debug_assert_eq ! (self . offset , self . unacked_len as u64) ; self . unsent = 0 ; } # [doc = " First stream offset unwritten by the application, i.e. the offset that the next write will"] # [doc = " begin at"] pub (super) fn offset (& self) -> u64 { self . offset } # [doc = " Whether all sent data has been acknowledged"] pub (super) fn is_fully_acked (& self) -> bool { self . unacked_len == 0 } # [doc = " Whether there's data to send"] # [doc = ""] # [doc = " There may be sent unacknowledged data even when this is false."] pub (super) fn has_unsent_data (& self) -> bool { self . unsent != self . offset || ! self . retransmits . is_empty () } # [doc = " Compute the amount of data that hasn't been acknowledged"] pub (super) fn unacked (& self) -> u64 { self . unacked_len as u64 - self . acks . iter () . map (| x | x . end - x . start) . sum :: < u64 > () } }
};
}
