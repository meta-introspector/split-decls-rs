// Generated macro for TokenLog (trait)
macro_rules! Depcrate_tokenTokenLog {
() => {
// Module: crate::token
// Provides: {"TokenLog"}
// Dependencies: {}
# [doc = " Responsible for limiting clients' ability to reuse validation tokens"] # [doc = ""] # [doc = " [_RFC 9000 § 8.1.4:_](https://www.rfc-editor.org/rfc/rfc9000.html#section-8.1.4)"] # [doc = ""] # [doc = " > Attackers could replay tokens to use servers as amplifiers in DDoS attacks. To protect"] # [doc = " > against such attacks, servers MUST ensure that replay of tokens is prevented or limited."] # [doc = " > Servers SHOULD ensure that tokens sent in Retry packets are only accepted for a short time,"] # [doc = " > as they are returned immediately by clients. Tokens that are provided in NEW_TOKEN frames"] # [doc = " > (Section 19.7) need to be valid for longer but SHOULD NOT be accepted multiple times."] # [doc = " > Servers are encouraged to allow tokens to be used only once, if possible; tokens MAY include"] # [doc = " > additional information about clients to further narrow applicability or reuse."] # [doc = ""] # [doc = " `TokenLog` pertains only to tokens provided in NEW_TOKEN frames."] pub trait TokenLog : Send + Sync { # [doc = " Record that the token was used and, ideally, return a token reuse error if the token may"] # [doc = " have been already used previously"] # [doc = ""] # [doc = " False negatives and false positives are both permissible. Called when a client uses an"] # [doc = " address validation token."] # [doc = ""] # [doc = " Parameters:"] # [doc = " - `nonce`: A server-generated random unique value for the token."] # [doc = " - `issued`: The time the server issued the token."] # [doc = " - `lifetime`: The expiration time of address validation tokens sent via NEW_TOKEN frames,"] # [doc = "   as configured by [`ServerValidationTokenConfig::lifetime`][1]."] # [doc = ""] # [doc = " [1]: crate::ValidationTokenConfig::lifetime"] # [doc = ""] # [doc = " ## Security & Performance"] # [doc = ""] # [doc = " To the extent that it is possible to repeatedly trigger false negatives (returning `Ok` for"] # [doc = " a token which has been reused), an attacker could use the server to perform [amplification"] # [doc = " attacks][2]. The QUIC specification requires that this be limited, if not prevented fully."] # [doc = ""] # [doc = " A false positive (returning `Err` for a token which has never been used) is not a security"] # [doc = " vulnerability; it is permissible for a `TokenLog` to always return `Err`. A false positive"] # [doc = " causes the token to be ignored, which may cause the transmission of some 0.5-RTT data to be"] # [doc = " delayed until the handshake completes, if a sufficient amount of 0.5-RTT data it sent."] # [doc = ""] # [doc = " [2]: https://en.wikipedia.org/wiki/Denial-of-service_attack#Amplification"] fn check_and_insert (& self , nonce : u128 , issued : SystemTime , lifetime : Duration ,) -> Result < () , TokenReuseError > ; }
};
}
