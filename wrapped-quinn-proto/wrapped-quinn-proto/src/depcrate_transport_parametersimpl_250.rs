// Generated macro for impl_250 (impl)
macro_rules! Depcrate_transport_parametersimpl_250 {
() => {
// Module: crate::transport_parameters
// Provides: {"impl_250"}
// Dependencies: {}
impl ReservedTransportParameter { # [doc = " Generates a transport parameter with a random payload and a reserved ID."] # [doc = ""] # [doc = " The implementation is inspired by quic-go and quiche:"] # [doc = " 1. <https://github.com/quic-go/quic-go/blob/3e0a67b2476e1819752f04d75968de042b197b56/internal/wire/transport_parameters.go#L338-L344>"] # [doc = " 2. <https://github.com/google/quiche/blob/cb1090b20c40e2f0815107857324e99acf6ec567/quiche/quic/core/crypto/transport_parameters.cc#L843-L860>"] fn random (rng : & mut impl RngCore) -> Self { let id = Self :: generate_reserved_id (rng) ; let payload_len = rng . random_range (0 .. Self :: MAX_PAYLOAD_LEN) ; let payload = { let mut slice = [0u8 ; Self :: MAX_PAYLOAD_LEN] ; rng . fill_bytes (& mut slice [.. payload_len]) ; slice } ; Self { id , payload , payload_len , } } fn write (& self , w : & mut impl BufMut) { w . write_var (self . id . 0) ; w . write_var (self . payload_len as u64) ; w . put_slice (& self . payload [.. self . payload_len]) ; } # [doc = " Generates a random reserved identifier of the form `31 * N + 27`, as required by RFC 9000."] # [doc = " Reserved transport parameter identifiers are used to test compliance with the requirement"] # [doc = " that unknown transport parameters must be ignored by peers."] # [doc = " See: <https://www.rfc-editor.org/rfc/rfc9000.html#section-18.1> and <https://www.rfc-editor.org/rfc/rfc9000.html#section-22.3>"] fn generate_reserved_id (rng : & mut impl RngCore) -> VarInt { let id = { let rand = rng . random_range (0u64 .. (1 << 62) - 27) ; let n = rand / 31 ; 31 * n + 27 } ; debug_assert ! (id % 31 == 27 , "generated id does not have the form of 31 * N + 27") ; VarInt :: from_u64 (id) . expect ("generated id does fit into range of allowed transport parameter IDs: [0; 2^62)" ,) } # [doc = " The maximum length of the payload to include as the parameter payload."] # [doc = " This value is not a specification-imposed limit but is chosen to match"] # [doc = " the limit used by other implementations of QUIC, e.g., quic-go and quiche."] const MAX_PAYLOAD_LEN : usize = 16 ; }
};
}
