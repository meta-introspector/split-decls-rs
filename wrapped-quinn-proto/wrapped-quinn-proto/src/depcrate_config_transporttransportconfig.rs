// Generated macro for TransportConfig (struct)
macro_rules! Depcrate_config_transportTransportConfig {
() => {
// Module: crate::config::transport
// Provides: {"TransportConfig"}
// Dependencies: {}
# [doc = " Parameters governing the core QUIC state machine"] # [doc = ""] # [doc = " Default values should be suitable for most internet applications. Applications protocols which"] # [doc = " forbid remotely-initiated streams should set `max_concurrent_bidi_streams` and"] # [doc = " `max_concurrent_uni_streams` to zero."] # [doc = ""] # [doc = " In some cases, performance or resource requirements can be improved by tuning these values to"] # [doc = " suit a particular application and/or network connection. In particular, data window sizes can be"] # [doc = " tuned for a particular expected round trip time, link capacity, and memory availability. Tuning"] # [doc = " for higher bandwidths and latencies increases worst-case memory consumption, but does not impair"] # [doc = " performance at lower bandwidths and latencies. The default configuration is tuned for a 100Mbps"] # [doc = " link with a 100ms round trip time."] pub struct TransportConfig { pub (crate) max_concurrent_bidi_streams : VarInt , pub (crate) max_concurrent_uni_streams : VarInt , pub (crate) max_idle_timeout : Option < VarInt > , pub (crate) stream_receive_window : VarInt , pub (crate) receive_window : VarInt , pub (crate) send_window : u64 , pub (crate) send_fairness : bool , pub (crate) packet_threshold : u32 , pub (crate) time_threshold : f32 , pub (crate) initial_rtt : Duration , pub (crate) initial_mtu : u16 , pub (crate) min_mtu : u16 , pub (crate) mtu_discovery_config : Option < MtuDiscoveryConfig > , pub (crate) pad_to_mtu : bool , pub (crate) ack_frequency_config : Option < AckFrequencyConfig > , pub (crate) persistent_congestion_threshold : u32 , pub (crate) keep_alive_interval : Option < Duration > , pub (crate) crypto_buffer_size : usize , pub (crate) allow_spin : bool , pub (crate) datagram_receive_buffer_size : Option < usize > , pub (crate) datagram_send_buffer_size : usize , # [cfg (test)] pub (crate) deterministic_packet_numbers : bool , pub (crate) congestion_controller_factory : Arc < dyn congestion :: ControllerFactory + Send + Sync > , pub (crate) enable_segmentation_offload : bool , pub (crate) qlog_sink : QlogSink , }
};
}
