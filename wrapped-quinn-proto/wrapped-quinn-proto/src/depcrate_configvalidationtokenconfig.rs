// Generated macro for ValidationTokenConfig (struct)
macro_rules! Depcrate_configValidationTokenConfig {
() => {
// Module: crate::config
// Provides: {"ValidationTokenConfig"}
// Dependencies: {}
# [doc = " Configuration for sending and handling validation tokens in incoming connections"] # [doc = ""] # [doc = " Default values should be suitable for most internet applications."] # [doc = ""] # [doc = " ## QUIC Tokens"] # [doc = ""] # [doc = " The QUIC protocol defines a concept of \"[address validation][1]\". Essentially, one side of a"] # [doc = " QUIC connection may appear to be receiving QUIC packets from a particular remote UDP address,"] # [doc = " but it will only consider that remote address \"validated\" once it has convincing evidence that"] # [doc = " the address is not being [spoofed][2]."] # [doc = ""] # [doc = " Validation is important primarily because of QUIC's \"anti-amplification limit.\" This limit"] # [doc = " prevents a QUIC server from sending a client more than three times the number of bytes it has"] # [doc = " received from the client on a given address until that address is validated. This is designed"] # [doc = " to mitigate the ability of attackers to use QUIC-based servers as reflectors in [amplification"] # [doc = " attacks][3]."] # [doc = ""] # [doc = " A path may become validated in several ways. The server is always considered validated by the"] # [doc = " client. The client usually begins in an unvalidated state upon first connecting or migrating,"] # [doc = " but then becomes validated through various mechanisms that usually take one network round trip."] # [doc = " However, in some cases, a client which has previously attempted to connect to a server may have"] # [doc = " been given a one-time use cryptographically secured \"token\" that it can send in a subsequent"] # [doc = " connection attempt to be validated immediately."] # [doc = ""] # [doc = " There are two ways these tokens can originate:"] # [doc = ""] # [doc = " - If the server responds to an incoming connection with `retry`, a \"retry token\" is minted and"] # [doc = "   sent to the client, which the client immediately uses to attempt to connect again. Retry"] # [doc = "   tokens operate on short timescales, such as 15 seconds."] # [doc = " - If a client's path within an active connection is validated, the server may send the client"] # [doc = "   one or more \"validation tokens,\" which the client may store for use in later connections to"] # [doc = "   the same server. Validation tokens may be valid for much longer lifetimes than retry token."] # [doc = ""] # [doc = " The usage of validation tokens is most impactful in situations where 0-RTT data is also being"] # [doc = " used--in particular, in situations where the server sends the client more than three times more"] # [doc = " 0.5-RTT data than it has received 0-RTT data. Since the successful completion of a connection"] # [doc = " handshake implicitly causes the client's address to be validated, transmission of 0.5-RTT data"] # [doc = " is the main situation where a server might be sending application data to an address that could"] # [doc = " be validated by token usage earlier than it would become validated without token usage."] # [doc = ""] # [doc = " [1]: https://www.rfc-editor.org/rfc/rfc9000.html#section-8"] # [doc = " [2]: https://en.wikipedia.org/wiki/IP_address_spoofing"] # [doc = " [3]: https://en.wikipedia.org/wiki/Denial-of-service_attack#Amplification"] # [doc = ""] # [doc = " These tokens should not be confused with \"stateless reset tokens,\" which are similarly named"] # [doc = " but entirely unrelated."] # [derive (Clone)] pub struct ValidationTokenConfig { pub (crate) lifetime : Duration , pub (crate) log : Arc < dyn TokenLog > , pub (crate) sent : u32 , }
};
}
