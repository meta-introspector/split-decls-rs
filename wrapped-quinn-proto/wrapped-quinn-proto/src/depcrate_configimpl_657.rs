// Generated macro for impl_657 (impl)
macro_rules! Depcrate_configimpl_657 {
() => {
// Module: crate::config
// Provides: {"impl_657"}
// Dependencies: {}
impl ServerConfig { # [doc = " Create a default config with a particular handshake token key"] pub fn new (crypto : Arc < dyn crypto :: ServerConfig > , token_key : Arc < dyn HandshakeTokenKey > ,) -> Self { Self { transport : Arc :: new (TransportConfig :: default ()) , crypto , token_key , retry_token_lifetime : Duration :: from_secs (15) , migration : true , validation_token : ValidationTokenConfig :: default () , preferred_address_v4 : None , preferred_address_v6 : None , max_incoming : 1 << 16 , incoming_buffer_size : 10 << 20 , incoming_buffer_size_total : 100 << 20 , time_source : Arc :: new (StdSystemTime) , } } # [doc = " Set a custom [`TransportConfig`]"] pub fn transport_config (& mut self , transport : Arc < TransportConfig >) -> & mut Self { self . transport = transport ; self } # [doc = " Set a custom [`ValidationTokenConfig`]"] pub fn validation_token_config (& mut self , validation_token : ValidationTokenConfig ,) -> & mut Self { self . validation_token = validation_token ; self } # [doc = " Private key used to authenticate data included in handshake tokens"] pub fn token_key (& mut self , value : Arc < dyn HandshakeTokenKey >) -> & mut Self { self . token_key = value ; self } # [doc = " Duration after a retry token was issued for which it's considered valid"] # [doc = ""] # [doc = " Defaults to 15 seconds."] pub fn retry_token_lifetime (& mut self , value : Duration) -> & mut Self { self . retry_token_lifetime = value ; self } # [doc = " Whether to allow clients to migrate to new addresses"] # [doc = ""] # [doc = " Improves behavior for clients that move between different internet connections or suffer NAT"] # [doc = " rebinding. Enabled by default."] pub fn migration (& mut self , value : bool) -> & mut Self { self . migration = value ; self } # [doc = " The preferred IPv4 address that will be communicated to clients during handshaking"] # [doc = ""] # [doc = " If the client is able to reach this address, it will switch to it."] pub fn preferred_address_v4 (& mut self , address : Option < SocketAddrV4 >) -> & mut Self { self . preferred_address_v4 = address ; self } # [doc = " The preferred IPv6 address that will be communicated to clients during handshaking"] # [doc = ""] # [doc = " If the client is able to reach this address, it will switch to it."] pub fn preferred_address_v6 (& mut self , address : Option < SocketAddrV6 >) -> & mut Self { self . preferred_address_v6 = address ; self } # [doc = " Maximum number of [`Incoming`][crate::Incoming] to allow to exist at a time"] # [doc = ""] # [doc = " An [`Incoming`][crate::Incoming] comes into existence when an incoming connection attempt"] # [doc = " is received and stops existing when the application either accepts it or otherwise disposes"] # [doc = " of it. While this limit is reached, new incoming connection attempts are immediately"] # [doc = " refused. Larger values have greater worst-case memory consumption, but accommodate greater"] # [doc = " application latency in handling incoming connection attempts."] # [doc = ""] # [doc = " The default value is set to 65536. With a typical Ethernet MTU of 1500 bytes, this limits"] # [doc = " memory consumption from this to under 100 MiB--a generous amount that still prevents memory"] # [doc = " exhaustion in most contexts."] pub fn max_incoming (& mut self , max_incoming : usize) -> & mut Self { self . max_incoming = max_incoming ; self } # [doc = " Maximum number of received bytes to buffer for each [`Incoming`][crate::Incoming]"] # [doc = ""] # [doc = " An [`Incoming`][crate::Incoming] comes into existence when an incoming connection attempt"] # [doc = " is received and stops existing when the application either accepts it or otherwise disposes"] # [doc = " of it. This limit governs only packets received within that period, and does not include"] # [doc = " the first packet. Packets received in excess of this limit are dropped, which may cause"] # [doc = " 0-RTT or handshake data to have to be retransmitted."] # [doc = ""] # [doc = " The default value is set to 10 MiB--an amount such that in most situations a client would"] # [doc = " not transmit that much 0-RTT data faster than the server handles the corresponding"] # [doc = " [`Incoming`][crate::Incoming]."] pub fn incoming_buffer_size (& mut self , incoming_buffer_size : u64) -> & mut Self { self . incoming_buffer_size = incoming_buffer_size ; self } # [doc = " Maximum number of received bytes to buffer for all [`Incoming`][crate::Incoming]"] # [doc = " collectively"] # [doc = ""] # [doc = " An [`Incoming`][crate::Incoming] comes into existence when an incoming connection attempt"] # [doc = " is received and stops existing when the application either accepts it or otherwise disposes"] # [doc = " of it. This limit governs only packets received within that period, and does not include"] # [doc = " the first packet. Packets received in excess of this limit are dropped, which may cause"] # [doc = " 0-RTT or handshake data to have to be retransmitted."] # [doc = ""] # [doc = " The default value is set to 100 MiB--a generous amount that still prevents memory"] # [doc = " exhaustion in most contexts."] pub fn incoming_buffer_size_total (& mut self , incoming_buffer_size_total : u64) -> & mut Self { self . incoming_buffer_size_total = incoming_buffer_size_total ; self } # [doc = " Object to get current [`SystemTime`]"] # [doc = ""] # [doc = " This exists to allow system time to be mocked in tests, or wherever else desired."] # [doc = ""] # [doc = " Defaults to [`StdSystemTime`], which simply calls [`SystemTime::now()`](SystemTime::now)."] pub fn time_source (& mut self , time_source : Arc < dyn TimeSource >) -> & mut Self { self . time_source = time_source ; self } pub (crate) fn has_preferred_address (& self) -> bool { self . preferred_address_v4 . is_some () || self . preferred_address_v6 . is_some () } }
};
}
