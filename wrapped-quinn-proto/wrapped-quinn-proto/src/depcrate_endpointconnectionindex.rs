// Generated macro for ConnectionIndex (struct)
macro_rules! Depcrate_endpointConnectionIndex {
() => {
// Module: crate::endpoint
// Provides: {"ConnectionIndex"}
// Dependencies: {}
# [doc = " Maps packets to existing connections"] # [derive (Default , Debug)] struct ConnectionIndex { # [doc = " Identifies connections based on the initial DCID the peer utilized"] # [doc = ""] # [doc = " Uses a standard `HashMap` to protect against hash collision attacks."] # [doc = ""] # [doc = " Used by the server, not the client."] connection_ids_initial : HashMap < ConnectionId , RouteDatagramTo > , # [doc = " Identifies connections based on locally created CIDs"] # [doc = ""] # [doc = " Uses a cheaper hash function since keys are locally created"] connection_ids : FxHashMap < ConnectionId , ConnectionHandle > , # [doc = " Identifies incoming connections with zero-length CIDs"] # [doc = ""] # [doc = " Uses a standard `HashMap` to protect against hash collision attacks."] incoming_connection_remotes : HashMap < FourTuple , ConnectionHandle > , # [doc = " Identifies outgoing connections with zero-length CIDs"] # [doc = ""] # [doc = " We don't yet support explicit source addresses for client connections, and zero-length CIDs"] # [doc = " require a unique four-tuple, so at most one client connection with zero-length local CIDs"] # [doc = " may be established per remote. We must omit the local address from the key because we don't"] # [doc = " necessarily know what address we're sending from, and hence receiving at."] # [doc = ""] # [doc = " Uses a standard `HashMap` to protect against hash collision attacks."] outgoing_connection_remotes : HashMap < SocketAddr , ConnectionHandle > , # [doc = " Reset tokens provided by the peer for the CID each connection is currently sending to"] # [doc = ""] # [doc = " Incoming stateless resets do not have correct CIDs, so we need this to identify the correct"] # [doc = " recipient, if any."] connection_reset_tokens : ResetTokenTable , }
};
}
