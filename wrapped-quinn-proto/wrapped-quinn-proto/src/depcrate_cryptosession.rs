// Generated macro for Session (trait)
macro_rules! Depcrate_cryptoSession {
() => {
// Module: crate::crypto
// Provides: {"Session"}
// Dependencies: {}
# [doc = " A cryptographic session (commonly TLS)"] pub trait Session : Send + Sync + 'static { # [doc = " Create the initial set of keys given the client's initial destination ConnectionId"] fn initial_keys (& self , dst_cid : ConnectionId , side : Side) -> Keys ; # [doc = " Get data negotiated during the handshake, if available"] # [doc = ""] # [doc = " Returns `None` until the connection emits `HandshakeDataReady`."] fn handshake_data (& self) -> Option < Box < dyn Any > > ; # [doc = " Get the peer's identity, if available"] fn peer_identity (& self) -> Option < Box < dyn Any > > ; # [doc = " Get the 0-RTT keys if available (clients only)"] # [doc = ""] # [doc = " On the client side, this method can be used to see if 0-RTT key material is available"] # [doc = " to start sending data before the protocol handshake has completed."] # [doc = ""] # [doc = " Returns `None` if the key material is not available. This might happen if you have"] # [doc = " not connected to this server before."] fn early_crypto (& self) -> Option < (Box < dyn HeaderKey > , Box < dyn PacketKey >) > ; # [doc = " If the 0-RTT-encrypted data has been accepted by the peer"] fn early_data_accepted (& self) -> Option < bool > ; # [doc = " Returns `true` until the connection is fully established."] fn is_handshaking (& self) -> bool ; # [doc = " Read bytes of handshake data"] # [doc = ""] # [doc = " This should be called with the contents of `CRYPTO` frames. If it returns `Ok`, the"] # [doc = " caller should call `write_handshake()` to check if the crypto protocol has anything"] # [doc = " to send to the peer. This method will only return `true` the first time that"] # [doc = " handshake data is available. Future calls will always return false."] # [doc = ""] # [doc = " On success, returns `true` iff `self.handshake_data()` has been populated."] fn read_handshake (& mut self , buf : & [u8]) -> Result < bool , TransportError > ; # [doc = " The peer's QUIC transport parameters"] # [doc = ""] # [doc = " These are only available after the first flight from the peer has been received."] fn transport_parameters (& self) -> Result < Option < TransportParameters > , TransportError > ; # [doc = " Writes handshake bytes into the given buffer and optionally returns the negotiated keys"] # [doc = ""] # [doc = " When the handshake proceeds to the next phase, this method will return a new set of"] # [doc = " keys to encrypt data with."] fn write_handshake (& mut self , buf : & mut Vec < u8 >) -> Option < Keys > ; # [doc = " Compute keys for the next key update"] fn next_1rtt_keys (& mut self) -> Option < KeyPair < Box < dyn PacketKey > > > ; # [doc = " Verify the integrity of a retry packet"] fn is_valid_retry (& self , orig_dst_cid : ConnectionId , header : & [u8] , payload : & [u8]) -> bool ; # [doc = " Fill `output` with `output.len()` bytes of keying material derived"] # [doc = " from the [Session]'s secrets, using `label` and `context` for domain"] # [doc = " separation."] # [doc = ""] # [doc = " This function will fail, returning [ExportKeyingMaterialError],"] # [doc = " if the requested output length is too large."] fn export_keying_material (& self , output : & mut [u8] , label : & [u8] , context : & [u8] ,) -> Result < () , ExportKeyingMaterialError > ; }
};
}
