// Generated macro for PendingAcks (struct)
macro_rules! Depcrate_connection_spacesPendingAcks {
() => {
// Module: crate::connection::spaces
// Provides: {"PendingAcks"}
// Dependencies: {}
# [derive (Debug)] pub (super) struct PendingAcks { # [doc = " Whether we should send an ACK immediately, even if that means sending an ACK-only packet"] # [doc = ""] # [doc = " When `immediate_ack_required` is false, the normal behavior is to send ACK frames only when"] # [doc = " there is other data to send, or when the `MaxAckDelay` timer expires."] immediate_ack_required : bool , # [doc = " The number of ack-eliciting packets received since the last ACK frame was sent"] # [doc = ""] # [doc = " Once the count _exceeds_ `ack_eliciting_threshold`, an immediate ACK is required"] ack_eliciting_since_last_ack_sent : u64 , non_ack_eliciting_since_last_ack_sent : u64 , ack_eliciting_threshold : u64 , # [doc = " The reordering threshold, controlling how we respond to out-of-order ack-eliciting packets"] # [doc = ""] # [doc = " Different values enable different behavior:"] # [doc = ""] # [doc = " * `0`: no special action is taken"] # [doc = " * `1`: an ACK is immediately sent if it is out-of-order according to RFC 9000"] # [doc = " * `>1`: an ACK is immediately sent if it is out-of-order according to the ACK frequency draft"] reordering_threshold : u64 , # [doc = " The earliest ack-eliciting packet since the last ACK was sent, used to calculate the moment"] # [doc = " upon which `max_ack_delay` elapses"] earliest_ack_eliciting_since_last_ack_sent : Option < Instant > , # [doc = " The packet number ranges of ack-eliciting packets the peer hasn't confirmed receipt of ACKs"] # [doc = " for"] ranges : ArrayRangeSet , # [doc = " The packet with the largest packet number, and the time upon which it was received (used to"] # [doc = " calculate ACK delay in [`PendingAcks::ack_delay`])"] largest_packet : Option < (u64 , Instant) > , # [doc = " The ack-eliciting packet we have received with the largest packet number"] largest_ack_eliciting_packet : Option < u64 > , # [doc = " The largest acknowledged packet number sent in an ACK frame"] largest_acked : Option < u64 > , }
};
}
