// Generated macro for impl_634 (impl)
macro_rules! Depcrate_config_transportimpl_634 {
() => {
// Module: crate::config::transport
// Provides: {"impl_634"}
// Dependencies: {}
impl TransportConfig { # [doc = " Maximum number of incoming bidirectional streams that may be open concurrently"] # [doc = ""] # [doc = " Must be nonzero for the peer to open any bidirectional streams."] # [doc = ""] # [doc = " Worst-case memory use is directly proportional to `max_concurrent_bidi_streams *"] # [doc = " stream_receive_window`, with an upper bound proportional to `receive_window`."] pub fn max_concurrent_bidi_streams (& mut self , value : VarInt) -> & mut Self { self . max_concurrent_bidi_streams = value ; self } # [doc = " Variant of `max_concurrent_bidi_streams` affecting unidirectional streams"] pub fn max_concurrent_uni_streams (& mut self , value : VarInt) -> & mut Self { self . max_concurrent_uni_streams = value ; self } # [doc = " Maximum duration of inactivity to accept before timing out the connection."] # [doc = ""] # [doc = " The true idle timeout is the minimum of this and the peer's own max idle timeout. `None`"] # [doc = " represents an infinite timeout. Defaults to 30 seconds."] # [doc = ""] # [doc = " **WARNING**: If a peer or its network path malfunctions or acts maliciously, an infinite"] # [doc = " idle timeout can result in permanently hung futures!"] # [doc = ""] # [doc = " ```"] # [doc = " # use std::{convert::TryInto, time::Duration};"] # [doc = " # use quinn_proto::{TransportConfig, VarInt, VarIntBoundsExceeded};"] # [doc = " # fn main() -> Result<(), VarIntBoundsExceeded> {"] # [doc = " let mut config = TransportConfig::default();"] # [doc = ""] # [doc = " // Set the idle timeout as `VarInt`-encoded milliseconds"] # [doc = " config.max_idle_timeout(Some(VarInt::from_u32(10_000).into()));"] # [doc = ""] # [doc = " // Set the idle timeout as a `Duration`"] # [doc = " config.max_idle_timeout(Some(Duration::from_secs(10).try_into()?));"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] pub fn max_idle_timeout (& mut self , value : Option < IdleTimeout >) -> & mut Self { self . max_idle_timeout = value . map (| t | t . 0) ; self } # [doc = " Maximum number of bytes the peer may transmit without acknowledgement on any one stream"] # [doc = " before becoming blocked."] # [doc = ""] # [doc = " This should be set to at least the expected connection latency multiplied by the maximum"] # [doc = " desired throughput. Setting this smaller than `receive_window` helps ensure that a single"] # [doc = " stream doesn't monopolize receive buffers, which may otherwise occur if the application"] # [doc = " chooses not to read from a large stream for a time while still requiring data on other"] # [doc = " streams."] pub fn stream_receive_window (& mut self , value : VarInt) -> & mut Self { self . stream_receive_window = value ; self } # [doc = " Maximum number of bytes the peer may transmit across all streams of a connection before"] # [doc = " becoming blocked."] # [doc = ""] # [doc = " This should be set to at least the expected connection latency multiplied by the maximum"] # [doc = " desired throughput. Larger values can be useful to allow maximum throughput within a"] # [doc = " stream while another is blocked."] pub fn receive_window (& mut self , value : VarInt) -> & mut Self { self . receive_window = value ; self } # [doc = " Maximum number of bytes to transmit to a peer without acknowledgment"] # [doc = ""] # [doc = " Provides an upper bound on memory when communicating with peers that issue large amounts of"] # [doc = " flow control credit. Endpoints that wish to handle large numbers of connections robustly"] # [doc = " should take care to set this low enough to guarantee memory exhaustion does not occur if"] # [doc = " every connection uses the entire window."] pub fn send_window (& mut self , value : u64) -> & mut Self { self . send_window = value ; self } # [doc = " Whether to implement fair queuing for send streams having the same priority."] # [doc = ""] # [doc = " When enabled, connections schedule data from outgoing streams having the same priority in a"] # [doc = " round-robin fashion. When disabled, streams are scheduled in the order they are written to."] # [doc = ""] # [doc = " Note that this only affects streams with the same priority. Higher priority streams always"] # [doc = " take precedence over lower priority streams."] # [doc = ""] # [doc = " Disabling fairness can reduce fragmentation and protocol overhead for workloads that use"] # [doc = " many small streams."] pub fn send_fairness (& mut self , value : bool) -> & mut Self { self . send_fairness = value ; self } # [doc = " Maximum reordering in packet number space before FACK style loss detection considers a"] # [doc = " packet lost. Should not be less than 3, per RFC5681."] pub fn packet_threshold (& mut self , value : u32) -> & mut Self { self . packet_threshold = value ; self } # [doc = " Maximum reordering in time space before time based loss detection considers a packet lost,"] # [doc = " as a factor of RTT"] pub fn time_threshold (& mut self , value : f32) -> & mut Self { self . time_threshold = value ; self } # [doc = " The RTT used before an RTT sample is taken"] pub fn initial_rtt (& mut self , value : Duration) -> & mut Self { self . initial_rtt = value ; self } # [doc = " The initial value to be used as the maximum UDP payload size before running MTU discovery"] # [doc = " (see [`TransportConfig::mtu_discovery_config`])."] # [doc = ""] # [doc = " Must be at least 1200, which is the default, and known to be safe for typical internet"] # [doc = " applications. Larger values are more efficient, but increase the risk of packet loss due to"] # [doc = " exceeding the network path's IP MTU. If the provided value is higher than what the network"] # [doc = " path actually supports, packet loss will eventually trigger black hole detection and bring"] # [doc = " it down to [`TransportConfig::min_mtu`]."] pub fn initial_mtu (& mut self , value : u16) -> & mut Self { self . initial_mtu = value . max (INITIAL_MTU) ; self } pub (crate) fn get_initial_mtu (& self) -> u16 { self . initial_mtu . max (self . min_mtu) } # [doc = " The maximum UDP payload size guaranteed to be supported by the network."] # [doc = ""] # [doc = " Must be at least 1200, which is the default, and lower than or equal to"] # [doc = " [`TransportConfig::initial_mtu`]."] # [doc = ""] # [doc = " Real-world MTUs can vary according to ISP, VPN, and properties of intermediate network links"] # [doc = " outside of either endpoint's control. Extreme care should be used when raising this value"] # [doc = " outside of private networks where these factors are fully controlled. If the provided value"] # [doc = " is higher than what the network path actually supports, the result will be unpredictable and"] # [doc = " catastrophic packet loss, without a possibility of repair. Prefer"] # [doc = " [`TransportConfig::initial_mtu`] together with"] # [doc = " [`TransportConfig::mtu_discovery_config`] to set a maximum UDP payload size that robustly"] # [doc = " adapts to the network."] pub fn min_mtu (& mut self , value : u16) -> & mut Self { self . min_mtu = value . max (INITIAL_MTU) ; self } # [doc = " Specifies the MTU discovery config (see [`MtuDiscoveryConfig`] for details)."] # [doc = ""] # [doc = " Enabled by default."] pub fn mtu_discovery_config (& mut self , value : Option < MtuDiscoveryConfig >) -> & mut Self { self . mtu_discovery_config = value ; self } # [doc = " Pad UDP datagrams carrying application data to current maximum UDP payload size"] # [doc = ""] # [doc = " Disabled by default. UDP datagrams containing loss probes are exempt from padding."] # [doc = ""] # [doc = " Enabling this helps mitigate traffic analysis by network observers, but it increases"] # [doc = " bandwidth usage. Without this mitigation precise plain text size of application datagrams as"] # [doc = " well as the total size of stream write bursts can be inferred by observers under certain"] # [doc = " conditions. This analysis requires either an uncongested connection or application datagrams"] # [doc = " too large to be coalesced."] pub fn pad_to_mtu (& mut self , value : bool) -> & mut Self { self . pad_to_mtu = value ; self } # [doc = " Specifies the ACK frequency config (see [`AckFrequencyConfig`] for details)"] # [doc = ""] # [doc = " The provided configuration will be ignored if the peer does not support the acknowledgement"] # [doc = " frequency QUIC extension."] # [doc = ""] # [doc = " Defaults to `None`, which disables controlling the peer's acknowledgement frequency. Even"] # [doc = " if set to `None`, the local side still supports the acknowledgement frequency QUIC"] # [doc = " extension and may use it in other ways."] pub fn ack_frequency_config (& mut self , value : Option < AckFrequencyConfig >) -> & mut Self { self . ack_frequency_config = value ; self } # [doc = " Number of consecutive PTOs after which network is considered to be experiencing persistent congestion."] pub fn persistent_congestion_threshold (& mut self , value : u32) -> & mut Self { self . persistent_congestion_threshold = value ; self } # [doc = " Period of inactivity before sending a keep-alive packet"] # [doc = ""] # [doc = " Keep-alive packets prevent an inactive but otherwise healthy connection from timing out."] # [doc = ""] # [doc = " `None` to disable, which is the default. Only one side of any given connection needs keep-alive"] # [doc = " enabled for the connection to be preserved. Must be set lower than the idle_timeout of both"] # [doc = " peers to be effective."] pub fn keep_alive_interval (& mut self , value : Option < Duration >) -> & mut Self { self . keep_alive_interval = value ; self } # [doc = " Maximum quantity of out-of-order crypto layer data to buffer"] pub fn crypto_buffer_size (& mut self , value : usize) -> & mut Self { self . crypto_buffer_size = value ; self } # [doc = " Whether the implementation is permitted to set the spin bit on this connection"] # [doc = ""] # [doc = " This allows passive observers to easily judge the round trip time of a connection, which can"] # [doc = " be useful for network administration but sacrifices a small amount of privacy."] pub fn allow_spin (& mut self , value : bool) -> & mut Self { self . allow_spin = value ; self } # [doc = " Maximum number of incoming application datagram bytes to buffer, or None to disable"] # [doc = " incoming datagrams"] # [doc = ""] # [doc = " The peer is forbidden to send single datagrams larger than this size. If the aggregate size"] # [doc = " of all datagrams that have been received from the peer but not consumed by the application"] # [doc = " exceeds this value, old datagrams are dropped until it is no longer exceeded."] pub fn datagram_receive_buffer_size (& mut self , value : Option < usize >) -> & mut Self { self . datagram_receive_buffer_size = value ; self } # [doc = " Maximum number of outgoing application datagram bytes to buffer"] # [doc = ""] # [doc = " While datagrams are sent ASAP, it is possible for an application to generate data faster"] # [doc = " than the link, or even the underlying hardware, can transmit them. This limits the amount of"] # [doc = " memory that may be consumed in that case. When the send buffer is full and a new datagram is"] # [doc = " sent, older datagrams are dropped until sufficient space is available."] pub fn datagram_send_buffer_size (& mut self , value : usize) -> & mut Self { self . datagram_send_buffer_size = value ; self } # [doc = " Whether to force every packet number to be used"] # [doc = ""] # [doc = " By default, packet numbers are occasionally skipped to ensure peers aren't ACKing packets"] # [doc = " before they see them."] # [cfg (test)] pub (crate) fn deterministic_packet_numbers (& mut self , enabled : bool) -> & mut Self { self . deterministic_packet_numbers = enabled ; self } # [doc = " How to construct new `congestion::Controller`s"] # [doc = ""] # [doc = " Typically the refcounted configuration of a `congestion::Controller`,"] # [doc = " e.g. a `congestion::NewRenoConfig`."] # [doc = ""] # [doc = " # Example"] # [doc = " ```"] # [doc = " # use quinn_proto::*; use std::sync::Arc;"] # [doc = " let mut config = TransportConfig::default();"] # [doc = " config.congestion_controller_factory(Arc::new(congestion::NewRenoConfig::default()));"] # [doc = " ```"] pub fn congestion_controller_factory (& mut self , factory : Arc < dyn congestion :: ControllerFactory + Send + Sync + 'static > ,) -> & mut Self { self . congestion_controller_factory = factory ; self } # [doc = " Whether to use \"Generic Segmentation Offload\" to accelerate transmits, when supported by the"] # [doc = " environment"] # [doc = ""] # [doc = " Defaults to `true`."] # [doc = ""] # [doc = " GSO dramatically reduces CPU consumption when sending large numbers of packets with the same"] # [doc = " headers, such as when transmitting bulk data on a connection. However, it is not supported"] # [doc = " by all network interface drivers or packet inspection tools. `quinn-udp` will attempt to"] # [doc = " disable GSO automatically when unavailable, but this can lead to spurious packet loss at"] # [doc = " startup, temporarily degrading performance."] pub fn enable_segmentation_offload (& mut self , enabled : bool) -> & mut Self { self . enable_segmentation_offload = enabled ; self } # [doc = " qlog capture configuration to use for a particular connection"] # [cfg (feature = "qlog")] pub fn qlog_stream (& mut self , stream : Option < QlogStream >) -> & mut Self { self . qlog_sink = stream . into () ; self } }
};
}
