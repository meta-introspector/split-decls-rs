// Generated macro for huerotate (function)
macro_rules! Depcrate_imageops_coloropshuerotate {
() => {
// Module: crate::imageops::colorops
// Provides: {"huerotate"}
// Dependencies: {}
# [doc = " Hue rotate the supplied image."] # [doc = " `value` is the degrees to rotate each pixel by."] # [doc = " 0 and 360 do nothing, the rest rotates by the given degree value."] # [doc = " just like the css webkit filter hue-rotate(180)"] # [doc = ""] # [doc = " *[See also `huerotate_in_place`.][huerotate_in_place]*"] pub fn huerotate < I , P , S > (image : & I , value : i32) -> ImageBuffer < P , Vec < S > > where I : GenericImageView < Pixel = P > , P : Pixel < Subpixel = S > + 'static , S : Primitive + 'static , { let (width , height) = image . dimensions () ; let mut out = ImageBuffer :: new (width , height) ; let angle : f64 = NumCast :: from (value) . unwrap () ; let cosv = angle . to_radians () . cos () ; let sinv = angle . to_radians () . sin () ; let matrix : [f64 ; 9] = [0.213 + cosv * 0.787 - sinv * 0.213 , 0.715 - cosv * 0.715 - sinv * 0.715 , 0.072 - cosv * 0.072 + sinv * 0.928 , 0.213 - cosv * 0.213 + sinv * 0.143 , 0.715 + cosv * 0.285 + sinv * 0.140 , 0.072 - cosv * 0.072 - sinv * 0.283 , 0.213 - cosv * 0.213 - sinv * 0.787 , 0.715 - cosv * 0.715 + sinv * 0.715 , 0.072 + cosv * 0.928 + sinv * 0.072 ,] ; for (x , y , pixel) in out . enumerate_pixels_mut () { let p = image . get_pixel (x , y) ; # [allow (deprecated)] let (k1 , k2 , k3 , k4) = p . channels4 () ; let vec : (f64 , f64 , f64 , f64) = (NumCast :: from (k1) . unwrap () , NumCast :: from (k2) . unwrap () , NumCast :: from (k3) . unwrap () , NumCast :: from (k4) . unwrap () ,) ; let r = vec . 0 ; let g = vec . 1 ; let b = vec . 2 ; let new_r = matrix [0] * r + matrix [1] * g + matrix [2] * b ; let new_g = matrix [3] * r + matrix [4] * g + matrix [5] * b ; let new_b = matrix [6] * r + matrix [7] * g + matrix [8] * b ; let max = 255f64 ; # [allow (deprecated)] let outpixel = Pixel :: from_channels (NumCast :: from (clamp (new_r , 0.0 , max)) . unwrap () , NumCast :: from (clamp (new_g , 0.0 , max)) . unwrap () , NumCast :: from (clamp (new_b , 0.0 , max)) . unwrap () , NumCast :: from (clamp (vec . 3 , 0.0 , max)) . unwrap () ,) ; * pixel = outpixel ; } out }
};
}
