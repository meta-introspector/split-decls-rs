// Generated macro for FlatSamples (struct)
macro_rules! Depcrate_flatFlatSamples {
() => {
// Module: crate::flat
// Provides: {"FlatSamples"}
// Dependencies: {}
# [doc = " A flat buffer over a (multi channel) image."] # [doc = ""] # [doc = " In contrast to `ImageBuffer`, this representation of a sample collection is much more lenient"] # [doc = " in the layout thereof. It also allows grouping by color planes instead of by pixel as long as"] # [doc = " the strides of each extent are constant. This struct itself has no invariants on the strides"] # [doc = " but not every possible configuration can be interpreted as a [`GenericImageView`] or"] # [doc = " [`GenericImage`]. The methods [`as_view`] and [`as_view_mut`] construct the actual implementors"] # [doc = " of these traits and perform necessary checks. To manually perform this and other layout checks"] # [doc = " use [`is_normal`] or [`has_aliased_samples`]."] # [doc = ""] # [doc = " Instances can be constructed not only by hand. The buffer instances returned by library"] # [doc = " functions such as [`ImageBuffer::as_flat_samples`] guarantee that the conversion to a generic"] # [doc = " image or generic view succeeds. A very different constructor is [`with_monocolor`]. It uses a"] # [doc = " single pixel as the backing storage for an arbitrarily sized read-only raster by mapping each"] # [doc = " pixel to the same samples by setting some strides to `0`."] # [doc = ""] # [doc = " [`GenericImage`]: ../trait.GenericImage.html"] # [doc = " [`GenericImageView`]: ../trait.GenericImageView.html"] # [doc = " [`ImageBuffer::as_flat_samples`]: ../struct.ImageBuffer.html#method.as_flat_samples"] # [doc = " [`is_normal`]: #method.is_normal"] # [doc = " [`has_aliased_samples`]: #method.has_aliased_samples"] # [doc = " [`as_view`]: #method.as_view"] # [doc = " [`as_view_mut`]: #method.as_view_mut"] # [doc = " [`with_monocolor`]: #method.with_monocolor"] # [derive (Clone , Debug)] pub struct FlatSamples < Buffer > { # [doc = " Underlying linear container holding sample values."] pub samples : Buffer , # [doc = " A `repr(C)` description of the layout of buffer samples."] pub layout : SampleLayout , # [doc = " Supplementary color information."] # [doc = ""] # [doc = " You may keep this as `None` in most cases. This is NOT checked in `View` or other"] # [doc = " converters. It is intended mainly as a way for types that convert to this buffer type to"] # [doc = " attach their otherwise static color information. A dynamic image representation could"] # [doc = " however use this to resolve representational ambiguities such as the order of RGB channels."] pub color_hint : Option < ColorType > , }
};
}
