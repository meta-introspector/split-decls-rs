// Generated macro for overlay_bounds (function)
macro_rules! Depcrate_imageopsoverlay_bounds {
() => {
// Module: crate::imageops
// Provides: {"overlay_bounds"}
// Dependencies: {}
# [doc = " Calculate the region that can be copied from top to bottom."] # [doc = ""] # [doc = " Given image size of bottom and top image, and a point at which we want to place the top image"] # [doc = " onto the bottom image, how large can we be? Have to wary of the following issues:"] # [doc = " * Top might be larger than bottom"] # [doc = " * Overflows in the computation"] # [doc = " * Coordinates could be completely out of bounds"] # [doc = ""] # [doc = " The main idea is to make use of inequalities provided by the nature of `saturating_add` and"] # [doc = " `saturating_sub`. These intrinsically validate that all resulting coordinates will be in bounds"] # [doc = " for both images."] # [doc = ""] # [doc = " We want that all these coordinate accesses are safe:"] # [doc = " 1. `bottom.get_pixel(x + [0..x_range), y + [0..y_range))`"] # [doc = " 2. `top.get_pixel([0..x_range), [0..y_range))`"] # [doc = ""] # [doc = " Proof that the function provides the necessary bounds for width. Note that all unaugmented math"] # [doc = " operations are to be read in standard arithmetic, not integer arithmetic. Since no direct"] # [doc = " integer arithmetic occurs in the implementation, this is unambiguous."] # [doc = ""] # [doc = " ```text"] # [doc = " Three short notes/lemmata:"] # [doc = " - Iff `(a - b) <= 0` then `a.saturating_sub(b) = 0`"] # [doc = " - Iff `(a - b) >= 0` then `a.saturating_sub(b) = a - b`"] # [doc = " - If  `a <= c` then `a.saturating_sub(b) <= c.saturating_sub(b)`"] # [doc = ""] # [doc = " 1.1 We show that if `bottom_width <= x`, then `x_range = 0` therefore `x + [0..x_range)` is empty."] # [doc = ""] # [doc = " x_range"] # [doc = "  = (top_width.saturating_add(x).min(bottom_width)).saturating_sub(x)"] # [doc = " <= bottom_width.saturating_sub(x)"] # [doc = ""] # [doc = " bottom_width <= x"] # [doc = " <==> bottom_width - x <= 0"] # [doc = " <==> bottom_width.saturating_sub(x) = 0"] # [doc = "  ==> x_range <= 0"] # [doc = "  ==> x_range  = 0"] # [doc = ""] # [doc = " 1.2 If `x < bottom_width` then `x + x_range < bottom_width`"] # [doc = ""] # [doc = " x + x_range"] # [doc = " <= x + bottom_width.saturating_sub(x)"] # [doc = "  = x + (bottom_width - x)"] # [doc = "  = bottom_width"] # [doc = ""] # [doc = " 2. We show that `x_range <= top_width`"] # [doc = ""] # [doc = " x_range"] # [doc = "  = (top_width.saturating_add(x).min(bottom_width)).saturating_sub(x)"] # [doc = " <= top_width.saturating_add(x).saturating_sub(x)"] # [doc = " <= (top_wdith + x).saturating_sub(x)"] # [doc = "  = top_width (due to `top_width >= 0` and `x >= 0`)"] # [doc = " ```"] # [doc = ""] # [doc = " Proof is the same for height."] # [must_use] pub fn overlay_bounds ((bottom_width , bottom_height) : (u32 , u32) , (top_width , top_height) : (u32 , u32) , x : u32 , y : u32 ,) -> (u32 , u32) { let x_range = top_width . saturating_add (x) . min (bottom_width) . saturating_sub (x) ; let y_range = top_height . saturating_add (y) . min (bottom_height) . saturating_sub (y) ; (x_range , y_range) }
};
}
