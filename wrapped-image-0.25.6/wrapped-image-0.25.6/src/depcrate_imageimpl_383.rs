// Generated macro for impl_383 (impl)
macro_rules! Depcrate_imageimpl_383 {
() => {
// Module: crate::image
// Provides: {"impl_383"}
// Dependencies: {}
impl ImageReadBuffer { # [doc = " Create a new `ImageReadBuffer`."] # [doc = ""] # [doc = " Panics if `scanline_bytes` doesn't fit into a usize, because that would mean reading anything"] # [doc = " from the image would take more RAM than the entire virtual address space. In other words,"] # [doc = " actually using this struct would instantly OOM so just get it out of the way now."] # [allow (dead_code)] pub (crate) fn new (scanline_bytes : u64 , total_bytes : u64) -> Self { Self { scanline_bytes : usize :: try_from (scanline_bytes) . unwrap () , buffer : Vec :: new () , consumed : 0 , total_bytes , offset : 0 , } } # [allow (dead_code)] pub (crate) fn read < F > (& mut self , buf : & mut [u8] , mut read_scanline : F) -> io :: Result < usize > where F : FnMut (& mut [u8]) -> io :: Result < usize > , { if self . buffer . len () == self . consumed { if self . offset == self . total_bytes { return Ok (0) ; } else if buf . len () >= self . scanline_bytes { let bytes_read = read_scanline (& mut buf [.. self . scanline_bytes]) ? ; self . offset += u64 :: try_from (bytes_read) . unwrap () ; return Ok (bytes_read) ; } else { if self . buffer . is_empty () { self . buffer . resize (self . scanline_bytes , 0) ; } self . consumed = 0 ; let bytes_read = read_scanline (& mut self . buffer [..]) ? ; self . buffer . resize (bytes_read , 0) ; self . offset += u64 :: try_from (bytes_read) . unwrap () ; assert ! (bytes_read == self . scanline_bytes || self . offset == self . total_bytes) ; } } let bytes_buffered = self . buffer . len () - self . consumed ; if bytes_buffered > buf . len () { buf . copy_from_slice (& self . buffer [self . consumed ..] [.. buf . len ()]) ; self . consumed += buf . len () ; Ok (buf . len ()) } else { buf [.. bytes_buffered] . copy_from_slice (& self . buffer [self . consumed ..] [.. bytes_buffered]) ; self . consumed = self . buffer . len () ; Ok (bytes_buffered) } } }
};
}
