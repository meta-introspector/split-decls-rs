// Generated macro for huerotate_in_place (function)
macro_rules! Depcrate_imageops_coloropshuerotate_in_place {
() => {
// Module: crate::imageops::colorops
// Provides: {"huerotate_in_place"}
// Dependencies: {}
# [doc = " Hue rotate the supplied image in place."] # [doc = ""] # [doc = " `value` is the degrees to rotate each pixel by."] # [doc = " 0 and 360 do nothing, the rest rotates by the given degree value."] # [doc = " just like the css webkit filter hue-rotate(180)"] # [doc = ""] # [doc = " *[See also `huerotate`.][huerotate]*"] pub fn huerotate_in_place < I > (image : & mut I , value : i32) where I : GenericImage , { let (width , height) = image . dimensions () ; let angle : f64 = NumCast :: from (value) . unwrap () ; let cosv = angle . to_radians () . cos () ; let sinv = angle . to_radians () . sin () ; let matrix : [f64 ; 9] = [0.213 + cosv * 0.787 - sinv * 0.213 , 0.715 - cosv * 0.715 - sinv * 0.715 , 0.072 - cosv * 0.072 + sinv * 0.928 , 0.213 - cosv * 0.213 + sinv * 0.143 , 0.715 + cosv * 0.285 + sinv * 0.140 , 0.072 - cosv * 0.072 - sinv * 0.283 , 0.213 - cosv * 0.213 - sinv * 0.787 , 0.715 - cosv * 0.715 + sinv * 0.715 , 0.072 + cosv * 0.928 + sinv * 0.072 ,] ; for y in 0 .. height { for x in 0 .. width { let pixel = image . get_pixel (x , y) ; # [allow (deprecated)] let (k1 , k2 , k3 , k4) = pixel . channels4 () ; let vec : (f64 , f64 , f64 , f64) = (NumCast :: from (k1) . unwrap () , NumCast :: from (k2) . unwrap () , NumCast :: from (k3) . unwrap () , NumCast :: from (k4) . unwrap () ,) ; let r = vec . 0 ; let g = vec . 1 ; let b = vec . 2 ; let new_r = matrix [0] * r + matrix [1] * g + matrix [2] * b ; let new_g = matrix [3] * r + matrix [4] * g + matrix [5] * b ; let new_b = matrix [6] * r + matrix [7] * g + matrix [8] * b ; let max = 255f64 ; # [allow (deprecated)] let outpixel = Pixel :: from_channels (NumCast :: from (clamp (new_r , 0.0 , max)) . unwrap () , NumCast :: from (clamp (new_g , 0.0 , max)) . unwrap () , NumCast :: from (clamp (new_b , 0.0 , max)) . unwrap () , NumCast :: from (clamp (vec . 3 , 0.0 , max)) . unwrap () ,) ; image . put_pixel (x , y , outpixel) ; } } }
};
}
