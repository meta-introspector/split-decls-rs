// Generated macro for impl_200 (impl)
macro_rules! Depcrate_flatimpl_200 {
() => {
// Module: crate::flat
// Provides: {"impl_200"}
// Dependencies: {}
impl < Buffer , P : Pixel > View < Buffer , P > where Buffer : AsRef < [P :: Subpixel] > , { # [doc = " Take out the sample buffer."] # [doc = ""] # [doc = " Gives up the normalization invariants on the buffer format."] pub fn into_inner (self) -> FlatSamples < Buffer > { self . inner } # [doc = " Get a reference on the inner sample descriptor."] # [doc = ""] # [doc = " There is no mutable counterpart as modifying the buffer format, including strides and"] # [doc = " lengths, could invalidate the accessibility invariants of the `View`. It is not specified"] # [doc = " if the inner buffer is the same as the buffer of the image from which this view was"] # [doc = " created. It might have been truncated as an optimization."] pub fn flat (& self) -> & FlatSamples < Buffer > { & self . inner } # [doc = " Get a reference on the inner buffer."] # [doc = ""] # [doc = " There is no mutable counter part since it is not intended to allow you to reassign the"] # [doc = " buffer or otherwise change its size or properties."] pub fn samples (& self) -> & Buffer { & self . inner . samples } # [doc = " Get a reference to a selected subpixel if it is in-bounds."] # [doc = ""] # [doc = " This method will return `None` when the sample is out-of-bounds. All errors that could"] # [doc = " occur due to overflow have been eliminated while construction the `View`."] pub fn get_sample (& self , channel : u8 , x : u32 , y : u32) -> Option < & P :: Subpixel > { if ! self . inner . in_bounds (channel , x , y) { return None ; } let index = self . inner . in_bounds_index (channel , x , y) ; self . samples () . as_ref () . get (index) } # [doc = " Get a mutable reference to a selected subpixel if it is in-bounds."] # [doc = ""] # [doc = " This is relevant only when constructed with `FlatSamples::as_view_with_mut_samples`.  This"] # [doc = " method will return `None` when the sample is out-of-bounds. All errors that could occur due"] # [doc = " to overflow have been eliminated while construction the `View`."] # [doc = ""] # [doc = " **WARNING**: Note that of course samples may alias, so that the mutable reference returned"] # [doc = " here can in fact modify more than the coordinate in the argument."] pub fn get_mut_sample (& mut self , channel : u8 , x : u32 , y : u32) -> Option < & mut P :: Subpixel > where Buffer : AsMut < [P :: Subpixel] > , { if ! self . inner . in_bounds (channel , x , y) { return None ; } let index = self . inner . in_bounds_index (channel , x , y) ; self . inner . samples . as_mut () . get_mut (index) } # [doc = " Get the minimum length of a buffer such that all in-bounds samples have valid indices."] # [doc = ""] # [doc = " See `FlatSamples::min_length`. This method will always succeed."] pub fn min_length (& self) -> usize { self . inner . min_length () . unwrap () } # [doc = " Return the portion of the buffer that holds sample values."] # [doc = ""] # [doc = " While this can not fail–the validity of all coordinates has been validated during the"] # [doc = " conversion from `FlatSamples`–the resulting slice may still contain holes."] pub fn image_slice (& self) -> & [P :: Subpixel] { & self . samples () . as_ref () [.. self . min_length ()] } # [doc = " Return the mutable portion of the buffer that holds sample values."] # [doc = ""] # [doc = " This is relevant only when constructed with `FlatSamples::as_view_with_mut_samples`. While"] # [doc = " this can not fail–the validity of all coordinates has been validated during the conversion"] # [doc = " from `FlatSamples`–the resulting slice may still contain holes."] pub fn image_mut_slice (& mut self) -> & mut [P :: Subpixel] where Buffer : AsMut < [P :: Subpixel] > , { let min_length = self . min_length () ; & mut self . inner . samples . as_mut () [.. min_length] } # [doc = " Shrink the inner image."] # [doc = ""] # [doc = " The new dimensions will be the minimum of the previous dimensions. Since the set of"] # [doc = " in-bounds pixels afterwards is a subset of the current ones, this is allowed on a `View`."] # [doc = " Note that you can not change the number of channels as an intrinsic property of `P`."] pub fn shrink_to (& mut self , width : u32 , height : u32) { let channels = self . inner . layout . channels ; self . inner . shrink_to (channels , width , height) ; } # [doc = " Try to convert this into an image with mutable pixels."] # [doc = ""] # [doc = " The resulting image implements `GenericImage` in addition to `GenericImageView`. While this"] # [doc = " has mutable samples, it does not enforce that pixel can not alias and that samples are"] # [doc = " packed enough for a mutable pixel reference. This is slightly cheaper than the chain"] # [doc = " `self.into_inner().as_view_mut()` and keeps the `View` alive on failure."] # [doc = ""] # [doc = " ```"] # [doc = " # use image::RgbImage;"] # [doc = " # use image::Rgb;"] # [doc = " let mut buffer = RgbImage::new(480, 640).into_flat_samples();"] # [doc = " let view = buffer.as_view_with_mut_samples::<Rgb<u8>>().unwrap();"] # [doc = ""] # [doc = " // Inspect some pixels, …"] # [doc = ""] # [doc = " // Doesn't fail because it was originally an `RgbImage`."] # [doc = " let view_mut = view.try_upgrade().unwrap();"] # [doc = " ```"] pub fn try_upgrade (self) -> Result < ViewMut < Buffer , P > , (Error , Self) > where Buffer : AsMut < [P :: Subpixel] > , { if ! self . inner . is_normal (NormalForm :: PixelPacked) { return Err ((Error :: NormalFormRequired (NormalForm :: PixelPacked) , self)) ; } Ok (ViewMut { inner : self . inner , phantom : PhantomData , }) } }
};
}
