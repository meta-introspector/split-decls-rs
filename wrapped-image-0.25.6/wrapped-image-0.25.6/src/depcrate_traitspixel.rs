// Generated macro for Pixel (trait)
macro_rules! Depcrate_traitsPixel {
() => {
// Module: crate::traits
// Provides: {"Pixel"}
// Dependencies: {}
# [doc = " A generalized pixel."] # [doc = ""] # [doc = " A pixel object is usually not used standalone but as a view into an image buffer."] pub trait Pixel : Copy + Clone { # [doc = " The scalar type that is used to store each channel in this pixel."] type Subpixel : Primitive ; # [doc = " The number of channels of this pixel type."] const CHANNEL_COUNT : u8 ; # [doc = " Returns the components as a slice."] fn channels (& self) -> & [Self :: Subpixel] ; # [doc = " Returns the components as a mutable slice"] fn channels_mut (& mut self) -> & mut [Self :: Subpixel] ; # [doc = " A string that can help to interpret the meaning each channel"] # [doc = " See [gimp babl](http://gegl.org/babl/)."] const COLOR_MODEL : & 'static str ; # [doc = " Returns the channels of this pixel as a 4 tuple. If the pixel"] # [doc = " has less than 4 channels the remainder is filled with the maximum value"] # [deprecated (since = "0.24.0" , note = "Use `channels()` or `channels_mut()`")] fn channels4 (& self ,) -> (Self :: Subpixel , Self :: Subpixel , Self :: Subpixel , Self :: Subpixel ,) ; # [doc = " Construct a pixel from the 4 channels a, b, c and d."] # [doc = " If the pixel does not contain 4 channels the extra are ignored."] # [deprecated (since = "0.24.0" , note = "Use the constructor of the pixel, for example `Rgba([r,g,b,a])` or `Pixel::from_slice`")] fn from_channels (a : Self :: Subpixel , b : Self :: Subpixel , c : Self :: Subpixel , d : Self :: Subpixel ,) -> Self ; # [doc = " Returns a view into a slice."] # [doc = ""] # [doc = " Note: The slice length is not checked on creation. Thus the caller has to ensure"] # [doc = " that the slice is long enough to prevent panics if the pixel is used later on."] fn from_slice (slice : & [Self :: Subpixel]) -> & Self ; # [doc = " Returns mutable view into a mutable slice."] # [doc = ""] # [doc = " Note: The slice length is not checked on creation. Thus the caller has to ensure"] # [doc = " that the slice is long enough to prevent panics if the pixel is used later on."] fn from_slice_mut (slice : & mut [Self :: Subpixel]) -> & mut Self ; # [doc = " Convert this pixel to RGB"] fn to_rgb (& self) -> Rgb < Self :: Subpixel > ; # [doc = " Convert this pixel to RGB with an alpha channel"] fn to_rgba (& self) -> Rgba < Self :: Subpixel > ; # [doc = " Convert this pixel to luma"] fn to_luma (& self) -> Luma < Self :: Subpixel > ; # [doc = " Convert this pixel to luma with an alpha channel"] fn to_luma_alpha (& self) -> LumaA < Self :: Subpixel > ; # [doc = " Apply the function ```f``` to each channel of this pixel."] fn map < F > (& self , f : F) -> Self where F : FnMut (Self :: Subpixel) -> Self :: Subpixel ; # [doc = " Apply the function ```f``` to each channel of this pixel."] fn apply < F > (& mut self , f : F) where F : FnMut (Self :: Subpixel) -> Self :: Subpixel ; # [doc = " Apply the function ```f``` to each channel except the alpha channel."] # [doc = " Apply the function ```g``` to the alpha channel."] fn map_with_alpha < F , G > (& self , f : F , g : G) -> Self where F : FnMut (Self :: Subpixel) -> Self :: Subpixel , G : FnMut (Self :: Subpixel) -> Self :: Subpixel ; # [doc = " Apply the function ```f``` to each channel except the alpha channel."] # [doc = " Apply the function ```g``` to the alpha channel. Works in-place."] fn apply_with_alpha < F , G > (& mut self , f : F , g : G) where F : FnMut (Self :: Subpixel) -> Self :: Subpixel , G : FnMut (Self :: Subpixel) -> Self :: Subpixel ; # [doc = " Apply the function ```f``` to each channel except the alpha channel."] fn map_without_alpha < F > (& self , f : F) -> Self where F : FnMut (Self :: Subpixel) -> Self :: Subpixel , { let mut this = * self ; this . apply_with_alpha (f , | x | x) ; this } # [doc = " Apply the function ```f``` to each channel except the alpha channel."] # [doc = " Works in place."] fn apply_without_alpha < F > (& mut self , f : F) where F : FnMut (Self :: Subpixel) -> Self :: Subpixel , { self . apply_with_alpha (f , | x | x) ; } # [doc = " Apply the function ```f``` to each channel of this pixel and"] # [doc = " ```other``` pairwise."] fn map2 < F > (& self , other : & Self , f : F) -> Self where F : FnMut (Self :: Subpixel , Self :: Subpixel) -> Self :: Subpixel ; # [doc = " Apply the function ```f``` to each channel of this pixel and"] # [doc = " ```other``` pairwise. Works in-place."] fn apply2 < F > (& mut self , other : & Self , f : F) where F : FnMut (Self :: Subpixel , Self :: Subpixel) -> Self :: Subpixel ; # [doc = " Invert this pixel"] fn invert (& mut self) ; # [doc = " Blend the color of a given pixel into ourself, taking into account alpha channels"] fn blend (& mut self , other : & Self) ; }
};
}
