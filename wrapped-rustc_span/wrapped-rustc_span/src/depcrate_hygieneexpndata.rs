// Generated macro for ExpnData (struct)
macro_rules! Depcrate_hygieneExpnData {
() => {
// Module: crate::hygiene
// Provides: {"ExpnData"}
// Dependencies: {}
# [doc = " A subset of properties from both macro definition and macro call available through global data."] # [doc = " Avoid using this if you have access to the original definition or call structures."] # [derive (Clone , Debug , Encodable , Decodable , HashStable_Generic)] pub struct ExpnData { pub kind : ExpnKind , # [doc = " The expansion that contains the definition of the macro for this expansion."] pub parent : ExpnId , # [doc = " The span of the macro call which produced this expansion."] # [doc = ""] # [doc = " This span will typically have a different `ExpnData` and `call_site`."] # [doc = " This recursively traces back through any macro calls which expanded into further"] # [doc = " macro calls, until the \"source call-site\" is reached at the root SyntaxContext."] # [doc = " For example, if `food!()` expands to `fruit!()` which then expands to `grape`,"] # [doc = " then the call-site of `grape` is `fruit!()` and the call-site of `fruit!()`"] # [doc = " is `food!()`."] # [doc = ""] # [doc = " For a desugaring expansion, this is the span of the expression or node that was"] # [doc = " desugared."] pub call_site : Span , # [doc = " Used to force two `ExpnData`s to have different `Fingerprint`s."] # [doc = " Due to macro expansion, it's possible to end up with two `ExpnId`s"] # [doc = " that have identical `ExpnData`s. This violates the contract of `HashStable`"] # [doc = " - the two `ExpnId`s are not equal, but their `Fingerprint`s are equal"] # [doc = " (since the numerical `ExpnId` value is not considered by the `HashStable`"] # [doc = " implementation)."] # [doc = ""] # [doc = " The `disambiguator` field is set by `update_disambiguator` when two distinct"] # [doc = " `ExpnId`s would end up with the same `Fingerprint`. Since `ExpnData` includes"] # [doc = " a `krate` field, this value only needs to be unique within a single crate."] disambiguator : u32 , # [doc = " The span of the macro definition (possibly dummy)."] # [doc = " This span serves only informational purpose and is not used for resolution."] pub def_site : Span , # [doc = " List of `#[unstable]`/feature-gated features that the macro is allowed to use"] # [doc = " internally without forcing the whole crate to opt-in"] # [doc = " to them."] pub allow_internal_unstable : Option < Arc < [Symbol] > > , # [doc = " Edition of the crate in which the macro is defined."] pub edition : Edition , # [doc = " The `DefId` of the macro being invoked,"] # [doc = " if this `ExpnData` corresponds to a macro invocation"] pub macro_def_id : Option < DefId > , # [doc = " The normal module (`mod`) in which the expanded macro was defined."] pub parent_module : Option < DefId > , # [doc = " Suppresses the `unsafe_code` lint for code produced by this macro."] pub (crate) allow_internal_unsafe : bool , # [doc = " Enables the macro helper hack (`ident!(...)` -> `$crate::ident!(...)`) for this macro."] pub local_inner_macros : bool , # [doc = " Should debuginfo for the macro be collapsed to the outermost expansion site (in other"] # [doc = " words, was the macro definition annotated with `#[collapse_debuginfo]`)?"] pub (crate) collapse_debuginfo : bool , # [doc = " When true, we do not display the note telling people to use the `-Zmacro-backtrace` flag."] pub hide_backtrace : bool , }
};
}
