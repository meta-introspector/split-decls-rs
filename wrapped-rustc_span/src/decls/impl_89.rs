macro_rules! deps {
    () => {
        SourceFile!();
        ExternalSourceKind!();
        SourceFileLines!();
        SourceFileHashAlgorithm!();
        StableSourceFileId!();
        FileName!();
        SourceFileHash!();
        ExternalSource!();
        SourceFileDiffs!();
        OffsetOverflowError!();
    };
}

macro_rules! impl_89 {
    () => {
        deps!();
        impl SourceFile { const MAX_FILE_SIZE : u32 = u32 :: MAX - 1 ; pub fn new (name : FileName , mut src : String , hash_kind : SourceFileHashAlgorithm , checksum_hash_kind : Option < SourceFileHashAlgorithm > ,) -> Result < Self , OffsetOverflowError > { let src_hash = SourceFileHash :: new_in_memory (hash_kind , src . as_bytes ()) ; let checksum_hash = checksum_hash_kind . map (| checksum_hash_kind | { if checksum_hash_kind == hash_kind { src_hash } else { SourceFileHash :: new_in_memory (checksum_hash_kind , src . as_bytes ()) } }) ; let normalized_pos = normalize_src (& mut src) ; let stable_id = StableSourceFileId :: from_filename_in_current_crate (& name) ; let source_len = src . len () ; let source_len = u32 :: try_from (source_len) . map_err (| _ | OffsetOverflowError) ? ; if source_len > Self :: MAX_FILE_SIZE { return Err (OffsetOverflowError) ; } let (lines , multibyte_chars) = analyze_source_file :: analyze_source_file (& src) ; Ok (SourceFile { name , src : Some (Arc :: new (src)) , src_hash , checksum_hash , external_src : FreezeLock :: frozen (ExternalSource :: Unneeded) , start_pos : BytePos :: from_u32 (0) , source_len : RelativeBytePos :: from_u32 (source_len) , lines : FreezeLock :: frozen (SourceFileLines :: Lines (lines)) , multibyte_chars , normalized_pos , stable_id , cnum : LOCAL_CRATE , }) } # [doc = " This converts the `lines` field to contain `SourceFileLines::Lines` if needed and freezes"] # [doc = " it."] fn convert_diffs_to_lines_frozen (& self) { let mut guard = if let Some (guard) = self . lines . try_write () { guard } else { return } ; let SourceFileDiffs { bytes_per_diff , num_diffs , raw_diffs } = match & * guard { SourceFileLines :: Diffs (diffs) => diffs , SourceFileLines :: Lines (..) => { FreezeWriteGuard :: freeze (guard) ; return ; } } ; let num_lines = num_diffs + 1 ; let mut lines = Vec :: with_capacity (num_lines) ; let mut line_start = RelativeBytePos (0) ; lines . push (line_start) ; assert_eq ! (* num_diffs , raw_diffs . len () / bytes_per_diff) ; match bytes_per_diff { 1 => { lines . extend (raw_diffs . into_iter () . map (| & diff | { line_start = line_start + RelativeBytePos (diff as u32) ; line_start })) ; } 2 => { lines . extend ((0 .. * num_diffs) . map (| i | { let pos = bytes_per_diff * i ; let bytes = [raw_diffs [pos] , raw_diffs [pos + 1]] ; let diff = u16 :: from_le_bytes (bytes) ; line_start = line_start + RelativeBytePos (diff as u32) ; line_start })) ; } 4 => { lines . extend ((0 .. * num_diffs) . map (| i | { let pos = bytes_per_diff * i ; let bytes = [raw_diffs [pos] , raw_diffs [pos + 1] , raw_diffs [pos + 2] , raw_diffs [pos + 3] ,] ; let diff = u32 :: from_le_bytes (bytes) ; line_start = line_start + RelativeBytePos (diff) ; line_start })) ; } _ => unreachable ! () , } * guard = SourceFileLines :: Lines (lines) ; FreezeWriteGuard :: freeze (guard) ; } pub fn lines (& self) -> & [RelativeBytePos] { if let Some (SourceFileLines :: Lines (lines)) = self . lines . get () { return & lines [..] ; } outline (| | { self . convert_diffs_to_lines_frozen () ; if let Some (SourceFileLines :: Lines (lines)) = self . lines . get () { return & lines [..] ; } unreachable ! () }) } # [doc = " Returns the `BytePos` of the beginning of the current line."] pub fn line_begin_pos (& self , pos : BytePos) -> BytePos { let pos = self . relative_position (pos) ; let line_index = self . lookup_line (pos) . unwrap () ; let line_start_pos = self . lines () [line_index] ; self . absolute_position (line_start_pos) } # [doc = " Add externally loaded source."] # [doc = " If the hash of the input doesn't match or no input is supplied via None,"] # [doc = " it is interpreted as an error and the corresponding enum variant is set."] # [doc = " The return value signifies whether some kind of source is present."] pub fn add_external_src < F > (& self , get_src : F) -> bool where F : FnOnce () -> Option < String > , { if ! self . external_src . is_frozen () { let src = get_src () ; let src = src . and_then (| mut src | { self . src_hash . matches (& src) . then (| | { normalize_src (& mut src) ; src }) }) ; self . external_src . try_write () . map (| mut external_src | { if let ExternalSource :: Foreign { kind : src_kind @ ExternalSourceKind :: AbsentOk , .. } = & mut * external_src { * src_kind = if let Some (src) = src { ExternalSourceKind :: Present (Arc :: new (src)) } else { ExternalSourceKind :: AbsentErr } ; } else { panic ! ("unexpected state {:?}" , * external_src) } FreezeWriteGuard :: freeze (external_src) }) ; } self . src . is_some () || self . external_src . read () . get_source () . is_some () } # [doc = " Gets a line from the list of pre-computed line-beginnings."] # [doc = " The line number here is 0-based."] pub fn get_line (& self , line_number : usize) -> Option < Cow < '_ , str > > { fn get_until_newline (src : & str , begin : usize) -> & str { let slice = & src [begin ..] ; match slice . find ('\n') { Some (e) => & slice [.. e] , None => slice , } } let begin = { let line = self . lines () . get (line_number) . copied () ? ; line . to_usize () } ; if let Some (ref src) = self . src { Some (Cow :: from (get_until_newline (src , begin))) } else { self . external_src . borrow () . get_source () . map (| src | Cow :: Owned (String :: from (get_until_newline (src , begin)))) } } pub fn is_real_file (& self) -> bool { self . name . is_real () } # [inline] pub fn is_imported (& self) -> bool { self . src . is_none () } pub fn count_lines (& self) -> usize { self . lines () . len () } # [inline] pub fn absolute_position (& self , pos : RelativeBytePos) -> BytePos { BytePos :: from_u32 (pos . to_u32 () + self . start_pos . to_u32 ()) } # [inline] pub fn relative_position (& self , pos : BytePos) -> RelativeBytePos { RelativeBytePos :: from_u32 (pos . to_u32 () - self . start_pos . to_u32 ()) } # [inline] pub fn end_position (& self) -> BytePos { self . absolute_position (self . source_len) } # [doc = " Finds the line containing the given position. The return value is the"] # [doc = " index into the `lines` array of this `SourceFile`, not the 1-based line"] # [doc = " number. If the source_file is empty or the position is located before the"] # [doc = " first line, `None` is returned."] pub fn lookup_line (& self , pos : RelativeBytePos) -> Option < usize > { self . lines () . partition_point (| x | x <= & pos) . checked_sub (1) } pub fn line_bounds (& self , line_index : usize) -> Range < BytePos > { if self . is_empty () { return self . start_pos .. self . start_pos ; } let lines = self . lines () ; assert ! (line_index < lines . len ()) ; if line_index == (lines . len () - 1) { self . absolute_position (lines [line_index]) .. self . end_position () } else { self . absolute_position (lines [line_index]) .. self . absolute_position (lines [line_index + 1]) } } # [doc = " Returns whether or not the file contains the given `SourceMap` byte"] # [doc = " position. The position one past the end of the file is considered to be"] # [doc = " contained by the file. This implies that files for which `is_empty`"] # [doc = " returns true still contain one byte position according to this function."] # [inline] pub fn contains (& self , byte_pos : BytePos) -> bool { byte_pos >= self . start_pos && byte_pos <= self . end_position () } # [inline] pub fn is_empty (& self) -> bool { self . source_len . to_u32 () == 0 } # [doc = " Calculates the original byte position relative to the start of the file"] # [doc = " based on the given byte position."] pub fn original_relative_byte_pos (& self , pos : BytePos) -> RelativeBytePos { let pos = self . relative_position (pos) ; let diff = match self . normalized_pos . binary_search_by (| np | np . pos . cmp (& pos)) { Ok (i) => self . normalized_pos [i] . diff , Err (0) => 0 , Err (i) => self . normalized_pos [i - 1] . diff , } ; RelativeBytePos :: from_u32 (pos . 0 + diff) } # [doc = " Calculates a normalized byte position from a byte offset relative to the"] # [doc = " start of the file."] # [doc = ""] # [doc = " When we get an inline assembler error from LLVM during codegen, we"] # [doc = " import the expanded assembly code as a new `SourceFile`, which can then"] # [doc = " be used for error reporting with spans. However the byte offsets given"] # [doc = " to us by LLVM are relative to the start of the original buffer, not the"] # [doc = " normalized one. Hence we need to convert those offsets to the normalized"] # [doc = " form when constructing spans."] pub fn normalized_byte_pos (& self , offset : u32) -> BytePos { let diff = match self . normalized_pos . binary_search_by (| np | (np . pos . 0 + np . diff) . cmp (& (self . start_pos . 0 + offset))) { Ok (i) => self . normalized_pos [i] . diff , Err (0) => 0 , Err (i) => self . normalized_pos [i - 1] . diff , } ; BytePos :: from_u32 (self . start_pos . 0 + offset - diff) } # [doc = " Converts an relative `RelativeBytePos` to a `CharPos` relative to the `SourceFile`."] fn bytepos_to_file_charpos (& self , bpos : RelativeBytePos) -> CharPos { let mut total_extra_bytes = 0 ; for mbc in self . multibyte_chars . iter () { debug ! ("{}-byte char at {:?}" , mbc . bytes , mbc . pos) ; if mbc . pos < bpos { total_extra_bytes += mbc . bytes as u32 - 1 ; assert ! (bpos . to_u32 () >= mbc . pos . to_u32 () + mbc . bytes as u32) ; } else { break ; } } assert ! (total_extra_bytes <= bpos . to_u32 ()) ; CharPos (bpos . to_usize () - total_extra_bytes as usize) } # [doc = " Looks up the file's (1-based) line number and (0-based `CharPos`) column offset, for a"] # [doc = " given `RelativeBytePos`."] fn lookup_file_pos (& self , pos : RelativeBytePos) -> (usize , CharPos) { let chpos = self . bytepos_to_file_charpos (pos) ; match self . lookup_line (pos) { Some (a) => { let line = a + 1 ; let linebpos = self . lines () [a] ; let linechpos = self . bytepos_to_file_charpos (linebpos) ; let col = chpos - linechpos ; debug ! ("byte pos {:?} is on the line at byte pos {:?}" , pos , linebpos) ; debug ! ("char pos {:?} is on the line at char pos {:?}" , chpos , linechpos) ; debug ! ("byte is on line: {}" , line) ; assert ! (chpos >= linechpos) ; (line , col) } None => (0 , chpos) , } } # [doc = " Looks up the file's (1-based) line number, (0-based `CharPos`) column offset, and (0-based)"] # [doc = " column offset when displayed, for a given `BytePos`."] pub fn lookup_file_pos_with_col_display (& self , pos : BytePos) -> (usize , CharPos , usize) { let pos = self . relative_position (pos) ; let (line , col_or_chpos) = self . lookup_file_pos (pos) ; if line > 0 { let Some (code) = self . get_line (line - 1) else { tracing :: info ! ("couldn't find line {line} {:?}" , self . name) ; return (line , col_or_chpos , col_or_chpos . 0) ; } ; let display_col = code . chars () . take (col_or_chpos . 0) . map (| ch | char_width (ch)) . sum () ; (line , col_or_chpos , display_col) } else { (0 , col_or_chpos , col_or_chpos . 0) } } }
    };
}

impl_89!()