macro_rules! ReasonPhrase {
    () => {
        # [doc = " A reason phrase in an HTTP/1 response."] # [doc = ""] # [doc = " # Clients"] # [doc = ""] # [doc = " For clients, a `ReasonPhrase` will be present in the extensions of the `http::Response` returned"] # [doc = " for a request if the reason phrase is different from the canonical reason phrase for the"] # [doc = " response's status code. For example, if a server returns `HTTP/1.1 200 Awesome`, the"] # [doc = " `ReasonPhrase` will be present and contain `Awesome`, but if a server returns `HTTP/1.1 200 OK`,"] # [doc = " the response will not contain a `ReasonPhrase`."] # [doc = ""] # [doc = " ```no_run"] # [doc = " # #[cfg(all(feature = \"tcp\", feature = \"client\", feature = \"http1\"))]"] # [doc = " # async fn fake_fetch() -> hyper::Result<()> {"] # [doc = " use hyper::{Client, Uri};"] # [doc = " use hyper::ext::ReasonPhrase;"] # [doc = ""] # [doc = " let res = Client::new().get(Uri::from_static(\"http://example.com/non_canonical_reason\")).await?;"] # [doc = ""] # [doc = " // Print out the non-canonical reason phrase, if it has one..."] # [doc = " if let Some(reason) = res.extensions().get::<ReasonPhrase>() {"] # [doc = "     println!(\"non-canonical reason: {}\", std::str::from_utf8(reason.as_bytes()).unwrap());"] # [doc = " }"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " # Servers"] # [doc = ""] # [doc = " When a `ReasonPhrase` is present in the extensions of the `http::Response` written by a server,"] # [doc = " its contents will be written in place of the canonical reason phrase when responding via HTTP/1."] # [derive (Clone , Debug , PartialEq , Eq , PartialOrd , Ord , Hash)] pub struct ReasonPhrase (Bytes) ;
    };
}

ReasonPhrase!()