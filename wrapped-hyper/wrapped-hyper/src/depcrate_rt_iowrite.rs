// Generated macro for Write (trait)
macro_rules! Depcrate_rt_ioWrite {
() => {
// Module: crate::rt::io
// Provides: {"Write"}
// Dependencies: {}
# [doc = " Write bytes asynchronously."] # [doc = ""] # [doc = " This trait is similar to `std::io::Write`, but for asynchronous writes."] pub trait Write { # [doc = " Attempt to write bytes from `buf` into the destination."] # [doc = ""] # [doc = " On success, returns `Poll::Ready(Ok(num_bytes_written)))`. If"] # [doc = " successful, it must be guaranteed that `n <= buf.len()`. A return value"] # [doc = " of `0` means that the underlying object is no longer able to accept"] # [doc = " bytes, or that the provided buffer is empty."] # [doc = ""] # [doc = " If the object is not ready for writing, the method returns"] # [doc = " `Poll::Pending` and arranges for the current task (via `cx.waker()`) to"] # [doc = " receive a notification when the object becomes writable or is closed."] fn poll_write (self : Pin < & mut Self > , cx : & mut Context < '_ > , buf : & [u8] ,) -> Poll < Result < usize , std :: io :: Error > > ; # [doc = " Attempts to flush the object."] # [doc = ""] # [doc = " On success, returns `Poll::Ready(Ok(()))`."] # [doc = ""] # [doc = " If flushing cannot immediately complete, this method returns"] # [doc = " `Poll::Pending` and arranges for the current task (via `cx.waker()`) to"] # [doc = " receive a notification when the object can make progress."] fn poll_flush (self : Pin < & mut Self > , cx : & mut Context < '_ >) -> Poll < Result < () , std :: io :: Error > > ; # [doc = " Attempts to shut down this writer."] fn poll_shutdown (self : Pin < & mut Self > , cx : & mut Context < '_ > ,) -> Poll < Result < () , std :: io :: Error > > ; # [doc = " Returns whether this writer has an efficient `poll_write_vectored`"] # [doc = " implementation."] # [doc = ""] # [doc = " The default implementation returns `false`."] fn is_write_vectored (& self) -> bool { false } # [doc = " Like `poll_write`, except that it writes from a slice of buffers."] fn poll_write_vectored (self : Pin < & mut Self > , cx : & mut Context < '_ > , bufs : & [std :: io :: IoSlice < '_ >] ,) -> Poll < Result < usize , std :: io :: Error > > { let buf = bufs . iter () . find (| b | ! b . is_empty ()) . map_or (& [] [..] , | b | & * * b) ; self . poll_write (cx , buf) } }
};
}
