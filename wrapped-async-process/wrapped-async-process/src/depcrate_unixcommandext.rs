// Generated macro for CommandExt (trait)
macro_rules! Depcrate_unixCommandExt {
() => {
// Module: crate::unix
// Provides: {"CommandExt"}
// Dependencies: {}
# [doc = " Unix-specific extensions to the [`Command`] builder."] # [doc = ""] # [doc = " This trait is sealed: it cannot be implemented outside `async-process`."] # [doc = " This is so that future additional methods are not breaking changes."] pub trait CommandExt : crate :: sealed :: Sealed { # [doc = " Sets the child process's user ID. This translates to a"] # [doc = " `setuid` call in the child process. Failure in the `setuid`"] # [doc = " call will cause the spawn to fail."] fn uid (& mut self , id : UserId) -> & mut Command ; # [doc = " Similar to `uid`, but sets the group ID of the child process. This has"] # [doc = " the same semantics as the `uid` field."] fn gid (& mut self , id : GroupId) -> & mut Command ; # [doc = " Performs all the required setup by this `Command`, followed by calling"] # [doc = " the `execvp` syscall."] # [doc = ""] # [doc = " On success this function will not return, and otherwise it will return"] # [doc = " an error indicating why the exec (or another part of the setup of the"] # [doc = " `Command`) failed."] # [doc = ""] # [doc = " `exec` not returning has the same implications as calling"] # [doc = " [`std::process::exit`] – no destructors on the current stack or any other"] # [doc = " thread’s stack will be run. Therefore, it is recommended to only call"] # [doc = " `exec` at a point where it is fine to not run any destructors. Note,"] # [doc = " that the `execvp` syscall independently guarantees that all memory is"] # [doc = " freed and all file descriptors with the `CLOEXEC` option (set by default"] # [doc = " on all file descriptors opened by the standard library) are closed."] # [doc = ""] # [doc = " This function, unlike `spawn`, will **not** `fork` the process to create"] # [doc = " a new child. Like spawn, however, the default behavior for the stdio"] # [doc = " descriptors will be to inherited from the current process."] # [doc = ""] # [doc = " # Notes"] # [doc = ""] # [doc = " The process may be in a \"broken state\" if this function returns in"] # [doc = " error. For example the working directory, environment variables, signal"] # [doc = " handling settings, various user/group information, or aspects of stdio"] # [doc = " file descriptors may have changed. If a \"transactional spawn\" is"] # [doc = " required to gracefully handle errors it is recommended to use the"] # [doc = " cross-platform `spawn` instead."] fn exec (& mut self) -> io :: Error ; # [doc = " Set executable argument"] # [doc = ""] # [doc = " Set the first process argument, `argv[0]`, to something other than the"] # [doc = " default executable path."] fn arg0 < S > (& mut self , arg : S) -> & mut Command where S : AsRef < OsStr > ; }
};
}
