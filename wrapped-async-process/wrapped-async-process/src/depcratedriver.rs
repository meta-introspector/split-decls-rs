// Generated macro for driver (function)
macro_rules! Depcratedriver {
() => {
// Module: crate
// Provides: {"driver"}
// Dependencies: {}
# [doc = " Runs the driver for the asynchronous processes."] # [doc = ""] # [doc = " This future takes control of global structures related to driving [`Child`]ren and reaping"] # [doc = " zombie processes. These responsibilities include listening for the `SIGCHLD` signal and"] # [doc = " making sure zombie processes are successfully waited on."] # [doc = ""] # [doc = " If multiple tasks run `driver()` at once, only one will actually drive the reaper; the other"] # [doc = " ones will just sleep. If a task that is driving the reaper is dropped, a previously sleeping"] # [doc = " task will take over. If all tasks driving the reaper are dropped, the \"async-process\" thread"] # [doc = " will be spawned. The \"async-process\" thread just blocks on this future and will automatically"] # [doc = " be spawned if no tasks are driving the reaper once a [`Child`] is created."] # [doc = ""] # [doc = " This future will never complete. It is intended to be ran on a background task in your"] # [doc = " executor of choice."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```no_run"] # [doc = " use async_executor::Executor;"] # [doc = " use async_process::{driver, Command};"] # [doc = ""] # [doc = " # futures_lite::future::block_on(async {"] # [doc = " // Create an executor and run on it."] # [doc = " let ex = Executor::new();"] # [doc = " ex.run(async {"] # [doc = "     // Run the driver future in the background."] # [doc = "     ex.spawn(driver()).detach();"] # [doc = ""] # [doc = "     // Run a command."] # [doc = "     Command::new(\"ls\").output().await.ok();"] # [doc = " }).await;"] # [doc = " # });"] # [doc = " ```"] # [allow (clippy :: manual_async_fn)] # [inline] pub fn driver () -> impl Future < Output = Infallible > + Send + 'static { async { let reaper = Reaper :: get () ; reaper . drivers . fetch_add (1 , Ordering :: SeqCst) ; let _guard = CallOnDrop (| | { let prev_count = reaper . drivers . fetch_sub (1 , Ordering :: SeqCst) ; if prev_count == 1 && (reaper . child_count . load (Ordering :: SeqCst) > 0 || reaper . sys . has_zombies ()) { reaper . ensure_driven () ; } }) ; let guard = reaper . sys . lock () . await ; reaper . sys . reap (guard) . await } }
};
}
