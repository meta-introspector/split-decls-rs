// Generated macro for Captures (trait)
macro_rules! DepcrateCaptures {
() => {
// Module: crate
// Provides: {"Captures"}
// Dependencies: {}
# [doc = " A trait that describes implementations of capturing groups."] # [doc = ""] # [doc = " When a matcher supports capturing group extraction, then it is the"] # [doc = " matcher's responsibility to provide an implementation of this trait."] # [doc = ""] # [doc = " Principally, this trait provides a way to access capturing groups"] # [doc = " in a uniform way that does not require any specific representation."] # [doc = " Namely, different matcher implementations may require different in-memory"] # [doc = " representations of capturing groups. This trait permits matchers to"] # [doc = " maintain their specific in-memory representation."] # [doc = ""] # [doc = " Note that this trait explicitly does not provide a way to construct a new"] # [doc = " capture value. Instead, it is the responsibility of a `Matcher` to build"] # [doc = " one, which might require knowledge of the matcher's internal implementation"] # [doc = " details."] pub trait Captures { # [doc = " Return the total number of capturing groups. This includes capturing"] # [doc = " groups that have not matched anything."] fn len (& self) -> usize ; # [doc = " Return the capturing group match at the given index. If no match of"] # [doc = " that capturing group exists, then this returns `None`."] # [doc = ""] # [doc = " When a matcher reports a match with capturing groups, then the first"] # [doc = " capturing group (at index `0`) must always correspond to the offsets"] # [doc = " for the overall match."] fn get (& self , i : usize) -> Option < Match > ; # [doc = " Return the overall match for the capture."] # [doc = ""] # [doc = " This returns the match for index `0`. That is it is equivalent to"] # [doc = " `get(0).unwrap()`"] # [inline] fn as_match (& self) -> Match { self . get (0) . unwrap () } # [doc = " Returns true if and only if these captures are empty. This occurs"] # [doc = " when `len` is `0`."] # [doc = ""] # [doc = " Note that capturing groups that have non-zero length but otherwise"] # [doc = " contain no matching groups are *not* empty."] # [inline] fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Expands all instances of `$name` in `replacement` to the corresponding"] # [doc = " capture group `name`, and writes them to the `dst` buffer given."] # [doc = ""] # [doc = " (Note: If you're looking for a convenient way to perform replacements"] # [doc = " with interpolation, then you'll want to use the `replace_with_captures`"] # [doc = " method on the `Matcher` trait.)"] # [doc = ""] # [doc = " `name` may be an integer corresponding to the index of the"] # [doc = " capture group (counted by order of opening parenthesis where `0` is the"] # [doc = " entire match) or it can be a name (consisting of letters, digits or"] # [doc = " underscores) corresponding to a named capture group."] # [doc = ""] # [doc = " A `name` is translated to a capture group index via the given"] # [doc = " `name_to_index` function. If `name` isn't a valid capture group"] # [doc = " (whether the name doesn't exist or isn't a valid index), then it is"] # [doc = " replaced with the empty string."] # [doc = ""] # [doc = " The longest possible name is used. e.g., `$1a` looks up the capture"] # [doc = " group named `1a` and not the capture group at index `1`. To exert"] # [doc = " more precise control over the name, use braces, e.g., `${1}a`. In all"] # [doc = " cases, capture group names are limited to ASCII letters, numbers and"] # [doc = " underscores."] # [doc = ""] # [doc = " To write a literal `$` use `$$`."] # [doc = ""] # [doc = " Note that the capture group match indices are resolved by slicing"] # [doc = " the given `haystack`. Generally, this means that `haystack` should be"] # [doc = " the same slice that was searched to get the current capture group"] # [doc = " matches."] # [inline] fn interpolate < F > (& self , name_to_index : F , haystack : & [u8] , replacement : & [u8] , dst : & mut Vec < u8 > ,) where F : FnMut (& str) -> Option < usize > , { interpolate (replacement , | i , dst | { if let Some (range) = self . get (i) { dst . extend (& haystack [range]) ; } } , name_to_index , dst ,) } }
};
}
