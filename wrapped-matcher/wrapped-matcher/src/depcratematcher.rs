// Generated macro for Matcher (trait)
macro_rules! DepcrateMatcher {
() => {
// Module: crate
// Provides: {"Matcher"}
// Dependencies: {}
# [doc = " A matcher defines an interface for regular expression implementations."] # [doc = ""] # [doc = " While this trait is large, there are only two required methods that"] # [doc = " implementors must provide: `find_at` and `new_captures`. If captures aren't"] # [doc = " supported by your implementation, then `new_captures` can be implemented"] # [doc = " with [`NoCaptures`]. If your implementation does support capture groups,"] # [doc = " then you should also implement the other capture related methods, as"] # [doc = " dictated by the documentation. Crucially, this includes `captures_at`."] # [doc = ""] # [doc = " The rest of the methods on this trait provide default implementations on"] # [doc = " top of `find_at` and `new_captures`. It is not uncommon for implementations"] # [doc = " to be able to provide faster variants of some methods; in those cases,"] # [doc = " simply override the default implementation."] pub trait Matcher { # [doc = " The concrete type of capturing groups used for this matcher."] # [doc = ""] # [doc = " If this implementation does not support capturing groups, then set"] # [doc = " this to `NoCaptures`."] type Captures : Captures ; # [doc = " The error type used by this matcher."] # [doc = ""] # [doc = " For matchers in which an error is not possible, they are encouraged to"] # [doc = " use the `NoError` type in this crate. In the future, when the \"never\""] # [doc = " (spelled `!`) type is stabilized, then it should probably be used"] # [doc = " instead."] type Error : std :: fmt :: Display ; # [doc = " Returns the start and end byte range of the first match in `haystack`"] # [doc = " after `at`, where the byte offsets are relative to that start of"] # [doc = " `haystack` (and not `at`). If no match exists, then `None` is returned."] # [doc = ""] # [doc = " The text encoding of `haystack` is not strictly specified. Matchers are"] # [doc = " advised to assume UTF-8, or at worst, some ASCII compatible encoding."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `at == 0`."] fn find_at (& self , haystack : & [u8] , at : usize ,) -> Result < Option < Match > , Self :: Error > ; # [doc = " Creates an empty group of captures suitable for use with the capturing"] # [doc = " APIs of this trait."] # [doc = ""] # [doc = " Implementations that don't support capturing groups should use"] # [doc = " the `NoCaptures` type and implement this method by calling"] # [doc = " `NoCaptures::new()`."] fn new_captures (& self) -> Result < Self :: Captures , Self :: Error > ; # [doc = " Returns the total number of capturing groups in this matcher."] # [doc = ""] # [doc = " If a matcher supports capturing groups, then this value must always be"] # [doc = " at least 1, where the first capturing group always corresponds to the"] # [doc = " overall match."] # [doc = ""] # [doc = " If a matcher does not support capturing groups, then this should"] # [doc = " always return 0."] # [doc = ""] # [doc = " By default, capturing groups are not supported, so this always"] # [doc = " returns 0."] # [inline] fn capture_count (& self) -> usize { 0 } # [doc = " Maps the given capture group name to its corresponding capture group"] # [doc = " index, if one exists. If one does not exist, then `None` is returned."] # [doc = ""] # [doc = " If the given capture group name maps to multiple indices, then it is"] # [doc = " not specified which one is returned. However, it is guaranteed that"] # [doc = " one of them is returned."] # [doc = ""] # [doc = " By default, capturing groups are not supported, so this always returns"] # [doc = " `None`."] # [inline] fn capture_index (& self , _name : & str) -> Option < usize > { None } # [doc = " Returns the start and end byte range of the first match in `haystack`."] # [doc = " If no match exists, then `None` is returned."] # [doc = ""] # [doc = " The text encoding of `haystack` is not strictly specified. Matchers are"] # [doc = " advised to assume UTF-8, or at worst, some ASCII compatible encoding."] # [inline] fn find (& self , haystack : & [u8]) -> Result < Option < Match > , Self :: Error > { self . find_at (haystack , 0) } # [doc = " Executes the given function over successive non-overlapping matches"] # [doc = " in `haystack`. If no match exists, then the given function is never"] # [doc = " called. If the function returns `false`, then iteration stops."] # [inline] fn find_iter < F > (& self , haystack : & [u8] , matched : F ,) -> Result < () , Self :: Error > where F : FnMut (Match) -> bool , { self . find_iter_at (haystack , 0 , matched) } # [doc = " Executes the given function over successive non-overlapping matches"] # [doc = " in `haystack`. If no match exists, then the given function is never"] # [doc = " called. If the function returns `false`, then iteration stops."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `at == 0`."] # [inline] fn find_iter_at < F > (& self , haystack : & [u8] , at : usize , mut matched : F ,) -> Result < () , Self :: Error > where F : FnMut (Match) -> bool , { self . try_find_iter_at (haystack , at , | m | Ok (matched (m))) . map (| r : Result < () , () > | r . unwrap ()) } # [doc = " Executes the given function over successive non-overlapping matches"] # [doc = " in `haystack`. If no match exists, then the given function is never"] # [doc = " called. If the function returns `false`, then iteration stops."] # [doc = " Similarly, if the function returns an error then iteration stops and"] # [doc = " the error is yielded. If an error occurs while executing the search,"] # [doc = " then it is converted to"] # [doc = " `E`."] # [inline] fn try_find_iter < F , E > (& self , haystack : & [u8] , matched : F ,) -> Result < Result < () , E > , Self :: Error > where F : FnMut (Match) -> Result < bool , E > , { self . try_find_iter_at (haystack , 0 , matched) } # [doc = " Executes the given function over successive non-overlapping matches"] # [doc = " in `haystack`. If no match exists, then the given function is never"] # [doc = " called. If the function returns `false`, then iteration stops."] # [doc = " Similarly, if the function returns an error then iteration stops and"] # [doc = " the error is yielded. If an error occurs while executing the search,"] # [doc = " then it is converted to"] # [doc = " `E`."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `at == 0`."] # [inline] fn try_find_iter_at < F , E > (& self , haystack : & [u8] , at : usize , mut matched : F ,) -> Result < Result < () , E > , Self :: Error > where F : FnMut (Match) -> Result < bool , E > , { let mut last_end = at ; let mut last_match = None ; loop { if last_end > haystack . len () { return Ok (Ok (())) ; } let m = match self . find_at (haystack , last_end) ? { None => return Ok (Ok (())) , Some (m) => m , } ; if m . start == m . end { last_end = m . end + 1 ; if Some (m . end) == last_match { continue ; } } else { last_end = m . end ; } last_match = Some (m . end) ; match matched (m) { Ok (true) => continue , Ok (false) => return Ok (Ok (())) , Err (err) => return Ok (Err (err)) , } } } # [doc = " Populates the first set of capture group matches from `haystack` into"] # [doc = " `caps`. If no match exists, then `false` is returned."] # [doc = ""] # [doc = " The text encoding of `haystack` is not strictly specified. Matchers are"] # [doc = " advised to assume UTF-8, or at worst, some ASCII compatible encoding."] # [inline] fn captures (& self , haystack : & [u8] , caps : & mut Self :: Captures ,) -> Result < bool , Self :: Error > { self . captures_at (haystack , 0 , caps) } # [doc = " Executes the given function over successive non-overlapping matches"] # [doc = " in `haystack` with capture groups extracted from each match. If no"] # [doc = " match exists, then the given function is never called. If the function"] # [doc = " returns `false`, then iteration stops."] # [inline] fn captures_iter < F > (& self , haystack : & [u8] , caps : & mut Self :: Captures , matched : F ,) -> Result < () , Self :: Error > where F : FnMut (& Self :: Captures) -> bool , { self . captures_iter_at (haystack , 0 , caps , matched) } # [doc = " Executes the given function over successive non-overlapping matches"] # [doc = " in `haystack` with capture groups extracted from each match. If no"] # [doc = " match exists, then the given function is never called. If the function"] # [doc = " returns `false`, then iteration stops."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `at == 0`."] # [inline] fn captures_iter_at < F > (& self , haystack : & [u8] , at : usize , caps : & mut Self :: Captures , mut matched : F ,) -> Result < () , Self :: Error > where F : FnMut (& Self :: Captures) -> bool , { self . try_captures_iter_at (haystack , at , caps , | caps | Ok (matched (caps))) . map (| r : Result < () , () > | r . unwrap ()) } # [doc = " Executes the given function over successive non-overlapping matches"] # [doc = " in `haystack` with capture groups extracted from each match. If no"] # [doc = " match exists, then the given function is never called. If the function"] # [doc = " returns `false`, then iteration stops. Similarly, if the function"] # [doc = " returns an error then iteration stops and the error is yielded. If"] # [doc = " an error occurs while executing the search, then it is converted to"] # [doc = " `E`."] # [inline] fn try_captures_iter < F , E > (& self , haystack : & [u8] , caps : & mut Self :: Captures , matched : F ,) -> Result < Result < () , E > , Self :: Error > where F : FnMut (& Self :: Captures) -> Result < bool , E > , { self . try_captures_iter_at (haystack , 0 , caps , matched) } # [doc = " Executes the given function over successive non-overlapping matches"] # [doc = " in `haystack` with capture groups extracted from each match. If no"] # [doc = " match exists, then the given function is never called. If the function"] # [doc = " returns `false`, then iteration stops. Similarly, if the function"] # [doc = " returns an error then iteration stops and the error is yielded. If"] # [doc = " an error occurs while executing the search, then it is converted to"] # [doc = " `E`."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `at == 0`."] # [inline] fn try_captures_iter_at < F , E > (& self , haystack : & [u8] , at : usize , caps : & mut Self :: Captures , mut matched : F ,) -> Result < Result < () , E > , Self :: Error > where F : FnMut (& Self :: Captures) -> Result < bool , E > , { let mut last_end = at ; let mut last_match = None ; loop { if last_end > haystack . len () { return Ok (Ok (())) ; } if ! self . captures_at (haystack , last_end , caps) ? { return Ok (Ok (())) ; } let m = caps . get (0) . unwrap () ; if m . start == m . end { last_end = m . end + 1 ; if Some (m . end) == last_match { continue ; } } else { last_end = m . end ; } last_match = Some (m . end) ; match matched (caps) { Ok (true) => continue , Ok (false) => return Ok (Ok (())) , Err (err) => return Ok (Err (err)) , } } } # [doc = " Populates the first set of capture group matches from `haystack`"] # [doc = " into `matches` after `at`, where the byte offsets in each capturing"] # [doc = " group are relative to the start of `haystack` (and not `at`). If no"] # [doc = " match exists, then `false` is returned and the contents of the given"] # [doc = " capturing groups are unspecified."] # [doc = ""] # [doc = " The text encoding of `haystack` is not strictly specified. Matchers are"] # [doc = " advised to assume UTF-8, or at worst, some ASCII compatible encoding."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `at == 0`."] # [doc = ""] # [doc = " By default, capturing groups aren't supported, and this implementation"] # [doc = " will always behave as if a match were impossible."] # [doc = ""] # [doc = " Implementors that provide support for capturing groups must guarantee"] # [doc = " that when a match occurs, the first capture match (at index `0`) is"] # [doc = " always set to the overall match offsets."] # [doc = ""] # [doc = " Note that if implementors seek to support capturing groups, then they"] # [doc = " should implement this method. Other methods that match based on"] # [doc = " captures will then work automatically."] # [inline] fn captures_at (& self , _haystack : & [u8] , _at : usize , _caps : & mut Self :: Captures ,) -> Result < bool , Self :: Error > { Ok (false) } # [doc = " Replaces every match in the given haystack with the result of calling"] # [doc = " `append`. `append` is given the start and end of a match, along with"] # [doc = " a handle to the `dst` buffer provided."] # [doc = ""] # [doc = " If the given `append` function returns `false`, then replacement stops."] # [inline] fn replace < F > (& self , haystack : & [u8] , dst : & mut Vec < u8 > , mut append : F ,) -> Result < () , Self :: Error > where F : FnMut (Match , & mut Vec < u8 >) -> bool , { let mut last_match = 0 ; self . find_iter (haystack , | m | { dst . extend (& haystack [last_match .. m . start]) ; last_match = m . end ; append (m , dst) }) ? ; dst . extend (& haystack [last_match ..]) ; Ok (()) } # [doc = " Replaces every match in the given haystack with the result of calling"] # [doc = " `append` with the matching capture groups."] # [doc = ""] # [doc = " If the given `append` function returns `false`, then replacement stops."] # [inline] fn replace_with_captures < F > (& self , haystack : & [u8] , caps : & mut Self :: Captures , dst : & mut Vec < u8 > , append : F ,) -> Result < () , Self :: Error > where F : FnMut (& Self :: Captures , & mut Vec < u8 >) -> bool , { self . replace_with_captures_at (haystack , 0 , caps , dst , append) } # [doc = " Replaces every match in the given haystack with the result of calling"] # [doc = " `append` with the matching capture groups."] # [doc = ""] # [doc = " If the given `append` function returns `false`, then replacement stops."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `at == 0`."] # [inline] fn replace_with_captures_at < F > (& self , haystack : & [u8] , at : usize , caps : & mut Self :: Captures , dst : & mut Vec < u8 > , mut append : F ,) -> Result < () , Self :: Error > where F : FnMut (& Self :: Captures , & mut Vec < u8 >) -> bool , { let mut last_match = at ; self . captures_iter_at (haystack , at , caps , | caps | { let m = caps . get (0) . unwrap () ; dst . extend (& haystack [last_match .. m . start]) ; last_match = m . end ; append (caps , dst) }) ? ; dst . extend (& haystack [last_match ..]) ; Ok (()) } # [doc = " Returns true if and only if the matcher matches the given haystack."] # [doc = ""] # [doc = " By default, this method is implemented by calling `shortest_match`."] # [inline] fn is_match (& self , haystack : & [u8]) -> Result < bool , Self :: Error > { self . is_match_at (haystack , 0) } # [doc = " Returns true if and only if the matcher matches the given haystack"] # [doc = " starting at the given position."] # [doc = ""] # [doc = " By default, this method is implemented by calling `shortest_match_at`."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `at == 0`."] # [inline] fn is_match_at (& self , haystack : & [u8] , at : usize ,) -> Result < bool , Self :: Error > { Ok (self . shortest_match_at (haystack , at) ? . is_some ()) } # [doc = " Returns an end location of the first match in `haystack`. If no match"] # [doc = " exists, then `None` is returned."] # [doc = ""] # [doc = " Note that the end location reported by this method may be less than the"] # [doc = " same end location reported by `find`. For example, running `find` with"] # [doc = " the pattern `a+` on the haystack `aaa` should report a range of `[0,"] # [doc = " 3)`, but `shortest_match` may report `1` as the ending location since"] # [doc = " that is the place at which a match is guaranteed to occur."] # [doc = ""] # [doc = " This method should never report false positives or false negatives. The"] # [doc = " point of this method is that some implementors may be able to provide"] # [doc = " a faster implementation of this than what `find` does."] # [doc = ""] # [doc = " By default, this method is implemented by calling `find`."] # [inline] fn shortest_match (& self , haystack : & [u8] ,) -> Result < Option < usize > , Self :: Error > { self . shortest_match_at (haystack , 0) } # [doc = " Returns an end location of the first match in `haystack` starting at"] # [doc = " the given position. If no match exists, then `None` is returned."] # [doc = ""] # [doc = " Note that the end location reported by this method may be less than the"] # [doc = " same end location reported by `find`. For example, running `find` with"] # [doc = " the pattern `a+` on the haystack `aaa` should report a range of `[0,"] # [doc = " 3)`, but `shortest_match` may report `1` as the ending location since"] # [doc = " that is the place at which a match is guaranteed to occur."] # [doc = ""] # [doc = " This method should never report false positives or false negatives. The"] # [doc = " point of this method is that some implementors may be able to provide"] # [doc = " a faster implementation of this than what `find` does."] # [doc = ""] # [doc = " By default, this method is implemented by calling `find_at`."] # [doc = ""] # [doc = " The significance of the starting point is that it takes the surrounding"] # [doc = " context into consideration. For example, the `\\A` anchor can only"] # [doc = " match when `at == 0`."] # [inline] fn shortest_match_at (& self , haystack : & [u8] , at : usize ,) -> Result < Option < usize > , Self :: Error > { Ok (self . find_at (haystack , at) ? . map (| m | m . end)) } # [doc = " If available, return a set of bytes that will never appear in a match"] # [doc = " produced by an implementation."] # [doc = ""] # [doc = " Specifically, if such a set can be determined, then it's possible for"] # [doc = " callers to perform additional operations on the basis that certain"] # [doc = " bytes may never match."] # [doc = ""] # [doc = " For example, if a search is configured to possibly produce results"] # [doc = " that span multiple lines but a caller provided pattern can never"] # [doc = " match across multiple lines, then it may make sense to divert to"] # [doc = " more optimized line oriented routines that don't need to handle the"] # [doc = " multi-line match case."] # [doc = ""] # [doc = " Implementations that produce this set must never report false"] # [doc = " positives, but may produce false negatives. That is, is a byte is in"] # [doc = " this set then it must be guaranteed that it is never in a match. But,"] # [doc = " if a byte is not in this set, then callers cannot assume that a match"] # [doc = " exists with that byte."] # [doc = ""] # [doc = " By default, this returns `None`."] # [inline] fn non_matching_bytes (& self) -> Option < & ByteSet > { None } # [doc = " If this matcher was compiled as a line oriented matcher, then this"] # [doc = " method returns the line terminator if and only if the line terminator"] # [doc = " never appears in any match produced by this matcher. If this wasn't"] # [doc = " compiled as a line oriented matcher, or if the aforementioned guarantee"] # [doc = " cannot be made, then this must return `None`, which is the default."] # [doc = " It is **never wrong** to return `None`, but returning a line terminator"] # [doc = " when it can appear in a match results in unspecified behavior."] # [doc = ""] # [doc = " The line terminator is typically `b'\\n'`, but can be any single byte or"] # [doc = " `CRLF`."] # [doc = ""] # [doc = " By default, this returns `None`."] # [inline] fn line_terminator (& self) -> Option < LineTerminator > { None } # [doc = " Return one of the following: a confirmed line match, a candidate line"] # [doc = " match (which may be a false positive) or no match at all (which **must"] # [doc = " not** be a false negative). When reporting a confirmed or candidate"] # [doc = " match, the position returned can be any position in the line."] # [doc = ""] # [doc = " By default, this never returns a candidate match, and always either"] # [doc = " returns a confirmed match or no match at all."] # [doc = ""] # [doc = " When a matcher can match spans over multiple lines, then the behavior"] # [doc = " of this method is unspecified. Namely, use of this method only"] # [doc = " makes sense in a context where the caller is looking for the next"] # [doc = " matching line. That is, callers should only use this method when"] # [doc = " `line_terminator` does not return `None`."] # [doc = ""] # [doc = " # Design rationale"] # [doc = ""] # [doc = " A line matcher is, fundamentally, a normal matcher with the addition"] # [doc = " of one optional method: finding a line. By default, this routine"] # [doc = " is implemented via the matcher's `shortest_match` method, which"] # [doc = " always yields either no match or a `LineMatchKind::Confirmed`. However,"] # [doc = " implementors may provide a routine for this that can return candidate"] # [doc = " lines that need subsequent verification to be confirmed as a match."] # [doc = " This can be useful in cases where it may be quicker to find candidate"] # [doc = " lines via some other means instead of relying on the more general"] # [doc = " implementations for `find` and `shortest_match`."] # [doc = ""] # [doc = " For example, consider the regex `\\w+foo\\s+`. Both `find` and"] # [doc = " `shortest_match` must consider the entire regex, including the `\\w+`"] # [doc = " and `\\s+`, while searching. However, this method could look for lines"] # [doc = " containing `foo` and return them as candidates. Finding `foo` might"] # [doc = " be implemented as a highly optimized substring search routine (like"] # [doc = " `memmem`), which is likely to be faster than whatever more generalized"] # [doc = " routine is required for resolving `\\w+foo\\s+`. The caller is then"] # [doc = " responsible for confirming whether a match exists or not."] # [doc = ""] # [doc = " Note that while this method may report false positives, it must never"] # [doc = " report false negatives. That is, it can never skip over lines that"] # [doc = " contain a match."] # [inline] fn find_candidate_line (& self , haystack : & [u8] ,) -> Result < Option < LineMatchKind > , Self :: Error > { Ok (self . shortest_match (haystack) ? . map (LineMatchKind :: Confirmed)) } }
};
}
