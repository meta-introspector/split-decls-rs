macro_rules! deps {
    () => {
        Value!();
        ArrayIter!();
        ArrayIterMut!();
        Array!();
        Item!();
    };
}

macro_rules! impl_6 {
    () => {
        deps!();
        impl Array { # [doc = " Returns an iterator over all values."] pub fn iter (& self) -> ArrayIter < '_ > { Box :: new (self . values . iter () . filter_map (Item :: as_value)) } # [doc = " Returns an iterator over all values."] pub fn iter_mut (& mut self) -> ArrayIterMut < '_ > { Box :: new (self . values . iter_mut () . filter_map (Item :: as_value_mut)) } # [doc = " Returns the length of the underlying Vec."] # [doc = ""] # [doc = " In some rare cases, placeholder elements will exist.  For a more accurate count, call"] # [doc = " `a.iter().count()`"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " let mut arr = toml_edit::Array::new();"] # [doc = " arr.push(1);"] # [doc = " arr.push(\"foo\");"] # [doc = " assert_eq!(arr.len(), 2);"] # [doc = " ```"] pub fn len (& self) -> usize { self . values . len () } # [doc = " Return true if `self.len() == 0`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " let mut arr = toml_edit::Array::new();"] # [doc = " assert!(arr.is_empty());"] # [doc = ""] # [doc = " arr.push(1);"] # [doc = " arr.push(\"foo\");"] # [doc = " assert!(! arr.is_empty());"] # [doc = " ```"] pub fn is_empty (& self) -> bool { self . len () == 0 } # [doc = " Clears the array, removing all values. Keeps the allocated memory for reuse."] pub fn clear (& mut self) { self . values . clear () ; } # [doc = " Returns a reference to the value at the given index, or `None` if the index is out of"] # [doc = " bounds."] pub fn get (& self , index : usize) -> Option < & Value > { self . values . get (index) . and_then (Item :: as_value) } # [doc = " Returns a reference to the value at the given index, or `None` if the index is out of"] # [doc = " bounds."] pub fn get_mut (& mut self , index : usize) -> Option < & mut Value > { self . values . get_mut (index) . and_then (Item :: as_value_mut) } # [doc = " Appends a new value to the end of the array, applying default formatting to it."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " let mut arr = toml_edit::Array::new();"] # [doc = " arr.push(1);"] # [doc = " arr.push(\"foo\");"] # [doc = " ```"] pub fn push < V : Into < Value > > (& mut self , v : V) { self . values . push (Item :: Value (v . into ())) ; } # [doc = " Appends a new, already formatted value to the end of the array."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # #[cfg(feature = \"parse\")] {"] # [doc = " let formatted_value = \"'literal'\".parse::<toml_edit::Value>().unwrap();"] # [doc = " let mut arr = toml_edit::Array::new();"] # [doc = " arr.push_formatted(formatted_value);"] # [doc = " # }"] # [doc = " ```"] pub fn push_formatted (& mut self , v : Value) { self . values . push (Item :: Value (v)) ; } # [doc = " Inserts an element at the given position within the array, applying default formatting to"] # [doc = " it and shifting all values after it to the right."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `index > len`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " let mut arr = toml_edit::Array::new();"] # [doc = " arr.push(1);"] # [doc = " arr.push(\"foo\");"] # [doc = ""] # [doc = " arr.insert(0, \"start\");"] # [doc = " ```"] pub fn insert < V : Into < Value > > (& mut self , index : usize , v : V) { self . values . insert (index , Item :: Value (v . into ())) ; } # [doc = " Inserts an already formatted value at the given position within the array, shifting all"] # [doc = " values after it to the right."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `index > len`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # #[cfg(feature = \"parse\")] {"] # [doc = " let mut arr = toml_edit::Array::new();"] # [doc = " arr.push(1);"] # [doc = " arr.push(\"foo\");"] # [doc = ""] # [doc = " let formatted_value = \"'start'\".parse::<toml_edit::Value>().unwrap();"] # [doc = " arr.insert_formatted(0, formatted_value);"] # [doc = " # }"] # [doc = " ```"] pub fn insert_formatted (& mut self , index : usize , v : Value) { self . values . insert (index , Item :: Value (v)) ; } # [doc = " Replaces the element at the given position within the array, preserving existing formatting."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `index >= len`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " let mut arr = toml_edit::Array::new();"] # [doc = " arr.push(1);"] # [doc = " arr.push(\"foo\");"] # [doc = ""] # [doc = " arr.replace(0, \"start\");"] # [doc = " ```"] pub fn replace < V : Into < Value > > (& mut self , index : usize , v : V) -> Value { let existing_decor = self . get (index) . unwrap_or_else (| | panic ! ("index {} out of bounds (len = {})" , index , self . len ())) . decor () ; let mut value = v . into () ; * value . decor_mut () = existing_decor . clone () ; self . replace_formatted (index , value) } # [doc = " Replaces the element at the given position within the array with an already formatted value."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if `index >= len`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # #[cfg(feature = \"parse\")] {"] # [doc = " let mut arr = toml_edit::Array::new();"] # [doc = " arr.push(1);"] # [doc = " arr.push(\"foo\");"] # [doc = ""] # [doc = " let formatted_value = \"'start'\".parse::<toml_edit::Value>().unwrap();"] # [doc = " arr.replace_formatted(0, formatted_value);"] # [doc = " # }"] # [doc = " ```"] pub fn replace_formatted (& mut self , index : usize , v : Value) -> Value { match mem :: replace (& mut self . values [index] , Item :: Value (v)) { Item :: Value (old_value) => old_value , x => panic ! ("non-value item {x:?} in an array") , } } # [doc = " Removes the value at the given index."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " let mut arr = toml_edit::Array::new();"] # [doc = " arr.push(1);"] # [doc = " arr.push(\"foo\");"] # [doc = ""] # [doc = " arr.remove(0);"] # [doc = " assert_eq!(arr.len(), 1);"] # [doc = " ```"] pub fn remove (& mut self , index : usize) -> Value { let removed = self . values . remove (index) ; match removed { Item :: Value (v) => v , x => panic ! ("non-value item {x:?} in an array") , } } # [doc = " Retains only the values specified by the `keep` predicate."] # [doc = ""] # [doc = " In other words, remove all values for which `keep(&value)` returns `false`."] # [doc = ""] # [doc = " This method operates in place, visiting each element exactly once in the"] # [doc = " original order, and preserves the order of the retained elements."] pub fn retain < F > (& mut self , mut keep : F) where F : FnMut (& Value) -> bool , { self . values . retain (| item | item . as_value () . map (& mut keep) . unwrap_or (false)) ; } # [doc = " Sorts the slice with a comparator function."] # [doc = ""] # [doc = " This sort is stable (i.e., does not reorder equal elements) and *O*(*n* \\* log(*n*)) worst-case."] # [doc = ""] # [doc = " The comparator function must define a total ordering for the elements in the slice. If"] # [doc = " the ordering is not total, the order of the elements is unspecified. An order is a"] # [doc = " total order if it is (for all `a`, `b` and `c`):"] # [doc = ""] # [doc = " * total and antisymmetric: exactly one of `a < b`, `a == b` or `a > b` is true, and"] # [doc = " * transitive, `a < b` and `b < c` implies `a < c`. The same must hold for both `==` and `>`."] # [doc = ""] # [doc = " For example, while [`f64`] doesn't implement [`Ord`] because `NaN != NaN`, we can use"] # [doc = " `partial_cmp` as our sort function when we know the slice doesn't contain a `NaN`."] # [inline] pub fn sort_by < F > (& mut self , mut compare : F) where F : FnMut (& Value , & Value) -> std :: cmp :: Ordering , { self . values . sort_by (move | lhs , rhs | { let lhs = lhs . as_value () ; let rhs = rhs . as_value () ; match (lhs , rhs) { (None , None) => std :: cmp :: Ordering :: Equal , (Some (_) , None) => std :: cmp :: Ordering :: Greater , (None , Some (_)) => std :: cmp :: Ordering :: Less , (Some (lhs) , Some (rhs)) => compare (lhs , rhs) , } }) ; } # [doc = " Sorts the array with a key extraction function."] # [doc = ""] # [doc = " This sort is stable (i.e., does not reorder equal elements) and *O*(*m* \\* *n* \\* log(*n*))"] # [doc = " worst-case, where the key function is *O*(*m*)."] # [inline] pub fn sort_by_key < K , F > (& mut self , mut f : F) where F : FnMut (& Value) -> K , K : Ord , { # [allow (clippy :: manual_map)] self . values . sort_by_key (move | item | { if let Some (value) = item . as_value () { Some (f (value)) } else { None } }) ; } }
    };
}

impl_6!()