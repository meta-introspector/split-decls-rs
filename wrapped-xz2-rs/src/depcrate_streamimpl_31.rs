// Generated macro for impl_31 (impl)
macro_rules! Depcrate_streamimpl_31 {
() => {
// Module: crate::stream
// Provides: {"impl_31"}
// Dependencies: {}
impl MtStreamBuilder { # [doc = " Creates a new blank builder to create a multithreaded encoding `Stream`."] pub fn new () -> MtStreamBuilder { unsafe { let mut init = MtStreamBuilder { raw : mem :: zeroed () , filters : None , } ; init . raw . threads = 1 ; return init ; } } # [doc = " Configures the number of worker threads to use"] pub fn threads (& mut self , threads : u32) -> & mut Self { self . raw . threads = threads ; self } # [doc = " Configures the maximum uncompressed size of a block"] # [doc = ""] # [doc = " The encoder will start a new .xz block every `block_size` bytes."] # [doc = " Using `FullFlush` or `FullBarrier` with `process` the caller may tell"] # [doc = " liblzma to start a new block earlier."] # [doc = ""] # [doc = " With LZMA2, a recommended block size is 2-4 times the LZMA2 dictionary"] # [doc = " size. With very small dictionaries, it is recommended to use at least 1"] # [doc = " MiB block size for good compression ratio, even if this is more than"] # [doc = " four times the dictionary size. Note that these are only recommendations"] # [doc = " for typical use cases; feel free to use other values. Just keep in mind"] # [doc = " that using a block size less than the LZMA2 dictionary size is waste of"] # [doc = " RAM."] # [doc = ""] # [doc = " Set this to 0 to let liblzma choose the block size depending on the"] # [doc = " compression options. For LZMA2 it will be 3*`dict_size` or 1 MiB,"] # [doc = " whichever is more."] # [doc = ""] # [doc = " For each thread, about 3 * `block_size` bytes of memory will be"] # [doc = " allocated. This may change in later liblzma versions. If so, the memory"] # [doc = " usage will probably be reduced, not increased."] pub fn block_size (& mut self , block_size : u64) -> & mut Self { self . raw . block_size = block_size ; self } # [doc = " Timeout to allow `process` to return early"] # [doc = ""] # [doc = " Multithreading can make liblzma to consume input and produce output in a"] # [doc = " very bursty way: it may first read a lot of input to fill internal"] # [doc = " buffers, then no input or output occurs for a while."] # [doc = ""] # [doc = " In single-threaded mode, `process` won't return until it has either"] # [doc = " consumed all the input or filled the output buffer. If this is done in"] # [doc = " multithreaded mode, it may cause a call `process` to take even tens of"] # [doc = " seconds, which isn't acceptable in all applications."] # [doc = ""] # [doc = " To avoid very long blocking times in `process`, a timeout (in"] # [doc = " milliseconds) may be set here. If `process would block longer than"] # [doc = " this number of milliseconds, it will return with `Ok`. Reasonable"] # [doc = " values are 100 ms or more. The xz command line tool uses 300 ms."] # [doc = ""] # [doc = " If long blocking times are fine for you, set timeout to a special"] # [doc = " value of 0, which will disable the timeout mechanism and will make"] # [doc = " `process` block until all the input is consumed or the output"] # [doc = " buffer has been filled."] pub fn timeout_ms (& mut self , timeout : u32) -> & mut Self { self . raw . timeout = timeout ; self } # [doc = " Compression preset (level and possible flags)"] # [doc = ""] # [doc = " The preset is set just like with `Stream::new_easy_encoder`. The preset"] # [doc = " is ignored if filters below have been specified."] pub fn preset (& mut self , preset : u32) -> & mut Self { self . raw . preset = preset ; self } # [doc = " Configure a custom filter chain"] pub fn filters (& mut self , filters : Filters) -> & mut Self { self . raw . filters = filters . inner . as_ptr () ; self . filters = Some (filters) ; self } # [doc = " Configures the integrity check type"] pub fn check (& mut self , check : Check) -> & mut Self { self . raw . check = check as lzma_sys :: lzma_check ; self } # [doc = " Calculate approximate memory usage of multithreaded .xz encoder"] pub fn memusage (& self) -> u64 { unsafe { lzma_sys :: lzma_stream_encoder_mt_memusage (& self . raw) } } # [doc = " Initialize multithreaded .xz stream encoder."] pub fn encoder (& self) -> Result < Stream , Error > { unsafe { let mut init = Stream { raw : mem :: zeroed () } ; cvt (lzma_sys :: lzma_stream_encoder_mt (& mut init . raw , & self . raw)) ? ; Ok (init) } } }
};
}
