// Generated macro for JavaVM (struct)
macro_rules! Depcrate_vm_java_vmJavaVM {
() => {
// Module: crate::vm::java_vm
// Provides: {"JavaVM"}
// Dependencies: {}
# [doc = " The Java VM API, including (optional) [Invocation API][invocation-api] support."] # [doc = ""] # [doc = " An existing JavaVM can be obtained either via [`JavaVM::singleton`], or [`Env::get_java_vm`]"] # [doc = " in an already attached thread, or a new VM can be [launched](#launching-jvm-from-rust) via"] # [doc = " [`JavaVM::new`]."] # [doc = ""] # [doc = " ## Minimum supported JNI version is 1.4"] # [doc = ""] # [doc = " The implementation of this crate assumes your Java VM supports at least JNI >= 1.4"] # [doc = ""] # [doc = " The implementation wouldn't be able to call `ExceptionCheck` without JNI 1.2 and requiring >="] # [doc = " 1.4 means we don't need any runtime version checks for the direct byte buffer APIs."] # [doc = ""] # [doc = " Since `GetVersion` requires a [`Env`] and is not one of the JNI APIs that is safe to use with"] # [doc = " pending exceptions then the implementation is not always able to explicitly assert the supported"] # [doc = " version."] # [doc = ""] # [doc = " ## Attaching Native Threads"] # [doc = ""] # [doc = " Your application always needs to explicitly attach `jni-rs` to the current thread before it can"] # [doc = " access the [`Env`] API (most of the interesting APIs are under [`Env`])."] # [doc = ""] # [doc = " If you're implementing a native/foreign method then JNI will pass a thread attachment (in the"] # [doc = " form of a raw [`sys::JNIEnv`] pointer that should be captured using the"] # [doc = " [`env::EnvUnowned`] type) and converted into a [`Env`] reference via"] # [doc = " [`env::EnvUnowned::with_env`])"] # [doc = ""] # [doc = " Note: [`Env`] is not a `#[transparent]` wrapper over a [`sys::JNIEnv`] pointer."] # [doc = ""] # [doc = " The attachment of the current thread is always represented via an [`AttachGuard`] which blocks"] # [doc = " the thread from being detached and acts as a marker for a JNI stack frame."] # [doc = ""] # [doc = " [`Env`] is only ever exposed in the public API by-reference, and will always borrow from an"] # [doc = " [`AttachGuard`] such that it's lifetime is tied to a fixed JNI stack frame."] # [doc = ""] # [doc = " Unless you are using `unsafe` APIs though, the [`AttachGuard`] itself will usually be hidden,"] # [doc = " and you will get a [`Env`] reference (that borrows from a hidden [`AttachGuard`])."] # [doc = ""] # [doc = " This library supports these modes of attachment:"] # [doc = " * A permanent attachment with [`JavaVM::attach_current_thread`]. The thread will automatically"] # [doc = "   detach itself before it terminates (recommended for attaching in native threads)."] # [doc = " * A scoped attachment with [`JavaVM::attach_current_thread_for_scope`]. The thread will"] # [doc = "   automatically detach itself once your given closure returns (and the hidden [`AttachGuard`]"] # [doc = "   is dropped)."] # [doc = " * Implicit (\"unowned\") attachments, for use in native methods. The thread will never get"] # [doc = "   explicitly detached by `jni-rs` if we have implicit attachment."] # [doc = ""] # [doc = " ### Local Reference Management"] # [doc = ""] # [doc = " Remember that the native thread attached to the VM must manage local references"] # [doc = " carefully, i.e., do not allocate an excessive number of references and release them promptly when"] # [doc = " they are no longer needed to enable the GC to collect them."] # [doc = ""] # [doc = " A common approach is to push appropriately-sized local frames for larger"] # [doc = " code fragments (see [`Env::with_local_frame`] or [`JavaVM::with_local_frame`])"] # [doc = " and [`objects::Auto`] for temporary references in loops."] # [doc = ""] # [doc = " See also the [JNI specification][spec-references] for details on referencing Java objects."] # [doc = ""] # [doc = " ## Launching JVM from Rust"] # [doc = ""] # [doc = " To [launch][launch-vm] a JVM from a native process, enable the `invocation` feature in the"] # [doc = " Cargo.toml:"] # [doc = ""] # [doc = " ```toml"] # [doc = " jni = { version = \"0.21.1\", features = [\"invocation\"] }"] # [doc = " ```"] # [doc = ""] # [doc = " The application will be able to use [`JavaVM::new`] which will dynamically load a `jvm` library"] # [doc = " (which is distributed with the JVM) at runtime:"] # [doc = ""] # [doc = " ```rust,ignore-aarch64-linux-android"] # [doc = " # use jni::errors;"] # [doc = " # //"] # [doc = " # // Ignore this test without invocation feature, so that simple `cargo test` works"] # [doc = " # #[cfg(feature = \"invocation\")]"] # [doc = " # fn main() -> errors::StartJvmResult<()> {"] # [doc = " # use jni::{AttachGuard, objects::JValue, InitArgsBuilder, Env, JNIVersion, JavaVM, sys::jint};"] # [doc = " # //"] # [doc = " // Build the VM properties"] # [doc = " let jvm_args = InitArgsBuilder::new()"] # [doc = "           // Pass the JNI API version (default is 8)"] # [doc = "           .version(JNIVersion::V1_8)"] # [doc = "           // You can additionally pass any JVM options (standard, like a system property,"] # [doc = "           // or VM-specific)."] # [doc = "           // Here we enable some extra JNI checks useful during development"] # [doc = "           .option(\"-Xcheck:jni\")"] # [doc = "           .build()"] # [doc = "           .unwrap();"] # [doc = ""] # [doc = " // Create a new VM"] # [doc = " let jvm = JavaVM::new(jvm_args)?;"] # [doc = ""] # [doc = " // Attach the current thread to call to the JavaVM"] # [doc = " jvm.attach_current_thread(|env| -> errors::Result<()> {"] # [doc = "     // Call Java Math#abs(-10)"] # [doc = "     let x = JValue::from(-10);"] # [doc = "     let val: jint = env.call_static_method(c\"java/lang/Math\", c\"abs\", c\"(I)I\", &[x])?"] # [doc = "         .i()?;"] # [doc = ""] # [doc = "     assert_eq!(val, 10);"] # [doc = "     Ok(())"] # [doc = " });"] # [doc = ""] # [doc = " # Ok(()) }"] # [doc = " #"] # [doc = " # // This is a stub that gets run instead if the invocation feature is not built"] # [doc = " # #[cfg(not(feature = \"invocation\"))]"] # [doc = " # fn main() {}"] # [doc = " ```"] # [doc = ""] # [doc = " At runtime, the JVM installation path is determined via the [java-locator] crate:"] # [doc = " 1. By the `JAVA_HOME` environment variable, if it is set."] # [doc = " 2. Otherwise — from `java` output."] # [doc = ""] # [doc = " It is recommended to set `JAVA_HOME`"] # [doc = ""] # [doc = " For the operating system to correctly load the `jvm` library it may also be necessary to update"] # [doc = " the path that the OS uses to find dependencies of the `jvm` library."] # [doc = " * On **Windows**, append the path to `$JAVA_HOME/bin` to the `PATH` environment variable."] # [doc = " * On **MacOS**, append the path to `libjvm.dylib` to `LD_LIBRARY_PATH` environment variable."] # [doc = " * On **Linux**, append the path to `libjvm.so` to `LD_LIBRARY_PATH` environment variable."] # [doc = ""] # [doc = " The exact relative path to `jvm` library is version-specific."] # [doc = ""] # [doc = " [invocation-api]: https://docs.oracle.com/en/java/javase/12/docs/specs/jni/invocation.html"] # [doc = " [get-vm]: struct.Env.html#method.get_java_vm"] # [doc = " [launch-vm]: struct.JavaVM.html#method.new"] # [doc = " [act]: struct.JavaVM.html#method.attach_current_thread"] # [doc = " [actp]: struct.JavaVM.html#method.attach_current_thread_permanently"] # [doc = " [spec-references]:"] # [doc = "     https://docs.oracle.com/en/java/javase/12/docs/specs/jni/design.html#referencing-java-objects"] # [doc = " [java-locator]: https://crates.io/crates/java-locator"] # [derive (Debug , Clone)] pub struct JavaVM (* mut sys :: JavaVM) ;
};
}
