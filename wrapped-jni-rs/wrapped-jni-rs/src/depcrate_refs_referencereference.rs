// Generated macro for Reference (trait)
macro_rules! Depcrate_refs_referenceReference {
() => {
// Module: crate::refs::reference
// Provides: {"Reference"}
// Dependencies: {}
# [doc = " A trait for types that represents a JNI reference (could be local, global or"] # [doc = " weak global as well as wrapper types like [`Auto`] and [`Global`])"] # [doc = ""] # [doc = ""] # [doc = " This makes it possible for APIs like [`Env::new_global_ref`] to be given a"] # [doc = " non-static local reference type like [`JString<'local>`] (or an [`Auto`]"] # [doc = " wrapper) and return a [`Global`] that is instead parameterized by"] # [doc = " [`JString<'static>`]."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The associated `Kind` and `GlobalKind` types must be transparent wrappers"] # [doc = " around the underlying JNI object reference types (such as `JObject` or"] # [doc = " `jobject`) and must not have any `Drop` side effects."] pub unsafe trait Reference : Sized { # [doc = " The generic associated [`Self::Kind`] type corresponds to the underlying"] # [doc = " class type (such as [`JObject`] or [`JString`]), parameterized by the"] # [doc = " lifetime that indicates whether the type holds a global reference"] # [doc = " (`'static`) or a local reference that's tied to a JNI stack frame."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This must be a transparent `JObject` or `jobject` wrapper type that"] # [doc = " has no `Drop` side effects."] type Kind < 'local > : Reference + Default + Into < JObject < 'local > > + AsRef < JObject < 'local > > + 'local ; # [doc = " The associated `GlobalKind` type should be equivalent to"] # [doc = " `Kind<'static>`, with the additional bound that ensures the type is"] # [doc = " `Send + Sync`"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " This must be a transparent `JObject` or `jobject` wrapper type that"] # [doc = " has no `Drop` side effects."] type GlobalKind : Reference + Default + Into < JObject < 'static > > + AsRef < JObject < 'static > > + Send + Sync + 'static ; # [doc = " Returns the underlying, raw [`crate::sys::jobject`] reference."] fn as_raw (& self) -> jobject ; # [doc = " Returns `true` if this is a `null` object reference"] fn is_null (& self) -> bool { self . as_raw () . is_null () } # [doc = " Returns `null` reference based on [`Self::Kind`]"] fn null < 'any > () -> Self :: Kind < 'any > { Self :: Kind :: default () } # [doc = " The fully qualified class name of the Java class represented by this"] # [doc = " reference."] # [doc = ""] # [doc = " The class name is expected to be dot-separated, in the same format as"] # [doc = " `Class.getName()` and suitable for passing to `Class.forName()`"] # [doc = ""] # [doc = " For example: `\"com.example.MyClass\"`"] # [doc = ""] # [doc = " Note: this format is very similar to the FindClass naming conventions,"] # [doc = " except for the use of dots instead of slashes."] # [doc = ""] # [doc = " An array of objects would look like: \"[Ljava.lang.Object;\" An array of"] # [doc = " integers would look like: \"[I\""] # [doc = ""] # [doc = " This returns a `Cow` so that in the common case a `&'static JNIStr`"] # [doc = " literal can be returned but for Array types they may compose the name"] # [doc = " dynamically."] # [doc = ""] # [doc = " There's no guarantee that the name is interned / cached, so it's not"] # [doc = " recommended to call this in any fast path, it's mainly intended for use"] # [doc = " when first loading a class, or for debugging."] fn class_name () -> Cow < 'static , JNIStr > ; # [doc = " Borrows a global reference to the [JClass] associated with this"] # [doc = " reference."] # [doc = ""] # [doc = " Whenever you need the class associated with some reference wrapper type"] # [doc = " (e.g. [`JObject`], [`JClass`], [`JString`] etc), prefer using this"] # [doc = " method instead of [Env::find_class], [Env::load_class] or"] # [doc = " [LoaderContext::load_class]."] # [doc = ""] # [doc = " This [Reference] trait is implemented for all reference wrapper types"] # [doc = " like [`JObject`], [`JString`], [`JClass`] etc."] # [doc = ""] # [doc = " All implementations will maintain a static cache holding a"] # [doc = " `Global<JClass>` that is cheap to lookup and doesn't require a JNI call"] # [doc = " or creating any new references."] # [doc = ""] # [doc = " For example, lookup the class for `java.lang.String` / [JString] like"] # [doc = " this:"] # [doc = " ```rust,no_run"] # [doc = " # use jni::{errors::Result, Env, objects::JClass};"] # [doc = " #"] # [doc = " # fn example<'local>(env: &mut Env<'local>) -> Result<()> {"] # [doc = " use jni::objects::{JString, Reference as _, LoaderContext};"] # [doc = " let string_class = JString::lookup_class(env, LoaderContext::None)?;"] # [doc = " let string_class_ref: &JClass = string_class.as_ref();"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " # Implementation notes"] # [doc = ""] # [doc = " This is used as part of casting checks to do a cached lookup of"] # [doc = " associated class references - avoiding the cost of repeated class"] # [doc = " lookups."] # [doc = ""] # [doc = " The implementation is expected to use"] # [doc = " [`once_cell::sync::OnceCell::get_or_try_init`] (or similar) to lookup"] # [doc = " cached API state, including a `Global<JClass>`."] # [doc = ""] # [doc = " In case no class reference is already cached then use"] # [doc = " [`LoaderContext::load_class`] (not [Env::find_class]) to lookup a class"] # [doc = " reference."] # [doc = ""] fn lookup_class < 'caller > (env : & Env < '_ > , loader_context : LoaderContext ,) -> crate :: errors :: Result < impl Deref < Target = Global < JClass < 'static > > > + 'caller > ; # [doc = " Returns a new reference type based on [`Self::Kind`] for the given `reference` that is tied"] # [doc = " to the specified lifetime."] # [doc = ""] # [doc = " This method in generally only expected to be used internally or when implementing your own"] # [doc = " reference wrapper types."] # [doc = ""] # [doc = " You should always prefer to use wrapper-provided `::from_raw()` methods (Such as"] # [doc = " `JString::from_raw()`) for wrapping raw local references because those will guarantee that"] # [doc = " the returned reference has a lifetime that's tied to a valid local reference frame."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " There must not be no other owning wrapper for the given `reference` (unless it is `null`)"] # [doc = " (as an exception it can be OK to create a temporary, hidden wrapper while borrowing an"] # [doc = " original, owning wrapper - e.g. as part of a type cast)"] # [doc = ""] # [doc = " Local references must have a lifetime that's associated with an AttachGuard or a Env that"] # [doc = " limits them to a single JNI stack frame."] # [doc = ""] # [doc = " This can also be used to create a borrowed view of a global reference (e.g. as part of a"] # [doc = " type cast), which may be associated with a `'static` lifetime only so long as the lifetime"] # [doc = " of the view is limited by borrowing from the original global wrapper."] # [doc = ""] # [doc = " You are responsible to knowing that `Self::Kind` is a suitable wrapper type for the given"] # [doc = " `reference`. E.g. because the `reference` came from an `into_raw` call from the same type."] unsafe fn kind_from_raw < 'env > (reference : jobject) -> Self :: Kind < 'env > ; # [doc = " Returns a (`'static`) reference type based on [`Self::GlobalKind`] for the given `global_ref`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " There must not be no other wrapper for the given `global_ref` reference (unless it is"] # [doc = " `null`)"] # [doc = ""] # [doc = " You are responsible to knowing that `Self::GlobalKind` is a suitable wrapper type for the"] # [doc = " given `global_ref` reference. E.g. because the `global_ref` came from an `into_raw`"] # [doc = " call from the same type."] # [doc = ""] unsafe fn global_kind_from_raw (global_ref : jobject) -> Self :: GlobalKind ; }
};
}
