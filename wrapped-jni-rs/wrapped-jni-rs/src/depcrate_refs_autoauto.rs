// Generated macro for Auto (struct)
macro_rules! Depcrate_refs_autoAuto {
() => {
// Module: crate::refs::auto
// Provides: {"Auto"}
// Dependencies: {}
# [doc = " A wrapper to `Auto` delete local references early (before the JNI stack"] # [doc = " frame unwinds)."] # [doc = ""] # [doc = " ---"] # [doc = ""] # [doc = " _**Note** that it's often not necessary, or even recommended, to create an"] # [doc = " `Auto` wrapper for local JNI references if you can instead rely on the"] # [doc = " references being deleted when the current JNI stack frame unwinds._"] # [doc = ""] # [doc = " ---"] # [doc = ""] # [doc = " ## Overview"] # [doc = ""] # [doc = " Anything passed to a foreign method or returned from JNI methods that refers"] # [doc = " to a `JObject` is considered a local reference."] # [doc = ""] # [doc = " JNI local references belong to a JNI stack frame (the top frame at the time"] # [doc = " they are created)."] # [doc = ""] # [doc = " These JNI references are normally automatically deleted once the their JNI"] # [doc = " stack frame unwinds, such as when a foreign method implementation returns"] # [doc = " back to the JavaVM."] # [doc = ""] # [doc = " In some situations you don't want to wait until the current JNI stack frame"] # [doc = " unwinds before you delete a local reference and you can wrap then with"] # [doc = " `Auto` so they are deleted earlier, when they are dropped."] # [doc = ""] # [doc = " For example, you might be creating a very large number of temporary"] # [doc = " references in a loop that can lead to running out of memory if they aren't"] # [doc = " explicitly deleted before the JNI stack frame unwinds."] # [doc = ""] # [doc = " This wrapper type provides automatic local reference deletion when it goes"] # [doc = " out of scope and gets dropped."] # [doc = ""] # [doc = " See also the [JNI specification][spec-references] for details on referencing"] # [doc = " Java objects and some [extra information][android-jni-references]."] # [doc = ""] # [doc = " ## `IntoAuto` trait (`.auto()`)"] # [doc = ""] # [doc = " As a convenience, the `IntoAuto` trait is implemented for all reference"] # [doc = " types (like `JObject`, `JClass`, `JString` etc). This allows you to easily"] # [doc = " create an `Auto` wrapper by calling the `.auto()` method on any local"] # [doc = " reference."] # [doc = ""] # [doc = " For example:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # use jni::{objects::Auto, Env};"] # [doc = " # fn example(env: &mut Env) -> std::result::Result<(), jni::errors::Error> {"] # [doc = " use jni::objects::IntoAuto as _;"] # [doc = " for i in 0..1000 {"] # [doc = "     // Ensure we aren't left with a new local for each iteration by"] # [doc = "     // wrapping the reference in an `Auto` wrapper."] # [doc = "     let auto_delete_string = env.new_string(c\"Hello, world!\")?.auto();"] # [doc = " }"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " ## Alternatives"] # [doc = ""] # [doc = " It is usually more efficient to rely on stack frame unwinding to release"] # [doc = " local references in bulk instead of creating [`Auto`] wrappers that are then"] # [doc = " deleted one-by-one."] # [doc = ""] # [doc = " If you aren't sure whether it's OK to create new local references in the"] # [doc = " current JNI frame (perhaps because you don't know when it will unwind) you"] # [doc = " can also consider using APIs like [`crate::Env::with_local_frame()`] which"] # [doc = " can run your code in a temporary stack frame that will release all local"] # [doc = " references in bulk, without needing to use [`Auto`]."] # [doc = ""] # [doc = " You can also explicitly delete a local reference with"] # [doc = " [crate::Env::delete_local_ref]."] # [doc = ""] # [doc = " [spec-references]:"] # [doc = "     https://docs.oracle.com/en/java/javase/12/docs/specs/jni/design.html#referencing-java-objects"] # [doc = " [android-jni-references]:"] # [doc = "     https://developer.android.com/training/articles/perf-jni#local-and-global-references"] # [derive (Debug)] pub struct Auto < 'local , T > where T : Into < JObject < 'local > > , { obj : ManuallyDrop < T > , _lifetime : PhantomData < & 'local T > , }
};
}
