// Generated macro for AttachGuard (struct)
macro_rules! Depcrate_vm_java_vmAttachGuard {
() => {
// Module: crate::vm::java_vm
// Provides: {"AttachGuard"}
// Dependencies: {}
# [doc = " Represents a JNI attachment of the current thread to a Java VM, which is"] # [doc = " required before you can access the [`Env`] API."] # [doc = ""] # [doc = " [`AttachGuard`] is an `unsafe`, low-level building block for the safe thread"] # [doc = " attachment APIs provided by [`JavaVM`]. Before considering this API you"] # [doc = " should look at:"] # [doc = ""] # [doc = "   - [`JavaVM::attach_current_thread()`]"] # [doc = "   - [`JavaVM::attach_current_thread_for_scope()`]"] # [doc = "   - [`JavaVM::attach_current_thread_with_config()`]"] # [doc = "   - [`JavaVM::with_local_frame()`]"] # [doc = "   - [`JavaVM::with_top_local_frame()`]"] # [doc = ""] # [doc = " More-specifically, an [`AttachGuard`] only represents an attachment for a"] # [doc = " single JNI stack frame or local reference frame."] # [doc = ""] # [doc = " The lifetime for an [`AttachGuard`] effectively names the scope of a single"] # [doc = " JNI stack frame. The lifetime of the guard is used to ensure that any local"] # [doc = " references created via the [`Env`] API are only accessible within the"] # [doc = " lifetime of the guard, which ensures that local references can't be used"] # [doc = " outside of the JNI stack frame that owns them."] # [doc = ""] # [doc = " If the [`AttachGuard`] \"owns\" the underlying JNI thread attachment, that"] # [doc = " means the guard will automatically detach the current thread from the Java"] # [doc = " VM when the guard is dropped."] # [doc = ""] # [doc = " If you're implementing a JNI native method which is passed a raw"] # [doc = " [`crate::sys::JNIEnv`] pointer, then you can get a corresponding guard via"] # [doc = " [`AttachGuard::from_unowned`]."] # [doc = ""] # [doc = " If you're implementing some JNI utility code that doesn't already have a raw"] # [doc = " [`crate::sys::JNIEnv`] pointer you could use"] # [doc = " [`JavaVM::attach_current_thread_guard`] to get an attachment guard."] # [doc = ""] # [doc = " # JavaVM::singleton() guarantee"] # [doc = ""] # [doc = " If you know that at least one [`AttachGuard`] has ever existed (which is"] # [doc = " implied if you have a [`Env`] reference) you can assume that"] # [doc = " [`JavaVM::singleton()`] will return `Some(JavaVM)`."] # [doc = ""] # [doc = " This can be useful if you need to access JNI from something like a Drop"] # [doc = " implementation, without needing to explicitly copy `JavaVM` pointers. In"] # [doc = " these cases you can often prove that at least one `AttachGuard` must have"] # [doc = " existed in order to construct the object that will later be dropped."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " [`AttachGuard`] management is considered `unsafe` because there are some"] # [doc = " safety rules that can't be automatically guaranteed through the Rust type"] # [doc = " system alone..."] # [doc = ""] # [doc = " 1. You must treat a guard as an immovable type that needs to live on the"] # [doc = "    stack and can't be re-ordered relative to other guards on the stack."] # [doc = ""] # [doc = " 2. Following from (1), an [`AttachGuard`] _MUST NOT_ be given a `'static`"] # [doc = "    lifetime (e.g. by boxing or moving into a `static` variable)."] # [doc = ""] # [doc = " 3. Any low-level JNI code that creates new local references must"] # [doc = "    runtime-assert that it has exclusive access to the top-most"] # [doc = "    [`AttachGuard`] on the stack."] # [doc = ""] # [doc = "    If you're only using the safe [`Env`] API then this is handled for you,"] # [doc = "    but if you're implementing low-level JNI code that somehow creates local"] # [doc = "    references without involving a checked `Env` API you must add fail-safe"] # [doc = "    runtime assertions. (See [`Env::assert_top`])"] # [doc = ""] # [doc = " # Safety Guidance"] # [doc = ""] # [doc = " If you do need to use an [`AttachGuard`] directly, then it's recommended"] # [doc = " that the guard should be kept private to a function that uses so it isn't"] # [doc = " ever exposed to safe code."] # [doc = ""] # [doc = " It's very important that [`AttachGuard`]s are never given a `'static`"] # [doc = " lifetime or re-ordered relative to other guards on the stack and keeping the"] # [doc = " guard private to a function is a good way to ensure that it remains fixed on"] # [doc = " the stack."] # [doc = ""] # [doc = " Beware of making it too easy to repeatedly materialize access to a mutable"] # [doc = " [`Env`] and increasing the risk that safe code could attempt to create new"] # [doc = " local references with an [`Env`] reference that is not at the top of the"] # [doc = " stack. If this happens the API will panic at runtime to avoid undefined"] # [doc = " behavior."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = "    The `Drop` implementation will `panic` if a guard is not dropped in the"] # [doc = "    same order that it was created, relative to other guards (LIFO order)."] # [derive (Debug)] pub struct AttachGuard < 'local > { env : Env < 'local > , }
};
}
