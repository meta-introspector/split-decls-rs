// Generated macro for impl_628 (impl)
macro_rules! Depcrate_strings_mutf8_charsimpl_628 {
() => {
// Module: crate::strings::mutf8_chars
// Provides: {"impl_628"}
// Dependencies: {}
impl < 'local , StringRef > MUTF8Chars < 'local , StringRef > where StringRef : AsRef < JString < 'local > > + Reference , { # [doc = " Constructs a [`MUTF8Chars`] from a `Env` and a `JString`."] # [doc = ""] # [doc = " The string will be `NULL` terminated and encoded as [Modified"] # [doc = " UTF-8](https://en.wikipedia.org/wiki/UTF-8#Modified_UTF-8) /"] # [doc = " [CESU-8](https://en.wikipedia.org/wiki/CESU-8)."] # [doc = ""] # [doc = " The implementation may either create a copy of the character array for"] # [doc = " the given `String` or it may pin it to avoid it being collected by the"] # [doc = " garbage collector."] # [doc = ""] # [doc = " Returns a [`MUTF8Chars`] that will automatically release the underlying"] # [doc = " character array when dropped (see [Self::release_string_utf_chars])."] pub (crate) fn from_get_string_utf_chars (env : & Env < '_ > , obj : StringRef) -> Result < Self > { let obj = null_check ! (obj , "get_string_utf_chars obj argument") ? ; unsafe { let mut is_copy : jboolean = false ; let ptr : * const c_char = jni_call_only_check_null_ret ! (env , v1_1 , GetStringUTFChars , obj . as_raw () , & mut is_copy as * mut _) ? ; let is_copy = is_copy == JNI_TRUE ; Ok (Self { obj , chars : ptr , is_copy , _lifetime : PhantomData , }) } } # [doc = " Destroys the [`MUTF8Chars`] without freeing the underlying contents, and"] # [doc = " returns a raw pointer to it."] # [doc = ""] # [doc = " The returned pointer is the same as the one returned by the"] # [doc = " [`as_ptr`][JNIStr::as_ptr] method. It points to a null-terminated"] # [doc = " string in [modified UTF-8] encoding (which is similar, but not"] # [doc = " identical, to [CESU-8]). It is valid when returned by this method, and"] # [doc = " will remain valid until freed (see below)."] # [doc = ""] # [doc = " [CESU-8]: https://en.wikipedia.org/wiki/CESU-8"] # [doc = " [modified UTF-8]: https://en.wikipedia.org/wiki/UTF-8#Modified_UTF-8"] # [doc = ""] # [doc = " This also returns the status for whether the original string was copied or not."] # [doc = ""] # [doc = " # Warning"] # [doc = ""] # [doc = " After calling this method, the underlying string must be manually"] # [doc = " freed. This can be done either by reconstructing the [`MUTF8Chars`] using"] # [doc = " [`MUTF8Chars::from_raw`] and then dropping it, or by passing the pointer"] # [doc = " to the JNI function `ReleaseStringUTFChars`."] pub fn into_raw (self) -> (* const c_char , bool) { let _dont_call_drop = std :: mem :: ManuallyDrop :: new (self) ; (_dont_call_drop . chars , _dont_call_drop . is_copy) } # [doc = " Constructs a [`MUTF8Chars`] from raw components."] # [doc = ""] # [doc = " The required components are, a [`Env`], a reference to a"] # [doc = " `java.lang.String` object, and a pointer to the characters of that"] # [doc = " `java.lang.String`."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " `ptr` must be a valid, non-null pointer, previously returned by"] # [doc = " [`MUTF8Chars::into_raw`] or the JNI function `GetStringUTFChars`. `ptr`"] # [doc = " must not belong to another [`MUTF8Chars`] at the same time."] # [doc = ""] # [doc = " `obj` must be a non-null reference to the same `java.lang.String` object"] # [doc = " that `ptr` was obtained from."] # [doc = ""] # [doc = " `is_copy` must be a boolean indicating whether the string was copied or"] # [doc = " not (as returned by `GetStringUTFChars`)."] # [doc = ""] # [doc = " # Example"] # [doc = " ```rust,no_run"] # [doc = " # use jni::{errors::Result, Env, strings::MUTF8Chars};"] # [doc = " #"] # [doc = " # fn example(env: &mut Env) -> Result<()> {"] # [doc = " let jstring = env.new_string(c\"foo\")?;"] # [doc = " let chars = jstring.mutf8_chars(env)?;"] # [doc = ""] # [doc = " let (ptr, is_copy) = chars.into_raw();"] # [doc = " // Do whatever you need with the pointer"] # [doc = " let chars = unsafe { MUTF8Chars::from_raw(env, &jstring, ptr, is_copy) };"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] pub unsafe fn from_raw (_env : & Env < '_ > , obj : StringRef , ptr : * const c_char , is_copy : bool ,) -> Self { Self { obj , chars : ptr , is_copy , _lifetime : PhantomData , } } # [doc = " Returns whether the string was copied or not."] pub fn is_copy (& self) -> bool { self . is_copy } }
};
}
