// Generated macro for impl_189 (impl)
macro_rules! Depcrate_refs_castimpl_189 {
() => {
// Module: crate::refs::cast
// Provides: {"impl_189"}
// Dependencies: {}
impl < 'any , 'from , To : Reference > Cast < 'any , 'from , To > { # [doc = " Creates a new cast from one object type to another."] # [doc = ""] # [doc = " This can be used to cast global or local references."] # [doc = ""] # [doc = " Returns [Error::WrongObjectType] if the object is not of the expected type."] pub (crate) fn new < 'env_local , From : Reference + AsRef < JObject < 'any > > > (env : & Env < 'env_local > , from : & 'from From ,) -> Result < Self > where 'any : 'from , { let from : & JObject = from . as_ref () ; if from . is_null () { return Ok (Self { _from : PhantomData , to : To :: null () , }) ; } if env . is_instance_of_cast_type :: < To > (from) ? { unsafe { Ok (Self { _from : PhantomData , to : To :: kind_from_raw :: < 'any > (from . as_raw ()) , }) } } else { Err (Error :: WrongObjectType) } } # [doc = " Creates a [`Cast`] from a raw JNI reference pointer"] # [doc = ""] # [doc = " Returns [Error::WrongObjectType] if the object is not of the expected type."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure that `from` is a valid reference (local or global) - which may be"] # [doc = " `null`."] # [doc = ""] # [doc = " The caller must ensure the `from` reference will not be deleted while the `Cast` exists."] # [doc = ""] # [doc = " Note: even though this API is `unsafe`, it will still do a runtime check that `from` is a"] # [doc = " valid instance of `To`, so you are not required to know this."] # [doc = ""] # [doc = " Note: this API is agnostic about whether the reference is local or global because the `Cast`"] # [doc = " wrapper doesn't give ownership over the reference and so you can't accidentally attempt to"] # [doc = " delete it using the wrong JNI API."] pub unsafe fn from_raw < 'env_local > (env : & Env < 'env_local > , from : & 'from jobject) -> Result < Self > where 'any : 'from , { let from = JObject :: from_raw (env , * from) ; let from = & from ; if from . is_null () { return Ok (Self { _from : PhantomData , to : To :: null () , }) ; } if env . is_instance_of_cast_type :: < To > (from) ? { unsafe { Ok (Self { _from : PhantomData , to : To :: kind_from_raw :: < 'any > (from . as_raw ()) , }) } } else { Err (Error :: WrongObjectType) } } # [doc = " Creates a new cast from one object type to another without a runtime check."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure that `from` is an instance of `To`, or null."] pub unsafe fn new_unchecked < From : Reference + AsRef < JObject < 'any > > > (from : & 'from From) -> Self where 'any : 'from , { unsafe { Self { _from : PhantomData , to : To :: kind_from_raw :: < 'any > (from . as_raw ()) , } } } # [doc = " Creates a [`Cast`] from a raw JNI object reference without a runtime check."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The caller must ensure that `from` is a valid JNI reference to an instance of `To` (or"] # [doc = " null)."] pub unsafe fn from_raw_unchecked (from : & 'from jobject) -> Self where 'any : 'from , { unsafe { Self { _from : PhantomData , to : To :: kind_from_raw :: < 'any > (* from) , } } } }
};
}
