// Generated macro for impl_655 (impl)
macro_rules! Depcrate_envimpl_655 {
() => {
// Module: crate::env
// Provides: {"impl_655"}
// Dependencies: {}
impl < 'local > EnvUnowned < 'local > { # [doc = " Runs a closure with a [`Env`] based on an unowned JNI thread attachment"] # [doc = " associated with an external JNI stack frame."] # [doc = ""] # [doc = " This API is specifically intended to be used within native/foreign Java"] # [doc = " method implementations in cases where you have named the lifetime for"] # [doc = " the caller's JNI stack frame."] # [doc = ""] # [doc = " It returns an [`EnvOutcome`] that supports mapping errors with access to"] # [doc = " an [`Env`], so you may choose to throw errors as exceptions."] # [doc = ""] # [doc = " To avoid the risk of unwinding into the JVM (which will abort the"] # [doc = " process) this API wraps the closure in a [`catch_unwind`] to catch any"] # [doc = " panics. Any panic can be handled via the [`ErrorPolicy`] given to"] # [doc = " [`EnvOutcome::resolve`] or handled directly via [`EnvOutcome::into_outcome`]."] # [doc = ""] # [doc = " Note: This API does not create a new JNI stack frame, since the JVM will"] # [doc = " clean up the JNI stack frame when the native method returns."] # [doc = ""] # [doc = " Note: This API returns an [`EnvOutcome`]"] pub fn with_env < F , T , E > (& mut self , f : F) -> EnvOutcome < 'local , T , E > where F : FnOnce (& mut Env < 'local >) -> std :: result :: Result < T , E > , E : From < Error > , { let mut guard : AttachGuard < 'local > = unsafe { AttachGuard :: from_unowned (self . ptr) } ; let env = guard . borrow_env_mut () ; let result = catch_unwind (AssertUnwindSafe (| | f (env))) ; let outcome = match result { Ok (ret) => match ret { Ok (t) => Outcome :: Ok (t) , Err (e) => Outcome :: Err (e) , } , Err (payload) => Outcome :: Panic (payload) , } ; EnvOutcome :: new (self . ptr , outcome) } # [doc = " Runs a closure with a [`Env`] based on an unowned JNI thread attachment"] # [doc = " associated with an external JNI stack frame."] # [doc = ""] # [doc = " This API is specifically intended to be used within native/foreign Java"] # [doc = " method implementations in cases where you have named the lifetime for"] # [doc = " the caller's JNI stack frame."] # [doc = ""] # [doc = " Since it would lead to undefined behaviour to allow Rust code to unwind"] # [doc = " across a native method call boundary, you probably want to use"] # [doc = " [`EnvUnowned::with_env`] instead, which will wrap the closure"] # [doc = " in a `catch_unwind` to catch any panics."] # [doc = ""] # [doc = " Note: This API does not create a new JNI stack frame, which is normally"] # [doc = " what you want when implementing a native method, since the JVM will"] # [doc = " clean up the JNI stack frame when the native method returns."] pub fn with_env_no_catch < F , T , E > (& mut self , f : F) -> EnvOutcome < 'local , T , E > where F : FnOnce (& mut Env < 'local >) -> std :: result :: Result < T , E > , E : From < Error > , { let mut guard : AttachGuard < 'local > = unsafe { AttachGuard :: from_unowned (self . ptr) } ; let result = f (guard . borrow_env_mut ()) ; match result { Ok (t) => EnvOutcome :: new (self . ptr , Outcome :: Ok (t)) , Err (e) => EnvOutcome :: new (self . ptr , Outcome :: Err (e)) , } } # [doc = " Creates a new `EnvUnowned` from a raw [`crate::sys::JNIEnv`] pointer."] # [doc = ""] # [doc = " It should be very uncommon to use this method directly, but could be"] # [doc = " useful if you are given a raw [`crate::sys::JNIEnv`] pointer that you"] # [doc = " know represents a valid JNI attachment for the current thread."] # [doc = ""] # [doc = " If you are implementing a native method in Rust though, you should"] # [doc = " prefer to use the [`EnvUnowned`] type as the first argument to your"] # [doc = " native method and avoid the need to use a raw pointer."] # [doc = ""] # [doc = " If you have a raw [`crate::sys::JNIEnv`] pointer, this API should be"] # [doc = " marginally safer than using [`crate::AttachGuard`] manually"] # [doc = " since since the attach guard management will be hidden within the"] # [doc = " [`Self::with_env`] and [`Self::with_env_no_catch`] methods."] # [doc = ""] # [doc = " Beware that [`Self::with_env`] and [`Self::with_env_no_catch`] will not"] # [doc = " create a new JNI stack frame, so if you are not implementing a native"] # [doc = " method with a JNI stack frame that will be cleaned up on return, you may"] # [doc = " need to consider the risk of leaking local references into the current"] # [doc = " stack frame (JNI references are only cleaned up when the stack frame is"] # [doc = " popped)"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " The pointer must be a valid, non-null pointer to a `jni_sys::JNIEnv`"] # [doc = " that represents an attachment of the current thread to a Java VM."] # [doc = ""] # [doc = " The assigned lifetime must not outlive the JNI stack frame that owns the"] # [doc = " `Env` pointer. For example it would _never_ be safe to use `'static`."] pub unsafe fn from_raw (ptr : * mut jni_sys :: JNIEnv) -> Self { assert ! (! ptr . is_null () , "EnvUnowned pointer must not be null") ; Self { ptr , _lifetime : std :: marker :: PhantomData , } } # [doc = " Returns the raw pointer to the underlying [`crate::sys::JNIEnv`]."] pub fn as_raw (& self) -> * mut jni_sys :: JNIEnv { self . ptr } # [doc = " Consumes the [`EnvUnowned`] and returns the raw pointer to the underlying [`crate::sys::JNIEnv`]."] pub fn into_raw (self) -> * mut jni_sys :: JNIEnv { self . ptr } }
};
}
