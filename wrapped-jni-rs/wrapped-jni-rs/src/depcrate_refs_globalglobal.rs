// Generated macro for Global (struct)
macro_rules! Depcrate_refs_globalGlobal {
() => {
// Module: crate::refs::global
// Provides: {"Global"}
// Dependencies: {}
# [doc = " A global reference to a Java object."] # [doc = ""] # [doc = " Global references are slower to create and delete than ordinary local"] # [doc = " references, but have several properties that distinguish them:"] # [doc = ""] # [doc = " * Global references are not bound to the lifetime of a [`Env`]."] # [doc = ""] # [doc = " * Global references are not bound to any particular thread; they have the"] # [doc = "   [`Send`] and [`Sync`] traits."] # [doc = ""] # [doc = " * Until a global reference is dropped, it will prevent the referenced Java"] # [doc = "   object from being garbage collected."] # [doc = ""] # [doc = " * It takes more time to create or delete a global reference than to create"] # [doc = "   or delete a local reference."] # [doc = ""] # [doc = " These properties make global references useful in a few specific situations:"] # [doc = ""] # [doc = " * When you need to keep a reference to the same Java object across multiple"] # [doc = "   invocations of a native method, especially if you need a guarantee that"] # [doc = "   it's the exact same object every time, one way to do it is by storing a"] # [doc = "   global reference to it in a Rust `static` variable."] # [doc = ""] # [doc = " * When you need to send a Java object reference to a different thread, or"] # [doc = "   use a Java object reference from several different threads at the same"] # [doc = "   time, a global reference can be used to do so."] # [doc = ""] # [doc = " * When you need a Java object to not be garbage collected too soon, because"] # [doc = "   some side effect will happen (via `java.lang.Object::finalize`,"] # [doc = "   `java.lang.ref.Cleaner`, or the like) when it is garbage collected, a"] # [doc = "   global reference can be used to prevent it from being garbage collected."] # [doc = "   (This hold is released when the global reference is dropped.)"] # [doc = ""] # [doc = " See also [`Weak`], a global reference that does *not* prevent the underlying"] # [doc = " Java object from being garbage collected."] # [doc = ""] # [doc = ""] # [doc = " # Creating and Deleting"] # [doc = ""] # [doc = " To create a global reference, use the [`Env::new_global_ref`] method or wrap"] # [doc = " an existing global reference (that you own) with [`Env::global_from_raw`]."] # [doc = " To delete it, simply drop the [`Global`]."] # [doc = ""] # [doc = " If you have a raw `jobject` global reference that you do not own, you can"] # [doc = " use [`Env::as_cast_raw`] to create a temporary wrapper that will not delete"] # [doc = " the reference when dropped."] # [doc = ""] # [doc = " Note that, because global references take more time to create or delete than"] # [doc = " local references do, they should only be used when their benefits outweigh"] # [doc = " this drawback. Also note that this performance penalty does not apply to"] # [doc = " *using* a global reference (such as calling methods on the underlying Java"] # [doc = " object), only to creation and deletion of the reference."] # [doc = ""] # [doc = ""] # [doc = " # Warning: Drop On an Attached Thread If Possible"] # [doc = ""] # [doc = " When a `Global` is dropped, a JNI call is made to delete the global"] # [doc = " reference. If this frequently happens on a thread that is not already"] # [doc = " attached to the JVM, the thread will be temporarily attached using"] # [doc = " [`JavaVM::attach_current_thread_for_scope`], causing a severe performance"] # [doc = " penalty."] # [doc = ""] # [doc = " To avoid this performance penalty, ensure that `Global`s are only dropped on"] # [doc = " a thread that is already attached (or never dropped at all)."] # [doc = ""] # [doc = " In the event that a global reference is dropped on an unattached thread, a"] # [doc = " message is [logged][log] at [`log::Level::Warn`]."] # [repr (transparent)] # [derive (Debug)] pub struct Global < T > where T : Into < JObject < 'static > > + AsRef < JObject < 'static > > + Default + Reference + Send + Sync + 'static , { obj : T , }
};
}
