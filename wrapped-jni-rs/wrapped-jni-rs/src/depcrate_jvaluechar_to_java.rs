// Generated macro for char_to_java (function)
macro_rules! Depcrate_jvaluechar_to_java {
() => {
// Module: crate::jvalue
// Provides: {"char_to_java"}
// Dependencies: {}
# [doc = " Converts a Rust `char` to a Java `char`, if possible. (Error-prone; see warning.)"] # [doc = ""] # [doc = " **Warning:** Converting a Rust `char` to a single Java `char` is not recommended. This can only"] # [doc = " succeed for code points up to U+FFFF. Code points above that are represented in Java as *two*"] # [doc = " `char`s, each representing one half of a UTF-16 [surrogate pair], which this function cannot"] # [doc = " handle. If possible, use one of these alternatives instead:"] # [doc = ""] # [doc = " * Use Java `int`s containing UTF-32. You can convert a Rust `char` to a Java `int` containing"] # [doc = "   UTF-32 using [`char_to_java_int`], which never fails."] # [doc = ""] # [doc = " * Convert a Rust [`str`] to a Java `String` using [`Env::new_string`]."] # [doc = ""] # [doc = " * Convert a Rust `char` to multiple Java `char`s at a time using [`char::encode_utf16`] (which"] # [doc = "   this function is a wrapper around). That will properly generate surrogate pairs as needed."] # [doc = ""] # [doc = " # See Also"] # [doc = ""] # [doc = " * [`JValue`] and [`JValueOwned`]'s implementation of `TryFrom<char>`, a wrapper for this function that produces"] # [doc = "   [`JValue::Char`] and [`JValueOwned::Char`]"] # [doc = " * [`char_from_java`], the opposite of this function"] # [doc = " * [`char_to_java_int`], a UTF-32 alternative to this function that never fails"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This function returns an error if the provided `char` cannot be represented in UTF-16 without a"] # [doc = " surrogate pair, and therefore cannot be converted to a single Java `char`."] # [doc = ""] # [doc = " [surrogate pair]: https://en.wikipedia.org/wiki/Surrogate_pair"] pub fn char_to_java (char : char) -> std :: result :: Result < jchar , CharToJavaError > { if char . len_utf16 () != 1 { return Err (CharToJavaError { char }) ; } let mut buf = [0u16 ; 1] ; let buf : & mut [u16] = char . encode_utf16 (& mut buf) ; Ok (buf [0]) }
};
}
