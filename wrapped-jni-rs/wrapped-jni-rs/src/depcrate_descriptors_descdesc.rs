// Generated macro for Desc (trait)
macro_rules! Depcrate_descriptors_descDesc {
() => {
// Module: crate::descriptors::desc
// Provides: {"Desc"}
// Dependencies: {}
# [doc = " Trait for things that can be looked up through the JNI via a descriptor."] # [doc = " This will be something like the fully-qualified class name"] # [doc = " `java/lang/String` or a tuple containing a class descriptor, method name,"] # [doc = " and method signature. For convenience, this is also implemented for the"] # [doc = " concrete types themselves in addition to their descriptors."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " Implementations of this trait must return the correct value from the"] # [doc = " `lookup` method. It must not, for example, return a random [`JMethodID`] or"] # [doc = " the [`JClass`] of a class other than the one requested. Returning such an"] # [doc = " incorrect value results in undefined behavior. This requirement also"] # [doc = " applies to the returned value's implementation of `AsRef<T>`."] pub unsafe trait Desc < 'local , T > { # [doc = " The type that this `Desc` returns."] type Output : AsRef < T > ; # [doc = " Look up the concrete type from the JVM."] # [doc = ""] # [doc = " Note that this method does not return exactly `T`. Instead, it returns"] # [doc = " some type that implements `AsRef<T>`. For this reason, it is often"] # [doc = " necessary to use turbofish syntax when calling this method:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # use jni::{descriptors::Desc, errors::Result, Env, objects::JClass};"] # [doc = " #"] # [doc = " # fn example(env: &mut Env) -> Result<()> {"] # [doc = " // The value returned by `lookup` is not exactly `JClass`."] # [doc = " let class/*: impl AsRef<JClass> */ ="] # [doc = "     Desc::<JClass>::lookup(c\"java/lang/Object\", env)?;"] # [doc = ""] # [doc = " // But `&JClass` can be borrowed from it."] # [doc = " let class: &JClass = class.as_ref();"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " **Warning:** Many built-in implementations of this trait return"] # [doc = " [`Auto`] from this method. If you then call [`JObject::as_raw`] on"] # [doc = " the returned object reference, this may result in the reference being"] # [doc = " [deleted][Env::delete_local_ref] before it is used, causing"] # [doc = " undefined behavior."] # [doc = ""] # [doc = " For example, don't do this:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # use jni::{descriptors::Desc, errors::Result, Env, objects::JClass};"] # [doc = " #"] # [doc = " # fn some_function<T>(ptr: *mut T) {}"] # [doc = " #"] # [doc = " # fn example(env: &mut Env) -> Result<()> {"] # [doc = " // Undefined behavior: the `JClass` is dropped before the raw pointer"] # [doc = " // is passed to `some_function`!"] # [doc = " some_function(Desc::<JClass>::lookup(c\"java/lang/Object\", env)?.as_raw());"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " Instead, do this:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # use jni::{descriptors::Desc, errors::Result, Env, objects::JClass};"] # [doc = " #"] # [doc = " # fn some_function<T>(ptr: *mut T) {}"] # [doc = " #"] # [doc = " # fn example(env: &mut Env) -> Result<()> {"] # [doc = " let class = Desc::<JClass>::lookup(c\"java/lang/Object\", env)?;"] # [doc = ""] # [doc = " some_function(class.as_raw());"] # [doc = ""] # [doc = " drop(class);"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " This will still work without the call to `drop` at the end, but calling"] # [doc = " `drop` ensures that the reference is not accidentally dropped earlier"] # [doc = " than it should be."] fn lookup (self , _ : & mut Env < 'local >) -> Result < Self :: Output > ; }
};
}
