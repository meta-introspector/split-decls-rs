// Generated macro for impl_526 (impl)
macro_rules! Depcrate_objects_jprimitive_arrayimpl_526 {
() => {
// Module: crate::objects::jprimitive_array
// Provides: {"impl_526"}
// Dependencies: {}
impl < 'local , T : TypeArray > JPrimitiveArray < 'local , T > { # [doc = " Creates a [`JPrimitiveArray`] that wraps the given `raw` [`jarray`]"] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " - `raw` must be a valid raw JNI local reference (or `null`)."] # [doc = " - `raw` must be an instance of the appropriate primitive array type, e.g. `int[]`."] # [doc = " - There must not be any other owning [`Reference`] wrapper for the same reference."] # [doc = " - The local reference must belong to the current thread and not outlive the"] # [doc = "   JNI stack frame associated with the [Env] `'env_local` lifetime."] pub unsafe fn from_raw < 'env_local > (env : & Env < 'env_local > , raw : jarray ,) -> JPrimitiveArray < 'env_local , T > { JPrimitiveArray { obj : JObject :: from_raw (env , raw as jobject) , _marker : PhantomData , } } # [doc = " Creates a new null reference."] # [doc = ""] # [doc = " Null references are always valid and do not belong to a local reference frame. Therefore,"] # [doc = " the returned `JPrimitiveArray` always has the `'static` lifetime."] pub const fn null () -> JPrimitiveArray < 'static , T > { JPrimitiveArray { obj : JObject :: null () , _marker : PhantomData , } } # [doc = " Unwrap to the raw jni type."] pub const fn into_raw (self) -> jarray { self . obj . into_raw () as jarray } # [doc = " Returns the length of the array."] pub fn len (& self , env : & Env) -> Result < usize > { let array = null_check ! (self . as_raw () , "JPrimitiveArray::len self argument") ? ; let len = unsafe { jni_call_unchecked ! (env , v1_1 , GetArrayLength , array) } as usize ; Ok (len) } # [doc = " Returns an [`AutoElements`] to access the elements of the given Java `array`."] # [doc = ""] # [doc = " The elements are accessible until the returned auto-release guard is dropped."] # [doc = ""] # [doc = " The returned array may be a copy of the Java array and changes made to"] # [doc = " the returned array will not necessarily be reflected in the original"] # [doc = " array until the [`AutoElements`] guard is dropped."] # [doc = ""] # [doc = " If you know in advance that you will only be reading from the array then"] # [doc = " pass [`ReleaseMode::NoCopyBack`] so that the JNI implementation knows"] # [doc = " that it's not necessary to copy any data back to the original Java array"] # [doc = " when the [`AutoElements`] guard is dropped."] # [doc = ""] # [doc = " Since the returned array may be a copy of the Java array, changes made to the"] # [doc = " returned array will not necessarily be reflected in the original array until"] # [doc = " the corresponding `Release*ArrayElements` JNI method is called."] # [doc = " [`AutoElements`] has a commit() method, to force a copy back of pending"] # [doc = " array changes if needed (and without releasing it)."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " ## No data races"] # [doc = ""] # [doc = " This API has no built-in synchronization that ensures there won't be any data"] # [doc = " races while accessing the array elements."] # [doc = ""] # [doc = " To avoid undefined behaviour it is the caller's responsibility to ensure there"] # [doc = " will be no data races between other Rust or Java threads trying to access the"] # [doc = " same array."] # [doc = ""] # [doc = " Acquiring a [`MonitorGuard`] lock for the `array` could be one way of ensuring"] # [doc = " mutual exclusion between Rust and Java threads, so long as the Java threads"] # [doc = " also acquire the same lock via `synchronized(array) {}`."] # [doc = ""] # [doc = " ## No aliasing"] # [doc = ""] # [doc = " Callers must not create more than one [`AutoElements`] or"] # [doc = " [`AutoElementsCritical`] per Java array at the same time - even if"] # [doc = " there is no risk of a data race."] # [doc = ""] # [doc = " The reason for this restriction is that [`AutoElements`] and"] # [doc = " [`AutoElementsCritical`] implement `DerefMut` which can provide a"] # [doc = " mutable `&mut [T]` slice reference for the elements and it would"] # [doc = " constitute undefined behaviour to allow there to be more than one"] # [doc = " mutable reference that points to the same memory."] # [doc = ""] # [doc = " # jboolean elements"] # [doc = ""] # [doc = " Keep in mind that arrays of `jboolean` values should only ever hold"] # [doc = " values of `0` or `1` because any other value could lead to undefined"] # [doc = " behaviour within the JVM."] # [doc = ""] # [doc = " Also see"] # [doc = " [`Env::get_array_elements_critical`] which"] # [doc = " imposes additional restrictions that make it less likely to incur the"] # [doc = " cost of copying the array elements."] pub unsafe fn get_elements (& self , env : & Env , mode : ReleaseMode ,) -> Result < AutoElements < 'local , T , & Self > > { AutoElements :: new (env , self , mode) } # [doc = " Returns an [`AutoElementsCritical`] to access the elements of this"] # [doc = " array."] # [doc = ""] # [doc = " The elements are accessible during the critical section that exists"] # [doc = " until the returned auto-release guard is dropped."] # [doc = ""] # [doc = " This API imposes some strict restrictions that help the JNI"] # [doc = " implementation avoid any need to copy the underlying array elements"] # [doc = " before making them accessible to native code:"] # [doc = ""] # [doc = " 1. No other use of JNI calls are allowed (on the same thread) within the"] # [doc = "    critical section that exists while holding the"] # [doc = "    [`AutoElementsCritical`] guard."] # [doc = " 2. No system calls can be made (Such as `read`) that may depend on a"] # [doc = "    result from another Java thread."] # [doc = ""] # [doc = " The JNI spec does not specify what will happen if these rules aren't"] # [doc = " adhered to but it should be assumed it will lead to undefined behaviour,"] # [doc = " likely deadlock and possible program termination."] # [doc = ""] # [doc = " Even with these restrictions the returned array may still be a copy of"] # [doc = " the Java array and changes made to the returned array will not"] # [doc = " necessarily be reflected in the original array until the"] # [doc = " [`AutoElementsCritical`] guard is dropped."] # [doc = ""] # [doc = " If you know in advance that you will only be reading from the array then"] # [doc = " pass [`ReleaseMode::NoCopyBack`] so that the JNI implementation knows"] # [doc = " that it's not necessary to copy any data back to the original Java array"] # [doc = " when the [`AutoElementsCritical`] guard is dropped."] # [doc = ""] # [doc = " A nested scope or explicit use of `std::mem::drop` can be used to"] # [doc = " control when the returned [`AutoElementsCritical`] is dropped to"] # [doc = " minimize the length of the critical section."] # [doc = ""] # [doc = " If this array is `null`, an [`Error::NullPtr`] is returned."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " ## Critical Section Restrictions"] # [doc = ""] # [doc = " This API is marked as `unsafe` due to the complex, far-reaching nature"] # [doc = " of the critical-section restrictions imposed here that can't be enforced"] # [doc = " through Rust's borrow checker rules."] # [doc = ""] # [doc = " The rules above about JNI usage and system calls _must_ be adhered to."] # [doc = ""] # [doc = " Using this API implies:"] # [doc = ""] # [doc = " 1. All garbage collection will likely be paused during the critical"] # [doc = "    section"] # [doc = " 2. Any use of JNI in other threads may block if they need to allocate"] # [doc = "    memory (due to the garbage collector being paused)"] # [doc = " 3. Any use of system calls that will wait for a result from another Java"] # [doc = "    thread could deadlock if that other thread is blocked by a paused"] # [doc = "    garbage collector."] # [doc = ""] # [doc = " A failure to adhere to the critical section rules could lead to any"] # [doc = " undefined behaviour, including aborting the program."] # [doc = ""] # [doc = " ## No data races"] # [doc = ""] # [doc = " This API has no built-in synchronization that ensures there won't be any"] # [doc = " data races while accessing the array elements."] # [doc = ""] # [doc = " To avoid undefined behaviour it is the caller's responsibility to ensure"] # [doc = " there will be no data races between other Rust or Java threads trying to"] # [doc = " access the same array."] # [doc = ""] # [doc = " Acquiring a [`MonitorGuard`] lock for this array could be one way of"] # [doc = " ensuring mutual exclusion between Rust and Java threads, so long as the"] # [doc = " Java threads also acquire the same lock via `synchronized(array) {}`."] # [doc = ""] # [doc = " ## No aliasing"] # [doc = ""] # [doc = " Callers must not create more than one [`AutoElements`] or"] # [doc = " [`AutoElementsCritical`] per Java array at the same time - even if there"] # [doc = " is no risk of a data race."] # [doc = ""] # [doc = " The reason for this restriction is that [`AutoElements`] and"] # [doc = " [`AutoElementsCritical`] implement `DerefMut` which can provide a"] # [doc = " mutable `&mut [T]` slice reference for the elements and it would"] # [doc = " constitute undefined behaviour to allow there to be more than one"] # [doc = " mutable reference that points to the same memory."] # [doc = ""] # [doc = " ## jboolean elements"] # [doc = ""] # [doc = " Keep in mind that arrays of `jboolean` values should only ever hold"] # [doc = " values of `0` or `1` because any other value could lead to undefined"] # [doc = " behaviour within the JVM."] # [doc = ""] # [doc = " Also see [`get_elements`](Self::get_elements) which has"] # [doc = " fewer restrictions, but is more likely to incur a cost from copying"] # [doc = " the array elements."] pub unsafe fn get_elements_critical (& self , env : & Env < '_ > , mode : ReleaseMode ,) -> Result < AutoElementsCritical < 'local , T , & Self > > { AutoElementsCritical :: new (env , self , mode) } # [doc = " Copy elements of the array from the `start` index to the"] # [doc = " `buf` slice. The number of copied elements is equal to the `buf` length."] # [doc = ""] # [doc = " # Errors"] # [doc = " If `start` is negative _or_ `start + buf.len()` is greater than [`array.length`]"] # [doc = " then no elements are copied, an `ArrayIndexOutOfBoundsException` is thrown,"] # [doc = " and `Err` is returned."] # [doc = ""] # [doc = " [`array.length`]: struct.Env.html#method.get_array_length"] pub fn get_region (& self , env : & Env , start : crate :: sys :: jsize , buf : & mut [T]) -> Result < () > { unsafe { T :: get_region (env , self . as_raw () as jarray , start , buf) } } # [doc = " Copy the contents of the `buf` slice to the java byte array at the"] # [doc = " `start` index."] pub fn set_region (& self , env : & Env , start : crate :: sys :: jsize , buf : & [T]) -> Result < () > { unsafe { T :: set_region (env , self . as_raw () as jarray , start , buf) } } }
};
}
