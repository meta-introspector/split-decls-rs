// Generated macro for impl_218 (impl)
macro_rules! Depcrate_refs_globalimpl_218 {
() => {
// Module: crate::refs::global
// Provides: {"impl_218"}
// Dependencies: {}
impl < T > Global < T > where T : Into < JObject < 'static > > + AsRef < JObject < 'static > > + Default + Reference + Send + Sync + 'static , { # [doc = " Creates a new auto-delete wrapper for the `'static` global reference"] # [doc = ""] # [doc = " Note: It's more likely that you want to look at the [`Env::new_global_ref`] API instead"] # [doc = " of this, since you can't get `'static` reference types through safe APIs."] # [doc = ""] # [doc = " The [`Env`] reference here serves as proof that the current thread is attached, which"] # [doc = " implies [`JavaVM::singleton()`] is initialized, which is required by the `Drop`"] # [doc = " implementation."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If the given reference is non-null, it must represent a global JNI reference."] pub (crate) unsafe fn new (_env : & Env , obj : T) -> Self { Self { obj } } # [doc = " Creates a [`Global`] wrapper for a `null` reference"] # [doc = ""] # [doc = " This is equivalent [`Global::default()`]"] pub fn null () -> Self { Self { obj : T :: default () } } # [doc = " Unwrap to the auto-delete global reference into a raw global `jobject`."] # [doc = ""] # [doc = " This prevents the global reference from being automatically deleted when"] # [doc = " this guard is dropped."] # [doc = ""] # [doc = " # Leaking References"] # [doc = ""] # [doc = " When unwrapping a [`Global`] you should consider how else you will"] # [doc = " ensure that the reference will get deleted."] # [doc = ""] # [doc = " The global reference may end up leaking unless a new [`Global`] wrapper"] # [doc = " is create later, or you find some way to call the JNI `DeleteGlobalRef`"] # [doc = " API on the raw reference."] # [doc = ""] # [doc = " **Note:** You should not find yourself unwrapping a raw global `jobject`"] # [doc = " that you do not logically own. If you have a raw global `jobject` that"] # [doc = " you do not own, you should instead use [`Env::as_cast_raw`] to create a"] # [doc = " temporary wrapper."] pub fn into_raw (mut self) -> sys :: jobject { let obj = std :: mem :: take (& mut self . obj) ; std :: mem :: forget (self) ; let obj : JObject = obj . into () ; obj . into_raw () } # [doc = " Borrows a `JObject` referring to the same Java object as this"] # [doc = " `Global`."] # [doc = ""] # [doc = " This method is zero-cost and does not create a new local reference."] # [doc = ""] # [doc = " `Global` also implements <code>[AsRef]&lt;[JObject]&gt;</code>."] # [doc = " That trait's `as_ref` method does the same thing as this method."] pub fn as_obj (& self) -> & JObject < 'static > { self . as_ref () } }
};
}
