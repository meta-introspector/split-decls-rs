// Generated macro for Env (struct)
macro_rules! Depcrate_envEnv {
() => {
// Module: crate::env
// Provides: {"Env"}
// Dependencies: {}
# [doc = " A non-transparent wrapper around a raw [`sys::JNIEnv`] pointer that provides"] # [doc = " safe access to the Java Native Interface (JNI) functions."] # [doc = ""] # [doc = " Since [Env] is not a type that is publicly constructible, you must obtain a"] # [doc = " reference, either through a thread attachment, by upgrading an"] # [doc = " [`EnvUnowned`] within a native method, or via the (unsafe)"] # [doc = " [`AttachGuard::from_unowned`] APIs."] # [doc = ""] # [doc = " See:"] # [doc = " - [`JavaVM::attach_current_thread`]"] # [doc = " - [`JavaVM::attach_current_thread_for_scope`]"] # [doc = " - [`JavaVM::with_local_frame`]"] # [doc = " - [`JavaVM::with_top_local_frame`]"] # [doc = " - [`EnvUnowned::with_env`]"] # [doc = ""] # [doc = " [Env] is a non-transparent wrapper that is not FFI-safe, so it must not be"] # [doc = " used to try and capture a raw [`sys::JNIEnv`] pointer passed to a native"] # [doc = " method."] # [doc = ""] # [doc = " See [`EnvUnowned`] for a wrapper that is FFI-safe and can be used to capture"] # [doc = " a `sys::JNIEnv` pointer passed to a native method."] # [doc = ""] # [doc = " # Overview of the API"] # [doc = ""] # [doc = " All the [`Env`] methods aim to follow the JNI specification naming closely"] # [doc = " enough to make it easier to map between the two. This means that method"] # [doc = " names, parameter types, and return types will generally be consistent with"] # [doc = " the JNI specification."] # [doc = ""] # [doc = " JNI APIs that can logically be thought of as methods, such as"] # [doc = " `GetObjectArrayElement`, can be found as methods on the relevant Rust type,"] # [doc = " in this case [`JObjectArray::get_element`]."] # [doc = ""] # [doc = " # Exception handling"] # [doc = ""] # [doc = " Since we're calling into the JVM with this, many methods also have the"] # [doc = " potential to cause an exception to get thrown. If this is the case, an `Err`"] # [doc = " result will be returned with the error kind [`Error::JavaException`]. Note"] # [doc = " that this will _not_ clear the exception - it's up to the caller to decide"] # [doc = " whether to do so or to let it continue being thrown."] # [doc = ""] # [doc = " # References and Lifetimes"] # [doc = ""] # [doc = " As in C JNI, interactions with Java objects happen through"] # [doc = " <dfn>references</dfn>, either local or global."] # [doc = ""] # [doc = " This crate provides various safe wrappers around JNI references, such as"] # [doc = " [`JObject`], [`JClass`], [`JString`] and [`JList`] which all implement the"] # [doc = " [`Reference`] trait."] # [doc = ""] # [doc = " By default these types represent <dfn>local references</dfn>, and they will"] # [doc = " be associated with a lifetime that names the local reference frame they"] # [doc = " belong to."] # [doc = ""] # [doc = " So for example `JObject<'local_1>`  and  `JObject<'local_2>` are local"] # [doc = " reference that are owned by two different local reference frames and"] # [doc = " although they may both be usable from a higher stack frame, it's not"] # [doc = " possible to simply move ownership of a local reference from one local"] # [doc = " reference frame to another."] # [doc = ""] # [doc = " A global reference can be represented by a [`Global`], which will wrap one"] # [doc = " of the above primitive reference types like `Global<JObject<'static>>`. In"] # [doc = " this case the `'static` lifetime indicates that the reference is not tied to"] # [doc = " a local reference frame and can be used from any thread."] # [doc = ""] # [doc = " So long as there is at least one reference to a Java object, the JVM garbage"] # [doc = " collector will not reclaim it."] # [doc = ""] # [doc = " <dfn>Global references</dfn> exist until deleted. Deletion occurs when the"] # [doc = " `Global` is dropped."] # [doc = ""] # [doc = " <dfn>Local references</dfn> belong to a local reference frame, and exist"] # [doc = " until [deleted][Env::delete_local_ref] or until the local reference frame is"] # [doc = " exited. A new <dfn>local reference frame</dfn> is entered when a native"] # [doc = " method is called from Java, or when Rust code does so explicitly using"] # [doc = " [`Env::with_local_frame`]. That local reference frame is exited when the"] # [doc = " native method or `with_local_frame` returns. When a local reference frame is"] # [doc = " exited, all local references created inside it are deleted."] # [doc = ""] # [doc = " Unlike C JNI, this crate creates a separate `Env` for each local reference"] # [doc = " frame. The associated Rust lifetime `'local` represents that local reference"] # [doc = " frame. Rust's borrow checker will ensure that local references are not used"] # [doc = " after their local reference frame exits (which would cause undefined"] # [doc = " behavior)."] # [doc = ""] # [doc = " Unlike global references, local references are not deleted when dropped by"] # [doc = " default. This is for performance: it is faster for the JVM to delete all of"] # [doc = " the local references in a frame all at once, than to delete each local"] # [doc = " reference one at a time. However, this can cause a memory leak if the local"] # [doc = " reference frame remains entered for a long time, such as a long-lasting"] # [doc = " loop, in which case local references should be deleted explicitly. Local"] # [doc = " references can be deleted when dropped if desired; use"] # [doc = " [`Env::delete_local_ref`] or wrap with [`IntoAuto::auto`] to arrange that."] # [doc = ""] # [doc = " ## Lifetime Names"] # [doc = ""] # [doc = " This crate uses the following convention for lifetime names:"] # [doc = ""] # [doc = " * `'local` is the lifetime of a local reference frame, as described above."] # [doc = ""] # [doc = " * `'other_local`, `'other_local_1`, and `'other_local_2` are the lifetimes"] # [doc = "   of some other local reference frame, which may be but doesn't have to be"] # [doc = "   the same as `'local`. For example, [`Env::new_local_ref`] accepts a local"] # [doc = "   reference in any local reference frame `'other_local` and creates a new"] # [doc = "   local reference to the same object in `'local`."] # [doc = ""] # [doc = " * `'obj_ref` is the lifetime of a borrow of a JNI reference, like"] # [doc = "   <code>&amp;[JObject]</code> or <code>&amp;[Global]</code>. For example,"] # [doc = "   [`Env::get_list`] constructs a new [`JList`] that borrows a `&'obj_ref"] # [doc = "   JObject`."] # [doc = ""] # [doc = " ## `null` Java references"] # [doc = " `null` Java references are handled by the following rules:"] # [doc = "   - Methods that require non-null references will return [Error::NullPtr] if"] # [doc = "     passed a `null` reference."] # [doc = "   - If a JNI function returns `null` to indicate an error (e.g."] # [doc = "     [`Env::new_int_array`]), it is converted to `Err`/[`Error::NullPtr`], or"] # [doc = "     some other more applicable error type, such as [`Error::MethodNotFound`]."] # [doc = "   - Otherwise `null` is considered a valid Java reference and is represented"] # [doc = "     as [`JObject::null()`]."] # [doc = ""] # [doc = " ## A `&mut Env` represents the top JNI stack frame"] # [doc = ""] # [doc = " Each `Env<'local>` represents a single local reference frame in the JVM."] # [doc = ""] # [doc = " A **mutable** `&mut Env` is special: it always represents the *current top*"] # [doc = " local reference frame on the JNI stack."] # [doc = ""] # [doc = " * You need `&mut Env` when creating new local references, since those can"] # [doc = "   only be created in the top frame."] # [doc = " * A shared `&Env` is fine for operations that don’t produce new local"] # [doc = "   references."] # [doc = ""] # [doc = " If you only have a shared `&Env` but need to create temporary local"] # [doc = " references (that won’t escape the scope), you can materialize a new mutable"] # [doc = " handle to the top frame using:"] # [doc = ""] # [doc = " * [`Env::with_local_frame`] – pushes a new frame, returns a mutable `Env`"] # [doc = "   for it."] # [doc = " * [`Env::with_top_local_frame`] – borrows a mutable `Env` for the existing"] # [doc = "   top frame without pushing a new one."] # [doc = ""] # [doc = " If you don’t even have an `Env` (e.g. inside a `Drop` implementation), you"] # [doc = " can still get one via:"] # [doc = ""] # [doc = " * [`JavaVM::with_local_frame`]"] # [doc = " * [`JavaVM::with_top_local_frame`]"] # [doc = ""] # [doc = " (and you can access a [`JavaVM`] via [`JavaVM::singleton`])."] # [doc = ""] # [doc = " These APIs are safe because they constrain the new [`Env`] lifetime to the"] # [doc = " scope of the call, so any references created cannot leak."] # [doc = ""] # [doc = " ## Runtime Top Frame Checks"] # [doc = ""] # [doc = " Rust’s type system ensures that a mutable `Env<'local>` only produces"] # [doc = " references tied to its frame."] # [doc = ""] # [doc = " Normally this crate also guarantees that a `&mut Env` represents the top"] # [doc = " frame."] # [doc = ""] # [doc = " However, because the JVM is global and multiple crates may independently use"] # [doc = " jni-rs, some valid usage patterns can be misused to create multiple mutable"] # [doc = " `Env`s in the same scope. For example:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # use jni::{JavaVM, errors::Result};"] # [doc = " # fn f(vm: &JavaVM) -> Result<()> {"] # [doc = " vm.attach_current_thread(|env1| -> Result<()> {"] # [doc = "     vm.attach_current_thread(|env2| -> Result<()> {"] # [doc = "         // env1 is still mutable, but no longer the top frame."] # [doc = "         // This will panic at runtime:"] # [doc = "         env1.new_string(c\"not valid here\").unwrap();"] # [doc = "         Ok(())"] # [doc = "     })"] # [doc = " })"] # [doc = " # ; Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " To prevent unsoundness, this crate enforces a **runtime check**:"] # [doc = ""] # [doc = " * Creating a new local reference with a mutable `Env` that is *not* the top"] # [doc = "   frame will cause a panic."] # [doc = ""] # [doc = " ### Rule of thumb"] # [doc = ""] # [doc = " * Keep only one `Env` in scope at a time."] # [doc = " * Always shadow outer `env` variables when introducing a new one."] # [doc = ""] # [doc = " As long as you follow this pattern, the type system will ensure that local"] # [doc = " references are only created from the valid top frame."] # [doc = ""] # [doc = " ## Troubleshooting `cannot borrow as mutable`"] # [doc = ""] # [doc = " If a function takes two or more parameters, one of them is `Env`, and"] # [doc = " another is something returned by a `Env` method (like [`JObject`]), then"] # [doc = " calls to that function may not compile:"] # [doc = ""] # [doc = " ```rust,compile_fail"] # [doc = " # use jni::{errors::Result, Env, objects::*};"] # [doc = " #"] # [doc = " # fn f(env: &mut Env) -> Result<()> {"] # [doc = " fn example_function("] # [doc = "     env: &mut Env,"] # [doc = "     obj: &JObject,"] # [doc = " ) {"] # [doc = "     // …"] # [doc = " }"] # [doc = ""] # [doc = " example_function("] # [doc = "     env,"] # [doc = "     // ERROR: cannot borrow `*env` as mutable more than once at a time"] # [doc = "     &env.new_object("] # [doc = "         c\"com/example/SomeClass\","] # [doc = "         c\"()V\","] # [doc = "         &[],"] # [doc = "     )?,"] # [doc = " )"] # [doc = " # ; Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " To fix this, the `Env` parameter needs to come *last*:"] # [doc = ""] # [doc = " ```rust,no_run"] # [doc = " # use jni::{errors::Result, Env, objects::*};"] # [doc = " #"] # [doc = " # fn f(env: &mut Env) -> Result<()> {"] # [doc = " fn example_function("] # [doc = "     obj: &JObject,"] # [doc = "     env: &mut Env,"] # [doc = " ) {"] # [doc = "     // …"] # [doc = " }"] # [doc = ""] # [doc = " example_function("] # [doc = "     &env.new_object("] # [doc = "         c\"com/example/SomeClass\","] # [doc = "         c\"()V\","] # [doc = "         &[],"] # [doc = "     )?,"] # [doc = "     env,"] # [doc = " )"] # [doc = " # ; Ok(())"] # [doc = " # }"] # [doc = " ```"] # [doc = ""] # [doc = " # Checked and unchecked methods"] # [doc = ""] # [doc = " Some of the methods come in two versions: checked (e.g. `call_method`) and"] # [doc = " unchecked (e.g. `call_method_unchecked`). Under the hood, checked methods"] # [doc = " perform some checks to ensure the validity of provided signatures, names and"] # [doc = " arguments, and then call the corresponding unchecked method."] # [doc = ""] # [doc = " Checked methods are more flexible as they allow passing class names and"] # [doc = " method/field descriptors as strings and may perform lookups of class objects"] # [doc = " and method/field ids for you, also performing all the needed precondition"] # [doc = " checks. However, these lookup operations are expensive, so if you need to"] # [doc = " call the same method (or access the same field) multiple times, it is"] # [doc = " [recommended](https://docs.oracle.com/en/java/javase/11/docs/specs/jni/design.html#accessing-fields-and-methods)"] # [doc = " to cache the instance of the class and the method/field id, e.g."] # [doc = "   - in loops"] # [doc = "   - when calling the same Java callback repeatedly."] # [doc = ""] # [doc = " If you do not cache references to classes and method/field ids, you will"] # [doc = " *not* benefit from the unchecked methods."] # [doc = ""] # [doc = " Calling unchecked methods with invalid arguments and/or invalid class and"] # [doc = " method descriptors may lead to segmentation fault."] # [doc = ""] # [doc = " # Zero-copy `AsRef<JNIStr>` arguments"] # [doc = ""] # [doc = " The [`Env`] JNI methods that take string arguments can often accept types"] # [doc = " that implement the [`AsRef<JNIStr>`] trait and allow for zero-copy"] # [doc = " conversions of `CStr` literals like `c\"java/lang/String\"`"] # [doc = ""] # [doc = " Under the hood, the JNI specification for many functions requires that"] # [doc = " string arguments are passed as NUL terminated, Modified UTF-8 encoded byte"] # [doc = " arrays."] # [doc = ""] # [doc = " For the majority of the JNI functions there is no practical difference"] # [doc = " between Modified UTF-8 and standard UTF-8 (for example class names and"] # [doc = " method names and signatures) and you can rely on a `const` validation at"] # [doc = " compile time that a given `CStr` literal is valid Modified UTF-8 string via"] # [doc = " [`JNIStr::from_cstr`]."] # [doc = ""] # [doc = " Anything that accepts an `AsRef<JNIStr>` can directly accept a `CStr`"] # [doc = " literal like `c\"java/lang/String\"` or `c\"(I)Ljava/lang/String;\"` and will be"] # [doc = " validated via [JNIStr::from_cstr]. Beware that validation failure will"] # [doc = " simply panic but in practice there should be no reason for these arguments"] # [doc = " to require non-UTF-8 strings."] # [doc = ""] # [doc = " For more complex strings that need full unicode support (for example when"] # [doc = " calling [`Env::new_string`] then you should use [JNIString::from] instead of"] # [doc = " relying on `CStr` literals."] # [doc = ""] # [derive (Debug)] pub struct Env < 'local > { # [doc = " A non-null JNIEnv pointer"] pub (crate) raw : * mut sys :: JNIEnv , # [doc = " The current [`jni::AttachGuard`] nesting level, which we assert matches"] # [doc = " the top/current nesting level whenever some API will return a new local"] # [doc = " reference"] pub (crate) level : usize , owns_attachment : bool , _lifetime : PhantomData < & 'local () > , }
};
}
