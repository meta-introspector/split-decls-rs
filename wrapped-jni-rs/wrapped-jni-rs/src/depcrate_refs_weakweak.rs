// Generated macro for Weak (struct)
macro_rules! Depcrate_refs_weakWeak {
() => {
// Module: crate::refs::weak
// Provides: {"Weak"}
// Dependencies: {}
# [doc = " A global reference to a Java object that does *not* prevent it from being"] # [doc = " garbage collected."] # [doc = ""] # [doc = " <dfn>Weak global references</dfn> have the same properties as [ordinary"] # [doc = " “strong” global references][Global], with one exception: a weak global"] # [doc = " reference does not prevent the referenced Java object from being garbage"] # [doc = " collected. In other words, the Java object can be garbage collected even if"] # [doc = " there is a weak global reference to it."] # [doc = ""] # [doc = ""] # [doc = " # Upgrading"] # [doc = ""] # [doc = " Because the Java object referred to by a weak global reference may be"] # [doc = " garbage collected at any moment, it cannot be directly used (such as"] # [doc = " calling methods on the referenced Java object). Instead, it must first be"] # [doc = " <dfn>upgraded</dfn> to a local or strong global reference, using the"] # [doc = " [`Weak::upgrade_local`] or [`Weak::upgrade_global`] method,"] # [doc = " respectively."] # [doc = ""] # [doc = " Both upgrade methods return an [`Option`]. If, when the upgrade method is"] # [doc = " called, the Java object has not yet been garbage collected, then the"] # [doc = " `Option` will be [`Some`] containing a newly created strong reference that"] # [doc = " can be used as normal. If not, the `Option` will be [`None`]."] # [doc = ""] # [doc = " Upgrading a weak global reference does not delete it. It is only deleted"] # [doc = " when the `Weak` is dropped, and it can be upgraded more than once."] # [doc = ""] # [doc = ""] # [doc = " # Creating and Deleting"] # [doc = ""] # [doc = " To create a weak global reference, use the [`Env::new_weak_ref`] method."] # [doc = " To delete it, simply drop the `Weak` (but be sure to do so on an attached"] # [doc = " thread if possible; see the warning below)."] # [doc = ""] # [doc = " It is also possible to create a new JNI weak global reference from an"] # [doc = " existing one. To do that, use the [`Weak::clone_in_jvm`] method."] # [doc = ""] # [doc = ""] # [doc = " # Warning: Drop On an Attached Thread If Possible"] # [doc = ""] # [doc = " When a `Weak` is dropped, a JNI call is made to delete the global"] # [doc = " reference. If this frequently happens on a thread that is not already"] # [doc = " attached to the JVM, the thread will be temporarily attached using"] # [doc = " [`JavaVM::attach_current_thread_for_scope`], causing a severe performance"] # [doc = " penalty."] # [doc = ""] # [doc = " To avoid this performance penalty, ensure that `Weak`s are only dropped"] # [doc = " on a thread that is already attached (or never dropped at all)."] # [doc = ""] # [doc = " In the event that a global reference is dropped on an unattached thread, a"] # [doc = " message is [logged][log] at [`log::Level::Warn`]."] # [repr (transparent)] # [derive (Debug)] pub struct Weak < T > where T : Into < JObject < 'static > > + AsRef < JObject < 'static > > + Default + Reference + Send + Sync , { obj : T , }
};
}
