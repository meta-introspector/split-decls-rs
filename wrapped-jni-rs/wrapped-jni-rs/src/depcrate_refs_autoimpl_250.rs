// Generated macro for impl_250 (impl)
macro_rules! Depcrate_refs_autoimpl_250 {
() => {
// Module: crate::refs::auto
// Provides: {"impl_250"}
// Dependencies: {}
impl < 'local , T > Auto < 'local , T > where T : Into < JObject < 'local > > , { # [doc = " Creates a new auto-delete wrapper for a local ref."] # [doc = ""] # [doc = " Once this wrapper goes out of scope, the `delete_local_ref` will be"] # [doc = " called on the object. While wrapped, the object can be accessed via"] # [doc = " the `Deref` impl."] pub fn new (obj : T) -> Self { Auto { obj : ManuallyDrop :: new (obj) , _lifetime : PhantomData , } } # [doc = " Unwrap the RAII, auto-delete wrapper, returning the original local"] # [doc = " reference."] # [doc = ""] # [doc = " This prevents the local reference from being automatically deleted when"] # [doc = " this guard is dropped, and the local reference will instead get deleted"] # [doc = " when the JNI local frame holding the reference gets unwound."] # [doc = ""] # [doc = " # Leaking References"] # [doc = ""] # [doc = " When unwrapping an [`Auto`] you should consider how else you will"] # [doc = " ensure that the local reference will get released."] # [doc = ""] # [doc = " If you are implementing a native method then you may not need to keep"] # [doc = " an [`Auto`] wrapper since you can assume that when you return back to"] # [doc = " the Java VM then the local JNI stack frame will unwind and delete all"] # [doc = " local references."] # [doc = ""] # [doc = " Another option can be to use `Env::with_local_frame` or similar APIs"] # [doc = " that create a temporary JNI local frame where you can assume that all"] # [doc = " local references will be deleted when that local frame is unwound, after"] # [doc = " the given closure is called."] # [doc = ""] pub fn unwrap (self) -> T { let self_md = ManuallyDrop :: new (self) ; unsafe { ptr :: read (& * self_md . obj) } } # [doc = " Unwrap the RAII, auto-delete wrapper, returning the original local reference."] # [doc = ""] # [doc = " See [`Self::unwrap`]"] # [deprecated = "Renamed to Auto::unwrap"] pub fn forget (self) -> T { self . unwrap () } }
};
}
