// Generated macro for impl_677 (impl)
macro_rules! Depcrate_vm_init_argsimpl_677 {
() => {
// Module: crate::vm::init_args
// Provides: {"impl_677"}
// Dependencies: {}
impl < 'a > InitArgsBuilder < 'a > { # [doc = " Create a new default InitArgsBuilder"] pub fn new () -> Self { Default :: default () } # [doc = " Adds a JVM option, such as `-Djavax.net.debug=all`."] # [doc = ""] # [doc = " See [the JNI specification][jni-options] for details on which options are accepted."] # [doc = ""] # [doc = " The `vfprintf`, `abort`, and `exit` options are unsupported at this time. Setting one of"] # [doc = " these options has no effect."] # [doc = ""] # [doc = " The option must not contain any U+0000 code points except one at the end. A U+0000 code"] # [doc = " point at the end is not required, but on platforms where UTF-8 is the default character"] # [doc = " encoding, including one U+0000 code point at the end will make this method run slightly"] # [doc = " faster."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This method can fail if:"] # [doc = ""] # [doc = " * `opt_string` contains a U+0000 code point before the end."] # [doc = " * `opt_string` cannot be represented in the platform default character encoding."] # [doc = " * the platform's character encoding conversion API reports some other error."] # [doc = " * `opt_string` is too long. (In the current implementation, the maximum allowed length is"] # [doc = "   1048576 bytes on Windows. There is currently no limit on other platforms.)"] # [doc = ""] # [doc = " Errors raised by this method are deferred. If an error occurs, it is returned from"] # [doc = " [`InitArgsBuilder::build`] instead."] # [doc = ""] # [doc = " [jni-options]: https://docs.oracle.com/en/java/javase/11/docs/specs/jni/invocation.html#jni_createjavavm"] pub fn option (mut self , opt_string : impl AsRef < str > + Into < Cow < 'a , str > >) -> Self { if let Err (error) = self . try_option (opt_string) { self . opts = Err (error) ; } self } # [doc = " Adds a JVM option, such as `-Djavax.net.debug=all`. Returns an error immediately upon"] # [doc = " failure."] # [doc = ""] # [doc = " This is an alternative to [`InitArgsBuilder::option`] that does not defer errors. See"] # [doc = " below for details."] # [doc = ""] # [doc = " See [the JNI specification][jni-options] for details on which options are accepted."] # [doc = ""] # [doc = " The `vfprintf`, `abort`, and `exit` options are unsupported at this time. Setting one of"] # [doc = " these options has no effect."] # [doc = ""] # [doc = " The option must not contain any U+0000 code points except one at the end. A U+0000 code"] # [doc = " point at the end is not required, but on platforms where UTF-8 is the default character"] # [doc = " encoding, including one U+0000 code point at the end will make this method run slightly"] # [doc = " faster."] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " This method can fail if:"] # [doc = ""] # [doc = " * `opt_string` contains a U+0000 code point before the end."] # [doc = " * `opt_string` cannot be represented in the platform default character encoding."] # [doc = " * the platform's character encoding conversion API reports some other error."] # [doc = " * `opt_string` is too long. (In the current implementation, the maximum allowed length is"] # [doc = "   1048576 bytes on Windows. There is currently no limit on other platforms.)"] # [doc = ""] # [doc = " Unlike the `option` method, this one does not defer errors. If the `opt_string` cannot be"] # [doc = " used, then this method returns `Err` and `self` is not changed. If there is already a"] # [doc = " deferred error, however, then this method does nothing."] # [doc = ""] # [doc = " [jni-options]: https://docs.oracle.com/en/java/javase/11/docs/specs/jni/invocation.html#jni_createjavavm"] pub fn try_option (& mut self , opt_string : impl Into < Cow < 'a , str > >) -> Result < () , JvmError > { let opt_string = opt_string . into () ; let opts = match & mut self . opts { Ok (ok) => ok , Err (_) => return Ok (()) , } ; if matches ! (opt_string . as_ref () , "" | "\0") { opts . push (Cow :: Borrowed (c"" ,)) ; return Ok (()) ; } else if SPECIAL_OPTIONS . contains (& & * opt_string) { return Ok (()) ; } let encoded : Cow < 'a , CStr > = { cfg_if ! { if # [cfg (windows)] { char_encoding_windows :: str_to_cstr_win32_default_codepage (opt_string) ? } else { char_encoding_generic :: utf8_to_cstr (opt_string) ? } } } ; opts . push (encoded) ; Ok (()) } # [doc = " Adds a JVM option, such as `-Djavax.net.debug=all`. The option must be a `CStr` encoded in"] # [doc = " the platform default character encoding."] # [doc = ""] # [doc = " This is an alternative to [`InitArgsBuilder::option`] that does not do any encoding. This"] # [doc = " method is not `unsafe` as it cannot cause undefined behavior, but the option will be"] # [doc = " garbled (that is, become [mojibake](https://en.wikipedia.org/wiki/Mojibake)) if not"] # [doc = " encoded correctly."] # [doc = ""] # [doc = " See [the JNI specification][jni-options] for details on which options are accepted."] # [doc = ""] # [doc = " The `vfprintf`, `abort`, and `exit` options are unsupported at this time. Setting one of"] # [doc = " these options has no effect."] # [doc = ""] # [doc = " This method does not fail, and will neither return nor defer an error."] # [doc = ""] # [doc = " [jni-options]: https://docs.oracle.com/en/java/javase/11/docs/specs/jni/invocation.html#jni_createjavavm"] pub fn option_encoded (mut self , opt_string : impl Into < Cow < 'a , CStr > >) -> Self { let opt_string = opt_string . into () ; let opts = match & mut self . opts { Ok (ok) => ok , Err (_) => return self , } ; if SPECIAL_OPTIONS_C . contains (& & * opt_string) { return self ; } opts . push (opt_string) ; self } # [doc = " Set JNI version for the init args"] # [doc = ""] # [doc = " Default: V8"] pub fn version (self , version : JNIVersion) -> Self { let mut s = self ; s . version = version ; s } # [doc = " Set the `ignoreUnrecognized` init arg flag"] # [doc = ""] # [doc = " If ignoreUnrecognized is true, JavaVM::new ignores all unrecognized option strings that"] # [doc = " begin with \"-X\" or \"_\". If ignoreUnrecognized is false, JavaVM::new returns Err as soon as"] # [doc = " it encounters any unrecognized option strings."] # [doc = ""] # [doc = " Default: `false`"] pub fn ignore_unrecognized (self , ignore : bool) -> Self { let mut s = self ; s . ignore_unrecognized = ignore ; s } # [doc = " Build the `InitArgs`"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " If a call to [`InitArgsBuilder::option`] caused a deferred error, it is returned from this"] # [doc = " method."] pub fn build (self) -> Result < InitArgs < 'a > , JvmError > { let opt_strings = self . opts ? ; let opts : Vec < JavaVMOption > = opt_strings . iter () . map (| opt_string | JavaVMOption { optionString : opt_string . as_ptr () as _ , extraInfo : ptr :: null_mut () , }) . collect () ; Ok (InitArgs { inner : JavaVMInitArgs { version : self . version . into () , ignoreUnrecognized : self . ignore_unrecognized as _ , options : opts . as_ptr () as _ , nOptions : opts . len () as _ , } , _opts : opts , _opt_strings : opt_strings , }) } # [doc = " Returns collected options."] # [doc = ""] # [doc = " If a call to [`InitArgsBuilder::option`] caused a deferred error, then this method returns"] # [doc = " a reference to that error."] pub fn options (& self) -> Result < & [Cow < 'a , CStr >] , & JvmError > { self . opts . as_ref () . map (Vec :: as_slice) } }
};
}
