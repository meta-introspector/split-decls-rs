// Generated macro for impl_237 (impl)
macro_rules! Depcrate_refs_weakimpl_237 {
() => {
// Module: crate::refs::weak
// Provides: {"impl_237"}
// Dependencies: {}
impl < T > Weak < T > where T : Into < JObject < 'static > > + AsRef < JObject < 'static > > + Default + Reference + Send + Sync , { # [doc = " Creates a new auto-delete wrapper for the `'static` weak global reference"] # [doc = ""] # [doc = " Note: It's more likely that you want to look at the [`Env::new_weak_ref`] API instead"] # [doc = " of this, since you can't get `'static` reference types through safe APIs."] # [doc = ""] # [doc = " The [`Env`] reference here serves as proof that the current thread is attached, which"] # [doc = " implies [`JavaVM::singleton()`] is initialized, which is required by the `Drop`"] # [doc = " implementation."] # [doc = ""] # [doc = " # Safety"] # [doc = ""] # [doc = " If the given reference is non-null, it must represent a weak global JNI reference."] pub (crate) unsafe fn new (_env : & Env , obj : T) -> Self { Self { obj } } # [doc = " Creates a [`Global`] wrapper for a `null` reference"] # [doc = ""] # [doc = " This is equivalent [`Weak::default()`]"] # [doc = ""] # [doc = " A `null` [`Weak`] acts as-if the object has been garbage collected"] # [doc = " ([`Self::is_garbage_collected()`] will return `true`)."] pub fn null () -> Self { Self { obj : T :: default () } } # [doc = " Returns the raw JNI weak reference."] pub fn as_raw (& self) -> sys :: jweak { self . obj . as_raw () } # [doc = " Creates a new local reference to this object."] # [doc = ""] # [doc = " This returns `None` if the object has already been garbage collected, otherwise it returns"] # [doc = " `Some(new_local_reference)`."] # [doc = ""] # [doc = " If this method returns `Some(r)`, it is guaranteed that the object will not be garbage"] # [doc = " collected at least until `r` is deleted or becomes invalid."] pub fn upgrade_local < 'local > (& self , env : & mut Env < 'local >) -> Result < Option < T :: Kind < 'local > > > { match env . new_local_ref (self) { Ok (local_ref) => Ok (Some (local_ref)) , Err (Error :: ObjectFreed) => Ok (None) , Err (err) => Err (err) , } } # [doc = " Creates a new strong global reference to this object."] # [doc = ""] # [doc = " This returns `None` if the object has already been garbage collected, otherwise it returns"] # [doc = " `Some(new_local_reference)`."] # [doc = ""] # [doc = " If this method returns `Some(r)`, it is guaranteed that the object will not be garbage"] # [doc = " collected at least until `r` is deleted or becomes invalid."] pub fn upgrade_global (& self , env : & Env) -> Result < Option < Global < T :: GlobalKind > > > { match env . new_global_ref (self) { Err (Error :: ObjectFreed) => Ok (None) , Err (err) => Err (err) , Ok (global_ref) => Ok (Some (global_ref)) , } } # [doc = " Checks if the object referred to by this `Weak` has been garbage collected."] # [doc = ""] # [doc = " Note that garbage collection can happen at any moment, so a return of `Ok(true)` from this"] # [doc = " method does not guarantee that [`Weak::upgrade_local`] or [`Weak::upgrade_global`]"] # [doc = " will succeed."] # [doc = ""] # [doc = " This is equivalent to"] # [doc = " <code>self.[is_same_object][Weak::is_same_object](env, [JObject::null]\\())</code>."] pub fn is_garbage_collected (& self , env : & Env) -> bool { env . is_same_object (self , JObject :: null ()) } # [doc = " Returns true if this weak reference refers to the given object. Otherwise returns false."] # [doc = ""] # [doc = " If `object` is [null][JObject::null], then this method is equivalent to"] # [doc = " [`Weak::is_garbage_collected`]: it returns true if the object referred to by this"] # [doc = " `Weak` has been garbage collected, or false if the object has not yet been garbage"] # [doc = " collected."] # [deprecated = "Use Env::is_same_object"] pub fn is_same_object < 'local , O > (& self , env : & Env < 'local > , object : O) -> bool where O : AsRef < JObject < 'local > > , { env . is_same_object (self , object) } # [doc = " Returns true if this weak reference refers to the same object as another weak reference."] # [doc = " Otherwise returns false."] # [doc = ""] # [doc = " This method will also return true if both weak references refer to an object that has been"] # [doc = " garbage collected."] # [deprecated = "Use Env::is_same_object"] pub fn is_weak_ref_to_same_object (& self , env : & Env , other : & Self) -> bool { env . is_same_object (self , other) } # [doc = " Creates a new weak reference to the same object that this one refers to."] # [doc = ""] # [doc = " This method returns `None` if the object has already been garbage collected."] pub fn clone_in_jvm (& self , env : & mut Env < '_ >) -> Result < Option < Weak < T :: GlobalKind > > > { match env . new_weak_ref (self) { Err (Error :: ObjectFreed) => Ok (None) , Err (err) => Err (err) , Ok (weak_ref) => Ok (Some (weak_ref)) , } } }
};
}
