macro_rules! deps {
    () => {
        Testable!();
        QuickCheck!();
        TestResult!();
        Gen!();
    };
}

macro_rules! impl_76 {
    () => {
        deps!();
        impl QuickCheck { # [doc = " Creates a new `QuickCheck` value."] # [doc = ""] # [doc = " This can be used to run `QuickCheck` on things that implement"] # [doc = " `Testable`. You may also adjust the configuration, such as the"] # [doc = " number of tests to run."] # [doc = ""] # [doc = " By default, the maximum number of passed tests is set to `100`, the max"] # [doc = " number of overall tests is set to `10000` and the generator is created"] # [doc = " with a size of `100`."] pub fn new () -> QuickCheck { let rng = Gen :: new (qc_gen_size ()) ; let tests = qc_tests () ; let max_tests = cmp :: max (tests , qc_max_tests ()) ; let min_tests_passed = qc_min_tests_passed () ; QuickCheck { tests , max_tests , min_tests_passed , rng } } # [doc = " Set the random number generator to be used by `QuickCheck`."] pub fn set_rng (self , rng : Gen) -> QuickCheck { QuickCheck { rng , .. self } } # [deprecated (since = "1.1.0" , note = "use `set_rng` instead")] pub fn r#gen (self , rng : Gen) -> QuickCheck { self . set_rng (rng) } # [doc = " Set the number of tests to run."] # [doc = ""] # [doc = " This actually refers to the maximum number of *passed* tests that"] # [doc = " can occur. Namely, if a test causes a failure, future testing on that"] # [doc = " property stops. Additionally, if tests are discarded, there may be"] # [doc = " fewer than `tests` passed."] pub fn tests (mut self , tests : u64) -> QuickCheck { self . tests = tests ; self } # [doc = " Set the maximum number of tests to run."] # [doc = ""] # [doc = " The number of invocations of a property will never exceed this number."] # [doc = " This is necessary to cap the number of tests because `QuickCheck`"] # [doc = " properties can discard tests."] pub fn max_tests (mut self , max_tests : u64) -> QuickCheck { self . max_tests = max_tests ; self } # [doc = " Set the minimum number of tests that needs to pass."] # [doc = ""] # [doc = " This actually refers to the minimum number of *valid* *passed* tests"] # [doc = " that needs to pass for the property to be considered successful."] pub fn min_tests_passed (mut self , min_tests_passed : u64) -> QuickCheck { self . min_tests_passed = min_tests_passed ; self } # [doc = " Tests a property and returns the result."] # [doc = ""] # [doc = " The result returned is either the number of tests passed or a witness"] # [doc = " of failure."] # [doc = ""] # [doc = " (If you're using Rust's unit testing infrastructure, then you'll"] # [doc = " want to use the `quickcheck` method, which will `panic!` on failure.)"] pub fn quicktest < A > (& mut self , f : A) -> Result < u64 , TestResult > where A : Testable , { let mut n_tests_passed = 0 ; for _ in 0 .. self . max_tests { if n_tests_passed >= self . tests { break ; } match f . result (& mut self . rng) { TestResult { status : Pass , .. } => n_tests_passed += 1 , TestResult { status : Discard , .. } => continue , r @ TestResult { status : Fail , .. } => return Err (r) , } } Ok (n_tests_passed) } # [doc = " Tests a property and calls `panic!` on failure."] # [doc = ""] # [doc = " The `panic!` message will include a (hopefully) minimal witness of"] # [doc = " failure."] # [doc = ""] # [doc = " It is appropriate to use this method with Rust's unit testing"] # [doc = " infrastructure."] # [doc = ""] # [doc = " Note that if the environment variable `RUST_LOG` is set to enable"] # [doc = " `info` level log messages for the `quickcheck` crate, then this will"] # [doc = " include output on how many `QuickCheck` tests were passed."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```rust"] # [doc = " use quickcheck::QuickCheck;"] # [doc = ""] # [doc = " fn prop_reverse_reverse() {"] # [doc = "     fn revrev(xs: Vec<usize>) -> bool {"] # [doc = "         let rev: Vec<_> = xs.clone().into_iter().rev().collect();"] # [doc = "         let revrev: Vec<_> = rev.into_iter().rev().collect();"] # [doc = "         xs == revrev"] # [doc = "     }"] # [doc = "     QuickCheck::new().quickcheck(revrev as fn(Vec<usize>) -> bool);"] # [doc = " }"] # [doc = " ```"] pub fn quickcheck < A > (& mut self , f : A) where A : Testable , { let _ = crate :: env_logger_init () ; let n_tests_passed = match self . quicktest (f) { Ok (n_tests_passed) => n_tests_passed , Err (result) => panic ! ("{}" , result . failed_msg ()) , } ; if n_tests_passed >= self . min_tests_passed { info ! ("(Passed {} QuickCheck tests.)" , n_tests_passed) ; } else { panic ! ("(Unable to generate enough tests, {} not discarded.)" , n_tests_passed) ; } } }
    };
}

impl_76!()