// Generated macro for convert_args (macro)
macro_rules! Depcrateconvert_args {
() => {
// Module: crate
// Provides: {"convert_args"}
// Dependencies: {}
# [doc = " Macro that converts the keys or key-value pairs passed to another maplit"] # [doc = " macro. The default conversion is to use the [`Into`] trait, if no"] # [doc = " custom conversion is passed."] # [doc = ""] # [doc = " The syntax is:"] # [doc = ""] # [doc = " `convert_args!(` `keys=` *function* `,` `values=` *function* `,`"] # [doc = "     *macro_name* `!(` [ *key* => *value* [, *key* => *value* ... ] ] `))`"] # [doc = ""] # [doc = " Here *macro_name* is any other maplit macro and either or both of the"] # [doc = " explicit `keys=` and `values=` parameters can be omitted."] # [doc = ""] # [doc = " [`Into`]: https://doc.rust-lang.org/std/convert/trait.Into.html"] # [doc = ""] # [doc = " **Note** To use `convert_args`, the macro that is being wrapped"] # [doc = " must itself be brought into the current scope with `#[macro_use]` or `use`."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " #[macro_use] extern crate maplit;"] # [doc = " # fn main() {"] # [doc = ""] # [doc = " use std::collections::HashMap;"] # [doc = " use std::collections::BTreeSet;"] # [doc = ""] # [doc = " // a. Use the default conversion with the Into trait."] # [doc = " // Here this converts both the key and value string literals to `String`,"] # [doc = " // but we need to specify the map type exactly!"] # [doc = ""] # [doc = " let map1: HashMap<String, String> = convert_args!(hashmap!("] # [doc = "     \"a\" => \"b\","] # [doc = "     \"c\" => \"d\","] # [doc = " ));"] # [doc = ""] # [doc = " // b. Specify an explicit custom conversion for the keys. If we don't specify"] # [doc = " // a conversion for the values, they are not converted at all."] # [doc = ""] # [doc = " let map2 = convert_args!(keys=String::from, hashmap!("] # [doc = "     \"a\" => 1,"] # [doc = "     \"c\" => 2,"] # [doc = " ));"] # [doc = ""] # [doc = " // Note: map2 is a HashMap<String, i32>, but we didn't need to specify the type"] # [doc = " let _: HashMap<String, i32> = map2;"] # [doc = ""] # [doc = " // c. convert_args! works with all the maplit macros -- and macros from other"] # [doc = " // crates that have the same \"signature\"."] # [doc = " // For example, btreeset and conversion from &str to Vec<u8>."] # [doc = ""] # [doc = " let set: BTreeSet<Vec<u8>> = convert_args!(btreeset!("] # [doc = "     \"a\", \"b\", \"c\", \"d\", \"a\", \"e\", \"f\","] # [doc = " ));"] # [doc = " assert_eq!(set.len(), 6);"] # [doc = ""] # [doc = ""] # [doc = " # }"] # [doc = " ```"] # [macro_export (local_inner_macros)] macro_rules ! convert_args { (keys =$ kf : expr , $ macro_name : ident ! ($ ($ k : expr) ,* $ (,) *)) => { $ macro_name ! { $ (($ kf) ($ k)) ,* } } ; (keys =$ kf : expr , values =$ vf : expr , $ macro_name : ident ! ($ ($ k : expr) ,* $ (,) *)) => { $ macro_name ! { $ (($ kf) ($ k)) ,* } } ; (keys =$ kf : expr , values =$ vf : expr , $ macro_name : ident ! ($ ($ k : expr => $ v : expr) ,* $ (,) *)) => { $ macro_name ! { $ (($ kf) ($ k) => ($ vf) ($ v)) ,* } } ; (keys =$ kf : expr , $ macro_name : ident ! ($ ($ rest : tt) *)) => { convert_args ! { keys =$ kf , values =$ crate :: __id , $ macro_name ! ($ ($ rest) *) } } ; (values =$ vf : expr , $ macro_name : ident ! ($ ($ rest : tt) *)) => { convert_args ! { keys =$ crate :: __id , values =$ vf , $ macro_name ! ($ ($ rest) *) } } ; ($ macro_name : ident ! $ ($ rest : tt) *) => { convert_args ! { keys =:: std :: convert :: Into :: into , values =:: std :: convert :: Into :: into , $ macro_name ! $ ($ rest) * } } ; }
};
}
