// Generated macro for main (function)
macro_rules! Depcratemain {
() => {
// Module: crate
// Provides: {"main"}
// Dependencies: {}
fn main () -> Result < () > { let args : Vec < String > = std :: env :: args () . collect () ; if args . len () < 2 { eprintln ! ("Usage: grast <file.rs>        # flatten to turtle") ; eprintln ! ("       grast <directory>      # flatten all .rs files in directory to turtle") ; eprintln ! ("       grast -u <file.turtle> # unflatten from turtle") ; eprintln ! ("       grast --vfs <file.rs> <dir>  # export to VFS") ; anyhow :: bail ! ("Incorrect usage.") ; } match args [1] . as_str () { "-u" | "--ungrast" => { if args . len () < 3 { anyhow :: bail ! ("Usage: grast -u <file.turtle>") ; } let input = fs :: read_to_string (& args [2]) . context (format ! ("Failed to read turtle file: {}" , & args [2])) ? ; let db = GrastDb :: from_turtle (& input) ; println ! ("Loaded {} triples" , db . triples . len ()) ; println ! ("{}" , db . to_turtle ()) ; } "--vfs" => { if args . len () < 4 { anyhow :: bail ! ("Usage: grast --vfs <file.rs> <dir>") ; } let code = fs :: read_to_string (& args [2]) . context (format ! ("Failed to read Rust file: {}" , & args [2])) ? ; let ast = parse_file (& code) . context ("Failed to parse Rust code") ? ; let mut db = GrastDb :: new () ; db . flatten (& ast) ; db . to_vfs (Path :: new (& args [3])) . context (format ! ("Failed to export to VFS at {}" , & args [3])) ? ; println ! ("Exported to VFS at {}" , args [3]) ; } input_path_str => { let input_path = PathBuf :: from (input_path_str) ; if input_path . is_dir () { for entry in WalkDir :: new (& input_path) . into_iter () . filter_map (| e | e . ok ()) { let path = entry . path () ; if path . is_file () && path . extension () . map_or (false , | ext | ext == "rs") { process_file_and_print_turtle (path) . unwrap_or_else (| e | eprintln ! ("Error processing {}: {}" , path . display () , e)) ; } } } else if input_path . is_file () { process_file_and_print_turtle (& input_path) ? ; } else { anyhow :: bail ! ("Input path '{}' is neither a file nor a directory." , input_path . display ()) ; } } } Ok (()) }
};
}
