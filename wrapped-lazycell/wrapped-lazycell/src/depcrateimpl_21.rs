// Generated macro for impl_21 (impl)
macro_rules! Depcrateimpl_21 {
() => {
// Module: crate
// Provides: {"impl_21"}
// Dependencies: {}
impl < T > LazyCell < T > { # [doc = " Creates a new, empty, `LazyCell`."] pub fn new () -> LazyCell < T > { LazyCell { inner : UnsafeCell :: new (None) } } # [doc = " Put a value into this cell."] # [doc = ""] # [doc = " This function will return `Err(value)` if the cell is already full."] pub fn fill (& self , value : T) -> Result < () , T > { let slot = unsafe { & * self . inner . get () } ; if slot . is_some () { return Err (value) ; } let slot = unsafe { & mut * self . inner . get () } ; * slot = Some (value) ; Ok (()) } # [doc = " Put a value into this cell."] # [doc = ""] # [doc = " Note that this function is infallible but requires `&mut self`. By"] # [doc = " requiring `&mut self` we're guaranteed that no active borrows to this"] # [doc = " cell can exist so we can always fill in the value. This may not always"] # [doc = " be usable, however, as `&mut self` may not be possible to borrow."] # [doc = ""] # [doc = " # Return value"] # [doc = ""] # [doc = " This function returns the previous value, if any."] pub fn replace (& mut self , value : T) -> Option < T > { mem :: replace (unsafe { & mut * self . inner . get () } , Some (value)) } # [doc = " Test whether this cell has been previously filled."] pub fn filled (& self) -> bool { self . borrow () . is_some () } # [doc = " Borrows the contents of this lazy cell for the duration of the cell"] # [doc = " itself."] # [doc = ""] # [doc = " This function will return `Some` if the cell has been previously"] # [doc = " initialized, and `None` if it has not yet been initialized."] pub fn borrow (& self) -> Option < & T > { unsafe { & * self . inner . get () } . as_ref () } # [doc = " Borrows the contents of this lazy cell mutably for the duration of the cell"] # [doc = " itself."] # [doc = ""] # [doc = " This function will return `Some` if the cell has been previously"] # [doc = " initialized, and `None` if it has not yet been initialized."] pub fn borrow_mut (& mut self) -> Option < & mut T > { unsafe { & mut * self . inner . get () } . as_mut () } # [doc = " Borrows the contents of this lazy cell for the duration of the cell"] # [doc = " itself."] # [doc = ""] # [doc = " If the cell has not yet been filled, the cell is first filled using the"] # [doc = " function provided."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the cell becomes filled as a side effect of `f`."] pub fn borrow_with < F : FnOnce () -> T > (& self , f : F) -> & T { if let Some (value) = self . borrow () { return value ; } let value = f () ; if self . fill (value) . is_err () { panic ! ("borrow_with: cell was filled by closure") } self . borrow () . unwrap () } # [doc = " Borrows the contents of this `LazyCell` mutably for the duration of the"] # [doc = " cell itself."] # [doc = ""] # [doc = " If the cell has not yet been filled, the cell is first filled using the"] # [doc = " function provided."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the cell becomes filled as a side effect of `f`."] pub fn borrow_mut_with < F : FnOnce () -> T > (& mut self , f : F) -> & mut T { if ! self . filled () { let value = f () ; if self . fill (value) . is_err () { panic ! ("borrow_mut_with: cell was filled by closure") } } self . borrow_mut () . unwrap () } # [doc = " Same as `borrow_with`, but allows the initializing function to fail."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the cell becomes filled as a side effect of `f`."] pub fn try_borrow_with < E , F > (& self , f : F) -> Result < & T , E > where F : FnOnce () -> Result < T , E > { if let Some (value) = self . borrow () { return Ok (value) ; } let value = f () ? ; if self . fill (value) . is_err () { panic ! ("try_borrow_with: cell was filled by closure") } Ok (self . borrow () . unwrap ()) } # [doc = " Same as `borrow_mut_with`, but allows the initializing function to fail."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " Panics if the cell becomes filled as a side effect of `f`."] pub fn try_borrow_mut_with < E , F > (& mut self , f : F) -> Result < & mut T , E > where F : FnOnce () -> Result < T , E > { if self . filled () { return Ok (self . borrow_mut () . unwrap ()) ; } let value = f () ? ; if self . fill (value) . is_err () { panic ! ("try_borrow_mut_with: cell was filled by closure") } Ok (self . borrow_mut () . unwrap ()) } # [doc = " Consumes this `LazyCell`, returning the underlying value."] pub fn into_inner (self) -> Option < T > { # [allow (unused_unsafe)] unsafe { self . inner . into_inner () } } }
};
}
