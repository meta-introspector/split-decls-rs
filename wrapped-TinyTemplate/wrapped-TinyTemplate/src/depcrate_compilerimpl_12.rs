// Generated macro for impl_12 (impl)
macro_rules! Depcrate_compilerimpl_12 {
() => {
// Module: crate::compiler
// Provides: {"impl_12"}
// Dependencies: {}
impl < 'template > TemplateCompiler < 'template > { # [doc = " Create a new template compiler to parse and compile the given template."] pub fn new (text : & 'template str) -> TemplateCompiler < 'template > { TemplateCompiler { original_text : text , remaining_text : text , instructions : vec ! [] , block_stack : vec ! [] , trim_next : false , } } # [doc = " Consume the template compiler to parse the template and return the generated bytecode."] pub fn compile (mut self) -> Result < Vec < Instruction < 'template > > > { while ! self . remaining_text . is_empty () { if self . remaining_text . starts_with ("{#") { self . trim_next = false ; let tag = self . consume_tag ("#}") ? ; let comment = tag [2 .. (tag . len () - 2)] . trim () ; if comment . starts_with ('-') { self . trim_last_whitespace () ; } if comment . ends_with ('-') { self . trim_next_whitespace () ; } } else if self . remaining_text . starts_with ("{{") { self . trim_next = false ; let (discriminant , rest) = self . consume_block () ? ; match discriminant { "if" => { let (path , negated) = if rest . starts_with ("not") { (self . parse_path (& rest [4 ..]) ? , true) } else { (self . parse_path (rest) ? , false) } ; self . block_stack . push ((discriminant , Block :: Branch (self . instructions . len ()))) ; self . instructions . push (Instruction :: Branch (path , ! negated , UNKNOWN)) ; } "else" => { self . expect_empty (rest) ? ; let num_instructions = self . instructions . len () + 1 ; self . close_branch (num_instructions , discriminant) ? ; self . block_stack . push ((discriminant , Block :: Branch (self . instructions . len ()))) ; self . instructions . push (Instruction :: Goto (UNKNOWN)) } "endif" => { self . expect_empty (rest) ? ; let num_instructions = self . instructions . len () ; self . close_branch (num_instructions , discriminant) ? ; } "with" => { let (path , name) = self . parse_with (rest) ? ; let instruction = Instruction :: PushNamedContext (path , name) ; self . instructions . push (instruction) ; self . block_stack . push ((discriminant , Block :: With)) ; } "endwith" => { self . expect_empty (rest) ? ; if let Some ((_ , Block :: With)) = self . block_stack . pop () { self . instructions . push (Instruction :: PopContext) } else { return Err (self . parse_error (discriminant , "Found a closing endwith that doesn't match with a preceeding with." . to_string ())) ; } } "for" => { let (path , name) = self . parse_for (rest) ? ; self . instructions . push (Instruction :: PushIterationContext (path , name)) ; self . block_stack . push ((discriminant , Block :: For (self . instructions . len ()))) ; self . instructions . push (Instruction :: Iterate (UNKNOWN)) ; } "endfor" => { self . expect_empty (rest) ? ; let num_instructions = self . instructions . len () + 1 ; let goto_target = self . close_for (num_instructions , discriminant) ? ; self . instructions . push (Instruction :: Goto (goto_target)) ; self . instructions . push (Instruction :: PopContext) ; } "call" => { let (name , path) = self . parse_call (rest) ? ; self . instructions . push (Instruction :: Call (name , path)) ; } _ => { return Err (self . parse_error (discriminant , format ! ("Unknown block type '{}'" , discriminant) ,)) ; } } } else if self . remaining_text . starts_with ('{') { self . trim_next = false ; let (path , name) = self . consume_value () ? ; let instruction = match name { Some (name) => Instruction :: FormattedValue (path , name) , None => Instruction :: Value (path) , } ; self . instructions . push (instruction) ; } else { let mut escaped = false ; loop { let mut text = self . consume_text (escaped) ; if self . trim_next { text = text . trim_left () ; self . trim_next = false ; } escaped = text . ends_with ('\\') ; if escaped { text = & text [.. text . len () - 1] ; } self . instructions . push (Instruction :: Literal (text)) ; if ! escaped { break ; } if escaped && self . remaining_text . is_empty () { return Err (self . parse_error (text , "Found an escape that doesn't escape any character." . to_string ())) ; } } } } if let Some ((text , _)) = self . block_stack . pop () { return Err (self . parse_error (text , "Expected block-closing tag, but reached the end of input." . to_string () ,)) ; } Ok (self . instructions) } # [doc = " Splits a string into a list of named segments which can later be used to look up values in the"] # [doc = " context."] fn parse_path (& self , text : & 'template str) -> Result < Path < 'template > > { if ! text . starts_with ('@') { Ok (text . split ('.') . map (| s | match s . parse :: < usize > () { Ok (n) => PathStep :: Index (s , n) , Err (_) => PathStep :: Name (s) , }) . collect :: < Vec < _ > > ()) } else if KNOWN_KEYWORDS . iter () . any (| k | * k == text) { Ok (vec ! [PathStep :: Name (text)]) } else { Err (self . parse_error (text , format ! ("Invalid keyword name '{}'" , text))) } } # [doc = " Finds the line number and column where an error occurred. Location is the substring of"] # [doc = " self.original_text where the error was found, and msg is the error message."] fn parse_error (& self , location : & str , msg : String) -> Error { let (line , column) = get_offset (self . original_text , location) ; ParseError { msg , line , column } } # [doc = " Tags which should have no text after the discriminant use this to raise an error if"] # [doc = " text is found."] fn expect_empty (& self , text : & str) -> Result < () > { if text . is_empty () { Ok (()) } else { Err (self . parse_error (text , format ! ("Unexpected text '{}'" , text))) } } # [doc = " Close the branch that is on top of the block stack by setting its target instruction"] # [doc = " and popping it from the stack. Returns an error if the top of the block stack is not a"] # [doc = " branch."] fn close_branch (& mut self , new_target : usize , discriminant : & str) -> Result < () > { let branch_block = self . block_stack . pop () ; if let Some ((_ , Block :: Branch (index))) = branch_block { match & mut self . instructions [index] { Instruction :: Branch (_ , _ , target) => { * target = new_target ; Ok (()) } Instruction :: Goto (target) => { * target = new_target ; Ok (()) } _ => panic ! () , } } else { Err (self . parse_error (discriminant , "Found a closing endif or else which doesn't match with a preceding if." . to_string () ,)) } } # [doc = " Close the for loop that is on top of the block stack by setting its target instruction and"] # [doc = " popping it from the stack. Returns an error if the top of the stack is not a for loop."] # [doc = " Returns the index of the loop's Iterate instruction for further processing."] fn close_for (& mut self , new_target : usize , discriminant : & str) -> Result < usize > { let branch_block = self . block_stack . pop () ; if let Some ((_ , Block :: For (index))) = branch_block { match & mut self . instructions [index] { Instruction :: Iterate (target) => { * target = new_target ; Ok (index) } _ => panic ! () , } } else { Err (self . parse_error (discriminant , "Found a closing endfor which doesn't match with a preceding for." . to_string () ,)) } } # [doc = " Advance the cursor to the next { and return the consumed text. If `escaped` is true, skips"] # [doc = " a { at the start of the text."] fn consume_text (& mut self , escaped : bool) -> & 'template str { let search_substr = if escaped { & self . remaining_text [1 ..] } else { self . remaining_text } ; let mut position = search_substr . find ('{') . unwrap_or_else (| | search_substr . len ()) ; if escaped { position += 1 ; } let (text , remaining) = self . remaining_text . split_at (position) ; self . remaining_text = remaining ; text } # [doc = " Advance the cursor to the end of the value tag and return the value's path and optional"] # [doc = " formatter name."] fn consume_value (& mut self) -> Result < (Path < 'template > , Option < & 'template str >) > { let tag = self . consume_tag ("}") ? ; let mut tag = tag [1 .. (tag . len () - 1)] . trim () ; if tag . starts_with ('-') { tag = tag [1 ..] . trim () ; self . trim_last_whitespace () ; } if tag . ends_with ('-') { tag = tag [0 .. tag . len () - 1] . trim () ; self . trim_next_whitespace () ; } if let Some (index) = tag . find ('|') { let (path_str , name_str) = tag . split_at (index) ; let name = name_str [1 ..] . trim () ; let path = self . parse_path (path_str . trim ()) ? ; Ok ((path , Some (name))) } else { Ok ((self . parse_path (tag) ? , None)) } } # [doc = " Right-trim whitespace from the last text block we parsed."] fn trim_last_whitespace (& mut self) { if let Some (Instruction :: Literal (text)) = self . instructions . last_mut () { * text = text . trim_right () ; } } # [doc = " Make a note to left-trim whitespace from the next text block we parse."] fn trim_next_whitespace (& mut self) { self . trim_next = true ; } # [doc = " Advance the cursor to the end of the current block tag and return the discriminant substring"] # [doc = " and the rest of the text in the tag. Also handles trimming whitespace where needed."] fn consume_block (& mut self) -> Result < (& 'template str , & 'template str) > { let tag = self . consume_tag ("}}") ? ; let mut block = tag [2 .. (tag . len () - 2)] . trim () ; if block . starts_with ('-') { block = block [1 ..] . trim () ; self . trim_last_whitespace () ; } if block . ends_with ('-') { block = block [0 .. block . len () - 1] . trim () ; self . trim_next_whitespace () ; } let discriminant = block . split_whitespace () . next () . unwrap_or (block) ; let rest = block [discriminant . len () ..] . trim () ; Ok ((discriminant , rest)) } # [doc = " Advance the cursor to after the given expected_close string and return the text in between"] # [doc = " (including the expected_close characters), or return an error message if we reach the end"] # [doc = " of a line of text without finding it."] # [doc = " Assumes that there's a start token with the same length as the close token at the start of"] # [doc = " currently remaining text."] fn consume_tag (& mut self , expected_close : & str) -> Result < & 'template str > { let start_len = expected_close . len () ; let end_len = expected_close . len () ; if let Some (line) = self . remaining_text . lines () . next () { if let Some (pos) = line [start_len ..] . find (expected_close) { let (tag , remaining) = self . remaining_text . split_at (pos + start_len + end_len) ; self . remaining_text = remaining ; Ok (tag) } else { Err (self . parse_error (line , format ! ("Expected a closing '{}' but found end-of-line instead." , expected_close) ,)) } } else { Err (self . parse_error (self . remaining_text , format ! ("Expected a closing '{}' but found end-of-text instead." , expected_close) ,)) } } # [doc = " Parse a with tag to separate the value path from the (optional) name."] fn parse_with (& self , with_text : & 'template str) -> Result < (Path < 'template > , & 'template str) > { if let Some (index) = with_text . find (" as ") { let (path_str , name_str) = with_text . split_at (index) ; let path = self . parse_path (path_str . trim ()) ? ; let name = name_str [" as " . len () ..] . trim () ; Ok ((path , name)) } else { Err (self . parse_error (with_text , format ! ("Expected 'as <path>' in with block, but found \"{}\" instead" , with_text) ,)) } } # [doc = " Parse a for tag to separate the value path from the name."] fn parse_for (& self , for_text : & 'template str) -> Result < (Path < 'template > , & 'template str) > { if let Some (index) = for_text . find (" in ") { let (name_str , path_str) = for_text . split_at (index) ; let name = name_str . trim () ; let path = self . parse_path (path_str [" in " . len () ..] . trim ()) ? ; Ok ((path , name)) } else { Err (self . parse_error (for_text , format ! ("Unable to parse for block text '{}'" , for_text) ,)) } } # [doc = " Parse a call tag to separate the template name and context value."] fn parse_call (& self , call_text : & 'template str) -> Result < (& 'template str , Path < 'template >) > { if let Some (index) = call_text . find (" with ") { let (name_str , path_str) = call_text . split_at (index) ; let name = name_str . trim () ; let path = self . parse_path (path_str [" with " . len () ..] . trim ()) ? ; Ok ((name , path)) } else { Err (self . parse_error (call_text , format ! ("Unable to parse call block text '{}'" , call_text) ,)) } } }
};
}
