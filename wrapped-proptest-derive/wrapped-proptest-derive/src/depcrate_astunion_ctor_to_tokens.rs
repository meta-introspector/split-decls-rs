// Generated macro for union_ctor_to_tokens (function)
macro_rules! Depcrate_astunion_ctor_to_tokens {
() => {
// Module: crate::ast
// Provides: {"union_ctor_to_tokens"}
// Dependencies: {}
# [doc = " Tokenizes a weighted list of `Ctor`."] # [doc = ""] # [doc = " The logic is that the output should be as linear as possible while still"] # [doc = " supporting enums with an unbounded number of variants without any boxing"] # [doc = " (erasure) or dynamic dispatch."] # [doc = ""] # [doc = " As `TupleUnion` is (currently) limited to 10 summands in the coproduct"] # [doc = " we can't just emit the entire thing linearly as this will fail on the 11:th"] # [doc = " variant."] # [doc = ""] # [doc = " A naive approach to solve might be to simply use a cons-list like so:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " TupleUnion::new("] # [doc = "     (w_1, s_1),"] # [doc = "     (w_2 + w_3 + w_4 + w_5,"] # [doc = "      TupleUnion::new("] # [doc = "         (w_2, s_2),"] # [doc = "         (w_3 + w_4 + w_5,"] # [doc = "          TupleUnion::new("] # [doc = "             (w_3, s_3),"] # [doc = "             (w_4 + w_5,"] # [doc = "              TupleUnion::new("] # [doc = "                 (w_4, s_4),"] # [doc = "                 (w_5, s_5),"] # [doc = "             ))"] # [doc = "         ))"] # [doc = "     ))"] # [doc = " )"] # [doc = " ```"] # [doc = ""] # [doc = " However, we can do better by being linear for the `10 - 1` first"] # [doc = " strategies and then switch to nesting like so:"] # [doc = ""] # [doc = " ```ignore"] # [doc = " (1, 2, 3, 4, 5, 6, 7, 8, 9,"] # [doc = "     (10, 11, 12, 13, 14, 15, 16, 17, 18,"] # [doc = "         (19, ..)))"] # [doc = " ```"] # [cfg (not (feature = "boxed_union"))] fn union_ctor_to_tokens (tokens : & mut TokenStream , ctors : & [(u32 , Ctor)]) { if ctors . is_empty () { return ; } if let [(_ , ctor)] = ctors { ctor . to_tokens (tokens) ; return ; } let mut chunks = ctors . chunks (UNION_CHUNK_SIZE) ; let chunk = chunks . next () . unwrap () ; let head = chunk . iter () . map (wrap_arc) ; let tail = Recurse (weight_sum (ctors) - weight_sum (chunk) , chunks) ; quote_append ! (tokens , _proptest :: strategy :: TupleUnion :: new ((# (# head ,) * # tail))) ; struct Recurse < 'a > (u32 , :: std :: slice :: Chunks < 'a , (u32 , Ctor) >) ; impl < 'a > ToTokens for Recurse < 'a > { fn to_tokens (& self , tokens : & mut TokenStream) { let (tweight , mut chunks) = (self . 0 , self . 1 . clone ()) ; if let Some (chunk) = chunks . next () { if let [(w , c)] = chunk { quote_append ! (tokens , (# w , :: std :: sync :: Arc :: new (# c))) ; } else { let head = chunk . iter () . map (wrap_arc) ; let tail = Recurse (tweight - weight_sum (chunk) , chunks) ; quote_append ! (tokens , (# tweight , :: std :: sync :: Arc :: new (_proptest :: strategy :: TupleUnion :: new ((# (# head ,) * # tail))))) ; } } } } fn weight_sum (ctors : & [(u32 , Ctor)]) -> u32 { use std :: num :: Wrapping ; let Wrapping (x) = ctors . iter () . map (| & (w , _) | Wrapping (w)) . sum () ; x } fn wrap_arc (arg : & (u32 , Ctor)) -> TokenStream { let (w , c) = arg ; quote ! ((# w , :: std :: sync :: Arc :: new (# c))) } }
};
}
