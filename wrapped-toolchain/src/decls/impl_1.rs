macro_rules! deps {
    () => {
        Tool!();
    };
}

macro_rules! impl_1 {
    () => {
        deps!();
        impl Tool { pub fn proxy (self) -> Option < Utf8PathBuf > { cargo_proxy (self . name ()) } # [doc = " Return a `PathBuf` to use for the given executable."] # [doc = ""] # [doc = " The current implementation checks three places for an executable to use:"] # [doc = " 1) `$CARGO_HOME/bin/<executable_name>`"] # [doc = "    where $CARGO_HOME defaults to ~/.cargo (see <https://doc.rust-lang.org/cargo/guide/cargo-home.html>)"] # [doc = "    example: for cargo, this tries $CARGO_HOME/bin/cargo, or ~/.cargo/bin/cargo if $CARGO_HOME is unset."] # [doc = "    It seems that this is a reasonable place to try for cargo, rustc, and rustup"] # [doc = " 2) Appropriate environment variable (erroring if this is set but not a usable executable)"] # [doc = "    example: for cargo, this checks $CARGO environment variable; for rustc, $RUSTC; etc"] # [doc = " 3) $PATH/`<executable_name>`"] # [doc = "    example: for cargo, this tries all paths in $PATH with appended `cargo`, returning the"] # [doc = "    first that exists"] # [doc = " 4) If all else fails, we just try to use the executable name directly"] pub fn prefer_proxy (self) -> Utf8PathBuf { invoke (& [cargo_proxy , lookup_as_env_var , lookup_in_path] , self . name ()) } # [doc = " Return a `PathBuf` to use for the given executable."] # [doc = ""] # [doc = " The current implementation checks three places for an executable to use:"] # [doc = " 1) Appropriate environment variable (erroring if this is set but not a usable executable)"] # [doc = "    example: for cargo, this checks $CARGO environment variable; for rustc, $RUSTC; etc"] # [doc = " 2) $PATH/`<executable_name>`"] # [doc = "    example: for cargo, this tries all paths in $PATH with appended `cargo`, returning the"] # [doc = "    first that exists"] # [doc = " 3) `$CARGO_HOME/bin/<executable_name>`"] # [doc = "    where $CARGO_HOME defaults to ~/.cargo (see <https://doc.rust-lang.org/cargo/guide/cargo-home.html>)"] # [doc = "    example: for cargo, this tries $CARGO_HOME/bin/cargo, or ~/.cargo/bin/cargo if $CARGO_HOME is unset."] # [doc = "    It seems that this is a reasonable place to try for cargo, rustc, and rustup"] # [doc = " 4) If all else fails, we just try to use the executable name directly"] pub fn path (self) -> Utf8PathBuf { invoke (& [lookup_as_env_var , lookup_in_path , cargo_proxy] , self . name ()) } pub fn path_in (self , path : & Utf8Path) -> Option < Utf8PathBuf > { probe_for_binary (path . join (self . name ())) } pub fn name (self) -> & 'static str { match self { Tool :: Cargo => "cargo" , Tool :: Rustc => "rustc" , Tool :: Rustup => "rustup" , Tool :: Rustfmt => "rustfmt" , } } }
    };
}

impl_1!();