// Generated macro for impl_309 (impl)
macro_rules! Depcrate_normalimpl_309 {
() => {
// Module: crate::normal
// Provides: {"impl_309"}
// Dependencies: {}
impl < F > LogNormal < F > where F : Float , StandardNormal : Distribution < F > , { # [doc = " Construct, from (log-space) mean and standard deviation"] # [doc = ""] # [doc = " Parameters are the \"standard\" log-space measures (these are the mean"] # [doc = " and standard deviation of the logarithm of samples):"] # [doc = ""] # [doc = " -   `mu` (`μ`, unrestricted) is the mean of the underlying distribution"] # [doc = " -   `sigma` (`σ`, must be finite) is the standard deviation of the"] # [doc = "     underlying Normal distribution"] # [inline] pub fn new (mu : F , sigma : F) -> Result < LogNormal < F > , Error > { let norm = Normal :: new (mu , sigma) ? ; Ok (LogNormal { norm }) } # [doc = " Construct, from (linear-space) mean and coefficient of variation"] # [doc = ""] # [doc = " Parameters are linear-space measures:"] # [doc = ""] # [doc = " -   mean (`μ > 0`) is the (real) mean of the distribution"] # [doc = " -   coefficient of variation (`cv = σ / μ`, requiring `cv ≥ 0`) is a"] # [doc = "     standardized measure of dispersion"] # [doc = ""] # [doc = " As a special exception, `μ = 0, cv = 0` is allowed (samples are `-inf`)."] # [inline] pub fn from_mean_cv (mean : F , cv : F) -> Result < LogNormal < F > , Error > { if cv == F :: zero () { let mu = mean . ln () ; let norm = Normal :: new (mu , F :: zero ()) . unwrap () ; return Ok (LogNormal { norm }) ; } if ! (mean > F :: zero ()) { return Err (Error :: MeanTooSmall) ; } if ! (cv >= F :: zero ()) { return Err (Error :: BadVariance) ; } let a = F :: one () + cv * cv ; let mu = F :: from (0.5) . unwrap () * (mean * mean / a) . ln () ; let sigma = a . ln () . sqrt () ; let norm = Normal :: new (mu , sigma) ? ; Ok (LogNormal { norm }) } # [doc = " Sample from a z-score"] # [doc = ""] # [doc = " This may be useful for generating correlated samples `x1` and `x2`"] # [doc = " from two different distributions, as follows."] # [doc = " ```"] # [doc = " # use rand::prelude::*;"] # [doc = " # use rand_distr::{LogNormal, StandardNormal};"] # [doc = " let mut rng = rand::rng();"] # [doc = " let z = StandardNormal.sample(&mut rng);"] # [doc = " let x1 = LogNormal::from_mean_cv(3.0, 1.0).unwrap().from_zscore(z);"] # [doc = " let x2 = LogNormal::from_mean_cv(2.0, 4.0).unwrap().from_zscore(z);"] # [doc = " ```"] # [inline] pub fn from_zscore (& self , zscore : F) -> F { self . norm . from_zscore (zscore) . exp () } }
};
}
