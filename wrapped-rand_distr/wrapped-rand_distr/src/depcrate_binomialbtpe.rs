// Generated macro for btpe (function)
macro_rules! Depcrate_binomialbtpe {
() => {
// Module: crate::binomial
// Provides: {"btpe"}
// Dependencies: {}
# [allow (clippy :: many_single_char_names)] fn btpe < R : Rng + ? Sized > (btpe : Btpe , flipped : bool , rng : & mut R) -> u64 { const SQUEEZE_THRESHOLD : i64 = 20 ; let n = btpe . n as f64 ; let np = n * btpe . p ; let q = 1. - btpe . p ; let npq = np * q ; let f_m = np + btpe . p ; let m = btpe . m ; let p1 = btpe . p1 ; let x_m = (m as f64) + 0.5 ; let x_l = x_m - p1 ; let x_r = x_m + p1 ; let c = 0.134 + 20.5 / (15.3 + (m as f64)) ; let p2 = p1 * (1. + 2. * c) ; fn lambda (a : f64) -> f64 { a * (1. + 0.5 * a) } let lambda_l = lambda ((f_m - x_l) / (f_m - x_l * btpe . p)) ; let lambda_r = lambda ((x_r - f_m) / (x_r * q)) ; let p3 = p2 + c / lambda_l ; let p4 = p3 + c / lambda_r ; let mut y : i64 ; let gen_u = Uniform :: new (0. , p4) . unwrap () ; let gen_v = Uniform :: new (0. , 1.) . unwrap () ; loop { let u = gen_u . sample (rng) ; let mut v = gen_v . sample (rng) ; if ! (u > p1) { y = f64_to_i64 (x_m - p1 * v + u) ; break ; } if ! (u > p2) { let x = x_l + (u - p1) / c ; v = v * c + 1.0 - (x - x_m) . abs () / p1 ; if v > 1. { continue ; } else { y = f64_to_i64 (x) ; } } else if ! (u > p3) { y = f64_to_i64 (x_l + v . ln () / lambda_l) ; if y < 0 { continue ; } else { v *= (u - p2) * lambda_l ; } } else { y = f64_to_i64 (x_r - v . ln () / lambda_r) ; if y > 0 && (y as u64) > btpe . n { continue ; } else { v *= (u - p3) * lambda_r ; } } let k = (y - m) . abs () ; if ! (k > SQUEEZE_THRESHOLD && (k as f64) < 0.5 * npq - 1.) { let s = btpe . p / q ; let a = s * (n + 1.) ; let mut f = 1.0 ; match m . cmp (& y) { Ordering :: Less => { let mut i = m ; loop { i += 1 ; f *= a / (i as f64) - s ; if i == y { break ; } } } Ordering :: Greater => { let mut i = y ; loop { i += 1 ; f /= a / (i as f64) - s ; if i == m { break ; } } } Ordering :: Equal => { } } if v > f { continue ; } else { break ; } } let k = k as f64 ; let rho = (k / npq) * ((k * (k / 3. + 0.625) + 1. / 6.) / npq + 0.5) ; let t = - 0.5 * k * k / npq ; let alpha = v . ln () ; if alpha < t - rho { break ; } if alpha > t + rho { continue ; } let x1 = (y + 1) as f64 ; let f1 = (m + 1) as f64 ; let z = (f64_to_i64 (n) + 1 - m) as f64 ; let w = (f64_to_i64 (n) - y + 1) as f64 ; fn stirling (a : f64) -> f64 { let a2 = a * a ; (13860. - (462. - (132. - (99. - 140. / a2) / a2) / a2) / a2) / a / 166320. } if alpha > x_m * (f1 / x1) . ln () + (n - (m as f64) + 0.5) * (z / w) . ln () + ((y - m) as f64) * (w * btpe . p / (x1 * q)) . ln () + stirling (f1) + stirling (z) - stirling (x1) - stirling (w) { continue ; } break ; } assert ! (y >= 0) ; let y = y as u64 ; if flipped { btpe . n - y } else { y } }
};
}
