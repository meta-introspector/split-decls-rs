// Generated macro for Block (struct)
macro_rules! Depcrate_blockBlock {
() => {
// Module: crate::block
// Provides: {"Block"}
// Dependencies: {}
# [doc = " An opaque type that holds an Objective-C block."] # [doc = ""] # [doc = " The generic type `F` must be a [`dyn`] [`Fn`] that implements"] # [doc = " the [`BlockFn`] trait (which means parameter and return types must be"] # [doc = " \"encodable\"), and describes the parameter and return types of the block."] # [doc = ""] # [doc = " For example, you may have the type `Block<dyn Fn(u8, u8) -> i32>`, and"] # [doc = " that would be a `'static` block that takes two `u8`s, and returns an"] # [doc = " `i32`."] # [doc = ""] # [doc = " If you want the block to carry a lifetime, use `Block<dyn Fn() + 'a>`,"] # [doc = " just like you'd usually do with `dyn Fn`."] # [doc = ""] # [doc = " [`dyn`]: https://doc.rust-lang.org/std/keyword.dyn.html"] # [doc = ""] # [doc = ""] # [doc = " # Memory layout"] # [doc = ""] # [doc = " This is intended to be an `extern type`, and as such the memory layout of"] # [doc = " this type is _not_ guaranteed. That said, **pointers** to this type are"] # [doc = " always thin, and match that of Objective-C blocks. So the layout of e.g."] # [doc = " `&Block<dyn Fn(...) -> ... + '_>` is defined, and guaranteed to be"] # [doc = " pointer-sized and ABI-compatible with a block pointer."] # [doc = ""] # [doc = ""] # [doc = " # Safety invariant"] # [doc = ""] # [doc = " Calling this potentially invokes foreign code, so you must verify, when"] # [doc = " creating a reference to this, or returning it from an external API, that"] # [doc = " it doesn't violate any of Rust's safety rules."] # [doc = ""] # [doc = " In particular, blocks are sharable with multiple references (see e.g."] # [doc = " [`Block::copy`]), so the caller must ensure that calling it can never"] # [doc = " cause a data race. This usually means you'll have to use some form of"] # [doc = " interior mutability, if you need to mutate something from inside a block."] # [repr (C)] pub struct Block < F : ? Sized > { _inner : [u8 ; 0] , # [doc = " We store `BlockHeader` + the closure captures, but `Block` has to"] # [doc = " remain an empty type because we don't know the size of the closure,"] # [doc = " and otherwise the compiler would think we only have provenance over"] # [doc = " `BlockHeader`."] # [doc = ""] # [doc = " This is possible to improve once we have extern types."] _header : PhantomData < BlockHeader > , _p : PhantomData < F > , }
};
}
