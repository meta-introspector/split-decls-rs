// Generated macro for Config (struct)
macro_rules! Depcrate_commonConfig {
() => {
// Module: crate::common
// Provides: {"Config"}
// Dependencies: {}
# [doc = " Configuration for `compiletest` *per invocation*."] # [doc = ""] # [doc = " In terms of `bootstrap`, this means that `./x test tests/ui tests/run-make` actually correspond"] # [doc = " to *two* separate invocations of `compiletest`."] # [doc = ""] # [doc = " FIXME: this `Config` struct should be broken up into smaller logically contained sub-config"] # [doc = " structs, it's too much of a \"soup\" of everything at the moment."] # [doc = ""] # [doc = " # Configuration sources"] # [doc = ""] # [doc = " Configuration values for `compiletest` comes from several sources:"] # [doc = ""] # [doc = " - CLI args passed from `bootstrap` while running the `compiletest` binary."] # [doc = " - Env vars."] # [doc = " - Discovery (e.g. trying to identify a suitable debugger based on filesystem discovery)."] # [doc = " - Cached output of running the `rustc` under test (e.g. output of `rustc` print requests)."] # [doc = ""] # [doc = " FIXME: make sure we *clearly* account for sources of *all* config options."] # [doc = ""] # [doc = " FIXME: audit these options to make sure we are not hashing less than necessary for build stamp"] # [doc = " (for changed test detection)."] # [derive (Debug , Clone)] pub struct Config { # [doc = " Some [`TestMode`]s support [snapshot testing], where a *reference snapshot* of outputs (of"] # [doc = " `stdout`, `stderr`, or other form of artifacts) can be compared to the *actual output*."] # [doc = ""] # [doc = " This option can be set to `true` to update the *reference snapshots* in-place, otherwise"] # [doc = " `compiletest` will only try to compare."] # [doc = ""] # [doc = " [snapshot testing]: https://jestjs.io/docs/snapshot-testing"] pub bless : bool , # [doc = " Attempt to stop as soon as possible after any test fails. We may still run a few more tests"] # [doc = " before stopping when multiple test threads are used."] pub fail_fast : bool , # [doc = " Path to libraries needed to run the *staged* `rustc`-under-test on the **host** platform."] # [doc = ""] # [doc = " FIXME: maybe rename this to reflect (1) which target platform (host, not target), and (2)"] # [doc = " which `rustc` (the `rustc`-under-test, not the stage 0 `rustc` unless forced)."] pub compile_lib_path : Utf8PathBuf , # [doc = " Path to libraries needed to run the compiled executable for the **target** platform. This"] # [doc = " corresponds to the **target** sysroot libraries, including the **target** standard library."] # [doc = ""] # [doc = " FIXME: maybe rename this to reflect (1) which target platform (target, not host), and (2)"] # [doc = " what \"run libraries\" are against."] # [doc = ""] # [doc = " FIXME: this is very under-documented in conjunction with the `remote-test-client` scheme and"] # [doc = " `RUNNER` scheme to actually run the target executable under the target platform environment,"] # [doc = " cf. [`Self::remote_test_client`] and [`Self::runner`]."] pub run_lib_path : Utf8PathBuf , # [doc = " Path to the *staged*  `rustc`-under-test. Unless forced, this `rustc` is *staged*, and must"] # [doc = " not be confused with [`Self::stage0_rustc_path`]."] # [doc = ""] # [doc = " FIXME: maybe rename this to reflect that this is the `rustc`-under-test."] pub rustc_path : Utf8PathBuf , # [doc = " Path to a *staged* **host** platform cargo executable (unless stage 0 is forced). This"] # [doc = " staged `cargo` is only used within `run-make` test recipes during recipe run time (and is"] # [doc = " *not* used to compile the test recipes), and so must be staged as there may be differences"] # [doc = " between e.g. beta `cargo` vs in-tree `cargo`."] # [doc = ""] # [doc = " FIXME: maybe rename this to reflect that this is a *staged* host cargo."] pub cargo_path : Option < Utf8PathBuf > , # [doc = " Path to the stage 0 `rustc` used to build `run-make` recipes. This must not be confused with"] # [doc = " [`Self::rustc_path`]."] pub stage0_rustc_path : Option < Utf8PathBuf > , # [doc = " Path to the stage 1 or higher `rustc` used to obtain target information via"] # [doc = " `--print=all-target-specs-json` and similar queries."] # [doc = ""] # [doc = " Normally this is unset, because [`Self::rustc_path`] can be used instead."] # [doc = " But when running \"stage 1\" ui-fulldeps tests, `rustc_path` is a stage 0"] # [doc = " compiler, whereas target specs must be obtained from a stage 1+ compiler"] # [doc = " (in case the JSON format has changed since the last bootstrap bump)."] pub query_rustc_path : Option < Utf8PathBuf > , # [doc = " Path to the `rustdoc`-under-test. Like [`Self::rustc_path`], this `rustdoc` is *staged*."] pub rustdoc_path : Option < Utf8PathBuf > , # [doc = " Path to the `src/tools/coverage-dump/` bootstrap tool executable."] pub coverage_dump_path : Option < Utf8PathBuf > , # [doc = " Path to the Python 3 executable to use for LLDB and htmldocck."] # [doc = ""] # [doc = " FIXME: the `lldb` setup currently requires I believe Python 3.10 **exactly**, it can't even"] # [doc = " be Python 3.11 or 3.9..."] pub python : String , # [doc = " Path to the `src/tools/jsondocck/` bootstrap tool executable."] pub jsondocck_path : Option < String > , # [doc = " Path to the `src/tools/jsondoclint/` bootstrap tool executable."] pub jsondoclint_path : Option < String > , # [doc = " Path to a host LLVM `FileCheck` executable."] pub llvm_filecheck : Option < Utf8PathBuf > , # [doc = " Path to a host LLVM bintools directory."] pub llvm_bin_dir : Option < Utf8PathBuf > , # [doc = " The path to the **target** `clang` executable to run `clang`-based tests with. If `None`,"] # [doc = " then these tests will be ignored."] pub run_clang_based_tests_with : Option < String > , # [doc = " Path to the directory containing the sources. This corresponds to the root folder of a"] # [doc = " `rust-lang/rust` checkout."] # [doc = ""] # [doc = " FIXME: this name is confusing, because this is actually `$checkout_root`, **not** the"] # [doc = " `$checkout_root/src/` folder."] pub src_root : Utf8PathBuf , # [doc = " Path to the directory containing the test suites sources. This corresponds to the"] # [doc = " `$src_root/tests/` folder."] # [doc = ""] # [doc = " Must be an immediate subdirectory of [`Self::src_root`]."] # [doc = ""] # [doc = " FIXME: this name is also confusing, maybe just call it `tests_root`."] pub src_test_suite_root : Utf8PathBuf , # [doc = " Path to the build directory (e.g. `build/`)."] pub build_root : Utf8PathBuf , # [doc = " Path to the test suite specific build directory (e.g. `build/host/test/ui/`)."] # [doc = ""] # [doc = " Must be a subdirectory of [`Self::build_root`]."] pub build_test_suite_root : Utf8PathBuf , # [doc = " Path to the directory containing the sysroot of the `rustc`-under-test."] # [doc = ""] # [doc = " When stage 0 is forced, this will correspond to the sysroot *of* that specified stage 0"] # [doc = " `rustc`."] # [doc = ""] # [doc = " FIXME: this name is confusing, because it doesn't specify *which* compiler this sysroot"] # [doc = " corresponds to. It's actually the `rustc`-under-test, and not the bootstrap `rustc`, unless"] # [doc = " stage 0 is forced and no custom stage 0 `rustc` was otherwise specified (so that it"] # [doc = " *happens* to run against the bootstrap `rustc`, but this non-custom bootstrap `rustc` case"] # [doc = " is not really supported)."] pub sysroot_base : Utf8PathBuf , # [doc = " The number of the stage under test."] pub stage : u32 , # [doc = " The id of the stage under test (stage1-xxx, etc)."] # [doc = ""] # [doc = " FIXME: reconsider this string; this is hashed for test build stamp."] pub stage_id : String , # [doc = " The [`TestMode`]. E.g. [`TestMode::Ui`]. Each test mode can correspond to one or more test"] # [doc = " suites."] # [doc = ""] # [doc = " FIXME: stop using stringly-typed test suites!"] pub mode : TestMode , # [doc = " The test suite."] # [doc = ""] # [doc = " Example: `tests/ui/` is [`TestSuite::Ui`] test *suite*, which happens to also be of the"] # [doc = " [`TestMode::Ui`] test *mode*."] # [doc = ""] # [doc = " Note that the same test suite (e.g. `tests/coverage/`) may correspond to multiple test"] # [doc = " modes, e.g. `tests/coverage/` can be run under both [`TestMode::CoverageRun`] and"] # [doc = " [`TestMode::CoverageMap`]."] pub suite : TestSuite , # [doc = " When specified, **only** the specified [`Debugger`] will be used to run against the"] # [doc = " `tests/debuginfo` test suite. When unspecified, `compiletest` will attempt to find all three"] # [doc = " of {`lldb`, `cdb`, `gdb`} implicitly, and then try to run the `debuginfo` test suite against"] # [doc = " all three debuggers."] # [doc = ""] # [doc = " FIXME: this implicit behavior is really nasty, in that it makes it hard for the user to"] # [doc = " control *which* debugger(s) are available and used to run the debuginfo test suite. We"] # [doc = " should have `bootstrap` allow the user to *explicitly* configure the debuggers, and *not*"] # [doc = " try to implicitly discover some random debugger from the user environment. This makes the"] # [doc = " debuginfo test suite particularly hard to work with."] pub debugger : Option < Debugger > , # [doc = " Run ignored tests *unconditionally*, overriding their ignore reason."] # [doc = ""] # [doc = " FIXME: this is wired up through the test execution logic, but **not** accessible from"] # [doc = " `bootstrap` directly; `compiletest` exposes this as `--ignored`. I.e. you'd have to use `./x"] # [doc = " test $test_suite -- --ignored=true`."] pub run_ignored : bool , # [doc = " Whether *staged* `rustc`-under-test was built with debug assertions."] # [doc = ""] # [doc = " FIXME: make it clearer that this refers to the staged `rustc`-under-test, not stage 0"] # [doc = " `rustc`."] pub with_rustc_debug_assertions : bool , # [doc = " Whether *staged* `std` was built with debug assertions."] # [doc = ""] # [doc = " FIXME: make it clearer that this refers to the staged `std`, not stage 0 `std`."] pub with_std_debug_assertions : bool , # [doc = " Only run tests that match these filters (using `libtest` \"test name contains\" filter logic)."] # [doc = ""] # [doc = " FIXME(#139660): the current hand-rolled test executor intentionally mimics the `libtest`"] # [doc = " \"test name contains\" filter matching logic to preserve previous `libtest` executor behavior,"] # [doc = " but this is often not intuitive. We should consider changing that behavior with an MCP to do"] # [doc = " test path *prefix* matching which better corresponds to how `compiletest` `tests/` are"] # [doc = " organized, and how users would intuitively expect the filtering logic to work like."] pub filters : Vec < String > , # [doc = " Skip tests matching these substrings. The matching logic exactly corresponds to"] # [doc = " [`Self::filters`] but inverted."] # [doc = ""] # [doc = " FIXME(#139660): ditto on test matching behavior."] pub skip : Vec < String > , # [doc = " Exactly match the filter, rather than a substring."] # [doc = ""] # [doc = " FIXME(#139660): ditto on test matching behavior."] pub filter_exact : bool , # [doc = " Force the pass mode of a check/build/run test to instead use this mode instead."] # [doc = ""] # [doc = " FIXME: make it even more obvious (especially in PR CI where `--pass=check` is used) when a"] # [doc = " pass mode is forced when the test fails, because it can be very non-obvious when e.g. an"] # [doc = " error is emitted only when `//@ build-pass` but not `//@ check-pass`."] pub force_pass_mode : Option < PassMode > , # [doc = " Explicitly enable or disable running of the target test binary."] # [doc = ""] # [doc = " FIXME: this scheme is a bit confusing, and at times questionable. Re-evaluate this run"] # [doc = " scheme."] # [doc = ""] # [doc = " FIXME: Currently `--run` is a tri-state, it can be `--run={auto,always,never}`, and when"] # [doc = " `--run=auto` is specified, it's run if the platform doesn't end with `-fuchsia`. See"] # [doc = " [`Config::run_enabled`]."] pub run : Option < bool > , # [doc = " A command line to prefix target program execution with, for running under valgrind for"] # [doc = " example, i.e. `$runner target.exe [args..]`. Similar to `CARGO_*_RUNNER` configuration."] # [doc = ""] # [doc = " Note: this is not to be confused with [`Self::remote_test_client`], which is a different"] # [doc = " scheme."] # [doc = ""] # [doc = " FIXME: the runner scheme is very under-documented."] pub runner : Option < String > , # [doc = " Compiler flags to pass to the *staged* `rustc`-under-test when building for the **host**"] # [doc = " platform."] pub host_rustcflags : Vec < String > , # [doc = " Compiler flags to pass to the *staged* `rustc`-under-test when building for the **target**"] # [doc = " platform."] pub target_rustcflags : Vec < String > , # [doc = " Whether the *staged* `rustc`-under-test and the associated *staged* `std` has been built"] # [doc = " with randomized struct layouts."] pub rust_randomized_layout : bool , # [doc = " Whether tests should be optimized by default (`-O`). Individual test suites and test files"] # [doc = " may override this setting."] # [doc = ""] # [doc = " FIXME: this flag / config option is somewhat misleading. For instance, in ui tests, it's"] # [doc = " *only* applied to the [`PassMode::Run`] test crate and not its auxiliaries."] pub optimize_tests : bool , # [doc = " Target platform tuple."] pub target : String , # [doc = " Host platform tuple."] pub host : String , # [doc = " Path to / name of the Microsoft Console Debugger (CDB) executable."] # [doc = ""] # [doc = " FIXME: this is an *opt-in* \"override\" option. When this isn't provided, we try to conjure a"] # [doc = " cdb by looking at the user's program files on Windows... See `debuggers::find_cdb`."] pub cdb : Option < Utf8PathBuf > , # [doc = " Version of CDB."] # [doc = ""] # [doc = " FIXME: `cdb_version` is *derived* from cdb, but it's *not* technically a config!"] # [doc = ""] # [doc = " FIXME: audit cdb version gating."] pub cdb_version : Option < [u16 ; 4] > , # [doc = " Path to / name of the GDB executable."] # [doc = ""] # [doc = " FIXME: the fallback path when `gdb` isn't provided tries to find *a* `gdb` or `gdb.exe` from"] # [doc = " `PATH`, which is... arguably questionable."] # [doc = ""] # [doc = " FIXME: we are propagating a python from `PYTHONPATH`, not from an explicit config for gdb"] # [doc = " debugger script."] pub gdb : Option < String > , # [doc = " Version of GDB, encoded as ((major * 1000) + minor) * 1000 + patch"] # [doc = ""] # [doc = " FIXME: this gdb version gating scheme is possibly questionable -- gdb does not use semver,"] # [doc = " only its major version is likely materially meaningful, cf."] # [doc = " <https://sourceware.org/gdb/wiki/Internals%20Versions>. Even the major version I'm not sure"] # [doc = " is super meaningful. Maybe min gdb `major.minor` version gating is sufficient for the"] # [doc = " purposes of debuginfo tests?"] # [doc = ""] # [doc = " FIXME: `gdb_version` is *derived* from gdb, but it's *not* technically a config!"] pub gdb_version : Option < u32 > , # [doc = " Version of LLDB."] # [doc = ""] # [doc = " FIXME: `lldb_version` is *derived* from lldb, but it's *not* technically a config!"] pub lldb_version : Option < u32 > , # [doc = " Version of LLVM."] # [doc = ""] # [doc = " FIXME: Audit the fallback derivation of"] # [doc = " [`crate::directives::extract_llvm_version_from_binary`], that seems very questionable?"] pub llvm_version : Option < Version > , # [doc = " Is LLVM a system LLVM."] pub system_llvm : bool , # [doc = " Path to the android tools."] # [doc = ""] # [doc = " Note: this is only used for android gdb debugger script in the debuginfo test suite."] # [doc = ""] # [doc = " FIXME: take a look at this; this is piggy-backing off of gdb code paths but only for"] # [doc = " `arm-linux-androideabi` target."] pub android_cross_path : Utf8PathBuf , # [doc = " Extra parameter to run adb on `arm-linux-androideabi`."] # [doc = ""] # [doc = " FIXME: is this *only* `arm-linux-androideabi`, or is it also for other Tier 2/3 android"] # [doc = " targets?"] # [doc = ""] # [doc = " FIXME: take a look at this; this is piggy-backing off of gdb code paths but only for"] # [doc = " `arm-linux-androideabi` target."] pub adb_path : String , # [doc = " Extra parameter to run test suite on `arm-linux-androideabi`."] # [doc = ""] # [doc = " FIXME: is this *only* `arm-linux-androideabi`, or is it also for other Tier 2/3 android"] # [doc = " targets?"] # [doc = ""] # [doc = " FIXME: take a look at this; this is piggy-backing off of gdb code paths but only for"] # [doc = " `arm-linux-androideabi` target."] pub adb_test_dir : String , # [doc = " Status whether android device available or not. When unavailable, this will cause tests to"] # [doc = " panic when the test binary is attempted to be run."] # [doc = ""] # [doc = " FIXME: take a look at this; this also influences adb in gdb code paths in a strange way."] pub adb_device_status : bool , # [doc = " Path containing LLDB's Python module."] # [doc = ""] # [doc = " FIXME: `PYTHONPATH` takes precedence over this flag...? See `runtest::run_lldb`."] pub lldb_python_dir : Option < String > , # [doc = " Verbose dump a lot of info."] # [doc = ""] # [doc = " FIXME: this is *way* too coarse; the user can't select *which* info to verbosely dump."] pub verbose : bool , # [doc = " Whether to use colors in test output."] # [doc = ""] # [doc = " Note: the exact control mechanism is delegated to [`colored`]."] pub color : ColorConfig , # [doc = " Where to find the remote test client process, if we're using it."] # [doc = ""] # [doc = " Note: this is *only* used for target platform executables created by `run-make` test"] # [doc = " recipes."] # [doc = ""] # [doc = " Note: this is not to be confused with [`Self::runner`], which is a different scheme."] # [doc = ""] # [doc = " FIXME: the `remote_test_client` scheme is very under-documented."] pub remote_test_client : Option < Utf8PathBuf > , # [doc = " [`CompareMode`] describing what file the actual ui output will be compared to."] # [doc = ""] # [doc = " FIXME: currently, [`CompareMode`] is a mishmash of lot of things (different borrow-checker"] # [doc = " model, different trait solver, different debugger, etc.)."] pub compare_mode : Option < CompareMode > , # [doc = " If true, this will generate a coverage file with UI test files that run `MachineApplicable`"] # [doc = " diagnostics but are missing `run-rustfix` annotations. The generated coverage file is"] # [doc = " created in `$test_suite_build_root/rustfix_missing_coverage.txt`"] pub rustfix_coverage : bool , # [doc = " Whether to run `tidy` (html-tidy) when a rustdoc test fails."] pub has_html_tidy : bool , # [doc = " Whether to run `enzyme` autodiff tests."] pub has_enzyme : bool , # [doc = " The current Rust channel info."] # [doc = ""] # [doc = " FIXME: treat this more carefully; \"stable\", \"beta\" and \"nightly\" are definitely valid, but"] # [doc = " channel might also be \"dev\" or such, which should be treated as \"nightly\"."] pub channel : String , # [doc = " Whether adding git commit information such as the commit hash has been enabled for building."] # [doc = ""] # [doc = " FIXME: `compiletest` cannot trust `bootstrap` for this information, because `bootstrap` can"] # [doc = " have bugs and had bugs on that logic. We need to figure out how to obtain this e.g. directly"] # [doc = " from CI or via git locally."] pub git_hash : bool , # [doc = " The default Rust edition."] # [doc = ""] # [doc = " FIXME: perform stronger validation for this. There are editions that *definitely* exists,"] # [doc = " but there might also be \"future\" edition."] pub edition : Option < String > , pub cc : String , pub cxx : String , pub cflags : String , pub cxxflags : String , pub ar : String , pub target_linker : Option < String > , pub host_linker : Option < String > , pub llvm_components : String , # [doc = " Path to a NodeJS executable. Used for JS doctests, emscripten and WASM tests."] pub nodejs : Option < String > , # [doc = " Path to a npm executable. Used for rustdoc GUI tests."] pub npm : Option < String > , # [doc = " Whether to rerun tests even if the inputs are unchanged."] pub force_rerun : bool , # [doc = " Only rerun the tests that result has been modified according to `git status`."] # [doc = ""] # [doc = " FIXME: this is undocumented."] # [doc = ""] # [doc = " FIXME: how does this interact with [`Self::force_rerun`]?"] pub only_modified : bool , pub target_cfgs : OnceLock < TargetCfgs > , pub builtin_cfg_names : OnceLock < HashSet < String > > , pub supported_crate_types : OnceLock < HashSet < String > > , # [doc = " FIXME: this is why we still need to depend on *staged* `std`, it's because we currently rely"] # [doc = " on `#![feature(internal_output_capture)]` for [`std::io::set_output_capture`] to implement"] # [doc = " `libtest`-esque `--no-capture`."] # [doc = ""] # [doc = " FIXME: rename this to the more canonical `no_capture`, or better, invert this to `capture`"] # [doc = " to avoid `!nocapture` double-negatives."] pub nocapture : bool , # [doc = " True if the experimental new output-capture implementation should be"] # [doc = " used, avoiding the need for `#![feature(internal_output_capture)]`."] pub new_output_capture : bool , # [doc = " Needed both to construct [`build_helper::git::GitConfig`]."] pub nightly_branch : String , pub git_merge_commit_email : String , # [doc = " True if the profiler runtime is enabled for this target. Used by the"] # [doc = " `needs-profiler-runtime` directive in test files."] pub profiler_runtime : bool , # [doc = " Command for visual diff display, e.g. `diff-tool --color=always`."] pub diff_command : Option < String > , # [doc = " Path to minicore aux library (`tests/auxiliary/minicore.rs`), used for `no_core` tests that"] # [doc = " need `core` stubs in cross-compilation scenarios that do not otherwise want/need to"] # [doc = " `-Zbuild-std`. Used in e.g. ABI tests."] pub minicore_path : Utf8PathBuf , # [doc = " Current codegen backend used."] pub default_codegen_backend : CodegenBackend , # [doc = " Name/path of the backend to use instead of `default_codegen_backend`."] pub override_codegen_backend : Option < String > , }
};
}
