macro_rules! Background {
    () => {
        # [doc = " The background color used for zebra-striping the table."] # [derive (Clone , Copy)] enum Background { Light , Dark , }
    };
}

Background!();