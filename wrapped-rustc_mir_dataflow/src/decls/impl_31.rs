macro_rules! deps {
    () => {
        Results!();
        Effect!();
        Analysis!();
        Direction!();
        EffectIndex!();
        CowMut!();
        CursorPosition!();
        ResultsCursor!();
    };
}

macro_rules! impl_31 {
    () => {
        deps!();
        impl < 'mir , 'tcx , A > ResultsCursor < 'mir , 'tcx , A > where A : Analysis < 'tcx > , { # [doc = " Returns the dataflow state at the current location."] pub fn get (& self) -> & A :: Domain { & self . state } # [doc = " Returns the body this analysis was run on."] pub fn body (& self) -> & 'mir mir :: Body < 'tcx > { self . body } fn new (body : & 'mir mir :: Body < 'tcx > , analysis : CowMut < 'mir , A > , results : Cow < 'mir , Results < A :: Domain > > ,) -> Self { let bottom_value = analysis . bottom_value (body) ; ResultsCursor { body , analysis , results , state_needs_reset : true , state : bottom_value , pos : CursorPosition :: block_entry (mir :: START_BLOCK) , # [cfg (debug_assertions)] reachable_blocks : mir :: traversal :: reachable_as_bitset (body) , } } # [doc = " Returns a new cursor that takes ownership of and inspects analysis results."] pub fn new_owning (body : & 'mir mir :: Body < 'tcx > , analysis : A , results : Results < A :: Domain > ,) -> Self { Self :: new (body , CowMut :: Owned (analysis) , Cow :: Owned (results)) } # [doc = " Returns a new cursor that borrows and inspects analysis results."] pub fn new_borrowing (body : & 'mir mir :: Body < 'tcx > , analysis : & 'mir mut A , results : & 'mir Results < A :: Domain > ,) -> Self { Self :: new (body , CowMut :: BorrowedMut (analysis) , Cow :: Borrowed (results)) } # [doc = " Allows inspection of unreachable basic blocks even with `debug_assertions` enabled."] # [cfg (test)] pub (crate) fn allow_unreachable (& mut self) { # [cfg (debug_assertions)] self . reachable_blocks . insert_all () } # [doc = " Returns the `Analysis` used to generate the underlying `Results`."] pub fn analysis (& self) -> & A { & self . analysis } # [doc = " Resets the cursor to hold the entry set for the given basic block."] # [doc = ""] # [doc = " For forward dataflow analyses, this is the dataflow state prior to the first statement."] # [doc = ""] # [doc = " For backward dataflow analyses, this is the dataflow state after the terminator."] pub (super) fn seek_to_block_entry (& mut self , block : BasicBlock) { # [cfg (debug_assertions)] assert ! (self . reachable_blocks . contains (block)) ; self . state . clone_from (& self . results [block]) ; self . pos = CursorPosition :: block_entry (block) ; self . state_needs_reset = false ; } # [doc = " Resets the cursor to hold the state prior to the first statement in a basic block."] # [doc = ""] # [doc = " For forward analyses, this is the entry set for the given block."] # [doc = ""] # [doc = " For backward analyses, this is the state that will be propagated to its"] # [doc = " predecessors (ignoring edge-specific effects)."] pub fn seek_to_block_start (& mut self , block : BasicBlock) { if A :: Direction :: IS_FORWARD { self . seek_to_block_entry (block) } else { self . seek_after (Location { block , statement_index : 0 } , Effect :: Primary) } } # [doc = " Resets the cursor to hold the state after the terminator in a basic block."] # [doc = ""] # [doc = " For backward analyses, this is the entry set for the given block."] # [doc = ""] # [doc = " For forward analyses, this is the state that will be propagated to its"] # [doc = " successors (ignoring edge-specific effects)."] pub fn seek_to_block_end (& mut self , block : BasicBlock) { if A :: Direction :: IS_BACKWARD { self . seek_to_block_entry (block) } else { self . seek_after (self . body . terminator_loc (block) , Effect :: Primary) } } # [doc = " Advances the cursor to hold the dataflow state at `target` before its \"primary\" effect is"] # [doc = " applied."] # [doc = ""] # [doc = " The \"early\" effect at the target location *will be* applied."] pub fn seek_before_primary_effect (& mut self , target : Location) { self . seek_after (target , Effect :: Early) } # [doc = " Advances the cursor to hold the dataflow state at `target` after its \"primary\" effect is"] # [doc = " applied."] # [doc = ""] # [doc = " The \"early\" effect at the target location will be applied as well."] pub fn seek_after_primary_effect (& mut self , target : Location) { self . seek_after (target , Effect :: Primary) } fn seek_after (& mut self , target : Location , effect : Effect) { assert ! (target <= self . body . terminator_loc (target . block)) ; if self . state_needs_reset || self . pos . block != target . block { self . seek_to_block_entry (target . block) ; } else if let Some (curr_effect) = self . pos . curr_effect_index { let mut ord = curr_effect . statement_index . cmp (& target . statement_index) ; if A :: Direction :: IS_BACKWARD { ord = ord . reverse () } match ord . then_with (| | curr_effect . effect . cmp (& effect)) { Ordering :: Equal => return , Ordering :: Greater => self . seek_to_block_entry (target . block) , Ordering :: Less => { } } } debug_assert_eq ! (target . block , self . pos . block) ; let block_data = & self . body [target . block] ; # [rustfmt :: skip] let next_effect = if A :: Direction :: IS_FORWARD { self . pos . curr_effect_index . map_or_else (| | Effect :: Early . at_index (0) , EffectIndex :: next_in_forward_order ,) } else { self . pos . curr_effect_index . map_or_else (| | Effect :: Early . at_index (block_data . statements . len ()) , EffectIndex :: next_in_backward_order ,) } ; let target_effect_index = effect . at_index (target . statement_index) ; A :: Direction :: apply_effects_in_range (& mut * self . analysis , & mut self . state , target . block , block_data , next_effect ..= target_effect_index ,) ; self . pos = CursorPosition { block : target . block , curr_effect_index : Some (target_effect_index) } ; } # [doc = " Applies `f` to the cursor's internal state."] # [doc = ""] # [doc = " This can be used, e.g., to apply the call return effect directly to the cursor without"] # [doc = " creating an extra copy of the dataflow state."] pub fn apply_custom_effect (& mut self , f : impl FnOnce (& mut A , & mut A :: Domain)) { f (& mut self . analysis , & mut self . state) ; self . state_needs_reset = true ; } }
    };
}

impl_31!()