// Generated macro for SmartDisplay (trait)
macro_rules! Depcrate_smart_displaySmartDisplay {
() => {
// Module: crate::smart_display
// Provides: {"SmartDisplay"}
// Dependencies: {}
# [doc = " Format trait that allows authors to provide additional information."] # [doc = ""] # [doc = " This trait is similar to [`Display`], but allows the author to provide additional information"] # [doc = " to the formatter. This information is provided in the form of a custom metadata type."] # [doc = ""] # [doc = " The only required piece of metadata is the width of the value. This is _before_ it is passed to"] # [doc = " the formatter (i.e. it does not include any padding added by the formatter). Other information"] # [doc = " can be stored in a custom metadata type as needed. This information may be made available to"] # [doc = " downstream users, but it is not required."] # [doc = ""] # [doc = " **Note**: While both `fmt_with_metadata` and `fmt` have default implementations, it is strongly"] # [doc = " recommended to implement only `fmt_with_metadata`. `fmt` should be implemented if and only if"] # [doc = " the type does not require any of the calculated metadata. In that situation, `fmt_with_metadata`"] # [doc = " should be omitted."] # [cfg_attr (__powerfmt_docs , rustc_must_implement_one_of (fmt , fmt_with_metadata))] pub trait SmartDisplay : Display { # [doc = " User-provided metadata type."] type Metadata ; # [doc = " Compute any information needed to format the value. This must, at a minimum, determine the"] # [doc = " width of the value before any padding is added by the formatter."] # [doc = ""] # [doc = " If the type uses other types that implement `SmartDisplay` verbatim, the inner types should"] # [doc = " have their metadata calculated and included in the returned value."] # [doc = ""] # [doc = " # Lifetimes"] # [doc = ""] # [doc = " This method's return type contains a lifetime to `self`. This ensures that the metadata will"] # [doc = " neither outlive the value nor be invalidated by a mutation of the value (barring interior"] # [doc = " mutability)."] # [doc = ""] # [doc = " ```rust,compile_fail"] # [doc = " # use std::fmt;"] # [doc = " # use std::fmt::Write;"] # [doc = " # use powerfmt::buf::WriteBuffer;"] # [doc = " # use powerfmt::smart_display::{self, FormatterOptions, Metadata, SmartDisplay};"] # [doc = " #[derive(Debug)]"] # [doc = " struct WrappedBuffer(WriteBuffer<128>);"] # [doc = ""] # [doc = " #[smart_display::delegate]"] # [doc = " impl SmartDisplay for WrappedBuffer {"] # [doc = "     type Metadata = ();"] # [doc = ""] # [doc = "     fn metadata(&self, _: FormatterOptions) -> Metadata<'_, Self> {"] # [doc = "         Metadata::new(self.0.len(), self, ())"] # [doc = "     }"] # [doc = ""] # [doc = "     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {"] # [doc = "         f.pad(self.0.as_str())"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " let mut buf = WrappedBuffer(WriteBuffer::new());"] # [doc = " let metadata = buf.metadata(FormatterOptions::default());"] # [doc = " // We cannot mutate the buffer while it is borrowed and use its previous metadata on the"] # [doc = " // following line."] # [doc = " write!(buf.0, \"Hello, world!\")?;"] # [doc = " assert_eq!(metadata.width(), 13);"] # [doc = " # Ok::<(), Box<dyn std::error::Error>>(())"] # [doc = " ```"] fn metadata (& self , f : FormatterOptions) -> Metadata < '_ , Self > ; # [doc = " Format the value using the given formatter and metadata. The formatted output should have"] # [doc = " the width indicated by the metadata. This is before any padding is added by the"] # [doc = " formatter."] # [doc = ""] # [doc = " If the metadata is not needed, you should implement the `fmt` method instead."] fn fmt_with_metadata (& self , f : & mut Formatter < '_ > , _metadata : Metadata < '_ , Self >) -> Result { SmartDisplay :: fmt (self , f) } # [doc = " Format the value using the given formatter. This is the same as [`Display::fmt`]."] # [doc = ""] # [doc = " The default implementation of this method calls `fmt_with_metadata` with the result of"] # [doc = " `metadata`. Generally speaking, this method should not be implemented. You should implement"] # [doc = " the `fmt_with_metadata` method instead."] fn fmt (& self , f : & mut Formatter < '_ >) -> Result { let metadata = self . metadata (f . into ()) ; self . fmt_with_metadata (f , metadata) } }
};
}
