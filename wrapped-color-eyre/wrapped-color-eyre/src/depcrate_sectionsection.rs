// Generated macro for Section (trait)
macro_rules! Depcrate_sectionSection {
() => {
// Module: crate::section
// Provides: {"Section"}
// Dependencies: {}
# [doc = " A helper trait for attaching informational sections to error reports to be"] # [doc = " displayed after the chain of errors"] # [doc = ""] # [doc = " # Details"] # [doc = ""] # [doc = " `color_eyre` provides two types of help text that can be attached to error reports: custom"] # [doc = " sections and pre-configured sections. Custom sections are added via the `section` and"] # [doc = " `with_section` methods, and give maximum control over formatting."] # [doc = ""] # [doc = " The pre-configured sections are provided via `suggestion`, `warning`, and `note`. These"] # [doc = " sections are displayed after all other sections with no extra newlines between subsequent Help"] # [doc = " sections. They consist only of a header portion and are prepended with a colored string"] # [doc = " indicating the kind of section, e.g. `Note: This might have failed due to ...\""] pub trait Section : crate :: private :: Sealed { # [doc = " The return type of each method after adding context"] type Return ; # [doc = " Add a section to an error report, to be displayed after the chain of errors."] # [doc = ""] # [doc = " # Details"] # [doc = ""] # [doc = " Sections are displayed in the order they are added to the error report. They are displayed"] # [doc = " immediately after the `Error:` section and before the `SpanTrace` and `Backtrace` sections."] # [doc = " They consist of a header and an optional body. The body of the section is indented by"] # [doc = " default."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust,should_panic"] # [doc = " use color_eyre::{eyre::eyre, eyre::Report, Section};"] # [doc = ""] # [doc = " Err(eyre!(\"command failed\"))"] # [doc = "     .section(\"Please report bugs to https://real.url/bugs\")?;"] # [doc = " # Ok::<_, Report>(())"] # [doc = " ```"] fn section < D > (self , section : D) -> Self :: Return where D : Display + Send + Sync + 'static ; # [doc = " Add a Section to an error report, to be displayed after the chain of errors. The closure to"] # [doc = " create the Section is lazily evaluated only in the case of an error."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " use color_eyre::{eyre::eyre, eyre::Report, Section, SectionExt};"] # [doc = ""] # [doc = " # #[cfg(not(miri))]"] # [doc = " # {"] # [doc = " let output = std::process::Command::new(\"ls\")"] # [doc = "     .output()?;"] # [doc = ""] # [doc = " let output = if !output.status.success() {"] # [doc = "     let stderr = String::from_utf8_lossy(&output.stderr);"] # [doc = "     Err(eyre!(\"cmd exited with non-zero status code\"))"] # [doc = "         .with_section(move || stderr.trim().to_string().header(\"Stderr:\"))?"] # [doc = " } else {"] # [doc = "     String::from_utf8_lossy(&output.stdout)"] # [doc = " };"] # [doc = ""] # [doc = " println!(\"{}\", output);"] # [doc = " # }"] # [doc = " # Ok::<_, Report>(())"] # [doc = " ```"] fn with_section < D , F > (self , section : F) -> Self :: Return where D : Display + Send + Sync + 'static , F : FnOnce () -> D ; # [doc = " Add an error section to an error report, to be displayed after the primary error message"] # [doc = " section."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust,should_panic"] # [doc = " use color_eyre::{eyre::eyre, eyre::Report, Section};"] # [doc = " use thiserror::Error;"] # [doc = ""] # [doc = " #[derive(Debug, Error)]"] # [doc = " #[error(\"{0}\")]"] # [doc = " struct StrError(&'static str);"] # [doc = ""] # [doc = " Err(eyre!(\"command failed\"))"] # [doc = "     .error(StrError(\"got one error\"))"] # [doc = "     .error(StrError(\"got a second error\"))?;"] # [doc = " # Ok::<_, Report>(())"] # [doc = " ```"] fn error < E > (self , error : E) -> Self :: Return where E : std :: error :: Error + Send + Sync + 'static ; # [doc = " Add an error section to an error report, to be displayed after the primary error message"] # [doc = " section. The closure to create the Section is lazily evaluated only in the case of an error."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust,should_panic"] # [doc = " use color_eyre::{eyre::eyre, eyre::Report, Section};"] # [doc = " use thiserror::Error;"] # [doc = ""] # [doc = " #[derive(Debug, Error)]"] # [doc = " #[error(\"{0}\")]"] # [doc = " struct StringError(String);"] # [doc = ""] # [doc = " Err(eyre!(\"command failed\"))"] # [doc = "     .with_error(|| StringError(\"got one error\".into()))"] # [doc = "     .with_error(|| StringError(\"got a second error\".into()))?;"] # [doc = " # Ok::<_, Report>(())"] # [doc = " ```"] fn with_error < E , F > (self , error : F) -> Self :: Return where F : FnOnce () -> E , E : std :: error :: Error + Send + Sync + 'static ; # [doc = " Add a Note to an error report, to be displayed after the chain of errors."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use std::{error::Error, fmt::{self, Display}};"] # [doc = " # use color_eyre::eyre::Result;"] # [doc = " # #[derive(Debug)]"] # [doc = " # struct FakeErr;"] # [doc = " # impl Display for FakeErr {"] # [doc = " #     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {"] # [doc = " #         write!(f, \"FakeErr\")"] # [doc = " #     }"] # [doc = " # }"] # [doc = " # impl std::error::Error for FakeErr {}"] # [doc = " # fn main() -> Result<()> {"] # [doc = " # fn fallible_fn() -> Result<(), FakeErr> {"] # [doc = " #       Ok(())"] # [doc = " # }"] # [doc = " use color_eyre::Section as _;"] # [doc = ""] # [doc = " fallible_fn().note(\"This might have failed due to ...\")?;"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] fn note < D > (self , note : D) -> Self :: Return where D : Display + Send + Sync + 'static ; # [doc = " Add a Note to an error report, to be displayed after the chain of errors. The closure to"] # [doc = " create the Note is lazily evaluated only in the case of an error."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```rust"] # [doc = " # use std::{error::Error, fmt::{self, Display}};"] # [doc = " # use color_eyre::eyre::Result;"] # [doc = " # #[derive(Debug)]"] # [doc = " # struct FakeErr;"] # [doc = " # impl Display for FakeErr {"] # [doc = " #     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {"] # [doc = " #         write!(f, \"FakeErr\")"] # [doc = " #     }"] # [doc = " # }"] # [doc = " # impl std::error::Error for FakeErr {}"] # [doc = " # fn main() -> Result<()> {"] # [doc = " # fn fallible_fn() -> Result<(), FakeErr> {"] # [doc = " #       Ok(())"] # [doc = " # }"] # [doc = " use color_eyre::Section as _;"] # [doc = ""] # [doc = " fallible_fn().with_note(|| {"] # [doc = "         format!(\"This might have failed due to ... It has failed {} times\", 100)"] # [doc = "     })?;"] # [doc = " # Ok(())"] # [doc = " # }"] # [doc = " ```"] fn with_note < D , F > (self , f : F) -> Self :: Return where D : Display + Send + Sync + 'static , F : FnOnce () -> D ; # [doc = " Add a Warning to an error report, to be displayed after the chain of errors."] fn warning < D > (self , warning : D) -> Self :: Return where D : Display + Send + Sync + 'static ; # [doc = " Add a Warning to an error report, to be displayed after the chain of errors. The closure to"] # [doc = " create the Warning is lazily evaluated only in the case of an error."] fn with_warning < D , F > (self , f : F) -> Self :: Return where D : Display + Send + Sync + 'static , F : FnOnce () -> D ; # [doc = " Add a Suggestion to an error report, to be displayed after the chain of errors."] fn suggestion < D > (self , suggestion : D) -> Self :: Return where D : Display + Send + Sync + 'static ; # [doc = " Add a Suggestion to an error report, to be displayed after the chain of errors. The closure"] # [doc = " to create the Suggestion is lazily evaluated only in the case of an error."] fn with_suggestion < D , F > (self , f : F) -> Self :: Return where D : Display + Send + Sync + 'static , F : FnOnce () -> D ; # [doc = " Whether to suppress printing of collected backtrace (if any)."] # [doc = ""] # [doc = " Useful for reporting \"unexceptional\" errors for which a backtrace"] # [doc = " isn't really necessary."] fn suppress_backtrace (self , suppress : bool) -> Self :: Return ; }
};
}
