// Generated macro for impl_144 (impl)
macro_rules! Depcrate_bits32_pagingimpl_144 {
() => {
// Module: crate::bits32::paging
// Provides: {"impl_144"}
// Dependencies: {}
impl PDEntry { # [doc = " Creates a new PDEntry."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = "  * `pt` - The physical address of the page table."] # [doc = "  * `flags`- Additional flags for the entry."] # [doc = ""] # [doc = " # Implementation notes"] # [doc = ""] # [doc = " This doesn't support PSE-36 or PSE-40."] pub fn new (pt : PAddr , flags : PDFlags) -> PDEntry { let mask = if flags . contains (PDFlags :: PS) { ADDRESS_MASK_PSE } else { ADDRESS_MASK } ; let pt_val = pt & mask ; assert ! (pt_val == pt . into ()) ; assert ! (pt % BASE_PAGE_SIZE == 0) ; PDEntry (pt_val | flags . bits) } # [doc = " Retrieves the physical address in this entry."] pub fn address (self) -> PAddr { if self . flags () . contains (PDFlags :: PS) { PAddr :: from (self . 0 & ADDRESS_MASK_PSE) } else { PAddr :: from (self . 0 & ADDRESS_MASK) } } # [doc = " Returns the flags corresponding to this entry."] pub fn flags (self) -> PDFlags { PDFlags :: from_bits_truncate (self . 0) } check_flag ! (doc = "Present; must be 1 to map a 4-MByte page." , is_present , PDFlags :: P) ; check_flag ! (doc = "Read/write; if 0, writes may not be allowed to the 4-MByte page referenced by this entry." , is_writeable , PDFlags :: RW) ; check_flag ! (doc = "User/supervisor; if 0, user-mode accesses are not allowed to the 4-MByte page referenced by this entry." , is_user_mode_allowed , PDFlags :: US) ; check_flag ! (doc = "Page-level write-through." , is_page_write_through , PDFlags :: PWT) ; check_flag ! (doc = "Page-level cache disable." , is_page_level_cache_disabled , PDFlags :: PCD) ; check_flag ! (doc = "Accessed; indicates whether software has accessed the 4-MByte page referenced by this entry." , is_accessed , PDFlags :: A) ; check_flag ! (doc = "Dirty; indicates whether software has written to the 4-MByte page referenced by this entry." , is_dirty , PDFlags :: D) ; check_flag ! (doc = "Page size; if set this entry maps a 4-MByte page; otherwise, this entry references a page directory." , is_page , PDFlags :: PS) ; check_flag ! (doc = "Global; if CR4.PGE = 1, determines whether the translation is global; ignored otherwise." , is_global , PDFlags :: G) ; check_flag ! (doc = "If the PAT is supported, indirectly determines the memory type used to access the 4-MByte page referenced by this entry; otherwise, reserved (must be 0)" , is_pat , PDFlags :: PAT) ; }
};
}
