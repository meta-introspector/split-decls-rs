// Generated macro for impl_148 (impl)
macro_rules! Depcrate_bits32_pagingimpl_148 {
() => {
// Module: crate::bits32::paging
// Provides: {"impl_148"}
// Dependencies: {}
impl PTEntry { # [doc = " Creates a new PTEntry."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = "  * `page` - The physical address of the backing 4 KiB page."] # [doc = "  * `flags`- Additional flags for the entry."] pub fn new (page : PAddr , flags : PTFlags) -> PTEntry { let page_val = page & ADDRESS_MASK ; assert ! (page_val == page . into ()) ; assert ! (page % BASE_PAGE_SIZE == 0) ; PTEntry (page_val | flags . bits) } # [doc = " Retrieves the physical address in this entry."] pub fn address (self) -> PAddr { PAddr :: from (self . 0 & ADDRESS_MASK) } # [doc = " Returns the flags corresponding to this entry."] pub fn flags (self) -> PTFlags { PTFlags :: from_bits_truncate (self . 0) } check_flag ! (doc = "Present; must be 1 to map a 4-KByte page." , is_present , PTFlags :: P) ; check_flag ! (doc = "Read/write; if 0, writes may not be allowed to the 4-KByte page referenced by this entry." , is_writeable , PTFlags :: RW) ; check_flag ! (doc = "User/supervisor; if 0, user-mode accesses are not allowed to the 4-KByte page referenced by this entry." , is_user_mode_allowed , PTFlags :: US) ; check_flag ! (doc = "Page-level write-through." , is_page_write_through , PTFlags :: PWT) ; check_flag ! (doc = "Page-level cache disable." , is_page_level_cache_disabled , PTFlags :: PCD) ; check_flag ! (doc = "Accessed; indicates whether software has accessed the 4-KByte page referenced by this entry." , is_accessed , PTFlags :: A) ; check_flag ! (doc = "Dirty; indicates whether software has written to the 4-KByte page referenced by this entry." , is_dirty , PTFlags :: D) ; check_flag ! (doc = "If the PAT is supported, indirectly determines the memory type used to access the 4-KByte page referenced by this entry; otherwise, reserved (must be 0)" , is_pat , PTFlags :: PAT) ; check_flag ! (doc = "Global; if CR4.PGE = 1, determines whether the translation is global; ignored otherwise." , is_global , PTFlags :: G) ; }
};
}
