// Generated macro for assert_parse (macro)
macro_rules! Depcrate_parser_testsassert_parse {
() => {
// Module: crate::parser::tests
// Provides: {"assert_parse"}
// Dependencies: {}
macro_rules ! assert_parse { ($ parser : ident ($ source : literal) , $ res : expr $ (, [$ ((($ line1 : literal , $ col1 : literal) , $ (($ line2 : literal , $ col2 : literal) ,) ? $ kind : path)) ,+]) ?) => { assert_parse ! ($ parser ($ source) => "" , $ res $ (, [$ ((($ line1 , $ col1) , $ (($ line2 , $ col2) ,) ? $ kind)) ,+]) ?) ; } ; ($ parser : ident ($ source : literal) => $ unparsed : literal , $ res : expr $ (, [$ ((($ line1 : literal , $ col1 : literal) , $ (($ line2 : literal , $ col2 : literal) ,) ? $ kind : path)) ,+]) ?) => { let (span , res) = $ parser ($ crate :: source_to_span ($ source)) . finish () . expect ("Parser should succeed.") ; :: pretty_assertions :: assert_eq ! (res , $ res) ; :: pretty_assertions :: assert_str_eq ! (* span . fragment () , $ unparsed) ; let mut diags = span . extra . take_diags () . into_iter () ; $ ($ (let diag = diags . next () . expect ("Expected a parser diagnostic.") ; assert_diag_eq ! (& diag , (($ line1 , $ col1) , $ (($ line2 , $ col2) ,) ? $ kind)) ;) +) ? let last_diag = diags . next () ; assert ! (last_diag . is_none () , "There's a diag left: {:#?}" , last_diag) ; } ; ($ parser : ident ($ source : literal) => Err ($ line : literal , $ col : literal)) => { assert_parse ! ($ parser ($ source) => Err (($ line , $ col) ,)) ; } ; ($ parser : ident ($ source : literal) => Err (($ line : literal , $ col : literal) , $ (Notes : [$ ((($ line1 : literal , $ col1 : literal) , $ note : literal)) ,+] $ (,) ?) ? $ (Diags : [$ ((($ line2 : literal , $ col2 : literal) , $ (($ line3 : literal , $ col3 : literal) ,) ? $ kind : path)) ,+]) ?)) => { let err = $ parser ($ crate :: source_to_span ($ source)) . finish () . expect_err ("Parser should fail.") ; let (line , column) = (err . line () , err . column ()) ; :: pretty_assertions :: assert_eq ! (line , $ line , "Actual line: {}" , line) ; :: pretty_assertions :: assert_eq ! (column , $ col , "Actual column: {}" , column) ; let mut notes = err . notes () . into_iter () ; $ ($ (let note = notes . next () . expect ("Expected a parser note.") ; let (line , column , note) = (note . line () , note . column () , note . note ()) ; :: pretty_assertions :: assert_eq ! (line , $ line1 , "Actual line: {}" , line) ; :: pretty_assertions :: assert_eq ! (column , $ col1 , "Actual column: {}" , column) ; assert ! (note . contains ($ note) , "Expected \"{}\" to contain \"{}\"" , note , $ note) ;) +) ? let last_note = notes . next () ; assert ! (last_note . is_none () , "There's a note left: {:#?}" , last_note) ; let mut diags = err . diags () . into_iter () ; $ ($ (let diag = diags . next () . expect ("Expected a parser diagnostic.") ; assert_diag_eq ! (diag , (($ line2 , $ col2) , $ (($ line3 , $ col3) ,) ? $ kind)) ;) +) ? let last_diag = diags . next () ; assert ! (last_diag . is_none () , "There's a diag left: {:#?}" , last_diag) ; } ; }
};
}
