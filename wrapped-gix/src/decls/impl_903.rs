macro_rules! deps {
    () => {
        Link!();
        Read!();
        Error!();
        Pipeline!();
        Buffer!();
        Note!();
        Path!();
        State!();
        Blob!();
        Commit!();
        Kind!();
    };
}

macro_rules! impl_903 {
    () => {
        deps!();
        # [doc = " Conversions"] impl Pipeline < '_ > { # [doc = " Convert a `src` stream (to be found at `rela_path`, a repo-relative path) to a representation suitable for storage in `git`"] # [doc = " by using all attributes at `rela_path` and configuration of the repository to know exactly which filters apply."] # [doc = " `index` is used in particularly rare cases where the CRLF filter in auto-mode tries to determine whether to apply itself,"] # [doc = " and it should match the state used when [instantiating this instance](Self::new())."] # [doc = " Note that the return-type implements [`std::io::Read`]."] pub fn convert_to_git < R > (& mut self , src : R , rela_path : & std :: path :: Path , index : & gix_index :: State ,) -> Result < gix_filter :: pipeline :: convert :: ToGitOutcome < '_ , R > , pipeline :: convert_to_git :: Error > where R : std :: io :: Read , { let entry = self . cache . at_path (rela_path , None , & self . repo . objects) ? ; Ok (self . inner . convert_to_git (src , rela_path , & mut | _ , attrs | { entry . matching_attributes (attrs) ; } , & mut | buf | -> Result < _ , gix_object :: find :: Error > { let entry = match index . entry_by_path (gix_path :: to_unix_separators_on_windows (gix_path :: into_bstr (rela_path)) . as_ref ()) { None => return Ok (None) , Some (entry) => entry , } ; let obj = self . repo . objects . try_find (& entry . id , buf) ? ; Ok (obj . filter (| obj | obj . kind == gix_object :: Kind :: Blob) . map (| _ | ())) } ,) ?) } # [doc = " Convert a `src` buffer located at `rela_path` (in the index) from what's in `git` to the worktree representation."] # [doc = " This method will obtain all attributes and configuration necessary to know exactly which filters to apply."] # [doc = " Note that the return-type implements [`std::io::Read`]."] # [doc = ""] # [doc = " Use `can_delay` to tell driver processes that they may delay the return of data. Doing this will require the caller to specifically"] # [doc = " handle delayed files by keeping state and using [`Self::into_parts()`] to get access to the driver state to follow the delayed-files"] # [doc = " protocol. For simplicity, most will want to disallow delayed processing."] pub fn convert_to_worktree < 'input > (& mut self , src : & 'input [u8] , rela_path : & BStr , can_delay : gix_filter :: driver :: apply :: Delay ,) -> Result < gix_filter :: pipeline :: convert :: ToWorktreeOutcome < 'input , '_ > , pipeline :: convert_to_worktree :: Error > { let entry = self . cache . at_entry (rela_path , None , & self . repo . objects) ? ; Ok (self . inner . convert_to_worktree (src , rela_path , & mut | _ , attrs | { entry . matching_attributes (attrs) ; } , can_delay ,) ?) } # [doc = " Add the worktree file at `rela_path` to the object database and return its `(id, entry, symlink_metadata)` for use in a tree or in the index, for instance."] # [doc = " If `rela_path` is a directory *and* is a repository with its `HEAD` pointing to a commit, it will also be provided with the appropriate kind."] # [doc = " Note that this can easily lead to embedded repositories as no submodule-crosscheck is performed. Otherwise, unreadable repositories or directories"] # [doc = " are ignored with `None` as return value."] # [doc = ""] # [doc = " `index` is used in particularly rare cases where the CRLF filter in auto-mode tries to determine whether to apply itself,"] # [doc = " and it should match the state used when [instantiating this instance](Self::new())."] # [doc = ""] # [doc = " Return `Ok(None)` the file didn't exist in the worktree, or if it was of an untrackable type."] pub fn worktree_file_to_object (& mut self , rela_path : & BStr , index : & gix_index :: State ,) -> Result < Option < (gix_hash :: ObjectId , gix_object :: tree :: EntryKind , std :: fs :: Metadata) > , pipeline :: worktree_file_to_object :: Error , > { use pipeline :: worktree_file_to_object :: Error ; let rela_path_as_path = gix_path :: from_bstr (rela_path) ; let repo = self . repo ; let worktree_dir = repo . workdir () . ok_or (Error :: MissingWorktree) ? ; let path = worktree_dir . join (& rela_path_as_path) ; let md = match std :: fs :: symlink_metadata (& path) { Ok (md) => md , Err (err) => { if gix_fs :: io_err :: is_not_found (err . kind () , err . raw_os_error ()) { return Ok (None) ; } else { return Err (Error :: IO { source : err , path }) ; } } } ; let (id , kind) = if md . is_symlink () { let target = std :: fs :: read_link (& path) . map_err (| source | Error :: IO { source , path }) ? ; let id = repo . write_blob (gix_path :: into_bstr (target) . as_ref ()) ? ; (id , gix_object :: tree :: EntryKind :: Link) } else if md . is_file () { use gix_filter :: pipeline :: convert :: ToGitOutcome ; let file = std :: fs :: File :: open (& path) . map_err (| source | Error :: IO { source , path }) ? ; let file_for_git = self . convert_to_git (file , rela_path_as_path . as_ref () , index) ? ; let id = match file_for_git { ToGitOutcome :: Unchanged (mut file) => repo . write_blob_stream (& mut file) ? , ToGitOutcome :: Buffer (buf) => repo . write_blob (buf) ? , ToGitOutcome :: Process (mut read) => repo . write_blob_stream (& mut read) ? , } ; let kind = if gix_fs :: is_executable (& md) { gix_object :: tree :: EntryKind :: BlobExecutable } else { gix_object :: tree :: EntryKind :: Blob } ; (id , kind) } else if md . is_dir () { let Some (submodule_repo) = crate :: open_opts (& path , repo . open_options () . clone ()) . ok () else { return Ok (None) ; } ; let Some (id) = submodule_repo . head_id () . ok () else { return Ok (None) ; } ; (id . detach () . attach (repo) , gix_object :: tree :: EntryKind :: Commit) } else { return Ok (None) ; } ; Ok (Some ((id . detach () , kind , md))) } # [doc = " Retrieve the static context that is made available to the process filters."] # [doc = ""] # [doc = " The context set here is relevant for the [`convert_to_git()`][Self::convert_to_git()] and"] # [doc = " [`convert_to_worktree()`][Self::convert_to_worktree()] methods."] pub fn driver_context_mut (& mut self) -> & mut gix_filter :: pipeline :: Context { self . inner . driver_context_mut () } }
    };
}

impl_903!();