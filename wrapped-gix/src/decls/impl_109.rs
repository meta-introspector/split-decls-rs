macro_rules! deps {
    () => {
        Note!();
        Options!();
    };
}

macro_rules! impl_109 {
    () => {
        deps!();
        impl Options { # [doc = " If `true`, default `false`, pathspecs and the directory walk itself will be setup to use the [prefix](crate::Repository::prefix)"] # [doc = " if patterns are empty."] # [doc = ""] # [doc = " This means that the directory walk will be limited to only what's inside the [repository prefix](crate::Repository::prefix)."] # [doc = " By default, the directory walk will see everything."] pub fn empty_patterns_match_prefix (mut self , toggle : bool) -> Self { self . empty_patterns_match_prefix = toggle ; self } # [doc = " Like [`empty_patterns_match_prefix()`](Self::empty_patterns_match_prefix), but only requires a mutably borrowed instance."] pub fn set_empty_patterns_match_prefix (& mut self , toggle : bool) -> & mut Self { self . empty_patterns_match_prefix = toggle ; self } # [doc = " If `toggle` is `true`, we will stop figuring out if any directory that is a candidate for recursion is also a nested repository,"] # [doc = " which saves time but leads to recurse into it. If `false`, nested repositories will not be traversed."] pub fn recurse_repositories (mut self , toggle : bool) -> Self { self . recurse_repositories = toggle ; self } # [doc = " Like [`recurse_repositories()`](Self::recurse_repositories), but only requires a mutably borrowed instance."] pub fn set_recurse_repositories (& mut self , toggle : bool) -> & mut Self { self . recurse_repositories = toggle ; self } # [doc = " If `toggle` is `true`, entries that are pruned and whose [Kind](gix_dir::entry::Kind) is known will be emitted."] pub fn emit_pruned (mut self , toggle : bool) -> Self { self . emit_pruned = toggle ; self } # [doc = " Like [`emit_pruned()`](Self::emit_pruned), but only requires a mutably borrowed instance."] pub fn set_emit_pruned (& mut self , toggle : bool) -> & mut Self { self . emit_pruned = toggle ; self } # [doc = " If `value` is `Some(mode)`, entries that are ignored will be emitted according to the given `mode`."] # [doc = " If `None`, ignored entries will not be emitted at all."] pub fn emit_ignored (mut self , value : Option < EmissionMode >) -> Self { self . emit_ignored = value ; self } # [doc = " Like [`emit_ignored()`](Self::emit_ignored), but only requires a mutably borrowed instance."] pub fn set_emit_ignored (& mut self , value : Option < EmissionMode >) -> & mut Self { self . emit_ignored = value ; self } # [doc = " When the walk is for deletion, `value` must be `Some(_)` to assure we don't collapse directories that have precious files in"] # [doc = " them, and otherwise assure that no entries are observable that shouldn't be deleted."] # [doc = " If `None`, precious files are treated like expendable files, which is usually what you want when displaying them"] # [doc = " for addition to the repository, and the collapse of folders can be more generous in relation to ignored files."] pub fn for_deletion (mut self , value : Option < ForDeletionMode >) -> Self { self . for_deletion = value ; self } # [doc = " Like [`for_deletion()`](Self::for_deletion), but only requires a mutably borrowed instance."] pub fn set_for_deletion (& mut self , value : Option < ForDeletionMode >) -> & mut Self { self . for_deletion = value ; self } # [doc = " If `toggle` is `true`, we will also emit entries for tracked items. Otherwise these will remain 'hidden',"] # [doc = " even if a pathspec directly refers to it."] pub fn emit_tracked (mut self , toggle : bool) -> Self { self . emit_tracked = toggle ; self } # [doc = " Like [`emit_tracked()`](Self::emit_tracked), but only requires a mutably borrowed instance."] pub fn set_emit_tracked (& mut self , toggle : bool) -> & mut Self { self . emit_tracked = toggle ; self } # [doc = " Controls the way untracked files are emitted. By default, this is happening immediately and without any simplification."] pub fn emit_untracked (mut self , mode : EmissionMode) -> Self { self . emit_untracked = mode ; self } # [doc = " Like [`emit_untracked()`](Self::emit_untracked), but only requires a mutably borrowed instance."] pub fn set_emit_untracked (& mut self , mode : EmissionMode) -> & mut Self { self . emit_untracked = mode ; self } # [doc = " If `toggle` is `true`, emit empty directories as well. Note that a directory also counts as empty if it has any"] # [doc = " amount or depth of nested subdirectories, as long as none of them includes a file."] # [doc = " Thus, this makes leaf-level empty directories visible, as those don't have any content."] pub fn emit_empty_directories (mut self , toggle : bool) -> Self { self . emit_empty_directories = toggle ; self } # [doc = " Like [`emit_empty_directories()`](Self::emit_empty_directories), but only requires a mutably borrowed instance."] pub fn set_emit_empty_directories (& mut self , toggle : bool) -> & mut Self { self . emit_empty_directories = toggle ; self } # [doc = " If `toggle` is `true`, we will not only find non-bare repositories in untracked directories, but also bare ones."] # [doc = ""] # [doc = " Note that this is very costly, but without it, bare repositories will appear like untracked directories when collapsed,"] # [doc = " and they will be recursed into."] pub fn classify_untracked_bare_repositories (mut self , toggle : bool) -> Self { self . classify_untracked_bare_repositories = toggle ; self } # [doc = " Like [`classify_untracked_bare_repositories()`](Self::classify_untracked_bare_repositories), but only requires a mutably borrowed instance."] pub fn set_classify_untracked_bare_repositories (& mut self , toggle : bool) -> & mut Self { self . classify_untracked_bare_repositories = toggle ; self } # [doc = " Control whether entries that are in an about-to-be collapsed directory will be emitted. The default is `None`,"] # [doc = " so entries in a collapsed directory are not observable."] pub fn emit_collapsed (mut self , value : Option < CollapsedEntriesEmissionMode >) -> Self { self . emit_collapsed = value ; self } # [doc = " Like [`emit_collapsed()`](Self::emit_collapsed), but only requires a mutably borrowed instance."] pub fn set_emit_collapsed (& mut self , value : Option < CollapsedEntriesEmissionMode >) -> & mut Self { self . emit_collapsed = value ; self } # [doc = " This is a `libgit2` compatibility flag, and if enabled, symlinks that point to directories will be considered a directory"] # [doc = " when checking for exclusion."] # [doc = ""] # [doc = " This is relevant if `src2` points to `src`, and is excluded with `src2/`. If `false`, `src2` will not be excluded,"] # [doc = " if `true` it will be excluded as the symlink is considered a directory."] # [doc = ""] # [doc = " In other words, for Git compatibility this flag should be `false`, the default, for `git2` compatibility it should be `true`."] pub fn symlinks_to_directories_are_ignored_like_directories (mut self , toggle : bool) -> Self { self . symlinks_to_directories_are_ignored_like_directories = toggle ; self } # [doc = " Like [`symlinks_to_directories_are_ignored_like_directories()`](Self::symlinks_to_directories_are_ignored_like_directories),"] # [doc = " but only requires a mutably borrowed instance."] pub fn set_symlinks_to_directories_are_ignored_like_directories (& mut self , toggle : bool) -> & mut Self { self . symlinks_to_directories_are_ignored_like_directories = toggle ; self } }
    };
}

impl_109!()