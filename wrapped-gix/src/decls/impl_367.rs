macro_rules! deps {
    () => {
        Error!();
        Item!();
        Id!();
        Commit!();
        Default!();
        Repository!();
        Options!();
        ObjectIdExt!();
        Spec!();
        Platform!();
    };
}

macro_rules! impl_367 {
    () => {
        deps!();
        # [doc = " Methods for resolving revisions by spec or working with the commit graph."] impl crate :: Repository { # [doc = " Parse a revision specification and turn it into the object(s) it describes, similar to `git rev-parse`."] # [doc = ""] # [doc = " # Deviation"] # [doc = ""] # [doc = " - `@` actually stands for `HEAD`, whereas `git` resolves it to the object pointed to by `HEAD` without making the"] # [doc = "   `HEAD` ref available for lookups."] # [doc (alias = "revparse" , alias = "git2")] # [cfg (feature = "revision")] pub fn rev_parse < 'a > (& self , spec : impl Into < & 'a BStr >) -> Result < revision :: Spec < '_ > , revision :: spec :: parse :: Error > { revision :: Spec :: from_bstr (spec , self , revision :: spec :: parse :: Options { object_kind_hint : self . config . object_kind_hint , .. Default :: default () } ,) } # [doc = " Parse a revision specification and return single object id as represented by this instance."] # [doc (alias = "revparse_single" , alias = "git2")] # [cfg (feature = "revision")] pub fn rev_parse_single < 'repo , 'a > (& 'repo self , spec : impl Into < & 'a BStr > ,) -> Result < Id < 'repo > , revision :: spec :: parse :: single :: Error > { let spec = spec . into () ; self . rev_parse (spec) ? . single () . ok_or (revision :: spec :: parse :: single :: Error :: RangedRev { spec : spec . into () }) } # [doc = " Obtain the best merge-base between commit `one` and `two`, or fail if there is none."] # [doc = ""] # [doc = " # Performance"] # [doc = " For repeated calls, prefer [`merge_base_with_cache()`](crate::Repository::merge_base_with_graph())."] # [doc = " Also be sure to [set an object cache](crate::Repository::object_cache_size_if_unset) to accelerate repeated commit lookups."] # [cfg (feature = "revision")] pub fn merge_base (& self , one : impl Into < gix_hash :: ObjectId > , two : impl Into < gix_hash :: ObjectId > ,) -> Result < Id < '_ > , super :: merge_base :: Error > { use crate :: prelude :: ObjectIdExt ; let one = one . into () ; let two = two . into () ; let cache = self . commit_graph_if_enabled () ? ; let mut graph = self . revision_graph (cache . as_ref ()) ; let bases = gix_revision :: merge_base (one , & [two] , & mut graph) ? . ok_or (super :: merge_base :: Error :: NotFound { first : one , second : two , }) ? ; Ok (bases [0] . attach (self)) } # [doc = " Obtain the best merge-base between commit `one` and `two`, or fail if there is none, providing a"] # [doc = " commit-graph `graph` to potentially greatly accelerate the operation by reusing graphs from previous runs."] # [doc = ""] # [doc = " # Performance"] # [doc = " Be sure to [set an object cache](crate::Repository::object_cache_size_if_unset) to accelerate repeated commit lookups."] # [cfg (feature = "revision")] pub fn merge_base_with_graph (& self , one : impl Into < gix_hash :: ObjectId > , two : impl Into < gix_hash :: ObjectId > , graph : & mut gix_revwalk :: Graph < '_ , '_ , gix_revwalk :: graph :: Commit < gix_revision :: merge_base :: Flags > > ,) -> Result < Id < '_ > , super :: merge_base_with_graph :: Error > { use crate :: prelude :: ObjectIdExt ; let one = one . into () ; let two = two . into () ; let bases = gix_revision :: merge_base (one , & [two] , graph) ? . ok_or (super :: merge_base_with_graph :: Error :: NotFound { first : one , second : two , }) ? ; Ok (bases [0] . attach (self)) } # [doc = " Get all merge-bases between commit `one` and `others`, or an empty list if there is none, providing a"] # [doc = " commit-graph `graph` to potentially greatly speed up the operation."] # [doc = ""] # [doc = " # Performance"] # [doc = " Be sure to [set an object cache](crate::Repository::object_cache_size_if_unset) to speed up repeated commit lookups."] # [doc (alias = "merge_bases_many" , alias = "git2")] # [cfg (feature = "revision")] pub fn merge_bases_many_with_graph (& self , one : impl Into < gix_hash :: ObjectId > , others : & [gix_hash :: ObjectId] , graph : & mut gix_revwalk :: Graph < '_ , '_ , gix_revwalk :: graph :: Commit < gix_revision :: merge_base :: Flags > > ,) -> Result < Vec < Id < '_ > > , gix_revision :: merge_base :: Error > { use crate :: prelude :: ObjectIdExt ; let one = one . into () ; Ok (gix_revision :: merge_base (one , others , graph) ? . unwrap_or_default () . into_iter () . map (| id | id . attach (self)) . collect ()) } # [doc = " Like [`merge_bases_many_with_graph()`](Self::merge_bases_many_with_graph), but without the ability to speed up consecutive calls with a [graph](gix_revwalk::Graph)."] # [doc = ""] # [doc = " # Performance"] # [doc = ""] # [doc = " Be sure to [set an object cache](crate::Repository::object_cache_size_if_unset) to speed up repeated commit lookups, and consider"] # [doc = " using [`merge_bases_many_with_graph()`](Self::merge_bases_many_with_graph) for consecutive calls."] # [doc (alias = "git2")] # [cfg (feature = "revision")] pub fn merge_bases_many (& self , one : impl Into < gix_hash :: ObjectId > , others : & [gix_hash :: ObjectId] ,) -> Result < Vec < Id < '_ > > , crate :: repository :: merge_bases_many :: Error > { let cache = self . commit_graph_if_enabled () ? ; let mut graph = self . revision_graph (cache . as_ref ()) ; Ok (self . merge_bases_many_with_graph (one , others , & mut graph) ?) } # [doc = " Return the best merge-base among all `commits`, or fail if `commits` yields no commit or no merge-base was found."] # [doc = ""] # [doc = " Use `graph` to speed up repeated calls."] # [cfg (feature = "revision")] pub fn merge_base_octopus_with_graph (& self , commits : impl IntoIterator < Item = impl Into < gix_hash :: ObjectId > > , graph : & mut gix_revwalk :: Graph < '_ , '_ , gix_revwalk :: graph :: Commit < gix_revision :: merge_base :: Flags > > ,) -> Result < Id < '_ > , crate :: repository :: merge_base_octopus_with_graph :: Error > { use crate :: { prelude :: ObjectIdExt , repository :: merge_base_octopus_with_graph } ; let commits : Vec < _ > = commits . into_iter () . map (Into :: into) . collect () ; let first = commits . first () . copied () . ok_or (merge_base_octopus_with_graph :: Error :: MissingCommit) ? ; gix_revision :: merge_base :: octopus (first , & commits [1 ..] , graph) ? . ok_or (merge_base_octopus_with_graph :: Error :: NoMergeBase) . map (| id | id . attach (self)) } # [doc = " Return the best merge-base among all `commits`, or fail if `commits` yields no commit or no merge-base was found."] # [doc = ""] # [doc = " For repeated calls, prefer [`Self::merge_base_octopus_with_graph()`] for cache-reuse."] # [cfg (feature = "revision")] pub fn merge_base_octopus (& self , commits : impl IntoIterator < Item = impl Into < gix_hash :: ObjectId > > ,) -> Result < Id < '_ > , crate :: repository :: merge_base_octopus :: Error > { let cache = self . commit_graph_if_enabled () ? ; let mut graph = self . revision_graph (cache . as_ref ()) ; Ok (self . merge_base_octopus_with_graph (commits , & mut graph) ?) } # [doc = " Create the baseline for a revision walk by initializing it with the `tips` to start iterating on."] # [doc = ""] # [doc = " It can be configured further before starting the actual walk."] # [doc (alias = "revwalk" , alias = "git2")] pub fn rev_walk (& self , tips : impl IntoIterator < Item = impl Into < gix_hash :: ObjectId > > ,) -> revision :: walk :: Platform < '_ > { revision :: walk :: Platform :: new (tips , self) } }
    };
}

impl_367!();