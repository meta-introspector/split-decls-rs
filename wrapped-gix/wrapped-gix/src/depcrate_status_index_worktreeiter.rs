// Generated macro for Iter (struct)
macro_rules! Depcrate_status_index_worktreeIter {
() => {
// Module: crate::status::index_worktree
// Provides: {"Iter"}
// Dependencies: {}
# [doc = " An iterator for changes between the index and the worktree."] # [doc = ""] # [doc = " Note that depending on the underlying configuration, there might be a significant delay until the first"] # [doc = " item is received due to the buffering necessary to perform rename tracking and/or sorting."] # [doc = ""] # [doc = " ### Submodules"] # [doc = ""] # [doc = " Note that submodules can be set to 'inactive', which will not exclude them from the status operation, similar to"] # [doc = " how `git status` includes them."] # [doc = ""] # [doc = " ### Index Changes"] # [doc = ""] # [doc = " Changes to the index are collected and it's possible to write the index back using [Outcome::write_changes()](crate::status::Outcome)."] # [doc = " Note that these changes are not observable, they will always be kept."] # [doc = ""] # [doc = " ### Parallel Operation"] # [doc = ""] # [doc = " Note that without the `parallel` feature, the iterator becomes 'serial', which means all status will be computed in advance"] # [doc = " and it's non-interruptible, yielding worse performance for is-dirty checks for instance as interruptions won't happen."] # [doc = " It's a crutch that is just there to make single-threaded applications possible at all, as it's not really an iterator"] # [doc = " anymore. If this matters, better run [Repository::index_worktree_status()] by hand as it provides all control one would need,"] # [doc = " just not as an iterator."] # [doc = ""] # [doc = " Also, even with `parallel` set, the first call to `next()` will block until there is an item available, without a chance"] # [doc = " to interrupt unless [`status::Platform::should_interrupt_*()`](crate::status::Platform::should_interrupt_shared()) was"] # [doc = " configured."] pub struct Iter { inner : crate :: status :: Iter , }
};
}
