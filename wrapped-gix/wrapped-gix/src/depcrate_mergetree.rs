// Generated macro for tree (module)
macro_rules! Depcrate_mergetree {
() => {
// Module: crate::merge
// Provides: {"tree"}
// Dependencies: {}
# [doc = ""] pub mod tree { use gix_merge :: blob :: builtin_driver ; pub use gix_merge :: tree :: { apply_index_entries , treat_as_unresolved , Conflict , ContentMerge , Resolution , ResolutionFailure , TreatAsUnresolved , } ; # [doc = " The outcome produced by [`Repository::merge_trees()`](crate::Repository::merge_trees())."] # [derive (Clone)] pub struct Outcome < 'repo > { # [doc = " The ready-made (but unwritten) *base* tree, including all non-conflicting changes, and the changes that had"] # [doc = " conflicts which could be resolved automatically."] # [doc = ""] # [doc = " This means, if all of their changes were conflicting, this will be equivalent to the *base* tree."] pub tree : crate :: object :: tree :: Editor < 'repo > , # [doc = " The set of conflicts we encountered. Can be empty to indicate there was no conflict."] # [doc = " Note that conflicts might have been auto-resolved, but they are listed here for completeness."] # [doc = " Use [`has_unresolved_conflicts()`](Outcome::has_unresolved_conflicts()) to see if any action is needed"] # [doc = " before using [`tree`](Outcome::tree)."] pub conflicts : Vec < Conflict > , # [doc = " `true` if `conflicts` contains only a single *unresolved* conflict in the last slot, but possibly more resolved ones."] # [doc = " This also makes this outcome a very partial merge that cannot be completed."] pub failed_on_first_unresolved_conflict : bool , } impl Outcome < '_ > { # [doc = " Return `true` if there is any conflict that would still need to be resolved as they would yield undesirable trees."] # [doc = " This is based on `how` to determine what should be considered unresolved."] pub fn has_unresolved_conflicts (& self , how : TreatAsUnresolved) -> bool { self . conflicts . iter () . any (| c | c . is_unresolved (how)) } # [doc = " Returns `true` if `index` changed as we applied conflicting stages to it, using `how` to determine if a"] # [doc = " conflict should be considered unresolved."] # [doc = ""] # [doc = " `removal_mode` decides how unconflicted entries should be removed if they are superseded by"] # [doc = " their conflicted counterparts."] # [doc = ""] # [doc = " It's important that `index` is at the state of [`Self::tree`]."] # [doc = " Note that in practice, whenever there is a single [conflict](Conflict), this function will return `true`."] pub fn index_changed_after_applying_conflicts (& self , index : & mut gix_index :: State , how : TreatAsUnresolved , removal_mode : apply_index_entries :: RemovalMode ,) -> bool { apply_index_entries (& self . conflicts , how , index , removal_mode) } } # [doc = " A way to configure [`Repository::merge_trees()`](crate::Repository::merge_trees())."] # [derive (Default , Debug , Clone)] pub struct Options { inner : gix_merge :: tree :: Options , file_favor : Option < FileFavor > , tree_favor : Option < TreeFavor > , } impl From < gix_merge :: tree :: Options > for Options { fn from (opts : gix_merge :: tree :: Options) -> Self { Options { inner : opts , file_favor : None , tree_favor : None , } } } impl From < Options > for gix_merge :: tree :: Options { fn from (value : Options) -> Self { let mut opts = value . inner ; if let Some (file_favor) = value . file_favor { let (resolve_binary , resolve_text) = match file_favor { FileFavor :: Ours => (builtin_driver :: binary :: ResolveWith :: Ours , builtin_driver :: text :: Conflict :: ResolveWithOurs ,) , FileFavor :: Theirs => (builtin_driver :: binary :: ResolveWith :: Theirs , builtin_driver :: text :: Conflict :: ResolveWithTheirs ,) , } ; opts . symlink_conflicts = Some (resolve_binary) ; opts . blob_merge . resolve_binary_with = Some (resolve_binary) ; opts . blob_merge . text . conflict = resolve_text ; } opts . tree_conflicts = value . tree_favor . map (Into :: into) ; opts } } # [doc = " Identify how files should be resolved in case of conflicts."] # [doc = ""] # [doc = " This works for…"] # [doc = ""] # [doc = " * content merges"] # [doc = " * binary files"] # [doc = " * symlinks (a form of file after all)"] # [doc = ""] # [doc = " Note that *union* merges aren't available as they aren't available for binaries or symlinks."] # [derive (Debug , Copy , Clone)] pub enum FileFavor { # [doc = " Choose *our* side in case of a conflict."] # [doc = " Note that this choice is precise, so *ours* hunk will only be chosen if they conflict with *theirs*,"] # [doc = " so *their* hunks may still show up in the merged result."] Ours , # [doc = " Choose *their* side in case of a conflict."] # [doc = " Note that this choice is precise, so *ours* hunk will only be chosen if they conflict with *theirs*,"] # [doc = " so *their* hunks may still show up in the merged result."] Theirs , } # [doc = " Control how irreconcilable changes to trees should be resolved."] # [doc = ""] # [doc = " Examples for such issues are:"] # [doc = ""] # [doc = " * *we*: delete, *they*: modify"] # [doc = " * *we*: rename, *they*: rename to something else"] # [doc = " * *we*: delete, *they*: rename"] # [doc = ""] # [doc = " Use this to control which entries are visible to in the resulting tree."] # [doc = " Also note that this does not apply to the many tree-related changes are reconcilable."] # [derive (Debug , Copy , Clone)] pub enum TreeFavor { # [doc = " Choose *our* side in case of a conflict."] # [doc = " Note that content-merges are *still* performed according to the [FileFavor]."] Ours , # [doc = " Choose the state of the shared common ancestor, dropping both *ours* and *their* changes."] # [doc = " Content merges are not performed here."] Ancestor , } impl From < TreeFavor > for gix_merge :: tree :: ResolveWith { fn from (value : TreeFavor) -> Self { match value { TreeFavor :: Ours => gix_merge :: tree :: ResolveWith :: Ours , TreeFavor :: Ancestor => gix_merge :: tree :: ResolveWith :: Ancestor , } } } # [doc = " Builder"] impl Options { # [doc = " If *not* `None`, rename tracking will be performed when determining the changes of each side of the merge."] pub fn with_rewrites (mut self , rewrites : Option < gix_diff :: Rewrites >) -> Self { self . inner . rewrites = rewrites ; self } # [doc = " If `Some(what-is-unresolved)`, the first unresolved conflict will cause the entire merge to stop."] # [doc = " This is useful to see if there is any conflict, without performing the whole operation, something"] # [doc = " that can be very relevant during merges that would cause a lot of blob-diffs."] pub fn with_fail_on_conflict (mut self , fail_on_conflict : Option < TreatAsUnresolved >) -> Self { self . inner . fail_on_conflict = fail_on_conflict ; self } # [doc = " When `None`, the default, both sides will be treated equally, and in case of conflict an unbiased representation"] # [doc = " is chosen both for content and for trees, causing a conflict."] # [doc = ""] # [doc = " With `Some(favor)` one can choose a side to prefer in order to forcefully resolve an otherwise irreconcilable conflict,"] # [doc = " loosing information in the process."] pub fn with_file_favor (mut self , file_favor : Option < FileFavor >) -> Self { self . file_favor = file_favor ; self } # [doc = " When `None`, the default, both sides will be treated equally, trying to keep both conflicting changes in the tree, possibly"] # [doc = " by renaming one side to move it out of the way."] # [doc = ""] # [doc = " With `Some(favor)` one can choose a side to prefer in order to forcefully resolve an otherwise irreconcilable conflict,"] # [doc = " loosing information in the process."] pub fn with_tree_favor (mut self , tree_favor : Option < TreeFavor >) -> Self { self . tree_favor = tree_favor ; self } } }
};
}
