// Generated macro for Iter (struct)
macro_rules! Depcrate_status_iter_typesIter {
() => {
// Module: crate::status::iter::types
// Provides: {"Iter"}
// Dependencies: {}
# [doc = " An iterator for changes between the index and the worktree and the head-tree and the index."] # [doc = ""] # [doc = " Note that depending on the underlying configuration, there might be a significant delay until the first"] # [doc = " item is received due to the buffering necessary to perform rename tracking and/or sorting."] # [doc = ""] # [doc = " ### Submodules"] # [doc = ""] # [doc = " Note that submodules can be set to 'inactive', which will not exclude them from the status operation, similar to"] # [doc = " how `git status` includes them."] # [doc = ""] # [doc = " ### Index Changes"] # [doc = ""] # [doc = " Changes to the index are collected, and it's possible to write the index back using [Outcome::write_changes()]."] # [doc = " Note that these changes are not observable, they will always be kept."] # [doc = ""] # [doc = " ### Parallel Operation"] # [doc = ""] # [doc = " Note that without the `parallel` feature, the iterator becomes 'serial', which means all status will be computed in advance,"] # [doc = " and it's non-interruptible, yielding worse performance for is-dirty checks for instance as interruptions won't happen."] # [doc = " It's a crutch that is just there to make single-threaded applications possible at all, as it's not really an iterator"] # [doc = " anymore. If this matters, better run [Repository::index_worktree_status()](crate::Repository::index_worktree_status) by hand"] # [doc = " as it provides all control one would need, just not as an iterator."] # [doc = ""] # [doc = " Also, even with `parallel` set, the first call to `next()` will block until there is an item available, without a chance"] # [doc = " to interrupt unless [`status::Platform::should_interrupt_*()`](crate::status::Platform::should_interrupt_shared()) was"] # [doc = " configured."] pub struct Iter { # [cfg (feature = "parallel")] # [allow (clippy :: type_complexity)] pub (super) rx_and_join : Option < (std :: sync :: mpsc :: Receiver < Item > , std :: thread :: JoinHandle < Result < Outcome , index_worktree :: Error > > , Option < std :: thread :: JoinHandle < Result < tree_index :: Outcome , tree_index :: Error > > > ,) > , # [cfg (feature = "parallel")] pub (super) should_interrupt : crate :: status :: OwnedOrStaticAtomicBool , # [doc = " Without parallelization, the iterator has to buffer all changes in advance."] # [cfg (not (feature = "parallel"))] pub (super) items : std :: vec :: IntoIter < Item > , # [doc = " The outcome of the operation, only available once the operation has ended."] pub (in crate :: status) out : Option < Outcome > , # [doc = " The set of `(entry_index, change)` we extracted in order to potentially write back the worktree index with the changes applied."] pub (super) index_changes : Vec < (usize , ApplyChange) > , }
};
}
