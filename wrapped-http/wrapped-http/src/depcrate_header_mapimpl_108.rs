// Generated macro for impl_108 (impl)
macro_rules! Depcrate_header_mapimpl_108 {
() => {
// Module: crate::header::map
// Provides: {"impl_108"}
// Dependencies: {}
impl < 'a , T > OccupiedEntry < 'a , T > { # [doc = " Returns a reference to the entry's key."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::header::{HeaderMap, Entry, HOST};"] # [doc = " let mut map = HeaderMap::new();"] # [doc = " map.insert(HOST, \"world\".parse().unwrap());"] # [doc = ""] # [doc = " if let Entry::Occupied(e) = map.entry(\"host\") {"] # [doc = "     assert_eq!(\"host\", e.key());"] # [doc = " }"] # [doc = " ```"] pub fn key (& self) -> & HeaderName { & self . map . entries [self . index] . key } # [doc = " Get a reference to the first value in the entry."] # [doc = ""] # [doc = " Values are stored in insertion order."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " `get` panics if there are no values associated with the entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::header::{HeaderMap, Entry, HOST};"] # [doc = " let mut map = HeaderMap::new();"] # [doc = " map.insert(HOST, \"hello.world\".parse().unwrap());"] # [doc = ""] # [doc = " if let Entry::Occupied(mut e) = map.entry(\"host\") {"] # [doc = "     assert_eq!(e.get(), &\"hello.world\");"] # [doc = ""] # [doc = "     e.append(\"hello.earth\".parse().unwrap());"] # [doc = ""] # [doc = "     assert_eq!(e.get(), &\"hello.world\");"] # [doc = " }"] # [doc = " ```"] pub fn get (& self) -> & T { & self . map . entries [self . index] . value } # [doc = " Get a mutable reference to the first value in the entry."] # [doc = ""] # [doc = " Values are stored in insertion order."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " `get_mut` panics if there are no values associated with the entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::header::{HeaderMap, Entry, HOST};"] # [doc = " let mut map = HeaderMap::default();"] # [doc = " map.insert(HOST, \"hello.world\".to_string());"] # [doc = ""] # [doc = " if let Entry::Occupied(mut e) = map.entry(\"host\") {"] # [doc = "     e.get_mut().push_str(\"-2\");"] # [doc = "     assert_eq!(e.get(), &\"hello.world-2\");"] # [doc = " }"] # [doc = " ```"] pub fn get_mut (& mut self) -> & mut T { & mut self . map . entries [self . index] . value } # [doc = " Converts the `OccupiedEntry` into a mutable reference to the **first**"] # [doc = " value."] # [doc = ""] # [doc = " The lifetime of the returned reference is bound to the original map."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " `into_mut` panics if there are no values associated with the entry."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::header::{HeaderMap, Entry, HOST};"] # [doc = " let mut map = HeaderMap::default();"] # [doc = " map.insert(HOST, \"hello.world\".to_string());"] # [doc = " map.append(HOST, \"hello.earth\".to_string());"] # [doc = ""] # [doc = " if let Entry::Occupied(e) = map.entry(\"host\") {"] # [doc = "     e.into_mut().push_str(\"-2\");"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(\"hello.world-2\", map[\"host\"]);"] # [doc = " ```"] pub fn into_mut (self) -> & 'a mut T { & mut self . map . entries [self . index] . value } # [doc = " Sets the value of the entry."] # [doc = ""] # [doc = " All previous values associated with the entry are removed and the first"] # [doc = " one is returned. See `insert_mult` for an API that returns all values."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::header::{HeaderMap, Entry, HOST};"] # [doc = " let mut map = HeaderMap::new();"] # [doc = " map.insert(HOST, \"hello.world\".parse().unwrap());"] # [doc = ""] # [doc = " if let Entry::Occupied(mut e) = map.entry(\"host\") {"] # [doc = "     let mut prev = e.insert(\"earth\".parse().unwrap());"] # [doc = "     assert_eq!(\"hello.world\", prev);"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(\"earth\", map[\"host\"]);"] # [doc = " ```"] pub fn insert (& mut self , value : T) -> T { self . map . insert_occupied (self . index , value) } # [doc = " Sets the value of the entry."] # [doc = ""] # [doc = " This function does the same as `insert` except it returns an iterator"] # [doc = " that yields all values previously associated with the key."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::header::{HeaderMap, Entry, HOST};"] # [doc = " let mut map = HeaderMap::new();"] # [doc = " map.insert(HOST, \"world\".parse().unwrap());"] # [doc = " map.append(HOST, \"world2\".parse().unwrap());"] # [doc = ""] # [doc = " if let Entry::Occupied(mut e) = map.entry(\"host\") {"] # [doc = "     let mut prev = e.insert_mult(\"earth\".parse().unwrap());"] # [doc = "     assert_eq!(\"world\", prev.next().unwrap());"] # [doc = "     assert_eq!(\"world2\", prev.next().unwrap());"] # [doc = "     assert!(prev.next().is_none());"] # [doc = " }"] # [doc = ""] # [doc = " assert_eq!(\"earth\", map[\"host\"]);"] # [doc = " ```"] pub fn insert_mult (& mut self , value : T) -> ValueDrain < '_ , T > { self . map . insert_occupied_mult (self . index , value) } # [doc = " Insert the value into the entry."] # [doc = ""] # [doc = " The new value is appended to the end of the entry's value list. All"] # [doc = " previous values associated with the entry are retained."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::header::{HeaderMap, Entry, HOST};"] # [doc = " let mut map = HeaderMap::new();"] # [doc = " map.insert(HOST, \"world\".parse().unwrap());"] # [doc = ""] # [doc = " if let Entry::Occupied(mut e) = map.entry(\"host\") {"] # [doc = "     e.append(\"earth\".parse().unwrap());"] # [doc = " }"] # [doc = ""] # [doc = " let values = map.get_all(\"host\");"] # [doc = " let mut i = values.iter();"] # [doc = " assert_eq!(\"world\", *i.next().unwrap());"] # [doc = " assert_eq!(\"earth\", *i.next().unwrap());"] # [doc = " ```"] pub fn append (& mut self , value : T) { let idx = self . index ; let entry = & mut self . map . entries [idx] ; append_value (idx , entry , & mut self . map . extra_values , value) ; } # [doc = " Remove the entry from the map."] # [doc = ""] # [doc = " All values associated with the entry are removed and the first one is"] # [doc = " returned. See `remove_entry_mult` for an API that returns all values."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::header::{HeaderMap, Entry, HOST};"] # [doc = " let mut map = HeaderMap::new();"] # [doc = " map.insert(HOST, \"world\".parse().unwrap());"] # [doc = ""] # [doc = " if let Entry::Occupied(e) = map.entry(\"host\") {"] # [doc = "     let mut prev = e.remove();"] # [doc = "     assert_eq!(\"world\", prev);"] # [doc = " }"] # [doc = ""] # [doc = " assert!(!map.contains_key(\"host\"));"] # [doc = " ```"] pub fn remove (self) -> T { self . remove_entry () . 1 } # [doc = " Remove the entry from the map."] # [doc = ""] # [doc = " The key and all values associated with the entry are removed and the"] # [doc = " first one is returned. See `remove_entry_mult` for an API that returns"] # [doc = " all values."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::header::{HeaderMap, Entry, HOST};"] # [doc = " let mut map = HeaderMap::new();"] # [doc = " map.insert(HOST, \"world\".parse().unwrap());"] # [doc = ""] # [doc = " if let Entry::Occupied(e) = map.entry(\"host\") {"] # [doc = "     let (key, mut prev) = e.remove_entry();"] # [doc = "     assert_eq!(\"host\", key.as_str());"] # [doc = "     assert_eq!(\"world\", prev);"] # [doc = " }"] # [doc = ""] # [doc = " assert!(!map.contains_key(\"host\"));"] # [doc = " ```"] pub fn remove_entry (self) -> (HeaderName , T) { if let Some (links) = self . map . entries [self . index] . links { self . map . remove_all_extra_values (links . next) ; } let entry = self . map . remove_found (self . probe , self . index) ; (entry . key , entry . value) } # [doc = " Remove the entry from the map."] # [doc = ""] # [doc = " The key and all values associated with the entry are removed and"] # [doc = " returned."] pub fn remove_entry_mult (self) -> (HeaderName , ValueDrain < 'a , T >) { let raw_links = self . map . raw_links () ; let extra_values = & mut self . map . extra_values ; let next = self . map . entries [self . index] . links . map (| l | drain_all_extra_values (raw_links , extra_values , l . next) . into_iter ()) ; let entry = self . map . remove_found (self . probe , self . index) ; let drain = ValueDrain { first : Some (entry . value) , next , lt : PhantomData , } ; (entry . key , drain) } # [doc = " Returns an iterator visiting all values associated with the entry."] # [doc = ""] # [doc = " Values are iterated in insertion order."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::header::{HeaderMap, Entry, HOST};"] # [doc = " let mut map = HeaderMap::new();"] # [doc = " map.insert(HOST, \"world\".parse().unwrap());"] # [doc = " map.append(HOST, \"earth\".parse().unwrap());"] # [doc = ""] # [doc = " if let Entry::Occupied(e) = map.entry(\"host\") {"] # [doc = "     let mut iter = e.iter();"] # [doc = "     assert_eq!(&\"world\", iter.next().unwrap());"] # [doc = "     assert_eq!(&\"earth\", iter.next().unwrap());"] # [doc = "     assert!(iter.next().is_none());"] # [doc = " }"] # [doc = " ```"] pub fn iter (& self) -> ValueIter < '_ , T > { self . map . value_iter (Some (self . index)) } # [doc = " Returns an iterator mutably visiting all values associated with the"] # [doc = " entry."] # [doc = ""] # [doc = " Values are iterated in insertion order."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::header::{HeaderMap, Entry, HOST};"] # [doc = " let mut map = HeaderMap::default();"] # [doc = " map.insert(HOST, \"world\".to_string());"] # [doc = " map.append(HOST, \"earth\".to_string());"] # [doc = ""] # [doc = " if let Entry::Occupied(mut e) = map.entry(\"host\") {"] # [doc = "     for e in e.iter_mut() {"] # [doc = "         e.push_str(\"-boop\");"] # [doc = "     }"] # [doc = " }"] # [doc = ""] # [doc = " let mut values = map.get_all(\"host\");"] # [doc = " let mut i = values.iter();"] # [doc = " assert_eq!(&\"world-boop\", i.next().unwrap());"] # [doc = " assert_eq!(&\"earth-boop\", i.next().unwrap());"] # [doc = " ```"] pub fn iter_mut (& mut self) -> ValueIterMut < '_ , T > { self . map . value_iter_mut (self . index) } }
};
}
