// Generated macro for impl_384 (impl)
macro_rules! Depcrate_uri_authorityimpl_384 {
() => {
// Module: crate::uri::authority
// Provides: {"impl_384"}
// Dependencies: {}
impl Authority { pub (super) fn empty () -> Self { Authority { data : ByteStr :: new () , } } pub (super) fn from_shared (s : Bytes) -> Result < Self , InvalidUri > { create_authority (s , | s | s) } # [doc = " Attempt to convert an `Authority` from a static string."] # [doc = ""] # [doc = " This function will not perform any copying, and the string will be"] # [doc = " checked if it is empty or contains an invalid character."] # [doc = ""] # [doc = " # Panics"] # [doc = ""] # [doc = " This function panics if the argument contains invalid characters or"] # [doc = " is empty."] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::uri::Authority;"] # [doc = " let authority = Authority::from_static(\"example.com\");"] # [doc = " assert_eq!(authority.host(), \"example.com\");"] # [doc = " ```"] pub fn from_static (src : & 'static str) -> Self { Authority :: from_shared (Bytes :: from_static (src . as_bytes ())) . expect ("static str is not valid authority") } # [doc = " Attempt to convert a `Bytes` buffer to a `Authority`."] # [doc = ""] # [doc = " This will try to prevent a copy if the type passed is the type used"] # [doc = " internally, and will copy the data if it is not."] pub fn from_maybe_shared < T > (src : T) -> Result < Self , InvalidUri > where T : AsRef < [u8] > + 'static , { if_downcast_into ! (T , Bytes , src , { return Authority :: from_shared (src) ; }) ; Authority :: try_from (src . as_ref ()) } pub (super) fn parse (s : & [u8]) -> Result < usize , InvalidUri > { let mut colon_cnt = 0u32 ; let mut start_bracket = false ; let mut end_bracket = false ; let mut has_percent = false ; let mut end = s . len () ; let mut at_sign_pos = None ; const MAX_COLONS : u32 = 8 ; for (i , & b) in s . iter () . enumerate () { match URI_CHARS [b as usize] { b'/' | b'?' | b'#' => { end = i ; break ; } b':' => { if colon_cnt >= MAX_COLONS { return Err (ErrorKind :: InvalidAuthority . into ()) ; } colon_cnt += 1 ; } b'[' => { if has_percent || start_bracket { return Err (ErrorKind :: InvalidAuthority . into ()) ; } start_bracket = true ; } b']' => { if (! start_bracket) || end_bracket { return Err (ErrorKind :: InvalidAuthority . into ()) ; } end_bracket = true ; colon_cnt = 0 ; has_percent = false ; } b'@' => { at_sign_pos = Some (i) ; colon_cnt = 0 ; has_percent = false ; } 0 if b == b'%' => { has_percent = true ; } 0 => { return Err (ErrorKind :: InvalidUriChar . into ()) ; } _ => { } } } if start_bracket ^ end_bracket { return Err (ErrorKind :: InvalidAuthority . into ()) ; } if colon_cnt > 1 { return Err (ErrorKind :: InvalidAuthority . into ()) ; } if end > 0 && at_sign_pos == Some (end - 1) { return Err (ErrorKind :: InvalidAuthority . into ()) ; } if has_percent { return Err (ErrorKind :: InvalidAuthority . into ()) ; } Ok (end) } fn parse_non_empty (s : & [u8]) -> Result < usize , InvalidUri > { if s . is_empty () { return Err (ErrorKind :: Empty . into ()) ; } Authority :: parse (s) } # [doc = " Get the host of this `Authority`."] # [doc = ""] # [doc = " The host subcomponent of authority is identified by an IP literal"] # [doc = " encapsulated within square brackets, an IPv4 address in dotted- decimal"] # [doc = " form, or a registered name.  The host subcomponent is **case-insensitive**."] # [doc = ""] # [doc = " ```notrust"] # [doc = " abc://username:password@example.com:123/path/data?key=value&key2=value2#fragid1"] # [doc = "                         |---------|"] # [doc = "                              |"] # [doc = "                             host"] # [doc = " ```"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::uri::*;"] # [doc = " let authority: Authority = \"example.org:80\".parse().unwrap();"] # [doc = ""] # [doc = " assert_eq!(authority.host(), \"example.org\");"] # [doc = " ```"] # [inline] pub fn host (& self) -> & str { host (self . as_str ()) } # [doc = " Get the port part of this `Authority`."] # [doc = ""] # [doc = " The port subcomponent of authority is designated by an optional port"] # [doc = " number following the host and delimited from it by a single colon (\":\")"] # [doc = " character. It can be turned into a decimal port number with the `as_u16`"] # [doc = " method or as a `str` with the `as_str` method."] # [doc = ""] # [doc = " ```notrust"] # [doc = " abc://username:password@example.com:123/path/data?key=value&key2=value2#fragid1"] # [doc = "                                     |-|"] # [doc = "                                      |"] # [doc = "                                     port"] # [doc = " ```"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Authority with port"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::uri::Authority;"] # [doc = " let authority: Authority = \"example.org:80\".parse().unwrap();"] # [doc = ""] # [doc = " let port = authority.port().unwrap();"] # [doc = " assert_eq!(port.as_u16(), 80);"] # [doc = " assert_eq!(port.as_str(), \"80\");"] # [doc = " ```"] # [doc = ""] # [doc = " Authority without port"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::uri::Authority;"] # [doc = " let authority: Authority = \"example.org\".parse().unwrap();"] # [doc = ""] # [doc = " assert!(authority.port().is_none());"] # [doc = " ```"] pub fn port (& self) -> Option < Port < & str > > { let bytes = self . as_str () ; bytes . rfind (':') . and_then (| i | Port :: from_str (& bytes [i + 1 ..]) . ok ()) } # [doc = " Get the port of this `Authority` as a `u16`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::uri::Authority;"] # [doc = " let authority: Authority = \"example.org:80\".parse().unwrap();"] # [doc = ""] # [doc = " assert_eq!(authority.port_u16(), Some(80));"] # [doc = " ```"] pub fn port_u16 (& self) -> Option < u16 > { self . port () . map (| p | p . as_u16 ()) } # [doc = " Return a str representation of the authority"] # [inline] pub fn as_str (& self) -> & str { & self . data [..] } }
};
}
