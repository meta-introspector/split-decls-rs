// Generated macro for impl_630 (impl)
macro_rules! Depcrate_extensionsimpl_630 {
() => {
// Module: crate::extensions
// Provides: {"impl_630"}
// Dependencies: {}
impl Extensions { # [doc = " Create an empty `Extensions`."] # [inline] pub fn new () -> Extensions { Extensions { map : None } } # [doc = " Insert a type into this `Extensions`."] # [doc = ""] # [doc = " If a extension of this type already existed, it will"] # [doc = " be returned and replaced with the new one."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::Extensions;"] # [doc = " let mut ext = Extensions::new();"] # [doc = " assert!(ext.insert(5i32).is_none());"] # [doc = " assert!(ext.insert(4u8).is_none());"] # [doc = " assert_eq!(ext.insert(9i32), Some(5i32));"] # [doc = " ```"] pub fn insert < T : Clone + Send + Sync + 'static > (& mut self , val : T) -> Option < T > { self . map . get_or_insert_with (Box :: default) . insert (TypeId :: of :: < T > () , Box :: new (val)) . and_then (| boxed | boxed . into_any () . downcast () . ok () . map (| boxed | * boxed)) } # [doc = " Get a reference to a type previously inserted on this `Extensions`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::Extensions;"] # [doc = " let mut ext = Extensions::new();"] # [doc = " assert!(ext.get::<i32>().is_none());"] # [doc = " ext.insert(5i32);"] # [doc = ""] # [doc = " assert_eq!(ext.get::<i32>(), Some(&5i32));"] # [doc = " ```"] pub fn get < T : Send + Sync + 'static > (& self) -> Option < & T > { self . map . as_ref () . and_then (| map | map . get (& TypeId :: of :: < T > ())) . and_then (| boxed | (* * boxed) . as_any () . downcast_ref ()) } # [doc = " Get a mutable reference to a type previously inserted on this `Extensions`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::Extensions;"] # [doc = " let mut ext = Extensions::new();"] # [doc = " ext.insert(String::from(\"Hello\"));"] # [doc = " ext.get_mut::<String>().unwrap().push_str(\" World\");"] # [doc = ""] # [doc = " assert_eq!(ext.get::<String>().unwrap(), \"Hello World\");"] # [doc = " ```"] pub fn get_mut < T : Send + Sync + 'static > (& mut self) -> Option < & mut T > { self . map . as_mut () . and_then (| map | map . get_mut (& TypeId :: of :: < T > ())) . and_then (| boxed | (* * boxed) . as_any_mut () . downcast_mut ()) } # [doc = " Get a mutable reference to a type, inserting `value` if not already present on this"] # [doc = " `Extensions`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::Extensions;"] # [doc = " let mut ext = Extensions::new();"] # [doc = " *ext.get_or_insert(1i32) += 2;"] # [doc = ""] # [doc = " assert_eq!(*ext.get::<i32>().unwrap(), 3);"] # [doc = " ```"] pub fn get_or_insert < T : Clone + Send + Sync + 'static > (& mut self , value : T) -> & mut T { self . get_or_insert_with (| | value) } # [doc = " Get a mutable reference to a type, inserting the value created by `f` if not already present"] # [doc = " on this `Extensions`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::Extensions;"] # [doc = " let mut ext = Extensions::new();"] # [doc = " *ext.get_or_insert_with(|| 1i32) += 2;"] # [doc = ""] # [doc = " assert_eq!(*ext.get::<i32>().unwrap(), 3);"] # [doc = " ```"] pub fn get_or_insert_with < T : Clone + Send + Sync + 'static , F : FnOnce () -> T > (& mut self , f : F ,) -> & mut T { let out = self . map . get_or_insert_with (Box :: default) . entry (TypeId :: of :: < T > ()) . or_insert_with (| | Box :: new (f ())) ; (* * out) . as_any_mut () . downcast_mut () . unwrap () } # [doc = " Get a mutable reference to a type, inserting the type's default value if not already present"] # [doc = " on this `Extensions`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::Extensions;"] # [doc = " let mut ext = Extensions::new();"] # [doc = " *ext.get_or_insert_default::<i32>() += 2;"] # [doc = ""] # [doc = " assert_eq!(*ext.get::<i32>().unwrap(), 2);"] # [doc = " ```"] pub fn get_or_insert_default < T : Default + Clone + Send + Sync + 'static > (& mut self) -> & mut T { self . get_or_insert_with (T :: default) } # [doc = " Remove a type from this `Extensions`."] # [doc = ""] # [doc = " If a extension of this type existed, it will be returned."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::Extensions;"] # [doc = " let mut ext = Extensions::new();"] # [doc = " ext.insert(5i32);"] # [doc = " assert_eq!(ext.remove::<i32>(), Some(5i32));"] # [doc = " assert!(ext.get::<i32>().is_none());"] # [doc = " ```"] pub fn remove < T : Send + Sync + 'static > (& mut self) -> Option < T > { self . map . as_mut () . and_then (| map | map . remove (& TypeId :: of :: < T > ())) . and_then (| boxed | boxed . into_any () . downcast () . ok () . map (| boxed | * boxed)) } # [doc = " Clear the `Extensions` of all inserted extensions."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::Extensions;"] # [doc = " let mut ext = Extensions::new();"] # [doc = " ext.insert(5i32);"] # [doc = " ext.clear();"] # [doc = ""] # [doc = " assert!(ext.get::<i32>().is_none());"] # [doc = " ```"] # [inline] pub fn clear (& mut self) { if let Some (ref mut map) = self . map { map . clear () ; } } # [doc = " Check whether the extension set is empty or not."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::Extensions;"] # [doc = " let mut ext = Extensions::new();"] # [doc = " assert!(ext.is_empty());"] # [doc = " ext.insert(5i32);"] # [doc = " assert!(!ext.is_empty());"] # [doc = " ```"] # [inline] pub fn is_empty (& self) -> bool { self . map . as_ref () . map_or (true , | map | map . is_empty ()) } # [doc = " Get the number of extensions available."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::Extensions;"] # [doc = " let mut ext = Extensions::new();"] # [doc = " assert_eq!(ext.len(), 0);"] # [doc = " ext.insert(5i32);"] # [doc = " assert_eq!(ext.len(), 1);"] # [doc = " ```"] # [inline] pub fn len (& self) -> usize { self . map . as_ref () . map_or (0 , | map | map . len ()) } # [doc = " Extends `self` with another `Extensions`."] # [doc = ""] # [doc = " If an instance of a specific type exists in both, the one in `self` is overwritten with the"] # [doc = " one from `other`."] # [doc = ""] # [doc = " # Example"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::Extensions;"] # [doc = " let mut ext_a = Extensions::new();"] # [doc = " ext_a.insert(8u8);"] # [doc = " ext_a.insert(16u16);"] # [doc = ""] # [doc = " let mut ext_b = Extensions::new();"] # [doc = " ext_b.insert(4u8);"] # [doc = " ext_b.insert(\"hello\");"] # [doc = ""] # [doc = " ext_a.extend(ext_b);"] # [doc = " assert_eq!(ext_a.len(), 3);"] # [doc = " assert_eq!(ext_a.get::<u8>(), Some(&4u8));"] # [doc = " assert_eq!(ext_a.get::<u16>(), Some(&16u16));"] # [doc = " assert_eq!(ext_a.get::<&'static str>().copied(), Some(\"hello\"));"] # [doc = " ```"] pub fn extend (& mut self , other : Self) { if let Some (other) = other . map { if let Some (map) = & mut self . map { map . extend (* other) ; } else { self . map = Some (other) ; } } } }
};
}
