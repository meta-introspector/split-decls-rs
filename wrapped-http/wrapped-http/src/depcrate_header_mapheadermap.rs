// Generated macro for HeaderMap (struct)
macro_rules! Depcrate_header_mapHeaderMap {
() => {
// Module: crate::header::map
// Provides: {"HeaderMap"}
// Dependencies: {}
# [doc = " A specialized [multimap](<https://en.wikipedia.org/wiki/Multimap>) for"] # [doc = " header names and values."] # [doc = ""] # [doc = " # Overview"] # [doc = ""] # [doc = " `HeaderMap` is designed specifically for efficient manipulation of HTTP"] # [doc = " headers. It supports multiple values per header name and provides"] # [doc = " specialized APIs for insertion, retrieval, and iteration."] # [doc = ""] # [doc = " The internal implementation is optimized for common usage patterns in HTTP,"] # [doc = " and may change across versions. For example, the current implementation uses"] # [doc = " [Robin Hood"] # [doc = " hashing](<https://en.wikipedia.org/wiki/Hash_table#Robin_Hood_hashing>) to"] # [doc = " store entries compactly and enable high load factors with good performance."] # [doc = " However, the collision resolution strategy and storage mechanism are not"] # [doc = " part of the public API and may be altered in future releases."] # [doc = ""] # [doc = " # Iteration order"] # [doc = ""] # [doc = " Unless otherwise specified, the order in which items are returned by"] # [doc = " iterators from `HeaderMap` methods is arbitrary; there is no guaranteed"] # [doc = " ordering among the elements yielded by such an iterator. Changes to the"] # [doc = " iteration order are not considered breaking changes, so users must not rely"] # [doc = " on any incidental order produced by such an iterator. However, for a given"] # [doc = " crate version, the iteration order will be consistent across all platforms."] # [doc = ""] # [doc = " # Adaptive hashing"] # [doc = ""] # [doc = " `HeaderMap` uses an adaptive strategy for hashing to maintain fast lookups"] # [doc = " while resisting hash collision attacks. The default hash function"] # [doc = " prioritizes performance. In scenarios where high collision rates are"] # [doc = " detected—typically indicative of denial-of-service attacks—the"] # [doc = " implementation switches to a more secure, collision-resistant hash function."] # [doc = ""] # [doc = " # Limitations"] # [doc = ""] # [doc = " A `HeaderMap` can store at most 32,768 entries \\(header name/value pairs\\)."] # [doc = " Attempting to exceed this limit will result in a panic."] # [doc = ""] # [doc = " [`HeaderName`]: struct.HeaderName.html"] # [doc = " [`HeaderMap`]: struct.HeaderMap.html"] # [doc = ""] # [doc = " # Examples"] # [doc = ""] # [doc = " Basic usage"] # [doc = ""] # [doc = " ```"] # [doc = " # use http::HeaderMap;"] # [doc = " # use http::header::{CONTENT_LENGTH, HOST, LOCATION};"] # [doc = " let mut headers = HeaderMap::new();"] # [doc = ""] # [doc = " headers.insert(HOST, \"example.com\".parse().unwrap());"] # [doc = " headers.insert(CONTENT_LENGTH, \"123\".parse().unwrap());"] # [doc = ""] # [doc = " assert!(headers.contains_key(HOST));"] # [doc = " assert!(!headers.contains_key(LOCATION));"] # [doc = ""] # [doc = " assert_eq!(headers[HOST], \"example.com\");"] # [doc = ""] # [doc = " headers.remove(HOST);"] # [doc = ""] # [doc = " assert!(!headers.contains_key(HOST));"] # [doc = " ```"] # [derive (Clone)] pub struct HeaderMap < T = HeaderValue > { mask : Size , indices : Box < [Pos] > , entries : Vec < Bucket < T > > , extra_values : Vec < ExtraValue < T > > , danger : Danger , }
};
}
